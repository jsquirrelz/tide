// Synthetic stub for github.com/anthropics/anthropic-sdk-go used only by the
// providerfirewall analysistest fixtures. analysistest resolves imports via
// GOPATH-style lookups under testdata/src/, so both the violation fixture
// (violation/pkg/controller/forbidden.go) and the allowed fixture
// (valid/internal/subagent/anthropic/allowed.go) resolve this import here.
// The package has no exported symbols; the analyzer only inspects the import
// string in the AST — the package content is irrelevant.
package anthropic_sdk_go
