---
phase: 08-medium-sample-http-transport-and-production-git-transport-po
plan: 06
type: execute
wave: 3
depends_on: [08-03, 08-04]
files_modified:
  - examples/projects/medium/README.md
  - examples/projects/README.md
  - pkg/git/doc.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "examples/projects/medium/README.md contains no reference to 'controller mounts demo-remote-pvc' (false claim removed)"
    - "examples/projects/medium/README.md documents the 9-step apply sequence (namespace → PVC → per-namespace-resources → signing-key mirror → init Job wait → git-http-server → tide-secrets → project → wait)"
    - "examples/projects/README.md has no file:// references; small row shows https://git.example.internal sentinel; medium row shows in-cluster http:// transport"
    - "pkg/git/doc.go final form: http(s)/SSH pure-Go; file:// NOT a production transport; demo uses tide-demo-init + in-cluster http server"
    - "grep -n 'demo-remote-pvc' examples/projects/medium/README.md returns 0 false-claim lines (any remaining references are accurate)"
  artifacts:
    - path: "examples/projects/medium/README.md"
      provides: "Corrected medium sample README with 9-step apply sequence"
      contains: "git-http-server"
    - path: "examples/projects/README.md"
      provides: "Updated top-level samples index with post-Phase-8 transport descriptions"
      contains: "git.example.internal"
    - path: "pkg/git/doc.go"
      provides: "Final reframed package doc"
      contains: "file:// is NOT"
  key_links:
    - from: "examples/projects/medium/README.md"
      to: "examples/projects/medium/per-namespace-resources.yaml"
      via: "kubectl apply step 3 in README sequence"
      pattern: "per-namespace-resources.yaml"
    - from: "examples/projects/medium/README.md"
      to: "examples/projects/medium/git-http-server-deployment.yaml"
      via: "kubectl apply step 6 in README sequence"
      pattern: "git-http-server-deployment.yaml"
    - from: "examples/projects/README.md"
      to: "examples/projects/small/ and examples/projects/medium/"
      via: "cost-spectrum table and prose descriptions"
      pattern: "git.example.internal|in-cluster.*http"
---

<objective>
Wave 3 (parallel with 08-05, depends on 08-03 for CEL accuracy, 08-04 for tag consistency): Correct the medium sample README's false "controller mounts demo-remote-pvc" claim and rewrite the apply sequence to reflect the 9-step http:// workflow. Finalize pkg/git/doc.go with the complete production-transport policy statement.

Purpose: SC-4 requires removing the false architectural claim from the README and ensuring the docs accurately reflect what Phase 8 built. The 08-02 executor did a partial reframe of doc.go; this plan delivers the final version after the git-http server image and DNS name are confirmed in 08-05.

Note: This plan runs in parallel with 08-05 because it only touches README.md and doc.go (no file overlap with 08-05's Dockerfile, manifests, or Go source). The executor of this plan can start immediately after 08-03 and 08-04 commit.

Output:
- examples/projects/medium/README.md — rewritten apply sequence, false mount claim removed
- examples/projects/README.md — cost-spectrum table and prose updated for post-Phase-8 transport reality
- pkg/git/doc.go — final transport doc (complete after 08-02's partial reframe)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md

<interfaces>
<!-- Key reference points for the README rewrite -->

False claim to REMOVE (examples/projects/medium/README.md line 96 — verified in RESEARCH Sources section):
  Any statement that the controller mounts demo-remote-pvc alongside the init Job, or that the controller's clone Job mounts demo-remote-pvc.
  The correct architecture: the git-http-server Deployment mounts demo-remote-pvc and serves it over HTTP. The controller's clone Job clones from http://. No mount of demo-remote-pvc by the controller ever happens.

9-step apply sequence from RESEARCH §"Question D Revised apply sequence":
  1. kubectl apply namespace.yaml
  2. kubectl apply demo-remote-pvc.yaml
  3. kubectl apply per-namespace-resources.yaml
  4. Mirror signing-key Secret from tide-system (kubectl get secret + kubectl apply -)
  5. kubectl apply demo-remote-init-job.yaml && kubectl wait --for=condition=Complete (MUST complete before step 6)
  6. kubectl apply git-http-server-deployment.yaml && kubectl wait --for=condition=Available
  7. kubectl create secret tide-secrets ANTHROPIC_API_KEY + GIT_PAT="" (empty for anonymous http)
  8. kubectl apply project.yaml
  9. kubectl wait --for=jsonpath='{.status.phase}'=Complete project/medium-project -n tide-sample-medium --timeout=30m

False mount claim is at README line 96 per RESEARCH Sources section.

The README must also:
- Document the minikube image reload gotcha (minikube image load does NOT overwrite existing tags — must rmi -f first)
- Document the tide-projects PVC prewarm requirement for kind clusters (local-path WaitForFirstConsumer)
- Note that tide-signing-key is cluster-unique and must be mirrored from tide-system post-install

pkg/git/doc.go (final):
- Finalize the paragraph written in 08-02 Task 3
- Add reference to images/tide-git-http-server as the demo transport bridge
- Complete the "Transport support" section with the specific DNS name http://git-http-server.tide-sample-medium.svc.cluster.local/demo-remote.git as an example
- Remove any remaining references to tide-push or claude-subagent installing git (these no longer do)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite examples/projects/medium/README.md</name>
  <files>examples/projects/medium/README.md</files>
  <read_first>
    - examples/projects/medium/README.md — full file (identify all existing sections; the false-mount claim at line 96; the existing apply sequence to replace)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question D: Medium Sample Wiring — Per-Namespace Deps" (9-step sequence + the resource table + RWO pitfall)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Pitfall 3: demo-remote-pvc RWO Mount Conflict"
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Pitfall 4: Stale Minikube Image Tags After Revert"
  </read_first>
  <action>
Rewrite `examples/projects/medium/README.md`. The new README must contain:

Section 1 — Overview: Brief description of the medium sample. State that it uses an in-cluster HTTP git remote (git-http-server Deployment + Service) instead of a host filesystem path, so the demo exercises the same pure-Go HTTP transport production uses. Note the ~$5 real Claude (Haiku) cost with the $5 hard cap in project.yaml.

Section 2 — Architecture: Describe the three-component flow: (a) demo-remote-init Job populates the bare repo on demo-remote-pvc via file:// internal to the pod; (b) git-http-server Deployment mounts demo-remote-pvc and serves it at http://git-http-server/demo-remote.git; (c) controller's clone and push Jobs use go-git HTTP transport (pure-Go, no git binary in those images). EXPLICITLY STATE: "The controller's clone and push Jobs do NOT mount demo-remote-pvc — they reach the repo via HTTP through the git-http-server Service." This is the false claim correction.

Section 3 — Prerequisites: TIDE installed via helm install (cert-manager pre-req), kubectl + minikube or kind, docker for building the git-http-server image, ANTHROPIC_API_KEY set in environment.

Section 4 — Apply Sequence (9 steps): Document the exact 9-step kubectl sequence from RESEARCH §"Question D". Each step must include the actual kubectl command and an explanation of why the step order matters. For step 5 (wait for init Job): explain RWO PVC conflict (Pitfall 3) — if the init Job and git-http server try to mount the same RWO PVC simultaneously, the second pod will stay Pending. For step 4 (signing-key mirror): include the exact kubectl get secret + kubectl apply - heredoc. For step 7: show GIT_PAT="" (empty) is correct for anonymous in-cluster http:// push.

Section 5 — Minikube-Specific Notes: Include the minikube image reload gotcha (minikube image load does NOT overwrite existing tags — use `minikube ssh -- docker rmi -f ghcr.io/jsquirrelz/tide-git-http-server:1.0.0` before `minikube image load`). Include the tide-projects PVC prewarm requirement for kind clusters using local-path provisioner.

Section 6 — Verification: After step 9, list verification commands: kubectl describe project/medium-project -n tide-sample-medium; kubectl get jobs -n tide-sample-medium; kubectl logs job/demo-remote-init -n tide-sample-medium.

Remove ALL references to the old apply sequence (the 5-step sequence that was false). Remove ALL references to the controller mounting demo-remote-pvc. Verify with `grep -n 'controller.*mounts\|mounts.*demo-remote-pvc' examples/projects/medium/README.md` → 0 results.
  </action>
  <verify>
    <automated>grep -n 'controller.*mounts\|mounts.*demo-remote-pvc\|demo-remote-pvc.*mount' /Users/justinsearles/Projects/tide/examples/projects/medium/README.md && echo FAIL_FALSE_CLAIM || echo CLAIM_REMOVED && grep -n 'git-http-server-deployment.yaml' /Users/justinsearles/Projects/tide/examples/projects/medium/README.md && echo SEQUENCE_STEP_PRESENT</automated>
  </verify>
  <done>README has no false "controller mounts demo-remote-pvc" claim; 9-step apply sequence documented; git-http-server-deployment.yaml referenced in step 6; per-namespace-resources.yaml referenced in step 3</done>
</task>

<task type="auto">
  <name>Task 2: Finalize pkg/git/doc.go transport policy statement</name>
  <files>pkg/git/doc.go</files>
  <read_first>
    - pkg/git/doc.go — full file (read the partial reframe from 08-02 Task 3; this task completes it)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"State of the Art" table row for pkg/git/doc.go
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Standard Stack" (go-git HTTP transport is pure-Go)
  </read_first>
  <action>
Finalize the "Transport support" paragraph in pkg/git/doc.go that was partially written in 08-02. The final paragraph must state:

1. HTTPS and SSH transports via go-git/v5 are pure-Go — no system git binary required. Runtime images (tide-push, claude-subagent) run distroless/slim with no git binary, confirming this.

2. file:// is NOT a supported production transport: go-git's file:// transport shells out to git-upload-pack / git-receive-pack at runtime. Use http(s) or SSH for all production targetRepo values. The CEL validator on ProjectSpec.targetRepo enforces this at admission (rejects file:// URLs).

3. The medium demo sample uses an in-cluster HTTP git server (images/tide-git-http-server, a git-http-backend CGI fronted by nginx) to serve its fixture repository. The demo exercises the same pure-Go HTTP transport as production HTTPS — the difference is the server is anonymous and cluster-local rather than an authenticated public host. The init Job (images/tide-demo-init) still uses file:// internally to populate the bare repo on the PVC, which is why it retains a git binary; the server then bridges that repo to the HTTP transport layer.

4. Keep the import firewall note unchanged.

Remove any remaining text that says tide-push or claude-subagent "install git" — these images are now git-less again (per 08-02 revert).

Run go build ./pkg/git/... to confirm the doc change doesn't introduce any syntax errors (doc comments must remain valid Go documentation).
  </action>
  <verify>
    <automated>grep -n "file:// is NOT" /Users/justinsearles/Projects/tide/pkg/git/doc.go && grep -n "tide-git-http-server" /Users/justinsearles/Projects/tide/pkg/git/doc.go && go build /Users/justinsearles/Projects/tide/pkg/git/... 2>&1 | grep -c 'error' | grep '^0$' && echo PASS</automated>
  </verify>
  <done>pkg/git/doc.go contains "file:// is NOT" and references tide-git-http-server; go build ./pkg/git/... exits 0; no references to tide-push or claude-subagent installing git remain</done>
</task>

<task type="auto">
  <name>Task 3: Update examples/projects/README.md (top-level samples index)</name>
  <files>examples/projects/README.md</files>
  <read_first>
    - examples/projects/README.md — full file (read actual line content before editing; lines ~25, ~26, ~54 reference file:// for small/medium transport — verify exact text by reading)
  </read_first>
  <action>
In `examples/projects/README.md`, update three stale references that describe the pre-Phase-8 transport situation:

1. Cost spectrum table — small row "Target" column: change "placeholder `file://` path (ignored by stub)" to "non-routable `https://git.example.internal` sentinel (ignored by stub)" (RFC 2606 .example TLD, not file://).

2. Cost spectrum table — medium row "Target" column: change "local-only `file://` git remote (in-cluster init)" to "in-cluster `http://` git remote (git-http-server Service)".

3. Prerequisites table — medium row "git push creds Secret" column: change "no (file:// is local)" to "no (anonymous in-cluster http://)". This clarifies that Phase 8's medium sample uses real http transport to an anonymous in-cluster server, not a local filesystem path.

4. The prose paragraph below the cost-spectrum table ("small is the make dry-run-v1 target…") references "the placeholder `file:///tmp/no-such-repo` value is load-bearing-by-virtue-of-being-ignored": update this sentence to reference the RFC 2606 sentinel `https://git.example.internal/stub/no-such-repo.git` instead of the file:// path. Keep the rest of the sentence intact (the smoke test reasoning and K8s plumbing description are still correct).

5. The prose paragraph about medium ("medium is the first taste of real LLM-driven planning…") describes "bootstraps a fresh local-only git remote from examples/tide-demo-fixture/" and says "all contained on the operator's own machine. Zero external dependencies." Update this paragraph to reflect Phase 8 reality: the in-cluster HTTP git server (git-http-server Deployment) serves the repo over http:// inside the cluster — still zero external dependencies, but now using the same pure-Go HTTP transport production uses. Remove "contained on the operator's own machine" (it runs in the cluster). Keep the cmd/tide-demo-init reference accurate.

Verify exact line numbers and content before editing — do NOT hardcode line numbers from the checker description; read the file first to confirm. Do NOT edit the large sample description, the A note on namespaces section, or any section beyond the five bullet points above.
  </action>
  <verify>
    <automated>! grep -q 'file://' /Users/justinsearles/Projects/tide/examples/projects/README.md && grep -q 'git.example.internal' /Users/justinsearles/Projects/tide/examples/projects/README.md && grep -qE 'in-cluster.*http' /Users/justinsearles/Projects/tide/examples/projects/README.md && echo PASS || { echo FAIL; false; }</automated>
  </verify>
  <done>examples/projects/README.md has no remaining file:// references; small row references the https://git.example.internal sentinel; medium row describes in-cluster http:// transport; prerequisites table medium row says "no (anonymous in-cluster http://)"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator understanding | Incorrect docs lead to misapplication (false mount claim caused the original bug) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Information Disclosure | README signing-key mirror step (heredoc with SIGNING_KEY var) | accept | The README shows the pattern using a shell variable — the actual key is not hardcoded. Operators substitute their own key. Low risk. |
| T-08-06-02 | Tampering | Stale docs leading operators to apply wrong sequence | mitigate | README completely rewritten; false mount claim explicitly removed; SC-4 verification asserts with grep |
| T-08-06-03 | Tampering | Stale top-level samples index misleading new operators about transport | mitigate | examples/projects/README.md updated in Task 3 to reflect post-Phase-8 sentinel (small) and http:// (medium); grep gate in verify confirms no file:// remains |
</threat_model>

<verification>
1. `grep -n 'controller.*mounts\|mounts.*demo-remote-pvc' examples/projects/medium/README.md` → 0 results (false claim removed)
2. `grep -n 'git-http-server-deployment.yaml' examples/projects/medium/README.md` → 1+ results (documented in sequence)
3. `grep -n 'per-namespace-resources.yaml' examples/projects/medium/README.md` → 1+ results
4. `grep -n 'file://' examples/projects/README.md` → 0 results (no stale file:// references in samples index)
5. `grep -n 'git.example.internal' examples/projects/README.md` → 1+ results (small sentinel updated)
6. `grep -n 'in-cluster.*http' examples/projects/README.md` → 1+ results (medium transport updated)
7. `grep -n 'file:// is NOT' pkg/git/doc.go` → 1 result
8. `grep -n 'tide-git-http-server' pkg/git/doc.go` → 1 result
9. `go build ./pkg/git/...` → exit 0
</verification>

<success_criteria>
- examples/projects/medium/README.md has no false "controller mounts demo-remote-pvc" claim
- README documents 9-step apply sequence referencing per-namespace-resources.yaml and git-http-server-deployment.yaml
- examples/projects/README.md has no remaining file:// references; small row shows https://git.example.internal sentinel; medium row shows in-cluster http:// transport
- pkg/git/doc.go final: http(s)/SSH pure-Go; file:// not a production transport; in-cluster http server described
- go build ./pkg/git/... exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-06-SUMMARY.md`
</output>
