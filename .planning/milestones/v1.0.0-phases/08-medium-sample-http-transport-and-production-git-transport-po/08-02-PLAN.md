---
phase: 08-medium-sample-http-transport-and-production-git-transport-po
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - images/tide-push/Dockerfile
  - images/claude-subagent/Dockerfile
  - pkg/git/doc.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "images/tide-push Dockerfile base is FROM gcr.io/distroless/static:nonroot (no apk/apt git layer)"
    - "images/claude-subagent Dockerfile has no apt-get install git RUN layer"
    - "pkg/git/doc.go states http(s)/SSH are pure-Go; file:// is NOT a supported production transport"
    - "Both Dockerfiles retain the non-root UID 1000 USER directive"
    - "images/tide-demo-init Dockerfile is UNCHANGED (still alpine:3.21 + apk add git)"
  artifacts:
    - path: "images/tide-push/Dockerfile"
      provides: "git-less distroless tide-push runtime image"
      contains: "distroless/static:nonroot"
    - path: "images/claude-subagent/Dockerfile"
      provides: "git-less node:22-slim claude-subagent runtime image"
    - path: "pkg/git/doc.go"
      provides: "updated package doc: http(s)/SSH supported; file:// unsupported"
      contains: "file:// is NOT"
  key_links:
    - from: "images/tide-push/Dockerfile"
      to: "SC-1 verification"
      via: "docker build + docker run --entrypoint which git"
      pattern: "distroless/static:nonroot"
---

<objective>
Wave 1a (parallel with 08-01): Revert the core-image git additions from commit 93595b9 — restore `images/tide-push` and `images/claude-subagent` to git-less distroless/slim bases. Also do a partial reframe of `pkg/git/doc.go` to reflect the production-only transport decision (the full doc.go reframe will be finalized in 08-06 alongside the medium README).

Purpose: Commit 93595b9 added git to tide-push and claude-subagent as a workaround for the file:// transport requirement. Phase 8 removes the file:// demo path and replaces it with an in-cluster HTTP server. The two core images must go back to distroless/slim (smaller attack surface, no git binary surface area). tide-demo-init KEEPS its alpine+git base — the init Job still uses file:// push internally within the pod.

Output:
- `images/tide-push/Dockerfile` — reverted to FROM gcr.io/distroless/static:nonroot; git layer removed; comment updated
- `images/claude-subagent/Dockerfile` — apt-get git layer removed; comment updated (still node:22-slim base)
- `pkg/git/doc.go` — transport doc updated (http(s)/SSH pure-Go supported; file:// NOT supported as production transport)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md

<interfaces>
<!-- Exact revert surface from 08-RESEARCH.md §"Question C: What 93595b9 Actually Changed" -->

images/tide-push/Dockerfile — BEFORE 93595b9 (the target state):
  FROM gcr.io/distroless/static:nonroot
  (no RUN apk add git; no adduser; USER not needed — distroless/static:nonroot runs as nonroot by default)
  The binary is copied in; ENTRYPOINT set to /usr/local/bin/tide-push.

  CURRENT (post-93595b9, to be reverted):
  FROM alpine:3.21
  RUN apk add --no-cache git && adduser -D -u 1000 nonroot
  USER 1000

images/claude-subagent/Dockerfile — the git layer to REMOVE (apt-get):
  CURRENT lines 47-51:
  RUN --mount=type=cache,target=/var/cache/apt \
      apt-get update \
      && apt-get install -y --no-install-recommends git \
      && rm -rf /var/lib/apt/lists/*
  (Remove this block; node:22-slim is a Debian-based image — no apk. The base stays node:22-slim.)

images/tide-demo-init/Dockerfile — DO NOT TOUCH (stays alpine:3.21 + apk add git).

pkg/git/doc.go — the "Transport dependency" paragraph to update (currently explains that file:// shells out to git binary and that tide-demo-init, tide-push, claude-subagent install git for that reason). New framing: HTTP(S) and SSH transports are pure-Go (no git binary needed in runtime images); file:// is NOT a supported production transport; the demo (tide-demo-init) uses file:// internally within the init pod and installs git for that purpose.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert images/tide-push/Dockerfile to distroless/static:nonroot</name>
  <files>images/tide-push/Dockerfile</files>
  <read_first>
    - images/tide-push/Dockerfile — full file (current post-93595b9 state with alpine:3.21 base and apk add git)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question C: What 93595b9 Actually Changed" row for images/tide-push/Dockerfile
  </read_first>
  <action>
Replace the runtime stage (Stage 2) of `images/tide-push/Dockerfile`. The target state for Stage 2:

- Base image: `gcr.io/distroless/static:nonroot` (exactly as it was pre-93595b9). Distroless static:nonroot provides a minimal read-only filesystem with no shell, no package manager, and runs as UID 65532 (the nonroot user). No RUN layer needed — no apk, no adduser.
- Remove the `RUN apk add --no-cache git && adduser -D -u 1000 nonroot` line entirely.
- Remove `USER 1000` — distroless/static:nonroot defaults to nonroot user; USER directive is redundant and adds noise.
- COPY --from=builder /out/tide-push /usr/local/bin/tide-push remains unchanged.
- ENTRYPOINT ["/usr/local/bin/tide-push"] remains unchanged.

Update the Stage 2 comment block. The new comment must:
1. State that tide-push uses go-git/v5's HTTPS and SSH transports, which are pure-Go (no system git binary needed).
2. State that file:// transport is NOT used by tide-push in Phase 8+ (production transport = http(s)/SSH only).
3. Reference the git-http server (images/tide-git-http-server) as the demo-only transport bridge.
4. Remove all references to the 93595b9 debug session and the old file:// rationale.

Also update the Stage 1 comment that currently says "tide-push pushes over whatever remote the Project declares, which for the medium sample is a file:// bare repo on the shared PVC" — update to reflect that tide-push now ONLY pushes over http(s) or SSH remotes. The medium sample's remote is an in-cluster HTTP git server.

Do NOT change the builder stage (Stage 1). The binary build command, COPY directives for pkg/git, internal/gitleaks, cmd/tide-push, and go.mod/go.sum caching are all correct and unchanged.
  </action>
  <verify>
    <automated>grep -q 'distroless/static:nonroot' /Users/justinsearles/Projects/tide/images/tide-push/Dockerfile && [ "$(grep -c 'apk add' /Users/justinsearles/Projects/tide/images/tide-push/Dockerfile)" = "0" ] && echo PASS || { echo FAIL; false; }</automated>
  </verify>
  <done>images/tide-push/Dockerfile Stage 2 FROM line is FROM gcr.io/distroless/static:nonroot; no RUN apk add git line exists; file compiles (docker build --no-cache would succeed — executor should verify with a local docker build if available)</done>
</task>

<task type="auto">
  <name>Task 2: Remove git layer from images/claude-subagent/Dockerfile</name>
  <files>images/claude-subagent/Dockerfile</files>
  <read_first>
    - images/claude-subagent/Dockerfile — full file (current post-93595b9 state; the apt-get git block is lines 47-51)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question C: What 93595b9 Actually Changed" row for images/claude-subagent/Dockerfile
  </read_first>
  <action>
Remove the `RUN --mount=type=cache,target=/var/cache/apt apt-get update && apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*` block from the claude-subagent Dockerfile. This is the only change to Stage 2 — the node:22-slim base stays; npm install @anthropic-ai/claude-code@2.1.142 stays; COPY --from=builder stays; USER 1000 stays; ENTRYPOINT stays.

Update the Stage 2 comment block that currently explains why git is needed (file:// transport). New framing: claude-subagent uses go-git's HTTP transport (pure-Go) for any git operations; no system git binary is needed in the runtime image. The in-cluster HTTP remote (git-http server) is the transport bridge for the demo. Remove the reference to the 93595b9 debug session.

The file's overall structure stays the same: builder (golang:1.26-alpine) + runtime (node:22-slim). Only the git RUN layer and its surrounding comment change.

NOTE: USER 1000 in node:22-slim requires a pre-existing UID. node:22-slim ships with UID 1000 as "node" user — this is correct and unchanged. Do not add or remove the USER directive.
  </action>
  <verify>
    <automated>[ "$(grep -c 'apt-get install.*git' /Users/justinsearles/Projects/tide/images/claude-subagent/Dockerfile)" = "0" ] && echo PASS || { echo FAIL; false; }</automated>
  </verify>
  <done>images/claude-subagent/Dockerfile has no apt-get install git line; the file is syntactically valid Dockerfile; node:22-slim base is retained</done>
</task>

<task type="auto">
  <name>Task 3: Reframe pkg/git/doc.go for production-only transport</name>
  <files>pkg/git/doc.go</files>
  <read_first>
    - pkg/git/doc.go — full file (the full package comment; the "Transport dependency on a system git binary" paragraph is the key section to reframe)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question C: What 93595b9 Actually Changed" (doc.go row — "PARTIAL REVERT: keep the fact, reframe for http-only context")
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"State of the Art" table row for pkg/git/doc.go
  </read_first>
  <action>
Replace the "Transport dependency on a system git binary" paragraph in pkg/git/doc.go with a new paragraph titled "Transport support". New content (directive prose, not code):

The paragraph must state:
1. HTTPS and SSH transports via go-git/v5 are pure-Go — no system git binary required in any runtime image.
2. file:// is NOT a supported production transport: go-git's file:// transport shells out to git-upload-pack / git-receive-pack at runtime. Production images (tide-push, claude-subagent) do not ship git binaries. file:// is used only internally by images/tide-demo-init, which does carry a git binary for that purpose.
3. The medium sample's demo remote is served over http:// via the in-cluster git-http-backend server (images/tide-git-http-server), which exercises the same pure-Go HTTP transport as production HTTPS.
4. Preserve the import firewall note at the end of the doc comment (this package MUST NOT import LLM SDKs or CRD API types).

Keep all other paragraphs in the doc comment intact: the provider-agnostic opening, HTTPS+PAT description, ART-03 note, ART-06 note, import firewall note.

This is a doc.go partial reframe — the full final version is in 08-06 alongside the medium README update. The 08-06 executor will finalize any remaining wording after the git-http server image exists and its DNS name is confirmed.
  </action>
  <verify>
    <automated>grep -n 'file:// is NOT' /Users/justinsearles/Projects/tide/pkg/git/doc.go && grep -n 'distroless\|tide-demo-init' /Users/justinsearles/Projects/tide/pkg/git/doc.go | head -5 && go build /Users/justinsearles/Projects/tide/pkg/git/... 2>&1 | grep -c 'error' | grep -E '^0$' && echo PASS</automated>
  </verify>
  <done>pkg/git/doc.go contains "file:// is NOT" in the transport paragraph; the file compiles (go build ./pkg/git/... exits 0); references to tide-demo-init explain it is the demo-only exception</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container image → host | Removing git from distroless images shrinks attack surface; no shell binary means no interactive exploitation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Elevation of Privilege | tide-push image (git binary present post-93595b9) | mitigate | Revert to distroless/static:nonroot — no git, no shell, no package manager; attack surface minimized |
| T-08-02-02 | Elevation of Privilege | claude-subagent image (git binary present post-93595b9) | mitigate | Remove apt-get git layer; subagent clones over HTTP (pure-Go); no system git binary in node:22-slim |
| T-08-02-03 | Information Disclosure | pkg/git/doc.go stating incorrect transport policy | accept | Doc-only file; no runtime impact; low risk |
</threat_model>

<verification>
1. `grep -n 'distroless/static:nonroot' images/tide-push/Dockerfile` → 1 result (Stage 2 FROM line)
2. `grep -c 'apk add' images/tide-push/Dockerfile` → 0 (no apk add git)
3. `grep -c 'apt-get install.*git' images/claude-subagent/Dockerfile` → 0
4. `grep -n 'file:// is NOT' pkg/git/doc.go` → 1 result
5. `go build ./pkg/git/...` → exit 0
6. `cat images/tide-demo-init/Dockerfile | grep -c 'apk add.*git'` → 1 (demo-init untouched)
</verification>

<success_criteria>
- images/tide-push/Dockerfile Stage 2 FROM is gcr.io/distroless/static:nonroot with no git layer
- images/claude-subagent/Dockerfile has no apt-get install git
- images/tide-demo-init/Dockerfile is UNCHANGED (still alpine:3.21 + apk add git)
- pkg/git/doc.go updated to reflect http(s)/SSH supported; file:// NOT a production transport
- go build ./pkg/git/... exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-02-SUMMARY.md`
</output>
