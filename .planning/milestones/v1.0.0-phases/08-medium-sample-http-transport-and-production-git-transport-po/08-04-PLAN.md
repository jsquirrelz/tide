---
phase: 08-medium-sample-http-transport-and-production-git-transport-po
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - examples/projects/medium/demo-remote-init-job.yaml
  - examples/projects/medium/project.yaml
  - examples/projects/large/project.yaml
  - examples/projects/small/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "grep -rn ':v1.' examples/ | grep image: returns 0 results (no v-prefixed tags)"
    - "examples/projects/medium/demo-remote-init-job.yaml uses :1.0.0 image tag"
    - "examples/projects/medium/project.yaml uses :1.0.0 image tag"
    - "examples/projects/large/project.yaml uses :1.0.0 image tag"
    - "examples/projects/small/README.md uses :1.0.0 image tag"
  artifacts:
    - path: "examples/projects/medium/demo-remote-init-job.yaml"
      provides: "demo-init Job manifest with aligned tag"
      contains: "tide-demo-init:1.0.0"
    - path: "examples/projects/medium/project.yaml"
      provides: "medium Project manifest with aligned tag"
      contains: "tide-claude-subagent:1.0.0"
    - path: "examples/projects/large/project.yaml"
      provides: "large Project manifest with aligned tag"
      contains: "tide-claude-subagent:1.0.0"
    - path: "examples/projects/small/README.md"
      provides: "small README with consistent stub-subagent tag"
      contains: "tide-stub-subagent:1.0.0"
  key_links:
    - from: "examples/projects/*/project.yaml"
      to: "hack/helm/tide-chart.yaml appVersion"
      via: "canonical no-v 1.0.0 tag"
      pattern: ":1.0.0"
---

<objective>
Wave 2 (parallel with 08-03, depends on 08-01 for file ordering clarity): Align all sample and README image tags from the v-prefix form `:v1.0.0` to the canonical no-v form `:1.0.0` that matches the chart SOT (hack/helm/tide-chart.yaml appVersion: "1.0.0") and the acceptance/load scripts.

Purpose: RESEARCH Question F identified 5 locations with v-prefix tags that diverge from the canonical no-v form used by the chart appVersion, acceptance-v1.sh, and load-images-if-needed.sh. This is a cosmetic but ship-blocking inconsistency — minikube reload requires tag-exact matches, and operators following the docs will pull the wrong digest.

Note: examples/projects/medium/project.yaml will be substantially rewritten in 08-05 (targetRepo change + new git.repoURL). This plan only fixes the image tag on line 90. The 08-05 executor will see this plan's commit as a prior context and must not regress the tag alignment when it rewrites the project.yaml.

Output:
- 4 files with :v1.0.0 → :1.0.0 (no-v) tag corrections
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md

<interfaces>
<!-- From 08-RESEARCH.md §"Question F: Image Tag Alignment" -->

Files requiring change (v-prefix → no-v):
1. examples/projects/medium/demo-remote-init-job.yaml line 42:
   BEFORE: ghcr.io/jsquirrelz/tide-demo-init:v1.0.0
   AFTER:  ghcr.io/jsquirrelz/tide-demo-init:1.0.0

2. examples/projects/medium/project.yaml line 90:
   BEFORE: ghcr.io/jsquirrelz/tide-claude-subagent:v1.0.0
   AFTER:  ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0

3. examples/projects/large/project.yaml line 105:
   BEFORE: ghcr.io/jsquirrelz/tide-claude-subagent:v1.0.0
   AFTER:  ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0

4. examples/projects/small/README.md lines 130-131:
   BEFORE: ghcr.io/jsquirrelz/tide-stub-subagent:v1.0.0
   AFTER:  ghcr.io/jsquirrelz/tide-stub-subagent:1.0.0
   (README was inconsistent with small/project.yaml which already uses :1.0.0 no-v)

Already correct (DO NOT CHANGE):
- examples/projects/small/project.yaml line 50 — already uses :1.0.0 no-v
- hack/helm/tide-chart.yaml — appVersion: "1.0.0" (SOT, no-v)
- hack/scripts/* — already use 1.0.0 no-v

The new git-http-server-deployment.yaml (created in 08-05) must also use :1.0.0 when it creates the git-http-server image reference. The 08-05 executor is responsible for that; this plan is only the 4 existing files listed above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align image tags in medium and large sample manifests</name>
  <files>examples/projects/medium/demo-remote-init-job.yaml, examples/projects/medium/project.yaml, examples/projects/large/project.yaml, examples/projects/small/README.md</files>
  <read_first>
    - examples/projects/medium/demo-remote-init-job.yaml — lines 38-46 (the Job's container spec with the tide-demo-init image)
    - examples/projects/medium/project.yaml — lines 85-95 (the subagent.image field)
    - examples/projects/large/project.yaml — lines 100-110 (the subagent.image field)
    - examples/projects/small/README.md — lines 125-135 (the stub-subagent image example)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question F: Image Tag Alignment"
  </read_first>
  <action>
Make exactly four targeted tag substitutions — nothing else changes in these files:

1. `examples/projects/medium/demo-remote-init-job.yaml` line 42: change `ghcr.io/jsquirrelz/tide-demo-init:v1.0.0` to `ghcr.io/jsquirrelz/tide-demo-init:1.0.0`. Update the comment on the line above if it references the v-prefix form.

2. `examples/projects/medium/project.yaml`: find the `subagent.image:` field containing `tide-claude-subagent:v1.0.0` and change the tag to `1.0.0` (no-v). Do NOT change targetRepo, git.repoURL, or any other field — those are handled by 08-05. Only the image tag changes.

3. `examples/projects/large/project.yaml`: find the `subagent.image:` field containing `tide-claude-subagent:v1.0.0` and change the tag to `1.0.0` (no-v). No other changes.

4. `examples/projects/small/README.md`: find all occurrences of `tide-stub-subagent:v1.0.0` and change to `tide-stub-subagent:1.0.0`. If there are code examples in the README that show docker pull or kubectl apply commands with the v-prefix tag, update those too. Add a one-line comment near the updated lines: "Canonical form: no-v prefix, matching chart appVersion 1.0.0".

After edits, run the SC-6 verification command:
`grep -rn ':v1\.' examples/ | grep image:` must return 0 results.
  </action>
  <verify>
    <automated>! { grep -rn ':v1\.' /Users/justinsearles/Projects/tide/examples/ | grep -q 'image:'; } && echo PASS || { echo FAIL_V_PREFIX_FOUND; false; }</automated>
  </verify>
  <done>grep -rn ':v1.' examples/ | grep image: returns 0 results; all four target files updated; examples/projects/small/project.yaml remains at :1.0.0 (no-v, untouched)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sample manifests | Operators apply samples directly; wrong tags cause ImagePullBackOff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Image tag inconsistency (v-prefix vs no-v) | mitigate | Align all tags to canonical no-v form matching chart appVersion SOT; reduces risk of operators loading wrong digest |
</threat_model>

<verification>
1. `grep -rn ':v1\.' examples/ | grep image:` → 0 results (SC-6 gate passes)
2. `grep -n 'tide-demo-init:1.0.0' examples/projects/medium/demo-remote-init-job.yaml` → 1 result
3. `grep -n 'tide-claude-subagent:1.0.0' examples/projects/medium/project.yaml` → 1 result
4. `grep -n 'tide-claude-subagent:1.0.0' examples/projects/large/project.yaml` → 1 result
5. `grep -n 'tide-stub-subagent:1.0.0' examples/projects/small/README.md` → at least 1 result
</verification>

<success_criteria>
- All :v1.0.0 image tags in examples/ replaced with :1.0.0 (no-v)
- grep -rn ':v1.' examples/ | grep image: returns 0 results
- No other fields changed in any of the 4 files
</success_criteria>

<output>
After completion, create `.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-04-SUMMARY.md`
</output>
