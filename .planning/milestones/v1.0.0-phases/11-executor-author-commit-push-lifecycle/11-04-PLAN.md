---
phase: 11-executor-author-commit-push-lifecycle
plan: "04"
type: execute
wave: 3
depends_on:
  - "11-03"
autonomous: false
files_modified: []
requirements: []

user_setup:
  - service: minikube
    why: "DoD re-run requires a live minikube cluster with the medium sample and real Claude key"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic console → API Keys"

must_haves:
  truths:
    - "All three images are rebuilt and loaded into minikube with B3–B6 changes (tide-controller, tide-push, claude-subagent) — per Pitfall 7"
    - "The medium sample runs to Project status.phase=Complete with all descendants Succeeded (SC-6)"
    - "The tide/run-* branch is pushed to the in-cluster http:// remote with real authored code (SC-6)"
    - "costSpentCents > 0 under the project cap (SC-6 / Phase 8 SC-2 DEFERRED→PASS)"
    - "10-VERIFICATION.md gate_decision updated from BLOCKED to APPROVED (Phase 8 SC-2 flip) — or a new 11-VERIFICATION.md captures it"
  artifacts:
    - path: ".planning/phases/11-executor-author-commit-push-lifecycle/11-VERIFICATION.md"
      provides: "gate_decision, evidence of pushed branch, costSpentCents>0, image digests"
  key_links:
    - from: "minikube cluster"
      to: "in-cluster http:// git remote"
      via: "tide/run-* branch pushed via tide-push --mode=push after task executor commits"
---

<objective>
DoD re-run: rebuild all three images with the B3–B6 changes, reload into minikube, run the medium sample, and capture the gate artifact.

Purpose: This is the final gate that closes Phase 8 SC-2 (DEFERRED→PASS) and unblocks the v1.0.0 retag. It requires a live minikube cluster and a real ANTHROPIC_API_KEY.

Output: 11-VERIFICATION.md with gate_decision, pushed branch evidence, cost evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-executor-author-commit-push-lifecycle/11-CONTEXT.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-VERIFICATION.md
@.planning/phases/11-executor-author-commit-push-lifecycle/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full unit test suite to confirm B3–B6 foundation before image rebuild</name>
  <files></files>
  <action>
    Before touching the cluster, confirm the unit test suite is green with all B3–B6 changes:

    Run: `go test ./pkg/git/... ./internal/harness/... ./cmd/claude-subagent/... ./cmd/tide-push/... ./internal/controller/... -count=1 -timeout 60s`

    Per CLAUDE.md lesson: read MAKE_EXIT explicitly. If any package is red, do not proceed to image rebuild — fix the failing tests first (the root of whatever broke them; do not bypass with skip or build tags).

    Also run: `make test` to catch any formatter/linter issues.

    If all green, proceed to Task 2.
  </action>
  <verify>
    <automated>go test ./pkg/git/... ./internal/harness/... ./cmd/claude-subagent/... ./cmd/tide-push/... ./internal/controller/... -count=1 -timeout 60s 2>&1 | grep -E "^(ok|FAIL|---)" && echo "SUITE_EXIT=$?"</automated>
  </verify>
  <done>All packages show `ok`. No `FAIL` lines. SUITE_EXIT=0.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild and reload tide-controller, tide-push, claude-subagent images into minikube</name>
  <files></files>
  <action>
    The in-cluster claude-subagent image predates B2 (f639340) per 10-VERIFICATION.md: "rebuild claude-subagent before the next run." All three images must be rebuilt.

    Consult Makefile for image build targets:
    `grep -nE '^(docker-build|build-images|image|controller-image|push-image|subagent-image):' Makefile | head -20`

    Use whatever targets exist. If there is a combined `make docker-build` or `make build-images`, use that. Otherwise build individually:
    `docker build -t ghcr.io/jsquirrelz/tide-controller:dev -f images/tide-controller/Dockerfile .`
    (similar for tide-push and claude-subagent)

    After building, load each into minikube:
    `minikube image load ghcr.io/jsquirrelz/tide-controller:dev`
    `minikube image load ghcr.io/jsquirrelz/tide-push:dev`
    `minikube image load ghcr.io/jsquirrelz/claude-subagent:dev`

    Record the image digests after load:
    `minikube ssh "docker inspect --format '{{.Id}}' ghcr.io/jsquirrelz/tide-controller:dev"`
    (similarly for the other two)

    These digests go into 11-VERIFICATION.md as evidence the cluster is running fresh images.

    Per CLAUDE.md "constrained-VM" lesson: do NOT run make acceptance-v1-smoke while the tide-test cluster is up — that target spawns its own cluster and OOMs the node.
  </action>
  <verify>
    <automated>minikube image ls 2>/dev/null | grep -E "tide-controller|tide-push|claude-subagent"</automated>
  </verify>
  <done>All three image tags appear in minikube image ls. Digests recorded for 11-VERIFICATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All B3–B6 changes merged (commits on main). Unit test suite green. All three images rebuilt with B3–B6 and loaded into the parked minikube cluster.</what-built>
  <how-to-verify>
    1. `kubectl rollout restart deployment/tide-controller-manager -n tide-system` — causes the controller pod to pull the newly-loaded image. Wait for rollout: `kubectl rollout status deployment/tide-controller-manager -n tide-system --timeout=120s`
    2. Apply the medium sample: `kubectl apply -f examples/projects/medium/` (or the appropriate apply command per the medium README)
    3. Watch progress: `kubectl get project -w` and `kubectl get task -A -w`
    4. After all Tasks reach Succeeded, check: `kubectl get project <name> -o jsonpath='{.status.phase}'` — must return `Complete`
    5. Verify the run branch was pushed: on the in-cluster git-http server, inspect the refs — or check that the push Job completed with exit 0 and the pushResult.Reason is empty: `kubectl get pods --field-selector=status.phase=Succeeded -n tide-sample-medium`
    6. Check cost: `kubectl get project <name> -o jsonpath='{.status.budget.costSpentCents}'` — must be > 0
    7. Capture evidence: copy the output of steps 4, 5, 6 into 11-VERIFICATION.md. Create the file at `.planning/phases/11-executor-author-commit-push-lifecycle/11-VERIFICATION.md` with gate_decision: APPROVED and the evidence fields filled in.
  </how-to-verify>
  <resume-signal>Type "approved" after 11-VERIFICATION.md is created with gate_decision: APPROVED and the evidence is captured. Or describe the failure for gap analysis.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ANTHROPIC_API_KEY → in-cluster Secret | real API key mounted as K8s Secret (EnvFrom on executor pod); never logged |
| in-cluster http:// git remote | the demo git-http-backend pod — anonymous push, no TLS; acceptable for in-cluster demo only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information Disclosure | ANTHROPIC_API_KEY in minikube test run | mitigate | Key is passed as K8s Secret (not env in the controller pod); never appears in logs (credproxy handles auth). Existing Phase 2 architecture unchanged. |
| T-11-04-02 | Tampering | v1.0.0 retag after DoD passes | transfer | MEMORY.md 2026-06-03 option (a): retag/push is user-gated, confirm-only. This plan does NOT push the tag. The user manually retags after reviewing 11-VERIFICATION.md. |
</threat_model>

<verification>
11-VERIFICATION.md exists with gate_decision: APPROVED AND:
- project.status.phase = Complete (evidence line)
- tide/run-* branch pushed to in-cluster http:// remote (evidence line)
- costSpentCents > 0 (evidence line)
- Image digests (three lines) confirming rebuilt images
</verification>

<success_criteria>
- `go test ./pkg/git/... ./internal/harness/... ./cmd/claude-subagent/... ./cmd/tide-push/... ./internal/controller/... -count=1 -timeout 60s` exits 0 (Task 1)
- All three images loaded into minikube with B3–B6 changes (Task 2)
- Medium sample reaches Project=Complete (checkpoint)
- tide/run-* pushed to in-cluster remote with real authored code (checkpoint)
- costSpentCents > 0 under cap (checkpoint)
- 11-VERIFICATION.md exists with gate_decision: APPROVED (checkpoint)
- Phase 8 SC-2 DEFERRED→PASS recorded in VERIFICATION.md
- v1.0.0 retag UNBLOCKED (retag/push itself is user-gated, confirm-only per MEMORY.md)
</success_criteria>

<output>
After completion, create .planning/phases/11-executor-author-commit-push-lifecycle/11-04-SUMMARY.md
</output>
