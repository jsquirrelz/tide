---
phase: 03-up-stack-reconcilers-git-integration-real-subagent-resumptio
plan: 07
type: execute
wave: 3
depends_on:
  - 5
files_modified:
  - cmd/claude-subagent/main.go
  - cmd/claude-subagent/main_test.go
  - images/claude-subagent/Dockerfile
  - internal/harness/worktree.go
  - internal/harness/worktree_test.go
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "cmd/claude-subagent is a ~50 LOC thin shim that loads EnvelopeIn from PVC, instantiates anthropic.New(), runs it through internal/harness, and writes EnvelopeOut to PVC."
    - "The shim IGNORES env.Dev entirely (real Claude image does not switch on testMode — that's stub-subagent behavior)."
    - "images/claude-subagent/Dockerfile uses node:22-slim base, installs @anthropic-ai/claude-code@2.1.142 globally, and copies the Go binary."
    - "Image is non-root (USER 1000). ANTHROPIC_API_KEY comes from credproxy-signed-token env, never from a mounted host directory."
    - "Harness executor-Role setup step (per D-B4 / B5) calls pkggit.AddWorktree(bareRepoPath, taskUID, branch) BEFORE invoking the Subagent's Run() — so by the time the prompt's `cd /workspace/worktrees/{task-uid}/` step runs, the directory exists and is a valid git working tree. Planner Tasks (Role=planner) SKIP this step (D-A4 invariant — planners don't need worktrees because they emit artifacts to /workspace/artifacts/... only)."
  artifacts:
    - path: "cmd/claude-subagent/main.go"
      provides: "Thin shim — envelope load → anthropic.Run → envelope write"
    - path: "images/claude-subagent/Dockerfile"
      provides: "node:22-slim + @anthropic-ai/claude-code@2.1.142 + claude-subagent Go binary"
    - path: "internal/harness/worktree.go"
      provides: "EnsureWorktree(in pkgdispatch.EnvelopeIn) — executor-Role-only D-B4 worktree-add wiring; planner-Role short-circuits without action"
  key_links:
    - from: "cmd/claude-subagent/main.go"
      to: "internal/subagent/anthropic"
      via: "anthropic.New(opts).Run(ctx, env)"
      pattern: "anthropic\\.New"
    - from: "images/claude-subagent/Dockerfile"
      to: "@anthropic-ai/claude-code@2.1.142"
      via: "npm install -g pin"
      pattern: "@anthropic-ai/claude-code@2\\.1\\.142"
---

<objective>
Wave 3 Stream C image. Ship the thin shim binary (`cmd/claude-subagent/main.go`) and the runtime image (`images/claude-subagent/Dockerfile`) that swaps stub-subagent for real Claude across the same pkg/dispatch.Subagent contract.

Purpose: Real Claude image swap-in (HARN-06). The shim is "stub-subagent's main.go minus the testMode switch" — load envelope, instantiate anthropic.New(), pipe through internal/harness, write EnvelopeOut. ~50 LOC per PATTERNS.md.

Output: 2 files (one .go shim, one Dockerfile). Tests use the same fake-exec seam as plan 03-05's Anthropic Subagent tests (no live Claude API calls in unit tests; live tests are TEST-03 nightly E2E and run separately).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-RESEARCH.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md
@cmd/stub-subagent/main.go
@images/stub-subagent/Dockerfile
@internal/harness/harness.go
@internal/harness/envelope_io.go
@CLAUDE.md

<interfaces>
<!-- The thin shim consumes internal/subagent/anthropic (from plan 03-05) and internal/harness (Phase 2). -->

Phase 2 harness contract (preserve):
- `harness.Execute(ctx, in pkgdispatch.EnvelopeIn, runtime harness.Runtime, stdout, stderr io.Writer) (pkgdispatch.EnvelopeOut, error)` — applies caps, redact, signed-token client, output-path validation. The Subagent's Run() is the Runtime in harness terms.

Wrapping anthropic Subagent as harness Runtime:
- `internal/harness/harness.go` defines `Runtime interface { Execute(ctx, in, stdout, stderr) (Usage, error) }`.
- pkg/dispatch.Subagent defines `Run(ctx, in) (EnvelopeOut, error)`.
- Adapter: the shim must bridge — either harness already has a SubagentRuntime adapter, or the shim writes a small inline adapter (~5 LOC).
- Verify by reading internal/harness/harness.go: if it exports a `SubagentAsRuntime(sa pkgdispatch.Subagent) Runtime` helper, use it. If not, declare the adapter inline in the shim.

Envelope IO (Phase 2):
- `harness.ReadEnvelopeIn(path)` returns pkgdispatch.EnvelopeIn from /workspace/envelopes/{task-uid}/in.json.
- `harness.WriteEnvelopeOut(path, env)` writes to /workspace/envelopes/{task-uid}/out.json + termination-message (Phase 02.2 PodStatusEnvelopeReader fallback).

Stub shim shape to mirror (cmd/stub-subagent/main.go lines 74-126 run() function):
- Parse flags (envelopePath).
- signal.NotifyContext for SIGTERM/SIGINT.
- Load EnvelopeIn.
- (Stub: switch on env.Dev.TestMode → dispatchSuccess/dispatchFail/etc.)
- (Claude: instantiate anthropic.New() + run through harness — NO switch on env.Dev.)
- Write EnvelopeOut.
- Return ExitCode.

Image base + version pin:
- Base: `node:22-slim` (Claude Code CLI requires Node).
- Pin: `@anthropic-ai/claude-code@2.1.142` (STACK floor v2.1.139; planner picks v2.1.142 per RESEARCH).
- USER 1000 (non-root).
- Binary at /usr/local/bin/claude-subagent (ENTRYPOINT).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cmd/claude-subagent/main.go — thin shim wiring anthropic.New() through internal/harness</name>
  <files>cmd/claude-subagent/main.go, cmd/claude-subagent/main_test.go</files>
  <read_first>
    - cmd/stub-subagent/main.go (lines 74-126 — run() function shape; lines 130-161 — loadEnvelope/writeEnvelope helpers; lines 27-29 — note "Real Claude image MUST ignore env.Dev entirely")
    - internal/subagent/anthropic/subagent.go (plan 03-05 — anthropic.New() + Run() signature)
    - internal/harness/harness.go (Phase 2 — Execute() function and Runtime interface)
    - internal/harness/envelope_io.go (Phase 2 — ReadEnvelopeIn / WriteEnvelopeOut)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md §"cmd/claude-subagent/main.go (NEW)" lines 416-443
  </read_first>
  <behavior>
    - Test 1 (envelope load + run + envelope write happy path via fake exec): pre-write an EnvelopeIn JSON at t.TempDir()+"/envelopes/t-1/in.json" with Provider.Vendor="anthropic", Provider.Model="claude-sonnet-4-6", Role="planner", Level="milestone". Use a fake exec.Command (mirrors plan 03-05 test 5 seam) so anthropic.Run shells to `bash -c 'cat <fixture.jsonl>'`. Run main with --envelope-path=<that path>. Verify out.json is written; verify the file contains a parsed EnvelopeOut with Usage and Result; exit code 0.
    - Test 2 (envelope load failure → exit 2): point --envelope-path at a non-existent file. Exit code = 2; stderr contains "open envelope" or "envelope".
    - Test 3 (vendor mismatch → wrap as failure envelope, exit non-zero): EnvelopeIn has Provider.Vendor="openai". Run main. anthropic.Run returns an error; the shim writes a failure-shaped EnvelopeOut (ExitCode!=0, Result containing the error description); main returns the EnvelopeOut.ExitCode. Verify out.json exists and contains the failure marker.
    - Test 4 (ignores env.Dev.TestMode): EnvelopeIn has Dev.TestMode="success" set. The shim does NOT switch on it — it always goes through anthropic.New().Run(). Behavior identical to Test 1 (i.e., the fake exec produces the same canned output regardless of env.Dev — that's the test).
  </behavior>
  <action>
    Create `cmd/claude-subagent/main.go` mirroring `cmd/stub-subagent/main.go` shim shape per PATTERNS §"cmd/claude-subagent/main.go (NEW)" verbatim (lines 416-443):

    ```
    package main

    import ( ... )

    func main() {
        fs := flag.NewFlagSet("claude-subagent", flag.ExitOnError)
        envelopePath := fs.String("envelope-path", "/workspace/envelopes/in.json", "path to EnvelopeIn JSON")
        if err := fs.Parse(os.Args[1:]); err != nil { os.Exit(2) }

        ctx, cancel := signal.NotifyContext(context.Background(), syscall.SIGTERM, syscall.SIGINT)
        defer cancel()

        os.Exit(run(ctx, *envelopePath, os.Stdout, os.Stderr))
    }

    func run(ctx context.Context, envelopePath string, stdout, stderr io.Writer) int {
        outPath := filepath.Join(filepath.Dir(envelopePath), "out.json")
        env, err := harness.ReadEnvelopeIn(envelopePath)
        if err != nil {
            // best-effort failure envelope at outPath (mirrors stub lines 81-92)
            _ = harness.WriteEnvelopeOut(outPath, pkgdispatch.EnvelopeOut{ ... failure shape ... })
            fmt.Fprintln(stderr, err)
            return 2
        }

        sa := anthropic.New(anthropic.Options{ ClaudeBinary: ..., WorkspaceRoot: ... })

        out, runErr := sa.Run(ctx, env)
        if runErr != nil {
            // wrap as failure envelope (set ExitCode, Result, write, return ExitCode)
            out.ExitCode = 1
            out.Result = runErr.Error()
        }
        if err := harness.WriteEnvelopeOut(outPath, out); err != nil {
            return 2
        }
        return out.ExitCode
    }
    ```

    Key invariants from PATTERNS line 442: **Real Claude image MUST ignore env.Dev entirely** — there is NO switch statement on env.Dev.TestMode in this file (unlike cmd/stub-subagent/main.go which has 5 cases). Verify by grep: `grep -c 'env\.Dev' cmd/claude-subagent/main.go` returns 0 or the only occurrence is a clearly-commented anti-pattern note.

    For the harness wiring: depending on what internal/harness/harness.go exports:
    - If `harness.Execute(ctx, in, runtime, stdout, stderr)` exists and `harness.SubagentAsRuntime(sa)` exists, wire it: `out, err := harness.Execute(ctx, env, harness.SubagentAsRuntime(sa), stdout, stderr)`.
    - If not, call `sa.Run(ctx, env)` directly. The harness Phase 2 work may already adapt this; the executor reads internal/harness/harness.go and chooses the right wiring.

    For testability: parameterize the anthropic.New options (especially ClaudeBinary) via flags or env vars so tests can override (mirrors plan 03-05's fake-exec seam).

    Mirror `loadEnvelope/writeEnvelope` patterns from `cmd/stub-subagent/main.go` lines 130-161 — same termination-message hook (Phase 02.2 PodStatusEnvelopeReader path).

    Tests in `cmd/claude-subagent/main_test.go`:
    - Tests 1-4 above. Use `t.TempDir()` for the envelope dirs; pre-write in.json with `json.Marshal(EnvelopeIn{...})`.
    - For the fake-exec seam: pass --claude-binary=/bin/bash via a flag override + a fixture JSONL file the bash command cat's.
    - Tests are fast (<5s each).

    Total LOC: target ~50-70 lines for main.go (per PATTERNS "~50 LOC per provider"). Tests can be longer; that's fine.
  </action>
  <verify>
    <automated>go test ./cmd/claude-subagent/... -count=1 -timeout 30s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'func main\(\)' cmd/claude-subagent/main.go` returns 1 hit.
    - `grep -nE 'func run\(ctx context\.Context, envelopePath string' cmd/claude-subagent/main.go` returns 1 hit.
    - `grep -cE 'anthropic\.New' cmd/claude-subagent/main.go` returns 1 hit (consumes plan 03-05 anthropic Subagent).
    - `grep -cE 'harness\.ReadEnvelopeIn|loadEnvelope' cmd/claude-subagent/main.go` returns ≥1 (envelope IO from Phase 2).
    - `grep -cE 'harness\.WriteEnvelopeOut|writeEnvelope' cmd/claude-subagent/main.go` returns ≥1.
    - `grep -cE 'switch.*env\.Dev\.TestMode|switch.*Dev' cmd/claude-subagent/main.go` returns 0 (the real Claude image IGNORES env.Dev — anti-pattern enforcement).
    - `wc -l cmd/claude-subagent/main.go` returns ≤ 100 (thin shim — PATTERNS target ~50 LOC; allow some buffer for imports + helper).
    - `go test ./cmd/claude-subagent/... -count=1 -timeout 30s` exits 0 with all 4 test cases PASS.
    - `go vet ./cmd/claude-subagent/...` exits 0.
    - `go build -o /tmp/claude-subagent ./cmd/claude-subagent` exits 0.
  </acceptance_criteria>
  <done>cmd/claude-subagent shim wires anthropic.Run + harness envelope IO; ignores env.Dev; ~50 LOC; 4 tests PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: images/claude-subagent/Dockerfile — node:22-slim + @anthropic-ai/claude-code@2.1.142 + Go binary</name>
  <files>images/claude-subagent/Dockerfile</files>
  <read_first>
    - images/stub-subagent/Dockerfile (builder stage; mirror it for the Go-build)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md §"images/claude-subagent/Dockerfile (NEW)" lines 446-473
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-RESEARCH.md (Claude Code CLI pin v2.1.142; Pitfall 3 — never npm install without version pin)
    - CLAUDE.md anti-pattern: "Don't mount host ~/.claude/ into executor containers. Use ANTHROPIC_API_KEY env from a K8s Secret."
  </read_first>
  <behavior>
    - Test 1 (Dockerfile structure — visual review during plan-checker; or hadolint if available): multi-stage; builder uses golang:1.26-alpine; runtime uses node:22-slim; npm install pins to @anthropic-ai/claude-code@2.1.142; USER 1000; ENTRYPOINT is the Go binary.
    - Test 2 (optional — docker build sanity): `docker build -f images/claude-subagent/Dockerfile -t claude-subagent:test .` succeeds (defer to plan 03-09 CI if docker not in local env).
  </behavior>
  <action>
    Create `images/claude-subagent/Dockerfile` per PATTERNS §"images/claude-subagent/Dockerfile (NEW)" verbatim:

    ```
    FROM golang:1.26-alpine AS builder
    WORKDIR /workspace
    COPY go.mod go.sum ./
    RUN go mod download
    COPY pkg/dispatch/ pkg/dispatch/
    COPY internal/harness/ internal/harness/
    COPY internal/subagent/ internal/subagent/
    COPY cmd/claude-subagent/ cmd/claude-subagent/
    RUN CGO_ENABLED=0 GOOS=linux go build \
          -ldflags="-s -w" \
          -o /out/claude-subagent \
          ./cmd/claude-subagent

    FROM node:22-slim
    RUN npm install -g @anthropic-ai/claude-code@2.1.142
    COPY --from=builder /out/claude-subagent /usr/local/bin/claude-subagent
    USER 1000
    ENTRYPOINT ["/usr/local/bin/claude-subagent"]
    ```

    Pin `@anthropic-ai/claude-code@2.1.142` (NEVER `npm install -g @anthropic-ai/claude-code` without `@<version>` — Pitfall 3 from RESEARCH).

    USER 1000: enforces non-root. Note: node:22-slim images sometimes have specific UID conventions; if `USER 1000` causes permission issues with /workspace mount, the alternative is `USER node` (Node's official base image creates a `node` user). Either works; document choice inline.

    Do NOT bind-mount or VOLUME any host path — the runtime relies on K8s mounting the per-Project PVC at /workspace. Dockerfile does not need a VOLUME directive.

    Do NOT set ANTHROPIC_API_KEY in the Dockerfile (anti-pattern) — that comes from envFrom Secret at Pod-spec time (Phase 2 credproxy + Phase 3 Helm chart in plan 03-09).

    Do NOT COPY ~/.claude or any host dir — there is no ~/.claude in the image at all (CLAUDE.md anti-pattern).
  </action>
  <verify>
    <automated>grep -E 'FROM node:22-slim' images/claude-subagent/Dockerfile && grep -E 'npm install -g @anthropic-ai/claude-code@2\.1\.142' images/claude-subagent/Dockerfile && grep -E 'USER\s+1000|USER\s+node' images/claude-subagent/Dockerfile && grep -E 'ENTRYPOINT.*claude-subagent' images/claude-subagent/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'FROM golang:1\.26-alpine AS builder' images/claude-subagent/Dockerfile` returns 1 hit.
    - `grep -nE 'FROM node:22-slim' images/claude-subagent/Dockerfile` returns 1 hit (runtime base).
    - `grep -nE 'npm install -g @anthropic-ai/claude-code@2\.1\.142' images/claude-subagent/Dockerfile` returns 1 hit (exact pin).
    - `grep -nE '^USER\s+(1000|node)' images/claude-subagent/Dockerfile` returns 1 hit (non-root).
    - `grep -nE 'ENTRYPOINT\s+\[\s*"/usr/local/bin/claude-subagent"\s*\]' images/claude-subagent/Dockerfile` returns 1 hit.
    - `grep -cE 'CGO_ENABLED=0' images/claude-subagent/Dockerfile` returns 1 (static Go binary in builder).
    - `grep -cE '~/\.claude|HOME.*\.claude' images/claude-subagent/Dockerfile` returns 0 (anti-pattern absence).
    - `grep -cE 'VOLUME|--mount' images/claude-subagent/Dockerfile` returns 0 (no host-bind expectations).
    - `grep -cE 'ANTHROPIC_API_KEY|ANTHROPIC_BASE_URL' images/claude-subagent/Dockerfile` returns 0 (env injected at K8s Pod spec, not Dockerfile).
  </acceptance_criteria>
  <done>Dockerfile uses node:22-slim runtime, pins claude-code@2.1.142, runs as non-root, has no ~/.claude or hardcoded API key references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: internal/harness — EnsureWorktree wiring for D-B4 executor parallelism (B5)</name>
  <files>internal/harness/worktree.go, internal/harness/worktree_test.go</files>
  <read_first>
    - internal/harness/harness.go (Phase 2 — Execute() method; EnsureWorktree will be called from a setup step inside Execute, or from the cmd/claude-subagent shim before invoking the Subagent; whichever site internal/harness already exposes for pre-Run setup hooks)
    - pkg/git/worktree.go (plan 03-03 — AddWorktree(bareRepoPath, taskUID, branch) signature; the function this task wires into the harness)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md (D-B4 — worktrees-per-Task; D-A4 — planner Tasks don't need worktrees)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md (PVC layout: /workspace/repo.git/, /workspace/worktrees/{task-uid}/, /workspace/artifacts/..., /workspace/envelopes/{task-uid}/)
    - cmd/claude-subagent/main.go (Task 1 of this plan — the shim that calls EnsureWorktree just before instantiating anthropic.New() + invoking Subagent.Run())
  </read_first>
  <behavior>
    - Test 1 (executor Role: AddWorktree called): given EnvelopeIn{Role:"executor", TaskUID:"task-abc", ...} and a temp PVC root with a valid bare repo at /tmp/pvc/repo.git/, calling `EnsureWorktree(in, /tmp/pvc, branchName)` results in /tmp/pvc/worktrees/task-abc/ existing as a valid git working tree (verify via `git -C /tmp/pvc/worktrees/task-abc status` exiting 0, or equivalent go-git PlainOpen check).
    - Test 2 (planner Role: short-circuit, no action): given EnvelopeIn{Role:"planner", Level:"milestone", TaskUID:"m-001", ...}, `EnsureWorktree(in, ...)` returns nil error WITHOUT calling pkggit.AddWorktree (verify via test seam: pass in a mock pkg/git AddWorktree func that records invocation count; assert count == 0).
    - Test 3 (executor Role: idempotent re-call): calling EnsureWorktree twice with the same TaskUID — second call detects the existing worktree dir and returns nil error without re-cloning (or returns a non-fatal sentinel that the caller ignores).
    - Test 4 (executor Role: missing bare repo error): if /workspace/repo.git doesn't exist, EnsureWorktree returns a wrapped error containing "bare repo" or "repo.git" — surfaces a clean failure mode if Project clone failed.
    - Test 5 (cmd/claude-subagent integration): the shim (Task 1) calls EnsureWorktree(env, workspaceRoot, branchName) BEFORE anthropic.New(...).Run() — verify by reading cmd/claude-subagent/main.go calls EnsureWorktree.
  </behavior>
  <action>
    Create `internal/harness/worktree.go`:

    - `func EnsureWorktree(in pkgdispatch.EnvelopeIn, workspaceRoot, branch string) error` — body:
      - **Planner short-circuit (D-A4)**: `if in.Role != "executor" { return nil }`. Up-stack planner Tasks (Role=planner) emit artifacts to /workspace/artifacts/... and envelopes only — they never touch the working repo, so they don't need worktrees. This preserves the Phase 2 D-A4 invariant.
      - Executor path: compute `bareRepoPath := filepath.Join(workspaceRoot, "repo.git")`. Verify it exists (stat); if not, return wrapped error "EnsureWorktree: bare repo at %s missing — Project clone must complete first".
      - Compute `worktreeDir := filepath.Join(workspaceRoot, "worktrees", in.TaskUID)`. Check if it already exists with a valid `.git` (idempotent re-call path); if yes, return nil.
      - Call `pkggit.AddWorktree(bareRepoPath, in.TaskUID, branch)` — function from plan 03-03. The returned path should match worktreeDir.
      - Return wrapped errors with TaskUID + branch context.
    - Godoc cites D-B4 + D-A4; documents the planner-short-circuit rationale verbatim.

    For testability: expose a package-level var or function field so tests can inject a fake pkggit.AddWorktree. The simplest seam: `var addWorktreeFunc = pkggit.AddWorktree` (var declaration at package level), and tests do `defer func(orig){addWorktreeFunc = orig}(addWorktreeFunc); addWorktreeFunc = mockAddWorktree`.

    Update `cmd/claude-subagent/main.go` (Task 1 of this plan) to invoke `harness.EnsureWorktree(env, workspaceRoot, branchName)` BEFORE calling `sa.Run(ctx, env)`. The branch name comes from the EnvelopeIn's project context (TBD: either as `in.ProjectBranch` field — requires plan 03-01 envelope schema check — or read from a small `/workspace/envelopes/{task-uid}/branch.txt` side file written by the orchestrator; this Task 3 finalizes the choice). If the field is not in the envelope schema, use the side-file pattern (the orchestrator writes /workspace/envelopes/{task-uid}/branch.txt at Job-creation time; cheap and avoids re-bumping the envelope schema mid-Phase-3).

    Tests in `internal/harness/worktree_test.go`:
    - Tests 1-4 above. Use `t.TempDir()` + a fake `addWorktreeFunc` that records call count + arguments. Init a real bare repo on disk for Test 1+3 (one-time setup helper); Test 4 uses a non-existent bare-repo path.

    Test 5 lives in `cmd/claude-subagent/main_test.go` (Task 1 of this plan) — add an assertion that EnsureWorktree is invoked before anthropic.Run via the same fake-exec seam Task 1 already uses.

    Import firewall property: `internal/harness/worktree.go` imports `pkg/git` (allowed — pkg/git is provider-agnostic) and `pkg/dispatch` (for EnvelopeIn type). It does NOT import `internal/subagent/anthropic` (provider-agnostic discipline).
  </action>
  <verify>
    <automated>go test ./internal/harness/... -run "TestEnsureWorktree" -count=1 -timeout 30s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'func EnsureWorktree\(in pkgdispatch\.EnvelopeIn, workspaceRoot, branch string\) error' internal/harness/worktree.go` returns 1 hit.
    - `grep -cE 'in\.Role != "executor"' internal/harness/worktree.go` returns ≥1 (D-A4 planner-short-circuit).
    - `grep -cE 'pkggit\.AddWorktree|addWorktreeFunc' internal/harness/worktree.go` returns ≥1 (D-B4 consumes plan 03-03 AddWorktree).
    - `grep -rcE 'internal/subagent/anthropic' internal/harness/worktree.go` returns 0 (provider-agnostic).
    - `grep -cE 'EnsureWorktree' cmd/claude-subagent/main.go` returns ≥1 (shim invokes the helper before sa.Run).
    - `go test ./internal/harness/... -run "TestEnsureWorktree" -count=1 -timeout 30s` exits 0 with 4 test cases PASS.
    - `go test ./cmd/claude-subagent/... -count=1 -timeout 30s` exits 0 (Test 5 integration; existing Task 1 tests + new EnsureWorktree-invocation assertion).
    - `go vet ./internal/harness/...` exits 0.
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>internal/harness ships EnsureWorktree with D-A4 planner-short-circuit + D-B4 executor wiring via pkggit.AddWorktree; cmd/claude-subagent shim invokes it before sa.Run; 5 test cases PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude-subagent pod main container ↔ credproxy sidecar (Phase 2 D-C1) | Same as plan 03-05's runtime threat surface. Image swap doesn't change the trust boundary. |
| Container image ↔ host filesystem | Image MUST NOT bind-mount or COPY anything from the host's ~/.claude/ — verified by Dockerfile grep gates above. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-305 (Information Disclosure — host credential leak) | images/claude-subagent/Dockerfile | mitigate | The Dockerfile has zero VOLUME, zero ~/.claude, zero hardcoded ANTHROPIC_API_KEY. Acceptance criteria 7/8/9 of Task 2 enforce via grep returning 0. Production deployment relies on Helm chart (plan 03-09) to inject ANTHROPIC_API_KEY via credproxy → signed-token env, never the raw key. |
| T-306 (Tampering — malicious prompt-template injection) | cmd/claude-subagent/main.go | mitigate (inherited) | Plan 03-05 already locked: templates are compiled-in via go:embed; subagent pod has zero K8s verbs (Phase 2 D-A4); this image inherits both properties. The shim adds NO new attack surface beyond loading EnvelopeIn from PVC (Phase 2 D-A2 envelope contract). |
</threat_model>

<verification>
- `go test ./cmd/claude-subagent/... -count=1 -timeout 30s` PASS.
- `go vet ./cmd/claude-subagent/...` exits 0.
- `go build ./...` exits 0.
- `wc -l cmd/claude-subagent/main.go` shows ≤ 100 lines (thin shim).
- Dockerfile passes all grep gates (no ~/.claude, no hardcoded API key, USER non-root, exact CLI version pin).
</verification>

<success_criteria>
- Thin shim (~50 LOC) wires anthropic.New → internal/harness → envelope IO.
- env.Dev.TestMode is NEVER switched on in the real Claude image.
- Dockerfile: node:22-slim + claude-code@2.1.142 + non-root + Go binary entrypoint.
- All 4 main_test.go cases PASS via fake-exec.
- No host ~/.claude/ references anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-07-SUMMARY.md`.
</output>
