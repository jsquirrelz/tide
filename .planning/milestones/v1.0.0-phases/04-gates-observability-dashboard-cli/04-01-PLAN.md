---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/metrics/registry.go
  - internal/metrics/registry_test.go
  - internal/metrics/doc.go
  - api/v1alpha1/project_types.go
  - api/v1alpha1/shared_types.go
  - api/v1alpha1/zz_generated.deepcopy.go
  - cmd/manager/main.go
autonomous: true
requirements: [OBS-02]
threat_model_id: T-04-O2
must_haves:
  truths:
    - "Manager process exposes /metrics with all 8 Phase 4 counters + 1 histogram registered"
    - "Every counter has bounded labels — at most {project, phase, plan} + optional {reason, outcome, vendor, level}"
    - "Project CRD ships new Phase constant PhasePushLeakBlocked alongside the existing PhasePushLeaseFailed (W-1 prep)"
    - "Shared types ship ConditionWaveOrLevelPaused + 4 gate-policy Reason constants (D-G2 prep)"
    - "Metric registry is import-side-effect-only — `import _ \"github.com/jsquirrelz/tide/internal/metrics\"` in cmd/manager/main.go triggers init()"
  artifacts:
    - path: internal/metrics/registry.go
      provides: "WavesDispatchedTotal, TasksCompletedTotal, TasksFailedTotal, DispatchLatency, SecretLeakBlockedTotal, PushJobsTotal, BudgetOverrunsTotal, ProviderRateLimitHitsTotal (re-export aliasing budget) — all *prometheus.CounterVec / *HistogramVec package-level vars"
      contains: "metrics.Registry.MustRegister"
    - path: api/v1alpha1/project_types.go
      provides: "const PhasePushLeakBlocked = \"PushLeakBlocked\""
      contains: "PhasePushLeakBlocked"
    - path: api/v1alpha1/shared_types.go
      provides: "ConditionWaveOrLevelPaused + ReasonAwaitingApproval, ReasonPausedAtBoundary, ReasonRejectedByUser, ReasonResumedByUser"
      contains: "ConditionWaveOrLevelPaused"
  key_links:
    - from: cmd/manager/main.go
      to: internal/metrics/registry.go
      via: "blank import side effect"
      pattern: "_ \"github.com/jsquirrelz/tide/internal/metrics\""
    - from: internal/metrics/registry.go
      to: sigs.k8s.io/controller-runtime/pkg/metrics
      via: "metrics.Registry.MustRegister in init()"
      pattern: "metrics\\.Registry\\.MustRegister"
---

<objective>
Ship the central Prometheus metric registry for Phase 4: all 8 v1 counters + 1 histogram with bounded-cardinality labels, plus the API constants downstream waves consume (PhasePushLeakBlocked for W-1, ConditionWaveOrLevelPaused + 4 Reasons for D-G2). This plan is the Wave-1 foundation that the gates wiring (04-05/04-06), the boundary push trigger (04-06), and every counter-Inc() site in subsequent reconciler edits depend on.

Purpose: Centralize metric definitions in one auditable file so the metric-cardinality lint analyzer (04-02) and downstream Inc() callers (04-06) point at exactly one seam. Lock the Phase constants and Conditions before any reconciler edits.

Output: `internal/metrics/registry.go` + tests + cmd/manager blank-import; `api/v1alpha1/project_types.go` and `shared_types.go` extended; codegen rerun.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@internal/budget/metrics.go
@api/v1alpha1/project_types.go
@api/v1alpha1/shared_types.go
@cmd/manager/main.go

<interfaces>
<!-- Existing reference pattern — copy verbatim shape -->

From internal/budget/metrics.go:
```go
package budget

import (
    "github.com/prometheus/client_golang/prometheus"
    "sigs.k8s.io/controller-runtime/pkg/metrics"
)

var ProviderRateLimitHitsTotal *prometheus.CounterVec

func init() {
    ProviderRateLimitHitsTotal = prometheus.NewCounterVec(
        prometheus.CounterOpts{
            Name: "tide_provider_rate_limit_hits_total",
            Help: "Count of times the per-Secret-UID rate-limit bucket was exhausted, surfaced by Project.",
        },
        []string{"project"},
    )
    metrics.Registry.MustRegister(ProviderRateLimitHitsTotal)
}
```

From api/v1alpha1/project_types.go (lines 297-316):
```go
const (
    PhasePending = "Pending"
    PhaseInitialized = "Initialized"
    PhaseInitFailed = "InitFailed"
    PhaseBudgetExceeded = "BudgetExceeded"
    PhaseRunning = "Running"
    PhasePushLeaseFailed = "PushLeaseFailed"
    PhaseComplete = "Complete"
)
```

From api/v1alpha1/shared_types.go (lines 74-93):
```go
const (
    ConditionCloned = "Cloned"
    ConditionAuthoringPlanner = "AuthoringPlanner"
    ConditionPushLeaseFailed = "PushLeaseFailed"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API constant additions — Phase + Condition + Reason</name>
  <read_first>
    - api/v1alpha1/project_types.go (lines 297-316 — Phase constants pattern)
    - api/v1alpha1/shared_types.go (lines 60-93 — Condition + Reason constants pattern)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (section F — CRD constant additions)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-W1 PhasePushLeakBlocked; D-G2 / D-G3 / D-G4 Condition + Reasons)
  </read_first>
  <behavior>
    - Test 1: Constant `PhasePushLeakBlocked` exists at package api/v1alpha1 with value `"PushLeakBlocked"` and is distinct from PhasePushLeaseFailed (W-1).
    - Test 2: Constant `ConditionWaveOrLevelPaused` exists with value `"WaveOrLevelPaused"` (D-G2).
    - Test 3: Constants `ReasonAwaitingApproval` ("AwaitingApproval"), `ReasonPausedAtBoundary` ("PausedAtBoundary"), `ReasonRejectedByUser` ("RejectedByUser"), `ReasonResumedByUser` ("ResumedByUser") all exist.
    - Test 4: `go vet ./api/v1alpha1/...` passes (no shadowed constants, no unused imports).
  </behavior>
  <action>
    Append to `api/v1alpha1/project_types.go` a new constant `PhasePushLeakBlocked = "PushLeakBlocked"` immediately after `PhasePushLeaseFailed` (line ~313), with a comment block matching the style of `PhasePushLeaseFailed`'s comment (cite D-W1: distinct from lease failure so W-1 counter `tide_secret_leak_blocked_total` fires only on exit-10 leak-detected). Append to `api/v1alpha1/shared_types.go` (next to existing `ConditionPushLeaseFailed` block, lines 74-93) the new `ConditionWaveOrLevelPaused = "WaveOrLevelPaused"` constant AND four Reason constants: `ReasonAwaitingApproval`, `ReasonPausedAtBoundary`, `ReasonRejectedByUser`, `ReasonResumedByUser` — values exactly as listed in the behavior block, with one-line doc comments referencing D-G2/D-G3/D-G4. Add unit tests in a new file `api/v1alpha1/phase4_constants_test.go` that assert each constant's exact string value using table-driven `t.Run` subtests. Run `make manifests generate` (or `controller-gen object`) to regenerate `zz_generated.deepcopy.go` if codegen complains (constant additions should not touch deepcopy, but verify).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./api/v1alpha1/... -run Phase4Constants -v &amp;&amp; grep -c "PhasePushLeakBlocked" api/v1alpha1/project_types.go &amp;&amp; grep -c "ConditionWaveOrLevelPaused" api/v1alpha1/shared_types.go</automated>
  </verify>
  <done>The four constants compile, tests pass, grep shows exactly one declaration per constant in their target files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: internal/metrics/registry.go — central Prometheus registry</name>
  <read_first>
    - internal/budget/metrics.go (entire file — exact shape to copy)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (section A — Prometheus counter registration)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-O2 — locked counter set with label specs)
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 900-953 — Q9 central registry)
  </read_first>
  <behavior>
    - Test 1: Calling `internal/metrics`' init (via blank import) registers exactly 7 metrics on `crmetrics.Registry`: `tide_waves_dispatched_total`, `tide_tasks_completed_total`, `tide_tasks_failed_total`, `tide_dispatch_latency_seconds`, `tide_secret_leak_blocked_total`, `tide_push_jobs_total`, `tide_budget_overruns_total`. (ProviderRateLimitHitsTotal stays in internal/budget — do NOT duplicate it.)
    - Test 2: `WavesDispatchedTotal.WithLabelValues("p", "ph", "pl").Inc()` succeeds (label arity is exactly 3: project, phase, plan).
    - Test 3: `TasksFailedTotal.WithLabelValues("p", "ph", "pl", "exit-1").Inc()` succeeds (arity 4: + reason). The set of allowed reasons is documented in the metric Help string as {exit-1, gitleaks, lease, auth, internal, budget}.
    - Test 4: `DispatchLatency.WithLabelValues("milestone").Observe(2.5)` succeeds; its bucket slice is exactly `[0.1, 0.5, 1, 5, 10, 30, 60, 300, 1800]` (D-Discretion locked).
    - Test 5: `SecretLeakBlockedTotal.WithLabelValues("p", "ph", "pl").Inc()` succeeds (arity 3, no task label).
    - Test 6: `PushJobsTotal.WithLabelValues("p", "success").Inc()` succeeds (arity 2: project, outcome ∈ {success, leak, lease, auth, internal}).
    - Test 7: `BudgetOverrunsTotal.WithLabelValues("p").Inc()` succeeds (arity 1).
    - Test 8: No metric in this file has `"task"` as a label literal — direct grep assertion in the test (anticipating the lint analyzer of plan 04-02).
  </behavior>
  <action>
    Create `internal/metrics/registry.go` and `internal/metrics/doc.go`. Each of the 7 metrics is a package-level `*prometheus.CounterVec` or `*prometheus.HistogramVec` variable, exported with capitalized name (WavesDispatchedTotal, TasksCompletedTotal, TasksFailedTotal, DispatchLatency, SecretLeakBlockedTotal, PushJobsTotal, BudgetOverrunsTotal). Each carries a meaningful `prometheus.CounterOpts.Help` string per D-O2 (e.g., "Count of Waves dispatched to executor pool, surfaced by project/phase/plan"). `DispatchLatency` uses `prometheus.HistogramOpts` with `Buckets: []float64{0.1, 0.5, 1, 5, 10, 30, 60, 300, 1800}` declared as a package-level `var dispatchLatencyBuckets = []float64{...}` constant per the CONTEXT specifics block. Label slices are literal `[]string{...}` (NEVER a variable — the analyzer of 04-02 walks literals). All 7 register in a single `init()` via `metrics.Registry.MustRegister(...)`. `doc.go` carries a package overview citing D-O2/D-W1/D-X4 and explaining the cardinality discipline rule (bounded labels, no task label, Pitfall 17). Place `internal/metrics/registry_test.go` using `github.com/prometheus/client_golang/prometheus/testutil` (testutil.GatherAndCount and testutil.CollectAndCount) to verify each metric exists in the registry post-init; the testutil package is the canonical tool here. Do NOT touch `internal/budget/metrics.go` — Phase 2's ProviderRateLimitHitsTotal stays in place.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./internal/metrics/... -race -v &amp;&amp; grep -E "prometheus\.New(CounterVec|HistogramVec|GaugeVec|SummaryVec)" internal/metrics/registry.go | grep -v '^#' | grep -c task ; if [ $? -eq 0 ]; then echo "FAIL: task label present"; exit 1; fi ; echo "PASS: no task labels"</automated>
  </verify>
  <done>All 7 metrics register, tests pass with -race, grep confirms zero `task` label literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire internal/metrics blank import into cmd/manager/main.go</name>
  <read_first>
    - cmd/manager/main.go (existing imports block, lines 1-60)
    - internal/metrics/registry.go (the file from Task 2)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (section J — env-driven config + blank import)
  </read_first>
  <behavior>
    - Test 1: After manager binary builds, running it briefly (or assertion via TestMetricsRegistered using metric.Registry.Gather()) shows the 7 Phase 4 metrics registered.
    - Test 2: `go vet ./cmd/manager/...` passes after the new blank import is added.
    - Test 3: A focused envtest in `cmd/manager/main_test.go` (or extending existing test file) asserts `crmetrics.Registry.Gather()` returns metric families including names `tide_waves_dispatched_total`, `tide_secret_leak_blocked_total`, `tide_dispatch_latency_seconds`. (testutil.GatherAndCount returns ≥1 for each.)
  </behavior>
  <action>
    Add `_ "github.com/jsquirrelz/tide/internal/metrics"` as a blank import in `cmd/manager/main.go`'s import block (place it after the existing `_ "github.com/jsquirrelz/tide/internal/budget"` if present, or as a standalone group below the named imports — preserve goimports grouping). Do NOT modify any other manager wiring — OTel init lands in plan 04-03, not here. Add or extend a manager-level test asserting metric registration via `testutil.GatherAndCount(crmetrics.Registry, "tide_waves_dispatched_total")` returning ≥ 1 (must use `sigs.k8s.io/controller-runtime/pkg/metrics` registry, NOT `prometheus.DefaultRegisterer`).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go build ./cmd/manager/... &amp;&amp; go test ./cmd/manager/... -run MetricsRegistered -v</automated>
  </verify>
  <done>Manager binary builds, the new test confirms all 7 Phase 4 metric families are registered on crmetrics.Registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler → /metrics endpoint | Bounded-cardinality metric definitions cross from controller code into the scrape surface; an unbounded label set DoSes Prometheus storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-O2 | DoS | internal/metrics/registry.go | mitigate | All counter/histogram registrations declare bounded label sets: {project, phase, plan} (+ at most one of reason/outcome/vendor/level). Compile-time grep test in this plan + the AST analyzer in plan 04-02 enforce the rule. |
| T-04-O5 | Info disclosure | internal/metrics/registry.go | accept | Metric NAMES are stable strings; Help text is operator-readable docs. No payload data crosses this boundary. |
</threat_model>

<verification>
- `go test ./api/v1alpha1/... ./internal/metrics/... ./cmd/manager/... -race -v` returns zero failures.
- `grep -E "prometheus\.New(CounterVec|HistogramVec|GaugeVec|SummaryVec)" internal/metrics/registry.go | grep -v '^#' | grep -c '"task"'` returns 0 (no `task` label literals).
- `grep -c "PhasePushLeakBlocked" api/v1alpha1/project_types.go` returns ≥ 1.
- `grep -c "ConditionWaveOrLevelPaused" api/v1alpha1/shared_types.go` returns ≥ 1.
- `make tide-lint` still passes (no new analyzer running yet — plan 04-02 ships it).
</verification>

<success_criteria>
- internal/metrics/registry.go centralizes all 7 Phase 4 metrics, each with bounded labels.
- DispatchLatency histogram uses the locked bucket slice `[0.1, 0.5, 1, 5, 10, 30, 60, 300, 1800]`.
- cmd/manager/main.go's blank import triggers registration on Manager start.
- Project CRD has PhasePushLeakBlocked constant (W-1 unblocked).
- shared_types.go has ConditionWaveOrLevelPaused + 4 Reasons (gates wiring in plans 04-04/04-05 unblocked).
- Zero references to `task` label literal in any new Phase 4 metric definition.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-01-SUMMARY.md` documenting which metrics were registered, the label-arity table, the new API constants, and a brief note on what downstream plans now consume.
</output>
