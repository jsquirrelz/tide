---
phase: 04
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-04]
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/milestone_controller_test.go
  - internal/controller/phase_controller.go
  - internal/controller/phase_controller_test.go
  - internal/controller/plan_controller.go
  - internal/controller/plan_controller_test.go
  - internal/controller/task_controller.go
  - internal/controller/task_controller_test.go
  - internal/controller/wave_controller.go
  - internal/controller/wave_controller_test.go
  - test/integration/envtest/gates_test.go
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, OBS-01]
threat_model_id: T-04-G1,T-04-G2,T-04-G3
must_haves:
  truths:
    - "When `Project.Spec.gates.milestone == \"approve\"` and no approve annotation exists, MilestoneReconciler patches `Status.Phase = \"AwaitingApproval\"` + sets ConditionWaveOrLevelPaused True with Reason=AwaitingApproval AND does NOT call patchMilestoneSucceeded"
    - "Same for PhaseReconciler, PlanReconciler, TaskReconciler at their respective level boundaries — all four reconcilers consult `internal/gates.EvaluatePolicy(project.Spec.Gates, level)` before patchSucceeded"
    - "When the operator writes annotation `tideproject.k8s/approve-milestone: \"true\"` on the Milestone, the next reconcile consumes the annotation (one-shot) and proceeds to patchMilestoneSucceeded"
    - "When `Project.Spec.gates.pauseBetweenWaves == true`, PlanReconciler pauses BEFORE dispatching wave N+1 — it sets the Plan's Condition WaveOrLevelPaused True with Reason=PausedAtBoundary and waits for annotation `tideproject.k8s/approve-wave-<N+1>: \"true\"`"
    - "When `tideproject.k8s/reject` annotation is present on a Project, every up-stack reconciler (Milestone/Phase/Plan/Task) short-circuits and patches Status.Phase=Failed with Reason=RejectedByUser"
    - "AnnotationChangedPredicate is wired into every modified reconciler's SetupWithManager so annotation writes trigger reconciliation (no polling)"
  artifacts:
    - path: internal/controller/milestone_controller.go
      provides: "gate-policy hook inserted before patchMilestoneSucceeded; patchMilestoneAwaitingApproval helper; AnnotationChangedPredicate wired"
      contains: "gates.EvaluatePolicy"
    - path: internal/controller/plan_controller.go
      provides: "gate-policy hook + PauseBetweenWaves wave-pause hook + CheckWaveApprove consume"
      contains: "gates.CheckWaveApprove"
    - path: internal/controller/wave_controller.go
      provides: "AnnotationChangedPredicate added to SetupWithManager so wave-approve annotation triggers requeue"
      contains: "AnnotationChangedPredicate"
    - path: test/integration/envtest/gates_test.go
      provides: "envtest covering approve-blocks-then-clears flow + reject-halts flow + pause-between-waves"
      contains: "TestGateApproveFlow"
  key_links:
    - from: internal/controller/milestone_controller.go
      to: internal/gates
      via: "EvaluatePolicy + CheckApprove + CheckRejected + ConsumeApprove"
      pattern: "gates\\.(EvaluatePolicy|CheckApprove|CheckRejected|ConsumeApprove)"
    - from: internal/controller/plan_controller.go
      to: internal/gates.CheckWaveApprove
      via: "PauseBetweenWaves wave-N check"
      pattern: "gates\\.CheckWaveApprove"
---

<objective>
Wire the `internal/gates` package (from plan 04-04) into all four up-stack reconcilers (Milestone, Phase, Plan, Task) and add a wave-pause hook to PlanReconciler for `PauseBetweenWaves`. Add `AnnotationChangedPredicate` to every modified reconciler's `SetupWithManager` so operator annotations (approve / reject / approve-wave-N) trigger reconciliation without polling. Add a focused envtest at `test/integration/envtest/gates_test.go` proving the three core gate flows (level approve, project reject, wave pause).

Purpose: This is the GATE-01..03 implementation plan. Reconciler bodies stay surgical (~20-30 LOC per file) because all domain logic lives in `internal/gates`. The hook insertion point in each `handleJobCompletion` (or equivalent post-children-Succeeded seam) is the same: between MaterializeChildCRDs and the final patchSucceeded call (per RESEARCH §371-397).

Output: 5 reconciler files modified, 1 envtest added, the gate-policy seam fires at every level boundary including wave boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-04-PLAN.md
@internal/gates/policy.go
@internal/gates/annotation.go
@internal/controller/milestone_controller.go
@internal/controller/phase_controller.go
@internal/controller/plan_controller.go
@internal/controller/task_controller.go
@internal/controller/wave_controller.go
@internal/controller/project_controller.go
@internal/controller/dispatch_helpers.go

<interfaces>
<!-- Existing reconciler structures + the seam where gate hook lands -->

From internal/controller/milestone_controller.go (lines 241-274 — handleJobCompletion seam):
The hook lands BEFORE `r.patchMilestoneSucceeded(ctx, ms)`. Insert two new steps:
1. `gates.EvaluatePolicy(project.Spec.Gates, "milestone")` → if "approve" or "pause", check annotation; if absent, patch AwaitingApproval and return.
2. `gates.CheckRejected(project)` → if true, patch Failed with Reason=RejectedByUser and return.

From internal/controller/project_controller.go (lines 619-645 — predicate.AnnotationChangedPredicate already wired here):
```go
For(&v1alpha1.Project{},
    builder.WithPredicates(predicate.Or(
        predicate.GenerationChangedPredicate{},
        predicate.AnnotationChangedPredicate{},
    )),
)
```
This is the pattern to replicate in milestone/phase/plan/task/wave SetupWithManager.

From api/v1alpha1/shared_types.go (plan 04-01 added these):
- `ConditionWaveOrLevelPaused = "WaveOrLevelPaused"`
- `ReasonAwaitingApproval = "AwaitingApproval"`
- `ReasonPausedAtBoundary = "PausedAtBoundary"`
- `ReasonRejectedByUser = "RejectedByUser"`
- `ReasonResumedByUser = "ResumedByUser"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate-policy hook in Milestone + Phase + Plan reconcilers + Task</name>
  <read_first>
    - internal/controller/milestone_controller.go (entire file — focus on lines 241-274 handleJobCompletion + lines 276-306 patchMilestoneSucceeded/Failed)
    - internal/controller/phase_controller.go (mirror file)
    - internal/controller/plan_controller.go (focus on lines 263-322 handlePlannerJobCompletion)
    - internal/controller/task_controller.go (focus on the 6-step body lines 121-188)
    - internal/gates/policy.go + annotation.go (the API from plan 04-04)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (section C — gate seam insertion)
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 365-397 Pattern 1)
  </read_first>
  <behavior>
    - Test 1 (Milestone): When `Project.Spec.Gates.Milestone = "approve"` and the Milestone has no `tideproject.k8s/approve-milestone` annotation, after handleJobCompletion executes, Milestone.Status.Phase is `"AwaitingApproval"` and Condition `WaveOrLevelPaused` is True with Reason `AwaitingApproval`. The function returns `ctrl.Result{}, nil` (no requeue — annotation write triggers AnnotationChangedPredicate).
    - Test 2 (Milestone resume): When the Milestone gets annotation `tideproject.k8s/approve-milestone: "true"`, next reconcile passes through the hook (annotation consumed via ConsumeApprove patch), Condition WaveOrLevelPaused is set False with Reason=ResumedByUser, then patchMilestoneSucceeded fires.
    - Test 3 (Reject): When `Project` has annotation `tideproject.k8s/reject: "<reason>"`, MilestoneReconciler patches Milestone.Status.Phase=Failed with Reason=RejectedByUser and Message containing the reason value.
    - Test 4 (auto): When `Gates.Milestone = "auto"` (or empty — default for phase/plan/task is auto), the gate hook is a no-op and patchMilestoneSucceeded fires immediately (today's behavior preserved).
    - Test 5: Repeat tests 1-4 for Phase + Plan + Task reconcilers with the level-appropriate annotation key (`approve-phase`, `approve-plan`, `approve-task`).
    - Test 6: `gates.EvaluatePolicy(project.Spec.Gates, level)` is called by every modified reconciler — grep assertion `grep -c "gates.EvaluatePolicy" internal/controller/{milestone,phase,plan,task}_controller.go` returns ≥ 4.
  </behavior>
  <action>
    For each of milestone_controller.go, phase_controller.go, plan_controller.go, task_controller.go: locate the post-MaterializeChildCRDs seam BEFORE the final patchSucceeded call (lines 241-274 for milestone; analogous for others). Insert two steps in order:

    Step A (reject short-circuit): Fetch the parent Project (already available in milestone/phase/plan via r.Get; for Task, fetch via the ProjectRef chain). If `gates.CheckRejected(project)` returns true, call a new helper `patch<Level>Failed(ctx, obj, tideprojectv1alpha1.ReasonRejectedByUser, gates.RejectedReason(project))` and return.

    Step B (gate hook): `level := "<milestone|phase|plan|task>"`; `policy := gates.EvaluatePolicy(project.Spec.Gates, level)`; if `policy == gates.PolicyApprove || policy == gates.PolicyPause`: check `gates.CheckApprove(obj, level)`. If false, call new helper `patch<Level>AwaitingApproval(ctx, obj, policy)` — set Status.Phase="AwaitingApproval", set Condition WaveOrLevelPaused True with Reason=AwaitingApproval (if policy=approve) or Reason=PausedAtBoundary (if policy=pause), and return `ctrl.Result{}, nil`. If approve annotation is true: call `newAnnotations := gates.ConsumeApprove(obj, level)`; patch the obj's annotations with `client.MergeFrom`; then proceed to patchSucceeded (which now also clears the WaveOrLevelPaused condition to False with Reason=ResumedByUser).

    Add the new helpers (patch<Level>AwaitingApproval per reconciler) mirroring the existing patch<Level>Succeeded/Failed helper shape (e.g., milestone_controller.go:276-306 — same `client.MergeFrom + meta.SetStatusCondition + r.Status().Patch` pattern). Reuse `ConditionWaveOrLevelPaused` and the Reason constants from plan 04-01's shared_types.go additions.

    For the Task reconciler specifically: the gate level for tasks is more granular — the Task gate hook fires BEFORE Job dispatch (not after children), because Tasks have no children. So in task_controller.go the seam is BEFORE the Job-create call in the 6-step body. Document this difference inline with a comment citing D-G1's per-level policy enum.

    Add unit tests in each reconciler's `*_test.go`: for milestone_controller_test.go, plan_controller_test.go etc., add a `TestReconciler_GateApprove` subtest exercising tests 1-4 above using the existing envtest harness (`suite_test.go`).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./internal/controller/... -run Gate -race -v &amp;&amp; grep -c "gates.EvaluatePolicy" internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go internal/controller/task_controller.go</automated>
  </verify>
  <done>All four reconcilers consult gates.EvaluatePolicy at their respective seams; four new patch<Level>AwaitingApproval helpers exist; gate-flow unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlanReconciler PauseBetweenWaves + AnnotationChangedPredicate wiring</name>
  <read_first>
    - internal/controller/plan_controller.go (focus on lines 379-540 reconcileWaveMaterialization + lines 551-585 SetupWithManager)
    - internal/controller/wave_controller.go (focus on SetupWithManager + handle wave-progression)
    - internal/gates/annotation.go (CheckWaveApprove, ConsumeWaveApprove)
    - internal/controller/project_controller.go (lines 619-645 — AnnotationChangedPredicate reference pattern)
    - api/v1alpha1/project_types.go (lines 47-64 — PauseBetweenWaves field)
  </read_first>
  <behavior>
    - Test 1: When `Project.Spec.Gates.PauseBetweenWaves == true` and a wave N has all tasks Succeeded, before triggering wave N+1, PlanReconciler patches the Plan's Condition WaveOrLevelPaused True with Reason=PausedAtBoundary and message "Awaiting approval for wave N+1". The Plan's Status carries a hint of which wave is pending.
    - Test 2: When the operator writes annotation `tideproject.k8s/approve-wave-<N+1>: "true"` on the Plan, the next reconcile (triggered by AnnotationChangedPredicate) consumes the annotation, clears the Condition, and proceeds to dispatch wave N+1.
    - Test 3: When `PauseBetweenWaves == false` (default), wave transitions happen automatically — no pause condition is set (today's behavior preserved).
    - Test 4: SetupWithManager in milestone/phase/plan/task/wave reconcilers all carry `builder.WithPredicates(predicate.Or(predicate.GenerationChangedPredicate{}, predicate.AnnotationChangedPredicate{}))` on their `For(...)` call. Grep assertion `grep -c "AnnotationChangedPredicate" internal/controller/{milestone,phase,plan,task,wave}_controller.go` returns ≥ 5.
  </behavior>
  <action>
    In `internal/controller/plan_controller.go`, locate the wave progression point (within `reconcileWaveMaterialization` or in a new helper `maybePauseForWaveApprove` called between waves). Determine wave completion status via the existing Wave CRDs + Task children (use the existing `pkg/dag.ComputeWaves` or iterate Tasks by wave label). When PauseBetweenWaves is true: after wave N is fully Succeeded, before dispatching wave N+1, call `gates.CheckWaveApprove(plan, N+1)`. If false: patch the Plan's Condition WaveOrLevelPaused True with Reason=PausedAtBoundary, set a `Status.WavePendingApproval = N+1` field (or surface via Condition Message — pick one consistent surface), and return `ctrl.Result{}, nil`. If true: call `newAnnotations := gates.ConsumeWaveApprove(plan, N+1)` → patch annotations → clear the Condition → proceed.

    In SetupWithManager for milestone_controller.go, phase_controller.go, plan_controller.go, task_controller.go, AND wave_controller.go: wrap the existing `For(...)` call's predicate with `builder.WithPredicates(predicate.Or(predicate.GenerationChangedPredicate{}, predicate.AnnotationChangedPredicate{}))`. Reference shape: project_controller.go lines 619-645. Preserve the existing `WithEventFilter(nsPred)` and `WithOptions(controller.Options{MaxConcurrentReconciles: ...})` calls — they remain unchanged.

    Add unit tests in plan_controller_test.go: `TestPlanReconciler_PauseBetweenWaves_Pauses`, `TestPlanReconciler_PauseBetweenWaves_ResumesAfterAnnotation`, `TestPlanReconciler_NoPauseWhenDisabled`. Add a test in wave_controller_test.go asserting AnnotationChangedPredicate is among the predicates passed to For (use a wave-pause assertion or a simple predicate-list grep on the file).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./internal/controller/... -run "WavePause|PauseBetweenWaves" -race -v &amp;&amp; grep -c "AnnotationChangedPredicate" internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go internal/controller/task_controller.go internal/controller/wave_controller.go</automated>
  </verify>
  <done>PlanReconciler honors PauseBetweenWaves; wave-approve annotation unblocks; all five reconcilers' SetupWithManager carry AnnotationChangedPredicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test/integration/envtest/gates_test.go — three-flow envtest</name>
  <read_first>
    - internal/controller/suite_test.go (existing envtest scaffolding)
    - internal/controller/milestone_controller_test.go (envtest spec shape, Eventually patterns)
    - .planning/phases/04-gates-observability-dashboard-cli/04-VALIDATION.md (Per-Task Verification Map — Gates section)
  </read_first>
  <behavior>
    - Test 1 (TestGateApproveFlow): Apply a Project with `gates.milestone=approve`; apply a Milestone owned by it; the milestone job-completion sequence runs; assert Milestone.Status.Phase=AwaitingApproval and WaveOrLevelPaused Condition True. Annotate the Milestone with `tideproject.k8s/approve-milestone=true`; Eventually the Milestone reaches Status.Phase=Succeeded and the annotation is consumed (gone from Annotations map).
    - Test 2 (TestRejectHalts): Apply a Project; mid-run, annotate it `tideproject.k8s/reject: "operator stop"`; Eventually all in-flight Milestone/Phase/Plan/Task CRDs have Status.Phase=Failed with a Condition carrying Reason=RejectedByUser and Message containing "operator stop".
    - Test 3 (TestWavePauseBetweenWaves): Apply a Project with `gates.pauseBetweenWaves=true`; apply a Plan with 2 waves of tasks; complete wave 1; assert Plan has Condition WaveOrLevelPaused True with Reason=PausedAtBoundary. Annotate plan `tideproject.k8s/approve-wave-2=true`; Eventually wave 2 dispatches and the Condition flips False.
  </behavior>
  <action>
    Create `test/integration/envtest/gates_test.go` (new file). Use the existing envtest scaffolding from `internal/controller/suite_test.go` (BeforeSuite registers the same scheme + starts envtest). Mirror the assertion style of `internal/controller/project_phase3_test.go` (Gomega Eventually with 30s timeout, 250ms poll interval).

    For test fixtures: declare YAML strings inline as `_ = ` heredoc-style constants and `kubectl.Apply` them via the controller-runtime client. Use `tideprojectv1alpha1.Gates{Milestone: gates.PolicyApprove}` directly (no YAML hop for the gate config — set in code).

    Three subtests in one parent test function. Each subtest creates its own namespace via uuid-suffix to isolate state. Use `client.Patch + client.MergeFrom` to set annotations. Use Gomega `Eventually` to poll for Status.Phase changes. After each subtest, clean up the namespace (`Expect(k8sClient.Delete(ctx, &ns)).To(Succeed())`).

    The new file should compile against the existing `internal/controller` package's exported helpers (use `internal/controller/suite_test.go`'s shared setup). If new test scaffolding is needed, add it to a separate `gates_envtest_suite_test.go` in the same directory rather than disturb the existing controller suite.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./test/integration/envtest/... -run Gate -race -timeout 5m -v</automated>
  </verify>
  <done>Three envtest subtests pass against envtest's fake apiserver; gate approve, reject, and wave-pause flows are demonstrably integration-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project CRD .spec.gates → reconciler decision | Reconciler MUST consult the gate seam EVERY level boundary — a bypass is a privilege escalation (T-04-G2). |
| Annotation → reconciler advance | Approve annotations are operator-attestation; ConsumeApprove ensures one-shot semantics so a stale annotation does not silently re-approve a subsequent boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-G1 | Spoofing / privilege escalation | internal/controller/*_controller.go | mitigate | All four up-stack reconcilers call gates.EvaluatePolicy at the same seam. Grep assertion in CI (Task 1 verify) confirms 4 call sites. CEL validation on the CRD restricts values to {auto, approve, pause}. |
| T-04-G2 | Tampering (replay-approval) | internal/controller/*_controller.go | mitigate | Annotation is consumed via ConsumeApprove → r.Patch BEFORE patchSucceeded fires. Replay would require a fresh annotation write — and the next level's gate check would block independently. |
| T-04-G3 | Spoofing (wave-skip) | internal/controller/plan_controller.go | mitigate | CheckWaveApprove keys on integer wave index; approval for wave N does not approve wave N+1. Test 2 of Task 2 asserts this. |
| T-04-G4 | DoS (gate-pause infinite loop) | internal/controller/*_controller.go | mitigate | When paused, reconciler returns `ctrl.Result{}, nil` (no requeue) — only AnnotationChangedPredicate triggers re-reconcile when an annotation is written. No polling loop. |
</threat_model>

<verification>
- `go test ./internal/controller/... -run Gate -race -v` passes.
- `go test ./test/integration/envtest/... -run Gate -race -timeout 5m -v` passes.
- `grep -c "gates.EvaluatePolicy" internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go internal/controller/task_controller.go` returns ≥ 4.
- `grep -c "AnnotationChangedPredicate" internal/controller/{milestone,phase,plan,task,wave}_controller.go` returns ≥ 5.
- `grep -c "gates.CheckWaveApprove" internal/controller/plan_controller.go` returns ≥ 1.
- `make tide-lint` passes.
</verification>

<success_criteria>
- 5 reconcilers modified — Milestone, Phase, Plan, Task, Wave (Wave gets the predicate only; the others get the policy hook + predicate).
- 4 new patch<Level>AwaitingApproval helpers exist alongside existing patch<Level>Succeeded/Failed helpers.
- PlanReconciler honors PauseBetweenWaves with annotation-driven unblock.
- Reject annotation halts all in-flight work with structured Condition Reason=RejectedByUser.
- New envtest at test/integration/envtest/gates_test.go covers approve/reject/wave-pause end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-05-SUMMARY.md` documenting the 4 reconciler seams modified, the new patch<Level>AwaitingApproval helpers, the PauseBetweenWaves flow in PlanReconciler, and the envtest coverage matrix.
</output>
