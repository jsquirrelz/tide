---
phase: 04
plan: 16
type: execute
wave: 9
depends_on: [04-13]
files_modified:
  - dashboard/web/src/components/PodLogStreamer.tsx
  - dashboard/web/src/components/PodLogStreamer.test.tsx
  - dashboard/web/src/components/EmptyState.tsx
  - dashboard/web/src/components/EmptyState.test.tsx
  - dashboard/web/src/components/ErrorState.tsx
  - dashboard/web/src/components/ErrorState.test.tsx
  - dashboard/web/src/components/LoadingState.tsx
  - dashboard/web/src/lib/sse.ts
  - dashboard/web/src/lib/sse.test.ts
  - dashboard/web/src/lib/ansi.ts
  - dashboard/web/src/lib/ansi.test.ts
  - dashboard/web/src/__tests__/bundle-size.test.ts
  - dashboard/web/src/App.tsx
  - cmd/dashboard/embed/dist/
  - Makefile
ui_gap: []
autonomous: true
requirements: [DASH-03, DASH-04]
threat_model_id: T-04-D1,T-04-D-bundle-bloat,T-04-D-eventsource-leak
must_haves:
  truths:
    - "PodLogStreamer streams via SSE to GET /api/v1/tasks/{name}/log; closes EventSource on unmount (Pitfall 22 client-side)"
    - "useSSEStream + useTaskLog hooks reconnect on transient disconnect with browser-native EventSource Last-Event-ID semantics per RESEARCH §768-786"
    - "ansi.ts parses SGR codes 30-37 / 90-97 / 40-47 / 100-107 / 1 / 22 / 39 / 49 / 0 only (UI-SPEC §8 scope) and strips others; file stays ≤ 200 lines"
    - "PodLogStreamer ring-buffer caps at 5000 lines; auto-scroll toggles when user scrolls up/down"
    - "Full-screen ErrorState ERR1 (backend-unreachable) and ERR2 (permission-denied) render the verbatim UI-SPEC copy + kubectl hint lines"
    - "EmptyState E1/E2/E3 render the verbatim UI-SPEC §13 copy (no-projects with ASCII wave; awaiting-first-milestone; plan-accepted-no-tasks)"
    - "LoadingState L1/L2/L3 (initial / pane / drawer skeleton) use Loader2 lucide icon with animate-spin"
    - "Bundle-size CI gate enforced: `npm run build` produces dist/ with total gzipped JS+CSS ≤ 500KB; the bundle-size.test.ts asserts this via fs read + zlib.gzipSync"
    - "Makefile target `dashboard-frontend` builds dashboard/web/dist/ and copies to cmd/dashboard/embed/dist/; `dashboard-build` then compiles the Go binary with the bundled SPA"
  artifacts:
    - path: dashboard/web/src/components/PodLogStreamer.tsx
      provides: "Inline log streamer with SSE, ring-buffer, ANSI rendering, pause/resume/copy/line-wrap toolbar"
      contains: "EventSource"
    - path: dashboard/web/src/lib/sse.ts
      provides: "useSSEStream(url) + useTaskLog(taskName) hooks with exponential backoff and cleanup-on-unmount"
      contains: "EventSource"
    - path: dashboard/web/src/lib/ansi.ts
      provides: "parseAnsi(line: string): AnsiSegment[] — 80-line bounded parser"
      contains: "SGR"
    - path: dashboard/web/src/__tests__/bundle-size.test.ts
      provides: "Asserts dist/ gzipped total ≤ 500KB"
      contains: "500"
    - path: Makefile
      provides: "dashboard-frontend + dashboard-build targets"
      contains: "dashboard-frontend"
  key_links:
    - from: dashboard/web/src/lib/sse.ts
      to: cmd/dashboard/api/events_sse.go
      via: "GET /api/v1/projects/{name}/events?stream=sse + browser-native Last-Event-ID header"
      pattern: "EventSource"
    - from: dashboard/web/src/components/PodLogStreamer.tsx
      to: dashboard/web/src/lib/ansi.ts
      via: "parseAnsi() called per-line in render"
      pattern: "parseAnsi"
    - from: cmd/dashboard/embed/dist
      to: dashboard/web/dist
      via: "Makefile dashboard-frontend target copies the Vite build output"
      pattern: "cp -r dist"
---

<objective>
Close out the frontend with the SSE consumption layer (`useSSEStream` + `useTaskLog`), the `<PodLogStreamer>` component (consumed by the drawer's "Open log stream" callback wired in plan 04-13), the remaining full-screen state components (EmptyState E1/E2/E3, ErrorState ERR1/ERR2, LoadingState L1/L2/L3), the ANSI parser, the bundle-size CI gate, and the Makefile glue that embeds the Vite build into `cmd/dashboard/embed/dist/`. After this plan: the dashboard is end-to-end functional inside a kind cluster — backend serves SSE, frontend renders DAGs + drawer + live log streamer + error/empty/loading states; bundle stays under 500KB gzipped.

Purpose: Splitting this from plan 04-13 keeps each plan ≤ 15 files and gives the SSE/streaming/bundle-gate layer the focus it needs. Plan 04-13 left explicit seams (the drawer's `onOpenLogStream` callback, the DAG views' "extend with SSE here" comments) that this plan plugs into.

Output: 14 files. The Makefile `dashboard-frontend` target produces `cmd/dashboard/embed/dist/` from the Vite build; `dashboard-build` produces `bin/dashboard` with the embedded bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md
@.planning/phases/04-gates-observability-dashboard-cli/04-11-PLAN.md
@.planning/phases/04-gates-observability-dashboard-cli/04-13-PLAN.md

<interfaces>
<!-- SSE hook (Pitfall 22 client-side + Pitfall 23 server-side mitigated) -->

```ts
// dashboard/web/src/lib/sse.ts
export function useSSEStream(url: string): {
  events: Event[];
  state: "connecting" | "connected" | "reconnecting" | "offline";
  lastEventId: number;
};

export function useTaskLog(taskName: string, opts?: { tail?: number }): {
  lines: string[];
  state: "connecting" | "connected" | "reconnecting" | "offline" | "idle-closed";
};
```

<!-- ANSI parser scope — strictly UI-SPEC §8 -->

Supported SGR codes:
- 30-37 (fg colors), 90-97 (bright fg), 40-47 (bg colors), 100-107 (bright bg)
- 1 (bold), 22 (no-bold)
- 39 (default fg), 49 (default bg)
- 0 (reset)

Everything else: strip the entire `\x1b[NNm` sequence; pass remaining text through.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sse.ts hooks + ansi.ts parser + EmptyState / ErrorState / LoadingState components</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§8 PodLogStreamer scope — supported SGR codes; §13 EmptyState E1/E2/E3 verbatim copy; §14 ErrorState ERR1/ERR2 verbatim copy + kubectl hint lines)
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 768-786 EventSource reconnect semantics)
    - cmd/dashboard/api/events_sse.go (from plan 04-11 — backend contract for SSE event shape)
    - cmd/dashboard/api/logs_sse.go (from plan 04-11 — pods/log SSE event shape)
  </read_first>
  <behavior>
    - Test 1 (sse.ts useSSEStream): Mock global EventSource; the hook subscribes to the URL; on `message` events, state.events array grows; on `error`, state.state flips to "reconnecting"; on cleanup (unmount), EventSource.close() is called exactly once.
    - Test 2 (sse.ts useTaskLog): URL is /api/v1/tasks/{name}/log; each new line emits a state update; ring-buffer cap (5000) honored — sending 6000 lines results in `lines.length === 5000` (oldest dropped).
    - Test 3 (Last-Event-ID reconnect): On reconnect after disconnect, EventSource is re-instantiated with the same URL — browser-native EventSource automatically sends Last-Event-ID header. Test by asserting EventSource constructor called twice (initial + reconnect).
    - Test 4 (ANSI parser scope): `parseAnsi("\x1b[31mERROR\x1b[0m normal\x1b[1;36mBOLD CYAN\x1b[0m")` returns `[{text:"ERROR", color:"red"}, {text:" normal"}, {text:"BOLD CYAN", color:"cyan", bold:true}]`.
    - Test 5 (ANSI parser unsupported codes): `parseAnsi("\x1b[38;5;208morange\x1b[0m")` (256-color) returns `[{text:"orange"}]` — unsupported codes stripped, text passes through without crash.
    - Test 6 (ANSI parser bounds): Source-level assertion that ansi.ts is ≤ 200 lines (UI-SPEC §8 "80-line scope" + test/import overhead allowance).
    - Test 7 (EmptyState E1 no-projects): `<EmptyState variant="no-projects" />` renders the verbatim UI-SPEC copy heading "No projects in this cluster", body containing "Apply one with `tide apply -f project.yaml`", and the ASCII wave decoration as a `<pre>` block.
    - Test 8 (EmptyState E2 + E3): variants "awaiting-first-milestone" and "plan-accepted-no-tasks" render their verbatim §13 copy.
    - Test 9 (ErrorState ERR1 backend-unreachable): `<ErrorState variant="backend-unreachable" url="http://localhost:8080" />` renders heading "Dashboard backend unreachable" + the URL in the body + the kubectl hint lines verbatim from UI-SPEC §14.
    - Test 10 (ErrorState ERR2 permission-denied): variant "permission-denied" renders the verbatim §14 copy + RBAC kubectl hint.
    - Test 11 (LoadingState L1/L2/L3): variants "initial", "pane", "drawer" each render Loader2 lucide icon with `animate-spin` className.
  </behavior>
  <action>
    Create `dashboard/web/src/lib/sse.ts` exposing the two hooks per the interfaces block. Implementation:
    - `useSSEStream(url)`: useEffect creates `new EventSource(url)`; handlers for open/message/error update React state; cleanup function closes the EventSource. Last-Event-ID is browser-native — no explicit header handling needed.
    - `useTaskLog(taskName, opts)`: built on useSSEStream; transforms `data: <line>\n\n` events to `string[]` state; ring-buffer at 5000 lines (drop oldest when full).

    Create `dashboard/web/src/lib/sse.test.ts` covering tests 1-3. Mock global EventSource via `vi.stubGlobal('EventSource', class { ... })` that the test pumps events into.

    Create `dashboard/web/src/lib/ansi.ts` — 80-line bounded parser per UI-SPEC §8:
    - Regex split input on `\x1b\[[0-9;]*m`; for each escape sequence, parse params into a list of SGR codes; for each code in supported set, update current attribute state; for unsupported codes, ignore.
    - Emit segments `{text: string, color?: string, bgColor?: string, bold?: boolean}`.
    - Export `parseAnsi(line: string): AnsiSegment[]`.

    Create `dashboard/web/src/lib/ansi.test.ts` covering tests 4-6.

    Create `dashboard/web/src/components/EmptyState.tsx`:
    - Variants `"no-projects" | "awaiting-first-milestone" | "plan-accepted-no-tasks"` mapping to UI-SPEC §13 E1/E2/E3 copy.
    - E1 includes an ASCII wave decoration inline as a `<pre>` block per UI-SPEC §13.

    Create `dashboard/web/src/components/ErrorState.tsx`:
    - Variants `"backend-unreachable" | "permission-denied"` (ERR1 / ERR2).
    - Full-screen card layout.
    - Includes the `kubectl` hint lines verbatim from UI-SPEC §14.

    Create `dashboard/web/src/components/LoadingState.tsx`:
    - Variants `"initial" | "pane" | "drawer"` (L1 / L2 / L3).
    - Spinner uses Loader2 lucide icon with `animate-spin`.

    Tests in `EmptyState.test.tsx` (tests 7-8), `ErrorState.test.tsx` (tests 9-10). LoadingState test (test 11) lives in `EmptyState.test.tsx` or its own file — planner's choice during execution.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide/dashboard/web &amp;&amp; npm run test -- sse ansi EmptyState ErrorState LoadingState &amp;&amp; wc -l src/lib/ansi.ts | awk '{ if ($1 &gt; 200) { print "FAIL: ansi.ts exceeds 200 lines"; exit 1 } else { print "PASS:", $1, "lines" } }'</automated>
  </verify>
  <done>SSE hooks reconnect + cleanup correctly; ANSI parser passes all 3 behaviors + line-count bound; EmptyState/ErrorState/LoadingState render verbatim UI-SPEC copy; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PodLogStreamer + bundle-size gate + Makefile embed wiring + App.tsx PodLogStreamer mount</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§8 PodLogStreamer — Connecting/Connected-no-lines/Disconnected states with locked copy; ring-buffer; auto-scroll behavior)
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§Stack & Bundle Targets — <500KB gzipped)
    - cmd/dashboard/embed/embed.go (from plan 04-10 — `//go:embed all:dist` target)
    - dashboard/web/src/components/TaskDetailDrawer.tsx (from plan 04-13 — `onOpenLogStream` callback seam)
  </read_first>
  <behavior>
    - Test 1 (PodLogStreamer mount + EventSource open): Mount `<PodLogStreamer taskName="t1" onClose={fn} />`; mock useTaskLog to return `state: "connected"` + 3 sample lines; assert the 3 lines appear in the viewport. Unmount → assert mock EventSource close called.
    - Test 2 (ring-buffer): Feed 6000 lines via mocked useTaskLog; viewport state's lines array is capped at 5000 (verified via DOM-rendered count or mock state inspection).
    - Test 3 (auto-scroll): User scrolls up → autoScroll switches to false (verify via state hook); user scrolls back to bottom → autoScroll = true. New lines append only when autoScroll is true.
    - Test 4 (ANSI rendering): Sample line `"\x1b[31mERROR\x1b[0m fail"` renders with `<span style={{color: 'red'}}>ERROR</span><span> fail</span>` — assert via `getByText` + `getComputedStyle` checks.
    - Test 5 (Connection state copy): When state="connecting", UI shows the verbatim UI-SPEC §8 "Connecting…" copy; "Connected, no lines yet" copy when state="connected" + lines empty; "Disconnected" copy when state="offline".
    - Test 6 (App.tsx PodLogStreamer mount): When `streamingTask` state (set by drawer's `onOpenLogStream`) is non-null, App.tsx mounts `<PodLogStreamer taskName={streamingTask} onClose={() => setStreamingTask(null)} />` as an inline panel below the drawer; the drawer's "Open log stream" button click → streamingTask is set → PodLogStreamer mounts.
    - Test 7 (bundle-size gate): A Vitest test reads dist/ directory after build, gzip-compresses each .js/.css file, sums totals, asserts total ≤ 500 * 1024 bytes (gzipped). Skips gracefully if dist/ doesn't exist (pre-build smoke).
    - Test 8 (Makefile dashboard-frontend): `make dashboard-frontend` runs `npm ci + npm run build` in dashboard/web/, then copies the Vite output to cmd/dashboard/embed/dist/. After the target runs, `cmd/dashboard/embed/dist/index.html` exists and contains the Vite-built (non-placeholder) markup.
    - Test 9 (go embed end-to-end): `cd cmd/dashboard && go build -o /tmp/dashboard` succeeds and the binary's go-embedded SPA contains the real Vite-built index.html, not the placeholder from plan 04-10.
  </behavior>
  <action>
    Create `dashboard/web/src/components/PodLogStreamer.tsx`:
    - Props: `taskName: string; onClose: () => void`.
    - Uses `useTaskLog(taskName)` from sse.ts. Lines state with ring-buffer cap 5000.
    - Toolbar: pause/resume + Copy logs + line-wrap toggle + close.
    - Viewport: scrollable div, mono font, each line in `<div className="whitespace-pre">` (or pre-wrap based on toggle). Lines pass through `parseAnsi` before render — segments become inline `<span style={{color, fontWeight: bold ? 600 : 400}}>`.
    - Connection state UI per UI-SPEC §8: Connecting / Connected-no-lines / Disconnected states with locked copy.
    - Auto-scroll: scroll-event handler toggles `autoScroll` state; new lines append only when true; scrolling back to bottom re-enables.

    Create `dashboard/web/src/components/PodLogStreamer.test.tsx` covering tests 1-5. Mock `useTaskLog` via `vi.mock("../lib/sse")` to control lines + state.

    Create `dashboard/web/src/__tests__/bundle-size.test.ts`:
    ```ts
    import { describe, it, expect } from "vitest";
    import { readFileSync, existsSync, readdirSync } from "fs";
    import { join } from "path";
    import { gzipSync } from "zlib";

    describe("Bundle size gate", () => {
      it("dist/ gzipped total <= 500KB", () => {
        const distDir = join(__dirname, "..", "..", "dist");
        if (!existsSync(distDir)) return;  // pre-build smoke
        const assets = join(distDir, "assets");
        const files = readdirSync(assets).filter(f => f.endsWith(".js") || f.endsWith(".css"));
        const total = files.map(f => gzipSync(readFileSync(join(assets, f))).byteLength).reduce((a, b) => a + b, 0);
        expect(total).toBeLessThanOrEqual(500 * 1024);
      });
    });
    ```

    Update root `Makefile` adding:
    ```makefile
    dashboard-frontend:
    	cd dashboard/web && npm ci && npm run build && npm run test
    	rm -rf cmd/dashboard/embed/dist
    	cp -r dashboard/web/dist cmd/dashboard/embed/dist

    dashboard-build: dashboard-frontend
    	go build -o bin/dashboard ./cmd/dashboard
    ```

    Update `dashboard/web/src/App.tsx` (from plan 04-13) to add `streamingTask` state + render `<PodLogStreamer taskName={streamingTask} onClose={() => setStreamingTask(null)} />` when `streamingTask !== null`. The drawer's `onOpenLogStream` callback now ends with `setStreamingTask(taskName)`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; make dashboard-frontend &amp;&amp; cd dashboard/web &amp;&amp; npm run test &amp;&amp; cd /Users/justinsearles/Projects/tide &amp;&amp; ls cmd/dashboard/embed/dist/index.html &amp;&amp; (cat cmd/dashboard/embed/dist/index.html | grep -q "TIDE" || (echo "FAIL: placeholder not replaced"; exit 1))</automated>
  </verify>
  <done>PodLogStreamer streams via SSE with ANSI parsing + ring-buffer + cleanup; bundle-size gate enforces <500KB; Makefile target builds frontend into cmd/dashboard/embed; binary builds with the real bundle embedded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE EventSource → browser memory | useTaskLog ring-buffers at 5000 lines to bound memory; runaway log streams cannot DoS the browser tab. |
| ANSI escape input → DOM render | parseAnsi strips unsupported SGR codes; output is plain text + style objects, NOT inline HTML. Test 4 verifies. |
| Vite dist/ → Go embed.FS | Build-time embed; bundle gate (500KB gzipped) is CI-enforced; the operator gets a single binary with a predictably-sized SPA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-D1 | XSS (via log content) | dashboard/web/src/components/PodLogStreamer.tsx | mitigate | parseAnsi returns plain text + style props; rendered via React text nodes (escape-by-default). NO innerHTML usage. |
| T-04-D-bundle-bloat | DoS (slow page load) | dashboard/web/src/__tests__/bundle-size.test.ts | mitigate | Bundle gate <500KB gzipped enforced via Vitest; CI fails if exceeded. UI-SPEC §Stack & Bundle Targets lists forbidden dependencies. |
| T-04-D-eventsource-leak | DoS (browser memory leak) | dashboard/web/src/lib/sse.ts | mitigate | useEffect cleanup function calls EventSource.close() — Pitfall 22 client-side. useTaskLog ring-buffers at 5000 lines. Test 1 verifies cleanup. |
</threat_model>

<verification>
- `cd dashboard/web && npm run test && npm run build` succeeds.
- `make dashboard-frontend` succeeds; `cmd/dashboard/embed/dist/index.html` is the Vite-built bundle (NOT the placeholder).
- `make dashboard-build` produces `bin/dashboard` with embedded bundle.
- `cd dashboard/web && du -sh dist/assets/*.js | head` reports asset sizes — gzipped sum < 500KB.
- `rg "dangerouslySetInnerHTML" dashboard/web/src/` returns zero hits (final cross-plan assertion).
- `wc -l dashboard/web/src/lib/ansi.ts` returns ≤ 200 (UI-SPEC §8 80-line scope).
- All Vitest tests pass.
</verification>

<success_criteria>
- PodLogStreamer streams via SSE with ANSI parsing + ring-buffer + cleanup + auto-scroll.
- useSSEStream + useTaskLog hooks reconnect + cleanup correctly (Pitfall 22).
- ANSI parser stays under 200 lines and handles unsupported codes gracefully.
- EmptyState (E1/E2/E3) + ErrorState (ERR1/ERR2) + LoadingState (L1/L2/L3) render verbatim UI-SPEC copy.
- Bundle-size CI gate <500KB gzipped enforced.
- Frontend bundle embedded into cmd/dashboard binary via Makefile.
- App.tsx mounts PodLogStreamer when streamingTask state is set (the drawer's onOpenLogStream seam from plan 04-13).
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-16-SUMMARY.md` documenting the SSE hook shapes, the ANSI parser scope, the ring-buffer + auto-scroll behavior, the bundle-size threshold, the Makefile glue from dashboard/web/dist → cmd/dashboard/embed/dist, and the end-to-end mount path (drawer → setStreamingTask → PodLogStreamer).
</output>
