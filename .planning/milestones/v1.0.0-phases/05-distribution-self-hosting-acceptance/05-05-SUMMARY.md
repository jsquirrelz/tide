---
phase: 05-distribution-self-hosting-acceptance
plan: 05
subsystem: infra
tags: [helm, chart-version, lockstep-bump, helmify, release-readiness]

# Dependency graph
requires:
  - phase: 02-distribution
    provides: "hack/helm/ SOT files + augment scripts; charts/tide/ + charts/tide-crds/ helmify-generated trees"
provides:
  - "Chart.yaml metadata bumped from 0.1.0-dev → 1.0.0 lockstep across tide + tide-crds (D-X3 enforced)"
  - "DIST-01 chart-version-readiness gate satisfied — chart pair ready for OCI publish (Plan 05-16)"
affects: ["05-16 (OCI publish)", "05-14 (CRD subchart annotation)", "future helm upgrade testing plans"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Helm chart-pair lockstep versioning (D-X3): tide + tide-crds Chart.yaml versions advance together in a single commit"
    - "SOT-driven chart metadata: hack/helm/*.yaml is the planning surface; charts/*/Chart.yaml is derived via augment scripts"

key-files:
  created:
    - ".planning/phases/05-distribution-self-hosting-acceptance/05-05-SUMMARY.md"
    - ".planning/phases/05-distribution-self-hosting-acceptance/deferred-items.md"
  modified:
    - "hack/helm/tide-chart.yaml — version: 0.1.0-dev → 1.0.0, appVersion: 0.1.0-dev → 1.0.0"
    - "hack/helm/tide-crds-chart.yaml — version: 0.1.0-dev → 1.0.0, appVersion: 0.1.0-dev → 1.0.0"
    - "charts/tide/Chart.yaml — regenerated by augment-tide-chart.sh; mirrors SOT (derived)"
    - "charts/tide-crds/Chart.yaml — regenerated by augment-tide-crds-chart.sh; mirrors SOT (derived)"

key-decisions:
  - "Reverted out-of-scope augment side-effects (charts/tide/values.yaml + charts/tide/templates/projects-pvc.yaml) — pre-existing SOT-vs-chart-tree drift in tide-values.yaml/projects-pvc.yaml, logged to deferred-items.md per Rule 4 (architectural drift, not blocking this plan)"

patterns-established:
  - "Lockstep bump audit: single commit modifies both SOT files + both derived Chart.yaml files (4 paths, 16 line changes — symmetric pair)"
  - "Selective augment-script side-effect management: when augment-tide-chart.sh's `cp` calls regress unrelated chart-tree state from stale SOT siblings, revert the unrelated paths to keep plan scope clean"

requirements-completed: [DIST-01]

# Metrics
duration: ~5min
completed: 2026-05-21
---

# Phase 05 Plan 05: Helm Chart Pair Lockstep Bump 0.1.0-dev → 1.0.0 Summary

**Helm chart version bumped from `0.1.0-dev` to `1.0.0` lockstep across both `tide` and `tide-crds` charts via the hack/helm SOT files + augment scripts (D-X3 lockstep enforced; DIST-01 release-readiness gate satisfied).**

## Performance

- **Duration:** ~5 min
- **Completed:** 2026-05-22T16:36:11Z
- **Tasks:** 1
- **Files modified:** 4 (2 SOT + 2 derived)
- **Files created:** 2 (this SUMMARY + deferred-items.md)

## Accomplishments

- `hack/helm/tide-chart.yaml` and `hack/helm/tide-crds-chart.yaml` now both read `version: 1.0.0` + `appVersion: "1.0.0"` (D-X3 lockstep — single commit lands both bumps).
- `charts/tide/Chart.yaml` and `charts/tide-crds/Chart.yaml` regenerated via augment scripts; mirrors of the SOT.
- No `0.1.0-dev` literals remain in any `Chart.yaml` file across the repo (verified via `grep -rn` returning 0 hits across the 4 in-scope files).
- `helm lint charts/tide` and `helm lint charts/tide-crds` both exit 0.

## Task Commits

1. **Task 1: Lockstep bump 0.1.0-dev → 1.0.0 in SOT chart metadata** — `3aa94fa` (feat)
   - Touched: `hack/helm/tide-chart.yaml`, `hack/helm/tide-crds-chart.yaml`, `charts/tide/Chart.yaml`, `charts/tide-crds/Chart.yaml`
   - Plus this SUMMARY.md and `deferred-items.md` rolled into the same commit per parallel executor protocol.

## Files Created/Modified

### Modified

- `hack/helm/tide-chart.yaml` — `version: 0.1.0-dev` → `version: 1.0.0`; `appVersion: "0.1.0-dev"` → `appVersion: "1.0.0"`. SOT for `charts/tide/Chart.yaml`.
- `hack/helm/tide-crds-chart.yaml` — `version: 0.1.0-dev` → `version: 1.0.0`; `appVersion: "0.1.0-dev"` → `appVersion: "1.0.0"`. SOT for `charts/tide-crds/Chart.yaml`.
- `charts/tide/Chart.yaml` — derived from SOT via `bash hack/helm/augment-tide-chart.sh`. Mirrors SOT values.
- `charts/tide-crds/Chart.yaml` — derived from SOT via `bash hack/helm/augment-tide-crds-chart.sh`. Mirrors SOT values.

### Created

- `.planning/phases/05-distribution-self-hosting-acceptance/05-05-SUMMARY.md` — this file.
- `.planning/phases/05-distribution-self-hosting-acceptance/deferred-items.md` — documents pre-existing SOT-vs-chart-tree drift in `hack/helm/tide-values.yaml` + `hack/helm/projects-pvc.yaml` (out of scope; future Phase 05 plan should resync).

## Verification

Per the plan's `<verify><automated>` block — all 9 acceptance criteria pass:

| Criterion | Command | Result |
|-----------|---------|--------|
| SOT tide-chart.yaml version | `grep -c "^version: 1.0.0$" hack/helm/tide-chart.yaml` | `1` |
| SOT tide-chart.yaml appVersion | `grep -c '^appVersion: "1.0.0"$' hack/helm/tide-chart.yaml` | `1` |
| SOT tide-crds-chart.yaml version | `grep -c "^version: 1.0.0$" hack/helm/tide-crds-chart.yaml` | `1` |
| SOT tide-crds-chart.yaml appVersion | `grep -c '^appVersion: "1.0.0"$' hack/helm/tide-crds-chart.yaml` | `1` |
| Derived charts/tide/Chart.yaml version | `grep -c "^version: 1.0.0$" charts/tide/Chart.yaml` | `1` |
| Derived charts/tide/Chart.yaml appVersion | `grep -c '^appVersion: "1.0.0"$' charts/tide/Chart.yaml` | `1` |
| Derived charts/tide-crds/Chart.yaml version | `grep -c "^version: 1.0.0$" charts/tide-crds/Chart.yaml` | `1` |
| Derived charts/tide-crds/Chart.yaml appVersion | `grep -c '^appVersion: "1.0.0"$' charts/tide-crds/Chart.yaml` | `1` |
| No residual 0.1.0-dev | `grep -rn "0\.1\.0-dev" charts/*/Chart.yaml hack/helm/tide-chart.yaml hack/helm/tide-crds-chart.yaml` | `0 matches` |

### Augment-script exit codes

- `bash hack/helm/augment-tide-chart.sh` → exit 0 (prints `OK: deployment.yaml Phase 2 + Phase 3 + Phase 4 fields injected (idempotent)` + `OK: charts/tide/ augmented with project-specific values + ConfigMap template`).
- `bash hack/helm/augment-tide-crds-chart.sh` → exit 0 (prints `OK: charts/tide-crds/ augmented with TIDE Chart.yaml metadata`).

### Helm lint exit codes

- `helm lint charts/tide` → exit 0 (`1 chart(s) linted, 0 chart(s) failed`; one pre-existing INFO about chart icon — unrelated to this plan).
- `helm lint charts/tide-crds` → exit 0 (`1 chart(s) linted, 0 chart(s) failed`; same icon INFO).

## Decisions Made

1. **Reverted out-of-scope augment side-effects.** Running `bash hack/helm/augment-tide-chart.sh` also `cp`'s `hack/helm/tide-values.yaml` → `charts/tide/values.yaml` and `hack/helm/projects-pvc.yaml` → `charts/tide/templates/projects-pvc.yaml`. The SOT sibling files for values/PVC are stale relative to the current chart-tree (missing `podAnnotations`, `accessModes` templated loop, etc.). Per the plan's explicit "Do NOT touch `charts/tide/values.yaml`" + CLAUDE.md's "Edits to `charts/tide/values.yaml` — chart is FIXED contract" rule, the unrelated regressions were reverted via `git checkout -- charts/tide/values.yaml charts/tide/templates/projects-pvc.yaml`. Drift is logged in `deferred-items.md` for a future SOT-resync plan to address.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4-adjacent — Architectural Scope] Reverted unrelated augment-script side-effects to preserve plan scope**

- **Found during:** Task 1, verification step after running augment scripts.
- **Issue:** `bash hack/helm/augment-tide-chart.sh` mutated `charts/tide/values.yaml` (dropped `podAnnotations: {}` + `accessModes: [ReadWriteMany]`) and `charts/tide/templates/projects-pvc.yaml` (regressed templated `accessModes` loop to hardcoded literal) because the augment script unconditionally copies `hack/helm/tide-values.yaml` and `hack/helm/projects-pvc.yaml`, which are stale SOT siblings that have drifted behind the chart-tree.
- **Fix:** `git checkout -- charts/tide/values.yaml charts/tide/templates/projects-pvc.yaml` after running augment scripts. Logged drift in `.planning/phases/05-distribution-self-hosting-acceptance/deferred-items.md`.
- **Why not auto-fix the SOT directly:** Plan 05-05's `files_modified` is explicitly the two Chart.yaml SOT files. CLAUDE.md `Working Rules → 2. Execute, Don't Ask → Exceptions` lists "Edits to `charts/tide/values.yaml`" as an "always confirm or refuse" exception. The drift is pre-existing (not caused by this plan) and is properly scoped to a follow-up SOT-resync plan.
- **Files modified by revert:** `charts/tide/values.yaml`, `charts/tide/templates/projects-pvc.yaml` (both reverted to HEAD state).
- **Verification:** Final `git diff --stat` shows only 4 files / 16 lines (symmetric SOT + derived pair) — the expected lockstep-bump diff shape.

---

**Total deviations:** 1 architectural-scope deviation (auto-handled by reverting + deferring).
**Impact on plan:** None on the lockstep bump itself. The deferred SOT drift is documented for a future plan to address.

## Issues Encountered

The augment-script side-effect described above. No other issues.

## Threat Flags

None — no new security-relevant surface introduced. Threats T-05-05-01 (chart pair version drift) and T-05-05-02 (helmify regen drift) are both mitigated by the symmetric lockstep commit + idempotent augment scripts.

## User Setup Required

None — no external service configuration required for a chart version bump.

## Next Phase Readiness

- **Plan 05-16 (OCI publish):** chart pair is now at `1.0.0` and ready for stable release artifact production. The lockstep rule held.
- **Plan 05-14 (CRD subchart `helm.sh/resource-policy: keep` injection):** runs in Wave 1 alongside this plan. The two are orthogonal (annotation injection vs. version bump); no merge conflict expected.
- **Deferred item:** SOT-vs-chart-tree drift in `hack/helm/tide-values.yaml` and `hack/helm/projects-pvc.yaml` should be resolved by a future Phase 05 plan before any subsequent run of `augment-tide-chart.sh` outside an explicit resync context.

## Self-Check

Files claimed:

- `[FOUND]` `hack/helm/tide-chart.yaml` carries `version: 1.0.0` + `appVersion: "1.0.0"` (verified via grep)
- `[FOUND]` `hack/helm/tide-crds-chart.yaml` carries `version: 1.0.0` + `appVersion: "1.0.0"` (verified via grep)
- `[FOUND]` `charts/tide/Chart.yaml` carries `version: 1.0.0` + `appVersion: "1.0.0"` (verified via grep)
- `[FOUND]` `charts/tide-crds/Chart.yaml` carries `version: 1.0.0` + `appVersion: "1.0.0"` (verified via grep)
- `[FOUND]` `.planning/phases/05-distribution-self-hosting-acceptance/deferred-items.md` exists
- `[FOUND]` `.planning/phases/05-distribution-self-hosting-acceptance/05-05-SUMMARY.md` exists (this file)

Commit hash: `3aa94fa` — verified via `git log --oneline -1`.

## Self-Check: PASSED

---
*Phase: 05-distribution-self-hosting-acceptance*
*Plan: 05*
*Completed: 2026-05-21*
