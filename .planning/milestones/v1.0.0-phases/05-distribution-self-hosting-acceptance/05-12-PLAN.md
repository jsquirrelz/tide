---
phase: 05-distribution-self-hosting-acceptance
plan: 12
type: execute
wave: 3
depends_on: [06, 11]
files_modified:
  - cmd/tide-demo-init/main.go
  - cmd/tide-demo-init/main_test.go
  - cmd/tide-demo-init/README.md
  - images/tide-demo-init/Dockerfile
  - examples/projects/medium/namespace.yaml
  - examples/projects/medium/project.yaml
  - examples/projects/medium/demo-remote-pvc.yaml
  - examples/projects/medium/demo-remote-init-job.yaml
  - examples/projects/medium/README.md
autonomous: true
requirements: [DIST-04]
tags: [examples, medium-sample, demo-init, local-only-git-remote]

must_haves:
  truths:
    - "cmd/tide-demo-init/ is a small Go binary that initializes a bare git repo at --bootstrap-dir and populates it from embedded examples/tide-demo-fixture/ content (D-B3 + RESEARCH §Topic 4 Option b)"
    - "Embed strategy RESOLVED at planning time per MEDIUM-11: //go:embed all:fixture directive in main.go points at a build-time-positioned `cmd/tide-demo-init/fixture/` directory; the Dockerfile COPYs `examples/tide-demo-fixture/` to that path at image build (no symlink-vs-go:generate ambiguity)"
    - "images/tide-demo-init/Dockerfile builds a distroless image suitable for in-cluster Job execution"
    - "examples/projects/medium/ sample includes the demo-remote PVC + init Job + project.yaml referencing the local-only file:// remote (Pitfall 12 mitigation: option 2 = in-cluster Job + PVC)"
    - "Plan split into 2 tasks per MEDIUM-8 revision: Task 1 = Go binary + unit test (cmd/tide-demo-init/main.go + main_test.go); Task 2 = image + docs + medium sample"
  artifacts:
    - path: "cmd/tide-demo-init/main.go"
      provides: "Go binary that initializes bare local-only git remote"
      contains: "go-git"
    - path: "images/tide-demo-init/Dockerfile"
      provides: "Container image for in-cluster bootstrap Job"
      contains: "distroless"
    - path: "examples/projects/medium/project.yaml"
      provides: "~$5 sample Project targeting local-only file:// remote"
      contains: "file:///demo-remote.git"
    - path: "examples/projects/medium/demo-remote-init-job.yaml"
      provides: "K8s Job that runs tide-demo-init image to seed the local bare repo"
      contains: "tide-demo-init"
  key_links:
    - from: "cmd/tide-demo-init/main.go"
      to: "cmd/tide-demo-init/fixture/ (build-time directory; Dockerfile COPYs from examples/tide-demo-fixture/)"
      via: "//go:embed all:fixture directive"
      pattern: "//go:embed"
    - from: "examples/projects/medium/demo-remote-init-job.yaml"
      to: "ghcr.io/jsquirrelz/tide-demo-init:v1.0.0"
      via: "Job.spec.template.spec.containers[].image"
      pattern: "tide-demo-init"
    - from: "examples/projects/medium/project.yaml"
      to: "demo-remote-pvc.yaml (shared PVC)"
      via: "targetRepo file:///demo-remote.git resolves via controller's PVC mount of the same PVC"
      pattern: "file:///demo-remote.git"
---

<objective>
Land `cmd/tide-demo-init/` (Go binary + Dockerfile), plus the medium-sample directory at `examples/projects/medium/` (Namespace, Project, demo-remote PVC, demo-remote init Job, README). Closes RESEARCH §"Topic 4 Option b" (the medium sample's local-only-git-remote bootstrap mechanism per D-B3) and completes the 3-sample cost spectrum (D-B1).

Purpose: REQ-DIST-04 medium sample. The medium sample lets operators run TIDE against real Claude with ~$5 spend without depending on an external public repo. The local-only file:// remote is bootstrapped by `cmd/tide-demo-init` running as an in-cluster Job that initializes a bare git repo on a small auxiliary PVC and populates it with embedded fixture content (Plan 05-06).

Plan split (per MEDIUM-8 revision 2026-05-22): originally one task spanning 4 files + symlink-vs-go:generate ambiguity. Now:
- Task 1: `cmd/tide-demo-init/main.go` + `main_test.go` — Go binary + unit test, mirrors `cmd/tide-push/main.go` pattern per PATTERNS.md.
- Task 2: `images/tide-demo-init/Dockerfile` + `cmd/tide-demo-init/README.md` + 5 files under `examples/projects/medium/`.

Embed strategy RESOLVED (per MEDIUM-11): `//go:embed all:fixture` directive in main.go embeds files from a sibling directory `cmd/tide-demo-init/fixture/`. The fixture content lives at `examples/tide-demo-fixture/` in the repo SOT; the Dockerfile COPYs it into the build context at `cmd/tide-demo-init/fixture/` at image build time. For local Go builds OUTSIDE Docker, executor uses a `go:generate` directive that copies the fixture into `cmd/tide-demo-init/fixture/` (gitignored). NO symlinks (incompatible across some toolchains). This is the locked-in strategy — not deferred to execution time.

Output: 9 files total: 3 under cmd/tide-demo-init/ (binary + test + README), 1 Dockerfile under images/, 5 under examples/projects/medium/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@cmd/tide-push/main.go
@cmd/stub-subagent/main.go
@images/tide-push/Dockerfile
@hack/boilerplate.go.txt
@hack/helm/projects-pvc.yaml
@test/e2e/testdata/live-claude-project.yaml
@examples/tide-demo-fixture/main.go
@examples/projects/small/project.yaml
@pkg/git
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cmd/tide-demo-init/main.go + main_test.go — Go binary + unit tests (mirrors cmd/tide-push pattern)</name>
  <files>cmd/tide-demo-init/main.go, cmd/tide-demo-init/main_test.go</files>
  <read_first>
    - cmd/tide-push/main.go (exact analog per PATTERNS.md §"P5.1" — Apache-2.0 header, package-doc comment shape, flag-based CLI, pkg/git import, structured exit codes; lines 1-100)
    - cmd/tide-push/main_test.go (analog for test shape — but the demo-init logic is simpler; one happy-path test + one --bootstrap-dir empty test suffices)
    - cmd/stub-subagent/main.go (alternate analog for tiny single-file CLI shape)
    - hack/boilerplate.go.txt (Apache-2.0 file header — manually applied at file authoring; this Go file is under cmd/ so headers must be present)
    - pkg/git package (verify go-git package import surface — package path is `github.com/go-git/go-git/v5` per existing imports in pkg/git/clone.go; the binary uses `gogit.PlainInit(path, true)` for bare repo init)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-B3"
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 4" lines 1131-1168 (Option b chosen; in-cluster Job pattern)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P5.1"
  </read_first>
  <behavior>
    - `cmd/tide-demo-init/main.go` is a Go binary with `package main` + Apache-2.0 file header + package-doc comment mirroring cmd/tide-push/main.go shape
    - Binary accepts `--bootstrap-dir <path>` flag (required; if empty, exit 2 with usage error)
    - Binary embeds `cmd/tide-demo-init/fixture/` via `//go:embed all:fixture` — the fixture directory is positioned by Dockerfile COPY at build time (Task 2 of this plan); for local `go build`, the fixture directory is materialized via `go generate` (a `//go:generate cp -r ...` directive on a separate non-build line in main.go)
    - Binary creates the bootstrap-dir if missing, runs `git init --bare <bootstrap-dir>` semantics via go-git `PlainInit(path, true)`, then writes the embedded fixture content to a temp working dir, commits it with message "Initial fixture content (Phase 5 D-B3)" and an author "TIDE Demo Bootstrap <tide-demo@noreply.example>", and pushes the commit into the bare repo
    - Exit codes: 0 = success, 1 = generic failure (git init / content extract / commit / push), 2 = invariant violation (--bootstrap-dir empty, target dir exists with non-bare repo)
    - `cmd/tide-demo-init/main_test.go` has at minimum: TestBootstrapDirRequired (calls main flow with empty --bootstrap-dir; asserts exit 2 — use testable internal function, not os.Exit), TestBootstrapHappyPath (creates a tempdir, runs the bootstrap, asserts the bare repo exists at the tempdir + clone-able)
    - `go build ./cmd/tide-demo-init/` succeeds locally (with `go generate ./cmd/tide-demo-init/...` run first to populate fixture/)
    - `go test ./cmd/tide-demo-init/...` exits 0
  </behavior>
  <action>
    Step 1: Create `cmd/tide-demo-init/main.go`. Start with the Apache-2.0 file header from `hack/boilerplate.go.txt` (the standard `/* Copyright 2026 TIDE Authors... */` block).

    Below the header, the package-doc comment per PATTERNS.md §"P5.1" pattern: `// Command tide-demo-init is the local-only git remote bootstrap binary for the TIDE medium sample (Phase 5 D-B3)...` continued through the exit-codes documentation.

    Package declaration `package main`. Imports: `context`, `embed`, `errors`, `flag`, `fmt`, `os`, `path/filepath`, `time`; plus `github.com/go-git/go-git/v5` aliased as `gogit` and `github.com/go-git/go-git/v5/plumbing/object`.

    Embed strategy (RESOLVED per MEDIUM-11):
    - Embed directive at package level: `//go:embed all:fixture` then `var fixtureFS embed.FS`.
    - The `fixture` directory is positioned at `cmd/tide-demo-init/fixture/` via two mechanisms:
      1. For Docker image builds: `images/tide-demo-init/Dockerfile` COPYs `examples/tide-demo-fixture/` to `cmd/tide-demo-init/fixture/` inside the build context (Task 2 of this plan; LOCKED — no decision deferred).
      2. For local `go build` outside Docker: a `//go:generate` directive at the top of main.go that copies `../../examples/tide-demo-fixture/` to `./fixture/` before build. Add `cmd/tide-demo-init/fixture/` to `.gitignore` (or commit it for simplicity; executor decides; either is fine — what matters is the directory exists at build time).
    - Concrete `//go:generate` line: `//go:generate bash -c "rm -rf fixture && cp -r ../../examples/tide-demo-fixture ./fixture"` placed near the top of main.go.

    Body of `main()`:
    - Parse `--bootstrap-dir <path>` flag (required)
    - If empty, print usage error, exit 2
    - Call `bootstrap(ctx, *bootstrapDir)` helper (separate testable function)
    - On error, print error to stderr, exit 1
    - On success, print `OK: bootstrapped local-only git remote at <path>`, exit 0

    `bootstrap(ctx context.Context, dir string) error` helper:
    1. Validate `dir` is not empty; return error if so (caller maps to exit 2 — but this is also the test path)
    2. Mkdir-all the bootstrap-dir's parent directory
    3. Check if dir exists; if so, error "target dir already exists" (refuse to overwrite — exit 2)
    4. `gogit.PlainInit(dir, true)` to create bare repo at dir; on error, return wrapped error
    5. Create a tempdir for the working tree; `gogit.PlainInit(workdir, false)` for the working clone
    6. Walk `fixtureFS` (the embedded fixture); for each file, write contents to the corresponding path under workdir (preserve directory structure under workdir)
    7. Open the workdir repo; `Worktree().Add(.)` all files; `Worktree().Commit("Initial fixture content (Phase 5 D-B3)", &gogit.CommitOptions{Author: &object.Signature{Name: "TIDE Demo Bootstrap", Email: "tide-demo@noreply.example", When: time.Now()}})`
    8. Add the bare repo as a remote on workdir; push refs/heads/main (or refs/heads/master per go-git default behavior — observe what go-git creates)
    9. Return nil on success

    Step 2: Create `cmd/tide-demo-init/main_test.go`. Apache-2.0 header.

    `package main`. Imports `os`, `path/filepath`, `testing`, `time`, `github.com/go-git/go-git/v5`.

    Test 1: `TestBootstrapDirRequired` — calls `bootstrap(context.Background(), "")` directly; asserts the returned error has message matching "bootstrap-dir required" (or similar). (Tests the validation path without invoking os.Exit; main() is a thin os.Exit wrapper.)

    Test 2: `TestBootstrapHappyPath` — creates a tempdir via `t.TempDir()`; calls `bootstrap(context.Background(), filepath.Join(tempdir, "demo-remote.git"))`; asserts no error returned; asserts the resulting path is a bare git repo (gogit.PlainOpen succeeds with `IsBare: true` in the config). Optionally clones the bare repo into a second tempdir and asserts the embedded `main.go` (from examples/tide-demo-fixture) appears in the clone.

    Step 3: Run `go generate ./cmd/tide-demo-init/...` to materialize the fixture directory. Then run `go build ./cmd/tide-demo-init/` — must succeed. Run `go test ./cmd/tide-demo-init/...` — must pass.
  </action>
  <verify>
    <automated>test -s cmd/tide-demo-init/main.go && test -s cmd/tide-demo-init/main_test.go && grep -q "Apache License" cmd/tide-demo-init/main.go && grep -q "package main" cmd/tide-demo-init/main.go && grep -q "//go:embed all:fixture" cmd/tide-demo-init/main.go && grep -q "//go:generate" cmd/tide-demo-init/main.go && grep -q "go-git/go-git/v5" cmd/tide-demo-init/main.go && grep -q "func bootstrap" cmd/tide-demo-init/main.go && grep -q "func TestBootstrap" cmd/tide-demo-init/main_test.go && go generate ./cmd/tide-demo-init/... && go build ./cmd/tide-demo-init/ && go test ./cmd/tide-demo-init/...</automated>
  </verify>
  <acceptance_criteria>
    - `test -s cmd/tide-demo-init/main.go` returns 0
    - `grep -q "Apache License" cmd/tide-demo-init/main.go` returns 0 (Apache-2.0 header present)
    - `grep -q "package main" cmd/tide-demo-init/main.go` returns 0
    - `grep -q "//go:embed all:fixture" cmd/tide-demo-init/main.go` returns 0 (embed strategy LOCKED per MEDIUM-11)
    - `grep -q "//go:generate" cmd/tide-demo-init/main.go` returns 0 (local-build fixture materialization)
    - `grep -q "github.com/go-git/go-git/v5" cmd/tide-demo-init/main.go` returns 0
    - `grep -q "func bootstrap" cmd/tide-demo-init/main.go` returns 0 (testable helper)
    - `grep -q "func TestBootstrapDirRequired\|func TestBootstrap" cmd/tide-demo-init/main_test.go` returns 0
    - `grep -q "func TestBootstrapHappyPath" cmd/tide-demo-init/main_test.go` returns 0
    - `go generate ./cmd/tide-demo-init/...` exits 0 (populates fixture/)
    - `go build ./cmd/tide-demo-init/` exits 0
    - `go test ./cmd/tide-demo-init/...` exits 0 with `PASS` in output
  </acceptance_criteria>
  <done>cmd/tide-demo-init/main.go + main_test.go authored; builds + tests pass; embed strategy locked per MEDIUM-11 (//go:embed + //go:generate hybrid).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: images/tide-demo-init/Dockerfile + cmd/tide-demo-init/README.md + medium/ sample directory</name>
  <files>images/tide-demo-init/Dockerfile, cmd/tide-demo-init/README.md, examples/projects/medium/namespace.yaml, examples/projects/medium/project.yaml, examples/projects/medium/demo-remote-pvc.yaml, examples/projects/medium/demo-remote-init-job.yaml, examples/projects/medium/README.md</files>
  <read_first>
    - images/tide-push/Dockerfile (exact analog per PATTERNS.md §"P5.2" — two-stage golang:1.26-alpine + distroless/static:nonroot)
    - cmd/tide-demo-init/main.go (Task 1 — confirm the embed strategy + reference paths to use in Dockerfile COPY commands)
    - examples/projects/small/project.yaml (Plan 05-11 — style + field consistency baseline)
    - examples/projects/large/project.yaml (Plan 05-11 — Variant B field shape reference)
    - test/e2e/testdata/live-claude-project.yaml (lines 47-87 — budget+providerSecretRef+subagent+git pattern)
    - hack/helm/projects-pvc.yaml (existing PVC shape — analog per PATTERNS.md §"P4.2 demo-remote-pvc.yaml")
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-B1" + §"D-B3" (medium ~$5; local-only git remote)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 4 Option b" + §"Pitfall 12 mitigation" (in-cluster Job + PVC; medium-sample's targetRepo is `file:///demo-remote.git` and the controller's clone Job mounts the same demo-remote-pvc with subPath)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P4.2" + §"P5.2"
  </read_first>
  <behavior>
    - `images/tide-demo-init/Dockerfile` is a two-stage build mirroring images/tide-push/Dockerfile shape: golang:1.26-alpine builder + distroless/static:nonroot final; COPY copies `cmd/tide-demo-init/` + `examples/tide-demo-fixture/` (positioned as `cmd/tide-demo-init/fixture/`) + `pkg/git/` + `go.mod`/`go.sum`; final stage runs as USER 1000 with ENTRYPOINT `/usr/local/bin/tide-demo-init`
    - `cmd/tide-demo-init/README.md` documents the binary: purpose, flags, exit codes, "how it's used" (referenced from images/tide-demo-init/Dockerfile and examples/projects/medium/demo-remote-init-job.yaml)
    - `examples/projects/medium/namespace.yaml` declares Namespace `tide-sample-medium`
    - `examples/projects/medium/demo-remote-pvc.yaml` declares PVC `demo-remote-pvc` in `tide-sample-medium` namespace with `accessModes: [ReadWriteOnce]` + `resources.requests.storage: 100Mi`
    - `examples/projects/medium/demo-remote-init-job.yaml` declares a K8s `batch/v1` `Job` named `demo-remote-init` in `tide-sample-medium` namespace; spec uses image `ghcr.io/jsquirrelz/tide-demo-init:v1.0.0`; mounts `demo-remote-pvc` at `/workspace`; runs `tide-demo-init --bootstrap-dir /workspace/demo-remote.git`; restartPolicy Never; backoffLimit 1
    - `examples/projects/medium/project.yaml` declares Project `medium-project` in `tide-sample-medium`; targetRepo `file:///demo-remote.git`; budget.absoluteCapCents 500 ($5 per D-B1); subagent.image = real Claude image; subagent.model = `claude-haiku-4-5` per RESEARCH §P4.2
    - `examples/projects/medium/README.md` walks operators through the apply sequence
    - All 4 YAML files pass `yq eval '.' <file> > /dev/null` (LOW-13 — pure YAML lint)
  </behavior>
  <action>
    Step 1: Create `images/tide-demo-init/Dockerfile`. Mirror images/tide-push/Dockerfile verbatim per PATTERNS.md §"P5.2". Adjust paths for tide-demo-init:

    ```Dockerfile
    FROM golang:1.26-alpine AS builder
    WORKDIR /workspace
    COPY go.mod go.sum ./
    RUN go mod download
    COPY pkg/git/ pkg/git/
    COPY cmd/tide-demo-init/ cmd/tide-demo-init/
    # Build-time positioning of the fixture per MEDIUM-11 resolution (//go:embed all:fixture target)
    COPY examples/tide-demo-fixture/ cmd/tide-demo-init/fixture/
    RUN CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o /out/tide-demo-init ./cmd/tide-demo-init

    FROM gcr.io/distroless/static:nonroot
    COPY --from=builder /out/tide-demo-init /usr/local/bin/tide-demo-init
    USER 1000
    ENTRYPOINT ["/usr/local/bin/tide-demo-init"]
    ```

    The COPY for `examples/tide-demo-fixture/` → `cmd/tide-demo-init/fixture/` is the canonical build-time positioning that satisfies the `//go:embed all:fixture` directive (MEDIUM-11 lock).

    Step 2: Create `cmd/tide-demo-init/README.md`. Heading `# tide-demo-init`.
    - Purpose: bootstrap a local-only bare git repo from embedded `examples/tide-demo-fixture/` content; used by the medium sample (Phase 5 D-B3).
    - Usage: `tide-demo-init --bootstrap-dir <path>` with one paragraph explanation.
    - Exit codes: 0 / 1 / 2 with one-line descriptions each.
    - "Build (local)": "Run `go generate ./cmd/tide-demo-init/...` to populate `cmd/tide-demo-init/fixture/` from `examples/tide-demo-fixture/`, then `go build ./cmd/tide-demo-init/`."
    - "Build (Docker)": "The Dockerfile COPYs `examples/tide-demo-fixture/` to `cmd/tide-demo-init/fixture/` in the build context automatically; just run `docker build -f images/tide-demo-init/Dockerfile .`."
    - "How it's used in the medium sample": brief explanation that the image `ghcr.io/jsquirrelz/tide-demo-init:v1.0.0` runs as an in-cluster Job (`examples/projects/medium/demo-remote-init-job.yaml`) mounted on `examples/projects/medium/demo-remote-pvc.yaml`; the resulting `/demo-remote.git` bare repo is what the medium-sample Project clones via `file://`.

    Step 3: Create `examples/projects/medium/namespace.yaml`:
    ```yaml
    apiVersion: v1
    kind: Namespace
    metadata:
      name: tide-sample-medium
      labels:
        tideproject.k8s/sample: medium
        app.kubernetes.io/managed-by: tide-sample
    ```

    Step 4: Create `examples/projects/medium/demo-remote-pvc.yaml`:
    ```yaml
    apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: demo-remote-pvc
      namespace: tide-sample-medium
      labels:
        tideproject.k8s/sample: medium
        app.kubernetes.io/component: demo-remote
    spec:
      accessModes: [ReadWriteOnce]
      resources:
        requests:
          storage: 100Mi
    ```

    Step 5: Create `examples/projects/medium/demo-remote-init-job.yaml`:
    ```yaml
    apiVersion: batch/v1
    kind: Job
    metadata:
      name: demo-remote-init
      namespace: tide-sample-medium
      labels:
        tideproject.k8s/sample: medium
        app.kubernetes.io/component: demo-remote-init
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app.kubernetes.io/component: demo-remote-init
        spec:
          restartPolicy: Never
          containers:
            - name: tide-demo-init
              image: ghcr.io/jsquirrelz/tide-demo-init:v1.0.0
              args:
                - --bootstrap-dir=/workspace/demo-remote.git
              volumeMounts:
                - name: demo-remote
                  mountPath: /workspace
          volumes:
            - name: demo-remote
              persistentVolumeClaim:
                claimName: demo-remote-pvc
    ```

    Step 6: Create `examples/projects/medium/project.yaml`. Multi-doc with `---` separator: Namespace + Project (or single-doc Project, since Namespace is in namespace.yaml separately — pick whichever is cleaner; single-doc is fine if README explains the apply order):

    ```yaml
    apiVersion: tideproject.k8s/v1alpha1
    kind: Project
    metadata:
      name: medium-project
      namespace: tide-sample-medium
    spec:
      targetRepo: file:///demo-remote.git
      branchStrategy: per-run
      governanceLevel: phase
      budget:
        absoluteCapCents: 500
        rollingWindowCapCents: 500
        rollingWindowDurationSeconds: 86400
      providerSecretRef: tide-secrets
      gitCredsSecretRef: tide-secrets
      subagent:
        image: ghcr.io/jsquirrelz/tide-claude-subagent:v1.0.0
        model: claude-haiku-4-5
      gates:
        milestone: auto
        phase: auto
        plan: auto
        task: auto
        pauseBetweenWaves: false
      outcomePrompt: |
        The targetRepo is a tiny Go scaffold from examples/tide-demo-fixture/.
        Add a second exported function alongside Greeting() that returns a
        formatted timestamp string. Update main.go to print both. Add a test
        for the new function. Constraint: ONE Phase, ONE Plan, target 3-5 tasks.
        Pass criterion: `go test ./...` is green inside the cloned repo.
    ```

    The exact field set depends on the v1alpha1 schema; executor verifies via `yq eval '.' examples/projects/medium/project.yaml > /dev/null` (LOW-13 — pure YAML lint) and adjusts field names while preserving the intent (local-only file:// remote, $5 cap, real Claude haiku image, small outcome bounding to 3-5 tasks).

    Step 7: Create `examples/projects/medium/README.md`:
    - Heading `# medium — ~$5 mini Claude (local-only git remote)`
    - Audience: "Operators trying TIDE with real LLM dispatch at ~$5 cost, no external repo dependency."
    - Status: "v1.0; local-only `file://` git remote bootstrapped by `cmd/tide-demo-init`."
    - Section "Prerequisites": Secret `tide-secrets` in `tide-sample-medium` namespace with `ANTHROPIC_API_KEY` set; tide-crds + tide charts installed (link to docs/INSTALL.md).
    - Section "Setup (in order)":
      1. `kubectl apply -f examples/projects/medium/namespace.yaml`
      2. `kubectl apply -f examples/projects/medium/demo-remote-pvc.yaml`
      3. `kubectl apply -f examples/projects/medium/demo-remote-init-job.yaml`
      4. `kubectl wait --for=condition=Complete job/demo-remote-init -n tide-sample-medium --timeout=2m`
      5. `kubectl create secret generic tide-secrets --from-literal=ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY -n tide-sample-medium`
      6. `kubectl apply -f examples/projects/medium/project.yaml`
      7. `kubectl wait --for=jsonpath="{.status.phase}"=Complete project/medium-project -n tide-sample-medium --timeout=30m`
    - Section "What you'll observe": TIDE clones the local file:// remote, plans the function-addition tasks, dispatches them via real Claude, commits results back, and the run-branch (on the demo-remote PVC) carries the authored content.
    - Section "Budget cap behavior": if cap fires at $5, dispatch halts; for full recovery semantics see [troubleshooting.md](../../docs/troubleshooting.md).
    - Section "Cleanup": `kubectl delete namespace tide-sample-medium` (this cascades the PVC + Job + Secret + Project)
    - Pitfall 9 reminder: namespace prefix `tide-sample-medium` distinct from `tide-samples` Phase 1 fixture.
  </action>
  <verify>
    <automated>test -s images/tide-demo-init/Dockerfile && test -s cmd/tide-demo-init/README.md && test -s examples/projects/medium/namespace.yaml && test -s examples/projects/medium/project.yaml && test -s examples/projects/medium/demo-remote-pvc.yaml && test -s examples/projects/medium/demo-remote-init-job.yaml && test -s examples/projects/medium/README.md && grep -q "FROM golang:1.26-alpine" images/tide-demo-init/Dockerfile && grep -q "distroless/static:nonroot" images/tide-demo-init/Dockerfile && grep -q "USER 1000" images/tide-demo-init/Dockerfile && grep -q "cmd/tide-demo-init/fixture/" images/tide-demo-init/Dockerfile && grep -q "tide-sample-medium" examples/projects/medium/namespace.yaml && grep -q "demo-remote-pvc" examples/projects/medium/demo-remote-pvc.yaml && grep -q "tide-demo-init:v1.0.0" examples/projects/medium/demo-remote-init-job.yaml && grep -q "file:///demo-remote.git" examples/projects/medium/project.yaml && grep -q "absoluteCapCents: 500" examples/projects/medium/project.yaml && grep -q "claude-haiku-4-5" examples/projects/medium/project.yaml && (command -v yq >/dev/null 2>&1 && yq eval '.' examples/projects/medium/project.yaml > /dev/null && yq eval '.' examples/projects/medium/namespace.yaml > /dev/null && yq eval '.' examples/projects/medium/demo-remote-pvc.yaml > /dev/null && yq eval '.' examples/projects/medium/demo-remote-init-job.yaml > /dev/null) || python3 -c "import yaml,sys; list(yaml.safe_load_all(open('examples/projects/medium/project.yaml'))); list(yaml.safe_load_all(open('examples/projects/medium/namespace.yaml'))); list(yaml.safe_load_all(open('examples/projects/medium/demo-remote-pvc.yaml'))); list(yaml.safe_load_all(open('examples/projects/medium/demo-remote-init-job.yaml')))"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist + non-empty (Dockerfile + cmd README + 5 medium-sample files)
    - `grep -q "FROM golang:1.26-alpine AS builder" images/tide-demo-init/Dockerfile` returns 0
    - `grep -q "cmd/tide-demo-init/fixture/" images/tide-demo-init/Dockerfile` returns 0 (MEDIUM-11 lock — build-time fixture positioning)
    - `grep -q "gcr.io/distroless/static:nonroot" images/tide-demo-init/Dockerfile` returns 0
    - `grep -q "ENTRYPOINT" images/tide-demo-init/Dockerfile` returns 0
    - `grep -q "tide-demo-init" cmd/tide-demo-init/README.md` returns 0
    - `grep -q "tide-sample-medium" examples/projects/medium/namespace.yaml` returns 0
    - `grep -q "accessModes:" examples/projects/medium/demo-remote-pvc.yaml` returns 0
    - `grep -q "ReadWriteOnce" examples/projects/medium/demo-remote-pvc.yaml` returns 0
    - `grep -q "tide-demo-init:v1.0.0" examples/projects/medium/demo-remote-init-job.yaml` returns 0
    - `grep -q "bootstrap-dir=/workspace/demo-remote.git" examples/projects/medium/demo-remote-init-job.yaml` returns 0
    - `grep -q "tideproject.k8s/v1alpha1" examples/projects/medium/project.yaml` returns 0
    - `grep -q "file:///demo-remote.git" examples/projects/medium/project.yaml` returns 0
    - `grep -q "absoluteCapCents: 500" examples/projects/medium/project.yaml` returns 0 ($5 cap)
    - `grep -q "claude-haiku-4-5" examples/projects/medium/project.yaml` returns 0
    - YAML lint via yq or python: all 4 medium-sample YAML files pass `yq eval '.' > /dev/null` (LOW-13 revision)
    - `grep -q "demo-remote-init" examples/projects/medium/README.md` returns 0
    - `grep -q "kubectl wait" examples/projects/medium/README.md` returns 0 (apply sequence documented)
    - `grep -q "kubectl create secret" examples/projects/medium/README.md` returns 0 (Secret creation step)
    - `grep -q "Cleanup" examples/projects/medium/README.md` returns 0
  </acceptance_criteria>
  <done>Dockerfile + cmd README + 5 medium-sample files exist; YAML lint clean (LOW-13); README walks through 7-step apply sequence; embed-strategy COPY ensures //go:embed resolves at image build (MEDIUM-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-cluster init Job → demo-remote-pvc | The init Job writes git bare-repo data; the controller's clone Job reads from the same PVC; cross-pod access via PVC is the trust handoff |
| Embedded fixture content → bootstrap binary → bare repo → controller clone | Chain of custody from repo source (examples/tide-demo-fixture) through container image (ghcr.io/.../tide-demo-init) into the operator's cluster — anyone modifying the published image could alter what TIDE clones |
| //go:embed source positioning → image build reproducibility | If the fixture directory is missing or differs between build environments, the embedded content drifts silently; MEDIUM-11 LOCK resolves this with explicit Dockerfile COPY + go:generate local-build step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12-01 | Tampering | Embedded fixture drift between repo and published image | mitigate | Dockerfile COPYs examples/tide-demo-fixture/ at build time; image is rebuilt + republished on every release (Plan 05-16); image tag `v1.0.0` is locked per chart version (Plan 05-05) |
| T-05-12-02 | Information Disclosure | demo-remote-pvc cross-pod access | accept | PVC accessModes is ReadWriteOnce; both init Job pod and controller pod can mount it (same node when RWO satisfied by the node-bound storage class) — this is standard K8s PVC semantics for dev workloads |
| T-05-12-03 | Tampering | go:embed source positioning across build environments | mitigate | MEDIUM-11 LOCK: Dockerfile COPYs fixture/ at image build (canonical path); `//go:generate` directive materializes fixture/ for local `go build` (developer convenience); embed directive `all:fixture` is single source-of-truth in main.go |
| T-05-12-04 | Elevation of Privilege | Job pod running as USER 1000 | mitigate | Dockerfile sets USER 1000 + uses distroless/static:nonroot base; Job spec inherits via image; no root access to PVC write surface |
</threat_model>

<verification>
After both tasks: `go generate ./cmd/tide-demo-init/... && go test ./cmd/tide-demo-init/...` passes; `yq eval '.' examples/projects/medium/project.yaml > /dev/null` (and the other 3 YAML files) all exit 0.
</verification>

<success_criteria>
- cmd/tide-demo-init/ Go binary + tests (Task 1) + README + Dockerfile (Task 2)
- 5 files under examples/projects/medium/ (Task 2)
- 3-sample cost spectrum (small/medium/large) complete after this plan
- DIST-04 medium-sample piece (3 of 3) satisfied
- Pitfall 12 mitigation (in-cluster Job + PVC) implemented
- MEDIUM-8 plan split applied (Go code in Task 1; image + samples in Task 2)
- MEDIUM-11 embed strategy LOCKED (//go:embed + Dockerfile COPY + //go:generate)
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-12-SUMMARY.md` documenting binary build/test results, medium-sample yq-lint output, embed-strategy verification (image build succeeds with fixture/ COPY), and commit SHA.
</output>
