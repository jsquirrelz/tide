---
phase: 05-distribution-self-hosting-acceptance
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/rbac.md
autonomous: true
requirements: [DIST-04, AUTH-02]
tags: [docs, rbac, per-namespace]

must_haves:
  truths:
    - "docs/rbac.md exists with per-Kind verbs table + per-namespace RoleBinding usage + conversion-webhook no-op section (D-X4, D-X7)"
    - "Doc references the per-namespace RoleBinding template (charts/tide/templates/per-namespace-rolebinding.yaml lands in Plan 05-13)"
    - "Doc references the projectNamespaces Helm value (also lands in Plan 05-13)"
    - "Doc explicitly notes the conversion webhook stays a no-op for v1.0 per D-X7 (but still needs RBAC plumbing)"
  artifacts:
    - path: "docs/rbac.md"
      provides: "Per-Kind verbs + per-ns RoleBinding usage doc"
      contains: "per-namespace"
  key_links:
    - from: "docs/rbac.md"
      to: "charts/tide/templates/per-namespace-rolebinding.yaml (Plan 05-13)"
      via: "usage example references the template by path"
      pattern: "per-namespace-rolebinding.yaml"
    - from: "docs/rbac.md"
      to: "Phase 1 per-Kind ClusterRoles (project-admin / phase-admin / plan-admin / task-admin / wave-admin / milestone-admin)"
      via: "verbs table enumerates each"
      pattern: "project-admin\\|phase-admin"
---

<objective>
Land `docs/rbac.md` documenting per-Kind verbs (Phase 1 plan 09 + AUTH-03 lock) + per-namespace RoleBinding usage (D-X4, AUTH-02 catch-up, Plan 05-13 template) + conversion-webhook no-op section (D-X7).

Purpose: REQ-DIST-04 RBAC reference + AUTH-02 catch-up. The per-namespace RoleBinding template (Plan 05-13) only fires when operators set `Values.projectNamespaces: [ns1, ns2, ...]`; this doc explains when + how to use it, plus documents the per-Kind ClusterRoles that ship from Phase 1.

Output: docs/rbac.md with per-Kind verbs table + RoleBinding usage + conversion-webhook caveat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@docs/rwx-drivers.md
@docs/live-e2e.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/rbac.md</name>
  <files>docs/rbac.md</files>
  <read_first>
    - docs/rwx-drivers.md (exact analog per PATTERNS.md §"P2.6" — table-driven matrix doc shape)
    - docs/live-e2e.md lines 1-15 (Audience/Status/Scope opener)
    - .planning/REQUIREMENTS.md line 233 (AUTH-02 traceability: "per-namespace RoleBinding YAML template ships in Phase 5 Helm chart")
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-X4" (per-namespace RoleBinding template, projectNamespaces values key) + §"D-X7" (conversion webhook stays a no-op for v1.0)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Architectural Responsibility Map" line 121 (per-namespace RoleBinding tier assignment) and §"Open Question 6" (subjects point to operator namespace, RoleBinding lives in project namespace — standard K8s pattern)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P2.6" (analog from rwx-drivers.md)
    - charts/tide/templates/ (list templates; Phase 1 plan 09 shipped per-Kind ClusterRoles; AUTH-03 lock — no wildcards)
  </read_first>
  <behavior>
    - File exists at `docs/rbac.md`
    - Opens with `# TIDE RBAC Reference` heading + Audience/Status/Scope opener
    - Section 1: Per-Kind verbs table (one row per CRD Kind: Project, Milestone, Phase, Plan, Task, Wave) with the verbs the orchestrator's `tide-controller-manager` SA has on body (typically `get/list/watch/create/update/patch/delete`) and on `/status` (`get/update/patch` per Phase 1 lock — no wildcards, AUTH-03)
    - Section 2: Per-namespace RoleBinding usage — explains when to use (multi-Project installs where each Project lives in its own namespace), how to use (set `Values.projectNamespaces: [...]` in Helm values), what gets created (one RoleBinding per listed namespace), and points to the chart template (path: `charts/tide/templates/per-namespace-rolebinding.yaml`, lands in Plan 05-13)
    - Section 3: Conversion webhook caveat — D-X7 explanation that the conversion webhook is scaffolded but no-op for v1.0; still needs RBAC plumbing (the webhook's ServiceAccount + CertificateRequest); references Phase 1 D-A3 schema-lock
    - Section 4: Auditing — `kubectl describe sa tide-controller-manager -n tide-system` recipe for verifying the SA's bindings; `kubectl auth can-i ...` recipe pattern
  </behavior>
  <action>
    Create `docs/rbac.md`. Heading `# TIDE RBAC Reference`.

    Audience/Status/Scope opener:
    - Audience: "Cluster operators and security reviewers auditing TIDE's K8s permissions."
    - Status: "v1.0; AUTH-02 (per-namespace RoleBinding template) and AUTH-03 (no wildcards) locked."
    - Scope of this doc: bulleted list naming the level-2 sections.

    Level-2 sections in order:

    1. `## Per-Kind verbs` — opening paragraph explaining AUTH-03 lock (no wildcards; per-Kind verb grants only — verified by Phase 1 plan 09 + `make verify-rbac-marker-discipline`). Then a Markdown table:

       | Kind | Body verbs | Status verbs | ClusterRole template |
       |------|------------|--------------|----------------------|
       | Project | get, list, watch, create, update, patch, delete | get, update, patch | project-admin-rbac.yaml |
       | Milestone | get, list, watch, create, update, patch, delete | get, update, patch | milestone-admin-rbac.yaml |
       | Phase | (same) | (same) | phase-admin-rbac.yaml |
       | Plan | (same) | (same) | plan-admin-rbac.yaml |
       | Task | (same) | (same) | task-admin-rbac.yaml |
       | Wave | (same) | (same) | wave-admin-rbac.yaml |

       After the table, a paragraph explaining the auxiliary RBAC (subagent SA per Phase 2 + dashboard SA per Phase 4 — both are read-only / restricted scope). Cite the corresponding template files.

    2. `## Per-namespace RoleBinding (AUTH-02 catch-up)` — opening paragraph explaining AUTH-02: "One TIDE install per cluster; each Project runs in its own namespace with namespace-scoped RBAC. The orchestrator SA (`tide-controller-manager` in `tide-system`) is the SUBJECT; the per-namespace RoleBinding lives in the Project's namespace and grants the SA the per-Kind ClusterRole verbs in that namespace."

       Show the Helm values opt-in pattern (referencing Plan 05-13's `projectNamespaces` key):
       ```yaml
       # values.yaml override
       projectNamespaces:
         - tide-customer-acme
         - tide-customer-globex
       ```

       Then explain what the template renders: one RoleBinding per listed namespace, subjects.namespace pointing to `tide-system`, roleRef.kind=ClusterRole binding to the appropriate per-Kind ClusterRole (Plan 05-13 finalizes the aggregation strategy — point readers to the template file).

       Note: empty default means no per-namespace RoleBindings are created out-of-the-box; this is opt-in by design (D-X4).

    3. `## Conversion webhook (D-X7 no-op for v1.0)` — D-X7 explanation. Two paragraphs:
       - The conversion webhook is scaffolded from Phase 1 plan 07 but remains a no-op for v1.0 because v1alpha1 IS the hub schema (Phase 1 D-A3) and no spoke (v1beta1) exists yet. CRD-05 / Pitfall 16 mitigation is the scaffold itself.
       - The webhook still requires RBAC plumbing — a webhook ServiceAccount, a CertificateRequest for cert-manager, etc. These ship from Phase 1 chart templates. When v1.x activates the webhook with a real ConvertTo/ConvertFrom body, no chart change is needed; just the webhook Go code.

    4. `## Auditing TIDE's RBAC` — recipes:
       ```bash
       # Show the orchestrator SA's bindings
       kubectl describe sa tide-controller-manager -n tide-system

       # Verify a specific permission
       kubectl auth can-i create projects.tideproject.k8s --as=system:serviceaccount:tide-system:tide-controller-manager

       # Lint-check no wildcards (CI gate from Phase 1)
       make verify-no-rbac-wildcards
       ```

    5. `## See also` — Markdown links: docs/INSTALL.md (Secret + per-namespace install), docs/gates.md (gate policy is RBAC-adjacent), docs/troubleshooting.md (RBAC-related failure modes).

    Voice: tight + table-heavy where verbs are listed. Concrete recipes in fenced code blocks.

    Length target: 100-250 lines.
  </action>
  <verify>
    <automated>test -s docs/rbac.md && grep -q "# TIDE RBAC Reference" docs/rbac.md && grep -qE "^## Per-Kind verbs" docs/rbac.md && grep -qE "^## Per-namespace RoleBinding" docs/rbac.md && grep -qE "^## Conversion webhook" docs/rbac.md && grep -q "projectNamespaces" docs/rbac.md && grep -q "no-op" docs/rbac.md && grep -q "AUTH-02" docs/rbac.md && grep -q "AUTH-03" docs/rbac.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/rbac.md` returns 0
    - `head -1 docs/rbac.md` matches `# TIDE RBAC Reference`
    - `grep -cE '^## ' docs/rbac.md` returns ≥ 4
    - `grep -q "Per-Kind verbs" docs/rbac.md` returns 0
    - `grep -q "Per-namespace RoleBinding" docs/rbac.md` returns 0
    - `grep -q "Conversion webhook" docs/rbac.md` returns 0
    - `grep -q "AUTH-02" docs/rbac.md` returns 0
    - `grep -q "AUTH-03" docs/rbac.md` returns 0
    - `grep -q "projectNamespaces" docs/rbac.md` returns 0
    - `grep -q "no-op" docs/rbac.md` returns 0
    - `grep -q "kubectl describe sa" docs/rbac.md` returns 0 (auditing recipe present)
    - `grep -q "kubectl auth can-i" docs/rbac.md` returns 0
    - `grep -q "Project" docs/rbac.md` returns 0 (verbs table has the Project Kind)
    - `grep -q "Wave" docs/rbac.md` returns 0 (verbs table has the Wave Kind)
    - `wc -l docs/rbac.md` outputs between 50 and 400 lines
  </acceptance_criteria>
  <done>docs/rbac.md exists with 4 level-2 sections (per-Kind verbs table + per-namespace usage + conversion-webhook caveat + auditing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Security reviewer → docs/rbac.md | Auditors rely on this doc to understand what TIDE can do in their cluster; misdocumented scope hides actual permissions |
| Multi-Project operator → projectNamespaces values key | Operators copy the projectNamespaces pattern; if doc shows wrong scope, the per-namespace RoleBinding could over-grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Elevation of Privilege | Per-namespace RoleBinding scope misdocumentation | mitigate | Doc explicitly states subjects.namespace = tide-system (operator namespace); roleRef = per-Kind ClusterRoles only (not wildcards); Plan 05-13 implements the template per these rules; Plan 05-16 CI gate `make verify-no-rbac-wildcards` enforces the no-wildcards lock |
| T-05-09-02 | Information Disclosure | Conversion webhook RBAC status | mitigate | D-X7 caveat section explicitly notes the webhook is no-op + still has scaffold RBAC; operators know what's active vs scaffold-only |
| T-05-09-03 | Tampering | Verbs table accuracy | mitigate | Table sourced from charts/tide/templates/*-admin-rbac.yaml (Phase 1 plan 09 templates); auditors can verify with `helm template charts/tide \| yq '.[..] \| select(.kind == "ClusterRole") \| .rules'` |
</threat_model>

<verification>
After Task 1: doc renders cleanly; references to projectNamespaces will become live after Plan 05-13 lands the actual template.
</verification>

<success_criteria>
- docs/rbac.md exists with 4 level-2 sections
- Per-Kind verbs table + per-namespace RoleBinding usage + conversion-webhook caveat
- AUTH-02 catch-up explicit + AUTH-03 no-wildcards lock referenced
- DIST-04 RBAC piece + AUTH-02 catch-up documentation satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-09-SUMMARY.md` with line count, section count, and commit SHA.
</output>
