---
phase: 05-distribution-self-hosting-acceptance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DIST-04]
tags: [readme, quickstart, oss-readiness]

must_haves:
  truths:
    - "README.md opens with a ~30-line Quickstart block (D-C1) BEFORE the existing paradigm spec content"
    - "Quickstart uses OCI helm install commands (D-X6) for fresh installs + a callout linking to docs/INSTALL.md"
    - "Existing spec content (lines 1-43 of pre-Phase-5 README and all body content) is preserved verbatim — only PREPENDED, not modified"
    - "Existing TOC anchors / heading IDs still resolve (no rename of existing headings)"
  artifacts:
    - path: "README.md"
      provides: "Quickstart-prefixed README at repo root"
      contains: "kind create cluster"
  key_links:
    - from: "README.md Quickstart block"
      to: "examples/projects/small/project.yaml"
      via: "kubectl apply -f reference"
      pattern: "kubectl apply -f examples/projects/small"
    - from: "README.md Quickstart callout"
      to: "docs/INSTALL.md"
      via: "Markdown link"
      pattern: "docs/INSTALL.md"
---

<objective>
Prepend a ~30-line Quickstart block to README.md (D-C1, D-C2, D-X6, Pitfall 4 mitigation, Pitfall 8 mitigation) above the existing load-bearing paradigm spec, without modifying any existing spec content. The Quickstart uses OCI helm install commands and references the small sample.

Purpose: REQ-DIST-04 README on-ramp. CLAUDE.md says "README.md is the design spec and doubles as the public-facing README" — Phase 5 preserves that load-bearing role but prevents the Pitfall 24 OSS-adoption-death-by-missing-docs failure mode by adding a 4-command "try it" block at the very top.

Output: README.md with a Quickstart block as the first ~30 lines (above the existing `# TIDE` title), then the entire pre-existing spec content verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prepend Quickstart block to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing lines 1-43 are the title + acronym block; Quickstart goes ABOVE this content per PATTERNS.md §"P2.1" self-reference; line 1 is currently ` # TIDE — Topologically-Indexed Dependency Execution`)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-C1" + §"D-X6" (locks OCI install command shape; "First time?" callout for Pitfall 8)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 11 External-operator-friendly install flow" (locks the 4-command sequence) and §"Pitfall 4" (CRD subchart MUST install first)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P2.1 README" (PATTERN: Quickstart block is FIRST content above title)
    - examples/projects/small/project.yaml (will not exist yet at Wave 1 execution time — reference it by future-known-path; Plan 05-11 lands it; the Quickstart command line references the path that WILL exist post-Plan-11; Phase 5 ships as a unit per D-D1)
  </read_first>
  <behavior>
    - README.md retains 100% of its existing content with no modifications to that content
    - A new Quickstart section is prepended ABOVE the existing first heading
    - The Quickstart contains exactly 4 copy-pasteable commands per RESEARCH §"Topic 11":
      1. `kind create cluster --name tide-demo`
      2. `helm install tide-crds oci://ghcr.io/jsquirrelz/tide-charts/tide-crds --version 1.0.0 -n tide-system --create-namespace`
      3. `helm install tide oci://ghcr.io/jsquirrelz/tide-charts/tide --version 1.0.0 -n tide-system`
      4. `kubectl apply -f examples/projects/small/project.yaml`
    - The Quickstart block is between 25 and 40 lines (target ~30 per D-C1 + space for expected-output annotations per CONTEXT.md specifics line 244)
    - A "First time?" callout block links to `docs/INSTALL.md` per Pitfall 8 mitigation (RESEARCH §"Pitfall 8" → quickstart-reinforcement; this docs path will be created in Plan 05-07 but is referenced now since Phase 5 ships as a unit)
    - The first heading in README.md is now `## Quickstart` (level-2) or similar level-2 introducer; the existing top-level `# TIDE — Topologically-Indexed Dependency Execution` heading is preserved BELOW the Quickstart and remains the top-level heading
    - No existing heading text changes (anchors preserved per A6 assumption)
  </behavior>
  <action>
    Read the full existing README.md content into memory. Create new content by prepending the Quickstart block to the existing content with no modification of the existing content. Write back the combined content.

    The prepended Quickstart block uses this shape (the executor renders Markdown verbatim — do NOT write the angle-bracket tags from this action; emit prose + fenced code blocks):
    - Level-2 heading `## Quickstart` (this becomes the very first content of README.md; the existing level-1 `# TIDE — Topologically-Indexed Dependency Execution` heading falls below it; this is intentional per D-C1 "Quickstart block at the TOP before the spec")
    - One-paragraph framing: "Four commands. $0 LLM cost. Tests the dispatch path end-to-end via the stub-subagent. Detailed install steps + per-OS prerequisites are in [docs/INSTALL.md](docs/INSTALL.md)."
    - One ` ```bash ` fenced code block containing the four commands listed in <behavior>, ONE COMMAND PER LINE, with the commands exactly as cited in RESEARCH §"Topic 11" (verbatim). Use `kind create cluster --name tide-demo` (not `kind create cluster`) so the cluster name is explicit.
    - Beneath the code block, three or four "expected output" annotation lines (per CONTEXT.md specifics #244 "~10 of the 30 lines"): one-liner showing `STATUS: deployed` for each helm install, `customresourcedefinition.apiextensions.k8s.io/projects.tideproject.k8s created` for CRDs, `project.tideproject.k8s/small-project created` for the apply, `condition met` for the kubectl wait. Format these as a separate ` ```text ` fenced block titled `# Expected output (abbreviated)`.
    - A callout block per RESEARCH §"Pitfall 8 mitigation" using Markdown blockquote syntax: `> **First time?** Skip to [docs/INSTALL.md](docs/INSTALL.md) for the 4-command install with full prerequisites and troubleshooting.`
    - A separator: a single horizontal rule `---` to visually divide the Quickstart from the spec content that follows.

    Then the existing README.md content (unmodified) follows.

    Do NOT modify the existing acronym block, the existing spec content, the existing Mermaid diagrams, the existing pseudocode blocks, or any existing heading text. The post-edit `tail -n +50 README.md` output (modulo the line-shift) must match the pre-edit `head -n -50 README.md` content (the same body shifted down by the prepend).

    No body content is added between the Quickstart's closing `---` and the existing `# TIDE — Topologically-Indexed Dependency Execution` heading.
  </action>
  <verify>
    <automated>head -40 README.md | grep -qE 'kind create cluster|helm install' && head -40 README.md | grep -q 'examples/projects/small' && head -1 README.md | grep -qE '^## Quickstart' && grep -c '# TIDE — Topologically-Indexed Dependency Execution' README.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `head -40 README.md` contains the literal string `kind create cluster` (Pitfall 8 mitigation per VALIDATION.md row "Quickstart prepended at TOP")
    - `head -40 README.md` contains the literal string `helm install tide-crds`
    - `head -40 README.md` contains the literal string `helm install tide`
    - `head -40 README.md` contains the literal string `examples/projects/small/project.yaml`
    - `head -1 README.md` matches the regex `^## Quickstart` (the very first line is the Quickstart heading)
    - `grep -cE '^# TIDE — Topologically-Indexed Dependency Execution$' README.md` returns 1 (the existing top-level heading is preserved, not duplicated)
    - `grep -q 'docs/INSTALL.md' README.md` returns 0 (the "First time?" callout links to INSTALL.md)
    - `grep -c '^---$' README.md` returns ≥ 1 (the horizontal rule separator between Quickstart and spec content; could match more if existing content uses `---` too)
    - Pre-edit pattern preserved: `grep -c "T — Topologically" README.md` returns ≥ 1 (the existing acronym block is intact)
    - Pre-edit pattern preserved: `grep -c "I — Indexed" README.md` returns ≥ 1
    - Line count: `wc -l README.md` is the pre-edit line count + 20-to-40 lines (the Quickstart insertion)
  </acceptance_criteria>
  <done>README.md opens with a Quickstart block per D-C1; existing spec content preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| First-time visitor → README.md | The Quickstart commands are copy-pasted by anyone evaluating TIDE; a broken or misleading command burns trust on the first impression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Quickstart command sequence | mitigate | Commands locked to D-X6 OCI shape; CRD-subchart-first order matches Pitfall 4 lock; Plan 05-15 dry-run will execute these same commands in DinD to catch drift |
| T-05-03-02 | Information Disclosure | Sample namespace name | mitigate | Quickstart uses `examples/projects/small/project.yaml` whose namespace `tide-sample-small` (Plan 05-11 lock) avoids Pitfall 9 collision with `tide-samples` Phase 1 fixture |
| T-05-03-03 | Tampering | Spec content preservation | mitigate | Acceptance criteria explicitly grep for the existing acronym block strings ("T — Topologically", "I — Indexed") to detect accidental clobber of the spec content |
</threat_model>

<verification>
`head -40 README.md` shows the Quickstart block; `grep -c "# TIDE — Topologically-Indexed Dependency Execution" README.md` returns exactly 1 (no duplicate top-level heading).
</verification>

<success_criteria>
- README.md prepended with ~30-line Quickstart block per D-C1
- 4 OCI commands present per D-X6 + Pitfall 4 (CRDs first)
- "First time?" callout links to docs/INSTALL.md per Pitfall 8 mitigation
- Existing spec content preserved verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-03-SUMMARY.md` documenting pre/post line count, the 4 commands placed, and commit SHA.
</output>
