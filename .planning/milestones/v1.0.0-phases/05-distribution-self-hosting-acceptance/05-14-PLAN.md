---
phase: 05-distribution-self-hosting-acceptance
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - hack/helm/augment-tide-crds-chart.sh
  - charts/tide-crds/templates/milestone-crd.yaml
  - charts/tide-crds/templates/phase-crd.yaml
  - charts/tide-crds/templates/plan-crd.yaml
  - charts/tide-crds/templates/project-crd.yaml
  - charts/tide-crds/templates/task-crd.yaml
  - charts/tide-crds/templates/wave-crd.yaml
autonomous: true
requirements: [DIST-01]
tags: [crd-subchart, resource-policy, helm-upgrade-safety]

must_haves:
  truths:
    - "All 6 CRD templates under charts/tide-crds/templates/ carry the `helm.sh/resource-policy: keep` annotation (RESEARCH Topic 8 finding #5 — Wave 1 critical gap fix per HIGH-2 revision)"
    - "hack/helm/augment-tide-crds-chart.sh is extended to inject the annotation on every `make helm-crds` regeneration (idempotent)"
    - "Annotation prevents `helm uninstall tide-crds` from cascading CRD delete (and the cascading Project/Milestone/Phase/Plan/Task/Wave delete)"
    - "The 6 generated CRD templates land with the annotation; rerunning `make helm-crds` produces identical output (idempotent post-augment)"
    - "Wave 1 placement: this plan modifies hack/helm/augment-tide-crds-chart.sh + 6 CRD templates — NO file overlap with Plan 05-05 (which edits hack/helm/tide-chart.yaml + hack/helm/tide-crds-chart.yaml); both run in parallel in Wave 1"
  artifacts:
    - path: "hack/helm/augment-tide-crds-chart.sh"
      provides: "CRD-subchart augment script extended for resource-policy annotation injection"
      contains: "helm.sh/resource-policy"
    - path: "charts/tide-crds/templates/project-crd.yaml"
      provides: "Project CRD template with annotation"
      contains: "helm.sh/resource-policy: keep"
  key_links:
    - from: "hack/helm/augment-tide-crds-chart.sh"
      to: "charts/tide-crds/templates/*-crd.yaml"
      via: "sed -i (or compatible) injects annotation into metadata.annotations block"
      pattern: "helm.sh/resource-policy"
---

<objective>
Add the `helm.sh/resource-policy: keep` annotation to all 6 CRD templates under `charts/tide-crds/templates/` (RESEARCH §"Topic 8 finding #5" — critical safety fix; VALIDATION.md flags as Wave 1 critical per HIGH-2 revision 2026-05-22). Extend `hack/helm/augment-tide-crds-chart.sh` to inject the annotation on every `make helm-crds` regeneration so the annotation survives.

Purpose: REQ-DIST-01 chart safety. Without this annotation, `helm uninstall tide-crds` deletes the CRDs, which cascades to delete every Project/Milestone/Phase/Plan/Task/Wave resource in the cluster. This is catastrophic data loss for any operator who uninstalls + reinstalls. Per RESEARCH §"Topic 8" + Pitfall 2, this annotation is the canonical Helm pattern for CRD preservation.

Wave 1 placement (per HIGH-2 revision): Plan 05-14 modifies `hack/helm/augment-tide-crds-chart.sh` + 6 generated CRD templates — NO file overlap with Plan 05-05 (which edits `hack/helm/tide-chart.yaml` + `hack/helm/tide-crds-chart.yaml`). Both plans can run in parallel in Wave 1.

Output: 6 CRD templates with the annotation present; augment script extended to be idempotent re-injector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@charts/tide-crds/templates/project-crd.yaml
@charts/tide-crds/templates/milestone-crd.yaml
@charts/tide-crds/templates/phase-crd.yaml
@charts/tide-crds/templates/plan-crd.yaml
@charts/tide-crds/templates/task-crd.yaml
@charts/tide-crds/templates/wave-crd.yaml
@hack/helm/augment-tide-crds-chart.sh
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend augment-tide-crds-chart.sh to inject helm.sh/resource-policy: keep + run it</name>
  <files>hack/helm/augment-tide-crds-chart.sh, charts/tide-crds/templates/milestone-crd.yaml, charts/tide-crds/templates/phase-crd.yaml, charts/tide-crds/templates/plan-crd.yaml, charts/tide-crds/templates/project-crd.yaml, charts/tide-crds/templates/task-crd.yaml, charts/tide-crds/templates/wave-crd.yaml</files>
  <read_first>
    - hack/helm/augment-tide-crds-chart.sh (current shape: lines 1-19; simple cp of tide-crds-chart.yaml to Chart.yaml — must be extended)
    - charts/tide-crds/templates/project-crd.yaml lines 1-10 (current metadata.annotations block — already has `controller-gen.kubebuilder.io/version: v0.20.1`; this plan adds a sibling `helm.sh/resource-policy: keep` line per PATTERNS.md §"P3.4")
    - charts/tide-crds/templates/milestone-crd.yaml (same annotations shape — verify lines 1-10)
    - charts/tide-crds/templates/{phase,plan,task,wave}-crd.yaml (verify each has the annotations block; the augment script must work for all 6)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md (RESEARCH lock — D-X3 lockstep CRD version bump means v1.0.0 + resource-policy annotation land together in the same v1.0 surface)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 8" lines 1235-1256 (canonical finding + recommendation — option (a) extend augment script via sed)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P3.4" (recommended pattern: option (a) extend augment script; option (b) kubebuilder source markers — but kubebuilder doesn't easily support CRD-level annotations through markers; option (a) wins)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-VALIDATION.md "CRD subchart safety annotation (Wave 1 critical — Research finding #5)" section (both items: annotation lands + augment script preserves)
  </read_first>
  <behavior>
    - `hack/helm/augment-tide-crds-chart.sh` is extended with a new injection step that adds `helm.sh/resource-policy: keep` annotation to each CRD template under `charts/tide-crds/templates/*-crd.yaml`
    - The injection is idempotent: running the script multiple times produces the same output (no duplicate annotations, no diff)
    - The injection uses portable awk (or sed compatible across GNU sed and macOS sed; if the existing repo standardizes on `sed -i`, use that with empty backup arg per cross-platform pattern `sed -i.bak ... && rm *.bak`)
    - All 6 CRD templates (project, milestone, phase, plan, task, wave) carry the annotation after augmentation
    - `helm lint charts/tide-crds` passes post-augmentation
    - `helm template charts/tide-crds | grep -c "helm.sh/resource-policy: keep"` returns exactly 6 (one per CRD)
    - Running `make helm-crds` produces clean output (no diff in charts/tide-crds/templates/*.yaml beyond what the augment script generates)
  </behavior>
  <action>
    Step 1: Read `hack/helm/augment-tide-crds-chart.sh` (current 19 lines per repository state). The current script only copies `tide-crds-chart.yaml` to `charts/tide-crds/Chart.yaml`. This plan extends it to ALSO add the annotation to each CRD template.

    Step 2: Edit `hack/helm/augment-tide-crds-chart.sh` to append (after the existing `cp` line at line 16 and the existing `echo OK` line at line 18) a new injection block. Choose between two patterns:

    Pattern A (portable across GNU/macOS sed) — preferred. The block uses `awk` to insert the new annotation line AFTER the existing `controller-gen.kubebuilder.io/version:` line in each CRD template, gated by a `grep -q` pre-check to ensure idempotency:

    For each `crd` matching `${CHART_DIR}/templates/*-crd.yaml`:
    - If the file does NOT already contain `helm.sh/resource-policy: keep`, run `awk` that prints every line as-is, EXCEPT when a line matches `/^    controller-gen.kubebuilder.io\/version:/` it prints that line then prints `    helm.sh/resource-policy: keep` on the next line.
    - Write the awk output to `${crd}.tmp` then `mv "${crd}.tmp" "${crd}"`.

    After the loop, emit `OK: charts/tide-crds/ augmented with helm.sh/resource-policy: keep on 6 CRDs`.

    The `awk` approach is portable + idempotent (the `grep -q` pre-check prevents double-insertion).

    Step 3: Run `bash hack/helm/augment-tide-crds-chart.sh` once. Verify each of the 6 CRD templates now has `helm.sh/resource-policy: keep` in its `metadata.annotations` block. Use `grep -l "helm.sh/resource-policy: keep" charts/tide-crds/templates/*-crd.yaml | wc -l` — must return 6.

    Step 4: Run `bash hack/helm/augment-tide-crds-chart.sh` again (idempotency check). The second run must NOT add a duplicate annotation. Use `grep -c "helm.sh/resource-policy: keep" charts/tide-crds/templates/project-crd.yaml` — must return 1.

    Step 5: Run `helm lint charts/tide-crds` — must exit 0.

    Step 6: Run `helm template charts/tide-crds` — must produce 6 CRDs, each containing `helm.sh/resource-policy: keep` in its annotations.

    Step 7: Run `make helm-crds` (the full helmify+augment chain). Verify the output is clean (no diff). This is the regen-resilience check: `make helm-crds` regenerates from `config/crd/bases/`, runs the augment script, and ends up with the annotation in place. If the diff is non-empty after `make helm-crds`, the augment script needs to handle the helmify output shape — executor iterates.

    NOTE: The annotation MUST land on the CRD's metadata.annotations block at LEVEL 1 indentation (4 spaces for `controller-gen.kubebuilder.io/version:` per existing format) — match the existing indentation exactly. The awk pattern in Pattern A above assumes 4-space indent; verify against the actual file.
  </action>
  <verify>
    <automated>bash hack/helm/augment-tide-crds-chart.sh && [ "$(grep -l 'helm.sh/resource-policy: keep' charts/tide-crds/templates/*-crd.yaml | wc -l)" -eq 6 ] && [ "$(grep -c 'helm.sh/resource-policy: keep' charts/tide-crds/templates/project-crd.yaml)" -eq 1 ] && bash hack/helm/augment-tide-crds-chart.sh && [ "$(grep -c 'helm.sh/resource-policy: keep' charts/tide-crds/templates/project-crd.yaml)" -eq 1 ] && helm lint charts/tide-crds && [ "$(helm template charts/tide-crds 2>/dev/null | grep -c 'helm.sh/resource-policy: keep')" -eq 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - `bash hack/helm/augment-tide-crds-chart.sh` exits 0
    - `grep -l 'helm.sh/resource-policy: keep' charts/tide-crds/templates/*-crd.yaml | wc -l` returns 6 (all six CRD files have the annotation)
    - Idempotency: running the script twice produces no diff in charts/tide-crds/templates/*.yaml; `grep -c 'helm.sh/resource-policy: keep' charts/tide-crds/templates/project-crd.yaml` returns exactly 1 (not 2 — no duplicate insertion)
    - `helm lint charts/tide-crds` exits 0
    - `helm template charts/tide-crds | grep -c 'helm.sh/resource-policy: keep'` returns 6 (each CRD rendered with the annotation)
    - `grep -c "helm.sh/resource-policy" hack/helm/augment-tide-crds-chart.sh` returns ≥ 1 (the script references the annotation)
    - `make helm-crds` exits 0 + `git diff --exit-code charts/tide-crds/` exits 0 (regen-resilient: `make helm-crds` produces identical output post-augment)
  </acceptance_criteria>
  <done>All 6 CRD templates carry `helm.sh/resource-policy: keep`; augment script is idempotent + regen-resilient; helm lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `helm uninstall tide-crds` → cluster CRD set | Without `resource-policy: keep`, uninstall cascades CRD delete → all CRDs gone → all Project/Milestone/Phase/Plan/Task/Wave resources cascaded delete → catastrophic data loss for the operator |
| `make helm-crds` regeneration → annotation preservation | The augment script must re-inject the annotation on every regen, OR the annotation gets stripped on the next CI run + accidentally lands in a release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14-01 | Denial of Service (data loss) | Operator uninstall of tide-crds | mitigate | `helm.sh/resource-policy: keep` annotation is Helm's canonical pattern for resource preservation; CRDs survive `helm uninstall tide-crds`; documented in docs/troubleshooting.md (Plan 05-10) row 7 + docs/INSTALL.md upgrade-order section (Plan 05-07) |
| T-05-14-02 | Tampering | Augment-script regen drift | mitigate | Acceptance criterion explicitly runs `make helm-crds && git diff --exit-code charts/tide-crds/` — fails if the regen produces non-empty diff; Plan 05-16 release-pipeline helmify-verify job mirrors this gate |
| T-05-14-03 | Tampering | Duplicate-annotation insertion via non-idempotent script | mitigate | Pre-check `grep -q` before injection prevents duplicate; acceptance criterion explicitly runs the script twice and asserts annotation count == 1 (not 2) |
</threat_model>

<verification>
After Task 1: `helm template charts/tide-crds | grep -c 'helm.sh/resource-policy: keep'` returns 6. `make helm-crds && git diff --exit-code charts/tide-crds/` exits 0.
</verification>

<success_criteria>
- All 6 CRD templates have the `helm.sh/resource-policy: keep` annotation
- Augment script extended + idempotent + regen-resilient
- helm lint passes
- Pitfall 2 / Topic 8 safety gap closed (Wave 1 critical — HIGH-2 revision)
- DIST-01 CRD-subchart-safety piece satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-14-SUMMARY.md` documenting annotation injection counts, idempotency check results, and commit SHA.
</output>
