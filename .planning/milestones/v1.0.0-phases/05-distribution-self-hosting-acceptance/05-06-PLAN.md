---
phase: 05-distribution-self-hosting-acceptance
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/tide-demo-fixture/README.md
  - examples/tide-demo-fixture/go.mod
  - examples/tide-demo-fixture/go.sum
  - examples/tide-demo-fixture/main.go
  - examples/tide-demo-fixture/main_test.go
autonomous: true
requirements: [DIST-04]
tags: [examples, fixture, scaffold, mit-license]

must_haves:
  truths:
    - "examples/tide-demo-fixture/ exists in THIS TIDE repo as the source-of-truth scaffold for the medium sample's target content (D-B3)"
    - "Fixture content is MIT-licensed inline (NOT TIDE's Apache-2.0) per D-B3 'distinct from TIDE's Apache-2.0 distribution'"
    - "Layout matches RESEARCH §P4.4 file list: README.md + go.mod + go.sum + main.go + main_test.go"
    - "main.go has a function TIDE will modify (the medium sample's outcome prompt operates on this)"
    - "main_test.go has one passing test for TIDE to extend"
    - "go.mod declares module github.com/jsquirrelz/tide-demo-fixture; go 1.26 (stdlib-only — no external deps)"
  artifacts:
    - path: "examples/tide-demo-fixture/README.md"
      provides: "Tiny Go scaffold descriptor + MIT license inline"
      contains: "MIT License"
    - path: "examples/tide-demo-fixture/go.mod"
      provides: "Tiny Go module root"
      contains: "module github.com/jsquirrelz/tide-demo-fixture"
    - path: "examples/tide-demo-fixture/main.go"
      provides: "Single-file Go program with a modifiable function"
      contains: "package main"
    - path: "examples/tide-demo-fixture/main_test.go"
      provides: "One passing unit test TIDE can extend"
      contains: "func Test"
  key_links:
    - from: "examples/tide-demo-fixture/main.go"
      to: "cmd/tide-demo-init/ (will embed this directory via //go:embed in Plan 05-12)"
      via: "go:embed directive in cmd/tide-demo-init/main.go embeds examples/tide-demo-fixture/"
      pattern: "//go:embed"
---

<objective>
Land the `examples/tide-demo-fixture/` scaffold (D-B3) — a tiny MIT-licensed Go module that serves as the source-of-truth content for the medium sample's local-only git remote (D-B1 ~$5 sample). The fixture is intentionally in this repo (NOT an external public repo per D-B3 explicit rejection) and is embedded via `//go:embed` into `cmd/tide-demo-init` in Plan 05-12.

Purpose: REQ-DIST-04 medium-sample target content. The fixture is the SEED — TIDE clones it via `file://` URL, plans modifications to its `main.go`, commits + pushes back to the local bare repo. The medium sample's outcome prompt operates on this content.

Output: 5 files under `examples/tide-demo-fixture/` (README + go.mod + go.sum + main.go + main_test.go), MIT-licensed inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@cmd/stub-subagent/main.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create examples/tide-demo-fixture/ scaffold</name>
  <files>examples/tide-demo-fixture/README.md, examples/tide-demo-fixture/go.mod, examples/tide-demo-fixture/go.sum, examples/tide-demo-fixture/main.go, examples/tide-demo-fixture/main_test.go</files>
  <read_first>
    - cmd/stub-subagent/main.go (closest analog for single-file Go entry-point shape; Apache-2.0 header convention — but tide-demo-fixture uses MIT inline per D-B3)
    - cmd/stub-subagent/main_test.go (closest analog for minimal _test.go shape)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-B3" (locks: MIT inline header, lives in THIS repo, is the source-of-truth for medium sample target content, fresh local-only git remote semantics)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P4.4" (file layout: README + go.mod + go.sum + main.go + main_test.go; no in-repo analog for MIT-inline doc convention)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Recommended File Layout" lines 350-356 (canonical layout)
    - go.mod (root) for the Go version pin pattern (current `go 1.26.0`); CLAUDE.md tech stack says Go 1.26 — fixture uses `go 1.26`
  </read_first>
  <behavior>
    - Directory `examples/tide-demo-fixture/` exists with exactly 5 files: README.md, go.mod, go.sum, main.go, main_test.go
    - `README.md` opens with `# tide-demo-fixture` heading + one-paragraph framing + MIT license inline block (per D-B3 "MIT-licensed within itself")
    - `go.mod` declares `module github.com/jsquirrelz/tide-demo-fixture` + `go 1.26` (stdlib-only)
    - `go.sum` is empty (no external deps means an empty go.sum is correct and `go mod tidy` is a no-op — actually `go mod tidy` removes go.sum entirely if there are no deps; the file may be created empty or absent — we create it empty for clarity)
    - `main.go` declares `package main`, has a `main()` function, and contains exactly one named function (e.g., `Greeting(name string) string`) that TIDE will modify. The function returns a deterministic string so the test can assert against it
    - `main_test.go` declares `package main`, contains one passing test (e.g., `TestGreeting`) that asserts the function's current behavior. The test passes when run with `go test ./...` inside the fixture directory
    - `cd examples/tide-demo-fixture && go test ./...` exits 0
    - No file references TIDE's Apache-2.0 license or any TIDE-specific package
  </behavior>
  <action>
    Create directory `examples/tide-demo-fixture/` and the five files within.

    File 1: `examples/tide-demo-fixture/README.md`. Heading `# tide-demo-fixture`. One paragraph: "Tiny Go scaffold for the TIDE medium sample (Phase 5 D-B3). On medium-sample apply, TIDE clones this content from a local-only `file://` git remote, plans a modification to `main.go`, commits + pushes back. Operators see TIDE drive its full clone → plan → execute → push loop offline." Then a level-2 heading `## License` followed by the standard MIT license boilerplate text (the canonical ~20-line MIT License text starting with "MIT License", "Copyright (c) 2026 The TIDE Authors", and the standard "Permission is hereby granted, free of charge..." paragraph). The MIT license is intentionally distinct from TIDE's Apache-2.0 distribution per D-B3 — be explicit about this in a one-line note: "*This fixture is MIT-licensed and distributed alongside (but distinct from) TIDE's Apache-2.0 distribution per Phase 5 D-B3.*"

    File 2: `examples/tide-demo-fixture/go.mod`. Two lines: `module github.com/jsquirrelz/tide-demo-fixture` then a blank line then `go 1.26`.

    File 3: `examples/tide-demo-fixture/go.sum`. Empty file (zero bytes). Stdlib-only means no entries needed. If the executor's `go mod tidy` removes it, that's acceptable — the file is a placeholder for forward-compat.

    File 4: `examples/tide-demo-fixture/main.go`. Apache-2.0 boilerplate header is NOT applied here (this fixture is MIT, not Apache). Instead, put a short MIT-license attribution comment at the top: `// SPDX-License-Identifier: MIT` then `// Copyright (c) 2026 The TIDE Authors` then a blank line.

    Body of `main.go`:
    - Package declaration: `package main`
    - Imports: `import "fmt"` (stdlib only)
    - One named function: `func Greeting(name string) string { return "Hello, " + name + "!" }`
    - `func main() { fmt.Println(Greeting("world")) }`

    File 5: `examples/tide-demo-fixture/main_test.go`. Same MIT SPDX header at top. Body:
    - Package declaration: `package main`
    - Imports: `import "testing"`
    - One test: `func TestGreeting(t *testing.T) { got := Greeting("Alice"); want := "Hello, Alice!"; if got != want { t.Errorf("Greeting(\"Alice\") = %q, want %q", got, want) } }`

    Run `cd examples/tide-demo-fixture && go test ./...` to verify the test passes. The expected output is `ok  	github.com/jsquirrelz/tide-demo-fixture	0.XXXs` (or similar Go test PASS line).

    NOTE: The fixture's `go.mod` declares it as a separate Go module. It is NOT added to the TIDE root go.mod's workspace or replace directives — it stands alone. This is intentional: the fixture is content that TIDE clones from a `file://` URL, not a dep of TIDE's own build.
  </action>
  <verify>
    <automated>test -d examples/tide-demo-fixture && test -s examples/tide-demo-fixture/README.md && test -s examples/tide-demo-fixture/go.mod && test -f examples/tide-demo-fixture/go.sum && test -s examples/tide-demo-fixture/main.go && test -s examples/tide-demo-fixture/main_test.go && grep -q "MIT License" examples/tide-demo-fixture/README.md && grep -q "module github.com/jsquirrelz/tide-demo-fixture" examples/tide-demo-fixture/go.mod && grep -q "package main" examples/tide-demo-fixture/main.go && grep -q "SPDX-License-Identifier: MIT" examples/tide-demo-fixture/main.go && cd examples/tide-demo-fixture && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `test -d examples/tide-demo-fixture` returns 0
    - Files exist: `ls examples/tide-demo-fixture/` lists README.md, go.mod, go.sum, main.go, main_test.go
    - `test -s examples/tide-demo-fixture/README.md` returns 0
    - `grep -q "MIT License" examples/tide-demo-fixture/README.md` returns 0
    - `grep -q "Copyright (c) 2026 The TIDE Authors" examples/tide-demo-fixture/README.md` returns 0
    - `grep -q "module github.com/jsquirrelz/tide-demo-fixture" examples/tide-demo-fixture/go.mod` returns 0
    - `grep -q "^go 1.26" examples/tide-demo-fixture/go.mod` returns 0
    - `grep -q "SPDX-License-Identifier: MIT" examples/tide-demo-fixture/main.go` returns 0
    - `grep -q "package main" examples/tide-demo-fixture/main.go` returns 0
    - `grep -q "func Greeting" examples/tide-demo-fixture/main.go` returns 0
    - `grep -q "func main()" examples/tide-demo-fixture/main.go` returns 0
    - `grep -q "func TestGreeting" examples/tide-demo-fixture/main_test.go` returns 0
    - `(cd examples/tide-demo-fixture && go test ./...)` exits 0 (the fixture's own test passes)
    - `grep -q "Apache License" examples/tide-demo-fixture/main.go` returns 1 (NOT 0 — file MUST NOT carry TIDE's Apache-2.0 header per D-B3)
  </acceptance_criteria>
  <done>5-file MIT-licensed Go scaffold lives at examples/tide-demo-fixture/; `go test` passes; license is MIT (not Apache) per D-B3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TIDE Apache-2.0 distribution → MIT-licensed fixture content | Mixing licenses without clear demarcation creates compliance ambiguity; D-B3 mandates the fixture be MIT-inline distinct from TIDE's Apache-2.0 |
| `cmd/tide-demo-init` go:embed → fixture content | Plan 05-12's binary embeds this directory; the binary's Dockerfile (Plan 05-12 also) becomes the in-cluster vehicle for the fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | License Compliance (custom) | MIT vs Apache-2.0 mix | mitigate | README.md MIT block + SPDX-License-Identifier on each .go file + explicit "distinct from TIDE's Apache-2.0 distribution" note; acceptance grep enforces presence + acceptance grep rejects Apache-2.0 header presence |
| T-05-06-02 | Tampering | Fixture content drift between Phase 5 SOT and what TIDE clones at runtime | accept | Fixture is small (single Go file + test); drift between repo SOT and runtime clone is a hygiene concern not a security boundary; Plan 05-12 embeds at build time so binary ships fixed content |
| T-05-06-03 | Information Disclosure | Module path `github.com/jsquirrelz/tide-demo-fixture` | accept | Module path is the canonical name in case the fixture is ever pulled out; D-B3 explicitly rejects the external public repo, so this is namespace-reservation only |
</threat_model>

<verification>
`(cd examples/tide-demo-fixture && go test ./...)` exits 0. All 5 files present.
</verification>

<success_criteria>
- 5 files under examples/tide-demo-fixture/
- MIT license inline (per D-B3) — Apache-2.0 header explicitly absent
- `go test` inside the fixture passes
- Fixture ready to be //go:embed-ed by Plan 05-12's `cmd/tide-demo-init`
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-06-SUMMARY.md` documenting the 5 file sizes, `go test` output, and commit SHA.
</output>
