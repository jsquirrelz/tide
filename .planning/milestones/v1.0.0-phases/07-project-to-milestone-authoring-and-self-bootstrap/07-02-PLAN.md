---
phase: 07-project-to-milestone-authoring-and-self-bootstrap
plan: 02
type: execute
wave: 0
depends_on:
  - "07-01"
files_modified:
  - test/integration/kind/bare_project_test.go
autonomous: true
requirements:
  - REQ-1
  - REQ-2
  - REQ-4
  - REQ-5
  - REQ-7a
  - REQ-7b

must_haves:
  truths:
    - "A Layer B Ginkgo spec file exists for the bare-Project cascade"
    - "The spec asserts all four CRD kinds (Milestone, Phase, Plan, Task) materialize"
    - "The spec asserts Plan.Status.ValidationState==Validated and Wave materializes"
    - "The spec asserts Project reaches status.phase=Complete"
    - "The spec compiles and can be discovered by make test-int"
  artifacts:
    - path: "test/integration/kind/bare_project_test.go"
      provides: "Layer B spec covering REQ-1/2/4/5/7a/7b (bare Project cascade)"
      contains: "Describe.*bare Project"
    - path: "test/integration/kind/testdata/bare-project.yaml"
      provides: "Fixture applied by this spec (created in Plan 07-01)"
  key_links:
    - from: "test/integration/kind/bare_project_test.go"
      to: "test/integration/kind/testdata/bare-project.yaml"
      via: "applyFile()"
      pattern: "applyFile.*bare-project"
    - from: "test/integration/kind/bare_project_test.go"
      to: "test/integration/kind/suite_test.go"
      via: "ensureProjectsPVC, ensureSubagentSA, ensureSigningKeySecret, pvcPrewarmPod helpers"
      pattern: "ensureProjectsPVC|ensureSubagentSA"
---

<objective>
Create the Layer B integration test spec (bare_project_test.go) that asserts the full bare-Project self-bootstrap cascade.

Purpose: This is the primary acceptance gate for REQ-1, REQ-2, REQ-4, REQ-5, REQ-7a, and REQ-7b. The spec is written before implementation (Wave 0) so it drives development RED → GREEN. It will be RED through Waves 1-3 and turn GREEN after Plan 07-05 completes. It MUST NOT be skipped or shortened — every assertion is load-bearing for the acceptance bar.

Output: test/integration/kind/bare_project_test.go — a Ginkgo v2 Layer B spec in package kind_integration that applies the bare-project.yaml fixture, asserts the full cascade materializes (Milestone → Phase → Plan → Task) and that Project.Status.Phase reaches Complete. Budget: ~5 minutes of wall-clock Jobs, well within the 18-minute kindTestTimeout.
</objective>

<execution_context>
@/Users/justinsearles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsearles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-SPEC.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-CONTEXT.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-RESEARCH.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-VALIDATION.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From test/integration/kind/suite_test.go (package kind_integration):
  var k8sClient client.Client       // initialized in BeforeSuite
  var ctx context.Context           // suite-level context
  const kindTestTimeout = 18 * time.Minute

  // Helpers (all already exist — reuse verbatim):
  ensureSubagentSA(ns string)              // creates tide-subagent SA in namespace
  ensureProjectsPVC(ns string)             // creates tide-projects PVC (RWO override for kind)
  pvcPrewarmPod(ns string)                 // pre-warms WaitForFirstConsumer PVC
  ensureSigningKeySecret(ns string)        // mirrors tide-system/tide-signing-key into namespace
  applyFile(path string) error             // applies YAML via kubectl
  deleteNamespace(ns string)               // AfterEach cleanup

From test/integration/kind/up_stack_dispatch_test.go (pattern to follow):
  const upStackNS = "up-stack-test"
  var _ = Describe("...", Label("kind"), func() {
      BeforeEach(func() {
          ensureSubagentSA(upStackNS)
          ensureProjectsPVC(upStackNS)
          pvcPrewarmPod(upStackNS)
          ensureSigningKeySecret(upStackNS)
          Expect(applyFile("testdata/up-stack-project.yaml")).To(Succeed())
      })
      AfterEach(func() { deleteNamespace(upStackNS) })
      It("...", Label("kind"), func() {
          Eventually(func() error { ... }, 3*time.Minute, 2*time.Second).Should(Succeed())
      })
  })

From api/v1alpha1/ (CRD types):
  tideprojectv1alpha1.Project      // .Status.Phase string
  tideprojectv1alpha1.Milestone    // .Status.Phase string
  tideprojectv1alpha1.Phase        // .Status.Phase string (named "Phase" type, namespace conflict — use tideprojectv1alpha1.Phase)
  tideprojectv1alpha1.Plan         // .Status.Phase string, .Status.ValidationState string
  tideprojectv1alpha1.Task         // .Status.Phase string
  tideprojectv1alpha1.Wave         // .Spec.PlanRef string

From client (sigs.k8s.io/controller-runtime/pkg/client):
  k8sClient.Get(ctx, client.ObjectKey{Name:..., Namespace:...}, obj)
  k8sClient.List(ctx, list, client.InNamespace(ns), client.MatchingFields{...})
  // For listing owned children: use ownerReferences filter in Eventually loop

Research note (from 07-RESEARCH.md §"Critical Gap #2"):
  Project.Status.Phase reaches "Complete" as soon as Milestone.Status.Phase=="Succeeded".
  Milestone.Status.Phase reaches "Succeeded" immediately after ITS OWN planner Job completes
  (milestone_controller.go:431 calls patchMilestoneSucceeded unconditionally on Job done).
  Therefore Project=Complete fires BEFORE Phase/Plan/Task are necessarily Succeeded.
  The spec MUST assert each child CRD EXISTS (materialized), and separately assert
  Plan.Status.ValidationState=="Validated" and Task.Status.Phase=="Succeeded" to prove
  the full REQ-7a/7b chain. Order assertions correctly — Project=Complete is a separate
  assertion that may happen earlier than Task=Succeeded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bare_project_test.go — Layer B cascade spec</name>
  <files>test/integration/kind/bare_project_test.go</files>
  <action>
    Create test/integration/kind/bare_project_test.go in package kind_integration.

    File header: Apache 2.0 license comment block (match existing test file headers). Then package declaration: package kind_integration.

    Imports needed: fmt, time, github.com/onsi/ginkgo/v2, github.com/onsi/gomega, k8s.io/apimachinery/pkg/api/errors (apierrors), sigs.k8s.io/controller-runtime/pkg/client, tideprojectv1alpha1 "github.com/jsquirrelz/tide/api/v1alpha1".

    Namespace constant: const bareProjectNS = "bare-project-test".

    Describe block: Label "kind". BeforeEach: call ensureSubagentSA(bareProjectNS), ensureProjectsPVC(bareProjectNS), pvcPrewarmPod(bareProjectNS), ensureSigningKeySecret(bareProjectNS), then Expect(applyFile("testdata/bare-project.yaml")).To(Succeed()). AfterEach: deleteNamespace(bareProjectNS).

    Single It block: "bare Project self-bootstraps full cascade to Project=Complete (REQ-1..5 + REQ-7a/b)", Label("kind").

    Inside the It block, write these Eventually assertions IN ORDER (each with appropriate timeout — see budget note in 07-RESEARCH.md §Test Harness):

    Assertion 1 — Milestone materializes (timeout 3 minutes):
      List Milestones in bareProjectNS; filter to those with an ownerReference whose Name matches "bare-project" and Controller=true. Eventually resolves when len >= 1. Store the milestone name for subsequent assertions.

    Assertion 2 — Milestone.Status.Phase reaches Succeeded (timeout 4 minutes):
      Get the Milestone by name; Eventually its Status.Phase == "Succeeded".

    Assertion 3 — Phase materializes owned by the Milestone (timeout 5 minutes):
      List Phases in bareProjectNS; filter to owner = milestone name. Eventually len >= 1. Store phase name.

    Assertion 4 — Plan materializes owned by the Phase (timeout 6 minutes):
      List Plans in bareProjectNS; filter to owner = phase name. Eventually len >= 1. Store plan name.

    Assertion 5 — Plan.Status.ValidationState == "Validated" (timeout 7 minutes, per REQ-7a):
      Get the Plan by name; Eventually its Status.ValidationState == "Validated".

    Assertion 6 — Task materializes owned by the Plan (timeout 7 minutes):
      List Tasks in bareProjectNS; filter to owner = plan name. Eventually len >= 1. Store task name.

    Assertion 7 — Wave materializes in bareProjectNS (timeout 8 minutes, verifies reconcileWaveMaterialization ran):
      List Waves in bareProjectNS; Eventually len >= 1.

    Assertion 8 — Task.Status.Phase reaches Succeeded (timeout 9 minutes, per REQ-7b proof that executor Job ran):
      Get Task by name; Eventually Status.Phase == "Succeeded".

    Assertion 9 — Project.Status.Phase reaches Complete (timeout 10 minutes, REQ-4):
      Get the Project named "bare-project"; Eventually Status.Phase == "Complete".

    Polling interval: 2 seconds for all Eventually calls.

    Owner reference filter helper: define a local function filterByOwner(objects []metav1.Object, ownerName string) []metav1.Object that iterates ownerReferences and returns objects where any ownerRef.Name == ownerName. Use this for assertions 1, 3, 4, 6.

    Do NOT use apierrors.IsNotFound as a pass condition — return the error so Eventually retries. The pattern is: err := k8sClient.Get(...); if err != nil { return err }; if obj.Status.Phase != "X" { return fmt.Errorf("want X got %s", obj.Status.Phase) }; return nil.

    Budget note: the full cascade takes ~160s of Job wall-clock time (5 planner Jobs + 1 executor Job at ~30s each). The 10-minute outer timeout is well within kindTestTimeout=18m.

    This spec will be RED until Plans 07-03 through 07-05 implement the production code. That is expected.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./test/integration/kind/... 2>&1 | head -20</automated>
  </verify>
  <done>test/integration/kind/bare_project_test.go exists; go build ./test/integration/kind/... exits 0; file contains all 9 Eventually assertions as described; assertions are in order (Milestone→Milestone.Succeeded→Phase→Plan→Plan.Validated→Task→Wave→Task.Succeeded→Project.Complete)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→K8s API (kind cluster) | Ginkgo spec sends kubectl apply and k8s client Get/List calls — operates as the test harness identity (cluster-admin in kind) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | bare_project_test.go | accept | Test namespace "bare-project-test" uses absoluteCapCents=0 and stub image; no real API key or LLM spend; deleteNamespace in AfterEach ensures cleanup |
| T-07-02-02 | Denial of Service | Eventually timeouts | accept | 10-minute per-assertion budgets are well within kindTestTimeout=18m; Phase 02.2 budget arithmetic established this is safe (inner go test ≤ 355s existing; new spec adds ~160s Job time) |
</threat_model>

<verification>
After task completes:
1. go build ./test/integration/kind/... exits 0
2. grep -c "Eventually" test/integration/kind/bare_project_test.go returns >= 9
3. grep "Project=Complete\|ValidationState\|Succeeded\|Wave" test/integration/kind/bare_project_test.go | wc -l returns >= 5
4. grep "Label.*kind" test/integration/kind/bare_project_test.go confirms kind label present
5. make test-int would compile and discover the spec (it would fail at runtime because production code not yet written — expected RED)
</verification>

<success_criteria>
Layer B spec file exists and compiles. Contains all 9 assertions in correct order. Intentionally RED until production code lands in Plans 07-03 through 07-05. Committed to git.
</success_criteria>

<output>
After completion, create .planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-02-SUMMARY.md
</output>
