---
phase: 07-project-to-milestone-authoring-and-self-bootstrap
plan: 04
type: execute
wave: 2
depends_on:
  - "07-03"
files_modified:
  - internal/controller/plan_controller.go
autonomous: true
requirements:
  - REQ-7a
  - REQ-7b

must_haves:
  truths:
    - "Plan.Status.ValidationState is stamped Validated in production after planner Job completes and Tasks materialize"
    - "PlanReconciler has a patchPlanSucceeded method that fires when all owned Tasks are Succeeded"
    - "reconcileWaveMaterialization proceeds past the ValidationState gate and creates Wave CRs"
    - "PhaseReconciler.handleJobCompletion can observe Plan.Status.Phase=Succeeded and advance"
    - "No regression in existing plan_controller tests"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "ValidationState=Validated stamp in handlePlannerJobCompletion + patchPlanSucceeded triggered by BoundaryDetected(plan, Task)"
      contains: "ValidationState.*Validated\|patchPlanSucceeded"
  key_links:
    - from: "plan_controller.go handlePlannerJobCompletion"
      to: "plan.Status.ValidationState"
      via: "r.Status().Patch after MaterializeChildCRDs succeeds and len(envOut.ChildCRDs) > 0"
      pattern: "ValidationState.*=.*Validated"
    - from: "plan_controller.go reconcileWaveMaterialization"
      to: "patchPlanSucceeded"
      via: "BoundaryDetected(ctx, r.Client, plan, Task) called after Wave materialization"
      pattern: "patchPlanSucceeded\|BoundaryDetected.*Task"
---

<objective>
Fix the two production gaps (REQ-7a and REQ-7b) that prevent the Phase→Plan→Task chain from completing.

Purpose: Research (07-RESEARCH.md) confirmed at HIGH confidence that two production gaps block the full cascade: (a) Plan.Status.ValidationState is never set to "Validated" in production code, causing reconcileWaveMaterialization to no-op forever; (b) PlanReconciler has no patchPlanSucceeded, so PhaseReconciler.handleJobCompletion loops forever waiting for Plan=Succeeded. These are the ONLY two down-stack reconciler edits permitted by SPEC boundary. If a third cascade surfaces during execution, surface it — do not silently expand (CLAUDE.md "don't predict chain terminator").

Output: Two additions to plan_controller.go only — the ValidationState stamp in handlePlannerJobCompletion and a new patchPlanSucceeded called from reconcileWaveMaterialization when BoundaryDetected(plan, "Task") is true.
</objective>

<execution_context>
@/Users/justinsearles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsearles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-SPEC.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-CONTEXT.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-RESEARCH.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-03-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From internal/controller/plan_controller.go:

  // handlePlannerJobCompletion (lines 355-458):
  // EXISTING flow: readEnvOut → MaterializeChildCRDs → gate checks → maybeTriggerBoundaryPush → clear Phase=""
  // INSERTION POINT for REQ-7a: AFTER len(envOut.ChildCRDs) > 0 MaterializeChildCRDs succeeds,
  //   BEFORE gate checks (lines 406+), stamp ValidationState="Validated":
  //
  //   if len(envOut.ChildCRDs) > 0 {
  //       // (existing MaterializeChildCRDs call succeeds here)
  //       // REQ-7a: stamp Validated so reconcileWaveMaterialization proceeds
  //       patch := client.MergeFrom(plan.DeepCopy())
  //       plan.Status.ValidationState = "Validated"
  //       if err := r.Status().Patch(ctx, plan, patch); err != nil { return ctrl.Result{}, err }
  //   }

  // reconcileWaveMaterialization (lines 533-649):
  // Gate at line 535: if plan.Status.ValidationState != "Validated" { return ctrl.Result{}, nil }
  // After materializeWaves and stampTaskLabels succeed, the function returns without Succeeded patch.
  // INSERTION POINT for REQ-7b: at the END of reconcileWaveMaterialization, AFTER stampTaskLabels,
  //   call BoundaryDetected(ctx, r.Client, plan, "Task") and if true, call r.patchPlanSucceeded(ctx, plan).

  // gates.BoundaryDetected signature:
  func BoundaryDetected(ctx context.Context, c client.Client, parent metav1.Object, childKind string) (bool, error)
  // Returns true when ≥1 owned child of childKind exists AND ALL are Status.Phase=="Succeeded".
  // "Task" is a valid childKind (gates/boundary.go handles it via dynamic list on tideprojectv1alpha1.TaskList).
  // Returns (false, nil) when no children exist (child-less guard prevents premature Succeeded).

  // PlanReconciler struct already has all needed fields:
  type PlanReconciler struct {
      client.Client
      Scheme *runtime.Scheme
      PlannerPool  *pool.Pool
      ExecutorPool *pool.Pool
      EnvReader    podjob.EnvelopeReader
      SubagentImage   string
      CredproxyImage  string
      SigningKey       []byte
      HelmProviderDefaults ProviderDefaults
      // ... other fields
  }

  // Existing Succeeded short-circuit (line 214 of plan_controller.go):
  //   if plan.Status.Phase == "Succeeded" { return ctrl.Result{}, nil }
  //   This guard prevents re-entering reconcileWaveMaterialization after Succeeded — correct.

  // patchPlanSucceeded (NEW method to add):
  func (r *PlanReconciler) patchPlanSucceeded(ctx context.Context, plan *tideprojectv1alpha1.Plan) (ctrl.Result, error) {
      patch := client.MergeFrom(plan.DeepCopy())
      plan.Status.Phase = "Succeeded"
      meta.SetStatusCondition(&plan.Status.Conditions, metav1.Condition{
          Type:               tideprojectv1alpha1.ConditionSucceeded,
          Status:             metav1.ConditionTrue,
          Reason:             "TasksCompleted",
          Message:            "All owned Tasks reached Succeeded; Plan complete",
          LastTransitionTime: metav1.Now(),
      })
      return ctrl.Result{}, r.Status().Patch(ctx, plan, patch)
  }

From internal/gates/boundary.go (verified behavior):
  // BoundaryDetected("Task") lists Tasks owned by the Plan in the Plan's namespace.
  // Returns false if zero Tasks exist (child-less guard).
  // Returns true only when ALL owned Tasks have Status.Phase == "Succeeded".
  // Correct behavior: Plan stays non-Succeeded until TaskReconciler drives Task=Succeeded.

From api/v1alpha1/plan_types.go:
  // Plan.Status.ValidationState is a string field (no enum constraint in Go types):
  type PlanStatus struct {
      Phase           string
      ValidationState string   // "" | "Validated" | "CycleDetected"
      CycleEdges      []string
      Conditions      []metav1.Condition
  }

  // Condition types (from api/v1alpha1/shared_types.go):
  const ConditionSucceeded = "Succeeded"  // or similar — check shared_types.go before using

CRITICAL: Read plan_controller.go fully before editing. The insertion points described above are line-number estimates from the research phase; the actual current file may differ slightly. Observe First (CLAUDE.md rule 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stamp ValidationState=Validated in handlePlannerJobCompletion (REQ-7a)</name>
  <files>internal/controller/plan_controller.go</files>
  <action>
    Read internal/controller/plan_controller.go first — specifically handlePlannerJobCompletion (currently lines 355-458 per research, but verify).

    Locate the block that handles len(envOut.ChildCRDs) > 0 and calls MaterializeChildCRDs. This block currently looks like:

      if len(envOut.ChildCRDs) > 0 {
          if mErr := MaterializeChildCRDs(...); mErr != nil { ... set Failed ... return }
      }

    IMMEDIATELY AFTER the closing brace of this if-block (i.e., after MaterializeChildCRDs has succeeded), add a separate status patch that sets plan.Status.ValidationState = "Validated". Use a client.MergeFrom patch pair (deepcopy before modification). This must be a distinct patch from the existing "clear Phase" patch that comes later.

    Place the stamp BEFORE the gate-policy checks (CheckRejected, EvaluatePolicy) and BEFORE maybeTriggerBoundaryPush. The rationale: the planner Job just authored Tasks — they are by definition valid for $0 stub execution. Stamping here is the correct controller seam (per 07-RESEARCH.md §Insertion Point).

    Do NOT add the stamp inside the len(envOut.ChildCRDs) > 0 block itself — add it after the closing brace, as a separate unconditional stamp when ChildCRDs were successfully materialized. If ChildCRDs is empty, do NOT stamp (no Tasks authored means no Wave to materialize).

    The stamp is: plan.Status.ValidationState = "Validated".

    Handle the patch error: if r.Status().Patch fails, return ctrl.Result{}, err.

    Do not touch any other part of handlePlannerJobCompletion.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./internal/controller/... -run "TestPlan" -v 2>&1 | tail -30</automated>
  </verify>
  <done>go test ./internal/controller/... -run TestPlan exits 0 (no regressions); grep -c "ValidationState.*Validated\|Validated.*ValidationState" internal/controller/plan_controller.go returns >= 1; grep returns the line in handlePlannerJobCompletion context (not just test code)</done>
</task>

<task type="auto">
  <name>Task 2: Add patchPlanSucceeded and wire via BoundaryDetected in reconcileWaveMaterialization (REQ-7b)</name>
  <files>internal/controller/plan_controller.go</files>
  <action>
    Read internal/controller/plan_controller.go again to confirm the current state after Task 1, specifically reconcileWaveMaterialization (currently lines 533-649 per research).

    Add a new private method patchPlanSucceeded(ctx context.Context, plan *tideprojectv1alpha1.Plan) (ctrl.Result, error) to PlanReconciler. This method:
      - Creates a MergeFrom patch pair (deepcopy before modification)
      - Sets plan.Status.Phase = "Succeeded"
      - Calls meta.SetStatusCondition with a Condition of type tideprojectv1alpha1.ConditionSucceeded (or the correct constant from api/v1alpha1/shared_types.go — read that file to confirm the constant name before using it), Status=ConditionTrue, Reason="TasksCompleted", Message="All owned Tasks reached Succeeded; Plan complete"
      - Patches and returns

    Place patchPlanSucceeded near the other patchPlan* methods (patchPlanFailed is at line 462 per research; place patchPlanSucceeded adjacent to it).

    In reconcileWaveMaterialization, locate the final return statement (after stampTaskLabels and resolveProjectName succeed). BEFORE that final return, add:

      detected, derr := gates.BoundaryDetected(ctx, r.Client, plan, "Task")
      if derr != nil { return ctrl.Result{}, derr }
      if detected { return r.patchPlanSucceeded(ctx, plan) }

    This check fires on EVERY reconcile of reconcileWaveMaterialization. The Succeeded short-circuit at the top of Reconcile (plan.Status.Phase == "Succeeded") prevents re-entry after the Plan is Succeeded. The BoundaryDetected child-less guard (returns false when 0 Tasks exist) prevents premature Succeeded before Tasks are created.

    Do NOT add a RequeueAfter — when Tasks are dispatched and not yet Succeeded, BoundaryDetected returns false, reconcileWaveMaterialization returns ctrl.Result{} (no-op), and the Owns(&Task{}) watch re-enqueues the Plan when Task status updates.

    Ensure the gates package is already imported (grep for "internal/gates" in plan_controller.go — it should be there from Phase 4).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./internal/controller/... 2>&1 | tail -20</automated>
  </verify>
  <done>go test ./internal/controller/... exits 0 (all existing plan, phase, milestone, wave, task tests pass); grep -c "func (r \*PlanReconciler) patchPlanSucceeded" internal/controller/plan_controller.go returns 1; grep -c "BoundaryDetected.*Task\|Task.*BoundaryDetected" internal/controller/plan_controller.go returns >= 1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan_controller.go → k8s status API | Status patches for ValidationState and Succeeded go through the standard controller-runtime Status().Patch() path — same as all other status transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | ValidationState stamp | accept | ValidationState="Validated" is stamped by the trusted PlanReconciler after MaterializeChildCRDs succeeds — not from user input. The admission webhook's cycle-detection still runs on Plan CREATE (it does not gate on ValidationState). Defense-in-depth is preserved: if the planner emits a cyclic Task DAG, reconcileWaveMaterialization's ComputeWaves detects it and sets ValidationState="CycleDetected" (line 566), which short-circuits future reconciles. |
| T-07-04-02 | Elevation of Privilege | patchPlanSucceeded | accept | Plan.Status.Phase="Succeeded" is a terminal state guarded by the Succeeded short-circuit at Reconcile entry (line 214). Once set, the reconciler does not re-enter reconcileWaveMaterialization. No escalation path from Plan=Succeeded to any privileged operation. |
| T-07-04-03 | Denial of Service | BoundaryDetected("Task") on every reconcileWaveMaterialization | accept | BoundaryDetected does a single List call on Tasks in the Plan's namespace with a field index selector. O(n) in number of Tasks; n=1 for the stub (single-task wave). Phase 02.2 baseline verified List latency is negligible in kind. No new DoS surface. |
</threat_model>

<verification>
After both tasks complete:
1. go test ./internal/controller/... exits 0 — no regressions
2. go build ./... exits 0
3. grep -c "ValidationState.*=.*\"Validated\"" internal/controller/plan_controller.go returns >= 1 (in production code, not test code)
4. grep -v "^//" internal/controller/plan_controller.go | grep -c "ValidationState.*Validated" returns >= 1
5. grep -c "func (r \*PlanReconciler) patchPlanSucceeded" internal/controller/plan_controller.go returns 1
6. grep -c "BoundaryDetected.*\"Task\"" internal/controller/plan_controller.go returns >= 1
</verification>

<success_criteria>
Both production gaps from 07-RESEARCH.md are fixed. ValidationState="Validated" is stamped in handlePlannerJobCompletion after Task children materialize (REQ-7a). patchPlanSucceeded fires when BoundaryDetected(plan, "Task") is true (REQ-7b). No regression in existing plan_controller tests. Files committed to git.
</success_criteria>

<output>
After completion, create .planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-04-SUMMARY.md
</output>
