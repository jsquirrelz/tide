---
phase: 07-project-to-milestone-authoring-and-self-bootstrap
plan: 06
type: execute
wave: 4
depends_on:
  - "07-05"
files_modified: []
autonomous: false
requirements:
  - REQ-6

must_haves:
  truths:
    - "make acceptance-v1-smoke exits 0"
    - "Project status.phase reaches Complete in the smoke run"
    - "The run log shows Milestone, Phase, Plan, Task CRDs materializing"
    - "No ANTHROPIC_API_KEY is consumed (cost = $0)"
    - "hack/scripts/acceptance-v1.sh is NOT modified"
    - "The existing 7/7 Layer B + 18/18 Layer A baseline remains green"
  artifacts:
    - path: "hack/scripts/acceptance-v1.sh"
      provides: "Unchanged smoke script (verify no edits)"
      contains: "acceptance-v1"
    - path: "examples/projects/small/project.yaml"
      provides: "Unchanged smoke fixture (verify no edits, gates already auto)"
      contains: "gates"
  key_links:
    - from: "Makefile acceptance-v1-smoke target"
      to: "hack/scripts/acceptance-v1.sh"
      via: "make target invokes script"
      pattern: "acceptance-v1-smoke"
    - from: "hack/scripts/acceptance-v1.sh"
      to: "examples/projects/small/project.yaml"
      via: "kubectl apply -f"
      pattern: "small/project.yaml"
---

<objective>
Run make acceptance-v1-smoke and verify Project reaches Complete at $0 — the cascade-7 closure gate.

Purpose: This is the v1.0 ship-readiness gate. All production code is in place after Plans 07-01 through 07-05. This plan runs the acceptance script, reads the output, and captures evidence of the cascade. No code changes are allowed — only verification. If the smoke run fails, surface the failure reason and do NOT silently expand scope.

Output: A VERIFICATION artifact capturing the acceptance run evidence. No files are modified.
</objective>

<execution_context>
@/Users/justinsearles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsearles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-SPEC.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-CONTEXT.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run regression suite — verify 7/7 Layer B + 18/18 Layer A baseline</name>
  <files></files>
  <action>
    Run make test-int and capture the result. This must show:
      - All 7 existing Layer B kind specs PASS (no new regressions from Phase 7 edits)
      - All 18 existing Layer A envtest specs PASS
      - The new bare_project_test.go spec PASSES (asserting the full cascade)

    If make test-int fails, read the failure output carefully and identify whether it is:
      (a) A regression in an existing test (must be fixed — do not proceed to Task 2)
      (b) A failure in the new bare_project_test.go spec (diagnose and fix the root cause — "Observe First" rule from CLAUDE.md)

    Do NOT ignore test failures. Do NOT proceed to Task 2 if any test fails.

    Capture the Ginkgo summary line (e.g., "Ran 8 Specs in 355s") as acceptance evidence.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make test-int 2>&1 | tee /tmp/07-test-int.log | tail -30</automated>
  </verify>
  <done>make test-int exits 0; tail -30 /tmp/07-test-int.log shows all specs PASS with no FAIL lines; the Ginkgo summary shows ≥ 8 Layer B specs (7 existing + 1 new bare-project) and 18 Layer A specs, all passing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 7 production code is implemented:
    - Stub planner-mode ChildCRD emission (Plan 07-03)
    - Down-stack fixes: ValidationState stamp + patchPlanSucceeded (Plan 07-04)
    - ProjectReconciler 5th dispatch site: reconcileProjectPlannerDispatch + handleProjectJobCompletion + checkProjectComplete (Plan 07-05)
    - make test-int passes 7/7 Layer B + 18/18 Layer A + new bare-project spec (Task 1)

    Now run the v1.0 acceptance gate.
  </what-built>
  <how-to-verify>
    Run: make acceptance-v1-smoke

    Expected behavior:
    1. A kind cluster is created (or reused if KEEP_KIND_CLUSTER=true)
    2. TIDE is installed via helm
    3. examples/projects/small/project.yaml is applied (gates: all auto, absoluteCapCents: 0, no spec.git)
    4. The script polls until Project.status.phase reaches Complete
    5. The script exits 0

    During the run, watch the cascade by running in a separate terminal:
      kubectl get project,milestone,phase,plan,task,wave --all-namespaces -w

    Expected cascade sequence:
      Project: Initialized → Running (planner Job dispatched)
      Milestone: appears (owner=Project), Running (milestone planner Job dispatched), Succeeded
      Phase: appears (owner=Milestone), Running, Phase CRD materializes
      Plan: appears (owner=Phase), ValidationState=Validated, Wave appears
      Task: appears (owner=Plan), Succeeded
      Project: Complete

    Cost check: no ANTHROPIC_API_KEY should be set (or if set, absoluteCapCents:0 ensures $0 spend):
      grep -c "BudgetExceeded" output OR confirm script exits 0 without API error

    Confirm hack/scripts/acceptance-v1.sh was NOT modified:
      git diff hack/scripts/acceptance-v1.sh → should show no changes

    Confirm examples/projects/small/project.yaml was NOT modified:
      git diff examples/projects/small/project.yaml → should show no changes
  </how-to-verify>
  <resume-signal>
    Type "approved" if make acceptance-v1-smoke exits 0 and Project reached Complete.
    Type "blocked: [description]" if it failed, including the last 50 lines of the script output.
    Per CLAUDE.md "don't predict chain terminator": if a third cascade surfaces, describe it here and do NOT silently expand — the executor will surface it to the planner.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| make acceptance-v1-smoke → kind cluster | Script creates a real K8s cluster locally and installs TIDE. No external network calls except to local Docker. |
| Project → stub subagent pod | absoluteCapCents:0 short-circuits at IsCapExceeded (cap.go: 0 is not > 0), so the planner Job dispatches and the stub runs. No real LLM call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Information Disclosure | acceptance-v1-smoke run | accept | No API key is consumed (absoluteCapCents:0 ensures $0). The script does not capture or log credentials. ANTHROPIC_API_KEY need not be set for the $0 path. |
| T-07-06-02 | Denial of Service | kind cluster resource consumption | accept | Phase 6's acceptance-v1.sh already proved this runs on a developer laptop. The stub subagent Jobs use minimal resources and exit quickly (~30s each). No new resource surface. |
</threat_model>

<verification>
Final gate (both required):
1. make test-int exits 0 — 7/7 Layer B (existing) + 1 new bare-project + 18/18 Layer A all PASS
2. make acceptance-v1-smoke exits 0 — Project status.phase=Complete confirmed by human
3. git diff hack/scripts/acceptance-v1.sh returns empty (script unchanged)
4. git diff examples/projects/small/project.yaml returns empty (fixture unchanged)
</verification>

<success_criteria>
make acceptance-v1-smoke exits 0 and the Project reaches status.phase=Complete at $0. The cascade log shows Milestone→Phase→Plan→Task materializing. The existing 7/7 Layer B + 18/18 Layer A baseline remains green. cascade-7 is CLOSED. v1.0 is ship-ready.
</success_criteria>

<output>
After completion, create .planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-06-SUMMARY.md

Also update .planning/STATE.md "Current Position" to reflect Phase 7 complete and cascade-7 closed.
</output>
