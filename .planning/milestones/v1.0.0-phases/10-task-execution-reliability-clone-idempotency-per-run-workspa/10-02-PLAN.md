---
phase: 10-task-execution-reliability-clone-idempotency-per-run-workspa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/push_helpers.go
  - internal/controller/push_helpers_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "buildCloneJob pod spec carries PodSecurityContext.FSGroup == 1000"
    - "buildPushJob pod spec carries PodSecurityContext.FSGroup == 1000"
    - "Kubelet chowns the workspace PVC subPath at pod startup so nonroot containers can mkdir/write under /workspace"
    - "No change to charts/tide/values.yaml (fixed contract)"
  artifacts:
    - path: "internal/controller/push_helpers.go"
      provides: "FSGroup=1000 on PodSecurityContext in both buildCloneJob and buildPushJob"
      contains: "FSGroup"
    - path: "internal/controller/push_helpers_test.go"
      provides: "TestBuildCloneJobFSGroup + TestBuildPushJobFSGroup"
      exports: ["TestBuildCloneJobFSGroup", "TestBuildPushJobFSGroup"]
  key_links:
    - from: "internal/controller/push_helpers.go"
      to: "internal/dispatch/podjob/jobspec.go:403-406"
      via: "Mirror the FSGroup pattern from jobspec.go"
      pattern: "FSGroup.*1000"
---

<objective>
Fix per-run workspace permissions (SC-2): `buildCloneJob` and `buildPushJob` in `internal/controller/push_helpers.go` must set `PodSecurityContext{FSGroup: new(int64(1000))}` on their pod specs, mirroring the exact pattern already working in `internal/dispatch/podjob/BuildJobSpec` at `jobspec.go:403-406`.

Purpose: The first legitimate task-execution run surfaced `mkdir /workspace/envelopes/push: permission denied` because push/clone Job pods run as nonroot but the PVC root is owned by root (default for newly-provisioned PV). Setting `fsGroup: 1000` causes kubelet to chown the mounted volume at pod startup with no extra container. This is a binary-only change — the chart `values.yaml` is a FIXED contract and must not be touched.

Output: Modified `internal/controller/push_helpers.go` with FSGroup=1000 on both builders; new tests `TestBuildCloneJobFSGroup` and `TestBuildPushJobFSGroup` in `internal/controller/push_helpers_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-CONTEXT.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. -->

From internal/controller/push_helpers.go (current state — confirmed by RESEARCH):
```
// buildPushJob constructs the K8s Job for a level-boundary push.
func buildPushJob(project *tideprojectv1alpha1.Project, pvcName string, opts PushOptions, scheme *runtime.Scheme) *batchv1.Job
// Pod spec at line ~196: corev1.PodSpec{ RestartPolicy: ..., ServiceAccountName: pushSAName, Volumes: volumes, Containers: [...] }
// No SecurityContext field at all. Uses `new(int32(2))` for BackoffLimit (pointer style).

// buildCloneJob constructs the K8s Job for the initial bare clone.
func buildCloneJob(project *tideprojectv1alpha1.Project, pvcName string, opts CloneOptions, scheme *runtime.Scheme) *batchv1.Job
// Pod spec at line ~275: same PodSpec shape, no SecurityContext. Same `new(int32(2))` style.
```

From internal/dispatch/podjob/jobspec.go:403-406 (working analog — must mirror exactly):
```go
SecurityContext: &corev1.PodSecurityContext{
    FSGroup: new(int64(1000)),
},
```

Style rule (RESEARCH Pitfall 2): push_helpers.go uses `new(int32(2))` for BackoffLimit.
Use `new(int64(1000))` for FSGroup to match the file's existing `new()` pointer style.
Do NOT use ptr.To (that's jobspec.go's style; push_helpers.go uses new()).

Do NOT modify charts/tide/values.yaml — it is a FIXED contract per CLAUDE.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave-0 FSGroup tests then wire SecurityContext in push_helpers.go</name>
  <files>internal/controller/push_helpers_test.go, internal/controller/push_helpers.go</files>
  <read_first>
    - internal/controller/push_helpers.go — full file; note exact PodSpec construction in buildPushJob (~line 196) and buildCloneJob (~line 275); note `new(int32(2))` BackoffLimit pointer style
    - internal/dispatch/podjob/jobspec.go — lines 395-411 for the FSGroup analog pattern
    - internal/controller/push_helpers_test.go — existing test shape (if file exists); imports for corev1, batchv1, tideprojectv1alpha1 fixtures
  </read_first>
  <behavior>
    - TestBuildCloneJobFSGroup: call buildCloneJob with a minimal Project fixture; assert job.Spec.Template.Spec.SecurityContext != nil; assert *job.Spec.Template.Spec.SecurityContext.FSGroup == 1000.
    - TestBuildPushJobFSGroup: call buildPushJob with a minimal Project fixture; assert job.Spec.Template.Spec.SecurityContext != nil; assert *job.Spec.Template.Spec.SecurityContext.FSGroup == 1000.
    - Both tests must FAIL before the fix, PASS after.
  </behavior>
  <action>
    First: write failing tests in internal/controller/push_helpers_test.go for TestBuildCloneJobFSGroup and TestBuildPushJobFSGroup. Use a minimal Project fixture sufficient to call the builders (name, namespace, UID, Spec.Git.RepoURL, Spec.Git.CredsSecretRef are the required fields).

    Then: in internal/controller/push_helpers.go, add `SecurityContext: &corev1.PodSecurityContext{FSGroup: new(int64(1000))}` to the `corev1.PodSpec{}` literal in BOTH buildPushJob (the push pod spec, after ServiceAccountName) AND buildCloneJob (the clone pod spec, after RestartPolicy). Use `new(int64(1000))` to match the file's existing `new()` style — NOT ptr.To.

    Do NOT touch charts/tide/values.yaml. Do NOT add imports unless needed (corev1 is already imported). Do NOT restructure the builders — two field additions only.
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'TestBuildCloneJobFSGroup|TestBuildPushJobFSGroup' -v -count=1</automated>
  </verify>
  <done>Both FSGroup tests pass. `grep -c "FSGroup" internal/controller/push_helpers.go` returns 2 (one per builder). go test ./internal/controller/... exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push/clone Job pod → PVC subPath | Nonroot container writes to /workspace; FSGroup causes kubelet to chown the mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-A | Information Disclosure | FSGroup chown exposing PVC data to other pods | mitigate | Per-Project PVC subPath isolation enforced at mount time (`{project.UID}/workspace` SubPath in VolumeMount). FSGroup chown applies at the subPath level inside the pod; it does not grant cross-subPath access. The mitigation is already in place (SubPath on every VolumeMount in push_helpers.go line ~159 and ~302). No new exposure introduced. |
| T-10-02-B | Tampering | push_helpers.go modifying chart values.yaml | accept | Fix is binary-only: PodSecurityContext is emitted by the Go binary at Job creation time. charts/tide/values.yaml is not modified (FIXED contract per CLAUDE.md). |
</threat_model>

<verification>
After task completes:
- `go test ./internal/controller/... -count=1` exits 0
- `grep -c "FSGroup" internal/controller/push_helpers.go` returns 2
- `grep "values.yaml" internal/controller/push_helpers.go` returns empty (no reference added)
- `go vet ./internal/controller/...` clean
</verification>

<success_criteria>
SC-2 (per-run workspace permissions) is automated-proof-green: TestBuildCloneJobFSGroup and TestBuildPushJobFSGroup pass; both buildCloneJob and buildPushJob now emit `FSGroup: 1000` on the PodSecurityContext. No chart change.
</success_criteria>

<output>
After completion, create `.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-02-SUMMARY.md`
</output>
