---
phase: "04.1"
plan: "01"
type: readiness-report
verified: 2026-05-20T10:31:18Z
tooling_readiness:
  docker: pass
  kind: pass
  kubectl: pass
  helm: pass
  gsd_sdk: pass
  chrome_devtools_mcp: fallback
  local_images: info-only
  anthropic_api_key: fallback
overall_gate: APPROVED
blocking_items: []
fallback_items:
  - "chrome_devtools_mcp: Wave 7 item 3 (dashboard live verification) will downgrade to manual-screenshot UAT per RESEARCH.md §UAT Closeout"
  - "anthropic_api_key: Wave 6 item 2 (live Claude E2E) will defer to a documented cost-acknowledgement checkpoint; if operator declines, item flips to 'deferred-to-Phase-5' status"
---

# Phase 04.1 Plan 01 — Tooling Readiness Report

**Verified:** 2026-05-20T10:31:18Z
**Executor host:** darwin (macOS)
**Overall gate:** APPROVED (0 BLOCKED items, 2 FALLBACK items with documented alternatives)

---

## Per-Tool Probe Results

### 1. Docker Daemon

| Field | Value |
|-------|-------|
| Status | **PASS** |
| Version | `29.4.3` |
| Probe | `docker info --format '{{.ServerVersion}}'` |
| Exit code | 0 |
| Stdout snippet | `29.4.3` |

Docker daemon is running and `docker ps` succeeds. No action required.

---

### 2. kind Binary

| Field | Value |
|-------|-------|
| Status | **PASS** |
| Version | `v0.31.0` |
| Probe | `kind version` |
| Exit code | 0 |
| Stdout snippet | `kind v0.31.0 go1.25.5 darwin/amd64` |

Version matches `kind v0.31.x` prefix — this is the exact empirically-closed Phase 02.2 baseline. No action required.

---

### 3. kubectl

| Field | Value |
|-------|-------|
| Status | **PASS** |
| Version | `v1.34.1` |
| Probe | `kubectl version --client -o yaml \| grep gitVersion` |
| Exit code | 0 |
| Stdout snippet | `gitVersion: v1.34.1` |

kubectl is on PATH and points at a readable context. No action required.

---

### 4. Helm v3

| Field | Value |
|-------|-------|
| Status | **PASS** |
| Version | `v3.16.3` |
| Probe | `helm version --short` |
| Exit code | 0 |
| Stdout snippet | `v3.16.3+gcfd0749` |

Helm 3 is present on PATH. No action required.

---

### 5. gsd-sdk CLI + audit-uat JSON Shape

| Field | Value |
|-------|-------|
| Status | **PASS** |
| Probe | `gsd-sdk query audit-uat` + Python schema assertion |
| Exit code | 0 |
| Schema check | `audit-uat schema OK: total_items=15` |

Full schema validation passed:
- `summary.total_items` = 15
- `summary.by_phase` = `{ "02": 6, "03": 5, "04": 4 }`
- `results[]` array present

The JSON shape matches what Plans 12–15 parse. No action required.

---

### 6. Chrome DevTools MCP

| Field | Value |
|-------|-------|
| Status | **FALLBACK** |
| Probe | Executor MCP listing + CLI probe (`which chrome`, `which chromium`) |
| Exit code | N/A — not detectable |
| Downstream impact | Wave 7 item 3 (dashboard live verification) |

Chrome and Chromium binaries not found on PATH. Chrome DevTools MCP is not detectable in the executor environment. Per plan design (`additional_context`), this is a documented FALLBACK — not BLOCKED.

**Documented fallback:** Wave 7 plan (04.1-14) will downgrade the dashboard live-verification item (item 3) to a manual-screenshot UAT per RESEARCH.md §UAT Closeout — Wave 7. The operator navigates to the dashboard URL and provides screenshots; the plan executor documents pass/fail based on operator attestation.

---

### 7. Image Availability (Advisory Only — INFO)

| Field | Value |
|-------|-------|
| Status | **INFO-ONLY** |
| Probe | `docker image ls --format '{{.Repository}}:{{.Tag}}' \| grep -E '^(controller|stub-subagent|credproxy|tide-push|claude-subagent):'` |
| Found locally | `controller:test` |
| Missing locally | `stub-subagent`, `credproxy`, `tide-push`, `claude-subagent` |

`controller:test` is present. The remaining images (`stub-subagent`, `credproxy`, `tide-push`, `claude-subagent`) are not present locally. Wave 5 (Plan 04.1-12) will invoke `make test-int-kind-prep` to build and load all required images before running UAT.

This is advisory only — Wave 5 always rebuilds images to ensure freshness. No action required.

---

### 8. ANTHROPIC_API_KEY Presence

| Field | Value |
|-------|-------|
| Status | **FALLBACK** |
| Probe | `[ -n "${ANTHROPIC_API_KEY:-}" ] && echo "SET" \|\| echo "UNSET"` |
| Result | `UNSET` |
| Downstream impact | Wave 6 item 2 (live Claude E2E dispatch) |

`ANTHROPIC_API_KEY` is not set in the executor environment.

**Documented fallback:** Wave 6 plan (04.1-13) will present a cost-acknowledgement checkpoint before the live Claude E2E item. If the operator sets the key and approves, item 2 runs live. If the operator declines, the item flips to `deferred-to-Phase-5` status in the UAT closeout report. This does not block the chain — all other Wave 6 items (non-Claude items) proceed regardless.

---

## Downstream Plan Impact

| Downstream Plan | Wave | Affected By | Action |
|----------------|------|-------------|--------|
| 04.1-12 | 5 | docker, kind, kubectl, helm | **Fully ready** — all four PASS. Wave 5 runs `make test-int-kind-prep` to build/load missing images; `controller:test` already present. |
| 04.1-13 | 6 | anthropic_api_key | **FALLBACK** — `ANTHROPIC_API_KEY` unset. Wave 6 will present cost-acknowledge checkpoint for item 2; other items unaffected. |
| 04.1-14 | 7 | docker, kind, kubectl, helm, chrome_devtools_mcp | **Mostly ready** — docker/kind/kubectl/helm all PASS. Chrome DevTools MCP unavailable; dashboard live-verification (item 3) downgrades to manual-screenshot fallback. |

Source-fix plans (Wave 1–4, Plans 02–09) are NOT listed — they need only `go build` / `go test`, assumed available on the executor host.

---

## Notes

- **kind version:** `v0.31.0` exactly matches the Phase 02.2 empirically-closed baseline (`kindTestTimeout=7m`, `helm --timeout 5m` contract from STATE.md cascade-8 note). No cascade risk from kind version drift.
- **gsd-sdk total_items=15:** Matches the 15 UAT items enumerated in `docs/audit-report-2026-05-20-merged.md` (4 P1 + 4 P2 + 2 P3 + 4 P4 + 15 UAT). Plans 12–15 can parse this without schema adaptation.
- **ANTHROPIC_API_KEY:** Must be set before Wave 6 if live Claude E2E is desired. Operator should `export ANTHROPIC_API_KEY=<key>` in the terminal before approving the Wave 6 checkpoint.
