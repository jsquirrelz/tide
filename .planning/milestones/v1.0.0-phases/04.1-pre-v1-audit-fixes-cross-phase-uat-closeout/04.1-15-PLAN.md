---
phase: "04.1"
plan: "15"
type: execute
wave: 10
depends_on: ["04.1-12", "04.1-13", "04.1-14"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: []
tags: [closeout, roadmap-update, state-update, audit-uat-zero, phase-04.1-final]
must_haves:
  truths:
    - "All upstream plans 04.1-01 through 04.1-14 have landed on `main` with their respective VERIFICATION artifacts (where applicable) at gate_decision=APPROVED OR the human has approved acceptable defers/caveats"
    - "`gsd-sdk query audit-uat | python3 -c '...' ` reports `summary.total_items: 0` (or = 1-2 if explicit deferrals were captured with caveats in their VERIFICATION.md result fields; document any non-zero count in the SUMMARY)"
    - "ROADMAP.md Phase 04.1 entry (lines 192-225) is updated: the `Plans: TBD` line is replaced with the actual 15-plan inventory; the `Plans:` count line shows 15 plans"
    - "ROADMAP.md Progress table at line 246+ gains a Phase 04.1 row: `Plans Complete: 15/15`, `Status: Complete`, `Completed: YYYY-MM-DD`"
    - "STATE.md body fields updated: Current Position reflects post-Phase-04.1 state (next: Phase 5); progress.completed_plans includes all 15 Phase 04.1 plans; Status: Phase 5 ready"
    - "The Wave-N filter pre-condition is HONORED: this closeout plan only runs after all upstream VERIFICATION gates landed APPROVED (per PIT-6 — `--auto` mode against a BLOCKED upstream gate would false-approve closeout)"
    - "PIT-7 honored — this closeout plan is NOT framed as 'chain terminator candidate vN'; it's framed as the natural close of Phase 04.1 given upstream approvals; if any upstream is BLOCKED, this plan SURFACES the blocker via the checkpoint task rather than proceeding"
    - "Commit on `main` (via worktree merge) records the phase completion with a `chore(04.1): close phase` message"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 04.1 entry updated with full plan inventory + Progress table row marked Complete"
      contains: "04.1 Pre-v1 audit fixes"
    - path: ".planning/STATE.md"
      provides: "Body fields updated to reflect Phase 04.1 closure; next-phase pointer at Phase 5"
      contains: "Phase 5"
  key_links:
    - from: "Upstream VERIFICATION.md gate decisions"
      to: "This closeout plan's progression"
      via: "Pre-check that every upstream VERIFICATION has gate_decision=APPROVED (PIT-6)"
      pattern: "gate_decision:\\s+APPROVED"
    - from: "gsd-sdk query audit-uat summary.total_items"
      to: "ROADMAP.md Progress row 'Complete'"
      via: "0 (or explicitly-caveated count) is the exit criterion"
      pattern: "total_items.*0"
---

<objective>
Close Phase 04.1 by updating ROADMAP.md + STATE.md with the final plan inventory and Progress row, after confirming every upstream plan's gate decision is APPROVED.

Per PIT-6 (PATTERNS.md / CLAUDE.md): this closeout MUST verify upstream gate decisions explicitly. `--auto` mode against a BLOCKED upstream would false-approve. The Task 1 below grep-checks every VERIFICATION.md file in the phase for `gate_decision: APPROVED` and ABORTS if any is BLOCKED.

Per PIT-7: this plan is NOT "chain terminator candidate v1." If an upstream is BLOCKED, this plan SURFACES the blocker and the human decides next steps. No predict-cleanup framing.

Per locked decision (this plan's additional_context): the closeout plan does NOT run any verification command beyond the upstream gate check + the final audit-uat zero check. It is a documentation + state update plan.

**Depends on Plans 04.1-12, 04.1-13, 04.1-14** explicitly (Wave 6/7/9 UAT runners). Implicitly depends on the entire P1/P2/P3/P4 chain via transitivity through the UAT plans.

Purpose: phase wrap. ROADMAP + STATE reflect closure; Phase 5 can dispatch cleanly.
Output: ROADMAP.md and STATE.md updated + committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-RESEARCH.md
@.planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The ROADMAP.md + STATE.md surfaces to update + the upstream gate evidence. -->

ROADMAP.md Phase 04.1 entry lives at lines ~192-225 (the inserted phase). The `Plans: TBD` placeholder at line ~225 is the field to replace.

Target Plans list format (mirror Phase 4's format at lines 170-186):
```markdown
Plans:
- [x] 04.1-01-PLAN.md — Wave 0 tooling readiness gate (Wave 0)
- [x] 04.1-02-PLAN.md — P1.1 wire ProjectReconciler.Dispatcher + manager-construction test (Wave 1)
- [x] 04.1-03-PLAN.md — P1.3 shared DefaultCaps helper (Wave 1)
- [x] 04.1-04-PLAN.md — P1.4 remove first-Project fallback + ParentUnresolved condition (Wave 2)
- [x] 04.1-05-PLAN.md — P1.2 planner Job contract refactor (Wave 3)
- [x] 04.1-06-PLAN.md — P2.1-P2.4 CI/tooling hygiene bundle (Wave 3)
- [x] 04.1-07-PLAN.md — P3.1 reconcileDispatch decomposition (Wave 4)
- [x] 04.1-08-PLAN.md — P3.2 TaskReconcilerDeps carrier (Wave 5)
- [x] 04.1-09-PLAN.md — P4.1 budget rolling-window reset (Wave 6)
- [x] 04.1-10-PLAN.md — P4.2 credproxy upstream allowlist (Wave 7)
- [x] 04.1-11-PLAN.md — P4.3 PROD_OVERRIDE markers + P4.4 logging convention sweep (Wave 8)
- [x] 04.1-12-PLAN.md — Phase 02 UAT runner (Wave 6)
- [x] 04.1-13-PLAN.md — Phase 03 UAT runner — stale + live + doc (Wave 7)
- [x] 04.1-14-PLAN.md — Phase 04 UAT runner — gate flow + dashboard + CLI (Wave 9)
- [x] 04.1-15-PLAN.md — Phase 04.1 closeout — ROADMAP + STATE update (Wave 10)
```

Plans count: 15.

Progress table (lines ~246+) target row:
```markdown
| 04.1. Pre-v1 audit fixes + cross-phase UAT closeout (INSERTED) | 15/15 | Complete | YYYY-MM-DD |
```

STATE.md body fields to update:
- `Current Position`: reflect "Phase 04.1 complete; next is Phase 5"
- `progress.completed_plans`: include the 15 Phase 04.1 plans (frontmatter-derived; the SDK may already track these)
- Any "Last activity" / status line: reflect closure

Per CLAUDE.md Operating Note PIT-2: `gsd-sdk query state.begin-phase` resets STATE.md body fields. **This plan does NOT call `state.begin-phase`** — that's a phase-start operation, not a phase-close operation. STATE.md body edits here are direct file edits via the Edit tool.
</interfaces>
</context>

<additional_context>
**PIT-6 Wave-N filter pre-check:**

Before any closeout edit, grep every upstream VERIFICATION.md for `gate_decision: APPROVED` (or for UAT plans, the VERIFICATION.md `status: pass`). If any returns BLOCKED, this plan ABORTS via the checkpoint task and surfaces the blocker.

Per PATTERNS.md PIT-6:
> "--auto mode auto-approves against a BLOCKED gate; closeout fires falsely. Filter dispatch with --wave <final-wave> until upstream plans record APPROVED."

This plan is Wave 10 (the final wave). The dispatch mechanism for Plan 15 should use `--wave 10` filter to enforce strict-wave gating.

**Acceptance of caveats:** explicit deferrals (e.g., Phase 03 item 2 deferred-to-Phase-5 with no API key; Phase 04 item 2 verified-by-construction) are ACCEPTABLE and do not block closeout. The acceptance criterion is: every upstream plan's VERIFICATION.md is `status: pass` (or has explicit, documented `result: deferred` for individual items with caveat).

**audit-uat tolerance:** RESEARCH.md §UAT Closeout documents that some items may remain as `deferred` with caveats. The strict ROADMAP SC#13 ("audit-uat reports total_items: 0") is the target; if 1-2 items are deferred with explicit caveats, document the count in the SUMMARY and note the caveats. The human can override "must be zero" via the checkpoint if the deferrals are legitimate.

**No source code changes** — only ROADMAP + STATE + (optionally) a commit message-only change to STATE if its frontmatter requires updating.

**Pre-merge worktree check** (PATTERNS.md PIT-5):
```
UNTRACKED=$(git status --porcelain | grep '^??' | grep "04.1-15-" || true)
```
</additional_context>

<tasks>

<task id="1" wave="10" name="Pre-check upstream gate decisions + audit-uat zero" depends_on="[]">
<action>
**Step 1 — Grep every upstream VERIFICATION.md for gate decision OR status:**

```bash
# UAT-runner VERIFICATION.md files use `status: pass` (no gate_decision field per the Phase 02/03/04 verification shape).
for phase in 02-dispatch-plan-validation-innermost-reconcilers-harness 03-up-stack-reconcilers-git-integration-real-subagent-resumptio 04-gates-observability-dashboard-cli; do
    echo "=== $phase ==="
    grep -E '^status:' ".planning/phases/$phase"/*-VERIFICATION.md 2>/dev/null || echo "MISSING"
done

# Source-fix plans (Waves 1-8) may not have VERIFICATION.md files — they're code commits with go build + test PASS as the gate. Verify those landed on main:
for plan in 02 03 04 05 06 07 08 09 10 11; do
    echo "=== 04.1-$plan ==="
    git log --oneline -1 -- ".planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-$plan-SUMMARY.md" 2>/dev/null || echo "MISSING SUMMARY"
done

# UAT plans (12, 13, 14) — check their VERIFICATION artifacts:
for plan in 12 13 14; do
    echo "=== 04.1-$plan ==="
    grep -E '^status:' ".planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-$plan-VERIFICATION.md" 2>/dev/null || echo "NO 04.1 VERIFICATION (uses target-phase VERIFICATION.md instead)"
done
```

Expected outcomes:
- Phase 02/03/04 VERIFICATION.md show `status: pass`
- Each source-fix plan has a 04.1-NN-SUMMARY.md committed
- UAT plans 12/13/14 have SUMMARY.md committed (each documents its own gate decision)

If any upstream is BLOCKED or MISSING, ABORT and surface to the checkpoint (Task 3).

**Step 2 — Final audit-uat check:**
```bash
gsd-sdk query audit-uat 2>&1 | python3 -c "
import json,sys
d = json.load(sys.stdin)
print('Total items remaining:', d['summary']['total_items'])
print('By phase:', d['summary']['by_phase'])
for r in d.get('results', []):
    for it in r.get('items', []):
        if it.get('result') in ('human_needed', 'pending'):
            print(f\"  UNCLOSED: phase {r['phase']} item {it.get('test')}: {it.get('name', '')[:80]}\")
"
```

Expected: `Total items remaining: 0` (or 1-2 with explicit caveats).

If the count is > 0, document each unclosed item and the rationale (e.g., "Phase 03 item 2: deferred-to-Phase-5 — no ANTHROPIC_API_KEY in this session").
</action>
<verify>
- Every upstream VERIFICATION.md confirmed via grep (or upstream SUMMARY confirmed via git log)
- audit-uat total_items captured (target: 0)
- Any non-zero count has documented caveat path
</verify>
<done>
Upstream gate state verified. audit-uat count captured. Ready for ROADMAP/STATE update or checkpoint abort.
</done>
</task>

<task id="2" wave="10" name="Update ROADMAP.md + STATE.md" depends_on="[1]">
<action>
**This task ONLY runs if Task 1's gate check passed (every upstream APPROVED/pass + audit-uat ≤ 2 with caveats).**

**Step 1 — Update ROADMAP.md Phase 04.1 entry:**

Find the Phase 04.1 block (lines ~192-225). At the `Plans:` placeholder line (currently `Plans: TBD`):

Replace with:
```markdown
**Plans:** 15/15 plans executed

Plans:
- [x] 04.1-01-PLAN.md — Wave 0 tooling readiness gate (Wave 0)
- [x] 04.1-02-PLAN.md — P1.1 wire ProjectReconciler.Dispatcher + manager-construction test (Wave 1)
- [x] 04.1-03-PLAN.md — P1.3 shared DefaultCaps helper for 300s wall-clock floor (Wave 1)
- [x] 04.1-04-PLAN.md — P1.4 remove first-Project fallback + ParentUnresolved condition (Wave 2)
- [x] 04.1-05-PLAN.md — P1.2 planner Job contract refactor via JobKind discriminator (Wave 3)
- [x] 04.1-06-PLAN.md — P2.1-P2.4 CI/tooling hygiene bundle (Wave 3)
- [x] 04.1-07-PLAN.md — P3.1 TaskReconciler.reconcileDispatch decomposition (Wave 4)
- [x] 04.1-08-PLAN.md — P3.2 TaskReconcilerDeps carrier struct (Wave 5)
- [x] 04.1-09-PLAN.md — P4.1 budget rolling-window reset + RollingWindowDuration field (Wave 6)
- [x] 04.1-10-PLAN.md — P4.2 cred-proxy upstream allowlist via Spec.Providers[*].AllowedRoutes (Wave 7)
- [x] 04.1-11-PLAN.md — P4.3 PROD_OVERRIDE markers + P4.4 logging convention sweep + logcheck tighten (Wave 8)
- [x] 04.1-12-PLAN.md — Phase 02 UAT runner — make test-int 6/6 items closed (Wave 6)
- [x] 04.1-13-PLAN.md — Phase 03 UAT runner — items 3+4 stale-flipped, 1+2+5 verified (Wave 7)
- [x] 04.1-14-PLAN.md — Phase 04 UAT runner — gate flow + dashboard + CLI (Wave 9)
- [x] 04.1-15-PLAN.md — Phase 04.1 closeout — ROADMAP + STATE update (Wave 10)
```

**Step 2 — Update ROADMAP.md Progress table:**

Find the Progress section (line ~246+). Add a row for Phase 04.1 between Phase 02.2 and Phase 3 (in numeric order):

```markdown
| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 1. Foundation — CRDs, pkg/dag, Controller Scaffold | 0/TBD | Not started | - |
| 2. Dispatch & Plan Validation | 9/13 | In Progress |  |
| 02.2. Layer B kind test timing fixes (INSERTED) | 13/13 | Complete | 2026-05-14 |
| 04.1. Pre-v1 audit fixes + cross-phase UAT closeout (INSERTED) | 15/15 | Complete | YYYY-MM-DD |
| 3. Up-Stack Reconcilers, Git Integration, Real Subagent, Resumption | 0/TBD | Not started | - |
| 4. Gates, Observability, Dashboard, CLI | 0/TBD | Not started | - |
| 5. Distribution & Self-Hosting Acceptance | 0/TBD | Not started | - |
```

Use the actual current date for `YYYY-MM-DD`.

**Step 3 — Update STATE.md body fields:**

Read the current STATE.md. Find:
- "Current Position" or equivalent — update to "Phase 04.1 complete; next: Phase 5"
- "Last activity" or equivalent — update to reflect this closeout commit
- Any progress fields — sync with the actual completed-plan count

DO NOT call `gsd-sdk query state.begin-phase` (that resets body fields — PIT-2 / CLAUDE.md Operating Note). Edit STATE.md directly via the Edit tool.

If STATE.md has a "Roadmap Evolution" section, append an entry:
```markdown
- 2026-05-XX: Phase 04.1 complete (15 plans across 11 waves). audit-uat total_items = <0 or N>. Phase 5 ready to dispatch.
```

**Step 4 — Commit** to a worktree branch `phase-04.1-15-closeout`:
```
git add .planning/ROADMAP.md .planning/STATE.md
git commit -m "$(cat <<'EOF'
chore(04.1): close phase — 15/15 plans complete; audit-uat total_items=<N>

Phase 04.1 (Pre-v1 audit fixes + cross-phase UAT closeout) complete:
- 4 P1 production-wiring fixes (ProjectReconciler.Dispatcher, planner Job contract,
  DefaultCaps helper, ParentUnresolved condition)
- 4 P2 CI/tooling hygiene (dagimports analyzer, named verify-* steps, go-mod-tidy
  diff check, live-e2e.yml workflow_dispatch-only)
- 2 P3 code-shape (reconcileDispatch decomposition, TaskReconcilerDeps carrier)
- 4 P4 improvements (rolling-window reset, configurable cred-proxy allowlist,
  PROD_OVERRIDE markers, logging convention sweep)
- 15 cross-phase UAT items (6 Phase 02 + 5 Phase 03 + 4 Phase 04) closed or
  deferred-with-caveat to Phase 5

ROADMAP.md Phase 04.1 entry updated with 15-plan inventory + Progress row marked
Complete. STATE.md body fields reflect Phase 5 readiness.

audit-uat total_items: <0 | N with caveats documented in upstream VERIFICATION.md
files>.

With Phase 04.1 clean, Phase 5's TIDE-on-TIDE acceptance test is a fair test of
v1 distribution and not a debugging session for known defects.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

DO NOT use `--no-verify` or skip pre-commit hooks.

**Step 5 — Verify post-commit state:**
```bash
git log --oneline -1 -- .planning/ROADMAP.md .planning/STATE.md
gsd-sdk query audit-uat | python3 -c "import json,sys; print(json.load(sys.stdin)['summary']['total_items'])"
grep -E '04\.1.*Complete' .planning/ROADMAP.md
grep -E 'Phase 5' .planning/STATE.md
```
</action>
<verify>
- `grep -E '^\| 04\.1\.' .planning/ROADMAP.md | grep 'Complete'` returns 1 line
- `grep -c '04\.1-..-PLAN\.md' .planning/ROADMAP.md` returns ≥ 15
- `grep -E 'Phase 5|Phase 04\.1 complete' .planning/STATE.md` returns ≥ 1 line
- `git log --oneline -1 -- .planning/ROADMAP.md .planning/STATE.md` returns the new commit
- `gsd-sdk query audit-uat` final count matches the SUMMARY's documented count
</verify>
<done>
ROADMAP.md Phase 04.1 entry shows Complete; Progress table row added; STATE.md reflects Phase 5 readiness; commit on worktree branch.
</done>
</task>

<task id="3" wave="10" name="Final checkpoint — human approves phase closure" depends_on="[1,2]" type="checkpoint:human-verify" gate="blocking">
<what-built>
**If Task 1 passed AND Task 2 ran:**
- ROADMAP.md Phase 04.1 entry shows 15/15 plans + Complete status
- STATE.md updated to reflect Phase 5 readiness
- Closeout commit on worktree branch ready for merge to `main`
- audit-uat total_items = <N> documented (target: 0; acceptable: 1-2 with explicit caveats)

**If Task 1 surfaced a blocker (Task 2 SKIPPED):**
- One or more upstream VERIFICATION.md is `status: human_needed` or has unresolved items
- Closeout PAUSED at this checkpoint
- Human decides: file the missing fix plan(s) OR accept the gap and override with explicit caveat
</what-built>
<how-to-verify>
1. **Read the final ROADMAP.md Phase 04.1 + Progress row:**
   ```
   grep -A 60 '### Phase 04\.1' .planning/ROADMAP.md | head -70
   grep -A 12 '## Progress' .planning/ROADMAP.md
   ```

2. **Verify final audit-uat:**
   ```
   gsd-sdk query audit-uat
   ```
   - Expected: `summary.total_items: 0`
   - Acceptable: 1-2 with caveats (e.g., Phase 03 item 2 deferred for no API key; Phase 04 item 2 verified-by-construction)

3. **Confirm Phase 5 readiness:**
   ```
   grep -E '5\.' .planning/ROADMAP.md | head -5
   ```
   The Phase 5 row should still show `0/TBD` and `Not started` — Phase 04.1 closure doesn't auto-dispatch Phase 5.

4. **Read the commit:**
   ```
   git log --oneline -5
   git show HEAD --stat
   ```

5. **Approve:**
   - "approved" — Phase 04.1 closed; Phase 5 can be planned next
   - "iterate" — file a fix plan for an unresolved item before closing
   - "blocked" — revise ROADMAP/STATE updates
</how-to-verify>
<resume-signal>
Type "approved" to confirm Phase 04.1 closure and merge the worktree branch to main.
Type "iterate" to file a fix plan for an upstream unresolved item.
Type "blocked" to revise the ROADMAP/STATE updates before merge.
</resume-signal>
</task>

</tasks>

<verification>
**Per-criterion mapping back to ROADMAP SC#13:**

| ROADMAP SC#13 clause | This plan's evidence |
|---|---|
| "`gsd-sdk query audit-uat` reports `summary.total_items: 0`" | Task 1 captures count; SUMMARY documents the value + any caveats |
| "before Phase 5 starts" | This closeout plan completes BEFORE Phase 5 plans are dispatched |

**PIT-6 enforced:** Task 1 explicitly grep-checks every upstream VERIFICATION.md before any ROADMAP/STATE edit. Aborts via Task 3 checkpoint if any is BLOCKED.

**PIT-7 honored:** Task 3 checkpoint surfaces blockers to the human; this plan is not framed as automatic-cleanup.

**PIT-2 honored:** STATE.md body edits via Edit tool, NOT via `state.begin-phase` (which would clobber the edits).
</verification>

<success_criteria>
Plan complete when:
- [ ] Task 1 confirms every upstream APPROVED/pass + audit-uat count captured
- [ ] Task 2 updates ROADMAP.md (Plans list + Progress row) + STATE.md
- [ ] Single commit on worktree branch
- [ ] Task 3 checkpoint returns "approved"
- [ ] Worktree branch ready for merge to `main`
</success_criteria>

<output>
After this plan completes, create `.planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-15-SUMMARY.md` documenting:
- Upstream gate check results (per-phase + per-source-fix-plan)
- Final audit-uat count + any caveat-deferred items
- ROADMAP.md changes (Plans list + Progress row diff)
- STATE.md changes
- Phase 04.1 final totals (15 plans, 8 waves, P1+P2+P3+P4 + 15 UAT items processed)
- Phase 5 readiness statement
- Commit SHA
</output>
