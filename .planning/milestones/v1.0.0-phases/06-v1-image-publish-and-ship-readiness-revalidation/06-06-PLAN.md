---
phase: 06
plan: 06
type: execute
wave: 5
depends_on:
  - "06-01"
  - "06-02"
  - "06-04"
  - "06-05"
autonomous: false
requirements:
  - ACC-01

must_haves:
  truths:
    - "make acceptance-v1-smoke (ACCEPTANCE_SAMPLE=small) exits 0 end-to-end"
    - "deploy/tide-controller-manager Available (kubectl wait --for=condition=Available)"
    - "deploy/tide-dashboard Running (kubectl wait --for=condition=Available)"
    - "project/small-project in tide-sample-small reaches status.phase=Complete"
    - "zero ERROR-level controller logs during the run"
    - "no orphan active Jobs after Project reaches terminal status"
    - "no ImagePullBackOff events in any namespace during the run"
    - "run evidence (transcript.log and verification.txt) captured under .acceptance-runs/"
  artifacts:
    - path: ".acceptance-runs/<timestamp>/transcript.log"
      provides: "Captured $0 BOOT-04 run output showing all D-06 criteria met"
    - path: ".acceptance-runs/<timestamp>/verification.txt"
      provides: "D-06 grep pass-criteria report"
  key_links:
    - from: "make acceptance-v1-smoke"
      to: "hack/scripts/acceptance-v1.sh ACCEPTANCE_SAMPLE=small"
      via: "Makefile acceptance-v1-smoke target"
      pattern: "ACCEPTANCE_SAMPLE=small"
    - from: "hack/scripts/load-images-if-needed.sh"
      to: "kind cluster (tide-acceptance-<ts>)"
      via: "kind load docker-image --name ${CLUSTER_NAME}"
      pattern: "kind load"
---

<objective>
Run the $0 BOOT-04 end-to-end acceptance gate, capture evidence, and verify D-06 pass criteria.

Purpose: ACC-01 is the phase closeout gate. It proves the entire Phase 6 change set works end-to-end — chart tag alignment (Plan 01), Dockerfile cross-compile fix (Plan 02), image-load helper + $0 script mode (Plan 04) — all functioning together in a live kind cluster. A green run with captured evidence closes Phase 6.

Output: Green `make acceptance-v1-smoke` run; captured transcript + verification under `.acceptance-runs/`; D-06 criteria report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-SPEC.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-CONTEXT.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-01-SUMMARY.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-02-SUMMARY.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-04-SUMMARY.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-05-SUMMARY.md

<interfaces>
<!-- D-06 pass criteria (from CONTEXT.md — the $0 mode subset of D-A3): -->
<!-- 1. kubectl wait --for=condition=Available deploy/tide-controller-manager -n tide-system --timeout=5m -->
<!-- 2. kubectl wait --for=condition=Available deploy/tide-dashboard -n tide-system --timeout=5m -->
<!-- 3. kubectl wait project/small-project -n tide-sample-small --for=jsonpath='{.status.phase}'=Complete --timeout=10m -->
<!-- 4. kubectl logs -n tide-system deploy/tide-controller-manager | grep -c '"level":"error"' == 0 -->
<!-- 5. kubectl get jobs --all-namespaces -l tideproject.k8s/project-uid=<uid> --field-selector=status.active=1 | tail -n +2 | wc -l == 0 -->
<!-- 6. kubectl get pods --all-namespaces | grep ImagePullBackOff | wc -l == 0 -->
<!-- NOT asserted: per-run-branch git log, commit shapes, gitleaks counter, budget spent (large-sample only) -->

<!-- make acceptance-v1-smoke = ACCEPTANCE_SAMPLE=small hack/scripts/acceptance-v1.sh -->
<!-- Expected image build: first time, all 6 images not in registry → docker build + kind load each -->
<!-- Expected wall-clock: ~10-15 min (cluster up + cert-manager + 6 image builds + helm install + Project wait) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification — confirm Plans 01-05 prerequisites are in place</name>
  <files>charts/tide/values.yaml, hack/helm/tide-values.yaml, hack/scripts/load-images-if-needed.sh, examples/projects/small/project.yaml, Makefile, Dockerfile, Dockerfile.dashboard, images/stub-subagent/Dockerfile, images/credproxy/Dockerfile, images/tide-push/Dockerfile, images/claude-subagent/Dockerfile</files>
  <read_first>
    Read .planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-01-SUMMARY.md to confirm CHART-01 landed. Read 06-04-SUMMARY.md to confirm load-images-if-needed.sh and acceptance-v1.sh $0 mode are in place. This is a fast pre-flight before the 15-minute end-to-end run.
  </read_first>
  <action>
    Run prerequisite checks before starting the end-to-end run:

    1. Verify CHART-01 tag alignment: `grep -cE 'v0\.1\.0-dev' charts/tide/values.yaml hack/helm/tide-values.yaml` must return 0.

    2. Verify chart resolves correctly: `helm template charts/tide | grep -E 'image:' | grep -v '1\.0\.0\|1\.36'` must return empty.

    3. Verify load helper exists and is executable: `test -x hack/scripts/load-images-if-needed.sh` must exit 0.

    4. Verify small project.yaml uses 1.0.0 tag (A7 fix): `grep -c 'tide-stub-subagent:1\.0\.0' examples/projects/small/project.yaml` must return 1.

    5. Verify Makefile has acceptance-v1-smoke: `grep -cE 'acceptance-v1-smoke' Makefile` must return >= 2.

    6. Verify Dockerfile cross-compile fix landed: `grep -cE 'FROM --platform=\$BUILDPLATFORM golang' Dockerfile Dockerfile.dashboard images/stub-subagent/Dockerfile images/credproxy/Dockerfile images/tide-push/Dockerfile images/claude-subagent/Dockerfile` must return 6.

    If any check fails, STOP and fix the prerequisite before proceeding to Task 2. Document which check failed in the SUMMARY.
  </action>
  <verify>
    <automated>grep -cE 'v0\.1\.0-dev' charts/tide/values.yaml hack/helm/tide-values.yaml</automated>
    Must return 0. All 6 prerequisite checks above must pass.
  </verify>
  <acceptance_criteria>
    - `grep -cE 'v0\.1\.0-dev' charts/tide/values.yaml hack/helm/tide-values.yaml` returns 0
    - `test -x hack/scripts/load-images-if-needed.sh` exits 0
    - `grep -c 'tide-stub-subagent:1\.0\.0' examples/projects/small/project.yaml` returns 1
    - `grep -cE 'FROM --platform=\$BUILDPLATFORM golang' Dockerfile Dockerfile.dashboard images/stub-subagent/Dockerfile images/credproxy/Dockerfile images/tide-push/Dockerfile images/claude-subagent/Dockerfile` returns 6
  </acceptance_criteria>
  <done>All 6 prerequisite checks pass; safe to proceed to the end-to-end run</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run make acceptance-v1-smoke and capture D-06 evidence</name>
  <read_first>
    Confirm Task 1 prerequisites passed. Estimate ~10-20 minutes wall-clock for the first run (6 docker builds + kind cluster + cert-manager + helm install + Project wait). Plan for this in scheduling.
  </read_first>
  <what-built>
    Plans 01-05 delivered: chart tag alignment (CHART-01), Dockerfile cross-compile (D-02), build-images CI job (D-01/D-04), load-images-if-needed.sh + acceptance-v1.sh $0 mode (D-05/IMG-LOAD-01), dry-run-v1.sh cert-manager (DRY-01), INSTALL.md maintainer doc (DOC-01). All the pieces are in place for the first full $0 BOOT-04 run.
  </what-built>
  <how-to-verify>
    Run the following commands in sequence:

    1. Start the $0 BOOT-04 run (captures output to a log file):
       ```
       mkdir -p .acceptance-runs/manual && make acceptance-v1-smoke 2>&1 | tee .acceptance-runs/manual/transcript.log
       ```
       Expected: the run takes ~10-20 minutes. Watch for:
       - "==> creating fresh kind cluster tide-acceptance-<ts>..." — cluster creation
       - "==> installing cert-manager v1.20.2..." — cert-manager bring-up
       - "==> tide-controller: not found in registry, building locally..." (x6) — image builds
       - "==> helm-installing tide-crds + tide..." — helm install
       - "==> applying examples/projects/small/project.yaml ($0 stub mode)..." — project apply
       - "project/small-project" condition check reaching Complete
       - Exit code 0

    2. After the run exits (code 0), verify the D-06 criteria against the live cluster. Get the CLUSTER_NAME from the transcript (grep for "CLUSTER_NAME = "):
       ```
       CLUSTER_NAME=$(grep 'CLUSTER_NAME' .acceptance-runs/manual/transcript.log | grep -oE 'tide-acceptance-[0-9]+')
       ```

    3. Verify each D-06 criterion:
       ```
       kubectl get pods --all-namespaces | grep ImagePullBackOff | wc -l  # must be 0
       kubectl logs -n tide-system deploy/tide-controller-manager | grep -c '"level":"error"'  # must be 0
       kubectl get jobs --all-namespaces --field-selector=status.active=1 | tail -n +2 | wc -l  # must be 0 (no orphan active jobs)
       kubectl get project small-project -n tide-sample-small -o jsonpath='{.status.phase}'  # must be "Complete"
       ```

    4. Capture verification output:
       ```
       { echo "D-06 Verification — $(date)"; \
         echo "ImagePullBackOff count: $(kubectl get pods --all-namespaces | grep ImagePullBackOff | wc -l) (expect 0)"; \
         echo "ERROR log count: $(kubectl logs -n tide-system deploy/tide-controller-manager | grep -c '"level":"error"' || echo 0) (expect 0)"; \
         echo "Orphan active jobs: $(kubectl get jobs --all-namespaces --field-selector=status.active=1 | tail -n +2 | wc -l) (expect 0)"; \
         echo "small-project phase: $(kubectl get project small-project -n tide-sample-small -o jsonpath='{.status.phase}')  (expect Complete)"; \
       } | tee .acceptance-runs/manual/verification.txt
       ```

    5. Confirm exit code 0 of the overall `make acceptance-v1-smoke` run.

    If any D-06 criterion fails: capture the kubectl describe / logs output for the failing component and report the failure details. Do NOT mark the run as passing.

    If the build step fails for any image: check the docker build output in the transcript — confirm the Dockerfile `--platform=$BUILDPLATFORM` change from Plan 02 is present and the alpine images have `GOARCH=${TARGETARCH}` in their go build RUN.
  </how-to-verify>
  <resume-signal>Type "approved" if all D-06 criteria pass (ImagePullBackOff=0, ERROR logs=0, orphan jobs=0, small-project=Complete, exit 0). Or describe which criterion failed and include the relevant log excerpt.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local kind cluster → installed TIDE | The acceptance cluster is ephemeral, isolated, and destroyed after the run; no production state affected |
| acceptance-v1-smoke → .acceptance-runs/ | Run evidence is written to .acceptance-runs/ which is now git-ignored (Plan 01 HYG-01); no accidental commit of cluster state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Denial of Service | Local docker builds consume resources during acceptance run | accept | $0 mode builds 6 images locally; this is a maintainer-only operation on a developer machine; resource contention is acceptable |
| T-06-06-02 | Information Disclosure | .acceptance-runs/ transcript.log contains cluster state | mitigate | .acceptance-runs/ is git-ignored (Plan 01 HYG-01); operator must not commit this directory |
</threat_model>

<verification>
D-06 full gate:
- `make acceptance-v1-smoke` exits 0
- `kubectl get pods --all-namespaces | grep ImagePullBackOff | wc -l` returns 0
- `kubectl logs -n tide-system deploy/tide-controller-manager | grep -c '"level":"error"'` returns 0
- `kubectl get project small-project -n tide-sample-small -o jsonpath='{.status.phase}'` returns Complete
- transcript.log and verification.txt exist under .acceptance-runs/
</verification>

<success_criteria>
`make acceptance-v1-smoke` exits 0 end-to-end. All 6 D-06 criteria pass: controller Available + dashboard Running + small-project Complete + zero ERROR logs + zero orphan jobs + zero ImagePullBackOff. Evidence captured in `.acceptance-runs/`. Phase 6 is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-06-SUMMARY.md`

This is the final plan in Phase 6. After the SUMMARY is written, update ROADMAP.md Phase 6 plan list and run `gsd-sdk query state.load` to confirm the phase is complete.
</output>
