---
phase: 2
plan: 11
type: execute
wave: 5
depends_on: ["02-03", "02-09"]
files_modified:
  - internal/webhook/v1alpha1/plan_webhook.go
  - internal/webhook/v1alpha1/strict_mode.go
  - internal/webhook/v1alpha1/strict_mode_test.go
  - internal/webhook/v1alpha1/plan_webhook_test.go
  - internal/controller/plan_webhook_test.go
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03]
must_haves:
  truths:
    - "PlanCustomValidator gains a `client.Client` field (mgr.GetClient) so ValidateCreate/Update can list owned Tasks by `.spec.planRef` field indexer at admission time."
    - "On Plan admission: webhook calls `pkg/dag.ComputeWaves(taskNames, edges)`. CycleError → AdmissionResponse.allowed=false + structured message naming `cyclic.InvolvedNodes` (PLAN-01)."
    - "File-touch ↔ dependsOn reconciliation per D-E2: derived edges = pairs of Tasks (a, b) where `a.filesTouched ∩ b.filesTouched` overlaps on EXACT path equality AND no declared `dependsOn` edge between them. Same-directory siblings (foo.go + foo_test.go) do NOT generate derived edges (Pitfall G defense)."
    - "Mode resolution per D-E3 precedence: Plan annotation `tideproject.k8s/file-touch-mode=strict|warn` > Project.Spec.PlanAdmission.FileTouchMode > Helm default. ResolveFileTouchMode helper in strict_mode.go."
    - "Strict mode + mismatches → AdmissionResponse.allowed=false + structured `Status.Details` listing mismatches. Warn mode + mismatches → AdmissionResponse.allowed=true + `admission.Warnings[]` (kubectl surfaces these)."
    - "Plan with zero owned Tasks visible at admission time → returns `admission.Warnings{\"plan has no owned Tasks visible at admission time; cycle detection will run when Tasks reconcile\"}` (Pitfall B mitigation — kubectl-apply order)."
    - "Both strict + warn modes fire a K8s Event on the Plan for audit traceability."
    - "PLAN-03 verified by absence: no `recoverCycle`, `cycleRecover`, `fix.*cycle`, `skip.*cycle` identifiers in webhook code."
  artifacts:
    - path: internal/webhook/v1alpha1/plan_webhook.go
      provides: "Phase 2 ValidateCreate + ValidateUpdate body: cycle detection + file-touch reconciliation + structured rejection"
      contains: "pkg/dag.ComputeWaves"
    - path: internal/webhook/v1alpha1/strict_mode.go
      provides: "ResolveFileTouchMode (precedence: annotation > Project.Spec > Helm default)"
      contains: "ResolveFileTouchMode"
  key_links:
    - from: "internal/webhook/v1alpha1/plan_webhook.go"
      to: "pkg/dag.ComputeWaves"
      via: "tasksToDAG translates Task CRDs to (nodes, edges); ComputeWaves returns CycleError on cycles"
      pattern: "dag\\.ComputeWaves"
    - from: "internal/webhook/v1alpha1/plan_webhook.go"
      to: "field indexer .spec.planRef on Tasks (registered by TaskReconciler in Plan 09)"
      via: "client.List with MatchingFields"
      pattern: "MatchingFields"
---

<objective>
Fill the Plan validating admission webhook body (PLAN-01, PLAN-02, PLAN-03). Phase 1 scaffolded `PlanCustomValidator` with no-op `ValidateCreate`/`Update`/`Delete`. This plan fills the bodies with: (1) cycle detection via `pkg/dag.ComputeWaves` (PLAN-01); (2) file-touch ↔ dependsOn reconciliation (PLAN-02) with layered strict/warn mode flag (D-E3); (3) `admission.Warnings` for the "no Tasks visible yet" case (Pitfall B). PLAN-03 is verified by absence — there's no cycle-recovery code path; the webhook rejects + surfaces.

Critical preservation per PATTERNS.md "self-extend" rules: the signatures `ValidateCreate`, `ValidateUpdate`, `ValidateDelete` MUST NOT change — only bodies. The PlanCustomValidator struct gains a `client.Client` field + `FileTouchMode` (Helm default) + `Recorder` (event sink) — these are the only struct shape changes.

Per RESEARCH.md "Pattern 3: Validating-Admission Webhook Body with Owned-Task Lookup": the webhook uses the cache-backed client (mgr.GetClient) + label selector to query owned Tasks. Cache miss is treated as a warning, not a hard rejection (Pitfall B).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md
@internal/webhook/v1alpha1/plan_webhook.go
@internal/controller/plan_webhook_test.go
@internal/controller/suite_test.go
@pkg/dag/kahn.go
@pkg/dag/errors.go
@api/v1alpha1/task_types.go
@api/v1alpha1/plan_types.go
@api/v1alpha1/project_types.go
@internal/owner/owner.go

<interfaces>
<!-- Per 02-RESEARCH.md "Pattern: Plan Webhook body (cycle + file-touch)" + "Webhook Implementation" verbatim. -->

`internal/webhook/v1alpha1/plan_webhook.go` PlanCustomValidator struct (extend Phase 1):
```
type PlanCustomValidator struct {
    Client                client.Client                   // NEW — mgr.GetClient
    DefaultFileTouchMode  string                          // NEW — Helm value; "warn" default
    Recorder              record.EventRecorder            // NEW — for audit Events
}

// SetupPlanWebhookWithManager (extend Phase 1 — gain config args)
func SetupPlanWebhookWithManager(mgr ctrl.Manager, defaultMode string) error {
    return ctrl.NewWebhookManagedBy(mgr, &tidev1alpha1.Plan{}).
        WithValidator(&PlanCustomValidator{
            Client:               mgr.GetClient(),
            DefaultFileTouchMode: defaultMode,
            Recorder:             mgr.GetEventRecorderFor("plan-webhook"),
        }).
        Complete()
}
```

ValidateCreate / ValidateUpdate body (delegated to shared `validate` method):
```
func (v *PlanCustomValidator) validate(ctx context.Context, plan *tidev1alpha1.Plan) (admission.Warnings, error) {
    warnings := admission.Warnings{}

    // List owned Tasks via field indexer (set up by TaskReconciler.SetupWithManager in Plan 09).
    var taskList tidev1alpha1.TaskList
    if err := v.Client.List(ctx, &taskList,
        client.InNamespace(plan.Namespace),
        client.MatchingFields{".spec.planRef": plan.Name},
    ); err != nil {
        return nil, fmt.Errorf("plan webhook: list tasks: %w", err)
    }

    if len(taskList.Items) == 0 {
        warnings = append(warnings, fmt.Sprintf(
            "plan %s/%s has no owned Tasks visible at admission time; cycle detection will run when Tasks reconcile",
            plan.Namespace, plan.Name))
        return warnings, nil
    }

    // PLAN-01 cycle detection.
    nodes, edges := tasksToDAG(taskList.Items)
    if _, err := dag.ComputeWaves(nodes, edges); err != nil {
        var cyc *dag.CycleError
        if errors.As(err, &cyc) {
            v.Recorder.Eventf(plan, corev1.EventTypeWarning, "CycleDetected",
                "cyclic task DAG involving %v", cyc.InvolvedNodes)
            return warnings, fmt.Errorf("plan %s/%s rejected: cyclic task DAG involving %v",
                plan.Namespace, plan.Name, cyc.InvolvedNodes)
        }
        return warnings, fmt.Errorf("plan %s/%s rejected: %w", plan.Namespace, plan.Name, err)
    }

    // PLAN-02 file-touch reconciliation.
    mode := ResolveFileTouchMode(plan, /* project */ nil, v.DefaultFileTouchMode)
    mismatches := computeFileTouchMismatches(taskList.Items)
    if len(mismatches) > 0 {
        if mode == "strict" {
            v.Recorder.Eventf(plan, corev1.EventTypeWarning, "FileTouchMismatch",
                "file-touch mismatches: %v", summariseMismatches(mismatches))
            return warnings, fmt.Errorf("plan %s/%s rejected (strict mode): file-touch mismatches: %v",
                plan.Namespace, plan.Name, summariseMismatches(mismatches))
        }
        // warn mode
        v.Recorder.Eventf(plan, corev1.EventTypeNormal, "FileTouchMismatch",
            "file-touch mismatches (warn mode): %v", summariseMismatches(mismatches))
        for _, m := range mismatches {
            warnings = append(warnings,
                fmt.Sprintf("file-touch mismatch on tasks %s/%s sharing path %q without declared dependsOn",
                    m.TaskA, m.TaskB, m.SharedPath))
        }
    }

    return warnings, nil
}
```

Helper types + functions in the same file:
```
type fileTouchMismatch struct {
    TaskA      string
    TaskB      string
    SharedPath string  // the exact path shared between TaskA.filesTouched and TaskB.filesTouched
}

// tasksToDAG: nodes = task names; edges = (DependsOn.From, Name).
func tasksToDAG(tasks []tidev1alpha1.Task) ([]dag.NodeID, []dag.Edge)

// computeFileTouchMismatches: returns pairs of Tasks (a, b) where their filesTouched
// sets overlap on EXACT path equality AND no declared dependsOn edge exists in
// either direction. (Pitfall G: same-directory siblings do NOT trigger.)
func computeFileTouchMismatches(tasks []tidev1alpha1.Task) []fileTouchMismatch
```

`internal/webhook/v1alpha1/strict_mode.go` (NEW small helper):
```
// ResolveFileTouchMode returns the active mode per D-E3 precedence:
//   1. Plan annotation tideproject.k8s/file-touch-mode=strict|warn
//   2. project.Spec.PlanAdmission.FileTouchMode (if project non-nil)
//   3. clusterDefault (Helm value)
// Returns "warn" if no value is set anywhere (defense-in-depth — Helm chart default).
//
// For Phase 2 cleanliness per RESEARCH.md Open Question #1: the webhook does NOT
// walk owner refs to find the Project (3 Gets per validate would be slow). Instead,
// PlanReconciler (Plan 09) caches the resolved mode in the Plan annotation
// `tideproject.k8s/file-touch-mode-resolved=<mode>` so the webhook reads it directly.
// If the resolved-cache annotation is absent (first admission before PlanReconciler
// reconciles), the webhook falls back to clusterDefault.
func ResolveFileTouchMode(plan *v1alpha1.Plan, project *v1alpha1.Project, clusterDefault string) string
```

Implementation:
```
func ResolveFileTouchMode(plan *v1alpha1.Plan, project *v1alpha1.Project, clusterDefault string) string {
    if plan != nil {
        if v, ok := plan.Annotations["tideproject.k8s/file-touch-mode"]; ok {
            if v == "strict" || v == "warn" {
                return v
            }
        }
        // Resolved-cache annotation (set by PlanReconciler at non-webhook time)
        if v, ok := plan.Annotations["tideproject.k8s/file-touch-mode-resolved"]; ok {
            if v == "strict" || v == "warn" {
                return v
            }
        }
    }
    if project != nil && project.Spec.PlanAdmission.FileTouchMode != "" {
        return project.Spec.PlanAdmission.FileTouchMode
    }
    return clusterDefault
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill plan_webhook.go body (cycle detection + file-touch reconciliation) + Event emission</name>
  <files>internal/webhook/v1alpha1/plan_webhook.go, internal/controller/suite_test.go</files>
  <read_first>
    - internal/webhook/v1alpha1/plan_webhook.go (Phase 1 Plan 07 no-op scaffold — preserve ValidateCreate/Update/Delete signatures; fill the body)
    - pkg/dag/kahn.go + pkg/dag/errors.go (ComputeWaves + CycleError)
    - api/v1alpha1/task_types.go (Task.Spec.DependsOn + FilesTouched fields)
    - api/v1alpha1/plan_types.go (Plan.Status.ValidationState — webhook doesn't set Status; PlanReconciler does on next reconcile)
    - api/v1alpha1/project_types.go (Project.Spec.PlanAdmission.FileTouchMode field from Plan 03)
    - internal/controller/suite_test.go (extend BeforeSuite to register the stateful PlanCustomValidator)
    - internal/controller/plan_webhook_test.go (Phase 1 Plan 07 test setup — extend with Phase 2 cases)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md (D-E1, D-E2, D-E3, D-E4)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (§"Pattern: Plan Webhook body (cycle + file-touch)" — full body code; §"Pitfall B — Webhook reading Tasks before kubectl apply -k has applied them" — admission warning vs hard reject; §"Pitfall G — File-touch reconciliation false-positives" — EXACT path equality only, not directory overlap)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"`internal/webhook/v1alpha1/plan_webhook.go` (extend — fill body)" — critical preservation rule on signatures)
  </read_first>
  <behavior>
    - `TestPlanWebhook_RejectsCyclicPlan` (envtest) — Apply a Plan + 3 Tasks with α→β→γ→α cycle; `kubectl apply` returns a structured error message containing the cycle node names.
    - `TestPlanWebhook_AdmitsAcyclicPlan` — Apply α…θ from `pkg/dag/kahn_test.go`'s fixture; admission succeeds.
    - `TestPlanWebhook_FileTouchStrictMode_RejectsMismatch` — Tasks α + β both write `pkg/x/y.go` with NO declared dependsOn between them; Plan annotation `tideproject.k8s/file-touch-mode=strict`; admission rejects.
    - `TestPlanWebhook_FileTouchWarnMode_ReturnsWarnings` — same fixture; annotation `=warn`; admission succeeds + returns admission.Warnings.
    - `TestPlanWebhook_FileTouchWarnMode_SameDirSiblingsNotFlagged` — α writes `pkg/x/y.go`, β writes `pkg/x/y_test.go`; warn mode; expect NO warnings (Pitfall G — same-directory siblings do NOT trigger).
    - `TestPlanWebhook_NoTasksVisible_ReturnsWarning` — Apply Plan first (no Tasks yet); admission succeeds + returns the "no owned Tasks visible" warning.
    - `TestPlanWebhook_ModeResolutionFromAnnotation` — Plan annotation = "strict"; Project default = "warn"; cluster default = "warn"; mode is "strict".
    - `TestPlanWebhook_ModeResolutionFromProjectSpec` — Plan annotation absent; Project.Spec.PlanAdmission.FileTouchMode = "strict"; cluster default = "warn"; mode is "strict" (but per Phase 2 cleanliness — the webhook reads the cached annotation on Plan; without PlanReconciler running, Project.Spec is NOT read; mode falls back to cluster default "warn"; document this as the Phase 2 trade-off; Phase 3 may add the resolver-cache wiring).
    - `TestPlanWebhook_ModeResolutionFromHelmDefault` — no annotation, no Project override; mode is clusterDefault.
    - `TestPlanWebhook_FiresK8sEventOnRejection` — fetch the Plan's Events post-admission-rejection; assert an Event with Reason=CycleDetected or FileTouchMismatch exists.
    - PLAN-03 verified by absence: `grep -nE "recoverCycle|cycleRecover|fix.*cycle|skip.*cycle" internal/webhook/v1alpha1/` returns zero matches.
  </behavior>
  <action>
    Edit `internal/webhook/v1alpha1/plan_webhook.go`:

    1. Extend `PlanCustomValidator` struct with the three new fields per <interfaces>.
    2. Change `SetupPlanWebhookWithManager(mgr ctrl.Manager)` signature to accept the `defaultMode string` arg. Construct the validator with `mgr.GetClient()`, `defaultMode`, `mgr.GetEventRecorderFor("plan-webhook")`.
    3. Fill `ValidateCreate` + `ValidateUpdate` (both delegate to the shared `validate(ctx, plan)` method) with the body per <interfaces>.
    4. Implement helpers: `tasksToDAG(tasks)`, `computeFileTouchMismatches(tasks)`, `summariseMismatches(mismatches)`.

    For `computeFileTouchMismatches`, the algorithm (Pitfall G defense):
    ```
    1. Build a name → declared-dependsOn set for fast lookup.
    2. For each pair (a, b) of tasks (a.Name < b.Name to avoid duplicates):
        - Compute exact-equality intersection: shared := []string{} for path in a.FilesTouched: if path in b.FilesTouched, append.
        - If shared is empty → skip.
        - If `b.Name in a.DependsOn[deps-via-edges]` OR `a.Name in b.DependsOn[...]` → declared, skip.
        - Else → for each shared path, append a fileTouchMismatch{TaskA: a.Name, TaskB: b.Name, SharedPath: path}.
    3. Return the list.
    ```
    EXACT-equality on paths only — `pkg/x/y.go` does NOT match `pkg/x/y_test.go` (Pitfall G). The algorithm is O(N² × P) where N is the task count and P is the average filesTouched length per task; for v1 Plan sizes (≤20 Tasks) this is fast.

    Add the strict_mode.go helper file (Task 2 — separate). For this task, the webhook calls `ResolveFileTouchMode(plan, /*project=*/ nil, v.DefaultFileTouchMode)` — project lookup is deferred per RESEARCH.md Open Question #1 + the Phase 2 trade-off documented in <interfaces>.

    Apache header per PATTERNS.md (webhook file — kubebuilder scaffolded — header stays).

    Extend `internal/controller/suite_test.go` BeforeSuite:
    - Replace the Phase 1 `webhookv1alpha1.SetupPlanWebhookWithManager(mgr)` call with `webhookv1alpha1.SetupPlanWebhookWithManager(mgr, "warn")` (cluster default).
    - Field indexer `.spec.planRef` is already registered by Plan 09 Task 1's TaskReconciler.SetupWithManager — verify the call lives in the suite setup (it does — TaskReconciler is wired in the manager).

    Extend `internal/controller/plan_webhook_test.go` (Phase 1 placeholder) with the Phase 2 admission test cases per <behavior>. Reuse existing Phase 1 "no-op admit" tests (they should still pass — empty Plan with no Tasks returns warnings but admits).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./internal/webhook/... &amp;&amp; make test &amp;&amp; ! grep -nE 'recoverCycle|cycleRecover' internal/webhook/v1alpha1/*.go</automated>
  </verify>
  <done>
    - `go build ./internal/webhook/...` exits 0
    - `make test` PASS on all 10 plan_webhook test cases (envtest suite runs both Phase 1 + Phase 2 cases in the same package)
    - PLAN-03 grep returns zero: `grep -nE 'recoverCycle|cycleRecover|fix.*cycle|skip.*cycle' internal/webhook/v1alpha1/` returns nothing
    - `grep -c 'dag.ComputeWaves' internal/webhook/v1alpha1/plan_webhook.go` returns ≥ 1
    - `grep -c 'EXACT' internal/webhook/v1alpha1/plan_webhook.go` returns ≥ 1 (Pitfall G defense documented)
    - K8s Events emitted: tests verify via `kubectl get events --field-selector involvedObject.name=<plan>`
    - Apache header preserved
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResolveFileTouchMode helper (strict_mode.go) + dedicated tests</name>
  <files>internal/webhook/v1alpha1/strict_mode.go, internal/webhook/v1alpha1/strict_mode_test.go</files>
  <read_first>
    - internal/owner/owner.go (small helper-with-doc-comment pattern)
    - api/v1alpha1/project_types.go (Plan 03 — PlanAdmissionConfig.FileTouchMode field)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md (D-E3 precedence: annotation > Project.Spec > Helm default)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (§"Open Question #1 — Plan webhook mode lookup" — annotation cache recommended)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"`internal/webhook/v1alpha1/strict_mode.go` (new — precedence resolver)")
  </read_first>
  <behavior>
    - `ResolveFileTouchMode(planWithAnnotation{mode=strict}, project{Spec.PlanAdmission.FileTouchMode=warn}, "warn")` → "strict" (annotation wins).
    - `ResolveFileTouchMode(planWithoutAnnotation, project{Spec.PlanAdmission.FileTouchMode=strict}, "warn")` → "strict" (Project.Spec wins over Helm default).
    - `ResolveFileTouchMode(planWithoutAnnotation, project{Spec.PlanAdmission.FileTouchMode=""}, "warn")` → "warn" (Helm default).
    - `ResolveFileTouchMode(nil, nil, "warn")` → "warn".
    - `ResolveFileTouchMode(planWithBogusAnnotation{mode=bogus}, project, "warn")` → falls through to next precedence layer (bogus value ignored).
    - `ResolveFileTouchMode(planWithCachedAnnotation{file-touch-mode-resolved=strict}, nil, "warn")` → "strict" (the resolved-cache annotation Plan 09's PlanReconciler may stamp).
  </behavior>
  <action>
    Create `internal/webhook/v1alpha1/strict_mode.go` per <interfaces>. Implementation exactly as the pseudocode block. Imports: `tidev1alpha1 "github.com/jsquirrelz/tide/api/v1alpha1"`.

    Apache header per PATTERNS.md (webhook subpackage — kubebuilder scaffolded — header stays). Note: PATTERNS.md says webhook files keep the header even though they're small helpers; follow the convention.

    Create `internal/webhook/v1alpha1/strict_mode_test.go` with stdlib testing table-driven tests per <behavior>. Pure-Go predicate; no apiserver needed; no Ginkgo.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./internal/webhook/v1alpha1/... -count=1 -run TestResolveFileTouchMode -v</automated>
  </verify>
  <done>
    - `go test ./internal/webhook/v1alpha1/... -count=1 -run TestResolveFileTouchMode` PASS on all 6 subtests
    - `grep -nE 'file-touch-mode|file-touch-mode-resolved' internal/webhook/v1alpha1/strict_mode.go` returns ≥ 2 (both annotation keys handled)
    - Apache header on strict_mode.go (webhook subpackage convention)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kubectl apply → kube-apiserver → Plan validating webhook | Validating admission runs synchronously on Plan POST; webhook is the gate for cycle + file-touch checks before etcd persists. |
| webhook → cache-backed Task list | Webhook reads owned Tasks via the cache-backed client (mgr.GetClient); informer cache lag (Pitfall B) mitigated by the warning-not-rejection fall-through. |
| webhook → K8s Event recorder | Webhook emits Events on Reasons (CycleDetected, FileTouchMismatch); operators see these via `kubectl describe plan`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11-01 | Tampering | Cyclic Plan applied silently corrupts Wave materialization | mitigate | Webhook rejects on `*dag.CycleError` (PLAN-01); PlanReconciler defense-in-depth check (Plan 09) also re-runs ComputeWaves. |
| T-02-11-02 | Tampering | LLM-hallucinated dependsOn that doesn't match real file touches (Pitfall 19) | mitigate | File-touch reconciliation in warn mode surfaces the mismatch as admission warning; strict mode rejects. Mode resolution layered (D-E3). |
| T-02-11-03 | Denial of Service | Plan with thousands of Tasks blows up webhook latency | accept | v1 Plans are bounded to ~20 Tasks per RESEARCH.md "summariseMismatches" assumption; O(N²) is acceptable. Documented for v1.x. |
| T-02-11-04 | Information Disclosure | Webhook leaks Task internals via admission warning message | accept | Warnings contain Task names + shared paths — same data already visible via kubectl get tasks. No additional disclosure. |
| T-02-11-05 | Repudiation | Webhook rejection not audit-logged | mitigate | K8s Event emitted on every rejection (CycleDetected / FileTouchMismatch Reasons). |
| T-02-11-06 | Elevation of Privilege | Cycle "recovery" sneaking in as a future helper | mitigate | PLAN-03 verified by absence; the test grep gate runs in `make test`. |
| T-02-11-07 | Tampering | Pitfall G — same-directory siblings flagged as false-positive | mitigate | `computeFileTouchMismatches` uses EXACT path equality only (no prefix/directory match); `TestPlanWebhook_FileTouchWarnMode_SameDirSiblingsNotFlagged` is the regression gate. |
</threat_model>

<verification>
- `go build ./...` exits 0
- `make test` PASS on all webhook + strict_mode tests (Phase 1 + Phase 2 admission cases all green)
- PLAN-03 grep: `grep -nE 'recoverCycle|cycleRecover|fix.*cycle|skip.*cycle' internal/webhook/v1alpha1/` returns zero
- `make verify-no-aggregates` exits 0 (webhook adds no new aggregate fields)
- Apache headers preserved on all webhook files (kubebuilder convention)
</verification>

<success_criteria>
Phase 2 Success Criterion #2: "`kubectl apply` a cyclic Plan and see the admission path reject it with a structured error naming the involved tasks (no Wave resource ever produced); applying a Plan whose LLM-declared `depends_on` disagrees with the file-touch sets produces an admission warning (strict mode rejects)" — Plan 11 ships the full webhook body.

REQ-PLAN-01 satisfied: Plan admission computes wave structure via `pkg/dag.ComputeWaves`; CycleError → AdmissionResponse.allowed=false + structured error listing involved nodes.
REQ-PLAN-02 satisfied: file-touch ↔ dependsOn reconciliation via exact-path-overlap; strict/warn modes per D-E3 precedence; both modes fire K8s Events.
REQ-PLAN-03 satisfied: no cycle-recovery code path — verified by grep gate; webhook rejects + surfaces only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-11-SUMMARY.md` describing:
- The exact tasks-to-DAG translation (node = Task.Name; edges from Task.Spec.DependsOn)
- The Pitfall G defense (EXACT path equality only, not directory overlap)
- The mode resolution precedence + the Phase 2 trade-off on Project.Spec lookup (webhook reads annotation cache; PlanReconciler can stamp the cached annotation in a future enhancement)
- The K8s Event Reasons emitted (CycleDetected, FileTouchMismatch) + how operators can query them
- The "no owned Tasks visible" warning + how kubectl-apply-order plays into Phase 2 admission ergonomics
</output>