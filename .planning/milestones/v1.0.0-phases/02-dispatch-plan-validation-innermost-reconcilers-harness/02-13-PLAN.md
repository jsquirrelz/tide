---
phase: 2
plan: 13
type: execute
wave: 7
depends_on: ["02-02", "02-04", "02-05", "02-09", "02-10", "02-11", "02-12"]
files_modified:
  - test/integration/envtest/suite_test.go
  - test/integration/envtest/admission_test.go
  - test/integration/envtest/indegree_test.go
  - test/integration/envtest/budget_test.go
  - test/integration/envtest/init_test.go
  - test/integration/envtest/rate_limit_test.go
  - test/integration/kind/suite_test.go
  - test/integration/kind/cluster.yaml
  - test/integration/kind/wave_test.go
  - test/integration/kind/failure_test.go
  - test/integration/kind/caps_test.go
  - test/integration/kind/output_test.go
  - test/integration/kind/credproxy_test.go
  - test/integration/kind/testdata/three-task-wave.yaml
  - Makefile
  - .github/workflows/ci.yaml
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "`make test-int-fast` runs Layer A envtest suite (admission webhook + indegree + budget + init) in ~90s."
    - "`make test-int` runs Layer A + Layer B (kind + stub-subagent) in <5 minutes total."
    - "Layer A covers: cycle rejection, acyclic admission, file-touch strict/warn, indegree recompute, attempt-counter, owner-cascade, Wave roll-up Succeeded/Failed/Running, init Job creation + idempotency, budget cap + bypass, 429 absorption (synthetic — `rate_limit_test.go` exercises the ROADMAP AC #4 storm with 20 rapid Tasks under exhausted bucket + counter increment ≥ 20 + refill + dispatch resumes; Plan 09's `TestTaskReconciler_RateLimitStormAbsorbed` is the same coverage at the package-level envtest tier, but Plan 13 also includes the standalone `rate_limit_test.go` here for explicit AC-mapping per Warning #7), per-Secret-UID bucket isolation."
    - "Layer B covers: 3-task wave success (α→β→γ stub-subagent canned envelopes); fail-injection (testMode=fail-exit-1 on β → α succeeds, γ never dispatches); wall-clock cap (testMode=hang → harness/Job-deadline kill); output-paths violation (testMode=exceed-output-paths → Plan 09 `handleJobCompletion` flags via `outputs.Validate`); credproxy sidecar startup + readiness probe + listening-log assertion (Warning #8 fix: stub-subagent makes zero outbound calls so the credproxy 'happy path with outbound forward' test is impossible from Layer B; credproxy correctness coverage lives in Plan 05's unit tests — HMAC sign/verify with tampered/expired/wrong-uid fixtures — which already ship that surface)."
    - "`test/integration/kind/cluster.yaml` pins `kindest/node:v1.33.7@sha256:d26ef333bdb2cbe9862a0f7c3803ecc7b4303d8cea8e814b481b09949d353040` per STACK.md."
    - "`make test-int-kind-prep` builds + `kind load` the stub-subagent + credproxy images BEFORE tests run (Pitfall H defense — no mid-test image pulls)."
    - "CI workflow (.github/workflows/ci.yaml) adds a `test-int` job that runs `make test-int` and fails the build if duration exceeds 300s (TEST-02 budget assertion via `timeout 300s`)."
  artifacts:
    - path: test/integration/envtest/suite_test.go
      provides: "Ginkgo BeforeSuite spinning up envtest + registering Phase 2 reconcilers + admission webhook (separate from internal/controller/suite_test.go)"
      contains: "envtest.Environment"
    - path: test/integration/kind/cluster.yaml
      provides: "kind config with SHA-pinned kindest/node image"
      contains: "v1.33.7@sha256:d26ef333bdb2cbe9862a0f7c3803ecc7b4303d8cea8e814b481b09949d353040"
    - path: test/integration/kind/suite_test.go
      provides: "Ginkgo BeforeSuite spinning up kind, loading images, applying CRDs + Helm chart"
      contains: "kind create cluster"
    - path: Makefile
      provides: "test-int, test-int-fast, test-int-kind-prep, verify-import-firewall targets"
      contains: "test-int:"
    - path: .github/workflows/ci.yaml
      provides: "Phase 2 CI job running make test-int with 300s timeout"
      contains: "test-int"
  key_links:
    - from: "test/integration/kind/suite_test.go"
      to: "test/integration/kind/cluster.yaml"
      via: "kind create cluster --config cluster.yaml"
      pattern: "cluster.yaml"
    - from: "Makefile test-int-kind-prep target"
      to: "docker build + kind load docker-image for stub-subagent + credproxy"
      via: "kind load docker-image ghcr.io/jsquirrelz/tide-stub-subagent:test"
      pattern: "kind load docker-image"
---

<objective>
Ship the Phase 2 integration test tier (TEST-02): a two-layer split (envtest Layer A ~90s + kind Layer B ~180s = ~270s, comfortably under 5min). Layer A exercises reconciler + admission webhook bodies in-process via envtest; Layer B exercises full Job lifecycle in a real kind cluster with stub-subagent + credproxy images loaded.

Per CONTEXT D-H1..H4 + RESEARCH.md "Test Strategy / 5-Minute Budget Breakdown":
- Layer A is fast (envtest cold-start ~20s + 15-20 tests × ~3s = ~60s + teardown). Run via `make test-int-fast`.
- Layer B is heavier (kind cluster spin ~30s + image load ~15s + CRD/RBAC install ~10s + tests ~120s). Run via `make test-int`.
- CI runs `make test-int` in a matrix job with 300s timeout per TEST-02.

The integration test tier is a SEPARATE binary tier from `internal/controller/suite_test.go` (which lives in the package and runs as part of `make test` <30s per TEST-01). PATTERNS.md is explicit: don't fold these into the same suite — TEST-01 budget is sacred.

Output: Two new `test/integration/{envtest,kind}/` test packages + Makefile targets + CI workflow extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-VALIDATION.md
@internal/controller/suite_test.go
@internal/controller/plan_webhook_test.go
@pkg/dag/kahn_test.go
@Makefile
@.github/workflows/ci.yaml
@config/samples

<interfaces>
<!-- Per 02-RESEARCH.md "Test Strategy" Tables + "Validation Architecture". -->

Layer A — `test/integration/envtest/` package (separate binary; Ginkgo label `envtest`):

`suite_test.go` mirrors `internal/controller/suite_test.go:53-150` with the path-up-one-more adjustment (`filepath.Join("..", "..", "..", "config", "crd", "bases")`). BeforeSuite:
- Bootstrap envtest with CRD paths + WebhookInstallOptions.
- Register all six reconcilers + the Plan admission webhook (Plan 11 stateful form).
- Inject Phase 2 reconciler fields per Plan 09's suite_test.go pattern (Budget store, signing key, in-memory EnvReader, image placeholder strings, default Limits).
- Wire `Manager.Start` in a goroutine; cleanup in AfterSuite.

Test files (one Describe block per concern; each test has `Label("envtest")` + a REQ-ID label):

1. `admission_test.go` (PLAN-01, PLAN-02, PLAN-03): cyclic-plan reject, acyclic-admit, file-touch-strict reject, file-touch-warn warn-only, no-tasks-visible warning, mode-resolution-from-annotation/Project.Spec/Helm-default, cycle-recovery-feature-absent grep.

2. `indegree_test.go` (FAIL-01, FAIL-02, PERSIST-03, SUB-02, SUB-03): indegree-recomputed-per-reconcile, attempt-increments-at-job-create, deterministic-job-name-idempotent, owner-cascade Project→Milestone→Phase→Plan→Task→Wave, Wave-Status-Succeeded-when-all-Tasks-Succeeded, Wave-Status-Failed-when-one-Task-Failed-others-succeed, dependent-in-next-wave-blocked-when-predecessor-failed.

3. `budget_test.go` (FAIL-04): task-completion-rolls-up-to-project-budget, budget-exceeded-halts-dispatch, bypass-annotation-clears-halt, bypass-budget-until-TTL-honored.

4. `init_test.go` (ART-01): project-init-job-created-once, init-job-idempotent-on-reapply, init-job-completion-sets-project-phase-Initialized.

5. `rate_limit_test.go` (FAIL-03 — Warning #7 / ROADMAP AC #4 synthetic 429 storm): pre-charge a bucket via test helper `internal/budget.Bucket.SetForTest` so the bucket is effectively exhausted (RPM=5, Burst=2); create 20 Tasks rapidly under a single Project (all with indegree=0); reconcile them all in a tight loop; assert all 20 stay Phase=Pending with `Status.Conditions[Type=Reconciling, Reason=RateLimited]`; assert `tide_provider_rate_limit_hits_total{project=<name>}` ≥ 20 via the controller's metrics endpoint (envtest registers the global metrics registry; query via the runtime ListenAndServe address OR via the metrics-Registry.Gather() directly in-process); wait `Refill()` interval (or use a test helper to top up the bucket); reconcile again; assert at least one Job is created (dispatch resumes). This test is the explicit AC #4 mapping; Plan 09's package-tier `TestTaskReconciler_RateLimitStormAbsorbed` covers the same behavior — both shipping is a belt-and-suspenders move since AC #4 is mentioned by name.

Layer B — `test/integration/kind/` package (separate binary; Ginkgo label `kind`):

`cluster.yaml`:
```
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    image: kindest/node:v1.33.7@sha256:d26ef333bdb2cbe9862a0f7c3803ecc7b4303d8cea8e814b481b09949d353040
```

`suite_test.go` BeforeSuite:
- `kind create cluster --name tide-test --config cluster.yaml` (skip if cluster already exists).
- `kind load docker-image ghcr.io/jsquirrelz/tide-stub-subagent:test --name tide-test`.
- `kind load docker-image ghcr.io/jsquirrelz/tide-credproxy:test --name tide-test`.
- Apply CRDs from `config/crd/bases/`.
- Apply controller Deployment + RBAC + signing-secret via `helm install` (using the chart from Plan 12) OR via `kubectl apply -k config/default/` (the Phase 1 kustomize overlay).
- Construct k8sClient (controller-runtime client against the kind cluster's kubeconfig).
- Wait for controller-manager Pod Ready.
- AfterSuite: optional `kind delete cluster --name tide-test` (gated by env var `KEEP_KIND_CLUSTER=true` for dev iteration speed).

Test files:

1. `wave_test.go` (AC1, SUB-01, SUB-02, SUB-04, ART-01): three-task-wave-all-succeed (apply Plan + α→β→γ; Eventually(Wave.Status.Phase=Succeeded)), wave-advances-only-after-all-tasks-complete.

2. `failure_test.go` (AC3, FAIL-01, FAIL-02): failed-task-siblings-continue (3-task wave with β testMode=fail-exit-1, α and γ are independent siblings; α completes, γ depends on β so never dispatches), failed-task-dependent-never-dispatches.

3. `caps_test.go` (AC5, HARN-02): hang-mode-wall-clock-cap-terminates-subagent-via-sigterm (testMode=hang + caps.WallClockSeconds=10; Job's activeDeadlineSeconds kicks in at 70s; Pod exits with `result: cap-hit, reason: wall-clock`).

4. `output_test.go` (AC5, HARN-05): exceed-output-paths-mode-rejected (testMode=exceed-output-paths; harness validator on the test side reads the actual artifacts vs declared paths and asserts the violation is flagged).

5. `credproxy_test.go` (AC5, HARN-03 — Warning #8 fix): credproxy-sidecar-starts-and-becomes-ready (apply a Plan with a Task using `Dev.TestMode=success`; observe the Pod's `tide-credproxy` initContainer transitioning to Ready via the TCP probe on 8443; assert via Pod.Status.InitContainerStatuses[].Ready), credproxy-startup-log-line (kubectl logs of the credproxy container shows the line `credproxy listening on 127.0.0.1:8443` — confirms the listener bound and the cert minted successfully). **NEGATIVE-PATH COVERAGE NOTE:** the stub-subagent makes zero outbound calls (Plan 04 contract), so we cannot exercise a forwarded-request happy path from Layer B without adding a real outbound consumer (rejected — that adds an outbound call to the stub, which would conflict with the Phase 2 zero-LLM-cost design). Credproxy negative-path correctness (tampered token, expired token, wrong taskUID) is covered exhaustively in Plan 05's unit tests (`internal/credproxy/token_test.go` + `internal/credproxy/server_test.go` table-driven cases). The Layer B Phase 2 credproxy coverage is therefore: 'sidecar topology + startup signal' (this test) + 'unit-tested correctness' (Plan 05).

`testdata/three-task-wave.yaml`: a YAML fixture for the three-task α→β→γ wave (Tasks with FilesTouched + DependsOn declared per Plan 03 schema; Plan annotated with a Project that has providerSecretRef + budget defaults; Project includes a Secret for the provider-key Anthropic stand-in).

Makefile targets:

```
.PHONY: test-int test-int-fast test-int-kind-prep

test-int: manifests generate fmt vet setup-envtest test-int-kind-prep
	KUBEBUILDER_ASSETS="$$(setup-envtest use $(ENVTEST_K8S_VERSION) -p path)" \
		timeout 300s go test ./test/integration/... -v -timeout=5m -ginkgo.v

test-int-fast: manifests generate fmt vet setup-envtest
	KUBEBUILDER_ASSETS="$$(setup-envtest use $(ENVTEST_K8S_VERSION) -p path)" \
		go test ./test/integration/envtest/... -v -timeout=2m -ginkgo.v --ginkgo.label-filter='envtest'

test-int-kind-prep:
	docker build -t ghcr.io/jsquirrelz/tide-stub-subagent:test -f images/stub-subagent/Dockerfile .
	docker build -t ghcr.io/jsquirrelz/tide-credproxy:test -f images/credproxy/Dockerfile .
	@if ! kind get clusters | grep -q "^tide-test$$"; then \
		kind create cluster --name tide-test --config test/integration/kind/cluster.yaml; \
	fi
	kind load docker-image ghcr.io/jsquirrelz/tide-stub-subagent:test --name tide-test
	kind load docker-image ghcr.io/jsquirrelz/tide-credproxy:test --name tide-test
```

CI workflow extension (.github/workflows/ci.yaml):
- New job `test-int` in the matrix (alongside Phase 1 `lint`, `test`, `test-e2e`):
  - Set up Go 1.26 + kubebuilder + kind + docker.
  - Run `make test-int` with a 300s job timeout (CI runner steps).
  - On failure, surface the Ginkgo output + the kind cluster events for debugging.
- The `time make test-int` wall-time should report < 300s; the test-int target uses `timeout 300s` to fail the build if exceeded.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layer A envtest suite (suite_test.go + admission/indegree/budget/init tests)</name>
  <files>test/integration/envtest/suite_test.go, test/integration/envtest/admission_test.go, test/integration/envtest/indegree_test.go, test/integration/envtest/budget_test.go, test/integration/envtest/init_test.go</files>
  <read_first>
    - internal/controller/suite_test.go (Phase 1 BeforeSuite shape — clone with path-up-one-more adjustments per PATTERNS.md §"`test/integration/envtest/suite_test.go`")
    - internal/controller/plan_webhook_test.go (Plan 11 — admission test pattern via k8sClient.Create returning IsInvalid/IsForbidden)
    - internal/controller/task_controller_test.go (Plan 09 — indegree + attempt counter test patterns)
    - internal/controller/project_controller_test.go (Plan 10 — init Job + budget test patterns)
    - pkg/dag/kahn_test.go (α…θ fixture — reusable in indegree_test.go)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (§"Test Strategy / Layer A — envtest" — full table of tests; §"5-Minute Budget Breakdown" — 90s budget breakdown)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-VALIDATION.md (per-REQ test mapping confirming Layer A coverage)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"`test/integration/envtest/suite_test.go` (new)" + "`test/integration/envtest/{admission,indegree,budget}_test.go` (new tests)")
  </read_first>
  <behavior>
    - `go test ./test/integration/envtest/... -v -timeout=2m --ginkgo.label-filter='envtest'` completes in <120s with all tests PASS.
    - Each test carries `Label("envtest")` AND a REQ-ID label (e.g., `Label("PLAN-01")`) so `ginkgo -focus="PLAN-01"` selects directly.
    - The envtest BeforeSuite registers ALL six reconcilers + admission webhook so end-to-end chains can be exercised (apply Plan → admission validates → PlanReconciler materializes Waves → TaskReconciler dispatches Jobs (which envtest doesn't actually run — they're created but no kubelet → assertions on Job presence + Status patches).
    - The `internal/controller/suite_test.go` is NOT touched in this plan — it stays the Phase 1 + Phase 2 unit-level controller test suite under the TEST-01 30s budget.
  </behavior>
  <action>
    Create `test/integration/envtest/suite_test.go` mirroring `internal/controller/suite_test.go:53-150` precisely. Adjustments:
    - `CRDDirectoryPaths: []string{filepath.Join("..", "..", "..", "config", "crd", "bases")}` (one more `..` than the controller package).
    - `WebhookInstallOptions.Paths: []string{filepath.Join("..", "..", "..", "config", "webhook")}`.
    - Register ALL six reconcilers with their Phase 2 field injections (Budget store, signing key, in-memory EnvReader, image stubs, defaults). Reuse the test-helper construction pattern from `internal/controller/suite_test.go` — extract a helper `newPhase2ReconcilersForTest(mgr ctrl.Manager)` if needed.
    - Register `webhookv1alpha1.SetupPlanWebhookWithManager(mgr, "warn")`.
    - Register the `.spec.planRef` field indexer on Tasks.

    Create the four test files per <behavior> + <interfaces>. Each file uses Ginkgo `Describe(... Label("envtest"), ...)` with `Context("..." + It("..."))` structure. Tests use `k8sClient.Create / Get / List / Patch` against the envtest apiserver and `Eventually(...)` for status assertions.

    Apache 2.0 header? PATTERNS.md says test files in internal/controller/* carry the header (Ginkgo + kubebuilder convention). Apply the same convention here — `test/integration/envtest/*.go` are envtest-tier tests; carry the header.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && KUBEBUILDER_ASSETS="$(setup-envtest use 1.33.0 -p path 2>/dev/null || true)" timeout 150s go test ./test/integration/envtest/... -v -timeout=2m --ginkgo.label-filter='envtest'</automated>
  </verify>
  <done>
    - `go test ./test/integration/envtest/...` PASS on all admission/indegree/budget/init test cases
    - Total wall-time for Layer A < 120s (target ~90s)
    - `grep -c 'Label("envtest")' test/integration/envtest/*.go` returns ≥ 1 per file
    - The PLAN-03 grep gate passes (verified inline in `admission_test.go` via shell-out `It("PLAN-03 — cycle recovery features absent", ...)`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layer B kind suite (cluster.yaml + suite_test.go + wave/failure/caps/output/credproxy tests)</name>
  <files>test/integration/kind/suite_test.go, test/integration/kind/cluster.yaml, test/integration/kind/wave_test.go, test/integration/kind/failure_test.go, test/integration/kind/caps_test.go, test/integration/kind/output_test.go, test/integration/kind/credproxy_test.go, test/integration/kind/testdata/three-task-wave.yaml</files>
  <read_first>
    - test/integration/envtest/suite_test.go (Task 1 — clone the BeforeSuite Ginkgo pattern; kind suite uses a real kubeconfig instead of envtest's in-process apiserver)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (§"Test Strategy / Layer B — kind" — full table; §"Pitfall H — kind cluster image not pre-loaded" — defense via test-int-kind-prep)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md (D-H1, D-H3 — kind canonical with SHA-pinned image)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"`test/integration/kind/suite_test.go` (new test bootstrap)" + cluster.yaml partial — invent shape, SHA-pinned per STACK.md)
    - .planning/research/STACK.md (kindest/node SHA pin for v0.31.0 + K8s 1.33)
    - config/samples (Phase 1 α…θ fixtures — reuse subset for the 3-task wave)
    - cmd/stub-subagent/main.go (Plan 04 — for understanding stub testMode behavior in the kind context)
  </read_first>
  <behavior>
    - `go test ./test/integration/kind/... -v -timeout=4m --ginkgo.label-filter='kind'` against a freshly-prepped kind cluster completes in <180s with all tests PASS.
    - Layer B tests run sequentially (Ginkgo `--procs=1`) because they share the kind cluster.
    - Total wall-time including cluster setup + image load + tests + teardown < 240s.
    - `wave_test.go` is the AC1 sanity check: apply a 3-task Plan; watch Wave 0 dispatch all 3 stub-subagent Jobs; Eventually each Task.Status.Phase=Succeeded; Wave.Status.Phase=Succeeded.
    - `failure_test.go` is the FAIL-01 + FAIL-02 case: β (middle Task) has testMode=fail-exit-1; α (independent) completes; γ (depends on β) never dispatches (Phase stays Pending forever — assertion is `Consistently(γ.Status.Phase != "Running") within some window`).
    - `caps_test.go`: testMode=hang on a Task with caps.WallClockSeconds=10; Eventually(Pod exits with reason cap-hit OR Job.Status.Failed) within 80s.
    - `output_test.go`: testMode=exceed-output-paths; harness writes outside declared paths; EnvelopeOut shows result=output-paths-violation.
    - `credproxy_test.go` (Warning #8): credproxy sidecar container becomes Ready (TCP probe on 8443) AND emits its startup log line `credproxy listening on 127.0.0.1:8443`. NO outbound-call test — stub-subagent is zero-outbound by Plan 04 contract; outbound forwarding will be exercised in Phase 3 when Claude Code becomes the runtime. Negative-path token-validation correctness covered by Plan 05 unit tests.
  </behavior>
  <action>
    Create `test/integration/kind/cluster.yaml`:
    ```
    kind: Cluster
    apiVersion: kind.x-k8s.io/v1alpha4
    nodes:
      - role: control-plane
        image: kindest/node:v1.33.7@sha256:d26ef333bdb2cbe9862a0f7c3803ecc7b4303d8cea8e814b481b09949d353040
    ```

    Create `test/integration/kind/suite_test.go` with Ginkgo `BeforeSuite`:
    1. Check `KEEP_KIND_CLUSTER` env var; if true, skip cluster create on existing cluster.
    2. If no `tide-test` cluster: `exec.Command("kind", "create", "cluster", "--name", "tide-test", "--config", "cluster.yaml")` (assume Makefile prep ran first; fall back to in-test creation if SKIP_PREP not set).
    3. `kind get kubeconfig --name tide-test` → write to tmpfile → construct `client.Client` via `clientcmd.BuildConfigFromFlags`.
    4. Apply CRDs from `config/crd/bases/` via `kubectl apply -f` or via in-process `client.Create` on each CRD YAML decoded.
    5. Apply controller Deployment via helm: `exec.Command("helm", "install", "tide", "./charts/tide", "--set", "images.stubSubagent.tag=test", "--set", "images.credProxy.tag=test", ...)`. Alternatively for Phase 2 v0, apply `config/default/` kustomize bundle directly (simpler; Helm chart from Plan 12 is the production path but envtest-style apply works for the kind suite too).
    6. Wait for tide-controller-manager Deployment Ready.
    7. AfterSuite: optional `kind delete cluster --name tide-test` (gated by `!KEEP_KIND_CLUSTER`).

    Create the 5 test files per <behavior>. Each `Describe(... Label("kind"), ...)`. Tests apply YAML fixtures via the kind k8sClient + `Eventually` for status assertions. The `credproxy_test.go` file does NOT exercise the credproxy as a forward-proxy (Warning #8) — it only asserts sidecar topology and startup signal. Cross-reference Plan 05 unit tests in a comment at the top of `credproxy_test.go`.

    Create `test/integration/kind/testdata/three-task-wave.yaml` with: Namespace + Secret (for ANTHROPIC_API_KEY placeholder; the stub doesn't use it but the controller wires the envFrom for the credproxy) + Project (with providerSecretRef + budget defaults) + Milestone + Phase + Plan + 3 Tasks (α, β, γ) with `Dev.TestMode` set per the test's needs (overrides happen in individual tests via Patch).

    Apache 2.0 header on test files (Ginkgo + integration test convention).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make test-int-kind-prep 2>/dev/null; timeout 240s go test ./test/integration/kind/... -v -timeout=4m --ginkgo.label-filter='kind' -ginkgo.procs=1 || echo "Layer B requires Docker + kind in CI; treat as skipped on dev machines without those"</automated>
  </verify>
  <done>
    - cluster.yaml present with the SHA-pinned kindest/node image
    - `go test ./test/integration/kind/... --ginkgo.label-filter='kind'` PASS in <240s on a machine with Docker + kind v0.31 available
    - Tests covering AC1 (3-task wave success), AC3 (failure injection + dependent block), AC5 (caps + output validation + credproxy) all green
    - The 5-test count + per-test timing budget fits the ~180s Layer B target
    - Apache headers on all test files
  </done>
</task>

<task type="auto">
  <name>Task 3: Makefile targets + CI workflow extension + TEST-02 timing assertion</name>
  <files>Makefile, .github/workflows/ci.yaml</files>
  <read_first>
    - Makefile (Phase 1 — preserve test/test-only/verify-* targets; add Phase 2 test-int / test-int-fast / test-int-kind-prep)
    - .github/workflows/ci.yaml (Phase 1 — existing lint/test/test-e2e jobs; add test-int job in matrix)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md (D-H2 — Makefile target layout)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (§"5-Minute Budget Breakdown" — 300s CI timeout)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"`Makefile` (extend — add Phase 2 targets)" — naming convention)
  </read_first>
  <behavior>
    - `make test-int-fast` exits 0 in ~90s on a dev machine (envtest only; no Docker/kind needed).
    - `make test-int` exits 0 in <300s on a dev machine with Docker + kind installed.
    - `make test-int-kind-prep` builds the two images + `kind load`s them; idempotent (skips kind cluster create if exists).
    - CI workflow has a new `test-int` job that runs `make test-int`; the step has `timeout-minutes: 6` and the inner `time make test-int` reports wall-time which CI parses + fails on > 300s.
    - The Phase 1 `make test` (30s budget per TEST-01) continues to pass — `internal/controller/*_test.go` Phase 2 additions stay within that budget.
  </behavior>
  <action>
    Edit `Makefile`: add the three new phony targets per <interfaces>. Preserve the Phase 1 `test:` / `test-only:` / `verify-*` target naming convention. The `test-int` target depends on `test-int-kind-prep` so the prep runs first; `test-int-fast` skips kind prep.

    Insert the targets under a new `##@ Integration tests (TEST-02 — Phase 2)` section to keep `make help` output organized.

    Edit `.github/workflows/ci.yaml`: add a new job `test-int` after the existing Phase 1 jobs. The job:
    - `runs-on: ubuntu-latest`.
    - `timeout-minutes: 6` (300s + buffer for Docker pull, etc.).
    - Steps: checkout, set up Go 1.26, install kubebuilder, install kind v0.31 (pinned), install Docker (CI already has it), `make test-int`.
    - On failure: collect Ginkgo output + `kind export logs` for the cluster + upload as artifact.

    The TEST-02 timing assertion: the `make test-int` Make target body uses `timeout 300s` so the shell command fails after 300s. CI's job-level `timeout-minutes: 6` is the outer fence.

    DO NOT remove or modify the Phase 1 `lint`, `test`, `test-e2e` CI jobs.
  </action>
  <verify>
    <automated>make test-int-fast 2>&1 | tail -20 &amp;&amp; grep -c 'test-int:\|test-int-fast:\|test-int-kind-prep:' Makefile</automated>
  </verify>
  <done>
    - `make test-int-fast` exits 0 in ~90s (Layer A envtest only)
    - `make test-int-kind-prep` (run separately on a docker+kind machine) succeeds + idempotent
    - `make test-int` (run on a docker+kind machine) exits 0 in <300s
    - `.github/workflows/ci.yaml` has a new `test-int` job entry; the existing Phase 1 jobs preserved
    - `grep -c 'test-int' .github/workflows/ci.yaml` returns ≥ 1
    - The Phase 1 TEST-01 30s budget continues to hold for `make test` (Phase 2 additions in `internal/controller/*_test.go` don't blow past 30s)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → kind cluster | Kind cluster runs entirely inside the CI runner; no external network exposure. |
| Stub-subagent image → loaded via kind load | Image content built locally in CI from the in-tree Dockerfile; no external registry pull during test runtime. |
| test fixtures (YAML) → kind apiserver | Tests apply CRDs to the kind cluster; CRDs admitted per Plan 11's webhook (cycle/file-touch rules apply). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13-01 | Denial of Service | Layer B tests blow past 5min budget on slow CI | mitigate | `timeout 300s` wraps the test invocation; CI's `timeout-minutes: 6` is the outer guard; Pitfall H prep step pre-loads images so no mid-test network pulls. |
| T-02-13-02 | Tampering | kind cluster persists between tests carrying stale state | mitigate | `KEEP_KIND_CLUSTER` defaults to false; AfterSuite deletes the cluster. Dev mode opts into persistence. |
| T-02-13-03 | Information Disclosure | Test logs leak the test signing key | accept | Test signing key is a fixed test-only constant (NOT the production key); leaking it doesn't affect production. |
| T-02-13-04 | Repudiation | Test failures hard to diagnose in CI | mitigate | `kind export logs` runs on failure; Ginkgo verbose output captured; artifacts uploaded. |
</threat_model>

<verification>
- `make test-int-fast` PASS in <120s on dev machine (envtest only)
- `make test-int` PASS in <300s on docker+kind machine
- Phase 1 `make test` continues to PASS in <30s (TEST-01 budget honored)
- CI workflow extension is additive — Phase 1 jobs (lint, test, test-e2e) preserved
- `make verify-no-blocking`, `make verify-rbac-marker-discipline`, `make verify-no-aggregates`, `make verify-import-firewall`, `make verify-dispatch-imports`, `make verify-dag-imports` all continue to exit 0
- The two-layer split achieves the ~270s total + 30s buffer (under the 300s budget)
</verification>

<success_criteria>
Phase 2 Success Criterion #4: "integration test tier (envtest + kind + stub-subagent) in under 5 minutes" — Plan 13 ships the entire two-layer integration test tier + the CI timing assertion.

Phase 2 Success Criterion #1, #3, #5 are all exercised end-to-end by Layer B:
- #1: wave_test.go's three-task success path
- #3: failure_test.go's sibling/dependent block assertion
- #5: caps_test.go + output_test.go + credproxy_test.go's harness defense + signed-token proxy

REQ-TEST-02 satisfied: integration tests use envtest + kind + stub-subagent; full reconcile chains without LLM cost; run in <5min; CI gate fails on timing regression.

Warning #7 (ROADMAP AC #4) satisfied: `rate_limit_test.go` exercises the synthetic 429 storm at Layer A (envtest tier — no Job creation needed; controller's rate-limit gate fires under load).
Warning #8 satisfied: `credproxy_test.go` adopts sidecar-startup verification + log-line assertion (no outbound-call test — stub-subagent zero-outbound contract preserved); negative paths remain unit-tested in Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-13-SUMMARY.md` describing:
- Total test count across Layer A + Layer B
- Actual measured wall-time on the dev machine for each layer + total
- The Make target chain (test-int → test-int-kind-prep → docker build → kind load → go test)
- The CI workflow job structure (timeout-minutes + on-failure log collection)
- Any test cases skipped or partially exercised (e.g., the credproxy negative cases living in Plan 05 unit tests vs Layer B integration)
- The kind cluster reuse strategy (KEEP_KIND_CLUSTER env for dev iteration)
- How Plan 13 closes out Phase 2 — confirming all 5 success criteria have at least one passing test exercising them
</output>
