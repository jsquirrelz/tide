---
phase: 02.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md
autonomous: true
requirements:
  - roadmap-goal-baseline-capture
user_setup: []
must_haves:
  truths:
    - "A single Markdown artifact `02.1-BASELINE.md` exists capturing the EXACT failure output of `make test-int` from a clean state (no prior kind cluster, no prior helm release)."
    - "The artifact records the per-spec pass/fail status of all 7 Layer B Ginkgo specs against the current (broken) install path so subsequent plans can prove the fix improved the score."
    - "Three pre-fix decisions are locked in writing: (a) credproxy boot-banner approach (stdout banner vs structured-log-grep update), (b) per-test subagent-SA strategy (per-namespace helper vs chart-templated), (c) default value of TIDE_REQUIRE_CONTROLLER for Phase 02.1 CI runs."
    - "No production source files (`cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`) are modified by this plan — this is a measurement + decision plan."
  artifacts:
    - path: .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md
      provides: "Recorded failure mode of make test-int Layer B against unmodified Phase 2 code; locked decisions for credproxy banner, SA strategy, TIDE_REQUIRE_CONTROLLER default"
      contains: "Decision Log"
  key_links:
    - from: ".planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md"
      to: "02.1-02-PLAN.md and 02.1-03-PLAN.md"
      via: "Decision Log section locks the credproxy-banner approach (consumed by Plan 03) and SA-strategy (consumed by Plan 02)"
      pattern: "Decision Log"
---

<objective>
Capture the EXACT current failure mode of `make test-int` (Layer B kind suite) from a clean state, and lock three pre-fix design decisions in a written artifact so subsequent plans don't re-litigate them mid-fix.

Purpose: A test-fix phase that doesn't record "what failed before" cannot honestly claim "the fix worked." Three decisions need to be made BEFORE the fixing starts so the fix plans (02 + 03) have unambiguous direction.

Output: `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md` — a single Markdown file with the captured failure output and the three locked decisions.

This plan does NOT change any production code. It runs solo in Wave 1 so Plans 02 and 03 can fire in parallel in Wave 2 with the decisions locked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-RESEARCH.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-PATTERNS.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-VALIDATION.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-13-SUMMARY.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture clean-state baseline + lock decisions in 02.1-BASELINE.md</name>
  <files>.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md</files>
  <read_first>
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-RESEARCH.md (root-cause analysis + Pitfall 8 banner options + Open Question #1 SA strategy + Assumption A7 TIDE_REQUIRE_CONTROLLER policy)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-PATTERNS.md (analog files)
    - test/integration/kind/suite_test.go (current applyController() — kustomize path that is broken)
    - test/integration/kind/credproxy_test.go (the literal log-grep at lines 199, 208)
    - cmd/credproxy/main.go (current zap-only log line at lines 118-123)
    - charts/tide/templates/serviceaccount-subagent.yaml (subagent SA template — only renders in .Release.Namespace; per-test namespaces would need their own SA copy)
  </read_first>
  <action>
    Step 1: Run a CLEAN-STATE baseline of `make test-int` to capture the EXACT current failure mode. Execute exactly these commands and capture stdout+stderr verbatim:
      (a) `kind delete cluster --name tide-test 2>&1 || true` (idempotent — succeeds whether or not the cluster exists)
      (b) `helm uninstall tide -n tide-system 2>&1 || true` (idempotent — clears any leftover release)
      (c) `timeout 600s make test-int 2>&1 | tee /tmp/02.1-baseline-output.log` (capture the full Make + Ginkgo output; 10-minute outer timeout is generous because the suite's own `timeout 300s` should fire first)
      Record exit code, total wall time, and the per-spec PASS/FAIL/SKIP outcome for each of the 7 Layer B Ginkgo `It(...)` blocks identified in RESEARCH.md "Phase Requirements → Test Map" table. If a spec did not run (suite aborted before reaching it), record SUITE-ABORTED.
    Step 2: Create `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md` with these sections:

      **Header:** phase, plan, captured-on date (today, 2026-05-13), invoking-developer hostname.

      **Section 1 — Reproducer:** literal commands (a)-(c) above, verbatim, copy-pasteable.

      **Section 2 — Captured Failure Output:** quote `/tmp/02.1-baseline-output.log`, redacted to fit the artifact (lead-trim the docker-build noise; keep all Ginkgo failure spec text). Aim for <300 lines of the most diagnostic output.

      **Section 3 — Per-Spec Outcome Table:** one row per spec from RESEARCH.md "Phase Requirements → Test Map" table (7 rows; columns: spec-name, file, outcome (PASS/FAIL/SKIP/SUITE-ABORTED), failure-mode-symptom (one sentence)). The "failure-mode-symptom" should be the dominant Ginkgo failure (e.g., "controller never reached Ready; spec timed out on Eventually(Task.Status.Phase=Succeeded)"). This is the BEFORE-FIX score.

      **Section 4 — Root-Cause Confirmation:** confirm or refute the RESEARCH.md root-cause hypothesis (kustomize-vs-helm install drift) by inspecting (a) `kubectl get pods -n tide-system` post-baseline (if the cluster persisted via KEEP_KIND_CLUSTER, or by re-running just the BeforeSuite path), (b) any CrashLoopBackOff message containing `TIDE_SIGNING_KEY env var is required`. Quote 1-2 lines of evidence per RESEARCH.md Pitfall 1's expected symptom. If the evidence contradicts the hypothesis, STOP and request planner re-evaluation — do not proceed to Section 5.

      **Section 5 — Decision Log (LOCKED):** Three subsections with explicit decisions:

        D-01 (Credproxy boot-banner approach): "Approach (a) — emit single-line plaintext banner to stdout via `fmt.Fprintf(os.Stdout, "credproxy listening on %s\\n", listenAddr)` in `cmd/credproxy/main.go`, KEEPING the existing structured zap `log.Info("starting credential proxy", ...)` line alongside it. Per RESEARCH.md Pitfall 8 recommendation and PATTERNS.md cmd/credproxy/main.go entry. Why: minimal code change (2 lines); the test grep at credproxy_test.go:199 passes unchanged; the banner is intentional human-readable boot signal that survives log-formatter changes — a common pattern in K8s operators. Rejected (b) update the test grep to match zap JSON — more brittle to log-format refactors and requires test changes that don't align with the spec's eventual operator-readable boot signal." Plan 03 implements this verbatim.

        D-02 (Per-test subagent-SA strategy): "Approach (a) — add a `ensureSubagentSA(ns string)` helper in `test/integration/kind/suite_test.go` (analog: existing `applyYAML` at lines 385-394), called from `createNamespace` in `failure_test.go:158-166`. Per RESEARCH.md Pitfall 6 + PATTERNS.md optional-helper section. Why: the chart's `serviceaccount-subagent.yaml` only templates the SA in `.Release.Namespace` (`tide-system`); tests apply Tasks into per-test namespaces (`tide-int-test`, `failure-test`, `caps-test`, `output-test`, `credproxy-test`). Without the SA in each per-test namespace, Pod creation fails with `serviceaccount "tide-subagent" not found`. The helper YAML is identical to the chart template minus the `tide.labels` block and the `{{ .Release.Namespace }}` — D-A4 (zero K8s verbs on subagent SA) preserved. Rejected (b) cluster-scoped SA — SAs are namespace-scoped by K8s API; cannot. Rejected (c) chart post-render hook — out of scope; tests own their per-namespace setup. Plan 02 implements this."

        D-03 (TIDE_REQUIRE_CONTROLLER default for Phase 02.1): "Approach: KEEP existing default of `unset` (silent skip when controller not ready) per the WR-09 contract established in Phase 2 Plan 13. Phase 02.1 CI runs MUST set `TIDE_REQUIRE_CONTROLLER=1` in the workflow env block to ensure install failures fail the CI build loudly. Local dev iterations may unset it for partial-coverage debugging. Per RESEARCH.md Assumption A7. Plan 04 wires the CI env var; Plan 02 keeps the existing soft-skip logic intact (do not invert the default)."

      **Section 6 — Open Questions Carried Forward:** Quote the four "Open Questions" from RESEARCH.md and annotate each with how Phase 02.1 will resolve them (e.g., "Open Question 1 — truncated-hierarchy fixtures — Plan 02 will read PlanReconciler.resolveProject post-install-fix; if the truncated hierarchy doesn't reconcile, Plan 02's task list grows by one fixture-enrichment task"). This is a forward-looking surface so Plan 02 doesn't get blocked.

      **Section 7 — What This Plan Did NOT Change:** explicitly list paths NOT touched: `cmd/credproxy/main.go`, `test/integration/kind/suite_test.go`, `Makefile`, `charts/tide/`, any `internal/` or `api/`. Confirms the measurement-only nature.

    No edits to ANY file outside `.planning/`. No `git commit` from this task — the orchestrator's final commit step covers it.
  </action>
  <verify>
    <automated>test -f .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md && grep -c '^## ' .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md | awk '{ if ($1 >= 7) exit 0; else exit 1 }' && grep -q 'D-01' .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md && grep -q 'D-02' .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md && grep -q 'D-03' .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md` exists.
    - File has at least 7 H2 sections (Reproducer, Captured Failure Output, Per-Spec Outcome Table, Root-Cause Confirmation, Decision Log, Open Questions Carried Forward, What This Plan Did NOT Change).
    - Decision Log section contains three explicitly-labeled decisions: D-01 (credproxy banner approach), D-02 (per-test SA strategy), D-03 (TIDE_REQUIRE_CONTROLLER default).
    - Per-Spec Outcome Table has exactly 7 rows (one per spec from RESEARCH.md's Phase Requirements → Test Map).
    - `grep -c "FAIL\|SUITE-ABORTED" .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md` returns ≥ 1 (confirms the baseline really captured failures, not a fluke green run).
    - No git diff outside `.planning/`.
  </acceptance_criteria>
  <done>
    - 02.1-BASELINE.md committed with all 7 H2 sections.
    - Three decisions (D-01, D-02, D-03) locked with rationale.
    - Per-spec failure outcomes captured for all 7 Layer B specs.
    - Root-cause confirmation either supports the kustomize-vs-helm hypothesis OR escalates a request to re-evaluate the phase.
    - Zero modifications outside `.planning/` (verified by `git status` showing only the new Markdown file).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → ephemeral kind cluster | Kind cluster runs in local Docker; no external network exposure during baseline capture. |
| Captured baseline output → committed Markdown artifact | Output from `make test-int` may contain pod names, namespace names, and ephemeral signing keys generated by Helm's `randAlphaNum 64`. These get captured into `.planning/` and committed to git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01-01 | Information Disclosure | `02.1-BASELINE.md` captures and commits a value of `TIDE_SIGNING_KEY` rendered by `helm install` (or its base64-encoded form from `kubectl get secret`) | mitigate | Before quoting `/tmp/02.1-baseline-output.log` into Section 2, run `sed -E 's/TIDE_SIGNING_KEY[":= ]+[A-Za-z0-9+/=]{20,}/TIDE_SIGNING_KEY=<redacted>/g'` on the captured text. Reject ANY 60+-char alphanumeric sequence next to the string `TIDE_SIGNING_KEY`. Helm regenerates the key on every install anyway (ephemeral test-cluster scope), but the redaction discipline establishes the pattern for Plan 04's verification logs. |
| T-02.1-01-02 | Information Disclosure | Captured baseline output includes plain ANTHROPIC_API_KEY env value from any leaked pod descriptions | accept | Phase 02.1 fixtures use a placeholder stub-key (no real provider key), per Phase 2 Plan 13's `testdata/three-task-wave.yaml` which uses a placeholder Secret value. No real key in the test path. |
| T-02.1-01-03 | Tampering | Baseline artifact misleadingly claims tests passed when they didn't (e.g., copy-paste error on Per-Spec Outcome Table) | mitigate | Acceptance criteria requires `grep -c "FAIL\|SUITE-ABORTED"` to return ≥ 1 — a baseline that shows zero failures fails the gate, forcing re-capture or escalation. |
| T-02.1-01-04 | Repudiation | Three locked decisions are made but Plan 02/03 deviates from them mid-implementation | mitigate | Decision Log is explicitly LOCKED (the word appears in the section header); Plans 02 and 03 reference D-01 / D-02 / D-03 in their task actions; checker workflow can grep for "D-01" in subsequent plans' SUMMARY.md to verify adherence. |
</threat_model>

<verification>
- 02.1-BASELINE.md exists with all 7 required H2 sections.
- Decisions D-01, D-02, D-03 are explicitly labeled in the Decision Log section.
- Per-Spec Outcome Table has 7 rows mapping to RESEARCH.md's spec inventory.
- `git status` shows zero changes outside `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/`.
- No `TIDE_SIGNING_KEY` 60+-char value appears in committed Markdown (sed-redaction was applied if the baseline log contained one).
</verification>

<success_criteria>
The Phase 02.1 fixers (Plans 02 + 03) start with: (a) a recorded failure score, (b) three locked decisions, (c) explicit confirmation that the install-path drift is the actual problem (not something else). Without those, the fix is shooting in the dark.

This plan does NOT advance the ROADMAP entry's checkboxes for "controller-manager Ready" or "7 specs pass" — those are Plans 02/03/04 deliverables. This plan delivers the measurement substrate.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-01-SUMMARY.md` describing:
- The literal commands run for the baseline capture (so it's reproducible)
- The baseline pass/fail score (e.g., "0/7 Layer B specs passing; SUITE-ABORTED before any spec ran")
- The three locked decisions (D-01, D-02, D-03) — copy the one-line summary of each
- Any deviations from the original RESEARCH.md hypothesis discovered during baseline capture
- Confirmation that no production code was modified
</output>
