---
phase: 01-foundation-crds-pkg-dag-controller-scaffold
plan: 11
type: execute
wave: 3
depends_on: [01, 05, 08, 09]
files_modified:
  - Makefile
  - charts/tide/Chart.yaml
  - charts/tide/values.yaml
  - charts/tide/templates/
  - charts/tide-crds/Chart.yaml
  - charts/tide-crds/templates/
  - config/default/kustomization.yaml
  - .github/workflows/ci.yaml
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "make helm produces two Helm charts via helmify: charts/tide/ (controller-only) and charts/tide-crds/ (CRD subchart)"
    - "charts/tide/values.yaml exposes plannerConcurrency (default 16), executorConcurrency (default 4), maxConcurrentReconciles per Kind, image.repository, image.tag"
    - "The Helm chart renders a ConfigMap mounted at /etc/tide/config.yaml in the controller Deployment"
    - "The final .github/workflows/ci.yaml includes: setup-go 1.26, make verify-dag-imports, make verify-no-aggregates, make verify-no-sqlite-dep, make verify-no-rbac-wildcards, make verify-rbac-marker-discipline, make verify-no-blocking, make tide-lint, go vet, make test (with -short for TEST-01 budget), AND asserts the test step completes in under 30s"
    - "kubebuilder Kustomize base under config/ is the source of truth; helmify reads it"
    - "Phase 5 will add dashboard templates + ServiceMonitor + LICENSE headers to the same chart pair — Phase 1's charts are Phase-5-compatible (helmify-driven, no hand-maintained templates that would conflict)"
  artifacts:
    - path: "Makefile (helm targets)"
      provides: "make helm, make helm-controller, make helm-crds"
      contains: "helmify"
    - path: "charts/tide/Chart.yaml"
      provides: "Helm Chart metadata for controller bundle"
    - path: "charts/tide/values.yaml"
      provides: "Tunable values surface"
      contains: "plannerConcurrency"
    - path: "charts/tide-crds/Chart.yaml"
      provides: "Helm Chart metadata for CRD subchart (safe upgrades)"
    - path: ".github/workflows/ci.yaml"
      provides: "Complete CI workflow with all Phase 1 gates"
      contains: "TEST-01"
  key_links:
    - from: "config/ (kubebuilder Kustomize output)"
      to: "charts/tide/ + charts/tide-crds/"
      via: "helmify"
      pattern: "kustomize build .* | helmify"
    - from: ".github/workflows/ci.yaml"
      to: "all Phase 1 make targets"
      via: "GitHub Actions steps"
      pattern: "make (verify|test|tide-lint)"
---

<revision_note>
**Revision 1 — checker feedback applied:**
- **Info 11:** Pinned helmify install to `@v0.4.17` (was `@latest`) for reproducible chart generation across executor runs and CI.
</revision_note>


<objective>
Wire `make helm` to produce the two Helm charts via helmify (the CNCF-blessed path for kubebuilder → Helm per CLAUDE.md). Finalize the .github/workflows/ci.yaml workflow so it gates on every Phase 1 verification target produced across Plans 02-09 AND asserts the TEST-01 30-second test budget. This is the last Phase 1 plan — its success criteria align with the phase's overall TEST-01 + distribution-ready commitments.

Per D-E1: Helm chart ships in Phase 1, not deferred to Phase 5. The release artifact is two charts: charts/tide/ (controller) and charts/tide-crds/ (CRDs as dedicated subchart for safe `helm upgrade`). Per D-E2: Phase 1's charts must remain Phase 5-compatible (helmify-driven).

Purpose: Distribution is a load-bearing Phase 1 commitment (Pitfall 16 future-proofing — CRD subchart pattern is structurally easier to scaffold once than to migrate into). The CI workflow ensures every PR satisfies every Phase 1 invariant before merge.

Output: charts/ directory committed (helmify output) + finalized Makefile helm targets + .github/workflows/ci.yaml with all gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-CONTEXT.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-VALIDATION.md
@CLAUDE.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-08-SUMMARY.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-09-SUMMARY.md

<interfaces>
01-RESEARCH.md "Helm Chart Pair (D-E1, D-E2)" (lines ~1257-1347) — exact helmify invocation + values.yaml surface + ConfigMap rendering pattern.

helmify v0.4.x (or latest) reads `kustomize build <dir>` stdout and produces Helm chart files in the target directory. Install:
```
go install github.com/arttor/helmify/cmd/helmify@v0.4.17  # revision Info 11 — pinned version for reproducible chart generation
```

Two charts, two invocations:
- `kustomize build config/default | helmify charts/tide`  → controller chart
- `kustomize build config/crd | helmify charts/tide-crds` → CRD subchart

For the controller chart (charts/tide/), additional values.yaml surface (per 01-RESEARCH.md):
```yaml
image:
  repository: ghcr.io/jsquirrelz/tide-controller
  tag: v0.1.0-dev
  pullPolicy: IfNotPresent

plannerConcurrency: 16
executorConcurrency: 4

maxConcurrentReconciles:
  project: 1
  milestone: 1
  phase: 2
  plan: 4
  wave: 8
  task: 16

leaderElection:
  enabled: true
```

helmify may not auto-detect these custom values — manual values.yaml editing is expected after the initial helmify run. The Helm template renders these into a ConfigMap at templates/configmap.yaml (manual addition) and the Deployment mounts /etc/tide/config.yaml.

For Phase 1's purposes, helmify output is committed RAW + the values.yaml is hand-augmented + a single ConfigMap template is added. Phase 5 will refine the chart for OSS distribution; Phase 1 commits to the helmify-driven posture so Phase 5 has the structure.

Final CI workflow gates (all from Plans 02-09):
1. make verify-dag-imports (DAG-05 — Plan 02)
2. make verify-no-aggregates (PERSIST-02 — Plan 05)
3. make verify-no-sqlite-dep (PERSIST-01 — Plan 05)
4. make verify-no-rbac-wildcards (AUTH-03 — Plan 09)
5. make verify-rbac-marker-discipline (AUTH-03 — Plan 09)
6. make verify-no-blocking (Pitfall 1 — Plan 06)
7. make tide-lint (POOL-03 — Plan 03)
8. go vet ./...
9. make test -short (TEST-01 with 30s budget assertion)

TEST-01: <30s budget is a HARD assertion. CI step must exit 1 if the test step exceeds 30 seconds.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add make helm targets + run helmify to produce charts/tide/ and charts/tide-crds/</name>
  <files>Makefile, charts/tide/, charts/tide-crds/, config/default/kustomization.yaml</files>
  <read_first>
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md "helmify workflow" (lines ~1259-1278) — exact make targets and command invocations
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md "Helm values surface" (lines ~1280-1305) — values.yaml shape
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md "ConfigMap rendering pattern" (lines ~1307-1347) — how ConfigMap mounts at /etc/tide/config.yaml
    - config/manager/manager.yaml (Plan 08 updated this — Deployment shape)
    - config/default/kustomization.yaml (kubebuilder default — may need editing so config/default doesn't include config/crd when generating the controller chart)
    - Makefile (current state, all targets from prior plans)
  </read_first>
  <action>
First, install helmify (if not already installed):
```
go install github.com/arttor/helmify/cmd/helmify@v0.4.17  # revision Info 11 — pinned version for reproducible chart generation
# Verify:
helmify --version
```

Add to Makefile:
```
.PHONY: helm helm-controller helm-crds

helm: helm-controller helm-crds

helm-controller: manifests
	@echo "generating charts/tide/ via helmify..."
	mkdir -p charts/tide
	kustomize build config/default | helmify charts/tide

helm-crds: manifests
	@echo "generating charts/tide-crds/ via helmify..."
	mkdir -p charts/tide-crds
	kustomize build config/crd | helmify charts/tide-crds
```

Verify config/default/kustomization.yaml doesn't include config/crd (otherwise both CRDs and controller manifests end up in charts/tide/, defeating the dedicated CRD subchart pattern). Read config/default/kustomization.yaml; if it lists `../crd` under resources, remove that line (helmify-controller will then NOT emit CRD templates into charts/tide/).

Run:
```
make helm
```

Expected outcome:
- charts/tide/Chart.yaml exists with name=tide, version (e.g., 0.1.0)
- charts/tide/templates/ contains: deployment.yaml (controller), serviceaccount.yaml, role.yaml, rolebinding.yaml, leader-election-role.yaml + binding, webhook configs, etc.
- charts/tide-crds/Chart.yaml exists with name=tide-crds
- charts/tide-crds/templates/ contains 6 CRD YAMLs (one per Kind)

After the initial helmify run, hand-augment charts/tide/values.yaml to add the Phase 1 tunables (plannerConcurrency, executorConcurrency, maxConcurrentReconciles map):

```yaml
# Append to charts/tide/values.yaml
plannerConcurrency: 16
executorConcurrency: 4

maxConcurrentReconciles:
  project: 1
  milestone: 1
  phase: 2
  plan: 4
  wave: 8
  task: 16

leaderElection:
  enabled: true
```

Add charts/tide/templates/configmap.yaml — hand-authored (helmify won't emit it because there's no ConfigMap in the Kustomize base):

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tide.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tide.labels" . | nindent 4 }}
data:
  config.yaml: |
    plannerConcurrency: {{ .Values.plannerConcurrency | default 16 }}
    executorConcurrency: {{ .Values.executorConcurrency | default 4 }}
    maxConcurrentReconciles:
      project: {{ .Values.maxConcurrentReconciles.project | default 1 }}
      milestone: {{ .Values.maxConcurrentReconciles.milestone | default 1 }}
      phase: {{ .Values.maxConcurrentReconciles.phase | default 2 }}
      plan: {{ .Values.maxConcurrentReconciles.plan | default 4 }}
      wave: {{ .Values.maxConcurrentReconciles.wave | default 8 }}
      task: {{ .Values.maxConcurrentReconciles.task | default 16 }}
```

The exact template helper names (`tide.fullname`, `tide.labels`) depend on what helmify emits in charts/tide/templates/_helpers.tpl. Inspect after helmify run.

Add a volumeMount + volume to charts/tide/templates/deployment.yaml (helmify-emitted Deployment) — splice in a `tide-config` ConfigMap mount at /etc/tide/. Reference 01-RESEARCH.md "ConfigMap rendering pattern" lines ~1330-1345 for the exact YAML.

Verify the chart lints:
```
helm lint charts/tide
helm lint charts/tide-crds
helm template charts/tide --dry-run > /dev/null
```

All three commands must exit 0.

Commit charts/ contents to git — Phase 1 keeps the helmify output committed so contributors can `helm install ./charts/tide` directly without running helmify locally.

If helmify is fiddly with the values surface (it's not always perfect), document any manual fixes in 01-11-SUMMARY.md so Phase 5 doesn't re-discover them.
  </action>
  <verify>
    <automated>make helm &amp;&amp; test -f charts/tide/Chart.yaml &amp;&amp; test -f charts/tide-crds/Chart.yaml &amp;&amp; helm lint charts/tide &amp;&amp; helm lint charts/tide-crds</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^helm:" Makefile` exits 0
    - `grep -q "helm-controller:" Makefile` exits 0
    - `grep -q "helm-crds:" Makefile` exits 0
    - `grep -q "helmify" Makefile` exits 0
    - `test -d charts/tide` exits 0
    - `test -f charts/tide/Chart.yaml` exits 0
    - `test -f charts/tide/values.yaml` exits 0
    - `grep -q "plannerConcurrency" charts/tide/values.yaml` exits 0
    - `grep -q "executorConcurrency" charts/tide/values.yaml` exits 0
    - `grep -q "maxConcurrentReconciles" charts/tide/values.yaml` exits 0
    - `test -d charts/tide/templates` exits 0
    - `test -f charts/tide/templates/configmap.yaml` exits 0 (hand-authored ConfigMap template)
    - `test -d charts/tide-crds` exits 0
    - `test -f charts/tide-crds/Chart.yaml` exits 0
    - `test -d charts/tide-crds/templates` exits 0
    - `ls charts/tide-crds/templates/*.yaml | wc -l` returns at least 6 (one CRD per Kind)
    - `helm lint charts/tide` exits 0
    - `helm lint charts/tide-crds` exits 0
    - `helm template charts/tide --dry-run` exits 0 (renders without error)
  </acceptance_criteria>
  <done>Two Helm charts produced via helmify and committed. values.yaml exposes the Phase 1 tunables. ConfigMap template hand-added. Both charts lint clean. Phase 5 inherits a working helmify pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize .github/workflows/ci.yaml with all Phase 1 gates + TEST-01 timing assertion</name>
  <files>.github/workflows/ci.yaml, Makefile</files>
  <read_first>
    - .github/workflows/ci.yaml (current state — from Plans 03, 05, 09)
    - Makefile (all targets from Plans 02, 03, 05, 06, 08, 09 — to enumerate the gates)
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md "Makefile + CI wiring" (lines ~1123-1158) — original ci.yaml shape; this task extends it
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-VALIDATION.md "Per-Task Verification Map" — full requirement coverage
  </read_first>
  <action>
Read the current .github/workflows/ci.yaml. It has accumulated steps from Plans 03 (initial), 05 (verify-no-aggregates, verify-no-sqlite-dep), and 09 (verify-no-rbac-wildcards, verify-rbac-marker-discipline). This task adds the remaining missing steps:
- make verify-no-blocking (Pitfall 1 — added by Plan 06's Makefile changes)
- the TEST-01 timing assertion (`time make test` with a hard 30s budget)
- a helm chart lint job (verify Plan 11's helm output stays valid as the repo changes)

Use the Edit tool to splice in the missing steps. The final ci.yaml shape should be:

```yaml
name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true

      # Phase 1 verification gates (in dependency order — fastest first)
      - name: Verify pkg/dag imports (DAG-05)
        run: make verify-dag-imports

      - name: Verify no aggregate schedule fields (PERSIST-02 / Pitfall 4)
        run: make verify-no-aggregates

      - name: Verify no DB drivers (PERSIST-01)
        run: make verify-no-sqlite-dep

      - name: Verify no RBAC wildcards (AUTH-03 / Pitfall 15)
        run: make verify-no-rbac-wildcards

      - name: Verify RBAC marker discipline (AUTH-03)
        run: make verify-rbac-marker-discipline

      - name: Verify no blocking I/O in reconcilers (Pitfall 1)
        run: make verify-no-blocking

      - name: Run custom analyzers (POOL-03 / Pitfall 6)
        run: make tide-lint

      - name: go vet
        run: go vet ./...

      # TEST-01: full test suite under 30s
      - name: Unit + envtest suite (TEST-01 — <30s budget)
        run: |
          set -e
          START=$(date +%s)
          make test
          END=$(date +%s)
          DUR=$((END - START))
          echo "Test duration: ${DUR}s"
          if [ "$DUR" -gt 30 ]; then
            echo "TEST-01 violation: test suite exceeded 30s budget (${DUR}s)"
            exit 1
          fi

  helm-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: 'v3.14.0'
      - name: helm lint charts/tide
        run: helm lint charts/tide
      - name: helm lint charts/tide-crds
        run: helm lint charts/tide-crds
      - name: helm template (dry-run render)
        run: |
          helm template charts/tide --dry-run > /dev/null
          helm template charts/tide-crds --dry-run > /dev/null
```

Note: the timing assertion uses GitHub Actions runner's `date +%s` for portability. The `if [ "$DUR" -gt 30 ]` check forces a TEST-01 violation to fail the build.

Additionally, ensure the Makefile's `test` target uses `-short` so the leader-election test (Plan 08) doesn't run in the budgeted suite. Verify:
```
grep "^test:" Makefile
```
should show something like:
```
test: manifests generate fmt vet
	go test -short -timeout 30s ./...
```

If the test target doesn't have -short, add it (per Plan 08's expectation).

Run the final CI workflow locally (via `act` or just sequentially):
```
make verify-dag-imports
make verify-no-aggregates
make verify-no-sqlite-dep
make verify-no-rbac-wildcards
make verify-rbac-marker-discipline
make verify-no-blocking
make tide-lint
go vet ./...
time make test
helm lint charts/tide
helm lint charts/tide-crds
```

All must exit 0. The `time make test` measurement should show <30 seconds wall-clock.

If `make test` runs OVER 30s on the local machine, that's a Phase 1 blocker — TEST-01 cannot land. Profile and trim: most likely culprit is envtest setup time (the testEnv.Start() call). Look at Plan 06's suite_test.go and Plan 07's webhook_suite_test.go — both spin up envtest. Consider using `setup-envtest use 1.30.x --bin-dir bin/k8s` to pre-fetch the binaries (kubebuilder Makefile typically has this), which shaves cold-start time.
  </action>
  <verify>
    <automated>grep -q "TEST-01" .github/workflows/ci.yaml &amp;&amp; grep -q "make verify-no-blocking" .github/workflows/ci.yaml &amp;&amp; grep -q "helm-lint:" .github/workflows/ci.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yaml` exists with the `test` job and `helm-lint` job both defined
    - `grep -c "make verify-" .github/workflows/ci.yaml` returns at least 5 (all five verify targets gated)
    - `grep -q "make tide-lint" .github/workflows/ci.yaml` exits 0
    - `grep -q "TEST-01" .github/workflows/ci.yaml` exits 0 (timing assertion documented)
    - `grep -q "30" .github/workflows/ci.yaml` exits 0 (30-second budget)
    - `grep -q "helm lint" .github/workflows/ci.yaml` exits 0 (helm chart linted)
    - Makefile's test target uses `-short`: `grep -A 2 "^test:" Makefile | grep -q "\-short"` exits 0 (or the target body uses `-short` in a `go test` call)
    - All Phase 1 verification commands run sequentially and exit 0:
      `make verify-dag-imports && make verify-no-aggregates && make verify-no-sqlite-dep && make verify-no-rbac-wildcards && make verify-rbac-marker-discipline && make verify-no-blocking && make tide-lint && go vet ./...`
    - `time make test` reports <30s wall-clock locally (TEST-01)
  </acceptance_criteria>
  <done>.github/workflows/ci.yaml gates on every Phase 1 invariant: 5 verify- targets + tide-lint + go vet + envtest with timing assertion + helm lint of both charts. Every PR satisfies every Phase 1 invariant before merge.</done>
</task>

</tasks>

<verification>
- charts/tide/ and charts/tide-crds/ both exist and pass `helm lint`
- charts/tide/values.yaml exposes plannerConcurrency, executorConcurrency, maxConcurrentReconciles
- charts/tide/templates/configmap.yaml renders the runtime config
- .github/workflows/ci.yaml has TWO jobs: test (with all gates + TEST-01 timing assertion) and helm-lint
- All verification Makefile targets exit 0 locally
- `time make test` measures <30s on the local machine (TEST-01 budget honored)
</verification>

<success_criteria>
- TEST-01: full test suite runs in <30s on CI, with a hard timing assertion
- D-E1 + D-E2: two Helm charts shipped (controller + CRD subchart), helmify-driven so Phase 5 just adds templates
- Every Phase 1 verification gate (DAG-05, PERSIST-01/02, AUTH-03 source + manifests, Pitfall 1, POOL-03) is wired to CI
- helm lint passes on both charts → Phase 5's distribution work inherits a working chart
</success_criteria>

<output>
Create .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-11-SUMMARY.md with:
- Final Makefile target inventory (full list of every `make X` target Phase 1 introduces)
- Final ci.yaml step list with REQ-ID mapping per step
- Helm chart structure (file tree under charts/tide/ and charts/tide-crds/)
- Any helmify quirks encountered + workarounds (so Phase 5 doesn't re-discover them)
- Measured test suite duration on the executor's machine (TEST-01 evidence)
- Phase 5 hand-off: charts/tide/ adds dashboard Deployment + ServiceMonitor + LICENSE + docs in Phase 5; the values.yaml surface gets `dashboard.enabled` + `prometheus.serviceMonitor.enabled` flags. The CRD subchart pattern is the structurally-correct foundation for safe `helm upgrade` per REQ-DIST-01.
</output>
