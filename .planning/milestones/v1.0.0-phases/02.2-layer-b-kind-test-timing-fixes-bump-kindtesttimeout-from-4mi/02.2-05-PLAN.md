---
phase: 02.2
plan: 05
type: execute
wave: 4
gap_closure: true
depends_on: ["02.2-04"]
files_modified:
  - cmd/manager/main.go
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, manager-flags, runtime-verification, kind-integration, phase-02.2, tactical, cascade-3]

threat_model_summary:
  - "T-02.2-11 (mitigate): adding `--webhook-cert-path` flag changes manager startup behavior — mitigated by keeping default `/tmp/k8s-webhook-server/serving-certs` identical to the chart's existing `manager.args` value at `charts/tide/values.yaml:24` so production renders the same effective cert directory the binary will read. Acceptance: `grep -c 'flag.StringVar.*\"webhook-cert-path\"' cmd/manager/main.go` returns 1 AND `grep -c '\"/tmp/k8s-webhook-server/serving-certs\"' cmd/manager/main.go` returns ≥ 1."
  - "T-02.2-12 (mitigate): `webhook.Options.CertDir` wire-up could collide with controller-runtime's auto-discovery or with a different webhook CertDir set elsewhere — verified absent at plan-time. The existing webhook.Options construction at `cmd/manager/main.go:139` is `webhook.NewServer(webhook.Options{Port: 9443})` — only `Port` is set; adding `CertDir: webhookCertPath` is additive, no field collision. `grep -rnE 'CertDir|webhook-cert-path' internal/ pkg/ cmd/` returns zero matches at plan-time outside the file this plan edits."
  - "T-02.2-13 (accept): tactical scope means `--health-probe-bind-address` (predicted cascade-4 from `02.2-04-VERIFICATION.md` §\"Predicted next cascade\") remains undefined and WILL likely surface as the next CrashLoopBackOff cause once the webhook-cert-path flag is defined — accepted per user choice for tactical iteration (third confirmation). Plan 02.2-05 Task 2 (runtime verification) MUST document the next surfacing failure mode in 02.2-05-VERIFICATION.md §\"Section 5 Root-Cause Summary\" if BLOCKED so the orchestrator can route to Plan 02.2-06 (or equivalent) for the next iteration."

must_haves:
  truths:
    - "`cmd/manager/main.go` defines a `--webhook-cert-path` string flag with default `/tmp/k8s-webhook-server/serving-certs` matching `charts/tide/values.yaml:24`"
    - "The Manager's `webhook.NewServer(webhook.Options{...}).CertDir` field reads from the new `webhookCertPath` variable (additive to the existing `Port: 9443` construction)"
    - "`go build ./cmd/manager/...` exits 0 after the flag addition"
    - "`go run ./cmd/manager --help` lists `webhook-cert-path` in the usage output"
    - "`02.2-05-VERIFICATION.md` exists with frontmatter `phase: 02.2`, `plan: 05`, `task: 2`, `artifact: verification`, a populated `gate_decision: APPROVED|BLOCKED` line, and Sections 1-6 mirroring `02.2-04-VERIFICATION.md`"
    - "If `02.2-05-VERIFICATION.md` records `gate_decision: APPROVED`, then Task 3 fires and Plan 02.2-02's `depends_on` becomes `[\"02.2-05\"]` and ALL references update from `02.2-01-VERIFICATION.md` (current state, since Plans 02.2-03 Task 4 and 02.2-04 Task 3 did NOT fire) to `02.2-05-VERIFICATION.md`"
    - "If `02.2-05-VERIFICATION.md` records `gate_decision: BLOCKED`, Task 3 does NOT fire — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (its current wiring) and the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract"
  artifacts:
    - path: "cmd/manager/main.go"
      provides: "`--webhook-cert-path` flag definition + webhook.Options.CertDir wire-up"
      contains: "webhook-cert-path"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md"
      provides: "Runtime verification artifact with gate_decision frontmatter — authoritative gate for Plan 02.2-02 closeout"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-05\"] + checkpoint references 02.2-05-VERIFICATION.md (gated on Task 2 APPROVED)"
      contains: "02.2-05"
  key_links:
    - from: "cmd/manager/main.go (new flag definition)"
      to: "controller-runtime webhook.Server's CertDir option"
      via: "webhook.NewServer(webhook.Options{Port: 9443, CertDir: webhookCertPath})"
      pattern: 'CertDir:\s*webhookCertPath'
    - from: "charts/tide/values.yaml:24 (--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs arg)"
      to: "cmd/manager/main.go --webhook-cert-path default"
      via: "matching default value across chart and binary"
      pattern: '"/tmp/k8s-webhook-server/serving-certs"'
    - from: "02.2-05-VERIFICATION.md frontmatter gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 wires Plan 02.2-02's depends_on + checkpoint references (gated on APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md (depends_on field)"
      to: "this plan's verification artifact"
      via: "frontmatter depends_on: [\"02.2-05\"]"
      pattern: 'depends_on: \[.{0,5}02.2-05.{0,5}\]'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-04-VERIFICATION.md` §"Section 5 Root-Cause Summary" Option A (continued tactical iteration). The chart's `manager.args` block passes `--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs` at `charts/tide/values.yaml:24`, but `cmd/manager/main.go` does NOT define that flag — Go's `flag.Parse()` calls `Usage()` and exits 2, kubelet restarts the Pod, and `tide-controller-manager` enters `CrashLoopBackOff`. helm install --wait blocks; outer `timeout 300s` fires; Layer B specs never run.

This is the THIRD tactical iteration (after Plan 02.2-03's chart PVC fix and Plan 02.2-04's `--metrics-bind-address` flag definition). The cascade pattern is well-established: each iteration unmasks the next layer's bug; the user has now confirmed tactical iteration over comprehensive audit three times. Reasoning: even with an audit, what surfaces *after* the flag-mismatch layer is genuinely unknown, so tactical iteration produces equal-value evidence per step.

This plan fixes ONLY the immediate blocker (`--webhook-cert-path`) and then re-runs the same end-to-end runtime reproducer captured in `02.2-04-VERIFICATION.md`. The expected outcome is one of:
- **APPROVED:** the manager starts cleanly, helm install --wait completes, all 7 Layer B specs run and PASS on clean + rerun, both within ≤ 300s go test wall. Plan 02.2-02 (closeout) gets unblocked via Task 3's surgical edits.
- **BLOCKED (predicted cascade-4 per `02.2-04-VERIFICATION.md` §"Predicted next cascade"):** the manager passes the `--webhook-cert-path` parse but immediately fails on the NEXT undefined flag — most likely the kubebuilder-standard `--health-probe-bind-address` which is referenced by `helmify`-generated manifests but does NOT appear in `charts/tide/values.yaml:18-24`'s explicit `manager.args` list (so the actual surfacing failure may be different). The BLOCKED gate captures the new failure mode root cause and routes back to the planner for Plan 02.2-06 (or equivalent next iteration).

**TACTICAL SCOPE — user explicitly chose tactical iteration THREE times:**
- This plan defines `--webhook-cert-path` ONLY
- This plan does NOT define `--health-probe-bind-address`, `--leader-elect` (already defined at `cmd/manager/main.go:99`), or any other kubebuilder-standard flag
- This plan does NOT audit `charts/tide/values.yaml:18-24` against `cmd/manager/main.go`'s defined flags (Option B from `02.2-04-VERIFICATION.md` §"Fix landscape" — explicitly rejected three times)
- This plan does NOT touch `charts/tide/values.yaml:24` — the chart side is the FIXED contract; the binary catches up
- This plan does NOT touch Plans 02.2-01, 02.2-02, 02.2-03, or 02.2-04 except for Task 3's surgical edits to `02.2-02-PLAN.md`
- This plan does NOT modify the 9 pre-existing flag definitions in `cmd/manager/main.go` (the 8 from before Plan 02.2-04 + the `--metrics-bind-address` flag added by Plan 02.2-04)
- This plan does NOT modify the existing `Metrics.BindAddress: metricsAddr` wire-up (Plan 02.2-04 work preserved verbatim)

**Plan 02.2-04's pattern this plan mirrors:**
Plan 02.2-04 ran a 3-task shape: T1 source edit (define `--metrics-bind-address` + wire into `Metrics.BindAddress`), T2 runtime verification (`02.2-04-VERIFICATION.md` with `gate_decision` frontmatter), T3 surgical edit of `02.2-02-PLAN.md` gated on APPROVED. This plan mirrors that shape exactly with cascade-3's target (`--webhook-cert-path` / `webhook.Options.CertDir`) substituted in. **The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic:** Task 3 fires ONLY if Task 2's `gate_decision: APPROVED`.

**Recovery state note (load-bearing for Task 3's rewire scope):** Plan 02.2-03 Task 4 was intended to rewire `02.2-02-PLAN.md` from `02.2-01-VERIFICATION.md` → `02.2-03-VERIFICATION.md` but did NOT fire (Plan 02.2-03 Task 3 gate was BLOCKED). Plan 02.2-04 Task 3 was intended to rewire `02.2-02-PLAN.md` from `02.2-01-VERIFICATION.md` → `02.2-04-VERIFICATION.md` but ALSO did NOT fire (Plan 02.2-04 Task 2 gate was BLOCKED). As a result, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (`grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 31 as of plan-time). This plan's Task 3 lands the long-pending rewire if Task 2's gate is APPROVED, redirecting all 31 references to `02.2-05-VERIFICATION.md` in one pass.

Purpose: close the BLOCKED runtime gate from `02.2-04-VERIFICATION.md` (Plan 02.2-04's metrics flag fix worked — verified by direct evidence; cascade-3 unmasked exactly as T-02.2-10 predicted) with the cheapest possible source-side fix that aligns the binary with the chart contract for one more flag. Tactical iteration: fix, re-verify, see what surfaces next.

Output:
- `cmd/manager/main.go` with `--webhook-cert-path` flag defined (default `/tmp/k8s-webhook-server/serving-certs`) and wired into `webhook.NewServer(webhook.Options{Port: 9443, CertDir: webhookCertPath})`
- `.planning/phases/02.2-.../02.2-05-VERIFICATION.md` mirroring `02.2-04-VERIFICATION.md`'s structural template; `gate_decision: APPROVED` or `BLOCKED`
- If Task 2 APPROVED: `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-05"]` and ALL 31 `02.2-01-VERIFICATION.md` references redirected to `02.2-05-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from cmd/manager/main.go + charts/tide/values.yaml + controller-runtime v0.24 webhook.Options. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From cmd/manager/main.go (current state — POST Plan 02.2-04 commit `afa7e45`; pre-edit for this plan):

Lines 86-96 (function entry — flag variable declarations block; `metricsAddr` was added at line 90 by Plan 02.2-04):
```go
func main() {
	var configPath string
	var leaderElect bool
	var watchNamespace string
	var metricsAddr string
	// Phase 2 flags (Plan 12 wiring).
	var subagentImage string
	var credproxyImage string
	var defaultFileTouchMode string
	var rateLimitDefaultRPM int
	var rateLimitDefaultBurst int
```

Lines 98-107 (current `flag.StringVar` / `flag.BoolVar` / `flag.IntVar` definitions block; `metricsAddr` flag definition at line 101 was added by Plan 02.2-04):
```go
	flag.StringVar(&configPath, "config", "/etc/tide/config.yaml", "Path to runtime config YAML")
	flag.BoolVar(&leaderElect, "leader-elect", true, "Enable leader election (CTRL-03)")
	flag.StringVar(&watchNamespace, "watch-namespace", "", "Restrict watches to this namespace (AUTH-02). Empty = all namespaces.")
	flag.StringVar(&metricsAddr, "metrics-bind-address", ":8443", "Bind address for the metrics endpoint (controllerManager.manager.args in values.yaml)")
	// Phase 2 flags — bound from Helm values.yaml via the controller Deployment args.
	flag.StringVar(&subagentImage, "subagent-image", "", "Image ref for the subagent container (images.stubSubagent in values.yaml)")
	flag.StringVar(&credproxyImage, "credproxy-image", "", "Image ref for the tide-credproxy sidecar (images.credProxy in values.yaml)")
	flag.StringVar(&defaultFileTouchMode, "default-file-touch-mode", "warn", "Cluster-level file-touch validation mode (planAdmission.fileTouchMode in values.yaml; warn|strict)")
	flag.IntVar(&rateLimitDefaultRPM, "rate-limit-default-rpm", 60, "Default requests-per-minute rate limit (rateLimits.defaults.requestsPerMinute in values.yaml)")
	flag.IntVar(&rateLimitDefaultBurst, "rate-limit-default-burst", 10, "Default burst size for rate-limit buckets (rateLimits.defaults.burst in values.yaml)")
```

Lines 133-140 (current Manager construction; `Metrics.BindAddress: metricsAddr` wire-up at line 138 was added by Plan 02.2-04):
```go
	mgr, err := ctrl.NewManager(ctrl.GetConfigOrDie(), ctrl.Options{
		Scheme:                 scheme,
		LeaderElection:         leaderElect,
		LeaderElectionID:       "tide-controller-leader.tideproject.k8s",
		HealthProbeBindAddress: ":8081",
		Metrics:                metricsserver.Options{BindAddress: metricsAddr},
		WebhookServer:          webhook.NewServer(webhook.Options{Port: 9443}),   // <-- THIS LINE: add CertDir: webhookCertPath
	})
```

From charts/tide/values.yaml lines 17-24 (the chart's `manager.args` block — DO NOT TOUCH):
```yaml
controllerManager:
  manager:
    args:
    - --metrics-bind-address=:8443
    - --config=/etc/tide/config.yaml
    - --watch-namespace=$(WATCH_NAMESPACE)
    - --leader-elect
    - --webhook-cert-path=/tmp/k8s-webhook-server/serving-certs
```

The chart passes `--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs` at `values.yaml:24`. This plan's flag definition's default value MUST match this verbatim.

From controller-runtime v0.24 webhook.Options (`sigs.k8s.io/controller-runtime/pkg/webhook`):
```go
// Options contains the server options.
type Options struct {
	// Host is the address that the server will listen on.
	Host string
	// Port is the port number that the server will serve.
	Port int
	// CertDir is the directory that contains the server key and certificate. Defaults to
	// <temp-dir>/k8s-webhook-server/serving-certs.
	CertDir string
	// ... other fields ...
}
```

The `CertDir` field IS the canonical field for the cert directory. Adding `CertDir: webhookCertPath` to the existing struct literal at `cmd/manager/main.go:139` is additive — `Port: 9443` is preserved verbatim; only the `CertDir` field is added.

The existing import at `cmd/manager/main.go:40` already provides `webhook` (`"sigs.k8s.io/controller-runtime/pkg/webhook"`). No new imports required.

From `02.2-04-VERIFICATION.md` §"Section 5 Fix landscape" Option A:
"Define `--webhook-cert-path` flag in `cmd/manager/main.go` adjacent to the existing flag definitions; wire it into the controller-runtime Manager's `WebhookServer` config (`Options.WebhookServer.Options.CertDir` likely, or equivalent). Tradeoff: cheap per-step, but if cascade-4 (`--health-probe-bind-address`) surfaces next, the user has now spent ~90 minutes of runtime verification + 3 plan iterations on what could have been a 5-minute audit."

From CLAUDE.md "Failure semantics at wave boundaries" contract:
- Failed task → siblings in same wave continue
- Dependents in later waves never dispatch
- Plan 02.2-02 reads the gate artifact; if BLOCKED, Plan 02.2-02 stays gated

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define `--webhook-cert-path` flag in cmd/manager/main.go and wire into webhook.Options.CertDir</name>
  <files>cmd/manager/main.go</files>

  <read_first>
    - cmd/manager/main.go (full file — 292 lines; particularly the flag declaration block at lines 86-96 with `metricsAddr` at line 90 from Plan 02.2-04; the flag.StringVar/BoolVar/IntVar definitions at lines 98-107 with `--metrics-bind-address` at line 101; the Manager construction at lines 133-140 where `WebhookServer: webhook.NewServer(webhook.Options{Port: 9443})` at line 139 is the wire-up target; the import block at line 40 already provides `webhook` — `"sigs.k8s.io/controller-runtime/pkg/webhook"`)
    - charts/tide/values.yaml lines 17-24 (chart `manager.args` — confirms `--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs` is the chart-side contract; this binary must match the default verbatim)
    - .planning/phases/02.2-.../02.2-04-VERIFICATION.md §"Section 5 — Gate Decision" and §"Root-Cause Summary" Option A (line 189) — the directed approach for this fix; the cascade-3 prediction confirmed; the next predicted cascade (cascade-4: `--health-probe-bind-address`) which this plan does NOT address per tactical scope
    - .planning/phases/02.2-.../02.2-04-PLAN.md Task 1 body (lines 167-277) — the structural model for THIS plan's Task 1; the edit pattern (variable declaration + flag.StringVar + wire-up into Manager.Options) is mirrored exactly with `webhookCertPath` / `webhook.Options.CertDir` substituted for `metricsAddr` / `Metrics.BindAddress`
  </read_first>

  <action>
    Edit `cmd/manager/main.go` to add a `--webhook-cert-path` string flag and wire it into the controller-runtime webhook server's `CertDir` option. The new flag's default value MUST be `/tmp/k8s-webhook-server/serving-certs` to match the chart's existing `--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs` arg at `charts/tide/values.yaml:24` verbatim (T-02.2-11 mitigation — no production behavior change).

    **Edit A — variable declaration:** In the flag variable declarations block at lines 86-96, add a new line for the webhook cert path variable. Place it immediately after `var metricsAddr string` (line 90, added by Plan 02.2-04) and before the `// Phase 2 flags (Plan 12 wiring).` comment.

    Add at line 91 (after `var metricsAddr string`):
    ```
    	var webhookCertPath string
    ```

    Variable name: `webhookCertPath` (camelCase, mirrors `metricsAddr` naming convention).

    **Edit B — flag.StringVar definition:** In the flag definitions block at lines 98-107, add a new `flag.StringVar` line for the new variable. Place it immediately after the existing `flag.StringVar(&metricsAddr, "metrics-bind-address", ...)` line (line 101, added by Plan 02.2-04) and BEFORE the `// Phase 2 flags — bound from Helm values.yaml via the controller Deployment args.` comment at line 102 (so it groups with the kubebuilder-standard flags `--config`, `--leader-elect`, `--watch-namespace`, `--metrics-bind-address`).

    Add immediately after the existing `flag.StringVar(&metricsAddr, ...)` line:
    ```
    	flag.StringVar(&webhookCertPath, "webhook-cert-path", "/tmp/k8s-webhook-server/serving-certs", "Path to the webhook serving cert directory (controller-runtime CertDir; controllerManager.manager.args in values.yaml)")
    ```

    Default value MUST be `"/tmp/k8s-webhook-server/serving-certs"` (matching `charts/tide/values.yaml:24` verbatim). The help-text suffix `(controller-runtime CertDir; controllerManager.manager.args in values.yaml)` mirrors the help-text suffix on the other chart-bound flags.

    **Edit C — webhook.Options.CertDir wire-up:** In the `ctrl.NewManager(...)` call at lines 133-140, modify the existing `WebhookServer: webhook.NewServer(webhook.Options{Port: 9443}),` line (line 139) to ALSO include `CertDir: webhookCertPath`.

    Replace the existing line 139:
    ```go
    		WebhookServer:          webhook.NewServer(webhook.Options{Port: 9443}),
    ```
    With:
    ```go
    		WebhookServer:          webhook.NewServer(webhook.Options{Port: 9443, CertDir: webhookCertPath}),
    ```

    (Preserve the trailing comma. Preserve `Port: 9443` verbatim — adding `CertDir: webhookCertPath` is additive. Preserve the surrounding `LeaderElection`, `LeaderElectionID`, `HealthProbeBindAddress`, `Metrics` lines verbatim — those are intentionally untouched. `HealthProbeBindAddress: ":8081"` MUST remain hardcoded; this plan does NOT add a `--health-probe-bind-address` flag per T-02.2-13 acceptance.)

    **Constraints:**
    - **DO NOT touch** `charts/tide/values.yaml` — the chart contract is unchanged; the binary catches up to it.
    - **DO NOT define** `--health-probe-bind-address` (predicted cascade-4 from `02.2-04-VERIFICATION.md` — out of scope per user's third tactical-iteration confirmation).
    - **DO NOT define** any other flag not in `charts/tide/values.yaml:18-24`'s explicit `manager.args` list.
    - **DO NOT remove or modify** any existing flag definition (`--config`, `--leader-elect`, `--watch-namespace`, `--metrics-bind-address`, `--subagent-image`, `--credproxy-image`, `--default-file-touch-mode`, `--rate-limit-default-rpm`, `--rate-limit-default-burst`). All nine pre-existing definitions stay verbatim. The diff should be additive: +1 variable declaration, +1 flag.StringVar line, +1 modified struct literal (`{Port: 9443}` → `{Port: 9443, CertDir: webhookCertPath}`).
    - **DO NOT change** the `--webhook-cert-path` flag default to anything other than `/tmp/k8s-webhook-server/serving-certs` — that would shift effective production behavior from the current chart contract.
    - **DO NOT modify** the existing `Metrics: metricsserver.Options{BindAddress: metricsAddr}` wire-up (Plan 02.2-04 work preserved verbatim).
    - **DO NOT modify** the existing `HealthProbeBindAddress: ":8081"` hardcode (out of scope per T-02.2-13).
    - **DO NOT introduce a separate `webhookOptions` config struct or named variable** — preserve the inline single-line struct literal pattern.
    - **DO NOT add new imports** — `sigs.k8s.io/controller-runtime/pkg/webhook` is already imported at line 40.

    Commit with message:
    ```
    fix(02.2-05): define --webhook-cert-path flag + wire into webhook.Options.CertDir — close 02.2-04 BLOCKED gate (cascade-3)
    ```

    (Per user emphasis on commit message quality. The body should reference `02.2-04-VERIFICATION.md` §"Section 5 Fix landscape" Option A and note tactical scope — this is the third tactical iteration; `--health-probe-bind-address` remains undefined per T-02.2-13.)
  </action>

  <verify>
    <automated><![CDATA[
      # Flag definition added
      [ "$(grep -c 'flag.StringVar.*"webhook-cert-path"' cmd/manager/main.go)" -eq 1 ] && \
      # Default value matches charts/tide/values.yaml:24 verbatim
      [ "$(grep -c '"/tmp/k8s-webhook-server/serving-certs"' cmd/manager/main.go)" -ge 1 ] && \
      # Variable declaration present
      [ "$(grep -c 'var webhookCertPath string' cmd/manager/main.go)" -eq 1 ] && \
      # webhook.Options.CertDir wire-up reads from webhookCertPath (NOT hardcoded)
      [ "$(grep -c 'CertDir:.*webhookCertPath' cmd/manager/main.go)" -ge 1 ] && \
      # Port: 9443 preserved in the webhook.Options construction (regression guard)
      [ "$(grep -c 'webhook.NewServer(webhook.Options{Port: 9443' cmd/manager/main.go)" -eq 1 ] && \
      # All 9 pre-existing flag definitions preserved verbatim (regression guard)
      [ "$(grep -c 'flag.StringVar.*"config"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.BoolVar.*"leader-elect"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"watch-namespace"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"metrics-bind-address"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"subagent-image"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"credproxy-image"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"default-file-touch-mode"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.IntVar.*"rate-limit-default-rpm"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.IntVar.*"rate-limit-default-burst"' cmd/manager/main.go)" -eq 1 ] && \
      # Plan 02.2-04's Metrics.BindAddress wire-up preserved verbatim (regression guard)
      [ "$(grep -c 'BindAddress:.*metricsAddr' cmd/manager/main.go)" -eq 1 ] && \
      # HealthProbeBindAddress hardcode preserved (T-02.2-13 acceptance — out of scope)
      [ "$(grep -c 'HealthProbeBindAddress:.*":8081"' cmd/manager/main.go)" -eq 1 ] && \
      # Build passes
      go build ./cmd/manager/... && \
      # Binary accepts the flag (behavior gate; uses `go run` — no output-path assumption per plan-checker preempt)
      go run ./cmd/manager --help 2>&1 | grep -q 'webhook-cert-path' && \
      # Out-of-scope flags are NOT defined by this plan (T-02.2-13 acceptance — tactical scope honored)
      # --health-probe-bind-address is NOT defined; --leader-elect IS defined (pre-existing).
      [ "$(grep -c 'flag.StringVar.*"health-probe-bind-address"' cmd/manager/main.go)" -eq 0 ] && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Flag defined:** `grep -c 'flag.StringVar.*"webhook-cert-path"' cmd/manager/main.go` returns exactly 1
    - **Default value matches chart verbatim:** `grep -c '"/tmp/k8s-webhook-server/serving-certs"' cmd/manager/main.go` returns ≥ 1 (the new flag's default literal; this is the EXACT string from `charts/tide/values.yaml:24`)
    - **Variable declared:** `grep -c 'var webhookCertPath string' cmd/manager/main.go` returns exactly 1
    - **webhook.Options.CertDir wire-up:** `grep -c 'CertDir:.*webhookCertPath' cmd/manager/main.go` returns ≥ 1 (the webhook.Options struct reads from the new variable)
    - **Port: 9443 preserved:** `grep -c 'webhook.NewServer(webhook.Options{Port: 9443' cmd/manager/main.go` returns exactly 1 (additive edit; existing field preserved verbatim)
    - **All 9 pre-existing flag definitions preserved (regression guard):** each `grep -c` for `config`, `leader-elect`, `watch-namespace`, `metrics-bind-address`, `subagent-image`, `credproxy-image`, `default-file-touch-mode`, `rate-limit-default-rpm`, `rate-limit-default-burst` returns exactly 1
    - **Plan 02.2-04's Metrics wire-up preserved:** `grep -c 'BindAddress:.*metricsAddr' cmd/manager/main.go` returns exactly 1 (Plan 02.2-04 work NOT regressed)
    - **HealthProbeBindAddress hardcode preserved (T-02.2-13 acceptance):** `grep -c 'HealthProbeBindAddress:.*":8081"' cmd/manager/main.go` returns exactly 1 (out-of-scope per tactical iteration)
    - **Build succeeds:** `go build ./cmd/manager/...` exits 0
    - **Binary surfaces the flag (behavior gate, plan-checker preempt: use `go run`):** `go run ./cmd/manager --help 2>&1 | grep -q 'webhook-cert-path'` matches (the flag is registered in the binary's flag set; no `./bin/manager` output-path assumption)
    - **Tactical scope honored (T-02.2-13):** `grep -c 'flag.StringVar.*"health-probe-bind-address"' cmd/manager/main.go` returns 0 (out-of-scope flag NOT defined by this plan)
  </acceptance_criteria>

  <done>
    `cmd/manager/main.go` has the `--webhook-cert-path` flag defined with default `/tmp/k8s-webhook-server/serving-certs` (matching `charts/tide/values.yaml:24` verbatim) and wired into the controller-runtime webhook server's `CertDir` option via `webhook.NewServer(webhook.Options{Port: 9443, CertDir: webhookCertPath})`. The existing `Port: 9443` field is preserved; `CertDir: webhookCertPath` is additive. All 9 pre-existing flag definitions are preserved verbatim. Plan 02.2-04's `Metrics.BindAddress: metricsAddr` wire-up is untouched. `HealthProbeBindAddress: ":8081"` hardcode is preserved (T-02.2-13). `go build ./cmd/manager/...` exits 0 and `go run ./cmd/manager --help` lists the new flag. Out-of-scope flags (`--health-probe-bind-address`) are NOT defined per tactical scope. Committed with the `fix(02.2-05): define --webhook-cert-path flag + wire into webhook.Options.CertDir — close 02.2-04 BLOCKED gate (cascade-3)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-05-VERIFICATION.md (clean run + rerun + gate decision)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-04-VERIFICATION.md (full file — 235 lines; the structural template this artifact mirrors verbatim; frontmatter shape, Section 1 / Section 2 / Section 3 / Section 4 / Section 5 / Section 6 layout, per-spec outcome tables, root-cause analysis prose, threat-model-mitigations-applied list, "What This Artifact Did Not Touch" closing section; particularly note the §"Pod-State Evidence" and §"Manager Log Evidence" blocks which are LOAD-BEARING for the gate decision)
    - .planning/phases/02.2-.../02.2-03-VERIFICATION.md (the earlier verification — for the spec list and the per-spec outcome table shape; the Before/After Comparison structure)
    - .planning/phases/02.2-.../02.2-01-VERIFICATION.md (the earliest verification — for the reproducer bash shape at Sections 1.1 and 2.1)
    - cmd/manager/main.go (post-Task-1 state — verify the `--webhook-cert-path` flag definition is committed before this task runs)
    - charts/tide/values.yaml lines 17-24 (chart's `manager.args` — the contract this plan's fix closes ONE more flag from; the others remain to surface as next failures per T-02.2-13)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-04-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md` using the same structural template.

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit:
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-05-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state for the artifact:**
    ```bash
    # Post-clean diagnostic snapshot — needed for both APPROVED and BLOCKED gate analysis
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-05-clean-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-05-clean-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-05-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --tail=30 2>&1 | tee /tmp/02.2-05-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-05-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-05-clean-pvc-phase.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block from the captured log — should show 7 Pass / 0 Skip / 0 Fail if cascade-3 is the last cascade; otherwise capture the actual outcome). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-04-VERIFICATION.md` §"Section 1" Per-Spec Outcome Table.

    **Critically: capture the manager logs.** The clean-run's `tide-controller-manager` Pod state is the load-bearing evidence:
    - **APPROVED case:** Pod transitions to `Ready 1/1`; manager logs show "starting manager"; NO `flag provided but not defined: -webhook-cert-path` line; NO `flag provided but not defined: -metrics-bind-address` line (Plan 02.2-04 fix preserved)
    - **BLOCKED case:** Pod stuck in CrashLoopBackOff; manager logs show `flag provided but not defined: -<next-undefined-flag>` (most likely `--health-probe-bind-address` per cascade-4 prediction, though that flag is NOT in `charts/tide/values.yaml:18-24`'s explicit args list — so the actual next blocker may be from a different surface such as the kubebuilder-default `manager` Deployment template that helmify did NOT cover, or the helmify-rendered chart may have additional args this plan-time read did not surface)

    **Sub-step B — Idempotent Re-run:**
    ```bash
    # Pre-state: tide-test cluster + cert-manager + tide release REVISION 1 persisted from clean run.
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-05-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    **After rerun:** capture same diagnostic snapshot (`/tmp/02.2-05-rerun-*.txt`) for the rerun.

    Capture into the artifact: exit code, wall time, per-spec outcomes. Verify the helm install on rerun succeeded due to `--replace` (Plan 02.2-01's fix). Verify the cluster was NOT deleted by AfterSuite (T-02.2-02 mitigation from Plan 02.2-01; re-verified at runtime).

    **Sub-step C — Author the artifact:**
    Write `02.2-05-VERIFICATION.md` mirroring `02.2-04-VERIFICATION.md`'s structural template. Required frontmatter (note `plan: 05`, `task: 2`):
    ```yaml
    ---
    phase: 02.2
    plan: 05
    task: 2
    artifact: verification
    captured_on: <YYYY-MM-DD>
    captured_by_host: <host>
    captured_by_branch: <git branch / worktree name>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary>
    ---
    ```

    Required sections (mirror `02.2-04-VERIFICATION.md` headers verbatim):
    - **Section 1 — Clean Start Run** (reproducer + per-spec outcome table + wall-time + exit + Pod-state evidence + manager-log evidence as the load-bearing diagnostic)
    - **Section 2 — Idempotent Re-run** (reproducer + per-spec outcome table + wall-time + exit + rerun-specific evidence — DO NOT skip the rerun the way Plan 02.2-04's verification did during recovery; both runs are required for an APPROVED gate)
    - **Section 3 — Before/After Comparison** (table comparing Plan 02.2-03, 02.2-04, and 02.2-05 outcomes; particularly: did the webhook-cert-path flag close the BLOCKED gate, and what's the NEW failure mode if BLOCKED again)
    - **Section 4 — Wall-time Budget Check** (same table shape as `02.2-04-VERIFICATION.md` §4; both runs must show `go test ≤ 300s` for APPROVED)
    - **Section 5 — Gate Decision** (the APPROVED-requires-ALL table + Required Next Step + Root-Cause Summary if BLOCKED + Fix landscape if BLOCKED — predicted cascade-4 is `--health-probe-bind-address` per `02.2-04-VERIFICATION.md` §"Predicted next cascade")
    - **Section 6 — Threat-Model Mitigations Applied** (T-02.2-11 / T-02.2-12 / T-02.2-13 each with evidence + carry-forward T-02.2-01 through T-02.2-10 noted as preserved)
    - **"What This Artifact Did Not Touch"** closing section
    - **"Closing Prose"** with one-paragraph summary

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/` (Task 1's source edit is already committed before this task runs). Task 3 edits Plan 02.2-02 (a planning artifact, not source) — and only fires if `gate_decision: APPROVED`. Document this explicitly in the artifact's "What This Artifact Did Not Touch" closing section.

    **If gate_decision = BLOCKED (predicted cascade-4 most likely):** Write a Root-Cause Summary section with numbered analysis points (mirror `02.2-04-VERIFICATION.md` §"Root-Cause Summary"). Surface a fix landscape:
    - **Option A** (one-more-tactical-iteration): define ONLY the next undefined flag (likely `--health-probe-bind-address`); same Plan-02.2-06 shape; predict the next-next failure.
    - **Option B** (comprehensive audit): audit ALL `charts/tide/values.yaml:18-24` entries + helmify-rendered Deployment template against `cmd/manager/main.go` and define every missing flag in one plan (closes the cascade).
    - **Option C** (chart side): trim non-essential args from `charts/tide/values.yaml` if the missing flags are not needed by TIDE's actual code paths.
    - **Option D** (kubebuilder-standard scaffold): refactor `cmd/manager/main.go` to use the kubebuilder-default scaffold with `--metrics-bind-address`, `--webhook-cert-path`, `--health-probe-bind-address`, `--leader-elect`, and `--zap-*` flags as standard.

    The Root-Cause Summary MUST name the specific surfacing failure mode (the exact `flag provided but not defined: -<name>` line from the manager logs OR a different failure shape if cascade-4 is not flag-mismatch) so the orchestrator can route to Plan 02.2-06 (or equivalent) with the next target identified.

    Task 3 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring — Plans 02.2-03 Task 4 and 02.2-04 Task 3 also did NOT fire).

    **If gate_decision = APPROVED:** Skip the Root-Cause Summary section. Note in the closing prose that Plan 02.2-02 (closeout) is unblocked once Task 3 wires its checkpoint to read this artifact, and that the cascade chain has terminated successfully after 3 tactical iterations (02.2-03 chart PVC, 02.2-04 metrics flag, 02.2-05 webhook-cert-path flag).

    Commit with one of:
    ```
    docs(02.2-05): runtime verification — APPROVED gate (webhook-cert-path flag mismatch resolved; 7/7 Layer B specs PASS clean + rerun)
    ```
    OR
    ```
    docs(02.2-05): runtime verification — BLOCKED gate (webhook-cert-path flag mismatch resolved; cascade-4 surfaced: <next-flag-or-failure>)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists and has the required frontmatter shape
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md ] && \
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 05' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^task: 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^artifact: verification' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -eq 1 ] && \
      # Gate decision is one of APPROVED or BLOCKED
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md && \
      # Required structural sections exist
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 4' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times (both runs)
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md && \
      grep -E '^rerun_exit: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md && \
      # Threat-model mitigations applied section exists
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-11/12/13 all referenced
      [ "$(grep -c 'T-02.2-11' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-12' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-13' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      # Manager log evidence captured (the load-bearing diagnostic for APPROVED vs BLOCKED)
      [ "$(grep -c 'webhook-cert-path\|flag provided but not defined\|starting manager' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      # Pod-state evidence captured (load-bearing per plan-checker tightening)
      [ "$(grep -c 'CrashLoopBackOff\|Running\|RESTARTS\|kubectl.*get pod' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md)" -ge 1 ] && \
      # No production source modified by this task (Task 1's source edit is already committed)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-05-VERIFICATION.md' | grep -v '^$' && echo "FAIL: non-artifact files modified beyond Task 1's pre-committed edit" && exit 1 || \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-05-VERIFICATION.md ]` returns true
    - **Frontmatter has required fields:** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 05' <artifact>` returns 1 AND `grep -c '^task: 2' <artifact>` returns 1 AND `grep -c '^artifact: verification' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: -?[0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' <artifact>` matches (rerun_exit allows -1 to denote skipped per `02.2-04-VERIFICATION.md` convention, but APPROVED case requires both = 0)
    - **All six structural sections present:** each `grep -c '^## Section [1-6]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all three new threats referenced:** `grep -c 'Threat-Model Mitigations Applied' <artifact>` ≥ 1 AND `grep -c 'T-02.2-11' <artifact>` ≥ 1 AND `grep -c 'T-02.2-12' <artifact>` ≥ 1 AND `grep -c 'T-02.2-13' <artifact>` ≥ 1
    - **Manager-log evidence captured (load-bearing for gate decision):** the artifact includes a verbatim excerpt of `kubectl logs deploy/tide-controller-manager` from the clean run — either the "starting manager" success message (APPROVED case) or the `flag provided but not defined: -<next-flag>` failure message (BLOCKED case)
    - **Pod-state evidence captured (load-bearing per plan-checker tightening):** the artifact includes verbatim output of `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` showing either `STATUS=Running RESTARTS ≤ 1` (APPROVED) or `STATUS=CrashLoopBackOff RESTARTS > 1` (BLOCKED)
    - **Behavior gate (APPROVED case — ALL must hold for APPROVED):**
      - (a) Clean run captured `EXIT=0` AND 7 Layer B specs marked PASS in the per-spec table
      - (b) Rerun captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (c) **Wall-time budget (LOAD-BEARING):** `clean_run_wall_seconds ≤ 300` AND `rerun_wall_seconds ≤ 300` (the budget contract from Phase 02.2-01 fix #5 — see `02.2-04-VERIFICATION.md` §Section 4) AND **no `timeout 300s` exit-124 line appears in either captured log on the go test step**
      - (d) Manager logs show "starting manager" (or analogous controller-runtime startup log) AND no `flag provided but not defined: -webhook-cert-path` line AND no `flag provided but not defined: -metrics-bind-address` line (Plan 02.2-04 fix preserved) in the captured manager logs
      - (e) **Pod-state (LOAD-BEARING):** `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` shows `STATUS=Running` AND restart count ≤ 1 (proves the binary actually started cleanly — without this an APPROVED could mask a subsequent stuck-Pending or CrashLoopBackOff on a different flag)
      - (f) **PVC preservation (Plan 02.2-03 fix not regressed):** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO`
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, a `Root-Cause Summary` section MUST exist with numbered analysis points mirroring `02.2-04-VERIFICATION.md` §"Root-Cause Summary" — particularly: (a) confirmation that Plan 02.2-05 Task 1's `--webhook-cert-path` flag definition IS WORKING (no `-webhook-cert-path` error in manager logs); (b) which next-undefined-flag-or-failure surfaced; (c) why this was predictable (cascade-4 per `02.2-04-VERIFICATION.md`); (d) fix-landscape options for the next iteration (A=tactical Plan 02.2-06, B=audit, C=chart trim, D=kubebuilder-standard refactor)
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows only the new `02.2-05-VERIFICATION.md` artifact (Task 1's `cmd/manager/main.go` edit must be committed BEFORE this task runs; Task 3 has not fired yet)
  </acceptance_criteria>

  <done>
    `02.2-05-VERIFICATION.md` exists at the correct path with the structural template mirroring `02.2-04-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, and `gate_decision: APPROVED` or `gate_decision: BLOCKED`. Sections 1-6 captured with per-spec outcome tables, Pod-state evidence, manager-log evidence (load-bearing for APPROVED vs BLOCKED), before/after comparison vs `02.2-04-VERIFICATION.md`, wall-time budget check, and threat-model mitigations (T-02.2-11/12/13 + carry-forward T-02.2-01 through T-02.2-10). If APPROVED, all five APPROVED-gate criteria hold: both runs exit 0 with 7/7 specs PASS, both ≤ 300s, no flag-not-defined errors for `-webhook-cert-path` or `-metrics-bind-address`, Pod STATUS=Running with restart ≤ 1, PVC Bound RWO. Task 3 fires to wire Plan 02.2-02's checkpoint. If BLOCKED, Task 3 does NOT fire and the failure routes to a planner re-evaluation per the wave-failure-semantics contract. Committed with a `docs(02.2-05): runtime verification — APPROVED gate ...` or `docs(02.2-05): runtime verification — BLOCKED gate (cascade-4 surfaced: <next-flag-or-failure>)` message that explains the outcome.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-05-VERIFICATION.md + bump depends_on (gated on Task 2 APPROVED gate)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file — 382 lines; frontmatter at lines 1-40 — particularly line 6 `depends_on: ["02.2-01"]`, lines 31-39 `key_links` block, lines 22-26 `must_haves.truths` block; `<context>` block at lines 63-68; Task 1 body at lines 72-136 — `<read_first>` at lines 77-79, `<what-built>` at lines 81-83, `<how-to-verify>` at lines 85-101, `<verify>` automated block at lines 117-123, `<acceptance_criteria>` at lines 125-129, `<done>` at lines 131-135 — all of which currently reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4 and 02.2-04 Task 3 did NOT fire)
    - .planning/phases/02.2-.../02.2-05-VERIFICATION.md (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the precondition for this Task 3 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - .planning/phases/02.2-.../02.2-04-PLAN.md Task 3 body (lines 443-540) — the structural model for this task's edits; mirror its surgical-edit pattern with `02.2-04` substituted for `02.2-05`
  </read_first>

  <action>
    **Gate precondition:** Task 3 fires ONLY IF Task 2's `02.2-05-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED`, halt Task 3 — Plan 02.2-02 stays unmodified (it will continue to reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4 and 02.2-04 Task 3 also did NOT fire); the BLOCKED failure routes to a planner re-evaluation.

    Verify the precondition by running:
    ```bash
    grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md
    ```
    If this does NOT match, halt Task 3 and surface the BLOCKED gate. Exit with a clear message: "Task 3 halted: 02.2-05-VERIFICATION.md gate_decision is not APPROVED — routing back to planner for next iteration (cascade-4)."

    If APPROVED, make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-05-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering. There are 31 references to `02.2-01-VERIFICATION.md` in `02.2-02-PLAN.md` (verified at plan-time by `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returning 31); all 31 are redirected to `02.2-05-VERIFICATION.md` in one pass.

    **NOTE on recovery state:** Plan 02.2-03 Task 4 was intended to do the same surgical edit (`02.2-01-VERIFICATION.md` → `02.2-03-VERIFICATION.md`) but never fired because its Task 3 gate was BLOCKED. Plan 02.2-04 Task 3 was intended to do the same surgical edit (`02.2-01-VERIFICATION.md` → `02.2-04-VERIFICATION.md`) but never fired because its Task 2 gate was BLOCKED. As a consequence, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today. This task's edits land the long-pending rewire, redirecting to `02.2-05-VERIFICATION.md`.

    **Edit A — frontmatter `depends_on` (line 6 of 02.2-02-PLAN.md):** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-05"]`. This is a single one-line change. The new dependency captures the gate-artifact dependency directly (Plan 02.2-05 produces the gate; Plan 02.2-02 reads it). The transitive dependency on Plans 02.2-01, 02.2-03, and 02.2-04 (which Plan 02.2-05 depends on transitively) is implicit in execute-phase's wave-ordering logic.

    **Edit B — frontmatter `must_haves.truths` block (lines 22-26 of 02.2-02-PLAN.md):** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-05-VERIFICATION.md`. Specifically the bullet currently reads "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → change to "Closeout fires ONLY when 02.2-05-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit C — frontmatter `key_links` block (lines 31-39 of 02.2-02-PLAN.md):** Update the entry that references `02.2-01-VERIFICATION.md` to reference `02.2-05-VERIFICATION.md`. Concretely: change the `from:` line's path from `02.2-01-VERIFICATION.md` to `02.2-05-VERIFICATION.md` and keep the rest of the entry unchanged.

    **Edit D — `<context>` block (line 66 of 02.2-02-PLAN.md):** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-05-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>` (lines 77-79 of 02.2-02-PLAN.md):** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-05-VERIFICATION.md (the gate artifact produced by Plan 05 Task 2 — Phase 02.2-05 closed the BLOCKED runtime gate from 02.2-04-VERIFICATION.md by defining the --webhook-cert-path flag in cmd/manager/main.go and wiring it into webhook.Options.CertDir)`. Update the analog Phase 02.1 reference if present.

    **Edit F — Task 1 `<what-built>` (lines 81-83 of 02.2-02-PLAN.md):** Update the prose to reference Plan 02.2-05's deliverable (webhook-cert-path flag definition + runtime re-verification) in addition to Plans 02.2-01, 02.2-03, and 02.2-04's deliverables. Specifically append: "Plan 02.2-05 then defined the `--webhook-cert-path` flag in `cmd/manager/main.go` and wired it into `webhook.Options.CertDir`, closing the cascade-3 mismatch and letting the controller manager Pod reach Ready so helm install --wait completes and the Layer B Ginkgo specs can execute end-to-end."

    **Edit G — Task 1 `<how-to-verify>` (lines 85-101 of 02.2-02-PLAN.md):** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-05-VERIFICATION.md`. There are multiple occurrences in the numbered steps; update each.

    **Edit H — Task 1 `<verify>` automated block (lines 117-123 of 02.2-02-PLAN.md):** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-05-VERIFICATION.md`. Two grep paths need bumping.

    **Edit I — Task 1 `<acceptance_criteria>` (lines 125-129 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-05-VERIFICATION.md`.

    **Edit J — Task 1 `<done>` (lines 131-135 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-05-VERIFICATION.md` if any are present.

    **(Edits to ROADMAP/STATE content NOT performed by this task — Plan 02.2-02 reads live ROADMAP/STATE at execution time via its own `<read_first>` and `<action>` blocks and will derive the current 5-plan / 4-done structure. Prescriptive content edits from THIS plan would (a) write a snapshot that is stale by the time Plan 02.2-02 runs, and (b) leak Plan 02.2-05's responsibilities into Plan 02.2-02's body. Plan 02.2-02 is responsible for its own ROADMAP/STATE shape decisions at execution time.)**

    **Constraints:**
    - Do NOT edit Plans 02.2-01, 02.2-03, 02.2-04, or 02.2-05 (this plan) — only `02.2-02-PLAN.md` is touched.
    - Do NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `Makefile`, `.github/`. This task is documentation-only.
    - Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — those are owned by Plan 02.2-02's tasks and by the orchestrator.
    - The diff against `02.2-02-PLAN.md` must be minimal — only the 31 references to `02.2-01-VERIFICATION.md` are changed to `02.2-05-VERIFICATION.md`, plus the one `depends_on` value change. No new tasks added, no existing tasks removed, no structural reorganization, no plan-list/decisions edits.

    Commit with message:
    ```
    docs(02.2-05): wire Plan 02.2-02 checkpoint to 02.2-05-VERIFICATION.md (depends_on bump 02.2-01 → 02.2-05)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Gate precondition: 02.2-05-VERIFICATION.md gate_decision is APPROVED (else this task should have been halted)
      grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md && \
      # Edit A: depends_on bumped to 02.2-05
      grep -E 'depends_on: \[.{0,5}02.2-05.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Old depends_on removed (must not contain 02.2-01, 02.2-03, or 02.2-04 as the active depends_on value)
      [ "$(grep -cE '^depends_on: \[.{0,5}02.2-0[134].{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-05-VERIFICATION.md is now the gate reference (count must be ≥ 25 — all 31 original 02.2-01-VERIFICATION.md references rewired with a buffer for any references that might consolidate)
      [ "$(grep -c '02.2-05-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 25 ] && \
      # 02.2-01-VERIFICATION.md references purged from 02.2-02-PLAN.md
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-03-VERIFICATION.md and 02.2-04-VERIFICATION.md references purged (those Task 4/3 didn't fire, so they were never there; assert anyway)
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-04-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Task 1 checkpoint grep gate uses the new path
      grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-05-VERIFICATION.md" .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Confirm no other plans were modified by this task (regression guard)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-02-PLAN.md' | grep -v '^$' && echo "FAIL: non-target plan files modified" && exit 1 || \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Gate precondition met:** `grep -E '^gate_decision: APPROVED$' 02.2-05-VERIFICATION.md` matches (Task 3 ran ONLY because Task 2's gate was APPROVED; otherwise Task 3 was halted before edits landed)
    - **depends_on bumped (Edit A):** `grep -E 'depends_on: \[.{0,5}02.2-05.{0,5}\]' 02.2-02-PLAN.md` matches AND `grep -cE '^depends_on: \[.{0,5}02.2-0[134].{0,5}\]' 02.2-02-PLAN.md` returns 0 (old values 02.2-01, 02.2-03, 02.2-04 all purged from the depends_on field)
    - **New gate-artifact reference count (Edits B-J):** `grep -c '02.2-05-VERIFICATION.md' 02.2-02-PLAN.md` returns ≥ 25 (Plan 02.2-02 had 31 `02.2-01-VERIFICATION.md` references at plan-time; the threshold accounts for the rewire while allowing for minor reference consolidation)
    - **Old gate-artifact references purged:** `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-03-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-04-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 (no stale references to any prior gate artifact)
    - **Task 1 `<verify>` automated grep targets the new path:** the regex `grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-05-VERIFICATION.md"` matches inside `02.2-02-PLAN.md`
    - **No other plans or source modified (regression guard):** `git status --porcelain` shows ONLY `02.2-02-PLAN.md` modified in this commit; no edits to `02.2-01-PLAN.md`, `02.2-03-PLAN.md`, `02.2-04-PLAN.md`, `02.2-05-PLAN.md` (this plan), or any production source
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has been surgically edited to redirect its checkpoint from `02.2-01-VERIFICATION.md` to `02.2-05-VERIFICATION.md`, with `depends_on` bumped from `["02.2-01"]` to `["02.2-05"]`. All 31 (or near-31, allowing minor consolidation) prior `02.2-01-VERIFICATION.md` references are now `02.2-05-VERIFICATION.md`. No stale `02.2-03-VERIFICATION.md` or `02.2-04-VERIFICATION.md` references exist. No other plans or production source are modified. Committed with the `docs(02.2-05): wire Plan 02.2-02 checkpoint to 02.2-05-VERIFICATION.md (depends_on bump 02.2-01 → 02.2-05)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `charts/tide/values.yaml:24` (chart contract) → `cmd/manager/main.go` flag set (binary contract) | The chart passes `--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs` as an arg to the manager binary; this plan adds the matching flag definition so the contract is satisfied at runtime. |
| `webhook.Options.CertDir` field in controller-runtime v0.24 (canonical API) → cmd/manager/main.go Manager construction | The CertDir field is the canonical place for the cert directory in controller-runtime v0.24; adding it to the existing struct literal is the idiomatic wire-up pattern. |
| Task 1 (source edit) → Task 2 (runtime verification) | Task 2 is run after Task 1's source edit is committed; the runtime reproducer exercises the new flag definition end-to-end. |
| Task 2 (gate artifact) → Task 3 (Plan 02.2-02 wiring) | Task 3 reads Task 2's `gate_decision:` frontmatter. APPROVED → Task 3 fires; BLOCKED → Task 3 halts and the BLOCKED failure routes to a planner re-evaluation. |
| Plan 02.2-05 Task 3 edits → Plan 02.2-02 checkpoint target | Surgical edit only — `depends_on` and verification-artifact references in `02.2-02-PLAN.md` are bumped; no other planning artifacts touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-11 | Tampering | Adding `--webhook-cert-path` flag changes manager startup behavior — the new default is read into `webhook.Options.CertDir` for the webhook serving cert directory | mitigate | Keep default `/tmp/k8s-webhook-server/serving-certs` identical to the chart's existing arg value at `charts/tide/values.yaml:24` verbatim. Production deployments rendering the chart without `--set` overrides continue to pass the same `--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs` arg to the binary, which now matches a flag definition with the same default. **The chart was already passing this arg as an unknown flag (causing CrashLoopBackOff); aligning the binary makes the chart's contract actually take effect** (this is a behavior CORRECTION, not a regression). **Acceptance gate:** `grep -c 'flag.StringVar.*"webhook-cert-path"' cmd/manager/main.go` returns 1 AND `grep -c '"/tmp/k8s-webhook-server/serving-certs"' cmd/manager/main.go` returns ≥ 1 AND `grep -c 'CertDir:.*webhookCertPath' cmd/manager/main.go` returns ≥ 1. |
| T-02.2-12 | Tampering | `webhook.Options.CertDir` wire-up could collide with controller-runtime's auto-discovery or with a different webhook CertDir set elsewhere | mitigate | The existing webhook.Options construction at `cmd/manager/main.go:139` is `webhook.NewServer(webhook.Options{Port: 9443})` — only `Port` is set; adding `CertDir: webhookCertPath` is additive, no field collision. controller-runtime v0.24's `webhook.Options.CertDir` defaults to `<temp-dir>/k8s-webhook-server/serving-certs` when unset (per the doc-comment in the v0.24 Options type), which happens to be the same path the chart was passing — so production deployments were getting the right cert path by coincidence; this plan makes that coincidence load-bearing. `grep -rnE 'CertDir|webhook-cert-path' internal/ pkg/ cmd/` returns zero matches at plan-time outside the file this plan edits. **Acceptance gate:** the verify block confirms `CertDir:.*webhookCertPath` matches in `cmd/manager/main.go`; no other file has a CertDir-related identifier. |
| T-02.2-13 | Denial of Service | Tactical scope means `--health-probe-bind-address` (predicted cascade-4 from `02.2-04-VERIFICATION.md` §"Predicted next cascade") remains undefined and WILL likely re-surface as the next CrashLoopBackOff cause once the webhook-cert-path flag is defined | accept | Accepted per user choice for tactical iteration (THIRD confirmation — Plans 02.2-03, 02.2-04 also accepted similar tactical scope; this is the third compounding acceptance with full evidence of the cascade pattern from `02.2-04-VERIFICATION.md`). The acceptance is explicit (documented in objective, must_haves, action sections of all three tasks). Plan 02.2-05 Task 2 (runtime verification) MUST document the next surfacing failure mode in `02.2-05-VERIFICATION.md` §"Section 5 Root-Cause Summary" so the orchestrator can route to Plan 02.2-06 (or equivalent) for the next iteration. **Acceptance gate:** if Task 2 records `gate_decision: BLOCKED`, the `Root-Cause Summary` section MUST name which next-undefined-flag-or-failure surfaced and predict whether further tactical iterations or a comprehensive audit (Option B / C / D from this artifact's §"Fix landscape") is the appropriate path forward. Note: `--health-probe-bind-address` is NOT in `charts/tide/values.yaml:18-24`'s explicit `manager.args` list — it would only surface if a helmify-rendered Deployment template injected it; the actual cascade-4 failure may be something different. |

</threat_model>

<verification>
After all three tasks complete (or after Task 2 if its gate is BLOCKED):

**Source gates (Task 1):**
- `cmd/manager/main.go` defines `--webhook-cert-path` with default `/tmp/k8s-webhook-server/serving-certs` (matches `charts/tide/values.yaml:24` verbatim)
- The Manager's `WebhookServer` construction uses `webhook.NewServer(webhook.Options{Port: 9443, CertDir: webhookCertPath})` (existing `Port: 9443` preserved; `CertDir` field additive)
- Plan 02.2-04's `Metrics.BindAddress: metricsAddr` wire-up preserved (not regressed)
- `HealthProbeBindAddress: ":8081"` hardcode preserved (T-02.2-13 — out of scope)
- All 9 pre-existing flag definitions preserved verbatim
- `go build ./cmd/manager/...` exits 0
- `go run ./cmd/manager --help` lists the new flag

**Runtime gates (Task 2):**
- `02.2-05-VERIFICATION.md` exists with all required frontmatter fields populated
- `gate_decision` is one of APPROVED or BLOCKED
- The artifact's Sections 1-6 mirror `02.2-04-VERIFICATION.md` structural template
- Manager-log evidence (load-bearing for gate decision) is captured verbatim in the artifact
- Pod-state evidence (load-bearing per plan-checker tightening) is captured: STATUS=Running or STATUS=CrashLoopBackOff with restart count
- Wall-time evidence (load-bearing per plan-checker tightening) is captured: both runs ≤ 300s for APPROVED
- All three new threats (T-02.2-11/12/13) have evidence in §6

**Wiring gates (Task 3 — gated on Task 2 APPROVED):**
- `02.2-02-PLAN.md` has `depends_on: ["02.2-05"]` and ALL 31 (or near-31) references updated from `02.2-01-VERIFICATION.md` to `02.2-05-VERIFICATION.md`
- Stale `02.2-01-VERIFICATION.md`, `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md` references purged from `02.2-02-PLAN.md`
- No other plans modified

**Structural gates (T-02.2-11/12/13 mitigation):**
- Out-of-scope flags (`--health-probe-bind-address`) NOT defined in `cmd/manager/main.go`
- Chart's `charts/tide/values.yaml:24` line NOT touched
- Plans 02.2-01, 02.2-02, 02.2-03, 02.2-04 NOT modified (except Plan 02.2-02 in Task 3)
- `.planning/STATE.md` and `.planning/ROADMAP.md` NOT touched (orchestrator owns)
</verification>

<success_criteria>
- Task 1 source edit landed: `cmd/manager/main.go` defines `--webhook-cert-path` flag with default `/tmp/k8s-webhook-server/serving-certs` wired into `webhook.Options.CertDir`; existing `Port: 9443` preserved; `go build` exits 0; `go run ./cmd/manager --help` lists the new flag
- Task 2 runtime verification artifact authored at `.planning/phases/02.2-.../02.2-05-VERIFICATION.md` with `gate_decision: APPROVED` or `gate_decision: BLOCKED` and all required structural sections including load-bearing wall-time AND Pod-state AND manager-log evidence
- Task 3 (gated on Task 2 APPROVED) wired Plan 02.2-02's checkpoint to `02.2-05-VERIFICATION.md` with `depends_on: ["02.2-05"]` and all 31 stale `02.2-01-VERIFICATION.md` references purged
- If Task 2 returns BLOCKED: Task 3 does NOT fire; Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring); the orchestrator routes to a planner re-evaluation for Plan 02.2-06 (or equivalent) per the wave-failure-semantics contract and the tactical-iteration acceptance recorded in T-02.2-13
- T-02.2-11/12/13 each have concrete evidence in the verification artifact and the source-edit acceptance gates
- Tactical scope honored: ONLY `--webhook-cert-path` defined; `--health-probe-bind-address` NOT defined; `charts/tide/values.yaml` NOT touched; Plans 02.2-01, 02.2-03, 02.2-04 NOT modified; Plan 02.2-04's `Metrics.BindAddress` wire-up preserved verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-SUMMARY.md` documenting:
- The source edit applied (Task 1): the `--webhook-cert-path` flag definition + `webhook.Options.CertDir` wire-up + the chart-matching default value rationale + Plan 02.2-04 work preserved
- The runtime verification outcome (Task 2): gate decision (APPROVED or BLOCKED), per-spec outcomes, wall times, exit codes, Pod-state evidence, manager-log evidence
- The Plan 02.2-02 wiring update (Task 3, if APPROVED): which references were updated (count from 31 references), depends_on bump, no plan-list/decisions edits per plan-checker preempt
- Any inconsistencies surfaced (e.g., expected next failure mode if BLOCKED; the predicted cascade-4 surfacing per T-02.2-13 — possibly `--health-probe-bind-address` or a different failure shape)
- The Phase 02.2 close-out chain progression: 4 source fixes (02.2-01) + 1 chart fix (02.2-03) + 1 binary flag fix (02.2-04) + 1 more binary flag fix (this plan) landed → runtime verified once (APPROVED) or routed back (BLOCKED) for next iteration
- Threat-model mitigations applied (T-02.2-11 / T-02.2-12 / T-02.2-13) with concrete evidence
</output>
