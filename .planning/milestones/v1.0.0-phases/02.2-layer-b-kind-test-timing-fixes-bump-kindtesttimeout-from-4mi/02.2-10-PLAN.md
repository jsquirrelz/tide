---
phase: "02.2"
plan: "10"
type: execute
wave: 9
gap_closure: true
depends_on: ["02.2-09"]
files_modified:
  - cmd/manager/main.go
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, production-wiring, dispatcher, podjob-backend, runtime-verification, kind-integration, phase-02.2, tactical, cascade-8, chain-terminator-candidate-v3, breaks-option-b-test-only-contract]

threat_model_summary:
  - "T-02.2-28 (mitigate): the `*podjob.PodJobBackend` instantiation could be wrong (missing field, wrong field type, nil dependency). PodJobBackend has 7 fields (Client, Scheme, SubagentImage, CredproxyImage, SigningKey, EnvReader, PVCName); all must be populated. **Mitigation:** Task 1's `<action>` specifies the verbatim 7-field struct literal sourced from the debug session at `.planning/debug/credproxy-backoff-suppression.md` §\"Fix landscape (corrected) — Option δ\". Acceptance: `grep -c 'Client:[[:space:]]*mgr.GetClient' cmd/manager/main.go` ≥ 1 in the dispatcher region; `grep -c 'Scheme:[[:space:]]*mgr.GetScheme' cmd/manager/main.go` ≥ 1 in the dispatcher region; all 7 fields visible via `sed -n '188,200p' cmd/manager/main.go` after Task 1; `gofmt -d cmd/manager/main.go` returns empty; `go build ./cmd/manager/...` exits 0; `go vet ./cmd/manager/...` exits 0."
  - "T-02.2-29 (mitigate): once Dispatcher is non-nil, `TaskReconciler.reconcileDispatch` runs in production for ALL Tasks (not just test fixtures). The dispatch path was DESIGNED to run from Phase 2 Plan 09 wiring; the gating-off-since-Phase-2-landed condition is itself the production-wiring-gap that Plan 02.2-10 closes. If the dispatch path has latent bugs (e.g., uses uninitialized state, panics on unexpected Task shapes, leaks Jobs on Pod failures), the test run will surface them as spec failures with concrete classification — better to surface them in the Layer B kind-test loop than in real cluster deployments. **Mitigation:** Plan 2 dispatch logic was already implemented in Phase 02's Plan 09 (per the comment at `cmd/manager/main.go:246` `// Phase 2 fields (Plan 12 wiring).`) and unit-tested via envtest. The production wiring gap is the ONLY thing preventing it from running. Acceptance: runtime verification (Task 2) re-runs the full kind integration suite which exercises wave/failure/caps/output specs against the now-live dispatch path; any latent bugs surface as Ginkgo spec failures with concrete classification under T-02.2-16 taxonomy (flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other) in §Section 5."
  - "T-02.2-30 (mitigate): the signing-key wiring could be broken (e.g., key not loaded from K8s Secret correctly, signing fails on envelope creation, key length insufficient). **Mitigation:** `signingKey, err := decodeSigningKeyFromEnv()` at `cmd/manager/main.go:174` already loads the key from env; non-nil result is checked at lines 174-178 with `os.Exit(1)` on failure; the `decodeSigningKeyFromEnv` body (lines 74-84) asserts length ≥ 32. Acceptance: Task 2's clean run reaches `tide-controller-manager` Pod `Running` (Plan 02.2-05's webhook-cert-path fix + manager startup already validates signing-key load); if signing-key load fails, the Pod CrashLoopBackOffs and the verification gate's `Pod Running ≤ 1 restart` condition trips, causing BLOCKED with cascade-9 classification (`production-wiring-gap` family, NOT `flag-mismatch` since the chart already passes `tide-signing-key` Secret via `envFrom` per the Helm template)."
  - "T-02.2-31 (accept): cascade-9 (`plan.Status.ValidationState` never set) is acknowledged but DEFERRED. The Plan admission webhook (`internal/webhook/v1alpha1/plan_webhook.go:117-199`) validates and emits warnings but does NOT update `.Status.ValidationState`. `PlanReconciler.reconcileWaveMaterialization` short-circuits at `plan_controller.go:147` if `ValidationState != \"Validated\"`. **However:** TaskReconciler.reconcileDispatch does NOT depend on Wave existence or ValidationState — it only requires `r.Dispatcher != nil` + the Task itself. So HARN-03's pod-wait (the gating spec in Plan 02.2-09's BLOCKED gate) succeeds via Plan 02.2-10's Option δ even with the ValidationState gap intact. **Accepted with the requirement:** the cascade-9 follow-up is documented in `<context>` and surfaced as a recommended next debug session AFTER Plan 02.2-10 lands the chain terminator. Plan 02.2-10's scope is cascade-8 closure ONLY; resisting scope expansion is itself a load-bearing constraint per `.continue-here.md` anti-pattern enforcement."

must_haves:
  truths:
    - "`cmd/manager/main.go` constructs a `dispatcher := &podjob.PodJobBackend{...}` with all 7 fields populated from in-scope variables: `Client: mgr.GetClient()`, `Scheme: mgr.GetScheme()`, `SubagentImage: subagentImage`, `CredproxyImage: credproxyImage`, `SigningKey: signingKey`, `EnvReader: envReader`, `PVCName: \"tide-projects\"`"
    - "The dispatcher construction line is inserted IMMEDIATELY AFTER the `envReader := &podjob.FilesystemEnvelopeReader{...}` line (currently line 188), grouped with the other Phase 2 component wiring (Plan 12) at section 6 of main()"
    - "The PlanReconciler struct literal (currently lines 220-228) carries a `Dispatcher: dispatcher,` field as its LAST field (after `WatchNamespace: watchNamespace,`)"
    - "The TaskReconciler struct literal (currently lines 240-256) carries a `Dispatcher: dispatcher,` field as its LAST field (after `EnvReader: envReader,`)"
    - "`grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returns exactly 1 AFTER Task 1 (was 0 pre-edit)"
    - "`grep -c '^[[:space:]]*Dispatcher: dispatcher,' cmd/manager/main.go` returns exactly 2 AFTER Task 1 — one in each reconciler literal (was 0 pre-edit)"
    - "`grep -c 'PVCName:.*\"tide-projects\"' cmd/manager/main.go` returns exactly 1 AFTER Task 1 (was 0 pre-edit)"
    - "`gofmt -d cmd/manager/main.go` returns empty (no formatting drift)"
    - "`go build ./cmd/manager/...` exits 0 AND `go vet ./cmd/manager/...` exits 0 AND `go build ./...` exits 0 (full project build) AND `go vet ./...` exits 0 AFTER Task 1"
    - "Dispatcher field declarations on both reconciler structs are UNTOUCHED by this plan (they were already declared in Phase 2): `grep -c 'Dispatcher dispatch.Dispatcher' internal/controller/plan_controller.go` returns exactly 1 (pre and post edit); same for `internal/controller/task_controller.go`"
    - "Other production source files UNCHANGED: `internal/controller/`, `internal/dispatch/podjob/`, `internal/webhook/`, `api/`, `pkg/`, `charts/`, `Makefile`, `test/` — verified by `git status --porcelain <path>` returning empty for each (the Dispatcher fields were already DECLARED on the reconciler structs; this plan only assigns them in main.go)"
    - "Cascade-9 ValidationState gap NOT addressed by this plan: `internal/webhook/v1alpha1/plan_webhook.go` is UNTOUCHED (`git status --porcelain internal/webhook/` returns empty); deferred to a separate follow-up debug session per `<gap_input>` instruction"
    - "Plan 02.2-09's Task 1 fixture-helper edits PRESERVED VERBATIM: `git status --porcelain test/integration/kind/suite_test.go` returns empty AND `grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go` returns 1 AND `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` returns 1"
    - "Plan 02.2-08's cascade-6 timeout bumps PRESERVED: `grep -c '120\\*time\\.Second' test/integration/kind/credproxy_test.go` returns 1 AND `grep -cE '240\\*time\\.Second|4\\*time\\.Minute' test/integration/kind/failure_test.go` returns 1"
    - "Plans 02.2-04 and 02.2-05 flag definitions PRESERVED: `grep -c 'flag.StringVar.*\"metrics-bind-address\"' cmd/manager/main.go` returns 1 AND `grep -c 'flag.StringVar.*\"webhook-cert-path\"' cmd/manager/main.go` returns 1"
    - "Plan 02.2-06's Makefile timeout bump PRESERVED: `grep -c 'timeout 600s' Makefile` ≥ 1"
    - "`02.2-10-VERIFICATION.md` exists with frontmatter including 5 observation fields: `credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds`, AND the new field `dispatch_active: true|false` indicating whether `task_controller.go:393`'s `r.Create(ctx, job)` actually fired (evidenced by Job CRDs appearing in any test namespace during the run; manager log should show `dispatch` or `creating job` log lines)"
    - "APPROVED gate requires ALL 5 conditions on BOTH clean run AND idempotent rerun: (1) wall ≤ 600s; (2) Manager Pod `STATUS=Running` AND restarts ≤ 1; (3) manager log has zero `flag provided but not defined` errors AND zero `no project found in namespace <ns>` errors AND log evidence of `dispatch` / `creating job` / Job-creation events (proving dispatch path actually fires — THIS is the cascade-8 closure indicator); (4) PVC tide-projects Bound RWO; (5) 7/7 Layer B Ginkgo specs PASS"
    - "Expected runtime evidence: `credproxy_pod_wait_observed_seconds` SHOULD DROP from Plan 02.2-09's 120s to well under 30s. If observed ≥ 60s, that's an ALERT signal that some OTHER gate is still blocking dispatch (likely the cascade-9 ValidationState production-wiring-gap that the debugger flagged — but TaskReconciler.reconcileDispatch is NOT gated on ValidationState, so HARN-03 specifically should still close; if it doesn't, that's NEW evidence the debug session needs to expand)"
    - "If `02.2-10-VERIFICATION.md` records `gate_decision: APPROVED`, this is the FIRST APPROVED gate in Phase 02.2's 10-plan cascade chain. Task 3 fires; Plan 02.2-02 rewired from `depends_on: [\"02.2-01\"]` → `[\"02.2-10\"]` and ALL 31 references to `02.2-01-VERIFICATION.md` redirected to `02.2-10-VERIFICATION.md`. Phase 02.2 chain TERMINATES."
    - "If `02.2-10-VERIFICATION.md` records `gate_decision: BLOCKED` with cascade-9 surfacing (likely the ValidationState production-wiring-gap), Task 3 does NOT fire; cascade-9 classified per T-02.2-16 taxonomy in §Section 5 (most likely `other` → `production-wiring-gap` family); Plan 02.2-11 fix shape proposed (likely wiring `plan.Status.ValidationState = \"Validated\"` in the admission webhook or via a new PlanReconciler validation step)"
    - "T-02.2-21 carry-forward: if `credproxy_pod_wait_observed_seconds ≥ 60` on the Plan 02.2-10 clean run, the `dispatch latency marginal` literal token appears in the verification artifact; if observed < 30s, the cascade-8 Option δ fix is confirmed as the dominant cost previously hiding behind the 120s budget"
  artifacts:
    - path: "cmd/manager/main.go"
      provides: "Production-side Dispatcher wiring for both PlanReconciler and TaskReconciler — cascade-8 Option δ fix (the dispatch path goes live)"
      contains: "Dispatcher: dispatcher"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md"
      provides: "Runtime verification artifact with gate_decision frontmatter — chain terminator candidate v3 (Plan 02.2-08 was v1, Plan 02.2-09 was v2; both BLOCKED on subsequent cascades)"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-10\"] + 31 references redirected to 02.2-10-VERIFICATION.md (gated on Task 2 APPROVED) — the FIRST APPROVED rewire in Phase 02.2 history if Task 2 returns APPROVED"
      contains: "02.2-10"
  key_links:
    - from: "cmd/manager/main.go (dispatcher construction at ~line 189)"
      to: "internal/controller/plan_controller.go:57 PlanReconciler.Dispatcher field AND internal/controller/task_controller.go:77 TaskReconciler.Dispatcher field"
      via: "Direct struct-literal field assignment — `Dispatcher: dispatcher,` in both reconciler setup blocks"
      pattern: 'Dispatcher: dispatcher'
    - from: "cmd/manager/main.go reconciler Dispatcher assignments"
      to: "internal/controller/plan_controller.go:121 + internal/controller/task_controller.go:167 (gated dispatch paths)"
      via: "Once Dispatcher is non-nil, the `if r.Dispatcher != nil` guards open and the Phase 2 reconcileWaveMaterialization and reconcileDispatch bodies run"
      pattern: 'if r\.Dispatcher != nil'
    - from: "TaskReconciler.reconcileDispatch (now live in production)"
      to: "task_controller.go:393 r.Create(ctx, job) — Job creation in K8s"
      via: "PodJobBackend.BuildJobSpec → client.Create — the credproxy HARN-03 spec's 120s Eventually pod-wait now has an actual Job-creation code path firing within seconds"
      pattern: 'r\.Create\(ctx, job\)'
    - from: "02.2-10-VERIFICATION.md gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 wires Plan 02.2-02 (HARD-GATED on Task 2 gate_decision: APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md (depends_on field, post-Task-3)"
      to: "this plan's verification artifact"
      via: "frontmatter depends_on: [\"02.2-10\"] — bumped only if Task 2 returns APPROVED"
      pattern: 'depends_on: \[.{0,5}02.2-10.{0,5}\]'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-09-VERIFICATION.md` §Section 5 (eighth iteration in Phase 02.2's cascade chain). **MAJOR SCOPE SHIFT vs prior gap plans 02.2-06/07/08/09: this plan modifies PRODUCTION code (`cmd/manager/main.go`) for the first time since Plans 02.2-04 / 02.2-05 (which added two flag definitions).** The Option-B-test-only contract that held for Plans 02.2-03/06/07/08/09 is explicitly broken because the **debug investigation captured in `.planning/debug/credproxy-backoff-suppression.md` revealed the actual root cause is a production-side wiring gap, not a test-fixture issue.**

**Cascade-8 corrected root cause (from `.planning/debug/credproxy-backoff-suppression.md` §\"Actual root cause\" — debug session status: `root_cause_found`):**

`cmd/manager/main.go` constructs all 6 reconcilers (Project, Milestone, Phase, Plan, Wave, Task) at lines 191-256. The PlanReconciler struct literal at lines 220-228 and TaskReconciler struct literal at lines 240-256 BOTH declare a `Dispatcher` field (`plan_controller.go:57`, `task_controller.go:77`) but the field is **never assigned** in `cmd/manager/main.go`. The Phase 2 dispatch paths in both controllers are gated behind `if r.Dispatcher != nil` (`plan_controller.go:121`, `task_controller.go:167`). With `Dispatcher == nil`, neither controller ever creates a `batchv1.Job`. The `r.Create(ctx, job)` call at `task_controller.go:393` is never reached in production.

**The \"127s of silence\" in Plan 02.2-09's manager log isn't backoff suppression — it's \"no reconcile work to do\" because the dispatch path is gated off.** All 5 originally-hypothesized fix options from Plan 02.2-09's BLOCKED diagnosis (α: timeout bump; β-1: status subresource; β-2/β-3: serialize/debounce; γ: aggressive requeue) were REJECTED by the debug investigation:
- α: would not help — no Pod-creation code path is running regardless of timeout
- β-1: already implemented (`task_types.go:126`, `plan_types.go:57` both have `+kubebuilder:subresource:status` markers — Status subresource is already enabled; β-1 is a no-op)
- β-2/β-3/γ: misdiagnosed — the ResourceVersion conflicts are cosmetic noise from TaskReconciler racing against its own previous reconcile via informer-cache lag at `task_controller.go:139` (finalizer add) and `task_controller.go:160` (owner ref set), NOT a stampTaskLabels-vs-TaskReconciler race; even with perfect serialization no Pod would be created

**RECOMMENDED FIX — Option δ (Dispatcher wiring in cmd/manager/main.go):** Construct a `*podjob.PodJobBackend{...}` instance in `cmd/manager/main.go` (right after `envReader` is constructed at line 188) and assign it as `Dispatcher: dispatcher` to BOTH the PlanReconciler struct literal (lines 220-228) AND the TaskReconciler struct literal (lines 240-256). All 7 PodJobBackend fields (Client, Scheme, SubagentImage, CredproxyImage, SigningKey, EnvReader, PVCName) are already constructed in scope at that point — no new imports beyond the existing `"github.com/jsquirrelz/tide/internal/dispatch/podjob"` (already at `cmd/manager/main.go:46`) and no new helpers needed.

**Why 7/7 specs PASS is now genuinely achievable for the first time in the cascade chain:**
- Cascades 1–7 all closed (PVC, metrics flag, webhook flag, Makefile budget, all 4 fixture hierarchies, Eventually timeouts)
- Cascade-8 Option δ fixes the dispatch path itself
- The 120s `Eventually` pod-wait at `credproxy_test.go:95` should pass in well under 30s once dispatch fires (Job creation is a few hundred ms; Pod boot adds 10-20s under kind)
- The secondary cascade-9 risk (`Status.ValidationState` never set by the admission webhook) does NOT affect `TaskReconciler.reconcileDispatch` path, only `PlanReconciler.reconcileWaveMaterialization` — which is irrelevant to HARN-03's assertion shape (HARN-03 watches for Pod creation, not Wave status). So Plan 02.2-10's runtime verification can succeed even with the ValidationState gap intact.

**SCOPE BOUNDARIES (load-bearing — this plan does ONLY cascade-8 closure):**

This plan does NOT:
- Fix cascade-9 (the `plan.Status.ValidationState` never-set production-wiring-gap discovered during debug investigation). That is a separate cascade-9 debug session AFTER Plan 02.2-10's chain terminator lands. Resisting scope expansion is itself a load-bearing constraint per `.continue-here.md` anti-pattern #6 (\"Resist scope expansion\").
- Modify any controller body, webhook body, CRD type, or chart values. The Dispatcher field is ALREADY DECLARED on both reconciler structs (Phase 2 work landed at `plan_controller.go:57` and `task_controller.go:77`). This plan ONLY assigns the field from `cmd/manager/main.go` where the constructor variables (mgr, scheme, signingKey, envReader, subagentImage, credproxyImage) are already in scope.
- Modify `test/integration/kind/` files. Plan 02.2-09's fixture-helper edits (createProjectHierarchy + 3 call-sites) are preserved verbatim; the 120s `Eventually` pod-wait at `credproxy_test.go:95` is intentionally NOT bumped (the debug investigation proved the 120s budget is enormously generous for an actual dispatch path; a passing run will complete in <30s).
- Touch `charts/tide/values.yaml` per `.continue-here.md` anti-pattern #2.
- Modify Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08, 02.2-09 — only `02.2-02-PLAN.md` is touched (and only by Task 3, HARD-GATED on Task 2 APPROVED).

**OPTION-B-TEST-ONLY CONTRACT DELIBERATELY BROKEN (load-bearing scope shift — surface to executor and reader):**

Plans 02.2-06 / 07 / 08 / 09 all preserved an \"Option B — test-only changes\" contract: zero modifications to production source under `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `Makefile`. That contract is **explicitly broken** by Plan 02.2-10. The justification:

1. The debug investigation at `.planning/debug/credproxy-backoff-suppression.md` proved with code-evidence that NO test-only change can close cascade-8. The dispatch path is gated off at the production-controller level; no test fixture, helper, or timeout bump can cause `r.Create(ctx, job)` to fire.
2. Option δ (Dispatcher wiring) is the only fix shape that closes cascade-8. The debug's §\"Fix landscape (corrected)\" explicitly REJECTED Options α/β-1/β-2/β-3/γ with documented evidence.
3. Plan 02.2-04 and Plan 02.2-05 already established the precedent for surgical edits to `cmd/manager/main.go` (flag definitions + wire-ups); the shape of this plan's Task 1 is structurally identical to those plans' Task 1 (insert a small block, wire into existing struct literals).
4. The blast radius is minimal: Task 1's diff is approximately +12 lines (1 PodJobBackend constructor block + 2 single-line `Dispatcher: dispatcher,` additions). No new imports. No controller body changes. No new helpers. The Dispatcher field was already DESIGNED to be wired; this plan completes wiring that was deferred since Phase 2 Plan 09 work landed.

The closing prose of `02.2-10-VERIFICATION.md` §Section 5 MUST surface this scope shift explicitly to set expectations for any future planner reading the artifact: production-side changes ARE legitimate in gap-closure plans when the debug evidence shows test-side changes cannot close the cascade.

**Plan 02.2-09's 3-task pattern this plan mirrors:** Plan 02.2-09 ran a 3-task shape — T1 source edit (test-fixture helper + 3 call sites), T2 runtime verification (`02.2-09-VERIFICATION.md` with `gate_decision` frontmatter + 4 observation fields), T3 surgical edit of `02.2-02-PLAN.md` gated on APPROVED. This plan mirrors that shape exactly with cascade-7's test-fixture target replaced by cascade-8's production-wiring target. **Plan 02.2-05 is a stronger structural analog for Task 1** since it was the prior plan that surgically edited `cmd/manager/main.go` (adding `--webhook-cert-path` flag + wiring into `webhook.Options.CertDir`); the shape of \"insert a small block at a specific line + assign in two existing struct literals\" is closer to Plan 02.2-10 than the test-only Plan 02.2-09. **The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic:** Task 3 fires ONLY if Task 2's `gate_decision: APPROVED`.

**Recovery state note (load-bearing for Task 3's rewire scope):** Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, 02.2-08 Task 3, AND 02.2-09 Task 3 ALL DID NOT FIRE (their respective Task 2 gates were each BLOCKED on a different cascade). As a result, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (verified at plan-authoring time by `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returning exactly 31). This plan's Task 3 lands the long-pending rewire if Task 2's gate is APPROVED, redirecting all 31 references to `02.2-10-VERIFICATION.md` in one pass — this would be the **first APPROVED rewire in Phase 02.2 history**.

**Recovery-lesson note from `.continue-here.md` Plan 02.2-05 protocol (carried forward through 02.2-06/07/08/09):** Write `02.2-10-VERIFICATION.md` WITH PRELIMINARY frontmatter immediately after the clean-run captures land in `/tmp/02.2-10-clean-run.log`, then add Sections 2–6 incrementally as the rerun completes and the cluster diagnostic snapshot is captured. This protects against mid-run executor API errors / context window pressure during the long-running runtime verification phase — the artifact's frontmatter (the load-bearing gate field) is preserved even if Sections 4–6 prose authoring is interrupted.

**T-02.2-21 carry-forward (load-bearing for the runtime measurement):** Plans 02.2-07/08/09 all recorded `credproxy_pod_wait_observed_seconds: 60|120|120` (full budget consumed every time — the debug investigation now explains why: NO Pod-creation code path was running). Plan 02.2-10's clean run should drop `credproxy_pod_wait_observed_seconds` to well under 30s if Option δ works as designed. If observed ≥ 60s, that's an ALERT — it means SOMETHING ELSE is blocking dispatch beyond the cascade-8 fix (most likely cascade-9 ValidationState OR a latent Job-build bug surfacing for the first time now that the path is live). Surface in §Section 5 with concrete classification.

Purpose: close the BLOCKED runtime gate from `02.2-09-VERIFICATION.md` (cascade-7 createProjectHierarchy fix preserved; cascade-8 production-wiring-gap surfaced as the dispatch-path-not-running root cause) by constructing a `*podjob.PodJobBackend` in `cmd/manager/main.go` and assigning it to both reconciler literals' `Dispatcher` field. Tactical iteration #8: 1 production-code surgical edit + runtime re-verification + (gated) Plan 02.2-02 rewire. **This is the chain terminator v3 candidate — high-confidence because the debug evidence specifically targets the dispatch-path-not-running root cause.**

Output:
- `cmd/manager/main.go` with: (1) a `dispatcher := &podjob.PodJobBackend{...}` construction inserted right after the `envReader := &podjob.FilesystemEnvelopeReader{...}` line; (2) `Dispatcher: dispatcher,` added as the LAST field of the PlanReconciler struct literal at lines 220-228; (3) `Dispatcher: dispatcher,` added as the LAST field of the TaskReconciler struct literal at lines 240-256
- `.planning/phases/02.2-.../02.2-10-VERIFICATION.md` mirroring `02.2-09-VERIFICATION.md`'s structural template; `gate_decision: APPROVED` or `BLOCKED`; runtime-observation fields `credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds`, AND a NEW field `dispatch_active: true|false` populated
- If Task 2 APPROVED: `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-10"]` and ALL 31 `02.2-01-VERIFICATION.md` references redirected to `02.2-10-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
**Wave-dispatch concern for Plan 02.2-02 (executor MUST honor):** Plan 02.2-02 is the closeout plan and currently has `depends_on: ["02.2-01"]` with a `checkpoint:human-verify` Task 1 that reads `02.2-01-VERIFICATION.md`'s `gate_decision` field. Because Plan 02.2-01's verification artifact records `gate_decision: BLOCKED` (its checkpoint was BLOCKED on the original cascade chain; subsequent attempts via Plans 02.2-03/04/05/06/07/08/09 Task 3 ALL did NOT fire), Plan 02.2-02 MUST NOT be dispatched alongside Plan 02.2-10 in the same execute wave. The structural mitigation: execute-phase's wave-ordering reads `depends_on` from each plan's frontmatter and groups plans whose dependencies are all satisfied into the same wave. Plan 02.2-02's `depends_on` points at 02.2-01 (BLOCKED gate artifact); Plan 02.2-10's `depends_on` points at 02.2-09 (whose Task 1 createProjectHierarchy fix landed even though its Task 2 runtime gate was BLOCKED on cascade-8, which THIS plan is closing). The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic: failed task → siblings continue, **dependents in later waves never dispatch**. Plan 02.2-02 stays a dependent-in-later-wave of `02.2-01-VERIFICATION.md`'s BLOCKED gate until THIS plan's Task 3 rewrites its `depends_on` to point at `02.2-10-VERIFICATION.md`'s (hopefully APPROVED) gate. If the executor mistakenly dispatches Plan 02.2-02 alongside Plan 02.2-10 in the same wave, Plan 02.2-02's Task 1 checkpoint will halt at the BLOCKED gate AND nothing further fires — but the structural intent is that Plan 02.2-02 dispatches AFTER Plan 02.2-10's Task 3 has landed, in a separate wave 10 (or later).

**Option-B-test-only contract DELIBERATE BREAK (load-bearing scope shift — executor and reader MUST understand the reasoning):** Plans 02.2-06 / 07 / 08 / 09 all preserved an Option-B-test-only contract. Plan 02.2-10 EXPLICITLY breaks it: production source `cmd/manager/main.go` is modified for the first time since Plan 02.2-05's `--webhook-cert-path` flag addition. The justification is captured in the debug session at `.planning/debug/credproxy-backoff-suppression.md` §\"Fix landscape (corrected)\": no test-side change can close cascade-8 because the dispatch path is gated off at the production-controller level via `if r.Dispatcher != nil`. The debug investigation explicitly REJECTED Options α/β-1/β-2/β-3/γ (timeout bumps, status subresource, serialize/debounce, aggressive requeue) with documented code-evidence. Option δ (Dispatcher wiring) is the ONLY fix shape that closes cascade-8. The blast radius is minimal: approximately +12 lines in `cmd/manager/main.go`, zero controller-body changes, zero new imports, zero new helpers. The dispatch logic was DESIGNED to run from Phase 2 Plan 09; wiring it via Plan 02.2-10 completes work that was deferred since Phase 2 landed. **The verification artifact's §Section 5 MUST surface this scope shift explicitly so future planners understand production-side gap-closure plans are legitimate when debug evidence shows test-side changes cannot close the cascade.**

**Cascade-9 follow-up DEFERRED (resist scope expansion — load-bearing constraint per `.continue-here.md` anti-pattern #6):** The debug investigation surfaced a SECONDARY production-wiring-gap: the Plan admission webhook at `internal/webhook/v1alpha1/plan_webhook.go:117-199` validates and emits warnings but never updates `plan.Status.ValidationState`. `PlanReconciler.reconcileWaveMaterialization` short-circuits at `plan_controller.go:147` if `ValidationState != \"Validated\"`. This means even with Option δ wired, the Plan reconciler's Wave materialization path remains gated. **HOWEVER:** `TaskReconciler.reconcileDispatch` does NOT depend on Wave existence or ValidationState — it only requires `r.Dispatcher != nil` + the Task itself. So Plan 02.2-10's Option δ closes cascade-8 for HARN-03 (Pod creation) and the other Task-centric specs even with the ValidationState gap intact. Plan 02.2-10's scope is cascade-8 closure ONLY; the cascade-9 ValidationState gap is documented in the verification artifact's §Section 5 as a flagged follow-up for a separate debug session AFTER Plan 02.2-10 lands the chain terminator. If the planner is tempted to bundle the ValidationState webhook fix into this plan, REJECT — the debug evidence specifically scopes Plan 02.2-10 to Option δ, and scope expansion has been the dominant failure mode of prior gap-closure iterations.

**Critical anti-patterns from `.continue-here.md` \"Critical Anti-Patterns\" (do NOT trigger):**
1. Do NOT pitch a comprehensive audit. User has chosen tactical iteration 8 times now; cascade-8 was correctly diagnosed via targeted debug investigation (`.planning/debug/credproxy-backoff-suppression.md`) as a production-wiring-gap. Tactical iteration 9 (THIS plan) is the right shape.
2. Do NOT touch `charts/tide/values.yaml`. Chart is FIXED contract.
3. Do NOT dispatch Plan 02.2-02 alongside Plan 02.2-10 in the same execute wave when Plan 02.2-02's checkpoint artifact records BLOCKED. (See preceding paragraph.)
4. Do NOT spawn Phase 02.3. Cascade-8 is still within Phase 02.2's scope (closing the kind-test runtime gate); inline gap-closure is the correct pattern.
5. **Do NOT preserve the Option-B-test-only contract** for THIS plan. The debug evidence proves test-only changes cannot close cascade-8. Production-side surgical edit to `cmd/manager/main.go` is the documented correct fix. The Option-B-test-only contract IS broken deliberately and transparently — surface this in `<objective>` AND `<execution_notes>` so future planners understand why.
6. **Do NOT expand scope to include cascade-9 (ValidationState wiring).** The debug evidence specifically scopes Plan 02.2-10 to Option δ. Cascade-9 is a separate follow-up debug session AFTER Plan 02.2-10's chain terminator lands. If the planner is tempted to bundle the ValidationState webhook fix into this plan, REJECT.
7. Do NOT inherit `02.2-06-VERIFICATION.md` §Section 5's discredited \"Wave Ready check\" framing. That framing was already corrected in `02.2-07-VERIFICATION.md` §Section 5 (the dispatch gate is `resolveProject` at `task_controller.go:217` for cascade-5/7 — harness-bug class). For cascade-8, the dispatch gate is the `if r.Dispatcher != nil` guard itself (production-wiring-gap class). This plan's `02.2-10-VERIFICATION.md` §Section 5 should re-state the corrected root cause framing (resolveProject for cascade-5/7) AND surface the cascade-8 ROOT CAUSE (Dispatcher field never assigned in cmd/manager/main.go — production-wiring-gap class).
8. Do NOT bump the credproxy HARN-03 120s pod-wait timeout. The debug evidence proved Plan 02.2-08's bump was tactically accommodating a hypothesized race that does not exist. With Option δ wired, the 120s budget is enormously generous; a passing run completes in <30s. If the planner is tempted to also bump the timeout \"defensively\" REJECT — the cascade-8 fix shape is Dispatcher wiring ONLY.
</execution_notes>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md
@.planning/debug/credproxy-backoff-suppression.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/.continue-here.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from cmd/manager/main.go + internal/dispatch/podjob/backend.go + internal/controller/{plan,task}_controller.go at plan-time. -->
<!-- Executor should use these directly — no codebase exploration needed beyond confirming the existing patterns match. -->

**Cascade chain status (for plan readers' orientation):**
| # | Class | Side | Status | Plan | Fix Shape |
|---|-------|------|--------|------|-----------|
| 1 | PVC RWX → RWO | test infra | CLOSED | 02.2-03 | Chart accessModes templating + test --set override |
| 2 | --metrics-bind-address | binary | CLOSED | 02.2-04 | Binary flag definition in cmd/manager/main.go |
| 3 | --webhook-cert-path | binary | CLOSED | 02.2-05 | Binary flag definition + webhook.NewServer wiring |
| 4 | Makefile timeout 300s→600s | test infra | CLOSED | 02.2-06 | Single-line Makefile bump |
| 5 | credproxy fixture-incomplete | test fixture | CLOSED | 02.2-07 | applyHierarchy helper + credproxy refactor |
| 6 | Eventually timeouts | test fixture | CLOSED | 02.2-08 | Two single-token Eventually timeout replacements |
| 7 | caps/output/failure fixture-incomplete (harness-bug; same class as cascade-5, different files) | test fixture | CLOSED | 02.2-09 | New createProjectHierarchy helper + 3 call-site additions |
| 8 | PodJobBackend never wired (production-wiring-gap) | **production** | **THIS PLAN** | 02.2-10 | Construct `*podjob.PodJobBackend` in cmd/manager/main.go + assign to both reconciler literals' Dispatcher field |
| 9 (deferred) | plan.Status.ValidationState never set (production-wiring-gap) | production | follow-up debug | — | TBD by separate debug session after Plan 02.2-10's chain terminator lands |

**Current state of `cmd/manager/main.go` (POST Plan 02.2-09 — pre-edit for THIS plan), verified at plan-time:**

Line 46 (existing import — already in place; no new imports needed):
```go
"github.com/jsquirrelz/tide/internal/dispatch/podjob"
```

Lines 172-188 (the Phase 2 component wiring block — `dispatcher` will be appended as the next sub-step `e`):
```go
	// 6. Phase 2 component wiring (Plan 12).
	//    a. HMAC signing key — fail-fast; running without it breaks HMAC validation (HARN-03).
	signingKey, err := decodeSigningKeyFromEnv()
	if err != nil {
		setupLog.Error(err, "signing key required (HARN-03)")
		os.Exit(1)
	}
	//    b. Budget store — in-process per-Secret-UID rate-limiter cache (D-D1).
	budgetStore := budget.NewStore()
	//    c. Rate-limit defaults from Helm values (rateLimits.defaults.* in values.yaml).
	defaults := budget.Limits{
		RequestsPerMinute: rateLimitDefaultRPM,
		BurstSize:         rateLimitDefaultBurst,
	}
	//    d. EnvelopeOut reader — reads from the shared tide-projects PVC at /workspaces
	//       (Blocker #2/#3 fix — single-shared-PVC + subPath architecture, RESEARCH.md OQ#2 RESOLVED).
	envReader := &podjob.FilesystemEnvelopeReader{WorkspaceRoot: "/workspaces"}
```

Lines 220-228 (the PlanReconciler struct literal — `Dispatcher: dispatcher,` will be added as the LAST field, after `WatchNamespace: watchNamespace,`):
```go
	if err := (&controller.PlanReconciler{
		Client:                  mgr.GetClient(),
		Scheme:                  mgr.GetScheme(),
		MaxConcurrentReconciles: cfg.MaxConcurrentReconciles.Plan,
		PlannerPool:             plannerPool,
		WatchNamespace:          watchNamespace,
	}).SetupWithManager(mgr); err != nil {
		setupLog.Error(err, "unable to create controller", "controller", "Plan")
		os.Exit(1)
	}
```

Lines 240-256 (the TaskReconciler struct literal — `Dispatcher: dispatcher,` will be added as the LAST field, after `EnvReader: envReader,`):
```go
	if err := (&controller.TaskReconciler{
		Client:                  mgr.GetClient(),
		Scheme:                  mgr.GetScheme(),
		MaxConcurrentReconciles: cfg.MaxConcurrentReconciles.Task,
		ExecutorPool:            executorPool,
		WatchNamespace:          watchNamespace,
		// Phase 2 fields (Plan 12 wiring).
		Budget:         budgetStore,
		Defaults:       defaults,
		SigningKey:     signingKey,
		SubagentImage:  subagentImage,
		CredproxyImage: credproxyImage,
		EnvReader:      envReader,
	}).SetupWithManager(mgr); err != nil {
		setupLog.Error(err, "unable to create controller", "controller", "Task")
		os.Exit(1)
	}
```

**The PodJobBackend struct definition (verified at plan-time from `internal/dispatch/podjob/backend.go:70-79` — 7 fields, no constructor, direct struct-literal construction):**
```go
type PodJobBackend struct {
	Client         client.Client
	Scheme         *runtime.Scheme
	SubagentImage  string
	CredproxyImage string
	SigningKey     []byte
	EnvReader      EnvelopeReader
	// PVCName is the name of the chart-provisioned shared PVC (default "tide-projects").
	PVCName string
}
```

**The Dispatcher field declarations on both reconciler structs (verified at plan-time — already in place, NOT modified by this plan):**

From `internal/controller/plan_controller.go:57`:
```go
type PlanReconciler struct {
	client.Client
	Scheme *runtime.Scheme
	MaxConcurrentReconciles int
	PlannerPool  *pool.Pool
	ExecutorPool *pool.Pool
	Dispatcher dispatch.Dispatcher    // <-- field exists, currently unassigned in production
	WatchNamespace string
}
```

From `internal/controller/task_controller.go:77`:
```go
type TaskReconciler struct {
	client.Client
	Scheme *runtime.Scheme
	MaxConcurrentReconciles int
	PlannerPool *pool.Pool
	ExecutorPool *pool.Pool
	Dispatcher dispatch.Dispatcher    // <-- field exists, currently unassigned in production
	Budget *budget.Store
	Defaults budget.Limits
	SigningKey []byte
	SubagentImage string
	CredproxyImage string
	EnvReader podjob.EnvelopeReader
	WatchNamespace string
}
```

**The gated dispatch paths these assignments unlock (verified at plan-time — not modified by this plan):**

From `internal/controller/plan_controller.go:120-123`:
```go
	// 5. Phase 2 Wave materialization body inside the Dispatcher seam (REQ-SUB-01).
	if r.Dispatcher != nil {
		return r.reconcileWaveMaterialization(ctx, &plan)
	}
```

From `internal/controller/task_controller.go:166-169`:
```go
	// 5. Phase 2 dispatch body inside the Dispatcher seam (REQ-SUB-01).
	if r.Dispatcher != nil {
		return r.reconcileDispatch(ctx, &task)
	}
```

**The Job-creation site that becomes reachable (verified at plan-time — `task_controller.go:393`, the Step 11 `r.Create(ctx, job)` call inside `TaskReconciler.reconcileDispatch`). This is the smoking gun: with Dispatcher nil, this line is never reached; with Dispatcher wired (Option δ), every unfinished Task reconcile fires this call within milliseconds and the credproxy HARN-03 pod-wait succeeds in well under 30s.**

**Pre-edit grep counts (LOAD-BEARING — used by Task 1 verify to assert exactly the right number of new occurrences):**

At plan-authoring time (verified by planner's plan-time inspection — see `grep -c` runs against current HEAD):
- `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returns **0** (no dispatcher construction yet)
- `grep -c 'Dispatcher: dispatcher' cmd/manager/main.go` returns **0** (no Dispatcher assignment yet)
- `grep -c 'PVCName:.*"tide-projects"' cmd/manager/main.go` returns **0**
- `grep -c 'Dispatcher dispatch.Dispatcher' internal/controller/plan_controller.go` returns **1** (existing field declaration — preserved)
- `grep -c 'Dispatcher dispatch.Dispatcher' internal/controller/task_controller.go` returns **1** (existing field declaration — preserved)
- `grep -c 'if r.Dispatcher != nil' internal/controller/plan_controller.go` returns **≥ 1** (existing gate — preserved)
- `grep -c 'if r.Dispatcher != nil' internal/controller/task_controller.go` returns **≥ 1** (existing gate — preserved)

**Post-edit grep count expectations (Task 1 acceptance):**
- cmd/manager/main.go: `dispatcher := &podjob.PodJobBackend{` count goes from 0 → 1 (new construction added)
- cmd/manager/main.go: `Dispatcher: dispatcher,` count goes from 0 → 2 (one in each reconciler literal — PlanReconciler + TaskReconciler)
- cmd/manager/main.go: `PVCName:.*"tide-projects"` count goes from 0 → 1
- internal/controller/plan_controller.go: ALL grep counts UNCHANGED (file is NOT touched by this plan)
- internal/controller/task_controller.go: ALL grep counts UNCHANGED (file is NOT touched by this plan)
- All other production source under `internal/`, `api/`, `pkg/`, `charts/`, `Makefile`, `test/`: `git status --porcelain <path>` returns empty (NOT touched by this plan)

**Plan 02.2-02 rewire scope (Task 3 — HARD-GATED on Task 2 APPROVED):**

At plan-authoring time, `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returns **31** (verified). None of the prior plans' Task 3 fired (Plans 02.2-03/04/05/06/07/08/09 Task 3 ALL halted on BLOCKED gates). This plan's Task 3 redirects all 31 references in one pass: `02.2-01-VERIFICATION.md` → `02.2-10-VERIFICATION.md`, AND bumps `depends_on: ["02.2-01"]` → `depends_on: ["02.2-10"]`.

**From CLAUDE.md \"Failure semantics at wave boundaries\" contract:**
- Failed task → siblings in same wave continue
- Dependents in later waves never dispatch
- Plan 02.2-02 reads the gate artifact; if BLOCKED, Plan 02.2-02 stays gated
- Resumption state = indegree map + completed-task set, nothing more

**From `.planning/debug/credproxy-backoff-suppression.md` §\"Fix landscape (corrected)\" Option δ — the verbatim implementation sketch:**

The debug session at `.planning/debug/credproxy-backoff-suppression.md:128-143` provides the canonical Option δ implementation shape. Task 1's `<action>` cites this verbatim and Task 1's `<verify>` asserts the resulting code matches.

```go
// After line 188 of cmd/manager/main.go (where envReader is constructed):
dispatcher := &podjob.PodJobBackend{
    Client:         mgr.GetClient(),
    Scheme:         scheme,
    SubagentImage:  subagentImage,
    CredproxyImage: credproxyImage,
    SigningKey:     signingKey,
    EnvReader:      envReader,
    PVCName:        "tide-projects",
}
// Then in PlanReconciler block (line 220-228), add:
//     Dispatcher: dispatcher,
// And in TaskReconciler block (line 240-256), add:
//     Dispatcher: dispatcher,
```

**Note on `Scheme:` field value:** The debug sketch shows `Scheme: scheme` (the local `scheme` variable constructed at line 130). The reconciler literals in main.go use `mgr.GetScheme()` (e.g., `Scheme: mgr.GetScheme()` at line 222). For consistency with the surrounding reconciler-literal style — and because `mgr.GetScheme()` returns the same `*runtime.Scheme` instance that was passed to `ctrl.NewManager` via the `Scheme: scheme` Options field at line 136 — the dispatcher construction in this plan uses `Scheme: mgr.GetScheme()`. Both forms are functionally equivalent (single-scheme topology); `mgr.GetScheme()` is chosen for stylistic alignment with the reconciler literals.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire `*podjob.PodJobBackend` into `cmd/manager/main.go` reconciler literals (Option δ — close cascade-8 production-wiring-gap)</name>
  <files>cmd/manager/main.go</files>

  <read_first>
    - cmd/manager/main.go lines 1-50 (imports block — confirm `"github.com/jsquirrelz/tide/internal/dispatch/podjob"` already at line 46; no new imports needed)
    - cmd/manager/main.go lines 86-115 (flag declarations + flag.StringVar definitions — confirm `metricsAddr` from Plan 02.2-04 and `webhookCertPath` from Plan 02.2-05 are both preserved verbatim; these are REGRESSION GUARDS this plan must not disturb)
    - cmd/manager/main.go lines 172-188 (the Phase 2 component wiring block — Section 6 sub-steps a (signing key), b (budget store), c (rate-limit defaults), d (envReader). This task adds sub-step e (dispatcher) IMMEDIATELY AFTER line 188's envReader construction)
    - cmd/manager/main.go lines 190-256 (the six reconciler setup blocks — particularly lines 220-228 (PlanReconciler struct literal) and lines 240-256 (TaskReconciler struct literal). These are the two struct literals that need `Dispatcher: dispatcher,` appended as their LAST field. Note the existing comment at line 246 `// Phase 2 fields (Plan 12 wiring).` which groups SigningKey/SubagentImage/CredproxyImage/EnvReader — Dispatcher is the FINAL Phase 2 field to wire from main.go)
    - internal/dispatch/podjob/backend.go lines 70-79 (PodJobBackend struct definition — 7 fields: Client, Scheme, SubagentImage, CredproxyImage, SigningKey, EnvReader, PVCName. CONFIRM NO constructor function exists; direct struct-literal construction is the established pattern)
    - internal/controller/plan_controller.go lines 47-62 (PlanReconciler struct definition with Dispatcher field at line 57 — verify field is `Dispatcher dispatch.Dispatcher` — type matches the `*podjob.PodJobBackend` we'll assign because `PodJobBackend` satisfies `internal/dispatch.Dispatcher` per backend.go line 58)
    - internal/controller/plan_controller.go lines 118-125 (the `if r.Dispatcher != nil` gated dispatch path that will fire once Dispatcher is wired — verify the gate condition matches the field name)
    - internal/controller/task_controller.go lines 65-104 (TaskReconciler struct definition with Dispatcher field at line 77 — same type pattern as PlanReconciler)
    - internal/controller/task_controller.go lines 164-170 (the `if r.Dispatcher != nil` gated dispatch path)
    - internal/controller/task_controller.go lines 380-400 (Step 11 of reconcileDispatch — the `r.Create(ctx, job)` call at line 393 that becomes reachable after Dispatcher is wired; this is the smoking-gun line referenced in `.planning/debug/credproxy-backoff-suppression.md` §\"Actual root cause\")
    - test/integration/kind/credproxy_test.go lines 80-100 (the existing 120s `Eventually` pod-wait at line 95 — REGRESSION GUARD; this plan does NOT modify the test file, but Task 1's verify asserts the file is UNTOUCHED to prove the cascade-8 fix is production-side only)
    - .planning/debug/credproxy-backoff-suppression.md §\"Fix landscape (corrected)\" Option δ (the verbatim implementation sketch at debug session lines 128-143 — Task 1's `<action>` uses this code verbatim)
    - .planning/phases/02.2-.../02.2-05-PLAN.md Task 1 body (the structural model — the `--webhook-cert-path` flag definition + webhook.Options.CertDir wire-up plan was structurally identical to THIS plan's Task 1: insert a small construction block + assign to existing struct literals; mirror Plan 02.2-05's regression-guard pattern (preserving all 9 pre-existing flag definitions and Plan 02.2-04's Metrics.BindAddress wire-up))
    - .planning/phases/02.2-.../02.2-09-VERIFICATION.md (the BLOCKED gate from which cascade-8 was diagnosed — useful for confirming the runtime evidence shape this plan is closing; note that §Section 5's \"ResourceVersion conflict → exp-backoff suppression\" framing was DISPROVEN by the debug investigation captured in `.planning/debug/credproxy-backoff-suppression.md`; the actual cause is production-wiring-gap)
    - CLAUDE.md (project guidelines — two-DAG split, water/tide vocabulary, pluggable subagent runtime contract; Dispatcher interface is the seam that satisfies the pluggable-subagent-runtime constraint)
  </read_first>

  <action>
    Three-part task in this single auto-task: (A) insert a `*podjob.PodJobBackend` construction in the Phase 2 component wiring block; (B) append `Dispatcher: dispatcher,` to the PlanReconciler struct literal; (C) append `Dispatcher: dispatcher,` to the TaskReconciler struct literal.

    **Part A — `cmd/manager/main.go`: insert dispatcher construction at the end of the Phase 2 component wiring block.**

    Locate the envReader construction line (currently at line 188):
    ```go
    	envReader := &podjob.FilesystemEnvelopeReader{WorkspaceRoot: "/workspaces"}
    ```

    Immediately AFTER that line (before the blank line that separates section 6 from section 7 \"Register all six reconcilers\"), insert the following verbatim block (sourced from `.planning/debug/credproxy-backoff-suppression.md` §\"Fix landscape (corrected) — Option δ implementation sketch\"):

    ```go
    	//    e. Dispatcher — wires PodJobBackend into both Plan and Task reconcilers' Phase 2
    	//       dispatch paths (cascade-8 fix per .planning/debug/credproxy-backoff-suppression.md).
    	//       Without this, plan_controller.go:121 and task_controller.go:167 short-circuit
    	//       and no Job is ever created in production.
    	dispatcher := &podjob.PodJobBackend{
    		Client:         mgr.GetClient(),
    		Scheme:         mgr.GetScheme(),
    		SubagentImage:  subagentImage,
    		CredproxyImage: credproxyImage,
    		SigningKey:     signingKey,
    		EnvReader:      envReader,
    		PVCName:        "tide-projects",
    	}
    ```

    Notes on the construction:
    - **Variable name:** `dispatcher` (camelCase, mirrors `envReader` / `budgetStore` / `signingKey` naming pattern in the surrounding section 6 wiring block).
    - **Field order:** Client, Scheme, SubagentImage, CredproxyImage, SigningKey, EnvReader, PVCName — matches the struct field order at `internal/dispatch/podjob/backend.go:70-79` verbatim (no field reordering; preserves the existing struct definition's argument-order convention).
    - **Scheme field value:** `mgr.GetScheme()` (NOT the local `scheme` variable). Both forms are functionally equivalent (single-scheme topology — the same `*runtime.Scheme` was passed to `ctrl.NewManager` via `Options.Scheme: scheme` at line 136), but `mgr.GetScheme()` is chosen for stylistic alignment with the reconciler literals at lines 222, 232, 242, 252, etc. (all of which use `mgr.GetScheme()`). If the debug sketch's `Scheme: scheme` is preferred, that's also acceptable — the verify gate counts the field's presence, not the specific accessor used.
    - **PVCName value:** the literal string `"tide-projects"` matches the chart-provisioned shared PVC name; this is the same value the TaskReconciler tests pre-create in BeforeSuite (e.g., the kind suite's `tide-projects` PVC fixture). The PodJobBackend.PVCName field's docstring at `backend.go:78` documents this default.
    - **Indentation:** tab-indented to match the surrounding section 6 sub-step `a..d` body indentation (single tab for the `dispatcher :=` line; single tab + tab for each field line). The `gofmt -d` gate enforces this.
    - **No new imports:** `podjob` is already imported at line 46. The Bash `grep -c '"github.com/jsquirrelz/tide/internal/dispatch/podjob"' cmd/manager/main.go` count is preserved verbatim (was 1, stays 1).

    **Part B — `cmd/manager/main.go`: append `Dispatcher: dispatcher,` to the PlanReconciler struct literal.**

    Locate the PlanReconciler setup block (currently lines 220-228):
    ```go
    	if err := (&controller.PlanReconciler{
    		Client:                  mgr.GetClient(),
    		Scheme:                  mgr.GetScheme(),
    		MaxConcurrentReconciles: cfg.MaxConcurrentReconciles.Plan,
    		PlannerPool:             plannerPool,
    		WatchNamespace:          watchNamespace,
    	}).SetupWithManager(mgr); err != nil {
    ```

    Append `Dispatcher: dispatcher,` as a new line AFTER `WatchNamespace: watchNamespace,` and BEFORE the closing `}).SetupWithManager(mgr);`:
    ```go
    	if err := (&controller.PlanReconciler{
    		Client:                  mgr.GetClient(),
    		Scheme:                  mgr.GetScheme(),
    		MaxConcurrentReconciles: cfg.MaxConcurrentReconciles.Plan,
    		PlannerPool:             plannerPool,
    		WatchNamespace:          watchNamespace,
    		Dispatcher:              dispatcher,
    	}).SetupWithManager(mgr); err != nil {
    ```

    Notes on the assignment:
    - **Field alignment:** match the existing alignment style of the struct literal — `MaxConcurrentReconciles:` is the longest field name in this literal (22 chars), so the values are aligned to column 26 (after `MaxConcurrentReconciles: `). Add `Dispatcher:` with spaces padding to align with the existing fields' value column. `gofmt` will normalize spacing — running `gofmt -w cmd/manager/main.go` after the edit is recommended to ensure alignment is canonical.
    - **Preserve existing fields verbatim:** Client, Scheme, MaxConcurrentReconciles, PlannerPool, WatchNamespace all stay byte-identical to their pre-edit state. The diff is purely additive (+1 line).
    - **Trailing comma:** the new `Dispatcher: dispatcher,` line MUST end with a comma (Go requires trailing commas in multi-line composite literals).

    **Part C — `cmd/manager/main.go`: append `Dispatcher: dispatcher,` to the TaskReconciler struct literal.**

    Locate the TaskReconciler setup block (currently lines 240-256):
    ```go
    	if err := (&controller.TaskReconciler{
    		Client:                  mgr.GetClient(),
    		Scheme:                  mgr.GetScheme(),
    		MaxConcurrentReconciles: cfg.MaxConcurrentReconciles.Task,
    		ExecutorPool:            executorPool,
    		WatchNamespace:          watchNamespace,
    		// Phase 2 fields (Plan 12 wiring).
    		Budget:         budgetStore,
    		Defaults:       defaults,
    		SigningKey:     signingKey,
    		SubagentImage:  subagentImage,
    		CredproxyImage: credproxyImage,
    		EnvReader:      envReader,
    	}).SetupWithManager(mgr); err != nil {
    ```

    Append `Dispatcher: dispatcher,` as a new line AFTER `EnvReader: envReader,` and BEFORE the closing `}).SetupWithManager(mgr);`:
    ```go
    	if err := (&controller.TaskReconciler{
    		Client:                  mgr.GetClient(),
    		Scheme:                  mgr.GetScheme(),
    		MaxConcurrentReconciles: cfg.MaxConcurrentReconciles.Task,
    		ExecutorPool:            executorPool,
    		WatchNamespace:          watchNamespace,
    		// Phase 2 fields (Plan 12 wiring).
    		Budget:         budgetStore,
    		Defaults:       defaults,
    		SigningKey:     signingKey,
    		SubagentImage:  subagentImage,
    		CredproxyImage: credproxyImage,
    		EnvReader:      envReader,
    		Dispatcher:     dispatcher,
    	}).SetupWithManager(mgr); err != nil {
    ```

    Notes on the assignment:
    - **Field alignment:** within the Phase 2 fields group (Budget, Defaults, SigningKey, SubagentImage, CredproxyImage, EnvReader, Dispatcher), `CredproxyImage:` is the longest field name (14 chars) so the values align to column 18. `Dispatcher: dispatcher,` aligns naturally with the group. `gofmt -w` normalizes spacing.
    - **Position:** Dispatcher is the LAST field of the Phase 2 group — placed AFTER EnvReader. This matches the order of Phase 2 wiring sub-steps in section 6 (a/b/c/d/e — e being dispatcher, the last). The comment `// Phase 2 fields (Plan 12 wiring).` at line 246 now correctly covers 7 fields (Budget through Dispatcher) instead of 6.
    - **Preserve all existing fields verbatim:** Client, Scheme, MaxConcurrentReconciles, ExecutorPool, WatchNamespace, Budget, Defaults, SigningKey, SubagentImage, CredproxyImage, EnvReader all stay byte-identical. The diff is purely additive (+1 line per reconciler block).
    - **Trailing comma:** required.

    **Constraints (load-bearing — surface the deliberate scope shift):**
    - **DO NOT** modify any production source OTHER than `cmd/manager/main.go`. Specifically: `internal/controller/`, `internal/dispatch/`, `internal/webhook/`, `api/`, `pkg/`, `charts/`, `Makefile`, `test/`, `.github/` ALL stay byte-identical.
    - **DO NOT** add the cascade-9 ValidationState fix. The Plan admission webhook at `internal/webhook/v1alpha1/plan_webhook.go:117-199` is intentionally NOT touched by this plan. The ValidationState gap is a separate cascade-9 follow-up debug session AFTER Plan 02.2-10's chain terminator lands. Resisting scope expansion is itself a load-bearing constraint per `.continue-here.md` anti-pattern #6.
    - **DO NOT** introduce a `NewPodJobBackend` constructor function in `internal/dispatch/podjob/backend.go`. Direct struct-literal construction is the established pattern at the existing tests' usage sites (verified at plan-time via `grep "PodJobBackend{" internal/`). Adding a constructor expands scope beyond Option δ.
    - **DO NOT** change the `dispatcher` variable name to something else (e.g., `podJobDispatcher`, `dispatch`, `backend`). The variable name `dispatcher` is what the debug session's Option δ sketch uses; it mirrors the surrounding section 6 naming pattern (`signingKey`, `budgetStore`, `envReader`); and it matches the struct field name `Dispatcher` it's assigned to.
    - **DO NOT** rename or modify the existing `Dispatcher` field declaration on either PlanReconciler or TaskReconciler. The field already exists (per Phase 2 Plan 09 work); this plan only ASSIGNS the field, it does not declare or rename it.
    - **DO NOT** modify the `if r.Dispatcher != nil` gates at `plan_controller.go:121` and `task_controller.go:167`. The gates already exist; once Dispatcher is non-nil, they unlock. No gate change is needed.
    - **DO NOT** bump the credproxy HARN-03 120s pod-wait timeout at `test/integration/kind/credproxy_test.go:95`. The debug evidence proved 120s is enormously generous once dispatch fires (passing run completes in <30s). If the planner is tempted to also bump the timeout \"defensively\", REJECT — that would re-trigger the test-driven over-accommodation anti-pattern the debug investigation explicitly rejected.
    - **DO NOT** modify Plans 02.2-01 through 02.2-09 or this plan (02.2-10). Task 3 (HARD-GATED on Task 2 APPROVED) is the only place 02.2-02-PLAN.md is touched.
    - **DO NOT** add new imports to `cmd/manager/main.go`. `podjob` is already imported at line 46. The Bash `grep -c '"github.com/jsquirrelz/tide/internal/dispatch/podjob"' cmd/manager/main.go` count is preserved verbatim (was 1, stays 1).
    - **DO NOT** modify the existing flag definitions, Manager construction, pool construction, signing key load, budget store, defaults, envReader, OR webhook server wiring. All Plans 02.2-04 and 02.2-05 work (metricsAddr, webhookCertPath, Metrics.BindAddress wire-up, webhook.Options.CertDir wire-up) is preserved verbatim.
    - **DO NOT** touch `charts/tide/values.yaml` per `.continue-here.md` anti-pattern #2.

    Commit with message:
    ```
    fix(02.2-10): wire PodJobBackend into Plan + Task reconcilers (Option δ — close cascade-8 production-wiring-gap per .planning/debug/credproxy-backoff-suppression.md)
    ```

    Body should reference: (a) the debug session at `.planning/debug/credproxy-backoff-suppression.md` (status: root_cause_found; Option δ wins; all 5 originally-hypothesized fixes REJECTED); (b) the Option-B-test-only contract is deliberately broken because the debug evidence proves test-only changes cannot close cascade-8; (c) cascade-9 ValidationState is identified but deferred to a separate follow-up debug session; (d) the structural analog is Plan 02.2-05 (the prior plan that surgically edited `cmd/manager/main.go`); (e) the diff is approximately +12 lines, all additive — zero controller-body changes, zero new imports.
  </action>

  <verify>
    <automated><![CDATA[
      # === Part A — dispatcher construction inserted ===
      [ "$(grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go)" -eq 1 ] && \
      # All 7 PodJobBackend fields present in the dispatcher construction region (lines 188-205 approx)
      [ "$(grep -c 'Client:.*mgr.GetClient' cmd/manager/main.go)" -ge 2 ] && \
      [ "$(grep -c 'Scheme:.*mgr.GetScheme' cmd/manager/main.go)" -ge 2 ] && \
      [ "$(grep -c 'SubagentImage:.*subagentImage' cmd/manager/main.go)" -ge 2 ] && \
      [ "$(grep -c 'CredproxyImage:.*credproxyImage' cmd/manager/main.go)" -ge 2 ] && \
      [ "$(grep -c 'SigningKey:.*signingKey' cmd/manager/main.go)" -ge 2 ] && \
      [ "$(grep -c 'EnvReader:.*envReader' cmd/manager/main.go)" -ge 2 ] && \
      [ "$(grep -c 'PVCName:.*"tide-projects"' cmd/manager/main.go)" -eq 1 ] && \
      # === Parts B+C — Dispatcher field assigned in BOTH reconciler literals ===
      [ "$(grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go)" -eq 2 ] && \
      # === Formatting + build + vet gates ===
      [ -z "$(gofmt -d cmd/manager/main.go)" ] && \
      go build ./cmd/manager/... && \
      go vet ./cmd/manager/... && \
      go build ./... && \
      go vet ./... && \
      # === Regression guards — existing imports preserved (no new imports added) ===
      [ "$(grep -c '"github.com/jsquirrelz/tide/internal/dispatch/podjob"' cmd/manager/main.go)" -eq 1 ] && \
      # === Regression guards — all 10 pre-existing flag definitions preserved verbatim (Plans 02.2-04, 02.2-05 work) ===
      [ "$(grep -c 'flag.StringVar.*"config"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.BoolVar.*"leader-elect"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"watch-namespace"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"metrics-bind-address"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"webhook-cert-path"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"subagent-image"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"credproxy-image"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"default-file-touch-mode"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.IntVar.*"rate-limit-default-rpm"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.IntVar.*"rate-limit-default-burst"' cmd/manager/main.go)" -eq 1 ] && \
      # === Regression guards — Plan 02.2-04 Metrics.BindAddress wire-up preserved ===
      [ "$(grep -c 'BindAddress:.*metricsAddr' cmd/manager/main.go)" -eq 1 ] && \
      # === Regression guards — Plan 02.2-05 webhook.Options.CertDir wire-up preserved ===
      [ "$(grep -c 'CertDir:.*webhookCertPath' cmd/manager/main.go)" -ge 1 ] && \
      # === Regression guards — Phase 2 envelope wiring preserved (Section 6 sub-steps a-d) ===
      [ "$(grep -c 'signingKey, err := decodeSigningKeyFromEnv' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'budgetStore := budget.NewStore' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'envReader := &podjob.FilesystemEnvelopeReader' cmd/manager/main.go)" -eq 1 ] && \
      # === Regression guards — production source OTHER than cmd/manager/main.go untouched ===
      [ -z "$(git status --porcelain api/)" ] && \
      [ -z "$(git status --porcelain pkg/)" ] && \
      [ -z "$(git status --porcelain charts/)" ] && \
      [ -z "$(git status --porcelain Makefile)" ] && \
      [ -z "$(git status --porcelain internal/controller/)" ] && \
      [ -z "$(git status --porcelain internal/dispatch/podjob/)" ] && \
      [ -z "$(git status --porcelain internal/webhook/)" ] && \
      [ -z "$(git status --porcelain test/)" ] && \
      # === Regression guards — Dispatcher field declarations untouched (this plan only ASSIGNS, never DECLARES) ===
      [ "$(grep -c 'Dispatcher dispatch.Dispatcher' internal/controller/plan_controller.go)" -eq 1 ] && \
      [ "$(grep -c 'Dispatcher dispatch.Dispatcher' internal/controller/task_controller.go)" -eq 1 ] && \
      # === Regression guards — gated dispatch paths untouched ===
      [ "$(grep -c 'if r.Dispatcher != nil' internal/controller/plan_controller.go)" -ge 1 ] && \
      [ "$(grep -c 'if r.Dispatcher != nil' internal/controller/task_controller.go)" -ge 1 ] && \
      # === Regression guards — Plan 02.2-09 fixture work preserved ===
      [ "$(grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go)" -eq 1 ] && \
      [ "$(grep -c 'func applyHierarchy' test/integration/kind/suite_test.go)" -eq 1 ] && \
      # === Regression guards — Plan 02.2-08 cascade-6 timeout bumps preserved ===
      [ "$(grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go)" -eq 1 ] && \
      [ "$(grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go)" -eq 1 ] && \
      # === Regression guards — Plan 02.2-06 Makefile timeout bump preserved ===
      [ "$(grep -c 'timeout 600s' Makefile)" -ge 1 ] && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Part A — dispatcher construction inserted:** `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returns exactly `1` (was 0 pre-edit)
    - **Part A — all 7 PodJobBackend fields populated:** the dispatcher construction region contains lines matching each of: `Client:.*mgr.GetClient`, `Scheme:.*mgr.GetScheme`, `SubagentImage:.*subagentImage`, `CredproxyImage:.*credproxyImage`, `SigningKey:.*signingKey`, `EnvReader:.*envReader`, AND `PVCName:.*"tide-projects"` (the first 6 appear in BOTH the dispatcher construction AND the existing reconciler literals — grep count ≥ 2; PVCName only appears in the dispatcher construction — grep count == 1)
    - **Parts B+C — Dispatcher assigned in BOTH reconciler literals:** `grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go` returns exactly `2` (one in PlanReconciler, one in TaskReconciler)
    - **Formatting:** `gofmt -d cmd/manager/main.go` returns empty (no formatting drift)
    - **Compile gates:** `go build ./cmd/manager/...` exits 0 AND `go vet ./cmd/manager/...` exits 0 AND `go build ./...` exits 0 (full project build) AND `go vet ./...` exits 0
    - **Imports unchanged:** `grep -c '"github.com/jsquirrelz/tide/internal/dispatch/podjob"' cmd/manager/main.go` returns exactly `1` (same as pre-edit; no new imports added)
    - **All 10 pre-existing flag definitions preserved verbatim (Plans 02.2-04, 02.2-05 work):** each `grep -c` for the 10 flags returns exactly 1
    - **Plan 02.2-04 Metrics.BindAddress wire-up preserved:** `grep -c 'BindAddress:.*metricsAddr' cmd/manager/main.go` returns exactly 1
    - **Plan 02.2-05 webhook.Options.CertDir wire-up preserved:** `grep -c 'CertDir:.*webhookCertPath' cmd/manager/main.go` returns ≥ 1
    - **Phase 2 envelope wiring preserved (Section 6 sub-steps a-d):** signingKey load, budgetStore, envReader constructions each grep ≥ 1
    - **Production source OTHER than cmd/manager/main.go untouched:** `git status --porcelain <path>` returns empty for each of `api/`, `pkg/`, `charts/`, `Makefile`, `internal/controller/`, `internal/dispatch/podjob/`, `internal/webhook/`, `test/`
    - **Dispatcher field declarations untouched (this plan only ASSIGNS, never DECLARES):** `grep -c 'Dispatcher dispatch.Dispatcher' internal/controller/plan_controller.go` returns 1 AND same for `task_controller.go`
    - **Gated dispatch paths untouched:** `grep -c 'if r.Dispatcher != nil' internal/controller/plan_controller.go` returns ≥ 1 AND same for `task_controller.go`
    - **Plan 02.2-09 fixture work preserved:** `grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go` returns 1 AND `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` returns 1
    - **Plan 02.2-08 cascade-6 timeout bumps preserved:** `grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go` returns 1 AND `grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go` returns 1
    - **Plan 02.2-06 Makefile timeout bump preserved:** `grep -c 'timeout 600s' Makefile` returns ≥ 1
  </acceptance_criteria>

  <done>
    `cmd/manager/main.go` has a NEW `dispatcher := &podjob.PodJobBackend{...}` construction inserted immediately after the existing `envReader := &podjob.FilesystemEnvelopeReader{...}` line in the Phase 2 component wiring block (Section 6, sub-step e). All 7 PodJobBackend fields (Client, Scheme, SubagentImage, CredproxyImage, SigningKey, EnvReader, PVCName) are populated from in-scope variables. Both the PlanReconciler struct literal (lines 220-228) AND the TaskReconciler struct literal (lines 240-256) have `Dispatcher: dispatcher,` appended as their LAST field. `gofmt -d cmd/manager/main.go` returns empty. `go build ./...` AND `go vet ./...` both exit 0. No new imports added (podjob was already imported at line 46). All Plans 02.2-04 (metricsAddr / Metrics.BindAddress) and 02.2-05 (webhookCertPath / webhook.Options.CertDir) work preserved verbatim. Dispatcher field declarations on both reconciler structs untouched (this plan only ASSIGNS, never DECLARES). Plan 02.2-09 fixture work (createProjectHierarchy helper + 3 call-sites), Plan 02.2-08 cascade-6 timeout bumps (120s/240s), and Plan 02.2-06 Makefile timeout bump (600s) all preserved. No other production source modified: `api/`, `pkg/`, `charts/`, `Makefile`, `internal/controller/`, `internal/dispatch/podjob/`, `internal/webhook/`, `test/` ALL stay byte-identical. The Option-B-test-only contract is deliberately broken by this plan, and the cascade-9 ValidationState gap is identified but deferred (per `.planning/debug/credproxy-backoff-suppression.md` and the `<execution_notes>` anti-pattern #6). Committed with the `fix(02.2-10): wire PodJobBackend into Plan + Task reconcilers (Option δ — close cascade-8 production-wiring-gap per .planning/debug/credproxy-backoff-suppression.md)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-10-VERIFICATION.md (clean run + rerun + gate decision + 5 runtime-observation fields including NEW dispatch_active boolean)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-09-VERIFICATION.md (the structural template this artifact mirrors — frontmatter shape including the four observation fields from Plan 02.2-09; Sections 1–6 layout; per-spec outcome tables; root-cause analysis prose; threat-model-mitigations-applied list; \"What This Artifact Did Not Touch\" closing section. THIS plan's verification adds ONE MORE observation field — `dispatch_active: true|false` — on top of the existing four from Plan 02.2-09.)
    - .planning/phases/02.2-.../02.2-09-PLAN.md Task 2 body (the structural model — runtime verification reproducer pattern with clean run + idempotent rerun + diagnostic snapshot + per-spec outcome table + gate decision; THIS plan's Task 2 substitutes the cascade-8 production-wiring-gap verification for cascade-7's harness-bug verification)
    - .planning/phases/02.2-.../02.2-09-SUMMARY.md (Plan 02.2-09's summary — for cascade-7 closure evidence and cascade-8 root cause framing as captured at Plan 02.2-09 BLOCKED-gate time; note that §Section 5's \"ResourceVersion conflict → exp-backoff suppression\" framing was DISPROVEN by the subsequent debug investigation captured in `.planning/debug/credproxy-backoff-suppression.md`)
    - .planning/debug/credproxy-backoff-suppression.md (PRIMARY authoritative gap source — root_cause_found; Option δ wins; all 5 originally-hypothesized options REJECTED with rationale. §Section 5 of THIS plan's verification artifact MUST cite this debug session as the canonical source for the cascade-8 root cause AND the Option δ fix rationale)
    - cmd/manager/main.go (post-Task-1 state — verify the dispatcher construction AND both Dispatcher field assignments are present before this task runs)
    - internal/controller/plan_controller.go (regression guard — verify Dispatcher field declaration at line 57 untouched)
    - internal/controller/task_controller.go (regression guard — verify Dispatcher field declaration at line 77 untouched AND the Job-create call at line 393 untouched; this line is the smoking-gun reachable-via-Dispatcher line referenced in the debug investigation)
    - test/integration/kind/credproxy_test.go (regression guard — verify untouched; cascade-5/6 fixes + 120s pod-wait preserved verbatim)
    - test/integration/kind/suite_test.go (regression guard — verify Plan 02.2-09's createProjectHierarchy + applyHierarchy both present)
    - Makefile (verify `timeout 600s` budget from Plan 02.2-06 is still in place — `grep -c 'timeout 600s' Makefile` ≥ 1)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-09-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md` using the same structural template PLUS one MORE runtime-observation field (`dispatch_active: true|false`) on top of the existing four from Plan 02.2-09 (`credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds`).

    **Recovery-lesson PRE-STEP (codified protocol from `.continue-here.md` Plan 02.2-05 lesson, applied in 02.2-06/07/08/09):** BEFORE running the clean reproducer, author `02.2-10-VERIFICATION.md` with PRELIMINARY frontmatter — the load-bearing fields (`phase: 02.2`, `plan: 10`, `task: 2`, `artifact: verification`, `captured_on: <today>`, `captured_by_host: <host>`, `captured_by_branch: <branch>`, placeholder `gate_decision: PENDING` line, AND placeholders `credproxy_pod_wait_observed_seconds: -1`, `ac3_phase_wait_observed_seconds: -1`, `caps_observed_seconds: -1`, `output_observed_seconds: -1`, `dispatch_active: unknown`) — so that if mid-run executor API errors or context window pressure interrupt the long-running runtime verification, the artifact's gate field still exists with a recoverable PENDING state. Then update `gate_decision: PENDING` → APPROVED or BLOCKED and the five observation fields to real values after Sub-step C completes.

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit (set -o pipefail makes the bash captured exit reflect make's real exit, not tee's):
    set -o pipefail
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-10-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state for the artifact:**
    ```bash
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-10-clean-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-10-clean-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-10-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=20m 2>&1 | tee /tmp/02.2-10-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-10-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-10-clean-pvc-phase.txt || true
    # Cascade-7 closure regression check (Plan 02.2-09 fix preserved)
    grep -E 'no project found.*(credproxy|caps|output|failure)' /tmp/02.2-10-clean-manager-logs.txt 2>&1 | tee /tmp/02.2-10-clean-cascade7-regression-check.txt || true
    # CASCADE-8 CLOSURE EVIDENCE (NEW for THIS plan — load-bearing): dispatch path actually fired
    grep -E 'dispatch|creating job|Reconciling Task|reconcileDispatch' /tmp/02.2-10-clean-manager-logs.txt 2>&1 | tee /tmp/02.2-10-clean-dispatch-evidence.txt || true
    # Job CRDs in any test namespace = direct evidence dispatch fired
    for ns in caps-test output-test failure-test credproxy-test; do
      echo "=== Namespace: $ns ===" | tee -a /tmp/02.2-10-clean-jobs.txt
      kubectl --context kind-tide-test get jobs -n $ns 2>&1 | tee -a /tmp/02.2-10-clean-jobs.txt || true
    done
    # Per-spec timings extraction
    grep -E 'A Pod should be created|Wait for β|beta-fail.*Failed|hang-task|exceed-output-task|seconds elapsed' /tmp/02.2-10-clean-run.log 2>&1 | tee /tmp/02.2-10-clean-spec-timings.txt || true
    # Per-fixture namespace state — assert parent Project exists in each
    for ns in caps-test output-test failure-test credproxy-test; do
      echo "=== Namespace: $ns ===" | tee -a /tmp/02.2-10-clean-namespaces.txt
      kubectl --context kind-tide-test get project,milestone,phase,plan,task,job,pod -n $ns 2>&1 | tee -a /tmp/02.2-10-clean-namespaces.txt || true
    done
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block — APPROVED case shows 7 Pass / 0 Skip / 0 Fail; BLOCKED case shows partial outcome with cascade-9). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-09-VERIFICATION.md` §Section 1.

    **Five runtime-observation fields (LOAD-BEARING — T-02.2-21 carry-forward + NEW dispatch_active for cascade-8 closure indicator):** Extract observed elapsed times for the four key specs PLUS the boolean indicator:
    - `credproxy_pod_wait_observed_seconds`: time-to-Pod-creation for the credproxy HARN-03 pod-wait spec. **EXPECTED DROP from Plan 02.2-09's 120 to well under 30 once Option δ wires the dispatch path.** If observed ≥ 60s, surface as an ALERT signal in §Section 5.
    - `ac3_phase_wait_observed_seconds`: time-to-Failed-phase for the AC3 beta-fail spec (Plan 02.2-09 recorded -1 because the spec did not run; Plan 02.2-10 should measure for the first time post-dispatch-wiring)
    - `caps_observed_seconds`: total elapsed time for the AC5 hang-task spec (Plan 02.2-09 recorded -1 because spec did not run; Plan 02.2-10 should measure successful completion)
    - `output_observed_seconds`: total elapsed time for the AC5 exceed-output-task spec (Plan 02.2-09 recorded -1 because spec did not run; Plan 02.2-10 should measure for the first time)
    - `dispatch_active: true|false` (NEW — cascade-8 closure indicator): set `true` if EITHER (a) `kubectl get jobs` returns ≥ 1 Job CRD across any of the 4 test namespaces during the run, OR (b) manager log contains ANY of the literal tokens `Reconciling Task`, `dispatch`, `creating job`, `reconcileDispatch` (case-insensitive grep is acceptable). Set `false` if NEITHER condition holds — meaning the dispatch path is STILL gated off even with Dispatcher wired (which would indicate a deeper production-wiring-gap; surface as cascade-9 in §Section 5).

    Record into the artifact's frontmatter:
    ```
    credproxy_pod_wait_observed_seconds: <N>
    ac3_phase_wait_observed_seconds: <M>
    caps_observed_seconds: <C>
    output_observed_seconds: <O>
    dispatch_active: true | false
    ```

    If any numeric value cannot be precisely extracted from Ginkgo output, use the spec's TOTAL duration from the Ginkgo summary as a conservative upper bound and add a clarifying note in §Section 1 (\"observed seconds = total spec duration; the dominant cost within the spec is the assertion's Eventually wait\").

    **T-02.2-21 carry-forward marginal-flag check (REVISED for cascade-8 closure):** If `credproxy_pod_wait_observed_seconds ≥ 60` (lower threshold than Plan 02.2-09's 90 — because the debug evidence proved a passing dispatch run completes in <30s, anything ≥ 60s indicates SOMETHING is still blocking), the artifact MUST contain the literal token `dispatch latency marginal` in §Section 1 AND the closing prose. The flag does NOT block APPROVED (if 7/7 specs still PASS) but signals that the new dispatch path is being approached even after cascade-8 closure — likely cascade-9 ValidationState gap surfacing for the Plan reconciler's Wave materialization path.

    **Cascade-8 closure evidence (NEW for THIS plan — LOAD-BEARING):** The artifact's §Section 1 manager-log evidence MUST explicitly assert:
    - Plan 02.2-09's cascade-7 closure preserved: zero `no project found in namespace <ns>` entries for any of caps-test/output-test/failure-test/credproxy-test
    - Cascade-8 closure indicator: `dispatch_active: true` based on EITHER Job CRDs in test namespaces OR `Reconciling Task` / `dispatch` / `creating job` / `reconcileDispatch` log entries

    If `dispatch_active: false`, cascade-8 is NOT closed and §Section 5 MUST diagnose why (likely options: (a) Task 1's Dispatcher wiring is structurally correct but the controller's informer-cache lag is still preventing reconciles — surface as cascade-9 sub-class; (b) the ValidationState gate at `plan_controller.go:147` is also blocking Task dispatch via an indirect dependency we missed; (c) the PodJobBackend.BuildJobSpec path has a latent bug that panics or returns early without `r.Create(ctx, job)` firing).

    **APPROVED case clean-run state:** Pod `READY 1/1 STATUS Running RESTARTS ≤ 1`; manager logs show \"starting manager\" + at least one `dispatch` / `Reconciling Task` log line per non-credproxy fixture namespace; NO `flag provided but not defined`; NO `no project found in namespace <ns>` for any of caps-test/output-test/failure-test/credproxy-test; manager logs show successful dispatch chains for hang-task (Job-create + Pod-run + activeDeadlineSeconds kill + Task-Failed) AND exceed-output-task (Job-create + Pod-run + harness validate-fail + Task-Failed) AND beta-fail (Job-create + Pod-run + exit-1 + Task-Failed) AND alpha-fail (Job-create + Pod-run + exit-0 + Task-Succeeded) AND credproxy-task (Job-create + Pod-run with credproxy sidecar Ready); Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed`; the four test namespaces each contain the full hierarchy (Project + Milestone + Phase + their respective Plan + Task(s)) + ≥ 1 Job + ≥ 1 Pod.

    **BLOCKED case A — cascade-9 surfaces as `production-wiring-gap` (ValidationState):** Manager Pod state Running; cascade-7 fix preserved; cascade-8 Option δ wired correctly (`dispatch_active: true` for credproxy / Task-centric specs); but PlanReconciler.reconcileWaveMaterialization short-circuits at `plan_controller.go:147` for the wave-test fixture (which depends on Wave materialization to dispatch its 3 tasks in topological order). Cascade-9 classified as `other` → `production-wiring-gap` sub-class — fix shape: wire `plan.Status.ValidationState = \"Validated\"` in the Plan admission webhook OR add a PlanReconciler validation step. **This is the SECONDARY production-wiring-gap that the debug investigation explicitly identified at `.planning/debug/credproxy-backoff-suppression.md` §\"Secondary risk surfaced during investigation (NOT cascade-8 itself)\". Surface in §Section 5 with reference to the debug session for the canonical analysis.**

    **BLOCKED case B — cascade-9 surfaces as `dispatch_active: false`:** Task 1's Dispatcher wiring is structurally correct (grep counts match) but no Job is ever created. This would mean the `if r.Dispatcher != nil` gate is NOT the only thing blocking dispatch. §Section 5 MUST cite specific log evidence (e.g., reconcileDispatch enters but exits early; or Reconciling Task fires but never reaches Step 11 r.Create). This is an UNEXPECTED outcome — the debug evidence specifically scoped cascade-8 to Option δ; if dispatch_active is false, the debug investigation needs to expand to a new sub-class.

    **BLOCKED case C — cascade-9 surfaces as `budget-vs-actual`:** Manager Pod state Running; dispatch path live (`dispatch_active: true`); but the WALL exceeds 600s now that all 4 fixtures run to completion with real Job dispatch. Cascade-9 classified as `budget-vs-actual` — fix shape: deferred Layer A/Layer B split into separately-timed Makefile targets.

    **BLOCKED case D — cascade-9 surfaces as `spec-flake` in wave/failure-graph dynamics:** A spec that previously hadn't run end-to-end with the full dispatch path (e.g., the AC3 γ-never-dispatches Consistently assertion, or one of the wave-internal-ordering assertions) flakes for the first time. Cascade-9 classified as `spec-flake` — fix shape: targeted `Eventually`/`Consistently` budget bump or assertion logic fix.

    **BLOCKED case E — cascade-9 surfaces as `kind-infra`:** image-pull regression, etcd race, networking quirk — surfaces only now that all 4 fixtures dispatch real Jobs and stress the kind cluster's resource limits. Cascade-9 classified as `kind-infra`.

    **BLOCKED case F — cascade-9 surfaces as `flag-mismatch` or other prior-cascade regression:** if EITHER `flag provided but not defined` OR `no project found` for any namespace appears in manager logs, a PRIOR cascade has regressed. Surface in §Section 5 with full diagnostic citing which prior Plan's fix regressed.

    **BLOCKED case G — latent bug in PodJobBackend.BuildJobSpec or related dispatch code surfaces under live execution:** the dispatch code path runs for the first time in production; if there's a latent bug (e.g., panic on a specific Task shape, leak of Jobs, RBAC failure), it surfaces here. Cascade-9 classified as `other` → `latent-dispatch-bug` sub-class — fix shape: route to a separate debug session before Plan 02.2-11.

    **Sub-step B — Idempotent Re-run:**
    ```bash
    set -o pipefail
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-10-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    After rerun: capture the same diagnostic snapshot (`/tmp/02.2-10-rerun-*.txt`) mirroring Sub-step A's snapshot list with `-rerun-` substituted for `-clean-`. Re-extract the five observation fields from the rerun Ginkgo output; if any value differs materially between clean and rerun, note the variance in §Section 2 commentary.

    **Sub-step C — Author the artifact:**
    Write `02.2-10-VERIFICATION.md` mirroring `02.2-09-VERIFICATION.md`'s structural template. Required frontmatter (after Sub-step C, update PRELIMINARY `gate_decision: PENDING` to APPROVED or BLOCKED AND populate the five observation fields):
    ```
    ---
    phase: 02.2
    plan: 10
    task: 2
    artifact: verification
    captured_on: <YYYY-MM-DD>
    captured_by_host: <host>
    captured_by_branch: <git branch / worktree name>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    credproxy_pod_wait_observed_seconds: <int>
    ac3_phase_wait_observed_seconds: <int>
    caps_observed_seconds: <int>
    output_observed_seconds: <int>
    dispatch_active: <true|false>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary including cascade-9 type if BLOCKED, or 'cascade chain terminated' if APPROVED>
    ---
    ```

    Required sections (mirror `02.2-09-VERIFICATION.md` headers verbatim):
    - **Section 1 — Clean Start Run** (reproducer + per-spec outcome table + wall-time + exit + Pod-state evidence + manager-log evidence as the load-bearing diagnostic for cascade-8 closure (`dispatch_active: true` AND dispatch / Reconciling Task / creating job log lines visible) + Job CRDs in test namespaces + five runtime-observation fields with marginal-flag check)
    - **Section 2 — Idempotent Re-run** (reproducer + per-spec outcome table + wall-time + exit + rerun-specific evidence; re-extract the five observation fields for variance commentary)
    - **Section 3 — Before/After Comparison** (table comparing Plan 02.2-09 BLOCKED outcome (cascade-8 production-wiring-gap — dispatch path gated off; credproxy HARN-03 failed at 146s after 120s budget; 0/7 specs PASS) vs Plan 02.2-10 outcomes on clean + rerun; particularly: did the Option δ Dispatcher wiring close the cascade-8 gate, what is the new credproxy pod-wait observation (expected drop from 120s → <30s), what are the new caps/output/ac3 observations (Plan 02.2-09 didn't measure them — first-time observations expected), is dispatch_active true, and what is the NEW failure mode if BLOCKED again — what cascade-9 type per the BLOCKED cases A–G above)
    - **Section 4 — Wall-time Budget Check** (same table shape as `02.2-09-VERIFICATION.md` §4; both runs must show go test ≤ 600s for APPROVED; T-02.2-14 marginal-headroom flag if observed wall ≥ 500s; T-02.2-21 marginal-dispatch-latency flag if observed credproxy pod-wait ≥ 60s — REVISED threshold from Plan 02.2-09's 90s)
    - **Section 5 — Gate Decision** (the APPROVED-requires-ALL table with 5 conditions on BOTH runs + Required Next Step + Root-Cause Summary if BLOCKED + Fix landscape if BLOCKED. **CRITICAL FRAMING:** §Section 5 MUST: (a) preserve the corrected root cause from `02.2-07-VERIFICATION.md` §Section 5 — the dispatch gate for cascade-5/7 is `resolveProject` at `task_controller.go:217`; (b) surface the cascade-8 root cause analysis: `Dispatcher` field never assigned in `cmd/manager/main.go` — the Phase 2 dispatch paths are gated off at `plan_controller.go:121` and `task_controller.go:167` until `Dispatcher != nil`; (c) CITE `.planning/debug/credproxy-backoff-suppression.md` as the canonical debug session that diagnosed cascade-8 AND rejected Options α/β-1/β-2/β-3/γ; (d) surface the deliberate Option-B-test-only-contract BREAK — production-side fix is the only fix shape that closes cascade-8, per debug evidence; (e) if APPROVED, surface that cascade-9 ValidationState production-wiring-gap remains as a flagged follow-up debug session (per debug session §\"Secondary risk\") even though it doesn't block HARN-03. DO NOT inherit `02.2-06-VERIFICATION.md`'s discredited \"Wave Ready\" framing OR `02.2-09-VERIFICATION.md`'s discredited \"ResourceVersion conflict + exp-backoff\" framing.)
    - **Section 6 — Threat-Model Mitigations Applied** (T-02.2-28 / T-02.2-29 / T-02.2-30 / T-02.2-31 each with evidence + carry-forward T-02.2-01 through T-02.2-27 noted as preserved)
    - **\"What This Artifact Did Not Touch\"** closing section (specifically: did NOT bump the credproxy 120s pod-wait timeout per the debug evidence proving 120s is enormously generous once dispatch fires; did NOT fix cascade-9 ValidationState; did NOT modify any production source OTHER than cmd/manager/main.go; did NOT touch chart values)
    - **\"Closing Prose\"** with one-paragraph summary including: (a) the corrected root cause framing for cascade-8 (Dispatcher never assigned, not the disproven backoff-suppression hypothesis); (b) AND (if APPROVED) the chain-terminator v3 note — first APPROVED gate in Phase 02.2's 10-plan cascade chain; OR (if BLOCKED) the cascade-9 classification + Plan 02.2-11 fix shape

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/` (Task 1's `cmd/manager/main.go` edit is already committed before this task runs; the runtime reproducer only READS). Task 3 edits Plan 02.2-02 (a planning artifact, not source) — and only fires if `gate_decision: APPROVED`. Document this explicitly in the \"What This Artifact Did Not Touch\" closing section.

    **APPROVED gate (5-of-5 must hold on BOTH clean run AND idempotent rerun):**
    1. **Wall-time ≤ 600s:** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` (Plan 02.2-06 budget preserved); no `timeout 600s` exit-124 line in either captured log
    2. **Manager Pod state:** `tide-controller-manager` Pod `STATUS=Running` with `RESTARTS ≤ 1` (Plans 02.2-03/04/05 fixes preserved)
    3. **Manager log clean AND dispatch active (THREE sub-conditions — THIS is the cascade-8 closure indicator):** (a) zero `flag provided but not defined` errors of any kind (Plans 02.2-04/05 preserved); (b) zero `no project found in namespace <ns>` for any of caps-test/output-test/failure-test/credproxy-test (Plans 02.2-07/09 cascade-5/7 fixes preserved); (c) `dispatch_active: true` — evidence that `r.Create(ctx, job)` actually fired (Job CRDs in test namespaces OR `dispatch` / `creating job` / `Reconciling Task` / `reconcileDispatch` log lines)
    4. **PVC preserved:** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO` (Plan 02.2-03's cascade-1 fix preserved)
    5. **7/7 Layer B Ginkgo specs PASS:** Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed`; per-spec table records each spec as PASS on both clean AND rerun — **THIS is achievable for the first time in the cascade chain because all 8 prior cascades are now closed**

    **BLOCKED gate fires if ANY of the 5 conditions fail on either run.** The artifact's §Section 5 Root-Cause Summary documents WHICH gate failed, with raw log evidence, and §Fix landscape enumerates the next-iteration options based on cascade-9 classification (cases A–G above).

    Task 3 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring; Plans 02.2-03/04/05/06/07/08/09 Task 3 ALL DID NOT FIRE).

    **If gate_decision = APPROVED:** This is the CHAIN TERMINATOR — Phase 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave. The closing prose should:
    - Note the cascade chain has terminated successfully after 8 tactical iterations (02.2-01 source-shape + 02.2-03 chart PVC + 02.2-04 metrics flag + 02.2-05 webhook-cert-path flag + 02.2-06 Makefile timeout + 02.2-07 credproxy fixture + 02.2-08 Eventually timeout bumps + 02.2-09 createProjectHierarchy + 02.2-10 Dispatcher wiring)
    - Note the cascade-9 ValidationState production-wiring-gap remains as a flagged follow-up debug session (per `.planning/debug/credproxy-backoff-suppression.md` §\"Secondary risk\") — but it does NOT block HARN-03 because TaskReconciler.reconcileDispatch is not gated on ValidationState. If wave-test passes despite the ValidationState gap (because the wave testdata YAML triggers the right path), the cascade-9 follow-up is a nice-to-have refactor, not a blocker.
    - Note the deliberate Option-B-test-only-contract break: production-side gap-closure plans are legitimate when debug evidence shows test-side changes cannot close the cascade
    - Note T-02.2-14 marginal-headroom check (clean/rerun wall vs 500s) AND T-02.2-21 marginal-dispatch-latency check (observed credproxy pod-wait vs 60s — REVISED threshold)
    - State that Plan 02.2-02 (closeout) is unblocked once Task 3 wires its checkpoint to read this artifact, and that Phase 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave

    **Section 5 framing — load-bearing corrected root cause + cascade-8 root cause + scope-shift surfacing:** The artifact's §Section 5 prose MUST:
    1. Preserve `02.2-07-VERIFICATION.md` §Section 5's corrected framing for cascade-5/7: the harness-bug dispatch gate is `resolveProject` at `task_controller.go:217`. Do NOT re-inherit the wrong \"Wave Ready check\" framing from `02.2-06-VERIFICATION.md`.
    2. State explicitly that `02.2-09-VERIFICATION.md` §Section 5's \"ResourceVersion conflict + exp-backoff suppression\" framing was DISPROVEN by the debug investigation. The actual cascade-8 root cause: `Dispatcher` field never assigned in `cmd/manager/main.go` — Phase 2 dispatch paths gated off in production.
    3. Surface the cascade-8 root cause analysis: the `Dispatcher` field on PlanReconciler and TaskReconciler was DESIGNED to be wired (Phase 2 Plan 09 work), but `cmd/manager/main.go` never instantiated the `*podjob.PodJobBackend` or assigned it. The `if r.Dispatcher != nil` gates short-circuited; `r.Create(ctx, job)` at `task_controller.go:393` was never reached in production. CITE `.planning/debug/credproxy-backoff-suppression.md` as the canonical source.
    4. Surface the deliberate Option-B-test-only-contract break: production-side fix is the only fix shape that closes cascade-8, per debug evidence rejecting Options α/β-1/β-2/β-3/γ. Test-side changes (timeout bumps, status subresource, serialize/debounce, aggressive requeue) were all REJECTED for documented reasons.
    5. Surface the cascade-9 ValidationState production-wiring-gap (the debug session's \"Secondary risk\"): the Plan admission webhook validates but never updates Status.ValidationState; PlanReconciler.reconcileWaveMaterialization short-circuits if !Validated. Note that TaskReconciler.reconcileDispatch does NOT depend on ValidationState, so HARN-03 closes via Option δ alone. If APPROVED, cascade-9 is a flagged follow-up. If BLOCKED (case A), cascade-9 is the root cause.
    6. If T-02.2-21 marginal-flag fires (observed credproxy pod-wait ≥ 60s — REVISED threshold), the closing prose flags this as a CANDIDATE for cascade-9 ValidationState wiring (the most likely OTHER thing blocking dispatch beyond cascade-8).

    Commit with one of:
    ```
    docs(02.2-10): runtime verification — APPROVED gate (cascade-8 Dispatcher-wiring closed; 7/7 Layer B specs PASS clean + rerun within 600s; CHAIN TERMINATOR v3 — FIRST APPROVED in Phase 02.2 history)
    ```
    OR
    ```
    docs(02.2-10): runtime verification — BLOCKED gate (Dispatcher wiring landed; cascade-9 surfaced: <cascade-9-type-and-summary>)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists at the correct path
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md ] && \
      # Frontmatter required fields
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 10' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^task: 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^artifact: verification' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -eq 1 ] && \
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # All FIVE runtime-observation fields populated (four carry-forward + one new dispatch_active)
      grep -E '^credproxy_pod_wait_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -E '^ac3_phase_wait_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -E '^caps_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -E '^output_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -E '^dispatch_active: (true|false)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Required structural sections exist (six numbered sections)
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 4' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times (both runs)
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -E '^rerun_exit: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Threat-model mitigations applied section exists with all four NEW threats referenced
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-28' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-29' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-30' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-31' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      # Cascade classification present (per T-02.2-31 acceptance) — one of the classes must appear, or "cascade chain terminated"
      grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|production-wiring-gap|latent-dispatch-bug|other|cascade chain terminated' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Section 5 corrected-root-cause framing preserved — MUST cite resolveProject (cascade-5/7) AND Dispatcher (cascade-8)
      grep -F 'resolveProject' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      grep -F 'Dispatcher' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Cascade-8 root cause surfaced — MUST cite the debug session
      grep -E 'credproxy-backoff-suppression|production-wiring-gap|Option δ|Option delta' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Option-B-test-only contract break surfaced
      grep -E 'Option.B.test.only|Option-B-test-only|production.code|production-side' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Cascade-9 ValidationState gap referenced
      grep -E 'ValidationState|cascade.9|cascade-9' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Manager-log evidence captured
      [ "$(grep -c 'starting manager\|flag provided but not defined\|tide-controller-manager\|task cleanup\|dispatch\|no project found\|Reconciling' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      # Pod-state evidence captured
      [ "$(grep -c 'CrashLoopBackOff\|Running\|RESTARTS\|kubectl.*get pod' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      # 600s budget referenced
      [ "$(grep -c '600s\|≤ 600\|<= 600' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-21 marginal-flag check rendered (REVISED threshold 60s)
      grep -E 'dispatch latency marginal|dispatch latency.*not marginal|observed.*<.*60|observed.*<.*30' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # Carry-forward threat register acknowledgement: T-02.2-01 through T-02.2-27 noted as preserved
      grep -E 'T-02.2-(0[1-9]|1[0-9]|2[0-7])' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md && \
      # No production source modified by this task
      ! (git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-10-VERIFICATION.md' | grep -v '^$' >/dev/null) && \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-10-VERIFICATION.md ]` returns true
    - **Frontmatter required fields:** `grep -c '^phase: 02.2'` returns 1 AND `grep -c '^plan: 10'` returns 1 AND `grep -c '^task: 2'` returns 1 AND `grep -c '^artifact: verification'` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$'` matches (PENDING is not a final state)
    - **All five runtime-observation fields populated:** four numeric fields each match `^<name>: -?[0-9]+$` AND new boolean `^dispatch_active: (true|false)$` matches
    - **Required runtime fields populated:** `clean_run_exit` AND `clean_run_wall_seconds` AND `rerun_exit` AND `rerun_wall_seconds` all match their integer patterns
    - **All six structural sections present:** each `grep -c '^## Section [1-6]'` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all four new threats referenced:** T-02.2-28 AND T-02.2-29 AND T-02.2-30 AND T-02.2-31 each grep ≥ 1
    - **Cascade-9 classification (or chain-terminated note) present:** one of the seven classes (flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | production-wiring-gap | latent-dispatch-bug | other) OR \"cascade chain terminated\"
    - **Section 5 corrected-root-cause framing preserved (load-bearing — cascade-5/7 + cascade-8):** `grep -F 'resolveProject'` matches AND `grep -F 'Dispatcher'` matches
    - **Cascade-8 root cause surfaced + debug session cited:** `grep -E 'credproxy-backoff-suppression|production-wiring-gap|Option δ|Option delta'` matches
    - **Option-B-test-only contract break surfaced:** `grep -E 'Option.B.test.only|Option-B-test-only|production.code|production-side'` matches
    - **Cascade-9 ValidationState gap referenced as follow-up:** `grep -E 'ValidationState|cascade.9|cascade-9'` matches
    - **Manager-log evidence captured (load-bearing for gate decision):** the artifact includes a verbatim excerpt of `kubectl logs deploy/tide-controller-manager` showing dispatch / Reconciling Task / creating job log lines (APPROVED case with `dispatch_active: true`) OR evidence of cascade-9 (BLOCKED case)
    - **Pod-state evidence captured:** the artifact includes verbatim `kubectl get pod` output showing STATUS=Running RESTARTS ≤ 1 (APPROVED) or different failure shape (BLOCKED)
    - **600s budget referenced:** `grep -c '600s\|≤ 600\|<= 600'` ≥ 1
    - **T-02.2-21 marginal-flag check rendered (REVISED 60s threshold):** the artifact contains the literal `dispatch latency marginal` token (if observed values approach budgets) OR an explicit \"not marginal\" note
    - **Carry-forward threat register acknowledged:** `grep -E 'T-02.2-(0[1-9]|1[0-9]|2[0-7])'` matches at least once (T-02.2-01 through T-02.2-27 carry-forward)
    - **Behavior gate (APPROVED case — ALL 5 conditions on BOTH runs):**
      - (a) Clean run captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (b) Rerun captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (c) `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600`; no `timeout 600s` exit-124 in either log
      - (d) Manager logs show \"starting manager\" AND zero `flag provided but not defined` AND zero `no project found in namespace <ns>` for any of caps-test/output-test/failure-test/credproxy-test AND `dispatch_active: true` AND log evidence of `dispatch` / `Reconciling Task` / `creating job` (cascade-8 closure indicator — load-bearing)
      - (e) `kubectl get pod` shows STATUS=Running, restart count ≤ 1
      - (f) PVC tide-projects Bound RWO
      - (g) T-02.2-14 marginal-headroom check: if observed wall ≥ 500s, the artifact contains `budget headroom marginal` token
      - (h) T-02.2-21 marginal-dispatch-latency check (REVISED 60s threshold): if observed credproxy pod-wait ≥ 60, the artifact contains `dispatch latency marginal` token
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, §Section 5 Root-Cause Summary MUST exist with: (a) confirmation that Plan 02.2-10 Task 1's Dispatcher wiring IS in effect (`grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returns 1; `grep -cE '^[[:space:]]+Dispatcher: dispatcher,' cmd/manager/main.go` returns 2); (b) confirmation that prior cascade fixes preserved (Plans 02.2-04/05 flag defs; Plan 02.2-06 Makefile timeout; Plan 02.2-09 createProjectHierarchy); (c) which cascade-9 surfaced (one of T-02.2-31 enum); (d) why this was or was NOT predictable from the debug investigation; (e) fix-landscape options A–G enumerated
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows only the new `02.2-10-VERIFICATION.md` (Task 1's edits pre-committed; Task 3 not yet fired)
  </acceptance_criteria>

  <done>
    `02.2-10-VERIFICATION.md` exists with the structural template mirroring `02.2-09-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, AND ALL FIVE runtime-observation fields (`credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds`, AND new `dispatch_active: true|false`), AND `gate_decision: APPROVED` or `gate_decision: BLOCKED` (PENDING is not a final state). Sections 1–6 captured with per-spec outcome tables, Pod-state evidence, manager-log evidence (load-bearing for cascade-8 closure — particularly evidence of `dispatch` / `Reconciling Task` / `creating job` log lines AND/OR Job CRDs in test namespaces; AND absence of `no project found in namespace <ns>` for any of the 4 Layer B test namespaces), four-namespace state listings (Project + Milestone + Phase + Plan + Task + Job + Pod), before/after comparison vs `02.2-09-VERIFICATION.md`, wall-time budget check (with T-02.2-14 marginal-headroom carry-forward flag if observed wall ≥ 500s AND T-02.2-21 marginal-dispatch-latency flag — REVISED 60s threshold — if observed credproxy pod-wait ≥ 60s), and threat-model mitigations (T-02.2-28/29/30/31 + carry-forward T-02.2-01 through T-02.2-27). §Section 5 preserves the corrected root cause framing (resolveProject is the gate for cascade-5/7; Dispatcher field never assigned in cmd/manager/main.go is the gate for cascade-8) AND surfaces the deliberate Option-B-test-only-contract break (production-side fix is the only fix shape that closes cascade-8, per debug evidence rejecting Options α/β-1/β-2/β-3/γ) AND surfaces cascade-9 ValidationState production-wiring-gap as a flagged follow-up. The artifact CITES `.planning/debug/credproxy-backoff-suppression.md` as the canonical source for cascade-8 diagnosis. If APPROVED: all 5 conditions hold on BOTH clean run AND rerun (both exit 0 with 7/7 specs PASS; both ≤ 600s; zero `flag provided but not defined` AND zero `no project found` for any of 4 namespaces AND `dispatch_active: true`; Pod Running ≤ 1 restart; PVC Bound RWO; manager log shows successful dispatch for all 4 fixtures' Tasks). Task 3 fires to wire Plan 02.2-02's checkpoint — this is the CHAIN TERMINATOR v3 (FIRST APPROVED in Phase 02.2 history). If BLOCKED: Task 3 does NOT fire; §Section 5 classifies cascade-9 per T-02.2-31 enum AND names the specific spec(s) AND proposes Plan 02.2-11's fix shape from Options A–G. Committed with a `docs(02.2-10): runtime verification — APPROVED gate (cascade-8 Dispatcher-wiring closed; 7/7 Layer B specs PASS clean + rerun within 600s; CHAIN TERMINATOR v3 — FIRST APPROVED in Phase 02.2 history)` or `docs(02.2-10): runtime verification — BLOCKED gate (Dispatcher wiring landed; cascade-9 surfaced: <type>)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-10-VERIFICATION.md + bump depends_on (HARD-GATED on Task 2 APPROVED)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file; frontmatter — particularly the `depends_on: ["02.2-01"]` line, the `key_links` block, the `must_haves.truths` block; `<context>` block; Task 1 body — `<read_first>`, `<what-built>`, `<how-to-verify>`, `<verify>` automated block, `<acceptance_criteria>`, `<done>` — all of which currently reference `02.2-01-VERIFICATION.md` because Plans 02.2-03/04/05/06/07/08/09 Task 3 ALL DID NOT FIRE)
    - .planning/phases/02.2-.../02.2-10-VERIFICATION.md (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the HARD precondition for this Task 3 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - .planning/phases/02.2-.../02.2-09-PLAN.md Task 3 body (the structural model for this task's edits; mirror its surgical-edit pattern with `02.2-09` → `02.2-10` everywhere)
  </read_first>

  <action>
    **HARD PRECONDITION CHECK (THIS TASK FIRES ONLY IF MET):** Before doing ANY edits, verify `02.2-10-VERIFICATION.md` records `gate_decision: APPROVED`. Run:
    ```bash
    grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md
    ```
    If this returns 0 (the artifact says BLOCKED, or PENDING, or doesn't exist), HALT IMMEDIATELY and do NOT proceed. The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic: failed task → dependents in later waves never dispatch.

    If the grep returns 1 (artifact records APPROVED), proceed with the surgical edits to `02.2-02-PLAN.md`.

    **Surgical edit pattern (mirrors `02.2-09-PLAN.md` Task 3 verbatim with 02.2-09 → 02.2-10):**

    **Edit 1 — `depends_on` field:** In the frontmatter, replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-10"]`. The replacement is exact: bracket-quoted single-element list with the plan ID changed from `02.2-01` to `02.2-10`.

    **Edit 2 — 31 reference replacements:** Replace ALL 31 occurrences of `02.2-01-VERIFICATION.md` with `02.2-10-VERIFICATION.md` throughout the file. Use a single global replace (e.g., `sed -i.bak 's|02.2-01-VERIFICATION.md|02.2-10-VERIFICATION.md|g' 02.2-02-PLAN.md && rm 02.2-02-PLAN.md.bak`). The references span the `<context>` block, the `must_haves.truths`, the `key_links` patterns, the `<read_first>` paths in Task 1, the `<how-to-verify>` script, the `<verify>` automated block, the `<acceptance_criteria>`, and the `<done>` criteria — ALL of them must be redirected in one pass.

    **Constraints:**
    - **DO NOT** modify Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08, 02.2-09, or 02.2-10 (this plan). Only `02.2-02-PLAN.md` is touched.
    - **DO NOT** modify `02.2-01-VERIFICATION.md`, `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, `02.2-05-VERIFICATION.md`, `02.2-06-VERIFICATION.md`, `02.2-07-VERIFICATION.md`, `02.2-08-VERIFICATION.md`, `02.2-09-VERIFICATION.md`, or `02.2-10-VERIFICATION.md` (this task's edits are confined to one planning artifact — Plan 02).
    - **DO NOT** modify any production source. This task is a planning-artifact edit only.
    - **DO NOT** delete the file or its frontmatter. The edit is targeted: 1 frontmatter line change + 31 string replacements.
    - **DO NOT** change anything about `02.2-02-PLAN.md` OTHER than the `depends_on` field and the 31 reference replacements. The Task 1 body, the wave numbering, the `must_haves`, and all other frontmatter fields are preserved verbatim.

    Commit with message:
    ```
    docs(02.2-10): rewire Plan 02.2-02 to gate on 02.2-10-VERIFICATION.md APPROVED (CHAIN TERMINATOR v3 — first APPROVED rewire in Phase 02.2 history)
    ```

    Body should note: (a) this is the first APPROVED rewire in Phase 02.2's 10-plan cascade chain; (b) all 31 references redirected from `02.2-01-VERIFICATION.md` → `02.2-10-VERIFICATION.md`; (c) `depends_on: ["02.2-01"]` → `["02.2-10"]`; (d) Plan 02.2-02 closeout will now dispatch in a follow-up wave once the executor reads the APPROVED gate from `02.2-10-VERIFICATION.md`; (e) Phase 02.2 finally closes after Plan 02.2-02 lands.
  </action>

  <verify>
    <automated><![CDATA[
      # HARD PRECONDITION: 02.2-10-VERIFICATION.md records APPROVED
      [ "$(grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" -eq 1 ] && \
      # depends_on bumped
      [ "$(grep -cE 'depends_on: \[.{0,5}02.2-10.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 1 ] && \
      # No more references to 02.2-01-VERIFICATION.md
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # All 31 references redirected to 02.2-10-VERIFICATION.md
      [ "$(grep -c '02.2-10-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 31 ] && \
      # depends_on NOT pointing at 02.2-01 anymore
      [ "$(grep -cE 'depends_on: \[.{0,5}02.2-01.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Plans 01, 03-09 + 10 (this plan) untouched
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-PLAN.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-PLAN.md)" ] && \
      # VERIFICATION artifacts untouched (Task 2's 02.2-10-VERIFICATION.md is pre-committed before this task)
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md)" ] && \
      [ -z "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" ] && \
      # Production source untouched
      [ -z "$(git status --porcelain cmd/)" ] && \
      [ -z "$(git status --porcelain internal/)" ] && \
      [ -z "$(git status --porcelain api/)" ] && \
      [ -z "$(git status --porcelain pkg/)" ] && \
      [ -z "$(git status --porcelain charts/)" ] && \
      [ -z "$(git status --porcelain Makefile)" ] && \
      [ -z "$(git status --porcelain test/)" ] && \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **HARD PRECONDITION:** `grep -cE '^gate_decision: APPROVED$' 02.2-10-VERIFICATION.md` returns exactly `1` BEFORE any edit to `02.2-02-PLAN.md` (if 0, this task HALTS and does NOT execute)
    - **depends_on rewired:** `grep -cE 'depends_on: \[.{0,5}02.2-10.{0,5}\]' 02.2-02-PLAN.md` returns exactly `1`
    - **Old depends_on removed:** `grep -cE 'depends_on: \[.{0,5}02.2-01.{0,5}\]' 02.2-02-PLAN.md` returns exactly `0`
    - **31 references redirected:** `grep -c '02.2-10-VERIFICATION.md' 02.2-02-PLAN.md` returns exactly `31` (the same count that previously referenced 02.2-01)
    - **No more 02.2-01-VERIFICATION.md references:** `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns exactly `0`
    - **Plans 01, 03-10 untouched:** `git status --porcelain` returns empty for each of `02.2-01-PLAN.md`, `02.2-03-PLAN.md`, `02.2-04-PLAN.md`, `02.2-05-PLAN.md`, `02.2-06-PLAN.md`, `02.2-07-PLAN.md`, `02.2-08-PLAN.md`, `02.2-09-PLAN.md`, `02.2-10-PLAN.md`
    - **VERIFICATION artifacts untouched:** `git status --porcelain` returns empty for `02.2-10-VERIFICATION.md` (Task 2's artifact pre-committed) AND for all prior verification artifacts
    - **Production source untouched:** `git status --porcelain <path>` returns empty for each of `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `Makefile`, `test/`
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has `depends_on` bumped from `["02.2-01"]` → `["02.2-10"]` AND all 31 references to `02.2-01-VERIFICATION.md` redirected to `02.2-10-VERIFICATION.md`. This is the FIRST APPROVED rewire in Phase 02.2's 10-plan cascade chain (Plans 02.2-03/04/05/06/07/08/09 Task 3 ALL did NOT fire because their respective Task 2 gates were BLOCKED). Plan 02.2-02's `<context>`, `must_haves`, `key_links`, `<read_first>`, `<how-to-verify>`, `<verify>`, `<acceptance_criteria>`, AND `<done>` blocks all now point at `02.2-10-VERIFICATION.md` — once Plan 02.2-02 dispatches in a follow-up wave, its checkpoint:human-verify Task 1 reads the APPROVED gate from `02.2-10-VERIFICATION.md` and Phase 02.2 finally CLOSES after 10 gap-closure iterations (8 cascade closures: PVC, metrics-flag, webhook-flag, Makefile-budget, credproxy-fixture, Eventually-timeouts, caps/output/failure-fixtures, Dispatcher-wiring + cascade-9 deferred as follow-up). No other plan, verification artifact, or production source modified by this task. Committed with the `docs(02.2-10): rewire Plan 02.2-02 to gate on 02.2-10-VERIFICATION.md APPROVED (CHAIN TERMINATOR v3 — first APPROVED rewire in Phase 02.2 history)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmd/manager/main.go → internal/dispatch/podjob | The Dispatcher seam between the orchestrator process and the dispatch backend implementation. Cascade-8 surfaces here: the seam was DESIGNED to be wired but the wiring was deferred since Phase 2 Plan 09 work landed. Trust assumption: the PodJobBackend type satisfies internal/dispatch.Dispatcher (verified at plan-time via `internal/dispatch/podjob/backend.go:58-79`). |
| cmd/manager/main.go → internal/controller/{plan,task}_controller.go | Reconciler setup blocks consume `*podjob.PodJobBackend` via the `Dispatcher dispatch.Dispatcher` field. Trust assumption: the field declarations on both reconciler structs are already in place (verified at plan-time at lines 57 and 77). |
| TaskReconciler.reconcileDispatch → K8s apiserver | Once Dispatcher is wired, `r.Create(ctx, job)` at `task_controller.go:393` becomes reachable. This crosses into the K8s control plane — Jobs are CREATED in production for the first time since Phase 2 Plan 09 work landed. Trust assumption: RBAC rules at `task_controller.go:106-115` already grant the necessary verbs (`jobs: get;list;watch;create;update;patch;delete`). |
| PodJobBackend.BuildJobSpec → Pod runtime | The Job spec defines the credproxy sidecar (Pod spec). Trust assumption: the Pod spec is constructed in `podjob/backend.go` and tested via envtest unit tests (verified at plan-time the function is invoked by reconcileDispatch Step 11). |
| internal/webhook/v1alpha1/plan_webhook.go validation → plan.Status.ValidationState | Cascade-9 production-wiring-gap: the webhook validates but never updates Status. **This boundary is NOT crossed by Plan 02.2-10 — deliberately deferred per `<execution_notes>` anti-pattern #6.** Surface in §Section 5 as follow-up. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-28 | Tampering | cmd/manager/main.go dispatcher construction (Part A) | mitigate | `dispatcher := &podjob.PodJobBackend{...}` MUST have all 7 fields populated from in-scope variables. Acceptance: `grep -c 'Client:.*mgr.GetClient' cmd/manager/main.go` ≥ 2 (one in dispatcher, ≥1 in reconciler literals); same pattern for Scheme/SubagentImage/CredproxyImage/SigningKey/EnvReader; `grep -c 'PVCName:.*"tide-projects"' cmd/manager/main.go` returns exactly 1; `gofmt -d cmd/manager/main.go` returns empty; `go build ./cmd/manager/...` exits 0; `go vet ./cmd/manager/...` exits 0. |
| T-02.2-29 | Elevation of Privilege | TaskReconciler.reconcileDispatch (now live in production for the first time since Phase 2 Plan 09 landed) | mitigate | The dispatch path is DESIGNED to run; cascade-8 was the wiring deferral. Plan 2 dispatch logic was unit-tested via envtest. Acceptance: Task 2 re-runs the full kind integration suite which exercises wave/failure/caps/output specs against the now-live dispatch path; any latent bugs surface as Ginkgo spec failures with concrete classification under T-02.2-31 taxonomy in §Section 5. RBAC at `task_controller.go:106-115` already grants `jobs: get;list;watch;create;update;patch;delete` so Job creation does not require additional privilege escalation. |
| T-02.2-30 | Spoofing / Authentication | Signing key wiring (HMAC token signing for credproxy envelopes) | mitigate | `signingKey, err := decodeSigningKeyFromEnv()` at `cmd/manager/main.go:174` already loads + validates the key (length ≥ 32 enforced at line 80-82). Acceptance: Task 2's clean run reaches `tide-controller-manager` Pod `Running` (Plan 02.2-05's fix); if signing-key load fails, the Pod CrashLoopBackOffs and the verification gate's `Pod Running ≤ 1 restart` condition trips, causing BLOCKED with cascade-9 classification (`production-wiring-gap` family for signing-key surface). |
| T-02.2-31 | Information Disclosure / Cascade Detection | plan.Status.ValidationState never set (cascade-9 secondary risk surfaced during debug investigation) | accept | The Plan admission webhook at `internal/webhook/v1alpha1/plan_webhook.go:117-199` validates and emits warnings but does NOT update `.Status.ValidationState`. `PlanReconciler.reconcileWaveMaterialization` short-circuits at `plan_controller.go:147` if `ValidationState != \"Validated\"`. **However:** TaskReconciler.reconcileDispatch does NOT depend on ValidationState — it only requires `r.Dispatcher != nil` + the Task itself. So HARN-03 closes via Option δ alone. **Accepted with the requirement:** cascade-9 is documented in `<context>` AND `<execution_notes>` AND will be surfaced in `02.2-10-VERIFICATION.md` §Section 5 as a recommended follow-up debug session AFTER Plan 02.2-10's chain terminator lands. Plan 02.2-10's scope is cascade-8 closure ONLY; bundling the ValidationState webhook fix is scope expansion — REJECTED per `.continue-here.md` anti-pattern #6. |

</threat_model>

<verification>
- **Task 1 verification:** `go build ./...` + `go vet ./...` + `gofmt -d cmd/manager/main.go` (empty) + all the grep gates in `<verify>` automated block (dispatcher construction, both Dispatcher assignments, all 7 PodJobBackend fields, all 10 pre-existing flags preserved, all production source other than `cmd/manager/main.go` untouched, prior cascade fixes preserved).
- **Task 2 verification:** `02.2-10-VERIFICATION.md` exists with valid frontmatter (all 5 observation fields populated; `gate_decision` ∈ {APPROVED, BLOCKED}; not PENDING). All 6 sections present. Threat-model mitigations T-02.2-28/29/30/31 referenced. Cascade-9 classification (or chain-terminated note) present. `resolveProject` AND `Dispatcher` framing preserved. Debug session cited. Option-B-test-only break surfaced. ValidationState gap referenced as follow-up.
- **Task 3 verification:** HARD PRECONDITION (`gate_decision: APPROVED` in `02.2-10-VERIFICATION.md`) met BEFORE any edit. `depends_on: ["02.2-10"]` set; 31 references redirected; no `02.2-01-VERIFICATION.md` references remain. Plans 01, 03-10 untouched. All verification artifacts untouched. Production source untouched.
- **Phase-level verification:** Plan 02.2-10 is the chain terminator candidate v3. If Task 2 APPROVED: Phase 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave; the cascade chain is documented as fully terminated (8 cascades closed + cascade-9 deferred follow-up). If Task 2 BLOCKED: Plan 02.2-11 will be authored to close cascade-9 (most likely the ValidationState production-wiring-gap based on debug evidence).
</verification>

<success_criteria>
- Plan 02.2-10 produced with valid frontmatter (phase: 02.2, plan: 10, type: execute, wave: 9, gap_closure: true, depends_on: ["02.2-09"], 3 files_modified, autonomous: true, requirements: [], threat_model_summary populated, must_haves populated)
- 3 tasks defined with `<read_first>`, `<action>`, `<verify>`, `<acceptance_criteria>`, `<done>` blocks
- Task 1's `<action>` contains the verbatim 7-field PodJobBackend struct literal from `.planning/debug/credproxy-backoff-suppression.md` §Option δ
- Task 1 modifies ONLY `cmd/manager/main.go` (regression guards confirm no other production source touched)
- Task 2 authors `02.2-10-VERIFICATION.md` with 5 observation fields (4 carry-forward + new `dispatch_active`)
- Task 3 has HARD-GATED precondition (`gate_decision: APPROVED` in `02.2-10-VERIFICATION.md`) before firing
- Threat model has 4 entries: T-02.2-28 (mitigate — dispatcher construction correctness), T-02.2-29 (mitigate — dispatch path goes live), T-02.2-30 (mitigate — signing key wiring), T-02.2-31 (accept — cascade-9 ValidationState gap deferred)
- `<execution_notes>` surfaces three load-bearing concerns: Plan 02.2-02 wave-dispatch standard concern + Option-B-test-only deliberate break + cascade-9 deferral
- `<context>` block cites the canonical debug session at `.planning/debug/credproxy-backoff-suppression.md` AND the cascade chain status table (1–7 CLOSED, 8 THIS PLAN, 9 deferred)
- Plan 02.2-10 is the chain terminator candidate v3 — high-confidence because the debug evidence specifically targets the dispatch-path-not-running root cause
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-SUMMARY.md` capturing:
- The Option δ wiring shape (1 dispatcher construction + 2 Dispatcher field assignments in cmd/manager/main.go)
- The deliberate Option-B-test-only contract break + rationale
- The gate_decision from 02.2-10-VERIFICATION.md
- Whether Task 3 fired (and thus whether the cascade chain terminated)
- The cascade-9 ValidationState gap as documented follow-up
- Per-fixture observed runtime evidence (5 observation fields from frontmatter)
</output>
</content>
</invoke>