---
phase: 02.2-layer-b-kind-test-timing-fixes
plan: 06
subsystem: infra
tags: [gap-closure, makefile, test-budget, runtime-verification, kind-integration, cascade-4, cascade-5, harness-bug, phase-02.2, tactical-iteration]

# Dependency graph
requires:
  - phase: 02.2 Plan 05
    provides: --webhook-cert-path flag definition + webhook.Options.CertDir wire-up; BLOCKED gate identifying cascade-4 (Makefile test-budget structural insufficiency) as next blocker
provides:
  - Makefile:124 outer wall-time budget bump from timeout 300s to timeout 600s (cascade-4 fix — EFFECTIVE)
  - 02.2-06-VERIFICATION.md runtime artifact (BLOCKED gate with cascade-5 surfaced — harness-bug in credproxy_test.go fixture; test budget is no longer the blocker)
  - Empirical proof that cascade-4 (test-budget) is resolved; cascade-5 is structurally distinct (harness-bug, not budget)
affects: [02.2-02 closeout (still gated), 02.2-07 next iteration (credproxy harness-bug fix), future credproxy dispatch path, task controller testMode shortcut]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cascade-type classification at BLOCKED boundary: each BLOCKED gate must classify cascade type using the T-02.2-16 taxonomy (flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other). This plan classified cascade-5 as harness-bug, enabling Plan 02.2-07 to immediately target the task controller testMode dispatch shortcut rather than auditing budget or flags."
    - "Effective fix validation via exit code shift: cascade-4 exit was 2 (Make Error 124 — SIGKILL) at 689s total wall; cascade-5 exit is 1 (FAIL — spec failures) at 304s total wall. The exit-code change from 2 to 1 (and wall-time reduction from 689s to 304s) proves the Makefile budget fix is effective even when a new cascade surfaces."

key-files:
  created:
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md
  modified:
    - Makefile

key-decisions:
  - "Makefile:124 outer timeout 300s → 600s — single-token replacement; inner -timeout=5m Ginkgo cap preserved verbatim (T-02.2-15 mitigation honored); no Layer A/Layer B split targets added (Option B deferred)"
  - "No rerun performed — clean run BLOCKED gate makes rerun redundant; credproxy failures are deterministic (structural harness-bug, not a timing flake)"
  - "Task 3 did NOT fire — gate_decision: BLOCKED per wave-failure-semantics contract; 02.2-02-PLAN.md still references 02.2-01-VERIFICATION.md (31 references unchanged)"
  - "Cascade-5 classified as harness-bug — credproxy test fixture creates Plan+Task without parent Milestone/Phase/Wave hierarchy; task controller's dispatch guard blocks Job creation; fix is testMode dispatch shortcut in task controller"
  - "T-02.2-14: budget headroom NOT marginal — clean run go test wall 304s < 500s threshold; no budget headroom marginal flag needed"
  - "T-02.2-16 ACCEPTED — cascade-5 classified and documented in §Section 5; Fix landscape provides harness-bug fix options for Plan 02.2-07"

patterns-established:
  - "Pattern: exit-code taxonomy for make test-int — exit 2 (Error 124) = outer timeout SIGKILL (budget issue); exit 1 (FAIL) = spec failures (correctness issue). This distinction enables rapid diagnosis of which layer is failing."
  - "Pattern: preliminary VERIFICATION.md before long-running tests — write frontmatter with PENDING gate_decision before the clean reproducer runs; update after. Protects against mid-run context window pressure or API errors losing the gate field. First established in Plan 02.2-05; now a confirmed protocol."
  - "Pattern: cascade-5 structural break — all 5 cascades (cascade-1 through cascade-5) are now of distinct classes: RWX→RWO (cascade-1), metrics-bind-address flag (cascade-2), webhook-cert-path flag (cascade-3), test-budget (cascade-4), harness-bug (cascade-5). Each class requires a different fix shape; the cascade chain is NOT a simple linear sequence."

requirements-completed: []

# Metrics
duration: 25min
completed: 2026-05-14
---

# Phase 02.2 Plan 06: Cascade-4 Closure (Makefile timeout 300s→600s) Summary

**One-liner:** Makefile:124 outer timeout bumped 300s→600s (cascade-4 test-budget resolved); runtime gate BLOCKED on cascade-5 (harness-bug: credproxy_test.go fixture lacks parent Wave hierarchy for task dispatch).

## What Landed

### Task 1 — Makefile:124 outer timeout bump (commit `3cc76e9`)

Single-token replacement: `timeout 300s go test ./test/integration/...` → `timeout 600s go test ./test/integration/...`. Inner `-timeout=5m` Ginkgo cap preserved verbatim (T-02.2-15). No other tokens changed. `test-int-fast` (2m budget) and `test-int-kind-prep` (no timeout) untouched. Option B (split Layer A/B targets) not implemented — deferred to Plan 02.2-07 if cascade-5 resolution requires it.

### Task 2 — Runtime verification (commit `1f91180`)

End-to-end clean run with `make test-int` (cluster delete/recreate). Results:

- **Layer A envtest:** 18/18 PASS in 27.18s
- **Layer B kind BeforeSuite:** PASSED in 66s (cert-manager wait + helm install + Pod Ready)
- **Layer B kind specs 1-2:** FAILED — `credproxy_test.go` HARN-03 both specs, "A Pod should be created for the credproxy-task Job: Timed out after 60.000s"
- **Layer B kind specs 3-7:** NOT RUN — inner Ginkgo `-timeout=5m` cap fired (panic: test timed out after 5m0s) after specs 1-2 consumed 149s
- **Total wall time:** 304s (vs. 689s prior clean run) — no SIGKILL; outer 600s budget was NOT the blocking issue
- **Exit code:** 1 (spec failures) vs. prior 2 (SIGKILL exit 124)

**Gate decision: BLOCKED** — cascade-4 (test-budget) is CLOSED; cascade-5 (harness-bug) surfaced.

### Task 3 — NOT FIRED

`grep -cE '^gate_decision: APPROVED$' 02.2-06-VERIFICATION.md` returned 0. Per the wave-failure-semantics contract, Task 3 (02.2-02-PLAN.md rewire) did not execute. The 31 references to `02.2-01-VERIFICATION.md` in `02.2-02-PLAN.md` remain unchanged.

## Cascade-4 Resolution Evidence

| Metric | Plan 02.2-05 (BLOCKED on cascade-4) | Plan 02.2-06 (cascade-4 CLOSED) |
|--------|--------------------------------------|----------------------------------|
| Exit code | 2 (Error 124 — SIGKILL) | 1 (FAIL — spec failures) |
| Total wall | 689s | 304s |
| go test wall | ~300s (timeout fired) | ~304s (no SIGKILL) |
| Outer budget breached? | YES — timeout 300s SIGKILL | NO — 304s < 600s budget |
| Inner -timeout=5m fired? | No (SIGKILL preceded it) | YES — fired at 5m0s panic |
| Cascade cause | Budget structural insufficiency | Harness-bug in credproxy fixture |

## Cascade-5 Root Cause

The `credproxy_test.go` specs (HARN-03) create:
- `credproxy-plan` (Plan CRD, `credproxy-test` namespace)
- `credproxy-task` (Task CRD, `spec.dev.testMode: success`)

Without a parent Milestone/Phase/Wave hierarchy. The task controller reconciles `credproxy-task` but the dispatch guard requires the parent Wave to be in Ready state before creating a batchv1.Job. No Job is created, so no Pod appears, and the 60s `Eventually` assertion times out.

Manager log confirms: `task cleanup` (finalizer path) at 14:50:29Z (60s after task creation), no `dispatch` or `job create` log lines for `credproxy-task`.

Cascade-5 classification: **`harness-bug`** (T-02.2-16 taxonomy).

## Deviations from Plan

None — plan executed exactly as written. Task 3 correctly did not fire (gate BLOCKED). The recovery-lesson pre-step (preliminary VERIFICATION.md before clean run) was honored. No `charts/tide/values.yaml` modifications.

## Known Stubs

None — this plan is infrastructure-only (Makefile + verification artifact). No UI or data path stubs.

## Self-Check

| Claim | Verification |
|-------|-------------|
| `02.2-06-VERIFICATION.md` exists with gate_decision: BLOCKED | `grep '^gate_decision:' ...VERIFICATION.md` → `gate_decision: BLOCKED` |
| Makefile has `timeout 600s` | `grep -c 'timeout 600s' Makefile` → 1 |
| Makefile has no `timeout 300s` | `grep -c 'timeout 300s' Makefile` → 0 |
| Task 1 commit exists | `3cc76e9` |
| Task 2 commit exists | `1f91180` |
| Task 3 did NOT fire | `grep -cE '^gate_decision: APPROVED$'` → 0; 02.2-02-PLAN.md unchanged |
| No `charts/tide/values.yaml` edits | git status shows only Makefile + planning artifacts |
