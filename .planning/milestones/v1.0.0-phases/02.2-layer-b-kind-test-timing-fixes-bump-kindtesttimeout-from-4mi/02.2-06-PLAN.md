---
phase: "02.2"
plan: "06"
type: execute
wave: 5
gap_closure: true
depends_on: ["02.2-05"]
files_modified:
  - Makefile
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, makefile, test-budget, runtime-verification, kind-integration, phase-02.2, tactical, cascade-4, structurally-different-cascade]

threat_model_summary:
  - "T-02.2-14 (mitigate): bumping outer `timeout 300s → 600s` at `Makefile:124` could mask a real Layer B performance regression where specs grow from healthy ~3-4 min to unhealthy ~6 min over time, hiding a slowdown behind the larger budget. Mitigated by requiring `02.2-06-VERIFICATION.md` §Section 4 to record actual observed wall-time for clean run and rerun and to flag `budget headroom marginal` if observed wall ≥ 500s even on APPROVED, so the next iteration can preemptively escalate to Option B (split Layer A / Layer B targets). Acceptance: `grep -E '^clean_run_wall_seconds: [0-9]+$' 02.2-06-VERIFICATION.md` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' 02.2-06-VERIFICATION.md` matches AND if either ≥ 500 the artifact contains the literal token `budget headroom marginal`."
  - "T-02.2-15 (mitigate): bumping the OUTER timeout to 600s without touching the INNER `-timeout=5m` Ginkgo arg leaves the inner cap LESS than the outer cap, which is intentional — a single hung spec gets SIGKILLed by Ginkgo's internal cap (5 min) before the outer wall fires (10 min). Mitigated by preserving `-timeout=5m` verbatim and gating on its presence. Acceptance: `grep -c -- '-timeout=5m' Makefile` returns ≥ 1 after the edit (the inner Ginkgo cap is untouched)."
  - "T-02.2-16 (accept): tactical scope means cascade-5 (if any) is genuinely unknown — the flag-mismatch cascade chain terminated at Plan 02.2-05; the test-budget fix is structurally different (budget-vs-actual rather than flag-vs-binary). The cascade-5 (if it fires) could be another structurally distinct class: a flake in one of the 7 specs that only manifests under sustained controller load, an etcd backup-restore race, a kind networking quirk, or something not foreseeable from the plan-time read. Accepted with the requirement that if Task 2's gate is BLOCKED, `02.2-06-VERIFICATION.md` §Section 5 Root-Cause Summary MUST classify the cascade type using one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other` so Plan 02.2-07 (or equivalent) can pick the right fix shape from the start."

must_haves:
  truths:
    - "`Makefile:124`'s `test-int` recipe uses outer `timeout 600s` (was `timeout 300s`) — bump preserves inner `-timeout=5m` Ginkgo arg and all other recipe tokens verbatim"
    - "`02.2-06-VERIFICATION.md` exists with frontmatter `phase: 02.2`, `plan: 06`, `task: 2`, `artifact: verification`, a populated `gate_decision: APPROVED|BLOCKED` line, and Sections 1-6 mirroring `02.2-05-VERIFICATION.md`"
    - "APPROVED gate requires ALL 5 conditions on BOTH clean run AND rerun: wall ≤ 600s; manager Pod `Running` with restart count ≤ 1; manager-log shows zero `flag provided but not defined` errors of any kind; PVC `tide-projects` Bound RWO; 7/7 Layer B Ginkgo specs PASS"
    - "If `02.2-06-VERIFICATION.md` records `gate_decision: APPROVED`, Task 3 fires and Plan 02.2-02's `depends_on` becomes `[\"02.2-06\"]` and all 31 references to `02.2-01-VERIFICATION.md` are redirected to `02.2-06-VERIFICATION.md`"
    - "If `02.2-06-VERIFICATION.md` records `gate_decision: BLOCKED`, Task 3 does NOT fire — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (its current wiring) and the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md"
  artifacts:
    - path: "Makefile"
      provides: "outer wall-time budget bump for the `test-int` recipe (300s → 600s); inner `-timeout=5m` Ginkgo cap preserved"
      contains: "timeout 600s"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md"
      provides: "Runtime verification artifact with gate_decision frontmatter — authoritative gate for Plan 02.2-02 closeout"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-06\"] + 31 references redirected to 02.2-06-VERIFICATION.md (gated on Task 2 APPROVED)"
      contains: "02.2-06"
  key_links:
    - from: "Makefile:124 (`test-int` recipe outer timeout)"
      to: "`go test ./test/integration/...` wall-time budget"
      via: "GNU `timeout` command wrapping `go test`"
      pattern: 'timeout 600s go test \./test/integration/'
    - from: "Makefile:124 (`test-int` recipe inner Ginkgo cap)"
      to: "`-timeout=5m` Ginkgo per-spec cap"
      via: "`-timeout=5m` arg preserved verbatim"
      pattern: '-timeout=5m'
    - from: "02.2-06-VERIFICATION.md frontmatter gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 wires Plan 02.2-02's depends_on + checkpoint references (gated on APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md (depends_on field)"
      to: "this plan's verification artifact"
      via: "frontmatter depends_on: [\"02.2-06\"]"
      pattern: 'depends_on: \[.{0,5}02.2-06.{0,5}\]'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-05-VERIFICATION.md` §"Section 5 Root-Cause Summary" Option A (continued tactical iteration, fourth iteration). The Makefile's `timeout 300s go test ./test/integration/...` budget contract at `Makefile:124` (Plan 02.2-01 fix #5 origin) is structurally too tight to fit Layer A (~20-25s envtest) plus Layer B kind suite's full 7-spec end-to-end run against a HEALTHY controller. The Layer B specs ARE executing successfully — manager Pod `Running 0 restarts` for 11+ min, at least 3 of 7 specs reach `AfterEach` cleanup — but `go test` is SIGKILLed by the outer `timeout` before Ginkgo can flush its summary line. This is fitness (budget-vs-actual-duration mismatch), not correctness.

**Cascade-4 is STRUCTURALLY DIFFERENT from cascades 1-3.** Cascades 1-3 were all flag mismatches (Plan 02.2-03 PVC `RWX→RWO`; Plan 02.2-04 `--metrics-bind-address` undefined; Plan 02.2-05 `--webhook-cert-path` undefined). The flag-mismatch cascade chain TERMINATED at 5/5 chart args defined in Plan 02.2-05 — all 5 `charts/tide/values.yaml:18-24` args (`--metrics-bind-address`, `--config`, `--watch-namespace`, `--leader-elect`, `--webhook-cert-path`) now have matching `flag.StringVar` / `flag.BoolVar` definitions in `cmd/manager/main.go`, and Plan 02.2-05's runtime verification proves the manager binary now starts cleanly (`Running 0 restarts`). Cascade-4 is a different bug class entirely — budget-vs-actual — and no flag audit could have predicted it.

**User decision: Option A (single-line Makefile timeout bump 300s → 600s).** Per `02.2-05-VERIFICATION.md` §"Fix landscape" enumeration, captured on 2026-05-14 (resume conversation):
- **Option A (recommended, this plan):** bump `Makefile:124` outer `timeout 300s` → `timeout 600s`. Smallest possible surface — one line. Honors the chart-as-FIXED-contract / binary-catches-up posture from prior plans (Makefile is test-side infrastructure; chart untouched).
- **Option B (escalation if Option A overflows):** split Layer A and Layer B into separately-timed `make` targets (e.g. `test-int-envtest` at 60s + `test-int-kind` at 600s). More robust but bigger surface; deferred to Plan 02.2-07 ONLY if Plan 02.2-06's 600s budget proves insufficient on clean + rerun.

The orchestrator passes the user's Option-A decision down so the planner does not re-litigate. **Why 600s specifically:** Plan 02.2-05's verification observed manager Pod `Running 0 restarts` for 11+ min during clean run + idempotent rerun; the SIGKILL fired well before that, but Ginkgo would have flushed the summary within the full run window. 600s gives 2× headroom over the prior 300s budget. If 600s isn't enough, the BLOCKED gate routes to Option B for Plan 02.2-07.

This plan fixes ONLY the immediate blocker (outer wall-time budget) and then re-runs the same end-to-end runtime reproducer captured in `02.2-05-VERIFICATION.md`. The expected outcome is one of:
- **APPROVED:** the manager continues to start cleanly (Plan 02.2-05's fix preserved), Layer A envtest passes 18/18 in ~25s, Layer B kind suite completes all 7 specs PASS within the new 600s outer budget on clean + rerun, both still respect the per-spec `-timeout=5m` Ginkgo cap. Plan 02.2-02 (closeout) gets unblocked via Task 3's surgical edits.
- **BLOCKED (predicted cascade-5 unknown structural shape):** the 600s budget is also exceeded, OR a new cascade fires (spec flake, kind networking quirk, etcd race, harness bug, or something not foreseeable). The BLOCKED gate captures the new failure mode root cause AND classifies the cascade type per T-02.2-16 acceptance so Plan 02.2-07 (or equivalent next iteration) can pick the right fix shape.

**TACTICAL SCOPE — user explicitly chose Option A (single-line bump) over Option B (split targets):**
- This plan bumps `Makefile:124` outer `timeout 300s → 600s` ONLY
- This plan does NOT split Layer A and Layer B into separate `make` targets (Option B — deferred to Plan 02.2-07 if needed)
- This plan does NOT touch the INNER `-timeout=5m` Ginkgo arg — it stays verbatim; the new outer 10 min cap > inner 5 min cap is intentional per T-02.2-15 (single hung spec gets caught by Ginkgo before outer SIGKILL)
- This plan does NOT touch `charts/tide/values.yaml` — chart is FIXED contract; this fix is test-side infrastructure
- This plan does NOT touch Plans 02.2-01, 02.2-02, 02.2-03, 02.2-04, or 02.2-05 except for Task 3's surgical edits to `02.2-02-PLAN.md`
- This plan does NOT modify `test-int-fast` (line 126-128 Layer-A-only target) or `test-int-kind-prep` (line 130+ image-build target) — only the line 124 token `timeout 300s` is bumped
- This plan does NOT modify any other Makefile recipe, no source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/`

**Plan 02.2-05's pattern this plan mirrors:**
Plan 02.2-05 ran a 3-task shape: T1 source edit (define `--webhook-cert-path` + wire into `webhook.Options.CertDir`), T2 runtime verification (`02.2-05-VERIFICATION.md` with `gate_decision` frontmatter), T3 surgical edit of `02.2-02-PLAN.md` gated on APPROVED. This plan mirrors that shape exactly with cascade-4's target (`Makefile:124` `timeout 300s` → `timeout 600s`) substituted for cascade-3's target. **The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic:** Task 3 fires ONLY if Task 2's `gate_decision: APPROVED`.

**Recovery state note (load-bearing for Task 3's rewire scope):** Plan 02.2-03 Task 4 was intended to rewire `02.2-02-PLAN.md` from `02.2-01-VERIFICATION.md` → `02.2-03-VERIFICATION.md` but did NOT fire (Plan 02.2-03 Task 3 gate was BLOCKED). Plan 02.2-04 Task 3 was intended to rewire `02.2-02-PLAN.md` → `02.2-04-VERIFICATION.md` but ALSO did NOT fire (Plan 02.2-04 Task 2 gate was BLOCKED). Plan 02.2-05 Task 3 was intended to rewire `02.2-02-PLAN.md` → `02.2-05-VERIFICATION.md` but ALSO did NOT fire (Plan 02.2-05 Task 2 gate was BLOCKED on cascade-4). As a result, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (`grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 31 as of plan-time). This plan's Task 3 lands the long-pending rewire if Task 2's gate is APPROVED, redirecting all 31 references to `02.2-06-VERIFICATION.md` in one pass.

**Recovery-lesson note from `.continue-here.md` Plan 02.2-05:** Write `02.2-06-VERIFICATION.md` WITH PRELIMINARY frontmatter immediately after the clean-run captures land in `/tmp/02.2-06-clean-run.log`, then add Sections 2-6 incrementally as the rerun completes and the cluster diagnostic snapshot is captured. This protects against mid-run executor API errors / context window pressure during the long-running runtime verification phase — the artifact's frontmatter (the load-bearing gate field) is preserved even if Sections 4-6 prose authoring is interrupted.

Purpose: close the BLOCKED runtime gate from `02.2-05-VERIFICATION.md` (Plan 02.2-05's webhook-cert-path fix worked — verified by direct evidence; manager Pod `Running 0 restarts` for 11+ min across both runs; cascade-4 unmasked as test-budget contract structural insufficiency, NOT a flag mismatch) with the cheapest possible test-infrastructure fix that gives Layer B's full 7-spec end-to-end run sufficient wall-time headroom. Tactical iteration #4: bump budget, re-verify, see if cascade chain has terminated.

Output:
- `Makefile:124` with outer `timeout 600s go test ./test/integration/... -v -timeout=5m -ginkgo.v` (was `timeout 300s ...`)
- `.planning/phases/02.2-.../02.2-06-VERIFICATION.md` mirroring `02.2-05-VERIFICATION.md`'s structural template; `gate_decision: APPROVED` or `BLOCKED`
- If Task 2 APPROVED: `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-06"]` and ALL 31 `02.2-01-VERIFICATION.md` references redirected to `02.2-06-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
**Wave-dispatch concern for Plan 02.2-02 (executor MUST honor):** Plan 02.2-02 is the closeout plan and currently has `depends_on: ["02.2-01"]` with a `checkpoint:human-verify` Task 1 that reads `02.2-01-VERIFICATION.md`'s `gate_decision` field. Because Plan 02.2-01's verification artifact records `gate_decision: BLOCKED` (its checkpoint Task 3 was BLOCKED on the original cascade chain), Plan 02.2-02 MUST NOT be dispatched alongside Plan 02.2-06 in the same execute wave. The structural mitigation: execute-phase's wave-ordering reads `depends_on` from each plan's frontmatter and groups plans whose dependencies are all satisfied into the same wave. Plan 02.2-02's `depends_on` points at 02.2-01 (BLOCKED); Plan 02.2-06's `depends_on` points at 02.2-05 (APPROVED in source-shape, BLOCKED on cascade-4 surfacing — but the source-shape fix IS landed). The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic: failed task → siblings continue, **dependents in later waves never dispatch**. Plan 02.2-02 stays a dependent-in-later-wave of 02.2-01-VERIFICATION.md's BLOCKED gate until THIS plan's Task 3 rewrites its `depends_on` to point at 02.2-06-VERIFICATION.md's APPROVED gate. If the executor dispatches Plan 02.2-02 alongside Plan 02.2-06 (e.g. due to a misread wave-derivation), Plan 02.2-02's Task 1 checkpoint will halt at the BLOCKED gate AND nothing further fires — but the structural intent is that Plan 02.2-02 dispatches AFTER Plan 02.2-06's Task 3 has landed, in a separate wave 6.
</execution_notes>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-05-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/.continue-here.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from Makefile + 02.2-05-VERIFICATION.md. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From Makefile lines 118-128 (current state — pre-edit; line 124 is the SINGLE edit target):

```makefile
##@ Integration tests (TEST-02 — Phase 2)

.PHONY: test-int test-int-fast test-int-kind-prep

test-int: manifests generate fmt vet setup-envtest test-int-kind-prep ## Run full integration test suite: Layer A (envtest) + Layer B (kind). Timeout 300s (TEST-02 budget). Requires Docker + kind.
	KUBEBUILDER_ASSETS="$$($(ENVTEST) use $(ENVTEST_K8S_VERSION) --bin-dir "$(LOCALBIN)" -p path)" \
		timeout 300s go test ./test/integration/... -v -timeout=5m -ginkgo.v

test-int-fast: manifests generate fmt vet setup-envtest ## Run Layer A integration tests only (envtest; no Docker/kind needed). Target: ~90s.
	KUBEBUILDER_ASSETS="$$($(ENVTEST) use $(ENVTEST_K8S_VERSION) --bin-dir "$(LOCALBIN)" -p path)" \
		go test ./test/integration/envtest/... -v -timeout=2m -ginkgo.v --ginkgo.label-filter='envtest'
```

The single edit target is line 124's token `timeout 300s` → `timeout 600s`. ALL other tokens on the same line stay verbatim:
- `KUBEBUILDER_ASSETS=...` env-var prefix stays verbatim
- `go test ./test/integration/...` invocation stays verbatim
- `-v` arg stays verbatim
- `-timeout=5m` arg (the inner Ginkgo cap) stays verbatim — DO NOT bump this
- `-ginkgo.v` arg stays verbatim
- The trailing newline + indentation stays verbatim
- The `test-int:` target dependencies (`manifests generate fmt vet setup-envtest test-int-kind-prep`) stay verbatim
- The `test-int:` comment (`## Run full integration test suite: ...`) stays verbatim — the inline comment text mentions "Timeout 300s" historically but updating the comment text is optional (acceptance gates do not require the comment update; only the recipe edit matters)

From `02.2-05-VERIFICATION.md` §"Section 4 Wall-time Budget Check":
- Clean run: total `make test-int` wall 689s; `go test` wall ~300s (timeout fired); Layer A envtest 25.41s; available for Layer B kind suite ~270s — Layer B's BeforeSuite (cert-manager wait + helm install --wait + Pod Ready) plus 7 spec executions exceeded this
- Rerun: total wall 315s; `go test` wall ~300s (same SIGKILL pattern)
- Phase 02.1 originally measured Layer B alone at ~190s; with cascade-3 fix landed (Plan 02.2-05) and a healthy controller, Layer B + Layer A combined needs ~200-510s — 300s budget is structurally short; 600s gives 2× headroom

From `02.2-05-VERIFICATION.md` §"Section 5 Root-Cause Summary" point 4:
- Manager-log activity captured 19:25:17Z through 19:33:18Z (~8.3 minutes) shows AT LEAST 3 of 7 Layer B specs reaching `AfterEach` cleanup: `output-plan` + `exceed-output-task` (output_test.go), `cap-plan` + `hang-task` (caps_test.go), `fail-plan` + `alpha-fail`/`beta-fail`/`gamma-fail` (failure_test.go). The remaining 4 specs (2× wave_test.go, 2× credproxy_test.go) did NOT have visible manager-log reconciliation activity in the captured window — they may not have started, or may have started but the activity was outside the captured tail window. With the 600s budget, all 7 should run to completion within `-timeout=5m` (the per-spec Ginkgo cap).

From `02.2-05-VERIFICATION.md` §"Section 5 Fix landscape" Option A:
"Bump `timeout 300s` to `timeout 600s` in the Makefile `test-int` recipe (Plan 02.2-06 — RECOMMENDED). Single-line edit at `Makefile:124`: `timeout 300s go test ...` → `timeout 600s go test ...`. Tradeoff: relaxes the budget contract that Plan 02.2-01 fix #5 deliberately set; the contract's purpose was to bound CI cost. Mitigation: 600s is still bounded; the contract becomes 'go test fits in 10 min' rather than 'go test fits in 5 min'. Phase 02.1's original measurement (190s for Layer B alone, plus ~25s Layer A) supports a 480-600s budget being realistic. CI's outer cost is bounded by the per-job timeout in `.github/workflows/ci.yaml`. Likely closes the cascade chain in one shot."

From `.continue-here.md` "Critical Anti-Patterns" (load-bearing for this plan's execution):
1. Do NOT pitch a comprehensive audit. User has chosen tactical iteration 4 times now; cascade-4 being test-budget (structurally different from cascades 1-3) validates the choice — comprehensive audit would still not have predicted a budget-vs-actual mismatch.
2. Do NOT touch `charts/tide/values.yaml`. Chart is FIXED contract; binary catches up (and the Makefile is test-side infrastructure, not chart-side). This plan touches the Makefile only.
3. Do NOT dispatch Plan 02.2-02 alongside Plan 02.2-06 in the same execute wave when Plan 02.2-02's checkpoint artifact records BLOCKED on 02.2-01-VERIFICATION.md. (Surfaced in `<execution_notes>` above.)

From CLAUDE.md "Failure semantics at wave boundaries" contract:
- Failed task → siblings in same wave continue
- Dependents in later waves never dispatch
- Plan 02.2-02 reads the gate artifact; if BLOCKED, Plan 02.2-02 stays gated
- Resumption state = indegree map + completed-task set, nothing more

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump Makefile:124 outer wall-time budget from `timeout 300s` to `timeout 600s`</name>
  <files>Makefile</files>

  <read_first>
    - Makefile (full file — particularly lines 118-130, the `Integration tests (TEST-02 — Phase 2)` section; line 122 is the `test-int:` target declaration with its prereq list; line 124 is the SINGLE edit target — the `timeout 300s go test ./test/integration/... -v -timeout=5m -ginkgo.v` recipe line; lines 126-128 are the `test-int-fast` Layer-A-only target — DO NOT TOUCH; lines 130+ are the `test-int-kind-prep` image-build target — DO NOT TOUCH)
    - .planning/phases/02.2-.../02.2-05-VERIFICATION.md §"Section 4 Wall-time Budget Check" and §"Section 5 Root-Cause Summary" point 3 — the directed approach for this fix; the cascade-4 surfacing confirmed; the next predicted cascade-5 (unknown structural shape) which this plan does NOT address per tactical scope
    - .planning/phases/02.2-.../02.2-05-PLAN.md Task 1 body — the structural model for THIS plan's Task 1; the edit pattern (single token replacement with regression guards on surrounding tokens) is mirrored exactly with `Makefile:124` `timeout 300s` substituted for `cmd/manager/main.go` flag definition
    - .planning/phases/02.2-.../02.2-RESEARCH.md (full file — the Phase 02.2 research; particularly Fix #5's origin which set the 300s budget contract that this plan relaxes — context for why 600s is still a bounded contract, not a regression to unbounded test time)
  </read_first>

  <action>
    Edit `Makefile` to bump the outer wall-time budget on the `test-int` recipe from `timeout 300s` to `timeout 600s`. This is a SINGLE-TOKEN replacement on line 124 (or thereabouts — the line number is approximate, the pattern is exact).

    **Edit A — outer timeout token (line 124):** Replace the literal token `timeout 300s` with `timeout 600s` on the `test-int` recipe line. The full line currently reads:

    ```
    		timeout 300s go test ./test/integration/... -v -timeout=5m -ginkgo.v
    ```

    After the edit, the line reads:

    ```
    		timeout 600s go test ./test/integration/... -v -timeout=5m -ginkgo.v
    ```

    The leading two-tab indentation (`\t\t`) stays verbatim. The `KUBEBUILDER_ASSETS=...` env-var prefix on the preceding line stays verbatim (it sits on its own line ending with a backslash continuation). All other tokens on the recipe line stay verbatim:
    - `go test` invocation: verbatim
    - `./test/integration/...` package pattern: verbatim
    - `-v` arg: verbatim
    - `-timeout=5m` arg (the INNER Ginkgo cap; T-02.2-15 mitigation): verbatim — DO NOT change to `-timeout=10m`
    - `-ginkgo.v` arg: verbatim

    **Constraints:**
    - **DO NOT touch** `charts/tide/values.yaml` — the chart contract is unchanged; the Makefile is test-side infrastructure.
    - **DO NOT touch** the `-timeout=5m` arg on the same recipe line. Bumping the inner cap is out of scope per T-02.2-15 acceptance; the new outer 10 min cap > inner 5 min cap is intentional (single hung spec gets SIGKILLed by Ginkgo before outer wall fires).
    - **DO NOT touch** the `test-int-fast` recipe (lines 126-128) — Layer-A-only target with its own 2m budget that is appropriate for envtest alone.
    - **DO NOT touch** the `test-int-kind-prep` recipe (lines 130+) — image-build target with no `timeout` wrapper.
    - **DO NOT touch** any other Makefile recipe — only the line 124 token `timeout 300s` is bumped.
    - **DO NOT add** new make targets (Option B from `02.2-05-VERIFICATION.md` §"Fix landscape" — splitting `test-int` into `test-int-layer-a` + `test-int-layer-b` separately-timed targets is deferred to Plan 02.2-07 if needed).
    - **DO NOT change** the `timeout 600s` value to something else (e.g. 480s, 900s, removing the timeout entirely) — 600s is the user's explicit Option A choice with 2× headroom over the prior 300s budget.
    - **DO NOT modify** any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/` — this task is Makefile-only.
    - **DO NOT modify** the `test-int:` target's inline comment (`## Run full integration test suite: Layer A (envtest) + Layer B (kind). Timeout 300s (TEST-02 budget). Requires Docker + kind.`). The comment text mentions "Timeout 300s" historically; updating the comment is OPTIONAL — acceptance gates do not require it. Leaving the comment stale is acceptable as long as the recipe edit is correct.

    Commit with message:
    ```
    fix(02.2-06): bump Makefile:124 outer timeout 300s -> 600s — close 02.2-05 BLOCKED gate (cascade-4 test-budget)
    ```

    (Per user emphasis on commit message quality. The body should reference `02.2-05-VERIFICATION.md` §"Section 5 Fix landscape" Option A and note tactical scope — this is the fourth tactical iteration; cascade-4 is structurally different from cascades 1-3 (budget-vs-actual rather than flag-vs-binary); cascade-5 if any remains unknown per T-02.2-16.)
  </action>

  <verify>
    <automated><![CDATA[
      # New outer timeout token present (≥ 1 occurrence to allow for any future test-int-* recipes also using 600s)
      [ "$(grep -c 'timeout 600s' Makefile)" -ge 1 ] && \
      # Old outer timeout token purged (the literal `timeout 300s` MUST be gone from the Makefile entirely)
      [ "$(grep -c 'timeout 300s' Makefile)" -eq 0 ] && \
      # The full recipe line shape preserved: `timeout 600s go test ./test/integration/...` on the SAME line
      grep -E 'timeout 600s go test \./test/integration/\.\.\. -v -timeout=5m -ginkgo\.v' Makefile && \
      # Inner Ginkgo cap (-timeout=5m) preserved (T-02.2-15 regression guard)
      [ "$(grep -c -- '-timeout=5m' Makefile)" -ge 1 ] && \
      # KUBEBUILDER_ASSETS env-var prefix preserved (regression guard on the line above 124)
      [ "$(grep -c 'KUBEBUILDER_ASSETS=' Makefile)" -ge 1 ] && \
      # test-int-fast Layer-A-only target preserved verbatim (regression guard)
      grep -E 'go test \./test/integration/envtest/\.\.\. -v -timeout=2m -ginkgo\.v --ginkgo\.label-filter=.envtest.' Makefile && \
      # test-int-kind-prep target preserved (regression guard)
      [ "$(grep -c '^test-int-kind-prep:' Makefile)" -eq 1 ] && \
      # No other timeout-related changes (no new timeout tokens introduced beyond the bump)
      [ "$(grep -c 'timeout 480s\|timeout 900s\|timeout 1200s' Makefile)" -eq 0 ] && \
      # Option B NOT implemented (no new make targets named test-int-layer-a or test-int-layer-b or test-int-envtest or test-int-kind beyond the existing test-int-kind-prep)
      [ "$(grep -cE '^(test-int-layer-a|test-int-layer-b|test-int-envtest|test-int-kind):' Makefile)" -eq 0 ] && \
      # No production source modified by this task (Makefile-only)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v -E '(^| )Makefile$' | grep -v '^$' && echo "FAIL: non-Makefile files modified by Task 1" && exit 1 || \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **New outer timeout token present:** `grep -c 'timeout 600s' Makefile` returns ≥ 1
    - **Old outer timeout token purged:** `grep -c 'timeout 300s' Makefile` returns exactly 0 (the literal string `timeout 300s` is gone from the Makefile)
    - **Full recipe line shape preserved:** `grep -E 'timeout 600s go test \./test/integration/\.\.\. -v -timeout=5m -ginkgo\.v' Makefile` matches (the entire recipe line shape on line 124 is preserved with only the timeout token bumped)
    - **Inner Ginkgo cap preserved (T-02.2-15 mitigation):** `grep -c -- '-timeout=5m' Makefile` returns ≥ 1 (the `-timeout=5m` inner cap is untouched)
    - **KUBEBUILDER_ASSETS env-var prefix preserved:** `grep -c 'KUBEBUILDER_ASSETS=' Makefile` returns ≥ 1 (the env-var prefix on the line above 124 is preserved)
    - **`test-int-fast` Layer-A-only target preserved:** `grep -E 'go test \./test/integration/envtest/\.\.\. -v -timeout=2m -ginkgo\.v --ginkgo\.label-filter=.envtest.' Makefile` matches (line 128 verbatim — DO NOT regress its 2m budget)
    - **`test-int-kind-prep` target preserved:** `grep -c '^test-int-kind-prep:' Makefile` returns exactly 1 (image-build target untouched)
    - **No spurious new timeout values:** `grep -c 'timeout 480s\|timeout 900s\|timeout 1200s' Makefile` returns 0 (only 600s, not some other value the executor might pick)
    - **Option B NOT implemented (tactical scope honored):** `grep -cE '^(test-int-layer-a|test-int-layer-b|test-int-envtest|test-int-kind):' Makefile` returns 0 (no Layer A / Layer B split targets added — this is Option A only)
    - **No production source modified:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows ONLY `Makefile` modified by this task (no edits to `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/`, or any other planning artifact)
  </acceptance_criteria>

  <done>
    `Makefile:124` (or thereabouts — the line number is approximate, the pattern `timeout 600s go test ./test/integration/... -v -timeout=5m -ginkgo.v` is exact) has the outer wall-time budget bumped from `timeout 300s` to `timeout 600s`. The inner Ginkgo `-timeout=5m` arg is preserved verbatim (T-02.2-15). All other recipe tokens (`KUBEBUILDER_ASSETS=...`, `go test`, `./test/integration/...`, `-v`, `-ginkgo.v`) are preserved verbatim. The `test-int-fast` and `test-int-kind-prep` recipes are untouched. No Layer A / Layer B split targets added (Option B deferred). No production source modified. Committed with the `fix(02.2-06): bump Makefile:124 outer timeout 300s -> 600s — close 02.2-05 BLOCKED gate (cascade-4 test-budget)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-06-VERIFICATION.md (clean run + rerun + gate decision)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-05-VERIFICATION.md (full file — 348 lines; the structural template this artifact mirrors verbatim; frontmatter shape with `phase`/`plan`/`task`/`artifact`/`captured_on`/`captured_by_host`/`captured_by_branch`/`clean_run_exit`/`clean_run_wall_seconds`/`clean_run_specs_passed`/`clean_run_specs_skipped`/`clean_run_specs_failed`/`clean_run_phase`/`rerun_exit`/`rerun_wall_seconds`/`rerun_specs_passed`/`rerun_specs_skipped`/`rerun_specs_failed`/`rerun_phase`/`gate_decision`/`gate_reason` fields; Section 1/2/3/4/5/6 layout; per-spec outcome tables; root-cause analysis prose; threat-model-mitigations-applied list; "What This Artifact Did Not Touch" closing section; particularly note the §"Pod-State Evidence" and §"Manager Log Evidence" blocks which are LOAD-BEARING for the gate decision and the §"Section 5 Fix landscape" enumeration shape used when BLOCKED)
    - .planning/phases/02.2-.../02.2-04-VERIFICATION.md (the earlier verification — for the spec list and the per-spec outcome table shape; the Before/After Comparison structure)
    - .planning/phases/02.2-.../02.2-01-VERIFICATION.md (the earliest verification — for the reproducer bash shape at Sections 1.1 and 2.1)
    - Makefile (post-Task-1 state — verify the `timeout 600s` bump is committed before this task runs; particularly lines 118-130)
    - test/integration/kind/suite_test.go (the Layer B kind suite harness — for understanding which specs are expected to run; particularly the `BeforeSuite` cert-manager wait + helm install pattern and the `AfterSuite` skip predicated on `KEEP_KIND_CLUSTER=true`)
    - .planning/phases/02.2-.../02.2-05-SUMMARY.md (Plan 02.2-05's summary — for the cascade-3 closure evidence and the cascade-4 surfacing analysis; particularly the manager-log evidence that at least 3 of 7 specs reached `AfterEach` cleanup)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-05-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md` using the same structural template.

    **Recovery-lesson PRE-STEP (from `.continue-here.md` Plan 02.2-05 lesson):** BEFORE running the clean reproducer, author `02.2-06-VERIFICATION.md` with PRELIMINARY frontmatter — the load-bearing fields (`phase: 02.2`, `plan: 06`, `task: 2`, `artifact: verification`, `captured_on: <today>`, `captured_by_host: <host>`, `captured_by_branch: <branch>`, and placeholder `gate_decision: PENDING` line) — so that if mid-run executor API errors or context window pressure interrupt the long-running runtime verification, the artifact's gate field still exists with a recoverable PENDING state. Then update `gate_decision: PENDING` to APPROVED or BLOCKED after Sub-step C is complete.

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit (set -o pipefail makes the bash captured exit reflect make's real exit, not tee's):
    set -o pipefail
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-06-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state for the artifact:**
    ```bash
    # Post-clean diagnostic snapshot — needed for both APPROVED and BLOCKED gate analysis
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-06-clean-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-06-clean-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-06-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=20m 2>&1 | tee /tmp/02.2-06-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-06-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-06-clean-pvc-phase.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block from the captured log — APPROVED case shows 7 Pass / 0 Skip / 0 Fail; BLOCKED case shows 0/7 PASS or a partial outcome). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-05-VERIFICATION.md` §"Section 1" Per-Spec Outcome Table.

    **Critically: capture the manager logs.** The clean-run's `tide-controller-manager` Pod state is the load-bearing evidence:
    - **APPROVED case:** Pod `READY 1/1 STATUS Running RESTARTS ≤ 1`; manager logs show "starting manager"; NO `flag provided but not defined: -<any-flag>` line (cascade-3 + cascade-2 fixes preserved); Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed`
    - **BLOCKED case A — budget still insufficient (600s also overflows):** Pod state same as clean (`Running 0 restarts`); manager-log activity shows continued spec progression beyond Plan 02.2-05's tail-window cutoff; `timeout 600s go test` SIGKILLs the process again; Section 5 Root-Cause Summary classifies cascade-5 as `budget-vs-actual` and §"Fix landscape" routes to Option B (split targets) for Plan 02.2-07
    - **BLOCKED case B — new structural cascade (cascade-5 different bug class):** Pod state OR manager-log activity OR Ginkgo output reveals a different failure shape (spec-flake on a specific spec, kind networking quirk, etcd race, harness bug, image-pull failure, etc.); Section 5 Root-Cause Summary classifies cascade-5 using T-02.2-16's enum (`flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`)

    **Sub-step B — Idempotent Re-run:**
    ```bash
    # Pre-state: tide-test cluster + cert-manager + tide release REVISION 1 persisted from clean run.
    # (KEEP_KIND_CLUSTER=true preserves the AfterSuite skip from Phase 02.1 D-03)
    set -o pipefail
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-06-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    **After rerun:** capture same diagnostic snapshot (`/tmp/02.2-06-rerun-*.txt`) for the rerun:
    ```bash
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-06-rerun-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-06-rerun-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-06-rerun-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=20m 2>&1 | tee /tmp/02.2-06-rerun-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-06-rerun-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-06-rerun-pvc-phase.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes. Verify the helm install on rerun succeeded due to `--replace` (Plan 02.2-01's fix). Verify the cluster was NOT deleted by AfterSuite (T-02.2-02 mitigation from Plan 02.2-01; re-verified at runtime).

    **Sub-step C — Author the artifact:**
    Write `02.2-06-VERIFICATION.md` mirroring `02.2-05-VERIFICATION.md`'s structural template. Required frontmatter (note `plan: 06`, `task: 2`; once Sub-step C completes, update the PRELIMINARY `gate_decision: PENDING` line to APPROVED or BLOCKED):
    ```yaml
    ---
    phase: 02.2
    plan: 06
    task: 2
    artifact: verification
    captured_on: <YYYY-MM-DD>
    captured_by_host: <host>
    captured_by_branch: <git branch / worktree name>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary including the cascade type if BLOCKED>
    ---
    ```

    Required sections (mirror `02.2-05-VERIFICATION.md` headers verbatim):
    - **Section 1 — Clean Start Run** (reproducer + per-spec outcome table + wall-time + exit + Pod-state evidence + manager-log evidence as the load-bearing diagnostic)
    - **Section 2 — Idempotent Re-run** (reproducer + per-spec outcome table + wall-time + exit + rerun-specific evidence — both runs are required for an APPROVED gate)
    - **Section 3 — Before/After Comparison** (table comparing Plan 02.2-05 BLOCKED outcome vs Plan 02.2-06 outcomes on clean + rerun; particularly: did the 600s budget close the BLOCKED gate, and what is the NEW failure mode if BLOCKED again — what cascade type per T-02.2-16)
    - **Section 4 — Wall-time Budget Check** (same table shape as `02.2-05-VERIFICATION.md` §4; both runs must show `go test ≤ 600s` for APPROVED; ADDITIONAL row required per T-02.2-14 mitigation: flag `budget headroom marginal` if observed wall ≥ 500s even on APPROVED)
    - **Section 5 — Gate Decision** (the APPROVED-requires-ALL table with 5 conditions on BOTH runs + Required Next Step + Root-Cause Summary if BLOCKED + Fix landscape if BLOCKED — enumerate Options A through D for Plan 02.2-07 per the cascade type classified)
    - **Section 6 — Threat-Model Mitigations Applied** (T-02.2-14 / T-02.2-15 / T-02.2-16 each with evidence + carry-forward T-02.2-01 through T-02.2-13 noted as preserved)
    - **"What This Artifact Did Not Touch"** closing section
    - **"Closing Prose"** with one-paragraph summary

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/` (Task 1's Makefile edit is already committed before this task runs). Task 3 edits Plan 02.2-02 (a planning artifact, not source) — and only fires if `gate_decision: APPROVED`. Document this explicitly in the artifact's "What This Artifact Did Not Touch" closing section.

    **APPROVED gate (5-of-5 must hold on BOTH clean run AND idempotent rerun):**
    1. **Wall-time ≤ 600s:** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` (the new budget contract — bumped 300s → 600s per Option A) AND no `timeout 600s` exit-124 line appears in either captured log on the go test step
    2. **Manager Pod state:** `tide-controller-manager` Pod `STATUS=Running` with `RESTARTS ≤ 1` (Plan 02.2-05's cascade-3 fix preserved — no CrashLoopBackOff)
    3. **Manager log clean:** zero `flag provided but not defined` errors of any kind in the captured manager-log excerpts (all 5 chart args defined per Plan 02.2-05 termination of flag-mismatch cascade chain)
    4. **PVC preserved:** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO` (Plan 02.2-03's PVC fix preserved)
    5. **7/7 Layer B Ginkgo specs PASS:** Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed` AND the per-spec outcome table records each of the 7 specs as PASS on both clean run AND rerun

    **BLOCKED gate fires if ANY of the 5 conditions fail on either run.** The artifact's §"Section 5 Root-Cause Summary" documents WHICH gate failed, with raw log evidence, and §"Fix landscape" enumerates the next-iteration options based on cascade-5 classification:
    - **Option A** (one-more-budget-bump): if cascade-5 is `budget-vs-actual` AND observed wall is within 100s of 600s, escalate to Option B from `02.2-05-VERIFICATION.md` (split Layer A and Layer B targets) rather than another bump.
    - **Option B** (Layer A / Layer B split — deferred from Plan 02.2-05): create `test-int-envtest` + `test-int-kind` separately-timed targets; `test-int: test-int-envtest test-int-kind` composite. Closes any further outer-budget pressure since each layer gets its own bounded budget.
    - **Option C** (spec-level fix): if cascade-5 is `spec-flake`, identify the specific spec from the Ginkgo summary's `Failed` list and route to a targeted fix plan (likely in `test/integration/kind/<spec>_test.go`).
    - **Option D** (harness-level fix): if cascade-5 is `harness-bug` or `kind-infra`, route to `test/integration/kind/suite_test.go` (BeforeSuite/AfterSuite) or to kind configuration (`kind-config.yaml` if exists, or `test-int-kind-prep`'s image-load step).

    Each option in the BLOCKED §"Fix landscape" MUST include the cascade-5 classification (one of `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) per T-02.2-16 acceptance.

    Task 3 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring — Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3 also did NOT fire).

    **If gate_decision = APPROVED:** Skip the Root-Cause Summary section. Note in the closing prose that Plan 02.2-02 (closeout) is unblocked once Task 3 wires its checkpoint to read this artifact, and that the cascade chain has terminated successfully after 4 tactical iterations (02.2-03 chart PVC, 02.2-04 metrics flag, 02.2-05 webhook-cert-path flag, 02.2-06 Makefile timeout). T-02.2-14 mitigation requires checking observed wall: if either run's wall ≥ 500s, the Section 4 table MUST contain the literal token `budget headroom marginal` and the closing prose MUST flag that preemptive escalation to Option B (split targets) for a future plan would be prudent even though this gate APPROVED.

    Commit with one of:
    ```
    docs(02.2-06): runtime verification — APPROVED gate (cascade-4 test-budget resolved; 7/7 Layer B specs PASS clean + rerun within 600s)
    ```
    OR
    ```
    docs(02.2-06): runtime verification — BLOCKED gate (Makefile timeout bump landed; cascade-5 surfaced: <cascade-type-and-summary>)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists and has the required frontmatter shape
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md ] && \
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 06' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^task: 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^artifact: verification' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -eq 1 ] && \
      # Gate decision is one of APPROVED or BLOCKED (PENDING is not acceptable as a final state; if PENDING the executor must update it before commit)
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md && \
      # Required structural sections exist
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 4' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times (both runs)
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md && \
      grep -E '^rerun_exit: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md && \
      # Threat-model mitigations applied section exists
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-14/15/16 all referenced
      [ "$(grep -c 'T-02.2-14' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-15' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-16' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      # Cascade-5 classification present (per T-02.2-16 acceptance) — one of the six classes must appear in the artifact
      grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md && \
      # Manager log evidence captured (the load-bearing diagnostic for APPROVED vs BLOCKED)
      [ "$(grep -c 'starting manager\|flag provided but not defined\|tide-controller-manager' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      # Pod-state evidence captured (load-bearing per plan-checker tightening)
      [ "$(grep -c 'CrashLoopBackOff\|Running\|RESTARTS\|kubectl.*get pod' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      # New budget reference (600s) captured in the artifact prose (proves the bump is referenced)
      [ "$(grep -c '600s\|≤ 600\|<= 600' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -ge 1 ] && \
      # No production source modified by this task (Task 1's Makefile edit is already committed; Task 3 has not fired yet)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-06-VERIFICATION.md' | grep -v '^$' && echo "FAIL: non-artifact files modified beyond Task 1's pre-committed edit" && exit 1 || \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-06-VERIFICATION.md ]` returns true
    - **Frontmatter has required fields:** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 06' <artifact>` returns 1 AND `grep -c '^task: 2' <artifact>` returns 1 AND `grep -c '^artifact: verification' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches (PENDING is not acceptable as a final state)
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: -?[0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' <artifact>` matches (rerun_exit allows -1 to denote skipped per `02.2-04-VERIFICATION.md` convention, but APPROVED case requires both = 0)
    - **All six structural sections present:** each `grep -c '^## Section [1-6]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all three new threats referenced:** `grep -c 'Threat-Model Mitigations Applied' <artifact>` ≥ 1 AND `grep -c 'T-02.2-14' <artifact>` ≥ 1 AND `grep -c 'T-02.2-15' <artifact>` ≥ 1 AND `grep -c 'T-02.2-16' <artifact>` ≥ 1
    - **Cascade-5 classification present (T-02.2-16 mitigation):** `grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other' <artifact>` matches at least once — the artifact classifies the cascade type observed (or notes "cascade chain terminated" if APPROVED with no cascade-5)
    - **Manager-log evidence captured (load-bearing for gate decision):** the artifact includes a verbatim excerpt of `kubectl logs deploy/tide-controller-manager` from the clean run — either the "starting manager" success message + 7-spec Ginkgo summary (APPROVED case) or evidence of the new failure shape (BLOCKED case)
    - **Pod-state evidence captured (load-bearing per plan-checker tightening):** the artifact includes verbatim output of `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` showing either `STATUS=Running RESTARTS ≤ 1` (APPROVED) or evidence of a different failure (BLOCKED)
    - **New 600s budget referenced in prose:** `grep -c '600s\|≤ 600\|<= 600' <artifact>` ≥ 1 (the artifact references the new outer-timeout value)
    - **Behavior gate (APPROVED case — ALL 5 conditions on BOTH runs must hold for APPROVED):**
      - (a) Clean run captured `EXIT=0` AND 7 Layer B specs marked PASS in the per-spec table
      - (b) Rerun captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (c) **Wall-time budget (LOAD-BEARING):** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` AND no `timeout 600s` exit-124 line appears in either captured log on the go test step
      - (d) Manager logs show "starting manager" (or analogous controller-runtime startup log) AND zero `flag provided but not defined` errors of any kind in the captured manager logs (Plan 02.2-05's full flag-mismatch chain termination preserved)
      - (e) **Pod-state (LOAD-BEARING):** `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` shows `STATUS=Running` AND restart count ≤ 1
      - (f) **PVC preservation (Plan 02.2-03 fix not regressed):** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO`
      - (g) **T-02.2-14 marginal-headroom check:** if `clean_run_wall_seconds ≥ 500` OR `rerun_wall_seconds ≥ 500`, the artifact's §Section 4 table contains the literal token `budget headroom marginal` and the closing prose flags preemptive Option B escalation as prudent (note: marginal-headroom does NOT block APPROVED, but must be surfaced)
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, a `Root-Cause Summary` section MUST exist with numbered analysis points mirroring `02.2-05-VERIFICATION.md` §"Root-Cause Summary" — particularly: (a) confirmation that Plan 02.2-06 Task 1's Makefile timeout bump IS in effect (`grep -c 'timeout 600s' Makefile` ≥ 1 evidence cited); (b) which cascade-5 surfaced (flag-mismatch / budget-vs-actual / spec-flake / harness-bug / kind-infra / other); (c) why this was or was NOT predictable; (d) fix-landscape options for Plan 02.2-07 (Option A = another budget bump only if `budget-vs-actual` AND wall ≥ 500s; Option B = Layer A / Layer B split targets; Option C = spec-level fix; Option D = harness-level fix)
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows only the new `02.2-06-VERIFICATION.md` artifact (Task 1's `Makefile` edit must be committed BEFORE this task runs; Task 3 has not fired yet)
  </acceptance_criteria>

  <done>
    `02.2-06-VERIFICATION.md` exists at the correct path with the structural template mirroring `02.2-05-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, and `gate_decision: APPROVED` or `gate_decision: BLOCKED` (PENDING is not a final state). Sections 1-6 captured with per-spec outcome tables, Pod-state evidence, manager-log evidence (load-bearing for APPROVED vs BLOCKED), before/after comparison vs `02.2-05-VERIFICATION.md`, wall-time budget check (with T-02.2-14 marginal-headroom flag if observed wall ≥ 500s), and threat-model mitigations (T-02.2-14/15/16 + carry-forward T-02.2-01 through T-02.2-13). If APPROVED, all 5 APPROVED-gate conditions hold on BOTH clean run AND rerun: both runs exit 0 with 7/7 specs PASS, both ≤ 600s, no `flag provided but not defined` errors, Pod STATUS=Running with restart ≤ 1, PVC Bound RWO. Task 3 fires to wire Plan 02.2-02's checkpoint. If BLOCKED, Task 3 does NOT fire and the failure routes to a planner re-evaluation per the wave-failure-semantics contract — with §Section 5 Root-Cause Summary classifying cascade-5 per T-02.2-16's enum. Committed with a `docs(02.2-06): runtime verification — APPROVED gate ...` or `docs(02.2-06): runtime verification — BLOCKED gate (Makefile timeout bump landed; cascade-5 surfaced: <type>)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-06-VERIFICATION.md + bump depends_on (gated on Task 2 APPROVED gate)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file — 382 lines; frontmatter at lines 1-40 — particularly line 6 `depends_on: ["02.2-01"]`, lines 31-39 `key_links` block, lines 22-26 `must_haves.truths` block; `<context>` block at lines 63-68; Task 1 body at lines 72-136 — `<read_first>` at lines 77-79, `<what-built>` at lines 81-83, `<how-to-verify>` at lines 85-101, `<verify>` automated block at lines 117-123, `<acceptance_criteria>` at lines 125-129, `<done>` at lines 131-135 — all of which currently reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, and 02.2-05 Task 3 did NOT fire)
    - .planning/phases/02.2-.../02.2-06-VERIFICATION.md (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the HARD precondition for this Task 3 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - .planning/phases/02.2-.../02.2-05-PLAN.md Task 3 body — the structural model for this task's edits; mirror its surgical-edit pattern with `02.2-05` substituted for `02.2-06`
    - CLAUDE.md "Failure semantics at wave boundaries" contract — the load-bearing logic that this task fires ONLY on APPROVED; on BLOCKED, this task halts and Plan 02.2-02 stays gated on its current `02.2-01-VERIFICATION.md` wiring
  </read_first>

  <action>
    **Gate precondition (HARD GATE):** Task 3 fires ONLY IF Task 2's `02.2-06-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED` (or PENDING, which is not a final state), halt Task 3 — Plan 02.2-02 stays unmodified (it will continue to reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, and 02.2-05 Task 3 also did NOT fire); the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md.

    Verify the precondition by running:
    ```bash
    grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md
    ```
    If this does NOT match (exit non-zero), halt Task 3 and surface the BLOCKED gate. Exit with a clear message: "Task 3 halted: 02.2-06-VERIFICATION.md gate_decision is not APPROVED — routing back to planner for next iteration (cascade-5 or whatever surfaced)."

    If APPROVED, make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-06-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering. There are 31 references to `02.2-01-VERIFICATION.md` in `02.2-02-PLAN.md` (verified at plan-time by `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returning 31); all 31 are redirected to `02.2-06-VERIFICATION.md` in one pass.

    **NOTE on recovery state:** Plan 02.2-03 Task 4 was intended to do the same surgical edit (`02.2-01-VERIFICATION.md` → `02.2-03-VERIFICATION.md`) but never fired because its Task 3 gate was BLOCKED. Plan 02.2-04 Task 3 was intended to do the same surgical edit (`02.2-01-VERIFICATION.md` → `02.2-04-VERIFICATION.md`) but never fired because its Task 2 gate was BLOCKED. Plan 02.2-05 Task 3 was intended to do the same surgical edit (`02.2-01-VERIFICATION.md` → `02.2-05-VERIFICATION.md`) but never fired because its Task 2 gate was BLOCKED on cascade-4. As a consequence, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today. This task's edits land the long-pending rewire, redirecting to `02.2-06-VERIFICATION.md`.

    **Pre-edit count assertion (BEFORE any edits):** Run `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md` and confirm the result is exactly `31`. If it is NOT 31 (e.g. some prior task fired partially and the count is different), HALT — the rewire scope assumed at plan-time has drifted and the task needs planner re-evaluation. If exactly 31, proceed with edits.

    **Edit A — frontmatter `depends_on` (line 6 of 02.2-02-PLAN.md):** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-06"]`. This is a single one-line change. The new dependency captures the gate-artifact dependency directly (Plan 02.2-06 produces the gate; Plan 02.2-02 reads it). The transitive dependency on Plans 02.2-01, 02.2-03, 02.2-04, and 02.2-05 (which Plan 02.2-06 depends on transitively) is implicit in execute-phase's wave-ordering logic.

    **Edit B — frontmatter `must_haves.truths` block (lines 22-26 of 02.2-02-PLAN.md):** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-06-VERIFICATION.md`. Specifically the bullet currently reads "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → change to "Closeout fires ONLY when 02.2-06-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit C — frontmatter `key_links` block (lines 31-39 of 02.2-02-PLAN.md):** Update the entry that references `02.2-01-VERIFICATION.md` to reference `02.2-06-VERIFICATION.md`. Concretely: change the `from:` line's path from `02.2-01-VERIFICATION.md` to `02.2-06-VERIFICATION.md` and keep the rest of the entry unchanged.

    **Edit D — `<context>` block (line 66 of 02.2-02-PLAN.md):** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-06-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>` (lines 77-79 of 02.2-02-PLAN.md):** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-06-VERIFICATION.md (the gate artifact produced by Plan 06 Task 2 — Phase 02.2-06 closed the BLOCKED runtime gate from 02.2-05-VERIFICATION.md by bumping the Makefile:124 outer wall-time budget from timeout 300s to timeout 600s, giving Layer A + Layer B's full 7-spec end-to-end run sufficient headroom to complete within budget against the healthy controller manager Pod that Plan 02.2-05 unblocked)`. Update the analog Phase 02.1 reference if present.

    **Edit F — Task 1 `<what-built>` (lines 81-83 of 02.2-02-PLAN.md):** Update the prose to reference Plan 02.2-06's deliverable (Makefile timeout bump + runtime re-verification) in addition to Plans 02.2-01, 02.2-03, 02.2-04, and 02.2-05's deliverables. Specifically append: "Plan 02.2-06 then bumped the Makefile:124 outer wall-time budget from `timeout 300s` to `timeout 600s`, closing the cascade-4 test-budget contract insufficiency that was SIGKILLing `go test` before Ginkgo could flush its Layer B summary line, and allowing the 7-spec end-to-end suite to complete within the new bounded budget on both clean run and idempotent rerun."

    **Edit G — Task 1 `<how-to-verify>` (lines 85-101 of 02.2-02-PLAN.md):** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-06-VERIFICATION.md`. There are multiple occurrences in the numbered steps; update each.

    **Edit H — Task 1 `<verify>` automated block (lines 117-123 of 02.2-02-PLAN.md):** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-06-VERIFICATION.md`. Two grep paths need bumping.

    **Edit I — Task 1 `<acceptance_criteria>` (lines 125-129 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-06-VERIFICATION.md`.

    **Edit J — Task 1 `<done>` (lines 131-135 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-06-VERIFICATION.md` if any are present.

    **Post-edit count assertion (AFTER all edits):** Run `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `0`. Run `grep -c '02.2-06-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `31`. If either count is not as expected, the redirection drifted — surface the diff and halt before commit.

    **(Edits to ROADMAP/STATE content NOT performed by this task — Plan 02.2-02 reads live ROADMAP/STATE at execution time via its own `<read_first>` and `<action>` blocks and will derive the current 6-plan / 5-done structure. Prescriptive content edits from THIS plan would (a) write a snapshot that is stale by the time Plan 02.2-02 runs, and (b) leak Plan 02.2-06's responsibilities into Plan 02.2-02's body. Plan 02.2-02 is responsible for its own ROADMAP/STATE shape decisions at execution time.)**

    **Constraints:**
    - Do NOT edit Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, or 02.2-06 (this plan) — only `02.2-02-PLAN.md` is touched.
    - Do NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `Makefile`, `.github/`. This task is documentation-only.
    - Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — those are owned by Plan 02.2-02's tasks and by the orchestrator.
    - The diff against `02.2-02-PLAN.md` must be minimal — only the 31 references to `02.2-01-VERIFICATION.md` are changed to `02.2-06-VERIFICATION.md`, plus the one `depends_on` value change. No new tasks added, no existing tasks removed, no structural reorganization, no plan-list/decisions edits.

    Commit with message:
    ```
    docs(02.2-06): wire Plan 02.2-02 checkpoint to 02.2-06-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-06; 31 references redirected)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Gate precondition: 02.2-06-VERIFICATION.md gate_decision is APPROVED (else this task should have been halted)
      [ "$(grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md)" -eq 1 ] && \
      # Edit A: depends_on bumped to 02.2-06
      [ "$(grep -cE 'depends_on: \[.{0,5}02.2-06.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 1 ] && \
      # Old depends_on removed (must not contain 02.2-01, 02.2-03, 02.2-04, or 02.2-05 as the active depends_on value)
      [ "$(grep -cE '^depends_on: \[.{0,5}02.2-0[1345].{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-06-VERIFICATION.md is now the gate reference — count should be exactly 31 (the rewire is one-for-one)
      [ "$(grep -c '02.2-06-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 31 ] && \
      # 02.2-01-VERIFICATION.md references purged from 02.2-02-PLAN.md (count must be exactly 0)
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-03-VERIFICATION.md, 02.2-04-VERIFICATION.md, 02.2-05-VERIFICATION.md references purged (those Task 4/3/3 didn't fire, so they were never there; assert anyway)
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-04-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-05-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Task 1 checkpoint grep gate uses the new path
      grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-06-VERIFICATION.md" .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Confirm no other plans were modified by this task (regression guard)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-02-PLAN.md' | grep -v '^$' && echo "FAIL: non-target plan files modified" && exit 1 || \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Gate precondition met (HARD GATE):** `grep -cE '^gate_decision: APPROVED$' 02.2-06-VERIFICATION.md` returns exactly 1 (Task 3 ran ONLY because Task 2's gate was APPROVED; otherwise Task 3 was halted before edits landed and this gate would fail-open)
    - **depends_on bumped (Edit A):** `grep -cE 'depends_on: \[.{0,5}02.2-06.{0,5}\]' 02.2-02-PLAN.md` returns exactly 1 AND `grep -cE '^depends_on: \[.{0,5}02.2-0[1345].{0,5}\]' 02.2-02-PLAN.md` returns 0 (old values 02.2-01, 02.2-03, 02.2-04, 02.2-05 all purged from the depends_on field)
    - **New gate-artifact reference count (Edits B-J):** `grep -c '02.2-06-VERIFICATION.md' 02.2-02-PLAN.md` returns exactly 31 (one-for-one redirection from the 31 plan-time `02.2-01-VERIFICATION.md` references; tighter than 02.2-05-PLAN.md's `≥ 25` because the rewire is now well-characterized after multiple BLOCKED gates left the original 31 references intact)
    - **Old gate-artifact references purged:** `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-03-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-04-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-05-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 (no stale references to any prior gate artifact)
    - **Task 1 `<verify>` automated grep targets the new path:** the regex `grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-06-VERIFICATION.md"` matches inside `02.2-02-PLAN.md`
    - **No other plans or source modified (regression guard):** `git status --porcelain` shows ONLY `02.2-02-PLAN.md` modified in this commit; no edits to `02.2-01-PLAN.md`, `02.2-03-PLAN.md`, `02.2-04-PLAN.md`, `02.2-05-PLAN.md`, `02.2-06-PLAN.md` (this plan), or any production source
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has been surgically edited to redirect its checkpoint from `02.2-01-VERIFICATION.md` to `02.2-06-VERIFICATION.md`, with `depends_on` bumped from `["02.2-01"]` to `["02.2-06"]`. All 31 prior `02.2-01-VERIFICATION.md` references are now `02.2-06-VERIFICATION.md` (one-for-one, count exactly 31). No stale `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, or `02.2-05-VERIFICATION.md` references exist. No other plans or production source are modified. Committed with the `docs(02.2-06): wire Plan 02.2-02 checkpoint to 02.2-06-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-06; 31 references redirected)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Makefile:124` (test-side outer wall-time budget) → `go test ./test/integration/...` invocation | The Makefile wraps `go test` in GNU `timeout` to bound CI wall-time. This plan relaxes the bound from 300s to 600s; the boundary's purpose (preventing unbounded test time) is preserved at a wider value. |
| `Makefile:124` (outer `timeout` cap) → `Makefile:124` (inner `-timeout=5m` Ginkgo cap) | Two nested time bounds: outer 10 min (this plan's new value) > inner 5 min (preserved verbatim). A single hung spec gets SIGKILLed by Ginkgo's internal cap first; the outer `timeout` only fires if the whole suite exceeds 10 min. T-02.2-15 mitigation. |
| Task 1 (Makefile edit) → Task 2 (runtime verification) | Task 2 is run after Task 1's Makefile edit is committed; the runtime reproducer exercises the new 600s budget end-to-end against the cascade-3-fixed binary from Plan 02.2-05. |
| Task 2 (gate artifact) → Task 3 (Plan 02.2-02 wiring) | Task 3 reads Task 2's `gate_decision:` frontmatter. APPROVED → Task 3 fires; BLOCKED → Task 3 halts and the BLOCKED failure routes to a planner re-evaluation. |
| Plan 02.2-06 Task 3 edits → Plan 02.2-02 checkpoint target | Surgical edit only — `depends_on` and 31 verification-artifact references in `02.2-02-PLAN.md` are bumped; no other planning artifacts touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-14 | Tampering | Bumping `timeout 300s → 600s` at `Makefile:124` could mask a real Layer B performance regression where specs grow from healthy ~3-4 min to unhealthy ~6 min over time, hiding a real slowdown behind the wider budget. The 300s budget was originally a guard against runaway test time; relaxing it to 600s widens the latitude for unnoticed regression. | mitigate | `02.2-06-VERIFICATION.md` §"Section 4 Wall-time Budget Check" MUST record the actual observed wall-time for both clean run and rerun, and the artifact's §Section 4 table MUST flag `budget headroom marginal` if observed wall ≥ 500s (within 100s of the new 600s ceiling) even on APPROVED. The closing prose MUST recommend preemptive escalation to Option B (Layer A / Layer B split targets) for a future plan if marginal-headroom is flagged. This preserves the original 300s-budget's "regression guard" intent at the new 600s level: marginal-but-passing runs surface a signal that the budget is again becoming tight. **Acceptance gate:** `grep -E '^clean_run_wall_seconds: [0-9]+$' 02.2-06-VERIFICATION.md` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' 02.2-06-VERIFICATION.md` matches AND if either value is ≥ 500, `grep -c 'budget headroom marginal' 02.2-06-VERIFICATION.md` returns ≥ 1. |
| T-02.2-15 | Tampering | Bumping the OUTER `timeout 300s → 600s` without touching the INNER `-timeout=5m` Ginkgo arg leaves the inner cap LESS than the outer cap (5 min < 10 min). This is INTENTIONAL — a single hung spec gets SIGKILLed by Ginkgo's internal cap (5 min) before the outer wall fires (10 min). The risk is that an executor mistakenly bumps both caps to "match", which would defeat the per-spec safety net. | mitigate | Preserve `-timeout=5m` verbatim on the same recipe line — do NOT bump it alongside the outer `timeout`. The verify block's acceptance gate ensures `grep -c -- '-timeout=5m' Makefile` returns ≥ 1 after the edit, and the action constraints explicitly forbid changing the inner cap. The two-tier cap structure (outer 10 min total > inner 5 min per spec) is documented in the action body and the closing-prose rationale. **Acceptance gate:** `grep -c -- '-timeout=5m' Makefile` returns ≥ 1 after Task 1 AND the regression-guard regex `grep -E 'timeout 600s go test \./test/integration/\.\.\. -v -timeout=5m -ginkgo\.v' Makefile` matches (proving both caps are preserved on the same recipe line). |
| T-02.2-16 | Denial of Service | Tactical scope means cascade-5 (if any) is genuinely unknown. The flag-mismatch cascade chain has terminated at Plan 02.2-05; the test-budget fix at this plan is structurally different (budget-vs-actual rather than flag-vs-binary). Cascade-5 could be a structurally distinct class: a spec flake that only manifests under sustained controller load, an etcd backup-restore race, a kind networking quirk, an image-pull failure, a harness bug not visible at plan-time, or something else. The accepted risk is that even with the 600s budget, the runtime gate may BLOCK again with cascade-5 — and unlike cascades 1-3 (predictable flag mismatches), cascade-5's shape is unforeseeable. | accept | Accepted with the requirement that if Task 2's gate is BLOCKED, `02.2-06-VERIFICATION.md` §"Section 5 Root-Cause Summary" MUST classify the cascade type using one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other` so Plan 02.2-07 (or equivalent next iteration) can pick the right fix shape from the start without re-litigating the diagnosis. Note: a `flag-mismatch` classification at this point would be a regression of Plan 02.2-05's fix and would require immediate investigation; `budget-vs-actual` at observed wall ≥ 600s indicates the new budget is still insufficient and Plan 02.2-07 should be Option B (split targets); the other four classes (`spec-flake`, `harness-bug`, `kind-infra`, `other`) require targeted fixes that this plan cannot specify in advance. **Acceptance gate:** if Task 2 records `gate_decision: BLOCKED`, the artifact contains one of the six classification tokens (`grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other' 02.2-06-VERIFICATION.md` matches) AND §Section 5 names the specific failure evidence (which spec failed, which log line surfaced the issue, etc.). If APPROVED, the artifact still references the cascade-5 enum in a "cascade chain terminated" closing note (no cascade-5 surfaced). |

</threat_model>

<verification>
After all three tasks complete (or after Task 2 if its gate is BLOCKED):

**Source gates (Task 1):**
- `Makefile:124` has outer `timeout 600s` (was `timeout 300s` — bump landed)
- The inner `-timeout=5m` Ginkgo arg is preserved verbatim on the same recipe line (T-02.2-15)
- The full recipe line shape `timeout 600s go test ./test/integration/... -v -timeout=5m -ginkgo.v` is intact
- `test-int-fast` (Layer-A-only target) is preserved verbatim with its own 2m budget (regression guard)
- `test-int-kind-prep` (image-build target) is preserved verbatim (regression guard)
- No Layer A / Layer B split targets added (Option B not implemented — deferred to Plan 02.2-07 if needed)
- No production source modified by Task 1

**Runtime gates (Task 2):**
- `02.2-06-VERIFICATION.md` exists with all required frontmatter fields populated (`phase: 02.2`, `plan: 06`, `task: 2`, `artifact: verification`, exit codes, wall seconds, spec counts)
- `gate_decision` is one of APPROVED or BLOCKED (PENDING not a final state)
- The artifact's Sections 1-6 mirror `02.2-05-VERIFICATION.md`'s structural template
- Manager-log evidence (load-bearing for gate decision) is captured verbatim in the artifact
- Pod-state evidence (load-bearing per plan-checker tightening) is captured: STATUS=Running with restart count OR evidence of a different failure
- Wall-time evidence (load-bearing): both runs ≤ 600s for APPROVED
- T-02.2-14 marginal-headroom flag present if observed wall ≥ 500s even on APPROVED
- All three new threats (T-02.2-14/15/16) have evidence in §6
- Cascade-5 classification present per T-02.2-16 (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`)

**Wiring gates (Task 3 — gated on Task 2 APPROVED):**
- `02.2-02-PLAN.md` has `depends_on: ["02.2-06"]` and ALL 31 references updated from `02.2-01-VERIFICATION.md` to `02.2-06-VERIFICATION.md` (exact count one-for-one)
- Stale `02.2-01-VERIFICATION.md`, `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, `02.2-05-VERIFICATION.md` references purged from `02.2-02-PLAN.md`
- No other plans modified

**Structural gates (T-02.2-14/15/16 mitigation):**
- Inner Ginkgo `-timeout=5m` cap NOT touched in Makefile
- Chart `charts/tide/values.yaml` NOT touched
- Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05 NOT modified
- Plan 02.2-02 modified ONLY in Task 3 (gated on APPROVED)
- `.planning/STATE.md` and `.planning/ROADMAP.md` NOT touched (orchestrator owns)
</verification>

<success_criteria>
- Task 1 source edit landed: `Makefile:124` outer `timeout 300s` bumped to `timeout 600s`; inner `-timeout=5m` Ginkgo cap preserved; `test-int-fast` and `test-int-kind-prep` recipes untouched; no Layer A / Layer B split targets added (Option B deferred)
- Task 2 runtime verification artifact authored at `.planning/phases/02.2-.../02.2-06-VERIFICATION.md` with `gate_decision: APPROVED` or `gate_decision: BLOCKED` and all required structural sections including load-bearing wall-time, Pod-state, manager-log, and cascade-5 classification evidence
- Task 3 (gated on Task 2 APPROVED) wired Plan 02.2-02's checkpoint to `02.2-06-VERIFICATION.md` with `depends_on: ["02.2-06"]` and all 31 stale `02.2-01-VERIFICATION.md` references one-for-one redirected
- If Task 2 returns BLOCKED: Task 3 does NOT fire; Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring); the orchestrator routes to a planner re-evaluation for Plan 02.2-07 (or equivalent) per the wave-failure-semantics contract and the tactical-iteration acceptance recorded in T-02.2-16; the cascade-5 classification in the verification artifact directs the planner to the right fix shape
- T-02.2-14/15/16 each have concrete evidence in the verification artifact and the source-edit acceptance gates
- Tactical scope honored: ONLY the outer `timeout 300s → 600s` token is bumped; inner `-timeout=5m` Ginkgo cap NOT bumped; `charts/tide/values.yaml` NOT touched; Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05 NOT modified; Plan 02.2-05's `cmd/manager/main.go` flag definitions preserved (verified by absence of `flag provided but not defined` errors in manager logs)
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-SUMMARY.md` documenting:
- The Makefile edit applied (Task 1): the single-line `Makefile:124` token bump from `timeout 300s` to `timeout 600s` + rationale (Plan 02.2-05's runtime evidence of 11+ min of healthy controller activity proves 600s is realistic headroom) + the preserved inner `-timeout=5m` Ginkgo cap (T-02.2-15) + the two-tier cap structure
- The runtime verification outcome (Task 2): gate decision (APPROVED or BLOCKED), per-spec outcomes, wall times, exit codes, Pod-state evidence, manager-log evidence, cascade-5 classification per T-02.2-16
- The Plan 02.2-02 wiring update (Task 3, if APPROVED): which references were updated (count exactly 31), depends_on bump, no plan-list/decisions edits per plan-checker preempt
- Any inconsistencies surfaced (e.g., marginal-headroom T-02.2-14 flag on APPROVED; cascade-5 surfacing on BLOCKED with its classification)
- The Phase 02.2 close-out chain progression: 4 source fixes (02.2-01) + 1 chart fix (02.2-03) + 1 binary metrics flag (02.2-04) + 1 binary webhook-cert-path flag (02.2-05) + 1 Makefile timeout bump (this plan) landed → runtime verified once (APPROVED) or routed back (BLOCKED) for next iteration
- Threat-model mitigations applied (T-02.2-14 / T-02.2-15 / T-02.2-16) with concrete evidence
- A note on the structural shift: cascades 1-3 were all flag-mismatch (predictable, terminated at 5/5 chart args defined); cascade-4 was budget-vs-actual (structurally different, single-line Makefile fix); cascade-5 (if any) shape is genuinely unknown per T-02.2-16 acceptance
</output>
</content>
</invoke>