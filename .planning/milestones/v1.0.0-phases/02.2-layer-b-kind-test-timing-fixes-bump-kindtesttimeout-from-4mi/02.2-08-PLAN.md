---
phase: "02.2"
plan: "08"
type: execute
wave: 7
gap_closure: true
depends_on: ["02.2-07"]
files_modified:
  - test/integration/kind/credproxy_test.go
  - test/integration/kind/failure_test.go
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, test-fixture, spec-flake, runtime-verification, kind-integration, phase-02.2, tactical, cascade-6, eventually-timeout-bump, chain-terminator-candidate]

threat_model_summary:
  - "T-02.2-21 (mitigate): bumping `Eventually` timeouts could mask a real performance regression in Task dispatch (e.g., if `resolveProject` or `stampTaskLabels` grows slow over time, the larger timeout would hide the regression rather than surface it). **Mitigation:** `02.2-08-VERIFICATION.md` §Section 1 MUST record the ACTUAL observed time-to-Pod-creation for the credproxy HARN-03 pod-wait spec AND the ACTUAL observed time-to-Failed-phase for the AC3 beta-fail spec. If the observed time exceeds 90s for credproxy pod-wait OR 180s for AC3 phase-wait (75% of the new budgets), the artifact MUST contain the literal token `dispatch latency marginal` in §Section 1 and the closing prose so future iterations can preemptively investigate the underlying ResourceVersion conflict retry overhead. **Acceptance gate:** VERIFICATION frontmatter records `credproxy_pod_wait_observed_seconds: <N>` AND `ac3_phase_wait_observed_seconds: <M>`, with the `dispatch latency marginal` flag fired if N ≥ 90 OR M ≥ 180."
  - "T-02.2-22 (mitigate): the `60*time.Second` and `2*time.Minute` (== `120*time.Second`) patterns appear in OTHER specs in the same files for legitimate reasons (credproxy_test.go has THREE `60*time.Second` Eventually call sites at lines 95, 119, 135 — only line 95's pod-wait is the cascade-6 gate; failure_test.go has TWO `2*time.Minute` Eventually call sites at lines 130 (AC3 beta-fail Failed-phase gate) and 140 (alpha-fail Succeeded-phase — independent sibling, not the cascade-6 gate). Bumping the wrong occurrence would either over-loosen unrelated assertions OR miss the actual failing one. **Mitigation:** Task 1 acceptance assertions count pre-edit AND post-edit occurrences AND assert the post-edit count is exactly one less than pre-edit per file (proving exactly one replacement happened per file). The replacement target is identified by SURROUNDING CONTEXT (the lambda body / comment / spec name), not by line number, because line numbers may have shifted post-Plan-02.2-07 refactor. For credproxy_test.go: target the Eventually whose lambda captures `podName` via `if len(p.Name) > 0 { podName = p.Name; return true }` and whose assertion message is `\"A Pod should be created for the credproxy-task Job\"` (this is the FIRST `60*time.Second` Eventually in the file, immediately preceding the credproxy container presence check). For failure_test.go: target the Eventually whose lambda returns `t.Status.Phase` for the `beta-fail` Task and whose assertion is `Should(Equal(\"Failed\"))` with comment `// Wait for β to fail.`."
  - "T-02.2-23 (accept): cascade-7 (if any) is genuinely unknown — but cascade-6 is the FIRST FITNESS issue in the chain (all 5 prior cascades 1–5 were structural/correctness issues: PVC RWX→RWO, metrics flag, webhook-cert-path flag, test-budget, harness-bug fixture-incomplete). The fix is empirical (specs are running and reaching their assertions; they just need more time per the ResourceVersion conflict retry + initial-run-since-Phase-02.1 budget headroom analysis in `02.2-07-VERIFICATION.md` §Section 5). **Probability cascade-7 surfaces:** LOW — if all dispatch paths now work end-to-end (cascade-5 fix confirmed at runtime in 02.2-07), the 7 specs should PASS deterministically once timeouts are adequate. **Accepted with the requirement:** if Task 2 records `gate_decision: BLOCKED`, `02.2-08-VERIFICATION.md` §Section 5 Root-Cause Summary MUST classify cascade-7 using the T-02.2-16 taxonomy (`flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) AND identify the specific spec(s) failing AND propose Plan 02.2-09's fix shape from the start so the next iteration is not a diagnostic round-trip. The note: a `flag-mismatch` or `budget-vs-actual` classification at this point would be a regression of Plans 02.2-04/05/06's fixes and would require immediate investigation."

must_haves:
  truths:
    - "`test/integration/kind/credproxy_test.go` has the pod-wait `Eventually` call's timeout bumped from `60*time.Second` to `120*time.Second` (polling interval `2*time.Second` and lambda body preserved verbatim; ONLY the pod-wait Eventually is changed — the other two `60*time.Second` Eventually call sites for credproxy container presence checks remain unchanged)"
    - "`test/integration/kind/failure_test.go` has the AC3 task-Failed-phase `Eventually` call's timeout bumped from `2*time.Minute` (== `120*time.Second`) to `240*time.Second` (polling interval `3*time.Second` and lambda body preserved verbatim; ONLY the beta-fail Failed-phase assertion is changed — the alpha-fail Succeeded-phase Eventually at line 140 remains unchanged)"
    - "`go build ./test/integration/kind/...` AND `go vet ./test/integration/kind/...` both exit 0 after Task 1"
    - "Option B contract preserved: zero modifications to `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `Makefile`, OR `test/integration/kind/suite_test.go` (applyHierarchy helper from Plan 02.2-07 stays intact). Other test files (wave_test.go, caps_test.go, output_test.go, testdata/three-task-wave.yaml) are also unmodified by this plan."
    - "`02.2-08-VERIFICATION.md` exists with frontmatter `phase: 02.2`, `plan: 08`, `task: 2`, `artifact: verification`, populated `gate_decision: APPROVED|BLOCKED` line, AND additional T-02.2-21 mitigation fields `credproxy_pod_wait_observed_seconds` and `ac3_phase_wait_observed_seconds`, plus Sections 1–6 mirroring `02.2-07-VERIFICATION.md`"
    - "APPROVED gate requires ALL 5 conditions on BOTH clean run AND idempotent rerun: wall ≤ 600s; Pod Running ≤ 1 restart; manager-log no `flag provided but not defined` errors AND no `no project found` errors; PVC `tide-projects` Bound RWO; 7/7 Layer B Ginkgo specs PASS"
    - "If `02.2-08-VERIFICATION.md` records `gate_decision: APPROVED`, this is the FIRST APPROVED gate in Phase 02.2's entire cascade chain — Task 3 fires and Plan 02.2-02's `depends_on` becomes `[\"02.2-08\"]` and all 31 references to `02.2-01-VERIFICATION.md` are redirected to `02.2-08-VERIFICATION.md`. Phase 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave."
    - "If `02.2-08-VERIFICATION.md` records `gate_decision: BLOCKED` with cascade-7, Task 3 does NOT fire — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring; Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3 also did NOT fire). Cascade-7 MUST be classified per T-02.2-16 taxonomy in §Section 5 Root-Cause Summary."
    - "T-02.2-21 marginal-dispatch-latency flag fires if observed credproxy pod-wait ≥ 90s OR AC3 phase-wait ≥ 180s — does NOT block APPROVED but MUST be surfaced as `dispatch latency marginal` literal token in §Section 1 and the closing prose"
  artifacts:
    - path: "test/integration/kind/credproxy_test.go"
      provides: "pod-wait Eventually timeout bumped to 120s (was 60s) — allows ResourceVersion conflict retry to complete within budget"
      contains: "120*time.Second"
    - path: "test/integration/kind/failure_test.go"
      provides: "AC3 task-Failed-phase Eventually timeout bumped to 240s (was 2*time.Minute) — first end-to-end run since Phase 02.1 needs headroom for full dispatch cycle"
      contains: "240*time.Second"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md"
      provides: "Runtime verification artifact with gate_decision frontmatter — authoritative gate for Plan 02.2-02 closeout AND T-02.2-21 dispatch-latency observation fields"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-08\"] + 31 references redirected to 02.2-08-VERIFICATION.md (gated on Task 2 APPROVED)"
      contains: "02.2-08"
  key_links:
    - from: "test/integration/kind/credproxy_test.go (pod-wait Eventually)"
      to: "ResourceVersion conflict retry cycle in task controller reconcile (per 02.2-07-VERIFICATION.md §Section 5)"
      via: "the timeout budget must accommodate the retry's extra reconcile cycles (~10–30s) on top of the initial Job-create round-trip"
      pattern: '120\*time\.Second.*Pod|Pod.*120\*time\.Second'
    - from: "test/integration/kind/failure_test.go (AC3 phase-wait Eventually)"
      to: "Task dispatch end-to-end cycle (Job create → Pod schedule → container exit-1 → Job Failed → Task status update)"
      via: "the timeout budget must accommodate the full dispatch round-trip with the now-active full hierarchy that Plan 02.2-07's applyHierarchy added"
      pattern: '240\*time\.Second.*Failed|Failed.*240\*time\.Second'
    - from: "02.2-08-VERIFICATION.md frontmatter gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 wires Plan 02.2-02's depends_on + checkpoint references (gated on APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md (depends_on field)"
      to: "this plan's verification artifact"
      via: "frontmatter depends_on: [\"02.2-08\"]"
      pattern: 'depends_on: \[.{0,5}02.2-08.{0,5}\]'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-07-VERIFICATION.md` §Section 5 "Cascade-6 Root Cause" (sixth iteration in Phase 02.2's cascade chain). Two distinct spec-flake failures surfaced after Plan 02.2-07 closed cascade-5 (the credproxy harness-bug):

**Failure A — `test/integration/kind/credproxy_test.go` HARN-03 pod-wait:** The pod-wait `Eventually` at the FIRST `60*time.Second` call site (the one whose lambda captures `podName` via `if len(p.Name) > 0 { podName = p.Name; return true }`, currently at line 95 post-Plan-02.2-07 refactor — but identified by lambda content not by line number per T-02.2-22) times out because the task controller hits a `ResourceVersion` conflict at first reconcile (`"Operation cannot be fulfilled on tasks.tideproject.k8s \"credproxy-task\": the object has been modified; please apply your changes to the latest version and try again"`). The conflict happens because `PlanReconciler.stampTaskLabels` concurrently modifies the Task during `materializeWaves` while `TaskReconciler` tries to update Task status. The retry resolves the conflict, but the retry + dispatch path (Job create → Pod schedule) takes > 60s total. Manager logs from `02.2-07-VERIFICATION.md` §Section 1 confirm: error at 15:50:05Z, `task cleanup` at 15:51:10Z (AfterEach's `deleteNamespace` ran 65s after spec start because the 60s `Eventually` timed out).

**Failure B — `test/integration/kind/failure_test.go` AC3 task-Failed-phase:** The AC3 phase-wait `Eventually` at the FIRST `2*time.Minute` call site (the one whose lambda returns `t.Status.Phase` for `beta-fail` and asserts `Should(Equal("Failed"))`, currently at line 130 — but identified by lambda content not by line number per T-02.2-22) times out because the full dispatch cycle for a testMode=fail-exit-1 Task (Job create → Pod schedule → container run → exit-1 → Job Failed → Task status updated to "Failed") exceeds 120s in the kind cluster. This spec hasn't run end-to-end since Phase 02.1 (the prior 5 cascades all gated the suite before reaching this spec), so the original 120s budget was calibrated against a faster dispatch path that no longer reflects reality with the full hierarchy (Project + Milestone + Phase + Plan + Task) now in play after Plan 02.2-07.

Both failures are **spec-flake** (timeout budgets too tight for the actual dispatch cycle). Zero production code changes required. Per `02.2-07-VERIFICATION.md` §"Fix landscape" Option C: "Both are spec-flake timeout issues; both fit in a single test-only plan; zero production code change. Both should be fixed together since they will both block the 7/7 spec gate independently."

**CRITICAL FRAMING NOTE (load-bearing — DO NOT inherit the wrong cascade class from prior verification framings):** Cascade-6 is a FITNESS issue, NOT a structural/correctness issue. All 5 prior cascades (cascade-1 PVC RWX→RWO; cascade-2 metrics-bind-address; cascade-3 webhook-cert-path; cascade-4 test-budget; cascade-5 fixture-incomplete) addressed structural mismatches in production code, chart, binary flags, Makefile, OR test fixture. Cascade-6 is the FIRST cascade in this chain where the specs themselves are correct, the production dispatch path is correct, and the only mismatch is between the assertions' time budgets and the actual observed dispatch latency. The fix is two single-token replacements in test assertions. Do NOT treat cascade-6 as a fixture-incompleteness or harness-bug problem — Plan 02.2-07's runtime evidence proves the fixture and dispatch path are now correct.

**User decision: bump both timeouts in a single test-only plan (Option C from 02.2-07-VERIFICATION.md §"Fix landscape").** The orchestrator passes this decision down so the planner does not re-litigate scope or split the two failures across two plans.

This plan fixes both spec-flake budgets in Task 1, re-runs the same end-to-end runtime reproducer captured in `02.2-07-VERIFICATION.md`, and (if APPROVED) lands the long-pending Plan 02.2-02 wiring rewire in Task 3. Because all 6 prior cascades are now closed (5 prior + cascade-5 from 02.2-07 + cascade-6 from THIS plan if Task 1 holds), achieving 7/7 Layer B Ginkgo specs PASS on clean + rerun within the 600s outer budget is the expected outcome for the first time in Phase 02.2's history. The expected gate state is:

- **APPROVED (chain terminator):** all 6 prior fixes preserved (manager Pod Running 0 restarts; no flag errors; no `no project found` errors; PVC Bound RWO; 600s budget sufficient); the credproxy pod-wait and AC3 phase-wait now have adequate headroom; all 7 Layer B specs (2 credproxy + 2 wave + 1 failure + 1 caps + 1 output) PASS on clean run AND idempotent rerun. Plan 02.2-02 (closeout) gets unblocked via Task 3's surgical edits. Phase 02.2 finally closes.
- **BLOCKED (cascade-7, unknown shape):** the timeout bumps land but a different failure surfaces — a real spec flake in wave/caps/output (none of which have run end-to-end with the full hierarchy yet), a kind-infra issue (image-pull regression, etcd race, networking quirk), a budget-vs-actual regression (if even 600s isn't enough for the now-slower full-hierarchy dispatch), or something genuinely unforeseeable per T-02.2-23 acceptance. The BLOCKED gate captures the new failure mode root cause AND classifies the cascade type per T-02.2-16 taxonomy so Plan 02.2-09 (or equivalent next iteration) can pick the right fix shape from the start.

**TACTICAL SCOPE — user explicitly chose Option C (single test-only plan covering both spec-flake fixes):**
- This plan modifies ONLY `test/integration/kind/credproxy_test.go` (one Eventually timeout change) AND `test/integration/kind/failure_test.go` (one Eventually timeout change)
- This plan does NOT modify any production source under `internal/`, `api/`, `cmd/`, `pkg/`, `charts/` — Option B contract from Plans 02.2-06/07 preserved
- This plan does NOT modify `test/integration/kind/suite_test.go` — the `applyHierarchy` helper from Plan 02.2-07 stays intact
- This plan does NOT modify other test files: `wave_test.go`, `caps_test.go`, `output_test.go`, `testdata/three-task-wave.yaml`
- This plan does NOT modify the `Makefile` — Plan 02.2-06's `timeout 600s` budget preserved verbatim
- This plan does NOT touch `charts/tide/values.yaml` — chart is FIXED contract per long-standing rule (`.continue-here.md` anti-pattern #2)
- This plan does NOT modify Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07 — only `02.2-02-PLAN.md` is touched (and only by Task 3, gated on APPROVED)
- This plan does NOT investigate the root cause of the ResourceVersion conflict (the underlying race between `PlanReconciler.stampTaskLabels` and `TaskReconciler` status updates) — that would be a production-code fix and is out of scope. The timeout bump is a tactical accommodation; the underlying race is documented in `02.2-07-VERIFICATION.md` §Section 5 and may be addressed in a future phase if dispatch latency consistently approaches the bumped budgets (T-02.2-21 marginal-flag mitigation surfaces this trigger).

**Plan 02.2-07's pattern this plan mirrors:**
Plan 02.2-07 ran a 3-task shape: T1 source edit (applyHierarchy helper + credproxy refactor), T2 runtime verification (`02.2-07-VERIFICATION.md` with `gate_decision` frontmatter), T3 surgical edit of `02.2-02-PLAN.md` gated on APPROVED. This plan mirrors that shape exactly with cascade-6's target (two single-token Eventually timeout replacements) substituted for cascade-5's target (helper + refactor). **The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic:** Task 3 fires ONLY if Task 2's `gate_decision: APPROVED`. **This plan is structurally simpler than 02.2-07** — no helper authoring, no fixture refactor, just two single-token replacements + runtime verify + gated rewire.

**Recovery state note (load-bearing for Task 3's rewire scope):** Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, and 02.2-07 Task 3 ALL DID NOT FIRE (their respective Task 2 gates were each BLOCKED on a different cascade). As a result, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (`grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returns 31 as of plan-time; verified during plan authoring). This plan's Task 3 lands the long-pending rewire if Task 2's gate is APPROVED, redirecting all 31 references to `02.2-08-VERIFICATION.md` in one pass.

**Recovery-lesson note from `.continue-here.md` Plan 02.2-05 protocol (carried forward through 02.2-06/07):** Write `02.2-08-VERIFICATION.md` WITH PRELIMINARY frontmatter immediately after the clean-run captures land in `/tmp/02.2-08-clean-run.log`, then add Sections 2–6 incrementally as the rerun completes and the cluster diagnostic snapshot is captured. This protects against mid-run executor API errors / context window pressure during the long-running runtime verification phase — the artifact's frontmatter (the load-bearing gate field) is preserved even if Sections 4–6 prose authoring is interrupted.

Purpose: close the BLOCKED runtime gate from `02.2-07-VERIFICATION.md` (cascade-5 fixture fix preserved; cascade-6 surfaced as spec-flake per dual Eventually timeout-budget-too-tight failures) by bumping the two flaking `Eventually` budgets in test-only files. Tactical iteration #6: replace two single tokens in two test files, re-verify, see if the cascade chain has finally terminated.

Output:
- `test/integration/kind/credproxy_test.go` with the pod-wait `Eventually` timeout bumped from `60*time.Second` to `120*time.Second` (line ~95 per plan-time grep; identified by lambda body, not line number)
- `test/integration/kind/failure_test.go` with the AC3 task-Failed-phase `Eventually` timeout bumped from `2*time.Minute` to `240*time.Second` (line ~130 per plan-time grep; identified by lambda body, not line number)
- `.planning/phases/02.2-.../02.2-08-VERIFICATION.md` mirroring `02.2-07-VERIFICATION.md`'s structural template; `gate_decision: APPROVED` or `BLOCKED`; T-02.2-21 mitigation fields `credproxy_pod_wait_observed_seconds` and `ac3_phase_wait_observed_seconds` populated
- If Task 2 APPROVED: `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-08"]` and ALL 31 `02.2-01-VERIFICATION.md` references redirected to `02.2-08-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
**Wave-dispatch concern for Plan 02.2-02 (executor MUST honor):** Plan 02.2-02 is the closeout plan and currently has `depends_on: ["02.2-01"]` with a `checkpoint:human-verify` Task 1 that reads `02.2-01-VERIFICATION.md`'s `gate_decision` field. Because Plan 02.2-01's verification artifact records `gate_decision: BLOCKED` (its checkpoint Task 3 was BLOCKED on the original cascade chain and was never updated; subsequent attempts via Plans 02.2-03/04/05/06/07 Task 3 ALL did not fire), Plan 02.2-02 MUST NOT be dispatched alongside Plan 02.2-08 in the same execute wave. The structural mitigation: execute-phase's wave-ordering reads `depends_on` from each plan's frontmatter and groups plans whose dependencies are all satisfied into the same wave. Plan 02.2-02's `depends_on` points at 02.2-01 (BLOCKED gate artifact); Plan 02.2-08's `depends_on` points at 02.2-07 (whose source-shape applyHierarchy helper IS landed and the cascade-5 fix IS confirmed at runtime even though its Task 2 runtime gate was BLOCKED on cascade-6 which THIS plan is closing). The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic: failed task → siblings continue, **dependents in later waves never dispatch**. Plan 02.2-02 stays a dependent-in-later-wave of `02.2-01-VERIFICATION.md`'s BLOCKED gate until THIS plan's Task 3 rewrites its `depends_on` to point at `02.2-08-VERIFICATION.md`'s (hopefully APPROVED) gate. If the executor dispatches Plan 02.2-02 alongside Plan 02.2-08 (e.g., due to a misread wave-derivation), Plan 02.2-02's Task 1 checkpoint will halt at the BLOCKED gate AND nothing further fires — but the structural intent is that Plan 02.2-02 dispatches AFTER Plan 02.2-08's Task 3 has landed, in a separate wave 8 (or later).

**Critical anti-patterns from `.continue-here.md` "Critical Anti-Patterns" (do NOT trigger):**
1. Do NOT pitch a comprehensive audit. User has chosen tactical iteration 6 times now; cascade-6 was correctly classified as spec-flake (a fitness issue, structurally different from cascades 1–5) by the runtime evidence in `02.2-07-VERIFICATION.md` §Section 5. Tactical iteration 7 (THIS plan) is the right shape.
2. Do NOT touch `charts/tide/values.yaml`. Chart is FIXED contract.
3. Do NOT dispatch Plan 02.2-02 alongside Plan 02.2-08 in the same execute wave when Plan 02.2-02's checkpoint artifact records BLOCKED. (See preceding paragraph.)
4. Do NOT spawn Phase 02.3. Cascade-6 is still within Phase 02.2's scope (closing the kind-test runtime gate); inline gap-closure is the correct pattern.
5. Do NOT modify production source files in this plan. Test-only changes per Option C — the entire dispatch chain in `internal/controller/task_controller.go` and the applyHierarchy helper in `suite_test.go` stay untouched.
6. Do NOT modify `test/integration/kind/suite_test.go`. The applyHierarchy helper from Plan 02.2-07 must stay intact — modifying it would risk a cascade-5 regression. Only `credproxy_test.go` and `failure_test.go` are touched by Task 1.
7. Do NOT inherit `02.2-06-VERIFICATION.md` §Section 5's wrong framing (Wave Ready gate hypothesis). That framing was already corrected in `02.2-07-VERIFICATION.md` §Section 5. This plan's `02.2-08-VERIFICATION.md` §Section 5 should re-state the corrected root cause from `02.2-07-VERIFICATION.md` (the gate was always `resolveProject` at task_controller.go:217) AND surface the cascade-6 ROOT CAUSE (ResourceVersion conflict retry + first-run-since-Phase-02.1 budget headroom analysis).
</execution_notes>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/.continue-here.md
@.planning/debug/credproxy-dispatch-guard.md
@CLAUDE.md

<interfaces>
<!-- Key call-site contracts the executor needs. Extracted from credproxy_test.go + failure_test.go at plan-time. -->
<!-- Executor should use these directly — no codebase exploration needed beyond confirming the lambda body matches. -->

**Cascade chain status (for plan readers' orientation):**
| # | Class | Status | Plan | Fix Shape |
|---|-------|--------|------|-----------|
| 1 | PVC RWX → RWO | CLOSED | 02.2-03 | Chart accessModes templating + test --set override |
| 2 | --metrics-bind-address | CLOSED | 02.2-04 | Binary flag definition in cmd/manager/main.go |
| 3 | --webhook-cert-path | CLOSED | 02.2-05 | Binary flag definition + webhook.NewServer wiring |
| 4 | Makefile timeout 300s | CLOSED | 02.2-06 | Single-line Makefile bump to 600s |
| 5 | credproxy fixture incomplete | CLOSED | 02.2-07 | applyHierarchy helper + credproxy refactor |
| 6 | Eventually timeout bumps (spec-flake) | **THIS PLAN** | 02.2-08 | Two single-token Eventually timeout replacements |

**Target file 1 — `test/integration/kind/credproxy_test.go` (post-Plan-02.2-07 state):**

At plan-authoring time, the file contains exactly THREE `60*time.Second` Eventually call sites:

1. **Line ~95 — THE CASCADE-6 GATE (Failure A target):**
   ```go
   // Wait for a Pod to be created (Job spawns a Pod).
   var podName string
   Eventually(func() bool {
       pods := &corev1.PodList{}
       _ = k8sClient.List(ctx, pods, client.InNamespace(credproxyNS))
       for _, p := range pods.Items {
           if len(p.Name) > 0 {
               podName = p.Name
               return true
           }
       }
       return false
   }, 60*time.Second, 2*time.Second).Should(BeTrue(),
       "A Pod should be created for the credproxy-task Job")
   ```
   **Identifying signature:** the lambda captures `podName` via `if len(p.Name) > 0 { podName = p.Name; return true }`; the assertion message is `"A Pod should be created for the credproxy-task Job"`; this is the FIRST `60*time.Second` Eventually in the file (sequentially before the credproxy container presence Eventually).

2. **Line ~119 — NOT the cascade-6 gate (do NOT modify):**
   ```go
   // Check if the credproxy sidecar container is present.
   Eventually(func() bool { ... InitContainers ... Containers ... credproxy ... }, 60*time.Second, 2*time.Second).Should(BeTrue(),
       "Pod should contain a credproxy container (tide-credproxy)")
   ```
   **Identifying signature:** the lambda iterates `p.Spec.InitContainers` and `p.Spec.Containers` looking for a container name containing `"credproxy"` or `"tide-credproxy"`; the assertion message is `"Pod should contain a credproxy container (tide-credproxy)"`. This Eventually runs AFTER the pod-wait Eventually (which would have already established the Pod exists), so it does NOT need a longer budget. **DO NOT MODIFY.**

3. **Line ~135 — NOT the cascade-6 gate (do NOT modify):**
   ```go
   // Wait for a Pod (second spec).
   Eventually(func() bool {
       pods := &corev1.PodList{}
       _ = k8sClient.List(ctx, pods, client.InNamespace(credproxyNS))
       return len(pods.Items) > 0
   }, 60*time.Second, 2*time.Second).Should(BeTrue())
   ```
   **Identifying signature:** belongs to the SECOND HARN-03 spec ("credproxy container log contains ..." at line ~126); the lambda is shorter (`return len(pods.Items) > 0`) with NO `podName` capture and NO assertion message. **DO NOT MODIFY** — if cascade-7 surfaces and this spec also flakes, Plan 02.2-09 can address it separately.

**Target file 2 — `test/integration/kind/failure_test.go` (post-Phase-02.1 state):**

At plan-authoring time, the file contains exactly TWO `2*time.Minute` Eventually call sites:

1. **Line ~130 — THE CASCADE-6 GATE (Failure B target):**
   ```go
   // Wait for β to fail.
   Eventually(func() string {
       t := &tideprojectv1alpha1.Task{}
       if err := k8sClient.Get(ctx, client.ObjectKey{Name: "beta-fail", Namespace: ns}, t); err != nil {
           return ""
       }
       return t.Status.Phase
   }, 2*time.Minute, 3*time.Second).Should(Equal("Failed"),
       "beta-fail should reach Failed phase (testMode=fail-exit-1)")
   ```
   **Identifying signature:** the lambda returns `t.Status.Phase` (string), the comment immediately above reads `// Wait for β to fail.`, the assertion is `Should(Equal("Failed"))`, the failure message is `"beta-fail should reach Failed phase (testMode=fail-exit-1)"`, and the lambda fetches the `"beta-fail"` Task. This is the FIRST `2*time.Minute` Eventually in the file.

2. **Line ~140 — NOT the cascade-6 gate (do NOT modify):**
   ```go
   // α (independent sibling) should eventually complete.
   Eventually(func() string {
       t := &tideprojectv1alpha1.Task{}
       if err := k8sClient.Get(ctx, client.ObjectKey{Name: "alpha-fail", Namespace: ns}, t); err != nil {
           return ""
       }
       return t.Status.Phase
   }, 2*time.Minute, 3*time.Second).Should(Equal("Succeeded"),
       "alpha-fail (independent sibling) should succeed even though beta-fail failed")
   ```
   **Identifying signature:** the lambda fetches the `"alpha-fail"` Task (NOT `"beta-fail"`); the comment above reads `// α (independent sibling) should eventually complete.`; the assertion is `Should(Equal("Succeeded"))`. **DO NOT MODIFY** — this is the alpha-fail assertion (independent sibling, succeeds via testMode=success), and `02.2-07-VERIFICATION.md` §Section 1 reported only the beta-fail assertion (line 130) as the cascade-6 gate. If alpha-fail also flakes post-fix (cascade-7), Plan 02.2-09 can address it.

**Replacement syntax note (LOAD-BEARING for executor):**

The two target Eventually calls use DIFFERENT syntaxes for their 120s budget — `60*time.Second` in credproxy_test.go vs `2*time.Minute` in failure_test.go. The seed's directive is "bump `120*time.Second` → `240*time.Second`" but the actual existing token in failure_test.go is `2*time.Minute` (which equals `120*time.Second` numerically but is syntactically different). The executor MUST:

- For **credproxy_test.go line ~95:** replace the literal token `60*time.Second` → `120*time.Second` (verbatim Go syntax; preserves single-line form)
- For **failure_test.go line ~130:** replace the literal token `2*time.Minute` → `240*time.Second` (Go syntax change from minutes-multiplier to seconds-multiplier). The acceptance check for failure_test.go therefore looks for `240*time.Second` in the new file AND verifies `2*time.Minute` count went from 2 → 1 (alpha-fail's call at line 140 still uses `2*time.Minute`).

If the executor prefers idiomatic-Go consistency, `4*time.Minute` is numerically equivalent to `240*time.Second` and would preserve the file's existing `time.Minute` convention. The acceptance gate accepts EITHER `240*time.Second` OR `4*time.Minute` as the valid post-edit token at line ~130. Pick one and stick with it. The `<verify>` automated block below tests for the disjunction `240\*time\.Second\|4\*time\.Minute`.

**Pre-edit grep counts (LOAD-BEARING — used by Task 1 verify to assert exactly one replacement per file):**

At plan-authoring time (verified by planner's plan-time grep, not by an executor's exploration):
- `grep -c '60\*time\.Second' test/integration/kind/credproxy_test.go` returns **3** (three call sites: pod-wait, credproxy presence, second-spec pod-wait)
- `grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go` returns **0** (no 120s timeout in file pre-edit)
- `grep -c '2\*time\.Minute' test/integration/kind/failure_test.go` returns **2** (two call sites: beta-fail Failed, alpha-fail Succeeded)
- `grep -c '120\*time\.Second' test/integration/kind/failure_test.go` returns **0** (no 120s seconds-form in file pre-edit)
- `grep -c '240\*time\.Second' test/integration/kind/failure_test.go` returns **0** (no 240s in file pre-edit)
- `grep -c '4\*time\.Minute' test/integration/kind/failure_test.go` returns **0** (no 4-min in file pre-edit)

**Post-edit grep count expectations (Task 1 acceptance):**
- credproxy_test.go: `60*time.Second` count goes from 3 → 2 (exactly one replacement); `120*time.Second` count goes from 0 → 1
- failure_test.go: `2*time.Minute` count goes from 2 → 1 (exactly one replacement — alpha-fail's call at line 140 still uses `2*time.Minute`); `240*time.Second` OR `4*time.Minute` count goes from 0 → 1 (whichever syntax the executor chose; the disjunction must total exactly 1)

**Plan 02.2-02 rewire scope (Task 3 — gated on Task 2 APPROVED):**

At plan-authoring time, `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returns **31**. None of the prior plans' Task 3 fired (Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3 ALL halted on BLOCKED gates). This plan's Task 3 redirects all 31 references in one pass: `02.2-01-VERIFICATION.md` → `02.2-08-VERIFICATION.md`, AND bumps `depends_on: ["02.2-01"]` → `depends_on: ["02.2-08"]`.

**From CLAUDE.md "Failure semantics at wave boundaries" contract:**
- Failed task → siblings in same wave continue
- Dependents in later waves never dispatch
- Plan 02.2-02 reads the gate artifact; if BLOCKED, Plan 02.2-02 stays gated
- Resumption state = indegree map + completed-task set, nothing more

**From `02.2-07-VERIFICATION.md` §Section 5 Root-Cause Summary (the corrected framing — load-bearing for §Section 5 of THIS plan's verification artifact):**
- Cascade-5 gate was `resolveProject` at `task_controller.go:217` (Step 3 of `reconcileDispatch`), NOT a "parent Wave Ready check"
- Cascade-5 fix CONFIRMED at runtime: Plan 02.2-07's applyHierarchy helper created the full hierarchy; manager logs show no `"no project found"` error for credproxy-task; `resolveProject` no longer fails
- Cascade-6 root cause for Failure A: `ResourceVersion` conflict at first reconcile (PlanReconciler's `stampTaskLabels` concurrently modifies the Task while TaskReconciler tries to update Task status). The retry resolves the conflict but adds ~10–30s to the dispatch cycle, exceeding the 60s pod-wait budget
- Cascade-6 root cause for Failure B: the AC3 spec hasn't run end-to-end since Phase 02.1 (prior cascades gated the suite before reaching it); the 120s budget was calibrated against a faster pre-hierarchy dispatch path; the full Project+Milestone+Phase+Plan+Task hierarchy added by Plan 02.2-07 adds reconcile overhead that pushes the cycle past 120s

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump two Eventually timeouts (credproxy_test.go pod-wait 60s→120s; failure_test.go AC3 phase-wait 2*time.Minute→240s)</name>
  <files>test/integration/kind/credproxy_test.go, test/integration/kind/failure_test.go</files>

  <read_first>
    - test/integration/kind/credproxy_test.go (full file — 166 lines post-Plan-02.2-07 refactor; particularly: lines 41–53 imports block; lines 55–69 Describe/BeforeEach/AfterEach structure; lines 73–123 spec 1 HARN-03 pod-wait + container presence — particularly lines 83–96 the THREE-target pod-wait Eventually whose lambda captures `podName` via `if len(p.Name) > 0 { podName = p.Name; return true }` with assertion message `"A Pod should be created for the credproxy-task Job"`; lines 99–120 the credproxy container presence Eventually with assertion message `"Pod should contain a credproxy container (tide-credproxy)"` — DO NOT MODIFY this Eventually; lines 126–165 spec 2 with the second pod-wait Eventually at line ~131-135 — DO NOT MODIFY this Eventually). Use the `<interfaces>` block above as the authoritative call-site map.
    - test/integration/kind/failure_test.go (full file — 196 lines; particularly: lines 25–35 imports block; lines 38–155 Describe block; lines 54–154 AC3 spec — particularly lines 124–131 the FIRST `2*time.Minute` Eventually whose lambda returns `t.Status.Phase` for `"beta-fail"` Task with comment `// Wait for β to fail.` and assertion `Should(Equal("Failed"))` — THIS IS THE TARGET; lines 134–141 the SECOND `2*time.Minute` Eventually for `"alpha-fail"` with comment `// α (independent sibling) should eventually complete.` — DO NOT MODIFY this Eventually). Use the `<interfaces>` block above as the authoritative call-site map.
    - test/integration/kind/suite_test.go (REGRESSION GUARD — read to confirm the `applyHierarchy` helper from Plan 02.2-07 is present at expected location; Task 1 MUST NOT touch this file; `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` should return ≥ 1 both before and after Task 1)
    - .planning/phases/02.2-.../02.2-07-VERIFICATION.md (§Section 5 "Cascade-6 Root Cause" — the gap source explaining WHY these two specific Eventually call sites need a budget bump; §Section 1 Failed-spec table — the runtime evidence; §"Fix landscape" Option C — the user's decision shape)
    - .planning/phases/02.2-.../02.2-07-PLAN.md Task 1 body (structural model for THIS plan's Task 1 — the regression-guard pattern for git status --porcelain on production source paths; THIS plan's Task 1 substitutes the two-file timeout-bump pattern for cascade-5's helper-authoring pattern)
  </read_first>

  <action>
    Two-part task in this single auto-task: bump one Eventually timeout in `credproxy_test.go`, then bump one Eventually timeout in `failure_test.go`. Both are single-token replacements identified by surrounding context (lambda body / assertion message), NOT by line number — line numbers may have shifted post-Plan-02.2-07 refactor.

    **Part A — `test/integration/kind/credproxy_test.go` pod-wait Eventually:**

    Locate the Eventually whose:
    - Lambda body iterates `pods.Items` and captures `podName` via `if len(p.Name) > 0 { podName = p.Name; return true }`
    - Assertion message is exactly `"A Pod should be created for the credproxy-task Job"`
    - Is sequentially the FIRST `60*time.Second` Eventually in the file (occurs in spec 1's HARN-03 body, before the credproxy container presence Eventually)

    Replace the single token `60*time.Second` → `120*time.Second` in THAT Eventually call only. Preserve:
    - The polling interval `2*time.Second` (the comma-separated second argument) verbatim
    - The lambda body verbatim
    - The assertion message verbatim
    - All surrounding formatting (line breaks, indentation, parentheses, the `.Should(BeTrue(), "...")` shape) verbatim

    DO NOT modify:
    - The Eventually at line ~119 (the credproxy container presence check with assertion message `"Pod should contain a credproxy container (tide-credproxy)"`) — its `60*time.Second` budget is appropriate because it runs AFTER the pod-wait has already established Pod existence
    - The Eventually at line ~135 (the second spec's pod-wait — shorter lambda `return len(pods.Items) > 0`, no podName capture, no assertion message) — if cascade-7 surfaces and this spec also flakes, Plan 02.2-09 addresses it
    - The Eventually at line ~78 (the Task-creation wait at `30*time.Second`) — already passes within budget
    - The Eventually at line ~138 (the credproxy log-line check at `90*time.Second`) — different budget, not the cascade-6 gate
    - Imports, comments, struct types, helper calls, or anything outside the single target Eventually

    Pre-edit assertion (run BEFORE editing): `grep -c '60\*time\.Second' test/integration/kind/credproxy_test.go` must return exactly `3`. If it returns a different count, the file has drifted from plan-time state — HALT and surface the drift before editing.

    Post-edit assertion (run AFTER editing): `grep -c '60\*time\.Second' test/integration/kind/credproxy_test.go` must return exactly `2` (one fewer; the pod-wait target was bumped to 120s); `grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go` must return exactly `1` (the new pod-wait value).

    **Part B — `test/integration/kind/failure_test.go` AC3 task-Failed-phase Eventually:**

    Locate the Eventually whose:
    - Lambda body fetches the `"beta-fail"` Task and returns `t.Status.Phase` (string)
    - The comment immediately above reads exactly `// Wait for β to fail.` (note: literal Greek beta character β — preserve verbatim)
    - Assertion is `Should(Equal("Failed"))` with failure message `"beta-fail should reach Failed phase (testMode=fail-exit-1)"`
    - Is sequentially the FIRST `2*time.Minute` Eventually in the file

    Replace the single token `2*time.Minute` → `240*time.Second` in THAT Eventually call only. (Alternatively, `4*time.Minute` is numerically equivalent and preserves the file's `time.Minute` convention — both are accepted by the post-edit assertion. Pick one; the rest of this action assumes `240*time.Second` was chosen, but substitute `4*time.Minute` if preferred.)

    Preserve:
    - The polling interval `3*time.Second` (the comma-separated second argument) verbatim
    - The lambda body verbatim (including the `k8sClient.Get(ctx, client.ObjectKey{Name: "beta-fail", Namespace: ns}, t)` call)
    - The assertion `Should(Equal("Failed"), "beta-fail should reach Failed phase (testMode=fail-exit-1)")` verbatim
    - The comment `// Wait for β to fail.` verbatim
    - All surrounding formatting verbatim

    DO NOT modify:
    - The Eventually at line ~134 (the alpha-fail Succeeded-phase Eventually — lambda fetches `"alpha-fail"` Task, assertion `Should(Equal("Succeeded"))`, comment `// α (independent sibling) should eventually complete.`) — this is the alpha-fail assertion (independent sibling, succeeds via testMode=success); if it flakes post-fix (cascade-7), Plan 02.2-09 addresses it
    - The Eventually at line ~117 (the Task-creation wait at `30*time.Second` inside the for-loop) — already passes within budget, NOT the cascade-6 gate
    - The Consistently at line ~144 (the γ never-dispatches assertion at `30*time.Second`) — uses Consistently not Eventually, different semantics; NOT the cascade-6 gate
    - Imports, comments outside the target's immediate context, helper functions like `createNamespace`/`makeKindTask`, the planYAML literal — only the single Eventually's first time-argument token is changed

    Pre-edit assertion (run BEFORE editing): `grep -c '2\*time\.Minute' test/integration/kind/failure_test.go` must return exactly `2`. If it returns a different count, HALT and surface the drift.

    Post-edit assertion (run AFTER editing): `grep -c '2\*time\.Minute' test/integration/kind/failure_test.go` must return exactly `1` (one fewer; the beta-fail target was bumped); the disjunction `grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go` must return exactly `1` (the new beta-fail value, whichever syntax was chosen).

    **Build + vet gates (run AFTER both Parts A and B complete):**
    - `go build ./test/integration/kind/...` must exit 0 (compilation OK; the timeout-bump is type-compatible — both forms are `time.Duration`)
    - `go vet ./test/integration/kind/...` must exit 0 (no unused imports introduced; both files already import `"time"`)

    **Constraints (Option C contract — test-only changes only):**
    - **DO NOT modify** any production source under `internal/`, `api/`, `cmd/`, `pkg/`, `charts/`, `Makefile`, `.github/` — this task is test-fixture-only per Option C's contract
    - **DO NOT modify** `test/integration/kind/suite_test.go` — the `applyHierarchy` helper from Plan 02.2-07 must stay intact (modifying it would risk a cascade-5 regression). Confirm `git status --porcelain test/integration/kind/suite_test.go` returns empty after Task 1
    - **DO NOT modify** other test files: `wave_test.go`, `caps_test.go`, `output_test.go`, `testdata/three-task-wave.yaml`. If cascade-7 surfaces in those specs, Plan 02.2-09 addresses them in a future iteration
    - **DO NOT modify** the testdata file `test/integration/kind/testdata/three-task-wave.yaml` — referenced by `applyFile` in `wave_test.go`, unchanged across all of Phase 02.2
    - **DO NOT modify** ANY OTHER `60*time.Second` OR `2*time.Minute` Eventually call site in credproxy_test.go or failure_test.go — only the two specific call sites identified by surrounding context above are changed. Bumping more than one per file would over-loosen unrelated assertions
    - **DO NOT touch** `charts/tide/values.yaml` per `.continue-here.md` anti-pattern #2
    - **DO NOT modify** Plans 02.2-01, 02.2-02 (Task 3 will handle 02.2-02), 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07 — only the new `02.2-08-VERIFICATION.md` (Task 2) and `02.2-02-PLAN.md` (Task 3, gated) are touched by this plan
    - **DO NOT change** the imports block in either file — both already import `"time"` which provides both `time.Second` and `time.Minute`; no new import needed
    - **DO NOT add** new specs to either file — the bump is scope-limited to two existing Eventually call sites
    - **DO NOT investigate** or fix the underlying ResourceVersion conflict race condition in the production code — that's a future-phase concern; the timeout bump is a tactical accommodation (T-02.2-21 marginal-flag surfaces the trigger if dispatch latency stays high)

    Commit with message:
    ```
    fix(02.2-08): bump Eventually timeouts for cascade-6 spec-flake (credproxy pod-wait 60s→120s; AC3 beta-fail 2m→240s)
    ```

    Body should reference `02.2-07-VERIFICATION.md` §Section 5 (the gap source); note that Option C (single test-only plan) is the user's decision; note that production source AND `suite_test.go`'s applyHierarchy helper are untouched (Option B contract preserved from Plans 02.2-06/07); note the cascade-6 root cause (ResourceVersion conflict retry for credproxy; first-end-to-end-run since Phase 02.1 for failure AC3).
  </action>

  <verify>
    <automated><![CDATA[
      # === Part A — credproxy_test.go pod-wait Eventually bumped ===
      # Pre-edit grep counts assumed: 60*time.Second × 3 (lines 95, 119, 135). Post-edit: × 2.
      [ "$(grep -c '60\*time\.Second' test/integration/kind/credproxy_test.go)" -eq 2 ] && \
      # New 120s value present exactly once (the pod-wait)
      [ "$(grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go)" -eq 1 ] && \
      # The bumped Eventually's lambda body + assertion message are preserved (surrounding-context regression guard)
      grep -F 'A Pod should be created for the credproxy-task Job' test/integration/kind/credproxy_test.go && \
      grep -F 'if len(p.Name) > 0' test/integration/kind/credproxy_test.go && \
      # The credproxy container presence Eventually's assertion message is preserved (untouched)
      grep -F 'Pod should contain a credproxy container (tide-credproxy)' test/integration/kind/credproxy_test.go && \
      # The Task-creation wait at 30*time.Second is preserved (untouched)
      [ "$(grep -c '30\*time\.Second' test/integration/kind/credproxy_test.go)" -ge 1 ] && \
      # The startup-log wait at 90*time.Second is preserved (untouched — different budget, not the cascade-6 gate)
      [ "$(grep -c '90\*time\.Second' test/integration/kind/credproxy_test.go)" -ge 1 ] && \
      # === Part B — failure_test.go AC3 beta-fail Eventually bumped ===
      # Pre-edit grep counts assumed: 2*time.Minute × 2 (lines 130 beta-fail, 140 alpha-fail). Post-edit: × 1.
      [ "$(grep -c '2\*time\.Minute' test/integration/kind/failure_test.go)" -eq 1 ] && \
      # New 240s OR 4-minute value present exactly once (the beta-fail Failed-phase wait)
      [ "$(grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go)" -eq 1 ] && \
      # The bumped Eventually's lambda body + assertion message + comment are preserved (surrounding-context regression guard)
      grep -F 'beta-fail should reach Failed phase (testMode=fail-exit-1)' test/integration/kind/failure_test.go && \
      grep -F 'Wait for β to fail.' test/integration/kind/failure_test.go && \
      grep -F 'Name: "beta-fail"' test/integration/kind/failure_test.go && \
      # The alpha-fail Succeeded-phase Eventually is preserved (untouched)
      grep -F 'alpha-fail (independent sibling) should succeed even though beta-fail failed' test/integration/kind/failure_test.go && \
      grep -F 'α (independent sibling) should eventually complete.' test/integration/kind/failure_test.go && \
      # The 30*time.Second Task-creation wait + 30*time.Second Consistently γ-never-dispatches are preserved (untouched — not the cascade-6 gate)
      [ "$(grep -c '30\*time\.Second' test/integration/kind/failure_test.go)" -ge 2 ] && \
      # === Compile + vet gates ===
      go build ./test/integration/kind/... && \
      go vet ./test/integration/kind/... && \
      # === Regression guards — production source untouched (Option B contract preserved) ===
      [ -z "$(git status --porcelain internal/)" ] && \
      [ -z "$(git status --porcelain cmd/)" ] && \
      [ -z "$(git status --porcelain api/)" ] && \
      [ -z "$(git status --porcelain pkg/)" ] && \
      [ -z "$(git status --porcelain charts/)" ] && \
      [ -z "$(git status --porcelain Makefile)" ] && \
      # === Regression guards — applyHierarchy helper from Plan 02.2-07 untouched ===
      [ -z "$(git status --porcelain test/integration/kind/suite_test.go)" ] && \
      [ "$(grep -c 'func applyHierarchy' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # === Regression guards — other test files untouched ===
      [ -z "$(git status --porcelain test/integration/kind/wave_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/caps_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/output_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/testdata/three-task-wave.yaml)" ] && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Part A — credproxy_test.go pod-wait bumped:** `grep -c '60\*time\.Second' test/integration/kind/credproxy_test.go` returns exactly `2` (one fewer than pre-edit count of 3 — exactly one replacement happened)
    - **Part A — new 120s value present:** `grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go` returns exactly `1` (the new pod-wait value)
    - **Part A — pod-wait assertion message preserved (regression guard):** `grep -F 'A Pod should be created for the credproxy-task Job' test/integration/kind/credproxy_test.go` matches (the target Eventually's failure message is intact)
    - **Part A — pod-wait lambda body preserved (regression guard):** `grep -F 'if len(p.Name) > 0' test/integration/kind/credproxy_test.go` matches (the target Eventually's lambda is intact)
    - **Part A — credproxy container presence Eventually untouched:** `grep -F 'Pod should contain a credproxy container (tide-credproxy)' test/integration/kind/credproxy_test.go` matches (the OTHER `60*time.Second` Eventually that we deliberately did NOT modify is intact)
    - **Part A — 30s Task-create Eventually untouched:** `grep -c '30\*time\.Second' test/integration/kind/credproxy_test.go` returns ≥ 1
    - **Part A — 90s startup-log Eventually untouched:** `grep -c '90\*time\.Second' test/integration/kind/credproxy_test.go` returns ≥ 1
    - **Part B — failure_test.go AC3 beta-fail bumped:** `grep -c '2\*time\.Minute' test/integration/kind/failure_test.go` returns exactly `1` (one fewer than pre-edit count of 2 — exactly one replacement happened; alpha-fail's `2*time.Minute` still present)
    - **Part B — new 240s (or 4-min) value present:** `grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go` returns exactly `1` (the new beta-fail value, whichever syntax was chosen)
    - **Part B — beta-fail Eventually's lambda body + assertion message preserved (regression guard):** `grep -F 'beta-fail should reach Failed phase (testMode=fail-exit-1)' test/integration/kind/failure_test.go` matches AND `grep -F 'Wait for β to fail.' test/integration/kind/failure_test.go` matches AND `grep -F 'Name: "beta-fail"' test/integration/kind/failure_test.go` matches
    - **Part B — alpha-fail Succeeded-phase Eventually untouched:** `grep -F 'alpha-fail (independent sibling) should succeed even though beta-fail failed' test/integration/kind/failure_test.go` matches AND `grep -F 'α (independent sibling) should eventually complete.' test/integration/kind/failure_test.go` matches (the OTHER `2*time.Minute` Eventually we deliberately did NOT modify is intact)
    - **Part B — 30s Task-create + Consistently γ-never-dispatches preserved:** `grep -c '30\*time\.Second' test/integration/kind/failure_test.go` returns ≥ 2
    - **go build exits 0:** `go build ./test/integration/kind/...` returns exit code 0
    - **go vet exits 0:** `go vet ./test/integration/kind/...` returns exit code 0
    - **Production source untouched (Option B contract — LOAD-BEARING):** all of `git status --porcelain internal/`, `git status --porcelain cmd/`, `git status --porcelain api/`, `git status --porcelain pkg/`, `git status --porcelain charts/`, `git status --porcelain Makefile` return empty (zero modified lines)
    - **applyHierarchy helper from Plan 02.2-07 untouched (cascade-5 regression guard):** `git status --porcelain test/integration/kind/suite_test.go` returns empty AND `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` returns ≥ 1 (helper still present post-Task 1)
    - **Other test files untouched:** `git status --porcelain test/integration/kind/wave_test.go test/integration/kind/caps_test.go test/integration/kind/output_test.go test/integration/kind/testdata/three-task-wave.yaml` returns empty (zero modified lines)
  </acceptance_criteria>

  <done>
    `test/integration/kind/credproxy_test.go` has the pod-wait Eventually's timeout bumped from `60*time.Second` to `120*time.Second` (identified by the lambda's `if len(p.Name) > 0 { podName = p.Name; return true }` body and the assertion message `"A Pod should be created for the credproxy-task Job"`; the other two `60*time.Second` Eventually call sites for credproxy container presence and second-spec pod-wait are untouched; the polling interval `2*time.Second` and the lambda body and assertion message are preserved verbatim). `test/integration/kind/failure_test.go` has the AC3 beta-fail-Failed-phase Eventually's timeout bumped from `2*time.Minute` to `240*time.Second` (or equivalently `4*time.Minute`; identified by the lambda fetching `"beta-fail"` Task and returning `t.Status.Phase`, the comment `// Wait for β to fail.`, and the assertion `Should(Equal("Failed"), "beta-fail should reach Failed phase (testMode=fail-exit-1)")`; the alpha-fail Succeeded-phase Eventually at line ~134 is untouched; the polling interval `3*time.Second` and the lambda body are preserved verbatim). `go build ./test/integration/kind/...` and `go vet ./test/integration/kind/...` both exit 0. No production source modified — `internal/`, `api/`, `cmd/`, `pkg/`, `charts/`, `Makefile` all untouched (Option B contract preserved). `test/integration/kind/suite_test.go` untouched — applyHierarchy helper from Plan 02.2-07 intact (cascade-5 regression guard). Other test files (`wave_test.go`, `caps_test.go`, `output_test.go`, `testdata/three-task-wave.yaml`) untouched. Committed with the `fix(02.2-08): bump Eventually timeouts for cascade-6 spec-flake (credproxy pod-wait 60s→120s; AC3 beta-fail 2m→240s)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-08-VERIFICATION.md (clean run + rerun + gate decision + T-02.2-21 marginal-flag observation fields)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-07-VERIFICATION.md (the structural template this artifact mirrors verbatim — frontmatter shape with `phase`/`plan`/`task`/`artifact`/`captured_on`/`captured_by_host`/`captured_by_branch`/`clean_run_exit`/`clean_run_wall_seconds`/`clean_run_specs_passed`/`clean_run_specs_skipped`/`clean_run_specs_failed`/`clean_run_phase`/`rerun_exit`/`rerun_wall_seconds`/`rerun_specs_passed`/`rerun_specs_skipped`/`rerun_specs_failed`/`rerun_phase`/`gate_decision`/`gate_reason` fields PLUS the new T-02.2-21 mitigation fields `credproxy_pod_wait_observed_seconds`/`ac3_phase_wait_observed_seconds`; Sections 1–6 layout; per-spec outcome tables; root-cause analysis prose; threat-model-mitigations-applied list; "What This Artifact Did Not Touch" closing section; particularly note the §"Pod-State Evidence" and §"Manager Log Evidence" blocks which are LOAD-BEARING for the gate decision)
    - .planning/phases/02.2-.../02.2-07-PLAN.md Task 2 body (the structural model for THIS plan's Task 2 — runtime verification reproducer pattern with clean run + idempotent rerun + diagnostic snapshot + per-spec outcome table + gate decision; THIS plan's Task 2 substitutes the cascade-6 spec-flake verification for cascade-5's harness-bug verification)
    - .planning/phases/02.2-.../02.2-07-SUMMARY.md (Plan 02.2-07's summary — for cascade-5 closure evidence and cascade-6 surfacing analysis; particularly the manager-log evidence that cascade-5 was closed and the dual-spec-flake cascade-6 evidence)
    - test/integration/kind/credproxy_test.go (post-Task-1 state — verify the pod-wait Eventually is at `120*time.Second` before this task runs)
    - test/integration/kind/failure_test.go (post-Task-1 state — verify the AC3 beta-fail Eventually is at `240*time.Second` or `4*time.Minute` before this task runs)
    - test/integration/kind/suite_test.go (post-Task-1 state — verify the applyHierarchy helper from Plan 02.2-07 is STILL present and unmodified; `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` should return ≥ 1)
    - Makefile (verify `timeout 600s` budget from Plan 02.2-06 is still in place before this task runs — the runtime reproducer uses this budget; `grep -c 'timeout 600s' Makefile` should return ≥ 1)
    - .planning/debug/credproxy-dispatch-guard.md (historical context for the cascade-5 corrected root cause; §Section 5 of THIS plan's verification artifact must preserve the corrected framing established in `02.2-07-VERIFICATION.md` AND surface the cascade-6 root cause)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-07-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md` using the same structural template PLUS the new T-02.2-21 mitigation fields (`credproxy_pod_wait_observed_seconds`/`ac3_phase_wait_observed_seconds`).

    **Recovery-lesson PRE-STEP (codified protocol from `.continue-here.md` Plan 02.2-05 lesson, applied in 02.2-06/07):** BEFORE running the clean reproducer, author `02.2-08-VERIFICATION.md` with PRELIMINARY frontmatter — the load-bearing fields (`phase: 02.2`, `plan: 08`, `task: 2`, `artifact: verification`, `captured_on: <today>`, `captured_by_host: <host>`, `captured_by_branch: <branch>`, placeholder `gate_decision: PENDING` line, AND placeholder `credproxy_pod_wait_observed_seconds: -1` / `ac3_phase_wait_observed_seconds: -1`) — so that if mid-run executor API errors or context window pressure interrupt the long-running runtime verification, the artifact's gate field still exists with a recoverable PENDING state. Then update `gate_decision: PENDING` → APPROVED or BLOCKED and the observation fields to real values after Sub-step C completes.

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit (set -o pipefail makes the bash captured exit reflect make's real exit, not tee's):
    set -o pipefail
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-08-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state for the artifact:**
    ```bash
    # Post-clean diagnostic snapshot — needed for both APPROVED and BLOCKED gate analysis
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-08-clean-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-08-clean-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-08-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=20m 2>&1 | tee /tmp/02.2-08-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-08-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-08-clean-pvc-phase.txt || true
    # Cascade-6-specific evidence — extract observed durations from Ginkgo output for T-02.2-21 marginal-flag check
    grep -E 'credproxy-task Job|Wait for β|beta-fail.*Failed|seconds elapsed' /tmp/02.2-08-clean-run.log 2>&1 | tee /tmp/02.2-08-clean-cascade6-timings.txt || true
    # Confirm cascade-5 fix preserved: no "no project found" for credproxy-task
    grep -E 'no project found.*credproxy' /tmp/02.2-08-clean-manager-logs.txt 2>&1 | tee /tmp/02.2-08-clean-cascade5-regression-check.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block from the captured log — APPROVED case shows 7 Pass / 0 Skip / 0 Fail; BLOCKED case shows 0/7 PASS or a partial outcome). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-07-VERIFICATION.md` §"Section 1" Per-Spec Outcome Table.

    **T-02.2-21 mitigation observation fields (LOAD-BEARING):** Extract the observed time-to-Pod-creation for the credproxy HARN-03 pod-wait spec AND time-to-Failed-phase for the AC3 beta-fail spec from the Ginkgo output. The Ginkgo summary block reports each spec's elapsed time. For the credproxy pod-wait Eventually, the elapsed time is bounded above by the new 120s budget; the OBSERVED elapsed (when the Eventually returned true) is the load-bearing measurement. Similarly for AC3 beta-fail — the elapsed time when the Failed phase was observed, bounded above by 240s. Record these into the artifact's frontmatter:
    ```
    credproxy_pod_wait_observed_seconds: <N>
    ac3_phase_wait_observed_seconds: <M>
    ```
    If the values cannot be precisely extracted from Ginkgo output (e.g., Ginkgo only reports the full spec duration, not individual Eventually elapsed times), use the spec's TOTAL duration as a conservative upper bound and add a clarifying note in §Section 1 ("observed seconds = total spec duration; the pod-wait Eventually is the dominant cost within the spec").

    **T-02.2-21 marginal-flag check:** If `credproxy_pod_wait_observed_seconds >= 90` OR `ac3_phase_wait_observed_seconds >= 180`, the artifact MUST contain the literal token `dispatch latency marginal` in §Section 1 AND the closing prose. The flag does NOT block APPROVED but signals that the new budgets are being approached — a future plan should investigate the underlying ResourceVersion conflict retry overhead OR escalate to a production-code fix in the task controller (debouncing concurrent status updates, optimistic-concurrency-aware retry budgets).

    **Critically: capture the manager logs AND the credproxy-test/failure-test namespace state.** The clean-run's `tide-controller-manager` Pod state is the load-bearing evidence:
    - **APPROVED case:** Pod `READY 1/1 STATUS Running RESTARTS ≤ 1`; manager logs show "starting manager"; NO `flag provided but not defined: -<any-flag>` line (all prior cascade fixes preserved); NO `no project found` error for credproxy-task (cascade-5 fix preserved); manager logs show a `dispatch` or `creating Job` log line for credproxy-task within the new 120s budget AND a Job-create + Pod-run + exit-1 + Task-status-update cycle for beta-fail within the new 240s budget; Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed`; the credproxy-test namespace contains the full hierarchy (Project + Milestone + Phase + Plan + Task) AND a Pod for credproxy-task; the failure-test namespace contains alpha-fail (Succeeded), beta-fail (Failed), gamma-fail (never dispatched).
    - **BLOCKED case A — credproxy pod-wait still fails at 120s budget:** Pod state Running; cascade-5 fix preserved (no "no project found"); but the credproxy spec STILL times out at 120s. This indicates dispatch latency exceeds even the bumped budget — cascade-7 classified as `budget-vs-actual` (the ResourceVersion conflict retry is structurally slower than the empirical 90s observation suggested). Plan 02.2-09 would need to bump further (180s? 240s?) OR escalate to a production-code fix.
    - **BLOCKED case B — AC3 beta-fail still fails at 240s budget:** Pod state Running; cascade-5 fix preserved; credproxy specs PASS; but the AC3 spec STILL times out at 240s. Same diagnosis as case A but for failure_test.go — cascade-7 classified as `budget-vs-actual` for the failure dispatch path. Plan 02.2-09 bumps further OR escalates.
    - **BLOCKED case C — different spec fails (wave/caps/output):** the previously-blocked specs (wave, caps, output — none have run end-to-end since Phase 02.1) surface a new failure. Cascade-7 classified per T-02.2-23 (one of `spec-flake | harness-bug | kind-infra | other`). The artifact's §Section 5 names the specific spec AND proposes the Plan 02.2-09 fix shape.
    - **BLOCKED case D — wall-time budget regression:** even with adequate per-spec budgets, the total wall exceeds 600s (the Makefile budget from Plan 02.2-06). Cascade-7 classified as `budget-vs-actual`. The fix shape is the deferred Option B from Plans 02.2-05/06: split Layer A + Layer B into separately-timed Makefile targets.
    - **BLOCKED case E — flag-mismatch or 'no project found' regression:** if EITHER `flag provided but not defined` OR `no project found` for credproxy-task appears in manager logs, a PRIOR cascade has regressed. This would be cascade-7 classified as `flag-mismatch` (regression of Plan 02.2-04/05) or `harness-bug` (regression of Plan 02.2-07). Either is a SHOWSTOPPER — surface in §Section 5 with full diagnostic citing which Plan's fix regressed.

    **Sub-step B — Idempotent Re-run:**
    ```bash
    # Pre-state: tide-test cluster + cert-manager + tide release REVISION 1 persisted from clean run.
    # (KEEP_KIND_CLUSTER=true preserves the AfterSuite skip from Phase 02.1 D-03)
    set -o pipefail
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-08-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    **After rerun:** capture same diagnostic snapshot (`/tmp/02.2-08-rerun-*.txt`) for the rerun, mirroring Sub-step A's snapshot list with `-rerun-` substituted for `-clean-`. Also re-extract `credproxy_pod_wait_observed_seconds` and `ac3_phase_wait_observed_seconds` from the rerun Ginkgo output — if either value differs materially between clean and rerun, note the variance in §Section 2 (Idempotent Re-run) commentary.

    **Sub-step C — Author the artifact:**
    Write `02.2-08-VERIFICATION.md` mirroring `02.2-07-VERIFICATION.md`'s structural template. Required frontmatter (note `plan: 08`, `task: 2`; once Sub-step C completes, update the PRELIMINARY `gate_decision: PENDING` line to APPROVED or BLOCKED AND populate the two new T-02.2-21 observation fields):
    ```
    ---
    phase: 02.2
    plan: 08
    task: 2
    artifact: verification
    captured_on: <YYYY-MM-DD>
    captured_by_host: <host>
    captured_by_branch: <git branch / worktree name>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    credproxy_pod_wait_observed_seconds: <int>
    ac3_phase_wait_observed_seconds: <int>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary including the cascade-7 type if BLOCKED>
    ---
    ```

    Required sections (mirror `02.2-07-VERIFICATION.md` headers verbatim):
    - **Section 1 — Clean Start Run** (reproducer + per-spec outcome table + wall-time + exit + Pod-state evidence + manager-log evidence as the load-bearing diagnostic + credproxy-test + failure-test namespace CRD/Pod listings + T-02.2-21 observation fields with marginal-flag check: if `credproxy_pod_wait_observed_seconds >= 90` OR `ac3_phase_wait_observed_seconds >= 180`, include the literal token `dispatch latency marginal` in §Section 1 prose AND a numeric breakdown showing observed vs new budget vs old budget)
    - **Section 2 — Idempotent Re-run** (reproducer + per-spec outcome table + wall-time + exit + rerun-specific evidence — both runs are required for an APPROVED gate; AND re-extract `credproxy_pod_wait_observed_seconds`/`ac3_phase_wait_observed_seconds` from rerun for variance commentary)
    - **Section 3 — Before/After Comparison** (table comparing Plan 02.2-07 BLOCKED outcome (cascade-6 spec-flake — 2 specs failed at old 60s + 120s budgets) vs Plan 02.2-08 outcomes on clean + rerun; particularly: did the timeout bump close the cascade-6 BLOCKED gate, what is the new dispatch-latency observation, and what is the NEW failure mode if BLOCKED again — what cascade-7 type per T-02.2-23 enum)
    - **Section 4 — Wall-time Budget Check** (same table shape as `02.2-07-VERIFICATION.md` §4; both runs must show `go test ≤ 600s` for APPROVED; ADDITIONAL row required per T-02.2-14 mitigation carry-forward: flag `budget headroom marginal` if observed wall ≥ 500s even on APPROVED so a future plan can preemptively escalate to Layer A / Layer B split targets; SEPARATE row required per T-02.2-21: per-spec dispatch-latency observation against the new 120s/240s budgets — if observed ≥ 90s/180s, flag `dispatch latency marginal`)
    - **Section 5 — Gate Decision** (the APPROVED-requires-ALL table with 5 conditions on BOTH runs + Required Next Step + Root-Cause Summary if BLOCKED + Fix landscape if BLOCKED. **CRITICAL FRAMING PRESERVATION**: §Section 5 MUST re-state the corrected root cause from `02.2-07-VERIFICATION.md` §Section 5 — the dispatch gate is `resolveProject` at `task_controller.go:217`, NOT a "parent Wave Ready check"; AND MUST surface the cascade-6 root cause analysis: ResourceVersion conflict retry for credproxy's pod-wait; first-end-to-end-run-since-Phase-02.1 for AC3 beta-fail. DO NOT inherit `02.2-06-VERIFICATION.md`'s discredited "Wave Ready" framing.)
    - **Section 6 — Threat-Model Mitigations Applied** (T-02.2-21 / T-02.2-22 / T-02.2-23 each with evidence + carry-forward T-02.2-01 through T-02.2-20 noted as preserved)
    - **"What This Artifact Did Not Touch"** closing section
    - **"Closing Prose"** with one-paragraph summary including the corrected root cause framing AND (if APPROVED) the chain-terminator note OR (if BLOCKED) the cascade-7 classification + Plan 02.2-09 fix shape

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/` (Task 1's test-file edits are already committed before this task runs; the runtime reproducer only READS from the codebase, doesn't WRITE). Task 3 edits Plan 02.2-02 (a planning artifact, not source) — and only fires if `gate_decision: APPROVED`. Document this explicitly in the artifact's "What This Artifact Did Not Touch" closing section.

    **APPROVED gate (5-of-5 must hold on BOTH clean run AND idempotent rerun):**
    1. **Wall-time ≤ 600s:** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` (the Plan 02.2-06 budget preserved) AND no `timeout 600s` exit-124 line appears in either captured log on the go test step
    2. **Manager Pod state:** `tide-controller-manager` Pod `STATUS=Running` with `RESTARTS ≤ 1` (Plan 02.2-05's cascade-3 fix preserved — no CrashLoopBackOff)
    3. **Manager log clean (TWO sub-conditions):** (a) zero `flag provided but not defined` errors of any kind in the captured manager-log excerpts (Plans 02.2-04/05's cascade-2/3 fixes preserved); (b) zero `no project found` errors for credproxy-task (Plan 02.2-07's cascade-5 fix preserved)
    4. **PVC preserved:** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO` (Plan 02.2-03's cascade-1 fix preserved)
    5. **7/7 Layer B Ginkgo specs PASS:** Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed` AND the per-spec outcome table records each of the 7 specs as PASS on both clean run AND rerun — **this is achievable for the first time in the cascade chain because all 6 prior cascades are now closed (PVC RWX→RWO; metrics-bind-address flag; webhook-cert-path flag; test-budget; fixture-incomplete; Eventually timeouts too tight)**

    **BLOCKED gate fires if ANY of the 5 conditions fail on either run.** The artifact's §"Section 5 Root-Cause Summary" documents WHICH gate failed, with raw log evidence, and §"Fix landscape" enumerates the next-iteration options based on cascade-7 classification:
    - **Option A** (further timeout bump): if cascade-7 is `budget-vs-actual` AND one of the bumped Eventually call sites STILL times out, route to a further bump in Plan 02.2-09 (T-02.2-21 marginal-flag would have already fired at 90s/180s — making this case predictable; the underlying ResourceVersion conflict retry overhead may need a production-code fix in Plan 02.2-09 or a future phase)
    - **Option B** (different-spec fix): if cascade-7 is `spec-flake` in a non-credproxy non-AC3 spec (e.g., wave_test.go, caps_test.go, output_test.go — none have run end-to-end since Phase 02.1), identify the specific spec from the Ginkgo summary's `Failed` list and route to a targeted fix plan
    - **Option C** (production-code fix for ResourceVersion conflict): if cascade-7 is `harness-bug` AND the conflict retry is happening MANY times per dispatch (not just the one cycle observed in cascade-6), route to a production-code fix in `internal/controller/task_controller.go` and/or `internal/controller/plan_controller.go` — debounce concurrent status updates, optimistic-concurrency-aware retry budgets
    - **Option D** (Layer A / Layer B split — deferred from Plan 02.2-05/06): if cascade-7 is `budget-vs-actual` on the outer 600s budget (not on a per-spec Eventually), escalate to splitting Layer A (envtest, fast) and Layer B (kind, slow) into separately-timed Makefile targets
    - **Option E** (kind-infra fix): if cascade-7 is `kind-infra` (image-pull regression, etcd race, networking quirk), route to `test/integration/kind/suite_test.go`'s BeforeSuite/AfterSuite OR to kind configuration

    Each option in the BLOCKED §"Fix landscape" MUST include the cascade-7 classification (one of `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) per T-02.2-23 acceptance.

    Task 3 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring — Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3 also did NOT fire).

    **If gate_decision = APPROVED:** This is the CHAIN TERMINATOR — Plan 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave. The closing prose should:
    - Note the cascade chain has terminated successfully after 6 tactical iterations (02.2-01 source-shape + 02.2-03 chart PVC + 02.2-04 metrics flag + 02.2-05 webhook-cert-path flag + 02.2-06 Makefile timeout + 02.2-07 credproxy fixture + 02.2-08 Eventually timeout bumps)
    - Note T-02.2-14 marginal-headroom check (clean/rerun wall vs 500s) AND T-02.2-21 marginal-dispatch-latency check (observed pod-wait/phase-wait vs 90s/180s) — if either marginal flag fired, the artifact §Section 4 contains the literal token AND the closing prose flags preemptive escalation as prudent
    - State that Plan 02.2-02 (closeout) is unblocked once Task 3 wires its checkpoint to read this artifact, and that Phase 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave

    **Section 5 framing — load-bearing corrected root cause + cascade-6 root cause:** The artifact's §Section 5 prose MUST:
    1. Preserve `02.2-07-VERIFICATION.md` §Section 5's corrected framing: the dispatch gate is `resolveProject` at `task_controller.go:217`, NOT a "parent Wave Ready check". Do NOT re-inherit the wrong framing from `02.2-06-VERIFICATION.md`.
    2. Surface the cascade-6 root cause analysis: ResourceVersion conflict at first reconcile (PlanReconciler's `stampTaskLabels` racing TaskReconciler's status update) → retry cycle adds ~10–30s to dispatch latency → 60s pod-wait budget insufficient; AC3 spec's 120s budget calibrated against pre-hierarchy dispatch path that no longer reflects the now-active full-hierarchy reconcile overhead from Plan 02.2-07's applyHierarchy.
    3. State explicitly: the cascade-6 fix (timeout bumps) is a TACTICAL ACCOMMODATION, not a fix for the underlying ResourceVersion conflict race; if T-02.2-21 marginal-flag fires consistently across future runs, a production-code fix (debounce concurrent status updates, optimistic-concurrency-aware retry budgets) is the correct durable fix and should be a future-phase target.

    Commit with one of:
    ```
    docs(02.2-08): runtime verification — APPROVED gate (cascade-6 spec-flake closed; 7/7 Layer B specs PASS clean + rerun within 600s; CHAIN TERMINATOR)
    ```
    OR
    ```
    docs(02.2-08): runtime verification — BLOCKED gate (Eventually timeouts bumped; cascade-7 surfaced: <cascade-7-type-and-summary>)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists at the correct path
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md ] && \
      # Frontmatter required fields (mirroring 02.2-07's shape + new T-02.2-21 observation fields)
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 08' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^task: 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^artifact: verification' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -eq 1 ] && \
      # Gate decision is one of APPROVED or BLOCKED (PENDING is not acceptable as a final state)
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # T-02.2-21 mitigation observation fields populated
      grep -E '^credproxy_pod_wait_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      grep -E '^ac3_phase_wait_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # Required structural sections exist (six numbered sections)
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 4' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times (both runs)
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      grep -E '^rerun_exit: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # Threat-model mitigations applied section exists
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-21/22/23 all referenced
      [ "$(grep -c 'T-02.2-21' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-22' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-23' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      # Cascade classification present (per T-02.2-23 acceptance) — one of the six classes must appear
      grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # Section 5 corrected-root-cause framing preserved — MUST cite resolveProject AND must NOT inherit Wave Ready framing
      grep -F 'resolveProject' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # Cascade-6 root cause surfaced — MUST mention ResourceVersion conflict OR first-end-to-end-run rationale
      grep -E 'ResourceVersion|first.*end.to.end|cascade-6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # Manager-log evidence captured (the load-bearing diagnostic for APPROVED vs BLOCKED)
      [ "$(grep -c 'starting manager\|flag provided but not defined\|tide-controller-manager\|task cleanup\|dispatch\|no project found' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      # Pod-state evidence captured (load-bearing)
      [ "$(grep -c 'CrashLoopBackOff\|Running\|RESTARTS\|kubectl.*get pod' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      # 600s budget referenced (carry-forward from Plan 02.2-06)
      [ "$(grep -c '600s\|≤ 600\|<= 600' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-21 marginal-flag check rendered — either the literal token OR an explicit "not marginal" note
      grep -E 'dispatch latency marginal|dispatch latency.*not marginal|observed.*<.*90|observed.*<.*180' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # Carry-forward threat register acknowledgement: T-02.2-01 through T-02.2-20 noted as preserved
      grep -E 'T-02.2-(0[1-9]|1[0-9]|20)' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md && \
      # No production source modified by this task (Task 1's test edits are pre-committed; Task 3 has not fired)
      ! (git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-08-VERIFICATION.md' | grep -v '^$' >/dev/null) && \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-08-VERIFICATION.md ]` returns true
    - **Frontmatter has required fields (mirroring 02.2-07's shape):** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 08' <artifact>` returns 1 AND `grep -c '^task: 2' <artifact>` returns 1 AND `grep -c '^artifact: verification' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches (PENDING is not a final state)
    - **T-02.2-21 mitigation observation fields populated (NEW for THIS plan):** `grep -E '^credproxy_pod_wait_observed_seconds: -?[0-9]+$' <artifact>` matches AND `grep -E '^ac3_phase_wait_observed_seconds: -?[0-9]+$' <artifact>` matches
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: -?[0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: -?[0-9]+$' <artifact>` matches (rerun_exit allows -1 to denote skipped, but APPROVED case requires both = 0)
    - **All six structural sections present:** each `grep -c '^## Section [1-6]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all three new threats referenced:** `grep -c 'Threat-Model Mitigations Applied' <artifact>` ≥ 1 AND `grep -c 'T-02.2-21' <artifact>` ≥ 1 AND `grep -c 'T-02.2-22' <artifact>` ≥ 1 AND `grep -c 'T-02.2-23' <artifact>` ≥ 1
    - **Cascade classification present (T-02.2-23 mitigation):** `grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other' <artifact>` matches at least once — the artifact classifies the cascade type observed (or notes "cascade chain terminated" with no cascade-7 surfaced if APPROVED)
    - **Section 5 corrected-root-cause framing preserved (load-bearing — DO NOT inherit the wrong "Wave Ready check" framing from `02.2-06-VERIFICATION.md`):** `grep -F 'resolveProject' <artifact>` matches (the artifact's §Section 5 explicitly names the actual gate function from 02.2-07's corrected analysis)
    - **Cascade-6 root cause surfaced:** `grep -E 'ResourceVersion|first.*end.to.end|cascade-6' <artifact>` matches at least once (the artifact's §Section 5 explains WHY the budgets were too tight — ResourceVersion conflict retry for credproxy; first-end-to-end-run-since-Phase-02.1 for AC3)
    - **Manager-log evidence captured (load-bearing for gate decision):** the artifact includes a verbatim excerpt of `kubectl logs deploy/tide-controller-manager` from the clean run — APPROVED case shows "starting manager" + dispatch log line for credproxy-task + Job-Failed line for beta-fail + 7-spec Ginkgo summary `0 Failed`; BLOCKED case shows evidence of the new failure shape with cascade-7 classification
    - **Pod-state evidence captured (load-bearing per plan-checker tightening):** the artifact includes verbatim output of `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` showing either `STATUS=Running RESTARTS ≤ 1` (APPROVED) or evidence of a different failure (BLOCKED)
    - **600s budget referenced (carry-forward from Plan 02.2-06):** `grep -c '600s\|≤ 600\|<= 600' <artifact>` ≥ 1 (the artifact references the preserved 600s budget)
    - **T-02.2-21 marginal-flag check rendered:** the artifact either contains the literal `dispatch latency marginal` token (if observed pod-wait ≥ 90s OR observed phase-wait ≥ 180s) OR an explicit "not marginal" note (if both observed values are below thresholds). Acceptance regex: `grep -E 'dispatch latency marginal|dispatch latency.*not marginal|observed.*<.*90|observed.*<.*180' <artifact>` matches
    - **Carry-forward threat register acknowledged:** the artifact's §Section 6 (or §"Carry-Forward Threat Register") notes that T-02.2-01 through T-02.2-20 are preserved (acceptance regex: `grep -E 'T-02.2-(0[1-9]|1[0-9]|20)' <artifact>` matches at least once — the artifact cites at least one carry-forward threat ID)
    - **Behavior gate (APPROVED case — ALL 5 conditions on BOTH runs must hold):**
      - (a) Clean run captured `EXIT=0` AND 7 Layer B specs marked PASS in the per-spec table
      - (b) Rerun captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (c) **Wall-time budget (LOAD-BEARING):** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` AND no `timeout 600s` exit-124 line appears in either captured log
      - (d) Manager logs show "starting manager" AND zero `flag provided but not defined` errors AND zero `no project found` errors for credproxy-task AND a `dispatch` or `creating Job` line for credproxy-task within the new 120s budget AND a Job-Failed cycle for beta-fail within the new 240s budget
      - (e) **Pod-state (LOAD-BEARING):** `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` shows `STATUS=Running` AND restart count ≤ 1
      - (f) **PVC preservation (Plan 02.2-03 fix not regressed):** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO`
      - (g) **T-02.2-14 marginal-headroom check (carry-forward):** if `clean_run_wall_seconds ≥ 500` OR `rerun_wall_seconds ≥ 500`, the artifact's §Section 4 table contains the literal token `budget headroom marginal` and the closing prose flags preemptive escalation as prudent (note: marginal-headroom does NOT block APPROVED, but must be surfaced)
      - (h) **T-02.2-21 marginal-dispatch-latency check (NEW):** if `credproxy_pod_wait_observed_seconds ≥ 90` OR `ac3_phase_wait_observed_seconds ≥ 180`, the artifact's §Section 4 table contains the literal token `dispatch latency marginal` and the closing prose flags the trigger for a future production-code fix of the ResourceVersion conflict race (note: marginal-dispatch-latency does NOT block APPROVED, but must be surfaced)
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, a `Root-Cause Summary` section MUST exist with numbered analysis points mirroring `02.2-07-VERIFICATION.md` §"Root-Cause Summary" — particularly: (a) confirmation that Plan 02.2-08 Task 1's timeout bumps ARE in effect (`grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go` ≥ 1 AND `grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go` ≥ 1 evidence cited); (b) confirmation that prior cascade fixes are preserved (`grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` ≥ 1; manager logs show no `no project found` for credproxy-task; PVC Bound RWO; no flag-not-defined errors); (c) which cascade-7 surfaced (one of the T-02.2-23 enum); (d) why this was or was NOT predictable; (e) fix-landscape options for Plan 02.2-09 (Options A–E enumerated per the action body)
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows only the new `02.2-08-VERIFICATION.md` artifact (Task 1's test-file edits must be committed BEFORE this task runs; Task 3 has not fired yet)
  </acceptance_criteria>

  <done>
    `02.2-08-VERIFICATION.md` exists at the correct path with the structural template mirroring `02.2-07-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, AND the new T-02.2-21 mitigation observation fields (`credproxy_pod_wait_observed_seconds` and `ac3_phase_wait_observed_seconds`), and `gate_decision: APPROVED` or `gate_decision: BLOCKED` (PENDING is not a final state). Sections 1–6 captured with per-spec outcome tables, Pod-state evidence, manager-log evidence (load-bearing for APPROVED vs BLOCKED — particularly the absence of `no project found` for credproxy-task per Plan 02.2-07's cascade-5 fix preservation), credproxy-test + failure-test namespace state, before/after comparison vs `02.2-07-VERIFICATION.md`, wall-time budget check (with T-02.2-14 marginal-headroom carry-forward flag if observed wall ≥ 500s AND new T-02.2-21 marginal-dispatch-latency flag if observed pod-wait ≥ 90s OR observed phase-wait ≥ 180s), and threat-model mitigations (T-02.2-21/22/23 + carry-forward T-02.2-01 through T-02.2-20). §Section 5 preserves the corrected root cause framing from `02.2-07-VERIFICATION.md` (`resolveProject` is the gate, NOT a Wave Ready check) AND surfaces the cascade-6 root cause (ResourceVersion conflict retry overhead for credproxy; first-end-to-end-run-since-Phase-02.1 budget calibration for AC3). If APPROVED, all 5 APPROVED-gate conditions hold on BOTH clean run AND rerun: both runs exit 0 with 7/7 specs PASS, both ≤ 600s, no `flag provided but not defined` AND no `no project found` errors, Pod STATUS=Running with restart ≤ 1, PVC Bound RWO, manager log shows dispatch for credproxy-task within 120s AND Job-Failed for beta-fail within 240s. Task 3 fires to wire Plan 02.2-02's checkpoint — this is the CHAIN TERMINATOR. If BLOCKED, Task 3 does NOT fire and the failure routes to a planner re-evaluation per the wave-failure-semantics contract — with §Section 5 Root-Cause Summary classifying cascade-7 per T-02.2-23's enum AND naming the specific spec(s) that failed AND proposing Plan 02.2-09's fix shape from Options A–E. Committed with a `docs(02.2-08): runtime verification — APPROVED gate (cascade-6 spec-flake closed; 7/7 Layer B specs PASS clean + rerun within 600s; CHAIN TERMINATOR)` or `docs(02.2-08): runtime verification — BLOCKED gate (Eventually timeouts bumped; cascade-7 surfaced: <type>)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-08-VERIFICATION.md + bump depends_on (HARD-GATED on Task 2 APPROVED)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file — 382 lines; frontmatter at lines 1–40 — particularly line 6 `depends_on: ["02.2-01"]`, lines 31–39 `key_links` block, lines 22–26 `must_haves.truths` block; `<context>` block at lines 63–68; Task 1 body at lines 72–136 — `<read_first>` at lines 77–79, `<what-built>` at lines 81–83, `<how-to-verify>` at lines 85–101, `<verify>` automated block at lines 117–123, `<acceptance_criteria>` at lines 125–129, `<done>` at lines 131–135 — all of which currently reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, AND 02.2-07 Task 3 did NOT fire)
    - .planning/phases/02.2-.../02.2-08-VERIFICATION.md (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the HARD precondition for this Task 3 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - .planning/phases/02.2-.../02.2-07-PLAN.md Task 3 body (the structural model for this task's edits; mirror its surgical-edit pattern with `02.2-07` substituted for `02.2-08` everywhere)
    - CLAUDE.md "Failure semantics at wave boundaries" contract — the load-bearing logic that this task fires ONLY on APPROVED; on BLOCKED, this task halts and Plan 02.2-02 stays gated on its current `02.2-01-VERIFICATION.md` wiring
  </read_first>

  <action>
    **Gate precondition (HARD GATE):** Task 3 fires ONLY IF Task 2's `02.2-08-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED` (or PENDING, which is not a final state), HALT Task 3 — Plan 02.2-02 stays unmodified (it will continue to reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, AND 02.2-07 Task 3 also did NOT fire); the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md (Plan 02.2-09 takes the next iteration).

    Verify the precondition by running:
    ```bash
    grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md
    ```
    If this returns 0 (no APPROVED match), HALT Task 3 and surface the BLOCKED gate. Exit with a clear message: "Task 3 halted: 02.2-08-VERIFICATION.md gate_decision is not APPROVED — routing back to planner for next iteration (cascade-7 or whatever surfaced)."

    If APPROVED (returns exactly 1), make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-08-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering. There are 31 references to `02.2-01-VERIFICATION.md` in `02.2-02-PLAN.md` (verified at plan-time by `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returning exactly 31); all 31 are redirected to `02.2-08-VERIFICATION.md` in one pass.

    **NOTE on recovery state:** Plan 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, AND 02.2-07 Task 3 each tried to do the same surgical edit (each pointing 02.2-02-PLAN.md at its own VERIFICATION.md), but none fired because each Task 2 gate was BLOCKED on the cascade-of-the-day. As a consequence, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (verified during plan authoring of THIS plan). This task's edits land the long-pending rewire, redirecting to `02.2-08-VERIFICATION.md`. If this is the chain terminator (APPROVED gate), this is also the FIRST time the rewire actually lands in Phase 02.2's entire history.

    **Pre-edit count assertion (BEFORE any edits):** Run `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md` and confirm the result is exactly `31`. If it is NOT 31 (e.g., some prior task fired partially OR a checker rewrote the file OR a developer manually edited it), HALT — the rewire scope assumed at plan-time has drifted and the task needs planner re-evaluation. If exactly 31, proceed with edits.

    **Edit A — frontmatter `depends_on` (line 6 of 02.2-02-PLAN.md):** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-08"]`. This is a single one-line change. The new dependency captures the gate-artifact dependency directly (Plan 02.2-08 produces the gate; Plan 02.2-02 reads it). The transitive dependency on Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07 (which Plan 02.2-08 depends on transitively via Plan 02.2-07) is implicit in execute-phase's wave-ordering logic.

    **Edit B — frontmatter `must_haves.truths` block (lines 22–26 of 02.2-02-PLAN.md):** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-08-VERIFICATION.md`. Specifically the bullet currently reads "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → change to "Closeout fires ONLY when 02.2-08-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit C — frontmatter `key_links` block (lines 31–39 of 02.2-02-PLAN.md):** Update the entry that references `02.2-01-VERIFICATION.md` to reference `02.2-08-VERIFICATION.md`. Concretely: change the `from:` line's path from `02.2-01-VERIFICATION.md` to `02.2-08-VERIFICATION.md` and keep the rest of the entry unchanged.

    **Edit D — `<context>` block (line 66 of 02.2-02-PLAN.md):** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-08-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>` (lines 77–79 of 02.2-02-PLAN.md):** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-08-VERIFICATION.md (the gate artifact produced by Plan 08 Task 2 — Phase 02.2-08 closed the BLOCKED runtime gate from 02.2-07-VERIFICATION.md by bumping two Eventually timeouts in test-only files: credproxy_test.go pod-wait 60s→120s (covers ResourceVersion conflict retry overhead) and failure_test.go AC3 beta-fail-Failed-phase 2*time.Minute→240*time.Second (covers full-hierarchy dispatch cycle for testMode=fail-exit-1 Task — first end-to-end run since Phase 02.1). All 6 prior cascades closed: cascade-1 PVC RWX→RWO; cascade-2 metrics-bind-address; cascade-3 webhook-cert-path; cascade-4 test-budget 300s→600s; cascade-5 credproxy-fixture-incomplete; cascade-6 Eventually-timeout-too-tight. With cascade-6 closed, all 7 Layer B Ginkgo specs PASS on clean + idempotent rerun within the 600s outer budget — Phase 02.2 cascade chain terminated)`. Update the analog Phase 02.1 reference if present.

    **Edit F — Task 1 `<what-built>` (lines 81–83 of 02.2-02-PLAN.md):** Update the prose to reference Plan 02.2-08's deliverable (two Eventually timeout bumps + runtime re-verification + chain termination) in addition to Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07's deliverables. Specifically append: "Plan 02.2-08 then bumped two `Eventually` timeouts in test-only files to close cascade-6 (spec-flake): `test/integration/kind/credproxy_test.go` pod-wait `Eventually` budget 60s → 120s (covers ResourceVersion conflict retry overhead identified in 02.2-07-VERIFICATION.md §Section 5 — PlanReconciler's `stampTaskLabels` races TaskReconciler's status update on the credproxy-task first reconcile, adding ~10–30s of retry cycle to the dispatch latency); and `test/integration/kind/failure_test.go` AC3 beta-fail Failed-phase `Eventually` budget 2*time.Minute → 240*time.Second (covers the full Project+Milestone+Phase+Plan+Task hierarchy dispatch cycle for a testMode=fail-exit-1 Task — first end-to-end run since Phase 02.1, when the budget was calibrated against a faster pre-hierarchy dispatch path). Zero production code changes (Option B contract preserved); zero `suite_test.go` changes (applyHierarchy helper from Plan 02.2-07 intact); the 600s outer Makefile budget from Plan 02.2-06 preserved. Cascade-6 is the FIRST FITNESS issue in the chain — all 5 prior cascades were structural/correctness. With cascade-6 closed, all 7 Layer B Ginkgo specs PASS on clean + idempotent rerun within the 600s outer budget, and Phase 02.2's cascade chain terminates."

    **Edit G — Task 1 `<how-to-verify>` (lines 85–101 of 02.2-02-PLAN.md):** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-08-VERIFICATION.md`. There are multiple occurrences in the numbered steps; update each.

    **Edit H — Task 1 `<verify>` automated block (lines 117–123 of 02.2-02-PLAN.md):** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-08-VERIFICATION.md`. Two grep paths need bumping.

    **Edit I — Task 1 `<acceptance_criteria>` (lines 125–129 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-08-VERIFICATION.md`.

    **Edit J — Task 1 `<done>` (lines 131–135 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-08-VERIFICATION.md` if any are present.

    **Post-edit count assertion (AFTER all edits):** Run `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `0`. Run `grep -c '02.2-08-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `31`. If either count is not as expected, the redirection drifted — surface the diff and HALT before commit.

    **(Edits to ROADMAP/STATE content NOT performed by this task — Plan 02.2-02 reads live ROADMAP/STATE at execution time via its own `<read_first>` and `<action>` blocks and will derive the current 8-plan / 7-done structure. Prescriptive content edits from THIS plan would (a) write a snapshot that is stale by the time Plan 02.2-02 runs, and (b) leak Plan 02.2-08's responsibilities into Plan 02.2-02's body. Plan 02.2-02 is responsible for its own ROADMAP/STATE shape decisions at execution time.)**

    **Constraints:**
    - Do NOT edit Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, or 02.2-08 (this plan) — only `02.2-02-PLAN.md` is touched.
    - Do NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `Makefile`, `.github/`, `test/`. This task is documentation-only.
    - Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — those are owned by Plan 02.2-02's tasks and by the orchestrator.
    - The diff against `02.2-02-PLAN.md` must be minimal — only the 31 references to `02.2-01-VERIFICATION.md` are changed to `02.2-08-VERIFICATION.md`, plus the one `depends_on` value change. No new tasks added, no existing tasks removed, no structural reorganization, no plan-list/decisions edits.

    Commit with message:
    ```
    docs(02.2-08): wire Plan 02.2-02 checkpoint to 02.2-08-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-08; 31 references redirected; CHAIN TERMINATOR — first APPROVED rewire in Phase 02.2's history)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Gate precondition: 02.2-08-VERIFICATION.md gate_decision is APPROVED (else this task should have been halted)
      [ "$(grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md)" -eq 1 ] && \
      # Edit A: depends_on bumped to 02.2-08
      [ "$(grep -cE 'depends_on: \[.{0,5}02.2-08.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 1 ] && \
      # Old depends_on removed (must not contain 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, or 02.2-07 as the active depends_on value)
      [ "$(grep -cE '^depends_on: \[.{0,5}02.2-0[134567].{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-08-VERIFICATION.md is now the gate reference — count should be exactly 31 (one-for-one rewire)
      [ "$(grep -c '02.2-08-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 31 ] && \
      # 02.2-01-VERIFICATION.md references purged from 02.2-02-PLAN.md (count must be exactly 0)
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Prior VERIFICATION refs (03/04/05/06/07) purged — those Task 3 didn't fire, so they were never there; assert anyway
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-04-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-05-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-06-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-07-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Task 1 checkpoint grep gate uses the new path (the embedded regex inside the plan's verify block)
      grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-08-VERIFICATION.md" .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Confirm no other plans were modified by this task (regression guard)
      ! (git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-02-PLAN.md' | grep -v '^$' >/dev/null) && \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Gate precondition met (HARD GATE):** `grep -cE '^gate_decision: APPROVED$' 02.2-08-VERIFICATION.md` returns exactly 1 (Task 3 ran ONLY because Task 2's gate was APPROVED; otherwise Task 3 was halted before edits landed)
    - **depends_on bumped (Edit A):** `grep -cE 'depends_on: \[.{0,5}02.2-08.{0,5}\]' 02.2-02-PLAN.md` returns exactly 1 AND `grep -cE '^depends_on: \[.{0,5}02.2-0[134567].{0,5}\]' 02.2-02-PLAN.md` returns 0 (old values 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07 all purged from the depends_on field)
    - **New gate-artifact reference count (Edits B–J):** `grep -c '02.2-08-VERIFICATION.md' 02.2-02-PLAN.md` returns exactly 31 (one-for-one redirection from the 31 plan-time `02.2-01-VERIFICATION.md` references)
    - **Old gate-artifact references purged:** `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-03-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-04-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-05-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-06-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-07-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 (no stale references to any prior gate artifact, including Plan 02.2-07's BLOCKED gate)
    - **Task 1 `<verify>` automated grep targets the new path:** the regex `grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-08-VERIFICATION.md"` matches inside `02.2-02-PLAN.md`
    - **No other plans or source modified (regression guard):** `git status --porcelain` shows ONLY `02.2-02-PLAN.md` modified in this commit; no edits to `02.2-01-PLAN.md`, `02.2-03-PLAN.md`, `02.2-04-PLAN.md`, `02.2-05-PLAN.md`, `02.2-06-PLAN.md`, `02.2-07-PLAN.md`, `02.2-08-PLAN.md` (this plan), or any production source
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has been surgically edited to redirect its checkpoint from `02.2-01-VERIFICATION.md` to `02.2-08-VERIFICATION.md`, with `depends_on` bumped from `["02.2-01"]` to `["02.2-08"]`. All 31 prior `02.2-01-VERIFICATION.md` references are now `02.2-08-VERIFICATION.md` (one-for-one, count exactly 31). No stale `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, `02.2-05-VERIFICATION.md`, `02.2-06-VERIFICATION.md`, OR `02.2-07-VERIFICATION.md` references exist. No other plans or production source are modified. **This is the CHAIN TERMINATOR rewire — the FIRST APPROVED rewire in Phase 02.2's entire history (Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3 all halted on BLOCKED gates; this Task 3 is the first to fire).** Committed with the `docs(02.2-08): wire Plan 02.2-02 checkpoint to 02.2-08-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-08; 31 references redirected; CHAIN TERMINATOR — first APPROVED rewire in Phase 02.2's history)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/integration/kind/credproxy_test.go` (pod-wait Eventually) → kind cluster's TaskReconciler reconcile path | The Eventually budget is the boundary between "the spec runs to its assertion" and "the spec times out before the production dispatch path completes." The bump from 60s → 120s expands the budget to accommodate the ResourceVersion conflict retry cycle (~10–30s extra) on top of the base dispatch latency. |
| `test/integration/kind/failure_test.go` (AC3 beta-fail Eventually) → kind cluster's TaskReconciler reconcile path (testMode=fail-exit-1 branch) | The Eventually budget is the boundary between "the spec observes the Failed phase" and "the spec times out before the testMode=fail-exit-1 Task completes its dispatch cycle (Job create → Pod schedule → exit-1 → Task status update)." The bump from 2*time.Minute → 240*time.Second expands the budget to accommodate the full-hierarchy dispatch overhead (Project+Milestone+Phase+Plan+Task) that Plan 02.2-07 added. |
| Task 1 (timeout bumps) → Task 2 (runtime verification) | Task 2 is run after Task 1's commits land; the runtime reproducer exercises the bumped Eventually budgets against the cascade-1-through-5-fixed binary + chart + helper. |
| Task 2 (gate artifact + T-02.2-21 observation fields) → Task 3 (Plan 02.2-02 wiring) | Task 3 reads Task 2's `gate_decision:` frontmatter. APPROVED → Task 3 fires; BLOCKED → Task 3 halts and the BLOCKED failure routes to a planner re-evaluation. |
| Plan 02.2-08 Task 3 edits → Plan 02.2-02 checkpoint target | Surgical edit only — `depends_on` and 31 verification-artifact references in `02.2-02-PLAN.md` are bumped; no other planning artifacts touched. |
| `02.2-08-VERIFICATION.md` T-02.2-21 observation fields → future-phase production-code fix decision | If observed pod-wait ≥ 90s OR observed phase-wait ≥ 180s, the `dispatch latency marginal` flag surfaces — signaling that a future phase should investigate the underlying ResourceVersion conflict race (debounce concurrent status updates) rather than continuing to bump test timeouts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-21 | Information Disclosure (regression masking) | Bumping `Eventually` timeouts could mask a real performance regression in Task dispatch: if `resolveProject` or `stampTaskLabels` grows slow over time (e.g., a future change adds an O(N²) ProjectList scan, or the ResourceVersion conflict retry becomes more frequent), the larger 120s/240s timeouts would hide the regression rather than surface it. The test would still pass, but the production system would have degraded latency that goes undetected. | mitigate | `02.2-08-VERIFICATION.md` §Section 1 MUST record the ACTUAL observed time-to-Pod-creation for the credproxy HARN-03 pod-wait spec AND the ACTUAL observed time-to-Failed-phase for the AC3 beta-fail spec, persisted in the frontmatter fields `credproxy_pod_wait_observed_seconds: <N>` and `ac3_phase_wait_observed_seconds: <M>`. If the observed time exceeds 90s for credproxy pod-wait OR 180s for AC3 phase-wait (75% of the new budgets), the artifact MUST contain the literal token `dispatch latency marginal` in §Section 1 AND the closing prose. **Acceptance gate:** VERIFICATION frontmatter contains both observation fields with numeric values; the marginal flag fires if N ≥ 90 OR M ≥ 180; future iterations comparing observation fields across runs can detect regression even when the test passes (a 75% → 90% budget consumption shift would be visible in the field values even before the budget is breached). |
| T-02.2-22 | Tampering (wrong-call-site replacement) | The `60*time.Second` and `2*time.Minute` (== `120*time.Second`) patterns appear in OTHER Eventually call sites in the same files for legitimate reasons. `credproxy_test.go` has THREE `60*time.Second` Eventually call sites (lines 95 pod-wait — the cascade-6 gate; 119 credproxy container presence; 135 second-spec pod-wait). `failure_test.go` has TWO `2*time.Minute` Eventually call sites (lines 130 beta-fail-Failed — the cascade-6 gate; 140 alpha-fail-Succeeded). Bumping the wrong occurrence would either over-loosen unrelated assertions (hiding bugs in the unbumped specs) OR miss the actual failing one (leaving cascade-6 open). | mitigate | Task 1 acceptance assertions count pre-edit AND post-edit occurrences AND assert the post-edit count is exactly one less than pre-edit per file (proving exactly one replacement happened per file). The replacement target is identified by SURROUNDING CONTEXT (the lambda body / comment / spec name), not by line number, because line numbers may have shifted post-Plan-02.2-07 refactor. For credproxy_test.go: target the Eventually whose lambda captures `podName` via `if len(p.Name) > 0 { podName = p.Name; return true }` and whose assertion message is `"A Pod should be created for the credproxy-task Job"` (this is the FIRST `60*time.Second` Eventually in the file, immediately preceding the credproxy container presence check). For failure_test.go: target the Eventually whose lambda returns `t.Status.Phase` for the `beta-fail` Task and whose assertion is `Should(Equal("Failed"))` with comment `// Wait for β to fail.`. **Acceptance gate:** post-edit `grep -c '60\*time\.Second' credproxy_test.go == 2` AND `grep -c '120\*time\.Second' credproxy_test.go == 1` AND `grep -c '2\*time\.Minute' failure_test.go == 1` AND `grep -cE '240\*time\.Second|4\*time\.Minute' failure_test.go == 1`; AND the preserved surrounding-context strings (`"A Pod should be created for the credproxy-task Job"`, `"beta-fail should reach Failed phase (testMode=fail-exit-1)"`, `"alpha-fail (independent sibling) should succeed even though beta-fail failed"`, `"Pod should contain a credproxy container (tide-credproxy)"`) all still match — proving the targeted Eventuallys were the ones changed AND the non-target Eventuallys were preserved. |
| T-02.2-23 | Denial of Service (unforeseeable cascade-7) | Cascade-7 (if any) is genuinely unknown — but cascade-6 is the FIRST FITNESS issue in the chain (all 5 prior cascades 1–5 were structural/correctness issues: PVC RWX→RWO, metrics flag, webhook-cert-path flag, test-budget, harness-bug fixture-incomplete). The fix is empirical (specs are running and reaching their assertions; they just need more time per the ResourceVersion conflict retry + initial-run-since-Phase-02.1 budget headroom analysis). Possible cascade-7 shapes: (a) `budget-vs-actual` — one of the bumped Eventuallys still times out (ResourceVersion conflict overhead exceeds even 120s/240s); (b) `spec-flake` in wave/caps/output specs (none have run end-to-end with full hierarchy yet); (c) `kind-infra` (image-pull regression, etcd race); (d) `harness-bug` (regression of cascade-5 — applyHierarchy helper somehow broke); (e) `flag-mismatch` (regression of cascade-2/3 — manager binary somehow re-broke); or (f) `other` (genuinely unforeseeable). | accept | Accepted with the requirement that if Task 2's gate is BLOCKED, `02.2-08-VERIFICATION.md` §"Section 5 Root-Cause Summary" MUST classify the cascade type using one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other` AND identify which specific spec(s) failed AND propose Plan 02.2-09's fix shape from the start — so the next iteration is not a diagnostic round-trip. Note: a `flag-mismatch` or `harness-bug` classification at this point would be a REGRESSION of Plans 02.2-04/05's or 02.2-07's fixes and would require immediate investigation; `budget-vs-actual` would suggest the underlying ResourceVersion conflict overhead exceeds even the bumped budgets and route to a production-code fix in Plan 02.2-09 (debounce concurrent status updates); `spec-flake` in a non-credproxy non-AC3 spec would surface real spec flakiness for the first time since Phase 02.1; `kind-infra` would route to suite_test.go BeforeSuite/AfterSuite or cluster configuration. **Probability cascade-7 surfaces:** LOW — if all dispatch paths now work end-to-end (cascade-5 fix confirmed at runtime in 02.2-07), the 7 specs should PASS deterministically once timeouts are adequate. The most likely outcome is APPROVED (chain terminator). **Acceptance gate:** if Task 2 records `gate_decision: BLOCKED`, the artifact contains one of the six classification tokens (`grep -E 'flag-mismatch\|budget-vs-actual\|spec-flake\|harness-bug\|kind-infra\|other' 02.2-08-VERIFICATION.md` matches) AND §Section 5 names the specific failure evidence. If APPROVED, the artifact still references the cascade-7 enum in a "cascade chain terminated" closing note (no cascade-7 surfaced). |

</threat_model>

<verification>
After all three tasks complete (or after Task 2 if its gate is BLOCKED):

**Source gates (Task 1):**
- `test/integration/kind/credproxy_test.go` pod-wait `Eventually` bumped from `60*time.Second` to `120*time.Second` (T-02.2-22 — exactly one replacement, identified by surrounding context)
- `test/integration/kind/failure_test.go` AC3 beta-fail `Eventually` bumped from `2*time.Minute` to `240*time.Second` (or equivalently `4*time.Minute`) (T-02.2-22 — exactly one replacement, identified by surrounding context)
- `go build ./test/integration/kind/...` exits 0
- `go vet ./test/integration/kind/...` exits 0
- No production source modified (internal/controller/, api/v1alpha1/, cmd/manager/, charts/tide/, Makefile all untouched — Option B contract preserved from Plans 02.2-06/07)
- `test/integration/kind/suite_test.go` untouched (applyHierarchy helper from Plan 02.2-07 intact — cascade-5 regression guard)
- Other test files (wave_test.go, caps_test.go, output_test.go, testdata/three-task-wave.yaml) untouched
- Non-target Eventually call sites in credproxy_test.go (lines 119, 135) preserved
- Non-target Eventually call site in failure_test.go (line 140 alpha-fail) preserved

**Runtime gates (Task 2):**
- `02.2-08-VERIFICATION.md` exists with all required frontmatter fields populated (`phase: 02.2`, `plan: 08`, `task: 2`, `artifact: verification`, exit codes, wall seconds, spec counts, T-02.2-21 observation fields `credproxy_pod_wait_observed_seconds` and `ac3_phase_wait_observed_seconds`)
- `gate_decision` is one of APPROVED or BLOCKED (PENDING not a final state)
- The artifact's Sections 1–6 mirror `02.2-07-VERIFICATION.md`'s structural template
- §Section 5 prose preserves the corrected root cause from `02.2-07-VERIFICATION.md` (names `resolveProject` as the actual gate; does NOT inherit the discredited "Wave Ready check" framing from `02.2-06-VERIFICATION.md`)
- §Section 5 prose surfaces the cascade-6 root cause (ResourceVersion conflict retry + first-end-to-end-run-since-Phase-02.1)
- Manager-log evidence (load-bearing for gate decision) is captured verbatim in the artifact, including a `dispatch` or `creating Job` line for credproxy-task within the new 120s budget AND a Job-Failed cycle for beta-fail within the new 240s budget (APPROVED case); evidence of cascade-7 failure shape with classification (BLOCKED case)
- Pod-state evidence (load-bearing per plan-checker tightening) is captured: STATUS=Running with restart count ≤ 1 (APPROVED) or evidence of a different failure (BLOCKED)
- Wall-time evidence (load-bearing): both runs ≤ 600s for APPROVED
- T-02.2-14 marginal-headroom carry-forward flag present if observed wall ≥ 500s even on APPROVED
- T-02.2-21 marginal-dispatch-latency flag (NEW) present if observed pod-wait ≥ 90s OR observed phase-wait ≥ 180s even on APPROVED
- All three new threats (T-02.2-21/22/23) have evidence in §6 + carry-forward T-02.2-01 through T-02.2-20
- Cascade-7 classification present per T-02.2-23 (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) — even if APPROVED, the artifact notes "cascade chain terminated" with explicit reference to the enum

**Wiring gates (Task 3 — HARD-GATED on Task 2 APPROVED):**
- `02.2-02-PLAN.md` has `depends_on: ["02.2-08"]` and ALL 31 references updated from `02.2-01-VERIFICATION.md` to `02.2-08-VERIFICATION.md` (exact count one-for-one)
- Stale `02.2-01-VERIFICATION.md`, `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, `02.2-05-VERIFICATION.md`, `02.2-06-VERIFICATION.md`, `02.2-07-VERIFICATION.md` references all purged from `02.2-02-PLAN.md`
- No other plans modified
- This is the CHAIN TERMINATOR rewire — the FIRST APPROVED Task 3 in Phase 02.2's entire history

**Structural gates (T-02.2-21/22/23 mitigation):**
- T-02.2-21 observation fields populated in `02.2-08-VERIFICATION.md` frontmatter; marginal-dispatch-latency flag rendered (either fired or "not marginal" noted)
- T-02.2-22 surrounding-context preservation: all four load-bearing assertion message strings still match in their respective files (proving the targeted Eventuallys were changed AND the non-targeted Eventuallys were preserved)
- T-02.2-23 cascade-7 classification token present per T-02.2-23 enum (even on APPROVED)
- Chart `charts/tide/values.yaml` NOT touched
- Production source (`internal/controller/task_controller.go`, `api/v1alpha1/task_types.go`, `internal/controller/plan_controller.go`, `cmd/manager/main.go`) NOT touched
- `test/integration/kind/suite_test.go` NOT touched (applyHierarchy helper from Plan 02.2-07 intact)
- Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07 NOT modified
- Plan 02.2-02 modified ONLY in Task 3 (gated on APPROVED)
- `.planning/STATE.md` and `.planning/ROADMAP.md` NOT touched (orchestrator owns)
</verification>

<success_criteria>
- Task 1 timeout bumps landed: `test/integration/kind/credproxy_test.go` pod-wait Eventually `60*time.Second` → `120*time.Second` (identified by lambda's `podName` capture + assertion message `"A Pod should be created for the credproxy-task Job"`); `test/integration/kind/failure_test.go` AC3 beta-fail Eventually `2*time.Minute` → `240*time.Second` (or `4*time.Minute`) (identified by lambda fetching `"beta-fail"` Task + assertion `Should(Equal("Failed"))` + comment `// Wait for β to fail.`); `go build` + `go vet` both exit 0; no production source modified (Option B contract preserved); `suite_test.go` untouched (applyHierarchy helper from Plan 02.2-07 intact); no other test files modified; non-target Eventually call sites in both files preserved verbatim
- Task 2 runtime verification artifact authored at `.planning/phases/02.2-.../02.2-08-VERIFICATION.md` with `gate_decision: APPROVED` or `gate_decision: BLOCKED` and all required structural sections, including the NEW T-02.2-21 mitigation observation fields (`credproxy_pod_wait_observed_seconds`/`ac3_phase_wait_observed_seconds`) + load-bearing wall-time + Pod-state + manager-log evidence + credproxy-test/failure-test namespace state + cascade-6 root cause analysis + cascade-7 classification (if BLOCKED); §Section 5 preserves the corrected root cause from `02.2-07-VERIFICATION.md` (`resolveProject` is the gate, NOT a Wave Ready check) AND surfaces the cascade-6 root cause (ResourceVersion conflict retry + first-end-to-end-run-since-Phase-02.1)
- Task 3 (HARD-GATED on Task 2 APPROVED) wired Plan 02.2-02's checkpoint to `02.2-08-VERIFICATION.md` with `depends_on: ["02.2-08"]` and all 31 stale `02.2-01-VERIFICATION.md` references one-for-one redirected — this is the CHAIN TERMINATOR rewire (first APPROVED Task 3 in Phase 02.2's history; Plans 02.2-03/04/05/06/07 Task 3 all halted on BLOCKED)
- If Task 2 returns BLOCKED: Task 3 does NOT fire; Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring); the orchestrator routes to a planner re-evaluation for Plan 02.2-09 (or equivalent) per the wave-failure-semantics contract and the tactical-iteration acceptance recorded in T-02.2-23; the cascade-7 classification in the verification artifact directs the planner to the right fix shape (Option A further timeout bump; Option B different-spec fix; Option C production-code fix for ResourceVersion conflict race; Option D Layer A/B split; Option E kind-infra fix)
- T-02.2-21/22/23 each have concrete evidence in the verification artifact and the source-edit acceptance gates
- Tactical scope honored: ONLY two test files modified (Option C — single test-only plan); production dispatch path in `internal/controller/task_controller.go` NOT touched; `test/integration/kind/suite_test.go`'s applyHierarchy helper NOT touched (cascade-5 regression guard); `charts/tide/values.yaml` NOT touched; Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07 NOT modified; Plan 02.2-06's Makefile `timeout 600s` preserved (verified by absence of timeout-124 SIGKILL in either run); Plan 02.2-07's applyHierarchy fix preserved (verified by absence of `no project found` for credproxy-task in manager logs)
- **Chain terminator note:** if APPROVED, Phase 02.2's cascade chain has terminated after 6 tactical iterations. Plan 02.2-02 dispatches in a follow-up wave to close Phase 02.2.
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-SUMMARY.md` documenting:
- The Eventually timeout bumps applied (Task 1): credproxy_test.go pod-wait `60*time.Second` → `120*time.Second` (identified by lambda's `podName` capture + assertion message); failure_test.go AC3 beta-fail `2*time.Minute` → `240*time.Second` (or `4*time.Minute`) (identified by lambda fetching `"beta-fail"` Task + Failed-phase assertion + comment); the cascade-6 root cause rationale (ResourceVersion conflict retry overhead for credproxy; first-end-to-end-run-since-Phase-02.1 for AC3); the non-target preservation evidence (T-02.2-22 mitigation — surrounding-context strings preserved for both files)
- The runtime verification outcome (Task 2): gate decision (APPROVED or BLOCKED), per-spec outcomes, wall times, exit codes, Pod-state evidence, manager-log evidence (particularly the `dispatch` line for credproxy-task within 120s budget AND Job-Failed cycle for beta-fail within 240s budget — proving the bumped budgets are adequate), T-02.2-21 observation fields (`credproxy_pod_wait_observed_seconds`/`ac3_phase_wait_observed_seconds`) and whether the marginal-dispatch-latency flag fired, cascade-7 classification per T-02.2-23 (or "cascade chain terminated" if APPROVED)
- The Plan 02.2-02 wiring update (Task 3, if APPROVED): which references were updated (count exactly 31), depends_on bump from 02.2-01 → 02.2-08, no plan-list/decisions edits per plan-checker preempt — this is the CHAIN TERMINATOR rewire (first APPROVED Task 3 in Phase 02.2's history)
- Any inconsistencies surfaced (e.g., marginal-headroom T-02.2-14 carry-forward flag on APPROVED; marginal-dispatch-latency T-02.2-21 NEW flag on APPROVED; cascade-7 surfacing on BLOCKED with its classification)
- The Phase 02.2 close-out chain progression: 5 source-shape fixes (02.2-01) + 1 chart PVC fix (02.2-03) + 1 binary metrics flag (02.2-04) + 1 binary webhook-cert-path flag (02.2-05) + 1 Makefile timeout bump (02.2-06) + 1 test-fixture refactor + helper (02.2-07) + 2 Eventually timeout bumps (this plan) landed → runtime verified once (APPROVED → chain terminator → Plan 02.2-02 unblocked → Phase 02.2 closes) or routed back (BLOCKED → Plan 02.2-09) for next iteration
- Threat-model mitigations applied (T-02.2-21 / T-02.2-22 / T-02.2-23) with concrete evidence + carry-forward T-02.2-01 through T-02.2-20
- A note on the structural shift: cascades 1–3 were all flag-mismatch (predictable, terminated at 5/5 chart args defined); cascade-4 was budget-vs-actual (structurally different, single-line Makefile fix); cascade-5 was harness-bug — fixture-incomplete (per corrected debug investigation in `02.2-07-VERIFICATION.md`); cascade-6 was spec-flake — the FIRST FITNESS issue in the chain (two single-token timeout bumps); cascade-7 (if any) shape genuinely unknown per T-02.2-23. The cascade chain is NOT a simple linear sequence — each iteration uncovered a structurally distinct class. **Phase 02.2's pattern: tactical iteration with runtime verification at each step is the right shape; comprehensive audit would not have caught any of the 6 cascades because each surfaced at runtime AFTER the prior was closed.**
- An explicit framing-correction preservation note: `02.2-06-VERIFICATION.md` §Section 5 hypothesized the cascade-5 gate as a "parent Wave Ready check" — that hypothesis was WRONG. The debug investigation captured at `.planning/debug/credproxy-dispatch-guard.md` corrected the framing to the `resolveProject` Project-in-namespace lookup. `02.2-07-VERIFICATION.md` §Section 5 documented the correction. THIS plan's `02.2-08-VERIFICATION.md` §Section 5 MUST preserve the corrected framing (the gate is `resolveProject` at `task_controller.go:217`, NOT a Wave Ready check) AND surface the cascade-6 root cause (ResourceVersion conflict retry + first-end-to-end-run-since-Phase-02.1).
- **Production-code fix surfaced as future-phase target:** the underlying ResourceVersion conflict race between `PlanReconciler.stampTaskLabels` and `TaskReconciler` status updates is a real production-code concern. The cascade-6 fix (test timeout bumps) is a TACTICAL ACCOMMODATION, not a durable fix. If T-02.2-21 marginal-dispatch-latency flag fires on this run (observed pod-wait ≥ 90s OR phase-wait ≥ 180s), the SUMMARY should explicitly recommend a future-phase production-code fix (debounce concurrent status updates, optimistic-concurrency-aware retry budgets) for `internal/controller/task_controller.go` and/or `internal/controller/plan_controller.go`.
</output>
</content>
</invoke>