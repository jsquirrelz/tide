---
phase: "02.2"
plan: "09"
subsystem: "test/integration/kind"
tags: [gap-closure, debug, test-fixture, harness-bug, runtime-verification, kind-integration, phase-02.2, tactical, cascade-7, cascade-8, scope-completion, chain-terminator-candidate-v2]

dependency_graph:
  requires: ["02.2-08"]
  provides: ["createProjectHierarchy helper", "cascade-7 closure (caps/output/failure fixtures)"]
  affects: ["test/integration/kind/suite_test.go", "test/integration/kind/caps_test.go", "test/integration/kind/output_test.go", "test/integration/kind/failure_test.go"]

tech_stack:
  added: []
  patterns: ["sibling-helper pattern (adds-not-modifies); createProjectHierarchy as parent-only variant of applyHierarchy"]

key_files:
  created:
    - ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md"
  modified:
    - "test/integration/kind/suite_test.go"
    - "test/integration/kind/caps_test.go"
    - "test/integration/kind/output_test.go"
    - "test/integration/kind/failure_test.go"

decisions:
  - "cascade-7 fix is structurally correct: createProjectHierarchy helper adds Namespace+Secret+Project+Milestone+Phase to caps/output/failure namespaces; zero no-project-found errors for those namespaces in manager logs"
  - "cascade-8 classified as spec-flake: credproxy-task ResourceVersion conflict triggers exponential-backoff, suppressing task controller for 127s (beyond 120s pod-wait budget)"
  - "Task 3 gated out: gate_decision BLOCKED per wave-failure-semantics contract (Task 3 fires ONLY on APPROVED)"
  - "Plan 02.2-10 fix shape: bump credproxy pod-wait 120s -> 240s (Option alpha, same class as cascade-6 fix)"
  - "Option B contract preserved: zero production code modifications"
  - "applyHierarchy byte-identical: existing function preserved verbatim (cascade-5 fix)"
  - "credproxy_test.go untouched: cascade-5/6 fixes preserved verbatim"

metrics:
  duration: "~40min (Task 1 ~10min build+verify; Task 2 ~30min clean run + log analysis)"
  completed_date: "2026-05-14"
  tasks_completed: 2
  tasks_total: 3
  files_created: 1
  files_modified: 4
---

# Phase 02.2 Plan 09: Cascade-7 Closure (createProjectHierarchy helper) Summary

**One-liner:** New `createProjectHierarchy` helper + 3 fixture call-site insertions closes cascade-7 (caps/output/failure namespaces now have parent Project hierarchy); cascade-8 (credproxy exponential-backoff suppression) surfaces as next gate; Task 3 gated out on BLOCKED.

## What Was Built

### Task 1 — New helper + 3 call-site insertions (COMMITTED: b971805)

Added `func createProjectHierarchy(ctx context.Context, ns string) error` to `test/integration/kind/suite_test.go` as a SIBLING function alongside the existing `applyHierarchy`. The new helper creates Namespace+Secret+Project+Milestone+Phase (stopping at Phase — no Plan or Task) with byte-identical YAML shapes to `applyHierarchy`'s corresponding sections (T-02.2-24 mitigation).

Three call-site insertions (one per remaining Layer B fixture file):
- `caps_test.go`: `Expect(createProjectHierarchy(ctx, capsNS)).To(Succeed())` before `applyYAML(capPlanYAML)` (replaced standalone `createNamespace` call)
- `output_test.go`: `Expect(createProjectHierarchy(ctx, outputNS)).To(Succeed())` before `applyYAML(outputPlanYAML)`
- `failure_test.go`: `Expect(createProjectHierarchy(ctx, ns)).To(Succeed())` before `applyYAML(planYAML)`

All acceptance gates passed:
- `grep -c 'func applyHierarchy' suite_test.go` = 1 (existing function preserved)
- `grep -c 'func createProjectHierarchy' suite_test.go` = 1
- `git status --porcelain test/integration/kind/credproxy_test.go` = empty (untouched)
- `git status --porcelain test/integration/kind/testdata/` = empty (untouched)
- `go build ./test/integration/kind/...` exits 0
- `go vet ./test/integration/kind/...` exits 0
- Zero modifications to `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `Makefile`

### Task 2 — Runtime verification (COMMITTED: 3cf9089)

Clean run executed (`make test-int`, fresh kind cluster). Results:

**Wall time:** 303s  **Exit code:** 1  **Ginkgo:** panic after 5m0s

**Cascade-7 confirmed CLOSED:** Manager logs for the clean run show ZERO `no project found in namespace caps-test`, `no project found in namespace output-test`, or `no project found in namespace failure-test` errors. The `createProjectHierarchy` fix is structurally correct.

**Cascade-8 surfaced:** `credproxy_test.go` HARN-03 (first spec to run) failed again — pod-wait `Eventually(120*time.Second)` timed out. Root cause: at plan-creation time, the plan controller and task controller simultaneously touch `credproxy-task` (PlanReconciler.stampTaskLabels race), both hitting ResourceVersion conflicts. The task controller's exponential-backoff holds `credproxy-task` off the work queue for 127s after the conflict — beyond the 120s pod-wait budget. The task controller ran exactly ONE reconcile for `credproxy-task` during the entire 120s window. No Job was created.

**BLOCKED gate** — `gate_decision: BLOCKED` (cascade-8: spec-flake/budget-vs-actual).

### Task 3 — Gated rewire (NOT EXECUTED)

Task 3 gates on `gate_decision: APPROVED` per wave-failure-semantics contract (CLAUDE.md). Since Task 2 recorded BLOCKED, Task 3 does not fire. `02.2-02-PLAN.md` remains unchanged (still references `02.2-01-VERIFICATION.md` at all 31 read paths).

## Deviations from Plan

### No Rule 1-4 deviations

The task code changes executed exactly as planned. The cascade-8 discovery is documented behavior (T-02.2-27 acceptance in the plan frontmatter explicitly anticipated this possibility).

### Cascade-8 Classification (per T-02.2-27 acceptance requirement)

**Classification (T-02.2-16 taxonomy):** `spec-flake` / `budget-vs-actual`

- NOT `flag-mismatch` (all 5 chart args satisfied since Plan 02.2-05)
- NOT `harness-bug` (the hierarchy is now in place; resolveProject would succeed)
- NOT `kind-infra` (cluster itself is healthy; manager running 0 restarts)
- YES `spec-flake`: the credproxy-task dispatch takes >120s due to a production-code race (PlanReconciler.stampTaskLabels concurrent ResourceVersion conflict) triggering controller-runtime exponential-backoff

**Specific spec failing:** `credproxy_test.go` HARN-03 at `credproxy_test.go:95` — `Eventually(120*time.Second)` for pod-wait.

**Evidence:** Task controller ran exactly 1 reconcile attempt for `credproxy-task` (at 17:58:49Z), hit ResourceVersion conflict, then exponential-backoff suppressed re-reconciliation for 127s (until 18:00:56Z AfterEach cleanup). Zero Job-created log entries during the 120s window.

**Plan 02.2-10 fix shape:**
- Task 1: Bump `credproxy_test.go` pod-wait timeout from `120*time.Second` → `240*time.Second` (single token replacement, same class as cascade-6's AC3 fix)
- Task 2: Runtime verification
- Task 3: Gated rewire of `02.2-02-PLAN.md` if Task 2 returns APPROVED

## Known Stubs

None — this plan adds test infrastructure helpers only; no data sources, UI components, or endpoints.

## Threat Surface Scan

No new production code introduced. Test-only changes. No new network endpoints, auth paths, or schema changes.

## Self-Check

### Files exist:
- `/Users/justinsearles/Projects/tide/.claude/worktrees/agent-ad64ce2ca38688db0/test/integration/kind/suite_test.go` — FOUND (modified: +91 lines new helper)
- `/Users/justinsearles/Projects/tide/.claude/worktrees/agent-ad64ce2ca38688db0/test/integration/kind/caps_test.go` — FOUND (modified: createProjectHierarchy call)
- `/Users/justinsearles/Projects/tide/.claude/worktrees/agent-ad64ce2ca38688db0/test/integration/kind/output_test.go` — FOUND (modified: createProjectHierarchy call)
- `/Users/justinsearles/Projects/tide/.claude/worktrees/agent-ad64ce2ca38688db0/test/integration/kind/failure_test.go` — FOUND (modified: createProjectHierarchy call)
- `.planning/phases/02.2-.../02.2-09-VERIFICATION.md` — FOUND (gate_decision: BLOCKED)

### Commits exist:
- b971805 — feat(02.2-09): add createProjectHierarchy helper + 3 fixture call-site insertions
- 3cf9089 — docs(02.2-09): runtime verification — BLOCKED gate

### Acceptance gate checks:
- `applyHierarchy` preserved: `git diff -- test/integration/kind/suite_test.go | grep -E '^-.*func applyHierarchy'` = 0 lines
- `credproxy_test.go` untouched: `git status --porcelain test/integration/kind/credproxy_test.go` = empty
- `createProjectHierarchy` in suite_test.go: count = 1
- Option B contract: zero modifications to internal/cmd/api/pkg/charts/Makefile

## Self-Check: PASSED
</content>
</invoke>