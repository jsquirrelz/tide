---
phase: 02.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/kind/suite_test.go
  - .github/workflows/ci.yaml
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md
autonomous: true
requirements: []
tags: [kind, ginkgo, helm, cert-manager, docker, ci-yaml, test-infrastructure, beforesuite, aftersuite, zombie-cleanup, kindTestTimeout]

threat_model_summary:
  - "T-02.2-01 (accept): TIDE_SIGNING_KEY leak in CI logs — Secret is in K8s Secret, not shell env; CI YAML edit adds no env surface"
  - "T-02.2-02 (mitigate): AfterSuite cleanup violates KEEP_KIND_CLUSTER=true — env-var short-circuit BEFORE cleanup helper runs"
  - "T-02.2-03 (mitigate): cert-manager v1.20 silent CRD-removal breaks chart webhook — clean-run reproducer exercises chart end-to-end"
  - "T-02.2-04 (mitigate): accidentally revert Phase 02.1 D-03 TIDE_REQUIRE_CONTROLLER env block — acceptance grep gate confirms env block preserved"

must_haves:
  truths:
    - "kindTestTimeout outer context exceeds helm install --timeout 5m budget plus pre-helm setup cost (~50s) plus variance margin"
    - "AfterSuite cleanup robustly removes zombie kind containers via docker rm -f fallback when kind delete cluster leaves residual containers"
    - "AfterSuite cleanup honors KEEP_KIND_CLUSTER=true short-circuit BEFORE the zombie-cleanup helper runs"
    - "helm install args include --replace so KEEP_KIND_CLUSTER=true rerun against an existing failed/successful release succeeds idempotently"
    - "cert-manager pinned to v1.20.2 (explicit K8s 1.33 support; chart-side cert-manager.io/v1 API usage unchanged)"
    - "CI YAML drops the redundant outer DUR > 300 wall-time check (Makefile inner timeout 300s is the source of truth) while preserving Phase 02.1 D-03 TIDE_REQUIRE_CONTROLLER=1 env block"
    - "make test-int from clean state exits 0 with 7/7 Layer B specs PASS; KEEP_KIND_CLUSTER=true rerun exits 0 with 7/7 specs PASS; both runs ≤ 300s on the go test step"
    - "02.2-01-VERIFICATION.md captures clean run + idempotent rerun + per-spec outcome tables + wall-time gate assertion + gate decision (APPROVED/BLOCKED)"
  artifacts:
    - path: "test/integration/kind/suite_test.go"
      provides: "kindTestTimeout = 7 * time.Minute; certManagerVersion = v1.20.2; helm install --replace flag; cleanupKindCluster() helper + rewired AfterSuite"
      contains: "kindTestTimeout = 7 * time.Minute"
    - path: "test/integration/kind/suite_test.go"
      provides: "cert-manager pin"
      contains: 'certManagerVersion = "v1.20.2"'
    - path: "test/integration/kind/suite_test.go"
      provides: "helm install idempotency flag"
      contains: '"--replace"'
    - path: "test/integration/kind/suite_test.go"
      provides: "AfterSuite zombie-cleanup helper"
      contains: "func cleanupKindCluster()"
    - path: "test/integration/kind/suite_test.go"
      provides: "kind container label filter for zombie detection"
      contains: "io.x-k8s.kind.cluster=%s"
    - path: ".github/workflows/ci.yaml"
      provides: "redundant DUR > 300 wall-time check dropped"
      forbids: "\\$DUR -gt 300"
    - path: ".github/workflows/ci.yaml"
      provides: "Phase 02.1 D-03 TIDE_REQUIRE_CONTROLLER=1 env block preserved"
      contains: "TIDE_REQUIRE_CONTROLLER"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md"
      provides: "End-to-end runtime verification artifact: clean run + idempotent rerun + per-spec outcomes + gate decision"
      contains: "gate_decision:"
  key_links:
    - from: "test/integration/kind/suite_test.go (kindTestTimeout const)"
      to: "test/integration/kind/suite_test.go (BeforeSuite context.WithTimeout)"
      via: "context.WithTimeout(context.Background(), kindTestTimeout) at line 88"
      pattern: "context\\.WithTimeout.*kindTestTimeout"
    - from: "test/integration/kind/suite_test.go (cleanupKindCluster helper)"
      to: "test/integration/kind/suite_test.go (AfterSuite body)"
      via: "AfterSuite invokes cleanupKindCluster() after KEEP_KIND_CLUSTER short-circuit"
      pattern: "cleanupKindCluster\\(\\)"
    - from: "test/integration/kind/suite_test.go (cleanupKindCluster docker fallback)"
      to: "kind's documented io.x-k8s.kind.cluster=<name> container label"
      via: "docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test"
      pattern: "io\\.x-k8s\\.kind\\.cluster="
    - from: "test/integration/kind/suite_test.go (applyController helm args)"
      to: "helm install --replace idempotency"
      via: "exec.CommandContext helm install ... --replace"
      pattern: '"--replace"'
    - from: ".github/workflows/ci.yaml (CI env block)"
      to: "Phase 02.1 D-03 hard-fail gate"
      via: "env: TIDE_REQUIRE_CONTROLLER: \"1\""
      pattern: "TIDE_REQUIRE_CONTROLLER"
    - from: "02.2-01-VERIFICATION.md (frontmatter)"
      to: "Phase 02.1 02.1-04-VERIFICATION.md structural template"
      via: "Same frontmatter fields (clean_run_exit, clean_run_wall_seconds, rerun_exit, rerun_wall_seconds, gate_decision)"
      pattern: "gate_decision:"
---

<objective>
Land all five Phase 02.2 source-shape fixes that close Phase 02.1's BLOCKED runtime gate captured in `02.1-04-VERIFICATION.md`, then verify the fix end-to-end with a clean run + idempotent rerun reproducer captured in `02.2-01-VERIFICATION.md`.

The five fixes:
1. Bump `kindTestTimeout = 4 * time.Minute` → `7 * time.Minute` so the outer BeforeSuite context budget exceeds `helm install --timeout 5m` plus the ~50s pre-helm setup cost plus variance margin.
2. Add a robust `cleanupKindCluster()` helper that falls back to `docker rm -f -v` against containers labeled `io.x-k8s.kind.cluster=tide-test` when `kind delete cluster` leaves residual containers (kind issue #1116 / moby/moby#51845 Docker 28→29 regression). Rewire AfterSuite to invoke it while preserving the `KEEP_KIND_CLUSTER=true` short-circuit at the top.
3. Drop the redundant `DUR > 300` outer wall-time check in `.github/workflows/ci.yaml` because the Makefile's existing inner `timeout 300s go test` enforces the contract on the correct span (the CI outer check measures the wrong span — includes the ~880s cold-cache prep phase). PRESERVE the Phase 02.1 D-03 `TIDE_REQUIRE_CONTROLLER=1` env block (separate line range; unrelated).
4. Bump `certManagerVersion = "v1.16.2"` → `"v1.20.2"` for explicit K8s 1.33 support and marginal startup-time savings. The chart's `cert-manager.io/v1` Issuer + Certificate API usage is unchanged.
5. Add `"--replace"` to helm install args at the BeforeSuite `applyController()` call so `KEEP_KIND_CLUSTER=true` rerun against an existing helm release succeeds idempotently.

After source edits land, the verification reproducer runs the same clean-run + idempotent-rerun pair captured in `02.1-04-VERIFICATION.md` and records the outcome in `02.2-01-VERIFICATION.md` with `gate_decision: APPROVED` or `BLOCKED`. The closeout plan (`02.2-02-PLAN.md`) is gated on APPROVED.

Purpose: this is debug closeout for Phase 02.1's structurally-correct-but-runtime-failing test infrastructure. Phase 02.1 Plan 02 pivoted to a helm-install BeforeSuite; that source shape is correct. The remaining failure mode is timing arithmetic + Docker-cleanup robustness + CI scope correction — all tactical, all in known files.

Output:
- `test/integration/kind/suite_test.go` with the four source-shape fixes (consts + helper + AfterSuite rewire + helm --replace)
- `.github/workflows/ci.yaml` with the redundant DUR check removed and the D-03 env block preserved
- `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md` capturing the clean run + idempotent rerun outcome
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-02-SUMMARY.md
@test/integration/kind/suite_test.go
@Makefile
@.github/workflows/ci.yaml

<interfaces>
<!-- Key identifiers and call sites from current source. Executor uses these directly — no codebase exploration needed. -->

From test/integration/kind/suite_test.go:
- Line 59 — const `kindTestTimeout = 4 * time.Minute` (TARGET: → `7 * time.Minute`)
- Line 88 — `ctx, cancel = context.WithTimeout(context.Background(), kindTestTimeout)` (consumer of the const; no edit needed)
- Lines 130-144 — `AfterSuite(func() { cancel(); if os.Getenv("KEEP_KIND_CLUSTER") == "true" { ... return } By(...); cmd := exec.Command("kind", "delete", "cluster", ...); ... })` (TARGET: rewire to call new `cleanupKindCluster()` helper after the KEEP_KIND_CLUSTER short-circuit)
- Line 217 — `const certManagerVersion = "v1.16.2"` (TARGET: → `"v1.20.2"`)
- Line 224-227 — `TIDE_CERT_MANAGER_VERSION` env-var override path (NO EDIT — preserve override semantics)
- Lines 325-337 — `helmCmd := exec.CommandContext(ctx, "helm", "install", "tide", chartDir, ... "--wait", "--timeout", "5m")` (TARGET: add `"--replace"` to the args)
- Lines 343-345 — `if os.Getenv("TIDE_REQUIRE_CONTROLLER") == "1" { Fail(...) }` (NO EDIT — preserve D-03 gate)
- `kindClusterName = "tide-test"` (line 57) — used by AfterSuite + zombie detection

From .github/workflows/ci.yaml:
- Lines 161-165 — env block: `TIDE_REQUIRE_CONTROLLER: "1"` (PRESERVE — Phase 02.1 D-03)
- Lines 200-211 — `Integration test suite (TEST-02 — make test-int)` step with `START=$(date +%s); time make test-int; END=$(date +%s); DUR=$((END - START)); echo ...; if [ "$DUR" -gt 300 ]; then exit 1; fi` (TARGET: drop START/END/DUR/if block; keep `time make test-int`)

From Makefile:
- Lines 122-124 — `test-int:` target with `timeout 300s go test ./test/integration/... -v -timeout=5m -ginkgo.v` (NO EDIT — already correctly scopes the 300s budget to the go test invocation)

From charts/tide/templates/serving-cert.yaml and metrics-certs.yaml:
- Both `Certificate` resources specify `issuerRef.kind: Issuer` explicitly (cert-manager v1.20 issuerRef-defaults revert is non-impacting; confirmed audit per RESEARCH.md Pitfall 4)

From cmd/credproxy/main.go:
- Line 128 — `fmt.Fprintf(os.Stdout, "credproxy listening on %s\n", listenAddr)` (NO EDIT — Phase 02.1 D-01 already shipped; spec 7 dependency)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply four source-shape fixes to suite_test.go (kindTestTimeout, certManagerVersion, helm --replace, cleanupKindCluster helper + AfterSuite rewire)</name>
  <files>test/integration/kind/suite_test.go</files>

  <read_first>
    - test/integration/kind/suite_test.go (current source: lines 30-50 imports; line 59 kindTestTimeout const; line 88 context.WithTimeout consumer; lines 130-144 AfterSuite body; line 217 certManagerVersion const; lines 325-337 helm install args; lines 343-345 TIDE_REQUIRE_CONTROLLER gate)
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md §"Key Findings" / §"Pattern 1: Outer-context-must-exceed-subprocess-budget" / §"Pattern 2: Robust Docker container cleanup as fallback to kind delete" / §"Pattern 4: cert-manager version bump (optional, low risk)" / §"Code Examples" §"Fix #1" §"Fix #2" §"Fix #4" / §"Open Questions Surfaced" Q1 / §"Pitfall 7: AfterSuite cancel() called before zombie cleanup uses ctx"
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md §"Failure Detail" (exact zombie-container error string + Docker daemon kill-event regression evidence) and §"Root-Cause Summary"
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-02-SUMMARY.md §"Issues Encountered" (KEEP_KIND_CLUSTER=true future-iterating users will hit 'helm release already exists' on re-run — Q1 collateral concern)
  </read_first>

  <action>
    Make four edits to `test/integration/kind/suite_test.go` in a single atomic change. All four touch separate, non-overlapping line ranges in the same file.

    **Edit A — line 59 (kindTestTimeout constant):** Replace `kindTestTimeout = 4 * time.Minute` with `kindTestTimeout = 7 * time.Minute`. Rationale (RESEARCH §"Pattern 1"): 7m = ~50s pre-helm setup + 300s helm --timeout 5m + 5s waitForControllerReady fallback + 60s+ variance margin for slow CI runners. Going below ~6m25s shadow-kills the helm subprocess before its own --timeout fires. Add an inline comment block above the const describing the budget arithmetic (per RESEARCH §"Code Examples" §"Fix #1"). The comment is part of the change.

    **Edit B — line 217 (certManagerVersion constant):** Replace `const certManagerVersion = "v1.16.2"` with `const certManagerVersion = "v1.20.2"`. Update the doc comment above to note v1.20.2 adds explicit K8s 1.33 support (matches kindest/node:v1.33.7 pinned by `cluster.yaml`); chart-side `cert-manager.io/v1` Issuer + Certificate usage is unchanged. Preserve the `TIDE_CERT_MANAGER_VERSION` env-var override semantics at lines 224-227 — that branch is NOT edited; only the default value changes.

    **Edit C — line ~336 (helm install args, add --replace):** In the `helmCmd := exec.CommandContext(...)` block (currently lines 325-337), add `"--replace"` as a new argument to the helm install invocation. Place it adjacent to `"--wait"` so the args block reads `..., "--wait", "--replace", "--timeout", "5m"`. Rationale (RESEARCH §"Open Questions Surfaced" Q1): on `KEEP_KIND_CLUSTER=true` rerun, the existing `tide` release in `tide-system` namespace causes helm to error with "cannot re-use a name that is still in use" without `--replace`. Phase 02.1 Plan 02 SUMMARY §"Issues Encountered" flagged this as a known collateral concern.

    **Edit D — lines 130-144 (AfterSuite + new cleanupKindCluster helper):** Rewrite the AfterSuite body so its only non-short-circuit action is a call to `cleanupKindCluster()`. The body must preserve the existing two-step ordering:
    1. `cancel()` (existing — unchanged)
    2. `if os.Getenv("KEEP_KIND_CLUSTER") == "true" { GinkgoWriter.Println(...); return }` (existing — unchanged; this MUST stay BEFORE any cleanup helper invocation per T-02.2-02 mitigation)
    3. `By("Deleting kind cluster " + kindClusterName)` (existing — unchanged)
    4. `cleanupKindCluster()` (NEW — replaces the inline `kind delete cluster` exec.Command block)

    Then add a new package-private helper function `cleanupKindCluster()` below the AfterSuite block (the helper can live anywhere below the suite functions; per RESEARCH recommendation place it near the existing cluster lifecycle helpers like `ensureKindCluster` for locality). The helper's contract follows the pattern in RESEARCH §"Pattern 2: Robust Docker container cleanup as fallback to kind delete" §"Code Examples" §"Fix #2":

    Helper behavior (read RESEARCH §"Pattern 2" for the full pseudocode pattern — do NOT inline it here):
    - Attempt 1: shell out `kind delete cluster --name <kindClusterName>` using plain `exec.Command(...)` (NOT `exec.CommandContext(ctx, ...)` — per RESEARCH Pitfall 7, the outer test ctx is cancelled by the time AfterSuite runs; using it would immediately abort cleanup). On failure, log a `GinkgoWriter.Printf` warning but DO NOT call `Fail()` (cleanup is best-effort per A4 / Pitfall 7).
    - Verify no residual containers regardless of attempt 1's exit code: `docker ps -aq --filter label=io.x-k8s.kind.cluster=<kindClusterName>`. If empty, return (happy path).
    - Attempt 2 (zombie fallback): `docker rm -f -v <container-IDs from attempt 2 listing>`. On failure, log a `GinkgoWriter.Printf` warning. Do NOT panic or `Fail()`.

    Import additions required: the helper uses `fmt`, `os/exec`, `strings` — all already imported in the file (lines 35-41). Verify no new imports are needed by `go vet` after the edit.

    The label literal in both docker subprocess invocations MUST be `io.x-k8s.kind.cluster=%s` formatted with `kindClusterName`. This is kind's documented internal labeling convention (RESEARCH A3 [LOW risk]) and survives a future multi-node `cluster.yaml` change.

    Constraints (CLAUDE.md):
    - No new external libraries; uses only `exec.Command`, `os.Getenv`, `fmt.Sprintf`, `strings.Fields`, `strings.TrimSpace`, `GinkgoWriter.Printf` — all stdlib + Ginkgo facilities already imported.
    - No host `~/.claude/` mount touched (irrelevant; no LLM code).
    - No Anthropic SDK import added (verified by `make verify-import-firewall`).
    - Preserve the existing `TIDE_REQUIRE_CONTROLLER` semantics at lines 343-345 — that env-var is the Phase 02.1 D-03 gate for soft-skip vs hard-fail; this task does NOT touch it.
    - Per RESEARCH Pitfall 7, the helper's two `docker` shell-outs use plain `exec.Command()`, NOT `exec.CommandContext(ctx, ...)`. The outer ctx is cancelled by the time AfterSuite runs (`cancel()` was called as the first AfterSuite line).
    - Per RESEARCH §"Anti-Patterns to Avoid": do NOT add `--force` to `kind delete cluster` (no such flag); do NOT add `set -e`-equivalent panics inside the helper (cleanup is best-effort); do NOT replace `kind delete` entirely with `docker rm -f` (kind owns kubeconfig context cleanup outside the container).
  </action>

  <verify>
    <automated><![CDATA[
      # Compile + vet
      go build ./test/integration/kind/... && \
      go vet ./test/integration/kind/... && \
      gofmt -l test/integration/kind/suite_test.go | tee /tmp/gofmt-out && [ ! -s /tmp/gofmt-out ] && \
      # Edit A: kindTestTimeout bumped to 7m
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'kindTestTimeout = 7 \* time.Minute')" -ge 1 ] && \
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'kindTestTimeout = 4 \* time.Minute')" -eq 0 ] && \
      # Edit B: certManagerVersion bumped to v1.20.2
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'certManagerVersion = \"v1.20.2\"')" -eq 1 ] && \
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'certManagerVersion = \"v1.16.2\"')" -eq 0 ] && \
      # Edit C: --replace flag added to helm install args
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c '\"--replace\"')" -ge 1 ] && \
      # Edit D: cleanupKindCluster helper exists, AfterSuite calls it
      [ "$(grep -c 'func cleanupKindCluster()' test/integration/kind/suite_test.go)" -eq 1 ] && \
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'cleanupKindCluster()')" -ge 2 ] && \
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'io.x-k8s.kind.cluster=%s')" -ge 1 ] && \
      # AfterSuite KEEP_KIND_CLUSTER short-circuit preserved (T-02.2-02 mitigation)
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'KEEP_KIND_CLUSTER')" -ge 1 ] && \
      # D-03 gate untouched (T-02.2-04 mitigation, file-scoped)
      [ "$(grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'TIDE_REQUIRE_CONTROLLER')" -ge 1 ] && \
      # Phase 1 import firewalls remain green
      make verify-dag-imports && \
      make verify-dispatch-imports && \
      make verify-import-firewall && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Compile + vet:** `go build ./test/integration/kind/...` exits 0 AND `go vet ./test/integration/kind/...` exits 0
    - **gofmt clean:** `gofmt -l test/integration/kind/suite_test.go` produces empty output
    - **Edit A (kindTestTimeout):** `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'kindTestTimeout = 7 \* time.Minute'` returns ≥ 1 AND `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'kindTestTimeout = 4 \* time.Minute'` returns 0
    - **Edit B (certManagerVersion):** `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'certManagerVersion = "v1.20.2"'` returns exactly 1 AND `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'certManagerVersion = "v1.16.2"'` returns 0
    - **Edit C (helm --replace):** `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c '"--replace"'` returns ≥ 1
    - **Edit D (cleanupKindCluster helper exists):** `grep -c 'func cleanupKindCluster()' test/integration/kind/suite_test.go` returns exactly 1
    - **Edit D (AfterSuite invokes helper + helper body uses kind label):** `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'cleanupKindCluster()'` returns ≥ 2 (one call site in AfterSuite + helper definition reference) AND `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'io.x-k8s.kind.cluster=%s'` returns ≥ 1
    - **T-02.2-02 mitigation (KEEP_KIND_CLUSTER short-circuit preserved):** `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'KEEP_KIND_CLUSTER'` returns ≥ 1
    - **T-02.2-04 file-scoped mitigation (D-03 env-var untouched in suite_test.go):** `grep -v '^[[:space:]]*//' test/integration/kind/suite_test.go | grep -c 'TIDE_REQUIRE_CONTROLLER'` returns ≥ 1 (the helm-install Fail() gate at line 343-345 is preserved)
    - **Phase 1 import firewalls remain green:** `make verify-dag-imports` exits 0 AND `make verify-dispatch-imports` exits 0 AND `make verify-import-firewall` exits 0
  </acceptance_criteria>

  <done>
    All four source-shape edits applied to `test/integration/kind/suite_test.go` in one atomic commit. File compiles, vets clean, and passes gofmt. The four grep gates above all pass with the specified counts. Phase 1's import-firewall verifications (DAG, dispatch, anthropic-SDK) remain green. Runtime verification of the four fixes is deferred to Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Drop redundant DUR > 300 wall-time check in CI workflow (preserve TIDE_REQUIRE_CONTROLLER=1 env block)</name>
  <files>.github/workflows/ci.yaml</files>

  <read_first>
    - .github/workflows/ci.yaml (current source: lines 155-228 — the `test-int` job; specifically lines 161-165 D-03 env block + lines 200-211 the redundant DUR check)
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md §"Pattern 3: Wall-time budget scoped to test invocation only" §"Option (a) **Drop CI's outer DUR check entirely**" / §"Code Examples" §"Fix #3 — Makefile re-scope (RECOMMENDED MINIMAL CHANGE — option (a))" / §"Pitfall 5: Makefile recipe ordering hides the prep cost from CI's wall-time assertion"
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md §"Section 4 — Wall-time Budget Check" (why the outer check measures the wrong span; 1187s clean-run wall time = ~880s prep + 300s timed go test)
    - Makefile lines 120-124 (existing `timeout 300s go test ...` inside `test-int` recipe — the source of truth for the budget)
  </read_first>

  <action>
    Edit `.github/workflows/ci.yaml` to drop the redundant outer wall-time check that measures the wrong span (full `make test-int` chain including unbounded `test-int-kind-prep`), while preserving the Phase 02.1 D-03 `TIDE_REQUIRE_CONTROLLER=1` env block which lives in a different line range.

    **Edit — the `Integration test suite (TEST-02 — make test-int)` step (currently lines 200-211):** Replace the `run:` block. Drop the `START=$(date +%s)`, `END=$(date +%s)`, `DUR=$((END - START))`, `echo "Integration test duration: ${DUR}s"`, and the `if [ "$DUR" -gt 300 ]; then ... exit 1; fi` block. Keep `set -e` and `time make test-int`. The Makefile's existing inner `timeout 300s go test ...` enforces the 300s budget correctly on the `go test` span; if it fires (exit 124), `time make test-int` propagates that exit code and the step fails. No outer assertion needed.

    Also update the explanatory comment block above the step (currently lines 196-199) to describe the new semantics per RESEARCH §"Code Examples" §"Fix #3" diff:
    - The `timeout 300s` inside `make test-int` enforces the 300s budget on the `go test` invocation only.
    - `test-int-kind-prep` (image builds + kind cluster create + kind load) is unbounded by design — cold-cache prep cost can reach ~880s on a clean GitHub Actions runner.
    - The outer `timeout-minutes: 6` job-level fence is the genuine ceiling on the entire job.
    - No separate `DUR > 300` check is needed; `timeout 300s` exits 124 if go test wall-time exceeds 300s and the step fails.

    Constraints:
    - **PRESERVE the env block at lines 161-165** exactly: `env:` with `TIDE_REQUIRE_CONTROLLER: "1"` and its Phase 02.1 D-03 cross-reference comment. This is the Phase 02.1 Plan 04 Task 1 commit `c6985eb` D-03 gate (T-02.2-04 mitigation).
    - **PRESERVE all other steps:** checkout, setup-go, install kind v0.31.0, install Helm v3.16.3, `Prepare manifests and envtest binaries`, `Collect kind cluster logs on failure`, `Upload kind logs artifact on failure`. None of these is affected.
    - **PRESERVE `timeout-minutes: 6` at line 158:** the CI job's outer fence is unchanged.
    - **DO NOT add new env vars, new steps, or new permission scopes.** This is a delete + comment-update edit only.
  </action>

  <verify>
    <automated><![CDATA[
      # YAML validates as parseable
      python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/ci.yaml')); print('YAML OK')" && \
      # The redundant DUR > 300 wall-time check is dropped
      [ "$(grep -c '\$DUR -gt 300' .github/workflows/ci.yaml)" -eq 0 ] && \
      [ "$(grep -c 'DUR=\$((END - START))' .github/workflows/ci.yaml)" -eq 0 ] && \
      [ "$(grep -c 'Integration test duration' .github/workflows/ci.yaml)" -eq 0 ] && \
      [ "$(grep -c 'TEST-02 violation' .github/workflows/ci.yaml)" -eq 0 ] && \
      # D-03 env block PRESERVED (T-02.2-04 mitigation)
      [ "$(grep -c 'TIDE_REQUIRE_CONTROLLER' .github/workflows/ci.yaml)" -ge 1 ] && \
      [ "$(grep -c 'TIDE_REQUIRE_CONTROLLER: \"1\"' .github/workflows/ci.yaml)" -eq 1 ] && \
      # CI fence preserved (timeout-minutes: 6 unchanged)
      [ "$(grep -c 'timeout-minutes: 6' .github/workflows/ci.yaml)" -ge 1 ] && \
      # The step still invokes make test-int (drops nothing critical)
      [ "$(grep -c 'time make test-int' .github/workflows/ci.yaml)" -eq 1 ] && \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **YAML parseable:** `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))"` exits 0
    - **Redundant DUR check dropped:** `grep -c '\$DUR -gt 300' .github/workflows/ci.yaml` returns 0 AND `grep -c 'DUR=\$((END - START))' .github/workflows/ci.yaml` returns 0 AND `grep -c 'Integration test duration' .github/workflows/ci.yaml` returns 0 AND `grep -c 'TEST-02 violation' .github/workflows/ci.yaml` returns 0
    - **T-02.2-04 mitigation (D-03 env block PRESERVED):** `grep -c 'TIDE_REQUIRE_CONTROLLER' .github/workflows/ci.yaml` returns ≥ 1 AND `grep -c 'TIDE_REQUIRE_CONTROLLER: "1"' .github/workflows/ci.yaml` returns exactly 1
    - **CI job fence preserved:** `grep -c 'timeout-minutes: 6' .github/workflows/ci.yaml` returns ≥ 1
    - **Step still invokes make test-int:** `grep -c 'time make test-int' .github/workflows/ci.yaml` returns exactly 1
  </acceptance_criteria>

  <done>
    `.github/workflows/ci.yaml` has the redundant outer `DUR > 300` wall-time check dropped. The D-03 `TIDE_REQUIRE_CONTROLLER=1` env block at lines 161-165 is preserved verbatim. The `timeout-minutes: 6` job-level fence is preserved. YAML parses cleanly. All grep gates above pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: End-to-End Runtime Verification — clean run + idempotent rerun + 02.2-01-VERIFICATION.md artifact with gate decision</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md (the BLOCKED gate template this artifact mirrors — frontmatter shape, Section 1 / Section 2 / Section 3 / Section 4 / Section 5 layout, per-spec outcome tables, root-cause analysis prose, threat-model-mitigations-applied list)
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md §"Validation Architecture" §"Phase Requirements → Test Map" (the four behaviors that must hold) and §"Wall-time budget validation"
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md §"Manual-Only Verifications" §"Sampling Rate" (the exact reproducer commands and gate criteria)
    - test/integration/kind/suite_test.go (the just-edited file from Task 1; must compile + vet before reproducer runs)
    - .github/workflows/ci.yaml (the just-edited file from Task 2; not exercised by the local reproducer but its post-edit shape is part of the gate decision rationale)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer that mirrors `02.1-04-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md` using the same structural template.

    **Step A — preflight self-check:** Confirm Task 1 and Task 2 are committed. Run `go build ./test/integration/kind/...`, `go vet ./test/integration/kind/...`, `gofmt -l test/integration/kind/suite_test.go`, and the YAML parse check from Task 2's verify. If any of these fail, halt and surface — do NOT proceed with the runtime reproducer.

    **Step B — Section 1 (Clean Start Run):** Execute the reproducer verbatim from `02.2-VALIDATION.md` §"Manual-Only Verifications" row 1, capturing into a temp log:

    ```bash
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block from the captured log — should show 7 Pass / 0 Skip / 0 Fail if the fix worked; transcribe each spec's outcome into a per-spec outcome table mirroring `02.1-04-VERIFICATION.md` §"Section 1" Per-Spec Outcome Table). Also capture the BeforeSuite detail block (kind connect timestamp, cert-manager install timestamp, helm install start + completion timestamps) — these are the evidence that fix #1 (kindTestTimeout bump) gave helm enough budget to finish.

    **Step C — Section 2 (Idempotent Re-run):** Without deleting the cluster from Section 1, execute the rerun reproducer:

    ```bash
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes. Verify the helm install on rerun succeeded due to `--replace` (fix #5 — search the captured log for `helm install completed` after `helm install tide`). Verify the cluster was NOT deleted by AfterSuite (T-02.2-02 mitigation — `KEEP_KIND_CLUSTER=true` short-circuit fired). Verify the AfterSuite zombie-cleanup code path was NOT exercised on this clean rerun (it only runs when `KEEP_KIND_CLUSTER` is unset AND `kind delete` fails — the path is exercised on the next non-KEEP run).

    **Step D — Section 3 (Before/After Comparison):** Write a 3-column comparison table: BEFORE-FIX (cite `02.1-04-VERIFICATION.md` numbers: 0/7 PASS clean, 0/7 NOT RUN rerun, 1187s + 39s walls); AFTER-FIX clean (this run); AFTER-FIX rerun (this run). Capture the failure-mode delta: what was broken vs what's now passing.

    **Step E — Section 4 (Wall-time Budget Check):** Table: clean wall vs 300s budget, rerun wall vs 300s budget. **CRITICAL:** the budget contract is `timeout 300s go test ...` from `Makefile:124`. Check `/tmp/02.2-clean-run.log` and `/tmp/02.2-rerun.log` for the `timeout` exit code 124. If `timeout 300s` did NOT fire, the budget is honored regardless of the total `make test-int` wall time (the outer wall includes unbounded `test-int-kind-prep`). The fix-#3 measurement-scope correction means the contract is now "go test wall ≤ 300s," not "total make wall ≤ 300s." Surface this distinction explicitly in the section.

    **Step F — Section 5 (Gate Decision):** Apply the criteria from `02.2-VALIDATION.md` §"Manual-Only Verifications":
    - Clean run exit 0 AND 7/7 specs PASS AND go test ≤ 300s → CONTRIBUTES to APPROVED
    - Rerun exit 0 AND 7/7 specs PASS AND go test ≤ 300s → CONTRIBUTES to APPROVED
    - All four contributions GREEN → `gate_decision: APPROVED`
    - Any single GATE FAIL → `gate_decision: BLOCKED` (with root-cause analysis prose)

    Write `gate_decision: APPROVED` or `gate_decision: BLOCKED` in the frontmatter and a matching prose summary at the head of Section 5.

    **Step G — Section 6 (Threat-Model Mitigations Applied):** For each Phase 02.2 threat from the plan's `threat_model_summary`:
    - **T-02.2-01 (TIDE_SIGNING_KEY leak):** `grep -c "TIDE_SIGNING_KEY" /tmp/02.2-clean-run.log` and `/tmp/02.2-rerun.log` (excluding the env-var reference text itself — verify no secret value bytes leak). Report counts.
    - **T-02.2-02 (KEEP_KIND_CLUSTER violation):** confirm the rerun did NOT delete the cluster (search rerun log for `KEEP_KIND_CLUSTER=true; keeping kind cluster`).
    - **T-02.2-03 (cert-manager v1.20 silent CRD-removal):** the clean run exercised the chart's webhook end-to-end; if it Ready'd, cert-manager v1.20.2 is compatible. Report the cert-manager install banner line from the clean-run log.
    - **T-02.2-04 (revert D-03 env block):** post-commit grep gate on `.github/workflows/ci.yaml` (`grep -c 'TIDE_REQUIRE_CONTROLLER'`).

    **Step H — frontmatter:** Write the artifact's frontmatter mirroring `02.1-04-VERIFICATION.md`:
    ```yaml
    ---
    phase: 02.2
    plan: 01
    task: 3
    artifact: verification
    captured_on: <ISO date>
    captured_by_host: <hostname>
    captured_by_branch: <git branch>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    gate_decision: APPROVED | BLOCKED
    ---
    ```

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md and the Phase 02.1 Plan 04 precedent — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`. Only the `.planning/phases/02.2-.../02.2-01-VERIFICATION.md` artifact is created. Document this explicitly in the artifact's final section ("What This Artifact Did Not Touch") mirroring `02.1-04-VERIFICATION.md`'s closing section.

    **If gate_decision = BLOCKED:** Write a Root-Cause Summary section with numbered analysis points (mirror `02.1-04-VERIFICATION.md` §"Root-Cause Summary"). Surface a fix landscape for a planner re-evaluation. Plan 02 (closeout) will NOT fire while the artifact says BLOCKED.

    **If gate_decision = APPROVED:** Skip the Root-Cause Summary section. Note in the closing prose that Plan 02 (closeout) is unblocked and can proceed.
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists and has the required frontmatter shape
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md ] && \
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 01' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -eq 1 ] && \
      # Gate decision is one of APPROVED or BLOCKED
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md && \
      # Required structural sections exist
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md && \
      grep -E '^rerun_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md && \
      # Threat-model mitigations applied section exists
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-04 mitigation referenced (CI YAML env block preserved)
      [ "$(grep -c 'TIDE_REQUIRE_CONTROLLER' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md)" -ge 1 ] && \
      # No production source modified by this task (artifact-only); verify git status shows only the artifact
      git status --porcelain | grep -v '^?? ' | grep -v 'VERIFICATION.md' | grep -v '^$' && echo "FAIL: non-artifact files modified" && exit 1 || \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md ]` returns true
    - **Frontmatter has required fields:** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 01' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' <artifact>` matches
    - **Section 1 (Clean Start Run), Section 2 (Idempotent Re-run), Section 5 (Gate Decision) all present:** each `grep -c '^## Section [125]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists:** `grep -c 'Threat-Model Mitigations Applied' <artifact>` returns ≥ 1
    - **T-02.2-04 (CI YAML env block preserved):** `grep -c 'TIDE_REQUIRE_CONTROLLER' <artifact>` returns ≥ 1
    - **Behavior gate (APPROVED case):** if `gate_decision: APPROVED`, then BOTH of: clean run captured `EXIT=0` AND 7 Layer B specs marked PASS in the per-spec table; rerun captured `EXIT=0` AND 7 Layer B specs marked PASS. AND neither captured log contains a `timeout 300s` exit code 124 occurrence on the go test step (verifiable from `/tmp/02.2-clean-run.log` and `/tmp/02.2-rerun.log` — both must be available for cross-reference at verification time but are not committed).
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, a `Root-Cause Summary` section MUST exist with numbered analysis points mirroring `02.1-04-VERIFICATION.md` §"Root-Cause Summary"
    - **No production source modified:** `git status --porcelain | grep -v '^?? '` shows only the new `02.2-01-VERIFICATION.md` artifact (and any matching `.planning/` files); no edits to `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `Makefile`, `.github/workflows/`. The source edits from Tasks 1 + 2 must be committed BEFORE this task runs.
  </acceptance_criteria>

  <done>
    `02.2-01-VERIFICATION.md` exists at the correct path with the structural template mirroring `02.1-04-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, and `gate_decision: APPROVED` or `gate_decision: BLOCKED`. Sections 1-5 captured with per-spec outcome tables and root-cause analysis (if BLOCKED). All four threat-model mitigations (T-02.2-01 through T-02.2-04) addressed with concrete evidence. No production source files modified by this task. If APPROVED, Plan 02 closeout can fire; if BLOCKED, Plan 02 stays gated and the failure is surfaced for a planner re-evaluation per the wave-failure-semantics contract.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/integration/kind/suite_test.go` → `kind` / `docker` / `helm` / `kubectl` subprocesses | Test process shells out to OS-level binaries; subprocess stdout/stderr flows into Ginkgo's log buffer and may be captured into the verification artifact |
| `.github/workflows/ci.yaml` env block → CI shell environment | Phase 02.1 D-03 `TIDE_REQUIRE_CONTROLLER=1` flows from workflow env to test process; T-02.2-04 protects against accidental revert |
| `cert-manager.io/v1` Issuer + Certificate APIs → cert-manager controller | TIDE's chart consumes cert-manager via stable v1 APIs; the v1.16.2 → v1.20.2 bump is verified against this contract |
| AfterSuite `docker rm -f` against `io.x-k8s.kind.cluster=tide-test`-labeled containers → other kind clusters on host | Label filter is exact-match; other kind clusters (e.g., `tide-test-e2e`) have different labels and are unaffected by cleanup |
| Captured verification log (`/tmp/02.2-clean-run.log`, `/tmp/02.2-rerun.log`) → `02.2-01-VERIFICATION.md` artifact | Helm install output may inadvertently echo Secret data values; T-02.2-01 disposition is `accept` because Helm 3 does not echo Secret data by default + chart's signing-key Secret is rendered server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-01 | Information Disclosure | Captured CI/verification logs may inadvertently echo `TIDE_SIGNING_KEY` | accept | Secret value is rendered server-side by the chart (`charts/tide/templates/signing-secret.yaml` uses `randAlphaNum 64`); Helm 3 does not print Secret `data:` values in install stdout. Workflow's `env:` block adds only `TIDE_REQUIRE_CONTROLLER: "1"` — a flag, not a secret. Verification artifact's Section 6 explicitly greps `/tmp/02.2-*.log` for `TIDE_SIGNING_KEY` to confirm zero leaks; if any are found, the gate decision is BLOCKED with redaction guidance. |
| T-02.2-02 | Tampering | AfterSuite zombie-cleanup helper might delete a kind cluster the developer wanted preserved (`KEEP_KIND_CLUSTER=true`) | mitigate | The short-circuit `if os.Getenv("KEEP_KIND_CLUSTER") == "true" { return }` MUST stay BEFORE the cleanup helper call. Task 1 acceptance criterion explicitly verifies `grep -c 'KEEP_KIND_CLUSTER' suite_test.go ≥ 1` and the structural ordering: cancel() → env-var short-circuit → By(...) → cleanupKindCluster(). The cleanup helper uses an exact-match label filter `io.x-k8s.kind.cluster=tide-test` — other kind clusters (e.g., `tide-test-e2e` from `Makefile:93`) have different labels and are not touched. |
| T-02.2-03 | Denial of Service | cert-manager v1.20 silent CRD-removal could break the chart's webhook Certificate at install time | mitigate | RESEARCH §"Pattern 4" audit confirms v1.20 maintains `cert-manager.io/v1` Issuer + Certificate as stable; no CRD removals in 1.16→1.20 affect TIDE's usage. The chart's two Certificate resources (`serving-cert.yaml`, `metrics-certs.yaml`) both specify `issuerRef.kind: Issuer` explicitly, surviving the v1.20 issuerRef-defaults revert. Task 3's clean-run reproducer exercises the chart's webhook end-to-end against v1.20.2 — if the bump breaks anything, the gate decision is BLOCKED with a `cert-manager v1.20.2 compatibility` root cause documented. |
| T-02.2-04 | Tampering | Accidentally revert Phase 02.1 D-03 `TIDE_REQUIRE_CONTROLLER=1` env block when editing `.github/workflows/ci.yaml` for fix #3 | mitigate | Task 2's acceptance criteria include TWO explicit grep gates on the post-edit YAML: `grep -c 'TIDE_REQUIRE_CONTROLLER' .github/workflows/ci.yaml ≥ 1` AND `grep -c 'TIDE_REQUIRE_CONTROLLER: "1"' .github/workflows/ci.yaml = 1`. The line range for fix #3 (drop DUR check at lines 200-211) is separate from the D-03 env block (lines 161-165) — the targeted delete preserves the env block. Task 3's verification artifact also greps the post-commit YAML to confirm the env block survived. |

</threat_model>

<verification>
After all three tasks complete, the overall plan verification:

**Source-shape gates (cumulative from Tasks 1 + 2):**
- `go build ./test/integration/kind/...` exits 0
- `go vet ./test/integration/kind/...` exits 0
- `gofmt -l test/integration/kind/suite_test.go` produces empty output
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))"` exits 0
- All grep gates from Tasks 1 + 2 pass

**Phase 1 import firewalls remain green:**
- `make verify-dag-imports` exits 0
- `make verify-dispatch-imports` exits 0
- `make verify-import-firewall` exits 0

**Verification artifact (Task 3):**
- `.planning/phases/02.2-.../02.2-01-VERIFICATION.md` exists with required frontmatter
- `gate_decision` is one of `APPROVED` or `BLOCKED`
- All runtime evidence fields populated (exit codes, wall times, spec counts)
</verification>

<success_criteria>
- All three tasks committed atomically
- `02.2-01-VERIFICATION.md` artifact captures runtime reality
- If `gate_decision: APPROVED`: 7/7 Layer B specs PASS on clean run AND 7/7 PASS on `KEEP_KIND_CLUSTER=true` rerun; `timeout 300s go test` does NOT fire on either run; all four threat-model mitigations verified with concrete evidence
- If `gate_decision: BLOCKED`: Root-Cause Summary section documents which fix(es) did not have the expected runtime effect; Plan 02 (closeout) stays gated; failure routes to planner re-evaluation per the wave-failure-semantics contract
- Plan 02 (`02.2-02-PLAN.md`) is unblocked iff `gate_decision: APPROVED` is captured in this plan's verification artifact
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-SUMMARY.md` documenting:
- The four source-shape fixes landed (kindTestTimeout, AfterSuite zombie cleanup, cert-manager bump, helm --replace; plus the CI YAML DUR-check drop)
- The verification artifact's gate decision (APPROVED or BLOCKED)
- Per-spec outcomes from the clean run + rerun
- Whether Plan 02 closeout is unblocked
- Any deviations from the locked decisions (should be zero; this is a tactical debug closeout)
- Threat-model mitigations applied (T-02.2-01 through T-02.2-04) with concrete evidence
</output>
</content>
</invoke>