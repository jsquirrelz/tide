---
phase: "02.2"
plan: "08"
subsystem: "test-integration"
tags: [gap-closure, debug, test-fixture, spec-flake, runtime-verification, kind-integration, phase-02.2, tactical, cascade-6, cascade-7, eventually-timeout-bump, harness-bug, blocked]
dependency_graph:
  requires: ["02.2-07"]
  provides: []
  affects: ["02.2-02"]
tech_stack:
  added: []
  patterns: [Eventually-timeout-bump, pre-post-edit-count-assertion, T-02.2-22-mitigation]
key_files:
  created:
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md
  modified:
    - test/integration/kind/credproxy_test.go
    - test/integration/kind/failure_test.go
decisions:
  - "T-02.2-22 mitigation applied: lambda-body context used to identify target Eventually calls (not line numbers); pre/post grep count assertions confirmed exactly one replacement per file"
  - "Task 3 DOES NOT FIRE — gate_decision: BLOCKED; Plan 02.2-02 stays gated on 02.2-01-VERIFICATION.md (6th consecutive Task 3 non-fire in Phase 02.2 cascade chain)"
  - "dispatch latency marginal flag fired: credproxy pod-wait consumed full 120s budget — confounded by caps-test controller pressure"
  - "cascade-7 classified as harness-bug (missing Project hierarchy in caps_test.go, output_test.go, failure_test.go — same class as cascade-5)"
metrics:
  duration: "~25 min (T1: 2 min, T2: 20 min, T3: not fired)"
  completed: "2026-05-14"
  tasks_completed: 2
  files_modified: 3
---

# Phase 02.2 Plan 08: Eventually Timeout Bumps (Cascade-6 Closure Attempt) Summary

**One-liner:** Two Eventually timeout bumps (credproxy 60s→120s, AC3 2m→240s) applied to test-only files; BLOCKED on cascade-7 harness-bug — caps/output/failure fixtures missing Project hierarchy (same root cause as cascade-5).

## What Was Built

### Task 1 — Eventually Timeout Bumps (COMPLETED, commit `e29e770`)

Applied two single-token replacements in test-only files, per cascade-6 specification from `02.2-07-VERIFICATION.md` §Section 5 Option C:

**Fix A — `test/integration/kind/credproxy_test.go`:**
- Changed pod-wait `Eventually` timeout from `60*time.Second` → `120*time.Second`
- Target identified by lambda body (captures `podName = p.Name; return true`) and assertion message ("A Pod should be created for the credproxy-task Job")
- T-02.2-22 mitigation: pre-edit count = 3, post-edit count = 2 (exactly one replacement)
- Other two 60s Eventually calls (sidecar presence check, log check) unchanged

**Fix B — `test/integration/kind/failure_test.go`:**
- Changed AC3 beta-fail phase-wait `Eventually` timeout from `2*time.Minute` → `240*time.Second`
- Target identified by lambda body (returns `t.Status.Phase` for `beta-fail`, asserts `Equal("Failed")`) and comment "Wait for β to fail"
- T-02.2-22 mitigation: pre-edit count = 2, post-edit count = 1 (exactly one replacement)
- alpha-fail Succeeded-phase Eventually (second `2*time.Minute` call at line 140) unchanged

**Option B contract verified:** Zero modifications to `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `Makefile`, or `test/integration/kind/suite_test.go`. All 8 regression guards pass. `go build ./test/integration/kind/...` exit 0. `go vet ./test/integration/kind/...` exit 0.

### Task 2 — Runtime Verification (COMPLETED — BLOCKED)

Full end-to-end kind integration test run executed. Results captured in `02.2-08-VERIFICATION.md`.

**Clean run:**
- Exit: 1, wall: 303s
- 2 specs FAIL (caps AC5 hang-task at 75s, credproxy HARN-03 at 136s), 5 NOT RUN (panic after 5m Ginkgo inner timeout)
- Manager Pod: Running 0 restarts, PVC: Bound RWO, no flag errors

**Key findings:**
1. Cascade-6 timeout bumps were applied but the credproxy 120s budget was consumed entirely — dispatch takes > 120s (ResourceVersion conflict retry more expensive than estimated)
2. `caps_test.go` AC5 reveals cascade-7: `caps-test` namespace has Plan+Task but no Project hierarchy → same `resolveProject` gate as cascade-5. No manager log entries for `hang-task` (controller fails at resolveProject and requeues without creating Job)
3. `output_test.go` is in the same class (not reached due to panic, but will fail identically)
4. `failure_test.go` AC3 is in the same class — AC3's 240s budget was not reached (panic first), but the spec will also fail at `resolveProject` for `alpha-fail`, `beta-fail`, `gamma-fail` tasks

**T-02.2-21 marginal flag fired:** `credproxy_pod_wait_observed_seconds: 120` (≥ 90s threshold). `dispatch latency marginal` token recorded in VERIFICATION.md §Section 1 and closing prose. Note: measurement confounded by caps-test controller work-queue pressure.

### Task 3 — Plan 02.2-02 Rewire (NOT FIRED)

`gate_decision: BLOCKED` → Task 3 does not execute. Plan 02.2-02 continues to reference `02.2-01-VERIFICATION.md` at all 31 read paths. This is the 6th consecutive Task 3 non-fire in Phase 02.2's cascade chain (Plans 02.2-03 T4, 02.2-04 T3, 02.2-05 T3, 02.2-06 T3, 02.2-07 T3, 02.2-08 T3 all BLOCKED).

## Deviations from Plan

### Auto-documented Issues

**1. [Rule 1 - Discovery] Cascade-6 timeout bumps insufficient for credproxy (120s still too tight)**
- **Found during:** Task 2 clean run
- **Issue:** The 120s credproxy pod-wait budget (bumped from 60s in Task 1) was consumed entirely. `credproxy-task` dispatch takes > 120s end-to-end with the ResourceVersion conflict retry. The `02.2-07-VERIFICATION.md §Section 5` estimate of "retry adds ~10-30s" was optimistic — actual overhead is > 60s additional.
- **Classification:** Confounded measurement — caps-test controller pressure may extend credproxy dispatch time. Re-measurement needed after caps/output/failure hierarchy gaps are closed.
- **Action taken:** Documented in VERIFICATION.md; `dispatch latency marginal` flag fired per T-02.2-21.

**2. [Rule 1 - Discovery] Cascade-7: caps_test.go, output_test.go, failure_test.go missing Project hierarchy**
- **Found during:** Task 2 clean run (caps_test.go AC5 spec failure)
- **Issue:** Three test files use `applyYAML` without `applyHierarchy`:
  - `caps_test.go` creates `cap-plan`/`hang-task` in `caps-test` — no Project
  - `output_test.go` creates Plan/Task in `output-test` — no Project
  - `failure_test.go` creates `fail-plan`/alpha-fail/beta-fail/gamma-fail in `failure-test` — no Project
  - All three fail at `resolveProject(ctx, task)` at `task_controller.go:217`: "no project found in namespace X"
- **Classification:** `harness-bug` per T-02.2-16 taxonomy (same class as cascade-5)
- **Action taken:** Documented in VERIFICATION.md §Section 5. Plan 02.2-09 fix shape proposed: `createProjectHierarchy(ctx, ns)` helper that adds Project+Milestone+Phase+Secret to a namespace, called from each of the three test files before their `applyYAML` calls. No modifications to production source required.

## Threat Flags

None. The VERIFICATION.md clean run confirms: Manager Pod Running 0 restarts, PVC Bound RWO, no `flag provided but not defined` errors, no `no project found` for previously-fixed fixtures. No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| T1 | `e29e770` | `test(02.2-08): bump Eventually timeouts for cascade-6 spec-flake (T1)` |
| T2 | (this commit) | `docs(02.2-08): complete cascade-7 BLOCKED verification + summary` |
| T3 | NOT FIRED | Gate BLOCKED |

## Cascade Chain Status

| Cascade | Class | Status |
|---------|-------|--------|
| cascade-1 (PVC RWX→RWO) | infra | CLOSED — Plan 02.2-03 |
| cascade-2 (metrics-bind-address) | flag-mismatch | CLOSED — Plan 02.2-04 |
| cascade-3 (webhook-cert-path) | flag-mismatch | CLOSED — Plan 02.2-05 |
| cascade-4 (test-budget 300s→600s) | budget-vs-actual | CLOSED — Plan 02.2-06 |
| cascade-5 (credproxy fixture hierarchy) | harness-bug | CLOSED — Plan 02.2-07 T1 |
| cascade-6 (Eventually timeout budgets) | spec-flake | PARTIALLY CLOSED (timeouts applied; credproxy 120s confounded) |
| cascade-7 (caps/output/failure fixture hierarchy) | harness-bug | **OPEN — Plan 02.2-09 target** |

## Next Action

Author and dispatch **Plan 02.2-09** with:
- **Task 1 (source edit):** Create `createProjectHierarchy(ctx context.Context, ns string) error` helper in `test/integration/kind/suite_test.go` that applies Project+Milestone+Phase+Secret in the given namespace (mirrors `applyHierarchy`'s hierarchy fragment, parameterized by `ns`). Update `caps_test.go`, `output_test.go`, and `failure_test.go` to call `createProjectHierarchy(ctx, capsNS/outputNS/failNS)` before their existing `applyYAML` calls.
- **Task 2 (runtime verification):** `02.2-09-VERIFICATION.md` with APPROVED gate (7/7 specs PASS on clean + rerun). Re-measure `credproxy_pod_wait_observed_seconds` after caps-test pressure is removed.
- **Task 3 (gated rewire):** If APPROVED, rewire `02.2-02-PLAN.md` → `depends_on: ["02.2-09"]` + 31 refs to `02.2-09-VERIFICATION.md`.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| `02.2-08-VERIFICATION.md` exists | FOUND |
| `02.2-08-SUMMARY.md` exists | FOUND |
| `credproxy_test.go` modified | FOUND |
| `failure_test.go` modified | FOUND |
| Task 1 commit `e29e770` exists | FOUND |
| `gate_decision: BLOCKED` in VERIFICATION.md | CONFIRMED |
| `credproxy_pod_wait_observed_seconds: 120` | CONFIRMED |
| `ac3_phase_wait_observed_seconds: -1` | CONFIRMED |
| `02.2-02-PLAN.md` depends_on still `["02.2-01"]` (Task 3 NOT fired) | CONFIRMED |
