---
phase: 09-cross-namespace-envelope-return-in-namespace-reporter
plan: 05
type: execute
wave: 2
depends_on: [09-04]
files_modified:
  - cmd/tide-reporter/main.go
  - cmd/tide-reporter/main_test.go
  - images/tide-reporter/Dockerfile
  - Makefile
  - test/integration/envtest/reporter_materialize_test.go
autonomous: true
requirements: [REQ-09-01]
must_haves:
  truths:
    - "cmd/tide-reporter reads out.json from the LOCAL (same-namespace) PVC and creates child CRs via the K8s API"
    - "The reporter resolves the parent by name to obtain its live UID, sets the same-namespace ownerRef, and is idempotent on re-run (spec-ref guard)"
    - "The reporter uses an in-cluster controller-runtime client (in-cluster SA token) with the tide scheme registered"
    - "A tide-reporter image builds and loads into the kind test cluster"
  artifacts:
    - path: "cmd/tide-reporter/main.go"
      provides: "Reporter binary: flag parse → testable run() seam → in-cluster client → read out.json → materialize"
      contains: "func run"
    - path: "images/tide-reporter/Dockerfile"
      provides: "distroless static image for the reporter binary"
      contains: "tide-reporter"
    - path: "test/integration/envtest/reporter_materialize_test.go"
      provides: "envtest: reporter create logic → child CR appears with ownerRef + specRef; idempotent re-run"
  key_links:
    - from: "cmd/tide-reporter/main.go"
      to: "internal/reporter.MaterializeChildCRDs"
      via: "run() unmarshals out.json then calls the lifted materializer with the resolved parent"
      pattern: "reporter.MaterializeChildCRDs"
    - from: "cmd/tide-reporter/main.go"
      to: "controller-runtime client"
      via: "config.GetConfig() + client.New(cfg, Options{Scheme}) honoring the in-cluster tide-reporter SA token"
      pattern: "config.GetConfig"
---

<objective>
Build the `cmd/tide-reporter` Go binary (the Option-C reader Job's program): it mounts the project-namespace PVC at `/workspace` (subPath `{project-uid}/workspace`), reads `out.json` from `/workspace/envelopes/<taskUID>/out.json` — a SAME-namespace read, the #11/#12 fix — resolves the parent CR by name to get its live UID, and creates the child CRs via an in-cluster controller-runtime client using the least-privilege `tide-reporter` SA (provisioned in plan 09-04). Idempotent by the lifted spec-ref guard. Ship its image and wire kind preload.

Purpose: cross-namespace control data (child CRDs) now flows through the K8s API from a trusted same-namespace identity, never a cross-namespace PVC read.
Output: `cmd/tide-reporter` (+ test); `images/tide-reporter/Dockerfile`; Makefile build/load; envtest materialize test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-04-SUMMARY.md

<interfaces>
From internal/reporter (plan 09-04):
  func MaterializeChildCRDs(ctx, c client.Client, scheme *runtime.Scheme, parent metav1.Object, children []pkgdispatch.ChildCRDSpec) error
  func ChildrenAlreadyMaterialized(ctx, c client.Client, parent metav1.Object) (bool, error)
From pkg/dispatch/envelope.go — EnvelopeOut.ChildCRDs []ChildCRDSpec (the field the reporter reads from out.json).
cmd-main testable seam (mirror cmd/tide-push/main.go:79-201): a by-value cfg struct, signal.NotifyContext(SIGTERM,SIGINT),
  os.Exit(run(ctx, cfg, stdout, stderr)). Exit-code const block (0 success, 2 invariant/bad-args).
In-cluster client (mirror cmd/manager/main.go:32-45): runtime.NewScheme() + clientgoscheme.AddToScheme +
  tidev1alpha1.AddToScheme; sigs.k8s.io/controller-runtime/pkg/client/config.GetConfig(); client.New(cfg, client.Options{Scheme}).
PVC layout: reader mounts subPath {project-uid}/workspace at /workspace, so out.json is at /workspace/envelopes/<taskUID>/out.json
  (SAME-namespace; NOT the Manager's /workspaces/{uid}/workspace layout).
Image/build pattern: images/tide-push/Dockerfile (distroless/static:nonroot, CGO_ENABLED=0, ldflags -s -w);
  Makefile test-int-kind-prep builds + `kind load docker-image` per image (lines ~155-169).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cmd/tide-reporter binary — read out.json, resolve parent, materialize</name>
  <read_first>
    - cmd/tide-push/main.go:79-201 (cfg struct + main + run() seam + exit-code consts to mirror)
    - cmd/manager/main.go:28-50 (scheme + in-cluster client wiring to mirror)
    - internal/dispatch/podjob/backend.go:94-105 (FilesystemEnvelopeReader.ReadOut — out.json read shape)
    - internal/reporter/materialize.go (plan 09-04 — MaterializeChildCRDs + ChildrenAlreadyMaterialized signatures)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "cmd/tide-reporter/main.go (NEW — cmd-main / reporter)")
  </read_first>
  <behavior>
    - run() with a fake client and a temp workspace containing out.json (with N childCRDs) creates N child CRs with the same-namespace ownerRef and the spec-parent-ref set
    - a second run() against the same parent + children is idempotent (no duplicate children; ChildrenAlreadyMaterialized short-circuits, or AlreadyExists is tolerated)
    - missing out.json → non-zero exit with a clear error (no partial create)
    - a child declaring a disallowed Kind → non-zero exit (allowlist enforced by the materializer), no children created
    - parent-by-name Get failure → non-zero exit (cannot resolve ownerRef)
  </behavior>
  <action>
    Create cmd/tide-reporter/main.go in package main. Mirror cmd/tide-push's cfg-by-value + run(ctx, cfg, stdout, stderr) int seam. Flags: --workspace (default /workspace), --project-uid, --task-uid (keys the out.json path), --parent-name, --parent-namespace, --parent-kind. main() parses flags, signal.NotifyContext(SIGTERM,SIGINT), os.Exit(run(...)). In run(): build the in-cluster client per cmd/manager wiring (config.GetConfig() + client.New with clientgoscheme + tidev1alpha1 registered) — accept an injectable client.Client in run() so tests pass a fake. Read out.json from filepath.Join(workspace, "envelopes", taskUID, "out.json"); unmarshal into pkgdispatch.EnvelopeOut. Resolve the parent: switch on --parent-kind to the typed object, c.Get by {parent-namespace, parent-name} to obtain the live UID. Call reporter.ChildrenAlreadyMaterialized(ctx, c, parent); if not already, call reporter.MaterializeChildCRDs(ctx, c, scheme, parent, out.ChildCRDs). Define an exit-code const block (0 success, 2 bad-args/invariant, 1 generic). The reporter Job runs as the tide-reporter SA (09-04), so the in-cluster token carries create+get on the 5 Kinds. The reporter does NOT write usage/git status — that rides the dispatch Job's termination message (CONTEXT line 49); the reporter's only K8s output is the created CRs.
  </action>
  <verify>
    <automated>go test ./cmd/tide-reporter/... -count=1 && go build ./cmd/tide-reporter</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func run' cmd/tide-reporter/main.go` and `grep -q 'config.GetConfig' cmd/tide-reporter/main.go` succeed
    - `grep -q 'reporter.MaterializeChildCRDs' cmd/tide-reporter/main.go` succeeds
    - main_test.go drives run() with a fake client (no os.Args) covering the <behavior> cases
    - `go build ./cmd/tide-reporter` succeeds; `make verify-import-firewall` still passes (reporter imports no LLM SDK)
  </acceptance_criteria>
  <done>The reporter binary reads out.json locally, resolves the parent, materializes children idempotently with same-namespace ownerRefs, and exits with a classifiable code.</done>
</task>

<task type="auto">
  <name>Task 2: tide-reporter image + kind preload wiring</name>
  <read_first>
    - images/tide-push/Dockerfile (distroless/static:nonroot, multi-stage static build to mirror)
    - Makefile:155-169 (test-int-kind-prep build+load block) and :248-254 (docker-build)
  </read_first>
  <action>
    Create images/tide-reporter/Dockerfile mirroring images/tide-push/Dockerfile: golang:1.26-alpine builder, COPY go.mod/go.sum + the packages cmd/tide-reporter needs (cmd/tide-reporter/, internal/reporter/, internal/owner/, api/v1alpha1/, pkg/dispatch/ — copy whatever the build requires; verify with a local build), CGO_ENABLED=0, ldflags -s -w, output /out/tide-reporter; runtime stage gcr.io/distroless/static:nonroot, ENTRYPOINT /usr/local/bin/tide-reporter. Add a build line `$(CONTAINER_TOOL) build -t ghcr.io/jsquirrelz/tide-reporter:test -f images/tide-reporter/Dockerfile .` and a `$(KIND) load docker-image ghcr.io/jsquirrelz/tide-reporter:test --name tide-test` line to Makefile test-int-kind-prep (alongside the tide-push lines), and update the target's doc comment to mention tide-reporter.
  </action>
  <verify>
    <automated>docker build -t ghcr.io/jsquirrelz/tide-reporter:test -f images/tide-reporter/Dockerfile . >/dev/null 2>&1; echo "build exit=$?" && grep -q 'tide-reporter' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - images/tide-reporter/Dockerfile builds a static nonroot binary (docker build succeeds)
    - Makefile test-int-kind-prep builds AND `kind load`s ghcr.io/jsquirrelz/tide-reporter:test
    - the image entrypoint is /usr/local/bin/tide-reporter
  </acceptance_criteria>
  <done>The reporter image builds reproducibly and is preloaded into the kind test cluster for Layer B.</done>
</task>

<task type="auto">
  <name>Task 3: envtest — reporter create logic produces child CR with ownerRef + specRef</name>
  <read_first>
    - test/integration/envtest/ (existing envtest suite setup + an existing materialize/admission test for the pattern)
    - internal/reporter/materialize_test.go (plan 09-04 — the unit-level shape; this is the envtest cross-ns-create integration level)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md (Validation Architecture: SC-1 envtest reporter_materialize_test.go)
  </read_first>
  <action>
    Add test/integration/envtest/reporter_materialize_test.go: against the envtest apiserver, apply a parent (e.g. a Milestone) in a test namespace, run the reporter's materialize path (call cmd/tide-reporter run() with a real envtest-backed client OR invoke reporter.MaterializeChildCRDs directly with the envtest client) using a fixture out.json carrying a child (e.g. a Phase). Assert the child CR exists in the same namespace with a controller ownerRef to the parent AND the spec-parent-ref set. Add a second-run assertion: re-invoking does not create a duplicate (spec-ref guard). Reuse the existing envtest BeforeSuite/scheme. Use a fixture out.json exercising BOTH the stub-authored and real-authored shapes (both populate EnvelopeOut.ChildCRDs identically per RESEARCH §Stub Compatibility).
  </action>
  <verify>
    <automated>make test-int-fast 2>&1 | tail -20; grep -q 'reporter_materialize' test/integration/envtest/reporter_materialize_test.go</automated>
  </verify>
  <acceptance_criteria>
    - test/integration/envtest/reporter_materialize_test.go exists and runs under the envtest suite
    - it asserts: child CR created in the parent's namespace, controller ownerRef present, spec-parent-ref set
    - it asserts idempotency on a second materialize (no duplicate child)
    - `make test-int-fast` passes (Layer A envtest, no Docker)
  </acceptance_criteria>
  <done>envtest proves the reporter's create logic produces a correctly-owned, spec-ref-stamped child CR and is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reporter Job → apiserver | reporter creates child CRs in the tenant namespace using the tide-reporter SA token (least-privilege, plan 09-04) |
| out.json (subagent-written) → reporter | the reporter trusts out.json content only as child-CRD declarations, gated by the allowlist + CEL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Spoofing | reporter Job impersonates the operator's broad verbs | mitigate | reporter runs as tide-reporter SA (create+get on 5 Kinds only), NOT the Manager ClusterRole binding |
| T-09-11 | Tampering | poisoned out.json declares disallowed Kind / spurious subtree | mitigate | childKindAllowlist (in the materializer) + spec-ref idempotency guard + CEL admission at create |
| T-09-12 | Elevation of privilege | sandboxed subagent reaching the apiserver | mitigate | reporter is a SEPARATE Job/pod with its own SA (Option C); the subagent stays on the zero-verb tide-subagent SA |
</threat_model>

<verification>
- `go test ./cmd/tide-reporter/... -count=1 && go build ./cmd/tide-reporter` green
- `docker build -f images/tide-reporter/Dockerfile .` succeeds
- `make test-int-fast` green (envtest reporter materialize)
- `make verify-import-firewall` still green
</verification>

<success_criteria>
REQ-09-01 (binary): the reader Job binary reads out.json same-namespace and creates child CRs via the K8s API with the least-privilege SA, idempotently, with same-namespace ownerRefs; image + kind preload wired; envtest proves cross-ns child creation.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-05-SUMMARY.md`
</output>
