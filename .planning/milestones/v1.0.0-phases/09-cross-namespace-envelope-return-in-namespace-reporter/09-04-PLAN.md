---
phase: 09-cross-namespace-envelope-return-in-namespace-reporter
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/reporter/materialize.go
  - internal/reporter/materialize_test.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/dispatch_helpers_test.go
  - hack/helm/reporter-rbac.yaml
  - hack/helm/augment-tide-chart.sh
  - charts/tide/templates/reporter-rbac.yaml
  - examples/projects/medium/per-namespace-resources.yaml
autonomous: true
requirements: [REQ-09-01]
must_haves:
  truths:
    - "MaterializeChildCRDs + childrenAlreadyMaterialized + childKindAllowlist live in internal/reporter (importable by cmd/tide-reporter)"
    - "The spec-parent-ref idempotency guard (NOT ownerRef) moves WITH materialization to the create-site"
    - "A least-privilege tide-reporter SA + Role (create+get on the 5 child Kinds only) + RoleBinding renders per project namespace"
    - "The medium sample's per-namespace-resources.yaml provisions tide-reporter SA+Role+RoleBinding"
  artifacts:
    - path: "internal/reporter/materialize.go"
      provides: "MaterializeChildCRDs, childrenAlreadyMaterialized, childKindAllowlist (lifted)"
      contains: "func MaterializeChildCRDs"
    - path: "hack/helm/reporter-rbac.yaml"
      provides: "SOT template for tide-reporter SA+Role+RoleBinding"
      contains: "tide-reporter"
    - path: "examples/projects/medium/per-namespace-resources.yaml"
      provides: "tide-reporter SA+Role+RoleBinding in tide-sample-medium"
      contains: "tide-reporter"
  key_links:
    - from: "internal/reporter/materialize.go"
      to: "internal/owner.EnsureOwnerRef"
      via: "MaterializeChildCRDs calls EnsureOwnerRef (same-namespace, race-free specRef guard)"
      pattern: "EnsureOwnerRef"
    - from: "charts/tide/templates/reporter-rbac.yaml"
      to: ".Values.projectNamespaces"
      via: "range fan-out renders the SA+Role+RoleBinding per project namespace"
      pattern: "range.*projectNamespaces"
---

<objective>
Lift the child-CRD materialization machinery out of the Manager into a new `internal/reporter` package the `cmd/tide-reporter` binary (plan 09-05) can import, and provision the least-privilege `tide-reporter` ServiceAccount + RBAC in both the chart (SOT) and the medium sample. This is the foundation wave-1 work for the Option-C reader Job. The cascade-9/10/11 spec-parent-ref idempotency guard MOVES WITH materialization (Pitfall 3: the guard must live at the create-site, NOT stay in the Manager).

Purpose: the reader Job (09-05) is the create-site; materialization + its guard + the Kind allowlist belong there.
Output: `internal/reporter` package (+ moved tests); `hack/helm/reporter-rbac.yaml` (SOT) + augment copy + rendered template; medium sample RBAC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md

<interfaces>
From internal/controller/dispatch_helpers.go — the three movers (lift verbatim):
  func MaterializeChildCRDs(ctx, c client.Client, scheme *runtime.Scheme, parent metav1.Object, children []pkgdispatch.ChildCRDSpec) error  (:304-377)
  func childrenAlreadyMaterialized(ctx, c client.Client, parent metav1.Object) (bool, error)  (:236-287) — matches by Spec.{Project,Milestone,Phase,Plan}Ref, IsControlledBy fallback
  var childKindAllowlist map[string]bool  (:80-86)
  The Task case stamps tk.Spec.PromptPath = child.SourcePath (:341-349) — moves with it.
From internal/owner/owner.go:50 — EnsureOwnerRef(child, parent metav1.Object, scheme) — imports only metav1 + runtime + controllerutil (import-safe from cmd; enforces same-namespace, Pitfall 23).
Chart SOT mechanism: hack/helm/<name>.yaml is the SOURCE; hack/helm/augment-tide-chart.sh does `cp ${HACK_DIR}/<name>.yaml ${CHART_DIR}/templates/<name>.yaml`. Never hand-edit charts/tide/templates/ directly — author hack/helm + add a cp line (mirror push-rbac.yaml at augment line ~119).
RBAC analogs: hack/helm/push-rbac.yaml (SA+Role+RoleBinding triple) + charts/tide/templates/per-namespace-rolebinding.yaml (`{{- range $ns := .Values.projectNamespaces }}` fan-out, namespace: {{ $ns }}).
Medium sample analog: examples/projects/medium/per-namespace-resources.yaml tide-push block (SA+Role+RoleBinding in tide-sample-medium).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift materialization + guard + allowlist into internal/reporter</name>
  <read_first>
    - internal/controller/dispatch_helpers.go:80-86 (childKindAllowlist), :236-287 (childrenAlreadyMaterialized), :304-377 (MaterializeChildCRDs)
    - internal/controller/dispatch_helpers_test.go:221-380 (the MaterializeChildCRDs tests to move)
    - internal/owner/owner.go:50-64 (EnsureOwnerRef — import-safe)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "internal/reporter/materialize.go (NEW — utility, lifted from controller)")
  </read_first>
  <action>
    Create package internal/reporter with materialize.go. Move MaterializeChildCRDs, childrenAlreadyMaterialized, and childKindAllowlist VERBATIM from internal/controller/dispatch_helpers.go (export childrenAlreadyMaterialized as ChildrenAlreadyMaterialized since cmd/tide-reporter will call it; keep MaterializeChildCRDs exported). The Task-prompt wiring (tk.Spec.PromptPath = child.SourcePath) moves with the Task case. The cascade-9/10/11 spec-parent-ref guard is unchanged: match by Spec.{Project,Milestone,Phase,Plan}Ref with metav1.IsControlledBy fallback — NEVER ownerRef alone (the comment block at dispatch_helpers.go:218-235 moves too). Use internal/owner.EnsureOwnerRef. Imports needed: api/v1alpha1, pkg/dispatch, internal/owner, controller-runtime client, metav1, runtime, apierrors, encoding/json — all import-safe (no internal/controller back-edge; verify no import cycle). Remove the three definitions from dispatch_helpers.go and update the four controllers' call sites to reference internal/reporter (or leave thin wrappers IF needed to keep this plan's controller diff minimal — but the inline calls in handle*JobCompletion are dropped entirely in plan 09-06, so prefer: keep dispatch_helpers.go compiling by importing internal/reporter for any remaining caller). Move the corresponding tests from dispatch_helpers_test.go into internal/reporter/materialize_test.go.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/reporter/... ./internal/controller/... -short -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func MaterializeChildCRDs' internal/reporter/materialize.go` succeeds
    - `grep -q 'ChildrenAlreadyMaterialized\|childrenAlreadyMaterialized' internal/reporter/materialize.go` succeeds; the guard matches Spec.*Ref (grep `Spec.ProjectRef\|Spec.MilestoneRef\|Spec.PhaseRef\|Spec.PlanRef`)
    - `grep -c 'func MaterializeChildCRDs' internal/controller/dispatch_helpers.go` returns 0 (moved out)
    - the moved tests run in internal/reporter and pass (happy path, unknown-kind reject, AlreadyExists idempotent)
    - `go build ./...` succeeds (no import cycle); `make verify-import-firewall` still passes (internal/reporter imports no LLM SDK)
  </acceptance_criteria>
  <done>Materialization, the spec-ref guard, and the Kind allowlist live in internal/reporter; the controller no longer defines them; no import cycle; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: tide-reporter RBAC — chart SOT + augment + rendered template</name>
  <read_first>
    - hack/helm/push-rbac.yaml (the SA+Role+RoleBinding triple to mirror; rule shape to swap)
    - charts/tide/templates/per-namespace-rolebinding.yaml:19-37 (the `range $ns := .Values.projectNamespaces` fan-out)
    - hack/helm/augment-tide-chart.sh:115-119 (the push-rbac cp line — add a parallel reporter-rbac cp)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "charts/tide/templates/reporter-rbac.yaml")
  </read_first>
  <action>
    Author hack/helm/reporter-rbac.yaml (SOT) as a SA+Role+RoleBinding triple named tide-reporter in {{ .Release.Namespace }}, plus a per-namespace fan-out mirroring per-namespace-rolebinding.yaml's `{{- range $ns := .Values.projectNamespaces }}` so the triple renders in each opted-in project namespace (the reader Job runs in the project namespace, so the SA must exist there). The Role rule is least-privilege: apiGroups ["tideproject.k8s"], resources ["milestones","phases","plans","tasks","waves"], verbs ["create","get"] — NO list/watch/update/delete/patch, NO projects, NO secrets, NO core resources (RESEARCH §Per-Namespace RBAC). automountServiceAccountToken: true. Add a `cp "${HACK_DIR}/reporter-rbac.yaml" "${CHART_DIR}/templates/reporter-rbac.yaml"` line to augment-tide-chart.sh (after the push-rbac cp at ~line 119), then run the augment script (or hand-mirror) so charts/tide/templates/reporter-rbac.yaml exists identically. Do NOT hand-edit the rendered template independently (CLAUDE.md chart-is-fixed-contract; SOT is hack/helm).
  </action>
  <verify>
    <automated>helm template charts/tide --set projectNamespaces='{tide-sample-medium}' | grep -A3 'name: tide-reporter' | grep -q 'create' && diff <(sed 's/{{.*}}//g' hack/helm/reporter-rbac.yaml) <(sed 's/{{.*}}//g' charts/tide/templates/reporter-rbac.yaml) >/dev/null; echo "render+SOT-parity exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - hack/helm/reporter-rbac.yaml exists with the create+get-on-5-Kinds Role and the projectNamespaces range
    - `grep -q 'reporter-rbac.yaml' hack/helm/augment-tide-chart.sh` succeeds (cp line added)
    - charts/tide/templates/reporter-rbac.yaml exists and is the augment output of the SOT (not hand-edited divergently)
    - `helm template charts/tide --set projectNamespaces='{tide-sample-medium}'` renders a tide-reporter Role with verbs create,get and NO delete/list/watch/patch/update
  </acceptance_criteria>
  <done>The chart provisions a least-privilege tide-reporter SA+Role+RoleBinding in .Release.Namespace and per project namespace, authored under SOT discipline.</done>
</task>

<task type="auto">
  <name>Task 3: Medium sample provisions tide-reporter RBAC</name>
  <read_first>
    - examples/projects/medium/per-namespace-resources.yaml:58-114 (tide-subagent + tide-push blocks to mirror)
    - hack/helm/reporter-rbac.yaml (the Role rule authored in Task 2 — copy the rule verbatim)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "examples/projects/medium/per-namespace-resources.yaml")
  </read_first>
  <action>
    Add a tide-reporter SA + Role + RoleBinding block to examples/projects/medium/per-namespace-resources.yaml in namespace tide-sample-medium, copying the structure of the existing tide-push block (lines ~72-114) but with the create+get-on-the-5-tide-Kinds rule from hack/helm/reporter-rbac.yaml. Use the same label set as the existing blocks (app.kubernetes.io/managed-by: tide-sample, tideproject.k8s/sample: medium). Update the file header comment (lines ~3-16) to document the new tide-reporter identity alongside tide-subagent + tide-push (same rationale: the controller's reader Job runs in the project namespace).
  </action>
  <verify>
    <automated>grep -c 'name: tide-reporter' examples/projects/medium/per-namespace-resources.yaml | grep -qE '^[3-9]' && kubectl apply --dry-run=client -f examples/projects/medium/per-namespace-resources.yaml >/dev/null 2>&1; echo "dry-run exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - the file contains a tide-reporter ServiceAccount, Role (verbs create,get on the 5 tide Kinds), and RoleBinding in namespace tide-sample-medium
    - `kubectl apply --dry-run=client -f examples/projects/medium/per-namespace-resources.yaml` succeeds (valid YAML/RBAC)
    - the Role grants NO delete/list/watch/patch/update and no secrets
  </acceptance_criteria>
  <done>The medium sample stages the tide-reporter identity in its namespace so the reader Job (09-05/06) can run there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader Job (project ns) → apiserver | the reporter SA can create child CRs in the tenant namespace — a higher blast radius than the operator's tide-system identity |
| poisoned children/*.json → reporter materialize | a planner could declare a non-TIDE Kind or a spurious child subtree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Elevation of privilege | over-privileged reporter SA | mitigate | least-privilege Role: create+get on 5 child Kinds only; no list/watch/delete/patch/update, no secrets, no core resources, no projects |
| T-09-08 | Tampering | poisoned child declares a non-TIDE Kind | mitigate | childKindAllowlist enforced in the reporter (moved here) + runner mirror retained + CEL admission guards regardless of creator |
| T-09-09 | Tampering | double-authored child subtree on re-dispatch | mitigate | spec-parent-ref idempotency guard moves WITH materialization to the create-site (Pitfall 3 / cascade-9/10/11) |
</threat_model>

<verification>
- `go build ./... && make verify-import-firewall` green (no import cycle, no LLM SDK in internal/reporter)
- `go test ./internal/reporter/... ./internal/controller/... -short -count=1` green
- `helm template charts/tide --set projectNamespaces='{tide-sample-medium}'` renders the least-privilege tide-reporter Role
</verification>

<success_criteria>
REQ-09-01 (foundation): materialization + spec-ref guard + Kind allowlist live in an import-safe package for the reader Job; the least-privilege tide-reporter identity is provisioned in the chart (SOT) and the medium sample.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-04-SUMMARY.md`
</output>
