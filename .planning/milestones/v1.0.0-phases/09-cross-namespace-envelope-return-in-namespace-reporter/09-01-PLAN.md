---
phase: 09-cross-namespace-envelope-return-in-namespace-reporter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/subagent/anthropic/pricing.go
  - internal/subagent/anthropic/pricing_test.go
  - internal/subagent/anthropic/subagent.go
autonomous: true
requirements: [REQ-09-05]
must_haves:
  truths:
    - "A real Claude (Haiku) dispatch produces EstimatedCostCents > 0 in its EnvelopeOut.Usage"
    - "The cost is computed from parsed token counts × a per-model price table keyed on the resolved model string"
    - "A table miss fails loud (logs + conservative default) rather than silently returning 0"
  artifacts:
    - path: "internal/subagent/anthropic/pricing.go"
      provides: "Per-model price table + estimatedCostCents(model, Usage) int64"
      contains: "claude-haiku-4-5"
    - path: "internal/subagent/anthropic/pricing_test.go"
      provides: "Unit test for cost computation across models + table-miss behavior"
  key_links:
    - from: "internal/subagent/anthropic/subagent.go"
      to: "estimatedCostCents"
      via: "usage.EstimatedCostCents = estimatedCostCents(in.Provider.Model, usage) before EnvelopeOut assembly"
      pattern: "EstimatedCostCents = estimatedCostCents"
---

<objective>
Resolve defect #6: the real Anthropic runner never computes `EstimatedCostCents`, so `RollUpUsage` accumulates zero and `Project.Status.budget.costSpentCents` stays 0 despite real token bills. Add a per-model price table in the anthropic package (behind the `Subagent` interface per CLAUDE.md), and multiply parsed token counts by it at the EnvelopeOut assembly point.

Purpose: the medium-sample DoD requires `costSpentCents > 0` under the cap.
Output: `internal/subagent/anthropic/pricing.go` (+ test); a one-line wire in `subagent.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md

<interfaces>
From pkg/dispatch/envelope.go — Usage struct (token fields the price table multiplies):
  InputTokens int64; OutputTokens int64; CacheReadTokens int64; CacheCreationTokens int64;
  EstimatedCostCents int64 (the field this plan populates).

Resolved models in scope (key the table on the EXACT strings):
  - examples/projects/medium/project.yaml subagent.model: `claude-haiku-4-5` (the medium per-Project default; the DoD model)
  - charts/tide/values.yaml subagent per-level defaults: milestone `claude-opus-4-7`, phase/plan `claude-sonnet-4-6`, task `claude-haiku-4-5`, defaults `claude-sonnet-4-6`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-model price table + estimatedCostCents</name>
  <read_first>
    - internal/subagent/anthropic/stream_parser.go:99-107 (token-field assembly — the inputs this table consumes)
    - pkg/dispatch/envelope.go:219-249 (Usage struct field set + EstimatedCostCents godoc)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "internal/subagent/anthropic/pricing.go (NEW) + subagent.go" — table shape + Pitfall 4)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md (section "Cost Surfacing (#6)" + ASSUMPTION A3)
  </read_first>
  <behavior>
    - claude-haiku-4-5 at input=1_000_000 tokens, output=0 → 100 cents (input price 100 cents/MTok per A3)
    - claude-haiku-4-5 at output=1_000_000, input=0 → 500 cents (output 500 cents/MTok)
    - cacheReadTokens billed at 10 cents/MTok; cacheCreationTokens at 125 cents/MTok
    - mixed small token counts → result is ceil'd to the next whole cent (no zero from sub-cent rounding-down)
    - unknown model string → returns a conservative non-zero estimate (use the most-expensive known tier) AND the function signals the miss (e.g. via a returned/logged sentinel the caller logs) — never silent 0 (Pitfall 4)
    - claude-sonnet-4-6 and claude-opus-4-7 keys present (chart per-level defaults)
  </behavior>
  <action>
    Create internal/subagent/anthropic/pricing.go in package anthropic. Define an unexported `modelPrice` struct with int64 fields `inputCentsPerMTok, outputCentsPerMTok, cacheReadCentsPerMTok, cacheWriteCentsPerMTok` and an unexported `priceTable map[string]modelPrice`. Confirm the exact Haiku model ID against examples/projects/medium/project.yaml (subagent.model = `claude-haiku-4-5`) and the chart per-level defaults in charts/tide/values.yaml before keying; key entries for `claude-haiku-4-5`, `claude-sonnet-4-6`, `claude-opus-4-7`. Use the A3 values for Haiku (cents/MTok: input=100, output=500, cacheWrite=125, cacheRead=10); for sonnet/opus use the documented Anthropic prices (cross-check the pricing page; cost magnitude is acceptable per A3 — accuracy MEDIUM). Implement `estimatedCostCents(model string, u pkgdispatch.Usage) int64` computing ceil((input*inPrice + output*outPrice + cacheRead*cacheReadPrice + cacheCreation*cacheWritePrice) / 1_000_000). On a table miss, log to the package logger (or stderr) a loud "pricing: unknown model %q, using conservative default" and apply the most-expensive known tier — do NOT return 0 (Pitfall 4). Keep ALL of this in internal/subagent/anthropic/ (provider-specific, behind the Subagent interface — CLAUDE.md anti-pattern).
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/ -run TestEstimatedCostCents -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/subagent/anthropic/pricing.go exists; `grep -q 'claude-haiku-4-5' internal/subagent/anthropic/pricing.go` succeeds
    - pricing_test.go covers the behavior cases above including the table-miss path
    - `go test ./internal/subagent/anthropic/ -run TestEstimatedCostCents -count=1` passes
    - no import of api/v1alpha1 or controller packages added (price table is leaf provider code)
  </acceptance_criteria>
  <done>estimatedCostCents computes >0 cents from token counts for all three keyed models and fails loud (non-zero) on miss; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire cost into the EnvelopeOut assembly point</name>
  <read_first>
    - internal/subagent/anthropic/subagent.go:259-285 (the ParseStream → EnvelopeOut assembly point; `usage` is in scope, `in.Provider.Model` is in scope)
    - internal/budget/tally.go:51 (RollUpUsage accumulates usage.EstimatedCostCents — stays provider-agnostic, unchanged)
  </read_first>
  <action>
    In internal/subagent/anthropic/subagent.go, immediately after `usage, resultText, parseErr := ParseStream(...)` returns and BEFORE the `out := pkgdispatch.EnvelopeOut{...}` assembly (~line 269), set `usage.EstimatedCostCents = estimatedCostCents(in.Provider.Model, usage)`. Do this on the success path AND ensure the failure path (waitErr branch) still carries the populated usage if usage was parsed. Do NOT touch internal/budget/tally.go — RollUpUsage already accumulates the field. Do NOT compute cost in any controller package (CLAUDE.md: provider cost lives behind the Subagent interface).
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/... ./internal/budget/... -short -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'EstimatedCostCents = estimatedCostCents' internal/subagent/anthropic/subagent.go` succeeds
    - the assignment precedes the EnvelopeOut literal assembly (verify by reading the diff)
    - `go test ./internal/subagent/anthropic/... ./internal/budget/... -short -count=1` passes
    - no new import of a controller/api package in subagent.go
  </acceptance_criteria>
  <done>Real runner sets EstimatedCostCents from the price table before assembling EnvelopeOut; budget rollup is unchanged and accumulates a non-zero cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider response → in-pod runner | token counts arrive from the Anthropic stream; the runner trusts them only for cost estimation (no control flow) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | price table keyed on wrong model → cost reads 0 | mitigate | key on exact resolved model string; table-miss falls back to most-expensive tier + loud log, never silent 0 (Pitfall 4) |
| T-09-02 | Information disclosure | cost figure leaks model/usage in logs | accept | usage/cost is operational telemetry, already surfaced on Project.Status.budget by design; no PII |
</threat_model>

<verification>
- `go test ./internal/subagent/anthropic/... ./internal/budget/... -short -count=1` green
- price table contains `claude-haiku-4-5` (DoD model) and the chart per-level model keys
</verification>

<success_criteria>
REQ-09-05 satisfied: the anthropic runner computes EstimatedCostCents from a per-model price table; the value is non-zero for real Haiku token counts and flows through RollUpUsage unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-01-SUMMARY.md`
</output>
