---
phase: 09-cross-namespace-envelope-return-in-namespace-reporter
plan: 06
type: execute
wave: 3
depends_on: [09-04, 09-05]
files_modified:
  - internal/controller/reporter_jobspec.go
  - internal/controller/reporter_jobspec_test.go
  - internal/controller/project_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - cmd/manager/main.go
  - cmd/manager/env.go
  - hack/helm/tide-values.yaml
  - hack/helm/augment-tide-chart.sh
  - test/integration/kind/reporter_pod_test.go
autonomous: true
requirements: [REQ-09-01]
must_haves:
  truths:
    - "On dispatch-Job completion, the Manager spawns a tide-reporter reader Job in the project namespace (instead of reading the cross-namespace PVC)"
    - "The four planner-completion handlers no longer call MaterializeChildCRDs/childrenAlreadyMaterialized inline"
    - "The Manager still reads the tiny status (usage/git/exitCode/reason) from the dispatch Job's termination message for budget rollup + failure classification"
    - "Children are observed via the existing Owns(child CRD) watch; the reader Job is discriminated from the dispatch Job by a role label"
    - "The reporter image is a Helm value threaded to the controller, with a kind Layer B test asserting reader-Job-spawn → children appear"
  artifacts:
    - path: "internal/controller/reporter_jobspec.go"
      provides: "buildReporterJob — short-lived reader Job in the project namespace (tide-reporter SA, PVC subPath mount)"
      contains: "buildReporterJob"
    - path: "test/integration/kind/reporter_pod_test.go"
      provides: "Layer B: Manager spawns reader Job → children appear in the project namespace"
  key_links:
    - from: "internal/controller/project_controller.go"
      to: "buildReporterJob"
      via: "handleProjectJobCompletion reads tiny status then spawns the reader Job, drops inline materialize"
      pattern: "buildReporterJob"
    - from: "internal/controller/reporter_jobspec.go"
      to: "tide-reporter SA + PVC subPath"
      via: "Job in project.Namespace, ServiceAccountName tide-reporter, SubPath {project.UID}/workspace"
      pattern: "ServiceAccountName"
---

<objective>
Switch the Manager from cross-namespace PVC reads to the Option-C reader Job: on dispatch-Job completion, spawn a short-lived `tide-reporter` Job in the project namespace (which materializes children via the K8s API, plan 09-05), and DROP the inline `MaterializeChildCRDs`/`childrenAlreadyMaterialized` calls from all four planner-completion handlers. The Manager still reads the tiny status from the dispatch Job's termination message for budget rollup + failure classification, and observes children via its existing `Owns(child CRD)` watch. Discriminate the reader Job from the dispatch Job by a role label.

Purpose: completes REQ-09-01 — the Manager no longer reads the project-namespace PVC for envelope-out; materialization happens in the reporter.
Output: `reporter_jobspec.go` + spawn wiring in 4 handlers + manager image plumbing + values/deployment + Layer B test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-04-SUMMARY.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-05-SUMMARY.md

<interfaces>
Spawn pattern (mirror): internal/controller/boundary_push.go:88-138 triggerBoundaryPush — idempotent Get-then-Create of a
  deterministic-named Job in project.Namespace; AlreadyExists tolerated; metric + log on dispatch.
Job builder analog: internal/controller/push_helpers.go buildCloneJob/buildPushJob (:132,:259) — Namespace: project.Namespace,
  RestartPolicy Never, BackoffLimit 2, TTLSecondsAfterFinished 300, PVC VolumeMount SubPath fmt.Sprintf("%s/workspace", project.UID),
  owner.EnsureOwnerRef(job, parent, scheme). buildReporterJob diverges: NO git-creds EnvFrom, SA tide-reporter (not tide-push),
  image opts.ReporterImage, hardened securityContext (RunAsNonRoot + drop ALL caps, mirror jobspec.go:368-375).
Reader Job args (plan 09-05 flags): --workspace=/workspace, --project-uid, --task-uid (envelope key), --parent-name,
  --parent-namespace, --parent-kind.
Handler sites to edit (drop inline materialize, add spawn): project_controller.go:790-829 (handleProjectJobCompletion),
  milestone_controller.go:~395-417, phase_controller.go:~321-340, plan_controller.go:~372-399. Each keeps its Owns(child) watch
  and checkComplete/roll-up. Keep reading tiny status via r.EnvReader.ReadOut for usage/git/exitCode/reason.
Manager image plumbing (mirror tidePushImage): cmd/manager/main.go:170 `tidePushImage := envOrDefault("TIDE_PUSH_IMAGE", ...)`;
  values block charts/tide values images.tidePush; deployment env --subagent-image style arg / TIDE_*_IMAGE env injected by
  hack/helm/augment-tide-chart.sh python block (~line 170).
Watch: each controller already has Owns(&batchv1.Job{}) — the reader Job is also a Job; discriminate by label
  tideproject.k8s/role: reporter so the dispatch-completion handler doesn't re-fire on the reader Job's own completion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildReporterJob + ReporterImage plumbing</name>
  <read_first>
    - internal/controller/push_helpers.go:132-319 (buildPushJob/buildCloneJob — the short-lived project-ns Job skeleton)
    - internal/dispatch/podjob/jobspec.go:368-375 (hardened securityContext: RunAsNonRoot + drop ALL caps)
    - cmd/manager/main.go:160-180 (tidePushImage env wiring) + cmd/manager/env.go (envOrDefault)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "internal/controller/reporter_jobspec.go (NEW — jobspec builder)")
  </read_first>
  <behavior>
    - buildReporterJob returns a Job in project.Namespace named deterministically (e.g. tide-reporter-<parentUID>) with ServiceAccountName "tide-reporter"
    - the Job mounts the project PVC with SubPath "<project.UID>/workspace" at /workspace
    - the container args carry --task-uid, --parent-name, --parent-namespace, --parent-kind, --project-uid
    - the Job carries a controller ownerRef to the parent (cascade-delete) and a label tideproject.k8s/role=reporter
    - the container has RunAsNonRoot + drops ALL capabilities; NO git-creds EnvFrom
    - the image is opts.ReporterImage (empty image → builder still produces a Job; the spawn-site skips when unconfigured, mirroring TidePushImage)
  </behavior>
  <action>
    Create internal/controller/reporter_jobspec.go with `buildReporterJob(parent metav1.Object, project *tidev1alpha1.Project, pvcName, taskUID, parentKind string, opts ReporterOptions, scheme *runtime.Scheme) *batchv1.Job` and a `ReporterOptions{ReporterImage string}`. Mirror buildCloneJob's skeleton (RestartPolicy Never, BackoffLimit 2, TTLSecondsAfterFinished 300, PVC subPath mount). Diverge: SA "tide-reporter", image opts.ReporterImage, hardened securityContext (RunAsNonRoot true, Capabilities drop ALL, mirror jobspec.go:368-375), NO EnvFrom git creds, label tideproject.k8s/role=reporter on the Job + pod template. Set the container Args per plan 09-05's flag set. owner.EnsureOwnerRef(job, parent, scheme). In cmd/manager/main.go add `reporterImage := envOrDefault("TIDE_REPORTER_IMAGE", "ghcr.io/jsquirrelz/tide-reporter:v0.1.0-dev")` (mirror tidePushImage at :170) and thread it into the four reconcilers as a new `ReporterImage` field (add the field to each reconciler struct alongside TidePushImage). Add the reporter-image flag/env documentation comment block mirroring TIDE_PUSH_IMAGE.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/ -run 'ReporterJob' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func buildReporterJob' internal/controller/reporter_jobspec.go` succeeds
    - reporter_jobspec_test.go asserts the <behavior> cases (SA name, subPath, args, ownerRef, role label, securityContext, no git EnvFrom)
    - `grep -q 'TIDE_REPORTER_IMAGE' cmd/manager/main.go` succeeds
    - `go build ./...` succeeds; reporter-job builder test passes
  </acceptance_criteria>
  <done>buildReporterJob produces a least-privilege, role-labelled, owner-referenced reader Job in the project namespace; the reporter image is threaded from env to the reconcilers.</done>
</task>

<task type="auto">
  <name>Task 2: Spawn reader Job + drop inline materialize in all four handlers</name>
  <read_first>
    - internal/controller/project_controller.go:790-829 (handleProjectJobCompletion — the full read+materialize block to replace)
    - internal/controller/milestone_controller.go:395-417, phase_controller.go:321-340, plan_controller.go:372-399 (the parallel blocks)
    - internal/controller/boundary_push.go:88-138 (triggerBoundaryPush — the idempotent Get-then-Create spawn pattern to mirror)
    - internal/controller/project_controller.go:1006-1029 (SetupWithManager Owns wiring — the reader Job is discriminated by label)
  </read_first>
  <action>
    In each of the four handlers: KEEP the tiny-status read (r.EnvReader.ReadOut for usage/git/exitCode/reason → budget rollup + failure classification — unchanged). REMOVE the `len(envOut.ChildCRDs) > 0 { childrenAlreadyMaterialized ... MaterializeChildCRDs ... }` block. ADD an idempotent reader-Job spawn mirroring triggerBoundaryPush: Get a deterministic-named reader Job in project.Namespace; if NotFound, buildReporterJob(parent, project, pvcName, <parentUID-as-taskUID-key>, <kind>, ReporterOptions{ReporterImage: r.ReporterImage}, r.Scheme) then c.Create (tolerate AlreadyExists). Skip with an Info log when r.ReporterImage is unconfigured (mirror the TidePushImage skip). Children arrive via the existing Owns(child CRD) watch (no change to SetupWithManager's Owns(child) lines). In each SetupWithManager, ensure the dispatch-completion path does NOT re-fire on the reader Job's completion: discriminate by the tideproject.k8s/role=reporter label in the Job-completion branch (the handler should ignore Jobs carrying that label, or only act on the dispatch Job). Drop the now-dead `MaterializeChildCRDs`/`childrenAlreadyMaterialized` controller wrappers if plan 09-04 left thin shims and nothing else references them.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -short -count=1 && grep -c 'MaterializeChildCRDs' internal/controller/project_controller.go internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MaterializeChildCRDs' internal/controller/{project,milestone,phase,plan}_controller.go` returns 0 for all four
    - each handler calls buildReporterJob + Create (grep `buildReporterJob` in all four)
    - each handler still calls r.EnvReader.ReadOut for the tiny status (grep retained)
    - the reader Job is discriminated by the tideproject.k8s/role=reporter label so dispatch-completion does not re-fire on it
    - `go build ./... && go test ./internal/controller/... -short -count=1` passes
  </acceptance_criteria>
  <done>The four handlers spawn the reader Job instead of materializing inline; the Manager reads only the tiny status and observes children via watch; no inline materialize remains.</done>
</task>

<task type="auto">
  <name>Task 3: Reporter image value + deployment env + Layer B reader-Job test</name>
  <read_first>
    - hack/helm/tide-values.yaml (images block — add tideReporter mirroring tidePush) + charts/tide/values.yaml:153-156 (rendered tidePush block)
    - hack/helm/augment-tide-chart.sh:~133-180 (deployment env/arg injection python — add TIDE_REPORTER_IMAGE)
    - test/integration/kind/ (existing Layer B suite + a bare_project / up_stack_dispatch test for the spawn+observe pattern)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md (Validation Architecture: SC-1 kind reporter_pod_test.go)
  </read_first>
  <action>
    In hack/helm/tide-values.yaml add an `images.tideReporter` block (repository ghcr.io/jsquirrelz/tide-reporter, tag "", pullPolicy IfNotPresent) mirroring tidePush; re-run augment (or hand-mirror) so charts/tide/values.yaml carries it. In hack/helm/augment-tide-chart.sh's deployment-env python block, inject TIDE_REPORTER_IMAGE env on the manager container resolving to `{{ .Values.images.tideReporter.repository }}:{{ .Values.images.tideReporter.tag | default .Chart.AppVersion }}` (mirror TIDE_PUSH_IMAGE). Add test/integration/kind/reporter_pod_test.go (Layer B): apply a parent + dispatch a stub planner Job whose out.json carries a child; assert the Manager spawns a tide-reporter Job in the project namespace AND the child CR subsequently appears (observed via watch) with the correct ownerRef. Use the stub subagent (CI path) — the test must pass with the stub's out.json (incl. the SourcePath fix from plan 09-02).
  </action>
  <verify>
    <automated>helm template charts/tide | grep -q 'TIDE_REPORTER_IMAGE' && grep -q 'tideReporter' charts/tide/values.yaml && grep -q 'reporter_pod' test/integration/kind/reporter_pod_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'tideReporter' hack/helm/tide-values.yaml charts/tide/values.yaml` succeeds
    - `helm template charts/tide` renders a TIDE_REPORTER_IMAGE env on the manager container
    - test/integration/kind/reporter_pod_test.go asserts reader-Job spawn → child CR appears via watch using the stub path
    - the Layer B test is wired into the kind suite (runs under `make test-int`)
  </acceptance_criteria>
  <done>The reporter image is a Helm value threaded to the controller; Layer B proves the Manager-spawns-reader-Job → children-appear flow on the stub path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manager (tide-system) → reader Job (project ns) | the Manager creates a Job in the tenant namespace; the Job runs as the least-privilege tide-reporter SA, not the Manager identity |
| reader Job completion → Manager handler | the Manager must not confuse the reader Job's completion with the dispatch Job's completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Repudiation / logic error | handler re-fires on the reader Job's own completion → duplicate spawn loop | mitigate | discriminate the reader Job by tideproject.k8s/role=reporter label; dispatch-completion path ignores it; idempotent Get-before-Create |
| T-09-14 | Elevation of privilege | reader Job inherits Manager verbs | mitigate | reader Job runs as tide-reporter SA (create+get on 5 Kinds, plan 09-04), NOT the Manager ClusterRole |
| T-09-15 | Tampering | stale cross-ns PVC read path left live | mitigate | inline MaterializeChildCRDs removed from all four handlers (grep gate == 0); materialization is the reporter's job |
</threat_model>

<verification>
- `go build ./... && go test ./internal/controller/... -short -count=1` green
- `grep -c 'MaterializeChildCRDs' internal/controller/{project,milestone,phase,plan}_controller.go` == 0 for all four
- `helm template charts/tide` renders TIDE_REPORTER_IMAGE
- `make test-int` green (Layer A + Layer B, incl. reporter_pod_test.go)
</verification>

<success_criteria>
REQ-09-01 (complete): the Manager spawns the reader Job on dispatch-Job completion, drops inline materialization, reads only the tiny status, and observes children via watch; the reporter image is plumbed; Layer B proves the end-to-end stub flow.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-06-SUMMARY.md`
</output>
