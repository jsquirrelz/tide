---
phase: 44-llm-message-array-spans-d-o5-redaction-size-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/reporter_jobspec.go
  - internal/controller/reporter_jobspec_test.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - cmd/manager/main.go
autonomous: true
requirements: [MSG-03]
tags: [kubernetes, jobspec, otlp, batch-span-processor, reporter]

must_haves:
  truths:
    - "Reporter Jobs carry OTEL_EXPORTER_OTLP_ENDPOINT + OTEL_BSP_MAX_EXPORT_BATCH_SIZE=6 env vars when the manager itself has an OTLP endpoint configured, and zero Env entries when it does not"
    - "BuildReporterJob can construct a trace-only Job shape (distinct name keyed on the completed Job's UID, --trace-only Args, no parent-CR flags) without colliding with the materialization reporter's deterministic name"
    - "All four planner-level reporter spawn sites forward the manager's OTLP endpoint into ReporterOptions"
  artifacts:
    - path: "internal/controller/reporter_jobspec.go"
      provides: "ReporterOptions.OTLPEndpoint + ReporterOptions.TraceOnly/TraceOnlyJobKey; conditional Env block; trace-only name/args branch"
      contains: "OTEL_BSP_MAX_EXPORT_BATCH_SIZE"
    - path: "cmd/manager/main.go"
      provides: "plannerDeps.OTLPEndpoint wired from the same OTEL_EXPORTER_OTLP_ENDPOINT env otelinit reads"
  key_links:
    - from: "internal/controller/dispatch_helpers.go"
      to: "internal/controller/reporter_jobspec.go"
      via: "spawnReporterIfNeeded populating ReporterOptions.OTLPEndpoint"
      pattern: "OTLPEndpoint"
    - from: "cmd/manager/main.go"
      to: "internal/controller/dispatch_helpers.go"
      via: "PlannerReconcilerDeps.OTLPEndpoint"
      pattern: "OTLPEndpoint:"
---

<objective>
Manager-side Job-spec plumbing for MSG-03's size-boundary model and Phase 44's trace-only shape: `ReporterOptions` gains `OTLPEndpoint` (forwarded as `OTEL_EXPORTER_OTLP_ENDPOINT` plus the hardcoded `OTEL_BSP_MAX_EXPORT_BATCH_SIZE=6` batch-size literal — the research-validated fix for the REAL aggregate-batch risk, zero values.yaml changes) and `TraceOnly`/`TraceOnlyJobKey` (the Job shape plan 44-05 spawns for Tasks). All four planner spawn sites forward the endpoint so combined (materialization+synth) reporter runs can export.

**EXECUTION GATE (phase-wide):** requires `worktree-phase-43-discuss` merged into `main` first. This plan compiles against Phase 43's shipped contracts: `ReporterOptions.TraceParent` + the conditional `--traceparent=` Arg append in `BuildReporterJob`, and `spawnReporterIfNeeded`'s trailing `traceParent string` param. Verify before editing: `grep -n 'TraceParent' internal/controller/reporter_jobspec.go` must hit.

This plan makes NO behavioral change to the reporter binary (44-04 owns that) and spawns NO new Jobs (44-05 owns the Task spawn). The `--trace-only` Arg is emitted only when `TraceOnly` is set, and nothing sets it until 44-05 — which runs after 44-04 registers the flag, so the Args↔flag crash-loop hazard (Phase 43 Pitfall 4) is structurally excluded by wave ordering.

Purpose: the OTLP batch-size env var is the load-bearing MSG-03 mitigation — 19/40 real Tasks exceed 4 MB in aggregate naive payload while zero individual spans do (RESEARCH §Size-Boundary Model); `OTEL_BSP_MAX_EXPORT_BATCH_SIZE` is read automatically by the pinned OTel SDK's `NewBatchSpanProcessor`, so this is pure Job-env plumbing.
Output: extended ReporterOptions + BuildReporterJob, four wired spawn sites, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-RESEARCH.md  (§"Size-Boundary Model (D-07)", §"Verified pattern: NewBatchSpanProcessor")
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-PATTERNS.md  (§reporter_jobspec.go, §reporter_jobspec_test.go)

<interfaces>
Phase 43 shipped contracts (pinned from the worktree — will be on main post-merge):

internal/controller/reporter_jobspec.go:
- `ReporterOptions{ReporterImage string; TraceParent string}` — TraceParent carried as a `--traceparent=` Arg (NOT Env; the file is 100% Args-based today, zero Env entries)
- `BuildReporterJob(parent metav1.Object, project *tideprojectv1alpha3.Project, pvcName string, taskUID string, parentKind string, opts ReporterOptions, scheme *runtime.Scheme) *batchv1.Job`
- Materialization Job name: `fmt.Sprintf("tide-reporter-%s", parent.GetUID())`; Args: --workspace=/workspace, --project-uid, --task-uid, --parent-name, --parent-namespace, --parent-kind (+conditional --traceparent)
- Container: single "reporter", PVC mount /workspace subPath `<project.UID>/workspace`, hardened SecurityContext, labels tideproject.k8s/role=reporter, BackoffLimit 2, TTL 300

internal/controller/dispatch_helpers.go:
- `spawnReporterIfNeeded(ctx, c, scheme, parent, project, parentKind, reporterImage, pvcName, traceParent string) (bool, error)` — builds `ReporterOptions{ReporterImage: reporterImage, TraceParent: traceParent}` internally (line ~120)
- `PlannerReconcilerDeps` struct (Dispatcher/EnvReader/SigningKey/CredproxyImage/TidePushImage/ReporterImage/HelmProviderDefaults/PricingOverridesJSON)

Spawn call sites (four):
- milestone_controller.go ~:639 and phase_controller.go ~:592 route through spawnReporterIfNeeded
- plan_controller.go ~:646 and project_controller.go ~:1901 construct inline `BuildReporterJob(..., ReporterOptions{...})` literals

cmd/manager/main.go:
- `plannerDeps := controller.PlannerReconcilerDeps{...}` (~line 423), assigned to all four planner reconcilers
- `otelinit.NewTracerProvider(signalCtx)` (~line 273) reads OTEL_EXPORTER_OTLP_ENDPOINT via os.Getenv — the SAME env var name this plan forwards (D-06 "same value it forwards into Job env")

OTel SDK (pinned v1.43.0, verified by research): `NewBatchSpanProcessor` reads OTEL_BSP_MAX_EXPORT_BATCH_SIZE automatically; `otelinit.NewTracerProvider` passes no explicit options, so the env var is authoritative in the reporter process.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReporterOptions.OTLPEndpoint + TraceOnly shape in BuildReporterJob</name>
  <files>internal/controller/reporter_jobspec.go, internal/controller/reporter_jobspec_test.go</files>
  <read_first>
    - internal/controller/reporter_jobspec.go (ReporterOptions doc conventions, BuildReporterJob args construction, the Phase-43 conditional --traceparent append this plan's Env block sits beside)
    - internal/controller/reporter_jobspec_test.go (TestBuildReporterJob_Image and TestBuildReporterJob_TraceparentArg — the present-when-set/absent-when-empty test style to mirror)
    - internal/dispatch/podjob/jobspec.go (~lines 372-415 — the subagentEnv conditional-append house pattern for "add env only when configured")
  </read_first>
  <behavior>
    - Test 1 (TestBuildReporterJob_OTLPEndpointEnv): opts.OTLPEndpoint="collector:4317" → container Env contains exactly {OTEL_EXPORTER_OTLP_ENDPOINT: "collector:4317"} and {OTEL_BSP_MAX_EXPORT_BATCH_SIZE: "6"}
    - Test 2 (TestBuildReporterJob_NoOTLPEndpointNoEnv): opts.OTLPEndpoint="" → container Env is empty/nil (byte-identical posture to today)
    - Test 3 (TestBuildReporterJob_TraceOnly): opts.TraceOnly=true, TraceOnlyJobKey="job-uid-123", taskUID="task-uid-9" → Job name "tide-reporter-trace-job-uid-123"; Args contain "--trace-only", "--workspace=/workspace", "--task-uid=task-uid-9"; Args contain NO "--parent-name"/"--parent-namespace"/"--parent-kind"; role label, SA, PVC subPath, SecurityContext identical to the materialization shape
    - Test 4 (regression): TraceOnly=false leaves the existing name/args byte-identical to today's shape (existing tests still green)
  </behavior>
  <action>
    Extend ReporterOptions with three fields, doc-commented in the existing house style:
    - `OTLPEndpoint string` — when non-empty, BuildReporterJob sets the container's Env (a NEW field; the container has none today) to two corev1.EnvVar entries: OTEL_EXPORTER_OTLP_ENDPOINT=<value> (so the reporter's otelinit.NewTracerProvider resolves the same collector the manager uses — TRACE-03/D-06) and OTEL_BSP_MAX_EXPORT_BATCH_SIZE="6" (hardcoded literal, NOT a Helm value — values.yaml is a FIXED contract per CLAUDE.md; cite RESEARCH Size-Boundary math in the comment: 6 spans × 512 KiB whole-span cap = 3 MiB, ~25% headroom under the 4 MiB OTLP gRPC ceiling; the pinned SDK's NewBatchSpanProcessor reads this env automatically, zero otelinit changes). When empty: no Env at all — the reporter's otelinit falls back to its no-op provider, which is D-06's materialization-mode no-op posture.
    - `TraceOnly bool` + `TraceOnlyJobKey string` — when TraceOnly is true: Job name becomes "tide-reporter-trace-"+TraceOnlyJobKey (callers pass the completed dispatch Job's UID — per-attempt exactly-once, AND it can never collide with the materialization reporter's "tide-reporter-<parentUID>" name, so a failed-then-retried planner Job's trace-only spawn can never block a later materialization spawn); Args become {"--trace-only", "--workspace=/workspace", "--task-uid=" + taskUID} plus the existing conditional "--traceparent=" append — the parent-CR flags (--parent-name/--parent-namespace/--parent-kind/--project-uid) are OMITTED because trace-only mode makes no K8s API calls (no CR-write RBAC needed; the tide-reporter SA is reused as-is, never widened — CONTEXT Claude's-Discretion RBAC note).
    Everything else (SA, volume/subPath, SecurityContext, role labels, BackoffLimit, TTL, ownerRef via owner.EnsureOwnerRef) stays shared between both shapes. Update BuildReporterJob's doc comment to describe both shapes and the name-collision rationale.
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'TestBuildReporterJob' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OTEL_BSP_MAX_EXPORT_BATCH_SIZE' internal/controller/reporter_jobspec.go` returns ≥1 with value literal "6"
    - `grep -c 'tide-reporter-trace-' internal/controller/reporter_jobspec.go` returns ≥1
    - TestBuildReporterJob_NoOTLPEndpointNoEnv asserts len(container.Env)==0 when OTLPEndpoint==""
    - TestBuildReporterJob_TraceOnly asserts absence of "--parent-name" in Args
    - `go test ./internal/controller/ -run 'TestBuildReporterJob' -count=1` exits 0 including all pre-existing TestBuildReporterJob_* tests
  </acceptance_criteria>
  <done>BuildReporterJob supports both shapes: materialization (unchanged unless OTLPEndpoint set, which adds exactly two env vars) and trace-only (distinct name, minimal Args, no parent flags), proven by present/absent unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Forward the OTLP endpoint through PlannerReconcilerDeps to all four spawn sites</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, cmd/manager/main.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go (spawnReporterIfNeeded signature + its internal ReporterOptions literal; PlannerReconcilerDeps struct + its field doc style)
    - internal/controller/milestone_controller.go ~:639 and internal/controller/phase_controller.go ~:592 (the spawnReporterIfNeeded call sites — each gains one trailing arg)
    - internal/controller/plan_controller.go ~:646 and internal/controller/project_controller.go ~:1901 (the inline BuildReporterJob + ReporterOptions literals — each gains OTLPEndpoint)
    - cmd/manager/main.go ~:270-290 (otelinit.NewTracerProvider call — the env-var precedent) and ~:415-435 (plannerDeps construction)
  </read_first>
  <action>
    1. Add `OTLPEndpoint string` to PlannerReconcilerDeps with a doc comment: "the manager's own OTEL_EXPORTER_OTLP_ENDPOINT, forwarded into reporter Job env so the reporter's TracerProvider resolves the same collector (Phase 44 TRACE-03/D-06); empty = tracing dark, reporter env omitted".
    2. In cmd/manager/main.go, near the otelinit.NewTracerProvider call, capture `otlpEndpoint := os.Getenv("OTEL_EXPORTER_OTLP_ENDPOINT")` once into a local (same env var otelinit reads — D-06's "same value it forwards" is literal) and set `OTLPEndpoint: otlpEndpoint` in the plannerDeps literal. Do NOT touch the TaskReconcilerDeps literal — plan 44-05 owns that.
    3. Extend spawnReporterIfNeeded with a trailing `otlpEndpoint string` param (mirrors Phase 43-04's traceParent param-addition precedent exactly) threaded into its internal ReporterOptions literal as OTLPEndpoint. Update the two helper call sites (milestone_controller.go, phase_controller.go) to pass `r.Deps.OTLPEndpoint`.
    4. Add `OTLPEndpoint: r.Deps.OTLPEndpoint` to the two inline ReporterOptions literals (plan_controller.go, project_controller.go).
    No TraceOnly usage anywhere in this task — the four planner sites keep spawning materialization-shape reporters on every terminal planner Job (success AND failure, today's existing behavior; the combined run gains its synth step in 44-04, which is how D-05's failed-planner-Job coverage lands without new spawn sites).
  </action>
  <verify>
    <automated>go build ./internal/controller/... ./cmd/manager/... && go test ./internal/controller/ -run 'TestBuildReporterJob' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OTLPEndpoint' internal/controller/dispatch_helpers.go` returns ≥3 (deps field + param + ReporterOptions literal)
    - `grep -c 'OTLPEndpoint: r.Deps.OTLPEndpoint' internal/controller/plan_controller.go internal/controller/project_controller.go` returns 1 each
    - `grep -c 'os.Getenv("OTEL_EXPORTER_OTLP_ENDPOINT")' cmd/manager/main.go` returns 1
    - `go build ./internal/controller/... ./cmd/manager/...` exits 0
    - `make test-heavy` passes (existing reporter-spawn envtest specs unaffected — spawn behavior is unchanged when the env var is unset, which is the envtest default)
  </acceptance_criteria>
  <done>All four planner spawn sites forward the manager's OTLP endpoint; behavior with no endpoint configured is byte-identical to today.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager env → tenant-namespace Job env | The manager forwards its own OTLP endpoint value into reporter Job containers running in project namespaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Denial of Service (tracing pipeline) | reporter Job OTLP export batches | mitigate | OTEL_BSP_MAX_EXPORT_BATCH_SIZE=6 literal on the reporter container bounds every export RPC to ~3 MiB worst case (6 × 512 KiB span cap), under the 4 MiB gRPC ceiling with headroom |
| T-44-06 | Elevation of Privilege | trace-only Job shape | mitigate | Trace-only Args omit all parent-CR flags; reuses the existing least-privilege tide-reporter SA with zero RBAC widening (trace-only mode never builds a K8s client) |
| T-44-07 | Information Disclosure | OTLP endpoint value in Job spec | accept | The endpoint is a host:port locator, not a credential; it is already visible in the manager's own pod env |
| T-44-SC | Tampering | package installs | accept | Zero new packages this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `go build ./internal/controller/... ./cmd/manager/...` exits 0
- `go test ./internal/controller/ -run 'TestBuildReporterJob' -count=1` exits 0
- `make test-heavy` — read MAKE_EXIT and `grep -nE '^--- FAIL|^FAIL\s'` the log, not just the Ginkgo summary (CLAUDE.md)
</verification>

<success_criteria>
- Reporter Jobs gain the two-env-var block only when an OTLP endpoint is configured
- Trace-only Job shape exists and is collision-free with materialization names
- Four planner spawn sites forward the endpoint; zero values.yaml changes
</success_criteria>

<output>
Create `.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-02-SUMMARY.md` when done.
</output>
