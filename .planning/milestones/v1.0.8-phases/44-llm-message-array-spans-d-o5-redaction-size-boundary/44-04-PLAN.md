---
phase: 44-llm-message-array-spans-d-o5-redaction-size-boundary
plan: 04
type: execute
wave: 3
depends_on: [44-03]
files_modified:
  - cmd/tide-reporter/main.go
  - cmd/tide-reporter/main_test.go
autonomous: true
requirements: [TRACE-03, MSG-01]
tags: [opentelemetry, tide-reporter, tracer-provider, shutdown-flush, trace-only]

must_haves:
  truths:
    - "tide-reporter constructs a TracerProvider and calls its bounded Shutdown on EVERY exit path — killing the process mid-batch never silently drops spans (TRACE-03)"
    - "A --trace-only run reads events.jsonl, emits message-array spans parented under the --traceparent context, and exits 0 regardless of synth/export outcome (MSG-01, D-10)"
    - "A combined (materialization) run performs the synth step best-effort BEFORE the out.json read, so failed planner Jobs with no/partial envelopes still get their conversation spans (D-05) and synth failures never change the materialization exit code (D-10)"
  artifacts:
    - path: "cmd/tide-reporter/main.go"
      provides: "--trace-only flag, TracerProvider lifecycle inside runWithClient, synth step for both modes"
      contains: "trace-only"
    - path: "cmd/tide-reporter/main_test.go"
      provides: "shutdown-on-every-exit-path proof + trace-only mode tests"
      contains: "TestRun"
  key_links:
    - from: "cmd/tide-reporter/main.go"
      to: "internal/reporter/tracesynth.go"
      via: "ReconstructConversation + EmitSpans calls"
      pattern: "reporter\\.(ReconstructConversation|EmitSpans)"
    - from: "cmd/tide-reporter/main.go"
      to: "internal/otelinit"
      via: "NewTracerProvider — the reporter's FIRST call site (only cmd/manager calls it today)"
      pattern: "otelinit\\.NewTracerProvider"
    - from: "cmd/tide-reporter/main.go"
      to: "pkg/otelai/tracecontext.go"
      via: "ExtractRemoteParent(cfg.TraceParent) — LLM spans nest under the level's AGENT span"
      pattern: "otelai\\.ExtractRemoteParent"
---

<objective>
Wire the synthesizer into the `tide-reporter` binary: the `--trace-only` flag (MSG-01's invocation surface — flag, not env var, matching the file's all-flag convention), the binary's first `otelinit.NewTracerProvider` call site with the deferred bounded-Shutdown flush discipline on every exit path (TRACE-03/D-12, 5-second bound mirroring the manager), and the best-effort synth step in the combined materialization run (D-02 all-level coverage + D-05 failed-Job parity + D-10 exit-0 posture).

**EXECUTION GATE (phase-wide):** requires `worktree-phase-43-discuss` merged into `main` first. This plan builds directly on 43's shipped reporter surface: `parseFlags(args []string) (reporterConfig, error)` (flag.ContinueOnError), `reporterConfig.TraceParent` (the `--traceparent` flag, registered but unconsumed until now — its doc string literally says "consumed starting Phase 44"), and the `run()`/`runWithClient()` seam. Verify before editing: `grep -n 'func parseFlags' cmd/tide-reporter/main.go` must hit.

Purpose: TRACE-03 is a phase gate criterion — a short-lived, os.Exit-driven one-shot binary that skips Shutdown silently drops its whole batch (RESEARCH Pitfall 5); and the trace-only mode is the vehicle by which Task conversations (and failed-Job conversations) reach Phoenix at all.
Output: tide-reporter with both modes synth-capable, every exit path flush-proven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-CONTEXT.md  (D-10/D-11/D-12 + Claude's-Discretion flag-surface note)
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-RESEARCH.md  (§Pitfall 5, §"TRACE-03" requirement row)
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-PATTERNS.md  (§cmd/tide-reporter/main.go, §cmd/tide-reporter/main_test.go, §"Bounded-flush deferred Shutdown")

<interfaces>
Current cmd/tide-reporter/main.go (post-43, pinned):
- `reporterConfig{Workspace, ProjectUID, TaskUID, ParentName, ParentNamespace, ParentKind, TraceParent string}` — Workspace default "/workspace"
- `parseFlags(args []string) (reporterConfig, error)` — 7 flags registered incl. --traceparent; flag.ContinueOnError
- `main()` → parseFlags → signal.NotifyContext → `os.Exit(run(ctx, cfg, os.Stdout, os.Stderr))`
- `run(ctx, cfg, stdout, stderr) int` → `runWithClient(ctx, cfg, _, stderr, nil)`
- runWithClient sequence: (1) validate TaskUID/ParentName/ParentNamespace/ParentKind → exitInvariant; (2) build K8s client (or clientOverride); (3) read out.json at filepath.Join(cfg.Workspace, "envelopes", cfg.TaskUID, "out.json") → exitInvariant on error; (4) resolveParent; (5) idempotency check; (6) MaterializeChildCRDs
- Exit consts: exitSuccess=0, exitGenericFail=1, exitInvariant=2

cmd/manager/main.go (~lines 273-290): the bounded-shutdown pattern to transplant — `tp, otelShutdown, err := otelinit.NewTracerProvider(ctx)` + deferred `context.WithTimeout(context.Background(), 5*time.Second)` + `otelShutdown(shutdownCtx)`

internal/otelinit: `NewTracerProvider(ctx) (trace.TracerProvider, ShutdownFunc, error)` — no-op provider + no-op ShutdownFunc when OTEL_EXPORTER_OTLP_ENDPOINT unset; calls otel.SetTracerProvider globally

internal/reporter (44-03): `ReconstructConversation(eventsPath, inJSONPath string) ([]CallSpan, error)` (or with the workspace-root param 44-03 settled on — read tracesynth.go first), `EmitSpans(ctx, tracer, calls, artifactPath string) error`

pkg/otelai: `ExtractRemoteParent(ctx context.Context, traceparent string) context.Context` — malformed input → ctx without a valid span context, never panics
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --trace-only flag + TracerProvider lifecycle + synth step in both modes</name>
  <files>cmd/tide-reporter/main.go</files>
  <read_first>
    - cmd/tide-reporter/main.go (the whole file — parseFlags block, run/runWithClient seam, exit-code map doc comment, validation ordering)
    - internal/reporter/tracesynth.go (44-03's landed signatures — pin ReconstructConversation's exact param list before wiring)
    - cmd/manager/main.go lines ~256-295 (the TracerProvider + deferred bounded Shutdown pattern to transplant — INSIDE runWithClient, never beside os.Exit in main(); Pitfall 5)
  </read_first>
  <action>
    1. parseFlags: register `fs.Bool("trace-only", false, "synthesize LLM message-array spans from events.jsonl only; no child-CR materialization (Phase 44 MSG-01)")`; add `TraceOnly bool` to reporterConfig. Same commit as any Args emission is already satisfied — 44-02 emits the Arg only for Jobs 44-05 spawns later (flag lands first; crash-loop hazard excluded).

    2. Introduce a package-level seam `var newTracerProvider = otelinit.NewTracerProvider` (test override point — otelinit reads env and sets the global provider, so tests can't inject an exporter through it; the seam mirrors buildFakeClient's "construct only what the test needs" minimalism). runWithClient's FIRST action (before flag validation, so every subsequent exit path is covered): call newTracerProvider(ctx); on error, log to stderr and continue with tracing disabled (D-10 — otel init failure must not fail materialization; use the no-op path semantics). Immediately `defer` the bounded shutdown: context.WithTimeout(context.Background(), 5*time.Second) — the D-12 constant, mirroring the manager — then otelShutdown(shutdownCtx), logging (never failing) on error. The defer lives one level below main()'s os.Exit, so every existing early return (missing flags → exitInvariant, client build → exitGenericFail, out.json read → exitInvariant, materialize → exitInvariant/exitGenericFail, success) flushes (TRACE-03).

    3. New shared helper `synthesizeSpans(ctx context.Context, cfg reporterConfig, stderr io.Writer)` — best-effort, returns nothing: derive eventsPath = filepath.Join(cfg.Workspace, "envelopes", cfg.TaskUID, "events.jsonl") and inJSONPath = filepath.Join(cfg.Workspace, "envelopes", cfg.TaskUID, "in.json"); parentCtx = otelai.ExtractRemoteParent(ctx, cfg.TraceParent) (empty/malformed traceparent → spans emit without the deterministic parent — bounded degradation, log a notice); tracer = otel.Tracer("tide.reporter") (the global provider newTracerProvider set — no-op when tracing is dark, which is D-06's natural materialization-mode no-op); calls, err := reporter.ReconstructConversation(...); on error log to stderr and return; reporter.EmitSpans(parentCtx, tracer, calls, "envelopes/"+cfg.TaskUID+"/events.jsonl"); on error log to stderr and return. NEVER influences any exit code (D-10 — extends the "observability must not gate" precedent; note in the doc comment that reporter-synthesized span IDs mint fresh per run, so exit-0 also avoids the Job-retry duplicate-span problem).

    4. Trace-only branch in runWithClient, placed after TP init: `if cfg.TraceOnly` — validate ONLY cfg.TaskUID (missing → stderr + exitInvariant; the parent-CR flags are not required and no K8s client is built); call synthesizeSpans; return exitSuccess unconditionally (D-10: a pure trace-only run exits 0 regardless of synth/export outcome).

    5. Combined-mode synth step: in the non-trace-only path, call synthesizeSpans after the existing flag validation block but BEFORE the client build (step 2) — placement is load-bearing: failed planner Jobs may have no readable out.json (exitInvariant at step 3), and D-05 requires their conversation spans to still emit; synth needs no K8s client, so it runs before any path that can exit. Materialization flow after it is byte-identical to today.

    6. Update the package doc comment: new exit-code note (trace-only always exits 0; synth/export failures in combined runs never alter the materialization exit code — a NEW class distinct from the existing invariant checks), and the two-mode description.
  </action>
  <verify>
    <automated>go build ./cmd/tide-reporter/... && go test ./cmd/tide-reporter/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fs.Bool("trace-only"' cmd/tide-reporter/main.go` returns 1
    - `grep -c 'var newTracerProvider = otelinit.NewTracerProvider' cmd/tide-reporter/main.go` returns 1
    - `grep -c '5\*time.Second' cmd/tide-reporter/main.go` returns ≥1 (D-12 bound), and the defer containing it is inside runWithClient (not main) — `grep -n 'defer' cmd/tide-reporter/main.go` shows it after runWithClient's opening
    - synthesizeSpans call appears BEFORE the client-build section in the combined path — verifiable by line order in the file
    - Existing tests (TestRunHappyPath, TestParseFlags*) still pass unmodified
  </acceptance_criteria>
  <done>tide-reporter has both modes: trace-only (exit-0-always, task-uid-only validation) and combined (synth-before-materialization, exit codes unchanged), with the TP lifecycle covering every exit path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shutdown-on-every-exit-path + trace-only mode tests</name>
  <files>cmd/tide-reporter/main_test.go</files>
  <read_first>
    - cmd/tide-reporter/main_test.go (buildFakeClient, writeOutJSON helpers + TestRunHappyPath style — the harness to extend)
    - cmd/tide-reporter/main.go (post-Task-1: the newTracerProvider seam signature and both mode paths)
    - internal/reporter/testdata/events_sample.jsonl and in_planner.json (44-03's fixtures — copy the SHAPE into t.TempDir-built workspaces here, or read them via a relative path if the test builds its workspace by copying; do not add new secret shapes)
  </read_first>
  <behavior>
    - Test 1 (TestRunWithClient_ShutdownOnEveryExitPath): table-driven over exit paths — (a) missing --task-uid → exitInvariant, (b) trace-only success → exitSuccess, (c) combined with fake client + missing out.json → exitInvariant, (d) combined happy path (buildFakeClient + writeOutJSON) → exitSuccess. For each: override newTracerProvider with a stub returning a TracerProvider built on sdktrace.NewTracerProvider(sdktrace.WithSyncer(recordingExporter)) and a shutdown func recording (invoked bool, hadDeadline bool); assert after runWithClient returns that invoked==true and hadDeadline==true (the D-12 bound is real) — on EVERY path
    - Test 2 (TestRunTraceOnly_EmitsSpans): t.TempDir workspace with envelopes/<uid>/events.jsonl + in.json (44-03 fixture shape); cfg{TraceOnly: true, TaskUID: uid, Workspace: tmp, TraceParent: a valid W3C string built via otelai.FormatTraceparent}; seam-injected recording exporter → runWithClient returns exitSuccess AND the recorder holds ≥1 span whose TraceID matches the traceparent's TraceID (parenting through ExtractRemoteParent proven)
    - Test 3 (TestRunTraceOnly_MissingEventsStillExitsZero): TraceOnly with an empty workspace → exitSuccess, zero spans, error line on stderr (D-10)
    - Test 4 (TestRunCombined_SynthFailureDoesNotChangeExit): combined happy path with NO events.jsonl present → exit code identical to TestRunHappyPath's (synth logged, materialization outcome authoritative)
  </behavior>
  <action>
    Extend main_test.go with an inline stub TracerProvider constructor matching the newTracerProvider seam: returns sdktrace.NewTracerProvider(sdktrace.WithSyncer(exp)) where exp is a minimal inline recording SpanExporter (records ExportSpans batches; no shared test-double package), plus a wrapped shutdown func that records it was invoked and whether its ctx carried a deadline. Use t.Cleanup to restore the seam and the global otel provider after each case (mirror setupSpanExporter's prev-provider restore discipline from internal/controller/span_emission_unit_test.go). Note in a comment why WithSyncer is used here despite TRACE-03 being about the async batch path: the seam test proves Shutdown is INVOKED with a bounded ctx on every path (the discipline), while the SDK's own contract guarantees Shutdown drains the batch queue — the batch-drain behavior itself is covered by 44-03's TestEmitSpans_BatchAggregateUnderCeiling ForceFlush/Shutdown path. Build trace-only workspaces in t.TempDir by writing fixture-shaped events.jsonl/in.json content (small — 2 calls is enough; reuse 44-03's fixture text where convenient).
  </action>
  <verify>
    <automated>go test ./cmd/tide-reporter/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestRunWithClient_ShutdownOnEveryExitPath covers ≥4 distinct exit paths and asserts both invoked==true and hadDeadline==true per path
    - TestRunTraceOnly_EmitsSpans asserts the emitted span's TraceID equals the TraceID embedded in the injected traceparent string
    - TestRunTraceOnly_MissingEventsStillExitsZero asserts exitSuccess with an empty workspace
    - `go test ./cmd/tide-reporter/ -count=1` exits 0 (all 8+ pre-existing tests plus the new ones)
  </acceptance_criteria>
  <done>Every runWithClient exit path provably invokes the bounded Shutdown; trace-only mode emits parented spans and honors the D-10 exit-0 contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reporter process lifetime → OTLP collector | Spans buffered in the batch processor must survive process exit (or be lost silently) |
| --traceparent Arg (manager-controlled) → span parenting | Malformed input must degrade, never crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Denial of Service (pipeline wedge) | otelShutdown against a dead collector | mitigate | D-12 bounded 5s context on every Shutdown call — a hung collector delays reporter exit by at most 5s, drop is logged |
| T-44-09 | Denial of Service | span loss on os.Exit | mitigate | TRACE-03: deferred Shutdown inside runWithClient (one level below os.Exit), proven on every exit path by test |
| T-44-10 | Tampering | malformed --traceparent | mitigate | otelai.ExtractRemoteParent never panics; invalid input → unparented spans (bounded degradation), logged |
| T-44-11 | Repudiation/Integrity | Job retries re-emitting duplicate conversation spans | mitigate | D-10 exit-0 posture: synth/export failure never fails the Job, so K8s backoff never re-runs a successful materialization purely to retry spans |
| T-44-SC | Tampering | package installs | accept | Zero new packages this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `go build ./cmd/tide-reporter/...` exits 0
- `go test ./cmd/tide-reporter/ -count=1` exits 0
- `golangci-lint run ./cmd/tide-reporter/...` introduces 0 new issues
</verification>

<success_criteria>
- TRACE-03 phase-gate criterion met: TracerProvider construction + deferred bounded Shutdown on every exit path, test-proven
- --trace-only mode functional end-to-end against fixture workspaces, exit 0 always
- Combined runs synthesize before materialization with unchanged exit-code semantics (D-02/D-05/D-10)
</success_criteria>

<output>
Create `.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-04-SUMMARY.md` when done.
</output>
