---
phase: 44-llm-message-array-spans-d-o5-redaction-size-boundary
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - internal/reporter/tracesynth.go
  - internal/reporter/tracesynth_test.go
  - internal/reporter/testdata/events_sample.jsonl
  - internal/reporter/testdata/events_truncated.jsonl
  - internal/reporter/testdata/in_planner.json
  - internal/reporter/testdata/in_executor.json
  - internal/reporter/testdata/children/task-01.json
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03]
tags: [opentelemetry, openinference, events-jsonl, redaction, size-boundary, reporter]

must_haves:
  truths:
    - "ReconstructConversation turns a real-schema events.jsonl into one CallSpan per message_start..message_stop cycle, seeded with the in.json/PromptPath prompt as conversation turn 0 (D-01, Pitfall 2)"
    - "A secret planted in a fixture events.jsonl never appears in any emitted span attribute, even when it straddles the truncation cut (MSG-02, D-09)"
    - "An oversized single message truncates head+tail with an explicit marker and the span carries ArtifactPath(events.jsonl) (MSG-03, D-08)"
    - "A ~32-call real-sized batch exports in chunks of ≤6 spans per Export RPC with per-batch attribute bytes under the 4 MB ceiling margin (MSG-03, the real aggregate risk)"
    - "A truncated/malformed events.jsonl still emits whatever conversation is reconstructable, stamped tide.trace.parse_degraded=true (D-05/D-11)"
  artifacts:
    - path: "internal/reporter/tracesynth.go"
      provides: "CallSpan, ReconstructConversation, EmitSpans — the events.jsonl→LLM-span synthesizer"
      contains: "func ReconstructConversation"
      min_lines: 200
    - path: "internal/reporter/testdata/events_sample.jsonl"
      provides: "Synthetic real-schema fixture: 3 API-call cycles, tool_use + thinking blocks, tool_result with a planted pattern-matching fake secret"
  key_links:
    - from: "internal/reporter/tracesynth.go"
      to: "internal/harness/redact"
      via: "redact.String applied to every message content BEFORE truncation"
      pattern: "redact\\.String"
    - from: "internal/reporter/tracesynth.go"
      to: "pkg/otelai"
      via: "LLMInputMessages/LLMOutputMessages/TokenCount/ArtifactPath/LLMSpanKind/TimingSynthetic/ParseDegraded"
      pattern: "otelai\\.LLM(Input|Output)Messages"
    - from: "internal/reporter/tracesynth.go"
      to: "internal/subagent/common"
      via: "common.ReadLines for the tolerant JSONL walk (16 MB per-line budget)"
      pattern: "common\\.ReadLines"
---

<objective>
The phase's core: `internal/reporter/tracesynth.go`, the synthesizer that reads a completed dispatch's `events.jsonl` (plus `in.json` for the out-of-band call-1 prompt) and emits one redacted, size-bounded OpenInference LLM-kind span per API call. This is the milestone's headline capability (MSG-01) with its two safety properties: redact-before-truncate (MSG-02/D-09, non-negotiable) and the triple size guard (MSG-03/D-07/D-08: 32 KiB per-message head+tail floor, 512 KiB whole-span budget, batch-size-6 export chunking validated by test).

**EXECUTION GATE (phase-wide):** requires `worktree-phase-43-discuss` merged into `main` first (this plan consumes `pkg/otelai` Phase-42 helpers plus plan 44-01's extensions; parenting context arrives via the `--traceparent` Arg Phase 43 wired into `BuildReporterJob`).

**Phoenix double-count note (routed, do not fix here):** per-call `TokenCount` on LLM spans (D-04, locked) may double-count in Phoenix's cumulative rollups against the pre-summed totals Phase 42 shipped on AGENT spans. Disposition: keep D-04 as locked, do NOT touch Phase 42's AGENT-span TokenCount call sites; the risk is routed to Phase 46 (OBS-02 owns session-level cost-rollup correctness) and Phase 47's live proof verifies empirically. Record this note verbatim in the SUMMARY.

Purpose: the Task level's full LLM conversation — the richest data in the system — gets its first in-namespace observability consumer, safely.
Output: `tracesynth.go` + committed synthetic fixtures + the full MSG-01/02/03 unit-test battery including the batch-aggregate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-CONTEXT.md  (D-01..D-12 — all locked decisions land in this plan's code)
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-RESEARCH.md  (§"events.jsonl Schema", §"Size-Boundary Model", §"Recommended tracesynth.go Shape", §"Code Examples", §Pitfalls 1-4)
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-PATTERNS.md  (§tracesynth.go composite analog, §tracesynth_test.go, §Shared Patterns)

<interfaces>
Consumed contracts (all on main post-43-merge + post-44-01):

pkg/otelai (44-01 extended):
- `Message{Role, Content string; ToolCalls []ToolCall; Contents []MessageContent}` — ToolCall{ID, Name, ArgumentsJSON}, MessageContent{Type, Text, Signature}
- `LLMInputMessages(msgs []Message)`, `LLMOutputMessages(msgs []Message)`, `TokenCount(prompt, completion, cacheRead, cacheWrite int)` (pre-sum semantics: prompt INCLUDES cacheRead+cacheWrite), `ArtifactPath(path string)`, `LLMIdentity(provider, model string)`, `LLMSpanKind()`, `TimingSynthetic()`, `ParseDegraded()`

internal/harness/redact (44-01): `String(s string) string`

internal/subagent/common/stream_reader.go:57: `ReadLines(r io.Reader, handler func(line []byte) error) error` — enforces the 16 MB per-line budget; reuse, never reimplement

pkg/dispatch EnvelopeIn (envelope.go): `Prompt string \`json:"prompt"\``, `PromptPath string \`json:"promptPath,omitempty"\`` (workspace-relative; executor Tasks have empty Prompt + PromptPath → `envelopes/<plannerUID>/children/task-NN.json`, whose `.spec.prompt` is the real instruction text)

Path conventions (producer: internal/subagent/anthropic/subagent.go + internal/dispatch/podjob/jobspec.go init container):
- events.jsonl: `<workspace>/envelopes/<taskUID>/events.jsonl`
- in.json:      `<workspace>/envelopes/<taskUID>/in.json`

events.jsonl real schema (verified against 58 real fixtures — RESEARCH §Architecture Patterns):
- `stream_event` lines: message_start/message_delta/message_stop + content_block_* deltas; message_start carries `message.model` and usage (input_tokens, cache_creation_input_tokens, cache_read_input_tokens); message_delta carries usage.output_tokens; NO absolute timestamps (only relative ttft_ms)
- `assistant` lines: one PER CONTENT BLOCK (thinking | tool_use | text), sharing message.id within a message_start..message_stop window — NOT one per call (Pitfall 1)
- `user` lines: tool_result turns; the ONLY event type carrying an absolute ISO-8601 `timestamp`
- `system` lines: CLI init/status noise (init carries model); `result` line: terminal summary
- The call-1 seed prompt is NEVER in the stream (passed out-of-band) — must be seeded from in.json (Pitfall 2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixtures + ReconstructConversation (MSG-01 core, D-01/D-05/D-11)</name>
  <files>internal/reporter/tracesynth.go, internal/reporter/tracesynth_test.go, internal/reporter/testdata/events_sample.jsonl, internal/reporter/testdata/events_truncated.jsonl, internal/reporter/testdata/in_planner.json, internal/reporter/testdata/in_executor.json, internal/reporter/testdata/children/task-01.json</files>
  <read_first>
    - internal/reporter/materialize.go (package doc / import-safety contract — tracesynth.go copies the "no internal/controller back-edge, import-safe from cmd binaries" framing verbatim; error-wrapping convention fmt.Errorf("FuncName: ...: %w", err))
    - internal/subagent/anthropic/stream_parser.go (the events.jsonl producer's schema comments + the tolerant unmarshal-error→skip posture ReconstructConversation mirrors; the streamEvent struct shapes to model the reader types on)
    - internal/subagent/common/stream_reader.go (ReadLines signature + 16 MB budget)
    - examples/projects/dogfood/salvage-20260618/pvc-envelopes/envelopes/ (spot-read ONE real events.jsonl head to confirm field spellings — do NOT copy real fixtures into testdata; RESEARCH Wave-0 gap explicitly forbids it: unknown provenance for CI + the redaction test needs a KNOWN planted secret)
    - .planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-RESEARCH.md (§"Reconstruction algorithm" + §"Verified pattern: conversation reconstruction" — the exact walk to implement; do not re-derive)
  </read_first>
  <behavior>
    - Test 1 (TestReconstructConversation): events_sample.jsonl + in_planner.json → exactly 3 CallSpans; call 1's InputMessages == [{role:user, content:<in.json .prompt>}] (the seed turn); call 2's InputMessages == call 1's input + call 1's assistant turn + the tool_result user turn; each call's OutputMessages is ONE assistant message aggregating ALL its content blocks (thinking + tool_use + text — Pitfall 1: assistant events are per-block, group by message.id within the start/stop window)
    - Test 2 (TestReconstructConversation_SeedsPromptFromPromptPath): in_executor.json (empty .prompt, .promptPath="envelopes/planner-uid/children/task-01.json") → turn 0 content == children/task-01.json's .spec.prompt
    - Test 3 (TestReconstructConversation_PerCallUsage): each CallSpan.Usage carries that call's message_start input/cache tokens + message_delta output_tokens (D-04)
    - Test 4 (TestReconstructConversation_TolerantSkip): events_truncated.jsonl (ends mid-call: message_start with assistant blocks but no message_stop, plus one non-JSON garbage line) → prior complete calls returned normally, the dangling call returned as a CallSpan with Degraded=true, no error, no panic (D-05/D-11)
    - Test 5 (TestReconstructConversation_MissingInJSON): absent in.json → conversation reconstructed without a seed turn, first CallSpan marked Degraded=true (missing foundation is a degraded condition, not a failure)
  </behavior>
  <action>
    Create internal/reporter/tracesynth.go in package reporter with the RESEARCH-recommended shape:

    - `type Usage struct { InputTokens, OutputTokens, CacheReadTokens, CacheCreationTokens int }`
    - `type CallSpan struct { Model string; InputMessages, OutputMessages []otelai.Message; Usage Usage; StartTime, EndTime time.Time; Degraded bool; TimingSynthetic bool }`
    - `func ReconstructConversation(eventsPath, inJSONPath string) ([]CallSpan, error)`

    Reconstruction walk (RESEARCH "Verified pattern", exact): stream events.jsonl via common.ReadLines; unmarshal errors → skip line and set a degraded flag for the in-progress call (D-11, mirrors ParseStream's posture — never propagate). On message_start: snapshot the running conversation as the new call's InputMessages, reset the block accumulator, record message.model and input/cache usage. On assistant events: append that event's content blocks to the accumulator (group by message.id; per-block, not per-call — Pitfall 1). On message_delta: record output_tokens. On message_stop: close the call — OutputMessages = one assistant Message aggregating the accumulated blocks; append that turn to the running conversation. On user events: append a user-role turn to the conversation (content from tool_result blocks; record its absolute timestamp for timing interpolation). EOF with an open call → emit it as Degraded (mid-stream kill, D-05).

    Content-block → otelai.Message mapping (D-03 spec-native, resolved YES): text blocks concatenate into Message.Content; tool_use blocks become ToolCall{ID: block.id, Name: block.name, ArgumentsJSON: json.Marshal(block.input)}; thinking blocks become MessageContent{Type: "reasoning", Text: block.thinking, Signature: block.signature}; tool_result user-blocks stringify their content into Message.Content. Store RAW (unredacted) content in CallSpan — redaction happens at emission time in EmitSpans (Task 2), keeping exactly one redaction chokepoint.

    Seed turn 0 (Pitfall 2): read inJSONPath; if .prompt non-empty → prepend {role:user, content:.prompt}; else if .promptPath non-empty → read filepath.Join(workspace-root-derived-from-eventsPath's directory structure — pass the workspace root explicitly as a third param if cleaner) at that workspace-relative path and unmarshal {spec:{prompt}} → prepend that. Missing/unreadable in.json or prompt file → no seed turn + first call Degraded=true (never an error).

    Timing (Claude's Discretion, floor = marked-synthetic): interpolate each call's StartTime/EndTime between the nearest preceding/following user-event timestamps; when no user timestamps exist at all, leave zero times (EmitSpans falls back, Task 2) — set TimingSynthetic=true on EVERY CallSpan unconditionally (no in-band absolute timestamp exists for any call itself; verified research fact).

    File header: copy materialize.go's import-safety contract framing plus span_emission.go's "never hold a span open across a function return" constraint note. Constants get the D-XX-citation comment style (PATTERNS §materialize.go).

    Fixtures (authored from the schema doc, NEVER copied from the dogfood corpus): events_sample.jsonl — system init line, then 3 message_start..message_stop cycles with realistic interleaving (call 1: thinking + tool_use blocks then two tool_result user events with timestamps; call 2: tool_use; call 3: text-only), one tool_result whose content embeds a fake-but-pattern-matching secret (e.g. an sk-ant-api03-… string matching SecretPatterns' Anthropic regex — reuse the fixture string from redact_test.go's table); events_truncated.jsonl — same head, ends mid-call with a trailing garbage non-JSON line; in_planner.json — {"prompt": "..."} EnvelopeIn shape; in_executor.json — {"prompt": "", "promptPath": "envelopes/planner-uid/children/task-01.json"}; children/task-01.json — {"spec": {"prompt": "..."}}.

    tracesynth_test.go: plain `testing` package style (matches materialize_test.go); table/DeepEqual assertions on the reconstruction invariants listed in behavior.
  </action>
  <verify>
    <automated>go test ./internal/reporter/ -run 'TestReconstructConversation' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'common.ReadLines' internal/reporter/tracesynth.go` returns ≥1 (no hand-rolled line reader)
    - `grep -c 'internal/controller' internal/reporter/tracesynth.go` returns 0 (import-safety contract)
    - TestReconstructConversation asserts call 2's input length == call 1's input length + 2 or more (assistant turn + tool_result growth — the D-01 snapshot semantics)
    - TestReconstructConversation_TolerantSkip passes against a fixture containing a non-JSON line AND a dangling message_start
    - `go test ./internal/reporter/ -count=1` exits 0 (materialize tests unaffected)
  </acceptance_criteria>
  <done>ReconstructConversation produces per-call CallSpans matching the verified real schema, seeds turn 0 from in.json/PromptPath, tolerates truncation/garbage with Degraded markers, and never errors on malformed input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EmitSpans — redact-then-truncate pipeline + triple size guard + batch-aggregate proof (MSG-02/MSG-03)</name>
  <files>internal/reporter/tracesynth.go, internal/reporter/tracesynth_test.go</files>
  <read_first>
    - internal/controller/span_emission.go (the tracer.Start + SetAttributes + SetStatus + End(WithTimestamp) shape EmitSpans mirrors per CallSpan; the spanEndTime defensive-nil-check style)
    - internal/controller/span_emission_unit_test.go (setupSpanExporter — tracetest.InMemoryExporter + sdktrace.WithSyncer swap harness, and the attrValue lookup helper — copy both verbatim into tracesynth_test.go)
    - pkg/otelai/attrs.go (post-44-01: the exact helper signatures EmitSpans calls)
    - internal/harness/redact/redact.go (String — the D-09-first call)
  </read_first>
  <behavior>
    - Test 1 (TestEmitSpans_SpanShape): 3-call reconstruction → 3 spans, each with openinference.span.kind=LLM, llm.provider="anthropic", llm.model_name from that call's message_start, llm.token_count.* per-call (prompt pre-summed = input+cacheRead+cacheWrite), tide.artifact_path="envelopes/<taskUID>/events.jsonl", tide.trace.timing_synthetic=true, span name == the call's model string
    - Test 2 (TestEmitSpans_Redacts): the planted fixture secret appears in ZERO attribute values across ALL emitted spans (iterate every attribute of every span; assert no substring match on the secret's distinctive tail)
    - Test 3 (TestEmitSpans_RedactsBeforeTruncate): a synthetic message whose planted secret STRADDLES the 32 KiB truncation cut (secret positioned so head+tail elision would split it) still shows zero secret bytes in the emitted attribute — proving order, not just outcome (D-09; a lucky non-split test cannot distinguish correct ordering)
    - Test 4 (TestEmitSpans_TruncatesOversizedMessage): a >32 KiB single message emits content of ~32 KiB shaped head(≈16 KiB) + marker + tail(≈16 KiB), marker contains the elided byte count (D-08), and the span carries tide.artifact_path
    - Test 5 (TestEmitSpans_WholeSpanBudget): a call whose redacted+truncated input side still sums >512 KiB degrades that side to ArtifactPath-only (role attributes may remain; content attributes absent) with a tide.trace.parse_degraded or dedicated marker per the action's rule — output side unaffected
    - Test 6 (TestEmitSpans_DegradedMarker): a Degraded CallSpan's span carries tide.trace.parse_degraded=true (D-11)
    - Test 7 (TestEmitSpans_BatchAggregateUnderCeiling): ~32 real-sized (NOT oversized — Pitfall 3) CallSpans with input contexts growing to ~300 KiB, exported through sdktrace.NewTracerProvider(sdktrace.WithBatcher(stub)) after t.Setenv("OTEL_BSP_MAX_EXPORT_BATCH_SIZE", "6") — a custom recording SpanExporter stub counts Export() calls: every batch has ≤6 spans AND per-batch summed attribute-value bytes < 3.5 MiB; total spans across batches == 32
  </behavior>
  <action>
    Add to tracesynth.go:

    - Size constants with citation comments (RESEARCH §Size-Boundary Model): `maxMessageContentBytes = 32 * 1024` (positioned ~1 KiB above the real p99 single-turn size of 31,020 B — a pathological-input backstop, near-never fires on real content), `maxSpanPayloadBytes = 512 * 1024` (real max observed per-call payload 334 KiB — bounds future growth), head/tail split 16 KiB each (D-08).
    - `func truncateHeadTail(s string, limit int) string` — when len(s) > limit: first limit/2 bytes + fmt marker "[... N bytes truncated by TIDE ...]" + last limit/2 bytes; byte-safe (splitting mid-UTF-8-rune is acceptable for a truncation backstop; note it in the comment).
    - `func EmitSpans(ctx context.Context, tracer trace.Tracer, calls []CallSpan, artifactPath string) error` — per CallSpan: start span named after CallSpan.Model (fallback "llm" when empty) with trace.WithTimestamp(StartTime; fallback time.Now() when zero); for EVERY message content string, tool-call ArgumentsJSON, and reasoning text: `redact.String` FIRST, `truncateHeadTail(..., maxMessageContentBytes)` SECOND — the single most safety-critical ordering in the phase (D-09, locked; cite RESEARCH Pitfall 4); after per-message processing, if one side's (input or output) summed attribute bytes exceed maxSpanPayloadBytes, drop that side's content attributes and emit role-only messages + ArtifactPath (already present) + ParseDegraded-style marker for that span; SetAttributes: otelai.LLMSpanKind(), otelai.LLMIdentity("anthropic", call.Model) (provider constant is deliberate — this synthesizer parses the Anthropic CLI stream format; Phase 45's adapter seam is where runtime-neutral dispatch lands, note in comment), otelai.LLMInputMessages/LLMOutputMessages, otelai.TokenCount(input+cacheRead+cacheCreation, output, cacheRead, cacheCreation) (D-04 per-call counts; D-08 pre-sum), otelai.ArtifactPath(artifactPath) on EVERY span (superset of MSG-03's truncation-time requirement — the full-fidelity pointer is always useful), otelai.TimingSynthetic() always, otelai.ParseDegraded() when call.Degraded; span status Ok; End with WithTimestamp(EndTime; fallback). LLM spans are flat SIBLINGS under ctx's remote parent (mirrors native openinference-instrumentation-langchain nesting — runtime-neutrality lock). Never hold a span open across a return.
    - Errors: EmitSpans returns error only for programming-level failures; content-level problems degrade to markers (D-10/D-11 posture — callers log and continue).

    Tests: copy setupSpanExporter + attrValue verbatim from span_emission_unit_test.go (WithSyncer for attribute tests — synchronous export, no flush needed). For Test 7 write an inline minimal `recordingExporter` implementing sdktrace.SpanExporter (records each ExportSpans batch's span count + summed attribute-value byte length; no shared test-double package — PATTERNS "construct only what the test needs"); t.Setenv BEFORE constructing the TracerProvider (NewBatchSpanProcessor reads the env at construction); end all spans, then ForceFlush/Shutdown and assert batch invariants.
  </action>
  <verify>
    <automated>go test ./internal/reporter/ -run 'TestEmitSpans' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - In tracesynth.go, every content path shows redact.String on the line(s) BEFORE truncateHeadTail — `grep -n 'redact.String\|truncateHeadTail' internal/reporter/tracesynth.go` output ordering demonstrates it, and TestEmitSpans_RedactsBeforeTruncate proves it behaviorally with a straddling secret
    - `grep -c 'maxMessageContentBytes = 32 \* 1024' internal/reporter/tracesynth.go` returns 1; `grep -c 'maxSpanPayloadBytes = 512 \* 1024' internal/reporter/tracesynth.go` returns 1
    - TestEmitSpans_BatchAggregateUnderCeiling constructs ≥30 spans with realistic (non-oversized) payloads and asserts ≤6 spans per Export batch (Pitfall 3: batch-aggregate, not single-fat-span)
    - `go test ./internal/reporter/ -count=1` exits 0
    - `golangci-lint run ./internal/reporter/...` introduces 0 new issues
  </acceptance_criteria>
  <done>EmitSpans emits redacted, bounded, spec-native LLM spans with all locked-decision markers; the redact-before-truncate order and the aggregate-batch ceiling are both behaviorally proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw events.jsonl (PVC, deliberately unredacted at source) → external OTLP collector | MSG-02's entire reason to exist — model-echoed repo secrets must never cross this boundary |
| adversarial/corrupted JSONL → reporter process | Untrusted file input parsed by the synthesizer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure | EmitSpans content pipeline | mitigate | redact.String (SecretPatterns) applied to every message content, tool-call args, and reasoning text BEFORE any attribute assignment; behaviorally tested with a planted secret |
| T-44-02 | Information Disclosure | truncation cut splitting a secret | mitigate | D-09 locked ordering redact-BEFORE-truncate + TestEmitSpans_RedactsBeforeTruncate straddle test proving order not just outcome |
| T-44-03 | Denial of Service | ReconstructConversation on malformed/hostile JSONL | mitigate | common.ReadLines 16 MB per-line budget + D-11 tolerant-skip (never panic/hang; unparseable lines skipped with degraded marker) |
| T-44-04 | Denial of Service (tracing pipeline) | span payload volume | mitigate | Triple guard: 32 KiB per-message + 512 KiB whole-span + batch-size-6 export chunking (44-02's env var), each independently tested |
| T-44-08 | Information Disclosure | ArtifactPath attribute value | mitigate | Value is the workspace-relative PVC path string ("envelopes/<uid>/events.jsonl"), never file content — the raw file reference stays PVC-internal; consumers fetch via tide artifact-get inside the cluster |
| T-44-SC | Tampering | package installs | accept | Zero new packages this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `go test ./internal/reporter/ -count=1` exits 0 (reconstruction + emission + regression on materialize tests)
- `go vet ./internal/reporter/...` clean
- Fixture hygiene: `grep -rc 'salvage-20260618' internal/reporter/testdata/` returns 0 (no real-corpus imports)
</verification>

<success_criteria>
- One LLM span per API call (D-01 validated as locked), seeded call-1 context, spec-native tool-call/reasoning encoding
- Planted secret never reaches any span attribute, including across truncation cuts
- All three size guards implemented at the researched constants and behaviorally tested, including the aggregate-batch test (the REAL risk)
- Degraded/synthetic-timing markers present per D-05/D-11 and the timing-discretion floor
</success_criteria>

<output>
Create `.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-03-SUMMARY.md` when done.
</output>
