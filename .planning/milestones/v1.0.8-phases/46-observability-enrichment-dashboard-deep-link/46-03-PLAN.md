---
phase: 46-observability-enrichment-dashboard-deep-link
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/dashboard/main.go
  - cmd/dashboard/router.go
  - cmd/dashboard/api/config.go
  - cmd/dashboard/api/config_test.go
  - cmd/dashboard/api/projects.go
  - cmd/dashboard/api/projects_test.go
  - cmd/dashboard/api/tasks.go
  - cmd/dashboard/api/tasks_test.go
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "GET /api/v1/config returns phoenixBaseURL beside telemetryEnabled — additive JSON field, telemetryEnabled semantics byte-identical (locked wire contract, D-10)"
    - "PHOENIX_BASE_URL env absent → phoenixBaseURL is \"\" — empty string IS the no-link sentinel; the server sends the RAW config value (trailing-slash normalization is SPA-side, in lib/phoenixLink.ts — exactly one place, D-11)"
    - "GET /api/v1/projects/{name} (projectDetail) carries traceId (deterministic, otelai.TraceIDFromUID) + traceSpanId (Status.ProjectTraceSpanID), and every childRef carries traceSpanId from its CR's {Level}TraceSpanID status field"
    - "GET /api/v1/tasks/{name} (taskDetail) carries traceId + traceSpanId; a broken Task→…→Project resolution chain degrades to empty strings, never a 500 (observability never gates)"
    - "The SPA never computes trace IDs — the Go server derives traceId and reads span IDs from CRD status (UI-SPEC data contract)"
  artifacts:
    - path: "cmd/dashboard/api/config.go"
      provides: "ConfigHandler.PhoenixBaseURL + configResponse.phoenixBaseURL"
      contains: "phoenixBaseURL"
    - path: "cmd/dashboard/api/projects.go"
      provides: "projectDetail.TraceID/TraceSpanID + childRef.TraceSpanID"
      contains: "traceId"
    - path: "cmd/dashboard/api/tasks.go"
      provides: "taskDetail.TraceID/TraceSpanID with graceful chain degradation"
      contains: "traceSpanId"
  key_links:
    - from: "cmd/dashboard/main.go"
      to: "cmd/dashboard/api/config.go"
      via: "PHOENIX_BASE_URL env → Dependencies.PhoenixBaseURL → ConfigHandler"
      pattern: "PHOENIX_BASE_URL"
    - from: "cmd/dashboard/api/projects.go"
      to: "pkg/otelai/tracecontext.go"
      via: "otelai.TraceIDFromUID(project.UID) — K8s-import-free by design, dashboard may import it (D-11)"
      pattern: "TraceIDFromUID"
---

<objective>
Carry trace identity and the Phoenix base URL through the dashboard's Go backend: the `PHOENIX_BASE_URL` config chain (the telemetryEnabled precedent applied verbatim) and new `traceId`/`traceSpanId` fields on the projectDetail/childRef/taskDetail payloads — the D-11 plumbing that is genuinely new (zero TraceSpanID references exist in cmd/dashboard/ today).

Purpose: OBS-04's server half. Plan 46-05 (SPA) consumes these exact field names as TS mirrors.
Output: config chain + payload trace identity, with handler tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-CONTEXT.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-RESEARCH.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-PATTERNS.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-UI-SPEC.md
</context>

<interfaces>
Locked wire-contract additions Plan 46-05's TS mirrors will consume — additive-only, camelCase, do not rename:

- `GET /api/v1/config` → `{ "telemetryEnabled": bool, "phoenixBaseURL": string }`
- `projectDetail` → `+ "traceId": string (omitempty), + "traceSpanId": string (omitempty)`
- `childRef` → `+ "traceSpanId": string (omitempty)` (traceId is run-wide — carried once on projectDetail, not per child)
- `taskDetail` → `+ "traceId": string (omitempty), + "traceSpanId": string (omitempty)`

Existing interfaces consumed (verified this session):
- `otelai.TraceIDFromUID(uid string) (trace.TraceID, error)` — pkg/otelai/tracecontext.go, K8s-import-free; hex via .String()
- `{Level}TraceSpanID` status fields on all five api/v1alpha3 types (Phase 43 PROP-02)
- `resolveTaskParents(ctx, task)` in cmd/dashboard/api/tasks.go returns (planName, projectName) strings — walks Task → Plan → Phase → Milestone → Project via Spec.*Refs
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: phoenixBaseURL config chain (telemetryEnabled precedent, applied verbatim)</name>
  <files>cmd/dashboard/main.go, cmd/dashboard/router.go, cmd/dashboard/api/config.go, cmd/dashboard/api/config_test.go</files>
  <read_first>
    - cmd/dashboard/api/config.go (whole file, ~57 lines — ConfigHandler struct, configResponse, Get handler; the locked telemetryEnabled wire contract phoenixBaseURL lands beside)
    - cmd/dashboard/api/config_test.go (existing handler test shape to extend)
    - cmd/dashboard/main.go (telemetryEnabledFromEnv lines ~243-259 + Dependencies{...} construction lines ~159-169)
    - cmd/dashboard/router.go (Dependencies struct fields lines ~74-95 + configHandler wiring lines ~212-215)
  </read_first>
  <action>
    D-10, RESEARCH Pattern 3 — byte-for-byte precedent, simplified (no three-state legacy fallback):
    1. main.go: add `phoenixBaseURLFromEnv() string` returning os.Getenv("PHOENIX_BASE_URL") verbatim (doc comment: empty string IS the no-link sentinel; the raw value is passed through — trailing-slash normalization happens exactly once, SPA-side in lib/phoenixLink.ts per D-11). Wire into the Dependencies literal.
    2. router.go: add `PhoenixBaseURL string` field to Dependencies (doc-comment style matching PrometheusEndpoint's) and pass `PhoenixBaseURL: deps.PhoenixBaseURL` into the ConfigHandler literal.
    3. api/config.go: add `PhoenixBaseURL string` to ConfigHandler and `PhoenixBaseURL string json:"phoenixBaseURL"` to configResponse; encode it in Get. telemetryEnabled field, semantics, and ordering untouched (locked wire contract — additive only).
    4. config_test.go: extend with cases — env-backed value present in the JSON body; unset → `"phoenixBaseURL":""`; telemetryEnabled assertions unchanged and still passing.
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/... -run 'TestConfig' -v</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/config handler test proves the body contains "phoenixBaseURL" with the configured value, and "" when unset
    - Existing telemetryEnabled test cases pass unmodified
    - No normalization/mutation of the env value anywhere server-side (source assertion: phoenixBaseURLFromEnv is a bare os.Getenv passthrough)
  </acceptance_criteria>
  <done>The config chain resolves chart env → /api/v1/config additively, raw-value passthrough, proven by handler tests.</done>
</task>

<task type="auto">
  <name>Task 2: Trace identity on projectDetail/childRef/taskDetail payloads</name>
  <files>cmd/dashboard/api/projects.go, cmd/dashboard/api/projects_test.go, cmd/dashboard/api/tasks.go, cmd/dashboard/api/tasks_test.go</files>
  <read_first>
    - cmd/dashboard/api/projects.go (projectSummary/projectDetail/childRef structs lines ~68-105; the Get handler that builds projectDetail — it already lists Milestone/Phase/Plan CRs to build childRefs, so their status fields are in hand)
    - cmd/dashboard/api/tasks.go (taskDetail struct lines ~72-88; Get handler + resolveTaskParents lines ~176+ — the graceful chain-degradation contract to preserve)
    - cmd/dashboard/api/projects_test.go + tasks_test.go (fake-client handler test conventions to extend)
    - pkg/otelai/tracecontext.go (TraceIDFromUID doc contract — deterministic, K8s-free)
  </read_first>
  <action>
    D-11 server-derived trace identity (the SPA only assembles URLs from parts it is handed):
    1. projects.go: add `TraceID string json:"traceId,omitempty"` and `TraceSpanID string json:"traceSpanId,omitempty"` to projectDetail; add `TraceSpanID string json:"traceSpanId,omitempty"` to childRef. In the Get handler: compute traceId via otelai.TraceIDFromUID(string(project.UID)).String() (on error: leave empty — never fail the request); set projectDetail.TraceSpanID from project.Status.ProjectTraceSpanID; populate each childRef.TraceSpanID from the listed CR's MilestoneTraceSpanID/PhaseTraceSpanID/PlanTraceSpanID respectively. Import pkg/otelai (K8s-import-free by design — sanctioned for the dashboard server by D-11).
    2. tasks.go: add `TraceID string json:"traceId,omitempty"` and `TraceSpanID string json:"traceSpanId,omitempty"` to taskDetail. TraceSpanID = tk.Status.TaskTraceSpanID (direct). TraceID needs the Project's UID: after resolveTaskParents yields projectName, when non-empty Get the Project CR (task's namespace) and compute TraceIDFromUID; ANY failure (empty projectName, Get error, derive error) degrades to empty strings on both fields — extend the handler's existing "resolution-chain breaks degrade gracefully" contract, never a 500.
    3. Tests: extend projects_test.go — a fixture Project/Milestone/Phase/Plan set with populated {Level}TraceSpanID status fields yields the expected traceId (assert deterministic derivation: same UID twice → same hex) and per-child traceSpanId; a fixture with EMPTY status fields yields omitted/empty JSON fields. Extend tasks_test.go — populated chain yields both fields; a Task whose Plan ref is missing yields empty trace fields and HTTP 200.
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/... -v</automated>
  </verify>
  <acceptance_criteria>
    - projectDetail JSON contains traceId + traceSpanId and each childRef contains traceSpanId when status fields are set (handler test assertion)
    - Empty status fields → fields omitted (omitempty) — no fabricated identity
    - taskDetail broken-chain fixture returns 200 with empty trace fields (behavior assertion in tasks_test.go)
    - `go vet ./cmd/dashboard/...` clean; zero new API endpoints (existing GET surfaces only, DASH-05 zero-mutation contract preserved)
  </acceptance_criteria>
  <done>Every payload backing the two link mount points carries server-derived trace identity with graceful degradation, proven by fake-client handler tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → browser | PHOENIX_BASE_URL config value crosses to the SPA via GET /api/v1/config |
| CRD status → browser | trace/span ID hex strings cross into JSON payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing | phoenixBaseURL passthrough to the SPA | accept | operator-controlled Helm value (same trust tier as otel.exporter.endpoint); served over the existing GET-only /api/v1/config (V4: DASH-05 zero-mutation contract unchanged); JSON encoding auto-escapes |
| T-46-02 | Tampering | trace/span IDs in payloads | mitigate | IDs are server-derived (TraceIDFromUID + CRD status reads) — never client input; empty-on-failure, no reflection of request data into the fields |
| T-46-SC | Tampering | package installs | accept | zero new packages (pkg/otelai is an in-repo import) |
</threat_model>

<verification>
- `go test ./cmd/dashboard/...` green
- `go build ./cmd/dashboard/` clean
</verification>

<success_criteria>
- OBS-04's server half complete: config chain + trace identity on exactly the payloads the two mount points read (projectDetail/childRef for NodeDetailPanel, taskDetail for TaskDetailDrawer)
- Locked /api/v1 wire contracts extended additively only
</success_criteria>

<output>
Create `.planning/phases/46-observability-enrichment-dashboard-deep-link/46-03-SUMMARY.md` when done.
</output>
