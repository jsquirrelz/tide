---
phase: 46-observability-enrichment-dashboard-deep-link
plan: 05
type: execute
wave: 2
depends_on: ["46-03"]
files_modified:
  - dashboard/web/src/lib/phoenixLink.ts
  - dashboard/web/src/lib/phoenixLink.test.ts
  - dashboard/web/src/components/PhoenixTraceLink.tsx
  - dashboard/web/src/lib/api.ts
  - dashboard/web/src/App.tsx
  - dashboard/web/src/components/TaskDetailDrawer.tsx
  - dashboard/web/src/components/__tests__/node-panel-integration.test.tsx
  - dashboard/web/src/components/__tests__/drawer.test.tsx
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "The 'View trace in Phoenix' link renders at BOTH mount points — NodeDetailPanel content (project/milestone/phase/plan) AND TaskDetailDrawer (task) — from ONE shared component; a diff touching only NodeDetailPanel silently drops Task-level deep links (RESEARCH Pitfall 1, UI-SPEC scope)"
    - "The link renders ONLY when phoenixBaseURL AND the node's traceSpanId are both non-empty; absent either, the component returns null — no dead button, no disabled state, no placeholder row (OBS-04 requirement letter)"
    - "URL assembly lives in exactly ONE module (lib/phoenixLink.ts): phoenixSpanURL is the rendered href (span-anchored, D-12 enhancement tier); phoenixTraceURL is exported and tested as the documented floor; zero inline /redirects/ template strings anywhere else including tests (D-11, UI-SPEC consistency contract)"
    - "phoenixBaseURL is fetched from GET /api/v1/config ONCE per page load in App.tsx and passed down as props — no per-mount fetch, no loading affordance; fetch failure degrades to \"\" → hidden (UI-SPEC config consumption contract)"
    - "The anchor is a real <a href> with target=_blank rel=\"noopener noreferrer\", aria-label 'View trace in Phoenix — opens in new tab', mono 13px, underlined, ExternalLink 14px trailing icon aria-hidden — exact UI-SPEC rendered shape"
  artifacts:
    - path: "dashboard/web/src/lib/phoenixLink.ts"
      provides: "phoenixTraceURL/phoenixSpanURL — trailing-slash normalization + encodeURIComponent, the ONE URL-assembly place"
      exports: ["phoenixTraceURL", "phoenixSpanURL"]
    - path: "dashboard/web/src/components/PhoenixTraceLink.tsx"
      provides: "shared eligibility + anchor component, sole consumer of phoenixLink.ts"
      contains: "phoenix-trace-link"
    - path: "dashboard/web/src/components/TaskDetailDrawer.tsx"
      provides: "task mount point — PhoenixTraceLink after the metadata grid (edge=top)"
      contains: "PhoenixTraceLink"
  key_links:
    - from: "dashboard/web/src/App.tsx"
      to: "dashboard/web/src/components/PhoenixTraceLink.tsx"
      via: "nodePanelContent mounts + phoenixBaseURL prop from the one-shot config fetch"
      pattern: "PhoenixTraceLink"
    - from: "dashboard/web/src/components/PhoenixTraceLink.tsx"
      to: "dashboard/web/src/lib/phoenixLink.ts"
      via: "phoenixSpanURL(baseURL, spanId) href"
      pattern: "phoenixSpanURL"
    - from: "dashboard/web/src/lib/api.ts"
      to: "cmd/dashboard/api/projects.go"
      via: "TS type mirrors of traceId/traceSpanId (Plan 46-03 wire contract)"
      pattern: "traceSpanId"
---

<objective>
Render the OBS-04 deep link: the `phoenixLink.ts` URL module, the shared `<PhoenixTraceLink>` component, and its two mount points (NodeDetailPanel content in App.tsx for the four planner kinds; TaskDetailDrawer for tasks), wired to the Plan 46-03 config field and payload trace identity.

Purpose: the operator-visible half of OBS-04 — one click from any DAG node's detail surface to its Phoenix span.
Output: two new SPA files, four extended files, Vitest coverage for render/hide at both mounts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-CONTEXT.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-UI-SPEC.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-PATTERNS.md
</context>

<interfaces>
Consumed from Plan 46-03 (Go wire contract — mirror verbatim in api.ts):

- `GET /api/v1/config` → `{ telemetryEnabled: boolean, phoenixBaseURL: string }`
- `ProjectDetail` → `+ traceId?: string, + traceSpanId?: string`
- childRef mirror (milestones/phases/plans arrays) → `+ traceSpanId?: string`
- `TaskDetailJSON` (mirrors cmd/dashboard/api/tasks.go::taskDetail) → `+ traceId?: string, + traceSpanId?: string`

Component contract (locked by UI-SPEC — do not redesign):

- `PhoenixTraceLinkProps = { baseURL: string; traceId: string; spanId?: string; edge: "top" | "bottom" }`
- Eligibility: render null unless baseURL AND spanId both non-empty (traceId alone does NOT qualify — spanId is the "span actually emitted" signal)
- Href: `phoenixSpanURL(baseURL, spanId)`; `phoenixTraceURL` exported+tested as the documented fallback floor
- Copy: `View trace in Phoenix` / aria-label `View trace in Phoenix — opens in new tab` — identical at both mounts
- Test ids: `phoenix-trace-link-row` (row div), `phoenix-trace-link` (anchor)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phoenixLink.ts URL module + PhoenixTraceLink component (UI-SPEC rendered shape)</name>
  <files>dashboard/web/src/lib/phoenixLink.ts, dashboard/web/src/lib/phoenixLink.test.ts, dashboard/web/src/components/PhoenixTraceLink.tsx</files>
  <read_first>
    - .planning/phases/46-observability-enrichment-dashboard-deep-link/46-UI-SPEC.md §"Component Contract: <PhoenixTraceLink>" (the exact rendered JSX shape, props, eligibility, states, copy — this is the locked design contract; implement it, do not restyle)
    - dashboard/web/src/lib/pluralize.ts + a co-located lib test (e.g. dashboard/web/src/lib/projects.test.ts) — the small-pure-module + co-located-test conventions
    - dashboard/web/src/lib/clsx.ts (the in-repo clsx helper the UI-SPEC JSX uses)
  </read_first>
  <behavior>
    - phoenixTraceURL("http://phoenix:6006", "abc123") === "http://phoenix:6006/redirects/traces/abc123"; phoenixSpanURL(..., "def456") ends "/redirects/spans/def456"
    - Trailing-slash normalization: "http://phoenix:6006/" and "http://phoenix:6006" produce identical URLs (strip one trailing slash from baseURL)
    - encodeURIComponent applied to the interpolated ID segment (a fixture id "a/b" renders "a%2Fb" — defensive, RESEARCH §Security: hex-safe today, helper must not assume forever)
    - PhoenixTraceLink returns null when baseURL is "" OR spanId is undefined/"" (both cases asserted); renders the row+anchor when both present
    - Rendered anchor: href from phoenixSpanURL, target="_blank", rel="noopener noreferrer", aria-label "View trace in Phoenix — opens in new tab", visible text "View trace in Phoenix", ExternalLink size 14 aria-hidden, mono 13px styling, edge prop selects border-t vs border-b on the row div
  </behavior>
  <action>
    1. New dashboard/web/src/lib/phoenixLink.ts: export `phoenixTraceURL(baseURL: string, traceId: string): string` and `phoenixSpanURL(baseURL: string, spanId: string): string` building `{normalized}/redirects/traces/{encodeURIComponent(traceId)}` and `{normalized}/redirects/spans/{encodeURIComponent(spanId)}` — Phoenix ≥ 14.2.0 shareable-URL routes (D-12 as research-resolved; NOT the /projects/{project}/... prior). Normalization = strip one trailing "/" from baseURL, here and only here (server sends the raw value — Plan 46-03 contract). Module doc comment cites D-11's one-place rule.
    2. New dashboard/web/src/components/PhoenixTraceLink.tsx implementing the UI-SPEC contract verbatim: the props type, the null-unless-eligible rule (component-owned — mount points never re-decide visibility), and the exact rendered shape from UI-SPEC §Rendered shape (row div with data-testid="phoenix-trace-link-row", px-6 py-4, edge-selected border; anchor with data-testid="phoenix-trace-link", clsx classes, inline mono 13px style, trailing lucide-react ExternalLink size 14 aria-hidden). Import ExternalLink from lucide-react (existing dependency — first ExternalLink usage in the SPA; no new packages). Href uses phoenixSpanURL; keep phoenixTraceURL imported-adjacent as the documented one-line fallback per UI-SPEC §Href.
    3. New co-located dashboard/web/src/lib/phoenixLink.test.ts covering the URL behaviors above (pure functions — no React Testing Library needed). Component render/hide cases are covered at the mount points in Task 2; if the executor prefers a dedicated PhoenixTraceLink test file as well, that is acceptable but the mount-point cases in Task 2 remain mandatory.
  </action>
  <verify>
    <automated>cd dashboard/web && npx vitest run src/lib/phoenixLink.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - phoenixLink.test.ts passes: route shapes, trailing-slash normalization, encodeURIComponent behavior all asserted
    - grep -rn 'redirects/' dashboard/web/src --include='*.ts' --include='*.tsx' | grep -v 'lib/phoenixLink' returns 0 lines (the one-place rule — including tests, which import the helpers)
    - PhoenixTraceLink.tsx contains no fetch call (data arrives via props only)
    - `cd dashboard/web && npx tsc --noEmit` clean (or the package's existing typecheck script)
  </acceptance_criteria>
  <done>URL module + shared component exist per the locked UI contract, with the one-place rule grep-provable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire both mount points + config fetch + type mirrors + tests</name>
  <files>dashboard/web/src/lib/api.ts, dashboard/web/src/App.tsx, dashboard/web/src/components/TaskDetailDrawer.tsx, dashboard/web/src/components/__tests__/node-panel-integration.test.tsx, dashboard/web/src/components/__tests__/drawer.test.tsx</files>
  <read_first>
    - dashboard/web/src/App.tsx (nodePanelContent construction lines ~606-655 — the project branch using projects.find and the milestone/phase/plan branch with the refs lookup at lines ~634-637; projectDetail state at line ~211 fetched via fetchProject at ~360-370; the TaskDetailDrawer mount at line ~673)
    - dashboard/web/src/components/TaskDetailDrawer.tsx (TaskDetailData type lines ~45-62; MetaRow grid lines ~308-334; the Actions row — the link row lands between them per UI-SPEC §Mount points, edge="top")
    - dashboard/web/src/lib/api.ts (ProjectDetail/childRef/TaskDetailJSON type mirrors + the "mirror Go struct fields verbatim" doc convention, lines ~30-54 and ~139-141)
    - dashboard/web/src/components/TelemetryView.tsx lines ~1107-1120 (the one-shot GET /api/v1/config fetch pattern to copy INTO App.tsx — leave TelemetryView itself untouched)
    - dashboard/web/src/components/__tests__/node-panel-integration.test.tsx + drawer.test.tsx (mock-api + render/assert conventions; confirm drawer.test.tsx is TaskDetailDrawer's test file — extend it; if it is not, create TaskDetailDrawer.test.tsx following its conventions)
    - .planning/phases/46-observability-enrichment-dashboard-deep-link/46-UI-SPEC.md §Mount points + §Testing Hooks
  </read_first>
  <action>
    1. api.ts: add `phoenixBaseURL: string` to (or introduce) the typed config shape, and add the optional trace-identity mirrors per this plan's interfaces block: ProjectDetail gains traceId?/traceSpanId?; the childRef-mirroring type used by milestones/phases/plans arrays gains traceSpanId?; TaskDetailJSON gains traceId?/traceSpanId?. Follow the file's "mirrors Go struct fields verbatim" doc-comment convention, citing cmd/dashboard/api/projects.go and tasks.go.
    2. App.tsx: add a one-shot config fetch on mount (useEffect + useState, copying TelemetryView's fetch shape): GET /api/v1/config → store phoenixBaseURL string; ANY failure or missing field → "" (hidden link; observability never gates; no loading affordance — pre-resolution absence is by design indistinguishable from hidden). Do NOT refactor TelemetryView's own fetch.
    3. App.tsx nodePanelContent (UI-SPEC mount 1, edge="bottom", first child inside the panel content — above ProjectSettingsPanel / ArtifactViewer): in the project branch render PhoenixTraceLink with traceId={projectDetail?.traceId ?? ""} spanId={projectDetail?.traceSpanId} baseURL={phoenixBaseURL}; in the milestone/phase/plan branch reuse the existing refs lookup (lines ~634-637) to pull the selected node's traceSpanId and render the same component with projectDetail?.traceId. One insertion per branch — the component owns eligibility, mounts pass data.
    4. TaskDetailDrawer.tsx (UI-SPEC mount 2, edge="top", immediately after the metadata grid dl, before the Actions row): add traceId?/traceSpanId? to TaskDetailData, thread them from wherever the drawer's data population maps TaskDetailJSON → TaskDetailData (grep the useTaskDetail hook/mapping), add a `phoenixBaseURL: string` prop passed from App.tsx's mount, and render PhoenixTraceLink. Copy string and aria-label come from the shared component — no drawer-local variants (UI-SPEC consistency contract).
    5. Tests: extend node-panel-integration.test.tsx — link renders (correct href/target/rel via the phoenixLink helpers, not inline strings) when baseURL+spanId present; NOTHING renders (queryByTestId phoenix-trace-link-row → null) when baseURL is "" or spanId absent. Extend the drawer test with the same render/hide cases plus placement after the metadata grid (data-testid ordering or DOM-structure assertion).
  </action>
  <verify>
    <automated>cd dashboard/web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - node-panel-integration.test.tsx proves render-with-config and hide-on-empty for a planner node; drawer test proves both for a task — all four cases green
    - grep -c 'PhoenixTraceLink' dashboard/web/src/App.tsx returns >= 2 (both nodePanelContent branches) and grep -c 'PhoenixTraceLink' dashboard/web/src/components/TaskDetailDrawer.tsx returns >= 1
    - grep -n '"View trace in Phoenix"' dashboard/web/src — the copy string exists ONLY in PhoenixTraceLink.tsx (tests may reference it via queries)
    - Full `cd dashboard/web && npm test` suite green; `npx tsc --noEmit` (or the package typecheck script) clean
  </acceptance_criteria>
  <done>Both mount points render the shared link from live config + payload identity, hide-states proven at both, one copy string, one URL module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server config → rendered href | operator-supplied phoenixBaseURL becomes an <a href> the operator clicks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing | phoenixBaseURL in the anchor href | mitigate | visible external-link affordance (underlined text + ExternalLink icon + "opens in new tab" aria-label) so the destination is inspectable before clicking; real <a href> rendered via JSX (auto-escaped) — never dangerouslySetInnerHTML, never eval'd; rel="noopener noreferrer" severs opener access |
| T-46-02 | Tampering | trace/span IDs interpolated into URLs | mitigate | IDs are server-derived (Plan 46-03), and phoenixLink.ts defensively encodeURIComponents every interpolated segment |
| T-46-SC | Tampering | package installs | accept | zero new npm packages — lucide-react ^1.16.0 already a dependency; ExternalLink is an existing icon in that set |
</threat_model>

<verification>
- `cd dashboard/web && npm test` green
- One-place grep: no `/redirects/` template strings outside lib/phoenixLink.ts
- TypeScript clean
</verification>

<success_criteria>
- OBS-04 complete: every Planning/Execution DAG node's detail surface deep-links to its Phoenix span when configured; renders nothing when not — no dead buttons
- UI-SPEC contract implemented verbatim (copy, states, a11y, both mounts, one implementation)
</success_criteria>

<output>
Create `.planning/phases/46-observability-enrichment-dashboard-deep-link/46-05-SUMMARY.md` when done.
</output>
