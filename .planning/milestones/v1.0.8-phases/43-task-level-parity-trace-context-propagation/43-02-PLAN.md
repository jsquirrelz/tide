---
phase: 43-task-level-parity-trace-context-propagation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/dispatch/podjob/jobspec.go
  - internal/dispatch/podjob/jobspec_test.go
  - internal/controller/reporter_jobspec.go
  - internal/controller/reporter_jobspec_test.go
  - cmd/tide-reporter/main.go
  - cmd/tide-reporter/main_test.go
autonomous: true
requirements: [PROP-01]

must_haves:
  truths:
    - "BuildJobSpec appends env TRACEPARENT to the subagent container when BuildOptions.TraceParent is non-empty, and omits it entirely when empty (D-05/D-06)"
    - "BuildReporterJob appends --traceparent=<value> to the reporter container Args when ReporterOptions.TraceParent is non-empty, and omits it when empty (Pitfall 3: Args, not Env)"
    - "cmd/tide-reporter registers the traceparent flag in the same commit that BuildReporterJob starts emitting it — an unknown-flag crash-loop is impossible (Pitfall 4)"
    - "tide-reporter flag parsing is unit-testable: parseFlags accepts --traceparent and rejects unknown flags with an error"
  artifacts:
    - path: "internal/dispatch/podjob/jobspec.go"
      provides: "BuildOptions.TraceParent field + conditional TRACEPARENT env append"
      contains: "TraceParent"
    - path: "internal/controller/reporter_jobspec.go"
      provides: "ReporterOptions.TraceParent field + conditional --traceparent Arg"
      contains: "traceparent"
    - path: "cmd/tide-reporter/main.go"
      provides: "traceparent flag registration + reporterConfig.TraceParent"
      contains: "traceparent"
  key_links:
    - from: "internal/controller/reporter_jobspec.go"
      to: "cmd/tide-reporter/main.go"
      via: "--traceparent Arg must be a registered flag (same-commit rule, Pitfall 4)"
      pattern: "traceparent"
---

<objective>
Add the inert-today, load-bearing-later traceparent carriers to both Job builders (PROP-01's mechanical surface): a `TraceParent` field on `podjob.BuildOptions` with conditional `TRACEPARENT` env injection, a `TraceParent` field on `ReporterOptions` with a conditional `--traceparent` Arg, and the matching flag registration in `cmd/tide-reporter` so the new Arg can never crash-loop reporter Jobs.

Purpose: Plans 43-04 and 43-05 thread real values through these carriers. This plan makes the carriers exist, unit-tested, with zero behavior change when the value is empty.

Output: Both builders accept and emit traceparent data; reporter binary accepts the flag; new unit tests prove presence-when-set and absence-when-empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-task-level-parity-trace-context-propagation/43-CONTEXT.md
@.planning/phases/43-task-level-parity-trace-context-propagation/43-PATTERNS.md

Decision locked by RESEARCH.md Pitfall 3 (honored here): the reporter Job gets `--traceparent=<value>` via Args, NOT a `corev1.EnvVar` — `reporter_jobspec.go` sets zero Env entries today and is 100% Args-based via stdlib flag. PROP-01/D-05's "env" wording is informal shorthand for "injected as data"; D-06's mirror-the-credproxy-env pattern is correctly scoped to `jobspec.go` only.
</context>

<interfaces>
Contracts downstream plans compile against (fixed — do not vary):

```go
// internal/dispatch/podjob/jobspec.go
type BuildOptions struct {
    // ... existing fields ...
    TraceParent string // W3C traceparent for this level's own subagent Job,
                       // sourced from the IMMEDIATE PARENT's persisted span ID (PROP-01).
}
// BuildJobSpec: if opts.TraceParent != "" → subagentEnv gains {Name: "TRACEPARENT", Value: opts.TraceParent}

// internal/controller/reporter_jobspec.go
type ReporterOptions struct {
    ReporterImage string
    TraceParent   string // W3C traceparent for the spawning level's OWN span (Pattern 4).
}
// BuildReporterJob: if opts.TraceParent != "" → args gains "--traceparent=" + opts.TraceParent

// cmd/tide-reporter/main.go
// parseFlags(args []string) (reporterConfig, error)  — extracted, flag.ContinueOnError
// reporterConfig gains TraceParent string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BuildOptions.TraceParent + conditional TRACEPARENT env in BuildJobSpec</name>
  <files>internal/dispatch/podjob/jobspec.go, internal/dispatch/podjob/jobspec_test.go</files>
  <read_first>
    - internal/dispatch/podjob/jobspec.go (BuildOptions struct ~lines 82-160; subagent env construction lines ~372-418 — the `PricingOverridesJSON` conditional at ~396-401 is the exact template, NOT the `credproxyEnabled` feature-bundle)
    - internal/dispatch/podjob/jobspec_test.go (existing `TestBuildJobSpec_*` plain-Go conventions and however they construct a baseline BuildOptions — reuse that helper/fixture)
    - .planning/phases/43-task-level-parity-trace-context-propagation/43-PATTERNS.md (§"internal/dispatch/podjob/jobspec.go" — exact field doc comment and append block)
  </read_first>
  <action>
    Add a `TraceParent string` field to `BuildOptions`, placed near `PricingOverridesJSON`, with the doc comment from 43-PATTERNS.md: W3C traceparent for this level's own subagent dispatch Job, sourced from the IMMEDIATE PARENT's persisted span ID (Phase 43 PROP-01); empty when there is genuinely no parent span available (Project's own dispatch is the sole such case — FormatTraceparent already returns "" for a zero/invalid parent, so call sites need no special-case branch).

    In `BuildJobSpec`, after the `PricingOverridesJSON` conditional append and mirroring its exact shape (D-06), append `corev1.EnvVar{Name: "TRACEPARENT", Value: opts.TraceParent}` to `subagentEnv` only when `opts.TraceParent != ""`. Env name is exactly `TRACEPARENT` (the standard OTel autoinstrumentation env var — the runtime-neutral contract per PROJECT.md).

    Add two plain-Go tests to jobspec_test.go following the existing `TestBuildJobSpec_*` naming: `TestBuildJobSpec_TraceparentEnvPresentWhenSet` (set `TraceParent: "00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01"`, assert the subagent container — and only containers, not init sidecars unless the existing env slice is shared — has env `TRACEPARENT` with that exact value) and `TestBuildJobSpec_TraceparentEnvAbsentWhenEmpty` (default options → no env var named `TRACEPARENT` on any container).
  </action>
  <verify>
    <automated>go test ./internal/dispatch/podjob/ -run 'TestBuildJobSpec' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/dispatch/podjob/ -run 'TestBuildJobSpec' -count=1` exits 0 including the two new tests
    - `grep -n 'Name:  "TRACEPARENT"' internal/dispatch/podjob/jobspec.go` (or equivalent formatting) returns exactly 1 hit inside an `if opts.TraceParent != ""` block
    - No other env var or container field in BuildJobSpec changed (`git diff` on the file shows only the struct field + conditional block + comments)
  </acceptance_criteria>
  <done>BuildJobSpec emits TRACEPARENT iff TraceParent is set; both new unit tests green; zero behavior change for empty value.</done>
</task>

<task type="auto">
  <name>Task 2: ReporterOptions.TraceParent + --traceparent Arg + flag registration (single commit — Pitfall 4)</name>
  <files>internal/controller/reporter_jobspec.go, internal/controller/reporter_jobspec_test.go, cmd/tide-reporter/main.go, cmd/tide-reporter/main_test.go</files>
  <read_first>
    - internal/controller/reporter_jobspec.go (ReporterOptions ~line 74; BuildReporterJob args literal ~lines 130-137 — the 6-flag Args convention to extend)
    - cmd/tide-reporter/main.go (main() flag block lines ~78-101, reporterConfig struct ~lines 60-67, run()/runWithClient signatures at 112/119 — parse currently happens inline in main with flag.ExitOnError)
    - internal/controller/reporter_jobspec_test.go (TestBuildReporterJob_Args at line 104 — the Args assertion pattern to extend)
    - cmd/tide-reporter/main_test.go (TestRun* conventions — tests exercise run() with a pre-built reporterConfig)
  </read_first>
  <action>
    All four file changes land in ONE task/commit — RESEARCH.md Pitfall 4: an Args entry without a registered flag crash-loops every reporter Job in the cluster (`flag` rejects unknown flags).

    1. reporter_jobspec.go: add `TraceParent string` to `ReporterOptions` (doc: W3C traceparent for the spawning level's OWN just-synthesized span — the reporter's future Phase 44 message spans nest under THIS level's dispatch, not the grandparent; consumed starting Phase 44). In `BuildReporterJob`, after the existing 6-element `args` literal, conditionally `args = append(args, "--traceparent="+opts.TraceParent)` when `opts.TraceParent != ""`. Args, not Env (Pitfall 3).

    2. cmd/tide-reporter/main.go: extract the flag block into `func parseFlags(args []string) (reporterConfig, error)` using `flag.NewFlagSet("tide-reporter", flag.ContinueOnError)` with the existing 6 registrations plus a 7th: `traceParent := fs.String("traceparent", "", "W3C traceparent for this level's own span (consumed starting Phase 44)")`. Add `TraceParent string` to `reporterConfig` and populate it in the returned literal. `main()` calls `parseFlags(os.Args[1:])`; the existing `if err != nil { fmt.Fprintf(os.Stderr, "tide-reporter: flag parse: %v\n", err); os.Exit(exitInvariant) }` branch (currently dead under ExitOnError) becomes the live error handler — behavior for an unknown flag remains crash-with-nonzero-exit, now testable. Nothing reads `cfg.TraceParent` downstream this phase — that is intentional (Phase 44's TracerProvider consumes it).

    3. reporter_jobspec_test.go: add `TestBuildReporterJob_TraceparentArg` — with `ReporterOptions{ReporterImage: ..., TraceParent: "00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01"}` assert the container Args contain exactly `--traceparent=00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01`; with TraceParent empty, assert no arg has prefix `--traceparent`.

    4. main_test.go: add `TestParseFlagsTraceparent` — `parseFlags([]string{"--traceparent=00-abc...", "--project-uid=x", ...})` returns cfg with `TraceParent` set to the exact value and nil error; and `TestParseFlagsUnknownFlagErrors` — `parseFlags([]string{"--bogus=1"})` returns a non-nil error (proves the crash-on-unknown contract survived the extraction).
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'TestBuildReporterJob' -count=1 && go test ./cmd/tide-reporter/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/ -run 'TestBuildReporterJob' -count=1` exits 0 including TestBuildReporterJob_TraceparentArg
    - `go test ./cmd/tide-reporter/ -count=1` exits 0 including TestParseFlagsTraceparent and TestParseFlagsUnknownFlagErrors (all 6 existing TestRun* still green)
    - `grep -c 'fs.String("traceparent"' cmd/tide-reporter/main.go` returns 1
    - The Args append and the flag registration are in the same commit (`git show --stat HEAD` lists both reporter_jobspec.go and cmd/tide-reporter/main.go)
    - `BuildReporterJob` still sets zero Env entries on the reporter container (`grep -c 'Env:' internal/controller/reporter_jobspec.go` returns 0, preserving the file's Args-only convention)
  </acceptance_criteria>
  <done>Reporter Job spec can carry --traceparent; binary accepts it and still hard-fails unknown flags; all existing + new tests green in one commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager → dispatched pods | TRACEPARENT env / --traceparent Arg values are entirely manager-authored (from `.status` the manager itself wrote) — never sourced from subagent/reporter output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Tampering | traceparent value in Job specs | accept | Manager is the sole author (existing T-308 trust-boundary model, RESEARCH.md Security Domain); no LLM-controlled process influences the value |
| T-43-04 | Denial of Service | reporter flag parse on malformed --traceparent | mitigate | Flag registered with safe "" default and unconsumed this phase; unknown-flag crash contract preserved and now unit-tested (TestParseFlagsUnknownFlagErrors). Phase 44's consumer side inherits `ExtractRemoteParent`'s no-panic-on-malformed-input guarantee (already unit-tested Phase 42 — do not regress) |
| T-43-05 | Information Disclosure | trace IDs in Job env/args | accept | Trace IDs are not sensitive per OTel's standard threat model (RESEARCH.md V6) |
| T-43-SC | Tampering | Supply chain | accept | No new packages this phase — Package Legitimacy Audit N/A per 43-RESEARCH.md |
</threat_model>

<verification>
- `go test ./internal/dispatch/podjob/ ./cmd/tide-reporter/ -count=1` green
- `go test ./internal/controller/ -run 'TestBuildReporterJob' -count=1` green
- `go build ./...` green
</verification>

<success_criteria>
Both pod hops can carry a traceparent as data (PROP-01 mechanics), empty value produces byte-identical Job specs to today, and the reporter crash-loop failure mode is structurally impossible plus regression-tested.
</success_criteria>

<output>
Create `.planning/phases/43-task-level-parity-trace-context-propagation/43-02-SUMMARY.md` when done. Note the Args-not-Env departure from PROP-01's literal wording (Pitfall 3 rationale) for the verifier.
</output>
