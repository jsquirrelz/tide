---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 10
type: execute
wave: 7
depends_on: [47-07]
files_modified:
  - internal/controller/reporter_spawn_idempotency_test.go
autonomous: true
gap_closure: true
requirements: [PROOF-01]

must_haves:
  truths:
    - "Envtest proves a deleted (TTL-GC-simulated) reporter Job is NOT re-created once the spawn marker is set — the exact proof the verifier's gap #2 missing field demands"
    - "The proof covers the three distinct spawn-site shapes: shared-helper planner tier (milestone), inline project arm, and the task trace-only path"
  artifacts:
    - path: "internal/controller/reporter_spawn_idempotency_test.go"
      provides: "ReporterSpawnIdempotency envtest suite, Label(\"envtest\", \"heavy\")"
      min_lines: 150
  key_links:
    - from: "internal/controller/reporter_spawn_idempotency_test.go"
      to: "api/v1alpha3 marker fields (47-07)"
      via: "assertions on <Level>ReporterSpawnedUID after first spawn"
      pattern: "ReporterSpawnedUID"
---

<objective>
Land the envtest proof for verification gap #2 (CR-01): after plan 47-07's durable spawn markers, prove in envtest that a reporter Job deleted after its first spawn (simulating the 300s TTL-GC) is NOT re-created when the parent's completion path re-runs — because the `.status` marker, not the Job's name-existence, now gates Create.

Purpose: the live proof showed 271/386 LLM spans missing enrichment because TTL-GC re-opened the name-only gate; green envtest fixtures had missed it. This test pins the regression class shut. Envtest cannot run the Job TTL controller, so TTL-GC is simulated by explicit reporter-Job deletion — which is exactly the gate-reopening state the defect exploits.

Scope decision (deliberate): the proof covers the THREE distinct code shapes rather than all five spawn sites individually — milestone stands in for the milestone/phase/plan tier, which shares the single `spawnReporterIfNeeded` helper and the identical gate+stamp idiom, so one representative spec pins all three; project's inline arm and task's trace-only path are structurally distinct and each get their own spec.

Output: `internal/controller/reporter_spawn_idempotency_test.go` covering milestone (representative of the milestone/phase/plan shared-helper shape), project (inline arm), and task (trace-only path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-VERIFICATION.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the ReporterSpawnIdempotency envtest suite</name>
  <files>internal/controller/reporter_spawn_idempotency_test.go</files>
  <read_first>
    - internal/controller/child_rollup_idempotency_test.go (the TTL-GC-simulation harness this suite mirrors: package layout, Label("envtest","heavy"), childRollupProject/cleanup/refetch helpers, mapEnvReader, the drive-completion-twice structure and its header comment explaining the simulation)
    - internal/controller/project_planner_completion_test.go (how a project-level planner-Job completion is driven end-to-end in envtest with ReporterImage SET, and the existing assertion at ~line 343 that tide-reporter-<uid> is created — the positive-path setup to reuse)
    - internal/controller/planner_job_absent_test.go:40-60 (expectReporterSpawned helper shape)
    - internal/controller/task_traceonly_reporter_test.go (how the task trace-only spawn is driven with a completedJob; the tide-reporter-trace-<jobUID> name shape)
    - internal/controller/milestone_controller.go and internal/controller/task_controller.go (the 47-07 gate+stamp blocks under test — read AFTER 47-07 has merged)
  </read_first>
  <action>
    Create `internal/controller/reporter_spawn_idempotency_test.go` in package controller with a file-header comment naming the defect (CR-01 / Phase 47 gap #2: name-only spawn gate re-opens after reporter TTL-GC; the live proof measured 115/386 enriched spans) and the simulation rule (envtest has no TTL controller — explicit reporter-Job Delete IS the TTL-GC end state). Ginkgo `Describe` blocks with `Label("envtest", "heavy")`, one per spawn-site shape:

    1. Milestone (shared-helper shape, representative of milestone/phase/plan): drive a milestone planner-Job completion with ReporterImage configured (reuse the harness conventions from project_planner_completion_test.go / child_rollup_idempotency_test.go). Assert: (a) the `tide-reporter-<milestone-uid>` Job exists; (b) `Milestone.Status.MilestoneReporterSpawnedUID` equals the completed planner Job's UID (or the deterministic-name fallback the site uses when driven with a nil Job — match how the harness drives it). Then DELETE the reporter Job with a background propagation policy and wait until Get returns IsNotFound. Re-drive the same completion path (the same mechanism the harness uses to trigger reconcile — e.g. annotating/touching the parent or re-invoking the completion handler, matching the rollup suite's approach). Then `Consistently` (a few seconds) assert the reporter Job is NOT re-created, and the marker still holds the original key.
    2. Project (inline arm): same four-beat sequence against `tide-reporter-<project-uid>` and `Project.Status.ProjectReporterSpawnedUID`.
    3. Task (trace-only path): drive a completed dispatch Job through the task completion path with OTLPEndpoint + ReporterImage deps set (task_traceonly_reporter_test.go conventions); assert `tide-reporter-trace-<completedJob-UID>` exists and `Task.Status.TaskTraceReporterSpawnedUID` equals the completed Job's UID; delete the trace-only reporter Job; re-drive; `Consistently` assert no re-creation.

    Negative control in at least the milestone Describe: a NEW attempt key (a different completed-Job identity, e.g. after resetting the marker or driving with a distinct job UID per the site's keying) DOES spawn a fresh reporter — proving the gate is per-attempt, not a permanent one-shot.

    Keep names/labels consistent so the suite is selectable: prefix all Describe strings with "ReporterSpawnIdempotency".
  </action>
  <verify>
    <automated>make manifests generate >/dev/null && KUBEBUILDER_ASSETS="$(bin/setup-envtest use 1.33 --bin-dir bin -p path 2>/dev/null || true)" go test ./internal/controller/... -timeout 20m -ginkgo.v -ginkgo.label-filter='heavy' -ginkgo.focus='ReporterSpawnIdempotency' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The focused run passes: `go test ./internal/controller/... -ginkgo.label-filter='heavy' -ginkgo.focus='ReporterSpawnIdempotency'` (with KUBEBUILDER_ASSETS resolved the same way the Makefile's test-heavy target does) reports >= 3 passed specs, 0 failed.
    - Each spec's post-delete phase uses `Consistently` (not a single Get) to assert non-recreation: `grep -c 'Consistently' internal/controller/reporter_spawn_idempotency_test.go` returns >= 3.
    - The suite asserts marker values, not just Job absence: `grep -cE '(Milestone|Project|TaskTrace)ReporterSpawnedUID' internal/controller/reporter_spawn_idempotency_test.go` returns >= 3.
    - The full heavy tier stays green: `make test-heavy` exits 0 (new suite coexists with the rollup idempotency suites in the same envtest manager).
    - The negative control exists: at least one spec proves a distinct attempt key still spawns (grep for the spec description or assert count of Create-observed Jobs across the two attempts).
  </acceptance_criteria>
  <done>The verifier's demanded proof exists and runs in the heavy envtest tier: TTL-GC'd reporter Jobs are never re-created for an observed attempt at all three spawn-site shapes, while new attempts still spawn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → envtest apiserver | test-only; no production trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-10-01 | Repudiation (regression invisibility) | CR-01 defect class | mitigate | This suite IS the mitigation — it pins the TTL-GC-reopen class into the heavy tier so a future regression fails CI instead of surfacing only in a paid live run |
| T-47-SC | Tampering | package installs | n/a | Test-only change; no new dependencies (Ginkgo/Gomega already pinned) |
</threat_model>

<verification>
- Focused Ginkgo run green (>= 3 specs); `make test-heavy` green end-to-end.
- No production code modified: `git diff --name-only` for this plan contains only the new test file.
</verification>

<success_criteria>
Gap #2 is fully closed: implementation (47-07) plus this proof — a deleted reporter Job is demonstrably not re-created once the marker is set, per-attempt semantics verified, at the shared-helper, inline-project, and task trace-only shapes.
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-10-SUMMARY.md` when done.
</output>
