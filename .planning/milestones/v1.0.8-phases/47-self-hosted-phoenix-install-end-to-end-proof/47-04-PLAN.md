---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 04
type: execute
wave: 3
depends_on: [47-03]
files_modified:
  - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md
  - docs/INSTALL.md
  - docs/observability.md
autonomous: true
requirements: [PROOF-01]

must_haves:
  truths:
    - "A fresh kind cluster (tide-phoenix-proof — never tide-test, never reusing tide-dogfood) runs TIDE at local HEAD plus Phoenix installed by following docs/INSTALL.md's own steps"
    - "All pre-flight static checks and the no-spend OTLP connectivity check passed BEFORE the first real-API dispatch"
    - "medium-project reached status.phase=Complete and Phoenix's API confirms traces arrived"
    - "Context decisions honored — D-11: fresh tide-phoenix-proof cluster stood up from the docs' own steps (stale tide-dogfood not reused, one heavy workload at a time); D-12: bounded real-spend driving run with pre-flight static checks and a no-spend OTLP connectivity check before the first dispatch"
  artifacts:
    - path: ".planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md"
      provides: "Commands actually run, doc divergences found+fixed, trace IDs, observed cost, provenance notes"
      min_lines: 40
  key_links:
    - from: "tide-system manager Deployment"
      to: "phoenix Service :4317"
      via: "OTEL_EXPORTER_OTLP_ENDPOINT env"
      pattern: "svc\\.cluster\\.local:4317"
    - from: "tide-otlp-headers Secret"
      to: "manager pod env"
      via: "valueFrom secretKeyRef (never literal)"
      pattern: "secretKeyRef"
---

<objective>
Stand up the PROOF-01 environment by following the Plan 47-03 docs literally (D-11: the recipe is proven by execution — any divergence is a doc bug root-fixed here), run the D-12 pre-flight static checks and no-spend connectivity check, then drive the real-spend `examples/projects/medium` run to Complete against the auth-ON, SQLite-on-PVC Phoenix.

Purpose: produce the live five-level trace data Plan 47-05 captures as milestone-close evidence. This phase emits nothing new — if the run surfaces a TIDE defect (missing spans, unredacted content, broken tree), it is named and root-fixed in-phase per D-14, never worked around.
Output: running proof cluster + Complete medium-project + 47-PROOF-RUNLOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-CONTEXT.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-RESEARCH.md

Operational constraints (binding):
- Dev VM is ~7.65 GiB. ONE heavy workload at a time. `kind-tide-dogfood` is confirmed stale (v1alpha1/v1alpha2, chart tide-1.0.5) AND its control-plane container alone uses ~2.65 GiB — D-11 locks delete/recreate; delete it FIRST to free the budget. Never name the new cluster `tide-test` (CI harness reserved) or `tide-dogfood`. Cluster name: `tide-phoenix-proof`.
- Real key lives at `~/.tide/anthropic.key` (109 bytes, outside repo, durable). The medium sample carries its own $5 hard cap.
- Honest substitution (record in runlog): the published OCI chart/images predate this phase's headers wiring, so the proof installs the LOCAL `./charts/tide` + `./charts/tide-crds` and locally-built HEAD images kind-loaded in — the docs' VALUES and step sequence are what is being proven; everything else follows the docs verbatim.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline pre-flight renders, fresh cluster, TIDE at local HEAD</name>
  <files>.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md</files>
  <read_first>
    - docs/INSTALL.md (the full install sequence being followed: cert-manager v1.20.2, tide-crds, tide, and the new "Enable tracing (Phoenix)" section)
    - hack/scripts/acceptance-v1.sh (the proven fresh-kind + image build/load + helm install playbook — mirror its image set and load mechanics)
    - hack/scripts/load-images-if-needed.sh (image-load helper; check its cluster-name assumption before reusing)
    - examples/projects/medium/README.md and examples/projects/medium/project.yaml (apply order + which images the run needs: manager, dashboard, reporter, credproxy, tide-push, claude subagent, git-http-server)
  </read_first>
  <action>
    OFFLINE PRE-FLIGHT FIRST (D-12 — cheap static checks before anything heavy): (1) `make helm-assert` green. (2) Render the TIDE chart with the proof values exactly as the docs specify (`helm template charts/tide --set dashboard.enabled=true --set otel.exporter.endpoint=<phoenix-svc-from-docs>.phoenix.svc.cluster.local:4317 --set otel.exporter.headersSecretRef.name=tide-otlp-headers --set phoenix.baseURL=http://localhost:6006`) and assert: endpoint value verbatim on manager env, OTEL_EXPORTER_OTLP_HEADERS via secretKeyRef on manager AND dashboard (run `python3 hack/helm/assert-otlp-headers-env.py <render> --expect-secretref tide-otlp-headers OTEL_EXPORTER_OTLP_HEADERS`), PHOENIX_BASE_URL present. (3) Render the Phoenix chart offline with the doc's quickstart values — must succeed (validatePersistence passes) and the Service name/ports must match what the docs say; a mismatch is a doc bug — fix docs/INSTALL.md / docs/observability.md now and note it in the runlog.

    THEN the cluster: (4) `kind get clusters`; delete `tide-dogfood` (`kind delete cluster --name tide-dogfood`) — locked by D-11 (stale, and its 2.65 GiB must be freed); confirm zero clusters remain. (5) `kind create cluster --name tide-phoenix-proof`. (6) Install cert-manager per docs/INSTALL.md (v1.20.2, wait for rollout). (7) Build + kind-load the local HEAD images the medium run needs, mirroring acceptance-v1.sh's image set and tags (manager/controller, dashboard, tide-reporter, tide-credproxy, tide-push, claude subagent, and any git-http-server/init image the medium sample references — read the sample manifests to confirm the exact set); pass the correct cluster name to any load helper. (8) Install `./charts/tide-crds` then `./charts/tide` with the locally-built image tags overridden, dashboard enabled, but tracing values NOT yet set (they land in Task 2 after Phoenix exists — this also demonstrates the NOTES.txt nudge appearing in the install output while tracing is dark; capture that output line in the runlog as live D-10 evidence). (9) Start `47-PROOF-RUNLOG.md` in the phase dir: record every command actually run, the NOTES.txt nudge observation, the image provenance note, and any doc divergence found+fixed.
  </action>
  <verify>
    <automated>kubectl --context kind-tide-phoenix-proof get pods -n tide-system --no-headers | grep -c Running && kubectl --context kind-tide-phoenix-proof get crd projects.tideproject.k8s -o jsonpath='{.spec.versions[*].name}' | grep -c v1alpha3</automated>
  </verify>
  <acceptance_criteria>
    - `kind get clusters` shows exactly `tide-phoenix-proof` (no tide-dogfood, no tide-test)
    - manager + dashboard pods Running in tide-system; Project CRD serves v1alpha3
    - All three offline pre-flight checks recorded as green in the runlog BEFORE any cluster command
    - helm install/upgrade output captured in the runlog shows the tracing-dark NOTES nudge (endpoint still empty at this step)
  </acceptance_criteria>
  <done>Fresh proof cluster runs TIDE HEAD; pre-flight evidence in runlog; zero API spend so far.</done>
</task>

<task type="auto">
  <name>Task 2: Phoenix install per docs, API key + headers Secret, TIDE tracing upgrade, no-spend connectivity checks</name>
  <files>.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md, docs/INSTALL.md, docs/observability.md</files>
  <read_first>
    - docs/INSTALL.md §"Enable tracing (Phoenix)" (THE script for this task — follow it step-for-step; deviations are doc bugs)
    - docs/observability.md §"Self-hosted Phoenix" (auth/API-key depth if the quickstart defers there)
  </read_first>
  <action>
    Follow the doc exactly: (1) `helm install tide-phoenix` with the doc's pinned version + quickstart values into namespace `phoenix`; `kubectl -n phoenix rollout status deploy --timeout=5m`; confirm the PVC is Bound at 2Gi. (2) Port-forward the Phoenix service 6006, retrieve the initial admin password from the chart-created Secret exactly as the doc says, log in, change the password, mint a System API key (if the doc's click-path/API route is wrong — RESEARCH Assumption A2 was MEDIUM-HIGH — find the real route, use it, and FIX THE DOC in the same commit, noting the divergence in the runlog). (3) Create the `tide-otlp-headers` Secret via the doc's --from-literal command (real key value never lands in any file or runlog). (4) Run the doc's `helm upgrade tide` with the endpoint/headersSecretRef/phoenix.baseURL flags, substituting the local `./charts/tide` path; wait for the manager rollout. Confirm the NOTES nudge is now ABSENT from the upgrade output (live D-10 both-ways evidence — record in runlog). (5) NO-SPEND CONNECTIVITY CHECKS (all before any API dispatch): (a) `kubectl --context kind-tide-phoenix-proof run otlp-nc -n tide-system --rm --restart=Never --image=busybox:1.36 -- nc -z -w5 <phoenix-svc>.phoenix.svc.cluster.local 4317` succeeds; (b) manager Deployment yaml shows OTEL_EXPORTER_OTLP_ENDPOINT with the bare host:port value and OTEL_EXPORTER_OTLP_HEADERS as valueFrom/secretKeyRef — `kubectl get deploy -n tide-system -o yaml | grep -A4 OTEL_EXPORTER_OTLP_HEADERS` must show secretKeyRef and NO literal value (T-47-01 live check); (c) manager logs contain zero OTLP export errors (`kubectl logs -n tide-system deploy/<manager> | grep -ci 'export.*error\|otlp.*error'` → 0). Update the runlog with each check's observed output.
  </action>
  <verify>
    <automated>kubectl --context kind-tide-phoenix-proof -n phoenix get pods --no-headers | grep -c Running && kubectl --context kind-tide-phoenix-proof get deploy -n tide-system -o yaml | grep -c "OTEL_EXPORTER_OTLP_HEADERS"</automated>
  </verify>
  <acceptance_criteria>
    - Phoenix pod Running; PVC Bound 2Gi; `helm list -n phoenix` shows the pinned chart version from the docs
    - `tide-otlp-headers` Secret exists in tide-system; manager Deployment env shows the headers entry as secretKeyRef with zero literal value (grep output in runlog)
    - nc probe to <phoenix-svc>:4317 exited 0; manager logs OTLP-error grep count is 0
    - NOTES nudge confirmed absent in the tracing-enabled upgrade output (runlog)
    - Any doc divergence encountered is fixed in docs/INSTALL.md / docs/observability.md in this task's commit and named in the runlog (zero divergences is also a valid, recorded outcome)
  </acceptance_criteria>
  <done>Auth-ON Phoenix reachable from TIDE with Secret-sourced headers; pipe proven without spending a cent.</done>
</task>

<task type="auto">
  <name>Task 3: Real-spend driving run — examples/projects/medium to Complete, span arrival confirmed</name>
  <files>.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md</files>
  <read_first>
    - examples/projects/medium/README.md and the manifest apply order in examples/projects/medium/project.yaml's header comment (9 steps: namespace → demo-remote-pvc → per-namespace-resources → signing-key mirror → demo-remote-init Job wait → git-http-server wait → tide-secrets → project.yaml → Complete wait)
    - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md (state so far — Phoenix svc name, API key location)
  </read_first>
  <action>
    (1) `export ANTHROPIC_API_KEY=$(cat ~/.tide/anthropic.key)` and follow the medium sample's 9-step apply order exactly (empty GIT_PAT — anonymous in-cluster http remote; $5 hard cap is enforced by the sample's own budget). (2) `kubectl wait --for=jsonpath='{.status.phase}'=Complete project/medium-project -n tide-sample-medium --timeout=30m`; while waiting, tail manager logs for dispatch/reporter activity and OTLP errors. If the run fails or halts: root-cause from manager logs + `kubectl describe` + reporter Job logs BEFORE any retry (no-flake doctrine; a TIDE defect found here is named in the runlog and becomes an in-phase root-fix per D-14 — surface it in the SUMMARY as a gap rather than working around it). (3) After Complete: confirm span arrival — port-forward Phoenix 6006 and `curl -s -H "Authorization: Bearer $PHOENIX_API_KEY" http://localhost:6006/v1/projects` (adapt the path to Phoenix's actual REST surface if it differs; any authenticated query proving non-zero traces/spans for the run is sufficient) — record the project/trace identifiers returned; also record `kubectl get jobs -n tide-sample-medium` reporter-Job counts (expect reporter Jobs across all five levels plus Task trace-only Jobs) and the observed run cost from the Project status/dashboard. (4) Finalize the runlog: trace IDs (full hex), Phoenix query used, cost observed vs the $5 cap, total wall time, reporter-Job tally, zero-OTLP-error grep results, and the honest provenance note (local HEAD chart/images). The real API key and the Phoenix bearer token must not appear anywhere in the runlog — reference them by Secret name only.
  </action>
  <verify>
    <automated>kubectl --context kind-tide-phoenix-proof get project medium-project -n tide-sample-medium -o jsonpath='{.status.phase}' | grep -c Complete && test -f .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md && grep -cE '[0-9a-f]{32}' .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - medium-project status.phase == Complete within the 30m window
    - Runlog contains ≥1 32-hex trace ID, the authenticated Phoenix API query + non-empty result, reporter-Job counts, and observed cost ≤ $5
    - `grep -c "sk-ant" 47-PROOF-RUNLOG.md` returns 0 and no bearer-token value appears (Secret names only)
    - Manager-log OTLP-error grep across the full run window returns 0
  </acceptance_criteria>
  <done>Real five-level run Complete with traces confirmed inside the self-hosted Phoenix; everything Plan 47-05 needs is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev VM → real Anthropic API | real credential + real spend ($5-capped) |
| runlog artifact → git repo | operational evidence must carry zero secret material |
| TIDE pods → auth-ON Phoenix | bearer-authenticated OTLP over the cluster network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information Disclosure | headers env on manager Deployment | mitigate | live secretKeyRef inspection in Task 2 (grep shows valueFrom, zero literal) |
| T-47-05 | Information Disclosure | secrets leaking into committed runlog | mitigate | Secret-name-only rule + `sk-ant` grep gate in Task 3 acceptance criteria |
| T-47-04 | Information Disclosure | Phoenix exposed beyond loopback | mitigate | ClusterIP + ingress off per recipe; access only via kubectl port-forward |
| T-47-06 | Denial of Service | VM OOM from concurrent heavy workloads | mitigate | tide-dogfood deleted first; one cluster, one heavy run; Phoenix footprint (1Gi req) counted in budget |
| T-47-SC | Tampering | Phoenix chart pull at install time | accept | version-pinned pull of the audit-Approved chart (RESEARCH.md legitimacy audit) |
</threat_model>

<verification>
- Task verifies green in order: pre-flight (offline) → cluster health → connectivity (no-spend) → Complete + span arrival
- Runlog is committed and secret-free
</verification>

<success_criteria>
- The documented recipe survived being followed literally (or every divergence became a same-commit doc fix)
- A Complete real run's traces are queryable in the auth-ON self-hosted Phoenix, ready for evidence capture
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-04-SUMMARY.md` when done. Leave the cluster RUNNING — Plan 47-05 captures evidence from it.
</output>
