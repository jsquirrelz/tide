---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 09
type: execute
wave: 5
depends_on: [47-04]
files_modified:
  - examples/projects/medium/README.md
  - examples/projects/medium/per-namespace-resources.yaml
autonomous: true
gap_closure: true
requirements: [PROOF-01]

must_haves:
  truths:
    - "The medium sample's documented apply sequence produces a bindable ReadWriteOnce tide-projects PVC on kind's RWO-only local-path provisioner"
    - "The RWO override is an explicit inline step in the README apply sequence, not a buried comment the operator must discover after a deadlock"
  artifacts:
    - path: "examples/projects/medium/README.md"
      provides: "inline kind/RWO override step (delete + recreate tide-projects PVC as ReadWriteOnce) in the apply sequence"
      contains: "ReadWriteOnce"
    - path: "examples/projects/medium/per-namespace-resources.yaml"
      provides: "PVC comment pointing at the README's override step"
  key_links:
    - from: "examples/projects/medium/per-namespace-resources.yaml"
      to: "examples/projects/medium/README.md"
      via: "comment cross-reference to the new apply-sequence step"
      pattern: "README"
---

<objective>
Close verification gap #4 (cross-phase example-fixture defect surfaced by the proof, 47-EVIDENCE Defect #3): `examples/projects/medium/per-namespace-resources.yaml` ships the `tide-projects` PVC as ReadWriteMany, which deadlocks at WaitForFirstConsumer on kind's `rancher.io/local-path` (RWO-only) provisioner. The file's own comment admits RWO is needed on kind, but the README's 9-step apply sequence has no override step — the 47-04 proof run had to discover and work around this operationally (delete/recreate as RWO).

Fix per the verifier's missing field: add an inline RWO override step to the medium sample's apply sequence, mirroring `hack/scripts/acceptance-v1.sh`'s small-sample inline-heredoc pattern (lines 110-153).

Output: a README apply sequence that works first-try on kind, and a PVC comment that routes operators to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-VERIFICATION.md

PVC `accessModes` are immutable — the override cannot be a `kubectl patch`; it must delete and recreate the claim, which is safe at this point in the sequence because nothing has consumed the PVC yet (it is Pending under WaitForFirstConsumer). The acceptance script's small-sample block (acceptance-v1.sh:135-153) is the house pattern: inline `kubectl apply -f - <<EOF` heredoc with compact metadata.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the inline RWO override step to the medium sample apply sequence</name>
  <files>examples/projects/medium/README.md, examples/projects/medium/per-namespace-resources.yaml</files>
  <read_first>
    - examples/projects/medium/README.md (the "Apply Sequence (9 steps — order matters)" section — step 3 applies per-namespace-resources.yaml; step numbering and the existing WHY-comment voice used at step 5)
    - examples/projects/medium/per-namespace-resources.yaml:40-61 (the kind/minikube note and the PVC's RWX-default comment)
    - hack/scripts/acceptance-v1.sh:110-153 (the RWO rationale comment + inline heredoc PVC shape to mirror)
  </read_first>
  <action>
    1. `examples/projects/medium/README.md`: insert a new step labeled `3b` (keeping existing numbering intact) immediately after step 3, scoped "kind / RWO-only provisioners ONLY — skip on RWX-capable clusters". Content: a WHY note in the step-5 comment voice — per-namespace-resources.yaml ships `tide-projects` as ReadWriteMany (the production default), but kind's `rancher.io/local-path` provisioner is RWO-only, so the RWX claim deadlocks at WaitForFirstConsumer and the controller never reaches the PVC-Bound gate; accessModes are immutable, so delete and recreate rather than patch (safe here: nothing has mounted the claim yet). Then two commands: `kubectl delete pvc tide-projects -n tide-sample-medium` followed by an inline `kubectl apply -f - <<EOF` heredoc recreating the PVC with name `tide-projects`, namespace `tide-sample-medium`, the same labels the YAML file carries (`app.kubernetes.io/name: tide`, `app.kubernetes.io/managed-by: tide-sample`, `tideproject.k8s/sample: medium`), `accessModes: [ReadWriteOnce]`, and `storage: 1Gi` — mirroring acceptance-v1.sh's small-sample heredoc shape. Note that RWO is correct on a single node (same-node pods share the mount; wave sequencing keeps mounts serialized), echoing the acceptance script's rationale comment.
    2. `examples/projects/medium/per-namespace-resources.yaml`: update the PVC's inline comment (currently "For kind-based testing, change to ReadWriteOnce and ensure only one Job mounts at a time") and the header's kind/minikube note to point at README step 3b as the concrete override procedure, instead of leaving the operator to edit the file by hand. Keep the RWX-is-production-default statement; do NOT change the shipped `accessModes: [ReadWriteMany]` value (RWX remains the default for RWX-capable clusters).
  </action>
  <verify>
    <automated>grep -c 'ReadWriteOnce' examples/projects/medium/README.md && grep -c 'kubectl delete pvc tide-projects' examples/projects/medium/README.md && grep -c 'accessModes: \[ReadWriteMany\]' examples/projects/medium/per-namespace-resources.yaml && python3 -c "import yaml,sys; docs=list(yaml.safe_load_all(open('examples/projects/medium/per-namespace-resources.yaml'))); print(len([d for d in docs if d]))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ReadWriteOnce' examples/projects/medium/README.md` returns >= 1 and `grep -c 'kubectl delete pvc tide-projects' examples/projects/medium/README.md` returns exactly 1 (the override step exists, once).
    - The README step is explicitly scoped to kind/RWO-only clusters: `grep -icE 'RWO-only|kind.*only|skip on RWX' examples/projects/medium/README.md` returns >= 1.
    - The heredoc PVC in the README carries namespace `tide-sample-medium` and `storage: 1Gi` (grep both strings in the README).
    - `grep -c 'accessModes: \[ReadWriteMany\]' examples/projects/medium/per-namespace-resources.yaml` returns 1 (shipped default unchanged).
    - per-namespace-resources.yaml's PVC comment references the README override step: `grep -c 'README' examples/projects/medium/per-namespace-resources.yaml` returns >= 1.
    - per-namespace-resources.yaml still parses as multi-doc YAML (the python3 yaml.safe_load_all probe in verify prints a document count without raising).
  </acceptance_criteria>
  <done>An operator following the medium README on kind never hits the WaitForFirstConsumer deadlock: step 3b replaces the RWX claim with an RWO one before anything consumes it, and the YAML file routes readers to that step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → cluster | documented kubectl commands executed by a human against their own cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-09-01 | Denial of Service (data loss on reuse) | `kubectl delete pvc tide-projects` in the new step | mitigate | Step is positioned immediately after first creation (claim is Pending, nothing has mounted it) and explicitly scoped to kind/RWO-only fresh installs; the WHY note states accessModes immutability as the reason for delete+recreate |
| T-47-SC | Tampering | package installs | n/a | Docs/example-only change; no dependencies |
</threat_model>

<verification>
- Grep assertions above pass; shipped YAML default unchanged and still parseable.
- The override step's heredoc mirrors the acceptance-v1.sh small-sample shape (manual read: same apply-from-stdin idiom, compact metadata, RWO + 1Gi).
</verification>

<success_criteria>
Gap #4 closed: the medium sample's documented apply sequence is kind-clean first-try — the exact operational workaround 47-04 had to improvise is now the documented, scoped step 3b.
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-09-SUMMARY.md` when done.
</output>
