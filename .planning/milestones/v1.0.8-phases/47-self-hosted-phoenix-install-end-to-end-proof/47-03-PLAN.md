---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 03
type: execute
wave: 2
depends_on: [47-01, 47-02]
files_modified:
  - docs/INSTALL.md
  - docs/observability.md
autonomous: true
requirements: [PHX-01, PHX-02]

must_haves:
  truths:
    - "docs/INSTALL.md walks install-Phoenix → wire-TIDE → verify with a pinned chart version, freshly re-verified at authoring time"
    - "docs/observability.md documents both storage paths (SQLite-on-PVC quickstart, bundled-Postgres durable), the auth-ON posture with Secret-sourced credentials, and the full headers wiring chain including the reporter-Job forward"
    - "No doc example uses a scheme-prefixed OTLP endpoint, a floating chart tag, port 6006 as an OTLP target, or a literal secret value"
    - "The observability.md deep-link placeholder ('install instructions land in Phase 47') is replaced with cross-references"
    - "Context decisions honored — D-01: INSTALL.md quickstart step + observability.md reference-depth split; D-02: separate helm install of the official OCI chart, exact version pinned, never floating; D-03: freshly re-fetched pin in every example; D-04: appVersion ≥ 14.2.0 floor, current auth.enableAuth default, and storage-mode exclusivity re-verified against the fetched chart; D-05: both chart storage strategies documented, no invented third; D-06: right-sized persistence.size and bounded retention days for the ~8 GiB dev VM; D-07: doc honest that only the SQLite-on-PVC path is live-proven; D-08: auth kept ON with Secret-sourced credentials and the default divergence called out; D-09: bare host:port targeting 4317 gRPC with the scheme-prefix warning and the full env chain shown"
  artifacts:
    - path: "docs/INSTALL.md"
      provides: "Quickstart step section"
      contains: "### Enable tracing (Phoenix)"
    - path: "docs/observability.md"
      provides: "Full reference recipe"
      contains: "Self-hosted Phoenix"
  key_links:
    - from: "docs/INSTALL.md"
      to: "charts/tide/templates/NOTES.txt"
      via: "the nudge points at this section's heading"
      pattern: "Enable tracing"
    - from: "docs/observability.md"
      to: "charts/tide/values.yaml"
      via: "env-var table row for the new key"
      pattern: "headersSecretRef"
---

<objective>
Author the PHX-01/PHX-02 documentation surface: an INSTALL.md "Enable tracing (Phoenix)" quickstart mirroring the Prometheus three-beat precedent, and an observability.md "Self-hosted Phoenix" reference subsection (both storage paths, auth posture, sizing, headers chain), discharging the deep-link placeholder. First action is the ROADMAP-mandated fresh chart-pin re-fetch with the D-04 three-fact re-verification.

Locked discretionary values (use consistently everywhere): namespace `phoenix`, Helm release `tide-phoenix`, `persistence.size=2Gi`, `database.defaultRetentionPolicyDays=7`, headers Secret name `tide-otlp-headers` in `tide-system`.

Purpose: PHX-01 and PHX-02's letter; the recipe these docs describe is proven by execution in Plans 47-04/47-05 (D-11 — a wrong step here fails the proof).
Output: two updated docs, every example pinned and copy-pasteable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-CONTEXT.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-RESEARCH.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PATTERNS.md

<interfaces>
Quickstart recipe values (RESEARCH-verified against chart 10.0.0 — every flag re-confirmed live 2026-07-17; re-verify the pin per Task 1 before embedding):

```bash
helm install tide-phoenix oci://registry-1.docker.io/arizephoenix/phoenix-helm \
  --version 10.0.0 \
  --namespace phoenix --create-namespace \
  --set persistence.enabled=true \
  --set persistence.size=2Gi \
  --set postgresql.enabled=false \
  --set database.defaultRetentionPolicyDays=7 \
  --set service.type=ClusterIP \
  --set ingress.enabled=false \
  --set auth.enableAuth=true
```

TIDE wiring (bare host:port, gRPC 4317 — substitute the REAL rendered Service name verified in Task 1, do not assume `tide-phoenix`):

```bash
helm upgrade tide oci://ghcr.io/jsquirrelz/tide-charts/tide -n tide-system --reuse-values \
  --set otel.exporter.endpoint=<phoenix-service>.phoenix.svc.cluster.local:4317 \
  --set otel.exporter.headersSecretRef.name=tide-otlp-headers \
  --set phoenix.baseURL=http://localhost:6006
```

Headers Secret (the --from-literal voice from INSTALL.md's Provider Secret section):

```bash
kubectl create secret generic tide-otlp-headers -n tide-system \
  --from-literal=OTEL_EXPORTER_OTLP_HEADERS="Authorization=Bearer ${PHOENIX_API_KEY}"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh pin re-check + D-04 re-verification + INSTALL.md "Enable tracing (Phoenix)" section</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - docs/INSTALL.md (lines 170-250: the "Enable telemetry (Prometheus)" three-beat section this mirrors; the "Existing Prometheus" callout style at ~215; the Provider Secret --from-literal voice at 217-235; "Verifying the install" idiom)
    - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-RESEARCH.md (§Fresh values.yaml facts — the verified defaults table; §Code Examples recipe; Assumption A2 on API-key minting)
  </read_first>
  <action>
    PRE-FLIGHT (binding, before writing any doc text): (a) run `helm show chart oci://registry-1.docker.io/arizephoenix/phoenix-helm` and record version/appVersion — if it differs from 10.0.0/18.0.0, pull the new chart (`helm pull ... --version <new> --untar` into a temp dir) and re-verify the D-04 trio: appVersion ≥ 14.2.0, `auth.enableAuth` default in values.yaml, the `phoenix.validatePersistence` fail-guard in templates/_helpers.tpl; use the NEW pin in every example and adjust any diverged doc claims (divergences update text, not just the number). (b) Render the Phoenix chart offline with the quickstart values (`helm template tide-phoenix <chart-ref> --version <pin> -n phoenix --set persistence.enabled=true --set postgresql.enabled=false --set service.type=ClusterIP --set ingress.enabled=false`) and read the rendered Service name and ports — use the REAL service DNS name in every endpoint example, and confirm the render succeeds (proves the value combination passes validatePersistence). Also confirm how the chart names its admin/secret resources (the `auth.name: phoenix-secret` Secret) so the login step is accurate.

    Then add a `### Enable tracing (Phoenix)` section to docs/INSTALL.md as a sibling directly after "Enable telemetry (Prometheus)" (before "## Provider Secret"), in the Prometheus section's exact three-beat shape (D-01): Step 1 — install Phoenix with the quickstart values from the interfaces block (pinned version, never latest; one-line note that this is the SQLite-on-PVC kind/dev path and that observability.md's Self-hosted Phoenix section covers the durable Postgres path, retention/PVC sizing rationale, and the dev-only auth-off override). Step 2 — mint a Phoenix System API key (log in at the port-forwarded UI with the initial admin password from the chart-created `phoenix-secret` Secret — name the exact `kubectl get secret ... -o jsonpath` incantation — then Settings → API keys; note the chart's weak `admin` fallback initial password must be changed at first login), create the `tide-otlp-headers` Secret with --from-literal (never paste the key into YAML — reuse the Provider Secret sentence voice), and `helm upgrade tide` with the three --set flags from the interfaces block. Include a warning callout (styled like the existing "Existing Prometheus" callout): the endpoint MUST be bare `host:port` — `otlptracegrpc.WithEndpoint` silently rejects `http://`/`https://` prefixes and traces just go dark (describe the failure mode in prose; never show a scheme-prefixed value as a copy-pasteable command) — and 4317 is the OTLP gRPC port; 6006 is the HTTP UI and is never the OTLP target. Add the one-line sampler reassurance: sampling already defaults to 1.0 since Phase 46, no override needed. Note the auth default explicitly: the CHART defaults `auth.enableAuth=true` — the opposite of the raw Phoenix image default — keep it on. Step 3/4 — done-signal: `kubectl -n phoenix port-forward svc/<phoenix-service> 6006:6006`, open http://localhost:6006, log in, and confirm the projects page loads; note traces appear after the first Project run, and that when `otel.exporter.endpoint` is set the NOTES.txt tracing-dark warning disappears from `helm upgrade` output (the D-10 tie-in, mirroring the Prometheus NOTES tie-in).
  </action>
  <verify>
    <automated>grep -c '### Enable tracing (Phoenix)' docs/INSTALL.md && grep -qc 'tide-otlp-headers' docs/INSTALL.md && grep -qc 'headersSecretRef' docs/INSTALL.md && test "$(grep -cE 'endpoint=https?://' docs/INSTALL.md)" = "0" && grep -q -- '--version' docs/INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/INSTALL.md contains exactly one `### Enable tracing (Phoenix)` heading, placed after the Prometheus section and before "## Provider Secret"
    - Every Phoenix `helm install`/`helm pull` example carries an explicit `--version <pin>`; grep for `phoenix-helm` lines shows zero without a version flag; zero occurrences of a `latest` tag in the section
    - Section contains: `4317`, `6006`, `host:port`, `auth.enableAuth`, `tide-otlp-headers`, `--from-literal`, `persistence.size=2Gi`, `defaultRetentionPolicyDays=7`
    - `grep -cE 'endpoint=https?://' docs/INSTALL.md` returns 0 (no scheme-prefixed endpoint in any copy-pasteable command)
    - The re-verified pin + fetch date are noted in the 47-03-SUMMARY (auditability chain per D-03)
  </acceptance_criteria>
  <done>INSTALL.md quickstart lands in the Prometheus precedent's shape with a same-day-verified pin and real rendered Service name.</done>
</task>

<task type="auto">
  <name>Task 2: observability.md "Self-hosted Phoenix" reference subsection + env-table row + deep-link placeholder replacement</name>
  <files>docs/observability.md</files>
  <read_first>
    - docs/observability.md (§Tracing ~line 156+: env-var table at 162-167, collector example at 169-174, opt-down sampler honesty text at 180-219, deep-link section at 236-260 with the Phase 47 placeholder line)
    - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-RESEARCH.md (§Fresh values.yaml facts — postgresql defaults, weak PHOENIX_POSTGRES_PASSWORD "postgres", auth.secret[] list shape, maxSpansQueueSize sizing comment, resource requests)
  </read_first>
  <action>
    (1) Add a `### Self-hosted Phoenix` subsection under `## Tracing` (after the existing collector example, presenting the Phoenix endpoint as the concrete instance of the already-shipped bare-host:port pattern, not a new one). Content, in the file's reference-depth voice (the Prometheus ServiceMonitor/Retention-sizing shape): (a) Quickstart path — SQLite-on-PVC values with the sizing rationale: chart's own ~50KiB/span heuristic (`server.maxSpansQueueSize` comment: 20,000 spans ≈ 1GiB) means 2Gi + 7-day retention comfortably holds fixture-scale runs on the ~8GiB dev VM; the chart's 20Gi default and retention `0` (never expire) are the values being deliberately overridden — infinite retention of full message arrays is unacceptable; fold Phoenix's pod footprint (requests 1Gi/limits 2Gi) into the one-heavy-workload-at-a-time VM discipline. (b) Durable path — bundled Postgres IS the chart default (`postgresql.enabled=true`, 20Gi); document it as the chart's tested happy path with the honesty line that the Phase 47 live proof exercised the quickstart path only (D-07); call out overriding `PHOENIX_POSTGRES_PASSWORD` (chart default is the literal string `postgres`) and the weak `admin` initial admin password — values to override, not accept; note the two storage modes are mutually exclusive and the chart itself fails the install on a bad combination (validatePersistence guard — no hand-holding needed). (c) Auth posture — chart-default auth ON (opposite of the raw image; keep it), credentials Secret-sourced, System-API-key → `Authorization=Bearer` header flow cross-referencing INSTALL.md's step; a dev-only `auth.enableAuth=false` override may be mentioned honestly with the exposure framing: port-forward-only access, never NodePort/LoadBalancer — a LAN-reachable Phoenix holds full (redacted but substantial) prompt/completion history. (2) Extend the env-var table with one row matching the existing column shape exactly: `OTEL_EXPORTER_OTLP_HEADERS` / `otel.exporter.headersSecretRef` / `""` (→ no headers sent). Below the table, describe the full chain in one short paragraph: values key → secretKeyRef env on manager + dashboard Deployments → manager forwards the value onto reporter Job env — with the one-sentence honesty note that the forward is a literal on Job specs in project namespaces (Job-read RBAC there already implies access to the strictly-more-sensitive tide-secrets). (3) In the deep-link section, replace the "install instructions land in Phase 47; this section covers only the config value and what it wires up" line with a cross-reference to the new Self-hosted Phoenix subsection and INSTALL.md's "Enable tracing (Phoenix)" step; keep the ≥ 14.2.0 appVersion floor sentence, updating it to note the pinned chart's appVersion (18.0.0 or the Task-1 re-verified value) clears it. Voice throughout: tight, declarative, em-dash-heavy; honest-limitation callouts in the opt-down sampler style.
  </action>
  <verify>
    <automated>grep -qc 'Self-hosted Phoenix' docs/observability.md && test "$(grep -c 'install instructions land in Phase 47' docs/observability.md)" = "0" && grep -qc 'OTEL_EXPORTER_OTLP_HEADERS' docs/observability.md && grep -qc 'headersSecretRef' docs/observability.md && test "$(grep -cE 'endpoint=https?://' docs/observability.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `### Self-hosted Phoenix` subsection exists under `## Tracing`; contains `persistence.enabled`, `postgresql.enabled`, `defaultRetentionPolicyDays`, `PHOENIX_POSTGRES_PASSWORD`, `auth.enableAuth`, and the D-07 honesty line naming which path the live proof exercised
    - Env table has the OTEL_EXPORTER_OTLP_HEADERS row; the reporter-forward honesty sentence is present
    - `grep -c 'install instructions land in Phase 47' docs/observability.md` returns 0; the deep-link section cross-references both the new subsection and INSTALL.md
    - Zero scheme-prefixed endpoint values in any command; zero unpinned Phoenix chart references
    - No literal secret/token values anywhere (all credential examples use --from-literal with a shell variable or reference a Secret by name)
  </acceptance_criteria>
  <done>observability.md carries the full reference recipe; placeholder discharged; PHX-01's both-paths + auth-default letter satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator copy-paste | doc examples become real cluster state; a weak default or literal secret in a doc becomes a deployed weakness |
| operator LAN → Phoenix UI/OTLP | exposure surface for prompt/completion history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Elevation of Privilege | weak Phoenix defaults (`admin`, `postgres`) copy-pasted | mitigate | doc explicitly names both as values to override, not accept (Task 2b/2c) |
| T-47-04 | Information Disclosure | Phoenix reachable beyond loopback (NodePort/ingress chart defaults) | mitigate | recipe pins `service.type=ClusterIP` + `ingress.enabled=false` + auth ON + port-forward-only framing for any auth-off mention |
| T-47-05 | Information Disclosure | literal credentials in doc examples | mitigate | every secret flows through `--from-literal` with a shell variable or a Secret name reference; acceptance criteria grep-gate scheme/literal patterns |
| T-47-SC | Tampering | Phoenix chart provenance | accept | RESEARCH.md Package Legitimacy Audit: Approved via two independent live channels (OCI digest + GitHub raw Chart.yaml); every doc example version-pinned |
</threat_model>

<verification>
- Both automated grep gates green; `make helm-assert` still green (docs don't touch the chart, sanity only)
- Manual read-through: the two sections cross-reference each other and NOTES.txt's pointer text matches INSTALL.md's heading
</verification>

<success_criteria>
- An operator can execute INSTALL.md's Phoenix step top-to-bottom without consulting anything else (the exact property Plan 47-04 will prove)
- observability.md answers every depth question the quickstart defers: storage choice, sizing, retention, auth, headers chain, deep-link floor
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-03-SUMMARY.md` when done (record the re-verified pin + fetch date).
</output>
