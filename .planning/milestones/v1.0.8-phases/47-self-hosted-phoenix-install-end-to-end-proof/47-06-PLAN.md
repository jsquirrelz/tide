---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 06
type: execute
wave: 5
depends_on: [47-01, 47-03]
files_modified:
  - internal/controller/reporter_jobspec.go
  - internal/controller/reporter_jobspec_test.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/controller/task_controller.go
  - cmd/manager/main.go
  - docs/observability.md
  - docs/INSTALL.md
autonomous: true
gap_closure: true
requirements: [PHX-01, PHX-02]

must_haves:
  truths:
    - "No reporter Job PodSpec carries the decoded OTLP bearer header as a literal EnvVar.Value"
    - "Reporter Jobs reference OTEL_EXPORTER_OTLP_HEADERS via valueFrom.secretKeyRef to the fixed-name Secret tide-otlp-headers in the project namespace, marked Optional"
    - "docs/observability.md no longer states the false RBAC-equivalence claim; it states the actual exposure posture and the per-namespace mirror requirement"
    - "docs/INSTALL.md's Phoenix step documents mirroring tide-otlp-headers into each Project namespace, in the same voice as the tide-signing-key mirror"
  artifacts:
    - path: "internal/controller/reporter_jobspec.go"
      provides: "ReporterOTLPHeadersSecretName/Key consts + secretKeyRef env construction, no literal header value"
    - path: "internal/controller/reporter_jobspec_test.go"
      provides: "Updated OTLPHeaders test pair asserting secretKeyRef shape and absence of literal Value"
    - path: "docs/observability.md"
      provides: "Corrected exposure-posture paragraph (WR-03 discharged)"
    - path: "docs/INSTALL.md"
      provides: "tide-otlp-headers project-namespace mirror step"
  key_links:
    - from: "cmd/manager/main.go"
      to: "internal/controller/dispatch_helpers.go"
      via: "OTLPHeadersSecret field on PlannerReconcilerDeps/TaskReconcilerDeps (Secret NAME, never the decoded value)"
      pattern: "OTLPHeadersSecret"
    - from: "internal/controller/{milestone,phase,plan,project,task}_controller.go"
      to: "internal/controller/reporter_jobspec.go"
      via: "ReporterOptions.OTLPHeadersSecret at all five spawn sites"
      pattern: "OTLPHeadersSecret:\\s*r\\.Deps\\.OTLPHeadersSecret"
    - from: "internal/controller/reporter_jobspec.go"
      to: "project-namespace Secret tide-otlp-headers"
      via: "valueFrom.secretKeyRef with Optional=true"
      pattern: "SecretKeyRef"
---

<objective>
Close verification gap #1 (CR-02, blocker — this phase's own wiring): `internal/controller/reporter_jobspec.go:305` writes the decoded `OTEL_EXPORTER_OTLP_HEADERS` bearer literal (`Authorization=Bearer <phoenix-token>`) as a plaintext `EnvVar.Value` on every reporter Job PodSpec in project namespaces. Root-fix by referencing a per-project-namespace mirrored Secret via `valueFrom.secretKeyRef` — mirroring the established `tide-signing-key` cross-namespace mirror pattern — and correct the tied false doc claim (WR-03, `docs/observability.md:170-179`).

Purpose: restore the codebase's own "never a literal secret value on a Job spec" invariant (enforced for manager/dashboard Deployments via secretKeyRef and for provider creds via EnvFrom:SecretRef in `internal/dispatch/podjob/jobspec.go:342-352`). The registered T-47-02 acceptance rationale ("Job-read RBAC already implies access to tide-secrets") is factually unsound: a secretKeyRef exposes only the Secret NAME; the literal exposed the decoded credential — a strictly larger exposure. Fix-shape is root-fix per the user's standing preference; do NOT record an override.

Output: reporter Jobs whose OTLP-headers env is Secret-referenced, updated unit tests proving it, and honest operator docs including the mirror step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-VERIFICATION.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-CONTEXT.md

Constraint: `charts/tide/values.yaml` is a FIXED contract. This plan makes NO chart changes — the chart's `otel.exporter.headersSecretRef` wiring (manager/dashboard Deployments via secretKeyRef) is already correct and stays untouched. Only the Go-side reporter-Job forwarding and docs change.

Established fixed-name convention (already in docs/INSTALL.md:251-252): the headers Secret is named `tide-otlp-headers` with data key `OTEL_EXPORTER_OTLP_HEADERS`. The project-namespace mirror reuses the same name/key, exactly like `tide-signing-key` (hardcoded by name in `internal/dispatch/podjob/jobspec.go:345`, mirrored by operators per `examples/projects/medium/README.md` step 4 and `hack/scripts/acceptance-v1.sh:135-151`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace literal OTLP-headers forwarding with per-namespace secretKeyRef across the Go chain</name>
  <files>internal/controller/reporter_jobspec.go, internal/controller/reporter_jobspec_test.go, internal/controller/dispatch_helpers.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/task_controller.go, cmd/manager/main.go</files>
  <read_first>
    - internal/controller/reporter_jobspec.go (ReporterOptions.OTLPHeaders field doc comment ~lines 99-112 carrying the unsound T-47-02 rationale; env construction ~lines 299-308; note the file's pointer-local style for bool pointers)
    - internal/controller/reporter_jobspec_test.go (TestBuildReporterJob_OTLPHeadersEnv / TestBuildReporterJob_OTLPHeadersWithoutEndpointNoEnv, and the OTLPEndpoint pair at lines 789-861 for the map-comparison idiom)
    - internal/controller/dispatch_helpers.go (PlannerReconcilerDeps.OTLPHeaders field ~lines 225-229)
    - internal/controller/task_controller.go (TaskReconcilerDeps OTLPHeaders field ~lines 90-119; spawnTaskTraceReporterIfNeeded call site ~line 1113)
    - internal/controller/milestone_controller.go:653-663, phase_controller.go:~610, plan_controller.go:~665, project_controller.go:~1920-1932 (the five ReporterOptions literals)
    - cmd/manager/main.go:280-295 (env read + comment), :435-445 (plannerDeps), :548-558 (TaskReconcilerDeps)
    - internal/dispatch/podjob/jobspec.go:342-352 (the tide-signing-key fixed-name Secret reference pattern being mirrored)
  </read_first>
  <action>
    This is ONE mechanical rename+reshape applied across 9 files (per 47-PATTERNS.md "one pattern applied N times" convention) — apply it at EVERY site listed, not just the first:

    1. `internal/controller/reporter_jobspec.go`: add two exported consts near the top-of-file const block: `ReporterOTLPHeadersSecretName = "tide-otlp-headers"` and `ReporterOTLPHeadersSecretKey = "OTEL_EXPORTER_OTLP_HEADERS"`, with a comment stating these mirror the fixed-name `tide-signing-key` per-project-namespace convention (podjob/jobspec.go:345) and match the Secret name/key docs/INSTALL.md already establishes for tide-system. Rename `ReporterOptions.OTLPHeaders` → `OTLPHeadersSecret` (still string — but it now carries the project-namespace Secret NAME to reference, empty = no headers env). REWRITE the field doc comment: delete the entire "The value is a literal on the Job spec ... accepted per threat T-47-02 ... strictly more sensitive credential than an ingest-scoped Phoenix token" passage; replace with: the env is emitted as `valueFrom.secretKeyRef` against the per-project-namespace `tide-otlp-headers` mirror (operator-mirrored, same convention as tide-signing-key); the Job spec exposes only the Secret name; the ref is Optional so a missing mirror degrades to an unauthenticated (dark-trace) reporter rather than blocking child-CRD materialization with CreateContainerConfigError (matching the Phase 44 D-10 exit-0/dark-pipe posture). In the env construction inside the `if opts.OTLPEndpoint != ""` block: replace the literal-append with an append of a `corev1.EnvVar{Name: ReporterOTLPHeadersSecretKey, ValueFrom: &corev1.EnvVarSource{SecretKeyRef: ...}}` where SecretKeyRef has `Name: opts.OTLPHeadersSecret`, `Key: ReporterOTLPHeadersSecretKey`, and `Optional` pointing at true (use a local `optionalTrue := true` matching the file's existing pointer-local style). Guard unchanged: only when both OTLPEndpoint and OTLPHeadersSecret are non-empty.
    2. `internal/controller/dispatch_helpers.go`: rename `PlannerReconcilerDeps.OTLPHeaders` → `OTLPHeadersSecret`; comment now says it carries the per-project-namespace headers-Secret NAME (never the decoded value) forwarded into reporter Jobs as a secretKeyRef; empty = no headers env.
    3. `internal/controller/task_controller.go`: same rename on `TaskReconcilerDeps`.
    4. All five spawn-site ReporterOptions literals (`milestone_controller.go` ~:657, `phase_controller.go` ~:610, `plan_controller.go` ~:665, `project_controller.go` ~:1925, `task_controller.go` ~:1113): change `OTLPHeaders: r.Deps.OTLPHeaders,` → `OTLPHeadersSecret: r.Deps.OTLPHeadersSecret,`. All five, same relative position after OTLPEndpoint.
    5. `cmd/manager/main.go`: keep the `os.Getenv("OTEL_EXPORTER_OTLP_HEADERS")` read (presence detection only). Derive `otlpHeadersSecret := ""` and set it to `controller.ReporterOTLPHeadersSecretName` when the env value is non-empty. Assign `OTLPHeadersSecret: otlpHeadersSecret,` in BOTH plannerDeps (~:443) and TaskReconcilerDeps (~:555), replacing the `OTLPHeaders: otlpHeaders,` lines. Update the Phase 47 comment at ~:285-288: the manager reads the env only to learn WHETHER headers are configured; the decoded value never leaves the manager process — reporter Jobs resolve the value themselves from the mirrored Secret.
    6. `internal/controller/reporter_jobspec_test.go`: update the existing pair. `TestBuildReporterJob_OTLPHeadersEnv`: set `OTLPHeadersSecret: controller.ReporterOTLPHeadersSecretName` alongside OTLPEndpoint; assert the container env contains an entry named `OTEL_EXPORTER_OTLP_HEADERS` whose `Value` is the empty string AND whose `ValueFrom.SecretKeyRef` has Name `tide-otlp-headers`, Key `OTEL_EXPORTER_OTLP_HEADERS`, and non-nil Optional dereferencing to true; the OTLP endpoint + batch-size entries remain literal as before. `TestBuildReporterJob_OTLPHeadersWithoutEndpointNoEnv`: OTLPHeadersSecret set, endpoint empty → zero env entries (semantics unchanged, field renamed).
  </action>
  <verify>
    <automated>go build ./cmd/manager/... ./internal/controller/... ./internal/dispatch/... && go vet ./cmd/manager/... ./internal/controller/... && go test ./internal/controller/... -run 'TestBuildReporterJob' -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/... -run 'TestBuildReporterJob' -count=1` passes with the updated OTLPHeaders pair present in output.
    - `grep -v '^\s*//' internal/controller/reporter_jobspec.go | grep -cE 'OTEL_EXPORTER_OTLP_HEADERS", Value:'` returns 0 (no literal-value construction survives outside comments).
    - `grep -rn 'OTLPHeadersSecret: r.Deps.OTLPHeadersSecret' internal/controller/ | wc -l` returns 5 (all five spawn sites converted).
    - `grep -rn 'OTLPHeaders string' internal/controller/dispatch_helpers.go internal/controller/task_controller.go internal/controller/reporter_jobspec.go | wc -l` returns 0 (no decoded-value field remains on Deps or ReporterOptions).
    - `grep -c 'ReporterOTLPHeadersSecretName' cmd/manager/main.go` returns >= 1 (manager threads the fixed name, not the value).
    - `grep -v '^\s*//' internal/controller/reporter_jobspec.go | grep -c 'T-47-02'` returns 0 (unsound rationale removed from live comment text).
  </acceptance_criteria>
  <done>Reporter Job specs reference the headers Secret by name via Optional secretKeyRef; the decoded bearer value appears nowhere in any Deps struct, ReporterOptions, or Job spec; unit pair proves both presence-shape and absence.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the exposure-posture docs and add the project-namespace mirror step</name>
  <files>docs/observability.md, docs/INSTALL.md</files>
  <read_first>
    - docs/observability.md:160-200 (the env-var table, the headersSecretRef paragraph at ~168-179 carrying the WR-03 false claim, and the surrounding house voice)
    - docs/INSTALL.md:236-268 ("Enable tracing (Phoenix)" step 2 — the tide-otlp-headers create + helm upgrade blocks)
    - docs/INSTALL.md Provider Secret section (~lines 268-290) and examples/projects/medium/README.md:124-138 (the signing-key mirror voice: extract-with-jsonpath OR repeat --from-literal in the target namespace; "never paste into YAML")
  </read_first>
  <action>
    1. `docs/observability.md` (~lines 168-179): REWRITE the paragraph beginning "`otel.exporter.headersSecretRef` is a `{name, key}` pair..." from the clause "the manager also forwards the same resolved header value onto every reporter Job it spawns" onward. Delete the false claim ("the value is a literal on the Job spec in the project namespace, accepted because Job-read RBAC there already implies access to the project's tide-secrets, a strictly more sensitive credential"). Replace with the actual posture, stated plainly in the file's declarative em-dash voice: reporter Jobs run in each Project's namespace and reference the headers via `valueFrom: secretKeyRef` against a Secret named `tide-otlp-headers` (key `OTEL_EXPORTER_OTLP_HEADERS`) mirrored into that namespace — the same fixed-name cross-namespace mirror convention as `tide-signing-key`. State the exposure delta explicitly: a secretKeyRef exposes only the Secret NAME on the Job spec; reading the token requires `get` on Secrets in that namespace — Job-read RBAC alone never sees the credential. State the degrade behavior: the ref is Optional — if the mirror is missing, the reporter runs without auth headers and an auth-ON Phoenix silently rejects its spans (dark traces, visible only in reporter pod logs), while child-CRD materialization proceeds; the fix is to create the mirror. Cross-reference the INSTALL.md mirror step added below.
    2. `docs/INSTALL.md` §"Enable tracing (Phoenix)" step 2: immediately after the existing `kubectl create secret generic tide-otlp-headers -n tide-system ...` block, add a short mirror instruction: reporter Jobs (which export the Task-level LLM spans) run in each Project's namespace, so the same Secret must exist there under the same name — repeat the create with `--namespace <project-namespace>` (e.g. `tide-sample-medium`) while `PHOENIX_API_KEY` is still in the shell, same `--from-literal` voice, "never paste the key into YAML". Note it mirrors the signing-key per-namespace mirror convention the samples already use, and that omitting it degrades exactly as described in observability.md (reporter spans dark, everything else unaffected). Keep the section's existing step numbering intact — this is an addition inside step 2, not a renumber.
  </action>
  <verify>
    <automated>grep -c 'tide-otlp-headers' docs/INSTALL.md && grep -c 'secretKeyRef' docs/observability.md && ! grep -qE 'already implies access to (the project.s )?tide-secrets' docs/observability.md && echo WR-03-CLAIM-REMOVED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'already implies access' docs/observability.md` returns 0 (RBAC-equivalence claim gone).
    - `grep -c 'tide-otlp-headers' docs/INSTALL.md` returns >= 2 (tide-system create + project-namespace mirror).
    - `grep -ci 'optional' docs/observability.md` returns >= 1 in the tracing section context (degrade behavior documented).
    - docs/observability.md names the Secret NAME-vs-value exposure delta in prose: `grep -cE 'Secret (NAME|name)' docs/observability.md` returns >= 1.
    - Neither file contains a raw example token: `grep -rc 'sk-ant' docs/INSTALL.md docs/observability.md` returns 0 for each.
  </acceptance_criteria>
  <done>Operators reading either doc see the true exposure posture, the fixed-name mirror requirement, the exact mirror command, and the missing-mirror symptom — no settled-fact false claim remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager (tide-system) → project-namespace Job specs | Job/Pod specs in project namespaces are readable by any principal with Job-read RBAC there — a weaker audience than Secret-read |
| reporter Job → Phoenix OTLP ingest | bearer-authenticated gRPC export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-06-01 | Information Disclosure | reporter Job PodSpec env | mitigate | Replace literal EnvVar.Value with valueFrom.secretKeyRef to per-namespace `tide-otlp-headers`. Corrected T-47-02 rationale: a secretKeyRef exposes the Secret NAME only; the literal exposed the decoded credential to anyone with Job-read RBAC — strictly larger, not equivalent, exposure. Root-fix, no override recorded. |
| T-47-06-02 | Denial of Service | reporter Job start (missing mirror Secret) | mitigate | `Optional: true` on the secretKeyRef — missing mirror degrades to unauthenticated export (dark traces) instead of CreateContainerConfigError blocking child-CRD materialization; symptom + fix documented in both docs |
| T-47-06-03 | Tampering (operator trust) | docs/observability.md:170-179 | mitigate | WR-03: rewrite the settled-fact false equivalence claim to the actual posture in Task 2 |
| T-47-SC | Tampering | package installs | n/a | No new dependencies installed by this plan (Go stdlib + existing k8s.io/api types only) |
</threat_model>

<verification>
- `go build ./cmd/manager/... ./internal/controller/... ./internal/dispatch/...` exits 0.
- `go test ./internal/controller/... -run 'TestBuildReporterJob' -count=1` passes (headers pair asserts secretKeyRef shape, no literal).
- No live-code literal-value construction for OTEL_EXPORTER_OTLP_HEADERS remains in reporter_jobspec.go (comment-filtered grep = 0).
- Both docs updated; false claim absent; mirror step present.
</verification>

<success_criteria>
Gap #1 (CR-02) closed at the root: the decoded Phoenix bearer never appears in any Job spec; reporter auth flows through the mirrored per-namespace Secret by reference; WR-03's false doc claim replaced with the true exposure posture and the operator mirror recipe.
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-06-SUMMARY.md` when done.
</output>
