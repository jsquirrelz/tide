---
phase: 45-runtime-neutral-adapter-seam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/tide-reporter/main.go
  - cmd/tide-reporter/main_test.go
  - cmd/tide-reporter/adapter_seam_test.go
  - internal/reporter/tracesynth.go
autonomous: true
requirements: [ADAPT-01]
must_haves:
  truths:
    - "parseFlags maps the bareword --skip-message-spans into reporterConfig.SkipMessageSpans; an absent flag parses to false (D-03 absent-means-synthesize, asserted on the PARSED struct field per Pitfall 3)"
    - "synthesizeSpans returns as its literal first statement when cfg.SkipMessageSpans is true — before path construction and before the sentinel os.Stat — and writes no sentinel (D-05 sole skip point)"
    - "With the flag set and a valid real-shaped events.jsonl on disk, a reporter run emits ZERO synthesized spans — the shared exporter records exactly the stub runtime's own span, parented under the injected traceparent (D-09 contract test, ADAPT-01 criterion 3)"
    - "With the flag absent, synthesis proceeds unchanged: TestRunTraceOnly_EmitsSpans passes UNMODIFIED (D-10 inverse pin — Phase 44 behavior byte-identical)"
    - "D-07: the seam stays the capability flag + W3C traceparent contract, NOT a Go interface — no TraceSynthesizer interface is extracted, tracesynth.go is not moved out of internal/reporter, and its updated doc contract names it the anthropic-CLI runtime's adapter (D-08)"
  artifacts:
    - path: "cmd/tide-reporter/main.go"
      provides: "skip-message-spans flag registration + reporterConfig.SkipMessageSpans + the D-05 skip guard in synthesizeSpans"
      contains: "SkipMessageSpans"
    - path: "cmd/tide-reporter/adapter_seam_test.go"
      provides: "D-09 contract test: stub self-instrumenting runtime + reporter sharing one InMemoryExporter, zero duplicate spans"
      contains: "TestAdapterSeam_SelfInstrumentingRuntimeNoDuplicateSpans"
      min_lines: 40
    - path: "cmd/tide-reporter/main_test.go"
      provides: "flag-parse assertions (present + absent) and the skip-behavior unit case"
      contains: "TestRunTraceOnly_SkipsSynthesisWhenFlagSet"
    - path: "internal/reporter/tracesynth.go"
      provides: "D-08 doc contract: names itself the anthropic-CLI runtime's trace adapter and pkg/dispatch.SelfInstruments as the routing datum"
      contains: "SelfInstruments"
  key_links:
    - from: "cmd/tide-reporter/main.go parseFlags"
      to: "reporterConfig"
      via: "fs.Bool registration copied into the returned struct literal"
      pattern: "skip-message-spans"
    - from: "cmd/tide-reporter/main.go synthesizeSpans"
      to: "skip decision"
      via: "early return guard on cfg.SkipMessageSpans as the first statement"
      pattern: "cfg\\.SkipMessageSpans"
    - from: "cmd/tide-reporter/adapter_seam_test.go"
      to: "pkg/otelai/tracecontext.go"
      via: "otelai.ExtractRemoteParent — the same env-carrier primitive the reporter itself uses"
      pattern: "ExtractRemoteParent"
---

<objective>
Land the reporter-side half of the ADAPT-01 seam: parse `--skip-message-spans` into `reporterConfig`, guard `synthesizeSpans` as the phase's SOLE skip point (D-05 — return before `ReconstructConversation`, no sentinel write), and prove the seam with the D-09 contract test — a stub self-instrumenting runtime sharing one `tracetest.InMemoryExporter` with a real reporter run over a real-shaped `events.jsonl`, asserting the exporter holds exactly the stub's own span and nothing synthesized. Finish with the D-08 doc contract on `tracesynth.go` so the seam is discoverable.

This plan is independent of Plan 01 (zero file overlap; the reporter binary never imports `pkg/dispatch.SelfInstruments` — it trusts only the manager-computed boolean parsed from its own Args, D-02). The shared contract between the plans is the exact flag string `--skip-message-spans`, locked in both plan texts.

Purpose: ADAPT-01 criteria 2 and 3 — "the reporter skips message-span synthesis entirely when the flag is set" and "a contract test proves zero duplicate spans end-to-end (env-carrier extraction only — no LangGraph-specific span shape assumed)."
Output: reporter flag + skip guard + unit tests; new `adapter_seam_test.go`; `tracesynth.go` doc contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-runtime-neutral-adapter-seam/45-CONTEXT.md
@.planning/phases/45-runtime-neutral-adapter-seam/45-PATTERNS.md

Discretion calls made by the planner (within CONTEXT.md's grants, mirrored in Plan 01):
- Flag: bareword `--skip-message-spans`, registered via `fs.Bool("skip-message-spans", false, ...)` — Go zero-value false = D-03 absent-means-synthesize at the parse layer.
- Contract test lives in a new `cmd/tide-reporter/adapter_seam_test.go` (package main) so it reuses `installStubTracerProvider`/`writeTraceOnlyFixture` unmodified (research Assumption A3).
- D-08 doc scope: tracesynth.go package doc + the :613 D-07 inline comment only; no `pkg/otelai` edits (attrs.go:232 cross-reference declined — keep the diff minimal).

<interfaces>
<!-- Verified against main this session. Executor: use these directly — no exploration needed. -->

From cmd/tide-reporter/main.go:
```go
type reporterConfig struct { // :87-96
    Workspace, ProjectUID, TaskUID, ParentName, ParentNamespace, ParentKind, TraceParent string
    TraceOnly bool // :95 — add SkipMessageSpans bool alongside
}
func parseFlags(args []string) (reporterConfig, error)                              // :113
func runWithClient(ctx context.Context, cfg reporterConfig, _ io.Writer,
    stderr io.Writer, clientOverride client.Client) int                              // :166 — tests pass nil client
func synthesizeSpans(ctx context.Context, cfg reporterConfig, stderr io.Writer)      // :316
// exitSuccess = 0 (:99); trace-only branch calls synthesizeSpans then returns exitSuccess (:193-200)
// sentinel: filepath.Join(cfg.Workspace, "envelopes", cfg.TaskUID, ".spans-emitted") (:319)
```

From cmd/tide-reporter/main_test.go (reuse verbatim — same package):
```go
func installStubTracerProvider(t *testing.T, exp sdktrace.SpanExporter) *shutdownRecorder // :396 — swaps newTracerProvider at otel.SetTracerProvider via sdktrace.NewTracerProvider(sdktrace.WithSyncer(exp))
func writeTraceOnlyFixture(t *testing.T, workspace, taskUID string)                        // :417 — real 2-call events.jsonl + in.json; normally yields 2 synthesized spans
// TestParseFlagsTraceparent (:336) — the parse-assertion shape; TestRunTraceOnly_EmitsSpans (:532) — the run-assertion shape (cfg literal + runWithClient(ctx, cfg, nil, &stderr, nil) + exp.GetSpans())
```

From pkg/otelai/tracecontext.go:
```go
func FormatTraceparent(traceID trace.TraceID, spanID trace.SpanID, sampled bool) string // :69
func ExtractRemoteParent(ctx context.Context, traceparent string) context.Context       // :93 — the env-carrier primitive; synthesizeSpans uses it today (:327)
```

From internal/reporter/tracesynth.go: package doc at :17-30 (currently "the Phase 44 LLM message-array-span synthesizer"); D-07 inline comment at :613-616 ("Phase 45's adapter seam is where runtime-neutral dispatch lands" — the forward reference this plan resolves).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse --skip-message-spans into reporterConfig and guard synthesizeSpans as the D-05 sole skip point</name>
  <files>cmd/tide-reporter/main.go, cmd/tide-reporter/main_test.go</files>
  <read_first>
    - cmd/tide-reporter/main.go — reporterConfig (:87-96), parseFlags (:113-141, the --trace-only fs.Bool precedent), synthesizeSpans (:316-356, current first statements: path construction then the sentinel os.Stat the new guard must PRECEDE)
    - cmd/tide-reporter/main_test.go — TestParseFlagsTraceparent (:336, the "assert the PARSED struct field" shape), TestRunTraceOnly_EmitsSpans (:532, the run shape to invert), installStubTracerProvider (:396), writeTraceOnlyFixture (:417)
    - .planning/phases/45-runtime-neutral-adapter-seam/45-PATTERNS.md §"cmd/tide-reporter/main.go" and §"main_test.go" — verified before/after excerpts including the exact guard text and Pitfall 3 warning
  </read_first>
  <behavior>
    - Test 1 (TestParseFlagsSkipMessageSpans, present subtest): parseFlags on args including bare "--skip-message-spans" (plus the required --project-uid/--task-uid/--parent-name/--parent-namespace/--parent-kind entries, mirroring TestParseFlagsTraceparent) returns cfg with SkipMessageSpans == true
    - Test 2 (absent subtest — D-03/D-10 polarity): parseFlags WITHOUT the flag returns cfg.SkipMessageSpans == false
    - Test 3 (TestRunTraceOnly_SkipsSynthesisWhenFlagSet): with installStubTracerProvider + writeTraceOnlyFixture (a fixture that normally yields spans) and cfg{TraceOnly: true, TaskUID: ..., Workspace: ..., SkipMessageSpans: true}, runWithClient(ctx, cfg, nil, &stderr, nil) returns exitSuccess, exp.GetSpans() is empty, os.Stat on the ".spans-emitted" sentinel returns a not-exist error, and stderr contains the skip log line
    - Invariant: TestRunTraceOnly_EmitsSpans is NOT modified — it is the D-10 inverse pin proving the unset flag still synthesizes
  </behavior>
  <action>
    Write the failing tests first, then implement three edits in main.go:

    1. reporterConfig: add `SkipMessageSpans bool` alongside TraceOnly with a short comment citing Phase 45 ADAPT-01/D-05.
    2. parseFlags: register `skipMessageSpans := fs.Bool("skip-message-spans", false, ...)` with usage text noting "self-instrumenting vendor; absent = synthesize (D-03)", and copy `*skipMessageSpans` into the returned reporterConfig literal as `SkipMessageSpans: *skipMessageSpans`. Pitfall 3: a registered flag never copied into the struct silently no-ops the feature — the test MUST assert the parsed struct field, not fs.Parse success.
    3. synthesizeSpans: add the skip guard as the LITERAL FIRST statement — before eventsPath/inJSONPath/sentinelPath construction and before the sentinel os.Stat: when cfg.SkipMessageSpans, write one stderr line (e.g. "tide-reporter: self-instrumenting vendor — skipping message-span synthesis (D-05)") and return. A skipped run touches no PVC path and writes no sentinel (the sentinel dedupes emission; a skipped run emits nothing). Both existing call sites (trace-only branch :198, combined-mode path :225) route through this one function, so no other edit is needed — D-05's single-skip-point falls out structurally. Do NOT touch materialization logic (combined-mode reporters still materialize unconditionally; the flag disables only synthesis). Do NOT add any new non-zero exit path (Phase 44 D-10 exit-0 posture preserved — skipping cannot fail).
  </action>
  <verify>
    <automated>go test ./cmd/tide-reporter/... -run 'TestParseFlags|TestRunTraceOnly' -v</automated>
  </verify>
  <acceptance_criteria>
    - reporterConfig contains `SkipMessageSpans bool`; parseFlags registers "skip-message-spans" with default false and the returned struct literal includes `SkipMessageSpans: *skipMessageSpans`
    - The guard `if cfg.SkipMessageSpans` is the first statement of synthesizeSpans, appearing BEFORE the line constructing eventsPath (source-order check: the cfg.SkipMessageSpans guard's line number is lower than eventsPath's within the function)
    - main_test.go contains TestParseFlagsSkipMessageSpans (present + absent subtests asserting cfg.SkipMessageSpans) and TestRunTraceOnly_SkipsSynthesisWhenFlagSet (zero spans + no sentinel + exitSuccess)
    - `git diff cmd/tide-reporter/main_test.go` shows zero hunks inside TestRunTraceOnly_EmitsSpans
    - `go test ./cmd/tide-reporter/... -run 'TestParseFlags|TestRunTraceOnly' -v` exits 0 with all cases PASS (including the pre-existing ones)
  </acceptance_criteria>
  <done>The reporter parses the manager's flag and synthesizeSpans is the proven sole skip point: flag set → zero synthesis, zero sentinel, exit 0; flag absent → Phase 44 behavior untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the D-09 adapter-seam contract test and the D-08 doc contract on tracesynth.go</name>
  <files>cmd/tide-reporter/adapter_seam_test.go, internal/reporter/tracesynth.go</files>
  <read_first>
    - cmd/tide-reporter/main_test.go — installStubTracerProvider (:396) and writeTraceOnlyFixture (:417) to reuse verbatim; TestRunTraceOnly_EmitsSpans (:532) for the TraceIDFromHex/FormatTraceparent/runWithClient conventions
    - pkg/otelai/tracecontext.go — ExtractRemoteParent (:93) and FormatTraceparent (:69) signatures
    - internal/reporter/tracesynth.go — package doc (:17-30) and the D-07 inline comment (:613-616) this task updates
    - .planning/phases/45-runtime-neutral-adapter-seam/45-RESEARCH.md §Pattern 4 — the complete worked example for the contract test (verified against real helper signatures)
    - .planning/phases/45-runtime-neutral-adapter-seam/45-PATTERNS.md §"internal/reporter/tracesynth.go" — exact replacement wording for both D-08 comment edits
  </read_first>
  <behavior>
    - TestAdapterSeam_SelfInstrumentingRuntimeNoDuplicateSpans, in one shared tracetest.InMemoryExporter installed via installStubTracerProvider:
      (a) env-carrier extraction: a stub self-instrumenting runtime calls otelai.ExtractRemoteParent(context.Background(), traceParent) on a synthetic traceparent built via trace.TraceIDFromHex("0af7651916cd43dd8448eb211c80319c") + trace.SpanIDFromHex("b7ad6b7169203331") + otelai.FormatTraceparent(traceID, spanID, true), then starts and ends its own span named "stub.graph.invoke" on that context — generic env-carrier mechanics only, ZERO LangGraph-specific span-shape assertions
      (b) zero duplicates: writeTraceOnlyFixture puts a REAL 2-call events.jsonl on disk; runWithClient with cfg{TraceOnly: true, TaskUID, Workspace, TraceParent: traceParent, SkipMessageSpans: true} returns exitSuccess; exp.GetSpans() has length exactly 1; that span's Name == "stub.graph.invoke" and SpanContext.TraceID() == the injected traceID (proving extraction activated the injected context)
      (c) no sentinel: os.Stat(workspace/envelopes/<taskUID>/.spans-emitted) returns a not-exist error (D-05)
  </behavior>
  <action>
    Create cmd/tide-reporter/adapter_seam_test.go in package main implementing the behavior block — RESEARCH.md §Pattern 4 contains a verified worked example whose helper calls all match the live signatures in this plan's interfaces block; follow it, asserting exact span count 1 (a length-0 exporter would mean the stub failed; length ≥2 would mean synthesis was not skipped — both must fail loudly with distinct messages). Order matters: emit the stub's span FIRST, then run the reporter, so a duplicate would be unambiguous.

    Then the D-08 doc contract on internal/reporter/tracesynth.go (comment-only, zero logic change):
    1. Package doc (:17-30): add that this file is specifically the anthropic-CLI runtime's trace adapter behind the ADAPT-01 seam (Phase 45) — it parses that runtime's events.jsonl stream format — and that pkg/dispatch.SelfInstruments is the routing datum the manager consults to decide whether a dispatch's reporter Job should invoke this parser at all (pointing to cmd/tide-reporter's synthesizeSpans skip guard).
    2. D-07 inline comment (:613-616): replace the stale forward reference "Phase 45's adapter seam is where runtime-neutral dispatch lands" with the resolved pointer: runtime-neutral routing lives one level up in pkg/dispatch.SelfInstruments + the reporter's --skip-message-spans skip guard (Phase 45); the hardcoded "anthropic" literal is correct because this function only ever runs for the anthropic-CLI adapter path. Do NOT parameterize LLMIdentity's vendor argument (Phase 42 D-07 locked the literal), do NOT move tracesynth.go, do NOT extract any interface (Phase 45 D-07).
  </action>
  <verify>
    <automated>go test ./cmd/tide-reporter/... -run TestAdapterSeam -v && go build ./... && go test ./internal/reporter/... && ! grep -rn "if.*[Vv]endor.*==" internal/controller cmd/tide-reporter internal/reporter</automated>
  </verify>
  <acceptance_criteria>
    - adapter_seam_test.go contains TestAdapterSeam_SelfInstrumentingRuntimeNoDuplicateSpans with the exact-count assertion `len(spans) != 1` (or equivalent) plus Name and TraceID assertions and the sentinel-absence check
    - The test contains no LangGraph-named identifiers or span-shape assumptions beyond the generic "stub.graph.invoke" stub name (grep for "langgraph"/"LangGraph"/"langchain" in adapter_seam_test.go returns zero hits)
    - `go test ./cmd/tide-reporter/... -run TestAdapterSeam -v` exits 0
    - tracesynth.go's package doc block (:17-30 region) contains both "anthropic-CLI" (or "anthropic CLI") and "SelfInstruments"; the :613-region comment no longer reads as a future reference ("is where runtime-neutral dispatch lands" no longer appears) and names SelfInstruments or --skip-message-spans
    - `git diff internal/reporter/tracesynth.go` contains only comment-line changes (no non-comment code hunks); `go test ./internal/reporter/...` exits 0 with existing tests unmodified
  </acceptance_criteria>
  <done>ADAPT-01 criterion 3 is proven in-process — a self-instrumenting runtime's spans coexist with a skipped reporter run at exactly zero duplicates — and the seam is discoverable from tracesynth.go itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager → reporter Job spec (Args) | manager-authored; the ONLY source the reporter may trust for the skip decision (D-02) |
| subagent pod → PVC (in.json / events.jsonl) | pod-writable, semi-trusted; the reporter reads it for synthesis content but never for control decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Spoofing / Repudiation | synthesizeSpans skip decision | mitigate | Guard reads only cfg.SkipMessageSpans parsed from the manager-authored Job Arg (Task 1); the reporter never opens in.json to re-derive Provider.Vendor for the skip decision, even though ReconstructConversation already reads in.json for the call-1 seed prompt. Suppressing synthesis (hiding LLM activity from Phoenix) therefore requires manager compromise, not pod compromise. |
| T-45-02 | Tampering (input) | parseFlags | mitigate | stdlib flag.Bool rejects malformed input automatically (ASVS V5, same validation --trace-only receives); unknown-flag crash-loop risk is covered by asserting parse of the exact registered name in TestParseFlagsSkipMessageSpans. Default false preserves fail-toward-visibility (D-03). |
| T-45-SC | Tampering (supply chain) | package installs | accept (N/A) | Zero new dependencies this phase; RESEARCH.md §Package Legitimacy Audit confirms the gate protocol is out of scope. |
</threat_model>

<verification>
- `go test ./cmd/tide-reporter/... ./internal/reporter/...` green — new flag/skip/contract tests pass AND every pre-existing Phase 44 test passes unmodified (the behavior-unchanged proof for the reporter side; TestRunTraceOnly_EmitsSpans is the D-10 inverse pin)
- `go test ./cmd/tide-reporter/... -run TestAdapterSeam -v` — the phase's headline proof (ADAPT-01 criterion 3)
- Source-shape gate: `grep -rn "if.*[Vv]endor.*==" internal/controller cmd/tide-reporter internal/reporter` returns zero hits
- `git diff internal/reporter/tracesynth.go` is comment-only; no changes to span shapes, redaction, or size bounds (Phase 44 D-01..D-12 stand as shipped)
</verification>

<success_criteria>
- ADAPT-01 criterion 2 satisfied: flag set → reporter skips synthesis entirely (no ReconstructConversation, no sentinel, exit 0); flag absent → synthesis byte-identical to Phase 44
- ADAPT-01 criterion 3 satisfied: contract test proves zero duplicate spans end-to-end with generic env-carrier extraction only
- D-08 legibility: a contributor reading tracesynth.go discovers the seam (SelfInstruments + skip guard) without reading planning docs
</success_criteria>

<output>
Create `.planning/phases/45-runtime-neutral-adapter-seam/45-02-SUMMARY.md` when done
</output>
