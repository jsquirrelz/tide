---
milestone: v1.0.6
name: Adoption-Path Correctness & Dispatch Safety
audited: 2026-06-29
status: tech_debt
scores:
  requirements: 13/13
  phases: 3/3
  integration: sound (0 blockers, 2 warnings)
  flows: n/a (no new API routes / frontend / E2E user flows; controller-seam fixes)
gaps: {}
tech_debt:
  - phase: project-level (pre-existing, Phase 27)
    items:
      - "W1 (ADOPT-04 asymmetry): project-level PlannerRolledUpUID stamp (project_controller.go:1376) uses plain client.MergeFrom + non-fatal swallow, NOT the retry.RetryOnConflict + MergeFromWithOptimisticLock pattern Phase 31/32 applied to the new child-level markers (MilestoneRolledUpUID/PhaseRolledUpUID/PlanRolledUpUID). Introduced in Phase 27 (BYPASS-03, 51c9b20) — pre-dates v1.0.6. A concurrent project-status write racing the project-planner completion could drop the marker on conflict → one double-count cycle. Low real-world probability (project planner fires once at run start under low concurrency). Fix: wrap in RetryOnConflict + re-fetch + optimistic lock, return error not swallow."
  - phase: 32-d3-dispatch-concurrency-cap
    items:
      - "W2 (CONCUR-04 cosmetic): charts/tide/templates/configmap.yaml:22 still renders `| default 16`; effective value is 4 (values.yaml:89 wins). The stale 16 only takes effect if an operator explicitly sets plannerConcurrency=null or omits it. Should read `| default 4`. Source: hack/helm/ is canonical; configmap.yaml template carries the fallback."
  - phase: 31/32/33 (traceability hygiene)
    items:
      - "Phase 32 SUMMARY frontmatter `requirements_completed` is empty ([]) for 32-01 and 32-02; CONCUR-01..04 are confirmed satisfied by 32-VERIFICATION.md (5/5) and REQUIREMENTS.md ([x]), but the SUMMARY frontmatter should list them for clean 3-source traceability."
  - phase: nyquist (process flags)
    items:
      - "Phase 31: no VALIDATION.md (Nyquist MISSING). Phase 32 & 33: VALIDATION.md present but nyquist_compliant:false (PARTIAL) — /gsd:validate-phase never run to flip the flag. Test coverage itself EXISTS and passes (envtests for every requirement; full controller suite 167 specs green); these are process-flag gaps, not coverage gaps."
nyquist:
  compliant_phases: 0
  partial_phases: 2   # 32, 33
  missing_phases: 1   # 31
  overall: partial
---

# v1.0.6 Milestone Audit — Adoption-Path Correctness & Dispatch Safety

**Audited:** 2026-06-29
**Status:** `tech_debt` — all requirements satisfied, no critical blockers, accumulated deferred items to review.

Milestone goal: close the four code-level defects dogfood run #2b surfaced on the v1.0.5 import/adoption path (D1 cost rollup, D2 lifecycle advance, D3 dispatch concurrency cap, D4 planner failure semantics). All fixes are narrow seam edits on existing controller code — no new CRDs, no new go.mod dependencies, no new persistence surface.

## Scope

| Phase | Name | Plans | VERIFICATION | Requirements |
|-------|------|-------|--------------|--------------|
| 31 | D2+D1 — Adoption Lifecycle Seam | 3/3 | passed (5/5) | ADOPT-01..05 |
| 32 | D3 — Dispatch Concurrency Cap | 2/2 | passed (5/5; re-verified from gaps_found 4/5) | CONCUR-01..04 |
| 33 | D4 — Planner Failure Semantics | 3/3 | passed, APPROVED (4/4) | PLANFAIL-01..04 |

## Requirements Coverage (3-source cross-reference)

13/13 satisfied. Sources: REQUIREMENTS.md traceability (all `[x]`/Complete) + phase VERIFICATION.md bodies (each REQ verified) + SUMMARY `requirements_completed` frontmatter.

| REQ | Phase | Traceability | VERIFICATION | SUMMARY frontmatter | Final |
|-----|-------|--------------|--------------|---------------------|-------|
| ADOPT-01..05 | 31 | Complete | passed (each cited) | listed (union 01–05) | satisfied |
| CONCUR-01..04 | 32 | Complete | passed (each cited) | **empty** (hygiene miss) → manually verified via VERIFICATION body | satisfied |
| PLANFAIL-01..04 | 33 | Complete | passed (each cited) | listed (33-03) | satisfied |

No unsatisfied requirements, no orphans → FAIL gate not tripped.

## Cross-Phase Integration

gsd-integration-checker verdict: **SOUND** — 0 blockers. The four seam fixes coexist correctly in the shared planner-dispatch / handleJobCompletion paths (independently re-verified by the orchestrator against source; full internal/controller envtest suite — 167 specs — ran green together on current main, `make lint` 0 issues):

1. D3 cap gate is ordered AFTER the D1/D2 suppression+adoption short-circuits at all four dispatch sites → adopted projects never consume a cap slot; no slot leak on any exit path.
2. Phase-32 RetryOnConflict hardening correctly wraps the Phase-31 child-level *RolledUpUID rollup blocks (no double-count regression).
3. Phase-33 `isPlannerFailure` fires AFTER budget rollup and BEFORE the gate-policy hook → a failed planner's cost still rolls up, the level is marked Failed (not parked at AwaitingApproval), and BoundaryDetected blocks the parent from falsely succeeding.
4. No condition-vocabulary collisions (ProjectPlannerSuppressed / BudgetBlocked / Failed+ReasonPlannerFailed are distinct and correctly scoped).

2 WARNINGS (both verified against source, both tech_debt — see frontmatter `tech_debt`): W1 project-level marker-stamp asymmetry (pre-existing Phase 27), W2 configmap `default 16` cosmetic fallback.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Status | Suggested action |
|-------|---------------|-------------------|--------|------------------|
| 31 | missing | — | MISSING | `/gsd:validate-phase 31` (coverage exists; flag unset) |
| 32 | present | false | PARTIAL | `/gsd:validate-phase 32` |
| 33 | present | false | PARTIAL | `/gsd:validate-phase 33` |

Discovery only. Actual test coverage exists and passes for every requirement (the VALIDATION.md `nyquist_compliant` flag was simply never flipped by a validate-phase run); these are process-flag gaps, not coverage gaps.

## Verdict

No blockers; 13/13 requirements satisfied; integration sound. Status `tech_debt` reflects the two verified non-blocking warnings plus the Nyquist process flags and the CONCUR SUMMARY-frontmatter hygiene miss. None blocks shipping v1.0.6; all are review/cleanup items.

**Recommendation:** Acceptable to proceed to release + milestone close-out, carrying the tech-debt items forward (W1 is the highest-value follow-up: retrofit the project-level PlannerRolledUpUID stamp to the hardened RetryOnConflict pattern). W2 is a 1-character chart fix (`default 16` → `default 4`) that could fold into the release's chart-version bump.
