---
phase: 51-the-task-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha3/task_types.go
  - api/v1alpha3/shared_types.go
  - api/v1alpha3/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_tasks.yaml
  - internal/controller/verification_immutability_test.go
autonomous: true
requirements: [TASK-01, TASK-05, ESC-03]
must_haves:
  truths:
    - "A Locked spec.verification cannot be mutated in place; a Draft one can"
    - "A Locked verification can be transitioned to Superseded to mint a new version"
    - "TaskStatus carries a LoopStatus (current-iteration summary + exit reason only) and a lockedSHA observation"
    - "The VerifyHalt condition/reason/annotation vocabulary exists alongside FailureHalt"
  artifacts:
    - path: "api/v1alpha3/task_types.go"
      provides: "VerificationSpec type + TaskSpec.Verification embed + TaskStatus LoopStatus/LockedSHA"
      contains: "VerificationSpec"
    - path: "api/v1alpha3/shared_types.go"
      provides: "ConditionVerifyHalt / ReasonVerifyExhausted / AnnotationVerifyResumedAt consts"
      contains: "ConditionVerifyHalt"
    - path: "config/crd/bases/tideproject.k8s_tasks.yaml"
      provides: "Generated CRD carrying the x-kubernetes-validations transition rule"
      contains: "x-kubernetes-validations"
  key_links:
    - from: "api/v1alpha3/task_types.go (VerificationSpec XValidation marker)"
      to: "config/crd/bases/tideproject.k8s_tasks.yaml"
      via: "controller-gen (make manifests)"
      pattern: "x-kubernetes-validations"
---

<objective>
Land the CRD schema foundation for the Task loop: a planner-authored, immutable-once-locked `VerificationSpec` on `TaskSpec`, the governing lifecycle enum on the SPEC (so a CEL transition rule can read `oldSelf`), the observed `lockedSHA` + `LoopStatus` on `TaskStatus`, and the `VerifyHalt` condition vocabulary. Every downstream plan in this phase consumes these types.

Purpose: TASK-01 requires the verification contract to be immutable once locked so `git show <lockedSHA>` reproduces exactly what was dispatched; TASK-05 requires a resumable `LoopStatus`; ESC-03 requires a distinct VerifyHalt vocabulary. This plan is the type + admission surface; no reconciler logic yet.
Output: `VerificationSpec` + CEL immutability rule + `LoopStatus`/`LockedSHA` on `TaskStatus` + VerifyHalt consts + regenerated CRD/deepcopy + an envtest proving the CEL rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-the-task-loop/51-CONTEXT.md
@.planning/phases/51-the-task-loop/51-RESEARCH.md
@.planning/phases/51-the-task-loop/51-PATTERNS.md

<interfaces>
<!-- Extracted from HEAD this session — executor uses these directly, no exploration needed. -->
From api/v1alpha3/task_types.go: `type TaskSpec struct` (:72), existing `Gates Gates` embed (:122) is the standalone-type precedent; existing `XValidation` markers at :88 and :203 show same-state rule syntax; `type TaskStatus struct` (:147) already carries `Attempt int` (:157) and `Conditions`.
From api/v1alpha3/loop_types.go: `type LoopStatus struct` (:92) — embed this on TaskStatus (Iteration/LastEvaluation/ExitReason/CostCents/Conditions); `TestLoopStatus_NoForbiddenFields` (Phase 49) guards LOOP-03.
From api/v1alpha3/shared_types.go: the `ConditionFailureHalt` block (:320-335) — `ConditionFailureHalt = "FailureHalt"`, `ReasonTaskFailedHalt = "TaskFailedHalt"`, `AnnotationFailureResumedAt = "tideproject.k8s/failure-resumed-at"` — clone verbatim swapping Failure→Verify.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VerificationSpec type + CEL immutability + TaskSpec/TaskStatus embeds</name>
  <files>api/v1alpha3/task_types.go</files>
  <read_first>
    - api/v1alpha3/task_types.go (the file being modified: TaskSpec :72, Gates embed :122, XValidation markers :88/:203, TaskStatus :147, Attempt :157)
    - api/v1alpha3/loop_types.go (LoopStatus :92 shape to embed; ExitReason vocabulary :202)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"Pattern 2: CEL conditional immutability" + §"Pitfall 2" + §"Open Questions #1" (the spec-vs-status placement resolution)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"api/v1alpha3/task_types.go" (Analogs A/B/C/D)
  </read_first>
  <behavior>
    - CREATE with phase=Draft succeeds unconstrained (transition rules skip on CREATE — no oldSelf).
    - UPDATE mutating any field of a Locked verification is REJECTED by admission.
    - UPDATE transitioning a Locked verification to phase=Superseded is ALLOWED.
    - UPDATE mutating a Draft verification is ALLOWED.
    - onExhaustion accepts only `escalate` or `requireApproval`; phase accepts only `Draft`/`Locked`/`Superseded` (enum-validated).
  </behavior>
  <action>
    Define a standalone `VerificationSpec` struct (NOT inline fields — mirror the `Gates`/`Caps` standalone-type precedent so D-01's shape generalizes to Plan/Project in Phase 52). Fields per D-01: `Phase string` (kubebuilder Enum=Draft;Locked;Superseded), `Version int32` (Minimum=1), `GateCommand string`, `Commands []string`, `RequiredArtifacts []string`, `Evaluator string`, `MaxIterations int32` (Minimum=0), `OnExhaustion string` (Enum=escalate;requireApproval). Per OQ1/Pitfall 2 the governing `Phase`/`Version` MUST live on `spec.verification` so the CEL transition rule can read `oldSelf` (a spec transition rule cannot reference status). Add the `+kubebuilder:validation:XValidation` transition-immutability marker from RESEARCH Pattern 2 on the `VerificationSpec` type (rule referencing `oldSelf.phase != 'Locked' || self == oldSelf || self.phase == 'Superseded'`, with a clear message). Embed `Verification VerificationSpec` on `TaskSpec` as `+optional` with a doc-comment noting Task-scoped-now / Plan+Project-in-Phase-52 and the Task>Plan>Project precedent (mirror the `Gates` precedence doc). On `TaskStatus`: embed `LoopStatus LoopStatus` (`+optional`, D-07 current-iteration-only) and add `LockedSHA string` (`+optional`, the runtime observation recorded at dispatch per D-03 — NOT the governing enum). Doc-comment `GateCommand` and `Commands` as the planner-authored pass-criteria EXECUTED FOR REAL downstream (NOT decorative/status): `GateCommand` is the single canonical primary pass-criterion (D-01 → resolves onto the existing `VerifyContext.GateCommand` wire field), `Commands` is the additional acceptance-signal list; Plan 06 resolves them into the transported `VerifyContext.Commands` list and the Plan 02 verifier executes EACH out-of-band (exit codes parsed, any non-zero dominates any LLM APPROVED — TASK-04). Doc-comment `OnExhaustion` that in Phase 51 BOTH values (`escalate`, `requireApproval`) resolve IDENTICALLY to `ConditionVerifyHalt` + await `tide resume` — per-value differentiation (`LoopPolicy.EscalationPolicy`) is Phase 52 scope, so the field is declared-but-uniform-now, never unenforced. Keep JSON tags lowerCamel.
  </action>
  <acceptance_criteria>
    - `grep -c 'VerificationSpec' api/v1alpha3/task_types.go` ≥ 3 (type def + TaskSpec embed + status/doc refs).
    - `grep -c 'XValidation' api/v1alpha3/task_types.go` increases by ≥ 1 vs HEAD (the transition rule).
    - `grep -E 'oldSelf.phase' api/v1alpha3/task_types.go` returns the transition rule line.
    - `grep -E 'LockedSHA|LoopStatus LoopStatus' api/v1alpha3/task_types.go` returns both status additions.
    - `go build ./api/...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go build ./api/... && grep -qE 'oldSelf.phase' api/v1alpha3/task_types.go</automated>
  </verify>
  <done>VerificationSpec exists with the CEL transition-immutability marker; TaskSpec embeds it; TaskStatus carries LoopStatus + LockedSHA; package builds.</done>
</task>

<task type="auto">
  <name>Task 2: VerifyHalt condition/reason/annotation vocabulary</name>
  <files>api/v1alpha3/shared_types.go</files>
  <read_first>
    - api/v1alpha3/shared_types.go (the FailureHalt block :320-335 to clone; BillingHalt block :264-282 for the three-generation doc-comment shape)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"api/v1alpha3/shared_types.go"
  </read_first>
  <action>
    Add a new const block mirroring the `ConditionFailureHalt` block verbatim, swapping Failure→Verify: `ConditionVerifyHalt = "VerifyHalt"`, `ReasonVerifyExhausted = "VerifyExhausted"`, `AnnotationVerifyResumedAt = "tideproject.k8s/verify-resumed-at"`. Keep the doc-comment discipline (what SETS it = loop exhaustion with a non-APPROVED verdict; what READS it = both dispatch chains via checkVerifyHalt; what CLEARS it = `tide resume` stamping the annotation). This is the distinct-halt-class vocabulary (ESC-03) — the doc-comment must state it is NOT a reinterpretation of Failed wave semantics.
  </action>
  <acceptance_criteria>
    - `grep -cE 'ConditionVerifyHalt|ReasonVerifyExhausted|AnnotationVerifyResumedAt' api/v1alpha3/shared_types.go` == 3.
    - `grep -E 'verify-resumed-at' api/v1alpha3/shared_types.go` returns the annotation.
    - `go build ./api/...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go build ./api/... && grep -qE 'ConditionVerifyHalt = "VerifyHalt"' api/v1alpha3/shared_types.go</automated>
  </verify>
  <done>The three VerifyHalt consts exist with distinct-halt-class doc-comments; package builds.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate CRD/deepcopy + CEL immutability envtest</name>
  <files>api/v1alpha3/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_tasks.yaml, internal/controller/verification_immutability_test.go</files>
  <read_first>
    - .planning/phases/51-the-task-loop/51-VALIDATION.md §"Wave 0 Requirements" (the immutability test scaffold)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"Pitfall 2" (CEL runs only against a real API server — envtest, not fake-client)
    - internal/controller (existing envtest suite setup pattern for a real API server)
  </read_first>
  <behavior>
    - envtest CREATE Task with verification.phase=Draft → succeeds.
    - envtest UPDATE that field-mutates a Locked verification → admission error (immutable message).
    - envtest UPDATE transitioning Locked→Superseded → succeeds.
    - envtest UPDATE mutating a Draft verification → succeeds.
  </behavior>
  <action>
    Run `make generate` (deepcopy for VerificationSpec + LoopStatus embed) and `make manifests` (regenerates config/crd/bases/tideproject.k8s_tasks.yaml with the x-kubernetes-validations rule). Create `internal/controller/verification_immutability_test.go` as an envtest (Ginkgo or plain go-test against the suite's real API server — CEL admission does NOT run under the fake client) covering the four behavior cases above. Name the test so it matches `-run VerificationImmutab`. Do NOT hand-edit the generated CRD YAML — the rule must originate from the marker (proves the marker→CRD link).
  </action>
  <acceptance_criteria>
    - `grep -c 'x-kubernetes-validations' config/crd/bases/tideproject.k8s_tasks.yaml` ≥ 1 AND the rule text contains `oldSelf`.
    - `make manifests generate` leaves a clean tree for these files on a second run (idempotent; zero-diff on re-run).
    - `go test ./internal/controller/... -run VerificationImmutab` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/controller/... -run VerificationImmutab -count=1</automated>
  </verify>
  <done>CRD carries the transition rule from the marker; deepcopy regenerated; the envtest proves Locked-immutable / Draft-mutable / Superseded-escape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → K8s API (Task spec authoring) | The planner writes `spec.verification`; a later actor must not silently mutate a Locked contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | `spec.verification` post-lock mutation | mitigate | CEL `x-kubernetes-validations` transition rule rejects any field mutation of a Locked verification; Superseded is the only escape (proven by the envtest) |
| T-51-01b | Tampering | `onExhaustion` / `phase` invalid values | mitigate | kubebuilder Enum constraints on `Phase` and `OnExhaustion` reject out-of-vocabulary values at admission |
| T-51-01c | Repudiation | which contract was dispatched | mitigate | `LockedSHA` on status records the locking commit so `git show` reproduces the dispatched contract (consumed by Plan 06) |
| T-51-SC | Tampering | package installs | N/A | No npm/pip/cargo installs in this plan — schema/regen only |
</threat_model>

<verification>
- `go build ./api/...` and `go vet ./api/...` clean.
- `make manifests generate` idempotent (zero-diff on re-run).
- `go test ./internal/controller/... -run VerificationImmutab -count=1` green.
- `TestLoopStatus_NoForbiddenFields` (Phase 49) still green after embedding LoopStatus on TaskStatus: `go test ./api/... -run LoopStatus -count=1`.
</verification>

<success_criteria>
- VerificationSpec is a standalone, CEL-immutable-once-Locked type embedded on TaskSpec.
- The governing phase/version live on spec; only lockedSHA lives on status (OQ1 resolved).
- LoopStatus embeds on TaskStatus with no accumulating history (LOOP-03 guard still green).
- VerifyHalt vocabulary exists as a distinct halt class.
- The generated CRD carries the transition rule and the envtest proves it.
</success_criteria>

<output>
Create `.planning/phases/51-the-task-loop/51-01-SUMMARY.md` when done.
</output>
