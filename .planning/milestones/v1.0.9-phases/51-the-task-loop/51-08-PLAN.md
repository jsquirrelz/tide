---
phase: 51-the-task-loop
plan: 08
type: execute
wave: 5
depends_on: [51-06, 51-07]
files_modified:
  - test/integration/kind/verifier_concurrency_test.go
autonomous: false
requirements: [ESC-04]
must_haves:
  truths:
    - "A kind concurrent-dispatch test proves verifier dispatch stays under the sized verifierInFlightCount cap"
    - "The full Task loop runs live on a kind cluster: a red gate command → REPAIRABLE → fresh attempt → ConditionVerifyHalt at maxIterations"
    - "The verifier runs as an independent read-only process and its cost is bounded by LoopPolicy.BudgetCents"
  artifacts:
    - path: "test/integration/kind/verifier_concurrency_test.go"
      provides: "Layer B Ginkgo spec asserting concurrent verifier dispatch stays under cap"
      contains: "verifierInFlightCount"
  key_links:
    - from: "test/integration/kind/verifier_concurrency_test.go"
      to: "the sized verifier cap"
      via: "concurrent-dispatch assertion under cap"
      pattern: "cap"
---

<objective>
Prove the concurrency/cost safety live: a kind-cluster concurrent-dispatch Ginkgo spec that holds verifier dispatch under the sized `verifierInFlightCount` cap (the run-2b single-node-OOM guard), plus a human-verified live run of the full Task loop against a real gate command (a Task whose locked gate fails is auto-repaired then halts on `ConditionVerifyHalt`).

Purpose: ESC-04 requires the concurrency cap + `LoopPolicy.BudgetCents` proven by a kind concurrent-dispatch test under the cap; Phase 51 Success Criterion 5 requires the live kind proof. This is the phase's terminal validation gate.
Output: verifier_concurrency_test.go (automated) + a recorded live-run verification (human checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-the-task-loop/51-CONTEXT.md
@.planning/phases/51-the-task-loop/51-RESEARCH.md
@.planning/phases/51-the-task-loop/51-VALIDATION.md
@.planning/phases/51-the-task-loop/51-06-SUMMARY.md
@.planning/phases/51-the-task-loop/51-07-SUMMARY.md

<interfaces>
From test/integration/kind/: `configmap_planner_concurrency_test.go` (planner cap-under-load shape; planner default cap 4) + `chaos_resume_test.go` (real-Job lifecycle spec shape) — the analogs for verifier_concurrency_test.go.
From Plan 06: `verifierInFlightCount` (role=verifier label, self-excluding) + the sized cap default (single-node-safe, e.g. 2 per OQ4 — pin the actual value the test asserts).
Live-run constraints (CLAUDE.md + MEMORY): single-node kind OOM discipline (delete→recreate→prewarm per heavy run; never run acceptance's own cluster concurrently); real Anthropic key at ~/.tide/anthropic.key; the live billable verify is a human checkpoint.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kind concurrent-dispatch spec — verifier dispatch stays under the sized cap</name>
  <files>test/integration/kind/verifier_concurrency_test.go</files>
  <read_first>
    - test/integration/kind/configmap_planner_concurrency_test.go (the cap-under-load analog)
    - test/integration/kind/chaos_resume_test.go (real-Job lifecycle spec shape)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"Open Questions #4" (cap default) + §"Pitfall 6" (no slot leak)
    - .planning/phases/51-the-task-loop/51-06-SUMMARY.md (the actual sized cap value + verifierInFlightCount signature)
  </read_first>
  <behavior>
    - Under N > cap contract-bearing Tasks reaching executor-complete simultaneously, the number of concurrently-Running verifier Jobs never exceeds the sized cap.
    - Excess verifier dispatches requeue (defer) rather than leaking slots; the count drains as verifiers complete.
    - Reservations settle (no orphaned reservation) after verifier completion.
  </behavior>
  <action>
    Create `test/integration/kind/verifier_concurrency_test.go` as a Ginkgo Layer B spec mirroring configmap_planner_concurrency_test.go + chaos_resume_test.go: apply enough contract-bearing Tasks to exceed the sized verifier cap, drive them to executor-complete, and assert the concurrent role=verifier Job count stays ≤ cap at all times (poll the JobList) and drains to zero. Assert no reservation is orphaned after completion. Pin the exact cap value from Plan 06's summary. Follow the single-node OOM discipline in comments (this spec must run within one kind node's memory alongside the executor cap).
  </action>
  <acceptance_criteria>
    - `grep -E 'verifierInFlightCount|role.*verifier|"verifier"' test/integration/kind/verifier_concurrency_test.go` returns the label/count assertion.
    - `grep -iE 'cap' test/integration/kind/verifier_concurrency_test.go` returns the under-cap assertion.
    - `make test-int` runs the spec; read the echoed MAKE_EXIT AND grep the log `^--- FAIL|^FAIL\s` (Ginkgo SUCCESS alone is insufficient — plain go-tests bundled in the kind package can fail the package). The spec passes and MAKE_EXIT is 0 for the touched package.
  </acceptance_criteria>
  <verify>
    <automated>make test-int 2>&1 | tee /tmp/51-08-kind.log; echo "MAKE_EXIT=${PIPESTATUS[0]}"; ! grep -nE '^--- FAIL|^FAIL\s' /tmp/51-08-kind.log</automated>
  </verify>
  <done>The kind concurrent-dispatch spec proves verifier dispatch stays under the sized cap with no slot/reservation leak; MAKE_EXIT verified 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Task-loop proof on kind (real gate command, billable)</name>
  <read_first>
    - CLAUDE.md §"Operating Notes" (single-node kind OOM discipline: delete→recreate→prewarm; never run acceptance's own cluster concurrently)
    - .planning/phases/51-the-task-loop/51-06-SUMMARY.md + 51-07-SUMMARY.md (the loop transitions + verifier-image Deps default)
    - .planning/phases/51-the-task-loop/51-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <action>
    This is a human-verification checkpoint that runs AFTER all autonomous work in this phase — no production-code edits here. The executor prepares the cluster (delete→recreate→prewarm a kind cluster; build + kind-load the tide-langgraph-verifier + manager images at dev-head; confirm ~/.tide/anthropic.key), applies the failing-gate Project/Task fixture, drives the loop, captures the kubectl/manager-log proof, then pauses for the operator to confirm the observed transitions per how-to-verify.
  </action>
  <what-built>
    The full Task loop: a contract-bearing Task whose LOCKED gate command fails is checked by the independent read-only langgraph verifier, classified REPAIRABLE, re-attempted with a bounded evidence packet, and (if still failing) halted on ConditionVerifyHalt at maxIterations — with the verifier counted against the concurrency cap and its cost bounded by LoopPolicy.BudgetCents.
  </what-built>
  <how-to-verify>
    1. Follow the single-node kind OOM discipline (CLAUDE.md): on kind-tide-dogfood or a throwaway cluster, delete→recreate→prewarm; do NOT run the acceptance cluster concurrently. Ensure the real key is present (~/.tide/anthropic.key) and the tide-langgraph-verifier + manager images are built/kind-loaded at dev-head.
    2. Apply a Project/Task with a `spec.verification` contract whose `gateCommand` deterministically FAILS on the first attempt (e.g. a command asserting a file the executor must create) and `maxIterations: 1` (or 2), `onExhaustion: requireApproval`.
    3. Observe: the executor runs and believes complete; a verifier Job (role=verifier) dispatches; `kubectl get task -o yaml` shows the verdict → REPAIRABLE → a FRESH attempt (Attempt incremented) → at maxIterations without APPROVED, `kubectl get project -o yaml` shows `ConditionVerifyHalt` True and dispatch parks.
    4. Confirm the verifier ran as an independent read-only Job (RO worktree, no git-write creds) and the reservation settled (no orphaned reservation / no BudgetCents overrun).
    5. Confirm a green-gate path: fix the Task so the gate passes → verifier returns APPROVED → Task Succeeded.
    6. Record the observed transitions + a proof artifact (kubectl yaml / manager log excerpt) in the SUMMARY; note approximate spend.
  </how-to-verify>
  <acceptance_criteria>
    - The operator confirms the observed sequence: executor belief-complete → verifier Job (role=verifier) dispatched → REPAIRABLE → fresh attempt (Attempt++) → ConditionVerifyHalt True at maxIterations, then a green-gate rerun → APPROVED → Succeeded.
    - A proof artifact (kubectl yaml / manager-log excerpt) + approximate spend recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms the REPAIRABLE→fresh-attempt→ConditionVerifyHalt transitions and the green-gate→APPROVED path on a live kind run, with a proof artifact recorded.</human-check>
  </verify>
  <done>The full Task loop is proven live on kind and the operator has signed off with a recorded proof artifact + approximate spend.</done>
  <resume-signal>Type "approved" with the observed loop transitions, or describe what diverged.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent verifier dispatch → node memory | Unbounded verifier fan-out OOMs a single node (run-2b D3) |
| live verifier → real credproxy/gate | The live run exercises the real read-only + gate-command trust boundary end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-08 | DoS | concurrent verifier dispatch OOMing a single node | mitigate | Kind concurrent-dispatch spec proves dispatch stays under the sized cap; BudgetCents reservation caps cost — the run-2b OOM guard, proven live |
| T-51-08b | Elevation | live verifier gaining write/push | mitigate | Live check confirms the RO worktree + cred omission (Phase 48) hold end-to-end |
| T-51-SC | Tampering | package installs | N/A | No package installs — test + live validation only |
</threat_model>

<verification>
- `make test-int` green for the verifier_concurrency spec with MAKE_EXIT=0 (grep `^--- FAIL|^FAIL\s`).
- Live-run human checkpoint APPROVED with observed REPAIRABLE→fresh-attempt→ConditionVerifyHalt transitions recorded.
</verification>

<success_criteria>
- Concurrent verifier dispatch provably stays under the sized cap (ESC-04) with no slot/reservation leak.
- The full Task loop is proven live on kind: red gate → auto-repair → ConditionVerifyHalt, independent read-only verifier, cost-bounded (Phase 51 Success Criterion 5).
</success_criteria>

<output>
Create `.planning/phases/51-the-task-loop/51-08-SUMMARY.md` when done (include the live-run proof artifact + observed transitions + approximate spend).
</output>
