---
phase: 51-the-task-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/provider.go
  - pkg/dispatch/vendor_capabilities.go
  - pkg/dispatch/vendor_capabilities_test.go
  - cmd/tide-langgraph-verifier/verifier/__main__.py
  - cmd/tide-langgraph-verifier/verifier/envelope.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py
autonomous: true
requirements: [TASK-04, OBS-03]
must_haves:
  truths:
    - "SelfInstruments(\"langgraph\") returns true; every other vendor returns false"
    - "The verifier image refuses to run when Provider.Vendor != \"langgraph\""
    - "The verifier entrypoint executes EACH resolved pass-criterion command from env.verify.commands out-of-band and parses each exit code (never self-reported)"
    - "TIDE_GATE_COMMAND is set from env.verify.gateCommand (the canonical primary) so the LLM's run_gate_command tool still works"
    - "A non-zero exit on ANY of the commands forces the verdict to REPAIRABLE/BLOCKED regardless of any LLM APPROVED"
    - "The assembled verdict is written into EnvelopeOut.Verdict with a blocker/gate-command finding carrying exit_code=N for the failing command"
  artifacts:
    - path: "pkg/dispatch/vendor_capabilities.go"
      provides: "SelfInstruments langgraph case"
      contains: "case \"langgraph\""
    - path: "cmd/tide-langgraph-verifier/verifier/__main__.py"
      provides: "vendor sentinel flip + TIDE_GATE_COMMAND injection + multi-command out-of-band deterministic verdict assembly"
      contains: "SUPPORTED_VENDOR"
  key_links:
    - from: "cmd/tide-langgraph-verifier/verifier/__main__.py (deterministic gate capture)"
      to: "EnvelopeOut.Verdict"
      via: "verdict assembly forcing non-APPROVED on non-zero exit"
      pattern: "exit_code"
---

<objective>
Register the `"langgraph"` vendor sentinel (Go predicate) and make the Phase-48 verifier image actually PRODUCE a dominant verdict: flip its vendor refusal, execute EACH planner-authored pass-criterion command (`env.verify.commands`, resolved in Plan 06 from the locked `gateCommand` + `commands`) out-of-band, parse each exit code, and assemble a `GateDecision` where a red gate on ANY command ALWAYS dominates an LLM APPROVED (the milestone's raison d'être). `TIDE_GATE_COMMAND` is still set from the canonical `gateCommand` so the LLM's `run_gate_command` tool keeps working.

Purpose: D-02 requires `SelfInstruments("langgraph")=true` (reporter skips events.jsonl synthesis) and the image to refuse other vendors at startup. D-06/TASK-04 require the deterministic gate exit(s) to dominate any probabilistic judge — enforced structurally in the verifier (this plan) and re-checked controller-side (Plan 07). This closes the BLOCKER that `commands` was dead schema: the authored `commands` are now executed for real here, each exit code parsed. This is the verifier-side half of the anti-silent-Complete fix.
Output: Go vendor sentinel + verifier-image verdict assembly with multi-command out-of-band deterministic dominance + pytest dominance case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-the-task-loop/51-CONTEXT.md
@.planning/phases/51-the-task-loop/51-RESEARCH.md
@.planning/phases/51-the-task-loop/51-PATTERNS.md

<interfaces>
From pkg/dispatch/vendor_capabilities.go: `func SelfInstruments(vendor string) bool` (:38) — every case returns false today; pure vendor predicate, NO signature change (preserves the Phase-45 ADAPT-01 seam).
From pkg/dispatch/provider.go: `ProviderSpec.Vendor` doc-comment canonical set (:36-40) — "anthropic","openai","google","xai","opencode".
From pkg/dispatch/verdict.go: `VerdictApproved`/`VerdictRepairable`/`VerdictBlocked`, `type GateDecision`, `type Finding`, `highSeverityFindingToken="blocker"` — the verdict shape the Python assembly mirrors (verdict.py, Phase 49).
From cmd/tide-langgraph-verifier/verifier/: `__main__.py` `SUPPORTED_VENDOR = "anthropic"` (:30) with forward-note `# The "langgraph" sentinel question is deferred to Phase 51`; injectable seams `build_model`/`run_agent_fn` (:74-75) for offline tests; `tools.py` `run_gate_command` reads ONLY `TIDE_GATE_COMMAND` (:56), fail-closed if empty, `del command` discards model arg (:157 `# noqa: S602` justified by orchestrator-set invariant), and its `_worktree_dir`/timeout discipline is the shared out-of-band-execution pattern to factor from; `verdict.py` `GateDecision`/`classify_verdict` (Phase 49, reuse).
From cmd/tide-langgraph-verifier/verifier/envelope.py: `EnvelopeIn.verify` is an untyped raw-dict passthrough (accept-and-ignore, :64-73) whose docstring forward-notes "Phase 51 consumes the concrete fields" — the resolved pass-criteria travel in it as `verify["gateCommand"]` (canonical) + `verify["commands"]` (the executed list); this plan is that consumer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the "langgraph" vendor sentinel (Go)</name>
  <files>pkg/dispatch/provider.go, pkg/dispatch/vendor_capabilities.go, pkg/dispatch/vendor_capabilities_test.go</files>
  <read_first>
    - pkg/dispatch/vendor_capabilities.go (SelfInstruments :38 — the file being modified)
    - pkg/dispatch/provider.go (Vendor doc-comment canonical set :36-40)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"pkg/dispatch/vendor_capabilities.go (MOD) + provider.go"
  </read_first>
  <behavior>
    - SelfInstruments("langgraph") → true.
    - SelfInstruments("anthropic"/"openai"/"google"/"xai"/"opencode") → false.
    - SelfInstruments("") and any unknown → false (fail-closed default).
  </behavior>
  <action>
    Add `case "langgraph": return true` to `SelfInstruments` with a doc-comment (self-instruments via openinference-instrumentation-langchain; reporter skips events.jsonl synthesis). Keep the signature unchanged. Add `"langgraph"` to the `ProviderSpec.Vendor` canonical-set doc-comment in provider.go (the doc-comment explicitly governs the sentinel set). Add/extend `vendor_capabilities_test.go` with a table covering true for langgraph and false for the five existing vendors + empty/unknown.
  </action>
  <acceptance_criteria>
    - `grep -E 'case "langgraph"' pkg/dispatch/vendor_capabilities.go` returns the true case.
    - `grep -E 'langgraph' pkg/dispatch/provider.go` returns the doc-comment addition.
    - `go test ./pkg/dispatch/... -run SelfInstruments -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/dispatch/... -run SelfInstruments -count=1</automated>
  </verify>
  <done>SelfInstruments("langgraph") is true, all others false, doc-comment updated, test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verifier entrypoint — vendor flip, multi-command out-of-band execution, deterministic verdict assembly (Python mirror)</name>
  <files>cmd/tide-langgraph-verifier/verifier/__main__.py, cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py</files>
  <read_first>
    - cmd/tide-langgraph-verifier/verifier/__main__.py (SUPPORTED_VENDOR :30, injectable seams :74-75 — the file being modified)
    - cmd/tide-langgraph-verifier/verifier/envelope.py (EnvelopeIn.verify raw-dict passthrough :64-73, read_envelope_in :120-137 — carries gateCommand + commands)
    - cmd/tide-langgraph-verifier/verifier/tools.py (run_gate_command :126, TIDE_GATE_COMMAND :56, fail-closed-if-empty, _worktree_dir :59 + timeout discipline)
    - cmd/tide-langgraph-verifier/verifier/verdict.py (GateDecision/Finding/classify_verdict — reuse, do not re-port)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"Pitfall 1" + §"Open Questions #3" (out-of-band capture recommendation) + §"Pattern 4"
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"cmd/tide-langgraph-verifier/verifier/__main__.py" (Analogs A/B/C/D)
  </read_first>
  <behavior>
    - Entrypoint with Provider.Vendor != "langgraph" → refuses at startup (fail-fast, mirrors the anthropic subagent vendor refusal).
    - The resolved pass-criteria list = env.verify.commands (Plan 06 populates it as the ordered union [gateCommand] ++ commands). The entrypoint executes EACH command out-of-band (subprocess pinned to the worktree, per-command timeout) and captures each returncode independently of the LLM's tool narration.
    - TIDE_GATE_COMMAND is set from env.verify.gateCommand (the canonical primary) before the agent runs, so the LLM's run_gate_command tool still functions (advisory narration only).
    - ANY command exit non-zero: assembled EnvelopeOut.Verdict is REPAIRABLE or BLOCKED even if the injected/fake LLM returns APPROVED — proven with the injectable build_model/run_agent_fn seam returning a fake APPROVED verdict.
    - All commands exit zero + LLM APPROVED: verdict may be APPROVED.
    - The non-zero case emits a Finding with dimension="gate-command", severity="blocker", evidence containing exit_code=N for the failing command.
    - Empty/absent env.verify.commands (no authored pass-criteria) stays fail-closed — no gate ran → verdict is not APPROVED (mirrors tools.py's fail-closed-if-empty discipline).
  </behavior>
  <action>
Flip `SUPPORTED_VENDOR = "anthropic"` → `"langgraph"` (D-02); the vendor-refusal branch now rejects any other vendor. Update the envelope.py `EnvelopeIn.verify` docstring (:57-62) to state Phase 51 IS the consumer that reads the concrete `gateCommand` + `commands` fields from the raw dict (the field stays an untyped passthrough — the Go↔Python duality is honored because the wire shape is re-implemented from the frozen struct tags, D-03). In `__main__.py`, extract `env.verify` and resolve the pass-criteria list from `env.verify["commands"]` (Plan 06 populates it as the ordered union `[gateCommand] ++ commands`). Set `os.environ["TIDE_GATE_COMMAND"]` from `env.verify["gateCommand"]` (the canonical primary) before constructing/running the agent, so the LLM's `run_gate_command` tool still functions. Implement the multi-command out-of-band deterministic capture (OQ3 recommendation): iterate the resolved list, run EACH command deterministically in the entrypoint (subprocess pinned to the worktree via a helper factored from tools.py's `_worktree_dir`/timeout discipline rather than duplicated), capture each returncode independently of the LLM's tool narration, and in verdict assembly FORCE `Verdict ∈ {REPAIRABLE, BLOCKED}` on ANY non-zero regardless of the LLM's verdict — emit a `blocker`/`gate-command` `Finding` carrying `exit_code=N` for the failing command. An empty/absent list stays fail-closed (verdict is not APPROVED). Reuse `verdict.py` `GateDecision`/`classify_verdict` (no re-port). Write the assembled `GateDecision` into `EnvelopeOut.Verdict`. Add a pytest to `tests/test_verdict.py` driving the full flow via the `build_model`/`run_agent_fn` injectable seams with a fake chat model returning APPROVED, asserting (a) a red gate on ONE of several commands downgrades the verdict AND stamps the gate-command finding with that command's exit code, and (b) all-green commands + LLM APPROVED stays APPROVED.
  </action>
  <acceptance_criteria>
    - `grep -E 'SUPPORTED_VENDOR\s*=\s*"langgraph"' cmd/tide-langgraph-verifier/verifier/__main__.py` returns the flipped sentinel.
    - `grep -E 'TIDE_GATE_COMMAND' cmd/tide-langgraph-verifier/verifier/__main__.py` shows the entrypoint SETS it from the canonical gateCommand.
    - `grep -E 'commands' cmd/tide-langgraph-verifier/verifier/__main__.py` shows the entrypoint reads the resolved list from env.verify and iterates it.
    - `grep -Ei 'gate-command|exit_code' cmd/tide-langgraph-verifier/verifier/__main__.py` returns the per-command finding-assembly line(s).
    - `grep -Ei 'gateCommand|commands' cmd/tide-langgraph-verifier/verifier/envelope.py` returns the docstring note that Phase 51 consumes both from the verify dict.
    - `make test-langgraph-verifier` exits 0 (pytest suite incl. the multi-command dominance case green).
  </acceptance_criteria>
  <verify>
    <automated>make test-langgraph-verifier</automated>
  </verify>
  <done>The verifier image refuses non-langgraph vendors, executes each resolved pass-criterion command out-of-band (each exit code parsed), and structurally forces a red gate on ANY command to dominate an LLM APPROVED — proven by pytest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM judge → assembled verdict | The probabilistic judge's textual verdict must not override the deterministic machine result |
| orchestrator env → verifier gate command | `TIDE_GATE_COMMAND` is orchestrator-set from the locked contract, never a model-supplied tool argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02 | Tampering / Repudiation | LLM judge silently overriding a red gate (the 2026-07-03 silent-Complete) | mitigate | Out-of-band deterministic exit capture of EACH pass-criterion command forces non-APPROVED on any non-zero regardless of the LLM verdict; a blocker/gate-command finding carries the structural signal for Plan 07's controller-side re-check. Residual: low (defence-in-depth completed in Plan 07) |
| T-51-02b | Elevation / Tampering | command injection via gateCommand | mitigate | Tool reads ONLY the orchestrator-set `TIDE_GATE_COMMAND`; `del command` discards any model-supplied arg; gate runs in the RO verifier pod, not the manager |
| T-51-02c | Spoofing | wrong image accepting a foreign vendor | mitigate | Startup vendor refusal — image runs only for Vendor=="langgraph" |
| T-51-SC | Tampering | package installs | N/A | No new packages; the verifier image deps are already patch-exact pinned + CI-gated (Phase 48) |
</threat_model>

<verification>
- `go test ./pkg/dispatch/... -run SelfInstruments -count=1` green.
- `make test-langgraph-verifier` green (incl. deterministic-dominance case).
- `make lint` clean for the touched Go files.
</verification>

<success_criteria>
- SelfInstruments("langgraph")=true; image refuses other vendors.
- A red gate command deterministically dominates an LLM APPROVED in the assembled verdict (the milestone's core integrity control, verifier-side).
</success_criteria>

<output>
Create `.planning/phases/51-the-task-loop/51-02-SUMMARY.md` when done.
</output>
