---
phase: 51-the-task-loop
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/dispatch/podjob/caps.go
  - internal/dispatch/podjob/names.go
  - internal/dispatch/podjob/jobspec.go
  - internal/dispatch/podjob/jobspec_test.go
autonomous: true
requirements: [TASK-04, ESC-04]
must_haves:
  truths:
    - "A JobKindVerifier exists with a verify caps floor, grep-distinct from executor/planner"
    - "VerifierJobName(taskUID, attempt) produces a deterministic tide-verifier-<uid>-<attempt> name labelled role=verifier"
    - "The verifier jobspec injects TIDE_GATE_COMMAND from a BuildOptions field"
    - "The verifier jobspec keeps /workspace ReadOnly AND adds a read-write envelopes/ subPath mount so out.json can be written"
    - "The RO worktree mount + git-write/push credential omission (Phase 48 EVAL-01) are preserved"
  artifacts:
    - path: "internal/dispatch/podjob/caps.go"
      provides: "JobKindVerifier + verify caps floor"
      contains: "JobKindVerifier"
    - path: "internal/dispatch/podjob/names.go"
      provides: "VerifierJobName deterministic name helper"
      contains: "VerifierJobName"
    - path: "internal/dispatch/podjob/jobspec.go"
      provides: "TIDE_GATE_COMMAND env injection + RW envelopes/ subPath mount"
      contains: "TIDE_GATE_COMMAND"
  key_links:
    - from: "internal/dispatch/podjob/jobspec.go (BuildOptions.GateCommand)"
      to: "verifier pod env TIDE_GATE_COMMAND"
      via: "conditional subagentEnv append"
      pattern: "TIDE_GATE_COMMAND"
---

<objective>
Build the verifier Job spec surface so Plan 06 has a caller-ready path: a dedicated `JobKindVerifier` (grep-distinct verify caps + `role=verifier` concurrency label), a deterministic `VerifierJobName`, `TIDE_GATE_COMMAND` env injection from a new `BuildOptions` field, and the genuinely-new RW `envelopes/` subPath mount alongside the existing ReadOnly `/workspace` mount (so `out.json` can be written under the read-only verifier variant).

Purpose: TASK-04 requires the verifier to run as a logically independent, read-only process; ESC-04 requires a `role=verifier` label so `verifierInFlightCount` (Plan 06) can select it. The `ReadOnly` jobspec variant exists (Phase 48) but is dispatched nowhere and cannot write out.json — resolving that mount split is explicitly this phase's job (jobspec.go:199-204 forward-note).
Output: JobKindVerifier + VerifierJobName + TIDE_GATE_COMMAND injection + RW envelopes/ mount, with static jobspec tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-the-task-loop/51-CONTEXT.md
@.planning/phases/51-the-task-loop/51-RESEARCH.md
@.planning/phases/51-the-task-loop/51-PATTERNS.md

<interfaces>
From internal/dispatch/podjob/caps.go: `JobKindExecutor JobKind = "executor"` (:36, 1200s floor), `JobKindPlanner JobKind = "planner"` (:37, 1800s floor); `func DefaultCaps(caps *Caps, kind JobKind) *Caps` (:99) switches on kind (:104).
From internal/dispatch/podjob/names.go: `func JobName(taskUID types.UID, attempt int) string` (:37), `func PlannerJobName(level, parentUID string, attempt int) string` (:52) — the `tide-{kind}-{uid}-{attempt}` deterministic-name tuple; the name IS the dedup key + label-selector target.
From internal/dispatch/podjob/jobspec.go: `type BuildOptions struct` (:88); `PricingOverridesJSON`/`Traceparent` conditional `subagentEnv` append (:447) is the env-injection shape; `ReadOnly bool` (:205) already: RO /workspace mount + verifier-scratch /scratch emptyDir + TMPDIR/HOME→/scratch + ReadOnlyRootFilesystem (:424-441, :496, :525). FORWARD-NOTE :199-204: out.json cannot be written through the ReadOnly mount — a separate RW envelopes/ subPath mount is Phase 51's job.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JobKindVerifier + verify caps floor + VerifierJobName</name>
  <files>internal/dispatch/podjob/caps.go, internal/dispatch/podjob/names.go</files>
  <read_first>
    - internal/dispatch/podjob/caps.go (JobKind enum :33-38, DefaultCaps switch :99-122, floor-doc discipline :40-65)
    - internal/dispatch/podjob/names.go (JobName :37, PlannerJobName :52)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"internal/dispatch/podjob/caps.go + names.go + jobspec.go" + RESEARCH §"Alternatives Considered" (A1: dedicated kind over ReadOnly-flag reuse)
  </read_first>
  <behavior>
    - JobKindVerifier is a distinct JobKind value ("verifier").
    - DefaultCaps(nil, JobKindVerifier) returns caps with the verify floor (Claude's-discretion value, shorter than executor's 1200s), documented with the reason.
    - VerifierJobName(taskUID, attempt) returns a deterministic tide-verifier-<uid>-<attempt> string, distinct from JobName's executor form.
  </behavior>
  <action>
    Add `JobKindVerifier JobKind = "verifier"` to the enum with a floor doc-comment (matching the :40-65 discipline explaining the chosen `verifierCapsFloorSeconds`). Add a `DefaultCaps` switch arm for JobKindVerifier applying that floor. Add `VerifierJobName(taskUID types.UID, attempt int) string` mirroring `JobName`'s tuple but with the `verifier` kind segment (the name is the dedup key + the `role=verifier` label-selector target for Plan 06's verifierInFlightCount).
  </action>
  <acceptance_criteria>
    - `grep -E 'JobKindVerifier JobKind = "verifier"' internal/dispatch/podjob/caps.go` returns the const.
    - `grep -E 'func VerifierJobName' internal/dispatch/podjob/names.go` returns the helper.
    - `grep -iE 'verifierCapsFloor|verify.*floor' internal/dispatch/podjob/caps.go` returns the documented floor.
    - `go test ./internal/dispatch/podjob/... -run 'Caps|JobName|Verifier' -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/dispatch/podjob/... -run 'Caps|JobName|Verifier' -count=1</automated>
  </verify>
  <done>JobKindVerifier + verify floor + VerifierJobName exist, grep-distinct from executor/planner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TIDE_GATE_COMMAND env injection + RW envelopes/ subPath mount (RO preserved)</name>
  <files>internal/dispatch/podjob/jobspec.go, internal/dispatch/podjob/jobspec_test.go</files>
  <read_first>
    - internal/dispatch/podjob/jobspec.go (BuildOptions :88, conditional env append :447, ReadOnly variant :188-205/:424-441/:496/:525, out.json forward-note :199-204)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"jobspec.go analog — env injection" + §"No Analog Found" (the RW envelopes/ subPath mount is net-new wiring)
    - Phase 48 EVAL-01 posture: RO worktree mount + git-write/push credential omission (must NOT regress)
  </read_first>
  <behavior>
    - BuildOptions gains a GateCommand field; when set (and ReadOnly/verifier variant), the subagent container env carries TIDE_GATE_COMMAND with that value.
    - The verifier variant keeps /workspace ReadOnly AND mounts a separate read-write subPath (envelopes/<uid>/) of the workspace volume so out.json is writable.
    - The RO worktree mount and the absence of git-write/push credentials in env (Phase 48) are preserved — no write/push tool or credential is added.
  </behavior>
  <action>
    Add `GateCommand string` to `BuildOptions`. In the subagentEnv assembly, conditionally append `corev1.EnvVar{Name: "TIDE_GATE_COMMAND", Value: opts.GateCommand}` when non-empty (mirror the PricingOverridesJSON/Traceparent conditional-append shape). NOTE: `TIDE_GATE_COMMAND` carries ONLY the canonical single `gateCommand` for the LLM's `run_gate_command` tool; the FULL authored pass-criteria list the verifier executes out-of-band travels via the envelope (`VerifyContext.Commands`, written to the PVC by Plan 06), NOT this env var — do not attempt to pack a list into the env. Resolve the out.json mount split (the :199-204 forward-note): under the ReadOnly variant, add a SECOND VolumeMount of `VolumeProjectWorkspace` with a read-write subPath scoped to `envelopes/<uid>/` so the reporter/entrypoint can write out.json while /workspace stays ReadOnly. Do NOT add any git-write/push credential or write tool — the Phase 48 read-only contract is load-bearing. Extend jobspec_test.go with static assertions: TIDE_GATE_COMMAND present when GateCommand set; /workspace mount is ReadOnly; a RW envelopes/ subPath mount exists; no git-write/push credential env is present.
  </action>
  <acceptance_criteria>
    - `grep -E 'GateCommand string' internal/dispatch/podjob/jobspec.go` returns the BuildOptions field.
    - `grep -E 'TIDE_GATE_COMMAND' internal/dispatch/podjob/jobspec.go` returns the conditional env append.
    - `grep -iE 'envelopes|subPath' internal/dispatch/podjob/jobspec.go` returns the RW subPath mount wiring.
    - `go test ./internal/dispatch/podjob/... -run 'Verifier|ReadOnly|JobSpec' -count=1` exits 0 (incl. RO-preserved + RW-envelopes + no-git-cred assertions).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/dispatch/podjob/... -run 'Verifier|ReadOnly|JobSpec' -count=1</automated>
  </verify>
  <done>The verifier jobspec injects TIDE_GATE_COMMAND and can write out.json via a RW envelopes/ subPath while /workspace stays ReadOnly and no write/push creds appear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager → verifier pod filesystem/env | The verifier must remain read-only for the worktree; only the envelopes/ path is writable; no git-write/push creds |
| orchestrator → gate command | TIDE_GATE_COMMAND is set from the manager (locked contract), not model-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Elevation | verifier gains write/push capability | mitigate | /workspace stays ReadOnly + no git-write/push credentials in env (Phase 48 EVAL-01 preserved); only a narrow RW envelopes/ subPath is added for out.json — static jobspec test asserts no cred env |
| T-51-04b | Tampering | verifier writing into the shared worktree | mitigate | The RW mount is subPath-scoped to envelopes/<uid>/, not the worktree root; worktree mount stays ReadOnly |
| T-51-04c | Elevation / Tampering | command injection via gate command | mitigate | TIDE_GATE_COMMAND value flows from the manager (Plan 06 sets it from the locked contract), never a model tool arg |
| T-51-SC | Tampering | package installs | N/A | No package installs — jobspec builder only |
</threat_model>

<verification>
- `go test ./internal/dispatch/podjob/... -count=1` green.
- `make lint` clean.
- Re-confirm Phase 48's `make test-verifier-readonly` intent is not regressed by inspection (the RO worktree + cred omission is asserted statically here; the live adversarial proof remains Phase 48's gate).
</verification>

<success_criteria>
- JobKindVerifier + VerifierJobName + verify caps floor exist.
- The verifier jobspec injects TIDE_GATE_COMMAND and writes out.json via a RW envelopes/ subPath while keeping /workspace ReadOnly and omitting git-write/push creds.
</success_criteria>

<output>
Create `.planning/phases/51-the-task-loop/51-04-SUMMARY.md` when done.
</output>
