---
phase: 50-execution-loop-hardening-loop-native-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/otelai/attrs.go
  - pkg/otelai/attrs_test.go
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "All 9 loop-native attribute keys (loop.kind, loop.run_id, loop.parent_run_id, loop.iteration, loop.candidate_version, loop.exit_reason, evaluation.result, evaluation.version, human_intervention) exist as pkg/otelai helpers, never string literals at call sites"
    - "The keys are deliberately NOT tide.-prefixed and the code documents why"
    - "TestKeysUseSemconvModule still passes with the new consts present"
    - "evaluation.* and human_intervention helpers are defined but have zero non-test callers in Phase 50"
  artifacts:
    - path: "pkg/otelai/attrs.go"
      provides: "loop.*/evaluation.*/human_intervention consts + LoopAttributes/LoopRunID/LoopIteration/EvaluationAttributes/HumanIntervention helpers"
      contains: "loop.run_id"
    - path: "pkg/otelai/attrs_test.go"
      provides: "exact-equality tests for every new helper + guard-test continuity"
      contains: "TestLoopAttributes"
  key_links:
    - from: "pkg/otelai/attrs_test.go"
      to: "pkg/otelai/attrs.go"
      via: "TestKeysUseSemconvModule source-grep guard"
      pattern: "TestKeysUseSemconvModule"
---

<objective>
Define the OBS-01 loop-native span attribute vocabulary as `pkg/otelai` helpers: the 9 keys (`loop.kind`/`loop.run_id`/`loop.parent_run_id`/`loop.iteration`/`loop.candidate_version`/`loop.exit_reason`/`evaluation.result`/`evaluation.version`/`human_intervention`) as package consts plus typed helper functions, per D-05.

Purpose: Plans 50-06 (AGENT span) and 50-07 (LLM-span subset) stamp these helpers; Phase 51's LangGraph evaluator reuses the same literal keys. Defining them once here keeps the trace schema stable ahead of its consumers and passes the `TestKeysUseSemconvModule` grep guard (string literals at stamping sites would fail review).
Output: extended `pkg/otelai` with helpers + tests, green under `go test ./pkg/otelai/...`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-execution-loop-hardening-loop-native-observability/50-CONTEXT.md
@.planning/phases/50-execution-loop-hardening-loop-native-observability/50-PATTERNS.md
@pkg/otelai/attrs.go
@pkg/otelai/attrs_test.go

<interfaces>
From pkg/otelai/attrs.go (verified 2026-07-18):
- Custom-key const block (:85-109): `keyAgentRole = "tide.role"` etc. — the block shape to mirror (the new block is deliberately NOT `tide.`-prefixed; see Pitfall 3)
- `AgentInvocation(system, name, role, level string) []attribute.KeyValue` (:240) — the positional-args composite-helper shape
- `FailureDetail(exitCode int, reason string) []attribute.KeyValue` (:294) — the int+string sibling shape
- `EnvelopeDegraded() attribute.KeyValue` (:308) — the bare-bool marker shape (template for HumanIntervention)
- `SessionID(id string) attribute.KeyValue` — the single-string helper shape (template for LoopRunID)

From pkg/otelai/attrs_test.go:
- `TestKeysUseSemconvModule` (:343-355): forbidden-prefix regex is exactly `"(llm\.|openinference\.|gen_ai\.|agent\.)` — `loop.` and `evaluation.` are NOT forbidden (verified). The test passes with the new consts unmodified.
- `TestSessionID` (:357-365): exact-equality `reflect.DeepEqual` against `attribute.String(...)` — the test shape for every new helper
- `findRepoRoot(t)` (:487) — repo-root walker helper
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loop.*/evaluation.*/human_intervention consts + helper functions</name>
  <files>pkg/otelai/attrs.go</files>
  <read_first>pkg/otelai/attrs.go (full — const-block idiom :85-109, AgentInvocation :240, FailureDetail :294, EnvelopeDegraded :308), .planning/phases/50-execution-loop-hardening-loop-native-observability/50-PATTERNS.md §"pkg/otelai/attrs.go" (the exact const spellings + the not-tide-prefixed rationale)</read_first>
  <behavior>
    - Test 1: `LoopAttributes(LoopKindExecution, "uid-1-2", "uid-1", 3, "abc123", "completed")` returns exactly 6 KeyValues with keys `loop.kind`/`loop.run_id`/`loop.parent_run_id`/`loop.iteration`/`loop.candidate_version`/`loop.exit_reason`
    - Test 2: `LoopAttributes(LoopKindExecution, "uid-1-2", "", 3, "", "")` omits the empty optionals — returns exactly 3 KeyValues (kind, run_id, iteration), never a fabricated empty value
    - Test 3: `LoopRunID("x")` == `attribute.String("loop.run_id", "x")`; `LoopIteration(4)` == `attribute.Int("loop.iteration", 4)`; `HumanIntervention()` == `attribute.Bool("human_intervention", true)`; `EvaluationAttributes("APPROVED", "v1")` returns `evaluation.result` + `evaluation.version`
  </behavior>
  <action>
    Add to `pkg/otelai/attrs.go` a NEW const block (do not extend the existing `tide.*` block) with exactly these 9 keys: `keyLoopKind = "loop.kind"`, `keyLoopRunID = "loop.run_id"`, `keyLoopParentRunID = "loop.parent_run_id"`, `keyLoopIteration = "loop.iteration"`, `keyLoopCandidateVer = "loop.candidate_version"`, `keyLoopExitReason = "loop.exit_reason"`, `keyEvaluationResult = "evaluation.result"`, `keyEvaluationVersion = "evaluation.version"`, `keyHumanIntervention = "human_intervention"`.

    Block doc comment (load-bearing, per Pitfall 3): these keys are DELIBERATELY not `tide.`-prefixed — they are the loop-native, cross-vendor convention from the five-loop model; Phase 51's LangGraph evaluator (a non-TIDE-branded process) emits spans with the SAME literal strings, and a `tide.` prefix would be semantically wrong for a cross-implementation convention. State explicitly that this is a documented deviation from the file's "only tide.* may be hand-rolled" note, so a future reader does not "fix" them into the tide namespace. Also state the population contract per key: `loop.*` keys are populated by Phase 50 (execution attempts); `evaluation.result`/`evaluation.version`/`human_intervention` are DEFINED here but populated only by Phase 51's evaluator/Task loop — Phase 50 code must never fake-populate them.

    Add an exported const `LoopKindExecution = "execution"` — the sole `loop.kind` value Phase 50 emits (Phase 51 adds evaluator/task kinds).

    Add helpers (all mirroring the file's existing shapes):
    - `func LoopAttributes(kind, runID, parentRunID string, iteration int, candidateVersion, exitReason string) []attribute.KeyValue` — always includes `loop.kind` (string), `loop.run_id` (string), `loop.iteration` (int); appends `loop.parent_run_id`, `loop.candidate_version`, `loop.exit_reason` ONLY when non-empty (the Phase-46 "absent when empty, never a fabricated empty value" enrichment discipline). Doc comment: primary home is the AGENT-kind span (D-05); callers gate the call on a non-empty runID.
    - `func LoopRunID(id string) attribute.KeyValue` and `func LoopIteration(n int) attribute.KeyValue` — the correlating subset stamped on per-call LLM spans (D-05); mirror `SessionID`'s single-value shape.
    - `func EvaluationAttributes(result, version string) []attribute.KeyValue` — returns `evaluation.result` + `evaluation.version`; doc comment: defined for schema stability, first caller is Phase 51's EVALUATOR span (OBS-03); no Phase 50 code calls this.
    - `func HumanIntervention() attribute.KeyValue` — `attribute.Bool(keyHumanIntervention, true)` mirroring `EnvelopeDegraded`'s marker shape; doc comment: first caller is Phase 51.
  </action>
  <verify>
    <automated>go build ./pkg/otelai/ && go test ./pkg/otelai/ -run TestKeysUseSemconvModule -v</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/otelai/attrs.go` defines a helper whose returned KeyValue uses `attribute.Key("loop.run_id")` semantics (i.e. `LoopRunID("x")` equals `attribute.String("loop.run_id", "x")`)
    - All 9 key literals appear exactly once each in `attrs.go` (const block), and `grep -c '"loop\.' pkg/otelai/attrs.go` returns 6
    - `LoopKindExecution` exported const exists with value `execution`
    - `go test ./pkg/otelai/ -run TestKeysUseSemconvModule` exits 0 (the guard passes unmodified — the new prefixes are not in its forbidden set)
    - The const-block doc comment contains the phrase "not" + "tide." prefix rationale and names Phase 51 as the evaluation.*/human_intervention populator
  </acceptance_criteria>
  <done>All 9 keys + LoopKindExecution + 5 helpers exist with documented population contract; package builds; semconv guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Exact-equality tests for every new helper</name>
  <files>pkg/otelai/attrs_test.go</files>
  <read_first>pkg/otelai/attrs_test.go (TestSessionID :357 — the exact-equality shape; TestKeysUseSemconvModule :343 — must stay green), pkg/otelai/attrs.go (the helpers just added)</read_first>
  <action>
    Add to `pkg/otelai/attrs_test.go`, mirroring `TestSessionID`'s `reflect.DeepEqual` exact-equality shape for EVERY new helper (all of them, not just the first):
    - `TestLoopAttributes_FullyPopulated` — the Task 1 behavior-1 case: 6 KeyValues, exact keys and values asserted in order.
    - `TestLoopAttributes_OmitsEmptyOptionals` — behavior-2: empty parentRunID/candidateVersion/exitReason → exactly 3 KeyValues (`loop.kind`, `loop.run_id`, `loop.iteration`); assert no KeyValue in the result has an empty string value.
    - `TestLoopRunID` / `TestLoopIteration` — exact equality against `attribute.String("loop.run_id", ...)` / `attribute.Int("loop.iteration", ...)`.
    - `TestEvaluationAttributes` — exact equality for the `evaluation.result`/`evaluation.version` pair.
    - `TestHumanIntervention` — exact equality against `attribute.Bool("human_intervention", true)`.
    Then run the full package to prove `TestKeysUseSemconvModule` and all pre-existing tests still pass.
  </action>
  <verify>
    <automated>go test ./pkg/otelai/... -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/otelai/... -run 'TestLoopAttributes|TestLoopRunID|TestLoopIteration|TestEvaluationAttributes|TestHumanIntervention'` exits 0 with all 6 new tests present
    - `go test ./pkg/otelai/...` (full package) exits 0 — TestKeysUseSemconvModule unmodified and green
  </acceptance_criteria>
  <done>Every new helper has an exact-equality test; full otelai suite green with the guard untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| span attributes → OTLP export | attribute values flow to the collector/Phoenix; keys are compiled-in consts, values are stamped by later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | Tampering | hand-rolled key literals drifting from the convention at stamping sites | mitigate | keys exist only as pkg/otelai consts + helpers; `TestKeysUseSemconvModule` review-guard + exact-equality tests lock the literal strings |
| T-50-05 | Information Disclosure | fabricated-empty attribute values leaking placeholder data into traces | mitigate | `LoopAttributes` omits empty optionals (absent-when-empty discipline, tested by TestLoopAttributes_OmitsEmptyOptionals) |
| T-50-SC | Tampering | package installs | accept | zero new packages this plan |
</threat_model>

<verification>
- `go test ./pkg/otelai/...` exits 0
- `go vet ./pkg/otelai/...` exits 0
- `grep -rn '"loop\.\|"evaluation\.\|"human_intervention"' --include="*.go" internal/ cmd/ | grep -v _test` returns 0 hits (no string literals outside pkg/otelai — stamping sites in later plans use the helpers)
</verification>

<success_criteria>
- All 9 OBS-01 keys defined as otelai helpers with documented not-tide-prefixed rationale and per-key population contract
- evaluation.*/human_intervention defined-but-uncalled (Phase 51 populates)
- Full otelai suite green including the unmodified semconv guard
</success_criteria>

<output>
Create `.planning/phases/50-execution-loop-hardening-loop-native-observability/50-02-SUMMARY.md` when done.
</output>
