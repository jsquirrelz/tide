---
phase: 50-execution-loop-hardening-loop-native-observability
plan: 05
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - cmd/tide-langgraph-verifier/verifier/envelope.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py
autonomous: true
requirements: [EXEC-02, EXEC-03]
must_haves:
  truths:
    - "The Python envelope writer mirrors every new Go field: terminal_reason, loop_run_id, attempt_id, run_evidence (EnvelopeOut) and terminal_reason, changed_file_count (TerminationStub)"
    - "Python parity is proven against the SAME shared golden fixture Go authored (pkg/dispatch/testdata/envelope_out_golden.json) — value-equivalence, never byte-compare"
    - "An unset terminal_reason writes as empty string, never silently defaults to completed"
    - "write_termination_stub with all new fields stays strictly under 4096 bytes (matching Go's < not <=)"
  artifacts:
    - path: "cmd/tide-langgraph-verifier/verifier/envelope.py"
      provides: "extended write_envelope_out/write_termination_stub + ENVELOPE_OUT_GOLDEN_FIXTURE path constant"
      contains: "terminal_reason"
    - path: "cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py"
      provides: "golden-fixture parity + truncation + no-silent-default tests"
      contains: "envelope_out_golden"
  key_links:
    - from: "cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py"
      to: "pkg/dispatch/testdata/envelope_out_golden.json"
      via: "ENVELOPE_OUT_GOLDEN_FIXTURE repo-root path (mirroring verdict.GOLDEN_FIXTURE)"
      pattern: "envelope_out_golden"
---

<objective>
Hand-port the Phase 50 envelope fields into the Python verifier mirror (`verifier/envelope.py`), proven by the shared golden fixture — the Go↔Python envelope duality (import-firewalled, hand-authored pair, never shared source). This is the Phase-49 discipline repeated: the Python side carries the schema ahead of its consumer (the verifier's FULL evidence population is Phase 51).

Purpose: every new `EnvelopeOut`/`TerminationStub` field lands in BOTH languages or the wire contract forks silently — the milestone's named "biggest hidden cost".
Output: extended `envelope.py` + `test_envelope.py`, green under `make test-langgraph-verifier`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-execution-loop-hardening-loop-native-observability/50-CONTEXT.md
@.planning/phases/50-execution-loop-hardening-loop-native-observability/50-PATTERNS.md
@cmd/tide-langgraph-verifier/verifier/envelope.py
@cmd/tide-langgraph-verifier/verifier/verdict.py
@cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py
@cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py
@pkg/dispatch/testdata/envelope_out_golden.json

<interfaces>
From cmd/tide-langgraph-verifier/verifier/envelope.py (verified 2026-07-18):
- `write_envelope_out(path, *, exit_code, result, reason="")` (:132-160) — builds the out dict, writes JSON, chmod 0o644
- `write_termination_stub(path, *, exit_code, reason="", gate_decision="", findings_count=0, high_severity_count=0)` (:163-216) — the strict `< TERMINATION_STUB_MAX_BYTES` (4096) progressive-truncation loop; ONLY `reason` is truncated; bounded fields join unconditionally (Phase-49 recorded decision: unconditional, deviating from Go's omitempty)
- `TERMINATION_STUB_MAX_BYTES = 4096`

From verifier/verdict.py:111 — the repo-root fixture-path idiom to replicate:
`GOLDEN_FIXTURE = _repo_root() / "pkg" / "dispatch" / "testdata" / "gate_decision_golden.json"`

From tests/test_verdict.py: `test_golden_fixture_round_trip` (:21-44, value-equivalence re-dump), `_write_stub_with_timeout` (:147-164, daemon-thread hang guard), `test_write_termination_stub_with_verdict_fields_stays_small` (:128+)

From Plan 50-01's golden fixture: `terminalReason: "cap_exceeded"`, `loopRunID`, `attemptID`, full `runEvidence` (specID/lockingCommit/commands/evaluatorVersions/changedFiles/changedFileTotal/model/promptVersion/runtimeVersion)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend write_envelope_out + write_termination_stub with the mirrored fields</name>
  <files>cmd/tide-langgraph-verifier/verifier/envelope.py</files>
  <read_first>cmd/tide-langgraph-verifier/verifier/envelope.py (full — both writer functions + the truncation loop), cmd/tide-langgraph-verifier/verifier/verdict.py (_repo_root/GOLDEN_FIXTURE idiom), pkg/dispatch/testdata/envelope_out_golden.json (the exact JSON key spellings to mirror — loopRunID/attemptID/terminalReason/runEvidence camelCase), pkg/dispatch/envelope.go + pkg/dispatch/run_evidence.go from Plan 50-01 (the Go field set being hand-ported)</read_first>
  <behavior>
    - Test 1: write_envelope_out with terminal_reason="blocked", loop_run_id/attempt_id, and a run_evidence dict produces JSON whose keys are exactly the Go camelCase spellings (terminalReason, loopRunID, attemptID, runEvidence)
    - Test 2: write_envelope_out with defaults writes terminalReason as "" — NEVER "completed"
    - Test 3: write_termination_stub with terminal_reason + changed_file_count + a 10KB reason terminates and stays strictly under 4096 bytes with the bounded fields intact
  </behavior>
  <action>
    Extend `write_envelope_out` keyword-only params: `terminal_reason: str = ""` (doc: the Go TerminalReason enum — `completed|cap_exceeded|blocked|tool_failure|invalid_output`; empty string is the unset sentinel and is written verbatim as `terminalReason` UNCONDITIONALLY, mirroring Go's no-omitempty tag — never defaulted to "completed"; consumers treat unset as NOT completed), `loop_run_id: str = ""` / `attempt_id: str = ""` (joined as `loopRunID`/`attemptID` only when non-empty, mirroring Go's omitempty), `run_evidence: dict[str, Any] | None = None` (joined as `runEvidence` when not None; the caller owns bounding — document the Go bounds consts by name in the docstring: max 100 changed files, 256-byte paths, 10×256-byte commands, 64-byte versions).

    Extend `write_termination_stub` keyword-only params: `terminal_reason: str = ""` and `changed_file_count: int = 0`, joined into the stub dict UNCONDITIONALLY (matching the Phase-49 recorded decision for gate_decision/findings_count) and NEVER subject to the truncation loop — only `reason` is unbounded free text; the loop body is unchanged. Preserve the strict `len(data) >= TERMINATION_STUB_MAX_BYTES` (i.e. `< 4096`) parity with Go.

    Add module-level `ENVELOPE_OUT_GOLDEN_FIXTURE = _repo_root() / "pkg" / "dispatch" / "testdata" / "envelope_out_golden.json"` mirroring verdict.py's GOLDEN_FIXTURE idiom (reuse/import the existing _repo_root helper per the module's current structure).
  </action>
  <verify>
    <automated>make test-langgraph-verifier 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'terminal_reason' cmd/tide-langgraph-verifier/verifier/envelope.py` ≥ 2 (both writers)
    - `grep -c 'ENVELOPE_OUT_GOLDEN_FIXTURE' cmd/tide-langgraph-verifier/verifier/envelope.py` == 1
    - `write_envelope_out` writes `terminalReason` unconditionally and `loopRunID`/`attemptID`/`runEvidence` conditionally (verified by Task 2 tests)
    - `make test-langgraph-verifier` exits 0 (no existing-test regressions)
  </acceptance_criteria>
  <done>Both Python writers carry every new field with Go-matching key spellings and sentinel semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Golden-fixture parity + truncation + no-silent-default tests</name>
  <files>cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py</files>
  <read_first>cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py (existing shape), cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py (test_golden_fixture_round_trip + _write_stub_with_timeout + stays_small shapes to mirror), pkg/dispatch/testdata/envelope_out_golden.json (values to assert)</read_first>
  <action>
    Extend `test_envelope.py` with:
    - `test_envelope_out_golden_fixture_parity` — read `envelope.ENVELOPE_OUT_GOLDEN_FIXTURE` bytes, `json.loads`, assert: `terminalReason == "cap_exceeded"` (the deliberately non-completed fixture value — a silent-default bug in either language breaks this), `loopRunID` and `attemptID` non-empty with attemptID ending in a `-<digit>` suffix, `runEvidence` present with all 9 keys (`specID`, `lockingCommit`, `commands`, `evaluatorVersions`, `changedFiles`, `changedFileTotal`, `model`, `promptVersion`, `runtimeVersion`) non-empty/non-zero, and `changedFiles[0]` having `path` + `status` keys. Then round-trip: pass the fixture's values through `write_envelope_out` into a tmp_path file and assert the re-read JSON is value-equivalent for every mirrored key (never a byte compare — key order differs across serializers).
    - `test_write_envelope_out_terminal_reason_never_defaults` — call with no `terminal_reason`; assert the written JSON has `terminalReason == ""` and NOT `"completed"`.
    - `test_write_termination_stub_with_loop_fields_stays_small` — mirror the WR-01/WR-02 shape: `terminal_reason="cap_exceeded"`, `changed_file_count=500`, `reason` of 10KB; use the `_write_stub_with_timeout` daemon-thread guard idiom; assert `len(data) < envelope.TERMINATION_STUB_MAX_BYTES`, `parsed["terminalReason"] == "cap_exceeded"`, `parsed["changedFileCount"] == 500`, and the reason was truncated (contains the truncation marker or is shorter than input).
  </action>
  <verify>
    <automated>make test-langgraph-verifier 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py` contains the three new tests and `make test-langgraph-verifier` exits 0
    - The parity test reads the SAME `pkg/dispatch/testdata/envelope_out_golden.json` file the Go test reads (no Python-local fixture copy exists — `ls cmd/tide-langgraph-verifier/verifier/tests/ | grep -c golden` returns 0)
    - The stays-small test asserts strictly `< 4096` with both new bounded fields present
  </acceptance_criteria>
  <done>Go↔Python parity locked by the shared fixture; truncation and no-silent-default proven on the Python side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier output → termination message | the Python writer feeds the same 4KB kubelet budget as Go; an unbounded field is a shared-contract DoS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-12 | Denial of Service | write_termination_stub exceeding the 4096 budget or hanging in the truncation loop | mitigate | new bounded fields join outside the loop; stays-small test with 10KB reason + daemon-thread timeout guard (WR-01 regression shape) |
| T-50-13 | Tampering | Python-side silent default (unset → completed) forking the fail-closed contract | mitigate | terminal_reason defaults to "" and is written verbatim; explicit never-defaults test; golden fixture pins a non-completed value |
| T-50-SC | Tampering | package installs | accept | zero new Python packages (stdlib json/dataclasses only; pins unchanged — verify-langgraph-pins unaffected) |
</threat_model>

<verification>
- `make test-langgraph-verifier` exits 0
- `make verify-dispatch-imports` exits 0 (import firewall intact — the Python image still cannot import Go types; the mirror stays hand-authored)
</verification>

<success_criteria>
- Every new Go envelope field has a hand-ported Python twin with matching JSON spellings, proven by the shared golden fixture
- Unset terminal_reason is visibly empty, never completed, in both languages
- The 4096-byte stub budget holds with all new fields
</success_criteria>

<output>
Create `.planning/phases/50-execution-loop-hardening-loop-native-observability/50-05-SUMMARY.md` when done.
</output>
