---
phase: 48-langgraph-evaluator-image-credproxy-tls-spike
plan: 04
type: execute
wave: 3
depends_on: [48-01, 48-03]
files_modified:
  - cmd/tide-langgraph-verifier/Dockerfile
  - hack/scripts/test-verifier-readonly.sh
  - Makefile
  - .github/workflows/ci.yaml
  - .github/workflows/release.yaml
autonomous: true
requirements: [EVAL-01, EVAL-02]
must_haves:
  truths:
    - "docker build from a clean checkout produces ghcr.io/jsquirrelz/tide-langgraph-verifier with pip install --require-hashes as the only install path"
    - "An adversarial git commit and git push attempted inside the image against a read-only-mounted fixture worktree fail at the filesystem/credential layer (EROFS / missing credentials), not by prompt refusal"
    - "The built image contains zero openinference-*/opentelemetry-* packages (Pitfall E)"
    - "The image is in the docker-buildx-snapshot target and the release build-images matrix"
  artifacts:
    - path: "cmd/tide-langgraph-verifier/Dockerfile"
      provides: "digest-pinned python:3.13-slim-bookworm single-stage build (D-10)"
      contains: "--require-hashes"
    - path: "hack/scripts/test-verifier-readonly.sh"
      provides: "D-09b adversarial behavioral test (pre-materialized fixture worktree + docker run --read-only + :ro mount)"
    - path: "Makefile"
      provides: "test-verifier-readonly target + snapshot-build entry"
      contains: "test-verifier-readonly"
  key_links:
    - from: "Makefile test-verifier-readonly"
      to: "hack/scripts/test-verifier-readonly.sh"
      via: "make target shelling to the script"
      pattern: "test-verifier-readonly"
    - from: ".github/workflows/release.yaml"
      to: "cmd/tide-langgraph-verifier/Dockerfile"
      via: "build-images matrix entry"
      pattern: "tide-langgraph-verifier"
---

<objective>
Build the `ghcr.io/jsquirrelz/tide-langgraph-verifier` image (D-05/D-10: digest-pinned base, hash-enforced install) and prove EVAL-01's read-only contract behaviorally: the D-09b adversarial test attempts `git commit`/`git push`/worktree writes inside the real container against a read-only mount and asserts failure at the filesystem/credential layer — the structural proof, independent of the tool-layer allowlist from 48-03 and the jobspec layer from 48-02 (CONTEXT: three independent layers, each load-bearing).

Purpose: Phase 48 success criteria 3 (adversarial structural proof) and 4's enforcement half (`--require-hashes` at build time = the supply-chain layer the pin gate can't cover).
Output: Dockerfile, behavioral test script + make target, snapshot/release-matrix/CI wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-CONTEXT.md
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-RESEARCH.md

<interfaces>
Dockerfile shape (RESEARCH §Code Examples — single-stage, no Go build stage): digest-pinned `python:3.13-slim-bookworm@sha256:...` (re-resolve the digest via docker pull AT BUILD TIME — the research-session digest `9d7f287...` is a reference point, not durable, per D-10's "re-verify at build time"), apt-get git + `git config --system --add safe.directory '*'`, COPY requirements.txt → `pip install --no-cache-dir --require-hashes -r requirements.txt`, COPY `verifier/`, `USER 1000`, `ENV SSL_CERT_FILE=/etc/tide/proxy/ca.crt`, `ENTRYPOINT ["python", "-m", "verifier"]`. Build from REPO ROOT with `-f cmd/tide-langgraph-verifier/Dockerfile`.

Build-wiring precedents (verified): Makefile `docker-buildx-snapshot` at :368 (per-image buildx lines, "all 7 component images" count text needs bumping); release.yaml build-images matrix entries at ~:309 (`component:` + `dockerfile:` pairs — `tide-claude-subagent`/`tide-import` are the pattern); `hack/scripts/verify-chart-images-published.sh` asserts chart→matrix coverage (this image is NOT chart-referenced this phase, so the gate direction is unaffected — adding a matrix entry cannot break it).

Behavioral test constraints (D-09b + Pitfall D): the fixture worktree is pre-materialized by the HARNESS (git init + commit) BEFORE the read-only mount — the container itself performs zero worktree creation. Adversarial invocations override the entrypoint (`--entrypoint`) to drive git directly — deliberately BYPASSING the tool-layer allowlist, because the claim under test is that the mount/credential layer holds even if the tool layer is defeated. `GIT_TERMINAL_PROMPT=0` makes the push credential failure deterministic (no interactive hang).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + build wiring (snapshot target, release matrix)</name>
  <files>cmd/tide-langgraph-verifier/Dockerfile, Makefile, .github/workflows/release.yaml</files>
  <read_first>48-RESEARCH.md (§Code Examples Dockerfile), images/claude-subagent/Dockerfile (comment style + pinning discipline to mirror), Makefile (:368-390 docker-buildx-snapshot), .github/workflows/release.yaml (~:300-330 build-images matrix)</read_first>
  <action>Write `cmd/tide-langgraph-verifier/Dockerfile` per the interfaces block. Re-resolve the `python:3.13-slim-bookworm` digest live (`docker pull` then inspect) and pin it inline with a dated comment mirroring images/claude-subagent/Dockerfile's style; note in the comment that `pip install --require-hashes` is the second, independent supply-chain layer (the grep gate catches a careless `requirements.in` edit; the hash check catches a substituted wheel). Do NOT copy `requirements-dev.txt`, `pyproject.toml`, or `verifier/tests/` into the image — runtime pins only, tests stay outside (add a `.dockerignore`-compatible COPY scoping or explicit paths; the existing re-include from 48-01 admits the whole tree, so scope the COPY instructions instead). Add the image to `docker-buildx-snapshot` in the Makefile (new buildx line for `ghcr.io/jsquirrelz/tide-langgraph-verifier:$(IMAGE_TAG)` with `-f cmd/tide-langgraph-verifier/Dockerfile`; update the "all 7 component images" count text) and add a single-arch local convenience target (e.g. `docker-build-langgraph-verifier`, tag `:test`) that the behavioral test and spike reuse. Add the release.yaml build-images matrix entry (`component: tide-langgraph-verifier`, `dockerfile: cmd/tide-langgraph-verifier/Dockerfile`) mirroring the tide-import entry shape — no chart reference exists this phase, so `verify-chart-images-published` is unaffected (assert it still passes).</action>
  <verify>
    <automated>make docker-build-langgraph-verifier && docker run --rm --entrypoint pip ghcr.io/jsquirrelz/tide-langgraph-verifier:test list 2>/dev/null | grep -ciE 'openinference|opentelemetry' | grep -qx 0 && bash hack/scripts/verify-chart-images-published.sh</automated>
  </verify>
  <acceptance_criteria>
    - `make docker-build-langgraph-verifier` builds from a clean context; the build log shows `--require-hashes` on the pip step
    - `docker run --rm --entrypoint pip <image> list | grep -ciE 'openinference|opentelemetry'` outputs 0 (Pitfall E / RESEARCH assumption A2 discharged at the image layer)
    - `docker run --rm --entrypoint python <image> -c "import verifier"` exits 0 (package present, imports resolve under the pinned closure)
    - `grep -c 'tide-langgraph-verifier' Makefile` outputs ≥2 (snapshot + convenience target) and `grep -c 'tide-langgraph-verifier' .github/workflows/release.yaml` outputs ≥1 (matrix entry)
    - `bash hack/scripts/verify-chart-images-published.sh` still exits 0
    - Image runs as USER 1000 with `ENV SSL_CERT_FILE=/etc/tide/proxy/ca.crt` (docker inspect assertions)
  </acceptance_criteria>
  <done>Image builds hash-enforced from repo root, joins the snapshot/release matrices, contains no OTel packages, runs as uid 1000.</done>
</task>

<task type="auto">
  <name>Task 2: D-09b adversarial behavioral test — make test-verifier-readonly</name>
  <files>hack/scripts/test-verifier-readonly.sh, Makefile, .github/workflows/ci.yaml</files>
  <read_first>48-CONTEXT.md (D-09 — both proof layers; "three independent layers" in Specific Ideas), 48-RESEARCH.md (§Pitfall D — pre-materialization requirement; §Validation Architecture EVAL-01 rows), hack/scripts/verify-chart-images-published.sh (script style/set -euo pipefail conventions), .github/workflows/ci.yaml (docker-capable job placement)</read_first>
  <action>Write `hack/scripts/test-verifier-readonly.sh` (set -euo pipefail, style-matched to the existing hack/scripts): (1) build or accept the image (reuse `make docker-build-langgraph-verifier`'s `:test` tag via an IMG override); (2) pre-materialize a fixture worktree in `mktemp -d` — git init, local user config, one committed file, plus an `origin` remote pointing at a credential-requiring https URL (e.g. a private-looking github URL that cannot anonymously push); (3) run three adversarial probes with `docker run --read-only -v "$FIXTURE":/workspace:ro --entrypoint` overrides and `GIT_TERMINAL_PROMPT=0`: (a) `git -C /workspace commit --allow-empty -m x` → assert nonzero exit AND stderr matching `read-only file system` (case-insensitive) — the mount layer; (b) `git -C /workspace push origin HEAD` → assert nonzero exit AND output matching an authentication/credential failure (`could not read Username|Authentication failed|terminal prompts disabled`), NOT a success — the credential layer; (c) `sh -c 'touch /workspace/x && touch /x'` → assert nonzero with read-only errors for both the mount and the root filesystem (`--read-only` container flag mirrors the jobspec's ReadOnlyRootFilesystem) — the rootfs layer. Each probe prints PASS/FAIL with the matched evidence line; any probe failing to fail exits the script 1. The script must clearly state (comment + output header) that entrypoint-override is DELIBERATE: it bypasses the tool allowlist to prove the structural layer holds independently (prompt refusal is explicitly not the mechanism under test — EVAL-01). Add `make test-verifier-readonly` invoking the script (help-comment references EVAL-01/D-09b) and wire a ci.yaml step running it in a docker-capable job, alongside a step running `make test-langgraph-verifier` (the CI Python-suite wiring deferred from 48-01 — provision uv/python via a pinned setup action).</action>
  <verify>
    <automated>make test-verifier-readonly</automated>
  </verify>
  <acceptance_criteria>
    - `make test-verifier-readonly` exits 0, printing three PASS lines with matched evidence (EROFS for commit, credential failure for push, read-only for the write probes)
    - The commit probe's matched stderr contains `read-only file system` (case-insensitive) — filesystem layer, not prompt refusal
    - The push probe's matched output shows a credential/auth failure, proving no ambient git credentials exist in the image
    - `grep -c 'test-verifier-readonly' .github/workflows/ci.yaml` outputs ≥1 and `grep -c 'test-langgraph-verifier' .github/workflows/ci.yaml` outputs ≥1
    - Script grep: fixture worktree creation happens before the `:ro` mount (pre-materialization per Pitfall D — no `git init`/`worktree` invocation inside any docker run)
  </acceptance_criteria>
  <done>Adversarial commit/push/write all fail at the mount/credential/rootfs layer inside the real image; test runs locally and in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → build | base image + wheels enter the image at docker build |
| container → mounted worktree | the adversarial surface EVAL-01 exists to close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-SC | Tampering (supply chain) | pip install at image build | mitigate | `pip install --require-hashes` (pip's own enforcement — a substituted wheel fails the build, RESEARCH §Validation "self-enforcing"); base image digest-pinned; all 7 pins slopcheck [OK] |
| T-48-09 | Tampering | writes to the worktree from inside the container | mitigate | D-09b behavioral probes assert EROFS on commit and direct writes under `--read-only` + `:ro` — kernel-layer enforcement proven, not assumed (ASVS V4) |
| T-48-10 | Elevation of Privilege | ambient git push credentials in the image | mitigate | push probe asserts credential failure with `GIT_TERMINAL_PROMPT=0`; no credential material is COPYd or ENVd in the Dockerfile |
| T-48-OTEL | Information Disclosure | transitive OTel auto-instrumentation double-emission | mitigate | image-layer `pip list` assertion: zero openinference/opentelemetry packages (Pitfall E, discharges RESEARCH assumption A2) |
</threat_model>

<verification>
- `make docker-build-langgraph-verifier` + `make test-verifier-readonly` both green locally
- ci.yaml runs the pin gate (48-01), the Python suite, and the behavioral test (v1.0.8 lesson: phase CI, not release pre-flight)
- `verify-chart-images-published` unaffected
</verification>

<success_criteria>
- Phase 48 success criterion 3 fully proven: adversarial commit/push fail at the mount/credential layer inside the real container (behavioral D-09b), complementing 48-02's static D-09a layer
- Phase 48 success criterion 4 fully closed: pin gate (48-01) + `--require-hashes` build enforcement (this plan)
- The image exists under the D-05 name and is release-matrix wired, ready for the 48-05 spike to run against
</success_criteria>

<output>
Create `.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-04-SUMMARY.md` when done.
</output>
