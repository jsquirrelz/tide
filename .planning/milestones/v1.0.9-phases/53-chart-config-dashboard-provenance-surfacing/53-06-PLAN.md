---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 06
type: execute
wave: 2
depends_on: ["53-02"]
files_modified:
  - internal/controller/task_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/level_verify.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/dispatch_helpers_loop_policy_test.go
  - internal/controller/verification_enabled_unit_test.go
autonomous: true
requirements: [CFG-01, CFG-02]
must_haves:
  truths:
    - "A Locked contract at a chart-disabled level (no authored Project-scope entry) never dispatches a verifier — the level stamps through exactly like today's no-contract path, zero spend"
    - "Flipping chart posture on later activates already-authored contracts without re-planning (enablement gates dispatch, not authoring — D-04)"
    - "Chart per-level defaults feed maxIterations/onExhaustion when the authored contract leaves them unset, but phase/milestone/project maxIterations stays clamped to 0 regardless of any chart value (Phase-52 D-07, pinned by test)"
    - "When subagent.verify.model is set, all three verifier dispatch tiers use it instead of borrowing the level executor's model; when empty, behavior is byte-identical to today (D-02)"
  artifacts:
    - path: internal/controller/dispatch_helpers.go
      provides: "resolveVerifierModel helper + chart-tier defaulting in ResolveLoopPolicy"
      contains: "resolveVerifierModel"
    - path: internal/controller/level_verify.go
      provides: "enablement AND-gate in levelVerifyDecision (one function, three levels)"
      contains: "verificationEnabledForLevel"
  key_links:
    - from: internal/controller/task_controller.go
      to: internal/controller/dispatch_helpers.go
      via: "verificationEnabledForLevel ANDed onto the hasVerificationContract branch"
      pattern: "verificationEnabledForLevel"
    - from: internal/controller/plan_controller.go
      to: internal/controller/dispatch_helpers.go
      via: "verificationEnabledForLevel ANDed onto the GateCommand/Locked branch (~:995)"
      pattern: "verificationEnabledForLevel"
    - from: internal/controller/level_verify.go
      to: internal/controller/dispatch_helpers.go
      via: "verificationEnabledForLevel in levelVerifyDecision's inactive early-return"
      pattern: "verificationEnabledForLevel"
---

<objective>
Wire D-04's enablement gate into the three verifier-dispatch chokepoints RESEARCH Finding 4
pinned, layer the chart per-level LoopPolicy defaults into the Phase-52 resolvers, and give
the verifier model its D-02 resolution (chart default replaces the borrow-the-executor-model
fallback only when supplied).

D-02 scoping (RESEARCH Finding 6, surfaced for cheap veto): `VerificationSpec.Evaluator`
is write-only today — carried onto LoopPolicy.EvaluatorRef, never resolved to an
image/model anywhere; no registry exists. Per Finding 6's recommendation (a), this phase
implements the two REAL tiers (chart default > compiled default) and leaves the authored
Evaluator string exactly as reserved/carried as it is today; the resolution helper's doc
comment records the full intended precedence (authored Evaluator > chart > compiled) so the
future registry slots in above the chart tier without restructuring.

Purpose: CFG-01's "follows the existing precedence chain" made real at every dispatch site;
CFG-02's boundedness enforced at the single place spend originates.
Output: three AND-gates, resolveVerifierModel consumed at three provider sites, chart-tier
defaults in ResolveLoopPolicy with the clamp pinned, and envtest enablement coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-CONTEXT.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-PATTERNS.md

<interfaces>
From plan 53-02 (already landed — consume, do not redeclare):
- internal/controller/dispatch_helpers.go: type VerifyDefaults { Image, Model string;
  Levels map[string]pkgdispatch.LevelVerifyDefault }; func verificationEnabledForLevel(
  project, level, chartDefaults) bool; VerifyDefaults field on PlannerReconcilerDeps and
  TaskReconcilerDeps.

The three chokepoints (verified line anchors, 2026-07-21):
- internal/controller/task_controller.go:1556 `} else if hasVerificationContract(task) {`
- internal/controller/plan_controller.go:995 `if verification.GateCommand != "" && verification.Phase == verificationPhaseLocked && !planVerifyResolvedByOperator(plan) {`
- internal/controller/level_verify.go:146-157 levelVerifyDecision (shared by phase/milestone/project via maybeRunLevelVerify at :168, which has project in scope)

The three verifier-model borrow sites (all currently ResolveProvider(...).Model):
- internal/controller/task_controller.go:2331-2333 (Vendor "langgraph", level "task")
- internal/controller/plan_controller.go:1265-1266 (level "plan")
- internal/controller/level_verify.go:403-404 (level target.Level)

Resolver signatures gaining the chart tier:
- ResolveLoopPolicy(project, plan, task, level) at dispatch_helpers.go:453 — clamp logic at :456-480
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AND the enablement gate into the three chokepoints + resolveVerifierModel at the three provider sites</name>
  <files>internal/controller/task_controller.go, internal/controller/plan_controller.go, internal/controller/level_verify.go, internal/controller/dispatch_helpers.go</files>
  <read_first>
    - internal/controller/task_controller.go:1540-1580 (the completion branch containing the :1556 chokepoint — confirm whether project is in scope; if not, note how the surrounding code resolves it) and :2320-2340 (the verifier ProviderSpec construction)
    - internal/controller/plan_controller.go:985-1020 (the :995 branch + its else clearing Phase to "") and :1255-1275 (ProviderSpec)
    - internal/controller/level_verify.go:140-180 (levelVerifyDecision + maybeRunLevelVerify caller) and :395-410 (ProviderSpec)
    - internal/controller/dispatch_helpers.go (verificationEnabledForLevel from 53-02 + ResolveProvider — signature context for the new helper)
  </read_first>
  <action>
    dispatch_helpers.go: add `resolveVerifierModel(project *tideprojectv1alpha3.Project,
    level string, deps-model-and-provider-defaults) string` beside
    verificationEnabledForLevel: return VerifyDefaults.Model when non-empty, else
    `ResolveProvider(project, level, helmProviderDefaults).Model` (today's borrow,
    byte-identical when chart config absent). Parameter shape is executor's choice
    (VerifyDefaults + ProviderDefaults values, or the Deps struct) — the PRECEDENCE lives
    only here. Doc comment records the full D-02 chain including the reserved authored-
    Evaluator tier (Finding 6: unconsumed name-ref, no registry — deliberate).

    Three AND-gates (pure sibling conditions — never suppress contract AUTHORING, D-04):
    (1) task_controller.go:1556 — extend to `} else if hasVerificationContract(task) &&
    verificationEnabledForLevel(project, "task", r.Deps.VerifyDefaults) {` (resolve project
    the same way the surrounding branch does; if project isn't already in scope, mirror the
    nearest existing project-resolution in the same reconcile path). A disabled level thus
    falls to the existing else → LevelPhaseSucceeded — exactly today's no-contract path.
    (2) plan_controller.go:995 — add `&& verificationEnabledForLevel(project, "plan",
    r.Deps.VerifyDefaults)` to the existing 3-clause condition; disabled falls to the
    existing else (Phase cleared to "").
    (3) level_verify.go — extend levelVerifyDecision's signature with the project +
    chartDefaults it needs (maybeRunLevelVerify already has project in scope; thread
    deps.VerifyDefaults through) and fold `!verificationEnabledForLevel(project, level,
    chartDefaults)` into the FIRST early-return (levelVerifyInactive), beside the existing
    GateCommand==""/not-Locked check. The level string must be the same level vocabulary
    verificationEnabledForLevel keys on (phase|milestone|project — confirm against how
    maybeRunLevelVerify names its target level).

    Three model sites: replace `ResolveProvider(project, <level>, ...).Model` with
    `resolveVerifierModel(project, <level>, ...)` at task_controller.go:2331-2333,
    plan_controller.go:1265-1266, level_verify.go:403-404. Vendor stays "langgraph"
    everywhere; nothing else in the ProviderSpec changes.

    Update every existing test that calls levelVerifyDecision with the old signature
    (compile-driven); where fixtures previously relied on implicit enablement, give them a
    chart default `{level: {Enabled: true}}` or an authored Project-scope entry so existing
    behavioral assertions keep passing on their original terms.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "verificationEnabledForLevel" internal/controller/task_controller.go internal/controller/plan_controller.go internal/controller/level_verify.go` reports >= 1 per file
    - `grep -c "resolveVerifierModel" internal/controller/task_controller.go internal/controller/plan_controller.go internal/controller/level_verify.go` reports >= 1 per file
    - `grep -cE "ResolveProvider\(.*\)\.Model" internal/controller/task_controller.go internal/controller/plan_controller.go internal/controller/level_verify.go` reports 0 per file (borrow centralized into the helper)
    - `go test ./internal/controller/... -count=1` exits 0 (full suite — envtest included, no vacuous filter)
  </acceptance_criteria>
  <done>Enablement is a single-function decision ANDed at exactly three sites; the verifier model resolves through one helper with chart-then-borrow precedence.</done>
</task>

<task type="auto">
  <name>Task 2: Chart-tier maxIterations/onExhaustion defaults in ResolveLoopPolicy + clamp pin + envtest enablement coverage</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/dispatch_helpers_loop_policy_test.go, internal/controller/verification_enabled_unit_test.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go:412-490 (ResolveVerificationSpec + ResolveLoopPolicy — the maxIter clamp at :456-480: task/plan floor-to-1 on 0, phase/milestone/project unconditional 0; find where OnExhaustion defaults)
    - internal/controller/dispatch_helpers_loop_policy_test.go (TestResolveLoopPolicy table incl. the :91 phase-clamp subtest — the idiom to extend)
    - Callers of ResolveLoopPolicy (grep) — every call site needs the new chartDefaults argument
  </read_first>
  <action>
    Extend ResolveLoopPolicy to take the chart defaults (add a VerifyDefaults — or
    map[string]pkgdispatch.LevelVerifyDefault — parameter; update all callers
    compile-driven, passing the Deps field). Layering rules (the chart tier sits BELOW
    authored values, ABOVE compiled fallbacks):
    - MaxIterations, task/plan levels only: when the resolved spec's MaxIterations == 0
      (unset — 0-at-repair-levels has always meant "floor to 1", so chart defaulting on 0
      preserves compatibility) AND the chart level entry has MaxIterations > 0, use the
      chart value; else the existing floor-to-1 behavior. Phase/milestone/project: the
      unconditional `maxIter = 0` clamp runs LAST and is unreachable by any chart value
      (D-07 Phase 52 — the chart must not be able to re-open it).
    - OnExhaustion: when the resolved spec's OnExhaustion == "" AND the chart level entry
      has a non-empty OnExhaustion, use the chart value; else the existing compiled
      default path unchanged.
    Document both rules in the function's doc comment, including the explicit note that an
    authored maxIterations 0 at task/plan is indistinguishable from unset by design
    (pre-existing semantics, now also the chart-default hook).

    dispatch_helpers_loop_policy_test.go — extend TestResolveLoopPolicy:
    (a) task level, spec MaxIterations 0 + chart task.MaxIterations 3 → policy 3;
    (b) task level, authored MaxIterations 2 + chart 5 → policy 2 (authored wins);
    (c) phase, milestone, AND project levels each with chart MaxIterations 5 → policy 0
    (the Specifics pin: "the chart must not be able to re-open D-07's clamp");
    (d) plan level, spec OnExhaustion "" + chart requireApproval → requireApproval;
    (e) no chart entry for the level → all prior subtests' behavior unchanged (regression).

    verification_enabled_unit_test.go — add an envtest-level behavioral spec ONLY if the
    existing Ginkgo suite files already cover the task/plan completion branches with
    fixtures you can extend cheaply; otherwise pin the dispatch-gating behavior with
    table-driven unit tests against the extracted decision functions (levelVerifyDecision
    with enabled/disabled chart fixtures; the plan/task branch conditions via their pure
    helpers). Required coverage either way: (i) Locked contract + chart-disabled + no
    authored entry → no Verifying transition/dispatch; (ii) same contract + chart-enabled →
    Verifying; (iii) same contract + authored Project.Spec.Verification entry +
    chart-disabled → Verifying (authored outranks chart). Remember the Phase 51-03 lesson:
    any new spec must genuinely execute — run the unfiltered package test as proof.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'TestResolveLoopPolicy|TestVerificationEnabledForLevel' -count=1 -v && go test ./internal/controller/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Subtest (c) proves chart MaxIterations 5 → 0 at ALL THREE escalate levels (three named subtests visible in -v output)
    - Subtest (b) proves authored-over-chart precedence at task level
    - Coverage items (i)/(ii)/(iii) exist and execute (visible in -v output, not vacuously filtered)
    - `go test ./internal/controller/... -count=1` exits 0
  </acceptance_criteria>
  <done>Chart defaults feed the loop policy exactly where authored values are absent; the maxIter=0 escalate-level clamp is provably chart-proof; enablement gating is behaviorally covered at every precedence branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chart-supplied VerifyDefaults → dispatch decision | operator config decides whether billable verifier Jobs are created |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-13 | Elevation of Privilege | chart values re-opening the escalate-level repair loop | mitigate | maxIter=0 clamp runs last in ResolveLoopPolicy, structurally unreachable by chart input; pinned by three explicit subtests |
| T-53-14 | Denial of Service / spend | enablement drift across three dispatch files | mitigate | precedence lives in exactly one function (D-04); all three sites AND the same call; grep acceptance pins one call per file |
| T-53-15 | Repudiation | silently skipped dispatch at a disabled level | accept | a disabled level is byte-identical to today's no-contract path (documented D-04 design); the posture is operator-chosen config, not an error state — no new logging surface required |
| T-53-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test ./internal/controller/... -count=1` (full envtest suite green — 51/51+ baseline plus new specs)
- `make lint` clean
</verification>

<success_criteria>
- All four must_have truths hold with executing tests behind each
- Zero behavior change for any configuration that existed before this phase (empty VerifyDefaults ⇒ byte-identical dispatch decisions)
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-06-SUMMARY.md` when done.
</output>
