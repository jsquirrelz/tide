---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/verify_defaults.go
  - pkg/dispatch/verify_defaults_test.go
  - cmd/manager/main.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/verification_enabled_unit_test.go
  - internal/controller/task_controller.go
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "A malformed or out-of-range --verify-levels-json crashes the manager at startup with a descriptive error (fail-closed, never a silent permissive posture)"
    - "verificationEnabledForLevel resolves authored Project-scope config > chart per-level enabled > off, in exactly one function"
    - "VerifyDefaults (Image, Model, Levels) is threaded onto BOTH PlannerReconcilerDeps and TaskReconcilerDeps from main.go"
  artifacts:
    - path: pkg/dispatch/verify_defaults.go
      provides: "LevelVerifyDefault type + ParseVerifyLevelDefaults fail-closed validator"
      exports: ["LevelVerifyDefault", "ParseVerifyLevelDefaults"]
    - path: internal/controller/dispatch_helpers.go
      provides: "VerifyDefaults struct + verificationEnabledForLevel helper beside ResolveVerificationSpec"
      contains: "verificationEnabledForLevel"
    - path: internal/controller/verification_enabled_unit_test.go
      provides: "pure-function precedence test in its own file (Phase 51-03 lesson: never rely on a -run filter against the shared TestControllers Ginkgo suite)"
      contains: "TestVerificationEnabledForLevel"
  key_links:
    - from: cmd/manager/main.go
      to: pkg/dispatch/verify_defaults.go
      via: "flag.StringVar --verify-levels-json + startup ParseVerifyLevelDefaults, os.Exit(1) on error"
      pattern: "verify-levels-json"
    - from: cmd/manager/main.go
      to: internal/controller/dispatch_helpers.go
      via: "VerifyDefaults assigned onto both Deps structs (mirrors VerifierImage dual-wiring at main.go:464,569)"
      pattern: "VerifyDefaults"
---

<objective>
Build the manager-side half of D-01/D-04: the fail-closed structured-config parser
(`ParseVerifyLevelDefaults`, mirroring `ParsePricingOverrides` byte-for-byte per RESEARCH
Finding 8), the `TIDE_VERIFIER_MODEL` scalar env read, the `VerifyDefaults` struct threaded
onto both reconciler Deps, and the ONE shared enablement helper
`verificationEnabledForLevel` (D-04's precedence: authored Project-scope config wins, else
chart per-level enabled, else off).

Purpose: everything the chart surface (plan 53-05) and the dispatch gates (plan 53-06)
consume — pure Go, no chart dependency, safe to land first (an unrendered flag simply means
empty defaults).
Output: new parser + tests, main.go wiring, VerifyDefaults on both Deps structs, the
enablement helper with its own unit-test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-CONTEXT.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-PATTERNS.md

<interfaces>
Contracts fixed by this plan (downstream plans 53-05/53-06 build against these — do not rename):

pkg/dispatch/verify_defaults.go:
- type LevelVerifyDefault struct { Enabled bool `json:"enabled"`; MaxIterations int32 `json:"maxIterations,omitempty"`; OnExhaustion string `json:"onExhaustion,omitempty"` }
- func ParseVerifyLevelDefaults(s string) (map[string]LevelVerifyDefault, error)

internal/controller/dispatch_helpers.go:
- type VerifyDefaults struct { Image string; Model string; Levels map[string]pkgdispatch.LevelVerifyDefault }
- func verificationEnabledForLevel(project *tideprojectv1alpha3.Project, level string, chartDefaults VerifyDefaults) bool

Existing anchors consumed (verified 2026-07-21):
- pkg/dispatch/pricing.go:39-82 ParsePricingOverrides — the exact validate-fail-closed shape
- cmd/manager/main.go:219 verifierImage envOrDefault read; :464 and :569 the two Deps
  assignments where VerifierImage is already dual-wired
- internal/controller/dispatch_helpers.go:361-379 projectLevelVerificationDefault (returns
  *VerificationSpec, nil vs authored already distinguishable per project_types.go:60)
- internal/controller/dispatch_helpers.go:412 ResolveVerificationSpec (place the helper beside it)
- api/v1alpha3/task_types.go:161 OnExhaustion kubebuilder enum: escalate;requireApproval
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParseVerifyLevelDefaults — fail-closed per-level JSON validator (mirrors pricing.go)</name>
  <files>pkg/dispatch/verify_defaults.go, pkg/dispatch/verify_defaults_test.go</files>
  <read_first>
    - pkg/dispatch/pricing.go (full file — ParsePricingOverrides + the doc comment at :25-31 explaining why the type lives in pkg/dispatch: manager validates at startup without importing the anthropic package, provider firewall D-C1)
    - pkg/dispatch/pricing_test.go (the table-driven test shape to mirror)
    - api/v1alpha3/task_types.go:135-163 (MaxIterations semantics — 0 is meaningful; OnExhaustion enum escalate|requireApproval)
  </read_first>
  <behavior>
    - Test 1: empty string and "{}" both return an empty non-nil map, nil error
    - Test 2: valid JSON `{"task":{"enabled":true,"maxIterations":3,"onExhaustion":"requireApproval"},"plan":{"enabled":false,"maxIterations":1,"onExhaustion":"requireApproval"},"phase":{"enabled":false},"milestone":{"enabled":true,"onExhaustion":"requireApproval"},"project":{"enabled":true,"onExhaustion":"requireApproval"}}` parses to a 5-entry map with the exact field values
    - Test 3: invalid JSON returns error containing "invalid JSON"
    - Test 4: unknown level key (e.g. "milestne") returns error containing the offending key (closed set task|plan|phase|milestone|project — reject, don't ignore)
    - Test 5: negative maxIterations returns error naming the level and the value
    - Test 6: onExhaustion value outside {"", "escalate", "requireApproval"} returns error naming the level and the value
  </behavior>
  <action>
    Create pkg/dispatch/verify_defaults.go declaring `LevelVerifyDefault` (fields per the
    interfaces block above: Enabled bool, MaxIterations int32, OnExhaustion string) and
    `ParseVerifyLevelDefaults(s string) (map[string]LevelVerifyDefault, error)` mirroring
    ParsePricingOverrides' exact structure: strings.TrimSpace; empty/"{}" fast-path to an
    empty map; json.Unmarshal with a wrapped "verify level defaults: invalid JSON: %w"
    error; then a validation loop. Unlike pricing (open model-ID keyspace), the level
    keyspace is CLOSED — reject any key outside task|plan|phase|milestone|project with the
    key in the error string; reject MaxIterations < 0; reject OnExhaustion outside
    ""|escalate|requireApproval (matching the kubebuilder enum on VerificationSpec). Error
    strings name the level + offending value so the operator's fix is mechanical (the
    pricing.go doc-comment convention). Doc comment explains the type's pkg/dispatch home
    (same D-C1 provider-firewall reason as PriceOverride) and that this is the transport
    schema for the chart's subagent.verify.levels map (D-01).

    RED first: write the test table, watch it fail to compile/pass, then implement.
  </action>
  <verify>
    <automated>go test ./pkg/dispatch/... -run TestParseVerifyLevelDefaults -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/dispatch/... -run TestParseVerifyLevelDefaults -count=1` exits 0 with >= 6 subtests
    - `grep -c "func ParseVerifyLevelDefaults" pkg/dispatch/verify_defaults.go` == 1
    - Behavior tests 1-6 above all pass as written
    - `go vet ./pkg/dispatch/...` exits 0
  </acceptance_criteria>
  <done>Malformed/out-of-range per-level config is rejected with a mechanical error; valid config round-trips to a typed map.</done>
</task>

<task type="auto">
  <name>Task 2: Manager wiring + VerifyDefaults on both Deps + verificationEnabledForLevel helper</name>
  <files>cmd/manager/main.go, internal/controller/dispatch_helpers.go, internal/controller/task_controller.go, internal/controller/verification_enabled_unit_test.go</files>
  <read_first>
    - cmd/manager/main.go:140-260 (flag declarations, the --pricing-overrides-json StringVar + fail-fast validation at :244-249, the envOrDefault reads at :186-230, and the two reconciler-Deps constructions at :455-480 and :560-580 where VerifierImage is assigned at :464/:569)
    - internal/controller/dispatch_helpers.go:142-260 (ProviderDefaults struct + PlannerReconcilerDeps incl. VerifierImage at :231-237), :356-414 (projectLevelVerificationDefault + ResolveVerificationSpec)
    - internal/controller/task_controller.go:110-165 (TaskReconcilerDeps — where the new VerifyDefaults field lands beside VerifierImage at :156-161)
    - internal/controller/dispatch_helpers_loop_policy_test.go:1-40 (the standalone unit-test file convention — own func TestX(t *testing.T), NOT a Ginkgo spec)
  </read_first>
  <action>
    cmd/manager/main.go: (1) add `verifierModel := envOrDefault("TIDE_VERIFIER_MODEL", "")`
    immediately after the TIDE_VERIFIER_IMAGE read at :219, with a doc comment stating that
    empty keeps today's borrow-the-level-executor-model behavior (D-02) and the chart's
    subagent.verify.model supplies it (plan 53-05); (2) declare
    `flag.StringVar(&verifyLevelsJSON, "verify-levels-json", "", ...)` beside
    --pricing-overrides-json, help text naming Helm subagent.verify.levels; (3) after
    flag.Parse, validate with `pkgdispatch.ParseVerifyLevelDefaults(verifyLevelsJSON)` in
    the same fail-fast block shape as pricing: on error `fmt.Fprintf(os.Stderr, "invalid
    --verify-levels-json: %v\n", err); os.Exit(1)`. Keep the PARSED map (unlike pricing's
    validate-and-discard) — it populates VerifyDefaults.Levels; (4) construct
    `controller.VerifyDefaults{Image: verifierImage, Model: verifierModel, Levels: parsed}`
    once and assign it onto BOTH Deps literals (:464 region and :569 region), mirroring the
    existing VerifierImage dual-wiring (RESEARCH Assumption A2 — if only one Deps gets it,
    one dispatch tier silently loses chart enablement).

    internal/controller/dispatch_helpers.go: declare `VerifyDefaults` struct (Image, Model
    string; Levels map[string]pkgdispatch.LevelVerifyDefault) beside ProviderDefaults with a
    doc comment mirroring ProviderDefaults'; add a `VerifyDefaults VerifyDefaults` field to
    PlannerReconcilerDeps beside VerifierImage. Implement
    `verificationEnabledForLevel(project *tideprojectv1alpha3.Project, level string,
    chartDefaults VerifyDefaults) bool` immediately beside ResolveVerificationSpec (D-04's
    placement instruction): return true when projectLevelVerificationDefault(project, level)
    != nil (authored Project-scope entry = explicitly ON — operator intent on the CR
    outranks the install default); else return chartDefaults.Levels[level].Enabled when the
    level key is present; else false. Doc comment states the precedence and that this phase
    deliberately has no authored-but-disabled knob (CONTEXT Deferred: per-Project opt-out).

    internal/controller/task_controller.go: add `VerifyDefaults VerifyDefaults` to
    TaskReconcilerDeps beside VerifierImage (:156-161). No dispatch-site behavior change in
    this plan — the AND gates land in 53-06.

    internal/controller/verification_enabled_unit_test.go (NEW file, own TestX entry point —
    Phase 51-03 lesson: a -run/-ginkgo.focus filter against the shared TestControllers suite
    vacuously passes): table-driven TestVerificationEnabledForLevel covering (a) authored
    Project.Spec.Verification.Task = &VerificationSpec{} + chart task.enabled=false → true;
    (b) nil authored + chart task.enabled=true → true; (c) nil authored + chart
    task.enabled=false → false; (d) nil authored + level absent from chart map → false;
    (e) nil project + chart enabled=true → true (chart tier still applies); (f) each of the
    5 level strings routes to its own VerificationDefaults pointer field.
  </action>
  <verify>
    <automated>go test ./pkg/dispatch/... ./internal/controller/... -run 'TestParseVerifyLevelDefaults|TestVerificationEnabledForLevel' -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/... -run TestVerificationEnabledForLevel -count=1 -v` exits 0 and lists >= 6 subtests (genuinely executed, not vacuous)
    - `grep -c "VerifyDefaults" cmd/manager/main.go` >= 3 (construction + two Deps assignments)
    - `grep -c "verify-levels-json" cmd/manager/main.go` >= 2 (flag declaration + fail-fast block)
    - Running the manager binary with an invalid flag value exits non-zero: `go run ./cmd/manager --verify-levels-json='{bad' 2>&1 | grep -c "invalid --verify-levels-json"` == 1 (command exits before manager start)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Manager fail-closes on bad verify config at boot; both reconciler tiers carry chart verify defaults; the single enablement chokepoint exists with proven precedence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values → manager CLI flag | operator-influenced structured JSON crosses into manager startup config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Tampering | --verify-levels-json parsing | mitigate | ParseVerifyLevelDefaults fail-closed at startup (ASVS V5): reject malformed JSON, unknown level keys, negative maxIterations, unknown onExhaustion — os.Exit(1), mirroring ParsePricingOverrides exactly |
| T-53-04 | Elevation of Privilege | verificationEnabledForLevel default path | mitigate | absent authored AND absent chart config resolves to OFF (D-04's stated precedence) — misconfiguration can never silently enable spend |
| T-53-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test ./pkg/dispatch/... ./internal/controller/... -count=1` (both new unit files green in their packages)
- `go build ./...` and `make lint` clean
</verification>

<success_criteria>
- Fail-closed parse proven by tests for every rejection class
- Precedence (authored > chart > off) proven by a genuinely-executing unit test in its own file
- VerifyDefaults reaches both reconciler Deps from one construction site in main.go
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-02-SUMMARY.md` when done.
</output>
