---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 10
type: execute
wave: 3
depends_on: ["53-03", "53-06"]
files_modified:
  - internal/controller/task_controller.go
  - internal/controller/artifact_push.go
  - internal/controller/artifact_push_test.go
  - internal/controller/task_findings_push_test.go
  - cmd/manager/main.go
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "A Task reaching VerifyHalted triggers a findings artifact push at its verdict-final transition — even while ConditionVerifyHalt freezes all dispatch project-wide (the push is a git write of already-computed evaluator output, not a dispatch action)"
    - "A verifier-approved Task (markVerifiedSucceeded) stages its findings at the verdict-final seam without waiting for a later boundary push"
    - "The trigger is edge-gated: once a push Job carries the task's staging entry (stagedEnvelopesAnnotation), subsequent reconciles of the halted task create no new Jobs and no requeue churn"
    - "The just-finalized task's entry rides the push even when the informer cache has not yet observed the status patch (ensure-entry union — no missed-entry frozen state)"
  artifacts:
    - path: internal/controller/task_controller.go
      provides: "verdict-final findings-push trigger at haltVerify + markVerifiedSucceeded, plus the VerifyHalted terminal-arm retry"
      contains: "triggerArtifactPush"
    - path: cmd/manager/main.go
      provides: "TaskReconcilerDeps.TidePushImage wiring (same tidePushImage local ProjectReconciler gets at main.go:454)"
      contains: "TidePushImage"
    - path: internal/controller/task_findings_push_test.go
      provides: "proof: VerifyHalted → push Job carrying the task entry while ConditionVerifyHalt=True; edge-gating; busy-race retry; nil-evaluation poison guard"
      contains: "ConditionVerifyHalt"
  key_links:
    - from: internal/controller/task_controller.go
      to: internal/controller/artifact_push.go
      via: "triggerArtifactPush called at the Task verdict-final seam with r.Deps.TidePushImage / r.sharedPVCName() / r.Deps.HelmProviderDefaults"
      pattern: "triggerArtifactPush"
    - from: internal/controller/task_controller.go
      to: internal/controller/push_helpers.go
      via: "stagedEnvelopesAnnotation read-back as the carried-entry edge gate"
      pattern: "stagedEnvelopesAnnotation"
---

<objective>
Close the plan-check BLOCKER: no code path has ever triggered an artifact push when a Task
transitions to VerifyHalted. triggerArtifactPush/maybeTriggerBoundaryPush fire only from
the milestone/phase/plan/project controllers, and checkVerifyHalt freezes ALL project-wide
dispatch during a VerifyHalt with 30s park-and-return (dispatch_helpers.go:788-817) — so no
other level's boundary push fires either. The findings only become browsable AFTER
`tide resume`, which is precisely the decision the 53-08 findings view exists to inform.
Plan 53-03 ships the collector predicate + allowlist; THIS plan ships the trigger that
fires the push at the Task verdict-final seam.

Design constraints (from the plan-check verdict — binding):
- This is a git write of already-computed evaluator output, NOT a dispatch action. It must
  NOT be gated by checkVerifyHalt/checkDispatchHolds — triggerArtifactPush carries no hold
  checks by construction and the new call sites all sit AFTER the dispatch gates.
- Reuse triggerArtifactPush + the existing push-Job machinery: deterministic Job name
  `tide-push-<project.UID>` single-flight, gitWriterInFlightCount family, Phase-34 flock
  push lease. No new push mechanism, no second writer class.
- Edge-gated: fires on the verdict-final transition, not every reconcile — the
  stagedEnvelopesAnnotation carried-entry check is the gate.

Purpose: the 53-08 "View findings" disclosure renders real data for the exact frozen-halt
scenario it serves, before the operator decides `tide resume`.
Output: TidePushImage on TaskReconcilerDeps + main.go wiring, the verdict-final trigger
with ensure-entry union, and a proving test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-RESEARCH.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verdict-final findings-push trigger (Deps field + main.go wiring + seam calls + terminal-arm retry)</name>
  <files>internal/controller/task_controller.go, internal/controller/artifact_push.go, internal/controller/artifact_push_test.go, cmd/manager/main.go</files>
  <read_first>
    - internal/controller/artifact_push.go (FULL file — collectStageEnvelopes incl. 53-03's task predicate, triggerArtifactPush's guard chain + single-flight Get at :217-226)
    - internal/controller/task_controller.go:2596-2700 (haltVerify's two-step patch shape + markVerifiedSucceeded), :360-398 (the terminal short-circuit arms — the Failed branch's setFailureHaltIfNeeded-at-terminal precedent the VerifyHalted retry arm mirrors), :100-165 (TaskReconcilerDeps — the VerifierImage field-comment idiom the new field copies), :189-191 (sharedPVCName)
    - internal/controller/push_helpers.go:142-151 (stagedEnvelopesAnnotation contract), :355 (where the annotation is stamped)
    - internal/controller/project_controller.go:2366-2406 (isStaleArtifactPush — the strict-superset supersede machinery that independently heals a partial-map push; cite, don't duplicate)
    - internal/controller/plan_controller.go:270, :973-978 (the existing triggerArtifactPush call shape + the errGitWriterBusy requeue idiom)
    - cmd/manager/main.go:440-460 (tidePushImage local + planner-Deps wiring), :561-582 (the TaskReconcilerDeps literal the new field lands in)
  </read_first>
  <action>
    Four pieces, all reusing existing machinery — no new push mechanism:

    (1) Deps + wiring: add `TidePushImage string` to TaskReconcilerDeps
    (task_controller.go, beside VerifierImage, same comment idiom: "image ref for the
    tide-push container; empty = trigger skipped — mirrors ProjectReconciler's field").
    Wire it in main.go's TaskReconcilerDeps literal (:561-582) with the SAME
    `tidePushImage` local ProjectReconciler already receives (:454). Empty stays a safe
    no-op via triggerArtifactPush's existing empty-image skip.

    (2) Ensure-entry union in artifact_push.go: extend collectStageEnvelopes /
    triggerArtifactPush with an optional ensured-entries mechanism (exact shape is
    executor's discretion — e.g. a variadic `ensure ...*tideprojectv1alpha3.Task` or
    pre-built entry strings, deduped by UID against the listed set) so a JUST-patched Task
    appears in the staging map even when the informer cache has not yet observed the status
    patch. The four existing planner-tier callers pass nothing and are behaviorally
    unchanged. Without this, a stale List at the seam pushes a map missing the task and the
    frozen project never retries — the exact blocker class this plan closes.

    (3) The trigger helper in task_controller.go (suggest `maybeTriggerTaskFindingsPush`;
    naming is discretion): eligibility = the 53-03 collector predicate (verdict-final phase
    AND `LoopStatus.LastEvaluation != nil` — NEVER trigger for a nil-evaluation halt; a
    task entry whose envelope dir lacks findings.json hard-fails the ENTIRE cumulative push
    in tide-push, cmd/tide-push/main.go:1242-1252). Then Get the deterministic push Job
    `tide-push-<project.UID>`:
      - exists AND its stagedEnvelopesAnnotation contains this task's `<uid>:task/<name>`
        entry → done, edge complete, no requeue;
      - exists WITHOUT the entry → signal retry to the caller (RequeueAfter ~5s, mirroring
        the errGitWriterBusy requeue at plan_controller.go:974-978); the ProjectReconciler
        isStaleArtifactPush supersede path independently heals this once the in-flight Job
        completes;
      - absent → triggerArtifactPush(ctx, r.Client, r.Scheme, project, "task",
        r.Deps.TidePushImage, r.sharedPVCName(), r.Deps.HelmProviderDefaults) with the
        ensure-entry for this task.
    Trigger errors are non-fatal: log and continue, never fail the verdict-final patch
    (mirrors setVerifyHaltIfNeeded's tolerated-error posture, level_status.go:228-230).

    (4) Call sites — exactly three, all post-dispatch-gate (do NOT add any
    checkVerifyHalt/checkDispatchHolds gating; the push is a git write, not a dispatch):
      (a) haltVerify, AFTER its second status patch lands — covers BOTH exhaustVerifyLoop
          legs in one seam: escalate→VerifyHalted (the frozen-halt blocker case) and
          requireApproval→AwaitingApproval (ESC-02 park — the operator needs findings to
          decide `tide approve`, same scenario class);
      (b) markVerifiedSucceeded, after its Status patch — immediate staging at the
          approved verdict-final, no waiting for the next boundary push;
      (c) the VerifyHalted terminal short-circuit arm (task_controller.go:372-374) —
          mirroring the Failed branch's setFailureHaltIfNeeded-at-terminal precedent
          directly below it: re-run the helper with the annotation edge-gate, converting
          the arm's ctrl.Result{} into RequeueAfter ONLY while the entry is not yet
          carried by any push Job. Once carried, steady state — no churn. Never an
          unconditional every-reconcile trigger.

    Extend artifact_push_test.go only as needed for the ensure-entry union (dedup by UID;
    existing-caller behavior unchanged). The reconciler-level proof is Task 2's file.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run TestArtifactPush -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test ./internal/controller/... -run TestArtifactPush -count=1` exits 0 (existing four-caller behavior preserved)
    - `grep -c 'triggerArtifactPush' internal/controller/task_controller.go` >= 1
    - `grep -c 'TidePushImage' cmd/manager/main.go` shows the TaskReconcilerDeps literal gained the wiring (count increased by exactly 1 vs pre-edit)
    - `grep -cE 'checkVerifyHalt|checkDispatchHolds' internal/controller/artifact_push.go` == 0 (no hold-gating added to the push path)
    - `grep -c 'stagedEnvelopesAnnotation' internal/controller/task_controller.go` >= 1 (the carried-entry edge gate exists)
  </acceptance_criteria>
  <done>A Task's verdict-final transition stages + pushes its findings through the existing push machinery, ungated by dispatch holds, edge-gated by the carried-entry annotation, with the just-patched task guaranteed in the map.</done>
</task>

<task type="auto">
  <name>Task 2: Proving tests — push fires while ConditionVerifyHalt is True</name>
  <files>internal/controller/task_findings_push_test.go</files>
  <read_first>
    - internal/controller/artifact_push_test.go (TestArtifactPush_TriggerCreatesJobWithStageEnvelopes + TestArtifactPush_ParkedMilestoneTriggersAndRequeues — the fake-client fixture/assertion idiom to copy)
    - internal/controller/verify_halt_test.go (the ConditionVerifyHalt fixture idiom)
  </read_first>
  <action>
    NEW file internal/controller/task_findings_push_test.go with its OWN Test entries
    (never a TestControllers filter — Phase 51-03 lesson), fake-client idiom from
    artifact_push_test.go:

    (a) THE BLOCKER PROOF: a contract-bearing Task at LevelPhaseVerifyHalted with
        LoopStatus.LastEvaluation set, on a Project carrying ConditionVerifyHalt=True →
        invoking the trigger path creates the deterministic push Job AND its
        stagedEnvelopesAnnotation contains `<taskUID>:task/<name>` — asserted WHILE the
        halt condition is True (dispatch frozen project-wide, push still fires).
    (b) Edge-gate: with the carrying Job present, invoking the trigger path again creates
        no second Job and signals no retry (steady state — no churn while halted).
    (c) Busy race: pre-create a push Job WHOSE annotation lacks the task's entry → the
        helper signals retry (non-zero RequeueAfter); delete/complete that Job → the next
        invocation creates the carrying Job.
    (d) Poison guard: a VerifyHalted task with NIL LoopStatus.LastEvaluation (the
        VerifierEnvelopeUnreadable shape — verifier may never have written findings.json)
        → the trigger does NOT fire and no Job is created.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestTaskFindingsPush -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/... -run TestTaskFindingsPush -count=1` exits 0 with all four subtests (a)-(d) visible in -v output
    - Test (a) asserts the push Job exists while `meta.IsStatusConditionTrue(project.Status.Conditions, ConditionVerifyHalt)` is true in the same fixture state
    - `go test ./internal/controller/... -count=1` full package green (no regression in the 51/52 verify-loop suites)
  </acceptance_criteria>
  <done>The frozen-halt findings push, its edge-gating, the busy-race retry, and the nil-evaluation poison guard are each pinned by an automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → run branch (git) during project-wide halt | the manager creates a credentialed tide-push Job while all dispatch is frozen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-23 | Denial of Service | push-Job churn from re-triggering while halted | mitigate | edge-gated on the stagedEnvelopesAnnotation carried-entry check + deterministic single-flight Job name + 37-02 clean-tree skip; test (b) pins no-churn |
| T-53-24 | Elevation of Privilege | pushing during a project-wide VerifyHalt (hold bypass concern) | accept | the hold family gates LLM dispatch (spend); tide-push is a git write of already-computed evaluator output through the EXISTING lease/serialization (Phase-34 flock, D-B5 single-flight) — same class as parked-milestone artifact pushes at AwaitingApproval; no new credential surface |
| T-53-25 | Denial of Service | a task entry without findings.json on the PVC hard-fails the ENTIRE cumulative push (tide-push fail-closed, cmd/tide-push/main.go:1242-1252) | mitigate | trigger AND collector both require LoopStatus.LastEvaluation != nil (parsed-verdict proxy for a written findings.json); test (d) |
| T-53-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- `go build ./... && go test ./internal/controller/... -count=1` green
- `make lint` clean
</verification>

<success_criteria>
- The 53-08 findings disclosure's primary scenario (VerifyHalted, project frozen) has real data on the run branch without `tide resume` — the checker's BLOCKER closed at the root
- Zero new push mechanisms; four existing triggerArtifactPush callers behaviorally unchanged
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-10-SUMMARY.md` when done.
</output>
