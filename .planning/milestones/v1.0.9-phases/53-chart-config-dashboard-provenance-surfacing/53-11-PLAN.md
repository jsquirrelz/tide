---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 11
type: execute
wave: 3
depends_on: ["53-03"]
files_modified:
  - cmd/tide-langgraph-verifier/verifier/__main__.py
  - cmd/tide-langgraph-verifier/verifier/envelope.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_findings_artifact.py
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "Every verifier run that assembles a parseable verdict (APPROVED/REPAIRABLE/BLOCKED — including gate-command-dominance rewrites and the empty-commands BLOCKED arm) writes findings.json into the SAME envelope directory as out.json"
    - "Degraded/no-verdict paths (envelope-missing, envelope-invalid, unsupported-vendor, agent-error, verify-context absent) write NO findings.json — exactly the paths where the controller never records LastEvaluation"
    - "A findings.json write failure never masks the verdict relay: out.json and the termination stub still land unchanged and main() returns the same exit code"
    - "findings.json content is the FULL gate_decision document (verdict + summary + findings[] with severity/confidence), schema-valid against the Phase-49 GateDecision pydantic model and identical to out.json's verdict object"
  artifacts:
    - path: cmd/tide-langgraph-verifier/verifier/envelope.py
      provides: "write_findings(path, verdict) helper beside write_envelope_out (same mkdir/0o644 idiom)"
      contains: "write_findings"
    - path: cmd/tide-langgraph-verifier/verifier/__main__.py
      provides: "guarded findings.json write gated on the exact verdict_out-is-not-None condition"
      contains: "findings.json"
    - path: cmd/tide-langgraph-verifier/verifier/tests/test_findings_artifact.py
      provides: "pytest proof: verdict paths write, degraded paths don't, OSError never masks the relay, golden round-trip"
      contains: "findings.json"
  key_links:
    - from: cmd/tide-langgraph-verifier/verifier/__main__.py
      to: cmd/tide-langgraph-verifier/verifier/envelope.py
      via: "envelope.write_findings called iff verdict_out is not None"
      pattern: "write_findings"
    - from: cmd/tide-langgraph-verifier/verifier/__main__.py
      to: cmd/tide-push/main.go
      via: "findings.json landed in /workspace/envelopes/<taskUID>/ is the file tide-push's fail-closed task-kind stage consumes (main.go:1242-1252)"
      pattern: "findings"
---

<objective>
Close the execution-surfaced gap 53-03 verified and flagged (53-03-SUMMARY §Next Phase
Readiness): `verifier/__main__.py` never writes `findings.json` — only `out.json` +
the termination stub — while `tide-push` hard-fails the ENTIRE cumulative staging run
(`artifact-stage-failed`, exitGenericFail) when a task envelope dir lacks it
(cmd/tide-push/main.go:1242-1252, the EVAL-05c / T-49-04-02 fail-closed guard). 53-03
(landed) emits a task-kind staging entry whenever `LastEvaluation` is recorded; 53-10
(this wave) adds the push trigger. Without a verifier-side writer, the FIRST verdict-final
task on a verify-enabled cluster poisons every subsequent push — worse than the original
plan-check blocker.

Alignment invariant (binding): findings.json presence on disk must be a superset of the
controller's staging predicate. `applyLoopStatus` (task_controller.go:2513) records
`LastEvaluation` iff `out.Verdict != nil`, reading out.json through the role-aware
`ReadVerifierOut` relay (task_controller.go:2944-2951 — the Phase-51 live-proof graft
that reads the VERIFIER's own envelope, not the executor's). The verifier sets out.json's
`verdict` key iff `verdict_out is not None` (env.verify present → _assemble_verdict ran).
Gating the findings write on that EXACT condition makes disk presence track the controller
predicate 1:1: every parseable verdict (APPROVED / REPAIRABLE / BLOCKED, including
dominance rewrites and the empty-commands BLOCKED arm) writes it; every degraded path
(_fail's envelope-missing / envelope-invalid / unsupported-vendor / agent-error — which
emit an exit_code=1 EnvelopeOut with NO verdict key) writes nothing, and correspondingly
never records LastEvaluation controller-side, so taskFindingsStageable stays false and
tide-push never probes that dir. The sole permitted divergence is a guarded OSError on
the findings write itself — a missing findings.json after a successfully relayed verdict
is precisely the bug tide-push's fail-closed guard exists to catch; do NOT soften it.

Purpose: 53-10's trigger and 53-03's collector become safe to run against a real verifier
— the findings pipeline's producer end finally exists (OBS-04's "staged findings
browsable through gitfetch" claim, D-07).
Output: write_findings helper, the guarded main() wiring, and a pytest file proving both
sides of the alignment invariant plus the failure-isolation contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: write_findings helper + verdict-gated main() wiring (RED first)</name>
  <files>cmd/tide-langgraph-verifier/verifier/envelope.py, cmd/tide-langgraph-verifier/verifier/__main__.py, cmd/tide-langgraph-verifier/verifier/tests/test_findings_artifact.py</files>
  <read_first>
    - cmd/tide-langgraph-verifier/verifier/__main__.py (FULL — main()'s verdict_out assembly at :206-225, _out_path_for, the _fail path)
    - cmd/tide-langgraph-verifier/verifier/envelope.py:140-209 (write_envelope_out — the mkdir-parents + write_bytes + os.chmod 0o644 writer idiom write_findings copies)
    - cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py:227-341 (the entrypoint.main injection idiom: envelope_in_dict fixture with provider vendor "langgraph" + verify dict, fake build_model/run_agent_fn, TIDE_WORKTREE_DIR/TIDE_GATE_COMMAND monkeypatch leak-guards — copy this shape verbatim)
    - cmd/tide-langgraph-verifier/verifier/tests/conftest.py (envelope_in_dict / fixture_worktree / gate_decision_dict fixtures — consume, never rebuild)
  </read_first>
  <behavior>
    - Test 1 (verdict path): env.verify present with green commands + fake LLM APPROVED → after main() returns 0, findings.json exists in the SAME directory as out.json; json.loads parses; verdict.GateDecision.model_validate accepts it; the dict equals out.json's "verdict" object exactly (same document, no drift)
    - Test 2 (dominance rewrite writes too): one red command among green + fake LLM APPROVED → findings.json written, verdict REPAIRABLE/BLOCKED, carries the gate-command blocker finding with exit_code in evidence — proving rewritten verdicts write identically to clean ones
    - Test 3 (empty-commands BLOCKED arm writes too): verify present with commands [] → findings.json written with verdict BLOCKED and findings [] — a parseable verdict is a parseable verdict, however fail-closed
  </behavior>
  <action>
    RED first: create tests/test_findings_artifact.py with the three behavior tests above
    (module docstring naming the 53-03-surfaced gap), run the suite, observe them fail
    because nothing writes findings.json. Then:

    (1) envelope.py: add `write_findings(path, *, verdict: dict[str, Any]) -> None`
    directly after write_envelope_out, mirroring its exact writer idiom (Path target,
    parent.mkdir(parents=True, exist_ok=True), write_bytes(json.dumps(...)), chmod 0o644).
    The document written is the verdict dict VERBATIM — the full gate_decision
    (verdict/summary/findings[]), already shaped by GateDecision.model_dump(by_alias=True)
    at the call site; write_findings adds and drops nothing. Docstring states the contract:
    this is the FULL findings artifact per the Phase-49 size×locality rule — PVC +
    run-branch destined (tide-push stages it to .tide/planning/task/<name>/findings.json),
    NEVER etcd; the bounded projection lives in the TerminationStub
    (gateDecision/findingsCount/highSeverityCount), so no truncation loop here.

    (2) __main__.py: add a module-level FINDINGS_FILENAME = "findings.json" (or a
    _findings_path_for(in_path) sibling to _out_path_for — executor's discretion) and, in
    main(), when `verdict_out is not None`, write it into the same envelope dir as
    out.json (`Path(resolved_in_path).parent`) — the dir tide-push resolves as
    workspace/envelopes/<taskUID>/. Guard the call: try/except OSError → single stderr
    line ("findings.json write failed: ..."), then continue — write_envelope_out and
    write_termination_stub MUST still run and the return value MUST stay 0. Place the
    guarded write immediately BEFORE write_envelope_out so the relay writes remain the
    function's final acts. Do NOT touch _fail (degraded paths write no findings.json by
    construction — that is the alignment invariant, per the objective) and do NOT change
    write_envelope_out/write_termination_stub signatures or ordering.

    GREEN: all three tests pass. Commit RED then GREEN per the tdd task protocol.
  </action>
  <verify>
    <automated>make test-langgraph-verifier</automated>
  </verify>
  <done>Every parseable-verdict path lands findings.json beside out.json with the full schema-valid gate_decision document; existing suite green (no envelope/verdict/agent regressions).</done>
</task>

<task type="auto">
  <name>Task 2: Alignment + failure-isolation + golden round-trip proofs</name>
  <files>cmd/tide-langgraph-verifier/verifier/tests/test_findings_artifact.py</files>
  <read_first>
    - cmd/tide-langgraph-verifier/verifier/tests/test_agent.py:170-250 (the _fail-path test idiom: missing in_path / bad vendor / raising run_agent_fn driving entrypoint.main to exit 1 with a structured stub)
    - cmd/tide-langgraph-verifier/verifier/tests/conftest.py:44 (GOLDEN_FIXTURE — pkg/dispatch/testdata/gate_decision_golden.json, the shared Go↔Python proof)
  </read_first>
  <action>
    Extend test_findings_artifact.py with the negative and contract halves:

    (a) No-verdict paths write NOTHING — one test per path, each asserting
    `not (envelope_dir / "findings.json").exists()` after main():
      - env.verify absent entirely (verdict_out is None; main() returns 0, out.json has
        no "verdict" key);
      - envelope-missing (nonexistent in_path → _fail, exit 1);
      - unsupported-vendor (provider.vendor "anthropic" → _fail, exit 1);
      - agent-error (run_agent_fn raises → _fail, exit 1).
    Each test's docstring states WHY absence is correct: these are exactly the paths where
    the controller's applyLoopStatus (LastEvaluation iff out.Verdict != nil, via the
    role-aware ReadVerifierOut relay) never records an evaluation, so taskFindingsStageable
    excludes the Task and tide-push never probes the dir.

    (b) OSError isolation — monkeypatch.setattr(envelope, "write_findings", <raiser>)
    (main() resolves it through the module attribute at call time, so the patch takes) on
    an otherwise-green verdict path → main() still returns 0, out.json carries the full
    verdict object, the termination stub carries gateDecision/findingsCount — the relay is
    never masked. Assert findings.json is absent (the divergence tide-push's fail-closed
    guard exists to catch — cite cmd/tide-push/main.go:1242-1252 in the docstring, and
    state that the consumer is deliberately NOT softened).

    (c) Golden round-trip — load conftest.GOLDEN_FIXTURE (the shared
    gate_decision_golden.json both languages prove against), write_findings it to a tmp
    path, re-read with json.loads → field-for-field equal to the golden document (the
    writer neither adds, drops, nor reorders semantic content), and
    GateDecision.model_validate accepts the re-read dict.
  </action>
  <verify>
    <automated>make test-langgraph-verifier</automated>
  </verify>
  <done>Both sides of the alignment invariant and the never-mask-the-relay contract are each pinned by an automated pytest; the golden fixture proves the written artifact is the unaltered Phase-49 wire document.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifier pod → PVC envelope dir → run branch | evaluator-authored findings.json crosses from the verifier container to the shared workspace PVC, then to the git run branch via tide-push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-26 | Tampering | findings.json content authored by the evaluator | accept | same trust class as out.json at the identical envelope seam (already trusted — the verifier writes both); dashboard ArtifactViewer renders it as JSON text through React escaping (53-08's T-53-19 covers the render side) |
| T-53-27 | Denial of Service | unbounded findings.json size | accept | the FULL artifact is intentionally unbounded-ish per the Phase-49 size×locality rule — PVC + run branch only, NEVER etcd; the bounded projection is the 4KB TerminationStub (gateDecision + two counts) |
| T-53-28 | Denial of Service | missing findings.json after a relayed verdict poisons the ENTIRE cumulative push (tide-push fail-closed, cmd/tide-push/main.go:1242-1252) | mitigate | this plan IS the root fix (writer gated on the exact verdict_out condition); the residual guarded-OSError arm is deliberately left for tide-push's fail-closed guard to surface — the consumer is never softened; Task 2(b) pins it |
| T-53-SC | Tampering | package installs | accept | zero new packages — pydantic/pytest already hash-locked in requirements*.txt |
</threat_model>

<verification>
- `make test-langgraph-verifier` green (full pytest suite: new file + zero regressions in test_envelope/test_verdict/test_agent/test_tools)
- `grep -c "write_findings" cmd/tide-langgraph-verifier/verifier/__main__.py` >= 1 and `grep -c "def write_findings" cmd/tide-langgraph-verifier/verifier/envelope.py` == 1
- `grep -c "findings" cmd/tide-langgraph-verifier/verifier/tests/test_findings_artifact.py` >= 8 (both invariant halves covered)
</verification>

<success_criteria>
- The 53-03-surfaced staging poison is closed at the producer: a verdict-final task on a verify-enabled cluster stages cleanly through 53-10's trigger instead of hard-failing every subsequent push
- findings.json presence on disk tracks the controller's LastEvaluation predicate 1:1 on every code path, with the single OSError divergence intentionally caught downstream
- No change to out.json/stub semantics, exit codes, or the fail-closed consumer
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-11-SUMMARY.md` when done.
</output>
