---
phase: 52-per-level-looppolicy-parameterization
plan: 11
type: execute
wave: 6
depends_on: ["52-05", "52-09", "52-10"]
files_modified:
  - test/integration/kind/level_verify_worktree_test.go
autonomous: false
requirements: [ESC-01]

must_haves:
  truths:
    - "The worktree-checkout init container works against a REAL PVC + bare repo on a kind cluster — the one behavior envtest is structurally blind to (RESEARCH Pitfall 2, VALIDATION Manual-Only row)"
    - "The kind spec is non-billable: it proves init-container provisioning without requiring a real ANTHROPIC key (the main verifier container's verdict is out of the automated spec's scope)"
    - "The full live billable loop proof (plan-check re-plan + level-verify escalation) is gated behind an explicit operator checkpoint (Phase 51-08 runbook precedent)"
  artifacts:
    - path: "test/integration/kind/level_verify_worktree_test.go"
      provides: "kind spec asserting init-container worktree provisioning"
      contains: "worktree-checkout"
  key_links:
    - from: "test/integration/kind/level_verify_worktree_test.go"
      to: "podjob.BuildJobSpec worktree init container"
      via: "a real Job on the kind cluster with the checkout init container"
      pattern: "WorktreeCheckoutImage"
---

<objective>
Close the phase's envtest-blind gap live: prove the 52-05 worktree-provisioning mechanism against a real kind PVC (the exact "test coverage vs. live proof" gap Phase 51 hit with the unwired VerifierImage), then checkpoint for the operator-approved billable live proof of the full loops.

Task 1 is deliberately non-billable: it exercises ONLY the init container (git checkout at run-branch tip) — the LangGraph main container may fail on the dummy key without invalidating the proof, because init containers complete before the main container starts.
Output: kind spec + operator checkpoint with runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (Pitfall 2 warning signs; Wave 0 Gaps — the live kind proof requirement)
@.planning/phases/52-per-level-looppolicy-parameterization/52-VALIDATION.md  (Manual-Only Verifications table)
</context>

<tasks>

<task type="auto">
  <name>Task 1: kind spec — init-container worktree provisioning against a real PVC</name>
  <files>test/integration/kind/level_verify_worktree_test.go</files>
  <read_first>
    - test/integration/kind/ directory listing + the Phase-51 concurrency spec (grep for the 51-08 esc04/concurrency spec file) — the harness idiom for building Jobs, PVC access, and namespace fixtures on the tide-test cluster
    - Makefile `test-int-kind-prep` target (:168-210 — which images are pre-built/pre-loaded; tide-push:test is already loaded, so the checkout image is available without new build plumbing)
    - internal/dispatch/podjob/jobspec.go (52-05's WorktreeCheckoutImage/WorktreeBranch composition — the spec drives BuildJobSpec directly or applies an equivalent Job manifest)
  </read_first>
  <action>
    Write a kind-layer test (matching the package's existing plain-Go/Ginkgo convention — read the neighbors first) that: (1) seeds a bare repo with one commit on a run branch into the cluster's shared workspace PVC (reuse whatever fixture path existing kind specs use to populate the PVC — e.g. a setup Job or the harness's existing repo-seeding helper); (2) builds a verifier-shaped Job via podjob.BuildJobSpec with Kind JobKindVerifier, a synthetic ParentObj UID, Level "phase", WorktreeCheckoutImage set to the loaded tide-push:test image, WorktreeBranch = the seeded branch, and a MAIN container overridden to a trivial assertion command (or the stub image) so the spec never needs an LLM key — the assertion target is the INIT container, not the verdict; (3) waits for the init container `worktree-checkout` to terminate with exit 0 (pod status InitContainerStatuses), and (4) asserts via a follow-up inspection (a small assertion Job or exec) that `/workspace/worktrees/<uid>/` contains a checkout whose HEAD equals the seeded branch tip SHA and that `git branch --show-current` is empty (detached — no branch minted).

    Respect the constrained-VM recipe (CLAUDE.md): this runs inside the normal `make test-int` kind layer, no second cluster. Remember the MAKE_EXIT discipline: verify with the exit code AND `grep -nE '^--- FAIL|^FAIL\s'` on the log, never the Ginkgo summary alone.
  </action>
  <verify>
    <automated>make test-int 2>&1 | tee /tmp/52-11-test-int.log; echo "MAKE_EXIT=$?" && grep -nE '^--- FAIL|^FAIL\s' /tmp/52-11-test-int.log || true</automated>
  </verify>
  <acceptance_criteria>
    - MAKE_EXIT=0 AND zero `^--- FAIL` lines in the log (both gates, per CLAUDE.md)
    - The new spec's assertions include: init container exit 0, HEAD SHA equality with the seeded tip, detached-HEAD (empty branch name)
    - The spec requires no ANTHROPIC key (grep the spec for ANTHROPIC — the main container is stubbed/overridden)
  </acceptance_criteria>
  <done>Worktree provisioning proven on a real PVC, non-billable, inside the standard kind layer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live billable loop proof — plan-check re-plan + level-verify escalation (operator-gated)</name>
  <files>none (live verification only — no production-code edits in this task)</files>
  <read_first>
    - CLAUDE.md operating notes (single-node kind OOM discipline: delete-recreate-prewarm; never run the acceptance cluster concurrently; real key at ~/.tide/anthropic.key)
    - .planning/phases/51-the-task-loop/51-08-SUMMARY.md (the live-proof runbook precedent this checkpoint mirrors)
    - .planning/phases/52-per-level-looppolicy-parameterization/52-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    Runs AFTER all autonomous Phase-52 work. The executor prepares the cluster (delete-recreate-prewarm kind; make test-int-kind-prep to build/load dev-head images; confirm ~/.tide/anthropic.key), applies the two fixture Projects described in how-to-verify, drives both loops, captures kubectl/manager-log proof artifacts, then PAUSES for the operator's billable-spend go-ahead and sign-off. No production code changes in this task.
  </action>
  <what-built>
    The complete Phase 52 loop set: plan-check re-plan loop (52-07/52-09), level-verify escalation at Phase/Milestone/Project (52-08/52-10), per-value onExhaustion semantics (52-06), and live-proven worktree provisioning (Task 1). Everything is envtest/kind-green; what remains unproven is the full BILLABLE loop against the real Anthropic API — the same class of live gate Phase 51 closed with its 51-08 runbook (which surfaced five stacked latent defects the green suites missed).
  </what-built>
  <how-to-verify>
    Operator-approved billable run on the kind-tide-test cluster (real key from ~/.tide/anthropic.key; follow the 51-08 SUMMARY runbook shape):

    1. Deploy dev-head images (make test-int-kind-prep) + the real-key tide-secrets into the test project namespace.
    2. Plan-check loop proof: apply a fixture Project whose Project.Spec.Verification.Plan default is Locked with a real gate command; drive a Plan whose first authored attempt is deliberately weak (the 51-08 red-fixture idiom) → observe in `kubectl get plan -o yaml` + manager logs: Verifying phase, tide-verifier-plan-*-1 Job, REPAIRABLE verdict, child-Task deletion, tide-plan-*-2 planner Job with the findings block in its prompt, then either APPROVED (loop closes) or exhaustion → the resolved escalation state.
    3. Level-verify proof: a fixture Project with a phase-level contract (Project.Spec.Verification.Phase) → after the phase's children succeed, observe the tide-verifier-phase-*-1 Job's init container provisioning the worktree, the gate command running for real in it, and a non-APPROVED verdict parking the phase at AwaitingApproval; `tide approve` resumes it to Succeeded with no second verifier Job.
    4. Confirm ESC-04 rails live: `kubectl get jobs -l tideproject.k8s/role=verifier` counts stay ≤ the concurrency cap during the run.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms the plan-check sequence: Verifying → tide-verifier-plan-*-1 → REPAIRABLE → child-Task deletion → tide-plan-*-2 with findings block → APPROVED or resolved escalation state
    - Operator confirms the level-verify sequence: worktree init container success → real gate-command run → AwaitingApproval park → tide approve → Succeeded with exactly one verifier Job
    - Verifier Job count stays within the concurrency cap throughout; proof artifacts (kubectl yaml / manager-log excerpts) + approximate spend recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms both live loop sequences and the rails observations above, with proof artifacts recorded in the SUMMARY.</human-check>
  </verify>
  <done>Both Phase-52 loops proven live on kind with operator sign-off, proof artifacts, and approximate spend recorded.</done>
  <resume-signal>Type "approved" with the observed outcomes (or paste failures/log excerpts) — billable spend requires your explicit go-ahead first; reply "skip live proof" to defer with the phase marked accordingly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kind spec → shared PVC | test Jobs write repo fixtures into the cluster's workspace volume |
| operator → billable API spend | the live proof spends real money and requires explicit human authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-35 | DoS (spend) | automated tests triggering billable LLM calls | mitigate | Task 1 stubs the main container (no key needed); all billable activity is behind the blocking human checkpoint |
| T-52-36 | Tampering | test fixtures leaking into non-test namespaces | mitigate | Standard kind-layer namespace isolation per the existing harness idiom; single-cluster rule from CLAUDE.md respected |
</threat_model>

<verification>
- `make test-int` MAKE_EXIT=0 + zero FAIL greps (the phase gate from 52-VALIDATION.md)
- `make lint` green
- Checkpoint resolved: live proof approved/recorded, or explicitly deferred by the operator
</verification>

<success_criteria>
- The envtest-blind worktree mechanism is proven on a real PVC without spend
- The billable end-to-end loop proof is operator-gated with a concrete runbook, mirroring the 51-08 precedent that caught what green suites missed
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-11-SUMMARY.md` when done.
</output>
