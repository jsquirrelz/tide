---
phase: 52-per-level-looppolicy-parameterization
plan: 09
type: execute
wave: 5
depends_on: ["52-07"]
files_modified:
  - internal/controller/plan_controller.go
  - internal/controller/plan_verify_loop_test.go
  - internal/controller/plan_verify_dispatch_test.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "A REPAIRABLE plan-check verdict drives exactly ONE findings-seeded fresh planner attempt at default maxIterations:1, then escalates (SC1, D-04)"
    - "The rejected attempt's child Tasks are deleted BEFORE the fresh planner dispatch — no stale-attempt Task ever dispatches, and the deletion is what unblocks reconcilePlannerDispatch's tasks-exist early-return (RESEARCH Pitfall 3)"
    - "A non-improving re-plan halts EARLY via severity-weighted stall detection, before consuming remaining iterations — proven at maxIterations:2 (D-05)"
    - "Plan-check's counter is LoopStatus.Iteration — quality re-plans only, distinct from WaveIntegration.Attempts (D-06)"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "repairOrHaltPlan (stall + boundary) + dispatchPlanRepair (delete-then-recreate) + attempt-aware reconcilePlannerDispatch"
      contains: "repairOrHaltPlan"
    - path: "internal/controller/plan_verify_loop_test.go"
      provides: "plain-Go stall-detection math tests mirroring task_verify_loop_test.go"
      contains: "Stall"
  key_links:
    - from: "internal/controller/plan_controller.go repairOrHaltPlan"
      to: "child Task deletion"
      via: "client.MatchingFields{taskPlanRefIndexKey: plan.Name} — the same list reconcilePlannerDispatch runs"
      pattern: "taskPlanRefIndexKey"
    - from: "internal/controller/plan_controller.go reconcilePlannerDispatch"
      to: "plan_planner.tmpl RepairFindings block (52-03)"
      via: "bounded findings annotation → prompt render field"
      pattern: "RepairFindings"
---

<objective>
Complete SC1: replace 52-07's marked conservative seam with `repairOrHaltPlan` — severity-weighted stall detection (D-05, scheme resolved: score = HighSeverityCount×10 + FindingsCount, must STRICTLY decrease), the MaxIterations boundary (counting the original attempt, Phase 51's `Attempt >= MaxIterations` semantics), and `dispatchPlanRepair` implementing the delete-then-recreate child-Task reconciliation (Assumption A2's recommended shape) with the D-04 findings-seeded fresh planner attempt.

Findings transport (discretion resolved): at repair time the verdict is in hand but the planner re-dispatch happens on a LATER reconcile (after child deletion completes) — findings persist across that gap as a BOUNDED single-current-iteration annotation on the Plan (`tideproject.k8s/replan-findings`, max 10 findings × 300-char summaries, gates-annotation precedent, cleared on consumption — NOT status, honoring LOOP-03's no-history rule). The existing `len(taskList.Items) > 0` early-return becomes the natural deletion barrier: the fresh planner Job only dispatches once the List is empty, which also kills the child-Task name-collision window RESEARCH warned about.

Output: the full re-plan loop, stall math unit tests, and Ginkgo RePlan specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (section: plan_controller.go points 5-7 — repairOrHalt/dispatchRepairAttempt/nextAttempt shapes; the applyLoopStatus-before-attempt-bump ordering)
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (§Child-Task reconciliation on re-plan; Pitfall 3; Open Question 3 — resolved: Iteration doubles as the planner attempt)
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md  (D-04, D-05, D-06)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repairOrHaltPlan + dispatchPlanRepair + attempt-aware planner dispatch</name>
  <files>internal/controller/plan_controller.go, internal/controller/plan_verify_loop_test.go</files>
  <behavior>
    Stall-score math (pure function, plain-Go tests written FIRST in plan_verify_loop_test.go, mirroring task_verify_loop_test.go's file shape):
    - score(findings=3, high=1) = 13; score(findings=0, high=0) = 0
    - previous LastEvaluation {FindingsCount:3, HighSeverityCount:1} vs new verdict scoring 13 → STALLED (not strictly decreasing)
    - previous 13 vs new 12 → improving, proceed
    - previous 13 vs new 14 → STALLED
    - Iteration 0 (first REPAIRABLE — no previous evaluation) → never stalled (nothing to compare)
    - boundary: Iteration >= MaxIterations → exhausted (MaxIterations=1: one authoring attempt + one re-plan, then escalate; the ORIGINAL attempt counts, Phase 51 repairOrHalt precedent)
  </behavior>
  <read_first>
    - internal/controller/task_controller.go lines 2440-2470 (applyLoopStatus — how HighSeverityCount/FindingsCount are computed from out.Verdict.Findings; reuse the same severity-token counting, do not invent a second rubric), lines 2638-2660 (repairOrHalt's ordering), lines 2736-2830 (dispatchRepairAttempt — the applyLoopStatus-BEFORE-attempt-reassignment load-bearing ordering comment)
    - internal/controller/plan_controller.go (52-07's state: the marked `// 52-09 replaces this` seam in handlePlanVerifierCompletion; reconcilePlannerDispatch's hardcoded `attempt := 1` at ~:283 and the jobName Sprintf at ~:388; the taskPlanRefIndexKey List at :267-276)
    - internal/controller/task_verify_loop_test.go (the plain-Go unit-file shape to mirror)
  </read_first>
  <action>
    1. Extract the stall math as pure functions: `severityScore(findingsCount, highSeverityCount int32) int` (= high×10 + findings, doc-commented as D-05's resolved weighting: high-severity dominates) and `replanStalled(prev tideprojectv1alpha3.EvaluationSummary, newScore int) bool` (strictly-decreasing requirement; no previous evaluation → false). Write plan_verify_loop_test.go FIRST covering every behavior case, then implement.

    2. `repairOrHaltPlan(ctx, plan, project, out)` replacing 52-07's marked seam for REPAIRABLE (and APPROVED+deterministic-failure): compute the new verdict's score from out.Verdict.Findings (same severity-token counting applyLoopStatus uses); policy := ResolveLoopPolicy(project, plan, nil, "plan"). Order: (a) stall check — if replanStalled against plan.Status.LoopStatus.LastEvaluation → exhaust with message "re-plan loop stalled" (route to exhaustVerifyLoop, same as 52-07's path); (b) boundary — if int(plan.Status.LoopStatus.Iteration) >= int(policy.MaxIterations) → exhaust ("plan-check iterations exhausted"); (c) else dispatchPlanRepair. Neither (a) nor (b) burns an iteration.

    3. `dispatchPlanRepair(ctx, plan, project, out)`: (i) stamp the LoopStatus mirroring applyLoopStatus's ordering contract — LastEvaluation summarizes the attempt JUST verified, Iteration increments to count the quality re-plan, ExitReason stays empty (loop continues); (ii) serialize a bounded findings block (cap 10 findings, each Summary truncated to 300 chars, keep Severity + Confidence — the RunEvidence.Bounded truncation idiom) into the `tideproject.k8s/replan-findings` annotation via a MergeFrom annotation patch; (iii) DELETE every child Task listed by `client.MatchingFields{taskPlanRefIndexKey: plan.Name}` (safe: D-03's Verifying hold guarantees none dispatched — no in-flight Jobs exist for them); (iv) status-patch the Plan's phase from Verifying back to its pre-materialization value ("") so the planner-dispatch path re-engages; (v) requeue. The NEXT reconciles handle the rest naturally: reconcilePlannerDispatch's tasks-exist early-return keeps returning until deletion completes (the collision-window barrier), then dispatches the fresh attempt.

    4. `reconcilePlannerDispatch`: replace BOTH hardcoded attempt-1 sites (the `attempt := 1` and the `tide-plan-%s-1` Sprintf) with `attempt := int(plan.Status.LoopStatus.Iteration) + 1` — doc-commented per Open Question 3's resolution: Plan planner Jobs had no pre-existing infra-retry identity, so the quality counter doubles as the Job attempt; a future infra-retry feature must mint a separate counter. When dispatching with a non-empty replan-findings annotation, decode it and execute the planner prompt template with the 52-03 pinned `.RepairFindings` field populated; clear the annotation in `handlePlannerJobCompletion` once the fresh attempt materializes (consumption point).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'TestPlanVerifyLoop|TestSeverityScore|TestReplanStalled' -count=1 -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - All stall-math behavior cases pass as named plain-Go subtests (file: plan_verify_loop_test.go — NOT Ginkgo, Pitfall 5)
    - `grep -n "52-09 replaces" internal/controller/plan_controller.go` = 0 (seam replaced)
    - `grep -n "attempt := 1\|tide-plan-%s-1" internal/controller/plan_controller.go` = 0 (both hardcoded sites gone)
    - `grep -n "replan-findings" internal/controller/plan_controller.go` shows set + consume + clear sites (≥ 3)
    - `grep -n "Delete" internal/controller/plan_controller.go` shows the child-Task deletion inside dispatchPlanRepair using taskPlanRefIndexKey
  </acceptance_criteria>
  <done>Full re-plan loop implemented with stall-first ordering, bounded findings transport, delete-then-recreate, and attempt-aware dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Ginkgo RePlan specs — one re-plan, stale-task invariant, stall at maxIterations:2</name>
  <files>internal/controller/plan_verify_dispatch_test.go</files>
  <read_first>
    - internal/controller/plan_verify_dispatch_test.go (52-07's fixtures — extend the same Describe family)
    - internal/controller/task_verify_dispatch_test.go (how Phase 51 specs complete verifier Jobs and write verdict envelopes for consumption)
  </read_first>
  <action>
    Add a `Describe("RePlan", …)` family: (a) default maxIterations:1 — REPAIRABLE verdict on attempt 1 → rejected children deleted (List by planRef returns 0), Plan phase leaves Verifying, a SECOND planner Job (`tide-plan-<uid>-2`) eventually dispatches once the list empties, and the replan-findings annotation is present at dispatch and cleared after the second materialization; a REPAIRABLE verdict on the re-planned attempt → exhaustion (VerifyHalted/AwaitingApproval per policy), NO third planner Job — "exactly one re-plan" pinned; (b) stale-task invariant: assert at every observed point that no Task from the rejected attempt ever reaches a dispatched state (no executor Jobs exist for deleted children); (c) stall detection with maxIterations:2 (D-05's required coverage): first REPAIRABLE verdict scores 13, the re-planned attempt's verdict scores 13 again (non-improving) → the loop halts at the stall check WITHOUT dispatching a third planner attempt even though MaxIterations would allow it; a control spec where the second score improves (13→5 with REPAIRABLE) → the loop proceeds to consume the remaining iteration.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestControllers --ginkgo.focus='RePlan' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Focus run reports ≥ 4 specs ran, 0 failures
    - Spec (a) asserts the exact Job-name progression tide-plan-<uid>-1 → tide-plan-<uid>-2 and the absence of a -3
    - Spec (c) proves early-halt-on-stall consumes NO additional planner dispatch (Pitfall 3's warning sign — "never observes a SECOND planner Job" — is inverted into a positive assertion here: the second Job exists, the third never does)
  </acceptance_criteria>
  <done>SC1's loop semantics (own counter, one re-plan, stall-early-halt) pinned end-to-end in envtest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → child-Task CRD deletion | the controller destroys planner-authored CRDs on a verifier's say-so |
| verifier findings → planner prompt | evaluator output is injected into the next LLM authoring context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-27 | Tampering/EoP | a stale rejected-attempt Task dispatching (spends budget on rejected work) | mitigate | Delete-then-recreate + the Verifying hold land as a pair (RESEARCH: "BOTH must land together; either alone is incomplete") — the hold shipped in 52-07, deletion here; spec (b) pins the invariant |
| T-52-28 | Tampering | child-Task name collision freezing a stale spec (AlreadyExists-as-success) | mitigate | The tasks-exist early-return doubles as the deletion barrier: the fresh planner never dispatches until List returns 0, so re-materialization cannot race deletion |
| T-52-29 | DoS (etcd) | unbounded findings accumulating on the Plan object | mitigate | Single current-iteration annotation, capped 10×300 chars, cleared on consumption; LOOP-03 status rule untouched (LastEvaluation counts only) |
| T-52-30 | Tampering | a re-plan gaming its own rubric (plan-level analog of TASK-06 anti-gaming) | accept | A planner authors CRD specs, not repo code — it cannot edit evaluator fixtures; weak-contract gaming is caught by the rubric's verification-derivability dimension and by stall detection halting non-improving loops; residual risk documented |
</threat_model>

<verification>
- Full envtest suite green: `go test ./internal/controller/... -run TestControllers -count=1`
- `make lint` green
</verification>

<success_criteria>
- SC1 fully true: own counter (Iteration), default 1, exactly one findings-seeded re-plan, severity-weighted stall halting early — each pinned by a spec
- Pitfall 3 structurally impossible (deletion unblocks the early-return; specs observe the second planner Job)
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-09-SUMMARY.md` when done.
</output>
