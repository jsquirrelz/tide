---
phase: 52-per-level-looppolicy-parameterization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/subagent/common/templates/plan_verifier.tmpl
  - internal/subagent/common/templates/phase_verifier.tmpl
  - internal/subagent/common/templates/milestone_verifier.tmpl
  - internal/subagent/common/templates/project_verifier.tmpl
  - internal/subagent/common/templates/plan_planner.tmpl
  - internal/subagent/common/prompt_templates.go
  - internal/subagent/common/prompt_templates_test.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "LoadPromptTemplate(\"verifier\", <level>) resolves a real template for plan, phase, milestone, and project with zero loader changes (D-09)"
    - "plan_verifier.tmpl carries the goal-backward rubric's four named dimensions: goal alignment, file-touch plausibility, dependency correctness, verification derivability (ESC-01)"
    - "All four new templates carry the coverage-not-conservatism finding directive verbatim (D-09)"
    - "plan_planner.tmpl renders a re-plan findings block when RepairFindings is populated, and renders identically to today when it is absent (D-04)"
  artifacts:
    - path: "internal/subagent/common/templates/plan_verifier.tmpl"
      provides: "goal-backward plan-check rubric prompt"
      min_lines: 40
    - path: "internal/subagent/common/templates/phase_verifier.tmpl"
      provides: "observable-outcome level-verify prompt"
      min_lines: 30
    - path: "internal/subagent/common/prompt_templates.go"
      provides: "PromptTemplateVersion bumped v4 → v5"
      contains: "\"v5\""
  key_links:
    - from: "internal/subagent/common/prompt_templates.go"
      to: "templates/*.tmpl"
      via: "go:embed glob picks up new files automatically"
      pattern: "go:embed templates"
---

<objective>
Author the four per-level verifier prompt templates (D-09) behind the existing `LoadPromptTemplate(role, level)` filename convention — zero loader code changes — plus the D-04 conditional re-plan findings block in `plan_planner.tmpl`, and bump `PromptTemplateVersion` v4→v5 once for all five template edits (maintenance rule: one package-level version, same commit as the template changes).

This plan also PINS the render-data contract downstream Go plans implement (interface-first): `plan_verifier.tmpl` renders with a struct exposing `.Verify` (the level-agnostic `pkg/dispatch.VerifyContext`), `.PlanGoal` (string — the Plan's authored goal/prompt text), and `.Children` (slice with fields `.Name`, `.DependsOn`, `.Files`, `.GateCommand` — a bounded child-Task summary). `phase/milestone/project_verifier.tmpl` render with `.Verify` and `.LevelGoal` (string). `plan_planner.tmpl`'s new block keys on `.RepairFindings` (slice with fields `.Severity`, `.Confidence`, `.Summary`). Plans 52-07/52-08/52-09 supply these exact structs.

Purpose: prompts are pure config — landing them in Wave 1 lets every dispatch plan wire against a fixed contract.
Output: 4 new templates + 1 extended template + version bump + render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (section: templates — the 7-point section-for-section map of task_verifier.tmpl to mirror)
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md  (D-04, D-09)
</context>

<tasks>

<task type="auto">
  <name>Task 1: plan_verifier.tmpl (goal-backward rubric) + plan_planner.tmpl findings block</name>
  <files>internal/subagent/common/templates/plan_verifier.tmpl, internal/subagent/common/templates/plan_planner.tmpl</files>
  <read_first>
    - internal/subagent/common/templates/task_verifier.tmpl (full — the structural skeleton: role framing, tools-honesty block, APPROVED/REPAIRABLE/BLOCKED verdict-semantics block with the "non-zero gate exit ≠ automatically BLOCKED" nuance, coverage-not-conservatism directive at ~lines 58-69, and the {{.Verify.GateCommand}}/{{range .Verify.RequiredArtifacts}} templating idiom)
    - internal/subagent/common/templates/plan_planner.tmpl (full — current structure and render-data references; the findings block must not disturb existing sections)
    - internal/subagent/common/prompt_templates.go lines 30-90 (embed directive, LoadPromptTemplate convention, PromptTemplateVersion maintenance-rule comment)
  </read_first>
  <action>
    1. Create `plan_verifier.tmpl` mirroring `task_verifier.tmpl` section-for-section per the PATTERNS.md 7-point map, with these plan-specific substitutions: role framing "TIDE's plan verifier — a read-only evaluator of an authored plan"; the PRIMARY judgment is the goal-backward rubric with ESC-01's four named dimensions as explicit numbered sections — (1) goal alignment: does the authored plan serve {{.PlanGoal}}; (2) file-touch plausibility: do the children's {{.Files}} declarations match the plan's scope; (3) dependency correctness: does each child's {{.DependsOn}} reflect real ordering constraints; (4) verification derivability: can each child's verification contract actually be evaluated against its declared outputs. Render the bounded child summary via {{range .Children}} with .Name/.DependsOn/.Files/.GateCommand. Keep the verdict-semantics block VERBATIM from task_verifier.tmpl (REPAIRABLE drives one findings-seeded re-plan per D-04 — say so); keep the coverage-not-conservatism directive VERBATIM; keep the {{.Verify.GateCommand}} gate-command section (a plan-level contract may carry a real command — it runs in the provisioned read-only worktree) and the {{if .Verify.EvidencePacketPath}} recheck block if present in the task template.

    2. Extend `plan_planner.tmpl` with a conditional block keyed on {{if .RepairFindings}}: a compact evidence section stating a prior plan-check rejected the previous attempt and every finding must be addressed in the fresh plan, then {{range .RepairFindings}} emitting severity/confidence/summary per finding. Positive-instruction framing (address each finding), scoped explicitly to apply to every finding, not just the first. When .RepairFindings is absent/empty the template's rendered output must be byte-identical to today's.
  </action>
  <verify>
    <automated>go test ./internal/subagent/common/... -run TestLoadPromptTemplate -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "goal alignment\|file-touch plausibility\|dependency correctness\|verification derivability" internal/subagent/common/templates/plan_verifier.tmpl` ≥ 4 (all four dimensions named)
    - `grep -c "Report a finding for EVERY deviation" internal/subagent/common/templates/plan_verifier.tmpl` = 1 (coverage directive verbatim)
    - `grep -c "RepairFindings" internal/subagent/common/templates/plan_planner.tmpl` ≥ 2 (guard + range)
    - Rendering plan_planner.tmpl with empty RepairFindings equals the pre-change rendering (test asserts this)
  </acceptance_criteria>
  <done>plan_verifier.tmpl carries the four-dimension rubric + verbatim shared blocks; plan_planner.tmpl gains the backward-compatible findings block.</done>
</task>

<task type="auto">
  <name>Task 2: phase/milestone/project_verifier.tmpl + version bump + render tests</name>
  <files>internal/subagent/common/templates/phase_verifier.tmpl, internal/subagent/common/templates/milestone_verifier.tmpl, internal/subagent/common/templates/project_verifier.tmpl, internal/subagent/common/prompt_templates.go, internal/subagent/common/prompt_templates_test.go</files>
  <read_first>
    - internal/subagent/common/templates/task_verifier.tmpl (already read in Task 1 — the skeleton)
    - internal/subagent/common/prompt_templates_test.go (full — existing render/load test idiom to extend)
  </read_first>
  <action>
    1. Create `phase_verifier.tmpl`, `milestone_verifier.tmpl`, `project_verifier.tmpl` — closer to task_verifier.tmpl's own framing than plan's (these DO run a real gate command in a real worktree): observable-outcome verification of {{.LevelGoal}} — deliverables exist on the run branch (the checked-out worktree is the reachable filesystem), gate command exit honored, constraint paths untouched. The verdict-semantics block keeps the three-way APPROVED/REPAIRABLE/BLOCKED vocabulary (ClassifyVerdict is unconditional) but notes that at THIS level exhaustion escalates rather than iterates — the REPAIRABLE-vs-BLOCKED judgment still signals severity to the human backstop even though no fresh attempt follows (PATTERNS.md point 3). Coverage-not-conservatism directive VERBATIM in all three. Substitute the level noun per file; state scope explicitly where a directive applies to every artifact/finding.

    2. Bump `const PromptTemplateVersion = "v4"` to `"v5"` in prompt_templates.go — one bump covering all five template changes in this plan (both tasks land in this plan; the version const changes exactly once).

    3. Extend prompt_templates_test.go: LoadPromptTemplate("verifier", level) succeeds and executes for all of plan/phase/milestone/project with representative render data (the pinned contract structs — anonymous test structs with Verify + PlanGoal/Children or LevelGoal fields), asserting each rendered output contains its level-distinct marker (e.g. the four rubric dimensions for plan; "run branch" framing for the other three) and the coverage directive.
  </action>
  <verify>
    <automated>go test ./internal/subagent/common/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ls internal/subagent/common/templates/ | grep -c "_verifier.tmpl"` = 5 (task + four new)
    - `grep -n "PromptTemplateVersion = \"v5\"" internal/subagent/common/prompt_templates.go` returns 1 line
    - `go test ./internal/subagent/common/...` exits 0 with render tests exercising all four new levels
    - `grep -c "Report a finding for EVERY deviation" internal/subagent/common/templates/{phase,milestone,project}_verifier.tmpl` totals 3
  </acceptance_criteria>
  <done>All four verifier templates load and render through the unchanged loader; version bumped once; task_verifier.tmpl untouched (`git diff --stat` shows no change to it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator-rendered prompt → LLM verifier | prompt text steers an evaluator whose verdict gates dispatch/spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06 | Elevation of Privilege | verdict semantics drifting fail-open in new prompts | mitigate | Verdict-semantics block copied verbatim from task_verifier.tmpl; ClassifyVerdict (Go-side, unchanged) remains the fail-closed authority regardless of prompt wording |
| T-52-07 | Information Disclosure | findings text echoed into re-plan prompts | accept | Findings originate from TIDE's own verifier over the operator's own repo; no cross-tenant boundary exists |
| T-52-08 | Tampering | a re-plan prompt instructing the planner to weaken child verification contracts | mitigate | plan_verifier.tmpl's verification-derivability dimension re-checks the fresh attempt's contracts on the next iteration; stall detection (52-09) halts non-improving loops |
</threat_model>

<verification>
- `go test ./internal/subagent/common/...` green; `make lint` green
- `git diff --stat internal/subagent/common/templates/task_verifier.tmpl` empty (D-09: untouched)
</verification>

<success_criteria>
- Four templates resolve via the existing loader with zero loader changes; render-data contract pinned for 52-07/52-08/52-09
- PromptTemplateVersion = v5, bumped exactly once
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-03-SUMMARY.md` when done.
</output>
