---
phase: 52-per-level-looppolicy-parameterization
plan: 04
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - internal/controller/dispatch_helpers.go
  - internal/controller/dispatch_helpers_loop_policy_test.go
  - cmd/manager/main.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "One resolver function keyed on level — not CRD kind — produces every level's effective LoopPolicy (SC3, D-02)"
    - "Precedence is Task > Plan > Project defaults > off; absence of any authored contract means the stage is off (D-01)"
    - "Level defaults: plan → maxIterations 1; phase/milestone/project → maxIterations 0 (clamped) with EscalationPolicy defaulting to requireApproval; task/plan unset onExhaustion defaults to escalate (behavior-preserving for Task)"
    - "PlannerReconcilerDeps carries VerifierImage + Reservations + ReserveEstimateCents so Plan/Phase/Milestone/Project reconcilers can dispatch verifiers through the same budget pool (D-10)"
  artifacts:
    - path: "internal/controller/dispatch_helpers.go"
      provides: "ResolveLoopPolicy + ResolveVerificationSpec + projectLevelVerificationDefault"
      contains: "func ResolveLoopPolicy"
    - path: "internal/controller/dispatch_helpers_loop_policy_test.go"
      provides: "TestResolveLoopPolicy precedence/default coverage + SC3 static guard"
      contains: "TestResolveLoopPolicy"
    - path: "cmd/manager/main.go"
      provides: "plannerDeps wired with verifierImage + the SAME ReservationStore instance"
      contains: "VerifierImage"
  key_links:
    - from: "internal/controller/dispatch_helpers.go"
      to: "api/v1alpha3 VerificationSpec/LoopPolicy/LoopLevel"
      via: "resolver reads the 52-01 schema fields"
      pattern: "LoopLevel\\(level\\)"
    - from: "cmd/manager/main.go"
      to: "PlannerReconcilerDeps"
      via: "same store instance as TaskReconciler (project-wide budget accounting)"
      pattern: "Reservations"
---

<objective>
Build the phase's centerpiece (SC3): `ResolveLoopPolicy(project, plan, task, level)` next to `ResolveProvider` in `dispatch_helpers.go`, plus the sibling `ResolveVerificationSpec` sharing one unexported precedence walk, plus the SC3 static guard test that forbids any controller from reading raw `Spec.Verification.MaxIterations`/`.OnExhaustion` outside the resolver. Also extend `PlannerReconcilerDeps` with the three dispatch-tier fields (`VerifierImage`, `Reservations`, `ReserveEstimateCents`) every non-Task verifier dispatch site needs, wired in `cmd/manager/main.go` from the existing `verifierImage` var and the SAME `ReservationStore` instance TaskReconciler already receives (D-10: one pool, no second counter).

Purpose: every downstream dispatch/exhaustion plan branches only on the RETURNED policy; none switches on CRD kind.
Output: resolver + spec-accessor + static guard + Deps plumbing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (section: dispatch_helpers.go — the ResolveProvider excerpt and the RESEARCH-authored resolver body to implement)
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (Code Examples §Resolver shape; State of the Art table row 1)
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md  (D-01, D-02)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResolveLoopPolicy + ResolveVerificationSpec + precedence walk</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/dispatch_helpers_loop_policy_test.go</files>
  <behavior>
    - task level, task has Locked contract with GateCommand + MaxIterations 3, onExhaustion unset → policy{Level: task, MaxIterations: 3, EscalationPolicy: escalate} (behavior-preserving default)
    - plan level, plan has authored contract, MaxIterations unset → policy{Level: plan, MaxIterations: 1, EscalationPolicy: escalate}
    - phase level, only Project.Spec.Verification.Phase default authored, onExhaustion unset → policy{Level: phase, MaxIterations: 0, EscalationPolicy: requireApproval}
    - phase level, Project default authors MaxIterations 3 → MaxIterations CLAMPED to 0 (auto-repair at these levels is out of scope; the resolver encodes D-07, not call-site ifs)
    - milestone/project levels with authored onExhaustion escalate → EscalationPolicy escalate (authored value wins over the requireApproval default)
    - task level, task spec empty but Project.Spec.Verification.Task default authored → the Project default applies (precedence walk falls through)
    - no authored contract anywhere at the level → returned VerificationSpec has empty GateCommand (the caller's "stage is off" signal); Level still stamped
    - plan level: plan.Spec.Verification wins over Project.Spec.Verification.Plan when both authored
  </behavior>
  <read_first>
    - internal/controller/dispatch_helpers.go lines 240-340 (`levelOverrideKey` + `ResolveProvider` — the precedence-chain idiom and doc-comment style to mirror; the resolver lands adjacent) and lines 415-440 (`resolveImage`'s per-level-key switch — the shape for `projectLevelVerificationDefault`)
    - api/v1alpha3/task_types.go lines 91-160 (VerificationSpec fields the walk reads: Phase, GateCommand, Commands, RequiredArtifacts, MaxIterations, OnExhaustion, Evaluator)
    - api/v1alpha3/loop_types.go (LoopPolicy fields incl. the new Level; EscalationPolicy constants)
    - 52-RESEARCH.md Code Examples §"Resolver shape (D-02)" (the worked function body — implement this shape, extended with the EscalationPolicy level defaults and the phase/milestone/project clamp specified here)
  </read_first>
  <action>
    Implement in dispatch_helpers.go, adjacent to ResolveProvider:

    1. `projectLevelVerificationDefault(project *tideprojectv1alpha3.Project, level string) *tideprojectv1alpha3.VerificationSpec` — per-level-key switch over the 52-01 `ProjectSpec.Verification` map fields (Task/Plan/Phase/Milestone/Project), mirroring resolveImage's switch shape; nil-safe on project.

    2. An unexported precedence walk `resolveAuthoredVerification(project, plan, task, level) tideprojectv1alpha3.VerificationSpec`: level=="task" && task authored (GateCommand != "") → task.Spec.Verification; level=="plan" && plan authored → plan.Spec.Verification; else the Project per-level default when non-nil; else zero value. "Authored" test = GateCommand != "" (matching the RESEARCH sketch and hasVerificationContract's activation key).

    3. `ResolveVerificationSpec(project, plan, task *…, level string) tideprojectv1alpha3.VerificationSpec` — thin exported wrapper over the walk (dispatch sites need the contract fields for envelope building).

    4. `ResolveLoopPolicy(project, plan, task *…, level string) tideprojectv1alpha3.LoopPolicy` — calls the walk, then applies level defaults: MaxIterations — task: as authored; plan: default 1 when unset (and an authored value wins); phase/milestone/project: CLAMP to 0 unconditionally with a doc comment citing D-07 ("no repair branch at these levels — the resolver encodes it, not level-specific if-statements") and the REQUIREMENTS Out-of-Scope exclusion of auto-repair at these levels. EscalationPolicy — authored OnExhaustion wins when set; unset defaults: task/plan → EscalationEscalate (preserves Phase 51's uniform behavior at Task defaults, D-02's no-regression bar), phase/milestone/project → EscalationRequireApproval (ROADMAP SC2: "escalates straight to requireApproval"). Stamp `Level: LoopLevel(level)` unconditionally. Carry EvaluatorRef from the spec's Evaluator field.

    5. Write `dispatch_helpers_loop_policy_test.go` FIRST (plain-Go, `func TestResolveLoopPolicy…(t *testing.T)` — NOT Ginkgo, per Pitfall 5) covering every behavior case above; then implement to green.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestResolveLoopPolicy -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - All 8 behavior cases above have a named subtest and pass
    - `grep -c "switch obj := obj.(type)\|switch .*(type)" internal/controller/dispatch_helpers.go` — the resolver contains no type-switch on CRD kinds (level string keying only)
    - `go vet ./internal/controller/...` clean
    - ResolveLoopPolicy has exactly one exported entry point for policy resolution (no per-level variants)
  </acceptance_criteria>
  <done>Resolver + spec accessor implemented over one shared precedence walk; unit tests prove precedence, defaults, clamp, and escalation-policy defaults.</done>
</task>

<task type="auto">
  <name>Task 2: SC3 static guard + PlannerReconcilerDeps plumbing</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/dispatch_helpers_loop_policy_test.go, cmd/manager/main.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go lines 172-235 (PlannerReconcilerDeps — where the three new fields go, matching existing doc-comment style)
    - internal/controller/task_controller.go lines 105-125 (TaskReconcilerDeps.Reservations/ReserveEstimateCents doc comments to mirror; note `*budget.ReservationStore` import path)
    - cmd/manager/main.go lines 200-230 and 440-470 and 550-575 (the `verifierImage` env var at :219, the `plannerDeps := controller.PlannerReconcilerDeps{…}` literal at :449, the TaskReconciler wiring at :562 — find where the ReservationStore instance is constructed so BOTH deps structs share it)
    - internal/analysis or wave_label_test.go precedent (Phase 50's hand-synced source-scanning guard style — `grep -rn "forbiddenRuntimeLabels" internal/` to locate) for the static-guard idiom
  </read_first>
  <action>
    1. Add to PlannerReconcilerDeps: `VerifierImage string` (doc: image ref for tide-langgraph-verifier used by plan-check and level-verify dispatch, mirrors TaskReconcilerDeps.VerifierImage), `Reservations *budget.ReservationStore` (doc: MUST be the same instance TaskReconciler receives — one project-wide pool, D-10/ESC-04), `ReserveEstimateCents int64`. Wire all three in cmd/manager/main.go's plannerDeps literal from the existing `verifierImage` variable and the same store/estimate values passed to TaskReconciler.

    2. Add the SC3 static guard to dispatch_helpers_loop_policy_test.go: `TestNoDirectVerificationPolicyReads` — walks every `internal/controller/*.go` file (os.ReadDir + filepath), excluding `_test.go` files and `dispatch_helpers.go` itself, strips line-comment content (drop everything after `//` on each line — grep-gate hygiene: doc prose must not self-invalidate the guard), and asserts ZERO matches of the regex `Spec\.Verification\.(MaxIterations|OnExhaustion)`. Failure message names SC3 and directs readers to ResolveLoopPolicy. (Note: task_controller.go still has direct reads until 52-06 lands — the guard will be RED if run against main before 52-06; since 52-06 is a later wave, scope the guard to pass NOW by ALSO excluding task_controller.go with an explicit TODO(52-06) comment saying the exclusion is removed by the Task-migration plan. 52-06's acceptance removes the exclusion.)
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run 'TestResolveLoopPolicy|TestNoDirectVerificationPolicyReads' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VerifierImage\|Reservations\|ReserveEstimateCents" internal/controller/dispatch_helpers.go` shows all three fields on PlannerReconcilerDeps with doc comments
    - `grep -n "VerifierImage" cmd/manager/main.go` shows ≥ 2 wiring sites (plannerDeps + existing TaskReconciler)
    - The ReservationStore instance passed to plannerDeps is verifiably the same variable passed to TaskReconciler (read the main.go diff — one shared identifier)
    - TestNoDirectVerificationPolicyReads passes and contains the TODO(52-06) exclusion comment for task_controller.go
  </acceptance_criteria>
  <done>Deps plumbing complete for every non-Task dispatch site; SC3 guard live with a single documented temporary exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRD spec (operator/planner-authored) → resolver → dispatch decisions | authored contracts become spend-triggering dispatch policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-09 | Elevation of Privilege | resolver fabricating an active contract where none authored | mitigate | "Stage off" = empty GateCommand from the walk; unit case asserts no-contract → empty spec; V5/ASVS: Locked-phase enforcement stays at the dispatch sites (hasVerificationContract idiom), not weakened here |
| T-52-10 | DoS (budget) | a second reservation pool undercounting project spend | mitigate | main.go wires the SAME ReservationStore instance into plannerDeps; acceptance criterion verifies one shared identifier |
| T-52-11 | Tampering | future code bypassing the resolver to read raw spec knobs | mitigate | TestNoDirectVerificationPolicyReads static guard (comment-stripped, SC3) fails the build on any new direct read |
</threat_model>

<verification>
- `go test ./internal/controller/... -run 'TestResolveLoopPolicy|TestNoDirectVerificationPolicyReads'` green
- `make lint` green
</verification>

<success_criteria>
- SC3 mechanically enforced: one resolver, level-keyed, statically guarded
- Task defaults provably unchanged (escalate + authored MaxIterations pass-through)
- Non-Task reconcilers have every Deps field required for verifier dispatch
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-04-SUMMARY.md` when done.
</output>
