---
phase: 52-per-level-looppolicy-parameterization
plan: 08
type: execute
wave: 4
depends_on: ["52-03", "52-04", "52-05", "52-06"]
files_modified:
  - internal/controller/level_verify.go
  - internal/controller/level_verify_unit_test.go
  - pkg/dispatch/envelope.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "ONE shared level-verify state machine serves Phase, Milestone, and Project — no per-controller fork (the depgraph.go shared-resolver precedent)"
    - "maxIterations:0 is encoded by the resolver's returned policy — the machinery has no repair branch and no level-specific if-statements (D-07)"
    - "A level whose LoopStatus.ExitReason is already set never re-dispatches a verifier (post-approval convergence guard)"
    - "EnvelopeIn.Level's stale doc comment lists all five levels including project (RESEARCH anti-pattern fix)"
  artifacts:
    - path: "internal/controller/level_verify.go"
      provides: "maybeRunLevelVerify + dispatchLevelVerifier + handleLevelVerifierCompletion (shared, level-parameterized)"
      contains: "func maybeRunLevelVerify"
    - path: "pkg/dispatch/envelope.go"
      provides: "corrected Level doc comment"
      contains: "project"
  key_links:
    - from: "internal/controller/level_verify.go"
      to: "internal/controller/level_status.go exhaustVerifyLoop"
      via: "every non-APPROVED terminal exit routes through the one D-08 branch point"
      pattern: "exhaustVerifyLoop"
    - from: "internal/controller/level_verify.go"
      to: "podjob.BuildOptions WorktreeCheckoutImage/WorktreeBranch"
      via: "every level-verify dispatch provisions the read-only worktree (Pitfall 2)"
      pattern: "WorktreeCheckoutImage"
---

<objective>
Factor the Phase/Milestone/Project level-verify state machine as ONE shared, level-parameterized unit in a new `internal/controller/level_verify.go` — dispatch (with every D-10 rail + the 52-05 worktree provisioning + `EnvelopeIn.Branch` at run-branch tip), completion consumption (fail-closed, deterministic-failure dominance), and terminal routing through 52-06's `exhaustVerifyLoop`. 52-10 wires the three controllers' pre-Succeeded seams onto it; this plan keeps those files untouched so the two wave-4 plans stay disjoint.

Purpose: SC2's machinery — any verify finding at these levels escalates per resolved policy with zero auto-repair, encoded by the resolver's clamped `MaxIterations: 0`, not call-site conditionals.
Output: shared machinery + unit tests + the envelope doc fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (sections: phase/milestone/project controllers — the pre-Succeeded seam shape; plan_controller.go points 1-4 for the dispatch/consume skeleton)
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (§The missing worktree — point 4: Branch population; Anti-pattern: stale EnvelopeIn.Level doc)
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md  (D-07, D-10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: level_verify.go — the shared dispatch/consume machinery</name>
  <files>internal/controller/level_verify.go</files>
  <read_first>
    - internal/controller/task_controller.go lines 650-700 (checkVerifyingState three-way branch), 2099-2240 (dispatchVerifier: cap-before-reserve, VerifierImage guard, AlreadyExists idempotency, label stamping), 2242-2320 (buildVerifierEnvelopeIn), 2855-2929 (handleVerifierCompletion fail-closed switch + hasDeterministicFailure)
    - internal/controller/level_status.go (patchLevelStatus + exhaustVerifyLoop signatures — the leaves this machinery composes)
    - internal/controller/dispatch_helpers.go (PlannerReconcilerDeps fields from 52-04; verifierInFlightCount; ResolveProvider)
    - internal/controller/span_emission.go lines 300-380 (synthesizeEvaluatorSpan — level-parameterized span name tide.dispatch.<level>.verify)
    - api/v1alpha3/project_types.go (Project.Status.Git — the run-branch field name for Branch/WorktreeBranch)
  </read_first>
  <action>
    Create `internal/controller/level_verify.go` with a doc header naming the depgraph.go factor-not-fork precedent. Implement, parameterized by a small `levelVerifyTarget` struct (fields: `Obj client.Object`, `Conditions *[]metav1.Condition`, `PhasePtr *string`, `LoopStatus *tideprojectv1alpha3.LoopStatus`, `Level string`, `Goal string`) so the three controllers pass their own accessors:

    1. `maybeRunLevelVerify(ctx, c, deps PlannerReconcilerDeps, project, target) (handled bool, res ctrl.Result, err error)` — the single entry the pre-Succeeded seams call: resolve `spec := ResolveVerificationSpec(project, nil, nil, target.Level)`; NOT active (`spec.GateCommand == "" || spec.Phase != "Locked"`) → return handled=false (caller proceeds to patch{Level}Succeeded exactly as today — the off-switch). Active but `target.LoopStatus.ExitReason != ""` → return handled=false (loop already concluded: approved verdict, or an operator's approve-resume accepted the findings — the convergence guard preventing verify re-dispatch after approval). Otherwise drive the state machine: phase not yet Verifying → patch it Verifying (via patchLevelStatus) and dispatch; phase Verifying → Get the Job by `podjob.VerifierJobName(target.Level, string(target.Obj.GetUID()), 1)` (attempt fixed at 1 — maxIterations:0 levels never mint a second quality attempt; Job backoff handles pod-level retries) with the NotFound→dispatch / terminal→consume / running→requeue three-way.

    2. `dispatchLevelVerifier` — mirrors dispatchVerifier: VerifierImage-empty log-and-skip guard; cap-before-reserve via verifierInFlightCount + deps.Reservations.Reserve(string(uid), deps.ReserveEstimateCents); envelope: Role "verifier", Level target.Level, `Branch` = the project run branch (Project.Status.Git — the field the executor's EnvelopeIn.Branch already carries; the Task verifier never set it, the level variant MUST); Verify from the resolved spec (gate command + commands union + required artifacts + evaluator). Prompt via LoadPromptTemplate("verifier", target.Level) rendered with the 52-03 pinned struct {Verify, LevelGoal: target.Goal}. BuildOptions: Kind JobKindVerifier, ParentObj target.Obj, Level, Attempt 1, ReadOnly true, SubagentImage deps.VerifierImage, WorktreeCheckoutImage deps.TidePushImage, WorktreeBranch = run branch, provider from ResolveProvider(project, target.Level, deps.HelmProviderDefaults). AlreadyExists-as-success + release-on-error + post-build label stamp, all per the Task idiom.

    3. `handleLevelVerifierCompletion` — role-aware out-envelope read (reuse Task's reader); unreadable/nil-verdict → fail-closed to exhaustion; ClassifyVerdict switch: APPROVED without deterministic failure → stamp LoopStatus (LastEvaluation + ExitReason approved), settle reservation, emit synthesizeEvaluatorSpan(level), return handled=false so the caller falls through to patch{Level}Succeeded on this same reconcile (the ExitReason guard makes later reconciles skip re-verify); ANY other outcome (REPAIRABLE, BLOCKED, degraded, or APPROVED+deterministic-failure) → stamp LoopStatus + settle + span, then `exhaustVerifyLoop(ctx, c, project, target.Obj, target.Conditions, target.PhasePtr, target.Level, ResolveLoopPolicy(project, nil, nil, target.Level), completedAt, summary)` — NO repair branch exists in this file (D-07: the resolver encodes it; there is deliberately no MaxIterations consultation here beyond the policy the exhaustion helper receives).
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "switch.*(type)" internal/controller/level_verify.go` = 0 (level string parameterization only — SC3)
    - `grep -n "exhaustVerifyLoop" internal/controller/level_verify.go` ≥ 1 and `grep -n "dispatchRepair\|repairOrHalt" internal/controller/level_verify.go` = 0 (no repair branch)
    - `grep -n "Branch" internal/controller/level_verify.go` shows EnvelopeIn.Branch AND WorktreeBranch both populated from the project run-branch field
    - `grep -n "ExitReason" internal/controller/level_verify.go` shows the convergence guard in maybeRunLevelVerify
  </acceptance_criteria>
  <done>One shared machine, three future callers, zero repair paths, every rail + worktree provisioning wired.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for the pure parts + EnvelopeIn.Level doc fix</name>
  <files>internal/controller/level_verify_unit_test.go, pkg/dispatch/envelope.go</files>
  <read_first>
    - internal/controller/span_emission_unit_test.go (Phase 51-03's precedent: pure-function tests live in a plain-Go unit file because TestControllers is the sole Ginkgo entry — Pitfall 5)
    - pkg/dispatch/envelope.go lines 55-75 (the stale Level doc comment listing only four levels)
  </read_first>
  <action>
    1. `level_verify_unit_test.go` (plain Go `func TestXxx`): cover the decision table of maybeRunLevelVerify's pure guards using fake/stub inputs — inactive contract → handled=false; active + ExitReason set → handled=false (convergence); active + empty ExitReason → the machine engages (assert the Verifying patch intent or the returned state for a not-yet-Verifying target — structure the pure guard as a small testable function if needed, e.g. an unexported `levelVerifyDecision(spec, loopStatus, phase) verdict` extracted for testability). Also unit-test the envelope construction shape: Branch populated, Level correct, commands union ordering (gate command first).

    2. `pkg/dispatch/envelope.go`: fix the `EnvelopeIn.Level` doc comment to list all five levels — `"project" | "milestone" | "phase" | "plan" | "task"` — per the RESEARCH anti-pattern note ("project" is already a live value at project_controller.go:1469; the doc is stale). No behavioral change; note the Go↔Python golden fixtures are unaffected (comment-only).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'TestLevelVerify' -count=1 -v && go test ./pkg/dispatch/...</automated>
  </verify>
  <acceptance_criteria>
    - TestLevelVerify* unit tests pass with ≥ 4 named subtests (inactive / converged / engaging / envelope shape)
    - `grep -n "\"project\"" pkg/dispatch/envelope.go` shows the Level doc comment now includes project
    - `go test ./pkg/dispatch/...` green (golden fixtures untouched)
  </acceptance_criteria>
  <done>Pure guards pinned in unit tests; stale doc corrected in the same phase that adds project-level dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier Job (checks out + executes gate commands against the run branch) → run-branch worktree | a Job executes repo-defined commands at new levels |
| verifier out-envelope → level Succeeded/halt decision | untrusted file content gates milestone/project closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-23 | Tampering | gate command executing attacker-modified run-branch content | accept | Same trust model as the Task verifier (Phase 51): the run branch is TIDE's own orchestrated output; the verifier container is ReadOnly-mounted + credential-stripped, so a malicious gate command cannot push or spend |
| T-52-24 | Elevation of Privilege | verdict fail-open closing a milestone/project on a malformed envelope | mitigate | Unreadable/nil-verdict → exhaustion, never Succeeded; the ClassifyVerdict default arm is the only route for unknown verdicts |
| T-52-25 | DoS | verify re-dispatch loop after approval (infinite billable evaluator runs) | mitigate | ExitReason convergence guard in maybeRunLevelVerify; unit-tested in Task 2 |
| T-52-26 | Repudiation | missing span/provenance for level-verify decisions | mitigate | synthesizeEvaluatorSpan emitted at every terminal with tide.dispatch.<level>.verify naming (D-10) |
</threat_model>

<verification>
- `go build ./...`, unit tests, `go test ./pkg/dispatch/...`, `make lint` all green
</verification>

<success_criteria>
- One shared, repair-free, level-parameterized machine ready for the three controllers
- Convergence + fail-closed guards unit-pinned; envelope doc corrected
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-08-SUMMARY.md` when done.
</output>
