---
phase: 52-per-level-looppolicy-parameterization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/dispatch/podjob/jobspec.go
  - internal/dispatch/podjob/names.go
  - internal/dispatch/podjob/names_test.go
  - internal/dispatch/podjob/jobspec_test.go
  - internal/controller/task_controller.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "A verifier Job can be built for ANY level's parent object — a nil opts.Task no longer panics (RESEARCH Pitfall 1)"
    - "VerifierJobName takes (level, parentUID, attempt) mirroring PlannerJobName's exact signature"
    - "The existing Task verifier keeps working — the Task-loop envtest suite stays green with the new name shape"
  artifacts:
    - path: "internal/dispatch/podjob/names.go"
      provides: "level-generic VerifierJobName"
      contains: "func VerifierJobName(level, parentUID string, attempt int) string"
    - path: "internal/dispatch/podjob/jobspec.go"
      provides: "JobKindVerifier case reading opts.ParentObj like JobKindPlanner"
      contains: "opts.ParentObj"
  key_links:
    - from: "internal/controller/task_controller.go"
      to: "internal/dispatch/podjob/names.go"
      via: "every VerifierJobName call site migrated in the same commit"
      pattern: "VerifierJobName\\(\"task\""
---

<objective>
Generalize `podjob`'s verifier Job construction from Task-only to level-generic (RESEARCH Pitfall 1 — a hard blocker for every non-Task verify dispatch): `case JobKindVerifier:` reads `opts.ParentObj.GetUID()` exactly like `case JobKindPlanner:` already does, and `VerifierJobName` gains `PlannerJobName`'s `(level, parentUID string, attempt int)` signature. The confirmed grep result (planned at plan-write time per RESEARCH Open Question 2): `tide-verifier-` name assertions exist ONLY in `internal/dispatch/podjob/names_test.go` and `jobspec_test.go` — no kind-test or dashboard coupling — so the Job-name format changes to `tide-verifier-<level>-<parentUID>-<attempt>` with no backward-compat special-casing.

Purpose: unblocks plan-check (52-07) and level-verify (52-08/52-10) dispatch; without this, a non-Task caller nil-panics at BuildJobSpec.
Output: level-generic verifier Job building, all existing Task-verifier tests migrated and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (sections: jobspec.go, names.go — contains the exact current broken case and the JobKindPlanner shape to mirror)
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (Pitfall 1, Assumption A3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize VerifierJobName + the JobKindVerifier case</name>
  <files>internal/dispatch/podjob/names.go, internal/dispatch/podjob/jobspec.go, internal/dispatch/podjob/names_test.go, internal/dispatch/podjob/jobspec_test.go</files>
  <read_first>
    - internal/dispatch/podjob/names.go (full — `PlannerJobName` at :41-54 is the signature to mirror; `VerifierJobName` at :56-71 is what changes)
    - internal/dispatch/podjob/jobspec.go lines 240-360 (the Kind switch: `case JobKindPlanner:` at :271-284 is the shape to copy; `case JobKindVerifier:` at :285-303 is what changes; note which labels each case stamps)
    - internal/dispatch/podjob/names_test.go lines 70-110 (the four hard-coded `tide-verifier-…` want-strings to update)
    - internal/dispatch/podjob/jobspec_test.go around line 1066 (the verifier-name assertion + any TestBuildJobSpec_Verifier_* statics asserting on Job name or labels)
  </read_first>
  <action>
    1. `names.go`: change to `func VerifierJobName(level, parentUID string, attempt int) string` returning `fmt.Sprintf("tide-verifier-%s-%s-%d", level, parentUID, attempt)`. Update the doc comment format line to `"tide-verifier-{level}-{parentUID}-{attempt}"` and note it mirrors PlannerJobName. Drop the `types.UID` import if now unused.

    2. `jobspec.go` `case JobKindVerifier:` — rewrite to mirror `case JobKindPlanner:` exactly: read `parentUID` from `opts.ParentObj.GetUID()` when `opts.ParentObj != nil` (remove the `opts.Task` reads entirely from this case); `jobName = VerifierJobName(opts.Level, parentUID, opts.Attempt)`; keep `labels["tideproject.k8s/role"] = "verifier"`; add `labels["tideproject.k8s/level"] = opts.Level`; keep stamping `labels["tideproject.k8s/task-uid"] = parentUID` (Assumption A3 — `verifierInFlightCount` filters on role+project labels, never the task-uid VALUE; keeping the key preserves compat, and the new `level` label disambiguates); keep the `estimated-cost` label block unchanged. Add the per-level `tideproject.k8s/<level>-uid` label mirroring the planner case's `fmt.Sprintf("tideproject.k8s/%s-uid", opts.Level)` stamp (52-09's plan-uid attempt scan and future dashboards rely on it).

    3. Update `names_test.go` want-strings to the three-segment format (e.g. `tide-verifier-task-task-uid-abc-1`) and add one case for a non-task level (e.g. `VerifierJobName("plan", "plan-uid-1", 2)` → `tide-verifier-plan-plan-uid-1-2`). Update `jobspec_test.go`'s verifier-name/label assertions; add a new assertion that `BuildJobSpec` with `Kind: JobKindVerifier`, `ParentObj: <a Plan object>`, `Task: nil` builds successfully (the Pitfall-1 nil-panic regression proof) and stamps `tideproject.k8s/level: plan`.
  </action>
  <verify>
    <automated>go test ./internal/dispatch/podjob/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "opts.Task" internal/dispatch/podjob/jobspec.go` returns 0 matches inside the `case JobKindVerifier:` block (verify by reading the case; other Kind cases may legitimately keep opts.Task)
    - `go test ./internal/dispatch/podjob/...` exits 0
    - A test exists proving `JobKindVerifier` + `Task: nil` + `ParentObj: non-Task` does NOT panic and produces name `tide-verifier-<level>-<uid>-<attempt>`
    - `grep -c "tideproject.k8s/level" internal/dispatch/podjob/jobspec.go` ≥ 2 (planner + verifier cases)
  </acceptance_criteria>
  <done>podjob builds verifier Jobs for any level; unit tests cover both task and non-task callers.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate every Task-side VerifierJobName call site in the same commit</name>
  <files>internal/controller/task_controller.go</files>
  <read_first>
    - Output of `grep -n "VerifierJobName" internal/controller/*.go` (find EVERY call site — known: `checkVerifyingState` ~:665, `dispatchVerifier` ~:2102; there may be others in completion/spend-settling paths)
    - internal/controller/task_controller.go lines 2099-2220 (`dispatchVerifier` — note it ALREADY passes `ParentObj: task` in BuildOptions at ~:2170, so only the name call and any `Task:`-field reliance need attention)
  </read_first>
  <action>
    Update every `VerifierJobName(task.UID, …)` call site to `VerifierJobName("task", string(task.UID), …)`. Confirm `dispatchVerifier`'s `BuildOptions` carries `Level: "task"` and `ParentObj: task` (add `Level` if absent — the Job name and level label now derive from it). Do NOT change any Task-loop behavior: same attempt source (`task.Status.Attempt`), same labels stamped post-BuildJobSpec (the `owner.LabelProject` stamp idiom at ~:2191-2204 stays). Run the Task verify envtest suite to prove the name-shape change is behavior-neutral end-to-end.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run TestControllers --ginkgo.focus='Verif' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VerifierJobName(" internal/controller/task_controller.go` — every match passes `"task"` as the first argument
    - `go build ./...` exits 0 (no lingering old-signature callers anywhere)
    - Ginkgo focus run on verifier specs reports > 0 specs ran (not vacuous — Pitfall 5) and 0 failures
  </acceptance_criteria>
  <done>All call sites migrated; Task-loop verifier envtest specs green under the new three-segment Job name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → Job API | deterministic Job names key idempotent dispatch; collisions or empty segments break the AlreadyExists-as-success contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Spoofing/DoS | empty parentUID in verifier Job name | mitigate | JobKindVerifier case mirrors JobKindPlanner's nil-guard; jobspec_test asserts a non-Task ParentObj produces a fully-populated name (no `tide-verifier--1` collisions across levels) |
| T-52-05 | Tampering | task-uid label carrying a non-Task UID (Assumption A3) | accept | verifierInFlightCount filters on role+project only; the new `level` label makes consumers level-aware; documented in the case's comment |
</threat_model>

<verification>
- `go test ./internal/dispatch/podjob/...` and the Verif-focused envtest suite green
- `make lint` green
</verification>

<success_criteria>
- Non-Task verifier Jobs buildable without panic; Task verifier behavior unchanged at defaults
- Every VerifierJobName caller migrated in the same commit (no compile-broken intermediate state)
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-02-SUMMARY.md` when done.
</output>
