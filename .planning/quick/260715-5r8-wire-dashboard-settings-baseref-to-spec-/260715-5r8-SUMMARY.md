---
phase: quick
plan: 260715-5r8
subsystem: dashboard-api
tags: [go, dashboard, settings, project-crd, baseref]

# Dependency graph
requires:
  - phase: 35-git-base-ref
    provides: "Spec.Git.BaseRef field on the Project CRD (GitConfig)"
  - phase: 37-dashboard-surfaces
    provides: "GET /api/v1/projects/{name}/settings handler with the forward-declared repoSettings.BaseRef stub"
provides:
  - "repoSettings.BaseRef wired to the live Spec.Git.BaseRef schema field"
  - "Round-trip test coverage for both the set-baseRef and git-less-empty-baseRef cases"
affects: [dashboard-frontend-settings-panel]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - cmd/dashboard/api/settings.go
    - cmd/dashboard/api/settings_test.go

key-decisions:
  - "Populated BaseRef inside the existing p.Spec.Git != nil nil-guard alongside RepoURL, rather than adding a second guard — Git is a pointer and nil for git-less Projects"

patterns-established: []

requirements-completed: [IN-02, W-1]

# Metrics
duration: ~10min
completed: 2026-07-15
---

# Quick Task 260715-5r8: Wire dashboard settings baseRef to Spec.Git.BaseRef Summary

**Dashboard settings endpoint now serializes `repo.baseRef` from the live `Spec.Git.BaseRef` schema field instead of the Phase-37 forward-declared empty stub.**

## Performance

- **Duration:** ~10 min
- **Completed:** 2026-07-15
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- `writeSettings` now sets `settings.Repo.BaseRef = p.Spec.Git.BaseRef` inside the existing `p.Spec.Git != nil` nil-guard, so a git-less Project still serves settings with `baseRef == ""` without panicking.
- Stale forward-declaration comments (both the `repoSettings` struct doc and the inline comment in `writeSettings`) replaced with comments describing live behavior, not edit history.
- `settings_test.go` fixture gained `BaseRef: "release/v2"`; `TestSettingsFullyPopulated` now asserts the round-trip; `TestSettingsHonestDefaults` keeps the empty/HEAD-default assertion but drops the stale "field not yet in schema" wording from its failure message.

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire repoSettings.BaseRef to Spec.Git.BaseRef and flip the tests** - `25a90c0` (fix)

_Note: task was marked `tdd="true"` in the plan, but the behavior was additive to already-passing tests (extending existing assertions rather than introducing a new failing test first) — implementation and test changes landed together in one commit._

## Files Created/Modified
- `cmd/dashboard/api/settings.go` - `writeSettings` populates `repoSettings.BaseRef` from `p.Spec.Git.BaseRef` inside the nil-guard; stale forward-declaration comments replaced with live-semantics comments
- `cmd/dashboard/api/settings_test.go` - fixture sets `BaseRef: "release/v2"`; fully-populated test asserts the round-trip; honest-defaults test keeps the empty-case assertion with updated failure message

## Decisions Made
- Populated BaseRef inside the existing `p.Spec.Git != nil` nil-guard alongside `RepoURL`, rather than adding a second guard — Git is a pointer and nil for git-less Projects (matches the plan's explicit instruction).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Prior to task execution, this worktree's HEAD was found behind the plan's expected base commit (`99e399f`, an ancestor of the expected `e79468d`); per the mandatory worktree branch-check protocol, `git reset --hard e79468de8a6e042dc8d0c44d8cf893622c99d162` brought HEAD to the correct base before any file edits.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The dashboard's `ProjectSettingsPanel` "HEAD (default)" fallback now renders correctly only when `baseRef` is genuinely empty — no frontend change was needed since the fallback logic was already correct, only the backend value was stale.
- No blockers. Requirements IN-02 and W-1 are satisfied by this change.

---
*Phase: quick*
*Completed: 2026-07-15*

## Self-Check: PASSED

- FOUND: cmd/dashboard/api/settings.go
- FOUND: cmd/dashboard/api/settings_test.go
- FOUND: .planning/quick/260715-5r8-wire-dashboard-settings-baseref-to-spec-/260715-5r8-SUMMARY.md
- FOUND: 25a90c0 (git log --oneline --all)
