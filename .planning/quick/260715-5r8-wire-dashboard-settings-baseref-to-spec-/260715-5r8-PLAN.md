---
phase: quick
plan: 260715-5r8
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/dashboard/api/settings.go
  - cmd/dashboard/api/settings_test.go
autonomous: true
requirements: [IN-02, W-1]

must_haves:
  truths:
    - "GET /api/v1/projects/{name}/settings returns repo.baseRef equal to Spec.Git.BaseRef when the Project sets one"
    - "A git-less Project (Spec.Git nil) still serves settings with baseRef == \"\" and does not panic"
    - "settings_test.go no longer asserts the stale 'field not yet in schema' behavior"
  artifacts:
    - path: "cmd/dashboard/api/settings.go"
      provides: "BaseRef wired from Spec.Git.BaseRef inside the existing nil-guard"
      contains: "p.Spec.Git.BaseRef"
    - path: "cmd/dashboard/api/settings_test.go"
      provides: "Round-trip assertion for a set baseRef + empty case retained"
      contains: "BaseRef"
  key_links:
    - from: "cmd/dashboard/api/settings.go"
      to: "api/v1alpha3/project_types.go GitConfig.BaseRef"
      via: "p.Spec.Git.BaseRef read in writeSettings"
      pattern: "p\\.Spec\\.Git\\.BaseRef"
---

<objective>
Wire the dashboard settings endpoint's `repo.baseRef` field to the live `Spec.Git.BaseRef` schema field, closing the Phase 37 forward-declaration stub.

Purpose: Phase 37's settings handler (plan 37-07) forward-declared `repoSettings.BaseRef` because `Spec.Git.BaseRef` had not landed yet. The field HAS existed since Phase 35 (`api/v1alpha3/project_types.go:242`, consumed by the controller at `project_controller.go:639`), so the ProjectSettingsPanel now renders "HEAD (default)" even when a baseRef is configured — only the raw-spec YAML card shows the truth. Found by Phase 37's own REVIEW (IN-02) and the v1.0.7 milestone-audit integration check (W-1).

Output: `writeSettings` populates `BaseRef` from `p.Spec.Git.BaseRef` (nil-safe); stale comments updated to live semantics; tests assert the wired round-trip plus the empty/HEAD-default case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@cmd/dashboard/api/settings.go
@cmd/dashboard/api/settings_test.go

<interfaces>
From api/v1alpha3/project_types.go (GitConfig, ~line 232-242):
```go
// BaseRef optionally names the ref the run branch is created from: an
// existing branch name, a tag, or a full commit SHA. Empty means the
// remote HEAD default branch. [...unresolvable refs surface as
// CloneFailed/BaseRefUnresolvable...]
BaseRef string `json:"baseRef,omitempty"`
```
`Project.Spec.Git` is `*GitConfig` — a POINTER, nil for git-less projects. settings.go already nil-guards it at ~line 186 (`if p.Spec.Git != nil { settings.Repo.RepoURL = ... }`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire repoSettings.BaseRef to Spec.Git.BaseRef and flip the tests</name>
  <files>cmd/dashboard/api/settings.go, cmd/dashboard/api/settings_test.go</files>
  <behavior>
    - Test 1 (TestSettingsFullyPopulated): fixture with `Spec.Git.BaseRef` set (e.g. "release/v2") → response `repo.baseRef == "release/v2"` (round-trip through the JSON payload).
    - Test 2 (TestSettingsHonestDefaults): git-less minimal Project (Spec.Git nil) → `repo.baseRef == ""` (HEAD default), handler serves 200 without panicking. Keep this assertion; update its error message to drop the stale "field not yet in schema" wording.
  </behavior>
  <action>
    In cmd/dashboard/api/settings.go:

    1. In `writeSettings` (~line 157-188): remove the stale inline comment "BaseRef intentionally \"\" — Spec.Git.BaseRef not present in schema at 37-07 execution time" from the `repoSettings` literal, and populate BaseRef inside the EXISTING `if p.Spec.Git != nil` nil-guard block (~line 186) alongside `settings.Repo.RepoURL`: `settings.Repo.BaseRef = p.Spec.Git.BaseRef`. Do NOT dereference `p.Spec.Git` in the struct literal itself — Git is a pointer and nil for git-less projects (TestSettingsHonestDefaults exercises exactly that path).

    2. Update the `repoSettings` struct doc comment (~line 50-53): replace the forward-declaration paragraph ("the Phase-35 Spec.Git.BaseRef had not landed at 37-07 execution time... Wire it to Spec.Git.BaseRef once that field exists") with live semantics: BaseRef mirrors Spec.Git.BaseRef; empty means the remote HEAD default branch (the UI renders the HEAD-default label). Match the file's existing comment voice — behavior, not edit history.

    In cmd/dashboard/api/settings_test.go:

    3. Add `BaseRef: "release/v2"` to the `GitConfig` literal in `fullSettingsProject()` (~line 60-63).

    4. In `TestSettingsFullyPopulated` (~line 109), extend the repo assertion to also check `ps.Repo.BaseRef != "release/v2"` → error (fold into the existing `ps.Repo.RepoURL/BranchName` check or add adjacent).

    5. In `TestSettingsHonestDefaults` (~line 205-206), KEEP the `ps.Repo.BaseRef != ""` assertion (git-less project → HEAD default) but rewrite the failure message to `want empty (HEAD default)` — drop "field not yet in schema".

    Scope note: Go-only change per constraints — NO frontend/SPA edits (ProjectSettingsPanel.tsx's "HEAD (default)" fallback renders only when baseRef is empty, which is now correct behavior), NO embed regen, NO chart changes.
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/... && go vet ./cmd/dashboard/...</automated>
  </verify>
  <done>
    `go test ./cmd/dashboard/...` green with the new BaseRef round-trip assertion passing; `go vet ./cmd/dashboard/...` clean; `grep -n 'p.Spec.Git.BaseRef' cmd/dashboard/api/settings.go` returns exactly one hit inside the nil-guard; no occurrence of "not yet in schema" or "once that field exists" remains in cmd/dashboard/api/.
  </done>
</task>

</tasks>

<verification>
- `go test ./cmd/dashboard/...` exits 0 (all three settings tests pass: fully-populated with BaseRef round-trip, redaction, honest-defaults with empty BaseRef).
- `go vet ./cmd/dashboard/...` exits 0.
- `grep -rn "not yet in schema\|once that field exists" cmd/dashboard/api/` returns nothing — stale forward-declaration language fully purged.
- `git diff --stat` touches only cmd/dashboard/api/settings.go and cmd/dashboard/api/settings_test.go.
</verification>

<success_criteria>
- Settings endpoint serializes `repo.baseRef` from `Spec.Git.BaseRef`; empty/nil-Git serializes `""` (UI's HEAD-default label is now honest).
- Test suite locks the WIRED behavior in both directions (set → round-trips; unset → empty).
- No frontend, embed, or chart files modified.
</success_criteria>

<output>
Create `.planning/quick/260715-5r8-wire-dashboard-settings-baseref-to-spec-/260715-5r8-SUMMARY.md` when done.
</output>
