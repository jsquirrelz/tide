---
phase: quick-260715-4jd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/kind/push_lease_test.go
  - test/integration/kind/medium_http_test.go
  - test/integration/kind/credproxy_test.go
  - test/integration/kind/integration_miss_test.go
  - test/integration/kind/wave_test.go
autonomous: true
requirements: [QUICK-260715-4jd]

must_haves:
  truths:
    - "push_lease_test.go's teardown blocks until push-lease-test is NotFound, so the next spec's BeforeEach never recreates a Terminating namespace"
    - "Every multi-spec kind test file that reuses a shared namespace constant waits for full namespace deletion in its teardown"
    - "Single-spec kind test files retain fire-and-forget deleteNamespace (no successor collision exists for them)"
  artifacts:
    - path: "test/integration/kind/push_lease_test.go"
      provides: "AfterEach blocking teardown"
      contains: "deleteNamespaceAndWait(pushLeaseNS)"
    - path: "test/integration/kind/credproxy_test.go"
      provides: "AfterEach blocking teardown"
      contains: "deleteNamespaceAndWait(credproxyNS)"
    - path: "test/integration/kind/wave_test.go"
      provides: "AfterEach blocking teardown for both namespaces"
      contains: "deleteNamespaceAndWait(testNS)"
    - path: "test/integration/kind/integration_miss_test.go"
      provides: "AfterAll blocking teardown"
      contains: "deleteNamespaceAndWait(integrationMissNamespace)"
    - path: "test/integration/kind/medium_http_test.go"
      provides: "AfterAll blocking teardown"
      contains: "deleteNamespaceAndWait(mediumHTTPNamespace)"
  key_links:
    - from: "test/integration/kind/push_lease_test.go"
      to: "test/integration/kind/suite_test.go deleteNamespaceAndWait (line ~1037)"
      via: "direct call in AfterEach"
      pattern: "deleteNamespaceAndWait\\(pushLeaseNS\\)"
    - from: "test/integration/kind/wave_test.go"
      to: "test/integration/kind/suite_test.go deleteNamespaceAndWait"
      via: "two direct calls in AfterEach (testNS and fixtureNS)"
      pattern: "deleteNamespaceAndWait\\((testNS|fixtureNS)\\)"
---

<objective>
Fix the kind-suite namespace-Terminating recreate race by swapping the fire-and-forget `deleteNamespace()` teardown calls to the blocking `deleteNamespaceAndWait()` in the five multi-spec kind test files that reuse a shared namespace constant.

Purpose: push_lease_test.go's four specs share the namespace constant `push-lease-test`. Its AfterEach calls `deleteNamespace()` (kubectl delete with `--timeout=30s`, returns while the namespace is still Terminating), so the next spec's BeforeEach recreated the namespace mid-termination — creates rejected, the tide-projects PVC never appeared, and pvcPrewarmPod's 60s Bound wait timed out (observed phase `""` = NotFound throughout). Verified 2026-07-15 on HEAD c09937f; focused rerun on a fresh cluster was 4/4 green, confirming non-determinism. The same race class exists in every multi-spec file with a shared namespace constant.

Output: 6 call-site swaps across 5 test files. No helper changes, no other files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@test/integration/kind/suite_test.go

<interfaces>
<!-- Both helpers live in test/integration/kind/suite_test.go. Drop-in swap: identical signature. -->

From test/integration/kind/suite_test.go:

```go
// line ~1016 — fire-and-forget: kubectl delete --timeout=30s returns while the
// namespace may still be Terminating.
func deleteNamespace(ns string)

// line ~1037 — issues the delete, then polls k8sClient.Get until NotFound
// (3-min budget, 5s interval). Honors KEEP_KIND_NAMESPACES=true the same way.
// Doc comment: "namespace %s must be fully deleted (NotFound) before next spec".
func deleteNamespaceAndWait(ns string)
```

Precedent: import_resume_test.go already uses `deleteNamespaceAndWait` for exactly this reason.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap 6 deleteNamespace call sites to deleteNamespaceAndWait in the 5 multi-spec kind test files</name>
  <files>test/integration/kind/push_lease_test.go, test/integration/kind/medium_http_test.go, test/integration/kind/credproxy_test.go, test/integration/kind/integration_miss_test.go, test/integration/kind/wave_test.go</files>
  <action>
    Replace `deleteNamespace(...)` with `deleteNamespaceAndWait(...)` at exactly these six call sites (line numbers verified on HEAD c09937f; treat as hints, match on the call text):

    1. push_lease_test.go:63 — `deleteNamespace(pushLeaseNS)` in AfterEach (the observed failure: 4 specs share `push-lease-test`)
    2. credproxy_test.go:64 — `deleteNamespace(credproxyNS)` in AfterEach
    3. wave_test.go:92 — `deleteNamespace(testNS)` in AfterEach
    4. wave_test.go:93 — `deleteNamespace(fixtureNS)` in the same AfterEach (both namespaces, not just the first)
    5. integration_miss_test.go:151 — `deleteNamespace(integrationMissNamespace)` in AfterAll
    6. medium_http_test.go:293 — `deleteNamespace(mediumHTTPNamespace)` in AfterAll

    Note: sites 5 and 6 are in AfterAll blocks, not AfterEach — the swap is identical (blocking teardown prevents cross-file contention when the sequential suite's next file starts heavy workloads; the helper's own doc comment covers this case).

    Comment accuracy (same files, comment-only): update the two comments that name the swapped helper so they stay truthful — integration_miss_test.go:165 ("cleanup belongs to AfterAll's deleteNamespace") and medium_http_test.go:467 ("deleteNamespace; this is the container's final spec") should reference `deleteNamespaceAndWait` — write the bare name with NO parentheses in these comments (a parenthesized mention would trip the count gate in Verify). No other prose changes.

    Do NOT touch: suite_test.go (both helpers stay as-is), single-spec kind test files (their namespaces are distinct — no successor collision — fire-and-forget is correct there), or any file outside the five listed.
  </action>
  <verify>
    <automated>go vet ./test/integration/kind/ && [ "$(grep -hE 'deleteNamespace\(' test/integration/kind/push_lease_test.go test/integration/kind/medium_http_test.go test/integration/kind/credproxy_test.go test/integration/kind/integration_miss_test.go test/integration/kind/wave_test.go | grep -cv 'deleteNamespaceAndWait(')" = "0" ] && [ "$(grep -hc 'deleteNamespaceAndWait(' test/integration/kind/push_lease_test.go test/integration/kind/medium_http_test.go test/integration/kind/credproxy_test.go test/integration/kind/integration_miss_test.go test/integration/kind/wave_test.go | paste -sd+ - | bc)" = "6" ]</automated>
  </verify>
  <done>All six call sites in the five files call deleteNamespaceAndWait; no bare deleteNamespace( call remains in those five files; `go vet ./test/integration/kind/` (which compiles the package) exits 0. No kind cluster run required in this task — the orchestrator runs the full suite separately after commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Test-harness-only change; no production code, no input handling, no new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q4jd-01 | DoS | kind test teardown | accept | deleteNamespaceAndWait's 3-min Eventually budget bounds worst-case added wall time per teardown (~6 sites); on healthy clusters namespaces vanish in <30s |
</threat_model>

<verification>
- `go vet ./test/integration/kind/` exits 0 (compiles all kind test files; package has no build tags).
- Scoped grep across ONLY the five modified files: zero bare `deleteNamespace(` calls remain (the `grep -v 'deleteNamespaceAndWait('` filter excludes the new calls; comment references at medium_http_test.go and integration_miss_test.go carry no `(` or are updated, so they don't false-positive).
- Exactly 6 `deleteNamespaceAndWait(` calls added across the five files (1+1+2+1+1).
- suite_test.go diff is empty (`git diff --stat test/integration/kind/suite_test.go` shows nothing).
</verification>

<success_criteria>
- The six teardown call sites block on NotFound before the next spec/file proceeds, eliminating the Terminating-recreate race observed in push_lease_test.go.
- No helper modified, no single-spec file modified, no file outside the five touched.
- Package compiles clean via go vet.
</success_criteria>

<output>
Create `.planning/quick/260715-4jd-fix-kind-suite-namespace-terminating-rec/260715-4jd-SUMMARY.md` when done.
</output>
