---
phase: quick-260610-x3d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/projects/dogfood/README.md
  - examples/projects/dogfood/01-analytics-project.yaml
  - examples/projects/dogfood/02-codex-runtime-project.yaml
  - examples/projects/dogfood/03-project-editor-project.yaml
  - api/v1alpha1/dogfood_manifests_test.go
autonomous: true
requirements: [TODO-draft-dogfood-project-crd-prompts]

must_haves:
  truths:
    - "Three apply-ready Project CR manifests exist under examples/projects/dogfood/, one per dogfood run, each schema-valid against api/v1alpha1.Project"
    - "Each outcomePrompt encodes its project's locked decisions (Prometheus-is-the-DB, per-level runtime selection, create+edit-drafts-only) so TIDE's planner cannot reinvent them"
    - "Each outcomePrompt poses — and does not answer — the open research questions flagged in .planning/research/questions.md"
    - "No manifest carries inline credential material; all secrets are referenced by name only"
    - "Run order (analytics → codex → editor) is encoded in numbered filenames and documented in README, since the Project CRD has no ordering mechanism"
  artifacts:
    - path: "examples/projects/dogfood/01-analytics-project.yaml"
      provides: "Analytics dogfood Project CR (Namespace + Project docs)"
      contains: "outcomePrompt"
    - path: "examples/projects/dogfood/02-codex-runtime-project.yaml"
      provides: "Codex runtime dogfood Project CR"
      contains: "outcomePrompt"
    - path: "examples/projects/dogfood/03-project-editor-project.yaml"
      provides: "Dashboard Project-editor dogfood Project CR"
      contains: "outcomePrompt"
    - path: "examples/projects/dogfood/README.md"
      provides: "Apply order, per-namespace prerequisites, secret creation steps"
    - path: "api/v1alpha1/dogfood_manifests_test.go"
      provides: "Strict-decode guard test proving manifests validate against the real Go types"
  key_links:
    - from: "api/v1alpha1/dogfood_manifests_test.go"
      to: "examples/projects/dogfood/*.yaml"
      via: "filepath.Glob + sigs.k8s.io/yaml UnmarshalStrict into v1alpha1.Project"
      pattern: "UnmarshalStrict"
    - from: "examples/projects/dogfood/*.yaml"
      to: "tideproject.k8s/v1alpha1"
      via: "apiVersion field (never tide.io, never placeholders)"
      pattern: "tideproject\\.k8s/v1alpha1"
---

<objective>
Draft the three TIDE-on-TIDE dogfood `Project` CR manifests — analytics, Codex
runtime, dashboard Project editor — as apply-ready, schema-valid YAML whose
outcome prompts seed TIDE's own milestone planner with the locked decisions
from `.planning/notes/tide-on-tide-dogfood-targets.md`.

Purpose: before TIDE can drive these internal projects, each needs a Project CR
encoding outcome + constraints so the planner doesn't reinvent settled
decisions, while leaving the flagged open questions genuinely open.
Output: `examples/projects/dogfood/` directory (3 manifests + README) plus a
guard test that strict-decodes every manifest against the real Go types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/notes/tide-on-tide-dogfood-targets.md
@.planning/todos/pending/draft-dogfood-project-crd-prompts.md
@.planning/research/questions.md
@api/v1alpha1/project_types.go
@examples/projects/large/project.yaml
@examples/projects/medium/project.yaml

<interfaces>
<!-- Confirmed from api/v1alpha1/project_types.go — drafts must use exactly these field names. -->

ProjectSpec (apiVersion: tideproject.k8s/v1alpha1, kind: Project, Namespaced):

```go
type ProjectSpec struct {
    TargetRepo         string              `json:"targetRepo"`          // required; http(s):// or git@ (CEL-validated)
    OutcomePrompt      string              `json:"outcomePrompt,omitempty"` // multi-line YAML literal `|` is the convention
    ProviderSecretRef  string              `json:"providerSecretRef,omitempty"` // Secret NAME only
    Budget             BudgetConfig        `json:"budget,omitempty"`    // absoluteCapCents, rollingWindowCapCents, rollingWindowDuration ("24h")
    PlanAdmission      PlanAdmissionConfig `json:"planAdmission,omitempty"` // fileTouchMode: strict|warn
    MaxAttemptsPerTask int32               `json:"maxAttemptsPerTask,omitempty"` // 1..10
    Subagent           SubagentConfig      `json:"subagent,omitempty"`  // image, model, levels{milestone,phase,plan,task}
    Git                *GitConfig          `json:"git,omitempty"`       // repoURL, credsSecretRef (Secret NAME, data key GIT_PAT)
    Gates              Gates               `json:"gates,omitempty"`     // milestone/phase/plan/task: auto|approve|pause; pauseBetweenWaves bool
}
```

Conventions from the large sample (closest precedent — also targets the TIDE repo):
- Two YAML docs per file: Namespace first, then Project, separated by `---`
- Labels: `app.kubernetes.io/name: tide`, `tideproject.k8s/sample: <name>`
- providerSecretRef + git.credsSecretRef name `tide-secrets` (created out-of-band, README step)
- subagent: `image: ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0`
- `rollingWindowDuration: 24h` (metav1.Duration string — NOT a seconds integer)
- Heavy explanatory comments tracing each field to its decision record
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three dogfood Project CR manifests + README</name>
  <files>examples/projects/dogfood/README.md, examples/projects/dogfood/01-analytics-project.yaml, examples/projects/dogfood/02-codex-runtime-project.yaml, examples/projects/dogfood/03-project-editor-project.yaml</files>
  <action>
Create `examples/projects/dogfood/` with three apply-ready manifests following
the large-sample structure exactly (Namespace doc + Project doc per file,
sample-style explanatory comments, `tideproject.k8s/v1alpha1` API group —
NEVER `tide.io` or placeholders).

**Shared CR scaffolding (all three files):**
- Namespaces: `tide-dogfood-analytics`, `tide-dogfood-codex`, `tide-dogfood-editor`.
  Project names: `dogfood-analytics`, `dogfood-codex-runtime`, `dogfood-project-editor`.
  Label `tideproject.k8s/sample: dogfood-<name>`.
- `targetRepo: https://github.com/jsquirrelz/tide.git` and `git.repoURL` the same;
  `providerSecretRef: tide-secrets` and `git.credsSecretRef: tide-secrets` —
  Secret NAMES only, created out-of-band per README. NEVER inline `data:` /
  `stringData:` or raw key material anywhere in these files.
- `budget`: `absoluteCapCents: 10000` (analytics, codex) / `7500` (editor),
  `rollingWindowCapCents` equal to the cap, `rollingWindowDuration: 24h`. Comment:
  "starting cap — tune before applying; dogfood runs are multi-phase real work."
- `subagent`: `image: ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0`,
  `model: claude-opus-4-7` (large-sample precedent for real feature work; comment
  notes claude-sonnet-4-6 / claude-haiku-4-5 as cheaper alternatives).
- `gates`: `milestone: approve`, `phase: approve`, `plan: auto`, `task: auto`,
  `pauseBetweenWaves: false` — dogfood runs get human oversight at the top two
  levels; comment that fully-auto is one edit away.
- `planAdmission.fileTouchMode: strict`, `maxAttemptsPerTask: 3`.
- Each file's header comment states its position in the run order and that the
  next manifest should be applied only after this Project reaches
  `status.phase: Complete` (the CRD has no ordering mechanism — sequencing is
  operator discipline).

**outcomePrompt content — written FOR TIDE's milestone-planner subagent:
outcome-focused with explicit constraints and explicitly-posed open questions,
like a good PROJECT.md seed. NOT a step-by-step task list (contrast with the
medium/large samples, whose tight numbered-deliverable shape was a cost-control
mechanism for single-phase runs — these are multi-phase outcomes).**

*01-analytics* must encode (per the dogfood-targets note — locked, do not soften):
- Outcome: run telemetry & cost — token spend, wall-clock, dispatch counts,
  failure rates at project/phase/wave granularity — plus new React dashboard
  surfaces showcasing them.
- **Prometheus is the DB.** Orchestrator exposes everything as Prometheus
  metrics via prometheus/client_golang (already in the stack); the dashboard
  queries Prometheus for history. The CRD-`.status`-only persistence constraint
  stays untouched — no new DB, no SQLite, no schedule caching in `.status`.
- The chart default is `prometheus.enabled=false`: graceful degradation to
  live-only CRD-`.status` views must be first-class, not an error state.
- **Label-cardinality budget:** project/phase/wave labels are fine; per-task
  labels are FORBIDDEN (cardinality explosion). Per-task detail stays in CRD
  `.status` / OTel spans.
- Open questions the prompt POSES for TIDE's planner to research (verbatim
  intent from .planning/research/questions.md — do not pre-answer): PromQL
  proxy through the existing chi `manager.Runnable` server vs the dashboard
  hitting Prometheus directly; where the cardinality budget is guarded (metric
  registration review vs lint); retention expectations for multi-day
  cost-over-time charts.

*02-codex-runtime* must encode:
- Outcome: a second `Subagent` implementation in `internal/subagent/codex/`
  mirroring the `internal/subagent/anthropic/` layering, with real heterogeneous
  dispatch — actually running mixed-provider waves, not just proving the interface.
- **Per-level runtime selection:** planner pool and executor pool each pin a
  runtime (e.g. Claude plans, Codex executes). Per-task selection rejected
  (large API/scheduler surface); per-project rejected (no mixing within a run).
- Credential path: `OPENAI_API_KEY`/`CODEX_API_KEY` via a K8s Secret (the
  established Secret-ref pattern; never mounted host config, never headless
  OAuth), plus the corresponding chart values.
- Mixed-provider waves keep IDENTICAL wave-boundary failure semantics (spec
  §"Failure handling at wave boundaries") — failed task → siblings continue,
  dependents never dispatch.
- Codex CLI facts to carry verbatim so the planner doesn't re-research:
  `codex exec` is first-class headless (one-shot, no TTY, final message to
  stdout); `--output-schema <file>` constrains the final response to a JSON
  schema — use it for child-CRD emission (child-CRD format is JSON, mandated);
  `--json` emits a JSONL event stream; `--ephemeral`, `--skip-git-repo-check`,
  `--ignore-user-config` for clean containers; sandbox defaults read-only so
  the runner needs `--sandbox workspace-write` (fine inside an already-isolated
  pod); API-key env auth is the documented container path.
- Constraint: all OpenAI/Codex-specific code stays behind the `Subagent`
  interface — no provider hard-coding in the controller.

*03-project-editor* must encode:
- Outcome: an authoring surface in the React dashboard for `Project` CRs —
  create a new Project (outcome prompt, target repo, Secret ref, gate config)
  and save → backend creates the CR. Plus editing DRAFT Projects (saved but
  not yet running). Editing in-flight Projects is OUT of scope.
- **Reference-only credentials:** the form offers a picker over existing Secret
  NAMES in the namespace; Secrets are created out-of-band via kubectl; secret
  material never transits the dashboard API or the browser.
- First write path in the dashboard: create/update mutation endpoints on the
  existing chi server (today's surface is read + SSE) and RBAC additions
  (create/update on Projects) for the manager's ServiceAccount.
- A draft/pending state the reconciler honors — saved-but-not-running lives ON
  the CR per artifacts-as-truth, not in dashboard state. The API shape is an
  OPEN design question the prompt poses, not answers: `spec.paused`-style
  boolean vs annotation vs phase-based gating (Flux/CAPI precedent is
  `spec.paused`/`spec.suspend`); edit semantics (server-side apply with a
  dashboard-owned field manager vs full replace, kubectl-edit conflicts);
  draft→running transition semantics (one-way for v1?); whether CEL
  `x-kubernetes-validations` with `oldSelf` should enforce post-draft
  immutability.
- **Trust the perimeter for v1:** no app-level auth on the new mutation
  endpoints; the deliverable includes DOCUMENTING that the dashboard must sit
  behind port-forward/ingress auth (hardening path already captured as the
  `dashboard-mutation-auth-hardening.md` seed).

**README.md** (match examples/projects/README.md voice — tight, declarative):
purpose (TIDE-on-TIDE dogfood runs, distinct audience from the cost-tier
samples); run-order table (analytics → codex → editor) with the rationale from
the dogfood-targets note (each run becomes observable/authorable through what
the previous run built); per-namespace prerequisites referencing
`examples/projects/medium/per-namespace-resources.yaml` as the template
(tide-projects PVC + tide-subagent SA + tide-signing-key mirror); secret
creation via `kubectl create secret generic tide-secrets
--from-literal=ANTHROPIC_API_KEY=... --from-literal=GIT_PAT=...` (placeholders,
never real values); the "apply next only after previous reaches Complete" rule;
pointer to the constrained-VM cluster-sizing recipe in CLAUDE.md.
  </action>
  <verify>
    <automated>test -f examples/projects/dogfood/README.md && ls examples/projects/dogfood/0*.yaml | wc -l | grep -q 3 && grep -l 'tideproject.k8s/v1alpha1' examples/projects/dogfood/0*.yaml | wc -l | grep -q 3 && ! grep -rE 'stringData:|sk-ant-|sk-proj-' examples/projects/dogfood/</automated>
  </verify>
  <done>Three numbered manifests + README exist under examples/projects/dogfood/; every manifest uses the tideproject.k8s/v1alpha1 group; each outcomePrompt encodes its locked decisions and poses (not answers) its open questions; no inline secret material anywhere in the directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard test — strict-decode the dogfood manifests against the real Go types</name>
  <files>api/v1alpha1/dogfood_manifests_test.go</files>
  <behavior>
    - Test 1: every `examples/projects/dogfood/0*.yaml` file is found by glob (count == 3) and every YAML document in each file parses
    - Test 2: each file's Project document strict-decodes into `v1alpha1.Project` via `sigs.k8s.io/yaml.UnmarshalStrict` (typo'd field names fail the test — this is the schema-validity proof without a cluster)
    - Test 3: per Project — `apiVersion == "tideproject.k8s/v1alpha1"`, `spec.targetRepo == "https://github.com/jsquirrelz/tide.git"`, `spec.outcomePrompt` non-empty, `spec.providerSecretRef` non-empty, `spec.git.credsSecretRef` non-empty
    - Test 4: no raw document in any file contains a `stringData` or `data` top-level key (no inline Secret material — reference-only credentials)
  </behavior>
  <action>
Add `dogfood_manifests_test.go` to the `v1alpha1` package (precedent:
`aggregates_guard_test.go` already hosts guard-style tests there). Glob
`../../examples/projects/dogfood/0*.yaml`, split each file on YAML document
boundaries (`"\n---"` after stripping leading doc separators, or a
`yaml.NewYAMLOrJSONDecoder` from k8s.io/apimachinery — pick whichever the
repo's existing tests already import), identify the `kind: Project` document
per file, and run the four behaviors above. Use `sigs.k8s.io/yaml`
(already a transitive dep via controller-runtime — confirm in go.mod before
importing; if absent, decode via `k8s.io/apimachinery/pkg/util/yaml` into
JSON then `json.Unmarshal` with `DisallowUnknownFields`). Standard go test,
not Ginkgo — matches the existing guard-test shape in this package.
  </action>
  <verify>
    <automated>go test ./api/v1alpha1/ -run DogfoodManifests -v 2>&1 | grep -E '^(=== RUN|--- (PASS|FAIL)|ok|FAIL)' && go test ./api/v1alpha1/ -run DogfoodManifests -count=1</automated>
  </verify>
  <done>`go test ./api/v1alpha1/ -run DogfoodManifests` passes; the test fails if a manifest field name drifts from the Go schema, a required field empties out, or inline secret material appears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public GitHub | manifests are committed to a public, Apache-2.0 repo |
| manifest → cluster | CRs are applied verbatim; outcome prompts are consumed by an LLM planner with repo write access (to a run branch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q260610-01 | Information Disclosure | dogfood manifests | mitigate | reference-only Secret names; guard test asserts no `data`/`stringData` keys; verify grep rejects `sk-ant-`/`sk-proj-` patterns |
| T-q260610-02 | Tampering | outcomePrompt scope | mitigate | budget `absoluteCapCents` hard cap + `gates.milestone/phase: approve` keep a human in the loop before TIDE acts on each prompt |
| T-q260610-03 | Elevation of Privilege | editor prompt's RBAC additions | accept | prompt explicitly scopes new RBAC to create/update on Projects and documents perimeter-trust; hardening seed already filed |
</threat_model>

<verification>
- `ls examples/projects/dogfood/` shows README.md + three numbered manifests
- `go test ./api/v1alpha1/ -run DogfoodManifests -count=1` exits 0
- `grep -c 'outcomePrompt' examples/projects/dogfood/0*.yaml` shows ≥1 per file
- `grep -rn 'tide\.io' examples/projects/dogfood/` returns nothing (domain rule)
- Each prompt poses its open questions: `grep -il 'open question' examples/projects/dogfood/01-*.yaml examples/projects/dogfood/03-*.yaml` matches both
</verification>

<success_criteria>
- Three apply-ready, strict-decode-valid Project CR manifests exist with outcome prompts encoding every locked decision from the dogfood-targets note (Prometheus-is-the-DB + cardinality budget; codex/ Subagent impl + per-level selection + Codex CLI facts; create+edit-drafts + reference-only secrets + perimeter trust)
- The draft-state API shape and Prometheus-integration questions are posed as open questions inside the relevant prompts, not pre-decided
- Run order is encoded in filenames and documented in README with the no-ordering-mechanism caveat
- Guard test green; no inline credential material anywhere in the new directory
</success_criteria>

<output>
Create `.planning/quick/260610-x3d-draft-the-three-tide-on-tide-dogfood-pro/260610-x3d-SUMMARY.md` when done
</output>
