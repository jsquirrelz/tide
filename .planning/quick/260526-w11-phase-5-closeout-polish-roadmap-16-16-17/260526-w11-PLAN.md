---
phase: quick-260526-w11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/dashboard/api/plans.go
  - cmd/dashboard/api/tasks.go
  - .planning/ROADMAP.md
  - .planning/phases/05-distribution-self-hosting-acceptance/deferred-items.md
autonomous: true
requirements: [POLISH-1, POLISH-2]
tags: [quick, phase-5, closeout-polish, gofmt-drift, roadmap-reconciliation]

must_haves:
  truths:
    - "gofmt -l cmd/dashboard/api/ returns empty (no drift)"
    - "go build ./... succeeds post-format"
    - "ROADMAP.md Progress row for Phase 5 reads 17/17 (reconciles with the 17 ticked plan rows above)"
    - "deferred-items.md gofmt entry is marked RESOLVED with date + commit SHA"
  artifacts:
    - path: "cmd/dashboard/api/plans.go"
      provides: "gofmt-clean struct field alignment for planTaskCard"
    - path: "cmd/dashboard/api/tasks.go"
      provides: "gofmt-clean struct field alignment for AttemptMax in task detail shape"
    - path: ".planning/ROADMAP.md"
      provides: "Progress table row body-reconciliation: 16/16 → 17/17 for Phase 5"
    - path: ".planning/phases/05-distribution-self-hosting-acceptance/deferred-items.md"
      provides: "RESOLVED marker for the gofmt-drift entry (date + commit SHA)"
  key_links:
    - from: "Plan 05-01's deferred gofmt entry (deferred-items.md:7)"
      to: "this quick task's atomic gofmt commit"
      via: "RESOLVED marker references the gofmt commit SHA"
      pattern: "RESOLVED 2026-05-27.*quick task 260526-w11"
    - from: ".planning/ROADMAP.md Progress row (line 306)"
      to: ".planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md §Deviations #2"
      via: "Deviation #2 noted the 16/16 vs 17/17 mismatch; this fix reconciles it to 17/17"
      pattern: "5\\. Distribution & Self-Hosting Acceptance \\| 17/17"
---

<objective>
Two cosmetic fixes deferred from Phase 5 closeout, plus the bookkeeping flip.

Purpose: close the gofmt-drift item and the ROADMAP 16/16 vs 17/17 mismatch before the maintainer tags `v1.0.0-rc.1`. Both items were pre-identified in `deferred-items.md` and `05-SUMMARY.md §Deviations` for a `/gsd:quick` pass; this is that pass.

Output:
- Two gofmt-clean Go files under `cmd/dashboard/api/`
- ROADMAP Progress row reconciled to `17/17`
- `deferred-items.md` gofmt entry marked `✅ RESOLVED 2026-05-27 (quick task 260526-w11, commit <SHA>)`

Scope guardrails (per the spawning constraints):
- DOES NOT touch `charts/tide/values.yaml` or any chart-tree file — chart is the FIXED contract per Phase 02.2's anti-pattern.
- DOES NOT touch `cmd/dashboard/api/*.go` Apache-2.0 headers — they are already in place; verify-license gate is unaffected.
- Executor commits ONLY code/config changes (Task 1 + Task 2). The orchestrator handles docs commits (PLAN.md, deferred-items.md flip) in workflow step 8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-distribution-self-hosting-acceptance/deferred-items.md
@./CLAUDE.md

<interfaces>
<!-- Current gofmt drift, captured 2026-05-26 -- these are the exact diffs to land. -->

In cmd/dashboard/api/plans.go (planTaskCard struct, lines 52-58):
```
-	Name       string   `json:"name"`
-	Phase      string   `json:"phase"`
-	WaveIndex  int      `json:"waveIndex"`
-	Attempt    int      `json:"attempt"`
-	DependsOn  []string `json:"dependsOn"`
+	Name      string   `json:"name"`
+	Phase     string   `json:"phase"`
+	WaveIndex int      `json:"waveIndex"`
+	Attempt   int      `json:"attempt"`
+	DependsOn []string `json:"dependsOn"`
```

In cmd/dashboard/api/tasks.go (task detail struct, ~line 78):
```
-	AttemptMax  int             `json:"attemptMax"`
+	AttemptMax   int             `json:"attemptMax"`
```

Note: both files already carry the Apache-2.0 license header — only struct field alignment is drifting. `make verify-license` gate unaffected.

In .planning/ROADMAP.md (line 306):
```
-| 5. Distribution & Self-Hosting Acceptance | 16/16 | Complete | 2026-05-23 |
+| 5. Distribution & Self-Hosting Acceptance | 17/17 | Complete | 2026-05-23 |
```

The plan-rows section above (line 260: `**Plans:** 16/16 plans executed`) refers to EXECUTION plans only — leave that unchanged. The 17 individual `- [x] 05-NN-PLAN.md` rows below it are already ticked and remain authoritative. The Progress table row's `17/17` should match the row-count reality (16 execution + 1 closeout = 17, all ticked).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gofmt cmd/dashboard/api/{plans,tasks}.go + atomic commit</name>
  <files>cmd/dashboard/api/plans.go, cmd/dashboard/api/tasks.go</files>
  <action>
    Run `gofmt -w cmd/dashboard/api/plans.go cmd/dashboard/api/tasks.go` from the repo root. Confirm with `gofmt -l cmd/dashboard/api/` returning empty (zero output). Then run `go build ./...` to confirm the package still compiles (the changes are alignment-only, so build is guaranteed but verify anyway per CLAUDE.md "Verify Before Claiming"). Do NOT edit the file headers; Apache-2.0 headers stay byte-for-byte identical. Do NOT touch any other file in this commit. Commit shape: `style(quick-260526-w11): gofmt cmd/dashboard/api/{plans,tasks}.go struct alignment` — body should reference Phase 05-01's deferred entry by file path and note that verify-license gate is unaffected (headers untouched). The orchestrator will commit docs changes (deferred-items.md flip, ROADMAP, SUMMARY, PLAN.md) in workflow step 8 — Task 1 commit is code-only.
  </action>
  <verify>
    <automated>gofmt -l cmd/dashboard/api/ 2>&1 | grep -v '^$' | wc -l | grep -q '^[[:space:]]*0[[:space:]]*$' && go build ./... 2>&1 | (grep -v '^$' && exit 1 || exit 0)</automated>
  </verify>
  <done>gofmt -l cmd/dashboard/api/ returns empty AND go build ./... succeeds AND a single commit lands with message starting `style(quick-260526-w11):` touching exactly the two .go files.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP Progress row 16/16 → 17/17 + atomic commit</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Edit `.planning/ROADMAP.md` line 306. Change the Progress table row from `| 5. Distribution & Self-Hosting Acceptance | 16/16 | Complete | 2026-05-23 |` to `| 5. Distribution & Self-Hosting Acceptance | 17/17 | Complete | 2026-05-23 |`. This reconciles the Progress row with the 17 ticked plan rows above it (16 execution + 1 closeout = 17 plans total, all `[x]`). Do NOT change line 260 (`**Plans:** 16/16 plans executed`) — that phrasing scopes to execution plans only and is correct as-is. Do NOT tick the top-level overview phase checkboxes (lines 19-23) — 05-SUMMARY.md Deviations #3 explicitly documents that those overview boxes are left unticked across all phase closeouts (precedent). Single-line edit; nothing else in ROADMAP.md should change. Commit shape: `docs(quick-260526-w11): reconcile ROADMAP Phase 5 Progress row to 17/17` — body should reference 05-SUMMARY.md §"Deviations from PLAN.md" #2 (which documented the mismatch) and note that the 17 individual plan rows above remain unchanged (already ticked).

    NOTE: The orchestrator's spawn-context constraint says "executor MUST NOT commit docs artifacts" — interpret "docs artifacts" as PLAN.md / SUMMARY.md / STATE.md / deferred-items.md (the GSD planning artifacts the orchestrator manages in step 8). ROADMAP.md is the project's project-state file, and the spawn-context EXPLICITLY says "ROADMAP.md row reconciliation" is a fix this quick task is closing — therefore THIS commit (the ROADMAP edit) lands in the executor's scope as a non-planning-artifact code/config commit. If interpretation is ambiguous at execution time, prefer the spawn-context's literal task substance ("Fix 1 — ROADMAP.md row reconciliation"). The orchestrator will still handle deferred-items.md in step 8.
  </action>
  <verify>
    <automated>grep -cE '^\| 5\. Distribution & Self-Hosting Acceptance \| 17/17 \| Complete \| 2026-05-23 \|$' .planning/ROADMAP.md | grep -q '^1$' && ! grep -E '^\| 5\. Distribution & Self-Hosting Acceptance \| 16/16 \|' .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP Progress row for Phase 5 reads `17/17`; the old `16/16` form is gone; no other lines in ROADMAP changed; a single commit lands with message starting `docs(quick-260526-w11):` touching exactly `.planning/ROADMAP.md`.</done>
</task>

</tasks>

<verification>
After both tasks land, run the full quick-task verification gate:

```bash
# Gate 1: gofmt clean
gofmt -l cmd/dashboard/api/ 2>&1
# Expected: empty output

# Gate 2: build clean
go build ./...
# Expected: zero output, exit 0

# Gate 3: ROADMAP body-reconciled
grep -cE '^\| 5\. Distribution & Self-Hosting Acceptance \| 17/17 \| Complete \| 2026-05-23 \|$' .planning/ROADMAP.md
# Expected: 1

# Gate 4: old 16/16 form gone
grep -cE '^\| 5\. Distribution & Self-Hosting Acceptance \| 16/16' .planning/ROADMAP.md
# Expected: 0

# Gate 5: verify-license gate still passes (Apache headers untouched)
bash hack/scripts/verify-license.sh 2>&1 | tail -5
# Expected: passes

# Gate 6: exactly 2 commits from this quick task in main
git log --oneline main^^..main | head -5
# Expected: 2 commits with `style(quick-260526-w11):` and `docs(quick-260526-w11):` prefixes
```

The orchestrator's step 8 will then:
1. Flip the gofmt entry in `deferred-items.md` to `✅ RESOLVED 2026-05-27 (quick task 260526-w11, commit <Task-1-SHA>)`.
2. Commit deferred-items.md + PLAN.md + SUMMARY.md as the docs/planning-artifact closeout.
</verification>

<success_criteria>
- `gofmt -l cmd/dashboard/api/` returns empty (Phase 05-01 deferred entry's root cause neutralized).
- `go build ./...` succeeds (no regression).
- ROADMAP.md line 306 reads `17/17` (cosmetic reconciliation noted in 05-SUMMARY.md §Deviations #2 closed).
- Exactly 2 atomic commits land in `main`: one `style(quick-260526-w11):` (gofmt) and one `docs(quick-260526-w11):` (ROADMAP). No other changes bundled.
- `verify-license.sh` still passes (Apache-2.0 headers byte-identical post-gofmt).
- Zero changes to `charts/tide/values.yaml`, `charts/tide-crds/`, or any chart-tree path (FIXED-contract anti-pattern honored).
- Top-level ROADMAP phase overview checkboxes (lines 19-23) remain unticked per 05-SUMMARY.md Deviations #3 precedent.
</success_criteria>

<output>
After completion, create `.planning/quick/260526-w11-phase-5-closeout-polish-roadmap-16-16-17/260526-w11-SUMMARY.md` summarizing:
- Both atomic commit SHAs
- Confirmation that gofmt and ROADMAP gates passed
- Confirmation that verify-license still passes (headers untouched)
- Note: deferred-items.md RESOLVED flip is handled by the orchestrator in step 8, NOT this executor's scope
- Note: `v1.0.0-rc.1` tag is now unblocked from a closeout-polish standpoint
</output>
