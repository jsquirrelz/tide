---
quick_id: 260611-cz8
description: Salvage branch merge prep — 4 review fixes on tide/run-dogfood-analytics-1781156464, then merge to main
date: 2026-06-11
branch: tide/run-dogfood-analytics-1781156464
base: 2e28934
merge_decision: (b)-minimal, user-approved 2026-06-11 (MILESTONE.md stays at repo root)
---

# Quick Task 260611-cz8: Salvage Branch Merge Prep

Co-review of dogfood run 1's salvaged deliverable (2e28934, 16 files, +1,514) found
4 merge-blockers. User chose option (b)-minimal: fix the 4 on the branch, merge to
main, file remaining gaps as follow-up work.

## Tasks

### Task 1: Remove the two 0-byte files
- **Files:** `dashboard/web/src/__tests__/telemetry-degradation.test.tsx`, `hack/scripts/verify-telemetry-invariants.sh`
- **Action:** Delete both. The empty `.test.tsx` fails `npm test` ("No test suite found", exit 1); the empty `.sh` is dead weight from a SIGKILLed task. Real Vitest coverage for TelemetryView is follow-up work alongside the AppShell mount.
- **Verify:** `npm test` exits 0 on the branch.

### Task 2: Rewrite tautological proxy test against the real PrometheusHandler
- **Files:** `cmd/dashboard/api/telemetry_proxy_integration_test.go`
- **Action:** The test builds a private `makeTelemetryProxyHandler` reimplementation and tests that — production `PrometheusHandler` has zero behavioral coverage. Delete the local handler; wire `&PrometheusHandler{Endpoint: ..., Log: logr.Discard()}` into the chi router. Keep the three subtests (CONFIGURED+REACHABLE, UNCONFIGURED, CONFIGURED+UNREACHABLE) and their assertions intact.
- **Verify:** `go test ./cmd/dashboard/...` exits 0.

### Task 3: Fix docs/observability.md contradictions
- **Files:** `docs/observability.md`
- **Action:** (a) The PromQL-proxy section claims empty endpoint → 503; the implementation (and docs/dashboard.md) returns HTTP 200 + `{"status":"unavailable"}` — fix to match. (b) The six token/cost metrics table describes unimplemented metrics as existing ("Phase 5 adds") — mark as planned (milestone-01 phase-01, not yet implemented).
- **Verify:** grep shows no 503 claim; planned-status wording present.

### Task 4: Fix PROM_ENDPOINT render-gate false positive
- **Files:** `charts/tide/templates/dashboard-deployment.yaml`, `hack/helm/assert-telemetry-render.sh`
- **Action:** Permutation A of the branch's own gate fails because the template's YAML comment (containing the literal `PROM_ENDPOINT`) renders into output and the script's naive `grep -q PROM_ENDPOINT` matches it. Convert the comment to a non-rendering Helm `{{- /* */}}` comment AND tighten the grep to match an env entry shape (`name: PROM_ENDPOINT`).
- **Verify:** `bash hack/helm/assert-telemetry-render.sh` exits 0; `assert-prometheus-env.py` still passes both permutations.

### Task 5: Merge to main
- **Action:** Pre-merge untracked-dup check per CLAUDE.md, `git merge --no-ff` the branch into main, push main and the updated branch.
- **Verify:** `make test` MAKE_EXIT=0 on merged main; `npm test` exit 0; merge commit on main.

## Out of scope (filed as follow-ups in run-1 backlog)
- PROM_ENDPOINT config wiring (helm injects; nothing reads it — dead config)
- TelemetryView AppShell mount + Vitest coverage + PromQL metric-name alignment
- phase-01 metric instrumentation (internal/metrics)
- Makefile wiring of hack/helm gate scripts; proxy HTTP client timeout
