---
phase: quick-260708-tv5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/project_boundary_push_test.go
  - internal/controller/push_helpers.go
  - internal/controller/project_controller.go
autonomous: true
requirements: [TV5-01]
must_haves:
  truths:
    - "A succeeded shared tide-push-<project.UID> Job whose stamped staged-envelope set is a STRICT SUBSET of the current collectStageEnvelopes result is NOT accepted as the terminal boundary push — the state machine deletes it and re-dispatches a fresh OWNED push carrying the FULL cumulative map."
    - "A succeeded shared push Job whose stamped staged-envelope set already matches the current collectStageEnvelopes result IS accepted as the terminal boundary push, with NO extra Job created (no churn, no regression of Test 2/6)."
    - "Every (re)dispatch inside reconcileBoundaryPush — first dispatch, headSHA-unreadable retry, stale-artifact-map supersede, and generic terminal-failure retry — now carries the freshest collectStageEnvelopes map via PushOptions.StageEnvelopes (dispatchBoundaryPush previously carried none)."
  artifacts:
    - path: "internal/controller/push_helpers.go"
      provides: "stagedEnvelopesAnnotation const + buildPushJob stamping the CSV staged set onto the Job's ObjectMeta.Annotations at create time"
      contains: "stagedEnvelopesAnnotation"
    - path: "internal/controller/project_controller.go"
      provides: "isStaleArtifactPush helper + reconcileBoundaryPush stale-map supersede arm + dispatchBoundaryPush StageEnvelopes threading"
      contains: "isStaleArtifactPush"
    - path: "internal/controller/project_boundary_push_test.go"
      provides: "RED-first Test 8 (stale-subset supersede) + Test 9 (full-map no-churn) envtest specs, debug13b label"
      contains: "Test 8: succeeded Job stamped"
  key_links:
    - from: "buildPushJob"
      to: "reconcileBoundaryPush"
      via: "stagedEnvelopesAnnotation stamped at create time, read back and compared against a fresh collectStageEnvelopes call on the boundary reconcile"
      pattern: "isStaleArtifactPush\\(&existingPush, current\\)"
---

<objective>
Fix Defect E (DASH-02 follow-up — see `.planning/debug/dash02-artifact-boundary-push.md`, "SESSION OUTCOME" and "Run-9" sections, root cause CONFIRMED, do not re-investigate): only the `project`-level planning artifact ever reaches the run branch. Milestone/phase/plan artifacts never do, because the D-B5/R-05 shared single-flight `tide-push-<project.UID>` Job lets an EARLY artifact push (fired before milestone/phase/plan children materialize) win the shared name and snapshot a `[project]`-only cumulative map. The later, fuller-map pushes — including the authoritative project-Complete boundary push — hit the single-flight guard and skip; the stale Job lingers and its "self-heals on the next push" comment (`artifact_push.go:216`) never fires (Run-9 confirmed: `triggered boundary push` log-line count = 0 for the whole cascade).

Implement the recommended Option A fix: `reconcileBoundaryPush`'s success arm (`internal/controller/project_controller.go`) must detect when the succeeded shared Job's staged map is a strict subset of what `collectStageEnvelopes` now returns, and — instead of accepting it as terminal — delete it and re-dispatch a fresh, state-machine-OWNED push carrying the FULL cumulative map. The restage is idempotent (37-02 clean-tree skip for already-staged levels) and bounded by the existing `maxBoundaryPushAttempts` cap.

KEY DESIGN DECISION (yours to implement, not to re-litigate): detecting "stale" requires knowing what map the existing Job staged. Stamp the staged set as a Job annotation at create time in `buildPushJob` (so EVERY push Job — artifact-triggered, per-level boundary, and project-Complete boundary — carries it uniformly), then compare against a fresh `collectStageEnvelopes` call on the boundary reconcile. A Job with NO stamp (created before this fix, or created when the map couldn't be computed) must be treated as unknown provenance and NEVER second-guessed — this is what keeps the existing Test 1/2/3/4/5/6/7 specs (which build bare, unstamped Jobs) passing unmodified.

HARD CONSTRAINTS (guardrails — do not violate):
- Preserve the D-B5 single-writer coupling EXACTLY. NO Job-name decoupling — the shared deterministic name `tide-push-<project.UID>` stays as-is. The fix lives entirely in the boundary-push supersede logic (annotation stamp + comparison + delete/redispatch), never in naming.
- Do NOT touch `test/integration/kind/artifact_staging_test.go` or relax its Assertion 4 (`HaveKey("milestone")`). It is correct per 37-06's cumulative-staging design.
- Do NOT edit `charts/tide/values.yaml` (FIXED contract).
- The Layer-B kind re-run that would confirm `.tide/planning/milestone/<name>/MILESTONE.md` lands on the run branch live is orchestrator-gated and OUT OF SCOPE for this plan. Do not attempt it; ENVTEST is the allowed verification surface here (mirrors how Run-5/Run-9's defect-C fix was verified).

Purpose: milestone/phase/plan planning artifacts land on the run branch alongside the project artifact, closing the staging-completeness gap Run-9 isolated.
Output: an envtest-proven controller fix (2 new Ginkgo specs pinning the supersede + no-churn behavior) with golangci-lint / gofmt / go vet clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/debug/dash02-artifact-boundary-push.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

collectStageEnvelopes(ctx, c client.Client, project *tideprojectv1alpha2.Project) ([]string, error)
  — internal/controller/artifact_push.go:84. Lists ALL Milestone/Phase/Plan CRs in project.Namespace
  (no ProjectRef filter), keeps only plannerMaterialized ones ("AwaitingApproval"/"Succeeded"/PhaseComplete),
  includes the project entry when project.Status.Phase==Complete OR len(milestones)>0. Returns entries
  sorted by (kind, name) as "<uid>:<kind>/<name>" strings, e.g. "uid-ma:milestone/m-alpha",
  "proj-uid:project/proj". Monotonic: once a level is plannerMaterialized it never un-materializes, so the
  returned set only grows over a Project's lifetime — a later call is always a superset-or-equal of an
  earlier one for the same Project.

PushOptions (internal/controller/push_helpers.go:39) — already has a StageEnvelopes []string field
  (line ~82), rendered by buildPushJob as --stage-envelopes=<CSV> (only appended when non-empty, line
  ~182-188). buildPushJob (line 163) is the SOLE builder used by triggerArtifactPush (artifact_push.go),
  triggerBoundaryPush (boundary_push.go, milestone/phase/plan levels), AND dispatchBoundaryPush
  (project_controller.go, the project-Complete level) — a single stamping point in buildPushJob covers
  all three creation paths.

dispatchBoundaryPush(ctx, project, pvcName, pushJobName, lastErr string) (ctrl.Result, error)
  — project_controller.go:897. THE project-Complete push creator. Builds a PushOptions literal
  (line ~904-910) that currently has NO StageEnvelopes field set at all — this function has never staged
  any cumulative map, a related gap this fix also closes. Called from 4 sites inside reconcileBoundaryPush:
  the "no push Job yet" arm (~line 754), the headSHA-unreadable redispatch (~line 781), the generic
  terminal-failure retry (~line 877), and (new) the stale-artifact-map supersede arm you are adding.

reconcileBoundaryPush(ctx, project) (ctrl.Result, error) — project_controller.go:692. State machine run
  ONLY once a Project reaches Complete. Early-return guards (already-True, leak, lease, attempts>=cap) run
  first (lines ~701-742). Then: Get on pushJobName (~745-750) — NotFound dispatches fresh (~753-755);
  isJobSucceeded (~759) reads the envelope via readPushEnvelope — if headSHA unreadable/empty, deletes +
  redispatches (~761-782, existing Test 7 behavior, DO NOT change) — otherwise proceeds to the terminal
  success patch (~784-804, LastPushedSHA = env.HeadSHA, BoundaryPushed=True/Pushed). YOUR new stale-map
  check goes between those two: after "headSHA is readable" is confirmed, BEFORE the terminal patch.
  isJobFailed (~808) classifies leak/lease/generic and the generic arm deletes+redispatches (~877).

isJobSucceeded(job *batchv1.Job) bool / isJobFailed(job *batchv1.Job) bool — project_controller.go:1706,
  1716. Pure predicates over job.Status.Conditions. Add the new isStaleArtifactPush helper right after
  isJobFailed (~line 1723), same file, same style (pure function, doc comment above).

Existing test helpers in project_boundary_push_test.go (package controller, envtest, Label("envtest",
  "debug13b")): reconcileN(r, name, n) drives N reconcile passes; getProject(name); pushJobName(uid);
  makeComplete(name, attempts) creates+patches a Complete Project with a run branch; makePushJob(jobName,
  projectName, uid) creates a bare (unstamped) placeholder push Job; markJobSucceeded(jobName) /
  markJobFailed(jobName) patch Job.Status conditions; fakePushPodSuccess(jobName, headSHA) attaches a
  Succeeded pod with a readable pushResultEnvelope; cleanup(name) deletes all Jobs+Pods in "default" and
  the named Project (does NOT delete Milestones — your new tests must clean up their own Milestone CRs).
  newReconciler(pvcName) wraps newTestProjectReconciler() (uses the direct k8sClient, NOT the cached
  mgrClient — so collectStageEnvelopes sees writes immediately, no cache-sync wait strictly required,
  though waitForCacheSync is still fine to call defensively as billing_halt_regression_test.go does).

Milestone CR creation pattern (billing_halt_regression_test.go:137-144): &tideprojectv1alpha2.Milestone{
  ObjectMeta: metav1.ObjectMeta{Name: msName, Namespace: "default"}, Spec:
  tideprojectv1alpha2.MilestoneSpec{ProjectRef: projName}} via k8sClient.Create, then
  waitForCacheSync(msName, "default", &tideprojectv1alpha2.Milestone{}); status phase set via
  client.MergeFrom + k8sClient.Status().Patch (same idiom as makeComplete uses for Project.Status.Phase).
</interfaces>

REFERENCE PATTERN for the new tests — Test 7 in project_boundary_push_test.go (~line 481-510): same
"succeeded Job → must NOT go terminal → delete + redispatch" shape you are extending. Your Test 8 differs
from Test 7 in WHY it must not go terminal (stale staged MAP, not an unreadable headSHA) — headSHA in
Test 8 is perfectly readable.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the stale-subset supersede contract with 2 new envtest specs</name>
  <files>internal/controller/project_boundary_push_test.go</files>
  <behavior>
    - Test 8: a succeeded shared push Job stamped (at creation) with a staged-envelope set covering ONLY
      the project entry, while a Milestone has since materialized (Status.Phase="Succeeded") in the same
      namespace, must NOT be accepted as the terminal boundary push even though its headSHA is readable.
      The controller must delete it and re-dispatch a fresh push whose OWN stamped annotation now
      contains the milestone entry too (the full map, not just the stale subset).
    - Test 9 (regression guard): a succeeded shared push Job whose stamp ALREADY matches the current
      full collectStageEnvelopes set (project + a materialized milestone, no further children appear)
      goes terminal (BoundaryPushed=True/Pushed, LastPushedSHA advances) with NO second Job created —
      proves the fix does not introduce needless churn on an already-complete stage.
  </behavior>
  <action>
    Add two local test helpers near the existing makePushJob/fakePushPodSuccess closures (same
    Describe("ProjectReconciler — boundary-push bounded auto-retry (debug #13b)") block, same style —
    plain func literals, not new top-level funcs):

    (1) makeStampedPushJob(jobName, projectName string, uid types.UID, staged []string) — identical to
    the existing makePushJob body, but sets ObjectMeta.Annotations on the constructed Job to
    map[string]string{"tideproject.k8s/staged-envelopes": strings.Join(staged, ",")} before Create. Use
    the LITERAL string "tideproject.k8s/staged-envelopes" here (Task 2 introduces the matching
    stagedEnvelopesAnnotation const in push_helpers.go — do not reference that identifier from this task,
    it does not exist yet; the literal keeps this task's file self-contained and independently
    compilable/RED against current project_controller.go).

    (2) makeMaterializedMilestone(name, projectName, phase string) types.UID — creates a Milestone (Spec:
    MilestoneSpec{ProjectRef: projectName}) via k8sClient.Create, waitForCacheSync, then
    client.MergeFrom+k8sClient.Status().Patch to set Status.Phase=phase; returns the created object's UID
    for building expected stage-envelope entry strings. Also add deleteMilestone(name string) — best-effort
    k8sClient.Get + Delete, ignore NotFound — since the shared cleanup() helper does not touch Milestones
    and collectStageEnvelopes lists the WHOLE "default" namespace.

    Add "strings" to this file's import block (needed by makeStampedPushJob's strings.Join).

    Then add two new Describe blocks after the existing "Test 7" block, before the closing `})` of the
    outer Describe:

    Describe "Test 8: succeeded Job stamped with a partial (subset) staged map → supersede, not terminal":
    const name = "bp13b-stalemap", const msName = "bp13b-stalemap-ms". AfterEach: deleteMilestone(msName)
    then cleanup(name). It(...): proj := makeComplete(name, 0); jn := pushJobName(proj.UID); stamp the Job
    via makeStampedPushJob(jn, proj.Name, proj.UID, []string{fmt.Sprintf("%s:project/%s", proj.UID,
    proj.Name)}) — ONLY the project entry, modeling the early artifact-push winner from Run-9;
    fakePushPodSuccess(jn, landedSHA) with a fresh 40-hex-char landedSHA constant; markJobSucceeded(jn).
    THEN materialize the milestone child: makeMaterializedMilestone(msName, proj.Name, "Succeeded") — this
    is what makes collectStageEnvelopes' current result a strict superset of what the Job staged. Run
    r := newReconciler("tide-projects-bp13b-8"); reconcileN(r, name, 4) (mirror Test 7's convergence
    passes). Assert via Eventually(5s, 100ms): got.Status.Git.LastPushedSHA is EMPTY (the partial stage
    must not be accepted as terminal); the BoundaryPushed condition Status is ConditionFalse with Reason
    ReasonPushing (not ReasonPushed); got.Status.BoundaryPush.Attempts >= 1 (the stale Job was superseded
    by an owned redispatch); a push Job named jn exists again (recreated). THEN — the decisive Defect-E
    signal — re-Get that recreated Job and assert its Annotations["tideproject.k8s/staged-envelopes"]
    contains the substring "milestone/"+msName, proving the superseding push carried the FULL cumulative
    map, not just a repeat of the stale subset.

    Describe "Test 9: succeeded Job already stamped with the FULL cumulative map → terminal success, no
    needless re-dispatch": const name = "bp13b-fullmap", const msName = "bp13b-fullmap-ms". AfterEach:
    deleteMilestone(msName) then cleanup(name). It(...): proj := makeComplete(name, 0); jn :=
    pushJobName(proj.UID); FIRST materialize the milestone: msUID := makeMaterializedMilestone(msName,
    proj.Name, "Succeeded") — so collectStageEnvelopes' current result already includes it; THEN stamp the
    Job with BOTH entries matching that current set exactly: makeStampedPushJob(jn, proj.Name, proj.UID,
    []string{fmt.Sprintf("%s:milestone/%s", msUID, msName), fmt.Sprintf("%s:project/%s", proj.UID,
    proj.Name)}) (kind-then-name order: "milestone" sorts before "project"); fakePushPodSuccess(jn,
    landedSHA) with a distinct landedSHA constant; markJobSucceeded(jn). Run reconcileN(r, name, 3).
    Assert (mirrors existing Test 2): BoundaryPushed Status=ConditionTrue/Reason=ReasonPushed,
    Status.Git.LastPushedSHA == landedSHA. THEN list all Jobs in "default" and assert exactly ONE has name
    jn (no second Job created — no churn when the stamp already covers the current full map).

    Do NOT modify push_helpers.go or project_controller.go in this task — test file only.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -count=1 -args -ginkgo.focus='Test 8: succeeded Job stamped' 2>&amp;1 | tail -50</automated>
  </verify>
  <done>
    The file compiles (only a plain string literal is used for the annotation key, no forward reference
    to Task 2's identifiers). Test 8 FAILS (RED) against the current, pre-fix project_controller.go —
    expect the Eventually block to report Status.Git.LastPushedSHA equal to landedSHA where BeEmpty() was
    expected (today's code has no stale-map check, so a readable headSHA always goes terminal). Test 9 is
    expected to PASS already at this stage (it exercises a path — "readable headSHA → terminal success,
    one Job" — that the current code already implements correctly; it becomes a regression guard once
    Task 2 lands, not a RED signal now). Confirm by reading the actual Ginkgo failure output for Test 8;
    do not assume.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — stamp the staged set, detect staleness, supersede in reconcileBoundaryPush</name>
  <files>internal/controller/push_helpers.go, internal/controller/project_controller.go</files>
  <action>
    In push_helpers.go: add a `stagedEnvelopesAnnotation` string constant valued exactly
    "tideproject.k8s/staged-envelopes" (matching Task 1's test literal), placed near the existing
    pushSAName/pushContainerName/pushWorkspaceVolume consts (~line 109-120), with a doc comment explaining
    it stamps the CSV of staged <uid>:<kind>/<name> entries so a later boundary reconcile can detect this
    Job as a stale artifact push (Defect E / DASH-02 follow-up) if collectStageEnvelopes later returns a
    strict superset; a Job with no stamp is unknown provenance and must never be treated as stale. In
    buildPushJob (~line 163-311), after the `job := &batchv1.Job{...}` literal is constructed and before
    `_ = owner.EnsureOwnerRef(job, project, scheme)` (~line 308), set job.Annotations to a map containing
    stagedEnvelopesAnnotation -> strings.Join(opts.StageEnvelopes, ",") WHEN len(opts.StageEnvelopes) > 0
    (mirror the existing `if len(opts.StageEnvelopes) > 0` guard at ~line 182 for the --stage-envelopes
    arg — same condition, so the annotation and the arg are always stamped together or not at all).

    In project_controller.go: add "strings" to the import block. Add a new pure function
    isStaleArtifactPush(job *batchv1.Job, current []string) bool right after isJobFailed (~line 1723),
    same doc-comment density as isJobSucceeded/isJobFailed. Behavior: read job.Annotations[the annotation
    key] with the comma-ok idiom; if absent, return false (unknown provenance, trust as-is — this is what
    keeps Tests 1/2/3/4/5/6/7's bare unstamped Jobs behaving exactly as before). If present, split on ","
    into a set (empty string yields an empty set). If len(current) <= len(staged-set), return false (not a
    strict superset — nothing new materialized, or staged is not smaller). Otherwise build a set from
    current and confirm every staged entry is present in it; if any staged entry is missing from current
    (an unexpected divergence — e.g. a level's CR was deleted), return false (don't second-guess an
    already-succeeded push); only when EVERY staged entry is confirmed present in a strictly-larger
    current set, return true.

    Modify reconcileBoundaryPush (~line 692-890): right after the existing `if pErr != nil &&
    !apierrors.IsNotFound(pErr) { return ctrl.Result{}, pErr }` block (~line 748-750), compute the
    cumulative map ONCE for this reconcile pass: `current, seErr := collectStageEnvelopes(ctx, r.Client,
    project)`; on seErr != nil, logger.Error it (non-fatal, mirror triggerBoundaryPush's best-effort
    degrade at boundary_push.go:124-128) and set current = nil so the function still proceeds. Thread
    `current` as a new final argument into ALL THREE existing dispatchBoundaryPush call sites: the
    "NotFound → dispatch first attempt" arm (~line 753-755), the "headSHA unreadable → redispatch" arm
    (~line 776-782, inside the isJobSucceeded branch — leave every other line of that arm unchanged), and
    the generic terminal-failure retry arm (~line 871-877, inside the isJobFailed branch). Then, inside the
    isJobSucceeded branch, insert a NEW check immediately AFTER the `if !ok || env.HeadSHA == ""` block
    returns (i.e., once you know headSHA is present) and BEFORE the terminal-success patch (~line 784
    `patch := client.MergeFrom(project.DeepCopy())`): `if isStaleArtifactPush(&existingPush, current) {
    delete the Job via r.deleteFailedPushJob(ctx, &existingPush) (return its error if non-nil), log at Info
    level that a partial-map push is being superseded by a fresh owned dispatch, then `return
    r.dispatchBoundaryPush(ctx, project, pvcName, pushJobName, "stale-artifact-map", current)` }`. This
    must run BEFORE the terminal patch so a stale-map success never sets BoundaryPushed=True.

    Update dispatchBoundaryPush's signature (~line 897) to accept a new trailing parameter
    `stageEnvelopes []string`, and set `StageEnvelopes: stageEnvelopes` in its PushOptions literal
    (~line 904-910, alongside the existing TidePushImage/Branch/LastPushedSHA/CommitMessage/LeaksConfigMap
    fields) — this closes the separate latent gap where the project-Complete boundary push never staged
    any cumulative map at all, even before Defect E.

    Do NOT change triggerArtifactPush, triggerBoundaryPush, triggerWaveIntegrationJob, readPushEnvelope, or
    the leak/lease/cap early-return guards at the top of reconcileBoundaryPush. Do NOT touch
    test/integration/kind/artifact_staging_test.go or charts/tide/values.yaml.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -count=1 -args -ginkgo.focus='debug13b' 2&gt;&amp;1 | tee /tmp/tv5-debug13b.log | tail -60; echo "GO_EXIT=$?"; grep -cE '^. \[FAILED\]|^--- FAIL' /tmp/tv5-debug13b.log || true</automated>
  </verify>
  <done>
    All 9 debug13b specs pass: Test 8 flips to GREEN (the recreated Job's stagedEnvelopesAnnotation now
    contains the milestone entry, LastPushedSHA stays empty, BoundaryPushed stays False/Pushing, attempts
    incremented) and Test 9 stays GREEN with exactly one Job named jn (no churn). Tests 1-7 are UNCHANGED —
    re-read their assertions and confirm none regressed (in particular Test 2 and Test 6, whose bare
    unstamped Jobs must still go terminal True on the first pass via the "no annotation -> not stale"
    guard in isStaleArtifactPush). Then run `make test` (fmt+vet+full unit/envtest tier) and confirm
    MAKE_EXIT=0 with no `--- FAIL` line, and `make lint` (golangci-lint + import firewalls) clean. Do not
    claim GREEN without reading the actual Ginkgo pass/fail summary and the MAKE_EXIT value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Job annotation (`tideproject.k8s/staged-envelopes`) -> `reconcileBoundaryPush` | The new staleness check trusts a value stamped on a Kubernetes Job object and read back by the controller. Any actor with create/patch rights on Jobs in the project namespace — already a highly-privileged in-cluster capability, same trust level as the pre-existing `pushResultEnvelope` termination-message read in `readPushEnvelope` — could forge this annotation. |
| PVC / run branch <-> push Job | Unchanged from the existing D-B5 trust model; not touched by this fix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-tv5-01 | Tampering | `isStaleArtifactPush` (project_controller.go) | low | accept | Same trust boundary as the pre-existing `readPushEnvelope` termination-message read — only Service Accounts already trusted to create push Jobs in-namespace can write the annotation. A forged "already-full" stamp at worst suppresses one supersede pass (the next genuine boundary trigger or a later reconcile with fresher `current` still catches it); a forged "always-stale" stamp at worst causes extra redispatches, which are bounded by the existing `maxBoundaryPushAttempts` cap (unchanged, no new authority granted). |
| T-tv5-02 | Denial of Service | `reconcileBoundaryPush` supersede loop | low | mitigate | The new stale-map redispatch path reuses the EXISTING `maxBoundaryPushAttempts` bound and `dispatchBoundaryPush`'s `Attempts` increment — no new unbounded retry path is introduced. Verified by envtest Test 8 converging within `reconcileN(r, name, 4)`. |

</threat_model>

<verification>
- `go test ./internal/controller/... -count=1 -args -ginkgo.focus='debug13b'` — all 9 specs (1-9) green.
- `make test` exits 0 (fmt+vet+unit/envtest tier); no `--- FAIL` in the log.
- `make lint` exits 0 (golangci-lint + import firewalls).
- `git diff internal/controller/push_helpers.go internal/controller/project_controller.go` touches ONLY
  buildPushJob's annotation stamp + the new const, plus reconcileBoundaryPush/dispatchBoundaryPush/the new
  isStaleArtifactPush helper. `triggerArtifactPush`, `triggerBoundaryPush`, `triggerWaveIntegrationJob`,
  `readPushEnvelope` bodies unchanged.
- No changes to `test/integration/kind/artifact_staging_test.go` or `charts/tide/values.yaml`.
</verification>

<success_criteria>
- A shared push Job that staged only a strict-subset cumulative map (the D-B5/R-05 single-flight winner
  from an early artifact push) is superseded — deleted and replaced by a fresh, owned push carrying the
  FULL current `collectStageEnvelopes` map — instead of being accepted as the terminal boundary push.
- A shared push Job whose stamped map already covers the current full map goes terminal with zero extra
  Job churn (Test 2/6/9 all green, no regression).
- Every `dispatchBoundaryPush` call site now threads the freshest cumulative map into `PushOptions.StageEnvelopes`.
- The D-B5 single-writer coupling (deterministic `tide-push-<project.UID>` name) is untouched — no name
  decoupling introduced.
- Full unit/envtest suite green (`make test`), lint clean (`make lint`).
- Layer-B kind confirmation that `.tide/planning/milestone/<name>/MILESTONE.md` lands on the run branch is
  explicitly DEFERRED — out of scope for this plan, owned by the orchestrator's next Layer-B run.
</success_criteria>

<output>
Create `.planning/quick/260708-tv5-fix-defect-e-dash-02-follow-up-milestone/260708-tv5-SUMMARY.md` when done.
</output>