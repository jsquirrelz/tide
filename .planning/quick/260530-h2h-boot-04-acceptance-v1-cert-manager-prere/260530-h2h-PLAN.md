---
phase: quick-260530-h2h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hack/scripts/acceptance-v1.sh
  - docs/INSTALL.md
autonomous: true
requirements:
  - QUICK-260530-h2h-A  # acceptance-v1.sh installs cert-manager before helm install tide
  - QUICK-260530-h2h-B  # INSTALL.md documents cert-manager prereq before helm install tide
user_setup: []

must_haves:
  truths:
    - "acceptance-v1.sh installs cert-manager (pinned v1.20.2, TIDE_CERT_MANAGER_VERSION override) BEFORE running 'helm install tide ...'"
    - "acceptance-v1.sh waits for all three cert-manager Deployments (cert-manager, cert-manager-cainjector, cert-manager-webhook) to roll out before the tide helm install fires"
    - "docs/INSTALL.md introduces cert-manager as a prerequisite BEFORE the 'helm install tide' instructions, with pinned v1.20.2 + rationale + cross-refs to observability.md and rbac.md"
    - "charts/ tree is untouched — chart contract preserved per Phase 02.2 anti-pattern"
    - "Two atomic commits land on main, one per fix; planning artifacts are NOT committed by the executor (orchestrator step 8 handles those)"
  artifacts:
    - path: "hack/scripts/acceptance-v1.sh"
      provides: "cert-manager install + rollout-wait block wedged between 'kind create cluster' and 'helm install tide'"
      contains: "cert-manager.io/v1"  # comment block citing the chart dependency
      contains_token: "TIDE_CERT_MANAGER_VERSION"
      contains_token: "rollout status deployment/cert-manager"
    - path: "docs/INSTALL.md"
      provides: "Prerequisite subsection introducing cert-manager v1.20.2 before 'helm install tide'"
      contains: "cert-manager"
      contains: "v1.20.2"
  key_links:
    - from: "hack/scripts/acceptance-v1.sh (cert-manager block)"
      to: "test/integration/kind/suite_test.go installCertManager()"
      via: "shared pinned version v1.20.2 + same Deployments + same rollout-status timeout (120s)"
      pattern: "v1\\.20\\.2"
    - from: "docs/INSTALL.md cert-manager subsection"
      to: "charts/tide/templates/{serving-cert,selfsigned-issuer,metrics-certs}.yaml"
      via: "rationale paragraph citing the chart's cert-manager.io/v1 CR dependency"
      pattern: "cert-manager"
    - from: "hack/scripts/acceptance-v1.sh cert-manager block"
      to: "next line is 'helm install tide ...'"
      via: "block lands immediately before the '# ── Helm install (both charts) ──' separator"
      pattern: "Helm install"
---

<objective>
Fix the BOOT-04 acceptance-v1 ritual so it no longer crashes on a missing cert-manager dependency, and document the prereq on the public install on-ramp.

Purpose: Today's `make acceptance-v1` attempt (BG task `bess2gftr`, exit 2 at 2026-05-30T16:13:45Z) failed inside `helm install tide ...` with `no matches for kind "Certificate" in version "cert-manager.io/v1"`. The tide chart's webhook + metrics Certificates require cert-manager CRDs to be present before helm tries to apply them; `hack/scripts/acceptance-v1.sh` jumped straight from `kind create cluster` to `helm install tide` with no cert-manager bring-up. The proven pattern already lives in `test/integration/kind/suite_test.go:329-369` (Layer B integration tests); we mirror it in bash for the acceptance script and document it in `docs/INSTALL.md` so the public on-ramp matches reality.

Output: Two atomic commits — (1) `hack/scripts/acceptance-v1.sh` carries a cert-manager install + rollout-wait wedge, (2) `docs/INSTALL.md` introduces cert-manager as a documented prereq before `helm install tide`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@hack/scripts/acceptance-v1.sh
@test/integration/kind/suite_test.go
@docs/INSTALL.md

<failure_observed>
The failure that motivated this fix, from today's `make acceptance-v1` BG run `bess2gftr` (exit 2 at 2026-05-30T16:13:45Z), happened during step 64 (`helm install tide ${REPO_ROOT}/charts/tide -n tide-system`):

```
Error: INSTALLATION FAILED: unable to build kubernetes objects from release manifest:
  [resource mapping not found for name: "tide-metrics-certs" namespace: "" from "":
   no matches for kind "Certificate" in version "cert-manager.io/v1"
   ensure CRDs are installed first, ...]
```

Helm refuses to apply CRs whose CRDs don't exist in the cluster. The tide chart legitimately depends on cert-manager via `charts/tide/templates/{serving-cert,selfsigned-issuer,metrics-certs}.yaml` (DO NOT TOUCH — chart contract is FIXED per Phase 02.2). The script just didn't install the dependency before it called helm.
</failure_observed>

<interfaces>
<!-- Key references the executor needs. Extracted from the codebase up front so no exploration is required. -->

From `test/integration/kind/suite_test.go` (Layer B integration test — the proven pattern to mirror in bash):

Lines 314-327 (constant + override pattern — the executor MUST honor the same env-var name so the two recipes stay in lockstep):
- `certManagerVersion = "v1.20.2"` is the pinned default (K8s 1.33-compatible per Phase 02.2 cert-manager bump decision).
- Override env var name: `TIDE_CERT_MANAGER_VERSION` (NOT `CERT_MANAGER_VERSION`, NOT `TIDE_CM_VERSION`). Same name MUST appear in the bash script.

Lines 329-362 (the install + wait sequence — what the bash block has to mirror):
- Single `kubectl apply -f <URL>` against `https://github.com/cert-manager/cert-manager/releases/download/${version}/cert-manager.yaml`.
- Then a loop over the three Deployments `cert-manager`, `cert-manager-cainjector`, `cert-manager-webhook`, each `kubectl -n cert-manager rollout status deployment/${deploy} --timeout=120s`.
- The integration test's Go code uses `--timeout=120s` on `kubectl apply -f <URL>`, but THAT IS THE GO `exec.CommandContext` PATTERN — when running `kubectl apply -f <URL>` directly from bash, `--timeout` is NOT a valid flag for `apply` against a URL. Drop `--timeout=120s` from the bash `kubectl apply` line. Only the `rollout status` calls carry `--timeout=120s`.

From `hack/scripts/acceptance-v1.sh` (current shape — exact insertion point):

- Line 49: `kind create cluster --name "${CLUSTER_NAME}"` is the last line of the "Cluster bring-up" block.
- Lines 51-59: an optional image-load block (guarded by `ACCEPTANCE_LOAD_IMAGES`) follows.
- Line 61: `# ── Helm install (both charts) ──────────────────────────────────────────────` is the separator that introduces the helm installs.
- Lines 62-64: the two helm installs (`tide-crds`, then `tide`).

The cert-manager wedge MUST land BETWEEN line 59 (close of the optional image-load block) and line 61 (the Helm install separator). It MUST run on every invocation (NOT guarded by `ACCEPTANCE_LOAD_IMAGES`).

From `docs/INSTALL.md` (insertion point for the prereq subsection):

- Line 95: `## Install order (Pitfall 4 — CRDs first)` — introduces the install-order narrative.
- Line 101: `### Quickstart (OCI registry — primary path)` — first code block, ends with `helm install tide oci://ghcr.io/jsquirrelz/tide-charts/tide ...` at line 107.
- Line 119: `### Cloned-repo install path` — second code block, ends with `helm install tide ./charts/tide -n tide-system` at line 127.
- Line 134: `### Verifying the install` — next subsection after both helm install blocks.

The cert-manager prereq subsection lands AFTER line 97's "CRDs first" paragraph and BEFORE line 101's `### Quickstart` heading, so it applies to BOTH install paths (Quickstart + cloned-repo). Use heading `### cert-manager prerequisite` (matches the existing `### Quickstart` / `### Cloned-repo install path` h3 cadence inside `## Install order`).

From `charts/tide/templates/` (DO NOT MODIFY — these prove the chart dependency exists; their presence is the rationale, not the change target):
- `serving-cert.yaml`, `selfsigned-issuer.yaml`, `metrics-certs.yaml` all reference `cert-manager.io/v1` resources.
- The `docs/INSTALL.md` rationale paragraph SHOULD name these three files when explaining why cert-manager is required.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Wedge cert-manager install + rollout-wait into hack/scripts/acceptance-v1.sh</name>
  <files>hack/scripts/acceptance-v1.sh</files>
  <action>
Insert a cert-manager install + rollout-wait block into `hack/scripts/acceptance-v1.sh` AFTER the kind/image-load section closes (current line 59) and BEFORE the `# ── Helm install (both charts) ──` separator (current line 61). The block MUST be unconditional (run on every invocation; NOT guarded by `ACCEPTANCE_LOAD_IMAGES`).

Required shape (paste structure; do NOT vary the env var name or pinned version):

```bash
# ── cert-manager (required by charts/tide webhook + metrics Certificates) ───
# The tide chart references cert-manager.io/v1 Certificate + Issuer resources
# in charts/tide/templates/{serving-cert,selfsigned-issuer,metrics-certs}.yaml.
# Helm refuses to apply CRs whose CRDs aren't present, so cert-manager MUST be
# installed BEFORE 'helm install tide ...' below. Mirrors the Layer B
# integration test pattern at test/integration/kind/suite_test.go:329-369.
# Pinned v1.20.2 per Phase 02.2 cert-manager bump decision (K8s 1.33-compatible).
CERT_MANAGER_VERSION="${TIDE_CERT_MANAGER_VERSION:-v1.20.2}"
echo "==> installing cert-manager ${CERT_MANAGER_VERSION}..."
kubectl apply -f "https://github.com/cert-manager/cert-manager/releases/download/${CERT_MANAGER_VERSION}/cert-manager.yaml"
echo "==> waiting for cert-manager Deployments to roll out..."
for deploy in cert-manager cert-manager-cainjector cert-manager-webhook; do
  kubectl -n cert-manager rollout status deployment/"${deploy}" --timeout=120s
done
```

Critical do-nots for this task:
- Do NOT add `--timeout=120s` (or any `--timeout` flag) to the `kubectl apply -f <URL>` line. `--timeout` is valid for `kubectl wait` and `kubectl rollout status`, NOT for `kubectl apply` against a remote URL. The integration test's Go code uses it via `exec.CommandContext`, but the bash form of `kubectl apply -f <URL>` will error out on the unknown flag in some kubectl versions. Only `rollout status` carries `--timeout=120s`.
- Do NOT rename the env override to anything other than `TIDE_CERT_MANAGER_VERSION` — that's the exact name used at `test/integration/kind/suite_test.go:334`, and keeping it identical means a maintainer can set it once in their shell and have both recipes honor it.
- Do NOT touch line 49's `kind create cluster` or any line at or above it; do NOT touch lines 62-64 (the helm installs) or anything below them. The wedge is purely additive between the kind block and the helm block.
- Do NOT touch any file under `charts/` — chart contract is FIXED per Phase 02.2 / CLAUDE.md anti-pattern.
- Do NOT actually run `make acceptance-v1` or invoke cert-manager from this task. Verification gates are syntactic only; the orchestrator handles the functional rerun after the fix lands.

Commit the change as a single atomic commit with shape:

```
fix(quick-260530-h2h): install cert-manager v1.20.2 before helm install tide in acceptance-v1.sh

Today's `make acceptance-v1` (BG task bess2gftr, exit 2 at 2026-05-30T16:13:45Z)
failed inside `helm install tide` with:

  no matches for kind "Certificate" in version "cert-manager.io/v1"

The tide chart (charts/tide/templates/{serving-cert,selfsigned-issuer,metrics-certs}.yaml)
references cert-manager.io/v1 CRs; helm refuses to apply CRs whose CRDs aren't
in the cluster yet. Wedge a cert-manager install + rollout-wait block between
`kind create cluster` and `helm install tide`, mirroring the proven Layer B
integration test pattern at test/integration/kind/suite_test.go:329-369. Pinned
v1.20.2 (K8s 1.33-compatible); overridable via TIDE_CERT_MANAGER_VERSION
(same env name the integration test honors so both recipes stay aligned).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Stage ONLY `hack/scripts/acceptance-v1.sh`. Do NOT stage any planning artifacts.
  </action>
  <verify>
    <automated>
      bash -c '
        set -e
        # Gate (a): cert-manager comment header + env var + apply URL + rollout-status loop
        # must contribute at least 3 non-comment-noise hits.
        HITS=$(grep -cE "cert-manager" hack/scripts/acceptance-v1.sh)
        if [ "${HITS}" -lt 3 ]; then
          echo "FAIL: expected >=3 cert-manager mentions in acceptance-v1.sh, got ${HITS}"; exit 1
        fi
        # Pinned version + env override + rollout loop must all be present.
        grep -qE "TIDE_CERT_MANAGER_VERSION" hack/scripts/acceptance-v1.sh
        grep -qE "v1\.20\.2" hack/scripts/acceptance-v1.sh
        grep -qE "rollout status deployment/" hack/scripts/acceptance-v1.sh
        # Gate (c): bash parses cleanly (no syntax errors introduced).
        bash -n hack/scripts/acceptance-v1.sh
        # Order check: cert-manager block must appear BEFORE the helm install tide line.
        CM_LINE=$(grep -nE "installing cert-manager" hack/scripts/acceptance-v1.sh | head -1 | cut -d: -f1)
        HELM_LINE=$(grep -nE "helm install tide " hack/scripts/acceptance-v1.sh | head -1 | cut -d: -f1)
        if [ -z "${CM_LINE}" ] || [ -z "${HELM_LINE}" ] || [ "${CM_LINE}" -ge "${HELM_LINE}" ]; then
          echo "FAIL: cert-manager block must precede helm install tide (cm=${CM_LINE} helm=${HELM_LINE})"; exit 1
        fi
        # Charts untouched in the staged tree.
        if git diff --cached --name-only | grep -qE "^charts/"; then
          echo "FAIL: charts/ files staged — chart contract must remain untouched"; exit 1
        fi
        echo "OK: cert-manager block landed; bash -n clean; order correct; charts/ untouched."
      '
    </automated>
  </verify>
  <done>
`hack/scripts/acceptance-v1.sh` contains a cert-manager install + rollout-wait block placed between the kind cluster create section and the helm install section. The block honors `TIDE_CERT_MANAGER_VERSION` with default `v1.20.2`, runs `kubectl apply -f <URL>` (no `--timeout`), and waits on all three cert-manager Deployments via `kubectl rollout status --timeout=120s`. `bash -n` exits 0. One atomic commit lands on main with the prescribed message shape; no `charts/` files are touched; no planning artifacts are staged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document cert-manager prereq in docs/INSTALL.md before helm install tide</name>
  <files>docs/INSTALL.md</files>
  <action>
Add a `### cert-manager prerequisite` subsection to `docs/INSTALL.md` inside the existing `## Install order (Pitfall 4 — CRDs first)` section. Insert it AFTER the line-97 "CRDs first" rationale paragraph and BEFORE the line-101 `### Quickstart (OCI registry — primary path)` heading, so the prereq applies to BOTH install paths (Quickstart + cloned-repo) without duplication.

The new `### cert-manager prerequisite` subsection MUST cover, in this order:

1. Rationale paragraph (1 short paragraph). State that the `tide` chart's webhook + metrics Certificate resources (specifically `charts/tide/templates/serving-cert.yaml`, `charts/tide/templates/selfsigned-issuer.yaml`, and `charts/tide/templates/metrics-certs.yaml`) reference `cert-manager.io/v1` kinds. Helm refuses to apply CRs whose CRDs are not registered in the cluster, so cert-manager MUST exist BEFORE `helm install tide ...`. (Tide-crds does NOT need cert-manager — only the main `tide` chart does — so cert-manager can install in either order relative to `tide-crds`.)

2. Install command (code block, fenced as `bash`). Use the pinned-version form:

   ```bash
   kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.20.2/cert-manager.yaml
   ```

3. Wait commands (code block, fenced as `bash`). Cover all three Deployments:

   ```bash
   kubectl -n cert-manager rollout status deployment/cert-manager --timeout=120s
   kubectl -n cert-manager rollout status deployment/cert-manager-cainjector --timeout=120s
   kubectl -n cert-manager rollout status deployment/cert-manager-webhook --timeout=120s
   ```

4. Version-pinning guidance paragraph (1 short paragraph). State that `v1.20.2` is the project's tested default, also pinned in `test/integration/kind/suite_test.go` (the Layer B integration harness) and `hack/scripts/acceptance-v1.sh` (the maintainer acceptance ritual). v1.20.2 is K8s 1.33-compatible (matches the project's `kindest/node` pin). Maintainers running the acceptance ritual can override via the `TIDE_CERT_MANAGER_VERSION` env var.

5. Cross-references paragraph (1 short paragraph). Note that cert-manager's broader role in the cluster is also covered by `docs/observability.md` (metrics-server cert wiring) and `docs/rbac.md` (RBAC for cert-manager's own service accounts). Link those docs by relative path: `[docs/observability.md](./observability.md)` and `[docs/rbac.md](./rbac.md)`.

Style/formatting requirements:
- Match the doc's existing h3 cadence within `## Install order` (other h3s in that section are `### Quickstart (OCI registry — primary path)` and `### Cloned-repo install path` — em-dash, sentence-case after).
- Fenced code blocks: triple-backtick `bash`. Use the same fence style the doc already uses.
- Voice: tight, declarative, em-dash-heavy per the project doc-voice (CLAUDE.md "Structural conventions in the spec document").
- Do NOT modify `docs/observability.md` or `docs/rbac.md`. Cross-references are read-only links to those docs.
- Do NOT modify any other section of `docs/INSTALL.md` (Prerequisites, Per-OS install, Quickstart code blocks, Cloned-repo code blocks, Verifying the install, Provider Secret, Git credentials Secret, First Project apply, Is TIDE right for me, Next steps).
- Do NOT touch any file under `charts/`.

Commit the change as a single atomic commit with shape:

```
docs(quick-260530-h2h): document cert-manager v1.20.2 prerequisite in INSTALL.md

The tide chart's webhook + metrics Certificate resources
(charts/tide/templates/{serving-cert,selfsigned-issuer,metrics-certs}.yaml)
require cert-manager CRDs to exist in the cluster before `helm install tide`
will succeed. cert-manager was mentioned in docs/observability.md and
docs/rbac.md but NOT documented on the install on-ramp, so operators
following INSTALL.md hit `no matches for kind "Certificate"` mid-install
with no warning. Add a `### cert-manager prerequisite` subsection inside
`## Install order` that lands BEFORE both Quickstart and cloned-repo paths,
covers the pinned v1.20.2 install + rollout-wait commands, and cross-refs
the existing observability + rbac docs.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Stage ONLY `docs/INSTALL.md`. Do NOT stage any planning artifacts.
  </action>
  <verify>
    <automated>
      bash -c '
        set -e
        # Gate (b): cert-manager appears at least once in INSTALL.md.
        HITS=$(grep -cE "cert-manager" docs/INSTALL.md)
        if [ "${HITS}" -lt 1 ]; then
          echo "FAIL: expected >=1 cert-manager mention in docs/INSTALL.md, got ${HITS}"; exit 1
        fi
        # The new heading must exist.
        grep -qE "^### cert-manager prerequisite$" docs/INSTALL.md
        # Pinned version must appear.
        grep -qE "v1\.20\.2" docs/INSTALL.md
        # The install URL must appear.
        grep -qE "github.com/cert-manager/cert-manager/releases/download/v1\.20\.2/cert-manager\.yaml" docs/INSTALL.md
        # Cross-references must point at observability.md and rbac.md.
        grep -qE "observability\.md" docs/INSTALL.md
        grep -qE "rbac\.md" docs/INSTALL.md
        # Order check: the cert-manager subsection must appear BEFORE the first `helm install tide ` line.
        CM_LINE=$(grep -nE "^### cert-manager prerequisite$" docs/INSTALL.md | head -1 | cut -d: -f1)
        HELM_LINE=$(grep -nE "helm install tide " docs/INSTALL.md | head -1 | cut -d: -f1)
        if [ -z "${CM_LINE}" ] || [ -z "${HELM_LINE}" ] || [ "${CM_LINE}" -ge "${HELM_LINE}" ]; then
          echo "FAIL: cert-manager subsection must precede first helm install tide reference (cm=${CM_LINE} helm=${HELM_LINE})"; exit 1
        fi
        # Charts untouched in the staged tree.
        if git diff --cached --name-only | grep -qE "^charts/"; then
          echo "FAIL: charts/ files staged — chart contract must remain untouched"; exit 1
        fi
        # observability.md + rbac.md untouched in the staged tree.
        if git diff --cached --name-only | grep -qE "^docs/(observability|rbac)\.md$"; then
          echo "FAIL: observability.md or rbac.md staged — cross-refs should be read-only"; exit 1
        fi
        echo "OK: cert-manager subsection landed before helm install tide; pinned version, install URL, and cross-refs present; charts/ + sibling docs untouched."
      '
    </automated>
  </verify>
  <done>
`docs/INSTALL.md` contains a new `### cert-manager prerequisite` subsection inside `## Install order`, landing before both Quickstart and cloned-repo install paths. The subsection includes: rationale citing the three chart Certificate templates, the pinned-v1.20.2 install command, all three rollout-status commands, version-pinning guidance referencing the integration test + acceptance ritual + `TIDE_CERT_MANAGER_VERSION` override, and cross-references to `docs/observability.md` and `docs/rbac.md`. One atomic commit lands on main with the prescribed message shape; `charts/`, `docs/observability.md`, and `docs/rbac.md` are untouched; no planning artifacts are staged.
  </done>
</task>

</tasks>

<verification>
After both tasks land on main, the orchestrator (NOT the executor) will run:

1. `grep -cE 'cert-manager' hack/scripts/acceptance-v1.sh` → expect ≥ 3.
2. `grep -cE 'cert-manager' docs/INSTALL.md` → expect ≥ 1, in or under the new `### cert-manager prerequisite` heading.
3. `bash -n hack/scripts/acceptance-v1.sh` → expect exit 0.
4. `git diff --name-only HEAD~2..HEAD -- charts/` → expect empty (chart contract preserved).
5. `git log --oneline -2` → expect two new commits, one `fix(quick-260530-h2h): ...`, one `docs(quick-260530-h2h): ...`.

Functional verification (re-running `make acceptance-v1`) is the orchestrator's responsibility AFTER both commits land — the executor MUST NOT invoke cert-manager or run the acceptance ritual. The orchestrator also handles the `deferred-items.md` flip in step 8 (appending the `## Discovered 2026-05-30 ... **RESOLVED 2026-05-30**` entry with the actual commit SHAs).
</verification>

<success_criteria>
- Two atomic commits land on `main`, in order: (1) `fix(quick-260530-h2h): install cert-manager v1.20.2 before helm install tide in acceptance-v1.sh`, (2) `docs(quick-260530-h2h): document cert-manager v1.20.2 prerequisite in INSTALL.md`.
- `hack/scripts/acceptance-v1.sh` carries the cert-manager install + rollout-wait wedge between the kind create block and the helm install block; `bash -n` exits 0; `TIDE_CERT_MANAGER_VERSION` env override and `v1.20.2` default are present; `--timeout` is NOT on the `kubectl apply` URL line.
- `docs/INSTALL.md` carries a new `### cert-manager prerequisite` subsection inside `## Install order`, placed before both Quickstart and cloned-repo install paths, with rationale + pinned-version install command + three rollout-status commands + version-pinning guidance + cross-refs to observability.md/rbac.md.
- `charts/` is untouched. `docs/observability.md` and `docs/rbac.md` are untouched.
- No planning artifacts (`PLAN.md`, `SUMMARY.md`, `STATE.md`, `deferred-items.md`) are committed by the executor. Orchestrator step 8 handles those.
- Executor did NOT run `make acceptance-v1` or invoke cert-manager / `kubectl apply` against any cluster. Verification was syntactic only.
</success_criteria>

<output>
After both tasks complete, write a one-page summary to `.planning/quick/260530-h2h-boot-04-acceptance-v1-cert-manager-prere/260530-h2h-SUMMARY.md` covering:

- What changed in `hack/scripts/acceptance-v1.sh` (cite the line range that now carries the cert-manager block).
- What changed in `docs/INSTALL.md` (cite the heading and where it landed relative to Quickstart / cloned-repo).
- Both commit SHAs.
- Confirmation that `charts/`, `docs/observability.md`, and `docs/rbac.md` were untouched.
- Confirmation that `bash -n hack/scripts/acceptance-v1.sh` exited 0.
- Note that the executor did NOT run the acceptance ritual — orchestrator picks up from there.

Do NOT commit the SUMMARY.md — the orchestrator handles planning-artifact commits in step 8.
</output>
