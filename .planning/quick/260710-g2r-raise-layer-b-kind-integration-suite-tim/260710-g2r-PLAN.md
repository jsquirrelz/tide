---
id: 260710-g2r
slug: raise-layer-b-kind-integration-suite-tim
type: quick
status: in-progress
created: 2026-07-10
description: >
  Raise the Layer B kind integration suite (make test-int) timeout budget across
  every layer so the suite completes — fixes main RED on the kind-sensitive check.
must_haves:
  truths:
    - The Ginkgo ctx < go-test < outer-shell < CI-step < CI-job nesting holds after the bump.
    - kind-sensitive (the merge-gating Layer-B check) can run make test-int to completion.
  artifacts:
    - test/integration/kind/suite_test.go (kindTestTimeout raised, iteration history appended)
    - Makefile (KIND_GO_TEST_TIMEOUT + INTEGRATION_TIMEOUT raised)
    - .github/workflows/kind-sensitive.yml (step + job timeout-minutes raised)
    - .github/workflows/nightly-integration.yml (kind step + job timeout-minutes raised)
  key_links:
    - test/integration/kind/suite_test.go:105
    - Makefile:21
    - .github/workflows/kind-sensitive.yml:124
    - .github/workflows/nightly-integration.yml:135
---

# Quick Task 260710-g2r — Raise Layer B kind suite timeout budget

## Problem

Main is RED. PR #6 (Phases 36–37, squash `eeb96cf`) is green on every check
**except the Layer B kind suite**, which no longer fits its timeout budget.

Phases 36/37 grew the kind suite: the agent-identity chart spec, and — the tail
hog — the **artifact-staging DASH-02 spec**, which drives a *live 4-level planner
cascade* (project → milestone → phase → plan) over a real http:// git transport
with a **12-minute `Eventually`** for the Project to reach `Complete`. It is
slow-but-passing (Phase 37 UAT was 8/8), **not hanging**.

### Evidence

| Run | Layer-B step budget | What killed it |
|---|---|---|
| kind-sensitive `29037860740` | 35m step | `##[error]…timed out after 35 minutes` — still in `artifact-staging-test` ns ~30m in |
| nightly `29085603242` | 25m step | step timeout at 25m; Layer B had run 21m54s and was not done |

The binding constraint is the **CI step `timeout-minutes`**, which is smaller
than the Makefile's own Layer-B budget and must also absorb Layer A (~5.5m) +
prep. Every timeout layer needs to grow so the innermost (Ginkgo ctx) fires
loudly *first* instead of the CI step killing the whole `make test-int` blind.

### New nesting (all consistent, innermost fails first)

```
Ginkgo ctx (kindTestTimeout) 45m  <  go-test 50m  <  outer shell 55m  <  CI step 60m  <  CI job 65m
```

Do **NOT** trim the artifact-staging spec — its `Eventually`s are the real
correctness budget for a live cascade; trimming risks flakes.

## Tasks

### Task 1 — Raise the internal (Go + Makefile) budgets
- **files:** `test/integration/kind/suite_test.go`, `Makefile`
- **action:**
  - `suite_test.go:105` — `kindTestTimeout` 25m → **45m**; append a Phase-37
    iteration-history entry; mark the stale Phase-34 "Budget stays inside the
    35m step" invariant as historical.
  - `Makefile:22` — `KIND_GO_TEST_TIMEOUT` 40m → **50m**.
  - `Makefile:21` — `INTEGRATION_TIMEOUT` 2700s → **3300s** (55m).
  - `Makefile:142` — refresh the `KIND_GO_TEST_TIMEOUT=40m / INTEGRATION_TIMEOUT=45m` comment.
- **verify:** `go vet ./test/integration/kind/...` compiles; `grep -n '45 \* time.Minute' suite_test.go`.
- **done:** internal nesting Ginkgo(45m) < go-test(50m) < outer(55m) holds.

### Task 2 — Raise the CI (GitHub Actions) budgets
- **files:** `.github/workflows/kind-sensitive.yml`, `.github/workflows/nightly-integration.yml`
- **action:**
  - `kind-sensitive.yml` — step `timeout-minutes` 35 → **60**; job 42 → **70**
    (60m step + ~5m prep needs headroom so the step fires before the job); refresh comment.
  - `nightly-integration.yml` — kind step `timeout-minutes` 25 → **60**; job 45 → **110**
    (fits SC-1 10m + kind 60m + e2e 25m + prep); refresh comment (also fix the stale `timeout 300s`).
- **verify:** both workflows parse (`python3 -c yaml.safe_load`); step < job in each.
- **done:** CI step (60m) > internal outer budget; job > all steps.

## Ship
Small PR to main. The **kind-sensitive** (Layer-B) check must go green before merge.
