---
phase: 34-pre-flight-tech-debt-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/tide/templates/configmap.yaml
  - internal/config/config_default_test.go
  - test/integration/kind/configmap_planner_concurrency_test.go
autonomous: true
requirements: [PREFLIGHT-01]

must_haves:
  truths:
    - "Rendering the chart with defaults yields plannerConcurrency: 4 in the configmap (the prior 16 is gone)"
    - "A fresh default deploy resolves the effective planner cap to 4 (config.Load on empty raw → PlannerConcurrency == 4)"
    - "The Helm template no longer carries the stale `default 16` fallback that could silently restore 16 on a values override omission"
  artifacts:
    - path: "charts/tide/templates/configmap.yaml"
      provides: "plannerConcurrency configmap key with a default-4 Helm fallback"
      contains: "default 4"
    - path: "internal/config/config_default_test.go"
      provides: "pure-Go assertion that empty/default config resolves PlannerConcurrency == 4"
    - path: "test/integration/kind/configmap_planner_concurrency_test.go"
      provides: "helm-template render contract test asserting plannerConcurrency: 4 and absence of `default 16`"
  key_links:
    - from: "charts/tide/templates/configmap.yaml"
      to: "internal/config/config.go"
      via: "configmap key plannerConcurrency consumed by config.Load → PlannerConcurrency"
      pattern: "plannerConcurrency"
    - from: "internal/config/config.go"
      to: "cmd/manager/main.go pool.New(cfg.PlannerConcurrency, \"planner\")"
      via: "resolved PlannerConcurrency sizes the planner pool"
      pattern: "PlannerConcurrency"
---

<objective>
Correct the chart configmap `plannerConcurrency` Helm fallback from `16` to `4` (PREFLIGHT-01), and pin the corrected default with two contract tests: a pure-Go assertion that default config resolution yields `PlannerConcurrency == 4`, and a helm-template render test asserting the rendered configmap contains `plannerConcurrency: 4` and no longer contains `default 16`.

Purpose: The stale `| default 16` fallback at configmap.yaml:22 is a latent single-node OOM footgun. Rendering with the current values.yaml (already `4`) yields 4 today, but a values override that omits `plannerConcurrency` would silently restore 16 and let the planning cascade over-dispatch and OOM the single node run #2 launches on. This closes the footgun and locks it shut.

Output: configmap.yaml fallback set to `default 4`; a pure-Go config-default test; a kind helm-template render contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- config.Load resolution path the pure-Go test asserts against. -->
From internal/config/config.go (applyAndValidate):
  resolveField("plannerConcurrency", raw.PlannerConcurrency, 4, &out.PlannerConcurrency)
  → on an empty rawConfig (nil PlannerConcurrency pointer), out.PlannerConcurrency resolves to the default 4.
config.Load(path string) (*Config, error) is the public entry; passing a nonexistent/empty path yields all-defaults.

From test/integration/kind/projects_pvc_test.go (helm-template render style — copy this exact shape):
  chartDir := filepath.Join("..", "..", "..", "charts", "tide")
  cmd := exec.Command("helm", "template", "tide", chartDir)
  out, err := cmd.CombinedOutput()
  → these are PLAIN go-tests (not Ginkgo) living in the kind package; they run under `make test-int`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix configmap plannerConcurrency Helm fallback 16 → 4</name>
  <files>charts/tide/templates/configmap.yaml</files>
  <read_first>
    - charts/tide/templates/configmap.yaml (the debt site is line 22)
    - charts/tide/values.yaml (line 89 is already `plannerConcurrency: 4` — DO NOT EDIT this file; read only to confirm the fallback should match)
    - internal/config/config.go:117 (config.go already defaults to 4 — the fallback must match this source of truth)
  </read_first>
  <action>
    In charts/tide/templates/configmap.yaml change the `plannerConcurrency` data line so its Helm fallback expression is `{{ .Values.plannerConcurrency | default 4 }}` (was `| default 16`). Change ONLY the integer in that single `default` clause; leave the `executorConcurrency` line and every `maxConcurrentReconciles.*` line untouched. Do NOT edit charts/tide/values.yaml (FIXED contract per CLAUDE.md — the binary catches up to the chart, the chart fallback now matches both values.yaml:89 and config.go:117).
  </action>
  <verify>
    <automated>grep -qE 'plannerConcurrency:.*default 4' charts/tide/templates/configmap.yaml && ! grep -qE 'default 16' charts/tide/templates/configmap.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'default 4' charts/tide/templates/configmap.yaml` matches the plannerConcurrency line
    - `grep 'default 16' charts/tide/templates/configmap.yaml` returns no matches (exit 1)
    - `git diff --stat charts/tide/values.yaml` shows NO change to values.yaml
    - `helm template tide charts/tide` exits 0 and its output contains `plannerConcurrency: 4`
  </acceptance_criteria>
  <done>configmap.yaml renders plannerConcurrency: 4 under defaults; the `default 16` fallback is gone; values.yaml is unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure-Go controller-level assertion that default config resolves PlannerConcurrency == 4</name>
  <files>internal/config/config_default_test.go</files>
  <read_first>
    - internal/config/config.go (lines ~95-122 — config.Load, applyAndValidate, resolveField default-4 resolution)
    - internal/controller/dispatch_concurrency_cap_test.go (style analog: pure-Go test, no envtest/Ginkgo — match its header-comment and package conventions, but this test lives in the internal/config package)
  </read_first>
  <behavior>
    - Test 1: config.Load on an empty/nonexistent path (all-defaults, no plannerConcurrency set) → returned Config.PlannerConcurrency == 4 (the safe single-node default; confirms a fresh default deploy dispatches at most 4 concurrent planners).
    - Test 2 (boundary, optional but preferred): an explicit plannerConcurrency override still wins (e.g. a raw value of 2 resolves to 2) — proving the default only applies when unset, not unconditionally.
  </behavior>
  <action>
    Create internal/config/config_default_test.go in package config (or config_test). Add a test asserting that resolving a default Config yields PlannerConcurrency == 4. Use config.Load against an empty/temp path so the all-defaults branch runs; if config.Load requires a readable file, write a minimal empty/whitespace config.yaml to a t.TempDir() and load that. Reference PREFLIGHT-01 in the test doc comment. This is the controller-level half of Success Criterion #1 — the effective cap a fresh default deploy dispatches at. No envtest, no fake K8s client needed.
  </action>
  <verify>
    <automated>go test ./internal/config/ -run TestDefault -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/config/ -run TestDefault -count=1` exits 0
    - the test asserts the resolved PlannerConcurrency equals 4 (not 16)
    - the test does NOT depend on envtest binaries or a live cluster
  </acceptance_criteria>
  <done>A pure-Go test proves default config resolution yields PlannerConcurrency == 4, passing green.</done>
</task>

<task type="auto">
  <name>Task 3: Kind helm-template render contract test for plannerConcurrency: 4</name>
  <files>test/integration/kind/configmap_planner_concurrency_test.go</files>
  <read_first>
    - test/integration/kind/projects_pvc_test.go (lines ~180-240 — copy the `helm template` exec.Command + chartDir + CombinedOutput render-test shape exactly; these are plain go-tests in the kind package)
    - charts/tide/templates/configmap.yaml (the rendered artifact under assertion)
  </read_first>
  <action>
    Create test/integration/kind/configmap_planner_concurrency_test.go as a plain go-test (NOT Ginkgo) in the kind package. Render the chart with `exec.Command("helm", "template", "tide", chartDir)` where chartDir is `filepath.Join("..", "..", "..", "charts", "tide")`, using CombinedOutput. Assert the rendered output contains the substring `plannerConcurrency: 4` AND does NOT contain `plannerConcurrency: 16`. Reference PREFLIGHT-01 and Success Criterion #1 in the test doc comment. Skip the test via testing.Short() guard if the repo's kind render tests already gate on a helm-availability/short check — mirror whatever guard projects_pvc_test.go's render tests use so it does not break a no-helm CI lane.
  </action>
  <verify>
    <automated>go test ./test/integration/kind/ -run TestConfigMapPlannerConcurrency -count=1 2>&1 | tee /tmp/34-01-render.log; echo "MAKE_EXIT=${PIPESTATUS[0]}"; grep -nE '^--- FAIL|^FAIL\s' /tmp/34-01-render.log || echo NO_GO_TEST_FAILURES</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./test/integration/kind/ -run TestConfigMapPlannerConcurrency -count=1` exits 0 (read the echoed exit code, not a Ginkgo summary)
    - `grep -nE '^--- FAIL|^FAIL\s'` over the test log returns no matches for this test
    - the test asserts rendered output contains `plannerConcurrency: 4` and not `plannerConcurrency: 16`
    - the test is a plain go-test (uses `func TestConfigMapPlannerConcurrency*(t *testing.T)`), not a Ginkgo spec
  </acceptance_criteria>
  <done>A kind-package render contract test proves the default-rendered configmap carries plannerConcurrency: 4 and not 16, passing green when run isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → chart values | An operator-supplied values override that omits `plannerConcurrency` falls through to the Helm template fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | configmap.yaml plannerConcurrency fallback | mitigate | Fallback corrected to `default 4` so an override that omits the key cannot silently restore a 16-wide planner dispatch that OOMs the single node; pinned by a render contract test |
| T-34-02 | Tampering | go/helm test deps (no new package installs) | accept | This plan adds no new go.mod or chart dependency; only stdlib `os/exec` (already used in the kind package) and existing test helpers |
</threat_model>

<verification>
- configmap.yaml renders `plannerConcurrency: 4` under defaults; `default 16` absent from the template.
- `go test ./internal/config/ -run TestDefault` green: default config resolves PlannerConcurrency == 4.
- `go test ./test/integration/kind/ -run TestConfigMapPlannerConcurrency` green (read exit code + grep `^--- FAIL|^FAIL`, not a Ginkgo summary).
- values.yaml unmodified.
</verification>

<success_criteria>
PREFLIGHT-01 (Success Criterion #1) satisfied: rendering the chart with defaults yields `plannerConcurrency: 4` (the prior 16 is gone), and a controller-level (config-resolution) assertion confirms a fresh default deploy resolves an effective planner cap of 4.
</success_criteria>

<output>
Create `.planning/phases/34-pre-flight-tech-debt-hardening/34-01-SUMMARY.md` when done
</output>
