---
phase: 28-plan-import-core
plan: 05
type: execute
wave: 3
depends_on: ["28-02", "28-04"]
files_modified:
  - internal/controller/project_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/controller/import_guard_test.go
  - cmd/manager/main.go
autonomous: true
requirements: [IMPORT-01]
must_haves:
  truths:
    - "Each of the 5 planner/task dispatch sites parks (requeue, never fail) when spec.importSource is set and ImportComplete is not yet True"
    - "Every guard sits BEFORE pool acquire so a parked dispatch never leaks a planner slot"
    - "Budget rollup is suppressed for the imported project-level envelope (no double-count)"
    - "The ImportController is registered in cmd/manager wired to TIDE_IMPORT_IMAGE"
    - "Once ImportComplete=True the guards clear and normal reconcile proceeds (planners skipped where envelopes exist)"
  artifacts:
    - path: "internal/controller/import_guard_test.go"
      provides: "per-site guard tests: parks before pool acquire when import pending; clears when complete"
      contains: "ConditionImportComplete"
    - path: "cmd/manager/main.go"
      provides: "importImage env read + ImportReconciler registration"
      contains: "ImportReconciler"
  key_links:
    - from: "internal/controller/project_controller.go"
      to: "api/v1alpha2.ConditionImportComplete"
      via: "guard reads project condition before PlannerPool.Acquire"
      pattern: "ConditionImportComplete"
    - from: "cmd/manager/main.go"
      to: "TIDE_IMPORT_IMAGE"
      via: "envOrDefault wired to ImportReconciler.ImportImage"
      pattern: "TIDE_IMPORT_IMAGE"
---

<objective>
Wire the import mechanism into the live dispatch path: insert the identical
`ImportComplete` park guard at all 5 planner/task dispatch sites (BEFORE pool acquire, so a
parked dispatch never leaks a slot — Pitfall 2), suppress budget rollup for the imported
project-level envelope (D-11/R-13, no double-count), and register the ImportController in
cmd/manager wired to `TIDE_IMPORT_IMAGE`. Per-site guard tests prove park-then-clear.

This plan closes IMPORT-01 end-to-end: with the guards in place, a Project carrying
spec.importSource holds all planner dispatch until the ImportController (plan 04) sets
ConditionImportComplete=True, after which the unchanged reconcilers find the imported
envelopes at new-UID paths and skip the planners.

Purpose: This is the final wiring wave. It depends on the condition constant (plan 02) and
the controller that sets it (plan 04, including the `ImportReconciler` type for registration).
It touches the 5 hot controllers, so it runs alone in its wave to avoid file conflicts.
Output: 5 guarded sites + rollup suppression + manager registration + guard tests, green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-plan-import-core/28-CONTEXT.md
@.planning/phases/28-plan-import-core/28-RESEARCH.md
@.planning/phases/28-plan-import-core/28-PATTERNS.md
@.planning/phases/28-plan-import-core/28-VALIDATION.md

<interfaces>
<!-- Fixed insertion anchors (verified against live code). -->
Guard inserts AFTER terminal short-circuit, BEFORE pool acquire at each site:
  project_controller.go: reconcileProjectPlannerDispatch :993 — guard before jobName :1007 and
    well before PlannerPool.Acquire :1073 (existing checkBillingHalt guard at :1056 is the analog).
  milestone_controller.go: reconcilePlannerDispatch :239 — guard before jobName :284 / Acquire :372.
  phase_controller.go: reconcilePlannerDispatch :234 — guard before jobName :278 / Acquire :370.
  plan_controller.go: reconcilePlannerDispatch :241 — guard before jobName :298 / Acquire :367.
  task_controller.go: gateChecks :305 — resolveProject at :339, CheckRejected at :366; guard
    goes AFTER :339 (project available) and BEFORE :366. Existing checkBillingHalt at :391 is the
    analog; the task guard returns a taskGateResult{shouldHalt:true, result: RequeueAfter}.
checkBillingHalt(project) guard analog: project_controller.go:1050-1069.
api/v1alpha2.ConditionImportComplete + meta.FindStatusCondition (plan 02).
cmd/manager/main.go: reporterImage env read :203, ProjectReconciler registration :397-424.
ImportReconciler (plan 04): {Client, Scheme, MaxConcurrentReconciles, WatchNamespace, ImportImage, SharedPVCName}.
handleProjectJobCompletion budget rollup at ~:1178 (the RollUpUsage call to gate on importSource).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the ImportComplete park guard at all 5 dispatch sites + suppress budget rollup</name>
  <files>internal/controller/project_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go</files>
  <read_first>
    - internal/controller/project_controller.go (reconcileProjectPlannerDispatch :993; the checkBillingHalt guard at :1050-1069 — the EXACT analog to mirror; PlannerPool.Acquire at :1073; handleProjectJobCompletion RollUpUsage at ~:1178)
    - internal/controller/milestone_controller.go (reconcilePlannerDispatch :239; jobName :284; Acquire :372; checkBillingHalt at :347 — confirm the in-scope project variable name, e.g. earlyProject)
    - internal/controller/phase_controller.go (reconcilePlannerDispatch :234; jobName :278; Acquire :370; checkBillingHalt at :345)
    - internal/controller/plan_controller.go (reconcilePlannerDispatch :241; jobName :298; Acquire :367; checkBillingHalt at :342)
    - internal/controller/task_controller.go (gateChecks :305; resolveProject :339; CheckRejected :366; checkBillingHalt :391 — the task guard analog returning taskGateResult)
    - .planning/phases/28-plan-import-core/28-PATTERNS.md (§"Five dispatch-site guards" — the EXACT guard code for the 4 planner sites and the task-site variant returning taskGateResult, plus the insertion-position table)
    - .planning/phases/28-plan-import-core/28-RESEARCH.md (Pitfall 1 task-site placement, Pitfall 2 before-pool-acquire, Pitfall 3 budget rollup suppression / Open Question 2)
    - .planning/phases/28-plan-import-core/28-CONTEXT.md (D-02 one-liner guard at each of 5 sites; D-11 budget rollup suppressed)
  </read_first>
  <action>
    At the 4 planner sites (project/milestone/phase/plan reconcilePlannerDispatch), insert the
    identical guard AFTER the terminal short-circuit and BEFORE the `jobName :=` declaration and
    pool acquire (Pitfall 2 — parking after acquire leaks a slot): if
    `project.Spec.ImportSource != nil` and `meta.FindStatusCondition(..., ConditionImportComplete)`
    is nil or not True → log V(1) "import pending; holding planner dispatch" and
    `return ctrl.Result{RequeueAfter: 5 * time.Second}, nil`. For milestone/phase/plan, use the
    already-resolved project variable that the checkBillingHalt analog uses at those sites (read
    the file to confirm its name — likely `earlyProject`).

    At the task site (gateChecks), insert the guard AFTER `resolveProject` succeeds (:339, so
    `project` is non-nil — Pitfall 1) and BEFORE `gates.CheckRejected(project)` (:366): if
    importSource set and ImportComplete not True → return
    `taskGateResult{shouldHalt: true, result: ctrl.Result{RequeueAfter: 5 * time.Second}}, nil`
    (match the shape checkBillingHalt returns at :391).

    Budget rollup suppression (D-11/R-13/Pitfall 3): in `handleProjectJobCompletion`, gate the
    `budget.RollUpUsage` call (~:1178) so it is skipped when `project.Spec.ImportSource != nil`
    (the prior run already counted the planning cost). Add a brief comment citing D-11/R-13.

    Replicate the PATTERNS.md guard verbatim at each site — no fenced code in this action.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./internal/controller/... && for f in project milestone phase plan task; do grep -q 'ConditionImportComplete' internal/controller/${f}_controller.go || { echo "MISSING guard in ${f}"; exit 1; }; done && grep -B3 -A4 'RollUpUsage' internal/controller/project_controller.go | grep -q 'ImportSource' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 of project/milestone/phase/plan/task `_controller.go` reference `ConditionImportComplete` in a dispatch guard
    - The guard at each planner site appears textually before that site's `PlannerPool.Acquire` call (verify by line order)
    - The task-site guard sits after `resolveProject` and before `gates.CheckRejected`, returning a `taskGateResult{shouldHalt:true,...}`
    - `handleProjectJobCompletion`'s `RollUpUsage` is skipped when `project.Spec.ImportSource != nil`
    - `go build ./internal/controller/...` exits 0
  </acceptance_criteria>
  <done>All 5 sites park on import-pending before pool acquire; budget rollup is suppressed for imported envelopes; controllers compile.</done>
</task>

<task type="auto">
  <name>Task 2: Per-site guard tests + register ImportController in cmd/manager</name>
  <files>internal/controller/import_guard_test.go, cmd/manager/main.go</files>
  <read_first>
    - internal/controller/suite_test.go (envtest harness for the guard tests)
    - cmd/manager/main.go (reporterImage env read :203 + ProjectReconciler registration :397-424 — the envOrDefault + SetupWithManager pattern to mirror for ImportReconciler)
    - internal/controller/import_controller.go (the ImportReconciler struct fields, from plan 04, for registration)
    - .planning/phases/28-plan-import-core/28-PATTERNS.md (§"cmd/manager/main.go — wire ImportController" — env read + registration block; the ImportReconciler has no pool/dispatcher/signing key)
    - .planning/phases/28-plan-import-core/28-VALIDATION.md (the per-task map — these rows cover the IMPORT-01 guard behavior)
  </read_first>
  <action>
    Create `internal/controller/import_guard_test.go` (envtest, reusing suite_test.go). Prove
    the guard contract for the planner sites and the task site:
    (1) park-on-pending: a Project with spec.importSource set and ConditionImportComplete absent
        (or False) → the relevant reconcile returns a RequeueAfter (parks) and dispatches NO
        planner Job AND does not acquire a pool slot (assert the planner pool's available count
        is unchanged, or that no Job was created). Cover at least the project site and the task
        site (the two distinct return shapes); a table can cover milestone/phase/plan.
    (2) clear-on-complete: same Project with ConditionImportComplete=True → the guard does not
        fire (normal dispatch path proceeds / planner skipped where an envelope exists).
    (3) no-importSource: a Project WITHOUT spec.importSource → guard never fires (regression
        guard — the import path must not affect normal Projects).

    In `cmd/manager/main.go`: add `importImage := envOrDefault("TIDE_IMPORT_IMAGE",
    "ghcr.io/jsquirrelz/tide-import:v0.1.0-dev")` immediately after the reporterImage read
    (:203), then register the ImportReconciler near the ProjectReconciler block (:397-424) with
    Client/Scheme/MaxConcurrentReconciles/WatchNamespace/ImportImage (and SharedPVCName default
    "tide-projects") + `.SetupWithManager(mgr)` with the os.Exit(1) error handling pattern.
    No fenced code in this action — replicate the PATTERNS.md analogs.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./cmd/... && grep -q 'ImportReconciler' cmd/manager/main.go && grep -q 'TIDE_IMPORT_IMAGE' cmd/manager/main.go && go test ./internal/controller/... -run 'ImportGuard|Guard' -count=1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/manager/main.go` reads `TIDE_IMPORT_IMAGE` via envOrDefault and registers `ImportReconciler` with `.SetupWithManager(mgr)`
    - `go build ./cmd/...` exits 0
    - `go test ./internal/controller/... -run 'Guard' -count=1` exits 0 with park-on-pending (no Job, no slot acquired), clear-on-complete, and no-importSource regression specs
    - The park-on-pending spec asserts NO planner pool slot is acquired (Pitfall 2 no-leak)
  </acceptance_criteria>
  <done>Guard tests prove park-then-clear with no slot leak; ImportController is registered in the manager wired to TIDE_IMPORT_IMAGE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import-pending Project → planner dispatch path | Until import completes, no planner Job (and no execution) may fire for an importing Project, or it would race the not-yet-materialized graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Denial of Service | planner pool slot leak (Pitfall 2) | mitigate | Every guard is placed BEFORE `PlannerPool.Acquire` at all 4 planner sites; the task guard parks before its dispatch. Test (1) asserts no slot is acquired while import is pending — a guard-after-acquire would leak a slot per parked reconcile and starve the pool. |
| T-28-05-02 | Tampering | premature dispatch against a partial/unmaterialized graph | mitigate | The guard holds ALL 5 dispatch sites until ConditionImportComplete=True (set only after the ImportController's atomic, cycle-checked materialization in plan 04). No planner or task can outrun the import. Test (2) confirms the guard clears only on True. |
| T-28-05-03 | Repudiation | budget double-count of salvaged planning cost (R-13) | mitigate | `handleProjectJobCompletion` skips `RollUpUsage` when `spec.importSource != nil` (D-11). The prior run already accounted for the cost; suppressing here prevents inflating the new run's spend. |
| T-28-05-04 | Spoofing | import path bleeding into normal (non-import) Projects | mitigate | The guard is gated on `project.Spec.ImportSource != nil`; Projects without it take the unchanged path. Test (3) is the regression guard. |
| T-28-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — guard one-liners + manager wiring against existing packages, zero new go.mod entries (RESEARCH §"No New Dependencies"). |
</threat_model>

<verification>
- `go build ./...` passes.
- `go test ./internal/controller/... -run Guard -count=1` green (park/clear/regression).
- Line-order check: each planner guard precedes its `PlannerPool.Acquire`; the task guard sits between resolveProject and CheckRejected.
- `make verify-dispatch-imports` + `make verify-import-firewall` green.
- VALIDATION map IMPORT-01 rows reference the guard test names.
</verification>

<success_criteria>
All 5 dispatch sites park on import-pending before acquiring a pool slot and clear once
ImportComplete=True; budget rollup is suppressed for imported envelopes; the ImportController
is registered in the manager wired to TIDE_IMPORT_IMAGE — proven by envtest, with normal
(non-import) Projects unaffected.
</success_criteria>

<output>
Create `.planning/phases/28-plan-import-core/28-05-SUMMARY.md` when done
</output>
