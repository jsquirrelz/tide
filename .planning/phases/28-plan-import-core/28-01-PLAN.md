---
phase: 28-plan-import-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/tide/values.yaml
  - charts/tide/templates/deployment.yaml
autonomous: true
requirements: [IMPORT-01]
must_haves:
  truths:
    - "The chart declares a tideImport image block before any binary reads TIDE_IMPORT_IMAGE"
    - "The manager Deployment injects TIDE_IMPORT_IMAGE from the chart value"
  artifacts:
    - path: "charts/tide/values.yaml"
      provides: "images.tideImport repository/tag/pullPolicy block"
      contains: "tideImport"
    - path: "charts/tide/templates/deployment.yaml"
      provides: "TIDE_IMPORT_IMAGE env var on the manager container"
      contains: "TIDE_IMPORT_IMAGE"
  key_links:
    - from: "charts/tide/templates/deployment.yaml"
      to: "charts/tide/values.yaml"
      via: ".Values.images.tideImport.repository / .tag | default .Chart.AppVersion"
      pattern: "TIDE_IMPORT_IMAGE"
---

<objective>
Land the FIXED chart contract for the Phase 28 import Job image BEFORE any binary or
controller reads it. Per CLAUDE.md, `charts/tide/values.yaml` is the fixed contract —
the chart leads, the binary follows. This plan adds the `images.tideImport` value block
and the `TIDE_IMPORT_IMAGE` env injection on the manager Deployment, mirroring the
existing `tideReporter` / `TIDE_REPORTER_IMAGE` pattern exactly.

Purpose: All downstream plans (cmd/tide-import binary, ImportController, manager wiring)
depend on this contract existing first. Sequencing chart-first prevents the binary-leads
anti-pattern.
Output: Additive chart changes only — no existing value is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-plan-import-core/28-CONTEXT.md
@.planning/phases/28-plan-import-core/28-RESEARCH.md
@.planning/phases/28-plan-import-core/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add images.tideImport block to chart values (FIXED contract)</name>
  <files>charts/tide/values.yaml</files>
  <read_first>
    - charts/tide/values.yaml (the file being modified — read the `images:` map, especially the `tideReporter` block at lines 192–195 and `tidePush` at 172, to match indentation and field order)
    - .planning/phases/28-plan-import-core/28-PATTERNS.md (§"charts/tide/values.yaml — add tideImport block" — the exact block to add and where)
    - CLAUDE.md (the "values.yaml is a FIXED contract" rule — this addition is additive, no existing value changes)
  </read_first>
  <action>
    Add a `tideImport` entry to the `images:` map in charts/tide/values.yaml, immediately
    after the `tideReporter` block (lines 192–195) and before the subagent.defaults comment
    at ~line 197. Fields: `repository: ghcr.io/jsquirrelz/tide-import`, `tag: ""`,
    `pullPolicy: IfNotPresent` — identical shape to `tideReporter`. Add a leading comment
    noting Phase 28 (IMPORT-01) and that an empty tag/empty TIDE_IMPORT_IMAGE makes the
    ImportController skip Job creation (mirrors the tideReporter empty-skip). Do NOT modify
    any existing value. No fenced code in this action — replicate the PATTERNS.md analog.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && grep -A3 'tideImport:' charts/tide/values.yaml | grep -q 'ghcr.io/jsquirrelz/tide-import' && helm lint charts/tide >/dev/null 2>&1 && echo OK || (helm template charts/tide --set images.tideImport.repository=x >/dev/null 2>&1 && echo OK)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A3 'tideImport:' charts/tide/values.yaml` shows `repository: ghcr.io/jsquirrelz/tide-import`, `tag: ""`, `pullPolicy: IfNotPresent`
    - `helm template charts/tide` (or `helm lint`) renders without error
    - `git diff charts/tide/values.yaml` shows only additions (no modified existing lines)
  </acceptance_criteria>
  <done>The `images.tideImport` block exists in values.yaml mirroring tideReporter; chart still renders.</done>
</task>

<task type="auto">
  <name>Task 2: Inject TIDE_IMPORT_IMAGE env on the manager Deployment</name>
  <files>charts/tide/templates/deployment.yaml</files>
  <read_first>
    - charts/tide/templates/deployment.yaml (the file being modified — read the manager container `env:` list around the `TIDE_REPORTER_IMAGE` block at lines 98–100, including the `phase9-reporter-env-injected` sentinel comment)
    - .planning/phases/28-plan-import-core/28-PATTERNS.md (§"charts/tide/templates/deployment.yaml — add TIDE_IMPORT_IMAGE" — exact env entry and placement)
  </read_first>
  <action>
    Add a `TIDE_IMPORT_IMAGE` env entry to the manager container's `env:` list in
    deployment.yaml, immediately after the `TIDE_REPORTER_IMAGE` entry (lines 98–99). Value
    template: `"{{ .Values.images.tideImport.repository }}:{{ .Values.images.tideImport.tag | default .Chart.AppVersion }}"` —
    identical templating to the reporter line. Add a `# phase28-import-env-injected` sentinel
    comment after the new entry (mirroring `# phase9-reporter-env-injected`). Replicate the
    PATTERNS.md analog; no fenced code in this action.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && helm template charts/tide --set images.tideImport.repository=ghcr.io/jsquirrelz/tide-import --set images.tideImport.tag=test 2>/dev/null | grep -q 'TIDE_IMPORT_IMAGE' && helm template charts/tide --set images.tideImport.tag=test 2>/dev/null | grep -A1 'TIDE_IMPORT_IMAGE' | grep -q 'tide-import:test' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `helm template charts/tide --set images.tideImport.tag=test` renders an env entry `TIDE_IMPORT_IMAGE` with value `ghcr.io/jsquirrelz/tide-import:test`
    - With no `--set tag`, the rendered value falls back to `:{{ .Chart.AppVersion }}` (same as reporter)
    - The `# phase28-import-env-injected` sentinel comment is present
  </acceptance_criteria>
  <done>The manager Deployment renders TIDE_IMPORT_IMAGE from the chart value with AppVersion fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → chart values | The image repository/tag is operator-controlled at install time; a malicious value could point the import Job at an attacker image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | images.tideImport repository/tag in values.yaml | accept | The chart is the trust anchor; only an operator with Helm/RBAC write can set it. Same trust model as the existing tideReporter/tidePush image values (no new exposure). Production overrides should pin `@sha256:` digests per CLAUDE.md, documented in the values comment. |
| T-28-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — pure chart YAML edits, zero new dependencies (RESEARCH §"No New Dependencies"). |
</threat_model>

<verification>
- `helm lint charts/tide` passes.
- `helm template charts/tide --set images.tideImport.tag=test` renders both the value block and the env var.
- `git diff` on both files is additive only.
</verification>

<success_criteria>
The chart declares `images.tideImport` and injects `TIDE_IMPORT_IMAGE` into the manager
Deployment, mirroring the tideReporter pattern exactly, with no existing value modified.
This contract is in place before any binary or controller reads it.
</success_criteria>

<output>
Create `.planning/phases/28-plan-import-core/28-01-SUMMARY.md` when done
</output>
