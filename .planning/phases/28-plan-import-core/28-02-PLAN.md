---
phase: 28-plan-import-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha2/import_types.go
  - api/v1alpha2/project_types.go
  - api/v1alpha2/shared_types.go
  - api/v1alpha2/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_projects.yaml
  - charts/tide/crds/tideproject.k8s_projects.yaml
autonomous: true
requirements: [IMPORT-01, IMPORT-03]
must_haves:
  truths:
    - "ProjectSpec carries an optional ImportSource *ImportSourceRef field"
    - "The ImportComplete condition vocabulary exists as exported constants consumed by the 5 dispatch sites and the ImportController"
    - "CRD manifests and deepcopy are regenerated so the new field round-trips through the API server"
  artifacts:
    - path: "api/v1alpha2/import_types.go"
      provides: "ImportSourceRef struct {SeedManifestConfigMap, SalvagedPVCSubPath}"
      contains: "type ImportSourceRef struct"
    - path: "api/v1alpha2/project_types.go"
      provides: "ImportSource *ImportSourceRef field on ProjectSpec"
      contains: "ImportSource *ImportSourceRef"
    - path: "api/v1alpha2/shared_types.go"
      provides: "ConditionImportComplete + reason/annotation constants"
      contains: "ConditionImportComplete"
  key_links:
    - from: "api/v1alpha2/project_types.go"
      to: "api/v1alpha2/import_types.go"
      via: "ImportSource *ImportSourceRef field references the new struct"
      pattern: "ImportSource \\*ImportSourceRef"
    - from: "config/crd/bases/tideproject.k8s_projects.yaml"
      to: "api/v1alpha2/project_types.go"
      via: "controller-gen regenerates the CRD schema with importSource"
      pattern: "importSource"
---

<objective>
Establish the api/v1alpha2 schema surface for import: the `ImportSourceRef` type, the
optional `ImportSource` pointer field on `ProjectSpec`, and the `ConditionImportComplete`
condition vocabulary (mirroring the existing BillingHalt const block). Regenerate CRD
manifests + deepcopy so the field round-trips through the API server. This is the contract
every downstream plan (binary, controller, guards) compiles against.

Purpose: Interface-first — the schema and condition constants are the contracts. Landing
them in Wave 1 (alongside the chart) lets the binary (Wave 2), controller (Wave 3), and
guards (Wave 4) build against fixed identifiers with no scavenger hunt.
Output: New type file, modified spec + shared types, regenerated CRD/deepcopy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-plan-import-core/28-CONTEXT.md
@.planning/phases/28-plan-import-core/28-RESEARCH.md
@.planning/phases/28-plan-import-core/28-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor mirrors. Do not explore the codebase — use these. -->
ProjectSpec (api/v1alpha2/project_types.go) ends with:
  Git *GitConfig `json:"git,omitempty"`            // line 394 — optional pointer pattern to mirror
  FailureProfile FailureProfileType `json:"failureProfile,omitempty"`  // line 405 — append after this

shared_types.go condition-block analog (lines 206–261): BillingHalt block
  ConditionBillingHalt = "BillingHalt"
  ReasonCreditBalanceTooLow = "CreditBalanceTooLow"
  AnnotationBillingResumedAt = "tideproject.k8s/billing-resumed-at"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ImportSourceRef type + ImportSource field + condition constants</name>
  <files>api/v1alpha2/import_types.go, api/v1alpha2/project_types.go, api/v1alpha2/shared_types.go</files>
  <read_first>
    - api/v1alpha2/project_types.go (the ProjectSpec struct, esp. the `Git *GitConfig` optional-pointer field at line 394 and `FailureProfile` at 405 — the exact append point and `+optional` marker style)
    - api/v1alpha2/shared_types.go (the BillingHalt/FailureHalt const blocks at lines 206–261 — the exact const-block + comment style to mirror, ending the block at ~261)
    - .planning/phases/28-plan-import-core/28-PATTERNS.md (§"api/v1alpha2/import_types.go", §"project_types.go — add ImportSource field", §"shared_types.go — add condition/reason constants" — the precise struct, field, and const block to write)
    - .planning/phases/28-plan-import-core/28-CONTEXT.md (D-02, D-03, D-07 — the ImportSourceRef shape is Claude's discretion within D-02/D-03; condition naming within D-02)
  </read_first>
  <action>
    Create `api/v1alpha2/import_types.go` with `type ImportSourceRef struct` carrying two
    fields per D-03: `SeedManifestConfigMap string` (json `seedManifestConfigMap`,
    `+kubebuilder:validation:MinLength=1`) and `SalvagedPVCSubPath string` (json
    `salvagedPVCSubPath`, `+kubebuilder:validation:MinLength=1`). Add the package header and
    doc comment matching the file convention.

    In `api/v1alpha2/project_types.go`, append an `ImportSource *ImportSourceRef` field to
    `ProjectSpec` immediately after `FailureProfile` (line 405), with `+optional` and json
    `importSource,omitempty` — mirror the `Git *GitConfig` optional-pointer pattern. Doc
    comment per PATTERNS.md (references D-01/D-02: when non-nil, ImportController runs and
    all 5 planner-dispatch sites park until ImportComplete=True).

    In `api/v1alpha2/shared_types.go`, add a new const block immediately after the FailureHalt
    block (ending ~line 261) per PATTERNS.md: `ConditionImportComplete = "ImportComplete"`,
    `ReasonImportSucceeded = "ImportSucceeded"`, `ReasonImportFailed = "ImportFailed"`,
    `ReasonCyclicPlanDetected = "CyclicPlanDetected"` (the IMPORT-04 cycle reason — add it
    alongside the others), and `AnnotationRetryImport = "tideproject.k8s/retry-import"`. Mirror
    the AnnotationBillingResumedAt comment/format. No fenced code in this action — replicate
    the PATTERNS.md analogs verbatim.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./api/... && grep -q 'type ImportSourceRef struct' api/v1alpha2/import_types.go && grep -q 'ImportSource \*ImportSourceRef' api/v1alpha2/project_types.go && grep -q 'ConditionImportComplete' api/v1alpha2/shared_types.go && grep -q 'ReasonCyclicPlanDetected' api/v1alpha2/shared_types.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `api/v1alpha2/import_types.go` contains `type ImportSourceRef struct` with both string fields and MinLength=1 markers
    - `api/v1alpha2/project_types.go` contains `ImportSource *ImportSourceRef` with `+optional`
    - `api/v1alpha2/shared_types.go` contains `ConditionImportComplete`, `ReasonImportSucceeded`, `ReasonImportFailed`, `ReasonCyclicPlanDetected`, `AnnotationRetryImport`
    - `go build ./api/...` exits 0
  </acceptance_criteria>
  <done>The schema types and condition vocabulary compile; identifiers are exported for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate deepcopy + CRD manifests</name>
  <files>api/v1alpha2/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_projects.yaml, charts/tide/crds/tideproject.k8s_projects.yaml</files>
  <read_first>
    - Makefile (the `manifests` and `generate` targets — confirm exact target names with `grep -nE '^manifests:|^generate:' Makefile` before running; the project rule is to verify a Makefile target exists before invoking)
    - api/v1alpha2/import_types.go (the struct just created — deepcopy is generated from it)
  </read_first>
  <action>
    Run `make generate` (controller-gen deepcopy) and `make manifests` (controller-gen CRD)
    to regenerate `zz_generated.deepcopy.go` (adds `DeepCopyInto`/`DeepCopy` for
    `ImportSourceRef` and updates `ProjectSpec` deepcopy to handle the new pointer) and the
    CRD YAML under `config/crd/bases/` (and the chart-mirrored CRD if the repo syncs it to
    `charts/tide/crds/`). If the chart CRD is generated by a separate sync step (check the
    Makefile for a `sync-crds`/`helmify`/copy target), run that too so the chart CRD matches.
    Do NOT hand-edit generated files. Confirm the regenerated CRD schema includes the
    `importSource` property with both sub-fields and MinLength validation.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make generate manifests >/dev/null 2>&1 && grep -q 'ImportSourceRef' api/v1alpha2/zz_generated.deepcopy.go && grep -q 'importSource' config/crd/bases/tideproject.k8s_projects.yaml && go build ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `api/v1alpha2/zz_generated.deepcopy.go` contains generated `ImportSourceRef` deepcopy methods
    - `config/crd/bases/tideproject.k8s_projects.yaml` contains an `importSource` property with `seedManifestConfigMap` and `salvagedPVCSubPath` (both minLength 1)
    - If the chart mirrors the CRD, `charts/tide/crds/tideproject.k8s_projects.yaml` also contains `importSource`
    - `go build ./...` exits 0; `git diff` on generated files shows only import-related additions
  </acceptance_criteria>
  <done>Deepcopy and CRD manifests are regenerated; the importSource field round-trips through the schema; the whole module builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ProjectSpec.ImportSource | The import trigger is a Project field; only an operator with namespace RBAC to create/patch Projects can set it (D-08 layer 1 — the trust anchor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Elevation of Privilege | spec.importSource as the import trigger | mitigate | The field is the operator gate (D-08 layer 1 / IMPORT-05). RBAC on Projects is the trust boundary; no admission logic is added here — the field merely declares intent. The actual containment (same-namespace pvcSubPath, Kind allowlist) is enforced in the ImportController + tide-import (plans 04/03). This plan's responsibility: keep the field optional and operator-settable only. |
| T-28-02-02 | Tampering | CEL/schema on ImportSourceRef sub-fields | mitigate | MinLength=1 markers prevent empty seedConfigMapRef / pvcSubPath (a blank pvcSubPath would otherwise resolve to the workspace root — a containment-escape precursor; the real containment is the K8s subPath mount in plan 04, but rejecting empties at the schema is defense-in-depth). |
| T-28-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — pure Go type additions + controller-gen regen, zero new go.mod entries (RESEARCH §"No New Dependencies"). |
</threat_model>

<verification>
- `go build ./...` passes after both tasks.
- The condition constants are referenced by name in plans 04 (ImportController sets them) and 05 (guards read ConditionImportComplete) — keep the names exactly as written here.
- CRD manifest diff is import-only and additive.
</verification>

<success_criteria>
ProjectSpec exposes `ImportSource *ImportSourceRef`; the ImportComplete condition
vocabulary exists as exported constants; deepcopy and CRD manifests are regenerated and the
module builds. Downstream plans compile against these fixed identifiers.
</success_criteria>

<output>
Create `.planning/phases/28-plan-import-core/28-02-SUMMARY.md` when done
</output>
