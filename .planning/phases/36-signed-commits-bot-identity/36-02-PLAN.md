---
phase: 36-signed-commits-bot-identity
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - api/v1alpha2/project_types.go
  - api/v1alpha1/project_types.go
  - api/v1alpha1/phase3_schema_test.go
  - api/v1alpha2/schema_test.go
  - config/crd/bases/tideproject.k8s_projects.yaml
  - internal/controller/dispatch_helpers.go
  - internal/controller/dispatch_helpers_test.go
  - cmd/manager/env.go
  - cmd/manager/env_test.go
autonomous: true
requirements: [SIGN-01]
must_haves:
  truths:
    - "Project CRD accepts spec.git.agentName/agentEmail in both API versions; admission rejects angle brackets and newlines in the name and non x@y shapes in the email"
    - "resolveAgentIdentity walks Project spec → chart tier → compiled default and is nil-safe on both project and project.Spec.Git"
    - "The manager reads TIDE_AGENT_NAME/TIDE_AGENT_EMAIL into ProviderDefaults with empty-is-unset semantics, so an unset chart value falls through to the compiled default"
  artifacts:
    - path: api/v1alpha2/project_types.go
      provides: "GitConfig.AgentName/AgentEmail with kubebuilder MaxLength + Pattern markers (D-03)"
    - path: api/v1alpha1/project_types.go
      provides: "Identical fields for type parity (served:false version, no conversion webhook)"
    - path: internal/controller/dispatch_helpers.go
      provides: "ProviderDefaults.AgentName/AgentEmail + resolveAgentIdentity(project, helmDefaults) (name, email string)"
    - path: cmd/manager/env.go
      provides: "tideHelmProviderDefaults populates AgentName/AgentEmail via envOrDefault(\"TIDE_AGENT_NAME\"/\"TIDE_AGENT_EMAIL\", \"\")"
  key_links:
    - "envOrDefault empty-string convention → ProviderDefaults zero value → resolveAgentIdentity falls through to pkggit.DefaultAgentName/Email — the D-03 chart tier"
    - "resolveAgentIdentity is the single resolution point Plan 36-03 calls at every Job-build site"
---

<objective>
Add the D-03 configuration surface: `spec.git.agentName`/`agentEmail` CRD fields (both API versions, Pattern-validated), the controller-side precedence resolver `resolveAgentIdentity` mirroring the validated `resolveImage` chain, and the manager env wiring that carries the chart tier (`TIDE_AGENT_NAME`/`TIDE_AGENT_EMAIL`) into `ProviderDefaults`. Depends on Plan 36-01 for the `pkg/git` compiled-in constants that anchor the bottom of the chain.

Purpose: SIGN-01 — the precedence chain Project spec → chart value → compiled-in default (D-03), implemented in the only place that sees Project spec and helm defaults together.
Output: CRD fields + regenerated CRD bases, resolver + tests, manager env reads + tests. Threat T-36-01 (commit-header corruption) is mitigated here via admission-time Pattern validation.
</objective>

## Artifacts this phase produces

**By this plan:**
- `api/v1alpha2/project_types.go` + `api/v1alpha1/project_types.go`: `GitConfig.AgentName string` (json `agentName,omitempty`; `+kubebuilder:validation:MaxLength=100`, ``+kubebuilder:validation:Pattern=`^[^<>\r\n]+$` ``, `+optional`) and `GitConfig.AgentEmail string` (json `agentEmail,omitempty`; `+kubebuilder:validation:MaxLength=254`, ``+kubebuilder:validation:Pattern=`^[^<>@\s]+@[^<>@\s]+$` ``, `+optional`)
- `config/crd/bases/tideproject.k8s_projects.yaml`: regenerated with both fields in both version schemas
- `internal/controller/dispatch_helpers.go`: `ProviderDefaults.AgentName string`, `ProviderDefaults.AgentEmail string`, `func resolveAgentIdentity(project *tideprojectv1alpha2.Project, helmDefaults ProviderDefaults) (name, email string)`
- `cmd/manager/env.go`: `tideHelmProviderDefaults` populates the two new fields from `TIDE_AGENT_NAME`/`TIDE_AGENT_EMAIL`
- New tests: `TestResolveAgentIdentity*` (dispatch_helpers_test.go), GitConfig round-trip extensions (both api test files), agent-identity cases in `cmd/manager/env_test.go`

**By sibling plans:** 36-01 provides `pkggit.DefaultAgentName/DefaultAgentEmail/EnvAgentName/EnvAgentEmail` + `AgentIdentity()`. 36-03 stamps resolved values into subagent-Job and push-Job env. 36-04 renders chart values `agent.name`/`agent.email` as the manager Deployment env this plan reads.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-signed-commits-bot-identity/36-CONTEXT.md
@.planning/phases/36-signed-commits-bot-identity/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agentName/agentEmail to GitConfig in both API versions + regen + schema tests</name>
  <files>api/v1alpha2/project_types.go, api/v1alpha1/project_types.go, api/v1alpha1/phase3_schema_test.go, api/v1alpha2/schema_test.go, config/crd/bases/tideproject.k8s_projects.yaml</files>
  <read_first>api/v1alpha2/project_types.go lines 195-235 (GitConfig — RepoURL Pattern precedent, LeaksConfigRef +optional precedent), api/v1alpha1/project_types.go lines 195-235 (parity sibling), api/v1alpha1/phase3_schema_test.go lines 100-130 and 295-330 (TestGitConfigTypeDeclared, TestGitConfigRoundTrip), api/v1alpha2/schema_test.go (existing coverage shape)</read_first>
  <action>Before editing, check whether Phase 35 has landed its baseRef field: `grep -n 'BaseRef' api/v1alpha2/project_types.go` — if present, mirror its exact dual-version placement and test conventions; if absent (current state), follow the phase3 GitConfig precedent directly. Add two fields to `GitConfig` in BOTH api/v1alpha2/project_types.go and api/v1alpha1/project_types.go (identical text — type parity only; v1alpha1 is served:false and no conversion webhook exists, do NOT add one): `AgentName string` with json tag `agentName,omitempty`, markers `+kubebuilder:validation:MaxLength=100`, `+kubebuilder:validation:Pattern` of `^[^<>\r\n]+$`, `+optional`; and `AgentEmail string` with json tag `agentEmail,omitempty`, markers `+kubebuilder:validation:MaxLength=254`, `+kubebuilder:validation:Pattern` of `^[^<>@\s]+@[^<>@\s]+$`, `+optional`. Doc comments per repo convention: AgentName describes the commit author/committer name used at all three TIDE commit sites and states the D-03 precedence chain (this field → chart agent.name → compiled-in TIDE Agent), notes angle brackets/newlines are rejected because they corrupt git commit headers; AgentEmail additionally notes operators should choose a real, routable address because deferred commit signing will require the committer email to match a verified machine-account email. Run `make generate manifests` to regenerate deepcopy + config/crd/bases. Extend TestGitConfigRoundTrip (v1alpha1) and the v1alpha2 schema test coverage with the two new fields (round-trip a populated GitConfig and assert field presence).</action>
  <verify>
    <automated>make generate manifests && go test ./api/... && grep -c 'agentName' config/crd/bases/tideproject.k8s_projects.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./api/...` exits 0
    - `grep -c 'agentName' config/crd/bases/tideproject.k8s_projects.yaml` returns >= 2 (field present in BOTH version schemas)
    - `grep -c 'agentEmail' config/crd/bases/tideproject.k8s_projects.yaml` returns >= 2
    - The generated projects CRD yaml carries maxLength 100/254 and pattern entries adjacent to the new properties
    - `grep -rn 'ConvertTo\|conversion' api/v1alpha1/project_types.go api/v1alpha2/project_types.go` returns 0 new hits (no webhook added)
  </acceptance_criteria>
  <done>Both API versions declare the validated fields; CRD bases regenerated; schema round-trip tests cover agentName/agentEmail; no conversion machinery introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolveAgentIdentity + ProviderDefaults extension</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/dispatch_helpers_test.go</files>
  <read_first>internal/controller/dispatch_helpers.go lines 110-130 (ProviderDefaults struct + comment conventions) and lines 265-295 (resolveImage — the precedence-walk precedent), internal/controller/dispatch_helpers_test.go (existing resolveImage test shape), pkg/git/identity.go (constants from Plan 36-01)</read_first>
  <behavior>
    - Test 1 (compiled tier): nil project + zero-value ProviderDefaults returns ("TIDE Agent", "tide-agent@tideproject.k8s")
    - Test 2 (chart tier): helmDefaults{AgentName:"Chart Agent", AgentEmail:"chart@example.com"} with project whose Spec.Git is nil returns the chart pair
    - Test 3 (spec tier beats chart): project with Spec.Git.AgentName="Spec Agent"/AgentEmail="spec@example.com" AND non-empty helmDefaults returns the spec pair
    - Test 4 (per-field independence): project sets only AgentName, chart sets only AgentEmail → returns ("Spec Agent", "chart@example.com")
    - Test 5 (nil safety): project non-nil with Spec.Git nil does not panic
  </behavior>
  <action>Extend `ProviderDefaults` (dispatch_helpers.go:114) with `AgentName string` and `AgentEmail string`, doc-commented with the same "empty string means no Helm default" convention as the existing Image field. Add `func resolveAgentIdentity(project *tideprojectv1alpha2.Project, helmDefaults ProviderDefaults) (name, email string)` beside resolveImage: initialize from `pkggit.DefaultAgentName`/`pkggit.DefaultAgentEmail` (import pkggit "github.com/jsquirrelz/tide/pkg/git"), override each field independently with non-empty helmDefaults values, then with non-empty `project.Spec.Git.AgentName`/`AgentEmail` — nil-checking both `project` and `project.Spec.Git` (Spec.Git is *GitConfig, Pitfall 7). Doc comment documents the D-03 chain in the same arrow style resolveImage uses (Spec.Git.agent* → helmDefaults.Agent* → compiled default). Write the five behavior tests first (RED) as plain go-tests in dispatch_helpers_test.go — they must run under `-run TestResolveAgentIdentity` without envtest assets — then implement (GREEN).</action>
  <verify>
    <automated>go test ./internal/controller/ -run TestResolveAgentIdentity -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/ -run TestResolveAgentIdentity -v` exits 0 with all five behavior cases green
    - `grep -c 'AgentName\|AgentEmail' internal/controller/dispatch_helpers.go` returns >= 6 (struct fields + resolver references)
    - resolveAgentIdentity's doc comment contains "D-03"
    - The resolver contains no os.Getenv call (resolution is pure — env reading is the manager's job)
  </acceptance_criteria>
  <done>ProviderDefaults carries the chart-tier identity; resolveAgentIdentity implements the full D-03 walk with per-field independence and nil safety, proven by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Manager env wiring — TIDE_AGENT_NAME/EMAIL into ProviderDefaults</name>
  <files>cmd/manager/env.go, cmd/manager/env_test.go</files>
  <read_first>cmd/manager/env.go lines 30-105 (envOrDefault + tideHelmProviderDefaults + the documented empty-is-unset convention), cmd/manager/env_test.go lines 20-60 (test naming/shape conventions), pkg/git/identity.go (EnvAgentName/EnvAgentEmail constants)</read_first>
  <action>In `tideHelmProviderDefaults` (cmd/manager/env.go:96), populate the two new ProviderDefaults fields: `AgentName: envOrDefault(pkggit.EnvAgentName, "")` and `AgentEmail: envOrDefault(pkggit.EnvAgentEmail, "")` (import pkggit "github.com/jsquirrelz/tide/pkg/git"; using the constants keeps the env-var names single-sourced). The empty-string fallback is deliberate and load-bearing: "" means the chart tier is unconfigured, so resolveAgentIdentity falls through to the compiled default — add a one-line comment citing (D-03) and the empty-is-unset convention documented on envOrDefault. Extend cmd/manager/env_test.go with two cases: env set (t.Setenv both vars → fields carry the values) and env unset (fields are empty strings, NOT the compiled defaults — the manager must not collapse the chart tier into the compiled tier).</action>
  <verify>
    <automated>go test ./cmd/manager/ -run 'TestTideHelmProviderDefaults|TestEnvOrDefault' -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/manager/` exits 0
    - env-unset test asserts AgentName == "" and AgentEmail == "" (empty, not defaulted — the resolver owns defaulting)
    - env-set test asserts both fields round-trip custom values
    - `grep -c 'EnvAgentName\|EnvAgentEmail' cmd/manager/env.go` returns 2 (constants used, no string literals duplicated)
  </acceptance_criteria>
  <done>Chart-rendered manager env reaches ProviderDefaults; unset chart values stay empty so the compiled default wins at resolve time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → K8s API | Untrusted CRD strings (agentName/agentEmail) cross admission into controller-managed state |
| controller → commit headers | Resolved identity strings flow into git commit headers downstream (Plans 01/03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Tampering | GitConfig.AgentName/AgentEmail → commit headers | medium | mitigate | kubebuilder Pattern markers reject angle brackets/CR/LF in name and enforce x@y shape (no whitespace/brackets) in email at admission; MaxLength 100/254 bounds the strings. This is the phase's primary mitigation — landed in Task 1. |
| T-36-02 | Spoofing | operator sets arbitrary identity via Project spec | low | accept | Same trust tier as chart install; identity is public commit metadata (precedent T-11-01-04). Docs (Plan 36-04) note the future email-match requirement for signing. |
| T-36-03 | Information Disclosure | identity values in CRD/pod env/logs | low | accept | Public commit metadata, not a secret |
| T-36-SC | Tampering | package installs | low | accept | No package-manager installs in this phase (Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `go test ./api/... ./cmd/manager/` green; `go test ./internal/controller/ -run TestResolveAgentIdentity` green
- CRD bases yaml contains agentName/agentEmail with pattern + maxLength in both version schemas
- Precedence proven with NON-DEFAULT values at every tier (Pitfall 3: the compiled default masks wiring gaps — tests must use custom strings)
</verification>

<success_criteria>
- An operator can set spec.git.agentName/agentEmail on a Project and admission validates the strings
- resolveAgentIdentity returns spec over chart over compiled, per field, nil-safe
- The chart tier transport (manager env → ProviderDefaults) preserves empty-is-unset so defaulting happens exactly once, in the resolver
</success_criteria>

<output>
Create `.planning/phases/36-signed-commits-bot-identity/36-02-SUMMARY.md` when done.
</output>
