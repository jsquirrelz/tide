---
phase: 36-signed-commits-bot-identity
plan: 04
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - hack/helm/tide-values.yaml
  - hack/helm/augment-tide-chart.sh
  - hack/helm/tide-chart.yaml
  - hack/helm/tide-crds-chart.yaml
  - charts/tide/
  - charts/tide-crds/
  - test/integration/kind/agent_identity_chart_test.go
  - docs/project-authoring.md
autonomous: true
requirements: [SIGN-01]
must_haves:
  truths:
    - "helm chart renders TIDE_AGENT_NAME/TIDE_AGENT_EMAIL on the manager Deployment sourced from agent.name/agent.email values (empty by default → compiled default wins)"
    - "The chart version is bumped exactly once across Phases 35+36 (D-06 batching) — conditional on the observed current version"
    - "charts/ is reproducible from hack/helm sources (make verify-chart-reproducible green)"
    - "docs/project-authoring.md documents agentName/agentEmail with the precedence chain and the routable-email note"
  artifacts:
    - path: hack/helm/tide-values.yaml
      provides: "agent.name/agent.email value block (empty defaults, precedence comment)"
    - path: hack/helm/augment-tide-chart.sh
      provides: "phase36-agent-env-injected marker block rendering the two env vars on the manager container"
    - path: test/integration/kind/agent_identity_chart_test.go
      provides: "helm-template contract test pinning the env render (plain go-test, no cluster)"
    - path: docs/project-authoring.md
      provides: "GitConfig table rows + identity precedence + routable-email guidance"
  key_links:
    - "agent.name/agent.email values → deployment env TIDE_AGENT_NAME/TIDE_AGENT_EMAIL → cmd/manager envOrDefault reads — the names must match Plan 36-02's reads byte-for-byte"
    - "config/crd/bases (regenerated in Plan 36-02) → make helm-crds → charts/tide-crds schema carries agentName/agentEmail"
---

<objective>
Wire the chart tier of the D-03 precedence chain: `agent.name`/`agent.email` values rendered as `TIDE_AGENT_NAME`/`TIDE_AGENT_EMAIL` env on the manager Deployment, the D-06 batched chart version bump (conditional — Phase 35 may have bumped already), regenerated `charts/` output, a helm-template contract test, and operator docs. All chart edits land in `hack/helm/` canonical sources — `charts/tide/values.yaml` is the FIXED contract and is only ever touched via `make helm-controller` regeneration (D-06; CLAUDE.md).

Purpose: SIGN-01 — operators can set an install-wide identity without touching every Project; the value names deliberately avoid the existing `signingKey` HMAC namespace (Claude's-discretion chart-shape question, resolved per RESEARCH).
Output: hack/helm source edits, regenerated charts/, contract test, docs.
</objective>

## Artifacts this phase produces

**By this plan:**
- `hack/helm/tide-values.yaml`: top-level `agent:` block — `agent.name: ""`, `agent.email: ""` with precedence comment (Project spec → these values → compiled-in `TIDE Agent <tide-agent@tideproject.k8s>`)
- `hack/helm/augment-tide-chart.sh`: new marker `# phase36-agent-env-injected` + heredoc block rendering `TIDE_AGENT_NAME: {{ .Values.agent.name }}` and `TIDE_AGENT_EMAIL: {{ .Values.agent.email }}` on the manager container
- `hack/helm/tide-chart.yaml` + `hack/helm/tide-crds-chart.yaml`: version/appVersion 1.0.7 (CONDITIONAL — only if still 1.0.6; D-06 batches with Phase 35)
- `charts/tide/`, `charts/tide-crds/`: regenerated via `make helm-controller helm-crds`
- `test/integration/kind/agent_identity_chart_test.go` (NEW): `TestHelmDeploymentTemplateRendersAgentIdentityEnv`
- `docs/project-authoring.md`: `git.agentName`/`git.agentEmail` table rows + identity note

**By sibling plans:** 36-01 fixes the env-var names + compiled default. 36-02 regenerates config/crd/bases with the CRD fields (input to `make helm-crds` here) and reads the rendered env in the manager. 36-03 stamps resolved values into workload Jobs.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-signed-commits-bot-identity/36-CONTEXT.md
@.planning/phases/36-signed-commits-bot-identity/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart sources — values block, env augment, conditional version bump, regen</name>
  <files>hack/helm/tide-values.yaml, hack/helm/augment-tide-chart.sh, hack/helm/tide-chart.yaml, hack/helm/tide-crds-chart.yaml, charts/tide/, charts/tide-crds/</files>
  <read_first>hack/helm/augment-tide-chart.sh lines 100-290 (the ENV3 marker-guarded heredoc mechanism at 233-283 — the exact pattern to copy, inserted before the envFrom anchor), hack/helm/tide-values.yaml lines 260-290 (existing block style; the signingKey HMAC block at :274 — a DIFFERENT namespace, do not touch or collide), hack/helm/tide-chart.yaml + hack/helm/tide-crds-chart.yaml (current version lines), Makefile lines 700-760 (helm-controller, helm-crds, verify-chart-reproducible)</read_first>
  <action>Three source edits, then regenerate. (1) hack/helm/tide-values.yaml: add a top-level `agent:` block with `name: ""` and `email: ""`, commented: agent commit identity (Phase 36 / SIGN-01); precedence Project.spec.git.agentName/agentEmail → these values → compiled-in default `TIDE Agent <tide-agent@tideproject.k8s>`; empty = use compiled default. Do NOT nest under `subagent.*` (identity applies to push Jobs too) and do NOT touch the `signingKey` block (HMAC envelope-signing — unrelated). (2) hack/helm/augment-tide-chart.sh: add a new marker-guarded block copying the ENV3 mechanism exactly — marker string `# phase36-agent-env-injected`, block containing `- name: TIDE_AGENT_NAME` / `value: "{{ .Values.agent.name }}"` and `- name: TIDE_AGENT_EMAIL` / `value: "{{ .Values.agent.email }}"`, inserted at the same manager-container env anchor the ENV3 block uses, idempotent via the marker check. (3) Conditional D-06 version bump: read hack/helm/tide-chart.yaml — if `version:` is still `1.0.6`, set version to 1.0.7 and appVersion to "1.0.7" in BOTH hack/helm/tide-chart.yaml and hack/helm/tide-crds-chart.yaml; if Phase 35 already bumped them past 1.0.6, change nothing (one batched bump total). Then run `make helm-controller helm-crds` and commit the regenerated charts/ output together with the hack/helm sources. Never edit charts/ by hand. Finally run `make verify-chart-reproducible`.</action>
  <verify>
    <automated>make helm-controller helm-crds && make verify-chart-reproducible && grep -c 'TIDE_AGENT_NAME\|TIDE_AGENT_EMAIL' charts/tide/templates/deployment.yaml && grep -c 'agentName' charts/tide-crds/templates/*.yaml | grep -v ':0'</automated>
  </verify>
  <acceptance_criteria>
    - `charts/tide/templates/deployment.yaml` contains both `TIDE_AGENT_NAME` and `TIDE_AGENT_EMAIL` wired to `.Values.agent.name` / `.Values.agent.email`
    - `charts/tide/values.yaml` (regenerated) contains the `agent:` block with empty-string name/email
    - `make verify-chart-reproducible` exits 0 (no hand-edited drift)
    - The charts/tide-crds project schema contains `agentName` and `agentEmail`
    - `grep -n 'version:' hack/helm/tide-chart.yaml hack/helm/tide-crds-chart.yaml` shows 1.0.7 on both OR both were already >1.0.6 before this task (record which case applied in the SUMMARY)
    - The `signingKey` block is byte-identical to before
  </acceptance_criteria>
  <done>Chart sources carry the agent identity surface; charts/ regenerated and reproducible; exactly one batched version bump across Phases 35+36.</done>
</task>

<task type="auto">
  <name>Task 2: Helm-template contract test for the identity env render</name>
  <files>test/integration/kind/agent_identity_chart_test.go</files>
  <read_first>test/integration/kind/projects_pvc_test.go lines 140-176 (TestHelmDeploymentTemplateRendersManagerPodAnnotations — the byte-contains contract-test style to copy, including package decl, any build tags, and the relative charts path ../../../charts/tide)</read_first>
  <action>Create test/integration/kind/agent_identity_chart_test.go in the same package (match the existing file's package declaration and build tags exactly). Add `TestHelmDeploymentTemplateRendersAgentIdentityEnv`: read charts/tide/templates/deployment.yaml via os.ReadFile with the same ../../../ relative path convention and assert it contains all four byte-strings — `TIDE_AGENT_NAME`, `TIDE_AGENT_EMAIL`, `.Values.agent.name`, `.Values.agent.email` — with failure messages citing SIGN-01/(D-03) in the style of the Phase 13 tests in that file. This is a plain go-test (no cluster, no helm binary): it locks the chart contract so a helmify regen that drops the augment block fails CI — the exact failure class CLAUDE.md's Phase-7 warning describes.</action>
  <verify>
    <automated>go test ./test/integration/kind/ -run TestHelmDeploymentTemplateRendersAgentIdentityEnv -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./test/integration/kind/ -run TestHelmDeploymentTemplateRendersAgentIdentityEnv -v` exits 0 (test runs and passes without a kind cluster)
    - The test asserts all four byte-strings (two env names + two value paths)
    - Deleting the augment block from a scratch copy of deployment.yaml would fail the test (assertion is on the committed template file)
  </acceptance_criteria>
  <done>A committed contract test pins the manager-deployment identity env render against helmify-regen drift.</done>
</task>

<task type="auto">
  <name>Task 3: Operator docs — GitConfig table rows + identity note</name>
  <files>docs/project-authoring.md</files>
  <read_first>docs/project-authoring.md lines 40-60 (the GitConfig field table — row format/voice to match) and lines 565-590 (the git credentials section — where the identity paragraph lands), .planning/phases/36-signed-commits-bot-identity/36-CONTEXT.md (specifics: routable-email guidance)</read_first>
  <action>Add two rows to the GitConfig field table (after the git.credsSecretRef row, matching column format and the tight em-dash-heavy voice): `git.agentName` — string, optional, default (none → compiled-in "TIDE Agent") — commit author/committer name at all three TIDE commit sites (task commits, integrate merges, boundary pushes); precedence Project spec → chart `agent.name` → compiled-in default; angle brackets/newlines rejected at admission. `git.agentEmail` — string, optional, default (none → compiled-in "tide-agent@tideproject.k8s") — same precedence via chart `agent.email`; must be a bare `local@domain` shape. Add a short paragraph in the git section noting: (a) the chart-wide values `agent.name`/`agent.email` set an install-wide identity without per-Project config; (b) choose a real, routable email — when (deferred) commit signing lands, the committer email must match a verified email on the machine account holding the public key, so choosing well now avoids identity churn on existing history. Do not document any signing-key configuration (D-01 — signing is deferred; nothing key-related exists).</action>
  <verify>
    <automated>grep -c 'agentName\|agentEmail' docs/project-authoring.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'agentName' docs/project-authoring.md` returns >= 2 (table row + prose)
    - `grep -c 'agentEmail' docs/project-authoring.md` returns >= 2
    - The routable-email/signing note is present; no signing-key Secret configuration is documented
    - Table rows render with the same column count as existing GitConfig rows
  </acceptance_criteria>
  <done>Operators can discover both configuration tiers and the email-choice guidance from the existing GitConfig docs anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helm values → manager env | Install-time operator config crosses into the controller Deployment (same trust tier as the install itself) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-02 | Spoofing | chart-tier identity values | low | accept | Whoever runs helm install already controls the manager image and RBAC — identity values add no new capability (precedent T-11-01-04). Docs note the future email-match requirement. |
| T-36-01 | Tampering | chart values bypass CRD Pattern validation (no admission gate on helm values) | low | accept | Chart tier is operator-only (not per-Project multi-tenant input); a malformed value degrades only the operator's own commit headers. CRD tier — the untrusted-input path — is validated (Plan 36-02). |
| T-36-SC | Tampering | package installs | low | accept | No package-manager installs in this phase (Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `make verify-chart-reproducible` green; contract test green without a cluster
- Rendered deployment env names byte-match the manager's envOrDefault reads (TIDE_AGENT_NAME/TIDE_AGENT_EMAIL)
- Exactly one chart version bump across Phases 35+36 (conditional logic recorded in SUMMARY)
</verification>

<success_criteria>
- `helm template` of charts/tide renders the two identity env vars from agent.* values; empty defaults preserve compiled-default behavior
- charts/ regenerable from hack/helm with zero drift
- Docs cover both config tiers, the precedence chain, and the routable-email guidance
</success_criteria>

<output>
Create `.planning/phases/36-signed-commits-bot-identity/36-04-SUMMARY.md` when done.
</output>
