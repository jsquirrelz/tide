---
phase: 35-git-base-ref
plan: 03
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - api/v1alpha2/shared_types.go
  - internal/controller/push_helpers.go
  - internal/controller/push_helpers_test.go
  - internal/controller/project_controller.go
  - internal/controller/project_baseref_halt_test.go
autonomous: true
requirements: [BASE-02, BASE-03]
must_haves:
  truths:
    - "A clone Job that terminal-fails with envelope reason baseref-unresolvable (and an envelope baseRef matching current spec) yields CloneFailed=True/BaseRefUnresolvable with ObservedGeneration set — and the controller does NOT delete the Job and does NOT re-dispatch, even after the Job is TTL-GC'd (the halt is the condition, not Job existence — Pitfall 2)"
    - "Editing spec.git.baseRef (new generation) releases the halt: a fresh clone Job dispatches (D-07 — classify-don't-retry, recovery costs one kubectl edit)"
    - "On clone success the controller reads the clone envelope FIRST and stamps status.git.baseSHA + CloneComplete=true in the SAME status patch (D-11); an unreadable success envelope delays the flip with a short requeue, bounded by a sub-TTL cutoff that flips with empty baseSHA and logs degraded provenance"
    - "Every other clone-failure reason (auth-failed, network-timeout, clone-failed, unparseable envelope) keeps the existing delete-and-re-dispatch behavior unchanged — halting those classes is an explicit non-goal (RESEARCH Open Q3)"
  artifacts:
    - "api/v1alpha2/shared_types.go — ReasonBaseRefUnresolvable constant beside ConditionCloneFailed"
    - "internal/controller/push_helpers.go — CloneOptions.BaseRef; buildCloneJob --base-ref + --project-uid args + termination-message wiring"
    - "internal/controller/project_controller.go — envelope BaseSHA/BaseRef fields, generation-scoped dispatch-halt gate, WR-03 classification branch, read-before-flip baseSHA stamp"
    - "internal/controller/project_baseref_halt_test.go — envtest specs for halt/release/stamp/other-reason semantics"
  key_links:
    - "envelope.reason == baseref-unresolvable → CloneFailed/BaseRefUnresolvable condition → dispatch guard third gate (the halt chain)"
    - "envelope.baseSHA → status.git.baseSHA in the CloneComplete flip patch (the provenance chain)"
    - "condition.ObservedGeneration == project.Generation is what scopes the halt to the bad spec (metadata.generation bump releases it)"
---

<objective>
Teach the ProjectReconciler to classify clone failures (D-06), halt re-dispatch for the baseref-unresolvable class in a TTL-GC-proof, generation-scoped way (D-07, Pitfall 2), and stamp `status.git.baseSHA` from the clone success envelope on every run (D-11) — extending the billing-halt classify-don't-retry posture to the clone path.

Purpose: BASE-02's typed-condition fail-fast and BASE-03's baseSHA stamp are controller behaviors; this plan is where the envelope contract from plan 35-02 meets the CRD fields from plan 35-01.
Output: controller plumbing + classification + stamping, locked by envtest specs beside the existing BYPASS-02 clone-idempotency suite.

Consumed contracts (fixed upstream, do not drift): envelope JSON keys `baseSHA`/`baseRef`/`reason`, reason value `baseref-unresolvable`, exit 2, Kind `CloneResult` (plan 35-02); `spec.git.baseRef` / `status.git.baseSHA` fields (plan 35-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-git-base-ref/35-CONTEXT.md
@.planning/phases/35-git-base-ref/35-RESEARCH.md
@.planning/phases/35-git-base-ref/35-PATTERNS.md
</context>

## Artifacts this phase produces (phase-wide index)

See 35-01-PLAN.md for the full table. This plan delivers: `ReasonBaseRefUnresolvable`, `CloneOptions.BaseRef`, the `--base-ref`/`--project-uid` clone-Job args, clone-container termination-message wiring, `pushResultEnvelope.BaseSHA`/`.BaseRef`, the generation-scoped dispatch-halt gate, the WR-03 classification branch, the read-before-flip baseSHA stamp, and `internal/controller/project_baseref_halt_test.go`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plumbing, classification, halt gate, and baseSHA stamp in the reconciler</name>
  <files>api/v1alpha2/shared_types.go, internal/controller/push_helpers.go, internal/controller/push_helpers_test.go, internal/controller/project_controller.go</files>
  <read_first>
    - internal/controller/project_controller.go:560-640 — the three clone arms exactly as they exist: dispatch guard (:571), set-on-success (:594-601), WR-03 terminal-failed (:610-631); and :40-109 pushResultEnvelope + readPushEnvelope (already job-name-parameterized — reusable for the clone Job as-is)
    - internal/controller/push_helpers.go:78-96 CloneOptions, :249-258 buildPushJob termination-message block (the wiring to copy), :292-374 buildCloneJob (arg-append idiom at :298-303; container at :343-364 currently lacks termination wiring)
    - internal/controller/billing_halt.go:104-135 — the classify-don't-retry condition-stamp precedent; NOTE: that site does not set ObservedGeneration — THIS site must (D-07 release-on-edit depends on it)
    - api/v1alpha2/shared_types.go:103-108 — ConditionCloneFailed block where the new Reason constant lands
    - .planning/phases/35-git-base-ref/35-RESEARCH.md §Pattern 2 (success-path read-before-flip contract — the plan-level pin RESEARCH demands), §Pattern 3, §Pattern 4, §Code Examples "Condition stamp"
  </read_first>
  <behavior>
    - TestBuildCloneJobBaseRefArg: CloneOptions.BaseRef "v1.2.3" → args contain the element `--base-ref=v1.2.3`; empty BaseRef → no --base-ref element
    - TestBuildCloneJobProjectUIDArg: args always contain `--project-uid=<project.UID>`
    - TestBuildCloneJobTerminationWiring: clone container has TerminationMessagePath `/dev/termination-log` and TerminationMessagePolicy FallbackToLogsOnError (parity with buildPushJob :257-258)
  </behavior>
  <action>
    api/v1alpha2/shared_types.go: add `ReasonBaseRefUnresolvable = "BaseRefUnresolvable"` beside ConditionCloneFailed (:108), with a comment distinguishing it from the existing "CloneJobFailed" reason (which keeps its delete-and-re-dispatch meaning). If api/v1alpha1's shared types file also declares ConditionCloneFailed, mirror the constant there (P9 parity); if not, v1alpha2-only is correct.
    push_helpers.go: add `BaseRef string` to CloneOptions (doc: D-01 forms, resolved in the clone Job — the single resolution site per D-04, no controller-side ls-remote preflight). In buildCloneJob: append `--project-uid=` + string(project.UID) unconditionally (enables the PVC envelope copy), append `--base-ref=` + opts.BaseRef when non-empty (same idiom as --run-branch at :298-303), and add TerminationMessagePath + TerminationMessagePolicy to the container (copy buildPushJob :257-258 verbatim — without this the controller can never read a clone envelope).
    project_controller.go, four coordinated edits:
    (1) pushResultEnvelope gains `BaseSHA string` json `baseSHA` and `BaseRef string` json `baseRef` — readPushEnvelope needs no other change (reuse it with cloneJobName).
    (2) Dispatch guard (:571) gains a third gate (Pattern 4): before dispatching, look up the CloneFailed condition via meta.FindStatusCondition; skip dispatch when Status==True AND Reason==ReasonBaseRefUnresolvable AND cond.ObservedGeneration == project.Generation. Also wire cloneOpts.BaseRef = project.Spec.Git.BaseRef inside the existing Spec.Git non-nil guard.
    (3) Set-on-success arm (:594-601) becomes read-before-flip (Pattern 2 — this is the pinned contract): call readPushEnvelope(ctx, ns, cloneJobName); on ok → in ONE MergeFrom status patch set Status.Git.BaseSHA = env.BaseSHA, Status.Git.CloneComplete = true, and meta.SetStatusCondition CloneFailed=False (Reason "CloneSucceeded", ObservedGeneration project.Generation) so a prior halt clears; on not-ok → if existingClone.Status.CompletionTime is non-nil and time.Since(it) > 60s (well under the 300s Job TTL — the cutoff MUST stay sub-TTL or the re-clone/no-fresh-envelope stall of RESEARCH Pattern 2 recurs) → flip CloneComplete with empty BaseSHA and logger.Info a degraded-provenance line; else return ctrl.Result{RequeueAfter: 10 * time.Second} without flipping.
    (4) WR-03 arm (:610-631) gains a classification branch BEFORE the existing delete: read the envelope; when ok AND env.Reason == "baseref-unresolvable" AND env.BaseRef == current spec baseRef (extract with a nil-safe read of project.Spec.Git; on mismatch the operator already edited — fall through to delete-and-re-dispatch so the corrected spec gets a fresh clone), stamp via meta.SetStatusCondition: Type ConditionCloneFailed, Status True, Reason ReasonBaseRefUnresolvable, ObservedGeneration project.Generation (explicitly — the billing-halt analog omits it), Message exactly `unable to resolve '<ref>' to a commit SHA; fix spec.git.baseRef to re-attempt the clone` with the ref interpolated (Argo CD canonical wording per D-06; charset already admission-filtered so the message is log/UI-safe — T-35-03), then status-patch and return ctrl.Result{} with NO Job deletion and NO requeue loop. Every other ok/not-ok combination falls through to the existing delete-and-re-dispatch + CloneJobFailed stamp unchanged.
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run TestBuildCloneJob -count=1 -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - The three buildCloneJob tests pass; `go build ./...` green
    - project_controller.go's dispatch guard references ReasonBaseRefUnresolvable and compares cond.ObservedGeneration to project.Generation (source assertion: both identifiers appear in the guard block)
    - The WR-03 classified branch contains no r.Delete call and returns without Requeue
    - The success arm sets BaseSHA, CloneComplete, and the CloneFailed=False condition inside a single MergeFrom/Status().Patch pair
    - The 60-second cutoff constant is declared with a comment naming the 300s TTL bound it must stay under
  </acceptance_criteria>
  <done>Controller plumbs baseRef to the clone Job, can read clone envelopes, halts the unresolvable class generation-scoped, and stamps provenance in the CloneComplete patch — all compile-and-unit verified; behavioral lock lands in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Envtest specs — halt, TTL-GC survival, release-on-edit, stamp, other-reason passthrough</name>
  <files>internal/controller/project_baseref_halt_test.go</files>
  <read_first>
    - internal/controller/project_clone_idempotency_test.go — the sibling suite (Ginkgo, Label("envtest")) whose harness, fixture, and Job-status-fabrication idioms this file copies; its Spec 3 covers today's WR-03 delete-to-re-dispatch (the behavior Spec F below must show is preserved for other reasons)
    - test/integration/envtest/leak_blocked_test.go — the proven idiom for fabricating a pod with a Terminated ContainerStatus carrying a JSON termination message (status subresource writes work in envtest; no kubelet needed)
    - .planning/phases/35-git-base-ref/35-RESEARCH.md §Pitfall 2 — the exact test plan this file implements
  </read_first>
  <behavior>
    - Spec A (classify + no-delete): Project with spec.git.baseRef "no-such-ref"; fabricate the tide-clone-<uid> Job terminal-Failed and its pod's termination message as JSON {reason: baseref-unresolvable, baseRef: no-such-ref, exitCode: 2}; reconcile → CloneFailed=True with Reason BaseRefUnresolvable, Message containing `unable to resolve 'no-such-ref' to a commit SHA`, ObservedGeneration == project's current generation; the Job still exists (not deleted)
    - Spec B (TTL-GC-proof halt — Pitfall 2): after Spec A's condition, delete the clone Job manually (simulating TTL GC), reconcile → NO new tide-clone-* Job is created
    - Spec C (release on edit — D-07): bump spec.git.baseRef to a different value (generation increments), reconcile → a fresh clone Job IS created
    - Spec D (stamp — D-11/BASE-03): Project with baseRef set; fabricate the clone Job Succeeded with a success envelope {baseSHA: <40-hex>, baseRef: <ref>, exitCode: 0}; reconcile → status.git.baseSHA == that SHA AND status.git.cloneComplete true (same reconcile), CloneFailed condition False; ALSO assert the created Project's spec.git.baseRef read back from the API server equals what was applied — envtest installs the regenerated CRDs from config/crd/bases, so this is the apply-half of BASE-03's upgrade-path test (unpruned round-trip through a real API server)
    - Spec E (unreadable success envelope): clone Job Succeeded but pod has no termination message → CloneComplete NOT flipped and the reconcile result requeues; then fabricate/backdate the Job's CompletionTime beyond the cutoff → next reconcile flips CloneComplete with empty baseSHA
    - Spec F (other reasons unchanged): terminal-Failed Job with envelope reason auth-failed (or an unparseable message) → Job deleted and CloneJobFailed condition stamped — the existing WR-03 behavior, byte-for-byte semantics
  </behavior>
  <action>
    Create internal/controller/project_baseref_halt_test.go as a Ginkgo Describe (Label "envtest") named for BASE-02/D-06/D-07 semantics, in the same package and suite as project_clone_idempotency_test.go. Fabricate Job status via status-subresource updates (Failed/Succeeded/CompletionTime) and pod termination messages via the leak_blocked_test.go idiom (create pod with label job-name=tide-clone-<uid>, set ContainerStatuses[0].State.Terminated.Message to the marshaled envelope JSON). Use the reason string literal "baseref-unresolvable" and JSON keys baseSHA/baseRef exactly as the plan 35-02 contract defines. For Spec E's cutoff, either backdate CompletionTime on the fabricated Job or expose the cutoff as a package-level var the test can shorten — prefer backdating (no production seam).
  </action>
  <verify>
    <automated>go test ./internal/controller/ -count=1 -timeout 360s -ginkgo.focus='baseref' -v</automated>
  </verify>
  <acceptance_criteria>
    - All six specs pass under the envtest harness (KUBEBUILDER_ASSETS via setup-envtest, as `make test` provides)
    - Spec B genuinely deletes the Job before asserting no re-dispatch (the assertion window uses Consistently or a bounded Eventually-negative, matching the sibling suite's idiom)
    - Spec C asserts generation actually incremented before asserting the new Job
    - Spec D asserts baseSHA and cloneComplete observed true in the same status snapshot, and asserts the read-back spec.git.baseRef equality (unpruned)
    - `make test` green (unit tier includes this package; read MAKE_EXIT, not just output)
  </acceptance_criteria>
  <done>BASE-02's classify-don't-retry mechanics (halt survives TTL GC, releases on spec edit) and BASE-03's stamp + API-server round-trip are behaviorally locked at Layer A.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clone Job pod → controller | termination-message envelope is attacker-influenceable in principle (FallbackToLogsOnError substitutes log tail); controller parses defensively |
| controller → Project status | condition message becomes operator-visible (kubectl/dashboard) — injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03 | Tampering | malicious/garbled ref text landing in condition message (log/UI injection) | medium | mitigate | Ref reached the system only through the Pattern-validated field (no control chars/newlines); message built from a fixed template with the ref interpolated once |
| T-35-05 | Tampering | non-JSON termination message (log-tail fallback) mis-classifying a failure | medium | mitigate | readPushEnvelope contract: any parse failure → (zero, false) → generic delete-and-re-dispatch path (never the halt, never the stamp) |
| T-35-06 | DoS | perishable failure evidence (pod GC'd while manager down) causing one extra wasted clone | low | accept | Re-dispatch of a bad ref re-fails in seconds and re-writes the envelope; system converges back to the halt (RESEARCH Pitfall 3 — documented, not built around) |
| T-35-SC | Tampering | supply chain | low | accept | Zero new packages this phase |
</threat_model>

<verification>
- `go test ./internal/controller/ -count=1` and `make test` green in dev environment (MAKE_EXIT checked)
- Grep-level sanity: dispatch guard, WR-03 arm, and success arm each reference the new envelope fields/constants
</verification>

<success_criteria>
- BASE-02 complete: unresolvable baseRef → typed condition naming the ref, no retry loop (TTL-GC-proof), recovery via spec edit
- BASE-03 behavior half: baseSHA stamped on every successful clone in the CloneComplete patch; unpruned API-server round-trip asserted under the regenerated CRDs
</success_criteria>

<output>
Create `.planning/phases/35-git-base-ref/35-03-SUMMARY.md` when done. Record: the final condition Type/Reason identifiers, the cutoff constant value, and the envelope-mismatch (stale-spec) fall-through decision for the verifier.
</output>
