---
phase: 35-git-base-ref
plan: 04
type: execute
wave: 3
depends_on: [35-01, 35-02, 35-03]
files_modified:
  - test/integration/kind/medium_http_test.go
  - test/integration/kind/fixtures_test.go
  - docs/project-authoring.md
  - docs/INSTALL.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
must_haves:
  truths:
    - "End-to-end on kind: a Project with spec.git.baseRef set to a non-default branch reaches CloneComplete with status.git.baseSHA equal to that branch's tip — proving the whole chain (CRD → controller → clone Job → resolution → envelope → stamp) against a real cluster and real git-http remote"
    - "docs/project-authoring.md enumerates the accepted ref forms explicitly (branch, tag, full 40-hex SHA, refs/-qualified) and the rejected forms (HEAD, short SHAs, ~/^ suffixes) — the Argo Workflows qualified-ref surprise is pre-empted in writing"
    - "docs state the inert-after-clone semantics (edits after a successful clone change nothing; adopted Projects likewise) and the edit-spec recovery path for an unresolvable ref"
    - "docs/INSTALL.md tells operators to upgrade the tide-crds chart before the tide chart so new fields are not silently pruned (P8)"
  artifacts:
    - "kind Layer B spec: baseRef e2e case riding the medium git-http fixture (second branch seeded)"
    - "withBaseRef(ref string) projectOpt fixture option in test/integration/kind/fixtures_test.go"
    - "docs/project-authoring.md 'Basing a run on a branch, tag, or SHA' section"
    - "docs/INSTALL.md CRD-chart-first upgrade-order note"
  key_links:
    - "The kind suite installs the tide-crds chart — this e2e is the cluster half of BASE-03's chart upgrade-path lock"
    - "Docs accepted-forms table IS the D-01 contract's public surface — it must match pkg/git behavior exactly"
---

<objective>
Close the phase with the end-to-end proof and the documented contract: a kind Layer B spec exercising baseRef against a real git-http remote (BASE-01 e2e + BASE-03 cluster-half), and operator docs for the accepted forms, failure/recovery semantics, and CRD-chart-first upgrade order (D-01/D-02/D-03 surface, D-07 recovery, D-09/D-10 inertness, P8).

Purpose: envtest fabricates envelopes; only Layer B proves the real clone Job resolves a real non-default branch through the real ref layout (the Pitfall 1 production shape) and hands the SHA back through a real termination message.
Output: one focused e2e spec + the two doc updates.

Scope guard: the e2e covers the HAPPY path only — halt/release mechanics are fully locked at Layer A (plan 35-03) and a kind-level unresolvable case would double suite runtime for no new coverage. Halting auth-failed/network-timeout clone reasons remains an explicit non-goal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-git-base-ref/35-CONTEXT.md
@.planning/phases/35-git-base-ref/35-RESEARCH.md
@.planning/phases/35-git-base-ref/35-PATTERNS.md
</context>

## Artifacts this phase produces (phase-wide index)

See 35-01-PLAN.md for the full table. This plan delivers: the baseRef kind e2e spec, `withBaseRef(ref string) projectOpt`, the docs/project-authoring.md accepted-forms section, and the docs/INSTALL.md upgrade-order note.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kind Layer B e2e — run based on a non-default branch, baseSHA stamped</name>
  <files>test/integration/kind/medium_http_test.go, test/integration/kind/fixtures_test.go</files>
  <read_first>
    - test/integration/kind/medium_http_test.go — the git-http fixture: mediumDemoRemoteInitJobYAML (:88) seeds the remote repo via an init Job script (where the second branch gets added), mediumGitHTTPServerYAML (:123), and the Ordered Describe (:198) whose setup this spec rides
    - test/integration/kind/bare_project_test.go — the lightest Project shape that reaches clone completion (analog for a clone-stage-only assertion; prefer this over waiting for full Complete)
    - test/integration/kind/fixtures_test.go:57-108 — newStubProject + projectOpt pattern (withGit at :90 is the shape for the new withBaseRef option)
    - CLAUDE.md constrained-VM recipe + the `make test-int` exit-code protocol (MAKE_EXIT + grep for FAIL lines — Ginkgo summary alone is insufficient)
  </read_first>
  <behavior>
    - Fixture: the demo-remote init script additionally creates branch `base-ref-target` with one commit on top of main (so its tip differs from main's), and prints a greppable line `BASE_REF_TARGET_TIP=<40-hex>` (git rev-parse) to the init Job's log
    - Spec: a Project (minimal/bare shape) with spec.git.baseRef "base-ref-target" against the http:// remote reaches status.git.cloneComplete true; status.git.baseSHA equals the BASE_REF_TARGET_TIP read from the init Job pod's logs; baseSHA differs from main's tip (sanity: the run really based off the non-default branch)
  </behavior>
  <action>
    Add `withBaseRef(ref string) projectOpt` to fixtures_test.go (sets Spec.Git.BaseRef; nil-safe like withGit). Extend the init Job script inside mediumDemoRemoteInitJobYAML: after the existing main-branch seeding, create `base-ref-target` from main, add one distinguishing commit, print the rev-parse line, and leave HEAD/default branch pointing at main so all existing specs see unchanged behavior. Add one new It (or a small Ordered sibling within the http Describe, after the git-http server Available gate) that creates the baseRef Project via createFixture, Eventually-asserts cloneComplete + baseSHA equality against the tip harvested from the init pod logs (client-go pod-log read or kubectl-style helper already used by the suite — reuse whatever idiom exists; if none, read the pod log via the controller-runtime clientset the suite already constructs). Keep the spec clone-stage-scoped — do not wait for Complete.
    Run under the constrained-VM recipe: fresh kind cluster, one heavy run, never concurrent with an acceptance cluster.
  </action>
  <verify>
    <automated>set -o pipefail; make test-int 2>&1 | tee /tmp/35-04-test-int.log; echo MAKE_EXIT=$?; grep -nE '^--- FAIL|^FAIL ' /tmp/35-04-test-int.log || true</automated>
  </verify>
  <acceptance_criteria>
    - MAKE_EXIT=0 AND zero `--- FAIL` lines in the log (both checks — a red plain go-test can hide behind a green Ginkgo summary per CLAUDE.md)
    - The new spec's assertions include exact string equality between status.git.baseSHA and the harvested 40-hex tip (not just non-empty)
    - All pre-existing medium-http specs still pass (default branch behavior unchanged by the fixture extension)
    - The helm-render test from plan 35-01 and this e2e together close BASE-03's "survives a tide-crds chart upgrade without silent pruning" criterion (the kind suite installs the chart CRDs)
  </acceptance_criteria>
  <done>The full production chain — chart-installed CRDs, controller plumb-through, real clone Job, refs/remotes/origin resolution, termination-message envelope, status stamp — is proven on a real cluster for the phase's motivating use case (base off an unmerged branch).</done>
</task>

<task type="auto">
  <name>Task 2: Operator docs — accepted forms, failure/recovery, inertness, upgrade order</name>
  <files>docs/project-authoring.md, docs/INSTALL.md</files>
  <read_first>
    - docs/project-authoring.md — existing structure and voice (declarative, em-dash-heavy per CLAUDE.md; match it, don't revert to hedged prose)
    - docs/INSTALL.md — where upgrade guidance lives today (place the CRD-ordering note where an upgrading operator will actually see it)
    - .planning/phases/35-git-base-ref/35-CONTEXT.md &lt;specifics&gt; — the Argo Workflows qualified-ref rejection is the named surprise these docs must pre-empt
  </read_first>
  <action>
    docs/project-authoring.md: add a section titled "Basing a run on a branch, tag, or SHA" documenting spec.git.baseRef. Content, in this order: (1) an accepted-forms table with exactly four rows — branch name (e.g. release/1.2), tag name (annotated tags resolve to the peeled commit), full 40-hex commit SHA (must be reachable from a branch or tag — D-03's documented limit), and fully qualified refs/... path (the explicit disambiguation escape hatch for branch/tag name collisions — D-02); (2) an explicit rejected-forms line: HEAD, short SHAs, and ~/^ suffix expressions are not accepted — exactly three short forms plus the qualified escape hatch (D-01); (3) absent field means the remote default branch — there is no other way to say "use HEAD" (P10); (4) failure semantics: an unresolvable value halts the clone with condition CloneFailed/BaseRefUnresolvable naming the ref, and the fix is editing spec.git.baseRef — the corrected spec re-runs the clone automatically; the same bad ref is never hot-looped (D-06/D-07); (5) inertness: baseRef is read once, when the run branch is created — edits after a successful clone change nothing, and adopted/imported Projects (whose run branch already exists) are likewise unaffected (D-09/D-10); (6) provenance: status.git.baseSHA records the commit the run branch was created from on every run, including default-HEAD runs (D-11).
    docs/INSTALL.md: add an upgrade-order note — when upgrading, apply/upgrade the tide-crds chart BEFORE the tide chart; a stale CRD schema silently prunes newer spec fields (baseRef among them) with no error, and the run quietly bases from HEAD (P8). Keep both edits in the repo's voice; no version numbers that would go stale (the chart version bump itself lands with Phase 36's batch).
  </action>
  <verify>
    <automated>grep -c 'baseRef' docs/project-authoring.md && grep -ci 'tide-crds' docs/INSTALL.md && grep -c 'BaseRefUnresolvable' docs/project-authoring.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/project-authoring.md contains: the four-row accepted-forms table; the strings `refs/`, `40-hex`, `BaseRefUnresolvable`, and `baseSHA`; an explicit statement that HEAD and short SHAs are rejected; the inert-after-clone statement covering adopted Projects
    - docs/INSTALL.md contains an ordering statement matching: tide-crds before tide on upgrade, with the silent-pruning rationale
    - No claims contradict pkg/git behavior (cross-check the rejected-forms list against plan 35-02's tests)
  </acceptance_criteria>
  <done>The D-01/D-02/D-03 contract, D-07 recovery, D-09/D-10 inertness, and P8 upgrade order are operator-discoverable — the docs half of every CONTEXT.md "documented only" decision is honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator behavior | wrong docs cause unsafe operator expectations (e.g. assuming short SHAs work, skipping CRD upgrade order) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-07 | Repudiation/Info | silent field pruning on stale CRDs (runs base from HEAD with no error) | high | mitigate | INSTALL.md CRD-chart-first note (this plan) + helm-render lock (35-01) + chart-installed-CRD e2e (this plan) |
| T-35-SC | Tampering | supply chain | low | accept | Zero new packages this phase |
</threat_model>

<verification>
- `make test-int` green per the MAKE_EXIT + FAIL-grep protocol
- Docs greps per Task 2 acceptance criteria
</verification>

<success_criteria>
- BASE-01 proven end-to-end on kind against a real remote (the motivating unmerged-branch use case)
- BASE-02/BASE-03 operator surfaces documented; BASE-03's chart upgrade-path criterion fully closed (render lock + chart-installed e2e)
</success_criteria>

<output>
Create `.planning/phases/35-git-base-ref/35-04-SUMMARY.md` when done. Record: e2e runtime impact on the kind suite, the fixture branch name, and where each doc section landed.
</output>
