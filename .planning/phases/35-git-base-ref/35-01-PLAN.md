---
phase: 35-git-base-ref
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha2/project_types.go
  - api/v1alpha1/project_types.go
  - api/v1alpha1/phase35_schema_test.go
  - config/crd/bases/tideproject.k8s_projects.yaml
  - charts/tide-crds/templates/project-crd.yaml
  - test/integration/kind/baseref_crd_render_test.go
autonomous: true
requirements: [BASE-01, BASE-03]
must_haves:
  truths:
    - "A Project manifest with spec.git.baseRef set to a branch/tag/SHA/refs-qualified string passes CRD schema validation; a value with a leading '-' or embedded space is rejected at admission"
    - "The generated Project CRD carries baseRef under spec.git and baseSHA under status.git in BOTH the v1alpha1 and v1alpha2 version blocks — in config/crd/bases AND in the rendered tide-crds chart"
    - "JSON round-trip v1alpha1⇄v1alpha2 preserves BaseRef and BaseSHA in both directions (strategy-None conversion equivalent)"
    - "Absent baseRef stays absent — the CRD schema carries no default for it (one encoding for HEAD)"
  artifacts:
    - "api/v1alpha2/project_types.go — GitConfig.BaseRef + GitStatus.BaseSHA with validation markers"
    - "api/v1alpha1/project_types.go — identical BaseRef/BaseSHA twins"
    - "api/v1alpha1/phase35_schema_test.go — source parity + CRD-YAML parity + JSON round-trip tests"
    - "test/integration/kind/baseref_crd_render_test.go — helm-render both-versions lock (P8)"
    - "config/crd/bases/tideproject.k8s_projects.yaml + charts/tide-crds/templates/project-crd.yaml — regenerated"
  key_links:
    - "Go struct markers → controller-gen (make manifests) → config/crd/bases → helmify (make helm-crds) → charts/tide-crds"
    - "Both-versions parity is what makes the strategy-None apiVersion rewrite lossless (no conversion webhook exists)"
---

<objective>
Land the milestone's first CRD schema change: `spec.git.baseRef` (operator-selectable base ref, D-01 contract surface) and `status.git.baseSHA` (D-11 provenance stamp) in BOTH API versions, with regenerated CRD artifacts and the parity/round-trip/chart-render test locks that BASE-03 requires.

Purpose: BASE-01 needs the field to exist and validate; BASE-03 needs it to exist in both versions, survive conversion round-trip, and survive a `tide-crds` chart upgrade without silent pruning (PITFALLS P8/P9/P10).
Output: API types + generated CRDs + three test layers (source parity, JSON round-trip, helm render).

Scope guard: NO `tide-crds` or `tide` `Chart.yaml` version bump in this phase — the bump batches with Phase 36's agent-identity fields per the FIXED-contract rule (RESEARCH A2; verifier: do not flag the unbumped chart). No CEL immutability rule (D-08). No defaulting marker on baseRef (P10 — absent is the only HEAD encoding, per STATE.md binding constraint).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-git-base-ref/35-CONTEXT.md
@.planning/phases/35-git-base-ref/35-RESEARCH.md
@.planning/phases/35-git-base-ref/35-PATTERNS.md
</context>

## Artifacts this phase produces (phase-wide index)

| Artifact | Kind | Delivered by |
|---|---|---|
| `GitConfig.BaseRef` (`spec.git.baseRef`) — both API versions | CRD spec field | **this plan** |
| `GitStatus.BaseSHA` (`status.git.baseSHA`) — both API versions | CRD status field | **this plan** |
| Regenerated `config/crd/bases/tideproject.k8s_projects.yaml` + `charts/tide-crds/templates/project-crd.yaml` | generated CRD YAML | **this plan** |
| `api/v1alpha1/phase35_schema_test.go`, `test/integration/kind/baseref_crd_render_test.go` | tests | **this plan** |
| `resolveBaseRef(repo, ref)` helper + `ErrBaseRefUnresolvable` sentinel in `pkg/git` | function/error | plan 35-02 |
| `EnsureRunBranch(bareRepoPath, branch, baseRef string) (plumbing.Hash, error)` — changed signature | function | plan 35-02 |
| `pushConfig.BaseRef` + `--base-ref` CLI flag on tide-push | CLI flag | plan 35-02 |
| `pushResult.BaseSHA`/`pushResult.BaseRef` envelope fields; `envelopeKindClone = "CloneResult"`; `writeCloneEnvelope` | envelope contract | plan 35-02 |
| envelope reason string `baseref-unresolvable` (exit 2) | taxonomy | plan 35-02 |
| `CloneOptions.BaseRef`; `--base-ref` + `--project-uid` clone-Job args; clone container termination-message wiring | controller helper | plan 35-03 |
| `ReasonBaseRefUnresolvable = "BaseRefUnresolvable"` in `api/v1alpha2/shared_types.go` | constant | plan 35-03 |
| `pushResultEnvelope.BaseSHA`/`.BaseRef` controller-side fields | struct fields | plan 35-03 |
| Generation-scoped dispatch-halt gate + WR-03 classification + read-before-flip baseSHA stamp | reconciler logic | plan 35-03 |
| `internal/controller/project_baseref_halt_test.go` envtest specs | tests | plan 35-03 |
| `withBaseRef(ref string) projectOpt` kind fixture option + baseRef e2e spec | tests | plan 35-04 |
| `docs/project-authoring.md` accepted-forms section; `docs/INSTALL.md` CRD-chart-first upgrade note | docs | plan 35-04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BaseRef/BaseSHA to both API versions and regenerate CRD artifacts</name>
  <files>api/v1alpha2/project_types.go, api/v1alpha1/project_types.go, config/crd/bases/tideproject.k8s_projects.yaml, charts/tide-crds/templates/project-crd.yaml</files>
  <read_first>
    - api/v1alpha2/project_types.go:205-260 — GitConfig (RepoURL Pattern-marker style at :206-212) and GitStatus (CloneComplete optional-field style at :251-255); BaseRef lands after LeaksConfigRef, BaseSHA after CloneComplete
    - api/v1alpha1/project_types.go:205-251 — the v1alpha1 twins. NOTE (verified): v1alpha1 GitStatus has NO CloneComplete field — do NOT add one; BaseSHA lands after LeaseFailureCount there
    - .planning/phases/35-git-base-ref/35-RESEARCH.md §Code Examples — the exact field comments + marker block to use (verbatim source of truth for this task)
    - Makefile:52 (manifests), Makefile:717 (helm-crds)
  </read_first>
  <behavior>
    - Test 1 (existing suites): `go test ./api/...` stays green after the struct change
    - Test 2 (generated output): both regenerated YAML artifacts carry the new properties in both version blocks (asserted properly in Task 2; smoke-grep here)
  </behavior>
  <action>
    Implement per D-01/D-08/D-09/D-11 and P9/P10. In api/v1alpha2/project_types.go add to GitConfig (after LeaksConfigRef) the field `BaseRef string` with json tag `baseRef,omitempty`, doc comment stating: names the ref the run branch is created from — an existing branch name, tag name, full 40-hex commit SHA, or a fully qualified refs/... path; absent means the remote default branch (HEAD); resolution happens in the clone Job and an unresolvable value surfaces as CloneFailed/BaseRefUnresolvable; edits after a successful clone are inert — the run branch is created once (see docs/project-authoring.md) [D-09]. Markers: `+kubebuilder:validation:MinLength=1`, `+kubebuilder:validation:MaxLength=250`, `+kubebuilder:validation:Pattern=` with pattern `^[A-Za-z0-9][A-Za-z0-9._+@/-]*$` (backtick-quoted like the RepoURL marker at :211), and `+optional`. The pattern's first-char class rejects leading `-` (argument-injection shape, T-35-01), leading `.` and `/`; the charset excludes spaces, control chars, and git-forbidden metacharacters while admitting `refs/`-qualified values and 40-hex SHAs. Per P10 and the STATE.md binding constraint, the marker block must contain NO defaulting marker of any kind — absence is the only HEAD encoding. Per D-08, NO transition/immutability rule (no XValidation with oldSelf).
    Add to GitStatus (after CloneComplete) the field `BaseSHA string` with json tag `baseSHA,omitempty`, `+optional`, doc comment: the commit SHA the run branch was created from, stamped on every run (annotated tags record the peeled commit) — provenance only [D-11].
    In api/v1alpha1/project_types.go add the identical twins: BaseRef after LeaksConfigRef in GitConfig, BaseSHA after LeaseFailureCount in GitStatus (same comments, same markers — P9 both-versions rule). Do NOT add CloneComplete to v1alpha1.
    Regenerate in the dev environment (go absent from host PATH — RESEARCH A4): run `make manifests generate` then `make helm-crds`. Never hand-edit config/crd/bases/ or charts/tide-crds/templates/. Do NOT touch charts/tide-crds/Chart.yaml or charts/tide/Chart.yaml versions (bump batches with Phase 36).
  </action>
  <verify>
    <automated>go build ./api/... && grep -c '^ *baseRef:' config/crd/bases/tideproject.k8s_projects.yaml && grep -c '^ *baseSHA:' config/crd/bases/tideproject.k8s_projects.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^ *baseRef:' config/crd/bases/tideproject.k8s_projects.yaml` returns exactly 2 (one per version block: v1alpha1 at name block line ~27, v1alpha2 at ~562)
    - `grep -c '^ *baseSHA:' config/crd/bases/tideproject.k8s_projects.yaml` returns exactly 2
    - `grep -c '^ *baseRef:' charts/tide-crds/templates/project-crd.yaml` returns exactly 2 and same for `baseSHA:`
    - The generated v1alpha2 baseRef property block contains `pattern:` with the exact charset regex and `maxLength: 250`
    - `sed -n '/BaseRef string/{=;}' api/v1alpha2/project_types.go` and the v1alpha1 file each match exactly once; both files also declare `BaseSHA string` exactly once
    - The BaseRef marker region (comment block through the field line) in both files contains `+optional` and the region contains no `default` marker line and no `XValidation` line
    - `git diff --name-only` includes the two generated YAML files and does NOT include charts/tide-crds/Chart.yaml or charts/tide/values.yaml
    - `go test ./api/...` exits 0 (run in dev env)
  </acceptance_criteria>
  <done>Both API versions declare BaseRef (validated, optional, no default) and BaseSHA; both generated CRD artifacts carry both fields under both version blocks; chart version untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parity, round-trip, and chart-render test locks</name>
  <files>api/v1alpha1/phase35_schema_test.go, test/integration/kind/baseref_crd_render_test.go</files>
  <read_first>
    - api/v1alpha1/phase3_schema_test.go — the repo's established static-parity convention: regex-over-source tests (:110 TestGitConfigTypeDeclared, :136/:145 spec/status field presence), generated-CRD-YAML tests (:207 TestProjectCRDSchemaHasGitSpec, :217 ...HasGitStatus, :230 ...HasRepoURLPattern), and cross-version JSON round-trip tests (:285 TestGitStatusRoundTrip, :304 TestGitConfigRoundTrip) — copy these shapes exactly
    - test/integration/kind/projects_pvc_test.go:140-203 — plain go-test helm-render style (TestHelmDeploymentTemplateRendersManagerPodAnnotations et al.): how the suite shells out to `helm template` and asserts on rendered output without a cluster
  </read_first>
  <behavior>
    - Test 1: source parity — both api/v1alpha1/project_types.go and api/v1alpha2/project_types.go declare `BaseRef string` in GitConfig and `BaseSHA string` in GitStatus (regex over source files, phase3 pattern)
    - Test 2: CRD YAML parity — config/crd/bases/tideproject.k8s_projects.yaml contains `baseRef:` under spec.git properties and `baseSHA:` under status.git properties in BOTH version blocks; the baseRef block carries the Pattern regex (analog :230)
    - Test 3: JSON round-trip — a v1alpha1.Project with Spec.Git.BaseRef="release/1.2" and Status.Git.BaseSHA set marshals to JSON and unmarshals into v1alpha2.Project with both values intact, and the reverse direction also preserves them (this is literally what strategy-None conversion does at the API server)
    - Test 4: helm render — `helm template charts/tide-crds` output for the Project CRD contains `baseRef:` and `baseSHA:` under BOTH the v1alpha1 and v1alpha2 schema blocks (P8 chart-skew lock, the BASE-03 upgrade-path test's render half)
  </behavior>
  <action>
    Create api/v1alpha1/phase35_schema_test.go following the phase3_schema_test.go conventions (same package, same file-reading helpers if exported/copyable). Name tests TestBaseRefFieldDeclaredBothVersions, TestBaseSHAFieldDeclaredBothVersions, TestProjectCRDSchemaHasBaseRefBothVersions, TestProjectCRDSchemaHasBaseRefPattern, TestBaseRefBaseSHARoundTrip (both directions in one test or two). The CRD-YAML tests must assert per-version-block presence (split the YAML on the version `name:` markers or count occurrences == 2, matching how phase3 tests locate blocks).
    Create test/integration/kind/baseref_crd_render_test.go with TestHelmTideCRDsRenderBaseRefBothVersions: render charts/tide-crds via the same helm-template invocation style as projects_pvc_test.go:140, extract the Project CRD document, assert `baseRef:` and `baseSHA:` each appear exactly twice (one per served/unserved version block). Plain go-test — must run without a kind cluster.
  </action>
  <verify>
    <automated>go test ./api/... -run 'BaseRef|BaseSHA|Phase35' -count=1 && go test ./test/integration/kind/ -run TestHelmTideCRDsRenderBaseRefBothVersions -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All new api tests pass; deliberately breaking one side (thought experiment, not committed) would fail: removing the v1alpha1 BaseRef line makes TestBaseRefFieldDeclaredBothVersions fail — the omission is loud (P9 goal)
    - TestHelmTideCRDsRenderBaseRefBothVersions passes with no cluster running
    - Round-trip test asserts BOTH directions (v1alpha1→v1alpha2 and v1alpha2→v1alpha1)
    - `go test ./api/... -count=1` full package green; `go vet ./api/... ./test/integration/kind/` clean
  </acceptance_criteria>
  <done>Schema drift in either version, either generated artifact, or the rendered chart now fails a fast test — BASE-03's parity/round-trip/render locks are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → API server | untrusted `spec.git.baseRef` string enters the system here; schema validation is the first (charset-only) filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Tampering/Elevation | baseRef field (argument-injection-shaped refs, leading `-`) | high | mitigate | Pattern marker `^[A-Za-z0-9][A-Za-z0-9._+@/-]*$` rejects leading `-`/`.`/`/`, spaces, shell metachars, control chars (this plan); defense-in-depth — the value never transits a shell (plan 35-02/03) |
| T-35-04 | DoS | CRD validation cost (pathological regex input) | low | mitigate | MaxLength=250 bounds regex evaluation; no CEL rule added |
| T-35-SC | Tampering | supply chain (package installs) | low | accept | No npm/pip/cargo/Go-module installs this phase — RESEARCH Package Legitimacy Audit: zero new packages |

Ref-existence validation at admission is intentionally NOT attempted (CEL cannot see the remote — P10); existence failures are classified reconcile-side (plan 35-03).
</threat_model>

<verification>
- `make test` green in dev environment (unit tier includes ./api/...)
- `grep -c '^ *baseRef:'` == 2 in both generated artifacts; same for baseSHA
- No Chart.yaml or values.yaml diffs
</verification>

<success_criteria>
- BASE-01 (field surface): spec.git.baseRef exists, optional, charset-validated, no default
- BASE-03 (schema half): both versions, round-trip test, chart-render lock all green; the API-server-apply half of the upgrade-path test lands with plan 35-03's envtest specs
</success_criteria>

<output>
Create `.planning/phases/35-git-base-ref/35-01-SUMMARY.md` when done. Record: exact Pattern regex shipped, generated-file diff summary, and confirmation that no chart version was bumped (Phase 36 batches it).
</output>
