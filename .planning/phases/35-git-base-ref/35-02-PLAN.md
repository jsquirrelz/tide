---
phase: 35-git-base-ref
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/git/branch.go
  - pkg/git/branch_test.go
  - pkg/git/clone_test.go
  - pkg/git/integrate.go
  - pkg/git/integrate_test.go
  - cmd/tide-push/main.go
  - cmd/tide-push/main_test.go
autonomous: true
requirements: [BASE-01, BASE-02]
must_haves:
  truths:
    - "EnsureRunBranch with baseRef set to a NON-DEFAULT branch name creates the run branch at that branch's tip in a production-shaped bare clone (where non-default branches live only under refs/remotes/origin/* — Pitfall 1, the load-bearing correction)"
    - "The chain resolves exactly: refs/-qualified verbatim (with refs/heads/→refs/remotes/origin fallback), branch, tag (annotated tags peeled to the commit), full 40-hex SHA — and rejects HEAD, short SHAs, and ~/^ suffixes (D-01/D-02)"
    - "An unresolvable ref makes tide-push clone mode exit 2 and write an envelope with reason baseref-unresolvable to /dev/termination-log; a resolvable run writes a success envelope carrying the resolved baseSHA — including default-HEAD runs with no baseRef (D-05/D-11)"
    - "A retried clone Job whose run branch already exists exits 0 without re-resolving (idempotency early-return stays FIRST — Pitfall 6, what makes D-09/D-10 free)"
  artifacts:
    - "pkg/git/branch.go — resolveBaseRef helper, ErrBaseRefUnresolvable sentinel, EnsureRunBranch(bareRepoPath, branch, baseRef string) (plumbing.Hash, error)"
    - "cmd/tide-push/main.go — pushConfig.BaseRef, --base-ref flag, writeCloneEnvelope, envelopeKindClone, baseSHA/baseRef envelope fields, reason baseref-unresolvable"
    - "New unit tests in pkg/git/branch_test.go and cmd/tide-push/main_test.go covering every chain arm and both envelope paths"
  key_links:
    - "EnsureRunBranch is the SINGLE resolution site (D-04) — runClone is its only production caller"
    - "runClone → writeCloneEnvelope → /dev/termination-log is the only Job→controller transport (plan 35-03 parses it)"
---

<objective>
Implement the D-01/D-02/D-03 resolution contract in `pkg/git` (the single resolution site, D-04) and adopt the push-mode envelope contract for clone mode in `cmd/tide-push` (D-05, D-11) — so an unresolvable baseRef becomes a classifiable exit-2 envelope and every successful clone reports the resolved base SHA.

Purpose: BASE-01's resolution behavior and BASE-02's fail-fast surface both live in this Job-side layer; plan 35-03's controller classification consumes the envelope contract defined here.
Output: extended `EnsureRunBranch` + `resolveBaseRef` chain with full unit coverage; clone-mode envelopes on every exit path.

Contract handed to plan 35-03 (fixed here, do not drift): envelope JSON keys `baseSHA` (success: resolved 40-hex SHA) and `baseRef` (the ref as given), `reason` value `baseref-unresolvable`, exit code 2 (exitInvariant — exit 14 is claimed by Phase 34's integration-incomplete), envelope Kind `CloneResult`, PVC copy at `<workspace>/envelopes/clone/<projectUID>.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-git-base-ref/35-CONTEXT.md
@.planning/phases/35-git-base-ref/35-RESEARCH.md
@.planning/phases/35-git-base-ref/35-PATTERNS.md
</context>

## Artifacts this phase produces (phase-wide index)

See 35-01-PLAN.md for the full table. This plan delivers: `resolveBaseRef`, `ErrBaseRefUnresolvable`, the changed `EnsureRunBranch(bareRepoPath, branch, baseRef string) (plumbing.Hash, error)` signature, `pushConfig.BaseRef`, the `--base-ref` flag, `pushResult.BaseSHA`/`pushResult.BaseRef` fields, `envelopeKindClone = "CloneResult"`, `writeCloneEnvelope`, and the `baseref-unresolvable` reason (exit 2).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveBaseRef chain + EnsureRunBranch signature in pkg/git</name>
  <files>pkg/git/branch.go, pkg/git/branch_test.go, pkg/git/clone_test.go, pkg/git/integrate.go, pkg/git/integrate_test.go, cmd/tide-push/main.go</files>
  <read_first>
    - pkg/git/branch.go:30-67 — current EnsureRunBranch: PlainOpen → existence early-return (:53-56) → repo.Head() (:58) → Storer.SetReference (:63). The early-return MUST stay first (Pitfall 6)
    - .planning/phases/35-git-base-ref/35-RESEARCH.md §Code Examples "Resolution chain skeleton" — the authoritative implementation shape, verified against go-git v5.19.0 tagged source; §Standard Stack table for the exact go-git API signatures (repo.Reference, TagObject, CommitObject, plumbing.IsHash, NewBranch/NewTag/NewRemoteReferenceName)
    - pkg/git/clone_test.go:40 seedBareRepo + :109 TestCloneSucceeds — fixture idiom; resolution tests MUST run against a repo produced by pkggit.Clone (production ref layout: non-default branches under refs/remotes/origin/*), NOT against the seeded source repo directly
    - pkg/git/branch_test.go:30,54,75 — existing test naming/structure to extend
    - pkg/git/integrate.go:39 — doc comment naming the EnsureRunBranch precondition (update wording for the new signature)
    - cmd/tide-push/main.go:296-301 — the one production call site (mechanical update this task; full wiring in Task 2)
  </read_first>
  <behavior>
    - RED first (fixture): extend seedBareRepo (or add a sibling helper) to also create a second branch (e.g. feature/hotfix, tip ≠ HEAD tip), an annotated tag (v1.0.0) and a lightweight tag (v1.0.0-lw) on distinct commits. Clone the seeded source via pkggit.Clone to get the production-shaped bare repo the tests resolve against.
    - Default HEAD: EnsureRunBranch(dest, "tide/run-x", "") creates the run branch at HEAD and returns HEAD's hash (existing behavior preserved, now with a hash return)
    - Non-default branch (Pitfall 1, load-bearing): baseRef "feature/hotfix" resolves via refs/remotes/origin/feature/hotfix; run branch tip == feature tip, returned hash == feature tip
    - Annotated tag: baseRef "v1.0.0" returns the PEELED commit hash (D-11), not the tag object hash
    - Lightweight tag: baseRef "v1.0.0-lw" returns its commit hash
    - Full 40-hex SHA of an existing commit: resolves; a syntactically valid 40-hex SHA of a nonexistent object: errors.Is(err, ErrBaseRefUnresolvable) and the message notes the branch-or-tag reachability requirement (D-03)
    - refs/-qualified verbatim (D-02): "refs/tags/v1.0.0" resolves; "refs/heads/feature/hotfix" resolves via the refs/remotes/origin fallback nuance
    - Rejections (D-01): "HEAD", a 7-char short SHA, "main~1", "main^" each return an error satisfying errors.Is(err, ErrBaseRefUnresolvable) with message containing `unable to resolve` and the ref
    - Idempotency ordering (Pitfall 6): pre-create the run branch, then call with a garbage baseRef → nil error, returns the EXISTING branch tip hash, no resolution attempted
  </behavior>
  <action>
    Change the signature to EnsureRunBranch(bareRepoPath, branch, baseRef string) (plumbing.Hash, error): the returned hash is what the run branch points at (existing tip on the idempotent early-return path; the resolved base on the create path). Keep the :53-56 existence early-return FIRST. When baseRef is empty, keep the repo.Head() arm and return head.Hash(). When baseRef is non-empty, call a new unexported helper resolveBaseRef(repo *gogit.Repository, ref string) (plumbing.Hash, error) implementing the RESEARCH skeleton exactly: (1) strings.HasPrefix "refs/" → repo.Reference verbatim, with a refs/heads/-prefixed retry through plumbing.NewRemoteReferenceName("origin", short) on miss; (2) branch arm — NewBranchReferenceName then NewRemoteReferenceName("origin", ref); (3) tag arm — NewTagReferenceName, then peel via repo.TagObject(hash) → tag.Commit() when it succeeds (annotated), keep the hash when TagObject misses (lightweight); (4) SHA arm — plumbing.IsHash(ref) gate then repo.CommitObject existence check with the D-03 reachability wording on miss; (5) after ANY successful arm, sanity-verify the final hash names a commit via repo.CommitObject (a refs/-verbatim ref could name a non-commit object). Do NOT use repo.ResolveRevision anywhere on this path — it accepts exactly the forms D-01 rejects.
    Declare `var ErrBaseRefUnresolvable = errors.New("baseref unresolvable")` in pkg/git; every unresolvable outcome wraps it via fmt.Errorf with %w and the message shape `unable to resolve %q to a commit SHA: baseRef must be an existing branch, tag, or full 40-hex commit SHA reachable from a branch or tag`.
    Create the run-branch ref with Storer.SetReference at the resolved hash (existing idiom at :63). Update the doc comments on EnsureRunBranch and the precondition note at pkg/git/integrate.go:39 for the new signature. Update ALL callers mechanically: pkg/git/branch_test.go (:35/:59/:65/:78), pkg/git/integrate_test.go:34, and cmd/tide-push/main.go:298 — at main.go:298 pass empty-string baseRef and discard the hash for now (`_, err :=` shape) so the tree stays green; Task 2 does the real wiring.
  </action>
  <verify>
    <automated>go test ./pkg/git/ -run TestEnsureRunBranch -count=1 -v && go build ./cmd/tide-push/</automated>
  </verify>
  <acceptance_criteria>
    - All behavior cases above have a named test and pass; the non-default-branch case FAILS if the refs/remotes/origin lookup is removed (verified during RED by writing it against the cloned repo, where refs/heads/feature/hotfix does not exist)
    - `errors.Is(err, git.ErrBaseRefUnresolvable)` is asserted in every rejection test (not string matching alone)
    - Error message for a nonexistent-object SHA contains the word `reachable`
    - `go test ./pkg/git/ -count=1` full package green; `go build ./...` succeeds (all callers updated)
    - pkg/git/branch.go contains exactly one exported EnsureRunBranch and its early-return existence check appears BEFORE any resolveBaseRef call in the function body
  </acceptance_criteria>
  <done>The D-01/D-02/D-03 contract is executable and unit-locked against a production-shaped bare clone; all callers compile; the idempotency guarantee that funds D-09/D-10 is regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clone-mode envelope + --base-ref flag in tide-push</name>
  <files>cmd/tide-push/main.go, cmd/tide-push/main_test.go</files>
  <read_first>
    - cmd/tide-push/main.go:36-46 (exit-code map header), :83-117 (pushConfig, pushResult, envelopeKind consts), :140-172 (flag wiring — mirror --run-branch), :255-326 (runClone — note the ":257 no envelope" comment goes stale), :655-703 (terminationMessagePath const + writePushEnvelope — the contract to replicate), :272 redactPAT usage
    - cmd/tide-push/main_test.go:437 TestRunCloneMode, :620 TestRunCloneModeNoRunBranchIsNoOp, :651 TestRunCloneProvisions, :46 seedBareRepo — the clone-mode test harness to extend
    - .planning/phases/35-git-base-ref/35-RESEARCH.md §Pattern 2 — the envelope contract incl. the 4096-byte termination-log cap
  </read_first>
  <behavior>
    - Success envelope, default HEAD (D-11): runClone with --run-branch and NO --base-ref writes JSON to the termination path override used in tests AND to <workspace>/envelopes/clone/<uid>.json with kind CloneResult, exitCode 0, reason empty, baseSHA == source HEAD hash
    - Success envelope, feature branch: --base-ref=feature/hotfix → run branch tip == feature tip AND envelope baseSHA == feature tip, baseRef field == "feature/hotfix"
    - Failure envelope (BASE-02): --base-ref=no-such-ref → runClone returns 2 (exitInvariant), envelope reason == "baseref-unresolvable", baseRef == "no-such-ref", baseSHA empty
    - No run branch: clone without --run-branch still exits 0 and writes a success envelope with empty baseSHA (legacy path; controller stamps empty — documented, not special-cased)
    - Existing clone-mode tests stay green with signature-level updates only
  </behavior>
  <action>
    Add `BaseRef string` to pushConfig and a `--base-ref` flag (help text: clone-mode — branch, tag, full 40-hex SHA, or refs/-qualified ref the run branch is created from; empty means remote HEAD). Extend pushResult with `BaseSHA string` json `baseSHA,omitempty` and `BaseRef string` json `baseRef,omitempty` (one struct for both modes — RESEARCH Open Q2 resolution). Add const `envelopeKindClone = "CloneResult"` beside envelopeKind.
    Add writeCloneEnvelope(cfg pushConfig, baseSHA string, exit int, reason string): marshal a pushResult with Kind envelopeKindClone, ProjectUID cfg.ProjectUID, BaseRef cfg.BaseRef, BaseSHA baseSHA, ExitCode exit, Reason reason; write best-effort to terminationMessagePath and, when ProjectUID is non-empty, to <workspace>/envelopes/clone/<uid>.json (mirror writePushEnvelope's MkdirAll + WriteFile idiom; the clone path is `envelopes/clone/` — deliberately distinct from `envelopes/push/` so a later boundary push never overwrites clone provenance). If writePushEnvelope's terminationMessagePath is a package const the tests cannot redirect, follow whatever seam main_test.go already uses for push-envelope assertions; add a minimal test seam only if none exists.
    Wire runClone per D-05/D-11: capture the hash returned by pkggit.EnsureRunBranch(destDir, cfg.RunBranch, cfg.BaseRef); on errors.Is(err, pkggit.ErrBaseRefUnresolvable) → writeCloneEnvelope(cfg, "", exitInvariant, "baseref-unresolvable") and return exitInvariant; on other EnsureRunBranch/worktree/config failures → writeCloneEnvelope(cfg, "", <existing exit code>, "clone-failed") keeping current exit codes; on the initial pkggit.Clone failure → writeCloneEnvelope(cfg, "", classifyGitError(err), "clone-failed") then return that code; on success (with or without RunBranch) → writeCloneEnvelope(cfg, resolvedHashStringOrEmpty, exitSuccess, "") before returning exitSuccess. All stderr messages on the new paths pass through redactPAT (existing :272 idiom) — resolution errors themselves contain only the ref and hash, never URL or PAT (T-35-02).
    Update the runClone doc comment (the "No envelope is written" sentence is now false) and extend the file-header exit-code map comment with the clone-mode reason line for exit 2 (baseref-unresolvable rides the invariant code; controller classifies on reason, not exit code — exit 14 belongs to Phase 34).
  </action>
  <verify>
    <automated>go test ./cmd/tide-push/ -run TestRunClone -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - New tests TestRunCloneWritesSuccessEnvelope (or equivalent names) assert the exact JSON keys `kind`, `reason`, `baseSHA`, `baseRef`, `exitCode` — key spelling locked to this plan's objective contract
    - Unresolvable case: return value == 2 AND envelope reason == "baseref-unresolvable"
    - Success case with annotated-tag baseRef is covered in pkg/git (Task 1); here at least default-HEAD + feature-branch + unresolvable + no-run-branch cases exist
    - PVC envelope lands at envelopes/clone/<uid>.json when --project-uid is set, and is skipped (no error) when unset
    - `go test ./cmd/tide-push/ -count=1` full package green
    - runClone's doc comment no longer claims clone mode writes no envelope
  </acceptance_criteria>
  <done>Clone mode speaks the push envelope contract: every exit path emits a classifiable envelope; success always carries the resolved base SHA (D-11), failure carries reason baseref-unresolvable at exit 2 (D-05) — ready for plan 35-03's controller to parse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → clone Job args | operator's baseRef crosses as an exec-array Job arg (never a shell) |
| clone Job → git remote | PAT-authenticated fetch; new error paths must not leak the PAT |
| clone Job → controller | termination-log envelope; controller must never trust it blindly (parsed defensively, plan 35-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Tampering/Elevation | baseRef reaching process execution | high | mitigate | Value only reaches exec-array Job args and pure go-git lookups — no git CLI receives it on the new path (verified: runClone's CLI calls take fixed args + cfg.RunBranch); Pattern marker (plan 35-01) is the admission filter |
| T-35-02 | Information disclosure | PAT leakage via new clone error paths | high | mitigate | redactPAT on all new stderr writes; resolveBaseRef errors contain only ref + hash, never URL/PAT; envelope carries no credential fields |
| T-35-05 | Tampering | envelope spoofing via log fallback (FallbackToLogsOnError yields non-JSON) | medium | mitigate | Envelope is written last on each path; controller-side parse failure → generic path (existing readPushEnvelope contract, Pitfall 5) |
| T-35-SC | Tampering | supply chain | low | accept | Zero new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test ./pkg/git/... ./cmd/tide-push/... -count=1` green in dev environment
- Chain behavior table from 35-RESEARCH §Phase Requirements → Test Map rows BASE-01/BASE-02 (unit rows) fully covered by named tests
</verification>

<success_criteria>
- BASE-01 (resolution): all four accepted forms resolve, all rejected forms fail typed, non-default branches work against production ref layout
- BASE-02 (Job half): unresolvable ref → exit 2 + reason baseref-unresolvable envelope before any subagent spend (D-04 — failure fires in the clone Job)
</success_criteria>

<output>
Create `.planning/phases/35-git-base-ref/35-02-SUMMARY.md` when done. Record: the final envelope JSON contract (keys + example), the exit-code/reason assignment, and the EnsureRunBranch signature for plan 35-03's consumption.
</output>
