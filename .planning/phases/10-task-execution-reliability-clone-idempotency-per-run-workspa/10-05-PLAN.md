---
phase: 10-task-execution-reliability-clone-idempotency-per-run-workspa
plan: 05
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
  - 10-02-PLAN.md
  - 10-03-PLAN.md
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All descendants of the medium-project reach status.phase=Succeeded"
    - "A tide/run-* branch is pushed to the in-cluster http:// remote with real authored code"
    - "Project status shows costSpentCents > 0 under cap"
    - "The executor pod logs show a non-empty branch (EnsureWorktree with tide/run-<project>-<unix>)"
    - "make test exits 0 before the live run"
  artifacts:
    - path: ".planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-VERIFICATION.md"
      provides: "gate_decision: APPROVED or BLOCKED with evidence"
  key_links:
    - from: "medium-project"
      to: "in-cluster http:// git remote"
      via: "tide-push Job pushing tide/run-* branch after all Tasks Succeeded"
      pattern: "tide/run-"
---

<objective>
Medium DoD re-run (SC-5, SC-6, SC-3 confirmation): Run the `examples/projects/medium` sample on the primed minikube cluster with real Claude (Haiku) after Plans 10-01/02/03 land. Verify all descendants reach Succeeded, a `tide/run-*` branch is pushed to the in-cluster `http://` remote with real authored code, `costSpentCents > 0`, and the executor worktree branch is non-empty (SC-3 confirmation).

Purpose: This is the v1.0.0-unblocking gate. Phase 9's cross-namespace orchestration mechanism is proven; this run validates the four task-execution reliability fixes under a real workload. Phase 8 SC-2 (real-Claude leg) flips DEFERRED→PASS. The v1.0.0 retag is unblocked but stays user-gated (confirm-only, NOT automated here).

Output: `10-VERIFICATION.md` with `gate_decision: APPROVED` (or `BLOCKED` with the next cascade evidence). The retag is NOT performed in this plan — user action only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-CONTEXT.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-RESEARCH.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-01-SUMMARY.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-02-SUMMARY.md
@.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and load Phase-10 images; run make test</name>
  <files></files>
  <read_first>
    - Makefile — docker-build targets for controller + tide-push + claude-subagent + tide-reporter; kind load targets; make test target
    - .planning/STATE.md — parked minikube state: fresh Phase-9 images loaded, TIDE_REPORTER_IMAGE set, reporter RBAC fixed
  </read_first>
  <action>
    1. Run `make test` (full unit suite). If it exits non-zero, stop and surface the failure — do not proceed to the live run with a red test suite.

    2. Rebuild images incorporating Phase-10 fixes:
       a. `make docker-build` (or the specific targets for: `tide-controller-manager`, `tide-push`, `claude-subagent`, `tide-reporter`) — all four binaries changed in Plans 10-01/02/03.
       b. Load into minikube: `make docker-load` or equivalent (`minikube image load` for each image).
       c. Restart the controller-manager deployment to pick up the new image digest: `kubectl rollout restart deployment/tide-controller-manager -n tide-system`.
       d. Wait for the rollout: `kubectl rollout status deployment/tide-controller-manager -n tide-system --timeout=120s`.

    3. Confirm minikube cluster is healthy: `kubectl get nodes` shows Ready; `kubectl get pods -n tide-system` shows controller + dashboard Running.

    4. Confirm git-http-server is running in tide-sample-medium: `kubectl get pods -n tide-sample-medium` shows the git-http-server pod Running (from Phase 8). If absent, apply the medium sample manifests: `kubectl apply -f examples/projects/medium/`.

    5. Confirm ANTHROPIC_API_KEY secret exists in tide-sample-medium: `kubectl get secret anthropic-creds -n tide-sample-medium` — if absent, create it per the medium sample README.
  </action>
  <verify>
    <automated>make test 2>&1 | tail -5</automated>
  </verify>
  <done>make test exits 0. New images loaded into minikube (kubectl describe pod in tide-system shows the new image digest). Controller running. Git-http-server running. API key secret present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase-10 fixes built, images loaded, cluster primed. Ready to start the medium DoD run.</what-built>
  <how-to-verify>
    Before starting:
    1. Delete any leftover medium-project CRD from a prior run: `kubectl delete project medium-project -n tide-sample-medium --ignore-not-found`.
    2. If the tide-projects PVC has stale state, do NOT manually wipe it — the clone idempotency fix (Plan 10-01) means a warm PVC is fine. Let the run proceed normally.
    3. Apply the medium sample project: `kubectl apply -f examples/projects/medium/project.yaml`.
    4. Watch the tree: `kubectl get project,milestone,phase,plan,task -n tide-sample-medium -w` (or `tide watch medium-project -n tide-sample-medium` if tide CLI is available).
    5. Monitor Jobs: `kubectl get jobs -n tide-sample-medium -w`.
    6. Check executor logs when Task Jobs appear: `kubectl logs job/tide-task-<uid>-1 -n tide-sample-medium -c claude-subagent 2>&1 | grep -i "branch\|worktree\|EnsureWorktree"` — confirm branch is non-empty (SC-3).
    7. Wait for Project status.phase=Complete (expected ~10-20 min with real Claude Haiku).

    DoD assertions (all must be TRUE to approve):
    a. `kubectl get project medium-project -n tide-sample-medium -o jsonpath='{.status.phase}'` == "Complete"
    b. All tasks Succeeded: `kubectl get task -n tide-sample-medium -o jsonpath='{.items[*].status.phase}'` shows no "Failed"
    c. Branch pushed: `kubectl exec -n tide-sample-medium deploy/git-http-server -- git ls-remote /repo.git 2>/dev/null | grep 'tide/run-'` returns at least one line
    d. Cost > 0: `kubectl get project medium-project -n tide-sample-medium -o jsonpath='{.status.budget.costSpentCents}'` > 0
    e. Executor branch non-empty: executor pod logs show `EnsureWorktree` with a non-empty branch (from step 6 above)

    If ANY assertion fails, record the failure with the exact error string and signal "blocked: <failure description>" below.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 DoD assertions pass.
    Type "blocked: [description]" if any assertion fails — include the exact error/output.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record gate artifact (10-VERIFICATION.md)</name>
  <files>.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-VERIFICATION.md — shape reference for the gate artifact format
  </read_first>
  <action>
    Create 10-VERIFICATION.md with:
    - Frontmatter: `gate_decision: APPROVED` (or `BLOCKED` if the checkpoint above reported failure)
    - Evidence section: exact kubectl output from each DoD assertion (a-e from the checkpoint task)
    - SC-3 confirmation: executor logs line showing non-empty branch
    - Phase 8 SC-2 status: DEFERRED → PASS (or remains DEFERRED if blocked)
    - v1.0.0 retag status: UNBLOCKED — user action required (delete + recreate tag at HEAD, then push — per MEMORY.md decision 2026-06-03 option a)
    - Note: retag is NOT performed here; it is user-gated confirm-only

    If `gate_decision: BLOCKED`: include the exact error evidence and identify the next cascade to fix. Do NOT proceed to the retag note.
  </action>
  <verify>
    <automated>grep -c '^gate_decision:' .planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-VERIFICATION.md</automated>
  </verify>
  <done>10-VERIFICATION.md exists with gate_decision set (APPROVED or BLOCKED). File committed to git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| minikube cluster → Anthropic API | Real API key used; ANTHROPIC_API_KEY from K8s Secret in tide-sample-medium namespace |
| tide-push Job → in-cluster git-http-server | Push uses http:// with no PAT (anonymous in-cluster remote — no credential needed for the demo server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-A | Information Disclosure | ANTHROPIC_API_KEY in DoD run | mitigate | Key is stored in K8s Secret `anthropic-creds` in tide-sample-medium; mounted via EnvFrom in claude-subagent pod only. Never logged. Budget cap (absoluteCapCents in project.yaml) limits max spend. |
| T-10-05-B | Repudiation | v1.0.0 retag without user confirmation | mitigate | The retag task is explicitly NOT automated in this plan. 10-VERIFICATION.md records the evidence and notes "user action required." The MEMORY.md decision (2026-06-03 option a: delete + recreate + push, confirm-only) is honored. |
</threat_model>

<verification>
Phase gate (before `/gsd-verify-work`):
- `make test` exits 0 (all unit tests green)
- `grep -c '^gate_decision: APPROVED$' .planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-VERIFICATION.md` returns 1
- OR `grep -c '^gate_decision: BLOCKED$' ...` returns 1 with cascade evidence documented
</verification>

<success_criteria>
SC-5 (legitimate medium Complete + push + costSpentCents > 0), SC-6 (v1.0.0 retag unblocked), and SC-3 (executor worktree branch non-empty) are validated by the checkpoint. 10-VERIFICATION.md records gate_decision: APPROVED. Phase 8 SC-2 flips to PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/10-task-execution-reliability-clone-idempotency-per-run-workspa/10-05-SUMMARY.md`
</output>
