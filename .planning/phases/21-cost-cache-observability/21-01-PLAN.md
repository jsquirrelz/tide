---
phase: 21-cost-cache-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/envelope.go
  - internal/subagent/anthropic/pricing.go
  - internal/subagent/anthropic/subagent.go
  - internal/metrics/registry.go
  - internal/controller/task_controller.go
  - internal/metrics/registry_test.go
  - internal/subagent/anthropic/pricing_test.go
autonomous: true
requirements:
  - OBSV-01
  - OBSV-02

must_haves:
  truths:
    - "tide_cache_savings_cents_total counter is registered in the Prometheus registry with labels {project, phase, plan, wave}"
    - "cacheSavingsCents(model, usage) computes CacheReadTokens × (inputRate - readRate) / 1_000_000 with truncation (not ceiling)"
    - "Usage.CacheSavingsCents int64 field carries the computed value from provider to controller with omitempty JSON tag"
    - "emitTaskMetrics() emits CacheSavingsCentsTotal.WithLabelValues(...).Add() next to CostCentsTotal — no pricing math in the controller"
    - "Hit-rate ratio is PromQL-derived per D-01 — no separate gauge metric is emitted; tide_cache_savings_cents_total is the emitted counter that satisfies OBSV-02 (criterion 2)"
    - "make lint passes — no task label in new counter, no provider firewall violation"
  artifacts:
    - path: "internal/metrics/registry.go"
      provides: "CacheSavingsCentsTotal var declaration, NewCounterVec construction, MustRegister entry"
      contains: "tide_cache_savings_cents_total"
    - path: "internal/subagent/anthropic/pricing.go"
      provides: "cacheSavingsCents method on *Anthropic"
      contains: "cacheSavingsCents"
    - path: "pkg/dispatch/envelope.go"
      provides: "CacheSavingsCents int64 field on Usage struct"
      contains: "cacheSavingsCents,omitempty"
    - path: "internal/controller/task_controller.go"
      provides: ".Add(float64(usage.CacheSavingsCents)) in emitTaskMetrics"
      contains: "CacheSavingsCentsTotal"
    - path: "internal/metrics/registry_test.go"
      provides: "TestRegistry_CacheSavingsCentsLabelArity + seed/want in AllMetricFamiliesPresent"
      contains: "CacheSavingsCentsLabelArity"
    - path: "internal/subagent/anthropic/pricing_test.go"
      provides: "TestCacheSavingsCents with 5 sub-tests"
      contains: "TestCacheSavingsCents"
  key_links:
    - from: "internal/subagent/anthropic/pricing.go"
      to: "pkg/dispatch.Usage.CacheSavingsCents"
      via: "subagent.go line 345 call site"
      pattern: "usage\\.CacheSavingsCents = a\\.cacheSavingsCents"
    - from: "pkg/dispatch.Usage.CacheSavingsCents"
      to: "internal/metrics/CacheSavingsCentsTotal"
      via: "emitTaskMetrics() in task_controller.go"
      pattern: "CacheSavingsCentsTotal\\.WithLabelValues"
---

<objective>
Add the realized-savings counter end-to-end through the Go stack: compute in the Anthropic provider, carry on Usage, register in the metrics registry, emit in the controller reconcile rollup.

Purpose: Satisfies OBSV-02 (emitted Prometheus counter for realized cache savings) and the backend half of OBSV-01 (the OBSV-01 audit guard in the registry test).

Output: `tide_cache_savings_cents_total{project,phase,plan,wave}` counter live in Prometheus after any Task completion; confirmed by unit tests + lint clean.
</objective>

<execution_context>
@/Users/justinsearles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsearles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-cost-cache-observability/21-CONTEXT.md
@.planning/phases/21-cost-cache-observability/21-RESEARCH.md
@.planning/phases/21-cost-cache-observability/21-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Verified against live code 2026-06-15. -->

From pkg/dispatch/envelope.go lines 270–301 (Usage struct):
  InputTokens          int64 `json:"inputTokens"`
  OutputTokens         int64 `json:"outputTokens"`
  CacheReadTokens      int64 `json:"cacheReadTokens"`
  CacheCreationTokens  int64 `json:"cacheCreationTokens"`
  EstimatedCostCents   int64 `json:"estimatedCostCents"`
  // ADD after EstimatedCostCents:
  // CacheSavingsCents  int64 `json:"cacheSavingsCents,omitempty"`

From internal/subagent/anthropic/pricing.go (signature of analog):
  func (a *Anthropic) estimatedCostCents(model string, u pkgdispatch.Usage) int64

From internal/subagent/anthropic/subagent.go line 345 (call site):
  usage.EstimatedCostCents = a.estimatedCostCents(in.Provider.Model, usage)
  // ADD immediately after ↑

From internal/metrics/registry.go (insertion points):
  var CostCentsTotal *prometheus.CounterVec                   // line 111 — add CacheSavingsCentsTotal after
  CostCentsTotal = prometheus.NewCounterVec(...)              // lines 213–219 — add construction after
  metrics.Registry.MustRegister(... CostCentsTotal, ...)      // lines 229–244 — add CacheSavingsCentsTotal after CostCentsTotal

From internal/controller/task_controller.go lines 1068–1072 (emission block):
  tidemetrics.CostCentsTotal.WithLabelValues(projectName, phase, plan, wave).Add(float64(usage.EstimatedCostCents))
  // ADD after ↑:
  // tidemetrics.CacheSavingsCentsTotal.WithLabelValues(projectName, phase, plan, wave).Add(float64(usage.CacheSavingsCents))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CacheSavingsCents to Usage, pricing.go helper, and subagent.go call site</name>
  <files>pkg/dispatch/envelope.go, internal/subagent/anthropic/pricing.go, internal/subagent/anthropic/subagent.go, internal/subagent/anthropic/pricing_test.go</files>
  <read_first>
    - pkg/dispatch/envelope.go lines 270–310 (Usage struct; EstimatedCostCents analog at line 282)
    - internal/subagent/anthropic/pricing.go lines 125–160 (estimatedCostCents analog; a.prices access pattern; conservativeTier fallback)
    - internal/subagent/anthropic/subagent.go lines 340–355 (call site context; how usage is assembled and returned)
    - internal/subagent/anthropic/pricing_test.go lines 40–80 (existing sub-test structure for haiku_cache_read)
    - pkg/dispatch/envelope_test.go lines 647–670 (TestNewTerminationStub_StaysSmall — run this after adding the field)
  </read_first>
  <behavior>
    - TestCacheSavingsCents/haiku_1M_read_tokens: cacheSavingsCents("claude-haiku-4-5", Usage{CacheReadTokens: 1_000_000}) == 90
    - TestCacheSavingsCents/sonnet_1M_read_tokens: cacheSavingsCents("claude-sonnet-4-6", Usage{CacheReadTokens: 1_000_000}) == 270
    - TestCacheSavingsCents/zero_read_tokens: cacheSavingsCents("claude-haiku-4-5", Usage{InputTokens: 1_000_000}) == 0
    - TestCacheSavingsCents/truncation_not_ceiling: cacheSavingsCents("claude-haiku-4-5", Usage{CacheReadTokens: 100}) == 0
    - TestCacheSavingsCents/unknown_model_uses_conservative: cacheSavingsCents("claude-unknown-model", Usage{CacheReadTokens: 1_000_000}) == 900 (conservativeTier: input=1000, read=100)
    - TestNewTerminationStub_StaysSmall still passes after adding CacheSavingsCents (omitempty keeps zero field absent)
  </behavior>
  <action>
    In pkg/dispatch/envelope.go: add field `CacheSavingsCents int64 \`json:"cacheSavingsCents,omitempty"\`` immediately after `EstimatedCostCents` at line 282. Add a comment explaining: "Computed by the provider (provider firewall D-C1), zero when CacheReadTokens is zero. Omitted from JSON when zero (common case)."

    In internal/subagent/anthropic/pricing.go: add method `cacheSavingsCents(model string, u pkgdispatch.Usage) int64` immediately after `estimatedCostCents` (after line 157). Key differences from the analog: (a) early-return `0` when `u.CacheReadTokens == 0`, (b) use truncation division `savings / 1_000_000` (not ceiling division — truncation is conservative for savings), (c) no stderr warning on table miss (silent fallback to conservativeTier), (d) reads `a.prices` (per-instance clone), never `priceTable` directly (T-14-02). Formula: `savings := u.CacheReadTokens * (price.inputCentsPerMTok - price.cacheReadCentsPerMTok); return savings / 1_000_000`.

    In internal/subagent/anthropic/subagent.go: add `usage.CacheSavingsCents = a.cacheSavingsCents(in.Provider.Model, usage)` immediately after `usage.EstimatedCostCents = a.estimatedCostCents(in.Provider.Model, usage)` at line 345. Both lines stay in `internal/subagent/anthropic/` per provider firewall D-C1.

    In internal/subagent/anthropic/pricing_test.go: add `TestCacheSavingsCents` function with the five sub-tests described in the behavior block. Use `a := New(Options{})` for the receiver, matching the existing test setup. Token counts must be ≥ 1_000_000 for nonzero results (integer division truncates at small counts — Pitfall 3).

    Write tests first (RED), then implement the helper to make them GREEN. Run `TestNewTerminationStub_StaysSmall` to confirm the omitempty field doesn't bloat the stub.
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/ -run TestCacheSavingsCents -v && go test ./pkg/dispatch/ -run TestNewTerminationStub_StaysSmall -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/subagent/anthropic/ -run TestCacheSavingsCents` exits 0 with all 5 sub-tests passing
    - `go test ./pkg/dispatch/ -run TestNewTerminationStub_StaysSmall` exits 0
    - `grep -c "cacheSavingsCents,omitempty" pkg/dispatch/envelope.go` returns 1
    - `grep -c "func.*cacheSavingsCents" internal/subagent/anthropic/pricing.go` returns 1
    - `grep -c "usage.CacheSavingsCents = a.cacheSavingsCents" internal/subagent/anthropic/subagent.go` returns 1
    - No import of `internal/subagent/anthropic` in `pkg/dispatch/` (provider firewall intact)
  </acceptance_criteria>
  <done>TestCacheSavingsCents passes with correct math; TerminationStub size test still green; CacheSavingsCents field on Usage with omitempty; cacheSavingsCents method on *Anthropic; call site in subagent.go after EstimatedCostCents assignment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register CacheSavingsCentsTotal counter + emit in emitTaskMetrics + registry tests</name>
  <files>internal/metrics/registry.go, internal/controller/task_controller.go, internal/metrics/registry_test.go</files>
  <read_first>
    - internal/metrics/registry.go lines 98–125 (var declarations section; CostCentsTotal at line 111)
    - internal/metrics/registry.go lines 210–250 (init construction block; MustRegister block lines 229–244)
    - internal/controller/task_controller.go lines 1044–1080 (emitTaskMetrics signature; emission block at 1068–1072)
    - internal/metrics/registry_test.go lines 54–100 (TestRegistry_AllMetricFamiliesPresent: seed lines + want slice)
    - internal/metrics/registry_test.go lines 214–230 (TestRegistry_CostCentsLabelArity — exact clone target)
  </read_first>
  <behavior>
    - TestRegistry_AllMetricFamiliesPresent: Gather() includes "tide_cache_savings_cents_total" in the family name list
    - TestRegistry_CacheSavingsCentsLabelArity: CacheSavingsCentsTotal.WithLabelValues("p","ph","pl","w").Add(1) → testutil.ToFloat64(...) >= 1
    - TestRegistry_NoTaskLabel: new counter does NOT contain "task" in any label slice (existing test must remain passing)
    - go test ./internal/metrics/ exits 0 for all registry tests
    - go test ./internal/controller/ exits 0 (emitTaskMetrics unit tests pass if they exist)
  </behavior>
  <action>
    In internal/metrics/registry.go:
    (a) After the `CostCentsTotal` var declaration (line 111), add: `// CacheSavingsCentsTotal counts the realized cache savings in US cents per Task (Phase 21 OBSV-02). var CacheSavingsCentsTotal *prometheus.CounterVec`
    (b) After the `CostCentsTotal` construction block (lines 213–219), add the NewCounterVec block: Name `tide_cache_savings_cents_total`, Help `Realized cache savings in US cents (input price minus cache-read price) per Task (Phase 21 OBSV-02).`, Labels `[]string{"project", "phase", "plan", "wave"}`. CRITICAL: the label slice MUST NOT contain `"task"` — the `metriccardinality` AST analyzer rejects it.
    (c) In the MustRegister block (lines 229–244), add `CacheSavingsCentsTotal,` after `CostCentsTotal,` and before `TaskDurationSeconds`. Do NOT add a second MustRegister call for `ProviderRateLimitHitsTotal` (the comment at line 246 explains the duplicate-registration rule).

    In internal/controller/task_controller.go: in `emitTaskMetrics()`, add `tidemetrics.CacheSavingsCentsTotal.WithLabelValues(projectName, phase, plan, wave).Add(float64(usage.CacheSavingsCents))` immediately after the `CostCentsTotal` `.Add()` call at line 1072. No pricing imports — the controller only reads `usage.CacheSavingsCents` (a plain int64).

    In internal/metrics/registry_test.go:
    (a) In `TestRegistry_AllMetricFamiliesPresent` seed block: add `tidemetrics.CacheSavingsCentsTotal.WithLabelValues("__seed__", "ph", "pl", "w").Add(0)` after the `CostCentsTotal` seed line.
    (b) In `TestRegistry_AllMetricFamiliesPresent` want slice: add `"tide_cache_savings_cents_total",` after `"tide_cost_cents_total",`.
    (c) Add new test function `TestRegistry_CacheSavingsCentsLabelArity` immediately after `TestRegistry_CostCentsLabelArity` (after line ~220), mirroring it exactly with `CacheSavingsCentsTotal`. Include the OBSV-01 audit comment: "Also serves as the OBSV-01 audit guard: the savings counter carries the same four-label set that makes per-level PromQL slicing work without extra instrumentation."

    Write tests first (add seed/want/arity test with compile error because CacheSavingsCentsTotal doesn't exist yet — RED), then add the registry.go changes (GREEN). The emitTaskMetrics change has no dedicated unit test path to write first; add it after the registry is green and run the existing controller tests to verify no regression.
  </action>
  <verify>
    <automated>go test ./internal/metrics/ -v -run TestRegistry && go test ./internal/controller/ -count=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/metrics/ -run TestRegistry` exits 0, including `TestRegistry_CacheSavingsCentsLabelArity` and `TestRegistry_AllMetricFamiliesPresent`
    - `grep -c "tide_cache_savings_cents_total" internal/metrics/registry.go` returns 1
    - `grep -c "CacheSavingsCentsTotal" internal/controller/task_controller.go` returns 1
    - `grep -c "CacheSavingsCentsLabelArity" internal/metrics/registry_test.go` returns 1
    - `grep -v '^//' internal/metrics/registry.go | grep -c '"task"'` returns 0 (no task label, excluding comments)
    - `make lint` exits 0 (metriccardinality + providerfirewall analyzers pass)
  </acceptance_criteria>
  <done>CacheSavingsCentsTotal registered; emitTaskMetrics emits it; registry tests all green including the new arity test and OBSV-01 audit guard; make lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider → controller | Usage.CacheSavingsCents carries an int64 computed inside internal/subagent/anthropic/; the controller reads it as a plain typed field with no Anthropic-specific import |
| controller → Prometheus | emitTaskMetrics() converts to float64 and adds to a fixed-label counter; no user input reaches label values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Spoofing | Usage.CacheSavingsCents int64 field | accept | Field is populated by the provider's own accounting after parsing the Anthropic API response; it does not come from an untrusted source. The provider firewall (D-C1) ensures only the Anthropic implementation sets pricing values. |
| T-21-01-02 | Tampering | pricing.go priceTable access | mitigate | cacheSavingsCents reads `a.prices` (per-instance clone via New()), never the package-level `priceTable` directly — T-14-02 invariant. Enforced in pricing_test.go and by the existing cost_parity_test.go. |
| T-21-01-03 | Denial of Service | Prometheus counter cardinality | mitigate | Locked four-label set `{project,phase,plan,wave}` — identical to all other TELEM-03 counters. The `metriccardinality` AST analyzer at `make lint` rejects any `"task"` label literal. No new cardinality surface introduced. |
| T-21-01-04 | Information Disclosure | Savings counter value leaks model pricing | accept | Counter is in US cents (integer), not a rate or model identifier. The 90% ratio is publicly documented by Anthropic. No credential or per-user data exposed. |
| T-21-01-05 | Elevation of Privilege | Provider firewall violation | mitigate | `tools/analyzers/providerfirewall` at `make lint` rejects any import of `internal/subagent/anthropic` in `internal/controller` or `pkg/dispatch`. The controller reads only `usage.CacheSavingsCents int64`. |
| T-21-01-SC | Tampering | npm/pip/cargo installs | accept | This plan adds zero new external packages (Go side). No package legitimacy gate required. |
</threat_model>

<verification>
Full Go test pass after both tasks are complete:

```
go test ./internal/metrics/ ./internal/subagent/anthropic/ ./pkg/dispatch/ ./internal/controller/ -count=1
make lint
```

Both must exit 0.

Source-level assertions:
- `grep -c "tide_cache_savings_cents_total" internal/metrics/registry.go` == 1
- `grep -c "cacheSavingsCents,omitempty" pkg/dispatch/envelope.go` == 1
- `grep -c "func.*Anthropic.*cacheSavingsCents" internal/subagent/anthropic/pricing.go` == 1
- `grep -c "usage.CacheSavingsCents = a.cacheSavingsCents" internal/subagent/anthropic/subagent.go` == 1
- `grep -c "CacheSavingsCentsTotal" internal/controller/task_controller.go` == 1
- `grep -c "CacheSavingsCentsLabelArity" internal/metrics/registry_test.go` == 1
- `grep -c "TestCacheSavingsCents" internal/subagent/anthropic/pricing_test.go` == 1
</verification>

<success_criteria>
- `tide_cache_savings_cents_total` counter registered with labels `{project,phase,plan,wave}`
- `cacheSavingsCents` helper in `pricing.go` computing savings as `CacheReadTokens × (inputRate − readRate) / 1_000_000` with truncation
- `Usage.CacheSavingsCents int64 \`json:"cacheSavingsCents,omitempty"\`` field on the carry struct
- `emitTaskMetrics()` emits the counter next to CostCentsTotal with identical label values
- All unit tests pass; `make lint` exits 0
- OBSV-01 audit guard: `TestRegistry_CacheSavingsCentsLabelArity` confirms the four-label set
</success_criteria>

<output>
Create `.planning/phases/21-cost-cache-observability/21-01-SUMMARY.md` when done.
</output>
