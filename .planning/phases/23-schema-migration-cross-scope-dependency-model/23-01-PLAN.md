---
phase: 23-schema-migration-cross-scope-dependency-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha2/groupversion_info.go
  - api/v1alpha2/wave_types.go
  - api/v1alpha2/task_types.go
  - api/v1alpha2/plan_types.go
  - api/v1alpha2/phase_types.go
  - api/v1alpha2/milestone_types.go
  - api/v1alpha2/project_types.go
  - api/v1alpha2/shared_types.go
  - api/v1alpha2/zz_generated.deepcopy.go
  - api/v1alpha2/schema_test.go
  - api/v1alpha1/plan_types.go
  - config/crd/bases/tideproject.k8s_waves.yaml
  - config/crd/bases/tideproject.k8s_tasks.yaml
  - config/crd/bases/tideproject.k8s_plans.yaml
  - config/crd/bases/tideproject.k8s_phases.yaml
  - config/crd/bases/tideproject.k8s_milestones.yaml
  - config/crd/bases/tideproject.k8s_projects.yaml
  - Makefile
autonomous: true
requirements: [SCHEMA-01, DEPS-01, DEPS-02]

must_haves:
  truths:
    - "A v1alpha2.WaveSpec carries ProjectRef + a global-scoped WaveIndex (no PlanRef)"
    - "v1alpha2.TaskSpec.DependsOn accepts any-level names (the plan-local D-F1 restriction is gone from the field doc and validation)"
    - "v1alpha2.PlanSpec has a DependsOn field; Phase/Milestone DependsOn docs say any-level not sibling-only"
    - "v1alpha2.ProjectSpec carries a discriminator field present only in v1alpha2 (enables the Plan-03 old-object guard)"
    - "make manifests regenerates CRDs with v1alpha2 as the storage version and v1alpha1 marked unserved"
    - "make verify-no-aggregates greps api/v1alpha2 as well as api/v1alpha1 and passes"
  artifacts:
    - path: "api/v1alpha2/wave_types.go"
      provides: "WaveSpec{ProjectRef, WaveIndex} global re-ownership (SCHEMA-01, D-07)"
      contains: "ProjectRef"
    - path: "api/v1alpha2/task_types.go"
      provides: "broadened TaskSpec.DependsOn (DEPS-01, D-F1 retired)"
      contains: "DependsOn"
    - path: "api/v1alpha2/plan_types.go"
      provides: "PlanSpec.DependsOn added + storageversion marker (DEPS-02)"
      contains: "+kubebuilder:storageversion"
    - path: "api/v1alpha2/groupversion_info.go"
      provides: "Version=v1alpha2 group registration"
      contains: "v1alpha2"
  key_links:
    - from: "api/v1alpha2/*_types.go"
      to: "config/crd/bases/*.yaml"
      via: "make manifests (controller-gen)"
      pattern: "storage: true"
    - from: "Makefile verify-no-aggregates"
      to: "api/v1alpha2/*_types.go"
      via: "grep glob"
      pattern: "api/v1alpha2"
---

<objective>
Introduce the `api/v1alpha2/` package as the new served+storage CRD version, mirroring all six Kinds from `api/v1alpha1/` with the Spring-Tide schema reshape: WaveSpec re-owned Plan→Project with a global WaveIndex (SCHEMA-01, D-07), `dependsOn` broadened to any-level on every level (DEPS-01/DEPS-02, D-01/D-02), and a v1alpha2-only discriminator field on ProjectSpec that Plan 03's old-object guard keys on. Move `+kubebuilder:storageversion` off v1alpha1 Plan onto each v1alpha2 Kind. Regenerate deepcopy + CRD YAML, and extend the `verify-no-aggregates` guard glob to cover the new package.

This plan delivers ONLY the type package + generated artifacts + structural unit tests. Scheme/webhook/controller rewiring is Plan 02; controller-side guards (old-object rejection, global cycle gate, metric resemantics) are Plan 03.

Purpose: Every downstream plan and Phase 24 imports these types; they are the contract. Shaping them first prevents a scavenger hunt.
Output: `api/v1alpha2/` package (8 Go files + generated deepcopy), regenerated CRD bases, an extended Makefile guard, and a unit test suite proving the field shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-schema-migration-cross-scope-dependency-model/23-CONTEXT.md
@.planning/phases/23-schema-migration-cross-scope-dependency-model/23-RESEARCH.md
@.planning/phases/23-schema-migration-cross-scope-dependency-model/23-VALIDATION.md

<interfaces>
<!-- Source-of-truth v1alpha1 shapes being mirrored. Executor copies these files
     into api/v1alpha2/, changes package to v1alpha2, and applies the deltas below.
     Do NOT explore the codebase for these — they are reproduced here. -->

api/v1alpha1/wave_types.go — WaveSpec (REPLACE PlanRef with ProjectRef):
  type WaveSpec struct {
    PlanRef   string `json:"planRef"`   // +kubebuilder:validation:MinLength=1  ← REMOVE
    WaveIndex int    `json:"waveIndex"` // +kubebuilder:validation:Minimum=0     ← KEEP, re-doc as global
  }
  WaveStatus (KEEP unchanged): Phase, Conditions, TaskRefs, DispatchedAt, CompletedAt.
  Root markers on Wave today: +kubebuilder:object:root=true / :subresource:status / :resource:scope=Namespaced + 3 printcolumns.

api/v1alpha1/task_types.go — TaskSpec.DependsOn (line 74-76), currently:
  // DependsOn lists sibling Task names in the same Plan (D-F1).
  // +optional
  DependsOn []string `json:"dependsOn,omitempty"`
  PlanRef (line 70-72, KEEP for ownership), FilesTouched, PromptPath, DeclaredOutputPaths,
  Caps, Dev, SharedContext all KEEP unchanged. Caps + TaskDev structs KEEP verbatim.

api/v1alpha1/plan_types.go — PlanSpec (line 24-35): PhaseRef + SharedContext only; NO dependsOn.
  Plan root marker block (line 70-76) currently carries +kubebuilder:storageversion ← MUST MOVE to v1alpha2.
  PlanStatus KEEP unchanged (Phase, Conditions, ValidationState, CycleEdges, IntegratedThroughWave).

api/v1alpha1/phase_types.go — PhaseSpec.DependsOn (line 29-31): doc says "sibling Phase names in the same Milestone".
api/v1alpha1/milestone_types.go — MilestoneSpec.DependsOn (line 29-31): doc says "sibling Milestone names in the same Project".
api/v1alpha1/project_types.go — ProjectSpec (line 298-365): large struct, mirror verbatim then ADD discriminator field.
  ProjectStatus (line 395-424) KEEP unchanged.

api/v1alpha1/groupversion_info.go — pattern to mirror with Version: "v1alpha2":
  SchemeGroupVersion = schema.GroupVersion{Group: "tideproject.k8s", Version: "v1alpha2"}
  SchemeBuilder = &scheme.Builder{GroupVersion: SchemeGroupVersion}; AddToScheme = SchemeBuilder.AddToScheme
  Package doc marker: // +kubebuilder:object:generate=true  and  // +groupName=tideproject.k8s

Makefile verify-no-aggregates (line 526-535): greps `api/v1alpha1/*_types.go` for
  'Schedule|Waves *\[\]|IndegreeMap|CachedDag|DerivedDag'. WaveIndex is NOT in the denylist
  (spec field, not a cached aggregate) — keep it that way.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create api/v1alpha2 package — schema reshape for Wave, Task, Plan, Phase, Milestone, Project</name>
  <files>api/v1alpha2/groupversion_info.go, api/v1alpha2/wave_types.go, api/v1alpha2/task_types.go, api/v1alpha2/plan_types.go, api/v1alpha2/phase_types.go, api/v1alpha2/milestone_types.go, api/v1alpha2/project_types.go, api/v1alpha2/shared_types.go, api/v1alpha1/plan_types.go</files>
  <read_first>
    api/v1alpha1/wave_types.go, api/v1alpha1/task_types.go, api/v1alpha1/plan_types.go,
    api/v1alpha1/phase_types.go, api/v1alpha1/milestone_types.go, api/v1alpha1/project_types.go,
    api/v1alpha1/groupversion_info.go, api/v1alpha1/shared_types.go
  </read_first>
  <behavior>
    - api/v1alpha2 compiles as a standalone package (Group=tideproject.k8s, Version=v1alpha2).
    - v1alpha2.WaveSpec has ProjectRef (string, MinLength=1) and WaveIndex (int, Minimum=0); has NO PlanRef field.
    - v1alpha2.TaskSpec.DependsOn exists, is optional, and its doc comment names any-level targets with D-F1 explicitly retired; PlanRef is retained for ownership.
    - v1alpha2.PlanSpec has a DependsOn []string field (optional) plus the existing PhaseRef + SharedContext.
    - v1alpha2.PhaseSpec / MilestoneSpec DependsOn doc comments say "any level node in this Project", not "sibling-only".
    - v1alpha2.ProjectSpec carries a discriminator field (e.g. SchemaRevision string, +kubebuilder:validation:Required, +kubebuilder:validation:Enum=v1alpha2) that is absent in v1alpha1.
    - Exactly one storageversion marker per Kind, all on v1alpha2; v1alpha1 Plan no longer carries +kubebuilder:storageversion.
  </behavior>
  <action>
    Copy each v1alpha1 type file into api/v1alpha2/ and change `package v1alpha1` to `package v1alpha2`. Mirror groupversion_info.go with `Version: "v1alpha2"` (keep Group `tideproject.k8s`, the `+kubebuilder:object:generate=true` and `+groupName=tideproject.k8s` package markers, and the `//nolint:staticcheck` scheme.Builder comment). Mirror shared_types.go verbatim (it carries Condition*/Reason* constants the v2 controllers reuse); ADD two new constants `ReasonRequiresReinstall = "RequiresReinstall"` and `ReasonGlobalCycleDetected = "GlobalCycleDetected"` for Plan 03 to consume.

    Apply these deltas in v1alpha2:
    - wave_types.go: replace `PlanRef string` (and its MinLength marker) with `ProjectRef string` carrying `+kubebuilder:validation:MinLength=1`; re-doc WaveIndex as the global monotonic 0-indexed wave position derived by pkg/dag.ComputeWaves over the whole Project, never cached in status (cite PERSIST-03 / verify-no-aggregates). Keep WaveStatus, WaveList, and all root markers identical to v1alpha1.
    - task_types.go: keep PlanRef for ownership; broaden the DependsOn doc to "names of Tasks (or Plan/Phase/Milestone scope nodes) in any plan/phase/milestone of this Project; D-F1 plan-local restriction retired; coarse scope refs are fan-out expanded by the Phase 24 assembler"; add a CEL marker on DependsOn rejecting empty-string entries: `+kubebuilder:validation:XValidation:rule="self.all(d, size(d) > 0)",message="dependsOn entries must not be empty strings"`. Keep Caps, TaskDev, FilesTouched, PromptPath, DeclaredOutputPaths, Dev, SharedContext verbatim.
    - plan_types.go: add `DependsOn []string` with `+optional` and a doc explaining plan-level coarse deps + progressive refinement (D-03); MOVE `+kubebuilder:storageversion` onto the Plan root marker block here.
    - phase_types.go / milestone_types.go: rewrite the DependsOn doc comment to say "any level node (Milestone/Phase/Plan/Task) in this Project"; keep the field shape.
    - project_types.go: mirror ProjectSpec verbatim, then add the discriminator field `SchemaRevision string` with `+kubebuilder:validation:Required` and `+kubebuilder:validation:Enum=v1alpha2` and a doc explaining it gates the Plan-03 old-object reinstall guard (an object lacking it was authored under v1alpha1). Keep ProjectStatus + all Project* constants + ProjectList unchanged.
    - All six Kinds: add `+kubebuilder:storageversion` to the v1alpha2 root marker block (on Plan, plus Wave/Task/Phase/Milestone/Project which had none in v1alpha1).

    In api/v1alpha1/plan_types.go: REMOVE the `+kubebuilder:storageversion` marker line (line 71). Do not otherwise touch v1alpha1 type bodies in this task — the unservedversion markers land in Plan 02.

    Do NOT add any `init()` SchemeBuilder.Register calls that differ from the v1alpha1 pattern — keep each file's `func init() { SchemeBuilder.Register(&Kind{}, &KindList{}) }`.
  </action>
  <verify>
    <automated>go build ./api/v1alpha2/... && grep -c '+kubebuilder:storageversion' api/v1alpha1/plan_types.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./api/v1alpha2/...` exits 0.
    - `grep -c '+kubebuilder:storageversion' api/v1alpha1/plan_types.go` returns 0 (marker moved off v1alpha1).
    - `grep -c '+kubebuilder:storageversion' api/v1alpha2/plan_types.go` returns 1.
    - `grep -c 'ProjectRef' api/v1alpha2/wave_types.go` ≥ 1 AND `grep -c 'PlanRef' api/v1alpha2/wave_types.go` returns 0.
    - `grep -c 'DependsOn' api/v1alpha2/plan_types.go` ≥ 1.
    - `grep -c 'SchemaRevision' api/v1alpha2/project_types.go` ≥ 1.
    - `grep -cE 'sibling' api/v1alpha2/phase_types.go api/v1alpha2/milestone_types.go api/v1alpha2/task_types.go` returns 0 (no stale sibling-only docs).
  </acceptance_criteria>
  <done>api/v1alpha2 package compiles with the reshaped schema; storageversion moved off v1alpha1 Plan onto every v1alpha2 Kind; discriminator field present on ProjectSpec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate deepcopy + CRDs, extend verify-no-aggregates glob, and add structural unit tests</name>
  <files>api/v1alpha2/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_waves.yaml, config/crd/bases/tideproject.k8s_tasks.yaml, config/crd/bases/tideproject.k8s_plans.yaml, config/crd/bases/tideproject.k8s_phases.yaml, config/crd/bases/tideproject.k8s_milestones.yaml, config/crd/bases/tideproject.k8s_projects.yaml, Makefile, api/v1alpha2/schema_test.go</files>
  <read_first>
    Makefile (verify-no-aggregates target, lines 526-535), api/v1alpha1/aggregates_guard_test.go,
    api/v1alpha1/phase3_schema_test.go, api/v1alpha2/wave_types.go, api/v1alpha2/task_types.go, api/v1alpha2/plan_types.go
  </read_first>
  <behavior>
    - `make generate` produces api/v1alpha2/zz_generated.deepcopy.go covering all six Kinds + sub-structs.
    - `make manifests` regenerates config/crd/bases/*.yaml so each CRD lists v1alpha2 with `served: true, storage: true` and v1alpha1 with `served: false`.
    - No CRD emits `conversion: strategy: Webhook` (the None default holds — clean break, D-09).
    - `make verify-no-aggregates` greps api/v1alpha2/*_types.go in addition to api/v1alpha1 and exits 0.
    - schema_test.go asserts: WaveSpec has ProjectRef not PlanRef (SCHEMA-01); TaskSpec.DependsOn accepts a cross-scope name with no plan-local rejection at the Go-type level (DEPS-01); PlanSpec.DependsOn round-trips (DEPS-02).
  </behavior>
  <action>
    Run `make generate manifests` (controller-gen) to emit api/v1alpha2/zz_generated.deepcopy.go and regenerate the six config/crd/bases CRDs. Do NOT hand-edit generated files; if controller-gen errors on multiple storage versions, fix the offending marker in the types (Pitfall 1) and rerun — never edit the YAML directly.

    Extend the Makefile `verify-no-aggregates` target: change the grep target from `api/v1alpha1/*_types.go` to cover both `api/v1alpha1/*_types.go api/v1alpha2/*_types.go` (keep the same `Schedule|Waves *\[\]|IndegreeMap|CachedDag|DerivedDag` pattern and the same fail-on-match logic and echo line). Confirm `WaveIndex` does not match the pattern (it must not — it is a permitted Wave spec field).

    Author api/v1alpha2/schema_test.go (package v1alpha2, stdlib `testing`) following the api/v1alpha1/phase3_schema_test.go style. Provide these test functions so the VALIDATION.md run-name map is satisfied:
    - `TestWaveSpec`: construct a v1alpha2.Wave with Spec.ProjectRef set + Spec.WaveIndex=3, assert the fields round-trip; assert (via reflect on WaveSpec) there is no field named "PlanRef".
    - `TestTaskDependsOn`: construct a Task whose Spec.DependsOn names a task in another plan (e.g. "milestone-b-phase-3-plan-c-task-07") and a scope node ("milestone-a"); assert both entries are retained on the struct (no plan-local filtering at the type level).
    - `TestPlanDependsOn`: construct a Plan with Spec.DependsOn=["phase-2","plan-x-task-01"] and assert round-trip; assert PlanSpec has a DependsOn field via reflect.
    These are type-level/structural tests (no envtest); the admission-time CEL behavior is exercised by Plan 03's envtest.
  </action>
  <verify>
    <automated>make generate manifests && make verify-no-aggregates && go test ./api/v1alpha2/... -run 'TestWaveSpec|TestTaskDependsOn|TestPlanDependsOn' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-no-aggregates` exits 0 AND `grep -c 'api/v1alpha2' Makefile` ≥ 1 (glob extended).
    - `go test ./api/v1alpha2/... -run 'TestWaveSpec|TestTaskDependsOn|TestPlanDependsOn' -count=1` exits 0.
    - In config/crd/bases/tideproject.k8s_plans.yaml: the v1alpha2 version entry has `storage: true` and the v1alpha1 entry has `served: false` (`grep -A2 'name: v1alpha2' config/crd/bases/tideproject.k8s_plans.yaml | grep -c 'storage: true'` returns 1).
    - `grep -rc 'strategy: Webhook' config/crd/bases/` returns 0 across all six CRDs.
    - api/v1alpha2/zz_generated.deepcopy.go exists and `go vet ./api/v1alpha2/...` exits 0.
  </acceptance_criteria>
  <done>Generated deepcopy + CRDs reflect v1alpha2-as-storage with v1alpha1 unserved and no conversion webhook; the aggregates guard covers the new package; structural unit tests for SCHEMA-01/DEPS-01/DEPS-02 pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kubectl/client → API server admission | Untrusted `dependsOn` string entries and CRD spec fields cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `dependsOn` []string entries (Task/Plan/Phase/Milestone) | mitigate | CEL `XValidation` on TaskSpec.DependsOn rejects empty-string entries at admission; MinLength on ProjectRef/PlanRef refs (V5 Input Validation) |
| T-23-02 | Tampering | CRD storage-version mislabel (two storage versions) | mitigate | controller-gen rejects dual storageversion markers at `make manifests`; acceptance criterion greps for exactly one v1alpha2 storageversion marker |
| T-23-03 | Information Disclosure | none — no PII, no secrets in schema fields | accept | Schema carries only structural refs + an enum discriminator; no sensitive data |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH "Package Legitimacy Audit: not applicable"); slopcheck N/A |
</threat_model>

<verification>
- `make generate manifests` regenerates cleanly with no dual-storage-version error.
- `make verify-no-aggregates` and `make verify-dag-imports` both exit 0.
- `go build ./api/v1alpha2/...` and `go test ./api/v1alpha2/... -count=1` exit 0.
- CRD bases show v1alpha2 served+storage, v1alpha1 served:false, conversion strategy None.
</verification>

<success_criteria>
- api/v1alpha2 package exists with all six reshaped Kinds and compiles.
- WaveSpec is re-owned to Project with a global WaveIndex (SCHEMA-01).
- TaskSpec.DependsOn is broadened past plan-local; PlanSpec.DependsOn exists; Phase/Milestone docs say any-level (DEPS-01, DEPS-02).
- ProjectSpec carries a v1alpha2-only discriminator field for the Plan-03 guard.
- storageversion is on v1alpha2 only; v1alpha1 Plan marker removed.
- verify-no-aggregates covers api/v1alpha2 and passes.
</success_criteria>

<output>
Create `.planning/phases/23-schema-migration-cross-scope-dependency-model/23-01-SUMMARY.md` when done.
</output>
