---
phase: 23-schema-migration-cross-scope-dependency-model
plan: "05"
type: execute
wave: 4
gap_closure: true
depends_on: ["23-01", "23-02", "23-03", "23-04"]
requirements: [DEPS-01, DEPS-02, DEPS-03]
files_modified:
  - api/v1alpha2/task_types.go
  - api/v1alpha2/plan_types.go
  - api/v1alpha2/milestone_types.go
  - api/v1alpha2/phase_types.go
  - internal/controller/project_controller.go
  - internal/controller/wave_controller.go
  - config/crd/bases/tideproject.k8s_tasks.yaml
  - config/crd/bases/tideproject.k8s_plans.yaml
  - config/crd/bases/tideproject.k8s_milestones.yaml
  - config/crd/bases/tideproject.k8s_phases.yaml
  # WR-03 alias rename touches the broad consumer set (verified by go build/vet)
must_haves:
  truths:
    - "WR-04: every dependsOn list across Task/Plan/Phase/Milestone v1alpha2 rejects empty-string entries and self-reference via CEL (clear message), not via the cycle detector"
    - "WR-02: editing a Task's dependsOn requeues its owning Project so a fixed cycle clears the CycleDetected condition (Project reconciler watches Task)"
    - "WR-01: the Wave observational roll-up lists Tasks scoped by project (owner.LabelProject == Wave.Spec.ProjectRef), no cross-plan/cross-project contamination"
    - "WR-03: a single import alias for api/v1alpha2 (tideprojectv1alpha2 / tidev1alpha2); no remaining tideprojectv1alpha1/tidev1alpha1 alias pointing at api/v1alpha2"
    - "go build ./... && go vet ./... exit 0; make verify-no-aggregates && make verify-dag-imports exit 0; make test and make test-int-fast pass"
  artifacts:
    - api/v1alpha2/task_types.go
    - internal/controller/project_controller.go
    - internal/controller/wave_controller.go

threat_model:
  - id: T-23-W04
    surface: V5 input validation
    threat: "An empty-string or self-referential dependsOn entry bypasses structural validation and degrades into a confusing runtime 'cyclic DAG' error or a malformed edge"
    mitigation: "CEL x-kubernetes-validations rejects empty items + self-reference at admission with a clear message (WR-04)"
    severity: low
---

<objective>
Close the 4 advisory code-review warnings (23-REVIEW.md) the user elected to fix in full. All are real correctness/clarity gaps against locked Phase-23 decisions. Drive `go build ./... && go vet ./...` and the test tiers green. This is a gap-closure plan on top of merged 23-01..04.
</objective>

<context>
Phase 23 migrated the operator to v1alpha2 (served+storage); v1alpha1 unserved. Code review (23-REVIEW.md) found 0 blockers, 4 warnings, doctrine clean. Fix all four. Do NOT build the Phase-24 global wave-derivation engine — WR-01 is an interim scoping fix to the observational stub, keeping its `// TODO(phase-24)` note.
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-04 — CEL-reject empty-string + self-reference on every v1alpha2 dependsOn; add the missing Plan.DependsOn rule; regenerate CRDs</name>
  <files>api/v1alpha2/task_types.go, api/v1alpha2/plan_types.go, api/v1alpha2/milestone_types.go, api/v1alpha2/phase_types.go, config/crd/bases/tideproject.k8s_{tasks,plans,milestones,phases}.yaml</files>
  <read_first>
    api/v1alpha2/task_types.go (DependsOn field + any existing CEL), api/v1alpha2/plan_types.go (DependsOn has NO CEL today — WR-04), api/v1alpha2/milestone_types.go + phase_types.go (DependsOn markers to mirror), api/v1alpha1/*_types.go (for the prior CEL idiom if any), .planning/phases/23-schema-migration-cross-scope-dependency-model/23-REVIEW.md (WR-04 detail)
  </read_first>
  <action>
    On EVERY `DependsOn []string` field across api/v1alpha2 (Task, Plan, Milestone, Phase): add item-level non-empty validation `+kubebuilder:validation:items:MinLength=1` (reject `[""]`), and a self-reference rejection. Self-reference is expressible as a Kind-root `+kubebuilder:validation:XValidation` rule referencing `self.metadata.name` and `self.spec.dependsOn`, e.g. rule `!has(self.spec.dependsOn) || !(self.metadata.name in self.spec.dependsOn)` with message `"a <kind> cannot depend on itself"`. Apply the root XValidation on each of the four Kinds (Task/Plan/Milestone/Phase). Ensure Plan.Spec.DependsOn — which currently has NO CEL — gets the same item-MinLength + the root self-ref rule (WR-04 explicitly flagged Plan admits `[""]`). Then run `make manifests` to regenerate the four CRD bases. Do NOT change the field shape (still flat []string — D-01). Do NOT add provides/exposes.
  </action>
  <verify>
    <automated>make manifests && go build ./... && grep -lE 'x-kubernetes-validations' config/crd/bases/tideproject.k8s_plans.yaml && grep -c 'cannot depend on itself' config/crd/bases/tideproject.k8s_tasks.yaml config/crd/bases/tideproject.k8s_plans.yaml config/crd/bases/tideproject.k8s_milestones.yaml config/crd/bases/tideproject.k8s_phases.yaml</automated>
  </verify>
  <acceptance_criteria>
    - All four CRD bases contain an `x-kubernetes-validations` self-reference rule (`grep -c 'cannot depend on itself'` ≥ 1 each).
    - `config/crd/bases/tideproject.k8s_plans.yaml` now carries dependsOn validation (previously absent).
    - `make manifests` regenerates cleanly; `go build ./...` exits 0.
  </acceptance_criteria>
  <done>dependsOn empty-string + self-reference are rejected at admission with a clear message on all four Kinds, including Plan.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — Project reconciler watches Task so a fixed cycle clears the CycleDetected condition</name>
  <files>internal/controller/project_controller.go</files>
  <read_first>
    internal/controller/project_controller.go (SetupWithManager ~line 1546; checkGlobalCycleGate ~1492-1557), internal/owner/ (LabelProject = "tideproject.k8s/project"), internal/controller/wave_controller.go (taskToWaveMapper pattern for EnqueueRequestsFromMapFunc), 23-REVIEW.md (WR-02 detail)
  </read_first>
  <action>
    In ProjectReconciler.SetupWithManager add `.Watches(&tideprojectv1alpha2.Task{}, handler.EnqueueRequestsFromMapFunc(r.taskToProject))` (mirror the existing mapper idiom). Implement `taskToProject(ctx, obj client.Object) []reconcile.Request` that reads `obj.GetLabels()[owner.LabelProject]` and, if non-empty, returns a single reconcile.Request for that Project in the Task's namespace (empty slice otherwise). This makes a Task dependsOn edit re-run checkGlobalCycleGate so the CycleDetected condition clears when the cycle is broken. Keep it a watch (not Owns) — Tasks are not owned by Project. Do not change the gate logic itself.
  </action>
  <verify>
    <automated>go build ./internal/controller/... && grep -c 'taskToProject' internal/controller/project_controller.go && grep -c 'Watches(&tideprojectv1alpha2.Task' internal/controller/project_controller.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Watches(&tideprojectv1alpha2.Task' internal/controller/project_controller.go` ≥ 1 and `taskToProject` mapper present.
    - `go build ./internal/controller/...` exits 0.
    - Existing project_controller cycle test still passes (`go test ./internal/controller/... -run TestGlobalCycleDetection -count=1`).
  </acceptance_criteria>
  <done>A Task dependsOn change requeues its owning Project; the cycle-gate condition is no longer sticky.</done>
</task>

<task type="auto">
  <name>Task 3: WR-01 — scope the Wave observational roll-up by project (no cross-plan contamination)</name>
  <files>internal/controller/wave_controller.go</files>
  <read_first>
    internal/controller/wave_controller.go (reconcileObservational ~142-193; lists Tasks by wave-index label only), internal/owner/ (LabelProject), 23-REVIEW.md (WR-01 detail)
  </read_first>
  <action>
    In reconcileObservational, add `owner.LabelProject: wave.Spec.ProjectRef` to the `client.MatchingLabels` used to list Tasks, so the roll-up only counts Tasks of THIS Wave's project (still filtered by wave-index). This removes the namespace-wide cross-plan/cross-project contamination from bare wave-index matching. Keep the existing `// TODO(phase-24)` note (full Wave→Task global re-association is Phase 24); this is the safe interim scoping. Remove the now-unnecessary `_ = wave.Spec.ProjectRef` discard if present (the field is now genuinely used).
  </action>
  <verify>
    <automated>go build ./internal/controller/... && grep -c 'LabelProject' internal/controller/wave_controller.go && test -z "$(grep -n '_ = wave.Spec.ProjectRef' internal/controller/wave_controller.go)" && echo SCOPED</automated>
  </verify>
  <acceptance_criteria>
    - The Task list in reconcileObservational filters by `owner.LabelProject == wave.Spec.ProjectRef`.
    - No `_ = wave.Spec.ProjectRef` discard remains (field is used).
    - `go build ./internal/controller/...` exits 0; wave_controller tests pass (`go test ./internal/controller/... -run Wave -count=1`).
  </acceptance_criteria>
  <done>Wave roll-up is project-scoped; multi-plan namespaces no longer cross-contaminate Wave membership.</done>
</task>

<task type="auto">
  <name>Task 4: WR-03 — rename the misleading v1alpha1 import alias to v1alpha2 (single alias for the package); iterate to green</name>
  <files>broad consumer set (alias rename); verified by go build ./... && go vet ./...</files>
  <read_first>
    23-REVIEW.md (WR-03 detail), a sample migrated file (e.g. internal/controller/milestone_controller.go) to see the `tideprojectv1alpha1 ".../api/v1alpha2"` misleading-alias pattern, internal/controller/project_controller.go (already uses tidev1alpha2 alias — the target name)
  </read_first>
  <action>
    Rename the import alias tokens so api/v1alpha2 has ONE alias. In every file whose import path is `github.com/jsquirrelz/tide/api/v1alpha2` but whose alias token is still `tideprojectv1alpha1` (or `tidev1alpha1`), rename the alias to `tideprojectv1alpha2` (resp. `tidev1alpha2`) AND every usage of that token in that file. Approach: for each such file, (a) rename on the import line, (b) `sed`/replace the token across that file, (c) if the file ALSO already imports api/v1alpha2 under the target alias, DEDUPE to a single import line. EXCLUDE files that legitimately import `api/v1alpha1` (the still-existing types): `api/v1alpha1/*`, internal/controller/project_controller_v2_guard_test.go (old-object rejection fixture), and any file whose v1alpha1 import path is genuinely `.../api/v1alpha1`. Do NOT touch string literals or comments containing `v1alpha1` (GVK strings for rejection tests, the migration doc, the unservedversion markers). After the rename, run goimports/gofmt. Iterate `go build ./...` then `go vet ./...` to exit 0. There must be ZERO import lines aliasing `api/v1alpha2` as `tideprojectv1alpha1`/`tidev1alpha1`.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && test -z "$(grep -rnE '(tideprojectv1alpha1|tidev1alpha1) "github.com/jsquirrelz/tide/api/v1alpha2"' --include='*.go' .)" && echo SINGLE_ALIAS</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `(tideprojectv1alpha1|tidev1alpha1) "…/api/v1alpha2"` (no misleading alias remains).
    - Files importing the real `api/v1alpha1` (guard test, api/v1alpha1 defs) are untouched and still compile.
    - `go build ./... && go vet ./...` both exit 0.
  </acceptance_criteria>
  <done>api/v1alpha2 is imported under a single, accurate alias everywhere; the type-identity-confusion trap is gone.</done>
</task>

</tasks>

<verification>
Final green bar (run all, observe — do not assume): `go build ./...`=0, `go vet ./...`=0, `make verify-no-aggregates`=0, `make verify-dag-imports`=0, `make test`=0, `make test-int-fast`=0. No misleading v1alpha1→v1alpha2 alias remains; all four CRD bases carry dependsOn self-ref CEL; Project watches Task; Wave roll-up project-scoped.
</verification>
