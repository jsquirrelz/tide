---
phase: 23-schema-migration-cross-scope-dependency-model
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - cmd/manager/main.go
  - internal/webhook/v1alpha1/plan_webhook.go
  - internal/webhook/v1alpha2/plan_webhook.go
  - internal/webhook/v1alpha1/wave_webhook.go
  - internal/webhook/v1alpha2/wave_webhook.go
  - internal/controller/plan_controller.go
  - internal/controller/wave_controller.go
  - api/v1alpha1/wave_types.go
  - api/v1alpha1/task_types.go
  - api/v1alpha1/phase_types.go
  - api/v1alpha1/milestone_types.go
  - api/v1alpha1/project_types.go
  - api/v1alpha1/plan_conversion.go
  - config/webhook/manifests.yaml
  - config/crd/bases/tideproject.k8s_waves.yaml
  - config/crd/bases/tideproject.k8s_tasks.yaml
  - config/crd/bases/tideproject.k8s_plans.yaml
  - config/crd/bases/tideproject.k8s_phases.yaml
  - config/crd/bases/tideproject.k8s_milestones.yaml
  - config/crd/bases/tideproject.k8s_projects.yaml
  - docs/migration/v1alpha1-to-v1alpha2.md
autonomous: true
requirements: [SCHEMA-03, DEPS-01]

must_haves:
  truths:
    - "The manager binary registers v1alpha2 in its scheme and serves only v1alpha2 (v1alpha1 unserved)"
    - "The no-op conversion-webhook scaffolding (plan_conversion.go Hub()) is deleted and no CRD requests a conversion webhook"
    - "The D-B1 wave admission guard (only the reconciler may create Waves) is re-registered for v1alpha2.Wave"
    - "The per-plan cycle webhook filters cross-scope dependsOn entries to task-only edges so ComputeWaves never errors on an unknown node"
    - "plan_controller.materializeWaves and wave_controller compile against v1alpha2 WaveSpec (ProjectRef, no PlanRef) via a clearly-marked Phase-24 stub"
    - "A migration doc documents the breaking change, the version bump, and the CRD delete+reapply reinstall procedure"
  artifacts:
    - path: "docs/migration/v1alpha1-to-v1alpha2.md"
      provides: "SCHEMA-03 documented migration/reinstall path"
      min_lines: 25
    - path: "internal/webhook/v1alpha2/wave_webhook.go"
      provides: "D-B1 re-registered for v1alpha2.Wave (RESEARCH OQ#2)"
      contains: "v1alpha2"
    - path: "api/v1alpha1/plan_conversion.go"
      provides: "DELETED — Hub() stub removed (D-09)"
  key_links:
    - from: "cmd/manager/main.go"
      to: "api/v1alpha2"
      via: "AddToScheme"
      pattern: "v1alpha2.*AddToScheme"
    - from: "internal/controller/wave_controller.go"
      to: "v1alpha2.WaveSpec.ProjectRef"
      via: "Task listing stub"
      pattern: "ProjectRef"
---

<objective>
Wire the clean-break migration (D-09, SCHEMA-03): register the v1alpha2 scheme in the manager, mark v1alpha1 types unserved, delete the no-op conversion-webhook scaffolding, re-register the D-B1 Wave admission guard for v1alpha2.Wave, make the per-plan cycle webhook tolerate cross-scope dependsOn (task-only filtering), and stub the per-plan wave materialization + wave controller so the build stays green against the re-owned WaveSpec — with a clearly-marked Phase-24 TODO at every point the global derivation belongs. Ship a migration doc with the reinstall procedure.

This plan does NOT build the global wave-derivation engine, the bidirectional global wave index, or scope-dep fan-out — those are Phase 24. It changes ownership/serving and keeps everything compiling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-schema-migration-cross-scope-dependency-model/23-CONTEXT.md
@.planning/phases/23-schema-migration-cross-scope-dependency-model/23-RESEARCH.md
@.planning/phases/23-schema-migration-cross-scope-dependency-model/23-01-SUMMARY.md

<interfaces>
<!-- Exact wiring sites the executor edits. Reproduced so no codebase exploration is needed. -->

cmd/manager/main.go:
  line 42:  tidev1alpha1 "github.com/jsquirrelz/tide/api/v1alpha1"
  line 63:  webhookv1alpha1 "github.com/jsquirrelz/tide/internal/webhook/v1alpha1"
  line 287: utilruntime.Must(tidev1alpha1.AddToScheme(scheme))      ← ADD v1alpha2 alongside
  line 553: webhookv1alpha1.SetupPlanWebhookWithManager(mgr, defaultFileTouchMode)
  line 557: webhookv1alpha1.SetupWaveWebhookWithManager(mgr)        ← swap to v1alpha2 wave webhook
  line 561: webhookv1alpha1.SetupProjectWebhookWithManager(mgr)

internal/webhook/v1alpha1/wave_webhook.go — D-B1 guard to PORT to v1alpha2:
  SetupWaveWebhookWithManager(mgr) registers ctrl.NewWebhookManagedBy(mgr, &Wave{}).WithValidator(&WaveCustomValidator{}).
  ValidateCreate rejects when len(obj.GetOwnerReferences()) == 0:
    "wave %s/%s rejected per D-B1: client-applied Waves not allowed; the WaveReconciler is the sole producer"
  Marker: +kubebuilder:webhook:path=/validate-tideproject-k8s-v1alpha1-wave,...,versions=v1alpha1,name=vwave-v1alpha1.kb.io

internal/webhook/v1alpha1/plan_webhook.go:
  - SetupPlanWebhookWithManager(mgr, defaultMode) registers for &v1alpha1.Plan{}.
  - validate() (line 124) lists Tasks by .spec.planRef, then tasksToDAG (line 250) builds nodes=task.Name,
    edges=(dep, task.Name) for every dep in task.Spec.DependsOn, then calls dag.ComputeWaves.
  - PROBLEM (Pitfall 6): a cross-scope dep names a task NOT in this plan's node set → ComputeWaves returns
    "edge references unknown node: <dep>" (kahn.go:57), a non-cycle error, breaking admission.

internal/controller/plan_controller.go:
  - materializeWaves (line 1347-1408): builds Wave with Spec: WaveSpec{PlanRef: plan.Name, WaveIndex: i}
    and name fmt.Sprintf("tide-wave-%s-%d", plan.UID, i). Spec.PlanRef no longer exists in v1alpha2.
  - imports tideprojectv1alpha1 throughout; r.Create(ctx, &v1alpha1.Wave{...}).

internal/controller/wave_controller.go:
  - line 106-113: uses wave.Spec.PlanRef to Get the parent Plan for owner ref.
  - line 148-152: lists Tasks via client.MatchingFields{taskPlanRefIndexKey: wave.Spec.PlanRef}.
  - line 227-246 taskToWaveMapper: matches w.Spec.PlanRef == task.Spec.PlanRef.

pkg/dag/kahn.go: ComputeWaves(nodes []NodeID, edges []Edge); NodeID = string;
  returns "edge references unknown node: %s" when an edge endpoint is absent from nodes.

config/webhook/manifests.yaml: holds the generated ValidatingWebhookConfiguration entries
  for plan + wave (+project) webhooks, keyed by the +kubebuilder:webhook markers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register v1alpha2 scheme, retire v1alpha1 serving + conversion scaffolding, mark v1alpha1 unserved</name>
  <files>cmd/manager/main.go, api/v1alpha1/plan_conversion.go, api/v1alpha1/wave_types.go, api/v1alpha1/task_types.go, api/v1alpha1/phase_types.go, api/v1alpha1/milestone_types.go, api/v1alpha1/project_types.go, config/crd/bases/tideproject.k8s_waves.yaml, config/crd/bases/tideproject.k8s_tasks.yaml, config/crd/bases/tideproject.k8s_plans.yaml, config/crd/bases/tideproject.k8s_phases.yaml, config/crd/bases/tideproject.k8s_milestones.yaml, config/crd/bases/tideproject.k8s_projects.yaml</files>
  <read_first>
    cmd/manager/main.go (lines 280-300 scheme build, 545-565 webhook setup), api/v1alpha1/plan_conversion.go,
    api/v1alpha1/groupversion_info.go, api/v1alpha2/groupversion_info.go,
    api/v1alpha1/wave_types.go, api/v1alpha1/project_types.go
  </read_first>
  <action>
    In cmd/manager/main.go: add an import alias `tidev1alpha2 "github.com/jsquirrelz/tide/api/v1alpha2"` and add `utilruntime.Must(tidev1alpha2.AddToScheme(scheme))` adjacent to the existing v1alpha1 AddToScheme (line 287). Keep v1alpha1 AddToScheme registered so the scheme can decode any still-present v1alpha1 objects for the Plan-03 reinstall guard (do NOT remove v1alpha1 from the scheme — the guard needs to read old objects).

    Delete api/v1alpha1/plan_conversion.go entirely (removes `func (*Plan) Hub() {}` — D-09; with no served conversion webhook the Hub marker is dead).

    Add `+kubebuilder:unservedversion` to the root marker block of each v1alpha1 Kind (Wave, Task, Plan, Phase, Milestone, Project) so controller-gen emits `served: false` for v1alpha1. Place it alongside the existing `+kubebuilder:object:root=true` markers. Do NOT add `+kubebuilder:storageversion` to any v1alpha1 type (Plan-01 already removed it from Plan).

    Run `make manifests` to regenerate config/crd/bases/*.yaml. Confirm each CRD now shows v1alpha1 `served: false` and v1alpha2 `served: true, storage: true`, and that no CRD requests `conversion: strategy: Webhook`.
  </action>
  <verify>
    <automated>test ! -f api/v1alpha1/plan_conversion.go && make manifests && go build ./cmd/... && grep -rc 'strategy: Webhook' config/crd/bases/</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f api/v1alpha1/plan_conversion.go` succeeds (file deleted).
    - `go build ./cmd/...` exits 0 after the scheme change.
    - `grep -c 'tidev1alpha2' cmd/manager/main.go` ≥ 1 AND the v1alpha2 AddToScheme call is present.
    - `grep -rc 'strategy: Webhook' config/crd/bases/` returns 0.
    - For each CRD: `grep -B1 -A3 'name: v1alpha1' config/crd/bases/tideproject.k8s_plans.yaml | grep -c 'served: false'` returns 1.
    - `grep -c '+kubebuilder:unservedversion' api/v1alpha1/wave_types.go api/v1alpha1/project_types.go` ≥ 1 each.
  </acceptance_criteria>
  <done>Manager registers v1alpha2 (and still decodes v1alpha1 for the guard); v1alpha1 marked unserved across all six Kinds; conversion Hub() deleted; no CRD requests a conversion webhook.</done>
</task>

<task type="auto">
  <name>Task 2: Re-register D-B1 Wave webhook for v1alpha2, filter per-plan cycle webhook to task-only edges, stub wave materialization + wave controller against v1alpha2 WaveSpec</name>
  <files>internal/webhook/v1alpha2/wave_webhook.go, internal/webhook/v1alpha2/plan_webhook.go, internal/webhook/v1alpha1/wave_webhook.go, internal/webhook/v1alpha1/plan_webhook.go, internal/controller/plan_controller.go, internal/controller/wave_controller.go, cmd/manager/main.go, config/webhook/manifests.yaml</files>
  <read_first>
    internal/webhook/v1alpha1/wave_webhook.go, internal/webhook/v1alpha1/plan_webhook.go (validate + tasksToDAG, lines 124-262),
    internal/controller/plan_controller.go (materializeWaves, lines 1339-1408),
    internal/controller/wave_controller.go (lines 100-165, 220-250), pkg/dag/kahn.go (lines 46-65)
  </read_first>
  <action>
    Create internal/webhook/v1alpha2/wave_webhook.go porting the D-B1 guard (RESEARCH OQ#2): new package `v1alpha2`, import `tideprojectv1alpha2 "github.com/jsquirrelz/tide/api/v1alpha2"`, `SetupWaveWebhookWithManager(mgr)` registering `ctrl.NewWebhookManagedBy(mgr, &tideprojectv1alpha2.Wave{}).WithValidator(&WaveCustomValidator{})`, with the same ValidateCreate rejection on `len(obj.GetOwnerReferences()) == 0` and the same D-B1 message. Update the `+kubebuilder:webhook` marker path/versions/name to v1alpha2 (path `/validate-tideproject-k8s-v1alpha2-wave`, `versions=v1alpha2`, `name=vwave-v1alpha2.kb.io`). Delete internal/webhook/v1alpha1/wave_webhook.go (its v1alpha1 marker would route to an unserved version). In cmd/manager/main.go swap the wave webhook setup call to the new `webhookv1alpha2.SetupWaveWebhookWithManager(mgr)` (add the `webhookv1alpha2` import alias).

    In internal/webhook/v1alpha1/plan_webhook.go (Pitfall 6): the plan webhook stays on v1alpha1.Plan for now is NOT viable since v1alpha1 is unserved — re-point it to v1alpha2.Plan (update import alias usage, the `+kubebuilder:webhook` marker to v1alpha2 path/versions/name, the validator's `*Plan` type params, and the `.spec.planRef` Task list to v1alpha2.TaskList). In `tasksToDAG` (or before the ComputeWaves call in `validate`): build the per-plan node set first, then include an edge ONLY when the dep name is in that node set — drop cross-scope dep names (a dep naming a task in another plan, or a scope node) from the per-plan DAG so ComputeWaves never returns "edge references unknown node". Add a code comment: cross-scope cycles are caught by the global gate in the Project reconciler (Plan 03, DEPS-03); the per-plan webhook validates within-plan edges only. LOCKED (do not leave as an executor fork): MOVE the file to internal/webhook/v1alpha2/plan_webhook.go (package `v1alpha2`) and DELETE internal/webhook/v1alpha1/plan_webhook.go — keeping webhook packages version-consistent with wave_webhook.go and matching the path plan 23-03 references. Update the main.go plan-webhook setup call + import alias to the v1alpha2 package and keep all package imports consistent.

    In internal/controller/plan_controller.go materializeWaves: change the constructed Wave to v1alpha2 and `Spec: tideprojectv1alpha2.WaveSpec{ProjectRef: <resolved project name>, WaveIndex: i}` (resolve the project name via the existing r.resolveProjectName helper already used a few lines above for the metric label). Keep the deterministic name but switch to the global-naming shape with a Phase-24 TODO: leave the name `tide-wave-%s-%d` keyed on plan.UID for now and add `// TODO(phase-24): replace per-plan materializeWaves with global derivation; name becomes tide-wave-<project.UID>-<globalIndex> and ownership moves to Project`. Update every `tideprojectv1alpha1` reference in this file's Wave-touching paths to v1alpha2 as needed to compile. Do NOT implement global derivation.

    In internal/controller/wave_controller.go: replace `wave.Spec.PlanRef` reads (owner-ref Get at line 106-113, Task list at line 148-152, taskToWaveMapper at line 227-246) so the file compiles against v1alpha2.WaveSpec which has ProjectRef not PlanRef. Minimal stub: gate the observational Task-listing block behind a `// TODO(phase-24): re-wire Wave→Task association off the global wave index; ProjectRef-scoped listing lands with the global scheduler` and either (a) list Tasks by `client.InNamespace + MatchingLabels{owner.LabelProject: wave.Spec.ProjectRef}` if a project-scoped listing compiles cleanly, or (b) short-circuit the observational roll-up to a no-op that still sets the Ready condition. Keep the reconciler compiling and its existing Ready-condition write intact. Do NOT build global dispatch.

    Run `make manifests` so config/webhook/manifests.yaml regenerates with the v1alpha2 webhook paths and drops the v1alpha1 wave webhook entry (Pitfall 2 — no stale webhook routing to an unserved version).
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/webhook/... ./internal/controller/... && grep -c 'v1alpha2-wave' config/webhook/manifests.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 (whole tree compiles against v1alpha2 WaveSpec).
    - `internal/webhook/v1alpha2/wave_webhook.go` exists and `grep -c 'sole producer' internal/webhook/v1alpha2/wave_webhook.go` ≥ 1 (D-B1 ported).
    - `grep -rc 'versions=v1alpha1' internal/webhook/` returns 0 (no webhook targets the unserved version).
    - `grep -c 'TODO(phase-24)' internal/controller/plan_controller.go internal/controller/wave_controller.go` ≥ 1 each (stub boundary marked).
    - `grep -c 'ProjectRef' internal/controller/wave_controller.go` ≥ 1 AND `grep -c 'wave.Spec.PlanRef' internal/controller/wave_controller.go` returns 0.
    - The plan webhook moved cleanly: `test -f internal/webhook/v1alpha2/plan_webhook.go` AND `test ! -f internal/webhook/v1alpha1/plan_webhook.go` (no fork left behind).
    - The per-plan cycle webhook: a unit or build-level assertion that a Task with a cross-scope dep does not produce an "unknown node" error — `grep -cE 'within-plan|cross-scope|task-only' internal/webhook/v1alpha2/plan_webhook.go` ≥ 1 (filtering comment present) AND `go test ./internal/webhook/... -count=1` exits 0.
  </acceptance_criteria>
  <done>D-B1 guard runs on v1alpha2.Wave; per-plan cycle webhook filters cross-scope deps to task-only edges; materializeWaves + wave_controller compile against v1alpha2 WaveSpec behind explicit Phase-24 stubs; webhook manifests carry no v1alpha1 routing.</done>
</task>

<task type="auto">
  <name>Task 3: Write the v1alpha1→v1alpha2 migration doc (SCHEMA-03)</name>
  <files>docs/migration/v1alpha1-to-v1alpha2.md</files>
  <read_first>
    docs/INSTALL.md, docs/releasing.md, .planning/phases/23-schema-migration-cross-scope-dependency-model/23-RESEARCH.md (Runtime State Inventory + Pattern 1)
  </read_first>
  <action>
    Author docs/migration/v1alpha1-to-v1alpha2.md documenting the breaking CRD change. Cover, as explicit sections: (1) what changed and why — Wave ownership Plan→Project with a global WaveIndex, `dependsOn` broadened to any-level on every level, conversion webhook retired (cite D-07, D-02, D-09); (2) the version bump — note this is a breaking CRD change requiring a chart/appVersion bump per the project release recipe, and that values.yaml is the FIXED contract (do not instruct editing it); (3) the reinstall procedure — because v1alpha1 is unserved and there is no conversion webhook, in-flight v1alpha1 objects must be removed and re-applied under v1alpha2: `kubectl delete project <name>` (owner-ref cascade removes child Milestones/Phases/Plans/Tasks/Waves), apply the new CRDs, re-apply the Project YAML carrying the new required `schemaRevision: v1alpha2` field; (4) the fail-closed safety net — any surviving v1alpha1-shape object is rejected by the controller with a `RequiresReinstall` status condition (forward-reference Plan 03) and never silently runs, satisfying SCHEMA-03's no-silent-corruption guarantee; (5) the dogfood-cluster note — kind-tide-dogfood holds v1alpha1 objects; the clean delete+reapply avoids stranded etcd objects (sidesteps the conversion-strategy ambiguity in RESEARCH assumptions A1/A2). State that NO automated data-migration tooling ships (pre-GA, dogfood-only).
  </action>
  <verify>
    <automated>test -f docs/migration/v1alpha1-to-v1alpha2.md && grep -cE 'schemaRevision|kubectl delete project|RequiresReinstall' docs/migration/v1alpha1-to-v1alpha2.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/migration/v1alpha1-to-v1alpha2.md exists and is ≥ 25 lines.
    - `grep -cE 'kubectl delete project' docs/migration/v1alpha1-to-v1alpha2.md` ≥ 1 (reinstall step present).
    - `grep -cE 'schemaRevision' docs/migration/v1alpha1-to-v1alpha2.md` ≥ 1 (discriminator field named).
    - `grep -cE 'RequiresReinstall' docs/migration/v1alpha1-to-v1alpha2.md` ≥ 1 (fail-closed net referenced).
    - `grep -ciE 'version bump|appVersion' docs/migration/v1alpha1-to-v1alpha2.md` ≥ 1 (version bump documented).
  </acceptance_criteria>
  <done>A migration doc names the breaking change, the version bump, the CRD delete+reapply reinstall procedure, and the fail-closed safety net — satisfying SCHEMA-03's documented-migration requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kubectl/client → Wave admission webhook | Untrusted client-applied Wave creation crosses here (D-B1) |
| etcd (stale v1alpha1 object) → controller read path | An object persisted under the old schema crosses into the reconciler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Spoofing/Elevation | client-applied Wave object | mitigate | D-B1 webhook re-registered for v1alpha2.Wave rejects any Wave lacking a reconciler-stamped owner-ref; only the WaveReconciler is the sole producer |
| T-23-05 | Tampering | stale v1alpha1 webhook routing after version cutover | mitigate | v1alpha1 wave webhook deleted + manifests regenerated so no ValidatingWebhookConfiguration routes to an unserved version (Pitfall 2); acceptance greps `versions=v1alpha1` → 0 |
| T-23-06 | Denial of Service | cross-scope dep producing "unknown node" admission failure (failurePolicy=fail blocks all Plan applies) | mitigate | per-plan cycle webhook filters cross-scope deps to task-only edges so ComputeWaves never errors on a foreign node |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH audit N/A) |
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` exit 0 against v1alpha2 WaveSpec.
- `make manifests` regenerates CRDs (v1alpha1 unserved) and webhook manifests (no v1alpha1 wave routing).
- `go test ./internal/webhook/... ./internal/controller/... -count=1` exits 0.
- Migration doc exists with reinstall + version-bump + fail-closed sections.
- `make verify-no-aggregates verify-dag-imports` stay green.
</verification>

<success_criteria>
- Manager registers v1alpha2 scheme; v1alpha1 unserved; conversion Hub() deleted (D-09).
- D-B1 Wave guard re-registered for v1alpha2 (RESEARCH OQ#2).
- Per-plan cycle webhook tolerates cross-scope deps (task-only edges).
- materializeWaves + wave_controller compile against v1alpha2 WaveSpec behind Phase-24 stubs (no global derivation built).
- Migration doc satisfies SCHEMA-03.
</success_criteria>

<output>
Create `.planning/phases/23-schema-migration-cross-scope-dependency-model/23-02-SUMMARY.md` when done.
</output>
