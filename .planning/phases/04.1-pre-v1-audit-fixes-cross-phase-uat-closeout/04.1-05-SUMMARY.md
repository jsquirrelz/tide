---
phase: "04.1"
plan: "05"
subsystem: "dispatch/controller"
tags: [p1-production-wiring, planner-jobs, dispatch-contract, kind-discriminator, biggest-fix]
dependency_graph:
  requires: ["04.1-03", "04.1-04"]
  provides: ["P1.2-kind-discriminator", "planner-dispatch-contract"]
  affects: ["milestone_controller", "phase_controller", "plan_controller", "task_controller", "podjob"]
tech_stack:
  added: ["JobKind discriminator (JobKindExecutor/JobKindPlanner)", "PlannerJobName()"]
  patterns: ["K8s 1.33 native sidecar (RestartPolicy=Always)", "subPath isolation", "credproxy.Sign for planner tokens"]
key_files:
  created:
    - test/integration/envtest/planner_dispatch_test.go
  modified:
    - internal/dispatch/podjob/jobspec.go
    - internal/dispatch/podjob/jobspec_test.go
    - internal/dispatch/podjob/names.go
    - internal/controller/milestone_controller.go
    - internal/controller/phase_controller.go
    - internal/controller/plan_controller.go
    - internal/controller/task_controller.go
    - internal/controller/milestone_controller_test.go
    - internal/controller/phase_controller_test.go
    - internal/controller/plan_planner_test.go
    - internal/controller/plan_controller_test.go
    - internal/controller/boundary_push_test.go
    - internal/controller/milestone_gates_test.go
    - internal/controller/phase_gates_test.go
    - internal/controller/plan_gates_test.go
    - test/integration/envtest/suite_test.go
    - cmd/manager/main.go
  deleted:
    - internal/controller/planner_job_helpers.go
decisions:
  - "subPath fallback: empty ProjectUID uses parentUID as prefix to satisfy K8s relative-path requirement"
  - "nil-Project guard in BuildJobSpec: Job Namespace falls back to ParentObj.GetNamespace(); credproxy EnvFrom skips provider secret when Project is nil"
  - "JobKind backward compat: empty Kind defaults to JobKindExecutor so pre-P1.2 callers continue to work"
metrics:
  duration: "~80 minutes (including context-window continuation)"
  completed: "2026-05-20"
  tasks_completed: 1
  files_changed: 19
---

# Phase 04.1 Plan 05: P1.2 — BuildJobSpec JobKind Discriminator + Planner Dispatch Contract Summary

Land P1.2 — the largest audit fix in Phase 04.1: factor `BuildJobSpec` with a `JobKind` discriminator so planner Jobs (Milestone/Phase/Plan) share the full Phase 2 executor dispatch contract (PVC subPath isolation, envelope-writer init, credproxy native sidecar, signed-token env, 3 SecurityContexts, bounded deadline), replacing the skeletal 14-line `batchv1Template` that had been silently discarding the `EnvelopeIn` payload with `_ = envInJSON`.

## What Was Built

**`internal/dispatch/podjob/jobspec.go`** — Extended `BuildOptions` with `Kind JobKind`, `ParentObj metav1.Object`, `Level string`, `Caps *tidev1alpha1.Caps`. `BuildJobSpec` now branches on `Kind` for name+labels only; the PodSpec body (envelope-writer init + credproxy native sidecar + subagent container with signed-token env + PVC subPath + 3 SecurityContexts + ActiveDeadlineSeconds via DefaultCaps) is Kind-invariant.

**`internal/dispatch/podjob/names.go`** — Added `PlannerJobName(level, parentUID, attempt) string` returning `tide-<level>-<uid>-<attempt>`.

**`internal/controller/planner_job_helpers.go`** — DELETED. The `batchv1Template` skeletal helper is replaced by `podjob.BuildJobSpec(Kind=JobKindPlanner, ...)`.

**Three up-stack reconcilers** (`milestone_controller.go`, `phase_controller.go`, `plan_controller.go`) — replaced `_ = envInJSON` discard + skeletal `buildPlannerJob` with:
1. `podjob.DefaultCaps(nil, JobKindPlanner)` → 600s/20iters floor
2. `credproxy.Sign(r.SigningKey, string(parent.UID), validity)` → mint token
3. `podjob.BuildJobSpec(BuildOptions{Kind: JobKindPlanner, ParentObj: parent, Level: "...", ...})` → full contract

**`cmd/manager/main.go`** — Wired `SigningKey`, `CredproxyImage`, `SubagentImage` into all three planner reconcilers.

**`test/integration/envtest/planner_dispatch_test.go`** (250 lines) — Layer A envtest asserting Milestone planner Job has: 2 init containers (envelope-writer + credproxy with RestartPolicy=Always), 1 subagent container with ANTHROPIC_API_KEY env, PVC subPath with no leading slash, ActiveDeadlineSeconds >= 660, correct `role=planner`/`level=milestone` labels.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] nil-Project panic in BuildJobSpec**
- **Found during:** Task 1 — test fixture exercise
- **Issue:** `opts.Project.Spec.ProviderSecretRef` and `opts.Project.Namespace` panicked when `Project == nil` (planner Jobs dispatched before Project chain resolves — Plan-level reconciler can have no Project)
- **Fix:** Added nil guard around `opts.Project` for credproxy EnvFrom and Job Namespace; Namespace falls back to `opts.ParentObj.GetNamespace()`
- **Files modified:** `internal/dispatch/podjob/jobspec.go`
- **Commit:** f30b72a

**2. [Rule 1 - Bug] Absolute subPath rejection by K8s API**
- **Found during:** Task 1 — TestPlanReconciler_NoOpUntilValidated and Test 5 (plan_planner_test.go)
- **Issue:** When `opts.ProjectUID == ""`, `subPath = "" + "/workspace"` = `"/workspace"` — K8s rejects absolute subPaths with 422
- **Fix:** When `ProjectUID == ""`, fall back to `parentUID` as subPath prefix so the path is always relative
- **Files modified:** `internal/dispatch/podjob/jobspec.go`
- **Commit:** f30b72a

**3. [Rule 2 - Missing Critical Functionality] Test reconciler instances missing SigningKey/CredproxyImage**
- **Found during:** Controller test suite run (88 "signing key empty" failures)
- **Issue:** All controller test files constructing MilestoneReconciler, PhaseReconciler, PlanReconciler were missing `SigningKey` and `CredproxyImage` — `credproxy.Sign` returns an error when key is empty
- **Fix:** Updated 7 test files + `newPlanReconciler()` factory function + `newPhase2ReconcilersForTest()` in integration envtest suite
- **Files modified:** boundary_push_test.go, milestone_controller_test.go, phase_controller_test.go, plan_planner_test.go, plan_controller_test.go, milestone_gates_test.go, phase_gates_test.go, plan_gates_test.go, test/integration/envtest/suite_test.go
- **Commit:** f30b72a

## Test Results

- `go test ./internal/dispatch/podjob/... -count=1` — 27 tests pass (18 existing + 9 new planner tests)
- `go test ./internal/controller/... -count=1` — 90/90 pass
- `go build ./...` — clean
- `go vet ./...` — clean
- `make tide-lint` — clean

## Self-Check

- [x] `internal/dispatch/podjob/jobspec.go` exists with `type JobKind string` and `BuildJobSpec` branching
- [x] `internal/dispatch/podjob/names.go` exists with `PlannerJobName`
- [x] `internal/controller/planner_job_helpers.go` deleted
- [x] `test/integration/envtest/planner_dispatch_test.go` created (250 lines)
- [x] Commit `f30b72a` exists on `worktree-agent-afb364feb1e39fc7e`
