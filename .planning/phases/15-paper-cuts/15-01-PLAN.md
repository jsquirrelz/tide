---
phase: 15-paper-cuts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/owner/label.go
  - internal/owner/label_test.go
  - internal/reporter/materialize.go
  - internal/reporter/materialize_test.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/milestone_controller_test.go
  - internal/controller/phase_controller_test.go
  - internal/gates/boundary.go
  - cmd/tide/approve_test.go
autonomous: true
requirements: [CUTS-01]
must_haves:
  truths:
    - "Reporter-created Milestone and Phase CRs carry the tideproject.k8s/project label at creation"
    - "tide approve discovers a gated reporter-created level on the FIRST call (no 'no level awaiting approval' false negative)"
    - "An existing unlabeled Milestone/Phase CR observed by its reconciler gets the project label backfilled from the OwnerRef chain, idempotently"
  artifacts:
    - path: "internal/owner/label.go"
      provides: "StampProjectLabel shared helper + LabelProject constant"
      exports: ["StampProjectLabel", "LabelProject"]
    - path: "internal/reporter/materialize.go"
      provides: "Label stamping at the reporter create site"
      contains: "StampProjectLabel"
    - path: "internal/controller/milestone_controller.go"
      provides: "Project-label backfill in MilestoneReconciler"
    - path: "internal/controller/phase_controller.go"
      provides: "Project-label backfill in PhaseReconciler"
  key_links:
    - from: "internal/reporter/materialize.go"
      to: "internal/owner/label.go"
      via: "owner.StampProjectLabel call before c.Create"
      pattern: "owner\\.StampProjectLabel"
    - from: "cmd/tide/approve.go"
      to: "tideproject.k8s/project label"
      via: "label-filtered discovery (unchanged per D-02)"
      pattern: "tideproject.k8s/project"
---

<objective>
Close CUTS-01 (run-1 finding 6): reporter-created Milestone/Phase CRs carried zero labels, so `tide approve` label-filtered discovery reported "no level awaiting approval" despite a parked CR. Implement D-01 universal label stamping via a shared helper, D-03 reconciler backfill for existing unlabeled CRs, and pin the run-1 symptom with regression tests. `tide approve` discovery itself is UNCHANGED (D-02 — no OwnerRef fallback). Project label only (D-04 — no role/level label parity).

Purpose: labels become reliable at creation; one source of truth for level discovery.
Output: shared `StampProjectLabel` helper, stamped reporter create site, self-healing backfill in two reconcilers, regression tests reproducing finding 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-paper-cuts/15-CONTEXT.md
@.planning/phases/15-paper-cuts/15-PATTERNS.md
@.planning/phases/15-paper-cuts/15-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StampProjectLabel helper + reporter create-site stamping (D-01)</name>
  <files>internal/owner/label.go, internal/owner/label_test.go, internal/reporter/materialize.go, internal/reporter/materialize_test.go</files>
  <read_first>
    - internal/owner/owner.go (Apache header, package doc style, EnsureOwnerRef nil-guard pattern — the helper mirrors this seam)
    - internal/reporter/materialize.go (MaterializeChildCRDs flow: stampParentRef → EnsureOwnerRef → c.Create; the insertion point is between stampParentRef and EnsureOwnerRef, around lines 252-263)
    - internal/reporter/materialize_test.go (existing test harness/fixtures to extend)
    - cmd/tide/inspect_wave_run.go lines 37-40 (existing labelProject const value to mirror)
    - .planning/phases/15-paper-cuts/15-PATTERNS.md section "internal/owner/label.go" (exact helper shape)
  </read_first>
  <behavior>
    - Test 1: StampProjectLabel on an object with nil labels map creates the map and sets tideproject.k8s/project=projectName
    - Test 2: StampProjectLabel with empty projectName is a no-op (fail-open — object labels untouched)
    - Test 3: StampProjectLabel overwrites a pre-existing (LLM-authored) tideproject.k8s/project value with the authoritative parent value
    - Test 4 (run-1 finding-6 regression): a Milestone/Phase created via MaterializeChildCRDs from a parent labeled tideproject.k8s/project=demo carries labels["tideproject.k8s/project"]=="demo" after create
    - Test 5: MaterializeChildCRDs from a parent WITHOUT the label still creates the child (fail-open, label absent)
  </behavior>
  <action>
    Create internal/owner/label.go in package owner: exported const LabelProject = "tideproject.k8s/project" and func StampProjectLabel(obj metav1.Object, projectName string). Semantics: no-op when projectName is empty; lazily init the labels map; set labels[LabelProject] = projectName (overwriting any prior value — Tampering mitigation: LLM-authored labels on child CRDs are ignored, same doctrine as stampParentRef); obj.SetLabels. Use the Apache 2.0 header and doc-comment voice from owner.go.

    In internal/reporter/materialize.go, inside MaterializeChildCRDs, after the stampParentRef(obj, parent.GetName()) call and before owner.EnsureOwnerRef, add: owner.StampProjectLabel(obj, parent.GetLabels()[owner.LabelProject]). The parent (Project/Milestone/Phase/Plan) carries the label; missing parent label fails open per the helper's empty-string no-op (RESEARCH Pitfall 1).

    Write the behavior tests above in internal/owner/label_test.go (table-driven, matching owner_test.go style) and extend internal/reporter/materialize_test.go with the finding-6 regression assertion.
  </action>
  <verify>
    <automated>go test ./internal/owner/... ./internal/reporter/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func StampProjectLabel" internal/owner/label.go` returns exactly 1 match
    - `grep -n "owner.StampProjectLabel" internal/reporter/materialize.go` returns exactly 1 match, positioned before the owner.EnsureOwnerRef call in MaterializeChildCRDs
    - `go test ./internal/owner/... ./internal/reporter/...` exits 0
    - materialize_test.go contains an assertion that a created child's labels include "tideproject.k8s/project" (the finding-6 regression)
  </acceptance_criteria>
  <done>StampProjectLabel exists with nil-guard/no-op/overwrite semantics; reporter create site stamps the label; all five behaviors green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconciler backfill for existing unlabeled CRs (D-03)</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/milestone_controller_test.go, internal/controller/phase_controller_test.go</files>
  <read_first>
    - internal/controller/plan_controller.go lines ~1280-1320 (stampTaskLabels label-presence guard + MergeFrom patch idiom, and resolveProjectName OwnerRef-chain walk — the backfill copies both patterns)
    - internal/controller/milestone_controller.go (current Reconcile flow — find the early observation point to insert the backfill)
    - internal/controller/phase_controller.go (same; note the AwaitingApproval early-return at :197-233 — backfill must run BEFORE any early-return so parked CRs also self-heal)
    - internal/controller/milestone_controller_test.go and phase_controller_test.go (envtest suite conventions)
  </read_first>
  <action>
    In MilestoneReconciler.Reconcile and PhaseReconciler.Reconcile, early in the loop (after the Get, before status-transition logic and before any early-return such as the Phase AwaitingApproval park), backfill the project label on the RECONCILED OBJECT ITSELF when labels[owner.LabelProject] is empty: derive the project name by walking the OwnerRef chain (Milestone → owner Project via a Get; Phase → owner Milestone → owner Project, max 2 Gets), then apply a metadata-only client.MergeFrom patch setting labels[owner.LabelProject]. Guard: ONLY patch when the label is missing (RESEARCH Pitfall 2 — the presence guard breaks the reconcile-retrigger loop; second reconcile must be a no-op). If the chain cannot resolve a Project (orphan), skip silently — do not error the reconcile. Place the chain-walk in a small shared unexported helper if both reconcilers can reuse it; otherwise duplicate the 2-Get walk per controller, matching each file's local conventions.

    Envtest specs (one per controller): create a Project, then a Milestone/Phase WITH OwnerRef to its parent but WITHOUT the project label (simulating a v1.0.0/run-1 CR); reconcile; assert labels["tideproject.k8s/project"] equals the Project name. Then reconcile again and assert ResourceVersion of the object is unchanged by the backfill path (idempotency — no second patch).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run "TestControllers" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Both milestone_controller.go and phase_controller.go contain a backfill guarded by a missing-label check (grep for LabelProject in both files returns >= 1 match each)
    - Envtest spec: unlabeled Milestone with OwnerRef → after reconcile, label == project name
    - Envtest spec: unlabeled Phase with OwnerRef chain → after reconcile, label == project name
    - Idempotency assertion present: second reconcile performs no metadata patch
    - `go test ./internal/controller/...` exits 0
  </acceptance_criteria>
  <done>Both reconcilers self-heal missing project labels from the OwnerRef chain, idempotently, with envtest coverage.</done>
</task>

<task type="auto">
  <name>Task 3: approve-discovery regression + boundary.go comment revisit</name>
  <files>cmd/tide/approve_test.go, internal/gates/boundary.go</files>
  <read_first>
    - cmd/tide/approve.go lines 196-243 and 301-310 (findAwaiting* label-filtered discovery — UNCHANGED per D-02; the test exercises it as-is)
    - cmd/tide/approve_test.go (existing fake-client test harness to extend)
    - internal/gates/boundary.go lines ~40-60 (the comment documenting "only Plan→Task stamps the label today" + OwnerRef-walk workaround — now stale after D-01)
  </read_first>
  <action>
    Add the CUTS-01 end-to-end regression to cmd/tide/approve_test.go: (a) symptom case — a Milestone at AwaitingApproval WITHOUT the tideproject.k8s/project label is NOT discovered (reproduces run-1 finding 6: "no level awaiting approval" despite a parked CR); (b) fix case — the same Milestone WITH the label (as MaterializeChildCRDs now stamps it) IS discovered on the first call. Use the existing fake-client/table style in approve_test.go. Do NOT modify approve.go — D-02 locks discovery as label-filter-only.

    In internal/gates/boundary.go, update the ~:40-60 comment block that documents the systemic label inconsistency: note that as of Phase 15 (CUTS-01/D-01) every child CR create site stamps tideproject.k8s/project via owner.StampProjectLabel, and the OwnerRef-walk remains only as a defensive fallback for pre-Phase-15 CRs pending backfill. Comment-only change — do not alter boundary.go behavior.
  </action>
  <verify>
    <automated>go test ./cmd/tide/... -run "Approve" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - approve_test.go contains both the unlabeled-not-found (symptom) and labeled-found-first-call (fix) cases
    - `git diff internal/gates/boundary.go` shows comment-line changes only (no statement changes)
    - `go test ./cmd/tide/...` exits 0
    - approve.go is NOT modified (`git status --porcelain cmd/tide/approve.go` is empty)
  </acceptance_criteria>
  <done>Finding-6 symptom and fix are both pinned in approve_test.go; boundary.go comment reflects universal stamping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-authored child CRD specs → reporter materialize | Untrusted LLM output crosses into K8s object metadata |
| Reconciler → CR metadata writes | Backfill patches labels on observed cluster objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | StampProjectLabel (label injection via reporter LLM output) | mitigate | helper OVERWRITES from the authoritative parent label; LLM-authored project labels are never trusted (same pattern as stampParentRef) |
| T-15-02 | Elevation of Privilege | Reconciler backfill patch | mitigate | metadata-only MergeFrom patch, label key fixed to LabelProject constant, value derived solely from the OwnerRef chain Get results — no user input in the patch |
| T-15-03 | Denial of Service | Backfill reconcile loop | mitigate | missing-label presence guard makes the patch one-shot; second reconcile is a no-op (Pitfall 2) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `go test ./internal/owner/... ./internal/reporter/... ./internal/controller/... ./cmd/tide/...` exits 0
- `gofmt -l internal/owner internal/reporter internal/controller cmd/tide` prints nothing
- Run-1 finding-6 symptom is asserted (unlabeled CR not discovered) AND the fix is asserted (labeled CR discovered first call)
</verification>

<success_criteria>
- Reporter-created Milestone/Phase CRs carry tideproject.k8s/project at creation (ROADMAP SC1)
- Backfill self-heals existing unlabeled CRs idempotently (D-03)
- tide approve discovery unchanged and now reliable (D-02)
- Regression tests reproduce the run-1 symptom per the milestone-wide rule
</success_criteria>

<output>
Create `.planning/phases/15-paper-cuts/15-01-SUMMARY.md` when done
</output>
