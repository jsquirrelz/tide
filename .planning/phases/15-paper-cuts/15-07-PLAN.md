---
phase: 15-paper-cuts
plan: 07
type: execute
wave: 2
depends_on: [15-05, 15-06]
files_modified:
  - dashboard/web/src/components/RunningWavesView.tsx
  - dashboard/web/src/components/__tests__/RunningWavesView.test.tsx
  - dashboard/web/src/components/EmptyState.tsx
  - dashboard/web/src/lib/sse.ts
  - dashboard/web/src/App.tsx
  - dashboard/web/src/App.test.tsx
autonomous: true
requirements: [CUTS-06]
must_haves:
  truths:
    - "With no plan selected, the right pane shows the aggregate all-running-waves view (the old 'Select a plan to view its execution DAG' empty state is GONE)"
    - "Each wave card shows plan name, WAVE N · x/y running label, and one StatusBadge chip per task; clicking (or Enter) a card selects that plan and swaps to its ExecutionDAGView"
    - "An 'All waves' button in the EXECUTION pane header returns from a selected plan to the aggregate and clears the URL hash"
    - "waves: [] renders the No running waves empty state; pre-snapshot renders a spinner; malformed events are ignored keeping last good state"
  artifacts:
    - path: "dashboard/web/src/components/RunningWavesView.tsx"
      provides: "Aggregate wave-card view consuming waves.snapshot SSE events"
      contains: "waves.snapshot"
    - path: "dashboard/web/src/lib/sse.ts"
      provides: "waves.snapshot registered in SSE_PROJECT_EVENT_TYPES"
      contains: "waves.snapshot"
    - path: "dashboard/web/src/App.tsx"
      provides: "Right-pane default swap (D-13) + All waves return affordance"
  key_links:
    - from: "dashboard/web/src/components/RunningWavesView.tsx"
      to: "dashboard/web/src/components/StatusBadge.tsx"
      via: "task chips + KNOWN_STATUS_VALUES coerce (from plan 15-05)"
      pattern: "StatusBadge"
    - from: "dashboard/web/src/App.tsx"
      to: "dashboard/web/src/components/RunningWavesView.tsx"
      via: "right-pane default branch when selectedPlan === null"
      pattern: "RunningWavesView"
    - from: "dashboard/web/src/lib/sse.ts"
      to: "cmd/dashboard/api waves.snapshot emission (plan 15-06)"
      via: "named addEventListener registration"
      pattern: "waves\\.snapshot"
---

<objective>
Frontend half of CUTS-06: the right pane's empty state becomes the aggregate "all running waves" view (D-13), with rich wave cards (D-14) and click-through navigation (D-16), consuming the `waves.snapshot` SSE events shipped by plan 15-06. Every visual and interaction detail is locked by the approved 15-UI-SPEC (C3, C4, Copywriting Contract) — implement it verbatim, including the build-blocking sse.ts event-type registration.

Purpose: operators see what is running RIGHT NOW across all plans without clicking through each one (run-1 user feature 2).
Output: RunningWavesView component, no-running-waves EmptyState variant, sse.ts registration, App.tsx pane swap + All waves return affordance, Vitest coverage per the UI-SPEC Validation Contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-paper-cuts/15-UI-SPEC.md
@.planning/phases/15-paper-cuts/15-PATTERNS.md

<interfaces>
<!-- Wire contract from plan 15-06 (server) — the client renders payload order, never re-derives (D-15): -->
waves.snapshot event data:
{ "waves": [ { "planName": string, "waveIndex": number, "tasks": [ { "name": string, "status": string } ] } ] }
Zero running waves => "waves": [] (never null). Sort: plan name asc, wave index asc, task name asc (server-side).

From plan 15-05 (StatusBadge.tsx):
export type StatusValue (11 values incl. "Complete")
export const KNOWN_STATUS_VALUES: readonly StatusValue[]  // derived from STATUS_TABLE keys
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunningWavesView component + EmptyState variant + sse.ts registration (UI-SPEC C3)</name>
  <files>dashboard/web/src/components/RunningWavesView.tsx, dashboard/web/src/components/EmptyState.tsx, dashboard/web/src/lib/sse.ts</files>
  <read_first>
    - .planning/phases/15-paper-cuts/15-UI-SPEC.md §C3 (THE contract: props, card anatomy ASCII, semantics, view states table, data path, copy) and §Copywriting Contract
    - dashboard/web/src/components/PlanningDAGView.tsx (the analog: useSSEStream subscription, snapshot-replace state, defensive JSON parse guard, coerce usage)
    - dashboard/web/src/lib/sse.ts (SSE_PROJECT_EVENT_TYPES flatMap construction at ~:80 — waves.snapshot does NOT fit the kind×action generator; append it explicitly, e.g. via concat/spread after the flatMap; useSSEStream options)
    - dashboard/web/src/components/EmptyState.tsx (variant switch shape — add the new variant following E2/E3 anatomy)
    - dashboard/web/src/components/StatusBadge.tsx as landed by plan 15-05 (KNOWN_STATUS_VALUES export to coerce chip statuses)
  </read_first>
  <action>
    sse.ts: append "waves.snapshot" to SSE_PROJECT_EVENT_TYPES — it is named outside the `<kind>.<action>` generator matrix, so concat it onto the flatMap result with a comment noting the plural-waves naming keeps it distinct from wave.create/update/delete. This is build-blocking if missed: named SSE events not in the list never reach onMessage (UI-SPEC C3 integration pitfall).

    EmptyState.tsx: add a "no-running-waves" variant to the variant union and switch — heading "No running waves" (18px/600), body "Wave cards appear here while task Jobs run — select a plan to view its execution DAG." (14px muted), centered, matching the existing E2/E3 anatomy.

    RunningWavesView.tsx (new): implement UI-SPEC C3 verbatim. Exported types RunningWaveTask{name, status}, RunningWave{planName, waveIndex, tasks}; Props{projectName, onPlanClick, initialSnapshot?}. Subscribe to projectEventsURL(projectName) via useSSEStream (own subscription — established multi-subscriber pattern); on each waves.snapshot event, defensively JSON-parse and full-replace state (no merging); malformed events are ignored keeping last good state. View states: null state → centered Loader2 spinner (L2 pane-loading); waves.length === 0 → EmptyState variant no-running-waves; otherwise header line "ALL RUNNING WAVES" (mono 12px/600, --color-text-muted) above a p-4 flex-col gap-2 overflow-y-auto card list, data-testid="running-waves-view". Wave card per the locked anatomy: rounded border --color-border-subtle bg --color-surface-raised, hover --color-surface-overlay, cursor-pointer; header row px-3 py-2 with Waves lucide icon (14px muted aria-hidden data-icon="Waves"), plan name (mono 13px/600, min-w-0 flex-1 truncate, title tooltip), wave label shrink-0 mono 12px/600 muted with locked format "WAVE <N> · <running>/<total> running" where running counts tasks in {Running, Dispatching}; 1px divider; chip row px-3 py-2 flex-wrap gap-2 aria-hidden="true" with one StatusBadge per task wrapped in span title={task.name} data-testid="wave-card-chip", statuses coerced through KNOWN_STATUS_VALUES (unknown → Pending). Card interaction (D-16): role="button" tabIndex={0}, click or Enter/Space fires onPlanClick(planName); data-testid="wave-card-<planName>-<waveIndex>", data-plan, data-wave-index, aria-label "plan <planName>, wave <N>, <running> of <total> tasks running". No accent rings, no chip click-through, no popovers, no re-sorting (render payload order).
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "waves.snapshot" dashboard/web/src/lib/sse.ts` returns >= 1 match outside the kind×action flatMap
    - RunningWavesView.tsx exists exporting RunningWave/RunningWaveTask types and accepting initialSnapshot
    - Wave label renders the exact locked format `WAVE <N> · <running>/<total> running` (grep the template string)
    - EmptyState supports variant "no-running-waves" with the locked copy
    - Chip row carries aria-hidden="true"; card carries role="button" and the locked data-testids
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Component implements the approved C3 contract verbatim; the SSE registration pitfall is closed.</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx pane swap + All waves return affordance (UI-SPEC C4, D-13)</name>
  <files>dashboard/web/src/App.tsx</files>
  <read_first>
    - dashboard/web/src/App.tsx (PaneHeader local component at :68; the two-column grid and right-pane branch at ~:200-235 incl. the empty-state div to replace; onPlanClick callback and the #/plan/ hash handling)
    - .planning/phases/15-paper-cuts/15-UI-SPEC.md §C4 (the locked swap + return-affordance spec; Phase 16 seam constraints)
  </read_first>
  <action>
    Replace the selectedPlan === null empty-state div ("Select a plan to view its execution DAG") with RunningWavesView, passing projectName={selectedProject ?? ""} and the existing onPlanClick callback — the URL hash + selection behavior is then identical to clicking a PlanNode (D-16 deep-linkable). The selectedPlan !== null ExecutionDAGView branch is unchanged.

    Return affordance: extend the local PaneHeader with an optional action?: ReactNode slot (PLANNING pane call untouched). In the EXECUTION PaneHeader, when selectedPlan !== null, render a right-aligned text button labeled "All waves" — mono 12px/600, --color-text-muted, hover --color-text-primary, no accent, no border; data-testid="execution-pane-all-waves", aria-label="Show all running waves". Click sets selectedPlan to null AND clears the #/plan/... URL hash (mirror however onPlanClick writes the hash — clear it the symmetric way), returning the right pane to the aggregate.

    Scope guard (Phase 16 seam): AppShell, Header, and the grid skeleton stay untouched — all changes live inside App.tsx's body branching.
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Select a plan to view its execution DAG" dashboard/web/src/App.tsx` returns 0
    - `grep -n "RunningWavesView" dashboard/web/src/App.tsx` shows the import and the null-branch render
    - `grep -n "execution-pane-all-waves" dashboard/web/src/App.tsx` returns >= 1 match
    - `git diff --stat dashboard/web/src/components/AppShell.tsx dashboard/web/src/components/Header.tsx` is empty
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Aggregate view is the right-pane default and stays reachable via the All waves return button.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest coverage per the UI-SPEC Validation Contract</name>
  <files>dashboard/web/src/components/__tests__/RunningWavesView.test.tsx, dashboard/web/src/App.test.tsx</files>
  <read_first>
    - dashboard/web/src/components/__tests__/dag-views.test.tsx (fixture + render conventions for SSE-fed views; how initialData/initialSnapshot bypasses SSE in tests)
    - dashboard/web/src/App.test.tsx (existing App spec to extend — selection/hash behavior harness)
    - .planning/phases/15-paper-cuts/15-UI-SPEC.md §Validation Contract rows "RunningWavesView" and "App"
  </read_first>
  <action>
    RunningWavesView.test.tsx (new): with initialSnapshot fixtures assert — cards render plan name, the exact "WAVE N · x/y running" label, one wave-card-chip per task; chip row has aria-hidden="true"; card click AND keyboard Enter fire onPlanClick(planName); waves: [] renders the "No running waves" empty state; no snapshot yet renders the spinner; a malformed waves.snapshot event is ignored and the last good state persists (drive via the SSE test seam the suite already uses, or simulate the event handler with bad JSON).

    App.test.tsx (extend): right pane defaults to RunningWavesView when selectedPlan === null and the old empty-state string is GONE (assert the literal "Select a plan to view its execution DAG" is absent); clicking a wave card swaps the pane to ExecutionDAGView and sets the #/plan/<name> hash; the All waves button (execution-pane-all-waves) returns to the aggregate and clears the hash.
  </action>
  <verify>
    <automated>cd dashboard/web && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - RunningWavesView.test.tsx covers: card content, label format, chips, aria-hidden, click + Enter, empty state, spinner, malformed-event resilience (8 behaviors)
    - App spec asserts the old empty-state string is absent (regression: the default actually changed)
    - App spec asserts card-click → ExecutionDAGView + hash, and All waves → aggregate + cleared hash
    - `npm run test -- --run` exits 0
  </acceptance_criteria>
  <done>The full C3/C4 validation contract is automated; the view's behavior is pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE payload (server-derived JSON) → React DOM | Plan/task names and statuses from cluster CRs render in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-20 | Tampering | XSS via plan/task names in wave cards | mitigate | all payload strings render as React text children / attribute values (auto-escaped); no dangerouslySetInnerHTML anywhere in the new component |
| T-15-21 | Denial of Service | Malformed/oversized snapshot events breaking the view | mitigate | defensive JSON parse ignores malformed events keeping last good state (asserted in test); stream failures surface via existing connection pill + toasts |
| T-15-22 | Tampering | Unknown status strings reaching badge styling | mitigate | chip statuses coerce through KNOWN_STATUS_VALUES; unknown → Pending (shared guard from 15-05) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | no new packages — Waves icon from the pinned lucide-react |
</threat_model>

<verification>
- `cd dashboard/web && npx tsc --noEmit && npm run test -- --run` exits 0
- Bundle gate (<500KB gzipped) green — ~1KB of new icon weight only
- Manual (phase gate, per 15-VALIDATION.md Manual-Only): live dashboard with >= 2 plans running shows wave cards updating over SSE; click-through and All waves return both work
</verification>

<success_criteria>
- ROADMAP SC6 client half: aggregate view of all currently-running waves across all Plans, fed exclusively by the server-derived waves.snapshot stream
- D-13/D-14/D-16 implemented exactly as locked in the approved UI-SPEC; Phase 16 AppShell seam untouched
</success_criteria>

<output>
Create `.planning/phases/15-paper-cuts/15-07-SUMMARY.md` when done
</output>
