---
phase: 15-paper-cuts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/webhook/v1alpha1/plan_webhook.go
  - internal/controller/plan_webhook_test.go
  - internal/controller/plan_controller.go
  - internal/controller/file_touch_gate_test.go
  - internal/subagent/common/templates/plan_planner.tmpl
  - internal/subagent/common/prompt_templates_test.go
autonomous: true
requirements: [CUTS-07]
must_haves:
  truths:
    - "A reporter-flow Plan whose sibling Tasks share a file with no dependsOn edge under fileTouchMode strict dispatches ZERO task Jobs and parks with ValidationState=FileTouchMismatch"
    - "The park condition names both tasks and the shared path"
    - "Fixing the task specs (adding dependsOn or splitting files) lifts the park on the next reconcile — park, never fail"
    - "The admission webhook resolves the actual project file-touch mode instead of passing nil project"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "File-touch dispatch gate in reconcileWaveMaterialization + patchPlanFileTouchMismatch park"
      contains: "FileTouchMismatch"
    - path: "internal/webhook/v1alpha1/plan_webhook.go"
      provides: "Exported ComputeFileTouchMismatches/SummariseMismatches + real project mode resolution (D-08)"
      exports: ["ComputeFileTouchMismatches", "SummariseMismatches"]
    - path: "internal/subagent/common/templates/plan_planner.tmpl"
      provides: "D-07 prompt guidance: sibling tasks in a wave must not share files"
  key_links:
    - from: "internal/controller/plan_controller.go"
      to: "internal/webhook/v1alpha1/plan_webhook.go"
      via: "reconciler gate calls exported ComputeFileTouchMismatches"
      pattern: "ComputeFileTouchMismatches"
    - from: "internal/controller/plan_controller.go"
      to: "api/v1alpha1/plan_types.go ValidationState enum"
      via: "park sets the dormant FileTouchMismatch enum value"
      pattern: "ValidationState = \"FileTouchMismatch\""
---

<objective>
Close CUTS-07 (root cause of run-1's two merge-conflict task branches despite fileTouchMode: strict). The overlap check `computeFileTouchMismatches` is correct but lives only in the Plan admission webhook, which early-returns when zero Tasks are visible (Pitfall B, plan_webhook.go:139-147) — and in the reporter flow Tasks always materialize AFTER the Plan, so the check never ran. Seat the authoritative check in the PlanReconciler dispatch path (D-05), park-not-fail on strict mismatch (D-06), patch the planner prompt to prevent the overlap at authoring time (D-07), and upgrade the webhook to real project mode resolution (D-08).

Purpose: strict fileTouchMode actually prevents sibling-wave file conflicts in every flow.
Output: reconciler dispatch gate + park, exported overlap helpers, webhook mode-resolution fix, prompt patch, envtest regression reproducing the run-1 symptom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-paper-cuts/15-CONTEXT.md
@.planning/phases/15-paper-cuts/15-PATTERNS.md
@.planning/phases/15-paper-cuts/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export overlap helpers + webhook real mode resolution (D-08)</name>
  <files>internal/webhook/v1alpha1/plan_webhook.go, internal/controller/plan_webhook_test.go</files>
  <read_first>
    - internal/webhook/v1alpha1/plan_webhook.go (full file: Pitfall B early-return :139-147, nil-project ResolveFileTouchMode call :174, computeFileTouchMismatches :247, summariseMismatches, fileTouchMismatch type)
    - internal/webhook/v1alpha1/strict_mode.go (ResolveFileTouchMode D-E3 precedence: annotation > resolved-cache > project.Spec > helm default)
    - internal/controller/plan_webhook_test.go (existing envtest webhook specs — extend, don't rewrite; confirm Pitfall B zero-Tasks early-return coverage)
    - internal/controller/plan_controller.go resolveProjectForPlan (~:1316) — the owner-chain walk the webhook variant mirrors
  </read_first>
  <action>
    Export the overlap core in plan_webhook.go: rename computeFileTouchMismatches → ComputeFileTouchMismatches, summariseMismatches → SummariseMismatches, and export the mismatch pair type if the reconciler needs it (e.g. fileTouchMismatch → FileTouchMismatchPair). No functional changes to the overlap logic (it already handles dir-prefix vs exact-path and canonical ordering). Update all in-package call sites.

    D-08: replace the nil-project mode resolution at :174 with a real resolution. Add an unexported resolveProjectForWebhook(ctx, v.Client, plan) that walks the OwnerRef/PhaseRef chain (Plan → Phase → Milestone → Project, ~3 Gets) mirroring resolveProjectForPlan's walk but in non-receiver form using v.Client; on any Get failure return nil (preserving today's nil-fallback so admission never hard-fails on a missing chain). Then call ResolveFileTouchMode(plan, project, v.DefaultFileTouchMode). Keep the Pitfall B zero-Tasks early-return INTACT — the reconciler gate (Task 2) is the authoritative seat; the webhook stays the early layer for direct applies.

    Tests in plan_webhook_test.go: (a) existing specs still green after the renames; (b) new spec — a Plan whose resolved Project sets Spec.FileTouchMode=strict (with no plan annotation) now gets strict semantics at admission where the old nil-project path would have fallen back to the cluster default; (c) Pitfall B preserved — zero visible Tasks still early-returns with a warning, no rejection.
  </action>
  <verify>
    <automated>go test ./internal/webhook/... ./internal/controller/... -run "Webhook|PlanWebhook" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func ComputeFileTouchMismatches" internal/webhook/v1alpha1/plan_webhook.go` returns 1; lowercase `func computeFileTouchMismatches` returns 0 matches
    - `grep -n "ResolveFileTouchMode(plan, nil" internal/webhook/v1alpha1/plan_webhook.go` returns 0 matches (nil-project call gone from the validate path)
    - Webhook spec asserts project.Spec.FileTouchMode now drives admission mode
    - Pitfall B zero-Tasks early-return spec still green
    - `go test ./internal/webhook/... ` exits 0
  </acceptance_criteria>
  <done>Overlap helpers exported; webhook resolves the real project mode with nil fallback; Pitfall B behavior unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlanReconciler file-touch dispatch gate + park (D-05, D-06)</name>
  <files>internal/controller/plan_controller.go, internal/controller/file_touch_gate_test.go</files>
  <read_first>
    - internal/controller/plan_controller.go (reconcileWaveMaterialization ~:918-955 — insertion point after the ValidationState check, before tasksToDAGLocal; patchPlanRejected ~:701-714 — the park-function pattern to replicate; resolveProjectForPlan; the dispatch gate stack :292-330 for gate-ordering context)
    - api/v1alpha1/plan_types.go :43-50 (ValidationState field + enum incl. dormant FileTouchMismatch)
    - internal/controller/plan_gates_test.go and plan_wave_integration_test.go (envtest fixture conventions for Plans with materialized Tasks)
    - .planning/phases/15-paper-cuts/15-PATTERNS.md "Condition + ValidationState park" shared pattern
  </read_first>
  <behavior>
    - Test 1 (run-1 symptom regression): Plan with fileTouchMode strict + two sibling Tasks declaring the same file with NO dependsOn edge → after reconcile, plan.Status.ValidationState == "FileTouchMismatch", a Conditions entry with Reason "FileTouchMismatch" whose Message names BOTH task names and the shared path, and ZERO Jobs exist for the Plan's tasks
    - Test 2 (park lifts, D-06): update one Task to add dependsOn on the other → next reconcile clears the park (ValidationState returns to "Validated"), wave derivation proceeds
    - Test 3 (non-strict mode): same overlap under warn/permissive mode → no park, dispatch proceeds
    - Test 4 (park is not Failed): while parked, plan.Status.Phase is NOT "Failed" (park-not-fail, Phase 12 D-05 doctrine)
  </behavior>
  <action>
    In reconcileWaveMaterialization, after the existing ValidationState handling and before tasksToDAGLocal/dag.ComputeWaves, when len(taskList.Items) > 0: resolve the project via r.resolveProjectForPlan, compute mode via webhookv1alpha1.ResolveFileTouchMode(plan, project, r.DefaultFileTouchMode) (confirm the reconciler has access to the same default the webhook uses — if PlanReconciler lacks a DefaultFileTouchMode field, add one wired from the same flag/config source the webhook consumes), compute mismatches via webhookv1alpha1.ComputeFileTouchMismatches(taskList.Items). If mismatches exist AND mode is strict, call a new patchPlanFileTouchMismatch and return — no wave derivation, no stampTaskLabels, no Job dispatch.

    patchPlanFileTouchMismatch (modeled on patchPlanRejected): MergeFrom status patch setting plan.Status.ValidationState = "FileTouchMismatch" AND a meta.SetStatusCondition entry (Type ConditionWaveOrLevelPaused, Status True, Reason "FileTouchMismatch", Message from SummariseMismatches naming both tasks and the shared path). Return ctrl.Result{} with no requeue — the next Task create/update event re-enters reconcile (matching how the reporter flow materializes Tasks asynchronously; RESEARCH Pitfall 3's false-negative window self-heals on the next Task event).

    Un-park path (D-06): when the gate runs and mismatches are empty but plan.Status.ValidationState == "FileTouchMismatch", patch ValidationState back to "Validated" and clear the paused condition (Status False, Reason indicating validation passed) before proceeding to wave derivation. NO new Status.Phase enum values anywhere (Phase 12 D-04); "Failed" is never set by this gate.

    Write the four behavior tests in a NEW file internal/controller/file_touch_gate_test.go using the existing envtest suite registration (package controller, same Describe/It conventions as plan_gates_test.go).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run "TestControllers" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FileTouchMismatch" internal/controller/plan_controller.go` returns >= 2 (gate + park function)
    - Envtest: strict + shared file + no edge → ValidationState=="FileTouchMismatch", condition message contains both task names AND the shared file path, zero child Jobs
    - Envtest: adding dependsOn lifts the park on the next reconcile
    - Envtest: non-strict mode does not park
    - plan.Status.Phase never set to "Failed" by the gate (assert in test)
    - `go test ./internal/controller/...` exits 0
  </acceptance_criteria>
  <done>Reconciler gate parks strict mismatches before any dispatch, names the conflict, and lifts on fix — the run-1 symptom is pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Planner prompt patch — no sibling-wave file sharing (D-07)</name>
  <files>internal/subagent/common/templates/plan_planner.tmpl, internal/subagent/common/prompt_templates_test.go</files>
  <read_first>
    - internal/subagent/common/templates/plan_planner.tmpl (current task-authoring guidance — find the section instructing the LLM on task DAG/filesTouched shape)
    - internal/subagent/common/prompt_templates_test.go (existing template-content assertion style)
  </read_first>
  <action>
    Add a short, imperative rule to plan_planner.tmpl in the task-DAG/filesTouched guidance section: sibling tasks in the same wave MUST NOT declare the same path in filesTouched — when two tasks need the same file, either declare a dependsOn edge between them (serializing into different waves) or split the file ownership so each task touches disjoint paths. Match the template's existing voice and formatting (this is a compiled-in Go template per the no-vendored-GSD constraint — keep template syntax valid).

    Extend prompt_templates_test.go with an assertion that the rendered plan-planner prompt contains the new rule (substring assertion on a stable phrase, e.g. "must not declare the same" — pick the exact phrase used in the template).
  </action>
  <verify>
    <automated>go test ./internal/subagent/common/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - plan_planner.tmpl contains the sibling-file rule (grep for the chosen stable phrase returns 1 match)
    - prompt_templates_test.go asserts the rendered prompt contains the rule
    - `go test ./internal/subagent/common/...` exits 0 (template still parses/renders)
  </acceptance_criteria>
  <done>The planner prompt attacks the root cause (LLM emitting overlapping filesTouched without edges); enforcement from Task 2 remains the guarantee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-authored Task specs → PlanReconciler dispatch | Untrusted filesTouched declarations drive Job dispatch decisions |
| kubectl apply → admission webhook | Direct-apply Plans cross into the cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | File-touch enforcement bypass via reporter flow (webhook never sees Tasks) | mitigate | reconciler gate (D-05) runs after Tasks materialize, before any dispatch — authoritative regardless of flow |
| T-15-05 | Tampering | Webhook nil-project fallback weakening strict mode (D-08) | mitigate | real project resolution at admission; nil fallback only on chain-Get failure, and the reconciler gate backstops it |
| T-15-06 | Denial of Service | +3 Gets per Plan validate-accept (D-08) | accept | Plan admissions are low-frequency operator/reporter actions; explicitly accepted in CONTEXT D-08 |
| T-15-07 | Repudiation | Parked Plan with no explanation | mitigate | condition Message names both tasks and the shared path via SummariseMismatches |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan |
</threat_model>

<verification>
- `go test ./internal/webhook/... ./internal/controller/... ./internal/subagent/common/...` exits 0
- `gofmt -l internal/webhook internal/controller internal/subagent` prints nothing
- Run-1 symptom pinned: strict + shared file + no edge → park, zero Jobs (the two merge-conflict task branches of run 1 can no longer ship)
</verification>

<success_criteria>
- ROADMAP SC7 satisfied at BOTH seats: admission (early layer, real mode) and reconciler dispatch (authoritative)
- ValidationState=FileTouchMismatch (dormant enum value) is now set and lifted correctly
- Park-not-fail doctrine preserved; no new Status.Phase values
- D-07 prompt rule present and asserted
</success_criteria>

<output>
Create `.planning/phases/15-paper-cuts/15-02-SUMMARY.md` when done
</output>
