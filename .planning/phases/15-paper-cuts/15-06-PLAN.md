---
phase: 15-paper-cuts
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/dashboard/api/waves.go
  - cmd/dashboard/api/waves_test.go
  - cmd/dashboard/api/informer_bridge.go
  - cmd/dashboard/api/informer_bridge_test.go
  - cmd/dashboard/api/events_sse.go
  - cmd/dashboard/api/events_sse_test.go
autonomous: true
requirements: [CUTS-06]
must_haves:
  truths:
    - "The manager API derives a running-waves aggregate from label-selector queries over Tasks (tideproject.k8s/project + tideproject.k8s/wave-index) — waves are NEVER stored in CRD status"
    - "The aggregate is delivered as SSE event type waves.snapshot over the EXISTING project-events channel — no new REST route"
    - "A snapshot is emitted on SSE subscribe and on every Task change; zero running waves serializes as waves: [] (never null)"
    - "A wave is running iff >= 1 member Task has phase in {Running, Dispatching}; deterministic sort: plan name asc, wave index asc, task name asc"
  artifacts:
    - path: "cmd/dashboard/api/waves.go"
      provides: "computeRunningWaves shared aggregate helper + waves.snapshot payload types"
      contains: "waves.snapshot"
    - path: "cmd/dashboard/api/informer_bridge.go"
      provides: "waves.snapshot publish on Task informer events"
    - path: "cmd/dashboard/api/events_sse.go"
      provides: "initial waves.snapshot on subscribe"
  key_links:
    - from: "cmd/dashboard/api/waves.go"
      to: "tideproject.k8s/wave-index label on Tasks"
      via: "label-selector grouping per spec derived-waves model"
      pattern: "wave-index"
    - from: "cmd/dashboard/api/informer_bridge.go"
      to: "hub.Publish"
      via: "waves.snapshot event on task add/update/delete"
      pattern: "waves\\.snapshot"
---

<objective>
Backend half of CUTS-06 (run-1 user feature 2): the manager API derives an all-running-waves aggregate per project via label-selector queries over Tasks and delivers it as a new named SSE event type `waves.snapshot` on the EXISTING `/api/v1/projects/{name}/events` stream (D-15 — no new REST route, thin client, waves derived never stored). Payload contract is locked by 15-UI-SPEC C3/C5.

Purpose: the dashboard's aggregate wave view (plan 15-07) has a live, spec-compliant data source.
Output: computeRunningWaves helper (reusing inspect_wave grouping semantics), informer-bridge emission on Task events, snapshot-on-subscribe, Go tests, invariants green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-paper-cuts/15-UI-SPEC.md
@.planning/phases/15-paper-cuts/15-PATTERNS.md
@.planning/phases/15-paper-cuts/15-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeRunningWaves aggregate helper + payload types</name>
  <files>cmd/dashboard/api/waves.go, cmd/dashboard/api/waves_test.go</files>
  <read_first>
    - cmd/tide/inspect_wave_run.go lines ~37-136 (labelProject/labelWaveIndex consts and the existing group-by-(planRef, wave-index) logic — the semantics to reuse; note how planRef is derived per Task)
    - cmd/dashboard/api/projects.go (handler/package conventions: struct shape, writeJSON, logr usage, Apache header)
    - cmd/dashboard/api/informer_bridge.go (client.Reader usage from the informer cache — the read path the helper must use)
    - .planning/phases/15-paper-cuts/15-UI-SPEC.md §C3 Semantics + §C5 (locked contract: running definition, sort order, JSON shape, []-never-null)
  </read_first>
  <behavior>
    - Test 1: Tasks across two plans with wave-index labels group into waves keyed by (planName, waveIndex); only waves with >= 1 task in {Running, Dispatching} are included (terminal waves excluded by construction)
    - Test 2: deterministic order — waves sorted plan name asc then wave index asc (numeric, not lexicographic); tasks within a wave sorted by name asc
    - Test 3: zero running waves marshals to {"waves":[]} — never null
    - Test 4: Tasks missing the wave-index label (not yet stamped) are skipped without error; Tasks from other projects (different tideproject.k8s/project label) are excluded
  </behavior>
  <action>
    Create cmd/dashboard/api/waves.go (Apache header, package api). Export payload types matching the UI-SPEC C3 wire contract exactly: RunningWave{PlanName string `json:"planName"`, WaveIndex int `json:"waveIndex"`, Tasks []RunningWaveTask `json:"tasks"`} and RunningWaveTask{Name string `json:"name"`, Status string `json:"status"`}, plus a snapshot envelope {Waves []RunningWave `json:"waves"`}. Implement computeRunningWaves(ctx, cli client.Reader, ns, projectName string) returning the envelope: List Tasks in the namespace filtered by client.MatchingLabels{"tideproject.k8s/project": projectName} (cache-backed read), group by (plan ref, wave-index label parsed via strconv.Atoi — skip unparsed), derive each task's status from its Status.Phase, include a wave iff >= 1 member task phase is "Running" or "Dispatching" (isRunningPhase helper), include ALL member tasks of an included wave (running count vs total is the client's render concern), sort waves by plan name asc then wave index asc and tasks by name asc, and initialize Waves to an empty non-nil slice. Derive the per-task plan ref the same way inspect_wave_run.go does — read that file first and mirror its key. Keep the grouping logic in this one helper so the CLI could later share it (CONTEXT discretion noted; do NOT refactor inspect_wave_run.go in this plan).

    Write the four behavior tests in waves_test.go using a fake client.Reader seeded with labeled Task fixtures (follow the fixture style of projects_test.go / informer_bridge_test.go).
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/api/... -run "Waves|RunningWaves" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - waves.go exists with computeRunningWaves consuming client.MatchingLabels on tideproject.k8s/project
    - JSON field names exactly: waves, planName, waveIndex, tasks, name, status (grep the struct tags)
    - Empty aggregate marshals to {"waves":[]} (asserted in test, not null)
    - Numeric wave-index sort asserted (e.g. wave 2 before wave 10)
    - `go test ./cmd/dashboard/api/...` exits 0
  </acceptance_criteria>
  <done>Aggregate helper implements the locked C3/C5 semantics with full unit coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Wire emission — Task events + snapshot-on-subscribe + invariants</name>
  <files>cmd/dashboard/api/informer_bridge.go, cmd/dashboard/api/informer_bridge_test.go, cmd/dashboard/api/events_sse.go, cmd/dashboard/api/events_sse_test.go</files>
  <read_first>
    - cmd/dashboard/api/informer_bridge.go (BridgeInformerToHub + newKindHandler: how kind handlers publish `<kind>.<action>` events via h.Publish with hub.Event{Type, JSON}; resolveProjectKey)
    - cmd/dashboard/api/events_sse.go (EventsHandler struct + options at ~:77-113, the Subscribe flow at ~:185-195 incl. Last-Event-ID replay and Pitfall 22 unsubscribe — the seam where the initial snapshot is written)
    - cmd/dashboard/api/events_sse_test.go and informer_bridge_test.go (existing harness for hub/SSE assertions)
    - cmd/dashboard/router.go route registration (confirm NOTHING is added here — D-15/Pitfall 6)
  </read_first>
  <action>
    Informer side: in newKindHandler's task-kind path (typePrefix "task"), after the existing per-Task event publish on add/update/delete, derive the aggregate via computeRunningWaves(ctx, cli, obj namespace, projectKey) and h.Publish(projectKey, hub.Event{Type: "waves.snapshot", JSON: marshaled envelope}). Reads are cache-backed (client.Reader over the informer cache) so per-event derivation is cheap; no debounce in v1 — record this as the accepted T-15-17 disposition in a code comment. Marshal failures: log and skip (never panic the handler).

    Subscribe side: extend EventsHandler with an optional client.Reader (new field or functional option following the existing EventsHandlerOption pattern, nil-safe so existing tests/constructors keep working). In the events handler flow, immediately after Hub.Subscribe and before the event loop, when the reader is non-nil: derive the aggregate for the requested project and write a `waves.snapshot` SSE frame directly to the response (event: waves.snapshot + data: payload, matching the existing frame-writing format in the handler) so the pane populates without waiting for a Task transition (UI-SPEC C3 data path). Wire the reader at the construction site where EventsHandler is built (find where NewEventsHandler is called and pass the manager's cache-backed reader).

    Tests: informer_bridge_test.go — a Task update publishes BOTH task.update and waves.snapshot to the project hub, and the waves.snapshot payload reflects the fixture's grouping. events_sse_test.go — a subscribe with a non-nil reader receives an initial waves.snapshot frame before any published event; with a nil reader, behavior is unchanged (no frame, no panic).

    Invariants: do NOT register any new route (router.go untouched); run `make verify-no-aggregates` (no waves in CRD status — this plan adds zero API-type fields) and the existing TestZeroMutationRoutes.
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/... -count=1 && make verify-no-aggregates</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "waves.snapshot" cmd/dashboard/api/informer_bridge.go cmd/dashboard/api/events_sse.go` returns >= 1 match in each file
    - `git diff --stat cmd/dashboard/router.go` is empty (no new route)
    - Bridge test asserts waves.snapshot published on Task update with correct grouping
    - SSE test asserts initial snapshot on subscribe (non-nil reader) and nil-reader no-op
    - `make verify-no-aggregates` exits 0; TestZeroMutationRoutes passes
    - `go test ./cmd/dashboard/...` exits 0
  </acceptance_criteria>
  <done>waves.snapshot flows on subscribe and on every Task change over the existing channel; spec invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cluster Task CRs → SSE payload | CR names/statuses cross into a browser-consumed JSON stream |
| SSE subscriber → aggregate derivation | Each subscribe and each Task event triggers a derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Information Disclosure | Cross-project leakage in the aggregate | mitigate | derivation filters on tideproject.k8s/project == the channel's project; the SSE channel is already project-scoped; asserted in Task 1 test 4 |
| T-15-17 | Denial of Service | Re-derivation on every Task event / subscribe | accept | cache-backed in-memory List, O(tasks-in-namespace); no apiserver round-trip; documented in code comment (UI-SPEC leaves debounce as backend concern — deferred to v1.x if profiling demands) |
| T-15-18 | Tampering | Stale informer cache producing a false aggregate | accept | bounded by informer sync; worst case is a brief SSE delay, corrected by the next event (RESEARCH security table) |
| T-15-19 | Tampering | Aggregate cached into CRD status (spec violation) | mitigate | no API-type changes; make verify-no-aggregates gate in the verify command |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan |
</threat_model>

<verification>
- `go test ./cmd/dashboard/... -count=1` exits 0; `make verify-no-aggregates` exits 0
- `gofmt -l cmd/dashboard` prints nothing
- No new route; read-only surface preserved (TestZeroMutationRoutes green)
</verification>

<success_criteria>
- ROADMAP SC6 server half: aggregate reads from label-selector queries per the spec's derived-waves model
- D-15 honored literally: existing SSE channel, new named event type, thin client contract
- Snapshot on subscribe + on Task change; [] never null
</success_criteria>

<output>
Create `.planning/phases/15-paper-cuts/15-06-SUMMARY.md` when done
</output>
