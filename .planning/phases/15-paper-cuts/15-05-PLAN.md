---
phase: 15-paper-cuts
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/web/src/components/StatusBadge.tsx
  - dashboard/web/src/components/StatusBadge.test.tsx
  - dashboard/web/src/components/PlanningDAGView.tsx
  - dashboard/web/src/components/ProjectPicker.tsx
  - dashboard/web/src/components/ProjectPicker.test.tsx
  - dashboard/web/src/components/__tests__/dag-views.test.tsx
autonomous: true
requirements: [CUTS-05]
must_haves:
  truths:
    - "A Project CR with status Complete renders a Complete badge (success styling), not Pending (run-1 finding 9b)"
    - "Both coerce sites (PlanningDAGView, ProjectPicker) accept Complete; every other unknown string still coerces to Pending"
    - "Complete and Succeeded share the success color but carry distinct icons (CircleCheckBig vs CircleCheck) per the color-blindness rule"
  artifacts:
    - path: "dashboard/web/src/components/StatusBadge.tsx"
      provides: "Complete StatusValue + STATUS_TABLE row + exported KNOWN_STATUS_VALUES"
      contains: "CircleCheckBig"
    - path: "dashboard/web/src/components/PlanningDAGView.tsx"
      provides: "coerce() accepting Complete via the shared KNOWN list"
    - path: "dashboard/web/src/components/ProjectPicker.tsx"
      provides: "coerceStatus accepting Complete via the shared KNOWN list"
  key_links:
    - from: "dashboard/web/src/components/PlanningDAGView.tsx"
      to: "dashboard/web/src/components/StatusBadge.tsx"
      via: "imported KNOWN_STATUS_VALUES (drift-class kill, UI-SPEC C2)"
      pattern: "KNOWN_STATUS_VALUES"
    - from: "dashboard/web/src/components/ProjectPicker.tsx"
      to: "dashboard/web/src/components/StatusBadge.tsx"
      via: "imported KNOWN_STATUS_VALUES"
      pattern: "KNOWN_STATUS_VALUES"
---

<objective>
Close CUTS-05 (run-1 finding 9b): the Project CRD's terminal-success value `Complete` (PhaseComplete, project_types.go:392) is missing from the dashboard's StatusValue union and both runtime coerce guards, so a completed Project displays "Pending". Add `Complete` as a first-class status per the approved 15-UI-SPEC (C1/C2), and exercise the UI-SPEC-recommended consolidation: export a single KNOWN_STATUS_VALUES list from StatusBadge derived from STATUS_TABLE, consumed at both coerce sites — killing the silent-drift bug class permanently.

Purpose: Project terminal state renders correctly; the Go-API-vs-KNOWN drift class is structurally eliminated.
Output: 11-value status vocabulary, synchronized coerce guards, Vitest regressions asserting the finding-9b symptom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-paper-cuts/15-UI-SPEC.md
@.planning/phases/15-paper-cuts/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete vocabulary row + coerce-guard consolidation (UI-SPEC C1, C2)</name>
  <files>dashboard/web/src/components/StatusBadge.tsx, dashboard/web/src/components/PlanningDAGView.tsx, dashboard/web/src/components/ProjectPicker.tsx</files>
  <read_first>
    - dashboard/web/src/components/StatusBadge.tsx (full file: StatusValue union :28-38, STATUS_TABLE rows incl. the Succeeded row whose shape the Complete row mirrors, lucide import block, data-testid render path)
    - dashboard/web/src/components/PlanningDAGView.tsx lines ~55-80 (KNOWN array + coerce())
    - dashboard/web/src/components/ProjectPicker.tsx lines ~28-50 (KNOWN_STATUSES + coerceStatus)
    - .planning/phases/15-paper-cuts/15-UI-SPEC.md §C1 (the locked vocabulary row: CircleCheckBig icon, --color-status-success, label "Complete", SR "Complete — all milestones succeeded", no animation)
  </read_first>
  <action>
    StatusBadge.tsx: insert "Complete" into the StatusValue union immediately after "Succeeded" (terminal-success family grouping per UI-SPEC); add CircleCheckBig to the lucide-react import; add the Complete STATUS_TABLE row exactly per the UI-SPEC C1 vocabulary table — icon CircleCheckBig, iconName "CircleCheckBig", label "Complete", colorVar var(--color-status-success), srDescription "Complete — all milestones succeeded". The icon is deliberately CircleCheckBig, NOT CircleCheck: Complete and Succeeded share green and can appear side by side, and the inherited color-blindness rule requires distinct glyphs (UI-SPEC overrides RESEARCH's CircleCheck suggestion). data-testid="status-badge-Complete" follows automatically from the existing render path — no component changes.

    Consolidation (UI-SPEC C2 recommendation, exercised): export from StatusBadge.tsx a KNOWN_STATUS_VALUES constant derived from STATUS_TABLE keys (typed readonly StatusValue[], e.g. Object.keys(STATUS_TABLE) with the appropriate cast). In PlanningDAGView.tsx, delete the local KNOWN literal and have coerce() consult the imported KNOWN_STATUS_VALUES. In ProjectPicker.tsx, delete the local KNOWN_STATUSES literal and have coerceStatus consult the same import. Required outcome either way: coerce("Complete") and coerceStatus("Complete") return "Complete"; any other unknown string still returns "Pending". Do NOT add Complete to FAILED_STATUSES or any border treatment (UI-SPEC: plain success badge; Complete is Project-only — never rendered for Milestone/Phase/Plan/Task nodes, which use Succeeded).
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - StatusValue union has 11 members; `grep -c "Complete" dashboard/web/src/components/StatusBadge.tsx` >= 3 (union + table row + SR text)
    - `grep -n "CircleCheckBig" dashboard/web/src/components/StatusBadge.tsx` returns >= 2 matches (import + table row)
    - `grep -n "KNOWN_STATUS_VALUES" dashboard/web/src/components/PlanningDAGView.tsx dashboard/web/src/components/ProjectPicker.tsx` shows both files importing the shared list; neither retains a local KNOWN literal containing "Succeeded"
    - `npx tsc --noEmit` exits 0 (no type drift)
    - Existing Vitest suite still green
  </acceptance_criteria>
  <done>Complete is a first-class status with distinct icon; both coerce sites consume one shared list derived from STATUS_TABLE.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest regressions — finding-9b symptom + vocabulary assertions</name>
  <files>dashboard/web/src/components/StatusBadge.test.tsx, dashboard/web/src/components/__tests__/dag-views.test.tsx, dashboard/web/src/components/ProjectPicker.test.tsx</files>
  <read_first>
    - dashboard/web/src/components/StatusBadge.test.tsx (existing table-iteration spec to extend to 11 entries)
    - dashboard/web/src/components/__tests__/dag-views.test.tsx (PlanningDAGView fixtures — how project-node phases are injected)
    - dashboard/web/src/components/ProjectPicker.test.tsx (row-badge assertions)
    - .planning/phases/15-paper-cuts/15-UI-SPEC.md §Validation Contract (the locked test matrix this task implements)
  </read_first>
  <action>
    Per the UI-SPEC Validation Contract:
    - StatusBadge.test.tsx: table iteration covers 11 entries; the Complete row renders icon CircleCheckBig (assert via data-icon or iconName affordance the suite already uses), label "Complete", and the success color var.
    - dag-views.test.tsx: the finding-9b regression — render PlanningDAGView with a Project node whose phase is "Complete"; assert the node contains data-testid="status-badge-Complete" and does NOT contain data-testid="status-badge-Pending" (assert the symptom, not just the green path). Add a companion assertion that a genuinely unknown phase string (e.g. "Bogus") still coerces to status-badge-Pending.
    - ProjectPicker.test.tsx: a project row with phase "Complete" renders the Complete badge (second coerce site, RESEARCH A2).
  </action>
  <verify>
    <automated>cd dashboard/web && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts status-badge-Complete present AND status-badge-Pending absent for a Complete project node (symptom assertion)
    - A test asserts unknown strings still coerce to Pending
    - StatusBadge table spec iterates 11 entries
    - ProjectPicker Complete-badge spec passes
    - `npm run test -- --run` exits 0
  </acceptance_criteria>
  <done>Finding-9b pinned at both coerce sites; vocabulary table integrity asserted at 11 entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE/API payload status strings → React render | Server-provided status strings cross into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering | Unknown/malicious status strings reaching styling logic | mitigate | coerce guards remain exhaustive-by-construction (derived from STATUS_TABLE keys); unknown strings degrade to Pending; React escapes text content |
| T-15-15 | Spoofing | Status displayed without backend authority | accept | dashboard is a read-only mirror of CR status; no decisions are made client-side |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | no new packages — CircleCheckBig comes from the already-pinned lucide-react |
</threat_model>

<verification>
- `cd dashboard/web && npx tsc --noEmit && npm run test -- --run` exits 0
- Bundle gate unaffected (no new dependencies; ~1KB tree-shaken icon)
</verification>

<success_criteria>
- ROADMAP SC5: project-node chip displays "Complete" when the Project CR status is Complete; the Pending mapping is corrected
- UI-SPEC C1/C2 implemented verbatim (CircleCheckBig, success token, both coerce sites)
- Drift class eliminated via single exported KNOWN_STATUS_VALUES
</success_criteria>

<output>
Create `.planning/phases/15-paper-cuts/15-05-SUMMARY.md` when done
</output>
