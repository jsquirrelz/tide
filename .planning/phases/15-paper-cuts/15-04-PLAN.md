---
phase: 15-paper-cuts
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/tide-push/main.go
  - cmd/tide-push/main_test.go
  - internal/controller/phase_gates_test.go
autonomous: true
requirements: [CUTS-02, CUTS-03]
must_haves:
  truths:
    - "Boundary push on a clean tree exits 0 and still pushes the run branch — no 'cannot create empty commit' error (run-1 finding 8 pinned by test)"
    - "A Phase at AwaitingApproval without an approve annotation stays at AwaitingApproval across multiple reconcile cycles — no AwaitingApproval↔Running oscillation (run-1 finding 2 pinned by test)"
  artifacts:
    - path: "cmd/tide-push/main_test.go"
      provides: "Verified (or gap-closed) coverage of the clean-tree boundary push incl. the SC2 'nothing to push' surface"
    - path: "internal/controller/phase_gates_test.go"
      provides: "Convergence regression: AwaitingApproval stays parked across 3 reconciles without an approve annotation"
      contains: "AwaitingApproval"
  key_links:
    - from: "internal/controller/phase_gates_test.go"
      to: "internal/controller/phase_controller.go:197-206"
      via: "regression test pins the abf177c AwaitingApproval early-return"
      pattern: "AwaitingApproval"
---

<objective>
CUTS-02 and CUTS-03 are already fixed on main (8f0b99b empty-commit skip in Phase 11; abf177c AwaitingApproval early-return in Phase 12-01). Per the milestone-wide rule and CONTEXT, the deliverable here is VERIFICATION + REGRESSION PINNING, not new behavior: confirm the run-1 symptom paths are fully covered by tests, close coverage gaps if (and only if) found, and add the convergence regression that pins the flapping fix.

Purpose: requirements CUTS-02/CUTS-03 close with regression evidence; future refactors cannot silently reintroduce finding 8 or finding 2.
Output: documented verification evidence in the SUMMARY, plus at most small test additions (and a minimal log line only if SC2's "nothing to push" message is genuinely absent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-paper-cuts/15-CONTEXT.md
@.planning/phases/15-paper-cuts/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CUTS-02 — verify clean-tree boundary push coverage, close gaps only if found</name>
  <files>cmd/tide-push/main.go, cmd/tide-push/main_test.go</files>
  <read_first>
    - cmd/tide-push/main.go lines ~372 and ~480-500 (the 8f0b99b fix: worktreeClean check, the comment naming the medium-DoD boundary-push defect, and whatever the clean-tree path logs/prints)
    - cmd/tide-push/main_test.go lines ~996-1045 (TestRunPushBoundaryCleanTreePushesIntegratedBranch — the existing coverage to audit)
    - .planning/ROADMAP.md Phase 15 SC2 ("tide push on a clean tree exits 0 with a 'nothing to push' message — no cannot create empty commit error")
  </read_first>
  <action>
    Verification-first task (Observe First). Run `go test ./cmd/tide-push/... -run TestRunPushBoundary -v -count=1` and read the fix + test source. Audit the existing coverage against the run-1 finding-8 symptom and SC2, checking each of: (a) clean tree → NO commit attempt (the "cannot create empty commit" path is unreachable); (b) the run branch is STILL pushed to the remote from a clean tree; (c) exit code 0; (d) a "nothing to push"-shaped message is emitted (a log/stderr line indicating the commit was skipped because the tree was clean).

    For (d): grep the clean-tree code path in main.go for an emitted message. If a skip message exists, assert it in the existing test (or confirm an existing assertion covers it). If NO message exists at all, add a single log line on the clean-tree skip path (matching main.go's existing logging idiom — read neighboring log calls first) plus a test assertion; this is the only production-code change this task is permitted to make. If (a)-(c) are already fully asserted (RESEARCH says they are), make no changes to those paths and record the evidence (test names + pass output) for the SUMMARY. Note: there is no `tide push` CLI subcommand — `tide push` in SC2 refers to the tide-push binary run as a boundary Job (RESEARCH gap check, confirmed no gap).
  </action>
  <verify>
    <automated>go test ./cmd/tide-push/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/tide-push/... -count=1` exits 0
    - Coverage audit result recorded: each of (a)-(d) mapped to a specific test name, or a gap-closing assertion added
    - A clean-tree skip message exists in main.go and is asserted by a test (added only if absent)
    - No changes beyond the (d) gap closure: `git diff --stat cmd/tide-push/` shows only main_test.go (and main.go only if the message was missing)
  </acceptance_criteria>
  <done>CUTS-02 closed with regression evidence: all four symptom facets covered by named, passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: CUTS-03 — convergence regression pinning the AwaitingApproval early-return</name>
  <files>internal/controller/phase_gates_test.go</files>
  <read_first>
    - internal/controller/phase_controller.go lines 197-233 (the abf177c fix: AwaitingApproval early-return; `return ctrl.Result{}, nil` with no requeue is the invariant under test)
    - internal/controller/phase_gates_test.go (existing AwaitingApproval specs around Test 5a — audit whether a stays-parked-across-reconciles assertion ALREADY exists before writing one)
    - internal/gates/ CheckApprove/ConsumeApprove signatures (the annotation contract the early-return consumes)
  </read_first>
  <action>
    First audit: grep phase_gates_test.go and phase_controller_test.go for an existing spec asserting that a parked Phase does NOT transition back to Running absent an approve annotation (e.g. a Consistently assertion or a multi-reconcile loop). If such a spec exists and asserts the finding-2 symptom (no AwaitingApproval→Running flap), record it as the regression evidence and stop — do not duplicate.

    If absent, add one spec to phase_gates_test.go (matching the existing Describe/It + envtest conventions): create a Phase whose Status.Phase is "AwaitingApproval" with NO approve annotation; invoke/trigger reconcile at least 3 times (direct reconciler invocation, or an Eventually+Consistently pair against the envtest manager); assert (1) Status.Phase remains "AwaitingApproval" throughout (Consistently over a few seconds), and (2) the reconcile result requests no requeue when invoked directly — pinning the `return ctrl.Result{}, nil` stay-parked branch. Then assert the recovery path is intact: adding the approve annotation transitions the Phase to "Running" (this half may already be covered by Test 5a-family specs — reference rather than duplicate if so).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run "TestControllers" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A spec exists (pre-existing and recorded, or newly added) asserting Status.Phase stays "AwaitingApproval" across >= 3 reconcile cycles without an approve annotation
    - The no-requeue invariant of the early-return is asserted (empty ctrl.Result) or covered by the Consistently assertion
    - `go test ./internal/controller/...` exits 0
    - phase_controller.go itself is NOT modified (`git status --porcelain internal/controller/phase_controller.go` is empty)
  </acceptance_criteria>
  <done>CUTS-03 closed with a convergence regression pinning the finding-2 fix; no production-code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds test coverage and at most one log line; no new input surfaces, no new privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Repudiation | Silent clean-tree skip (no operator-visible message) | mitigate | SC2 gap check ensures a "nothing to push" message exists and is test-asserted |
| T-15-13 | All categories | New attack surface | accept | none introduced — verification/test-only plan |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan |
</threat_model>

<verification>
- `go test ./cmd/tide-push/... ./internal/controller/... -count=1` exits 0
- SUMMARY records the evidence map: finding-8 facets (a)-(d) → test names; finding-2 → convergence spec name
</verification>

<success_criteria>
- ROADMAP SC2 and SC3 verified with named regression tests (milestone-wide rule: tests reproduce the run-1 symptoms)
- Zero behavior rebuilt — already-fixed cuts closed as verify/pin work per CONTEXT
</success_criteria>

<output>
Create `.planning/phases/15-paper-cuts/15-04-SUMMARY.md` when done
</output>
