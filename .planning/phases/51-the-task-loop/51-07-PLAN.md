---
phase: 51-the-task-loop
plan: 07
type: execute
wave: 4
depends_on: [51-03, 51-06]
files_modified:
  - internal/controller/task_controller.go
  - internal/controller/task_verify_loop_test.go
autonomous: true
requirements: [TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, OBS-03]
must_haves:
  truths:
    - "A terminal verifier Job's EnvelopeOut.Verdict is classified fail-closed (ClassifyVerdict); an unreadable/absent verifier envelope is BLOCKED, never APPROVED"
    - "A deterministic gate-command finding forces non-APPROVED controller-side even if the top-level verdict is APPROVED (D-06 defence-in-depth)"
    - "A REPAIRABLE verdict stages a bounded evidence packet and mints a FRESH attempt (Attempt++) seeded with the original locked spec + packet — never the prior agent's full context"
    - "Infra-retry (eviction/transient rerun, same attemptID) stays a distinct, preserved path from quality-iteration (new attemptID)"
    - "An attempt whose RunEvidence.ChangedFiles intersects the protected evaluator/fixture/threshold set is a system escalation, never a pass"
    - "At maxIterations without APPROVED, setVerifyHaltIfNeeded stamps ConditionVerifyHalt; LoopStatus (current-iteration summary + exit reason) re-derives across a controller restart"
    - "The EVALUATOR sibling span is emitted at verifier completion"
  artifacts:
    - path: "internal/controller/task_controller.go"
      provides: "verdict consumption decision tree + repairOrHalt + anti-gaming + evidence packet + LoopStatus + haltVerify + EVALUATOR span call site"
      contains: "ClassifyVerdict"
  key_links:
    - from: "internal/controller/task_controller.go (verifier completion)"
      to: "pkg/dispatch.ClassifyVerdict"
      via: "verdict consumption decision tree"
      pattern: "ClassifyVerdict"
    - from: "internal/controller/task_controller.go (repairOrHalt)"
      to: "RunEvidence.ChangedFiles ∩ protected paths"
      via: "anti-gaming intersection"
      pattern: "ChangedFiles"
    - from: "internal/controller/task_controller.go (exhaustion)"
      to: "internal/controller/verify_halt.go (setVerifyHaltIfNeeded)"
      via: "onExhaustion → ConditionVerifyHalt"
      pattern: "setVerifyHaltIfNeeded"
---

<objective>
Build the BACKWARD half of the verifier sub-state-machine: consume the verifier `EnvelopeOut.Verdict` (fail-closed `ClassifyVerdict` + controller-side deterministic-dominance re-check), and drive the three-tier decision — APPROVED→Succeeded, REPAIRABLE→(anti-gaming check → fresh evidence-seeded attempt OR halt at maxIterations), BLOCKED/exhausted→`ConditionVerifyHalt`. Preserve infra-retry as a distinct path, maintain a re-derivable `LoopStatus`, and emit the EVALUATOR sibling span at verifier completion.

Purpose: TASK-02 (REPAIRABLE→fresh attempt + compact evidence packet), TASK-03 (infra-retry ≠ quality-iteration), TASK-04 (controller-side deterministic dominance re-check), TASK-05 (maxIterations bound + resumable LoopStatus), TASK-06 (three-tier escalation + structural anti-gaming), OBS-03 (EVALUATOR span call site). This closes the loop opened in Plan 06.
Output: verdict consumption + repairOrHalt + anti-gaming + evidence packet staging + LoopStatus/haltVerify + EVALUATOR span call site + the loop envtests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-the-task-loop/51-CONTEXT.md
@.planning/phases/51-the-task-loop/51-RESEARCH.md
@.planning/phases/51-the-task-loop/51-PATTERNS.md
@.planning/phases/51-the-task-loop/51-03-SUMMARY.md
@.planning/phases/51-the-task-loop/51-06-SUMMARY.md

<interfaces>
From pkg/dispatch/verdict.go: `func ClassifyVerdict(raw json.RawMessage) Verdict` (:102) — empty/malformed/missing → VerdictBlocked, never VerdictApproved; `VerdictApproved/VerdictRepairable/VerdictBlocked`; `type GateDecision`/`Finding`; `highSeverityFindingToken="blocker"` (retune point for D-06/D-08).
From pkg/dispatch/envelope.go: `EnvelopeOut.Verdict *GateDecision` (:283), `EnvelopeOut.RunEvidence *RunEvidence` (:289).
From pkg/dispatch/run_evidence.go: `RunEvidence.ChangedFiles []ChangedFile{Path,Status}` (:70), `Bounded()`, `MaxRunEvidenceChangedFiles=100`.
From internal/controller/task_controller.go: EnvReader.ReadOut + unreadable→fail-closed (mirror EnvelopeReadFailed :1251); nextAttempt (:1771, current max +1); blind maxAttemptsPerTask quality-retry (:729 region, SUPERSEDED — not the eviction path); Reservations Settle (:1247); the Verifying sub-state + verifier dispatch (Plan 06).
From internal/controller/verify_halt.go (Plan 05): `setVerifyHaltIfNeeded(ctx,c,project,taskCompletedAt)`.
From internal/controller/span_emission.go (Plan 03): `synthesizeEvaluatorSpan(...)` — the call site is here.
From pkg/dispatch/envelope.go: `VerifyContext.EvidencePacketPath` (:453) — the D-04 packet transport.
From api/v1alpha3 (Plan 01): `TaskStatus.LoopStatus`, `TaskSpec.Verification.MaxIterations`/`OnExhaustion`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verdict consumption decision tree + controller-side deterministic dominance + haltVerify</name>
  <files>internal/controller/task_controller.go, internal/controller/task_verify_loop_test.go</files>
  <read_first>
    - internal/controller/task_controller.go (handleJobCompletion :1236, EnvelopeReadFailed fail-closed :1251, Verifying sub-state from Plan 06)
    - pkg/dispatch/verdict.go (ClassifyVerdict :102, highSeverityFindingToken)
    - internal/controller/verify_halt.go (setVerifyHaltIfNeeded — from Plan 05)
    - internal/controller/span_emission.go (synthesizeEvaluatorSpan — from Plan 03)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"Consuming the verifier verdict" + §"Pitfall 1 (controller-side re-check)" + §"Pitfall 4"
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"task_controller.go (4)" + §"Shared Patterns: Fail-closed verdict classification"
  </read_first>
  <behavior>
    - Terminal verifier Job → EnvReader.ReadOut; err or nil Verdict → haltVerify (BLOCKED, never APPROVED).
    - ClassifyVerdict(raw)==APPROVED AND no deterministic gate-command failure finding → markSucceeded (existing terminal path).
    - ClassifyVerdict==APPROVED BUT a Finding{Severity:"blocker", Dimension:"gate-command"} present → treat as REPAIRABLE (D-06 defence-in-depth: a red gate dominates even an APPROVED top-level verdict).
    - ClassifyVerdict==REPAIRABLE → repairOrHalt (Task 2).
    - ClassifyVerdict==BLOCKED → haltVerify → setVerifyHaltIfNeeded stamps ConditionVerifyHalt.
    - The EVALUATOR sibling span is emitted at verifier completion (call site for synthesizeEvaluatorSpan).
  </behavior>
  <action>
    In the verifier-completion branch (the Verifying sub-state added in Plan 06), read the verifier out.json via EnvReader.ReadOut; on error/nil Verdict call haltVerify (fail-closed BLOCKED, mirroring EnvelopeReadFailed :1251). Marshal out.Verdict and switch on `dispatch.ClassifyVerdict(raw)`. Implement `hasDeterministicFailure(*GateDecision) bool` keying off a `Finding{Severity: highSeverityFindingToken, Dimension: "gate-command"}` (the structural signal Plan 02 emits) — when present, force the APPROVED branch into repairOrHalt (D-06 controller-side re-check). Add `haltVerify(...)` that calls `setVerifyHaltIfNeeded` (passing the completion timestamp for the CR-02 time-fence) and records the LoopStatus.ExitReason. Add the `synthesizeEvaluatorSpan(...)` call site at verifier completion (OBS-03). Start task_verify_loop_test.go with the verdict-tree cases incl. the APPROVED-over-red-gate dominance case (must NOT pass) and the unreadable-envelope BLOCKED case.
  </action>
  <acceptance_criteria>
    - `grep -E 'ClassifyVerdict' internal/controller/task_controller.go` returns the switch.
    - `grep -iE 'hasDeterministicFailure|gate-command' internal/controller/task_controller.go` returns the D-06 re-check.
    - `grep -E 'setVerifyHaltIfNeeded' internal/controller/task_controller.go` returns the haltVerify wiring.
    - `grep -E 'synthesizeEvaluatorSpan' internal/controller/task_controller.go` returns the span call site.
    - `go test ./internal/controller/... -run 'VerifyLoop' -count=1` exits 0 (incl. APPROVED-over-red-gate cannot pass).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/controller/... -run 'VerifyLoop' -count=1</automated>
  </verify>
  <done>The verdict decision tree consumes the verifier verdict fail-closed, a red gate dominates an APPROVED controller-side, BLOCKED/unreadable halts on ConditionVerifyHalt, and the EVALUATOR span is emitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: repairOrHalt — anti-gaming intersection + fresh evidence-seeded attempt (infra-retry preserved) + LoopStatus resume</name>
  <files>internal/controller/task_controller.go, internal/controller/task_verify_loop_test.go</files>
  <read_first>
    - internal/controller/task_controller.go (nextAttempt :1771, blind maxAttemptsPerTask :729 region — SUPERSEDED for quality, NOT eviction; Reservations Settle :1247)
    - pkg/dispatch/run_evidence.go (ChangedFiles :70, Bounded, MaxRunEvidenceChangedFiles)
    - pkg/dispatch/envelope.go (VerifyContext.EvidencePacketPath :453)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"The fresh attempt + anti-gaming gate" + §"Pitfall 4" (infra vs quality) + §"Pitfall 5" (protected-path scope: evaluator+fixtures the contract depends on, NOT all *_test.go)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"task_controller.go (5)" + §"Shared Patterns: Bounded run-evidence + anti-gaming intersection"
    - .planning/phases/51-the-task-loop/51-CONTEXT.md D-04/D-05/D-08
  </read_first>
  <behavior>
    - repairOrHalt: if out.RunEvidence.ChangedFiles intersects the protected evaluator/fixture/threshold path set → escalateSystem (system escalation, NEVER a pass, NEVER a plain repair).
    - Else if Attempt >= Verification.MaxIterations → haltVerify (onExhaustion → ConditionVerifyHalt), LoopStatus.ExitReason=iterationsExhausted.
    - Else: stage a BOUNDED evidence packet (relevant failures/diffs/test output from RunEvidence + verdict findings) to the PVC, set VerifyContext.EvidencePacketPath, mint a FRESH attempt (Attempt++ → new attemptID) seeded with the ORIGINAL locked spec + the packet — never the prior agent's full context.
    - Infra-retry (eviction/transient rerun, same attemptID, no evaluator feedback) remains a distinct, preserved path — grep-distinguishable from quality-iteration; the blind maxAttemptsPerTask quality-retry is superseded (NOT the eviction path).
    - LoopStatus carries only the current-iteration summary + exit reason (LOOP-03); after a controller restart mid-loop the state re-derives from Task.Status.Attempt + the completed-set (no persisted history).
  </behavior>
  <action>
    Add `repairOrHalt(...)`. Implement `intersectsProtected(changed []ChangedFile, protected []string) bool` and `protectedPathsFor(task)` scoped to the evaluator + fixtures the verification contract depends on (planner-declared/config set — NOT all `*_test.go`, Pitfall 5). On intersection call `escalateSystem(...)` (never a pass). Otherwise check `task.Status.Attempt >= task.Spec.Verification.MaxIterations` → haltVerify. Otherwise `stageEvidencePacket(...)` producing a BOUNDED packet (reuse RunEvidence.Bounded() discipline; reference-only) whose PVC path is passed via EvidencePacketPath on the next verify, then re-dispatch the executor as a QUALITY-iteration fresh attempt (increment Task.Status.Attempt → new attemptID, seeded with the locked spec + packet). Keep the eviction/infra-retry path (same attemptID) UNTOUCHED and grep-distinguishable (distinct code path + comments). Update LoopStatus (current-iteration summary + ExitReason only) each transition; ensure the loop re-derives after a restart from Attempt + completed-set. Extend task_verify_loop_test.go: (a) REPAIRABLE→fresh attempt seeded with locked spec+packet; (b) attempt editing an evaluator/fixture path → escalateSystem (never a pass) AND an ordinary code+test repair → NOT flagged (true-positive + true-negative, Pitfall 5); (c) eviction rerun keeps attemptID / does NOT consume a maxIterations slot; (d) resume-after-restart re-derives at Attempt=N.
  </action>
  <acceptance_criteria>
    - `grep -E 'func .*repairOrHalt|intersectsProtected|escalateSystem|stageEvidencePacket' internal/controller/task_controller.go` returns all four.
    - `grep -E 'ChangedFiles' internal/controller/task_controller.go` returns the anti-gaming intersection.
    - `grep -E 'EvidencePacketPath' internal/controller/task_controller.go` returns the packet path wiring.
    - `grep -iE 'infra.?retry|eviction' internal/controller/task_controller.go` returns the preserved distinct path comment/code.
    - `go test ./internal/controller/... -run 'VerifyLoop|AntiGaming|InfraRetry|Resume' -count=1` exits 0 (incl. anti-gaming true-positive AND true-negative).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/controller/... -run 'VerifyLoop|AntiGaming|InfraRetry|Resume' -count=1</automated>
  </verify>
  <done>REPAIRABLE mints a fresh evidence-seeded attempt; evaluator/fixture edits escalate (never pass) while ordinary repairs don't; infra-retry stays distinct; the loop bounds at maxIterations→VerifyHalt and re-derives on restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier verdict → Task correctness | An unreadable/ambiguous verdict must never collapse to APPROVED |
| attempt diff → evaluator integrity | An attempt must not weaken the evaluator/fixtures to force a pass |
| LLM judge → deterministic result | A red gate finding must dominate even an APPROVED top-level verdict, controller-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07 | Tampering / Repudiation | LLM judge APPROVED over a red gate (controller-side leg) | mitigate | hasDeterministicFailure forces non-APPROVED when a blocker/gate-command finding is present, layered on ClassifyVerdict fail-closed; regression asserts APPROVED-over-red-gate cannot pass. Residual: low (verifier-side leg in Plan 02 = defence-in-depth) |
| T-51-07b | Tampering (integrity) | agent edits evaluator/fixtures/thresholds to game a pass | mitigate | RunEvidence.ChangedFiles ∩ protected path set → system escalation, never a pass; test proves true-positive (evaluator edit) AND true-negative (ordinary repair) |
| T-51-07c | Availability / DoS | unbounded retry-until-pass | mitigate | maxIterations bound → onExhaustion → ConditionVerifyHalt; the loop is never unbounded/silent |
| T-51-07d | Repudiation | prior-agent full context leaking into a fresh attempt | mitigate | Fresh attempt seeded ONLY with the locked spec + a bounded evidence packet (reference-only), never the prior agent's full context |
| T-51-07e | Availability | loop state lost on controller restart | mitigate | LoopStatus current-iteration-only (LOOP-03); re-derive from Attempt + completed-set — resume test proves it |
| T-51-SC | Tampering | package installs | N/A | No package installs — controller Go only |
</threat_model>

<verification>
- `go test ./internal/controller/... -run 'VerifyLoop|AntiGaming|InfraRetry|Resume' -count=1` green.
- Full package: `go test ./internal/controller/... -count=1` green (no regression to executor/planner paths).
- `make lint` clean.
- `make test-int` (envtest Layer A) — remember MAKE_EXIT ≠ Ginkgo-green; grep the log for `^--- FAIL|^FAIL\s`.
</verification>

<success_criteria>
- The verifier verdict drives APPROVED→Succeeded / REPAIRABLE→fresh-attempt / BLOCKED→VerifyHalt fail-closed, with a red gate dominating APPROVED controller-side.
- Anti-gaming is enforced structurally (true-positive + true-negative proven); infra-retry stays distinct; the loop is bounded and resumable.
</success_criteria>

<output>
Create `.planning/phases/51-the-task-loop/51-07-SUMMARY.md` when done.
</output>
