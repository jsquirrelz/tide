---
phase: 51-the-task-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/provider.go
  - pkg/dispatch/vendor_capabilities.go
  - pkg/dispatch/vendor_capabilities_test.go
  - cmd/tide-langgraph-verifier/verifier/__main__.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py
autonomous: true
requirements: [TASK-04, OBS-03]
must_haves:
  truths:
    - "SelfInstruments(\"langgraph\") returns true; every other vendor returns false"
    - "The verifier image refuses to run when Provider.Vendor != \"langgraph\""
    - "The verifier entrypoint sets TIDE_GATE_COMMAND from env.verify.gateCommand before the agent runs"
    - "A non-zero gate-command exit forces the verdict to REPAIRABLE/BLOCKED regardless of any LLM APPROVED"
    - "The assembled verdict is written into EnvelopeOut.Verdict with a blocker/gate-command finding carrying exit_code=N"
  artifacts:
    - path: "pkg/dispatch/vendor_capabilities.go"
      provides: "SelfInstruments langgraph case"
      contains: "case \"langgraph\""
    - path: "cmd/tide-langgraph-verifier/verifier/__main__.py"
      provides: "vendor sentinel flip + TIDE_GATE_COMMAND injection + out-of-band deterministic verdict assembly"
      contains: "SUPPORTED_VENDOR"
  key_links:
    - from: "cmd/tide-langgraph-verifier/verifier/__main__.py (deterministic gate capture)"
      to: "EnvelopeOut.Verdict"
      via: "verdict assembly forcing non-APPROVED on non-zero exit"
      pattern: "exit_code"
---

<objective>
Register the `"langgraph"` vendor sentinel (Go predicate) and make the Phase-48 verifier image actually PRODUCE a dominant verdict: flip its vendor refusal, inject `TIDE_GATE_COMMAND` from the envelope, capture the gate exit out-of-band, and assemble a `GateDecision` where a red gate ALWAYS dominates an LLM APPROVED (the milestone's raison d'être).

Purpose: D-02 requires `SelfInstruments("langgraph")=true` (reporter skips events.jsonl synthesis) and the image to refuse other vendors at startup. D-06/TASK-04 require the deterministic gate exit to dominate any probabilistic judge — enforced structurally in the verifier (this plan) and re-checked controller-side (Plan 07). This is the verifier-side half of the anti-silent-Complete fix.
Output: Go vendor sentinel + verifier-image verdict assembly with out-of-band deterministic dominance + pytest dominance case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-the-task-loop/51-CONTEXT.md
@.planning/phases/51-the-task-loop/51-RESEARCH.md
@.planning/phases/51-the-task-loop/51-PATTERNS.md

<interfaces>
From pkg/dispatch/vendor_capabilities.go: `func SelfInstruments(vendor string) bool` (:38) — every case returns false today; pure vendor predicate, NO signature change (preserves the Phase-45 ADAPT-01 seam).
From pkg/dispatch/provider.go: `ProviderSpec.Vendor` doc-comment canonical set (:36-40) — "anthropic","openai","google","xai","opencode".
From pkg/dispatch/verdict.go: `VerdictApproved`/`VerdictRepairable`/`VerdictBlocked`, `type GateDecision`, `type Finding`, `highSeverityFindingToken="blocker"` — the verdict shape the Python assembly mirrors (verdict.py, Phase 49).
From cmd/tide-langgraph-verifier/verifier/: `__main__.py` `SUPPORTED_VENDOR = "anthropic"` (:30) with forward-note `# The "langgraph" sentinel question is deferred to Phase 51`; injectable seams `build_model`/`run_agent_fn` (:74-75) for offline tests; `tools.py` `run_gate_command` reads ONLY `TIDE_GATE_COMMAND` (:56), fail-closed if empty, `del command` discards model arg (:157 `# noqa: S602` justified by orchestrator-set invariant); `verdict.py` `GateDecision`/`classify_verdict` (Phase 49, reuse).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the "langgraph" vendor sentinel (Go)</name>
  <files>pkg/dispatch/provider.go, pkg/dispatch/vendor_capabilities.go, pkg/dispatch/vendor_capabilities_test.go</files>
  <read_first>
    - pkg/dispatch/vendor_capabilities.go (SelfInstruments :38 — the file being modified)
    - pkg/dispatch/provider.go (Vendor doc-comment canonical set :36-40)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"pkg/dispatch/vendor_capabilities.go (MOD) + provider.go"
  </read_first>
  <behavior>
    - SelfInstruments("langgraph") → true.
    - SelfInstruments("anthropic"/"openai"/"google"/"xai"/"opencode") → false.
    - SelfInstruments("") and any unknown → false (fail-closed default).
  </behavior>
  <action>
    Add `case "langgraph": return true` to `SelfInstruments` with a doc-comment (self-instruments via openinference-instrumentation-langchain; reporter skips events.jsonl synthesis). Keep the signature unchanged. Add `"langgraph"` to the `ProviderSpec.Vendor` canonical-set doc-comment in provider.go (the doc-comment explicitly governs the sentinel set). Add/extend `vendor_capabilities_test.go` with a table covering true for langgraph and false for the five existing vendors + empty/unknown.
  </action>
  <acceptance_criteria>
    - `grep -E 'case "langgraph"' pkg/dispatch/vendor_capabilities.go` returns the true case.
    - `grep -E 'langgraph' pkg/dispatch/provider.go` returns the doc-comment addition.
    - `go test ./pkg/dispatch/... -run SelfInstruments -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/dispatch/... -run SelfInstruments -count=1</automated>
  </verify>
  <done>SelfInstruments("langgraph") is true, all others false, doc-comment updated, test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verifier entrypoint — vendor flip, TIDE_GATE_COMMAND injection, out-of-band deterministic verdict assembly</name>
  <files>cmd/tide-langgraph-verifier/verifier/__main__.py, cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py</files>
  <read_first>
    - cmd/tide-langgraph-verifier/verifier/__main__.py (SUPPORTED_VENDOR :30, injectable seams :74-75 — the file being modified)
    - cmd/tide-langgraph-verifier/verifier/tools.py (run_gate_command :126, TIDE_GATE_COMMAND :56, fail-closed-if-empty)
    - cmd/tide-langgraph-verifier/verifier/verdict.py (GateDecision/Finding/classify_verdict — reuse, do not re-port)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"Pitfall 1" + §"Open Questions #3" (out-of-band capture recommendation) + §"Pattern 4"
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"cmd/tide-langgraph-verifier/verifier/__main__.py" (Analogs A/B/C/D)
  </read_first>
  <behavior>
    - Entrypoint with Provider.Vendor != "langgraph" → refuses at startup (fail-fast, mirrors the anthropic subagent vendor refusal).
    - Given env.verify.gateCommand set, the entrypoint sets TIDE_GATE_COMMAND before the agent runs.
    - Gate command exit non-zero: assembled EnvelopeOut.Verdict is REPAIRABLE or BLOCKED even if the injected/fake LLM returns APPROVED — proven with the injectable build_model/run_agent_fn seam returning a fake APPROVED verdict.
    - Gate command exit zero + LLM APPROVED: verdict may be APPROVED.
    - The non-zero case emits a Finding with dimension="gate-command", severity="blocker", evidence containing exit_code=N.
    - Empty/absent TIDE_GATE_COMMAND stays fail-closed (unchanged tools.py behavior) — no gate → verdict is not APPROVED.
  </behavior>
  <action>
    Flip `SUPPORTED_VENDOR = "anthropic"` → `"langgraph"` (D-02); the vendor-refusal branch now rejects any other vendor. In the entrypoint, extract `env.verify.gateCommand` and set `os.environ["TIDE_GATE_COMMAND"]` before constructing/running the agent (the tool reads only this orchestrator-set env). Implement the out-of-band deterministic capture (OQ3 recommendation): run the same `TIDE_GATE_COMMAND` once deterministically in the entrypoint, capture its returncode independently of the LLM's tool narration, and in verdict assembly FORCE `Verdict ∈ {REPAIRABLE, BLOCKED}` on non-zero regardless of the LLM's verdict — emit a `blocker`/`gate-command` `Finding` carrying `exit_code=N`. Reuse `verdict.py` `GateDecision`/`classify_verdict` (no re-port). Write the assembled `GateDecision` into `EnvelopeOut.Verdict`. Add a pytest to `tests/test_verdict.py` driving the full flow via the `build_model`/`run_agent_fn` injectable seams with a fake chat model returning APPROVED, asserting a red gate downgrades the verdict AND stamps the gate-command finding.
  </action>
  <acceptance_criteria>
    - `grep -E 'SUPPORTED_VENDOR\s*=\s*"langgraph"' cmd/tide-langgraph-verifier/verifier/__main__.py` returns the flipped sentinel.
    - `grep -E 'TIDE_GATE_COMMAND' cmd/tide-langgraph-verifier/verifier/__main__.py` shows the entrypoint SETS it (not just reads).
    - `grep -Ei 'gate-command|exit_code' cmd/tide-langgraph-verifier/verifier/__main__.py` returns the finding-assembly line(s).
    - `make test-langgraph-verifier` exits 0 (pytest suite incl. the new dominance case green).
  </acceptance_criteria>
  <verify>
    <automated>make test-langgraph-verifier</automated>
  </verify>
  <done>The verifier image refuses non-langgraph vendors, injects TIDE_GATE_COMMAND, and structurally forces a red gate to dominate an LLM APPROVED — proven by pytest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM judge → assembled verdict | The probabilistic judge's textual verdict must not override the deterministic machine result |
| orchestrator env → verifier gate command | `TIDE_GATE_COMMAND` is orchestrator-set from the locked contract, never a model-supplied tool argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02 | Tampering / Repudiation | LLM judge silently overriding a red gate (the 2026-07-03 silent-Complete) | mitigate | Out-of-band deterministic exit capture forces non-APPROVED on non-zero regardless of the LLM verdict; a blocker/gate-command finding carries the structural signal for Plan 07's controller-side re-check. Residual: low (defence-in-depth completed in Plan 07) |
| T-51-02b | Elevation / Tampering | command injection via gateCommand | mitigate | Tool reads ONLY the orchestrator-set `TIDE_GATE_COMMAND`; `del command` discards any model-supplied arg; gate runs in the RO verifier pod, not the manager |
| T-51-02c | Spoofing | wrong image accepting a foreign vendor | mitigate | Startup vendor refusal — image runs only for Vendor=="langgraph" |
| T-51-SC | Tampering | package installs | N/A | No new packages; the verifier image deps are already patch-exact pinned + CI-gated (Phase 48) |
</threat_model>

<verification>
- `go test ./pkg/dispatch/... -run SelfInstruments -count=1` green.
- `make test-langgraph-verifier` green (incl. deterministic-dominance case).
- `make lint` clean for the touched Go files.
</verification>

<success_criteria>
- SelfInstruments("langgraph")=true; image refuses other vendors.
- A red gate command deterministically dominates an LLM APPROVED in the assembled verdict (the milestone's core integrity control, verifier-side).
</success_criteria>

<output>
Create `.planning/phases/51-the-task-loop/51-02-SUMMARY.md` when done.
</output>
