---
phase: 51-the-task-loop
plan: 05
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - internal/controller/verify_halt.go
  - internal/controller/verify_halt_test.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/task_controller.go
  - internal/controller/project_controller.go
  - internal/controller/co_occurring_holds_test.go
autonomous: true
requirements: [ESC-02, ESC-03]
must_haves:
  truths:
    - "checkVerifyHalt/setVerifyHaltIfNeeded exist mirroring failure_halt.go including the Phase-25 resume time-fence"
    - "checkVerifyHalt gates the planner tier (checkDispatchHolds) and the task tier (gateChecks)"
    - "gateChecks is migrated onto checkDispatchHolds — one uniform hold order across all five levels"
    - "the Project planner chain now calls checkFailureHalt AND checkVerifyHalt (folds the missing-gate todo)"
    - "a VerifyHalt leaves phase/wave-siblings/conservative-profile propagation untouched (distinct from Failed)"
  artifacts:
    - path: "internal/controller/verify_halt.go"
      provides: "checkVerifyHalt + setVerifyHaltIfNeeded (clone of failure_halt.go + time-fence)"
      contains: "setVerifyHaltIfNeeded"
    - path: "internal/controller/co_occurring_holds_test.go"
      provides: "co-occurring-holds envtest pinning uniform hold order across levels"
      contains: "CoOccurring"
  key_links:
    - from: "internal/controller/dispatch_helpers.go (checkDispatchHolds)"
      to: "internal/controller/verify_halt.go (checkVerifyHalt)"
      via: "hold chain insertion adjacent to checkFailureHalt"
      pattern: "checkVerifyHalt"
    - from: "internal/controller/task_controller.go (gateChecks)"
      to: "internal/controller/dispatch_helpers.go (checkDispatchHolds)"
      via: "gateChecks migrated onto the shared chain"
      pattern: "checkDispatchHolds"
---

<objective>
Add `ConditionVerifyHalt` as a distinct halt class by cloning `failure_halt.go` into `verify_halt.go` (including the Phase-25 resume time-fence), wire `checkVerifyHalt` into BOTH the planner tier (`checkDispatchHolds`) and the task tier (`gateChecks`), and — in the same change — unify the five dispatch-hold chains: migrate `gateChecks` onto `checkDispatchHolds` (normalizing Task's Import-order divergence) and add the missing `checkFailureHalt`+`checkVerifyHalt` to the Project planner chain. Folds both carried-forward W-2 dispatch-gate todos.

Purpose: ESC-02 requires `ConditionVerifyHalt` to mirror `failure_halt.go` and gate both tiers; ESC-03 requires it to be a distinct halt class (never a reinterpretation of `Failed` wave semantics). D-09 folds the two dispatch-gate todos here because this phase edits exactly those chains — a structural fix (shared chain > per-site divergence), gated behind a deliberate co-occurring-holds envtest.
Output: verify_halt.go + unified hold chains + co-occurring-holds + distinct-class regression tests. The exhaustion TRIGGER that calls setVerifyHaltIfNeeded lands in Plan 07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-the-task-loop/51-CONTEXT.md
@.planning/phases/51-the-task-loop/51-RESEARCH.md
@.planning/phases/51-the-task-loop/51-PATTERNS.md
@.planning/phases/51-the-task-loop/51-01-SUMMARY.md

<interfaces>
From internal/controller/failure_halt.go: `func checkFailureHalt(project *Project) bool` (:56), `func setFailureHaltIfNeeded(ctx, c, project, taskCompletedAt time.Time) error` (:79) — self-contained 2-func file with the CR-02 resume time-fence (:90-103); header (:33-37) notes it is NOT added to project_controller.go (execution-only) — Plan 51 REVERSES this for VerifyHalt and retro-adds FailureHalt to the Project chain.
From api/v1alpha3/shared_types.go (Plan 01): `ConditionVerifyHalt`, `ReasonVerifyExhausted`, `AnnotationVerifyResumedAt`.
From internal/controller/dispatch_helpers.go: `func checkDispatchHolds(ctx, project, level, objName string) (held bool, result ctrl.Result)` (:580) — chain order Billing→Failure→Budget→Import; the doc-comment (:571-576) documents TaskReconciler intentionally NOT calling it (to be reversed).
From internal/controller/task_controller.go: `func (r *TaskReconciler) gateChecks(...)` (:334) — inline chain with the documented Import-order divergence (:390-402); terminal short-circuit + FailureHalt-at-terminal with CR-02 time-fence (:336-360).
From internal/controller/project_controller.go: planner-dispatch hold block (:1540) — has checkBillingHalt + checkBudgetBlocked + Import but NO checkFailureHalt (the gap, folds todo #1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify_halt.go — clone failure_halt.go with exhaustion trigger + resume time-fence</name>
  <files>internal/controller/verify_halt.go, internal/controller/verify_halt_test.go</files>
  <read_first>
    - internal/controller/failure_halt.go (the file being cloned: checkFailureHalt :56, setFailureHaltIfNeeded :79, CR-02 time-fence :90-103, header :33-37)
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"internal/controller/verify_halt.go (NEW)" (the full clone excerpt + divergences)
    - api/v1alpha3/shared_types.go (ConditionVerifyHalt/ReasonVerifyExhausted/AnnotationVerifyResumedAt from Plan 01)
  </read_first>
  <behavior>
    - checkVerifyHalt(nil) → false; checkVerifyHalt(project with ConditionVerifyHalt true) → true.
    - setVerifyHaltIfNeeded is idempotent (no-op if already set).
    - The resume time-fence: a completion timestamp BEFORE AnnotationVerifyResumedAt does NOT re-stamp (stale pre-resume straggler); zero/unparseable timestamp → fail-closed to stamp.
    - Trigger differs from FailureHalt: setVerifyHaltIfNeeded stamps on LOOP EXHAUSTION (a non-APPROVED verdict with attempts exhausted / onExhaustion), NOT on FailureProfile==conservative.
  </behavior>
  <action>
    Create `internal/controller/verify_halt.go` cloning `failure_halt.go` file-for-file, swapping Failure→Verify vocabulary (ConditionVerifyHalt / ReasonVerifyExhausted / AnnotationVerifyResumedAt) and KEEPING the CR-02 resume time-fence verbatim (a fresh impl re-introduces the pre-resume-straggler re-freeze bug). Diverge only in the set trigger: gate on loop exhaustion (caller passes the exhaustion signal), not on `FailureProfile == conservative`. Per Claude's-discretion D-09, keep it a hand-synced CLONE (matches the BillingHalt→FailureHalt→VerifyHalt three-generation precedent; the metriccardinality precedent favors deliberate non-sharing of guard layers). Update the header doc-comment to state VerifyHalt DOES gate the Project chain (reversing failure_halt's execution-only note). Add verify_halt_test.go mirroring the failure_halt test suite, covering the four behaviors incl. the time-fence.
  </action>
  <acceptance_criteria>
    - `grep -cE 'func (checkVerifyHalt|setVerifyHaltIfNeeded)' internal/controller/verify_halt.go` == 2.
    - `grep -E 'AnnotationVerifyResumedAt' internal/controller/verify_halt.go` returns the time-fence use.
    - `grep -iE 'exhaust' internal/controller/verify_halt.go` returns the exhaustion-trigger doc/logic (NOT a FailureProfile check).
    - `go test ./internal/controller/... -run 'VerifyHalt' -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/controller/... -run 'VerifyHalt' -count=1</automated>
  </verify>
  <done>verify_halt.go mirrors failure_halt.go with the resume time-fence, triggers on exhaustion, and its test suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify the dispatch-hold chains (wire checkVerifyHalt both tiers; migrate gateChecks; fix Project chain)</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/task_controller.go, internal/controller/project_controller.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go (checkDispatchHolds :580-620, chain order, doc-comment :571-576)
    - internal/controller/task_controller.go (gateChecks :334, Import-order divergence comment :390-402, terminal FailureHalt time-fence :336-360)
    - internal/controller/project_controller.go (planner dispatch block :1540 — missing checkFailureHalt)
    - .planning/phases/51-the-task-loop/51-RESEARCH.md §"Pitfall 3" (co-occurring holds divergence) + §"Common Pitfalls"
    - .planning/phases/51-the-task-loop/51-PATTERNS.md §"dispatch_helpers.go" + §"task_controller.go (1)" + §"project_controller.go"
    - .planning/todos/pending/2026-07-12-task-dispatch-gate-order-divergence.md + 2026-07-12-project-dispatch-missing-failurehalt-gate.md
  </read_first>
  <behavior>
    - checkDispatchHolds returns held=true for a project under VerifyHalt (inserted adjacent to the Failure hold, uniform order Billing→Failure→Verify→Budget→Import).
    - gateChecks routes its hold decisions through checkDispatchHolds (no longer an inline divergent chain) — the same hold fires for a Task as for a Plan under co-occurring holds.
    - The Project planner chain holds under a conservative-profile FailureHalt AND under VerifyHalt (previously it spent under both).
    - A VerifyHalt does NOT change any level's phase, wave-sibling dispatch, or conservative-profile propagation (distinct-class regression).
  </behavior>
  <action>
    In dispatch_helpers.go, insert `checkVerifyHalt(project)` into `checkDispatchHolds` adjacent to the `checkFailureHalt` hold (30s requeue), preserving the load-bearing chain order → Billing→Failure→Verify→Budget→Import; update the doc-comment (:571-576) since gateChecks now DOES call it. In task_controller.go, migrate `gateChecks` onto `checkDispatchHolds` (D-09 Option 1) — remove the inline divergent chain (Import-checked-second), delegate to the shared chain, normalizing the order; keep the existing terminal FailureHalt-at-terminal + CR-02 time-fence hook and add the parallel VerifyHalt-at-terminal hook shape (the exhaustion trigger itself is Plan 07). In project_controller.go, add BOTH `checkFailureHalt(project)` and `checkVerifyHalt(project)` to the planner dispatch block (:1540), matching checkDispatchHolds' order (30s requeue each) — folds todo #1. Create co_occurring_holds_test.go: an envtest pinning WHICH hold fires under simultaneous holds (e.g. Import-pending AND VerifyHalt) so it is uniform across Milestone/Phase/Plan/Task, AND a conservative-profile Project-chain case proving the Project planner now holds (not spends). Add a distinct-class (ESC-03) regression asserting a VerifyHalt leaves phase / wave-siblings / conservative-profile propagation untouched.
  </action>
  <acceptance_criteria>
    - `grep -c 'checkVerifyHalt' internal/controller/dispatch_helpers.go` ≥ 1 AND `grep -c 'checkVerifyHalt' internal/controller/project_controller.go` ≥ 1 AND `grep -c 'checkFailureHalt' internal/controller/project_controller.go` ≥ 1.
    - `grep -c 'checkDispatchHolds' internal/controller/task_controller.go` ≥ 1 (gateChecks now delegates).
    - `grep -iE 'CoOccurring' internal/controller/co_occurring_holds_test.go` returns the test.
    - `go test ./internal/controller/... -run 'CoOccurring|VerifyHalt|DispatchHolds' -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/controller/... -run 'CoOccurring|VerifyHalt|DispatchHolds' -count=1</automated>
  </verify>
  <done>checkVerifyHalt gates both tiers; gateChecks delegates to checkDispatchHolds; the Project chain holds under Failure/Verify halts; co-occurring-holds + distinct-class tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| halt condition → dispatch gate | A halted project must stop spending across ALL five dispatch chains, uniformly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05 | DoS (spend) | Project planner dispatching under a conservative halt | mitigate | Add checkFailureHalt+checkVerifyHalt to the Project chain (folds the missing-gate todo) — the planner stops spending under a halt; proven by the conservative-profile envtest |
| T-51-05b | Tampering (behavior divergence) | different hold firing by level under co-occurring holds | mitigate | Migrate gateChecks onto checkDispatchHolds (uniform order); co-occurring-holds envtest pins which hold fires |
| T-51-05c | Tampering | VerifyHalt re-freezing after a resume (pre-resume straggler) | mitigate | CR-02 resume time-fence carried verbatim into verify_halt.go; test covers the pre-resume-completion no-op |
| T-51-05d | Availability | VerifyHalt mis-reinterpreting Failed wave semantics | mitigate | Distinct-class regression asserts phase/wave-siblings/conservative-profile untouched (ESC-03) |
| T-51-SC | Tampering | package installs | N/A | No package installs — controller Go only |
</threat_model>

<verification>
- `go test ./internal/controller/... -run 'VerifyHalt|CoOccurring|DispatchHolds|FailureHalt' -count=1` green (FailureHalt suite must stay green after the Project-chain + gateChecks migration).
- `make lint` clean.
- `make test-int` note: MAKE_EXIT ≠ Ginkgo-green — grep `^--- FAIL|^FAIL\s` in the log; the co-occurring-holds behavior change is deliberate and tested.
</verification>

<success_criteria>
- ConditionVerifyHalt is a distinct halt class gating both tiers, mirroring failure_halt.go + the resume time-fence.
- The five dispatch-hold chains carry a uniform hold order; the Project chain no longer spends under a halt.
- Both W-2 dispatch-gate todos are folded and closed by tested behavior changes.
</success_criteria>

<output>
Create `.planning/phases/51-the-task-loop/51-05-SUMMARY.md` when done.
</output>
