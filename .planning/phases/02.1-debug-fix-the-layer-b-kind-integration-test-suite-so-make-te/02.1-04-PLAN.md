---
phase: 02.1
plan: 04
type: execute
wave: 3
depends_on:
  - "02.1-02"
  - "02.1-03"
files_modified:
  - .github/workflows/ci.yaml
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - roadmap-goal-ci-green
  - roadmap-goal-idempotency
  - roadmap-goal-phase-closeout
user_setup: []
must_haves:
  truths:
    - "The `test-int` CI job in `.github/workflows/ci.yaml` installs Helm 3 BEFORE running `make test-int` (the new BeforeSuite path from Plan 02 requires `helm` in PATH; without this step the suite soft-skips and CI silently passes with no Layer B coverage)."
    - "The `test-int` CI job sets `TIDE_REQUIRE_CONTROLLER: \"1\"` in its env block per D-03 in `02.1-BASELINE.md` so install failures are loud (the CI exits non-zero instead of soft-skipping). D-03 source: 02.1-BASELINE.md §Decision Log §D-03 — copy that decision's prescriptive text verbatim into the env-block YAML comment."
    - "`make test-int` executed twice consecutively against the SAME kind cluster (with `KEEP_KIND_CLUSTER=true` on the first run) completes both runs successfully in ≤ 300s wall-time each — second run uses the existing cluster + existing helm release, exercising the chart's `lookup` + resource-policy: keep guards on tide-signing-key Secret + tide-projects PVC."
    - "`make test-int` from a CLEAN state (no kind cluster, no helm release) completes in ≤ 300s wall-time with all 7 Layer B specs passing AND exit code 0."
    - "ROADMAP.md Phase 02.1 plan rows are flipped from `- [ ]` to `- [x]` for all four plans. The Goal line, **Plans:** count line, and Requirements line are LEFT UNCHANGED — they were already populated by the planner during initial plan authoring (verify by reading the current file before editing)."
    - "STATE.md `last_activity`, `last_updated`, and a new 'Phase 02.1 closed' entry are added. The frontmatter fields `completed_phases`, `completed_plans`, `total_plans`, `percent`, and the body Progress bar are LEFT UNCHANGED in this plan — they exhibit pre-existing internal inconsistencies (frontmatter `completed_phases: 3` vs ROADMAP top-level checklist showing Phases 1+2 unchecked; frontmatter `percent: 50` vs body `83%`); reconciling those is OUT OF SCOPE for Phase 02.1 closeout and is documented as a known issue."
    - "No production code in `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/` is modified by this plan — the fix work is done; this plan only wires CI + closes out the phase."
  artifacts:
    - path: .github/workflows/ci.yaml
      provides: "CI test-int job that installs Helm + sets TIDE_REQUIRE_CONTROLLER=1 so Layer B failures fail the build"
      contains: "azure/setup-helm"
    - path: .planning/ROADMAP.md
      provides: "Phase 02.1 plan rows flipped to checked"
      contains: "[x] 02.1-01-PLAN.md"
    - path: .planning/STATE.md
      provides: "Phase 02.1 closeout entry; last_activity/last_updated bumped"
      contains: "Phase 02.1 closed"
  key_links:
    - from: ".github/workflows/ci.yaml test-int job"
      to: "azure/setup-helm@v4 action"
      via: "new install-step before make test-int"
      pattern: "azure/setup-helm"
    - from: ".github/workflows/ci.yaml test-int job env block"
      to: "test/integration/kind/suite_test.go WR-09 gate"
      via: "TIDE_REQUIRE_CONTROLLER=1 forces hard-fail on missing controller"
      pattern: "TIDE_REQUIRE_CONTROLLER"
---

<objective>
Close out Phase 02.1 by: (a) wiring the CI `test-int` job so it has `helm` in PATH and `TIDE_REQUIRE_CONTROLLER=1` set, (b) verifying idempotency (running `make test-int` twice consecutively against the same cluster succeeds within 300s each) AND clean-start correctness (running from a deleted cluster also succeeds within 300s), and (c) updating ROADMAP.md + STATE.md with minimal, targeted edits that reflect Phase 02.1 completion without inventing fixes for pre-existing planning-artifact inconsistencies.

This plan depends on BOTH Plan 02 (helm-install BeforeSuite pivot) AND Plan 03 (credproxy banner). Without both fixes, the Layer B suite has at least one failing spec, and Plan 04 cannot truthfully mark the phase complete.

This plan does NOT modify any production code. It modifies only CI + planning artifacts. The fix work was done in Plans 02 + 03; Plan 04 is the closeout — AND it is the runtime gate that proves the source-code changes in Plans 02 + 03 actually result in a green end-to-end suite (this plan's Task 2 is where the "≥6/7 specs pass after Plan 02; 7/7 specs pass after Plans 02 + 03" claim is verified).

Output: A modified `.github/workflows/ci.yaml` with helm-install + TIDE_REQUIRE_CONTROLLER env; an updated `.planning/ROADMAP.md` with the four Phase 02.1 plan rows flipped to checked; an updated `.planning/STATE.md` with the Roadmap Evolution log appended and timestamps bumped. Pre-existing STATE.md/ROADMAP.md inconsistencies (completed_phases vs top-level checklist; frontmatter percent vs body bar) are documented as known issues but NOT reconciled here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-RESEARCH.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-VALIDATION.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-02-SUMMARY.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-03-SUMMARY.md
@./CLAUDE.md
@.github/workflows/ci.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire helm + TIDE_REQUIRE_CONTROLLER into the test-int CI job</name>
  <files>.github/workflows/ci.yaml</files>
  <read_first>
    - .github/workflows/ci.yaml (the FULL file; particularly the existing `helm-lint` job at lines 102-153 — that job already uses `azure/setup-helm@v4 with: version: 'v3.16.3'` so the version pin matches; and the `test-int` job at lines 155-219 — that job currently has NO helm install step)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md (§Decision Log §D-03 — copy D-03's prescriptive text verbatim into the env-block YAML comment so future readers can trace the env var back to the locking decision)
    - test/integration/kind/suite_test.go (the Plan-02-modified applyController() which gates on `exec.LookPath("helm")` — without helm, it soft-skips with `GinkgoWriter.Println("helm not in PATH; falling back to CRDs-only mode")`)
  </read_first>
  <behavior>
    - The `test-int` job in `.github/workflows/ci.yaml` has a new step `Install Helm` between `Install kind v0.31.0` (currently at lines 173-178) and `Prepare manifests and envtest binaries` (currently at lines 181-184).
    - The new step uses `uses: azure/setup-helm@v4` and `version: 'v3.16.3'` — matching the existing helm-lint job's helm version pin (no new version drift introduced).
    - The `test-int` job declares `env: { TIDE_REQUIRE_CONTROLLER: "1" }` at the job level (so all steps see it, including the `make test-int` step). This makes `make test-int` exit non-zero when the controller doesn't reach Ready (instead of soft-skipping per WR-09). A YAML comment above the env entry cross-references `02.1-BASELINE.md §Decision Log §D-03` as the canonical source of this decision (so a future reader can locate the locking rationale verbatim).
    - The Phase 1 `test` job and the `helm-lint` job are UNCHANGED — no regression on the existing CI surface.
    - The job's `timeout-minutes: 6` is preserved (the outer CI fence; the inner `timeout 300s` inside `make test-int` is the budget gate).
    - The failure-collection steps (`Collect kind cluster logs on failure` and `Upload kind logs artifact on failure` at lines 204-219) are preserved.
    - `yamllint` / GitHub Actions YAML syntax is valid (no tabs; consistent 2-space indentation; the file passes `actionlint` if available).
    - The file remains gofmt/yamlfmt-clean.
  </behavior>
  <action>
    Edit `.github/workflows/ci.yaml`. In the `test-int:` job block (currently starting at line 155):

      1. Add an env block at the job level. Insert directly under `permissions: contents: read` (currently at line 159-160), at the same indentation as `permissions:`, the new block. The YAML comment must cross-reference `02.1-BASELINE.md §D-03` so a future reader can locate the decision text verbatim (per W-07 — D-03 wording lives in BASELINE.md, this file just points to it):

         ```
             env:
               # Phase 02.1 / WR-09 / D-03 source: .planning/phases/02.1-.../02.1-BASELINE.md §Decision Log §D-03.
               # D-03 prescribes: KEEP TIDE_REQUIRE_CONTROLLER default (unset = soft-skip)
               # for dev runs; CI MUST set =1 so install failures fail the CI build loudly.
               TIDE_REQUIRE_CONTROLLER: "1"
         ```

         When the executor edits this file, after creating 02.1-BASELINE.md in Plan 01, it should optionally re-read BASELINE.md §D-03 and use a 1-2 sentence quote of D-03's prescriptive text in the comment (paraphrase OK, but the file path + section reference MUST appear so the chain of evidence is browsable).

      2. Add a `Install Helm` step. Insert directly AFTER the existing `Install kind v0.31.0` step (currently ending at line 178) and BEFORE `Prepare manifests and envtest binaries` (currently starting at line 181), at the same indentation as the surrounding steps:

         ```
               - name: Install Helm
                 uses: azure/setup-helm@v4
                 with:
                   version: 'v3.16.3'
         ```

         The version pin must match the existing `helm-lint` job's pin at lines 121-123 verbatim.

      3. Do NOT modify any other step. Do NOT delete the cluster-logs-on-failure block. Do NOT change `timeout-minutes`. Do NOT change the Phase 1 `test` job or the `helm-lint` job.

      4. Verify the YAML is syntactically valid: indentation is 2 spaces, no tabs, no trailing whitespace on the new lines. The existing file uses `azure/setup-helm@v4` in the helm-lint job (line 121), so action availability is already established.

    Save and stop. The actual CI run validates the workflow on push.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && grep -A2 "azure/setup-helm" .github/workflows/ci.yaml | grep -c "v3.16.3" | awk '{ if ($1 == 2) exit 0; else exit 1 }' && grep -c "TIDE_REQUIRE_CONTROLLER" .github/workflows/ci.yaml | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "02.1-BASELINE.md" .github/workflows/ci.yaml | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))" && grep -c "^  test-int:" .github/workflows/ci.yaml | awk '{ if ($1 == 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))"` exits 0 (valid YAML).
    - `grep -c "azure/setup-helm" .github/workflows/ci.yaml` returns 2 (one in helm-lint, one new in test-int).
    - `grep -c "TIDE_REQUIRE_CONTROLLER" .github/workflows/ci.yaml` returns ≥ 1 (the new env block).
    - `grep -c "02.1-BASELINE.md" .github/workflows/ci.yaml` returns ≥ 1 (the D-03 source xref comment present per W-07).
    - The Phase 1 `test:` job exists unchanged: `grep -c "^  test:$" .github/workflows/ci.yaml` returns 1.
    - The `helm-lint:` job exists unchanged: `grep -c "^  helm-lint:$" .github/workflows/ci.yaml` returns 1.
    - The `test-int:` job exists with the new env + helm step: `grep -c "^  test-int:$" .github/workflows/ci.yaml` returns 1.
  </acceptance_criteria>
  <done>
    - CI workflow has helm installed before `make test-int` runs.
    - CI workflow sets TIDE_REQUIRE_CONTROLLER=1 with a D-03 cross-reference comment.
    - Phase 1 `test` job and `helm-lint` job preserved.
    - YAML is syntactically valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run end-to-end verification (clean start + idempotency) and capture results</name>
  <files>.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md (the BEFORE-FIX score for the per-spec outcome table; reuse the same 7-row table shape)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-02-SUMMARY.md (Plan 02 outcome — confirms source-code shape changes landed; runtime spec count proven HERE in this task)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-03-SUMMARY.md (Plan 03 outcome — credproxy banner emitted; runtime spec count proven HERE in this task)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-VALIDATION.md (the validation strategy — full make test-int is the phase gate)
  </read_first>
  <behavior>
    - A new artifact `02.1-04-VERIFICATION.md` exists in the phase directory capturing the post-fix score: 7/7 Layer B specs passing on a clean run; 7/7 Layer B specs passing on an idempotent re-run.
    - The artifact contains the verbatim commands run for clean-start verification and idempotent re-run.
    - The artifact contains the AFTER-FIX per-spec outcome table (same 7 rows as 02.1-BASELINE.md Section 3) showing all 7 PASS.
    - Wall-time for each run is recorded; both runs MUST complete in ≤ 300s (the TEST-02 budget gate from Phase 2 Plan 13).
    - If ANY spec fails OR wall-time > 300s on either run OR idempotency breaks (second run fails), this artifact records the failure as `BLOCKED` and the plan halts BEFORE Task 3 (the ROADMAP/STATE updates). Phase 02.1 cannot close with a partial fix or with a busted budget.
    - This task is the RUNTIME gate that Plans 02 + 03 deferred: `must_haves.dataflow` in Plan 02 and the integration-test acceptance criterion in Plan 03 both point HERE for the end-to-end proof.
  </behavior>
  <action>
    Step 1 — CLEAN START verification:
      (a) `kind delete cluster --name tide-test 2>&1 || true`
      (b) `helm uninstall tide -n tide-system 2>&1 || true`
      (c) Capture wall-time and exit code into separate variables: `START=$(date +%s); make test-int 2>&1 | tee /tmp/02.1-04-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean_run: exit=$EXIT wall=${WALL}s"`
      (d) Record per-spec outcomes from the Ginkgo output (use the same 7-row table shape as 02.1-BASELINE.md Section 3).
      (e) ASSERT both `EXIT == 0` AND `WALL <= 300`. If either fails, gate decision is BLOCKED.

    Step 2 — IDEMPOTENCY verification:
      (a) WITHOUT deleting the kind cluster, immediately re-run with the same wall-time + exit-code capture pattern: `START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.1-04-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"`
      (b) The second run reuses the existing kind cluster AND the existing helm release. The chart's `lookup` + `resource-policy: keep` guards on `tide-signing-key` Secret + `tide-projects` PVC make this safe (HARN-03 / Phase 2 design).
      (c) Record wall-time + exit code + per-spec outcomes.
      (d) ASSERT both `EXIT == 0` AND `WALL <= 300`. If either fails, gate decision is BLOCKED.

    Step 3 — Create `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md` with sections:
      - Header (phase, plan, date, hostname)
      - Section 1 — Clean Start Run (reproducer commands; per-spec outcome table; wall-time; exit code; **explicit assertion line:** `Wall-time gate: ${WALL}s ≤ 300s → PASS` or `→ FAIL`)
      - Section 2 — Idempotent Re-run (reproducer commands; per-spec outcome table; wall-time; exit code; same explicit wall-time assertion line; explicit note that helm + chart's lookup guards make the re-install no-op)
      - Section 3 — Before/After Comparison (a small table: number of passing specs BEFORE per 02.1-BASELINE.md vs AFTER; expected: 0/7 → 7/7 or BEFORE→AFTER as captured)
      - Section 4 — Wall-time Budget Check (consolidate Section 1 + Section 2 wall-times; assert BOTH ≤ 300s; if one exceeded the budget, note it explicitly — RESEARCH.md A3 said the 90s waitForControllerReady ceiling becomes a no-op once helm --wait does the work; verify by observing the helm install duration relative to total)
      - Section 5 — Gate Decision (explicit: "Phase 02.1 closeout APPROVED — proceeding to Task 3 (ROADMAP/STATE update)" OR "Phase 02.1 closeout BLOCKED — see Section X for failure"). APPROVED requires ALL of: (clean run exit=0) AND (clean run wall ≤ 300s) AND (rerun exit=0) AND (rerun wall ≤ 300s) AND (clean run shows 7/7 specs PASS) AND (rerun shows 7/7 specs PASS). Any one of these failing → BLOCKED.

    If Section 5 is APPROVED, gate is open and Task 3 proceeds. If BLOCKED, abort the plan; orchestrator decides next step.

    Apply the same TIDE_SIGNING_KEY sed-redaction discipline from Plan 01 (T-02.1-01-01) when quoting logs into the artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md && grep -q "Gate Decision" .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md && grep -qE 'APPROVED|BLOCKED' .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md && grep -q "Wall-time" .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md && grep -cE '(≤|<=) 300s' .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md | awk '{ if ($1 >= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `02.1-04-VERIFICATION.md` exists with 5 H2 sections.
    - Section 5 (Gate Decision) is explicitly APPROVED or BLOCKED.
    - Section 1 and Section 2 each contain an explicit wall-time assertion line (e.g., `Wall-time gate: 187s ≤ 300s → PASS`). The grep for `(≤|<=) 300s` returns ≥ 2 (one per run).
    - If APPROVED: Section 3 shows all 7 specs PASS on both runs; wall-time ≤ 300s on both runs; Task 3 proceeds.
    - If BLOCKED: Plan halts; SUMMARY.md records the partial state; orchestrator notified.
  </acceptance_criteria>
  <done>
    - Verification artifact exists with explicit gate decision AND explicit per-run wall-time assertions.
    - If APPROVED, Task 3 (ROADMAP/STATE update) is unblocked.
    - If BLOCKED, the plan stops here and surfaces the failure.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md and STATE.md to reflect Phase 02.1 closure (minimal targeted edits)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (the FULL CURRENT file — IMPORTANT: do NOT assume placeholder strings still exist; the planner replaced `[Urgent work - to be planned]` / `**Plans:** 13/13 plans complete` / `- [ ] TBD (run /gsd-plan-phase 02.1 to break down)` during initial Phase 02.1 plan authoring. Verify by reading the file FIRST; the actual current Phase 02.1 entry already has a real Goal line, a `**Plans:** 4 plans` line, and four `- [ ] 02.1-NN-PLAN.md` plan rows. The ONLY edit this task makes to ROADMAP.md is flipping those four checkboxes from `- [ ]` to `- [x]`.)
    - .planning/STATE.md (the FULL CURRENT file — IMPORTANT: this file has pre-existing internal inconsistencies that are OUT OF SCOPE for this task: frontmatter `completed_phases: 3` vs top-level ROADMAP checklist showing Phases 1 + 2 still `- [ ]` unchecked; frontmatter `percent: 50` vs body `Progress: [████████░░] 83%`. This task does NOT reconcile those inconsistencies — see <action> Step 2 for the rationale.)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md (must contain the APPROVED gate decision — if BLOCKED, this task does NOT run)
  </read_first>
  <behavior>
    - ROADMAP.md edits: ONLY the four plan-row checkboxes flip from `- [ ]` to `- [x]`. The Goal line, **Plans:** count line, Requirements line, and Depends-on line are LEFT UNCHANGED (they were correctly populated by the planner during initial plan authoring; verify by reading the file first).
    - STATE.md edits: minimal, targeted, additive only:
      - `last_updated`: bumped to a current ISO-8601 timestamp (any reasonable format the executor chooses; `2026-05-13Txx:xx:xxZ` with UTC offset accepted).
      - `last_activity`: bumped to today's date (`2026-05-13` or whatever the current date is at execute time).
      - `stopped_at` (frontmatter): updated to `"Phase 02.1 closed; ready to plan Phase 03"`.
      - `status` (frontmatter): keep `ready_to_plan` (already correct).
      - Body §Roadmap Evolution: APPEND a new bullet after the existing `Phase 02.1 inserted after Phase 2: ...` line: `- Phase 02.1 closed YYYY-MM-DD: make test-int runs end-to-end on developer laptop; all 7 Layer B Ginkgo specs pass; CI test-int job wired with helm install + TIDE_REQUIRE_CONTROLLER=1`.
      - Body §Current Position: update `Phase: 02.1` to `Phase: 03`.
      - Body Notes (new or existing section): APPEND a `Known inconsistency:` entry documenting the pre-existing frontmatter/body discrepancies so they are visible to the next planner without this plan being on the hook to fix them.
    - The following STATE.md fields are LEFT UNCHANGED in this plan (per B-03 option (a) — Plan 04 is closeout for Phase 02.1, not a STATE.md reconciliation phase):
      - `progress.completed_phases` (currently 3) — inconsistent with ROADMAP top-level checklist; reconcile separately
      - `progress.completed_plans` (currently 24) — depends on whether Phase 02.1's 4 plans count as part of the same milestone tally; existing total is itself stale
      - `progress.total_plans` (currently 24) — pre-dates Phase 02.1 and Phase 3 plans
      - `progress.percent` (currently 50)
      - Body Progress bar (`[████████░░] 83%`) — internally inconsistent with frontmatter percent in CURRENT file
    - No production code touched.
  </behavior>
  <action>
    GATE CHECK: Read `02.1-04-VERIFICATION.md` and confirm Section 5 says APPROVED. If BLOCKED, abort this task; orchestrator surfaces the failure and decides next steps.

    Step 1 — Update ROADMAP.md (minimal edits):

      Use the Read tool first to confirm the CURRENT state of the Phase 02.1 entry (around lines 95-106). The file has ALREADY been updated by the planner during initial plan authoring. The Goal line at line 97 contains real prose (not the `[Urgent work - to be planned]` placeholder). The `**Plans:** 4 plans` line at line 100 is already correct. The four plan rows at lines 103-106 are present but unchecked (`- [ ]`).

      Use the Edit tool for ONLY the following four checkbox flips (one Edit call per line, or batch into a single multi-line Edit if the surrounding text uniquely identifies each line):

      (a) `- [ ] 02.1-01-PLAN.md — Baseline capture + lock D-01/D-02/D-03 decisions (Wave 1)` → `- [x] 02.1-01-PLAN.md — Baseline capture + lock D-01/D-02/D-03 decisions (Wave 1)`
      (b) `- [ ] 02.1-02-PLAN.md — Helm-install BeforeSuite pivot + ensureSubagentSA helper (Wave 2)` → `- [x] 02.1-02-PLAN.md — Helm-install BeforeSuite pivot + ensureSubagentSA helper (Wave 2)`
      (c) `- [ ] 02.1-03-PLAN.md — Credproxy boot-banner for log-line spec (Wave 2)` → `- [x] 02.1-03-PLAN.md — Credproxy boot-banner for log-line spec (Wave 2)`
      (d) `- [ ] 02.1-04-PLAN.md — CI integration + idempotency verification + phase closeout (Wave 3)` → `- [x] 02.1-04-PLAN.md — CI integration + idempotency verification + phase closeout (Wave 3)`

      DO NOT touch any other line in ROADMAP.md. DO NOT modify the Goal line, the **Plans:** count line, the Requirements line, the Depends-on line, or the Progress table. DO NOT touch the top-level Phases checklist (the integer-phase checkboxes for Phases 1 and 2 are visibly unchecked in the file BUT that is a pre-existing state separate from Phase 02.1 closeout; reconciling those is OUT OF SCOPE for this plan — see B-03 / Known Inconsistency note in STATE.md).

      If any of the four Edit calls fails with "string not found", STOP. Read the file again and confirm the actual text of that line; report the discrepancy to the orchestrator. Do not invent fixes for fields you cannot find.

    Step 2 — Update STATE.md (minimal, additive edits):

      Use the Read tool first to confirm CURRENT state of STATE.md. Note the pre-existing inconsistencies between frontmatter and body (frontmatter `percent: 50` vs body `[████████░░] 83%`; frontmatter `completed_phases: 3` vs top-level ROADMAP checklist showing Phases 1 and 2 unchecked).

      Use the Edit tool for ONLY the following targeted edits:

      (a) Frontmatter `last_updated`: Edit the current value (currently `"2026-05-13T09:01:39.562Z"`) to a current UTC timestamp. The executor MAY use any reasonable ISO-8601 format (e.g., `date -Iseconds -u` output, or simply `"2026-05-13T<HH>:<MM>:<SS>Z"`). DROP the brittle `date -u +"%Y-%m-%dT%H:%M:%S.000Z"` prescription from the prior version of this plan — the orchestrator does not care about trailing `.000Z`.

      (b) Frontmatter `last_activity`: confirm value is today's date; bump if not.

      (c) Frontmatter `stopped_at: Phase 2 context gathered` → `stopped_at: Phase 02.1 closed; ready to plan Phase 03`.

      (d) Body §Current Position block (lines 27-31): Edit `Phase: 02.1` to `Phase: 03`. Leave `Plan: Not started`, `Status: Ready to plan`, and `Last activity: 2026-05-13` (or whatever the body line currently says — bump it to today's date if older).

      (e) Body §Roadmap Evolution (after line 72): APPEND a new bullet AFTER the existing `Phase 02.1 inserted after Phase 2: ...` line: `- Phase 02.1 closed YYYY-MM-DD: make test-int runs end-to-end on developer laptop; all 7 Layer B Ginkgo specs pass; CI test-int job wired with helm install + TIDE_REQUIRE_CONTROLLER=1` (substitute today's date for `YYYY-MM-DD`).

      (f) Body §Notes (or §Blockers/Concerns if §Notes does not exist — the latter is at lines 124-130 in the current file): APPEND a `Known inconsistency` entry:
        `- Known inconsistency (out of scope for Phase 02.1 closeout): STATE.md frontmatter shows `completed_phases: 3`, `completed_plans: 24`, `percent: 50`, but the top-level ROADMAP.md Phases checklist shows Phases 1 and 2 as `- [ ]` unchecked, and the body Progress bar reads `83%`. These pre-existing discrepancies were noted during Phase 02.1 closeout and should be reconciled during Phase 03 planning (or during the next milestone-boundary review). Plan 04 left these fields unchanged per B-03 option (a) — closeout for Phase 02.1 is not a STATE.md reconciliation phase.`

      DO NOT modify `progress.completed_phases`, `progress.completed_plans`, `progress.total_plans`, `progress.percent`, or the body Progress bar (`[████████░░] 83%`). Leave them at their current values. The Known inconsistency note is the record that they remain inconsistent.

    Step 3 — Validate the edits:

      Read both files back via the Read tool to verify the edits applied as intended. Confirm:
        - ROADMAP.md: four `- [x] 02.1-NN-PLAN.md` rows present; no other lines changed.
        - STATE.md: frontmatter `last_updated` / `last_activity` / `stopped_at` updated; body §Current Position shows `Phase: 03`; body §Roadmap Evolution has the new Phase 02.1 closed bullet; body has a Known inconsistency entry; frontmatter `completed_phases` / `completed_plans` / `total_plans` / `percent` and body Progress bar UNCHANGED from pre-edit values.

      If any edit failed silently (e.g., the literal-match string drifted from the actual file content), fix and re-verify.

    DO NOT touch any production source files. DO NOT touch any other .planning/ file outside of ROADMAP.md and STATE.md. DO NOT modify the Progress table at the bottom of ROADMAP.md (decimal phases are not represented in that table per the ROADMAP's own convention).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && grep -c '\[x\] 02.1-01-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '\[x\] 02.1-02-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '\[x\] 02.1-03-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '\[x\] 02.1-04-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '\[ \] 02.1-' .planning/ROADMAP.md | awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -c 'Phase 02.1 closed' .planning/STATE.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'Known inconsistency' .planning/STATE.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'Phase: 03' .planning/STATE.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md: four `- [x] 02.1-NN-PLAN.md` rows present (one each for plans 01, 02, 03, 04). `grep -c '\[ \] 02.1-' .planning/ROADMAP.md` returns 0 (no Phase 02.1 plan rows remain unchecked).
    - ROADMAP.md Goal line for Phase 02.1, **Plans:** count line, Requirements line, Depends-on line: UNCHANGED from pre-edit state (verified by spot-check or git diff).
    - ROADMAP.md top-level Phases checklist (lines 19-23): UNCHANGED from pre-edit state (the Phase 1 + Phase 2 unchecked rows are pre-existing and reconciled separately).
    - STATE.md frontmatter: `last_updated`, `last_activity`, `stopped_at` bumped; `completed_phases`, `completed_plans`, `total_plans`, `percent` UNCHANGED.
    - STATE.md body: §Current Position shows `Phase: 03`; §Roadmap Evolution contains a `Phase 02.1 closed` entry; a `Known inconsistency` entry documents the unchanged-but-inconsistent frontmatter fields.
    - STATE.md body Progress bar (`[████████░░] 83%`): UNCHANGED from pre-edit state.
    - No git diff outside `.github/workflows/ci.yaml`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/phases/02.1-.../02.1-04-VERIFICATION.md` (verified with `git status`).
  </acceptance_criteria>
  <done>
    - ROADMAP.md plan rows for Phase 02.1 reflect closure (four `- [x]` rows).
    - STATE.md timestamps + Roadmap Evolution + Current Position bumped; Known inconsistency note added.
    - Pre-existing STATE.md inconsistencies (frontmatter vs body, frontmatter vs ROADMAP top-level checklist) are documented but UNCHANGED — they are out of scope for Phase 02.1 closeout per B-03 option (a).
    - No production code touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → kind cluster | Phase 02.1 CI runs entirely inside the GitHub Actions runner; no external network exposure during the test-int job (helm install pulls from the local working directory; cert-manager manifests pulled from GitHub Releases). |
| `02.1-04-VERIFICATION.md` artifact → committed Markdown | The verification artifact captures `make test-int` output. If the captured output contains TIDE_SIGNING_KEY values (unlikely — helm doesn't echo Secret data values by default), they must be redacted before commit per the discipline established in Plan 01 (T-02.1-01-01). |
| TIDE_REQUIRE_CONTROLLER=1 env var → suite_test.go WR-09 gate | This env var controls whether install failures hard-fail or soft-skip. Setting it in CI is the right behavior; setting it in dev environments is at the developer's discretion. No security boundary; this is a CI behavior toggle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04-01 | Information Disclosure | `02.1-04-VERIFICATION.md` captures helm install output; if helm 3 ever changes default Secret-printing behavior to echo `data:` values, the TIDE_SIGNING_KEY could be committed | mitigate | Apply the same sed redaction discipline from Plan 01 (T-02.1-01-01) when quoting captured logs into the artifact. Verify by inspecting the artifact before commit. As of 2026, Helm 3 does NOT print Secret resource data values in `helm install` output. |
| T-02.1-04-02 | Tampering | A regression in the CI workflow (e.g., a typo in the helm-install step that silently no-ops) leaves the test-int job soft-skipping Layer B without anyone noticing | mitigate | The TIDE_REQUIRE_CONTROLLER=1 env var causes Plan 02's WR-09 gate to hard-fail when the suite's helm-install path can't find helm. The CI run is loud, not silent. The acceptance criteria's `grep -c "azure/setup-helm" .github/workflows/ci.yaml` returns 2 — a regression that drops the helm-install step fails the gate. |
| T-02.1-04-03 | Repudiation | The phase is marked complete in STATE.md and ROADMAP.md without the actual test-int suite being green or within budget | mitigate | Task 2 (Verification) is GATED on the actual `make test-int` runs being green AND idempotent AND under 300s wall-time per run. Task 3 (ROADMAP/STATE update) is blocked from running unless Task 2 emits APPROVED in its gate decision. APPROVED requires explicit per-run wall-time assertions (W-08 fix) — a regression that lets a 400s run through the gate fails the grep for `(≤|<=) 300s` count ≥ 2. Tampering with the gate (manually flipping APPROVED) requires editing 02.1-04-VERIFICATION.md, which is committed and reviewable. |
| T-02.1-04-04 | Denial of Service | The new CI test-int job exceeds the 300s budget on a slow GitHub Actions runner | accept | The outer `timeout-minutes: 6` is a 360s ceiling; the inner `timeout 300s` in `make test-int` (Phase 2 Plan 13 contract) enforces TEST-02. If a slow runner consistently busts the budget, RESEARCH.md Pitfall 9 outlines mitigations (move long waits to test-int-kind-prep, etc.) — that's a Phase 2 v2 concern, not Phase 02.1's. |
</threat_model>

<verification>
- `.github/workflows/ci.yaml` parses as valid YAML.
- `.github/workflows/ci.yaml` test-int job has helm install step + TIDE_REQUIRE_CONTROLLER env + D-03 cross-reference comment.
- Phase 1 test job and helm-lint job unchanged.
- `02.1-04-VERIFICATION.md` has explicit APPROVED gate decision AND explicit per-run wall-time assertion lines (`(≤|<=) 300s` count ≥ 2).
- ROADMAP.md Phase 02.1 entry: four `- [x] 02.1-NN-PLAN.md` rows present; Goal/Plans/Requirements lines UNCHANGED from pre-edit state.
- STATE.md: timestamps bumped, Roadmap Evolution + Current Position updated, Known inconsistency note added; `progress.*` fields and body Progress bar UNCHANGED.
- No production code modified.
- No file outside `.github/workflows/ci.yaml`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/phases/02.1-.../02.1-04-VERIFICATION.md` touched.
</verification>

<success_criteria>
ROADMAP closeout: Phase 02.1 entry shows four `[x]` plan rows. The Goal, Plans count, Requirements, and Depends-on lines are left as they already were after initial plan authoring (correct content; no churn).

CI verification: The `test-int` job in `.github/workflows/ci.yaml` will exit non-zero on any future Layer B regression (TIDE_REQUIRE_CONTROLLER=1 ensures loud failures; helm in PATH ensures the suite uses the production install path).

Runtime gate (Plan 02 + Plan 03 deliverables): Plan 04 Task 2 explicitly verifies ≥6/7 → 7/7 specs go green and both runs complete in ≤ 300s wall-time, proving the source-code changes from Plans 02 and 03 actually work end-to-end.

Idempotency proven: Running `make test-int` twice (with KEEP_KIND_CLUSTER=true on the first run) succeeds both times within budget — the chart's `lookup` + `resource-policy: keep` guards work as designed.

Honors D-03 from `02.1-BASELINE.md`: TIDE_REQUIRE_CONTROLLER default behavior preserved (unset = soft-skip for dev; CI sets =1 to hard-fail). The CI workflow YAML carries an explicit cross-reference to the BASELINE.md §D-03 section so the decision provenance is browsable.

STATE.md/ROADMAP.md known-inconsistency posture: Plan 04 documents but does NOT silently fix pre-existing inconsistencies. Reconciliation is deferred to a future planning cycle.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-SUMMARY.md` describing:
- The exact lines added to `.github/workflows/ci.yaml` (line range + content summary; include the D-03 cross-reference comment text)
- The verification results from Task 2 (clean-start run wall-time + idempotent re-run wall-time + per-spec outcomes table; explicit confirmation both runs ≤ 300s)
- The ROADMAP.md edits applied (the four checkbox flips; explicit note that Goal/Plans/Requirements lines were already correct and were NOT touched)
- The STATE.md edits applied (frontmatter `last_updated` / `last_activity` / `stopped_at` bumped; body §Current Position / §Roadmap Evolution updated; Known inconsistency entry added; explicit note that `progress.*` fields and Progress bar UNCHANGED per B-03 option (a))
- A final sentence confirming Phase 02.1 is closed and Phase 03 is the next phase
- Cross-references to 02.1-04-VERIFICATION.md so the chain of evidence is browsable
- Cross-reference to the pre-existing STATE.md inconsistency for the next planner to address
</output>
</output>
