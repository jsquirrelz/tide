---
phase: 02.1
plan: 04
type: execute
wave: 3
depends_on:
  - "02.1-02"
  - "02.1-03"
files_modified:
  - .github/workflows/ci.yaml
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - roadmap-goal-ci-green
  - roadmap-goal-idempotency
  - roadmap-goal-phase-closeout
user_setup: []
must_haves:
  truths:
    - "The `test-int` CI job in `.github/workflows/ci.yaml` installs Helm 3 BEFORE running `make test-int` (the new BeforeSuite path from Plan 02 requires `helm` in PATH; without this step the suite soft-skips and CI silently passes with no Layer B coverage)."
    - "The `test-int` CI job sets `TIDE_REQUIRE_CONTROLLER: \"1\"` in its env block per D-03 in `02.1-BASELINE.md` so install failures are loud (the CI exits non-zero instead of soft-skipping)."
    - "`make test-int` executed twice consecutively against the SAME kind cluster (with `KEEP_KIND_CLUSTER=true` on the first run) completes 0+0 — second run uses the existing cluster + existing helm release, exercising the chart's `lookup` + resource-policy: keep guards on tide-signing-key Secret + tide-projects PVC."
    - "`make test-int` from a CLEAN state (no kind cluster, no helm release) completes in <300s on a developer laptop with all 7 Layer B specs passing."
    - "ROADMAP.md Phase 02.1 entry is updated: Goal line replaces the `[Urgent work - to be planned]` placeholder with the actual one-sentence summary; the unique placeholder plan row `- [ ] TBD (run /gsd-plan-phase 02.1 to break down)` is replaced with four `- [x] 02.1-NN-PLAN.md` rows (one per plan); the phase row in the Progress table is updated; the top-level Phases checklist gets the `Phase 02.1` checkbox marked complete (the bullet currently doesn't appear in the Phases checklist because decimal phases are inserted via the Phase Details section)."
    - "STATE.md is updated: `progress.completed_plans` increments by 4 (24 → 28); `progress.percent` recomputed accordingly; the `Roadmap Evolution` section appends a `Phase 02.1 closed: make test-int runs end-to-end on developer laptop; all 7 Layer B specs pass` note; the `Current Position` block advances (Phase: 02.1 → Phase: 03; Status updated)."
    - "No production code in `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/` is modified by this plan — the fix work is done; this plan only wires CI + closes out the phase."
  artifacts:
    - path: .github/workflows/ci.yaml
      provides: "CI test-int job that installs Helm + sets TIDE_REQUIRE_CONTROLLER=1 so Layer B failures fail the build"
      contains: "azure/setup-helm"
    - path: .planning/ROADMAP.md
      provides: "Phase 02.1 entry updated with real goal text + 4-row plan list + phase-row marked complete"
      contains: "02.1-01-PLAN.md"
    - path: .planning/STATE.md
      provides: "Project state advanced to Phase 03 ready-to-plan; progress counts updated"
      contains: "completed_plans: 28"
  key_links:
    - from: ".github/workflows/ci.yaml test-int job"
      to: "azure/setup-helm@v4 action"
      via: "new install-step before make test-int"
      pattern: "azure/setup-helm"
    - from: ".github/workflows/ci.yaml test-int job env block"
      to: "test/integration/kind/suite_test.go WR-09 gate"
      via: "TIDE_REQUIRE_CONTROLLER=1 forces hard-fail on missing controller"
      pattern: "TIDE_REQUIRE_CONTROLLER"
---

<objective>
Close out Phase 02.1 by: (a) wiring the CI `test-int` job so it has `helm` in PATH and `TIDE_REQUIRE_CONTROLLER=1` set, (b) verifying idempotency (running `make test-int` twice consecutively against the same cluster succeeds) AND clean-start correctness (running from a deleted cluster also succeeds), and (c) updating ROADMAP.md + STATE.md to reflect Phase 02.1 completion.

This plan depends on BOTH Plan 02 (helm-install BeforeSuite pivot) AND Plan 03 (credproxy banner). Without both fixes, the Layer B suite has at least one failing spec, and Plan 04 cannot truthfully mark the phase complete.

This plan does NOT modify any production code. It modifies only CI + planning artifacts. The fix work was done in Plans 02 + 03; Plan 04 is the closeout.

Output: A modified `.github/workflows/ci.yaml` with helm-install + TIDE_REQUIRE_CONTROLLER env; an updated `.planning/ROADMAP.md` with Phase 02.1 fully populated and checked; an updated `.planning/STATE.md` with progress counts and current position advanced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-RESEARCH.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-VALIDATION.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-02-SUMMARY.md
@.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-03-SUMMARY.md
@./CLAUDE.md
@.github/workflows/ci.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire helm + TIDE_REQUIRE_CONTROLLER into the test-int CI job</name>
  <files>.github/workflows/ci.yaml</files>
  <read_first>
    - .github/workflows/ci.yaml (the FULL file; particularly the existing `helm-lint` job at lines 102-153 — that job already uses `azure/setup-helm@v4 with: version: 'v3.16.3'` so the version pin matches; and the `test-int` job at lines 155-219 — that job currently has NO helm install step)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md (D-03 — TIDE_REQUIRE_CONTROLLER=1 in CI per the WR-09 contract)
    - test/integration/kind/suite_test.go (the Plan-02-modified applyController() which gates on `exec.LookPath("helm")` — without helm, it soft-skips with `GinkgoWriter.Println("helm not in PATH; falling back to CRDs-only mode")`)
  </read_first>
  <behavior>
    - The `test-int` job in `.github/workflows/ci.yaml` has a new step `Install Helm` between `Install kind v0.31.0` (currently at lines 173-178) and `Prepare manifests and envtest binaries` (currently at lines 181-184).
    - The new step uses `uses: azure/setup-helm@v4` and `version: 'v3.16.3'` — matching the existing helm-lint job's helm version pin (no new version drift introduced).
    - The `test-int` job declares `env: { TIDE_REQUIRE_CONTROLLER: "1" }` at the job level (so all steps see it, including the `make test-int` step). This makes `make test-int` exit non-zero when the controller doesn't reach Ready (instead of soft-skipping per WR-09).
    - The Phase 1 `test` job and the `helm-lint` job are UNCHANGED — no regression on the existing CI surface.
    - The job's `timeout-minutes: 6` is preserved (the outer CI fence; the inner `timeout 300s` inside `make test-int` is the budget gate).
    - The failure-collection steps (`Collect kind cluster logs on failure` and `Upload kind logs artifact on failure` at lines 204-219) are preserved.
    - `yamllint` / GitHub Actions YAML syntax is valid (no tabs; consistent 2-space indentation; the file passes `actionlint` if available).
    - The file remains gofmt/yamlfmt-clean.
  </behavior>
  <action>
    Edit `.github/workflows/ci.yaml`. In the `test-int:` job block (currently starting at line 155):

      1. Add an env block at the job level. Insert directly under `permissions: contents: read` (currently at line 159-160), at the same indentation as `permissions:`, the new block:

         ```
             env:
               # Phase 02.1 / WR-09 / D-03 (02.1-BASELINE.md): CI runs MUST fail loudly
               # when the controller doesn't reach Ready. Local dev runs may leave this
               # unset to allow CRDs-only fallback.
               TIDE_REQUIRE_CONTROLLER: "1"
         ```

      2. Add a `Install Helm` step. Insert directly AFTER the existing `Install kind v0.31.0` step (currently ending at line 178) and BEFORE `Prepare manifests and envtest binaries` (currently starting at line 181), at the same indentation as the surrounding steps:

         ```
               - name: Install Helm
                 uses: azure/setup-helm@v4
                 with:
                   version: 'v3.16.3'
         ```

         The version pin must match the existing `helm-lint` job's pin at lines 121-123 verbatim.

      3. Do NOT modify any other step. Do NOT delete the cluster-logs-on-failure block. Do NOT change `timeout-minutes`. Do NOT change the Phase 1 `test` job or the `helm-lint` job.

      4. Verify the YAML is syntactically valid: indentation is 2 spaces, no tabs, no trailing whitespace on the new lines. The existing file uses `azure/setup-helm@v4` in the helm-lint job (line 121), so action availability is already established.

    Save and stop. The actual CI run validates the workflow on push.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && grep -A2 "azure/setup-helm" .github/workflows/ci.yaml | grep -c "v3.16.3" | awk '{ if ($1 == 2) exit 0; else exit 1 }' && grep -c "TIDE_REQUIRE_CONTROLLER" .github/workflows/ci.yaml | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))" && grep -c "^  test-int:" .github/workflows/ci.yaml | awk '{ if ($1 == 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))"` exits 0 (valid YAML).
    - `grep -c "azure/setup-helm" .github/workflows/ci.yaml` returns 2 (one in helm-lint, one new in test-int).
    - `grep -c "TIDE_REQUIRE_CONTROLLER" .github/workflows/ci.yaml` returns ≥ 1 (the new env block).
    - The Phase 1 `test:` job exists unchanged: `grep -c "^  test:$" .github/workflows/ci.yaml` returns 1.
    - The `helm-lint:` job exists unchanged: `grep -c "^  helm-lint:$" .github/workflows/ci.yaml` returns 1.
    - The `test-int:` job exists with the new env + helm step: `grep -c "^  test-int:$" .github/workflows/ci.yaml` returns 1.
  </acceptance_criteria>
  <done>
    - CI workflow has helm installed before `make test-int` runs.
    - CI workflow sets TIDE_REQUIRE_CONTROLLER=1.
    - Phase 1 `test` job and `helm-lint` job preserved.
    - YAML is syntactically valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run end-to-end verification (clean start + idempotency) and capture results</name>
  <files>.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-BASELINE.md (the BEFORE-FIX score for the per-spec outcome table; reuse the same 7-row table shape)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-02-SUMMARY.md (Plan 02 outcome — 6 of 7 specs should be green)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-03-SUMMARY.md (Plan 03 outcome — the 7th spec should be green after credproxy image rebuild)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-VALIDATION.md (the validation strategy — full make test-int is the phase gate)
  </read_first>
  <behavior>
    - A new artifact `02.1-04-VERIFICATION.md` exists in the phase directory capturing the post-fix score: ≥ 7/7 Layer B specs passing on a clean run; ≥ 7/7 Layer B specs passing on an idempotent re-run.
    - The artifact contains the verbatim commands run for clean-start verification and idempotent re-run.
    - The artifact contains the AFTER-FIX per-spec outcome table (same 7 rows as 02.1-BASELINE.md Section 3) showing all 7 PASS.
    - Wall-time for each run is recorded; both runs complete in <300s.
    - If ANY spec fails OR wall-time > 300s OR idempotency breaks (second run fails), this artifact records the failure and the plan halts BEFORE Task 3 (the ROADMAP/STATE updates). Phase 02.1 cannot close with a partial fix.
  </behavior>
  <action>
    Step 1 — CLEAN START verification:
      (a) `kind delete cluster --name tide-test 2>&1 || true`
      (b) `helm uninstall tide -n tide-system 2>&1 || true`
      (c) Capture wall-time and exit code: `time make test-int 2>&1 | tee /tmp/02.1-04-clean-run.log; echo "exit=$?"`
      (d) Record per-spec outcomes from the Ginkgo output (use the same 7-row table shape as 02.1-BASELINE.md Section 3).

    Step 2 — IDEMPOTENCY verification:
      (a) WITHOUT deleting the kind cluster, immediately re-run: `time KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.1-04-rerun.log; echo "exit=$?"`
      (b) The second run reuses the existing kind cluster AND the existing helm release. The chart's `lookup` + `resource-policy: keep` guards on `tide-signing-key` Secret + `tide-projects` PVC make this safe (HARN-03 / Phase 2 design).
      (c) Record wall-time + exit code + per-spec outcomes.

    Step 3 — Create `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md` with sections:
      - Header (phase, plan, date, hostname)
      - Section 1 — Clean Start Run (reproducer commands; per-spec outcome table; wall-time; exit code)
      - Section 2 — Idempotent Re-run (reproducer commands; per-spec outcome table; wall-time; exit code; explicit note that helm + chart's lookup guards make the re-install no-op)
      - Section 3 — Before/After Comparison (a small table: number of passing specs BEFORE per 02.1-BASELINE.md vs AFTER; expected: 0/7 → 7/7 or BEFORE→AFTER as captured)
      - Section 4 — Wall-time Budget Check (both runs <300s; if one exceeded the budget, note it explicitly — RESEARCH.md A3 said the 90s waitForControllerReady ceiling becomes a no-op once helm --wait does the work; verify by observing the helm install duration relative to total)
      - Section 5 — Gate Decision (explicit: "Phase 02.1 closeout APPROVED — proceeding to Task 3 (ROADMAP/STATE update)" OR "Phase 02.1 closeout BLOCKED — see Section X for failure"). If BLOCKED, do NOT proceed to Task 3.

    If both Section 1 + Section 2 show all-green outcomes AND both runs ≤300s, gate decision is APPROVED. Otherwise BLOCKED — abort the plan; orchestrator decides next step.

    Apply the same TIDE_SIGNING_KEY sed-redaction discipline from Plan 01 (T-02.1-01-01) when quoting logs into the artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md && grep -q "Gate Decision" .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md && grep -qE 'APPROVED|BLOCKED' .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `02.1-04-VERIFICATION.md` exists with 5 H2 sections.
    - Section 5 (Gate Decision) is explicitly APPROVED or BLOCKED.
    - If APPROVED: Section 3 shows all 7 specs PASS on both runs; wall-time ≤300s on both runs; Task 3 proceeds.
    - If BLOCKED: Plan halts; SUMMARY.md records the partial state; orchestrator notified.
  </acceptance_criteria>
  <done>
    - Verification artifact exists with explicit gate decision.
    - If APPROVED, Task 3 (ROADMAP/STATE update) is unblocked.
    - If BLOCKED, the plan stops here and surfaces the failure.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md and STATE.md to reflect Phase 02.1 closure</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (the FULL file; particularly the Phase 02.1 entry at lines 95-104 — note the placeholder `[Urgent work - to be planned]` Goal line at line 97 and the placeholder plan list at lines 102-103)
    - .planning/STATE.md (the FULL file; particularly the frontmatter at lines 1-15 and the Roadmap Evolution section at lines 70-72)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md (must contain the APPROVED gate decision — if BLOCKED, this task does NOT run)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-01-PLAN.md (the four plan filenames + objectives — for populating the ROADMAP plan list)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-02-PLAN.md (same)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-03-PLAN.md (same)
  </read_first>
  <behavior>
    - ROADMAP.md Phase 02.1 entry updated:
      - The `**Goal:**` line at line 97 replaces `[Urgent work - to be planned]` with a real one-sentence goal sourced from the phase's H1 description at line 95 (e.g., "Layer B integration test suite (make test-int) runs end-to-end on developer laptop: tide-controller-manager Deployment Ready in kind, Plan webhook service has live endpoints, all 7 Layer B Ginkgo specs pass.").
      - The `**Plans:** 13/13 plans complete` line at line 100 is corrected to `**Plans:** 4 plans` (13/13 was an incorrect carryover from the placeholder — Phase 02.1 has 4 plans, not 13).
      - The `Plans:` list at lines 102-103 (currently the placeholder `- [ ] TBD (run /gsd-plan-phase 02.1 to break down)`) is replaced with four entries:
        - `- [x] 02.1-01-PLAN.md — Baseline capture + lock D-01/D-02/D-03 decisions (Wave 1)`
        - `- [x] 02.1-02-PLAN.md — Helm-install BeforeSuite pivot + ensureSubagentSA helper (Wave 2)`
        - `- [x] 02.1-03-PLAN.md — Credproxy boot-banner for log-line spec (Wave 2)`
        - `- [x] 02.1-04-PLAN.md — CI integration + idempotency verification + phase closeout (Wave 3)`
      - The Progress table row for Phase 02.1 does NOT exist (decimal phases are inserted in the Phase Details section, not the Progress table). NO change needed to the Progress table.
    - STATE.md frontmatter updated:
      - `progress.completed_plans` increments from 24 to 28 (24 + 4).
      - `progress.percent` recomputed: round(28/total_phases_plans). The frontmatter currently shows `total_plans: 24` — that count is stale (it pre-dates Phase 02.1). Update `total_plans` to a more accurate count by reading the ROADMAP plan rows OR keep the stale total and just bump `completed_plans` — the orchestrator's STATE schema is loose. SAFE behavior: bump `total_plans` to 28 too (so percent stays = 100% for the completed set) OR leave total_plans=24 and accept percent>100% as a temporary inconsistency until Phase 3 plans are authored. Use the simpler approach: set both `total_plans: 28` and `completed_plans: 28`, recompute `progress.percent` for completed_phases.
      - `progress.completed_phases` increments from 3 (Phases 1, 2 — both shown as complete) to 4 (adding 02.1). NOTE: depending on how the orchestrator counts decimal phases, this may be 3 (if 02.1 is folded under "Phase 2") or 4 (if 02.1 is its own row). The conservative behavior is to bump completed_phases by 1 (3 → 4) since Phase 02.1 is roadmap-named with its own directory.
      - `progress.percent` updates to `int(completed_phases / total_phases * 100)` = `int(4 / 6 * 100)` = 66.
      - `last_updated` set to the current ISO-8601 timestamp.
      - `last_activity` set to today's date (2026-05-13).
      - `stopped_at` updated to `"Phase 02.1 closed; ready to plan Phase 03"`.
      - `status` updated to `ready_to_plan`.
    - STATE.md body updated:
      - Roadmap Evolution section (currently at lines 70-72 with `Phase 02.1 inserted after Phase 2: ...`): append a new line `Phase 02.1 closed 2026-05-13: make test-int runs end-to-end on developer laptop; all 7 Layer B Ginkgo specs pass; CI test-int job wired with helm install + TIDE_REQUIRE_CONTROLLER=1`.
      - Current Position section (currently at lines 27-31 showing `Phase: 02.1`, `Status: Ready to plan`): update to `Phase: 03`, `Status: Ready to plan`, with `Last activity: 2026-05-13`.
      - Progress bar updated proportionally.
    - No production code touched.
  </behavior>
  <action>
    GATE CHECK: Read `02.1-04-VERIFICATION.md` and confirm Section 5 says APPROVED. If BLOCKED, abort this task; orchestrator surfaces the failure and decides next steps.

    Step 1 — Update ROADMAP.md:
      Use the Edit tool (NOT a full rewrite) for each of the following changes to `.planning/ROADMAP.md`:

      (a) Line 97 (`**Goal:** [Urgent work - to be planned]`): Edit to `**Goal:** Layer B integration test suite (make test-int) runs end-to-end on developer laptop — tide-controller-manager Deployment Ready in kind, Plan webhook service has live endpoints, all 7 Layer B Ginkgo specs pass.` (one sentence; sourced from the H1 description at line 95).

      (b) Line 100 (`**Plans:** 13/13 plans complete`): Edit to `**Plans:** 4/4 plans complete`.

      (c) Lines 102-103 (`Plans:\n- [ ] TBD (run /gsd-plan-phase 02.1 to break down)`): Replace with the four-row plan list per <behavior> above. Format must match the existing Phase 1 and Phase 2 plan-list style (lines 39-50 for Phase 1; lines 68-93 for Phase 2 — checkbox + filename + em-dash + description + (Wave N) suffix).

    Step 2 — Update STATE.md frontmatter (lines 1-15):
      Use the Edit tool for each of:

      (a) Line 5 (`status: ready_to_plan`): Edit value to `ready_to_plan` (already correct; verify and skip if already so).

      (b) Line 6 (`stopped_at: Phase 2 context gathered`): Edit to `stopped_at: Phase 02.1 closed; ready to plan Phase 03`.

      (c) Line 7 (`last_updated: "2026-05-13T09:01:39.562Z"`): Edit value to current UTC timestamp in ISO-8601 format (e.g., `"2026-05-13T<HH>:<MM>:<SS>.000Z"` — use `date -u +"%Y-%m-%dT%H:%M:%S.000Z"` to generate).

      (d) Line 8 (`last_activity: 2026-05-13`): keep `2026-05-13` (today).

      (e) Lines 9-14 (progress block): bump `completed_phases: 3` → `completed_phases: 4`; bump `completed_plans: 24` → `completed_plans: 28`; bump `total_plans: 24` → `total_plans: 28`; recompute `percent: 50` → `percent: 66` (computed as `int(4/6*100)`).

    Step 3 — Update STATE.md body:
      Use the Edit tool for each:

      (a) Lines 27-31 (Current Position block): Edit `Phase: 02.1` to `Phase: 03`; `Plan: Not started` stays; `Status: Ready to plan` stays; `Last activity: 2026-05-13` stays.

      (b) Line 34 (Progress bar `[████████░░] 83%`): Edit to a value consistent with 66% (e.g., `[██████░░░░] 66%`).

      (c) Lines 70-72 (Roadmap Evolution): Append a new bullet AFTER the existing `Phase 02.1 inserted after Phase 2:` line: `- Phase 02.1 closed 2026-05-13: make test-int runs end-to-end on developer laptop; all 7 Layer B Ginkgo specs pass; CI test-int job wired with helm install + TIDE_REQUIRE_CONTROLLER=1`.

    Step 4 — Validate the edits:
      Read both files back via the Read tool to verify the edits applied as intended. If any edit failed silently (e.g., the literal-match string drifted from the actual file content), fix and re-verify.

    DO NOT touch any production source files. DO NOT touch any other .planning/ file outside of ROADMAP.md and STATE.md. DO NOT modify the Progress table at the bottom of ROADMAP.md (decimal phases are not represented in that table per the ROADMAP's own convention).
  </action>
  <verify>
    <automated>grep -c '02.1-01-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '02.1-02-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '02.1-03-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '02.1-04-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c 'Urgent work - to be planned' .planning/ROADMAP.md | awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -c 'TBD (run /gsd-plan-phase 02.1' .planning/ROADMAP.md | awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -c 'completed_plans: 28' .planning/STATE.md | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c 'Phase 02.1 closed' .planning/STATE.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 02.1 entry: Goal line is NOT `[Urgent work - to be planned]` (replaced).
    - ROADMAP.md Phase 02.1 entry: contains the four `02.1-NN-PLAN.md` rows in checkbox-checked form.
    - ROADMAP.md no longer contains the placeholder string `TBD (run /gsd-plan-phase 02.1 to break down)`.
    - STATE.md frontmatter: `completed_plans: 28`, `total_plans: 28`, `completed_phases: 4`, `percent: 66`.
    - STATE.md body: Current Position shows `Phase: 03`.
    - STATE.md body: Roadmap Evolution contains a `Phase 02.1 closed` entry with date `2026-05-13`.
    - No git diff outside `.planning/ROADMAP.md` and `.planning/STATE.md` (verified with `git status`).
  </acceptance_criteria>
  <done>
    - ROADMAP.md and STATE.md reflect Phase 02.1 closure.
    - The four plan filenames are listed in the ROADMAP plan list with closed checkboxes.
    - STATE.md progress counts updated; current position advanced to Phase 03.
    - No production code touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → kind cluster | Phase 02.1 CI runs entirely inside the GitHub Actions runner; no external network exposure during the test-int job (helm install pulls from the local working directory; cert-manager manifests pulled from GitHub Releases). |
| `02.1-04-VERIFICATION.md` artifact → committed Markdown | The verification artifact captures `make test-int` output. If the captured output contains TIDE_SIGNING_KEY values (unlikely — helm doesn't echo Secret data values by default), they must be redacted before commit per the discipline established in Plan 01 (T-02.1-01-01). |
| TIDE_REQUIRE_CONTROLLER=1 env var → suite_test.go WR-09 gate | This env var controls whether install failures hard-fail or soft-skip. Setting it in CI is the right behavior; setting it in dev environments is at the developer's discretion. No security boundary; this is a CI behavior toggle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04-01 | Information Disclosure | `02.1-04-VERIFICATION.md` captures helm install output; if helm 3 ever changes default Secret-printing behavior to echo `data:` values, the TIDE_SIGNING_KEY could be committed | mitigate | Apply the same sed redaction discipline from Plan 01 (T-02.1-01-01) when quoting captured logs into the artifact. Verify by inspecting the artifact before commit. As of 2026, Helm 3 does NOT print Secret resource data values in `helm install` output. |
| T-02.1-04-02 | Tampering | A regression in the CI workflow (e.g., a typo in the helm-install step that silently no-ops) leaves the test-int job soft-skipping Layer B without anyone noticing | mitigate | The TIDE_REQUIRE_CONTROLLER=1 env var causes Plan 02's WR-09 gate to hard-fail when the suite's helm-install path can't find helm. The CI run is loud, not silent. The acceptance criteria's `grep -c "azure/setup-helm" .github/workflows/ci.yaml` returns 2 — a regression that drops the helm-install step fails the gate. |
| T-02.1-04-03 | Repudiation | The phase is marked complete in STATE.md and ROADMAP.md without the actual test-int suite being green | mitigate | Task 2 (Verification) is GATED on the actual `make test-int` runs being green AND idempotent AND under 300s. Task 3 (ROADMAP/STATE update) is blocked from running unless Task 2 emits APPROVED in its gate decision. Tampering with the gate (manually flipping APPROVED) requires editing 02.1-04-VERIFICATION.md, which is committed and reviewable. |
| T-02.1-04-04 | Denial of Service | The new CI test-int job exceeds the 300s budget on a slow GitHub Actions runner | accept | The outer `timeout-minutes: 6` is a 360s ceiling; the inner `timeout 300s` in `make test-int` (Phase 2 Plan 13 contract) enforces TEST-02. If a slow runner consistently busts the budget, RESEARCH.md Pitfall 9 outlines mitigations (move long waits to test-int-kind-prep, etc.) — that's a Phase 2 v2 concern, not Phase 02.1's. |
</threat_model>

<verification>
- `.github/workflows/ci.yaml` parses as valid YAML.
- `.github/workflows/ci.yaml` test-int job has helm install step + TIDE_REQUIRE_CONTROLLER env.
- Phase 1 test job and helm-lint job unchanged.
- `02.1-04-VERIFICATION.md` has explicit APPROVED gate decision.
- ROADMAP.md Phase 02.1 entry has real Goal text + 4-row plan list.
- STATE.md progress counts incremented; current position advanced to Phase 03.
- No production code modified.
- No file outside `.github/workflows/ci.yaml`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/phases/02.1-.../02.1-04-VERIFICATION.md` touched.
</verification>

<success_criteria>
ROADMAP closeout: Phase 02.1 entry shows real plan filenames and a non-placeholder Goal. STATE.md shows Phase 02.1 complete and Phase 03 as the next target.

CI verification: The `test-int` job in `.github/workflows/ci.yaml` will exit non-zero on any future Layer B regression (TIDE_REQUIRE_CONTROLLER=1 ensures loud failures; helm in PATH ensures the suite uses the production install path).

Idempotency proven: Running `make test-int` twice (with KEEP_KIND_CLUSTER=true on the first run) succeeds both times — the chart's `lookup` + `resource-policy: keep` guards work as designed.

Honors D-03 from `02.1-BASELINE.md`: TIDE_REQUIRE_CONTROLLER default behavior preserved (unset = soft-skip for dev; CI sets =1 to hard-fail).
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-SUMMARY.md` describing:
- The exact lines added to `.github/workflows/ci.yaml` (line range + content summary; no full diff dump)
- The verification results from Task 2 (clean-start run wall-time + idempotent re-run wall-time + per-spec outcomes table)
- The ROADMAP.md edits applied (line numbers + before-vs-after summaries)
- The STATE.md edits applied (frontmatter changes + body changes)
- A final sentence confirming Phase 02.1 is closed and Phase 03 is the next phase
- Cross-references to 02.1-04-VERIFICATION.md so the chain of evidence is browsable
</output>
