---
phase: 02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te
plan: 03
subsystem: testing
tags: [credproxy, integration-tests, kind, ginkgo, fmt-fprintf, boot-banner]

# Dependency graph
requires:
  - phase: 02.1
    provides: D-01 (credproxy boot-banner approach; LOCKED in 02.1-BASELINE.md by Plan 01)
provides:
  - Plaintext boot banner `credproxy listening on 127.0.0.1:8443\n` emitted to stdout in cmd/credproxy/main.go
  - Satisfies the literal-grep assertion at test/integration/kind/credproxy_test.go:199,208 (HARN-03 spec)
  - Structured zap log line `starting credential proxy` preserved alongside (machine-readable surface kept)
affects:
  - 02.1-04 (CI verification — Plan 04 Task 2 owns the end-to-end run that proves HARN-03 passes against a Plan-02-fixed cluster)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Plaintext-banner-alongside-structured-log: emit a `fmt.Fprintf(os.Stdout, ...)` plaintext line beside zap-behind-logr structured logs when an integration test or human operator depends on a stable literal that survives log-formatter refactors (etcd / kubelet / kube-proxy use the same pattern)."

key-files:
  created: []
  modified:
    - cmd/credproxy/main.go

key-decisions:
  - "D-01 (LOCKED in 02.1-BASELINE.md) honored verbatim: approach (a) plaintext stdout banner via fmt.Fprintf; structured zap log.Info kept alongside; no test grep changes."
  - "Comment-text discipline: the explanatory 3-line comment uses 'the literal asserted by test/integration/kind/credproxy_test.go' rather than paraphrasing the grep target — keeps the literal substring 'credproxy listening on' to exactly one non-comment line, satisfying the gate."

patterns-established:
  - "Banner divergence is intentional and documented in-line: when a single binary diverges from the project-wide structured-log-only convention (cmd/manager/main.go's pattern), a 2-3 line comment above the divergence MUST cite the reason (integration-test grep / operator-readable signal) without repeating the literal grep target."

requirements-completed: [roadmap-goal-3-specs-pass-credproxy-banner-side]

# Metrics
duration: 2min
completed: 2026-05-13
---

# Phase 02.1 Plan 03: Credproxy Plaintext Boot Banner Summary

**One-line `fmt.Fprintf(os.Stdout, "credproxy listening on %s\n", listenAddr)` added to cmd/credproxy/main.go alongside the existing structured zap log, satisfying the HARN-03 integration test's literal-grep contract without touching imports, go.mod, or behavior.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-13T10:27:32Z
- **Completed:** 2026-05-13T10:29:41Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Added a single `fmt.Fprintf(os.Stdout, "credproxy listening on %s\n", listenAddr)` line at `cmd/credproxy/main.go:128`, immediately after the existing structured `log.Info("starting credential proxy", ...)` block at lines 118-123 and immediately before the `// 6. Block on ListenAndServe; graceful shutdown when ctx is cancelled.` comment at line 130.
- Added a 3-line comment block at lines 125-127 explaining the intentional divergence from `cmd/manager/main.go`'s structured-log-only convention, citing the integration-test grep dependency and the operator-readable-boot-signal pattern without repeating the literal grep target.
- Preserved the existing structured zap `log.Info("starting credential proxy", "listenAddr", listenAddr, "certDir", certDir, "upstreamURL", upstreamURL, "taskUID", taskUID)` call unchanged (machine-readable surface stays for log-aggregator consumption).
- Verified zero changes to `go.mod` and `go.sum` (`fmt` already imported at line 74 for the existing `fmt.Fprintf(os.Stderr, ...)` call; `os` already imported for `os.Exit` / `os.Stderr` / `os.Getenv`).

## Task Commits

Single-task plan; committed atomically as one feat commit:

1. **Task 1: Add plaintext boot banner to cmd/credproxy/main.go** — `f62710b` (feat)

_Note: TDD framing applied at the plan level — the RED test pre-exists in `test/integration/kind/credproxy_test.go:199,208` (already failing in the BASELINE per the SUITE-ABORTED row in 02.1-BASELINE.md's per-spec outcome table). This commit is the GREEN gate. The end-to-end GREEN proof (running the kind suite against a Plan-02-fixed install path) is owned by Plan 04 Task 2's verification script._

## Files Created/Modified

- `cmd/credproxy/main.go` — Added 5 lines (3-line explanatory comment + 1 `fmt.Fprintf` banner line + 1 trailing blank line between the new banner and the `// 6.` comment). Inserted at lines 125-129 of the post-edit file, between the existing log.Info block (lines 118-123) and the existing `// 6. Block on ListenAndServe;` comment (now line 130).

### Exact text rendered when `listenAddr == "127.0.0.1:8443"`

The `fmt.Fprintf` call produces these bytes on stdout (verbatim, including the trailing newline):

```
credproxy listening on 127.0.0.1:8443
```

This is byte-for-byte the literal the integration test greps for at `test/integration/kind/credproxy_test.go:199,208`:

```go
if strings.Contains(logs, "credproxy listening on 127.0.0.1:8443") {
```

When `--listen-addr` is overridden, the `%s` interpolation reflects the override and the test would need to be updated to match — outside the scope of Phase 02.1 (the integration test pins the default).

## Decisions Made

Followed plan as specified. The plan's `<action>` block locked the comment wording suggestion ("Plaintext boot banner (intentional divergence from cmd/manager/main.go's structured-log-only convention). Operator-readable signal that the listener bound; the literal asserted by test/integration/kind/credproxy_test.go.") and the exact format-string line — both were used verbatim. D-01 from `02.1-BASELINE.md` is the parent decision; this plan executes it without re-litigation.

## Deviations from Plan

None — plan executed exactly as written.

The plan's `<verify>` chain, the comment-line discipline gate, the go.mod/go.sum diff gate, and the import-firewall gate all passed on the first run with no auto-fixes required.

## Issues Encountered

None.

## Verification

Plan `<verify>` chain (run end-to-end as a single shell pipeline; all gates passed with exit 0):

| Gate | Result |
|------|--------|
| `go build ./cmd/credproxy/...` | PASS (exit 0) |
| `go vet ./cmd/credproxy/...` | PASS (exit 0) |
| `gofmt -l cmd/credproxy/main.go` | empty (gofmt-clean) |
| `go test ./cmd/credproxy/... ./internal/credproxy/...` | PASS — `internal/credproxy` 0.795s; `cmd/credproxy` has no test files |
| `grep -c "fmt.Fprintf.*os.Stdout.*credproxy listening on" cmd/credproxy/main.go` | returns `1` (exactly one banner emission line) |
| `grep -v "^[[:space:]]*//" cmd/credproxy/main.go \| grep -c "credproxy listening on"` | returns `1` (only the Fprintf line; not duplicated elsewhere) |
| `grep "^[[:space:]]*//" cmd/credproxy/main.go \| grep -c "credproxy listening on"` | returns `0` (explanatory comment does NOT paraphrase the literal grep target) |
| `git diff --stat go.mod go.sum` | empty (no new imports — `fmt` + `os` were already imported) |
| `make verify-import-firewall` | PASS (exit 0) — `cmd/credproxy/` not in the firewalled set; `cmd/tide-lint` multichecker clean |

End-to-end Ginkgo HARN-03 run is owned by Plan 04 Task 2's `02.1-04-VERIFICATION.md` (requires Plan 02's helm install-path fix to land in the kind suite's BeforeSuite first; Plans 02 + 03 are wave-2 parallel and file-disjoint).

## User Setup Required

None — no external service configuration required. The kind cluster does not need re-bootstrapping for this change; it only needs the credproxy image to be rebuilt. `make test-int-kind-prep` (invoked transitively by `make test-int`) rebuilds and loads `ghcr.io/jsquirrelz/tide-credproxy:test` into the kind cluster automatically, so the next `make test-int` run picks up the new banner with zero manual ops.

## Security Note (CLAUDE.md compliance)

CLAUDE.md's secret-material rule for the credproxy boot banner is honored verbatim: **the banner emits ONLY the `listenAddr` value via the single `%s` interpolation**. `TIDE_SIGNING_KEY`, `ANTHROPIC_API_KEY`, and `TIDE_TASK_UID` remain exclusively in the structured zap log (where they can be selectively redacted by zap config if needed) and are NOT in the plaintext banner. The `grep -c "fmt.Fprintf.*os.Stdout.*credproxy listening on"` gate returning exactly 1 also serves as a regression guard: a future edit that adds a second secret-leaking banner line would fail this gate.

## Parallel-Wave Coordination

This plan is **Wave 2 parallel with Plan 02** (`02.1-02-PLAN.md`, owns `test/integration/kind/suite_test.go`).

- **File disjointness:** Plan 02 modifies `test/integration/kind/suite_test.go` (and possibly `Makefile`). Plan 03 (this plan) modifies `cmd/credproxy/main.go`. Zero overlap.
- **Verification disjointness:** Plan 02 fixes the install-path drift (kustomize→helm in BeforeSuite) so the Layer B suite reaches per-spec execution at all. Plan 03 makes the 7th spec (HARN-03 log-line check) pass once the suite reaches it.
- **End-to-end interaction:** Both plans must land before HARN-03 will pass in the kind suite — Plan 02 is the precondition (the suite has to make it past BeforeSuite), Plan 03 is the contract satisfier (the banner has to exist when the grep runs).
- **Plan 04 Task 2** runs the full `make test-int` against both Plan 02 + Plan 03 already merged and asserts 7/7 Layer B specs pass.

## Next Phase Readiness

This plan completes its self-contained slice. Readiness signals for downstream consumers:

- **Plan 04 (Verification + CI wiring):** Ready. The banner emission is in place; `make test-int-kind-prep` will rebuild and load the credproxy image with the new banner on the next run. Plan 04 Task 2's verification script can grep for `credproxy listening on` in `kubectl logs` output to confirm the spec passes.
- **No blockers introduced.** No new dependencies, no new env vars, no new configuration surface, no schema changes, no behavior changes — purely additive stdout-write.

## Self-Check: PASSED

- `cmd/credproxy/main.go` exists and contains the banner line at line 128: FOUND.
- Commit `f62710b` exists in git history: FOUND (`git log --oneline -1` → `f62710b feat(02.1-03): add plaintext boot banner to cmd/credproxy/main.go`).
- Gate counts (1 / 1 / 0 / 0-mod/sum-changes) all match acceptance criteria.

---
*Phase: 02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te*
*Completed: 2026-05-13*
