---
phase: 02.1
slug: debug-fix-the-layer-b-kind-integration-test-suite-so-make-te
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-13
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

This phase IS a test-fix phase — the Layer B Ginkgo specs are themselves the validation. Plans MUST drive the Layer B suite from red → green.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go `testing` + Ginkgo v2.28 + Gomega |
| **Config file** | `test/integration/kind/suite_test.go` (BeforeSuite/AfterSuite controller install) |
| **Quick run command** | `make test-int-fast` (envtest only, no kind cluster) |
| **Full suite command** | `make test-int` (Layer A envtest + Layer B kind) |
| **Estimated runtime** | ~5 min (Layer A: <30s · Layer B: <5min target per Phase 2 success criterion 4) |

---

## Sampling Rate

- **After every task commit:** Run `go build ./...` + relevant package tests (`go test ./internal/...` or `go test ./cmd/...` scoped to changed files)
- **After every plan wave:** Run `make test-int-layer-a` (fast envtest tier — catches regressions) — note: this Makefile target may be named `test-int-fast` instead; consult Makefile lines 126-128
- **Before `/gsd-verify-work`:** Full `make test-int` must be green (Layer A + all 7 Layer B specs)
- **Max feedback latency:** 30s for unit + envtest; 5min for full kind tier

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02.1-01-T1 | 01 | 1 | roadmap-goal-baseline-capture | T-02.1-01-01..04 | Sed-redaction of TIDE_SIGNING_KEY before commit; decision log integrity gate | Markdown artifact + decision log presence | `test -f .planning/phases/02.1-.../02.1-BASELINE.md && grep -c '^## ' ... \| awk '{ if ($1 >= 7) exit 0; else exit 1 }' && grep -q 'D-01' ... && grep -q 'D-02' ... && grep -q 'D-03' ...` | yes (created by task) | ⬜ pending |
| 02.1-02-T1 | 02 | 2 | roadmap-goal-1-controller-manager-ready, roadmap-goal-2-webhook-endpoints-live, roadmap-goal-3-specs-pass-install-side | T-02.1-02-01..05 | Helm `--set` flags carry only static literals (no secrets); ensureSubagentSA helper hard-codes zero-RBAC SA per D-A4 | Go build + vet + integration test | `go build ./test/integration/kind/... && go vet ./test/integration/kind/... && grep -c 'helm.*install\|helm.*"install"' test/integration/kind/suite_test.go \| awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c '"kustomize"' test/integration/kind/suite_test.go \| awk '{ if ($1 == 0) exit 0; else exit 1 }'` followed by `make test-int-kind-prep && TIDE_REQUIRE_CONTROLLER=1 timeout 360s go test ./test/integration/kind/... -v -ginkgo.label-filter='kind' -ginkgo.procs=1` (≥6/7 specs PASS) | yes (test/integration/kind/suite_test.go pre-exists; modified in place) | ⬜ pending |
| 02.1-03-T1 | 03 | 2 | roadmap-goal-3-specs-pass-credproxy-banner-side | T-02.1-03-01..03 | Banner emits ONLY listenAddr (no HMAC token, no API key) per CLAUDE.md secret-material rule | Go build + vet + unit test + integration test | `go build ./cmd/credproxy/... && go vet ./cmd/credproxy/... && go test ./cmd/credproxy/... ./internal/credproxy/... && grep -c "fmt.Fprintf.*os.Stdout.*credproxy listening on" cmd/credproxy/main.go \| awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -v "^[[:space:]]*//" cmd/credproxy/main.go \| grep -c "credproxy listening on" \| awk '{ if ($1 == 1) exit 0; else exit 1 }' && make verify-import-firewall` plus the spec-level `ginkgo -focus='HARN-03: credproxy container log contains'` PASS | yes (cmd/credproxy/main.go pre-exists; modified in place) | ⬜ pending |
| 02.1-04-T1 | 04 | 3 | roadmap-goal-ci-green | T-02.1-04-02 | TIDE_REQUIRE_CONTROLLER=1 set in CI env to surface install failures loudly | YAML lint + grep gates | `grep -A2 "azure/setup-helm" .github/workflows/ci.yaml \| grep -c "v3.16.3" \| awk '{ if ($1 == 2) exit 0; else exit 1 }' && grep -c "TIDE_REQUIRE_CONTROLLER" .github/workflows/ci.yaml \| awk '{ if ($1 >= 1) exit 0; else exit 1 }' && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))" && grep -c "^  test-int:" .github/workflows/ci.yaml \| awk '{ if ($1 == 1) exit 0; else exit 1 }'` | yes (.github/workflows/ci.yaml pre-exists; edited) | ⬜ pending |
| 02.1-04-T2 | 04 | 3 | roadmap-goal-idempotency | T-02.1-04-01, T-02.1-04-03 | Verification artifact gates Task 3 (no phase-complete claim without proven green run + idempotency) | Markdown artifact + gate-decision presence | `test -f .planning/phases/02.1-.../02.1-04-VERIFICATION.md && grep -q "Gate Decision" ... && grep -qE 'APPROVED\|BLOCKED' ...` | yes (created by task) | ⬜ pending |
| 02.1-04-T3 | 04 | 3 | roadmap-goal-phase-closeout | T-02.1-04-03 | ROADMAP/STATE updates only run when Task 2 emits APPROVED gate | Grep gates against planning artifacts | `grep -c '02.1-01-PLAN.md' .planning/ROADMAP.md \| awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '02.1-02-PLAN.md' .planning/ROADMAP.md \| awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '02.1-03-PLAN.md' .planning/ROADMAP.md \| awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '02.1-04-PLAN.md' .planning/ROADMAP.md \| awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c 'Urgent work - to be planned' .planning/ROADMAP.md \| awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -c 'TBD (run /gsd-plan-phase 02.1' .planning/ROADMAP.md \| awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -c 'completed_plans: 28' .planning/STATE.md \| awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c 'Phase 02.1 closed' .planning/STATE.md \| awk '{ if ($1 >= 1) exit 0; else exit 1 }'` | yes (.planning/ROADMAP.md + .planning/STATE.md pre-exist; edited) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Phase 02.1 inherits the entire Layer B Ginkgo harness from Phase 2 — no new test framework install needed. The 7 specs already exist:

- `test/integration/kind/wave_test.go` — 3-task wave success + indegree decrement (2 `It` blocks)
- `test/integration/kind/failure_test.go` — sibling-continues + dependent-never-dispatches contract (1 `It` block)
- `test/integration/kind/caps_test.go` — harness wall-clock cap enforcement (1 `It` block)
- `test/integration/kind/output_test.go` — output-path violation rejected by validator (1 `It` block)
- `test/integration/kind/credproxy_test.go` — sidecar topology + listening-log line (2 `It` blocks)

*Wave 0 is empty — existing infrastructure covers all phase requirements. wave_0_complete: true.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `make test-int` runs end-to-end on a fresh developer laptop | ROADMAP scope | Requires clean `kind` cluster + Docker daemon — CI runs are not "developer laptop" | Wipe local kind clusters (`kind delete clusters --all`), restart Docker, run `make test-int` from a clean checkout. This IS one of the validation runs Plan 04 Task 2 performs and captures in `02.1-04-VERIFICATION.md`. |
| `make test-int` runs twice in a row idempotently | Inferred from "end-to-end" | Re-run safety verifies BeforeSuite cleanup logic — automated CI typically runs once | Plan 04 Task 2 performs this: first run with default (no KEEP_KIND_CLUSTER), then immediate second run with `KEEP_KIND_CLUSTER=true make test-int`. Both must pass. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (populated by `gsd-planner` 2026-05-13)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has an `<automated>` block)
- [x] Wave 0 covers all MISSING references (N/A — no Wave 0)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (unit/envtest) / 5min (kind)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending execution
