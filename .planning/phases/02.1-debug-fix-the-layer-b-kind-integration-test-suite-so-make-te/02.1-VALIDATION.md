---
phase: 02.1
slug: debug-fix-the-layer-b-kind-integration-test-suite-so-make-te
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-13
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

This phase IS a test-fix phase — the Layer B Ginkgo specs are themselves the validation. Plans MUST drive the Layer B suite from red → green.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go `testing` + Ginkgo v2.28 + Gomega |
| **Config file** | `test/integration/kind/suite_test.go` (BeforeSuite/AfterSuite controller install) |
| **Quick run command** | `make test-int-layer-a` (envtest only, no kind cluster) |
| **Full suite command** | `make test-int` (Layer A envtest + Layer B kind) |
| **Estimated runtime** | ~5 min (Layer A: <30s · Layer B: <5min target per Phase 2 success criterion 4) |

---

## Sampling Rate

- **After every task commit:** Run `go build ./...` + relevant package tests (`go test ./internal/...` or `go test ./cmd/...` scoped to changed files)
- **After every plan wave:** Run `make test-int-layer-a` (fast envtest tier — catches regressions)
- **Before `/gsd-verify-work`:** Full `make test-int` must be green (Layer A + all 7 Layer B specs)
- **Max feedback latency:** 30s for unit + envtest; 5min for full kind tier

---

## Per-Task Verification Map

*Populated by `gsd-planner` from per-plan task lists. Each row maps a task to its `<automated>` verify command.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD-by-planner | — | — | — | — | — | — | — | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Phase 02.1 inherits the entire Layer B Ginkgo harness from Phase 2 — no new test framework install needed. The 7 specs already exist:

- `test/integration/kind/wave_test.go` — 3-task wave success + indegree decrement (2 `It` blocks)
- `test/integration/kind/failure_test.go` — sibling-continues + dependent-never-dispatches contract (1 `It` block)
- `test/integration/kind/caps_test.go` — harness wall-clock cap enforcement (1 `It` block)
- `test/integration/kind/output_test.go` — output-path violation rejected by validator (1 `It` block)
- `test/integration/kind/credproxy_test.go` — sidecar topology + listening-log line (2 `It` blocks)

*Wave 0 is empty — existing infrastructure covers all phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `make test-int` runs end-to-end on a fresh developer laptop | ROADMAP scope | Requires clean `kind` cluster + Docker daemon — CI runs are not "developer laptop" | Wipe local kind clusters (`kind delete clusters --all`), restart Docker, run `make test-int` from a clean checkout |
| `make test-int` runs twice in a row idempotently | Inferred from "end-to-end" | Re-run safety verifies BeforeSuite cleanup logic — automated CI typically runs once | Run `make test-int && make test-int` — both must pass without manual cluster reset |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (N/A — no Wave 0)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (unit/envtest) / 5min (kind)
- [ ] `nyquist_compliant: true` set in frontmatter (set by planner after Per-Task map populated)

**Approval:** pending
