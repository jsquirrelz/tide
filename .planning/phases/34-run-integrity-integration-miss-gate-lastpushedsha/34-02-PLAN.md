---
phase: 34-run-integrity-integration-miss-gate-lastpushedsha
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - api/v1alpha2/shared_types.go
  - api/v1alpha1/shared_types.go
  - api/v1alpha2/plan_types.go
  - api/v1alpha1/plan_types.go
  - api/v1alpha2/zz_generated.deepcopy.go
  - api/v1alpha1/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_plans.yaml
  - internal/controller/git_writer.go
  - internal/controller/git_writer_test.go
  - internal/controller/push_helpers.go
  - internal/controller/push_helpers_test.go
  - internal/controller/project_controller.go
  - internal/metrics/registry.go
  - internal/metrics/registry_test.go
autonomous: true
requirements: [INTEG-02, INTEG-03, INTEG-04]
must_haves:
  truths:
    - "Condition/reason vocabulary for the integration gate exists in both API versions (parity)"
    - "Plan.Status carries a wave-integration bounded-retry counter in both API versions, and the regenerated Plan CRD schema includes it"
    - "Push/wave Jobs are labeled so a live List can find in-flight git-writers (D-02 prerequisite)"
    - "A package-level envelope reader exists that any reconciler can use, reading the per-pod termination log (never the shared PVC path)"
  artifacts:
    - path: "internal/controller/git_writer.go"
      provides: "succeededTaskBranches, gitWriterInFlightCount, readJobPushEnvelope shared helpers"
      exports: ["succeededTaskBranches", "gitWriterInFlightCount", "readJobPushEnvelope"]
    - path: "api/v1alpha2/plan_types.go"
      provides: "WaveIntegrationStatus struct + Plan.Status.WaveIntegration field"
      contains: "WaveIntegrationStatus"
    - path: "api/v1alpha2/shared_types.go"
      provides: "ConditionIntegrationIncomplete / ReasonIntegrationIncomplete / ReasonMergeConflict constants"
      contains: "ConditionIntegrationIncomplete"
    - path: "internal/metrics/registry.go"
      provides: "IntegrationOutcomesTotal counter"
      contains: "tide_integration_outcomes_total"
  key_links:
    - from: "internal/controller/push_helpers.go"
      to: "internal/controller/git_writer.go"
      via: "buildPushJob applies the git-writer labels the List gate matches on"
      pattern: "git-writer"
    - from: "internal/controller/git_writer.go"
      to: "internal/controller/project_controller.go"
      via: "readJobPushEnvelope unmarshals into pushResultEnvelope; the existing ProjectReconciler method delegates to it"
      pattern: "pushResultEnvelope"
---

<objective>
Land every shared contract the two wave-3 controller plans (34-04, 34-05) consume in parallel: API condition/reason vocabulary and the Plan wave-integration retry status (both versions, D-04/D-11), the git-writer Job labels plus the three shared controller helpers (D-02/D-03/D-07 machinery), the extended push-envelope struct fields (D-09/D-12 channel), and the D-12 integration-outcome metric.

Purpose: interface-first — 34-04 and 34-05 must compile against these symbols without touching each other's files.
Output: contracts compiled, unit-tested, CRDs regenerated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-CONTEXT.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-PATTERNS.md
</context>

## Assumptions (headless decisions, within CONTEXT discretion)

- **RESEARCH Open Question 1 resolved as recommended:** the wave-integration attempts counter lives on `Plan.Status` mirroring the #13b `BoundaryPushStatus` precedent (survives restarts, operator-visible), in BOTH API versions. There is no conversion webhook in this repo — dual-version parity means identical mirrored struct shapes + regenerated CRD schemas (the `IntegratedThroughWave` precedent).
- **Naming pinned** (CONTEXT leaves it to discretion): condition `IntegrationIncomplete`; reasons `IntegrationIncomplete`, `MergeConflict`; metric `tide_integration_outcomes_total{project, outcome}` with outcome ∈ {success, miss, conflict, transient, stamp-skip}; Job labels `tideproject.k8s/role: git-writer` + `tideproject.k8s/project: <name>`.
- **Envelope detail channel:** new optional fields on the push envelope (`missingBranches`, `missingTotal`, `conflictBranch`) rather than overloading the `reason` string — matches the existing typed-envelope design; termination-log 4096-byte cap respected by truncating to first 10 + total (D-12).

<tasks>

<task type="auto">
  <name>Task 1: API vocabulary + WaveIntegrationStatus in both versions + CRD regen</name>
  <files>api/v1alpha2/shared_types.go, api/v1alpha1/shared_types.go, api/v1alpha2/plan_types.go, api/v1alpha1/plan_types.go, api/v1alpha2/zz_generated.deepcopy.go, api/v1alpha1/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_plans.yaml</files>
  <read_first>
    - api/v1alpha2/shared_types.go (reason-block style at lines ~197–212: ReasonWaveIntegrationFailed doc-comment pattern)
    - api/v1alpha1/shared_types.go (parity mirror of the same constants — both files carry the vocabulary)
    - api/v1alpha2/project_types.go (BoundaryPushStatus at ~:289 — the struct shape to mirror)
    - api/v1alpha2/plan_types.go and api/v1alpha1/plan_types.go (IntegratedThroughWave dual-version parity precedent)
    - api/v1alpha1/phase4_constants_test.go (constants parity-guard test style — new constants may need mirrored literals)
  </read_first>
  <action>
    Implements D-04 (retry state home) and D-11 vocabulary. (1) Add a "Phase 34 condition + reason vocabulary" const block to BOTH `api/v1alpha2/shared_types.go` and `api/v1alpha1/shared_types.go`: `ConditionIntegrationIncomplete = "IntegrationIncomplete"` (Project condition, push-gate outcome — sticky after retry cap per D-08), `ReasonIntegrationIncomplete = "IntegrationIncomplete"`, `ReasonMergeConflict = "MergeConflict"` (content conflict — parks immediately per D-09; also the Plan-Failed reason for D-10). Doc-comment each following the ReasonWaveIntegrationFailed style. (2) Add to BOTH `plan_types.go` files: `type WaveIntegrationStatus struct { Wave int `json:"wave,omitempty"`; Attempts int32 `json:"attempts,omitempty"`; LastAttemptTime *metav1.Time `json:"lastAttemptTime,omitempty"`; LastError string `json:"lastError,omitempty"` }` mirroring BoundaryPushStatus's doc style, plus `WaveIntegration WaveIntegrationStatus `json:"waveIntegration,omitempty"`` on `PlanStatus`. Wave records which Kahn wave the Attempts counter refers to (counter resets when the blocking wave changes). (3) Run `make manifests generate` to regenerate deepcopy + CRD YAML. Cite D-04, D-08, D-09, D-10, D-11 in the doc comments where each constant/field serves that decision.
  </action>
  <verify>
    <automated>go build ./api/... && go test ./api/... -count=1 && grep -c "waveIntegration" config/crd/bases/tideproject.k8s_plans.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ConditionIntegrationIncomplete = "IntegrationIncomplete"' api/v1alpha2/shared_types.go` returns 1, and the same grep on api/v1alpha1/shared_types.go returns 1
    - `grep -c 'ReasonMergeConflict = "MergeConflict"' api/v1alpha2/shared_types.go` returns 1 (v1alpha1 likewise)
    - `grep -c 'WaveIntegrationStatus struct' api/v1alpha2/plan_types.go` and on api/v1alpha1/plan_types.go each return 1
    - `grep -c 'waveIntegration' config/crd/bases/tideproject.k8s_plans.yaml` ≥ 2 (schema present under both served versions)
    - `go test ./api/... -count=1` passes (parity/aggregate guard tests green — extend them if they enumerate constants)
  </acceptance_criteria>
  <done>Vocabulary + retry-status contract exists identically in both API versions; CRD schema regenerated; api tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: git-writer labels + shared helpers (List gate, cumulative set, envelope reader)</name>
  <files>internal/controller/git_writer.go, internal/controller/git_writer_test.go, internal/controller/push_helpers.go, internal/controller/push_helpers_test.go, internal/controller/project_controller.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go (lines ~293–329: `plannerInFlightCount` — the D-02 List-gate template; `isJobTerminal`)
    - internal/controller/push_helpers.go (buildPushJob ObjectMeta at ~:205 — currently carries NO labels; `--integrate-task-branches` CSV arg plumbing at ~:158)
    - internal/controller/project_controller.go (lines 66–109: `pushResultEnvelope` struct + `readPushEnvelope` method to generalize)
    - internal/controller/boundary_push.go (lines 215–222: the per-caller Succeeded-branch collection being centralized)
    - cmd/tide/resume.go (~:274: List-by-project-label precedent) and test/integration/kind/fixtures_test.go (~:106: `tideproject.k8s/project` label)
    - pkg/git/worktree.go (TaskBranchName)
  </read_first>
  <behavior>
    - `succeededTaskBranches(ctx, c, namespace, projectName)` returns the sorted branch names (`tide/wt-<uid>`) of every Task CR in the namespace labeled `tideproject.k8s/project=<projectName>` whose Status.Phase is Succeeded; empty slice when none.
    - `gitWriterInFlightCount(ctx, c, namespace, projectName, excludeJobName)` counts non-terminal Jobs matching BOTH labels (`tideproject.k8s/role=git-writer`, `tideproject.k8s/project=<projectName>`), skipping Jobs with a DeletionTimestamp and skipping the Job named excludeJobName (Pitfall 7 self-exclusion — a state machine must not deadlock on the Job it itself observes).
    - `readJobPushEnvelope(ctx, c, namespace, jobName)` returns the parsed pushResultEnvelope from the FIRST pod labeled `job-name=<jobName>` with a terminated container carrying a non-empty termination message; (zero, false) otherwise. Termination-log only — never the shared PVC JSON (Pitfall 2 collision).
    - `buildPushJob` output Job carries both git-writer labels.
  </behavior>
  <action>
    Implements D-02 (gate helper), D-03/D-07 (cumulative-set helper), and the D-09/D-14 envelope channel. (1) New file `internal/controller/git_writer.go`: label constants `gitWriterRoleLabelKey = "tideproject.k8s/role"`, `gitWriterRoleLabelValue = "git-writer"`, `gitWriterProjectLabelKey = "tideproject.k8s/project"`, plus the three package-level functions per <behavior> (copy `plannerInFlightCount`'s shape for the gate; use RESEARCH Pattern 1's shape for the set helper with `sort.Strings` for deterministic Job args across retries; match Task Succeeded phase the same way boundary_push.go:216–221 does). (2) `push_helpers.go`: buildPushJob ObjectMeta gains `Labels: {role: git-writer, project: project.Name}` using the new constants; extend `TestBuildPushJob*` in push_helpers_test.go to assert both labels. (3) `project_controller.go` — TWO minimal edits ONLY (plan 34-05 owns the rest of this file): (a) extend `pushResultEnvelope` with `MissingBranches []string `json:"missingBranches,omitempty"``, `MissingTotal int `json:"missingTotal,omitempty"``, `ConflictBranch string `json:"conflictBranch,omitempty"``; (b) replace the body of the `readPushEnvelope` method with a one-line delegation to `readJobPushEnvelope(ctx, r.Client, namespace, pushJobName)`. (4) `git_writer_test.go`: fake-client/envtest units per <behavior> — including the exclusion case (an in-flight Job named excludeJobName is not counted) and the terminated-pod envelope parse (construct a Pod with `job-name` label and a Terminated container state carrying envelope JSON with the new fields).
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'GitWriter|SucceededTaskBranches|PushEnvelope|BuildPushJob' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func succeededTaskBranches' internal/controller/git_writer.go` = 1; same for `func gitWriterInFlightCount` and `func readJobPushEnvelope`
    - `grep -c 'git-writer' internal/controller/push_helpers.go` ≥ 1 (labels applied in buildPushJob)
    - `grep -c 'missingBranches' internal/controller/project_controller.go` = 1 and `grep -c 'conflictBranch' internal/controller/project_controller.go` = 1 (struct fields landed)
    - Unit tests cover: sorted-branch determinism, DeletionTimestamp skip, excludeJobName skip, envelope round-trip with MissingBranches/ConflictBranch
    - `go build ./internal/...` exits 0; targeted test run green
  </acceptance_criteria>
  <done>Both wave-3 plans can List in-flight git-writers, compute the cumulative Succeeded set, and read typed envelopes without touching each other's files.</done>
</task>

<task type="auto">
  <name>Task 3: IntegrationOutcomesTotal metric (D-12)</name>
  <files>internal/metrics/registry.go, internal/metrics/registry_test.go</files>
  <read_first>
    - internal/metrics/registry.go (lines ~173–179: PushJobsTotal CounterVec shape + registration block)
    - internal/metrics/registry_test.go (seed-and-assert test pattern)
  </read_first>
  <action>
    Implements D-12's result-labeled integration-outcome counter beside PushJobsTotal. Add `IntegrationOutcomesTotal = prometheus.NewCounterVec(prometheus.CounterOpts{Name: "tide_integration_outcomes_total", Help: "Count of terminal integration/verify outcomes, with outcome ∈ {success, miss, conflict, transient, stamp-skip} (Phase 34 D-12)."}, []string{"project", "outcome"})`, register it in the same MustRegister block as PushJobsTotal, and extend the seed-and-assert registry test to cover the new vec. `stamp-skip` is the Pitfall-4 observability arm (envelope unreadable at stamp time — TTL'd pod); `miss`/`conflict`/`transient` map to the tide-push envelope reasons; `success` fires on successful integrations and verified pushes.
  </action>
  <verify>
    <automated>go test ./internal/metrics/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tide_integration_outcomes_total' internal/metrics/registry.go` = 1
    - registry_test.go asserts the new counter registers and increments with labels (project, outcome)
    - `go test ./internal/metrics/ -count=1` passes
  </acceptance_criteria>
  <done>Counter available for 34-04/34-05 arms; kubectl-describe + Prometheus both observable per D-12 (COST-02 lesson: pod logs get GC'd).</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- Constants (both `api/v1alpha1` and `api/v1alpha2` `shared_types.go`): `ConditionIntegrationIncomplete`, `ReasonIntegrationIncomplete`, `ReasonMergeConflict`
- Type `WaveIntegrationStatus{Wave, Attempts, LastAttemptTime, LastError}` + field `PlanStatus.WaveIntegration` (both API versions; CRD schema key `waveIntegration`)
- New file `internal/controller/git_writer.go`: `succeededTaskBranches()`, `gitWriterInFlightCount()`, `readJobPushEnvelope()`, label constants `gitWriterRoleLabelKey/Value`, `gitWriterProjectLabelKey`
- Job labels `tideproject.k8s/role: git-writer` + `tideproject.k8s/project: <name>` on every buildPushJob output
- `pushResultEnvelope` fields: `MissingBranches []string`, `MissingTotal int`, `ConflictBranch string`
- Metric `tide_integration_outcomes_total{project, outcome}` (`internal/metrics.IntegrationOutcomesTotal`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job pod → controller | envelope crosses via pod termination log (API-server-mediated) |
| controller → K8s API | new label-scoped Job Lists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-03 | Tampering | envelope channel | medium | mitigate | readJobPushEnvelope reads ONLY the per-pod termination log (API-server-mediated, pod-scoped); the shared PVC JSON path is never consumed for controller decisions (Pitfall 2) |
| T-34-04 | Elevation of privilege | new Job labels / List gate | low | accept | Labels are metadata only; Lists reuse the manager's existing Job read RBAC (plannerInFlightCount precedent) — no RBAC widening |
| T-34-05 | Spoofing | branch names in status/args | medium | mitigate | succeededTaskBranches derives branches exclusively from controller-generated Task UIDs via TaskBranchName — never from CRD spec input (RESEARCH V5 control) |
| T-34-SC | Tampering | package installs | low | accept | No new packages (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `go build ./...` green; `go test ./api/... ./internal/metrics/ -count=1` green
- Targeted controller tests for the three helpers green
- CRD YAML contains `waveIntegration` under both versions
</verification>

<success_criteria>
- 34-04 and 34-05 can be authored/executed in parallel against these symbols with zero file overlap between them
- No chart file touched (`charts/` diff empty — values.yaml FIXED constraint)
</success_criteria>

<output>
Create `.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-02-SUMMARY.md` when done.
</output>
