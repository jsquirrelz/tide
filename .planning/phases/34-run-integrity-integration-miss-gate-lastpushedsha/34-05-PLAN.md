---
phase: 34-run-integrity-integration-miss-gate-lastpushedsha
plan: 05
type: execute
wave: 3
depends_on: [34-02, 34-03]
files_modified:
  - internal/controller/project_controller.go
  - internal/controller/project_boundary_push_test.go
  - internal/controller/project_pushresult_test.go
  - cmd/tide/resume.go
  - cmd/tide/resume_test.go
autonomous: true
requirements: [INTEG-03, INTEG-04]
must_haves:
  truths:
    - "After a successful boundary push, status.git.lastPushedSHA equals the push envelope's HeadSHA — stamped in the same arm that sets BoundaryPushed=True"
    - "An integration-incomplete push failure rides the #13b bounded retry; only after the attempt cap does a sticky IntegrationIncomplete condition park the push, naming each missing task + branch (truncated)"
    - "A merge-conflict push failure parks immediately with a distinct reason (no retries burned on a content problem)"
    - "Mid-run (pre-Complete) boundary pushes are observed: success stamps the SHA, failure classifies and retries — not only the Complete fast-path"
    - "tide resume resets the boundary-push attempt counter and clears the sticky condition via the annotation mechanism; the condition also auto-clears on a later successful verify+push"
  artifacts:
    - path: "internal/controller/project_controller.go"
      provides: "LastPushedSHA stamp, IntegrationIncomplete/MergeConflict arms, mid-run push observation, reset-annotation consumption"
      contains: "LastPushedSHA = "
    - path: "cmd/tide/resume.go"
      provides: "D-13 boundary-push reset via annotation"
      contains: "reset-boundary-push"
  key_links:
    - from: "internal/controller/project_controller.go"
      to: "internal/controller/git_writer.go"
      via: "success/failure arms read the envelope via readJobPushEnvelope (delegated method); dispatch uses succeededTaskBranches + gitWriterInFlightCount"
      pattern: "succeededTaskBranches|gitWriterInFlightCount"
    - from: "cmd/tide/resume.go"
      to: "internal/controller/project_controller.go"
      via: "annotation tideproject.k8s/reset-boundary-push set by CLI, consumed once by the controller"
      pattern: "reset-boundary-push"
---

<objective>
Land the operator-facing half of the gate: the D-14 `lastPushedSHA` stamp (INTEG-04), the D-08/D-09/D-11/D-12 condition arms for miss and conflict on the Project, cumulative-set + single-flight gating in `dispatchBoundaryPush` (D-03/D-02), mid-run push observation, and D-13 `tide resume` recovery.

Purpose: a gate miss must be diagnosable from `kubectl describe project` alone and recoverable with the single sanctioned verb — and the force-with-lease fence must finally be armed.
Output: ProjectReconciler + tide CLI changes, envtest/unit covered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-CONTEXT.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-PATTERNS.md
</context>

## Assumptions (headless decisions, within CONTEXT discretion)

- **RESEARCH Open Question 2 resolved as option (b)** — the explicit scope decision the research demanded: ProjectReconciler observes ANY terminal `tide-push-<project.UID>` Job (mid-run included), handling BOTH arms — success (SHA stamp, INTEG-04) and failure (condition + D-08 bounded retry). Rationale: SC-3/SC-4 are phrased generically over boundary pushes, D-05 admits no unverified push class, and D-12 requires kubectl-describe diagnosability; a mid-run miss surfacing nothing until Complete would violate that. Mechanically cheap: `Owns(&batchv1.Job{})` already re-enqueues the Project on Job completion (verified at project_controller.go:2070); the state machine gains a `dispatchIfMissing` flag so the mid-run path never INITIATES pushes (level triggers own initiation).
- **Reset annotation name:** `tideproject.k8s/reset-boundary-push` (bypassPushLeaseAnnotation precedent, project_controller.go:125–128). `kubectl annotate` remains the escape hatch per D-13.
- **Gate-wait is not an attempt:** when `dispatchBoundaryPush` requeues on the D-02 gate, `BoundaryPush.Attempts` is NOT incremented — attempts count dispatched Jobs, not waits.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Success arm SHA stamp + mid-run observation + gated cumulative dispatch (D-14, D-03, D-02)</name>
  <files>internal/controller/project_controller.go, internal/controller/project_boundary_push_test.go</files>
  <read_first>
    - internal/controller/project_controller.go (Complete fast-path :496–505; #13b machine :668–884 — esp. success arm :734–748, failure switch :753–823, cap arm :700–718, dispatchBoundaryPush :842–884, deleteFailedPushJob :895–904; the unimplemented promise comment at :472–474; SetupWithManager Owns(Job) :2070)
    - internal/controller/git_writer.go (readJobPushEnvelope, succeededTaskBranches, gitWriterInFlightCount from 34-02)
    - internal/controller/project_boundary_push_test.go and project_pushresult_test.go (existing #13b spec patterns, envelope-pod construction)
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md (Pitfall 4: TTL erases evidence — stamp-skip tolerance)
  </read_first>
  <behavior>
    - Test 1 (D-14): a Succeeded push Job whose pod termination log carries HeadSHA "abc123..." → Status.Git.LastPushedSHA == that SHA, set in the SAME reconcile pass that sets BoundaryPushed=True/Pushed (CONTEXT specifics: observed first-run state was BoundaryPushed=True + empty SHA — the pair must move together).
    - Test 2 (Pitfall 4): a Succeeded push Job with NO readable envelope (pod gone) → BoundaryPushed=True still set, LastPushedSHA left as-was, IntegrationOutcomesTotal{outcome="stamp-skip"} incremented — the condition is never blocked on envelope readability.
    - Test 3 (mid-run success): a project NOT in Complete phase with a terminal Succeeded push Job → SHA stamped all the same.
    - Test 4 (auto-clear): a successful push while ConditionIntegrationIncomplete is True → condition removed (D-13 auto-clear).
    - Test 5 (dispatch): dispatchBoundaryPush builds PushOptions with IntegrateTaskBranches = the live sorted Succeeded set; with another git-writer Job in flight it requeues WITHOUT incrementing Attempts.
  </behavior>
  <action>
    In `project_controller.go`: (1) D-14 — in the push-Job success arm (:734–748), read the envelope (`r.readPushEnvelope`, now delegating to readJobPushEnvelope) and when available set `project.Status.Git.LastPushedSHA = env.HeadSHA` inside the SAME MergeFrom status patch that resets LeaseFailureCount; on unreadable envelope: structured log + IntegrationOutcomesTotal{project, "stamp-skip"}.Inc(), condition transition untouched (Pitfall 4); also RemoveStatusCondition(ConditionIntegrationIncomplete) if present (auto-clear) and increment IntegrationOutcomesTotal{project, "success"}; finally resolve the stale doc comment at :472–474 (the promise is now implemented). (2) Mid-run observation (Assumption/OQ2 option b): change `reconcileBoundaryPush` to `reconcileBoundaryPush(ctx, project, dispatchIfMissing bool)` — when the deterministic Job is absent: dispatch only if dispatchIfMissing (Complete fast-path :504 passes true), else return no-op. In the main Reconcile path for non-Complete projects (immediately after the Complete fast-path check), Get `tide-push-<project.UID>`; if it exists AND isJobTerminal → return `r.reconcileBoundaryPush(ctx, project, false)`; if it exists and is running, fall through (Owns(Job) re-enqueues on completion). (3) D-03/D-02 in `dispatchBoundaryPush` (:842–884): set `IntegrateTaskBranches` from succeededTaskBranches (the project-level nil-branch site RESEARCH flagged); before Create, gitWriterInFlightCount excluding pushJobName — if busy, requeue 5s WITHOUT touching Attempts. (4) Extend project_boundary_push_test.go per <behavior>.
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'ProjectBoundaryPush|PushResult|LastPushedSHA' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Status.Git.LastPushedSHA = ' internal/controller/project_controller.go` ≥ 1 (the never-landed assignment now exists in non-test code)
    - `grep -c 'dispatchIfMissing' internal/controller/project_controller.go` ≥ 2 (signature + Complete fast-path call)
    - `grep -c 'succeededTaskBranches' internal/controller/project_controller.go` ≥ 1 (dispatch site carries the cumulative set)
    - All five behavior tests green; existing #13b specs still green
  </acceptance_criteria>
  <done>The force-with-lease fence is armed (INTEG-04) for every boundary push, Complete-path and mid-run alike.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failure arms — miss retry-then-stick + conflict park (D-08, D-09, D-11, D-12)</name>
  <files>internal/controller/project_controller.go, internal/controller/project_boundary_push_test.go</files>
  <read_first>
    - internal/controller/project_controller.go (failure-arm switch :753–823 — the lease-rejected sticky arm at :781–798 is the shape to mirror; re-entry guards :682–695; cap-exhaustion arm :700–718)
    - api/v1alpha2/shared_types.go (ConditionIntegrationIncomplete, ReasonIntegrationIncomplete, ReasonMergeConflict from 34-02)
    - internal/metrics/registry.go (IntegrationOutcomesTotal)
  </read_first>
  <behavior>
    - Test 1 (D-08 retry): a Failed push Job with envelope reason "integration-incomplete" routes through the bounded-retry default (Attempts++, Background job delete, re-dispatch with backoff), LastError recorded, IntegrationOutcomesTotal{outcome="miss"} incremented — no sticky condition yet.
    - Test 2 (D-08 stick at cap): after maxBoundaryPushAttempts (5), ConditionIntegrationIncomplete=True with Reason=IntegrationIncomplete; the message names each missing task AND its branch (missing branches from the envelope's missingBranches, mapped to Task names via the branch's embedded task UID / a Task List), truncated to the first 10 with the missingTotal count appended; push not re-dispatched (parked); once-only guard (no counter churn on subsequent reconciles).
    - Test 3 (D-09 conflict park): envelope reason "merge-conflict" parks IMMEDIATELY — ConditionIntegrationIncomplete=True with Reason=MergeConflict, message naming env.ConflictBranch and the run branch plus the recovery hint (replan, then tide resume); zero further dispatch attempts; IntegrationOutcomesTotal{outcome="conflict"} incremented once.
    - Test 4 (existing arms untouched): leak-detected and lease-rejected behavior unchanged.
  </behavior>
  <action>
    Implements D-08/D-09/D-11/D-12 in the failure-arm switch. (1) Case `"merge-conflict"`: mirror the lease-rejected sticky-arm shape (:781–798) — set ConditionIntegrationIncomplete (Type from 34-02) with Reason=ReasonMergeConflict, Message = "merge conflict integrating <conflictBranch> into <runBranch>: content problem, human needed — replan, then `tide resume --retry-failed`" (branch names only, never diff content — RESEARCH V5/D-12 control), BoundaryPush.LastError="merge-conflict", metric outcome "conflict", FindStatusCondition re-entry guard, return without requeue. (2) Reason `"integration-incomplete"`: flow into the EXISTING default bounded-retry arm (attempts increment, deleteFailedPushJob Background, boundaryPushRequeue) but record LastError="integration-incomplete" and metric outcome "miss"; persist the envelope's MissingBranches/MissingTotal into the retry state (e.g. carry via LastError suffix or re-read the envelope at the cap arm before the Job is deleted — pin: capture the missing list at classification time and store the formatted detail string in BoundaryPush.LastError, since the Job/pod may be TTL'd by cap time). (3) Cap-exhaustion arm (:700–718): when LastError starts with "integration-incomplete", set the sticky ConditionIntegrationIncomplete with Reason=ReasonIntegrationIncomplete and the D-12 message (each missing task + branch, first 10 + total; map branch `tide/wt-<uid>` → Task name by listing the project's Task CRs and matching UIDs); once-only guard mirroring :688–695. (4) Extend the tests per <behavior>.
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'IntegrationIncomplete|MergeConflict|ProjectBoundaryPush' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "merge-conflict"' internal/controller/project_controller.go` = 1 and the case body contains no re-dispatch call
    - `grep -c 'ConditionIntegrationIncomplete' internal/controller/project_controller.go` ≥ 3 (conflict arm, cap arm, success auto-clear)
    - Condition messages verified in tests to contain task names + branch names and the truncation count for >10 misses
    - All behavior tests green; `go test ./internal/controller/ -count=1` green
  </acceptance_criteria>
  <done>A miss is retried then parked sticky; a conflict parks instantly with a distinct reason — both diagnosable from kubectl describe project alone (D-12).</done>
</task>

<task type="auto">
  <name>Task 3: tide resume — boundary-push reset via annotation (D-13)</name>
  <files>cmd/tide/resume.go, cmd/tide/resume_test.go, internal/controller/project_controller.go</files>
  <read_first>
    - cmd/tide/resume.go (resumeRun :70–125 — the annotation/status-patch re-fetch discipline: metadata and status are separate subresources with separate resourceVersion windows)
    - internal/controller/project_controller.go (:125–128 bypassPushLeaseAnnotation declaration; :527 consumption site — the pattern to mirror)
    - cmd/tide/resume_test.go and resume_failure_test.go (existing CLI test style)
  </read_first>
  <action>
    Implements D-13 (single-recovery-verb; annotation mechanism pinned by CONTEXT). (1) Declare `resetBoundaryPushAnnotation = "tideproject.k8s/reset-boundary-push"` beside bypassPushLeaseAnnotation in project_controller.go. (2) `cmd/tide/resume.go`: in resumeRun, when the fetched Project has ConditionIntegrationIncomplete OR Status.BoundaryPush.Attempts > 0, add a metadata patch setting the annotation to "true" (follow the file's re-fetch-between-patches discipline; print a one-line notice mirroring the existing resume output style). (3) Controller consumption, mirroring the :527 bypass pattern: when the annotation is "true" — status-patch `BoundaryPush.Attempts = 0` and `BoundaryPush.LastError = ""`, RemoveStatusCondition(ConditionIntegrationIncomplete), then a separate metadata patch deleting the annotation (consume-once). Doc-comment that `kubectl annotate project <name> tideproject.k8s/reset-boundary-push=true` is the sanctioned escape hatch. (4) Tests: CLI test asserting the annotation lands when the condition/attempts state is present (and does NOT land otherwise); controller test asserting consumption resets Attempts, clears the condition, and removes the annotation, after which the push state machine re-dispatches.
  </action>
  <verify>
    <automated>go test ./cmd/tide/ -run 'Resume' -count=1 && go test ./internal/controller/ -run 'ResetBoundaryPush|Resume' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'reset-boundary-push' cmd/tide/resume.go` ≥ 1 and `grep -c 'resetBoundaryPushAnnotation' internal/controller/project_controller.go` ≥ 2 (declared + consumed)
    - Controller test proves: annotation consumed exactly once (removed after reset), Attempts back to 0, condition gone, re-dispatch occurs on the next reconcile
    - `go build ./cmd/tide/` exits 0; targeted tests green
  </acceptance_criteria>
  <done>One verb recovers a parked push; the sticky condition also self-clears on any later successful verify+push (Task 1).</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `Status.Git.LastPushedSHA` assignment (first ever in non-test code) in the push success arm — INTEG-04
- `reconcileBoundaryPush(ctx, project, dispatchIfMissing bool)` signature + mid-run terminal-Job observation path
- Failure-arm cases: `merge-conflict` (immediate park, Reason=MergeConflict) and `integration-incomplete` (bounded retry → sticky Reason=IntegrationIncomplete at cap) on `ConditionIntegrationIncomplete`
- D-12 condition message format: per-missing "task <name> (branch <tide/wt-uid>)", first 10 + total
- `resetBoundaryPushAnnotation = "tideproject.k8s/reset-boundary-push"` + `tide resume` extension + consume-once controller reset
- `IntegrationOutcomesTotal` increments: success / miss / conflict / stamp-skip at push-Job terminal arms

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job pod → controller | envelope (HeadSHA, reason, missing/conflict detail) drives status + conditions |
| operator → controller | reset annotation (kubectl/tide CLI) mutates retry state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-12 | Repudiation | silent gate misses (GC'd pod logs) | high | mitigate | Sticky condition + result-labeled metric + named missing-branch message — diagnosable from kubectl describe alone (D-12, COST-02 lesson) |
| T-34-13 | Information disclosure | condition messages | medium | mitigate | Messages carry task names + branch names only — never diff content or remote URLs; branch names are controller-generated UIDs |
| T-34-14 | Tampering | forged envelope stamping a bogus LastPushedSHA | medium | mitigate | Envelope read only from the deterministic controller-owned Job's pod termination log via the API server; a bogus SHA merely disarms force-with-lease to the pre-existing (current) safety level |
| T-34-15 | Elevation of privilege | reset annotation abused to spin unbounded pushes | low | accept | Annotation requires RBAC to patch Projects (same bar as kubectl edit); each reset grants exactly one fresh attempt budget of 5 (bounded) |
| T-34-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- Targeted controller + CLI test runs green; full `go test ./internal/controller/ ./cmd/tide/ -count=1` green
- Grep gates: LastPushedSHA assignment exists; both failure cases present; annotation declared + consumed
</verification>

<success_criteria>
- SC-3 satisfied: miss → typed reason → bounded retry → sticky condition; verdict always recomputed from git by the Job (the controller never caches a completeness verdict in .status — locked constraint)
- SC-4 satisfied: lastPushedSHA == push envelope HeadSHA after success, same arm as BoundaryPushed=True
- `Complete` still stamps as the control-plane rollup; only the PUSH parks (#13b carried forward, locked)
</success_criteria>

<output>
Create `.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-05-SUMMARY.md` when done.
</output>
