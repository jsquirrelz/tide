---
phase: 34-run-integrity-integration-miss-gate-lastpushedsha
plan: 06
type: execute
wave: 4
depends_on: [34-01, 34-04, 34-05]
files_modified:
  - test/integration/kind/integration_miss_test.go
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03, INTEG-04, INTEG-05]
must_haves:
  truths:
    - "Both INTEG-05 repro specs — RED in plan 34-01 — now pass GREEN against the fixed code with no assertion weakened"
    - "The full make test-int suite (Layer A + Layer B + plain go-test contract tests) exits 0"
    - "Layer B fallout from the delayed Plan=Succeeded timing (Pitfall 6) is resolved without deleting or skipping any spec"
  artifacts:
    - path: "test/integration/kind/integration_miss_test.go"
      provides: "GREEN regression lock for the wave-parallel integration miss"
      min_lines: 150
  key_links:
    - from: "test/integration/kind/integration_miss_test.go"
      to: "internal/controller/plan_controller.go"
      via: "spec GREEN depends on the full-range wave loop + cumulative triggers landing"
      pattern: "integration miss"
---

<objective>
Flip the INTEG-05 regression specs from RED to GREEN and take the phase gate: full `make test-int` green under the MAKE_EXIT + FAIL-grep discipline, with kind-level Pitfall-6 fallout resolved and spec-count conserved.

Purpose: the phase's verification hinges on the same specs that demonstrated the bug now locking the fix — plus proof no existing behavior regressed.
Output: GREEN focused run + green full suite + evidence in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-CONTEXT.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Focused GREEN run of the integration-miss specs</name>
  <files>(run-only; no file changes expected)</files>
  <read_first>
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-01-SUMMARY.md (the recorded RED evidence — the exact assertions that must now pass)
    - CLAUDE.md (constrained-VM recipe: fresh kind cluster per heavy run, one heavy run at a time; never concurrent with an acceptance cluster)
  </read_first>
  <action>
    Fresh-cluster GREEN run: (1) `kind delete cluster --name tide-test || true`; recreate from `test/integration/kind/cluster.yaml`; pre-warm (provisioner Ready + `kind load docker-image busybox:1.36 --name tide-test`); (2) `make test-int-kind-prep` (rebuilds ALL images so the fixed manager + tide-push binaries are what runs — verify image digests changed vs any cached run); (3) `go test ./test/integration/kind/... -v -timeout=40m -ginkgo.v -ginkgo.focus='integration miss' 2>&1 | tee /tmp/34-06-green-run.log`. Both specs must pass: single-wave degenerate (both branches ancestors) and 2-parallel-task final wave (all three branches ancestors + `lastPushedSHA` non-empty + BoundaryPushed=True). If a spec fails, this is a fix defect, not a test defect — debug the controller/tide-push path (read manager logs + kubectl describe per CLAUDE.md Observe First) rather than adjusting assertions.
  </action>
  <verify>
    <automated>tail -30 /tmp/34-06-green-run.log && grep -cE 'SUCCESS!|Test Suite Passed' /tmp/34-06-green-run.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/34-06-green-run.log shows both "integration miss" specs passed (Ginkgo `Ran N Specs` with 0 failures for the focused set)
    - go test exit code 0 for the focused run
    - No assertion in integration_miss_test.go was weakened or removed relative to the 34-01 RED commit (`git diff <red-commit> -- test/integration/kind/integration_miss_test.go` shows no loosened Expects; timeout bumps are acceptable)
  </acceptance_criteria>
  <done>The observed first-run failure shape (Complete + BoundaryPushed=True + missing deliverable + empty lastPushedSHA) is impossible on fixed code, proven in-cluster.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite phase gate + kind-level Pitfall-6 sweep</name>
  <files>test/integration/kind/integration_miss_test.go</files>
  <read_first>
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md (Pitfall 6; Validation Architecture phase-gate row)
    - CLAUDE.md (Verify Before Claiming: `make test-int` exit ≠ Ginkgo green — read MAKE_EXIT AND grep FAIL lines; plain go-tests share the kind package)
  </read_first>
  <action>
    Phase gate: (1) fresh cluster again per the constrained-VM recipe; (2) `make test-int 2>&1 | tee /tmp/34-06-full-suite.log; echo "MAKE_EXIT=$?" | tee -a /tmp/34-06-full-suite.log`; (3) `grep -nE '^--- FAIL|^FAIL[[:space:]]' /tmp/34-06-full-suite.log` must return nothing. If existing Layer B specs fail on the new delayed Plan=Succeeded timing (Pitfall 6 — git-configured fixtures now wait for final-wave integration Jobs), extend their timeouts or assert the new intermediate state; the only file this plan may modify is test code, and only timing/ordering expectations — spec-count conservation is a hard rule (count `Ran [0-9]+ of [0-9]+ Specs` totals against the pre-phase baseline; no spec deleted, no spec newly Pending/Skipped). If a PRODUCTION defect surfaces, stop and report it as a gap rather than patching production code in this plan (that work belongs to a gap-closure plan). Record in the SUMMARY: MAKE_EXIT value, Ginkgo summary lines for both layers, spec-count comparison, and any test adjustments with rationale.
  </action>
  <verify>
    <automated>grep 'MAKE_EXIT=' /tmp/34-06-full-suite.log && grep -cE '^--- FAIL|^FAIL[[:space:]]' /tmp/34-06-full-suite.log || true</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/34-06-full-suite.log ends with MAKE_EXIT=0
    - grep for FAIL lines (`^--- FAIL` and `^FAIL` + whitespace) yields zero matches in the full-suite log
    - Both Layer A and Layer B print their Ginkgo summary with 0 failed specs; the plain go-test contract tests in the kind package pass
    - Spec totals ≥ the pre-phase baseline (new specs added, none lost)
  </acceptance_criteria>
  <done>Phase 34 verification complete: INTEG-01..05 locked by a green full suite with the regression specs active.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- GREEN evidence logs: `/tmp/34-06-green-run.log`, `/tmp/34-06-full-suite.log` (transient; quoted in SUMMARY)
- Possible timing-expectation adjustments in kind Layer B specs (test-only; enumerated in SUMMARY)

## Phase-wide artifact index (all plans, for verify-work)

- API (both versions): `ConditionIntegrationIncomplete`, `ReasonIntegrationIncomplete`, `ReasonMergeConflict`, `WaveIntegrationStatus`, `PlanStatus.WaveIntegration` (34-02)
- Controller: `internal/controller/git_writer.go` (succeededTaskBranches / gitWriterInFlightCount / readJobPushEnvelope), git-writer Job labels, `errGitWriterBusy`, full-range wave loop, `maxWaveIntegrationAttempts`, `dispatchIfMissing` push observation, `Status.Git.LastPushedSHA` stamp, `resetBoundaryPushAnnotation` (34-02/04/05)
- tide-push: flock `tide-integrate.lock`, verify gate (`merge-base --is-ancestor`), exit codes 14/15, envelope reasons `integration-incomplete`/`merge-conflict`, envelope fields `missingBranches`/`missingTotal`/`conflictBranch`, wave-success envelope (34-03)
- pkg/git: `MergeConflictError`, merge-abort hygiene (34-03)
- CLI: `tide resume` boundary-push reset (34-05)
- Metrics: `tide_integration_outcomes_total{project, outcome}` (34-02)
- Tests: `test/integration/kind/integration_miss_test.go`, `internal/controller/plan_wave_integration_test.go`, extended envtest/unit suites (34-01/04/05)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test cluster → host | kind cluster + images on the constrained 7.65 GiB VM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-16 | Integrity | stale images masking the fix | medium | mitigate | test-int-kind-prep rebuild on fresh cluster; verify image digests changed and pods rotated (CLAUDE.md Observe First rule 4) |
| T-34-17 | Denial of service | concurrent heavy runs OOM the VM (exit 137) | medium | mitigate | One heavy run at a time; fresh cluster per run; never alongside an acceptance cluster (CLAUDE.md recipe) |
| T-34-SC | Tampering | package installs | low | accept | No packages installed in this plan |
</threat_model>

<verification>
- Focused run: both regression specs GREEN, assertions unweakened vs the RED commit
- Full suite: MAKE_EXIT=0 AND zero FAIL-line grep hits AND spec-count conservation
</verification>

<success_criteria>
- Phase success criteria 1–5 (ROADMAP) all observable: single-wave + final-wave integration, serialized idempotent merges, gated push with sticky miss condition, lastPushedSHA armed, kind repro locking the fix
- The minikube evidence PVC was never touched (repro dependence retired by the kind suite, per STATE.md blocker note)
</success_criteria>

<output>
Create `.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-06-SUMMARY.md` when done (include MAKE_EXIT evidence + spec-count table).
</output>
