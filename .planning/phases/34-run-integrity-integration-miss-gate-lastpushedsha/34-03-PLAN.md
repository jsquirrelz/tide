---
phase: 34-run-integrity-integration-miss-gate-lastpushedsha
plan: 03
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - pkg/git/integrate.go
  - pkg/git/integrate_test.go
  - cmd/tide-push/main.go
  - cmd/tide-push/main_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [INTEG-02, INTEG-03]
must_haves:
  truths:
    - "tide-push holds a kernel flock across integrate → verify → push (belt-and-braces behind the control-plane gate; no lockfile-existence protocol)"
    - "After merging and before pushing, tide-push verifies every expected branch with git merge-base --is-ancestor; on a miss it exits nonzero with a typed envelope naming the missing branches"
    - "A genuine merge conflict is classified distinctly from transient failure in the envelope reason, and the integration worktree is left clean (merge aborted) for the next Job"
    - "A wave-integration (integration-only) Job writes a success envelope, making wave outcomes observable"
  artifacts:
    - path: "pkg/git/integrate.go"
      provides: "MergeConflictError type + merge-abort hygiene in IntegrateTaskBranches"
      contains: "MergeConflictError"
    - path: "cmd/tide-push/main.go"
      provides: "flock, verify step, exitIntegrationMiss/exitMergeConflict codes, conflict classification, wave-success envelope"
      contains: "merge-base"
  key_links:
    - from: "cmd/tide-push/main.go"
      to: "pkg/git/integrate.go"
      via: "errors.As on MergeConflictError to classify the integrate failure"
      pattern: "MergeConflictError"
    - from: "cmd/tide-push/main.go"
      to: "internal/controller/git_writer.go"
      via: "envelope reason strings + missingBranches/conflictBranch JSON fields consumed by readJobPushEnvelope (cross-binary contract; JSON tags must match pushResultEnvelope)"
      pattern: "missingBranches"
---

<objective>
Make the tide-push data plane enforce run integrity: kernel flock serialization (INTEG-02 belt-and-braces), the D-05/D-06 in-Job verify gate (`git merge-base --is-ancestor` per expected branch, after integrate, before push — INTEG-03's predicate), D-09 conflict-vs-transient classification with Pitfall-1 merge-abort hygiene, and the Pitfall-3 wave-success envelope.

Purpose: with this plan alone, an incomplete run branch can no longer ship — every push-mode Job verifies from git state before pushing, at all four levels (D-05), regardless of what the controller believed.
Output: hardened tide-push binary + pkg/git, unit-tested through the in-process run(cfg) seam.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-CONTEXT.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-PATTERNS.md
</context>

## Assumptions (headless decisions, within CONTEXT discretion)

- **flock lockfile** (D-02 discretion): `<workspace>/repo.git/tide-integrate.lock` via `golang.org/x/sys/unix.Flock` (already in go.sum v0.44.0; promoted indirect→direct). Inside repo.git so no checkout ever cleans it; kernel releases on process exit — the lockfile itself is never deleted and its existence carries NO meaning (locked constraint: flock(2) only).
- **Branch-list handoff** (D-07 discretion): stays CSV `--integrate-task-branches` args (~45 B/branch; 500 tasks ≈ 23 KB — far from limits). The verify set IS the integrate set — no second flag.
- **Verify location**: against the bare repo `<workspace>/repo.git` (the integration worktree shares refs with it via `git worktree`; the bare repo is what the push reads — same repo the INTEG-05 assertion Job inspects).
- **New exit codes**: `exitIntegrationMiss = 14`, `exitMergeConflict = 15` (next free after the existing 0/1/2/10–13 map).
- **PVC envelope copy left as-is**: the controller consumes termination logs only (Pitfall 2); re-keying the PVC JSON path risks churning envelope-residue tooling — documented in a code comment instead.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pkg/git — typed conflict error + merge-abort hygiene (Pitfall 1, D-09 input)</name>
  <files>pkg/git/integrate.go, pkg/git/integrate_test.go</files>
  <read_first>
    - pkg/git/integrate.go (full file, esp. the merge loop at ~:94–107 and the "already checked out" tolerance at ~:74–81; do NOT touch the bot-identity env block at ~:85–92 — Phase 36 lands there)
    - pkg/git/integrate_test.go (scratch-repo test fixture style)
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md (Pattern 5: verified conflict output shape on git 2.54)
  </read_first>
  <behavior>
    - Test 1 (conflict classification): two branches touching the same line → IntegrateTaskBranches returns an error satisfying `errors.As(&MergeConflictError{})` whose TaskBranch/RunBranch fields name both branches; after return, the integration worktree has no in-progress merge (a follow-up `git -C dir merge --abort` reports nothing to abort / status is clean).
    - Test 2 (idempotent re-merge, INTEG-02): merging an already-merged branch a second time exits clean, creates no new commit ("Already up to date." semantics — verified empirically in RESEARCH).
    - Test 3 (self-healing start): a manufactured in-progress merge state in the integration worktree (leftover MERGE_HEAD) does not break a fresh IntegrateTaskBranches call — the defensive abort clears it.
    - Test 4 (transient stays plain): a merge failure that is NOT a content conflict (e.g. nonexistent branch) returns a plain wrapped error, not MergeConflictError.
  </behavior>
  <action>
    In `pkg/git/integrate.go`: (1) add exported `type MergeConflictError struct { TaskBranch, RunBranch, Output string }` with an `Error()` naming both branches (D-10's condition message source — integrate.go's error text already carries the merge pair; make it structured). (2) In the merge loop's failure path: detect content conflicts by matching the combined output for the substrings `"CONFLICT ("` or `"Automatic merge failed"` (conservative string match mirroring classifyPushError's style; shapes verified on git 2.54 per RESEARCH Pattern 5); ALWAYS run `git -C <integrationDir> merge --abort` (error ignored) before returning any merge failure, so a lingering in-progress merge never poisons the shared-PVC worktree for the next Job (Pitfall 1); return `&MergeConflictError{...}` for conflicts, the existing wrapped error otherwise. (3) At integration start, run a defensive `git -C <integrationDir> merge --abort` with tolerated failure (self-heal after a crashed prior Job). Keep the exec style (`exec.Command("git", ...).CombinedOutput()`) consistent with the file.
  </action>
  <verify>
    <automated>go test ./pkg/git/ -run 'Integrate|MergeConflict' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type MergeConflictError struct' pkg/git/integrate.go` = 1
    - `grep -c 'merge --abort\|"merge", "--abort"' pkg/git/integrate.go` ≥ 2 (failure-path abort + defensive start abort)
    - All four behavior tests pass; the bot-identity env block is unchanged (`git diff` on lines 85–92 region shows no identity changes)
    - `go test ./pkg/git/ -count=1` fully green (no regression in existing worktree/integrate tests)
  </acceptance_criteria>
  <done>Conflicts are structurally classifiable and the shared integration worktree is always left mergeable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tide-push — flock + verify gate + new exit codes (D-05/D-06, INTEG-03)</name>
  <files>cmd/tide-push/main.go, cmd/tide-push/main_test.go, go.mod, go.sum</files>
  <read_first>
    - cmd/tide-push/main.go (full flow: exit-code map comment at :36–46; integrate step + integration-only early-exit at ~:359–380; clean-tree handling ~:488–528; push + envelope at ~:560–572; writePushEnvelope ~:655–703; redactPAT ~:774)
    - cmd/tide-push/main_test.go (the in-process run(cfg) seam driving push mode against local fixture repos)
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md (Patterns 3–4: flock shape, --is-ancestor exit-code semantics incl. the empty-diff case)
  </read_first>
  <behavior>
    - Test 1 (miss): run(cfg) in push mode where one branch in --integrate-task-branches has a tip NOT reachable from the run branch after integrate is bypassed/undone → returns exitIntegrationMiss (14); envelope reason "integration-incomplete"; envelope missingBranches contains exactly the unmerged branch; nothing was pushed to the remote fixture.
    - Test 2 (complete set): all expected branches merged → verify passes silently, push proceeds, envelope reason empty, HeadSHA set (existing success behavior preserved).
    - Test 3 (empty-diff task): an expected branch whose tip is already an ancestor (no unique commits) passes verify with no special-casing (INTEG-03 clarification — verified git semantics).
    - Test 4 (truncation): >10 missing branches → envelope missingBranches has exactly 10 entries and missingTotal carries the full count (termination-log 4096-byte cap, D-12 bound).
    - Test 5 (flock): the lockfile is created at <workspace>/repo.git/tide-integrate.lock and run(cfg) completes normally while holding it.
  </behavior>
  <action>
    In `cmd/tide-push/main.go`: (1) extend the exit-code map (comment + constants): `exitIntegrationMiss = 14` (envelope.reason=integration-incomplete), `exitMergeConflict = 15` (envelope.reason=merge-conflict — wired in Task 3). (2) flock (D-02 belt-and-braces, locked constraint kernel-flock-only): at the top of the push-mode git-mutation sequence (before the integrate step), open/create `<workspace>/repo.git/tide-integrate.lock` (0o664) and take `unix.Flock(fd, unix.LOCK_EX)` (blocking); no unlock and no lockfile removal — the kernel releases on exit, and lockfile EXISTENCE must never be interpreted (comment this). Import `golang.org/x/sys/unix`; run `go mod tidy` (promotes x/sys to a direct require at the already-resolved v0.44.0 — no new module fetched). (3) Verify step (D-05/D-06): after the integrate block and BEFORE both the integration-only early exit and the commit/push path, for each branch in cfg.IntegrateTaskBranches run `git -C <workspace>/repo.git merge-base --is-ancestor <branch> <cfg.Branch>`; treat `exec.ExitError.ExitCode()==1` as a miss (collect), exit code 0 as pass, any other error as infrastructure failure (envelope reason "integration-failed", exitGenericFail — rides the existing transient-retry default). On misses: `writePushEnvelope` with empty HeadSHA, exitIntegrationMiss, reason `integration-incomplete`, MissingBranches = first 10 (sorted), MissingTotal = full count; one PAT-redacted stderr line; return exitIntegrationMiss. (4) Extend the `pushResult` struct + `writePushEnvelope` signature to carry `MissingBranches []string `json:"missingBranches,omitempty"``, `MissingTotal int `json:"missingTotal,omitempty"``, `ConflictBranch string `json:"conflictBranch,omitempty"`` — JSON tags MUST match the controller-side pushResultEnvelope fields added in plan 34-02; update all existing writePushEnvelope call sites with zero values. Route every new stderr/envelope string through redactPAT (RESEARCH security control).
  </action>
  <verify>
    <automated>go test ./cmd/tide-push/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'exitIntegrationMiss' cmd/tide-push/main.go` ≥ 2 (const + use); `grep -c 'unix.Flock' cmd/tide-push/main.go` = 1
    - `grep -c 'merge-base' cmd/tide-push/main.go` ≥ 1 (verify predicate present, after integrate and before push in the flow)
    - `grep -c 'missingBranches' cmd/tide-push/main.go` = 1 (JSON tag matches controller side exactly)
    - `grep -c 'golang.org/x/sys' go.mod` = 1 as a direct require, version v0.44.0 (no version change)
    - All five behavior tests pass; the full `go test ./cmd/tide-push/ -count=1` is green
  </acceptance_criteria>
  <done>No push can ship an incomplete run branch: the verify gate runs from git state inside the Job at every level (D-05), atomic with the push under one flock (D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tide-push — conflict classification + wave-success envelope (D-09, Pitfall 3)</name>
  <files>cmd/tide-push/main.go, cmd/tide-push/main_test.go</files>
  <read_first>
    - cmd/tide-push/main.go (integrate-failure path at ~:362 writing the undifferentiated "integration-failed" reason; classifyPushError at ~:705–734; integration-only early exit ~:374–379)
    - pkg/git/integrate.go as modified by Task 1 (MergeConflictError fields)
  </read_first>
  <behavior>
    - Test 1 (conflict envelope): run(cfg) where the integrate step hits a content conflict → returns exitMergeConflict (15); envelope reason "merge-conflict"; envelope conflictBranch names the conflicting task branch; the integration worktree is clean afterward (Task 1's abort).
    - Test 2 (transient unchanged): a non-conflict integrate failure still produces reason "integration-failed" with exitGenericFail (existing bounded-retry class).
    - Test 3 (wave-success envelope): an integration-only invocation (empty ArtifactPaths) that succeeds writes a termination-log envelope with ExitCode 0 and empty reason before exiting — today it writes none (Pitfall 3).
  </behavior>
  <action>
    In `cmd/tide-push/main.go`: (1) at the integrate-failure path (~:362), classify with `errors.As(err, &mergeConflict)` against `pkggit.MergeConflictError`: conflicts → `writePushEnvelope` with exitMergeConflict, reason `merge-conflict`, ConflictBranch = the error's TaskBranch (the run branch is already known to the controller from Status.Git — D-10/D-11 condition messages name both from these two sources); non-conflicts keep the existing `integration-failed` reason (transient default, D-04's retry class). (2) Wave-success envelope: at the integration-only early exit, write a success envelope (empty HeadSHA — the D-14 stamp reads boundary-push envelopes only, per Pitfall 2 the controller keys by job-name so there is no collision) before returning exit 0. (3) Update the exit-code map comment to document 14/15 and the wave-success envelope semantics.
  </action>
  <verify>
    <automated>go test ./cmd/tide-push/ -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"merge-conflict"' cmd/tide-push/main.go` ≥ 1 and `grep -c 'ConflictBranch' cmd/tide-push/main.go` ≥ 1
    - The integration-only success path contains a writePushEnvelope call (behavior Test 3 passes)
    - Exit-code map comment documents 14 and 15
    - `go test ./cmd/tide-push/ -count=1` and `go test ./pkg/git/ -count=1` both green; `go build ./...` exits 0
  </acceptance_criteria>
  <done>Wave and push Jobs emit uniformly classifiable envelopes: success / miss / conflict / transient — the controller arms in 34-04/34-05 consume exactly these.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `pkg/git.MergeConflictError{TaskBranch, RunBranch, Output string}` (exported type)
- Merge-abort hygiene (failure-path + defensive-start) in `pkg/git.IntegrateTaskBranches`
- tide-push exit codes: `exitIntegrationMiss = 14`, `exitMergeConflict = 15`
- tide-push envelope reasons: `integration-incomplete`, `merge-conflict`; envelope JSON fields `missingBranches`, `missingTotal`, `conflictBranch`
- flock at `<workspace>/repo.git/tide-integrate.lock` (`golang.org/x/sys/unix.Flock`, promoted to direct dependency)
- In-Job verify gate: `git merge-base --is-ancestor <branch> <runBranch>` per expected branch, in `<workspace>/repo.git`
- Wave-integration success envelope (integration-only invocations now observable)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job pod → git remote | PAT-authenticated push (existing D-B1: PAT is Job-pod-only via envFrom) |
| Job pod → shared PVC | integration worktree + lockfile + envelope residue |
| Job pod → controller | termination-log envelope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-06 | Integrity | boundary push shipping unmerged work | high | mitigate | D-05/D-06 verify gate: merge-base --is-ancestor per expected branch, recomputed from git inside the Job, before every push — the phase's raison d'être |
| T-34-07 | Information disclosure | new verify/conflict stderr + envelope text | medium | mitigate | All new output routed through redactPAT (main.go:774); envelope carries branch names only, never diff content (gitleaks remains the diff gate) |
| T-34-08 | Denial of service | blocking flock wedging a Job forever | low | accept | Control-plane single-flight gate (D-02) is the primary serializer, so contention is exceptional; Job activeDeadline/BackoffLimit bound the wait; kernel releases on any process exit (no stale-lock protocol possible by design) |
| T-34-SC | Tampering | package installs | low | accept | No new module fetched — x/sys v0.44.0 already in go.sum, promoted indirect→direct only |
</threat_model>

<verification>
- `go test ./pkg/git/ ./cmd/tide-push/ -count=1` fully green
- `go build ./...` green
- Envelope JSON tags byte-identical to plan 34-02's pushResultEnvelope fields (grep both sides)
</verification>

<success_criteria>
- A push-mode Job cannot push while any expected Succeeded branch is missing from the run branch
- Conflict vs transient vs miss are three distinct, machine-readable envelope outcomes
- Re-running any Job against the shared PVC is safe: idempotent re-merge + always-clean worktree + meaning-free lockfile
</success_criteria>

<output>
Create `.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-03-SUMMARY.md` when done.
</output>
