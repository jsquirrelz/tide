---
phase: 34-run-integrity-integration-miss-gate-lastpushedsha
plan: 04
type: execute
wave: 3
depends_on: [34-02, 34-03]
files_modified:
  - internal/controller/plan_controller.go
  - internal/controller/boundary_push.go
  - internal/controller/boundary_push_test.go
  - internal/controller/plan_wave_integration_test.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
autonomous: true
requirements: [INTEG-01, INTEG-02]
must_haves:
  truths:
    - "A single-wave plan dispatches a wave-integration Job for its only wave; every plan's final Kahn wave integrates (the last-wave skip is closed)"
    - "Every boundary-push trigger (all four levels) carries the cumulative Succeeded-branch set computed from a live List — the nil-branches state is gone"
    - "No new run-branch-writer Job is created while another is in flight (single-flight gate), without deadlocking on the Job a state machine itself observes"
    - "A wave-integration Job failure rides a bounded-retry state machine; a merge conflict skips retries and fails the Plan with a condition naming both branches"
    - "Plan=Succeeded now implies all waves integrated (Succeeded stamps only after the final-wave integration Job completes)"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "full-range wave-boundary loop + D-04 bounded retry + D-10 conflict handling"
      contains: "for k := 0; k < len(layers); k++"
    - path: "internal/controller/boundary_push.go"
      provides: "cumulative-set computation inside triggerBoundaryPush + D-02 gate + errGitWriterBusy"
      contains: "succeededTaskBranches"
  key_links:
    - from: "internal/controller/plan_controller.go"
      to: "internal/controller/git_writer.go"
      via: "gitWriterInFlightCount gate before wave-Job dispatch; readJobPushEnvelope for failure classification"
      pattern: "gitWriterInFlightCount|readJobPushEnvelope"
    - from: "internal/controller/boundary_push.go"
      to: "internal/controller/git_writer.go"
      via: "succeededTaskBranches computed inside the shared trigger (D-03)"
      pattern: "succeededTaskBranches"
---

<objective>
Close INTEG-01 (the `plan_controller.go:1193` last-wave skip) and land the control-plane half of INTEG-02: cumulative Succeeded-branch sets computed inside the shared triggers (D-03/D-07), the D-02 single-flight git-writer gate at wave and trigger dispatch sites, and the D-04 bounded-retry state machine for wave-integration Jobs with D-09/D-10 conflict classification.

Purpose: the structural fix — every wave including the final one integrates, whichever trigger wins the create race integrates identically, and only one writer touches the run branch at a time.
Output: PlanReconciler + boundary-push trigger changes, envtest-covered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-CONTEXT.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-PATTERNS.md
</context>

## Assumptions (headless decisions, within CONTEXT discretion)

- **RESEARCH Open Question 3 resolved as recommended:** wave-integration Jobs carry the CUMULATIVE Succeeded set, not just wave-k branches — one helper, one behavior, self-healing per D-01's defense-in-depth intent (re-merge is idempotent, verified).
- **Busy signaling:** `triggerBoundaryPush` returns a sentinel `errGitWriterBusy` when the D-02 gate finds another writer in flight; call sites (plan/phase/milestone) translate it into a short requeue (5s, matching existing intervals) instead of an error-log requeue. This keeps the wrapper signatures simple while honoring D-02's "if one is active, requeue".
- **Wave retry cap:** `maxWaveIntegrationAttempts = 5`, backoff via the existing `boundaryPushRequeue(attempts)` (same package) — mirrors #13b exactly rather than inventing a second curve.
- **Plan-level wrapper simplification:** `PlanReconciler.maybeTriggerBoundaryPush` drops its `taskItems` parameter — the cumulative set is computed inside the shared trigger (D-03), so per-caller collection is dead code (the only live call passes nil anyway, pre-Tasks, per RESEARCH).

<tasks>

<task type="auto">
  <name>Task 1: boundary_push.go — cumulative set inside the shared trigger + D-02 gate (D-03/D-07)</name>
  <files>internal/controller/boundary_push.go, internal/controller/boundary_push_test.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go</files>
  <read_first>
    - internal/controller/boundary_push.go (full: triggerBoundaryPush :58–134, triggerWaveIntegrationJob :146–193, four per-level entry points :200–223)
    - internal/controller/git_writer.go (from 34-02: succeededTaskBranches, gitWriterInFlightCount, label constants)
    - internal/controller/milestone_controller.go (:751, :773) and internal/controller/phase_controller.go (:670, :692) — the maybeTriggerBoundaryPush call sites and their error handling context
    - internal/controller/plan_controller.go (:770 — the plan-level call site passing nil, and the CR-03 comment at :757–769 explaining why)
  </read_first>
  <action>
    Implements D-03 + D-07 + D-02 at the trigger sites. (1) Inside `triggerBoundaryPush`, after the existing Get-then-Create idempotency check and BEFORE Create: compute `branches, err := succeededTaskBranches(ctx, c, project.Namespace, project.Name)` and set `PushOptions.IntegrateTaskBranches = branches` — for ALL four levels, replacing the caller-passed branches parameter entirely (drop the `branches []string` parameter from `triggerBoundaryPush`'s signature; whichever level's trigger wins the deterministic `tide-push-<project.UID>` create race now integrates identically, per D-03). (2) Add the D-02 gate in the same spot: `n, err := gitWriterInFlightCount(ctx, c, project.Namespace, project.Name, pushJobName)`; if n > 0, return the new package sentinel `errGitWriterBusy = errors.New("git-writer busy: another run-branch writer Job is in flight")` (declare it in boundary_push.go). Pitfall 7: the exclusion parameter is the deterministic push Job's own name, so the boundary-push retry loop never blocks on itself. (3) Simplify `PlanReconciler.maybeTriggerBoundaryPush`: drop the `taskItems` parameter and its dead branch-collection loop; update the single call site at plan_controller.go:770. (4) At all five maybeTriggerBoundaryPush call sites (milestone_controller.go:751/:773, phase_controller.go:670/:692, plan_controller.go:770): on `errors.Is(err, errGitWriterBusy)`, requeue after 5s (or return a requeue result up the enclosing function's contract) instead of propagating an error — a busy gate is normal serialization, not a failure. (5) Extend boundary_push_test.go: trigger with 3 Succeeded + 1 Failed Task CRs creates a Job whose `--integrate-task-branches` arg contains exactly the 3 sorted Succeeded branches; trigger with an in-flight labeled git-writer Job (different name) returns errGitWriterBusy and creates nothing; trigger with only its OWN deterministic Job pre-existing returns nil (idempotent path, unchanged).
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'BoundaryPush|TriggerBoundary|GitWriterBusy' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'succeededTaskBranches' internal/controller/boundary_push.go` ≥ 1 (set computed inside the shared trigger)
    - `grep -c 'errGitWriterBusy' internal/controller/boundary_push.go` ≥ 2 (declared + returned)
    - `grep -c 'taskItems' internal/controller/boundary_push.go` = 0 — the per-caller collection is gone <!-- planner-discipline-allow: taskItems -->
    - All five call sites handle errGitWriterBusy as a requeue, verified by grep for errGitWriterBusy in milestone_controller.go, phase_controller.go, plan_controller.go (≥ 1 each)
    - Targeted tests green; `go build ./internal/...` exits 0
  </acceptance_criteria>
  <done>Every trigger carries the live cumulative set; a second writer cannot be created while one is in flight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: plan_controller.go — full-range wave loop + bounded retry + conflict→Plan Failed (INTEG-01, D-04, D-10)</name>
  <files>internal/controller/plan_controller.go, internal/controller/plan_wave_integration_test.go</files>
  <read_first>
    - internal/controller/plan_controller.go (wave-boundary block :981–1221: reconcileWaveBoundary early-outs :997–1009 incl. the no-git short-circuit at :1007, RESPONSIBILITY A failure arm :1031–1037, wave-Job dispatch :1072, the loop at :1193, BoundaryDetected→patchPlanSucceeded :1208–1214)
    - internal/controller/project_controller.go (#13b template: cap guard :700–718, attempts-increment :866–873, Background-propagation delete :895–904, boundaryPushRequeue)
    - api/v1alpha2/plan_types.go (WaveIntegrationStatus from 34-02)
    - internal/controller/git_writer.go (readJobPushEnvelope, gitWriterInFlightCount)
    - internal/metrics/registry.go (IntegrationOutcomesTotal from 34-02)
  </read_first>
  <behavior>
    - Test 1 (INTEG-01 single-wave): a git-configured Plan with one wave of Succeeded tasks dispatches wave-integration Job `tide-push-wave-<plan.UID>-0` — today it dispatches nothing.
    - Test 2 (INTEG-01 final wave): a two-wave plan dispatches Jobs for k=0 AND k=1.
    - Test 3 (Pitfall 6 ordering): the Plan does NOT stamp Succeeded until the final-wave integration Job is observed complete; stub projects (no git) are unaffected via the :1007 short-circuit.
    - Test 4 (D-04 transient retry): a failed wave Job with envelope reason "integration-failed" increments Plan.Status.WaveIntegration.Attempts, deletes the Job with Background propagation, and requeues with capped backoff; after 5 attempts the Plan goes Failed with ReasonWaveIntegrationFailed.
    - Test 5 (D-09/D-10 conflict): a failed wave Job with envelope reason "merge-conflict" and conflictBranch set skips retries entirely and fails the Plan with ReasonMergeConflict, the condition message naming the conflicting task branch and the run branch.
    - Test 6 (D-02 gate): with another in-flight git-writer Job present, RESPONSIBILITY B requeues instead of creating the wave Job.
  </behavior>
  <action>
    In `plan_controller.go`: (1) INTEG-01 — change the :1193 loop bound to run k over the FULL layers range (`k < len(layers)`), updating the loop comment: the final boundary gates Plan completion rather than wave k+1 dispatch (`layers[k]` stays valid at k = len(layers)-1). (2) RESPONSIBILITY B — before triggerWaveIntegrationJob: call `gitWriterInFlightCount(ctx, r.Client, plan.Namespace, project.Name, integJobName)` (self-exclusion on its own wave-Job name, Pitfall 7); if > 0 requeue after 5s. Replace the wave-k-only branch collection feeding the dispatch with the cumulative `succeededTaskBranches(...)` result (D-01 cumulative everywhere, Assumption/OQ3). (3) RESPONSIBILITY A failure arm (:1031–1037) — replace the immediate patchPlanFailed with classification: `env, ok := readJobPushEnvelope(ctx, r.Client, plan.Namespace, integJobName)`; if ok and env.Reason == "merge-conflict" → patchPlanFailed with ReasonMergeConflict and a message naming env.ConflictBranch and the project's run branch (D-10 — conflicting parallel tasks were not independent; recovery is replan + `tide resume --retry-failed`), increment IntegrationOutcomesTotal{outcome="conflict"}; otherwise bounded retry mirroring #13b: manage `plan.Status.WaveIntegration` (reset Attempts when .Wave differs from the current waveNum; increment Attempts + LastAttemptTime + LastError via MergeFrom status patch), declare `maxWaveIntegrationAttempts = 5`, delete the failed Job with Background propagation (write a small local delete helper in plan_controller.go — do NOT call ProjectReconciler methods), requeue via boundaryPushRequeue(attempts), increment IntegrationOutcomesTotal{outcome="transient"}; at cap → patchPlanFailed with ReasonWaveIntegrationFailed ("after N attempts"). (4) RESPONSIBILITY A success arm — increment IntegrationOutcomesTotal{outcome="success"} where IntegratedThroughWave is stamped. (5) New spec file `plan_wave_integration_test.go` implementing <behavior> (envtest style of the existing wave-boundary specs; envelope pods constructed with job-name label + Terminated state carrying JSON, per the 34-02 test precedent).
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'WaveIntegration|WaveBoundary' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'for k := 0; k < len\(layers\); k\+\+' internal/controller/plan_controller.go` = 1 (full-range loop present)
    - `grep -c 'maxWaveIntegrationAttempts = 5' internal/controller/plan_controller.go` = 1
    - `grep -c 'ReasonMergeConflict' internal/controller/plan_controller.go` ≥ 1 and the conflict path contains NO retry (conflict test proves single-shot)
    - `grep -c 'DeletePropagationBackground' internal/controller/plan_controller.go` ≥ 1 (Foreground is the verified envtest footgun)
    - All six behavior tests green; `go test ./internal/controller/ -count=1` green
  </acceptance_criteria>
  <done>The two mechanical bug mechanisms (last-wave skip, nil-branch triggers) are structurally closed with retry/conflict semantics per D-04/D-09/D-10.</done>
</task>

<task type="auto">
  <name>Task 3: Layer A sweep — Pitfall 6 timing audit + full envtest pass</name>
  <files>internal/controller/plan_wave_integration_test.go, internal/controller/boundary_push_test.go</files>
  <read_first>
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md (Pitfall 6: extending the wave loop delays Plan=Succeeded for git-configured fixtures)
    - Output of: grep -rln 'withGit' internal/controller/*_test.go test/integration/envtest/*_test.go (the audit surface)
  </read_first>
  <action>
    Audit and stabilize the unit + Layer A tiers against the new Plan=Succeeded timing (Pitfall 6): run `go test ./internal/controller/ -count=1` and `make test-int-fast`; for any failing spec that uses a git-configured fixture and asserted quick Plan=Succeeded, update it to either simulate the final-wave integration Job's completion (the existing wave-boundary spec pattern) or assert the new intermediate state — do NOT weaken assertions to Eventually-with-huge-timeout without simulating the Job, and do NOT delete specs (spec-count conservation). Stub/no-git fixtures must need no changes (the :1007 short-circuit) — if one does, that is a regression in the loop change; fix the code, not the test. Record in the SUMMARY which specs were adjusted and why.
  </action>
  <verify>
    <automated>make test-int-fast 2>&1 | tail -5 && go test ./internal/controller/ -count=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/ -count=1` exits 0
    - `make test-int-fast` exits 0 (read the actual make exit code, not the Ginkgo summary — CLAUDE.md discipline)
    - No spec deleted or skipped; adjustments listed in SUMMARY with rationale
  </acceptance_criteria>
  <done>Unit + Layer A tiers fully green under the new integration semantics; kind Layer B fallout is deferred to plan 34-06 by design.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- Full-range wave-boundary loop in `PlanReconciler` (final Kahn wave integrates; `Plan=Succeeded` implies all waves integrated)
- `triggerBoundaryPush` computes the cumulative Succeeded set internally; signature loses the caller-supplied branches parameter; `PlanReconciler.maybeTriggerBoundaryPush` loses `taskItems`
- Sentinel `errGitWriterBusy` (internal/controller/boundary_push.go) + requeue handling at 5 call sites
- `maxWaveIntegrationAttempts = 5` + wave-Job bounded-retry state machine writing `Plan.Status.WaveIntegration`
- D-10 conflict semantics: Plan Failed with `ReasonMergeConflict` naming both branches
- `IntegrationOutcomesTotal` increments: success / transient / conflict at wave-Job terminal arms
- New spec file `internal/controller/plan_wave_integration_test.go`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → Job args | branch list crosses control→data plane as CSV args |
| Job pod → controller | envelope reason/conflictBranch drives Plan state transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-09 | Integrity | final-wave/single-wave work never merged | high | mitigate | Full-range loop + cumulative set inside every trigger — the INTEG-01 structural fix, locked by envtest specs and the 34-01 kind repro |
| T-34-10 | Tampering | forged envelope flipping a Plan to Failed | medium | mitigate | readJobPushEnvelope reads only pods of the deterministic controller-created Job via the K8s API (`job-name` label); PVC JSON never consumed (Pitfall 2) |
| T-34-11 | Denial of service | gate deadlock (writer waits on itself) | medium | mitigate | Pitfall 7 self-exclusion parameter on every gitWriterInFlightCount call; 5s requeue keeps progress; Test 6 covers the gate path |
| T-34-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- Targeted wave-integration + boundary-push test runs green
- `make test-int-fast` exit 0 (MAKE_EXIT discipline)
- Grep gates: full-range loop present; Background deletes; no taskItems plumbing remaining in boundary_push.go
</verification>

<success_criteria>
- Both INTEG-01 mechanisms closed at the control plane; wave parallelism untouched (only run-branch WRITERS serialize — spec §failure-semantics preserved: task-level semantics unchanged by D-04)
- Deterministic Job names remain the base serialization layer; the D-02 gate LAYERS on it (D-B5 kept)
</success_criteria>

<output>
Create `.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-04-SUMMARY.md` when done.
</output>
