---
phase: 34-run-integrity-integration-miss-gate-lastpushedsha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/kind/integration_miss_test.go
autonomous: true
requirements: [INTEG-05]
must_haves:
  truths:
    - "The Go toolchain builds this repo on the chosen execution surface (go build ./... exits 0)"
    - "A kind-suite spec reproduces the single-wave degenerate integration miss and fails RED against pre-fix code"
    - "A kind-suite spec reproduces the observed 2-parallel-task final-wave miss (missing ancestry + empty lastPushedSHA despite BoundaryPushed=True) and fails RED against pre-fix code"
  artifacts:
    - path: "test/integration/kind/integration_miss_test.go"
      provides: "INTEG-05 regression specs (both repro mechanisms, per CONTEXT specifics)"
      min_lines: 150
  key_links:
    - from: "test/integration/kind/integration_miss_test.go"
      to: "test/integration/kind/medium_http_test.go"
      via: "reuses the hermetic git-http-server fixture stack (same package)"
      pattern: "git-http-server|mediumHTTP"
    - from: "test/integration/kind/integration_miss_test.go"
      to: "pkg/git/worktree.go"
      via: "task branch names resolved via pkggit.TaskBranchName(task.UID)"
      pattern: "TaskBranchName"
---

<objective>
Establish the execution surface (Go toolchain — the only missing host tool) and land the INTEG-05 kind-suite regression test RED against pre-fix code. This is the phase's Wave 0: the two repro specs define exactly what "fixed" means, and every later plan's fix is locked by flipping these specs GREEN in plan 34-06.

Purpose: RED-first discipline (RESEARCH Pitfall 9) — the kind suite is expensive, so the RED state must be demonstrated before any fix merges, or it never will be.
Output: verified toolchain + `test/integration/kind/integration_miss_test.go` with two failing specs and recorded RED evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-CONTEXT.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md
@.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-PATTERNS.md
@.planning/todos/pending/2026-07-03-wave-parallel-integration-miss.md
</context>

## Assumptions (headless decisions, within CONTEXT discretion)

- **Execution surface:** `brew install go` on the host (kind v0.32.0 and kubectl v1.36.1 are already present at `/opt/homebrew/bin`; only `go` is missing per RESEARCH Environment Availability). Devcontainer (`.devcontainer/devcontainer.json`) is the fallback if brew fails.
- **RED specs are committed active** (not Ginkgo-Pending): the kind Layer B suite only runs at the phase gate (plan 34-06), and intermediate plans verify with unit/envtest tiers only, so a temporarily-RED kind spec breaks nothing mid-phase.

<tasks>

<task type="auto">
  <name>Task 1: Toolchain gate — establish and verify the execution surface</name>
  <files>(none — host toolchain only; no repo files modified)</files>
  <read_first>
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-RESEARCH.md (sections "Environment Availability" and "Planner directive — Task 0 toolchain gate")
    - Makefile (targets `test`, `test-int-fast`, `test-int`, `test-int-kind-prep` — lines 85–160)
  </read_first>
  <action>
    Establish the Go toolchain per the RESEARCH Task-0 directive. Steps: (1) `command -v go` — if already present, verify only. (2) If absent, `brew install go` (go.mod requires go 1.26.0; Homebrew's current go satisfies it). (3) Smoke-verify the full surface: `go version`, `go build ./...`, `kind version` (v0.32.0 installed vs repo-pinned v0.31 — acceptable because Layer B node images are pinned by @sha256 per STACK.md rules; note this in the SUMMARY), `kubectl version --client`, `docker info` (Docker 29.5.3 expected up). (4) Do NOT delete or touch the running `minikube` container — it holds the perishable integration-miss repro evidence on the `tide-projects` PVC (STATE.md blocker). If brew fails, fall back to the devcontainer (`.devcontainer/devcontainer.json`: golang image + docker-in-docker) and record which surface was chosen in the SUMMARY.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/ ./cmd/tide-push/ ./pkg/git/ && kind version && kubectl version --client</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 from the repo root on the chosen surface
    - `go version` reports go1.26.x or newer
    - `kind version` reports v0.32.0 (or the repo-pinned v0.31); `kubectl version --client` succeeds
    - `docker ps` still lists the `minikube` container (evidence untouched)
  </acceptance_criteria>
  <done>Toolchain verified end-to-end; SUMMARY records the chosen surface and the kind v0.32-vs-v0.31 note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the single-wave degenerate repro spec + assertion harness</name>
  <files>test/integration/kind/integration_miss_test.go</files>
  <read_first>
    - test/integration/kind/medium_http_test.go (hermetic git-http-server fixture stack: Deployment + Service + demo-remote-init Job, `mediumHTTPTargetRepo` in-cluster URL, anonymous push)
    - test/integration/kind/fixtures_test.go (`newStubProject`/`withGit`/`newStubPlan`/`newStubTask`/`withTaskDependsOn`, `labelProject`)
    - test/integration/kind/wave_test.go (parents+tasks direct-apply pattern for multi-wave plans)
    - test/integration/kind/chaos_resume_test.go (lines ~360–430: inline Job mounting the `tide-projects` PVC at subPath `<project.UID>/workspace`, result via exit code)
    - test/integration/kind/suite_test.go (suite entry, image-loading helpers — `loadRequiredImage` pattern)
    - pkg/git/worktree.go (`TaskBranchName` — branch = `tide/wt-<task.UID>`)
  </read_first>
  <behavior>
    - Spec 1 ("single-wave degenerate"): a git-configured Project with one Plan containing 2 stub Tasks with NO dependsOn (one Kahn wave) is driven to Complete via the stub subagent; the spec then asserts BOTH task branches are ancestors of the run branch in the project's bare repo. Pre-fix this FAILS: the `k < len(layers)-1` loop skips the only wave and every live boundary-push trigger passes nil branches, so nothing ever merges either branch (RESEARCH Summary).
    - Assertion vehicle: an inline "ancestry-assert" Job mounting the `tide-projects` PVC at subPath `<project.UID>/workspace`, image = the tide-push image (git-capable, already built/loaded by `make test-int-kind-prep`), command `git -C /workspace/repo.git merge-base --is-ancestor <task-branch> <run-branch>` per branch; Job success = all ancestors, failure = miss.
  </behavior>
  <action>
    Create `test/integration/kind/integration_miss_test.go` (package `kind`, same package as the analogs so all fixture helpers are directly reusable). Build: (a) a dedicated namespace (e.g. `integration-miss-test`) with the git-http-server fixture stack reused from medium_http_test.go helpers; (b) a helper `assertBranchesAreAncestors(projectUID string, runBranch string, taskBranches []string)` that creates the PVC-inspection Job described in <behavior> (adapt chaos_resume_test.go's writer-Job shape: RestartPolicy Never, ServiceAccountName tide-subagent, FSGroup, PVC volume `tide-projects`, subPath `<project.UID>/workspace`; swap busybox for the tide-push image so git is available) and Gomega-Eventually's on Job completion; (c) Spec 1 per <behavior>, Ginkgo `It` description containing the literal substring "integration miss" so a `-ginkgo.focus='integration miss'` run selects exactly these specs. Resolve the run branch from `project.Status.Git.Branch` (or the fixture's known branch) and task branches via `pkggit.TaskBranchName(string(task.UID))`. Follow the Ginkgo label conventions used by sibling kind specs. Do NOT mark specs Pending — see plan Assumptions.
  </action>
  <verify>
    <automated>go vet ./test/integration/kind/</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/kind/integration_miss_test.go` exists, compiles under `go vet ./test/integration/kind/`
    - Spec 1's It-description contains the substring "integration miss"
    - The assertion Job helper mounts PVC claim `tide-projects` with subPath `<project.UID>/workspace` and execs `merge-base --is-ancestor` (grep: `grep -c 'merge-base' test/integration/kind/integration_miss_test.go` ≥ 1)
    - No production file touched (git status shows only the new test file)
  </acceptance_criteria>
  <done>Spec 1 + harness authored and compiling; RED run deferred to Task 3 (one cluster spin for both specs).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the 2-parallel-task final-wave spec; run both specs RED</name>
  <files>test/integration/kind/integration_miss_test.go</files>
  <read_first>
    - .planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-CONTEXT.md (&lt;specifics&gt; — the observed-run acceptance narrative; the two distinct bug mechanisms)
    - test/integration/kind/push_lease_test.go (BoundaryPushed condition assertion patterns)
    - CLAUDE.md (constrained-VM full-suite recipe: fresh kind cluster per heavy run, one heavy run at a time)
  </read_first>
  <behavior>
    - Spec 2 ("2-parallel-task final wave" — the observed shape): one Plan with task A (no deps, wave 0) and tasks B + C (both `dependsOn: A` — final wave); driven to Complete. Asserts, in this order: (1) all THREE task branches are ancestors of the run branch (pre-fix: only A integrates via the wave-0 boundary Job; B and C never merge); (2) `project.Status.Git.LastPushedSHA` is non-empty (pre-fix: assigned nowhere — always empty); (3) `BoundaryPushed=True` (passes pre-fix — included so the spec reproduces the full observed contradiction: Complete + BoundaryPushed=True + missing deliverable + empty lastPushedSHA).
    - RED expectation: both specs fail on the ancestry/lastPushedSHA assertions — NOT on harness errors (image pull, namespace setup, git-server readiness).
  </behavior>
  <action>
    Add Spec 2 to `integration_miss_test.go` per <behavior> (It-description also containing "integration miss"). Then run RED: (1) fresh cluster per the constrained-VM recipe — `kind delete cluster --name tide-test || true`, recreate from `test/integration/kind/cluster.yaml`, pre-warm (`kind load docker-image busybox:1.36 --name tide-test` and wait for the local-path provisioner Ready); (2) `make test-int-kind-prep`; (3) focused run: `go test ./test/integration/kind/... -v -timeout=40m -ginkgo.v -ginkgo.focus='integration miss' 2>&1 | tee /tmp/34-01-red-run.log`. Confirm both specs FAIL and each failure message is the ancestry assertion or the lastPushedSHA assertion (grep the log). Commit the test with a message following the TDD RED convention, e.g. `test(34-01): add failing kind repro for wave-parallel integration miss`. Record the exact failing assertion lines in the SUMMARY as the RED evidence (INTEG-05's RED half). Do not run any other heavy suite concurrently (7.65 GiB VM).
  </action>
  <verify>
    <automated>grep -E 'FAIL|• \[FAILED\]' /tmp/34-01-red-run.log | head -20 && grep -cE 'is-ancestor|LastPushedSHA|lastPushedSHA|ancestor' /tmp/34-01-red-run.log</automated>
  </verify>
  <acceptance_criteria>
    - Focused run log `/tmp/34-01-red-run.log` shows BOTH "integration miss" specs FAILED
    - Failure output cites the ancestry assertion (missing task branch) and/or the empty-lastPushedSHA assertion — not an image-pull/fixture error
    - `git log --oneline -1 -- test/integration/kind/integration_miss_test.go` shows the RED test commit
    - No fix code was written (git diff on internal/, cmd/, pkg/ is empty)
  </acceptance_criteria>
  <done>Both INTEG-05 repro specs are committed and demonstrably RED against pre-fix code, with evidence in the run log and SUMMARY.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `test/integration/kind/integration_miss_test.go` — new Layer B spec file; two Ginkgo specs whose It-descriptions contain "integration miss"
- Test helper `assertBranchesAreAncestors(projectUID, runBranch string, taskBranches []string)` (package `kind`)
- RED evidence log `/tmp/34-01-red-run.log` (transient, quoted in SUMMARY)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test pod → tide-projects PVC | assertion Job reads the project workspace (bare repo) |
| test → in-cluster git remote | anonymous-push hermetic git-http-server (test-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Elevation of privilege | ancestry-assert Job RBAC | low | mitigate | Job reuses the existing `tide-subagent` ServiceAccount (chaos_resume precedent); no new Role/RoleBinding added |
| T-34-02 | Information disclosure | test logs | low | accept | Hermetic fixture repo contains only canned stub content; no PAT is used (anonymous push, test-only) |
| T-34-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this plan (RESEARCH Package Legitimacy Audit: none); `brew install go` is a host toolchain install from Homebrew core, not a project dependency |
</threat_model>

<verification>
- `go build ./...` green on the chosen surface (Task 1)
- `go vet ./test/integration/kind/` green (Tasks 2–3)
- Focused kind run demonstrates both specs RED for the right reason (Task 3 log evidence)
</verification>

<success_criteria>
- Toolchain gate passed and recorded; every later plan can run `go test`/`make` targets
- INTEG-05 repro exists covering BOTH bug mechanisms (deterministic last-wave skip AND nil-branches trigger state) as separate specs
- RED demonstrated and committed before any fix code exists
</success_criteria>

<output>
Create `.planning/phases/34-run-integrity-integration-miss-gate-lastpushedsha/34-01-SUMMARY.md` when done (include: chosen execution surface, kind version note, RED evidence excerpt).
</output>
