---
phase: 16-telemetry-completion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/web/package.json
  - dashboard/web/package-lock.json
  - dashboard/web/src/components/TelemetryView.tsx
  - dashboard/web/src/components/__tests__/TelemetryView.test.tsx
autonomous: true
requirements: [TELEM-04, TELEM-02]
must_haves:
  truths:
    - "All four panels query only metric names registered in internal/metrics/registry.go — tide_tasks_dispatched_total and tide_tokens_used_total{model} are gone"
    - "A panel with data renders a real SVG time-series chart (recharts), not a text list of last values"
    - "TelemetryView scoped to the picker-selected project filters every query with {project=\"<name>\"}; the All projects toggle switches to by (project) aggregates with a per-project budget card grid"
    - "Range selector offers 24h/7d/30d; panels re-fetch every 60s while the document is visible and immediately on range/scope change"
    - "Both degradation shapes (200 unavailable sentinel, 502 error) render TelemetryUnavailableNotice in every panel — covered by Vitest"
  artifacts:
    - path: "dashboard/web/src/components/TelemetryView.tsx"
      provides: "Reworked TelemetryView: D-06 queries, recharts panels, scope/range toolbar, polling, budget grid"
      contains: "recharts"
    - path: "dashboard/web/src/components/__tests__/TelemetryView.test.tsx"
      provides: "Vitest coverage: both degradation shapes, range/scope behavior, budget grid, chart render"
      min_lines: 100
    - path: "dashboard/web/package.json"
      provides: "recharts@3.8.1 dependency (exact pin)"
      contains: "recharts"
  key_links:
    - from: "dashboard/web/src/components/TelemetryView.tsx"
      to: "/api/v1/query_range"
      via: "fetchQueryRange (existing, preserved)"
      pattern: "api/v1/query_range"
    - from: "TelemetryView panels"
      to: "locked metric names"
      via: "D-06 query builders"
      pattern: "tide_waves_dispatched_total"
    - from: "TelemetryView budget grid"
      to: "Project.Status.Budget"
      via: "projects prop (budget already in the projects list API payload — verified, no new fetch)"
      pattern: "budget"
---

<objective>
Close TELEM-04 and the TelemetryView half of TELEM-02: replace the two dead PromQL names with the D-06 locked shapes, replace the text-only Sparkline with recharts time-series charts, add the D-02/D-04 scope toggle, D-07 range selector + 60s visibility-aware polling, and the D-03 per-project budget card grid — all per the approved 16-UI-SPEC contract. Ship the locked Vitest degradation coverage.

Purpose: the phase is named "completion" — the Telemetry tab must look finished (real charts), and its queries must return data once 16-02's metrics emit.
Output: reworked TelemetryView with new props contract (consumed by plan 16-05), recharts@3.8.1 installed, comprehensive Vitest suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-telemetry-completion/16-CONTEXT.md
@.planning/phases/16-telemetry-completion/16-UI-SPEC.md
@.planning/phases/16-telemetry-completion/16-PATTERNS.md

Planner-resolved facts:
- **Budget data source (RESEARCH Open Question 1 — RESOLVED):** the projects list API already returns `budget` per project (`cmd/dashboard/api/projects.go` budgetSummary `{capCents, currentSpend, withinBudget}`; frontend `Project` type in `dashboard/web/src/lib/api.ts` has `budget: BudgetSummary`). No new backend fetch — TelemetryView receives the projects array as a prop.
- **New props contract (consumed by plan 16-05):** `TelemetryViewProps` becomes `{ projects: Project[]; selectedProject: string | null }` (replacing `projectName/namespace/budget` — `namespace` was never used). Scope mode derives per D-04: `selectedProject != null` → project scope default with toggle; `null` → all-projects, project segment not rendered.
- **D-06 "+" clarification:** MILESTONE.md's "tide_waves_dispatched_total + tide_tasks_completed_total" enumerates the metrics; UI-SPEC C4 is authoritative that Dispatch Counts renders TWO distinct series with legend labels — not a PromQL arithmetic sum. PATTERNS.md's single-query `+` sketch is superseded by the per-series query table in Task 1.
- **Dispatch metric emission caveat (pre-existing, out of scope):** `tide_waves_dispatched_total` / `tide_tasks_completed_total` / `tide_tasks_failed_total` are registered but not yet emitted anywhere — those panels will legitimately show "No data in range" until emission lands in a later phase. TELEM-04's acceptance is query-name correctness against registry.go (CONTEXT Specifics), not data presence.
- Test convention: tests live in `dashboard/web/src/components/__tests__/` (not adjacent). `npm test` runs `vitest run` (no watch mode). ResizeObserver polyfill already in `src/__tests__/setup.ts`.
</context>

<interfaces>
New props contract this plan establishes (plan 16-05 builds against it):

From dashboard/web/src/components/TelemetryView.tsx (after this plan):
```typescript
export type TelemetryViewProps = {
  /** Full projects list (includes per-project budget) — from App.tsx's existing fetch. */
  projects: Project[];
  /** ProjectPicker selection; null → all-projects mode (D-04). */
  selectedProject: string | null;
};
export default function TelemetryView(props: TelemetryViewProps): JSX.Element;
```

From dashboard/web/src/lib/api.ts (existing, unchanged):
```typescript
export type BudgetSummary = { capCents: number; currentSpend: number; withinBudget: boolean };
export type Project = { name: string; /* ... */ budget: BudgetSummary };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts, replace dead queries with D-06 per-series builders, replace Sparkline with recharts charts</name>
  <files>dashboard/web/package.json, dashboard/web/package-lock.json, dashboard/web/src/components/TelemetryView.tsx</files>
  <read_first>
    - dashboard/web/src/components/TelemetryView.tsx (entire file: PANELS at :73-95 with the two dead names, PanelState/PanelDef types, fetchQueryRange at :108, Sparkline at :167-205, ChartPanel, BudgetCard, TelemetryView body — the PanelState machine and fetchQueryRange survive; PANELS, Sparkline, and the single-fetch effect are reworked)
    - .planning/phases/16-telemetry-completion/16-UI-SPEC.md (§C4 chart construction rules — exact colors, grid, axes, tooltip, legend, stacking, empty state; §Color chart series palette table)
    - .planning/phases/16-telemetry-completion/16-PATTERNS.md (§TelemetryView — matrixToPoints transform shape; NOTE: its single-query "+" PANELS sketch is superseded by this task's per-series table)
    - dashboard/web/src/index.css (confirm the --color-status-* and --font-mono token names used for series colors)
  </read_first>
  <action>
    1. Install the audited dependency: `cd dashboard/web && npm install recharts@3.8.1 --save-exact` (Package Legitimacy Audit: [OK], approved — 16-RESEARCH.md).

    2. Replace the PANELS const with per-series query builders. Shape: each panel def gets `buildSeries(scope, window): Array<{ key?: string; query: string }>` where `scope` is `{ kind: "project"; name: string } | { kind: "all" }` and `window` is the range-derived PromQL window string. When `key` is set, results take that legend label; when omitted (all-projects `by (project)` queries), the series key is `metric.project` from the response. Exact queries (P = selected project name, W = window):

       Selected-project mode:
       - cost-over-time: key `cost` → `sum(increase(tide_cost_cents_total{project="P"}[W]))`
       - dispatch-counts: key `waves dispatched` → `sum(increase(tide_waves_dispatched_total{project="P"}[W]))`; key `tasks completed` → `sum(increase(tide_tasks_completed_total{project="P"}[W]))`
       - failure-rate: key `failure rate` → `sum(rate(tide_tasks_failed_total{project="P"}[W])) / (sum(rate(tide_tasks_failed_total{project="P"}[W])) + sum(rate(tide_tasks_completed_total{project="P"}[W])))`
       - token-breakdown: keys `input`/`output`/`cache read`/`cache creation` → `sum(increase(tide_tokens_input_total{project="P"}[W]))` and likewise for `tide_tokens_output_total`, `tide_tokens_cache_read_total`, `tide_tokens_cache_creation_total`

       All-projects mode (keys from `metric.project`):
       - cost-over-time: `sum(increase(tide_cost_cents_total[W])) by (project)`
       - dispatch-counts: `sum(increase(tide_waves_dispatched_total[W])) by (project) + sum(increase(tide_tasks_completed_total[W])) by (project)` (combined per-project dispatch activity — planner-picked D-06-conformant aggregate per UI-SPEC C4)
       - failure-rate: `sum(rate(tide_tasks_failed_total[W])) by (project) / (sum(rate(tide_tasks_failed_total[W])) by (project) + sum(rate(tide_tasks_completed_total[W])) by (project))`
       - token-breakdown: the same four token-type queries WITHOUT the project filter (token types stay the stack dimension cluster-wide — UI-SPEC C4: do NOT stack by project)

       The dead names `tide_tasks_dispatched_total` and `tide_tokens_used_total` must not appear anywhere in the file.

    3. Per-panel fetch: issue one `fetchQueryRange` per series query (the existing function is unchanged); merge results into a single PanelState — any `unavailable`/`unreachable` result wins for the whole panel; otherwise `data` with all series, tagging each matrix series with its legend key.

    4. Add `matrixToPoints(series)` per PATTERNS Pattern 7: merge into `[{ time, <seriesKey>: number }]` sorted by time, keyed on step timestamps.

    5. Replace `Sparkline` with a `TimeSeriesChart` component per UI-SPEC C4 (delete Sparkline entirely):
       - imports from `recharts`: AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer
       - `<ResponsiveContainer width="100%" height={180}>` (fixed height — Pitfall 5), `<CartesianGrid strokeDasharray="3 3" stroke="var(--color-border-subtle)" />`
       - XAxis: `dataKey="time" type="number" scale="time" domain={["dataMin","dataMax"]}`, tick formatter HH:MM for 24h, abbreviated month-day for 7d/30d; ticks 12px `var(--font-mono)` muted
       - series palette via CSS tokens in order: `var(--color-status-running)`, `var(--color-status-success)`, `var(--color-status-warning)`, `var(--color-status-blocked)`, `var(--color-status-pending)`, cycling; Failure Rate single-series mode uses `var(--color-status-error)` exclusively (multi-project mode uses the normal palette — all-red is forbidden)
       - Area: `strokeWidth={2}`, `fillOpacity={0.2}` (0.35 for the stacked Token Breakdown), `isAnimationActive={false}`; Token Breakdown areas share a `stackId`
       - Tooltip styled: background `var(--color-surface-overlay)`, 1px `var(--color-border-subtle)` border, radius 4px, 12px mono; Y-axis + tooltip formatters per panel: cost → existing `formatCents`, failure-rate → percent with domain `[0, 1]`, tokens → abbreviated (e.g. `1.2M`), counts → integers
       - Legend below chart, 12px mono muted, rendered only when >1 series; leave recharts `accessibilityLayer` enabled; chart wrapper carries `aria-label="<panel title> chart"`
       - Empty data (`series.length === 0` or all empty): centered `No data in range` 12px mono muted in the 180px area — NOT the unavailable notice
       In ChartPanel's `state.kind === "data"` branch, render TimeSeriesChart. Panel chrome (`data-testid="panel-<id>"`, uppercase mono title, degradation rendering via TelemetryUnavailableNotice) is preserved verbatim.
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"recharts": "3.8.1"' dashboard/web/package.json` returns 1 (exact pin, no caret)
    - `grep -c "tide_tasks_dispatched_total\|tide_tokens_used_total" dashboard/web/src/components/TelemetryView.tsx` returns 0
    - `grep -c "tide_waves_dispatched_total" dashboard/web/src/components/TelemetryView.tsx` returns at least 2 (both scope modes)
    - `grep -c "Sparkline" dashboard/web/src/components/TelemetryView.tsx` returns 0
    - `grep -c "ResponsiveContainer" dashboard/web/src/components/TelemetryView.tsx` returns at least 1
    - `cd dashboard/web && npx tsc --noEmit` exits 0; `npm test` exits 0 (existing suite unbroken)
  </acceptance_criteria>
  <done>recharts pinned and installed; all panel queries use only registry.go names in both scope modes; Sparkline deleted; charts follow the UI-SPEC C4 construction contract.</done>
</task>

<task type="auto">
  <name>Task 2: Scope toggle, range selector, 60s visibility-aware polling, budget card grid, new props contract</name>
  <files>dashboard/web/src/components/TelemetryView.tsx</files>
  <read_first>
    - dashboard/web/src/components/TelemetryView.tsx (post-Task-1 state: the per-series builders this task parameterizes, BudgetCard at the old :265 with its 24px/700 figure to fix, TelemetryViewProps to replace)
    - .planning/phases/16-telemetry-completion/16-UI-SPEC.md (§C1 segmented-control visual contract, §C3 toolbar + range/step mapping + polling rules, §C5 budget card/grid anatomy and copy, §Copywriting table — exact strings)
    - dashboard/web/src/lib/api.ts (Project + BudgetSummary types to import)
  </read_first>
  <action>
    1. Replace `TelemetryViewProps` with the contract in this plan's `<interfaces>` block: `{ projects: Project[]; selectedProject: string | null }`. Derive scope state per D-04: internal `useState<"project" | "all">` initialized from `selectedProject != null ? "project" : "all"`; a `useEffect` on `selectedProject` re-derives it (picker change while mounted → follow the picker; cleared → all). Transient — never persisted.

    2. Toolbar row above the budget surface, `flex items-center justify-between` (UI-SPEC C3):
       - Left, scope toggle `data-testid="telemetry-scope-toggle"`: segment 1 = selected project's name (label text in `var(--font-mono)` — resource-name rule), segment 2 = `All projects`. When `selectedProject` is null render ONLY the `All projects` segment. Buttons use `aria-pressed` (not tabs).
       - Right, range selector `data-testid="telemetry-range-selector"`: segments `24h` / `7d` / `30d`, default `24h`, `aria-pressed`.
       - Both follow the C1 segmented-control contract: container `inline-flex rounded border` with `var(--color-border-subtle)`; segments `<button type="button">` `px-2 py-1` 12px/600; active = `var(--color-surface-overlay)` bg + `var(--color-text-primary)`; inactive = transparent + `var(--color-text-muted)`; NO accent color; never suppress the global focus ring. Implement as repeated inline construction (planner choice within C1's discretion — a shared component would couple this plan to 16-05's tab-semantics variant).

    3. Range mapping (UI-SPEC C3 prescriptive defaults): 24h → start now−86400s, step `300`, window `5m` (288 points); 7d → now−604800s, step `1800`, window `30m` (336 points); 30d → now−2592000s, step `7200`, window `2h` (360 points). Window feeds the Task-1 query builders; step feeds fetchQueryRange.

    4. Polling (D-07, UI-SPEC-resolved 60s): one `useEffect` owning a 60s `setInterval` that re-fetches all panels only while `document.visibilityState === "visible"`; a `visibilitychange` listener pauses when hidden and resumes with an immediate fetch; range or scope change triggers an immediate re-fetch and resets the interval; cleanup clears interval and listener on unmount. No SSE. Re-fetches must not reset panels to `loading` (update in place — polling never causes layout jumps or focus steals).

    5. Budget surface (D-03, UI-SPEC C5):
       - Project scope: single `BudgetCard` for the selected project's `budget` from the `projects` prop; fix the typography compliance violation — spend figure becomes 20px / 600 / mono (currently 24px/700; weight 700 is forbidden). Keep `data-testid="budget-card"`, the `of <$cap> cap` line (13px mono muted), and the `Within budget`/`Over budget` status line.
       - All-projects scope: grid `data-testid="budget-card-grid"`, inline style `display: grid; gridTemplateColumns: "repeat(auto-fill, minmax(240px, 1fr))"; gap: 16px`; one card per project (`data-testid="budget-card-<project>"`), project name as first line (13px mono, `var(--color-text-primary)`); `capCents <= 0` → render `No budget configured` (12px mono muted) instead of figures — never `$0.00 of $0.00`, never NaN; zero projects → centered `No projects in this cluster` (12px mono muted).
       - Budget cards render from props only — zero Prometheus dependency, available even when every panel is degraded.

    All copy verbatim from the UI-SPEC Copywriting table. No new colors, no icons, no localStorage.
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="telemetry-scope-toggle"\|data-testid="telemetry-range-selector"\|data-testid="budget-card-grid"' dashboard/web/src/components/TelemetryView.tsx` returns 3
    - `grep -c "selectedProject" dashboard/web/src/components/TelemetryView.tsx` returns at least 2 (prop + scope derivation)
    - `grep -c "visibilitychange" dashboard/web/src/components/TelemetryView.tsx` returns at least 1
    - `grep -c "localStorage" dashboard/web/src/components/TelemetryView.tsx` returns 0 (transient state, D-04)
    - `grep -cE "fontWeight: *(\"|')?700|font-bold" dashboard/web/src/components/TelemetryView.tsx` returns 0 (typography fix landed)
    - `grep -c "No budget configured\|No projects in this cluster\|No data in range" dashboard/web/src/components/TelemetryView.tsx` returns 3
    - `cd dashboard/web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TelemetryView exposes the new props contract; scope follows the picker with a working all-projects toggle; range selector drives window/step; 60s visibility-aware polling; budget card(s) render per UI-SPEC C5 with the typography fix.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest suite — both locked degradation shapes plus scope/range/grid/chart contracts</name>
  <files>dashboard/web/src/components/__tests__/TelemetryView.test.tsx</files>
  <read_first>
    - dashboard/web/src/components/TelemetryView.tsx (post-Task-2 final shape — props, testids, polling effect)
    - dashboard/web/src/lib/api.test.ts (the `vi.stubGlobal("fetch", ...)` + `as unknown as typeof fetch` idiom and afterEach unstub hygiene)
    - dashboard/web/src/components/__tests__/dag-views.test.tsx (render/waitFor/act + `vi.useFakeTimers` + `advanceTimersByTimeAsync` conventions)
    - dashboard/web/src/components/TelemetryUnavailableNotice.tsx (the `data-testid="telemetry-unavailable-notice"` and wording the assertions hang off)
    - .planning/phases/16-telemetry-completion/16-UI-SPEC.md (§Validation Contract — the six test surfaces this file covers)
  </read_first>
  <action>
    Create `dashboard/web/src/components/__tests__/TelemetryView.test.tsx` (the `__tests__/` directory is the package convention — supersedes PATTERNS' adjacent-file suggestion). Use `vi.useFakeTimers()` in beforeEach and `vi.useRealTimers()` + `cleanup()` + `vi.unstubAllGlobals()` in afterEach (Pitfall 6 — the polling interval must never fire realtime in jsdom). Fixture: a `projects` array of two Projects with budgets (one `capCents: 10000, currentSpend: 500, withinBudget: true`; one `capCents: 0`).

    Cover, per the UI-SPEC Validation Contract:
    1. **Degradation shape 1 (locked TELEM-02):** fetch stub resolves `{ ok: true, status: 200, json: async () => ({ status: "unavailable" }) }` → after flushing timers/microtasks, `screen.getAllByTestId("telemetry-unavailable-notice")` has length 4 (every panel).
    2. **Degradation shape 2 (locked TELEM-02):** stub resolves `{ ok: false, status: 502, json: async () => ({ status: "error", message: "upstream unreachable" }) }` → all four notices render and the notice text matches `/unreachable/`.
    3. **Scope queries (D-02/D-04):** with `selectedProject="p1"`, every fetch URL's `query` param contains `project="p1"`; clicking the `All projects` segment fires fresh fetches whose `query` contains `by (project)` (or, for token-breakdown, omits the project filter); with `selectedProject={null}`, the project segment is absent and queries are all-projects from the start.
    4. **Range selector (D-07):** clicking `7d` issues new fetches with `step=1800` and `start ≈ now−604800` (inspect the stubbed fetch's URLSearchParams).
    5. **Budget surface (D-03):** project scope renders `budget-card` with `$5.00` spend and `of $100.00 cap`; all-projects scope renders `budget-card-grid` with one `budget-card-<name>` per project and `No budget configured` on the zero-cap project; `projects=[]` renders `No projects in this cluster`.
    6. **Chart render (D-05):** stub a success payload `{ status: "success", data: { resultType: "matrix", result: [{ metric: { project: "p1" }, values: [[1700000000, "1"], [1700000300, "2"]] }] } }` → the panel contains an `svg` element (recharts) and no `telemetry-unavailable-notice`; an empty `result: []` renders the `No data in range` copy.

    Build the fetch stub so each call can be inspected (capture `fetch.mock.calls` URLs and parse with URLSearchParams). Flush initial fetches with `await act(async () => { await vi.advanceTimersByTimeAsync(0); })` per the dag-views pattern.
  </action>
  <verify>
    <automated>cd dashboard/web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npm test` exits 0 with the new file running at least 8 test cases
    - The suite asserts `getAllByTestId("telemetry-unavailable-notice")` length 4 for BOTH shapes (200 sentinel and 502) — the locked TELEM-02 requirement
    - At least one assertion inspects a fetched URL's `query` param for `project="p1"` and one for `by (project)`
    - At least one assertion finds an `svg` inside a panel after a success payload
    - `grep -c "useFakeTimers" dashboard/web/src/components/__tests__/TelemetryView.test.tsx` returns at least 1
  </acceptance_criteria>
  <done>The Vitest suite pins both locked degradation shapes plus scope, range, budget-grid, and chart-render contracts; full dashboard suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus response → browser DOM | Query results render into React components |
| npm registry → build | One new package enters the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Tampering (XSS) | chart rendering of Prometheus payloads | mitigate | Values pass through `parseFloat` before rendering; all text renders via React JSX (auto-escaped); the repo's no-dangerous-html test (`src/__tests__/no-dangerous-html.test.ts`) continues to gate `dangerouslySetInnerHTML`. |
| T-16-11 | Tampering | read-only contract (DASH-05) | mitigate | TelemetryView issues only GET `/api/v1/query_range` fetches; no mutation endpoints exist or are called; budget data is read from props. |
| T-16-12 | Information Disclosure | localStorage persistence of scope/view | mitigate | D-04: state is transient React state — acceptance criteria assert zero `localStorage` usage. |
| T-16-SC | Tampering | recharts@3.8.1 npm install | mitigate | Package Legitimacy Audit complete (16-RESEARCH.md): ~10yr age, ~4M/wk downloads, slopcheck [OK], pinned exact with `--save-exact`. No other packages. |
</threat_model>

<verification>
- `cd dashboard/web && npx tsc --noEmit` exits 0
- `cd dashboard/web && npm test` exits 0 (full suite including bundle-size and no-dangerous-html gates)
- `grep -rc "tide_tasks_dispatched_total\|tide_tokens_used_total" dashboard/web/src/` returns 0 across the source tree
- recharts pinned at exactly 3.8.1 in package.json
</verification>

<success_criteria>
- TELEM-04: all four panels query only names present in internal/metrics/registry.go after 16-02 lands (CONTEXT acceptance shape), in both scope modes
- TELEM-02 (view half): both locked degradation shapes covered by Vitest; the view ships finished — real charts, scope toggle, range selector, polling, budget grid per the approved UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/16-telemetry-completion/16-04-SUMMARY.md` when done
</output>
