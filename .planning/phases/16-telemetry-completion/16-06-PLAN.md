---
phase: 16-telemetry-completion
plan: 06
type: execute
wave: 3
depends_on: ["16-04"]
files_modified:
  - dashboard/web/src/components/TelemetryView.tsx
  - dashboard/web/src/components/__tests__/TelemetryView.test.tsx
autonomous: true
gap_closure: true
requirements: [TELEM-02, TELEM-04]
tags: [react, recharts, prometheus, vitest, telemetry, dashboard]

must_haves:
  truths:
    - "In all-projects scope, the Cost Over Time panel renders one distinct series per project returned by the by (project) query — not one collapsed series"
    - "In all-projects scope, multi-series panels (Dispatch Counts) render per-project series with disambiguated legend keys"
    - "Project-scope panels are unchanged: series keys remain the fixed SeriesDef keys"
    - "A Vitest case covers all-projects scope with a multi-result success payload and fails if series collapse onto one key"
  artifacts:
    - path: "dashboard/web/src/components/TelemetryView.tsx"
      provides: "Scope-aware series-key derivation in fetchPanel merge step"
      contains: "scope === \"all\""
    - path: "dashboard/web/src/components/__tests__/TelemetryView.test.tsx"
      provides: "All-projects multi-result render regression test (the test whose absence let CR-01 slip)"
  key_links:
    - from: "fetchPanel merge step"
      to: "matrixToPoints"
      via: "per-matrix key derived from matrix.metric[\"project\"] when scope is all"
      pattern: "matrix\\.metric\\[\"project\"\\]"
---

<objective>
Close CR-01 (16-VERIFICATION.md gap, BLOCKING): in all-projects scope every `SeriesDef` in `PANELS` sets a fixed `key`, so the `matrix.metric["project"]` fallback at TelemetryView.tsx:329 is dead code and `matrixToPoints` (:347) lets the last project's matrix silently overwrite all others — the Cost Over Time / Dispatch Counts / Failure Rate panels render one arbitrary project's values mislabeled as the cluster aggregate.

Purpose: the "All projects toggle switches to by (project) aggregates" truth (plan 16-04, D-02) must hold at the rendering layer, not just the query layer. This falsified truth is the one gap blocking a clean Phase 16 PASS.
Output: scope-aware key derivation in `fetchPanel` + a Vitest regression test for the all-projects multi-result success path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-telemetry-completion/16-VERIFICATION.md   (gap CR-01 — primary input)
@.planning/phases/16-telemetry-completion/16-REVIEW.md          (CR-01 narrative + suggested fix shape)
@.planning/phases/16-telemetry-completion/16-04-SUMMARY.md      (what 16-04 built; deviation #5 = budget-card name-collision lesson)
@dashboard/web/src/components/TelemetryView.tsx
@dashboard/web/src/components/__tests__/TelemetryView.test.tsx

<interfaces>
From dashboard/web/src/components/TelemetryView.tsx (current, the defect):

```ts
// :111-114 — SeriesDef
type SeriesDef = {
  key?: string; // fixed legend label; omitted → use metric.project from response
  buildQuery: (scope: ScopeKind, project: string, window: string) => string;
};

// :293-301 — fetchPanel already receives scope; no call-site change needed
async function fetchPanel(panelDef: PanelDef, scope: ScopeKind, projectName: string,
  startSec: number, endSec: number, step: number, window: string): Promise<PanelState>

// :325-332 — the defective merge step
results.forEach((r, i) => {
  if (r.kind !== "data") return;
  const sd = seriesDefs[i];
  r.result.forEach((matrix) => {
    const key = sd.key ?? matrix.metric["project"] ?? Object.values(matrix.metric).join(",") ?? "value";
    allSeries.push({ key, matrix });
  });
});
```

Panel shape facts the fix must respect:
- Cost Over Time / Failure Rate: 1 SeriesDef; all-projects query is `... by (project)` → matrices carry `metric.project`.
- Dispatch Counts: 2 SeriesDefs ("waves dispatched", "tasks completed"); all-projects queries are `by (project)` → N projects x 2 series need disambiguated keys.
- Token Breakdown: 4 SeriesDefs; all-projects queries are plain cluster sums (NO `by (project)`) → `matrix.metric` is empty `{}` → keys must remain "input"/"output"/"cache read"/"cache creation".

From __tests__/TelemetryView.test.tsx (existing helpers to reuse):
- `stubFetchOK(payload)` (:100) — global fetch stub returning the payload for every panel fetch
- success-payload const at :77-92 (`status: "success"`, `data.result: [...]`)
- `flushInitialFetch()` (:126)
- recharts `vi.mock` ResponsiveContainer override (jsdom fix, established pattern)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive per-project series keys in all-projects scope + Vitest multi-result regression test</name>
  <files>dashboard/web/src/components/TelemetryView.tsx, dashboard/web/src/components/__tests__/TelemetryView.test.tsx</files>
  <read_first>
    - dashboard/web/src/components/TelemetryView.tsx (:111-114 SeriesDef, :293-335 fetchPanel, :341-352 matrixToPoints, :405-510 TimeSeriesChart/Legend)
    - dashboard/web/src/components/__tests__/TelemetryView.test.tsx (:77-132 payload consts + stubFetchOK + flushInitialFetch; :200-265 existing all-projects scope tests)
    - .planning/phases/16-telemetry-completion/16-VERIFICATION.md (gaps frontmatter — the two `missing` items this task implements)
  </read_first>
  <behavior>
    - Test 1 (RED first — this is the test whose absence let CR-01 ship): render `<TelemetryView projects={[]} selectedProject={null} />` (all-projects mode per D-04; empty projects array prevents budget-card text collision with project names — 16-04 deviation #5). Stub fetch with a success payload whose `data.result` contains TWO matrices: `{metric: {project: "p1"}, values: [[t1,"1"],[t2,"2"]]}` and `{metric: {project: "p2"}, values: [[t1,"3"],[t2,"4"]]}`. After `flushInitialFetch()`, assert BOTH "p1" and "p2" appear as rendered legend entries (`screen.getAllByText("p1")` / `getAllByText("p2")` each length >= 1 — single-SeriesDef panels key by bare project name). Against unfixed code this fails: all matrices collapse onto the fixed `sd.key` and no project name renders.
    - Test 2: same payload/render — assert a multi-SeriesDef panel disambiguates: legend text "waves dispatched (p1)" and "waves dispatched (p2)" both present (Dispatch Counts has 2 SeriesDefs, so bare project names would collide across its own series).
    - Test 3 (no-regression): project scope (`selectedProject="p1"`, projects containing p1) with a single-matrix success payload — series keys remain the fixed SeriesDef keys (e.g. Cost panel legend/series key is "cost", chart renders, no "p1 (cost)"-style keys). Existing project-scope tests may already pin this; extend only if they don't assert key identity.
  </behavior>
  <action>
    In `fetchPanel`'s merge step (TelemetryView.tsx :325-332), replace the dead-fallback key derivation with scope-aware derivation (review CR-01 suggested shape, adapted to in-scope identifiers):

    - `const projectLabel = matrix.metric["project"];`
    - When `scope === "all" && projectLabel` is truthy: `key = seriesDefs.length > 1 ? `${sd.key} (${projectLabel})` : projectLabel` — per-project series, disambiguated when the panel has multiple SeriesDefs.
    - Otherwise: `key = sd.key ?? projectLabel ?? "value"` (drop the dead `Object.values(matrix.metric).join(",")` link — `join()` never returns undefined, so it and the trailing `?? "value"` after it were both unreachable).

    `fetchPanel` already receives `scope` as its second parameter — no call-site changes. `matrixToPoints` is untouched: with distinct keys, per-project values no longer overwrite. The Token Breakdown all-projects case is handled by the conditional: its cluster-sum queries return `metric: {}` → `projectLabel` undefined → fixed keys preserved.

    Update the stale `SeriesDef.key` comment at :112 to describe the new contract (fixed legend label in project scope and for non-grouped queries; all-projects `by (project)` results key by project name, suffixed with the series label when the panel has multiple SeriesDefs).

    Write the Vitest cases from <behavior> FIRST and observe Test 1 + Test 2 fail against the unfixed merge step, then apply the fix and re-run to green. Place them in a new `describe("TelemetryView — all-projects per-project series (CR-01)")` block reusing `stubFetchOK`/`flushInitialFetch` and the established recharts mock. Do NOT change any PromQL builder, PanelDef, polling, or degradation logic — this fix is confined to the merge-step key derivation plus its comment.
  </action>
  <verify>
    <automated>cd dashboard/web && npx vitest run src/components/__tests__/TelemetryView.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'scope === "all"' dashboard/web/src/components/TelemetryView.tsx` returns >= 1 (scope-aware derivation present in fetchPanel)
    - `grep -c 'Object.values(matrix.metric)' dashboard/web/src/components/TelemetryView.tsx` returns 0 (dead fallback removed)
    - `grep -c 'all-projects per-project series' dashboard/web/src/components/__tests__/TelemetryView.test.tsx` returns >= 1 (regression describe block exists)
    - `cd dashboard/web && npx vitest run src/components/__tests__/TelemetryView.test.tsx` exits 0 with the new tests included (no skips)
    - `cd dashboard/web && npx vitest run` exits 0 (full dashboard suite — no regression in the other 26 files)
    - `cd dashboard/web && npx tsc --noEmit` exits 0
    - RED proof recorded in SUMMARY: Test 1 failure output against the pre-fix merge step (run tests before applying the source change)
  </acceptance_criteria>
  <done>All-projects scope renders one series per project with correct legend keys on every project-grouped panel; project scope and Token Breakdown behavior unchanged; the multi-result success path is pinned by Vitest so CR-01 cannot regress silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus response → React render | `matrix.metric["project"]` values originate from Prometheus label values (K8s resource names via emitted metrics) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-20 | Tampering/XSS | TelemetryView legend keys | mitigate | Project label values render only through React JSX text nodes (legend/dataKey) — no dangerouslySetInnerHTML; same posture as 16-04 T-16-10 |
| T-16-SC | Tampering | npm installs | accept | No new packages installed in this plan (recharts already pinned 3.8.1) |
</threat_model>

<verification>
- New regression test fails on pre-fix code, passes post-fix (RED→GREEN evidence in summary)
- Full dashboard Vitest suite green; tsc clean
- No changes outside TelemetryView.tsx merge step + comment and the test file
</verification>

<success_criteria>
- CR-01 gap truth satisfied: "The All projects toggle switches to by (project) aggregates and renders a per-project series for each project"
- Both `missing` items from 16-VERIFICATION.md gaps frontmatter implemented (scope-aware key derivation; all-projects multi-result Vitest case)
</success_criteria>

<output>
Create `.planning/phases/16-telemetry-completion/16-06-SUMMARY.md` when done
</output>
