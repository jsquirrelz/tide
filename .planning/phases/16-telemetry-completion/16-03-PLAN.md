---
phase: 16-telemetry-completion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - .github/workflows/ci.yaml
  - hack/helm/assert-prometheus-env.py
  - hack/helm/assert-telemetry-render.sh
autonomous: true
requirements: [TELEM-05]
must_haves:
  truths:
    - "make helm-telemetry-assert runs assert-telemetry-render.sh plus both assert-prometheus-env.py invocations (--expect-endpoint and --expect-absent) and passes locally"
    - "make helm-assert aggregates helm-rbac-assert + helm-telemetry-assert and passes locally"
    - "Every push runs make helm-assert in ci.yaml's helm-lint job"
    - "No docstring claims helm-rbac-assert drives the telemetry scripts"
  artifacts:
    - path: "Makefile"
      provides: "helm-telemetry-assert and helm-assert targets"
      contains: "helm-telemetry-assert"
    - path: ".github/workflows/ci.yaml"
      provides: "make helm-assert step in the helm-lint job"
      contains: "make helm-assert"
  key_links:
    - from: "Makefile helm-telemetry-assert"
      to: "hack/helm/assert-prometheus-env.py"
      via: "two python3 invocations (--expect-absent and --expect-endpoint)"
      pattern: "assert-prometheus-env\\.py"
    - from: ".github/workflows/ci.yaml helm-lint job"
      to: "Makefile helm-assert"
      via: "run: make helm-assert"
      pattern: "make helm-assert"
---

<objective>
Close TELEM-05: wire the orphaned `hack/helm` telemetry render gates into the Makefile (D-13 umbrella targets) and CI (D-14 per-push step), and correct the docstrings that falsely claim `make helm-rbac-assert` drives them.

Purpose: the gates exist and pass, but nothing runs them — chart regressions in PROM_ENDPOINT injection would ship silently.
Output: `helm-telemetry-assert` + aggregate `helm-assert` Makefile targets, a `make helm-assert` CI step, corrected docstrings.

Note on the ROADMAP "pass on a running cluster" wording: per locked D-14, both scripts are pure `helm template` render gates (assert-telemetry-render.sh states "No cluster connection needed") — the criterion is satisfied without a cluster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-telemetry-completion/16-CONTEXT.md
@.planning/phases/16-telemetry-completion/16-PATTERNS.md

Verified facts:
- `Makefile:546-555` holds the `helm-rbac-assert` target — the style template (PHONY, `##` docstring, `@helm template ... > /tmp/...` then `@python3 ...`).
- `hack/helm/assert-prometheus-env.py` takes a render file plus `--expect-endpoint <url>` XOR `--expect-absent`; its docstring falsely claims `make helm-rbac-assert` drives it.
- `hack/helm/assert-telemetry-render.sh` is the self-contained EC-7 four-permutation gate; runs `helm template` itself.
- `.github/workflows/ci.yaml:141-193` is the `helm-lint` job: helm installed via azure/setup-helm@v4 (~line 159), python3 present on ubuntu-latest, chart-reproducibility step ends ~line 192. No new setup steps needed.
- Chart is a FIXED contract — this plan reads chart renders, it never edits `charts/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add helm-telemetry-assert + helm-assert Makefile targets and fix the gate docstrings (D-13)</name>
  <files>Makefile, hack/helm/assert-prometheus-env.py, hack/helm/assert-telemetry-render.sh</files>
  <read_first>
    - Makefile (lines ~540-560: the `helm-lint-validate` and `helm-rbac-assert` targets whose style — .PHONY, `## docstring`, @-prefixed recipe lines, /tmp render paths — the new targets copy; and the `##@ Legal compliance gates` section boundary the new block sits before)
    - hack/helm/assert-prometheus-env.py (the `--expect-endpoint` / `--expect-absent` CLI and the false `make helm-rbac-assert` docstring claim to correct)
    - hack/helm/assert-telemetry-render.sh (header comments — check for the same false claim; the script self-runs helm template)
    - .planning/phases/16-telemetry-completion/16-PATTERNS.md (§ Makefile — the exact target recipes)
  </read_first>
  <action>
    Add two targets immediately after the `helm-rbac-assert` block (before the `##@ Legal compliance gates` section), following the PATTERNS recipe exactly:

    1. `helm-telemetry-assert` (.PHONY, docstring "Assert PROM_ENDPOINT env injection and telemetry render gates (Phase 16 TELEM-05 D-13).") with four recipe lines:
       - `helm template charts/tide --set dashboard.enabled=true > /tmp/tide-helm-render.yaml` then `python3 hack/helm/assert-prometheus-env.py /tmp/tide-helm-render.yaml --expect-absent`
       - `helm template charts/tide --set dashboard.enabled=true --set prometheus.endpoint=http://prom:9090 > /tmp/tide-helm-render-prom.yaml` then `python3 hack/helm/assert-prometheus-env.py /tmp/tide-helm-render-prom.yaml --expect-endpoint http://prom:9090`
       - `bash hack/helm/assert-telemetry-render.sh`
       All recipe lines @-prefixed per house style.
    2. `helm-assert` (.PHONY) with prerequisites `helm-rbac-assert helm-telemetry-assert` and docstring "Run all Helm render gate assertions (Phase 16 TELEM-05 D-13)." — no recipe body needed.

    Docstring corrections (D-13): in `hack/helm/assert-prometheus-env.py`, replace the claim that `make helm-rbac-assert` drives it with `make helm-telemetry-assert` (which now actually does). Check `hack/helm/assert-telemetry-render.sh` header for the same false claim and correct it identically if present. If the `helm-rbac-assert` Makefile docstring itself mentions the telemetry scripts, trim it to describe only the RBAC check. Do not change any script logic — docstrings/comments only.
  </action>
  <verify>
    <automated>make helm-telemetry-assert && make helm-assert</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^helm-telemetry-assert:' Makefile` returns 1 and `grep -cE '^helm-assert: helm-rbac-assert helm-telemetry-assert' Makefile` returns 1
    - `make helm-telemetry-assert` exits 0 locally (both assert-prometheus-env.py permutations + assert-telemetry-render.sh pass)
    - `make helm-assert` exits 0 locally
    - `grep -rn "helm-rbac-assert" hack/helm/assert-prometheus-env.py hack/helm/assert-telemetry-render.sh` returns 0 matches (false claims gone)
    - `git status --porcelain charts/` is empty (chart untouched)
  </acceptance_criteria>
  <done>Both umbrella targets exist, follow house style, and pass locally; no docstring misattributes the telemetry gates to helm-rbac-assert.</done>
</task>

<task type="auto">
  <name>Task 2: Add the make helm-assert step to ci.yaml's helm-lint job (D-14)</name>
  <files>.github/workflows/ci.yaml</files>
  <read_first>
    - .github/workflows/ci.yaml (the `helm-lint` job at ~lines 141-193: checkout, azure/setup-helm@v4, lint/template/reproducibility steps — the new step's indentation and naming must match these; confirm the job checks out the full repo so the Makefile and hack/ are present)
  </read_first>
  <action>
    Append one step to the `helm-lint` job, after the chart-reproducibility step (~line 192), matching the job's existing step style:

    - name: `Helm render gate assertions (TELEM-05 D-14)`
    - run: `make helm-assert`

    No new setup steps — helm is installed by azure/setup-helm@v4 and python3 ships on ubuntu-latest. Do not touch any other job.
  </action>
  <verify>
    <automated>grep -A1 "Helm render gate assertions" .github/workflows/ci.yaml | grep -c "make helm-assert"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "make helm-assert" .github/workflows/ci.yaml` returns 1, located inside the `helm-lint` job (verify with `awk '/^  helm-lint:/,/^  [a-z-]+:/' .github/workflows/ci.yaml | grep -c "make helm-assert"` returning 1)
    - YAML parses: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yaml'))"` exits 0
    - No other job in ci.yaml is modified (`git diff --stat .github/workflows/ci.yaml` shows a single-hunk addition)
  </acceptance_criteria>
  <done>Every push runs the full helm-assert gate suite inside the existing helm-lint job with zero added setup cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo scripts | ci.yaml executes repo-controlled make targets and python/bash gate scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Tampering | CI executes hack/helm scripts | accept | Scripts are repo-tracked and review-gated; the new step grants no new permissions (same job, same GITHUB_TOKEN scope, no secrets consumed). |
| T-16-09 | Elevation of Privilege | helm-lint job scope | accept | Pure render gates — `helm template` only, no cluster credentials, no kubeconfig in the job. |
| T-16-SC | Tampering | package installs | accept | Zero new packages or actions (azure/setup-helm@v4 already pinned in the job). |
</threat_model>

<verification>
- `make helm-assert` exits 0 locally
- ci.yaml parses as valid YAML and the step lands in helm-lint only
- `git status --porcelain charts/` empty
</verification>

<success_criteria>
- TELEM-05: `make helm-telemetry-assert` and `make helm-assert` exist, pass locally, run per-push in CI's helm-lint job; docstrings corrected (CONTEXT acceptance shape: "make helm-assert passes locally and the same target runs green in ci.yaml's helm-lint job")
</success_criteria>

<output>
Create `.planning/phases/16-telemetry-completion/16-03-SUMMARY.md` when done
</output>
