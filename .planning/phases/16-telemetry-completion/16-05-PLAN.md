---
phase: 16-telemetry-completion
plan: 05
type: execute
wave: 2
depends_on: ["16-04"]
files_modified:
  - dashboard/web/src/App.tsx
  - dashboard/web/src/components/Header.tsx
  - dashboard/web/src/components/__tests__/view-switcher.test.tsx
autonomous: true
requirements: [TELEM-02]
must_haves:
  truths:
    - "The header shows a DAGs | Telemetry segmented switcher; default view is DAGs and the existing two-pane body (including Phase 15's RunningWavesView right-pane default) renders exactly as before"
    - "Clicking Telemetry mounts a full-width TelemetryView in the main slot; clicking DAGs restores the two-pane body"
    - "Opening Telemetry with a project selected lands in selected-project scope; with none selected, all-projects scope (D-04 follow-the-picker)"
    - "View state is transient — nothing is persisted"
  artifacts:
    - path: "dashboard/web/src/components/Header.tsx"
      provides: "viewSwitcher optional slot in HeaderProps"
      contains: "viewSwitcher"
    - path: "dashboard/web/src/App.tsx"
      provides: "activeView state, ViewSwitcher tablist control, telemetry body branch"
      contains: "TelemetryView"
    - path: "dashboard/web/src/components/__tests__/view-switcher.test.tsx"
      provides: "App-level Vitest: default DAGs, tab switch mounts telemetry-view, DAGs body restored"
      min_lines: 60
  key_links:
    - from: "dashboard/web/src/App.tsx"
      to: "dashboard/web/src/components/TelemetryView.tsx"
      via: "telemetry body branch with the 16-04 props contract"
      pattern: "projects=\\{projects\\}|selectedProject=\\{selectedProject\\}"
    - from: "dashboard/web/src/App.tsx ViewSwitcher"
      to: "Header viewSwitcher slot"
      via: "Header prop"
      pattern: "viewSwitcher="
---

<objective>
Close the mounting half of TELEM-02 (D-01): add the header view switcher ("DAGs" | "Telemetry") and mount TelemetryView as the Telemetry view's full-width body, leaving the DAGs view and Phase 15's right-pane logic byte-for-byte untouched, with App-level Vitest coverage.

Purpose: TelemetryView exists and (after 16-04) is finished — but nothing renders it. This is the final wiring.
Output: Header slot, App.tsx view state + switcher + body branch, view-switcher test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-telemetry-completion/16-CONTEXT.md
@.planning/phases/16-telemetry-completion/16-UI-SPEC.md
@.planning/phases/16-telemetry-completion/16-PATTERNS.md

Dependencies on 16-04 (must be merged before this plan executes):
- `TelemetryViewProps` is `{ projects: Project[]; selectedProject: string | null }` (see 16-04 `<interfaces>`); recharts is installed. App.tsx already holds the `projects` array (with budgets) and `selectedProject` state — pass them straight through; no new fetches.

Guardrails:
- Phase 15 D-13: the RunningWavesView right-pane default and the entire two-pane body (App.tsx ~:197-:313) are passed through UNCHANGED in the DAGs branch — additive conditional only.
- `AppShell.tsx` is untouched (pure layout shell — the switch lives in App.tsx state + Header slot).
- Conditional render, not display:none — the Telemetry body does not mount the DAG views and vice versa (UI-SPEC C2).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header viewSwitcher slot + App.tsx view state, ViewSwitcher control, and telemetry body branch (D-01)</name>
  <files>dashboard/web/src/components/Header.tsx, dashboard/web/src/App.tsx</files>
  <read_first>
    - dashboard/web/src/components/Header.tsx (HeaderProps at ~:15-20 with the optional `projectPicker` slot pattern to copy; the left-cluster `gap-4` flex row at ~:79-88 where the switcher renders)
    - dashboard/web/src/App.tsx (entire body-branch structure ~:197-313: the projectsError/loading/empty/two-pane chain the telemetry branch extends; the state block ~:89-100; the Header invocation ~:286-292; where `projects` and `selectedProject` live)
    - dashboard/web/src/components/TelemetryView.tsx (the post-16-04 TelemetryViewProps contract — verify before wiring)
    - .planning/phases/16-telemetry-completion/16-UI-SPEC.md (§C1 segmented-control contract, §C2 view switcher semantics: tablist/tab/aria-selected, ArrowLeft/ArrowRight roving focus, testids, segment order DAGs→Telemetry)
  </read_first>
  <action>
    1. `Header.tsx`: add optional `viewSwitcher?: ReactNode` to `HeaderProps` (same pattern as `projectPicker`), destructure it, and render `{viewSwitcher}` in the left cluster immediately after `{projectPicker}` inside the existing `gap-4` row. Header height stays `h-12`; right group untouched.

    2. `App.tsx`: add `const [activeView, setActiveView] = useState<"dags" | "telemetry">("dags")` beside the existing selection state (transient — no localStorage, D-04). Define a `ViewSwitcher` function component above `App` (PaneHeader-style local component): container `role="tablist"` `aria-label="Dashboard view"` `data-testid="view-switcher"`; two `<button type="button">` segments in order `DAGs`, `Telemetry` with `role="tab"`, `aria-selected`, `data-testid="view-tab-dags"` / `data-testid="view-tab-telemetry"`; ArrowLeft/ArrowRight onKeyDown moves selection AND focus between the two tabs (roving focus, UI-SPEC C2). Visuals per the C1 contract: `px-2 py-1` 12px/600 labels, active = `var(--color-surface-overlay)` background + `var(--color-text-primary)`, inactive = transparent + `var(--color-text-muted)`, border `var(--color-border-subtle)`, NO accent, never suppress the global focus ring.

    3. Pass `viewSwitcher={<ViewSwitcher activeView={activeView} onChange={setActiveView} />}` in the existing `<Header ...>` invocation.

    4. Body branch: in the existing body chain, AFTER the error/loading/empty guards and BEFORE the two-pane else-branch, add `else if (activeView === "telemetry")` rendering `<TelemetryView projects={projects} selectedProject={selectedProject} />` full-width in the main slot (no two-pane grid, no split-ratio involvement). The final else keeps the existing two-pane body verbatim — zero edits inside it. Import `TelemetryView` from `./components/TelemetryView`.
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "viewSwitcher" dashboard/web/src/components/Header.tsx` returns at least 2 (prop type + render)
    - `grep -c 'data-testid="view-tab-dags"\|data-testid="view-tab-telemetry"' dashboard/web/src/App.tsx` returns 2
    - `grep -c 'role="tablist"' dashboard/web/src/App.tsx` returns 1
    - `grep -c "localStorage" dashboard/web/src/App.tsx` returns 0
    - `git diff dashboard/web/src/App.tsx` shows NO changes inside the existing two-pane body JSX (additive branch + state + imports + Header prop only)
    - `cd dashboard/web && npx tsc --noEmit && npm test` exits 0 (existing suites — including RunningWavesView tests — unbroken)
  </acceptance_criteria>
  <done>Switcher renders in the header left cluster with full tab semantics; Telemetry branch mounts TelemetryView with the 16-04 props; DAGs branch and Phase 15 pane logic untouched.</done>
</task>

<task type="auto">
  <name>Task 2: App-level Vitest — default view, tab switching, DAGs passthrough</name>
  <files>dashboard/web/src/components/__tests__/view-switcher.test.tsx</files>
  <read_first>
    - dashboard/web/src/components/__tests__/dag-views.test.tsx (the harness to copy: `vi.mock("@xyflow/react")` useNodesInitialized stub, FakeEventSource class for SSE, fetch stubbing, act/waitFor flushing)
    - dashboard/web/src/App.tsx (post-Task-1: what App fetches on mount and which testids the assertions target)
    - .planning/phases/16-telemetry-completion/16-UI-SPEC.md (§Validation Contract row 1 — the four behaviors to pin)
  </read_first>
  <action>
    Create `view-switcher.test.tsx` rendering the real `<App />` with the dag-views harness (FakeEventSource stub for jsdom's missing EventSource, `vi.mock("@xyflow/react")` for deterministic layout, `vi.stubGlobal("fetch", ...)` routing by URL: `/api/v1/projects` → a fixture list of 1-2 projects with budgets; `/api/v1/query_range` → `{ status: "unavailable" }`; any other GET → a benign empty-shape response matching what App requests — discover the exact set from App.tsx and the dag-views stubs). Use fake timers per the established pattern to keep TelemetryView's polling inert.

    Pin four behaviors (UI-SPEC Validation Contract):
    1. Default: after initial render+flush, `view-tab-dags` has `aria-selected="true"`, `telemetry-view` is NOT in the document, and the two-pane DAGs body testids are present.
    2. Switch to Telemetry: `fireEvent.click` on `view-tab-telemetry` → `telemetry-view` present, the two-pane body testids absent, `view-tab-telemetry` aria-selected.
    3. Switch back: click `view-tab-dags` → two-pane body restored (Phase 15 right-pane default testid present), `telemetry-view` gone.
    4. Keyboard: with focus on `view-tab-dags`, ArrowRight selects + focuses `view-tab-telemetry` (roving focus).

    Follow the file's surrounding conventions for cleanup (`cleanup`, `vi.unstubAllGlobals`, `vi.useRealTimers` in afterEach).
  </action>
  <verify>
    <automated>cd dashboard/web && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npm test` exits 0 with the new file contributing at least 4 passing test cases
    - One assertion proves `telemetry-view` absent by default and present after clicking `view-tab-telemetry`
    - One assertion proves the DAGs two-pane body is restored after switching back
    - One assertion exercises ArrowRight roving focus between the tabs
  </acceptance_criteria>
  <done>App-level suite pins the D-01 switcher contract: default DAGs, mount/unmount on switch, Phase 15 body passthrough, keyboard semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → view state | Tab clicks/keys mutate only client-side React state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering | read-only contract (DASH-05) | mitigate | The switcher writes React state only; no new network calls, no mutation endpoints; acceptance criteria forbid localStorage. |
| T-16-14 | Denial of Service | double-mounting both views | mitigate | Conditional render (UI-SPEC C2) — only one body tree mounts; TelemetryView's polling stops on unmount via its cleanup (16-04). |
| T-16-SC | Tampering | package installs | accept | Zero new packages in this plan. |
</threat_model>

<verification>
- `cd dashboard/web && npx tsc --noEmit` exits 0
- `cd dashboard/web && npm test` exits 0 — full suite, including the pre-existing dag-views and RunningWavesView specs (Phase 15 regression guard)
</verification>

<success_criteria>
- TELEM-02 (mount half): AppShell's header renders the Telemetry tab; clicking it mounts TelemetryView; DAGs view and Phase 15 right-pane default verifiably unchanged; App-level Vitest green
</success_criteria>

<output>
Create `.planning/phases/16-telemetry-completion/16-05-SUMMARY.md` when done
</output>
