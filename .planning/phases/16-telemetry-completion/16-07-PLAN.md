---
phase: 16-telemetry-completion
plan: 07
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - internal/controller/task_controller.go
  - internal/controller/task_controller_metrics_test.go
  - internal/controller/plan_controller.go
  - internal/controller/plan_controller_metrics_test.go
autonomous: true
gap_closure: true
requirements: [TELEM-03, TELEM-04]
tags: [metrics, prometheus, task-controller, plan-controller, telemetry]

must_haves:
  truths:
    - "tide_tasks_completed_total increments on every Succeeded task terminal commit (same exactly-once seam as emitTaskMetrics/RollUpUsage)"
    - "tide_tasks_failed_total increments with a bounded documented reason on every envelope-bearing Failed terminal commit"
    - "tide_waves_dispatched_total increments exactly once per Wave CR creation in materializeWaves (not on AlreadyExists, not on idempotent re-reconcile)"
    - "TaskDurationSeconds never observes a negative duration (stale envelope / clock skew skipped with a logged signal)"
    - "Metric names and label sets in internal/metrics/registry.go are byte-for-byte unchanged"
  artifacts:
    - path: "internal/controller/task_controller.go"
      provides: "TasksCompletedTotal/TasksFailedTotal emission inside emitTaskMetrics + metricFailureReason mapping + negative-duration guard"
      contains: "TasksCompletedTotal"
    - path: "internal/controller/plan_controller.go"
      provides: "WavesDispatchedTotal emission at the materializeWaves Create commit point"
      contains: "WavesDispatchedTotal"
    - path: "internal/controller/plan_controller_metrics_test.go"
      provides: "Wave-dispatch counter regression tests (create-once + idempotent re-call)"
  key_links:
    - from: "internal/controller/task_controller.go emitTaskMetrics"
      to: "tidemetrics.TasksCompletedTotal / tidemetrics.TasksFailedTotal"
      via: "WithLabelValues at the three RollUpUsage seams"
      pattern: "TasksFailedTotal\\.WithLabelValues"
    - from: "internal/controller/plan_controller.go materializeWaves"
      to: "tidemetrics.WavesDispatchedTotal"
      via: "Inc() on successful r.Create(ctx, wave) only"
      pattern: "WavesDispatchedTotal\\.WithLabelValues"
---

<objective>
Close CR-02 (16-REVIEW.md Critical) and WR-04 (Warning) — both in the controller metrics path:

1. CR-02: `tide_waves_dispatched_total`, `tide_tasks_completed_total`, `tide_tasks_failed_total` are registered (Phase 4 D-O2) but have zero production `.Inc()` sites, so the Dispatch Counts and Failure Rate panels render "No data in range" on every cluster, permanently. Phase 16 is the last v1.0.1 phase — there is no later phase to absorb emission. Emit from the natural seams: the TaskReconciler terminal commit points where `emitTaskMetrics` already resolves `{project, phase, plan}`, and the Plan controller's Wave-CR creation commit point (WaveReconciler is observational by design — D-B2 — and never dispatches; `materializeWaves` at plan_controller.go:1295 is the only Wave creation site).
2. WR-04: `TaskDurationSeconds.Observe` (task_controller.go:1040-1043) guards `completedAt.IsZero()` but not `completedAt < StartedAt` — a stale envelope from a prior attempt or manager↔pod clock skew drags the histogram `_sum` permanently negative.

Constraint (locked): metric NAMES and LABEL SETS are unchanged. `tide_waves_dispatched_total` and `tide_tasks_completed_total` carry `{project, phase, plan}` (3 labels, no wave); `tide_tasks_failed_total` carries `{project, phase, plan, reason}` with the bounded documented reason enum {exit-1, gitleaks, lease, auth, internal, budget}. No registry.go edits.

Purpose: TELEM-04's Dispatch Counts and Failure Rate panels become live; TELEM-03's histogram stays corruption-free.
Output: emission wiring + bounded reason mapping + negative-duration guard, each pinned by Go regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-telemetry-completion/16-VERIFICATION.md   (CR-02 + WR-04 anti-pattern rows, Data-Flow Trace ✗ STATIC row)
@.planning/phases/16-telemetry-completion/16-REVIEW.md          (CR-02 + WR-04 narratives with suggested fix shapes)
@.planning/phases/16-telemetry-completion/16-02-SUMMARY.md      (emitTaskMetrics/resolveWave seams built by 16-02; unique-label-set test pattern)
@internal/metrics/registry.go
@internal/controller/task_controller.go
@internal/controller/plan_controller.go
@internal/controller/task_controller_metrics_test.go

<interfaces>
From internal/metrics/registry.go (FIXED — do not modify):

```go
// :126-148
WavesDispatchedTotal  // name "tide_waves_dispatched_total",  labels []string{"project", "phase", "plan"}
TasksCompletedTotal   // name "tide_tasks_completed_total",   labels []string{"project", "phase", "plan"}
TasksFailedTotal      // name "tide_tasks_failed_total",      labels []string{"project", "phase", "plan", "reason"}
// Documented reason enum (:52-64): {exit-1, gitleaks, lease, auth, internal, budget}
```

From internal/controller/task_controller.go:

```go
// :1011 — current signature (this plan extends it)
func (r *TaskReconciler) emitTaskMetrics(ctx context.Context, task *tideprojectv1alpha1.Task,
    project *tideprojectv1alpha1.Project, usage pkgdispatch.Usage, completedAt time.Time) error
// :1040-1043 — the unguarded Observe (WR-04)
// Seams (call sites): :864 OutputValidationError (Failed), :896 OutputPathsViolation (Failed),
//   :952 standard terminal (Failed-or-Succeeded decided at :905:
//   out.ExitCode != 0 || out.Result == "cap-hit" || out.Result == "output-paths-violation")
// :819-833 EnvelopeReadFailed branch has NO RollUpUsage and NO emitTaskMetrics — see action for the deliberate exclusion.
```

From internal/controller/plan_controller.go:

```go
// :1295 — the only Wave creation site (WaveReconciler is observational, D-B2)
func (r *PlanReconciler) materializeWaves(ctx context.Context, plan *tideprojectv1alpha1.Plan,
    _ []tideprojectv1alpha1.Task, layers [][]dag.NodeID) error
// :1320-1325 — r.Create(ctx, wave); AlreadyExists tolerated as idempotent success
// :1388 — resolveProjectName(ctx, plan) (string, error): label fast-path then owner-chain walk; ErrParentUnresolved on miss
// phase label source: plan.Spec.PhaseRef
```

Test pattern (16-02, task_controller_metrics_test.go): fake client + `testutil.ToFloat64` / `testutil.CollectAndCount`; UNIQUE label values per test (counters cannot be reset — cross-test interference otherwise).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit TasksCompletedTotal/TasksFailedTotal at the emitTaskMetrics seams + guard negative durations (CR-02 task counters + WR-04)</name>
  <files>internal/controller/task_controller.go, internal/controller/task_controller_metrics_test.go</files>
  <read_first>
    - internal/controller/task_controller.go (:800-965 handleJobCompletion incl. all three seams and the :905 failure predicate; :989-1045 resolveWave + emitTaskMetrics)
    - internal/controller/task_controller_metrics_test.go (existing TestEmitTaskMetrics_EndToEnd / _PhaseMissSentinel — signature change ripples here; unique-label-set pattern)
    - internal/metrics/registry.go (:40-64 the three Phase 4 counters' label sets + bounded reason enum — the contract this task must NOT alter)
  </read_first>
  <behavior>
    - Test 1: emitTaskMetrics with empty failureReason ("" = Succeeded) increments tide_tasks_completed_total by exactly 1 with {project, phase, plan} (no wave label) AND leaves tide_tasks_failed_total untouched for that label set; the six 16-02 metrics still emit (existing assertions preserved).
    - Test 2: emitTaskMetrics with failureReason "budget" increments tide_tasks_failed_total{...,reason="budget"} by exactly 1 and does NOT increment tide_tasks_completed_total.
    - Test 3 (table test for metricFailureReason): ("cap-hit", 0) → "budget"; ("cap-hit", 1) → "budget"; ("output-paths-violation", 0) → "internal"; ("", 1) → "exit-1"; ("some-other-result", 2) → "exit-1"; ("", 0) → "internal" (defensive default — only reachable if the :905 predicate changes).
    - Test 4 (WR-04): emitTaskMetrics with completedAt BEFORE task.Status.StartedAt (e.g. StartedAt=now, completedAt=now-5m) emits all five counters but records ZERO histogram observations for that unique label set (assert via testutil.CollectAndCount delta or sample count == 0 for the labels); no panic.
    - Test 5 (WR-04 positive control): completedAt after StartedAt still observes the duration (existing end-to-end test keeps passing).
  </behavior>
  <action>
    All changes in task_controller.go; registry.go untouched.

    1. Add helper `metricFailureReason(envelopeResult string, exitCode int) string` returning ONLY values from the documented bounded enum: `"cap-hit"` → `"budget"`; `"output-paths-violation"` → `"internal"` (policy violation surfaced controller-side — not a CLI exit class); any other input with `exitCode != 0` → `"exit-1"`; default → `"internal"`. Do NOT reuse `conditionReasonFromEnvelopeResult` output as the label value — it capitalizes arbitrary envelope result strings and is unbounded (cardinality Pitfall 17 territory).
    2. Extend `emitTaskMetrics` with a trailing `failureReason string` parameter (empty string = Succeeded). After the existing six emissions: if `failureReason == ""` → `tidemetrics.TasksCompletedTotal.WithLabelValues(projectName, phase, plan).Inc()`; else → `tidemetrics.TasksFailedTotal.WithLabelValues(projectName, phase, plan, failureReason).Inc()`. Note the arity difference from the 16-02 metrics: these two counters carry NO wave label — pass exactly 3 (completed) / 4-with-reason (failed) values. This shares label resolution with the existing seams exactly as 16-REVIEW.md CR-02 prescribes, and inherits D-12's exactly-once guarantee (terminal status patch + gateChecks Succeeded/Failed short-circuit shields replay).
    3. Update the three call sites: seam 1 (:864, OutputValidationError) and seam 2 (:896, OutputPathsViolation) pass `"internal"`; seam 3 (:952) passes a `failureReason` variable computed in the :905 branch — `metricFailureReason(out.Result, out.ExitCode)` in the Failed arm, `""` in the Succeeded arm.
    4. Deliberate exclusion (document in a comment at the :819 branch or in the SUMMARY): the EnvelopeReadFailed early return performs no `budget.RollUpUsage` and no `emitTaskMetrics` — task counters keep strict parity with the D-12 budget-rollup commit points, matching CR-02's "the two early-return Failed branches" scope (seams 1 and 2). Widening to envelope-less failures is a deliberate non-goal of this gap closure.
    5. WR-04: replace the :1040-1043 block with a signed-duration guard — compute `d := completedAt.Sub(task.Status.StartedAt.Time)`; `Observe(d.Seconds())` only when `d >= 0`; on negative, log at `V(1)` via `logf.FromContext(ctx)` with task name and both timestamps (stale-envelope/clock-skew signal per review WR-04). Keep the existing `StartedAt != nil && !completedAt.IsZero()` outer guard.
    6. Update existing tests for the new signature (pass `""` where success semantics were assumed) and add the <behavior> tests using UNIQUE label values per test (16-02 pattern — Prometheus counters cannot be reset between tests).

    Write the new tests first where practical (Tests 2-4 fail to compile/pass against the unmodified code — record RED evidence), then implement.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/... && go test ./internal/controller/... -run 'TestEmitTaskMetrics|TestMetricFailureReason|TestResolveWave' -count=1 && go test ./internal/metrics/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TasksCompletedTotal.WithLabelValues' internal/controller/task_controller.go` returns 1
    - `grep -c 'TasksFailedTotal.WithLabelValues' internal/controller/task_controller.go` returns 1
    - `grep -c 'func metricFailureReason' internal/controller/task_controller.go` returns 1
    - `git diff --stat internal/metrics/registry.go` is empty (names/label sets/Help untouched)
    - metricFailureReason's return values are a subset of {exit-1, gitleaks, lease, auth, internal, budget}: `grep -A 20 'func metricFailureReason' internal/controller/task_controller.go | grep -oE '"(exit-1|gitleaks|lease|auth|internal|budget)"' | sort -u` covers every return literal in the function body
    - Negative-duration guard present: `grep -nE 'd >= 0|duration >= 0' internal/controller/task_controller.go` returns >= 1 inside emitTaskMetrics
    - `go test ./internal/controller/... -run 'TestEmitTaskMetrics|TestMetricFailureReason' -count=1 -v` exits 0 and the output names all five <behavior> scenarios
    - `go build ./...` and `go vet ./internal/controller/...` exit 0
  </acceptance_criteria>
  <done>Failure Rate panel's two metric families emit at every envelope-bearing terminal commit with bounded reasons; Succeeded tasks increment the completed counter; histogram can never go negative; D-12 exactly-once parity with budget rollup preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit WavesDispatchedTotal at the materializeWaves Create commit point (CR-02 wave counter)</name>
  <files>internal/controller/plan_controller.go, internal/controller/plan_controller_metrics_test.go</files>
  <read_first>
    - internal/controller/plan_controller.go (:1292-1337 materializeWaves; :1381-1398 resolveProjectName; :1045 caller context)
    - internal/controller/task_controller_metrics_test.go (fake-client + testutil + unique-label-set pattern to replicate)
    - internal/metrics/registry.go (:40-46, :126-132 — WavesDispatchedTotal {project, phase, plan}, wave name intentionally elided)
  </read_first>
  <behavior>
    - Test 1: materializeWaves with a Plan (PhaseRef set, project label `tideproject.k8s/project` stamped for the fast-path) and 2 layers against a fake client containing no Waves → creates 2 Waves AND tide_waves_dispatched_total{project,phase,plan} == 2 (use unique label values, assert via testutil.ToFloat64).
    - Test 2 (idempotency): calling materializeWaves a second time with the same inputs (Waves now exist in the fake client → Get hits the exists-branch) leaves the counter at 2 — no double count on reconcile replay.
    - Test 3 (sentinel): a Plan with empty PhaseRef and no resolvable Project increments the counter with phase="unknown" and project="unknown" — never an empty label value (Metric Label Sentinel, RESEARCH Pitfall 4 — same rule 16-02 followed).
  </behavior>
  <action>
    In `materializeWaves` (plan_controller.go:1295):

    1. Before the layer loop, resolve labels once: `projectName, err := r.resolveProjectName(ctx, plan)` → on error use `"unknown"` (non-fatal — metrics emission must never fail wave materialization); `phaseName := plan.Spec.PhaseRef` → `"unknown"` when empty.
    2. Increment `tidemetrics.WavesDispatchedTotal.WithLabelValues(projectName, phaseName, plan.Name).Inc()` ONLY on the `r.Create(ctx, wave)` success path (`err == nil`) — NOT on the AlreadyExists tolerance branch (:1321-1324, the watch-lag race: the reconcile whose Create succeeded already counted it) and NOT on the Wave-exists branch (:1327-1334). Create success is the once-only dispatch commit point — reconcile replays Get the existing Wave and never re-count, mirroring D-12's exactly-once shape. Place the Inc() adjacent to the existing `logger.Info("created wave", ...)` line.
    3. Import `tidemetrics "github.com/jsquirrelz/tide/internal/metrics"` matching task_controller.go's import alias.
    4. New file `internal/controller/plan_controller_metrics_test.go` with the <behavior> tests, replicating the 16-02 fake-client pattern (scheme with tide types registered, unique label values per test). Write tests first, observe RED (counter stays 0 against unmodified materializeWaves), then implement.

    Do NOT emit from WaveReconciler (observational only, D-B2/D-B4) and do NOT add a wave-name label (registry doc :43-45: wave count rolls up to the parent Plan by design; label set is fixed).
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/... && go test ./internal/controller/... -run 'TestMaterializeWaves' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WavesDispatchedTotal.WithLabelValues' internal/controller/plan_controller.go` returns 1
    - The Inc() site is inside the Create-success path: `grep -B 8 'WavesDispatchedTotal' internal/controller/plan_controller.go` shows it within the NotFound/Create branch of materializeWaves (not the exists-branch)
    - `grep -c 'WavesDispatchedTotal' internal/controller/wave_controller.go` returns 0 (WaveReconciler stays observational)
    - `git diff --stat internal/metrics/registry.go` is empty
    - `go test ./internal/controller/... -run 'TestMaterializeWaves' -count=1 -v` exits 0 covering create-once, replay-idempotency, and unknown-sentinel scenarios
    - `go test ./internal/controller/... -count=1` exits 0 (no regression in the package)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Every Wave CR creation increments tide_waves_dispatched_total exactly once with {project, phase, plan}; reconcile replays and watch-lag races never double-count; the Dispatch Counts panel's wave series goes live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Envelope (PVC out.json) → metric label values | `out.Result`/`out.ExitCode` come from the subagent pod's envelope — attacker-influenceable text |
| CRD names → metric label values | project/phase/plan label values are K8s resource names (apiserver-validated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-21 | Tampering (cardinality bomb) | TasksFailedTotal reason label | mitigate | metricFailureReason maps arbitrary envelope results onto the closed six-value enum — envelope free-text never becomes a label value (explicitly forbidden: reusing conditionReasonFromEnvelopeResult output) |
| T-16-22 | DoS (metric corruption) | TaskDurationSeconds _sum | mitigate | d >= 0 guard skips stale-envelope/clock-skew observations with a V(1) log signal (WR-04) |
| T-16-23 | Repudiation (over/under-count) | WavesDispatchedTotal | mitigate | Inc only on Create success — AlreadyExists race and reconcile replay paths never count; parity test pins it |
| T-16-SC | Tampering | package installs | accept | No new dependencies in this plan |
</threat_model>

<verification>
- All new/updated tests green: `go test ./internal/controller/... ./internal/metrics/... -count=1`
- `go build ./...` + `go vet ./internal/controller/...` clean
- registry.go diff empty — metric names, label sets, Help strings untouched
- 16-VERIFICATION.md Data-Flow Trace row "Dispatch Counts, Failure Rate panels ✗ STATIC" now satisfiable: all three families have production Inc() sites
</verification>

<success_criteria>
- CR-02 closed: zero registered-but-never-emitted metric families queried by TelemetryView panels (`grep -rn "WavesDispatchedTotal\|TasksCompletedTotal\|TasksFailedTotal" internal/controller/ --include='*.go' | grep -v _test` shows production call sites)
- WR-04 closed: negative durations skipped and logged, pinned by test
- Bounded reason enum preserved exactly as documented in registry.go
</success_criteria>

<output>
Create `.planning/phases/16-telemetry-completion/16-07-SUMMARY.md` when done
</output>
