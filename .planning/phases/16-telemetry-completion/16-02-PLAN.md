---
phase: 16-telemetry-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/metrics/registry.go
  - internal/metrics/registry_test.go
  - internal/controller/task_controller.go
  - internal/controller/task_controller_metrics_test.go
autonomous: true
requirements: [TELEM-03]
must_haves:
  truths:
    - "A Task reaching ANY terminal branch (success or failure) emits tokens, cost, and duration metrics at the exact commit point where budget.RollUpUsage runs — Prometheus totals and Budget accounting cannot diverge"
    - "All six locked metrics carry the {project, phase, plan, wave} label set; no metric carries a task label"
    - "tide_task_duration_seconds uses minutes-scale buckets (30s–2h), not the useless 10s-capped Prometheus defaults"
    - "The wave label value is the owning Wave CRD name from the Task's OwnerReferences; unresolvable labels fall back to the 'unknown' sentinel, never empty string"
  artifacts:
    - path: "internal/metrics/registry.go"
      provides: "Six locked metric registrations: tide_tokens_input_total, tide_tokens_output_total, tide_tokens_cache_read_total, tide_tokens_cache_creation_total, tide_cost_cents_total, tide_task_duration_seconds"
      contains: "tide_task_duration_seconds"
    - path: "internal/controller/task_controller.go"
      provides: "resolveWave + emitTaskMetrics helpers and three emission call sites piggybacking the RollUpUsage seams"
      contains: "emitTaskMetrics"
  key_links:
    - from: "internal/controller/task_controller.go"
      to: "internal/metrics registry vars"
      via: "tidemetrics import + WithLabelValues at the three RollUpUsage seams"
      pattern: "TokensInputTotal\\.WithLabelValues"
    - from: "internal/controller/task_controller.go resolveWave"
      to: "Wave CRD name"
      via: "Task OwnerReferences Kind==Wave"
      pattern: "ref\\.Kind == \"Wave\""
---

<objective>
Close TELEM-03: register the six locked metrics in `internal/metrics/registry.go` and emit them from all three TaskReconciler terminal branches at the `budget.RollUpUsage` commit points, with the `{project, phase, plan, wave}` label set per the MILESTONE.md table (49e93cb — locked, do NOT re-derive names).

Purpose: the Cost Over Time and Token Breakdown panels, and the entire cost-accounting story, are dead until these metrics exist and emit.
Output: six registered metric families, `resolveWave` + phase resolution + `emitTaskMetrics` helpers, three emission call sites, unit tests for registration arity, buckets, resolution, and emission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-telemetry-completion/16-CONTEXT.md
@.planning/phases/16-telemetry-completion/16-PATTERNS.md

**PLANNER CORRECTION to 16-PATTERNS.md (verified by grep during planning):** the pattern map's `emitTaskMetrics` sketch reads `task.Labels["tideproject.k8s/phase"]` — that label DOES NOT EXIST anywhere in the codebase (Tasks carry only `tideproject.k8s/project` and `tideproject.k8s/wave-paused`). It would silently emit `phase=""` on every series. Resolve phase via the spec-ref chain instead: `task.Spec.PlanRef` names the Plan (api/v1alpha1/task_types.go:70-72); a cached `r.Get` of that Plan yields `plan.Spec.PhaseRef` (api/v1alpha1/plan_types.go:25-27) which IS the Phase name. On any miss, use the `"unknown"` sentinel (PATTERNS § Metric Label Sentinel). Everything else in the PATTERNS map is verified-correct — follow it.

Other verified facts:
- The three `budget.RollUpUsage` terminal call sites are task_controller.go:857 (output-validation error path), :883 (output-paths-violation path), :932 (standard terminal path). D-12: emission piggybacks each, immediately after the RollUpUsage call, non-fatal.
- `out.Usage` fields (pkg/dispatch/envelope.go:252-282): `InputTokens`, `OutputTokens`, `CacheReadTokens`, `CacheCreationTokens`, `EstimatedCostCents` — all int64. `out.CompletedAt` is a time.Time; `task.Status.StartedAt` is `*metav1.Time`.
- The `metriccardinality` analyzer forbids only the literal `"task"` label; `"wave"` and `"plan"` are permitted (tools/analyzers/metriccardinality/analyzer.go, verified).
- `internal/controller/billing_halt_test.go` shows the package's plain-go-test + `fake.NewClientBuilder().WithScheme(s)` convention — use it for the new test file (no envtest needed).
- Existing 7 metrics live in one `init()` with ONE `metrics.Registry.MustRegister(...)` call — extend that call, never add a second (double-register panics, RESEARCH Pitfall 3).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the six locked metrics with {project, phase, plan, wave} labels and D-11 buckets</name>
  <files>internal/metrics/registry.go, internal/metrics/registry_test.go</files>
  <read_first>
    - internal/metrics/registry.go (the var + init() + single MustRegister pattern for the existing 7 metrics; the `dispatchLatencyBuckets` var at ~line 33 the new bucket slice sits beside)
    - internal/metrics/registry_test.go (the `__seed__` sentinel seeding, AllMetricFamiliesPresent, per-metric arity tests, bucket source check at ~lines 54-141, and the NoTaskLabel substring test at ~line 172)
    - .planning/phases/16-telemetry-completion/16-PATTERNS.md (§ internal/metrics/registry.go — complete var names, metric names, Help strings, and registration block)
    - docs/observability.md (§ "Token, cost, and duration metrics" — the locked table this implements; read-only, confirm names match)
  </read_first>
  <action>
    Extend `internal/metrics/registry.go` per the PATTERNS map (D-06/D-10/D-11 locked):

    1. Add `var taskDurationBuckets = []float64{30, 60, 120, 300, 600, 1200, 1800, 3600, 7200}` beside `dispatchLatencyBuckets`, with a doc comment citing D-11 (Prometheus defaults top out at 10s — useless for agentic tasks).
    2. Declare six exported vars: `TokensInputTotal`, `TokensOutputTotal`, `TokensCacheReadTotal`, `TokensCacheCreationTotal`, `CostCentsTotal` (all `*prometheus.CounterVec`) and `TaskDurationSeconds` (`*prometheus.HistogramVec`).
    3. In the existing `init()`, construct them with metric names `tide_tokens_input_total`, `tide_tokens_output_total`, `tide_tokens_cache_read_total`, `tide_tokens_cache_creation_total`, `tide_cost_cents_total`, `tide_task_duration_seconds` — label slice `[]string{"project", "phase", "plan", "wave"}` on ALL six (D-10). `TaskDurationSeconds` gets `Buckets: taskDurationBuckets`. Help strings per the PATTERNS map (cite Phase 16 TELEM-03).
    4. Append all six to the EXISTING single `metrics.Registry.MustRegister(...)` call — do not add a second call.

    Extend `internal/metrics/registry_test.go` following its existing structure:
    - Seed each new metric with the `__seed__` sentinel labels (4 values now) so the AllMetricFamiliesPresent-style test sees all six new families; extend that test's expected-family list with the six locked names.
    - Add per-metric arity tests calling `WithLabelValues("p", "ph", "pl", "w")` (4 args) for each of the six — a 3-arg call must not compile/pass.
    - Add a `taskDurationBuckets` source check mirroring the existing DispatchLatencyBuckets test: assert the slice equals `{30, 60, 120, 300, 600, 1200, 1800, 3600, 7200}`.
    - Confirm the existing NoTaskLabel test still passes (it greps for the literal label string `"task"` — the new `"wave"` and `"plan"` labels are safe; do not weaken the test).
  </action>
  <verify>
    <automated>go test ./internal/metrics/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tide_tokens_input_total\|tide_tokens_output_total\|tide_tokens_cache_read_total\|tide_tokens_cache_creation_total\|tide_cost_cents_total\|tide_task_duration_seconds' internal/metrics/registry.go` returns 6
    - `grep -c 'MustRegister' internal/metrics/registry.go` returns 1 (single call, extended)
    - `grep -cE '"project", "phase", "plan", "wave"' internal/metrics/registry.go` returns 6
    - `go test ./internal/metrics/... -count=1` exits 0 including the unmodified NoTaskLabel test
  </acceptance_criteria>
  <done>Six locked metric families registered with 4-label arity and D-11 buckets; registry tests cover presence, arity, and bucket boundaries; analyzer-relevant NoTaskLabel test green.</done>
</task>

<task type="auto">
  <name>Task 2: Emit the six metrics at the three RollUpUsage terminal seams with resolveWave + phase resolution</name>
  <files>internal/controller/task_controller.go, internal/controller/task_controller_metrics_test.go</files>
  <read_first>
    - internal/controller/task_controller.go (the three RollUpUsage sites at :857/:883/:932; `resolveProject` + `walkOwnerChainToProject` at :948-1002 — the pattern resolveWave mirrors; the `setBudgetBlockedIfNeeded` non-fatal call shape at :941-943)
    - internal/controller/billing_halt_test.go (the package's plain-test + fake.NewClientBuilder convention for the new test file)
    - api/v1alpha1/task_types.go (:70-72 `Spec.PlanRef`, :129-133 `Status.StartedAt`)
    - api/v1alpha1/plan_types.go (:25-27 `Spec.PhaseRef` — the phase resolution source)
    - pkg/dispatch/envelope.go (:252-282 Usage struct field names and types)
    - .planning/phases/16-telemetry-completion/16-PATTERNS.md (§ internal/controller/task_controller.go — but NOTE the planner correction in this plan's context: phase comes from PlanRef→PhaseRef, NOT from a tideproject.k8s/phase label, which does not exist)
  </read_first>
  <action>
    Add two helpers and three call sites to `task_controller.go` (D-08/D-09/D-12 locked):

    1. `resolveWave(task *tideprojectv1alpha1.Task) string` — iterate `task.GetOwnerReferences()`; first ref with `Kind == "Wave"` returns `ref.Name` (the Wave CRD name IS the label value, D-09); no hit returns `"unknown"` (sentinel, never empty — RESEARCH Pitfall 4). Pure in-memory, no ctx/API call. Doc comment cites D-09 and the wave_controller SetControllerReference provenance.
    2. `emitTaskMetrics(ctx context.Context, task *tideprojectv1alpha1.Task, project *tideprojectv1alpha1.Project, usage dispatch.Usage, completedAt time.Time)` — resolves the four label values then emits all six metrics:
       - `project` = `project.Name`
       - `plan` = `task.Spec.PlanRef` (fallback `"unknown"` if empty)
       - `phase` = fetch the Plan via `r.Get(ctx, client.ObjectKey{Namespace: task.Namespace, Name: task.Spec.PlanRef}, &plan)` and use `plan.Spec.PhaseRef`; on Get error or empty PhaseRef use `"unknown"` (PLANNER CORRECTION — there is no phase label on Tasks)
       - `wave` = `resolveWave(task)`
       Emit with `tidemetrics.TokensInputTotal.WithLabelValues(project, phase, plan, wave).Add(float64(usage.InputTokens))` and likewise for `TokensOutputTotal`/`OutputTokens`, `TokensCacheReadTotal`/`CacheReadTokens`, `TokensCacheCreationTotal`/`CacheCreationTokens`, `CostCentsTotal`/`EstimatedCostCents`. For duration: only when `task.Status.StartedAt != nil && !completedAt.IsZero()`, `TaskDurationSeconds.WithLabelValues(...).Observe(completedAt.Sub(task.Status.StartedAt.Time).Seconds())`. Return nothing or an error per the non-fatal logging shape — match `setBudgetBlockedIfNeeded`'s caller pattern.
    3. Insert the emission call immediately AFTER each of the three `budget.RollUpUsage` blocks (:857, :883, :932) — same once-only terminal commit point, success AND failure branches (D-08/D-12: Prometheus cost totals must never diverge from Budget accounting). Use `out.CompletedAt` as the completedAt argument at all three sites. Non-fatal: log via `logger.Error(..., "(non-fatal)", "task", task.Name)` and proceed — the task is already terminal.
    4. Import `tidemetrics "github.com/jsquirrelz/tide/internal/metrics"` (match the alias used in project_controller.go).

    New test file `internal/controller/task_controller_metrics_test.go` (plain go test + fake client per billing_halt_test.go):
    - `resolveWave`: Task with OwnerRef Kind "Wave" Name "tide-wave-abc-0" → returns "tide-wave-abc-0"; Task with no Wave owner → returns "unknown".
    - `emitTaskMetrics` end-to-end: build a fake client seeded with a Plan (Name "plan-a", `Spec.PhaseRef: "phase-a"`); a Task (Namespace "default", `Spec.PlanRef: "plan-a"`, Wave OwnerRef "tide-wave-x-0", `Status.StartedAt` set 90s before completedAt); a Project "proj-a". Call emitTaskMetrics with Usage{InputTokens: 100, OutputTokens: 50, CacheReadTokens: 10, CacheCreationTokens: 5, EstimatedCostCents: 42}. Assert via `prometheus/client_golang/prometheus/testutil`: `testutil.ToFloat64(tidemetrics.TokensInputTotal.WithLabelValues("proj-a", "phase-a", "plan-a", "tide-wave-x-0")) == 100` (and the other four counters at their values). For the histogram, gather via `testutil.CollectAndCount(tidemetrics.TaskDurationSeconds)` >= 1 and/or inspect the metric DTO for one observation with sample sum ≈ 90. Use unique label values per test (the registry is global) to avoid cross-test interference.
    - Phase-miss sentinel: Task whose PlanRef names a nonexistent Plan → emission lands under `phase="unknown"` (assert the counter exists with that label value).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'TestResolveWave|TestEmitTaskMetrics' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "emitTaskMetrics" internal/controller/task_controller.go` returns 4 (one definition + three call sites)
    - `grep -cE 'ref\.Kind == "Wave"' internal/controller/task_controller.go` returns 1
    - `grep -c 'tideproject.k8s/phase' internal/controller/task_controller.go` returns 0 (the nonexistent label is NOT used)
    - `go test ./internal/controller/... -run 'TestResolveWave|TestEmitTaskMetrics' -count=1` exits 0
    - `go test ./internal/metrics/... ./internal/controller/... -count=1` exits 0 (no regressions in either package)
    - `go vet ./internal/...` exits 0
  </acceptance_criteria>
  <done>All three terminal branches emit the six metrics with correct {project, phase, plan, wave} values at the RollUpUsage commit point; wave resolves from OwnerRef, phase from PlanRef→PhaseRef; misses use the "unknown" sentinel; unit tests prove values and labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor envelope → reconciler | `out.Usage` values originate from the subagent container's envelope (semi-trusted) |
| metrics endpoint → scraper | Label values become visible to anything that can scrape /metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | metric label values | mitigate | Label values are exclusively K8s resource names (Project/Phase/Plan/Wave CRD names) — never envelope free-text, never secrets, never prompt content. emitTaskMetrics constructs labels only from CR names and the "unknown" sentinel. |
| T-16-06 | Denial of Service | series cardinality | mitigate | Locked label set {project, phase, plan, wave}; per-task labels forbidden and enforced by the metriccardinality analyzer; wave ⊂ plan so series count matches the approved Plan roll-up budget (D-10, docs/observability.md cardinality table). |
| T-16-07 | Tampering | usage values from envelope | accept | Same trust level as the existing budget.RollUpUsage path — the envelope already drives Budget accounting; metrics add no new trust exposure (D-12 piggybacks the identical values). |
| T-16-SC | Tampering | package installs | accept | Zero new packages (prometheus/client_golang already pinned). |
</threat_model>

<verification>
- `go build ./...` exits 0
- `go test ./internal/metrics/... ./internal/controller/... -count=1` exits 0
- `go vet ./internal/...` exits 0 (metriccardinality-adjacent hygiene)
- Metric names exactly match the MILESTONE.md locked table — `grep` each of the six names in internal/metrics/registry.go
</verification>

<success_criteria>
- TELEM-03: six locked metrics registered and emitted from ALL terminal branches at the RollUpUsage seams with {project, phase, plan, wave}; D-11 buckets on the histogram; exactly-once semantics inherited from the rollup commit point; regression tests for resolution, labels, and values
</success_criteria>

<output>
Create `.planning/phases/16-telemetry-completion/16-02-SUMMARY.md` when done
</output>
