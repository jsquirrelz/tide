---
phase: 01-foundation-crds-pkg-dag-controller-scaffold
plan: 10
type: execute
wave: 3
depends_on: [05]
files_modified:
  - config/samples/kustomization.yaml
  - config/samples/tide_v1alpha1_project.yaml
  - config/samples/tide_v1alpha1_milestone.yaml
  - config/samples/tide_v1alpha1_phase.yaml
  - config/samples/tide_v1alpha1_plan.yaml
  - config/samples/tide_v1alpha1_task_alpha.yaml
  - config/samples/tide_v1alpha1_task_beta.yaml
  - config/samples/tide_v1alpha1_task_gamma.yaml
  - config/samples/tide_v1alpha1_task_delta.yaml
  - config/samples/tide_v1alpha1_task_epsilon.yaml
  - config/samples/tide_v1alpha1_task_zeta.yaml
  - config/samples/tide_v1alpha1_task_eta.yaml
  - config/samples/tide_v1alpha1_task_theta.yaml
  - config/samples/namespace.yaml
autonomous: true
requirements:
  - CRD-01
must_haves:
  truths:
    - "config/samples/ contains a hand-authored worked-example set: one Project, one Milestone, one Phase, one Plan, eight Tasks named alpha through theta (D-G1)"
    - "Task dependsOn edges exactly match the spec: alpha→delta, beta→delta, gamma→eta, zeta→eta, delta→epsilon, eta→theta (DAG-04 regression fixture parity)"
    - "Each Task declares non-empty filesTouched per D-F2 (e.g., pkg/example/<name>.go)"
    - "config/samples/kustomization.yaml orders the resources so kubectl apply -k respects owner-ref dependencies (Project → Milestone → Phase → Plan → Tasks)"
    - "kubectl apply -k config/samples/ against an envtest cluster accepts all CRDs with CEL passing (verified in suite_test.go)"
    - "A reader greppingfor 'alpha' across the repo finds: pkg/dag/kahn_test.go (TestComputeWaves_AlphaThroughTheta), config/samples/tide_v1alpha1_task_alpha.yaml, README.md (spec worked example)"
  artifacts:
    - path: "config/samples/kustomization.yaml"
      provides: "Ordered apply of all sample CRDs"
      contains: "resources"
    - path: "config/samples/tide_v1alpha1_task_alpha.yaml through tide_v1alpha1_task_theta.yaml"
      provides: "Eight Task CRDs with dependsOn matching the α…θ spec"
  key_links:
    - from: "config/samples/tide_v1alpha1_task_<name>.yaml"
      to: "pkg/dag/kahn_test.go TestComputeWaves_AlphaThroughTheta fixture"
      via: "shared task names + edges"
      pattern: "alpha|beta|gamma|delta|epsilon|zeta|eta|theta"
---

<objective>
Author the α…θ worked-example sample CRDs that prove the Phase 1 surface works end-to-end against envtest, AND that match the pkg/dag regression fixture name-for-name. The same eight task names that pin the Kahn algorithm test (Plan 02) also pin the sample CRD set — making the α…θ example the spine of the phase's verification story.

Purpose: D-G1 commits Phase 1 to a hand-authored worked-example set under config/samples/. This is the apply-time verification surface — the envtest tests in Plan 06 use these samples; Plan 11's Helm chart references them; CRD-01's "developer can kubectl apply a sample" success criterion is satisfied by this directory.

Output: 12 YAML files under config/samples/ (Project, Milestone, Phase, Plan, 8 Tasks, namespace) + kustomization.yaml ordering them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-CONTEXT.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-VALIDATION.md
@README.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-02-SUMMARY.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-05-SUMMARY.md

<interfaces>
01-RESEARCH.md "config/samples/ α…θ Task fixture (D-G1)" (lines ~1651-1676) has the sample Task YAML shape. README.md has the spec's worked example.

α…θ edge set (must match pkg/dag fixture exactly):
- alpha:   dependsOn: []                (Wave 1)
- beta:    dependsOn: []                (Wave 1)
- gamma:   dependsOn: []                (Wave 1)
- zeta:    dependsOn: []                (Wave 1)
- delta:   dependsOn: [alpha, beta]     (Wave 2)
- eta:     dependsOn: [gamma, zeta]     (Wave 2)
- epsilon: dependsOn: [delta]           (Wave 3)
- theta:   dependsOn: [eta]             (Wave 3)

Expected waves: [{alpha, beta, gamma, zeta}, {delta, eta}, {epsilon, theta}]

Namespace: tide-samples (a dedicated namespace so the samples don't pollute the default namespace in tests)

Per D-G2: file names follow `tide_v1alpha1_<kind>[_<name>].yaml` convention.

kustomization.yaml resource ordering: namespace.yaml first (creates tide-samples namespace), then Project, then Milestone (refs Project), then Phase (refs Milestone), then Plan (refs Phase), then eight Tasks (each refs Plan). kustomize doesn't enforce strict order on apply but listing resources top-down approximates owner-ref dependency order.

PER-TASK filesTouched (D-F2 requires MinItems=1):
- alpha:   pkg/example/alpha.go
- beta:    pkg/example/beta.go
- gamma:   pkg/example/gamma.go
- zeta:    pkg/example/zeta.go
- delta:   pkg/example/delta.go
- eta:     pkg/example/eta.go
- epsilon: pkg/example/epsilon.go
- theta:   pkg/example/theta.go
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author all 12 sample YAML files + kustomization.yaml</name>
  <files>config/samples/namespace.yaml, config/samples/kustomization.yaml, config/samples/tide_v1alpha1_project.yaml, config/samples/tide_v1alpha1_milestone.yaml, config/samples/tide_v1alpha1_phase.yaml, config/samples/tide_v1alpha1_plan.yaml, config/samples/tide_v1alpha1_task_alpha.yaml, config/samples/tide_v1alpha1_task_beta.yaml, config/samples/tide_v1alpha1_task_gamma.yaml, config/samples/tide_v1alpha1_task_delta.yaml, config/samples/tide_v1alpha1_task_epsilon.yaml, config/samples/tide_v1alpha1_task_zeta.yaml, config/samples/tide_v1alpha1_task_eta.yaml, config/samples/tide_v1alpha1_task_theta.yaml</files>
  <read_first>
    - api/v1alpha1/project_types.go (Plan 05 — Spec field shape to author against)
    - api/v1alpha1/task_types.go (Plan 05 — DependsOn + FilesTouched shape)
    - api/v1alpha1/wave_types.go (Plan 05 — note: Wave is NOT authored as a sample because D-B1 forbids client-applied Waves)
    - pkg/dag/kahn_test.go (Plan 02 — verify the eight Task names match exactly: alpha, beta, gamma, delta, epsilon, zeta, eta, theta)
    - README.md (spec's α…θ worked example for sanity check)
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md "config/samples/ α…θ Task fixture" (lines ~1651-1676)
  </read_first>
  <action>
Create the 14 YAML files (12 CRDs + namespace + kustomization).

config/samples/namespace.yaml:
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: tide-samples
  labels:
    tide.io/sample: "true"
```

config/samples/tide_v1alpha1_project.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Project
metadata:
  name: sample-project
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
spec:
  targetRepo: https://github.com/jsquirrelz/tide.git
```

config/samples/tide_v1alpha1_milestone.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Milestone
metadata:
  name: sample-milestone
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
spec:
  projectRef: sample-project
```

config/samples/tide_v1alpha1_phase.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Phase
metadata:
  name: sample-phase
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
spec:
  milestoneRef: sample-milestone
```

config/samples/tide_v1alpha1_plan.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Plan
metadata:
  name: sample-plan
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
spec:
  phaseRef: sample-phase
```

Eight Task files. config/samples/tide_v1alpha1_task_alpha.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Task
metadata:
  name: alpha
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
    tide.io/plan: sample-plan
spec:
  planRef: sample-plan
  filesTouched:
    - pkg/example/alpha.go
```

config/samples/tide_v1alpha1_task_beta.yaml (identical shape, name=beta, filesTouched=beta.go):
```yaml
apiVersion: tide.io/v1alpha1
kind: Task
metadata:
  name: beta
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
    tide.io/plan: sample-plan
spec:
  planRef: sample-plan
  filesTouched:
    - pkg/example/beta.go
```

config/samples/tide_v1alpha1_task_gamma.yaml — same shape, name=gamma, filesTouched=gamma.go, no dependsOn

config/samples/tide_v1alpha1_task_zeta.yaml — same shape, name=zeta, filesTouched=zeta.go, no dependsOn

config/samples/tide_v1alpha1_task_delta.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Task
metadata:
  name: delta
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
    tide.io/plan: sample-plan
spec:
  planRef: sample-plan
  dependsOn:
    - alpha
    - beta
  filesTouched:
    - pkg/example/delta.go
```

config/samples/tide_v1alpha1_task_eta.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Task
metadata:
  name: eta
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
    tide.io/plan: sample-plan
spec:
  planRef: sample-plan
  dependsOn:
    - gamma
    - zeta
  filesTouched:
    - pkg/example/eta.go
```

config/samples/tide_v1alpha1_task_epsilon.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Task
metadata:
  name: epsilon
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
    tide.io/plan: sample-plan
spec:
  planRef: sample-plan
  dependsOn:
    - delta
  filesTouched:
    - pkg/example/epsilon.go
```

config/samples/tide_v1alpha1_task_theta.yaml:
```yaml
apiVersion: tide.io/v1alpha1
kind: Task
metadata:
  name: theta
  namespace: tide-samples
  labels:
    tide.io/sample: "true"
    tide.io/plan: sample-plan
spec:
  planRef: sample-plan
  dependsOn:
    - eta
  filesTouched:
    - pkg/example/theta.go
```

config/samples/kustomization.yaml (orders the apply for owner-ref dependency):
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - tide_v1alpha1_project.yaml
  - tide_v1alpha1_milestone.yaml
  - tide_v1alpha1_phase.yaml
  - tide_v1alpha1_plan.yaml
  - tide_v1alpha1_task_alpha.yaml
  - tide_v1alpha1_task_beta.yaml
  - tide_v1alpha1_task_gamma.yaml
  - tide_v1alpha1_task_zeta.yaml
  - tide_v1alpha1_task_delta.yaml
  - tide_v1alpha1_task_eta.yaml
  - tide_v1alpha1_task_epsilon.yaml
  - tide_v1alpha1_task_theta.yaml
```

Note: kubebuilder may have already scaffolded the base config/samples/kustomization.yaml — replace with the version above.

Verify the YAML is well-formed:
```
kubectl apply -k config/samples/ --dry-run=client
```
This should print the rendered manifests without errors.

If yq is available, sanity-check the dependsOn edges:
```
for f in config/samples/tide_v1alpha1_task_*.yaml; do
  yq '.metadata.name + " -> " + (.spec.dependsOn // [] | join(","))' "$f"
done
```

Expected output:
```
alpha -> 
beta -> 
gamma -> 
zeta -> 
delta -> alpha,beta
eta -> gamma,zeta
epsilon -> delta
theta -> eta
```
  </action>
  <verify>
    <automated>test -f config/samples/kustomization.yaml &amp;&amp; test -f config/samples/tide_v1alpha1_project.yaml &amp;&amp; test -f config/samples/tide_v1alpha1_task_theta.yaml &amp;&amp; kubectl apply -k config/samples/ --dry-run=client 2>/dev/null || kustomize build config/samples/</automated>
  </verify>
  <acceptance_criteria>
    - All 14 expected files exist: `ls config/samples/ | wc -l` returns at least 14 (8 tasks + project + milestone + phase + plan + namespace + kustomization)
    - Eight task files exist: `ls config/samples/tide_v1alpha1_task_*.yaml | wc -l` returns 8
    - Task names cover α through θ: for each name in {alpha,beta,gamma,delta,epsilon,zeta,eta,theta}, `grep -q "name: $name" config/samples/tide_v1alpha1_task_$name.yaml` exits 0
    - Each Task has filesTouched: `grep -c "filesTouched:" config/samples/tide_v1alpha1_task_*.yaml` returns at least 8
    - Wave-1 tasks have no dependsOn: alpha, beta, gamma, zeta — `! grep -q "dependsOn" config/samples/tide_v1alpha1_task_alpha.yaml` exits 0 (same for beta, gamma, zeta)
    - delta depends on [alpha, beta]: `grep -A 2 "dependsOn" config/samples/tide_v1alpha1_task_delta.yaml | grep -q "alpha" && grep -A 2 "dependsOn" config/samples/tide_v1alpha1_task_delta.yaml | grep -q "beta"` exits 0
    - eta depends on [gamma, zeta]: same check for gamma and zeta in eta's yaml
    - epsilon depends on [delta]: `grep -A 1 "dependsOn" config/samples/tide_v1alpha1_task_epsilon.yaml | grep -q "delta"` exits 0
    - theta depends on [eta]: `grep -A 1 "dependsOn" config/samples/tide_v1alpha1_task_theta.yaml | grep -q "eta"` exits 0
    - No Wave sample file exists (D-B1 — Waves are NEVER client-applied): `! ls config/samples/tide_v1alpha1_wave*.yaml 2>/dev/null` exits 0
    - kustomization.yaml orders resources: `head -5 config/samples/kustomization.yaml | grep -q "resources:"` exits 0
    - `kubectl apply -k config/samples/ --dry-run=client 2>/dev/null` exits 0 OR `kustomize build config/samples/` exits 0 (yaml is valid)
  </acceptance_criteria>
  <done>All 12 sample CRD YAML files exist + namespace + kustomization.yaml. Task dependsOn edges match the α…θ spec exactly. No Wave sample (D-B1). The α…θ name spine now ties together pkg/dag/kahn_test.go (Plan 02), config/samples/ (this plan), and README.md.</done>
</task>

</tasks>

<verification>
- 8 Task YAML files exist with names alpha through theta
- dependsOn edges match: alpha→delta, beta→delta, gamma→eta, zeta→eta, delta→epsilon, eta→theta
- Every Task has filesTouched with at least one path
- kustomization.yaml renders cleanly via kustomize build
- No client-applied Wave samples (D-B1 preserved)
</verification>

<success_criteria>
- CRD-01: developer can `kubectl apply -k config/samples/` against the envtest cluster (Plan 06's tests do this)
- The α…θ name spine ties algorithm spec (README), algorithm test (pkg/dag/kahn_test.go), and integration sample (config/samples/) together
</success_criteria>

<output>
Create .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-10-SUMMARY.md with:
- File inventory (14 YAMLs in config/samples/)
- α…θ edge table reproduced
- kustomization.yaml ordering rationale
- Phase 2 hand-off: the same samples drive Phase 2's integration tests. WaveReconciler's Phase 2 wiring will derive the wave structure from these Tasks via pkg/dag.ComputeWaves and create three Wave resources (one per layer). Phase 2 must not modify these sample files — they are the apply-time fixture across both phases.
</output>
