---
phase: 01-foundation-crds-pkg-dag-controller-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - PROJECT
  - Makefile
  - Dockerfile
  - api/v1alpha1/groupversion_info.go
  - api/v1alpha1/project_types.go
  - api/v1alpha1/milestone_types.go
  - api/v1alpha1/phase_types.go
  - api/v1alpha1/plan_types.go
  - api/v1alpha1/task_types.go
  - api/v1alpha1/wave_types.go
  - api/v1alpha1/zz_generated.deepcopy.go
  - internal/controller/project_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/controller/wave_controller.go
  - internal/controller/suite_test.go
  - internal/webhook/v1alpha1/plan_webhook.go
  - internal/webhook/v1alpha1/wave_webhook.go
  - config/crd/bases/
  - config/rbac/
  - config/webhook/
  - config/manager/
  - config/default/
  - cmd/main.go
  - hack/boilerplate.go.txt
autonomous: true
requirements:
  - CRD-05
  - BOOT-01
  - BOOT-03
must_haves:
  truths:
    - "Go module `github.com/jsquirrelz/tide` exists with go 1.26 toolchain"
    - "kubebuilder v4.14 has scaffolded six v1alpha1 CRDs (Project, Milestone, Phase, Plan, Task, Wave) under api/v1alpha1/"
    - "kubebuilder has scaffolded controller skeletons for all six Kinds under internal/controller/"
    - "kubebuilder has scaffolded webhooks for Plan (validating + conversion) and Wave (validating only) under internal/webhook/v1alpha1/"
    - "config/ Kustomize directory exists with crd/bases/, rbac/, webhook/, manager/, default/ subdirectories"
    - "All six api/v1alpha1/*_types.go files declare the v1alpha1 group/version (BOOT-03)"
  artifacts:
    - path: "go.mod"
      provides: "Go module identity + dependency pinning"
      contains: "module github.com/jsquirrelz/tide"
    - path: "api/v1alpha1/groupversion_info.go"
      provides: "v1alpha1 group/version registration"
      contains: "v1alpha1"
    - path: "PROJECT"
      provides: "kubebuilder project metadata (domain, repo, layout version)"
      contains: "tide.io"
  key_links:
    - from: "api/v1alpha1/*_types.go"
      to: "config/crd/bases/*.yaml"
      via: "controller-gen via `make manifests`"
      pattern: "kubebuilder:object:root=true"
    - from: "internal/controller/*_controller.go"
      to: "cmd/main.go"
      via: "SetupWithManager registration"
      pattern: "SetupWithManager"
---

<revision_note>
**Revision 1 — checker feedback applied:**
- **Warning 5:** Added the `<scope_note>` block below. All 30+ files in this plan are kubebuilder scaffolder output — raw `kubebuilder init / create api / create webhook` invocations. The executor performs zero hand-edits in this plan. Plan 05 hand-edits `api/v1alpha1/*_types.go`; Plan 06 hand-edits `internal/controller/*_controller.go`; Plan 07 hand-edits `internal/webhook/v1alpha1/*_webhook.go`. The 15-file warning threshold is acceptable here because the count reflects scaffolder breadth, not the executor's edit surface.
</revision_note>

<scope_note>
> All files in this plan are kubebuilder scaffolder output (raw `kubebuilder init / create api / create webhook` invocations). The executor performs zero hand-edits — Plan 05 hand-edits the `*_types.go` files; Plan 06 hand-edits the controller files; Plan 07 hand-edits the webhook files.
</scope_note>

<objective>
Run the kubebuilder v4.14 scaffolding sequence to produce the project skeleton for TIDE. This plan executes the one-time, idempotent commands that create the Go module, kubebuilder project metadata, six v1alpha1 CRDs (Project, Milestone, Phase, Plan, Task, Wave), six controller skeletons, and two webhook stubs (Plan with --conversion --programmatic-validation; Wave with --programmatic-validation only). The output is committed RAW — no hand-edits to types, controllers, or webhooks happen here. Wave 2 plans hand-edit the scaffolded files; this plan establishes the surface they edit.

Purpose: All other Phase 1 work depends on the kubebuilder scaffold existing. This plan unblocks the rest of Phase 1 with zero ambiguity about the starting state.

Output: A committed kubebuilder scaffold matching the file layout in 01-RESEARCH.md §"Recommended Project Structure", plus go.mod pinning at Go 1.26 and the dependency versions named in CLAUDE.md's Technology Stack table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-CONTEXT.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md
@.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-VALIDATION.md
@CLAUDE.md
@README.md

<interfaces>
<!-- Locked decisions from CONTEXT.md that govern this scaffold step. -->

- D-A1: Go module path is `github.com/jsquirrelz/tide`
- D-A2: Container images publish to `ghcr.io/jsquirrelz/` (tide-controller, tide-dashboard)
- D-B3: Plan gets validating admission webhook (cycle detection wires in P2) + conversion webhook (CRD-05 future-proofing)
- D-B1: Wave gets validating webhook only (rejects client-applies — no-op in P1)
- D-C1: All six Kinds get controller scaffolds (Standard depth in Wave 2)
- BOOT-03: Single v1alpha1 group/version on all six CRDs — no v1beta1 stubs

Versions pinned per CLAUDE.md Technology Stack table:
- Go 1.26 (toolchain >= 1.25)
- kubebuilder v4.14.0
- controller-runtime v0.24.x (in-place upgrade after scaffold; kubebuilder v4.14 emits v0.23.3 by default per 01-RESEARCH.md §"Open Questions")
- k8s.io/* v0.36.x (transitive via controller-runtime)
- Ginkgo v2.28.x + Gomega
- zap v1.28.x
- logr v1.4.x
- prometheus/client_golang v1.23.x (transitive)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run kubebuilder init to scaffold the project</name>
  <files>go.mod, go.sum, PROJECT, Makefile, Dockerfile, cmd/main.go, hack/boilerplate.go.txt, config/default/, config/manager/, .dockerignore, .gitignore</files>
  <read_first>
    - CLAUDE.md (Technology Stack table — Go 1.26, kubebuilder v4.14.0 pinning; "What NOT to Use" section)
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md §"kubebuilder Scaffolding Sequence" (lines ~142-202) — exact command flags
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-CONTEXT.md §"Module identity" (D-A1, D-A2) — module path is `github.com/jsquirrelz/tide`
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-VALIDATION.md §"Wave 0 Requirements" — full list of artifacts this task must produce
  </read_first>
  <action>
Verify kubebuilder v4.14.0 is installed:
```bash
kubebuilder version
# Expect: Version: cli/v4.14.x ; ProjectVersion: 3
```

If not installed, install per https://book.kubebuilder.io/quick-start.html:
```bash
curl -L -o /tmp/kubebuilder "https://go.kubebuilder.io/dl/v4.14.0/$(go env GOOS)/$(go env GOARCH)"
chmod +x /tmp/kubebuilder
sudo mv /tmp/kubebuilder /usr/local/bin/kubebuilder
```

From the repo root `/Users/justinsearles/Projects/tide`, run the init command. Note: kubebuilder refuses to init in a non-empty directory unless `--force` is passed. README.md and CLAUDE.md already exist in this repo so `--force` IS required.

```bash
kubebuilder init \
  --domain tide.io \
  --repo github.com/jsquirrelz/tide \
  --owner "TIDE Authors" \
  --license apache2 \
  --project-name tide \
  --force
```

This creates: `go.mod`, `Makefile`, `Dockerfile`, `cmd/main.go`, `hack/boilerplate.go.txt`, `PROJECT` metadata file, `config/default/`, `config/manager/`, `.dockerignore`, `.gitignore` patterns appended.

After init, edit `go.mod` to set the toolchain directive and pin minimum versions:
```
module github.com/jsquirrelz/tide

go 1.26.0

toolchain go1.26.0
```

Run `go mod tidy` to resolve initial dependency graph. Do NOT yet upgrade controller-runtime to v0.24.x — that happens in Task 4 after all `kubebuilder create` commands run (mixing versions mid-scaffold can confuse the scaffolder).

Do NOT delete the kubebuilder-default `cmd/main.go` here. Plan 08 (Wave 3) replaces it with the custom Manager wiring. Keeping it intact during Wave 1/2 means `go build ./...` keeps working.
  </action>
  <verify>
    <automated>test -f go.mod && test -f PROJECT && test -f cmd/main.go && grep -q "module github.com/jsquirrelz/tide" go.mod && grep -q "go 1.26" go.mod && grep -q "tide.io" PROJECT</automated>
  </verify>
  <acceptance_criteria>
    - `test -f go.mod` exits 0
    - `grep -q "^module github.com/jsquirrelz/tide$" go.mod` exits 0 (D-A1)
    - `grep -qE "^go 1\.26" go.mod` exits 0 (Go 1.26 per CLAUDE.md)
    - `test -f PROJECT` exits 0 (kubebuilder project metadata)
    - `grep -q "domain: tide.io" PROJECT` exits 0
    - `grep -q "projectName: tide" PROJECT` exits 0
    - `test -f cmd/main.go` exits 0
    - `test -f Makefile` exits 0
    - `test -d config/default` exits 0
    - `test -d config/manager` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>kubebuilder project initialized, Go module declared with correct identity, scaffold files in place, `go build ./...` passes against the empty manager.</done>
</task>

<task type="auto">
  <name>Task 2: Run kubebuilder create api for all six Kinds (Project, Milestone, Phase, Plan, Task, Wave)</name>
  <files>api/v1alpha1/groupversion_info.go, api/v1alpha1/project_types.go, api/v1alpha1/milestone_types.go, api/v1alpha1/phase_types.go, api/v1alpha1/plan_types.go, api/v1alpha1/task_types.go, api/v1alpha1/wave_types.go, internal/controller/project_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go, internal/controller/wave_controller.go, internal/controller/suite_test.go, config/crd/bases/, config/samples/, cmd/main.go (updated by scaffolder)</files>
  <read_first>
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md §"kubebuilder Scaffolding Sequence" (lines ~155-178) — the six `kubebuilder create api` command invocations, exact flag set
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md §"Important nuance" note on the two-step pattern (create api THEN create webhook)
    - cmd/main.go (state after kubebuilder init — to see what gets edited by scaffolder)
    - PROJECT (kubebuilder layout descriptor, scaffolder updates this)
  </read_first>
  <action>
From the repo root, run all six `kubebuilder create api` commands in order. Each command emits both the `--resource` (types under `api/v1alpha1/`) AND the `--controller` (reconciler stub under `internal/controller/`). Run them sequentially — kubebuilder mutates PROJECT and cmd/main.go on each invocation.

```bash
kubebuilder create api --group tide --version v1alpha1 --kind Project    --resource --controller
kubebuilder create api --group tide --version v1alpha1 --kind Milestone  --resource --controller
kubebuilder create api --group tide --version v1alpha1 --kind Phase      --resource --controller
kubebuilder create api --group tide --version v1alpha1 --kind Plan       --resource --controller
kubebuilder create api --group tide --version v1alpha1 --kind Task       --resource --controller
kubebuilder create api --group tide --version v1alpha1 --kind Wave       --resource --controller
```

Each command:
- Adds `api/v1alpha1/<kind>_types.go` (Spec/Status struct skeleton)
- Adds `internal/controller/<kind>_controller.go` (Reconcile skeleton + SetupWithManager)
- Updates `cmd/main.go` to register the controller
- Updates `PROJECT` metadata to record the resource
- Adds `config/samples/tide_v1alpha1_<kind>.yaml` skeleton (Plan 10 replaces these)
- Adds `config/crd/bases/tide.io_<plurals>.yaml` skeleton (generated from struct markers)

After all six runs:
```bash
make generate manifests
```

This regenerates `zz_generated.deepcopy.go` and the CRD YAML manifests. Both must succeed without errors. Manifest YAMLs will be near-empty (Spec/Status structs have no fields yet — Plan 05 fills them).

Do NOT hand-edit any `_types.go` file in this task — that work belongs to Plan 05 (Wave 2). This task commits the RAW scaffolder output so future plans have a clean baseline.
  </action>
  <verify>
    <automated>test -f api/v1alpha1/groupversion_info.go && test -f api/v1alpha1/project_types.go && test -f api/v1alpha1/milestone_types.go && test -f api/v1alpha1/phase_types.go && test -f api/v1alpha1/plan_types.go && test -f api/v1alpha1/task_types.go && test -f api/v1alpha1/wave_types.go && test -f internal/controller/project_controller.go && test -f internal/controller/wave_controller.go && test -f internal/controller/suite_test.go && grep -c "v1alpha1" api/v1alpha1/groupversion_info.go</automated>
  </verify>
  <acceptance_criteria>
    - All six `api/v1alpha1/<kind>_types.go` files exist (Project, Milestone, Phase, Plan, Task, Wave)
    - All six `internal/controller/<kind>_controller.go` files exist
    - `internal/controller/suite_test.go` exists (Ginkgo+envtest harness scaffold)
    - `api/v1alpha1/groupversion_info.go` exists and references `Group: "tide.io"` and `Version: "v1alpha1"`
    - `grep -c '^// +kubebuilder:.*v1alpha1' api/v1alpha1/groupversion_info.go` returns 1 (BOOT-03 — exactly one v1alpha1 group marker; no v1beta1 stubs)
    - `make generate` exits 0 (deepcopy regeneration succeeds)
    - `make manifests` exits 0 (CRD YAML generation succeeds)
    - `test -d config/crd/bases` and at least 6 yaml files therein
    - `go build ./...` exits 0
    - `grep -c "SetupWithManager" cmd/main.go` returns >= 6 (six controllers registered by scaffolder)
  </acceptance_criteria>
  <done>Six CRD type files, six controller skeletons, suite_test.go, and CRD YAML manifests all generated by kubebuilder. Repo builds cleanly. Wave 2 plans can now hand-edit the scaffold.</done>
</task>

<task type="auto">
  <name>Task 3: Run kubebuilder create webhook for Plan (validating + conversion) and Wave (validating only)</name>
  <files>internal/webhook/v1alpha1/plan_webhook.go, internal/webhook/v1alpha1/wave_webhook.go, internal/webhook/v1alpha1/webhook_suite_test.go, config/webhook/, config/certmanager/, cmd/main.go (updated by scaffolder), PROJECT (updated)</files>
  <read_first>
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md §"kubebuilder Scaffolding Sequence" lines ~166-178 — exact create webhook invocations and the webhook scaffolding flag reference
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md §"Webhook Scaffolding" (lines ~1160-1216) — Phase 1 webhook bodies are no-op; cert handling for envtest
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-CONTEXT.md D-B1 (Wave webhook rejects client-applies — no-op in P1), D-B3 (Plan validating webhook computes cycles in P2 — no-op in P1)
    - REQUIREMENTS.md CRD-04 + CRD-05 lines 12-13
  </read_first>
  <action>
Run the two `kubebuilder create webhook` commands. Order: Plan first (has both flags), Wave second (validating only):

```bash
# Plan: validating admission (cycle detection wires in P2 - no-op in P1) + conversion (CRD-05 future-proofing)
kubebuilder create webhook --group tide --version v1alpha1 --kind Plan --conversion --programmatic-validation

# Wave: validating only (D-B1: rejects client-applied Waves - no-op in P1, P2 wires the rejection)
kubebuilder create webhook --group tide --version v1alpha1 --kind Wave --programmatic-validation
```

Each `create webhook` invocation:
- Emits `internal/webhook/v1alpha1/<kind>_webhook.go` with empty `ValidateCreate/Update/Delete` method stubs (and `ConvertTo/ConvertFrom` for the `--conversion` case)
- Updates `cmd/main.go` to wire `builder.WebhookManagedBy(mgr, &tidev1alpha1.<Kind>{})` (Plan 08 may replace this when it overhauls main.go; the scaffolder emits the standard pattern here)
- Adds `config/webhook/manifests.yaml` entries (`ValidatingWebhookConfiguration`, `MutatingWebhookConfiguration` placeholders)
- Updates `PROJECT` metadata to track which Kinds have webhooks

After both runs, regenerate manifests:
```bash
make manifests
```

Important nuance from 01-RESEARCH.md: `kubebuilder create api` does NOT accept `--conversion --defaulting --programmatic-validation` directly — those belong to `kubebuilder create webhook`. The two-step pattern (`create api` then `create webhook`) is the v4.14 idiom.

Do NOT hand-edit the webhook bodies in this task — Plan 07 (Wave 2) edits them to ensure the no-op `return nil, nil` shape and document the Phase 2 wire-points. This task commits the raw scaffolder output.

Cert handling note (from 01-RESEARCH.md): envtest auto-generates self-signed certs. cert-manager integration is deferred to Phase 5. Phase 1's CI uses envtest only — no `kind` E2E (decision recorded in 01-CONTEXT.md "Claude's Discretion").
  </action>
  <verify>
    <automated>test -f internal/webhook/v1alpha1/plan_webhook.go && test -f internal/webhook/v1alpha1/wave_webhook.go && grep -q "ValidateCreate" internal/webhook/v1alpha1/plan_webhook.go && grep -q "ValidateCreate" internal/webhook/v1alpha1/wave_webhook.go && grep -q "ConvertTo\|conversion" internal/webhook/v1alpha1/plan_webhook.go</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/webhook/v1alpha1/plan_webhook.go` exits 0
    - `test -f internal/webhook/v1alpha1/wave_webhook.go` exits 0
    - `grep -q "ValidateCreate" internal/webhook/v1alpha1/plan_webhook.go` exits 0 (CRD-04: validating webhook scaffolded)
    - `grep -q "ValidateCreate" internal/webhook/v1alpha1/wave_webhook.go` exits 0 (D-B1)
    - Plan webhook has conversion scaffolding: `grep -qE "ConvertTo|conversion|Hub\(\)|Spoke" internal/webhook/v1alpha1/plan_webhook.go` exits 0 (CRD-05)
    - Wave webhook does NOT have conversion scaffolding: `! grep -qE "ConvertTo|Hub\(\)" internal/webhook/v1alpha1/wave_webhook.go` exits 0
    - `test -d config/webhook` exits 0 with at least manifests.yaml present
    - `make manifests` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Plan webhook (validating + conversion) and Wave webhook (validating only) scaffolded. Phase 2 has the call sites; Plan 07 edits them to no-op shape with documentation.</done>
</task>

<task type="auto">
  <name>Task 4: Upgrade controller-runtime to v0.24.x and pin all Stack-table versions</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - CLAUDE.md (Technology Stack table — controller-runtime v0.24.x, Ginkgo v2.28.x, zap v1.28.x, logr v1.4.x; "Version Compatibility" matrix)
    - .planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-RESEARCH.md §"Open Questions" — kubebuilder v4.14 scaffolds with controller-runtime v0.23.3; in-place upgrade to v0.24.x is the documented path
    - go.mod (post-scaffold state)
  </read_first>
  <action>
After Tasks 1-3 complete, kubebuilder v4.14 has pinned controller-runtime to v0.23.3. Upgrade to v0.24.x (Phase 1's chosen pin per CLAUDE.md). Run from repo root:

```bash
# Upgrade controller-runtime (kubebuilder v4.14 scaffolded v0.23.3 — bump to v0.24.x for Phase 1)
go get sigs.k8s.io/controller-runtime@v0.24.1

# Verify Ginkgo + Gomega are at the right minor (kubebuilder scaffold pulls them in)
go get github.com/onsi/ginkgo/v2@v2.28.3
go get github.com/onsi/gomega@latest

# Verify zap + logr per Stack table
go get go.uber.org/zap@v1.28.0
go get github.com/go-logr/logr@v1.4.3

# Tidy after pinning
go mod tidy
```

The v0.23 → v0.24 upgrade introduces the generics-based webhook builder (`builder.WebhookManagedBy(mgr, &T{})` two-arg form per CLAUDE.md). If `go build ./...` now fails because the kubebuilder v4.14-emitted webhook registration in `cmd/main.go` uses the old single-arg form, fix the registration in-place. Reference 01-RESEARCH.md §"Manager Wiring" for the v0.24 idiom:
```go
must(builder.WebhookManagedBy(mgr, &tidev1alpha1.Plan{}).
    WithValidator(&webhookv1alpha1.PlanValidator{}).
    Complete())
```

If the kubebuilder scaffolder used the old `.For(&tidev1alpha1.Plan{})` form, switch to the new two-arg `WebhookManagedBy(mgr, obj)` pattern. Plan 08 will fully replace cmd/main.go with the custom Manager wiring — Task 4's job is only to make the post-upgrade scaffold compile.

Then verify `go.mod` reflects the locked versions:
```bash
grep -E "sigs.k8s.io/controller-runtime|onsi/ginkgo|onsi/gomega|go.uber.org/zap|go-logr/logr" go.mod
```
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && grep -E "sigs.k8s.io/controller-runtime v0\.24" go.mod && grep -E "github.com/onsi/ginkgo/v2 v2\.28" go.mod && grep -E "go.uber.org/zap v1\.28" go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "sigs.k8s.io/controller-runtime v0\.24" go.mod` returns at least 1 match (CLAUDE.md pin)
    - `grep -E "github.com/onsi/ginkgo/v2 v2\.28" go.mod` returns at least 1 match
    - `grep -E "github.com/onsi/gomega" go.mod` returns at least 1 match
    - `grep -E "go.uber.org/zap v1\.28" go.mod` returns at least 1 match
    - `grep -E "github.com/go-logr/logr v1\.4" go.mod` returns at least 1 match
    - `grep -E "^go 1\.26" go.mod` returns at least 1 match
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - `go mod verify` exits 0 (checksums valid)
  </acceptance_criteria>
  <done>All Phase 1 Go dependency versions pinned per CLAUDE.md Stack table. Repo builds clean against controller-runtime v0.24.x. Wave 2 plans can hand-edit the scaffold confident the import surface is stable.</done>
</task>

</tasks>

<verification>
- All six api/v1alpha1/*_types.go files exist with v1alpha1 group/version
- All six internal/controller/*_controller.go files exist
- Two webhooks scaffolded: Plan (validating + conversion), Wave (validating only)
- `make generate manifests` exits 0
- `go build ./...` exits 0 with all Stack-table versions pinned
- BOOT-03 verified: `grep -c '^// +kubebuilder:.*v1alpha1' api/v1alpha1/groupversion_info.go` returns exactly 1
</verification>

<success_criteria>
- The kubebuilder scaffold is committed RAW (no hand-edits to types or controllers — those happen in Wave 2 plans)
- Go module identity matches D-A1: `github.com/jsquirrelz/tide`
- All controller-runtime v0.24, Ginkgo v2.28, zap v1.28, logr v1.4 dependencies pinned
- Single v1alpha1 group on all six Kinds (BOOT-03 verified)
- Conversion webhook scaffolding exists for Plan (CRD-05 future-proofing)
- M0 commitment marker still in ROADMAP.md (BOOT-01 — no action needed, just verified)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-crds-pkg-dag-controller-scaffold/01-01-SUMMARY.md` covering:
- Exact kubebuilder version used
- Final Go version + go.mod contents (key lines)
- File tree under api/v1alpha1/, internal/controller/, internal/webhook/v1alpha1/, config/
- Any deviations from RESEARCH.md (e.g., kubebuilder default file names vs research recommendations)
- Notes for Wave 2 plans (which files to hand-edit; which to leave alone)
</output>
</output>
