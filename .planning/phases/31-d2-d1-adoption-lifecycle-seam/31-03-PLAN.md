---
phase: 31-d2-d1-adoption-lifecycle-seam
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/child_rollup_idempotency_test.go
autonomous: true
requirements: [ADOPT-02, ADOPT-04]
must_haves:
  truths:
    - "Child-level budget rollup (milestone, phase, plan) fires unconditionally for adopted Projects — there is no ImportSource skip guard at the child levels (audited and confirmed)."
    - "Each child level rolls up its planner Job usage exactly once, gated by a durable per-level PlannerRolledUpUID marker — NOT by isFirstCompletion alone."
    - "A reconcile after the 300s reporter-Job TTL-GC window does NOT re-increment Project.Status.Budget.CostSpentCents for the same child planner Job."
    - "As child planners complete, Project.Status.Budget.CostSpentCents and TokensSpent increase (rollup reaches the Project budget)."
  artifacts:
    - path: "internal/controller/milestone_controller.go"
      provides: "MilestoneRolledUpUID-gated rollup"
      contains: "PlannerRolledUpUID"
    - path: "internal/controller/phase_controller.go"
      provides: "PhaseRolledUpUID-gated rollup"
      contains: "PlannerRolledUpUID"
    - path: "internal/controller/plan_controller.go"
      provides: "PlanRolledUpUID-gated rollup (new — plan level had no marker)"
      contains: "PlannerRolledUpUID"
    - path: "internal/controller/child_rollup_idempotency_test.go"
      provides: "Envtest for ADOPT-04 exactly-once across TTL-GC"
      contains: "RolledUpUID"
  key_links:
    - from: "milestone/phase/plan handleJobCompletion"
      to: "budget.RollUpUsage"
      via: "gated by <level>.Status.PlannerRolledUpUID != jobName"
      pattern: "PlannerRolledUpUID.*!=.*tide-"
    - from: "post-rollup"
      to: "<level>.Status.PlannerRolledUpUID = jobName"
      via: "Status().Patch(MergeFrom) after successful RollUpUsage"
      pattern: "Status\\(\\)\\.Patch.*MergeFrom"
---

<objective>
Implement D1 idempotency: make child-level (milestone / phase / plan) planner
budget rollup exactly-once by gating each `budget.RollUpUsage` call on a durable
per-level `PlannerRolledUpUID` marker, mirroring the project-level Phase-27
pattern. Today the child rollups gate only on `isFirstCompletion`, which flips
back to true once the reporter Job TTL-GCs (300s) — causing the double-count that
corrupts `CostSpentCents` on halt→resume. The project-level rollup (and the
adoption advance) ship in plans 02/01; this plan closes the child-level
exactly-once guarantee so spend that accrues under an adopted Project (ADOPT-02)
is also correct (ADOPT-04).

Purpose: Removes the non-idempotent-reconcile anti-pattern at the three child
levels; guarantees the budget meter is trustworthy under TTL-GC.
Output: Marker-gated rollups in three controllers; new envtest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-d2-d1-adoption-lifecycle-seam/31-CONTEXT.md
@.planning/phases/31-d2-d1-adoption-lifecycle-seam/31-01-PLAN.md

<interfaces>
From plan 01: MilestoneStatus / PhaseStatus / PlanStatus each now carry
`PlannerRolledUpUID string` (json plannerRolledUpUID,omitempty).

The project-level prior-art gate to mirror (project_controller.go:1303-1322):
  plannerJobName := fmt.Sprintf("tide-project-%s-1", project.UID)
  if isFirstCompletion && envReadOK {
      if project.Status.Budget.PlannerRolledUpUID != plannerJobName {
          if rollErr := budget.RollUpUsage(...); rollErr == nil {
              markerPatch := client.MergeFrom(project.DeepCopy())
              project.Status.Budget.PlannerRolledUpUID = plannerJobName
              r.Status().Patch(ctx, project, markerPatch) // non-fatal on err
          }
      }
  }

Child rollup sites that must adopt this gate (currently isFirstCompletion-only):
  - milestone_controller.go:587-591   (jobName: tide-milestone-<ms.UID>-1, see L284)
  - phase_controller.go:518-522       (jobName: tide-phase-<ph.UID>-1, see L278)
  - plan_controller.go:593            (jobName: tide-plan-<plan.UID>-1, see L298)
    NOTE D-03a: the plan level currently has NO marker and NO RolledUpUID gate —
    it calls budget.RollUpUsage unconditionally inside isFirstCompletion. It is a
    genuine double-count risk and MUST gain the marker, not just be confirmed.

Marker target struct: the child's OWN Status (e.g. ms.Status.PlannerRolledUpUID),
NOT Project.Status.Budget — only Project carries BudgetStatus. The rollup still
writes into Project.Status.Budget via budget.RollUpUsage(ctx, r.Client, project,
out.Usage); the per-child marker records that THIS child already contributed.

Conventions: meta.Set/FindStatusCondition + Status().Patch(MergeFrom) (D-09);
rollup-skip returns no err (D-08, the existing sites already log non-fatal).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit for ImportSource skip-guards, then gate all three child rollups on per-level markers</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go</files>
  <read_first>
    - internal/controller/project_controller.go:1303-1322 (project-level marker gate — the exact pattern to mirror)
    - internal/controller/milestone_controller.go:578-591 (isFirstCompletion + RollUpUsage; jobName at L284)
    - internal/controller/phase_controller.go:512-522 (jobName at L278)
    - internal/controller/plan_controller.go:585-600 (jobName at L298; NO existing marker — D-03a)
    - api/v1alpha2/milestone_types.go / phase_types.go / plan_types.go (the PlannerRolledUpUID fields from plan 01)
  </read_first>
  <behavior>
    - For each of milestone / phase / plan handleJobCompletion: the first observation of a completed planner Job rolls its usage into Project.Status.Budget exactly once and stamps <level>.Status.PlannerRolledUpUID = "tide-<level>-<uid>-1".
    - A second reconcile after the reporter Job has TTL-GC'd (isFirstCompletion true again) finds the marker already set and SKIPS budget.RollUpUsage — Project CostSpentCents is unchanged.
    - The rollup remains unconditional w.r.t. ImportSource at child levels (no `if project.Spec.ImportSource != nil { skip }` guard) so adopted Projects accrue child planner spend (ADOPT-02).
    - On a RollUpUsage error the marker is NOT stamped (next reconcile retries); on marker-Patch error the rollup is non-fatal (logged), matching the project-level site.
  </behavior>
  <action>
    FIRST, as an explicit step, grep every child rollup site for an existing
    skip-guard and an existing marker:
    `grep -nE 'ImportSource|RolledUpUID' internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go`.
    Confirm there is NO `if project.Spec.ImportSource != nil { ... skip rollup }`
    at any child level (child rollup must stay unconditional — STATE.md pending
    todo). If such a guard is found, remove it and note it in the SUMMARY.
    Confirm (D-03a) the plan level has no pre-existing marker; it does not, so add one.

    At each site, mirror the project-level gate: compute the level's planner
    jobName via fmt.Sprintf("tide-milestone-%s-1", ms.UID) /
    "tide-phase-%s-1" (ph.UID) / "tide-plan-%s-1" (plan.UID). Wrap the existing
    `if isFirstCompletion && envReadOK && project != nil` rollup so it also
    checks `<level>.Status.PlannerRolledUpUID != jobName` before calling
    budget.RollUpUsage. On a successful (nil-error) RollUpUsage, build
    `markerPatch := client.MergeFrom(<level>.DeepCopy())`, set
    `<level>.Status.PlannerRolledUpUID = jobName`, and
    `r.Status().Patch(ctx, <level>, markerPatch)` — logging Patch errors as
    non-fatal (do not return err — D-08). Do NOT clear the marker anywhere.

    Use Status().Patch + MergeFrom and the meta condition helpers (D-09); do not
    add Status().Update sites. Do NOT touch the project-level rollup or its
    PlannerRolledUpUID (D-10 — preserved in plan 02 / unchanged here). Touch no
    values.yaml (D-11).
  </action>
  <acceptance_criteria>
    - `grep -nE 'ImportSource' internal/controller/{milestone,phase,plan}_controller.go` shows no skip-around-rollup guard (audit recorded in SUMMARY).
    - Each of the three child controllers contains a `PlannerRolledUpUID != ` gate immediately governing its `budget.RollUpUsage` call: `grep -c 'PlannerRolledUpUID' internal/controller/plan_controller.go` returns >= 2 (gate read + marker write).
    - Each controller stamps the marker via a MergeFrom Status().Patch only after a nil-error RollUpUsage.
    - `grep -c 'Status().Update' internal/controller/{milestone,phase,plan}_controller.go` did not increase versus HEAD (D-09).
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./... && test "$(grep -lc PlannerRolledUpUID internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go | grep -c :)" -ge 0 && grep -q PlannerRolledUpUID internal/controller/plan_controller.go && echo OK</automated>
  </verify>
  <done>All three child rollups are gated on a durable per-level marker, stamped only after a successful rollup; rollup stays unconditional w.r.t. ImportSource; project-level rollup untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Envtest for ADOPT-02 (accrual) and ADOPT-04 (exactly-once across TTL-GC)</name>
  <files>internal/controller/child_rollup_idempotency_test.go</files>
  <read_first>
    - internal/controller/suite_test.go (harness, client, scheme)
    - internal/controller/project_planner_completion_test.go:103-184 (the BYPASS-03 double-count-prevention pattern — directly analogous; simulates repeated nil-Job completion and asserts the marker prevents re-count)
    - internal/controller/milestone_controller_test.go and phase_controller_test.go and plan_controller_test.go (per-level handleJobCompletion drive + EnvReader stub patterns)
    - internal/budget/tally.go (RollUpUsage — how CostSpentCents/TokensSpent land on Project)
  </read_first>
  <action>
    Create internal/controller/child_rollup_idempotency_test.go (Ginkgo, same suite).

    ADOPT-02 (accrual): for each child level (table-driven or three specs),
    set up a Project + an owned child with an EnvReader stub returning a known
    Usage; drive handleJobCompletion as a first completion; assert
    Project.Status.Budget.CostSpentCents and TokensSpent increased by the
    stubbed Usage, and that <level>.Status.PlannerRolledUpUID == the level's
    planner jobName.

    ADOPT-04 (exactly-once across TTL-GC): after the first rollup above,
    reload the Project, capture CostSpentCents, then drive handleJobCompletion
    AGAIN simulating the post-TTL-GC condition (reporter Job IsNotFound →
    isFirstCompletion flips true again, mirroring the BYPASS-03 test setup) while
    the marker is already set; assert CostSpentCents is UNCHANGED (no second
    increment for the same Job). Do this for at least the plan level (the newly
    guarded one) and one of milestone/phase.

    Mirror project_planner_completion_test.go's approach for forcing the
    repeated-completion path. Use Status().Patch+MergeFrom for any test seeding.
    Do not assert on values.yaml.
  </action>
  <acceptance_criteria>
    - `go test ./internal/controller/ -run 'ChildRollup|RolledUp|ADOPT_0[24]' -count=1` passes.
    - An accrual spec asserts CostSpentCents AND TokensSpent increased by the stubbed Usage after first completion (ADOPT-02).
    - An idempotency spec asserts CostSpentCents is byte-for-byte unchanged after a second post-TTL-GC completion with the marker already set (ADOPT-04), covering at least the plan level.
    - New file builds and runs under the existing controller suite.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./internal/controller/ -run 'ChildRollup|RolledUp|ADOPT' -count=1</automated>
  </verify>
  <done>Envtest proves child planner spend accrues to the Project budget (ADOPT-02) and never double-counts after reporter-Job TTL-GC (ADOPT-04), including the plan level that previously had no marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconcile goroutine → CRD `.status` | Child marker write + Project budget write across re-entrant reconciles; the financial-integrity surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering (financial integrity / double-count) | child budget.RollUpUsage | mitigate | Durable per-level PlannerRolledUpUID marker gates the rollup; survives reporter-Job TTL-GC because it lives in CRD `.status`, not in the reporter-Job-presence signal. ADOPT-04 envtest is the regression guard. |
| T-31-08 | Repudiation / lost-update | marker stamp vs rollup ordering | mitigate | Marker is stamped only AFTER a nil-error RollUpUsage (mirrors project-level Pitfall-2 ordering); a failed rollup leaves the marker unset so the next reconcile retries — no silent loss. |
| T-31-09 | Denial of Service (retry storm) | rollup-skip / marker-patch error path | mitigate | D-08: skip and patch-error paths log non-fatally and return no err, matching the existing project-level site. |

No new external input boundary. The single high-value asset is the budget
meter's correctness; T-31-07 (double-count) is the financial-integrity threat
this plan exists to close, mitigated by the durable marker and proven by the
ADOPT-04 envtest.
</threat_model>

<verification>
- `go build ./...` and `go vet ./internal/controller/` clean.
- `go test ./internal/controller/ -run 'ChildRollup|RolledUp|ADOPT' -count=1` passes.
- Audit recorded: no ImportSource skip-guard at any child rollup site.
- No new `Status().Update` sites; project-level rollup unchanged; values.yaml unchanged.
</verification>

<success_criteria>
ADOPT-02 and ADOPT-04 are proven by Layer-A envtest: child planner spend accrues
to the Project budget under the adoption path, and budget rollup is exactly-once
at the milestone, phase, and plan levels across reporter-Job TTL-GC — closing the
double-count that corrupted CostSpentCents on halt→resume.
</success_criteria>

<output>
Create `.planning/phases/31-d2-d1-adoption-lifecycle-seam/31-03-SUMMARY.md` when done.
</output>
