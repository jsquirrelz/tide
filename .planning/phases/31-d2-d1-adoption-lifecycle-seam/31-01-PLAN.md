---
phase: 31-d2-d1-adoption-lifecycle-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha2/shared_types.go
  - api/v1alpha2/project_types.go
  - api/v1alpha2/milestone_types.go
  - api/v1alpha2/phase_types.go
  - api/v1alpha2/plan_types.go
  - api/v1alpha2/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_milestones.yaml
  - config/crd/bases/tideproject.k8s_phases.yaml
  - config/crd/bases/tideproject.k8s_plans.yaml
autonomous: true
requirements: [ADOPT-01, ADOPT-04, ADOPT-05]
must_haves:
  truths:
    - "A new ConditionProjectPlannerSuppressed string constant exists with Reason=AdoptionComplete, beside the existing dispatch-suppressing conditions."
    - "Each child level (Milestone, Phase, Plan) has a durable scalar marker field on its own .status to record that its planner Job's usage was already rolled up."
    - "controller-gen regenerated DeepCopy + CRD manifests with no drift (make generate manifests is clean)."
  artifacts:
    - path: "api/v1alpha2/shared_types.go"
      provides: "ConditionProjectPlannerSuppressed + ReasonAdoptionComplete constants"
      contains: "ConditionProjectPlannerSuppressed"
    - path: "api/v1alpha2/milestone_types.go"
      provides: "MilestoneStatus.PlannerRolledUpUID marker"
      contains: "PlannerRolledUpUID"
    - path: "api/v1alpha2/phase_types.go"
      provides: "PhaseStatus.PlannerRolledUpUID marker"
      contains: "PlannerRolledUpUID"
    - path: "api/v1alpha2/plan_types.go"
      provides: "PlanStatus.PlannerRolledUpUID marker"
      contains: "PlannerRolledUpUID"
  key_links:
    - from: "internal/controller/project_controller.go (plan 02)"
      to: "tidev1alpha2.ConditionProjectPlannerSuppressed"
      via: "meta.SetStatusCondition / meta.FindStatusCondition"
      pattern: "ConditionProjectPlannerSuppressed"
    - from: "internal/controller/{milestone,phase,plan}_controller.go (plan 03)"
      to: "<level>.Status.PlannerRolledUpUID"
      via: "marker check before budget.RollUpUsage"
      pattern: "PlannerRolledUpUID"
---

<objective>
Add the durable `.status` vocabulary that plans 02 and 03 consume: a new
project-planner suppression condition constant (D-01) and per-child-level
exactly-once rollup markers (D-03). This is the contract-first wave — no
controller logic changes here, only API types + regenerated artifacts. Plans 02
and 03 implement against these symbols.

Purpose: Honors the interface-first ordering — downstream controller plans
receive the exact identifiers in their context, no codebase scavenger hunt.
Output: New constants/fields in `api/v1alpha2/`, regenerated DeepCopy + CRD YAML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-d2-d1-adoption-lifecycle-seam/31-CONTEXT.md

<interfaces>
Existing convention in api/v1alpha2/shared_types.go (the family the new condition joins):

  ConditionBillingHalt    = "BillingHalt"     // ReasonCreditBalanceTooLow
  ConditionBudgetBlocked  = "BudgetBlocked"   // ReasonBudgetCapReached
  ConditionFailureHalt    = "FailureHalt"     // ReasonTaskFailedHalt
  ConditionImportComplete = "ImportComplete"  // ReasonImportSucceeded

Each is a durable, operator-visible, dispatch-suppressing `.status` condition
declared as an untyped string const in its own grouped `const (...)` block with
a doc comment. The new condition MUST follow this exact shape.

Existing project-level marker in api/v1alpha2/project_types.go (BudgetStatus, the
prior-art pattern for the new child-level markers):

  // PlannerRolledUpUID is the name of the most recent planner Job whose Usage
  // was successfully rolled up into CostSpentCents...
  PlannerRolledUpUID string `json:"plannerRolledUpUID,omitempty"`

Child status structs that receive the new marker:
  - MilestoneStatus (api/v1alpha2/milestone_types.go:52) — currently {Phase, Conditions}
  - PhaseStatus     (api/v1alpha2/phase_types.go:48)     — currently {Phase, Conditions}
  - PlanStatus      (api/v1alpha2/plan_types.go:54)      — {Phase, Conditions, ValidationState, CycleEdges, IntegratedThroughWave}

NOTE: BudgetStatus is embedded ONLY on Project.Status.Budget (project_types.go:459).
Milestone/Phase/Plan do NOT carry BudgetStatus — the new marker is a standalone
scalar string field directly on each child's own Status struct, keyed by that
level's own planner Job name (tide-<level>-<uid>-1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add suppression condition + per-child-level rollup markers to API types</name>
  <files>api/v1alpha2/shared_types.go, api/v1alpha2/milestone_types.go, api/v1alpha2/phase_types.go, api/v1alpha2/plan_types.go</files>
  <read_first>
    - api/v1alpha2/shared_types.go (the condition/reason const family, lines ~205-289)
    - api/v1alpha2/project_types.go:263-287 (BudgetStatus.PlannerRolledUpUID prior art)
    - api/v1alpha2/milestone_types.go:52-60 (MilestoneStatus)
    - api/v1alpha2/phase_types.go:48-56 (PhaseStatus)
    - api/v1alpha2/plan_types.go:54-90 (PlanStatus)
  </read_first>
  <action>
    In api/v1alpha2/shared_types.go, add a new grouped const block (mirroring the
    Phase 28 ImportComplete block style) declaring `ConditionProjectPlannerSuppressed = "ProjectPlannerSuppressed"`
    and `ReasonAdoptionComplete = "AdoptionComplete"`, each with a doc comment
    explaining: durable `.status` condition that permanently suppresses
    project-planner re-dispatch on an adopted/imported Project once the import
    tree is confirmed present; survives manager restart because it lives in
    `.status` and is self-documenting in `kubectl describe`; read by
    reconcileProjectPlannerDispatch as a short-circuit before the live List.

    Add a standalone scalar field `PlannerRolledUpUID string` with
    `json:"plannerRolledUpUID,omitempty"` and a `+optional` marker to EACH of
    MilestoneStatus, PhaseStatus, and PlanStatus. Doc-comment each per D-03:
    records the name of this level's planner Job whose Usage was already rolled
    up into the Project budget, gating re-count after the reporter Job TTL-GCs.
    Mirror the wording of BudgetStatus.PlannerRolledUpUID. Do NOT embed
    BudgetStatus on the child structs — a single scalar string only (minimal,
    re-derivable, per the CRD-.status-only persistence constraint).

    Do NOT add a marker to the Project level — its BudgetStatus.PlannerRolledUpUID
    already exists and is preserved exactly (D-10). Touch no values.yaml (D-11).
  </action>
  <acceptance_criteria>
    - `grep -c 'ConditionProjectPlannerSuppressed' api/v1alpha2/shared_types.go` returns >= 1.
    - `grep -c 'ReasonAdoptionComplete' api/v1alpha2/shared_types.go` returns >= 1.
    - `grep -l 'PlannerRolledUpUID' api/v1alpha2/milestone_types.go api/v1alpha2/phase_types.go api/v1alpha2/plan_types.go` lists all three files.
    - `git diff --name-only -- charts/tide/values.yaml` is empty (D-11 untouched).
    - `gofmt -l api/v1alpha2/*.go` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && gofmt -l api/v1alpha2/*.go | grep -q . && echo FAIL || (grep -q ConditionProjectPlannerSuppressed api/v1alpha2/shared_types.go && grep -lq PlannerRolledUpUID api/v1alpha2/plan_types.go && echo OK)</automated>
  </verify>
  <done>Suppression condition + reason constants exist; all three child Status structs carry a PlannerRolledUpUID scalar marker; values.yaml untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate DeepCopy + CRD manifests and confirm build</name>
  <files>api/v1alpha2/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_milestones.yaml, config/crd/bases/tideproject.k8s_phases.yaml, config/crd/bases/tideproject.k8s_plans.yaml</files>
  <read_first>
    - Makefile (targets `generate` ~line 66, `manifests` ~line 52, `controller-gen` ~line 443)
    - api/v1alpha2/zz_generated.deepcopy.go (to confirm regeneration target)
  </read_first>
  <action>
    Run `make generate` then `make manifests` to regenerate
    api/v1alpha2/zz_generated.deepcopy.go and config/crd/bases/*.yaml from the
    new fields. New fields are scalar strings, so DeepCopy needs no hand-edits;
    the CRD YAML for milestones/phases/plans gains the new `plannerRolledUpUID`
    status property. Then `go build ./...` to confirm the api package compiles.
    Do not edit generated files by hand — if drift appears, fix the source type
    and re-run the generators.
  </action>
  <acceptance_criteria>
    - `make generate manifests` exits 0.
    - After regen, `git status --porcelain config/crd/bases/tideproject.k8s_{milestones,phases,plans}.yaml` shows them modified.
    - `grep -c 'plannerRolledUpUID' config/crd/bases/tideproject.k8s_plans.yaml` returns >= 1.
    - `go build ./...` exits 0.
    - Re-running `make generate manifests` a second time produces no further diff (idempotent generation, no manual drift).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make generate manifests && go build ./... && grep -q plannerRolledUpUID config/crd/bases/tideproject.k8s_plans.yaml && echo OK</automated>
  </verify>
  <done>Generators run clean and idempotently; CRD YAML carries the new status field; api package builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → CRD `.status` | New status fields are controller-written only; no new external/user-supplied input crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | new `.status` marker/condition fields | accept | Status subresource is RBAC-gated to the controller's ServiceAccount; fields are scalar strings with no parsing/eval; no new write path beyond existing `Status().Patch`. |
| T-31-02 | Information Disclosure | `kubectl describe` exposes suppression condition | accept | Intentional and required (D-spec: operator-legible); contains a condition name + Job-UID string, no secret material. |

No high-severity threats: this plan adds only scalar `.status` schema, no new
external input surface, no new code path. Real-world races are addressed in
plans 02/03 (single-patch ordering, marker-before-rollup).
</threat_model>

<verification>
- `make generate manifests` clean and idempotent (no second-run diff).
- `go build ./...` and `gofmt -l api/v1alpha2/*.go` clean.
- New symbols present: ConditionProjectPlannerSuppressed, ReasonAdoptionComplete,
  PlannerRolledUpUID on all three child Status structs.
- charts/tide/values.yaml unchanged.
</verification>

<success_criteria>
The API vocabulary for D2 suppression (one condition) and D1 child-level
exactly-once rollup (three scalar markers) exists, is generated cleanly into
DeepCopy + CRD manifests, and compiles. Plans 02 and 03 can implement against
these symbols with no further type changes.
</success_criteria>

<output>
Create `.planning/phases/31-d2-d1-adoption-lifecycle-seam/31-01-SUMMARY.md` when done.
</output>
