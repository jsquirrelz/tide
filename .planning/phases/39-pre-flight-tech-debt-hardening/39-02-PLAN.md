---
phase: 39-pre-flight-tech-debt-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/project_controller.go
  - internal/controller/project_rollup_idempotency_test.go
autonomous: true
requirements: [PREFLIGHT-02]

must_haves:
  truths:
    - "The project-level PlannerRolledUpUID stamp uses the same RetryOnConflict + re-fetch + MergeFromWithOptimisticLock pattern as the milestone/phase markers"
    - "A second project-level reconcile after reporter-Job TTL-GC does NOT increment Project.Status.Budget.CostSpentCents a second time for the same planner Job"
    - "On RetryOnConflict exhaustion the project-level stamp returns an error to requeue rather than swallowing it (no best-effort last-write-wins window remains)"
  artifacts:
    - path: "internal/controller/project_controller.go"
      provides: "hardened exactly-once PlannerRolledUpUID stamp at the project planner rollup site"
      contains: "RetryOnConflict"
    - path: "internal/controller/project_rollup_idempotency_test.go"
      provides: "envtest proving no project-level double-count across TTL-GC simulation"
  key_links:
    - from: "internal/controller/project_controller.go handleProjectJobCompletion"
      to: "Project.Status.Budget.PlannerRolledUpUID"
      via: "re-fetched + optimistic-lock patch after successful budget.RollUpUsage"
      pattern: "PlannerRolledUpUID"
    - from: "internal/controller/project_controller.go"
      to: "milestone_controller.go MilestoneRolledUpUID stamp"
      via: "ported exactly-once pattern (analog)"
      pattern: "MergeFromWithOptimisticLock"
---

<objective>
Harden the project-level planner-cost rollup marker (`Project.Status.Budget.PlannerRolledUpUID`) to the milestone/phase exactly-once pattern (PREFLIGHT-02). Replace the current best-effort `client.MergeFrom(project.DeepCopy())` + logged-only `r.Status().Patch(...)` at project_controller.go:1370-1382 with a `retry.RetryOnConflict` block that re-fetches the latest Project, short-circuits idempotently if the marker is already stamped, patches with `MergeFromWithOptimisticLock`, and returns the error on retry-budget exhaustion. Prove the property with a project-level envtest mirroring the existing milestone/phase/plan idempotency test.

Purpose: The current project-level stamp is a last-write-wins window with no re-fetch and no conflict retry, and it swallows the patch error ("patch PlannerRolledUpUID failed (non-fatal)"). If the marker fails to persist, a second reconcile after the reporter Job's 300s TTL-GC (when `isFirstCompletion` flips true again) double-counts the project planner's spend into `CostSpentCents`, corrupting the $100-cap accounting run #2 depends on. The milestone/phase/plan controllers already solved this; the project level is the one outstanding best-effort site.

Output: hardened stamp in project_controller.go; a project-level no-double-count-across-TTL-GC envtest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- The exact pattern to PORT, from milestone_controller.go:620-633 (the analog). -->
The milestone stamp wraps the marker patch in:
  retry.RetryOnConflict(retry.DefaultRetry, func() error {
    latest := &tideprojectv1alpha2.Milestone{}
    if err := r.Get(ctx, client.ObjectKeyFromObject(ms), latest); err != nil { return err }
    if latest.Status.MilestoneRolledUpUID == milestoneJobName { return nil } // idempotent short-circuit
    markerPatch := client.MergeFromWithOptions(latest.DeepCopy(), client.MergeFromWithOptimisticLock{})
    latest.Status.MilestoneRolledUpUID = milestoneJobName
    return r.Status().Patch(ctx, latest, markerPatch)
  })
On exhaustion: return ctrl.Result{}, fmt.Errorf("patch MilestoneRolledUpUID: %w", mErr)

<!-- Project-side identifiers (note the import alias differs). -->
In project_controller.go the api alias is tidev1alpha2 (NOT tideprojectv1alpha2):
  tidev1alpha2 "github.com/jsquirrelz/tide/api/v1alpha2"
The enclosing function is:
  func (r *ProjectReconciler) handleProjectJobCompletion(ctx, project *tidev1alpha2.Project, completedJob *batchv1.Job) (ctrl.Result, error)
The project marker field is project.Status.Budget.PlannerRolledUpUID; the job name is
  plannerJobName := fmt.Sprintf("tide-project-%s-1", project.UID)
The rollup is already correctly ORDERED: budget.RollUpUsage runs first, the marker is stamped only on rollup success (Pitfall-2). PRESERVE that ordering. The retry import path is k8s.io/client-go/util/retry (already imported in milestone_controller.go:31; add it to project_controller.go if absent).

<!-- envtest analog to extend (proves the property for milestone/phase/plan). -->
internal/controller/child_rollup_idempotency_test.go drives handleJobCompletion twice with ReporterImage="" (so isFirstCompletion=true on every call, simulating post-TTL-GC), asserts CostSpentCents accrues once then stays constant on the second call. The project-level test calls r.handleProjectJobCompletion twice and asserts the same against project.Status.Budget.CostSpentCents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the exactly-once RetryOnConflict marker stamp to the project level</name>
  <files>internal/controller/project_controller.go</files>
  <read_first>
    - internal/controller/project_controller.go (lines ~1354-1395 — the debt site: the best-effort PlannerRolledUpUID stamp + the enclosing handleProjectJobCompletion return signature; also confirm the api alias tidev1alpha2 at line 44 and the return ctrl.Result{...} shapes near line 1395)
    - internal/controller/milestone_controller.go (lines ~599-636 — the analog to mirror EXACTLY: RetryOnConflict + re-fetch latest + idempotent short-circuit + MergeFromWithOptimisticLock + return-error-on-exhaustion)
    - internal/controller/milestone_controller.go (line 31 — the k8s.io/client-go/util/retry import to mirror)
  </read_first>
  <action>
    In project_controller.go, replace the marker-stamp block inside the else-if isFirstCompletion-and-envReadOK branch (the part that currently does markerPatch := client.MergeFrom(project.DeepCopy()); project.Status.Budget.PlannerRolledUpUID = plannerJobName; r.Status().Patch(...) with a logged-only error) with a retry.RetryOnConflict(retry.DefaultRetry, func() error { ... }) block that: re-fetches the latest Project into a fresh tidev1alpha2.Project via r.Get(ctx, client.ObjectKeyFromObject(project), latest); returns nil (idempotent short-circuit) if latest.Status.Budget.PlannerRolledUpUID equals plannerJobName; sets latest.Status.Budget.PlannerRolledUpUID = plannerJobName; patches with client.MergeFromWithOptions(latest.DeepCopy(), client.MergeFromWithOptimisticLock{}) via r.Status().Patch. On RetryOnConflict exhaustion, RETURN the error to requeue — match the enclosing handleProjectJobCompletion return signature (ctrl.Result, error), i.e. return ctrl.Result{}, fmt.Errorf("patch PlannerRolledUpUID: %w", err) rather than the current logged-only swallow. PRESERVE the existing ordering invariant: the stamp stays gated behind a successful budget.RollUpUsage call (stamp only on rollup success, Pitfall-2) and behind the project.Spec.ImportSource == nil, isFirstCompletion-and-envReadOK, and PlannerRolledUpUID != plannerJobName guards already present. Add the k8s.io/client-go/util/retry import if not already present in project_controller.go.
  </action>
  <verify>
    <automated>go build ./... && grep -qE 'RetryOnConflict\(' internal/controller/project_controller.go && grep -qE 'MergeFromWithOptimisticLock' internal/controller/project_controller.go && grep -qE 'fmt.Errorf\("patch PlannerRolledUpUID' internal/controller/project_controller.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0
    - project_controller.go contains `retry.RetryOnConflict(` within the PlannerRolledUpUID stamp block
    - project_controller.go contains `MergeFromWithOptimisticLock` for the project marker patch
    - project_controller.go returns an error on retry exhaustion (`fmt.Errorf("patch PlannerRolledUpUID...`) — the prior `"patch PlannerRolledUpUID failed (non-fatal)"` logged-only swallow is gone
    - the re-fetch-and-short-circuit idempotency guard (`latest.Status.Budget.PlannerRolledUpUID == plannerJobName` → return nil) is present
    - the stamp still runs only after a successful budget.RollUpUsage (ordering preserved)
  </acceptance_criteria>
  <done>The project-level PlannerRolledUpUID stamp uses RetryOnConflict + re-fetch + MergeFromWithOptimisticLock and returns the error on exhaustion — no best-effort last-write-wins window remains at the project level (Success Criterion #3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project-level envtest — no double-count across TTL-GC</name>
  <files>internal/controller/project_rollup_idempotency_test.go</files>
  <read_first>
    - internal/controller/child_rollup_idempotency_test.go (the direct analog: drives handleJobCompletion twice with ReporterImage="" to simulate post-TTL-GC isFirstCompletion=true; asserts CostSpentCents accrues once then stays constant — extend this pattern to the PROJECT level via handleProjectJobCompletion)
    - internal/controller/project_planner_completion_test.go (BYPASS-03 PlannerRolledUpUID exactly-once analog — reuse its ProjectReconciler construction / envReader / stubDispatcher wiring conventions)
    - internal/controller/project_controller.go (handleProjectJobCompletion — the function under test; confirm which reconciler fields it needs: Client, Scheme, EnvReader, ReporterImage="" to force isFirstCompletion=true)
  </read_first>
  <behavior>
    - Test 1 (PREFLIGHT-02 accrual): create a minimal Project with Status.Phase=Running, stub the project planner EnvelopeOut with a known EstimatedCostCents, call r.handleProjectJobCompletion once → Project.Status.Budget.CostSpentCents == the stubbed cost AND Project.Status.Budget.PlannerRolledUpUID == "tide-project-<uid>-1".
    - Test 2 (PREFLIGHT-02 idempotency across TTL-GC): with ReporterImage="" so isFirstCompletion flips true again (reporter Job gone = post-300s-TTL-GC), call r.handleProjectJobCompletion a SECOND time → Project.Status.Budget.CostSpentCents is UNCHANGED (Consistently), proving the durable marker is the sole guard and no second rollup fires for the same planner Job.
  </behavior>
  <action>
    Create internal/controller/project_rollup_idempotency_test.go as a Ginkgo envtest (Label("envtest")) mirroring child_rollup_idempotency_test.go's structure but at the project level: create a minimal auto-gated Project, set Status.Phase=Running, register a stubbed project-planner EnvelopeOut (ExitCode 0, ChildCount 0, a known Usage.EstimatedCostCents and InputTokens) keyed by the project UID via the same mapEnvReader helper, construct a ProjectReconciler with ReporterImage="" (so spawnReporterIfNeeded yields isFirstCompletion=true on every call), then call handleProjectJobCompletion twice. Assert CostSpentCents accrues once (Eventually) and PlannerRolledUpUID is set to tide-project-<uid>-1, then assert CostSpentCents stays constant on the second call (Consistently). Reference PREFLIGHT-02 in the test doc comment. Reuse existing helpers (mapEnvReader, stubDispatcher, newPlannerPoolForTest, waitForCacheSync, the refetchProject-equivalent) rather than re-inventing them. If handleProjectJobCompletion requires a non-nil completedJob argument for the rollup branch, pass the same nil-or-stub shape the BYPASS-03 analog uses.
  </action>
  <verify>
    <automated>make test-int 2>&1 | tee /tmp/39-02-clean-run.log; echo "MAKE_EXIT=${PIPESTATUS[0]}"; grep -nE '^--- FAIL|^FAIL\s' /tmp/39-02-clean-run.log || echo NO_GO_TEST_FAILURES</automated>
  </verify>
  <acceptance_criteria>
    - Read the echoed MAKE_EXIT AND grep `^--- FAIL|^FAIL\s` over /tmp/39-02-clean-run.log — do NOT trust a Ginkgo "SUCCESS!" summary alone (per CLAUDE.md: the kind package bundles plain go-tests that can fail the package while Ginkgo prints SUCCESS)
    - The new ProjectRollupIdempotency envtest passes: its accrual assertion and its Consistently no-double-count assertion both green
    - The envtest asserts Project.Status.Budget.CostSpentCents is UNCHANGED on the second post-TTL-GC reconcile (the exactly-once property)
    - Any remaining `^--- FAIL|^FAIL` is ONLY the pre-existing kind `medium_http` fixture flake (no Phase-39 commit touches test/integration/kind/) — confirm by `git diff --stat` showing no changes under test/integration/kind/; otherwise it is a real regression and must be fixed
  </acceptance_criteria>
  <done>A project-level envtest proves planning-cost rollup is exactly-once across a reporter-Job TTL-GC cycle — the second reconcile does not increment CostSpentCents (Success Criterion #2), validated by reading MAKE_EXIT + grep, not the Ginkgo summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconcile → Project.Status (etcd) | Concurrent status writers (multiple reconcile rounds / restart) race on the budget marker patch |
| planner Job name construction | plannerJobName = fmt.Sprintf("tide-project-%s-1", project.UID) — derived from project.UID, NOT external input (documented construction-site invariant at project_controller.go:1361) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Tampering | Project.Status.Budget.PlannerRolledUpUID concurrent patch | mitigate | RetryOnConflict + re-fetch + MergeFromWithOptimisticLock makes the stamp durable against a concurrent status write, and the error is returned to requeue rather than swallowed — closing the last-write-wins window that allowed a double-count |
| T-39-04 | Repudiation | budget rollup exactly-once accounting | mitigate | Durable PlannerRolledUpUID marker (re-fetched and optimistically locked) is the sole idempotency guard; the envtest proves CostSpentCents cannot be incremented twice for the same planner Job across TTL-GC, keeping $100-cap accounting accurate |
| T-39-05 | Tampering | go module dependencies | accept | No new go.mod entries — only k8s.io/client-go/util/retry, already a transitive/used dependency (imported in milestone_controller.go) |
</threat_model>

<verification>
- go build ./... green; project_controller.go stamp uses RetryOnConflict + re-fetch + MergeFromWithOptimisticLock and returns the error on exhaustion.
- make test-int: read MAKE_EXIT and grep `^--- FAIL|^FAIL` (not the Ginkgo summary). The new ProjectRollupIdempotency envtest is green; the only acceptable residual FAIL is the pre-existing kind medium_http flake, confirmed by no Phase-39 changes under test/integration/kind/.
- CostSpentCents unchanged on the second post-TTL-GC reconcile.
</verification>

<success_criteria>
PREFLIGHT-02 satisfied: the project-level rollup marker follows the milestone/phase exactly-once pattern (Success Criterion #3 — RetryOnConflict + re-fetch, no best-effort window), and an envtest proves a second reconcile after reporter-Job TTL-GC does not increment the project's CostSpentCents a second time for the same Job (Success Criterion #2).
</success_criteria>

<output>
Create `.planning/phases/39-pre-flight-tech-debt-hardening/39-02-SUMMARY.md` when done
</output>
