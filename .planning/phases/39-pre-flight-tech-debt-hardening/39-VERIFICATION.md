---
phase: 39-pre-flight-tech-debt-hardening
verified: 2026-06-29T00:00:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
re_verification:
gaps:
deferred:
human_verification:
---

# Phase 39: Pre-flight Tech-Debt Hardening Verification Report

**Phase Goal:** The two load-bearing v1.0.7 audit carry-ins land in the controller and chart before run #2 launches — the single-node OOM-safety default (`plannerConcurrency 16→4`) and exactly-once project-level cost rollup — so the run can be deployed and metered without a latent over-dispatch or a double-counted cap.
**Verified:** 2026-06-29
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

(Roadmap Success Criteria are the contract; PLAN frontmatter truths are folded in as supporting detail.)

| # | Truth (Roadmap Success Criterion) | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Rendering the chart with defaults yields `plannerConcurrency: 4` (the prior `16` is gone), and a controller-level assertion confirms a fresh default deploy dispatches at most 4 concurrent planners. | ✓ VERIFIED | `helm template tide charts/tide` renders `plannerConcurrency: 4` at line 177, exit 0; no `plannerConcurrency: 16` anywhere in the render. `internal/config/config.go:117` resolves the `plannerConcurrency` field default to `4`. `go test ./internal/config/ -run TestDefault` green (config.Load on empty config → PlannerConcurrency == 4; explicit-override-wins boundary test also green). |
| 2 | An envtest proves the project-level rollup marker follows the milestone/phase exactly-once pattern: a second reconcile after reporter-Job TTL-GC does not increment the project's `CostSpentCents` a second time for the same Job. | ✓ VERIFIED | `internal/controller/project_rollup_idempotency_test.go` (171 lines, Ginkgo `Label("envtest")`) drives `handleProjectJobCompletion` twice with `ReporterImage=""` (forces `isFirstCompletion=true` on every call), asserts accrual once then `Consistently` unchanged `CostSpentCents`. Ran independently: `KUBEBUILDER_ASSETS=<1.36.0> go test ./internal/controller/ -ginkgo.focus=ProjectRollupIdempotency` → `ok` (exit 0, 18.8s). |
| 3 | The hardened project-level marker stamp uses the same `RetryOnConflict` + re-fetch pattern as the milestone/phase markers (no best-effort last-write-wins window remains at the project level). | ✓ VERIFIED | `project_controller.go:1382-1395`: `retry.RetryOnConflict(retry.DefaultRetry, ...)` → `r.Get` re-fetch of latest Project → idempotent short-circuit (`latest.Status.Budget.PlannerRolledUpUID == plannerJobName`) → `MergeFromWithOptimisticLock` patch → `return ctrl.Result{}, fmt.Errorf("patch PlannerRolledUpUID: %w", mErr)` on exhaustion. Line-for-line mirror of `milestone_controller.go:620-632`. The prior logged-only `(non-fatal)` swallow is gone; `retry` import present at line 34. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `charts/tide/templates/configmap.yaml` | plannerConcurrency configmap key with default-4 fallback | ✓ VERIFIED | Line 22: `plannerConcurrency: {{ .Values.plannerConcurrency \| default 4 }}`. The lone `default 16` (line 30) is the unrelated `maxConcurrentReconciles.task` key, intentionally untouched. |
| `hack/helm/augment-tide-chart.sh` | source of the default-4 fallback (regenerated by `make helm`) | ✓ VERIFIED | Line 90 carries `default 4`. Source fixed (not only the generated chart) so the chart-reproducibility pre-commit hook does not revert it. |
| `internal/config/config_default_test.go` | pure-Go assertion default config resolves PlannerConcurrency == 4 | ✓ VERIFIED | 85 lines; two tests (default→4, explicit-override→2). No envtest dependency. Green. |
| `test/integration/kind/configmap_planner_concurrency_test.go` | helm-template render contract test asserting `plannerConcurrency: 4` and absence of `16` | ✓ VERIFIED | Plain go-test (`TestConfigMapPlannerConcurrency*`), renders chart via `helm template`, asserts contains `plannerConcurrency: 4` and not `plannerConcurrency: 16`. Green (exit 0). |
| `internal/controller/project_controller.go` | hardened exactly-once PlannerRolledUpUID stamp | ✓ VERIFIED | Contains `RetryOnConflict`, `MergeFromWithOptimisticLock`, returns error on exhaustion. Builds clean. |
| `internal/controller/project_rollup_idempotency_test.go` | envtest proving no project-level double-count across TTL-GC | ✓ VERIFIED | 171 lines; accrual + `Consistently` no-double-count assertions. Green under envtest. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `configmap.yaml plannerConcurrency` | `internal/config/config.go` | config key consumed by config.Load → PlannerConcurrency | ✓ WIRED | `config.go:45/76/117` define the field and resolve default 4. |
| `internal/config/config.go` | `cmd/manager/main.go pool.New(cfg.PlannerConcurrency, "planner")` | resolved cap sizes the planner pool | ✓ WIRED | `main.go:343 plannerPool := pool.New(cfg.PlannerConcurrency, "planner")`. |
| `project_controller.go handleProjectJobCompletion` | `Project.Status.Budget.PlannerRolledUpUID` | re-fetched + optimistic-lock patch after RollUpUsage | ✓ WIRED | Stamp gated behind successful `budget.RollUpUsage` (ordering preserved at lines 1372-1396). |
| `project_controller.go` | `milestone_controller.go MilestoneRolledUpUID stamp` | ported exactly-once pattern (analog) | ✓ WIRED | Pattern is a line-for-line mirror of `milestone_controller.go:620-632`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Chart renders plannerConcurrency: 4, no 16 | `helm template tide charts/tide \| grep plannerConcurrency` | `plannerConcurrency: 4`; no `16` in render | ✓ PASS |
| Default config resolves to 4 | `go test ./internal/config/ -run TestDefault` | `ok` | ✓ PASS |
| Render contract test | `go test ./test/integration/kind/ -run TestConfigMapPlannerConcurrency` | `ok` (exit 0) | ✓ PASS |
| Project rollup exactly-once across TTL-GC | `KUBEBUILDER_ASSETS=<1.36.0> go test ./internal/controller/ -ginkgo.focus=ProjectRollupIdempotency` | `ok` (exit 0, 18.8s) | ✓ PASS |
| Production packages build | `go build ./internal/... ./cmd/manager/...` | exit 0 | ✓ PASS |

Note: an initial envtest run failed in BeforeSuite with `exec: "etcd": executable file not found in $PATH` — a sandbox PATH issue, not a test failure. Re-running with `KUBEBUILDER_ASSETS` pointed at the installed `1.36.0-darwin-amd64` assets produced a clean pass.

### Probe Execution

Not applicable — Phase 39 declares no `scripts/*/tests/probe-*.sh` probes; verification is via go-tests + envtest + helm render, all executed above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| PREFLIGHT-01 | 39-01 | Chart configmap default `plannerConcurrency` corrected 16→4; controller-level effective-cap assertion | ✓ SATISFIED | Truth 1; configmap line 22 + source script line 90; config.Load default 4; render + config tests green. |
| PREFLIGHT-02 | 39-02 | Project-level rollup marker hardened to milestone/phase exactly-once pattern; no double-count across TTL-GC | ✓ SATISFIED | Truths 2 & 3; RetryOnConflict + MergeFromWithOptimisticLock in project_controller.go; idempotency envtest green. |

No orphaned requirements: REQUIREMENTS.md maps PREFLIGHT-01 and PREFLIGHT-02 to Phase 39 and both appear in the plan `requirements` fields. Deploy (INFRA-02) and release (RELEASE-01) of these fixes are correctly scoped to Phases 35/39, not Phase 39.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| — | — | No `TBD`/`FIXME`/`XXX` debt markers in any phase-modified file | — | None — auditable |

The Phase 39 code review (`39-REVIEW.md`) found 0 critical, 2 warnings, 3 info — all quality/robustness items:
- **WR-01** (in-memory `PlannerRolledUpUID` left stale after the retry block) — consistent-by-design with all four marker sites (milestone/phase/plan/project); harmless within a single reconcile call. Not a BLOCKER.
- **WR-02** (narrow double-count window if the marker patch exhausts retries on a *non-conflict* error after RollUpUsage already incremented) — a pre-existing accepted tradeoff shared by milestone/phase/plan; the change closes the common *conflict* window, which is what the success criterion requires. Not a BLOCKER.
- IN-01..03 — informational (PREFLIGHT-01 is defense-in-depth since values.yaml already pinned 4; test diagnostic-excerpt cosmetics).

None of these block the phase goal; they document inconsistencies already present in the shipped milestone/phase/plan markers that this phase deliberately mirrors.

### Human Verification Required

None. All three success criteria are programmatically verifiable and were verified by direct render/test/envtest execution in this pass.

### Gaps Summary

No gaps. Both load-bearing v1.0.7 audit carry-ins are landed and independently verified in the codebase:

1. **PREFLIGHT-01** — the single-node OOM-safety default is corrected at the authoritative source (`hack/helm/augment-tide-chart.sh:90`) and the regenerated chart (`configmap.yaml:22`); `helm template` renders `plannerConcurrency: 4` with no `16`; the config-resolution path defaults to 4 and sizes the planner pool; pinned by a pure-Go test and a kind render contract test.
2. **PREFLIGHT-02** — the project-level `PlannerRolledUpUID` stamp is hardened to `RetryOnConflict` + re-fetch + `MergeFromWithOptimisticLock` + return-error-on-exhaustion, a line-for-line mirror of the milestone marker; the best-effort last-write-wins window is gone; a project-level envtest proves `CostSpentCents` is not double-counted across a reporter-Job TTL-GC cycle.

The run can be deployed and metered without a latent over-dispatch or a double-counted cap. The two code-review warnings are pre-existing, accepted tradeoffs inherited from the milestone/phase/plan pattern this phase intentionally replicates — not Phase-39 regressions and not goal blockers.

---

_Verified: 2026-06-29_
_Verifier: Claude (gsd-verifier)_
