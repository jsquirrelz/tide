---
phase: 25-global-dispatch-failure-semantics-gates-resumption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha2/shared_types.go
  - api/v1alpha2/project_types.go
  - config/crd/bases/tideproject.k8s_projects.yaml
  - internal/controller/failure_halt_test.go
  - test/integration/envtest/global_dispatch_test.go
  - cmd/tide/resume_failure_test.go
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, RESUME-01]

must_haves:
  truths:
    - "FailureProfile is a CEL-validated enum {strict|conservative} defaulting to strict on the Project CRD"
    - "ConditionFailureHalt / ReasonTaskFailedHalt / AnnotationFailureResumedAt vocabulary exists and compiles"
    - "RED test scaffolds for DISP-01/02/03 + RESUME-01 + resume-clear exist and FAIL (logic unimplemented)"
    - "The DISP-02 strict RED scaffold covers BOTH a later-wave non-dependent AND a same-global-wave CROSS-PLAN independent sibling (ROADMAP success criterion 2: independent siblings in the same global wave continue)"
    - "Wave 0 grep confirms whether coarse-ref DependsOn (Plan/Phase/Milestone names) appears in authored Task fixtures"
  artifacts:
    - path: "api/v1alpha2/project_types.go"
      provides: "FailureProfile field on ProjectSpec with kubebuilder enum + default markers"
      contains: "FailureProfile FailureProfileType"
    - path: "api/v1alpha2/shared_types.go"
      provides: "FailureProfileType enum + FailureHalt condition vocabulary"
      contains: "ConditionFailureHalt"
    - path: "internal/controller/failure_halt_test.go"
      provides: "RED unit tests for checkFailureHalt/setFailureHaltIfNeeded"
      contains: "func TestSetFailureHaltIfNeeded"
    - path: "test/integration/envtest/global_dispatch_test.go"
      provides: "RED Ginkgo suite for DISP-01/02/03/RESUME-01 incl. same-global-wave cross-plan sibling continuation"
      contains: "Label(\"phase25\")"
    - path: "cmd/tide/resume_failure_test.go"
      provides: "RED test for tide resume --retry-failed clearing FailureHalt"
      contains: "func TestResumeRunClearsFailureHalt"
  key_links:
    - from: "api/v1alpha2/project_types.go"
      to: "config/crd/bases/tideproject.k8s_projects.yaml"
      via: "make manifests regenerates CEL enum + default into CRD schema"
      pattern: "enum:[\\s-]*strict"
---

<objective>
Land the Phase 25 API vocabulary (the `FailureProfile` enum, the `FailureHalt` condition/reason/annotation constants) and author all RED test scaffolds the implementation plans (25-02, 25-03) must turn GREEN. This is the Wave 0 of Nyquist validation: every downstream task already has a failing automated check waiting for it.

Purpose: Tests cannot compile against types that do not exist, and Nyquist requires the failing checks to precede implementation. Landing the API surface first also lets `make manifests` regenerate the CRD CEL enum once, with no churn from later plans.
Output: `FailureProfile` field + `FailureProfileType` enum + `ConditionFailureHalt`/`ReasonTaskFailedHalt`/`AnnotationFailureResumedAt` constants; regenerated CRD; three RED test files; a recorded A1 grep finding that decides whether 25-02 must build the shared fan-out resolver.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-global-dispatch-failure-semantics-gates-resumption/25-CONTEXT.md
@.planning/phases/25-global-dispatch-failure-semantics-gates-resumption/25-RESEARCH.md
@.planning/phases/25-global-dispatch-failure-semantics-gates-resumption/25-PATTERNS.md
@.planning/phases/25-global-dispatch-failure-semantics-gates-resumption/25-VALIDATION.md

<interfaces>
Existing condition/reason vocabulary the new block mirrors (api/v1alpha2/shared_types.go ~199-216):
  - const ConditionBillingHalt = "BillingHalt"
  - const ReasonCreditBalanceTooLow = "CreditBalanceTooLow"
  - const AnnotationBillingResumedAt = "tideproject.k8s/billing-resumed-at"
Existing enum pattern (api/v1alpha2/shared_types.go ~47-64): GatePolicy string with `+kubebuilder:validation:Enum=auto;approve;pause`.
ProjectSpec last field (api/v1alpha2/project_types.go:376): `Git *GitConfig json:"git,omitempty"`.
Project label constant: internal/owner.LabelProject = "tideproject.k8s/project".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FailureProfile enum + FailureHalt condition vocabulary to the v1alpha2 API</name>
  <read_first>
    - api/v1alpha2/shared_types.go (the Phase 13 BillingHalt block ~199-216 and the Phase 14 block ~219-233 — the exact const-block + comment style to mirror; the GatePolicy enum ~47-64 for the +kubebuilder:validation:Enum marker shape)
    - api/v1alpha2/project_types.go (ProjectSpec ~300-377; the trailing `Git *GitConfig` field at line 376 is the insertion anchor)
  </read_first>
  <behavior>
    - FailureProfileType is a string type with two constants: FailureProfileStrict="strict", FailureProfileConservative="conservative".
    - ProjectSpec gains a FailureProfile field carrying `+kubebuilder:validation:Enum=strict;conservative`, `+kubebuilder:default=strict`, `+optional`, json tag `failureProfile,omitempty`.
    - Three new package-level constants exist: ConditionFailureHalt="FailureHalt", ReasonTaskFailedHalt="TaskFailedHalt", AnnotationFailureResumedAt="tideproject.k8s/failure-resumed-at".
  </behavior>
  <action>
    In api/v1alpha2/shared_types.go, after the Phase 14 block (end ~line 235, before the Phase 23 block at 237), add a "Phase 25 condition + reason vocabulary — task failure halt (DISP-02 conservative)" const block declaring `ConditionFailureHalt = "FailureHalt"`, `ReasonTaskFailedHalt = "TaskFailedHalt"`, `AnnotationFailureResumedAt = "tideproject.k8s/failure-resumed-at"` — mirror the Phase 13 BillingHalt block comment style exactly. In the same file add the `FailureProfileType string` type with the `+kubebuilder:validation:Enum=strict;conservative` marker and the two constants `FailureProfileStrict`/`FailureProfileConservative`. In api/v1alpha2/project_types.go, add the `FailureProfile FailureProfileType json:"failureProfile,omitempty"` field immediately after `Git *GitConfig` (line 376) with the three kubebuilder markers (Enum, default=strict, optional) per the PATTERNS.md project_types section. Do NOT name the type or field anything matching the verify-no-aggregates grep (`Schedule|Waves *\[\]|IndegreeMap|CachedDag|DerivedDag`) — `FailureProfile`/`FailureProfileType` are plain enums and are safe. Run `make manifests generate` to regenerate the CRD CEL enum + default and the deepcopy.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./api/... && grep -q 'ConditionFailureHalt = "FailureHalt"' api/v1alpha2/shared_types.go && grep -q 'FailureProfile FailureProfileType' api/v1alpha2/project_types.go && grep -nE 'enum:|strict' config/crd/bases/tideproject.k8s_projects.yaml | grep -q strict && make verify-no-aggregates</automated>
  </verify>
  <acceptance_criteria>
    - api/v1alpha2/shared_types.go contains `ConditionFailureHalt = "FailureHalt"`, `ReasonTaskFailedHalt = "TaskFailedHalt"`, `AnnotationFailureResumedAt = "tideproject.k8s/failure-resumed-at"`.
    - api/v1alpha2/shared_types.go contains `type FailureProfileType string` with `+kubebuilder:validation:Enum=strict;conservative` and constants `FailureProfileStrict`/`FailureProfileConservative`.
    - api/v1alpha2/project_types.go ProjectSpec contains `FailureProfile FailureProfileType` with `+kubebuilder:default=strict`.
    - config/crd/bases/tideproject.k8s_projects.yaml shows the `failureProfile` property with an enum constraining to strict/conservative and a default of strict.
    - `go build ./api/...` exits 0; `make verify-no-aggregates` exits 0.
  </acceptance_criteria>
  <done>The Project CRD validates FailureProfile to {strict|conservative}, defaults omitted to strict, and the FailureHalt vocabulary compiles. verify-no-aggregates stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Author RED unit + envtest + resume test scaffolds (Nyquist Wave 0) and record the A1 coarse-ref grep finding</name>
  <read_first>
    - internal/controller/billing_halt_test.go (whole file — the unit-test structure, `fakeSchemeWithAll`, fake-client-with-status-subresource fixture to mirror for failure_halt_test.go)
    - test/integration/envtest/indegree_test.go (whole file — `makeTask` ~306, BeforeEach/AfterEach cleanup, the Project/Plan fixture builders, `createSimplePlanInNS`/`makeBoundPVC` helpers to reuse)
    - cmd/tide/resume_test.go (whole file — `makeProject`, `testScheme`, the resumeRun(...) call signature, the WithStatusSubresource builder)
    - .planning/phases/25-global-dispatch-failure-semantics-gates-resumption/25-PATTERNS.md (the exact test-case lists and fixture excerpts for all three files)
    - .planning/phases/25-global-dispatch-failure-semantics-gates-resumption/25-VALIDATION.md (Wave 0 Requirements list + Per-Task Verification Map to fill)
  </read_first>
  <action>
    Create internal/controller/failure_halt_test.go (package controller) with the seven RED cases from PATTERNS.md: TestCheckFailureHalt_TrueWhenConditionPresent, TestCheckFailureHalt_FalseWhenConditionAbsent, TestCheckFailureHalt_FalseForNilProject, TestSetFailureHaltIfNeeded_StrictProfile_NoOp, TestSetFailureHaltIfNeeded_ConservativeStampsHalt, TestSetFailureHaltIfNeeded_IdempotentSecondCall, TestSetFailureHaltIfNeeded_NilProject_NoOp — referencing `checkFailureHalt`/`setFailureHaltIfNeeded` which do not yet exist (RED via compile-then-fail in 25-03). Create test/integration/envtest/global_dispatch_test.go (package envtest_integration) with a `Describe("Phase 25 ...", Label("envtest","phase25"))` containing RED specs labeled DISP-01 (cross-plan dependent stays non-Running until predecessor Succeeds), DISP-02 strict — TWO scenarios required: (a) a failed task's independent LATER-wave non-dependent still dispatches and its dependent never does; AND (b) a SAME-GLOBAL-WAVE CROSS-PLAN independent sibling continues: two Tasks in the same global wave (wave index 0, no deps) but DIFFERENT plans, one fails, assert the other still dispatches/continues independently (ROADMAP success criterion 2 — proves siblings in the same global wave continue at GLOBAL scope with cross-plan tasks, not merely within-plan siblings; the indegree model handles this by design but the spec requires the test to prove it cross-plan), DISP-02 conservative (first failure stamps ConditionFailureHalt; all new dispatch frozen), DISP-03 (task gate `approve` holds a globally-ready task while a non-dependent flows), RESUME-01 (A→B→C cross-plan; status-patch A,B to Succeeded; assert C dispatches with no new persisted field) — use the `makeGlobalTask` cross-plan helper from PATTERNS.md. Create cmd/tide/resume_failure_test.go (package main) with TestResumeRunClearsFailureHalt (retryFailed=true clears FailureHalt + output mentions FailureHalt) and TestResumeWithoutRetryFailedLeavesFailureHalt (retryFailed=false leaves it True). Then run the A1 grep `grep -rn "dependsOn:" test/ config/samples/ --include="*.yaml"` and `grep -rn "DependsOn:" test/integration internal/controller --include="*_test.go"` to determine whether any authored Task fixture uses a Plan/Phase/Milestone name (coarse ref) as a dep vs only Task names; write the finding (coarse-refs PRESENT or ABSENT, with file:line evidence) into the SUMMARY so 25-02 knows whether the shared depgraph resolver is load-bearing for current fixtures. Note: regardless of the grep result, 25-02 still builds the shared resolver because Plan/Phase/Milestone-level DependsOn carriers (assembleProjectDepGraph 6b/6c/6d) are part of the global contract — the grep only decides whether existing fixtures already exercise it. Fill the VALIDATION.md Per-Task Verification Map rows and flip its Wave 0 checkboxes; set `wave_0_complete: true` and `nyquist_compliant: true` in its frontmatter.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go vet ./internal/controller/... ./cmd/tide/... 2>&1 | head; test -f internal/controller/failure_halt_test.go && test -f test/integration/envtest/global_dispatch_test.go && test -f cmd/tide/resume_failure_test.go && grep -q 'Label("envtest", "phase25")' test/integration/envtest/global_dispatch_test.go && grep -q 'func TestResumeRunClearsFailureHalt' cmd/tide/resume_failure_test.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/controller/failure_halt_test.go exists, package controller, contains all seven TestCheckFailureHalt_*/TestSetFailureHaltIfNeeded_* functions referencing `checkFailureHalt` and `setFailureHaltIfNeeded`.
    - test/integration/envtest/global_dispatch_test.go exists with `Label("envtest", "phase25")` and specs tagged DISP-01, DISP-02, DISP-03, RESUME-01.
    - The DISP-02 strict block contains TWO scenarios: a later-wave non-dependent continuation AND a same-global-wave CROSS-PLAN independent sibling continuation (two no-dep Tasks at wave index 0 in DIFFERENT plans; one fails; the other continues). Both are RED (logic in 25-02/25-03 turns them GREEN).
    - cmd/tide/resume_failure_test.go exists with TestResumeRunClearsFailureHalt and TestResumeWithoutRetryFailedLeavesFailureHalt.
    - The tests are RED: `go test ./internal/controller/... -run TestSetFailureHaltIfNeeded` fails to build or fails (logic not yet implemented) — this is expected and recorded in the SUMMARY.
    - The SUMMARY records the A1 finding (coarse-ref DependsOn PRESENT/ABSENT in fixtures, with file:line evidence).
    - .planning/phases/.../25-VALIDATION.md has `wave_0_complete: true` and `nyquist_compliant: true`, Wave 0 checkboxes ticked, Per-Task map rows filled.
  </acceptance_criteria>
  <done>All three test scaffolds exist and are RED, the A1 grep finding is recorded, and VALIDATION.md is marked Nyquist-compliant with Wave 0 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Project.Spec | A human (or LLM planner) sets `failureProfile`; an out-of-enum value would silently change dispatch semantics if unvalidated |
| CRD schema → controller | The CEL enum is the only thing preventing a bogus profile string from reaching the dispatch gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | `Project.Spec.FailureProfile` | mitigate | `+kubebuilder:validation:Enum=strict;conservative` rejects any other value at admission; `+kubebuilder:default=strict` ensures an omitted field is never empty/ambiguous. Verified by the regenerated CRD enum in the verify command. |
| T-25-01-02 | Spoofing | empty/unset FailureProfile interpreted as conservative | accept | Default=strict means unset ⇒ strict (fail-open to *running*, which is the documented default behavior, not a security downgrade). Conservative is opt-in; no path makes unset ⇒ conservative. |
| T-25-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure Go API + tests, zero new dependencies. RESEARCH.md Package Legitimacy Audit: N/A. |
</threat_model>

<verification>
- `go build ./api/...` and `go vet ./internal/controller/... ./cmd/tide/...` compile.
- `make manifests` regenerated CRD carries the failureProfile enum + default.
- `make verify-no-aggregates`, `make verify-no-sqlite-dep`, `make verify-dag-imports` stay green (no aggregate types, no new deps, pkg/dag untouched).
- The three test files are RED (referenced symbols unimplemented) — confirmed and recorded.
</verification>

<success_criteria>
- FailureProfile enum + FailureHalt vocabulary exist and the CRD validates them.
- RED scaffolds for DISP-01/02/03 + RESUME-01 + resume-clear exist (Nyquist Wave 0 satisfied).
- A1 coarse-ref grep finding recorded to steer 25-02's shared-resolver scope.
- All four guards (verify-no-aggregates/-sqlite-dep/-dag-imports, metric cardinality) green.
</success_criteria>

<output>
Create `.planning/phases/25-global-dispatch-failure-semantics-gates-resumption/25-01-SUMMARY.md` when done. Record the A1 coarse-ref finding explicitly.
</output>
