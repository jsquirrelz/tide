---
phase: 13-dispatch-image-resolution-provider-halt
plan: 06
type: execute
wave: 3
depends_on: [13-02, 13-03, 13-04]
gap_closure: true
autonomous: true
requirements: [HALT-01, DISPATCH-02]
files_modified:
  - internal/controller/billing_halt_regression_test.go
  - test/integration/kind/testdata/three-task-wave.yaml
  - test/integration/kind/testdata/chaos-resume-three-task.yaml
  - test/integration/kind/output_test.go
  - charts/tide/templates/deployment.yaml
  - test/integration/kind/projects_pvc_test.go

must_haves:
  truths:
    - "The four planner-level BillingHalt hold specs are non-vacuous: reconcilers enter the dispatch body (Dispatcher wired), the hold's 30s requeue is asserted, and a control spec proves a Job IS created once the halt clears — deleting checkBillingHalt now fails the suite (HALT-01 regression honesty)"
    - "three-task-wave.yaml, chaos-resume-three-task.yaml, and output_test.go's inline Task pass CRD admission (spec.promptPath present, required since b612fce)"
    - "helm template fails at render time with a named error when subagent.defaults.image is empty (WR-04 — 'empty image is a config error' enforced at the cheapest place)"
  artifacts:
    - path: "internal/controller/billing_halt_regression_test.go"
      provides: "Dispatcher-wired newBH* helpers + 30s-requeue assertions + per-level halt-cleared control specs"
      contains: "stubDispatcher"
    - path: "test/integration/kind/testdata/three-task-wave.yaml"
      provides: "promptPath on tasks alpha/beta/gamma"
      contains: "promptPath"
    - path: "test/integration/kind/testdata/chaos-resume-three-task.yaml"
      provides: "promptPath on all Task specs"
      contains: "promptPath"
    - path: "charts/tide/templates/deployment.yaml"
      provides: "required guard on subagent.defaults.image"
      contains: "required"
  key_links:
    - from: "billing_halt_regression_test.go newBH* helpers"
      to: "reconciler dispatch bodies"
      via: "Dispatcher: &stubDispatcher{} (the r.Dispatcher != nil outer gate)"
      pattern: "Dispatcher:\\s*&stubDispatcher\\{\\}"
    - from: "projects_pvc_test.go"
      to: "charts/tide/templates/deployment.yaml required guard"
      via: "helm template --set subagent.defaults.image=\"\" must fail"
      pattern: "subagent.defaults.image"
---

<objective>
Close the test-debt half of 13-VERIFICATION.md gap 3 plus the trivially-adjacent review items
the work order green-lights:

1. **promptPath fixture debt (DISPATCH-02 / gap 3):** testdata/three-task-wave.yaml (tasks
   alpha/beta/gamma), chaos-resume-three-task.yaml, and output_test.go's inline Task lack
   `spec.promptPath` — Required + MinLength=1 since b612fce (2026-06-08); admission rejects the
   apply, so 3 of the 4 Layer B failures are mechanical fixture debt.
2. **WR-01 (HALT-01, trivially adjacent):** the four planner-level BillingHalt hold specs are
   vacuous — newBH{Milestone,Phase,Plan,Project}Reconciler never set Dispatcher, so the
   reconcilers exit at the `r.Dispatcher != nil` outer gate (milestone_controller.go:175 and
   siblings) before checkBillingHalt is ever reachable. The suite stays green with all four
   gates deleted.
3. **WR-04 (DISPATCH-02, trivially adjacent):** `--set subagent.defaults.image=""` renders the
   garbage env `":<appVersion>"` (InvalidImageName at runtime) and an overridden-away block
   passes nil to regexMatch (cryptic sprig error). One-line `required` fix at render time.

WR-05 (empty-image contract at the 6 Go dispatch sites) is deliberately NOT taken: it is a
6-site signature-adjacent change with per-level failure-surface decisions — not trivially
adjacent. It remains a tracked warning.

Runs in wave 3 parallel with 13-05: zero file overlap (13-05 owns the controllers,
billing_halt.go/_test.go, resume, credproxy; this plan owns only the regression-test file,
kind fixtures, and the chart template).

Purpose: HALT-01's regression suite becomes honest; the mechanical Layer B failures are fixed
so 13-07's single heavy gate run isolates the one unattributed reporter failure.
Output: de-vacuated regression specs, admitted fixtures, render-time chart guard + contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-VERIFICATION.md

<interfaces>
From internal/controller (extracted — do not re-explore):
```go
// All four planner reconcilers gate their ENTIRE dispatch body on:
if r.Dispatcher != nil { return r.reconcilePlannerDispatch(ctx, &obj) }
// milestone_controller.go:175, phase_controller.go:165, plan_controller.go:167, project_controller.go:234
// Job creation inside the body is r.Create(ctx, job) — stubDispatcher only opens the gate.
// The BillingHalt hold returns ctrl.Result{RequeueAfter: 30 * time.Second}.
```
`stubDispatcher` already exists in the controller test package and is wired the same way in
milestone_gates_test.go:116 and dispatch_image_test.go:141,236 — copy that wiring.

From b612fce (PromptPath contract): TaskSpec.PromptPath is Required, MinLength=1, the
PVC-relative path of the originating `children/task-NN.json` artifact (production: materializer
copies child.SourcePath). Kind Layer B tasks run the stub subagent (dev.testMode) which never
reads the file — the value must merely match the production shape.

From charts/tide/templates/deployment.yaml:59-65:
```yaml
{{- $img := .Values.subagent.defaults.image }}
{{- if or (regexMatch ":[^/]+$" $img) (contains "@" $img) }} ... {{- else }} printf "%s:%s" $img .Chart.AppVersion ...
```
Existing helm-template contract tests: test/integration/kind/projects_pvc_test.go
(TestHelmDeploymentTemplateDropsSubagentImageFlag :171,
TestHelmDeploymentTemplateSubagentImageEnvFromDefaults :184) — follow their helm-template
invocation pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 — de-vacuate the four planner-level BillingHalt hold specs</name>
  <files>internal/controller/billing_halt_regression_test.go</files>
  <read_first>internal/controller/billing_halt_regression_test.go:100-320 (the four hold specs) and :700-760 (the newBH* helpers), internal/controller/milestone_gates_test.go:110-125 (canonical Dispatcher-wired reconciler construction)</read_first>
  <behavior>
    - Each of the four hold specs (milestone/phase/plan/project): with BillingHalt=True, the
      final Reconcile returns RequeueAfter == 30*time.Second (proves checkBillingHalt FIRED,
      not merely that no Job appeared) AND no planner Job labelled with the parent UID exists.
    - Control spec per level: remove the BillingHalt condition from the Project, re-reconcile,
      assert a planner Job IS created (List + parent-uid label match). This is the
      non-vacuousness proof — it fails if the reconciler never enters the dispatch body.
  </behavior>
  <action>
    Wire `Dispatcher: &stubDispatcher{}` into newBHMilestoneReconciler, newBHPhaseReconciler,
    newBHPlanReconciler, newBHProjectReconciler (mirror milestone_gates_test.go:116; keep the
    existing SigningKey/CredproxyImage/HelmProviderDefaults/EnvReader fields). Fix the stale
    helper comment ("Uses testSigningKey so the dispatch guard passes") to name BOTH gates:
    Dispatcher != nil outer gate + SigningKey. Capture the Reconcile result in each hold spec
    and assert RequeueAfter == 30s. Add the per-level halt-cleared control specs (a shared
    helper that clears the condition via mgrClient status patch and asserts Job creation is
    fine — keep it readable). Expect each control spec's Job create to succeed in envtest: the
    halt projects have no ProviderSecretRef (secret lookup skipped) and HelmProviderDefaults
    carries testSubagentImage. Clean up created Jobs in AfterEach so assertNoJobForParent in
    sibling specs stays valid. Mutation honesty check (manual, during development, not
    committed): commenting out one checkBillingHalt call must turn that level's hold spec red.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestAPIs -count=1 2>&1 | tail -20 (envtest suite green incl. 4 strengthened holds + 4 control specs)</automated>
  </verify>
  <done>All 8 specs green; RequeueAfter asserted in holds; control specs prove dispatch-body entry; helper comment corrected.</done>
</task>

<task type="auto">
  <name>Task 2: promptPath fixture debt + WR-04 render-time image guard</name>
  <files>test/integration/kind/testdata/three-task-wave.yaml, test/integration/kind/testdata/chaos-resume-three-task.yaml, test/integration/kind/output_test.go, charts/tide/templates/deployment.yaml, test/integration/kind/projects_pvc_test.go</files>
  <read_first>test/integration/kind/testdata/three-task-wave.yaml (Task specs), test/integration/kind/testdata/chaos-resume-three-task.yaml (Task specs), test/integration/kind/output_test.go (inline exceed-output Task), test/integration/kind/projects_pvc_test.go:149-200 (helm-template test pattern), charts/tide/templates/deployment.yaml:55-66</read_first>
  <action>
    (1) Add `promptPath` to every Task spec missing it, values matching the production
    PromptPath shape (PVC-relative children artifact path per b612fce): three-task-wave.yaml
    alpha → `children/task-01.json`, beta → `children/task-02.json`, gamma →
    `children/task-03.json`; chaos-resume-three-task.yaml tasks analogously numbered;
    output_test.go's inline exceed-output Task gets `PromptPath: "children/task-01.json"` in
    its TaskSpec literal. The stub subagent (dev.testMode) never reads the file — the field
    only needs to pass admission with a production-shaped value. Do NOT touch other fixtures
    (bare-project/push-lease/up-stack have no Task objects or already pass).

    (2) WR-04: in deployment.yaml change the assignment to
    `{{- $img := required "subagent.defaults.image must be a non-empty image ref (Phase 13 D-01: the chart dispatches the real subagent by default; set it explicitly or use the documented stub opt-in)" .Values.subagent.defaults.image }}`
    — render-time enforcement of the "empty image is a config error" contract. This also fixes
    the nil-to-regexMatch sprig crash when a user values file replaces the subagent block
    without defaults.image. values.yaml is NOT touched (fixed contract; its shipped default is
    non-empty so default renders are unaffected).

    (3) Add a plain go contract test in projects_pvc_test.go following the existing
    TestHelmDeploymentTemplate* pattern: `helm template` with
    `--set subagent.defaults.image=""` must FAIL and stderr must contain
    "subagent.defaults.image must be a non-empty image ref"; and the default render (no
    overrides) must still succeed.
  </action>
  <verify>
    <automated>go test ./test/integration/kind/ -run 'TestHelmDeploymentTemplate' -count=1 AND go vet ./test/integration/kind/ AND helm template charts/tide >/dev/null (default render unaffected)</automated>
  </verify>
  <done>All three fixture surfaces carry promptPath; empty-image render fails with the named message; default render green; new contract test green. (Cluster-backed proof of fixture admission lands in 13-07's single heavy run — do NOT spin a kind cluster here; constrained-VM one-heavy-run rule.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helm values → rendered manifests | operator-supplied config crosses into pod specs |
| test suite → regression confidence | vacuous specs misrepresent production gate coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-G5 | Repudiation | billing_halt_regression_test.go | mitigate | Dispatcher wiring + 30s-requeue + control specs make the HALT-01 regression suite fail when the gates are removed |
| T-13-G6 | DoS | chart empty-image render | mitigate | `required` guard moves InvalidImageName from runtime Job failure to render-time named error (WR-04) |
| T-13-G7 | Tampering | fixture promptPath values | accept | stub subagent never reads the file; values are production-shaped per b612fce contract |
</threat_model>

<verification>
- envtest controller suite green with 8 (4 hold + 4 control) non-vacuous billing specs.
- `grep -c "promptPath" test/integration/kind/testdata/three-task-wave.yaml` returns 3;
  chaos fixture ≥ its task count; output_test.go inline Task has PromptPath.
- `helm template charts/tide --set subagent.defaults.image=""` exits non-zero with the named message.
</verification>

<success_criteria>
- WR-01 closed (HALT-01's planner-level regression honest), the 3 mechanical Layer B fixture
  failures fixed, WR-04 closed — leaving 13-07 a clean single-variable reporter diagnosis.
- charts/tide/values.yaml untouched (fixed contract).
</success_criteria>

<output>
Create `.planning/phases/13-dispatch-image-resolution-provider-halt/13-06-SUMMARY.md` when done.
</output>
