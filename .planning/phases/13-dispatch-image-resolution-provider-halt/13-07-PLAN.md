---
phase: 13-dispatch-image-resolution-provider-halt
plan: 07
type: execute
wave: 4
depends_on: [13-05, 13-06]
gap_closure: true
autonomous: true
requirements: [DISPATCH-02]
files_modified:
  - test/integration/kind/reporter_pod_test.go

must_haves:
  truths:
    - "The reporter_pod_test.go:196 materialization failure has ASSIGNED PROVENANCE (root-caused from runtime evidence, not guessed) and either a root-cause code fix with regression coverage or evidence-backed proof it was environmental"
    - "Full `make test-int` is green on a fresh kind cluster: MAKE_EXIT=0 AND zero `^--- FAIL|^FAIL ` lines in the log (the 13-03 documented success criterion / D-02 deliverable, finally true)"
  artifacts:
    - path: "/tmp/13-07-test-int-full.log"
      provides: "the single heavy gate run's evidence: both Ginkgo layer summaries + MAKE_EXIT echo"
  key_links:
    - from: "make test-int"
      to: "MAKE_EXIT=0"
      via: "set -e single-shell recipe (Layer A envtest + Layer B kind + plain go contract tests)"
      pattern: "MAKE_EXIT=0"
---

<objective>
Close the remainder of 13-VERIFICATION.md gap 3: root-cause the UNATTRIBUTED
reporter-materialization failure, then prove the full suite green with ONE heavy run.

The failing assertion: reporter_pod_test.go:196 — the reporter Job spawns but no child
Milestone owned by the project UID materializes within 4m ("total in ns: 0" across 3
flake-attempts). Phase 13 did not touch reporter or stub-subagent code (last change debug-17 /
Phase 9), and the stub image wiring traces correctly — but it was never positively proven green
pre-phase-13 and blocks the documented green-suite criterion.

This is a diagnose-THEN-fix task: per CLAUDE.md Observe First and the cascade-5/cascade-8
lessons, read the runtime artifacts BEFORE hypothesizing or reading reconciler source. Do not
guess; do not defer (owner's standing root-cause-over-defer preference).

Depends on both wave-3 plans: 13-05's controller fixes and 13-06's fixture/test fixes must be
merged so the final gate run measures main with all gap closures in, and so the reporter spec
is the only remaining variable.

Purpose: DISPATCH-02's green-suite criterion holds with evidence; the phase's last gap closes.
Output: provenance + fix for the reporter failure; one full green `make test-int` log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-VERIFICATION.md

Constrained-VM recipe (CLAUDE.md, binding): the dev Docker VM is ~7.65 GiB. Fresh kind cluster
per heavy run, ONE heavy run at a time, never with another cluster up
(`kind get clusters` must show at most tide-test). `make test-int-kind-prep` builds the 5
images and creates tide-test if absent — delete the cluster first for a truly fresh run.

Exit discipline (CLAUDE.md, binding): `make test-int` bundles plain go-tests with Ginkgo specs;
one RED go-test fails the package even when both layers print SUCCESS. Always read the echoed
MAKE_EXIT AND grep `'^--- FAIL|^FAIL\s'` — never trust the Ginkgo summary alone.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose the reporter materialization failure from runtime evidence, then fix at root cause</name>
  <files>test/integration/kind/reporter_pod_test.go (plus root-cause files TBD by diagnosis — candidates: images/tide-reporter/, images/stub-subagent/, internal/controller/project_controller.go reporter path)</files>
  <read_first>test/integration/kind/reporter_pod_test.go (full spec: setup, what the stub planner is expected to write, the :196 assertion)</read_first>
  <action>
    REPRO + EVIDENCE FIRST (no source reading until the artifacts are in hand):
    (1) Check leftover evidence from the failed run if still present (the orchestrator exported
    kind logs to /var/folders/.../kind-logs-tide-test during the failed run — check
    `ls /var/folders/*/T/kind-logs-tide-test* 2>/dev/null` and /tmp for the prior full-run log);
    a fresh repro is expected to be needed.
    (2) Fresh targeted repro: `kind delete cluster --name tide-test` (clean-rerun sequence —
    allowed), `make test-int-kind-prep`, then run ONLY the reporter spec:
    `timeout 30m go test ./test/integration/kind/ -v -timeout=20m -ginkgo.v -ginkgo.focus='<focus string matching the reporter Describe text>' 2>&1 | tee /tmp/13-07-reporter-repro.log`
    (grep reporter_pod_test.go for the exact Describe/It text first).
    (3) While the spec runs/fails, collect the evidence chain in order: manager log
    (`kubectl logs -n tide-system deploy/<manager> | grep -iE 'reporter|milestone|<test-ns>'`),
    the stub planner Job's pod log in the test namespace (did it write out.json with a
    Milestone ChildCRD?), the reporter Job's pod log (did it read out.json? what did it POST?
    any RBAC denial?), `kubectl get milestones -n <test-ns> -o yaml`,
    `kubectl describe job -n <test-ns>`, and PVC content if reachable
    (kubectl exec / debug pod on the workspace PVC).
    (4) Assign provenance with git archaeology once evidence points somewhere:
    `git log -S '<token>' -- <file>` — hypotheses from the work order (do NOT anchor on one):
    same fixture class as the promptPath debt, Phase-12 descent-hold interaction with the
    reporter path, stub envelope/ChildCRD shape drift, or pre-existing Phase-9 reporter flake.
    (5) FIX at root cause: if a code defect, fix it where it lives and add/strengthen
    regression coverage reproducing the symptom; if the defect is in the test harness itself,
    fix the harness; if genuinely environmental (e.g. image-load/timing on the constrained VM),
    prove it with TWO consecutive green focused runs on fresh clusters and document the
    evidence in the SUMMARY — "flaky/pre-existing" is a claim that requires this proof, not a
    waiver (CLAUDE.md: a dismissal is a claim, not verification).
    (6) Verify the fix with one more focused reporter run on the cluster (green), then
    `kind delete cluster --name tide-test` to leave a clean slate for Task 2.
  </action>
  <verify>
    <automated>grep -E 'Milestone .* materialized|SUCCESS' /tmp/13-07-reporter-repro-fixed.log (focused reporter run green post-fix) AND go build ./...</automated>
  </verify>
  <done>Provenance assigned and recorded (which commit/phase introduced it, or evidence-backed environmental attribution); root-cause fix landed with regression coverage (or two-green-runs proof); focused reporter spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Final gate — ONE full `make test-int` on a fresh cluster, MAKE_EXIT=0, zero FAIL lines</name>
  <files>(none — gate run; fallout fixes only if the run is red)</files>
  <action>
    Preconditions: `kind get clusters` shows no leftover clusters (delete tide-test from Task 1
    if still up; NEVER run while any other cluster exists — OOM exit-137 class). Working tree
    is merged main containing 13-05, 13-06, and Task 1's fix.
    Run the single heavy gate:
    `kind delete cluster --name tide-test 2>/dev/null; make test-int 2>&1 | tee /tmp/13-07-test-int-full.log; echo "MAKE_EXIT=$?" | tee -a /tmp/13-07-test-int-full.log`
    (test-int-kind-prep inside the target recreates the cluster and pre-loads images).
    Evidence checks, all four required:
    (a) `tail -5 /tmp/13-07-test-int-full.log` shows MAKE_EXIT=0;
    (b) `grep -nE '^--- FAIL|^FAIL\s' /tmp/13-07-test-int-full.log` returns nothing;
    (c) both layer summaries present: `grep -c 'Ran .* Specs in' /tmp/13-07-test-int-full.log` ≥ 2 with SUCCESS! on each;
    (d) the gap symptoms return 0 in this log: `grep -c 'no Milestone owned by project UID' /tmp/13-07-test-int-full.log` shows the assertion no longer FAILS (polling lines may appear; the spec must not be in the FAIL list) and no promptPath admission rejections (`grep -ci 'promptPath' /tmp/13-07-test-int-full.log` only in passing context).
    If the run is red: this is fallout, not a new phase — read the log + kind artifacts first
    (Observe First), fix forward within this plan, fresh cluster, re-run ONCE more. If a second
    full run is still red with a NEW failure class, stop and surface (don't brute-force a third
    blind run).
  </action>
  <verify>
    <automated>tail -1 /tmp/13-07-test-int-full.log | grep -c '^MAKE_EXIT=0' returns 1 AND grep -cE '^--- FAIL|^FAIL ' /tmp/13-07-test-int-full.log returns 0</automated>
  </verify>
  <done>13-VERIFICATION.md gap 3's truth is observable: full make test-int green (MAKE_EXIT=0, zero FAIL lines, both layers SUCCESS) on a fresh cluster with all gap-closure fixes merged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub planner out.json → reporter Job → K8s API | cross-namespace child-CR materialization path under diagnosis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-G8 | Repudiation | unattributed test failure | mitigate | evidence-first diagnosis + git -S provenance; environmental claims require two-green-runs proof |
| T-13-G9 | DoS | constrained VM during heavy runs | mitigate | fresh-cluster-per-run, one heavy run at a time, no concurrent clusters (CLAUDE.md recipe) |
</threat_model>

<verification>
- Provenance recorded in 13-07-SUMMARY.md with the evidence chain (logs/git refs).
- /tmp/13-07-test-int-full.log: MAKE_EXIT=0, zero FAIL lines, ≥2 Ginkgo SUCCESS summaries.
</verification>

<success_criteria>
- DISPATCH-02's documented green-suite criterion met with evidence (not claimed).
- All three 13-VERIFICATION.md gaps closed across plans 13-05/06/07.
</success_criteria>

<output>
Create `.planning/phases/13-dispatch-image-resolution-provider-halt/13-07-SUMMARY.md` when done.
</output>
