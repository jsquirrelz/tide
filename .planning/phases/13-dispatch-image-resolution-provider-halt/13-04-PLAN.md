---
phase: 13-dispatch-image-resolution-provider-halt
plan: 04
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/controller/task_controller.go
  - internal/controller/task_gates_test.go
  - internal/controller/billing_halt_regression_test.go
autonomous: true
requirements: [HALT-01]
must_haves:
  truths:
    - "While BillingHalt=True on the Project, zero new Jobs are dispatched at any of the five levels — affected levels PARK (status unchanged, requeue), never cascade to Failed"
    - "A failed Job whose envelope Reason carries the billing signature sets BillingHalt=True on exactly the owning Project (reconciler backstop, D-04 layer 2)"
    - "Run-1 regression: first billing-classified failure → BillingHalt on Project → sibling dispatch held → condition cleared (resume semantics) → dispatch resumes"
  artifacts:
    - path: "internal/controller/task_controller.go"
      provides: "BillingHalt dispatch-entry hold in gateChecks (third hold after CheckRejected + parent-approval) + backstop call in handleJobCompletion"
      contains: "checkBillingHalt"
    - path: "internal/controller/billing_halt_regression_test.go"
      provides: "Envtest regression encoding the run-1 fan-out-burn symptom end to end"
  key_links:
    - from: "internal/controller/*_controller.go (five dispatch gates)"
      to: "checkBillingHalt (billing_halt.go, plan 13-02)"
      via: "dispatch-entry hold before pool acquire / Job create"
      pattern: "checkBillingHalt\\(project\\)"
    - from: "internal/controller/task_controller.go handleJobCompletion"
      to: "setBillingHaltIfNeeded"
      via: "failed-envelope classification (out.Reason)"
      pattern: "setBillingHaltIfNeeded"
---

<objective>
Wire HALT-01 into the five reconcilers: the BillingHalt dispatch-entry hold (D-05 — the
third hold in the Phase 12 family after CheckRejected and checkParentApproval, composable
with both) and the reconciler backstop that classifies billing failures from failed-Job
envelopes (D-04 layer 2 — the cheaply-testable layer). Ends with the run-1 regression test:
billing failure → project-wide halt → park → resume → dispatch resumes.

Spec contract preserved: this is a dispatch-entry hold, not a failure state — wave-boundary
failure semantics (siblings continue, dependents never dispatch) are untouched; parked
levels keep their Status.Phase.

Output: gates + backstop in five controllers, per-level hold specs, end-to-end regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-CONTEXT.md
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-RESEARCH.md
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-PATTERNS.md
@.planning/phases/12-gate-semantics-reject-resume/12-CONTEXT.md

<interfaces>
From plan 13-02 (wave 1, internal/controller/billing_halt.go):
- `checkBillingHalt(project *tideprojectv1alpha1.Project) bool` — nil-safe gate predicate
- `isBillingFailureReason(reason string) bool` — "credit balance" substring / "billing-halt:" prefix
- `setBillingHaltIfNeeded(ctx, c client.Client, project, reason string) error` — condition writer
- Constants: tideprojectv1alpha1.ConditionBillingHalt, ReasonCreditBalanceTooLow

Verified gate/backstop anchors (line numbers pre-13-01; re-locate by function name after merge):
- task_controller.go gateChecks: CheckRejected :314 → checkParentApproval hold :326-333 (5s requeue) → BudgetExceeded gate :335-348. INSERT the BillingHalt hold between parent-approval and budget (RESEARCH Pitfall 2: before pool acquire, before Job create).
- phase_controller.go :277 / plan_controller.go :287 — checkParentApproval holds; :290/:300 earlyProject CheckRejected blocks (the dispatch-path reject holds to sit beside).
- milestone_controller.go :301 earlyProject CheckRejected (milestone has no parent-approval hold — it is the top planner level under project).
- project_controller.go — project-level planner dispatch (Step 8/9 region ~:981); the reconciled object IS the project: gate is checkBillingHalt(project) directly before BuildOptions.
- Backstop sites (each reconciler's envelope interpretation): task_controller.go handleJobCompletion :738+ (reads out via EnvReader; failure branch at `out.ExitCode != 0 || out.Result == "cap-hit" ...` ~:805); milestone_controller.go handleJobCompletion :428+ (envelope tiny-status read, envReadOK flag ~:462-478); phase :395+ and plan :448+ have the same termination-message tiny-status pattern; project_controller.go planner-envelope handling (reason from env.Reason, analog at :648 for the push path — the planner path is separate; locate it by EnvelopeOut usage).
- Park pattern (PATTERNS.md): requeue ctrl.Result{RequeueAfter: 30 * time.Second} WITHOUT changing Status.Phase (billing recovery is operator-paced; 30s vs the 5s approval hold). Dogfood finding "status flapping": do NOT stamp per-level conditions on every held reconcile — log at V(1) and requeue; the operator-visible signal is the single Project condition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BillingHalt dispatch-entry hold at all five levels (RED-first)</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/task_controller.go, internal/controller/task_gates_test.go, internal/controller/billing_halt_regression_test.go</files>
  <read_first>internal/controller/task_controller.go (gateChecks :275-360 — hold ordering), internal/controller/billing_halt.go (helpers from 13-02), internal/controller/task_gates_test.go (existing reject/approval hold specs — fixture shape to mirror), the dispatch regions of the four planner controllers (anchors above), .planning/phases/12-gate-semantics-reject-resume/12-CONTEXT.md (D-05 park-not-fail, D-06 resume)</read_first>
  <behavior>
    - task_gates_test.go: Task with BillingHalt=True on its Project → gateChecks returns shouldHalt with a 30s requeue, Task Status.Phase unchanged (no "Failed", no new Task condition), no Job created
    - billing_halt_regression_test.go (per-level holds): for each planner level (milestone, phase, plan, project), a ready-to-dispatch object whose Project carries BillingHalt=True → Consistently no planner Job appears; object's Status.Phase unchanged (PARK, not fail)
  </behavior>
  <action>
    RED specs first (mirror the existing reject-hold/approval-hold envtest specs — find them via `grep -rn 'CheckRejected\|AwaitingApproval' internal/controller/*_test.go`).

    Then insert the hold (D-05) — same shape at every site, composable with the existing holds:
    1. task_controller.go gateChecks: after the checkParentApproval hold, before the BudgetExceeded gate — `if checkBillingHalt(project)` → V(1) log "dispatch held: project billing halt" + return taskGateResult{shouldHalt: true, result: ctrl.Result{RequeueAfter: 30 * time.Second}}.
    2. milestone/phase/plan controllers: in the planner-dispatch path, immediately after the existing earlyProject CheckRejected block (and after the parent-approval hold where present), with the resolved project: same check → log + return ctrl.Result{RequeueAfter: 30 * time.Second} (signature-appropriate: the dispatch helper functions return (ctrl.Result, bool, error) in some controllers — match each site's contract; the hold means "no dispatch this pass", never an error, never a Failed patch).
    3. project_controller.go: directly before the Step-8/9 planner BuildOptions, `if checkBillingHalt(project)` → requeue 30s.
    4. Position invariant at every site: BEFORE pool/slot acquisition and BEFORE Job creation (Pitfall 2). Do NOT touch in-flight Job handling, wave-boundary failure semantics, or Status.Phase. Comment each insertion: "Phase 13 HALT-01 / D-05: third dispatch-entry hold (after CheckRejected + parent-approval); park, never fail; cleared by tide resume."
  </action>
  <verify>
    <automated>go test ./internal/controller/... -count=1 -timeout 600s</automated>
  </verify>
  <done>All five hold specs green; existing reject/approval/budget gate specs untouched and green</done>
  <acceptance_criteria>
    - `grep -c 'checkBillingHalt(project)' internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go internal/controller/project_controller.go internal/controller/task_controller.go | awk -F: '{s+=$2} END {print s}'` returns 5
    - Every insertion precedes the site's Job-create/pool-acquire code path (manual diff review against gateChecks ordering)
    - No insertion writes Status.Phase or a per-level Condition (park = log + requeue only)
    - `go test ./internal/controller/... -count=1` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconciler backstop classification + run-1 end-to-end regression (RED-first)</name>
  <files>internal/controller/task_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/billing_halt_regression_test.go</files>
  <read_first>internal/controller/task_controller.go (handleJobCompletion :738-845 — failure branch + project in scope), internal/controller/milestone_controller.go (handleJobCompletion :428-505 — envReadOK pattern), the phase/plan termination-message tiny-status regions (:395+/:448+), internal/controller/project_controller.go (planner envelope handling — locate via EnvelopeOut usage), internal/controller/billing_halt.go (setBillingHaltIfNeeded contract), 13-CONTEXT.md specifics block (the exact run-1 regression assertion)</read_first>
  <behavior>
    Run-1 regression (billing_halt_regression_test.go, envtest, encodes 13-CONTEXT specifics verbatim):
    - Given a Project with a Plan and two sibling Tasks ready to dispatch, inject task A's EnvelopeOut as ExitCode=1, Reason="claude exit 1: API Error: 400 Your credit balance is too low to access the Anthropic API."
    - Then: (1) Project gains BillingHalt=True / Reason=CreditBalanceTooLow after task A's completion reconcile; (2) sibling task B's reconcile creates NO Job while the condition is present (Consistently); (3) removing the condition via meta.RemoveStatusCondition + status update (exactly what resumeRun does — CLI itself unit-tested in 13-02) followed by re-reconcile → task B's Job IS created
    - Backstop unit-level: a failed envelope with Reason="forced-failure" does NOT set BillingHalt (classifier specificity holds at the call site)
  </behavior>
  <action>
    RED regression spec first (fixture shape: existing envtest specs that inject EnvelopeOut via the suite's EnvReader fake — find via `grep -rn 'SetOut\|EnvReader' internal/controller/*_test.go`).

    Then wire the backstop (D-04 layer 2) at every envelope-failure interpretation site:
    1. task_controller.go handleJobCompletion: in the failure branch (out.ExitCode != 0 path), call `setBillingHaltIfNeeded(ctx, r.Client, project, out.Reason)`; log the returned error non-fatally (the Task's own terminal patch must proceed regardless — pattern: budget.RollUpUsage error handling in the same function).
    2. milestone/phase/plan controllers: where the planner envelope tiny-status is interpreted and envReadOK && out.ExitCode != 0 (each handleJobCompletion-equivalent), call setBillingHaltIfNeeded with the resolved project + out.Reason. project may be nil at these sites — the helper nil-guards (Pitfall 3: only the resolved owning Project is ever patched; never scan project lists).
    3. project_controller.go: same call where the project-level planner envelope failure is interpreted (NOT the push-Job path — push failures have their own leak/lease classification and are not provider-billing events).
    4. Reason channel note for reviewers: the Anthropic harness truncates stderr to 256 chars into out.Reason; "credit balance" sits early in Claude Code's error output (A2 mitigation: credproxy latch from 13-02 also re-injects the substring into its local 400 body).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -count=1 -timeout 600s && make test-int-fast 2>&1 | tee /tmp/13-04-fast.log; test "${pipestatus[1]:-${PIPESTATUS[0]}}" -eq 0 && ! grep -qE '^--- FAIL|^FAIL\s' /tmp/13-04-fast.log</automated>
  </verify>
  <done>Run-1 regression spec green end to end (halt → hold → clear → resume); Layer A suite green with exit discipline</done>
  <acceptance_criteria>
    - `grep -c 'setBillingHaltIfNeeded' internal/controller/task_controller.go internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go internal/controller/project_controller.go | awk -F: '{s+=$2} END {print s}'` returns 5
    - Regression spec asserts all three legs: condition set, zero sibling Jobs while present, dispatch resumes after clear
    - The push-Job failure path in project_controller.go contains no billing classification call
    - `make test-int-fast` MAKE_EXIT == 0 AND zero `^--- FAIL|^FAIL\s` lines in the log
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Failed Job envelope → Project status | out.Reason (workload-produced) drives a project-wide control-plane condition |
| Project status → five dispatch gates | The condition read must not be bypassable by any dispatch path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Denial of Service | Billing-halt bypass (a dispatch path missing the gate) | mitigate | Gate inserted at all five levels with a grep-countable invariant (5 call sites) + per-level hold specs; position before pool acquire/Job create at every site (Pitfall 2). |
| T-13-13 | Tampering | Forged out.Reason halting the project (workload → control-plane influence) | accept | Self-DoS only, scoped to the owning Project (resolved via label/owner-chain, nil-guarded — Pitfall 3); recovery is `tide resume`; envelopes already come from controller-spawned, HMAC-gated pods. |
| T-13-14 | Tampering | Cross-project contamination (condition patched onto the wrong Project) | mitigate | setBillingHaltIfNeeded only receives the reconciler's resolved owning Project; no list-scan fallback exists (removed in Phase 04.1 P1.4); regression spec pins single-project scoping. |
| T-13-15 | Denial of Service | Park misimplemented as Fail (cascade) — violates D-05 + spec wave-boundary semantics | mitigate | Hold specs assert Status.Phase unchanged at every level; no per-level condition writes (also prevents status flapping). |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `go test ./internal/controller/... -count=1` and `make test-int-fast` green (exit discipline)
- Phase gate: full `make test-int` green (runs under plan 13-03's harness changes) before /gsd:verify-work
- ROADMAP criterion 3 trace: billing 400 → BillingHalt condition → subsequent reconcile loops skip dispatch while present — each leg has an automated spec
</verification>

<success_criteria>
- BillingHalt composes with CheckRejected and parent-approval as the third dispatch-entry hold at all five levels; parked levels never cascade to Failed
- Backstop classifies billing failures from envelopes at all five envelope-interpretation sites, patching only the owning Project
- The run-1 symptom (fan-out crashing one session at a time) is encoded as a green regression test including the resume leg
</success_criteria>

<output>
Create `.planning/phases/13-dispatch-image-resolution-provider-halt/13-04-SUMMARY.md` when done
</output>
