---
phase: 13-dispatch-image-resolution-provider-halt
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - charts/tide/templates/deployment.yaml
  - charts/tide/values.yaml
  - hack/helm/tide-values.yaml
  - test/integration/kind/projects_pvc_test.go
  - test/integration/kind/suite_test.go
  - hack/scripts/acceptance-v1.sh
autonomous: true
requirements: [DISPATCH-02]
must_haves:
  truths:
    - "A released-chart install no longer passes --subagent-image; production installs dispatch the real subagent out of the box"
    - "The chart's image-default posture is documented in values.yaml with an explicit comment (resolution chain + stub opt-in instructions)"
    - "Test installs (kind Layer B, acceptance small mode) opt into the stub explicitly via subagent.defaults.image — the full test suite is green after the chart change"
  artifacts:
    - path: "charts/tide/templates/deployment.yaml"
      provides: "Flag dropped; CLAUDE_SUBAGENT_IMAGE sourced from subagent.defaults.image (D-01 brings the dead config alive)"
      contains: ".Values.subagent.defaults.image"
    - path: "charts/tide/values.yaml"
      provides: "Documented resolution chain + stub opt-in comment at subagent.defaults.image"
    - path: "test/integration/kind/projects_pvc_test.go"
      provides: "Chart-contract go tests: no --subagent-image= in template; CLAUDE_SUBAGENT_IMAGE sourced from subagent.defaults.image"
      contains: "TestHelmDeploymentTemplateDropsSubagentImageFlag"
    - path: "test/integration/kind/suite_test.go"
      provides: "Layer B stub opt-in via --set subagent.defaults.image"
  key_links:
    - from: "charts/tide/templates/deployment.yaml"
      to: "charts/tide/values.yaml subagent.defaults.image"
      via: "CLAUDE_SUBAGENT_IMAGE env value template"
      pattern: "subagent.defaults.image"
    - from: "test/integration/kind/suite_test.go"
      to: "stub image"
      via: "helmControllerArgs --set subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent:test"
---

<objective>
DISPATCH-02 / D-01 / D-02: drop the chart's unconditional `--subagent-image=<stub>` flag
(deployment.yaml:30 — the line that silently forced the stub in every v1.0.0 install),
make `subagent.defaults.image` the live Helm-default channel feeding CLAUDE_SUBAGENT_IMAGE,
and convert every test surface that relied on the implicit stub flag to explicit stub opt-in.
A green suite after the chart change is part of the deliverable (D-02).

The chart is a FIXED contract — this flag drop is the explicitly-decided exception (D-01)
and must be documented in values.yaml.

Output: rewired chart templates + values comments (both values files), chart-contract go
tests, Layer B harness + acceptance-script stub opt-in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-CONTEXT.md
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-RESEARCH.md
@.planning/phases/13-dispatch-image-resolution-provider-halt/13-PATTERNS.md

<interfaces>
Verified chart anchors:
- deployment.yaml:30 — `- --subagent-image={{ .Values.images.stubSubagent.repository }}:{{ .Values.images.stubSubagent.tag | default .Chart.AppVersion }}` (the line to DELETE)
- deployment.yaml:45-46 — `CLAUDE_SUBAGENT_IMAGE` currently templated from `.Values.images.claudeSubagent.repository/tag`; D-01 requires it sourced from `.Values.subagent.defaults.image` instead
- values.yaml:195-198 — `subagent.defaults.image: ghcr.io/jsquirrelz/tide-claude-subagent` (bare repo, NO tag — template must handle tag defaulting)
- values.yaml:124-135 — Phase 2 comment block claiming images are "Passed to the controller via --subagent-image" (now stale, update); hack/helm/tide-values.yaml mirrors the same lines (:124-138) and must be updated in tandem
- suite_test.go:469-506 — helmControllerArgs (existing `--set images.stubSubagent.tag=test` at :477; the stub image loaded into kind is ghcr.io/jsquirrelz/tide-stub-subagent:test)
- suite_test.go:375 — comment referencing the --subagent-image flag (update)
- projects_pvc_test.go:144-152 — TestHelmDeploymentTemplateRendersManagerPodAnnotations: raw os.ReadFile of the template + bytes.Contains (the contract-test pattern to copy; runs WITHOUT a cluster)
- acceptance-v1.sh:104-105 — `helm install tide ... --set 'workspaces.pvc.accessModes={ReadWriteOnce}'` runs BEFORE the ACCEPTANCE_SAMPLE mode branch at :109; small ($0) mode relies on the stub with NO providerSecretRef. IMAGE_TAG="1.0.0" at :78.
- Binary side (landed in 13-01): manager resolves the default tier as flag-overrides-CLAUDE_SUBAGENT_IMAGE; with the flag gone from the chart, the env is the sole default tier.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chart contract — drop the flag, rewire CLAUDE_SUBAGENT_IMAGE, document posture (RED-first)</name>
  <files>charts/tide/templates/deployment.yaml, charts/tide/values.yaml, hack/helm/tide-values.yaml, test/integration/kind/projects_pvc_test.go</files>
  <read_first>charts/tide/templates/deployment.yaml (:26-60 args+env block), charts/tide/values.yaml (:124-140 images comment block, :194-205 subagent block), hack/helm/tide-values.yaml (mirror of the same regions), test/integration/kind/projects_pvc_test.go (:144-152 raw-template contract-test pattern), 13-PATTERNS.md chart section</read_first>
  <behavior>
    - TestHelmDeploymentTemplateDropsSubagentImageFlag: raw deployment.yaml template does NOT contain the byte sequence `--subagent-image=` (RED against current template)
    - TestHelmDeploymentTemplateSubagentImageEnvFromDefaults: raw template contains `CLAUDE_SUBAGENT_IMAGE` AND `.Values.subagent.defaults.image` (RED against current template, which sources the env from images.claudeSubagent)
    - helm-render checks (Task verify, not go test): default render produces `ghcr.io/jsquirrelz/tide-claude-subagent:<appVersion>`; `--set subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent:test` renders exactly that tag-qualified ref unchanged
  </behavior>
  <action>
    Add the two RED contract tests to test/integration/kind/projects_pvc_test.go using the existing raw os.ReadFile + bytes.Contains pattern (they run cluster-free via -run filter).

    Then change the chart (D-01 — the deliberate fixed-contract exception):
    1. deployment.yaml: delete line 30 (`--subagent-image=...stubSubagent...`) entirely. Leave --credproxy-image and all other args untouched.
    2. deployment.yaml: re-template the CLAUDE_SUBAGENT_IMAGE env value to source `.Values.subagent.defaults.image`; when the value has no tag suffix (no `:` in the final path segment — use sprig `regexMatch ":[^/]+$"`), append `:{{ .Chart.AppVersion }}`; tag- or digest-qualified values pass through unchanged. Add a comment block directly above the env entry documenting the resolution chain (Levels.<level>.Image → Spec.Subagent.Image → this env) and the stub opt-in (`--set subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent:<tag>`), per PATTERNS.md.
    3. values.yaml: (a) at subagent.defaults.image, add the explicit posture comment ROADMAP criterion 2 requires — production default is the real claude subagent; stub is opt-in for tests/CI; show the exact --set; note digest pinning passes through. (b) Update the stale :124-135 comment block: images.stubSubagent.* keys are retained for image build/load tooling but are NO LONGER injected as a --subagent-image flag (Phase 13 D-01); subagent image default now flows via subagent.defaults.image → CLAUDE_SUBAGENT_IMAGE.
    4. hack/helm/tide-values.yaml: apply the identical comment/value updates (it mirrors values.yaml line-for-line in these regions).
    Do not touch any other chart template (chart remains a fixed contract outside this decided exception).
  </action>
  <verify>
    <automated>go test ./test/integration/kind/ -run 'TestHelmDeploymentTemplate' -count=1 -timeout 120s && helm template tide charts/tide | grep -c 'subagent-image=' | grep -qx 0 && helm template tide charts/tide | grep -A1 'CLAUDE_SUBAGENT_IMAGE' | grep -q 'tide-claude-subagent' && helm template tide charts/tide --set subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent:test | grep -A1 'CLAUDE_SUBAGENT_IMAGE' | grep -q 'tide-stub-subagent:test'</automated>
  </verify>
  <done>Both contract tests green; helm renders show no flag, real-subagent default, stub override honored verbatim</done>
  <acceptance_criteria>
    - `grep -c 'subagent-image' charts/tide/templates/deployment.yaml` returns 0
    - `grep -c 'subagent.defaults.image' charts/tide/templates/deployment.yaml` returns at least 1
    - `helm template tide charts/tide` exits 0 and the rendered CLAUDE_SUBAGENT_IMAGE value is `ghcr.io/jsquirrelz/tide-claude-subagent:<chart appVersion>`
    - `helm template tide charts/tide --set subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent:test` renders the env value exactly `ghcr.io/jsquirrelz/tide-stub-subagent:test`
    - values.yaml AND hack/helm/tide-values.yaml both contain a comment documenting the chain and the stub --set (grep `Levels.<level>.Image` in each returns ≥ 1)
    - `go test ./test/integration/kind/ -run 'TestHelmDeploymentTemplate' -count=1` exits 0 (including the pre-existing podAnnotations contract test — do not drop it; Phase-7 near-miss)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Stub opt-in for kind Layer B + acceptance small mode; prove the suite green</name>
  <files>test/integration/kind/suite_test.go, hack/scripts/acceptance-v1.sh</files>
  <read_first>test/integration/kind/suite_test.go (:375 comment, :431 image-build comment, :469-506 helmControllerArgs), test/integration/kind/projects_pvc_test.go (:111-143 helmControllerArgs unit tests — they pin the args contract and may need a new assertion), hack/scripts/acceptance-v1.sh (:74-130 image-load + helm install + mode branch), CLAUDE.md (constrained-VM full-suite recipe + test-int exit discipline)</read_first>
  <action>
    D-02: stub becomes explicit opt-in on every harness that previously rode the implicit flag.
    1. suite_test.go helmControllerArgs: add `"--set", "subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent:test"` (the stub ref the harness already builds + kind-loads with tag test). Keep the existing images.stubSubagent.* sets (they still drive image build/load completeness). Update the stale :375 comment that references the --subagent-image flag.
    2. projects_pvc_test.go helmControllerArgs unit tests: add an assertion that the args contain the subagent.defaults.image stub --set (containsString pattern), so the harness contract is pinned.
    3. acceptance-v1.sh: the helm install at :104 runs before the mode branch — hoist a `HELM_EXTRA_SET_ARGS` (or equivalent) computed from ACCEPTANCE_SAMPLE: when "small" ($0 mode — stub, no API key), include `--set subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent:${IMAGE_TAG}`; the real-sample mode adds nothing (chart default = real subagent is now correct out of the box). Comment the why (Phase 13 D-01/D-02: implicit stub flag dropped; stub is explicit test opt-in).
    4. Run the full Layer A+B suite: `make test-int 2>&1 | tee /tmp/13-03-test-int.log; echo "MAKE_EXIT=$?"`. Constrained-VM recipe applies: fresh kind cluster, one heavy run at a time, never alongside an acceptance cluster. Read MAKE_EXIT AND `grep -nE '^--- FAIL|^FAIL\s' /tmp/13-03-test-int.log` — Ginkgo SUCCESS lines alone are not green (CLAUDE.md discipline). The DISPATCH-02 regression evidence: Layer B dispatches the stub ONLY via the explicit subagent.defaults.image channel now; a green run proves the new chain end-to-end through a real chart install. Confirm the run-1 bug signature is structurally gone: with the harness override removed (chart defaults), the rendered manager env points at the real subagent — covered by Task 1's helm-render assertions.
  </action>
  <verify>
    <automated>go test ./test/integration/kind/ -run 'TestHelmControllerArgs' -count=1 -timeout 120s && make test-int 2>&1 | tee /tmp/13-03-test-int.log; test "${pipestatus[1]:-${PIPESTATUS[0]}}" -eq 0 && ! grep -qE '^--- FAIL|^FAIL\s' /tmp/13-03-test-int.log</automated>
  </verify>
  <done>helmControllerArgs unit tests green with the new assertion; full make test-int exits 0 with zero FAIL lines</done>
  <acceptance_criteria>
    - `grep -c 'subagent.defaults.image=ghcr.io/jsquirrelz/tide-stub-subagent' test/integration/kind/suite_test.go` returns 1
    - `grep -c 'subagent.defaults.image' hack/scripts/acceptance-v1.sh` returns at least 1, and it is applied only for the small/stub mode path
    - `make test-int` MAKE_EXIT == 0 AND `grep -cE '^--- FAIL|^FAIL\s' /tmp/13-03-test-int.log` == 0
    - No remaining reference to an implicit `--subagent-image` install path: `grep -rn 'subagent-image' charts/ test/integration/kind/ hack/scripts/ | grep -v 'no longer\|dropped\|Phase 13'` returns nothing load-bearing (comments explaining the removal are fine)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values → manager Deployment | subagent.defaults.image becomes the operator-controlled default image channel |
| Project CR author → controller | CRD image fields override the operator default (decided posture, see 13-01 T-13-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Tampering | Helm-value image injection (subagent.defaults.image) | accept | Helm install/upgrade rights == cluster-operator trust; identical trust level to the flag it replaces. Values comment recommends @sha256 digest pinning for production (passes through the template unchanged). |
| T-13-10 | Spoofing | Mutable-tag default (`:appVersion`) substituted at the registry | mitigate | values.yaml comment carries the existing digest-pinning guidance; tag-/digest-qualified overrides bypass appVersion templating entirely. |
| T-13-11 | Denial of Service | Chart upgrade window where old pod still passes the dropped flag (RESEARCH Pitfall 4) | mitigate | 13-01's main.go shim keeps the flag functional as the default tier; Layer B helm upgrade uses --wait, asserting against the NEW pod only. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `make test-int` green per CLAUDE.md exit discipline (MAKE_EXIT + FAIL-grep, never Ginkgo summary alone)
- Manual phase-gate item (verify-work, per 13-VALIDATION.md): kind install with chart defaults + a Project pinning a real image → `kubectl get job -o yaml` shows the pinned image; no `stub-*` children; no `"planner stub success"` termination message
</verification>

<success_criteria>
- Released-chart install dispatches the real subagent by default; pinning works; stub is explicit opt-in everywhere
- The posture and resolution chain are documented in values.yaml (and its hack/helm mirror)
- Full test suite green after the chart change — the D-02 deliverable, not an afterthought
</success_criteria>

<output>
Create `.planning/phases/13-dispatch-image-resolution-provider-halt/13-03-SUMMARY.md` when done
</output>
