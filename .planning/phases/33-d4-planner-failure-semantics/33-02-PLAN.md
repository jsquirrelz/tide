---
phase: 33-d4-planner-failure-semantics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/tide/values.yaml
autonomous: true
requirements: [PLANFAIL-01]
must_haves:
  truths:
    - "The chart's plannerConcurrency comment no longer states a hard 'must be >= widest wave' requirement"
    - "The comment documents the single-node throughput-for-safety tradeoff (a wide milestone serializes, it does not deadlock)"
    - "The plannerConcurrency default value remains 4 (no behavior change)"
  artifacts:
    - path: "charts/tide/values.yaml"
      provides: "Softened plannerConcurrency sizing-policy guidance per D-04 (carried-in D3 debt)"
      contains: "plannerConcurrency: 4"
  key_links: []
---

<objective>
Resolve the carried-in D3 sizing-policy documentation debt (32-REVIEW, captured as 33-CONTEXT.md D-04): soften the `charts/tide/values.yaml` comment that overstates `plannerConcurrency` must be sized "≥ widest expected planning wave" into a per-workload tuning note that documents the single-node throughput-for-safety tradeoff. This is a docs/comment-only change — the default value stays 4.

Purpose: The Phase 32 `plannerConcurrency=4` default was a deliberate single-node-kind safety choice (each planner pod = subagent + credproxy sidecar; leave headroom for the executor pool + system pods on a 7.65 GiB node). The chart comment's "needs >= 6 to avoid serialising" wording wrongly implies a correctness requirement. A cap below wave width serializes dispatch but does NOT deadlock — single-shot planner Jobs drain and the next dispatches. Fixing only the wording preserves the safety choice (D-04 locks the value at 4; raising it is a deferred multi-node concern).

Output: Updated comment block above `plannerConcurrency: 4` in `charts/tide/values.yaml`.

This plan is deliberately isolated from the failure-semantics work (Plans 01/03) so the chart-contract edit does not entangle the controller change. `charts/tide/values.yaml` is a FIXED contract — this edit changes ONLY the comment, never the value (CLAUDE.md; D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-d4-planner-failure-semantics/33-CONTEXT.md
@.planning/phases/33-d4-planner-failure-semantics/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Soften plannerConcurrency sizing-policy comment in values.yaml</name>
  <read_first>
    - charts/tide/values.yaml (lines 78-89 — the current plannerConcurrency comment block and the `plannerConcurrency: 4` line)
    - .planning/phases/33-d4-planner-failure-semantics/33-CONTEXT.md (D-04 — the exact recommended softening)
    - .planning/phases/33-d4-planner-failure-semantics/33-RESEARCH.md ("Verified: chart plannerConcurrency comment" — the exact current text and what overstates the requirement)
  </read_first>
  <action>
    In charts/tide/values.yaml, edit ONLY the comment block above `plannerConcurrency: 4` (currently lines ~79-87). Replace the sentence "Must be sized at least as wide as the widest expected planning wave (e.g. a 6-phase milestone needs plannerConcurrency >= 6 to avoid serialising phase dispatch)." with a softer per-workload tuning note that states: sizing the cap below a milestone's widest planning wave serializes (not deadlocks) that wave's dispatch — single-shot planner Jobs drain and the next dispatches, so a narrower cap degrades throughput but is safe. Add a one-line note that the single-node default of 4 intentionally trades throughput for safety (memory headroom for the executor pool + system pods). Preserve the surrounding D3 mechanism description (cached client.List, RequeueAfter(10s) park) and the existing "Increase for multi-node clusters" closing line. Keep the water/tide vocabulary consistent with the surrounding comments. Do NOT change `plannerConcurrency: 4` — the value stays 4 (D-04; chart is the FIXED contract, comment-only edit). Do NOT touch `executorConcurrency` or any other key.
  </action>
  <verify>
    <automated>grep -q '^plannerConcurrency: 4$' charts/tide/values.yaml && ! grep -q 'needs plannerConcurrency >= 6' charts/tide/values.yaml && helm template ./charts/tide >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `charts/tide/values.yaml` still contains `plannerConcurrency: 4` (value unchanged).
    - The phrase "needs plannerConcurrency >= 6 to avoid serialising" no longer appears.
    - The comment now states that a narrower cap serializes but does not deadlock, and documents the single-node throughput-for-safety tradeoff.
    - `helm template ./charts/tide` renders without error (comment edit did not break YAML).
    - `git diff --stat charts/tide/values.yaml` shows changes only within the comment block region (no value lines altered).
  </acceptance_criteria>
  <done>The chart comment documents the tradeoff without the overstated hard requirement; the default value is still 4; the chart renders cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only edit to a Helm values file. No code, no value change, no new input/output surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02 | Tampering | charts/tide/values.yaml default value | mitigate | The value `plannerConcurrency: 4` MUST remain unchanged (D-04). Verification greps the literal `^plannerConcurrency: 4$` to prove no behavior change; `helm template` confirms YAML validity. |

No high-severity threats: documentation-only edit to a FIXED chart contract with no value mutation.
</threat_model>

<verification>
- `grep -q '^plannerConcurrency: 4$' charts/tide/values.yaml` succeeds (value preserved).
- `! grep -q 'needs plannerConcurrency >= 6' charts/tide/values.yaml` succeeds (overstated requirement removed).
- `helm template ./charts/tide` renders without error.
</verification>

<success_criteria>
- The carried-in D3 sizing-policy comment is softened to a per-workload tuning note documenting the throughput-for-safety tradeoff.
- `plannerConcurrency` default is still 4; no controller or binary behavior changes.
</success_criteria>

<output>
Create `.planning/phases/33-d4-planner-failure-semantics/33-02-SUMMARY.md` when done.
</output>
