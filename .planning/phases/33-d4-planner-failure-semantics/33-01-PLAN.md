---
phase: 33-d4-planner-failure-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/planner_failure.go
  - internal/controller/planner_failure_test.go
  - api/v1alpha2/shared_types.go
autonomous: true
requirements: [PLANFAIL-01, PLANFAIL-02]
must_haves:
  truths:
    - "isPlannerFailure(out, envReadOK) returns true ONLY for envReadOK && ExitCode!=0 && ChildCount==0"
    - "A genuine leaf (ExitCode==0, ChildCount==0, envReadOK==true) yields isPlannerFailure==false"
    - "An unreadable envelope (envReadOK==false) yields isPlannerFailure==false"
    - "ReasonPlannerFailed condition vocabulary exists in the served v1alpha2 API"
  artifacts:
    - path: "internal/controller/planner_failure.go"
      provides: "Shared isPlannerFailure predicate + package doc explaining phase/milestone-only scope (D-02 exclusion of plan/project)"
      contains: "func isPlannerFailure("
    - path: "internal/controller/planner_failure_test.go"
      provides: "4-case pure unit table for isPlannerFailure"
      contains: "func TestIsPlannerFailure("
    - path: "api/v1alpha2/shared_types.go"
      provides: "ReasonPlannerFailed = \"PlannerFailed\" constant"
      contains: "ReasonPlannerFailed"
  key_links:
    - from: "internal/controller/planner_failure.go"
      to: "github.com/jsquirrelz/tide/pkg/dispatch"
      via: "pkgdispatch.EnvelopeOut parameter type"
      pattern: "pkgdispatch\\.EnvelopeOut"
---

<objective>
Land the Wave-0 foundation for D4 planner failure semantics: the shared `isPlannerFailure` predicate, its pure-unit test table, and the `ReasonPlannerFailed` condition vocabulary. This plan introduces NO controller behavior change — it only creates the building blocks Plan 03 wires into the phase and milestone succession sites.

Purpose: Establish a single source of truth for the D4 false-leaf contract (`envReadOK && ExitCode != 0 && ChildCount == 0`) before any controller is touched, so the guard is DRY across both levels (PLANFAIL-02) and the leaf-succeeds invariant (PLANFAIL-03) is pinned by a fast unit test before envtests exist.

Output: `internal/controller/planner_failure.go`, `internal/controller/planner_failure_test.go`, and a new `ReasonPlannerFailed` const block in `api/v1alpha2/shared_types.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-d4-planner-failure-semantics/33-CONTEXT.md
@.planning/phases/33-d4-planner-failure-semantics/33-RESEARCH.md
@.planning/phases/33-d4-planner-failure-semantics/33-VALIDATION.md

<interfaces>
<!-- Extracted from codebase; executor should use these directly, no exploration needed. -->

The `out` parameter type is `pkgdispatch.EnvelopeOut` (import alias `pkgdispatch "github.com/jsquirrelz/tide/pkg/dispatch"`).
Relevant fields (pkg/dispatch/envelope.go):
- `ExitCode int`   — 0 = success, nonzero = planner failed internally
- `ChildCount int` — count of authored child CRDs

Existing shared-helper file convention to mirror for file shape + license header + package-doc style:
- internal/controller/failure_halt.go
- internal/controller/billing_halt.go

Existing Reason* const block to insert the new constant AFTER (api/v1alpha2/shared_types.go:197-204):
- `ReasonWaveIntegrationFailed = "WaveIntegrationFailed"` (PascalCase value convention)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReasonPlannerFailed to v1alpha2 shared_types.go</name>
  <read_first>
    - api/v1alpha2/shared_types.go (lines 190-210 — the ReasonParentRefNotFound and ReasonWaveIntegrationFailed const blocks; the new block goes after ReasonWaveIntegrationFailed)
  </read_first>
  <behavior>
    - The served v1alpha2 API exposes a `ReasonPlannerFailed` string constant with PascalCase value `"PlannerFailed"`.
    - The constant is documented as: phase or milestone planner exited nonzero with zero children; level marked terminal Failed; recovery via `tide resume --retry-failed`.
  </behavior>
  <action>
    Add a new `const` block to api/v1alpha2/shared_types.go immediately after the `ReasonWaveIntegrationFailed` block (~line 204). Define exactly one exported constant `ReasonPlannerFailed = "PlannerFailed"` with a doc comment headed "Phase 33 condition + reason vocabulary — planner failure semantics (D4)." per D-05. Value MUST be PascalCase `"PlannerFailed"` (matches the existing Reason* convention; see ReasonWaveIntegrationFailed). Do NOT add this constant to api/v1alpha1/shared_types.go — controllers import only v1alpha2 (RESEARCH Pitfall 3; v1alpha1 is the legacy schema, not the served API).
  </action>
  <verify>
    <automated>grep -q 'ReasonPlannerFailed = "PlannerFailed"' api/v1alpha2/shared_types.go && ! grep -rq 'ReasonPlannerFailed' api/v1alpha1/ && go build ./api/...</automated>
  </verify>
  <acceptance_criteria>
    - `api/v1alpha2/shared_types.go` contains `ReasonPlannerFailed = "PlannerFailed"`.
    - `api/v1alpha1/` contains NO occurrence of `ReasonPlannerFailed` (grep returns zero).
    - `go build ./api/...` exits 0.
  </acceptance_criteria>
  <done>The served API exposes `ReasonPlannerFailed` with value `"PlannerFailed"`; v1alpha1 untouched; api package builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create isPlannerFailure shared helper + pure unit test</name>
  <read_first>
    - internal/controller/failure_halt.go (license header + package-doc + shared-helper file shape to mirror)
    - internal/controller/billing_halt.go (second example of the shared-helper file convention)
    - pkg/dispatch/envelope.go (EnvelopeOut struct — confirm ExitCode and ChildCount field names)
    - .planning/phases/33-d4-planner-failure-semantics/33-RESEARCH.md (Pattern 1 — the exact helper signature, doc comment, and the four-branch reasoning)
    - .planning/phases/33-d4-planner-failure-semantics/33-VALIDATION.md (Wave 0 Requirements — the 4-case unit table)
  </read_first>
  <behavior>
    - isPlannerFailure(EnvelopeOut{ExitCode:1, ChildCount:0}, true)  == true   (false-leaf)
    - isPlannerFailure(EnvelopeOut{ExitCode:0, ChildCount:0}, true)  == false  (genuine leaf — PLANFAIL-03 invariant)
    - isPlannerFailure(EnvelopeOut{ExitCode:1, ChildCount:0}, false) == false  (envelope unreadable)
    - isPlannerFailure(EnvelopeOut{ExitCode:1, ChildCount:3}, true)  == false  (children present)
  </behavior>
  <action>
    Create internal/controller/planner_failure.go in `package controller` with the Apache license header (copy from failure_halt.go). Add a file-level package doc comment in the tide vocabulary explaining: this is the shared D4 false-leaf guard for the phase and milestone planner-succession sites; plan and project are DELIBERATELY excluded because they succeed only via gates.BoundaryDetected (returns matched>0, false on zero children) so a zero-child planner can never falsely advance them — cite 33-CONTEXT.md D-02 so a future reader does not "complete the set." Import `pkgdispatch "github.com/jsquirrelz/tide/pkg/dispatch"`. Define a package-level function `isPlannerFailure(out pkgdispatch.EnvelopeOut, envReadOK bool) bool` returning exactly `envReadOK && out.ExitCode != 0 && out.ChildCount == 0` (D-06; do NOT expand to the ExitCode!=0 && ChildCount>0 case — RESEARCH Open Question 1). Document the four branches in the function doc per RESEARCH Pattern 1. Do NOT inline this logic anywhere — it is the single source of truth.

    Then create internal/controller/planner_failure_test.go in `package controller` as a stdlib `testing` table test `TestIsPlannerFailure` asserting the four behavior cases above (no envtest, no Ginkgo — pure function). Use subtests with descriptive names.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestIsPlannerFailure -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/controller/planner_failure.go` contains `func isPlannerFailure(out pkgdispatch.EnvelopeOut, envReadOK bool) bool`.
    - The function body is exactly the expression `envReadOK && out.ExitCode != 0 && out.ChildCount == 0` (no extra cases).
    - The package doc comment names plan + project as deliberately excluded and references D-02.
    - `go test ./internal/controller/... -run TestIsPlannerFailure -count=1` exits 0 with 4 subtest cases.
    - `grep -c 'envReadOK && out.ExitCode != 0 && out.ChildCount == 0' internal/controller/*.go` returns 1 (single source of truth — no inline duplication).
  </acceptance_criteria>
  <done>The shared predicate exists with the documented phase/milestone-only scope, and its 4-case unit table is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a pure in-process boolean predicate and an API string constant. No new input parsing, no network/disk I/O, no secret handling, no new API surface served to external clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `isPlannerFailure` correctness | mitigate | The predicate's misclassification risk (false leaf treated as success → planning-DAG corruption) is pinned by the 4-case unit table; the `envReadOK &&` prefix prevents a zero-value envelope (read error → ExitCode==0) from firing the guard. |
| T-33-NEWDEP | Tampering | go.mod / packages | accept | No new packages, no go.mod change (RESEARCH Package Legitimacy Audit: none introduced). Nothing to verify. |

No high-severity threats: this is internal controller state-transition logic with zero new external surface.
</threat_model>

<verification>
- `go test ./internal/controller/... -run TestIsPlannerFailure -count=1` exits 0.
- `go build ./api/... ./internal/controller/...` exits 0.
- `grep -q 'ReasonPlannerFailed = "PlannerFailed"' api/v1alpha2/shared_types.go` and the constant is absent from `api/v1alpha1/`.
</verification>

<success_criteria>
- The shared `isPlannerFailure` predicate exists, is the only place the D4 expression lives, and is documented with the plan/project exclusion rationale.
- The 4-case unit table is green (covers PLANFAIL-03 leaf invariant + the envReadOK guard + the children-present case).
- `ReasonPlannerFailed` vocabulary is available in v1alpha2 for Plan 03's helpers to reference.
</success_criteria>

<output>
Create `.planning/phases/33-d4-planner-failure-semantics/33-01-SUMMARY.md` when done.
</output>
