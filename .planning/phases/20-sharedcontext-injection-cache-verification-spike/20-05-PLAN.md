---
phase: 20-sharedcontext-injection-cache-verification-spike
plan: 05
type: execute
wave: 3
depends_on: ["20-02", "20-03", "20-04"]
files_modified:
  - .planning/PROJECT.md
  - internal/subagent/anthropic/subagent.go
autonomous: false
requirements: [CACHE-01, CACHE-05]
user_setup:
  - service: anthropic
    why: "The CACHE-01 verdict requires two real claude -p --bare dispatches against the live Anthropic API on the durable dogfood cluster."
    env_vars:
      - name: TIDE_PROXY_ENDPOINT
        source: "Running credproxy base URL on kind-tide-dogfood (per MEMORY make eval recipe)"
      - name: TIDE_SIGNED_TOKEN
        source: "HMAC signed token minted for the running credproxy (throwaway signing key, golang:1.26.3 container)"
    dashboard_config:
      - task: "Stand up credproxy + minted token in a golang:1.26.3 Linux container (macOS ignores SSL_CERT_FILE); real key at ~/.tide/anthropic.key"
        location: "Local — /tmp/tide-eval/ helper per MEMORY.md"
must_haves:
  truths:
    - "The spike (make spike) ran live on kind-tide-dogfood and produced a PASS (sibling #2 cache_read_input_tokens > 0) or a FAIL with a named root-cause divergence"
    - "make eval ran against the SharedContext-grown templates and confirmed whether the combined stable prefix clears the active model's documented floor (>=1,024 Sonnet/Opus), with the Haiku 4,096 gap documented"
    - "PROJECT.md records the CACHE-01 decision: cross-pod caching confirmed OR does-not-fire-with-root-cause, plus the per-provider floor table as a known input"
    - "PROJECT.md records OpenAI/Codex parity deferred to the run-#2 milestone (CACHE-05)"
    - "The design is verified provider-neutral: no markers/conditionals/vendor branches on the SharedContext path (grep-asserted)"
    - "D-08 contingency resolved: if the FAIL divergence is cheaply+safely normalizable AND contained (no pod-isolation/chart change), the fix is attempted in-phase and the spike re-run; otherwise recorded as a scoped follow-up"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "CACHE-01 decision record + provider floor table + CACHE-05 deferral note in Key Decisions"
      contains: "cross-pod"
  key_links:
    - from: "make spike verdict (events.jsonl cache_read_input_tokens)"
      to: ".planning/PROJECT.md Key Decisions"
      via: "decision record prose with root cause"
      pattern: "cross-pod|cache_read"
---

<objective>
Close CACHE-01 and CACHE-05 with empirical evidence and a recorded decision. Run the
Plan-04 spike live on the durable `kind-tide-dogfood` cluster to settle whether
`claude -p --bare` yields cross-pod prefix-cache hits (D-01/D-02). Run `make eval` against
the SharedContext-grown templates (Plans 01-03) to confirm whether the combined stable
prefix clears the model's documented floor. Record the outcome as a decision in PROJECT.md
(criterion 1, UNCONDITIONAL) with the per-provider floor table and the CACHE-05 deferral.
Verify provider-neutrality (no markers/branches). Resolve the D-08 contingency: attempt an
in-phase CLI-prefix normalization ONLY if the FAIL divergence is cheap, safe, and contained
(no pod-isolation-contract violation, no `charts/tide/values.yaml` change); otherwise record
a scoped follow-up. The SharedContext field ships either way (already done in Plans 01-03).

Purpose: Turn the spike into a recorded decision that frames all future caching work, and
prove the optimization is provider-neutral.
Output: A PROJECT.md decision record; an optional contained D-08 normalization; the
verified provider-neutral grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-sharedcontext-injection-cache-verification-spike/20-CONTEXT.md
@.planning/phases/20-sharedcontext-injection-cache-verification-spike/20-RESEARCH.md
@cmd/tide-spike/main.go
@internal/subagent/anthropic/subagent.go

<interfaces>
<!-- Decision-record target + the per-provider floor table to inline. Do NOT explore further. -->

.planning/PROJECT.md "## Key Decisions" — markdown table: | Decision | Rationale | Outcome |
  Append a CACHE-01 row AND a short prose decision block beneath the table (the floor table
  needs its own block, not a single cell).

Per-provider floor table (from 20-CONTEXT.md code_context appendix — record as a known input):
  OpenAI 1,024 | Anthropic 1,024 Sonnet/Opus · 4,096 Haiku | Gemini 1,024 Flash / 2,048 Pro |
  Bedrock 1,024 or 4,096 | DeepSeek none(64-tok chunks) | Mistral 64
  Design implication: NEVER hardcode 1,024 (D-06); document as "active model's documented floor".

D-08 normalization candidate (ONLY if FAIL + contained):
  internal/subagent/anthropic/subagent.go — the --add-dir eventsDir arg (per-TaskUID path) is
  the suspected per-pod divergence. A normalized/aliased shared --add-dir path COULD restore
  byte-identity. GUARD: must NOT violate per-task pod isolation (write-scope vs read-isolation —
  RESEARCH Pitfall 1 D-08 check) and must NOT touch charts/tide/values.yaml. If the safe fix is
  larger than a contained arg change, STOP and record a scoped follow-up instead.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the live spike + make eval; capture the verdict</name>
  <action>Human-gated live run: the executor pauses here while the maintainer stands up the credproxy and runs the live spike + make eval against kind-tide-dogfood, then pastes back the verdict. No autonomous code change in this task.</action>
  <what-built>
    The Plan-04 `make spike` harness (two real claude -p --bare dispatches, distinct --add-dir
    paths, ≥1,024-token identical prefix) and the SharedContext-grown planner templates
    (Plans 01-03). Both need a LIVE run against the durable kind-tide-dogfood cluster — they
    cannot be verified in unit tests (network + real Anthropic API).
  </what-built>
  <how-to-verify>
    1. Stand up the credproxy + mint a TIDE_SIGNED_TOKEN in a golang:1.26.3 Linux container per the MEMORY.md `make eval` recipe (macOS ignores SSL_CERT_FILE; real key at ~/.tide/anthropic.key). Export TIDE_PROXY_ENDPOINT + TIDE_SIGNED_TOKEN.
    2. Run `make spike`. Record: dispatch #1 `create=<n>`, dispatch #2 `read=<n>`. PASS iff dispatch #2 `read > 0` within the 5-min TTL AND net-negative realized cost; exit 0. On FAIL (exit 1), capture the printed credproxy body diff naming the divergence (e.g. the `--add-dir` path in the `system` field).
    3. Run `make eval` (with a representative SharedContext populated in the eval fixture or a live planner dispatch) and record the per-template real token counts — does the combined stable prefix clear ≥1,024 (Sonnet/Opus)? Note the Haiku 4,096 gap.
    4. Paste back: the spike verdict (PASS/FAIL + numbers), the FAIL divergence root cause (if any), and the make eval token counts.
  </how-to-verify>
  <resume-signal>Paste the spike verdict (read/create numbers), any FAIL divergence root cause, and the make eval token counts; then type "approved" or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the CACHE-01 decision in PROJECT.md + provider-neutrality verification</name>
  <read_first>
    - .planning/PROJECT.md (lines 100-124 — Key Decisions table format; append a row + a prose block)
    - .planning/phases/20-sharedcontext-injection-cache-verification-spike/20-CONTEXT.md (code_context appendix — the per-provider floor table to inline; D-06 "never hardcode 1,024")
    - .planning/ROADMAP.md (Phase 20 success criteria 1 + 5 — the exact decision wording the criteria require)
  </read_first>
  <action>
    Using the verdict from Task 1, append to PROJECT.md "## Key Decisions": (a) one table row summarizing the CACHE-01 outcome ("cross-pod prefix-cache hits confirmed under claude -p --bare" OR "cross-pod caching does not fire under claude -p --bare; reframed to token-minimization-only" with the named root-cause divergence), and (b) a prose block beneath the table recording: the spike numbers (dispatch #1 create / dispatch #2 read), the per-provider floor table (from CONTEXT.md appendix) as a known input with the explicit note that 1,024 is NEVER hardcoded (D-06, "active model's documented floor"), the make eval result (does the SharedContext-grown prefix clear the floor; Haiku 4,096 gap documented), and the CACHE-05 note that OpenAI/Codex live parity is deferred to the run-#2 milestone (SharedContext is on the provider-agnostic EnvelopeIn with no vendor branches).
    Then verify provider-neutrality across the whole SharedContext path: `grep -rniE "anthropic|openai|gemini|bedrock|cache_control|vendor" pkg/dispatch/envelope.go pkg/dispatch/childcrd.go internal/controller/dispatch_helpers.go internal/subagent/common/templates/*planner.tmpl | grep -i sharedcontext` MUST return zero vendor-coupled SharedContext lines — record the clean result in the SUMMARY.
  </action>
  <verify>
    <automated>grep -niE "cross-pod|cache_read|token-minimization" .planning/PROJECT.md && grep -niE "run-#2|run #2|OpenAI" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md Key Decisions contains a CACHE-01 row with one of the two ROADMAP-mandated verdicts (criterion 1) and the named root cause if FAIL.
    - PROJECT.md records the per-provider floor table and the explicit "never hardcode 1,024" note (D-06).
    - PROJECT.md records the make eval floor result + Haiku 4,096 gap, and the CACHE-05 OpenAI/Codex run-#2 deferral.
    - The provider-neutrality grep returns zero vendor-coupled SharedContext lines (recorded in SUMMARY) — CACHE-05 verified.
  </acceptance_criteria>
  <done>The spike outcome is a committed decision in PROJECT.md with the floor table and CACHE-05 deferral; the SharedContext path is grep-verified provider-neutral.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: D-08 contingency — in-phase normalization vs scoped follow-up</name>
  <action>Decision checkpoint: the executor pauses for the maintainer to choose normalize-in-phase vs scoped-followup (or pass-no-op if the spike passed). The chosen option is executed in Task 4. No autonomous code change in this task.</action>
  <decision>
    If Task 1 was a FAIL with a named divergence (likely the per-pod --add-dir eventsDir path),
    do we attempt the CLI-prefix normalization in-phase, or record it as a scoped follow-up?
    (If Task 1 was a PASS, this checkpoint is a no-op: record "spike passed, no normalization
    needed" and skip the options.)
  </decision>
  <context>
    D-08: ship SharedContext regardless (done in Plans 01-03). Attempt the fix ONLY if it is
    cheap, safe, and CONTAINED — no per-task pod-isolation-contract violation, no
    charts/tide/values.yaml change. The suspected fix is normalizing the --add-dir path in
    internal/subagent/anthropic/subagent.go so the CLI's system prefix is byte-identical across
    pods. The guard question (RESEARCH Pitfall 1): --add-dir is a WRITE-scope arg; does a
    shared/aliased path let siblings write into each other's space? If yes, it violates isolation
    → follow-up. If no (write-scope stays per-pod another way, or the path can be aliased without
    granting cross-sibling writes), the contained fix is in-bounds.
  </context>
  <options>
    <option id="normalize-in-phase">
      <name>Attempt contained normalization now + re-run spike</name>
      <pros>Recovers real Claude-path cross-pod cache hits — the win the milestone chases; aligns with the project's "fix the root cause" posture.</pros>
      <cons>Only valid if provably contained; risks scope creep if the isolation analysis is wrong.</cons>
    </option>
    <option id="scoped-followup">
      <name>Record a scoped follow-up (CLI-prefix normalization as its own phase)</name>
      <pros>Keeps the spike a spike; SharedContext ships as token-minimization-only now; defers the risky fix to a properly-scoped phase.</pros>
      <cons>No cache hits on the Claude path this milestone (token minimization only).</cons>
    </option>
  </options>
  <resume-signal>Select: normalize-in-phase, scoped-followup, or pass-no-op (if spike passed).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute the D-08 resolution from Task 3</name>
  <read_first>
    - internal/subagent/anthropic/subagent.go (lines 285-305 — the --add-dir eventsDir arg; the suspected divergence)
    - .planning/phases/20-sharedcontext-injection-cache-verification-spike/20-RESEARCH.md (Pitfall 1 D-08 containment check — write-scope vs read-isolation)
    - .planning/STATE.md (Blockers/Concerns — the contingency framing)
  </read_first>
  <action>
    Execute the Task-3 decision:
    - If `normalize-in-phase`: apply the CONTAINED --add-dir normalization in subagent.go (a shared/aliased path that does NOT grant cross-sibling writes and does NOT touch charts/tide/values.yaml), keep `go build ./... && go test ./internal/subagent/...` green, then have the human re-run `make spike` (a brief checkpoint within this task's verify) to confirm the recovered cache hit; update the PROJECT.md decision with the post-fix verdict. If, mid-implementation, the fix turns out to require a pod-isolation or chart change, STOP, revert, and fall through to the scoped-followup branch.
    - If `scoped-followup`: do NOT touch subagent.go. Record the follow-up via the project's quick-task/backlog mechanism (a `.planning/` note or STATE.md Pending Todos entry: "CLI-prefix normalization as its own phase — divergence: <root cause from Task 1>"). Confirm PROJECT.md already states the reframe (token-minimization-only) from Task 2.
    - If `pass-no-op`: record "spike passed; no normalization needed" in the SUMMARY; no code change.
    Either way, confirm the SharedContext field shipped (Plans 01-03) and `make test` is green before phase close.
  </action>
  <verify>
    <automated>go build ./... && go test ./pkg/dispatch/... ./internal/controller/... ./internal/reporter/... ./internal/subagent/... ./internal/eval/...</automated>
  </verify>
  <acceptance_criteria>
    - The Task-3 decision is executed: either a contained subagent.go normalization with a re-run spike confirmation, OR a recorded scoped follow-up with no subagent.go change, OR a no-op for a passing spike.
    - `go build ./...` exits 0 and the full unit suite (`go test ./pkg/dispatch/... ./internal/controller/... ./internal/reporter/... ./internal/subagent/... ./internal/eval/...`) is green.
    - If a scoped follow-up was chosen, a backlog/STATE.md entry naming the divergence exists.
    - No `charts/tide/values.yaml` change in the diff (D-08 + CLAUDE.md fixed-contract guard) — `git diff --name-only` does not include charts/tide/values.yaml.
  </acceptance_criteria>
  <done>The D-08 contingency is resolved per the human decision; SharedContext shipped; suite green; no chart change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike harness → credproxy → upstream Anthropic | Live dispatch; the real key lives only in the credproxy sidecar; the spike passes only the signed token. |
| FAIL-path body tee → local disk | Request bodies captured for the divergence diff. |
| --add-dir normalization (D-08) → pod isolation contract | A shared/aliased path must not grant cross-sibling writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05-01 | Information Disclosure | FAIL-path body tee or spike output leaking the real Anthropic key / signed token | mitigate | Tee captures the request BODY only (key is a proxy-injected header, not body); spike never logs the token (Plan 04 requireFlag). Verified in Plan 04; this plan only consumes those guarantees. |
| T-20-05-02 | Elevation of Privilege | D-08 --add-dir normalization granting cross-sibling write access (isolation break) | mitigate | The Task-3 checkpoint explicitly gates on the write-scope-vs-isolation question; normalize-in-phase is chosen ONLY if the shared/aliased path does not grant cross-sibling writes. If it would, the fix reverts to a scoped follow-up (Task 4 fall-through). |
| T-20-05-03 | Tampering | throwaway spike signing key committed to the repo | mitigate | Ephemeral key minted at runtime in the golang container; never committed (.gitignore covers .secrets/; MEMORY recipe). |
| T-20-05-04 | Tampering | D-08 fix silently editing the FIXED chart contract | mitigate | Explicit acceptance criterion: `git diff --name-only` must not include charts/tide/values.yaml (D-08 guard + CLAUDE.md). |
| T-20-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies. No installs. |
</threat_model>

<verification>
- Live spike ran; verdict (PASS/FAIL + numbers + root cause) captured.
- make eval floor result captured; Haiku 4,096 gap documented.
- PROJECT.md records the CACHE-01 decision + floor table + CACHE-05 deferral (criteria 1 + 5).
- Provider-neutrality grep clean (no vendor-coupled SharedContext lines).
- D-08 resolved (contained fix re-run-confirmed, OR scoped follow-up); no chart change.
- `go build ./...` + full unit suite green.
</verification>

<success_criteria>
- CACHE-01 decision committed to PROJECT.md (ROADMAP criterion 1, unconditional).
- CACHE-05 verified provider-neutral + run-#2 deferral recorded (criterion 5).
- D-08 contingency resolved per the project's "fix the root cause if contained" posture.
- SharedContext field shipped (Plans 01-03) regardless of spike outcome.
</success_criteria>

<output>
Create `.planning/phases/20-sharedcontext-injection-cache-verification-spike/20-05-SUMMARY.md` when done.
</output>
