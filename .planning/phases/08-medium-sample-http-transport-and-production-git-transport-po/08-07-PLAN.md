---
phase: 08-medium-sample-http-transport-and-production-git-transport-po
plan: 07
type: execute
wave: 4
depends_on: [08-05, 08-06]
files_modified:
  - .github/workflows/nightly-integration.yml
  - test/integration/kind/medium_http_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "nightly-integration.yml contains an image-smoke step checking tide-push and claude-subagent have no git binary"
    - "nightly-integration.yml image-smoke step also asserts tide-demo-init RETAINS git"
    - "test/integration/kind/medium_http_test.go It blocks are no longer Skip-only — they assert real behavior against the git-http-server image"
    - "make test-int Layer B suite includes the medium_http_test.go spec (auto-discovered by package)"
    - "make test-int exits 0 (medium_http_test.go specs pass in kind cluster)"
  artifacts:
    - path: ".github/workflows/nightly-integration.yml"
      provides: "SC-1 CI image-smoke step + SC-5 medium-http kind test wiring"
      contains: "tide-push.*git.*exit 1"
    - path: "test/integration/kind/medium_http_test.go"
      provides: "Active Ginkgo spec for hermetic medium-http kind test"
      contains: "git-http-server"
  key_links:
    - from: ".github/workflows/nightly-integration.yml"
      to: "docker images (tide-push:1.0.0, claude-subagent:1.0.0, tide-demo-init:1.0.0)"
      via: "docker run --entrypoint which git"
      pattern: "docker run.*which.*git"
    - from: "test/integration/kind/medium_http_test.go"
      to: "examples/projects/medium/git-http-server-deployment.yaml"
      via: "kubectl apply in BeforeAll"
      pattern: "git-http-server-deployment.yaml"
---

<objective>
Wave 4 (depends on 08-05 for git-http image + manifests, 08-06 for docs finality): Wire the two CI coverage items:
1. Image-smoke step in nightly-integration.yml verifying SC-1 (tide-push and claude-subagent are git-less; tide-demo-init retains git)
2. Active Ginkgo spec assertions in medium_http_test.go replacing the scaffolded Skips with real kind-cluster behavior (SC-5)

Purpose: Phase 8's CI coverage gap (medium was NEVER in CI) closes here. The nightly-integration.yml already builds images and runs make test-int; adding the image-smoke step and wiring the medium_http spec into the Layer B suite completes the hermetic coverage for both SC-1 and SC-5.

Output:
- .github/workflows/nightly-integration.yml — new image-smoke step (SC-1) inserted after the image-build step
- test/integration/kind/medium_http_test.go — Skip() calls replaced with real assertions (SC-5)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VALIDATION.md

<interfaces>
<!-- From 08-RESEARCH.md §"Question E: CI Coverage Shape" and 08-VALIDATION.md §"Per-Task Verification Map" -->

nightly-integration.yml structure (confirmed from reading the file):
  - Steps: checkout → setup-go → install kind → install helm → verify go.mod/go.sum → prepare manifests → Layer B kind integration → TIDE kind_e2e → collect logs
  - The image-smoke step must run AFTER the images are built but the current nightly workflow does NOT build images explicitly (it uses make test-int which loads from prebuilt images via kind load).
  - IMPORTANT: The nightly workflow's kind-suites job uses images built by `make test-int-kind-prep` (which builds and loads stub-subagent + credproxy images). For the SC-1 smoke test, the full production images (tide-push:1.0.0, claude-subagent:1.0.0, tide-demo-init:1.0.0) are NOT automatically available in CI.
  - Resolution: The image-smoke step should run `docker build` for the three images locally (using the Dockerfiles) and then run the presence/absence check. This is lightweight — the images are small and the build cache will warm after the first run.
  - Place the image-smoke step AFTER `prepare manifests and envtest binaries` and BEFORE `Layer B kind integration suite`.

SC-1 image-smoke step commands (from 08-VALIDATION.md Per-Task Verification Map):
  docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-push:1.0.0 git → MUST exit 1 (git absent)
  docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 git → MUST exit 1 (git absent)
  docker run --rm --entrypoint git ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --version → MUST exit 0 (git present)
  
  The CI step must build the images first if they don't exist:
  docker build -t ghcr.io/jsquirrelz/tide-push:1.0.0 -f images/tide-push/Dockerfile .
  docker build -t ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 -f images/claude-subagent/Dockerfile .
  docker build -t ghcr.io/jsquirrelz/tide-demo-init:1.0.0 -f images/tide-demo-init/Dockerfile .

  Use `set -e` and `! docker run --rm --entrypoint which <img> git` (the `!` negates the exit code — if which succeeds/finds git, the step fails).

medium_http_test.go spec wiring (replacing Skips with real assertions):
  The kind cluster is already up (BeforeSuite handles this in suite_test.go).
  The git-http server must be loaded into the kind cluster via kind load docker-image (similar to how stub-subagent is loaded in the existing suite).
  
  It("initializes the git-http server via demo-remote-init Job"):
    - Creates namespace medium-http-test
    - kind load docker-image ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --name tide-test (if not already loaded)
    - kubectl apply demo-remote-init-job.yaml into medium-http-test namespace
    - Wait for job Complete with Gomega Eventually 2-minute timeout
    - Assert no Skip
  
  It("git-http server Deployment is Available"):
    - kind load docker-image ghcr.io/jsquirrelz/tide-git-http-server:1.0.0 --name tide-test
    - kubectl apply git-http-server-deployment.yaml into medium-http-test namespace
    - Wait for deployment Available with Gomega Eventually 2-minute timeout
    - Curl the info/refs endpoint from within a busybox pod: exec a pod with curl http://git-http-server.medium-http-test.svc.cluster.local/demo-remote.git/info/refs?service=git-upload-pack expecting HTTP 200 (verifies git-http-backend is serving, RESEARCH Pitfall 1 validation)

  It("medium Project with stub-subagent reaches Complete over http://"):
    - Apply per-namespace-resources.yaml into medium-http-test namespace
    - Mirror tide-signing-key from tide-system into medium-http-test
    - Create Project with targetRepo http://git-http-server.medium-http-test.svc.cluster.local/demo-remote.git, stub-subagent, absoluteCapCents=0
    - Wait for Project status.phase=Complete with Gomega Eventually 10-minute timeout
    - No Skip

Analogous to how bare_project_test.go applies its fixture and waits for conditions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SC-1 image-smoke step to nightly-integration.yml</name>
  <files>.github/workflows/nightly-integration.yml</files>
  <read_first>
    - .github/workflows/nightly-integration.yml — full file (all existing steps; find the correct insertion point after prepare manifests / before Layer B kind integration)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VALIDATION.md §"Per-Task Verification Map" row for SC-1
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question E: CI Coverage Shape"
  </read_first>
  <action>
Insert a new step named "SC-1 image smoke — verify git presence/absence in core images" after the "Prepare manifests and envtest binaries" step and BEFORE the "Layer B kind integration suite" step.

The step body (directive — no fenced code):

Set bash strict mode (set -e). Build the three images locally (docker build from repo root using the respective Dockerfiles). Then run the presence/absence checks:

For tide-push: run `docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-push:1.0.0 git` and assert it exits non-zero (git absent). Use the shell form `! docker run ...` to invert the exit code so the step fails if git IS found.

For claude-subagent: same pattern — `! docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 git` must exit 0 (i.e., the entrypoint must exit non-zero, meaning git is NOT found).

For tide-demo-init: run `docker run --rm ghcr.io/jsquirrelz/tide-demo-init:1.0.0 git --version` (using the default entrypoint is wrong here — tide-demo-init's ENTRYPOINT is the binary, not git). Use `docker run --rm --entrypoint git ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --version` and assert it exits 0 (git present).

Add `timeout-minutes: 10` to this step — docker build for three images should complete well within 10 minutes on a warm cache.

Do NOT add a new job — add a step to the existing `kind-suites` job. Do NOT change any existing step names or ordering (other than the insertion point). Do NOT add `--platform` flags — CI runners are linux/amd64 natively and cross-compilation is for release builds only.
  </action>
  <verify>
    <automated>grep -n 'SC-1 image smoke\|tide-push.*git.*which\|git-http-backend' /Users/justinsearles/Projects/tide/.github/workflows/nightly-integration.yml | head -5 && python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/nightly-integration.yml'))" 2>/dev/null && echo YAML_VALID || echo YAML_INVALID</automated>
  </verify>
  <done>nightly-integration.yml has SC-1 image smoke step with docker build + docker run presence/absence checks; yaml.safe_load parses the file without error</done>
</task>

<task type="auto">
  <name>Task 2: Wire medium_http_test.go spec (replace Skips with real assertions)</name>
  <files>test/integration/kind/medium_http_test.go</files>
  <read_first>
    - test/integration/kind/medium_http_test.go — full file (the scaffolded Skips from 08-01; replace each Skip with real behavior)
    - test/integration/kind/bare_project_test.go — full file (the closest analog: applies YAML fixtures, waits for Project=Complete via k8sClient + Eventually)
    - test/integration/kind/suite_test.go — lines 140-250 (ensureKindCluster, k8sClient, ctx vars, helper functions like skipIfCRDsOnlyMode)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question E: CI Coverage Shape" Option 1 details
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Pitfall 1: git-http-backend PATH_INFO vs GIT_PROJECT_ROOT Misconfiguration"
  </read_first>
  <action>
Rewrite `test/integration/kind/medium_http_test.go` replacing all Skip() calls with real test logic. The file stays in package `kind_integration` with the same Describe block structure from 08-01, but all three It blocks now run actual assertions.

The overall test flow (Ordered Describe):

BeforeAll:
  - skipIfCRDsOnlyMode() call (consistent with other Layer B specs that guard against BeforeSuite failures)
  - Create namespace "medium-http-test" via kubectl (exec.Command, ignore AlreadyExists)
  - Load the git-http-server image into the kind cluster: exec.Command("kind", "load", "docker-image", "ghcr.io/jsquirrelz/tide-git-http-server:1.0.0", "--name", kindClusterName). If the image doesn't exist locally, build it first (docker build).
  - Load the tide-demo-init image similarly
  - Apply per-namespace-resources.yaml (kubectl apply --namespace medium-http-test -f path) — creates tide-projects PVC and tide-subagent SA
  - Mirror tide-signing-key Secret from tide-system into medium-http-test namespace (using the exec pattern: kubectl get secret tide-signing-key -n tide-system -o json | kubectl apply -n medium-http-test -f -)

AfterAll:
  - kubectl delete namespace medium-http-test --ignore-not-found (best effort)

It("initializes the git-http server via demo-remote-init Job"):
  - Apply demo-remote-pvc.yaml into medium-http-test namespace (kubectl apply)
  - Apply demo-remote-init-job.yaml into medium-http-test namespace (kubectl apply)
  - Wait for Job Complete: use Gomega Eventually with a 2-minute timeout polling kubectl get job demo-remote-init -n medium-http-test -o jsonpath='{.status.conditions[?(@.type=="Complete")].status}' == "True"

It("git-http server Deployment reaches Available and serves git protocol"):
  - Apply git-http-server-deployment.yaml into medium-http-test namespace
  - Wait for Deployment Available: Eventually 2-minute timeout polling kubectl rollout status deployment/git-http-server -n medium-http-test
  - Verify git-http-backend serves: run a busybox pod with curl to hit the info/refs endpoint. Use exec.Command to kubectl run a transient pod:
    kubectl run git-smoke --rm -i --restart=Never --image=busybox --namespace=medium-http-test -- wget -q -O- "http://git-http-server.medium-http-test.svc.cluster.local/demo-remote.git/info/refs?service=git-upload-pack"
    Assert the output contains "git-upload-pack" (RESEARCH Pitfall 1 validation command adapted for kubectl run)

It("medium Project with stub-subagent reaches Complete over http://"):
  - Create a Provider Secret (stub API key, base64 of "test-api-key") in medium-http-test namespace
  - Create a tide-secrets Secret with GIT_PAT="" (empty, for anonymous http:// push) in medium-http-test
  - Create a Project CRD (using k8sClient.Create like bare_project_test.go does) with:
    - targetRepo: http://git-http-server.medium-http-test.svc.cluster.local/demo-remote.git
    - spec.git.repoURL: same URL
    - spec.git.credsSecretRef: "tide-secrets"
    - subagent.model: "stub" (stub-subagent, no LLM calls)
    - absoluteCapCents: 0
    - all gates: auto
  - Wait for Project status.phase=Complete: Gomega Eventually 10-minute timeout
  - Assert: k8sClient.Get project and check status.phase == "Complete"

If the cluster doesn't have the stub-subagent image as the controller default for model: "stub", fallback to explicitly setting subagent.image to the test-loaded stub image (following how bare_project_test.go handles this — check the existing test for the pattern).

Keep the same Apache 2.0 license header from 08-01's scaffold. Remove the comment "RED scaffold — assertions wired in plans 08-05 and 08-07".
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./test/integration/kind/... 2>&1 && echo BUILD_OK</automated>
  </verify>
  <done>go build ./test/integration/kind/... exits 0; medium_http_test.go has no Skip() calls remaining; test file compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → docker daemon | Image smoke step builds and inspects production images in CI |
| kind cluster → git-http-server | Medium spec verifies in-cluster HTTP transport using the kind cluster's internal DNS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Denial of Service | CI nightly timeout (new build steps add ~5-7min) | accept | New step has timeout-minutes: 10; kind-suites job already has timeout-minutes: 45; margin is sufficient |
| T-08-07-02 | Tampering | CI step using docker build without --platform (images built for wrong arch in CI) | accept | GitHub Actions ubuntu-latest runners are linux/amd64; TIDE images are amd64; no cross-compilation needed for CI smoke test; release uses --platform=$BUILDPLATFORM |
| T-08-07-03 | Information Disclosure | Medium spec logs potentially containing cluster internals | accept | Specs use GinkgoWriter for logs; kind logs are exported only on failure (existing pattern); no secrets in the medium test (GIT_PAT is empty) |
</threat_model>

<verification>
1. `grep -n 'SC-1 image smoke' .github/workflows/nightly-integration.yml` → 1 result
2. `grep -n 'tide-push.*which.*git\|which.*tide-push.*git' .github/workflows/nightly-integration.yml` → 1+ results
3. `grep -n 'tide-demo-init.*git.*version' .github/workflows/nightly-integration.yml` → 1 result
4. `go build ./test/integration/kind/...` → exit 0
5. `grep -c 'Skip(' test/integration/kind/medium_http_test.go` → 0 (no Skips remaining)
6. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/nightly-integration.yml'))"` → no error
</verification>

<success_criteria>
- nightly-integration.yml has SC-1 image-smoke step with git presence/absence assertions for all 3 images
- medium_http_test.go has real assertions (no Skip() calls); compiles cleanly
- make test-int can discover and run medium_http_test.go specs (auto-discovery via package)
</success_criteria>

<output>
After completion, create `.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-07-SUMMARY.md`
</output>
