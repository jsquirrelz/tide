---
phase: 08-medium-sample-http-transport-and-production-git-transport-po
plan: 08
type: execute
wave: 5
depends_on: [08-07]
files_modified: []
autonomous: false
requirements: []

user_setup:
  - service: anthropic
    why: "Live minikube re-test requires real Claude Haiku API key (~$5)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → API Keys"
    dashboard_config:
      - task: "Ensure API key has sufficient credits for ~$5 Haiku run"
        location: "console.anthropic.com → Billing"

must_haves:
  truths:
    - "kubectl wait for project/medium-project -n tide-sample-medium status.phase=Complete exits 0"
    - "kubectl get job -n tide-sample-medium shows push Jobs (controller ran push after task completion)"
    - "per-run branch appears in git-http-server (git ls-remote from inside cluster)"
    - "docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-push:1.0.0 git exits 1 (git absent in rebuilt image)"
    - "docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 git exits 1 (git absent)"
    - "docker run --rm --entrypoint git ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --version exits 0 (git retained)"
    - "grep -rn ':v1.' examples/ | grep image: returns 0 results (all tags aligned)"
  artifacts:
    - path: "examples/projects/medium/"
      provides: "Complete medium sample manifest set"
      contains: "git-http-server"
    - path: ".planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md"
      provides: "Phase gate with SC-2 evidence"
      contains: "gate_decision"
  key_links:
    - from: "minikube cluster"
      to: "project/medium-project status.phase=Complete"
      via: "kubectl wait"
      pattern: "Complete"
---

<objective>
Wave 5 (final gate, depends on all prior plans): Live minikube re-test with real Claude (Haiku) to validate SC-2. This is a human-verify checkpoint because it requires:
1. A real ANTHROPIC_API_KEY (~$5 spend on Haiku 4.5)
2. Manual minikube image reload (stale images from pre-Phase-8 must be force-removed and reloaded)
3. Human observation that the full medium sample drives Project=Complete on the minikube cluster

After human confirms SC-2 is green, the executor creates the 08-VERIFICATION.md phase gate artifact and the phase is complete.

Purpose: SC-2 is the integration proof that all Phase 8 changes work together in the real environment. No CI gate can substitute for this because it requires a real LLM API key and real cluster execution.

Output:
- Human verification that medium-project reaches Complete on minikube with real Haiku
- .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/RESUME.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare minikube for live re-test (rebuild + reload reverted images + new image)</name>
  <files></files>
  <read_first>
    - .planning/RESUME.md — §"Live environment" section and §"Hard rules" (minikube image reload gotcha; must rmi -f before reloading)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Pitfall 4: Stale Minikube Image Tags After Revert"
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Runtime State Inventory" (existing minikube state)
  </read_first>
  <action>
Prepare the minikube cluster for the live medium re-test. Execute in order:

Step A — Verify minikube is up: `kubectl config current-context` must show "minikube"; `kubectl cluster-info --context=minikube` must succeed. If minikube is down, start it: `minikube start --driver=docker --cpus=4 --memory=6g`.

Step B — Build the three updated images locally:
  docker build -t ghcr.io/jsquirrelz/tide-push:1.0.0 -f images/tide-push/Dockerfile .
  docker build -t ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 -f images/claude-subagent/Dockerfile .
  docker build -t ghcr.io/jsquirrelz/tide-git-http-server:1.0.0 -f images/tide-git-http-server/Dockerfile .
  (tide-demo-init is already correct from 93595b9 — rebuild only if Dockerfile changed in this phase)

Step C — SC-1 local verification before reloading (Verify Before Claiming — CLAUDE.md rule):
  `docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-push:1.0.0 git` → MUST exit 1
  `docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 git` → MUST exit 1
  `docker run --rm --entrypoint git ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --version` → MUST exit 0
  If any assertion fails, STOP. Diagnose the Dockerfile before proceeding.

Step D — Force-remove stale minikube image digests (per Pitfall 4):
  minikube ssh -- docker rmi -f ghcr.io/jsquirrelz/tide-push:1.0.0 ghcr.io/jsquirrelz/tide-push:v1.0.0 2>/dev/null || true
  minikube ssh -- docker rmi -f ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 ghcr.io/jsquirrelz/tide-claude-subagent:v1.0.0 2>/dev/null || true
  (Also force-remove the old tide-demo-init at both tags if it was rebuilt)
  The `|| true` is intentional — the image may not exist yet (fresh cluster state).

Step E — Load updated images into minikube:
  minikube image load ghcr.io/jsquirrelz/tide-push:1.0.0
  minikube image load ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0
  minikube image load ghcr.io/jsquirrelz/tide-git-http-server:1.0.0
  minikube image load ghcr.io/jsquirrelz/tide-demo-init:1.0.0

Step F — SC-1 post-reload verification (Verify Before Claiming):
  minikube ssh -- docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-push:1.0.0 git → exit non-zero
  minikube ssh -- docker run --rm --entrypoint git ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --version → exit 0

Step G — SC-6 verification:
  `grep -rn ':v1\.' examples/ | grep image:` → 0 results

Step H — Apply medium sample 9-step sequence per examples/projects/medium/README.md:
  Steps 1-4 in sequence (namespace, demo-remote-pvc, per-namespace-resources, signing-key mirror).
  Step 5: kubectl apply demo-remote-init-job.yaml && kubectl wait --for=condition=Complete --timeout=2m.
  Step 6: kubectl apply git-http-server-deployment.yaml && kubectl wait --for=condition=Available --timeout=2m.
  Step 7: kubectl create secret tide-secrets with ANTHROPIC_API_KEY and GIT_PAT="" in tide-sample-medium.
  Note: If tide-sample-medium namespace already exists from the previous failed run, clean it up first: kubectl delete namespace tide-sample-medium --ignore-not-found; wait for it to terminate fully before re-applying.

If any step fails, STOP and diagnose. Report the failure mode before proceeding to Task 2.
  </action>
  <verify>
    <automated>kubectl wait --for=condition=Available deployment/git-http-server -n tide-sample-medium --timeout=2m 2>&1 && kubectl get pods -n tide-sample-medium && echo INFRA_READY</automated>
  </verify>
  <done>git-http-server Deployment is Available in tide-sample-medium; all per-namespace deps applied; demo-remote-init Job is Complete; tide-secrets Secret created; SC-1 local and minikube verification passed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 8 infrastructure is in place on minikube:
    - tide-push and claude-subagent images rebuilt WITHOUT git binary (SC-1)
    - tide-demo-init image retains git binary (SC-1)
    - git-http-server image built and loaded into minikube
    - Medium sample 9-step apply sequence executed: namespace, PVCs, signing-key mirror, init Job (Complete), git-http-server Deployment (Available), tide-secrets Secret
    - Now applying the Project CRD for the live Haiku run (~$5)

    The executor will apply examples/projects/medium/project.yaml and monitor progress.
  </what-built>
  <how-to-verify>
    Step 1 — Apply the medium project:
      kubectl apply -f examples/projects/medium/project.yaml

    Step 2 — Monitor the cascade (expect 5-15 minutes for real Haiku):
      kubectl get project/medium-project -n tide-sample-medium -w
      Watch for status.phase progression: Pending → Initialized → Planning → Executing → Complete

    Step 3 — Wait for completion (30 minute hard timeout):
      kubectl wait --for=jsonpath='{.status.phase}'=Complete project/medium-project -n tide-sample-medium --timeout=30m

    Step 4 — Verify push happened (a per-run branch was pushed to the in-cluster http:// remote):
      kubectl exec -n tide-sample-medium deployment/git-http-server -- git ls-remote /srv/git/demo-remote.git
      Expect to see at least one tide/run-medium-project-* branch in the output.

    Step 5 — Confirm SC-1 on rebuilt images:
      docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-push:1.0.0 git
      Expected: exit 1 (git NOT found — correct, git was removed in 08-02)

      docker run --rm --entrypoint git ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --version
      Expected: exit 0 + git version output (git retained in demo-init — correct)

    Step 6 — Confirm SC-6:
      grep -rn ':v1.' examples/ | grep image:
      Expected: 0 results (all tags aligned to 1.0.0 no-v)

    Report: type "approved" when all 6 steps confirm green, or describe the failure mode if any step fails.
  </how-to-verify>
  <resume-signal>Type "approved" when medium-project reaches Complete with real Haiku and per-run branch is visible; or describe the specific failure if the run does not complete</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create 08-VERIFICATION.md phase gate artifact</name>
  <files>.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-VERIFICATION.md — full file (the exact format used by gsd-verify-work; gate_decision: APPROVED shape, success_criteria checks, evidence shape)
  </read_first>
  <action>
Create `.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md` following the exact same YAML frontmatter + body format as `07-VERIFICATION.md`. The frontmatter must include:
  gate_decision: APPROVED (only after human confirms in Task 2)
  phase: 08
  verified_at: (current timestamp)
  
Body sections:
- Summary: Phase 8 closed the v1.0 ship-quality gap; medium sample drives Project=Complete on real Haiku via in-cluster http:// transport
- SC-1 evidence: docker run git absence/presence results (copy actual terminal output)
- SC-2 evidence: kubectl wait exit code + git ls-remote output showing per-run branch
- SC-3 evidence: make test pass output (CEL admission tests GREEN)
- SC-4 evidence: grep output for false mount claim (0 results)
- SC-5 evidence: make test-int output showing medium_http_test.go specs passed
- SC-6 evidence: grep output (0 :v1. tags in examples/)
- Release sequencing note: v1.0.0 tag held at 8a8e843; after Phase 8 APPROVED, delete and re-create at new HEAD (user confirms before push)

Only create this file AFTER the human-verify checkpoint returns "approved".
  </action>
  <verify>
    <automated>grep -c '^gate_decision: APPROVED$' /Users/justinsearles/Projects/tide/.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md</automated>
  </verify>
  <done>08-VERIFICATION.md exists with gate_decision: APPROVED; all 6 SC evidence sections present; phase gate artifact committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ANTHROPIC_API_KEY → minikube Secret | Real key injected as Secret; never logged; never committed |
| in-cluster git remote → push Job | Push Job authenticates with GIT_PAT="" (anonymous; accepted by git-http-backend with http.receivepack=true) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Information Disclosure | ANTHROPIC_API_KEY in tide-secrets Secret | mitigate | kubectl create secret (not in manifest files); never committed; live test only; delete Secret after test with kubectl delete secret tide-secrets -n tide-sample-medium |
| T-08-08-02 | Tampering | per-run branch pushed to in-cluster http:// remote (unauthenticated) | accept | Demo-only in-cluster remote; ClusterIP not exposed externally; anonymous push intentional for demo context; production uses authenticated https:// |
| T-08-08-03 | Denial of Service | Live Haiku run cost overrun | accept | Hard cap absoluteCapCents: 500 ($5) in project.yaml; if cap is hit controller halts dispatch; expected cost well under $5 for the tight outcome prompt |
</threat_model>

<verification>
SC-1: `docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-push:1.0.0 git` → exit 1
SC-1: `docker run --rm --entrypoint which ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 git` → exit 1
SC-1: `docker run --rm --entrypoint git ghcr.io/jsquirrelz/tide-demo-init:1.0.0 --version` → exit 0
SC-2: `kubectl wait --for=jsonpath='{.status.phase}'=Complete project/medium-project -n tide-sample-medium --timeout=30m` → exit 0
SC-2: `kubectl exec -n tide-sample-medium deployment/git-http-server -- git ls-remote /srv/git/demo-remote.git` → shows tide/run-* branch
SC-3: `make test` → exit 0 (CEL admission tests GREEN)
SC-4: `grep -n 'demo-remote-pvc' examples/projects/medium/README.md` → no false-claim lines
SC-5: `make test-int` → exit 0 (medium_http_test.go specs included + passing)
SC-6: `grep -rn ':v1\.' examples/ | grep image:` → 0 results
</verification>

<success_criteria>
- SC-2: medium-project reaches Complete on real minikube with real Claude Haiku via in-cluster http:// transport
- SC-2: per-run branch visible in git-http-server (proves push over anonymous http:// worked)
- SC-1: tide-push and claude-subagent rebuilt without git; tide-demo-init retains git
- All 6 SC assertions green as documented in 08-VERIFICATION.md gate_decision: APPROVED
- Phase 8 complete; v1.0.0 tag ready to be moved to post-Phase-8 HEAD (user confirms before push)
</success_criteria>

<output>
After completion, create `.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-SUMMARY.md`
Update ROADMAP.md Phase 8 Plans: count to 8 plans and list all 8.
Update STATE.md to reflect Phase 8 COMPLETE.
</output>
