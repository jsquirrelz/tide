---
phase: 08-medium-sample-http-transport-and-production-git-transport-po
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/kind/testdata/bare-project.yaml
  - examples/projects/small/project.yaml
  - test/integration/envtest/admission_test.go
  - test/integration/kind/medium_http_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "grep -rn 'targetRepo.*file://' test/ returns 0 results (all test fixtures migrated)"
    - "examples/projects/small/project.yaml uses targetRepo https://git.example.internal/stub/no-such-repo.git"
    - "test/integration/envtest/admission_test.go contains a Project CEL admission Describe block testing file:// rejection"
    - "test/integration/kind/medium_http_test.go contains a Ginkgo spec skeleton for the medium-http path (RED — no implementation yet)"
  artifacts:
    - path: "test/integration/kind/testdata/bare-project.yaml"
      provides: "fixture with sentinel targetRepo (https://git.example.internal/stub/no-such-repo.git)"
      contains: "git.example.internal"
    - path: "examples/projects/small/project.yaml"
      provides: "small sample with sentinel targetRepo"
      contains: "git.example.internal"
    - path: "test/integration/envtest/admission_test.go"
      provides: "CEL admission tests for Project targetRepo validation"
      contains: "Project CEL"
    - path: "test/integration/kind/medium_http_test.go"
      provides: "Ginkgo spec for hermetic medium-http kind test"
      contains: "medium.*http"
  key_links:
    - from: "test/integration/kind/testdata/bare-project.yaml"
      to: "CEL validator (api/v1alpha1/project_types.go)"
      via: "admission on kubectl apply"
      pattern: "git.example.internal"
    - from: "test/integration/envtest/admission_test.go"
      to: "Project CEL XValidation"
      via: "k8sClient.Create rejection"
      pattern: "file:// is not a supported"
---

<objective>
Wave 0: Migrate test fixtures away from file:// targetRepo and scaffold the two missing test files (CEL admission test + medium-http kind spec) before the CEL validator change lands.

Purpose: RESEARCH Pitfall 6 makes this mandatory — the CEL change in 08-03 will break any fixture still carrying a file:// targetRepo. Wave 0 must land first so the Layer B suite remains green after 08-03 commits. The test scaffolds for SC-3 and SC-5 exist in this wave so executors in later waves know exactly which file/function they are making GREEN.

Output:
- `test/integration/kind/testdata/bare-project.yaml` — sentinel migrated (no file:// targetRepo)
- `examples/projects/small/project.yaml` — sentinel migrated
- `test/integration/envtest/admission_test.go` — new Project CEL Describe block (RED until 08-03 codegen lands in chart)
- `test/integration/kind/medium_http_test.go` — new Ginkgo spec file (RED until 08-05 git-http server image + manifests exist)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From test/integration/kind/testdata/bare-project.yaml (lines 37-44 — the stale field):
  targetRepo: "file:///tmp/no-such-repo"   ← CHANGE TO sentinel

From examples/projects/small/project.yaml (line 37):
  targetRepo: file:///tmp/no-such-repo     ← CHANGE TO sentinel

Sentinel (RFC 2606 .example TLD — non-routable; stub-subagent ignores targetRepo entirely):
  targetRepo: https://git.example.internal/stub/no-such-repo.git

From test/integration/envtest/admission_test.go — existing package header:
  package envtest_integration
  Describe("Plan Admission Webhook", Label("envtest"), ...) — the existing Describe block to model the new Project CEL block on.

From test/integration/kind/suite_test.go — Layer B package:
  package kind_integration
  Ginkgo boilerplate: Label("kind"), ordered or unordered Describe blocks.

From test/integration/kind/bare_project_test.go — analog for new medium_http_test.go Describe shape:
  The bare_project test uses k8sClient (suite-level var), ctx, GinkgoWriter.
  It applies a fixture YAML, waits for a Project condition, then cleans up.

CEL error message we will assert (from api/v1alpha1/project_types.go line 272 — current, pre-change):
  "targetRepo must be a http(s), SSH, or file:// git URL"
  After 08-03 change, the message will be:
  "targetRepo must be an http(s) or SSH (git@) URL; file:// is not a supported production transport"
  The admission test should assert the NEW message (testing what will exist after the CEL change).
  Mark the block with a comment: "GREEN after 08-03 lands — CEL marker change + controller-gen + make helm".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all test fixtures from file:// to sentinel targetRepo</name>
  <files>test/integration/kind/testdata/bare-project.yaml, examples/projects/small/project.yaml</files>
  <read_first>
    - test/integration/kind/testdata/bare-project.yaml — full file (the stale line 43 to replace)
    - examples/projects/small/project.yaml — lines 30-50 (sentinel + comment block)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question B CEL targetRepo Validation" and §"Small sample sentinel problem"
  </read_first>
  <action>
In `test/integration/kind/testdata/bare-project.yaml`, change line 43's value from `"file:///tmp/no-such-repo"` to `"https://git.example.internal/stub/no-such-repo.git"`. Update the surrounding comment to reflect the new sentinel: the stub-subagent ignores targetRepo; the https://git.example.internal domain is RFC 2606 reserved and non-routable, making the sentinel's intent unmistakable. The sentinel must pass the new CEL rule (starts with https://) while the stub-subagent continues to ignore it (per the file's existing comment about stub behavior).

In `examples/projects/small/project.yaml`, change the targetRepo field from `file:///tmp/no-such-repo` to `https://git.example.internal/stub/no-such-repo.git`. Update the inline comment explaining why the stub ignores the targetRepo value; reference that file:// is no longer a supported production transport (per 08-03's CEL change). The small sample's behavior is unchanged — stub-subagent returns canned envelopes regardless of targetRepo.

Do NOT change any other YAML field in either file. Do not change spec.git blocks — the bare-project.yaml has no git block and the small sample has no git block either (D-04 guard: git ops are skipped when spec.git is nil or empty repoURL).
  </action>
  <verify>
    <automated>grep -rn 'targetRepo.*file://' test/ examples/projects/small/ && echo FAIL || echo PASS</automated>
  </verify>
  <done>grep -rn 'targetRepo.*file://' test/ returns zero results; grep -n 'git.example.internal' test/integration/kind/testdata/bare-project.yaml examples/projects/small/project.yaml returns 2 results (one per file)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Project CEL admission tests to envtest admission_test.go</name>
  <files>test/integration/envtest/admission_test.go</files>
  <read_first>
    - test/integration/envtest/admission_test.go — full file (read the entire existing Plan Admission Webhook Describe block for the exact pattern: k8sClient.Create, apierrors.IsInvalid, StatusReasonInvalid, error message assertion)
    - api/v1alpha1/project_types.go — lines 270-285 (current XValidation marker text — executor must know current message vs post-08-03 message)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question B: CEL targetRepo Validation"
  </read_first>
  <behavior>
    - Test A: k8sClient.Create with Project targetRepo "file:///tmp/test" returns apierrors.IsInvalid error containing "file:// is not a supported production transport" (RED until 08-03 CEL marker change; mark with TODO comment)
    - Test B: k8sClient.Create with Project targetRepo "https://git.example.internal/stub/no-such-repo.git" returns nil (accepted)
    - Test C: k8sClient.Create with Project targetRepo "http://git-http-server/demo-remote.git" returns nil (accepted — http:// starts with http:// matches the new rule)
    - Test D: k8sClient.Create with Project targetRepo "git@github.com:owner/repo.git" returns nil (accepted)
  </behavior>
  <action>
Append a new top-level `Describe("Project CEL targetRepo admission", Label("envtest"), ...)` block to `test/integration/envtest/admission_test.go` (below the existing Plan Admission Webhook Describe block). Use the same package name (`package envtest_integration`) and the same `k8sClient` suite-level variable.

Each It block creates a minimal Project object using `tideprojectv1alpha1.Project` with: Name generated unique (fmt.Sprintf("cel-test-%d", GinkgoRandomSeed())), Namespace `admissionNamespace` (the const already defined in the file), and only the required Spec fields: TargetRepo, ProviderSecretRef (set to "any-secret" — admission doesn't check the secret exists for CEL validation), Budget.AbsoluteCapCents (0 is valid — absoluteCapCents:0 means disabled). No spec.git block.

For the REJECT tests (Test A): assert `apierrors.IsInvalid(err)` is true AND `err.Error()` contains "file:// is not a supported production transport". Add a comment: `// RED until 08-03 lands (CEL marker change + controller-gen + make helm regenerates CRD schema)`.

For the ACCEPT tests (Tests B, C, D): assert `err == nil`. Add AfterEach cleanup (k8sClient.Delete) for objects that succeeded. The existing Plan Admission Webhook AfterEach is plan-scoped; add a separate project-scoped AfterEach.

Do NOT use any fenced code blocks in this action. Do NOT import new packages — the existing admission_test.go already imports apierrors, metav1, tideprojectv1alpha1, and gomega. Do NOT add the `git@` test if it would require a non-trivial Project struct field not already in the existing tests — keep the git@ case simple (just targetRepo).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make test 2>&1 | tail -30</automated>
  </verify>
  <done>make test passes (the new Project CEL tests are RED for the file:// rejection assertions because the CEL marker hasn't changed yet — but they must COMPILE and the ACCEPT tests must pass); the test file has no syntax errors; `go build ./test/integration/envtest/...` exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold medium_http_test.go in Layer B kind suite</name>
  <files>test/integration/kind/medium_http_test.go</files>
  <read_first>
    - test/integration/kind/suite_test.go — lines 1-130 (package header, imports, suite-level vars, BeforeSuite shape)
    - test/integration/kind/bare_project_test.go — full file (the closest analog: applies a fixture Project, waits for status.phase=Complete, cleanup)
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-RESEARCH.md §"Question E: CI Coverage Shape" Option 1 description
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VALIDATION.md §"Wave 0 Requirements" row for medium_http_test.go
  </read_first>
  <action>
Create `test/integration/kind/medium_http_test.go` with package `kind_integration` (matching suite_test.go). Include the standard Apache 2.0 license header.

The file contains a single top-level `Describe("Medium http transport", Label("kind"), Ordered, ...)` block. Inside, declare constants for the test namespace (`medium-http-test`) and service name (`git-http-server`).

The spec has three It blocks:

It("initializes the git-http server via demo-remote-init Job") — applies the demo-remote-init Job from `examples/projects/medium/demo-remote-init-job.yaml` into the test namespace (using kubectl apply via exec.Command, consistent with how other Layer B specs apply fixtures), waits for Job condition Complete with a 2-minute timeout, then marks the test Pending with GinkgoWriter.Printf and `Skip("git-http-server image not yet built — pending 08-05")`.

It("git-http server Deployment is Available") — applies `examples/projects/medium/git-http-server-deployment.yaml`, waits for Deployment Available condition 2-minute timeout, Skip("pending 08-05").

It("medium Project with stub-subagent reaches Complete over http://") — creates a Project with targetRepo `http://git-http-server.medium-http-test.svc.cluster.local/demo-remote.git`, subagent.model stub, absoluteCapCents 0, waits for status.phase=Complete 10-minute timeout. Skip("pending 08-05 + 08-07 wiring").

Add BeforeAll to create the `medium-http-test` namespace (kubectl create namespace, ignore AlreadyExists). Add AfterAll cleanup (kubectl delete namespace medium-http-test --ignore-not-found).

All It blocks should Skip immediately — this file is the RED scaffold. The Skips are replaced by real assertions in 08-05 and 08-07. Do NOT add this test to any Makefile target manually — the Ginkgo suite discovery picks it up automatically because it's in the `kind_integration` package. Add a comment at the top of the file: `// RED scaffold — assertions wired in plans 08-05 (git-http server) and 08-07 (CI nightly wiring)`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./test/integration/kind/... 2>&1</automated>
  </verify>
  <done>go build ./test/integration/kind/... exits 0 (no compile errors); the file exists at test/integration/kind/medium_http_test.go; grep -n 'pending 08-05' test/integration/kind/medium_http_test.go returns at least 2 results</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → apiserver | YAML test fixtures cross the admission control boundary; a stale file:// fixture will break admission after 08-03 CEL change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | bare-project.yaml fixture | mitigate | Migrate sentinel before CEL change; makes the test suite coherent regardless of CEL state |
| T-08-01-02 | Denial of Service | make test-int Layer B | accept | Test scaffolds compile but skip — no test-suite breakage; skips are replaced in later plans |
</threat_model>

<verification>
1. `grep -rn 'targetRepo.*file://' test/ examples/projects/small/` → 0 results
2. `grep -n 'git.example.internal' test/integration/kind/testdata/bare-project.yaml` → 1 result
3. `grep -n 'git.example.internal' examples/projects/small/project.yaml` → 1 result
4. `cd /Users/justinsearles/Projects/tide && go build ./test/integration/...` → exit 0
5. `cd /Users/justinsearles/Projects/tide && make test` → passes (new CEL tests compile; file:// rejection tests are RED at runtime but that is expected before 08-03)
</verification>

<success_criteria>
- All file:// targetRepo values in test/ and examples/projects/small/ replaced with the https://git.example.internal/stub/no-such-repo.git sentinel
- test/integration/envtest/admission_test.go compiles and contains Project CEL admission test block
- test/integration/kind/medium_http_test.go compiles and contains the medium-http Ginkgo Describe skeleton
- make test exits 0 (compile + unit + envtest layer pass; new CEL file:// rejection tests may show as SKIP or FAIL during this wave — that is expected)
</success_criteria>

<output>
After completion, create `.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-01-SUMMARY.md`
</output>
