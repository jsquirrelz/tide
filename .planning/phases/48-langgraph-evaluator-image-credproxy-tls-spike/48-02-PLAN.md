---
phase: 48-langgraph-evaluator-image-credproxy-tls-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/dispatch/podjob/jobspec.go
  - internal/dispatch/podjob/jobspec_readonly_test.go
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "BuildJobSpec with ReadOnly:true produces a Job whose subagent /workspace mount is ReadOnly, with a /scratch emptyDir and ReadOnlyRootFilesystem:true"
    - "BuildJobSpec with ReadOnly:false (and zero-value legacy callers) produces byte-identical behavior to today — no regression"
    - "No git-write/push credential secret name appears in any container's Env/EnvFrom of a built Job, regardless of ReadOnly — proven by a regression test, not assumed"
    - "No reconciler dispatches the ReadOnly variant this phase (Phase 51 wires dispatch)"
  artifacts:
    - path: "internal/dispatch/podjob/jobspec.go"
      provides: "ReadOnly bool field on BuildOptions + boolean-gated branch in BuildJobSpec (RESEARCH Pattern 2)"
      contains: "ReadOnly bool"
    - path: "internal/dispatch/podjob/jobspec_readonly_test.go"
      provides: "TestBuildJobSpec_Verifier_* static assertions (D-09a)"
      contains: "TestBuildJobSpec_Verifier"
  key_links:
    - from: "internal/dispatch/podjob/jobspec_readonly_test.go"
      to: "internal/dispatch/podjob/jobspec.go"
      via: "BuildJobSpec(BuildOptions{ReadOnly: true})"
      pattern: "ReadOnly:\\s*true"
---

<objective>
Land D-08's read-only verifier jobspec variant as a single `ReadOnly bool` field on the existing `BuildOptions`/`BuildJobSpec` (RESEARCH Pattern 2 — reuse the `credproxyEnabled`-style boolean branch, NOT a forked `buildVerifierJobSpec`), plus the D-09a static unit assertions including the git-credential-absence regression test. Tests are written FIRST (RED), then the field lands (GREEN). Nothing dispatches this variant — Phase 51 wires dispatch.

Purpose: EVAL-01's "read-only enforced structurally" language becomes real in-repo (Phase 48 success criterion 3's mount/credential layer), unit-proven before the behavioral adversarial test (plan 48-04) exercises it in a real container.
Output: `ReadOnly` field + branch in jobspec.go, `jobspec_readonly_test.go` with the `TestBuildJobSpec_Verifier_*` family, full podjob suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-CONTEXT.md
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-RESEARCH.md

<interfaces>
Verified seams in `internal/dispatch/podjob/jobspec.go` (direct read, 2026-07-18):
- `type BuildOptions struct` at :82 (`Kind JobKind` :86, existing fields) — the `ReadOnly bool` field lands here
- `func BuildJobSpec(opts BuildOptions) *batchv1.Job` at :199
- `credproxyEnabled := opts.Project != nil && opts.Project.Spec.ProviderSecretRef != ""` at :322 — the boolean-branch idiom to mirror
- subagent `/workspace` mount (`VolumeProjectWorkspace`, currently RW) at :396
- credproxy cert mount `ReadOnly: true` precedent at :434
- subagent `ReadOnlyRootFilesystem: new(false)` (with explanatory comment) at :450-451
- `VolumeProjectWorkspace = "project-workspace"` const at :57

Credential isolation already true by construction (verified: `internal/controller/push_helpers.go` — `GIT_PAT` via `project.Spec.Git.CredsSecretRef` is injected ONLY into the separate `tide-push` Job): the credential task below is a regression TEST asserting absence, not new omission logic.

Existing test style: `internal/dispatch/podjob/jobspec_test.go` — plain table-style `func TestBuildJobSpec_*(t *testing.T)` (NOT Ginkgo), e.g. `TestBuildJobSpec_SubagentDoesNotReceiveProviderSecret_envFrom` at :309 is the closest analog for the credential-absence assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing TestBuildJobSpec_Verifier_* assertions (RED)</name>
  <files>internal/dispatch/podjob/jobspec_readonly_test.go</files>
  <read_first>internal/dispatch/podjob/jobspec_test.go (test helper/style — especially TestBuildJobSpec_SubagentDoesNotReceiveProviderSecret_envFrom :309 and TestBuildJobSpec_VolumesIncludeProjectWorkspaceAndCertShared :357), internal/dispatch/podjob/jobspec.go (:82 BuildOptions, :199 BuildJobSpec, :394-460 subagent container wiring), 48-RESEARCH.md (§Pattern 2)</read_first>
  <action>Create `jobspec_readonly_test.go` (same package as jobspec_test.go) with the `TestBuildJobSpec_Verifier_*` family, referencing the not-yet-existing `BuildOptions.ReadOnly` field so the package fails to compile — that compile failure is this task's RED state. Assertions to author: (1) `TestBuildJobSpec_Verifier_WorkspaceMountReadOnly` — with `ReadOnly: true`, the subagent container's `VolumeProjectWorkspace` mount has `ReadOnly: true`; (2) `TestBuildJobSpec_Verifier_ScratchEmptyDirMounted` — a `verifier-scratch` emptyDir volume exists in the pod spec and is mounted read-write at `/scratch` on the subagent container; (3) `TestBuildJobSpec_Verifier_ReadOnlyRootFilesystem` — the subagent container's SecurityContext has `ReadOnlyRootFilesystem: true` when `ReadOnly: true`; (4) `TestBuildJobSpec_Verifier_NoGitCredsInAnyContainer` — build a Job from a Project whose `Spec.Git.CredsSecretRef` is set, then assert that secret name appears in NO container's (containers + initContainers) `Env` or `EnvFrom`, for BOTH `ReadOnly: true` and `ReadOnly: false` (D-08 regression test: creds are tide-push-Job-only by construction); (5) `TestBuildJobSpec_Verifier_DefaultUnchanged` — with `ReadOnly: false` (and with a zero-value BuildOptions legacy shape), the workspace mount is RW, no `verifier-scratch` volume exists, and `ReadOnlyRootFilesystem` is false (current :450 behavior). Use the existing table-style plain go-test idiom, reusing jobspec_test.go's option-builder helpers rather than duplicating fixture setup.</action>
  <verify>
    <automated>! go test ./internal/dispatch/podjob/ -run TestBuildJobSpec_Verifier 2>&1 | grep -q 'ok  '</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/dispatch/podjob/ -run TestBuildJobSpec_Verifier` FAILS (compile error on the missing ReadOnly field — RED confirmed)
    - `grep -c 'func TestBuildJobSpec_Verifier' internal/dispatch/podjob/jobspec_readonly_test.go` outputs ≥5
    - The credential-absence test constructs a Project with `Spec.Git.CredsSecretRef` set (source assertion: grep `CredsSecretRef` in the new test file returns ≥1)
  </acceptance_criteria>
  <done>Five failing assertions authored covering mount, scratch, rootfs, credential-absence, and default-path non-regression.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the ReadOnly BuildOptions field + branch (GREEN)</name>
  <files>internal/dispatch/podjob/jobspec.go</files>
  <read_first>internal/dispatch/podjob/jobspec.go (:82, :322, :394-475), internal/dispatch/podjob/jobspec_readonly_test.go (the contract just authored), 48-RESEARCH.md (§Pattern 2 — the exact field shape and branch sites)</read_first>
  <action>Add `ReadOnly bool` to `BuildOptions` (:82) with a doc comment stating: it marks the read-only verifier variant (D-08); when true the `/workspace` mount is ReadOnly, a `verifier-scratch` emptyDir is added at `/scratch`, and `ReadOnlyRootFilesystem` flips true on the subagent container; git-write/push credentials are never added to any BuildJobSpec Job regardless of this flag (tide-push-Job-only by construction — the regression test pins it); AND a forward-note that when Phase 51 wires dispatch, the envelope write-back path (`/workspace/envelopes/<uid>/out.json`) will need a separate read-write `envelopes/` subPath mount of the same volume — out.json cannot be written through a ReadOnly mount, and resolving that mount split is explicitly Phase 51 dispatch-wiring scope, not this phase's (nothing dispatches this spec yet). Implement the branch in `BuildJobSpec` mirroring the `credproxyEnabled` idiom at :322: set the subagent workspace mount's `ReadOnly: opts.ReadOnly` (:396), append the `verifier-scratch` emptyDir volume + `/scratch` mount when `opts.ReadOnly`, and make the subagent SecurityContext's `ReadOnlyRootFilesystem` follow `opts.ReadOnly` (updating the :450 comment to explain both branches). Do NOT introduce a `JobKindVerifier` const (RESEARCH Open Question 2 resolution: smallest diff, boolean only — Phase 51 introduces the JobKind when dispatch needs role labels). Do NOT touch `pkg/dispatch` (the Subagent interface + envelope contract stay literally unchanged — CLAUDE.md pluggable-runtime seam).</action>
  <verify>
    <automated>go test ./internal/dispatch/podjob/... && go build ./... && make verify-dispatch-imports</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/dispatch/podjob/ -run TestBuildJobSpec_Verifier` passes (GREEN)
    - `go test ./internal/dispatch/podjob/...` passes in full (all pre-existing TestBuildJobSpec_* unaffected)
    - `git diff --stat pkg/dispatch/` is empty (seam untouched)
    - `grep -rn 'ReadOnly:\s*true' internal/controller/ --include='*.go' | grep -v _test | grep -ci 'BuildOptions'` outputs 0 (no reconciler dispatches the variant this phase)
    - The ReadOnly doc comment names the Phase-51 envelopes-subPath forward-note (source assertion: grep `envelopes` on the BuildOptions comment block returns ≥1)
  </acceptance_criteria>
  <done>ReadOnly variant implemented per Pattern 2, all five Verifier assertions green, full podjob suite green, seam and reconcilers untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier pod → PVC worktree | untrusted agentic process gets filesystem access to the Task worktree |
| verifier pod → git credentials | a verifier must never hold push/write credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | verifier `/workspace` mount | mitigate | `ReadOnly: true` VolumeMount (kernel/mount-namespace enforcement, ASVS V4) + `ReadOnlyRootFilesystem: true` + writes redirected to `/scratch` emptyDir — asserted by TestBuildJobSpec_Verifier_WorkspaceMountReadOnly/_ScratchEmptyDirMounted/_ReadOnlyRootFilesystem |
| T-48-02 | Information Disclosure / Elevation of Privilege | git push credentials in verifier env | mitigate | already absent by construction (tide-push-Job-only, push_helpers.go); TestBuildJobSpec_Verifier_NoGitCredsInAnyContainer pins it as a regression test so a future refactor cannot silently re-introduce them |
| T-48-03 | Tampering | symlink/bind escape inside the worktree | accept | K8s ReadOnly mount is enforced below the filesystem-path layer — a symlink pointing outside the worktree is still read-only from the pod (RESEARCH §Known Threat Patterns); behavioral confirmation rides plan 48-04's adversarial test |
</threat_model>

<verification>
- `go test ./internal/dispatch/podjob/...` fully green
- `make verify-dispatch-imports` green (pkg/dispatch untouched)
- No reconciler references `ReadOnly: true` in BuildOptions construction (dispatch is Phase 51)
</verification>

<success_criteria>
- Phase 48 success criterion 3's mount/credential layer exists and is unit-proven (D-09a static layer; D-09b behavioral layer lands in plan 48-04)
- RESEARCH Pattern 2 followed exactly: one field, one function, boolean branch — no forked BuildVerifierJobSpec, no JobKindVerifier
- The Phase-51 envelopes-write-back mount question is documented on the field, not silently deferred
</success_criteria>

<output>
Create `.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-02-SUMMARY.md` when done.
</output>
