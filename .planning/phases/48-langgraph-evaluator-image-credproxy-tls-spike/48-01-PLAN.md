---
phase: 48-langgraph-evaluator-image-credproxy-tls-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/tide-langgraph-verifier/requirements.in
  - cmd/tide-langgraph-verifier/requirements.txt
  - cmd/tide-langgraph-verifier/requirements-dev.in
  - cmd/tide-langgraph-verifier/requirements-dev.txt
  - cmd/tide-langgraph-verifier/pyproject.toml
  - cmd/tide-langgraph-verifier/verifier/__init__.py
  - cmd/tide-langgraph-verifier/verifier/tests/__init__.py
  - cmd/tide-langgraph-verifier/verifier/tests/conftest.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_sanity.py
  - .dockerignore
  - .gitignore
  - Makefile
  - .github/workflows/ci.yaml
autonomous: true
requirements: [EVAL-01, EVAL-02]
must_haves:
  truths:
    - "Every direct Python dependency is patch-exact pinned (==) in requirements*.in with a hash-locked requirements*.txt lockfile"
    - "make verify-langgraph-pins exits 0 on the real pin files and exits 1 when any range/unpinned specifier is present"
    - "make test-langgraph-verifier stands up the pytest environment and runs the Python test suite (VALIDATION.md Wave 0 closed)"
    - "No openinference-*/opentelemetry-* package appears in any requirements file (RESEARCH Pitfall E)"
  artifacts:
    - path: "cmd/tide-langgraph-verifier/requirements.in"
      provides: "7 patch-exact runtime pins (D-10)"
      contains: "langgraph==1.2.9"
    - path: "cmd/tide-langgraph-verifier/requirements.txt"
      provides: "hash-locked transitive closure"
      contains: "--hash=sha256:"
    - path: "cmd/tide-langgraph-verifier/pyproject.toml"
      provides: "pytest configuration (first Python test framework in the repo)"
    - path: "cmd/tide-langgraph-verifier/verifier/tests/conftest.py"
      provides: "fixture-worktree builder + envelope-dict factory fixtures"
    - path: "Makefile"
      provides: "verify-langgraph-pins + test-langgraph-verifier targets"
      contains: "verify-langgraph-pins"
  key_links:
    - from: ".github/workflows/ci.yaml"
      to: "Makefile verify-langgraph-pins"
      via: "CI step invoking the pin gate"
      pattern: "verify-langgraph-pins"
    - from: ".dockerignore"
      to: "cmd/tide-langgraph-verifier/"
      via: "re-include line (deny-by-default ** otherwise excludes the Python tree)"
      pattern: "tide-langgraph-verifier"
---

<objective>
Stand up the `cmd/tide-langgraph-verifier/` Python tree: patch-exact pinned + hash-locked dependencies (D-10), the repo's first pytest infrastructure (VALIDATION.md "Wave 0 Requirements"), shared test fixtures, and the `make verify-langgraph-pins` CI gate. This is the scaffolding wave — every later plan's Python tests depend on the fixtures and pytest config created here.

Purpose: EVAL-02's pinning clause (Phase 48 success criterion 4: "Every Python dependency is patch-exact pinned, and a CI gate rejects any unpinned/range specifier") plus the test infrastructure EVAL-01's Python unit tests require.
Output: `cmd/tide-langgraph-verifier/` skeleton with lockfiles, pytest config, conftest fixtures, two new Makefile targets, CI wiring, `.dockerignore`/`.gitignore` entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-CONTEXT.md
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-RESEARCH.md
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-VALIDATION.md

<interfaces>
Pin set (D-10, re-verified live against PyPI 2026-07-18, zero drift — RESEARCH §Standard Stack):

```
langgraph==1.2.9
langchain==1.3.14
langchain-anthropic==1.4.8
langchain-core==1.4.9
anthropic==0.117.0
pydantic==2.13.4
httpx==0.28.1
```

Lockfile generation (proven end-to-end in the research session): `uv pip compile requirements.in --generate-hashes --output-file requirements.txt` — output includes manylinux cp313 wheel hashes even when compiled on macOS. Pass `--python-platform linux --python-version 3.13` for determinism.

Pin-gate grep expression (RESEARCH §Code Examples, mirrors `verify-dispatch-imports` Makefile:523 style): a violation = any line surviving `grep -vE '^\s*(#|$)'` that does not match `^[A-Za-z0-9_.-]+==[0-9]`.

Envelope fixture field names (verbatim json tags from `pkg/dispatch/envelope.go:45-165` + `pkg/dispatch/provider.go`): `apiVersion`, `kind`, `taskUID`, `role`, `level`, `prompt`, `filesTouched`, `declaredOutputPaths`, `caps`, `proxyEndpoint`, `signedToken`, `provider` (`vendor`/`model`/`params`). Discriminator values: `apiVersion == "dispatch.tideproject.k8s/v1alpha1"`, `kind == "TaskEnvelopeIn"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold cmd/tide-langgraph-verifier — pins, lockfiles, pytest config, conftest fixtures</name>
  <files>cmd/tide-langgraph-verifier/requirements.in, cmd/tide-langgraph-verifier/requirements.txt, cmd/tide-langgraph-verifier/requirements-dev.in, cmd/tide-langgraph-verifier/requirements-dev.txt, cmd/tide-langgraph-verifier/pyproject.toml, cmd/tide-langgraph-verifier/verifier/__init__.py, cmd/tide-langgraph-verifier/verifier/tests/__init__.py, cmd/tide-langgraph-verifier/verifier/tests/conftest.py, cmd/tide-langgraph-verifier/verifier/tests/test_sanity.py, .dockerignore, .gitignore, Makefile</files>
  <read_first>.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-RESEARCH.md (§Standard Stack, §Installation, §Package Legitimacy Audit, §Recommended Project Structure), .dockerignore, Makefile (`eval:` ~line 243 and `verify-dispatch-imports:` ~line 523 for target/help-comment style), pkg/dispatch/envelope.go (EnvelopeIn json tags :45-165), pkg/dispatch/provider.go (ProviderSpec json tags :36-60)</read_first>
  <action>Create `cmd/tide-langgraph-verifier/requirements.in` with the exact 7 runtime pins from the interfaces block (D-10) — no other packages, and explicitly NO `openinference-*`/`opentelemetry-*`/`deepagents` per RESEARCH Pitfall E and the REQUIREMENTS.md Out-of-Scope table. Create `requirements-dev.in` with a patch-exact `pytest==<latest stable, resolved at execution time>` pin; before adding it, run `slopcheck` against `pytest` on the pypi ecosystem (the researcher-sanctioned mechanism for packages beyond the 7 already-audited pins — RESEARCH §Package Legitimacy Audit) and abort if the verdict is not OK. Generate both lockfiles with `uv pip compile <in> --generate-hashes --python-platform linux --python-version 3.13 --output-file <txt>`. Create `pyproject.toml` containing only `[tool.pytest.ini_options]` (`testpaths = ["verifier/tests"]`) — no packaging metadata; the image COPYs source directly. Create the `verifier/` package skeleton (`__init__.py`, `tests/__init__.py`) and `tests/conftest.py` with two shared fixtures: (1) `fixture_worktree` — builds a real git repository in `tmp_path` (git init, local user.name/user.email config, one committed tracked file) representing an ALREADY-MATERIALIZED Task worktree per RESEARCH Pitfall D — downstream tests must never call `git worktree add`; (2) `envelope_in_dict` — a factory returning a valid `TaskEnvelopeIn` dict using the verbatim field names and discriminator values in the interfaces block (the wire contract the Python image re-implements, D-03). Add `tests/test_sanity.py` asserting both fixtures build, so the suite collects ≥1 test this wave. Add the `!cmd/tide-langgraph-verifier/**` re-include to `.dockerignore` (deny-by-default `**` otherwise silently excludes the tree) and `cmd/tide-langgraph-verifier/.venv/` to `.gitignore`. Add a `make test-langgraph-verifier` target: idempotently creates `cmd/tide-langgraph-verifier/.venv` via `uv venv --python 3.13`, installs both lockfiles, runs `python -m pytest verifier/tests/ -x -q`. Mirror the existing Makefile `## description` help-comment style.</action>
  <verify>
    <automated>make test-langgraph-verifier</automated>
  </verify>
  <acceptance_criteria>
    - `make test-langgraph-verifier` exits 0 and pytest reports ≥1 passed test
    - `cd cmd/tide-langgraph-verifier && grep -vE '^\s*(#|$)' requirements.in requirements-dev.in | grep -vE ':[A-Za-z0-9_.-]+==[0-9]' | wc -l` outputs 0 (every non-comment line is patch-exact pinned)
    - `grep -c -- '--hash=sha256:' cmd/tide-langgraph-verifier/requirements.txt` outputs a number > 30 (full transitive closure hash-locked)
    - `grep -ciE 'openinference|opentelemetry|deepagents' cmd/tide-langgraph-verifier/requirements.in cmd/tide-langgraph-verifier/requirements.txt` outputs 0 for each file (Pitfall E)
    - `grep -c 'tide-langgraph-verifier' .dockerignore` outputs ≥1
    - slopcheck verdict for pytest recorded as [OK] in the task output
  </acceptance_criteria>
  <done>Python tree scaffolded; lockfiles hash-locked; pytest runs green via make; .dockerignore/.gitignore entries in place.</done>
</task>

<task type="auto">
  <name>Task 2: make verify-langgraph-pins gate + negative self-test + CI wiring</name>
  <files>Makefile, .github/workflows/ci.yaml</files>
  <read_first>Makefile (`verify-dispatch-imports:` ~line 523, `verify-no-aggregates:` ~line 564 — the grep-gate style to mirror), .github/workflows/ci.yaml (lines ~55-85 where verify-no-aggregates/verify-dispatch-imports run), .planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-RESEARCH.md (§Code Examples — the exact gate recipe)</read_first>
  <action>Add a `verify-langgraph-pins` Makefile target implementing the RESEARCH §Code Examples recipe, extended in two ways: (1) it greps ALL files matching `cmd/tide-langgraph-verifier/requirements*.in` (runtime AND dev pins — Phase 48 success criterion 4 says "every Python dependency", not just runtime), failing with an "EVAL-02 violation" message listing offending lines when any non-comment line does not match `^[A-Za-z0-9_.-]+==[0-9]`; (2) it accepts a `PINS_GLOB` override variable (defaulting to the real glob) so a negative self-test can point it at a fixture. Note the grep must filter comments/blank lines first (`grep -vE '^\s*(#|$)'`) — a bare count would self-invalidate on header comments. Wire the gate into `.github/workflows/ci.yaml` as a step adjacent to the existing `make verify-dispatch-imports` step (~line 81) — same job, no new job needed, no Python setup required (the gate is pure grep). Do NOT wire `test-langgraph-verifier` into CI in this task (the CI Python-setup step lands with the image build in plan 48-04, where docker is already provisioned). Verify the negative path by creating a temp file containing `langgraph>=1.2` and invoking the gate with `PINS_GLOB` pointed at it, asserting exit 1.</action>
  <verify>
    <automated>make verify-langgraph-pins && T=$(mktemp -d)/req.in && printf 'langgraph>=1.2\n' > "$T" && ! make verify-langgraph-pins PINS_GLOB="$T"</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-langgraph-pins` exits 0 against the real pin files
    - `make verify-langgraph-pins PINS_GLOB=<temp file containing 'langgraph>=1.2'>` exits 1 and prints the offending line
    - `grep -c 'verify-langgraph-pins' .github/workflows/ci.yaml` outputs ≥1
    - `git diff --stat` shows only Makefile + ci.yaml touched by this task
  </acceptance_criteria>
  <done>Pin gate green on real files, red on a range specifier, and running in CI (v1.0.8 release-cascade lesson: gates land in phase CI, not release pre-flight).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → repo | third-party package code enters the build via pip/uv |
| CI → merge | unpinned/tampered dependencies could land without the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-SC | Tampering (supply chain) | pip installs (7 runtime pins + pytest) | mitigate | RESEARCH §Package Legitimacy Audit ran slopcheck 7/7 [OK] on the runtime pins; this plan runs slopcheck on pytest before adding it; patch-exact pins + `--generate-hashes` lockfiles; `verify-langgraph-pins` grep gate in CI; `pip install --require-hashes` enforcement lands in the Dockerfile (plan 48-04) |
| T-48-SC2 | Tampering (config drift) | requirements*.in edited with a range specifier | mitigate | `verify-langgraph-pins` rejects any non-`==` specifier in BOTH runtime and dev pin files, wired into ci.yaml this plan |
| T-48-OTEL | Information Disclosure (double span emission) | transitive OTel auto-instrumentation | mitigate | requirements.in excludes all `openinference-*`/`opentelemetry-*` packages (Pitfall E); acceptance criterion greps both pin files; build-time `pip list` assertion lands in plan 48-04 |
</threat_model>

<verification>
- `make test-langgraph-verifier` green (pytest collects and passes the sanity test)
- `make verify-langgraph-pins` green on real files; exit 1 on a `>=` fixture
- ci.yaml contains the verify-langgraph-pins step
- No openinference/opentelemetry/deepagents string in any requirements file
</verification>

<success_criteria>
- VALIDATION.md Wave 0 items closed: pyproject.toml (pytest config), conftest.py fixtures, .dockerignore re-include
- Phase 48 success criterion 4 satisfied at the source layer: patch-exact pins + CI gate rejecting ranges (Dockerfile `--require-hashes` enforcement completes it in plan 48-04)
- Wave 2/3 plans can write pytest tests against `fixture_worktree` and `envelope_in_dict` without further setup
</success_criteria>

<output>
Create `.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-01-SUMMARY.md` when done.
</output>
