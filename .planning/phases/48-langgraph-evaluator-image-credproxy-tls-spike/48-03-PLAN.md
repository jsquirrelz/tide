---
phase: 48-langgraph-evaluator-image-credproxy-tls-spike
plan: 03
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - cmd/tide-langgraph-verifier/verifier/envelope.py
  - cmd/tide-langgraph-verifier/verifier/tools.py
  - cmd/tide-langgraph-verifier/verifier/agent.py
  - cmd/tide-langgraph-verifier/verifier/__main__.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_tools.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_agent.py
autonomous: true
requirements: [EVAL-01]
must_haves:
  truths:
    - "A skewed apiVersion or kind in in.json is rejected with a hard failure before any other processing (strict equality, first step — D-03)"
    - "The image exposes exactly two tools — git_read (allowlisted read-only git subcommands against the already-materialized worktree) and run_gate_command — and no checkpointer"
    - "The agent is built via langchain.agents.create_agent with an explicit config recursion_limit at invoke time (never the 9999 framework default)"
    - "The happy path emits a trivial EnvelopeOut (exitCode 0, one-line result, no findings/Git/ChildCRDs) plus a ≤4KB TerminationStub — exercising the output path, not a verdict schema (D-01)"
    - "Any failure exits nonzero with a structured TerminationStub reason — fail-closed, never a silent success"
  artifacts:
    - path: "cmd/tide-langgraph-verifier/verifier/envelope.py"
      provides: "Python re-implementation of the EnvelopeIn/EnvelopeOut/TerminationStub JSON wire shapes + strict validation (D-03)"
      contains: "dispatch.tideproject.k8s/v1alpha1"
    - path: "cmd/tide-langgraph-verifier/verifier/tools.py"
      provides: "@tool git_read + @tool run_gate_command (the only two tools)"
      contains: "run_gate_command"
    - path: "cmd/tide-langgraph-verifier/verifier/agent.py"
      provides: "create_agent wiring with explicit recursion_limit"
      contains: "recursion_limit"
    - path: "cmd/tide-langgraph-verifier/verifier/__main__.py"
      provides: "entrypoint: read/validate in.json → agent → out.json + TerminationStub"
  key_links:
    - from: "cmd/tide-langgraph-verifier/verifier/__main__.py"
      to: "cmd/tide-langgraph-verifier/verifier/envelope.py"
      via: "strict-validate before anything else"
      pattern: "read_envelope_in"
    - from: "cmd/tide-langgraph-verifier/verifier/agent.py"
      to: "langchain.agents.create_agent"
      via: "the non-deprecated factory (RESEARCH Pitfall B)"
      pattern: "from langchain.agents import create_agent"
---

<objective>
Implement the verifier runtime: the Python re-implementation of the envelope wire contract (D-03), the two hand-authored tools (D-02), and the `create_agent` loop + entrypoint that emits a trivial `EnvelopeOut` (D-01). Plain `ChatAnthropic` construction throughout — D-07 was REVISED: no defensive client factory, no pre-flight probe, no subclass; the pinned `langchain-anthropic==1.4.8` has no client hook (RESEARCH Pitfall A) and the spike (plan 48-05) IS the TLS measurement.

Purpose: EVAL-01's runtime half — a genuine LangGraph agent behind the unchanged `pkg/dispatch.Subagent` + envelope seam (the Python image conforms to the JSON shape; it cannot import the Go package).
Output: `envelope.py`/`tools.py`/`agent.py`/`__main__.py` + their pytest suites, all green offline (no network, no real API key).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-CONTEXT.md
@.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-RESEARCH.md

<interfaces>
The wire contract the Python image re-implements (verbatim json tags, direct read of `pkg/dispatch/envelope.go` + `provider.go`):

EnvelopeIn (:45): `apiVersion` (must equal `"dispatch.tideproject.k8s/v1alpha1"`), `kind` (must equal `"TaskEnvelopeIn"`), `taskUID`, `role`, `level`, `prompt`, `promptPath` (opt), `branch` (opt), `filesTouched`, `dependsOn` (opt), `declaredOutputPaths`, `caps`, `proxyEndpoint`, `signedToken`, `provider` `{vendor, model, params}`, `dispatch` (opt), `dev` (opt), `sharedContext` (opt). Unknown fields must be tolerated (accept-and-ignore); only apiVersion/kind are strict-equality validated, FIRST (mirrors `ValidateAPIVersionKind` :446 + `harness.ReadEnvelopeIn` semantics).

EnvelopeOut (:170): `apiVersion` (same value), `kind` = `"TaskEnvelopeOut"`, `exitCode` (0 = success), `result` (one-line summary), `reason` (structured code when exitCode != 0). D-01: no `git`, no `childCRDs` keys emitted (a complete envelope has ExitCode==0 AND len(ChildCRDs)==ChildCount — omitting both leaves ChildCount 0, which is complete).

TerminationStub (:394): tiny-status subset — `exitCode`, `reason`; total serialized size MUST be ≤ 4096 bytes. Written to `$TIDE_TERMINATION_MESSAGE_PATH` (default `/dev/termination-log` — see `cmd/claude-subagent/main.go:169`).

Envelope path resolution (mirrors `cmd/claude-subagent/main.go:79-93`): `$TIDE_ENVELOPE_PATH` if set, else `/workspace/envelopes/$TIDE_TASK_UID/in.json`; `out.json` is written next to `in.json` with permissions 0o644 (setgid-shared envelopes dir — `internal/harness/envelope_io.go` mkdirSharedAll rationale).

Agent factory (RESEARCH Pattern 1, verified signature from the pinned wheel): `langchain.agents.create_agent(model, tools=[...], system_prompt=...)` → invoke with `{"messages": [{"role": "user", "content": ...}]}` and `config={"recursion_limit": 10}`; final text = `result["messages"][-1].content`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: envelope.py — wire-shape re-implementation + strict validation</name>
  <files>cmd/tide-langgraph-verifier/verifier/envelope.py, cmd/tide-langgraph-verifier/verifier/tests/test_envelope.py</files>
  <read_first>pkg/dispatch/envelope.go (:30-60 discriminators + EnvelopeIn, :170-260 EnvelopeOut + completeness, :380-460 TerminationStub + ValidateAPIVersionKind), internal/harness/envelope_io.go (ReadEnvelopeIn/WriteEnvelopeOut semantics + 0o644 + setgid-dir rationale), cmd/tide-langgraph-verifier/verifier/tests/conftest.py (envelope_in_dict fixture from 48-01)</read_first>
  <behavior>
    - Valid TaskEnvelopeIn dict (conftest fixture) round-trips through read_envelope_in
    - apiVersion "dispatch.tideproject.k8s/v1alpha2" (skew) → hard error naming the expected value
    - kind "TaskEnvelopeOut" in an in-file → hard error
    - Malformed JSON → hard error (never a partial object)
    - Unknown extra fields are tolerated (accept-and-ignore)
    - write_envelope_out emits exactly apiVersion/kind/exitCode/result (+reason when nonzero), no git/childCRDs keys, file mode 0o644
    - write_termination_stub refuses/truncates to ≤4096 serialized bytes
  </behavior>
  <action>Implement `envelope.py` as a field-for-field re-implementation of the JSON wire shapes from the interfaces block (D-03 — the Go struct tags are the frozen contract; the Python image cannot import `pkg/dispatch`, per the import-firewall in `pkg/dispatch/doc.go`). Provide `read_envelope_in(path)` (open → json decode → strict apiVersion/kind equality FIRST, mirroring `ValidateAPIVersionKind` — a mismatch is a hard error naming expected vs got), `write_envelope_out(path, ...)` (mkdir parents, 0o644), and `write_termination_stub(path, exit_code, reason)` (≤4096-byte enforcement). Use plain dataclasses or typed dicts — do NOT pull in extra schema libraries; pydantic is already pinned and acceptable if it keeps the code smaller, but no new dependencies. Constants module-level: `API_VERSION = "dispatch.tideproject.k8s/v1alpha1"`, `KIND_IN = "TaskEnvelopeIn"`, `KIND_OUT = "TaskEnvelopeOut"`. Write test_envelope.py first (behavior list above), watch it fail, then implement.</action>
  <verify>
    <automated>cd cmd/tide-langgraph-verifier && .venv/bin/python -m pytest verifier/tests/test_envelope.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest verifier/tests/test_envelope.py -x` passes with ≥6 tests covering all behavior bullets
    - Source assertion: `grep -c 'dispatch.tideproject.k8s/v1alpha1' cmd/tide-langgraph-verifier/verifier/envelope.py` outputs ≥1 (strict discriminator constant present)
    - Source assertion: no import of any `pkg/dispatch`-adjacent Go artifact; imports are stdlib (+pydantic at most)
  </acceptance_criteria>
  <done>Envelope module strict-validates first, emits the trivial out-shape, enforces the 4KB stub cap; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tools.py — git_read + run_gate_command</name>
  <files>cmd/tide-langgraph-verifier/verifier/tools.py, cmd/tide-langgraph-verifier/verifier/tests/test_tools.py</files>
  <read_first>48-RESEARCH.md (§Pattern 1 tool sketches, §Pitfall D — the worktree-add prohibition, §Security Domain — command-injection + path-traversal rows), cmd/tide-langgraph-verifier/verifier/tests/conftest.py (fixture_worktree)</read_first>
  <behavior>
    - git_read("log --oneline -1") against fixture_worktree returns the commit line
    - git_read("show HEAD:<tracked-file>") returns file content
    - git_read rejects mutating/admin subcommands at the tool layer: "commit", "push", "worktree add", "config user.email x", "fetch" → error string, subprocess never spawned
    - git_read rejects option-injection/traversal args: leading "-C", "--git-dir", "--work-tree" tokens → rejected (V12)
    - run_gate_command("true") returns exit_code=0; run_gate_command("false") returns exit_code=1 with combined output
    - Both tools run with cwd pinned to the worktree dir and enforce timeouts (30s git / 60s gate)
  </behavior>
  <action>Implement the two `@tool` functions (from `langchain_core.tools`) — the ONLY tools this image ships (D-02: no file-edit/commit/push tools, no deepagents). `git_read(args)`: split args, validate the first token against a read-only subcommand allowlist (`show`, `diff`, `log`, `ls-tree`, `cat-file`, `rev-parse`, `status`, `ls-files`), reject any token starting with `-` before the subcommand and any `--git-dir`/`--work-tree`/`-C` anywhere (option-injection/V12 traversal guard), then run `["git", ...]` with `cwd=<worktree dir>`, `timeout=30`, no shell. The docstring must state the Pitfall-D invariant: reads the ALREADY-MATERIALIZED Task worktree; never `worktree add`/`commit`/`push`/`config` — those write `.git` admin state and hard-fail under the D-08 ReadOnly mount. `run_gate_command(command)`: `subprocess.run(command, shell=True, cwd=<worktree dir>, timeout=60, check=False)`, returning `exit_code=` + stdout + stderr combined. Its docstring must state the load-bearing security invariant (T-48-05 / ASVS V5): the `command` string originates ONLY from the orchestrator-authored envelope — never from repo content or model output. Worktree dir resolution: a module-level function reading `$TIDE_WORKTREE_DIR` with fallback `/workspace/worktrees/$TIDE_TASK_UID` (the layout `pkg/git/worktree.go:50-58` materializes). Write test_tools.py first against `fixture_worktree`, watch it fail, then implement. The tool-layer allowlist is defense-in-depth ON TOP of the structural mount layer (48-02/48-04) — name that layering in a comment so nobody later "simplifies" one layer away because the other exists.</action>
  <verify>
    <automated>cd cmd/tide-langgraph-verifier && .venv/bin/python -m pytest verifier/tests/test_tools.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest verifier/tests/test_tools.py -x` passes with ≥8 tests covering all behavior bullets
    - Source assertion: `grep -c 'worktree add' cmd/tide-langgraph-verifier/verifier/tools.py` outputs ≥1 (Pitfall-D invariant documented) and no code path constructs a `git worktree` invocation
    - Source assertion: run_gate_command's docstring names the orchestrator-authored-envelope-only invariant (grep `orchestrator` in tools.py returns ≥1)
  </acceptance_criteria>
  <done>Two tools, allowlist-guarded, invariants documented, suite green offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: agent.py + __main__.py — create_agent wiring and the seam-conformant entrypoint</name>
  <files>cmd/tide-langgraph-verifier/verifier/agent.py, cmd/tide-langgraph-verifier/verifier/__main__.py, cmd/tide-langgraph-verifier/verifier/tests/test_agent.py</files>
  <read_first>48-RESEARCH.md (§Pattern 1 — verified create_agent signature + invoke shape, §Pitfall B/C — deprecation + recursion_limit 9999), cmd/claude-subagent/main.go (:79-93 envelope-path resolution, :169 termination-log path — the entrypoint flow to mirror), pkg/dispatch/provider.go (:26 vendor-sentinel rejection semantics), cmd/tide-langgraph-verifier/verifier/envelope.py + tools.py (this plan's modules)</read_first>
  <behavior>
    - build_agent(model) returns a compiled graph wired with exactly [git_read, run_gate_command] and NO checkpointer
    - invoke path passes config recursion_limit=10 explicitly (never the framework 9999/10007 defaults)
    - Offline loop test with a scripted fake chat model (langchain_core fake chat-model utilities): one tool call + final answer terminates, last-message content extracted
    - Entrypoint: missing in.json → exit nonzero + TerminationStub reason "envelope-missing" (or similarly structured)
    - Entrypoint: envelope with provider.vendor != "anthropic" → exit nonzero + structured stub (D-04 sentinel, mirroring provider.go rejection)
    - Entrypoint happy path (agent injected/faked): writes out.json with exitCode 0 + one-line result next to in.json, plus stub
  </behavior>
  <action>`agent.py`: `from langchain.agents import create_agent` (NOT the deprecated `langgraph.prebuilt.create_react_agent` — RESEARCH Pitfall B); `build_agent(model)` compiles the graph with the two tools and `system_prompt` from the envelope prompt; the invoke helper passes `config={"recursion_limit": 10}` explicitly (Pitfall C: create_agent bakes in 9999) and returns `result["messages"][-1].content`. `__main__.py` (entrypoint, `python -m verifier`): resolve the in.json path (`$TIDE_ENVELOPE_PATH` else `/workspace/envelopes/$TIDE_TASK_UID/in.json`, mirroring cmd/claude-subagent/main.go), `read_envelope_in` (strict validation is inherently step one), reject `provider.vendor != "anthropic"` (D-04 — the Phase-51 "langgraph" sentinel question is deferred; do not add a discriminator), construct PLAIN `ChatAnthropic(model=envelope.provider.model, ...)` with the api key taken from the envelope's `signedToken` (falling back to `$ANTHROPIC_API_KEY` for standalone runs) — base URL and CA trust come purely from the pod env (`ANTHROPIC_BASE_URL`, `SSL_CERT_FILE`), which is the entire D-07 REVISED posture: no probe, no subclass, no client injection, no `client=` kwarg (Pitfall A: it silently misroutes into model_kwargs). Run the agent on the envelope prompt, write the trivial EnvelopeOut (exitCode 0, one-line result, no findings/git/childCRDs — D-01) and the ≤4KB TerminationStub; wrap the whole flow so ANY exception exits nonzero with a structured stub reason (fail-closed). Structure `__main__.py` so the model/agent construction is injectable for tests (a `main(argv, build_model=...)` seam), letting test_agent.py drive the full entrypoint offline with a fake chat model and tmp-path envelope dirs. Write test_agent.py first, watch it fail, then implement.</action>
  <verify>
    <automated>cd cmd/tide-langgraph-verifier && .venv/bin/python -m pytest verifier/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Full Python suite (`pytest verifier/tests/ -x`) green offline — no network, no real key
    - Source assertion: `grep -c 'from langchain.agents import create_agent' cmd/tide-langgraph-verifier/verifier/agent.py` outputs 1, and grepping `create_react_agent` across `cmd/tide-langgraph-verifier/verifier/` returns 0 matches
    - Source assertion: `grep -c 'recursion_limit' cmd/tide-langgraph-verifier/verifier/agent.py` outputs ≥1 with an explicit small value
    - Source assertion: `grep -cE 'checkpointer' cmd/tide-langgraph-verifier/verifier/agent.py` outputs 0 or names only the None default (no checkpointer wired — D-02)
    - Source assertion: `grep -cE 'client=|anthropic_client=|http_client=' cmd/tide-langgraph-verifier/verifier/*.py` outputs 0 (D-07 REVISED — no injection attempts)
    - Behavior: entrypoint failure paths exit nonzero and write a structured stub (covered by tests)
  </acceptance_criteria>
  <done>Seam-conformance shell complete per D-01: decode → tools + one model call path → trivial EnvelopeOut + stub, fail-closed, fully offline-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → in.json | controller-authored envelope crosses the PVC into the untrusted-runtime pod |
| repo content → model context | worktree file content read by git_read enters the LLM prompt |
| model output → subprocess | tool arguments produced by the model reach subprocess.run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Spoofing/Tampering | envelope decode (`read_envelope_in`) | mitigate | strict apiVersion/kind equality as the FIRST step (ASVS V5), mirroring ValidateAPIVersionKind; malformed/skewed input is a hard failure, never partial processing — regression tests in test_envelope.py |
| T-48-05 | Tampering (command injection) | `run_gate_command` `shell=True` | mitigate | the gate command string originates ONLY from the orchestrator-authored envelope, never from repo content or model output — documented as the tool's docstring invariant + the entrypoint never feeds model text into the command parameter; ASVS V5 |
| T-48-06 | Information Disclosure / Tampering | `git_read` argument surface | mitigate | read-only subcommand allowlist + rejection of `-C`/`--git-dir`/`--work-tree`/leading-dash tokens + cwd pinned to the worktree (ASVS V12 path-traversal guard) — tested in test_tools.py |
| T-48-07 | Elevation of Privilege | prompt injection via repo content read by git_read | accept | Phase 48's trivial scope has no gate_decision verdict to corrupt (D-01); named now so Phase 49's verdict-schema work inherits the awareness (RESEARCH §Known Threat Patterns) — structural layers (mount/credentials) bound the blast radius |
| T-48-08 | Denial of Service | unbounded agent loop / hung subprocess | mitigate | explicit `recursion_limit: 10` at invoke (Pitfall C) + subprocess timeouts (30s/60s); the K8s Job `activeDeadlineSeconds` remains the ultimate backstop |
</threat_model>

<verification>
- `make test-langgraph-verifier` green (full Python suite: envelope + tools + agent, offline)
- No `create_react_agent`, no checkpointer, no client-injection kwargs anywhere in `verifier/`
- Entrypoint fail-closed paths proven by tests (nonzero exit + structured stub)
</verification>

<success_criteria>
- Phase 48 success criterion 1's runtime half: a genuine LangGraph (`create_agent`) runtime that conforms to the unchanged envelope seam — git-read + bash gate tool only, no checkpointer
- D-01/D-02/D-03/D-04/D-07(revised) all implemented exactly as locked
- Everything testable offline is tested offline; the only untested seam is live TLS (plan 48-05, by design)
</success_criteria>

<output>
Create `.planning/phases/48-langgraph-evaluator-image-credproxy-tls-spike/48-03-SUMMARY.md` when done.
</output>
