---
phase: 43-task-level-parity-trace-context-propagation
plan: 04
type: execute
wave: 3
depends_on: ["43-01", "43-02", "43-03"]
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/dispatch_traceparent_test.go
autonomous: true
requirements: [PROP-01]

must_haves:
  truths:
    - "Milestone/Phase/Plan subagent dispatch Jobs carry env TRACEPARENT whose span-ID segment is the IMMEDIATE PARENT's persisted {Level}TraceSpanID; Project's dispatch Job carries none (root, D-02/D-05)"
    - "All four planner levels' reporter Jobs carry --traceparent whose span-ID segment is THAT level's OWN just-synthesized span — not the grandparent's (RESEARCH Pattern 4: two different recipients, two different parent references)"
    - "A missing/unpersisted parent span ID degrades to an omitted TRACEPARENT env (empty string → no env var), never a malformed value"
    - "The two traceparent recipients are never conflated: dispatch-prep reads the PARENT's field; reporter-spawn reads the level's OWN field"
  artifacts:
    - path: "internal/controller/dispatch_helpers.go"
      provides: "spawnReporterIfNeeded threads traceParent into ReporterOptions"
      contains: "traceParent"
    - path: "internal/controller/dispatch_traceparent_test.go"
      provides: "envtest proof of both hops: dispatch-Job TRACEPARENT env + reporter-Job --traceparent Arg"
      min_lines: 80
  key_links:
    - from: "internal/controller/{milestone,phase,plan}_controller.go dispatch-prep"
      to: "parent CRD .status.{Level}TraceSpanID"
      via: "traceparentForLevel(project, parentHex) → BuildOptions.TraceParent"
      pattern: "traceparentForLevel"
    - from: "internal/controller/*_controller.go completion handlers"
      to: "ReporterOptions.TraceParent"
      via: "spawnReporterIfNeeded param (Milestone/Phase) and inline literals (Plan/Project) — all FOUR sites, not one shared helper (43-PATTERNS call-shape table)"
      pattern: "TraceParent:"
---

<objective>
Thread real traceparent values through the carriers plan 43-02 built, at both pod hops for the four planner levels: each level's own subagent dispatch Job gets its IMMEDIATE PARENT's span (dispatch-prep hop), and each level's reporter Job gets that level's OWN span (reporter hop).

Purpose: Completes PROP-01 for the four planner levels (Task's hop lands in plan 43-05). The values are inert this phase — Phase 44's reporter TracerProvider and Phase 45's self-instrumenting runtime are the consumers — but the contract must be correct now, per level, per hop.

Output: Four dispatch-prep sites + four reporter-spawn sites wired; new envtest file proves the created Jobs carry the right W3C strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-task-level-parity-trace-context-propagation/43-CONTEXT.md
@.planning/phases/43-task-level-parity-trace-context-propagation/43-PATTERNS.md

Critical distinction (RESEARCH Pattern 4 — do NOT build one value for both uses):
| Recipient | Traceparent's span-ID segment | Read from |
|-----------|------------------------------|-----------|
| This level's subagent dispatch Job | the IMMEDIATE PARENT's persisted span ID | parent CRD's `.status.{Level}TraceSpanID` at dispatch-prep (a different, earlier-written durable value) |
| This level's reporter Job | THIS level's OWN just-synthesized span ID | the level's own `Status.{Level}TraceSpanID` — persisted by 43-03's second patch and mirrored in-memory in the same reconcile |

Reporter call-shape table (43-PATTERNS — verified, do NOT assume all four route through the shared helper):
| Level | Shape | Site |
|-------|-------|------|
| Milestone | via spawnReporterIfNeeded | milestone_controller.go:~608 |
| Phase | via spawnReporterIfNeeded | phase_controller.go:~545 |
| Plan | inline BuildReporterJob(...) | plan_controller.go:~599-601 |
| Project | inline BuildReporterJob(...) | project_controller.go:~1876-1877 |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch-prep TRACEPARENT at the four planner BuildOptions sites</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go</files>
  <read_first>
    - internal/controller/milestone_controller.go (BuildOptions literal at line ~444 and the surrounding dispatch-prep function — `project` is already resolved and nil-guarded here per CR-01)
    - internal/controller/phase_controller.go (BuildOptions at ~409; dispatch-prep resolves only Project via resolveProject at ~362 — Milestone is NOT in scope, a new Get is required)
    - internal/controller/plan_controller.go (BuildOptions at ~429; resolveProjectForPlan's label fast-path at ~361 skips Phase entirely — a new Get is required)
    - internal/controller/project_controller.go (BuildOptions at ~1720 — Project is the root; no TraceParent)
    - internal/controller/span_emission.go (traceparentForLevel helper from plan 43-03 — the single-line call each site uses)
  </read_first>
  <action>
    Add `TraceParent:` to three of the four planner BuildOptions literals, sourcing the IMMEDIATE PARENT's persisted span ID (D-05, RESEARCH Immediate-Parent-Fetch Asymmetry — this is the dispatch-time half of the 6-fetch budget):

    - milestone_controller.go (~444): `TraceParent: traceparentForLevel(project, project.Status.ProjectTraceSpanID)` — Project is Milestone's immediate parent and already resolved; no new fetch.
    - phase_controller.go (~409): before the literal, fetch the parent Milestone — `var parentMs tideprojectv1alpha3.Milestone` via `r.Get(ctx, client.ObjectKey{Namespace: ph.Namespace, Name: ph.Spec.MilestoneRef}, &parentMs)`; on error or empty ref, pass `""` (helper handles it — env omitted, non-fatal). Then `TraceParent: traceparentForLevel(project, parentMs.Status.MilestoneTraceSpanID)`.
    - plan_controller.go (~429): same shape via `plan.Spec.PhaseRef` → `parentPh.Status.PhaseTraceSpanID`.
    - project_controller.go (~1720): NO TraceParent field — Project is the run root (D-02); its dispatch Job legitimately carries no TRACEPARENT. Leave the literal untouched except a one-line comment noting the deliberate omission.

    Empty-value semantics are already correct end-to-end: `traceparentForLevel` returns "" for missing/invalid input, and BuildJobSpec omits the env var for "" (plan 43-02) — no special-case branches at these call sites.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/ -run 'TestTraceparentForLevel' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `grep -c 'TraceParent:' internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go` returns exactly 1 per file at the BuildOptions literal (reporter sites come in Task 2)
    - `grep -c 'TraceParent:' internal/controller/project_controller.go` returns 0 after this task (root omission)
    - phase_controller.go and plan_controller.go each gained exactly one new parent `r.Get` in their dispatch-prep path (visible in `git diff`)
  </acceptance_criteria>
  <done>Three planner dispatch Jobs carry parent-derived TRACEPARENT; Project's carries none; missing parent data degrades to omission.</done>
</task>

<task type="auto">
  <name>Task 2: Reporter traceparent — spawnReporterIfNeeded param + all four ReporterOptions sites</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go (spawnReporterIfNeeded, lines ~93-133 — full signature and the BuildReporterJob call inside)
    - .planning/phases/43-task-level-parity-trace-context-propagation/43-PATTERNS.md (§dispatch_helpers.go — the reporter call-shape table with all four exact sites)
    - internal/controller/plan_controller.go (~599-601) and internal/controller/project_controller.go (~1876-1877) — the two INLINE BuildReporterJob sites that do NOT route through the helper
  </read_first>
  <action>
    1. dispatch_helpers.go: add a trailing `traceParent string` parameter to `spawnReporterIfNeeded` and thread it into the internal call as `ReporterOptions{ReporterImage: reporterImage, TraceParent: traceParent}`.
    2. Update all FOUR reporter sites to pass the level's OWN span traceparent (Pattern 4 — own span, not the parent's), sourced from the level's own status field (persisted by 43-03's second patch and mirrored in-memory when emitted this reconcile, so the value is correct in both the same-reconcile and later-reconcile spawn cases):
       - milestone_controller.go (~608, via helper): append arg `traceparentForLevel(project, ms.Status.MilestoneTraceSpanID)`.
       - phase_controller.go (~545, via helper): `traceparentForLevel(project, ph.Status.PhaseTraceSpanID)`.
       - plan_controller.go (~599-601, inline literal): add `TraceParent: traceparentForLevel(project, plan.Status.PlanTraceSpanID)` to the `ReporterOptions{...}`.
       - project_controller.go (~1876-1877, inline literal): `TraceParent: traceparentForLevel(project, project.Status.ProjectTraceSpanID)`.
    If span emission never happened for a level (marker unstamped, degraded run), the field is empty → helper returns "" → BuildReporterJob omits the arg — the reporter Job still functions exactly as today.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 (both helper call sites compile with the new arg)
    - `grep -c 'TraceParent:' internal/controller/dispatch_helpers.go` returns 1
    - plan_controller.go and project_controller.go each show `TraceParent:` inside their inline `ReporterOptions{...}` literal (grep confirms 1 occurrence in each beyond Task 1's BuildOptions hit — total `TraceParent:` count: plan_controller.go == 2, project_controller.go == 1)
    - The reporter value at every site references the level's OWN status field (source assertion: milestone site references `ms.Status.MilestoneTraceSpanID`, never `project.Status.ProjectTraceSpanID`)
  </acceptance_criteria>
  <done>All four reporter Jobs carry their spawning level's own-span traceparent; helper and inline shapes both updated; degraded case omits the arg.</done>
</task>

<task type="auto">
  <name>Task 3: Envtest proof — dispatch-hop env and reporter-hop Arg</name>
  <files>internal/controller/dispatch_traceparent_test.go</files>
  <read_first>
    - internal/controller/dispatch_image_test.go (the reconcile → Eventually-fetch-created-Job → assert-container-field pattern, including fixture setup for a dispatchable Plan — mirror this structure)
    - internal/controller/span_emission_test.go (Milestone block fixtures + how handleJobCompletion is invoked directly with a synthetic terminal Job — reuse for the reporter-hop spec)
    - internal/controller/reporter_jobspec.go (reporter Job naming/labels, to locate the spawned reporter Job in the API)
  </read_first>
  <action>
    Create `internal/controller/dispatch_traceparent_test.go` (Ginkgo, `Label("envtest", "heavy")`, package `controller` — same suite bootstrap) with three specs:

    1. **Dispatch hop, value present (Plan level — exercises the genuinely-new parent fetch):** build a Project→Milestone→Phase→Plan fixture chain where the parent Phase's `Status.PhaseTraceSpanID` is seeded to a known 16-hex value (e.g. `b7ad6b7169203331`) via a status update; drive Plan dispatch (mirror dispatch_image_test.go's plan-dispatch fixture); Eventually fetch the created planner Job and assert the subagent container has env `TRACEPARENT` equal to `"00-" + expectedTraceIDHex + "-b7ad6b7169203331-01"`, where expectedTraceIDHex comes from `otelai.TraceIDFromUID(string(project.UID))` on the API-assigned UID.
    2. **Dispatch hop, value absent:** same chain but with the parent Phase's field left empty — assert the created Job has NO env var named `TRACEPARENT`.
    3. **Reporter hop (Milestone level):** reuse the span_emission_test.go-style Milestone completion fixture (TracerProvider swap FIRST per WR-04); invoke `handleJobCompletion` with a synthetic succeeded Job; then fetch the spawned reporter Job from the API and assert its container Args contain `"--traceparent=00-" + expectedTraceIDHex + "-" + ms.Status.MilestoneTraceSpanID + "-01"` where the status field is re-read after the handler returns (proves emit → persist → mirror → reporter threading end-to-end in one reconcile).
  </action>
  <verify>
    <automated>make test-heavy</automated>
  </verify>
  <acceptance_criteria>
    - `make test-heavy` exits 0 with the three new specs included in the run output
    - Spec 1 asserts the FULL W3C string (all four segments), not just env-var presence
    - Spec 3's expected span-ID segment comes from the re-fetched CRD status, not a hardcoded value
    - `make test-int-fast` exits 0 (no regression in the broader Layer A tier)
  </acceptance_criteria>
  <done>Both hops proven at controller level with exact W3C strings; absent-parent case proves omission; heavy tier and Layer A green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager → dispatched pods | traceparent values injected into Jobs are derived exclusively from manager-written `.status` fields — never from subagent/reporter output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08 | Tampering | TRACEPARENT env / --traceparent Arg | accept | Manager-authored end-to-end (RESEARCH.md Security Domain / T-308 model); an adversarial pod can lie in its own future spans but cannot influence the manager's |
| T-43-09 | Information Disclosure | trace IDs visible in Job specs (kubectl describe) | accept | Trace/span IDs are not secrets per OTel threat model (RESEARCH.md V6) |
| T-43-SC | Tampering | Supply chain | accept | No new packages this phase — Package Legitimacy Audit N/A per 43-RESEARCH.md |
</threat_model>

<verification>
- `go build ./...` green after each task
- `make test-heavy` green (three new specs)
- `make test-int-fast` green (Layer A gate)
</verification>

<success_criteria>
PROP-01 complete for the four planner levels: dispatch Jobs carry the parent's span as TRACEPARENT env, reporter Jobs carry the level's own span as --traceparent Arg, the two recipients are never conflated, and missing data degrades to omission — all proven by envtest with exact W3C strings.
</success_criteria>

<output>
Create `.planning/phases/43-task-level-parity-trace-context-propagation/43-04-SUMMARY.md` when done.
</output>
