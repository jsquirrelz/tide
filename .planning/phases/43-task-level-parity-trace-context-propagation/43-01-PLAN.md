---
phase: 43-task-level-parity-trace-context-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha3/milestone_types.go
  - api/v1alpha3/phase_types.go
  - api/v1alpha3/plan_types.go
  - api/v1alpha3/project_types.go
  - api/v1alpha3/task_types.go
  - config/crd/bases/tideproject.k8s_milestones.yaml
  - config/crd/bases/tideproject.k8s_phases.yaml
  - config/crd/bases/tideproject.k8s_plans.yaml
  - config/crd/bases/tideproject.k8s_projects.yaml
  - config/crd/bases/tideproject.k8s_tasks.yaml
autonomous: true
requirements: [PROP-02]

must_haves:
  truths:
    - "All five CRD Status structs carry a flat {Level}TraceSpanID string field storing this level's own synthesized OTel SpanID hex (D-03)"
    - "TaskStatus additionally carries TaskSpanEmittedUID, mirroring the four existing planner-level idempotency markers (D-04)"
    - "The new fields are additive-only: no existing status field is renamed, removed, or re-typed"
    - "Regenerated CRD manifests expose all six new JSON properties, so envtest can persist them"
  artifacts:
    - path: "api/v1alpha3/task_types.go"
      provides: "TaskSpanEmittedUID + TaskTraceSpanID on TaskStatus"
      contains: "TaskTraceSpanID"
    - path: "api/v1alpha3/milestone_types.go"
      provides: "MilestoneTraceSpanID on MilestoneStatus"
      contains: "MilestoneTraceSpanID"
    - path: "config/crd/bases/tideproject.k8s_tasks.yaml"
      provides: "regenerated CRD schema with taskSpanEmittedUID + taskTraceSpanID properties"
      contains: "taskTraceSpanID"
  key_links:
    - from: "api/v1alpha3/*_types.go"
      to: "config/crd/bases/*.yaml"
      via: "make manifests generate"
      pattern: "traceSpanID"
---

<objective>
Add the durable per-level span-ID carrier (PROP-02) to all five CRD Status structs — `MilestoneTraceSpanID`, `PhaseTraceSpanID`, `PlanTraceSpanID`, `ProjectTraceSpanID`, `TaskTraceSpanID` — plus Task's net-new `TaskSpanEmittedUID` idempotency marker, and regenerate CRD manifests.

Purpose: These fields are the durable parent-carrier surface every other Phase 43 plan reads and writes — child levels read their parent's field to build remote SpanContexts and TRACEPARENT strings; completion handlers write their own field after span emission. Nothing else in the phase compiles against real data until these exist.

Output: Six new additive status fields across five `*_types.go` files + regenerated `config/crd/bases/*.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-task-level-parity-trace-context-propagation/43-CONTEXT.md
@.planning/phases/43-task-level-parity-trace-context-propagation/43-PATTERNS.md

Field-shape decision (CONTEXT.md "Claude's Discretion", resolved here): flat `{Level}TraceSpanID string` fields, NOT a nested `Status.Trace` struct — matches the existing flat `{Level}SpanEmittedUID` house style. Store ONLY the SpanID hex (`trace.SpanID.String()`, 16 hex chars), never the TraceID (always re-derivable from `Project.UID` via `otelai.TraceIDFromUID`). PROP-02's literal `.status.trace` wording is satisfied by intent (durable, small, re-readable carrier) per CONTEXT.md's explicit discretion grant — note this in the SUMMARY for the verifier.
</context>

<interfaces>
Field names downstream plans compile against (fixed contract — do not vary):

```go
// MilestoneStatus:  MilestoneTraceSpanID string `json:"milestoneTraceSpanID,omitempty"`
// PhaseStatus:      PhaseTraceSpanID     string `json:"phaseTraceSpanID,omitempty"`
// PlanStatus:       PlanTraceSpanID      string `json:"planTraceSpanID,omitempty"`
// ProjectStatus:    ProjectTraceSpanID   string `json:"projectTraceSpanID,omitempty"`
// TaskStatus:       TaskSpanEmittedUID   string `json:"taskSpanEmittedUID,omitempty"`
// TaskStatus:       TaskTraceSpanID      string `json:"taskTraceSpanID,omitempty"`
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add span-ID carrier fields to all five CRD Status structs</name>
  <files>api/v1alpha3/milestone_types.go, api/v1alpha3/phase_types.go, api/v1alpha3/plan_types.go, api/v1alpha3/project_types.go, api/v1alpha3/task_types.go</files>
  <read_first>
    - api/v1alpha3/milestone_types.go (MilestoneStatus — the `MilestoneSpanEmittedUID` field at ~line 70-78 is the doc-comment + `+optional` style template)
    - api/v1alpha3/task_types.go (TaskStatus lines ~147-170 — has NEITHER field family today; both are net-new)
    - .planning/phases/43-task-level-parity-trace-context-propagation/43-PATTERNS.md (§"api/v1alpha3/... — the new durable span-ID field" — exact doc-comment text for Task's two fields)
  </read_first>
  <action>
    Add one flat string field to each of the four planner-level Status structs, placed immediately after that struct's existing `{Level}SpanEmittedUID` field, with a `+optional` marker and a doc comment following the sibling field's style. Exact names per the interfaces block: `MilestoneTraceSpanID` (json `milestoneTraceSpanID,omitempty`) in `MilestoneStatus`; `PhaseTraceSpanID` (json `phaseTraceSpanID,omitempty`) in `PhaseStatus`; `PlanTraceSpanID` (json `planTraceSpanID,omitempty`) in `PlanStatus`; `ProjectTraceSpanID` (json `projectTraceSpanID,omitempty`) in `ProjectStatus`, after `PlannerSpanEmittedUID` (~line 529). Doc comment content for each: this level's own synthesized dispatch-span OTel SpanID hex, persisted as the durable parent carrier for child-level spans and Phase 46's dashboard deep-link (Phase 43 PROP-02); never stores the TraceID — always re-derivable from Project.UID via otelai.TraceIDFromUID (D-03/D-04).

    In `TaskStatus` (task_types.go, after `CompletedAt`), add BOTH net-new fields using the exact doc-comment text from 43-PATTERNS.md's task_types.go section: `TaskSpanEmittedUID` (json `taskSpanEmittedUID,omitempty` — the Job-UID idempotency marker mirroring the four planner-level `{Level}SpanEmittedUID` fields, Phase 43 TRACE-01) and `TaskTraceSpanID` (json `taskTraceSpanID,omitempty`).

    Additive only — do not touch any existing field, kubebuilder marker, or CEL validation. Per D-04, do NOT fold span identity into the SpanEmittedUID markers: they answer different questions (identity vs. already-emitted-for-this-Job-UID).
  </action>
  <verify>
    <automated>go build ./api/... && grep -c "TraceSpanID string" api/v1alpha3/milestone_types.go api/v1alpha3/phase_types.go api/v1alpha3/plan_types.go api/v1alpha3/project_types.go api/v1alpha3/task_types.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./api/...` exits 0
    - Each of the five `*_types.go` files contains exactly one `{Level}TraceSpanID string` field with json tag `{level}TraceSpanID,omitempty` and a `+optional` marker
    - `grep -c "TaskSpanEmittedUID" api/v1alpha3/task_types.go` returns >= 1 (field present with json tag `taskSpanEmittedUID,omitempty`)
    - `git diff api/v1alpha3/ | grep -c '^-[^-]'` returns 0 (no existing lines removed — purely additive)
  </acceptance_criteria>
  <done>All six fields exist with correct names, json tags, +optional markers, and sibling-style doc comments; api package builds.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate CRD manifests and deepcopy, verify schema exposure</name>
  <files>config/crd/bases/tideproject.k8s_milestones.yaml, config/crd/bases/tideproject.k8s_phases.yaml, config/crd/bases/tideproject.k8s_plans.yaml, config/crd/bases/tideproject.k8s_projects.yaml, config/crd/bases/tideproject.k8s_tasks.yaml</files>
  <read_first>
    - Makefile (targets `manifests` line 52 and `generate` line 66 — confirmed present; controller-gen driven)
  </read_first>
  <action>
    Run `make manifests generate`. Commit the regenerated `config/crd/bases/*.yaml` (and `api/v1alpha3/zz_generated.deepcopy.go` if controller-gen touches it — plain string fields normally produce no deepcopy diff, but include whatever regen changes). Do not hand-edit any generated file. If the regen produces diffs in unrelated CRD files beyond the new properties, stop and inspect before committing — that would signal toolchain-version drift, not this change.
  </action>
  <verify>
    <automated>make manifests generate && grep -l "milestoneTraceSpanID" config/crd/bases/tideproject.k8s_milestones.yaml && grep -l "taskTraceSpanID" config/crd/bases/tideproject.k8s_tasks.yaml && grep -l "taskSpanEmittedUID" config/crd/bases/tideproject.k8s_tasks.yaml && git diff --exit-code config/crd/bases/ && echo REGEN-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five CRD YAMLs contains its level's `{level}TraceSpanID` property under the status schema
    - `config/crd/bases/tideproject.k8s_tasks.yaml` contains both `taskSpanEmittedUID` and `taskTraceSpanID`
    - Running `make manifests generate` a second time produces zero diff (regen is committed and idempotent — the `git diff --exit-code` above proves it)
    - `go vet ./api/...` exits 0
  </acceptance_criteria>
  <done>CRD manifests regenerated, all six JSON properties present in schemas, regen idempotent, tree builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager → etcd (CRD status) | New status fields are written exclusively by the manager's own reconcilers — no external writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | New `.status` fields | accept | Fields are manager-authored bookkeeping (span-ID hex, Job UIDs); status subresource RBAC is unchanged — no new write surface (RESEARCH.md Security Domain: V4 no new RBAC surface) |
| T-43-02 | Information Disclosure | Span IDs persisted in etcd | accept | Trace/span IDs are not secrets per OTel's standard threat model (RESEARCH.md V6 analysis); already visible in exported spans |
| T-43-SC | Tampering | Supply chain | accept | No new packages this phase — Package Legitimacy Audit N/A per 43-RESEARCH.md |
</threat_model>

<verification>
- `go build ./...` green
- `make manifests generate` idempotent after commit
- All six JSON keys present in `config/crd/bases/` schemas
</verification>

<success_criteria>
PROP-02's storage surface exists on all five CRDs (flat fields per CONTEXT.md discretion), Task's idempotency marker exists (D-04), manifests regenerated — downstream plans can compile parent-read and self-write code against these exact field names.
</success_criteria>

<output>
Create `.planning/phases/43-task-level-parity-trace-context-propagation/43-01-SUMMARY.md` when done. Note explicitly for the verifier: flat `{Level}TraceSpanID` chosen over nested `.status.trace` struct per CONTEXT.md Claude's-Discretion grant.
</output>
