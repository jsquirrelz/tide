---
phase: 05-distribution-self-hosting-acceptance
plan: 17
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14, 15, 16]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, DIST-05, BOOT-02, BOOT-04, AUTH-02]
tags: [closeout, roadmap, state, summary]

must_haves:
  truths:
    - "All 16 Phase 5 plans (01-16) are marked complete in ROADMAP.md (status check boxes)"
    - "ROADMAP.md Phase 5 row in the Progress table is updated to `Complete` with the closeout date"
    - "STATE.md progress fields advance: completed_plans bumps by 17 (16 plans + this closeout), completed_phases bumps from 7 to 8, percent 100→100 (project complete)"
    - "STATE.md Current Position / Status text reflects Phase 5 complete and the M0 → M_self bridge closed"
    - "05-SUMMARY.md exists summarizing the phase: deliverables shipped, REQ-IDs satisfied, deferred items not addressed, residual UAT items if any"
    - "All 8 requirements (DIST-01..DIST-05, BOOT-02, BOOT-04, AUTH-02 catch-up) marked as Phase 5 satisfied in REQUIREMENTS.md (cross-reference)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 row marked Complete + all 16 plan rows ticked"
      contains: "Phase 5.*Complete"
    - path: ".planning/STATE.md"
      provides: "completed_phases 8/8 + completed_plans bumped by 17"
      contains: "completed_phases: 8"
    - path: ".planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md"
      provides: "Phase 5 closeout summary"
      contains: "DIST-01"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md Phase 5 row"
      via: "completed_phases counter + Status: Complete entry"
      pattern: "Phase 5.*Complete"
---

<objective>
Close Phase 5 — the v1.0 ship phase. Update ROADMAP.md (Phase 5 row + 16 plan rows), STATE.md (progress fields + Current Position + Status), and create 05-SUMMARY.md documenting Phase 5 deliverables.

Purpose: Closes the M0 → M_self bridge. After this plan, completed_phases = 8 of 8; project state reflects v1.0 ship readiness. Maintainer can now tag `v1.0.0-rc.1` to fire dry-run.yaml; on rc success, tag `v1.0.0` to fire release.yaml's full chain. Optional: maintainer runs `make acceptance-v1` for the BOOT-04 ritual.

Output: ROADMAP + STATE updated; SUMMARY committed; Phase 5 + project marked complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-15-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update ROADMAP.md (Phase 5 row + 16 plan rows + Progress table)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — Phase 5 section starts at line 247; Progress table at line 267; Plans: TBD placeholder at line 260)
    - .planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-15-SUMMARY.md (analog for closeout SUMMARY shape per CONTEXT.md "recent closeout SUMMARY" reference)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md (decisions + REQ-IDs to cross-reference in SUMMARY)
    - .planning/REQUIREMENTS.md (verify the 7 REQ-IDs + AUTH-02 catch-up traceability entries)
    - .planning/phases/05-distribution-self-hosting-acceptance/{05-01..05-16}-SUMMARY.md (per-plan summaries — verify all 16 exist before declaring closeout; this Task 1 reads each and aggregates the count)
  </read_first>
  <behavior>
    - ROADMAP.md Phase 5 section's `**Plans**:` placeholder line is replaced with `**Plans:** 16/16 plans executed` (matching the Phase 4 / 04.1 closeout SUMMARY format)
    - ROADMAP.md Phase 5 section adds a `Plans:` list with 16 bulleted lines `- [x] 05-NN-PLAN.md — <brief>` for plans 01-16 (matching the Phase 4 / 04.1 listing format)
    - ROADMAP.md `## Progress` table row for Phase 5 is updated: `5. Distribution & Self-Hosting Acceptance | 16/16 | Complete | 2026-MM-DD` (executor uses the actual closeout date)
    - All other ROADMAP rows are preserved
  </behavior>
  <action>
    Step 1: Read `.planning/ROADMAP.md`. Locate Phase 5 section (starts at `### Phase 5: Distribution & Self-Hosting Acceptance` per current state). Locate the `**Plans**: TBD` placeholder (line 260).

    Step 2: Replace `**Plans**: TBD` with `**Plans:** 16/16 plans executed` matching the Phase 4 / 04.1 format.

    Step 3: After the Plans count line, append a Plans bulleted list. Use the format from Phase 4 (line 169 onwards) — `Plans:` then 16 bullets. For each plan 01-16, the bullet format is `- [x] 05-NN-PLAN.md — <one-line brief>` where the brief is sourced from the per-plan `<objective>` first sentence:
      - 01: LICENSE + NOTICE + verify-license.sh (DIST-03)
      - 02: CONTRIBUTING.md + SECURITY.md (DIST-04)
      - 03: README Quickstart prepend (DIST-04 + D-C1)
      - 04: docs/README.md index + concepts.md + verify-docs-coverage.sh (DIST-04 + D-C3)
      - 05: Chart.yaml lockstep version bump 0.1.0-dev → 1.0.0 (DIST-01 + D-X3)
      - 06: examples/tide-demo-fixture/ MIT-licensed scaffold (DIST-04 + D-B3)
      - 07: docs/INSTALL.md (DIST-04 + D-C2 + Pitfall 4 + Pitfall 8)
      - 08: docs/project-authoring.md (DIST-04 + Variant B prompt guidance)
      - 09: docs/rbac.md (DIST-04 + AUTH-02 catch-up doc + D-X7)
      - 10: docs/troubleshooting.md (DIST-04 + D-C4 ≥12-row table)
      - 11: examples/projects/{small,large}/ samples (DIST-04 + BOOT-04 acceptance project.yaml)
      - 12: cmd/tide-demo-init/ binary + medium/ sample (DIST-04 + D-B3 + Topic 4)
      - 13: per-namespace-rolebinding.yaml + projectNamespaces values key (DIST-01 + AUTH-02 catch-up template)
      - 14: CRD-subchart resource-policy: keep annotation (DIST-01 + Pitfall 2)
      - 15: Makefile dry-run-v1 + acceptance-v1 + 4 hack/scripts (DIST-05 + BOOT-02 + BOOT-04)
      - 16: release.yaml extensions + dry-run.yaml (DIST-01 + DIST-02 + DIST-05)

    Step 4: Update the `## Progress` table row for Phase 5. Current state (line 275): `| 5. Distribution & Self-Hosting Acceptance | 0/TBD | Not started | - |`. Replace with `| 5. Distribution & Self-Hosting Acceptance | 16/16 | Complete | $(date +%Y-%m-%d) |` (executor uses the actual current date, e.g., `2026-05-22`).

    Step 5: Add a closing line to the Progress table footer (after the table) summarizing project completion: `All 8 phases complete — TIDE v1.0 ship-ready.` (placement: right after the Progress table closing row).

    NO other ROADMAP changes. Preserve all Phase 1-4, 02.1, 02.2, 04.1 content verbatim.
  </action>
  <verify>
    <automated>grep -q "5. Distribution & Self-Hosting Acceptance | 16/16 | Complete" .planning/ROADMAP.md && grep -q "16/16 plans executed" .planning/ROADMAP.md && grep -c "05-.*PLAN.md" .planning/ROADMAP.md | awk '{ if ($1 >= 16) print "OK"; else print "FAIL: " $1 }' | grep -q OK && [ "$(grep -cE '^- \[x\] 05-' .planning/ROADMAP.md)" -ge 16 ] && grep -q "All 8 phases complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "5. Distribution & Self-Hosting Acceptance | 16/16 | Complete" .planning/ROADMAP.md` returns 0
    - `grep -q "16/16 plans executed" .planning/ROADMAP.md` returns 0
    - `grep -cE '^- \\[x\\] 05-' .planning/ROADMAP.md` returns ≥ 16 (16 ticked plan rows)
    - `grep -q "All 8 phases complete" .planning/ROADMAP.md` returns 0
    - `grep -q "Phase 5.*Not started" .planning/ROADMAP.md` returns 1 (NOT 0 — "Not started" status MUST NOT remain for Phase 5)
    - `grep -q "Phase 5.*TBD" .planning/ROADMAP.md` returns 1 (NOT 0 — "TBD" MUST NOT remain for Phase 5)
    - Preserved: `grep -q "Phase 1: Foundation" .planning/ROADMAP.md` returns 0 (existing Phase 1 content intact)
    - Preserved: `grep -q "Phase 04.1.*Complete.*2026-05-22" .planning/ROADMAP.md` returns 0 (Phase 04.1 entry unchanged)
  </acceptance_criteria>
  <done>ROADMAP Phase 5 row + 16 plan rows + Progress table updated; all 8 phases marked Complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update STATE.md (frontmatter progress + Current Position + Last activity)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — frontmatter lines 1-15 carry progress counters: total_phases 8, completed_phases 7, total_plans 83, completed_plans 83, percent 100; Body lines 27-31 carry Current Position text)
    - .planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-15-SUMMARY.md (analog for closeout STATE update shape)
    - gsd-sdk query state.load (returns current STATE; verify counters match before bumping)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md (vision: v1 = self-hosting MVP — TIDE drives its own next milestone — this is the closeout moment)
  </read_first>
  <behavior>
    - STATE.md frontmatter `completed_phases` bumps from 7 to 8
    - STATE.md frontmatter `total_plans` bumps from 83 to 100 (83 existing + 17 Phase 5: 16 plans + 1 closeout)
    - STATE.md frontmatter `completed_plans` bumps from 83 to 100
    - STATE.md frontmatter `percent` stays at 100 (project was at 100% Phase 04.1; now stays at 100% with Phase 5 closed since all phases are complete)
    - STATE.md frontmatter `status`, `last_updated`, `last_activity`, `stopped_at` reflect Phase 5 closeout
    - STATE.md body "Current Position" / "Status" prose updated: Phase 5 COMPLETE; M0 → M_self bridge closed; v1.0 ship-ready
    - STATE.md "Session Continuity" updated: stopped_at = Phase 5 closeout; resume file = `.planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md`
  </behavior>
  <action>
    Step 1: Read `.planning/STATE.md`. Frontmatter (lines 1-15):
    - Set `completed_phases: 8`
    - Set `total_plans: 100` (83 prior + 17 Phase 5)
    - Set `completed_plans: 100`
    - Keep `percent: 100`
    - Update `status:` to a new prose paragraph describing Phase 5 close: "Phase 5 closed 2026-MM-DD. All 16 plans executed across 6 waves (1 OSS-readiness root files + 4 docs + 1 fixture + 1 chart version bump + 2 chart-template additions + 2 sample sets + 1 demo-init binary + 1 hack/scripts plumbing + 1 release-pipeline + 1 closeout). LICENSE + NOTICE landed; 4 new docs + concepts.md indexed; 3-sample cost spectrum (small/medium/large) ready; chart version 1.0.0 lockstep; per-namespace-rolebinding template + resource-policy:keep annotation in place; make dry-run-v1 + make acceptance-v1 + release.yaml chart-publish all wired. M0 → M_self bridge closed. Next step: maintainer tags v1.0.0-rc.1 to fire dry-run.yaml, then v1.0.0 for full release."
    - Update `last_updated:` to `"$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)"` (ISO-8601)
    - Update `last_activity:` to `"$(date +%Y-%m-%d) -- Plan 05-17 (Phase 5 closeout): ROADMAP.md Phase 5 row → Complete; STATE.md completed_phases 7 → 8, completed_plans 83 → 100, percent stays 100."`
    - Update `stopped_at:` to `Phase 5 closed`

    Step 2: Update STATE.md body `## Current Position` section (lines 27-31):
    - Line "Phase: ..." → `Phase: 5 (distribution-self-hosting-acceptance) — COMPLETE`
    - Line "Plan: ..." → `Plan: 17 of 17 — Phase 5 closeout commit landed; ROADMAP Progress row = Complete; v1.0 ship-ready.`
    - Line "Status: ..." → mirror the frontmatter status prose
    - Line "Last activity: ..." → mirror the frontmatter last_activity prose
    - Line "Progress: [██████████] 100% ..." → keep at 100% scope; update the scope label to `(M0 → M_self complete; v1.0 ready)` from the current `(Phase 02.2 scope)` label

    Step 3: Update `## Session Continuity` section (lines 156-159):
    - `Last session: $(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)`
    - `Stopped at: Phase 5 closed`
    - `Resume file: .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md`

    Step 4: NO other STATE.md changes. Preserve `## Performance Metrics`, `## Accumulated Context`, `## Quick Tasks Completed` blocks verbatim.
  </action>
  <verify>
    <automated>grep -q "^completed_phases: 8" .planning/STATE.md && grep -q "^completed_plans: 100" .planning/STATE.md && grep -q "^total_plans: 100" .planning/STATE.md && grep -q "^percent: 100" .planning/STATE.md && grep -q "Phase 5 closed" .planning/STATE.md && grep -q "Phase: 5.*COMPLETE" .planning/STATE.md && grep -q "v1.0 ship-ready\|v1.0 ready" .planning/STATE.md && grep -q "Resume file:.*05-SUMMARY.md" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^completed_phases: 8" .planning/STATE.md` returns 1
    - `grep -c "^completed_plans: 100" .planning/STATE.md` returns 1
    - `grep -c "^total_plans: 100" .planning/STATE.md` returns 1
    - `grep -c "^percent: 100" .planning/STATE.md` returns 1
    - `grep -q "Phase 5 closed" .planning/STATE.md` returns 0
    - `grep -q "Phase: 5.*COMPLETE\\|Phase 5.*COMPLETE" .planning/STATE.md` returns 0
    - `grep -q "Plan: 17 of 17" .planning/STATE.md` returns 0
    - `grep -q "Resume file:.*05-SUMMARY.md" .planning/STATE.md` returns 0
    - Preserved: `grep -q "Performance Metrics" .planning/STATE.md` returns 0
    - Preserved: `grep -q "Quick Tasks Completed" .planning/STATE.md` returns 0
  </acceptance_criteria>
  <done>STATE.md frontmatter + body + session continuity updated for Phase 5 closeout; counts bump to 8/100/100; existing sections preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 05-SUMMARY.md Phase 5 closeout summary + commit</name>
  <files>.planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-15-SUMMARY.md (analog closeout SUMMARY shape per CONTEXT.md reference)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md (decision list + REQ-IDs)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md (Pitfalls addressed + assumptions to call out as residual)
    - .planning/phases/05-distribution-self-hosting-acceptance/{05-01..05-16}-SUMMARY.md (per-plan summaries — aggregate file lists, key decisions, commit SHAs)
  </read_first>
  <behavior>
    - File `.planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` exists
    - Sections: Goal recap; Plans summary (16 rows); Requirements satisfied (8 IDs); Pitfalls mitigated (count); Deferred items (CODE_OF_CONDUCT, GOVERNANCE, mkdocs, etc.); Residual UAT items (if any); Next steps (maintainer tag rc.1 → release)
    - Carries a closing line: `Phase 5 closed YYYY-MM-DD — M0 → M_self bridge complete — v1.0 ship-ready.`
  </behavior>
  <action>
    Create `.planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` modeling the structure of `.planning/phases/04.1-pre-v1-audit-fixes-cross-phase-uat-closeout/04.1-15-SUMMARY.md`.

    Frontmatter:
    ```yaml
    ---
    phase: 05-distribution-self-hosting-acceptance
    plan: 17
    status: closeout
    closed: <YYYY-MM-DD>
    ---
    ```

    Sections:

    `# Phase 5 Closeout — Distribution & Self-Hosting Acceptance`

    `## Goal` — one paragraph paraphrasing ROADMAP Phase 5 goal: Helm chart packaging + Apache-2.0 + docs + external-operator dry-run + v1 acceptance test. Result: M_self consumes M0 artifacts under same v1alpha1 schema with no breaking changes.

    `## Plans (16/16)` — Markdown table with columns Plan | Goal | Status | Commit; 16 rows for 05-01 through 05-16; each row lists the plan number, the one-line objective (lifted from plan's <objective> first sentence), status `Complete`, and commit SHA (executor reads each per-plan SUMMARY for the commit SHA).

    `## Requirements satisfied (8/8)` — bulleted list:
    - DIST-01: Helm chart pair + per-namespace-rolebinding + resource-policy:keep ✓ (Plans 05, 13, 14, 16)
    - DIST-02: helmify verify-only in release.yaml ✓ (Plan 16)
    - DIST-03: Apache-2.0 LICENSE + NOTICE + Go-file headers ✓ (Plan 01)
    - DIST-04: 5 new docs + concepts.md + 3-sample cost spectrum + CONTRIBUTING + SECURITY + Quickstart ✓ (Plans 02, 03, 04, 06, 07, 08, 09, 10, 11, 12)
    - DIST-05: make dry-run-v1 + dry-run.yaml + dry-run-report.json schema ✓ (Plans 15, 16)
    - BOOT-02: make acceptance-v1 with fresh kind + helm install ✓ (Plan 15)
    - BOOT-04: examples/projects/large/project.yaml Variant B + 7-check D-A3 verifier ✓ (Plans 11, 15)
    - AUTH-02 catch-up: per-namespace-rolebinding.yaml template + docs/rbac.md ✓ (Plans 09, 13)

    `## Decisions honored` — table of D-* decisions (D-A1..A4, D-B1..B4, D-C1..C5, D-D1..D4, D-X1..X7 = 21 decisions) with one-line "implemented by Plan NN" trace.

    `## Pitfalls mitigated` — list per RESEARCH §"Common Pitfalls":
    - Pitfall 1 (helmify regen) — augment-script preservation in Plans 13 + 14
    - Pitfall 2 (CRD subchart upgrade drops CRDs) — resource-policy:keep annotation Plan 14
    - Pitfall 3 (helm push permissions) — packages: write in Plan 16
    - Pitfall 4 (CRDs not registered first) — docs/INSTALL.md install order Plan 07
    - Pitfall 5 (dry-run 30-min margin) — DRY_RUN_TIMEOUT_SECONDS env var Plan 15 (mitigation deferred unless first run > 25 min)
    - Pitfall 6 (DinD permissions) — canonical /var/run/docker.sock mount Plan 15
    - Pitfall 7 (rolling-window reset mid-run) — Plan 11 explicit rollingWindowDurationSeconds in large sample
    - Pitfall 8 (OSS-adoption-death-by-missing-docs) — README Quickstart prepend Plan 03 + concepts.md Plan 04 + "Is TIDE right for me?" Plan 07
    - Pitfall 9 (sample namespace collision) — tide-sample-* prefix Plans 11, 12
    - Pitfall 10 (large sample outcome vague) — Variant B verbatim Plan 11
    - Pitfall 11 (goreleaser extra_files glob) — actions/upload-artifact + download-artifact Plan 16
    - Pitfall 12 (tide-demo-init path unreachable) — in-cluster Job + PVC pattern Plan 12

    `## Deferred items` (per CONTEXT.md §"Deferred Ideas"):
    - CODE_OF_CONDUCT.md — post-v1
    - GOVERNANCE.md — post-v1
    - Mkdocs site at tide.dev — post-v1
    - External public fixture repo — explicitly rejected
    - Friend-of-project cold-read pass — informal sanity check before v1.0 tag (not gating)
    - Nightly cron dry-run — rejected (tag-gate only)
    - Cosign / SLSA provenance signing — v1.x scope
    - OperatorHub / OLM bundle — v1.x
    - index.yaml chart distribution — OCI is v1 primary (researcher recommends deferring)

    `## Residual UAT items`
    - **BOOT-04 acceptance ritual** — `make acceptance-v1` is the maintainer ritual; executor of Plan 15 does NOT run it end-to-end (cost $25). Maintainer runs this manually before tagging v1.0.0 release.
    - **Dashboard screenshot** — Chrome DevTools MCP step in acceptance-v1.sh is documented but operator-only; maintainer captures + attaches to release notes.
    - **First dry-run baseline** — Phase 5's first `make dry-run-v1` is the timing baseline. If `totalSeconds > 1620` (27 min, leaving < 3 min margin), Plan 16's Pitfall 5 caching optimization (kind images cache) should be added as a quick task.

    `## Next steps`
    1. Maintainer tags `v1.0.0-rc.1`. dry-run.yaml fires + produces dry-run-report.json artifact.
    2. Maintainer reviews dry-run-report.json (totalSeconds < 1800).
    3. Maintainer runs `make acceptance-v1` locally with ANTHROPIC_API_KEY + GH_PAT envs. Inspects evidence under .acceptance-runs/<ts>/. Reviews per-run branch.
    4. If all green: maintainer tags `v1.0.0`. release.yaml fires: helmify-verify + pre-flight (verifies recent rc success) + release (goreleaser) + chart-publish (helm push to ghcr.io).
    5. v1.0.0 release page on GitHub carries: binaries (goreleaser), helm charts (OCI at ghcr.io/jsquirrelz/tide-charts), dry-run-report.json artifact (from rc workflow).

    Closing line: `Phase 5 closed <YYYY-MM-DD> — M0 → M_self bridge complete — v1.0 ship-ready.`
  </action>
  <verify>
    <automated>test -s .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "Phase 5 Closeout" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "16/16" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "DIST-01" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "DIST-05" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "BOOT-04" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "AUTH-02" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "M0.*M_self" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md && grep -q "v1.0 ship-ready" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -s .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `head -10 .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` contains `phase: 05-distribution-self-hosting-acceptance` and `status: closeout`
    - `grep -q "# Phase 5 Closeout" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `grep -q "16/16" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - All 8 requirement IDs present: `for r in DIST-01 DIST-02 DIST-03 DIST-04 DIST-05 BOOT-02 BOOT-04 AUTH-02; do grep -q "$r" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md || exit 1; done` exits 0
    - `grep -q "Pitfalls mitigated" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `grep -q "Deferred items" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `grep -q "Residual UAT items" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `grep -q "Next steps" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `grep -q "M0.*M_self" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `grep -q "v1.0 ship-ready" .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns 0
    - `wc -l .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` returns between 80 and 250 lines
  </acceptance_criteria>
  <done>05-SUMMARY.md exists; Phase 5 documented end-to-end; all 8 REQ-IDs traced; deferred + residual items called out; next-steps maintainer playbook present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Closeout commit → project state | After this plan, ROADMAP + STATE assert v1.0 ship-ready; if state is wrong, downstream tooling (gsd-sdk queries, future phase planners) builds on bad foundation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17-01 | Tampering | STATE.md progress counters | mitigate | Acceptance criteria explicitly grep for the exact counts (completed_phases 8, completed_plans 100, total_plans 100); off-by-one drift caught at verify time |
| T-05-17-02 | Tampering | ROADMAP Phase 5 row | mitigate | Acceptance criteria grep for "Phase 5 ... Complete" + assert "Not started" and "TBD" are NOT present (returns 1 = no match) |
| T-05-17-03 | Information Disclosure | 05-SUMMARY.md ANTHROPIC_API_KEY reference | accept | Summary references `make acceptance-v1` env requirement but does NOT inline key material; standard pattern for closeout docs |
</threat_model>

<verification>
After all 3 tasks: `grep -q "5. Distribution & Self-Hosting Acceptance | 16/16 | Complete" .planning/ROADMAP.md && grep -q "completed_phases: 8" .planning/STATE.md && test -s .planning/phases/05-distribution-self-hosting-acceptance/05-SUMMARY.md` exits 0.

Then `gsd-sdk query state.load 2>/dev/null | jq '.progress.completed_phases'` returns 8 (if SDK is invoked post-edit; if not invocable, the grep above suffices).
</verification>

<success_criteria>
- ROADMAP.md Phase 5 row marked Complete + 16 plan rows ticked
- STATE.md counters bumped (8/100/100) + Current Position reflects closeout
- 05-SUMMARY.md exists with 8/8 REQ-IDs traced + deferred + residual + next steps
- Phase 5 closed; v1.0 ship-ready; M0 → M_self bridge complete
</success_criteria>

<output>
After completion, the closeout summary IS the output. No additional SUMMARY file beyond 05-SUMMARY.md.

Final commit message (executor uses this for the closeout commit landing all 3 files):
`docs(05-17): close Phase 5 — 16/16 plans, M0 → M_self bridge complete, v1.0 ship-ready`
</output>
