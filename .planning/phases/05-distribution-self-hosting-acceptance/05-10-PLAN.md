---
phase: 05-distribution-self-hosting-acceptance
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/troubleshooting.md
autonomous: true
requirements: [DIST-04]
tags: [docs, troubleshooting, table]

must_haves:
  truths:
    - "docs/troubleshooting.md is a single Markdown table Symptom | Cause | Recipe per D-C4"
    - "Table has at least 12 entries covering the canonical failure modes locked in D-C4"
    - "Each row's Recipe column gives an executable command or link to the deeper doc"
    - "Table includes the finalizer-stuck recipe explicitly (ROADMAP mandates this)"
  artifacts:
    - path: "docs/troubleshooting.md"
      provides: "Symptom-driven failure-mode lookup table"
      contains: "Symptom"
  key_links:
    - from: "docs/troubleshooting.md"
      to: "docs/rwx-drivers.md, docs/git-hosts.md, docs/gates.md, docs/observability.md, docs/rbac.md (cross-links from individual rows)"
      via: "Markdown links in Recipe column"
      pattern: "\\]\\("
---

<objective>
Land `docs/troubleshooting.md` as a single Markdown table Symptom | Cause | Recipe with ≥12 entries (D-C4 lock) covering the canonical failure modes operators hit during install + first apply + steady-state.

Purpose: REQ-DIST-04 troubleshooting reference. The table format wins over paragraph blocks for scannability per D-C4. Each row points to either an executable kubectl recipe or a deeper doc. Compulsory entries include the finalizer-stuck recipe (ROADMAP P5 mandate).

Output: docs/troubleshooting.md with the symptom/cause/recipe table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@docs/rwx-drivers.md
@docs/live-e2e.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/troubleshooting.md</name>
  <files>docs/troubleshooting.md</files>
  <read_first>
    - docs/rwx-drivers.md (exact analog per PATTERNS.md §"P2.5" — single-table-as-doc Markdown format; lines 21-27 are the canonical table shape)
    - docs/live-e2e.md lines 108-132 (paragraph-block Symptom/Cause/Fix format — superseded by the table format per D-C4; cited as reference for content extraction)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-C4" (locks: single Markdown table, ~12 entries enumerated; finalizer stuck, ANTHROPIC_API_KEY 401, push lease conflict, gitleaks blocked, PVC RWX missing, dashboard 404, CRDs not registered, admission webhook 422, budget halted, gate awaiting approval, ImagePullBackoff, leader election lost)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Pitfall 2 → docs/troubleshooting.md recipe row" (CRD subchart not upgraded — additional row pre-figured by Research)
    - ROADMAP.md Phase 5 line 254 ("finalizer stuck, `kubectl patch` manual unstick recipe") — mandatory entry
  </read_first>
  <behavior>
    - File exists at `docs/troubleshooting.md`
    - Opens with `# TIDE Troubleshooting` heading + Audience/Status/Scope opener
    - One Markdown table with header row `| Symptom | Cause | Recipe |` and `|---------|-------|--------|`
    - Table has ≥ 12 data rows covering the 12 canonical failure modes from D-C4 (finalizer stuck, ANTHROPIC_API_KEY 401, push lease conflict, gitleaks blocked, PVC RWX missing, dashboard 404, CRDs not registered, admission webhook 422, budget halted, gate awaiting approval, ImagePullBackoff, leader election lost)
    - Each row's Recipe column contains either a kubectl/helm command OR a Markdown link to a deeper doc (rwx-drivers/git-hosts/gates/observability/rbac/etc.)
    - The finalizer-stuck row uses the `kubectl patch ... --type=merge -p '{"metadata":{"finalizers":[]}}'` recipe per ROADMAP Phase 5 mandate
    - The CRDs-not-registered row links to docs/INSTALL.md install-order section (Pitfall 4)
    - Audience line names ops engineers + operators in incident
  </behavior>
  <action>
    Create `docs/troubleshooting.md`. Heading `# TIDE Troubleshooting`.

    Audience/Status/Scope opener:
    - Audience: "Operators and ops engineers diagnosing TIDE failures during install or steady-state."
    - Status: "v1.0; ~12 canonical failure modes covered in a Symptom/Cause/Recipe table per D-C4."
    - Scope of this doc: "Table-driven lookup. Each Recipe column gives either a kubectl/helm command or a link to the deeper doc that owns the deeper recipe."

    Then a single Markdown table with header `| Symptom | Cause | Recipe |` and ≥ 12 data rows. The rows must include all 12 D-C4-canonical entries; the planner orders them by frequency (most common first). Concrete row content (the executor writes these as Markdown table cells; backticks for inline code; pipe `|` characters escaped as `\|` inside cells):

    1. **finalizer stuck** | Project hangs in Deleting phase | `kubectl patch project <name> -n <ns> --type=merge -p '{"metadata":{"finalizers":[]}}'` (ROADMAP P5 mandate)
    2. **Controller log: 401 unauthorized** | Invalid ANTHROPIC_API_KEY in Secret | Recreate Secret with valid key; `kubectl rollout restart deploy/tide-controller-manager -n tide-system`
    3. **Project.Status.git.condition=PushLeaseFailed** | Concurrent push race per Phase 3 D-B6 | `kubectl annotate project <name> tideproject.k8s/bypass-push-lease=true` — see [gates.md](gates.md)
    4. **tide_secret_leak_blocked_total counter increments** | gitleaks blocked a secret pattern in subagent output | Inspect run-branch diff; fix offending output; see [observability.md](observability.md)
    5. **Pod can't bind PVC: WaitForFirstConsumer** | PVC ReadWriteMany missing for cluster | See [rwx-drivers.md](rwx-drivers.md) — supported drivers + driver-install steps
    6. **Dashboard 404 at https://tide-dashboard.\<cluster\>/** | port-forward needed or ingress not configured | `kubectl port-forward svc/tide-dashboard 8080:80 -n tide-system` — see [dashboard.md](dashboard.md)
    7. **`kubectl get project` returns "no matches for kind"** | tide-crds chart not installed / CRDs not registered first | `helm install tide-crds oci://ghcr.io/jsquirrelz/tide-charts/tide-crds --version 1.0.0 -n tide-system --create-namespace` — see [INSTALL.md](INSTALL.md#install-order-pitfall-4--crds-first) (Pitfall 4)
    8. **Admission webhook 422 on Plan apply** | Cycle detected in task DAG OR file-touch mismatch (Phase 2 D-E1..E4) | Inspect rejection reason in `kubectl apply` output; fix DAG or `files_touched` sets
    9. **Project.Status.conditions show BudgetExceeded** | Hard budget cap reached | `kubectl annotate project <name> tideproject.k8s/bypass-budget=true` (only if scoped recovery; production runs should refuse bypass per D-A2)
    10. **Project paused at gate** | Awaiting human approval per Project.Spec.gates | `tide approve <project>` or `kubectl annotate project <name> tideproject.k8s/gate-approved=<level>` — see [gates.md](gates.md)
    11. **Pod ImagePullBackoff for tide-controller-manager** | Image pull secret missing or image tag drift | `kubectl describe pod -n tide-system` shows reason; create pull-secret or verify chart `appVersion` matches available image tags
    12. **Controller leader election lost / restart loop** | Leader election lease conflict (e.g., zombie leader from chaos-resume scenario) | Wait for lease window (~15s); if persistent, inspect `kubectl get leases -n tide-system` and delete stale lease holder — see [live-e2e.md](live-e2e.md) chaos-resume section
    13. **`kubectl get project` works but child CRDs missing after upgrade** | tide-crds subchart not upgraded in lockstep | `helm upgrade tide-crds oci://ghcr.io/jsquirrelz/tide-charts/tide-crds --version 1.0.0` (Pitfall 2 from Research)

    Row 13 is added per RESEARCH §"Pitfall 2" recommendation, bringing the table to 13 entries (≥ 12 satisfies D-C4).

    After the table, add a short level-2 `## See also` section with links to docs/INSTALL.md + docs/rbac.md + docs/observability.md + docs/dashboard.md + docs/gates.md.

    Voice: terse. Cells are 1-2 sentences. Each row is a self-contained recipe.

    Length target: 50-150 lines (table + opener + see also).
  </action>
  <verify>
    <automated>test -s docs/troubleshooting.md && grep -q "# TIDE Troubleshooting" docs/troubleshooting.md && grep -q "| Symptom | Cause | Recipe |" docs/troubleshooting.md && grep -q "finalizer" docs/troubleshooting.md && grep -q "ANTHROPIC_API_KEY" docs/troubleshooting.md && grep -q "kubectl patch" docs/troubleshooting.md && grep -q "ImagePullBackoff" docs/troubleshooting.md && grep -q "BudgetExceeded" docs/troubleshooting.md && grep -q "PushLeaseFailed" docs/troubleshooting.md && [ "$(grep -cE '^\|.*\|.*\|.*\|$' docs/troubleshooting.md)" -ge 13 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/troubleshooting.md` returns 0
    - `head -1 docs/troubleshooting.md` matches `# TIDE Troubleshooting`
    - Table header present: `grep -q "| Symptom | Cause | Recipe |" docs/troubleshooting.md` returns 0
    - Table separator present: `grep -qE "^\|---" docs/troubleshooting.md` returns 0
    - Row count: `grep -cE '^\|.*\|.*\|.*\|$' docs/troubleshooting.md` returns ≥ 14 (1 header + 1 separator + 13 data rows ≥ 12 D-C4 entries)
    - `grep -q "finalizer" docs/troubleshooting.md` returns 0
    - `grep -q "kubectl patch.*finalizers" docs/troubleshooting.md` returns 0 (ROADMAP mandatory entry)
    - `grep -q "ANTHROPIC_API_KEY" docs/troubleshooting.md` returns 0
    - `grep -q "401 unauthorized" docs/troubleshooting.md` returns 0
    - `grep -q "PushLeaseFailed" docs/troubleshooting.md` returns 0
    - `grep -q "tide_secret_leak_blocked_total" docs/troubleshooting.md` returns 0
    - `grep -q "ReadWriteMany" docs/troubleshooting.md` returns 0
    - `grep -q "dashboard 404\|Dashboard 404" docs/troubleshooting.md` returns 0
    - `grep -q "no matches for kind" docs/troubleshooting.md` returns 0
    - `grep -q "BudgetExceeded" docs/troubleshooting.md` returns 0
    - `grep -q "ImagePullBackoff" docs/troubleshooting.md` returns 0
    - `grep -q "leader election" docs/troubleshooting.md` returns 0
    - `wc -l docs/troubleshooting.md` outputs between 30 and 200 lines
  </acceptance_criteria>
  <done>docs/troubleshooting.md exists with 13-row Symptom/Cause/Recipe table covering all D-C4-mandated entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator in incident → docs/troubleshooting.md | Inaccurate recipe causes a wrong fix; misdocumented Symptom string causes operator to skip the right row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Tampering | Recipe accuracy (kubectl/helm commands) | mitigate | Recipes lifted from existing repo patterns (Phase 1 RBAC verify, Phase 2 budget bypass annotation, Phase 3 push-lease bypass, Phase 4 dashboard port-forward); cross-link to deeper docs ensures source-of-truth lives in the per-domain doc, not in the table |
| T-05-10-02 | Elevation of Privilege | bypass-budget recipe documentation | mitigate | Row 9 explicitly notes "only if scoped recovery; production runs should refuse bypass per D-A2" — discourages reflexive use; the recipe is documented for completeness but framed with the caveat |
| T-05-10-03 | Information Disclosure | Secret-related recipes | mitigate | Row 2 (401 unauthorized) instructs Secret recreation via kubectl; never instructs editing existing Secret in plaintext YAML; doesn't expose key material in the recipe text |
</threat_model>

<verification>
After Task 1: visual scan of table renders 13 rows; `grep -cE '^\|.*\|.*\|.*\|$' docs/troubleshooting.md` returns ≥ 14.
</verification>

<success_criteria>
- docs/troubleshooting.md exists with ≥ 13-row Markdown table
- All 12 D-C4-canonical entries present
- Finalizer-stuck recipe explicit (ROADMAP mandate)
- Cross-links to dashboard/gates/git-hosts/observability/rbac/rwx-drivers docs
- DIST-04 troubleshooting piece satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-10-SUMMARY.md` with table row count and commit SHA.
</output>
