---
phase: 05-distribution-self-hosting-acceptance
plan: 13
type: execute
wave: 2
depends_on: [05]
files_modified:
  - hack/helm/tide-values.yaml
  - hack/helm/augment-tide-chart.sh
  - charts/tide/values.yaml
  - charts/tide/templates/per-namespace-rolebinding.yaml
  - hack/scripts/test-per-ns-rb.sh
  - Makefile
autonomous: true
requirements: [DIST-01, AUTH-02]
tags: [helm-template, per-namespace, rolebinding, auth-02-catchup]

must_haves:
  truths:
    - "hack/helm/tide-values.yaml gains additive `projectNamespaces: []` key (D-X4) — empty default per opt-in design"
    - "hack/helm/augment-tide-chart.sh propagates the new value key into charts/tide/values.yaml (chart-is-fixed-contract rule honored)"
    - "charts/tide/templates/per-namespace-rolebinding.yaml renders zero RoleBindings when projectNamespaces is empty (default-safe)"
    - "Template renders one RoleBinding per listed namespace when projectNamespaces has entries; subjects.namespace = tide-system (operator namespace), roleRef = aggregated tide-orchestrator-namespace ClusterRole (or bound to per-Kind roles)"
    - "hack/scripts/test-per-ns-rb.sh asserts the template renders correctly via helm template + grep"
    - "AUTH-02 catch-up requirement satisfied (per REQUIREMENTS.md traceability — Phase 1 deferred to Phase 5 Helm chart)"
  artifacts:
    - path: "charts/tide/templates/per-namespace-rolebinding.yaml"
      provides: "Per-namespace RoleBinding Helm template (AUTH-02 catch-up)"
      contains: "range $ns := .Values.projectNamespaces"
    - path: "hack/helm/tide-values.yaml"
      provides: "Source-of-truth Helm values with new projectNamespaces key"
      contains: "projectNamespaces:"
    - path: "hack/scripts/test-per-ns-rb.sh"
      provides: "Per-ns RoleBinding template render gate"
  key_links:
    - from: "charts/tide/templates/per-namespace-rolebinding.yaml"
      to: "charts/tide/templates/serviceaccount.yaml (orchestrator SA name)"
      via: "subjects[].name references the same SA name"
      pattern: "tide.fullname.*controller-manager"
---

<objective>
Land the per-namespace RoleBinding Helm template (charts/tide/templates/per-namespace-rolebinding.yaml) driven by a new `projectNamespaces` Helm value (empty default per opt-in design D-X4); add the value key via hack/helm/tide-values.yaml (SOT) + propagate via augment-tide-chart.sh; verify via hack/scripts/test-per-ns-rb.sh.

Purpose: REQ-DIST-01 + AUTH-02 catch-up. Phase 1 deferred the per-namespace RoleBinding YAML template to Phase 5 per REQUIREMENTS.md traceability. This plan delivers the template + values key + render gate.

Output: New per-namespace-rolebinding.yaml chart template; projectNamespaces value key in SOT + generated values.yaml; render-assertion script; Makefile target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@charts/tide/templates/manager-rbac.yaml
@charts/tide/templates/serviceaccount.yaml
@charts/tide/templates/_helpers.tpl
@hack/helm/tide-values.yaml
@hack/helm/augment-tide-chart.sh
@charts/tide/values.yaml
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add projectNamespaces value key to SOT + augment script + generated values.yaml</name>
  <files>hack/helm/tide-values.yaml, hack/helm/augment-tide-chart.sh, charts/tide/values.yaml</files>
  <read_first>
    - hack/helm/tide-values.yaml (current SOT for charts/tide/values.yaml — additive examples: dashboard block, images block lines 137-166 per PATTERNS.md §P3.5)
    - hack/helm/augment-tide-chart.sh (the script that copies SOT to charts/tide/values.yaml; existing logic appends additive keys via awk; verify by reading the script structure — see lines 41-65 per PATTERNS.md)
    - charts/tide/values.yaml (existing; do NOT direct-edit per CLAUDE.md chart-is-fixed-contract anti-pattern; this file only gets updated via re-augmenting)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-X4" (locks: empty default, opt-in per multi-Project install)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Code Examples → Per-Namespace RoleBinding Template" lines 602-610 (the values.yaml comment block + projectNamespaces: [] default)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P3.5" (additive pattern; SOT-first)
  </read_first>
  <behavior>
    - hack/helm/tide-values.yaml gains a new top-level key `projectNamespaces: []` with a comment block above it explaining AUTH-02 catch-up + opt-in semantics
    - The comment block matches the canonical wording from RESEARCH §"Code Examples → Per-Namespace RoleBinding Template" lines 603-609
    - hack/helm/augment-tide-chart.sh is updated IF the existing augment logic doesn't already propagate the SOT verbatim to charts/tide/values.yaml — if it does (simple `cp` or sed-based merge), no augment-script change is needed. Executor reads the augment script first to determine.
    - After running `bash hack/helm/augment-tide-chart.sh`, `charts/tide/values.yaml` contains the same projectNamespaces key + comment block
    - The augment script remains idempotent: running it twice produces the same charts/tide/values.yaml
    - `helm lint charts/tide` passes
  </behavior>
  <action>
    Step 1: Open `hack/helm/tide-values.yaml`. Append at the END of the file (last lines, after the existing config) the following block (verbatim from RESEARCH §"Code Examples → Per-Namespace RoleBinding Template" lines 603-610):

    ```yaml
    # Phase 5 D-X4 — per-namespace RoleBinding template (AUTH-02 catch-up).
    # Empty default: no per-namespace RoleBindings shipped unless operator opts in.
    # For multi-Project installs, list the Project namespaces here:
    #   projectNamespaces:
    #     - tide-customer-acme
    #     - tide-customer-globex
    projectNamespaces: []
    ```

    Step 2: Read `hack/helm/augment-tide-chart.sh` carefully (entire file). Determine: does the existing augment logic propagate ARBITRARY additive keys from SOT to charts/tide/values.yaml? Most likely the augment script does an awk-based merge OR a wholesale copy from a SOT block. If the existing logic copies the entire SOT file verbatim to charts/tide/values.yaml, no augment-script change is needed.

    If the augment script applies per-section logic (e.g., only specific top-level keys are merged), executor must extend the script to handle the new `projectNamespaces` key. Pattern: add a new sed/awk insertion block at the bottom of the augment script that appends the projectNamespaces block to charts/tide/values.yaml after `make helm` regenerates it from helmify.

    Idempotency rule: running `bash hack/helm/augment-tide-chart.sh` twice produces identical output. The script must be safe to re-run. Use `grep -q "^projectNamespaces:" "${CHART_VALUES}" || cat <<...>> "${CHART_VALUES}"` pattern if extending.

    Step 3: Run `bash hack/helm/augment-tide-chart.sh`. Verify charts/tide/values.yaml now contains `projectNamespaces: []` and the comment block. Run again to confirm idempotency.

    Step 4: Run `helm lint charts/tide` — must exit 0.

    Verify post-change: `grep -q "^projectNamespaces:" hack/helm/tide-values.yaml` returns 0; `grep -q "^projectNamespaces:" charts/tide/values.yaml` returns 0.
  </action>
  <verify>
    <automated>grep -q "^projectNamespaces: \[\]" hack/helm/tide-values.yaml && grep -q "Phase 5 D-X4" hack/helm/tide-values.yaml && bash hack/helm/augment-tide-chart.sh && grep -q "^projectNamespaces:" charts/tide/values.yaml && bash hack/helm/augment-tide-chart.sh && helm lint charts/tide</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^projectNamespaces: \\[\\]" hack/helm/tide-values.yaml` returns 1
    - `grep -q "Phase 5 D-X4" hack/helm/tide-values.yaml` returns 0
    - `grep -q "AUTH-02" hack/helm/tide-values.yaml` returns 0
    - `bash hack/helm/augment-tide-chart.sh` exits 0
    - `grep -c "^projectNamespaces:" charts/tide/values.yaml` returns 1 (the SOT value propagated)
    - Idempotent: running `bash hack/helm/augment-tide-chart.sh` again exits 0 and produces no diff in charts/tide/values.yaml
    - `helm lint charts/tide` exits 0
  </acceptance_criteria>
  <done>projectNamespaces value key in SOT + augmented charts/tide/values.yaml; augment script idempotent; helm lint clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create per-namespace-rolebinding.yaml Helm template + test-per-ns-rb.sh + Makefile target</name>
  <files>charts/tide/templates/per-namespace-rolebinding.yaml, hack/scripts/test-per-ns-rb.sh, Makefile</files>
  <read_first>
    - charts/tide/templates/manager-rbac.yaml (RoleBinding shape lines 79-92 per PATTERNS.md §"P3.1"; this is the closest existing template — confirms `apiVersion: rbac.authorization.k8s.io/v1`, `kind: ClusterRoleBinding` shape, roleRef structure)
    - charts/tide/templates/serviceaccount.yaml (the SA name to bind: `name: {{ include "tide.fullname" . }}-controller-manager` — Phase 1 lock)
    - charts/tide/templates/_helpers.tpl (the `tide.labels` and `tide.fullname` helpers used by other templates per PATTERNS.md §"Shared Patterns → Helm template label boilerplate")
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Code Examples → Per-Namespace RoleBinding Template" lines 575-600 (canonical template body) and §"Open Question 6" lines 912-916 (subjects point to operator namespace = tide-system, RoleBinding lives in project namespace — standard K8s pattern)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P3.1" (manager-rbac.yaml + project-admin-rbac.yaml analog; note the ClusterRole aggregation strategy question — planner chooses option: bind to `tide-orchestrator-namespace` aggregate ClusterRole IF it exists, ELSE bind to manager-role per Phase 1 — verify which exists by listing chart templates)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-VALIDATION.md row "DIST-01 — per-namespace-rolebinding.yaml renders with non-empty projectNamespaces" (the gate command)
  </read_first>
  <behavior>
    - `charts/tide/templates/per-namespace-rolebinding.yaml` exists
    - Template uses `{{- range $ns := .Values.projectNamespaces }}` wrapper
    - Renders zero RoleBindings when `projectNamespaces` is empty (default)
    - Renders one RoleBinding per listed namespace when non-empty
    - Each RoleBinding has metadata.name `tide-orchestrator-{{ $ns }}`, metadata.namespace `{{ $ns }}`, standard tide.labels via `{{- include "tide.labels" $ | nindent 4 }}`
    - roleRef.kind `ClusterRole`, roleRef.name binds to the appropriate ClusterRole (manager-role since it's the existing Phase 1 ClusterRole with the per-Kind verb grants; tide-orchestrator-namespace aggregate ClusterRole doesn't exist yet — bind to manager-role for v1.0 per minimal-surface-area principle; document this choice in plan SUMMARY)
    - subjects: kind: ServiceAccount, name: `{{ include "tide.fullname" $ }}-controller-manager`, namespace: `{{ $.Release.Namespace }}` (operator namespace, NOT the project namespace, per RESEARCH §"Open Question 6")
    - `hack/scripts/test-per-ns-rb.sh` exists, executable, asserts that `helm template charts/tide --set 'projectNamespaces={ns1,ns2}'` renders ≥ 2 RoleBindings AND each has subjects[0].namespace == `tide-system` (or whatever Release.Namespace resolves to; the test uses `-n tide-system` or asserts the value matches expected)
    - Makefile gains `.PHONY: test-per-ns-rb` target running the script
    - `helm lint charts/tide` and `helm template charts/tide --set 'projectNamespaces={ns1,ns2}'` both pass
  </behavior>
  <action>
    Step 1: Create `charts/tide/templates/per-namespace-rolebinding.yaml`. Use the canonical template from RESEARCH §"Code Examples → Per-Namespace RoleBinding Template" lines 578-599. Choose `roleRef.name: {{ include "tide.fullname" $ }}-manager-role` (binding to the existing Phase 1 manager-role ClusterRole). Verify the existing manager-role ClusterRole name by reading `charts/tide/templates/manager-rbac.yaml` and matching the same naming convention.

    Final template body:
    ```yaml
    {{- range $ns := .Values.projectNamespaces }}
    ---
    apiVersion: rbac.authorization.k8s.io/v1
    kind: RoleBinding
    metadata:
      name: tide-orchestrator-{{ $ns }}
      namespace: {{ $ns }}
      labels:
        {{- include "tide.labels" $ | nindent 4 }}
        app.kubernetes.io/component: per-namespace-rbac
    roleRef:
      apiGroup: rbac.authorization.k8s.io
      kind: ClusterRole
      name: '{{ include "tide.fullname" $ }}-manager-role'
    subjects:
    - kind: ServiceAccount
      name: '{{ include "tide.fullname" $ }}-controller-manager'
      namespace: {{ $.Release.Namespace }}
    {{- end }}
    ```

    NOTE on roleRef.name: read `charts/tide/templates/manager-rbac.yaml` to confirm the exact ClusterRole name that ships from Phase 1; the helmify+augment output names it something like `tide-manager-role` (with the chart fullname prefix). Use the matching name. If the existing manager-role doesn't cover all per-Kind verbs (because Phase 1 uses per-Kind ClusterRoles like `project-admin-role`, `phase-admin-role`, etc.), executor can EITHER bind one RoleBinding per per-Kind role inside the range OR bind to manager-role (which has the consolidated controller verbs). Choose manager-role for simplicity at v1.0; document the trade in summary (a future v1.x plan can add per-Kind granularity if needed).

    Verify with `helm template charts/tide --set 'projectNamespaces={tide-acme,tide-globex}' -n tide-system` — output must contain 2 `kind: RoleBinding` documents.

    Step 2: Create `hack/scripts/test-per-ns-rb.sh` with shell-script preamble per PATTERNS.md §"Shell-script preamble":
    ```bash
    #!/usr/bin/env bash
    # Phase 5 DIST-01 / AUTH-02 catch-up — per-namespace RoleBinding template render gate.
    set -euo pipefail
    REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
    ```

    Implement two render assertions:

    Assertion 1: Empty default — `helm template charts/tide -n tide-system | grep -c '^kind: RoleBinding'` returns N; without the `--set projectNamespaces=...` flag, the per-namespace RoleBindings produce zero rows; helm template's TOTAL RoleBinding count = number of existing RoleBindings in charts/tide (could be 0 or 1; the test asserts no INCREASE from baseline — but the simpler form is: `helm template charts/tide -n tide-system | grep -c 'name: tide-orchestrator-'` returns 0 when projectNamespaces empty).

    Assertion 2: Non-empty — `helm template charts/tide --set 'projectNamespaces={ns1,ns2}' -n tide-system | grep -c 'name: tide-orchestrator-'` returns 2.

    Assertion 3: subjects.namespace correctness — `helm template charts/tide --set 'projectNamespaces={tide-acme}' -n tide-system | grep -A 3 'tide-orchestrator-tide-acme' | grep -A 5 'subjects:' | grep 'namespace: tide-system'` returns at least one match (the subject SA lives in the operator namespace tide-system).

    On all-pass: `echo "PASS: per-namespace-rolebinding template renders correctly"; exit 0`. On fail: print expected vs actual + exit 1.

    `chmod +x hack/scripts/test-per-ns-rb.sh`.

    Step 3: Add Makefile target after `verify-docs:` (added in Plan 05-04):
    ```
    .PHONY: test-per-ns-rb
    test-per-ns-rb: ## Phase 5 DIST-01 / AUTH-02 — verify per-namespace-rolebinding.yaml renders correctly.
    	@bash hack/scripts/test-per-ns-rb.sh
    ```

    Step 4: Run `helm lint charts/tide` (must exit 0) and `make test-per-ns-rb` (must exit 0).
  </action>
  <verify>
    <automated>test -s charts/tide/templates/per-namespace-rolebinding.yaml && grep -q "range \$ns" charts/tide/templates/per-namespace-rolebinding.yaml && grep -q "tide-orchestrator-" charts/tide/templates/per-namespace-rolebinding.yaml && grep -q "tide.fullname" charts/tide/templates/per-namespace-rolebinding.yaml && helm lint charts/tide && [ "$(helm template charts/tide --set 'projectNamespaces={ns1,ns2}' -n tide-system 2>/dev/null | grep -c 'name: tide-orchestrator-')" -eq 2 ] && test -x hack/scripts/test-per-ns-rb.sh && bash hack/scripts/test-per-ns-rb.sh && grep -qE '^test-per-ns-rb:' Makefile && make test-per-ns-rb</automated>
  </verify>
  <acceptance_criteria>
    - `test -s charts/tide/templates/per-namespace-rolebinding.yaml` returns 0
    - `grep -q "range \\$ns" charts/tide/templates/per-namespace-rolebinding.yaml` returns 0
    - `grep -q "kind: RoleBinding" charts/tide/templates/per-namespace-rolebinding.yaml` returns 0
    - `grep -q "tide-orchestrator-" charts/tide/templates/per-namespace-rolebinding.yaml` returns 0
    - `grep -q "tide.fullname" charts/tide/templates/per-namespace-rolebinding.yaml` returns 0 (Phase 1 helper usage)
    - `grep -q "tide.labels" charts/tide/templates/per-namespace-rolebinding.yaml` returns 0
    - `helm lint charts/tide` exits 0
    - Empty render: `helm template charts/tide -n tide-system 2>/dev/null | grep -c 'tide-orchestrator-'` returns 0 (default empty)
    - Non-empty render: `helm template charts/tide --set 'projectNamespaces={ns1,ns2}' -n tide-system | grep -c 'name: tide-orchestrator-'` returns 2
    - `test -x hack/scripts/test-per-ns-rb.sh` returns 0
    - `bash hack/scripts/test-per-ns-rb.sh` exits 0
    - `grep -cE '^test-per-ns-rb:' Makefile` returns 1
    - `make test-per-ns-rb` exits 0
  </acceptance_criteria>
  <done>Per-namespace RoleBinding template + test script + Makefile target exist; renders 0 by default, N when set; AUTH-02 catch-up satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm install → cluster RBAC surface | The per-namespace RoleBinding template grants the orchestrator SA verbs in operator-listed namespaces; the SCOPE of the binding (which ClusterRole) determines what TIDE can actually do |
| projectNamespaces values key → multi-Project install | Misconfigured projectNamespaces could grant verbs in unintended namespaces; opt-in empty default is the safety lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-01 | Elevation of Privilege | per-namespace RoleBinding scope | mitigate | roleRef binds to existing Phase 1 manager-role ClusterRole (which has per-Kind verbs only — no wildcards per AUTH-03 lock); test-per-ns-rb.sh asserts subjects.namespace == operator namespace; helm lint catches structural drift |
| T-05-13-02 | Tampering | projectNamespaces values default | mitigate | Empty default `[]` ships; opt-in by operator; documented in docs/rbac.md (Plan 05-09) |
| T-05-13-03 | Tampering | Chart-augment-script idempotency | mitigate | Task 1 acceptance criterion explicitly re-runs the augment script and verifies no diff |
| T-05-13-04 | Elevation of Privilege | roleRef.name aggregation choice | accept | manager-role binds the consolidated controller verbs; alternative per-Kind aggregation is a v1.x granularity improvement; documented in plan summary |
</threat_model>

<verification>
After both tasks: `make test-per-ns-rb` exits 0 and `helm lint charts/tide` exits 0. The chart can be re-augmented (`bash hack/helm/augment-tide-chart.sh`) and produces identical output (idempotent).
</verification>

<success_criteria>
- projectNamespaces value key in SOT + generated values.yaml + augment script idempotent
- charts/tide/templates/per-namespace-rolebinding.yaml renders 0 by default, N when --set
- hack/scripts/test-per-ns-rb.sh + Makefile target + helm lint clean
- AUTH-02 catch-up requirement satisfied
- DIST-01 per-namespace template piece satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-13-SUMMARY.md` documenting the augment-script idempotency, helm lint/template results, and commit SHA.
</output>
