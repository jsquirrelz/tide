---
phase: 05-distribution-self-hosting-acceptance
plan: 08
type: execute
wave: 2
depends_on: [06]
files_modified:
  - docs/project-authoring.md
autonomous: true
requirements: [DIST-04]
tags: [docs, project-authoring, samples-walkthrough]

must_haves:
  truths:
    - "docs/project-authoring.md is the Project.Spec field reference + 3-sample walkthrough (D-C3 item 2)"
    - "Walkthrough covers small ($0 stub) → medium (~$5 mini Claude) → large (~$25 acceptance) per D-B1 cost spectrum"
    - "Doc includes outcome-prompt authoring guidance (RESEARCH §Topic 5 — Variant B is the recommended phrasing)"
    - "Doc references the medium sample's local-only git remote bootstrap via cmd/tide-demo-init (D-B3 + Topic 4)"
    - "Doc references examples/projects/{small,medium,large}/project.yaml even though they ship in Plan 05-11 (forward-referenced; coherent at end of Wave 3)"
  artifacts:
    - path: "docs/project-authoring.md"
      provides: "Project.Spec reference + 3-sample walkthrough"
      contains: "Project.Spec"
  key_links:
    - from: "docs/project-authoring.md"
      to: "api/v1alpha1/project_types.go field comments"
      via: "Project.Spec reference section lifts from these"
      pattern: "Project.Spec"
    - from: "docs/project-authoring.md"
      to: "examples/projects/{small,medium,large}/project.yaml"
      via: "3-sample walkthrough"
      pattern: "examples/projects/"
---

<objective>
Land `docs/project-authoring.md` covering Project.Spec field reference + the 3-sample walkthrough (small/medium/large per D-B1 cost spectrum) + outcome-prompt authoring guidance (Variant B from RESEARCH §"Topic 5").

Purpose: REQ-DIST-04 Project-authoring documentation. INSTALL.md taught operators to install TIDE; project-authoring.md teaches them to write their first Project.yaml. The 3-sample walkthrough teases small → medium → large; each shows different Project.Spec features.

Output: docs/project-authoring.md with Project.Spec reference, 3-sample walkthrough, outcome-prompt guidance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@docs/cli.md
@docs/git-hosts.md
@docs/live-e2e.md
@api/v1alpha1/project_types.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/project-authoring.md</name>
  <files>docs/project-authoring.md</files>
  <read_first>
    - docs/cli.md (analog per PATTERNS.md §"P2.4" — verb-by-verb table-of-contents shape)
    - docs/git-hosts.md (per-host structure analog for the 3-sample walkthrough)
    - docs/live-e2e.md lines 1-15 (Audience/Status/Scope opener)
    - api/v1alpha1/project_types.go (the canonical Project.Spec field definitions; field comments are the source-of-truth for the field reference table)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-B1" + §"D-B2" + §"D-B3" + §"D-B4" (3-sample cost spectrum, examples/projects/{small,medium,large}/ location, MIT fixture, outcome prompt shape)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 5" lines 1170-1184 (Variants A/B/C; B recommended) and §"Code Examples → Large Sample Project.yaml Outcome Prompt" lines 717-784 (Variant B canonical text)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P2.4" (verb-by-verb table TOC shape from cli.md)
  </read_first>
  <behavior>
    - File exists at `docs/project-authoring.md`
    - Opens with `# Authoring a TIDE Project` heading + Audience/Status/Scope opener
    - Has at least 5 level-2 sections: Project.Spec reference, 3-sample walkthrough (small/medium/large), outcome-prompt authoring guidance, provider configuration, next steps
    - Project.Spec section is a structured field reference (table or bulleted with one paragraph per field)
    - 3-sample walkthrough has 3 level-3 subheadings (small / medium / large) with per-sample cost framing per D-B1
    - Outcome-prompt section explains Variant A/B/C tradeoffs (RESEARCH §"Topic 5") and recommends Variant B
    - Forward links to docs/cli.md (next: drive the project with tide CLI) and docs/gates.md (next: configure gates if needed)
  </behavior>
  <action>
    Create `docs/project-authoring.md` with heading `# Authoring a TIDE Project`.

    Audience/Status/Scope opener:
    - Audience: "Operators writing their first `project.yaml`."
    - Status: "v1.0; covers Project.Spec at the v1alpha1 schema lock (Phase 1 D-A3)."
    - Scope of this doc: bulleted list naming the level-2 sections.

    Level-2 sections in order:

    1. `## Project.Spec field reference` — a Markdown table or structured list of the Project.Spec top-level fields. Source the field names and short descriptions from `api/v1alpha1/project_types.go` field comments. Required fields: `targetRepo`, `providerSecretRef` (or `secretRefs`), `subagent.image`, `subagent.model`, `git.repoURL`, `git.credsSecretRef`, `budget.absoluteCapCents`, `budget.rollingWindowDurationSeconds` (per Phase 04.1 P4.1), `gates.{milestone,phase,plan,task,pauseBetweenWaves}` (per Phase 4 D-G1), `outcomePrompt`, `governanceLevel`, `branchStrategy`. For each, one-line description + example value.

    2. `## 3-sample walkthrough` — opening paragraph framing the cost spectrum (D-B1). Then three level-3 subsections:

       `### small — $0 stub-subagent` — explains the small sample uses the stub-subagent (no API key needed), is the DIST-05 dry-run target, and tests the full dispatch path with canned envelopes. Walk through the project.yaml fields (`targetRepo`, `budget.absoluteCapCents: 0`, `subagent.image: ghcr.io/jsquirrelz/tide-stub-subagent:v1.0.0`). Show `kubectl apply -f examples/projects/small/project.yaml` + expected `Project.Status.Phase` progression.

       `### medium — ~$5 mini Claude (local-only git remote)` — explains the medium sample uses a real Claude image with a budget cap of $5 and a local-only git remote bootstrapped by `cmd/tide-demo-init` (per D-B3 + RESEARCH Topic 4). Walk through the project.yaml: `targetRepo: file:///demo-remote.git`, budget cap, model `claude-haiku-4-5`, the `demo-remote-pvc.yaml` + `demo-remote-init-job.yaml` preconditions. Show the apply sequence (PVC + init Job + project.yaml).

       `### large — ~$25 acceptance test` — explains the large sample IS the v1 acceptance test (D-A1 + D-B4), targets THIS TIDE repo, has a hard $25 cap (D-A2), and authors `internal/subagent/openai/` per Variant B outcome prompt. Reference `make acceptance-v1` Make target (Plan 05-15). Walk through the project.yaml fields: `targetRepo` = this repo, `budget.absoluteCapCents: 2500`, `gates: { milestone: auto, phase: auto, plan: auto, task: auto }` (no human gates per D-A1 Single Phase scope), `outcomePrompt: |` followed by the full Variant B text.

    3. `## Outcome-prompt authoring guidance` (RESEARCH §"Topic 5") — explains the tradeoffs:
       - Variant A (over-prescriptive): hand-authors the plan; TIDE just types. Avoid.
       - Variant B (recommended): concrete file list + scope constraint + pass criterion. Bounds task count, tests dispatch correctness.
       - Variant C (under-specified): TIDE wanders, hits budget cap. Avoid.
       Give a worked example for each tradeoff using the OpenAI provider authoring task.

    4. `## Provider configuration` — points to docs/git-hosts.md for git provider setup; explains how `providerSecretRef` points to a Secret holding `ANTHROPIC_API_KEY`. Per-level model selection note (subagent.model can be overridden per Project.Spec).

    5. `## Next steps` — Markdown links:
       - "Drive your project: [cli.md](cli.md) — operator-facing CLI verbs (apply, watch, approve, reject)"
       - "Configure gates: [gates.md](gates.md) — per-level gate policy"
       - "Troubleshoot: [troubleshooting.md](troubleshooting.md) — common failure modes"

    Voice: tight, declarative. Tables for the field reference. Fenced YAML for project.yaml examples; fenced bash for kubectl/apply commands.

    Length target: 300-500 lines.
  </action>
  <verify>
    <automated>test -s docs/project-authoring.md && grep -q "# Authoring a TIDE Project" docs/project-authoring.md && grep -qE "^## Project.Spec field reference" docs/project-authoring.md && grep -qE "^## 3-sample walkthrough" docs/project-authoring.md && grep -qE "^### small" docs/project-authoring.md && grep -qE "^### medium" docs/project-authoring.md && grep -qE "^### large" docs/project-authoring.md && grep -qE "^## Outcome-prompt authoring" docs/project-authoring.md && grep -qE "Variant B" docs/project-authoring.md && grep -q "internal/subagent/openai" docs/project-authoring.md && grep -q "examples/projects/" docs/project-authoring.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/project-authoring.md` returns 0
    - `head -1 docs/project-authoring.md` matches `# Authoring a TIDE Project`
    - `grep -cE '^## ' docs/project-authoring.md` returns ≥ 5
    - `grep -cE '^### ' docs/project-authoring.md` returns ≥ 3 (the three sample subsections)
    - `grep -q "Project.Spec" docs/project-authoring.md` returns 0
    - `grep -q "targetRepo" docs/project-authoring.md` returns 0
    - `grep -q "providerSecretRef\|secretRefs" docs/project-authoring.md` returns 0
    - `grep -q "absoluteCapCents" docs/project-authoring.md` returns 0
    - `grep -q "outcomePrompt" docs/project-authoring.md` returns 0
    - `grep -q "small" docs/project-authoring.md` returns 0
    - `grep -q "medium" docs/project-authoring.md` returns 0
    - `grep -q "large" docs/project-authoring.md` returns 0
    - `grep -q "Variant B" docs/project-authoring.md` returns 0
    - `grep -q "internal/subagent/openai" docs/project-authoring.md` returns 0
    - `grep -q "cli.md" docs/project-authoring.md` returns 0
    - `grep -q "gates.md" docs/project-authoring.md` returns 0
    - `grep -q "troubleshooting.md" docs/project-authoring.md` returns 0
    - `wc -l docs/project-authoring.md` outputs between 200 and 700 lines
  </acceptance_criteria>
  <done>docs/project-authoring.md exists; 5 sections; 3-sample walkthrough complete; Variant B outcome prompt embedded; forward-links present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator authoring project.yaml → docs/project-authoring.md | Misdocumented field semantics produce silently-broken Projects (e.g., wrong budget unit interpretation, wrong gate semantics) |
| Outcome-prompt text → TIDE planner subagent | Variant C-style vague prompts cause TIDE to fan out unpredictably, blowing the budget cap; docs steer operators to Variant B |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | Project.Spec field reference accuracy | mitigate | Field list sourced from api/v1alpha1/project_types.go comments — single source of truth; doc cites the file path so readers can verify |
| T-05-08-02 | Cost | Outcome-prompt phrasing in 3-sample walkthrough | mitigate | Variant B recommended verbatim from RESEARCH §"Topic 5" Code Examples; Variant A/C tradeoffs documented as anti-patterns; protects operators from running TIDE on under-specified outcomes |
| T-05-08-03 | Information Disclosure | Sample project.yaml credentials | accept | Sample project.yaml files (Plan 05-11) use `providerSecretRef: anthropic-creds` — name reference, not raw key; docs follow same pattern |
</threat_model>

<verification>
After Task 1: `bash hack/scripts/verify-docs-coverage.sh --require-all-files` is closer to passing; only troubleshooting.md + rbac.md remain missing (lands in Plans 05-09 + 05-10).
</verification>

<success_criteria>
- docs/project-authoring.md exists with 5 level-2 sections + 3 sample subsections
- Variant B outcome prompt embedded in the large-sample walkthrough
- Forward-links to cli.md/gates.md/troubleshooting.md
- DIST-04 project-authoring piece satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-08-SUMMARY.md` with line count, section count, and commit SHA.
</output>
