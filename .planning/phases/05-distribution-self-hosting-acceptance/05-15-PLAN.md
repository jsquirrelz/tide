---
phase: 05-distribution-self-hosting-acceptance
plan: 15
type: execute
wave: 4
depends_on: [05, 11, 12, 13, 14]
files_modified:
  - Makefile
  - hack/scripts/dry-run-v1.sh
  - hack/scripts/render-dry-run-report.sh
  - hack/scripts/acceptance-v1.sh
  - hack/scripts/acceptance-verify.sh
autonomous: true
requirements: [DIST-05, BOOT-02, BOOT-04]
tags: [dry-run, acceptance, makefile, dind, evidence-capture]

must_haves:
  truths:
    - "Makefile carries `dry-run-v1` and `acceptance-v1` targets (D-D1 + D-A4)"
    - "hack/scripts/dry-run-v1.sh runs Docker-in-Docker on ubuntu:24.04, executes the README Quickstart verbatim, times each phase, exits non-zero if > 30 min OR any step fails"
    - "hack/scripts/render-dry-run-report.sh shapes dry-run-report.json with schemaVersion: 1 (D-D4 + Topic 10)"
    - "hack/scripts/acceptance-v1.sh spins fresh kind cluster, helm-installs charts, applies large-sample project.yaml, captures evidence under .acceptance-runs/<ts>/, refuses to run without ANTHROPIC_API_KEY env (D-A4)"
    - "hack/scripts/acceptance-verify.sh runs the 7 D-A3 pass checks and exits 0 only when all 7 pass"
  artifacts:
    - path: "hack/scripts/dry-run-v1.sh"
      provides: "DIST-05 DinD external-operator dry-run driver"
      contains: "docker run"
    - path: "hack/scripts/render-dry-run-report.sh"
      provides: "dry-run-report.json shaper (schemaVersion 1)"
    - path: "hack/scripts/acceptance-v1.sh"
      provides: "BOOT-04 maintainer-ritual driver"
      contains: "ANTHROPIC_API_KEY"
    - path: "hack/scripts/acceptance-verify.sh"
      provides: "7-check D-A3 verifier"
  key_links:
    - from: "Makefile dry-run-v1"
      to: "hack/scripts/dry-run-v1.sh"
      via: "recipe invocation"
      pattern: "dry-run-v1.sh"
    - from: "hack/scripts/dry-run-v1.sh"
      to: "examples/projects/small/project.yaml (timer-stop target via Project.Status.Phase=Complete)"
      via: "DinD container runs `kubectl apply -f examples/projects/small/`"
      pattern: "examples/projects/small"
    - from: "hack/scripts/acceptance-v1.sh"
      to: "examples/projects/large/project.yaml (the BOOT-04 acceptance target)"
      via: "kubectl apply"
      pattern: "examples/projects/large"
    - from: "hack/scripts/acceptance-verify.sh"
      to: "the 7 D-A3 pass criteria"
      via: "sequential check blocks"
      pattern: "Check 1.*Check 2.*Check 7"
---

<objective>
Land the four shell scripts under `hack/scripts/` (dry-run-v1.sh + render-dry-run-report.sh + acceptance-v1.sh + acceptance-verify.sh) and add the two Makefile targets (`dry-run-v1` + `acceptance-v1`) that wrap them. Closes the operator-readiness execution loop locked in D-D1..D-D4 and D-A1..D-A4.

Purpose: REQ-DIST-05 + REQ-BOOT-02 + REQ-BOOT-04. `make dry-run-v1` proves install-flow correctness in DinD on every `v*-rc.*` tag (Plan 05-16 wires the CI gate). `make acceptance-v1` is the maintainer's one-command acceptance test against the large sample (BOOT-04 v1 acceptance test).

Output: 4 scripts under hack/scripts/, 2 Makefile targets, dry-run-report.json schema landed, 7-check acceptance verifier ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@Makefile
@hack/helm/augment-tide-chart.sh
@.github/workflows/ci.yaml
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: hack/scripts/dry-run-v1.sh + render-dry-run-report.sh + Makefile dry-run-v1 target</name>
  <files>hack/scripts/dry-run-v1.sh, hack/scripts/render-dry-run-report.sh, Makefile</files>
  <read_first>
    - hack/helm/augment-tide-chart.sh lines 1-30 (PATTERNS.md §"Shell-script preamble" — `set -euo pipefail` + REPO_ROOT)
    - Makefile lines 190-197 (`test-e2e-live` target — env-gate + script invocation pattern per PATTERNS.md §"P6.2") + lines 153-167 (`test-int-kind-prep` — DinD-adjacent docker build + kind load orchestration)
    - .github/workflows/ci.yaml lines 184-189 (pinned kind v0.31.0 install pattern; line 129 helm 3.16.3 pin) — RESEARCH §"P7.1" identifies these as canonical pinned tool versions
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Code Examples → make dry-run-v1" lines 615-633 + §"Code Examples → dry-run-v1.sh skeleton" lines 635-715 (CANONICAL — copy verbatim with adjustments) + §"Topic 10" lines 1330-1382 (dry-run-report.json schema)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-D1..D-D4" (DinD, ubuntu:24.04, timer-stop at small Project Complete, < 30 min, schema)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P7.1" + §"P7.3"
    - examples/projects/small/project.yaml (Plan 05-11 — the dry-run timer-stop target; namespace `tide-sample-small`, project name `small-project`)
    - charts/tide/Chart.yaml + charts/tide-crds/Chart.yaml (Plan 05-05 — confirm version 1.0.0 to use in --version flags inside the script)
  </read_first>
  <behavior>
    - `hack/scripts/dry-run-v1.sh` is executable; uses set -euo pipefail + REPO_ROOT preamble
    - Script runs Docker-in-Docker via `docker run --rm -v /var/run/docker.sock:/var/run/docker.sock --network host ubuntu:24.04 bash -c '...'`
    - Inside the DinD container: installs kind v0.31.0, helm v3.16.3, kubectl v1.31.0 (pinned URLs matching ci.yaml); git clones the TIDE repo (or uses /workspace mount from host); runs the 4 README Quickstart commands verbatim
    - Timer stops at `kubectl wait --for=jsonpath="{.status.phase}"=Complete project/small-project -n tide-sample-small --timeout=10m`
    - Outputs transcript.log + dry-run-report.json under `${DRY_RUN_DIR:-/tmp/tide-dry-run-$(date +%s)}/`
    - Exits non-zero if elapsed > `${DRY_RUN_TIMEOUT_SECONDS:-1800}` (30 min default per D-D3) OR if any inner command fails
    - `hack/scripts/render-dry-run-report.sh` produces a JSON file matching the schema in RESEARCH §"Topic 10" lines 1342-1378 (schemaVersion 1; runId; phases array with elapsed-per-phase; versions object; host object)
    - Makefile `dry-run-v1` target wraps the script; help text per PATTERNS.md §"P6.2"
  </behavior>
  <action>
    Step 1: Create `hack/scripts/dry-run-v1.sh`. Copy the canonical skeleton from RESEARCH §"Code Examples → dry-run-v1.sh skeleton" lines 638-715 verbatim. Adjustments for THIS plan:

    - File header line 1: `#!/usr/bin/env bash`
    - File header line 2: `# Phase 5 D-D1 — Docker-in-Docker external-operator dry-run.`
    - Line 3-5: brief description + `set -euo pipefail`
    - Then the REPO_ROOT + DRY_RUN_DIR + REPORT_PATH + TRANSCRIPT_PATH + TIMEOUT_SECONDS setup
    - The Docker invocation: `docker run --rm -v /var/run/docker.sock:/var/run/docker.sock -v "$DRY_RUN_DIR":/workspace --network host ubuntu:24.04 bash -c '...'`
    - Inside the container: apt-get install curl ca-certificates git, then install pinned kind/helm/kubectl, then git clone the TIDE repo via `git clone https://github.com/jsquirrelz/tide /workspace/tide` (CI sets this URL; local runs can override DRY_RUN_REPO_URL env var to point at a local path)
    - The 4 Quickstart commands inside the DinD: kind create + helm install tide-crds (local chart path `./charts/tide-crds`, NOT OCI for the v1.0-pre-release dry-run; OCI install path goes live after Plan 05-16 publishes the chart; the dry-run mirrors the Quickstart's "cloned-repo" alternative path for the rc tag gate) + helm install tide + kubectl apply small + kubectl wait Complete
    - At the end of the docker run: capture exit code; capture elapsed seconds; invoke `hack/scripts/render-dry-run-report.sh` with elapsed + exit code as args; check elapsed > TIMEOUT_SECONDS and exit non-zero if so
    - Final emit: `echo "PASS: dry-run completed in ${ELAPSED}s (under ${TIMEOUT_SECONDS}s cap)"` and `echo "Evidence: $REPORT_PATH + $TRANSCRIPT_PATH"`

    Pinned versions per RESEARCH §"P7.1" — embed as constants:
    - kind: v0.31.0
    - helm: v3.16.3
    - kubectl: v1.31.0

    `chmod +x hack/scripts/dry-run-v1.sh`.

    Step 2: Create `hack/scripts/render-dry-run-report.sh`. Preamble + REPO_ROOT.

    Accepts positional args: `$1 = report_path`, `$2 = elapsed_seconds`, `$3 = exit_code`, optional `$4 = per-phase-timings-json-fragment`.

    Renders the JSON to `${REPORT_PATH}` per RESEARCH §"Topic 10" lines 1342-1378 schema:
    ```json
    {
      "schemaVersion": 1,
      "runId": "${GITHUB_REF_NAME:-local-$(date +%s)}",
      "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
      "totalSeconds": <elapsed>,
      "exitCode": <exit_code>,
      "phases": [],
      "versions": {
        "kindVersion": "v0.31.0",
        "helmVersion": "v3.16.3",
        "kubeVersion": "v1.31.0",
        "goVersion": "1.26"
      },
      "host": {
        "baseImage": "ubuntu:24.04",
        "arch": "linux/amd64"
      },
      "tideVersion": "${GITHUB_REF_NAME:-v1.0.0-dev}",
      "chartVersions": {
        "tide": "1.0.0",
        "tide-crds": "1.0.0"
      }
    }
    ```

    Use a pure-bash heredoc (per RESEARCH §"P7.3" — option (a); schema is flat). Use `jq` ONLY if jq is available; otherwise heredoc + interpolation. Echo `OK: rendered ${REPORT_PATH}` on success.

    `chmod +x hack/scripts/render-dry-run-report.sh`.

    Step 3: Append to Makefile (after `verify-license:` Plan 05-01 target / after `verify-docs:` Plan 05-04 target / after `test-per-ns-rb:` Plan 05-13 target). Use the canonical block from RESEARCH §"Code Examples → make dry-run-v1" lines 615-624:

    ```
    ##@ Phase 5 v1.0 ship gates

    DRY_RUN_IMAGE ?= ubuntu:24.04
    DRY_RUN_TIMEOUT_SECONDS ?= 1800

    .PHONY: dry-run-v1
    dry-run-v1: ## Phase 5 D-D1 — Docker-in-Docker external-operator dry-run.
    	@hack/scripts/dry-run-v1.sh
    ```

    Place AFTER existing `##@ Live nightly E2E ...` section per PATTERNS.md §"P6.2".

    Step 4: Validate by running `bash -n hack/scripts/dry-run-v1.sh` (shell syntax) and `bash -n hack/scripts/render-dry-run-report.sh`. Do NOT execute `make dry-run-v1` end-to-end at edit time (it's a 20+ min DinD operation that needs Docker daemon access) — the script is verified via syntax check + structural assertions. End-to-end run is a verification step in Plan 05-16 release CI.
  </action>
  <verify>
    <automated>test -x hack/scripts/dry-run-v1.sh && test -x hack/scripts/render-dry-run-report.sh && bash -n hack/scripts/dry-run-v1.sh && bash -n hack/scripts/render-dry-run-report.sh && grep -q "set -euo pipefail" hack/scripts/dry-run-v1.sh && grep -q "docker run" hack/scripts/dry-run-v1.sh && grep -q "ubuntu:24.04" hack/scripts/dry-run-v1.sh && grep -q "examples/projects/small" hack/scripts/dry-run-v1.sh && grep -q "kind create cluster" hack/scripts/dry-run-v1.sh && grep -q "v0.31.0" hack/scripts/dry-run-v1.sh && grep -q "v3.16.3" hack/scripts/dry-run-v1.sh && grep -q "schemaVersion" hack/scripts/render-dry-run-report.sh && grep -qE '^dry-run-v1:' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `test -x hack/scripts/dry-run-v1.sh` returns 0
    - `bash -n hack/scripts/dry-run-v1.sh` exits 0 (syntax valid)
    - `head -1 hack/scripts/dry-run-v1.sh` matches `#!/usr/bin/env bash`
    - `grep -c "set -euo pipefail" hack/scripts/dry-run-v1.sh` returns ≥ 1
    - `grep -q "REPO_ROOT" hack/scripts/dry-run-v1.sh` returns 0
    - `grep -q "docker run --rm" hack/scripts/dry-run-v1.sh` returns 0 (DinD invocation)
    - `grep -q "/var/run/docker.sock" hack/scripts/dry-run-v1.sh` returns 0 (canonical DinD socket mount per Pitfall 6)
    - `grep -q "ubuntu:24.04" hack/scripts/dry-run-v1.sh` returns 0
    - `grep -q "v0.31.0" hack/scripts/dry-run-v1.sh` returns 0 (kind pinned)
    - `grep -q "v3.16.3" hack/scripts/dry-run-v1.sh` returns 0 (helm pinned)
    - `grep -q "v1.31.0" hack/scripts/dry-run-v1.sh` returns 0 (kubectl pinned)
    - `grep -q "kind create cluster" hack/scripts/dry-run-v1.sh` returns 0
    - `grep -q "examples/projects/small" hack/scripts/dry-run-v1.sh` returns 0 (timer-stop target)
    - `grep -q "small-project" hack/scripts/dry-run-v1.sh` returns 0
    - `grep -q "tide-sample-small" hack/scripts/dry-run-v1.sh` returns 0
    - `grep -q "DRY_RUN_TIMEOUT_SECONDS" hack/scripts/dry-run-v1.sh` returns 0
    - `test -x hack/scripts/render-dry-run-report.sh` returns 0
    - `bash -n hack/scripts/render-dry-run-report.sh` exits 0
    - `grep -q '"schemaVersion": 1' hack/scripts/render-dry-run-report.sh` returns 0
    - `grep -q "tideVersion" hack/scripts/render-dry-run-report.sh` returns 0
    - `grep -q "chartVersions" hack/scripts/render-dry-run-report.sh` returns 0
    - `grep -cE '^dry-run-v1:' Makefile` returns 1
    - `grep -q "DRY_RUN_TIMEOUT_SECONDS" Makefile` returns 0
  </acceptance_criteria>
  <done>dry-run-v1.sh + render-dry-run-report.sh exist; pinned versions match ci.yaml; Makefile target wired; syntax valid; ready for DinD execution in Plan 05-16 release CI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: hack/scripts/acceptance-v1.sh + acceptance-verify.sh + Makefile acceptance-v1 target</name>
  <files>hack/scripts/acceptance-v1.sh, hack/scripts/acceptance-verify.sh, Makefile</files>
  <read_first>
    - Makefile lines 190-197 (`test-e2e-live` — env-gate + script invocation pattern per PATTERNS.md §"P6.2")
    - Makefile lines 473-482 (`verify-no-blocking` — grep-driven OK/FAIL pattern per PATTERNS.md §"P7.2") + lines 486-495 (`verify-no-rbac-wildcards` — multi-condition verifier pattern)
    - hack/helm/augment-tide-chart.sh lines 1-30 (preamble)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Acceptance Test Flow" lines 274-322 (canonical evidence-capture + 7-check sequence) + §"Code Examples → P7.2 acceptance-verify.sh" structure (lines 705-746) + §"Code Examples → P7.4 acceptance-v1.sh" structure (lines 783-821)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-A1..D-A4" (Single Phase scope, $25 cap, 7 pass criteria, maintainer-ritual, .acceptance-runs/<ts>/ evidence)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P7.2" + §"P7.4"
    - examples/projects/large/project.yaml (Plan 05-11 — the BOOT-04 acceptance project.yaml)
  </read_first>
  <behavior>
    - `hack/scripts/acceptance-v1.sh` exists, executable; refuses to run if ANTHROPIC_API_KEY env not set; ALSO refuses if GH_PAT (or similar git PAT) env not set
    - Spins fresh kind cluster `tide-acceptance-<unix-ts>`; loads built images if applicable; helm-installs tide-crds + tide
    - Creates `tide-secrets` Secret in `tide-sample-large` namespace with `ANTHROPIC_API_KEY` + git PAT
    - Applies `examples/projects/large/project.yaml`
    - Waits for `Project.Status.Phase=Complete` (or BudgetExceeded) with 4h timeout
    - Captures evidence under `.acceptance-runs/<ts>/` per CONTEXT.md D-A4: controller.log, CRDs yaml, per-run-branch git log, optional dashboard screenshot stub
    - Invokes `hack/scripts/acceptance-verify.sh` with project name + namespace + run-start args
    - Exits 0 only if verifier exits 0
    - `hack/scripts/acceptance-verify.sh` exists, executable; runs 7 sequential checks per D-A3 (per-run branch exists, 4 commit-message shapes present, Phase=Complete, zero ERROR logs, no orphan Jobs, secret-leak metric=0, costSpentCents<2500); each check emits OK/FAIL; final result reports `PASS: all 7 D-A3 checks passed` or `FAIL: N of 7 D-A3 checks failed` with offending check details
    - Makefile `acceptance-v1` target wraps acceptance-v1.sh; env-gate `if [ -z "$$ANTHROPIC_API_KEY" ]; then ... exit 1; fi`
  </behavior>
  <action>
    Step 1: Create `hack/scripts/acceptance-v1.sh`. Preamble per PATTERNS.md §"Shell-script preamble":
    ```bash
    #!/usr/bin/env bash
    # Phase 5 D-A4 — maintainer-only acceptance ritual ($25 hard cap; requires ANTHROPIC_API_KEY).
    set -euo pipefail
    REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
    ```

    Implementation per RESEARCH §"P7.4 acceptance-v1.sh" lines 783-821 structure:

    - Env validation: `: "${ANTHROPIC_API_KEY:?ANTHROPIC_API_KEY required for acceptance-v1 — see docs/INSTALL.md}"` (the `:?` syntax aborts with that error if unset; matches `test-e2e-live` pattern). Also validate `${GH_PAT:?GH_PAT required for git creds Secret}` for the GitHub push credentials.
    - Generate timestamp: `TIMESTAMP=$(date +%s)`; create evidence directory `ACCEPTANCE_DIR="${REPO_ROOT}/.acceptance-runs/${TIMESTAMP}"; mkdir -p "${ACCEPTANCE_DIR}"`
    - Run start timestamp for log filter: `RUN_START=$(date -u +%Y-%m-%dT%H:%M:%SZ)`
    - Create kind cluster: `kind create cluster --name "tide-acceptance-${TIMESTAMP}"`
    - Image load if needed: if `${SKIP_IMAGE_LOAD:-}` not set, build + load via existing `test-int-kind-prep` invocation (or expect images already pushed); deferable to docs/INSTALL.md — acceptance harness can use published images via `pullPolicy: IfNotPresent`
    - Helm install: `helm install tide-crds "${REPO_ROOT}/charts/tide-crds" -n tide-system --create-namespace` then `helm install tide "${REPO_ROOT}/charts/tide" -n tide-system`; wait for `kubectl wait --for=condition=Available deploy/tide-controller-manager -n tide-system --timeout=5m`
    - Create namespace + Secret: `kubectl apply -f "${REPO_ROOT}/examples/projects/large/namespace.yaml"` then `kubectl create secret generic tide-secrets --from-literal=ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY}" --from-literal=GIT_PAT="${GH_PAT}" -n tide-sample-large --dry-run=client -o yaml | kubectl apply -f -`
    - Apply project: `kubectl apply -f "${REPO_ROOT}/examples/projects/large/project.yaml"`
    - Wait for completion: `kubectl wait --for=jsonpath='{.status.phase}'=Complete project/large-project -n tide-sample-large --timeout=4h` (D-A3 allows up to 4h wall-clock; if BudgetExceeded fires first, kubectl wait exits non-zero on Phase change to BudgetExceeded — the verifier catches this)
    - Evidence capture:
      - `kubectl logs -n tide-system deploy/tide-controller-manager --since="${RUN_START}" > "${ACCEPTANCE_DIR}/controller.log"`
      - `kubectl get project,phase,plan,task,wave,job -A -o yaml > "${ACCEPTANCE_DIR}/crds.yaml"`
      - `(cd "${REPO_ROOT}" && git fetch origin && git log "origin/tide/run-large-project-*" --oneline > "${ACCEPTANCE_DIR}/run-branch.log" || true)` (optional — branch may not exist locally; fetch + log)
      - Dashboard screenshot is documented but not auto-captured per RESEARCH §"Open Question 5"; the script prints `"Acceptance phase complete. Open dashboard at http://localhost:8080 and screenshot it for the release notes."`
    - Final verifier invocation: `bash "${REPO_ROOT}/hack/scripts/acceptance-verify.sh" large-project tide-sample-large "${RUN_START}"`
    - On verifier pass: `echo "ACCEPTANCE PASS — evidence at ${ACCEPTANCE_DIR}"; exit 0`. On fail: forward exit code.

    `chmod +x hack/scripts/acceptance-v1.sh`.

    Step 2: Create `hack/scripts/acceptance-verify.sh`. Preamble + REPO_ROOT.

    Args: `$1 = project_name`, `$2 = namespace`, `$3 = run_start_iso8601`.

    Implementation per RESEARCH §"P7.2 acceptance-verify.sh" lines 708-746 — 7 sequential checks:

    Check 1: Per-run branch exists. `git ls-remote --heads origin "tide/run-${PROJECT_NAME}-*" | grep -q .` (uses local origin remote; CI runs with the project's actual remote configured).

    Check 2: Per-run branch carries 4 D-B2 commit shapes (per Phase 3 D-B2). For each shape regex (`tide: plan .+ authored \\+ executed`, `tide: phase .+ authored`, `tide: project complete`), git log the per-run branch and assert ≥ 1 match. The 4th shape (`tide: milestone — N/A for Single Phase scope`) is per CONTEXT.md D-A3 #2 — note that D-A1 Single Phase scope means the milestone shape might literally read "N/A" or might be omitted; executor reads the actual Phase 3 D-B2 message format from `internal/controller/dispatch_helpers.go` to confirm.

    Check 3: `Project.Status.Phase == Complete`. `STATUS=$(kubectl get project "${PROJECT_NAME}" -n "${NAMESPACE}" -o jsonpath='{.status.phase}')` then `[ "${STATUS}" = "Complete" ]`.

    Check 4: Zero ERROR controller logs since RUN_START. `kubectl logs -n tide-system deploy/tide-controller-manager --since="${RUN_START}" | grep -cE '^.*"level":"ERROR"' || true` returns 0 (handle grep no-match non-zero exit safely via `|| true` + count assertion).

    Check 5: No orphan Jobs. `kubectl get jobs --all-namespaces -l "tideproject.k8s/project-uid=$(kubectl get project ${PROJECT_NAME} -n ${NAMESPACE} -o jsonpath='{.metadata.uid}')" -o jsonpath='{range .items[*]}{.metadata.name}={.status.succeeded}{"\\n"}{end}'` — every Job's `status.succeeded` must be 1.

    Check 6: `tide_secret_leak_blocked_total{project=PROJECT_NAME}` == 0. Hit the controller's /metrics endpoint via port-forward OR via `kubectl get --raw` proxy URL; parse Prometheus text format for the metric. For maintainer-runtime ritual, a simpler version: `kubectl get -n tide-system deploy/tide-controller-manager -o jsonpath='{...}'` won't suffice — need the metrics endpoint. Use: `kubectl exec -n tide-system deploy/tide-controller-manager -c manager -- wget -qO- localhost:8080/metrics 2>/dev/null | grep "^tide_secret_leak_blocked_total" | grep "project=\"${PROJECT_NAME}\"" | awk '{print $2}'` — must be 0 or empty.

    Check 7: `Project.Status.budget.costSpentCents < 2500`. `SPENT=$(kubectl get project "${PROJECT_NAME}" -n "${NAMESPACE}" -o jsonpath='{.status.budget.costSpentCents}')` then `[ "${SPENT}" -lt 2500 ]`.

    Each check: emit `Check N: <name>...` then on success `  OK` else `  FAIL: <details>` + increment FAILS.

    Final: `if [ "${FAILS:-0}" -gt 0 ]; then echo "FAIL: ${FAILS} of 7 D-A3 checks failed"; exit 1; fi; echo "PASS: all 7 D-A3 checks passed"`.

    `chmod +x hack/scripts/acceptance-verify.sh`.

    Step 3: Append Makefile `acceptance-v1` target (use the canonical block from RESEARCH §"Code Examples → make acceptance-v1" lines 625-632):
    ```
    .PHONY: acceptance-v1
    acceptance-v1: ## Phase 5 D-A4 — maintainer ritual ($25 hard cap; requires ANTHROPIC_API_KEY).
    	@if [ -z "$$ANTHROPIC_API_KEY" ]; then \
    	  echo "ERROR: ANTHROPIC_API_KEY env not set — refusing to run acceptance-v1"; \
    	  echo "       See docs/INSTALL.md for Secret setup."; \
    	  exit 1; \
    	fi
    	@hack/scripts/acceptance-v1.sh
    ```

    Step 4: `bash -n` syntax check both scripts. Do NOT execute end-to-end (acceptance-v1 is maintainer-only with ~$25 cost).
  </action>
  <verify>
    <automated>test -x hack/scripts/acceptance-v1.sh && test -x hack/scripts/acceptance-verify.sh && bash -n hack/scripts/acceptance-v1.sh && bash -n hack/scripts/acceptance-verify.sh && grep -q "ANTHROPIC_API_KEY" hack/scripts/acceptance-v1.sh && grep -q "examples/projects/large" hack/scripts/acceptance-v1.sh && grep -q ".acceptance-runs" hack/scripts/acceptance-v1.sh && grep -q "acceptance-verify.sh" hack/scripts/acceptance-v1.sh && grep -q "Check 1" hack/scripts/acceptance-verify.sh && grep -q "Check 7" hack/scripts/acceptance-verify.sh && grep -q "tide/run-" hack/scripts/acceptance-verify.sh && grep -q "tide_secret_leak_blocked_total" hack/scripts/acceptance-verify.sh && grep -q "costSpentCents" hack/scripts/acceptance-verify.sh && grep -qE '^acceptance-v1:' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `test -x hack/scripts/acceptance-v1.sh` returns 0
    - `bash -n hack/scripts/acceptance-v1.sh` exits 0
    - `grep -q "set -euo pipefail" hack/scripts/acceptance-v1.sh` returns 0
    - `grep -q "ANTHROPIC_API_KEY" hack/scripts/acceptance-v1.sh` returns 0 (env guard)
    - `grep -q "kind create cluster" hack/scripts/acceptance-v1.sh` returns 0
    - `grep -q "helm install tide-crds" hack/scripts/acceptance-v1.sh` returns 0
    - `grep -q "examples/projects/large/project.yaml" hack/scripts/acceptance-v1.sh` returns 0
    - `grep -q ".acceptance-runs" hack/scripts/acceptance-v1.sh` returns 0 (evidence capture dir)
    - `grep -q "acceptance-verify.sh" hack/scripts/acceptance-v1.sh` returns 0 (verifier invocation)
    - `grep -q "kubectl logs" hack/scripts/acceptance-v1.sh` returns 0
    - `grep -q "kubectl get project" hack/scripts/acceptance-v1.sh` returns 0
    - `test -x hack/scripts/acceptance-verify.sh` returns 0
    - `bash -n hack/scripts/acceptance-verify.sh` exits 0
    - `grep -c "Check [1-7]:" hack/scripts/acceptance-verify.sh` returns 7 (one for each D-A3 check)
    - `grep -q "tide/run-" hack/scripts/acceptance-verify.sh` returns 0 (Check 1: per-run branch)
    - `grep -q 'Phase.*== Complete\|status.phase' hack/scripts/acceptance-verify.sh` returns 0 (Check 3)
    - `grep -q "tide_secret_leak_blocked_total" hack/scripts/acceptance-verify.sh` returns 0 (Check 6)
    - `grep -q "costSpentCents" hack/scripts/acceptance-verify.sh` returns 0 (Check 7)
    - `grep -q "PASS: all 7 D-A3 checks passed" hack/scripts/acceptance-verify.sh` returns 0
    - `grep -cE '^acceptance-v1:' Makefile` returns 1
    - Make target gates: `make acceptance-v1` (without ANTHROPIC_API_KEY) exits non-zero with the expected "ERROR: ANTHROPIC_API_KEY env not set" message
  </acceptance_criteria>
  <done>acceptance-v1.sh + acceptance-verify.sh + Makefile target exist; env-gate refuses to run without ANTHROPIC_API_KEY; 7-check verifier ready for maintainer ritual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer's local ANTHROPIC_API_KEY → kind cluster → Anthropic API | The acceptance ritual carries the key into a kind cluster via Secret; Phase 2 credproxy isolates it from subagent pods; but the key still passes through the maintainer's machine + the Secret YAML pipe |
| DinD Docker socket → host Docker daemon | dry-run-v1.sh mounts /var/run/docker.sock; the ubuntu:24.04 container can fully control the host Docker daemon (and thus kind, and thus the cluster) — this is the canonical DinD pattern but it's full root-equivalent power |
| Per-run branch push → configured git remote | acceptance-v1 pushes to `tide/run-large-project-<ts>` on the configured remote (this repo for D-A1); `--force-with-lease` per Phase 3 D-B6 prevents accidental main override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15-01 | Information Disclosure | ANTHROPIC_API_KEY in acceptance-v1 invocation | mitigate | Env var passed via `--from-literal=ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY}"` through stdin (dry-run=client -o yaml \| kubectl apply -f -); never written to disk; Phase 2 HARN-04 redaction applies once it reaches the controller |
| T-05-15-02 | Elevation of Privilege | DinD docker.sock mount in dry-run | mitigate | Canonical DinD pattern (mounted, not nested-dockerd); --network host scopes kind's bridge; no --privileged flag; script runs as the operator's local user (no sudo); container is `--rm` cleanup so no persistent state |
| T-05-15-03 | Information Disclosure | Evidence capture under .acceptance-runs/<ts>/ | mitigate | Captured files (controller.log, crds.yaml, run-branch.log) live on local disk only — never automatically uploaded; .acceptance-runs/ is .gitignore-listed (executor verifies + adds if missing) so accidental commit doesn't leak production data |
| T-05-15-04 | Tampering | Per-run branch push during acceptance | mitigate | Phase 3 D-B6 `--force-with-lease` + per-run branch naming `tide/run-<project>-<ts>` (never `main`); gitleaks scan on every push (Phase 3 D-B3); Check 6 in acceptance-verify.sh asserts secret-leak metric is 0 |
| T-05-15-05 | Tampering | 7-check verifier reading stale evidence | mitigate | Check 4 (ERROR logs) uses `--since="${RUN_START}"` to scope log filter to the run; Check 5 (orphan Jobs) uses project-uid label to scope to the run; checks are run-scoped, not cluster-scoped |
</threat_model>

<verification>
After both tasks: `bash -n` syntax-validates both scripts. `make acceptance-v1` (without ANTHROPIC_API_KEY set) fails with the expected env-guard error. `make dry-run-v1` end-to-end is verified in Plan 05-16 release CI.
</verification>

<success_criteria>
- 4 shell scripts under hack/scripts/ (dry-run-v1.sh + render-dry-run-report.sh + acceptance-v1.sh + acceptance-verify.sh)
- 2 Makefile targets (dry-run-v1 + acceptance-v1)
- All scripts pass bash -n syntax check
- 7-check D-A3 verifier complete
- DIST-05 + BOOT-02 + BOOT-04 plumbing satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-15-SUMMARY.md` documenting script line counts, Makefile target lines added, env-guard test result, and commit SHA.
</output>
