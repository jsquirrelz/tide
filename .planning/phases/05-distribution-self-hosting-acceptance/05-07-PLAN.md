---
phase: 05-distribution-self-hosting-acceptance
plan: 07
type: execute
wave: 2
depends_on: [05]
files_modified:
  - docs/INSTALL.md
autonomous: true
requirements: [DIST-04]
tags: [docs, install, on-ramp]

must_haves:
  truths:
    - "docs/INSTALL.md exists as the single source of truth for install steps (D-C2)"
    - "INSTALL.md covers prerequisites, CRD-subchart-first install order (Pitfall 4), per-OS prerequisite install (macOS/Linux/Windows-WSL2), ANTHROPIC_API_KEY Secret creation, git creds Secret creation, first-Project apply"
    - "INSTALL.md uses chart version 1.0.0 throughout (depends_on Plan 05-05 lockstep bump)"
    - "INSTALL.md includes an 'Is TIDE right for me?' 3-bullet section (Pitfall 8 mitigation)"
    - "INSTALL.md shows both OCI install paths (D-X6) and the cloned-repo path"
  artifacts:
    - path: "docs/INSTALL.md"
      provides: "Operator install on-ramp"
      contains: "helm install tide-crds"
  key_links:
    - from: "docs/INSTALL.md"
      to: "docs/project-authoring.md (next step)"
      via: "forward link at end"
      pattern: "project-authoring.md"
    - from: "docs/INSTALL.md"
      to: "examples/projects/small/project.yaml (first apply target)"
      via: "first-Project section"
      pattern: "examples/projects/small"
---

<objective>
Land `docs/INSTALL.md` as the single source of truth for installing TIDE (D-C2). Covers prerequisites + CRD-subchart-first install order (Pitfall 4) + per-OS prereq install (macOS/Linux/Windows-WSL2 per D-C2) + ANTHROPIC_API_KEY Secret + git creds Secret + first-Project apply + "Is TIDE right for me?" section per Pitfall 8 mitigation.

Purpose: REQ-DIST-04 install documentation. INSTALL.md is the on-ramp from the README Quickstart — operators land here for full prereq + per-OS + Secret + multi-sample install detail. CRDs-first ordering is the critical Pitfall 4 mitigation; chart version 1.0.0 throughout (from Plan 05-05 lockstep bump).

Output: docs/INSTALL.md with all D-C2-required sections + Pitfall 4 + Pitfall 8 mitigations baked in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@docs/git-hosts.md
@docs/live-e2e.md
@docs/dashboard.md
@docs/rwx-drivers.md
@charts/tide/Chart.yaml
@charts/tide-crds/Chart.yaml
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/INSTALL.md</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - docs/git-hosts.md (exact analog per PATTERNS.md §"P2.3" — per-platform recipes structure for the per-OS sections)
    - docs/live-e2e.md lines 1-15 (Audience/Status/Scope opener convention)
    - docs/dashboard.md (port-forward section — INSTALL.md links here for dashboard access)
    - docs/rwx-drivers.md (RWX driver matrix — INSTALL.md links here for PVC setup)
    - charts/tide/Chart.yaml + charts/tide-crds/Chart.yaml (verify version 1.0.0 from Plan 05-05; INSTALL.md cites these explicitly)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-C2" + §"D-X6" (locks: single source of truth; per-OS sections; OCI commands)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Pitfall 4" (CRDs first — install order is load-bearing) and §"Pitfall 8" ("Is TIDE for me?" 3-bullet section)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P2.3" (per-host structure analog from git-hosts.md)
  </read_first>
  <behavior>
    - File exists at `docs/INSTALL.md`
    - Opens with `# TIDE Install Guide` + Audience/Status/Scope opener per docs/live-e2e.md
    - 7 level-2 sections per RESEARCH §"P2.3" heading shape (audience/scope/per-OS prereq install/CRD-subchart-first install order/Secret creation/git creds Secret/first-Project apply/"Is TIDE for me?")
    - Per-OS sections: macOS (brew install ...), Linux (curl/wget pinned versions), Windows-WSL2 (WSL2 caveat + Linux instructions reuse)
    - Install order section opens with Pitfall 4 mitigation: "Install `tide-crds` BEFORE `tide`. CRDs must register before the main chart's webhook configs apply."
    - All helm commands use `--version 1.0.0` (post-Plan-05-05 bump)
    - OCI install path + cloned-repo install path both shown (D-X6 lock)
    - "Is TIDE for me?" section per Pitfall 8: 3 bullets each for "yes if:" and "no if:" — 6 bullets total
    - Forward link at end: "Next: [project-authoring.md](project-authoring.md) — author your first Project"
  </behavior>
  <action>
    Create `docs/INSTALL.md`. Heading `# TIDE Install Guide`.

    Audience/Status/Scope opener per docs/live-e2e.md convention:
    - Audience: "Operators installing TIDE into a Kubernetes cluster."
    - Status: "v1.0; ships Helm chart pair (`tide` + `tide-crds`) via OCI registry (ghcr.io). The cloned-repo install path is also documented."
    - Scope of this doc: bulleted list naming the level-2 sections.

    Level-2 sections in this order:

    1. `## Prerequisites` — bulleted list: kubectl >= 1.31, Docker (or compatible: podman, nerdctl), kind v0.31 (for local cluster), Helm 3.16.x (>= 3.8 for OCI). Cite versions matching CLAUDE.md tech stack.

    2. `## Per-OS prerequisite install` — three level-3 subheadings:
       - `### macOS (brew)` — `brew install kubectl helm kind`, point to Docker Desktop or colima for Docker runtime.
       - `### Linux (curl/apt)` — pinned curl downloads for kind (`v0.31.0`), helm (`v3.16.3`), kubectl (`v1.31.0`) matching RESEARCH §"P7.1" pinned versions; apt notes for Debian-family.
       - `### Windows (WSL2)` — note WSL2 is required for Docker compatibility; reuse the Linux instructions inside WSL2.

    3. `## Install order (Pitfall 4 — CRDs first)` — explicit ordering rule. State: "Install `tide-crds` BEFORE `tide`. The main `tide` chart's webhook configs reference CRDs; if CRDs aren't registered, the webhook config templates fail to apply." Then show the OCI install commands (D-X6 lock):
       ```bash
       kind create cluster --name tide
       helm install tide-crds oci://ghcr.io/jsquirrelz/tide-charts/tide-crds --version 1.0.0 -n tide-system --create-namespace
       helm install tide oci://ghcr.io/jsquirrelz/tide-charts/tide --version 1.0.0 -n tide-system
       ```
       Then a level-3 subheading `### Cloned-repo install path` with the alternative: `helm install tide-crds ./charts/tide-crds -n tide-system --create-namespace` then `helm install tide ./charts/tide -n tide-system`.

    4. `## Provider Secret (ANTHROPIC_API_KEY)` — show `kubectl create secret generic` command pattern with `--from-literal=ANTHROPIC_API_KEY=...` in the target namespace (e.g., `tide-sample-medium` for the medium sample). Link to `docs/project-authoring.md` for which fields in Project.Spec reference this Secret.

    5. `## Git credentials Secret` — same shape; link forward to `docs/git-hosts.md` for per-host PAT setup. Show the `kubectl create secret generic git-creds --from-literal=GIT_PAT=...` pattern.

    6. `## First Project apply` — show `kubectl apply -f examples/projects/small/project.yaml`. Note that the small sample uses the $0 stub-subagent and needs no API key. Link forward to `docs/project-authoring.md` for the 3-sample walkthrough.

    7. `## Is TIDE right for me?` (Pitfall 8 mitigation) — two level-3 subheadings:
       - `### Yes, if:` — 3 bullets: (a) "You run K8s and want agentic coding pipelines"; (b) "You coordinate LLM dispatch across multiple developers"; (c) "Your org needs audited LLM cost caps".
       - `### No, if:` — 3 bullets: (a) "Solo dev with a non-K8s workflow"; (b) "Latency-critical apps (TIDE is for batch agentic work)"; (c) "Mission-critical prod with no observability tolerance".

    8. `## Next steps` — forward link `[project-authoring.md](project-authoring.md)`. Also link `[troubleshooting.md](troubleshooting.md)` for common install issues.

    Voice: tight, declarative, em-dash-friendly per CLAUDE.md voice rule. Code blocks fenced with ```bash for shell commands. No marketing language.

    Length target: 150-300 lines.
  </action>
  <verify>
    <automated>test -s docs/INSTALL.md && grep -q "# TIDE Install Guide" docs/INSTALL.md && grep -q "Install order" docs/INSTALL.md && grep -qE "tide-crds.*BEFORE.*tide|tide-crds before|crds first" docs/INSTALL.md && grep -q "macOS" docs/INSTALL.md && grep -q "Linux" docs/INSTALL.md && grep -q "WSL2" docs/INSTALL.md && grep -q "Is TIDE right for me" docs/INSTALL.md && grep -q "ANTHROPIC_API_KEY" docs/INSTALL.md && grep -q "1.0.0" docs/INSTALL.md && grep -q "project-authoring.md" docs/INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/INSTALL.md` returns 0
    - `head -1 docs/INSTALL.md` matches `# TIDE Install Guide`
    - `grep -cE '^## ' docs/INSTALL.md` returns ≥ 7 (one per level-2 section)
    - `grep -cE '^### ' docs/INSTALL.md` returns ≥ 5 (per-OS subsections + "Is TIDE for me?" subsections + cloned-repo)
    - `grep -q "tide-crds.*BEFORE.*tide\|tide-crds before\|crds first" docs/INSTALL.md` (case-insensitive) returns 0 — install order explicit
    - `grep -q "macOS" docs/INSTALL.md` returns 0
    - `grep -q "Linux" docs/INSTALL.md` returns 0
    - `grep -q "WSL2" docs/INSTALL.md` returns 0
    - `grep -q "ANTHROPIC_API_KEY" docs/INSTALL.md` returns 0
    - `grep -q "Is TIDE right for me" docs/INSTALL.md` returns 0
    - `grep -q "oci://ghcr.io/jsquirrelz/tide-charts/tide" docs/INSTALL.md` returns 0
    - `grep -q "--version 1.0.0" docs/INSTALL.md` returns 0
    - `grep -q "project-authoring.md" docs/INSTALL.md` returns 0
    - `grep -q "troubleshooting.md" docs/INSTALL.md` returns 0
    - `wc -l docs/INSTALL.md` outputs between 100 and 400 lines
  </acceptance_criteria>
  <done>docs/INSTALL.md exists with 7 level-2 sections, per-OS subsections, OCI + cloned-repo paths, Pitfall 4 + Pitfall 8 mitigations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New operator → docs/INSTALL.md | INSTALL.md is the trusted on-ramp; if it omits the CRDs-first ordering, operators hit Pitfall 4 failure mode on first install |
| Operator-supplied ANTHROPIC_API_KEY → Secret | INSTALL.md teaches the Secret-creation pattern; misdocumented key creation leaks the key to controller logs or commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | Install order (CRDs first) documentation | mitigate | §"Install order (Pitfall 4 — CRDs first)" is its own level-2 section; acceptance grep enforces presence; Plan 05-15 dry-run executes this order in DinD as live verification |
| T-05-07-02 | Information Disclosure | ANTHROPIC_API_KEY in install instructions | mitigate | Instructions use `kubectl create secret generic ... --from-literal=ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY` pattern; never instructs users to inline raw key in YAML; docs/troubleshooting.md (Plan 05-10) adds a "401 invalid key" recipe |
| T-05-07-03 | Information Disclosure | Git PAT in install instructions | mitigate | Same Secret-via-kubectl pattern; cross-links to docs/git-hosts.md for per-host PAT scope guidance |
| T-05-07-04 | Tampering | Chart version 1.0.0 references | mitigate | Acceptance grep on `--version 1.0.0` ensures INSTALL.md isn't stale relative to Plan 05-05 chart bump |
</threat_model>

<verification>
After Task 1: run `bash hack/scripts/verify-docs-coverage.sh --require-all-files` — exit code MAY still be non-zero if Plans 05-08/09/10 haven't landed yet, but the INSTALL.md row no longer fails.

Plan 05-04's verify-docs-coverage.sh confirms `docs/INSTALL.md` is non-empty after this plan lands.
</verification>

<success_criteria>
- docs/INSTALL.md exists with 7 level-2 sections
- CRDs-first install order explicit (Pitfall 4)
- per-OS prereq install (Pitfall 8 path-of-least-resistance)
- "Is TIDE right for me?" 3+3 bullet section
- Chart version 1.0.0 referenced throughout
- DIST-04 INSTALL.md piece satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-07-SUMMARY.md` documenting INSTALL.md line count, section count, and commit SHA.
</output>
