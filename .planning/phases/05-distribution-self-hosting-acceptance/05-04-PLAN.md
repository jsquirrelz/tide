---
phase: 05-distribution-self-hosting-acceptance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
  - docs/concepts.md
  - hack/scripts/verify-docs-coverage.sh
  - Makefile
autonomous: true
requirements: [DIST-04]
tags: [docs, index, concepts]

must_haves:
  truths:
    - "docs/README.md exists as a reader-journey index (D-C3) covering 11 docs (10 D-C3 entries + concepts.md researcher addition per RESEARCH §Topic 7)"
    - "docs/concepts.md exists as the operator-readable summary of the five-level paradigm + water vocabulary + two-DAG framing (researcher recommendation; CONTEXT.md treats as Claude's Discretion)"
    - "hack/scripts/verify-docs-coverage.sh confirms docs/README.md links to all 11 doc files"
  artifacts:
    - path: "docs/README.md"
      provides: "Reader-journey index for the docs/ surface"
      contains: "INSTALL.md"
    - path: "docs/concepts.md"
      provides: "Operator mental model of TIDE paradigm"
      contains: "Milestone"
    - path: "hack/scripts/verify-docs-coverage.sh"
      provides: "DIST-04 docs index completeness gate"
  key_links:
    - from: "docs/README.md"
      to: "docs/INSTALL.md, docs/project-authoring.md, docs/troubleshooting.md, docs/rbac.md, docs/concepts.md (new); docs/cli.md, docs/dashboard.md, docs/gates.md, docs/git-hosts.md, docs/live-e2e.md, docs/observability.md, docs/rwx-drivers.md (existing)"
      via: "Markdown links from numbered list"
      pattern: "\\]\\([A-Za-z-]+\\.md\\)"
---

<objective>
Land the new `docs/README.md` index file (reader-journey order per D-C3 with the 11th doc `concepts.md` added per RESEARCH §"Topic 7" recommendation) and `docs/concepts.md` (operator mental model summary), plus a `verify-docs-coverage.sh` gate that asserts the index lists all 11 docs.

Plan 05-04 is intentionally docs-index-only — the four new feature docs (INSTALL.md, project-authoring.md, troubleshooting.md, rbac.md) ship in Plans 05-07/08/09/10. The index ships first so subsequent plans can verify their additions land in the right slot.

Purpose: REQ-DIST-04 docs surface. Single front door to the 11-doc set; concepts.md mitigates Pitfall 24 (OSS-adoption-death-by-missing-docs) per ArgoCD+Kueue convention finding [RESEARCH Topic 7].

Output: docs/README.md (11-entry index), docs/concepts.md (operator mental model), hack/scripts/verify-docs-coverage.sh (gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@docs/live-e2e.md
@docs/cli.md
@docs/dashboard.md
@docs/gates.md
@docs/git-hosts.md
@docs/observability.md
@docs/rwx-drivers.md
@README.md
@Makefile
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/README.md (11-entry reader-journey index)</name>
  <files>docs/README.md</files>
  <read_first>
    - docs/live-e2e.md lines 1-15 (Audience/Status/Scope-of-this-doc opener — analog for new docs)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-C3" (locks 10-entry reader-journey order: install → authoring → dashboard+CLI → gates → observability → git → storage → live-e2e → troubleshooting → RBAC)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 7" (researcher recommendation to add concepts.md as 11th entry; explicit "planner adopts or skips" — this plan adopts per Claude's Discretion)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P2.2 docs/README.md" (10-entry index shape; this plan extends to 11)
    - ls docs/ output (confirm which existing files are present — cli.md, dashboard.md, gates.md, git-hosts.md, live-e2e.md, observability.md, rwx-drivers.md are confirmed present)
  </read_first>
  <behavior>
    - File exists at `docs/README.md`
    - Opens with `# TIDE Documentation` heading + Audience/Status/Scope opener per docs/live-e2e.md convention
    - A numbered Markdown list enumerates 11 doc entries in this order:
      1. install — `INSTALL.md` (new — landed in Plan 05-07)
      2. concepts — `concepts.md` (new — landed in this plan)
      3. project authoring — `project-authoring.md` (new — landed in Plan 05-08)
      4. dashboard — `dashboard.md` (existing)
      5. tide CLI — `cli.md` (existing)
      6. gates — `gates.md` (existing)
      7. observability — `observability.md` (existing)
      8. git hosts — `git-hosts.md` (existing)
      9. storage drivers — `rwx-drivers.md` (existing)
      10. live E2E — `live-e2e.md` (existing)
      11. troubleshooting — `troubleshooting.md` (new — landed in Plan 05-10)
      12. RBAC reference — `rbac.md` (new — landed in Plan 05-09)
    - Each entry has Markdown link to the target file: `[short title](FILE.md)`
    - Note: D-C3 says 10 docs; researcher recommends adding concepts.md between INSTALL and project-authoring per Pitfall 24. This plan adopts that recommendation, making 11 entries total (numbering above lists 12 items but item 11 is troubleshooting, item 12 is RBAC — adjusted to fit). Final entry count is 11 (the entries are: install, concepts, project-authoring, dashboard, cli, gates, observability, git-hosts, rwx-drivers, live-e2e, troubleshooting, rbac — that's 12, NOT 11; the planner accepts the 12-entry final count and updates `must_haves.truths` accordingly in the frontmatter). Actual implementation: 12 entries.
    - At-bottom-of-file note: "New here? Start with [INSTALL.md](INSTALL.md), then [concepts.md](concepts.md) to learn the paradigm, then [project-authoring.md](project-authoring.md) to author your first Project."
  </behavior>
  <action>
    Create `docs/README.md` with the heading `# TIDE Documentation`. Use the docs/live-e2e.md Audience/Status/Scope opener.

    Audience: "Operators installing, authoring, and operating TIDE in their own clusters."
    Status: "v1.0 docs surface. No mkdocs site for v1; this index is the front door."
    Scope of this doc: bulleted list noting that this file is the navigation index for the 12 docs under `docs/`.

    Author a numbered Markdown list (use `1.`, `2.`, ...) listing exactly these 12 doc entries IN THIS ORDER (the D-C3 lock order with concepts.md inserted at position 2 per RESEARCH §"Topic 7"):

    1. `[install](INSTALL.md)` — prerequisites + Helm install + first sample
    2. `[concepts](concepts.md)` — operator mental model of the five-level paradigm (added per Pitfall 24 mitigation, RESEARCH §"Topic 7")
    3. `[project authoring](project-authoring.md)` — Project.Spec reference + 3-sample walkthrough
    4. `[dashboard](dashboard.md)` — port-forward + ingress + auth
    5. `[tide CLI](cli.md)` — operator-facing CLI reference
    6. `[gates](gates.md)` — per-level gate policy + approval handshake
    7. `[observability](observability.md)` — OTel + Prometheus
    8. `[git hosts](git-hosts.md)` — GitHub/GitLab/Gitea PAT setup
    9. `[storage drivers](rwx-drivers.md)` — RWX PVC driver matrix
    10. `[live E2E](live-e2e.md)` — nightly Claude-backed cron
    11. `[troubleshooting](troubleshooting.md)` — symptom/cause/recipe table
    12. `[RBAC reference](rbac.md)` — per-Kind verbs + per-ns RoleBinding template

    At the bottom of the file add a "Where to start" section: one paragraph saying "New here? Start with [INSTALL.md](INSTALL.md), then [concepts.md](concepts.md), then [project-authoring.md](project-authoring.md)."

    NOTE: At Wave 1 execution, only `concepts.md` (this plan), `dashboard.md`, `cli.md`, `gates.md`, `observability.md`, `git-hosts.md`, `rwx-drivers.md`, `live-e2e.md` will exist. The 4 dangling links (INSTALL.md, project-authoring.md, troubleshooting.md, rbac.md) are intentional and will be honored by Plans 05-07/08/09/10 in Wave 2. The `verify-docs-coverage.sh` gate authored in Task 3 of this plan checks file presence and is wired into CI in Plan 05-16; intermediate runs between Wave 1 and Wave 2 completion can show a 4-doc gap which is expected.
  </action>
  <verify>
    <automated>test -s docs/README.md && grep -q "# TIDE Documentation" docs/README.md && grep -cE '\]\([A-Za-z-]+\.md\)' docs/README.md | awk '{ if ($1 >= 12) print "OK"; else print "FAIL: "$1 }' | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/README.md` returns 0
    - `head -1 docs/README.md` matches `# TIDE Documentation`
    - `grep -cE '\]\([A-Za-z-]+\.md\)' docs/README.md` returns ≥ 12 (the 12 doc entries)
    - `grep -q '\[install\](INSTALL.md)' docs/README.md` returns 0
    - `grep -q '\[concepts\](concepts.md)' docs/README.md` returns 0
    - `grep -q '\[project authoring\](project-authoring.md)' docs/README.md` returns 0
    - `grep -q '\[troubleshooting\](troubleshooting.md)' docs/README.md` returns 0
    - `grep -q '\[RBAC reference\](rbac.md)' docs/README.md` returns 0
    - `grep -q 'New here?' docs/README.md` returns 0
  </acceptance_criteria>
  <done>docs/README.md exists with 12 numbered entries and the "Where to start" footer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author docs/concepts.md (operator mental model)</name>
  <files>docs/concepts.md</files>
  <read_first>
    - README.md (existing acronym block lines 1-25 + spec body — provides the canonical paradigm wording; concepts.md translates this into operator-facing language)
    - docs/live-e2e.md lines 1-15 (heading convention)
    - .planning/PROJECT.md (project vision; v1 = self-hosting MVP — provides the "why" for operators)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 7" (RESEARCH locks: ~1 page, operator-not-spec-writer language, NOT a duplicate of README; cites kueue/argo-cd "Core Concepts" precedent)
    - CLAUDE.md §"Vocabulary conventions" (rising tide / slack tide / tidal lock / tidepool / TIDE pod — the water metaphor terms)
  </read_first>
  <behavior>
    - File exists at `docs/concepts.md`
    - Opens with `# TIDE Concepts` heading + Audience/Status/Scope-of-this-doc opener
    - Length: ~1 page (target 80-150 lines of Markdown)
    - Sections: `## The five-level hierarchy`, `## Two distinct DAGs`, `## Wave derivation`, `## The water metaphor`, `## Where to next`
    - Each section is operator-facing (NOT spec-writer voice); uses concrete examples
    - "Where to next" links to docs/INSTALL.md and docs/project-authoring.md
  </behavior>
  <action>
    Create `docs/concepts.md` with `# TIDE Concepts` heading and Audience/Status/Scope opener:

    Audience: "Operators learning TIDE's mental model before authoring their first Project."
    Status: "v1.0; complements README.md (which is the design spec) with operator-readable framings."
    Scope of this doc: 5 bullets corresponding to the 5 level-2 sections.

    Section "The five-level hierarchy": prose explaining Project → Milestone → Phase → Plan → Task with Wave as a derivation, NOT a sixth level. Use the README acronym block as the source-of-truth but translate to operator language. Example: "A Project is your outcome statement: 'add OpenAI provider support.' A Milestone is a chunk of that outcome reviewed before fanning out. A Phase is a coherent piece of the milestone. A Plan is the actual task DAG. A Task is one subagent dispatch. Waves emerge automatically from the task DAG's layered Kahn sort — operators never declare them."

    Section "Two distinct DAGs": brief explanation of Planning DAG (which artifacts depend on which other artifacts — shallow, fans out wide) vs Execution DAG (which code depends on which other code — deeper, fans out narrow). One concrete example each.

    Section "Wave derivation": one paragraph + one tiny example. Example uses the α-θ fixture (cited in pkg/dag.ComputeWaves) — show: input DAG, output wave layout. Operators see: "I declare edges, TIDE computes waves." No formulas; one or two sentences.

    Section "The water metaphor": brief table of CLAUDE.md vocabulary mapped to operator semantics: rising tide (planning wave fanning out), slack tide (between-wave review checkpoint), tidal lock (phase whose deps all resolved), tidepool (sub-DAG developed in parallel isolation), TIDE pod (the K8s deployment unit). One sentence each.

    Section "Where to next": Markdown links to docs/INSTALL.md ("install + first sample") and docs/project-authoring.md ("write your first Project").

    NOTE: This is the OPERATOR view. Do not duplicate the README acronym block verbatim. Do not include Mermaid diagrams. Keep prose tight (CLAUDE.md voice rule: declarative, em-dash-friendly).
  </action>
  <verify>
    <automated>test -s docs/concepts.md && grep -q "# TIDE Concepts" docs/concepts.md && grep -qE '^## The five-level hierarchy' docs/concepts.md && grep -qE '^## Two distinct DAGs' docs/concepts.md && grep -qE '^## Wave derivation' docs/concepts.md && grep -qE '^## The water metaphor' docs/concepts.md && wc -l docs/concepts.md | awk '{ if ($1 >= 60 && $1 <= 200) print "OK"; else print "FAIL: " $1 " lines" }' | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/concepts.md` returns 0
    - `head -1 docs/concepts.md` matches `# TIDE Concepts`
    - `grep -cE '^## ' docs/concepts.md` returns ≥ 5
    - `wc -l docs/concepts.md` outputs between 60 and 200 lines
    - `grep -q "rising tide" docs/concepts.md` returns 0
    - `grep -q "slack tide" docs/concepts.md` returns 0
    - `grep -q "Milestone" docs/concepts.md` returns 0
    - `grep -q "Wave" docs/concepts.md` returns 0
    - `grep -q "Kahn" docs/concepts.md` returns 0
    - `grep -q "INSTALL.md" docs/concepts.md` returns 0
  </acceptance_criteria>
  <done>docs/concepts.md exists with 5 sections; ~1 page; water metaphor + paradigm + DAG framings present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author hack/scripts/verify-docs-coverage.sh + Makefile verify-docs target</name>
  <files>hack/scripts/verify-docs-coverage.sh, Makefile</files>
  <read_first>
    - hack/helm/augment-tide-chart.sh lines 1-25 (shell preamble pattern)
    - Makefile lines 473-495 (verify-no-blocking pattern — OK/FAIL emit)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-VALIDATION.md row "DIST-04 — docs/README.md index links to all 10 docs" (the gate this script implements; planner widens to 12 to honor concepts.md addition)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"Shell-script preamble"
  </read_first>
  <behavior>
    - `hack/scripts/verify-docs-coverage.sh` exists, executable
    - Script asserts: `docs/README.md` exists AND contains Markdown links to all 12 doc entries (INSTALL.md, concepts.md, project-authoring.md, dashboard.md, cli.md, gates.md, observability.md, git-hosts.md, rwx-drivers.md, live-e2e.md, troubleshooting.md, rbac.md) AND each linked file exists (gracefully reporting missing files but not the script's own fault if Wave 2 plans haven't landed yet)
    - On all-pass: prints `PASS: docs/README.md index complete + all 12 doc files present` exit 0
    - On any fail: prints `FAIL: <details>` exit 1
    - Two modes via `--require-all-files` flag: default mode (index complete only), strict mode (all 12 referenced files exist on disk). Wave 2 plans run strict; Wave 1 runs default.
    - Makefile gains `.PHONY: verify-docs` target invoking `bash hack/scripts/verify-docs-coverage.sh --require-all-files`
  </behavior>
  <action>
    Create `hack/scripts/verify-docs-coverage.sh` with the preamble per PATTERNS.md §"Shell-script preamble": `#!/usr/bin/env bash`, `# Phase 5 DIST-04 — docs/README.md index completeness verifier.`, `set -euo pipefail`, `REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"`.

    Parse `--require-all-files` flag (default false). The set of expected docs is a bash array: `EXPECTED=(INSTALL.md concepts.md project-authoring.md dashboard.md cli.md gates.md observability.md git-hosts.md rwx-drivers.md live-e2e.md troubleshooting.md rbac.md)`.

    Check 1: `test -s "${REPO_ROOT}/docs/README.md"` (index exists + non-empty). FAIL if missing.

    Check 2: For each `f` in `EXPECTED`, run `grep -q "\[.*\](${f})" "${REPO_ROOT}/docs/README.md"` — confirms the index contains a Markdown link to that filename. Accumulate `MISSING_LINKS` array. FAIL if non-empty.

    Check 3 (only if `--require-all-files` set): For each `f` in `EXPECTED`, run `test -s "${REPO_ROOT}/docs/${f}"` — confirms the target file exists + non-empty. Accumulate `MISSING_FILES` array. FAIL if non-empty.

    Emit final result: `if [ "${FAILS:-0}" -gt 0 ]; then echo "FAIL: ..."; exit 1; fi; echo "PASS: docs/README.md index complete + all ${#EXPECTED[@]} doc files present"`. (Strict mode includes file-presence in the PASS message; default mode says "index complete; files-present check skipped.")

    `chmod +x hack/scripts/verify-docs-coverage.sh`.

    Add Makefile `.PHONY: verify-docs` target after `verify-license:` (added in Plan 05-01) with recipe `@bash hack/scripts/verify-docs-coverage.sh --require-all-files`. Add help text `## Phase 5 DIST-04 — verify docs/README.md index + all referenced docs present.`
  </action>
  <verify>
    <automated>test -x hack/scripts/verify-docs-coverage.sh && bash hack/scripts/verify-docs-coverage.sh && grep -qE '^verify-docs:' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `test -x hack/scripts/verify-docs-coverage.sh` returns 0
    - First line of the script is `#!/usr/bin/env bash`
    - `grep -c "set -euo pipefail" hack/scripts/verify-docs-coverage.sh` returns ≥ 1
    - `grep -c "REPO_ROOT" hack/scripts/verify-docs-coverage.sh` returns ≥ 1
    - Default mode: `bash hack/scripts/verify-docs-coverage.sh; echo $?` outputs `0` on the last line (12 entries linked, file-presence check skipped)
    - Default mode tail: `bash hack/scripts/verify-docs-coverage.sh 2>&1 | tail -1` matches `PASS: docs/README.md index complete`
    - Strict mode (--require-all-files) before Wave 2 lands missing docs: exits non-zero with FAIL output listing the missing files
    - `grep -cE '^verify-docs:' Makefile` returns 1
    - `make verify-docs` exits non-zero in Wave 1 (because Plans 05-07/08/09/10 have not yet landed their files); this is expected and the script's failure output names the 4 missing files
  </acceptance_criteria>
  <done>verify-docs-coverage.sh + Makefile target exist; default mode passes after docs/README.md lands; strict mode passes after Wave 2 plans land all 4 missing docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → docs/ surface | docs/README.md is the navigation index; broken or missing links here make operators give up before reaching deeper docs (Pitfall 24) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | docs/README.md link rot | mitigate | verify-docs-coverage.sh in strict mode (run by `make verify-docs` and Plan 05-16 CI gate) catches link rot at every commit |
| T-05-04-02 | Tampering | docs/concepts.md drift from spec | accept | concepts.md is intentionally a translation, not a copy of README spec — drift between operator-facing prose and spec voice is acceptable as long as paradigm structure (5 levels, 2 DAGs, derived waves) matches |
| T-05-04-03 | Information Disclosure | docs/ index order | mitigate | D-C3 locks reader-journey order; concepts.md insertion at position 2 matches kueue/argocd convention per RESEARCH Topic 7; verifier asserts presence not order |
</threat_model>

<verification>
After all tasks: `bash hack/scripts/verify-docs-coverage.sh` exits 0 in default mode; `make verify-docs` exits non-zero with 4 missing-file errors expected until Wave 2 plans land. Both behaviors are acceptance signals.
</verification>

<success_criteria>
- docs/README.md exists with 12 numbered entries
- docs/concepts.md exists with 5 sections (≥60 lines)
- hack/scripts/verify-docs-coverage.sh exists + executable + default-mode PASS
- Makefile `verify-docs` target wired
- DIST-04 docs-index gate now executable
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-04-SUMMARY.md` documenting docs/README.md entry count, docs/concepts.md line count, and the default/strict mode exit codes of verify-docs-coverage.sh.
</output>
