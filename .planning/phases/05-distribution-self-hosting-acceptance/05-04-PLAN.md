---
phase: 05-distribution-self-hosting-acceptance
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/README.md
  - docs/concepts.md
  - hack/scripts/verify-docs-coverage.sh
  - Makefile
autonomous: true
requirements: [DIST-04]
tags: [docs, index, concepts]

must_haves:
  truths:
    - "docs/README.md exists as a reader-journey index (D-C3) covering 11 entries (per CONTEXT.md D-C3 revision 2026-05-22, commit e476c68; entry #4 is co-located dashboard + CLI = 12 files total)"
    - "docs/concepts.md exists as the operator-readable summary of the five-level paradigm + water vocabulary + two-DAG framing (D-C3 entry #2 per user revision 2026-05-22)"
    - "hack/scripts/verify-docs-coverage.sh confirms docs/README.md lists 11 numbered entries (12 file links); --non-strict mode checks index + concepts.md only (Wave 2 gate); --strict mode requires all 12 referenced files present (Wave 2 closeout + CI)"
    - "Plan 05-04 depends_on Plan 05-01 because both edit Makefile (HIGH-5 file-overlap rule); landing order: 05-01 verify-license target first, then 05-04 verify-docs target appended as sibling stanza"
  artifacts:
    - path: "docs/README.md"
      provides: "Reader-journey index for the docs/ surface (11 numbered entries per D-C3)"
      contains: "INSTALL.md"
    - path: "docs/concepts.md"
      provides: "Operator mental model of TIDE paradigm (D-C3 entry #2)"
      contains: "Milestone"
    - path: "hack/scripts/verify-docs-coverage.sh"
      provides: "DIST-04 docs index completeness gate with --non-strict + --strict modes"
    - path: "Makefile"
      provides: "Makefile gains verify-docs target"
      contains: "verify-docs:"
  key_links:
    - from: "docs/README.md"
      to: "docs/INSTALL.md, docs/concepts.md, docs/project-authoring.md, docs/dashboard.md + docs/cli.md (entry #4 co-located), docs/gates.md, docs/observability.md, docs/git-hosts.md, docs/rwx-drivers.md, docs/live-e2e.md, docs/troubleshooting.md, docs/rbac.md"
      via: "Markdown links from numbered list"
      pattern: "\\]\\([A-Za-z-]+\\.md\\)"
---

<objective>
Land the new `docs/README.md` index file (reader-journey order per D-C3, 11 numbered entries covering 12 doc files — entry #4 is the co-located dashboard + CLI pair per CONTEXT.md D-C3 revision 2026-05-22) and `docs/concepts.md` (operator mental model summary at slot #2 per user-accepted Open Question #1 resolution), plus a `verify-docs-coverage.sh` gate that asserts the index lists all 11 entries (12 files) with `--strict` / `--non-strict` modes.

Plan 05-04 is intentionally docs-index-only — the four new feature docs (INSTALL.md, project-authoring.md, troubleshooting.md, rbac.md) ship in Plans 05-07/08/09/10. The index ships first so subsequent plans can verify their additions land in the right slot.

Purpose: REQ-DIST-04 docs surface. Single front door to the 11-entry / 12-file doc set; concepts.md mitigates Pitfall 24 (OSS-adoption-death-by-missing-docs) per ArgoCD+Kueue convention finding [RESEARCH Topic 7]; user accepted via discuss-phase HIGH-4 revision 2026-05-22.

Output: docs/README.md (11-entry numbered index), docs/concepts.md (operator mental model), hack/scripts/verify-docs-coverage.sh (gate with `--strict` / `--non-strict` modes), Makefile `verify-docs` target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@docs/live-e2e.md
@docs/cli.md
@docs/dashboard.md
@docs/gates.md
@docs/git-hosts.md
@docs/observability.md
@docs/rwx-drivers.md
@README.md
@Makefile
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/README.md (11 numbered entries — D-C3 reader-journey index)</name>
  <files>docs/README.md</files>
  <read_first>
    - docs/live-e2e.md lines 1-15 (Audience/Status/Scope opener — analog for new docs)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-C3" (REVISED 2026-05-22 commit e476c68: 11 numbered entries; entry #2 = concepts.md (NEW slot per user revision); entry #4 = dashboard + CLI co-located)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Open Questions (RESOLVED)" Q1 (researcher recommendation accepted; concepts.md adopted as entry #2)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P2.2 docs/README.md" (index shape; this plan extends to 11 entries per the D-C3 revision)
    - ls docs/ output (confirm which existing files are present — cli.md, dashboard.md, gates.md, git-hosts.md, live-e2e.md, observability.md, rwx-drivers.md are confirmed present)
  </read_first>
  <behavior>
    - File exists at `docs/README.md`
    - Opens with `# TIDE Documentation` heading + Audience/Status/Scope opener per docs/live-e2e.md convention
    - A numbered Markdown list enumerates EXACTLY 11 entries in the D-C3 reader-journey order
    - Entry #4 is the co-located "dashboard + CLI" pair — both Markdown-linked on a single numbered line per D-C3 revision
    - At-bottom-of-file "New here?" note links to INSTALL.md → concepts.md → project-authoring.md as the recommended on-ramp
  </behavior>
  <action>
    Create `docs/README.md` with the heading `# TIDE Documentation`. Use the docs/live-e2e.md Audience/Status/Scope opener.

    Audience: "Operators installing, authoring, and operating TIDE in their own clusters."
    Status: "v1.0 docs surface. No mkdocs site for v1; this index is the front door."
    Scope of this doc: bulleted list noting that this file is the navigation index for the 11 entries (12 files; entry #4 is co-located) under `docs/`.

    Author a numbered Markdown list (use `1.`, `2.`, ...) listing exactly these 11 entries IN THIS ORDER (the D-C3 lock order per CONTEXT.md revision 2026-05-22, commit e476c68):

    1. `[install](INSTALL.md)` — prerequisites + Helm install + first sample
    2. `[concepts](concepts.md)` — operator mental model of the five-level paradigm (NEW slot — added per Pitfall 24 mitigation, RESEARCH §"Open Questions (RESOLVED)" Q1, user-accepted 2026-05-22)
    3. `[project authoring](project-authoring.md)` — Project.Spec reference + 3-sample walkthrough
    4. `[dashboard](dashboard.md)` + `[tide CLI](cli.md)` — operator UIs (co-located entry #4 per D-C3: dashboard is the visual surface, `tide` CLI is the headless equivalent)
    5. `[gates](gates.md)` — per-level gate policy + approval handshake
    6. `[observability](observability.md)` — OTel + Prometheus
    7. `[git hosts](git-hosts.md)` — GitHub/GitLab/Gitea PAT setup
    8. `[storage drivers](rwx-drivers.md)` — RWX PVC driver matrix
    9. `[live E2E](live-e2e.md)` — nightly Claude-backed cron
    10. `[troubleshooting](troubleshooting.md)` — symptom/cause/recipe table
    11. `[RBAC reference](rbac.md)` — per-Kind verbs + per-ns RoleBinding template

    At the bottom of the file add a "Where to start" section: one paragraph saying "New here? Start with [INSTALL.md](INSTALL.md), then [concepts.md](concepts.md), then [project-authoring.md](project-authoring.md)."

    NOTE: At Wave 2 execution time, only `concepts.md` (this plan), `dashboard.md`, `cli.md`, `gates.md`, `observability.md`, `git-hosts.md`, `rwx-drivers.md`, `live-e2e.md` will exist. The 4 dangling links (INSTALL.md, project-authoring.md, troubleshooting.md, rbac.md) are intentional and will be honored by Plans 05-07/08/09/10 in Wave 2. The `verify-docs-coverage.sh` gate authored in Task 3 has a `--non-strict` mode for the Wave 1/2 partial state (default) and a `--strict` mode (Wave 2 closeout / CI gate) per LOW-15 revision.
  </action>
  <verify>
    <automated>test -s docs/README.md && grep -q "# TIDE Documentation" docs/README.md && grep -cE '\]\([A-Za-z-]+\.md\)' docs/README.md | awk '{ if ($1 >= 12) print "OK"; else print "FAIL: "$1 }' | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/README.md` returns 0
    - `head -1 docs/README.md` matches `# TIDE Documentation`
    - `grep -cE '\]\([A-Za-z-]+\.md\)' docs/README.md` returns ≥ 12 (11 entries; entry #4 is co-located dashboard + cli = 2 links on one numbered line)
    - `grep -cE '^[0-9]+\. ' docs/README.md` returns 11 (exactly 11 numbered entries per D-C3 revision)
    - `grep -q '\[install\](INSTALL.md)' docs/README.md` returns 0
    - `grep -q '\[concepts\](concepts.md)' docs/README.md` returns 0
    - `grep -q '\[project authoring\](project-authoring.md)' docs/README.md` returns 0
    - `grep -q '\[dashboard\](dashboard.md)' docs/README.md` returns 0
    - `grep -q '\[tide CLI\](cli.md)' docs/README.md` returns 0
    - `grep -q '\[troubleshooting\](troubleshooting.md)' docs/README.md` returns 0
    - `grep -q '\[RBAC reference\](rbac.md)' docs/README.md` returns 0
    - `grep -q 'New here?' docs/README.md` returns 0
  </acceptance_criteria>
  <done>docs/README.md exists with exactly 11 numbered entries (12 file links — entry #4 co-located) and the "Where to start" footer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author docs/concepts.md (operator mental model — D-C3 entry #2)</name>
  <files>docs/concepts.md</files>
  <read_first>
    - README.md (existing acronym block lines 1-25 + spec body — provides the canonical paradigm wording; concepts.md translates this into operator-facing language)
    - docs/live-e2e.md lines 1-15 (heading convention)
    - .planning/PROJECT.md (project vision; v1 = self-hosting MVP — provides the "why" for operators)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Open Questions (RESOLVED)" Q1 (RESEARCH locks: ~1 page, operator-not-spec-writer language, NOT a duplicate of README; cites kueue/argo-cd "Core Concepts" precedent)
    - CLAUDE.md §"Vocabulary conventions" (rising tide / slack tide / tidal lock / tidepool / TIDE pod — the water metaphor terms)
  </read_first>
  <behavior>
    - File exists at `docs/concepts.md`
    - Opens with `# TIDE Concepts` heading + Audience/Status/Scope-of-this-doc opener
    - Length: ~1 page (target 80-150 lines of Markdown)
    - Sections: `## The five-level hierarchy`, `## Two distinct DAGs`, `## Wave derivation`, `## The water metaphor`, `## Where to next`
    - Each section is operator-facing (NOT spec-writer voice); uses concrete examples
    - "Where to next" links to docs/INSTALL.md and docs/project-authoring.md
  </behavior>
  <action>
    Create `docs/concepts.md` with `# TIDE Concepts` heading and Audience/Status/Scope opener:

    Audience: "Operators learning TIDE's mental model before authoring their first Project."
    Status: "v1.0; complements README.md (which is the design spec) with operator-readable framings."
    Scope of this doc: 5 bullets corresponding to the 5 level-2 sections.

    Section "The five-level hierarchy": prose explaining Project → Milestone → Phase → Plan → Task with Wave as a derivation, NOT a sixth level. Use the README acronym block as the source-of-truth but translate to operator language. Example: "A Project is your outcome statement: 'add OpenAI provider support.' A Milestone is a chunk of that outcome reviewed before fanning out. A Phase is a coherent piece of the milestone. A Plan is the actual task DAG. A Task is one subagent dispatch. Waves emerge automatically from the task DAG's layered Kahn sort — operators never declare them."

    Section "Two distinct DAGs": brief explanation of Planning DAG (which artifacts depend on which other artifacts — shallow, fans out wide) vs Execution DAG (which code depends on which other code — deeper, fans out narrow). One concrete example each.

    Section "Wave derivation": one paragraph + one tiny example. Example uses the α-θ fixture (cited in pkg/dag.ComputeWaves) — show: input DAG, output wave layout. Operators see: "I declare edges, TIDE computes waves." No formulas; one or two sentences.

    Section "The water metaphor": brief table of CLAUDE.md vocabulary mapped to operator semantics: rising tide (planning wave fanning out), slack tide (between-wave review checkpoint), tidal lock (phase whose deps all resolved), tidepool (sub-DAG developed in parallel isolation), TIDE pod (the K8s deployment unit). One sentence each.

    Section "Where to next": Markdown links to docs/INSTALL.md ("install + first sample") and docs/project-authoring.md ("write your first Project").

    NOTE: This is the OPERATOR view. Do not duplicate the README acronym block verbatim. Do not include Mermaid diagrams. Keep prose tight (CLAUDE.md voice rule: declarative, em-dash-friendly).
  </action>
  <verify>
    <automated>test -s docs/concepts.md && grep -q "# TIDE Concepts" docs/concepts.md && grep -qE '^## The five-level hierarchy' docs/concepts.md && grep -qE '^## Two distinct DAGs' docs/concepts.md && grep -qE '^## Wave derivation' docs/concepts.md && grep -qE '^## The water metaphor' docs/concepts.md && wc -l docs/concepts.md | awk '{ if ($1 >= 60 && $1 <= 200) print "OK"; else print "FAIL: " $1 " lines" }' | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -s docs/concepts.md` returns 0
    - `head -1 docs/concepts.md` matches `# TIDE Concepts`
    - `grep -cE '^## ' docs/concepts.md` returns ≥ 5
    - `wc -l docs/concepts.md` outputs between 60 and 200 lines
    - `grep -q "rising tide" docs/concepts.md` returns 0
    - `grep -q "slack tide" docs/concepts.md` returns 0
    - `grep -q "Milestone" docs/concepts.md` returns 0
    - `grep -q "Wave" docs/concepts.md` returns 0
    - `grep -q "Kahn" docs/concepts.md` returns 0
    - `grep -q "INSTALL.md" docs/concepts.md` returns 0
  </acceptance_criteria>
  <done>docs/concepts.md exists with 5 sections; ~1 page; water metaphor + paradigm + DAG framings present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author hack/scripts/verify-docs-coverage.sh + Makefile verify-docs target</name>
  <files>hack/scripts/verify-docs-coverage.sh, Makefile</files>
  <read_first>
    - hack/helm/augment-tide-chart.sh lines 1-25 (shell preamble pattern)
    - Makefile lines 473-495 (verify-no-blocking pattern — OK/FAIL emit)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-VALIDATION.md row "DIST-04 — docs/README.md index links to all 11 docs (12 files — entry #4 co-located)" (the gate this script implements; LOW-15 revision adds non-strict default for Wave 2 partial state)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"Shell-script preamble"
    - Makefile (after Plan 05-01's `verify-license:` target lands per HIGH-5 dep ordering; this plan's `verify-docs:` target is appended as a sibling stanza)
  </read_first>
  <behavior>
    - `hack/scripts/verify-docs-coverage.sh` exists, executable
    - Default mode (`--non-strict`): asserts that `docs/README.md` exists AND contains Markdown links to `concepts.md` (the entry #2 doc landed in this plan); other doc-file presence checks are skipped. This is the Wave 1/2 gate when not all referenced docs exist yet.
    - Strict mode (`--strict`): asserts that `docs/README.md` exists AND contains Markdown links to all 12 file references (INSTALL.md, concepts.md, project-authoring.md, dashboard.md, cli.md, gates.md, observability.md, git-hosts.md, rwx-drivers.md, live-e2e.md, troubleshooting.md, rbac.md) AND every file referenced exists on disk + is non-empty. This is the Wave 2 closeout / CI gate after Plans 05-07/08/09/10 land their docs.
    - On all-pass: prints `PASS: docs/README.md index complete + N docs present` (where N = 2 in non-strict mode, 12 in strict mode); exit 0.
    - On any fail: prints `FAIL: <details>` (listing the missing entries / files); exit 1.
    - Makefile gains `.PHONY: verify-docs` target invoking `bash hack/scripts/verify-docs-coverage.sh --strict` (Plan 05-04 makes Wave 2 closeout the default Makefile gate per LOW-15 revision; CI calls strict mode).
  </behavior>
  <action>
    Create `hack/scripts/verify-docs-coverage.sh` with the preamble per PATTERNS.md §"Shell-script preamble": `#!/usr/bin/env bash`, `# Phase 5 DIST-04 — docs/README.md index completeness verifier (--non-strict default; --strict for Wave 2 closeout + CI).`, `set -euo pipefail`, `REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"`.

    Parse mode flag: `MODE="${1:---non-strict}"` (default `--non-strict`). Accept `--strict` or `--non-strict` only; on any other arg emit `FAIL: unrecognized arg "$1"` and exit 2.

    The set of expected entries is a bash array (the 12 file references corresponding to the 11 D-C3 entries, with entry #4 expanded into dashboard + cli): `EXPECTED=(INSTALL.md concepts.md project-authoring.md dashboard.md cli.md gates.md observability.md git-hosts.md rwx-drivers.md live-e2e.md troubleshooting.md rbac.md)`. (Twelve elements; the index lists 11 numbered entries because dashboard + cli share line #4 per D-C3.)

    Check 1 (both modes): `test -s "${REPO_ROOT}/docs/README.md"` (index exists + non-empty). FAIL if missing.

    Check 2 (both modes): For `concepts.md` specifically (the Wave 1/2 minimal gate), `grep -q "\[.*\](concepts.md)" "${REPO_ROOT}/docs/README.md"`. FAIL if missing.

    Check 3 (`--strict` mode only): For each `f` in `EXPECTED`, run `grep -q "\[.*\](${f})" "${REPO_ROOT}/docs/README.md"` — confirms the index contains a Markdown link to that filename. Accumulate `MISSING_LINKS` array. FAIL if non-empty.

    Check 4 (`--strict` mode only): For each `f` in `EXPECTED`, run `test -s "${REPO_ROOT}/docs/${f}"` — confirms the target file exists + non-empty. Accumulate `MISSING_FILES` array. FAIL if non-empty.

    Emit final result based on mode:
    - Non-strict default: `echo "PASS: docs/README.md index present + concepts.md linked (--non-strict mode; full coverage check requires --strict)"; exit 0`
    - Strict: `echo "PASS: docs/README.md index complete + all ${#EXPECTED[@]} doc files present (--strict mode)"; exit 0`
    - Either mode FAIL: `echo "FAIL: ..."; exit 1`

    `chmod +x hack/scripts/verify-docs-coverage.sh`.

    Add Makefile `.PHONY: verify-docs` target appended as sibling stanza after `verify-license:` (Plan 05-01 adds verify-license per HIGH-5 ordering). Recipe: `@bash hack/scripts/verify-docs-coverage.sh --strict`. Help text: `## Phase 5 DIST-04 — verify docs/README.md index + all referenced docs present.`
  </action>
  <verify>
    <automated>test -x hack/scripts/verify-docs-coverage.sh && bash hack/scripts/verify-docs-coverage.sh && grep -qE '^verify-docs:' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `test -x hack/scripts/verify-docs-coverage.sh` returns 0
    - First line of the script is `#!/usr/bin/env bash`
    - `grep -c "set -euo pipefail" hack/scripts/verify-docs-coverage.sh` returns ≥ 1
    - `grep -c "REPO_ROOT" hack/scripts/verify-docs-coverage.sh` returns ≥ 1
    - Default mode (`--non-strict` is the script's default): `bash hack/scripts/verify-docs-coverage.sh; echo $?` outputs `0` on the last line (docs/README.md index present + concepts.md linked; file-presence check skipped for the 4 docs not yet landed)
    - Default mode tail: `bash hack/scripts/verify-docs-coverage.sh 2>&1 | tail -1` matches `PASS: docs/README.md index present + concepts.md linked`
    - Strict mode (`--strict`) before Wave 2 fully lands: exits non-zero with FAIL output listing the missing files (INSTALL.md, project-authoring.md, troubleshooting.md, rbac.md)
    - Strict mode after all Wave 2 docs land: exits 0
    - Script accepts the `--strict` and `--non-strict` flag values verbatim; any other arg yields exit code 2
    - `grep -cE '^verify-docs:' Makefile` returns 1
    - `make verify-docs` invokes `--strict` mode; exits non-zero in Wave 1 (because Plans 05-07/08/09/10 have not yet landed their files); this is expected. Default `bash hack/scripts/verify-docs-coverage.sh` (non-strict) passes after this plan lands the index + concepts.md.
  </acceptance_criteria>
  <done>verify-docs-coverage.sh + Makefile target exist; `--non-strict` default mode passes after docs/README.md + concepts.md land in this plan; `--strict` mode passes after Wave 2 plans land all 4 missing docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → docs/ surface | docs/README.md is the navigation index; broken or missing links here make operators give up before reaching deeper docs (Pitfall 24) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | docs/README.md link rot | mitigate | verify-docs-coverage.sh in `--strict` mode (run by `make verify-docs` and Plan 05-16 CI gate) catches link rot at every commit |
| T-05-04-02 | Tampering | docs/concepts.md drift from spec | accept | concepts.md is intentionally a translation, not a copy of README spec — drift between operator-facing prose and spec voice is acceptable as long as paradigm structure (5 levels, 2 DAGs, derived waves) matches |
| T-05-04-03 | Information Disclosure | docs/ index order | mitigate | D-C3 (revised 2026-05-22 commit e476c68) locks reader-journey order; concepts.md insertion at slot #2 matches kueue/argocd convention per RESEARCH Topic 7; verifier asserts presence not order |
</threat_model>

<verification>
After all tasks: `bash hack/scripts/verify-docs-coverage.sh` (default `--non-strict`) exits 0; `make verify-docs` (`--strict`) exits non-zero with 4 missing-file errors expected until Wave 2 plans land. Both behaviors are acceptance signals.
</verification>

<success_criteria>
- docs/README.md exists with 11 numbered entries (12 file links — entry #4 co-located)
- docs/concepts.md exists with 5 sections (≥60 lines)
- hack/scripts/verify-docs-coverage.sh exists + executable + `--non-strict` default-mode PASS
- Makefile `verify-docs` target wired (invokes `--strict`)
- DIST-04 docs-index gate now executable
- HIGH-5 file-overlap rule honored: depends_on [01] for Makefile sibling-stanza order
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-04-SUMMARY.md` documenting docs/README.md entry count (11 numbered; 12 file links), docs/concepts.md line count, and the `--non-strict` / `--strict` mode exit codes of verify-docs-coverage.sh.
</output>
