---
phase: 05-distribution-self-hosting-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - SECURITY.md
autonomous: true
requirements: [DIST-04]
tags: [contributing, security, oss-readiness]

must_haves:
  truths:
    - "Repo root has CONTRIBUTING.md covering prereqs, dev workflow, branch naming, commit message conventions, DCO signoff (D-C5)"
    - "Repo root has SECURITY.md covering vulnerability reporting + scope (D-C5)"
    - "Both files use the docs/live-e2e.md Audience/Status/Scope opener convention"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "OSS contributor on-ramp"
      contains: "Conventional Commits"
    - path: "SECURITY.md"
      provides: "Vulnerability reporting policy"
      contains: "How to report"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "Makefile test targets (make test, make test-int, make test-e2e-kind, make lint)"
      via: "Development workflow section enumerates them"
      pattern: "make test"
    - from: "SECURITY.md"
      to: "In/out-of-scope per CONTEXT.md D-C5"
      via: "Scope sections"
      pattern: "in scope"
---

<objective>
Land CONTRIBUTING.md + SECURITY.md at repo root, completing the OSS-readiness file set for v1.0 (D-C5). Defer CODE_OF_CONDUCT.md and GOVERNANCE.md to post-v1 per the solo-maintainer posture lock.

Purpose: REQ-DIST-04 documentation coverage. CONTRIBUTING tells outside contributors how to engage; SECURITY tells security researchers how to report vulnerabilities. Both are tier-1 OSS-readiness signals scanned by the GitHub repo health checker.

Output: CONTRIBUTING.md + SECURITY.md at repo root, each following the docs/live-e2e.md heading convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@docs/live-e2e.md
@docs/gates.md
@Makefile
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - docs/live-e2e.md lines 1-15 (Audience/Status/Scope-of-this-doc opener — PATTERNS.md analog for new docs)
    - Makefile (verify the canonical test targets: `make test` line 81, `make test-int` line 145-147, `make test-e2e-kind` line 138-139, `make lint` line 200)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Code Examples → CONTRIBUTING.md skeleton" (lines 805-849 — canonical content)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-C5" (sections covered: prereqs, workflow, branch naming, commit messages, DCO, PR template)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P1.3 CONTRIBUTING.md" (partial analog + reference targets)
    - CLAUDE.md §"Technology Stack" (Go 1.26, kubebuilder v4.14.0, kind v0.31.0, Helm 3 — exact versions to cite)
  </read_first>
  <behavior>
    - File exists at `/CONTRIBUTING.md` (repo root)
    - Opens with `# Contributing to TIDE` heading
    - Carries the Audience/Status/Scope-of-this-doc convention block from docs/live-e2e.md (Audience: outside contributors; Status: v1.0; Scope: prerequisites, dev workflow, branch naming, commit conventions, DCO, PR template, issue triage)
    - Prerequisites section lists Go 1.26 (per CLAUDE.md stack), kubebuilder v4.14.0, kind v0.31.0, Helm 3.16.x, Docker
    - Dev workflow section names the four Make targets — `make test`, `make test-int`, `make test-e2e-kind`, `make lint` — each with a one-line description matching the Makefile help text
    - Branch naming section lists `feat/`, `fix/`, `docs/`, `refactor/` prefix conventions per D-C5
    - Commit messages section names Conventional Commits with the four scope examples per RESEARCH skeleton
    - DCO signoff section explains `git commit -s` per D-C5
    - PR template section lists the 3 required PR description elements per D-C5: link the issue, test plan, breaking change identification
    - Issue triage section per RESEARCH skeleton (bugs / feature requests / questions split)
  </behavior>
  <action>
    Create `/CONTRIBUTING.md` modeling the structure in RESEARCH §"Code Examples → CONTRIBUTING.md skeleton" lines 805-849 but emit prose-not-fenced-code (this is a real document, not an excerpt). Start with `# Contributing to TIDE` then the Audience/Status/Scope-of-this-doc opener (mirroring docs/live-e2e.md lines 1-15 structure with Audience = "Outside contributors to TIDE", Status = "v1.0", Scope = the bulleted list of sections below).

    Sections, in order, each as a level-2 heading: `## Prerequisites`, `## Development workflow`, `## Branch naming`, `## Commit messages`, `## DCO signoff`, `## Pull request template`, `## Issue triage`.

    Prerequisites: bulleted list of Go 1.26.x, kubebuilder v4.14.0, kind v0.31.0, Helm 3.16.x (matching CLAUDE.md pinned versions), Docker (or compatible). Each bullet a one-liner; install via `brew install <tool>` where applicable.

    Development workflow: bulleted list naming `make test` (unit + envtest, <30s budget per TEST-01), `make test-int` (Layer A envtest + Layer B kind integration, <5min per TEST-02), `make test-e2e-kind` (Phase 4 dashboard + gate-flow + tail kind E2E), `make lint` (golangci-lint + import firewalls + custom analyzers).

    Branch naming: bulleted list of `feat/<short-name>`, `fix/<short-name>`, `docs/<short-name>`, `refactor/<short-name>`.

    Commit messages: Conventional Commits link + bullets for `feat(scope): ...`, `fix(scope): ...`, `docs(scope): ...`, `chore(scope): ...`.

    DCO signoff: one paragraph plus a code block `git commit -s -m "..."` showing the signoff convention.

    PR template: bullet list of required PR description elements: linked issue, test plan, breaking change (CRD schema, API surface, Helm value, RBAC scope changes).

    Issue triage: bulleted list with bugs (require kubectl output reproduction), feature requests (cite REQUIREMENTS.md if applicable), questions (route to GitHub Discussions not Issues).

    Do not introduce GOVERNANCE/CODE_OF_CONDUCT sections (deferred-to-post-v1 per CONTEXT.md §"Deferred Ideas").
  </action>
  <verify>
    <automated>test -s CONTRIBUTING.md && grep -q "# Contributing to TIDE" CONTRIBUTING.md && grep -q "Conventional Commits" CONTRIBUTING.md && grep -q "make test" CONTRIBUTING.md && grep -q "make test-int" CONTRIBUTING.md && grep -q "make lint" CONTRIBUTING.md && grep -q "DCO" CONTRIBUTING.md && grep -q "git commit -s" CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -s CONTRIBUTING.md` returns 0
    - `head -1 CONTRIBUTING.md` matches `# Contributing to TIDE`
    - `grep -cE '^## ' CONTRIBUTING.md` returns ≥ 7 (one per required section)
    - `grep -q "Go 1.26" CONTRIBUTING.md` returns 0
    - `grep -q "kind v0.31" CONTRIBUTING.md` returns 0
    - `grep -q "Helm 3" CONTRIBUTING.md` returns 0
    - `grep -q "Conventional Commits" CONTRIBUTING.md` returns 0
    - `grep -q "git commit -s" CONTRIBUTING.md` returns 0
    - `grep -q "make test" CONTRIBUTING.md` returns 0 (and the four-target list is present)
    - `grep -q "make test-int" CONTRIBUTING.md` returns 0
    - `grep -q "make test-e2e-kind" CONTRIBUTING.md` returns 0
    - `grep -q "make lint" CONTRIBUTING.md` returns 0
    - `grep -qi "code of conduct" CONTRIBUTING.md` returns 1 (NOT 0 — section MUST NOT be present per deferred-to-post-v1)
  </acceptance_criteria>
  <done>CONTRIBUTING.md exists at repo root with all 7 required sections; pinned tool versions present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author SECURITY.md</name>
  <files>SECURITY.md</files>
  <read_first>
    - docs/gates.md lines 1-15 (analog opener; table-driven content style)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-C5 SECURITY.md" (scope locked: how-to-report, response-time, in/out-of-scope)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P1.4 SECURITY.md"
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Security Domain" (known threat patterns Phase 5 surfaces — informs in/out-of-scope decisions)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"Markdown doc audience/scope opener" (the heading shape)
  </read_first>
  <behavior>
    - File exists at `/SECURITY.md` (repo root)
    - Opens with `# TIDE Security Policy` heading
    - Audience block declares the file is for security researchers + operators reporting vulnerabilities
    - "How to report" section gives email address + GitHub Security Advisory URL (use placeholder `security@tide.example` since no real address is locked; do NOT invent a real email — researchers can use GitHub Security Advisory per the convention)
    - "Expected response time" section locks 48h acknowledgement per D-C5
    - "Scope (in)" section lists controller, dashboard, CRDs, RBAC, Helm chart templates
    - "Scope (out)" section excludes third-party LLM provider key compromise, host K8s cluster compromise, and explicitly notes chart-signing is v1.x scope (cosign signing deferred per .goreleaser.yaml line 113)
  </behavior>
  <action>
    Create `/SECURITY.md` with the Audience/Status/Scope opener per PATTERNS.md §"Markdown doc audience/scope opener". Heading: `# TIDE Security Policy`.

    Audience: "Security researchers and operators reporting vulnerabilities in TIDE." Status: "v1.0; cosign supply-chain signing deferred to v1.x per .goreleaser.yaml." Scope of this doc bullets: how to report, response time, in/out of scope.

    Sections (level-2 headings in order): `## Reporting a vulnerability`, `## Expected response time`, `## In scope`, `## Out of scope`.

    Reporting a vulnerability: instruct researchers to use the GitHub Security Advisory feature (https://github.com/jsquirrelz/tide/security/advisories/new) as the preferred channel. If GitHub is inaccessible, fall back to a placeholder mail address `security@tide.example` (the project will update once a real address is provisioned; do not invent one).

    Expected response time: 48h acknowledgement per D-C5; resolution timeline depends on severity; advisory + patch released coordinated with reporter.

    In scope (bulleted list): TIDE controller (cmd/manager + internal/controller), TIDE dashboard (cmd/dashboard + dashboard/web), CRDs (api/v1alpha1/*), RBAC (charts/tide/templates/*-rbac.yaml), Helm chart templates (charts/tide/templates/, charts/tide-crds/templates/), CLI (cmd/tide), credproxy (cmd/credproxy).

    Out of scope (bulleted list): third-party LLM provider key compromise (ANTHROPIC_API_KEY is operator-supplied), host Kubernetes cluster compromise (operator's responsibility), chart provenance signing (v1.x scope — link to `.goreleaser.yaml` line 113 comment), bugs in third-party Go deps (route upstream).

    Do NOT add CODE_OF_CONDUCT/GOVERNANCE references — those are deferred per CONTEXT.md §"Deferred Ideas".
  </action>
  <verify>
    <automated>test -s SECURITY.md && grep -q "# TIDE Security Policy" SECURITY.md && grep -q "GitHub Security Advisory" SECURITY.md && grep -q "48h" SECURITY.md && grep -qE "^## In scope" SECURITY.md && grep -qE "^## Out of scope" SECURITY.md && grep -qi "cosign" SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -s SECURITY.md` returns 0
    - `head -1 SECURITY.md` matches `# TIDE Security Policy`
    - `grep -cE '^## ' SECURITY.md` returns ≥ 4
    - `grep -q "GitHub Security Advisory" SECURITY.md` returns 0
    - `grep -q "48h" SECURITY.md` returns 0
    - `grep -q "controller" SECURITY.md` returns 0
    - `grep -q "dashboard" SECURITY.md` returns 0
    - `grep -q "CRDs" SECURITY.md` returns 0
    - `grep -q "ANTHROPIC_API_KEY" SECURITY.md` returns 0
    - `grep -qi "cosign" SECURITY.md` returns 0 (v1.x scope referenced)
  </acceptance_criteria>
  <done>SECURITY.md exists at repo root with all 4 required sections; in/out-of-scope explicit; v1.x cosign-signing caveat present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External security researcher → maintainer | SECURITY.md is the documented channel for vulnerability reports; misdirection here either delays patches (slow channel) or leaks vulnerabilities (public disclosure on Issues instead of Advisories) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | SECURITY.md reporting channel | mitigate | Direct researchers to GitHub Security Advisory (private by default until disclosure); fallback email is placeholder, not a leakable real address |
| T-05-02-02 | Repudiation | SECURITY.md scope | mitigate | Explicit in/out-of-scope sections prevent later "you should have covered X" disputes; cosign signing deferral made explicit so v1.0 users know the supply-chain limit |
| T-05-02-03 | Tampering | CONTRIBUTING.md DCO assertion | accept | DCO signoff is honor-system; cryptographic commit signing is post-v1 (no GPG enforcement in CI per CLAUDE.md anti-pattern guidance) |
</threat_model>

<verification>
After both tasks: `grep -lE 'TIDE Security Policy|Contributing to TIDE' CONTRIBUTING.md SECURITY.md` returns both files.
</verification>

<success_criteria>
- CONTRIBUTING.md exists at repo root with 7 required sections
- SECURITY.md exists at repo root with 4 required sections
- Both follow the docs/live-e2e.md heading convention
- DIST-04 OSS-readiness sub-requirement (CONTRIBUTING + SECURITY) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-02-SUMMARY.md` documenting both file sizes, section counts, and commit SHA.
</output>
