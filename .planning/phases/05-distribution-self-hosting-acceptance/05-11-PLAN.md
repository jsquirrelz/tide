---
phase: 05-distribution-self-hosting-acceptance
plan: 11
type: execute
wave: 2
depends_on: [05]
files_modified:
  - examples/projects/README.md
  - examples/projects/small/namespace.yaml
  - examples/projects/small/project.yaml
  - examples/projects/small/README.md
  - examples/projects/large/namespace.yaml
  - examples/projects/large/project.yaml
  - examples/projects/large/README.md
autonomous: true
requirements: [DIST-04, BOOT-04]
tags: [examples, samples, small, large, project-yaml]

must_haves:
  truths:
    - "examples/projects/README.md exists describing the 3-sample cost spectrum (D-B1)"
    - "examples/projects/small/ has $0 stub-subagent project.yaml + tide-sample-small namespace + README (D-B2 + Pitfall 9)"
    - "examples/projects/large/ has $25 acceptance project.yaml using Variant B outcome prompt (D-A1..A3, D-B4, RESEARCH §Topic 5)"
    - "Small sample's targetRepo uses a placeholder `file:///tmp/no-such-repo` (MEDIUM-7 revision 2026-05-22) — stub-subagent ignores targetRepo, so an unreachable path is safer than referencing a real GitHub URL that may slow the dry-run on stub-subagent envelope-out smoke test"
    - "Both samples valid YAML — verified via `yq eval '.' <file> > /dev/null` (LOW-13 revision: pure YAML lint, no kubeconfig dep)"
    - "apiVersion tideproject.k8s/v1alpha1 (BOOT-03 schema lock)"
    - "Plan 05-12 lands the medium/ sample (depends on cmd/tide-demo-init); this plan only lands small + large + the top-level README"
  artifacts:
    - path: "examples/projects/README.md"
      provides: "Cost-spectrum overview"
      contains: "small"
    - path: "examples/projects/small/project.yaml"
      provides: "$0 stub-subagent sample"
      contains: "Project"
    - path: "examples/projects/large/project.yaml"
      provides: "$25 acceptance sample"
      contains: "outcomePrompt"
  key_links:
    - from: "examples/projects/small/project.yaml"
      to: "ghcr.io/jsquirrelz/tide-stub-subagent:v1.0.0 image"
      via: "subagent.image field"
      pattern: "tide-stub-subagent"
    - from: "examples/projects/large/project.yaml"
      to: "Variant B outcome prompt + $25 hard cap"
      via: "outcomePrompt + budget.absoluteCapCents fields"
      pattern: "2500"
---

<objective>
Land 2 of the 3 sample Project directories (small + large) plus the top-level `examples/projects/README.md` cost-spectrum overview. The medium sample is split into Plan 05-12 because it depends on `cmd/tide-demo-init` (Plan 05-12 also).

Purpose: REQ-DIST-04 + REQ-BOOT-04 samples surface. small/ is the DIST-05 dry-run target ($0, stub-subagent); large/ IS the BOOT-04 acceptance-test project.yaml ($25, Variant B outcome prompt authoring `internal/subagent/openai/`).

Revision notes (iteration 2, 2026-05-22):
- MEDIUM-7: small sample's `targetRepo` uses placeholder `file:///tmp/no-such-repo` (stub-subagent ignores the field anyway; an unreachable file:// path is safer than a real GitHub URL for dry-run smoke tests).
- LOW-13: YAML validation switched from `kubectl apply --dry-run=client` (fragile, kubeconfig-dependent) to `yq eval '.' <file> > /dev/null` (pure YAML lint).

Output: examples/projects/README.md + examples/projects/small/{namespace,project,README} + examples/projects/large/{namespace,project,README}.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md
@.planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md
@config/samples/tide_v1alpha1_project.yaml
@config/samples/namespace.yaml
@test/e2e/testdata/live-claude-project.yaml
@docs/rwx-drivers.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author examples/projects/README.md + small/ sample (placeholder targetRepo per MEDIUM-7; yq lint per LOW-13)</name>
  <files>examples/projects/README.md, examples/projects/small/namespace.yaml, examples/projects/small/project.yaml, examples/projects/small/README.md</files>
  <read_first>
    - config/samples/tide_v1alpha1_project.yaml (lines 1-13 — minimal Project shape; analog per PATTERNS.md §"P4.1")
    - config/samples/namespace.yaml (lines 1-11 — labels convention)
    - test/e2e/testdata/live-claude-project.yaml (multi-doc Namespace + Project pattern)
    - docs/rwx-drivers.md lines 21-27 (overview-paragraph + matrix-table form for examples/projects/README.md per PATTERNS.md §"P4 examples/projects/README.md")
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-B1" + §"D-B2" (cost spectrum, examples/projects/ location)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Topic 11" (small sample is 4-command Quickstart target) and §"Pitfall 9" (tide-sample- namespace prefix)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P4.1" + §"Sample namespace name prefix"
    - charts/tide-crds/templates/project-crd.yaml (verify Project CRD schema fields available at v1alpha1 — apiVersion: tideproject.k8s/v1alpha1; Kind: Project)
    - cmd/stub-subagent/main.go (verify stub-subagent behavior on receiving an unreachable targetRepo — should still return canned envelope success per RESEARCH §"Topic 2: stub-subagent envelope semantics")
  </read_first>
  <behavior>
    - `examples/projects/README.md` exists with cost-spectrum overview (small $0 → medium ~$5 → large ~$25)
    - `examples/projects/small/namespace.yaml` declares Namespace `tide-sample-small` with labels per Pitfall 9 convention
    - `examples/projects/small/project.yaml` declares a Project named `small-project` with budget cap 0, subagent.image = stub-subagent, targetRepo = `file:///tmp/no-such-repo` (placeholder; stub-subagent ignores it per MEDIUM-7 revision)
    - `examples/projects/small/README.md` explains setup + apply + expected behavior + smoke-test note: "Stub-subagent envelope-out succeeds with the unreachable targetRepo (smoke test verified as part of Plan 06 cmd/stub-subagent unit tests)"
    - All YAML passes `yq eval '.' <file> > /dev/null` validation (LOW-13 revision — pure YAML lint, no kubeconfig dep)
    - apiVersion is `tideproject.k8s/v1alpha1` throughout (BOOT-03 schema lock)
  </behavior>
  <action>
    Create `examples/projects/README.md`. Heading `# TIDE Samples`.

    Audience: "Operators trying TIDE end-to-end against three cost tiers."
    Status: "v1.0; three cost-spectrum samples per D-B1."
    Scope: bulleted list of small/medium/large.

    Markdown table:
    | Sample | Cost | LLM | Target | Run with |
    |--------|------|-----|--------|---------|
    | [small](small/) | $0 | stub-subagent (canned) | placeholder file:// path (ignored by stub) | `kubectl apply -f examples/projects/small/` |
    | [medium](medium/) | ~$5 | Claude (cheap model) | local-only `file://` git remote | apply PVC + init Job + project.yaml — see medium/README.md |
    | [large](large/) | ~$25 | Claude (real model) | THIS TIDE repo | `make acceptance-v1` (maintainer ritual) |

    Closing paragraph: explain that small is the DIST-05 dry-run target ($0, safely repeatable); medium gives the first taste of real LLM planning; large IS the v1 acceptance test.

    Create `examples/projects/small/namespace.yaml`:
    ```yaml
    apiVersion: v1
    kind: Namespace
    metadata:
      name: tide-sample-small
      labels:
        tideproject.k8s/sample: small
        app.kubernetes.io/managed-by: tide-sample
    ```

    Create `examples/projects/small/project.yaml`. Use the test/e2e/testdata/live-claude-project.yaml multi-doc pattern but adapted to stub-subagent. Multi-doc YAML with `---` separators for Namespace + Project:

    Document 1: re-include the Namespace from namespace.yaml (multi-doc convenience).

    Document 2: Project resource:
    ```yaml
    apiVersion: tideproject.k8s/v1alpha1
    kind: Project
    metadata:
      name: small-project
      namespace: tide-sample-small
    spec:
      # $0 — stub-subagent returns canned envelopes regardless of targetRepo (MEDIUM-7 revision).
      # Placeholder file:// path: stub-subagent ignores this; never resolves the path.
      targetRepo: file:///tmp/no-such-repo
      budget:
        absoluteCapCents: 0
      subagent:
        image: ghcr.io/jsquirrelz/tide-stub-subagent:v1.0.0
        model: stub
      gates:
        milestone: auto
        phase: auto
        plan: auto
        task: auto
        pauseBetweenWaves: false
      governanceLevel: phase
    ```

    The exact field set depends on the v1alpha1 Project.Spec schema — executor should `yq eval '.' examples/projects/small/project.yaml > /dev/null` after writing it (LOW-13: pure YAML lint, no kubeconfig dep). If yq isn't installed, executor falls back to `python3 -c "import yaml; yaml.safe_load_all(open('examples/projects/small/project.yaml'))"`. If validation fails, executor reads `api/v1alpha1/project_types.go` to identify the missing/incorrect fields and corrects them while preserving the intent (cap=0, stub-subagent image, all gates=auto, placeholder targetRepo).

    Create `examples/projects/small/README.md`. Audience: "Operators running the $0 small sample."
    - Section "What this does": apply Namespace + Project; TIDE dispatches the stub-subagent which returns canned envelopes; Project.Status.Phase progresses to Complete in ~1-2 minutes; $0 LLM cost.
    - Section "On the placeholder targetRepo": "targetRepo is `file:///tmp/no-such-repo` — the stub-subagent ignores targetRepo and returns canned envelopes regardless (verified as Plan 06 smoke test). A real path is not needed for the small sample."
    - Section "Apply": `kubectl apply -f examples/projects/small/` (uses multi-doc YAML)
    - Section "Watch": `kubectl get project small-project -n tide-sample-small -w` and `kubectl wait --for=jsonpath="{.status.phase}"=Complete project/small-project -n tide-sample-small --timeout=10m`
    - Section "Cleanup": `kubectl delete -f examples/projects/small/`
    - Section "Troubleshooting": if `kubectl get project` returns "no matches for kind", install tide-crds chart first per [docs/INSTALL.md](../../docs/INSTALL.md) (Pitfall 4 mitigation reminder)

    NOTE Pitfall 9: namespace name `tide-sample-small` distinct from Phase 1's `tide-samples` (plural).

    Plan 05-11 acceptance criterion (per MEDIUM-7): the small-sample project.yaml's stub-subagent envelope-out behavior is smoke-tested as part of Plan 06's cmd/stub-subagent main_test.go (verify the subagent returns success when invoked with an unreachable targetRepo). If Plan 06's stub-subagent test does not already cover this, add a new test case at execution time.
  </action>
  <verify>
    <automated>test -s examples/projects/README.md && test -s examples/projects/small/namespace.yaml && test -s examples/projects/small/project.yaml && test -s examples/projects/small/README.md && grep -q "tide-sample-small" examples/projects/small/namespace.yaml && grep -q "tideproject.k8s/v1alpha1" examples/projects/small/project.yaml && grep -q "small-project" examples/projects/small/project.yaml && grep -q "absoluteCapCents: 0" examples/projects/small/project.yaml && grep -q "tide-stub-subagent" examples/projects/small/project.yaml && grep -q "file:///tmp/no-such-repo" examples/projects/small/project.yaml && (command -v yq >/dev/null 2>&1 && yq eval '.' examples/projects/small/project.yaml > /dev/null && yq eval '.' examples/projects/small/namespace.yaml > /dev/null) || python3 -c "import yaml,sys; list(yaml.safe_load_all(open('examples/projects/small/project.yaml'))); list(yaml.safe_load_all(open('examples/projects/small/namespace.yaml')))"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files (`examples/projects/README.md`, `small/namespace.yaml`, `small/project.yaml`, `small/README.md`) exist + non-empty
    - `grep -q "tide-sample-small" examples/projects/small/namespace.yaml` returns 0
    - `grep -q "tideproject.k8s/v1alpha1" examples/projects/small/project.yaml` returns 0
    - `grep -q "small-project" examples/projects/small/project.yaml` returns 0
    - `grep -q "absoluteCapCents: 0" examples/projects/small/project.yaml` returns 0 (the $0 stub cap)
    - `grep -q "tide-stub-subagent" examples/projects/small/project.yaml` returns 0
    - `grep -q "file:///tmp/no-such-repo" examples/projects/small/project.yaml` returns 0 (MEDIUM-7 placeholder)
    - YAML lint via yq or python: `yq eval '.' examples/projects/small/project.yaml > /dev/null 2>&1` exits 0 (or python yaml.safe_load_all equivalent) — LOW-13 revision: pure YAML lint, no kubeconfig dep
    - `grep -q "small" examples/projects/README.md` returns 0
    - `grep -q "medium" examples/projects/README.md` returns 0
    - `grep -q "large" examples/projects/README.md` returns 0
    - `grep -q "kubectl apply" examples/projects/small/README.md` returns 0
    - `grep -q "Pitfall\|crds\|tide-crds" examples/projects/small/README.md` returns 0 (CRDs-first troubleshooting reminder)
    - `grep -q "placeholder\|file:///tmp/no-such-repo" examples/projects/small/README.md` returns 0 (MEDIUM-7 README note explains the placeholder)
  </acceptance_criteria>
  <done>examples/projects/README.md + small/ sample directory exist with valid YAML (yq-linted per LOW-13) + README; placeholder targetRepo per MEDIUM-7; smoke-test note added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author large/ sample with Variant B outcome prompt (yq lint per LOW-13)</name>
  <files>examples/projects/large/namespace.yaml, examples/projects/large/project.yaml, examples/projects/large/README.md</files>
  <read_first>
    - test/e2e/testdata/live-claude-project.yaml (lines 47-87 — budget + providerSecretRef + subagent + git pattern; analog per PATTERNS.md §"P4.3")
    - .planning/phases/05-distribution-self-hosting-acceptance/05-RESEARCH.md §"Code Examples → Large Sample Project.yaml Outcome Prompt" lines 719-784 (Variant B verbatim)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-CONTEXT.md §"D-A1..D-A4" (Single Phase scope, $25 cap, 7-check pass criteria, maintainer ritual) + §"D-B4" (outcome prompt phrasing)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"P4.3"
    - examples/projects/small/project.yaml (just-written by Task 1 — for consistent field set + style)
    - .planning/phases/05-distribution-self-hosting-acceptance/05-PATTERNS.md §"Sample namespace name prefix" (tide-sample-large)
  </read_first>
  <behavior>
    - `examples/projects/large/namespace.yaml` declares Namespace `tide-sample-large` with labels
    - `examples/projects/large/project.yaml` declares Project `large-project` in `tide-sample-large` namespace
    - Project.Spec carries:
      - targetRepo = `https://github.com/jsquirrelz/tide.git` (THIS repo per D-A1)
      - budget.absoluteCapCents = 2500 ($25 hard cap per D-A2)
      - budget.rollingWindowDurationSeconds = 86400 (24h per Phase 04.1 P4.1; explicit per Pitfall 7)
      - subagent.image = ghcr.io/jsquirrelz/tide-claude-subagent:v1.0.0 (real Claude image)
      - subagent.model = claude-opus-4-7 (per RESEARCH §"Code Examples" line 449)
      - providerSecretRef / secretRefs pointing to `tide-secrets` (or `anthropic-creds` — match what the controller expects per Phase 3 D-B3)
      - git.repoURL + git.credsSecretRef
      - gates: all auto + pauseBetweenWaves: false (per D-A1 Single Phase scope + D-A2)
      - governanceLevel: phase
      - outcomePrompt = the multi-line Variant B text from RESEARCH §"Code Examples → Large Sample Project.yaml Outcome Prompt" lines 744-777 verbatim
    - `examples/projects/large/README.md` explains the acceptance-test purpose, the $25 hard cap, why it's a maintainer ritual not a CI gate, and points to `make acceptance-v1` (Plan 05-15)
    - All YAML passes `yq eval '.' <file> > /dev/null` (LOW-13 revision)
  </behavior>
  <action>
    Create `examples/projects/large/namespace.yaml`:
    ```yaml
    apiVersion: v1
    kind: Namespace
    metadata:
      name: tide-sample-large
      labels:
        tideproject.k8s/sample: large
        app.kubernetes.io/managed-by: tide-sample
    ```

    Create `examples/projects/large/project.yaml`. Multi-doc YAML with `---` separator: Namespace + Project. Mirror the test/e2e/testdata/live-claude-project.yaml field set (lines 47-87) but adapt to the large-sample acceptance shape.

    For the Project body, use the canonical Variant B shape from RESEARCH §"Code Examples → Large Sample Project.yaml Outcome Prompt" lines 720-777. Key fields:

    ```yaml
    apiVersion: tideproject.k8s/v1alpha1
    kind: Project
    metadata:
      name: large-project
      namespace: tide-sample-large
    spec:
      targetRepo: https://github.com/jsquirrelz/tide.git
      branchStrategy: per-run
      governanceLevel: phase
      budget:
        absoluteCapCents: 2500
        rollingWindowCapCents: 2500
        rollingWindowDurationSeconds: 86400
      providerSecretRef: tide-secrets
      gitCredsSecretRef: tide-secrets
      subagent:
        image: ghcr.io/jsquirrelz/tide-claude-subagent:v1.0.0
        model: claude-opus-4-7
      gates:
        milestone: auto
        phase: auto
        plan: auto
        task: auto
        pauseBetweenWaves: false
      outcomePrompt: |
        Author the scaffold for `internal/subagent/openai/` in this repository,
        mirroring the existing `internal/subagent/anthropic/` layout.

        Concrete deliverables (tight scope — target 5-7 tasks, ONE Plan, ONE Phase):

        1. `internal/subagent/openai/client.go` — defines `Client` struct + constructor.
           Match the shape of `internal/subagent/anthropic/client.go`. DO NOT call the
           real OpenAI API; the constructor takes an API key string but the methods
           are stubbed.

        2. `internal/subagent/openai/run.go` — defines `Subagent.Run(ctx, EnvelopeIn)
           (EnvelopeOut, error)` matching `pkg/dispatch.Subagent`. STUB implementation:
           return a canned `EnvelopeOut{Status: "success", Artifacts: []}` envelope.
           Add a TODO comment explaining real-API integration is v1.x scope.

        3. `internal/subagent/openai/Dockerfile` — multi-stage build mirroring
           `internal/subagent/anthropic/Dockerfile`. Final image must build clean.

        4. `internal/subagent/openai/run_test.go` — ONE unit test verifying stub
           returns canned envelope and matches the `Subagent` interface.

        5. `internal/subagent/openai/doc.go` — package doc comment referencing
           Phase 3 D-C1 layering pattern.

        Constraints:
        - DO NOT modify any files outside `internal/subagent/openai/`.
        - DO NOT add the openai package to `cmd/manager`'s build (the contract is
          authoring the scaffold, not wiring it; wiring is v1.x scope).
        - Follow the existing repo conventions in CLAUDE.md (Apache-2.0 headers,
          logging discipline, error handling).

        Pass criterion: `go test ./internal/subagent/openai/...` is green;
        `docker build -f internal/subagent/openai/Dockerfile .` builds without error.
    ```

    The exact Project.Spec field names depend on the v1alpha1 schema in `api/v1alpha1/project_types.go`. Executor must verify by running `yq eval '.' examples/projects/large/project.yaml > /dev/null` (LOW-13: YAML lint) and adjusting field names to match the canonical schema while preserving the Variant B intent: $25 cap, real Claude image, no human gates, openai scaffold authoring prompt.

    Create `examples/projects/large/README.md`. Audience: "Maintainer running the $25 v1 acceptance test."
    - Section "What this does": acceptance test — applies the large sample which targets THIS TIDE repo; TIDE authors `internal/subagent/openai/` scaffold; commits + pushes to a per-run branch on the configured remote; halts at Complete or BudgetExceeded.
    - Section "Run with": `make acceptance-v1` (lands in Plan 05-15). Note: requires `ANTHROPIC_API_KEY` env + GitHub PAT in `tide-secrets` Secret. NEVER pushes to `main`.
    - Section "Hard $25 cap": per D-A2, no bypass; if cap fires, `Project.Status.Phase=BudgetExceeded` and the maintainer inspects.
    - Section "Pass criteria": list the 7 D-A3 checks (per-run branch exists; 3-of-4 commit-message shapes — milestone N/A for D-A1 Single Phase scope per MEDIUM-6; Phase=Complete; zero ERROR logs; no orphan Jobs; `tide_secret_leak_blocked_total{project=large-project}=0`; `costSpentCents<2500`).
    - Section "Evidence": `.acceptance-runs/<timestamp>/` captures controller logs, CRDs yaml, run-branch git log, dashboard screenshot.
    - Section "Why maintainer-only": D-A4 — live LLM cost per PR/nightly is unjustified before OSS adoption proves demand; acceptance is reusable on every release.
  </action>
  <verify>
    <automated>test -s examples/projects/large/namespace.yaml && test -s examples/projects/large/project.yaml && test -s examples/projects/large/README.md && grep -q "tide-sample-large" examples/projects/large/namespace.yaml && grep -q "tideproject.k8s/v1alpha1" examples/projects/large/project.yaml && grep -q "large-project" examples/projects/large/project.yaml && grep -q "absoluteCapCents: 2500" examples/projects/large/project.yaml && grep -q "claude-opus-4-7" examples/projects/large/project.yaml && grep -q "internal/subagent/openai" examples/projects/large/project.yaml && grep -q "outcomePrompt" examples/projects/large/project.yaml && grep -q "5-7 tasks" examples/projects/large/project.yaml && grep -q "ONE Plan" examples/projects/large/project.yaml && (command -v yq >/dev/null 2>&1 && yq eval '.' examples/projects/large/project.yaml > /dev/null && yq eval '.' examples/projects/large/namespace.yaml > /dev/null) || python3 -c "import yaml,sys; list(yaml.safe_load_all(open('examples/projects/large/project.yaml'))); list(yaml.safe_load_all(open('examples/projects/large/namespace.yaml')))"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files (`large/namespace.yaml`, `large/project.yaml`, `large/README.md`) exist + non-empty
    - `grep -q "tide-sample-large" examples/projects/large/namespace.yaml` returns 0
    - `grep -q "tideproject.k8s/v1alpha1" examples/projects/large/project.yaml` returns 0
    - `grep -q "large-project" examples/projects/large/project.yaml` returns 0
    - `grep -q "absoluteCapCents: 2500" examples/projects/large/project.yaml` returns 0 (D-A2 $25 cap)
    - `grep -q "claude-opus-4-7" examples/projects/large/project.yaml` returns 0
    - `grep -q "internal/subagent/openai" examples/projects/large/project.yaml` returns 0 (Variant B outcome)
    - `grep -q "5-7 tasks" examples/projects/large/project.yaml` returns 0 (Variant B scope constraint)
    - `grep -q "ONE Plan" examples/projects/large/project.yaml` returns 0
    - `grep -q "outcomePrompt" examples/projects/large/project.yaml` returns 0
    - YAML lint via yq or python: `yq eval '.' examples/projects/large/project.yaml > /dev/null 2>&1` exits 0 (LOW-13 revision: pure YAML lint)
    - `grep -q "make acceptance-v1" examples/projects/large/README.md` returns 0
    - `grep -q "ANTHROPIC_API_KEY" examples/projects/large/README.md` returns 0
    - `grep -q "7 .* check\|D-A3" examples/projects/large/README.md` returns 0 (7 pass criteria mentioned)
    - `grep -q "BudgetExceeded\|hard.*cap\|\\$25" examples/projects/large/README.md` returns 0
  </acceptance_criteria>
  <done>examples/projects/large/ sample directory exists with Variant B outcome prompt; YAML validates via yq (LOW-13); README explains the acceptance ritual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator applying sample YAML → K8s cluster | Bad sample YAML refuses to apply (admission webhook 422) on first try; operator gives up on TIDE |
| Sample outcome prompt → real LLM | Misphrased outcome prompt blows the $25 budget cap and wastes maintainer money |
| Small sample's placeholder targetRepo → stub-subagent | If stub-subagent ever changes behavior to attempt to clone the targetRepo, the placeholder `file:///tmp/no-such-repo` would fail; smoke test in Plan 06 catches this regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Information Disclosure | Sample project.yaml credentials | mitigate | Both samples use Secret reference (`providerSecretRef: tide-secrets`) — never inline a raw key; sample README instructs operator to create the Secret via `kubectl create secret generic` |
| T-05-11-02 | Tampering | Sample YAML drift from v1alpha1 schema | mitigate | Acceptance criterion runs `yq eval '.' <file>` against both samples (LOW-13 — pure YAML lint, no kubeconfig dep); CRDs are present (Phase 1 D-A3 schema lock); BOOT-03 invariant means schema stays put across M0 → M_self |
| T-05-11-03 | Cost | Large-sample outcome prompt phrasing | mitigate | Variant B verbatim from RESEARCH §"Code Examples" — scope constraint "5-7 tasks, ONE Plan, ONE Phase" + "do NOT integrate against real OpenAI API" bounds budget consumption per Pitfall 10 |
| T-05-11-04 | Elevation of Privilege | Sample namespace collisions | mitigate | Per Pitfall 9, namespace prefix `tide-sample-` distinct from Phase 1's `tide-samples`; sample README instructs operator to delete the namespace if it already exists |
| T-05-11-05 | Tampering | Stub-subagent behavior change on unreachable targetRepo | mitigate | MEDIUM-7: small sample uses `file:///tmp/no-such-repo`; Plan 06's stub-subagent main_test.go verifies the subagent returns canned success regardless of targetRepo; regression test catches behavior drift |
</threat_model>

<verification>
After both tasks: `yq eval '.' examples/projects/small/project.yaml > /dev/null && yq eval '.' examples/projects/large/project.yaml > /dev/null` exits 0 (LOW-13).
</verification>

<success_criteria>
- examples/projects/README.md cost-spectrum overview
- small/ sample ($0 stub-subagent, placeholder targetRepo per MEDIUM-7) + large/ sample ($25 Variant B acceptance)
- Both YAML files pass `yq eval '.'` lint (LOW-13)
- READMEs explain setup + apply + cleanup
- DIST-04 samples piece (2 of 3) + BOOT-04 acceptance project.yaml satisfied
- MEDIUM-7 + LOW-13 revisions applied
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution-self-hosting-acceptance/05-11-SUMMARY.md` documenting file sizes, yq-lint results for both samples, smoke-test verification for stub-subagent placeholder targetRepo, and commit SHA.
</output>
