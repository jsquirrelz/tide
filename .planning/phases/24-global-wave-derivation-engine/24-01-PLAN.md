---
phase: 24-global-wave-derivation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/envtest/global_wave_derivation_test.go
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04]
must_haves:
  truths:
    - "A multi-plan / cross-phase envtest fixture exists encoding the README worked example (tasks across Plans, cross-scope dependsOn)"
    - "The test file compiles and runs RED against current main (global derivation not yet built)"
    - "Cross-scope fixture helpers (createSimplePhase / createSimpleMilestone) exist alongside the existing makeTask/makeTaskWithWaveLabel"
  artifacts:
    - path: "test/integration/envtest/global_wave_derivation_test.go"
      provides: "EXEC-01..04 envtest contract + cross-scope fixture helpers"
      contains: "func TestGlobalWaveDerivation"
      min_lines: 120
  key_links:
    - from: "test/integration/envtest/global_wave_derivation_test.go"
      to: "test/integration/envtest/indegree_test.go"
      via: "reuses makeTask / makeTaskWithWaveLabel helpers (no duplication)"
      pattern: "makeTask"
---

<objective>
Author the Wave 0 envtest scaffold for Phase 24: a global-wave-derivation test file and the cross-scope fixture helpers it needs. This file IS the executable contract for EXEC-01..04 — the derivation engine (Plans 02/03) is built against it. It MUST run RED against current `main` (the engine does not exist yet), turning GREEN as Plans 02/03 land.

Purpose: VALIDATION.md flags this file + cross-scope fixtures as the single Wave 0 gap; no derivation task in later plans has automated verification without it.
Output: `test/integration/envtest/global_wave_derivation_test.go` with `Describe` blocks named so the per-task commands in VALIDATION.md (`-run GlobalDag`, `-run GlobalWaveIndex`, `-run BidirectionalIndex`, `-run WaveRederivation`) select them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-global-wave-derivation-engine/24-VALIDATION.md
@.planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md

<interfaces>
<!-- The contract the test asserts against. From api/v1alpha2 (Phase 23). -->
WaveSpec carries global scope: WaveSpec{ProjectRef string, WaveIndex int}.
TaskSpec carries: PlanRef string, DependsOn []string (any-level targets), FilesTouched/DeclaredOutputPaths []string.
PlanSpec carries: PhaseRef string, DependsOn []string.
PhaseSpec carries: MilestoneRef string, DependsOn []string.
MilestoneSpec carries: ProjectRef string, DependsOn []string.
Project label key: owner.LabelProject = "tideproject.k8s/project".
Wave-index label key (string literal, no constant): "tideproject.k8s/wave-index".
Global Wave CR naming convention (target of derivation): tide-wave-<project.Name>-<globalIndex>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-scope fixture helpers + global-derivation test file (RED)</name>
  <files>test/integration/envtest/global_wave_derivation_test.go</files>
  <read_first>
    - test/integration/envtest/indegree_test.go (ENTIRE file — package name `envtest_integration`, imports, BeforeEach/AfterEach cleanup shape, `createSimplePlan`, `makeTask`, `makeTaskWithWaveLabel`, Wave Get/assert patterns; reuse these helpers, do NOT duplicate them)
    - test/integration/envtest/suite_test.go (suite registration, `k8sClient` / `mgrClient` handles, namespace constants)
    - .planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md (the `global_wave_derivation_test.go` section — exact helper signatures `createSimplePhase(ctx, name, milestoneRef)` / `createSimpleMilestone(ctx, name, projectRef)`, Wave CR name assertion helper, the README worked-example fixture)
    - .planning/phases/24-global-wave-derivation-engine/24-RESEARCH.md (OQ-5 — the README:54 conformance fixture: tasks α,β,γ in plan-A; δ,ε in plan-B; ζ,η,θ in plan-C; edges α→δ,β→δ,γ→η,ζ→η,δ→ε,η→θ; expected waves [{α,β,γ,ζ},{δ,η},{ε,θ}])
  </read_first>
  <action>
    Create `test/integration/envtest/global_wave_derivation_test.go` in package `envtest_integration`, reusing the existing `makeTask`/`makeTaskWithWaveLabel`/`createSimplePlan` helpers from `indegree_test.go` (import nothing new beyond what indegree_test.go imports). Add two new helpers in this file: `createSimplePhase(ctx, name, milestoneRef string)` (creates a Phase with `Spec.MilestoneRef`) and `createSimpleMilestone(ctx, name, projectRef string)` (creates a Milestone with `Spec.ProjectRef`), each following the `createSimplePlan` Create+Eventually-Get shape.

    Author a top-level `Describe("Global Wave Derivation", ...)` whose nested `Describe`/`Context` names contain the substrings `GlobalDag`, `GlobalWaveIndex`, `BidirectionalIndex`, and `WaveRederivation` so the VALIDATION.md `-run` selectors match. Cover, against the README worked-example fixture (tasks spanning plan-A/B/C, the six edges above):
    - GlobalDag (EXEC-01): after applying the multi-plan Project, the derivation produces a global schedule spanning all three plans — assert Wave CRs `tide-wave-<project>-0/1/2` eventually exist.
    - GlobalWaveIndex (EXEC-02): Wave CRs are named `tide-wave-<project>-<N>` (NOT `tide-wave-<plan.UID>-<i>`) and carry `Spec.WaveIndex == N` and `Spec.ProjectRef == <project>`; the membership of each global wave matches [{α,β,γ,ζ},{δ,η},{ε,θ}].
    - BidirectionalIndex (EXEC-03): task→wave — each Task's `tideproject.k8s/wave-index` label equals its expected global index; wave→tasks — a `client.MatchingLabels{"tideproject.k8s/wave-index":"0", owner.LabelProject: project}` list returns exactly {α,β,γ,ζ}.
    - WaveRederivation (EXEC-04): add a Task that depends on a wave-2 task (extends the schedule) OR remove a dependency (compresses it) and assert the Wave CR set and wave-index labels re-derive (including PRUNE of a now-stale high-index Wave CR); assert no `Schedule`/`Waves[]` aggregate appears on `Project.Status` (the schedule is never cached).
    - Include one cross-phase / cross-milestone case using `createSimplePhase`/`createSimpleMilestone` plus a Plan-level or Phase-level `dependsOn` (coarse ref) to exercise fan-out: assert a coarse `dependsOn` naming a Plan fans in over every Task in that Plan.

    Use generous `Eventually` timeouts (the indegree_test.go `"30s","500ms"` / `"60s","500ms"` style) since derivation runs on ProjectReconciler reconcile. Follow the indegree_test.go AfterEach cleanup shape and explicitly delete Waves so global Wave CRs from one `It` do not leak into the next.

    Do NOT modify any production code in this plan. This file is expected to FAIL/compile-and-go-RED against current `main` (the engine is built in Plans 02/03). The point is a runnable, asserting contract.
  </action>
  <verify>
    <automated>go vet ./test/integration/envtest/... 2>&1 | tee /tmp/24-01-vet.log; grep -c 'createSimplePhase\|createSimpleMilestone' test/integration/envtest/global_wave_derivation_test.go</automated>
  </verify>
  <acceptance_criteria>
    - File `test/integration/envtest/global_wave_derivation_test.go` exists, package `envtest_integration`.
    - `go vet ./test/integration/envtest/...` exits 0 (file compiles; no unused imports; helpers reused not redeclared — `grep -c 'func makeTask' test/integration/envtest/global_wave_derivation_test.go` returns 0, helpers come from indegree_test.go).
    - `grep -vE '^[[:space:]]*//' test/integration/envtest/global_wave_derivation_test.go | grep -cE 'GlobalDag|GlobalWaveIndex|BidirectionalIndex|WaveRederivation'` returns ≥ 4 (all four selector substrings present in non-comment lines).
    - `grep -c 'func createSimplePhase\|func createSimpleMilestone' test/integration/envtest/global_wave_derivation_test.go` returns 2.
    - The file asserts the global Wave name format `tide-wave-` with `project` (not `plan.UID`): `grep -c 'tide-wave-' test/integration/envtest/global_wave_derivation_test.go` returns ≥ 1.
    - Running `go test ./test/integration/envtest/... -run GlobalWaveIndex -v` against current main goes RED (assertions on `tide-wave-<project>-<N>` Wave CRs are unmet because derivation is not yet implemented) — record the RED summary line in the SUMMARY.
  </acceptance_criteria>
  <done>The global-derivation test file compiles, reuses indegree_test.go helpers, adds the two cross-scope fixture helpers, names its blocks to match the VALIDATION.md `-run` selectors, encodes the README worked example, and runs RED on current main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan; no production trust boundary touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | new test file | accept | Test-only code in `test/integration/envtest/`; no runtime/CRD/endpoint surface added. No external packages installed (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `go vet ./test/integration/envtest/...` exits 0.
- The four VALIDATION.md selector substrings are present.
- The file runs RED on current main (contract is real and unmet).
</verification>

<success_criteria>
The Wave 0 gap from VALIDATION.md is closed: a compiling, asserting, RED global-derivation test plus cross-scope fixture helpers exist, ready for Plans 02/03 to turn GREEN.
</success_criteria>

<output>
Create `.planning/phases/24-global-wave-derivation-engine/24-01-SUMMARY.md` when done. Record the RED test summary line proving the contract is unmet on current main.
</output>
