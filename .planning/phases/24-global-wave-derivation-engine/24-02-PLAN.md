---
phase: 24-global-wave-derivation-engine
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - internal/controller/project_controller.go
autonomous: true
requirements: [EXEC-01]
must_haves:
  truths:
    - "assembleProjectDepGraph fans out a dependsOn naming a Plan/Phase/Milestone into edges over every Task in that scope (in-memory, never persisted)"
    - "Plan-/Phase-/Milestone-level dependsOn carriers are reconciled (not just Task.DependsOn)"
    - "Fan-out edges are de-duplicated before ComputeWaves (no double indegree from coarse+fine overlap)"
    - "assembleProjectDepGraph is called ONCE per reconcile and its (nodes,edges) shared by the cycle gate (no double List)"
  artifacts:
    - path: "internal/controller/project_controller.go"
      provides: "Full fan-out assembler over all four dependsOn carriers; assemble-once refactor of the cycle gate"
      contains: "tasksByPlan"
  key_links:
    - from: "ProjectReconciler.Reconcile"
      to: "checkGlobalCycleGate"
      via: "pre-assembled (nodes,edges) passed in (assemble-once, Pitfall 7)"
      pattern: "checkGlobalCycleGate"
    - from: "assembleProjectDepGraph"
      to: "pkg/dag.Edge"
      via: "in-memory fan-out resolution via spec.planRef/phaseRef/milestoneRef maps"
      pattern: "planToPhase|phaseToMS|tasksByPlan"
---

<objective>
Extend `ProjectReconciler.assembleProjectDepGraph` from conservative task-only edges (Phase 23) to FULL FAN-OUT (D-04/D-06): a `dependsOn` entry naming a Plan/Phase/Milestone expands to fan-in over every Task in that scope, resolved in-memory via `spec.planRef`/`spec.phaseRef`/`spec.milestoneRef` — never written back (D-05). Cover all four carriers (Task/Plan/Phase/Milestone `DependsOn`). Refactor so the assembler runs ONCE per reconcile and shares its `(nodes, edges)` with the cycle gate (Pitfall 7).

Purpose: EXEC-01 — assemble ONE global Execution DAG of all Tasks across scopes. This plan delivers the assembly; Plan 03 derives waves from it.
Output: extended `assembleProjectDepGraph` returning the fully-resolved global node/edge set; `checkGlobalCycleGate` refactored to accept pre-assembled `(nodes,edges)`; `Reconcile` calls the assembler once.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-global-wave-derivation-engine/24-CONTEXT.md
@.planning/phases/24-global-wave-derivation-engine/24-RESEARCH.md
@.planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md

<interfaces>
<!-- Extracted from current code (verified line locations). -->
internal/controller/project_controller.go:
  - assembleProjectDepGraph(ctx, project) (nodes []dag.NodeID, edges []dag.Edge, err error) — lines 1443-1480 (conservative task-only today)
  - checkGlobalCycleGate(ctx, project) (blocked bool, result ctrl.Result, err error) — lines 1493-1528; line 1497 calls assembleProjectDepGraph then DISCARDS waves
  - Reconcile cycle-gate call site — line 259: `if blocked, result, gErr := r.checkGlobalCycleGate(ctx, &v2project); blocked { ... }`
api/v1alpha2: TaskSpec.PlanRef, TaskSpec.DependsOn, PlanSpec.PhaseRef, PlanSpec.DependsOn, PhaseSpec.MilestoneRef, PhaseSpec.DependsOn, MilestoneSpec.ProjectRef, MilestoneSpec.DependsOn
pkg/dag: ComputeWaves(nodes []NodeID, edges []Edge) ([][]NodeID, error); Edge{From,To NodeID}; CycleError{InvolvedNodes []NodeID}
owner.LabelProject = "tideproject.k8s/project"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full fan-out resolution in assembleProjectDepGraph (all four dependsOn carriers)</name>
  <files>internal/controller/project_controller.go</files>
  <behavior>
    - A Task.DependsOn entry naming another Task → single task→task edge (existing behavior preserved).
    - A Task.DependsOn entry naming a Plan → edges from every Task in that Plan to this Task.
    - A Task.DependsOn entry naming a Phase → edges from every Task in every Plan under that Phase.
    - A Task.DependsOn entry naming a Milestone → edges from every Task transitively under that Milestone.
    - A Plan.DependsOn entry → for every Task in the owning Plan, edge from every Task in the referenced scope.
    - A Phase.DependsOn / Milestone.DependsOn entry → same coarse fan-out semantics over their member Tasks.
    - A coarse+fine overlap (dependsOn lists both `plan-A` and a specific task in plan-A) produces exactly ONE edge per (from,to) pair (de-duplicated).
    - An unresolved ref (names nothing) contributes no edge (conservative; never invents an edge).
    - Resolution is in-memory only: no CRD write, no new api field.
  </behavior>
  <read_first>
    - internal/controller/project_controller.go lines 1432-1543 (assembleProjectDepGraph 1443-1480, checkGlobalCycleGate 1493-1528, taskToProject 1535-1542) — the functions being extended/refactored
    - .planning/phases/24-global-wave-derivation-engine/24-RESEARCH.md (OQ-2 fan-out resolution maps `planToPhase`/`phaseToMS`/`tasksByPlan`; Code Example Pattern 1; Pitfall 2 edge de-dup; Pitfall 7 assemble-once; Open Question 1 — fan-out must cover all four carrier types)
    - .planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md (assembleProjectDepGraph section + checkGlobalCycleGate section; the note that Plan/Phase/Milestone `DependsOn` must also be iterated)
    - api/v1alpha2/task_types.go, plan_types.go, phase_types.go, milestone_types.go (the `DependsOn` + `*Ref` spec fields; confirm exact field names)
  </read_first>
  <action>
    In `assembleProjectDepGraph`, after the existing Task List, add List calls for `PlanList`, `PhaseList`, `MilestoneList` (`client.InNamespace(project.Namespace)`). Build in-memory maps: `taskNames map[string]struct{}`, `tasksByPlan map[string][]string` (keyed by `task.Spec.PlanRef`), `planToPhase map[string]string` (`plan.Name`→`plan.Spec.PhaseRef`), `phaseToMS map[string]string` (`phase.Name`→`phase.Spec.MilestoneRef`). Add a `tasksForScope(name string) []string` closure resolving a ref to its task set: direct task → `[name]`; plan → `tasksByPlan[name]`; phase → union of `tasksByPlan` for plans whose `planToPhase==phase`; milestone → transitive union over phases whose `phaseToMS==milestone`; unresolved → empty. Use an `edgeSet map[string]struct{}` keyed `from+"\x00"+to` to de-duplicate before appending `dag.Edge{From,To}` (Pitfall 2). Iterate `Task.DependsOn` (edge from each `tasksForScope(dep)` member → the task). THEN iterate `Plan.DependsOn`, `Phase.DependsOn`, `Milestone.DependsOn` carriers (Open Question 1): for each carrier-level dep, add edges from every task in the referenced scope to every task in the owning scope (owning-scope tasks via the same `tasksForScope` on the carrier's own name). Keep `nodes` = all Task names. Do NOT persist any resolved edge or add any api type field (D-05 / `verify-no-aggregates`).

    Refactor for assemble-once (Pitfall 7): change `checkGlobalCycleGate` to accept pre-assembled `(nodes []dag.NodeID, edges []dag.Edge)` instead of calling `assembleProjectDepGraph` itself (it keeps the `ComputeWaves`-for-validation + `CycleError`→`CycleDetected` condition logic, still discarding the waves). In `Reconcile` at the line-259 call site, call `r.assembleProjectDepGraph(ctx, &v2project)` ONCE, then pass `(nodes,edges)` into `checkGlobalCycleGate`. Plan 03 consumes the same `(nodes,edges)` for derivation, so structure the call site so the assembled result is reachable by the derivation step Plan 03 adds (e.g. assemble in Reconcile and hold the result in a local). Keep `pkg/dag` import usage unchanged; do not touch `pkg/dag` (D-11, `verify-dag-imports`).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -count=1 -timeout 120s 2>&1 | tee /tmp/24-02-unit.log; grep -vE '^[[:space:]]*//' internal/controller/project_controller.go | grep -cE 'tasksByPlan|planToPhase|phaseToMS'</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0.
    - `grep -vE '^[[:space:]]*//' internal/controller/project_controller.go | grep -cE 'tasksByPlan|planToPhase|phaseToMS'` returns ≥ 3 (the three resolution maps exist in code, not comments).
    - `grep -vE '^[[:space:]]*//' internal/controller/project_controller.go | grep -cE 'planList|phaseList|msList|MilestoneList'` returns ≥ 1 (coarse-scope List calls added).
    - The four carriers are iterated: `grep -vE '^[[:space:]]*//' internal/controller/project_controller.go | grep -cE '\.Spec\.DependsOn'` returns ≥ 4 (Task + Plan + Phase + Milestone).
    - Edge de-dup present: `grep -vE '^[[:space:]]*//' internal/controller/project_controller.go | grep -c 'edgeSet'` returns ≥ 1.
    - `checkGlobalCycleGate` no longer calls `assembleProjectDepGraph` internally: `sed -n '/func (r \*ProjectReconciler) checkGlobalCycleGate/,/^}/p' internal/controller/project_controller.go | grep -c 'assembleProjectDepGraph'` returns 0; the assembler is instead called once in `Reconcile`.
    - `make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep` exits 0 (no aggregate field, no k8s import leaked into pkg/dag, no DB dep).
    - `make MAKE_EXIT` proxy: `go test ./internal/controller/... -count=1` exits 0 (no controller unit regressions). Cross-scope envtest behavior from Plan 01 may still be RED until Plan 03 — that is expected and noted in SUMMARY.
  </acceptance_criteria>
  <done>assembleProjectDepGraph performs full in-memory fan-out over all four dependsOn carriers with de-duplicated edges; the cycle gate consumes pre-assembled (nodes,edges); the assembler runs once per reconcile; all three verify guards stay green; `go build ./...` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project CR `dependsOn` → assembler | Operator-authored `dependsOn` strings (any-level refs) crossing into in-memory edge assembly. CEL MinLength/no-empty constraints already shipped in v1alpha2 (Phase 23). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | adversarial cross-scope `dependsOn` cycle via coarse refs | mitigate | Fan-out runs BEFORE `checkGlobalCycleGate`; `ComputeWaves` returns `CycleError`; gate sets `CycleDetected` and blocks — no derivation, no dispatch (RESEARCH Security Domain, verified project_controller.go cycle path). |
| T-24-02-02 | Denial of Service | extra Plan/Phase/Milestone List calls per reconcile | accept | Assemble-once refactor (Pitfall 7) halves List calls vs naive double-assembly; resolution is O(V+E) map lookups; single-namespace tenancy bounds list size. No new input surface. |
| T-24-02-03 | Tampering | resolved-edge write-back to CRD | mitigate | D-05 — resolution is in-memory only; `verify-no-aggregates` blocks any `Schedule`/`Waves[]`/`IndegreeMap` field; acceptance criteria assert the guard stays green. |
</threat_model>

<verification>
- `go build ./...` and `go test ./internal/controller/...` green.
- `make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep` green.
- assembler covers all four carriers, de-dups edges, runs once, resolution in-memory only.
</verification>

<success_criteria>
EXEC-01 assembly is in place: one global node/edge set spanning all scopes, fan-out over all four dependsOn carriers, de-duplicated, in-memory only, assembled once and shared with the cycle gate — ready for Plan 03 to derive waves from it.
</success_criteria>

<output>
Create `.planning/phases/24-global-wave-derivation-engine/24-02-SUMMARY.md` when done. Note which Plan-01 envtest assertions remain RED pending Plan 03 derivation.
</output>
