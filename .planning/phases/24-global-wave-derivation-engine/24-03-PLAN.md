---
phase: 24-global-wave-derivation-engine
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - internal/controller/project_controller.go
autonomous: true
requirements: [EXEC-02, EXEC-03, EXEC-04]
must_haves:
  truths:
    - "After the cycle gate passes, ProjectReconciler derives global waves via pkg/dag.ComputeWaves over the assembled (nodes,edges)"
    - "One Wave CR per global wave named tide-wave-<project>-<globalIndex>, owned by the Project, WaveIndex set"
    - "Each Task carries its GLOBAL tideproject.k8s/wave-index label (task->wave); a label selector lists a wave's Tasks (wave->tasks)"
    - "Re-derivation reconciles the Wave CR set on every reconcile: creates missing, prunes stale high-index extras; metric increments exactly once on Create"
    - "No schedule is cached in Project.status; re-derivation is O(V+E) from the DAG each pass (PERSIST-03 guards green)"
  artifacts:
    - path: "internal/controller/project_controller.go"
      provides: "deriveGlobalWaves + stampGlobalTaskLabels + Wave CR reconcile/prune wired after the cycle gate; Owns Wave added"
      contains: "deriveGlobalWaves"
  key_links:
    - from: "ProjectReconciler.Reconcile"
      to: "deriveGlobalWaves"
      via: "called after checkGlobalCycleGate passes, before the dispatcher seam"
      pattern: "deriveGlobalWaves"
    - from: "deriveGlobalWaves"
      to: "Wave CR tide-wave-<project>-<N>"
      via: "Create with EnsureOwnerRef project + WaveIndex; prune index >= len(waves)"
      pattern: "tide-wave"
    - from: "stampGlobalTaskLabels"
      to: "Task tideproject.k8s/wave-index label"
      via: "client.MergeFrom + Patch"
      pattern: "wave-index"
---

<objective>
Add the global wave-derivation engine to ProjectReconciler: after checkGlobalCycleGate passes, call pkg/dag.ComputeWaves on the assembled (nodes,edges) from Plan 02, reconcile the global Wave CR set (tide-wave-<project>-<globalIndex>, Project-owned, create/prune), and stamp each Task's GLOBAL tideproject.k8s/wave-index label. Add Owns(&Wave{}) to SetupWithManager. No schedule is cached (D-05/PERSIST-03); re-derivation is idempotent and O(V+E) (D-10).

Purpose: EXEC-02 (global monotonic wave indices + Project-scoped Wave CRs), EXEC-03 (bidirectional index restores README:54 invariant Project-wide), EXEC-04 (re-derive on every task add/complete, no cache).
Output: deriveGlobalWaves + stampGlobalTaskLabels on ProjectReconciler, wired into Reconcile; Owns(&Wave{}) added; Plan 01's global-derivation envtest goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-global-wave-derivation-engine/24-CONTEXT.md
@.planning/phases/24-global-wave-derivation-engine/24-RESEARCH.md
@.planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md

<interfaces>
<!-- Port source: plan_controller.go materializeWaves (1339-1412) + stampTaskLabels (1421-1455). -->
pkg/dag.ComputeWaves(nodes []NodeID, edges []Edge) ([][]NodeID, error)  // layers = waves, lexicographic, O(V+E), CycleError on cycle
api/v1alpha2.WaveSpec{ProjectRef string, WaveIndex int}
owner.EnsureOwnerRef(obj, ownerObj, scheme) error   // sets controller ownerRef, BlockOwnerDeletion
owner.LabelProject = "tideproject.k8s/project"
internal/metrics.WavesDispatchedTotal  // CounterVec labels {project, phase, plan} — arity LOCKED at 3
Wave-index label key (string literal): "tideproject.k8s/wave-index"
Reconcile: assembler runs once (Plan 02); the (nodes,edges) result is reachable at the cycle-gate call site (Reconcile ~line 259). The dispatcher seam (if r.Dispatcher != nil { return r.reconcileProjectPhase2(...) }) is at ~line 264 — derivation MUST run before it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveGlobalWaves + Wave CR reconcile/prune + idempotent metric</name>
  <files>internal/controller/project_controller.go</files>
  <behavior>
    - ComputeWaves over the assembled global (nodes,edges) yields layers; layer i becomes global wave i.
    - For each layer i: a Wave CR named tide-wave-PROJECT-i exists with Spec.ProjectRef==project.Name, Spec.WaveIndex==i, owner-ref to Project (BlockOwnerDeletion).
    - Create path increments WavesDispatchedTotal exactly once (sentinel "global" for phase and plan labels, never empty); AlreadyExists and reconcile-replay (Get-success) paths do NOT increment.
    - Re-derivation with fewer waves prunes Wave CRs whose WaveIndex is at or above len(waves) (persisted Wave set == current derivation, no orphans).
    - Identical inputs across reconciles produce no Wave churn and no metric double-count (idempotent).
  </behavior>
  <read_first>
    - internal/controller/project_controller.go (the Reconcile cycle-gate call site ~259 and dispatcher seam ~264 from Plan 02; the assembled (nodes,edges) local)
    - internal/controller/plan_controller.go lines 1339-1412 (materializeWaves — the EXACT idempotent Get/Create/AlreadyExists + exactly-once-on-Create metric pattern to PORT; note the sentinel rule at ~1351-1361, never emit an empty label value)
    - .planning/phases/24-global-wave-derivation-engine/24-RESEARCH.md (OQ-4 port pattern + prune mechanic + Pitfall 3 metric sentinel global + Pitfall 5 prune-race idempotency note + Open Question 3 prune-comment-for-Phase-25)
    - .planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md (deriveGlobalWaves port section + prune loop + Owner-ref/metric-sentinel shared patterns)
    - internal/metrics/registry.go lines ~132-138 (WavesDispatchedTotal arity {project,phase,plan} — LOCKED at 3)
    - internal/owner/label.go (EnsureOwnerRef, LabelProject)
  </read_first>
  <action>
    Add deriveGlobalWaves(ctx, project, nodes, edges) on ProjectReconciler, ported from materializeWaves. Call dag.ComputeWaves(nodes, edges) (D-11, reused unchanged); treat a dag.CycleError defensively (it should not reach here — the gate caught it). For each layer index i, build a Wave with Spec WaveSpec{ProjectRef: project.Name, WaveIndex: i} named via fmt.Sprintf with format tide-wave-%s-%d (project.Name, i). Port the Get then NotFound then EnsureOwnerRef(wave, project, r.Scheme) then Create pattern; on Create success increment metrics.WavesDispatchedTotal.WithLabelValues(project.Name, "global", "global") exactly once; on apierrors.IsAlreadyExists treat as idempotent success (no increment); on Get-success ensure owner-ref only (no increment). Use sentinel "global" for phase/plan (Pitfall 3 — never emit an empty label; the metriccardinality analyzer forbids only the task label). After the create loop, prune: List Waves by client.MatchingLabels{owner.LabelProject: project.Name} and r.Delete any with Spec.ProjectRef==project.Name and Spec.WaveIndex at or above len(layers) (ignore NotFound). Add a code comment that Phase 25 should gate prune on Wave succeeded-status to avoid pruning an in-flight Wave (RESEARCH Open Question 3). Wire the call into Reconcile AFTER checkGlobalCycleGate passes and BEFORE the dispatcher seam (if r.Dispatcher != nil), passing the already-assembled (nodes,edges). Add Owns(&tidev1alpha2.Wave{}) to SetupWithManager so Project-owned Wave changes re-enqueue the Project. Do NOT add any api type field; the global index lives only as WaveSpec.WaveIndex (scalar) + the per-Task label (Task 2).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0.
    - grep -c 'func (r \*ProjectReconciler) deriveGlobalWaves' internal/controller/project_controller.go returns 1.
    - Global Wave naming present in non-comment code: grep -vE '^[[:space:]]*//' internal/controller/project_controller.go piped to grep -c 'tide-wave-%s-%d' returns 1 or more.
    - Metric sentinel present, no empty label: grep -c 'WithLabelValues(project.Name, "global", "global")' internal/controller/project_controller.go returns 1 or more; grep -c 'WithLabelValues(project.Name, "", ' internal/controller/project_controller.go returns 0.
    - Prune present: grep -c 'WaveIndex >= len(' internal/controller/project_controller.go returns 1 or more.
    - Owns Wave added: grep -c 'Owns(&tidev1alpha2.Wave{})' internal/controller/project_controller.go returns 1.
    - Derivation wired before the dispatcher seam (line of deriveGlobalWaves call is below the assembler and above the r.Dispatcher check in Reconcile) — confirm by reading the Reconcile body and recording the line ordering in the SUMMARY.
    - make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep exits 0.
    - go test ./internal/controller/... exits 0 (no controller unit regressions).
  </acceptance_criteria>
  <done>deriveGlobalWaves derives waves via ComputeWaves, reconciles the Wave CR set (create/prune) with exactly-once global-sentinel metric, is wired after the cycle gate and before dispatch, adds Owns Wave, and keeps all verify guards green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stampGlobalTaskLabels — global wave-index label + bidirectional index GREEN</name>
  <files>internal/controller/project_controller.go</files>
  <behavior>
    - Each Task in global wave i carries label tideproject.k8s/wave-index == "i" and tideproject.k8s/project == PROJECT (task to wave).
    - A label selector {wave-index: "i", project: PROJECT} returns exactly the Tasks of global wave i (wave to tasks).
    - Re-derivation re-stamps changed indices; a Task whose index is unchanged is not re-patched (no churn).
    - Adding a Task triggers ProjectReconciler (existing taskToProject watch); the whole schedule re-derives O(V+E); no schedule cached in Project.status.
  </behavior>
  <read_first>
    - internal/controller/plan_controller.go lines 1421-1455 (stampTaskLabels — the EXACT MergeFrom+Patch + skip-if-already-correct pattern to PORT)
    - internal/controller/project_controller.go (deriveGlobalWaves from Task 1; the assembled taskList.Items reachable from the assembler — pass Task objects through to avoid redundant Gets)
    - test/integration/envtest/global_wave_derivation_test.go (Plan 01 — the GREEN target: GlobalDag/GlobalWaveIndex/BidirectionalIndex/WaveRederivation assertions)
    - .planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md (stampGlobalTaskLabels port section + Task-label-patch shared pattern)
  </read_first>
  <action>
    Add stampGlobalTaskLabels(ctx, tasks []tidev1alpha2.Task, globalWaves [][]dag.NodeID, projectName string) on ProjectReconciler, ported verbatim in idiom from stampTaskLabels: build a name-to-globalWaveIndex map from globalWaves, then for each Task patch tideproject.k8s/wave-index = fmt.Sprintf("%d", idx) and owner.LabelProject = projectName using client.MergeFrom(t.DeepCopy()) + r.Patch, SKIPPING the patch when both labels are already correct (idempotent, no churn). Pass the Task objects already held by the assembler (from taskList.Items in Plan 02) so no redundant Gets are issued. Call stampGlobalTaskLabels from deriveGlobalWaves (or immediately after it in Reconcile) with the same globalWaves layers. Confirm nothing writes the schedule to Project.Status (the wave-index lives only on Task labels + WaveSpec.WaveIndex). The stamped index is the GLOBAL index — this becomes the only writer of the wave-index label once Plan 04 removes the per-plan stamper.
  </action>
  <verify>
    <automated>go build ./... && go test ./test/integration/envtest/... -run 'GlobalDag|GlobalWaveIndex|BidirectionalIndex|WaveRederivation' -count=1 -timeout 300s</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0.
    - grep -c 'func (r \*ProjectReconciler) stampGlobalTaskLabels' internal/controller/project_controller.go returns 1.
    - The MergeFrom+Patch idiom is used (not Update): grep -c 'client.MergeFrom' internal/controller/project_controller.go returns 1 or more in the stamper path.
    - Plan 01 envtest goes GREEN: go test ./test/integration/envtest/... -run 'GlobalDag|GlobalWaveIndex|BidirectionalIndex|WaveRederivation' -count=1 -timeout 300s exits 0; the log shows no lines matching '^--- FAIL' or '^FAIL\s'.
    - Bidirectional proven by the BidirectionalIndex spec: task->wave label assertion and wave->tasks label-selector assertion both pass (in the GREEN run above).
    - No schedule cached: make verify-no-aggregates exits 0 (still green after Task 2).
  </acceptance_criteria>
  <done>stampGlobalTaskLabels stamps the global wave-index label idempotently; the README:54 bidirectional invariant holds Project-wide; Plan 01's GlobalDag/GlobalWaveIndex/BidirectionalIndex/WaveRederivation specs are GREEN; verify-no-aggregates stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived state -> persistence | Derived waves/labels could be cached; D-05/PERSIST-03 forbid it. The only writes are Wave CR spec (scalar WaveIndex) + per-Task label. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Tampering | cached schedule in Project.status | mitigate | D-05/PERSIST-03 — no aggregate field; index lives only as WaveSpec.WaveIndex + Task label; acceptance criteria assert verify-no-aggregates stays green. |
| T-24-03-02 | Repudiation/Integrity | metric double-count on re-derivation replay | mitigate | Exactly-once-on-Create increment (ported from materializeWaves); AlreadyExists and Get-success paths do not increment (RESEARCH OQ-4). |
| T-24-03-03 | Denial of Service | Wave prune flap under rapid re-enqueue | accept | Idempotent reconcile + controller-runtime WorkQueue dedup bound the flap; corrects on next pass (RESEARCH Pitfall 5). No new input surface. |
| T-24-03-04 | Tampering | adversarial cross-scope cycle reaching derivation | mitigate | checkGlobalCycleGate runs BEFORE deriveGlobalWaves and blocks on CycleError; deriveGlobalWaves treats CycleError defensively (RESEARCH Security Domain). |
</threat_model>

<verification>
- go build ./... and go test ./internal/controller/... green.
- Plan 01 global-derivation envtest specs GREEN (GlobalDag/GlobalWaveIndex/BidirectionalIndex/WaveRederivation).
- make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep green.
- Read MAKE_EXIT and grep for '^--- FAIL|^FAIL\s' in the int log, not just the Ginkgo summary (CLAUDE.md protocol).
</verification>

<success_criteria>
EXEC-02/03/04 met: global monotonic Wave CRs (tide-wave-<project>-<N>) owned by the Project, bidirectional global wave index via labels, re-derivation O(V+E) with no cached schedule. The README:54 namesake invariant holds Project-wide and Plan 01's contract is GREEN.
</success_criteria>

<output>
Create `.planning/phases/24-global-wave-derivation-engine/24-03-SUMMARY.md` when done. Record the GREEN int-test summary line, the Reconcile call ordering (assemble -> cycle gate -> derive -> dispatch seam), and confirm verify-no-aggregates green.
</output>
