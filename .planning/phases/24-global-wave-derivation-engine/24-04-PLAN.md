---
phase: 24-global-wave-derivation-engine
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - internal/controller/plan_controller.go
  - internal/controller/wave_controller.go
autonomous: true
requirements: [EXEC-02, EXEC-03]
must_haves:
  truths:
    - "PlanReconciler no longer creates Wave CRs or stamps wave-index labels — exactly ONE writer of Wave CRs (the ProjectReconciler)"
    - "PlanReconciler.SetupWithManager no longer declares Owns Wave (no spurious Plan reconciles on Project-owned Wave creates)"
    - "WaveReconciler's four Phase-24 TODOs are closed: ProjectRef-scoped wave->task roll-up and O(1) task->wave mapper off the global label"
    - "make test-int is green (MAKE_EXIT 0, no go-test FAIL lines) and all three verify guards pass"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "Per-plan materializeWaves/stampTaskLabels removed; Owns Wave removed; call sites neutralized"
    - path: "internal/controller/wave_controller.go"
      provides: "Four Phase-24 TODOs closed; O(1) global task->wave mapper"
      contains: "tide-wave"
  key_links:
    - from: "WaveReconciler.taskToWaveMapper"
      to: "Wave CR tide-wave-<project>-<waveIndex>"
      via: "O(1) name derivation from task labels (no list-all-Waves)"
      pattern: "tide-wave"
    - from: "WaveReconciler.reconcileObservational"
      to: "Tasks of a global wave"
      via: "label selector wave-index + project (ProjectRef-scoped)"
      pattern: "wave-index"
---

<objective>
Make the ProjectReconciler the SINGLE writer of Wave CRs (D-03): remove the superseded per-plan materializeWaves + stampTaskLabels and their call sites in reconcileWaveMaterialization, and remove Owns(&Wave{}) from PlanReconciler.SetupWithManager (Pitfall 1/6). Close the four Phase-24 TODOs in WaveReconciler so wave->task roll-up and the task->wave mapper run off the GLOBAL wave-index label. Run the full phase gate.

Purpose: EXEC-02/EXEC-03 — eliminate the per-plan-waves defect this milestone exists to fix; restore the bidirectional index off the global label. This plan runs LAST so the single global writer (Plan 03) exists before the per-plan one is removed.
Output: per-plan Wave creation deleted/neutralized; Owns Wave removed from PlanReconciler; WaveReconciler TODOs closed; make test-int + verify guards green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-global-wave-derivation-engine/24-CONTEXT.md
@.planning/phases/24-global-wave-derivation-engine/24-RESEARCH.md
@.planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md
@.planning/phases/24-global-wave-derivation-engine/24-VALIDATION.md

<interfaces>
<!-- Verified current line locations. -->
internal/controller/plan_controller.go:
  - materializeWaves — func at line 1347 (creates tide-wave-<plan.UID>-<i>, Plan-owned)
  - stampTaskLabels — func at line ~1421
  - reconcileWaveMaterialization — body 1014-1165; calls materializeWaves at 1092 and stampTaskLabels at 1155
  - SetupWithManager — func at 1488; Owns(&tideprojectv1alpha2.Wave{}) at line 1498 (REMOVE)
internal/controller/wave_controller.go:
  - TODO(phase-24) at lines 104, 134, 236, 248
  - reconcileObservational — body 133-230; label query at ~152-160 already correct (wave-index + project), just remove TODO 134
  - taskToWaveMapper — func at 240-263; currently lists ALL Waves in namespace; replace with O(1) name derivation
Global Wave naming convention (from Plan 03): tide-wave-<project>-<waveIndex>
owner.LabelProject = "tideproject.k8s/project"; wave-index label key (literal): "tideproject.k8s/wave-index"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove per-plan Wave writer + Owns Wave from PlanReconciler</name>
  <files>internal/controller/plan_controller.go</files>
  <read_first>
    - internal/controller/plan_controller.go lines 1009-1165 (reconcileWaveMaterialization — the call sites at 1092 materializeWaves and 1155 stampTaskLabels), 1339-1455 (materializeWaves + stampTaskLabels function bodies), 1488-1512 (SetupWithManager, Owns Wave at 1498)
    - .planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md (plan_controller.go removal section — the grep-confirmation invariant + the critical Owns removal at 1498)
    - .planning/phases/24-global-wave-derivation-engine/24-RESEARCH.md (Pitfall 1, Pitfall 6, D-03 — exactly one Wave writer; deprecated list)
  </read_first>
  <action>
    Remove materializeWaves (the function) and stampTaskLabels (the function) from plan_controller.go. Neutralize their call sites inside reconcileWaveMaterialization: the body that computed per-plan layers and called these two functions (around lines 1092 and 1155) must no longer create Wave CRs or stamp wave-index labels — global derivation now owns both. Preserve all OTHER reconcileWaveMaterialization responsibilities that are NOT wave-creation/label-stamping (e.g. the ValidationState gating, the Plan succeeded/awaiting-approval transitions, child-task counting) — only the wave-materialization + label-stamping steps are removed. If layer computation in that body existed solely to feed those two removed calls, remove it too; if it also feeds a still-needed step, keep the minimum. Remove Owns(&tideprojectv1alpha2.Wave{}) from SetupWithManager (line 1498) IN THE SAME CHANGE (Pitfall 1 — leaving it causes spurious Plan reconciles on every Project-owned Wave create). Remove the now-dead WavesDispatchedTotal per-plan increment along with materializeWaves. Ensure no orphaned imports remain (run goimports/go build). Leave a brief comment at the former materialization site pointing to ProjectReconciler.deriveGlobalWaves as the single global writer.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/... && go test ./internal/controller/... -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0; go vet ./internal/controller/... exits 0 (no unused imports/vars left behind).
    - The per-plan Wave writer is gone: grep -c 'func (r \*PlanReconciler) materializeWaves' internal/controller/plan_controller.go returns 0; grep -c 'func (r \*PlanReconciler) stampTaskLabels' internal/controller/plan_controller.go returns 0.
    - No remaining Wave-create or wave-index-stamp call site: grep -cE 'materializeWaves\(|stampTaskLabels\(' internal/controller/plan_controller.go returns 0.
    - Owns Wave removed: grep -c 'Owns(&tideprojectv1alpha2.Wave{})' internal/controller/plan_controller.go returns 0.
    - No per-plan metric increment remains: grep -c 'WavesDispatchedTotal' internal/controller/plan_controller.go returns 0.
    - go test ./internal/controller/... exits 0 (Plan reconcile non-wave responsibilities still pass).
  </acceptance_criteria>
  <done>PlanReconciler no longer creates Wave CRs, no longer stamps wave-index labels, and no longer Owns Wave; the ProjectReconciler is the single Wave writer; build and controller unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Close the four WaveReconciler Phase-24 TODOs (global bidirectional index)</name>
  <files>internal/controller/wave_controller.go</files>
  <read_first>
    - internal/controller/wave_controller.go lines 76-130 (Reconcile + TODO 104), 130-230 (reconcileObservational + TODO 134 + the label query at ~152-160), 232-263 (taskToWaveMapper + TODOs 236/248), 271-290 (SetupWithManager)
    - .planning/phases/24-global-wave-derivation-engine/24-PATTERNS.md (wave_controller.go section — exact fix for each of the four TODOs incl. the O(1) mapper replacement; pattern source project_controller.go taskToProject)
    - internal/controller/project_controller.go (taskToProject mapper shape — read one label, build one NamespacedName; the global Wave naming tide-wave-<project>-<idx> from Plan 03)
  </read_first>
  <action>
    Close all four TODO(phase-24) sites:
    - Line 104: remove the TODO. ProjectReconciler.deriveGlobalWaves sets the owner-ref at create time, so no parent walk is needed here. Optionally add a small fallback: if wave.OwnerReferences is empty and wave.Spec.ProjectRef is non-empty, fetch the Project and EnsureOwnerRef (same idiom as plan_controller's owner-ref ensure).
    - Line 134: remove the TODO. The existing label query in reconcileObservational (list Tasks by tideproject.k8s/wave-index == wave.Spec.WaveIndex AND owner.LabelProject == wave.Spec.ProjectRef) is already the correct GLOBAL query now that ProjectReconciler stamps global indices — confirm it reads wave.Spec.ProjectRef (not a PlanRef) and leave the query in place.
    - Lines 236 and 248 (taskToWaveMapper): replace the list-ALL-Waves-in-namespace body with an O(1) name derivation: read the Task's owner.LabelProject and "tideproject.k8s/wave-index" labels; if either is empty, return nil; else construct waveName via fmt.Sprintf format tide-wave-%s-%s (projectName, waveIndexStr) and return a single reconcile.Request for {task.Namespace, waveName}. Mirror the taskToProject mapper shape. Remove both TODO comments. Drop the now-unused PlanRef gate and the WaveList listing.
    Run go build + go vet to confirm no unused vars/imports remain after the mapper rewrite.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/... && go test ./test/integration/envtest/... -run 'GlobalDag|GlobalWaveIndex|BidirectionalIndex|WaveRederivation' -count=1 -timeout 300s</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... and go vet ./internal/controller/... exit 0.
    - All four Phase-24 TODOs closed: grep -c 'TODO(phase-24)' internal/controller/wave_controller.go returns 0.
    - O(1) mapper in place (no list-all-Waves): grep -c 'tide-wave-%s-%s' internal/controller/wave_controller.go returns 1 or more; record in SUMMARY that the WaveList listing was removed from taskToWaveMapper.
    - reconcileObservational query is ProjectRef-scoped: grep -c 'wave.Spec.ProjectRef' internal/controller/wave_controller.go returns 1 or more.
    - Plan 01 global-derivation envtest stays GREEN: the run above exits 0 with no lines matching '^--- FAIL' or '^FAIL' followed by whitespace.
  </acceptance_criteria>
  <done>All four WaveReconciler Phase-24 TODOs are closed; wave->task roll-up is ProjectRef-scoped off the global label; task->wave mapper is O(1) name derivation; the global-derivation envtest stays GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase gate + guard sweep</name>
  <files>internal/controller/plan_controller.go, internal/controller/wave_controller.go</files>
  <read_first>
    - .planning/phases/24-global-wave-derivation-engine/24-VALIDATION.md (phase-gate sampling: make test-int green AND verify guards green; the MAKE_EXIT + grep '^--- FAIL|^FAIL' protocol)
    - CLAUDE.md (Verify Before Claiming — make test-int exit != Ginkgo green; read MAKE_EXIT and grep the log for go-test FAIL lines, not just the Ginkgo summary)
  </read_first>
  <action>
    Run the full phase gate and resolve anything red. Execute make test-int capturing its exit code (MAKE_EXIT) to a log; the package bundles plain go-tests (helm-template contracts) alongside Ginkgo specs, so a RED go-test fails the package even when Ginkgo prints SUCCESS — read MAKE_EXIT AND grep the log for '^--- FAIL' / '^FAIL' followed by whitespace, not just the Ginkgo summary. Run make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep. Fix any regression introduced by Plans 02-04 (e.g. an existing per-plan wave test that assumed Plan-owned Wave naming and must now expect Project-owned global naming, or a fixture relying on the removed materializeWaves path). Do NOT weaken or skip a failing test to make the gate pass; correct the code or update the test to the new global contract with justification recorded in the SUMMARY. This task may only touch test files and the two controller files already in this plan's files_modified; if a fix requires editing project_controller.go, flag it as gap-closure (wave conflict with Plans 02/03) rather than editing here.
  </action>
  <verify>
    <automated>make test-int 2>&1 | tee /tmp/24-04-testint.log; echo "MAKE_EXIT=${PIPESTATUS[0]}"; grep -nE '^--- FAIL|^FAIL[[:space:]]' /tmp/24-04-testint.log; make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep</automated>
  </verify>
  <acceptance_criteria>
    - make test-int completes with MAKE_EXIT 0 (read from PIPESTATUS, NOT the Ginkgo summary line).
    - grep -nE '^--- FAIL|^FAIL[[:space:]]' /tmp/24-04-testint.log returns nothing (no bundled go-test failed).
    - The log contains a Ginkgo summary line matching 'Ran .* Specs .* SUCCESS!'.
    - make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep exits 0 (all three guards green).
    - The global-derivation specs (GlobalDag/GlobalWaveIndex/BidirectionalIndex/WaveRederivation) are green within the suite.
    - No test was skipped or weakened to pass; any test updated to the global contract is justified in the SUMMARY.
  </acceptance_criteria>
  <done>make test-int is green by the CLAUDE.md protocol (MAKE_EXIT 0, no go-test FAIL lines, Ginkgo SUCCESS) and all three verify guards pass; no test skipped/weakened.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dual Wave writers -> single writer | Removing the per-plan writer must not leave a window with zero writers or two writers. Plan ordering (04 after 03) guarantees the global writer exists first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Tampering | two Wave writers (per-plan + global) coexisting | mitigate | D-03 — per-plan path removed in this plan; acceptance grep asserts materializeWaves/stampTaskLabels/Owns Wave are all gone; ProjectReconciler is sole writer. |
| T-24-04-02 | Denial of Service | spurious Plan reconciles on every Project-owned Wave create | mitigate | Remove Owns(&Wave{}) from PlanReconciler in the same change as materializeWaves removal (Pitfall 1/6). |
| T-24-04-03 | Tampering | green-washing a regressed gate (Ginkgo SUCCESS hiding a go-test FAIL) | mitigate | Task 3 acceptance enforces MAKE_EXIT + grep '^--- FAIL|^FAIL' protocol, not just the Ginkgo summary (CLAUDE.md); no test may be skipped/weakened to pass. |
</threat_model>

<verification>
- go build ./..., go vet ./internal/controller/... green.
- All per-plan Wave writer artifacts gone (grep counts 0); Owns Wave removed; four TODOs closed.
- make test-int: MAKE_EXIT 0, no '^--- FAIL|^FAIL' lines, Ginkgo SUCCESS.
- make verify-no-aggregates verify-dag-imports verify-no-sqlite-dep green.
</verification>

<success_criteria>
The per-plan-waves defect is fully removed: ProjectReconciler is the single Wave writer, PlanReconciler no longer owns or creates Waves, the WaveReconciler bidirectional index runs off the global label, and the full integration gate (test-int + verify guards) is genuinely green by the CLAUDE.md protocol.
</success_criteria>

<output>
Create `.planning/phases/24-global-wave-derivation-engine/24-04-SUMMARY.md` when done. Record MAKE_EXIT, the Ginkgo SUCCESS line, the grep-FAIL result (empty), the three verify-guard exits, and confirmation that taskToWaveMapper no longer lists all Waves.
</output>
