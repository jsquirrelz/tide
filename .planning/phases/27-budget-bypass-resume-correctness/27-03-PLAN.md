---
phase: 27-budget-bypass-resume-correctness
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - internal/controller/project_controller.go
  - internal/controller/project_planner_completion_test.go
autonomous: true
requirements: [BYPASS-03, BYPASS-05]
must_haves:
  truths:
    - "Planner-level Usage rolls up exactly once across a halt→resume cycle even when the reporter Job has TTL-GC'd (D-03)"
    - "The rollup-once guard is the durable PlannerRolledUpUID marker, not reporter-Job existence"
    - "PlannerRolledUpUID is set only after a successful RollUpUsage, and never cleared on bypass"
    - "A TTL-GC companion envtest proves the durable marker rolls up exactly once when the planner Job is absent (BYPASS-05)"
  artifacts:
    - path: "internal/controller/project_controller.go"
      provides: "PlannerRolledUpUID-gated rollup in handleProjectJobCompletion"
      contains: "PlannerRolledUpUID"
    - path: "internal/controller/project_planner_completion_test.go"
      provides: "BYPASS-03 double-count spec + BYPASS-05 TTL-GC companion scenario"
      contains: "PlannerRolledUpUID"
  key_links:
    - from: "internal/controller/project_controller.go handleProjectJobCompletion"
      to: "Status.Budget.PlannerRolledUpUID"
      via: "rollup idempotency guard keyed on planner job name tide-project-<uid>-1"
      pattern: "PlannerRolledUpUID"
    - from: "internal/controller/project_controller.go"
      to: "budget.RollUpUsage"
      via: "marker patched only on rollup success"
      pattern: "RollUpUsage"
---

<objective>
Make planner-level Usage roll up exactly once across a halt→resume cycle by replacing the
reporter-Job-existence `isFirstCompletion` signal with the durable `PlannerRolledUpUID` marker
(added in Plan 01), and add the BYPASS-05 TTL-GC companion envtest scenario.

Purpose: BYPASS-03 — after a halt long enough for the reporter Job's 300s TTL to expire,
`isFirstCompletion` flips true again and `budget.RollUpUsage` fires a second time, double-counting
planning cost. BYPASS-05 — the existing QQH-01 test (confirmed GREEN in Plan 01) covers the
planner-Job-present path; this plan adds the planner-Job-absent (`nil`, GC'd) companion that proves
the durable marker still rolls up exactly once.
Output: A `PlannerRolledUpUID`-gated rollup in `handleProjectJobCompletion`, set only on rollup
success and never cleared on bypass, plus two new test specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-budget-bypass-resume-correctness/27-CONTEXT.md
@.planning/phases/27-budget-bypass-resume-correctness/27-PATTERNS.md
@.planning/phases/27-budget-bypass-resume-correctness/27-RESEARCH.md

<interfaces>
From internal/controller/project_controller.go:
- `handleProjectJobCompletion(ctx, project, completedJob *batchv1.Job)` (~1117-1182): nil-safe in `completedJob`. `isFirstCompletion` (~1145-1175) is set true when reporter Job `IsNotFound`. Rollup at ~1178-1182: `if isFirstCompletion && envReadOK { budget.RollUpUsage(ctx, r.Client, project, out.Usage) }` (non-fatal-logged on error).
- Planner job name (~:955): `jobName := fmt.Sprintf("tide-project-%s-1", project.UID)` — the deterministic rollup marker key.
- TTL/GC fallthrough (~969-972): `handleProjectJobCompletion(ctx, project, nil)` is the GC'd-planner-Job path.
- Status-patch idiom: `client.MergeFrom(project.DeepCopy())` + `r.Status().Patch`. Non-fatal logging: `logger.Error(err, "... (non-fatal)", ...)`.

From test helpers (use directly):
- `internal/controller/project_planner_completion_test.go`: `qqhCreateProject(ctx, name)`, `qqhBuildReconciler(envReader)`, `newMapEnvReader()`, `envReader.SetOut(uid, EnvelopeOut{...Usage...})`, `qqhCleanupProject`, primary + control Describe blocks.
- `internal/controller/milestone_controller_test.go`: `makeFakeJobTerminal(ctx, c, name, ns, succeeded)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BYPASS-03 — PlannerRolledUpUID rollup-once guard in handleProjectJobCompletion</name>
  <files>internal/controller/project_controller.go</files>
  <read_first>
    internal/controller/project_controller.go (handleProjectJobCompletion 1117-1182, isFirstCompletion 1145-1175, rollup 1178-1182, planner job name :955);
    .planning/phases/27-budget-bypass-resume-correctness/27-PATTERNS.md (Change C);
    27-CONTEXT.md D-03; 27-RESEARCH.md Pitfall 2 (set marker only on rollup success) + the "never clear on bypass" rule
  </read_first>
  <behavior>
    - When `PlannerRolledUpUID == jobName`, `budget.RollUpUsage` is NOT called (rollup already happened).
    - When `PlannerRolledUpUID != jobName` and `RollUpUsage` succeeds, `PlannerRolledUpUID` is patched to `jobName`.
    - When `RollUpUsage` returns an error, `PlannerRolledUpUID` is left unchanged so the next reconcile retries (Pitfall 2).
    - `jobName` is the deterministic planner name `tide-project-<uid>-1`.
  </behavior>
  <action>
    In `handleProjectJobCompletion`, wrap the existing `budget.RollUpUsage` call so it runs only when
    `project.Status.Budget.PlannerRolledUpUID != jobName` (jobName = `fmt.Sprintf("tide-project-%s-1", project.UID)`,
    matching :955). On successful rollup (`rollErr == nil`), patch `project.Status.Budget.PlannerRolledUpUID = jobName`
    via `client.MergeFrom(project.DeepCopy())` + `r.Status().Patch`; on rollup error, log non-fatally and
    leave the marker unset (Pitfall 2). Keep the existing `isFirstCompletion && envReadOK` outer condition;
    the marker is an additional inner idempotency guard, not a replacement for `envReadOK`. Do NOT add any
    code path that clears `PlannerRolledUpUID` (it must persist across halt→resume per D-03). Before storing,
    confirm the value matches the `tide-project-<uid>-1` shape (it is constructed from `project.UID`, so this
    is a construction-site invariant, not external input — note this in the SUMMARY for T-27-03-01).
  </action>
  <verify>
    <automated>cd internal/controller && go test -run TestControllers -timeout 5m . -ginkgo.focus="BYPASS-03|double-count|rollup" 2>&1 | grep -E "SUCCESS!|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `project_controller.go` guards `budget.RollUpUsage` with `project.Status.Budget.PlannerRolledUpUID != jobName`
    - The marker is patched only inside the `rollErr == nil` branch
    - No code path clears `PlannerRolledUpUID`
    - `grep -c 'PlannerRolledUpUID' internal/controller/project_controller.go` ≥ 2 (check + set)
  </acceptance_criteria>
  <done>Planner Usage rolls up at most once per planner Job name, gated by the durable marker, regardless of reporter Job TTL-GC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BYPASS-03 double-count spec + BYPASS-05 TTL-GC companion scenario</name>
  <files>internal/controller/project_planner_completion_test.go</files>
  <read_first>
    internal/controller/project_planner_completion_test.go (primary Describe 97-197, control Describe 200-263, helper calls qqhCreateProject/qqhBuildReconciler/newMapEnvReader/SetOut, AfterEach cleanup 107-119);
    .planning/phases/27-budget-bypass-resume-correctness/27-PATTERNS.md (the project_planner_completion_test.go extension section, with the TTL-GC + double-count spec shapes);
    27-CONTEXT.md D-05; 27-RESEARCH.md "BYPASS-05: QQH-01 regression test status"
  </read_first>
  <behavior>
    - Calling `handleProjectJobCompletion` twice with the GC'd-planner path (`nil` Job) and the same seeded Usage rolls up exactly once: final `CostSpentCents == plannerCostCents` (not 2×).
    - After the calls, `Status.Budget.PlannerRolledUpUID == "tide-project-<uid>-1"`.
    - The TTL-GC companion (planner Job absent / `nil`) still spawns the reporter and rolls up cost (BYPASS-05 GC path), mirroring the QQH-01 present-Job assertion.
  </behavior>
  <action>
    Add a new `Describe("BYPASS-03 / BYPASS-05 rollup-once across halt+GC", Label("envtest"))` block to
    `project_planner_completion_test.go` using the existing helpers (`qqhCreateProject`, `qqhBuildReconciler`,
    `newMapEnvReader`, `envReader.SetOut` with a `plannerCostCents` Usage). Spec 1 (BYPASS-03 double-count):
    invoke `r.handleProjectJobCompletion(ctx, proj, nil)` twice (simulating the GC'd-planner resume path),
    then `Eventually` assert `Status.Budget.CostSpentCents == plannerCostCents` (NOT 2×) and
    `Status.Budget.PlannerRolledUpUID == fmt.Sprintf("tide-project-%s-1", proj.UID)`. Spec 2 (BYPASS-05
    TTL-GC companion): with a fresh project, call `handleProjectJobCompletion(ctx, proj, nil)` once and
    assert the reporter Job spawns AND `CostSpentCents` reflects the planner spend — proving the ordering
    fix holds on the planner-Job-absent path, not just the present-Job path the QQH-01 primary spec covers.
    Reuse the AfterEach Job-prefix cleanup shape from lines 107-119. Use distinct per-spec project names
    (never reuse a constant across Describe blocks) to avoid cross-spec state leakage.
  </action>
  <verify>
    <automated>cd internal/controller && go test -run TestControllers -timeout 5m . -ginkgo.focus="rollup-once|BYPASS-03|BYPASS-05" 2>&1 | grep -E "SUCCESS!|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `project_planner_completion_test.go` has a `Label("envtest")` Describe with a double-count spec asserting `CostSpentCents == plannerCostCents` after two `nil`-Job completion calls
    - The same block asserts `PlannerRolledUpUID == tide-project-<uid>-1`
    - A BYPASS-05 TTL-GC companion spec asserts the reporter spawns + cost rolls up on the `nil`-Job path
    - Focused envtest reports Ginkgo `SUCCESS!`
  </acceptance_criteria>
  <done>The durable rollup-once marker is proven against the GC'd-planner resume path, and the ordering fix is locked in against regression on both the present- and absent-Job paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → CRD `.status` budget tally | `PlannerRolledUpUID` is controller-written from `project.UID`; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | PlannerRolledUpUID value | mitigate | Marker is constructed from `project.UID` via `fmt.Sprintf("tide-project-%s-1", ...)` (not external input); construction-site invariant confirms the `tide-project-<uid>-1` shape |
| T-27-03-02 | Repudiation | double-count of planning cost | mitigate | Durable marker makes rollup idempotent per planner Job name; cost cannot be silently inflated across halt→resume; never cleared on bypass |
</threat_model>

<verification>
- `make test-int-fast` green.
- `grep -c 'PlannerRolledUpUID' internal/controller/project_controller.go` ≥ 2.
- Double-count spec proves `CostSpentCents == plannerCostCents` after two completion calls.
</verification>

<success_criteria>
- BYPASS-03: planner Usage rolls up exactly once across halt→resume; marker durable, never cleared on bypass.
- BYPASS-05: TTL-GC companion scenario proves rollup-once on the GC'd-planner path.
</success_criteria>

<output>
Create `.planning/phases/27-budget-bypass-resume-correctness/27-03-SUMMARY.md` when done.
</output>
