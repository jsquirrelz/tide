---
phase: 27-budget-bypass-resume-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha2/project_types.go
  - api/v1alpha2/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_projects.yaml
autonomous: true
requirements: [BYPASS-02, BYPASS-03, BYPASS-04, BYPASS-05]
must_haves:
  truths:
    - "The Project CRD schema carries durable status fields CloneComplete, PlannerRolledUpUID, and BypassBaselineCents"
    - "Generated deepcopy and CRD manifest reflect the three new optional status fields"
    - "The existing QQH-01 ordering-regression envtest is confirmed GREEN at HEAD before any controller-logic change lands"
  artifacts:
    - path: "api/v1alpha2/project_types.go"
      provides: "CloneComplete bool on GitStatus; PlannerRolledUpUID string + BypassBaselineCents int64 on BudgetStatus"
      contains: "CloneComplete"
    - path: "api/v1alpha2/zz_generated.deepcopy.go"
      provides: "DeepCopy coverage for the new fields"
    - path: "config/crd/bases/tideproject.k8s_projects.yaml"
      provides: "Regenerated CRD with cloneComplete, plannerRolledUpUID, bypassBaselineCents"
      contains: "cloneComplete"
  key_links:
    - from: "api/v1alpha2/project_types.go"
      to: "config/crd/bases/tideproject.k8s_projects.yaml"
      via: "make manifests"
      pattern: "cloneComplete|plannerRolledUpUID|bypassBaselineCents"
---

<objective>
Land the three additive `+optional omitempty` CRD status fields that the Wave 2+ controller-logic
fixes depend on, regenerate the CRD manifest and deepcopy code, and confirm the existing QQH-01
ordering-regression envtest is GREEN before any behavioral change lands.

Purpose: D-06 requires the schema fields to exist and compile before the dependent controller logic
(BYPASS-02/03/04) can reference them. This is the schema-foundation plan that gates the phase.
Output: Three new status fields, regenerated `zz_generated.deepcopy.go` + CRD YAML, and a confirmed
GREEN baseline for the `2a5e0dc` ordering fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-budget-bypass-resume-correctness/27-CONTEXT.md
@.planning/phases/27-budget-bypass-resume-correctness/27-PATTERNS.md

<interfaces>
From api/v1alpha2/project_types.go — existing `+optional omitempty` field shapes to mirror:

GitStatus carries `BranchName string \`json:"branchName,omitempty"\`` (string shape) and
`LeaseFailureCount int32 \`json:"leaseFailureCount,omitempty"\`` (numeric shape).
BudgetStatus carries `WindowStart *metav1.Time \`json:"windowStart,omitempty"\`` and
`CostSpentCents int64`.

New fields to add (three-line shape: doc comment, `// +optional`, field with `omitempty`):
- GitStatus.CloneComplete    bool   `json:"cloneComplete,omitempty"`        (BYPASS-02)
- BudgetStatus.PlannerRolledUpUID string `json:"plannerRolledUpUID,omitempty"` (BYPASS-03)
- BudgetStatus.BypassBaselineCents int64 `json:"bypassBaselineCents,omitempty"` (BYPASS-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three durable status fields to project_types.go and regenerate</name>
  <files>api/v1alpha2/project_types.go, api/v1alpha2/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_projects.yaml</files>
  <read_first>
    api/v1alpha2/project_types.go (GitStatus ~234-250, BudgetStatus ~252-269 — the analog structs);
    .planning/phases/27-budget-bypass-resume-correctness/27-PATTERNS.md (section "api/v1alpha2/project_types.go — two new +optional omitempty status fields" for the exact three-line shape and placement);
    Makefile lines 52-70 (manifests + generate targets)
  </read_first>
  <action>
    Add `CloneComplete bool` to `GitStatus` after `LeaseFailureCount`, json tag `cloneComplete,omitempty`,
    `+optional`, doc comment referencing BYPASS-02 / durable clone-idempotency guard. Add
    `PlannerRolledUpUID string` to `BudgetStatus` after `WindowStart`, json tag `plannerRolledUpUID,omitempty`,
    `+optional`, doc comment referencing BYPASS-03 / rollup-once marker. Add `BypassBaselineCents int64`
    to `BudgetStatus` (D-04 discretionary minimal durable baseline representation), json tag
    `bypassBaselineCents,omitempty`, `+optional`, doc comment referencing BYPASS-04 / acknowledged-spend
    baseline. All three follow the existing three-line `+optional omitempty` shape used by `BranchName`,
    `LeaseFailureCount`, and `WindowStart`. Then run `make manifests && make generate` (in that order) and
    commit the regenerated `config/crd/bases/tideproject.k8s_projects.yaml` and
    `api/v1alpha2/zz_generated.deepcopy.go` together with the type change. Do NOT bump the CRD schema
    version (additive optional fields only, per D-06).
  </action>
  <verify>
    <automated>grep -q 'cloneComplete' config/crd/bases/tideproject.k8s_projects.yaml && grep -q 'plannerRolledUpUID' config/crd/bases/tideproject.k8s_projects.yaml && grep -q 'bypassBaselineCents' config/crd/bases/tideproject.k8s_projects.yaml && go build ./api/... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `api/v1alpha2/project_types.go` contains `CloneComplete bool` on GitStatus, `PlannerRolledUpUID string` and `BypassBaselineCents int64` on BudgetStatus, each `+optional` with `omitempty`
    - `config/crd/bases/tideproject.k8s_projects.yaml` contains `cloneComplete`, `plannerRolledUpUID`, and `bypassBaselineCents`
    - `api/v1alpha2/zz_generated.deepcopy.go` is regenerated (no stale fields)
    - `go build ./api/...` exits 0
    - No CRD `APIVersion`/storage-version change introduced
  </acceptance_criteria>
  <done>The three additive status fields exist on the v1alpha2 schema, the CRD manifest and deepcopy are regenerated, and the package builds.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm QQH-01 ordering-regression envtest is GREEN at HEAD (BYPASS-05 baseline)</name>
  <files>internal/controller/project_planner_completion_test.go (read-only verification)</files>
  <read_first>
    internal/controller/project_planner_completion_test.go (the QQH-01 primary + control Describe blocks committed in 2a5e0dc);
    .planning/phases/27-budget-bypass-resume-correctness/27-RESEARCH.md (section "BYPASS-05: QQH-01 regression test status")
  </read_first>
  <action>
    Run the focused envtest for the QQH-01 planner-completion spec to confirm it is GREEN on the
    new schema (after Task 1 regen). This is the BYPASS-05 "confirm GREEN before adding the TTL-GC
    companion" gate from D-05; the TTL-GC companion scenario itself is added in Plan 03. Do not modify
    the test in this plan — only confirm it passes against the regenerated CRD so the schema change did
    not break the existing ordering assertion. Record the Ginkgo summary line in the plan SUMMARY.
  </action>
  <verify>
    <automated>cd internal/controller && go test -run TestControllers -timeout 5m . -ginkgo.focus="QQH-01|planner.*completion|reporter" 2>&1 | grep -E "SUCCESS!|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - The QQH-01 planner-completion envtest spec runs and reports Ginkgo `SUCCESS!` (no `FAIL`)
    - The test runs against the regenerated CRD from Task 1 (manifests/generate already applied)
    - The plan SUMMARY records the Ginkgo `Ran X of Y Specs` summary line for the focused run
  </acceptance_criteria>
  <done>The existing ordering-regression test is confirmed GREEN on the new schema, establishing the BYPASS-05 baseline before dependent logic lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → CRD `.status` | New status fields are controller-written only; no external input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | new `.status` fields | accept | Fields are Job-name strings / counters / bools, no PII or secrets; readable only via existing K8s RBAC, no new permissions |
| T-27-02 | Tampering | CRD schema regen | mitigate | `make manifests && make generate` are deterministic codegen; CI `make test` re-runs both and fails on stale generated files |
</threat_model>

<verification>
- `go build ./api/...` and `go vet ./api/...` clean.
- `grep -c 'cloneComplete\|plannerRolledUpUID\|bypassBaselineCents'` on the CRD YAML returns 3.
- QQH-01 focused envtest reports Ginkgo SUCCESS.
</verification>

<success_criteria>
- Three additive optional status fields exist and compile.
- CRD manifest + deepcopy regenerated and committed with the type change.
- QQH-01 ordering-regression test confirmed GREEN at HEAD on the new schema.
</success_criteria>

<output>
Create `.planning/phases/27-budget-bypass-resume-correctness/27-01-SUMMARY.md` when done.
</output>
