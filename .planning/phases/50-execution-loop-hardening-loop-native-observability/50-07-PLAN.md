---
phase: 50-execution-loop-hardening-loop-native-observability
plan: 07
type: execute
wave: 3
depends_on: ["50-02", "50-06"]
files_modified:
  - internal/controller/reporter_jobspec.go
  - internal/controller/reporter_jobspec_test.go
  - internal/controller/task_controller.go
  - cmd/tide-reporter/main.go
  - cmd/tide-reporter/main_test.go
  - internal/reporter/tracesynth.go
  - internal/reporter/tracesynth_test.go
autonomous: true
requirements: [EXEC-01, OBS-01]
must_haves:
  truths:
    - "The per-call LLM spans (one per tool/action iteration) carry loop.run_id (the attemptID) and a 1-indexed loop.iteration — completing EXEC-01's span-per-iteration correlation"
    - "Loop identity reaches the separately-spawned tide-reporter Job via Args only (--attempt-id/--loop-run-id), never Env — the Phase 46 session/metadata/tags precedent exactly"
    - "The stamping is conditional: absent when attemptID is empty, never a fabricated empty value"
    - "EmitSpans's loop iterates with an index — no redundant CallSpan ordinal field is added"
  artifacts:
    - path: "internal/controller/reporter_jobspec.go"
      provides: "ReporterOptions.AttemptID/LoopRunID + Args threading"
      contains: "attempt-id"
    - path: "cmd/tide-reporter/main.go"
      provides: "--attempt-id/--loop-run-id flags through reporterConfig to EmitSpans"
      contains: "attempt-id"
    - path: "internal/reporter/tracesynth.go"
      provides: "EmitSpans signature with loop identity + indexed iteration stamping"
      contains: "LoopIteration"
  key_links:
    - from: "internal/controller/task_controller.go"
      to: "internal/controller/reporter_jobspec.go"
      via: "ReporterOptions{AttemptID, LoopRunID} at the spawn site"
      pattern: "AttemptID"
    - from: "internal/controller/reporter_jobspec.go"
      to: "cmd/tide-reporter/main.go"
      via: "--attempt-id=/--loop-run-id= Args parsed by parseFlags"
      pattern: "loop-run-id"
    - from: "internal/reporter/tracesynth.go"
      to: "pkg/otelai/attrs.go"
      via: "otelai.LoopRunID/LoopIteration on each per-call LLM span"
      pattern: "otelai\\.LoopRunID"
---

<objective>
Thread loop identity to the per-call LLM spans: `ReporterOptions` gains `AttemptID`/`LoopRunID`, `BuildReporterJob` passes them as Args, `cmd/tide-reporter` parses them into `reporterConfig`, and `EmitSpans` stamps `loop.run_id` + 1-indexed `loop.iteration` on every per-call LLM span (D-01b/D-05's correlating subset) — so Phoenix groups each tool/action iteration under its attempt. This completes EXEC-01: the iteration spans already exist (one LLM span per message pair); this plan makes them correlatable.

Mirror the Phase 46 `--session-id`/`--metadata`/`--tags` precedent EXACTLY at all four layers (ReporterOptions doc comment → Args append → parseFlags → EmitSpans conditional stamping). The reporter file convention is 100% Args-based — never Env (reporter_jobspec.go:162's documented rejection).

Output: LLM spans carrying the correlating loop subset, green across controller + reporter + cmd tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-execution-loop-hardening-loop-native-observability/50-CONTEXT.md
@.planning/phases/50-execution-loop-hardening-loop-native-observability/50-PATTERNS.md
@internal/controller/reporter_jobspec.go
@internal/controller/task_controller.go
@cmd/tide-reporter/main.go
@internal/reporter/tracesynth.go

<interfaces>
Verified shapes (2026-07-18):

From internal/controller/reporter_jobspec.go:
- `ReporterOptions` (:158-179): `SessionID`/`MetadataJSON`/`Tags` fields with the "Carried as an Arg (--session-id=), not Env... this file is 100% Args-based" doc discipline
- `BuildReporterJob` Args append (:292-306): `if opts.SessionID != "" { args = append(args, "--session-id="+opts.SessionID) }` — the exact idiom to extend
- OTLP batch ceiling comment near :322 (`OTEL_BSP_MAX_EXPORT_BATCH_SIZE=6`)

From internal/controller/task_controller.go:
- `spawnTaskTraceReporterIfNeeded` (:1082-1147): the `ReporterOptions{...}` literal at :1124-1135 (SessionID/MetadataJSON/Tags populated) — the spawn site to extend; `task.Status.Attempt` and `task.UID` in scope

From cmd/tide-reporter/main.go:
- `reporterConfig` (:88), `parseFlags(args)` (:118, `sessionID := fs.String("session-id", ...)` at :133), `run(ctx, cfg, ...)` (:175), `synthesizeSpans(ctx, cfg, stderr)` (:347) → the EmitSpans call site (:594 area)

From internal/reporter/tracesynth.go:
- `CallSpan` (:91-100) — NO ordinal field; slice order IS iteration order (RESEARCH Pitfall 5: use the range index, do not add a field)
- `EmitSpans(ctx, tracer, calls []CallSpan, artifactPath, sessionID, metadataJSON string, tags []string) error` (:594-664) — `for _, call := range calls` loop; the conditional enrichment triple (`if sessionID != ""` etc.) at the loop tail

From Plan 50-02 (pkg/otelai): `LoopRunID(id string) attribute.KeyValue`, `LoopIteration(n int) attribute.KeyValue`
From api/v1alpha3/loop_types.go:93-94: LoopStatus.Iteration is documented "1-indexed once dispatched" — the iteration numbering convention to match
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReporterOptions + Args threading + spawn-site population</name>
  <files>internal/controller/reporter_jobspec.go, internal/controller/reporter_jobspec_test.go, internal/controller/task_controller.go</files>
  <read_first>internal/controller/reporter_jobspec.go (ReporterOptions doc-comment discipline :158-179 + the Args-append idiom :292-306 — the binding precedent), internal/controller/task_controller.go :1082-1147 (the spawn-site ReporterOptions literal), internal/controller/reporter_jobspec_test.go (existing Args-assertion test shape)</read_first>
  <behavior>
    - Test 1: BuildReporterJob with AttemptID "abc-2" and LoopRunID "abc" produces container Args containing "--attempt-id=abc-2" and "--loop-run-id=abc"
    - Test 2: BuildReporterJob with empty AttemptID/LoopRunID produces Args containing NEITHER flag (absent when empty)
  </behavior>
  <action>
    `internal/controller/reporter_jobspec.go`: add `AttemptID string` and `LoopRunID string` fields to `ReporterOptions` with doc comments matching the SessionID discipline verbatim in shape: 50 D-01/D-05 — the Execution-loop attempt identity (`{taskUID}-{attempt}`, matching podjob.JobName's tuple) and its parent Task-loop run (taskUID); stamped as `loop.run_id`/`loop.iteration` context on every reporter-emitted LLM span; carried as Args (`--attempt-id=`/`--loop-run-id=`), not Env, per this file's 100% Args-based convention. In `BuildReporterJob`, extend the Args append block beside the SessionID/MetadataJSON/Tags triple with the identical conditional shape for both new options (append only when non-empty).

    `internal/controller/task_controller.go`: in `spawnTaskTraceReporterIfNeeded`'s `ReporterOptions{...}` literal (:1124), add `AttemptID: fmt.Sprintf("%s-%d", task.UID, task.Status.Attempt)` and `LoopRunID: string(task.UID)` — the same derivation `buildEnvelopeIn` uses (Plan 50-06), so a Task's AGENT span and its reporter's LLM spans carry byte-identical identity values (mirror the existing "byte-identical values" comment discipline already present for the enrichment triple at :1118-1122).

    Extend `internal/controller/reporter_jobspec_test.go` with behaviors 1-2 following the existing Args-assertion tests.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'ReporterJob|Reporter' -v 2>&1 | tail -20 && go build ./internal/controller/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'attempt-id=' internal/controller/reporter_jobspec.go` == 1 and `grep -c 'loop-run-id=' internal/controller/reporter_jobspec.go` == 1
    - The spawn-site literal in task_controller.go populates both fields from task.UID + task.Status.Attempt
    - Args tests prove presence when set AND absence when empty
    - `go test ./internal/controller/...` exits 0
  </acceptance_criteria>
  <done>Loop identity rides the reporter Job's Args exactly like the Phase 46 triple; spawn site derives the same tuple as dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tide-reporter flags + EmitSpans indexed loop stamping</name>
  <files>cmd/tide-reporter/main.go, cmd/tide-reporter/main_test.go, internal/reporter/tracesynth.go, internal/reporter/tracesynth_test.go</files>
  <read_first>cmd/tide-reporter/main.go (parseFlags :118-160, reporterConfig :88, synthesizeSpans :347 through the EmitSpans call — the full threading path), internal/reporter/tracesynth.go (EmitSpans :594-664 — the loop being indexed + the conditional-enrichment tail), internal/reporter/tracesynth_test.go (existing EmitSpans test setup with span recorder), api/v1alpha3/loop_types.go :93-94 (the 1-indexed convention)</read_first>
  <behavior>
    - Test 1: EmitSpans with attemptID "abc-2", loopRunID "abc", and 3 CallSpans emits 3 LLM spans where span N carries loop.run_id "abc-2" and loop.iteration N+1 (1-indexed: 1, 2, 3)
    - Test 2: EmitSpans with empty attemptID emits spans with NO loop.* attributes (absent when empty)
    - Test 3: parseFlags round-trips --attempt-id/--loop-run-id into reporterConfig; omitted flags yield empty strings
  </behavior>
  <action>
    `cmd/tide-reporter/main.go`: add `attempt-id` and `loop-run-id` string flags in `parseFlags` beside `session-id` (:133), with matching `reporterConfig` fields; thread both through `synthesizeSpans` to the `EmitSpans` call site — the state-instruction scope is EVERY EmitSpans call site in the repo (the production call in main.go plus every test call site in tracesynth_test.go and any main_test.go usage — grep `EmitSpans(` first and update all).

    `internal/reporter/tracesynth.go`: extend `EmitSpans`'s signature with `attemptID string, loopRunID string` parameters after `tags` (mirroring how sessionID/metadataJSON/tags were appended in Phase 46). Change the loop from `for _, call := range calls` to `for i, call := range calls`. In the conditional-enrichment tail (beside the `if sessionID != ""` triple), add: when `attemptID != ""`, stamp `otelai.LoopRunID(attemptID)` and `otelai.LoopIteration(i + 1)` (1-indexed to match LoopStatus.Iteration's documented convention — cite loop_types.go:93-94 in the comment). Per D-05 the LLM-span subset is exactly `loop.run_id` + `loop.iteration` — do NOT stamp loop.kind/parent_run_id/candidate_version/exit_reason here (those live on the AGENT span; loopRunID is accepted as a parameter for signature symmetry with the flag pair and future use, but only run_id+iteration are stamped this phase — document that in the comment). Do NOT add an ordinal field to CallSpan (RESEARCH Pitfall 5 — the range index IS the iteration number).

    Extend `internal/reporter/tracesynth_test.go` (behaviors 1-2, using the existing span-recorder idiom) and `cmd/tide-reporter/main_test.go` (behavior 3, following the existing parseFlags tests).
  </action>
  <verify>
    <automated>go test ./internal/reporter/... ./cmd/tide-reporter/... -v 2>&1 | tail -25 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/reporter/tracesynth.go` EmitSpans signature contains `attemptID string, loopRunID string` and the loop reads `for i, call := range calls`
    - `grep -c 'otelai.LoopIteration' internal/reporter/tracesynth.go` == 1 and `grep -c 'Iteration' internal/reporter/tracesynth.go` shows no new CallSpan field (struct unchanged)
    - A test asserts 1-indexed iteration values across ≥ 3 spans and attribute absence when attemptID is empty
    - `go test ./internal/reporter/... ./cmd/tide-reporter/...` exits 0 and `go build ./...` exits 0 (every call site updated)
  </acceptance_criteria>
  <done>Every per-call LLM span is correlatable to its attempt via loop.run_id + 1-indexed loop.iteration; threading is Args-based end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → reporter Job Args | identity values cross a process boundary as CLI Args on a spawned Job spec |
| reporter → OTLP export | per-span attribute payload growth vs the batch ceiling (OTEL_BSP_MAX_EXPORT_BATCH_SIZE=6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-17 | Denial of Service | per-span payload growth exceeding the OTLP batch ceiling | mitigate | 2 small scalar attributes per LLM span; add a one-line note at the OTEL_BSP_MAX_EXPORT_BATCH_SIZE comment (reporter_jobspec.go:322) telling future reviewers to re-check the size budget on attribute growth (RESEARCH security table) |
| T-50-18 | Information Disclosure | identity values in Job Args visible via kubectl describe | accept (documented) | attemptID/loopRunID are K8s object UIDs + counters — already public to anyone who can read the Task object; no secret content |
| T-50-19 | Tampering | Env-based threading bypassing the Args convention | mitigate | plan mandates Args-only per the file's documented convention; tests assert flag round-trip |
| T-50-SC | Tampering | package installs | accept | zero new packages this plan |
</threat_model>

<verification>
- `go test ./internal/controller/... ./internal/reporter/... ./cmd/tide-reporter/...` exits 0
- `go build ./...` exits 0
- `go vet ./...` exits 0 (includes the extended metriccardinality analyzer from 50-03 via make lint's tide-lint)
- `make lint` exits 0
</verification>

<success_criteria>
- EXEC-01 complete: every attempt's per-call LLM spans carry loop.run_id + 1-indexed loop.iteration, correlated to the AGENT span's identity via byte-identical derivation
- The Phase 46 Args precedent followed at all four layers; no Env, no CallSpan ordinal field
- Absent-when-empty discipline holds on every new attribute
</success_criteria>

<output>
Create `.planning/phases/50-execution-loop-hardening-loop-native-observability/50-07-SUMMARY.md` when done.
</output>
