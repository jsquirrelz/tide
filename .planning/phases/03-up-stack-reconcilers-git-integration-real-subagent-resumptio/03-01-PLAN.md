---
phase: 03-up-stack-reconcilers-git-integration-real-subagent-resumptio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/envelope.go
  - pkg/dispatch/childcrd.go
  - pkg/dispatch/provider.go
  - pkg/dispatch/envelope_test.go
  - pkg/dispatch/childcrd_test.go
  - pkg/dispatch/provider_test.go
autonomous: true
requirements:
  - ART-03
  - ART-06
  - AUTH-01
must_haves:
  truths:
    - "pkg/dispatch.EnvelopeIn carries Provider {Vendor, Model, Params}, Role, and Level fields."
    - "pkg/dispatch.EnvelopeOut carries ChildCRDs []ChildCRDSpec, Git.HeadSHA, and Usage.CacheReadTokens/CacheCreationTokens."
    - "pkg/dispatch.ChildCRDSpec carries Kind/Name/Spec(runtime.RawExtension) without importing api/v1alpha1."
    - "Unit tests prove the schema additions round-trip JSON cleanly and ValidateAPIVersionKind still rejects unknown apiVersion."
  artifacts:
    - path: "pkg/dispatch/envelope.go"
      provides: "Extended EnvelopeIn (Provider, Role, Level) + EnvelopeOut (childCRDs, git.headSHA, cache token fields)"
    - path: "pkg/dispatch/childcrd.go"
      provides: "ChildCRDSpec type for orchestrator-side child-CRD materialization"
    - path: "pkg/dispatch/provider.go"
      provides: "ProviderSpec type for vendor+model+params"
    - path: "pkg/dispatch/envelope_test.go"
      provides: "Round-trip JSON + ValidateAPIVersionKind regression coverage"
  key_links:
    - from: "pkg/dispatch/envelope.go"
      to: "pkg/dispatch/provider.go"
      via: "EnvelopeIn.Provider ProviderSpec"
      pattern: "Provider\\s+ProviderSpec"
    - from: "pkg/dispatch/envelope.go"
      to: "pkg/dispatch/childcrd.go"
      via: "EnvelopeOut.ChildCRDs []ChildCRDSpec"
      pattern: "ChildCRDs\\s+\\[\\]ChildCRDSpec"
---

<objective>
Bump the `pkg/dispatch` envelope schema with the Phase 3 fields all four parallel work streams need: `EnvelopeIn.Provider` (vendor+model+params), `EnvelopeIn.Role`/`Level` (planner|executor / milestone|phase|plan|task), `EnvelopeOut.ChildCRDs` (typed child-CRD specs the orchestrator materializes), `EnvelopeOut.Git.HeadSHA`, and `EnvelopeOut.Usage` cache-token extensions.

Purpose: Wave 1 foundation. Stream A (up-stack reconcilers), Stream B (push Job), Stream C (Claude subagent), and Stream D (chaos-resume) all consume this contract. Adding it once, with tests, prevents downstream drift.

Output: Three Go files in `pkg/dispatch/` (one extended, two new) + JSON-round-trip + ValidateAPIVersionKind regression tests. Schema remains additive under `tideproject.k8s/v1alpha1` (Phase 2 D-A3 envelope-rejection-on-unknown-apiVersion preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-RESEARCH.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md
@pkg/dispatch/envelope.go
@pkg/dispatch/subagent.go
@pkg/dispatch/envelope_test.go
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Source-of-truth: pkg/dispatch (Phase 2). -->
<!-- Phase 2 D-A1 locks the Subagent interface; Phase 3 extends EnvelopeIn/Out ADDITIVELY. -->

Existing types in pkg/dispatch (Phase 2) the executor MUST preserve:
- `const APIVersionV1Alpha1 = "tideproject.k8s/v1alpha1"` — Phase 3 stays under this; do NOT bump.
- `type EnvelopeIn struct { APIVersion, Kind, TaskUID, Role, Level string; Caps Caps; Dev *Dev; ... }` — Role/Level may already exist (PATTERNS.md notes lines 36-41). Verify with grep before adding; if absent, add per D-C3.
- `type EnvelopeOut struct { APIVersion, Kind, ExitCode int; Result string; Usage Usage; ... }` — extend Usage with cache-token fields; add Git sub-struct.
- `type Caps struct { WallClockSeconds, Iterations int; InputTokens, OutputTokens int64 }` — godoc style mirror this.
- `func ValidateAPIVersionKind(apiVersion, kind, expectedKind string) error` — Phase 3 fields stay under the same apiVersion so this still passes.
- `type Subagent interface { Run(ctx context.Context, in EnvelopeIn) (EnvelopeOut, error) }` — UNCHANGED. D-A3 verbatim preservation.

Phase 3 additions (this plan creates):
- `type ProviderSpec struct { Vendor, Model string; Params map[string]string }` — value (not pointer) on EnvelopeIn.
- `type ChildCRDSpec struct { Kind, Name string; Spec runtime.RawExtension }` — uses `k8s.io/apimachinery/pkg/runtime`.
- `type Usage` extension fields: `CacheReadTokens, CacheCreationTokens int64`.
- `type GitOutput struct { HeadSHA string }` (or inline on EnvelopeOut) — embedded as `Git *GitOutput \`json:"git,omitempty"\``.
- `EnvelopeOut.ChildCRDs []ChildCRDSpec \`json:"childCRDs,omitempty"\``.

JSON tag convention (mirrors Phase 2 pkg/dispatch/envelope.go): lowerCamelCase. Optional sub-structs are pointer + `,omitempty`. Required value types omit `,omitempty`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProviderSpec + ChildCRDSpec types with tests</name>
  <files>pkg/dispatch/provider.go, pkg/dispatch/childcrd.go, pkg/dispatch/provider_test.go, pkg/dispatch/childcrd_test.go</files>
  <read_first>
    - pkg/dispatch/envelope.go (verbatim — note Caps lines 121-139 + Dev lines 162-175 as struct/godoc style analogs)
    - pkg/dispatch/envelope_test.go (test-style analog)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md (D-A1, D-C3 locked decisions)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md §"pkg/dispatch/childcrd.go (NEW)" and §"pkg/dispatch/provider.go (NEW)"
  </read_first>
  <behavior>
    - Test 1: `ProviderSpec{Vendor:"anthropic", Model:"claude-sonnet-4-6", Params:map[string]string{"thinking-budget":"4096"}}` round-trips through `json.Marshal`+`json.Unmarshal` with field tags `vendor`, `model`, `params,omitempty`.
    - Test 2: `ProviderSpec{Vendor:"anthropic", Model:"claude-haiku-4-5"}` (no params) marshals without a `params` field (omitempty).
    - Test 3: `ChildCRDSpec{Kind:"Phase", Name:"phase-foo", Spec: runtime.RawExtension{Raw:[]byte(`{"projectRef":"p1"}`)}}` round-trips; Spec.Raw bytes preserved exactly.
    - Test 4: `ChildCRDSpec` with empty Kind serializes the empty string (required field — no omitempty).
  </behavior>
  <action>
    Create `pkg/dispatch/provider.go` declaring `type ProviderSpec struct { Vendor string \`json:"vendor"\`; Model string \`json:"model"\`; Params map[string]string \`json:"params,omitempty"\` }`. Godoc above the struct must cite D-C3 verbatim from CONTEXT.md (paragraph form, mirror the godoc style on `Caps` at pkg/dispatch/envelope.go lines 121-127). Do NOT add validation methods in this file (validation lives in `internal/subagent/anthropic` per D-C3 — fail-fast on Vendor mismatch happens in the provider package, not here).

    Create `pkg/dispatch/childcrd.go` declaring `type ChildCRDSpec struct { Kind string \`json:"kind"\`; Name string \`json:"name"\`; Spec runtime.RawExtension \`json:"spec"\` }`. Import `k8s.io/apimachinery/pkg/runtime` for `RawExtension`. Godoc above the struct cites D-A1 (planner emits typed child-CRD specs; orchestrator materializes server-side; this type lives in pkg/dispatch to avoid pkg/dispatch ⇄ api/v1alpha1 import cycle). Mirror Caps godoc voice.

    Tests in `pkg/dispatch/provider_test.go` and `pkg/dispatch/childcrd_test.go` use the table-driven `t.Run(name, func(t *testing.T) {...})` pattern from `pkg/dispatch/envelope_test.go`. Use `encoding/json` + `reflect.DeepEqual` for round-trip assertions. For `runtime.RawExtension` round-trip: compare `.Raw` bytes via `bytes.Equal`.

    `runtime` import paths: `import "k8s.io/apimachinery/pkg/runtime"` — already a transitive dep via controller-runtime; verify with `go list -m k8s.io/apimachinery` returns non-error after the change.
  </action>
  <verify>
    <automated>go test -run "TestProviderSpec|TestChildCRDSpec" ./pkg/dispatch/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'type ProviderSpec struct' pkg/dispatch/provider.go` returns 1 hit.
    - `grep -nE 'type ChildCRDSpec struct' pkg/dispatch/childcrd.go` returns 1 hit.
    - `grep -nE 'runtime\.RawExtension' pkg/dispatch/childcrd.go` returns 1 hit (Spec field type).
    - `grep -nE 'json:"vendor"' pkg/dispatch/provider.go` returns 1 hit (canonical JSON tag).
    - `grep -nE 'json:"params,omitempty"' pkg/dispatch/provider.go` returns 1 hit (optional with omitempty).
    - `go vet ./pkg/dispatch/...` exits 0.
    - `go test ./pkg/dispatch/... -run "TestProviderSpec|TestChildCRDSpec" -count=1` exits 0 with all subtests PASS.
    - No imports from `pkg/dispatch/*.go` reference `api/v1alpha1` (verify: `grep -rnE 'tideproject\.k8s|api/v1alpha1' pkg/dispatch/*.go` returns 0).
  </acceptance_criteria>
  <done>ProviderSpec + ChildCRDSpec land as standalone types, JSON round-trip cleanly, and pkg/dispatch stays free of api/v1alpha1 imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend EnvelopeIn (Provider/Role/Level) + EnvelopeOut (ChildCRDs/Git.HeadSHA/Usage cache tokens) with tests</name>
  <files>pkg/dispatch/envelope.go, pkg/dispatch/envelope_test.go</files>
  <read_first>
    - pkg/dispatch/envelope.go (current EnvelopeIn/Out shape — extend in place; note ValidateAPIVersionKind lines 184-192 must keep working)
    - pkg/dispatch/envelope_test.go (existing round-trip + apiVersion-validation tests — extend, don't replace)
    - pkg/dispatch/provider.go (created in Task 1 — used as `Provider ProviderSpec`)
    - pkg/dispatch/childcrd.go (created in Task 1 — used as `ChildCRDs []ChildCRDSpec`)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md (D-A1, D-C3, D-C5 — cache token fields + Git.HeadSHA)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md §"pkg/dispatch/envelope.go (EXTEND — additive)"
  </read_first>
  <behavior>
    - Test 1: `EnvelopeIn` with Role="planner", Level="milestone", Provider populated round-trips through json and ValidateAPIVersionKind still passes for `apiVersion=tideproject.k8s/v1alpha1`, `kind=TaskEnvelopeIn`.
    - Test 2: `EnvelopeIn` with unknown apiVersion (`tideproject.k8s/v9999`) is rejected by ValidateAPIVersionKind (preserves Phase 2 D-A3 schema rejection).
    - Test 3: `EnvelopeOut` with `ChildCRDs:[]ChildCRDSpec{{Kind:"Phase", Name:"phase-foo", Spec: runtime.RawExtension{Raw:[]byte("{}")}}}` round-trips; absent ChildCRDs serialize without the field (omitempty).
    - Test 4: `EnvelopeOut.Git = &GitOutput{HeadSHA:"abc123"}` round-trips; nil Git omits the `git` JSON field.
    - Test 5: `EnvelopeOut.Usage.CacheReadTokens=100; .CacheCreationTokens=50` round-trip via JSON; field tags `cacheReadTokens`, `cacheCreationTokens`.
    - Test 6: Existing Phase 2 round-trip tests for Caps/Dev/Result/ExitCode still pass without modification.
  </behavior>
  <action>
    In `pkg/dispatch/envelope.go`, extend struct `EnvelopeIn`: ensure `Role string \`json:"role"\`` and `Level string \`json:"level"\`` fields exist (PATTERNS.md notes they may already be present at lines 36-41 — verify with grep; if present, no change). Add a new field `Provider ProviderSpec \`json:"provider"\`` (value type, not pointer — every dispatch carries a provider per D-C3). Godoc above Provider cites D-C3.

    In `pkg/dispatch/envelope.go`, extend struct `EnvelopeOut`: add field `ChildCRDs []ChildCRDSpec \`json:"childCRDs,omitempty"\`` (godoc cites D-A1 — orchestrator materializes child CRDs from this slice). Add `Git *GitOutput \`json:"git,omitempty"\`` field; declare `type GitOutput struct { HeadSHA string \`json:"headSHA"\` }` in the same file or split to `pkg/dispatch/git.go` (planner's choice — both are accepted).

    In `pkg/dispatch/envelope.go`, extend struct `Usage`: add `CacheReadTokens int64 \`json:"cacheReadTokens"\`` and `CacheCreationTokens int64 \`json:"cacheCreationTokens"\``. Godoc cites D-C5 (stream-json `cache_read_input_tokens` and `cache_creation_input_tokens` map here).

    Do NOT modify `APIVersionV1Alpha1` const. Do NOT change `ValidateAPIVersionKind` body — Phase 3 additions are additive under the same apiVersion (CLAUDE.md anti-pattern: don't bump apiVersion for additive changes; D-A3 envelope-rejection-on-unknown stays untouched).

    Extend `pkg/dispatch/envelope_test.go`: add the 5 new test cases above as subtests within existing test functions where logical (e.g., a `Provider` subtest within the existing `TestEnvelopeIn_RoundTrip`-style test; new `TestUsage_CacheTokens` if no parent fits). Keep table-driven style. Verify existing tests still pass via `go test ./pkg/dispatch/... -count=1`.
  </action>
  <verify>
    <automated>go test ./pkg/dispatch/... -count=1 && grep -c 'Provider\s\+ProviderSpec' pkg/dispatch/envelope.go && grep -c 'ChildCRDs\s\+\[\]ChildCRDSpec' pkg/dispatch/envelope.go && grep -c 'CacheReadTokens\s\+int64' pkg/dispatch/envelope.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Provider\s+ProviderSpec\s+`json:"provider"`' pkg/dispatch/envelope.go` returns 1 hit.
    - `grep -nE 'ChildCRDs\s+\[\]ChildCRDSpec\s+`json:"childCRDs,omitempty"`' pkg/dispatch/envelope.go` returns 1 hit.
    - `grep -nE 'Git\s+\*GitOutput\s+`json:"git,omitempty"`' pkg/dispatch/envelope.go` returns 1 hit.
    - `grep -nE 'CacheReadTokens\s+int64' pkg/dispatch/envelope.go` returns 1 hit.
    - `grep -nE 'CacheCreationTokens\s+int64' pkg/dispatch/envelope.go` returns 1 hit.
    - `grep -nE 'APIVersionV1Alpha1 = "tideproject.k8s/v1alpha1"' pkg/dispatch/envelope.go` returns 1 hit (UNCHANGED — apiVersion preserved).
    - `go test ./pkg/dispatch/... -count=1` exits 0; all pre-existing Phase 2 envelope tests still PASS plus the 5+ new ones.
    - `go vet ./pkg/dispatch/...` exits 0.
    - No new imports of `api/v1alpha1` from `pkg/dispatch/*.go` (verify: `grep -rnE 'tideproject\.k8s|tide/api/v1alpha1' pkg/dispatch/*.go` returns 0).
  </acceptance_criteria>
  <done>EnvelopeIn carries Provider/Role/Level; EnvelopeOut carries ChildCRDs, Git.HeadSHA, and cache-token Usage fields; round-trip + apiVersion-rejection tests pass; pkg/dispatch import firewall intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner subagent pod → orchestrator | Subagent emits EnvelopeOut.ChildCRDs (typed RawExtension); orchestrator decodes and server-side-creates child CRDs. Subagent has zero K8s verbs (Phase 2 D-A4). The RawExtension is the only path from a subagent process into the cluster's typed CRD graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-308 | Tampering / Elevation | EnvelopeOut.ChildCRDs RawExtension consumed by reconciler | mitigate (deferred to consumer plan 03-08) | This plan ships the TYPE only; the actual Kind allowlist + ApplySSA validation lives in `internal/controller/dispatch_helpers.go` (plan 03-08). Note in childcrd.go godoc: "Consumers MUST validate Kind against an allowlist before server-side create." |
| T-307 | Information Disclosure (budget hiding) | EnvelopeOut.Usage cache token fields | mitigate | Cache token fields ARE the mitigation — D-C5 commits to capturing CacheReadTokens + CacheCreationTokens so budget rollups in `Project.Status.budget` (Phase 2 D-D2) see the real spend. This plan's test 5 asserts the fields round-trip. |
</threat_model>

<verification>
- `go test ./pkg/dispatch/... -count=1` PASS — round-trip + apiVersion-rejection regression intact.
- `go vet ./pkg/dispatch/...` exits 0.
- `grep -rnE 'api/v1alpha1|tideproject\.k8s' pkg/dispatch/*.go` returns 0 hits (import firewall intact — does NOT match the apiVersion string constant which lives inside a quoted literal that grep with `*.go` will catch; refine to `grep -rE '"tide/api/v1alpha1"' pkg/dispatch/*.go` if false-positive; the firewall property is "no Go imports of api/v1alpha1").
- `go build ./...` exits 0.
</verification>

<success_criteria>
- Three Phase 3 envelope-schema artifacts land (provider.go, childcrd.go, envelope.go extensions).
- 5+ new tests cover round-trip + apiVersion rejection across the new fields.
- Phase 2 envelope tests still PASS — additive change, no breakage.
- `pkg/dispatch` import firewall preserved (no api/v1alpha1 imports).
</success_criteria>

<output>
After completion, create `.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-01-SUMMARY.md`.
</output>
