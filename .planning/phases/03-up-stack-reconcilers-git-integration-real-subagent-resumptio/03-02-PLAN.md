---
phase: 03-up-stack-reconcilers-git-integration-real-subagent-resumptio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha1/project_types.go
  - api/v1alpha1/shared_types.go
  - api/v1alpha1/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_projects.yaml
  - cmd/stub-subagent/main.go
  - cmd/stub-subagent/main_test.go
autonomous: true
requirements:
  - AUTH-01
  - PERSIST-04
  - TEST-04
must_haves:
  truths:
    - "Project CRD Spec carries Subagent (image, model, levels.{milestone,phase,plan,task}) and Git (repoURL, credsSecretRef, leaksConfigRef)."
    - "Project CRD Status carries Git (branchName, lastPushedSHA, leaseFailureCount)."
    - "Condition vocabulary in shared_types.go adds Cloned, AuthoringPlanner, PushLeaseFailed; Phase constants add PhasePushLeaseFailed and PhaseComplete."
    - "Stub-subagent has a new dispatch mode `wait-for-signal` that polls /workspace/envelopes/{task-uid}/release every 500ms (per 03-RESEARCH Open Questions Q4 RESOLVED — locked at 500ms unless test wall-clock budget pressure surfaces) and on file appearance emits the canned success envelope."
    - "All schema additions are validated via CEL markers where applicable; CRD codegen runs clean."
  artifacts:
    - path: "api/v1alpha1/project_types.go"
      provides: "SubagentConfig + GitConfig + GitStatus + Phase constants"
    - path: "api/v1alpha1/shared_types.go"
      provides: "ConditionCloned, ConditionAuthoringPlanner, ConditionPushLeaseFailed"
    - path: "config/crd/bases/tideproject.k8s_projects.yaml"
      provides: "Regenerated CRD schema with new Spec/Status fields"
    - path: "cmd/stub-subagent/main.go"
      provides: "dispatchWaitForSignal mode + switch-case integration"
  key_links:
    - from: "api/v1alpha1/project_types.go"
      to: "config/crd/bases/tideproject.k8s_projects.yaml"
      via: "make manifests (controller-gen)"
      pattern: "subagent:|git:|branchName:"
    - from: "cmd/stub-subagent/main.go"
      to: "/workspace/envelopes/{task-uid}/release"
      via: "os.Stat polling at 500ms ticker"
      pattern: "release.*os\\.Stat"
---

<objective>
Wave 1 schema and test-fixture foundation, in parallel with 03-01. Two concerns, one plan because both unblock downstream streams immediately:

1. `Project` CRD Spec/Status extensions (D-B6, D-C2) — `Subagent` + `Git` Spec blocks, `Git` Status block, condition vocabulary additions (`Cloned`, `AuthoringPlanner`, `PushLeaseFailed`).
2. `cmd/stub-subagent` extension with `wait-for-signal` mode (D-D3) — file-poll dispatch mode the chaos-resume Layer B test (plan 03-10) depends on.

Purpose: Streams B (push Job) and C (Claude subagent image) read `Project.Spec.subagent` + `Project.Spec.git`; the chaos-resume integration test needs `wait-for-signal` mode to pin Tasks at `Running` long enough for pod-kill + leader-handoff to observe. Both gates Wave 1; both small. Combining keeps the wave parallelism budget tight.

Output: Extended `api/v1alpha1/project_types.go` + `shared_types.go`, regenerated CRD via `make manifests`, extended stub-subagent main.go with the new dispatch mode + table test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-RESEARCH.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md
@api/v1alpha1/project_types.go
@api/v1alpha1/shared_types.go
@cmd/stub-subagent/main.go
@cmd/stub-subagent/main_test.go
@CLAUDE.md

<interfaces>
<!-- API group: tideproject.k8s (CLAUDE.md domain rule — never tide.io or placeholders). -->
<!-- Phase 1 D-A3 established this; Phase 3 adds fields under the same group + version. -->

Existing types the executor MUST preserve (api/v1alpha1/project_types.go):
- `type ProjectSpec struct { ... }` — Phase 1 + Phase 2 fields locked; Phase 3 ADDS Subagent + Git sub-blocks; do NOT remove or rename existing fields.
- `type ProjectStatus struct { ... Budget BudgetStatus ... }` — Phase 3 ADDS Git GitStatus sub-block.
- Existing Phase constants (BudgetExceeded, etc.) — Phase 3 ADDS PhasePushLeaseFailed and PhaseComplete; do NOT renumber or rename.

Existing types the executor MUST preserve (api/v1alpha1/shared_types.go):
- Condition type constants (`ConditionPending`, `ConditionReady`, `ConditionReconciling`, `ConditionFailed`, etc.) — Phase 3 ADDS ConditionCloned, ConditionAuthoringPlanner, ConditionPushLeaseFailed.
- Reason constants — Phase 3 may add `ReasonLeaseFailed` if needed; otherwise existing reasons suffice.

Stub-subagent dispatch contract (cmd/stub-subagent/main.go):
- `dispatchSuccess(ctx, env, outPath, stderr) int` — writes canned success EnvelopeOut to outPath, returns 0.
- `dispatchFailExit1`, `dispatchHang`, `dispatchExceedOutputPaths` — Phase 2 D-F3 existing modes.
- Switch statement at the run() function dispatches by `env.Dev.TestMode`; Phase 3 ADDS `case "wait-for-signal":`.

Polling implementation pattern (from CLAUDE.md "Observe First" — use real file I/O, not channel hacks):
- `time.NewTicker(500 * time.Millisecond)` + `for { select { case <-ctx.Done(): return 0; case <-ticker.C: if _, err := os.Stat(signalPath); err == nil { return dispatchSuccess(...) } } }`.
- `signalPath := filepath.Join("/workspace/envelopes", env.TaskUID, "release")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Project CRD Spec/Status + condition vocabulary; regenerate CRD manifests</name>
  <files>api/v1alpha1/project_types.go, api/v1alpha1/shared_types.go, api/v1alpha1/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_projects.yaml</files>
  <read_first>
    - api/v1alpha1/project_types.go (preserve existing ProjectSpec/Status shape; mirror BudgetStatus + PlanAdmissionConfig field-comment style)
    - api/v1alpha1/shared_types.go (mirror existing Condition*/Reason* const blocks)
    - api/v1alpha1/budget_types.go or similar (if exists — mirror sub-block convention)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md (D-B6, D-C2)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md §"api/v1alpha1/project_types.go (EXTEND)"
    - Makefile (verify `make manifests` target exists; if not, use `make generate manifests`)
  </read_first>
  <behavior>
    - Test 1 (envtest): apply a Project CRD with `spec.subagent.image="ghcr.io/foo/bar:v1"`, `spec.subagent.model="claude-sonnet-4-6"`, `spec.subagent.levels.task.model="claude-haiku-4-5"`, `spec.git.repoURL="https://github.com/owner/repo.git"`, `spec.git.credsSecretRef="git-creds"` — apply succeeds; round-trip Get returns identical Spec.
    - Test 2 (envtest): apply a Project with `spec.git.repoURL="ftp://foo"` — CEL validation rejects (`^https?://.+` pattern).
    - Test 3 (envtest): patch Project.Status.git.{branchName="tide/run-foo-123", lastPushedSHA="abc123", leaseFailureCount=2} — patch succeeds; Get returns identical Status.
    - Test 4 (Go unit): `api/v1alpha1.PhasePushLeaseFailed == "PushLeaseFailed"`; `api/v1alpha1.PhaseComplete == "Complete"`; `api/v1alpha1.ConditionCloned == "Cloned"`; `api/v1alpha1.ConditionAuthoringPlanner == "AuthoringPlanner"`; `api/v1alpha1.ConditionPushLeaseFailed == "PushLeaseFailed"`.
  </behavior>
  <action>
    In `api/v1alpha1/project_types.go`, declare new types per PATTERNS.md §"api/v1alpha1/project_types.go (EXTEND)" verbatim:

    - `type SubagentConfig struct { Image string \`json:"image,omitempty"\`; Model string \`json:"model,omitempty"\`; Levels LevelOverrides \`json:"levels,omitempty"\` }` — godoc cites D-C2.
    - `type LevelOverrides struct { Milestone *LevelConfig; Phase *LevelConfig; Plan *LevelConfig; Task *LevelConfig }` — all four fields `json:"...,omitempty"` and pointer.
    - `type LevelConfig struct { Model string; Params map[string]string; Image string }` — all three `,omitempty`. Image field is schema-present-but-not-enforced-in-v1.0 per CONTEXT.md "Deferred Ideas" (cross-vendor per-level overrides).
    - `type GitConfig struct { RepoURL string \`json:"repoURL"\`; CredsSecretRef string \`json:"credsSecretRef"\`; LeaksConfigRef string \`json:"leaksConfigRef,omitempty"\` }` — RepoURL has kubebuilder marker `// +kubebuilder:validation:Pattern=`^https?://.+`` directly above the field; godoc cites D-B6.
    - `type GitStatus struct { BranchName string \`json:"branchName,omitempty"\`; LastPushedSHA string \`json:"lastPushedSHA,omitempty"\`; LeaseFailureCount int32 \`json:"leaseFailureCount,omitempty"\` }` — godoc cites D-B6 (Unix epoch branch name format, lease-mismatch counter).

    Add to `ProjectSpec`: `Subagent SubagentConfig \`json:"subagent,omitempty"\`` and `Git GitConfig \`json:"git,omitempty"\`` (placement: end of struct, after existing fields; godoc above each cites the D-code).

    Add to `ProjectStatus`: `Git GitStatus \`json:"git,omitempty"\`` (placement: alongside existing Budget sub-block).

    Append Phase constants block: `const ( PhasePushLeaseFailed = "PushLeaseFailed"; PhaseComplete = "Complete" )` (group near existing Phase constants).

    In `api/v1alpha1/shared_types.go`, append: `const ( ConditionCloned = "Cloned"; ConditionAuthoringPlanner = "AuthoringPlanner"; ConditionPushLeaseFailed = "PushLeaseFailed" )` (group with existing Condition constants).

    Run `make manifests` (or `make generate manifests` — verify Makefile target) to regenerate `config/crd/bases/tideproject.k8s_projects.yaml` + `zz_generated.deepcopy.go`. Do NOT hand-edit the generated files; the tests on the generated YAML are the contract.

    Per CLAUDE.md domain rule: API group MUST remain `tideproject.k8s` (verify the generated CRD's `spec.group: tideproject.k8s` line is unchanged).
  </action>
  <verify>
    <automated>make manifests && go vet ./api/... && go test ./api/... -count=1 && grep -c 'subagent:' config/crd/bases/tideproject.k8s_projects.yaml && grep -c 'branchName:' config/crd/bases/tideproject.k8s_projects.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'type SubagentConfig struct' api/v1alpha1/project_types.go` returns 1 hit.
    - `grep -nE 'type GitConfig struct' api/v1alpha1/project_types.go` returns 1 hit.
    - `grep -nE 'type GitStatus struct' api/v1alpha1/project_types.go` returns 1 hit.
    - `grep -nE 'Subagent\s+SubagentConfig' api/v1alpha1/project_types.go` returns 1 hit (Spec field wiring).
    - `grep -nE 'Git\s+GitStatus' api/v1alpha1/project_types.go` returns ≥1 hit (Status field wiring).
    - `grep -cE 'PhasePushLeaseFailed\s*=\s*"PushLeaseFailed"' api/v1alpha1/project_types.go` returns 1.
    - `grep -cE 'ConditionCloned\s*=\s*"Cloned"' api/v1alpha1/shared_types.go` returns 1.
    - `grep -cE 'ConditionAuthoringPlanner\s*=\s*"AuthoringPlanner"' api/v1alpha1/shared_types.go` returns 1.
    - `grep -cE 'ConditionPushLeaseFailed\s*=\s*"PushLeaseFailed"' api/v1alpha1/shared_types.go` returns 1.
    - `grep -cE '^\s+subagent:$' config/crd/bases/tideproject.k8s_projects.yaml | grep -v '^#'` returns ≥1 (schema regenerated).
    - `grep -cE '^\s+branchName:$' config/crd/bases/tideproject.k8s_projects.yaml | grep -v '^#'` returns ≥1.
    - `grep -cE '^\s+pattern:\s+\^https\?://' config/crd/bases/tideproject.k8s_projects.yaml | grep -v '^#'` returns ≥1 (CEL pattern landed for repoURL).
    - `grep -cE '^\s+group:\s+tideproject\.k8s' config/crd/bases/tideproject.k8s_projects.yaml | grep -v '^#'` returns 1 (CLAUDE.md domain rule).
    - `go vet ./api/...` exits 0.
    - `go test ./api/... -count=1` exits 0 (pre-existing API tests still PASS; new fields don't break round-trip).
    - `make manifests` exits 0 (codegen + CRD regen successful).
  </acceptance_criteria>
  <done>Project CRD carries Subagent + Git Spec/Status blocks, condition vocabulary expanded, CRD YAML regenerated, CEL pattern enforces HTTPS repoURL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend stub-subagent with `wait-for-signal` dispatch mode</name>
  <files>cmd/stub-subagent/main.go, cmd/stub-subagent/main_test.go</files>
  <read_first>
    - cmd/stub-subagent/main.go (full — note dispatchHang at lines 240-250 as pattern for "block until external event"; note switch-on-TestMode at lines 100-114 as wiring seam; note dispatchSuccess at lines 173-211 as canned envelope shape)
    - cmd/stub-subagent/main_test.go (table-test convention to mirror)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md (D-D3 — file-poll at 500ms; signal at /workspace/envelopes/{task-uid}/release)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md §"cmd/stub-subagent/main.go (EXTEND — wait-for-signal mode per D-D3)"
  </read_first>
  <behavior>
    - Test 1: Set `env.Dev.TestMode="wait-for-signal"`, env.TaskUID="t-1"; pre-create the signal file at temp `{tempdir}/envelopes/t-1/release` (use `t.TempDir()` and remap signal path via env or arg); run dispatchWaitForSignal with 1s context timeout — returns 0, writes canned success envelope to outPath (mirrors dispatchSuccess output shape).
    - Test 2: TestMode="wait-for-signal", signal file ABSENT, context cancels at 1s — returns 0 (ctx.Done path), no envelope written.
    - Test 3: TestMode="wait-for-signal", signal file APPEARS mid-poll (test goroutine `time.AfterFunc(200ms, func(){os.WriteFile(signalPath, nil, 0644)})`) — returns 0 within 1s, envelope written with the dispatchSuccess shape.
    - Test 4: Unknown TestMode returns non-zero exit code (regression — dispatchUnknown path unchanged).
  </behavior>
  <action>
    In `cmd/stub-subagent/main.go`, add a new package-level function `dispatchWaitForSignal(ctx context.Context, env pkgdispatch.EnvelopeIn, outPath string, stderr io.Writer) int` mirroring the signature of `dispatchHang` (lines 240-250 of existing file):

    Body shape (from PATTERNS.md §"cmd/stub-subagent/main.go (EXTEND)"):
    - Compute `signalPath := filepath.Join("/workspace/envelopes", env.TaskUID, "release")`.
    - Create `ticker := time.NewTicker(500 * time.Millisecond)` with `defer ticker.Stop()`.
    - In a `for { select { ... } }` loop: `case <-ctx.Done(): return 0`; `case <-ticker.C: if _, err := os.Stat(signalPath); err == nil { return dispatchSuccess(ctx, env, outPath, stderr) }`.

    Add the case to the switch in `run()` (existing lines 100-114): `case "wait-for-signal": return dispatchWaitForSignal(ctx, env, outPath, stderr)`. Preserve case ordering; do NOT touch other modes.

    For testability of the absolute path `/workspace/envelopes/...`: either (a) extract the signal path into a package-level var that tests can override, or (b) parameterize `dispatchWaitForSignal` to accept a `workspaceRoot string` arg with a default of `/workspace`. Mirror whichever Phase 2 modes do for their PVC paths (read existing main.go to confirm).

    Add four test cases to `cmd/stub-subagent/main_test.go` per the behavior block above. Use `t.TempDir()` for the workspace root override; use `time.AfterFunc` for the signal-mid-poll case; use `context.WithTimeout(parent, 1*time.Second)` for the bounded test budget. Mirror existing test patterns (table-driven if possible, otherwise individual `Test...` functions).

    Phase 3 stub mode count: 5 modes total (success | fail-exit-1 | hang | exceed-output-paths | wait-for-signal). The "Real Claude image MUST ignore env.Dev entirely" note from PATTERNS.md is enforced by `internal/subagent/anthropic/` in plan 03-05, not here.
  </action>
  <verify>
    <automated>go test ./cmd/stub-subagent/... -count=1 -run "TestWaitForSignal|TestDispatch"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'func dispatchWaitForSignal' cmd/stub-subagent/main.go` returns 1 hit.
    - `grep -cE 'case "wait-for-signal":' cmd/stub-subagent/main.go` returns 1 (switch wiring).
    - `grep -cE 'time\.NewTicker\(500\s*\*\s*time\.Millisecond\)' cmd/stub-subagent/main.go` returns 1 (D-D3 polling cadence).
    - `grep -cE '/workspace/envelopes' cmd/stub-subagent/main.go` returns ≥1 (signal path matches PVC layout).
    - `grep -cE '"release"' cmd/stub-subagent/main.go` returns ≥1 (signal file name).
    - `go test ./cmd/stub-subagent/... -count=1 -timeout 30s` exits 0 with all 4 new test cases PASS plus pre-existing tests PASS.
    - `go vet ./cmd/stub-subagent/...` exits 0.
  </acceptance_criteria>
  <done>Stub-subagent has dispatchWaitForSignal mode wired into the switch; four test cases cover the signal-arrived, signal-absent-ctx-cancel, signal-mid-poll, and existing-mode-regression paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External (kubectl user) → API server | New ProjectSpec.Subagent + Git fields are user-controllable. CEL `Pattern` on repoURL is the boundary's input validation; CredsSecretRef points to a Secret in the same namespace (cross-namespace Secret refs are NOT permitted in v1.0). |
| Chaos-resume test fixture → /workspace PVC | The wait-for-signal mode is only used in tests; the release file is written by the test harness (Plan 03-10), not by external users in production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-310 (Information Disclosure) | Test fixture leaks credentials into kind cluster | mitigate | The stub-subagent's wait-for-signal mode uses NO credentials (no ANTHROPIC_API_KEY, no git PAT — it polls a file). The chaos-resume Layer B test fixture (plan 03-10) uses this mode + local httptest-backed git remote. No real PAT is ever applied to the test cluster. This plan ships only the stub mode; plan 03-10 enforces the test fixture's credential-free invariant. |
| (Cross-namespace) | Tampering / Elevation | Project.Spec.git.credsSecretRef | mitigate (deferred to consumer plan 03-06) | This plan ships the field as a plain string (Secret name only, no namespace allowed in the schema). Push Job pod (plan 03-06) MUST mount the Secret from the Project's own namespace. Multi-namespace Secret refs are NOT supported in v1.0. |
</threat_model>

<verification>
- `make manifests` regenerates `config/crd/bases/tideproject.k8s_projects.yaml` with subagent/git Spec + Status fields.
- `go test ./api/... ./cmd/stub-subagent/... -count=1 -timeout 30s` exits 0.
- `go vet ./...` exits 0 (no schema-related compilation errors elsewhere).
- API group remains `tideproject.k8s` (CLAUDE.md domain rule).
</verification>

<success_criteria>
- Project CRD Spec/Status extended with all D-B6 + D-C2 fields.
- Condition vocabulary expanded with Cloned/AuthoringPlanner/PushLeaseFailed.
- Stub-subagent wait-for-signal mode wired with 500ms poll cadence on `/workspace/envelopes/{task-uid}/release`.
- All tests (api + stub-subagent) PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-02-SUMMARY.md`.
</output>
