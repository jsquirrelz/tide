---
phase: 03-up-stack-reconcilers-git-integration-real-subagent-resumptio
plan: 10
type: execute
wave: 6
depends_on:
  - 9
files_modified:
  - test/integration/kind/chaos_resume_test.go
  - test/integration/kind/push_lease_test.go
  - test/integration/kind/up_stack_dispatch_test.go
  - test/integration/kind/testdata/chaos-resume-three-task.yaml
  - test/integration/kind/testdata/push-lease-project.yaml
  - test/integration/kind/testdata/up-stack-project.yaml
  - Makefile
autonomous: true
requirements:
  - PERSIST-04
  - TEST-04
must_haves:
  truths:
    - "chaos_resume_test.go is a Layer B kind spec that exercises the D-D2 mixed-state 3-task fixture (T-α success, T-β + T-γ wait-for-signal) and asserts the D-D4 four-pillar invariants + algorithmic invariant (5th: pkg/dag.ComputeWaves post-restart matches a pre-kill snapshot via golden-file comparison) across `kubectl delete pod -l app=tide-controller-manager`. Authority per CONTEXT.md <specifics> #5: \"Chaos-resume four-pillar assertion is a single integration test, not a constellation of small tests\" — single load-bearing claim framing, BUT the Ginkgo It block is split into 5 named subtests (1 per pillar + 1 for algorithmic invariant) via Ginkgo's nested When/It pattern so a failure localizes which pillar regressed (per Phase 02.2 cascade lesson #4 — localized assertions cut iteration count)."
    - "push_lease_test.go is a Layer B kind spec that verifies: (a) first push omits the lease and succeeds; (b) subsequent push with correct lastPushedSHA succeeds; (c) stale-lease scenario (external push between TIDE pushes) triggers PushLeaseFailed + Status.git.LeaseFailureCount increment; (d) bypass annotation resumes the push."
    - "up_stack_dispatch_test.go is a Layer B kind spec that verifies: applying a Milestone CRD triggers MilestoneReconciler to dispatch a planner Job; child Phase CRDs materialize from the envelope; OwnerRefs cascade correctly."
    - "Makefile test-int target preloads the new tide-push and claude-subagent images into the kind cluster (mirrors Phase 2's image-preload pattern for stub-subagent/credproxy)."
    - "All three Ginkgo specs PASS in a clean `make test-int` run and a `KEEP_KIND_CLUSTER=true make test-int` rerun (Phase 02.2 closeout pattern)."
  artifacts:
    - path: "test/integration/kind/chaos_resume_test.go"
      provides: "Layer B chaos-resume four-pillar assertions (D-D4)"
    - path: "test/integration/kind/push_lease_test.go"
      provides: "Layer B push lease semantics + bypass annotation recovery"
    - path: "test/integration/kind/up_stack_dispatch_test.go"
      provides: "Layer B up-stack reconciler dispatch + child materialization"
    - path: "test/integration/kind/testdata/{chaos-resume-three-task,push-lease-project,up-stack-project}.yaml"
      provides: "Three test-CRD fixtures backing the three specs"
  key_links:
    - from: "test/integration/kind/chaos_resume_test.go"
      to: "test/integration/kind/testdata/chaos-resume-three-task.yaml"
      via: "applyFile fixture"
      pattern: "applyFile.*chaos-resume"
    - from: "test/integration/kind/chaos_resume_test.go"
      to: "kubectl delete pod -l app=tide-controller-manager"
      via: "test exec call mid-spec"
      pattern: "kubectl delete pod"
---

<objective>
Wave 6 — final verification tier. Three Layer B kind integration specs covering Phase 3's three "proof properties":

1. **chaos_resume_test.go (PERSIST-04 / TEST-04 / D-D4)**: Kill the controller pod mid-wave; new leader takes over; four-pillar invariants hold (Job UID continuity, Attempt unchanged, completed-set preserved, post-release Wave reaches Succeeded). The existence proof for "resumption state = indegree map + completed-task set".
2. **push_lease_test.go (ART-06 / D-B6)**: First push omits lease; subsequent push honors lease; stale-lease scenario flips Project to PushLeaseFailed; bypass annotation recovers. Pitfall 13 mitigation.
3. **up_stack_dispatch_test.go (ART-03 / D-A1 / D-A2)**: Applying a Milestone CRD triggers MilestoneReconciler dispatch + child Phase CRD materialization with correct OwnerRefs. The minimal smoke proving the up-stack reconciler bodies wire end-to-end.

Purpose: Phase 3's success criteria #5 (chaos-resume) + #1-2 (push semantics) + #3 (real subagent flow — at least the dispatch shape, even though we use stub-subagent for these specs to keep them cost-bounded) are not provable without these specs. TEST-03 (live nightly E2E with real Claude) is covered by its own plan 03-11 (added per checker B4 + CONTEXT.md <specifics> #5 — "TEST-03 as a separate plan from chaos-resume; live cost is bounded by Phase 2 budget cap infrastructure"); these specs in 03-10 use stub-subagent with the new `wait-for-signal` mode and are cost-free.

Output: 3 new test files + 3 fixture YAMLs + Makefile updates for image preloading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-RESEARCH.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-VALIDATION.md
@test/integration/kind/wave_test.go
@test/integration/kind/suite_test.go
@test/integration/kind/projects_pvc_test.go
@test/integration/kind/credproxy_test.go
@internal/controller/leader_election_test.go
@Makefile
@CLAUDE.md

<interfaces>
<!-- Three specs reuse Phase 2 Layer B test infrastructure (test/integration/kind/suite_test.go). -->
<!-- D-D4 four-pillar assertion set is the load-bearing test design for chaos-resume. -->

Phase 2 Layer B test patterns to mirror:
- `test/integration/kind/wave_test.go` lines 39-91 — applyFile fixture + Eventually-Get-Task-Status pattern.
- `test/integration/kind/credproxy_test.go` — observes Pod-status terminationMessagePath via PodStatusEnvelopeReader (Phase 02.2 cascade-10 pivot).
- `test/integration/kind/suite_test.go` — BeforeSuite helm install + AfterSuite cleanupKindCluster (Phase 02.2 cascade-2 helper).

Leader-election pattern (chaos_resume_test.go reuses):
- `internal/controller/leader_election_test.go` lines 53-114 — pod-kill + Eventually-watch-lease-holder-changes pattern. envtest version; this plan's kind version does the same against the real kind cluster.

Mixed-state 3-task fixture (D-D2):
```yaml
# testdata/chaos-resume-three-task.yaml
apiVersion: tideproject.k8s/v1alpha1
kind: Project
metadata:
  name: chaos-resume
  namespace: chaos-resume-test
spec:
  subagent: {image: "ghcr.io/jsquirrelz/tide-stub-subagent:v0.1.0"}
  # No git config — chaos-resume is internal-only; no push happens during this spec.
---
# Plan fixture with three Tasks: T-α success, T-β wait-for-signal, T-γ deps=[T-α] wait-for-signal.
```

Push lease test fixture (uses a local httptest git server inside the kind cluster, OR a NodePort to host-side httptest):
- Option A: deploy a small in-cluster git server (gitea-lite or similar) — heavy.
- Option B: use kind's port-mapping to expose host-side httptest server — adds complexity to suite_test.go.
- Option C: spawn a Pod running `gitea` or `git-http-backend` for the spec — simpler.
- For v1.0, option C with a small in-cluster fixture image. The fixture image lives at `test/integration/kind/testdata/git-server/` (not in scope of this plan — but the spec can use a pre-existing testdata image if available; if not, skip the live-push assertion and verify the push Job spec wiring only, with a documented Wave-7 TODO).

Decision: planner picks the cheapest verification that proves the lease semantics. If a full in-cluster git server is too heavy, the spec can:
- (a) Verify the push Job IS DISPATCHED with the right Args (lastPushedSHA, branch) — observable via kubectl.
- (b) Mock the push Job's exit by directly patching its status to mimic a leak-detected or lease-rejected outcome.
- (c) Verify the ProjectReconciler reacts correctly to each Job outcome (lease-rejected → PushLeaseFailed; bypass annotation → recovery).
This trades full end-to-end coverage for testability speed. Document the choice inline.

D-D4 four-pillar assertion set (chaos_resume_test.go):
1. Job UID continuity (β, γ).
2. Task.Status.Attempt unchanged.
3. Completed-set preserved (α stays Succeeded; same CompletedAt).
4. Observed completion across kill — after signaling release for β/γ (write to `/workspace/envelopes/{β-uid}/release` via test exec or a small init Job), both reach Succeeded; Wave reaches Succeeded.
5. (Algorithmic invariant) `pkg/dag.ComputeWaves` invoked post-restart against live Tasks produces identical wave structure (golden-file comparison).

How to write the release file from the test:
- Best: `kubectl exec` into a Pod with the PVC mounted (the controller-manager Pod itself, or a small "writer" Job).
- Alternative: scale up a Pod with the PVC mounted via an inline `kubectl run -i --rm` invocation; have it `touch /workspace/envelopes/<task-uid>/release` and exit.

Image preloading in Makefile:
- Phase 2's existing pattern (read Makefile target test-int-kind-prep) preloads stub-subagent and credproxy. Phase 3 adds tide-push and claude-subagent — though claude-subagent is NOT needed for these specs (they use stub-subagent). Add tide-push to preload list IF push_lease_test.go uses the real push Job; otherwise skip.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chaos_resume_test.go — D-D4 four-pillar Layer B chaos-resume spec + fixture YAML</name>
  <files>test/integration/kind/chaos_resume_test.go, test/integration/kind/testdata/chaos-resume-three-task.yaml</files>
  <read_first>
    - test/integration/kind/wave_test.go (entire — the closest Layer B analog; mirror Describe/BeforeEach/It structure)
    - internal/controller/leader_election_test.go lines 53-114 (pod-kill + lease-handoff pattern; kind version mirrors)
    - cmd/stub-subagent/main.go (plan 03-02 — confirm wait-for-signal mode is wired and path is /workspace/envelopes/{task-uid}/release)
    - test/integration/kind/suite_test.go (test infrastructure — applyFile, ctx, k8sClient, helmInstall helpers)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-PATTERNS.md §"test/integration/kind/chaos_resume_test.go (NEW — Layer B kind, D-D1..D4)" lines 854-899
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md (D-D1, D-D2, D-D3, D-D4)
  </read_first>
  <behavior>
    - Test 1 (full spec, clean run): `make test-int` runs the spec; pre-kill snapshot captured; controller pod killed; new leader Eventually-takes-over (within 30s lease window); post-kill snapshot captured. Ginkgo `It("chaos-resume D-D4 + algorithmic invariant", func() { ... })` block uses 5 NAMED `When` or `By` subtests (B6 — localized failure attribution per Phase 02.2 cascade lesson #4):
      - Subtest 1 `By("Pillar 1: Job UID continuity for in-flight Tasks")`: assert pre.JobUID == post.JobUID for β and γ.
      - Subtest 2 `By("Pillar 2: Task.Status.Attempt unchanged across kill")`: assert pre.Attempt == post.Attempt for all 3 Tasks.
      - Subtest 3 `By("Pillar 3: Completed-set preserved (T-α Succeeded with same CompletedAt)")`: assert α stays Succeeded and α.CompletedAt is unchanged.
      - Subtest 4 `By("Pillar 4: Observed completion across kill")`: write release files for β + γ; Eventually both reach Succeeded; the Wave reaches Succeeded; exactly 3 Jobs status.succeeded=1.
      - Subtest 5 `By("Pillar 5: Algorithmic invariant — ComputeWaves post-restart matches pre-kill snapshot")` (W12 — the 5th D-D4 pillar from CONTEXT.md D-D4 (5)): invoke `pkg/dag.ComputeWaves` against the post-restart Task list; compare to a JSON golden file `testdata/chaos-resume-waves.golden.json` (committed as part of this plan) saved as the pre-kill ComputeWaves output. Equality assertion is on the wave-layer structure (same node sets per wave).
      Each `By(...)` records to the Ginkgo report so a Pillar-N failure surfaces by name without re-running the full spec.
    - Test 2 (rerun with KEEP_KIND_CLUSTER=true): spec PASS on second invocation — fixtures clean up via AfterEach (delete namespace) and BeforeEach re-applies (Phase 02.2 cascade-1 helm install --replace pattern).
    - Total wall budget: ≤ 90s per Phase 3 RESEARCH note (60s + 30s buffer; lease window is ~15s).
  </behavior>
  <action>
    Create `test/integration/kind/testdata/chaos-resume-three-task.yaml` per D-D2 fixture:

    ```yaml
    apiVersion: v1
    kind: Namespace
    metadata:
      name: chaos-resume-test
    ---
    apiVersion: tideproject.k8s/v1alpha1
    kind: Project
    metadata: {name: chaos-resume, namespace: chaos-resume-test}
    spec:
      subagent:
        image: ghcr.io/jsquirrelz/tide-stub-subagent:v0.1.0
      # No git block — this fixture doesn't exercise push.
    ---
    apiVersion: tideproject.k8s/v1alpha1
    kind: Plan
    metadata: {name: chaos-resume-plan, namespace: chaos-resume-test}
    spec:
      # Pre-validated DAG so Plan admission doesn't gate the test.
      tasks:
        - name: t-alpha
          dependsOn: []
          filesTouched: ["alpha.txt"]
          # Stub mode: success — runs fast (~1s) → Succeeded pre-kill.
          dev: {testMode: success}
        - name: t-beta
          dependsOn: []
          filesTouched: ["beta.txt"]
          # Stub mode: wait-for-signal — hangs until /workspace/envelopes/{task-uid}/release appears.
          dev: {testMode: wait-for-signal}
        - name: t-gamma
          dependsOn: ["t-alpha"]
          filesTouched: ["gamma.txt"]
          dev: {testMode: wait-for-signal}
    ---
    # Inline PersistentVolumeClaim (per Phase 02.2 cascade-10 namespace-scoping pattern).
    apiVersion: v1
    kind: PersistentVolumeClaim
    metadata: {name: chaos-resume-projects, namespace: chaos-resume-test}
    spec:
      accessModes: [ReadWriteMany]
      resources: {requests: {storage: 1Gi}}
    ```

    (Verify the exact Spec shape against api/v1alpha1; if PlanSpec doesn't carry inline tasks, materialize Tasks via separate Task CRDs in the YAML. Read api/v1alpha1/plan_types.go to confirm. Adjust fixture accordingly.)

    Create `test/integration/kind/chaos_resume_test.go` per PATTERNS §"test/integration/kind/chaos_resume_test.go (NEW)" lines 854-899:

    Ginkgo `Describe("Chaos-resume: kill controller pod mid-wave", Label("kind"), func() { ... })` block with one `It("D-D4 four pillars hold", func() { ... })`:

    1. **Apply fixture**: `Expect(applyFile(filepath.Join("testdata", "chaos-resume-three-task.yaml"))).To(Succeed())`.
    2. **Wait for pre-kill states**: Eventually:
       - t-alpha.Status.Phase == "Succeeded" (fast).
       - t-beta.Status.Phase == "Running" (Job created, harness polling for signal).
       - t-gamma.Status.Phase == "Running" (deps on alpha satisfied; harness polling).
       Timeout: 90s.
    3. **Snapshot pre-kill state**: `Map[TaskName → {JobUID, Attempt, Phase, JobCreationTimestamp, CompletedAt (alpha only)}]`. Use `kubectl get task <name> -o jsonpath` style via the K8s client.
    4. **Kill controller pod**: `exec.Command("kubectl", "delete", "pod", "-l", "app=tide-controller-manager", "-n", "tide-system").Run()`. Use the kubectl binary on PATH (suite_test.go conventions).
    5. **Wait for new leader**: Eventually poll the Lease object `kube-system/tide-controller-leader-election` (or the Phase 1 D-G1-named lease) until HolderIdentity changes from the killed pod. Timeout: 30s.
    6. **Snapshot post-restart state**: same Map.
    7. **Pillar 1 (Job UID continuity)**: assert pre.JobUID == post.JobUID for β and γ.
    8. **Pillar 2 (Attempt counter unchanged)**: assert pre.Attempt == post.Attempt for all 3 Tasks.
    9. **Pillar 3 (Completed-set preserved)**: assert α.Status.Phase=="Succeeded" pre AND post; α.CompletedAt unchanged.
    10. **Signal β + γ release**: `kubectl exec` into the controller-manager Pod (or spawn a small writer Job with PVC mount) → `touch /workspace/<chaos-resume.UID>/workspace/envelopes/<task-uid>/release` for β AND γ. (Path includes Project.UID per Phase 2 D-G2 PVC subPath layout.)
    11. **Pillar 4 (Observed completion across kill)**: Eventually β and γ reach Succeeded; the Wave that owns them reaches Succeeded; `kubectl get jobs --all-namespaces -l tideproject.k8s/project-uid=<project-uid>` returns exactly 3 Jobs with status.succeeded=1.
    12. **Pillar 5 (Algorithmic invariant — W12)**: invoke `pkg/dag.ComputeWaves` against the post-restart Task list; compare to a golden file. The golden file is a small JSON snapshot of the pre-kill wave structure (saved via `testdata/chaos-resume-waves.golden.json`); if pre/post match, assertion passes. The golden file is committed alongside this spec — generation recipe in the test header comment: "to regenerate, set env GENERATE_GOLDEN=1 and rerun; the test writes the pre-kill ComputeWaves output to the golden path and exits without running the post-kill comparison."

    **CRITICAL test-structure note (B6 — checker authority)**: wrap each pillar assertion in a `By(name)` label so Ginkgo's progress reporter prints the pillar name at each step boundary. A failure surfaces as "Pillar 2: Task.Status.Attempt unchanged across kill — failed" rather than the entire chaos-resume spec failing opaquely. CONTEXT.md `<specifics>` #5 ("single load-bearing claim, not a constellation of small tests") is honored — there's still ONE `It(...)` block; the named `By(...)` subtests are within the same load-bearing claim. The 5 By labels are: "Pillar 1: Job UID continuity", "Pillar 2: Attempt unchanged", "Pillar 3: Completed-set preserved", "Pillar 4: Observed completion across kill", "Pillar 5: Algorithmic invariant (ComputeWaves golden-file comparison)".

    Use `Label("kind")` so envtest-only filters skip it; use `By(...)` for step narration.

    AfterEach: `Expect(deleteNamespace("chaos-resume-test")).To(Succeed())` to leave the cluster clean for the next spec / rerun.
  </action>
  <verify>
    <automated>make test-int 2>&1 | tee /tmp/03-10-chaos-resume.log; grep -cE 'chaos-resume.*PASS' /tmp/03-10-chaos-resume.log</automated>
  </verify>
  <acceptance_criteria>
    - `ls -la test/integration/kind/testdata/chaos-resume-three-task.yaml` shows a non-zero-byte file.
    - `grep -cE 'testMode: wait-for-signal' test/integration/kind/testdata/chaos-resume-three-task.yaml` returns ≥2 (β and γ use the new mode from plan 03-02).
    - `grep -cE 'testMode: success' test/integration/kind/testdata/chaos-resume-three-task.yaml` returns ≥1 (α).
    - `grep -cE 'kubectl delete pod' test/integration/kind/chaos_resume_test.go` returns ≥1 (pod-kill step).
    - `grep -cE 'Label\("kind"\)' test/integration/kind/chaos_resume_test.go` returns 1 (Layer B label).
    - `grep -cE 'JobUID|Job UID' test/integration/kind/chaos_resume_test.go` returns ≥1 (Pillar 1 reference).
    - `grep -cE 'Attempt' test/integration/kind/chaos_resume_test.go` returns ≥1 (Pillar 2 reference).
    - `grep -cE 'Succeeded.*alpha|alpha.*Succeeded' test/integration/kind/chaos_resume_test.go` returns ≥1 (Pillar 3 reference).
    - `grep -cE '/release|"release"' test/integration/kind/chaos_resume_test.go` returns ≥1 (signal file).
    - `grep -cE 'ComputeWaves' test/integration/kind/chaos_resume_test.go` returns ≥1 (Pillar 5 — algorithmic invariant).
    - `grep -cE 'By\("Pillar [1-5]:' test/integration/kind/chaos_resume_test.go` returns 5 (B6 — one By label per pillar for localized failure attribution).
    - `ls -la test/integration/kind/testdata/chaos-resume-waves.golden.json` returns a non-zero-byte file (Pillar 5 golden snapshot committed).
    - `make test-int` runs the spec; the Ginkgo summary line includes "chaos-resume" with a PASS status.
    - Wall-clock for chaos-resume spec ≤ 90s (verify via Ginkgo `--reportFormat=json` or by reading the spec timing in test output).
  </acceptance_criteria>
  <done>chaos_resume_test.go ships with the full D-D4 four-pillar (+ algorithmic invariant) assertion set; fixture YAML uses wait-for-signal mode; spec PASSes in clean run + rerun.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: push_lease_test.go + up_stack_dispatch_test.go — push semantics and reconciler dispatch smoke specs</name>
  <files>test/integration/kind/push_lease_test.go, test/integration/kind/up_stack_dispatch_test.go, test/integration/kind/testdata/push-lease-project.yaml, test/integration/kind/testdata/up-stack-project.yaml, Makefile</files>
  <read_first>
    - test/integration/kind/wave_test.go (Layer B kind spec template — applyFile + Eventually-Get pattern; mirror)
    - test/integration/kind/credproxy_test.go (Ginkgo Describe shape + PodStatusEnvelopeReader observation pattern)
    - test/integration/kind/suite_test.go (BeforeSuite helm install, AfterSuite cleanup)
    - internal/controller/project_controller.go (plan 03-08 — push Job + bypass annotation behavior to assert from this spec)
    - internal/controller/milestone_controller.go (plan 03-08 — child Phase materialization to assert from up-stack-dispatch spec)
    - Makefile (test-int and test-int-kind-prep targets — image preload pattern to extend)
    - .planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-CONTEXT.md (D-B5, D-B6, D-A1, D-A2)
  </read_first>
  <behavior>
    - Test 1 (push_lease, first push omits lease): apply a Project with an httptest-or-fixture-image git server URL; trigger a level boundary (manually create a completed Milestone via direct kubectl patch — bypasses real planner flow for this spec); reconciler dispatches `tide-push-<project-uid>` Job; observe Job has no --last-pushed-sha arg (or empty arg); Job completes; Project.Status.Git.LastPushedSHA is populated.
    - Test 2 (push_lease, subsequent push honors lease): from Test 1's end state, trigger another level boundary; push Job dispatched with --last-pushed-sha=<value from Test 1>; succeeds; Status.Git.LastPushedSHA updated.
    - Test 3 (push_lease, stale-lease rejection): mock the push Job's outcome by directly patching its status to mimic exit code 1 with envelope reason="lease-rejected" — reconciler patches Status.Phase="PushLeaseFailed" + Condition + LeaseFailureCount++.
    - Test 4 (push_lease, bypass annotation recovery): from Test 3 end state, `kubectl annotate project foo tideproject.k8s/bypass-push-lease=true`; reconciler clears Status.Phase and requeues; next push attempt fires.
    - Test 5 (up_stack_dispatch, Milestone → Phase materialization): apply a Milestone CRD; MilestoneReconciler dispatches Job `tide-milestone-<uid>-1`; simulate Job completion by patching the Job status + writing a canned EnvelopeOut with childCRDs=[{Kind:"Phase", Name:"p1", Spec:{milestoneRef: ...}}]; reconciler materializes Phase CRD; assert Phase exists with OwnerRef pointing at Milestone.
    - All specs Label("kind"); both must PASS in clean run + KEEP_KIND_CLUSTER=true rerun.
  </behavior>
  <action>
    For test scaffolding ergonomics, the planner picks ONE of these approaches per spec, document inline:

    **For push_lease_test.go** (Tests 1-4):
    - Real git server in-cluster is heavy. Acceptable shortcut: skip the actual `git push` against a remote AND skip the diff/gitleaks scan; mock the push Job by deploying a "fake-push" Pod that emits a pre-canned push-result envelope based on a test-mode env var (similar to stub-subagent). This isolates the test to the ProjectReconciler's HANDLING of push outcomes — which is the actual contract.
    - Implementation: define a test-only image OR use `cmd/stub-subagent` extension with a `push-result-emit` mode (NOT in plan 03-02 scope; if absent, the spec can simply patch the existing tide-push Job's status directly).
    - Simplest: directly patch Job.Status via the test's k8sClient. This bypasses the actual push Job execution but exercises the ProjectReconciler's completion handler — the contract under test.

    Create `test/integration/kind/testdata/push-lease-project.yaml`:
    ```
    apiVersion: tideproject.k8s/v1alpha1
    kind: Project
    metadata: {name: push-lease, namespace: push-lease-test}
    spec:
      git:
        repoURL: "https://example.invalid/owner/repo.git"  # never actually pushed to — we mock outcomes
        credsSecretRef: push-lease-creds
      subagent: {image: "ghcr.io/jsquirrelz/tide-stub-subagent:v0.1.0"}
    ---
    apiVersion: v1
    kind: Secret
    metadata: {name: push-lease-creds, namespace: push-lease-test}
    type: Opaque
    stringData:
      GIT_PAT: dummy-pat  # not used; remote is never contacted in this spec
    ```

    Create `test/integration/kind/push_lease_test.go` with `Describe("Push lease semantics", Label("kind"), func() { ... })`:
    - Test 1: apply fixture; wait for Project.Status.Git.BranchName to be set; assert format matches `tide/run-push-lease-\d+`; create a fake completed Milestone (direct CRD apply with Status patched to Succeeded); observe push Job created with name=tide-push-<project-uid> and args containing --branch=<branch>; patch Job.Status to Succeeded + simulate push-result envelope writeback (this requires PodStatusEnvelopeReader's terminationMessagePath path — write the envelope JSON to a Pod's terminationMessage); assert Project.Status.Git.LastPushedSHA populated.
    - Test 2: simulate another level boundary; observe second push Job dispatched with --last-pushed-sha=<value from Test 1>.
    - Test 3: simulate Job failure + reason="lease-rejected" envelope; assert Status.Phase="PushLeaseFailed" + LeaseFailureCount==1.
    - Test 4: annotate the Project with `tideproject.k8s/bypass-push-lease=true`; assert Status.Phase cleared and another push Job dispatched.

    **For up_stack_dispatch_test.go** (Test 5):
    - Less risky — exercises the Milestone → Phase materialization shape without git side effects.

    Create `test/integration/kind/testdata/up-stack-project.yaml`. **W13 caveat per checker**: ownerReferences with placeholder UIDs are REJECTED by K8s admission (UID mismatch). Two options: (a) omit ownerReferences from the YAML and rely on the orchestrator's owner-cascade logic to set them on first reconcile (preferred for v1.0 — matches the orchestrator's normal flow), OR (b) apply the Project first, read back its UID via the k8sClient, then construct + apply the Milestone programmatically with the real UID. This plan uses (a) — let the controller set ownerReferences:
    ```
    apiVersion: tideproject.k8s/v1alpha1
    kind: Project
    metadata: {name: up-stack, namespace: up-stack-test}
    spec:
      subagent: {image: "ghcr.io/jsquirrelz/tide-stub-subagent:v0.1.0"}
    ---
    apiVersion: tideproject.k8s/v1alpha1
    kind: Milestone
    metadata: {name: m1, namespace: up-stack-test}
    spec:
      projectRef: up-stack
    # ownerReferences omitted — ProjectReconciler sets them on first Reconcile after the Milestone is observed.
    ```
    The test asserts the OwnerReferences are set CORRECTLY by the controller (which is the actual under-test contract).

    Create `test/integration/kind/up_stack_dispatch_test.go` with `Describe("Up-stack dispatch", Label("kind"), func() { ... })`:
    - Test 5: apply fixture; observe MilestoneReconciler dispatches Job tide-milestone-<m1-uid>-1 (via stub-subagent; mode=success); simulate Job completion (patch status) + write a canned EnvelopeOut.childCRDs=[{Kind:"Phase", Name:"p1", Spec: rawJSON(PhaseSpec{MilestoneRef: "m1"})}]; assert Phase p1 is created with OwnerRef pointing at Milestone m1 (Controller=true).

    Extend `Makefile`:
    - test-int-kind-prep target: add image preload for `tide-push` (if push_lease spec uses the real image; if all push outcomes are mocked, skip). For now, preload tide-push since the buildPushJob spec still produces a Job that references the image (the kind kubelet will try to pull; preload avoids ImagePullBackoff blocking the test).
    - Mirror the Phase 2 image-preload pattern: `kind load docker-image ghcr.io/jsquirrelz/tide-push:v0.1.0 --name $(KIND_CLUSTER_NAME)`.
    - Do NOT preload claude-subagent (not needed for any plan 03-10 spec — those use stub-subagent with wait-for-signal).
  </action>
  <verify>
    <automated>make test-int 2>&1 | tee /tmp/03-10-push-lease-upstack.log; grep -cE 'push lease.*PASS|up-stack.*PASS' /tmp/03-10-push-lease-upstack.log</automated>
  </verify>
  <acceptance_criteria>
    - `ls -la test/integration/kind/testdata/push-lease-project.yaml test/integration/kind/testdata/up-stack-project.yaml` shows 2 non-zero-byte files.
    - `grep -cE 'Label\("kind"\)' test/integration/kind/push_lease_test.go test/integration/kind/up_stack_dispatch_test.go | grep -v 0` returns 2 (Layer B labels).
    - `grep -cE 'tide-push-' test/integration/kind/push_lease_test.go` returns ≥1 (deterministic Job name assertion).
    - `grep -cE 'tide/run-' test/integration/kind/push_lease_test.go` returns ≥1 (branch name format assertion — `tide/run-<name>-<unix>`).
    - `grep -cE 'PushLeaseFailed' test/integration/kind/push_lease_test.go` returns ≥1 (Test 3 assertion).
    - `grep -cE 'bypass-push-lease' test/integration/kind/push_lease_test.go` returns ≥1 (Test 4 annotation).
    - `grep -cE 'tide-milestone-' test/integration/kind/up_stack_dispatch_test.go` returns ≥1 (Test 5 Job name assertion).
    - `grep -cE 'OwnerRef|ownerReferences' test/integration/kind/up_stack_dispatch_test.go` returns ≥1 (cascade assertion).
    - `grep -cE 'kind load docker-image.*tide-push' Makefile` returns ≥1 (image preload).
    - `make test-int` runs all 3 new Layer B specs + the existing 7 Phase 2 specs; all 10 PASS.
    - `KEEP_KIND_CLUSTER=true make test-int` rerun (immediately following clean run) — all 10 PASS again (Phase 02.2 closeout pattern).
    - Total `make test-int` wall-clock ≤ 600s (within the Phase 02.2 1800s outer timeout with margin).
  </acceptance_criteria>
  <done>push_lease_test.go + up_stack_dispatch_test.go both ship and PASS in clean + rerun; fixture YAMLs land; Makefile preloads tide-push image; total 10/10 Layer B specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → kind cluster | Tests use stub-subagent (no real Claude calls), dummy git PAT ("dummy-pat" — never sent to a real remote since outcomes are mocked), and namespace-scoped fixtures. No real credentials in the test cluster. |
| Pod-kill orchestration → controller-runtime leader election | The chaos-resume spec exercises the actual leader-election handoff that production relies on. If the test reveals a stuck-lease bug, that's a Phase 3 regression that gates the verification gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-310 (Information Disclosure — test fixture leaks credentials into kind cluster) | testdata/*.yaml | mitigate | All fixtures use placeholder PAT "dummy-pat" + invalid remote URLs (example.invalid). Push outcomes are mocked via Job status patches — no real PAT is ever transmitted. The chaos-resume spec uses NO git config at all. Acceptance criteria verify no real credentials in testdata files (`grep -E 'ghp_|gho_|ghu_|ghs_|ghr_|github_pat_' test/integration/kind/testdata/*.yaml` returns 0). |
| (Stale leader holding lease — chaos-resume failure mode) | Tampering / DoS | controller-runtime leader-election | mitigate | If chaos_resume_test.go fails — that's a regression in the leader-election + dispatch flow, NOT a security issue. The test gates Phase 3 verification; failures block the gate. Phase 02.2 cascade lessons: investigate logs first, don't hypothesize causes. |
</threat_model>

<verification>
- `make test-int` runs ALL Layer B specs (7 Phase 2 + 3 Phase 3 = 10 specs) and ALL PASS.
- `KEEP_KIND_CLUSTER=true make test-int` rerun PASS (idempotency property from Phase 02.2 closeout).
- Total wall-clock ≤ 600s.
- `grep -E 'ghp_|gho_|ghu_|ghs_|ghr_|github_pat_' test/integration/kind/testdata/*.yaml` returns 0 (no real PATs in testdata).
- `go vet ./test/integration/...` exits 0.
</verification>

<success_criteria>
- chaos_resume_test.go covers D-D4 four pillars + algorithmic invariant; PASS in clean + rerun.
- push_lease_test.go covers first-push, subsequent-push lease, stale-lease rejection, bypass-annotation recovery.
- up_stack_dispatch_test.go covers Milestone→Phase materialization with OwnerRef cascade.
- Makefile image preload extended for tide-push.
- Phase 3 success criteria #1, #2, #5 (chaos-resume) are validated by this plan; #3 (real subagent flow) and #4 (live Claude E2E TEST-03) are validated by plan 03-11.
</success_criteria>

<output>
After completion, create `.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-10-SUMMARY.md`.
</output>
