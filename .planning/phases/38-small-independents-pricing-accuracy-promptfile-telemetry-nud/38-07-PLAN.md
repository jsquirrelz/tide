---
phase: 38-small-independents-pricing-accuracy-promptfile-telemetry-nud
plan: 07
type: execute
wave: 2
depends_on: [38-02]
files_modified:
  - internal/controller/project_controller.go
  - internal/controller/child_rollup_idempotency_test.go
  - internal/controller/suite_test.go
  - internal/controller/*_test.go
  - Makefile
autonomous: true
requirements: [DEBT-01, DEBT-03]
must_haves:
  truths:
    - "The project-level PlannerRolledUpUID stamp uses the hardened RetryOnConflict + MergeFromWithOptimisticLock pattern and returns the error instead of swallowing (DEBT-01 / audit W1)"
    - "Heavy controller envtest specs run in the integration tier via Label(\"heavy\") + a suite-level short-mode guard; the -short unit tier skips them unmodified (DEBT-03)"
    - "Total spec count is conserved across the split, measured with the same instrument (Ginkgo's own Ran X of Y summaries) before and after (Pitfall 6)"
    - "make test-leader-election keeps working; the leader-election Short() guard is untouched"
  artifacts:
    - path: "internal/controller/project_controller.go"
      provides: "hardened PlannerRolledUpUID stamp (WR-02/WR-03 pattern)"
      contains: "retry.RetryOnConflict"
    - path: "internal/controller/suite_test.go"
      provides: "suite-level BeforeEach skipping heavy-labeled specs under -short"
      contains: "heavy"
    - path: "Makefile"
      provides: "test-heavy target + heavy-tier line in test-int-fast and test-int"
      contains: "test-heavy"
  key_links:
    - from: "internal/controller/suite_test.go guard"
      to: "Label(\"heavy\") specs"
      via: "labels are the single source of truth; the guard reads CurrentSpecReport().Labels()"
      pattern: "Label(\"heavy\")"
    - from: "Makefile test-int-fast / test-int"
      to: "internal/controller heavy specs"
      via: "go test ./internal/controller/... -ginkgo.label-filter='heavy'"
      pattern: "label-filter='heavy'"
---

<objective>
Retire the two remaining v1.0.6 audit debts that live in internal/controller: harden the project-level `PlannerRolledUpUID` stamp to the WR-02/WR-03 pattern the sibling controllers already use (DEBT-01), and split the heavy envtest specs out of the TEST-01 unit tier into the integration tier via Ginkgo labels with spec-count conservation (DEBT-03).

Purpose: W1 is the last plain-MergeFrom + swallowed-error rollup marker (a durable-marker race under concurrent status writers); the 167-spec envtest suite makes `make test` drift past its TEST-01 budget.
Output: hardened stamp + test, heavy labels + suite guard, Makefile heavy tier, conservation arithmetic in the SUMMARY.

Sequenced wave 2 after plan 38-02 because both touch project_controller.go and the controller test files.
</objective>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| hardened PlannerRolledUpUID stamp | rewritten block, project_controller.go ~1370-1381 | retry.RetryOnConflict + MergeFromWithOptimisticLock + return-don't-swallow |
| `Label("heavy")` | Ginkgo label on selected slow specs | data-driven selection, threshold ≥ 2s |
| suite-level short-mode heavy guard | BeforeEach in suite_test.go | `testing.Short()` + CurrentSpecReport().Labels() |
| `make test-heavy` | new Makefile target | `go test ./internal/controller/... -ginkgo.label-filter='heavy'` with envtest assets |
| heavy-tier invocation in test-int-fast / test-int | Makefile lines | "Layer A2" alongside the existing Layer A |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-CONTEXT.md
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md
@.planning/milestones/v1.0.6-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-01 — harden the project-level PlannerRolledUpUID stamp (audit W1)</name>
  <files>internal/controller/project_controller.go, internal/controller/child_rollup_idempotency_test.go</files>
  <read_first>
    - internal/controller/milestone_controller.go lines 599-636 — the exact hardened block to adapt (WR-02/WR-03 comments + retry.RetryOnConflict + MergeFromWithOptimisticLock + return ctrl.Result{}, fmt.Errorf on exhaustion)
    - internal/controller/project_controller.go lines 1354-1395 — the W1 site: current plain MergeFrom + non-fatal log swallow, the Pitfall-2 marker-after-rollup ordering comment, and the setBillingHaltIfNeeded code that FOLLOWS (whose flow must be preserved when the happy path continues)
    - internal/controller/child_rollup_idempotency_test.go — FULL file: the ReporterImage="" isFirstCompletion simulation, helper conventions, and the three existing child-level specs to mirror at project level
  </read_first>
  <action>
    Replace the marker-patch block at project_controller.go ~1376-1380 (inside the `else` arm after a successful `budget.RollUpUsage`) with the milestone_controller.go:620-633 pattern adapted to Project/PlannerRolledUpUID (DEBT-01, audit W1):
    - re-fetch `latest` Project via `r.Get(ctx, client.ObjectKeyFromObject(project), ...)` inside `retry.RetryOnConflict(retry.DefaultRetry, ...)`
    - idempotent early-return when `latest.Status.Budget.PlannerRolledUpUID == plannerJobName` (another writer already stamped)
    - `client.MergeFromWithOptions(latest.DeepCopy(), client.MergeFromWithOptimisticLock{})` patch setting the marker on `latest`
    - on retry exhaustion: `return ctrl.Result{}, fmt.Errorf("patch PlannerRolledUpUID: %w", mErr)` — return, don't swallow (WR-03)
    - copy the WR-02/WR-03 explanatory comment block (adapted wording) and PRESERVE the existing Pitfall-2 comment/ordering: the marker is attempted only after a successful rollup; a failed rollup still leaves the marker unset for retry. Import `k8s.io/client-go/util/retry` if not already imported in the file.
    Note the plan 38-02 rebase: the site now also carries a setPricingFallbackIfNeeded call — keep it adjacent and untouched.

    Extend child_rollup_idempotency_test.go with a project-level spec family mirroring the milestone one: first planner-completion rolls up usage and stamps PlannerRolledUpUID; a second completion pass (simulated post-TTL-GC, ReporterImage="") does NOT double-count CostSpentCents — the durable marker is the sole guard. Ginkgo Describe text should include "PlannerRolledUpUID" so the RESEARCH validation map's `-ginkgo.focus='PlannerRolledUpUID'` selects it.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -short -timeout 360s -ginkgo.focus='PlannerRolledUpUID' -v</automated>
  </verify>
  <acceptance_criteria>
    - The W1 site uses retry.RetryOnConflict + MergeFromWithOptimisticLock and returns the error on exhaustion: `awk '/plannerJobName := fmt.Sprintf/,/^	}/' internal/controller/project_controller.go | grep -c 'RetryOnConflict'` >= 1 and the same region contains `MergeFromWithOptimisticLock`
    - The marker-only-after-successful-rollup ordering is preserved (rollErr branch still logs non-fatally WITHOUT stamping)
    - New PlannerRolledUpUID spec(s) pass; the full existing suite stays green under `make test`
    - No other rollup site or guard in project_controller.go is modified
  </acceptance_criteria>
  <done>The last plain-MergeFrom rollup marker is retired; conflict-loss and swallow-on-error are both closed with envtest coverage.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-03 — measure spec timings, label heavy specs, add the suite-level short guard</name>
  <files>internal/controller/suite_test.go, internal/controller/*_test.go (selection data-driven)</files>
  <read_first>
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md — Open Question 4 (the umbrella-invocation flag constraint: -ginkgo.* flags fail on non-Ginkgo packages, so the unit tier CANNOT take a label-filter; the guard must live in code) and Pitfall 6 (conservation instrument)
    - internal/controller/leader_election_test.go lines 45-60 — the existing testing.Short() skip precedent (leave it untouched; keep make test-leader-election working)
    - internal/controller/suite_test.go — suite bootstrap (TestControllers/RunSpecs) where the guard installs
  </read_first>
  <action>
    Mechanism (planner's pick per CONTEXT discretion, honoring the RESEARCH Q4 constraint): Ginkgo `Label("heavy")` is the SINGLE source of truth; the `-short` unit tier skips heavy specs via one suite-level guard, so `make test`'s umbrella invocation needs no flag changes.

    1. BASELINE FIRST (Pitfall 6 — same instrument): run `go test ./internal/controller/... -short -timeout 360s` (KUBEBUILDER_ASSETS set) and record the Ginkgo summary line `Ran X of Y Specs` — X_pre and Y_pre go in the SUMMARY.
    2. Timing report: rerun with `--ginkgo.json-report=/tmp/controller-timings.json`; parse per-spec RunTime from the report. Select every spec with runtime >= 2s (the RESEARCH-suggested threshold; the envtest-apiserver-heavy files like billing_halt_regression_test.go and project_controller_test.go are the expected population). Record the selected list + timings in the SUMMARY. If the environment cannot produce the report (envtest assets unavailable), fall back to labeling the RESEARCH-named heavy candidate files' top-level Describes and say so in the SUMMARY — never guess silently.
    3. Add `Label("heavy")` to the selected specs — prefer labeling the containing Describe/Context when all its Its are heavy, individual Its otherwise. Do NOT label or modify the Leader Election spec (its Short() guard + make test-leader-election stay as-is).
    4. suite_test.go: add a suite-level `var _ = BeforeEach(func() { ... })` that, when `testing.Short()` is true and `CurrentSpecReport().Labels()` contains "heavy", calls `Skip("heavy spec: excluded from the -short unit tier (Phase 38 DEBT-03); runs via make test-heavy / test-int")`. Comment explains the mechanism choice: labels are the single source of truth; the guard exists because the umbrella `make test` invocation spans non-Ginkgo packages where -ginkgo.label-filter is an unknown flag (RESEARCH Q4).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -short -timeout 360s 2>&1 | grep -E 'Ran [0-9]+ of [0-9]+ Specs'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'Label("heavy")' internal/controller/*_test.go | awk -F: '{s+=$2} END {print s}'` >= 1 (at least one spec labeled, selection justified by the timing report in the SUMMARY)
    - Unit-tier run now prints `Ran X_unit of Y Specs` with Y == Y_pre (no specs lost) and X_unit < X_pre (heavy specs skipped)
    - The skipped delta equals the number of heavy-labeled runtime specs (X_pre - X_unit == heavy count from step 2's selection)
    - leader_election_test.go is unmodified (git diff clean on that file)
  </acceptance_criteria>
  <done>Heavy specs carry the label; the -short unit tier skips exactly them; baseline arithmetic recorded.</done>
</task>

<task type="auto">
  <name>Task 3: DEBT-03 — Makefile heavy tier + conservation proof</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 80-160 — test/test-only (umbrella, MUST stay single-invocation), test-leader-election (KUBEBUILDER_ASSETS one-liner shape to copy), test-int-fast and test-int (the Layer A echo/invocation pattern to extend)
  </read_first>
  <action>
    1. Add a `test-heavy` target after `test-leader-election`, same shape: `.PHONY: test-heavy` / help text "Run the heavy controller envtests extracted from the unit tier (Phase 38 DEBT-03; label-filter='heavy')" / `KUBEBUILDER_ASSETS=... go test ./internal/controller/... -timeout 20m -v -ginkgo.v -ginkgo.label-filter='heavy'`. (Single Ginkgo-importing package — the label-filter flag is valid here, per the test-leader-election precedent.)
    2. Extend BOTH `test-int-fast` and `test-int` with a heavy-tier line following the existing Layer A style: an `echo "=== Layer A2 (heavy controller envtests, Phase 38 DEBT-03) ==="` followed by the same `go test ./internal/controller/... -timeout 20m -ginkgo.v -ginkgo.label-filter='heavy' -ginkgo.flake-attempts=3` invocation (inside test-int's `set -e` shell so a failure fails the target; in test-int-fast as an additional recipe line). Do NOT touch the `test`/`test-only` umbrella invocations (RESEARCH Q4: the umbrella spans non-Ginkgo packages).
    3. Conservation proof (Pitfall 6, same instrument): run the unit tier (`go test ./internal/controller/... -short ...` → `Ran X_unit of Y`) and the heavy tier (`make test-heavy` → `Ran X_heavy of Y`); assert Y is identical in both and X_unit + X_heavy == X_pre from Task 2's baseline (the leader-election Short() skip appears in both unit runs and cancels). Record all five numbers (X_pre, Y_pre, X_unit, X_heavy, Y) in the SUMMARY as the DEBT-03 conservation evidence.
  </action>
  <verify>
    <automated>grep -c "label-filter='heavy'" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "label-filter='heavy'" Makefile` == 3 (test-heavy + test-int-fast + test-int)
    - `make test-heavy` runs only heavy-labeled specs and exits 0 (read MAKE_EXIT and grep for FAIL lines per CLAUDE.md verification discipline — Ginkgo green alone is not the gate)
    - Conservation arithmetic holds: X_unit + X_heavy == X_pre with Y unchanged — numbers recorded in the SUMMARY
    - `make test` recipe lines for the umbrella invocation are unchanged (git diff on Makefile shows only the new target + the two Layer A2 additions)
  </acceptance_criteria>
  <done>DEBT-03 complete: heavy tier wired into the integration targets, unit tier unchanged in shape, zero specs lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent reconcilers → Project .status | durable idempotency marker written under contention |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-19 | Tampering | lost PlannerRolledUpUID stamp under ResourceVersion conflict → double budget count on halt→resume | high | mitigate | RetryOnConflict + optimistic lock + return-don't-swallow (the audited WR-02/WR-03 pattern); envtest exactly-once spec |
| T-38-20 | Repudiation | specs silently dropped in the tier split (coverage loss disguised as speedup) | medium | mitigate | same-instrument conservation arithmetic (Ginkgo Ran X of Y sums) recorded in SUMMARY; acceptance-gated |
| T-38-21 | DoS | heavy tier bloating CI critical path | low | accept | heavy tier rides test-int-fast/test-int (already off the per-push TEST-01 path) |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: empty) |
</threat_model>

<verification>
- `make test` green AND faster/equal unit tier; `make test-heavy` green; `make test-leader-election` still green (all in the dev VM / docker golang:1.26 — Go absent on host Mac; read MAKE_EXIT + grep '^--- FAIL' per CLAUDE.md)
- Conservation numbers recorded and arithmetic holds
</verification>

<success_criteria>
- DEBT-01: the W1 stamp is hardened with coverage. DEBT-03: heavy specs run in the integration tier, spec count conserved, unit-tier umbrella untouched.
</success_criteria>

<output>
Create `.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-07-SUMMARY.md` when done, including the heavy-spec selection list with timings and the five conservation numbers.
</output>
