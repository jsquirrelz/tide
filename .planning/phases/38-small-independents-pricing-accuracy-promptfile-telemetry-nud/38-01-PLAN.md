---
phase: 38-small-independents-pricing-accuracy-promptfile-telemetry-nud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json
  - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md
autonomous: false
requirements: [COST-03, COST-01]
must_haves:
  truths:
    - "The cache-write TTL the pinned claude CLI actually requests is observed from a real teed dispatch and recorded before any pricing row ships (D-05/D-06/D-07)"
    - "The 2026-07-03 first-run per-dispatch token usage is preserved in-repo before the perishable minikube PVC evidence is lost (D-04)"
    - "The recorded verdict names exactly one cacheWriteMultiplier value (125/100 or 200/100) that plan 38-06 compiles"
  artifacts:
    - path: ".planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md"
      provides: "COST-03 probe evidence: distinct cache_control values, CLI version, model, date, derived multiplier verdict"
      contains: "verdict: cacheWriteMultiplier"
    - path: "internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json"
      provides: "Distilled per-dispatch model + token usage from the 2026-07-03 run envelopes, with provenance"
      contains: "provenance"
  key_links:
    - from: "internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json"
      to: "internal/subagent/anthropic/run_mix_regression_test.go (plan 38-06)"
      via: "run-mix fixture reads this testdata file"
      pattern: "first_run_2026-07-03_usage"
    - from: ".planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md"
      to: "internal/subagent/anthropic/pricing.go (plan 38-06)"
      via: "cacheWriteMultiplier constant comment cites this probe evidence verbatim (D-08)"
      pattern: "cacheWriteMult"
---

<objective>
Land the two operator-evidence gates that the COST cluster depends on: (1) export the 2026-07-03 first-run envelope usage off the perishable minikube PVC into an in-repo testdata fixture (D-04), and (2) run the COST-03 teed-credproxy probe on the operator's live cluster to observe which cache-write TTL the `claude` CLI dispatch surface requests (D-05, D-06, D-07), recording the verdict as a phase artifact.

Purpose: D-07 locks sequencing — the pricing rows (plan 38-06) must not ship ahead of the probe result, and the run-mix fixture needs the real token counts before cluster cleanup destroys them (RESEARCH Pitfall 2).
Output: `38-01-PROBE-RESULT.md` (verdict artifact) + `internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json` (distilled usage fixture data).
</objective>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json` | new test fixture file | JSON object: `{"provenance": string, "dispatches": [{"envelope": string, "model": string, "usage": {"inputTokens": n, "outputTokens": n, "cacheReadTokens": n, "cacheCreationTokens": n, "estimatedCostCents": n}}]}` |
| `.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md` | new phase artifact | Contains a machine-greppable line `verdict: cacheWriteMultiplier = 125/100` or `verdict: cacheWriteMultiplier = 200/100` |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-CONTEXT.md
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the 2026-07-03 first-run envelope usage off the minikube PVC into testdata</name>
  <files>internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json</files>
  <read_first>
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md — section "COST-01 Envelope Export Recipe" (the exact kubectl commands, PVC layout, project namespace)
    - pkg/dispatch/envelope.go — the `Usage` struct JSON tags (inputTokens, outputTokens, cacheReadTokens, cacheCreationTokens, estimatedCostCents) and the EnvelopeIn `Provider.Model` JSON path, so the distilled schema uses the exact wire field names
    - internal/dispatch/podjob/backend.go — envelope directory layout (`/workspaces/{project-uid}/workspace/envelopes/{uid}/{in,out}.json`)
  </read_first>
  <action>
    Execute the RESEARCH.md "COST-01 Envelope Export Recipe" (D-04): `kubectl config use-context minikube`; find the manager pod via `kubectl -n tide-system get pod -l control-plane=controller-manager -o jsonpath='{.items[0].metadata.name}'`; resolve the project UID from `kubectl -n tide-cashboard get project -o jsonpath='{.items[0].metadata.uid}'` (or, if the Project CRD was deleted, `kubectl -n tide-system exec "$MGR" -- ls /workspaces`); `kubectl -n tide-system exec "$MGR" -- find /workspaces/$PUID/workspace/envelopes -name out.json`; then `kubectl -n tide-system cp` the whole `envelopes/` tree to a LOCAL UNTRACKED temp dir (e.g. `/tmp/cb1605-envelopes` — the raw envelopes contain prompt/result text and MUST NOT be committed).

    Distill ONLY billing-relevant data into `internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json`: for each envelope dir, read the model ID from `in.json` (the EnvelopeIn provider model field — confirm the exact JSON path from pkg/dispatch/envelope.go tags) and the usage block from `out.json`. Schema: top-level object with `provenance` (string: run date 2026-07-03, cluster, export date, how many envelopes were found vs expected, and any gaps) and `dispatches` (array of `{envelope, model, usage:{inputTokens, outputTokens, cacheReadTokens, cacheCreationTokens, estimatedCostCents}}`). estimatedCostCents is the OLD table's tally — kept for the regression comment in plan 38-06, not replayed.

    Fallback (D-04 pre-authorizes): if the cluster, namespace, or PVC data is unreachable or partially GC'd, still create the file with whatever dispatches are recoverable and a `provenance` string that states exactly what was attempted, what was found, and what is missing. Do NOT fabricate token counts. The folded todo's model-mix prose and CONTEXT D-04's prose disagree ("2 Sonnet-5 + 1 Fable-5" vs the todo's variant) — the exported envelopes are ground truth; trust them over either prose (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>jq -e '.provenance and (.dispatches | type == "array")' internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json</automated>
  </verify>
  <acceptance_criteria>
    - `internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json` exists and `jq .` parses it (exit 0)
    - `.provenance` is a non-empty string naming the run date 2026-07-03 and the export outcome (full / partial / none)
    - Every entry in `.dispatches` has a non-empty `model` string and integer `usage.inputTokens`/`usage.outputTokens`/`usage.cacheReadTokens`/`usage.cacheCreationTokens`
    - No raw envelope `in.json`/`out.json` content (prompts, results) is committed anywhere under the repo — `git status --porcelain` shows only the testdata JSON as the new file from this task
  </acceptance_criteria>
  <done>Distilled usage fixture data committed in testdata with honest provenance; raw envelopes remain only in an untracked local dir.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: COST-03 probe — operator observes the claude CLI's cache-write TTL through a teed credproxy</name>
  <files>none — operator-run probe on the live minikube cluster; the evidence artifact is written by Task 3</files>
  <read_first>
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md — section "COST-03 Probe Recipe" (the full operator recipe, verbatim)
    - internal/credproxy/server.go — `--tee-body-dir` behavior (writes `req-N.json` per /v1/messages request, 0700 dir contract)
  </read_first>
  <action>
    PAUSE and present the RESEARCH.md "COST-03 Probe Recipe" to the operator verbatim (D-05/D-06 locked: the probe runs on the operator's live minikube cluster with the real key — Claude MUST NOT run it). Wait for the operator's report (the resume-signal below), then proceed to Task 3. This checkpoint is never auto-approvable: D-07 gates the pricing rows (plan 38-06) on this observation.
  </action>
  <what-built>Nothing is built by this task — it is the D-05/D-06 locked evidence gate. The recipe (authored at research time) dispatches one real `claude -p --bare` call through a teed credproxy on the operator's live minikube cluster with the real key, then reads the `cache_control` objects out of the teed request bodies.</what-built>
  <how-to-verify>
    Run the RESEARCH.md "COST-03 Probe Recipe" verbatim (it is operator-facing and copy-paste ready). Summary of the recipe you will run:
    1. `kubectl config use-context minikube`; create a 0700 tee dir (`TEE_DIR=$(mktemp -d) && chmod 0700 "$TEE_DIR"`).
    2. Start credproxy locally with `--tee-body-dir="$TEE_DIR"` and the real ANTHROPIC_API_KEY + TIDE_SIGNING_KEY from the cluster Secrets.
    3. EITHER run `go run -tags spike ./cmd/tide-spike/ -model claude-haiku-4-5` against the teed proxy (host CLI), OR — faithful to D-05 — run one dispatch from inside the claude-subagent image via the `docker run ... claude -p --model claude-haiku-4-5 --output-format stream-json ... --bare` command in the recipe (flag set copied from internal/subagent/anthropic/subagent.go:285-294).
    4. Read the verdict: `grep -o '"cache_control":{[^}]*}' "$TEE_DIR"/req-*.json | sort -u`
       - every match is `{"type":"ephemeral"}` (no ttl key) → 5-minute TTL → multiplier 1.25×
       - any match contains `"ttl":"1h"` → 1-hour TTL → multiplier 2×
       - a MIX of both shapes → report the full set (per RESEARCH A4 the phase treats any-1h-present as 2× and flags the mix)
    5. Also capture `claude --version` (from wherever step 3 ran), the model used, and today's date.
    6. Delete the tee dir afterward (`rm -rf "$TEE_DIR"`) — teed bodies contain prompt text and billing headers.
  </how-to-verify>
  <acceptance_criteria>
    - Operator reports: the distinct `cache_control` JSON values observed, `claude --version`, the model used, and the run date
    - The report is unambiguous about whether any `"ttl":"1h"` appeared
  </acceptance_criteria>
  <verify>
    <human-check>The operator's pasted report contains at least one verbatim cache_control JSON value and a claude --version string (or an explicit "probe not run" statement invoking the RESEARCH fallback)</human-check>
  </verify>
  <done>The operator has reported the observed cache_control set (or explicitly declined), giving Task 3 an unambiguous input.</done>
  <resume-signal>Paste the probe report: distinct cache_control values (verbatim), claude --version output, model, date. If the probe cannot be run at all, say so explicitly — the phase then encodes 1.25× (the current stored literals, zero numeric change) per the RESEARCH fallback, and the constant flip stays a one-line post-probe diff (D-08).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the probe verdict as the D-08 evidence artifact</name>
  <files>.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md</files>
  <read_first>
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-CONTEXT.md — D-08 (constant encoding + comment citing probe evidence) and the specifics section (CACHE-01 evidence style: date, model mix, both figures)
  </read_first>
  <action>
    Write `38-01-PROBE-RESULT.md` from the operator's Task 2 report, in the CACHE-01 evidence style. Required contents, in order: (1) heading with probe date and cluster; (2) the operator's verbatim report — distinct `cache_control` values, `claude --version`, model, date; (3) exactly one machine-greppable verdict line, either `verdict: cacheWriteMultiplier = 125/100` (all cache_control objects were `{"type":"ephemeral"}` with no ttl key → 5m TTL) or `verdict: cacheWriteMultiplier = 200/100` (any `"ttl":"1h"` observed → 1h TTL); (4) if the observed set was MIXED, a `mixed-ttl: true` line plus a note that the 2× verdict was chosen conservatively per RESEARCH A4 and that D-08's single-constant encoding should be revisited if mixes recur; (5) if the operator could not run the probe, verdict line `verdict: cacheWriteMultiplier = 125/100` with an explicit `probe-status: NOT RUN — pre-probe fallback (RESEARCH fallback: matches every existing row's stored literals)` line so plan 38-06 knows the evidence grade.
  </action>
  <verify>
    <automated>grep -cE '^verdict: cacheWriteMultiplier = (125|200)/100$' .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and contains exactly one line matching `^verdict: cacheWriteMultiplier = (125|200)/100$`
    - The operator's reported cache_control values, CLI version, model, and date appear verbatim in the file
    - If the probe was not run, the file contains a `probe-status: NOT RUN` line
  </acceptance_criteria>
  <done>Plan 38-06 can read a single unambiguous multiplier verdict with cited evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator laptop → live minikube cluster | real provider key + real billing traffic cross here during the probe |
| cluster PVC → git repo | run evidence (which embeds prompt/result text) crosses toward a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Information disclosure | teed request bodies (prompts + billing headers) in TEE_DIR | medium | mitigate | 0700 mktemp dir per credproxy server.go contract; operator-local only; recipe instructs `rm -rf` after reading the verdict |
| T-38-02 | Information disclosure | exported envelopes (prompt/result text) leaking into the public repo | high | mitigate | raw export goes to an untracked /tmp dir; only distilled model+token numbers are committed; Task 1 acceptance asserts git status shows only the testdata JSON |
| T-38-03 | Information disclosure | real ANTHROPIC_API_KEY pasted into shell history | medium | mitigate | recipe reads the key from the cluster Secret via kubectl rather than hand-pasting where possible; operator-run per locked D-06 |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: empty) |
</threat_model>

<verification>
- `jq -e '.provenance and (.dispatches | type == "array")' internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json` exits 0
- `grep -cE '^verdict: cacheWriteMultiplier = (125|200)/100$' .planning/phases/38-*/38-01-PROBE-RESULT.md` prints 1
- `git status --porcelain` shows no raw envelope JSON committed
</verification>

<success_criteria>
- Both evidence artifacts exist with honest provenance; the probe verdict is a single unambiguous multiplier; plan 38-06 is unblocked per D-07.
</success_criteria>

<output>
Create `.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-SUMMARY.md` when done, recording: export outcome (full/partial/none), the probe verdict line verbatim, and any mixed-TTL flag.
</output>
