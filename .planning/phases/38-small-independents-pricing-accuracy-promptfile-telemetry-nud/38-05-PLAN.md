---
phase: 38-small-independents-pricing-accuracy-promptfile-telemetry-nud
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/dashboard/main.go
  - cmd/dashboard/router.go
  - cmd/dashboard/api/config.go
  - cmd/dashboard/api/config_test.go
  - dashboard/web/src/components/TelemetryDisabledBanner.tsx
  - dashboard/web/src/components/TelemetryDisabledBanner.test.tsx
  - dashboard/web/src/components/TelemetryView.tsx
  - dashboard/web/src/components/__tests__/TelemetryView.test.tsx
autonomous: true
requirements: [TELEM-03]
must_haves:
  truths:
    - "The dashboard shows a telemetry banner that distinguishes disabled-by-config (PROMETHEUS_ENABLED=false) from no-data (enabled, queries return empty) — TELEM-03, D-14"
    - "The banner is text-distinguishable per state (the words differ, not just color) and read-only (no buttons, no dismissal)"
    - "The banner hides whenever any panel has data, is loading, or is unreachable (unreachable keeps the existing per-panel notice)"
    - "A legacy install without the PROMETHEUS_ENABLED env still behaves sensibly (binary falls back to PROM_ENDPOINT presence)"
  artifacts:
    - path: "cmd/dashboard/api/config.go"
      provides: "GET /api/v1/config handler returning {\"telemetryEnabled\": bool}"
      contains: "telemetryEnabled"
    - path: "dashboard/web/src/components/TelemetryDisabledBanner.tsx"
      provides: "presentational banner component, two states"
      contains: "telemetry-disabled-banner"
    - path: "dashboard/web/src/components/TelemetryView.tsx"
      provides: "banner state derivation + first-child rendering"
      contains: "TelemetryDisabledBanner"
  key_links:
    - from: "cmd/dashboard/main.go"
      to: "cmd/dashboard/router.go Dependencies"
      via: "TelemetryEnabled bool resolved from PROMETHEUS_ENABLED (fallback: PROM_ENDPOINT non-empty)"
      pattern: "PROMETHEUS_ENABLED"
    - from: "dashboard/web/src/components/TelemetryView.tsx"
      to: "GET /api/v1/config"
      via: "one-shot fetch feeding the telemetryEnabled boolean"
      pattern: "api/v1/config"
---

<objective>
Ship the TELEM-03 "telemetry disabled" banner: the dashboard server reads the chart-passed `PROMETHEUS_ENABLED` env (D-14; chart side lands in plan 38-04) and exposes it to the UI as `GET /api/v1/config` → `{"telemetryEnabled": bool}`; the Telemetry view renders a single view-level banner distinguishing disabled-by-config from no-data, per the 38-UI-SPEC Banner Contract (placement, tokens, copy, a11y are all locked there).

Purpose: on the first external-repo run the telemetry tab quietly showed empty panels; the operator had no signal that Prometheus was simply never wired.
Output: config endpoint + Dependencies field, TelemetryDisabledBanner component, TelemetryView derivation, Go + vitest coverage.
</objective>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `GET /api/v1/config` | new dashboard route (GET-only, DASH-05) | body `{"telemetryEnabled": true|false}` |
| `ConfigHandler` | new type, `cmd/dashboard/api/config.go` | field `TelemetryEnabled bool`, method `Get` |
| `Dependencies.TelemetryEnabled bool` | new field, `cmd/dashboard/router.go` | wired from main.go |
| `telemetryEnabledFromEnv() bool` | new func, `cmd/dashboard/main.go` | "true"→true, "false"→false, unset/other→ PROM_ENDPOINT != "" |
| `TelemetryDisabledBanner` | new React component | props `state: "disabled-by-config" \| "no-data"`; `data-testid="telemetry-disabled-banner"`, `data-state` attr, `role="status"` |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-UI-SPEC.md
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard server — PROMETHEUS_ENABLED env + GET /api/v1/config</name>
  <files>cmd/dashboard/main.go, cmd/dashboard/router.go, cmd/dashboard/api/config.go, cmd/dashboard/api/config_test.go</files>
  <read_first>
    - cmd/dashboard/router.go — the FULL route-table doc block (every route MUST be GET; TestZeroMutationRoutes walks it), Dependencies struct, RegisterRoutes wiring
    - cmd/dashboard/main.go lines 140-165 — the Dependencies literal (PrometheusEndpoint: os.Getenv("PROM_ENDPOINT") precedent)
    - cmd/dashboard/api/prometheus.go — handler struct + JSON encoding conventions (Content-Type, json.NewEncoder)
    - cmd/dashboard/router_test.go — TestZeroMutationRoutes and handler test conventions
  </read_first>
  <behavior>
    - Test 1: ConfigHandler{TelemetryEnabled: true}.Get → HTTP 200, Content-Type application/json, body `{"telemetryEnabled":true}`
    - Test 2: ConfigHandler{TelemetryEnabled: false}.Get → body `{"telemetryEnabled":false}`
    - Test 3: telemetryEnabledFromEnv: PROMETHEUS_ENABLED="true" → true; ="false" → false (even with PROM_ENDPOINT set); unset with PROM_ENDPOINT="http://x" → true; unset with PROM_ENDPOINT unset → false
    - Test 4: TestZeroMutationRoutes still passes with the new route registered
  </behavior>
  <action>
    1. `cmd/dashboard/api/config.go` (NEW): `ConfigHandler` struct with `TelemetryEnabled bool` and `Log logr.Logger`; `Get(w, r)` writes HTTP 200 JSON `{"telemetryEnabled": <bool>}` (key exactly `telemetryEnabled`). Doc comment: Phase 38 TELEM-03/D-14 — exposes the chart's prometheus.enabled toggle to the UI so the banner can distinguish disabled-by-config from no-data; GET-only per the DASH-05 zero-mutation contract.
    2. `cmd/dashboard/router.go`: add `TelemetryEnabled bool` to Dependencies (doc comment citing D-14); register `r.Get("/api/v1/config", configHandler.Get)` inside the existing `/api/v1` Route block; add the route to the route-table doc block comment (`GET /api/v1/config — telemetry-enabled flag (Phase 38 TELEM-03)`).
    3. `cmd/dashboard/main.go`: add `telemetryEnabledFromEnv() bool` — reads `PROMETHEUS_ENABLED`: literal "true" → true; literal "false" → false; unset or anything else → fall back to `os.Getenv("PROM_ENDPOINT") != ""` (legacy-chart compatibility: binary catches up to chart, never requires it; comment cites D-13/D-14). Wire `TelemetryEnabled: telemetryEnabledFromEnv()` into the Dependencies literal.
    4. `cmd/dashboard/api/config_test.go` (NEW): httptest-based tests for behaviors 1-2; env-behavior test for telemetryEnabledFromEnv lives beside it in cmd/dashboard (t.Setenv) — place where the function is visible (main package test file cmd/dashboard/main_test.go if one exists, else export the helper decision: keep the function in package main and test via a small main-package test file).
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/... -run 'TestZeroMutationRoutes|Config|TelemetryEnabled' -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"/api/v1/config"' cmd/dashboard/router.go` == 1 (registered exactly once, GET)
    - `curl`-shape contract locked by test: body is exactly `{"telemetryEnabled":true}` / `{"telemetryEnabled":false}`
    - TestZeroMutationRoutes passes (route walked, GET-only invariant holds)
    - PROMETHEUS_ENABLED="false" wins over a set PROM_ENDPOINT (explicit false is authoritative — D-14 disabled-by-config)
  </acceptance_criteria>
  <done>UI can fetch one boolean; legacy env-less deploys degrade to the endpoint-presence signal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TelemetryDisabledBanner component + TelemetryView derivation (UI-SPEC Banner Contract)</name>
  <files>dashboard/web/src/components/TelemetryDisabledBanner.tsx, dashboard/web/src/components/TelemetryDisabledBanner.test.tsx, dashboard/web/src/components/TelemetryView.tsx, dashboard/web/src/components/__tests__/TelemetryView.test.tsx</files>
  <read_first>
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-UI-SPEC.md — §Banner Contract (state machine + precedence), §Copywriting Contract (exact copy strings), §Color/§Typography (token names)
    - dashboard/web/src/components/TelemetryView.tsx — the PanelState union (lines 78-82), panelStates array ownership (~line 1101), the telemetry-view container (~line 1204), classifier (~lines 267-315)
    - dashboard/web/src/components/TelemetryUnavailableNotice.tsx — the per-panel notice this banner must NOT replace (styling idiom to echo)
    - dashboard/web/src/components/__tests__/TelemetryView.test.tsx — fetch-mocking conventions for the existing tests
  </read_first>
  <behavior>
    - Test 1: state="disabled-by-config" renders label "TELEMETRY DISABLED", message naming prometheus.enabled and docs/INSTALL.md, border/label using var(--color-status-warning), data-state="disabled-by-config", role="status"
    - Test 2: state="no-data" renders label "NO TELEMETRY DATA", message mentioning the Targets page, border var(--color-border-subtle), data-state="no-data"
    - Test 3 (TelemetryView): /api/v1/config → {telemetryEnabled:false} → banner (disabled-by-config) renders as first child of [data-testid="telemetry-view"]; per-panel TelemetryUnavailableNotice still renders in panel slots
    - Test 4 (TelemetryView): telemetryEnabled:true + all panels resolve kind:"data" with zero points → banner (no-data)
    - Test 5 (TelemetryView): telemetryEnabled:true + any panel has data OR any panel loading OR any panel unreachable → NO banner element in the DOM
    - Test 6 (TelemetryView): /api/v1/config fetch fails → no banner unless every panel resolved "unavailable" (defensive sentinel fallback)
  </behavior>
  <action>
    1. `TelemetryDisabledBanner.tsx` (NEW, purely presentational): props `{ state: "disabled-by-config" | "no-data" }`. Anatomy per UI-SPEC: `rounded border p-4`, background `var(--color-surface-raised)`, flex column `gap-2`; label line 12px/600, fontFamily var(--font-mono), uppercase, letterSpacing 0.05em, color = border token; message line 13px mono, color `var(--color-text-muted)`; border `var(--color-status-warning)` for disabled-by-config, `var(--color-border-subtle)` for no-data. Copy EXACTLY from the UI-SPEC Copywriting Contract table (both labels + both messages, verbatim). `data-testid="telemetry-disabled-banner"`, `data-state={state}`, `role="status"`. No buttons, links, dismissal, or animation (read-only dashboard).
    2. `TelemetryView.tsx`: on mount, fetch `/api/v1/config` once; hold `telemetryEnabled: boolean | null` (null = unknown/failed). Derive banner state with the UI-SPEC precedence: disabled-by-config when `telemetryEnabled === false` OR (defensively) every panel resolved "unavailable"; no-data when `telemetryEnabled === true` AND every panel is kind "data" with zero series points AND no panel is loading/unreachable; hidden otherwise (any data, any loading, any unreachable — unreachable keeps the existing per-panel notice). Render the banner as the FIRST child of the `data-testid="telemetry-view"` container, above the toolbar. Do not modify the PanelState machine, ChartPanel, or TelemetryUnavailableNotice.
    3. Tests: colocated `TelemetryDisabledBanner.test.tsx` for behaviors 1-2; extend `__tests__/TelemetryView.test.tsx` (mock fetch: route /api/v1/config plus the existing query_range mocks) for behaviors 3-6. Assert text distinguishability: the two states' rendered text must differ by the words "DISABLED" vs "NO TELEMETRY DATA" (TELEM-03's distinguishing requirement is text, not color).
  </action>
  <verify>
    <automated>cd dashboard/web && npx vitest run TelemetryDisabledBanner TelemetryView</automated>
  </verify>
  <acceptance_criteria>
    - All six behavior tests pass on host node 22 (`npx vitest run TelemetryDisabledBanner TelemetryView`)
    - Banner element carries data-testid, data-state, and role="status"; zero interactive elements inside it (no button/a tags)
    - Copy strings match the UI-SPEC Copywriting Contract verbatim
    - No new npm dependency added (package.json untouched)
    - Existing TelemetryView tests still pass unmodified in intent (only additive mocks)
  </acceptance_criteria>
  <done>TELEM-03 shipped: one view-level banner, two text-distinct states, correct precedence, per-panel degradation intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dashboard API | read-only config surface exposed to any dashboard viewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-13 | Tampering | new route breaking the DASH-05 zero-mutation contract | medium | mitigate | GET-only handler; TestZeroMutationRoutes walks the route table (acceptance-gated) |
| T-38-14 | Information disclosure | /api/v1/config leaking configuration detail | low | accept | exposes a single boolean; the same fact is already inferable from the unavailable sentinel |
| T-38-15 | Spoofing | banner claiming "disabled" while data flows (stale/wrong env) | low | mitigate | hidden-state precedence: any panel with data suppresses the banner; legacy env fallback in telemetryEnabledFromEnv |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: empty; UI-SPEC Registry Safety: none) |
</threat_model>

<verification>
- `go test ./cmd/dashboard/...` green (dev VM or docker golang:1.26)
- `cd dashboard/web && npx vitest run TelemetryDisabledBanner TelemetryView` green (host)
- Dashboard remains read-only: no mutation surface added (TestZeroMutationRoutes)
</verification>

<success_criteria>
- TELEM-03: banner distinguishes disabled-by-config from no-data by text and token, placed per UI-SPEC, with server boolean transported via GET /api/v1/config.
</success_criteria>

<output>
Create `.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-05-SUMMARY.md` when done.
</output>
