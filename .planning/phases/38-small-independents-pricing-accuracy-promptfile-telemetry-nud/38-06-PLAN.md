---
phase: 38-small-independents-pricing-accuracy-promptfile-telemetry-nud
plan: 06
type: execute
wave: 2
depends_on: [38-01, 38-02]
files_modified:
  - internal/subagent/anthropic/pricing.go
  - internal/subagent/anthropic/subagent.go
  - internal/subagent/anthropic/pricing_test.go
  - internal/subagent/anthropic/subagent_test.go
  - internal/subagent/anthropic/run_mix_regression_test.go
autonomous: true
requirements: [COST-01, COST-03]
must_haves:
  truths:
    - "A run on claude-sonnet-5 (and every Claude 5 family model) tallies BudgetStatus.CostSpentCents at real per-MTok rates — no more conservative-tier billing for a known model (COST-01)"
    - "A dated model ID (e.g. claude-sonnet-5-20260514) resolves via one -YYYYMMDD strip retry; a genuinely unknown model still falls to the conservative tier with NO family prefix-matching (D-01)"
    - "Every model's cache-write rate derives from one named cacheWriteMultiplier constant whose value cites the 38-01 probe evidence (D-08 / COST-03)"
    - "The provider sets Usage.PricingFallbackModel whenever the post-normalizer lookup misses, feeding plan 38-02's condition + metric (D-02 provider side)"
    - "The 2026-07-03 run-mix replay through the new table is locked as a regression: pinned exact cents, far below the old 1086-cent overcount (D-04, Pitfall 1)"
  artifacts:
    - path: "internal/subagent/anthropic/pricing.go"
      provides: "claude-sonnet-5 row, lookupPrice normalizer, cacheWriteMult constants"
      contains: "claude-sonnet-5"
    - path: "internal/subagent/anthropic/run_mix_regression_test.go"
      provides: "run-mix regression fixture replaying testdata/first_run_2026-07-03_usage.json"
      contains: "first_run_2026-07-03_usage"
    - path: "internal/subagent/anthropic/subagent.go"
      provides: "PricingFallbackModel stamped on the envelope on post-normalizer miss"
      contains: "PricingFallbackModel"
  key_links:
    - from: "internal/subagent/anthropic/pricing.go lookupPrice"
      to: "estimatedCostCents AND cacheSavingsCents"
      via: "both cost paths share the one normalizer (D-01)"
      pattern: "lookupPrice"
    - from: ".planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md"
      to: "internal/subagent/anthropic/pricing.go"
      via: "cacheWriteMult constants set from the probe verdict, comment cites the evidence (D-08)"
      pattern: "cacheWriteMult"
    - from: "internal/subagent/anthropic/subagent.go"
      to: "pkg/dispatch envelope Usage.PricingFallbackModel (plan 38-02)"
      via: "envelope assembly stamps the field on lookup miss"
      pattern: "PricingFallbackModel"
---

<objective>
Fix the 2.8× Claude-5 budget overcount at the source (COST-01/COST-03): add the missing `claude-sonnet-5` price row at sticker rates, insert the D-01 `-YYYYMMDD` date-suffix normalizer between exact lookup and the conservative fallback, encode the cache-write multiplier as one named constant set from the 38-01 probe verdict (D-08), stamp `Usage.PricingFallbackModel` on post-normalizer misses (D-02 provider side), and lock the fix with unit tests plus the D-04 run-mix regression fixture replaying the recorded 2026-07-03 usage.

Purpose: the first external-repo run billed `claude-sonnet-5` at the fable-5 conservative tier — TIDE tallied $10.86 vs the console's $3.84. This is the phase's headline number.
Output: pricing.go + subagent.go changes and three test surfaces, all inside `internal/subagent/anthropic/` (provider firewall).
</objective>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `claude-sonnet-5` price row | new priceTable entry | 300/1500/30 + write derived from multiplier (sticker rates; intro-pricing caveat in comment) |
| `lookupPrice(model string) (modelPrice, bool)` | new method, pricing.go | exact → one date-strip retry → conservativeTier,false; NO family matching (D-01) |
| `dateSuffixRe` | new package var, pricing.go | `regexp.MustCompile("-\\d{8}$")` |
| `cacheWriteMultNum`, `cacheWriteMultDen` | new consts, pricing.go | value from 38-01-PROBE-RESULT.md verdict; comment cites probe evidence verbatim (D-08) |
| `run_mix_regression_test.go` | new test file | replays testdata fixture; pins exact cents |
| `Usage.PricingFallbackModel` stamping | subagent.go envelope assembly | fed to plan 38-02's condition/metric |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-CONTEXT.md
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-SUMMARY.md
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sonnet-5 row + D-01 normalizer + D-08 multiplier + fallback-flag stamping</name>
  <files>internal/subagent/anthropic/pricing.go, internal/subagent/anthropic/subagent.go, internal/subagent/anthropic/pricing_test.go, internal/subagent/anthropic/subagent_test.go</files>
  <read_first>
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-01-PROBE-RESULT.md — the verdict line (125/100 or 200/100) and the evidence to cite in the constant comment
    - internal/subagent/anthropic/pricing.go — FULL file: table shape, conservativeTier, estimatedCostCents + cacheSavingsCents lookup blocks, the T-14-02 "always read a.prices" rule, header comment rate list
    - internal/subagent/anthropic/pricing_test.go — existing table/override test conventions (extend, don't replace — the conservative-tier fixtures at fable-5 rates stay)
    - internal/subagent/anthropic/subagent.go lines 335-360 — the envelope-assembly block where usage.EstimatedCostCents/CacheSavingsCents are computed (the flag stamps here)
    - pkg/dispatch/envelope.go — the Usage.PricingFallbackModel field landed by plan 38-02
  </read_first>
  <behavior>
    - Test 1 (row): estimatedCostCents("claude-sonnet-5", 1M input) == 300¢; 1M output == 1500¢; 1M cacheRead == 30¢; 1M cacheCreation == the multiplier-derived write rate (375¢ at 125/100, 600¢ at 200/100)
    - Test 2 (normalizer hit): "claude-sonnet-5-20260514" prices identically to "claude-sonnet-5"; "claude-haiku-4-5-20250101" identically to "claude-haiku-4-5"
    - Test 3 (no family matching): "claude-sonnet-6" and "claude-sonnet" price at conservativeTier (fable-5 rates), NOT at any sonnet row (D-01)
    - Test 4 (unknown + date): "claude-nova-9-20270101" → conservativeTier (strip retry also misses)
    - Test 5 (overrides through normalizer): Options.PricingOverrides["claude-sonnet-5"] with a custom input rate → "claude-sonnet-5-20260514" bills at the override rate (normalizer resolves against a.prices, which includes merged overrides)
    - Test 6 (multiplier consistency): for EVERY row in priceTable, cacheWriteCentsPerMTok == inputCentsPerMTok * cacheWriteMultNum / cacheWriteMultDen
    - Test 7 (fallback flag): dispatch via the existing fake-exec fixture with an unknown model → EnvelopeOut.Usage.PricingFallbackModel == that model ID; with a known model → empty
    - Test 8 (cacheSavings shares the normalizer): cacheSavingsCents("claude-sonnet-5-20260514", usage with cache reads) uses sonnet-5 rates
  </behavior>
  <action>
    All inside `internal/subagent/anthropic/` (provider firewall — no orchestrator/controller leakage):

    1. pricing.go: add consts `cacheWriteMultNum` / `cacheWriteMultDen` with the value from the 38-01-PROBE-RESULT.md verdict — 125/100 if the probe observed only `{"type":"ephemeral"}` (5m TTL), 200/100 if any 1-hour ttl marker was observed. The comment MUST cite the probe evidence verbatim: date, `claude --version`, model, the observed cache_control values, and the PROBE-RESULT path — plus the rule "5m TTL → 1.25× input; 1h TTL → 2× input; flip this one constant on the next TTL shift" (D-08). If PROBE-RESULT says `probe-status: NOT RUN`, encode 125/100 and say so in the comment (RESEARCH fallback — matches every existing stored literal).
    2. Add the `claude-sonnet-5` row: inputCentsPerMTok 300, outputCentsPerMTok 1500, cacheReadCentsPerMTok 30, cacheWriteCentsPerMTok = 300*cacheWriteMultNum/cacheWriteMultDen expressed as the literal the multiplier dictates. Row comment: sticker rates $3/$15 (claude-api-skill-verified 2026-07-04); intro pricing $2/$10 runs through 2026-08-31 — sticker compiled deliberately (never under-counts, durable past the intro window; RESEARCH Pitfall 1). IF the verdict is 200/100, also update every existing row's cacheWriteCentsPerMTok literal to 2× its input rate (fable 2000, opus rows 1000, sonnet-4-6 600, haiku 200) and refresh the header-comment cache-rate note; at 125/100 the existing literals already comply.
    3. Add `var dateSuffixRe = regexp.MustCompile` for a trailing dash + 8 digits anchored at end (`-\d{8}$`), and `func (a *Anthropic) lookupPrice(model string) (modelPrice, bool)`: exact `a.prices[model]` hit → return; else strip the date suffix and, if the string changed, retry `a.prices[stripped]` once; else/still-miss → return conservativeTier, false. NO prefix/family matching — comment why (D-01: a genuinely unknown model must never inherit an old cheaper family rate). Rewire `estimatedCostCents` and `cacheSavingsCents` to use lookupPrice (keep the loud stderr warning in estimatedCostCents on miss; cacheSavingsCents stays silent per its existing contract).
    4. subagent.go envelope assembly (~line 345): after computing usage costs, `if _, ok := a.lookupPrice(in.Provider.Model); !ok { usage.PricingFallbackModel = in.Provider.Model }` — the D-02 provider side feeding plan 38-02's rollup surface. Comment cites Phase 38 COST-02.
    5. Update pricing.go's header rate list to include sonnet-5 and the multiplier rule; extend pricing_test.go and subagent_test.go with behaviors 1-8 following the files' existing table-test style.
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/ -run 'TestEstimatedCostCents|TestCacheSavingsCents|TestCostParity|Lookup|Normalizer|Multiplier|Fallback' -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"claude-sonnet-5"' internal/subagent/anthropic/pricing.go` == 1 (the row exists exactly once)
    - `grep -c 'cacheWriteMultNum' internal/subagent/anthropic/pricing.go` >= 2 (const + at least one use/derivation site)
    - The constant's comment names the probe date and CLI version from 38-01-PROBE-RESULT.md (or the explicit NOT-RUN fallback note)
    - Behaviors 1-8 all pass; ALL pre-existing tests in the package still pass (conservative-tier fixtures unchanged)
    - `grep -c 'priceTable\[' internal/subagent/anthropic/pricing.go` shows no new package-level table reads in cost paths (only the conservativeTier initializer uses it — T-14-02)
    - No file outside internal/subagent/anthropic/ is modified by this task
  </acceptance_criteria>
  <done>Sonnet-5 priced, normalizer live in both cost paths, multiplier single-sourced with cited evidence, fallback flag stamped.</done>
</task>

<task type="auto">
  <name>Task 2: D-04 run-mix regression fixture — replay the 2026-07-03 run through the new table</name>
  <files>internal/subagent/anthropic/run_mix_regression_test.go</files>
  <read_first>
    - internal/subagent/anthropic/testdata/first_run_2026-07-03_usage.json — the distilled dispatches + provenance (from plan 38-01; the schema is documented in 38-01-PLAN's artifacts table)
    - internal/subagent/anthropic/cost_parity_test.go — the package's evidence-style comment conventions (cite figures in the test comment)
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-CONTEXT.md — D-04 (proof shape) and the specifics section (CACHE-01 evidence style)
  </read_first>
  <action>
    New `run_mix_regression_test.go`: load testdata/first_run_2026-07-03_usage.json; for each dispatch, run its usage block through `New(Options{}).estimatedCostCents(dispatch.Model, usage)`; sum the cents. Assertions:
    (a) the sum equals a PINNED literal — compute it once during execution from the real fixture data at the new table's rates and hard-code that number with a derivation comment (this is the regression lock: any future table/normalizer drift moves the sum);
    (b) the sum is < 615 cents (1.6 × the console's 384¢ — the RESEARCH Pitfall-1 tolerance: sticker rates legitimately exceed the intro-priced $3.84 bill on sonnet-5 lines, so the fixture must NOT assert equality with 384);
    (c) the sum is < 1086 cents — stated as its own assertion with its own message so the 2.8× overcount regression reads explicitly in failures.
    The file-header comment follows the CACHE-01 evidence style (CONTEXT specifics): run date 2026-07-03, the model mix AS OBSERVED IN THE FIXTURE (not the prose — envelopes are ground truth), TIDE's old tally $10.86 vs console $3.84, the sonnet-5 intro-pricing caveat (intro $2/$10 through 2026-08-31; sticker compiled), and the fixture's provenance string quoted. If the fixture's provenance declares partial/no evidence, build the test from whatever dispatches exist and say so in the comment (D-04 pre-authorizes); if the dispatches array is EMPTY, reconstruct approximate usage from the folded todo's figures with a provenance comment stating the reconstruction — never skip the test silently.
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/ -run RunMix -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/subagent/anthropic/ -run RunMix` passes
    - The test asserts one pinned exact-cents literal AND the < 615 bound AND the < 1086 bound as three separate assertions with distinct failure messages
    - The file-header comment contains: 2026-07-03, both dollar figures ($10.86 and $3.84), the intro-pricing caveat, and the fixture provenance
    - The test reads the testdata file (no inlined token counts unless provenance-disclosed reconstruction was necessary)
  </acceptance_criteria>
  <done>The 2.8× overcount is a locked regression: replaying the real run through the table can never again land near 1086¢ without a red test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model ID (operator spec / chart default) → billing math | untrusted-ish string selects the price row |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-16 | Repudiation | silent under-billing via family prefix-matching a new expensive model to an old cheap row | high | mitigate | D-01: exact + one date-strip retry ONLY; behavior Test 3 locks the refusal; conservative tier on any miss |
| T-38-17 | Tampering | pricing logic leaking outside the Subagent interface | medium | mitigate | all edits confined to internal/subagent/anthropic/; acceptance asserts no external file touched |
| T-38-18 | Repudiation | multiplier drifting from the observed TTL without evidence | medium | mitigate | single D-08 constant with probe-citation comment; consistency test derives every row from it |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: empty) |
</threat_model>

<verification>
- `go test ./internal/subagent/anthropic/ -v` fully green (dev VM or docker golang:1.26 — Go absent on host Mac)
- `make test` unit tier green
- Post-check (network permitting, non-blocking): `hack/check-pricing-drift.sh` validates the sonnet-5 row against the published pricing page
</verification>

<success_criteria>
- COST-01 and COST-03 satisfied: every Claude 5 family model bills at real rates through exact-ID + normalizer lookup; the multiplier is empirically grounded; the first-run overcount is a pinned regression.
</success_criteria>

<output>
Create `.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-06-SUMMARY.md` when done, recording the pinned fixture sum and the multiplier value compiled.
</output>
