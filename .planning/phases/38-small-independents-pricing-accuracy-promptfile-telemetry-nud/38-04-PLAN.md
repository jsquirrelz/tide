---
phase: 38-small-independents-pricing-accuracy-promptfile-telemetry-nud
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - hack/helm/tide-values.yaml
  - charts/tide/values.yaml
  - hack/helm/augment-tide-chart.sh
  - charts/tide/templates/configmap.yaml
  - charts/tide/templates/NOTES.txt
  - charts/tide/templates/dashboard-deployment.yaml
  - hack/helm/assert-telemetry-render.sh
  - docs/INSTALL.md
autonomous: true
requirements: [TELEM-01, TELEM-02, DEBT-02]
must_haves:
  truths:
    - "Installing with prometheus.enabled=false (the default) prints a NOTES.txt warning that run telemetry beyond budget is unavailable (TELEM-02, D-12)"
    - "The chart passes prometheus.enabled to the dashboard deployment as the PROMETHEUS_ENABLED env var (D-14, chart side)"
    - "The rendered configmap defaults plannerConcurrency to 4, matching values.yaml (DEBT-02)"
    - "An operator following INSTALL.md's enable-telemetry step (including the kube-prometheus-stack release-label fix) ends at a Prometheus Targets page showing TIDE scraped (TELEM-01, D-15)"
    - "No chart version bump — the v1.0.7 bump is one release-time event (D-13)"
    - "Every chart edit lands in BOTH the hack/helm source and the rendered charts/tide copy (Pitfall 3)"
  artifacts:
    - path: "charts/tide/templates/NOTES.txt"
      provides: "post-install summary + conditional telemetry warning"
      contains: "prometheus.enabled"
    - path: "charts/tide/values.yaml"
      provides: "prometheus.enabled umbrella key (default false) + refreshed pricing.overrides comment naming the Claude-5 use case"
      contains: "enabled: false"
    - path: "charts/tide/templates/dashboard-deployment.yaml"
      provides: "PROMETHEUS_ENABLED env on the dashboard container"
      contains: "PROMETHEUS_ENABLED"
    - path: "charts/tide/templates/configmap.yaml"
      provides: "plannerConcurrency fallback default 4"
      contains: "default 4"
    - path: "docs/INSTALL.md"
      provides: "Enable telemetry walkthrough ending at the Targets page"
      contains: "kube-prometheus-stack"
  key_links:
    - from: "hack/helm/tide-values.yaml"
      to: "charts/tide/values.yaml"
      via: "augment-tide-chart.sh cp — byte-identical mirror"
      pattern: "prometheus:"
    - from: "charts/tide/values.yaml prometheus.enabled"
      to: "charts/tide/templates/NOTES.txt"
      via: "NOTES conditional on not .Values.prometheus.enabled"
      pattern: "prometheus.enabled"
    - from: "charts/tide/values.yaml prometheus.enabled"
      to: "charts/tide/templates/dashboard-deployment.yaml"
      via: "PROMETHEUS_ENABLED env value"
      pattern: "PROMETHEUS_ENABLED"
---

<objective>
Land all Phase 38 chart-surface and docs work under the D-13 batching rule (edits on main now, ONE chart version bump at v1.0.7 release): the new `prometheus.enabled` umbrella key + `PROMETHEUS_ENABLED` dashboard env (D-14 chart side, per the RESEARCH Q2 headless ruling/A6), the net-new NOTES.txt with the conditional telemetry warning (TELEM-02, D-12), the configmap `default 16`-remnant fix to `default 4` (DEBT-02), the pricing.overrides comment refresh naming the Claude-5 use case (D-03 — the plumbing itself is already fully built per RESEARCH), and the INSTALL.md enable-telemetry walkthrough (TELEM-01, D-15).

Purpose: telemetry was dark on the first external-repo run because nothing nudged the operator to wire Prometheus; and the v1.0.6 audit W2 configmap remnant misleads operators reading rendered output.
Output: dual-source chart edits (hack/helm + charts/tide), extended render assertions, INSTALL.md section.
</objective>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| `prometheus.enabled: false` | new chart value (both values files) | umbrella telemetry toggle per D-14 + Q2 ruling; serviceMonitor.enabled and endpoint keep their existing roles |
| `PROMETHEUS_ENABLED` | new env var on dashboard container | always rendered, value `{{ quote .Values.prometheus.enabled }}` |
| `charts/tide/templates/NOTES.txt` | net-new template (+ heredoc in augment-tide-chart.sh) | post-install summary + conditional warning |
| `plannerConcurrency ... default 4` | fixed fallback (augment script + rendered configmap) | DEBT-02 / v1.0.6 audit W2 |
| refreshed `pricing.overrides` comment | values comment (both values files) | D-03: names the new-Claude-model-without-image-rebuild use case |
| INSTALL.md "Enable telemetry (Prometheus)" section | new docs section | D-15 walkthrough |
| assert-telemetry-render.sh permutations E/F/G | extended render gate | PROMETHEUS_ENABLED default/set + NOTES warning conditional |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-CONTEXT.md
@.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: prometheus.enabled key, PROMETHEUS_ENABLED env, configmap default 4, pricing comment (dual-source)</name>
  <files>hack/helm/tide-values.yaml, charts/tide/values.yaml, hack/helm/augment-tide-chart.sh, charts/tide/templates/configmap.yaml, charts/tide/templates/dashboard-deployment.yaml</files>
  <read_first>
    - hack/helm/augment-tide-chart.sh — full script: step 2 cp's tide-values.yaml → charts/tide/values.yaml byte-identically; step 4 heredoc-writes configmap.yaml (the plannerConcurrency fallback is at line 90); dashboard-deployment.yaml is NOT touched by the script (static tracked template — single-copy edit)
    - charts/tide/values.yaml lines 320-360 — the prometheus: block (comment style, existing keys endpoint/retentionTime/serviceMonitor) and lines 125-150 — the pricing.overrides block comment
    - charts/tide/templates/dashboard-deployment.yaml lines 45-75 — the PROM_ENDPOINT conditional env block and its EC-7 template comment (default render must carry no PROM_ENDPOINT env entry)
    - hack/helm/assert-telemetry-render.sh lines 60-110 — the env-entry-shaped grep the EC-7 gate uses
  </read_first>
  <action>
    All values/configmap edits go to the hack/helm SOURCE first, then mirror into charts/tide (Pitfall 3). Preferred flow: edit hack/helm files, run `./hack/helm/augment-tide-chart.sh`, then verify `git diff --name-only` shows ONLY the intended files; if the script produces unrelated diffs, revert its output and hand-mirror the edits into charts/tide instead.

    1. `hack/helm/tide-values.yaml` (mirrored to charts/tide/values.yaml): inside the existing `prometheus:` block add `enabled: false` as the first key, with a comment: Phase 38 D-14/TELEM umbrella toggle — declares whether run telemetry is wired for this install; drives the NOTES.txt warning and the dashboard's PROMETHEUS_ENABLED env (banner signal); it does NOT flip serviceMonitor.enabled (scrape gate, stays default-off per the CLAUDE.md anti-pattern) and does NOT replace endpoint (the dashboard's PromQL proxy URL) — operators enabling telemetry set all three.
    2. Same file: refresh the `pricing.overrides` comment (D-03): add one sentence noting the primary use case — correcting or adding rows for newly released Claude models (e.g. a new claude-sonnet generation) without waiting for an image rebuild; overrides merge over the compiled table at manager startup and reach subagents via TIDE_PRICING_OVERRIDES_JSON (plumbing landed Phase 14; verified by existing tests — pricing_test.go override cases and test/integration/kind/projects_pvc_test.go render assertions).
    3. `hack/helm/augment-tide-chart.sh` line 90: change the plannerConcurrency fallback from 16 to 4 so the heredoc emits `plannerConcurrency: {{ .Values.plannerConcurrency | default 4 }}` (DEBT-02 / audit W2). Leave the `maxConcurrentReconciles.task` fallback at line 98 untouched. Mirror the same one-token fix into `charts/tide/templates/configmap.yaml` line 22 (or regenerate via the script).
    4. `charts/tide/templates/dashboard-deployment.yaml`: after the PROM_ENDPOINT conditional block, add an ALWAYS-rendered env entry: `- name: PROMETHEUS_ENABLED` / `value: {{ quote .Values.prometheus.enabled }}` with a template comment citing Phase 38 D-14 (banner disabled-by-config signal; read by cmd/dashboard/main.go; explicit "false" is meaningful — legacy charts that lack the key leave the var unset and the binary falls back to PROM_ENDPOINT presence). This does NOT trip the EC-7 gate: the gate's grep matches the exact env-entry shape `name: PROM_ENDPOINT` end-of-line, which `PROMETHEUS_ENABLED` does not match — verify by running the existing assert script.
  </action>
  <verify>
    <automated>helm template tide-render charts/tide --set plannerConcurrency=null | grep -c 'plannerConcurrency: 4' && ./hack/helm/assert-telemetry-render.sh</automated>
  </verify>
  <acceptance_criteria>
    - `diff hack/helm/tide-values.yaml charts/tide/values.yaml` is empty (byte-identical mirror preserved)
    - `grep -c 'plannerConcurrency: {{ .Values.plannerConcurrency | default 4 }}' hack/helm/augment-tide-chart.sh` == 1 AND same grep == 1 in charts/tide/templates/configmap.yaml
    - `helm template tide-render charts/tide --set plannerConcurrency=null | grep -c 'plannerConcurrency: 4'` == 1
    - Default render carries `name: PROMETHEUS_ENABLED` with value "false"; `--set prometheus.enabled=true` renders value "true"
    - `./hack/helm/assert-telemetry-render.sh` passes (EC-7 permutations A-D intact)
    - `git diff --name-only` does NOT include charts/tide/Chart.yaml or hack/helm/tide-chart.yaml (D-13: no version bump)
  </acceptance_criteria>
  <done>Umbrella key, env var, configmap default, and pricing comment landed in both source and rendered copies with the render gate green.</done>
</task>

<task type="auto">
  <name>Task 2: NOTES.txt (net-new, dual-source) + extend the render assertions</name>
  <files>hack/helm/augment-tide-chart.sh, charts/tide/templates/NOTES.txt, hack/helm/assert-telemetry-render.sh</files>
  <read_first>
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md — "NOTES.txt sketch" code example (the exact content shape and D-12 tone)
    - hack/helm/augment-tide-chart.sh lines 67-101 — the configmap heredoc pattern to copy for the NOTES.txt heredoc (quoted 'EOF' so Helm template syntax survives)
    - hack/helm/assert-telemetry-render.sh — die()/PASS echo conventions for new permutations
  </read_first>
  <action>
    1. Create `charts/tide/templates/NOTES.txt` per the RESEARCH sketch and D-12 (tight declarative voice, a few useful lines, not warning-only): (a) `TIDE {{ .Chart.AppVersion }} installed in {{ .Release.Namespace }}.`; (b) a Dashboard line with the port-forward command `kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "tide.fullname" . }}-dashboard 8080:80` — FIRST verify the dashboard Service's actual name template and port by reading charts/tide/templates/dashboard-svc/service template (grep charts/tide/templates for the dashboard Service manifest) and use the real name/port, not the sketch's guess; (c) a Docs line pointing at docs/INSTALL.md; (d) the conditional warning gated on `{{- if not .Values.prometheus.enabled }}`: WARNING — run telemetry beyond the budget tally is unavailable; prometheus.enabled is false; token spend over time, dispatch counts, and per-level durations will be dark; Enable: the "Enable telemetry" step in docs/INSTALL.md.
    2. Add a matching heredoc section to `hack/helm/augment-tide-chart.sh` (new step alongside the configmap heredoc, e.g. "# 4b. NOTES.txt — post-install summary + telemetry nudge (Phase 38 TELEM-02/D-12)") writing the identical content via `cat > "${CHART_DIR}/templates/NOTES.txt" <<'EOF'` — the script owns the file the same way it owns configmap.yaml, so a future augment run cannot delete it (Pitfall 3).
    3. Extend `hack/helm/assert-telemetry-render.sh` with three permutations following the existing die/PASS pattern:
       - [E] default render: dashboard container carries env entry `name: PROMETHEUS_ENABLED` with value "false" (env-entry-shaped grep like permutation A/B's)
       - [F] `--set prometheus.enabled=true`: PROMETHEUS_ENABLED value is "true"
       - [G] NOTES conditional: render NOTES via `helm template tide-notes charts/tide --show-only templates/NOTES.txt` — if --show-only does not emit NOTES.txt on the pinned Helm (NOTES is excluded from manifest output in some versions), fall back to `helm install tide-notes charts/tide --dry-run=client 2>&1 | sed -n '/^NOTES:/,$p'`; assert the warning line ("run telemetry beyond the budget tally is unavailable") is PRESENT by default and ABSENT with `--set prometheus.enabled=true`. Pick whichever invocation works on the repo's helm and codify only that one in the script, with a comment naming the helm version it was verified against.
  </action>
  <verify>
    <automated>./hack/helm/assert-telemetry-render.sh</automated>
  </verify>
  <acceptance_criteria>
    - `charts/tide/templates/NOTES.txt` exists AND `grep -c 'NOTES.txt' hack/helm/augment-tide-chart.sh` >= 1 (script owns it)
    - Rendered NOTES output contains the warning text when prometheus.enabled=false (default) and omits it with --set prometheus.enabled=true — proven by the new [G] permutation
    - `./hack/helm/assert-telemetry-render.sh` exits 0 with new PASS lines for E, F, G
    - The augment-script heredoc and the tracked NOTES.txt are content-identical (running the script produces no diff on the file)
    - `helm lint charts/tide` still exits 0 (existing permutation D)
  </acceptance_criteria>
  <done>TELEM-02 shipped: net-new NOTES.txt warns exactly when the umbrella key is off, locked by a codified render gate.</done>
</task>

<task type="auto">
  <name>Task 3: INSTALL.md enable-telemetry walkthrough (TELEM-01, D-15)</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - docs/INSTALL.md — full file: section voice (tight, declarative, copy-paste blocks), the "### Verifying the install" section (~line 156) after which the new section inserts, and the "## Provider Secret" heading (~line 173) it must precede
    - charts/tide/templates/servicemonitor.yaml — the labels the ServiceMonitor actually carries today (control-plane + tide.labels, NO release: label — the root cause the doc must explain)
    - .planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-RESEARCH.md — section "TELEM-01 release: label fix — what INSTALL.md must say" (the two documented fixes + A1 caveat)
  </read_first>
  <action>
    Insert a new `### Enable telemetry (Prometheus)` section into docs/INSTALL.md between "Verifying the install" and "Provider Secret" (D-15). Full copy-paste kube-prometheus-stack walkthrough in the doc's existing voice:
    1. Install kube-prometheus-stack: `helm repo add prometheus-community https://prometheus-community.github.io/helm-charts && helm repo update`, then `helm install kps prometheus-community/kube-prometheus-stack -n monitoring --create-namespace`.
    2. Explain the `release:` label gotcha in one tight paragraph: kube-prometheus-stack's Prometheus selects ServiceMonitors carrying `release: <its-release-name>`; TIDE's ServiceMonitor ships without that label, so a default install ignores it. Primary fix (copy-paste): `kubectl -n tide-system label servicemonitor -l control-plane=controller-manager release=kps`. Alternative (mention in one line): set `prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues=false` on the kps install so it selects all ServiceMonitors. NOTE (RESEARCH A1): verify the release-label selection claim against the kube-prometheus-stack chart README while authoring if network access is available; both fixes stay documented either way since together they cover either selector behavior.
    3. Enable TIDE's telemetry surfaces: `helm upgrade tide oci://ghcr.io/jsquirrelz/charts/tide -n tide-system --reuse-values --set prometheus.serviceMonitor.enabled=true --set prometheus.enabled=true --set prometheus.endpoint=http://prometheus-operated.monitoring:9090` (match the upgrade command shape already used elsewhere in INSTALL.md; the endpoint value reuses values.yaml's own documented example URL).
    4. Verification end-state (TELEM-01's success bar): `kubectl -n monitoring port-forward svc/prometheus-operated 9090:9090`, open http://localhost:9090 → Status → Targets, and confirm the tide metrics endpoint shows UP. State explicitly that this is the done signal.
    5. Short variant note for operators pointing at an EXISTING Prometheus (not kube-prometheus-stack): what the ServiceMonitor must match — the operator's Prometheus must select the `tide-system` namespace and the ServiceMonitor's labels (or the operator adds whatever label their serviceMonitorSelector requires); `prometheus.endpoint` then points at their Prometheus service URL.
    Do not touch any other INSTALL.md section.
  </action>
  <verify>
    <automated>grep -c 'kube-prometheus-stack' docs/INSTALL.md && grep -c 'Enable telemetry' docs/INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - The new section sits after "### Verifying the install" and before "## Provider Secret" (heading order verified by `grep -n '^##' docs/INSTALL.md`)
    - Section contains: kps install commands, the kubectl label command for the release-label fix, the one-line serviceMonitorSelectorNilUsesHelmValues alternative, the three --set flags (serviceMonitor.enabled, enabled, endpoint), the port-forward + Targets-page verification, and the existing-Prometheus variant note
    - Every command is copy-paste runnable (no placeholder angle-brackets except the operator's own release names where unavoidable)
  </acceptance_criteria>
  <done>TELEM-01 shipped: an operator can follow the doc from zero to a green TIDE target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart values → rendered manifests | operator-supplied values flow into cluster manifests |
| docs → operator shell | copy-paste commands run with operator privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-10 | Elevation of privilege | ServiceMonitor default flipped on, breaking plain clusters (CRD-not-found) | medium | mitigate | serviceMonitor.enabled default stays false (binding constraint); NOTES/banner/doc compensate with guidance, never by flipping the default |
| T-38-11 | Tampering | rendered-only chart edits silently reverted by the next augment run | medium | mitigate | dual-source rule enforced per task; acceptance asserts script-vs-rendered identity (Pitfall 3) |
| T-38-12 | Repudiation | unversioned chart drift (edits without a version story) | low | accept | deliberate per D-13 — all v1.0.7 chart edits batch under one release-time bump |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: empty) |
</threat_model>

<verification>
- `./hack/helm/assert-telemetry-render.sh` exits 0 (permutations A-G)
- `helm template tide-render charts/tide --set plannerConcurrency=null | grep -c 'plannerConcurrency: 4'` == 1
- `diff hack/helm/tide-values.yaml charts/tide/values.yaml` empty; Chart.yaml untouched
- helm runs on the host (v4.2.0 — if template output diverges from the pinned Helm 3, rerun inside the dev VM per RESEARCH Environment Availability)
</verification>

<success_criteria>
- TELEM-01/TELEM-02/DEBT-02 satisfied; D-03 comment + D-14 chart side landed; every edit dual-sourced; no chart version bump.
</success_criteria>

<output>
Create `.planning/phases/38-small-independents-pricing-accuracy-promptfile-telemetry-nud/38-04-SUMMARY.md` when done. Plan 38-05 consumes the PROMETHEUS_ENABLED env contract — state the final env name and value semantics in the summary.
</output>
