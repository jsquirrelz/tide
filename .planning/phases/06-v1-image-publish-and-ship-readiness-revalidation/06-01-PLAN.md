---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hack/helm/tide-values.yaml
  - charts/tide/values.yaml
  - examples/projects/small/project.yaml
  - .gitignore
  - docs/troubleshooting.md
autonomous: true
requirements:
  - CHART-01
  - HYG-01

must_haves:
  truths:
    - "grep -cE 'v0\\.1\\.0-dev' charts/tide/values.yaml hack/helm/tide-values.yaml returns 0"
    - "helm template charts/tide resolves all 6 TIDE component images to tag 1.0.0 (no v prefix)"
    - "The busybox third-party tag 1.36 at hack/helm/tide-values.yaml line 148 is unchanged"
    - "examples/projects/small/project.yaml uses stub-subagent tag 1.0.0 (no v prefix, matching appVersion)"
    - ".acceptance-runs/ is git-ignored"
    - "docs/troubleshooting.md contains an ImagePullBackOff mid-install entry"
  artifacts:
    - path: "hack/helm/tide-values.yaml"
      provides: "SOT chart values with 5 component tags set to empty string"
      contains: "tag: \"\""
    - path: "charts/tide/values.yaml"
      provides: "Generated chart values — regenerated from SOT via make helm"
    - path: "examples/projects/small/project.yaml"
      provides: "Small sample with stub-subagent:1.0.0 (A7 fix)"
    - path: ".gitignore"
      provides: ".acceptance-runs/ entry"
    - path: "docs/troubleshooting.md"
      provides: "ImagePullBackOff mid-install troubleshooting row (D-C4 Symptom|Cause|Recipe shape)"
  key_links:
    - from: "hack/helm/tide-values.yaml"
      to: "charts/tide/values.yaml"
      via: "make helm (bash hack/helm/augment-tide-chart.sh step 2 — cp SOT → charts/tide/values.yaml)"
      pattern: "augment-tide-chart\\.sh"
    - from: "charts/tide/values.yaml tag ''"
      to: "helm template output"
      via: "| default .Chart.AppVersion resolves to 1.0.0"
      pattern: "appVersion"
---

<objective>
Kill the 5 dead `v0.1.0-dev` chart tags, fix the project.yaml stub tag inconsistency, and add two hygiene items.

Purpose: CHART-01 eliminates the ImagePullBackOff root cause for the $0 acceptance path — once all 6 component tags resolve to `1.0.0` via `appVersion`, the auto-detect image-load helper (Plan 04) can build and load at `:1.0.0` and the chart will find them. HYG-01 unclutters the worktree and gives operators a troubleshooting recipe.

Output: Updated SOT `hack/helm/tide-values.yaml` (5 empty tags) + regenerated `charts/tide/values.yaml` + fixed `examples/projects/small/project.yaml` (stub tag `1.0.0` not `v1.0.0`) + `.gitignore` entry + troubleshooting row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-SPEC.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-CONTEXT.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-PATTERNS.md

<interfaces>
<!-- CHART-01 SOT edit surface — verified live codebase 2026-05-30 -->
<!-- hack/helm/tide-values.yaml — 5 lines to change, 1 to preserve: -->
<!--   line 39:  tag: v0.1.0-dev  → tag: ""   (controllerManager.manager.image.tag) -->
<!--   line 140: tag: v0.1.0-dev  → tag: ""   (images.stubSubagent.tag) -->
<!--   line 144: tag: v0.1.0-dev  → tag: ""   (images.credProxy.tag) -->
<!--   line 155: tag: v0.1.0-dev  → tag: ""   (images.tidePush.tag) -->
<!--   line 165: tag: v0.1.0-dev  → tag: ""   (images.claudeSubagent.tag) -->
<!--   line 148: tag: "1.36"      MUST NOT CHANGE (busybox, third-party) -->
<!-- Dashboard SOT (proven working pattern, line 244): -->
<!--   tag: ""             # default to .Chart.AppVersion -->

<!-- examples/projects/small/project.yaml — A7 fix: -->
<!--   line 49: image: ghcr.io/jsquirrelz/tide-stub-subagent:v1.0.0 -->
<!--   →        image: ghcr.io/jsquirrelz/tide-stub-subagent:1.0.0  -->

<!-- .gitignore — append after existing entries (no leading slash; directory): -->
<!--   # Phase 6 ACC-01 — acceptance run evidence archives (maintainer-local, never committed) -->
<!--   .acceptance-runs/ -->

<!-- docs/troubleshooting.md — D-C4 format: Symptom | Cause | Recipe table -->
<!--   Existing row pattern (line 27): -->
<!--   | `Pod tide-controller-manager-…` stuck in `ImagePullBackoff` | ... | ... | -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CHART-01 — edit SOT and regenerate chart; fix small project.yaml A7 tag</name>
  <files>hack/helm/tide-values.yaml, charts/tide/values.yaml, examples/projects/small/project.yaml</files>
  <read_first>
    Read hack/helm/tide-values.yaml lines 35-170 to confirm the 5 `v0.1.0-dev` line numbers (39, 140, 144, 155, 165) and line 148 third-party tag before editing. Read examples/projects/small/project.yaml line 49. Read charts/tide/Chart.yaml to confirm appVersion is "1.0.0".
  </read_first>
  <action>
    1. Edit hack/helm/tide-values.yaml (the SOT — never edit charts/tide/values.yaml directly per CLAUDE.md chart-vs-binary anti-pattern):
       - Line 39: change `tag: v0.1.0-dev` to `tag: ""` (controllerManager.manager.image.tag — per CHART-01 and D-03)
       - Line 140: change `tag: v0.1.0-dev` to `tag: ""` (images.stubSubagent.tag)
       - Line 144: change `tag: v0.1.0-dev` to `tag: ""` (images.credProxy.tag)
       - Line 155: change `tag: v0.1.0-dev` to `tag: ""` (images.tidePush.tag)
       - Line 165: change `tag: v0.1.0-dev` to `tag: ""` (images.claudeSubagent.tag)
       - Line 148: MUST NOT TOUCH — `tag: "1.36"` (busybox, third-party image preserved per D-03)
       The empty string `""` is the proven pattern from line 244 (dashboard) — the Helm template `| default .Chart.AppVersion` resolves it to `1.0.0`.

    2. Run `make helm` from the repo root to regenerate charts/tide/values.yaml from the SOT (runs bash hack/helm/augment-tide-chart.sh which copies the SOT to charts/tide/values.yaml). Do NOT hand-edit charts/tide/values.yaml.

    3. Edit examples/projects/small/project.yaml line 49 (RESEARCH A7 fix):
       Change `image: ghcr.io/jsquirrelz/tide-stub-subagent:v1.0.0` to `image: ghcr.io/jsquirrelz/tide-stub-subagent:1.0.0`
       Rationale: after CHART-01 the chart resolves tide-stub-subagent to tag `1.0.0` (no `v`). The project.yaml must use the same tag so the auto-detect image-load helper in Plan 04 builds/loads a single consistent tag and kind-loaded images match what the chart requests.
  </action>
  <verify>
    <automated>grep -cE 'v0\.1\.0-dev' charts/tide/values.yaml hack/helm/tide-values.yaml</automated>
    Must return 0. Also: helm template charts/tide | grep -E 'image:' | grep -v '1\.0\.0\|1\.36' — must return empty (no images resolving to unexpected tags). Also: grep -E 'tag: "1\.36"' hack/helm/tide-values.yaml — must return 1 line (busybox preserved).
  </verify>
  <acceptance_criteria>
    - `grep -cE 'v0\.1\.0-dev' charts/tide/values.yaml hack/helm/tide-values.yaml` exits with count 0
    - `helm template charts/tide | grep -E 'image:' | grep -v '1\.0\.0\|1\.36'` returns empty output
    - `grep -cE 'tag: "1\.36"' hack/helm/tide-values.yaml` returns 1
    - `grep 'tide-stub-subagent:1\.0\.0' examples/projects/small/project.yaml` returns 1 line
    - `grep 'tide-stub-subagent:v1\.0\.0' examples/projects/small/project.yaml` returns 0 lines
  </acceptance_criteria>
  <done>SOT has 5 empty tags; generated chart has no v0.1.0-dev; helm template resolves all 6 TIDE images to 1.0.0; busybox 1.36 unchanged; project.yaml stub tag uses 1.0.0</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HYG-01 — add .acceptance-runs/ to .gitignore and ImagePullBackOff row to troubleshooting.md</name>
  <files>.gitignore, docs/troubleshooting.md</files>
  <read_first>
    Read .gitignore to see the existing structure (look for /dist/ goreleaser entry at lines 19-20 as the analog). Read docs/troubleshooting.md to see the Symptom|Cause|Recipe table format and find the existing ImagePullBackoff row at line 27 (the analog). This new row adds a DISTINCT scenario: mid-install before images are published, not credential-missing.
  </read_first>
  <action>
    1. Append to .gitignore after the existing entries (after the `cmd/tide-demo-init/fixture/` entry):
       Add a blank line then:
       `# Phase 6 ACC-01 — acceptance run evidence archives (maintainer-local, never committed)`
       `.acceptance-runs/`
       Note: use bare `.acceptance-runs/` (no leading `/`) so it matches regardless of depth; the directory is always at repo root.

    2. Append to docs/troubleshooting.md a new row in the existing Symptom|Cause|Recipe table. The existing row at line 27 covers credential-missing / appVersion tag drift for RUNNING pods. The new row is specific to the pre-publish scenario (images were never pushed) and appears after the existing ImagePullBackoff row. Use the D-C4 format (pipe-delimited). Content:
       Symptom: `deploy/tide-controller-manager` or `tide-dashboard` pod stuck in `ImagePullBackOff` immediately after `helm install` (before the controller reaches `Available`)
       Cause: Chart references component images not yet published to ghcr.io (pre-release state), OR chart tag pin (`v0.1.0-dev`) doesn't match any published tag
       Recipe: 1. Check image existence: `docker manifest inspect ghcr.io/jsquirrelz/tide-controller:1.0.0`. 2. If not published: run `make acceptance-v1-smoke` (builds and kind-loads all 6 images locally). 3. Verify chart tags: `grep -E 'v0\\.1\\.0-dev' charts/tide/values.yaml` should return 0 after Phase 6. See `docs/INSTALL.md` §"Maintainer: image-publish" for the publish pipeline.
  </action>
  <verify>
    <automated>git check-ignore .acceptance-runs/ && grep -ciE 'ImagePullBackOff' docs/troubleshooting.md</automated>
    `git check-ignore .acceptance-runs/` must print the path. `grep -ciE 'ImagePullBackOff' docs/troubleshooting.md` must return >= 2 (existing row + new row).
  </verify>
  <acceptance_criteria>
    - `git check-ignore .acceptance-runs/` exits 0 and prints the path
    - `grep -ciE 'ImagePullBackOff' docs/troubleshooting.md` returns >= 2
    - `grep -c 'acceptance-runs' .gitignore` returns 1
    - New troubleshooting row references `make acceptance-v1-smoke` and `docker manifest inspect`
  </acceptance_criteria>
  <done>.acceptance-runs/ is git-ignored; troubleshooting.md has a new ImagePullBackOff mid-install row with a recipe pointing to the $0 smoke target</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart values → rendered manifest | tag: "" resolves via .Chart.AppVersion — planner confirms this is the correct empty-string falsy behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | charts/tide/values.yaml | mitigate | Edit SOT only (hack/helm/tide-values.yaml); run `make helm` to propagate; `helmify-verify` CI gate catches direct edits at release time |
| T-06-01-02 | Information Disclosure | .acceptance-runs/ | mitigate | .gitignore prevents accidental commit of run evidence (which may contain partial cluster state); this task adds the entry |
</threat_model>

<verification>
After both tasks complete:
- `grep -cE 'v0\.1\.0-dev' charts/tide/values.yaml hack/helm/tide-values.yaml` returns 0
- `helm template charts/tide | grep -E 'image:' | grep -v '1\.0\.0\|1\.36'` returns empty
- `grep -cE 'tag: "1\.36"' hack/helm/tide-values.yaml` returns 1
- `git check-ignore .acceptance-runs/` exits 0
- `grep -ciE 'ImagePullBackOff' docs/troubleshooting.md` >= 2
- `grep 'tide-stub-subagent:1\.0\.0' examples/projects/small/project.yaml` returns 1 line
</verification>

<success_criteria>
CHART-01 done: five `v0.1.0-dev` dead pins replaced with `""` in the SOT; chart regenerated; all 6 TIDE component tags resolve to `1.0.0`; busybox `1.36` preserved. HYG-01 done: `.acceptance-runs/` git-ignored; troubleshooting row added. A7 resolved: small project.yaml uses consistent `1.0.0` tag.
</success_criteria>

<output>
After completion, create `.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-01-SUMMARY.md`
</output>
