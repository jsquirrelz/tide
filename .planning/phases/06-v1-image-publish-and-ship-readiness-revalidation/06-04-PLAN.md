---
phase: 06
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
files_modified:
  - hack/scripts/load-images-if-needed.sh
  - hack/scripts/acceptance-v1.sh
  - hack/scripts/dry-run-v1.sh
  - Makefile
autonomous: true
requirements:
  - IMG-LOAD-01
  - DRY-01
  - ACC-01

must_haves:
  truths:
    - "hack/scripts/load-images-if-needed.sh exists and calls 'kind load docker-image ... --name ${cluster_name}' directly (NOT via test-int-kind-prep which hardcodes --name tide-test)"
    - "load-images-if-needed.sh uses 'docker manifest inspect' for the existence probe (no layer download)"
    - "load-images-if-needed.sh builds and loads all 6 images at tag 1.0.0 (no v prefix, matches chart appVersion after CHART-01)"
    - "acceptance-v1.sh ACCEPTANCE_SAMPLE=small mode skips ANTHROPIC_API_KEY and GH_PAT env-gates"
    - "acceptance-v1.sh replaces the broken ACCEPTANCE_LOAD_IMAGES block (lines 55-59) with a call to load-images-if-needed.sh"
    - "acceptance-v1.sh applies examples/projects/small/project.yaml in small mode (not large)"
    - "dry-run-v1.sh has cert-manager bring-up inside the DinD heredoc (DRY-01)"
    - "dry-run-v1.sh calls load-images-if-needed.sh from the outer script between the cluster-create DinD pass and the helm-install DinD pass (IMG-LOAD-01)"
    - "Makefile has acceptance-v1-smoke target (ACCEPTANCE_SAMPLE=small make acceptance-v1) and docker-buildx-snapshot target"
  artifacts:
    - path: "hack/scripts/load-images-if-needed.sh"
      provides: "Shared auto-detect image-load helper — docker manifest inspect probe + docker build + kind load"
      exports: ["load_images_if_needed function or direct script invocation with cluster_name and image_tag args"]
    - path: "hack/scripts/acceptance-v1.sh"
      provides: "Modified acceptance script with $0 small-sample mode (D-05)"
    - path: "hack/scripts/dry-run-v1.sh"
      provides: "DinD script with cert-manager bring-up (DRY-01) and image-load (IMG-LOAD-01)"
    - path: "Makefile"
      provides: "acceptance-v1-smoke and docker-buildx-snapshot targets"
  key_links:
    - from: "acceptance-v1.sh ACCEPTANCE_SAMPLE=small"
      to: "load-images-if-needed.sh"
      via: "bash ${REPO_ROOT}/hack/scripts/load-images-if-needed.sh ${CLUSTER_NAME} 1.0.0"
      pattern: "load-images-if-needed"
    - from: "dry-run-v1.sh outer script"
      to: "load-images-if-needed.sh"
      via: "split DinD heredoc — outer script loads images between cluster-create and helm-install passes"
      pattern: "load-images-if-needed"
    - from: "load-images-if-needed.sh"
      to: "kind cluster (dynamic name)"
      via: "kind load docker-image ... --name ${cluster_name} (NOT --name tide-test)"
      pattern: "kind load.*--name"
---

<objective>
Create the shared `load-images-if-needed.sh` auto-detect helper, wire it into both acceptance scripts, add cert-manager bring-up to `dry-run-v1.sh`, and add the two new Makefile targets.

Purpose: These are the script-wiring changes that close the two BOOT-04 cascade root causes: (1) ImagePullBackOff — the load helper ensures images exist in the cluster before helm install, (2) missing cert-manager — DRY-01 mirrors the proven pattern from acceptance-v1.sh. Together with Plan 01's tag alignment, these changes enable the $0 BOOT-04 gate (ACC-01) that Plan 06 exercises.

Output: New `hack/scripts/load-images-if-needed.sh` helper; modified `acceptance-v1.sh` with $0 small-sample mode; modified `dry-run-v1.sh` with cert-manager + image-load; `Makefile` with two new targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-SPEC.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-CONTEXT.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-PATTERNS.md
@.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-01-SUMMARY.md

<interfaces>
<!-- acceptance-v1.sh current relevant sections (verified live 2026-05-30): -->
<!-- lines 27-28: env-gates (ANTHROPIC_API_KEY, GH_PAT — must become conditional on ACCEPTANCE_SAMPLE != small) -->
<!-- lines 30-37: TIMESTAMP, RUN_START, ACCEPTANCE_DIR, CLUSTER_NAME, PROJECT_NAME=large-project, PROJECT_NAMESPACE=tide-sample-large -->
<!-- lines 54-59: ACCEPTANCE_LOAD_IMAGES block — broken delegation to test-int-kind-prep; REPLACE entirely -->
<!-- lines 61-74: cert-manager block (already exists in acceptance-v1.sh — the DRY-01 source to copy) -->
<!-- lines 80-102: large sample namespace+secret creation and project apply — must become conditional -->

<!-- dry-run-v1.sh current structure (verified live 2026-05-30): -->
<!-- lines 55-89: docker run --rm ... bash -c "..." heredoc containing the entire inner pipeline -->
<!-- inner heredoc lines: apt-get tools → kind create cluster --name tide-dry-run → helm install tide-crds → helm install tide → kubectl wait → kubectl apply small → kubectl wait Complete -->
<!-- cert-manager is ABSENT from the heredoc — DRY-01 adds it between kind create and helm install tide-crds -->
<!-- IMG-LOAD-01: split the heredoc; outer script calls load-images-if-needed.sh between two docker run passes -->

<!-- Makefile existing analog targets (verified live 2026-05-30): -->
<!-- acceptance-v1 (lines 566-573): ANTHROPIC_API_KEY guard + hack/scripts/acceptance-v1.sh -->
<!-- docker-buildx (lines 261-269): existing single-image buildx target -->
<!-- IMAGE_TAG ?= 1.0.0 variable: add once at the top of the docker targets section -->

<!-- load-images-if-needed.sh must NOT use test-int-kind-prep (Pitfall 1): -->
<!-- test-int-kind-prep hardcodes --name tide-test in Makefile lines 158-167 regardless of KIND_CLUSTER env -->
<!-- The helper must call: kind load docker-image "${img}" --name "${cluster_name}" directly -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create hack/scripts/load-images-if-needed.sh shared auto-detect helper</name>
  <files>hack/scripts/load-images-if-needed.sh</files>
  <read_first>
    Read hack/scripts/acceptance-v1.sh lines 22-24 (REPO_ROOT derivation pattern and shebang/set -euo pipefail header to replicate). Read Makefile lines 153-167 (test-int-kind-prep — the docker build + kind load shape to copy, but cluster name must be dynamic NOT hardcoded to tide-test). Confirm examples/projects/small/project.yaml has stub image tag 1.0.0 (from Plan 01 completion).
  </read_first>
  <action>
    Create hack/scripts/load-images-if-needed.sh as an executable bash script. Usage: `bash load-images-if-needed.sh <cluster_name> <image_tag>`. The script:

    Header:
    - `#!/usr/bin/env bash` shebang
    - Comment block explaining purpose: "Shared auto-detect image-load helper. For each of the 6 chart-referenced component images, checks registry existence via docker manifest inspect; if absent (pre-publish), builds the image locally and kind-loads it into the named cluster. Accepts: cluster_name (kind cluster name) image_tag (e.g. 1.0.0). Does NOT use test-int-kind-prep (which hardcodes --name tide-test — IMG-LOAD-01 Pitfall 1)."
    - `set -euo pipefail`
    - `REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"`

    Arguments (positional, not flags):
    - `CLUSTER_NAME="${1:?cluster_name argument required}"` 
    - `IMAGE_TAG="${2:?image_tag argument required (e.g. 1.0.0 — no v prefix)}"`

    Image and Dockerfile arrays (D-03 fixed inventory, verified against PATTERNS.md):
    - IMAGES array with 6 entries: ghcr.io/jsquirrelz/tide-controller, ghcr.io/jsquirrelz/tide-dashboard, ghcr.io/jsquirrelz/tide-stub-subagent, ghcr.io/jsquirrelz/tide-credproxy, ghcr.io/jsquirrelz/tide-push, ghcr.io/jsquirrelz/tide-claude-subagent
    - DOCKERFILES array with 6 entries (parallel to IMAGES): ./Dockerfile, ./Dockerfile.dashboard, images/stub-subagent/Dockerfile, images/credproxy/Dockerfile, images/tide-push/Dockerfile, images/claude-subagent/Dockerfile

    Loop over the arrays:
    For each index i from 0 to 5:
    - img="${IMAGES[$i]}:${IMAGE_TAG}"
    - df="${DOCKERFILES[$i]}"
    - Run `docker manifest inspect "${img}" > /dev/null 2>&1`
      - If exit 0: print "==> ${img}: found in registry, skipping local build"
      - If non-zero exit: print "==> ${img}: not found in registry, building locally..." then:
        - `docker build -t "${img}" -f "${df}" "${REPO_ROOT}"`
        - `kind load docker-image "${img}" --name "${CLUSTER_NAME}"`
        - Print "==> ${img}: built and loaded into cluster ${CLUSTER_NAME}"

    The image tag `1.0.0` (no `v` prefix) matches chart appVersion after Plan 01's CHART-01 fix and the A7 project.yaml fix. The local build at `:1.0.0` matches what `helm template charts/tide` requests, so `pullPolicy: IfNotPresent` (set on all 6 images in the chart) causes the controller to use the kind-loaded image without attempting a registry pull.

    Make the script executable: `chmod +x hack/scripts/load-images-if-needed.sh`
  </action>
  <verify>
    <automated>bash -n hack/scripts/load-images-if-needed.sh && grep -cE 'kind load docker-image.*--name.*CLUSTER_NAME' hack/scripts/load-images-if-needed.sh</automated>
    `bash -n` checks syntax. `grep -cE 'kind load docker-image.*--name.*CLUSTER_NAME'` must return 1. Also: `grep -c 'test-int-kind-prep' hack/scripts/load-images-if-needed.sh` must return 0 (no delegation to the broken target). Also: `grep -cE 'docker manifest inspect' hack/scripts/load-images-if-needed.sh` must return 1. Also: `grep -c 'tide-stub-subagent' hack/scripts/load-images-if-needed.sh` must return 1 (stub image in list).
  </verify>
  <acceptance_criteria>
    - `bash -n hack/scripts/load-images-if-needed.sh` exits 0 (syntax valid)
    - `grep -cE 'kind load docker-image.*--name.*\$\{CLUSTER_NAME\}' hack/scripts/load-images-if-needed.sh` returns 1
    - `grep -c 'test-int-kind-prep' hack/scripts/load-images-if-needed.sh` returns 0
    - `grep -cE 'docker manifest inspect' hack/scripts/load-images-if-needed.sh` returns 1
    - `grep -c 'tide-claude-subagent' hack/scripts/load-images-if-needed.sh` returns 1 (all 6 in IMAGES array)
    - `test -x hack/scripts/load-images-if-needed.sh` exits 0 (executable bit set)
  </acceptance_criteria>
  <done>load-images-if-needed.sh exists, is executable, uses docker manifest inspect for probing, calls kind load with dynamic cluster name, covers all 6 D-03 images; never delegates to test-int-kind-prep</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modify acceptance-v1.sh for $0 small-sample mode (D-05) and wire image-load helper</name>
  <files>hack/scripts/acceptance-v1.sh</files>
  <read_first>
    Read hack/scripts/acceptance-v1.sh fully — specifically lines 1-30 (shebang/comments/env-gates), lines 30-60 (TIMESTAMP/CLUSTER_NAME setup and ACCEPTANCE_LOAD_IMAGES block), lines 61-80 (cert-manager block and helm install), lines 80-110 (large sample namespace+secret+apply block). The cert-manager block at lines 61-74 STAYS — it is already there from commit adb1053. The broken ACCEPTANCE_LOAD_IMAGES block at lines 55-59 is the primary replacement target.
  </read_first>
  <action>
    Modify hack/scripts/acceptance-v1.sh with these four changes in order:

    CHANGE 1 — Add ACCEPTANCE_SAMPLE variable and make env-gates conditional.
    After the existing shebang, set statement, and REPO_ROOT line, add (before the env-gate lines 27-28):
    `ACCEPTANCE_SAMPLE="${ACCEPTANCE_SAMPLE:-large}"`
    Then wrap the existing two env-gate lines (ANTHROPIC_API_KEY and GH_PAT) in a condition:
    `if [ "${ACCEPTANCE_SAMPLE}" != "small" ]; then`
    `  : "${ANTHROPIC_API_KEY:?...}"` (existing text unchanged)
    `  : "${GH_PAT:?...}"` (existing text unchanged)
    `fi`
    Rationale: $0 mode requires no API key or git PAT; failing fast on their absence would prevent the $0 path from working (D-05).

    CHANGE 2 — Set PROJECT_NAME and PROJECT_NAMESPACE conditionally.
    The existing lines set PROJECT_NAME=large-project and PROJECT_NAMESPACE=tide-sample-large. Add after CLUSTER_NAME:
    `if [ "${ACCEPTANCE_SAMPLE}" = "small" ]; then`
    `  PROJECT_NAME="small-project"`
    `  PROJECT_NAMESPACE="tide-sample-small"`
    `else`
    `  PROJECT_NAME="large-project"` (existing value)
    `  PROJECT_NAMESPACE="tide-sample-large"` (existing value)
    `fi`

    CHANGE 3 — Replace the broken ACCEPTANCE_LOAD_IMAGES block (lines 55-59) with an unconditional call to load-images-if-needed.sh.
    Remove the entire if/fi block:
    `if [ -n "${ACCEPTANCE_LOAD_IMAGES:-}" ]; then`
    `  KIND_CLUSTER="${CLUSTER_NAME}" make -C "${REPO_ROOT}" test-int-kind-prep || true`
    `fi`
    Replace with:
    `# Auto-detect: pull published images or build+kind-load locally (IMG-LOAD-01 / RESEARCH Pitfall 1).`
    `# Uses docker manifest inspect probe; never delegates to test-int-kind-prep (hardcodes --name tide-test).`
    `IMAGE_TAG="1.0.0"  # matches chart appVersion after CHART-01`
    `bash "${REPO_ROOT}/hack/scripts/load-images-if-needed.sh" "${CLUSTER_NAME}" "${IMAGE_TAG}"`

    CHANGE 4 — Make the project-apply and wait section conditional on ACCEPTANCE_SAMPLE.
    The existing large-sample block creates a namespace, creates a tide-secrets Secret from env vars, applies the large project.yaml, and waits. Wrap the entire existing large-sample block in `if [ "${ACCEPTANCE_SAMPLE}" != "small" ]; then ... fi`.
    Add the small-sample branch before the else:
    `if [ "${ACCEPTANCE_SAMPLE}" = "small" ]; then`
    `  echo "==> applying examples/projects/small/project.yaml ($0 stub mode — D-05)..."`
    `  kubectl apply -f "${REPO_ROOT}/examples/projects/small/project.yaml"`
    `  kubectl wait --for=jsonpath='{.status.phase}'=Complete \`
    `    "project/${PROJECT_NAME}" \`
    `    -n "${PROJECT_NAMESPACE}" \`
    `    --timeout=10m`
    `else`
    `  # existing large-sample Secret creation + project apply + 4h wait — unchanged`
    `  ...`
    `fi`

    The D-06 pass criteria (controller Available + dashboard Running + small Project terminal + zero ERROR logs + no orphan Jobs + no ImagePullBackOff) are verified by the existing `kubectl wait` steps plus the final acceptance-verify.sh call which can be skipped or made conditional for $0 mode (the verifier asserts D-A3 which includes per-run-branch checks — those are large-sample only). If acceptance-verify.sh is called at the end, gate its invocation on `ACCEPTANCE_SAMPLE != small`, or ensure it handles the small mode gracefully.
  </action>
  <verify>
    <automated>bash -n hack/scripts/acceptance-v1.sh && grep -cE 'ACCEPTANCE_SAMPLE' hack/scripts/acceptance-v1.sh</automated>
    `bash -n` checks syntax. `grep -cE 'ACCEPTANCE_SAMPLE'` must return >= 4 (variable assignment + multiple conditional checks). Also: `grep -c 'load-images-if-needed.sh' hack/scripts/acceptance-v1.sh` must return 1. Also: `grep -c 'test-int-kind-prep' hack/scripts/acceptance-v1.sh` must return 0 (broken delegation removed). Also: `grep -c 'small-project' hack/scripts/acceptance-v1.sh` must return >= 1. Also: `grep -c 'ANTHROPIC_API_KEY' hack/scripts/acceptance-v1.sh` must return >= 1 (still present but conditional).
  </verify>
  <acceptance_criteria>
    - `bash -n hack/scripts/acceptance-v1.sh` exits 0
    - `grep -c 'load-images-if-needed.sh' hack/scripts/acceptance-v1.sh` returns 1
    - `grep -c 'test-int-kind-prep' hack/scripts/acceptance-v1.sh` returns 0
    - `grep -cE 'ACCEPTANCE_SAMPLE' hack/scripts/acceptance-v1.sh` returns >= 4
    - `grep -c 'small-project' hack/scripts/acceptance-v1.sh` returns >= 1
    - `grep -cE 'examples/projects/small/project\.yaml' hack/scripts/acceptance-v1.sh` returns 1
    - `grep -cE 'cert-manager' hack/scripts/acceptance-v1.sh` returns >= 3 (existing block preserved from commit adb1053)
  </acceptance_criteria>
  <done>acceptance-v1.sh has $0 small-sample mode (ACCEPTANCE_SAMPLE=small skips API key gates, applies small project, waits for small-project Complete); broken ACCEPTANCE_LOAD_IMAGES replaced with load-images-if-needed.sh call; cert-manager block preserved</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Modify dry-run-v1.sh — DRY-01 cert-manager + IMG-LOAD-01 image-load via DinD split; add Makefile targets</name>
  <files>hack/scripts/dry-run-v1.sh, Makefile</files>
  <read_first>
    Read hack/scripts/dry-run-v1.sh fully — specifically lines 50-95 (the docker run heredoc containing kind create, helm install, kubectl apply). The heredoc currently has: kind create cluster --name tide-dry-run → helm install tide-crds → helm install tide → kubectl wait controller → kubectl apply small → kubectl wait Complete. DRY-01 inserts cert-manager between kind create and helm install tide-crds. IMG-LOAD-01 requires splitting: first docker run creates the cluster; outer script calls load-images-if-needed.sh; second docker run runs helm install + kubectl apply + wait. Read Makefile lines 255-270 (docker-buildx target for analog) and lines 562-573 (acceptance-v1 target for acceptance-v1-smoke analog).
  </read_first>
  <action>
    CHANGE 1 — Modify hack/scripts/dry-run-v1.sh for DRY-01 and IMG-LOAD-01.

    The key insight (RESEARCH Open Question 1): `dry-run-v1.sh` uses `docker run --network host -v /var/run/docker.sock:/var/run/docker.sock`. The DinD container shares the host Docker daemon. A kind cluster created inside the DinD container (which uses the host Docker daemon) is reachable from the outer script via `kind get clusters`. This means we can split the single `docker run` heredoc into two passes:
    - Pass 1: `docker run` heredoc creates the kind cluster and installs tools (no helm install yet)
    - Outer script: calls `load-images-if-needed.sh` to build+load images into `tide-dry-run` cluster
    - Pass 2: `docker run` heredoc runs cert-manager install + helm install + kubectl apply + kubectl wait

    Implementation:

    Split the existing single `docker run ... bash -c "..."` heredoc into two separate `docker run` invocations.

    Pass 1 docker run (new; inherits same docker run flags: --rm, -v /var/run/docker.sock, -v ${DRY_RUN_DIR}:/workspace, --network host, ubuntu:24.04):
    Inner script: apt-get tools (curl, ca-certificates, git) + install kind + install helm + install kubectl + `kind create cluster --name tide-dry-run` + `git clone ${DRY_RUN_REPO_URL} /workspace/tide`
    (Everything up to and NOT including the helm install steps)

    Outer script (between Pass 1 and Pass 2):
    Add after Pass 1 docker run completes:
    `echo "==> loading images into tide-dry-run cluster (IMG-LOAD-01)..."`
    `bash "${REPO_ROOT}/hack/scripts/load-images-if-needed.sh" "tide-dry-run" "1.0.0"`

    Pass 2 docker run (new; same docker run flags):
    Inner script:
    `cd /workspace/tide` (the git clone is already there from Pass 1)
    DRY-01 cert-manager block (mirror from acceptance-v1.sh lines 68-74):
    `CERT_MANAGER_VERSION="${TIDE_CERT_MANAGER_VERSION:-v1.20.2}"`
    `kubectl apply -f "https://github.com/cert-manager/cert-manager/releases/download/${CERT_MANAGER_VERSION}/cert-manager.yaml"`
    `for deploy in cert-manager cert-manager-cainjector cert-manager-webhook; do`
    `  kubectl -n cert-manager rollout status deployment/"${deploy}" --timeout=120s`
    `done`
    Then: `helm install tide-crds ./charts/tide-crds -n tide-system --create-namespace`
    Then: `helm install tide ./charts/tide -n tide-system`
    Then: `kubectl wait --for=condition=Available deploy/tide-controller-manager -n tide-system --timeout=5m`
    Then: `kubectl apply -f examples/projects/small/project.yaml`
    Then: `kubectl wait --for=jsonpath='{.status.phase}'=Complete project/small-project -n tide-sample-small --timeout=10m`

    The `TIDE_CERT_MANAGER_VERSION` override: inside the heredoc, bash variable expansion works for `${TIDE_CERT_MANAGER_VERSION:-v1.20.2}` because heredocs interpolate by default. Pass the env var via `-e TIDE_CERT_MANAGER_VERSION="${TIDE_CERT_MANAGER_VERSION:-v1.20.2}"` on the Pass 2 docker run line so operators can override it.

    The `EXIT_CODE=${PIPESTATUS[0]}` and `END_TIME` + `ELAPSED` + `render-dry-run-report.sh` call at the end of the script must be updated to capture the exit code of Pass 2 (the last docker run), not the overall pipeline exit (use `set -e` to propagate failures naturally given `set -euo pipefail` at the top).

    CHANGE 2 — Makefile: add acceptance-v1-smoke and docker-buildx-snapshot targets.

    Add `IMAGE_TAG ?= 1.0.0` variable near the existing PLATFORMS/IMG variables (after line ~255 in the docker target section).

    Add after the existing `acceptance-v1` target (after line 573):
    ```
    .PHONY: acceptance-v1-smoke
    acceptance-v1-smoke: ## Phase 6 ACC-01 — $0 BOOT-04 revalidation (stub-subagent, no API key required).
    	@ACCEPTANCE_SAMPLE=small hack/scripts/acceptance-v1.sh
    ```

    Add after the existing `docker-buildx` target (after line ~270):
    ```
    .PHONY: docker-buildx-snapshot
    docker-buildx-snapshot: ## Phase 6 IMG-01 — local multi-arch snapshot build of all 6 component images (no push; IMAGE_TAG=$(IMAGE_TAG)).
    	@echo "Building all 6 component images for linux/amd64,linux/arm64 (no push)..."
    	$(CONTAINER_TOOL) buildx build --platform linux/amd64,linux/arm64 \
    		-t ghcr.io/jsquirrelz/tide-controller:$(IMAGE_TAG) -f ./Dockerfile .
    	$(CONTAINER_TOOL) buildx build --platform linux/amd64,linux/arm64 \
    		-t ghcr.io/jsquirrelz/tide-dashboard:$(IMAGE_TAG) -f ./Dockerfile.dashboard .
    	$(CONTAINER_TOOL) buildx build --platform linux/amd64,linux/arm64 \
    		-t ghcr.io/jsquirrelz/tide-stub-subagent:$(IMAGE_TAG) -f images/stub-subagent/Dockerfile .
    	$(CONTAINER_TOOL) buildx build --platform linux/amd64,linux/arm64 \
    		-t ghcr.io/jsquirrelz/tide-credproxy:$(IMAGE_TAG) -f images/credproxy/Dockerfile .
    	$(CONTAINER_TOOL) buildx build --platform linux/amd64,linux/arm64 \
    		-t ghcr.io/jsquirrelz/tide-push:$(IMAGE_TAG) -f images/tide-push/Dockerfile .
    	$(CONTAINER_TOOL) buildx build --platform linux/amd64,linux/arm64 \
    		-t ghcr.io/jsquirrelz/tide-claude-subagent:$(IMAGE_TAG) -f images/claude-subagent/Dockerfile .
    ```
    Note: Makefile recipe lines use tab indentation, not spaces.
  </action>
  <verify>
    <automated>bash -n hack/scripts/dry-run-v1.sh && grep -cE 'cert-manager' hack/scripts/dry-run-v1.sh && grep -cE 'load-images-if-needed' hack/scripts/dry-run-v1.sh</automated>
    `bash -n` must exit 0. `grep -cE 'cert-manager'` must return >= 1. `grep -cE 'load-images-if-needed'` must return 1. Also: `grep -cE 'acceptance-v1-smoke' Makefile` must return >= 2 (target declaration + phony). `grep -cE 'docker-buildx-snapshot' Makefile` must return >= 2.
  </verify>
  <acceptance_criteria>
    - `bash -n hack/scripts/dry-run-v1.sh` exits 0
    - `grep -cE 'cert-manager' hack/scripts/dry-run-v1.sh` returns >= 2 (kubectl apply + rollout status loop)
    - `grep -cE 'load-images-if-needed' hack/scripts/dry-run-v1.sh` returns 1
    - `grep -cE 'TIDE_CERT_MANAGER_VERSION' hack/scripts/dry-run-v1.sh` returns >= 1
    - `grep -cE '^.PHONY: acceptance-v1-smoke' Makefile` returns 1
    - `grep -cE '^.PHONY: docker-buildx-snapshot' Makefile` returns 1
    - `grep -cE 'ACCEPTANCE_SAMPLE=small' Makefile` returns 1 (acceptance-v1-smoke target)
  </acceptance_criteria>
  <done>dry-run-v1.sh has cert-manager bring-up (DRY-01) and image-load via load-images-if-needed.sh with DinD heredoc split (IMG-LOAD-01); Makefile has acceptance-v1-smoke and docker-buildx-snapshot targets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → kind cluster | docker build + kind load is local-only; no credentials required; images tagged :1.0.0 match chart appVersion |
| docker manifest inspect → ghcr.io | Uses local Docker credential store; if not logged in, probe returns non-zero → triggers local build (correct behavior for pre-publish $0 run) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Spoofing | load-images-if-needed.sh builds images from local Dockerfiles | accept | This is a local maintainer-only tool; images are built from the repo's own Dockerfiles; no external image pull occurs for the build step itself |
| T-06-04-02 | Information Disclosure | ACCEPTANCE_SAMPLE=small bypasses ANTHROPIC_API_KEY gate | accept | The $0 path explicitly has no API key — the stub-subagent binary (cmd/stub-subagent) makes zero API calls; absoluteCapCents: 0 in small/project.yaml enforces this at the controller level |
| T-06-04-03 | Tampering | DinD split — outer script loads images into tide-dry-run kind cluster | accept | The cluster name `tide-dry-run` is fixed in dry-run-v1.sh; the outer load-images-if-needed.sh call uses this exact name; no cross-cluster contamination possible |
</threat_model>

<verification>
After all 3 tasks complete:
- `bash -n hack/scripts/load-images-if-needed.sh` exits 0
- `bash -n hack/scripts/acceptance-v1.sh` exits 0
- `bash -n hack/scripts/dry-run-v1.sh` exits 0
- `grep -c 'test-int-kind-prep' hack/scripts/acceptance-v1.sh hack/scripts/load-images-if-needed.sh` returns 0
- `grep -cE 'cert-manager' hack/scripts/dry-run-v1.sh` returns >= 2
- `grep -cE 'ACCEPTANCE_SAMPLE' hack/scripts/acceptance-v1.sh` returns >= 4
- `grep -cE '^.PHONY: acceptance-v1-smoke' Makefile` returns 1
</verification>

<success_criteria>
load-images-if-needed.sh exists and uses docker manifest inspect + kind load with dynamic cluster name for all 6 D-03 images at tag 1.0.0. acceptance-v1.sh has ACCEPTANCE_SAMPLE=small mode (no API key gate, applies small project, calls load helper). dry-run-v1.sh has cert-manager bring-up (DRY-01) and image-load via helper. Makefile has acceptance-v1-smoke and docker-buildx-snapshot targets. `make acceptance-v1-smoke` can be invoked and will reach the script without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-v1-image-publish-and-ship-readiness-revalidation/06-04-SUMMARY.md`
</output>
