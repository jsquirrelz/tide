---
phase: 44-llm-message-array-spans-d-o5-redaction-size-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/harness/redact/redact.go
  - internal/harness/redact/redact_test.go
  - pkg/otelai/attrs.go
  - pkg/otelai/attrs_test.go
  - pkg/otelai/doc.go
autonomous: true
requirements: [MSG-02, MSG-03]
tags: [opentelemetry, otelai, redaction, openinference]

must_haves:
  truths:
    - "redact.String applies the same SecretPatterns denylist as RedactingWriter to a whole in-memory string"
    - "pkg/otelai can express tool-call and reasoning content blocks with spec-native OpenInference keys (D-03 resolved YES per research)"
    - "An LLM-kind span-kind attribute helper exists so tracesynth never hand-rolls openinference.span.kind"
    - "Both guard tests (TestNoPayloadHelperOnPublicSurface, TestKeysUseSemconvModule) still pass after the additions — updated deliberately, never deleted"
  artifacts:
    - path: "internal/harness/redact/redact.go"
      provides: "String(s string) string — non-streaming MSG-02 redaction helper"
      contains: "func String"
    - path: "pkg/otelai/attrs.go"
      provides: "ToolCall/MessageContent types, extended flattenMessages, LLMSpanKind, TimingSynthetic, ParseDegraded helpers"
      contains: "func LLMSpanKind"
    - path: "pkg/otelai/doc.go"
      provides: "D-O5 text evolved to bounded-inline + ArtifactPath co-attribute; helper enumeration updated"
  key_links:
    - from: "pkg/otelai/attrs.go"
      to: "openinference-semantic-conventions v0.1.1"
      via: "semconv.* constants + indexer helpers for every spec-family key"
      pattern: "semconv\\.(LLMOutputMessageToolCallKey|MessageContents|OpenInferenceSpanKind)"
    - from: "internal/harness/redact/redact.go"
      to: "internal/harness/redact/patterns.go"
      via: "package-level SecretPatterns var"
      pattern: "SecretPatterns"
---

<objective>
Build the two foundation surfaces every later Phase 44 plan compiles against: (1) a non-streaming `redact.String` helper (the mandated MSG-02 pass), and (2) `pkg/otelai` extensions for spec-native tool-call/reasoning encoding (D-03 resolved YES by research — the vendored v0.1.1 module carries all keys), an LLM span-kind helper, and the two new `tide.*` marker attributes (synthetic timing, parse degradation) — plus the deliberate D-O5 doc/guard-test evolution MSG-03 mandates.

**EXECUTION GATE (phase-wide):** Phase 44 execution REQUIRES branch `worktree-phase-43-discuss` merged into `main` first. This plan's target files reference Phase-42/43 state (`pkg/otelai/tracecontext.go`, the 43-shipped guard-test wording); verify the merge landed before editing: `git log --oneline -3 -- pkg/otelai/tracecontext.go` must show Phase 42/43 commits on the current branch.

Purpose: interface-first — Plan 44-03's synthesizer consumes these helpers; defining them first removes all encoding ambiguity from the heavy plan.
Output: extended `pkg/otelai` public surface + `redact.String`, all unit-tested, both guard tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-CONTEXT.md
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-RESEARCH.md  (§"D-03 Resolution", §"Don't Hand-Roll")
@.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-PATTERNS.md  (§redact.go, §attrs.go, §attrs_test.go, §doc.go sections)

<interfaces>
Verified against the vendored module at `$(go env GOMODCACHE)/github.com/!arize-ai/openinference/go/openinference-semantic-conventions@v0.1.1/`:

Constants (attributes.go / enums.go):
- `OpenInferenceSpanKind = "openinference.span.kind"`, `SpanKindLLM = "LLM"` (enums.go:11)
- `MessageContents = "message.contents"`, `MessageContentType = "message_content.type"`, `MessageContentText = "message_content.text"`, `MessageContentSignature = "message_content.signature"` (type values include "reasoning")
- `MessageToolCalls = "message.tool_calls"`, `ToolCallID = "tool_call.id"`, `ToolCallFunctionName = "tool_call.function.name"`, `ToolCallFunctionArgumentsJSON = "tool_call.function.arguments"`

Indexers (indexers.go):
- `LLMOutputMessageToolCallKey(i, j int, child string) string` — e.g. "llm.output_messages.0.message.tool_calls.0.tool_call.function.name"
- `LLMInputMessageToolCallKey(i, j int, child string) string`
- There is NO public indexer for message.contents — those keys must be COMPOSED from semconv constants (`fmt.Sprintf` over `semconv.LLMInputMessages`/`semconv.LLMOutputMessages` + index + `semconv.MessageContents` + index + `semconv.MessageContentType`/`Text`/`Signature`). Composition from constants satisfies TestKeysUseSemconvModule (the guard rejects raw `"llm."`-prefixed string literals, not constant-composed keys).

Current pkg/otelai surface (attrs.go, post-Phase-42):
- `Message{Role, Content string}`, `flattenMessages(input bool, msgs []Message) []attribute.KeyValue` (2*N flat keys)
- `TokenCount(prompt, completion, cacheRead, cacheWrite int)`, `AgentInvocation(system, name, role, level string)`, `ArtifactPath(path string)`, `LLMIdentity(provider, model string)`, `FailureDetail(exitCode int, reason string)`, `EnvelopeDegraded()`
- tide.* const block: keyAgentRole, keyAgentInvocationLevel, keyArtifactPath, keyExitCode, keyReason, keyEnvelopeDegraded
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add redact.String — the non-streaming MSG-02 pass</name>
  <files>internal/harness/redact/redact.go, internal/harness/redact/redact_test.go</files>
  <read_first>
    - internal/harness/redact/redact.go (current RedactingWriter + Close(); String sits beside them and mirrors Close()'s full-buffer ReplaceAll loop)
    - internal/harness/redact/patterns.go (SecretPatterns — the compiled six-pattern denylist String iterates)
    - internal/harness/redact/redact_test.go (TestRedactingWriter table ~lines 56-100 — reuse its exact secret fixtures)
  </read_first>
  <behavior>
    - Test 1: TestString table-driven — each of the existing TestRedactingWriter secret fixtures (Anthropic sk-ant key, JWT, AWS key, GitHub PAT, Slack token) passed to String returns the same "[REDACTED]"-substituted output the writer produces
    - Test 2: a string with no secret returns byte-identical input
    - Test 3: empty string returns empty string
  </behavior>
  <action>
    Add exported func String(s string) string to internal/harness/redact/redact.go, placed after RedactingWriter's methods. Implementation: convert to []byte, loop the package-level SecretPatterns applying re.ReplaceAll with []byte("[REDACTED]"), return string. Doc comment must state: (a) unlike RedactingWriter (stream-oriented, tail-keep buffered), String operates on a fully-materialized string — the correct shape for per-message redaction (MSG-02); (b) D-09 contract for callers: String MUST run BEFORE any truncation step (a truncation cut can split a secret so the pattern no longer matches — same bug class RedactingWriter's tail-keep buffer defends against); cite CONTEXT.md D-09 and RESEARCH.md Pitfall 4. In redact_test.go add TestString reusing the exact fixture strings from the TestRedactingWriter table (do not invent new secret shapes). No D-09 ordering test here — ordering is a tracesynth call-site concern tested in plan 44-03.
  </action>
  <verify>
    <automated>go test ./internal/harness/redact/... -run 'TestString|TestRedactingWriter' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func String(s string) string' internal/harness/redact/redact.go` returns 1
    - String's doc comment contains the literal token "D-09"
    - TestString exercises at least 5 secret-pattern fixtures plus a no-secret passthrough case
    - `go test ./internal/harness/redact/... -count=1` exits 0
  </acceptance_criteria>
  <done>redact.String exists, is documented with the D-09 caller contract, and passes a table test reusing the proven RedactingWriter fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pkg/otelai — tool-call/reasoning encoding, LLMSpanKind, markers, D-O5 doc evolution</name>
  <files>pkg/otelai/attrs.go, pkg/otelai/attrs_test.go, pkg/otelai/doc.go</files>
  <read_first>
    - pkg/otelai/attrs.go (Message, flattenMessages, the tide.* const block with its citation style, AgentInvocation — the shape LLMSpanKind mirrors, EnvelopeDegraded — the shape the two new markers mirror)
    - pkg/otelai/attrs_test.go (TestNoPayloadHelperOnPublicSurface forbidden list: "func Payload"/"func InlinePayload"/"func RawContent"/"func Body"/"func MessageBody"; TestKeysUseSemconvModule regex `"(llm\.|openinference\.|gen_ai\.|agent\.)` on comment-stripped attrs.go; TestFailureDetail/TestEnvelopeDegraded DeepEqual style; TestEmptyInputsNoPanic)
    - pkg/otelai/doc.go ("Public surface (8 helpers)" enumeration + "# D-O5 — no payload inlining" section — both get the deliberate MSG-03 update)
    - $(go env GOMODCACHE)/github.com/!arize-ai/openinference/go/openinference-semantic-conventions@v0.1.1/indexers.go (re-confirm LLMOutputMessageToolCallKey/LLMInputMessageToolCallKey signatures via `go doc` before coding)
  </read_first>
  <behavior>
    - Test 1: a Message with ToolCalls=[{ID:"toolu_1", Name:"Bash", ArgumentsJSON:"{\"command\":\"ls\"}"}] at output index 0 emits exactly the keys "llm.output_messages.0.message.tool_calls.0.tool_call.id", ".../tool_call.function.name", ".../tool_call.function.arguments" with matching values (assert exact key strings)
    - Test 2: a Message with Contents=[{Type:"reasoning", Text:"Let me read...", Signature:"EpICCmUIDhgC"}] at output index 0 emits "llm.output_messages.0.message.contents.0.message_content.type"="reasoning", ".../message_content.text", ".../message_content.signature" (assert exact key strings)
    - Test 3: LLMSpanKind() returns attribute.String("openinference.span.kind", "LLM")
    - Test 4: TimingSynthetic() returns attribute.Bool("tide.trace.timing_synthetic", true); ParseDegraded() returns attribute.Bool("tide.trace.parse_degraded", true)
    - Test 5: a Message with nil ToolCalls and nil Contents emits exactly the legacy 2-key role/content shape (backward compatibility with Phase 42 call sites)
    - Test 6: nil/empty-slice inputs to the extended paths do not panic (extend TestEmptyInputsNoPanic)
    - Test 7 (regression): TestNoPayloadHelperOnPublicSurface and TestKeysUseSemconvModule still pass
  </behavior>
  <action>
    Four additions to pkg/otelai/attrs.go, all following the existing thin-helper + semconv-resolution house style (ATTR-03):

    1. New types: ToolCall{ID, Name, ArgumentsJSON string} and MessageContent{Type, Text, Signature string}. Extend Message with two optional fields: ToolCalls []ToolCall and Contents []MessageContent (nil = legacy flat encoding, zero behavior change for existing callers).

    2. Extend flattenMessages: after emitting the existing role/content pair per message, when m.ToolCalls is non-empty emit 3 attributes per call via the module's own indexers — semconv.LLMOutputMessageToolCallKey(i, j, semconv.ToolCallID / semconv.ToolCallFunctionName / semconv.ToolCallFunctionArgumentsJSON) for output messages, semconv.LLMInputMessageToolCallKey(...) for input. When m.Contents is non-empty emit type/text/signature per content block using COMPOSED keys (no public indexer exists): fmt.Sprintf over the prefix constant (semconv.LLMInputMessages or semconv.LLMOutputMessages), message index, semconv.MessageContents, content index, and semconv.MessageContentType/MessageContentText/MessageContentSignature. Omit the signature attribute when Signature is empty (input-side tool_result blocks have none). NEVER type a raw "llm."/"openinference."-prefixed string literal — TestKeysUseSemconvModule greps for exactly that.

    3. New helpers mirroring existing shapes: LLMSpanKind() attribute.KeyValue returning attribute.String(semconv.OpenInferenceSpanKind, semconv.SpanKindLLM) (AgentInvocation hardcodes SpanKindAgent and is NOT reusable — PATTERNS §"openinference.span.kind attribute discipline"); TimingSynthetic() and ParseDegraded() returning single Bool attributes on two NEW tide.* consts added to the existing const block: keyTimingSynthetic = "tide.trace.timing_synthetic" (Claude's-Discretion timing floor — flags non-measured span timing) and keyParseDegraded = "tide.trace.parse_degraded" (D-11 marker — conversation reconstructed from a stream with skipped/truncated lines). Both consts get the D-05 "no module counterpart" citation comment matching the six existing entries.

    4. doc.go — the deliberate MSG-03 evolution: update "Public surface (8 helpers)" to enumerate the new count including LLMSpanKind/TimingSynthetic/ParseDegraded; rewrite the "# D-O5 — no payload inlining" section from "prefer ArtifactPath" to the Phase 44 contract: bounded-inline (redacted via internal/harness/redact.String per MSG-02, then truncated per-message per MSG-03/D-08) PLUS ArtifactPath co-attribute on the same span as the full-fidelity PVC reference. Update the Message.Content paragraph: production callers MAY now inline content that has passed the redact-then-truncate pipeline (cite MSG-02/MSG-03/D-09). Keep the TestNoPayloadHelperOnPublicSurface enforcement sentence — the forbidden-name list is unchanged and the guard is extended in comment only, never weakened.

    In attrs_test.go: add the behavior tests above in the existing reflect.DeepEqual / exact-key-string style; update TestNoPayloadHelperOnPublicSurface's doc comment (not its forbidden list) to reference the evolved D-O5 contract. Do not remove any existing assertion.
  </action>
  <verify>
    <automated>go test ./pkg/otelai/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/otelai/... -run 'TestNoPayloadHelperOnPublicSurface|TestKeysUseSemconvModule' -count=1` exits 0 (guards intact)
    - A test asserts the literal expected key string "llm.output_messages.0.message.tool_calls.0.tool_call.function.name"
    - A test asserts the literal expected key string "llm.output_messages.0.message.contents.0.message_content.type" with value "reasoning"
    - `grep -c 'func LLMSpanKind' pkg/otelai/attrs.go` returns 1; `grep -c 'tide.trace.timing_synthetic\|tide.trace.parse_degraded' pkg/otelai/attrs.go` returns 2
    - `grep -vE '^\s*(//|\*)' pkg/otelai/attrs.go | grep -cE '"(llm\.|openinference\.)'` returns 0 (no hand-rolled spec literals outside comments)
    - doc.go contains "ArtifactPath co-attribute" (the evolved D-O5 wording)
    - `go build ./pkg/otelai/... ./internal/controller/...` exits 0 (Message extension breaks no Phase 42 call site)
  </acceptance_criteria>
  <done>pkg/otelai encodes tool calls and reasoning blocks spec-natively, exposes LLMSpanKind + the two markers, doc.go documents the bounded-inline + co-attribute contract, and both guard tests pass unmodified in their assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo/model content → span attribute values | Untrusted text (which may embed leaked secrets) becomes OTLP attribute values via Message.Content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure | redact.String | mitigate | Single source of truth: String reuses SecretPatterns (no second regex set → no pattern drift between stdout-redaction and trace-redaction paths) |
| T-44-02 | Information Disclosure | pkg/otelai Message.Content inlining | mitigate | D-O5 doc contract requires the redact-then-truncate pipeline before inlining; guard test TestNoPayloadHelperOnPublicSurface stays intact |
| T-44-SC | Tampering | package installs | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit: N/A — all deps pinned since Phase 42) |
</threat_model>

<verification>
- `go test ./internal/harness/redact/... ./pkg/otelai/... -count=1` exits 0
- `go vet ./internal/harness/redact/... ./pkg/otelai/...` clean
- `golangci-lint run ./internal/harness/redact/... ./pkg/otelai/...` introduces 0 new issues
</verification>

<success_criteria>
- redact.String exists with the D-09 caller contract documented
- Tool-call + reasoning-block spec-native encoding proven by exact-key-string tests
- LLMSpanKind/TimingSynthetic/ParseDegraded available for plan 44-03
- Both D-O5/ATTR-03 guard tests pass; doc.go reflects the bounded-inline + ArtifactPath co-attribute contract
</success_criteria>

<output>
Create `.planning/phases/44-llm-message-array-spans-d-o5-redaction-size-boundary/44-01-SUMMARY.md` when done.
</output>
