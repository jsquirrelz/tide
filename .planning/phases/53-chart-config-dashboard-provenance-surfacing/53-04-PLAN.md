---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/dashboard/api/tasks.go
  - cmd/dashboard/api/tasks_test.go
  - cmd/dashboard/api/plans.go
  - cmd/dashboard/api/plans_test.go
  - cmd/dashboard/api/projects.go
  - cmd/dashboard/api/projects_test.go
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "The task detail payload carries the current-iteration loop summary (iteration, exit reason, last evaluation, derived loopRunId/attemptId) read straight off Task.Status.LoopStatus — never an iteration-history array (LOOP-03)"
    - "verifyMaxIterations comes from spec.verification.maxIterations and is a DIFFERENT field from attemptMax (which stays derived from Caps.Iterations) — the Phase-51 infra-vs-quality firewall holds on the wire"
    - "A Project with ConditionVerifyHalt=True lists it in blockingConditions alongside BudgetBlocked/BillingHalt"
  artifacts:
    - path: cmd/dashboard/api/tasks.go
      provides: "loop-provenance fields on taskDetail"
      contains: "loopIteration"
    - path: cmd/dashboard/api/plans.go
      provides: "plan-check LoopStatus summary on planDetail"
      contains: "loopIteration"
    - path: cmd/dashboard/api/projects.go
      provides: "blockingConditions whitelist extended with ConditionVerifyHalt"
      contains: "ConditionVerifyHalt"
  key_links:
    - from: cmd/dashboard/api/tasks.go
      to: api/v1alpha3/task_types.go
      via: "tk.Status.LoopStatus / tk.Spec.Verification projection"
      pattern: "LoopStatus"
    - from: cmd/dashboard/api/projects.go
      to: api/v1alpha3/shared_types.go
      via: "ConditionVerifyHalt constant in the whitelist filter"
      pattern: "ConditionVerifyHalt"
---

<objective>
Project the loop-provenance summary the CRDs already hold into the dashboard wire payloads
(D-07), and admit `ConditionVerifyHalt` to the project blocking-conditions whitelist (D-09
server half). Current-iteration summary ONLY — LOOP-03 means no history array exists in
etcd, by design; per-iteration depth rides findings (plan 53-03) and Phoenix (existing
link), not this payload.

Purpose: the wire contract plans 53-07/53-08 build the SPA against.
Output: extended taskDetail/planDetail structs + the 3-way condition filter, with handler
tests pinning JSON field names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-PATTERNS.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-UI-SPEC.md

<interfaces>
Wire contract fixed by this plan (SPA plans 53-07/53-08 mirror these JSON tags byte-for-byte):

taskDetail additions (cmd/dashboard/api/tasks.go):
- HasVerification     bool                `json:"hasVerification,omitempty"`   // GateCommand != "" && Phase == "Locked" (mirrors hasVerificationContract)
- LoopIteration       int32               `json:"loopIteration,omitempty"`     // Status.LoopStatus.Iteration
- VerifyMaxIterations int32               `json:"verifyMaxIterations,omitempty"` // Spec.Verification.MaxIterations — NEVER Caps.Iterations
- LoopExitReason      string              `json:"loopExitReason,omitempty"`    // Status.LoopStatus.ExitReason
- LastEvaluation      *taskLoopEvaluation `json:"lastEvaluation,omitempty"`
- LoopRunID           string              `json:"loopRunId,omitempty"`         // derived: string(tk.UID)
- AttemptID           string              `json:"attemptId,omitempty"`         // derived: fmt.Sprintf("%s-%d", tk.UID, tk.Status.Attempt)

type taskLoopEvaluation struct {
  Decision          string `json:"decision"`
  FindingsCount     int32  `json:"findingsCount"`
  HighSeverityCount int32  `json:"highSeverityCount"`
}

planDetail additions (cmd/dashboard/api/plans.go), emitted only when the plan-check loop has
run (Status.LoopStatus.Iteration > 0 || LastEvaluation != nil):
- LoopIteration       int32  `json:"loopIteration,omitempty"`
- VerifyMaxIterations int32  `json:"verifyMaxIterations,omitempty"` // plan.Spec.Verification.MaxIterations
- LoopDecision        string `json:"loopDecision,omitempty"`        // Status.LoopStatus.LastEvaluation.Decision

CRD sources (verified 2026-07-21):
- api/v1alpha3/task_types.go:232 TaskSpec.Verification (VerificationSpec, MaxIterations at :153)
- api/v1alpha3/task_types.go:343 TaskStatus.LoopStatus (value struct)
- api/v1alpha3/plan_types.go:177 PlanStatus.LoopStatus (value struct)
- api/v1alpha3/loop_types.go:100-167 LoopStatus{Iteration, LastEvaluation, ExitReason} + EvaluationSummary{Decision, FindingsCount, HighSeverityCount}
- api/v1alpha3/shared_types.go:371 ConditionVerifyHalt = "VerifyHalt"
- Loop identity derivation precedent: internal/controller/task_controller.go:2088-2089 (LoopRunID = task.UID, AttemptID = "%s-%d" of UID+attempt — re-derived, never persisted)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: taskDetail loop-provenance fields + derived identity</name>
  <files>cmd/dashboard/api/tasks.go, cmd/dashboard/api/tasks_test.go</files>
  <read_first>
    - cmd/dashboard/api/tasks.go (FULL file — the taskDetail struct at :79-96, the traceId/traceSpanId additive-field precedent + graceful-degradation doc comments, the attemptMax derivation from Caps.Iterations at :135-138 which must NOT be touched or reused, the writeJSON literal at :173-190)
    - cmd/dashboard/api/tasks_test.go (handler test fixtures — extend, don't fork)
    - api/v1alpha3/loop_types.go:100-167 (LoopStatus/EvaluationSummary field shapes)
  </read_first>
  <action>
    Add the seven taskDetail fields + the taskLoopEvaluation struct exactly as declared in
    the interfaces block. Population rules: set HasVerification when
    tk.Spec.Verification.GateCommand != "" && tk.Spec.Verification.Phase == "Locked"
    (mirror hasVerificationContract's semantics — the drawer's section-eligibility signal
    per UI-SPEC Component Contract 1). Populate the loop fields unconditionally from
    tk.Status.LoopStatus (omitempty hides zero values — the "absent = loop never ran"
    convention from traceId). LastEvaluation maps from
    tk.Status.LoopStatus.LastEvaluation (nil → omitted). Derive LoopRunID/AttemptID with
    the exact task_controller.go:2088-2089 formulas (string(tk.UID) and
    fmt.Sprintf("%s-%d", tk.UID, tk.Status.Attempt)) — re-derived, never persisted, and
    only emitted when HasVerification is true (they render inside the drawer's
    Verification section only). VerifyMaxIterations reads
    tk.Spec.Verification.MaxIterations. Do NOT modify the attemptMax block at :135-138 —
    the existing Attempt row keeps its infra-cap source (Phase-51 D-05 firewall; UI-SPEC's
    non-negotiable).

    Extend tasks_test.go: (a) a task fixture with a Locked contract
    (GateCommand="make test", MaxIterations=2), LoopStatus{Iteration: 2, ExitReason:
    "verify_exhausted", LastEvaluation: &{Decision: "REPAIRABLE", FindingsCount: 3,
    HighSeverityCount: 1}}, Caps.Iterations=5, Status.Attempt=1 → assert the JSON body
    contains "hasVerification":true, "loopIteration":2, "verifyMaxIterations":2,
    "attemptMax":5 (unchanged source), "lastEvaluation" with the three sub-fields,
    "loopRunId" == string(uid), "attemptId" == uid+"-1"; (b) a fixture with no verification
    contract → none of the new keys present in the body (omitempty + gating verified);
    (c) assert the response never contains a key matching "history" or an array of
    iterations (LOOP-03 wire-level pin).
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/api/... -run TestTask -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/dashboard/api/... -count=1` exits 0
    - `grep -c "loopIteration" cmd/dashboard/api/tasks.go` >= 1 and `grep -c "verifyMaxIterations" cmd/dashboard/api/tasks.go` >= 1
    - `grep -n "Caps.Iterations" cmd/dashboard/api/tasks.go` shows the attemptMax block unchanged (still the only consumer, still feeding attemptMax)
    - Test (b) proves a contract-less task emits zero new keys; test (c) pins no-history
  </acceptance_criteria>
  <done>Task wire payload carries the D-07 summary with the infra/quality firewall intact and identity derived by the controller's exact formula.</done>
</task>

<task type="auto">
  <name>Task 2: planDetail loop summary + ConditionVerifyHalt in blockingConditions</name>
  <files>cmd/dashboard/api/plans.go, cmd/dashboard/api/plans_test.go, cmd/dashboard/api/projects.go, cmd/dashboard/api/projects_test.go</files>
  <read_first>
    - cmd/dashboard/api/plans.go (planDetail struct at :66-73 + its Get handler population site)
    - cmd/dashboard/api/projects.go:365-395 (summarize()'s whitelist loop — the != && != filter and the make([]projectCondition, 0, 2) pre-allocation)
    - cmd/dashboard/api/projects_test.go and plans_test.go (fixture idioms to extend)
    - api/v1alpha3/plan_types.go:167-177 (PlanStatus.LoopStatus value struct)
  </read_first>
  <action>
    plans.go: add the three planDetail fields from the interfaces block, populated only when
    plan.Status.LoopStatus.Iteration > 0 || plan.Status.LoopStatus.LastEvaluation != nil
    (zero-value LoopStatus = plan-check never ran → all three omitted; the UI-SPEC plan-line
    eligibility rule "absence renders nothing"). VerifyMaxIterations reads
    plan.Spec.Verification.MaxIterations; LoopDecision reads
    LastEvaluation.Decision (nil-safe: empty when nil).

    projects.go: extend summarize()'s filter from the 2-way `c.Type !=
    ConditionBudgetBlocked && c.Type != ConditionBillingHalt` to a 3-way chain adding
    `tidev1alpha3.ConditionVerifyHalt`, and bump `make([]projectCondition, 0, 2)` to
    `0, 3`. Update the whitelist doc comment to name all three. This is the ENTIRE D-09
    server half — the frontend's open-string wire shape means the badge renders as soon as
    the table row (plan 53-07) exists, in either deploy order. Do NOT add
    ConditionFailureHalt — CONTEXT explicitly defers that gap (not OBS-04 scope; UI-SPEC
    neither requires nor styles it); if desired later it is a todo, not this phase.

    Tests: plans_test.go — a plan fixture with LoopStatus{Iteration:1, LastEvaluation:
    &{Decision:"APPROVED"}} + Spec.Verification.MaxIterations=1 asserts the three JSON keys;
    a zero-LoopStatus fixture asserts all three absent. projects_test.go — a project with
    Conditions [{Type:"VerifyHalt", Status:True, Reason:"VerifyExhausted", Message:"..."}]
    asserts blockingConditions contains type "VerifyHalt" with the message verbatim; a
    False-status VerifyHalt condition asserts exclusion (Status==True filter preserved).
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/api/... -run 'TestPlan|TestProject|TestSummarize' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/dashboard/api/... -count=1` exits 0
    - `grep -c "ConditionVerifyHalt" cmd/dashboard/api/projects.go` >= 1 and `grep -c "0, 3" cmd/dashboard/api/projects.go` == 1
    - `grep -c "ConditionFailureHalt" cmd/dashboard/api/projects.go` == 0 (deferred gap NOT folded in)
    - plans_test zero-LoopStatus fixture proves key absence (eligibility rule on the wire)
  </acceptance_criteria>
  <done>Plan surface has its minimal LoopStatus summary; VerifyHalt reaches the project payload as a blocking condition; the deferred FailureHalt gap stays deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRD status → dashboard JSON/SSE payloads | controller-authored status crosses to browser clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Information Disclosure | new taskDetail/planDetail fields | mitigate | fields are bounded LoopStatus summary scalars + derived UIDs already exposed elsewhere in the payload (task UID is the resource identity); no secret material, no findings CONTENT, no env/credential fields — test (c) pins that no iteration-history array leaks (LOOP-03) |
| T-53-09 | Tampering | condition whitelist loosening | mitigate | whitelist stays a literal !=-chain (3 literals); Status==True filter preserved and tested |
| T-53-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test ./cmd/dashboard/api/... -count=1` green
- `make lint` clean
</verification>

<success_criteria>
- Wire contract for the SPA is fixed, tested, and byte-named for 53-07/53-08 to mirror
- attemptMax vs verifyMaxIterations firewall proven at the payload layer
- VerifyHalt is a first-class blocking condition on the project payload
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-04-SUMMARY.md` when done.
</output>
