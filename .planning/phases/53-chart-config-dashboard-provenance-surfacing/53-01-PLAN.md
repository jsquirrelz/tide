---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hack/helm/tide-values.yaml
  - hack/helm/augment-tide-chart.sh
  - charts/tide/values.yaml
  - charts/tide/templates/deployment.yaml
  - test/integration/kind/verify_chart_config_test.go
  - test/integration/kind/suite_test.go
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "helm template renders TIDE_VERIFIER_IMAGE on the manager container from images.tideLanggraphVerifier values"
    - "The kind suite pins the verifier image via --set images.tideLanggraphVerifier.tag=test with no post-install kubectl set env patch"
    - "make verify-chart-reproducible passes — committed charts/tide/ matches a fresh hack/helm/ regeneration"
  artifacts:
    - path: hack/helm/tide-values.yaml
      provides: "images.tideLanggraphVerifier {repository,tag,pullPolicy} block"
      contains: "tideLanggraphVerifier"
    - path: charts/tide/templates/deployment.yaml
      provides: "TIDE_VERIFIER_IMAGE env injection (regenerated output)"
      contains: "TIDE_VERIFIER_IMAGE"
    - path: test/integration/kind/verify_chart_config_test.go
      provides: "static chart-contract test for the verifier env"
      contains: "TestHelmDeploymentTemplateRendersVerifierEnv"
  key_links:
    - from: hack/helm/augment-tide-chart.sh
      to: charts/tide/templates/deployment.yaml
      via: "phase53-verifier-image-env-injected marker block"
      pattern: "phase53-verifier-image-env-injected"
    - from: charts/tide/templates/deployment.yaml
      to: cmd/manager/main.go
      via: "TIDE_VERIFIER_IMAGE env consumed by envOrDefault (main.go:219)"
      pattern: "TIDE_VERIFIER_IMAGE"
---

<objective>
Close the TIDE_VERIFIER_IMAGE chart-wiring gap (RESEARCH Finding 3): the manager has read
`TIDE_VERIFIER_IMAGE` since Phase 51 (`cmd/manager/main.go:219`, compiled dev-tag default),
but `grep -c verifier charts/tide/values.yaml charts/tide/templates/deployment.yaml` is 0 —
nothing in the chart ever sets it, and the kind harness papers over it with a post-install
`kubectl set env` patch that names this phase as its owner.

Purpose: prerequisite plumbing for D-01/D-02 — the chart finally supplies the verifier image,
mirroring the existing `TIDE_REPORTER_IMAGE`/`TIDE_IMPORT_IMAGE` wiring exactly (CFG-01's
"existing precedence chain").
Output: `images.tideLanggraphVerifier` values block, the env injection in the regenerated
deployment.yaml, kind-suite image pins with the workaround retired, and a static
chart-contract test guarding against a dropped augment block.

CRITICAL constraint (RESEARCH Finding 2 / Pitfall 1): `charts/tide/values.yaml` and
`charts/tide/templates/deployment.yaml` are GENERATED by `make helm-controller` from
`hack/helm/tide-values.yaml` + `hack/helm/augment-tide-chart.sh`. Never hand-edit
`charts/tide/`; author in `hack/helm/`, regenerate, and commit both in the SAME commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-CONTEXT.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author images.tideLanggraphVerifier + TIDE_VERIFIER_IMAGE env in hack/helm/ and regenerate the chart</name>
  <files>hack/helm/tide-values.yaml, hack/helm/augment-tide-chart.sh, charts/tide/values.yaml, charts/tide/templates/deployment.yaml</files>
  <read_first>
    - hack/helm/tide-values.yaml (the images: block at ~:200-220 — tideReporter/tideImport siblings to mirror; the file is cp'd wholesale onto charts/tide/values.yaml)
    - hack/helm/augment-tide-chart.sh (the ENV9_MARKER "# phase9-reporter-env-injected" Python block at ~:316-326 — the exact idiom; the "# phase47-otlp-headers-env-injected" marker at ~:425-437 — the anchor)
    - Makefile:806-816 (helm-controller target — the regeneration command)
    - cmd/manager/main.go:213-219 (the existing TIDE_VERIFIER_IMAGE envOrDefault read this env feeds)
  </read_first>
  <action>
    In hack/helm/tide-values.yaml, add a `tideLanggraphVerifier` entry under the existing
    `images:` map as a sibling of `tideReporter`/`tideImport`, with the identical 3-key shape:
    `repository: ghcr.io/jsquirrelz/tide-langgraph-verifier`, `tag: ""`,
    `pullPolicy: IfNotPresent`. Match the surrounding comment style.

    In hack/helm/augment-tide-chart.sh's Python heredoc, add a new marker-anchored block:
    marker string `# phase53-verifier-image-env-injected`, emitting one env entry
    `TIDE_VERIFIER_IMAGE` with value
    `"{{ .Values.images.tideLanggraphVerifier.repository }}:{{ .Values.images.tideLanggraphVerifier.tag | default .Chart.AppVersion }}"`
    — byte-mirroring the ENV9_BLOCK shape (name/value/trailing marker line, idempotent
    `if MARKER not in content` guard, `re.sub` anchored with `count=1`). Anchor the insertion
    after the `# phase47-otlp-headers-env-injected` line (the most recent marker), per
    RESEARCH Finding 2's recommendation.

    Then run `make helm-controller` and verify the regenerated charts/tide/values.yaml and
    charts/tide/templates/deployment.yaml carry the new block/env. Commit hack/helm/ sources
    AND regenerated charts/tide/ output in the SAME commit (Pitfall 1 — CI's helm-lint runs
    a fresh `make helm` and diffs charts/).

    Do NOT add subagent.verify values, the model env, or the posture marker here — those land
    in plan 53-05 alongside the manager flag they depend on.
  </action>
  <verify>
    <automated>make helm-controller && git diff --exit-code charts/ && grep -c "TIDE_VERIFIER_IMAGE" charts/tide/templates/deployment.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tideLanggraphVerifier" hack/helm/tide-values.yaml` >= 1 and `grep -c "tideLanggraphVerifier" charts/tide/values.yaml` >= 1 (values regenerated)
    - `grep -c "TIDE_VERIFIER_IMAGE" charts/tide/templates/deployment.yaml` >= 1
    - `grep -c "phase53-verifier-image-env-injected" charts/tide/templates/deployment.yaml` >= 1 (marker survived regeneration)
    - After a second `make helm-controller` run, `git diff --exit-code charts/` exits 0 (idempotent — the augment block does not double-inject)
    - `helm template tide charts/tide --set images.tideLanggraphVerifier.tag=v9.9.9 2>/dev/null | grep -c "tide-langgraph-verifier:v9.9.9"` >= 1 (values flow through the render)
  </acceptance_criteria>
  <done>Chart supplies TIDE_VERIFIER_IMAGE from images.tideLanggraphVerifier; hack/helm/ source and regenerated charts/tide/ are committed together and reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Static chart-contract test + kind-suite image pin + retire the kubectl set env workaround</name>
  <files>test/integration/kind/verify_chart_config_test.go, test/integration/kind/suite_test.go</files>
  <read_first>
    - test/integration/kind/agent_identity_chart_test.go (TestHelmDeploymentTemplateRendersAgentIdentityEnv — the static-grep idiom to mirror byte-for-byte)
    - test/integration/kind/suite_test.go:475-540 (the TIDE_VERIFIER_IMAGE kubectl-set-env workaround at :487-500 to retire; helmControllerArgs and the images.tideReporter.tag=test / images.tideImport.tag=test --set pairs at :511-535 to mirror)
  </read_first>
  <action>
    Create test/integration/kind/verify_chart_config_test.go with
    `TestHelmDeploymentTemplateRendersVerifierEnv`: read
    `charts/tide/templates/deployment.yaml` via os.ReadFile (the `filepath.Join("..", "..",
    "..", "charts", "tide", ...)` path shape from agent_identity_chart_test.go) and assert
    these substrings are present: `TIDE_VERIFIER_IMAGE`,
    `.Values.images.tideLanggraphVerifier.repository`,
    `.Values.images.tideLanggraphVerifier.tag`. Failure message must name
    `make helm-controller` as the fix (mirrors the existing test's message). This is a plain
    go test (no cluster, no Ginkgo label) — it guards the exact "augment block dropped by a
    helmify regen" CI-gate class from the v1.0.8 release cascade.

    In test/integration/kind/suite_test.go: add `"--set",
    "images.tideLanggraphVerifier.tag=test"` and `"--set",
    "images.tideLanggraphVerifier.pullPolicy=IfNotPresent"` to helmControllerArgs, mirroring
    the tideReporter/tideImport pairs exactly. Then REMOVE the Phase-51 post-install
    `kubectl set env` workaround block at :487-500 (including its comment naming Phase 53) —
    the chart-set env makes it redundant, and leaving it would mask a future chart regression
    the new static test exists to catch. Preserve every other line of the install sequence.
  </action>
  <verify>
    <automated>go test ./test/integration/kind/... -run TestHelmDeploymentTemplateRendersVerifierEnv -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./test/integration/kind/... -run TestHelmDeploymentTemplateRendersVerifierEnv -count=1` exits 0
    - `grep -c "images.tideLanggraphVerifier.tag=test" test/integration/kind/suite_test.go` == 1
    - `grep -c "kubectl.*set.*env\|TIDE_VERIFIER_IMAGE=ghcr" test/integration/kind/suite_test.go` == 0 (workaround gone)
    - `go vet ./test/integration/kind/...` exits 0 (no dangling imports/vars from the removed block)
  </acceptance_criteria>
  <done>A dropped verifier-env augment block fails a fast no-cluster test; the kind suite resolves the verifier image purely through chart values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values → manager Deployment env | operator-supplied image ref crosses into the manager's dispatch config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | images.tideLanggraphVerifier values → TIDE_VERIFIER_IMAGE | accept | image-ref supply is the same trust level as every existing images.* block (tideReporter/tideImport); RBAC on values/release is the control, no new surface |
| T-53-02 | Denial of Service | dispatchVerifier with an empty/broken image | mitigate | the existing `VerifierImage == ""` benign-skip guard (task_controller.go:2178) stays untouched; chart default is non-empty |
| T-53-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `make helm-controller` then `git diff --exit-code charts/` (reproducibility — the local mirror of CI's helm-lint gate)
- `go test ./test/integration/kind/... -run TestHelmDeploymentTemplateRendersVerifierEnv -count=1`
- `make verify-chart-reproducible` (D-10, run in-phase not at release pre-flight)
</verification>

<success_criteria>
- TIDE_VERIFIER_IMAGE renders from chart values on a plain `helm template`
- The kind harness has zero post-install env patches for the verifier image
- charts/tide/ is byte-reproducible from hack/helm/ (verify-chart-reproducible green)
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-01-SUMMARY.md` when done.
</output>
