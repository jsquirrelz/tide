---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 08
type: execute
wave: 3
depends_on: ["53-07", "53-03"]
files_modified:
  - dashboard/web/src/components/TaskDetailDrawer.tsx
  - dashboard/web/src/components/TaskDetailDrawer.test.tsx
  - dashboard/web/src/App.tsx
  - cmd/dashboard/embed/dist
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "A task with a verification contract shows the Verification drawer section (iteration, verdict, findings, exit reason, loop identity); a task without one shows NO section — absence renders nothing"
    - "The Attempt row (Caps.Iterations source) and the Iteration row (spec.verification.maxIterations source) render simultaneously with independent values — the Phase-51 infra/quality firewall visible to operators"
    - "View findings toggles an in-drawer disclosure served by the EXISTING artifacts endpoint (fetchNodeArtifacts('task', …)) — no new endpoint, no navigation"
    - "VerifyHalted's actions row leads with primary Resume copying tide resume {project}; Verifying mirrors Running's arm — clipboard-copy only, dashboard stays read-only"
    - "The plan node panel shows the one-line plan-check mirror when planDetail carries LoopStatus, nothing when it doesn't"
  artifacts:
    - path: dashboard/web/src/components/TaskDetailDrawer.tsx
      provides: "Verification section + findings disclosure + Verifying/VerifyHalted action arms"
      contains: "data-testid=\"drawer-verification\""
    - path: dashboard/web/src/components/TaskDetailDrawer.test.tsx
      provides: "NEW test file — section eligibility, Attempt/Iteration firewall, action arms"
      contains: "TaskDetailDrawer"
    - path: dashboard/web/src/App.tsx
      provides: "plan-kind nodePanelContent minimal loop line"
      contains: "Plan check"
  key_links:
    - from: dashboard/web/src/components/TaskDetailDrawer.tsx
      to: dashboard/web/src/lib/api.ts
      via: "fetchNodeArtifacts(\"task\", task.name, task.projectName) for the findings disclosure"
      pattern: "fetchNodeArtifacts"
    - from: dashboard/web/src/components/TaskDetailDrawer.tsx
      to: dashboard/web/src/lib/tasks.ts
      via: "TaskDetailData loop fields (hasVerification gates the section)"
      pattern: "hasVerification"
---

<objective>
The D-07/D-08 depth surfaces, built exactly to the APPROVED 53-UI-SPEC.md: the
TaskDetailDrawer "Verification" section (Component Contract 1), the in-drawer findings
disclosure riding the existing artifacts endpoint (Contract 2), the plan-level one-line
mirror (Contract 3), and the actionsForStatus arms for Verifying/VerifyHalted
(Contract 4). Every label, copy string, spacing class, and state is LOCKED by the UI-SPEC —
drift is a spec violation; the ui-auditor points at its §Component Test Contract.

LOOP-03 boundary: the section renders the CURRENT LoopStatus summary only and links out for
history — findings via the artifacts disclosure, spans via the EXISTING PhoenixTraceLink
mount directly below the section (untouched — no second mount, no in-dashboard span
rendering, no iteration timeline).

Data-arrival contract (plan-check revision note): findings for a verdict-final task
(VerifyHalted, Succeeded, or requireApproval-parked) are staged + pushed to the run branch
by plan 53-10's trigger at the Task verdict-final seam — including while a project-wide
VerifyHalt freezes all dispatch. The disclosure's absent state ("No findings staged yet")
is therefore a TRANSIENT state (mid-iteration, or push-in-flight latency), never a
permanent one for the VerifyHalted scenario this view primarily serves. The locked UI-SPEC
copy already describes this correctly; no copy or contract change — 53-10 is a backend
runtime guarantee, not a build-time dependency of this plan (both are wave 3, zero file
overlap).

Output: drawer section + disclosure + action arms + plan line + a NEW
TaskDetailDrawer.test.tsx, with the embed regenerated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-UI-SPEC.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaskDetailDrawer Verification section + findings disclosure + action arms + NEW test file</name>
  <files>dashboard/web/src/components/TaskDetailDrawer.tsx, dashboard/web/src/components/TaskDetailDrawer.test.tsx</files>
  <read_first>
    - .planning/phases/53-chart-config-dashboard-provenance-surfacing/53-UI-SPEC.md (Component Contracts 1, 2, 4 + Copywriting Contract — the locked layout/copy/state tables; implement verbatim)
    - dashboard/web/src/components/TaskDetailDrawer.tsx (FULL file — TaskDetailData type at :46-66, actionsForStatus switch at :99-176, metadata grid at :319-345, PhoenixTraceLink mount at :347-356, the MetaRow component + border-t section-separator convention, existing data-testid naming)
    - dashboard/web/src/components/ArtifactViewer.tsx (the kind prop at :23, StatePanel state vocabulary — reuse for the disclosure states)
    - dashboard/web/src/components/NodeDetailPanel.test.tsx (the stateful-prop component-test shape for the NEW test file)
    - dashboard/web/src/lib/tasks.ts (the loop fields as threaded by 53-07 — consume, don't redeclare)
  </read_first>
  <action>
    TaskDetailDrawer.tsx — if 53-07 did not already extend TaskDetailData, add the seven
    optional loop fields (hasVerification, loopIteration, verifyMaxIterations,
    loopExitReason, lastEvaluation, loopRunId, attemptId) mirroring the traceId? optional
    convention. Insert the Verification section IMMEDIATELY after the metadata grid and
    BEFORE the PhoenixTraceLink mount, with section anatomy `border-t
    border-[var(--color-border-subtle)] px-6 py-4` and data-testid="drawer-verification".
    Eligibility: render ONLY when task.hasVerification is true; otherwise render nothing
    (no header, no placeholder). Internal layout per Contract 1, top to bottom:
    (1) header "Verification" in the MetaRow dt convention (11px uppercase 0.04em muted);
    (2) 2-col dl grid (grid-cols-2 gap-x-4 gap-y-3, 13px values) rows in order —
    Iteration "{loopIteration} of {verifyMaxIterations}" plain;
    Verdict: lastEvaluation.decision mono colored APPROVED→var(--color-status-success),
    REPAIRABLE→var(--color-status-warning), BLOCKED→var(--color-status-blocked), and when
    lastEvaluation is absent the muted non-mono "No verdict yet";
    Findings "{findingsCount} total · {highSeverityCount} high", "—" when no evaluation;
    Exit reason: loopExitReason mono, "—" when empty;
    Loop run: loopRunId mono truncate with title tooltip;
    Attempt ID: attemptId mono truncate with title tooltip;
    (3) the findings link row. The EXISTING Attempt MetaRow keeps its source and label
    untouched — do NOT move, relabel, or re-source it (the firewall).

    Findings disclosure (Contract 2): link labeled "View findings", PhoenixTraceLink's
    anchor anatomy (mono 13px underline --color-text-primary,
    hover:bg-[var(--color-surface-overlay)]) with leading ScanSearch 14px aria-hidden icon
    — NOT accent, NOT a button. Clicking toggles an inline area below (aria-expanded +
    aria-controls) that fetches via the existing fetchNodeArtifacts("task", task.name,
    task.projectName) path — either <ArtifactViewer kind="task" …/> wholesale or a lighter
    mono <pre> reusing ArtifactViewer's fetch + StatePanel states (executor's choice; the
    states table binds either way). Task-kind absent-state copy verbatim: heading "No
    findings staged yet", body "Findings land on the run branch after a verifier attempt
    completes. Check again after the current iteration finishes.", action "Check again".
    Error/no-git states reuse the existing locked ArtifactViewer copy.

    actionsForStatus (Contract 4): add explicit case arms — Verifying: destructive
    "Cancel" copying `tide cancel {project} --force`, then secondary "Tail logs (CLI)"
    copying `tide tail {task}` (mirrors Running's arm); VerifyHalted: PRIMARY "Resume"
    copying `tide resume {project}`, then secondary "Inspect wave" copying `tide
    inspect-wave {plan}`. No approve/reject buttons (mutation-free; CLI territory).

    TaskDetailDrawer.test.tsx (NEW): (a) a fixture with hasVerification true +
    lastEvaluation {decision REPAIRABLE, findingsCount 3, highSeverityCount 1} +
    loopIteration 2 / verifyMaxIterations 2 + attempt 1 / attemptMax 3 asserts BOTH rows
    render simultaneously with independent values ("1 of 3" AND "2 of 2" — the firewall
    test the UI-SPEC mandates); (b) hasVerification false/undefined asserts
    drawer-verification testid absent; (c) "No verdict yet" renders when lastEvaluation
    absent; (d) VerifyHalted status asserts the Resume button first with the exact copied
    command; Verifying asserts Cancel + Tail logs; (e) the findings link has aria-expanded
    wiring (toggle assertion with a mocked fetch).
  </action>
  <verify>
    <automated>cd dashboard/web && npx vitest run TaskDetailDrawer</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx vitest run TaskDetailDrawer` exits 0 with the five test groups (a)-(e) visible
    - `grep -c 'data-testid="drawer-verification"' dashboard/web/src/components/TaskDetailDrawer.tsx` == 1
    - `grep -c "No findings staged yet" dashboard/web/src/components/TaskDetailDrawer.tsx` (or the disclosure child component) == 1 — locked copy verbatim
    - The existing Attempt MetaRow line is byte-unchanged (`git diff` on TaskDetailDrawer.tsx shows no edit to the Attempt row)
    - `grep -c "tide resume" dashboard/web/src/components/TaskDetailDrawer.tsx` >= 1 (recovery verb taught by the primary action)
    - PhoenixTraceLink mount untouched (no second mount: `grep -c "PhoenixTraceLink" dashboard/web/src/components/TaskDetailDrawer.tsx` unchanged from before the edit)
  </acceptance_criteria>
  <done>The drawer carries the full Contract-1/2/4 depth with the firewall and every locked copy string proven by its new test file.</done>
</task>

<task type="auto">
  <name>Task 2: Plan-level one-line mirror in App.tsx + embed regeneration</name>
  <files>dashboard/web/src/App.tsx, cmd/dashboard/embed/dist</files>
  <read_first>
    - .planning/phases/53-chart-config-dashboard-provenance-surfacing/53-UI-SPEC.md (Component Contract 3 — the exact line format and eligibility)
    - dashboard/web/src/App.tsx:660-710 (the milestone/phase/plan nodePanelContent branch — PhoenixTraceLink + ArtifactViewer composition; how projectDetail refs resolve)
    - dashboard/web/src/lib/tasks.ts (useTasks()'s fetchPlan call — RESEARCH Finding 12's reuse point; how PlanDetail reaches App state)
  </read_first>
  <action>
    Per Contract 3: when selectedNode.kind === "plan", render ONE line above the existing
    ArtifactViewer in MetaRow anatomy (11px uppercase muted label + 13px value) — label
    "Plan check", value `iteration {loopIteration} of {verifyMaxIterations} ·
    {loopDecision}` with the decision substring colored by the same verdict map
    (APPROVED→success, REPAIRABLE→warning, BLOCKED→blocked). Eligibility: render nothing
    when the plan payload carries no loop fields (all undefined — absence renders
    nothing). Data source (Finding 12 — genuinely new wiring): prefer reusing the
    PlanDetail already fetched by useTasks()'s fetchPlan for the selected plan; if that
    detail isn't reachable in the plan branch's scope, add a small targeted fetch of the
    existing GET plan endpoint — never a new endpoint, never a ChildRef shape change
    server-side (53-04 already landed the payload). No plan-level findings link, no plan
    drawer section (Task is the required depth — D-08).

    Regenerate the embed (`make dashboard-frontend`), commit dist changes in the same
    commit, and confirm `make verify-dashboard-freshness` passes.
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit -p . && npx vitest run && cd ../.. && make verify-dashboard-freshness</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Plan check" dashboard/web/src/App.tsx` == 1
    - `cd dashboard/web && npx tsc --noEmit -p .` exits 0 and the full `npx vitest run` suite passes
    - `make verify-dashboard-freshness` exits 0 (embed fresh with both this and Task 1's edits)
    - No new fetch helper hits a non-existent route: any fetch added resolves to the existing plan endpoint already typed in lib/api.ts
  </acceptance_criteria>
  <done>Plan-check provenance has its minimal locked mirror; the shipped embed contains the entire OBS-04 SPA surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifacts endpoint → in-drawer render | verifier-authored findings.json content reaches the DOM through the disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-18 | Information Disclosure | findings content in the drawer | accept | same content class the ArtifactViewer already renders for 4 kinds under the same access model; no secret material by construction (verifier is read-only, credential-free — Phase 48 D-08/D-09) |
| T-53-19 | Tampering (XSS) | findings.json rendered in the disclosure | mitigate | render as text through React's default escaping (ArtifactViewer/mono pre path) — never dangerouslySetInnerHTML; the existing ArtifactViewer path already satisfies this |
| T-53-20 | Elevation of Privilege | dashboard mutating VerifyHalt state | mitigate | actions are clipboard-copy only (Contract 4) — nothing executes; no approve/resume API call exists to reach |
| T-53-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- `cd dashboard/web && npx vitest run` full suite green
- `make verify-dashboard-freshness` green (D-10)
- Optional operator eyeball (VALIDATION.md manual row): deploy on kind, open a Task with loop iterations, confirm the drawer + badges render legibly — deferred to phase verification, not a plan gate
</verification>

<success_criteria>
- All five must_have truths proven by the new test file + full-suite green
- Embed regenerated; UI-SPEC copy verbatim throughout (checker can diff §Copywriting Contract)
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-08-SUMMARY.md` when done.
</output>
