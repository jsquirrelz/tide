---
phase: 53
slug: chart-config-dashboard-provenance-surfacing
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-21
---

# Phase 53 — UI Design Contract

> Visual and interaction contract for OBS-04 (dashboard loop provenance + distinct VerifyHalt).
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Scope guard:** CFG-01/CFG-02 (chart config) carry NO UI surface and are not covered here.
> The dashboard stays **read-only** — every "action" is a clipboard-copy CLI command, never a
> mutation. NO new top-level views, NO new endpoints, NO restyle of the existing system.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (hand-rolled, locked since 04-UI-SPEC; extended by 14/15/46-UI-SPEC lineage) |
| Preset | not applicable |
| Component library | none — bespoke React 18 + TypeScript components with locked vocabulary tables |
| Icon library | lucide-react `^1.16.0` (already installed — zero new npm packages this phase) |
| Font | system stack only: `--font-sans` / `--font-mono` (zero web-font bytes) |
| Styling | Tailwind v4 CSS-first — all tokens live in `dashboard/web/src/index.css` `@theme` |
| Theme | dark default, light opt-in via `.light-theme` — new work MUST use `var(--*)` tokens so both themes inherit for free |

**This phase is additive-only.** It adds rows to two existing vocabulary tables, one drawer
section, one plan-panel line, and new `actionsForStatus` arms. It introduces zero new tokens,
zero new palettes, zero new typefaces. Per the project CLAUDE.md Opus-4.8 note: never drift
toward cream/serif/terracotta editorial defaults — this is a dev-tool dashboard and its
palette is already declared below.

---

## Spacing Scale

Existing locked scale (`index.css` `--spacing-*`, 4px base) — reused verbatim, no additions:

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| `--spacing-1` | 4px | Badge internal gap (`gap-1`), MetaRow label/value gap (`gap-0.5` = 2px is a pre-existing declared exception) |
| `--spacing-2` | 8px | Action-button stack gap (`gap-2`) |
| `--spacing-3` | 12px | Drawer status-row gap (`gap-3`), metadata grid `gap-y-3` |
| `--spacing-4` | 16px | Drawer section vertical padding (`py-4`), metadata grid `gap-x-4` |
| `--spacing-6` | 24px | Drawer section horizontal padding (`px-6`) |
| `--spacing-8` | 32px | (layout gaps — not touched this phase) |
| `--spacing-12` | 48px | (major section breaks — not touched) |
| `--spacing-16` | 64px | (page-level — not touched) |

**Contract for the new Verification drawer section:** identical section anatomy to every
existing drawer section — `border-t border-[var(--color-border-subtle)] px-6 py-4`; internal
grid `grid-cols-2 gap-x-4 gap-y-3`. No new spacing values.

Exceptions: none new. (Pre-existing: MetaRow `gap-0.5`/2px micro-gap — reuse, don't extend.)

---

## Typography

Existing locked set (`index.css` `@theme`) — 3 sizes + 1 mono variant, 2 weights. Reused
verbatim:

| Role | Size | Weight | Line Height | Used here for |
|------|------|--------|-------------|---------------|
| Body | 14px (`--text-body`) | 400 | 1.5 | (default shell — not directly used in new work) |
| Label | 12px (`--text-label`) | 600 (badges) | 1.4 | StatusBadge/ConditionBadge pill text (mono) |
| Heading | 18px (`--text-heading`) | 600 | 1.3 | (drawer title — existing, untouched) |
| Mono | 13px (`--text-mono`) | 400 | 1.4 | Metadata values, verdict line, links, IDs |

Pre-existing declared exception, reused not extended: MetaRow `<dt>` micro-label — 11px,
uppercase, `letterSpacing: 0.04em`, `--color-text-muted`. The new "Verification" section
header and all its field labels use exactly this convention.

Weights: 400 + 600 only. No new sizes, no new weights.

---

## Color

Existing locked 60/30/10 split (`index.css`) — dark values shown; light theme inherits via
token overrides automatically:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0b0f14` (`--color-surface-base`) | Background |
| Secondary (30%) | `#161b22` / `#1f2937` (`--color-surface-raised` / `-overlay`) | Drawer, cards, hover |
| Accent (10%) | `#06b6d4` (`--color-accent`) | See reserved list — NOTHING in this phase uses accent |
| Destructive | `#f85149` (`--color-destructive`) | Destructive action buttons only (existing Cancel) |

Accent reserved for (locked 6-item list, `index.css` header — unchanged): primary CTA button
background · selected-node ring · active wave-band edge highlight · SSE-connected dot ·
focus ring · connected-status pill. **The new findings link, verification section, and both
new badges use ZERO accent.**

### Status-family tokens (the load-bearing contract for OBS-04)

| Token | Dark | Light | This phase assigns to |
|-------|------|-------|-----------------------|
| `--color-status-running` | `#06b6d4` | `#0891b2` | `Verifying` (running-family: active work) |
| `--color-status-blocked` | `#a371f7` | `#9333ea` | `VerifyHalted` status + `VerifyHalt` condition (policy-halted, operator-recoverable — same family as BudgetBlocked/BillingHalt/PushLeakBlocked) |
| `--color-status-error` | `#f85149` | `#dc2626` | `Failed` (existing — the token VerifyHalted must NOT share) |
| `--color-status-success` | `#3fb950` | `#16a34a` | Verdict text `APPROVED` |
| `--color-status-warning` | `#d29922` | `#ca8a04` | Verdict text `REPAIRABLE` |

**Distinctness contract (OBS-04 "visually distinct from Failed", executable):**
`VerifyHalted` differs from `Failed` on all three axes — color token
(`--color-status-blocked` ≠ `--color-status-error`), lucide glyph (`ShieldBan` ≠ `CircleX`),
and label (`Verify halted` ≠ `Failed`). §Component Test Contract below pins the assertions.

---

## Status Vocabulary — two new STATUS_TABLE rows

`dashboard/web/src/components/StatusBadge.tsx` — extend the `StatusValue` union and
`STATUS_TABLE` with exactly these rows (verbatim; any divergence is a UI-SPEC violation).
Wire strings match `api/v1alpha3/shared_types.go` `LevelPhaseVerifying`/`LevelPhaseVerifyHalted`.

| StatusValue | lucide Icon | Label | Color token | Animation | SR Description |
|-------------|-------------|-------|-------------|-----------|----------------|
| `Verifying` | `SearchCheck` | `Verifying` | `var(--color-status-running)` | `animate-pulse` | `Verifying — independent evaluator reviewing this attempt` |
| `VerifyHalted` | `ShieldBan` | `Verify halted` | `var(--color-status-blocked)` | none | `Verify halted — verification ended without an approved verdict. Review findings and run \`tide resume\` to advance` |

```typescript
Verifying: {
  icon: SearchCheck,
  iconName: "SearchCheck",
  label: "Verifying",
  colorVar: "var(--color-status-running)",
  srDescription: "Verifying — independent evaluator reviewing this attempt",
  animationClass: "animate-pulse",
},
VerifyHalted: {
  icon: ShieldBan,
  iconName: "ShieldBan",
  label: "Verify halted",
  colorVar: "var(--color-status-blocked)",
  srDescription:
    "Verify halted — verification ended without an approved verdict. Review findings and run `tide resume` to advance",
},
```

- Both icons verified importable from the installed `lucide-react@^1.16.0` (Finding 13 +
  re-verified this session via `node_modules` listing).
- `ShieldBan` deliberately ≠ `PushLeakBlocked`'s `ShieldAlert` — the color-blindness rule in
  StatusBadge's own `Complete`-vs-`Succeeded` comment: two same-family badges never share a glyph.
- `KNOWN_STATUS_VALUES` / `coerceStatus` auto-propagate from `Object.keys(STATUS_TABLE)` —
  adding the rows is the ONLY frontend vocabulary change needed (Finding 11); `TideNodeShell`
  and both DAG views inherit automatically.
- Pill anatomy is owned by `StatusBadge` (15% tint bg, 40% border, mono 12px/600) — new rows
  inherit it; do not restyle.

---

## Condition Vocabulary — one new CONDITION_TABLE row

`dashboard/web/src/components/ConditionBadge.tsx` — add one row (verbatim):

| Condition type | lucide Icon | Label | Color token | SR Description |
|----------------|-------------|-------|-------------|----------------|
| `VerifyHalt` | `OctagonPause` | `Verify halted` | `var(--color-status-blocked)` | `Verification halted without an approved verdict — dispatch held. Review staged findings and run \`tide resume\`` |

```typescript
VerifyHalt: {
  icon: OctagonPause,
  iconName: "OctagonPause",
  label: "Verify halted",
  colorVar: "var(--color-status-blocked)",
  srDescription:
    "Verification halted without an approved verdict — dispatch held. Review staged findings and run `tide resume`",
},
```

- `OctagonPause` (verified available) — NOT `ShieldBan`: the condition badge (project-level)
  and the `VerifyHalted` status badge (task-level) can co-occur in one viewport, and the
  blocked family already carries the no-shared-glyph rule (Wallet / CreditCard / ShieldAlert
  are all distinct). Octagon = stop-sign semantics fits "dispatch held".
- SR mirrors `BillingHalt`'s shape exactly: *cause — halt statement. recovery action naming
  `tide resume`.* The badge teaches the fix.
- `title` attribute continues to carry the controller-stamped `message` verbatim (never
  paraphrased) — the existing T-14-07-01 contract, inherited, not re-decided.

**Server side (lands with the row):** `cmd/dashboard/api/projects.go:386` blocking-conditions
filter admits `ConditionVerifyHalt` (`shared_types.go:371`); bump the `make([]projectCondition, 0, 2)`
pre-allocation to 3. Wire shape stays an open string — unknown types render nothing, so
server and table can land in either order without breaking rendering.

**Out of contract:** the observed `FailureHalt` filter gap is NOT part of this contract
(CONTEXT §Deferred — not OBS-04). If the planner opts to close it in the same edit, it must
follow this same row anatomy (blocked-family token, unique glyph); this spec neither requires
nor styles it.

---

## Component Contract 1 — TaskDetailDrawer "Verification" section

**Placement:** new section in `TaskDetailDrawer.tsx` immediately AFTER the metadata grid and
BEFORE the `PhoenixTraceLink` mount. Section anatomy: `border-t border-[var(--color-border-subtle)]
px-6 py-4` (the existing drawer section-separator convention).

**Eligibility (owned by the section, PhoenixTraceLink precedent — mount point never
re-decides):** renders ONLY when the task payload carries a verification contract (new
payload fields present/non-null). A task with no verification contract shows no section at
all — absence renders nothing, zero noise.

**Internal layout, top to bottom:**

1. **Section header** — text `Verification`, MetaRow `<dt>` convention: 11px, uppercase,
   `letterSpacing: 0.04em`, `--color-text-muted`.
2. **2-column `<dl>` grid** (`grid-cols-2 gap-x-4 gap-y-3`, values 13px), rows in order:

   | Label (dt) | Value (dd) | Format |
   |------------|-----------|--------|
   | `Iteration` | `{LoopStatus.Iteration} of {spec.verification.maxIterations}` | plain text (matches existing "Attempt" row format) |
   | `Verdict` | `LastEvaluation.Decision` | mono, colored: `APPROVED` → `var(--color-status-success)`, `REPAIRABLE` → `var(--color-status-warning)`, `BLOCKED` → `var(--color-status-blocked)`; when `LastEvaluation` is nil → `No verdict yet` in `--color-text-muted` (not colored, not mono) |
   | `Findings` | `{FindingsCount} total · {HighSeverityCount} high` | plain; `—` when no evaluation yet |
   | `Exit reason` | `LoopStatus.ExitReason` | mono; `—` when empty (loop still open) |
   | `Loop run` | derived `loopRunID` | mono, `truncate`, `title` tooltip = full ID (Envelope-path row precedent) |
   | `Attempt ID` | derived `attemptID` | mono, `truncate`, `title` tooltip = full ID |

3. **Findings link row** — see Component Contract 2.

**The attemptMax/maxIterations firewall (Phase-51 D-05 distinction, non-negotiable):**

- The EXISTING metadata-grid row `Attempt — {attempt} of {attemptMax}` keeps deriving from
  `Caps.Iterations` (infra-retry cap). It is NOT moved, NOT relabeled, NOT re-sourced.
- The NEW `Iteration — N of M` row derives from `LoopStatus.Iteration` /
  `spec.verification.maxIterations` (quality-iteration bound).
- Two rows, two labels (`Attempt` vs `Iteration`), two data sources. They MUST never be
  merged, cross-wired, or displayed as one number. A drawer test asserts both rows render
  simultaneously with independent values (e.g. Attempt `1 of 3` alongside Iteration `2 of 2`).

**Phoenix deep-link:** the existing `PhoenixTraceLink` mount (directly below this section) IS
the span-level provenance surface — Project run → level → attempt → tool spans live in
Phoenix, reached via the existing link. No second mount, no change to `PhoenixTraceLink`,
no in-dashboard span rendering. The dashboard shows the current-iteration summary and links
out for history (LOOP-03: no iteration-history timeline exists in CRD status, by design —
do NOT build one).

---

## Component Contract 2 — Findings browsing (existing ArtifactViewer, existing endpoint)

**Trigger:** link labeled `View findings` at the bottom of the Verification section.
Anchor anatomy mirrors `PhoenixTraceLink`'s (the locked link style): mono 13px, underline,
`--color-text-primary`, `hover:bg-[var(--color-surface-overlay)]`, with a leading
`ScanSearch` 14px icon (`aria-hidden`). NOT accent, NOT a button.

**Interaction:** in-drawer disclosure — clicking toggles an inline area below the link that
renders the task's staged `findings.json` from the EXISTING
`GET /api/v1/nodes/task/{name}/artifacts` gitfetch endpoint (`fetchNodeArtifacts("task", …)`
— `ArtifactViewer`'s `kind` prop is already an open `string`, Finding 14). The trigger
carries `aria-expanded` + `aria-controls`. No navigation away, no new route, no new endpoint.
Reusing `<ArtifactViewer kind="task" …/>` wholesale inside the disclosure is the sanctioned
implementation; a lighter single-file mono `<pre>` render reusing ArtifactViewer's fetch +
StatePanel states is equally acceptable — executor's choice, states contract below binds
either way.

**States (reuse ArtifactViewer's StatePanel vocabulary):**

| State | Copy |
|-------|------|
| available | findings.json content rendered (mono) |
| absent (task kind — new copy) | heading `No findings staged yet` · body `Findings land on the run branch after a verifier attempt completes. Check again after the current iteration finishes.` · action `Check again` |
| error | existing locked copy verbatim: `Couldn't fetch artifacts` / `The dashboard could not read the run branch. Check the dashboard logs: kubectl logs deploy/tide-dashboard` · action `Retry` |
| no-git | existing locked copy verbatim (unchanged) |

---

## Component Contract 3 — Plan-level mirror (minimal line)

Per D-08 (discretion resolved to the minimal shape): the Plan detail surface
(`App.tsx` `nodePanelContent`, plan branch) gains ONE line rendered above the existing
`ArtifactViewer`, using MetaRow anatomy (11px uppercase muted label + 13px value):

- Label: `Plan check`
- Value: `iteration {LoopStatus.Iteration} of {maxIterations} · {LastEvaluation.Decision}` —
  decision substring colored per the Verdict color map above; when the plan payload carries
  no `LoopStatus`, the line renders nothing (absence = nothing, same eligibility rule).

No plan-level findings link, no plan drawer section this phase — Task is the required depth.

---

## Component Contract 4 — actionsForStatus arms (drawer Actions row)

`TaskDetailDrawer.tsx` `actionsForStatus` currently falls through to `[]` for both new
statuses (Finding 11). Add two explicit case arms — clipboard-copy commands only (dashboard
stays read-only; recovery is CLI-only):

| Status | Buttons (variant · label · command) |
|--------|-------------------------------------|
| `Verifying` | destructive · `Cancel` · `tide cancel {project} --force` — then — secondary · `Tail logs (CLI)` · `tide tail {task}` (mirrors `Running`'s arm) |
| `VerifyHalted` | **primary · `Resume` · `tide resume {project}`** — then — secondary · `Inspect wave` · `tide inspect-wave {plan}` |

`Resume` is the primary (top) button for `VerifyHalted` — the button, the status SR, and the
condition SR all teach the same recovery verb (`tide resume`), mirroring `Paused`/`Rejected`/
`BillingHalt`. No approve/reject buttons for VerifyHalted (mutation-free surface; the
approve-an-exhausted-loop flow is CLI territory).

---

## Component Test Contract (the OBS-04 "visually distinct" criterion, executable)

Extend `StatusBadge.test.tsx` (existing table-driven idiom) with assertions the ui-auditor
and verifier can point at:

```typescript
// Distinctness on all three axes — VerifyHalted vs Failed:
expect(STATUS_TABLE.VerifyHalted.colorVar).toBe("var(--color-status-blocked)");
expect(STATUS_TABLE.Failed.colorVar).toBe("var(--color-status-error)");
expect(STATUS_TABLE.VerifyHalted.colorVar).not.toBe(STATUS_TABLE.Failed.colorVar);
expect(STATUS_TABLE.VerifyHalted.iconName).not.toBe(STATUS_TABLE.Failed.iconName); // ShieldBan vs CircleX
expect(STATUS_TABLE.VerifyHalted.label).not.toBe(STATUS_TABLE.Failed.label);       // "Verify halted" vs "Failed"
```

Plus rendered-DOM assertions via the existing test ids (`status-badge-VerifyHalted`
`data-icon="ShieldBan"`; `Verifying` badge carries `animate-pulse`). `ConditionBadge.test.tsx`
gains the `VerifyHalt` row test (renders; unknown types still render null). The drawer test
asserts the Attempt/Iteration firewall (both rows, independent values) and that a
no-contract task renders no Verification section.

---

## Copywriting Contract

Voice: existing terse dev-tool register — mono for machine terms, sentence case, em-dash
cause/recovery pairs, every halt teaches its CLI recovery verb. All copy below is LOCKED
verbatim (drift = spec violation).

| Element | Copy |
|---------|------|
| Primary CTA (`VerifyHalted` actions row) | `Resume` → copies `tide resume {project}` |
| Section header | `Verification` |
| Findings link | `View findings` |
| Empty state — no verification contract | (section hidden entirely — absence renders nothing) |
| Empty state — findings not yet staged | `No findings staged yet` / `Findings land on the run branch after a verifier attempt completes. Check again after the current iteration finishes.` / action `Check again` |
| No-verdict placeholder | `No verdict yet` |
| Error state — findings fetch | existing locked ArtifactViewer copy verbatim (`Couldn't fetch artifacts` …) |
| `Verifying` SR | `Verifying — independent evaluator reviewing this attempt` |
| `VerifyHalted` SR | `Verify halted — verification ended without an approved verdict. Review findings and run \`tide resume\` to advance` |
| `VerifyHalt` condition SR | `Verification halted without an approved verdict — dispatch held. Review staged findings and run \`tide resume\`` |
| Destructive confirmation | none needed — dashboard is clipboard-copy only; nothing executes. The only destructive-variant button is the existing `Cancel` (`tide cancel {project} --force`), reused verbatim |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized (established hand-rolled system) | not applicable |
| third-party | none | not applicable |

No new npm/Go packages. `lucide-react` icons used (`SearchCheck`, `ShieldBan`,
`OctagonPause`, `ScanSearch`) verified present in the installed `^1.16.0` — 2026-07-21.

---

## Build-Gate Reminder (D-10)

Any SPA edit in this phase MUST regenerate `cmd/dashboard/embed/dist` and pass
`make verify-dashboard-freshness` (Phase-22 gate) inside phase verification, alongside
`make lint` and the kind chart-contract tests — not at release pre-flight.

---

## Auto-Resolved Decision Log (`--auto` mode — no interactive user)

| # | Question | Resolution | Grounding |
|---|----------|------------|-----------|
| 1 | shadcn init gate | Skipped — locked hand-rolled design system exists (04-UI-SPEC lineage; `components.json` absent by design, verified) | Codebase scout; constraints "design system is ESTABLISHED and locked" |
| 2 | `Verifying` icon/animation | `SearchCheck` + `animate-pulse`, running-family | RESEARCH Finding 13 recommendation; running-family per constraints |
| 3 | `VerifyHalted` icon | `ShieldBan` (≠ `ShieldAlert`, ≠ `CircleX`) | Finding 13; StatusBadge's own no-shared-glyph rule |
| 4 | `VerifyHalt` condition icon | `OctagonPause` — NOT `ShieldBan`, so the co-occurring task badge and project condition badge (both blocked-purple) never share a glyph | Blocked-family precedent (Wallet/CreditCard/ShieldAlert all unique) |
| 5 | Labels | `Verifying` / `Verify halted` (both surfaces) | CONTEXT D-09 ("Verify halted") |
| 6 | Plan provenance depth | Minimal one-line mirror (not a full section) | CONTEXT D-07/D-08 discretion — resolved to the minimal shape |
| 7 | Verification section placement | After metadata grid, before PhoenixTraceLink | Adjacency to the metadata it extends; Phoenix link stays the section's history exit |
| 8 | Findings display mechanism | In-drawer disclosure reusing `fetchNodeArtifacts("task", …)` + ArtifactViewer state vocabulary; full `ArtifactViewer` reuse sanctioned | Finding 14 (untyped `kind` prop); OBS-04 no-new-endpoint |
| 9 | `actionsForStatus` arms | Added for both new statuses (Verifying mirrors Running; VerifyHalted leads with Resume) | Finding 11 companion-fix flag; recovery-verb constraint |
| 10 | `FailureHalt` filter gap | Left out of this contract (deferred, not OBS-04); row anatomy noted if planner opts in | CONTEXT §Deferred |
| 11 | Loop identity display | `loopRunID`/`attemptID` as truncated mono MetaRows with title tooltips | D-07 payload fields; Envelope-path row precedent |
| 12 | Verdict color mapping | APPROVED→success, REPAIRABLE→warning, BLOCKED→blocked (never error — halt-class rule) | v1.0.9 binding constraint: VerifyHalt is a distinct halt class, not a Failed reinterpretation |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
