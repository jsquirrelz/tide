---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/artifact_push.go
  - internal/controller/artifact_push_test.go
  - cmd/dashboard/api/artifacts.go
  - cmd/dashboard/api/artifacts_test.go
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "A verdict-final Task with a recorded evaluation (VerifyHalted, Succeeded, or requireApproval-parked AwaitingApproval — all with LoopStatus.LastEvaluation set) produces a task-kind stage-envelope entry so its findings.json lands on the run branch"
    - "A task with NO recorded evaluation is NEVER staged — including a VerifyHalted task whose verifier crashed before writing findings.json (an entry without findings.json hard-fails the ENTIRE cumulative push in tide-push)"
    - "GET /api/v1/nodes/task/{name}/artifacts is no longer rejected as an invalid kind (allowlist admits task)"
    - "The allowlist stays a closed literal map — no regex, no open string (T-37-07-04 preserved)"
  artifacts:
    - path: internal/controller/artifact_push.go
      provides: "collectStageEnvelopes emits task-kind entries for verdict-final tasks with a recorded evaluation"
      contains: "entry{\"task\""
    - path: cmd/dashboard/api/artifacts.go
      provides: "artifactKinds allowlist including task + corrected 400 message"
      contains: "\"task\":"
  key_links:
    - from: internal/controller/artifact_push.go
      to: cmd/tide-push/main.go
      via: "entry{\"task\", name, uid} consumed by tide-push's existing kind==task findings.json staging (Phase 49, unchanged)"
      pattern: "entry\\{\"task\""
    - from: cmd/dashboard/api/artifacts.go
      to: dashboard/web/src/lib/api.ts
      via: "fetchNodeArtifacts(\"task\", ...) now passes the kind allowlist"
      pattern: "\"task\""
---

<objective>
Close the two findings-staging gaps RESEARCH Finding 10 verified (both required for D-07's
"findings browsable through the existing gitfetch/artifacts API" to be literally true, and
both explicitly additive — OBS-04 forbids a new endpoint, and none is added):

1. Controller side: `collectStageEnvelopes` (internal/controller/artifact_push.go:84-147)
   emits only milestone/phase/plan/project entries — nothing has ever produced a `task`
   entry (deferred in Phase 49 with a doc comment naming Phase 51; never closed).
2. Dashboard side: `artifactKinds` (cmd/dashboard/api/artifacts.go:55-60) 400s on
   `kind=task`.

The tide-push CLI half (cmd/tide-push/main.go:1233-1256) is already correct — kind=="task"
stages a single findings.json at .tide/planning/task/<name>/findings.json, fail-closed if
the file is absent under an existing dir. DO NOT touch cmd/tide-push.

Scope split (plan-check revision): this plan ships the COLLECTOR PREDICATE + the allowlist
admission. The trigger that FIRES a push at the Task verdict-final seam — required because
checkVerifyHalt freezes every other level's boundary push while a VerifyHalt is active —
is plan 53-10 (same phase, wave 3), which builds directly on this plan's predicate.

Purpose: the dashboard findings link (plan 53-08) renders real data instead of a
silently-forever-absent state (RESEARCH Pitfall 4).
Output: task entries in the staging collector (verdict-final + evaluation-guarded
predicate, superseding RESEARCH Assumption A4's unguarded VerifyHalted arm), the allowlist
admission, and unit coverage for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-RESEARCH.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: collectStageEnvelopes emits task-kind entries for verdict-final tasks with a recorded evaluation</name>
  <files>internal/controller/artifact_push.go, internal/controller/artifact_push_test.go</files>
  <read_first>
    - internal/controller/artifact_push.go (FULL file — the milestone/phase/plan entry loops at :94-125, the entry struct + DestPrefix/uid formatting at :135-147, and triggerArtifactPush at :179)
    - internal/controller/push_helpers.go:130-170 (the cumulative-map STRICT-SUPERSET push-trigger logic — how a newly-eligible task entry supersedes a stale partial-map push)
    - cmd/tide-push/main.go:1215-1256 (the kind=="task" findings.json staging consumer — note BOTH behaviors: an ABSENT envelope dir is soft-skipped, but a dir that EXISTS without findings.json hard-fails the ENTIRE push; DO NOT MODIFY)
    - internal/controller/artifact_push_test.go (existing collectStageEnvelopes coverage — the fixture/assertion idiom to extend)
    - api/v1alpha3/task_types.go:337-343 (Task.Status.LoopStatus) and api/v1alpha3/shared_types.go:518-536 (LevelPhaseVerifying/LevelPhaseVerifyHalted constants)
  </read_first>
  <action>
    In collectStageEnvelopes, add a Task list+filter loop after the plan loop, mirroring the
    existing shape (List in-namespace, iterate by index, append
    `entry{"task", t.Name, string(t.UID)}`).

    Predicate — verdict-final AND evaluation-recorded (do NOT reuse plannerMaterialized,
    whose planner phases don't fit Task vocabulary; supersedes RESEARCH Assumption A4's
    unguarded VerifyHalted arm): include a Task when
    `t.Status.LoopStatus.LastEvaluation != nil` AND `t.Status.Phase` is one of:
      - `LevelPhaseVerifyHalted` (escalate-leg halt),
      - `LevelPhaseSucceeded` (approved verdict-final),
      - `LevelPhaseAwaitingApproval` (the ESC-02 requireApproval park — verdict-final, the
        operator needs the findings to decide `tide approve`; mirrors plannerMaterialized
        already admitting AwaitingApproval for the other four kinds).
    The LastEvaluation guard applies to ALL arms, VerifyHalted included: a VerifyHalted
    task whose verifier crashed before writing findings.json (VerifierEnvelopeUnreadable /
    VerifierVerdictMissing) has a PVC envelope dir (the executor's out.json created it) but
    no findings.json — and tide-push hard-fails the ENTIRE cumulative push on that shape
    (cmd/tide-push/main.go:1242-1252), poisoning every other level's artifacts. A parsed
    verdict (LastEvaluation set via applyLoopStatus) is the presence proxy: the controller
    only records it after reading the verifier's envelope. Verifying/Running tasks are
    excluded (never stage mid-iteration). A pre-verify Succeeded task (nil LastEvaluation)
    is excluded.

    Two verification obligations (read-first evidence, then act):
    (1) uid→srcDir: the entry's uid must resolve to the directory the verifier's
    findings.json actually lands in (the envelopes/<taskUID>/ PVC dir — Phase 51 mounts RW
    envelopes/<uid>/ for the verifier; the verifier Job runs against the TASK's uid). If the
    observed findings path differs, fix the ENTRY construction (not tide-push) and cite the
    observed path in the summary.
    (2) LastEvaluation-as-presence-proxy: confirm against applyLoopStatus
    (task_controller.go:2505-2530) and the verifier's write path that a recorded
    LastEvaluation implies findings.json was written for at least one attempt. If evidence
    contradicts this (a verdict can be parsed without findings.json landing), cite
    file:line in the summary and tighten the predicate accordingly — never loosen it.

    Note: this plan does NOT add a push trigger. The Task verdict-final trigger (required
    because a VerifyHalt freezes all other levels' boundary pushes) is plan 53-10, which
    consumes this predicate. Design the loop so the predicate is trivially shareable (a
    small named helper, e.g. `taskFindingsStageable(t)`), since 53-10's trigger must apply
    the SAME eligibility check.

    Extend artifact_push_test.go fixtures: (a) VerifyHalted + LastEvaluation set → entry
    present; (b) VerifyHalted + nil LastEvaluation (verifier-crashed shape) → NO entry
    (the poison guard); (c) Succeeded + LastEvaluation set → entry present; (d) Succeeded
    + nil LastEvaluation (pre-verify-tier task) → NO entry; (e) Verifying + LastEvaluation
    set (mid-loop repair) → NO entry; (f) AwaitingApproval + LastEvaluation set
    (requireApproval park) → entry present. Assert the emitted DestPrefix/envelope string
    for a task entry uses kind "task" via the existing formatting machinery (strings.Cut
    on the first segment is what tide-push keys on).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestCollectStageEnvelopes -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/... -run TestCollectStageEnvelopes -count=1` exits 0 and the output lists the six new task-predicate subtests (a)-(f)
    - `grep -c 'entry{"task"' internal/controller/artifact_push.go` == 1
    - `git diff --stat cmd/tide-push/` is empty (CLI half untouched)
    - The predicate lives in a named helper reusable by 53-10's trigger
    - Summary records the verified uid→srcDir path and the LastEvaluation-presence-proxy confirmation (or the surfaced contradiction with file:line and the tightened predicate)
  </acceptance_criteria>
  <done>Verdict-final tasks with a recorded evaluation stage findings.json onto the run branch through the existing Phase-49 machinery; mid-loop, pre-verify, and evaluation-less halted tasks never stage (and can never poison the cumulative push).</done>
</task>

<task type="auto">
  <name>Task 2: Admit kind=task to the artifacts endpoint's closed allowlist</name>
  <files>cmd/dashboard/api/artifacts.go, cmd/dashboard/api/artifacts_test.go</files>
  <read_first>
    - cmd/dashboard/api/artifacts.go (the artifactKinds map at :51-60 with its T-37-07-04 doc comment, and the 400 error string at ~:116 that enumerates the allowed kinds)
    - cmd/dashboard/api/artifacts_test.go (the existing allowlist-rejection test idiom to extend)
  </read_first>
  <action>
    Add `"task": true` to the artifactKinds map — one literal key, preserving the closed-map
    shape exactly (ASVS V4: never loosen to a regex or open string). Update the 400 error
    string at ~:116 from "must be one of project, milestone, phase, plan" to include task,
    so the message describes the allowlist it enforces.

    Extend artifacts_test.go: (a) kind=task no longer 400s (reaches the normal
    gitfetch-backed handling — assert status != 400 using the test's existing store fixture,
    or the absent-state 200 shape if that is what the fixture yields); (b) a garbage kind
    (e.g. "tasks", "task/../project") still 400s; (c) the 400 body now names task in its
    allowed-kinds enumeration.
  </action>
  <verify>
    <automated>go test ./cmd/dashboard/api/... -run TestArtifacts -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/dashboard/api/... -count=1` exits 0
    - `grep -c '"task":\s*true' cmd/dashboard/api/artifacts.go` == 1
    - `grep -cE 'must be one of.*task' cmd/dashboard/api/artifacts.go` == 1 (error message updated)
    - A request test with kind "task/../project" (or equivalent traversal string) asserts 400 (closed allowlist preserved)
  </acceptance_criteria>
  <done>The existing artifacts endpoint serves task-kind requests; every non-allowlisted kind still fails closed with an accurate error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard HTTP client → artifacts endpoint | user-influenced {kind}/{name} path params cross into gitfetch prefix filtering |
| Run branch (git) → dashboard render | verifier-authored findings.json becomes newly browsable for task kind |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-05 | Tampering / Information Disclosure | artifactKinds kind param | mitigate | closed literal-map allowlist extended by exactly one key; traversal-shaped kinds asserted 400 in tests (T-37-07-04 preserved) |
| T-53-06 | Information Disclosure | task findings.json served to dashboard | accept | no NEW disclosure class — the endpoint already serves full file content for 4 kinds under the same per-Project git-creds resolution (resolveAuth) and namespace-scoped RBAC (RESEARCH Security Domain) |
| T-53-07 | Denial of Service | staging predicate over-matching | mitigate | verdict-final + LastEvaluation-guarded on ALL arms (VerifyHalted/Succeeded/AwaitingApproval): excludes mid-loop tasks AND evaluation-less halts whose missing findings.json would hard-fail the ENTIRE cumulative push (tide-push fail-closed, main.go:1242-1252); fixtures (b)/(d)/(e) pin the exclusions |
| T-53-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `go test ./internal/controller/... ./cmd/dashboard/api/... -count=1` green
- `make lint` clean
</verification>

<success_criteria>
- Both Finding-10 gaps closed with unit coverage; zero new endpoints; tide-push untouched
- The predicate is poison-proof (no evaluation-less entry can ever reach a push) and shareable by 53-10's trigger
- Findings for a VerifyHalted task are reachable end-to-end once 53-10's trigger lands (kind-tier proof rides plan 53-09's phase-gate `make test-int`)
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-03-SUMMARY.md` when done.
</output>
