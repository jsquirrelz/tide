---
phase: 53-chart-config-dashboard-provenance-surfacing
plan: 07
type: execute
wave: 2
depends_on: ["53-04"]
files_modified:
  - dashboard/web/src/components/StatusBadge.tsx
  - dashboard/web/src/components/StatusBadge.test.tsx
  - dashboard/web/src/components/ConditionBadge.tsx
  - dashboard/web/src/components/ConditionBadge.test.tsx
  - dashboard/web/src/lib/api.ts
  - dashboard/web/src/lib/tasks.ts
  - cmd/dashboard/embed/dist
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "VerifyHalted renders visually distinct from Failed on all three axes — color token (--color-status-blocked vs --color-status-error), glyph (ShieldBan vs CircleX), label (Verify halted vs Failed) — asserted by a component test"
    - "Verifying renders as running-family activity (SearchCheck + animate-pulse); the VerifyHalt condition renders as a blocked-family badge (OctagonPause) whose SR text teaches tide resume"
    - "The TS wire types mirror the 53-04 Go JSON tags byte-for-byte; coerceStatus auto-admits the two new statuses via Object.keys(STATUS_TABLE)"
  artifacts:
    - path: dashboard/web/src/components/StatusBadge.tsx
      provides: "Verifying + VerifyHalted STATUS_TABLE rows per UI-SPEC (verbatim)"
      contains: "VerifyHalted"
    - path: dashboard/web/src/components/ConditionBadge.tsx
      provides: "VerifyHalt CONDITION_TABLE row per UI-SPEC (verbatim)"
      contains: "VerifyHalt"
    - path: dashboard/web/src/lib/tasks.ts
      provides: "loop fields threaded through TaskDetailJSON → taskDetailJSONToData"
      contains: "loopIteration"
  key_links:
    - from: dashboard/web/src/lib/tasks.ts
      to: cmd/dashboard/api/tasks.go
      via: "TaskDetailJSON fields byte-matching the Go json tags (hasVerification, loopIteration, verifyMaxIterations, loopExitReason, lastEvaluation, loopRunId, attemptId)"
      pattern: "verifyMaxIterations"
    - from: dashboard/web/src/components/StatusBadge.tsx
      to: api/v1alpha3/shared_types.go
      via: "wire strings match LevelPhaseVerifying/LevelPhaseVerifyHalted exactly"
      pattern: "Verifying"
---

<objective>
The D-09 vocabulary half of OBS-04: two STATUS_TABLE rows (Verifying, VerifyHalted), one
CONDITION_TABLE row (VerifyHalt), the distinctness component-test contract, and the TS wire
types mirroring the 53-04 payload — everything the 53-08 drawer work builds on.

The 53-UI-SPEC.md is APPROVED and LOCKS every token, icon, label, and SR string — copy is
verbatim from the spec's own code blocks (§Status Vocabulary, §Condition Vocabulary,
§Component Test Contract). Where 53-PATTERNS.md shows slightly different SR text or
suggests ShieldQuestion for the condition icon, the UI-SPEC wins (it post-dates and locks
the contract: SR strings from UI-SPEC, condition icon OctagonPause).

Deliberately NOT here: the FailureHalt condition row (CONTEXT Deferred — not OBS-04; the
UI-SPEC neither requires nor styles it).

Every SPA edit regenerates the embedded dist (Phase-22 gate, D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-UI-SPEC.md
@.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verifying/VerifyHalted STATUS_TABLE rows + VerifyHalt CONDITION_TABLE row + distinctness tests</name>
  <files>dashboard/web/src/components/StatusBadge.tsx, dashboard/web/src/components/StatusBadge.test.tsx, dashboard/web/src/components/ConditionBadge.tsx, dashboard/web/src/components/ConditionBadge.test.tsx</files>
  <read_first>
    - .planning/phases/53-chart-config-dashboard-provenance-surfacing/53-UI-SPEC.md (§Status Vocabulary, §Condition Vocabulary, §Component Test Contract — the LOCKED row literals and assertions; copy verbatim)
    - dashboard/web/src/components/StatusBadge.tsx (StatusValue union at :29-40, STATUS_TABLE at :56-143, the Complete-vs-Succeeded no-shared-glyph comment at :102-105, KNOWN_STATUS_VALUES derivation at :154-156)
    - dashboard/web/src/components/StatusBadge.test.tsx (the table-driven EXPECTED map + it.each idiom)
    - dashboard/web/src/components/ConditionBadge.tsx (CONDITION_TABLE at :35-52, the BillingHalt row shape)
    - dashboard/web/src/components/ConditionBadge.test.tsx (the BILLING_HALT fixture + its it() blocks)
  </read_first>
  <action>
    StatusBadge.tsx: add `Verifying` and `VerifyHalted` to the StatusValue union and
    STATUS_TABLE using the UI-SPEC §Status Vocabulary code block VERBATIM — Verifying:
    icon SearchCheck, label "Verifying", colorVar var(--color-status-running),
    animationClass "animate-pulse", SR "Verifying — independent evaluator reviewing this
    attempt"; VerifyHalted: icon ShieldBan, label "Verify halted", colorVar
    var(--color-status-blocked), no animation, SR "Verify halted — verification ended
    without an approved verdict. Review findings and run `tide resume` to advance".
    Import SearchCheck/ShieldBan from lucide-react (both present in the pinned ^1.16.0 —
    Finding 13; zero npm installs). KNOWN_STATUS_VALUES/coerceStatus auto-propagate from
    Object.keys — no other frontend vocabulary change.

    ConditionBadge.tsx: add the `VerifyHalt` row VERBATIM from §Condition Vocabulary —
    icon OctagonPause (NOT ShieldBan: the project-level condition badge and the task-level
    status badge can co-occur in one viewport; the blocked family's no-shared-glyph rule),
    label "Verify halted", colorVar var(--color-status-blocked), SR "Verification halted
    without an approved verdict — dispatch held. Review staged findings and run `tide
    resume`". The title attribute keeps carrying the controller message verbatim
    (T-14-07-01, inherited).

    StatusBadge.test.tsx: extend the EXPECTED table with both rows AND add the UI-SPEC
    §Component Test Contract assertions verbatim: VerifyHalted.colorVar ===
    var(--color-status-blocked), Failed.colorVar === var(--color-status-error), the three
    not-equal assertions (colorVar, iconName, label vs Failed), plus rendered-DOM checks —
    status-badge-VerifyHalted has data-icon="ShieldBan"; the Verifying badge carries
    animate-pulse.

    ConditionBadge.test.tsx: a VERIFY_HALT fixture mirroring BILLING_HALT's test set —
    renders with label "Verify halted", OctagonPause icon, blocked color token, SR text
    naming tide resume; unknown condition types still render null.
  </action>
  <verify>
    <automated>cd dashboard/web && npx vitest run StatusBadge ConditionBadge</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx vitest run StatusBadge ConditionBadge` exits 0
    - The three distinctness assertions (color/icon/label ≠ Failed) exist in StatusBadge.test.tsx and pass — OBS-04's "visually distinct" as an executable check (D-09)
    - `grep -c "OctagonPause" dashboard/web/src/components/ConditionBadge.tsx` >= 1 and `grep -c "ShieldBan" dashboard/web/src/components/ConditionBadge.tsx` == 0 (no glyph sharing)
    - `grep -c "FailureHalt" dashboard/web/src/components/ConditionBadge.tsx` == 0 (deferred item not folded in)
    - Wire strings are exactly "Verifying" / "VerifyHalted" (match LevelPhaseVerifying/LevelPhaseVerifyHalted in api/v1alpha3/shared_types.go:518,536)
  </acceptance_criteria>
  <done>Both new statuses and the condition render from locked vocabulary rows; distinctness-from-Failed is a passing test, not a review opinion.</done>
</task>

<task type="auto">
  <name>Task 2: TS wire types + JSON→React mapping + embed regeneration</name>
  <files>dashboard/web/src/lib/api.ts, dashboard/web/src/lib/tasks.ts, cmd/dashboard/embed/dist</files>
  <read_first>
    - dashboard/web/src/lib/tasks.ts (TaskDetailJSON at :185-202, taskDetailJSONToData at :105-124, the traceId?/traceSpanId? optional-field precedent)
    - dashboard/web/src/lib/api.ts (PlanDetail at :154-161; fetchNodeArtifacts export at ~:297)
    - cmd/dashboard/api/tasks.go and plans.go AS LANDED BY 53-04 (the Go json tags are the source of truth — mirror exactly)
    - Makefile:327-333 (dashboard-frontend target — builds the SPA, runs frontend tests, copies into cmd/dashboard/embed/dist)
  </read_first>
  <action>
    lib/tasks.ts — extend TaskDetailJSON with the 53-04 fields byte-matching the Go tags:
    hasVerification?: boolean; loopIteration?: number; verifyMaxIterations?: number;
    loopExitReason?: string; lastEvaluation?: { decision: string; findingsCount: number;
    highSeverityCount: number }; loopRunId?: string; attemptId?: string. Thread all seven
    through taskDetailJSONToData unchanged (plain scalars/object — no coercion; only
    status needs coerceStatus) into TaskDetailData's matching optional fields (the
    TaskDetailData type itself lives in TaskDetailDrawer.tsx and is extended by plan
    53-08 — if the mapper needs the target type NOW, add the fields to TaskDetailData in
    this task and note it in the summary; keep the drawer's RENDERING untouched either
    way — that is 53-08's scope).

    lib/api.ts — extend PlanDetail with loopIteration?: number; verifyMaxIterations?:
    number; loopDecision?: string (byte-matching 53-04's plans.go tags).

    Regenerate the embedded dist: run `make dashboard-frontend` and commit the
    cmd/dashboard/embed/dist changes in the SAME commit as the SPA source change
    (Phase-22 gate). Then `make verify-dashboard-freshness` must pass.
  </action>
  <verify>
    <automated>cd dashboard/web && npx tsc --noEmit -p . && npx vitest run && cd ../.. && make verify-dashboard-freshness</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx tsc --noEmit -p .` exits 0 (types compile)
    - `grep -c "verifyMaxIterations" dashboard/web/src/lib/tasks.ts` >= 1 and `grep -c "loopDecision" dashboard/web/src/lib/api.ts` >= 1
    - Field names match the Go tags exactly: `grep -o 'json:"[a-zA-Z]*' cmd/dashboard/api/tasks.go | grep -E 'loopIteration|verifyMaxIterations|hasVerification|loopRunId|attemptId'` names all appear in lib/tasks.ts
    - `make verify-dashboard-freshness` exits 0 (embed regenerated with the SPA change)
  </acceptance_criteria>
  <done>The SPA's wire layer mirrors the landed Go payload exactly; the embedded dist is fresh; the drawer work in 53-08 has typed data waiting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON → browser render | server-sent status/condition strings render through client vocabulary tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-16 | Spoofing / injection | unknown status/condition strings from a compromised or newer server | mitigate | existing defensive coercion preserved — coerceStatus unknown→Pending, ConditionBadge unknown→null; both re-asserted in the extended tests |
| T-53-17 | Information Disclosure | SR/title text | accept | all copy is the LOCKED UI-SPEC vocabulary; title carries the controller message verbatim under the existing T-14-07-01 contract — no new data class reaches the DOM |
| T-53-SC | Tampering | package installs | accept | zero new npm packages — lucide icons ship in the pinned ^1.16.0 (RESEARCH Finding 13) |
</threat_model>

<verification>
- `cd dashboard/web && npx vitest run` (full frontend suite)
- `make verify-dashboard-freshness` (D-10 in-phase)
</verification>

<success_criteria>
- Distinctness contract green; both badges render from table rows with zero bespoke components
- Wire types byte-match the landed Go payload; embed fresh
</success_criteria>

<output>
Create `.planning/phases/53-chart-config-dashboard-provenance-surfacing/53-07-SUMMARY.md` when done.
</output>
