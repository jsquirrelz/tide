---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/milestone_controller_test.go
  - internal/controller/task_controller_test.go
  - internal/controller/plan_controller_test.go
  - internal/controller/wave_controller_test.go
  - internal/controller/project_boundary_push_test.go
  - internal/controller/task_gates_test.go
  - internal/controller/billing_halt_regression_test.go
  - internal/controller/plan_wavepause_test.go
  - internal/controller/dispatch_image_test.go
  - internal/controller/project_baseref_halt_test.go
  - internal/controller/budget_blocked_regression_test.go
  - internal/controller/plan_gates_test.go
autonomous: true
requirements: [REFAC-06]

must_haves:
  truths:
    - "One reconcile-retry driver family remains in the package: reconcileWithRetry (error-only) plus one result-returning core it delegates to — the three receiver-typed duplicates are gone"
    - "Conflict detection uses apierrors.IsConflict(err), not error-text substring matching"
    - "The FULL internal/controller test package passes (no -run narrowing — OQ-2 resolution: prove IsConflict catches every conflict shape the substring match did)"
  artifacts:
    - path: "internal/controller/milestone_controller_test.go"
      provides: "unified conflict-retrying reconcile driver(s) using apierrors.IsConflict"
      contains: "apierrors.IsConflict"
  key_links:
    - from: "internal/controller/*_test.go call sites (~89)"
      to: "reconcileWithRetry / result-returning variant"
      via: "bound method value r.Reconcile matching reconcilerFunc"
      pattern: "reconcileWithRetry\\("
---

<objective>
Seed item 6: unify the package's four envtest reconcile-retry drivers onto the one that is already package-standard (`reconcileWithRetry`, milestone_controller_test.go:42-59, ~78 existing call sites across 12 files), fix its conflict detection to `apierrors.IsConflict`, and delete the three receiver-typed duplicates (`reconcileN`+`isConflict`, `reconcilePlanN`, `reconcileWaveN`) — repointing their ~89 call sites. Test-tier only; zero production code changes.

Purpose: one driver, one conflict-detection implementation, no per-reconciler-type copies to drift.
Output: two commits; full internal/controller package green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md

Per CONTEXT D-01: line numbers are hints — re-locate by symbol before editing.
RESEARCH finding that reshapes the seed: do NOT author a new generic — the destination driver already exists. Item 6 is a delete-and-repoint, not a design task.
</context>

<interfaces>
Current shapes (verified at HEAD):
- Survivor: `func reconcileWithRetry(r reconcilerFunc, name types.NamespacedName, n int) error` with `type reconcilerFunc func(context.Context, reconcile.Request) (ctrl.Result, error)` — milestone_controller_test.go:42-59. Conflict match today: `strings.Contains(err.Error(), "the object has been modified") || strings.Contains(err.Error(), "Conflict")`.
- Duplicates to delete: `func reconcileN(r *TaskReconciler, name types.NamespacedName, n int) (ctrl.Result, error)` + `func isConflict(err error) bool` (task_controller_test.go:83-112); `func reconcilePlanN(r *PlanReconciler, ...) (ctrl.Result, error)` (plan_controller_test.go:153); `func reconcileWaveN(r *WaveReconciler, ...) (ctrl.Result, error)` (wave_controller_test.go:146).
- The conflict string `"the object has been modified"` appears in exactly 4 test files — the 4 driver definitions. No other inline retry loops use it.
- Result-consuming call sites (7 total, verified): billing_halt_regression_test.go:768, :836; task_gates_test.go:267, :350; budget_blocked_regression_test.go:176, :250; task_controller_test.go:272. All other call sites use only the error (mostly `_, err :=` or `_, _ =` forms).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert the surviving driver to apierrors.IsConflict and add a result-returning core</name>
  <files>internal/controller/milestone_controller_test.go</files>
  <read_first>
    - internal/controller/milestone_controller_test.go (lines 1-60: import block + reconcilerFunc + reconcileWithRetry)
    - internal/controller/task_controller_test.go (lines 83-112: reconcileN + isConflict — the loop semantics to preserve, including the "err = nil; continue" reset)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md (section "Item 6")
  </read_first>
  <action>
    In milestone_controller_test.go, beside reconcileWithRetry:
    1. Add `func reconcileWithRetryResult(r reconcilerFunc, name types.NamespacedName, n int) (ctrl.Result, error)` — same nested loop shape (n outer iterations, up to 5 conflict retries each), returning the LAST ctrl.Result and final error. Conflict detection: `apierrors.IsConflict(err)` — no string matching. Preserve reconcileN's semantics of clearing err on a conflict retry so a still-conflicting final attempt doesn't leak a stale error where the old drivers returned nil.
    2. Reimplement reconcileWithRetry as a thin wrapper: `_, err := reconcileWithRetryResult(r, name, n); return err` — exactly one loop implementation survives.
    3. Add the import `apierrors "k8s.io/apimachinery/pkg/api/errors"` (aliased exactly as in dispatch_helpers.go). Drop the "strings" import if it becomes unused in this file.
    Do not change reconcileWithRetry's signature — 78 existing call sites depend on the error-only shape.
  </action>
  <verify>
    <automated>go vet ./internal/controller/... && go test ./internal/controller/... -run 'Milestone' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'apierrors.IsConflict' internal/controller/milestone_controller_test.go` returns ≥ 1
    - `grep -c 'the object has been modified' internal/controller/milestone_controller_test.go` returns 0
    - `grep -c 'func reconcileWithRetryResult' internal/controller/milestone_controller_test.go` returns 1
    - `go vet ./internal/controller/...` exits 0
  </acceptance_criteria>
  <done>One conflict-retry loop implementation exists, using apierrors.IsConflict; error-only wrapper signature unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the three typed drivers and repoint all ~89 call sites</name>
  <files>internal/controller/task_controller_test.go, internal/controller/plan_controller_test.go, internal/controller/wave_controller_test.go, internal/controller/project_boundary_push_test.go, internal/controller/task_gates_test.go, internal/controller/billing_halt_regression_test.go, internal/controller/plan_wavepause_test.go, internal/controller/dispatch_image_test.go, internal/controller/project_baseref_halt_test.go, internal/controller/budget_blocked_regression_test.go, internal/controller/plan_gates_test.go</files>
  <read_first>
    - internal/controller/task_controller_test.go (reconcileN + isConflict definitions and this file's call sites)
    - internal/controller/plan_controller_test.go (reconcilePlanN :153), internal/controller/wave_controller_test.go (reconcileWaveN :146)
    - Output of `grep -rn 'reconcileN(\|reconcilePlanN(\|reconcileWaveN(' internal/controller --include='*_test.go'` (the authoritative call-site list — enumerate before editing)
  </read_first>
  <action>
    1. Delete `reconcileN`, `isConflict` (task_controller_test.go), `reconcilePlanN` (plan_controller_test.go), `reconcileWaveN` (wave_controller_test.go).
    2. Repoint every call site (89 across the 11 files listed in files_modified minus milestone_controller_test.go):
       - Error-only sites (`_, err := reconcileN(r, name, n)` / `_, _ = ...`): replace with `err := reconcileWithRetry(r.Reconcile, name, n)` (or `_ = reconcileWithRetry(...)` for discarded results), preserving each site's existing error assertion.
       - The 7 result-consuming sites (listed in <interfaces>): replace with `result, err := reconcileWithRetryResult(r.Reconcile, name, n)`, preserving each site's existing result assertions (RequeueAfter checks) verbatim.
       - The bound-method-value form `r.Reconcile` works for every reconciler type (that is the point of reconcilerFunc) — the variable name of the reconciler at each site varies (`r`, `reconciler`); keep whatever the site uses.
    3. Run the FULL package (no -run filter): the ~90-call-site blast radius plus the IsConflict conversion must be proven against every suite (OQ-2 resolution from the planning decisions — the substring match may have caught wrapped errors IsConflict doesn't; a full green run is the proof it doesn't matter here).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'func reconcileN(\|func reconcilePlanN(\|func reconcileWaveN(\|func isConflict(' internal/controller --include='*_test.go'` returns 0 lines
    - `grep -rn '"the object has been modified"' internal/controller --include='*_test.go'` returns 0 lines
    - `grep -rc 'reconcileWithRetry\|reconcileWithRetryResult' internal/controller --include='*_test.go' | awk -F: '{s+=$2} END {print s}'` totals ≥ 160 (78 pre-existing + ~89 repointed)
    - `go test ./internal/controller/... -count=1` exits 0 — FULL package, no -run narrowing
  </acceptance_criteria>
  <done>Three duplicate drivers deleted, all call sites on the unified pair, full internal/controller suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-tier-only change; no production code, input path, or privilege surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | Repudiation (test-signal integrity) | envtest conflict-retry drivers | mitigate | apierrors.IsConflict is structurally tied to metav1.StatusReasonConflict — removes the false-negative class where the API server rewords its message text; full-package run proves no conflict shape regressed |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `go test ./internal/controller/... -count=1` green (full package, both tasks' final gate)
- Grep gates: zero typed-driver definitions, zero conflict-substring matches in tests
</verification>

<success_criteria>
- Seed item 6 closed per its Verify line (`go test ./internal/controller/...`, full package per OQ-2)
- Production code untouched (`git diff --name-only` across both commits shows only *_test.go files)
- REFAC-06 satisfied
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-02-SUMMARY.md` when done
</output>
