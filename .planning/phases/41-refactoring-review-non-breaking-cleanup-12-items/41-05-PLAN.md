---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 05
type: execute
wave: 4
depends_on: [41-04]
files_modified:
  - internal/controller/dispatch_helpers.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - .planning/todos/pending/2026-07-12-task-dispatch-gate-order-divergence.md
autonomous: true
requirements: [REFAC-07]

must_haves:
  truths:
    - "One shared checkDispatchHolds implementation carries the planner-tier project-scoped hold chain (Billing 30s → Failure 30s → Budget 30s → Import 5s, in that order) — Milestone, Phase, and Plan all call it"
    - "Gate ordering and requeue intervals are byte-equivalent to the pre-refactor behavior at each of the three migrated sites; 'dispatch held: ...' and 'import pending; holding planner dispatch' log messages are byte-identical (load-bearing greps)"
    - "Task's divergent inline chain (Import checked SECOND, before Billing/Failure/Budget, plus the task-only reservation-headroom check) is UNTOUCHED, documented with a cross-reference comment, and recorded as a follow-up todo — behavior invariance is the phase boundary"
  artifacts:
    - path: "internal/controller/dispatch_helpers.go"
      provides: "checkDispatchHolds shared gate-chain helper"
      contains: "func checkDispatchHolds"
    - path: ".planning/todos/pending/2026-07-12-task-dispatch-gate-order-divergence.md"
      provides: "follow-up finding: Task-tier Import-hold position diverges from planner tier"
  key_links:
    - from: "internal/controller/{milestone,phase,plan}_controller.go dispatch entry"
      to: "checkDispatchHolds in dispatch_helpers.go"
      via: "direct call after the level-specific Reject/parent-approval gates"
      pattern: "checkDispatchHolds\\("
---

<objective>
Seed item 7: extract the byte-identical project-scoped dispatch-holds chain (Billing→Failure→Budget→Import) from the Milestone, Phase, and Plan controllers into a single `checkDispatchHolds` helper in dispatch_helpers.go — the file that already houses the cross-reconciler `checkParentApproval` gate. One controller migrates per task (D-07: gate ordering is semantically load-bearing; slot leaks and park-before-acquire are the bug class).

Scope boundary (resolved plan-time decision — do not re-open): the helper covers Milestone/Phase/Plan ONLY. Task's chain checks Import SECOND (not last) and adds a reservation-headroom hold with no planner-tier counterpart — silently normalizing it would be a behavior change in a non-breaking phase. Task's site gets a divergence-documenting comment plus a follow-up todo, not a migration.

Purpose: Phase 28's import hold changed four files; the next project-scoped gate should change one. The ordering invariant currently exists only as replicated comments.
Output: three commits (helper+Milestone; Phase; Plan+Task-comment+todo), gates suites green after each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-RESEARCH.md

Per CONTEXT D-01: line numbers are hints — re-locate by symbol/grep.

RESEARCH Pitfall 1 is the load-bearing constraint: the four sites do NOT share one order. Verified per-site order at HEAD:
- Milestone (~:343-393): Reject → Billing(30s) → Failure(30s) → Budget(30s) → Import(5s, LAST)
- Phase (~:330-386): ParentApproval(5s) → { Reject → Billing → Failure → Budget → Import(LAST) }
- Plan (~:343-400): identical to Phase
- Task (~:366-458): Reject → ParentApproval(5s) → Import(5s, SECOND) → Billing → Failure → Budget → reservation-headroom(30s, task-only)

What the helper absorbs (project-scoped holds only, per the seed's shape): checkBillingHalt (30s, "dispatch held: project billing halt"), checkFailureHalt (30s, "dispatch held: project failure halt (conservative profile)"), checkBudgetBlocked && !budget.IsBypassed(project, time.Now()) (30s, "dispatch held: project budget blocked"), ImportSource != nil && ConditionImportComplete not True (5s, "import pending; holding planner dispatch"). What stays at call sites: the earlyProject fetch, gates.CheckRejected → patch*Rejected (level-specific status patch), checkParentApproval.
</context>

<interfaces>
Helper contract (from CONTEXT's Specific Ideas sketch, adapted to the verified call-site shape — the project is pre-fetched at every site, so no client param is needed):

From internal/controller/dispatch_helpers.go (new):
- `func checkDispatchHolds(ctx context.Context, project *tideprojectv1alpha3.Project, level, objName string) (held bool, result ctrl.Result)`
- nil-safe: `project == nil` returns `(false, ctrl.Result{})` (matches the existing nil-safe check* helpers — a missing project is not a hold)
- Order INSIDE the helper is fixed: Billing → Failure → Budget → Import. Requeue literals: `30 * time.Second` for the three halts, `5 * time.Second` for import.
- Log lines mirror the current sites exactly: `logf.FromContext(ctx).V(1).Info("<current message>", level, objName, "project", project.Name)` — `level` is the structured key name ("milestone"/"phase"/"plan"), preserving today's per-site key shape; `project.Name` equals the Spec.*Ref value the old sites logged (the fetch succeeded whenever project != nil).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author checkDispatchHolds and migrate MilestoneReconciler</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/milestone_controller.go</files>
  <read_first>
    - internal/controller/milestone_controller.go (the dispatch-entry gate block ~:343-393 — the source text the helper must reproduce byte-for-byte in behavior, including the "position BEFORE pool acquire (Pitfall 2)" comment context)
    - internal/controller/dispatch_helpers.go (checkParentApproval ~:425-467 — the shape/placement precedent; file import block for logf/ctrl/time/budget/meta availability)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md (section "Item 7" — the full current call-site excerpt)
  </read_first>
  <action>
    1. In dispatch_helpers.go, add `checkDispatchHolds` per the <interfaces> contract, with a doc comment stating: (a) it centralizes the planner-tier project-scoped hold chain, (b) the Billing→Failure→Budget→Import order and the 30s/5s requeues are load-bearing (slot leaks, park-before-pool-acquire), (c) Task's chain intentionally diverges (Import second + reservation headroom) and is NOT a caller — cross-reference task_controller.go.
    2. In milestone_controller.go, replace the four inline hold checks (checkBillingHalt / checkFailureHalt / checkBudgetBlocked+IsBypassed / ImportComplete block) with: `if held, res := checkDispatchHolds(ctx, earlyProject, "milestone", ms.Name); held { return res, nil }`. Keep the earlyProject fetch and the gates.CheckRejected → patchMilestoneRejected lines exactly where they are (before the helper call). The helper call must sit at the same position in the reconcile flow the last inline check occupied — before any pool acquire.
    3. Do not modify billing_halt.go / failure_halt.go / budget_blocked.go — the helper calls the existing nil-safe wrappers.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run 'Gates|Halt|Budget|Import' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func checkDispatchHolds' internal/controller/dispatch_helpers.go` returns 1
    - `grep -c 'dispatch held: project billing halt' internal/controller/milestone_controller.go` returns 0 (moved into the helper)
    - `grep -c '30 \* time.Second' internal/controller/dispatch_helpers.go` returns ≥ 3 and `grep -c '5 \* time.Second'` returns ≥ 1 within the helper region (requeue literals preserved)
    - `go test ./internal/controller/... -run 'Gates|Halt|Budget|Import' -count=1` exits 0 (milestone_gates_test.go + halt/budget regression suites green)
  </acceptance_criteria>
  <done>Helper exists with the exact planner-tier order; Milestone dispatches through it; gates suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate PhaseReconciler</name>
  <files>internal/controller/phase_controller.go</files>
  <read_first>
    - internal/controller/phase_controller.go (gate block ~:330-386 — confirm the ParentApproval-first wrapper and the project-scoped block within)
    - internal/controller/dispatch_helpers.go (the helper landed in Task 1)
  </read_first>
  <action>
    Replace phase_controller.go's inline project-scoped hold block with `checkDispatchHolds(ctx, earlyProject, "phase", ph.Name)` at the same flow position. checkParentApproval (5s) stays FIRST at the call site, then Reject → the helper — matching today's Phase order exactly. No other phase_controller.go changes.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run 'Gates|Halt|Budget|Import' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispatch held: project billing halt' internal/controller/phase_controller.go` returns 0
    - `grep -c 'checkDispatchHolds(' internal/controller/phase_controller.go` returns 1
    - phase_gates_test.go specs green within the -run filter
  </acceptance_criteria>
  <done>Phase dispatches through the shared helper with unchanged gate order; suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate PlanReconciler; document the Task-site divergence</name>
  <files>internal/controller/plan_controller.go, internal/controller/task_controller.go, .planning/todos/pending/2026-07-12-task-dispatch-gate-order-divergence.md</files>
  <read_first>
    - internal/controller/plan_controller.go (gate block ~:343-400)
    - internal/controller/task_controller.go (gate block ~:366-458 — read the FULL chain to quote its actual order in the comment: Reject → ParentApproval → Import → Billing → Failure → Budget → reservation-headroom)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-RESEARCH.md (Pitfall 1 — the divergence statement the todo captures)
  </read_first>
  <action>
    1. plan_controller.go: same migration as Phase — `checkDispatchHolds(ctx, earlyProject, "plan", plan.Name)`, ParentApproval + Reject staying at the call site.
    2. task_controller.go: add a comment block at the head of its gate chain (no logic change): the Task chain intentionally remains inline — it checks Import SECOND (before Billing/Failure/Budget; planner tier via checkDispatchHolds checks Import LAST) and adds a task-only reservation-headroom hold; normalizing the order would change which hold message/requeue fires when Import-pending coincides with a Billing/Failure/Budget halt, and Phase 41's boundary is behavior invariance; cross-reference checkDispatchHolds in dispatch_helpers.go and the follow-up todo.
    3. Create `.planning/todos/pending/2026-07-12-task-dispatch-gate-order-divergence.md` with frontmatter (name: task-dispatch-gate-order-divergence, type: candidate-finding, captured: 2026-07-12, source: Phase 41 plan 41-05 / RESEARCH Pitfall 1) and a body stating: the four dispatch sites' hold order diverges on the Import-check position (Task: second; Milestone/Phase/Plan: last); consequence (different hold message + requeue interval — 5s vs 30s — for the same Project state); the fix fork (normalize Task to the shared helper with a documented, tested order change, or declare Task a permanent structural outlier); routed to a future phase decision.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run 'Gates|Halt|Budget|Import' -count=1 && make test-int-fast</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'dispatch held: project billing halt' internal/controller/*.go | awk -F: '{s+=$2} END {print s}'` totals exactly 2 for non-test files (the helper + Task's inline chain) — down from 4
    - `grep -c 'checkDispatchHolds' internal/controller/task_controller.go` returns ≥ 1 (cross-reference comment) while `grep -cE 'if held, res := checkDispatchHolds' internal/controller/task_controller.go` returns 0 (no call — comment only)
    - `test -f .planning/todos/pending/2026-07-12-task-dispatch-gate-order-divergence.md` succeeds
    - `go test ./internal/controller/... -run 'Gates|Halt|Budget|Import' -count=1` exits 0 AND `make test-int-fast` exits 0 (envtest tier green after the last migration)
  </acceptance_criteria>
  <done>Three planner-tier controllers share one hold chain; Task's divergence is surfaced (comment + todo), not silently changed; envtest tier green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project status → dispatch decision | The hold chain is the enforcement point that stops Job dispatch (and therefore LLM spend) on BillingHalt / FailureHalt / BudgetBlocked / Import-pending — a regression here leaks money or dispatches against an un-imported workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05a | Elevation of Privilege (spend-gate bypass) | checkDispatchHolds ordering/short-circuit | mitigate | Helper reproduces the verified order with the same nil-safe wrappers; one-controller-per-task migration with Gates/Halt/Budget/Import suites green after EACH task; requeue literals asserted by grep |
| T-41-05b | Denial of Service (over-holding) | requeue intervals (30s/5s) | mitigate | Intervals preserved as literals inside the helper and grep-asserted; envtest gates specs pin park/resume behavior |
| T-41-05c | Repudiation (operator observability) | "dispatch held: ..." log lines | mitigate | Messages byte-identical (CLAUDE.md runtime-gate greps depend on them); structured key shape preserved via the level param |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `go test ./internal/controller/... -run 'Gates|Halt|Budget|Import' -count=1` green after each task
- `make test-int-fast` green at plan close
- Hold-message count grep: 4 → 2 non-test occurrences (helper + Task inline)
</verification>

<success_criteria>
- Seed item 7 closed for the planner tier; the next project-scoped gate is a one-file change
- Task-tier behavior byte-identical; divergence documented in code + todo (resolved plan-time decision honored)
- REFAC-07 satisfied
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-05-SUMMARY.md` when done
</output>
