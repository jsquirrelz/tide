---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 03
type: execute
wave: 2
depends_on: [41-02]
files_modified:
  - internal/controller/task_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/controller/wave_controller.go
  - cmd/manager/main.go
  - internal/controller/*_test.go (SubagentImage assignment removals — ~21 files, enumerated by grep)
  - cmd/manager/wiring_test.go
autonomous: true
requirements: [REFAC-04]

must_haves:
  truths:
    - "gateDispatch and ensureJob no longer exist in task_controller.go (their //nolint:unused grep contracts are historical — only Phase 41's own planning artifacts reference them)"
    - "No reconciler struct carries a dead SubagentImage field; the LIVE SubagentImage surfaces (podjob backend field, JobSpec population from resolveImage, the --subagent-image flag feeding HelmProviderDefaults) are untouched"
    - "WaveReconciler no longer declares PlannerPool/ExecutorPool (zero reads existed); Dispatcher stays (read at wave_controller.go:110)"
    - "go build ./... && go vet ./... green; internal/controller and cmd/manager suites green"
  artifacts:
    - path: "internal/controller/task_controller.go"
      provides: "TaskReconcilerDeps without SubagentImage; no gateDispatch/ensureJob"
    - path: "cmd/manager/main.go"
      provides: "wiring without dead SubagentImage/ExecutorPool assignments"
  key_links:
    - from: "cmd/manager/main.go --subagent-image flag"
      to: "helmProviderDefaults.Image override (~:252-253)"
      via: "the ONLY live path for the flag — must survive the deletion"
      pattern: "helmProviderDefaults.Image = subagentImage"
---

<objective>
Seed item 4: delete dead code and dead struct fields — `gateDispatch` + `ensureJob` (task_controller.go, //nolint:unused, grep contracts long shipped), the `SubagentImage` reconciler-struct field on all 5 reconcilers ("dead since Phase 13" — resolveImage owns resolution), and `WaveReconciler.PlannerPool`/`ExecutorPool` (wired but never read) — end-to-end: struct defs, main.go wiring, and test fixtures.

File-count note: the ~29-file total is deliberate, not scope creep — Tasks 1-2 touch only 7 production files; the remaining ~22 are the test-fixture sweep, already isolated in its own task and commit (Task 3), whose edits are grep-enumerated one-line `SubagentImage:` assignment removals per file. Mechanical, enumerated, compile-checked — the wide count carries no cross-file semantic risk.

Purpose: `ensureJob` duplicates `createDispatchJob`'s Job-build block and drifts when BuildOptions grows; dead fields invite the cascade-8 "wired but unused / used but unwired" confusion class.
Output: three commits; build, vet, and both test packages green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md

Per CONTEXT D-01: line numbers are hints — re-locate by symbol/grep before editing.

CRITICAL disambiguation (verified at HEAD — do not over-delete). "SubagentImage" names THREE different things:
1. DEAD (delete): the reconciler-struct fields — TaskReconcilerDeps.SubagentImage (task_controller.go ~:96-98), MilestoneReconciler.SubagentImage (~:83-85), PhaseReconciler (~:75-77), PlanReconciler (~:89-91), ProjectReconciler (~:202-204) — and their main.go wiring lines (~:433, :465, :491, :516, :559).
2. LIVE (keep): the podjob dispatch-backend field `PodJobBackend.SubagentImage` populated at main.go ~:401 with helmProviderDefaults.Image.
3. LIVE (keep): the per-dispatch JobSpec/BuildOptions population `SubagentImage: resolvedImage` / `SubagentImage: resolveImage(...)` at the six dispatch sites (task_controller.go ~:797/:1472, project_controller.go ~:1758, milestone_controller.go ~:507, phase_controller.go ~:466, plan_controller.go ~:501).
Also LIVE (keep): the `--subagent-image` flag (main.go ~:140/:159) and its helmProviderDefaults.Image override (~:252-253) — that is the flag's real consumer.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete gateDispatch and ensureJob from task_controller.go</name>
  <files>internal/controller/task_controller.go</files>
  <read_first>
    - internal/controller/task_controller.go (the two functions: gateDispatch at ~:1433-1449, ensureJob at ~:1451 onward — read to the end of ensureJob's body to capture its full extent and any helpers only it uses)
    - Output of `grep -rln 'ensureJob\|gateDispatch' .planning/ --include='*.md'` (confirm only Phase 41's own artifacts + the seed reference them — the historical plan grep contracts have shipped)
  </read_first>
  <action>
    Delete both functions and their //nolint:unused markers: `func (r *TaskReconciler) gateDispatch(projectName, secretUID string, limits budget.Limits) time.Duration` and `func (r *TaskReconciler) ensureJob(ctx, task, project, attempt int, token string, envInJSON []byte) (*batchv1.Job, error)`. After deletion, remove any import or private helper that becomes unused ONLY because these two are gone (goimports + `go vet` will surface them). Do not touch createDispatchJob — it is the live path ensureJob duplicated.
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/controller/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gateDispatch\|ensureJob' internal/controller/task_controller.go` returns 0
    - `grep -c 'nolint:unused' internal/controller/task_controller.go` returns 0
    - `go build ./... && go vet ./internal/controller/...` exits 0
  </acceptance_criteria>
  <done>Both dead functions gone; build and vet green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead SubagentImage reconciler fields and WaveReconciler pools + main.go wiring</name>
  <files>internal/controller/task_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/wave_controller.go, cmd/manager/main.go</files>
  <read_first>
    - The five reconciler struct definitions (locate by `grep -n 'SubagentImage' internal/controller/*.go | grep -v _test` — field defs carry the "dead since Phase 13" doc comment)
    - internal/controller/wave_controller.go (struct def ~:56-62 — PlannerPool/ExecutorPool declared, Dispatcher READ at :110, keep Dispatcher)
    - cmd/manager/main.go (wiring sites ~:428-570; the WaveReconciler literal ~:528-534 with ExecutorPool assignment)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md (section "Item 4")
  </read_first>
  <action>
    1. Delete the SubagentImage field + its "dead since Phase 13" doc comment from: TaskReconcilerDeps, MilestoneReconciler, PhaseReconciler, PlanReconciler, ProjectReconciler.
    2. Delete `PlannerPool *pool.Pool` and `ExecutorPool *pool.Pool` (and their doc comments) from WaveReconciler — keep `Dispatcher` (read at wave_controller.go:110 to gate the observational roll-up).
    3. cmd/manager/main.go: delete the five dead wiring lines `SubagentImage: subagentImage,` (in the Project/Milestone/Phase/Plan reconciler literals and inside the TaskReconcilerDeps literal) and `ExecutorPool: executorPool,` in the WaveReconciler literal. PRESERVE: the `--subagent-image` flag registration, the `helmProviderDefaults.Image = subagentImage` override, and `PodJobBackend.SubagentImage: helmProviderDefaults.Image` (see the disambiguation note in <context> — those are the flag's live path).
    4. Do NOT touch the `SubagentImage: resolvedImage` / `resolveImage(...)` JobSpec-population lines at the six dispatch sites — that field belongs to the dispatch options struct, not the reconcilers.
  </action>
  <verify>
    <automated>go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'SubagentImage' internal/controller --include='*.go' | grep -v _test | grep -cv 'resolvedImage\|resolveImage'` returns 0 (only the six live JobSpec-population lines remain)
    - `grep -c 'SubagentImage:' cmd/manager/main.go` returns exactly 1 (the PodJobBackend line)
    - `grep -c 'PlannerPool\|ExecutorPool' internal/controller/wave_controller.go` returns 0
    - `grep -nA6 'controller.WaveReconciler{' cmd/manager/main.go | grep -c 'ExecutorPool'` returns 0
    - `go build ./... && go vet ./...` exits 0
  </acceptance_criteria>
  <done>Dead fields and their wiring gone; every live SubagentImage surface intact; build/vet green.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep test fixtures that set the deleted fields</name>
  <files>internal/controller/*_test.go (21 files — enumerate by grep), cmd/manager/wiring_test.go</files>
  <read_first>
    - Output of `grep -rln 'SubagentImage' internal/controller cmd/manager --include='*_test.go'` (the authoritative fixture list — 21 internal/controller files at HEAD)
    - internal/controller/dispatch_image_test.go (may assert the reconciler field is IGNORED at dispatch — those assertions become meaningless with the field gone; read before deciding delete-vs-rewrite per assertion)
  </read_first>
  <action>
    Remove every `SubagentImage: ...` assignment that targets a deleted reconciler-struct field from the test files (fixture constructions like `&MilestoneReconciler{..., SubagentImage: "x"}` or `TaskReconcilerDeps{..., SubagentImage: "x"}`). In dispatch_image_test.go, keep any assertion that pins resolveImage/BuildOptions behavior (live surface); delete only assertions that existed to prove the dead reconciler field was ignored. If any WaveReconciler test fixture sets PlannerPool/ExecutorPool, remove those lines too (grep found none at HEAD — re-verify). Do not weaken any resolveImage behavioral assertion.
  </action>
  <verify>
    <automated>go test ./internal/controller/... ./cmd/manager/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'SubagentImage' internal/controller cmd/manager --include='*_test.go' | grep -cv 'resolvedImage\|resolveImage\|BuildOptions'` returns 0 or every remaining hit targets a live dispatch-options struct (manual justification in SUMMARY if nonzero)
    - `go test ./internal/controller/... -count=1` exits 0 (full package)
    - `go test ./cmd/manager/... -count=1` exits 0
  </acceptance_criteria>
  <done>All fixtures compile against the slimmed structs; both packages fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure deletion of never-executed code and never-read fields; the live image-resolution path (resolveImage → JobSpec) is explicitly fenced off from the deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | Denial of Service (regression) | dispatch Job construction | mitigate | ensureJob is proven dead (nolint:unused, zero callers); createDispatchJob path untouched; full controller + manager suites must pass (Task 3 verify) |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `go build ./... && go vet ./...` green after each task
- `go test ./internal/controller/... ./cmd/manager/... -count=1` green (final gate — matches the seed's item-4 Verify line)
</verification>

<success_criteria>
- Seed item 4 closed: dead functions, dead fields, and dead wiring gone end-to-end
- Zero live-surface regression: --subagent-image flag behavior, PodJobBackend field, and resolveImage JobSpec population byte-identical
- REFAC-04 satisfied
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-03-SUMMARY.md` when done
</output>
