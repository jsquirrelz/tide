---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 06
type: execute
wave: 5
depends_on: [41-05]
files_modified:
  - internal/controller/dispatch_helpers.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - cmd/manager/main.go
  - cmd/manager/wiring_test.go
  - cmd/manager/wave_dispatcher_wiring_test.go
  - internal/controller/*_test.go (planner-reconciler fixture constructions — ~20 files, enumerated by grep)
autonomous: true
requirements: [REFAC-08]

must_haves:
  truths:
    - "The four planner reconcilers (Milestone/Phase/Plan/Project — Project included per RESEARCH Pitfall 2) carry their dispatch-tier dependencies in one PlannerReconcilerDeps carrier, built ONCE in main.go and assigned four times — a forgotten wiring is a one-place mistake (the cascade-8 class this item closes)"
    - "cmd/manager wiring-lock tests assert the Deps-carried fields are non-nil for all four reconcilers (extended from the existing Task.Deps.Dispatcher case)"
    - "Behavior invariant: full internal/controller and cmd/manager suites green"
  artifacts:
    - path: "internal/controller/dispatch_helpers.go"
      provides: "PlannerReconcilerDeps carrier struct (8 dispatch-tier fields)"
      contains: "type PlannerReconcilerDeps struct"
    - path: "cmd/manager/main.go"
      provides: "single plannerDeps construction assigned to 4 reconcilers"
      contains: "PlannerReconcilerDeps{"
  key_links:
    - from: "cmd/manager/main.go plannerDeps literal"
      to: "MilestoneReconciler/PhaseReconciler/PlanReconciler/ProjectReconciler .Deps"
      via: "Deps: plannerDeps in each SetupWithManager literal"
      pattern: "Deps:\\s*plannerDeps"
---

<objective>
Seed item 8: consolidate the ~9 dispatch-tier fields each planner reconciler redeclares (Dispatcher, EnvReader, SigningKey, CredproxyImage, TidePushImage, ReporterImage, HelmProviderDefaults, PricingOverridesJSON — SubagentImage already deleted by 41-03) into a `PlannerReconcilerDeps` carrier, mirroring the existing `TaskReconcilerDeps` pattern (task_controller.go:80-142). Project is IN scope (RESEARCH Pitfall 2): it declares the same fields, and leaving it out preserves exactly the forgotten-Dispatcher bug class (cascade-8) this item exists to prevent.

File-count note: the ~28-file total is deliberate, not scope creep — Tasks 1-2 touch only 8 production files; the remaining ~20 are the test-fixture sweep, already isolated in its own task and commit (Task 3), whose edits follow one grep-enumerated mechanical rule per fixture (the 8 carrier field names move into `Deps: PlannerReconcilerDeps{...}`, nothing else changes). Mechanical, enumerated, compile-checked — the wide count carries no cross-file semantic risk.

Purpose: four hand-maintained copies of the same wiring, each with its own "CR-01 fix: Dispatcher must be assigned" comment, collapse to one construction site locked by tests.
Output: three commits; wiring locks extended; full suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md

Per CONTEXT D-01: line numbers are hints — re-locate by symbol/grep.

Field-scoping rule (mirrors TaskReconcilerDeps' own doc comment): the carrier holds dispatch-tier deps ONLY. Stay as DIRECT reconciler fields: Client/Scheme, MaxConcurrentReconciles, PlannerPool/ExecutorPool, WatchNamespace, Recorder, SharedPVCName (Project; plan 41-09 adds it to the others as a direct field — keep the carrier out of that), TidePushImage—NO: TidePushImage IS dispatch-tier (consumed by triggerBoundaryPush) and goes IN the carrier. Budget-tier fields on ProjectReconciler (if any beyond the 8) stay direct. Exactly these 8 go in: Dispatcher, EnvReader, SigningKey, CredproxyImage, TidePushImage, ReporterImage, HelmProviderDefaults, PricingOverridesJSON.

In-body reference sweep sizes (verified at HEAD): milestone 14, phase 13, plan 17, project 17 occurrences of `r.<one of the 8 fields>` → `r.Deps.<field>`. boundary_push.go's maybeTriggerBoundaryPush wrappers reference r.TidePushImage / r.HelmProviderDefaults on the three planner reconcilers — those move to r.Deps.* too (boundary_push.go joins the sweep if the wrappers live there — grep before editing).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define PlannerReconcilerDeps and migrate the four reconciler structs</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/boundary_push.go</files>
  <read_first>
    - internal/controller/task_controller.go (TaskReconcilerDeps struct + doc comment ~:80-142 — the naming, doc-density, and "pool fields stay direct" scoping precedent to mirror)
    - internal/controller/project_controller.go (struct def ~:175-224 — the widest field set; identify which of its fields are the 8 carrier fields vs direct-keep fields like SharedPVCName/TidePushImage... verify TidePushImage presence per controller)
    - The other three struct defs (milestone ~:70-100, phase ~:65-95, plan ~:80-110 — locate by `grep -n 'type .*Reconciler struct' internal/controller/*.go`)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md (section "Item 8" — struct/wiring/lock-test excerpts)
  </read_first>
  <action>
    1. In dispatch_helpers.go, define `type PlannerReconcilerDeps struct` with exactly: Dispatcher dispatch.Dispatcher; EnvReader podjob.EnvelopeReader; SigningKey []byte; CredproxyImage string; TidePushImage string; ReporterImage string; HelmProviderDefaults ProviderDefaults; PricingOverridesJSON string. Doc comment mirrors TaskReconcilerDeps: populated at Manager wiring time, never mutated; pools/WatchNamespace/Recorder/SharedPVCName stay direct fields (concurrency limiters and per-reconciler config, not dispatch-tier deps); notes this carrier serves the four PLANNER reconcilers (Milestone/Phase/Plan/Project) and cross-references the cascade-8 forgotten-Dispatcher rationale.
    2. In each of the four reconciler structs: delete the 8 direct field declarations, add `Deps PlannerReconcilerDeps` with a one-line comment referencing this plan's consolidation. Keep every other field untouched.
    3. Sweep in-body references in the four controllers (+ boundary_push.go wrappers if they host r.TidePushImage/r.HelmProviderDefaults refs): `r.Dispatcher` → `r.Deps.Dispatcher`, etc., for all 8 names. Use per-file grep to enumerate (expected ~61 total) — do not regex-replace blind; a `r.Recorder` or `r.SharedPVCName` must NOT be swept.
  </action>
  <verify>
    <automated>go build ./internal/controller/... && go vet ./internal/controller/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type PlannerReconcilerDeps struct' internal/controller/dispatch_helpers.go` returns 1
    - For each of the four controllers: `grep -cE '^\s(Dispatcher|EnvReader|SigningKey|CredproxyImage|TidePushImage|ReporterImage|HelmProviderDefaults|PricingOverridesJSON)\s' <file>` returns 0 in the struct-def region (fields live only in the carrier)
    - `grep -rc 'r\.Deps\.' internal/controller/{milestone,phase,plan,project}_controller.go internal/controller/boundary_push.go | awk -F: '{s+=$2} END {print s}'` totals ≥ 55
    - `go build ./internal/controller/...` exits 0
  </acceptance_criteria>
  <done>Carrier defined; four structs and their bodies compile against Deps.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire main.go and extend the wiring-lock tests</name>
  <files>cmd/manager/main.go, cmd/manager/wiring_test.go, cmd/manager/wave_dispatcher_wiring_test.go</files>
  <read_first>
    - cmd/manager/main.go (reconciler construction block ~:390-575 — the four planner literals with their per-field CR-01 comments, and the TaskReconciler Deps literal ~:546-568 as the shape precedent)
    - cmd/manager/wiring_test.go (existing direct-field lock cases ~:53-93 — Project.Dispatcher, Milestone.Dispatcher/EnvReader, Phase.*, Plan.*, and the Task.Deps.Dispatcher case shape to replicate)
    - cmd/manager/wave_dispatcher_wiring_test.go (whether it constructs planner reconcilers with direct dispatch fields)
  </read_first>
  <action>
    1. main.go: construct `plannerDeps := controller.PlannerReconcilerDeps{Dispatcher: dispatcher, EnvReader: envReader, SigningKey: signingKey, CredproxyImage: credproxyImage, TidePushImage: tidePushImage, ReporterImage: reporterImage, HelmProviderDefaults: helmProviderDefaults, PricingOverridesJSON: pricingOverridesJSON}` once, after all eight source variables exist and before the first planner-reconciler literal (verify each variable's exact name in main.go — e.g. tidePushImage — before writing). Consolidate the per-reconciler CR-01 "Dispatcher must be assigned" comments into ONE comment on this construction (preserving the cascade-8 rationale text). Replace the 8 field lines in each of the four literals with `Deps: plannerDeps,`.
    2. wiring_test.go: convert the existing direct-field cases to `.Deps.<Field>` accessors (e.g. `(&controller.MilestoneReconciler{Deps: controller.PlannerReconcilerDeps{Dispatcher: dispatcher}}).Deps.Dispatcher == nil`), keeping each case's message text and adding cases so all four reconcilers lock at minimum Deps.Dispatcher and Deps.EnvReader non-nil.
    3. wave_dispatcher_wiring_test.go: update any planner-reconciler construction to the Deps form (WaveReconciler itself is untouched — it has no carrier).
  </action>
  <verify>
    <automated>go build ./cmd/manager/... && go test ./cmd/manager/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'plannerDeps := controller.PlannerReconcilerDeps{' cmd/manager/main.go` returns 1
    - `grep -c 'Deps:\s*plannerDeps' cmd/manager/main.go` returns 4 (grep -cE)
    - `grep -c 'Deps.Dispatcher' cmd/manager/wiring_test.go` returns ≥ 5 (4 planner + 1 task case)
    - `go test ./cmd/manager/... -count=1` exits 0
  </acceptance_criteria>
  <done>One construction site, four assignments, wiring locks extended and green.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep internal/controller test fixtures to the Deps form</name>
  <files>internal/controller/*_test.go (~20 files constructing Milestone/Phase/Plan/Project reconcilers — enumerate by grep, incl. suite_test.go)</files>
  <read_first>
    - Output of `grep -rln 'MilestoneReconciler{\|PhaseReconciler{\|PlanReconciler{\|ProjectReconciler{' internal/controller --include='*_test.go'` (the authoritative fixture list)
    - internal/controller/suite_test.go (the shared BeforeSuite constructions — highest blast radius, read first)
  </read_first>
  <action>
    In every test fixture constructing one of the four planner reconcilers, move assignments of the 8 carrier fields into `Deps: PlannerReconcilerDeps{...}` (same package — no import churn in internal/controller tests). Leave non-carrier fields (Client, Scheme, pools, WatchNamespace, Recorder, SharedPVCName, budget fields) where they are. Mechanical rule: only the 8 field names move; nothing else in a fixture changes.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... ` exits 0 (nothing anywhere still sets the deleted direct fields)
    - `go test ./internal/controller/... -count=1` exits 0 — full package, behavior invariance proof
    - `grep -rn 'Dispatcher:' internal/controller --include='*_test.go' | grep -v 'Deps' | grep -cv 'WaveReconciler\|TaskReconciler'` returns 0 (no planner fixture bypasses the carrier; Wave keeps its direct Dispatcher)
  </acceptance_criteria>
  <done>All fixtures on the carrier; full controller suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Wiring-shape refactor; the same dependency values flow to the same consumers — no input path or privilege change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06 | Denial of Service (silent unwired dep — the cascade-8 class) | planner dispatch tiers | mitigate | Single construction site + wiring-lock tests asserting Deps.Dispatcher/Deps.EnvReader non-nil for all four reconcilers; full suites green |
| T-41-06b | Information Disclosure | SigningKey []byte now inside a copied struct | accept | Same in-process memory semantics as today (struct copy at construction, never logged); no serialization surface added |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `go test ./cmd/manager/... ./internal/controller/... -count=1` green (matches the seed's item-8 Verify line)
- Grep: no reconciler constructed with a zero-value Deps in production wiring (the 4 `Deps: plannerDeps` assignments)
</verification>

<success_criteria>
- Seed item 8 closed with Project included (Pitfall 2 honored)
- Wiring locks extended — a future forgotten assignment fails tests, not a dogfood run
- REFAC-08 satisfied
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-06-SUMMARY.md` when done
</output>
