---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 07
type: execute
wave: 6
depends_on: [41-06]
files_modified:
  - internal/controller/level_status.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/controller/project_controller.go
autonomous: true
requirements: [REFAC-10]

must_haves:
  truths:
    - "One shared leaf status-mutation primitive implements the MergeFrom → set Phase → SetStatusCondition → Status().Patch twelve-line pattern; the 15 patch{Level}{Outcome} functions remain as thin wrappers with unchanged names and signatures (zero call-site churn)"
    - "One shared consumeApproveAndResume body implements the approve-annotation consume + Running/ApprovedByUser two-step; every byte-equivalent copy in milestone/phase/plan controllers delegates to it; the D-04 invariant (approve never jumps a level to Succeeded past its children) is structurally preserved — the helper returns to the Running branch, never touches ChildCount logic"
    - "One shared countChildren body backs the four countChild{Phases,Plans,Tasks,Milestones} wrappers"
    - "Behavior invariant: Gates/Approve/Boundary envtest suites and the full controller package green"
  artifacts:
    - path: "internal/controller/level_status.go"
      provides: "leaf status-mutation primitives: patch-level-status helper, consumeApproveAndResume, countChildren"
      min_lines: 60
  key_links:
    - from: "15 patch* wrappers across milestone/phase/plan/task controllers"
      to: "shared leaf primitive in level_status.go"
      via: "direct call with per-level phase/condition/result params"
      pattern: "patchLevelStatus|patchLevel"
    - from: "approve-consume sites (milestone :258/:715, phase :252/:634, plan :260/:725)"
      to: "consumeApproveAndResume"
      via: "delegation from the AwaitingApproval branches"
      pattern: "consumeApproveAndResume\\("
---

<objective>
Seed item 10: extract the duplicated leaf status-mutation primitives — the ~12-line `MergeFrom → set Phase → SetStatusCondition → Status().Patch` body repeated across 15 `patch{Milestone,Phase,Plan,Task}{Succeeded,Failed,Rejected,AwaitingApproval,FileTouchMismatch}` functions, the approve-annotation consume-and-resume two-step repeated at six sites across three controllers, and the namespace-List+ownerRef-filter `countChild*` body repeated four times — into shared functions following the codebase's own "one shared body + thin typed entry points" idiom (triggerBoundaryPush, spawnReporterIfNeeded).

Explicitly NOT a generic level-reconciler (seed's own boundary): reconcilePlannerDispatch/handleJobCompletion bodies stay intact — the ~10% that differs (ChildCount gating, rollup markers, fallbacks) encodes cascade fixes. Only leaves extract; bodies shrink indirectly.

Purpose: approval semantics (annotation-consume ordering, the D-04 "never jump to Succeeded past children" invariant) currently live in copies that must stay in lockstep by hand.
Output: two commits; Gates/Approve/Boundary suites + full package green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md

Per CONTEXT D-01: line numbers are hints — re-locate by symbol/grep.

Verified inventory at HEAD:
- patch* functions (15): milestone {Failed :867, Succeeded :883, Rejected :914, AwaitingApproval :934}; phase {Failed :777, Succeeded :793, Rejected :821, AwaitingApproval :838}; plan {Succeeded :844, Rejected :874, FileTouchMismatch :896, Failed :937, AwaitingApproval :955}; task {Rejected :854, AwaitingApproval :874}.
- approve-consume sites (gates.CheckApprove + gates.ConsumeApprove pairs): milestone :258+:715, phase :252+:634, plan :260+:725 (six planner-tier copies — the seed's "three copies" undercounted; plan_controller has the same pair); task :548 has an INVERTED-flow variant (`if !gates.CheckApprove`) inside its own AwaitingApproval branch — migrate it ONLY if its consume+resume steps are byte-equivalent after the guard inversion; otherwise leave inline with a cross-reference comment.
- countChild* (4): countChildPhases (milestone :847), countChildPlans (phase :757), countChildTasks (plan :983), countChildMilestones (project :1453).
- New-file decision (Claude's discretion per PATTERNS "home file TBD"): internal/controller/level_status.go — level_status naming matches the domain; dispatch_helpers.go stays dispatch-scoped.
- Post-41-06 note: reconciler dispatch deps live behind r.Deps now — the leaf helpers take plain params (client.Client, objects), not reconciler receivers, so they are unaffected; wrappers pass r.Client / r.Recorder as needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the patch-level-status leaf and the countChildren body</name>
  <files>internal/controller/level_status.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go, internal/controller/project_controller.go</files>
  <read_first>
    - All 15 patch* function bodies (enumerate via `grep -n 'func (r \*.*Reconciler) patch' internal/controller/*.go | grep -v _test` — read each; they differ only in phase value, condition type/reason/message, returned ctrl.Result, and plan's FileTouchMismatch extra param)
    - The 4 countChild* bodies (grep `func.*countChild`)
    - internal/controller/dispatch_helpers.go spawnReporterIfNeeded (~:68-107) and internal/controller/boundary_push.go triggerBoundaryPush (~:76-291) — the two "shared body + thin typed entry points" analogs whose shape this file mirrors
  </read_first>
  <action>
    1. Create internal/controller/level_status.go with a package-doc-style header comment naming the idiom (leaf status-mutation primitives shared by the level reconcilers; NOT a generic level-reconciler — cite the seed's boundary).
    2. Add a leaf helper (suggested name patchLevelStatus) taking plain params — derive the exact signature from the 15 bodies you just read; expected shape: (ctx, c client.Client, obj client.Object, conditions *[]metav1.Condition, phasePtr *string, newPhase string, cond metav1.Condition, result ctrl.Result) (ctrl.Result, error) — performing MergeFrom-deep-copy, phase assignment (skip when newPhase is empty — some patch funcs set only conditions), meta.SetStatusCondition, Status().Patch, returning (result, patch-error). If a Recorder event fires inside any wrapper today, keep the event in the wrapper — the leaf does status only.
    3. Convert all 15 patch* functions to thin wrappers calling the leaf, preserving each function's EXACT name, signature, phase value, condition (type/status/reason/message), requeue result, and any pre/post logic the leaf doesn't cover (e.g. patchPlanFileTouchMismatch's mismatch-pair formatting stays in its wrapper; only its patch step delegates). Use the 41-04 LevelPhase* constants for phase values.
    4. Add countChildren(ctx, c client.Client, namespace string, ownerUID types.UID, list client.ObjectList) int in level_status.go — namespace-scoped List + controller-ownerRef-UID filter, mirroring the four existing bodies (read them for the exact ownerRef matching predicate — do not invent a different one). Convert the four countChild* methods to thin wrappers.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run 'Gates|Approve|Boundary' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/controller/level_status.go` succeeds
    - Each of the 15 patch* functions' bodies contains a call to the shared leaf (grep the chosen leaf name across the four controllers: count ≥ 14 call sites; if patchPlanFileTouchMismatch cannot delegate cleanly, its exemption is documented in the SUMMARY with the reason)
    - `grep -c 'func countChildren' internal/controller/level_status.go` returns 1 and the four countChild* wrappers each call it
    - `go test ./internal/controller/... -run 'Gates|Approve|Boundary' -count=1` exits 0
  </acceptance_criteria>
  <done>15 wrappers + 4 wrappers over 2 shared bodies; targeted suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Extract consumeApproveAndResume and migrate the approve-consume copies</name>
  <files>internal/controller/level_status.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go</files>
  <read_first>
    - milestone_controller.go :251-284 (the canonical copy, INCLUDING the D-04/GATE-01 comment block above it — the invariant text the helper doc must carry) and :710-750 (the handleJobCompletion copy)
    - phase_controller.go :252-276 and :630-660; plan_controller.go :260-284 and :720-750 (confirm byte-equivalence of the consume+resume two-step before migrating each)
    - task_controller.go :545-560 (the inverted-guard variant — decide migrate-vs-comment per the rule in <context>)
  </read_first>
  <action>
    1. Add consumeApproveAndResume to level_status.go: takes (ctx, c client.Client, obj client.Object, conditions *[]metav1.Condition, phasePtr *string, level string, resumeMessage string) and performs the two-step exactly as the copies do today: (a) gates.ConsumeApprove(obj, level) → SetAnnotations → c.Patch with MergeFrom (annotation consume FIRST — T-04-G2 one-shot ordering is load-bearing); (b) separate MergeFrom status patch setting phase to LevelPhaseRunning + ConditionWaveOrLevelPaused False/ReasonApprovedByUser with the per-level message. Returns (ctrl.Result{Requeue: true}, error) mirroring today's "requeue immediately — the Running branch owns ChildCount-gated succession" contract. Doc comment MUST carry the D-04 invariant: this helper never advances to Succeeded; succession is the Running-branch/handleJobCompletion's job.
    2. Migrate the six planner-tier copies (milestone ×2, phase ×2, plan ×2) to call it, keeping each site's surrounding gates.CheckApprove guard and any site-specific post-resume logic (e.g. milestone's 37-06 artifact re-trigger while parked stays at the call site). If a handleJobCompletion copy differs beyond the two-step (rollup markers etc.), delegate only the two-step and keep the difference inline — do not force-fit.
    3. task_controller.go :548: migrate if byte-equivalent per the rule; otherwise add a one-line comment cross-referencing consumeApproveAndResume and why the Task variant stays inline.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'gates.ConsumeApprove' internal/controller/*.go | awk -F: '{s+=$2} END {print s}'` (non-test) totals ≤ 2: the shared helper + at most the Task inline variant
    - `grep -c 'func consumeApproveAndResume' internal/controller/level_status.go` returns 1 and its doc comment contains "Succeeded" in the D-04 never-jump statement
    - `go test ./internal/controller/... -count=1` exits 0 — full package (gates_test.go, annotation/approve flows, boundary suites all green)
  </acceptance_criteria>
  <done>Approval semantics live in one body; six-plus sites delegate; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator approve annotation → level resumption | The consume-and-resume two-step is the human-gate enforcement path; mis-ordering (status before annotation consume) would double-fire approvals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07a | Elevation of Privilege (gate double-fire / skip) | consumeApproveAndResume ordering | mitigate | Helper preserves annotation-consume-FIRST ordering (T-04-G2) and the D-04 never-jump-to-Succeeded contract structurally (returns to Running branch); gates/approve envtest suites green |
| T-41-07b | Tampering (status races) | patchLevelStatus MergeFrom semantics | mitigate | Leaf reproduces the exact MergeFrom + Status().Patch mechanism of the 15 originals — no Update/Patch mechanism swap (that is a separate concern per PATTERNS item-9 note) |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `go test ./internal/controller/... -run 'Gates|Approve|Boundary' -count=1` green after Task 1
- `go test ./internal/controller/... -count=1` full green after Task 2 (seed's item-10 Verify line)
</verification>

<success_criteria>
- Seed item 10 closed at the leaf level; flat state machines untouched (D-09 do-not list honored)
- 15 patch* names/signatures unchanged — zero call-site churn outside the wrappers
- REFAC-10 satisfied
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-07-SUMMARY.md` when done
</output>
