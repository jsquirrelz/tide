---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 09
type: execute
wave: 8
depends_on: [41-08]
files_modified:
  - internal/owner/label.go
  - internal/controller/git_writer.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/controller/project_controller.go
  - internal/controller/wave_controller.go
  - internal/controller/dispatch_helpers.go
  - internal/dispatch/podjob/backend.go
  - internal/dispatch/podjob/jobspec.go
  - cmd/dashboard/api/execution_dag.go
  - cmd/dashboard/api/waves.go
  - cmd/dashboard/api/waves_test.go
  - cmd/dashboard/api/informer_bridge_test.go
  - cmd/tide/cancel.go
  - cmd/tide/approve.go
  - cmd/tide/resume.go
  - cmd/tide/inspect_wave_run.go
  - cmd/manager/main.go
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "The label keys tideproject.k8s/wave-paused, wave-index, and attempt are constants in internal/owner next to LabelProject; no non-test production code ANYWHERE in internal/ or cmd/ spells them as raw literals — including the podjob dispatch backend, the dashboard API, and the tide CLI (the sweep is repo-wide, not controller-package-local)"
    - "All THREE private duplicate-const anti-patterns are deleted: git_writer.go's gitWriterProjectLabelKey, cmd/dashboard/api/waves.go's labelProject/labelWaveIndex pair, and cmd/tide/inspect_wave_run.go's labelProject/labelWaveIndex pair — every former consumer imports internal/owner directly (owner has zero project-internal imports; the 'import cycle' claim in execution_dag.go's comment is factually wrong and gets corrected)"
    - "--workspaces-pvc-name / TIDE_WORKSPACES_PVC_NAME is genuinely honored by EVERY dispatch Job: Milestone/Phase/Plan/Task reconcilers gain a SharedPVCName field wired from main.go's existing sharedPVCName variable (mirroring ImportReconciler), and project_controller.go's planner-dispatch site stops bypassing its own sharedPVCName() accessor — this fixes a latent config bug, not just style"
    - "The credproxy endpoint https://127.0.0.1:8443 and the planner default Iterations=20 are single named constants"
  artifacts:
    - path: "internal/owner/label.go"
      provides: "LabelWavePaused, LabelWaveIndex, LabelAttempt constants beside LabelProject"
      contains: "LabelWavePaused"
    - path: "cmd/manager/main.go"
      provides: "SharedPVCName wired to Milestone/Phase/Plan/Task reconcilers"
  key_links:
    - from: "cmd/tide/{cancel,approve,resume,inspect_wave_run}.go, cmd/dashboard/api/{waves,execution_dag}.go, internal/dispatch/podjob/{backend,jobspec}.go"
      to: "internal/owner label constants"
      via: "direct import of github.com/jsquirrelz/tide/internal/owner (owner imports only apimachinery/controller-runtime — no cycle is possible)"
      pattern: "owner\\.Label"
    - from: "cmd/manager/main.go sharedPVCName variable (~:219, TIDE_WORKSPACES_PVC_NAME)"
      to: "Milestone/Phase/Plan/Task reconciler SharedPVCName fields"
      via: "struct-literal wiring mirroring ImportReconciler (~:576-585, WR-03 comment pattern)"
      pattern: "SharedPVCName:\\s*sharedPVCName"
    - from: "6 dispatch Job builders"
      to: "r.sharedPVCName() accessor (fallback defaultSharedPVCName)"
      via: "PVCName field population"
      pattern: "PVCName:\\s*r\\.sharedPVCName\\(\\)"
---

<objective>
Seed item 11: centralize the repeated magic literals — label keys into `internal/owner` beside `LabelProject` and swept repo-wide (internal/controller, internal/dispatch/podjob, cmd/tide, cmd/dashboard/api — the checker-verified inventory is ~40 non-test sites, not the controller package alone), the credproxy endpoint and planner-iterations default into `dispatch_helpers.go` constants, and (the real bug) plumb the shared-PVC name from configuration into every dispatch Job. Today `--workspaces-pvc-name` is silently non-configurable for planner AND executor Jobs: only ProjectReconciler and ImportReconciler carry `SharedPVCName`, and even project_controller.go's own planner-dispatch site (~:1763) hardcodes `"tide-projects"` instead of calling its own accessor (RESEARCH Pitfall 3).

File-count note: 20 files is deliberate — Task 1 is a grep-enumerated mechanical constant substitution (byte-identical values, one owner.* identifier per former literal) isolated in its own commit; Tasks 2-3 touch only the 6 controller files + main.go. The wide count carries no cross-file semantic risk.

Purpose: (a) a non-default PVC name currently deploys a cluster where init/clone/push Jobs mount the configured PVC while all dispatch Jobs mount a nonexistent default — the literal wins over config; (b) three packages have independently re-derived private label consts (git_writer, dashboard waves API, tide CLI) — the exact drift anti-pattern the owner package exists to prevent.
Output: three commits; grep gates prove literal AND duplicate-const elimination; controller, dispatch, and cmd suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md

Per CONTEXT D-01: line numbers are hints — re-locate by symbol/grep.

Verified literal inventory at HEAD (checker-verified 2026-07-12, repo-wide):
- "tide-projects" PVCName hardcodes (6 dispatch sites): project_controller.go:1763, milestone_controller.go:512, phase_controller.go:471, plan_controller.go:506, task_controller.go:802 + :1477. NOT in scope: import_jobspec.go:53 `importWorkspaceVolume = "tide-projects"` (a pod-spec VOLUME name constant, coincidentally equal — leave), defaultSharedPVCName def (project_controller.go:122 — stays), prose comments.
- SharedPVCName field exists ONLY on ProjectReconciler (~:189-192, accessor sharedPVCName() ~:2158-2164) and ImportReconciler; Milestone/Phase/Plan/Task have NO field. main.go's `sharedPVCName := envOrDefault("TIDE_WORKSPACES_PVC_NAME", "tide-projects")` (~:219) is in scope at every construction site.
- Label literals — internal/controller: wave-paused → git_writer.go:77, task_controller.go:529, plan_controller.go:1495/1499/1526/1533; wave-index → project_controller.go:2545/2553, wave_controller.go:144/250; attempt → task_controller.go:1408; raw "tideproject.k8s/project" → task_controller.go:1109, plan_controller.go:1004/1562, plus git_writer.go:44's private `gitWriterProjectLabelKey` duplicate.
- Label literals — OUTSIDE internal/controller (the checker-flagged expansion): internal/dispatch/podjob/backend.go:387 (project), internal/dispatch/podjob/jobspec.go:224 (attempt), cmd/dashboard/api/execution_dag.go:74+86 (project), cmd/tide/cancel.go:110/123/136/149 (project), cmd/tide/approve.go:258/272/286/300/370/387/404/421 (project), cmd/tide/resume.go:223/248/273/311 (project).
- Private duplicate consts to DELETE (same anti-pattern as gitWriterProjectLabelKey): cmd/dashboard/api/waves.go:53-54 (labelProject/labelWaveIndex — used at waves.go:98/110 AND by identifier in waves_test.go:37-38/42 + informer_bridge_test.go:379-380) and cmd/tide/inspect_wave_run.go:38-39 (labelProject/labelWaveIndex — used at :80/:83).
- IMPORT-CYCLE MYTH: execution_dag.go:68-70's comment claims the raw literal avoids "an import cycle with internal/owner." False — internal/owner imports ONLY apimachinery/controller-runtime (verified: owner.go/label.go import blocks), so no project package can cycle with it. podjob/backend.go already imports internal/owner. Correct the comment during the sweep.
- Endpoint "https://127.0.0.1:8443": task_controller.go:1559, project_controller.go:1722, milestone_controller.go:467, plan_controller.go:460, phase_controller.go:425 (5 sites). Iterations=20: project_controller.go:1713, milestone_controller.go:461, phase_controller.go:419, plan_controller.go:454 (4 sites).
- Post-41-06 note: SharedPVCName is a per-reconciler CONFIG field — it goes DIRECT on each reconciler (mirroring ProjectReconciler/ImportReconciler), NOT into PlannerReconcilerDeps (the carrier doc from 41-06 says config fields stay direct).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Label-key constants in internal/owner + repo-wide non-test replacement (internal/ AND cmd/)</name>
  <files>internal/owner/label.go, internal/controller/git_writer.go, internal/controller/task_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/wave_controller.go, internal/dispatch/podjob/backend.go, internal/dispatch/podjob/jobspec.go, cmd/dashboard/api/execution_dag.go, cmd/dashboard/api/waves.go, cmd/dashboard/api/waves_test.go, cmd/dashboard/api/informer_bridge_test.go, cmd/tide/cancel.go, cmd/tide/approve.go, cmd/tide/resume.go, cmd/tide/inspect_wave_run.go</files>
  <read_first>
    - internal/owner/label.go (full file — LabelProject at ~:29-33 sets the doc-comment density and naming pattern to extend)
    - internal/controller/git_writer.go (:40-80 — the private gitWriterProjectLabelKey const and its use sites)
    - cmd/dashboard/api/waves.go (:30-60 — the private labelProject/labelWaveIndex const block, its explanatory comment at :34, and use sites :98/:110)
    - cmd/tide/inspect_wave_run.go (:35-90 — the second private const pair and its use sites :80/:83)
    - cmd/dashboard/api/execution_dag.go (:65-90 — the two raw-literal sites and the FALSE import-cycle comment at :68-70 that must be corrected, per <context>)
    - Output of `grep -rnE '"tideproject.k8s/(wave-paused|wave-index|attempt|project)"' internal cmd --include='*.go' | grep -v _test | grep -v 'internal/owner/label.go'` (authoritative site list — spans internal/controller, internal/dispatch/podjob, cmd/dashboard/api, cmd/tide)
  </read_first>
  <action>
    1. internal/owner/label.go: add three constants beside LabelProject with matching one-sentence doc comments naming the writer/reader convention: LabelWavePaused = "tideproject.k8s/wave-paused" (stamped by PlanReconciler wave-pause, read by task gate + git_writer), LabelWaveIndex = "tideproject.k8s/wave-index" (stamped on Tasks by the wave aggregator, read by project roll-up, the dashboard waves/execution-dag API, and the tide CLI), LabelAttempt = "tideproject.k8s/attempt" (stamped on dispatch Jobs by podjob jobspec, read by task retry accounting).
    2. Replace EVERY non-test production literal from the grep list — across internal/controller, internal/dispatch/podjob, cmd/dashboard/api, AND cmd/tide — with the owner.* constant. Add the `github.com/jsquirrelz/tide/internal/owner` import where missing (podjob/backend.go already has it; jobspec.go, the cmd/tide files, and the cmd/dashboard/api files likely need it — verify per file). internal/owner imports only apimachinery/controller-runtime, so no import cycle is possible from any of these packages.
    3. Delete all THREE private duplicate-const sets and repoint their consumers at owner.*: (a) git_writer.go's gitWriterProjectLabelKey → owner.LabelProject; (b) cmd/dashboard/api/waves.go:53-54's labelProject/labelWaveIndex → owner.LabelProject/owner.LabelWaveIndex at waves.go:98/110, updating the :34 comment that references the deleted consts; (c) cmd/tide/inspect_wave_run.go:38-39's labelProject/labelWaveIndex → owner.* at :80/:83.
    4. cmd/dashboard/api test files reference the deleted consts BY IDENTIFIER (waves_test.go:37-38/42, informer_bridge_test.go:379-380) — swap those identifiers to the raw string literals ("tideproject.k8s/project", "tideproject.k8s/wave-index") so tests keep pinning the wire format independently of the constants (same philosophy as the rest of the phase: tests keep literals).
    5. execution_dag.go: replace the two raw literals with owner.LabelProject and CORRECT the :68-70 comment — the import-cycle claim is factually wrong (see <context>); the new comment should say the constant is owner.LabelProject, stamped by internal/owner at create time.
    Value strings are byte-identical — this cannot change behavior; tests keep their literals (they pin the wire format).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... ./internal/dispatch/... ./cmd/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '"tideproject.k8s/(wave-paused|wave-index|attempt|project)"' internal cmd --include='*.go' | grep -v _test | grep -v 'internal/owner/label.go' | grep -vE ':[0-9]+:\s*//'` returns 0 lines (the proof — repo-wide)
    - `grep -c 'gitWriterProjectLabelKey' internal/controller/git_writer.go` returns 0
    - `grep -rn 'labelProject\|labelWaveIndex' cmd --include='*.go' | wc -l` returns 0 (both private const pairs gone, all identifier consumers — including test files and comments — repointed)
    - `grep -cE 'Label(WavePaused|WaveIndex|Attempt)\s*=' internal/owner/label.go` returns 3 (the three definitions)
    - `go test ./internal/controller/... ./internal/dispatch/... ./cmd/... -count=1` exits 0
  </acceptance_criteria>
  <done>All four label keys resolve to owner.* constants in production code across internal/ and cmd/; all three private duplicate-const pairs deleted; suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Plumb SharedPVCName to the 4 reconcilers and fix the 6 hardcoded dispatch sites</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go, internal/controller/project_controller.go, cmd/manager/main.go</files>
  <read_first>
    - internal/controller/project_controller.go (:185-195 the SharedPVCName field + doc comment; :2158-2164 the sharedPVCName() accessor with defaultSharedPVCName fallback; :1763 the bypassing planner-dispatch site)
    - cmd/manager/main.go (:215-225 the sharedPVCName variable; :576-585 the ImportReconciler wiring with its WR-03 comment — the exact wiring shape to replicate)
    - The 6 hardcoded sites (re-locate: `grep -n 'PVCName:.*"tide-projects"' internal/controller/*.go`)
  </read_first>
  <action>
    1. Add `SharedPVCName string` (direct field, NOT in Deps — see <context>) to MilestoneReconciler, PhaseReconciler, PlanReconciler, TaskReconciler, each with a doc comment mirroring ProjectReconciler's ("name of the cluster-wide PVC; defaults to tide-projects; configurable via --workspaces-pvc-name / TIDE_WORKSPACES_PVC_NAME").
    2. Add a `sharedPVCName() string` accessor on each of the four, byte-mirroring ProjectReconciler's (return r.SharedPVCName if non-empty, else defaultSharedPVCName). defaultSharedPVCName stays defined in project_controller.go (same package — all accessors reference it).
    3. Replace the 6 hardcoded `PVCName: "tide-projects"` dispatch-site lines with `PVCName: r.sharedPVCName()` — including project_controller.go:1763 (the accessor's own package was bypassing it).
    4. cmd/manager/main.go: add `SharedPVCName: sharedPVCName,` to the Milestone/Phase/Plan/Task reconciler literals, with a shared one-line comment mirroring ImportReconciler's WR-03 rationale (dispatch Jobs must mount the same PVC the rest of the controller writes to).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... ./cmd/manager/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '"tide-projects"' internal/controller --include='*.go' | grep -v _test | grep -vE ':[0-9]+:\s*//' ` returns exactly 2 lines: the defaultSharedPVCName definition (project_controller.go) and the importWorkspaceVolume volume-name constant (import_jobspec.go — out of scope, documented in SUMMARY)
    - `grep -c 'PVCName:\s*r.sharedPVCName()' internal/controller/*.go` totals 6 across the five controllers (grep -cE per file, sum)
    - `grep -c 'SharedPVCName:\s*sharedPVCName' cmd/manager/main.go` returns ≥ 6 (Project + Import pre-existing + 4 new — count with grep -cE)
    - `go test ./internal/controller/... ./cmd/manager/... -count=1` exits 0
  </acceptance_criteria>
  <done>Every dispatch Job mounts the configured PVC name; the latent config bug is closed; suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Constants for the credproxy endpoint and planner-iterations default</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/task_controller.go, internal/controller/project_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go</files>
  <read_first>
    - The 5 endpoint sites and 4 iterations sites (re-locate: `grep -rn '127.0.0.1:8443' internal/controller --include='*.go' | grep -v _test` and `grep -rn 'Iterations = 20' internal/controller --include='*.go'`)
    - internal/controller/milestone_controller.go (:455-462 — the explanatory comment above the iterations default that must survive the swap)
    - internal/controller/dispatch_helpers.go (existing const placement conventions)
  </read_first>
  <action>
    In dispatch_helpers.go add two documented constants: `credproxyEndpoint = "https://127.0.0.1:8443"` (the in-pod credproxy sidecar address every dispatch passes to subagent Jobs) and `defaultPlannerIterations = 20` (applied when the operator has not set Caps.Iterations). Replace the 5 endpoint literals and the 4 `plannerCaps.Iterations = 20` assignments with the constants, keeping surrounding comments (trim per-site prose that merely restates the constant's new doc comment).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '127.0.0.1:8443' internal/controller --include='*.go' | grep -v _test` returns exactly 1 line (the const definition)
    - `grep -rn 'Iterations = 20' internal/controller --include='*.go' | grep -v _test` returns 0 lines (all sites use defaultPlannerIterations)
    - `go test ./internal/controller/... -count=1` exits 0
  </acceptance_criteria>
  <done>Endpoint and iterations defaults are single-source; suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config (--workspaces-pvc-name / TIDE_WORKSPACES_PVC_NAME) → dispatch Job pod specs | Task 2 makes a previously-dead config knob live: the PVC name now flows from operator config into every dispatch Job's volume mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-09a | Information Disclosure (unintended PVC mount) | SharedPVCName → Job volume | accept | The name originates ONLY from the existing operator-controlled flag/env (same trust tier as image names, already trusted for ProjectReconciler init/clone/push Jobs since Plan 12 and ImportReconciler since WR-03); no CRD-sourced or tenant-sourced override is added — a hostile value requires cluster-operator privileges that already imply full access |
| T-41-09b | Denial of Service (config skew) | default-fallback divergence between reconcilers | mitigate | All five accessors share the single defaultSharedPVCName constant; wiring copies the one main.go variable — grep gates prove no literal remains to skew |
| T-41-09c | Tampering (label-selector drift) | wave-pause/wave-index/attempt/project label readers vs writers across controller, podjob backend, dashboard API, and CLI | mitigate | Byte-identical constant substitution repo-wide; controller + dispatch + cmd suites green prove reader/writer agreement; deleting the three private duplicate consts removes the drift surface itself |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- Seed's item-11 Verify line: `rg '"tide-projects"' internal | grep -v _test` → only the constant definitions (defaultSharedPVCName + the out-of-scope volume-name const, documented)
- Repo-wide label proof: `grep -rnE '"tideproject.k8s/(wave-paused|wave-index|attempt|project)"' internal cmd --include='*.go' | grep -v _test | grep -v 'internal/owner/label.go' | grep -vE ':[0-9]+:\s*//'` → 0 lines
- `go test ./internal/controller/... ./internal/dispatch/... ./cmd/... -count=1` green
</verification>

<success_criteria>
- Seed item 11 closed including the RESEARCH-expanded scope (field + accessor on 4 reconcilers, main.go wiring — Pitfall 3 honored, not a one-line literal swap) AND the checker-expanded label scope (repo-wide sweep across podjob, dashboard API, and CLI; all three private duplicate-const pairs deleted)
- `--workspaces-pvc-name` verifiably honored by all dispatch Jobs
- REFAC-11 satisfied
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-09-SUMMARY.md` when done
</output>
