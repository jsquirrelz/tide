---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/billing_halt.go
  - internal/controller/failure_halt.go
  - internal/controller/budget_blocked.go
  - internal/controller/dispatch_helpers.go
  - internal/subagent/anthropic/subagent.go
  - AGENTS.md
autonomous: true
requirements: [REFAC-02, REFAC-05, REFAC-12]

must_haves:
  truths:
    - "checkBillingHalt/checkFailureHalt/checkBudgetBlocked (and the already-halted guard in setFailureHaltIfNeeded) return identical verdicts via meta.IsStatusConditionTrue — Halt/Budget suites stay green"
    - "Zero mojibake byte sequences remain in dispatch_helpers.go and subagent.go; the change is comment-only"
    - "AGENTS.md Logging section codifies the codebase's actual lowercase-initial convention; zero logger.Info/Error call sites edited"
  artifacts:
    - path: "internal/controller/billing_halt.go"
      provides: "checkBillingHalt via meta.IsStatusConditionTrue, nil-safe wrapper preserved"
      contains: "meta.IsStatusConditionTrue"
    - path: "internal/controller/failure_halt.go"
      provides: "checkFailureHalt + setFailureHaltIfNeeded guard via meta.IsStatusConditionTrue"
      contains: "meta.IsStatusConditionTrue"
    - path: "internal/controller/budget_blocked.go"
      provides: "checkBudgetBlocked via meta.IsStatusConditionTrue"
      contains: "meta.IsStatusConditionTrue"
    - path: "AGENTS.md"
      provides: "Logging section matching the 88 real lowercase-initial call sites"
  key_links:
    - from: "internal/controller/billing_halt.go"
      to: "k8s.io/apimachinery/pkg/api/meta"
      via: "unaliased import (same style as dispatch_helpers.go)"
      pattern: "k8s.io/apimachinery/pkg/api/meta"
---

<objective>
Land three independently-shippable quick wins from the Phase 41 refactoring review: seed item 2 (replace hand-rolled condition loops with `meta.IsStatusConditionTrue`), seed item 5 (fix mojibake in comments), and seed item 12 (amend AGENTS.md's Logging section to codify the lowercase-initial convention the codebase already follows — D-05: doc change, zero code churn).

Purpose: finish extractions/conventions the codebase already started, with zero behavior change.
Output: three commits (one per task), each green on its targeted verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md

Per CONTEXT D-01: every cited line number is a hint, not an anchor — re-locate by symbol/grep before editing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hand-rolled condition loops with meta.IsStatusConditionTrue (seed item 2, REFAC-02)</name>
  <files>internal/controller/billing_halt.go, internal/controller/failure_halt.go, internal/controller/budget_blocked.go</files>
  <read_first>
    - internal/controller/billing_halt.go (checkBillingHalt ~:78-89)
    - internal/controller/failure_halt.go (checkFailureHalt ~:56-67 AND the already-halted guard loop inside setFailureHaltIfNeeded ~:93-98)
    - internal/controller/budget_blocked.go (checkBudgetBlocked ~:55-66)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md (section "Item 2" — exact current/target shapes and import style)
  </read_first>
  <action>
    Convert exactly 4 loop bodies to meta.IsStatusConditionTrue, keeping every nil-safe wrapper intact (the `if project == nil { return false }` guard is load-bearing — apimachinery's helper does not accept a nil object):
    1. checkBillingHalt: loop over project.Status.Conditions testing ConditionBillingHalt → `return meta.IsStatusConditionTrue(project.Status.Conditions, tideprojectv1alpha3.ConditionBillingHalt)`
    2. checkFailureHalt: same transform with ConditionFailureHalt
    3. setFailureHaltIfNeeded's "Already halted: idempotent no-op" loop: replace the for-range with `if meta.IsStatusConditionTrue(project.Status.Conditions, tideprojectv1alpha3.ConditionFailureHalt) { return nil }` — preserve the existing comment
    4. checkBudgetBlocked: same transform with ConditionBudgetBlocked
    Add the unaliased import "k8s.io/apimachinery/pkg/api/meta" to each file's import block (mirror the style in dispatch_helpers.go). Remove metav1 imports only if they become unused (goimports decides). Function signatures, names, and return semantics are unchanged (per D-06/item-2 shape: this is a body-only swap).
  </action>
  <verify>
    <automated>go build ./internal/controller/... && go test ./internal/controller/... -run 'Halt|Budget'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'meta.IsStatusConditionTrue' internal/controller/billing_halt.go` returns 1; `internal/controller/failure_halt.go` returns 2; `internal/controller/budget_blocked.go` returns 1
    - `grep -c 'for _, c := range project.Status.Conditions' internal/controller/billing_halt.go internal/controller/budget_blocked.go` returns 0 for each; the equivalent for-range condition loop is gone from failure_halt.go (`grep -cE 'for _, cond := range project.Status.Conditions' internal/controller/failure_halt.go` returns 0)
    - `go test ./internal/controller/... -run 'Halt|Budget'` exits 0 (billing_halt_regression_test.go + budget_blocked_regression_test.go green)
  </acceptance_criteria>
  <done>All four condition checks delegate to meta.IsStatusConditionTrue behind unchanged nil-safe wrappers; Halt/Budget regression suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix mojibake in comments (seed item 5, REFAC-05)</name>
  <files>internal/controller/dispatch_helpers.go, internal/subagent/anthropic/subagent.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go (13 mojibake lines — locate with `grep -n 'â' internal/controller/dispatch_helpers.go`)
    - internal/subagent/anthropic/subagent.go (9 mojibake lines — same grep)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md (section "Item 5" — anchors and D-06 overlap note)
  </read_first>
  <action>
    Restore the corrupted UTF-8 in comments: every occurrence is a double-encoded em-dash (—) or arrow (→) rendering as a garbled `â`-prefixed byte sequence. Locate all occurrences by grepping for the raw byte `â` in the two files; replace each with the correct character based on the sentence (em-dash in prose breaks, arrow in flow descriptions like "levels.{level}.{model,params} → Project default"). Comment-only change — zero code tokens touched. D-06 exception: this overlaps a Phase-40 REVIEW.md Info comment-drift finding in dispatch_helpers.go — resolve both in the same edit if they land on the same lines.
  </action>
  <verify>
    <automated>test "$(grep -c 'â' internal/controller/dispatch_helpers.go)" = "0" && test "$(grep -c 'â' internal/subagent/anthropic/subagent.go)" = "0" && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'â' internal/controller/dispatch_helpers.go` returns 0 (down from 13)
    - `grep -c 'â' internal/subagent/anthropic/subagent.go` returns 0 (down from 9)
    - `go build ./...` exits 0
    - The task commit touches only the two files (comment-only diff — no changes outside `//` lines)
  </acceptance_criteria>
  <done>Both files free of mojibake; build green; commit diff is comment-only.</done>
</task>

<task type="auto">
  <name>Task 3: Amend AGENTS.md Logging section to codify lowercase-initial (seed item 12, REFAC-12, D-05)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (the "### Logging" section, ~lines 213-230 — the "Start from a capital letter" bullet, example block, and upstream K8s SIG reference)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md (section "Item 12" — the real convention with sample call sites)
  </read_first>
  <action>
    Amend ONLY the "### Logging" section of AGENTS.md so the documented convention matches the codebase's actual, 100%-consistent practice (88 lowercase-initial logger.Info/Error sites, 0 uppercase). Specifically:
    1. Replace the "Start from a capital letter" bullet with: start log messages lowercase-initial (repo convention — deliberate deviation from the upstream K8s SIG style).
    2. Replace the example block's three messages with lowercase-initial equivalents drawn from real call sites (e.g. "spawned reporter Job", "dispatch held: project billing halt" — copy real messages from PATTERNS section Item 12, do not invent new phrasings).
    3. Keep the remaining bullets (no trailing period, active voice, past tense, specify object type, balanced key-value pairs) — they match practice.
    4. Add one sentence stating WHY messages must not churn: exact log strings are load-bearing — phase_gates_test.go and the CLAUDE.md runtime-gate verification protocols grep exact strings such as "creating job" and "dispatch held".
    5. Keep the upstream kubernetes/community reference link, annotated as the source this repo deviates from on initial-letter casing.
    Zero .go files change in this task (D-05: amend the doc, never the code).
  </action>
  <verify>
    <automated>test "$(grep -c 'Start from a capital letter' AGENTS.md)" = "0" && grep -qi 'lowercase' AGENTS.md && go build ./... </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Start from a capital letter' AGENTS.md` returns 0
    - `grep -ci 'lowercase' AGENTS.md` returns ≥ 1 (inside the Logging section)
    - `rg -l 'dispatch held|creating job' internal test .planning` produces the identical file list before and after this task (no load-bearing grep target moved)
    - The task commit touches only AGENTS.md (`git diff --name-only HEAD~1` shows exactly AGENTS.md)
  </acceptance_criteria>
  <done>AGENTS.md and the codebase agree on lowercase-initial; no code or test grep target changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | All three tasks are behavior-invariant refactor/doc changes inside the controller binary and repo docs; no input path, privilege, or network surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | billing/failure/budget halt checks (spend-enforcement path) | mitigate | Body-only swap to the apimachinery helper with identical truth-table; nil-safe wrappers preserved; billing_halt_regression + budget_blocked_regression suites must stay green (Task 1 verify) |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase — `k8s.io/apimachinery` is already vendored; no Package Legitimacy Gate applies (RESEARCH.md audit section) |
</threat_model>

<verification>
- `go build ./...` green after each task
- `go test ./internal/controller/... -run 'Halt|Budget'` green (Task 1)
- Mojibake greps return 0 in both files (Task 2)
- AGENTS.md grep assertions pass and the load-bearing log-grep file list is unchanged (Task 3)
</verification>

<success_criteria>
- Seed items 2, 5, and 12 closed with three independent commits
- Zero behavior change: no log message text, no function signature, no CRD artifact touched
- REFAC-02, REFAC-05, REFAC-12 satisfied per their seed Verify lines
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-01-SUMMARY.md` when done
</output>
