---
phase: 41-refactoring-review-non-breaking-cleanup-12-items
plan: 04
type: execute
wave: 3
depends_on: [41-03]
files_modified:
  - api/v1alpha3/shared_types.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/controller/project_controller.go
  - internal/controller/wave_controller.go
  - cmd/tide/approve.go
  - cmd/tide/watch.go
  - cmd/tide/resume.go
  - cmd/dashboard/api/projects.go
  - cmd/dashboard/api/plans.go
  - cmd/dashboard/api/waves.go
autonomous: true
requirements: [REFAC-01]

must_haves:
  truths:
    - "Every non-test Status.Phase-context literal for the six level values (Succeeded/Failed/Running/AwaitingApproval/Pending/ZeroMembers) in internal/controller and cmd is replaced by an api/v1alpha3 constant — a typo can no longer compile silently"
    - "Zero CRD artifact change: Status.Phase field types, json tags, and doc comments untouched; make manifests produces an empty diff (D-03: strictly non-breaking, no Enum marker)"
    - "The constants follow the Project precedent (documented const block in api/v1alpha3) using the seed's LevelPhase* naming"
  artifacts:
    - path: "api/v1alpha3/shared_types.go"
      provides: "LevelPhase* constant vocabulary for Milestone/Phase/Plan/Task/Wave Status.Phase"
      contains: "LevelPhaseSucceeded"
  key_links:
    - from: "internal/controller/*.go + cmd/**"
      to: "api/v1alpha3 LevelPhase* constants"
      via: "existing tideprojectv1alpha3 import at every consumer"
      pattern: "LevelPhase"
---

<objective>
Seed item 1: introduce typed Status.Phase constants for the five level kinds (Milestone/Phase/Plan/Task/Wave) in api/v1alpha3 and mechanically replace the ~117 non-test literal sites across internal/controller, cmd/tide, and cmd/dashboard. Project already has constants (`Phase*`, project_types.go:460-486) — this finishes the pattern for the other kinds. Field type stays `string`; no Enum marker; no CRD regen diff (D-03).

Purpose: `"Succeded"` compiles today and silently breaks indegree math, gate holds, and CLI behavior — constants close that failure class.
Output: three commits (constants; controller sweep; cmd sweep) with build/test green after each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-CONTEXT.md
@.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-PATTERNS.md

Per CONTEXT D-01: line numbers are hints — re-locate by symbol/grep.

Placement decision (documents D-03 application): the five level kinds share ONE Phase vocabulary, so the constants land as a single `LevelPhase*` block in `api/v1alpha3/shared_types.go` — the package's existing cross-kind types file (already home to ConditionParentUnresolved and the shared condition/reason vocabulary). Per-kind duplicate blocks would collide with Project's `Phase*` namespace in the same package (`PhasePhaseSucceeded` for the Phase kind is not acceptable), and the seed's own shape example names the constant `LevelPhaseSucceeded`. D-03's locked core — constants in api/v1alpha3, field stays string, NO kubebuilder Enum marker — is honored exactly.

Verified value vocabulary (from all non-test `Status.Phase = "..."` assignments + comparisons at HEAD):
- Shared by Milestone/Phase/Plan/Task/Wave: "Pending", "Running", "Succeeded", "Failed", "AwaitingApproval"
- Wave-only: "ZeroMembers" (wave_controller.go:177 aggregator)
- NOT part of this vocabulary: "" (empty-reset — leave as literal ""), "Dispatching" (compared in cmd/dashboard but never assigned by any controller — leave untouched, out of scope), Project's own values (constants already exist — see bonus site below)
- Bonus in-scope site: task_controller.go:432 compares `project.Status.Phase == "BudgetExceeded"` — replace with the EXISTING `tideprojectv1alpha3.PhaseBudgetExceeded`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the LevelPhase* constant block to api/v1alpha3/shared_types.go</name>
  <files>api/v1alpha3/shared_types.go</files>
  <read_first>
    - api/v1alpha3/project_types.go (lines ~458-486 — the Project Phase* const-block pattern: doc-comment density, one comment per constant)
    - api/v1alpha3/shared_types.go (full file — pick a placement consistent with its existing const-block organization)
  </read_first>
  <action>
    Add one documented const block: LevelPhasePending = "Pending", LevelPhaseRunning = "Running", LevelPhaseSucceeded = "Succeeded", LevelPhaseFailed = "Failed", LevelPhaseAwaitingApproval = "AwaitingApproval", LevelPhaseZeroMembers = "ZeroMembers". Block doc comment: these are the Status.Phase values shared by the five level kinds (Milestone/Phase/Plan/Task/Wave), distinct from Project's Phase* vocabulary; per-constant comments follow the project_types.go density (one sentence each; ZeroMembers documented as Wave-only, set by the wave aggregator when TaskRefs is empty). Do NOT touch any struct field, json tag, field doc comment, or kubebuilder marker — const blocks do not feed controller-gen, so the CRD manifests must not change.
  </action>
  <verify>
    <automated>go build ./api/... && make manifests && git diff --exit-code config/crd charts/tide-crds</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'LevelPhase' api/v1alpha3/shared_types.go` returns ≥ 6 (six definitions)
    - `make manifests` then `git diff --exit-code config/crd charts/tide-crds` exits 0 (zero generated-artifact drift)
    - `go build ./api/...` exits 0
  </acceptance_criteria>
  <done>Constants exist, documented, with provably zero CRD-generation impact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep internal/controller non-test literal sites</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go, internal/controller/project_controller.go, internal/controller/wave_controller.go, internal/controller/*.go (any other non-test file a vocabulary grep surfaces, e.g. git_writer.go, artifact_push.go, boundary_push.go)</files>
  <read_first>
    - Output of `grep -rnE '"(Succeeded|Failed|Running|AwaitingApproval|Pending|ZeroMembers)"' internal/controller --include='*.go' | grep -v _test` (the authoritative site list — enumerate before editing; expect ~90 code sites plus comment lines)
    - .planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-RESEARCH.md (Pitfall 4 — why this is per-kind scripted with a build between passes)
  </read_first>
  <action>
    Replace every Status.Phase-CONTEXT literal with the matching tideprojectv1alpha3.LevelPhase* constant: assignments (`ms.Status.Phase = "Running"`), comparisons (`task.Status.Phase == "Succeeded"`), switch cases (`case "Succeeded":` in wave_controller.go), and phase-local variables that feed Status.Phase (wave_controller.go:177/180 `phase = "ZeroMembers"` / `"Succeeded"`). Also task_controller.go:432: `"BudgetExceeded"` → existing `tideprojectv1alpha3.PhaseBudgetExceeded`. Work one controller file at a time with `go build ./internal/controller/...` between files (Pitfall 4 discipline). Do NOT touch: condition Reason strings, Event reasons, log MESSAGE text, label values, empty-string resets (`Phase = ""`), comments (prose may keep quoted values), or anything in *_test.go files (tests keep literals — they independently pin the wire format).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '"(Succeeded|Failed|Running|AwaitingApproval|Pending|ZeroMembers)"' internal/controller --include='*.go' | grep -v _test | grep -vE ':[0-9]+:\s*(//|\*)'` returns 0 lines (comment-filtered — no code-position literal survives)
    - `grep -c '"BudgetExceeded"' internal/controller/task_controller.go` returns 0
    - `go test ./internal/controller/... -count=1` exits 0 (full package — behavior invariance is the contract)
  </acceptance_criteria>
  <done>internal/controller consumes only constants for the level-phase vocabulary; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep cmd/tide and cmd/dashboard literal sites</name>
  <files>cmd/tide/approve.go, cmd/tide/watch.go, cmd/tide/resume.go, cmd/dashboard/api/projects.go, cmd/dashboard/api/plans.go, cmd/dashboard/api/waves.go</files>
  <read_first>
    - Output of `grep -rnE '"(Succeeded|Failed|Running|AwaitingApproval|Pending|ZeroMembers)"' cmd --include='*.go' | grep -v _test` (authoritative list; known sites: approve.go:373-424 AwaitingApproval ×4, watch.go:124 switch, resume.go:229-317 Failed ×4, projects.go:180/326, plans.go:165, waves.go:182)
  </read_first>
  <action>
    Same mechanical replacement as Task 2 in the cmd tier, using each file's existing api/v1alpha3 import alias (check per file — cmd may alias differently than internal/controller). watch.go:124's switch case list (`case "Running", "Pending", "AwaitingApproval", "":`) converts the three named values and keeps `""` literal. plans.go:165 (`c.Phase == "Dispatching" || c.Phase == "Running"`): convert ONLY "Running" — "Dispatching" stays a literal (never assigned by controllers; not in the minted vocabulary; flag it in the SUMMARY as a latent dead comparison for a future finding). Comments keep prose literals.
  </action>
  <verify>
    <automated>go build ./... && go test ./cmd/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '"(Succeeded|Failed|Running|AwaitingApproval|Pending|ZeroMembers)"' cmd --include='*.go' | grep -v _test | grep -vE ':[0-9]+:\s*(//|\*)'` returns 0 lines
    - `grep -c '"Dispatching"' cmd/dashboard/api/plans.go cmd/dashboard/api/waves.go` returns unchanged counts (untouched, documented in SUMMARY)
    - `go build ./... && go test ./cmd/... -count=1` exits 0
  </acceptance_criteria>
  <done>cmd tier consumes only constants for the level-phase vocabulary; the seed's Verify grep (`rg '"Succeeded"' internal/controller cmd`) is clean of non-test code hits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Literal-to-constant substitution with byte-identical string values; no input path, schema, or privilege surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Tampering (silent-typo class) | Status.Phase comparisons driving gate holds and indegree math | mitigate | This item IS the mitigation — constants make phase-value typos a compile error; full controller + cmd suites prove value-invariance of the sweep |
| T-41-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `make manifests && git diff --exit-code config/crd charts/tide-crds` — zero generated drift (D-03 non-breaking proof)
- `go test ./internal/controller/... ./cmd/... -count=1` green
- Comment-filtered vocabulary greps return 0 in internal/controller and cmd non-test code
</verification>

<success_criteria>
- Seed item 1 closed per its Verify line, updated with comment-filtered grep hygiene
- Zero CRD/schema change; zero test-file churn
- REFAC-01 satisfied
</success_criteria>

<output>
Create `.planning/phases/41-refactoring-review-non-breaking-cleanup-12-items/41-04-SUMMARY.md` when done
</output>
