---
phase: "02.2"
plan: "11"
type: execute
wave: 10
gap_closure: true
depends_on: ["02.2-10"]
files_modified:
  - test/integration/kind/testdata/three-task-wave.yaml
  - test/integration/envtest/indegree_test.go
  - Makefile
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, cascade-9-closure-target, sub-class-a-deadline, sub-class-b-spec-flake, sub-class-c-walltime, runtime-verification, kind-integration, phase-02.2, tactical]

threat_model_summary:
  - "T-02.2-32 (mitigate — cascade-9A fix correctness): increasing the Task `caps.wallClockSeconds` in the wave-test fixture must produce `activeDeadline = wallClockSeconds + DefaultWallClockGraceSeconds(60) ≥ 180` per `internal/dispatch/podjob/jobspec.go:113`. Selected fix site: `test/integration/kind/testdata/three-task-wave.yaml` Tasks alpha/beta/gamma each get `spec.caps.wallClockSeconds: 120` (yielding activeDeadline=180s — matches Plan 02.2-08's credproxy pod-wait budget convention; gives the credproxy initContainer + tide-credproxy sidecar startup ≥120s headroom). Mitigation: smallest-surface fix (3 fixture-line additions, zero production-code touched); preserves Option-B-test-only contract; mirrors the established `caps: wallClockSeconds: 10` pattern already in `caps_test.go:81-82`. Production-code fix (Option (b) in §Section 5 line 261) REJECTED because it would touch `internal/dispatch/podjob/jobspec.go` and require a profile-config surface that has no caller yet. Acceptance: `grep -c 'wallClockSeconds: 120' test/integration/kind/testdata/three-task-wave.yaml` returns exactly 3; runtime reproducer in T2 shows `grep -c 'DeadlineExceeded' /tmp/02.2-11-clean-run.log` returns 0; jobs in tide-int-test namespace transition Pod through Pending→Running→Succeeded without hitting deadline."
  - "T-02.2-33 (mitigate — cascade-9B fix correctness): bumping the Wave-roll-up `Eventually` from `20s` to `60s` at `test/integration/envtest/indegree_test.go:232` keeps the Option-B-test-only contract (no production `reconcileDispatch` change). Mitigation: literal-string change with byte-level acceptance: `grep -cE 'Eventually\\(func\\(\\) string' indegree_test.go` count unchanged; the specific Wave-roll-up `Eventually` block's timeout argument is `60s` (not the prior `20s`); the polling interval `500ms` is preserved. Risk: a 60s budget might mask a real production-side requeue-loop bug. Acceptance gate compensates by requiring T2 to assert `tail -30 /tmp/02.2-11-envtest.log` shows the spec passing at ≤ 45s observed wall (not just within the 60s budget) — the spec passes WITH HEADROOM, proving the bump is for race-tolerance, not for masking a 60s+ regression. Option (c) at line 270-271 (fix upstream reconcileDispatch requeue load) REJECTED as out of cascade-9 scope per planner authority limits (the requeue is a side-effect of Plan 02.2-10's correct Dispatcher wiring; addressing the upstream cause is a Phase 03+ production-code follow-up)."
  - "T-02.2-34 (accept — cascade-9C safety-net wall-time budget): Makefile `timeout 600s` → `timeout 1800s` is a single-line safety net per §Section 5 line 305-313. If sub-classes A+B closure alone drops wall-time under 600s, the 1800s headroom is unused — acceptable. If they do not, the 1800s budget absorbs the residual wall-time over-budget without re-opening cascade-9. Acceptance: `grep -c 'timeout 1800s' Makefile` returns ≥ 1 AND `grep -c 'timeout 600s' Makefile` returns 0; runtime reproducer in T2 records `clean_run_wall_seconds` and `rerun_wall_seconds`; APPROVED gate requires wall ≤ 1800s on both runs; T2 records `wall_under_600s: true|false` annotation flagging whether A+B alone met the original aspiration."
  - "T-02.2-35 (accept — cascade-10 surfacing risk): the cascade-9 closure removes the three identified blockers but cannot prevent a different failure mode from surfacing under a now-correctly-dispatching kind run. Cascade-10 could be: (a) AC1 Wave-roll-up still fails even with 60s `Eventually` if requeue load is heavier than estimated → sub-class B mitigation insufficient; (b) the now-passing Pods produce output that exposes a downstream WaveReconciler bug (Task→Wave roll-up race in real kind); (c) `KEEP_KIND_CLUSTER=true` rerun trips an idempotency bug in Job naming or PVC reuse; (d) a flag-mismatch or charts/values regression from accumulated `helm install` state under the longer suite. Per CLAUDE.md Operating Note #1, this plan is NOT tagged as a chain-terminator candidate. Acceptance: if T2 records `gate_decision: BLOCKED`, the artifact §Section 5 MUST classify cascade-10 per T-02.2-16 taxonomy (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) AND identify the specific failing spec(s) AND propose Plan 02.2-12's fix shape inline. If APPROVED, the artifact notes 'cascade chain potentially terminated at iteration 11; further runs will validate stability' without claiming finality."
  - "T-02.2-36 (mitigate — production wiring untouched): Plan 02.2-10's `cmd/manager/main.go` Dispatcher wiring landed at commit `bd67a3b` and is the architectural foundation that made cascade-9 surfacing possible. This plan MUST NOT touch that file or risk regressing cascade-8 closure. Acceptance gate: `git status --porcelain cmd/manager/main.go` returns empty; `grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go` returns 2 (PlanReconciler + TaskReconciler both still wired); `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returns 1. Anti-pattern guard: same gate applies to `charts/tide/values.yaml` (FIXED contract per CLAUDE.md Execute-Don't-Ask exception) — `git status --porcelain charts/` returns empty."

must_haves:
  truths:
    - "`test/integration/kind/testdata/three-task-wave.yaml` has `spec.caps.wallClockSeconds: 120` added to each of the three Tasks (alpha, beta, gamma) — three additions total; the deterministic activeDeadline computed by `BuildJobSpec` is `120 + DefaultWallClockGraceSeconds(60) = 180s`, giving credproxy initContainer + sidecar startup ≥120s headroom under kind (sub-class A closed)"
    - "`test/integration/envtest/indegree_test.go` Wave-roll-up spec timeout at the line currently containing `\"20s\", \"500ms\"` (the Eventually returning `w.Status.Phase` and asserting `Equal(\"Succeeded\")` inside the `Describe(\"Wave roll-up: Succeeded when all Tasks Succeeded\", Label(\"SUB-02\"))` block) is bumped from `\"20s\"` to `\"60s\"`; polling interval `\"500ms\"` preserved; other `Eventually` timeouts in the same file are unchanged (sub-class B closed)"
    - "`Makefile` `test-int` recipe `timeout 600s` (line 124 per plan-time inspection) is bumped to `timeout 1800s` (sub-class C safety-net closed); no other Makefile lines modified"
    - "Production source UNTOUCHED: `git status --porcelain internal/` returns empty AND `git status --porcelain cmd/manager/main.go` returns empty AND `git status --porcelain api/` returns empty AND `git status --porcelain pkg/` returns empty (Plan 02.2-10's Dispatcher wiring at `cmd/manager/main.go` preserved; cascade-8 closure NOT regressed)"
    - "Chart UNTOUCHED: `git status --porcelain charts/` returns empty (FIXED-contract anti-pattern from CLAUDE.md Execute-Don't-Ask exception preserved; binary catches up to chart, never reverse)"
    - "Cascade-5/6/7 regressions guarded: `git status --porcelain test/integration/kind/credproxy_test.go` returns empty; `git status --porcelain test/integration/kind/suite_test.go` returns empty; `git status --porcelain test/integration/kind/caps_test.go` returns empty; `git status --porcelain test/integration/kind/output_test.go` returns empty; `git status --porcelain test/integration/kind/failure_test.go` returns empty; `git status --porcelain test/integration/kind/wave_test.go` returns empty (only `testdata/three-task-wave.yaml` modified inside `test/integration/kind/`)"
    - "Build + vet gates pass after T1 edits: `go build ./...` exits 0 AND `go vet ./...` exits 0 AND `go test -count=1 -run TestBuildJobSpec ./internal/dispatch/podjob/...` exits 0 (existing `TestBuildJobSpec_ActiveDeadlineSeconds_HasGrace` is unaffected because it constructs synthetic BuildOptions, not the wave-fixture Tasks)"
    - "T2 worktree pre-merge cwd-drift check (per CLAUDE.md Operating Notes): if T2 ran in a worktree, `UNTRACKED=$(git status --porcelain | grep '^?? ' | grep '02.2-11-' || true)` returns empty before merge to main; any 02.2-11-named artifacts in the main worktree must be diffed against the worktree branch and duplicates removed before commit"
    - "T2 image-rebuild verification (per CLAUDE.md Working Rule #4): the manager image digest changed between Plan 02.2-10's clean run and Plan 02.2-11's clean run (record both digests in `02.2-11-VERIFICATION.md` frontmatter as `manager_image_digest_prior` and `manager_image_digest_current`); the manager Pod has rotated (record both Pod ages — current run's Pod is younger than the time elapsed since image load); without this gate, the new fixture and Makefile edits cannot be claimed to have shaped the runtime evidence"
    - "`02.2-11-VERIFICATION.md` exists with frontmatter including `phase: 02.2`, `plan: 11`, `task: 2`, `artifact: verification`, `gate_decision: APPROVED|BLOCKED`, AND SIX runtime-observation fields: `clean_run_wall_seconds`, `rerun_wall_seconds`, `credproxy_pod_wait_observed_seconds`, `ac1_wave_rollup_observed_seconds` (NEW — sub-class B closure indicator), `deadline_exceeded_count` (NEW — sub-class A closure indicator; APPROVED requires exactly 0), `wall_under_600s` (boolean — TRUE if A+B alone met the aspiration; FALSE if the 1800s safety-net absorbed residual wall-time)"
    - "APPROVED gate requires ALL 7 conditions on BOTH clean run AND idempotent rerun: (1) wall ≤ 1800s; (2) Manager Pod `STATUS=Running` AND restarts ≤ 1; (3) `grep -c 'DeadlineExceeded' /tmp/02.2-11-clean-run.log` returns exactly 0 (sub-class A closed); (4) `grep -E '\"creating job\"|\"dispatch\"' /tmp/02.2-11-clean-run.log` returns ≥ 1 line AND `kubectl get jobs --all-namespaces` shows Jobs in the test namespaces during the run (dispatch still active — cascade-8 closure preserved); (5) 18/18 Layer A envtest specs PASS in the clean run's Layer A phase (the AC1 Wave-roll-up Succeeded spec passes — sub-class B closed); (6) PVC `tide-projects` Bound RWO; (7) manager log shows zero `flag provided but not defined` AND zero `no project found in namespace <ns>` for any test namespace (cascades 2/3/5/6/7 not regressed)"
    - "If `02.2-11-VERIFICATION.md` records `gate_decision: APPROVED`, this is the **FIRST APPROVED gate in Phase 02.2's 11-plan history**: Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, 02.2-08 Task 3, 02.2-09 Task 3, AND 02.2-10 Task 3 ALL halted on BLOCKED gates. Task 3 of THIS plan would be the first to fire, redirecting Plan 02.2-02's `depends_on: [\"02.2-01\"]` → `[\"02.2-11\"]` and all 31 references to `02.2-01-VERIFICATION.md` → `02.2-11-VERIFICATION.md`"
    - "If `02.2-11-VERIFICATION.md` records `gate_decision: BLOCKED`, Task 3 does NOT fire — Plan 02.2-02 stays gated on its current wiring; cascade-10 classified per T-02.2-16 taxonomy in §Section 5 with a proposed Plan 02.2-12 fix shape (per CLAUDE.md Operating Note #1, do NOT predict this is the chain terminator)"

  artifacts:
    - path: "test/integration/kind/testdata/three-task-wave.yaml"
      provides: "Three `spec.caps.wallClockSeconds: 120` additions on Tasks alpha, beta, gamma — closes cascade-9A by making `BuildJobSpec` compute `activeDeadlineSeconds = 120 + 60 = 180s` (vs the 0+60=60s observed in Plan 02.2-10's clean run that caused DeadlineExceeded for the credproxy initContainer)"
      contains: "wallClockSeconds: 120"
    - path: "test/integration/envtest/indegree_test.go"
      provides: "Wave-roll-up SUB-02 `Eventually` timeout bump `20s` → `60s` at the spec returning `w.Status.Phase` and asserting `Equal(\"Succeeded\")` inside the `Describe(\"Wave roll-up: Succeeded when all Tasks Succeeded\")` block — closes cascade-9B by giving the spec 3× the prior budget to absorb `reconcileDispatch` requeue-load competition"
      contains: '"60s", "500ms"'
    - path: "Makefile"
      provides: "`timeout 600s` → `timeout 1800s` on the `test-int` target's `go test` line (line 124 per plan-time inspection) — safety-net for cascade-9C; A+B closure may make this headroom unused (acceptable per T-02.2-34)"
      contains: "timeout 1800s"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md"
      provides: "Runtime verification artifact with `gate_decision: APPROVED|BLOCKED` frontmatter AND six runtime-observation fields (clean+rerun wall, credproxy_pod_wait, ac1_wave_rollup, deadline_exceeded_count, wall_under_600s); FIRST-APPROVED-eligible chain artifact"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-11\"] + 31 references redirected from 02.2-01-VERIFICATION.md to 02.2-11-VERIFICATION.md (HARD-GATED on T2 APPROVED) — FIRST APPROVED Task-3 rewire fire in Phase 02.2's history if T2 returns APPROVED"
      contains: "02.2-11"

  key_links:
    - from: "test/integration/kind/testdata/three-task-wave.yaml `spec.caps.wallClockSeconds: 120`"
      to: "internal/dispatch/podjob/jobspec.go:108-113 activeDeadline computation"
      via: "BuildJobSpec reads `opts.Task.Spec.Caps.WallClockSeconds` and adds `DefaultWallClockGraceSeconds=60` → activeDeadlineSeconds=180s; the credproxy initContainer + tide-credproxy sidecar complete startup under this budget"
      pattern: 'wallClockSeconds:\\s+120'
    - from: "test/integration/envtest/indegree_test.go Wave-roll-up Eventually `60s`"
      to: "internal/controller/wave_controller.go reconcile loop"
      via: "the spec patches each Task.Status.Phase=Succeeded then asserts `w.Status.Phase == \"Succeeded\"` within 60s; the bumped budget accommodates the reconcileDispatch-driven requeue load Plan 02.2-10 introduced"
      pattern: '"60s",\\s*"500ms"'
    - from: "Makefile `timeout 1800s`"
      to: "go test ./test/integration/... -v -timeout=5m -ginkgo.v"
      via: "outer wall budget is 1800s; inner Ginkgo `-timeout=5m` per-spec cap unchanged; A+B closure should drop wall under 600s (T-02.2-34 acceptance flag annotates whether this aspiration met)"
      pattern: 'timeout 1800s'
    - from: "02.2-11-VERIFICATION.md gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 of THIS plan wires Plan 02.2-02 (HARD-GATED on APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md depends_on field (post-Task-3)"
      to: "this plan's verification artifact"
      via: "frontmatter `depends_on: [\"02.2-11\"]` — bumped only if T2 returns APPROVED"
      pattern: 'depends_on: \\[.{0,5}02.2-11.{0,5}\\]'
    - from: "02.2-11-VERIFICATION.md manager_image_digest_current"
      to: "Plan 02.2-10's prior manager_image_digest_prior"
      via: "image-rebuild verification per CLAUDE.md Working Rule #4 — the binary must have rebuilt since the prior cascade-9-surfacing run, AND the manager Pod must have rotated; without digest+age change, the fixture+Makefile edits cannot be claimed to have shaped the runtime evidence"
      pattern: 'manager_image_digest_(prior|current):'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-10-VERIFICATION.md` §Section 5 (eleventh iteration in Phase 02.2's cascade chain — but the FIRST iteration since cascade-8 (production-wiring-gap) closed at the architectural level). Plan 02.2-10's Option-δ Dispatcher wiring landed at commit `bd67a3b` and produced `dispatch_active: true`: 2 Jobs were created in `tide-int-test` for the wave-test fixture Tasks alpha + beta. This is the major-milestone outcome that confirms Plan 02.2-10 was structurally correct. However, that correctness exposed cascade-9 with three sub-classes (A: job activeDeadlineSeconds=60 too tight; B: Layer A envtest SUB-02 Wave-roll-up spec-flake; C: 1486s wall vs 600s Makefile budget). This plan bundles all three sub-classes into a single tactical iteration mirroring the bundle-class pattern from Plans 02.2-08 (2 Eventually bumps) and 02.2-09 (3 fixture call sites).

**Why bundled into one plan:** the three sub-classes have independent target sites with zero file-overlap (`test/integration/kind/testdata/three-task-wave.yaml` for A; `test/integration/envtest/indegree_test.go` for B; `Makefile` for C), so a single Task-1 edit pass lands all three atomically. Splitting into three plans would inflate the planning overhead without any runtime-verification independence benefit — all three must be tested together by the same `make test-int` reproducer.

**Sub-class A — fix shape decision (per `02.2-10-VERIFICATION.md` §Section 5 lines 249-261, two options spelled out):**

- **Option (a) — increase fixture Task budget (CHOSEN):** add `spec.caps.wallClockSeconds: 120` to the three Tasks in `test/integration/kind/testdata/three-task-wave.yaml`. The `BuildJobSpec` function at `internal/dispatch/podjob/jobspec.go:107-113` computes `activeDeadline = opts.Task.Spec.Caps.WallClockSeconds + DefaultWallClockGraceSeconds(60)`. With no `caps` set today, wallClockSeconds=0 → activeDeadline=60s — exactly the value observed in Plan 02.2-10's evidence at line 250. With `wallClockSeconds: 120`, activeDeadline=180s — sufficient for the credproxy initContainer + tide-credproxy sidecar startup observed in Plan 02.2-10's evidence. The 120s value matches Plan 02.2-08's credproxy_test pod-wait budget convention (T-02.2-21 carry-forward) and the convention already established in `caps_test.go:81-82`. Smallest surface — 3 fixture-line additions; zero production-code touched; preserves Option-B-test-only contract.
- **Option (b) — make activeDeadlineSeconds configurable per profile (REJECTED):** would touch `internal/dispatch/podjob/jobspec.go` and require a profile-config surface that has no caller yet. Per planner authority limits (CLAUDE.md), the "test vs production profile" surface is a Phase 03+ design question, not a cascade-9 closure scope item. REJECTED.

**Sub-class B — fix shape decision (per §Section 5 lines 263-272, three options spelled out):**

- **Option (a) — bump Eventually 20s → 60s (CHOSEN):** at `test/integration/envtest/indegree_test.go:232`, change the Wave-roll-up SUB-02 `Eventually` timeout from `"20s", "500ms"` to `"60s", "500ms"`. Plan 02.2-10's evidence shows the spec passes in isolation under SUB-02 label filter but flakes under full-suite execution because `reconcileDispatch` requeues add work-queue load (the requeue is a side-effect of Plan 02.2-10's correct Dispatcher wiring — Tasks in the indegree namespace lack a parent Project, so `resolveProject` errors and TaskReconciler requeues). 60s is 3× the prior 20s budget, giving headroom for the race-tolerance without masking a hypothetical 60s+ regression (T-02.2-33 acceptance asserts observed wall ≤ 45s — the spec passes WITH HEADROOM, not at the budget edge).
- **Option (b) — add stabilization helper (REJECTED for surface):** larger surface (helper function + uses in multiple specs); same race-tolerance outcome as (a) with more code.
- **Option (c) — fix upstream reconcileDispatch requeue load (REJECTED for scope):** would touch `internal/controller/task_controller.go` and require addressing the no-parent-Project requeue-loop semantics. Per planner authority limits, this is a Phase 03+ production-code follow-up — the requeue is functionally correct (errors should retry); only the test-side budget is too tight. Plan 02.2-11 documents this as a deferred follow-up.

**Sub-class C — fix shape decision (per §Section 5 lines 273-281 + 305-313, two options spelled out):**

- **Option (a) — bump Makefile `timeout 600s` → `timeout 1800s` (CHOSEN as safety-net):** single-line Makefile change; mirrors the surface-shape of Plan 02.2-06. The prior 1486s wall-time observation included multiple AC1 failures consuming 5m Ginkgo per-spec timeouts AFTER cascade-9A fired. With cascade-9A closed (Pods now complete in <30s under the credproxy_test budget convention per `02.2-10-VERIFICATION.md` Plan 02.2-08's prediction), the per-spec consumption drops dramatically. T-02.2-34 acceptance annotates `wall_under_600s: true|false` to record whether A+B alone met the original 600s aspiration — if true, the 1800s headroom is unused (acceptable); if false, the 1800s budget absorbs the residual.
- **Option (b) — rely on A+B alone (REJECTED as too risky):** if A+B unexpectedly leave residual wall-time over 600s, cascade-9C re-opens and forces a redundant Plan 02.2-12. The safety-net is cheap insurance.

**CRITICAL FRAMING NOTE (load-bearing per CLAUDE.md Operating Note #1):** This plan is NOT a "chain terminator candidate vN". Plans 02.2-08/09/10 each predicted termination and all surfaced new cascades. This plan is framed as iteration 11 in tactical tuning — the chain terminates when T2 records `gate_decision: APPROVED`, not before. If cascade-10 surfaces, classify per T-02.2-16 taxonomy and propose Plan 02.2-12 inline. If APPROVED, the artifact notes "cascade chain potentially terminated at iteration 11; further runs will validate stability" without claiming finality. Tags include `cascade-9-closure-target` and `tactical`, NOT `chain-terminator-candidate-vN`.

**Cascade-9 ValidationState gap (DEFERRED — out of scope per §Section 5 lines 283-291):** `internal/webhook/v1alpha1/plan_webhook.go:117-199` validates Plans but never sets `plan.Status.ValidationState`. The debug evidence proved this does NOT block HARN-03 dispatch (TaskReconciler.reconcileDispatch is independent of ValidationState). Plan 02.2-11 explicitly does NOT touch this gap; it is documented as a deferred follow-up for a future debug session.

**TACTICAL SCOPE — bundled-A+B+C contract:**

- This plan modifies ONLY `test/integration/kind/testdata/three-task-wave.yaml` (3 line-additions: alpha + beta + gamma caps), `test/integration/envtest/indegree_test.go` (one literal-string change at line 232 region), AND `Makefile` (one literal-string change at line 124 region).
- NO production source modified — `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `.github/` all untouched (T-02.2-36 mitigation).
- `charts/tide/values.yaml` is FIXED contract per CLAUDE.md Execute-Don't-Ask exception — UNTOUCHED.
- `cmd/manager/main.go` is Plan 02.2-10's Dispatcher wiring — UNTOUCHED (do not regress cascade-8 closure).
- All prior cascade fixes preserved: cascade-5/6/7 fixture refactors in `credproxy_test.go`/`suite_test.go`/`caps_test.go`/`output_test.go`/`failure_test.go` all UNTOUCHED.
- Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08, 02.2-09, 02.2-10, AND 02.2-11 (this plan) NOT modified — only `02.2-02-PLAN.md` is touched (and only by Task 3, HARD-GATED on T2 APPROVED).

Purpose: close cascade-9 sub-classes A/B/C in one tactical iteration to unblock Phase 02.2's chain-termination criterion (APPROVED gate enables Plan 02.2-02 dispatch in a follow-up wave to close Phase 02.2).
Output: Sub-class A fixture-budget addition + Sub-class B envtest timeout bump + Sub-class C Makefile safety-net + runtime verification artifact + (if APPROVED) FIRST-EVER Task-3 rewire of `02.2-02-PLAN.md` in Phase 02.2's 11-plan history.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-10-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-PLAN.md
@./CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase at plan time. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From `api/v1alpha1/task_types.go` (Caps schema — confirms the field shape used in T1 Part A):
```go
// Caps declares resource caps applied to the subagent pod executing a Task (Phase 2+).
type Caps struct {
    // WallClockSeconds is the maximum wall-clock time for the subagent execution.
    WallClockSeconds int32 `json:"wallClockSeconds,omitempty"`
}

type TaskSpec struct {
    // ... other fields ...
    // Caps optionally restricts subagent resource usage (Phase 2+).
    Caps *Caps `json:"caps,omitempty"`
}
```

From `internal/dispatch/podjob/jobspec.go` (the deterministic activeDeadline computation — the cascade-9A mechanism):
```go
const (
    // DefaultWallClockGraceSeconds is added to Task.Spec.Caps.WallClockSeconds
    // when setting Job.spec.activeDeadlineSeconds.
    DefaultWallClockGraceSeconds = 60
)

func BuildJobSpec(opts BuildOptions) *batchv1.Job {
    // 1. Compute the active deadline.
    var wallClockSeconds int64
    if opts.Task.Spec.Caps != nil {
        wallClockSeconds = int64(opts.Task.Spec.Caps.WallClockSeconds)
    }
    activeDeadline := wallClockSeconds + DefaultWallClockGraceSeconds
    // ...
}
```

Without `caps`, `wallClockSeconds=0` → `activeDeadline=60s` (the cascade-9A observation). With `wallClockSeconds: 120`, `activeDeadline=180s`.

Established convention from `test/integration/kind/caps_test.go` lines 80-82 (the only existing Task in the kind suite that sets caps — confirms the YAML field shape):
```yaml
spec:
  # ...
  caps:
    wallClockSeconds: 10
```

The wave-test fixture's three Tasks (alpha at lines 66-83, beta at lines 86-101, gamma at lines 104-123 of `test/integration/kind/testdata/three-task-wave.yaml`) currently have NO `caps` block. T1 Part A adds `caps:\n    wallClockSeconds: 120` under each Task's `spec:` (3 additions total).

From `test/integration/envtest/indegree_test.go` Wave-roll-up SUB-02 spec (lines 188-235 region — the cascade-9B target):
```go
Describe("Wave roll-up: Succeeded when all Tasks Succeeded", Label("SUB-02"), func() {
    It("sets Wave.Status.Phase=Succeeded when all member Tasks are Succeeded", func() {
        // ... setup ...
        // The flaky Eventually — bumped from "20s" to "60s":
        Eventually(func() string {
            w := &tideprojectv1alpha1.Wave{}
            if err := k8sClient.Get(ctx, client.ObjectKey{Name: waveName, Namespace: indegreeNamespace}, w); err != nil {
                return ""
            }
            return w.Status.Phase
        }, "20s", "500ms").Should(Equal("Succeeded"),
            "Wave.Status.Phase should be Succeeded when all Tasks are Succeeded")
    })
})
```

The target literal string is `"20s", "500ms"` (with that exact spacing). T1 Part B replaces JUST this `"20s"` with `"60s"`. The polling interval `"500ms"` is preserved. Other `Eventually` blocks in the file are NOT modified.

From `Makefile` lines 120-128 (the cascade-9C target):
```makefile
.PHONY: test-int test-int-fast test-int-kind-prep

test-int: manifests generate fmt vet setup-envtest test-int-kind-prep ## Run full integration test suite ...
	KUBEBUILDER_ASSETS="$(shell "$(ENVTEST)" use $(ENVTEST_K8S_VERSION) --bin-dir "$(LOCALBIN)" -p path)" \
		timeout 600s go test ./test/integration/... -v -timeout=5m -ginkgo.v
```

The target literal string is `timeout 600s` on the recipe's second line (line 124 per plan-time inspection). T1 Part C replaces JUST `600s` with `1800s` on that line. The inner Ginkgo per-spec `-timeout=5m` is preserved (unchanged). The `test-int-fast` recipe (line 128) and other Makefile targets are NOT modified.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply cascade-9 sub-class A+B+C fixes (3 fixture-line additions + 1 envtest timeout bump + 1 Makefile timeout bump)</name>
  <files>test/integration/kind/testdata/three-task-wave.yaml, test/integration/envtest/indegree_test.go, Makefile</files>

  <read_first>
    - `test/integration/kind/testdata/three-task-wave.yaml` (full file; the three Tasks `alpha` (lines 66-83 per plan-time inspection), `beta` (lines 86-101), `gamma` (lines 104-123). Each Task currently has `spec.planRef`, `spec.filesTouched`, `spec.declaredOutputPaths`, `spec.dev.testMode: success`, but NO `spec.caps` block. Part A adds `caps:\n    wallClockSeconds: 120` indented under each Task's `spec:`.)
    - `test/integration/kind/caps_test.go` lines 75-90 (the existing Task YAML in `capPlanYAML` that already sets `spec.caps.wallClockSeconds: 10` — REGRESSION GUARD; this file is NOT modified by Part A; the caps value here is 10 to PROVE the deadline-exceeded behavior at the test level, intentionally different from the wave-test fixture's 120)
    - `internal/dispatch/podjob/jobspec.go` lines 30-115 (specifically `DefaultWallClockGraceSeconds = 60` constant at line 49 and `BuildJobSpec`'s lines 107-113 deadline computation. CONFIRMS the deterministic formula: `activeDeadline = opts.Task.Spec.Caps.WallClockSeconds + 60`. Part A's `wallClockSeconds: 120` yields `activeDeadline = 180s`. DO NOT MODIFY — this file is production source under Option-B test-only contract.)
    - `internal/dispatch/podjob/jobspec_test.go` lines 127-138 (specifically `TestBuildJobSpec_ActiveDeadlineSeconds_HasGrace` — this unit test constructs synthetic BuildOptions and asserts the +60 grace; unaffected by Part A because Part A modifies fixture YAML, not the unit-test's synthetic input. REGRESSION GUARD: `go test ./internal/dispatch/podjob/...` exits 0 after Part A.)
    - `test/integration/envtest/indegree_test.go` lines 186-235 (the `Describe("Wave roll-up: Succeeded when all Tasks Succeeded", Label("SUB-02"))` block. The target `Eventually` is the OUTER one at lines 226-233 — the one returning `w.Status.Phase` and asserting `Equal("Succeeded")`. The INNER `Eventually` at lines 215-222 — which uses `"10s", "200ms"` for Task status updates — is NOT modified. There is exactly ONE occurrence of the literal `"20s", "500ms"` in this file at line 232 — the cascade-9B target.)
    - `test/integration/envtest/indegree_test.go` lines 237-295 (the `Describe("FAIL-02: Wave roll-up: Failed when one Task fails")` block — REGRESSION GUARD; this spec also has an `Eventually` but uses its OWN budget; Part B does NOT modify this spec. Run `grep -c '"20s", "500ms"' indegree_test.go` BEFORE editing to confirm exactly 1 occurrence; after editing, exactly 0 occurrences AND exactly 1 occurrence of `"60s", "500ms"`.)
    - `Makefile` lines 75-130 (specifically the `test-int` target at lines 122-124. The target literal `timeout 600s` appears exactly once in the file on line 124's recipe second line. Other `-timeout=` and `-timeout 60s` patterns in the file are inner Ginkgo/Go-test cap arguments, NOT the outer `timeout 600s` Plan 02.2-06 fix. Run `grep -c 'timeout 600s' Makefile` BEFORE editing to confirm exactly 1 occurrence; after editing, exactly 0 occurrences AND exactly 1 occurrence of `timeout 1800s`.)
    - `cmd/manager/main.go` (REGRESSION GUARD; Plan 02.2-10 Dispatcher wiring lives here at lines 188-256 — DO NOT TOUCH. Confirm with `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returning 1 AND `grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go` returning 2 both BEFORE and AFTER Task 1.)
    - `charts/tide/values.yaml` (REGRESSION GUARD — FIXED contract per CLAUDE.md Execute-Don't-Ask exception. Confirm with `git status --porcelain charts/` returning empty both BEFORE and AFTER Task 1.)
    - `.planning/phases/02.2-.../02.2-10-VERIFICATION.md` §Section 5 lines 247-313 (the cascade-9 fix-landscape source — Sub-class A at line 249-261; Sub-class B at line 263-272; Sub-class C at line 273-281; 4-point recommendation list at lines 295-310. THIS IS THE AUTHORITATIVE FIX-SHAPE SOURCE used to derive Options (a) for all three sub-classes per the `<plan_decision>` in `<objective>`.)
    - `.planning/phases/02.2-.../02.2-09-PLAN.md` Task 1 body (the structural model for THIS plan's Task 1 — bundle-class pattern with byte-level acceptance + Option-B contract preservation + regression guards on prior cascade fixes)
    - `./CLAUDE.md` Working Rules (Observe First — read all target sites BEFORE editing; Execute Don't Ask — once observed, apply all 3 sub-fixes; Verify Before Claiming — run the post-edit grep gates and `go build`/`go vet` before declaring Task 1 done)
  </read_first>

  <action>
    Three-part task in this single auto-task: (A) add `caps.wallClockSeconds: 120` to three Tasks in `test/integration/kind/testdata/three-task-wave.yaml`; (B) bump `Eventually` `"20s"` → `"60s"` in `test/integration/envtest/indegree_test.go`; (C) bump `timeout 600s` → `timeout 1800s` in `Makefile`. Apply all three before any commit.

    **Pre-edit grep assertions (run BEFORE any edits — Working Rule #1):**
    - `grep -c 'wallClockSeconds: 120' test/integration/kind/testdata/three-task-wave.yaml` must return `0` (no caps block present today)
    - `grep -c 'caps:' test/integration/kind/testdata/three-task-wave.yaml` must return `0` (no caps present)
    - `grep -c '"20s", "500ms"' test/integration/envtest/indegree_test.go` must return `1` (the cascade-9B target line exists)
    - `grep -c '"60s", "500ms"' test/integration/envtest/indegree_test.go` must return `0` (target value not yet present)
    - `grep -c 'timeout 600s' Makefile` must return `1` (the Plan 02.2-06 fix value, currently present)
    - `grep -c 'timeout 1800s' Makefile` must return `0` (target value not yet present)
    - `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` must return `1` (Plan 02.2-10 wiring present)
    - `grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go` must return `2` (both PlanReconciler + TaskReconciler wired)

    If ANY pre-edit assertion fails, HALT — the plan-time observations have drifted and the plan needs re-evaluation.

    **Part A — `test/integration/kind/testdata/three-task-wave.yaml`: add `caps.wallClockSeconds: 120` to alpha, beta, gamma.**

    For each of the three Tasks (`alpha` at lines 66-83 per plan-time inspection; `beta` at lines 86-101; `gamma` at lines 104-123), add a `caps:` block under the `spec:` key with `wallClockSeconds: 120` (2-space indented under `caps:`, which is itself 2-space indented under `spec:`). The placement is at the END of each `spec:` block, AFTER all existing fields (`planRef`, `filesTouched`, `declaredOutputPaths`, `dev`, and for gamma also `dependsOn`).

    The exact YAML shape added to each Task's `spec:` block:
    ```
      caps:
        wallClockSeconds: 120
    ```

    (With 2-space indentation matching the existing fields under `spec:`. The `caps:` key is at the same indent level as `planRef:`, `filesTouched:`, etc.; the `wallClockSeconds: 120` is indented 4 spaces — one nesting deeper.)

    DO NOT modify any other field of the Tasks. DO NOT change indentation of any existing line. The block is purely additive: 2 new lines per Task = 6 new lines total added to the file.

    For reference of the established convention, `test/integration/kind/caps_test.go` line 81-82 shows the same pattern with `wallClockSeconds: 10`. Use `120` for the wave-test fixture (not `10`) per T-02.2-32 acceptance — 120s gives the credproxy initContainer + sidecar startup ≥120s headroom; 10s would re-trigger DeadlineExceeded.

    **Part B — `test/integration/envtest/indegree_test.go`: bump Wave-roll-up Eventually 20s → 60s.**

    Locate the SINGLE occurrence of the literal `"20s", "500ms"` in the file (at line 232 per plan-time inspection, inside the `Describe("Wave roll-up: Succeeded when all Tasks Succeeded", Label("SUB-02"))` block, on the `Eventually` that returns `w.Status.Phase` and asserts `Equal("Succeeded")`). Replace the `"20s"` portion with `"60s"`. The full target line becomes `}, "60s", "500ms").Should(Equal("Succeeded"),` (the surrounding context — closing brace of the function literal, comma, polling interval `"500ms"`, `.Should(Equal(...))` chain — is byte-identical).

    DO NOT modify any other `Eventually` block in the file. The INNER `Eventually` at the task-Status-Update loop (lines 215-222 per plan-time inspection, using `"10s", "200ms"`) is preserved. The FAIL-02 `Describe` block (lines 237-295) is preserved.

    **Part C — `Makefile`: bump test-int target timeout 600s → 1800s.**

    Locate the SINGLE occurrence of the literal `timeout 600s` in `Makefile` (at line 124 per plan-time inspection, on the second line of the `test-int` recipe — the `go test ./test/integration/...` invocation). Replace `600s` with `1800s`. The full target line becomes:
    ```
		timeout 1800s go test ./test/integration/... -v -timeout=5m -ginkgo.v
    ```
    (Note: the leading whitespace is a single TAB character — Makefile recipe requirement. Do not change to spaces.)

    DO NOT modify any other Makefile line. The `-timeout=5m` inner Ginkgo per-spec cap is preserved. The `test-int-fast` recipe is preserved. The `-timeout 60s` inside the unit-test targets (lines 77, 81) is preserved.

    **Post-edit assertions (run AFTER all three Parts — Working Rule #3):**
    - `grep -c 'wallClockSeconds: 120' test/integration/kind/testdata/three-task-wave.yaml` must return exactly `3` (one per Task — alpha, beta, gamma)
    - `grep -c 'caps:' test/integration/kind/testdata/three-task-wave.yaml` must return exactly `3` (one block per Task)
    - `grep -c '"20s", "500ms"' test/integration/envtest/indegree_test.go` must return exactly `0` (the cascade-9B target value purged)
    - `grep -c '"60s", "500ms"' test/integration/envtest/indegree_test.go` must return exactly `1` (the cascade-9B replacement landed)
    - `grep -c 'timeout 600s' Makefile` must return exactly `0` (the cascade-9C prior value purged)
    - `grep -c 'timeout 1800s' Makefile` must return exactly `1` (the cascade-9C replacement landed)
    - `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` must STILL return `1` (Plan 02.2-10 wiring NOT regressed)
    - `grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go` must STILL return `2`

    **Build + vet gates (run AFTER all three Parts):**
    - `go build ./...` must exit 0 (no compilation errors from any package; YAML changes do not affect Go compilation but the gate validates the codebase is healthy)
    - `go vet ./...` must exit 0
    - `go test -count=1 -run TestBuildJobSpec ./internal/dispatch/podjob/...` must exit 0 (the existing unit test that asserts the +60-grace formula is unaffected because it uses synthetic BuildOptions, not the wave-fixture YAML)

    **Constraints (T-02.2-36 mitigation — production wiring untouched):**
    - **DO NOT modify** any production source under `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `.github/` — this task is test-fixture + Makefile + envtest only
    - **DO NOT modify** `cmd/manager/main.go` — Plan 02.2-10's Dispatcher wiring lives there; touching this file risks regressing cascade-8 closure
    - **DO NOT modify** `charts/tide/values.yaml` — FIXED contract per CLAUDE.md Execute-Don't-Ask exception (anti-pattern from Phase 02.2)
    - **DO NOT modify** `internal/dispatch/podjob/jobspec.go` — `DefaultWallClockGraceSeconds = 60` is a deterministic contract; the fix lands at the Task-fixture side, not the BuildJobSpec side (Option (a) chosen per `<objective>` decision)
    - **DO NOT modify** any kind integration test file other than `testdata/three-task-wave.yaml` — `credproxy_test.go`/`caps_test.go`/`output_test.go`/`failure_test.go`/`suite_test.go`/`wave_test.go` all preserved verbatim (cascade-5/6/7 fixes intact)
    - **DO NOT modify** any envtest file other than `indegree_test.go` — `suite_test.go`/`leader_test.go`/etc. preserved verbatim
    - **DO NOT modify** any other `Eventually` in `indegree_test.go` — only the SUB-02 Wave-roll-up Eventually at the `"20s", "500ms"` line is bumped
    - **DO NOT modify** any other Makefile line — only the `test-int` recipe's `timeout 600s` is bumped to `timeout 1800s`
    - **DO NOT modify** Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08, 02.2-09, 02.2-10, OR 02.2-11 (this plan) — only `02.2-02-PLAN.md` is touched (and only by Task 3, HARD-GATED on T2 APPROVED)

    Commit with message (per CLAUDE.md HEREDOC convention):
    ```
    fix(02.2-11): close cascade-9 sub-classes A+B+C (Task caps wallClockSeconds: 120; envtest Eventually 20s->60s; Makefile timeout 600s->1800s)

    Sub-class A: three-task-wave.yaml Tasks alpha/beta/gamma get caps.wallClockSeconds: 120 (BuildJobSpec computes activeDeadline=180s, giving credproxy initContainer ≥120s headroom under kind).
    Sub-class B: indegree_test.go SUB-02 Wave-roll-up Eventually budget 20s->60s (3x prior budget to absorb reconcileDispatch requeue-load competition introduced by Plan 02.2-10's correct Dispatcher wiring).
    Sub-class C: Makefile test-int recipe timeout 600s->1800s (safety-net per 02.2-10-VERIFICATION.md §Section 5 line 305-313; A+B alone may meet the 600s aspiration, in which case the 1800s headroom is unused acceptable).

    Production source UNTOUCHED (Option-B test-only contract): internal/, cmd/manager/main.go, api/, pkg/, charts/ all byte-identical. Cascade-8 Dispatcher wiring at cmd/manager/main.go preserved. cascade-9 ValidationState deferred (TaskReconciler.reconcileDispatch is independent).
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # === Part A — three-task-wave.yaml caps additions ===
      [ "$(grep -c 'wallClockSeconds: 120' test/integration/kind/testdata/three-task-wave.yaml)" -eq 3 ] && \
      [ "$(grep -c 'caps:' test/integration/kind/testdata/three-task-wave.yaml)" -eq 3 ] && \
      # === Part B — indegree_test.go Eventually bump ===
      [ "$(grep -c '"20s", "500ms"' test/integration/envtest/indegree_test.go)" -eq 0 ] && \
      [ "$(grep -c '"60s", "500ms"' test/integration/envtest/indegree_test.go)" -eq 1 ] && \
      # Surrounding context preserved: the SUB-02 Describe block still exists
      grep -F 'Describe("Wave roll-up: Succeeded when all Tasks Succeeded", Label("SUB-02")' test/integration/envtest/indegree_test.go && \
      # The inner "10s", "200ms" Eventually preserved (task-status patch loop)
      [ "$(grep -c '"10s", "200ms"' test/integration/envtest/indegree_test.go)" -ge 1 ] && \
      # === Part C — Makefile timeout bump ===
      [ "$(grep -c 'timeout 600s' Makefile)" -eq 0 ] && \
      [ "$(grep -c 'timeout 1800s' Makefile)" -eq 1 ] && \
      # Inner Ginkgo -timeout=5m preserved
      grep -E 'timeout 1800s go test \./test/integration/\.\.\. -v -timeout=5m -ginkgo\.v' Makefile && \
      # === T-02.2-36 mitigation — production wiring NOT regressed ===
      [ "$(grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go)" -eq 2 ] && \
      # === Compile + vet gates ===
      go build ./... && \
      go vet ./... && \
      # Unit test for the +60 grace formula still passes (synthetic BuildOptions unaffected)
      go test -count=1 -run TestBuildJobSpec ./internal/dispatch/podjob/... && \
      # === Regression guards — production source untouched ===
      [ -z "$(git status --porcelain internal/)" ] && \
      [ -z "$(git status --porcelain cmd/)" ] && \
      [ -z "$(git status --porcelain api/)" ] && \
      [ -z "$(git status --porcelain pkg/)" ] && \
      [ -z "$(git status --porcelain charts/)" ] && \
      # === Regression guards — cascade-5/6/7 fixes preserved (kind test files other than testdata UNTOUCHED) ===
      [ -z "$(git status --porcelain test/integration/kind/credproxy_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/caps_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/output_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/failure_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/suite_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/wave_test.go)" ] && \
      # === Regression guards — envtest files other than indegree_test.go UNTOUCHED ===
      [ -z "$(git status --porcelain test/integration/envtest/suite_test.go)" ] && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Sub-class A — three wallClockSeconds additions:** `grep -c 'wallClockSeconds: 120' test/integration/kind/testdata/three-task-wave.yaml` returns exactly `3` AND `grep -c 'caps:' test/integration/kind/testdata/three-task-wave.yaml` returns exactly `3` (one caps block per Task — alpha, beta, gamma)
    - **Sub-class B — Eventually bump landed:** `grep -c '"20s", "500ms"' test/integration/envtest/indegree_test.go` returns exactly `0` AND `grep -c '"60s", "500ms"' test/integration/envtest/indegree_test.go` returns exactly `1`
    - **Sub-class B — surrounding context preserved:** `grep -F 'Describe("Wave roll-up: Succeeded when all Tasks Succeeded", Label("SUB-02")' test/integration/envtest/indegree_test.go` matches AND `grep -c '"10s", "200ms"' test/integration/envtest/indegree_test.go` returns ≥ 1 (the inner task-status-update Eventually preserved)
    - **Sub-class C — Makefile bump landed:** `grep -c 'timeout 600s' Makefile` returns exactly `0` AND `grep -c 'timeout 1800s' Makefile` returns exactly `1` AND `grep -E 'timeout 1800s go test \./test/integration/\.\.\. -v -timeout=5m -ginkgo\.v' Makefile` matches (the full recipe line shape preserved with only the wall-budget changed)
    - **T-02.2-36 cascade-8 closure preserved:** `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returns exactly `1` AND `grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go` returns exactly `2`
    - **Compile + vet gates:** `go build ./...` exits 0 AND `go vet ./...` exits 0 AND `go test -count=1 -run TestBuildJobSpec ./internal/dispatch/podjob/...` exits 0
    - **Production source UNTOUCHED (T-02.2-36 + CLAUDE.md anti-pattern):** all of `git status --porcelain internal/`, `git status --porcelain cmd/`, `git status --porcelain api/`, `git status --porcelain pkg/`, `git status --porcelain charts/` return empty
    - **Cascade-5/6/7 fixes preserved:** `git status --porcelain test/integration/kind/credproxy_test.go` empty AND `git status --porcelain test/integration/kind/caps_test.go` empty AND `git status --porcelain test/integration/kind/output_test.go` empty AND `git status --porcelain test/integration/kind/failure_test.go` empty AND `git status --porcelain test/integration/kind/suite_test.go` empty AND `git status --porcelain test/integration/kind/wave_test.go` empty
    - **Envtest other files preserved:** `git status --porcelain test/integration/envtest/suite_test.go` empty
  </acceptance_criteria>

  <done>
    `test/integration/kind/testdata/three-task-wave.yaml` has `spec.caps.wallClockSeconds: 120` added to each of the three Tasks (alpha, beta, gamma — three additive `caps:` blocks, 2-space indented under each Task's `spec:`). `test/integration/envtest/indegree_test.go` has the SUB-02 Wave-roll-up `Eventually` budget bumped from `"20s", "500ms"` to `"60s", "500ms"` at exactly one site; other `Eventually` blocks unchanged. `Makefile` `test-int` recipe has `timeout 600s` bumped to `timeout 1800s` at exactly one site; other Makefile content unchanged. Production source (`internal/`, `cmd/manager/main.go`, `api/`, `pkg/`, `charts/`) byte-identical to pre-edit state — Plan 02.2-10's Dispatcher wiring preserved verbatim (cascade-8 closure NOT regressed). All cascade-5/6/7 fixture refactors in `credproxy_test.go`/`caps_test.go`/`output_test.go`/`failure_test.go`/`suite_test.go`/`wave_test.go` preserved. `go build ./...` + `go vet ./...` + `go test -count=1 -run TestBuildJobSpec ./internal/dispatch/podjob/...` all exit 0. Committed with the `fix(02.2-11): close cascade-9 sub-classes A+B+C ...` HEREDOC message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-11-VERIFICATION.md (clean run + idempotent rerun + gate decision + six runtime-observation fields)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md</files>

  <read_first>
    - `.planning/phases/02.2-.../02.2-10-VERIFICATION.md` (the structural template this artifact mirrors — frontmatter shape; Sections 1-6 layout; per-spec outcome tables; root-cause analysis prose; threat-model-mitigations-applied list; "What This Artifact Did Not Touch" closing section. THIS plan's verification adds two MORE observation fields (`ac1_wave_rollup_observed_seconds`, `deadline_exceeded_count`, `wall_under_600s`) on top of the existing fields. Particularly: §Section 5 lines 247-313 — the cascade-9 fix landscape source — must be referenced in this artifact's §Section 5 as "the prior surfacing; closed by Plan 02.2-11".)
    - `.planning/phases/02.2-.../02.2-10-SUMMARY.md` (Plan 02.2-10's summary — for cascade-8 closure evidence and the Dispatcher-wiring at `cmd/manager/main.go` lines 188-256 that THIS plan must NOT regress)
    - `.planning/phases/02.2-.../02.2-09-PLAN.md` Task 2 body (the structural model — runtime verification reproducer pattern with clean run + idempotent rerun + diagnostic snapshot + per-spec outcome table + gate decision; THIS plan's Task 2 mirrors the same flow but with cascade-9 sub-class closure evidence as the load-bearing assertions)
    - `test/integration/kind/testdata/three-task-wave.yaml` (post-Task-1 state — verify the three `caps.wallClockSeconds: 120` additions are present before this task runs)
    - `test/integration/envtest/indegree_test.go` (post-Task-1 state — verify the `"60s", "500ms"` Eventually present at the SUB-02 Wave-roll-up site)
    - `Makefile` (post-Task-1 state — verify `timeout 1800s` present on the `test-int` recipe)
    - `cmd/manager/main.go` (REGRESSION GUARD — verify Plan 02.2-10 Dispatcher wiring untouched; this task creates verification artifacts but must capture image-rebuild evidence per CLAUDE.md Working Rule #4 — the manager image digest must have CHANGED from Plan 02.2-10's run, AND the Pod must have rotated; without those, the cascade-9 closure evidence is invalid)
    - `internal/dispatch/podjob/jobspec.go` lines 49 + 107-113 (REGRESSION GUARD — verify `DefaultWallClockGraceSeconds = 60` and the activeDeadline formula `wallClockSeconds + 60` are byte-identical; T1 Option (a) chose to land the fix at the fixture side, NOT the production side)
    - `./CLAUDE.md` Working Rules + Operating Notes (the verification protocol — Working Rule #3 verification protocols for Phase-02.2-style runtime gates; Operating Note #2 worktree cwd-drift mitigation if running in a worktree; Operating Note #1 do NOT predict chain terminator)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-10-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md` using the same structural template PLUS three additional runtime-observation fields (`ac1_wave_rollup_observed_seconds`, `deadline_exceeded_count`, `wall_under_600s`) on top of the existing fields from Plan 02.2-10 (`credproxy_pod_wait_observed_seconds`).

    **Recovery-lesson PRE-STEP (codified protocol from `.continue-here.md` Plan 02.2-05 lesson, applied in 02.2-06/07/08/09/10):** BEFORE running the clean reproducer, author `02.2-11-VERIFICATION.md` with PRELIMINARY frontmatter — the load-bearing fields (`phase: 02.2`, `plan: 11`, `task: 2`, `artifact: verification`, `captured_on: <today>`, `captured_by_host: <host>`, `captured_by_branch: <branch>`, placeholder `gate_decision: PENDING`, AND placeholders for SIX observation fields: `clean_run_wall_seconds: -1`, `rerun_wall_seconds: -1`, `credproxy_pod_wait_observed_seconds: -1`, `ac1_wave_rollup_observed_seconds: -1`, `deadline_exceeded_count: -1`, `wall_under_600s: false`, AND image-digest fields `manager_image_digest_prior: <copy from 02.2-10-VERIFICATION.md>`, `manager_image_digest_current: TBD`) — so that if mid-run executor API errors or context window pressure interrupt the long-running runtime verification, the artifact's gate field still exists with a recoverable PENDING state. Then update `gate_decision: PENDING` → APPROVED or BLOCKED and all six observation fields + image digest to real values after Sub-step C completes.

    **Pre-run image-rebuild verification (CLAUDE.md Working Rule #4 — load-bearing):**
    ```bash
    # Capture the prior manager image digest from Plan 02.2-10's evidence (if recorded; else from kind state)
    PRIOR_DIGEST=$(grep -E 'manager_image_digest' .planning/phases/02.2-.../02.2-10-VERIFICATION.md | head -1 | awk -F': ' '{print $2}' || echo "unknown")
    echo "Prior manager image digest: $PRIOR_DIGEST" | tee /tmp/02.2-11-prior-digest.txt

    # Force rebuild — make test-int-kind-prep should rebuild manager + stub-subagent + credproxy
    make test-int-kind-prep 2>&1 | tee /tmp/02.2-11-prep.log

    # Capture the NEW manager image digest after rebuild
    NEW_DIGEST=$(docker image inspect tide-controller-manager:latest --format '{{.Id}}' 2>/dev/null || docker image ls --format '{{.Repository}}:{{.Tag}} {{.ID}}' | grep tide-controller-manager | head -1 | awk '{print $2}')
    echo "Current manager image digest: $NEW_DIGEST" | tee /tmp/02.2-11-current-digest.txt
    ```
    Record both digests in the artifact's frontmatter (`manager_image_digest_prior` and `manager_image_digest_current`). If they MATCH, the binary did NOT rebuild — HALT and investigate (Working Rule #4 violation; the fixture+Makefile edits cannot be claimed to have shaped the runtime evidence).

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state — CLAUDE.md Execute-Don't-Ask exception 7: kind delete cluster is explicitly authorized inside this clean-rerun sequence):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit (set -o pipefail makes bash's captured exit reflect make's real exit, not tee's):
    set -o pipefail
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-11-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state per CLAUDE.md Verify-Before-Claiming protocols:**
    ```bash
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-11-clean-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-11-clean-controller-pod.txt || true
    # Capture manager Pod's IMAGE_ID for digest verification + AGE for rotation verification
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o jsonpath='{.items[0].status.containerStatuses[0].imageID}{"\n"}{.items[0].metadata.creationTimestamp}' 2>&1 | tee /tmp/02.2-11-clean-controller-imageid.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-11-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=30m 2>&1 | tee /tmp/02.2-11-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-11-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-11-clean-pvc-phase.txt || true
    # Cascade-9A closure evidence — assert ABSENCE of "DeadlineExceeded" (the sub-class A target)
    grep -c 'DeadlineExceeded' /tmp/02.2-11-clean-run.log 2>&1 | tee /tmp/02.2-11-clean-deadline-count.txt || true
    grep -E 'DeadlineExceeded' /tmp/02.2-11-clean-manager-logs.txt 2>&1 | tee /tmp/02.2-11-clean-deadline-logs.txt || true
    # Cascade-8 preservation evidence — dispatch still active
    grep -cE '"creating job"|"dispatch"' /tmp/02.2-11-clean-run.log 2>&1 | tee /tmp/02.2-11-clean-dispatch-count.txt || true
    kubectl --context kind-tide-test get jobs --all-namespaces 2>&1 | tee /tmp/02.2-11-clean-jobs.txt || true
    # Cascade-9B closure evidence — Layer A AC1 Wave-roll-up Succeeded spec
    grep -E 'Wave roll-up: Succeeded|sets Wave.Status.Phase=Succeeded' /tmp/02.2-11-clean-run.log 2>&1 | tee /tmp/02.2-11-clean-ac1-evidence.txt || true
    # Per-spec timings — Ginkgo summary lines
    grep -E 'Ran [0-9]+ Specs|Spec Runs|FAIL|PASS' /tmp/02.2-11-clean-run.log 2>&1 | tee /tmp/02.2-11-clean-spec-summary.txt || true
    # Cascade-5/6/7 regression check — assert ABSENCE of "no project found" for any test namespace
    grep -E 'no project found.*(credproxy|caps|output|failure|tide-int-test|indegree)' /tmp/02.2-11-clean-manager-logs.txt 2>&1 | tee /tmp/02.2-11-clean-cascade-regression.txt || true
    # Cascade-2/3 regression check — assert ABSENCE of "flag provided but not defined"
    grep -E 'flag provided but not defined' /tmp/02.2-11-clean-manager-logs.txt 2>&1 | tee /tmp/02.2-11-clean-flag-regression.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block — APPROVED case shows 18/18 Layer A specs PASS + 7/7 Layer B specs PASS within the 1800s budget; BLOCKED case shows partial outcome with cascade-10). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-10-VERIFICATION.md` §Section 1.

    **Six runtime-observation fields (LOAD-BEARING — sub-class closure indicators):**
    - `clean_run_wall_seconds`: end-to-end wall time of the clean run (was 1486 in Plan 02.2-10; APPROVED requires ≤ 1800)
    - `rerun_wall_seconds`: end-to-end wall time of the `KEEP_KIND_CLUSTER=true` rerun (idempotency check; APPROVED requires ≤ 1800)
    - `credproxy_pod_wait_observed_seconds`: time-to-Pod-Ready for the credproxy HARN-03 pod-wait spec (carry-forward from Plan 02.2-08; should drop substantially now that cascade-9A is closed and the controller is no longer cycling DeadlineExceeded retries)
    - `ac1_wave_rollup_observed_seconds`: time-to-Succeeded for the Layer A SUB-02 Wave-roll-up Succeeded spec (NEW — cascade-9B closure indicator; APPROVED requires the spec PASS with observed seconds ≤ 45 — proving the 60s budget bump has headroom, not masking a 60s+ regression)
    - `deadline_exceeded_count`: integer count of `DeadlineExceeded` strings in the manager logs (NEW — cascade-9A closure indicator; APPROVED requires exactly 0)
    - `wall_under_600s`: boolean (`true` if clean_run_wall_seconds ≤ 600 AND rerun_wall_seconds ≤ 600 — A+B alone met original aspiration; `false` if the 1800s safety-net absorbed residual)

    Record into the artifact's frontmatter:
    ```
    clean_run_wall_seconds: <N>
    rerun_wall_seconds: <M>
    credproxy_pod_wait_observed_seconds: <C>
    ac1_wave_rollup_observed_seconds: <A>
    deadline_exceeded_count: <D>
    wall_under_600s: <true|false>
    manager_image_digest_prior: <P>
    manager_image_digest_current: <Q>
    gate_decision: APPROVED|BLOCKED
    ```

    **APPROVED case clean-run state (cascade-9 closed):**
    - Pod `READY 1/1 STATUS Running RESTARTS ≤ 1`; manager logs show "starting manager"
    - NO `flag provided but not defined` (cascades 2/3 not regressed)
    - NO `no project found in namespace <ns>` for any test namespace (cascades 5/6/7 not regressed)
    - `deadline_exceeded_count: 0` (cascade-9A closed — no DeadlineExceeded in logs)
    - `kubectl get jobs --all-namespaces` shows Jobs in test namespaces during the run (cascade-8 preserved)
    - Ginkgo Layer A summary: `Ran 18 Specs in <X>s` with `0 Failed` (the AC1 Wave-roll-up Succeeded spec passes — cascade-9B closed)
    - `ac1_wave_rollup_observed_seconds: ≤ 45` (proves 60s budget has headroom)
    - Ginkgo Layer B summary: 7/7 Layer B specs PASS within budget
    - `clean_run_wall_seconds ≤ 1800` AND `rerun_wall_seconds ≤ 1800`
    - `manager_image_digest_current` ≠ `manager_image_digest_prior` (Working Rule #4 satisfied)

    **BLOCKED case A — cascade-10 surfaces as `spec-flake`:** the Layer A AC1 Wave-roll-up Succeeded spec STILL flakes even with the 60s budget. Classification: `spec-flake`. Fix shape: bump Eventually to 90s OR address the upstream `reconcileDispatch` requeue-load (production-code fix in `internal/controller/task_controller.go`). Plan 02.2-12 may need to be a production-code plan.

    **BLOCKED case B — cascade-10 surfaces as `budget-vs-actual`:** clean_run_wall_seconds exceeds 1800s even with cascade-9A+B closed. Classification: `budget-vs-actual`. Fix shape: split Layer A and Layer B into separate Makefile targets with independent timeouts (Option C from `02.2-10-VERIFICATION.md` §Section 5 line 295-310).

    **BLOCKED case C — cascade-10 surfaces as `kind-infra`:** Pod fails to start, image pull error, networking issue surfaces — classification `kind-infra`. Fix shape: kind-config tuning or suite_test.go BeforeSuite robustness.

    **BLOCKED case D — cascade-10 surfaces as `harness-bug`:** a fixture incompatibility surfaces (e.g., the `caps.wallClockSeconds: 120` field is rejected by the CRD validation, or the schema field is named differently than expected). Classification: `harness-bug`. Fix shape: re-read the Task CRD schema; correct the field path.

    **BLOCKED case E — cascade-10 surfaces as `flag-mismatch` or prior-cascade regression:** if EITHER `flag provided but not defined` OR `no project found` appears in manager logs, a PRIOR cascade has regressed. Classification: `flag-mismatch` or `harness-bug` per which cascade. Surface in §Section 5 with full diagnostic citing which prior Plan's fix regressed.

    **BLOCKED case F — cascade-10 surfaces as `other` (production-code bug under correctly-dispatching kind run):** a real controller bug surfaces — e.g., WaveReconciler Task→Wave roll-up race, reconcile-loop contention, work-queue saturation, ResourceVersion conflict cascade. Classification: `other`. Fix shape: route to Phase 03+ production-code fix; Plan 02.2-12 may need to be a production-code plan rather than a tactical plan.

    **Sub-step B — Idempotent Re-run:**
    ```bash
    set -o pipefail
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-11-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    Capture rerun_wall_seconds. APPROVED requires both runs ≤ 1800s. The rerun exercises Job idempotency (SUB-03 from the Ginkgo labels) — Job names must be deterministic; AlreadyExists must not be treated as failure.

    **Sub-step C — Worktree cwd-drift pre-merge check (per CLAUDE.md Operating Notes):**
    ```bash
    # If T2 ran in a worktree, this check MUST be run BEFORE merging the worktree branch into main:
    UNTRACKED=$(git status --porcelain | grep '^?? ' | grep '02.2-11-' || true)
    if [ -n "$UNTRACKED" ]; then
      echo "WORKTREE CWD-DRIFT DETECTED — duplicates in main worktree:"
      echo "$UNTRACKED"
      echo "Diff each against the worktree branch's commits and remove duplicates before merge."
      exit 1
    fi
    ```

    **Sub-step D — Author the verification artifact's full body:**

    Mirror `02.2-10-VERIFICATION.md`'s structure:
    - **Frontmatter:** `phase: 02.2`, `plan: 11`, `task: 2`, `artifact: verification`, `captured_on: <today>`, `captured_by_host: <host>`, `captured_by_branch: <branch>`, `gate_decision: APPROVED|BLOCKED`, all six runtime-observation fields, both image-digest fields
    - **§Section 1 — Clean Run Evidence:** exit code, wall time, per-spec outcome table, manager-log evidence (load-bearing for cascade-9A closure: `deadline_exceeded_count: 0`), Pod-state evidence (image digest, restart count, age), namespace listings
    - **§Section 2 — Idempotent Rerun Evidence:** exit code, wall time, idempotency verification (Job AlreadyExists handled cleanly; no duplicate Jobs)
    - **§Section 3 — Cascade-8 Preservation Evidence:** `dispatch_active: true` still observed; `cmd/manager/main.go` Dispatcher wiring confirmed at runtime via "creating job" log lines
    - **§Section 4 — Cascade-9 Sub-Class Closure Evidence:**
      - Sub-class A: `deadline_exceeded_count: 0`; wave-test Tasks alpha/beta/gamma transition through Pending→Running→Succeeded
      - Sub-class B: Layer A AC1 Wave-roll-up Succeeded spec PASS in ≤ 45s (`ac1_wave_rollup_observed_seconds`)
      - Sub-class C: wall ≤ 1800s on both runs; `wall_under_600s: true|false` annotation
    - **§Section 5 — Root-Cause Summary + Cascade-10 Classification (if BLOCKED):** preserve the cascade-1-through-9 history; classify cascade-10 per T-02.2-16 taxonomy with specific failing spec(s) + Plan 02.2-12 fix-shape proposal inline; if APPROVED, note "cascade chain potentially terminated at iteration 11; further runs will validate stability" (per CLAUDE.md Operating Note #1, do NOT claim chain terminator definitively)
    - **§Section 6 — Threat-Model Mitigations Applied:** evidence for T-02.2-32/33/34/35/36 + carry-forward T-02.2-01 through T-02.2-31
    - **What This Artifact Did Not Touch:** ValidationState gap still deferred; production source untouched; cmd/manager/main.go preserved; charts/ untouched; cascades 5/6/7 fixture files untouched

    **Constraints:**
    - DO NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `Makefile`, `test/`, `.github/` (T2 is artifact-authoring + reproducer-running only)
    - DO NOT touch any plan file under `.planning/phases/02.2-.../` other than the new `02.2-11-VERIFICATION.md` (Task 3 owns the `02.2-02-PLAN.md` edits, HARD-GATED on T2 APPROVED)
    - DO NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` (orchestrator owns those)
    - If the runtime reproducer encounters API errors or context-window pressure, the PRELIMINARY frontmatter with `gate_decision: PENDING` is the recovery state — resume by re-running the diagnostic snapshot commands and updating to APPROVED|BLOCKED

    Commit with message:
    ```
    docs(02.2-11): runtime verification — <APPROVED|BLOCKED> gate (cascade-9 sub-classes A+B+C closed: deadline_exceeded_count=<N>; ac1_wave_rollup_observed=<M>s; wall_under_600s=<true|false>; cascade-10 <classification|terminated>)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Verification artifact exists with required frontmatter
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md ] && \
      grep -cE '^phase: "?02\.2"?$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^plan: "?11"?$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^task: "?2"?$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^artifact: verification$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      # Gate decision is APPROVED or BLOCKED (not PENDING)
      [ "$(grep -cE '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -cE '^gate_decision: PENDING$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md)" -eq 0 ] && \
      # All six runtime-observation fields present (real values, not -1)
      grep -cE '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^rerun_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^credproxy_pod_wait_observed_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^ac1_wave_rollup_observed_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^deadline_exceeded_count: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^wall_under_600s: (true|false)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      # Both image digest fields present
      grep -cE '^manager_image_digest_prior: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      grep -cE '^manager_image_digest_current: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      # Section markers present
      grep -cE '^## Section [0-9]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      # Per-CLAUDE.md verification protocol: clean-run log captured
      [ -f /tmp/02.2-11-clean-run.log ] && \
      # APPROVED case requires deadline_exceeded_count: 0
      ! (grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-.../02.2-11-VERIFICATION.md >/dev/null && ! grep -cE '^deadline_exceeded_count: 0$' .planning/phases/02.2-.../02.2-11-VERIFICATION.md >/dev/null) && \
      # Production source untouched (T-02.2-36 + CLAUDE.md anti-patterns)
      [ -z "$(git status --porcelain internal/)" ] && \
      [ -z "$(git status --porcelain cmd/)" ] && \
      [ -z "$(git status --porcelain charts/)" ] && \
      # Cascade-9 closure section referenced in the artifact
      grep -cE 'cascade-9|sub-class' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md && \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact exists with required frontmatter:** `02.2-11-VERIFICATION.md` present with `phase: 02.2`, `plan: 11`, `task: 2`, `artifact: verification`, `captured_on`, `captured_by_host`, `captured_by_branch`
    - **Gate decision finalized (not PENDING):** `grep -cE '^gate_decision: (APPROVED|BLOCKED)$' 02.2-11-VERIFICATION.md` returns exactly `1` AND `grep -cE '^gate_decision: PENDING$' 02.2-11-VERIFICATION.md` returns `0`
    - **Six runtime-observation fields populated with real values:** all six fields (`clean_run_wall_seconds`, `rerun_wall_seconds`, `credproxy_pod_wait_observed_seconds`, `ac1_wave_rollup_observed_seconds`, `deadline_exceeded_count`, `wall_under_600s`) present with integer/boolean values (not `-1` or placeholder)
    - **Image-rebuild verification (CLAUDE.md Working Rule #4):** both `manager_image_digest_prior` and `manager_image_digest_current` present in frontmatter; current != prior (binary rebuilt)
    - **APPROVED ⇒ deadline_exceeded_count: 0:** if `gate_decision: APPROVED`, then `grep -cE '^deadline_exceeded_count: 0$' 02.2-11-VERIFICATION.md` returns `1` (sub-class A closed)
    - **APPROVED ⇒ ac1_wave_rollup_observed_seconds ≤ 45:** if `gate_decision: APPROVED`, the recorded `ac1_wave_rollup_observed_seconds` value is ≤ 45 (sub-class B closed with headroom; not at 60s budget edge)
    - **APPROVED ⇒ both runs ≤ 1800s:** if `gate_decision: APPROVED`, both `clean_run_wall_seconds` and `rerun_wall_seconds` are ≤ 1800 (sub-class C closed)
    - **APPROVED ⇒ dispatch_active preserved:** clean-run log captured at `/tmp/02.2-11-clean-run.log` shows at least one `"creating job"` or `"dispatch"` line; `kubectl get jobs --all-namespaces` snapshot shows Jobs in test namespaces (cascade-8 NOT regressed)
    - **APPROVED ⇒ prior cascades preserved:** clean-run manager log captured at `/tmp/02.2-11-clean-manager-logs.txt` shows ZERO `flag provided but not defined` AND ZERO `no project found in namespace` matches (cascades 2/3/5/6/7 not regressed)
    - **APPROVED ⇒ Layer A passes 18/18:** the Ginkgo summary in clean-run log shows `Ran 18 Specs in <X>s` with `0 Failed` for Layer A (the AC1 Wave-roll-up Succeeded spec passes — sub-class B closed)
    - **BLOCKED ⇒ cascade-10 classified:** if `gate_decision: BLOCKED`, the artifact §Section 5 contains one of the six T-02.2-16 classification tokens (`flag-mismatch`/`budget-vs-actual`/`spec-flake`/`harness-bug`/`kind-infra`/`other`) AND identifies the specific failing spec(s) AND proposes Plan 02.2-12's fix shape inline
    - **Sections 1-6 structural template:** `grep -cE '^## Section [0-9]' 02.2-11-VERIFICATION.md` returns ≥ 4 (Sections 1-6 present per mirror of `02.2-10-VERIFICATION.md`)
    - **Cascade-9 closure section present:** `grep -cE 'cascade-9|sub-class' 02.2-11-VERIFICATION.md` returns ≥ 1 (the artifact references the cascade-9 closure context)
    - **Production source UNTOUCHED by T2:** all of `git status --porcelain internal/`, `git status --porcelain cmd/`, `git status --porcelain charts/` return empty
    - **Worktree cwd-drift mitigation honored:** if T2 ran in a worktree, the pre-merge check ran and showed no orphan `02.2-11-` files in main before merge
  </acceptance_criteria>

  <done>
    `02.2-11-VERIFICATION.md` exists with the structural template mirroring `02.2-10-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, ALL SIX runtime-observation fields (`clean_run_wall_seconds`, `rerun_wall_seconds`, `credproxy_pod_wait_observed_seconds`, `ac1_wave_rollup_observed_seconds`, `deadline_exceeded_count`, `wall_under_600s`), both image-digest fields (`manager_image_digest_prior`, `manager_image_digest_current` — current ≠ prior per Working Rule #4), AND `gate_decision: APPROVED` or `gate_decision: BLOCKED` (PENDING is not a final state). Sections 1-6 captured with per-spec outcome tables, Pod-state evidence, manager-log evidence (load-bearing for cascade-9A closure — `deadline_exceeded_count: 0` if APPROVED), cascade-8 preservation evidence (dispatch still active), wall-time budget check, T-02.2-32 through T-02.2-36 threat-model mitigations with concrete evidence + carry-forward T-02.2-01 through T-02.2-31. §Section 5 classifies cascade-10 per T-02.2-16 taxonomy if BLOCKED, OR notes "cascade chain potentially terminated at iteration 11; further runs will validate stability" if APPROVED (per CLAUDE.md Operating Note #1, no chain-terminator claim). If APPROVED, this is the FIRST APPROVED gate in Phase 02.2's 11-plan history; Task 3 fires. If BLOCKED, Task 3 does NOT fire; §Section 5 proposes Plan 02.2-12's fix shape. Committed with the `docs(02.2-11): runtime verification — <APPROVED|BLOCKED> gate (cascade-9 sub-classes A+B+C closed ...)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-11-VERIFICATION.md + bump depends_on (HARD-GATED on Task 2 APPROVED — FIRST APPROVED rewire in Phase 02.2's 11-plan history)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - `.planning/phases/02.2-.../02.2-02-PLAN.md` (full file; frontmatter — particularly the `depends_on: ["02.2-01"]` line at line 6, the `key_links` block, the `must_haves.truths` block; `<context>` block; Task 1 body — `<read_first>`, `<what-built>`, `<how-to-verify>`, `<verify>` automated block, `<acceptance_criteria>`, `<done>` — all of which currently reference `02.2-01-VERIFICATION.md` because Plans 02.2-03/04/05/06/07/08/09/10 Task 3 ALL DID NOT FIRE). Plan-time inspection: `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returned exactly `31`.
    - `.planning/phases/02.2-.../02.2-11-VERIFICATION.md` (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the HARD precondition for this Task 3 to fire)
    - `.planning/phases/02.2-.../02.2-01-PLAN.md` (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - `.planning/phases/02.2-.../02.2-09-PLAN.md` Task 3 body (the structural model for this task's edits; mirror its surgical-edit pattern with `02.2-09` → `02.2-11` everywhere)
    - `.planning/phases/02.2-.../02.2-10-PLAN.md` Task 3 body (the prior immediate-iteration Task 3 — also did not fire because Plan 02.2-10 returned BLOCKED; this task is the lineal successor)
    - `./CLAUDE.md` Working Rules + Operating Notes (Working Rule #3 verification protocols — "Cascade closed: the specific manager-log error that defined the cascade returns 0 in the new run's log"; Operating Note #2 — pre-merge cwd-drift check before declaring Task 3 complete; CLAUDE.md "Failure semantics at wave boundaries" contract — this task fires ONLY on APPROVED)
  </read_first>

  <action>
    **Gate precondition (HARD GATE — CLAUDE.md Verify-Before-Claiming):** Task 3 fires ONLY IF Task 2's `02.2-11-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED` (or PENDING, which is not a final state), HALT Task 3 — Plan 02.2-02 stays unmodified (it will continue to reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, 02.2-08 Task 3, 02.2-09 Task 3, AND 02.2-10 Task 3 ALL did NOT fire); the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md (Plan 02.2-12 takes the next iteration).

    Verify the precondition by running:
    ```bash
    grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md
    ```
    If this returns 0 (no APPROVED match), HALT Task 3 and surface the BLOCKED gate. Exit with a clear message: "Task 3 halted: 02.2-11-VERIFICATION.md gate_decision is not APPROVED — routing back to planner for next iteration (cascade-10 or whatever surfaced)."

    If APPROVED (returns exactly 1), make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-11-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering. There are 31 references to `02.2-01-VERIFICATION.md` in `02.2-02-PLAN.md` (verified at plan-time by `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returning exactly 31); all 31 are redirected to `02.2-11-VERIFICATION.md` in one pass.

    **HISTORIC NOTE on recovery state:** Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, 02.2-08 Task 3, 02.2-09 Task 3, AND 02.2-10 Task 3 each tried to do the same surgical edit (each pointing 02.2-02-PLAN.md at its own VERIFICATION.md), but none fired because each Task 2 gate was BLOCKED on the cascade-of-the-day. As a consequence, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today. This task's edits land the long-pending rewire — **THE FIRST APPROVED TASK-3 FIRE IN PHASE 02.2'S 11-PLAN HISTORY**.

    **Pre-edit count assertion (BEFORE any edits — Working Rule #1):** Run `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` and confirm the result is exactly `31`. If it is NOT 31, HALT — the rewire scope assumed at plan-time has drifted and the task needs planner re-evaluation.

    **Edit A — frontmatter `depends_on`:** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-11"]`. Single one-line change at the frontmatter line.

    **Edit B — frontmatter `must_haves.truths` block:** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-11-VERIFICATION.md`. "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → "Closeout fires ONLY when 02.2-11-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit C — frontmatter `key_links` block:** Update the entry that references `02.2-01-VERIFICATION.md` to reference `02.2-11-VERIFICATION.md`.

    **Edit D — `<context>` block:** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-11-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>`:** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-11-VERIFICATION.md (the gate artifact produced by Plan 11 Task 2 — Phase 02.2-11 closed the BLOCKED runtime gate from 02.2-10-VERIFICATION.md by bundling cascade-9 sub-class A (Task caps wallClockSeconds: 120 — credproxy initContainer deadline) + sub-class B (envtest Wave-roll-up Eventually 20s → 60s — reconcileDispatch requeue-load race) + sub-class C (Makefile timeout 600s → 1800s — wall-time safety net). All 9 prior cascades closed: cascade-1 PVC RWX→RWO; cascade-2 metrics-bind-address flag; cascade-3 webhook-cert-path flag; cascade-4 test-budget 300s→600s; cascade-5 credproxy-fixture-incomplete; cascade-6 Eventually-timeout-too-tight; cascade-7 caps/output/failure-fixture-incomplete; cascade-8 production-wiring-gap (cmd/manager/main.go Dispatcher field unassigned); cascade-9 sub-classes A+B+C. With cascade-9 closed, all 7 Layer B Ginkgo specs PASS on clean + idempotent rerun within the 1800s outer budget; 18/18 Layer A envtest specs PASS — Phase 02.2 cascade chain terminated at iteration 11)`.

    **Edit F — Task 1 `<what-built>`:** Update the prose to reference Plan 02.2-11's deliverable (cascade-9 sub-class A+B+C closure + runtime re-verification + chain termination) in addition to Plans 02.2-01, 02.2-03 through 02.2-10's deliverables. Append: "Plan 02.2-11 then closed cascade-9 (the cascade that surfaced after Plan 02.2-10's correct Dispatcher wiring made dispatch live for the first time). Three sub-class fixes bundled into one tactical iteration: sub-class A added `spec.caps.wallClockSeconds: 120` to the three wave-test Tasks in `test/integration/kind/testdata/three-task-wave.yaml`, raising `BuildJobSpec`'s computed `activeDeadlineSeconds` from 60s (the prior cascade-9A observation) to 180s — sufficient for the credproxy initContainer + tide-credproxy sidecar startup under kind. Sub-class B bumped the SUB-02 Wave-roll-up `Eventually` budget from `20s` to `60s` in `test/integration/envtest/indegree_test.go` — accommodating the reconcileDispatch requeue-load competition that Plan 02.2-10's correct Dispatcher wiring introduced. Sub-class C bumped the `Makefile` `test-int` recipe's outer `timeout 600s` to `timeout 1800s` as a wall-time safety net (the cascade-9A closure alone may meet the original 600s aspiration; the safety net absorbs residual if not). Zero production code changes (Option-B-test-only contract preserved — `internal/`, `cmd/manager/main.go`, `api/`, `pkg/`, `charts/` all byte-identical); Plan 02.2-10's Dispatcher wiring at `cmd/manager/main.go` lines 188-256 preserved verbatim (cascade-8 closure NOT regressed). Cascade-9 ValidationState gap (the deferred secondary risk from the credproxy-backoff-suppression debug session) explicitly DEFERRED — TaskReconciler.reconcileDispatch is independent of `plan.Status.ValidationState`, so the gap does NOT block HARN-03 dispatch. Plan 02.2-11's runtime verification (`02.2-11-VERIFICATION.md`) records the six load-bearing observation fields: `clean_run_wall_seconds`, `rerun_wall_seconds`, `credproxy_pod_wait_observed_seconds`, `ac1_wave_rollup_observed_seconds`, `deadline_exceeded_count` (APPROVED requires 0), `wall_under_600s` (true if A+B alone met aspiration; false if safety net absorbed residual). With cascade-9 closed and Phase 02.2's cascade chain terminated at iteration 11, the Phase can dispatch its closeout (Plan 02.2-02) in the next wave."

    **Edit G — Task 1 `<how-to-verify>`:** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-11-VERIFICATION.md`. Specifically: the human-verify instructions should now read `cat 02.2-11-VERIFICATION.md | grep gate_decision` (and similar) — bumping all path references.

    **Edit H — Task 1 `<verify>` automated block:** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-11-VERIFICATION.md`. The Task 1 acceptance grep gate `grep -E '^gate_decision: (APPROVED.{0,5}).{0,5}$' .*02.2-XX-VERIFICATION.md` becomes `.*02.2-11-VERIFICATION.md`.

    **Edit I — Task 1 `<acceptance_criteria>`:** Update the references to `02.2-01-VERIFICATION.md` → `02.2-11-VERIFICATION.md`.

    **Edit J — Task 1 `<done>`:** Update the references to `02.2-01-VERIFICATION.md` → `02.2-11-VERIFICATION.md` if any are present.

    **Post-edit count assertion (AFTER all edits — Working Rule #3):** Run `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `0`. Run `grep -c '02.2-11-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `31`. If either count is not as expected, surface the diff and HALT before commit.

    **(Edits to ROADMAP/STATE content NOT performed by this task — Plan 02.2-02 reads live ROADMAP/STATE at execution time via its own `<read_first>` and `<action>` blocks. Plan 02.2-02 is responsible for its own ROADMAP/STATE shape decisions at execution time.)**

    **Constraints:**
    - Do NOT edit Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08, 02.2-09, 02.2-10, or 02.2-11 (this plan) — only `02.2-02-PLAN.md` is touched
    - Do NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `Makefile`, `.github/`, `test/`. This task is documentation-only
    - Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — those are owned by Plan 02.2-02's tasks and by the orchestrator
    - Do NOT modify any other `.planning/phases/02.2-.../*-VERIFICATION.md` artifacts
    - The diff against `02.2-02-PLAN.md` must be minimal — only the 31 references to `02.2-01-VERIFICATION.md` are changed to `02.2-11-VERIFICATION.md`, plus the one `depends_on` value change. No new tasks added, no existing tasks removed, no structural reorganization.

    Commit with message:
    ```
    docs(02.2-11): wire Plan 02.2-02 checkpoint to 02.2-11-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-11; 31 references redirected; FIRST APPROVED Task-3 rewire in Phase 02.2's 11-plan history; cascade chain terminated at iteration 11)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Gate precondition: 02.2-11-VERIFICATION.md gate_decision is APPROVED
      [ "$(grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-VERIFICATION.md)" -eq 1 ] && \
      # Edit A: depends_on bumped to 02.2-11
      [ "$(grep -cE 'depends_on: \[.{0,5}02.2-11.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 1 ] && \
      # Old depends_on values 02.2-01 through 02.2-10 all purged from the depends_on field
      [ "$(grep -cE '^depends_on: \[.{0,5}02.2-(0[1345-9]|10).{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-11-VERIFICATION.md is now the gate reference — count exactly 31
      [ "$(grep -c '02.2-11-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 31 ] && \
      # 02.2-01-VERIFICATION.md references purged from 02.2-02-PLAN.md
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Prior VERIFICATION refs (03/04/05/06/07/08/09/10) purged from 02.2-02-PLAN.md
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-04-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-05-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-06-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-07-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-08-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-09-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-10-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Confirm no other plans were modified by this task
      ! (git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-02-PLAN.md' | grep -v '^$' >/dev/null) && \
      # Production source untouched
      [ -z "$(git status --porcelain internal/)" ] && \
      [ -z "$(git status --porcelain cmd/)" ] && \
      [ -z "$(git status --porcelain charts/)" ] && \
      [ -z "$(git status --porcelain test/)" ] && \
      [ -z "$(git status --porcelain Makefile)" ] && \
      echo "TASK 3 GATES PASSED — FIRST APPROVED REWIRE IN PHASE 02.2 HISTORY"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Gate precondition met (HARD GATE):** `grep -cE '^gate_decision: APPROVED$' 02.2-11-VERIFICATION.md` returns exactly `1`
    - **depends_on bumped (Edit A):** `grep -cE 'depends_on: \[.{0,5}02.2-11.{0,5}\]' 02.2-02-PLAN.md` returns exactly `1` AND `grep -cE '^depends_on: \[.{0,5}02.2-(0[1345-9]|10).{0,5}\]' 02.2-02-PLAN.md` returns `0` (old values 02.2-01 through 02.2-10 all purged from the depends_on field)
    - **New gate-artifact reference count (Edits B-J):** `grep -c '02.2-11-VERIFICATION.md' 02.2-02-PLAN.md` returns exactly `31` (one-for-one redirection from the 31 plan-time `02.2-01-VERIFICATION.md` references)
    - **Old gate-artifact references purged:** ALL of `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-03-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-04-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-05-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-06-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-07-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-08-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-09-VERIFICATION.md' 02.2-02-PLAN.md`, `grep -c '02.2-10-VERIFICATION.md' 02.2-02-PLAN.md` return `0`
    - **No other plans or source modified (regression guard):** `git status --porcelain` shows ONLY `02.2-02-PLAN.md` modified — no production source under `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `test/`, `Makefile` touched
    - **No other planning artifacts modified:** other than `02.2-02-PLAN.md`, no plans or VERIFICATION.md files in `.planning/phases/02.2-.../` are modified
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has been surgically edited to redirect its checkpoint from `02.2-01-VERIFICATION.md` to `02.2-11-VERIFICATION.md`, with `depends_on` bumped from `["02.2-01"]` to `["02.2-11"]`. All 31 prior `02.2-01-VERIFICATION.md` references are now `02.2-11-VERIFICATION.md` (one-for-one, count exactly 31). No stale references to `02.2-03/04/05/06/07/08/09/10-VERIFICATION.md` exist. No other plans or production source are modified. **This is the FIRST APPROVED Task-3 rewire in Phase 02.2's 11-plan history — Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, 02.2-08 Task 3, 02.2-09 Task 3, AND 02.2-10 Task 3 ALL halted on BLOCKED gates; this Task 3 is the first to fire.** Phase 02.2 cascade chain terminated at iteration 11. Plan 02.2-02 now dispatches in a follow-up wave to close Phase 02.2. Committed with the `docs(02.2-11): wire Plan 02.2-02 checkpoint to 02.2-11-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-11; 31 references redirected; FIRST APPROVED Task-3 rewire in Phase 02.2's 11-plan history; cascade chain terminated at iteration 11)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/integration/kind/testdata/three-task-wave.yaml` (`spec.caps.wallClockSeconds: 120`) → `internal/dispatch/podjob/jobspec.go:107-113` BuildJobSpec | The fixture's caps value is consumed by `BuildJobSpec` to compute `activeDeadlineSeconds = wallClockSeconds + DefaultWallClockGraceSeconds(60)`. The boundary is between "the test's Task CRD declares caps" and "the dispatch code computes the Job's deadline". After Task 1 Part A, the deadline becomes 180s, sufficient for the credproxy initContainer + sidecar startup observed in Plan 02.2-10's evidence. |
| `test/integration/envtest/indegree_test.go` (`Eventually` `60s` budget) → `internal/controller/wave_controller.go` reconcile loop | The spec patches each Task.Status.Phase=Succeeded then asserts the Wave's roll-up to Succeeded within the budget. The boundary is between "the test asserts a controller-driven status transition" and "the controller actually performs the transition under concurrent reconcile-load from `reconcileDispatch`'s requeues in the indegree namespace". 60s gives 3x the prior 20s budget. |
| `Makefile` `timeout 1800s` → `go test ./test/integration/...` | The outer timeout caps total suite wall-time. Inner Ginkgo per-spec `-timeout=5m` is preserved unchanged. The 1800s is a safety-net per T-02.2-34 — if A+B closure drops wall under 600s, the 1800s headroom is unused. |
| Task 1 (3 sub-fixes) → Task 2 (runtime verification) | Task 2 runs after Task 1's commit lands; the runtime reproducer exercises the bundled cascade-9 closure end-to-end, validating all three sub-class fixes simultaneously. |
| Task 2 (gate artifact + 6 observation fields) → Task 3 (Plan 02.2-02 wiring) | Task 3 reads Task 2's `gate_decision:` frontmatter. APPROVED → Task 3 fires (first APPROVED Task-3 fire in Phase 02.2's history); BLOCKED → Task 3 halts and the BLOCKED failure routes to a planner re-evaluation for Plan 02.2-12. |
| Plan 02.2-11 Task 3 edits → Plan 02.2-02 checkpoint target | Surgical edit only — `depends_on` and 31 verification-artifact references in `02.2-02-PLAN.md` are bumped; no other planning artifacts touched. |
| `02.2-11-VERIFICATION.md` runtime-observation fields → Phase 03+ production-code follow-up decision | If `deadline_exceeded_count > 0` even after the cascade-9A fixture fix (e.g., the credproxy initContainer takes >120s under kind for an unrelated reason), the gate is BLOCKED and the analysis routes to Plan 02.2-12 or a production-code follow-up. The ValidationState gap (deferred per `02.2-10-VERIFICATION.md` §Section 5 lines 283-291) is also a Phase 03+ follow-up — not in this plan's scope. |
| Worktree branch (if T2 ran in worktree) → main branch | Per CLAUDE.md Operating Note #2, pre-merge cwd-drift check: any untracked `02.2-11-` files in main's working tree must be diffed against the worktree branch's commits and duplicates removed before `git merge`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-32 | Tampering (cascade-9A fix at wrong site) | The cascade-9A fix could land at the wrong site — e.g., touching `internal/dispatch/podjob/jobspec.go` to change `DefaultWallClockGraceSeconds` from 60 to 120 (Option (b) from `02.2-10-VERIFICATION.md` §Section 5 line 261). That production-side change would (a) violate Option-B test-only contract; (b) regress the existing `TestBuildJobSpec_ActiveDeadlineSeconds_HasGrace` unit test which asserts the +60 grace formula; (c) introduce a production-code surface (the "test vs production profile" Option (c) at line 260-261) that has no caller yet. | mitigate | The fix lands at the FIXTURE SIDE only — `test/integration/kind/testdata/three-task-wave.yaml` Tasks alpha/beta/gamma get `spec.caps.wallClockSeconds: 120`. The production-code path `internal/dispatch/podjob/jobspec.go` is UNTOUCHED (`DefaultWallClockGraceSeconds = 60` preserved). **Acceptance gate:** `grep -c 'wallClockSeconds: 120' three-task-wave.yaml` returns exactly 3; `git status --porcelain internal/dispatch/podjob/` returns empty; `go test -count=1 -run TestBuildJobSpec ./internal/dispatch/podjob/...` exits 0 (unit test unaffected). The smallest-surface fix preserves Option-B contract, mirrors the established `caps_test.go` convention, and produces a deterministic 180s `activeDeadline` for the wave-test Tasks. |
| T-02.2-33 | Tampering (cascade-9B fix scope creep) | The cascade-9B fix could expand scope — bumping every `Eventually` in `indegree_test.go` (lazy approach) OR touching the upstream `reconcileDispatch` (Option (c) at line 270-271 — out of cascade-9 scope per planner authority limits). Either expansion risks (a) masking a real 60s+ regression with overly-generous budgets; (b) introducing production-code changes that should be a Phase 03+ debug session. | mitigate | The fix is SCOPED to exactly ONE `Eventually` — the SUB-02 Wave-roll-up Succeeded spec at `indegree_test.go:232` (the literal `"20s", "500ms"` line). Other `Eventually` blocks (the inner `"10s", "200ms"` task-status-patch loop; the FAIL-02 block at lines 237-295) are preserved. The 60s budget choice is 3x the prior — generous enough to absorb requeue-load race but tight enough that observed wall ≤ 45s proves the fix has headroom, not masking a regression. **Acceptance gate:** `grep -c '"20s", "500ms"' indegree_test.go` returns 0 AND `grep -c '"60s", "500ms"' indegree_test.go` returns 1; `grep -c '"10s", "200ms"' indegree_test.go` returns ≥ 1 (inner loop preserved); T2 verifies `ac1_wave_rollup_observed_seconds ≤ 45` for APPROVED. The `reconcileDispatch` requeue-load root cause is documented as deferred follow-up — NOT addressed in Plan 02.2-11. |
| T-02.2-34 | Denial of Service (cascade-9C safety-net unused) | The Makefile `timeout 600s` → `timeout 1800s` bump is a safety net. If cascade-9A+B closure alone drops wall under 600s (the original Plan 02.2-06 aspiration), the 1800s headroom is unused — wasteful but acceptable. The risk is the inverse: if A+B closure does NOT drop wall under 1800s either, cascade-9C is genuinely insufficient and we need to split Layer A and Layer B into separate Makefile targets (Option C from `02.2-10-VERIFICATION.md` §Section 5 line 295-310). | accept | T2 records `wall_under_600s: true|false` flag. APPROVED requires wall ≤ 1800 on both runs; the boolean annotates whether A+B alone met aspiration (informational, not gate). If a future Plan 02.2-12 surfaces because wall exceeds 1800s, the next iteration would be the Layer A / Layer B split. **Acceptance gate:** `grep -cE '^wall_under_600s: (true|false)$' 02.2-11-VERIFICATION.md` returns 1; clean_run_wall_seconds + rerun_wall_seconds both ≤ 1800. |
| T-02.2-35 | Denial of Service (cascade-10 surfacing — unknown shape) | Cascade-10 (if any) is genuinely unknown — but cascade-9 closes the cascade chain's currently-visible blockers. Per CLAUDE.md Operating Note #1, this plan is NOT tagged as a chain-terminator candidate. Possible cascade-10 shapes: (a) AC1 Wave-roll-up still fails with 60s budget — `spec-flake` requiring 90s OR upstream `reconcileDispatch` fix; (b) Pods now passing produce output exposing a WaveReconciler bug — `other`; (c) `KEEP_KIND_CLUSTER=true` rerun trips idempotency bug — `harness-bug`; (d) flag/charts regression — `flag-mismatch`; (e) image pull or kind networking under longer suite — `kind-infra`; (f) ResourceVersion conflict cascade under correctly-dispatching kind run — `other` (production-code follow-up). | accept | Accepted with the requirement that if T2's gate is BLOCKED, `02.2-11-VERIFICATION.md` §Section 5 MUST classify cascade-10 using one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other` AND identify the specific failing spec(s) AND propose Plan 02.2-12's fix shape inline. If APPROVED, the artifact notes "cascade chain potentially terminated at iteration 11; further runs will validate stability" — NOT claiming definitive termination (per Operating Note #1). **Acceptance gate:** if BLOCKED, the artifact contains one of the six classification tokens AND §Section 5 names the specific failure evidence; if APPROVED, the closing prose is iteration-shaped, not termination-shaped. |
| T-02.2-36 | Tampering (cascade-8 closure regression) | Plan 02.2-10's `cmd/manager/main.go` Dispatcher wiring at lines 188-256 is the architectural foundation that made cascade-9 surfacing possible. Touching `cmd/manager/main.go` in Plan 02.2-11 would risk regressing cascade-8 closure — either by accidentally deleting the `dispatcher := &podjob.PodJobBackend{...}` block, removing one of the two `Dispatcher: dispatcher,` field assignments, or modifying any of the 7 PodJobBackend fields (Client/Scheme/SubagentImage/CredproxyImage/SigningKey/EnvReader/PVCName). Similarly, touching `charts/tide/values.yaml` violates the FIXED contract anti-pattern from CLAUDE.md. | mitigate | **Acceptance gate (T1 pre+post-edit; T2 runtime verification):** `grep -c 'dispatcher := &podjob.PodJobBackend{' cmd/manager/main.go` returns exactly 1 BOTH before and after Task 1; `grep -cE '^[[:space:]]+Dispatcher:[[:space:]]+dispatcher,' cmd/manager/main.go` returns exactly 2 BOTH before and after Task 1; `git status --porcelain cmd/manager/main.go` returns empty after Task 1; `git status --porcelain charts/` returns empty. **Runtime evidence in T2:** `grep -cE '"creating job"|"dispatch"' /tmp/02.2-11-clean-run.log` returns ≥ 1 (dispatch still active); `kubectl get jobs --all-namespaces` shows Jobs in test namespaces during the run (cascade-8 architecturally preserved). If the runtime evidence shows ZERO `creating job` lines OR ZERO Jobs in test namespaces, cascade-8 has regressed and T2 gate is BLOCKED with `harness-bug` or `other` classification. |
| Carry-forward T-02.2-01 through T-02.2-31 | Various | All prior threat mitigations from Plans 02.2-03 through 02.2-10 | mitigate | Carried forward verbatim. Specifically: T-02.2-10 through T-02.2-18 (Makefile budget — bumped from 600s to 1800s in this plan; the Plan 02.2-06 fix lineage preserved); T-02.2-19 through T-02.2-21 (Eventually timeouts — cascade-6 closure preserved); T-02.2-22 through T-02.2-27 (Plan 02.2-09 createProjectHierarchy — cascade-7 closure preserved); T-02.2-28 through T-02.2-31 (Plan 02.2-10 PodJobBackend instantiation correctness + dispatch-path coverage + signing-key wiring — cascade-8 closure preserved). **Acceptance gate:** runtime evidence in T2 shows zero `flag provided but not defined` (cascades 2/3 preserved); zero `no project found in namespace <ns>` for any test namespace (cascades 5/6/7 preserved); PVC `tide-projects` Bound RWO (cascade-1 preserved); dispatch_active true (cascade-8 preserved). |

</threat_model>

<verification>
After all three tasks complete (or after Task 2 if its gate is BLOCKED):

**Source gates (Task 1):**
- `test/integration/kind/testdata/three-task-wave.yaml` has three `spec.caps.wallClockSeconds: 120` additions (alpha, beta, gamma — `grep -c 'wallClockSeconds: 120' three-task-wave.yaml` returns 3)
- `test/integration/envtest/indegree_test.go` SUB-02 Wave-roll-up `Eventually` bumped 20s → 60s (`grep -c '"20s", "500ms"' indegree_test.go` returns 0; `grep -c '"60s", "500ms"' indegree_test.go` returns 1)
- `Makefile` `test-int` recipe `timeout 600s` bumped to `timeout 1800s` (`grep -c 'timeout 600s' Makefile` returns 0; `grep -c 'timeout 1800s' Makefile` returns 1)
- Production source UNTOUCHED — `internal/`, `cmd/manager/main.go`, `api/`, `pkg/`, `charts/` all byte-identical (T-02.2-36 mitigation)
- Plan 02.2-10's Dispatcher wiring at `cmd/manager/main.go` preserved (cascade-8 closure NOT regressed)
- Cascade-5/6/7 fixture refactors preserved — `credproxy_test.go`/`caps_test.go`/`output_test.go`/`failure_test.go`/`suite_test.go`/`wave_test.go` all UNTOUCHED
- `go build ./...` + `go vet ./...` + `go test -count=1 -run TestBuildJobSpec ./internal/dispatch/podjob/...` all exit 0

**Runtime gates (Task 2):**
- `02.2-11-VERIFICATION.md` exists with all required frontmatter fields populated (`phase: 02.2`, `plan: 11`, `task: 2`, `artifact: verification`, exit codes, wall seconds, spec counts, AND ALL SIX runtime-observation fields: `clean_run_wall_seconds`, `rerun_wall_seconds`, `credproxy_pod_wait_observed_seconds`, `ac1_wave_rollup_observed_seconds`, `deadline_exceeded_count`, `wall_under_600s`, AND both image-digest fields)
- `gate_decision` is APPROVED or BLOCKED (PENDING not a final state)
- Image-rebuild verification per CLAUDE.md Working Rule #4: `manager_image_digest_current ≠ manager_image_digest_prior` (binary rebuilt; manager Pod rotated)
- Sections 1-6 mirror `02.2-10-VERIFICATION.md`'s structural template
- Cascade-8 preservation evidence: `grep -cE '"creating job"|"dispatch"' /tmp/02.2-11-clean-run.log` returns ≥ 1; `kubectl get jobs --all-namespaces` shows Jobs in test namespaces (cascade-8 NOT regressed)
- Cascade-9A closure evidence (APPROVED only): `deadline_exceeded_count: 0`
- Cascade-9B closure evidence (APPROVED only): Layer A AC1 Wave-roll-up Succeeded spec PASS; `ac1_wave_rollup_observed_seconds ≤ 45`
- Cascade-9C closure evidence (APPROVED only): both runs ≤ 1800s; `wall_under_600s: true|false` annotation
- Prior cascades preserved (APPROVED only): zero `flag provided but not defined` AND zero `no project found` for any test namespace
- Worktree cwd-drift mitigation honored: no orphan `02.2-11-` files in main worktree before merge (CLAUDE.md Operating Note #2)
- Cascade-10 classification present per T-02.2-16 taxonomy if BLOCKED (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`)
- Iteration-shaped closing prose if APPROVED (no chain-terminator claim per CLAUDE.md Operating Note #1)

**Wiring gates (Task 3 — HARD-GATED on Task 2 APPROVED):**
- `02.2-02-PLAN.md` has `depends_on: ["02.2-11"]` and ALL 31 references updated from `02.2-01-VERIFICATION.md` to `02.2-11-VERIFICATION.md` (exact count one-for-one)
- Stale references to `02.2-01/03/04/05/06/07/08/09/10-VERIFICATION.md` all purged from `02.2-02-PLAN.md`
- No other plans modified
- **FIRST APPROVED Task-3 rewire in Phase 02.2's 11-plan history**

**Structural gates (T-02.2-32 through T-02.2-36 mitigation):**
- T-02.2-32: cascade-9A fix at fixture site (not production code); 3 caps additions to three-task-wave.yaml
- T-02.2-33: cascade-9B scoped to ONE Eventually only; ac1_wave_rollup_observed_seconds ≤ 45 proves headroom
- T-02.2-34: cascade-9C safety-net annotated with `wall_under_600s` flag; APPROVED accepts wall ≤ 1800s
- T-02.2-35: cascade-10 classification present if BLOCKED; iteration-shaped closing if APPROVED
- T-02.2-36: cmd/manager/main.go untouched; charts/ untouched; dispatch_active preserved at runtime
- Carry-forward T-02.2-01 through T-02.2-31: zero flag-mismatch errors; zero no-project errors; PVC RWO; dispatch_active true
- Cascade-9 ValidationState gap explicitly DEFERRED (not in scope per `02.2-10-VERIFICATION.md` §Section 5 lines 283-291)
</verification>

<success_criteria>
- Task 1 cascade-9 sub-class A+B+C fixes landed: three `caps.wallClockSeconds: 120` additions in `three-task-wave.yaml`; one `Eventually` budget bump 20s → 60s in `indegree_test.go`; one Makefile `timeout 600s` → `timeout 1800s` bump; production source UNTOUCHED (`internal/`, `cmd/manager/main.go`, `api/`, `pkg/`, `charts/` all byte-identical — T-02.2-36 mitigation); cascade-8 Dispatcher wiring at `cmd/manager/main.go` preserved verbatim; cascade-5/6/7 fixture refactors preserved; `go build` + `go vet` + `go test TestBuildJobSpec` all exit 0
- Task 2 runtime verification artifact authored at `.planning/phases/02.2-.../02.2-11-VERIFICATION.md` with `gate_decision: APPROVED` or `gate_decision: BLOCKED` and all required structural sections, including ALL SIX runtime-observation fields (`clean_run_wall_seconds`/`rerun_wall_seconds`/`credproxy_pod_wait_observed_seconds`/`ac1_wave_rollup_observed_seconds`/`deadline_exceeded_count`/`wall_under_600s`) + both image-digest fields + cascade-8 preservation evidence (`"creating job"` log lines + Jobs in test namespaces) + cascade-9 sub-class closure evidence (deadline_exceeded_count: 0 if APPROVED; AC1 Wave-roll-up PASS at ≤ 45s if APPROVED; wall ≤ 1800s on both runs if APPROVED) + four-namespace state listings + cascade-10 classification per T-02.2-16 taxonomy if BLOCKED; §Section 5 preserves the cascade-1-through-9 history AND classifies cascade-10 if BLOCKED OR notes "cascade chain potentially terminated at iteration 11" if APPROVED (per CLAUDE.md Operating Note #1, no chain-terminator claim)
- Task 3 (HARD-GATED on Task 2 APPROVED) wired Plan 02.2-02's checkpoint to `02.2-11-VERIFICATION.md` with `depends_on: ["02.2-11"]` and all 31 stale `02.2-01-VERIFICATION.md` references one-for-one redirected — **this is the FIRST APPROVED Task-3 rewire in Phase 02.2's 11-plan history** (Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, 02.2-08 Task 3, 02.2-09 Task 3, 02.2-10 Task 3 ALL halted on BLOCKED)
- If Task 2 returns BLOCKED: Task 3 does NOT fire; Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring); the orchestrator routes to a planner re-evaluation for Plan 02.2-12 per the wave-failure-semantics contract; the cascade-10 classification in the verification artifact directs the planner to the right fix shape (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`)
- T-02.2-32 through T-02.2-36 each have concrete evidence in the verification artifact and the source-edit acceptance gates
- Tactical scope honored: ONLY `three-task-wave.yaml` (3 caps additions) + `indegree_test.go` (1 Eventually bump) + `Makefile` (1 timeout bump) + `02.2-11-VERIFICATION.md` (new artifact) + (if APPROVED) `02.2-02-PLAN.md` modified. NO production source under `internal/`, `cmd/manager/main.go`, `api/`, `pkg/`, `charts/` touched (T-02.2-36); NO chart modifications (CLAUDE.md Execute-Don't-Ask exception); NO Plans 02.2-01, 02.2-03 through 02.2-10 or 02.2-11 (this plan) modified; NO `.planning/STATE.md` or `.planning/ROADMAP.md` touched
- **Iteration-11 framing preserved per CLAUDE.md Operating Note #1:** if APPROVED, the artifact and SUMMARY use iteration-shaped language ("cascade chain potentially terminated at iteration 11; further runs will validate stability") NOT chain-terminator-candidate-vN claims. If BLOCKED, the artifact classifies cascade-10 and proposes Plan 02.2-12's fix shape inline — the chain continues until APPROVED is observed empirically
- Worktree cwd-drift mitigation (CLAUDE.md Operating Note #2): if T2 ran in a worktree, the pre-merge check ran and any orphan `02.2-11-` files in main were diffed against the worktree branch's commits and duplicates removed before merge
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-11-SUMMARY.md` documenting:
- The cascade-9 sub-class A+B+C bundle (Task 1): three `caps.wallClockSeconds: 120` additions in `three-task-wave.yaml`; the `Eventually` budget bump 20s → 60s in `indegree_test.go`; the Makefile `timeout 600s` → `timeout 1800s` bump; rationale for Option (a) selection at each sub-class per `<plan_decision>` in objective; the non-modification of `cmd/manager/main.go` (cascade-8 closure preserved); the non-modification of `internal/dispatch/podjob/jobspec.go` (production-code formula preserved); the non-modification of `charts/tide/values.yaml` (CLAUDE.md anti-pattern); the non-modification of cascade-5/6/7 fixture files
- The runtime verification outcome (Task 2): gate decision (APPROVED or BLOCKED), per-spec outcomes, wall times, exit codes, Pod-state evidence (including image-digest change per Working Rule #4), manager-log evidence (particularly the absence of `DeadlineExceeded` if APPROVED — proving cascade-9A closure), all six runtime-observation fields (`clean_run_wall_seconds`/`rerun_wall_seconds`/`credproxy_pod_wait_observed_seconds`/`ac1_wave_rollup_observed_seconds`/`deadline_exceeded_count`/`wall_under_600s`), cascade-8 preservation evidence (`"creating job"` log lines + Jobs in test namespaces), cascade-10 classification per T-02.2-16 (or iteration-shaped "cascade chain potentially terminated at iteration 11" if APPROVED)
- The Plan 02.2-02 wiring update (Task 3, if APPROVED): which references were updated (count exactly 31), depends_on bump from 02.2-01 → 02.2-11 — **FIRST APPROVED Task-3 rewire in Phase 02.2's 11-plan history**
- Any inconsistencies surfaced (marginal-headroom flag if observed wall ≥ 1500s; marginal-AC1-headroom flag if `ac1_wave_rollup_observed_seconds ≥ 50`; cascade-10 surfacing on BLOCKED with classification)
- The Phase 02.2 close-out chain progression: cascade-1 PVC + cascade-2 metrics flag + cascade-3 webhook-cert-path flag + cascade-4 Makefile timeout + cascade-5 credproxy fixture + cascade-6 Eventually timeouts + cascade-7 caps/output/failure fixtures + cascade-8 Dispatcher wiring + cascade-9 (sub-class A activeDeadline + sub-class B Wave-roll-up Eventually + sub-class C wall budget) → runtime verified at iteration 11 (APPROVED → Plan 02.2-02 unblocked → Phase 02.2 closes; or BLOCKED → routed to Plan 02.2-12 with cascade-10 classification)
- Threat-model mitigations applied (T-02.2-32 / T-02.2-33 / T-02.2-34 / T-02.2-35 / T-02.2-36) with concrete evidence + carry-forward T-02.2-01 through T-02.2-31
- An explicit cascade-9 ValidationState gap DEFERRAL note: per `02.2-10-VERIFICATION.md` §Section 5 lines 283-291, the `plan.Status.ValidationState` never-set gap is a real production-side concern but does NOT block `TaskReconciler.reconcileDispatch` (the HARN-03 dispatch path is independent of ValidationState). Plan 02.2-11 explicitly does NOT touch this gap; it remains a Phase 03+ debug-session follow-up
- An explicit framing-preservation note per CLAUDE.md Operating Note #1: Plan 02.2-11 does NOT claim chain-terminator-candidate-vN status. The chain terminates when APPROVED is observed empirically, not when a plan predicts termination. If APPROVED, the SUMMARY's closing prose is iteration-shaped: "Phase 02.2's cascade chain reached its terminal-condition APPROVED gate at iteration 11; further runs will validate stability." If BLOCKED, the SUMMARY notes the cascade-10 classification and the routing to Plan 02.2-12
- **Future-phase targets surfaced:** (a) the underlying `reconcileDispatch` requeue-load semantics for Tasks in namespaces without a parent Project — a Phase 03+ production-code fix to either short-circuit cleanly on missing-Project (returning ctrl.Result{} without requeue) OR add backoff to the requeue (rather than infinite retry); (b) the cascade-9 ValidationState gap (`internal/webhook/v1alpha1/plan_webhook.go:117-199` never sets `plan.Status.ValidationState`); (c) if T-02.2-34 records `wall_under_600s: false`, the Layer A / Layer B Makefile target split (Option C from `02.2-10-VERIFICATION.md` §Section 5 line 295-310) becomes a real concern
- A structural-shift observation: cascades 1-3 were flag-mismatch (predictable, terminated at 5/5 chart args); cascade-4 was budget-vs-actual (Makefile fix); cascade-5 was harness-bug — credproxy fixture-incomplete; cascade-6 was spec-flake (Eventually bumps); cascade-7 was harness-bug — caps/output/failure fixture-incomplete; cascade-8 was production-wiring-gap (cmd/manager/main.go Dispatcher field unassigned — the architectural blocker that all prior cascades were SYMPTOMS of); cascade-9 was the cascade-of-cascades surfaced by cascade-8's closure (multi-class: activeDeadline + spec-flake + budget-vs-actual). **Phase 02.2's pattern: tactical iteration with runtime verification at each step was the right shape; comprehensive audit could not have caught cascade-9 because it required cascade-8 closure to surface.** The 11-plan iteration count reflects accumulated debt in the prior phases' "happy path" assumptions about K8s integration testing under kind — debt that the runtime reproducer surfaced one cascade at a time
</output>

## PLANNING COMPLETE
