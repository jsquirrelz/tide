---
phase: 02.2
plan: 04
type: execute
wave: 3
gap_closure: true
depends_on: ["02.2-03"]
files_modified:
  - cmd/manager/main.go
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, manager-flags, runtime-verification, kind-integration, phase-02.2, tactical]

threat_model_summary:
  - "T-02.2-08 (mitigate): adding --metrics-bind-address changes manager startup behavior in production deployments — mitigated by keeping default `:8443` identical to the chart's existing `manager.args` value (charts/tide/values.yaml:20) so production renders the same effective bind address. Acceptance: `grep -c 'flag.StringVar.*\"metrics-bind-address\"' cmd/manager/main.go` returns 1 AND `grep -c '\":8443\"' cmd/manager/main.go` returns ≥ 1"
  - "T-02.2-09 (mitigate): Manager.Options.Metrics.BindAddress wire-up could collide with an existing metrics endpoint in pkg/otelai or internal/ — verified absent by `grep -rnE 'metrics-bind-address|MetricsBindAddress' internal/ pkg/ cmd/` returning zero matches at plan-time; the existing wire-up at cmd/manager/main.go line 136 (`Metrics: metricsserver.Options{BindAddress: \":8080\"}`) hardcodes `:8080`, not `:8443`, so changing it to read from the new flag (default `:8443`) aligns the binary with the chart and removes the cross-purpose hardcode"
  - "T-02.2-10 (accept): tactical scope means --leader-elect and --webhook-cert-path remain undefined in cmd/manager/main.go and WILL likely re-surface as the next CrashLoopBackOff cause once the metrics flag is defined — accepted per user choice for tactical iteration. Plan 02.2-04 Task 2 (runtime verification) MUST document the next surfacing failure mode in 02.2-04-VERIFICATION.md so the orchestrator can route to Plan 02.2-05 (or equivalent) for the next iteration"

must_haves:
  truths:
    - "`cmd/manager/main.go` defines a `--metrics-bind-address` flag with default `:8443` matching `charts/tide/values.yaml:20`"
    - "The Manager's `ctrl.Options.Metrics.BindAddress` is wired from the new `metricsAddr` variable (replacing the prior hardcoded `:8080`)"
    - "`go build ./cmd/manager/...` exits 0 after the flag addition"
    - "`02.2-04-VERIFICATION.md` exists with frontmatter `phase: 02.2`, `plan: 04`, `task: 3`, `artifact: verification`, a populated `gate_decision: APPROVED|BLOCKED` line, and Sections 1-6 mirroring `02.2-03-VERIFICATION.md`"
    - "If `02.2-04-VERIFICATION.md` records `gate_decision: APPROVED`, then Task 3 fires and Plan 02.2-02's `depends_on` becomes `[\"02.2-04\"]` and its checkpoint references `02.2-04-VERIFICATION.md`"
    - "If `02.2-04-VERIFICATION.md` records `gate_decision: BLOCKED`, Task 3 does NOT fire — Plan 02.2-02 stays gated on `02.2-03-VERIFICATION.md` (Plan 02.2-03's prior wiring) and the BLOCKED failure routes to a planner re-evaluation"
  artifacts:
    - path: "cmd/manager/main.go"
      provides: "`--metrics-bind-address` flag definition + Manager.Options.Metrics.BindAddress wire-up"
      contains: "metrics-bind-address"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md"
      provides: "Runtime verification artifact with gate_decision frontmatter — authoritative gate for Plan 02.2-02 closeout"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-04\"] + checkpoint references 02.2-04-VERIFICATION.md (gated on Task 2 APPROVED)"
      contains: "02.2-04"
  key_links:
    - from: "cmd/manager/main.go (new flag definition)"
      to: "controller-runtime Manager's Metrics.BindAddress option"
      via: "ctrl.Options{ Metrics: metricsserver.Options{BindAddress: metricsAddr} }"
      pattern: 'BindAddress:\s*metricsAddr'
    - from: "charts/tide/values.yaml:20 (--metrics-bind-address=:8443 arg)"
      to: "cmd/manager/main.go --metrics-bind-address=:8443 default"
      via: "matching default value across chart and binary"
      pattern: '":8443"'
    - from: "02.2-04-VERIFICATION.md frontmatter gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 wires Plan 02.2-02's depends_on + checkpoint references (gated on APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md (depends_on field)"
      to: "this plan's verification artifact"
      via: "frontmatter depends_on: [\"02.2-04\"]"
      pattern: 'depends_on: \[.{0,5}02.2-04.{0,5}\]'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-03-VERIFICATION.md` §"Root-Cause Summary" Option A. The chart's `manager.args` block passes `--metrics-bind-address=:8443` at `charts/tide/values.yaml:20`, but `cmd/manager/main.go` does NOT define that flag — Go's `flag.Parse()` calls `Usage()` and exits 2, kubelet restarts the Pod, and `tide-controller-manager` enters `CrashLoopBackOff`. helm install --wait blocks; outer `timeout 300s` fires; Layer B specs never run.

This plan fixes ONLY the immediate blocker (`--metrics-bind-address`) and then re-runs the same end-to-end runtime reproducer captured in `02.2-03-VERIFICATION.md`. The expected outcome is one of:
- **APPROVED:** the manager starts cleanly, helm install --wait completes, all 7 Layer B specs run and PASS. Plan 02.2-02 (closeout) gets unblocked via Task 3's surgical edits.
- **BLOCKED (most likely per the verification's Root-Cause analysis):** the manager passes the `--metrics-bind-address` parse but immediately fails on the NEXT undefined flag (`--leader-elect` is most likely per `02.2-03-VERIFICATION.md` §"Fix landscape" Option C audit prediction; `--webhook-cert-path` would surface after that). The BLOCKED gate captures the new failure mode root cause and routes back to the planner for Plan 02.2-05 (or equivalent next iteration).

**TACTICAL SCOPE — user explicitly chose tactical iteration over comprehensive audit:**
- This plan defines `--metrics-bind-address` ONLY
- This plan does NOT define `--leader-elect`, `--webhook-cert-path`, `--health-probe-bind-address`, or any other kubebuilder-standard flag not in the chart's `manager.args` block
- This plan does NOT audit `charts/tide/values.yaml:18-24` against `cmd/manager/main.go`'s defined flags (Option C from the verification's Fix landscape — explicitly rejected per user choice)
- This plan does NOT touch `charts/tide/values.yaml:20` — the chart side is unchanged; the binary catches up to the chart contract
- This plan does NOT touch Plans 02.2-01, 02.2-02, or 02.2-03 except for Task 3's surgical edits to 02.2-02-PLAN.md

**Plan 02.2-03's pattern this plan mirrors:**
- Plan 02.2-03 Task 4 surgically edited Plan 02.2-02's `depends_on` from `["02.2-01"]` to `["02.2-03"]` (which never fired because Task 3's gate decision was BLOCKED — Plan 02.2-02 still references `02.2-01-VERIFICATION.md` at its read paths). This plan's Task 3 picks up that wiring exactly where Plan 02.2-03 Task 4 left off, redirecting from `02.2-01-VERIFICATION.md` → `02.2-04-VERIFICATION.md` and bumping `depends_on` from `["02.2-01"]` → `["02.2-04"]`. **The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic:** Task 3 fires ONLY if Task 2's `gate_decision: APPROVED`.

Purpose: close the BLOCKED runtime gate from `02.2-03-VERIFICATION.md` (chart fix worked; new pre-existing manager-args flag mismatch unmasked) with the cheapest possible source-side fix that aligns the binary with the chart contract for one flag. Tactical iteration: fix, re-verify, see what surfaces next.

Output:
- `cmd/manager/main.go` with `--metrics-bind-address` flag defined (default `:8443`) and wired into `ctrl.Options.Metrics.BindAddress`
- `.planning/phases/02.2-.../02.2-04-VERIFICATION.md` mirroring `02.2-03-VERIFICATION.md`'s structural template; `gate_decision: APPROVED` or `BLOCKED`
- If Task 2 APPROVED: `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-04"]` and checkpoint references pointing at `02.2-04-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from cmd/manager/main.go + charts/tide/values.yaml. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From cmd/manager/main.go (current state — pre-edit):

Line 86-95 (function entry — flag variable declarations block):
```go
func main() {
    var configPath string
    var leaderElect bool
    var watchNamespace string
    // Phase 2 flags (Plan 12 wiring).
    var subagentImage string
    var credproxyImage string
    var defaultFileTouchMode string
    var rateLimitDefaultRPM int
    var rateLimitDefaultBurst int
```

Lines 97-105 (current flag.StringVar / flag.BoolVar / flag.IntVar definitions block):
```go
    flag.StringVar(&configPath, "config", "/etc/tide/config.yaml", "Path to runtime config YAML")
    flag.BoolVar(&leaderElect, "leader-elect", true, "Enable leader election (CTRL-03)")
    flag.StringVar(&watchNamespace, "watch-namespace", "", "Restrict watches to this namespace (AUTH-02). Empty = all namespaces.")
    // Phase 2 flags — bound from Helm values.yaml via the controller Deployment args.
    flag.StringVar(&subagentImage, "subagent-image", "", "Image ref for the subagent container (images.stubSubagent in values.yaml)")
    flag.StringVar(&credproxyImage, "credproxy-image", "", "Image ref for the tide-credproxy sidecar (images.credProxy in values.yaml)")
    flag.StringVar(&defaultFileTouchMode, "default-file-touch-mode", "warn", "Cluster-level file-touch validation mode (planAdmission.fileTouchMode in values.yaml; warn|strict)")
    flag.IntVar(&rateLimitDefaultRPM, "rate-limit-default-rpm", 60, "Default requests-per-minute rate limit (rateLimits.defaults.requestsPerMinute in values.yaml)")
    flag.IntVar(&rateLimitDefaultBurst, "rate-limit-default-burst", 10, "Default burst size for rate-limit buckets (rateLimits.defaults.burst in values.yaml)")
```

**Important: `--leader-elect` IS defined at line 98. The next-most-likely undefined flag from `charts/tide/values.yaml:18-24` is `--webhook-cert-path=/tmp/k8s-webhook-server/serving-certs` at values.yaml line 24. Verify your prediction in Task 2.**

Lines 131-138 (current Manager construction — the wire-up target):
```go
    mgr, err := ctrl.NewManager(ctrl.GetConfigOrDie(), ctrl.Options{
        Scheme:                 scheme,
        LeaderElection:         leaderElect,
        LeaderElectionID:       "tide-controller-leader.tideproject.k8s",
        HealthProbeBindAddress: ":8081",
        Metrics:                metricsserver.Options{BindAddress: ":8080"},  // <-- CURRENT HARDCODE; change to metricsAddr
        WebhookServer:          webhook.NewServer(webhook.Options{Port: 9443}),
    })
```

From charts/tide/values.yaml lines 17-24 (the chart's `manager.args` block — DO NOT TOUCH):
```yaml
controllerManager:
  manager:
    args:
    - --metrics-bind-address=:8443
    - --config=/etc/tide/config.yaml
    - --watch-namespace=$(WATCH_NAMESPACE)
    - --leader-elect
    - --webhook-cert-path=/tmp/k8s-webhook-server/serving-certs
```

From 02.2-03-VERIFICATION.md §"Root-Cause Summary" Option A (line 282):
"Define `--metrics-bind-address` in `cmd/manager/main.go`: Add `flag.StringVar(&metricsAddr, \"metrics-bind-address\", \":8443\", \"Bind address for the metrics endpoint\")` and consume it when constructing the controller-runtime Manager (likely already done via `Manager.Options.Metrics.BindAddress`). Tradeoff: minimal touch; aligns with kubebuilder convention and operator-sdk patterns. Most likely THE fix."

From CLAUDE.md "Failure semantics at wave boundaries" contract:
- Failed task → siblings in same wave continue
- Dependents in later waves never dispatch
- Plan 02.2-02 reads the gate artifact; if BLOCKED, Plan 02.2-02 stays gated

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define `--metrics-bind-address` flag in cmd/manager/main.go and wire into Manager.Options.Metrics.BindAddress</name>
  <files>cmd/manager/main.go</files>

  <read_first>
    - cmd/manager/main.go (full file — 291 lines; particularly the flag declaration block at lines 86-95, the flag.StringVar/BoolVar/IntVar definitions at lines 97-105, and the Manager construction at lines 131-138 where Metrics.BindAddress is currently hardcoded to `":8080"`)
    - charts/tide/values.yaml lines 17-24 (chart `manager.args` — confirms `--metrics-bind-address=:8443` is the chart-side contract; this binary must match the default)
    - .planning/phases/02.2-.../02.2-03-VERIFICATION.md §"Section 5 — Gate Decision" and §"Root-Cause Summary" Option A (lines 264-282) — the directed approach for this fix
    - .planning/phases/02.2-.../02.2-03-VERIFICATION.md §"Closing Prose" (line 360-370) — context for why the chart bug surfaced only after Plan 02.2-03's PVC fix
  </read_first>

  <action>
    Edit `cmd/manager/main.go` to add a `--metrics-bind-address` string flag and wire it into the controller-runtime Manager's `Metrics.BindAddress` option. The new flag's default value MUST be `:8443` to match the chart's existing `--metrics-bind-address=:8443` arg at `charts/tide/values.yaml:20` (T-02.2-08 mitigation — no production behavior change).

    **Edit A — variable declaration:** In the flag variable declarations block at lines 86-95, add a new line for the metrics address variable. Place it among the existing string vars, immediately after `var watchNamespace string` (line 89) and before the `// Phase 2 flags (Plan 12 wiring).` comment. Suggested name: `metricsAddr` (mirrors kubebuilder convention; short, clear).

    Add at line 90 (after `var watchNamespace string`):
    ```
        var metricsAddr string
    ```

    **Edit B — flag.StringVar definition:** In the flag definitions block at lines 97-105, add a new `flag.StringVar` line for the new variable. Place it adjacent to the other top-level flag definitions, BEFORE the `// Phase 2 flags — bound from Helm values.yaml via the controller Deployment args.` comment at line 100 (so it groups with the kubebuilder-standard flags `--config`, `--leader-elect`, `--watch-namespace`).

    Add immediately after the existing `flag.StringVar(&watchNamespace, ...)` line (line 99):
    ```
        flag.StringVar(&metricsAddr, "metrics-bind-address", ":8443", "Bind address for the metrics endpoint (controllerManager.manager.args in values.yaml)")
    ```

    Default value MUST be `":8443"` (matching `charts/tide/values.yaml:20`). The help-text suffix `(controllerManager.manager.args in values.yaml)` mirrors the help-text suffix on the other chart-bound flags (lines 101-105).

    **Edit C — Manager.Options.Metrics.BindAddress wire-up:** In the `ctrl.NewManager(...)` call at lines 131-138, replace the hardcoded `":8080"` in the `Metrics: metricsserver.Options{BindAddress: ":8080"},` line (line 136) with `metricsAddr`.

    Replace line 136:
    ```go
            Metrics:                metricsserver.Options{BindAddress: ":8080"},
    ```
    With:
    ```go
            Metrics:                metricsserver.Options{BindAddress: metricsAddr},
    ```

    (Preserve the trailing comma. Preserve the surrounding `LeaderElection`, `LeaderElectionID`, `HealthProbeBindAddress`, `WebhookServer` lines verbatim — those are intentionally untouched and are tracked by T-02.2-10 acceptance.)

    **Constraints:**
    - **DO NOT touch** `charts/tide/values.yaml` — the chart contract is unchanged; the binary catches up to it.
    - **DO NOT define** `--leader-elect` (already defined at line 98 — verify in your read pass), `--webhook-cert-path` (likely the next blocker — out of scope per user's tactical choice), `--health-probe-bind-address` (kubebuilder-standard but NOT in chart args — out of scope), or any other flag not explicitly mentioned in `charts/tide/values.yaml:18-24`.
    - **DO NOT remove or modify** any existing flag definition. The diff should be additive: +1 variable declaration, +1 flag.StringVar line, +1 modified literal (`":8080"` → `metricsAddr`).
    - **DO NOT add a `--metrics-bind-address` flag definition with default other than `:8443`** — that would change effective production behavior since the chart's arg currently passes `:8443` and existing deployments expect the manager to bind there.
    - **DO NOT add additional metrics-related infrastructure** (e.g., authn middleware, secure serving) — out of scope; this is a minimum-touch flag-definition fix.
    - **DO NOT introduce a separate `metricsserver` config struct** — preserve the existing single-line struct literal pattern.

    Commit with message:
    ```
    fix(02.2-04): define --metrics-bind-address flag in cmd/manager — close 02.2-03 BLOCKED gate (chart arg has no matching flag definition)
    ```

    (Per user emphasis on commit message quality. The body should reference 02.2-03-VERIFICATION.md §"Root-Cause Summary" Option A and note tactical scope.)
  </action>

  <verify>
    <automated><![CDATA[
      # Flag definition added
      [ "$(grep -c 'flag.StringVar.*"metrics-bind-address"' cmd/manager/main.go)" -eq 1 ] && \
      # Default value is :8443 (matches charts/tide/values.yaml:20)
      [ "$(grep -c '":8443"' cmd/manager/main.go)" -ge 1 ] && \
      # Variable declaration present
      [ "$(grep -c 'var metricsAddr string' cmd/manager/main.go)" -eq 1 ] && \
      # Manager wire-up: BindAddress reads from metricsAddr (NOT hardcoded)
      [ "$(grep -c 'BindAddress:.*metricsAddr' cmd/manager/main.go)" -eq 1 ] && \
      # Prior hardcode removed
      [ "$(grep -c 'BindAddress:.*":8080"' cmd/manager/main.go)" -eq 0 ] && \
      # Other flag definitions preserved (regression guard)
      [ "$(grep -c 'flag.StringVar.*"config"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.BoolVar.*"leader-elect"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"watch-namespace"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"subagent-image"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"credproxy-image"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.StringVar.*"default-file-touch-mode"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.IntVar.*"rate-limit-default-rpm"' cmd/manager/main.go)" -eq 1 ] && \
      [ "$(grep -c 'flag.IntVar.*"rate-limit-default-burst"' cmd/manager/main.go)" -eq 1 ] && \
      # Build passes
      go build ./cmd/manager/... && \
      # Binary accepts the flag without "flag provided but not defined" stderr (behavior gate)
      # Use --help to enumerate flags; check that --metrics-bind-address appears in the usage output.
      ./bin/manager --help 2>&1 | grep -q 'metrics-bind-address' && \
      # Out-of-scope flags are NOT defined by this plan (T-02.2-10 acceptance — tactical scope honored)
      # --leader-elect IS defined (pre-existing at line 98); --webhook-cert-path and --health-probe-bind-address are NOT.
      [ "$(grep -c 'flag.StringVar.*"webhook-cert-path"' cmd/manager/main.go)" -eq 0 ] && \
      [ "$(grep -c 'flag.StringVar.*"health-probe-bind-address"' cmd/manager/main.go)" -eq 0 ] && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Flag defined:** `grep -c 'flag.StringVar.*"metrics-bind-address"' cmd/manager/main.go` returns exactly 1
    - **Default value matches chart:** `grep -c '":8443"' cmd/manager/main.go` returns ≥ 1 (the new flag's default literal)
    - **Variable declared:** `grep -c 'var metricsAddr string' cmd/manager/main.go` returns exactly 1
    - **Manager wire-up:** `grep -c 'BindAddress:.*metricsAddr' cmd/manager/main.go` returns exactly 1 (the Manager reads from the new variable)
    - **Prior hardcode removed:** `grep -c 'BindAddress:.*":8080"' cmd/manager/main.go` returns 0 (the `:8080` literal in the Metrics struct is replaced — protects against accidentally adding the new flag without rewiring)
    - **All other flag definitions preserved (regression guard):** the 8 pre-existing flag definitions (`config`, `leader-elect`, `watch-namespace`, `subagent-image`, `credproxy-image`, `default-file-touch-mode`, `rate-limit-default-rpm`, `rate-limit-default-burst`) each match `grep -c` = 1
    - **Build succeeds:** `go build ./cmd/manager/...` exits 0
    - **Binary surfaces the flag:** `./bin/manager --help 2>&1 | grep -q 'metrics-bind-address'` matches (the flag is registered in the binary's flag set)
    - **Tactical scope honored (T-02.2-10):** `grep -c 'flag.StringVar.*"webhook-cert-path"' cmd/manager/main.go` returns 0 AND `grep -c 'flag.StringVar.*"health-probe-bind-address"' cmd/manager/main.go` returns 0 (these out-of-scope flags are NOT defined by this plan)
  </acceptance_criteria>

  <done>
    `cmd/manager/main.go` has the `--metrics-bind-address` flag defined with default `:8443` and wired into the controller-runtime Manager's `Metrics.BindAddress` option. The prior `:8080` hardcode is replaced. All other flag definitions are preserved verbatim. `go build ./cmd/manager/...` exits 0 and `./bin/manager --help` lists the new flag. Out-of-scope flags (`--webhook-cert-path`, `--health-probe-bind-address`) are NOT defined per tactical scope. Committed with the `fix(02.2-04): define --metrics-bind-address flag in cmd/manager — close 02.2-03 BLOCKED gate (chart arg has no matching flag definition)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-04-VERIFICATION.md (clean run + rerun + gate decision)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-03-VERIFICATION.md (full file — 369 lines; the structural template this artifact mirrors verbatim; frontmatter shape, Section 1 / Section 2 / Section 3 / Section 4 / Section 5 / Section 6 layout, per-spec outcome tables, root-cause analysis prose, threat-model-mitigations-applied list, "What This Artifact Did Not Touch" closing section)
    - .planning/phases/02.2-.../02.2-01-VERIFICATION.md (the earlier verification — for the spec list and reproducer shape, particularly the bash blocks at Sections 1.1 and 2.1)
    - cmd/manager/main.go (post-Task-1 state — verify the flag definition is committed before this task runs)
    - charts/tide/values.yaml lines 17-24 (chart's `manager.args` — the contract this plan's fix closes ONE flag from; the others remain to surface as next failures)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-03-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md` using the same structural template.

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit:
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-04-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state for the artifact:**
    ```bash
    # Post-clean diagnostic snapshot — needed for both APPROVED and BLOCKED gate analysis
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-04-clean-pods.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-04-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --tail=20 2>&1 | tee /tmp/02.2-04-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-04-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-04-clean-pvc-phase.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block from the captured log — should show 7 Pass / 0 Skip / 0 Fail if both prior plan fixes + this plan's binary fix worked together; otherwise capture the actual outcome). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-03-VERIFICATION.md` §"Section 1" Per-Spec Outcome Table.

    **Critically: capture the manager logs.** The clean-run's `tide-controller-manager` Pod state is the load-bearing evidence:
    - **APPROVED case:** Pod transitions to `Ready 1/1`; manager logs show "starting manager"; NO `flag provided but not defined: -metrics-bind-address` line
    - **BLOCKED case:** Pod stuck in CrashLoopBackOff; manager logs show `flag provided but not defined: -<next-undefined-flag>` (most likely `--webhook-cert-path` per the chart's args at values.yaml line 24, since `--leader-elect` is already defined at cmd/manager/main.go:98 and `--config` / `--watch-namespace` are already defined)

    **Sub-step B — Idempotent Re-run:**
    ```bash
    # Pre-state: tide-test cluster + cert-manager + tide release REVISION 1 persisted from clean run.
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-04-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    **After rerun:** capture same diagnostic snapshot (`/tmp/02.2-04-rerun-*.txt`) for the rerun.

    Capture into the artifact: exit code, wall time, per-spec outcomes. Verify the helm install on rerun succeeded due to `--replace` (Plan 02.2-01's fix; not re-verified here, but the rerun must complete without "cannot re-use a name" error). Verify the cluster was NOT deleted by AfterSuite (T-02.2-02 mitigation from Plan 02.2-01; re-verified at runtime).

    **Sub-step C — Author the artifact:**
    Write `02.2-04-VERIFICATION.md` mirroring `02.2-03-VERIFICATION.md`'s structural template. Required frontmatter:
    ```yaml
    ---
    phase: 02.2
    plan: 04
    task: 3
    artifact: verification
    captured_on: <YYYY-MM-DD>
    captured_by_host: <host>
    captured_by_branch: <git branch / worktree name>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary>
    ---
    ```

    Required sections (mirror 02.2-03-VERIFICATION.md headers verbatim):
    - **Section 1 — Clean Start Run** (reproducer + per-spec outcome table + wall-time + exit + BeforeSuite detail with manager logs as the load-bearing evidence)
    - **Section 2 — Idempotent Re-run** (reproducer + per-spec outcome table + wall-time + exit + rerun-specific evidence)
    - **Section 3 — Before/After Comparison** (table comparing Plan 02.2-01, 02.2-03, and 02.2-04 outcomes; particularly: did the metrics flag close the BLOCKED gate, and what's the NEW failure mode if BLOCKED again)
    - **Section 4 — Wall-time Budget Check** (same table shape as 02.2-03-VERIFICATION.md §4)
    - **Section 5 — Gate Decision** (the APPROVED-requires-ALL table + Required Next Step + Root-Cause Summary if BLOCKED + Fix landscape if BLOCKED)
    - **Section 6 — Threat-Model Mitigations Applied** (T-02.2-08 / T-02.2-09 / T-02.2-10 each with evidence + carry-forward T-02.2-01 through T-02.2-07)
    - **"What This Artifact Did Not Touch"** closing section
    - **"Closing Prose"** with one-paragraph summary

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/` (Task 1's source edit is already committed before this task runs). Task 3 edits Plan 02.2-02 (a planning artifact, not source) — and only fires if gate_decision: APPROVED. Document this explicitly in the artifact's "What This Artifact Did Not Touch" closing section.

    **If gate_decision = BLOCKED (most likely per the verification's Root-Cause analysis):** Write a Root-Cause Summary section with numbered analysis points (mirror `02.2-03-VERIFICATION.md` §"Root-Cause Summary"). Surface a fix landscape:
    - **Option A** (one-more-tactical-iteration): define ONLY the next undefined flag (likely `--webhook-cert-path`); same Plan-02.2-05 shape; predict the next-next failure.
    - **Option B** (comprehensive audit): audit ALL `charts/tide/values.yaml:18-24` entries against `cmd/manager/main.go` and define every missing flag in one plan (closes the cascade).
    - **Option C** (chart side): trim non-essential args from `charts/tide/values.yaml` if the missing flags are not needed (e.g., kubebuilder-standard `--metrics-bind-address` was added by helmify even though TIDE's main.go didn't need it; same may be true of others).
    Task 3 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated on `02.2-03-VERIFICATION.md` (the prior wave's wiring).

    **If gate_decision = APPROVED:** Skip the Root-Cause Summary section. Note in the closing prose that Plan 02.2-02 (closeout) is unblocked once Task 3 wires its checkpoint to read this artifact.

    Commit with one of:
    ```
    docs(02.2-04): runtime verification — APPROVED gate (manager-args flag mismatch resolved; 7/7 Layer B specs PASS clean + rerun)
    ```
    OR
    ```
    docs(02.2-04): runtime verification — BLOCKED gate (manager-args flag mismatch partially resolved; next undefined flag <name> surfaces as new blocker)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists and has the required frontmatter shape
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md ] && \
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 04' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^task: 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^artifact: verification' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -eq 1 ] && \
      # Gate decision is one of APPROVED or BLOCKED
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md && \
      # Required structural sections exist
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 4' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md && \
      grep -E '^rerun_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md && \
      # Threat-model mitigations applied section exists
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-08/09/10 all referenced
      [ "$(grep -c 'T-02.2-08' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-09' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-10' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      # Manager log evidence captured (the load-bearing diagnostic for APPROVED vs BLOCKED)
      [ "$(grep -c 'metrics-bind-address\|flag provided but not defined\|starting manager' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md)" -ge 1 ] && \
      # No production source modified by this task (Task 1's source edit is already committed before this task)
      # The only NEW file should be 02.2-04-VERIFICATION.md (and possibly /tmp scratch files, which are not tracked).
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-04-VERIFICATION.md' | grep -v '^$' && echo "FAIL: non-artifact files modified beyond Task 1's pre-committed edit" && exit 1 || \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-04-VERIFICATION.md ]` returns true
    - **Frontmatter has required fields:** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 04' <artifact>` returns 1 AND `grep -c '^task: 3' <artifact>` returns 1 AND `grep -c '^artifact: verification' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' <artifact>` matches
    - **All six structural sections present:** each `grep -c '^## Section [1-6]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all three new threats referenced:** `grep -c 'Threat-Model Mitigations Applied' <artifact>` ≥ 1 AND `grep -c 'T-02.2-08' <artifact>` ≥ 1 AND `grep -c 'T-02.2-09' <artifact>` ≥ 1 AND `grep -c 'T-02.2-10' <artifact>` ≥ 1
    - **Manager-log evidence captured (load-bearing for gate decision):** the artifact includes a verbatim excerpt of `kubectl logs deploy/tide-controller-manager` from the clean run — either the "starting manager" success message (APPROVED case) or the `flag provided but not defined: -<next-flag>` failure message (BLOCKED case)
    - **Behavior gate (APPROVED case):** if `gate_decision: APPROVED`, then ALL of: clean run captured `EXIT=0` AND 7 Layer B specs marked PASS in the per-spec table; rerun captured `EXIT=0` AND 7 Layer B specs marked PASS. AND `kubectl logs deploy/tide-controller-manager` shows "starting manager" (or analogous controller-runtime startup log). AND no `flag provided but not defined: -metrics-bind-address` line appears in the captured manager logs.
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, a `Root-Cause Summary` section MUST exist with numbered analysis points mirroring `02.2-03-VERIFICATION.md` §"Root-Cause Summary" — particularly: which next-undefined-flag surfaced, why this was predictable from `charts/tide/values.yaml:18-24` audit, fix-landscape options for the next iteration.
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows only the new `02.2-04-VERIFICATION.md` artifact (Task 1's `cmd/manager/main.go` edit must be committed BEFORE this task runs; Task 3 has not fired yet)
  </acceptance_criteria>

  <done>
    `02.2-04-VERIFICATION.md` exists at the correct path with the structural template mirroring `02.2-03-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, and `gate_decision: APPROVED` or `gate_decision: BLOCKED`. Sections 1-6 captured with per-spec outcome tables, manager-log evidence (load-bearing for APPROVED vs BLOCKED), before/after comparison vs `02.2-03-VERIFICATION.md`, and threat-model mitigations (T-02.2-08/09/10 + carry-forward T-02.2-01 through T-02.2-07). If APPROVED, Task 3 fires to wire Plan 02.2-02's checkpoint; if BLOCKED, Task 3 does NOT fire and the failure routes to a planner re-evaluation per the wave-failure-semantics contract. Committed with a `docs(02.2-04): runtime verification — APPROVED gate ...` or `docs(02.2-04): runtime verification — BLOCKED gate (root cause: <one-line>)` message that explains the outcome.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-04-VERIFICATION.md + bump depends_on (gated on Task 2 APPROVED gate)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file — 382 lines; frontmatter at lines 1-40 — particularly line 6 `depends_on: ["02.2-01"]`, lines 31-39 `key_links` block, lines 22-26 `must_haves.truths` block; `<context>` block at lines 63-68; Task 1 body at lines 72-136 — `<read_first>` at lines 77-79, `<what-built>` at lines 81-83, `<how-to-verify>` at lines 85-101, `<verify>` automated block at lines 117-123, `<acceptance_criteria>` at lines 125-129, `<done>` at lines 131-135 — all of which currently reference `02.2-01-VERIFICATION.md`)
    - .planning/phases/02.2-.../02.2-04-VERIFICATION.md (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the precondition for this Task 3 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - .planning/phases/02.2-.../02.2-03-PLAN.md Task 4 body (lines 497-540) — the structural model for this task's edits; mirror its surgical-edit pattern exactly
  </read_first>

  <action>
    **Gate precondition:** Task 3 fires ONLY IF Task 2's `02.2-04-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED`, halt Task 3 — Plan 02.2-02 stays unmodified (it will continue to reference `02.2-01-VERIFICATION.md` because Plan 02.2-03 Task 4 also did NOT fire); the BLOCKED failure routes to a planner re-evaluation.

    Verify the precondition by running:
    ```bash
    grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md
    ```
    If this does NOT match, halt Task 3 and surface the BLOCKED gate. Exit with a clear message: "Task 3 halted: 02.2-04-VERIFICATION.md gate_decision is not APPROVED — routing back to planner for next iteration."

    If APPROVED, make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-04-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering.

    **NOTE:** Plan 02.2-03 Task 4 was intended to do the same surgical edit (`02.2-01-VERIFICATION.md` → `02.2-03-VERIFICATION.md`) but never fired because its Task 3 gate was BLOCKED. As a consequence, `02.2-02-PLAN.md` currently still references `02.2-01-VERIFICATION.md` at all its read paths. This task's edits land the original Plan 02.2-03 Task 4 intent but redirect to `02.2-04-VERIFICATION.md` instead.

    **Edit A — frontmatter `depends_on` (line 6 of 02.2-02-PLAN.md):** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-04"]`. This is a single one-line change. The new dependency captures the gate-artifact dependency directly (Plan 02.2-04 produces the gate; Plan 02.2-02 reads it). The transitive dependency on Plans 02.2-01 and 02.2-03 (which Plan 02.2-04 depends on) is implicit in execute-phase's wave-ordering logic.

    **Edit B — frontmatter `must_haves.truths` block (lines 22-26 of 02.2-02-PLAN.md):** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-04-VERIFICATION.md`. Specifically the bullet currently reads "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → change to "Closeout fires ONLY when 02.2-04-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit C — frontmatter `key_links` block (lines 31-39 of 02.2-02-PLAN.md):** Update the entry that references `02.2-01-VERIFICATION.md` to reference `02.2-04-VERIFICATION.md`. Concretely: change the `from:` line's path from `02.2-01-VERIFICATION.md` to `02.2-04-VERIFICATION.md` and keep the rest of the entry unchanged.

    **Edit D — `<context>` block (line 66 of 02.2-02-PLAN.md):** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-04-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>` (lines 77-79 of 02.2-02-PLAN.md):** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-04-VERIFICATION.md (the gate artifact produced by Plan 04 Task 2 — Phase 02.2-04 closed the BLOCKED runtime gate from 02.2-03-VERIFICATION.md by defining the --metrics-bind-address flag in cmd/manager/main.go)`. Update the analog Phase 02.1 reference if needed.

    **Edit F — Task 1 `<what-built>` (lines 81-83 of 02.2-02-PLAN.md):** Update the prose to reference Plan 02.2-04's deliverables (manager flag definition + runtime re-verification) in addition to Plans 02.2-01 and 02.2-03's deliverables. Specifically append: "Plan 02.2-04 then defined the `--metrics-bind-address` flag in `cmd/manager/main.go` so the manager binary accepts the chart's `manager.args` block, unblocking the Pod's Ready state and letting helm install --wait complete."

    **Edit G — Task 1 `<how-to-verify>` (lines 85-101 of 02.2-02-PLAN.md):** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-04-VERIFICATION.md`. There are multiple occurrences in the numbered steps; update each.

    **Edit H — Task 1 `<verify>` automated block (lines 117-123 of 02.2-02-PLAN.md):** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-04-VERIFICATION.md`. Two grep paths need bumping.

    **Edit I — Task 1 `<acceptance_criteria>` (lines 125-129 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-04-VERIFICATION.md`.

    **Edit J — Task 1 `<done>` (lines 131-135 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-04-VERIFICATION.md` if any are present.

    **Edit K — Task 2 `<action>` Edit B body (around line 174 of 02.2-02-PLAN.md):** The "Plans: 2/2 plans executed" line and the plan list (currently `02.2-01-PLAN.md` + `02.2-02-PLAN.md`) need updating to reflect the actual plan set (now 4 plans: 01 + 02 + 03 + 04). Update to "Plans: 4/4 plans executed" and list all four plans with `[x]` checkboxes (assumes APPROVED gate means all four plans land green).

    **Edit L — Task 3 STATE.md edit body (around line 254 of 02.2-02-PLAN.md):** The frontmatter `progress.completed_plans` increment math currently says "this phase's 2 plans" — update to "this phase's 4 plans (Plan 01 + Plan 02 + Plan 03 + Plan 04)". Update the Performance Metrics table append to include all four plans' rows.

    **Edit M — Task 3 STATE.md Decisions block edits (around line 274 of 02.2-02-PLAN.md):** Append a bullet for the Plan 02.2-04 decision:
    ```
    - [Phase 02.2]: tactical iteration over comprehensive audit (Plan 02.2-04): user explicitly chose to fix --metrics-bind-address ONLY and re-verify, NOT to audit charts/tide/values.yaml:18-24 against cmd/manager/main.go in one pass. Acknowledged risk: --webhook-cert-path (and any other chart arg without matching flag definition) remain as next-likely blockers. The tactical-iteration cost (N debug plans, one per missing flag) is accepted as the price of evidence-driven incremental progress over speculative audit-everything-at-once.
    ```

    **Constraints:**
    - Do NOT edit Plans 02.2-01 or 02.2-03 or 02.2-04 (this plan) — only `02.2-02-PLAN.md` is touched.
    - Do NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `Makefile`, `.github/`. This task is documentation-only.
    - Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — those are owned by Plan 02.2-02's tasks and by the orchestrator.
    - The diff against `02.2-02-PLAN.md` must be minimal — only the references to `02.2-01-VERIFICATION.md` are changed to `02.2-04-VERIFICATION.md`, plus the plan-list/decisions edits in Edits K, L, M. No new tasks added, no existing tasks removed, no structural reorganization.

    Commit with message:
    ```
    docs(02.2-04): wire Plan 02.2-02 checkpoint to 02.2-04-VERIFICATION.md (depends_on bump 02.2-01 → 02.2-04)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Gate precondition: 02.2-04-VERIFICATION.md gate_decision is APPROVED (else this task should have been halted)
      grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-VERIFICATION.md && \
      # Edit A: depends_on bumped
      grep -E 'depends_on: \[.{0,5}02.2-04.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Old depends_on removed (must not contain 02.2-01 or 02.2-03 as the active depends_on value)
      [ "$(grep -cE '^depends_on: \[.{0,5}02.2-0[13].{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-04-VERIFICATION.md is now the gate reference (multiple paths updated)
      [ "$(grep -c '02.2-04-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 5 ] && \
      # 02.2-01-VERIFICATION.md references purged from 02.2-02-PLAN.md
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-03-VERIFICATION.md references purged from 02.2-02-PLAN.md (Plan 02.2-03 Task 4 didn't fire, so they were never there; assert anyway)
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Task 1 checkpoint grep gate uses the new path
      grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-04-VERIFICATION.md" .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Plan list updated to reflect 4 plans (Edit K)
      [ "$(grep -c '02.2-01-PLAN.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      [ "$(grep -c '02.2-02-PLAN.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      [ "$(grep -c '02.2-03-PLAN.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      [ "$(grep -c '02.2-04-PLAN.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      # Tactical-iteration decision recorded in Edit M's appended bullet
      [ "$(grep -c 'tactical iteration over comprehensive audit' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      # Confirm no other plans were modified by this task (T-02.2-08 regression guard)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-02-PLAN.md' | grep -v '^$' && echo "FAIL: non-target plan files modified" && exit 1 || \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Gate precondition met:** `grep -E '^gate_decision: APPROVED$' 02.2-04-VERIFICATION.md` matches (Task 3 ran ONLY because Task 2's gate was APPROVED; otherwise Task 3 was halted before edits landed)
    - **depends_on bumped (Edit A):** `grep -E 'depends_on: \[.{0,5}02.2-04.{0,5}\]' 02.2-02-PLAN.md` matches AND `grep -cE '^depends_on: \[.{0,5}02.2-0[13].{0,5}\]' 02.2-02-PLAN.md` returns 0 (old values purged)
    - **New gate-artifact reference added (Edits B-J):** `grep -c '02.2-04-VERIFICATION.md' 02.2-02-PLAN.md` returns ≥ 5 (multiple paths updated: must_haves.truths, key_links, context, Task 1 read_first, how-to-verify, verify automated block, acceptance_criteria, done)
    - **Old gate-artifact references purged:** `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-03-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 (no stale references to prior gate artifacts)
    - **Task 1 `<verify>` automated grep targets the new path:** the regex `grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-04-VERIFICATION.md"` matches inside `02.2-02-PLAN.md`
    - **Plan list updated (Edit K):** all four plan filenames (`02.2-01-PLAN.md`, `02.2-02-PLAN.md`, `02.2-03-PLAN.md`, `02.2-04-PLAN.md`) each appear ≥ 1 time in the document
    - **Tactical-iteration decision recorded (Edit M):** `grep -c 'tactical iteration over comprehensive audit' 02.2-02-PLAN.md` returns ≥ 1
    - **No other plans modified (T-02.2-08 regression guard):** `git status --porcelain` shows ONLY `02.2-02-PLAN.md` modified in this commit; no edits to `02.2-01-PLAN.md`, `02.2-03-PLAN.md`, `02.2-04-PLAN.md`, or any production source
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has been surgically edited to redirect its checkpoint from `02.2-01-VERIFICATION.md` to `02.2-04-VERIFICATION.md`, with `depends_on` bumped from `["02.2-01"]` to `["02.2-04"]`. All stale `02.2-01-VERIFICATION.md` and `02.2-03-VERIFICATION.md` references are purged. The plan list reflects the actual 4-plan structure of Phase 02.2. The tactical-iteration decision is recorded in the Decisions-block edit (Edit M). No other plans or production source are modified. Committed with the `docs(02.2-04): wire Plan 02.2-02 checkpoint to 02.2-04-VERIFICATION.md (depends_on bump 02.2-01 → 02.2-04)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `charts/tide/values.yaml:20` (chart contract) → `cmd/manager/main.go` flag set (binary contract) | The chart passes `--metrics-bind-address=:8443` as an arg to the manager binary; this plan adds the matching flag definition so the contract is satisfied at runtime. |
| Task 1 (source edit) → Task 2 (runtime verification) | Task 2 is run after Task 1's source edit is committed; the runtime reproducer exercises the new flag definition end-to-end. |
| Task 2 (gate artifact) → Task 3 (Plan 02.2-02 wiring) | Task 3 reads Task 2's `gate_decision:` frontmatter. APPROVED → Task 3 fires; BLOCKED → Task 3 halts and the BLOCKED failure routes to a planner re-evaluation. |
| Plan 02.2-04 Task 3 edits → Plan 02.2-02 checkpoint target | Surgical edit only — depends_on and verification-artifact references in 02.2-02-PLAN.md are bumped; no other planning artifacts touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-08 | Tampering | Adding `--metrics-bind-address` flag changes manager startup behavior in production deployments | mitigate | Keep default `:8443` identical to the chart's existing arg value at `charts/tide/values.yaml:20`. Production deployments rendering the chart without `--set` overrides continue to pass the same `--metrics-bind-address=:8443` arg to the binary, which now matches a flag definition with the same default. **Acceptance gate:** `grep -c 'flag.StringVar.*"metrics-bind-address"' cmd/manager/main.go` returns 1 AND `grep -c '":8443"' cmd/manager/main.go` returns ≥ 1 AND `grep -c 'BindAddress:.*metricsAddr' cmd/manager/main.go` returns 1. The prior hardcode (`":8080"`) is removed — that's intentional, since the chart was passing `:8443` and the binary was binding `:8080`, so the chart's contract was effectively a no-op for the metrics endpoint. Aligning them is a behavior CORRECTION, not a regression. |
| T-02.2-09 | Tampering | `Manager.Options.Metrics.BindAddress` wire-up could collide with an existing metrics endpoint registration | mitigate | Verified absent at plan-time: `grep -rnE 'metrics-bind-address\|MetricsBindAddress' internal/ pkg/ cmd/` returns zero matches outside the file this plan edits. The existing wire-up at `cmd/manager/main.go:136` (`Metrics: metricsserver.Options{BindAddress: ":8080"}`) hardcodes `:8080`; changing it to `metricsAddr` (default `:8443`) is a single-line surgical replacement with no other metric-related code path affected. **Acceptance gate:** the verify block confirms `BindAddress:.*metricsAddr` matches exactly once. |
| T-02.2-10 | Information Disclosure / Repudiation | Tactical scope means `--webhook-cert-path` (chart's values.yaml:24) remains undefined and WILL likely re-surface as the next CrashLoopBackOff cause | accept | Accepted per user choice for tactical iteration. The acceptance is explicit (documented in objective, must_haves, action sections of all three tasks). Plan 02.2-04 Task 2 (runtime verification) MUST document the next surfacing failure mode in 02.2-04-VERIFICATION.md §"Section 5 Gate Decision" Root-Cause Summary so the orchestrator can route to Plan 02.2-05 (or equivalent) for the next iteration. **Acceptance gate:** if Task 2 records `gate_decision: BLOCKED`, the `Root-Cause Summary` section MUST name which next-undefined-flag surfaced and predict whether further tactical iterations or a comprehensive audit (Option B from 02.2-03-VERIFICATION.md §"Fix landscape") is the appropriate path forward. |

</threat_model>

<verification>
After all three tasks complete (or after Task 2 if its gate is BLOCKED):

**Source gates (Task 1):**
- `cmd/manager/main.go` defines `--metrics-bind-address` with default `:8443` (matches `charts/tide/values.yaml:20`)
- The Manager's `Metrics.BindAddress` reads from the new `metricsAddr` variable (prior `:8080` hardcode removed)
- `go build ./cmd/manager/...` exits 0
- `./bin/manager --help` lists the new flag

**Runtime gates (Task 2):**
- `02.2-04-VERIFICATION.md` exists with all required frontmatter fields populated
- `gate_decision` is one of APPROVED or BLOCKED
- The artifact's Sections 1-6 mirror `02.2-03-VERIFICATION.md` structural template
- Manager-log evidence (load-bearing for gate decision) is captured verbatim in the artifact
- All three new threats (T-02.2-08/09/10) have evidence in §6

**Wiring gates (Task 3 — gated on Task 2 APPROVED):**
- `02.2-02-PLAN.md` has `depends_on: ["02.2-04"]` and all references updated from `02.2-01-VERIFICATION.md` to `02.2-04-VERIFICATION.md`
- Stale `02.2-01-VERIFICATION.md` and `02.2-03-VERIFICATION.md` references purged from `02.2-02-PLAN.md`
- No other plans modified

**Structural gates (T-02.2-08/09/10 mitigation):**
- Out-of-scope flags (`--webhook-cert-path`, `--health-probe-bind-address`) NOT defined in `cmd/manager/main.go`
- Chart's `charts/tide/values.yaml:20` line NOT touched
- Plans 02.2-01, 02.2-03, 02.2-04 NOT modified
- `.planning/STATE.md` and `.planning/ROADMAP.md` NOT touched (orchestrator owns)
</verification>

<success_criteria>
- Task 1 source edit landed: `cmd/manager/main.go` defines `--metrics-bind-address` flag with default `:8443` wired into Manager.Options.Metrics.BindAddress; `go build` exits 0
- Task 2 runtime verification artifact authored at `.planning/phases/02.2-.../02.2-04-VERIFICATION.md` with `gate_decision: APPROVED` or `gate_decision: BLOCKED` and all required structural sections
- Task 3 (gated on Task 2 APPROVED) wired Plan 02.2-02's checkpoint to `02.2-04-VERIFICATION.md` with `depends_on: ["02.2-04"]` and all stale references purged
- If Task 2 returns BLOCKED: Task 3 does NOT fire; Plan 02.2-02 stays gated on `02.2-03-VERIFICATION.md` (prior wiring); the orchestrator routes to a planner re-evaluation for Plan 02.2-05 (or equivalent) per the wave-failure-semantics contract and the tactical-iteration acceptance recorded in T-02.2-10
- T-02.2-08/09/10 each have concrete evidence in the verification artifact and the source-edit acceptance gates
- Tactical scope honored: ONLY `--metrics-bind-address` defined; `--webhook-cert-path` and `--health-probe-bind-address` NOT defined; `charts/tide/values.yaml` NOT touched; Plans 02.2-01, 02.2-03, 02.2-04 NOT modified
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-04-SUMMARY.md` documenting:
- The source edit applied (Task 1): the `--metrics-bind-address` flag definition + Manager wire-up + the chart-matching default value rationale
- The runtime verification outcome (Task 2): gate decision (APPROVED or BLOCKED), per-spec outcomes, wall times, exit codes, manager-log evidence
- The Plan 02.2-02 wiring update (Task 3, if APPROVED): which references were updated, depends_on bump, plan-list and decisions edits
- Any inconsistencies surfaced (e.g., expected next failure mode if BLOCKED; the predicted `--webhook-cert-path` surfacing per T-02.2-10)
- The Phase 02.2 close-out chain progression: 4 source fixes + 1 chart fix + 1 binary flag fix landed → runtime verified once (APPROVED) or routed back (BLOCKED) for next iteration
- Threat-model mitigations applied (T-02.2-08 / T-02.2-09 / T-02.2-10) with concrete evidence
</output>
</content>
