---
phase: 02.2
plan: 02
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: []
tags: [closeout, roadmap, state, gated, verification-gate, phase-02.2]

threat_model_summary:
  - "T-02.2-04 (mitigate): closeout fires on a BLOCKED verification artifact, falsely marking the phase done — gated by checkpoint:human-verify that reads 02.2-01-VERIFICATION.md frontmatter and confirms gate_decision: APPROVED before any roadmap/state edits"
  - "T-02.2-05 (mitigate): ROADMAP/STATE edits silently regress a different in-flight phase's checked state — acceptance criteria target only Phase 02.2 entries; other phase entries verified unchanged via diff inspection"

must_haves:
  truths:
    - "ROADMAP.md Phase 02.2 entry reflects the completed status with the actual plan list (02.2-01-PLAN.md replacing the TBD placeholder) and an outcome-shaped Goal line"
    - "STATE.md updated to record Phase 02.2 completion, the verification gate APPROVED decision, last_activity timestamp, and accumulated context (the 4 + 1 fixes landed, the AfterSuite cleanup pattern, the CI YAML measurement-scope correction)"
    - "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED — BLOCKED short-circuits the plan and routes back to planner re-evaluation"
    - "No production source modified by this plan (it's documentation-only closeout)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 02.2 entry marked checked with finalized Goal + Plans block"
      contains: "02.2-01-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Phase 02.2 completion recorded with verification gate decision + accumulated context"
      contains: "Phase 02.2"
  key_links:
    - from: ".planning/phases/02.2-.../02.2-01-VERIFICATION.md (frontmatter gate_decision)"
      to: "this plan's checkpoint:human-verify task"
      via: "reading the frontmatter line"
      pattern: "^gate_decision: APPROVED$"
    - from: ".planning/ROADMAP.md (Phase 02.2 entry)"
      to: "Phase 02.2 final plan list"
      via: "Plans: section update"
      pattern: "02.2-01-PLAN.md"
---

<objective>
Close Phase 02.2 by updating `.planning/ROADMAP.md` and `.planning/STATE.md` to reflect the phase's completion, **only after** confirming Plan 01's verification artifact (`02.2-01-VERIFICATION.md`) records `gate_decision: APPROVED`.

This plan mirrors the structure of Phase 02.1 Plan 04 Task 3 (which was correctly BLOCKED in that prior phase). The verification gate is the structural contract:
- If `02.2-01-VERIFICATION.md` says `gate_decision: APPROVED` → ROADMAP/STATE edits fire and Phase 02.2 is marked done.
- If `02.2-01-VERIFICATION.md` says `gate_decision: BLOCKED` → this plan halts at the checkpoint and surfaces the failure for planner re-evaluation. No ROADMAP/STATE edits happen.

The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic here: a failed task in Wave N must not cause downstream tasks in Wave N+1 to fire. Plan 01 produces the verification gate as its final artifact; Plan 02 reads that artifact's `gate_decision` field and either proceeds (APPROVED) or halts (BLOCKED).

Purpose: Phase 02.2 was inserted into the ROADMAP via `mvp-phase` or analogous tooling to close Phase 02.1's BLOCKED runtime gate. With the four source-shape fixes + one micro-fix landed in Plan 01 and runtime-verified in `02.2-01-VERIFICATION.md`, the ROADMAP entry needs to flip from in-progress to checked, and STATE.md needs to record the milestone-level outcome so the next phase planner has accurate accumulated context.

Output:
- `.planning/ROADMAP.md` with Phase 02.2 entry updated (Goal finalized, Plans list updated with `02.2-01-PLAN.md`, checkbox marked)
- `.planning/STATE.md` with Phase 02.2 completion recorded (last_activity bumped, accumulated context Phase 02.2 lessons + patterns appended)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gate check — verify 02.2-01-VERIFICATION.md says gate_decision: APPROVED before any ROADMAP/STATE edits</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 — read the frontmatter `gate_decision:` field; if BLOCKED, also read the Root-Cause Summary section)
    - .planning/phases/02.1-debug-fix-the-layer-b-kind-integration-test-suite-so-make-te/02.1-04-VERIFICATION.md (the analog BLOCKED gate from Phase 02.1 — for reference on what a BLOCKED outcome looks like and what root-cause analysis to expect)
  </read_first>

  <what-built>
    Plan 01 landed four source-shape fixes to `test/integration/kind/suite_test.go` (kindTestTimeout 4m→7m, certManagerVersion v1.16.2→v1.20.2, cleanupKindCluster helper + AfterSuite rewire, helm --replace) plus a CI YAML edit dropping the redundant `DUR > 300` outer wall-time check. Plan 01 Task 3 ran the end-to-end runtime reproducer (clean `make test-int` + `KEEP_KIND_CLUSTER=true` rerun) and captured the outcome in `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md`.
  </what-built>

  <how-to-verify>
    1. Read the frontmatter of `02.2-01-VERIFICATION.md` and confirm the `gate_decision:` line.
    2. Run: `grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md`
    3. Inspect the per-spec outcome tables in Section 1 (Clean Start Run) and Section 2 (Idempotent Re-run). For an APPROVED gate, both must show 7/7 specs PASS.
    4. Check the wall-time budget rows in Section 4 — for APPROVED, both clean and rerun must show go test wall-time ≤ 300s (the inner Makefile `timeout 300s` did not fire).
    5. Skim Section 6 (Threat-Model Mitigations Applied) — for APPROVED, all four threats (T-02.2-01 through T-02.2-04) must have concrete evidence.

    **Pass criteria — `gate_decision: APPROVED`:**
    - Both runs exit 0
    - 7/7 specs PASS on clean run AND 7/7 PASS on rerun
    - `timeout 300s` did NOT fire on the go test step of either run
    - All four threat-model mitigations have positive evidence

    **Fail criteria — `gate_decision: BLOCKED`:**
    - Any of: spec failures, exit non-zero, `timeout 300s` exit 124, threat mitigation evidence missing
    - Plan 01 Task 3 will have written a Root-Cause Summary section
    - The orchestrator should route this failure to a planner re-evaluation — DO NOT make any ROADMAP/STATE edits
  </how-to-verify>

  <resume-signal>
    Respond with one of:
    - **`approved`** — `02.2-01-VERIFICATION.md` says `gate_decision: APPROVED`; proceed to Tasks 2 + 3 (ROADMAP + STATE edits).
    - **`blocked: <root-cause summary>`** — `02.2-01-VERIFICATION.md` says `gate_decision: BLOCKED`; halt this plan. The orchestrator should route to a planner re-evaluation. Surface the Root-Cause Summary from the verification artifact verbatim so the planner can build a follow-up phase if needed.

    Any other response halts the plan pending clarification.
  </resume-signal>

  <action>
    Halt automated execution and wait for human verification. Do not edit ROADMAP.md or STATE.md until the human verifier returns `approved`. The executor reads `02.2-01-VERIFICATION.md` frontmatter, then presents the gate decision and per-spec outcomes (per `<how-to-verify>` above) to the human and pauses. The human inspects, validates, and replies with one of the two `<resume-signal>` options. On `approved`, Tasks 2 + 3 fire; on `blocked: ...`, this plan halts and the failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md.
  </action>

  <verify>
    <automated><![CDATA[
      # Pre-checkpoint sanity: the verification artifact MUST exist with a gate_decision line; otherwise the human can't make a decision.
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md ] && \
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md && \
      echo "TASK 1 PRE-CHECKPOINT SANITY PASSED — awaiting human resume-signal"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Verification artifact exists:** `[ -f .planning/phases/02.2-.../02.2-01-VERIFICATION.md ]` returns true
    - **Gate decision present + valid:** `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches (the artifact has a parseable gate decision)
    - **Human resume-signal received:** the executor received either `approved` (proceed to Task 2) or `blocked: <root-cause>` (halt and route to planner re-evaluation; Tasks 2 + 3 do NOT fire)
  </acceptance_criteria>

  <done>
    The checkpoint has resumed with one of two outcomes:
    - **`approved`** — the human confirmed `02.2-01-VERIFICATION.md` records `gate_decision: APPROVED` (7/7 specs PASS clean + rerun, both ≤ 300s go test wall, all four threat mitigations verified). Tasks 2 + 3 are unblocked.
    - **`blocked: <root-cause summary>`** — the human surfaced a BLOCKED gate. Tasks 2 + 3 are NOT fired. The orchestrator routes the failure to a planner re-evaluation per the wave-failure-semantics contract.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md to mark Phase 02.2 complete</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md (current source: the Phase 02.2 entry at lines 95-103 with placeholders to finalize; the Phase 02.1 entry at lines 105-116 as a template for the post-edit shape; the Progress table at lines 169-175)
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md (the gate artifact — for the actual Goal sentence from a runtime-verified perspective)
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-PLAN.md (this plan set's Plan 01 file — for the plan list)
  </read_first>

  <action>
    Edit `.planning/ROADMAP.md` to finalize the Phase 02.2 entry.

    **Edit A — flip the top-of-phase checkbox:** Currently the phases list at the top of the doc (lines 19-23) doesn't yet have a Phase 02.2 entry inline; the inserted phase only appears in §"Phase Details" at lines 95-103. The ROADMAP convention for decimal-numbered INSERTED phases is to leave them out of the §"Phases" summary list and only edit the §"Phase Details" entry. NO EDIT to the §"Phases" summary list.

    **Edit B — finalize the Phase 02.2 §"Phase Details" entry (lines 95-103):**
    Replace the current placeholder shape:
    ```
    ### Phase 02.2: Layer B kind test timing fixes — ... (INSERTED)

    **Goal:** [Urgent work - to be planned]
    **Requirements**: TBD
    **Depends on:** Phase 02
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd-plan-phase 02.2 to break down)
    ```

    With the post-completion shape (mirror the Phase 02.1 entry structure at lines 105-116):
    ```
    ### Phase 02.2: Layer B kind test timing fixes — bump kindTestTimeout from 4min to 6min so helm --timeout 5m can complete; robust AfterSuite cleanup that handles zombie kind containers when BeforeSuite half-installs; re-scope make test-int wall-time goal to bound only the go test invocation (not test-int-kind-prep image builds + cluster create); optional cert-manager v1.16.2 to v1.20 bump. Closes Phase 02.1's BLOCKED runtime gate captured in 02.1-04-VERIFICATION.md. (INSERTED)

    **Goal:** Phase 02.1's BLOCKED runtime gate is closed — `make test-int` clean run reaches 7/7 Layer B specs PASS in ≤ 300s go test wall-time AND `KEEP_KIND_CLUSTER=true make test-int` rerun reaches 7/7 PASS in ≤ 300s, both verified end-to-end on a developer laptop with cert-manager v1.20.2 + helm install --replace + robust AfterSuite zombie cleanup.
    **Requirements**: No formal REQ-IDs — Phase 02.2 is debug closeout for Phase 02.1's runtime gate; the de facto requirements are the 4 source-shape fixes + 1 micro-fix enumerated in 02.2-RESEARCH.md.
    **Depends on:** Phase 02 (Phase 02.1 was a sibling debug phase; Phase 02.2 closes its runtime gate)
    **Plans:** 2/2 plans executed

    Plans:
    - [x] 02.2-01-PLAN.md — Source-shape fixes (kindTestTimeout 4m→7m, certManagerVersion v1.20.2, cleanupKindCluster helper, helm --replace, CI YAML DUR-check drop) + end-to-end runtime verification (Wave 1)
    - [x] 02.2-02-PLAN.md — ROADMAP/STATE closeout, gated on 02.2-01-VERIFICATION.md gate_decision=APPROVED (Wave 2)
    ```

    **Edit C — update the §"Progress" table (lines 169-175):** Add a row for Phase 02.2:
    ```
    | 02.2. Layer B kind test timing fixes (INSERTED) | 2/2 | Complete | 2026-05-13 (replace with actual completion date) |
    ```

    Place this row in numeric phase order — after the Phase 02.1 row (if it exists) or after the Phase 2 row (if 02.1 is still in progress). Verify the row insertion does NOT change any other phase's row content.

    Constraints:
    - Do NOT edit the §"Phases" summary list at lines 19-23 (decimal-numbered INSERTED phases are not added there).
    - Do NOT edit any other phase's entry. Run a diff inspection to confirm only the Phase 02.2 entry + the Progress table row changed (T-02.2-05 mitigation).
    - The Goal sentence is outcome-shaped (per goal-backward methodology): "Phase 02.1's BLOCKED runtime gate is closed — 7/7 Layer B specs PASS on clean run and rerun, both ≤ 300s go test wall-time, end-to-end on a developer laptop."
    - Use lowercase-x for the checkbox marker `[x]` (matches existing convention at line 116).
  </action>

  <verify>
    <automated><![CDATA[
      # Phase 02.2 entry is finalized
      [ "$(grep -c '### Phase 02.2:' .planning/ROADMAP.md)" -eq 1 ] && \
      [ "$(grep -c '02.2-01-PLAN.md' .planning/ROADMAP.md)" -ge 1 ] && \
      [ "$(grep -c '02.2-02-PLAN.md' .planning/ROADMAP.md)" -ge 1 ] && \
      # Placeholders removed
      [ "$(grep -c 'TBD (run /gsd-plan-phase 02.2' .planning/ROADMAP.md)" -eq 0 ] && \
      [ "$(grep -c 'Phase 02.2 .* 0 plans' .planning/ROADMAP.md)" -eq 0 ] && \
      [ "$(grep -c '\[Urgent work - to be planned\]' .planning/ROADMAP.md)" -eq 0 ] && \
      # Goal line is outcome-shaped (mentions 7/7 PASS or runtime gate closed)
      grep -E 'Phase 02.1.{0,30}runtime gate.{0,50}closed' .planning/ROADMAP.md | head -1 && \
      # Plans list marked complete
      [ "$(grep -E '^\- \[x\] 02.2-01-PLAN.md' .planning/ROADMAP.md)" ] && \
      [ "$(grep -E '^\- \[x\] 02.2-02-PLAN.md' .planning/ROADMAP.md)" ] && \
      # Progress table row added (loose match - the date is dynamic)
      [ "$(grep -c '02.2.*Layer B kind test timing fixes' .planning/ROADMAP.md)" -ge 2 ] && \
      [ "$(grep -E '\| 02.2\.' .planning/ROADMAP.md | wc -l)" -ge 1 ] && \
      # Other phase entries unchanged (T-02.2-05 mitigation)
      [ "$(grep -c '### Phase 1:' .planning/ROADMAP.md)" -eq 1 ] && \
      [ "$(grep -c '### Phase 2:' .planning/ROADMAP.md)" -eq 1 ] && \
      [ "$(grep -c '### Phase 02.1:' .planning/ROADMAP.md)" -eq 1 ] && \
      [ "$(grep -c '### Phase 3:' .planning/ROADMAP.md)" -eq 1 ] && \
      [ "$(grep -c '### Phase 4:' .planning/ROADMAP.md)" -eq 1 ] && \
      [ "$(grep -c '### Phase 5:' .planning/ROADMAP.md)" -eq 1 ] && \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Phase 02.2 entry finalized:** `grep -c '### Phase 02.2:' .planning/ROADMAP.md` returns exactly 1 (the existing header is preserved; only the body changes)
    - **Plans list populated:** `grep -c '02.2-01-PLAN.md' .planning/ROADMAP.md` returns ≥ 1 AND `grep -c '02.2-02-PLAN.md' .planning/ROADMAP.md` returns ≥ 1
    - **Placeholders removed:** `grep -c 'TBD (run /gsd-plan-phase 02.2' .planning/ROADMAP.md` returns 0 AND `grep -c '\[Urgent work - to be planned\]' .planning/ROADMAP.md` returns 0 AND `grep -c 'Phase 02.2 .* 0 plans' .planning/ROADMAP.md` returns 0
    - **Goal is outcome-shaped:** `grep -E 'Phase 02.1.{0,30}runtime gate.{0,50}closed' .planning/ROADMAP.md` matches (or equivalent regex confirming the Goal references the closed runtime gate)
    - **Plan checkboxes marked complete:** `grep -E '^\- \[x\] 02.2-01-PLAN.md' .planning/ROADMAP.md` matches AND `grep -E '^\- \[x\] 02.2-02-PLAN.md' .planning/ROADMAP.md` matches
    - **Progress table row added:** at least one new row matching `02.2.*Layer B kind test timing fixes` exists in the table region
    - **T-02.2-05 (other phase entries unchanged):** all six other phase headings count exactly 1: `grep -c '### Phase 1:'`, `### Phase 2:`, `### Phase 02.1:`, `### Phase 3:`, `### Phase 4:`, `### Phase 5:`
  </acceptance_criteria>

  <done>
    `.planning/ROADMAP.md` has the Phase 02.2 entry finalized with the actual Goal sentence, plan list, and completion status. All other phase entries are untouched. The Progress table has a row for Phase 02.2. All grep gates pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md to record Phase 02.2 completion with accumulated context</name>
  <files>.planning/STATE.md</files>

  <read_first>
    - .planning/STATE.md (current source: lines 1-15 frontmatter with progress block; lines 23-34 Current Position; lines 36-50 Performance Metrics; lines 76-85 Roadmap Evolution; lines 87-119 Decisions list)
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md (frontmatter wall times + spec counts; Section 1 + 2 outcomes for the metrics row)
    - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-PLAN.md (must_haves block + key_links for the Decisions / Patterns Established entries)
  </read_first>

  <action>
    Edit `.planning/STATE.md` to record Phase 02.2 completion.

    **Edit A — frontmatter `last_updated` and `last_activity`:** Bump `last_updated` to the current ISO timestamp (use `date -u +"%Y-%m-%dT%H:%M:%S.000Z"` or equivalent). Update `last_activity` to a Phase 02.2 closeout line (e.g., "2026-05-13 -- Phase 02.2 closeout: 02.1 runtime gate closed (7/7 specs PASS clean + rerun)"). Leave `milestone` and `milestone_name` fields alone (they refer to v1.0, not to the per-phase work). Leave `status: executing` alone (the milestone is still in flight; only Phase 02.2 is done).

    **Edit B — frontmatter `progress.completed_phases` and `progress.completed_plans`:** The current STATE.md shows `total_phases: 7`, `completed_phases: 3`, `total_plans: 28`, `completed_plans: 28`, `percent: 100` — but these numbers appear to be stale (Phase 02.1 was in flight, not done; the 100% is wrong). DO NOT regenerate these counts speculatively. Instead, set:
    - `completed_phases`: increment by 1 (assuming this is the next phase to close)
    - `completed_plans`: increment by 2 (this phase's 2 plans)

    If the existing counts look wrong (the `completed_plans: 28` with `percent: 100` suggests a stale state), surface a note in the SUMMARY rather than silently overwriting. The SDK-side `state.load` query is the source of truth; if it's stale, the orchestrator should reconcile it separately.

    **Edit C — "Current Position" block (lines 23-34):** Update from "Phase: 02.1 (debug-fix-...) — EXECUTING" to "Phase: 02.2 (layer-b-kind-test-timing-fixes-...) — COMPLETE". The next-phase pointer should reflect the user's roadmap state — likely Phase 3 is the next planned milestone work. If unclear, leave a note that the next phase is "TBD by user" rather than guess.

    **Edit D — "Performance Metrics" table (the per-phase rows around line 50+):** Append a row for Phase 02.2:
    ```
    | Phase 02.2 P01 | <duration>min | 3 tasks | 3 files |
    | Phase 02.2 P02 | <duration>min | 3 tasks | 2 files |
    ```
    Duration values come from the SUMMARY's `## Performance` section (filled in after each plan completes). If unavailable at this task's runtime, leave the field as `-`.

    **Edit E — "Accumulated Context" §"Roadmap Evolution" block (lines 76-85):** Update the Phase 02.2 line from a forward-looking insertion note to a completion note. Find the line "Phase 02.2 inserted after Phase 02: Layer B kind test timing fixes — ..." and update to reflect that the phase is now COMPLETE:
    ```
    - Phase 02.2 COMPLETED 2026-05-13: Closed Phase 02.1's BLOCKED runtime gate. Five tactical fixes landed (kindTestTimeout 4m→7m; cert-manager v1.16.2→v1.20.2; helm install --replace for idempotent rerun; cleanupKindCluster() helper with io.x-k8s.kind.cluster=tide-test label-filter fallback for Docker 28→29 zombie-container regression; CI YAML DUR-check drop to align with Makefile's inner timeout 300s scope). End-to-end runtime verification PASSED both clean run (7/7 Layer B specs) and KEEP_KIND_CLUSTER=true rerun (7/7); captured in 02.2-01-VERIFICATION.md.
    ```

    **Edit F — "Accumulated Context" §"Decisions" block (after the long bullet list at line 87+):** Append new bullets for Phase 02.2 decisions:
    ```
    - [Phase 02.2]: kindTestTimeout = 7m (NOT 6m floor): RESEARCH §"Pattern 1" budget arithmetic — 50s pre-helm setup + 300s helm --timeout 5m + 5s waitForControllerReady + 60s+ variance margin. 7m gives 2m margin above helm's 5m, survives slow CI runners. 6m floor leaves only 60s margin and re-introduces shadow-kill risk.
    - [Phase 02.2]: AfterSuite zombie cleanup pattern = kind delete → docker ps -aq --filter label=io.x-k8s.kind.cluster=<name> → docker rm -f -v fallback. Best-effort (GinkgoWriter.Printf warnings, NOT Fail()). KEEP_KIND_CLUSTER=true short-circuit MUST precede the cleanup helper (T-02.2-02 mitigation). Plain exec.Command() (not exec.CommandContext) because outer ctx is cancelled by AfterSuite entry (Pitfall 7).
    - [Phase 02.2]: Test wall-time budget scope = go test only (not full make test-int chain). Makefile's existing inner `timeout 300s go test` is the source of truth; CI's outer DUR > 300 check measured the wrong span (includes ~880s cold-cache test-int-kind-prep). Dropped the CI outer check; rely on `timeout 300s` exit 124 propagating through `time make test-int`.
    - [Phase 02.2]: cert-manager v1.20.2 bump verified non-breaking for TIDE — chart uses cert-manager.io/v1 Issuer + Certificate (stable since 1.x); both Certificate templates already specify issuerRef.kind: Issuer explicitly (v1.20 issuerRef-defaults revert is non-impacting per RESEARCH Pattern 4 + Pitfall 4).
    - [Phase 02.2]: helm install --replace flag added to applyController() helm args for KEEP_KIND_CLUSTER=true rerun idempotency (Q1 micro-fix from RESEARCH §"Open Questions Surfaced"). Resolves "cannot re-use a name that is still in use" failure mode when rerun encounters an existing tide release in tide-system.
    ```

    **Edit G — "Session Continuity" block (lines 119-123):** Update `Last session:` to current ISO timestamp. Update `Stopped at:` to "Phase 02.2 complete — closed Phase 02.1 BLOCKED gate" (or similar). Update `Resume file:` to `None` (Phase 02.2 work is fully closed).

    Constraints:
    - **DO NOT touch the `milestone: v1.0` or `milestone_name:` fields** — those refer to the v1.0 self-hosting MVP, not to individual phases.
    - **DO NOT change `status: executing`** — the milestone is still in flight; only this phase is done.
    - **DO NOT regenerate the percent field if it looks inconsistent** (current `percent: 100` with `completed_plans: 28` is likely stale; this task's increment shouldn't blindly bring it further into a wrong state). Leave it at its current value or set to a conservative `null` / `tbd`. Surface the inconsistency in the SUMMARY for the orchestrator to address separately.
    - **DO NOT add Phase 02.2 to STATE.md's `## Blockers/Concerns` section** — the gate was APPROVED, no blockers.
  </action>

  <verify>
    <automated><![CDATA[
      # Phase 02.2 marked complete in STATE.md
      [ "$(grep -c 'Phase 02.2' .planning/STATE.md)" -ge 1 ] && \
      [ "$(grep -ci 'COMPLETED.*02.2\|02.2.*COMPLETE' .planning/STATE.md)" -ge 1 ] && \
      # Five fixes referenced in Decisions
      [ "$(grep -c 'kindTestTimeout = 7m' .planning/STATE.md)" -ge 1 ] && \
      [ "$(grep -c 'cert-manager v1.20' .planning/STATE.md)" -ge 1 ] && \
      [ "$(grep -c '\-\-replace' .planning/STATE.md)" -ge 1 ] && \
      [ "$(grep -c 'io.x-k8s.kind.cluster' .planning/STATE.md)" -ge 1 ] && \
      [ "$(grep -c 'DUR > 300\|DUR check\|wall-time budget scope' .planning/STATE.md)" -ge 1 ] && \
      # Frontmatter last_updated bumped to a 2026 ISO timestamp
      grep -E '^last_updated: "2026-' .planning/STATE.md && \
      # 02.2 verification artifact referenced
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/STATE.md)" -ge 1 ] && \
      # Milestone block untouched
      grep -E '^milestone: v1.0$' .planning/STATE.md && \
      grep -E '^status: executing$' .planning/STATE.md && \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Phase 02.2 completion recorded:** `grep -c 'Phase 02.2' .planning/STATE.md` returns ≥ 1 AND `grep -ci 'COMPLETED.*02.2\|02.2.*COMPLETE' .planning/STATE.md` returns ≥ 1
    - **All five fixes referenced in Decisions block:**
      - `grep -c 'kindTestTimeout = 7m' .planning/STATE.md` returns ≥ 1
      - `grep -c 'cert-manager v1.20' .planning/STATE.md` returns ≥ 1
      - `grep -c '\-\-replace' .planning/STATE.md` returns ≥ 1
      - `grep -c 'io.x-k8s.kind.cluster' .planning/STATE.md` returns ≥ 1
      - `grep -c 'DUR > 300\|DUR check\|wall-time budget scope' .planning/STATE.md` returns ≥ 1
    - **last_updated bumped:** `grep -E '^last_updated: "2026-' .planning/STATE.md` matches (year 2026 ISO timestamp)
    - **Verification artifact referenced:** `grep -c '02.2-01-VERIFICATION.md' .planning/STATE.md` returns ≥ 1
    - **Milestone block untouched:** `grep -E '^milestone: v1.0$' .planning/STATE.md` matches AND `grep -E '^status: executing$' .planning/STATE.md` matches (milestone fields NOT edited)
  </acceptance_criteria>

  <done>
    `.planning/STATE.md` has Phase 02.2 recorded as complete with all five fixes documented in the Decisions block, the verification artifact cross-referenced, and the timestamps bumped. The milestone fields (`milestone: v1.0`, `status: executing`) are preserved. All grep gates pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `02.2-01-VERIFICATION.md` (Plan 01 output) → Plan 02 Task 1 checkpoint | The verification artifact's frontmatter `gate_decision:` field IS the gate; this plan reads it as the authoritative decision |
| Plan 02 Task 2 + 3 edits → ROADMAP.md / STATE.md | Documentation-only edits; no runtime impact; T-02.2-05 protects against accidentally regressing other phase entries |
| Human-verify checkpoint → executor | The checkpoint's `resume-signal` accepts "approved" or "blocked: <root-cause>"; any other response halts the plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-04 (this plan's interpretation) | Tampering | Closeout fires on a BLOCKED verification artifact, falsely marking the phase done | mitigate | `checkpoint:human-verify` at Task 1 reads `02.2-01-VERIFICATION.md` frontmatter and confirms `gate_decision: APPROVED` before any ROADMAP/STATE edits. The `<resume-signal>` block accepts "approved" (proceed) or "blocked: <summary>" (halt and route to planner). The Phase 02.1 Plan 04 Task 3 precedent (which correctly BLOCKED on the prior phase's failed gate) is the structural model. |
| T-02.2-05 | Tampering | ROADMAP/STATE edits silently regress a different in-flight phase's checked state | mitigate | Task 2 acceptance criteria explicitly verify all six other phase headings (`Phase 1:`, `Phase 2:`, `Phase 02.1:`, `Phase 3:`, `Phase 4:`, `Phase 5:`) each appear exactly once after the edit — if any other phase's section is accidentally duplicated or deleted, the gate fires. Task 3 acceptance criteria explicitly preserve the milestone block (`milestone: v1.0`, `status: executing`) from accidental edits. |

</threat_model>

<verification>
After all three tasks complete:

**Documentation gates:**
- `.planning/ROADMAP.md` has Phase 02.2 entry finalized with all placeholders removed and the two plans marked `[x]`
- `.planning/STATE.md` has Phase 02.2 recorded as COMPLETE with the five fixes documented and timestamps bumped
- `.planning/phases/02.2-.../02.2-01-VERIFICATION.md` (the gate artifact from Plan 01) is referenced in STATE.md

**Structural gates (T-02.2-05 mitigation):**
- All six other phase entries in ROADMAP.md are unchanged (each header appears exactly once)
- STATE.md milestone block (`milestone: v1.0`, `status: executing`) is unchanged
</verification>

<success_criteria>
- Task 1 (gate check) PASSED with `approved` signal — `02.2-01-VERIFICATION.md` says `gate_decision: APPROVED`
- Task 2 ROADMAP.md edit applied; all acceptance criteria pass
- Task 3 STATE.md edit applied; all acceptance criteria pass
- No production source files modified (this plan is documentation-only closeout)
- If Task 1 returns `blocked: ...`, Tasks 2 + 3 do NOT fire; the failure routes to a planner re-evaluation per the wave-failure-semantics contract
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-SUMMARY.md` documenting:
- The verification gate decision (APPROVED — required for this plan to run; otherwise this SUMMARY is not written and a BLOCKED summary is documented in the verification artifact's Root-Cause Summary instead)
- The ROADMAP.md and STATE.md edits applied
- Any inconsistencies surfaced (e.g., STATE.md `progress.percent: 100` looking stale — for orchestrator follow-up, not this plan's territory to fix)
- The complete Phase 02.2 close-out chain: 4 source fixes + 1 micro-fix landed → runtime verified → ROADMAP/STATE finalized
- Threat-model mitigations applied (T-02.2-04 / T-02.2-05) with concrete evidence
</output>
</content>
</invoke>