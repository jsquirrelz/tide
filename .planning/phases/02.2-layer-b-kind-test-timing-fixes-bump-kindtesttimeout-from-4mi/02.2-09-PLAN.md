---
phase: "02.2"
plan: "09"
type: execute
wave: 8
gap_closure: true
depends_on: ["02.2-08"]
files_modified:
  - test/integration/kind/suite_test.go
  - test/integration/kind/caps_test.go
  - test/integration/kind/output_test.go
  - test/integration/kind/failure_test.go
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, test-fixture, harness-bug, runtime-verification, kind-integration, phase-02.2, tactical, cascade-7, scope-completion, chain-terminator-candidate-v2]

threat_model_summary:
  - "T-02.2-24 (mitigate): the new `createProjectHierarchy` helper might subtly diverge from `applyHierarchy`'s Project/Milestone/Phase shape (e.g., different field set, different API version, different Secret name, different budget). Tests using `createProjectHierarchy` would then create a structurally-different Project than what `resolveProject` expects in the credproxy namespace, OR `resolveProject` would succeed but the Plan/Task created by the test's `applyYAML` would point at a Phase shape that doesn't validate. **Mitigation:** the new helper's Secret+Project+Milestone+Phase YAML MUST be byte-identical (modulo namespace/name parameterization) to the corresponding section of `applyHierarchy`'s Step 3. Acceptance: a diff between `createProjectHierarchy`'s hierarchy YAML and `applyHierarchy`'s Step 3 lines (minus the Plan+Task parts) shows ONLY parameter substitutions, no field-shape differences. The Secret name (`tide-provider-secret`), Secret data key (`ANTHROPIC_API_KEY`), Secret data value (the same base64 placeholder), Project `targetRepo` shape, `providerSecretRef`, `budget.absoluteCapCents: 100000`, Milestone `projectRef`, Phase `milestoneRef`, and API group `tideproject.k8s/v1alpha1` are all field-for-field identical to `applyHierarchy`. The ONLY difference is `createProjectHierarchy` STOPS at Phase — it does NOT emit Plan or Task; those are supplied by the caller's `applyYAML`."
  - "T-02.2-25 (mitigate): adding a new helper to `suite_test.go` could accidentally damage the existing `applyHierarchy` (e.g., displacing its function body, breaking its imports, introducing a duplicate function name that fails to compile). **Mitigation:** Task 1 verify block asserts (a) `applyHierarchy` still exists verbatim AFTER the edit (`grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` returns ≥ 1, AND `git diff` for `suite_test.go` shows zero lines beginning with `-` that match `func applyHierarchy` — proving the existing function header line was NOT deleted); (b) `test/integration/kind/credproxy_test.go` is UNTOUCHED (`git status --porcelain test/integration/kind/credproxy_test.go` returns empty); (c) `go build ./test/integration/kind/...` exits 0 (confirming no duplicate-symbol or import collision). The cascade-5/6 fix is preserved verbatim via the credproxy_test.go untouched-guard. The new helper is a SIBLING function, added alongside `applyHierarchy`, NOT a refactor of it. **Acceptance gate:** `git status --porcelain` on `credproxy_test.go` returns empty; `grep -c 'func applyHierarchy' suite_test.go` ≥ 1; `grep -c 'func createProjectHierarchy' suite_test.go` returns exactly 1; `go build ./test/integration/kind/...` exits 0."
  - "T-02.2-26 (mitigate): the existing tests have specific Plan and Task names in their inline YAML (caps: `cap-plan`/`hang-task`; output: `output-plan`/`exceed-output-task`; failure: `fail-plan`/`alpha-fail`/`beta-fail`/`gamma-fail`). If `createProjectHierarchy`'s Phase has a forward reference (e.g., an `expectedPlanRef` field) pointing to a specific Plan name, the existing tests' Plan names might not match and the controller might reject the Plan-Phase link. **Mitigation:** confirm by reading `applyHierarchy`'s Phase YAML at suite_test.go lines 597–603 — the Phase has ONLY `milestoneRef` (pointing UP at its Milestone parent), NOT any `planRef`/`expectedPlanRef`/`children` field (the parent-to-child link comes from the Plan's `spec.phaseRef`, not from the Phase). The new helper's Phase MUST also have ONLY `milestoneRef`, mirroring `applyHierarchy`. Acceptance: `grep -E 'milestoneRef|projectRef|targetRepo' suite_test.go` count post-edit equals the pre-edit count + the new helper's emissions (exactly 3 new field references — `targetRepo`, `projectRef`, `milestoneRef` — and ZERO new `planRef`/`expectedPlanRef`/`children` field references in the helper body)."
  - "T-02.2-27 (accept): cascade-8 (if any) — low probability but possible. The cascade-7 fix (createProjectHierarchy applied to the 3 remaining Layer B fixture files) is structurally complete for the harness-bug class — all 4 Layer B fixture files (credproxy/caps/output/failure) will use the parent-hierarchy fix pattern after Task 1, leaving NO Layer B fixture WITHOUT a parent Project hierarchy. If a NEW failure surfaces, it must be (a) a real spec-flake/timing race in wave/failure-graph dynamics; (b) a kind-infra issue (networking, image pull, etcd race) that the prior 7 iterations didn't trigger; (c) a stub-subagent image regression; (d) a genuine production controller bug surfacing under the load of 4 fixtures now all running to completion (e.g., reconcile-loop contention, work-queue saturation, leader-election flakes); or (e) a 4-minute budget exceedance now that 7 specs all run end-to-end (vs. the 2 that ran in Plan 02.2-08's BLOCKED outcome). **Acceptance:** if Task 2 records `gate_decision: BLOCKED`, the artifact's §Section 5 Root-Cause Summary MUST classify cascade-8 per T-02.2-16 taxonomy (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) AND identify the specific spec(s) failing AND propose Plan 02.2-10's fix shape from the start. The most likely cascade-8 candidate is `budget-vs-actual` on the outer 600s wall-time (now that all 4 fixtures consume their full per-spec budgets) — if so, Plan 02.2-10 escalates to the deferred Layer A/Layer B split. A `harness-bug` regression would indicate `createProjectHierarchy` somehow diverges from `applyHierarchy` — investigate the byte-diff per T-02.2-24."

must_haves:
  truths:
    - "`test/integration/kind/suite_test.go` has a NEW function `func createProjectHierarchy(ctx context.Context, ns string) error` added alongside the EXISTING `func applyHierarchy(ctx context.Context, ns, planName, taskName string) error` (both functions present after Task 1; both compile; both callable independently)"
    - "`test/integration/kind/caps_test.go` calls `createProjectHierarchy(ctx, capsNS)` exactly once, BEFORE its `Expect(applyYAML(capPlanYAML)).To(Succeed())` line — the call appears between the existing `By(\"Creating a Plan with a hanging Task ...\")` block and the existing `applyYAML` invocation"
    - "`test/integration/kind/output_test.go` calls `createProjectHierarchy(ctx, outputNS)` exactly once, BEFORE its `Expect(applyYAML(outputPlanYAML)).To(Succeed())` line"
    - "`test/integration/kind/failure_test.go` calls `createProjectHierarchy(ctx, ns)` exactly once (where `ns` is the local variable holding `failNS`), BEFORE its `Expect(applyYAML(planYAML)).To(Succeed())` line"
    - "Existing `applyHierarchy` function body is byte-identical to its pre-edit state — `git diff suite_test.go | grep -E '^-.*func applyHierarchy'` returns empty (no `-` line deleting the header) AND `grep -c 'func applyHierarchy' suite_test.go` ≥ 1 (the function definition still exists in the file)"
    - "`test/integration/kind/credproxy_test.go` is UNTOUCHED — `git status --porcelain test/integration/kind/credproxy_test.go` returns empty (cascade-5 + cascade-6 fixes preserved verbatim)"
    - "`test/integration/kind/testdata/three-task-wave.yaml` is UNTOUCHED — `git status --porcelain test/integration/kind/testdata/` returns empty"
    - "`test/integration/kind/wave_test.go` is UNTOUCHED — `git status --porcelain test/integration/kind/wave_test.go` returns empty (wave tests use applyFile against testdata/three-task-wave.yaml which already has a full hierarchy; no change needed)"
    - "`go build ./test/integration/kind/...` AND `go vet ./test/integration/kind/...` both exit 0 after Task 1 (no duplicate-symbol, no import collision, no missing-import — the new helper uses `context.Context`, `fmt`, `applyYAML`, `createNamespace` which are all already imported/defined in `suite_test.go`)"
    - "Option B contract preserved: zero modifications to `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `Makefile`, OR `.github/`"
    - "`02.2-09-VERIFICATION.md` exists with frontmatter including `phase: 02.2`, `plan: 09`, `task: 2`, `artifact: verification`, `gate_decision: APPROVED|BLOCKED`, AND four runtime-observation fields: `credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds` (post-cascade-7-fix, all 4 fixtures now run to completion; measuring each)"
    - "APPROVED gate requires ALL 5 conditions on BOTH clean run AND idempotent rerun: (1) wall ≤ 600s; (2) Manager Pod `STATUS=Running` AND restarts ≤ 1; (3) manager log has zero `flag provided but not defined` errors AND zero `no project found in namespace <ns>` errors for any of caps-test/output-test/failure-test/credproxy-test namespaces (THIS is the cascade-7 closure indicator — the absence of these errors proves all 4 fixtures now have parent Project hierarchies); (4) PVC `tide-projects` Bound RWO; (5) 7/7 Layer B Ginkgo specs PASS"
    - "If `02.2-09-VERIFICATION.md` records `gate_decision: APPROVED`, this is the FIRST APPROVED gate in Phase 02.2's cascade chain (terminating the 7-iteration chain) — Task 3 fires; Plan 02.2-02's `depends_on` becomes `[\"02.2-09\"]` and all 31 references to `02.2-01-VERIFICATION.md` are redirected to `02.2-09-VERIFICATION.md`"
    - "If `02.2-09-VERIFICATION.md` records `gate_decision: BLOCKED` with cascade-8, Task 3 does NOT fire — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring); cascade-8 classified per T-02.2-16 taxonomy in §Section 5; cascade-8 root cause and Plan 02.2-10 fix shape proposed in §Section 5 Fix landscape"
    - "T-02.2-21 carry-forward measurement: `credproxy_pod_wait_observed_seconds` re-measured on the Plan 02.2-09 clean run; if observed value < 90s (substantial drop from Plan 02.2-08's confounded 120s observation), the cascade-7 controller-pressure hypothesis is confirmed AND no further credproxy timeout bump is needed; if observed value ≥ 90s, the marginal-dispatch-latency flag fires and a future plan may need to address the production-side concurrent-status-update root cause"

  artifacts:
    - path: "test/integration/kind/suite_test.go"
      provides: "NEW `createProjectHierarchy(ctx, ns) error` helper added alongside existing `applyHierarchy` — creates Namespace+Secret+Project+Milestone+Phase (NO Plan, NO Task; callers supply those via their own `applyYAML`)"
      contains: "func createProjectHierarchy"
    - path: "test/integration/kind/caps_test.go"
      provides: "call to `createProjectHierarchy(ctx, capsNS)` before `applyYAML(capPlanYAML)` — closes cascade-7 for the caps fixture by ensuring the `resolveProject` controller lookup succeeds for the `caps-test` namespace"
      contains: "createProjectHierarchy(ctx, capsNS)"
    - path: "test/integration/kind/output_test.go"
      provides: "call to `createProjectHierarchy(ctx, outputNS)` before `applyYAML(outputPlanYAML)` — closes cascade-7 for the output fixture"
      contains: "createProjectHierarchy(ctx, outputNS)"
    - path: "test/integration/kind/failure_test.go"
      provides: "call to `createProjectHierarchy(ctx, ns)` before `applyYAML(planYAML)` — closes cascade-7 for the failure fixture; covers all 3 Tasks (alpha-fail/beta-fail/gamma-fail) since they all share the same `ns` and parent Project"
      contains: "createProjectHierarchy(ctx, ns)"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md"
      provides: "Runtime verification artifact with gate_decision frontmatter — chain terminator candidate v2 (Plan 02.2-08 was candidate v1 but BLOCKED on cascade-7)"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-09\"] + 31 references redirected to 02.2-09-VERIFICATION.md (gated on Task 2 APPROVED) — FIRST APPROVED rewire in Phase 02.2 history if Task 2 returns APPROVED"
      contains: "02.2-09"

  key_links:
    - from: "test/integration/kind/caps_test.go (createProjectHierarchy call)"
      to: "internal/controller/task_controller.go:217 resolveProject"
      via: "Project CRD now exists in caps-test namespace; the label fast-path miss + ProjectList fallback in resolveProject finds the deterministic `caps-test-project` and Step 3 of reconcileDispatch succeeds"
      pattern: 'createProjectHierarchy\(\s*ctx\s*,\s*capsNS\s*\)'
    - from: "test/integration/kind/output_test.go (createProjectHierarchy call)"
      to: "internal/controller/task_controller.go:217 resolveProject"
      via: "Project CRD now exists in output-test namespace; same resolveProject fast-path/fallback as caps"
      pattern: 'createProjectHierarchy\(\s*ctx\s*,\s*outputNS\s*\)'
    - from: "test/integration/kind/failure_test.go (createProjectHierarchy call)"
      to: "internal/controller/task_controller.go:217 resolveProject"
      via: "Project CRD now exists in failure-test namespace; ALL THREE Tasks (alpha-fail, beta-fail, gamma-fail) share this Project parent because they share the namespace"
      pattern: 'createProjectHierarchy\(\s*ctx\s*,\s*ns\s*\)'
    - from: "createProjectHierarchy helper in suite_test.go"
      to: "existing applyHierarchy helper in suite_test.go (preserved verbatim)"
      via: "sibling functions — both call createNamespace + applyYAML internally; createProjectHierarchy is the Plan-and-Task-OMITTED variant of applyHierarchy; both share the same Secret/Project/Milestone/Phase YAML shape (T-02.2-24 byte-identical mitigation)"
      pattern: 'func createProjectHierarchy.*func applyHierarchy|func applyHierarchy.*func createProjectHierarchy'
    - from: "02.2-09-VERIFICATION.md gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 wires 02.2-02 (HARD-GATED on APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md (depends_on field, post-Task-3)"
      to: "this plan's verification artifact"
      via: "frontmatter depends_on: [\"02.2-09\"] — bumped only if Task 2 returns APPROVED"
      pattern: 'depends_on: \[.{0,5}02.2-09.{0,5}\]'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-08-VERIFICATION.md` §Section 5 (seventh iteration in Phase 02.2's cascade chain). Cascade-7 is a SCOPE-COMPLETION issue: Plan 02.2-07 authored `applyHierarchy(ctx, ns, planName, taskName)` and refactored ONLY `credproxy_test.go` to use it; the three other Layer B fixture files (`caps_test.go`, `output_test.go`, `failure_test.go`) still call `createNamespace(ns)` + inline `applyYAML(planYAML)` WITHOUT a parent Project hierarchy in the namespace. All three trip the SAME `resolveProject` "no project found in namespace <ns>" gate at `task_controller.go:217` that Plan 02.2-07 fixed for credproxy.

**Why this didn't surface in Plan 02.2-07's verification:** Ginkgo's inner `-timeout=5m` cap fired after credproxy's two specs failed (60s + 60s + overhead ≈ 5m budget consumed), preventing caps/output/failure specs from running. Plan 02.2-08's `Eventually` timeout bumps gave each spec enough per-spec budget to RUN to its assertion failure, surfacing the remaining three harness-bugs simultaneously in the clean run's per-spec outcome table.

**SAME CLASS as cascade-5 (harness-bug); different fixture files; scope-completion fix.** Plan 02.2-07's helper authoring was the right architectural call, but Plan 02.2-07's apply scope was incomplete — only one of the four Layer B fixture files received the refactor. Plan 02.2-09 applies the parent-hierarchy fix to the remaining three.

**User decision: extend the helper pattern to the remaining 3 files via a new sibling helper `createProjectHierarchy(ctx, ns)`** (Option β from 02.2-08-VERIFICATION.md §Section 5 "Fix landscape"). The fix shape was chosen over two alternatives:

- **Option α (reuse existing applyHierarchy with throwaway names):** Each test calls `applyHierarchy(ctx, ns, ns+"-helper-plan", ns+"-helper-task")` BEFORE its own `applyYAML(planYAML)`. The helper's throwaway Plan+Task sit unused alongside the test's actual Plan+Task. Smallest surface — 3 single-line additions; zero `suite_test.go` modifications. Slight cosmetic waste (extra unused CRDs per test) but functionally clean. REJECTED for cosmetic-waste reason; the helper authoring cost is small.
- **Option β (new `createProjectHierarchy` helper) — CHOSEN:** Add a NEW helper `createProjectHierarchy(ctx context.Context, ns string) error` to `suite_test.go` that creates ONLY Namespace+Secret+Project+Milestone+Phase (stops before Plan+Task). The 3 tests call this new helper instead of `applyHierarchy`. Cleaner semantics (no throwaway resources); ~50 lines new helper code in `suite_test.go` plus 3 single-line call-site insertions. **Adds-not-modifies `suite_test.go` (existing `applyHierarchy` stays intact — cascade-5 fix preserved verbatim).** SELECTED.
- **Option γ (refactor applyHierarchy to extract a shared sub-function):** Touches the existing `applyHierarchy` more invasively — risk to cascade-5 fix. REJECTED for blast-radius reason.

**CRITICAL FRAMING NOTE (load-bearing — DO NOT inherit the wrong cascade class from prior verification framings):** Cascade-7 is a HARNESS-BUG, NOT a spec-flake. Plan 02.2-08's `Eventually` timeout bumps are correct and remain in effect (failure_test.go AC3 beta-fail at `240*time.Second`; credproxy_test.go pod-wait at `120*time.Second`). The cascade-7 root cause is NOT that the bumped budgets are insufficient — it is that 3 of 4 Layer B fixture files never had a parent Project hierarchy in their namespace, causing `resolveProject` to fail at the dispatch gate BEFORE any Pod-wait timer starts. The bumped `Eventually` budgets are still required (they enable the cascade-7 specs to RUN to their assertion failures instead of getting truncated by the Ginkgo inner-timeout), but they are NOT the cascade-7 fix. The cascade-7 fix is the new `createProjectHierarchy` helper.

**Why this is likely THE chain terminator (v2):**
- Cascades 1–6 are all closed:
  - cascade-1 (PVC RWX→RWO): CLOSED (Plan 02.2-03)
  - cascade-2 (--metrics-bind-address flag): CLOSED (Plan 02.2-04)
  - cascade-3 (--webhook-cert-path flag): CLOSED (Plan 02.2-05)
  - cascade-4 (Makefile timeout 300s→600s): CLOSED (Plan 02.2-06)
  - cascade-5 (credproxy fixture-incomplete; applyHierarchy authored): CLOSED (Plan 02.2-07)
  - cascade-6 (Eventually timeout budgets too tight): CLOSED (Plan 02.2-08)
- Cascade-7 is the same CLASS as cascade-5 but in 3 additional fixture files — a SCOPE issue, not a new class
- After applying the parent-hierarchy fix to all 4 Layer B fixture files (credproxy via cascade-5 fix; caps/output/failure via cascade-7 fix), all 7 Layer B specs should reach their assertions and PASS
- Cascade-8 would have to be something genuinely outside the test-fixture surface (a real spec-flake/timing race, kind-infra issue, stub-subagent image regression, or production controller bug). The prior 7 iterations have exhausted the harness-bug surface area; the probability of cascade-8 surfacing is LOW

**T-02.2-21 carry-forward (load-bearing for the runtime measurement):** Plan 02.2-08's verification recorded `credproxy_pod_wait_observed_seconds: 120` (full 120s budget consumed), but the measurement was CONFOUNDED by controller work-queue pressure from concurrent `resolveProject` failures in caps/output/failure namespaces (each repeatedly retrying `resolveProject` against the same missing-Project namespace). With cascade-7 fixed and those concurrent failures removed, the credproxy pod-wait observation on the Plan 02.2-09 clean run should drop substantially. If it still consumes 90s+ on the clean run, the marginal-dispatch-latency flag still fires AND a future plan (or post-Phase-02.2 production-code fix) may need to address the underlying ResourceVersion conflict race between `PlanReconciler.stampTaskLabels` and `TaskReconciler` status updates.

**TACTICAL SCOPE — Option β contract:**
- This plan modifies ONLY `test/integration/kind/suite_test.go` (one new function added, alongside existing functions), `test/integration/kind/caps_test.go` (one new call-site line), `test/integration/kind/output_test.go` (one new call-site line), AND `test/integration/kind/failure_test.go` (one new call-site line)
- This plan does NOT modify any production source under `internal/`, `api/`, `cmd/`, `pkg/`, `charts/`, `Makefile`, `.github/` — Option B contract from Plans 02.2-06/07/08 preserved
- This plan does NOT modify `test/integration/kind/credproxy_test.go` — cascade-5/6 fixes preserved verbatim
- This plan does NOT modify `test/integration/kind/wave_test.go` — wave tests already work via `applyFile(testdata/three-task-wave.yaml)` which has a full hierarchy embedded in the YAML file
- This plan does NOT modify `test/integration/kind/testdata/` — fixture YAMLs unchanged
- This plan does NOT modify the EXISTING `applyHierarchy` function body in `suite_test.go` — the new `createProjectHierarchy` is a SIBLING function, added alongside; cascade-5 fix preserved verbatim
- This plan does NOT touch `charts/tide/values.yaml` per `.continue-here.md` anti-pattern #2
- This plan does NOT modify Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08 — only `02.2-02-PLAN.md` is touched (and only by Task 3, HARD-GATED on Task 2 APPROVED)
- This plan does NOT investigate the underlying ResourceVersion conflict race in the production code — that remains a future-phase concern; T-02.2-21 marginal-flag surfaces the trigger if dispatch latency stays high after cascade-7 closure

**Plan 02.2-08's 3-task pattern this plan mirrors:** Plan 02.2-08 ran a 3-task shape — T1 source edit (two Eventually timeout bumps), T2 runtime verification (`02.2-08-VERIFICATION.md` with `gate_decision` frontmatter + T-02.2-21 observation fields), T3 surgical edit of `02.2-02-PLAN.md` gated on APPROVED. This plan mirrors that shape exactly with cascade-7's target (new `createProjectHierarchy` helper + 3 call-site additions) substituted for cascade-6's target (two `Eventually` timeout replacements). **The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic:** Task 3 fires ONLY if Task 2's `gate_decision: APPROVED`.

**Recovery state note (load-bearing for Task 3's rewire scope):** Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, AND 02.2-08 Task 3 ALL DID NOT FIRE (their respective Task 2 gates were each BLOCKED on a different cascade). As a result, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (verified at plan-authoring time by `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returning exactly 31). This plan's Task 3 lands the long-pending rewire if Task 2's gate is APPROVED, redirecting all 31 references to `02.2-09-VERIFICATION.md` in one pass.

**Recovery-lesson note from `.continue-here.md` Plan 02.2-05 protocol (carried forward through 02.2-06/07/08):** Write `02.2-09-VERIFICATION.md` WITH PRELIMINARY frontmatter immediately after the clean-run captures land in `/tmp/02.2-09-clean-run.log`, then add Sections 2–6 incrementally as the rerun completes and the cluster diagnostic snapshot is captured. This protects against mid-run executor API errors / context window pressure during the long-running runtime verification phase — the artifact's frontmatter (the load-bearing gate field) is preserved even if Sections 4–6 prose authoring is interrupted.

Purpose: close the BLOCKED runtime gate from `02.2-08-VERIFICATION.md` (cascade-6 timeout bumps preserved; cascade-7 surfaced as 3 additional fixture files needing the parent-hierarchy fix) by adding a sibling `createProjectHierarchy` helper to `suite_test.go` and inserting one call-site line in each of the 3 remaining Layer B fixture files. Tactical iteration #7: add one new function + 3 single-line call additions, re-verify, see if the cascade chain has finally terminated.

Output:
- `test/integration/kind/suite_test.go` with a NEW `func createProjectHierarchy(ctx context.Context, ns string) error` added alongside the existing `applyHierarchy` function (existing function preserved byte-identical; new function created as a SIBLING, not a refactor)
- `test/integration/kind/caps_test.go` with `Expect(createProjectHierarchy(ctx, capsNS)).To(Succeed())` inserted before the existing `Expect(applyYAML(capPlanYAML)).To(Succeed())` line at line ~87
- `test/integration/kind/output_test.go` with `Expect(createProjectHierarchy(ctx, outputNS)).To(Succeed())` inserted before the existing `Expect(applyYAML(outputPlanYAML)).To(Succeed())` line at line ~84
- `test/integration/kind/failure_test.go` with `Expect(createProjectHierarchy(ctx, ns)).To(Succeed())` inserted before the existing `Expect(applyYAML(planYAML)).To(Succeed())` line at line ~112
- `.planning/phases/02.2-.../02.2-09-VERIFICATION.md` mirroring `02.2-08-VERIFICATION.md`'s structural template; `gate_decision: APPROVED` or `BLOCKED`; runtime-observation fields `credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds` populated
- If Task 2 APPROVED: `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-09"]` and ALL 31 `02.2-01-VERIFICATION.md` references redirected to `02.2-09-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
**Wave-dispatch concern for Plan 02.2-02 (executor MUST honor):** Plan 02.2-02 is the closeout plan and currently has `depends_on: ["02.2-01"]` with a `checkpoint:human-verify` Task 1 that reads `02.2-01-VERIFICATION.md`'s `gate_decision` field. Because Plan 02.2-01's verification artifact records `gate_decision: BLOCKED` (its checkpoint was BLOCKED on the original cascade chain; subsequent attempts via Plans 02.2-03/04/05/06/07/08 Task 3 ALL did NOT fire), Plan 02.2-02 MUST NOT be dispatched alongside Plan 02.2-09 in the same execute wave. The structural mitigation: execute-phase's wave-ordering reads `depends_on` from each plan's frontmatter and groups plans whose dependencies are all satisfied into the same wave. Plan 02.2-02's `depends_on` points at 02.2-01 (BLOCKED gate artifact); Plan 02.2-09's `depends_on` points at 02.2-08 (whose Task 1 timeout bumps landed even though its Task 2 runtime gate was BLOCKED on cascade-7, which THIS plan is closing). The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic: failed task → siblings continue, **dependents in later waves never dispatch**. Plan 02.2-02 stays a dependent-in-later-wave of `02.2-01-VERIFICATION.md`'s BLOCKED gate until THIS plan's Task 3 rewrites its `depends_on` to point at `02.2-09-VERIFICATION.md`'s (hopefully APPROVED) gate. If the executor mistakenly dispatches Plan 02.2-02 alongside Plan 02.2-09 in the same wave, Plan 02.2-02's Task 1 checkpoint will halt at the BLOCKED gate AND nothing further fires — but the structural intent is that Plan 02.2-02 dispatches AFTER Plan 02.2-09's Task 3 has landed, in a separate wave 9 (or later).

**Critical anti-patterns from `.continue-here.md` "Critical Anti-Patterns" (do NOT trigger):**
1. Do NOT pitch a comprehensive audit. User has chosen tactical iteration 7 times now; cascade-7 was correctly classified as `harness-bug` (same class as cascade-5, different fixture files — a SCOPE issue, not a new class) by the runtime evidence in `02.2-08-VERIFICATION.md` §Section 5. Tactical iteration 8 (THIS plan) is the right shape.
2. Do NOT touch `charts/tide/values.yaml`. Chart is FIXED contract.
3. Do NOT dispatch Plan 02.2-02 alongside Plan 02.2-09 in the same execute wave when Plan 02.2-02's checkpoint artifact records BLOCKED. (See preceding paragraph.)
4. Do NOT spawn Phase 02.3. Cascade-7 is still within Phase 02.2's scope (closing the kind-test runtime gate); inline gap-closure is the correct pattern.
5. Do NOT modify production source files in this plan. Test-only changes per Option β — the entire dispatch chain in `internal/controller/task_controller.go` and the applyHierarchy helper body in `suite_test.go` stay untouched.
6. Do NOT modify the EXISTING `applyHierarchy` function body in `suite_test.go`. The new `createProjectHierarchy` is a SIBLING function added alongside it. The existing function is preserved verbatim (cascade-5 fix preservation). **NEW for this plan: adding a NEW function alongside is fine — the anti-pattern is REFACTORING the existing function, not adding adjacent code.**
7. Do NOT modify `test/integration/kind/credproxy_test.go`. The cascade-5 refactor (applyHierarchy call) AND the cascade-6 timeout bump (`120*time.Second`) are preserved verbatim. Confirm by `git status --porcelain credproxy_test.go` returning empty after Task 1.
8. Do NOT inherit `02.2-06-VERIFICATION.md` §Section 5's discredited "Wave Ready check" framing. That framing was already corrected in `02.2-07-VERIFICATION.md` §Section 5. This plan's `02.2-09-VERIFICATION.md` §Section 5 should re-state the corrected root cause (the gate is `resolveProject` at `task_controller.go:217`) AND surface the cascade-7 ROOT CAUSE (Plan 02.2-07's helper apply scope was incomplete — only credproxy got the refactor; caps/output/failure remained unrefactored).
</execution_notes>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-08-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/.continue-here.md
@.planning/debug/credproxy-dispatch-guard.md
@CLAUDE.md

<interfaces>
<!-- Key call-site contracts and helper-shape templates the executor needs. Extracted from suite_test.go + caps_test.go + output_test.go + failure_test.go at plan-time. -->
<!-- Executor should use these directly — no codebase exploration needed beyond confirming the existing patterns match. -->

**Cascade chain status (for plan readers' orientation):**
| # | Class | Status | Plan | Fix Shape |
|---|-------|--------|------|-----------|
| 1 | PVC RWX → RWO | CLOSED | 02.2-03 | Chart accessModes templating + test --set override |
| 2 | --metrics-bind-address | CLOSED | 02.2-04 | Binary flag definition in cmd/manager/main.go |
| 3 | --webhook-cert-path | CLOSED | 02.2-05 | Binary flag definition + webhook.NewServer wiring |
| 4 | Makefile timeout 300s→600s | CLOSED | 02.2-06 | Single-line Makefile bump |
| 5 | credproxy fixture incomplete | CLOSED | 02.2-07 | applyHierarchy helper + credproxy refactor |
| 6 | Eventually timeout bumps (spec-flake) | CLOSED | 02.2-08 | Two single-token Eventually timeout replacements |
| 7 | caps/output/failure fixture incomplete (harness-bug; same class as cascade-5, different files) | **THIS PLAN** | 02.2-09 | New createProjectHierarchy helper + 3 call-site additions |

**Existing applyHierarchy helper shape (suite_test.go lines 529–648, preserved verbatim — DO NOT MODIFY):**

```
// applyHierarchy creates the full Namespace+Secret+Project+Milestone+Phase+Plan+Task
// hierarchy required by the task controller's resolveProject dispatch path.
// ... (docstring continues for ~30 lines)
func applyHierarchy(ctx context.Context, ns, planName, taskName string) error {
    // Step 1: Create Namespace + tide-subagent SA (createNamespace calls ensureSubagentSA).
    createNamespace(ns)

    // Step 2: Derive deterministic names for the hierarchy parents.
    projectName := ns + "-project"
    milestoneName := ns + "-milestone"
    phaseName := ns + "-phase"

    // Step 3: Construct the full hierarchy as a single multi-doc YAML, mirroring
    // testdata/three-task-wave.yaml field-for-field (T-02.2-17 mitigation).
    // Secret + Project + Milestone + Phase + Plan + Task (Namespace already created above).
    hierarchyYAML := fmt.Sprintf(`apiVersion: v1
kind: Secret
metadata:
  name: tide-provider-secret
  namespace: %s
type: Opaque
data:
  ANTHROPIC_API_KEY: dGVzdC1hcGkta2V5LXN0dWItc3ViYWdlbnQtZG9lcy1ub3QtdXNlLWl0
---
apiVersion: tideproject.k8s/v1alpha1
kind: Project
metadata:
  name: %s
  namespace: %s
spec:
  targetRepo: "https://github.com/example/%s.git"
  providerSecretRef: "tide-provider-secret"
  budget:
    absoluteCapCents: 100000
---
apiVersion: tideproject.k8s/v1alpha1
kind: Milestone
metadata:
  name: %s
  namespace: %s
spec:
  projectRef: %s
---
apiVersion: tideproject.k8s/v1alpha1
kind: Phase
metadata:
  name: %s
  namespace: %s
spec:
  milestoneRef: %s
---
apiVersion: tideproject.k8s/v1alpha1
kind: Plan
metadata:
  name: %s
  namespace: %s
  labels:
    tideproject.k8s/project: %s
spec:
  phaseRef: %s
---
apiVersion: tideproject.k8s/v1alpha1
kind: Task
metadata:
  name: %s
  namespace: %s
  labels:
    tideproject.k8s/project: %s
    tideproject.k8s/wave-index: "0"
spec:
  planRef: %s
  filesTouched:
    - %s.go
  declaredOutputPaths:
    - %s.go
  dev:
    testMode: success
`,
        // Secret namespace
        ns,
        // Project: name, namespace, targetRepo ns
        projectName, ns, ns,
        // Milestone: name, namespace, projectRef
        milestoneName, ns, projectName,
        // Phase: name, namespace, milestoneRef
        phaseName, ns, milestoneName,
        // Plan: name, namespace, label project, phaseRef
        planName, ns, projectName, phaseName,
        // Task: name, namespace, label project, planRef, filesTouched, declaredOutputPaths
        taskName, ns, projectName, planName, taskName, taskName,
    )

    // Step 4: Apply the hierarchy via the existing applyYAML primitive.
    return applyYAML(hierarchyYAML)
}
```

**New createProjectHierarchy helper shape (Task 1 authors this — SIBLING to applyHierarchy):**

The new helper has the SAME Namespace+Secret+Project+Milestone+Phase shape as `applyHierarchy`'s Steps 1–3 (minus the Plan and Task documents), and the SAME Step 4 applyYAML call. Concretely:

```
// createProjectHierarchy creates the Namespace+Secret+Project+Milestone+Phase
// hierarchy required by the task controller's resolveProject dispatch path,
// WITHOUT a Plan or Task. The calling test supplies its own Plan and Task
// via a follow-up applyYAML call.
//
// Purpose: Plan 02.2-07's applyHierarchy authored the full
// Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy and refactored
// credproxy_test.go to call it. Three other Layer B fixture files
// (caps_test.go, output_test.go, failure_test.go) have their OWN inline
// Plan+Task YAML and only need the parent-Project hierarchy — they cannot
// reuse applyHierarchy because applyHierarchy's Plan+Task would collide
// with their own. This helper provides the parent-only variant
// (T-02.2-24 mitigation: byte-identical Secret+Project+Milestone+Phase
// fields to applyHierarchy; only the Plan+Task documents are omitted).
//
// Signature: (ctx context.Context, ns string) error — mirrors applyHierarchy's
// signature shape but with only the namespace parameter (no planName/taskName
// because the caller supplies those).
//
// Like applyHierarchy, this helper calls createNamespace(ns) (which also calls
// ensureSubagentSA(ns)) as its first step; callers must NOT call
// createNamespace themselves before calling createProjectHierarchy to avoid
// double-create noise.
//
// API group: tideproject.k8s/v1alpha1 (per CLAUDE.md TIDE domain rule —
// never tide.io).
func createProjectHierarchy(ctx context.Context, ns string) error {
    // Step 1: Create Namespace + tide-subagent SA.
    createNamespace(ns)

    // Step 2: Derive deterministic names for the hierarchy parents
    // (same shape as applyHierarchy).
    projectName := ns + "-project"
    milestoneName := ns + "-milestone"
    phaseName := ns + "-phase"

    // Step 3: Construct the parent hierarchy as a single multi-doc YAML,
    // mirroring applyHierarchy's Secret+Project+Milestone+Phase sections
    // field-for-field (T-02.2-24 byte-identical mitigation). Plan and Task
    // are intentionally omitted — the caller supplies those via its own
    // applyYAML call.
    hierarchyYAML := fmt.Sprintf(`apiVersion: v1
kind: Secret
metadata:
  name: tide-provider-secret
  namespace: %s
type: Opaque
data:
  ANTHROPIC_API_KEY: dGVzdC1hcGkta2V5LXN0dWItc3ViYWdlbnQtZG9lcy1ub3QtdXNlLWl0
---
apiVersion: tideproject.k8s/v1alpha1
kind: Project
metadata:
  name: %s
  namespace: %s
spec:
  targetRepo: "https://github.com/example/%s.git"
  providerSecretRef: "tide-provider-secret"
  budget:
    absoluteCapCents: 100000
---
apiVersion: tideproject.k8s/v1alpha1
kind: Milestone
metadata:
  name: %s
  namespace: %s
spec:
  projectRef: %s
---
apiVersion: tideproject.k8s/v1alpha1
kind: Phase
metadata:
  name: %s
  namespace: %s
spec:
  milestoneRef: %s
`,
        // Secret namespace
        ns,
        // Project: name, namespace, targetRepo ns
        projectName, ns, ns,
        // Milestone: name, namespace, projectRef
        milestoneName, ns, projectName,
        // Phase: name, namespace, milestoneRef
        phaseName, ns, milestoneName,
    )

    // Step 4: Apply the hierarchy via the existing applyYAML primitive.
    return applyYAML(hierarchyYAML)
}
```

**Why the signature includes `ctx context.Context` even though the body doesn't directly use it:** Mirrors `applyHierarchy`'s signature shape exactly (T-02.2-20 acceptance carry-forward — the 1-arg-after-ctx signature is the established pattern); `applyYAML` is the underlying primitive and it accepts the package-level `ctx` variable through `applyYAML`'s internal `exec.CommandContext(ctx, ...)` — the helper's `ctx` parameter is for API symmetry with `applyHierarchy`, not for direct use. Future refactors to thread the parameterized `ctx` through `applyYAML` are out of scope for this plan.

**Target file 1 — `test/integration/kind/caps_test.go` (current state, line 87):**

```
59:		createNamespace(capsNS)
60:
61:		capPlanYAML := fmt.Sprintf(`
...:		<inline Plan + Task YAML>
85:		`, capsNS, capsNS)
86:
87:		Expect(applyYAML(capPlanYAML)).To(Succeed())
```

**Insertion point:** between line 85 (closing backtick of the YAML literal) and line 87 (the `applyYAML` call), insert:

```
		Expect(createProjectHierarchy(ctx, capsNS)).To(Succeed())
```

**Optional cleanup (planner's discretion — either choice is acceptable):** the `createNamespace(capsNS)` call at line 59 becomes redundant because `createProjectHierarchy` calls `createNamespace` internally as its Step 1. The executor MAY delete line 59 (`createNamespace(capsNS)`) OR leave it (`createNamespace` is idempotent — it calls `applyYAML` with `kubectl apply` semantics; double-create is a no-op). Plan 02.2-09 accepts both outcomes; the acceptance gate counts `createProjectHierarchy` calls (must equal 1) and `createNamespace(capsNS)` calls (may be 0 or 1, both acceptable; preferred is 0 for cleanliness but 1 is functionally equivalent).

**Target file 2 — `test/integration/kind/output_test.go` (current state, line 84):**

```
58:		createNamespace(outputNS)
59:
60:		outputPlanYAML := fmt.Sprintf(`
...:		<inline Plan + Task YAML>
82:		`, outputNS, outputNS)
83:
84:		Expect(applyYAML(outputPlanYAML)).To(Succeed())
```

**Insertion point:** between line 82 (closing backtick) and line 84 (the `applyYAML` call), insert:

```
		Expect(createProjectHierarchy(ctx, outputNS)).To(Succeed())
```

Same `createNamespace` cleanup discretion as caps_test.go.

**Target file 3 — `test/integration/kind/failure_test.go` (current state, line 112):**

```
56:		ns := failNS
57:		createNamespace(ns)
58:
59:		planYAML := fmt.Sprintf(`
...:		<inline Plan + 3 Task YAML>
110:		`, ns, ns, ns, ns)
111:
112:		Expect(applyYAML(planYAML)).To(Succeed())
```

**Insertion point:** between line 110 (closing backtick) and line 112 (the `applyYAML` call), insert:

```
		Expect(createProjectHierarchy(ctx, ns)).To(Succeed())
```

Note: `ns` is the local-variable assignment from `ns := failNS` at line 56. The call passes `ns` (the local variable), NOT `failNS` directly, to match the convention established by the existing `createNamespace(ns)` call at line 57. Same `createNamespace` cleanup discretion.

**Pre-edit grep counts (LOAD-BEARING — used by Task 1 verify to assert exactly one new call per file):**

At plan-authoring time (verified by planner's plan-time inspection):
- `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` returns **1** (the existing function)
- `grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go` returns **0** (new function not yet added)
- `grep -c 'createProjectHierarchy(' test/integration/kind/caps_test.go` returns **0**
- `grep -c 'createProjectHierarchy(' test/integration/kind/output_test.go` returns **0**
- `grep -c 'createProjectHierarchy(' test/integration/kind/failure_test.go` returns **0**
- `grep -c 'applyHierarchy(' test/integration/kind/credproxy_test.go` returns **≥ 1** (Plan 02.2-07's refactor — preserved)
- `grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go` returns **1** (Plan 02.2-08's cascade-6 bump — preserved)
- `grep -c '240\*time\.Second' test/integration/kind/failure_test.go` returns **1** (Plan 02.2-08's cascade-6 bump — preserved)

**Post-edit grep count expectations (Task 1 acceptance):**
- suite_test.go: `func applyHierarchy` count UNCHANGED (still 1 — existing function preserved); `func createProjectHierarchy` count goes from 0 → 1 (new function added)
- caps_test.go: `createProjectHierarchy(` count goes from 0 → 1 (new call-site inserted)
- output_test.go: `createProjectHierarchy(` count goes from 0 → 1
- failure_test.go: `createProjectHierarchy(` count goes from 0 → 1
- credproxy_test.go: `applyHierarchy(` count UNCHANGED (cascade-5 fix preserved); `120*time.Second` count UNCHANGED (cascade-6 fix preserved)
- failure_test.go: `240*time.Second` count UNCHANGED (cascade-6 fix preserved)

**Plan 02.2-02 rewire scope (Task 3 — HARD-GATED on Task 2 APPROVED):**

At plan-authoring time, `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returns **31** (verified). None of the prior plans' Task 3 fired (Plans 02.2-03/04/05/06/07/08 Task 3 ALL halted on BLOCKED gates). This plan's Task 3 redirects all 31 references in one pass: `02.2-01-VERIFICATION.md` → `02.2-09-VERIFICATION.md`, AND bumps `depends_on: ["02.2-01"]` → `depends_on: ["02.2-09"]`.

**From CLAUDE.md "Failure semantics at wave boundaries" contract:**
- Failed task → siblings in same wave continue
- Dependents in later waves never dispatch
- Plan 02.2-02 reads the gate artifact; if BLOCKED, Plan 02.2-02 stays gated
- Resumption state = indegree map + completed-task set, nothing more

**From `02.2-08-VERIFICATION.md` §Section 5 Root-Cause Summary (the corrected cascade-7 classification — load-bearing for §Section 5 of THIS plan's verification artifact):**
- Cascade-5 gate was `resolveProject` at `task_controller.go:217` (corrected framing established in 02.2-07-VERIFICATION.md, NOT a "parent Wave Ready check")
- Cascade-7 root cause: Plan 02.2-07's `applyHierarchy` helper apply scope was incomplete — only `credproxy_test.go` was refactored; `caps_test.go`, `output_test.go`, `failure_test.go` still use `createNamespace + applyYAML` without a parent Project
- Cascade-7 evidence in Plan 02.2-08 clean run: manager logs show ZERO entries for `hang-task` / `cap-plan` / `caps-test` (same fingerprint as cascade-5 for credproxy); Ginkgo failure at `caps_test.go:100` reports `"A Job should be created for the hang-task"` with `Expected <int>: 0 to be >= <int>: 1`
- Cascade-7 fix is the SAME shape as cascade-5: ensure the parent Project hierarchy exists in the namespace before the test creates its Plan+Task via applyYAML
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add createProjectHierarchy helper to suite_test.go + insert one call-site in each of caps_test.go, output_test.go, failure_test.go</name>
  <files>test/integration/kind/suite_test.go, test/integration/kind/caps_test.go, test/integration/kind/output_test.go, test/integration/kind/failure_test.go</files>

  <read_first>
    - test/integration/kind/suite_test.go lines 529–648 (the existing `applyHierarchy` function — particularly: docstring lines 529–556; function signature line 557 `func applyHierarchy(ctx context.Context, ns, planName, taskName string) error`; Steps 1–4 implementation lines 558–648 — the multi-doc YAML with Secret + Project + Milestone + Phase + Plan + Task; the `fmt.Sprintf` argument layout). This is the structural template the new `createProjectHierarchy` MUST mirror field-for-field for its Secret/Project/Milestone/Phase sections (T-02.2-24 byte-identical mitigation). DO NOT MODIFY this function — confirm `grep -c 'func applyHierarchy' suite_test.go` returns ≥ 1 both before and after Task 1.
    - test/integration/kind/suite_test.go lines 1–50 (imports block + package-level `ctx` variable). Confirm the imports already include `"context"`, `"fmt"`. No new imports are needed by the new helper because it uses the same primitives as `applyHierarchy`.
    - test/integration/kind/caps_test.go lines 40–90 (Describe + BeforeEach + It block + the `By(...)` line 58 + `createNamespace(capsNS)` line 59 + the inline `capPlanYAML` literal lines 61–85 + the `applyYAML(capPlanYAML)` call line 87). The insertion point is between line 85 (closing backtick) and line 87 (the `applyYAML` line).
    - test/integration/kind/output_test.go lines 40–85 (Describe + It block + `createNamespace(outputNS)` line 58 + inline `outputPlanYAML` lines 60–82 + `applyYAML(outputPlanYAML)` line 84). Insertion point is between line 82 (closing backtick) and line 84.
    - test/integration/kind/failure_test.go lines 38–115 (Describe + It block + `ns := failNS` line 56 + `createNamespace(ns)` line 57 + inline `planYAML` lines 59–110 + `applyYAML(planYAML)` line 112). Insertion point is between line 110 (closing backtick) and line 112.
    - test/integration/kind/credproxy_test.go (REGRESSION GUARD — do NOT modify; confirm `git status --porcelain credproxy_test.go` returns empty after Task 1; confirm `grep -c 'applyHierarchy(' credproxy_test.go` returns ≥ 1 — Plan 02.2-07's refactor preserved; confirm `grep -c '120\*time\.Second' credproxy_test.go` returns 1 — Plan 02.2-08's cascade-6 bump preserved)
    - test/integration/kind/testdata/three-task-wave.yaml (REGRESSION GUARD — do NOT modify; reference template for the parent-hierarchy YAML shape)
    - .planning/phases/02.2-.../02.2-08-VERIFICATION.md §Section 5 "Cascade-7 Root Cause" — the gap source explaining WHY the three additional fixture files need the parent-hierarchy fix
    - .planning/phases/02.2-.../02.2-08-PLAN.md Task 1 body — the structural model for THIS plan's Task 1 (regression-guard pattern for `git status --porcelain` on production source paths + Option β contract preservation)
    - .planning/phases/02.2-.../02.2-07-PLAN.md Task 1 body — the structural model for the helper-authoring pattern (suite_test.go gets a new function alongside existing helpers; surrounding fixture files get call-site insertions)
  </read_first>

  <action>
    Four-part task in this single auto-task: (A) add the new `createProjectHierarchy` helper to `suite_test.go` alongside the existing `applyHierarchy`; (B) insert one call-site line in `caps_test.go`; (C) insert one call-site line in `output_test.go`; (D) insert one call-site line in `failure_test.go`.

    **Part A — `test/integration/kind/suite_test.go`: add `createProjectHierarchy` helper alongside existing `applyHierarchy`.**

    Locate the existing `applyHierarchy` function (currently at lines 557–648 per plan-time inspection; identified by the function header line `func applyHierarchy(ctx context.Context, ns, planName, taskName string) error` and the closing `}` ≈ 90 lines below). Add the new `createProjectHierarchy` function IMMEDIATELY BEFORE OR IMMEDIATELY AFTER the existing `applyHierarchy` (executor's choice — adjacency to the existing helper makes the sibling relationship visually obvious in code review).

    The new function MUST have:
    - **Signature exactly:** `func createProjectHierarchy(ctx context.Context, ns string) error`
    - **Docstring:** mirror `applyHierarchy`'s docstring shape (brief Purpose paragraph + Signature note + relationship to `applyHierarchy` note + API group note). Reference T-02.2-24 (byte-identical hierarchy shape) and T-02.2-25 (sibling-not-refactor preservation of cascade-5 fix). State explicitly: "This helper creates the parent hierarchy ONLY (Namespace + Secret + Project + Milestone + Phase). The calling test supplies its own Plan + Task via a follow-up `applyYAML` call."
    - **Body Step 1:** call `createNamespace(ns)` — mirrors `applyHierarchy` Step 1 exactly
    - **Body Step 2:** derive deterministic names — `projectName := ns + "-project"`, `milestoneName := ns + "-milestone"`, `phaseName := ns + "-phase"` — mirrors `applyHierarchy` Step 2 exactly
    - **Body Step 3:** construct the parent hierarchy via `fmt.Sprintf` with a multi-doc YAML containing FOUR documents (Secret, Project, Milestone, Phase) — byte-identical to `applyHierarchy`'s Steps 3 lines for those four documents, MINUS the Plan and Task documents. The Secret name (`tide-provider-secret`), Secret data (`ANTHROPIC_API_KEY: dGVzdC1hcGkta2V5LXN0dWItc3ViYWdlbnQtZG9lcy1ub3QtdXNlLWl0`), Project shape (`targetRepo`, `providerSecretRef: "tide-provider-secret"`, `budget.absoluteCapCents: 100000`), Milestone shape (`projectRef`), and Phase shape (`milestoneRef`) MUST be field-for-field identical to `applyHierarchy`'s emissions. The `fmt.Sprintf` argument order MUST match `applyHierarchy`'s argument order for the matching documents (Secret ns; Project name+ns+targetRepoNS; Milestone name+ns+projectRef; Phase name+ns+milestoneRef).
    - **Body Step 4:** return `applyYAML(hierarchyYAML)` — mirrors `applyHierarchy` Step 4 exactly

    The full body should look approximately like the template in the `<interfaces>` block above (the new createProjectHierarchy helper shape). The executor may copy that template directly and adjust the docstring wording as desired. The `ctx context.Context` parameter is intentional (API symmetry with `applyHierarchy`); the body does not directly use `ctx` because `applyYAML` uses the package-level `ctx` variable internally — same as `applyHierarchy`.

    **CRITICAL — do NOT modify the existing `applyHierarchy` function in any way.** The new function is added as a sibling; the existing function's body, docstring, and signature stay byte-identical to their pre-edit state.

    Pre-edit assertions (run BEFORE editing):
    - `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` must return exactly `1`
    - `grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go` must return exactly `0`

    Post-edit assertions (run AFTER editing):
    - `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` must return exactly `1` (existing function preserved)
    - `grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go` must return exactly `1` (new function added)
    - `git diff test/integration/kind/suite_test.go | grep -cE '^-.*func applyHierarchy'` must return `0` (no `-` line removing the existing function header; if this gate fails, the executor accidentally deleted/displaced `applyHierarchy` — HALT and restore)

    **Part B — `test/integration/kind/caps_test.go`: insert `createProjectHierarchy` call before `applyYAML`.**

    Locate the `applyYAML(capPlanYAML)` call (currently at line 87 — identified by the literal `Expect(applyYAML(capPlanYAML)).To(Succeed())`). Insert immediately BEFORE that line (i.e., between the closing `` ` `` of the `capPlanYAML` literal and the `Expect(applyYAML...)` call) the new line:

    ```
    		Expect(createProjectHierarchy(ctx, capsNS)).To(Succeed())
    ```

    Indentation: match the existing `Expect(applyYAML(capPlanYAML)).To(Succeed())` line's indentation exactly (two tabs).

    Optional cleanup (executor's discretion — either is acceptable to the verify gate): delete line 59 `createNamespace(capsNS)` (now redundant because `createProjectHierarchy` calls it internally). Preferred is to delete for cleanliness; leaving it is functionally equivalent (idempotent double-create). The verify gate accepts both — it asserts `createProjectHierarchy(ctx, capsNS)` count is exactly 1 and does NOT assert on `createNamespace(capsNS)` count.

    **Part C — `test/integration/kind/output_test.go`: insert `createProjectHierarchy` call before `applyYAML`.**

    Locate the `applyYAML(outputPlanYAML)` call (currently at line 84). Insert immediately BEFORE that line:

    ```
    		Expect(createProjectHierarchy(ctx, outputNS)).To(Succeed())
    ```

    Indentation: match the existing `applyYAML` line. Optional cleanup of line 58 `createNamespace(outputNS)` same as Part B.

    **Part D — `test/integration/kind/failure_test.go`: insert `createProjectHierarchy` call before `applyYAML`.**

    Locate the `applyYAML(planYAML)` call (currently at line 112). Insert immediately BEFORE that line:

    ```
    		Expect(createProjectHierarchy(ctx, ns)).To(Succeed())
    ```

    Pass `ns` (the local variable from `ns := failNS` at line 56), NOT `failNS` directly — match the convention established by the existing `createNamespace(ns)` call at line 57. Indentation: match the existing `applyYAML` line. Optional cleanup of line 57 `createNamespace(ns)` same as Part B.

    **Build + vet gates (run AFTER all four Parts complete):**
    - `go build ./test/integration/kind/...` must exit 0 (compilation OK; no duplicate-symbol; no missing-import; no unused-variable)
    - `go vet ./test/integration/kind/...` must exit 0

    **Constraints (Option β contract — test-only changes):**
    - **DO NOT modify** any production source under `internal/`, `api/`, `cmd/`, `pkg/`, `charts/`, `Makefile`, `.github/` — this task is test-fixture-only per Option β
    - **DO NOT modify** the EXISTING `applyHierarchy` function body in `suite_test.go`. The new `createProjectHierarchy` is added as a SIBLING; the existing function is preserved byte-identical. Verify with `git diff suite_test.go | grep -E '^-.*func applyHierarchy'` returning empty (no `-` line deleting the existing header) AND `grep -c 'func applyHierarchy' suite_test.go` returning 1 (existing function still present).
    - **DO NOT modify** `test/integration/kind/credproxy_test.go`. Plan 02.2-07's `applyHierarchy` refactor AND Plan 02.2-08's `120*time.Second` timeout bump are preserved verbatim. Confirm with `git status --porcelain credproxy_test.go` returning empty AND `grep -c 'applyHierarchy(' credproxy_test.go` returning ≥ 1 AND `grep -c '120\*time\.Second' credproxy_test.go` returning 1.
    - **DO NOT modify** `test/integration/kind/wave_test.go` — wave tests use `applyFile(testdata/three-task-wave.yaml)` which has a full hierarchy embedded; no fix needed
    - **DO NOT modify** `test/integration/kind/testdata/` files — fixture YAMLs unchanged
    - **DO NOT modify** the `Makefile` — Plan 02.2-06's `timeout 600s` budget preserved verbatim
    - **DO NOT touch** `charts/tide/values.yaml` per `.continue-here.md` anti-pattern #2
    - **DO NOT modify** Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08, OR 02.2-09 (this plan) — only `02.2-02-PLAN.md` is touched (and only by Task 3, gated on APPROVED)
    - **DO NOT add** new imports to any of the 4 test files — `suite_test.go` already imports `"context"` and `"fmt"`; the three fixture files already import `time`, ginkgo, gomega, and `tideprojectv1alpha1`; the package-level `ctx` variable in `suite_test.go` is in scope for all `_test.go` files in the same package
    - **DO NOT add** new specs (It blocks) to any of the 3 fixture files — the change is scope-limited to ONE call-site insertion per file
    - **DO NOT modify** the existing inline YAML literals (`capPlanYAML`, `outputPlanYAML`, `planYAML`) in any way — the test's own Plan+Task remain authored by the test; the parent hierarchy is added BY THE NEW HELPER

    Commit with message:
    ```
    fix(02.2-09): add createProjectHierarchy helper + apply to caps/output/failure tests (close cascade-7 harness-bug — scope-completion of Plan 02.2-07's applyHierarchy refactor)
    ```

    Body should reference `02.2-08-VERIFICATION.md` §Section 5 (the gap source); note that Option β (new sibling helper) is the user's decision; note that the existing `applyHierarchy` function is preserved byte-identical (cascade-5 fix); note that `credproxy_test.go` is untouched (cascade-5/6 fixes preserved); note the cascade-7 root cause (Plan 02.2-07's apply scope was incomplete — only credproxy got the refactor; caps/output/failure remained unrefactored).
  </action>

  <verify>
    <automated><![CDATA[
      # === Part A — suite_test.go new helper added; existing applyHierarchy preserved ===
      [ "$(grep -c 'func applyHierarchy' test/integration/kind/suite_test.go)" -eq 1 ] && \
      [ "$(grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go)" -eq 1 ] && \
      # The new function signature matches the contract: ctx context.Context, ns string
      grep -E 'func createProjectHierarchy\(ctx context\.Context, ns string\) error' test/integration/kind/suite_test.go && \
      # T-02.2-25 regression guard: existing applyHierarchy header was NOT deleted (no '-' diff line removing it)
      [ "$(git diff test/integration/kind/suite_test.go | grep -cE '^-.*func applyHierarchy')" -eq 0 ] && \
      # T-02.2-24 byte-identical mitigation spot-check: the new helper's hierarchy uses the same Secret/Project/Milestone/Phase fields as applyHierarchy
      grep -c 'tide-provider-secret' test/integration/kind/suite_test.go | grep -qE '^[0-9]+$' && \
      [ "$(grep -c 'tide-provider-secret' test/integration/kind/suite_test.go)" -ge 2 ] && \
      [ "$(grep -c 'absoluteCapCents: 100000' test/integration/kind/suite_test.go)" -ge 2 ] && \
      [ "$(grep -c 'milestoneRef:' test/integration/kind/suite_test.go)" -ge 2 ] && \
      [ "$(grep -c 'projectRef:' test/integration/kind/suite_test.go)" -ge 2 ] && \
      # T-02.2-26 mitigation: the new Phase has ONLY milestoneRef (no planRef/expectedPlanRef/children) — check by counting expectedPlanRef/children occurrences (must be 0)
      [ "$(grep -c 'expectedPlanRef' test/integration/kind/suite_test.go)" -eq 0 ] && \
      [ "$(grep -c 'children:' test/integration/kind/suite_test.go)" -eq 0 ] && \
      # === Part B — caps_test.go call-site inserted ===
      [ "$(grep -c 'createProjectHierarchy(' test/integration/kind/caps_test.go)" -eq 1 ] && \
      grep -E 'Expect\(createProjectHierarchy\(ctx, capsNS\)\)\.To\(Succeed\(\)\)' test/integration/kind/caps_test.go && \
      # Existing applyYAML call preserved
      grep -F 'Expect(applyYAML(capPlanYAML)).To(Succeed())' test/integration/kind/caps_test.go && \
      # === Part C — output_test.go call-site inserted ===
      [ "$(grep -c 'createProjectHierarchy(' test/integration/kind/output_test.go)" -eq 1 ] && \
      grep -E 'Expect\(createProjectHierarchy\(ctx, outputNS\)\)\.To\(Succeed\(\)\)' test/integration/kind/output_test.go && \
      grep -F 'Expect(applyYAML(outputPlanYAML)).To(Succeed())' test/integration/kind/output_test.go && \
      # === Part D — failure_test.go call-site inserted ===
      [ "$(grep -c 'createProjectHierarchy(' test/integration/kind/failure_test.go)" -eq 1 ] && \
      grep -E 'Expect\(createProjectHierarchy\(ctx, ns\)\)\.To\(Succeed\(\)\)' test/integration/kind/failure_test.go && \
      grep -F 'Expect(applyYAML(planYAML)).To(Succeed())' test/integration/kind/failure_test.go && \
      # Cascade-6 timeout bump preserved in failure_test.go (Plan 02.2-08's fix)
      [ "$(grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go)" -eq 1 ] && \
      # === Compile + vet gates ===
      go build ./test/integration/kind/... && \
      go vet ./test/integration/kind/... && \
      # === Regression guards — Option β contract: production source untouched ===
      [ -z "$(git status --porcelain internal/)" ] && \
      [ -z "$(git status --porcelain cmd/)" ] && \
      [ -z "$(git status --porcelain api/)" ] && \
      [ -z "$(git status --porcelain pkg/)" ] && \
      [ -z "$(git status --porcelain charts/)" ] && \
      [ -z "$(git status --porcelain Makefile)" ] && \
      # === Regression guards — credproxy_test.go untouched (cascade-5/6 fixes preserved) ===
      [ -z "$(git status --porcelain test/integration/kind/credproxy_test.go)" ] && \
      [ "$(grep -c 'applyHierarchy(' test/integration/kind/credproxy_test.go)" -ge 1 ] && \
      [ "$(grep -c '120\*time\.Second' test/integration/kind/credproxy_test.go)" -eq 1 ] && \
      # === Regression guards — wave_test.go and testdata untouched ===
      [ -z "$(git status --porcelain test/integration/kind/wave_test.go)" ] && \
      [ -z "$(git status --porcelain test/integration/kind/testdata/)" ] && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Part A — new helper added:** `grep -c 'func createProjectHierarchy' test/integration/kind/suite_test.go` returns exactly `1` (function added)
    - **Part A — existing helper preserved:** `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` returns exactly `1` (existing function still present) AND `git diff test/integration/kind/suite_test.go | grep -cE '^-.*func applyHierarchy'` returns `0` (no `-` diff line deleting the existing header — T-02.2-25 mitigation)
    - **Part A — new helper signature matches contract:** `grep -E 'func createProjectHierarchy\(ctx context\.Context, ns string\) error' test/integration/kind/suite_test.go` matches
    - **Part A — T-02.2-24 byte-identical hierarchy fields:** `grep -c 'tide-provider-secret' test/integration/kind/suite_test.go` returns ≥ 2 (one occurrence in applyHierarchy, one in createProjectHierarchy); `grep -c 'absoluteCapCents: 100000' test/integration/kind/suite_test.go` returns ≥ 2; `grep -c 'milestoneRef:' test/integration/kind/suite_test.go` returns ≥ 2; `grep -c 'projectRef:' test/integration/kind/suite_test.go` returns ≥ 2
    - **Part A — T-02.2-26 forward-reference guard:** `grep -c 'expectedPlanRef' test/integration/kind/suite_test.go` returns `0` AND `grep -c 'children:' test/integration/kind/suite_test.go` returns `0` (the new Phase has only `milestoneRef`, no forward references to Plan)
    - **Part B — caps_test.go call inserted:** `grep -c 'createProjectHierarchy(' test/integration/kind/caps_test.go` returns exactly `1` AND `grep -E 'Expect\(createProjectHierarchy\(ctx, capsNS\)\)\.To\(Succeed\(\)\)' test/integration/kind/caps_test.go` matches AND `grep -F 'Expect(applyYAML(capPlanYAML)).To(Succeed())' test/integration/kind/caps_test.go` still matches (existing call preserved)
    - **Part C — output_test.go call inserted:** `grep -c 'createProjectHierarchy(' test/integration/kind/output_test.go` returns exactly `1` AND `grep -E 'Expect\(createProjectHierarchy\(ctx, outputNS\)\)\.To\(Succeed\(\)\)' test/integration/kind/output_test.go` matches AND `grep -F 'Expect(applyYAML(outputPlanYAML)).To(Succeed())' test/integration/kind/output_test.go` still matches
    - **Part D — failure_test.go call inserted:** `grep -c 'createProjectHierarchy(' test/integration/kind/failure_test.go` returns exactly `1` AND `grep -E 'Expect\(createProjectHierarchy\(ctx, ns\)\)\.To\(Succeed\(\)\)' test/integration/kind/failure_test.go` matches AND `grep -F 'Expect(applyYAML(planYAML)).To(Succeed())' test/integration/kind/failure_test.go` still matches
    - **Cascade-6 bump preserved in failure_test.go:** `grep -cE '240\*time\.Second|4\*time\.Minute' test/integration/kind/failure_test.go` returns exactly `1` (Plan 02.2-08's fix preserved)
    - **Compile gate:** `go build ./test/integration/kind/...` exits 0
    - **Vet gate:** `go vet ./test/integration/kind/...` exits 0
    - **Option β contract preserved (production source untouched):** all of `git status --porcelain internal/`, `git status --porcelain cmd/`, `git status --porcelain api/`, `git status --porcelain pkg/`, `git status --porcelain charts/`, `git status --porcelain Makefile` return empty
    - **Cascade-5/6 fixes preserved (credproxy_test.go untouched):** `git status --porcelain test/integration/kind/credproxy_test.go` returns empty AND `grep -c 'applyHierarchy(' credproxy_test.go` ≥ 1 AND `grep -c '120\*time\.Second' credproxy_test.go` returns 1
    - **wave_test.go and testdata untouched:** `git status --porcelain test/integration/kind/wave_test.go` returns empty AND `git status --porcelain test/integration/kind/testdata/` returns empty
  </acceptance_criteria>

  <done>
    `test/integration/kind/suite_test.go` has a NEW `func createProjectHierarchy(ctx context.Context, ns string) error` added alongside the EXISTING `func applyHierarchy(ctx context.Context, ns, planName, taskName string) error`. The new helper creates Namespace+Secret+Project+Milestone+Phase (no Plan, no Task — those are supplied by the caller); the Secret/Project/Milestone/Phase fields are byte-identical to `applyHierarchy`'s emissions (T-02.2-24 mitigation). The existing `applyHierarchy` function body is preserved byte-identical (T-02.2-25 mitigation — cascade-5 fix). `test/integration/kind/caps_test.go`, `test/integration/kind/output_test.go`, and `test/integration/kind/failure_test.go` each have exactly one new call-site line: `Expect(createProjectHierarchy(ctx, <ns>)).To(Succeed())` inserted immediately BEFORE the existing `applyYAML(...)` call. `test/integration/kind/credproxy_test.go` is UNTOUCHED — cascade-5 refactor and cascade-6 timeout bump preserved verbatim. `test/integration/kind/wave_test.go` and `test/integration/kind/testdata/` are UNTOUCHED. `go build ./test/integration/kind/...` and `go vet ./test/integration/kind/...` both exit 0. No production source modified — `internal/`, `api/`, `cmd/`, `pkg/`, `charts/`, `Makefile` all untouched (Option β contract preserved). Committed with the `fix(02.2-09): add createProjectHierarchy helper + apply to caps/output/failure tests (close cascade-7 harness-bug — scope-completion of Plan 02.2-07's applyHierarchy refactor)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-09-VERIFICATION.md (clean run + rerun + gate decision + 4 runtime-observation fields including new caps_observed_seconds and output_observed_seconds)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-08-VERIFICATION.md (the structural template this artifact mirrors — frontmatter shape including the new T-02.2-21 observation fields; Sections 1–6 layout; per-spec outcome tables; root-cause analysis prose; threat-model-mitigations-applied list; "What This Artifact Did Not Touch" closing section. THIS plan's verification adds two MORE observation fields (`caps_observed_seconds`, `output_observed_seconds`) on top of the existing two from Plan 02.2-08.)
    - .planning/phases/02.2-.../02.2-08-PLAN.md Task 2 body (the structural model — runtime verification reproducer pattern with clean run + idempotent rerun + diagnostic snapshot + per-spec outcome table + gate decision; THIS plan's Task 2 substitutes the cascade-7 harness-bug verification for cascade-6's spec-flake verification)
    - .planning/phases/02.2-.../02.2-08-SUMMARY.md (Plan 02.2-08's summary — for cascade-6 closure evidence and cascade-7 surfacing analysis)
    - test/integration/kind/suite_test.go (post-Task-1 state — verify the new `createProjectHierarchy` is present AND existing `applyHierarchy` is preserved before this task runs)
    - test/integration/kind/caps_test.go (post-Task-1 state — verify the `createProjectHierarchy(ctx, capsNS)` call is present before this task runs)
    - test/integration/kind/output_test.go (post-Task-1 state — verify the call is present)
    - test/integration/kind/failure_test.go (post-Task-1 state — verify the call is present AND the 240s cascade-6 bump is preserved)
    - test/integration/kind/credproxy_test.go (regression guard — verify untouched; cascade-5/6 fixes preserved)
    - Makefile (verify `timeout 600s` budget from Plan 02.2-06 is still in place — `grep -c 'timeout 600s' Makefile` ≥ 1)
    - .planning/debug/credproxy-dispatch-guard.md (historical context for the cascade-5 corrected root cause; §Section 5 of THIS plan's verification artifact must preserve the corrected framing AND surface the cascade-7 root cause analysis as scope-completion of cascade-5)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-08-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md` using the same structural template PLUS two MORE runtime-observation fields (`caps_observed_seconds`, `output_observed_seconds`) on top of the existing two from Plan 02.2-08 (`credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`).

    **Recovery-lesson PRE-STEP (codified protocol from `.continue-here.md` Plan 02.2-05 lesson, applied in 02.2-06/07/08):** BEFORE running the clean reproducer, author `02.2-09-VERIFICATION.md` with PRELIMINARY frontmatter — the load-bearing fields (`phase: 02.2`, `plan: 09`, `task: 2`, `artifact: verification`, `captured_on: <today>`, `captured_by_host: <host>`, `captured_by_branch: <branch>`, placeholder `gate_decision: PENDING` line, AND placeholders `credproxy_pod_wait_observed_seconds: -1`, `ac3_phase_wait_observed_seconds: -1`, `caps_observed_seconds: -1`, `output_observed_seconds: -1`) — so that if mid-run executor API errors or context window pressure interrupt the long-running runtime verification, the artifact's gate field still exists with a recoverable PENDING state. Then update `gate_decision: PENDING` → APPROVED or BLOCKED and the four observation fields to real values after Sub-step C completes.

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit (set -o pipefail makes the bash captured exit reflect make's real exit, not tee's):
    set -o pipefail
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-09-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state for the artifact:**
    ```bash
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-09-clean-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-09-clean-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-09-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=20m 2>&1 | tee /tmp/02.2-09-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-09-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-09-clean-pvc-phase.txt || true
    # Cascade-7-specific evidence — assert ABSENCE of "no project found" for each of the 4 Layer B namespaces
    grep -E 'no project found.*(credproxy|caps|output|failure)' /tmp/02.2-09-clean-manager-logs.txt 2>&1 | tee /tmp/02.2-09-clean-cascade7-regression-check.txt || true
    # Per-spec timings extraction
    grep -E 'A Pod should be created|Wait for β|beta-fail.*Failed|hang-task|exceed-output-task|seconds elapsed' /tmp/02.2-09-clean-run.log 2>&1 | tee /tmp/02.2-09-clean-spec-timings.txt || true
    # Per-fixture namespace state — assert parent Project exists in each
    for ns in caps-test output-test failure-test credproxy-test; do
      echo "=== Namespace: $ns ===" | tee -a /tmp/02.2-09-clean-namespaces.txt
      kubectl --context kind-tide-test get project,milestone,phase,plan,task -n $ns 2>&1 | tee -a /tmp/02.2-09-clean-namespaces.txt || true
    done
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block — APPROVED case shows 7 Pass / 0 Skip / 0 Fail; BLOCKED case shows partial outcome with cascade-8). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-08-VERIFICATION.md` §Section 1.

    **Four runtime-observation fields (LOAD-BEARING — T-02.2-21 carry-forward + NEW for caps/output):** Extract observed elapsed times for the four key specs:
    - `credproxy_pod_wait_observed_seconds`: time-to-Pod-creation for the credproxy HARN-03 pod-wait spec (Plan 02.2-08 recorded 120 — confounded by concurrent caps/output/failure `resolveProject` failures; Plan 02.2-09 re-measures without that pressure)
    - `ac3_phase_wait_observed_seconds`: time-to-Failed-phase for the AC3 beta-fail spec (Plan 02.2-08 recorded -1 because the spec did not run; Plan 02.2-09 should measure for the first time post-cascade-7-fix)
    - `caps_observed_seconds`: total elapsed time for the AC5 hang-task spec (NEW field — Plan 02.2-08 recorded the spec FAILED at 75s due to cascade-7; Plan 02.2-09 should measure successful completion)
    - `output_observed_seconds`: total elapsed time for the AC5 exceed-output-task spec (NEW field — Plan 02.2-08 did NOT RUN this spec due to panic; Plan 02.2-09 should measure for the first time post-cascade-7-fix)

    Record into the artifact's frontmatter:
    ```
    credproxy_pod_wait_observed_seconds: <N>
    ac3_phase_wait_observed_seconds: <M>
    caps_observed_seconds: <C>
    output_observed_seconds: <O>
    ```

    If any value cannot be precisely extracted from Ginkgo output, use the spec's TOTAL duration from the Ginkgo summary as a conservative upper bound and add a clarifying note in §Section 1 ("observed seconds = total spec duration; the dominant cost within the spec is the assertion's Eventually wait").

    **T-02.2-21 carry-forward marginal-flag check:** If `credproxy_pod_wait_observed_seconds ≥ 90` OR `ac3_phase_wait_observed_seconds ≥ 180`, the artifact MUST contain the literal token `dispatch latency marginal` in §Section 1 AND the closing prose. The flag does NOT block APPROVED but signals that the new budgets are being approached even after cascade-7 closure.

    **Cascade-7 closure evidence (NEW for THIS plan — LOAD-BEARING):** The artifact's §Section 1 manager-log evidence MUST explicitly assert:
    - Zero `no project found in namespace caps-test` entries (cascade-7 fix for caps closed)
    - Zero `no project found in namespace output-test` entries (cascade-7 fix for output closed)
    - Zero `no project found in namespace failure-test` entries (cascade-7 fix for failure closed)
    - Zero `no project found in namespace credproxy-test` entries (cascade-5 fix preserved)

    If ANY of the four namespaces shows the `no project found` error, cascade-7 is NOT closed for that fixture — surface in §Section 5 as a regression or a Task-1 incomplete fix.

    **APPROVED case clean-run state:** Pod `READY 1/1 STATUS Running RESTARTS ≤ 1`; manager logs show "starting manager"; NO `flag provided but not defined` of any kind; NO `no project found in namespace <ns>` for any of caps-test/output-test/failure-test/credproxy-test; manager logs show successful dispatch chains for hang-task (Job-create + Pod-run + activeDeadlineSeconds kill + Task-Failed) AND exceed-output-task (Job-create + Pod-run + harness validate-fail + Task-Failed) AND beta-fail (Job-create + Pod-run + exit-1 + Task-Failed) AND alpha-fail (Job-create + Pod-run + exit-0 + Task-Succeeded) AND credproxy-task (Job-create + Pod-run with credproxy sidecar Ready); Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed`; the four test namespaces each contain the full hierarchy (Project + Milestone + Phase + their respective Plan + Task(s)).

    **BLOCKED case A — cascade-8 surfaces as `budget-vs-actual`:** Manager Pod state Running; cascade-5/6/7 fixes preserved; but the WALL exceeds 600s now that all 4 fixtures run to completion. Cascade-8 classified as `budget-vs-actual` on the outer budget — fix shape: deferred Layer A/Layer B split into separately-timed Makefile targets (Option D from Plan 02.2-08-VERIFICATION.md §Section 5).

    **BLOCKED case B — cascade-8 surfaces as `spec-flake` in wave/failure-graph dynamics:** A spec that previously hadn't run end-to-end with the full hierarchy (e.g., the AC3 γ-never-dispatches Consistently assertion, or one of the wave-internal-ordering assertions) flakes for the first time. Cascade-8 classified as `spec-flake` — fix shape: targeted `Eventually`/`Consistently` budget bump or assertion logic fix in the failing spec.

    **BLOCKED case C — cascade-8 surfaces as `kind-infra`:** image-pull regression, etcd race, networking quirk — surfaces only now that all 4 fixtures run concurrently and stress the kind cluster's resource limits. Cascade-8 classified as `kind-infra` — fix shape: suite_test.go BeforeSuite/AfterSuite robustness OR kind config tuning.

    **BLOCKED case D — cascade-8 surfaces as `harness-bug` regression:** the new `createProjectHierarchy` diverges from `applyHierarchy` (T-02.2-24 mitigation failed) OR the `applyHierarchy` was inadvertently modified (T-02.2-25 mitigation failed). Cascade-8 classified as `harness-bug` regression — fix shape: byte-diff between the two helpers' Secret/Project/Milestone/Phase sections; restore byte-identical fields. This would be a SHOWSTOPPER — surface in §Section 5 with the diff.

    **BLOCKED case E — cascade-8 surfaces as `flag-mismatch` or other prior-cascade regression:** if EITHER `flag provided but not defined` OR `no project found` for credproxy-task appears in manager logs, a PRIOR cascade has regressed. Surface in §Section 5 with full diagnostic citing which prior Plan's fix regressed.

    **BLOCKED case F — controller-side production bug surfaces under 4-fixture load:** reconcile-loop contention, work-queue saturation, leader-election flake, or a real ResourceVersion conflict cascade that the prior 7 iterations didn't trigger because fewer fixtures ran simultaneously. Cascade-8 classified as `other` — fix shape: route to a future-phase production-code fix in `internal/controller/`; Plan 02.2-10 may need to be a production-code plan rather than a test-fixture plan.

    **Sub-step B — Idempotent Re-run:**
    ```bash
    set -o pipefail
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-09-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    After rerun: capture the same diagnostic snapshot (`/tmp/02.2-09-rerun-*.txt`) mirroring Sub-step A's snapshot list with `-rerun-` substituted for `-clean-`. Re-extract the four observation fields from the rerun Ginkgo output; if any value differs materially between clean and rerun, note the variance in §Section 2 commentary.

    **Sub-step C — Author the artifact:**
    Write `02.2-09-VERIFICATION.md` mirroring `02.2-08-VERIFICATION.md`'s structural template. Required frontmatter (after Sub-step C, update PRELIMINARY `gate_decision: PENDING` to APPROVED or BLOCKED AND populate the four observation fields):
    ```
    ---
    phase: 02.2
    plan: 09
    task: 2
    artifact: verification
    captured_on: <YYYY-MM-DD>
    captured_by_host: <host>
    captured_by_branch: <git branch / worktree name>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    credproxy_pod_wait_observed_seconds: <int>
    ac3_phase_wait_observed_seconds: <int>
    caps_observed_seconds: <int>
    output_observed_seconds: <int>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary including cascade-8 type if BLOCKED, or 'cascade chain terminated' if APPROVED>
    ---
    ```

    Required sections (mirror `02.2-08-VERIFICATION.md` headers verbatim):
    - **Section 1 — Clean Start Run** (reproducer + per-spec outcome table + wall-time + exit + Pod-state evidence + manager-log evidence as the load-bearing diagnostic for cascade-7 closure (zero `no project found` for any of the 4 namespaces) + four runtime-observation fields with marginal-flag check)
    - **Section 2 — Idempotent Re-run** (reproducer + per-spec outcome table + wall-time + exit + rerun-specific evidence; re-extract the four observation fields for variance commentary)
    - **Section 3 — Before/After Comparison** (table comparing Plan 02.2-08 BLOCKED outcome (cascade-7 harness-bug — 3 fixtures failed at resolveProject gate) vs Plan 02.2-09 outcomes on clean + rerun; particularly: did the createProjectHierarchy fix close the cascade-7 gate, what is the new credproxy pod-wait observation post-controller-pressure-removal, what are the new caps and output observations, and what is the NEW failure mode if BLOCKED again — what cascade-8 type per the BLOCKED cases A–F above)
    - **Section 4 — Wall-time Budget Check** (same table shape as `02.2-08-VERIFICATION.md` §4; both runs must show go test ≤ 600s for APPROVED; ADDITIONAL row required per T-02.2-14 mitigation carry-forward: flag `budget headroom marginal` if observed wall ≥ 500s even on APPROVED; SEPARATE row required per T-02.2-21: per-spec dispatch-latency observation against the new 120s/240s/90s/120s budgets — if observed ≥ 90s/180s for credproxy/AC3 respectively, flag `dispatch latency marginal`)
    - **Section 5 — Gate Decision** (the APPROVED-requires-ALL table with 5 conditions on BOTH runs + Required Next Step + Root-Cause Summary if BLOCKED + Fix landscape if BLOCKED. **CRITICAL FRAMING PRESERVATION**: §Section 5 MUST re-state the corrected root cause from `02.2-07-VERIFICATION.md` §Section 5 — the dispatch gate is `resolveProject` at `task_controller.go:217`, NOT a "parent Wave Ready check"; AND MUST surface the cascade-7 root cause analysis: Plan 02.2-07's `applyHierarchy` apply scope was incomplete — only credproxy got the refactor; caps/output/failure remained unrefactored until THIS plan. DO NOT inherit `02.2-06-VERIFICATION.md`'s discredited "Wave Ready" framing.)
    - **Section 6 — Threat-Model Mitigations Applied** (T-02.2-24 / T-02.2-25 / T-02.2-26 / T-02.2-27 each with evidence + carry-forward T-02.2-01 through T-02.2-23 noted as preserved)
    - **"What This Artifact Did Not Touch"** closing section
    - **"Closing Prose"** with one-paragraph summary including the corrected root cause framing AND (if APPROVED) the chain-terminator note OR (if BLOCKED) the cascade-8 classification + Plan 02.2-10 fix shape

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/` (Task 1's test-file edits are already committed before this task runs; the runtime reproducer only READS). Task 3 edits Plan 02.2-02 (a planning artifact, not source) — and only fires if `gate_decision: APPROVED`. Document this explicitly in the "What This Artifact Did Not Touch" closing section.

    **APPROVED gate (5-of-5 must hold on BOTH clean run AND idempotent rerun):**
    1. **Wall-time ≤ 600s:** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` (Plan 02.2-06 budget preserved); no `timeout 600s` exit-124 line in either captured log
    2. **Manager Pod state:** `tide-controller-manager` Pod `STATUS=Running` with `RESTARTS ≤ 1` (Plan 02.2-05's cascade-3 fix preserved)
    3. **Manager log clean (TWO sub-conditions — THIS is the cascade-7 closure indicator):** (a) zero `flag provided but not defined` errors of any kind (Plans 02.2-04/05 preserved); (b) zero `no project found in namespace <ns>` for any of caps-test/output-test/failure-test/credproxy-test (Plan 02.2-07's cascade-5 fix preserved for credproxy + THIS plan's cascade-7 fix for the other three)
    4. **PVC preserved:** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO` (Plan 02.2-03's cascade-1 fix preserved)
    5. **7/7 Layer B Ginkgo specs PASS:** Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed`; per-spec table records each spec as PASS on both clean AND rerun — **THIS is achievable for the first time in the cascade chain because all 7 prior cascades are now closed**

    **BLOCKED gate fires if ANY of the 5 conditions fail on either run.** The artifact's §Section 5 Root-Cause Summary documents WHICH gate failed, with raw log evidence, and §Fix landscape enumerates the next-iteration options based on cascade-8 classification (cases A–F above).

    Task 3 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring; Plans 02.2-03/04/05/06/07/08 Task 3 ALL DID NOT FIRE).

    **If gate_decision = APPROVED:** This is the CHAIN TERMINATOR — Plan 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave. The closing prose should:
    - Note the cascade chain has terminated successfully after 7 tactical iterations (02.2-01 source-shape + 02.2-03 chart PVC + 02.2-04 metrics flag + 02.2-05 webhook-cert-path flag + 02.2-06 Makefile timeout + 02.2-07 credproxy fixture + 02.2-08 Eventually timeout bumps + 02.2-09 createProjectHierarchy)
    - Note T-02.2-14 marginal-headroom check (clean/rerun wall vs 500s) AND T-02.2-21 marginal-dispatch-latency check (observed pod-wait/phase-wait vs 90s/180s); if either marginal flag fired, the closing prose flags preemptive escalation as prudent
    - State that Plan 02.2-02 (closeout) is unblocked once Task 3 wires its checkpoint to read this artifact, and that Phase 02.2 finally closes after Plan 02.2-02 dispatches in a follow-up wave

    **Section 5 framing — load-bearing corrected root cause + cascade-7 root cause:** The artifact's §Section 5 prose MUST:
    1. Preserve `02.2-07-VERIFICATION.md` §Section 5's corrected framing: the dispatch gate is `resolveProject` at `task_controller.go:217`, NOT a "parent Wave Ready check". Do NOT re-inherit the wrong framing from `02.2-06-VERIFICATION.md`.
    2. Surface the cascade-7 root cause analysis: Plan 02.2-07's `applyHierarchy` apply scope was incomplete (only credproxy refactored); caps/output/failure remained on the old `createNamespace + applyYAML` pattern; the parent-Project hierarchy was missing in those three namespaces; `resolveProject` failed for hang-task/exceed-output-task/alpha-fail/beta-fail/gamma-fail with `no project found in namespace <ns>` errors.
    3. State explicitly: cascade-7 is the SAME CLASS as cascade-5 (harness-bug — fixture-incomplete), differing only in WHICH fixture files were affected. The fix shape (createProjectHierarchy helper) is the parent-only variant of applyHierarchy; both helpers now coexist as siblings; future tests requiring a parent-only hierarchy use createProjectHierarchy, future tests requiring a full Plan+Task hierarchy use applyHierarchy.
    4. If T-02.2-21 marginal-flag fires (observed credproxy pod-wait ≥ 90s OR observed AC3 phase-wait ≥ 180s), the closing prose flags this as a CANDIDATE for a future-phase production-code fix (debounce concurrent status updates in the ResourceVersion conflict race).

    Commit with one of:
    ```
    docs(02.2-09): runtime verification — APPROVED gate (cascade-7 harness-bug closed; 7/7 Layer B specs PASS clean + rerun within 600s; CHAIN TERMINATOR v2)
    ```
    OR
    ```
    docs(02.2-09): runtime verification — BLOCKED gate (createProjectHierarchy applied to 3 fixtures; cascade-8 surfaced: <cascade-8-type-and-summary>)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists at the correct path
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md ] && \
      # Frontmatter required fields
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 09' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^task: 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^artifact: verification' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -eq 1 ] && \
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # All four runtime-observation fields populated (two carry-forward + two new)
      grep -E '^credproxy_pod_wait_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      grep -E '^ac3_phase_wait_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      grep -E '^caps_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      grep -E '^output_observed_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # Required structural sections exist (six numbered sections)
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 4' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times (both runs)
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      grep -E '^rerun_exit: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # Threat-model mitigations applied section exists with all four NEW threats referenced
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-24' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-25' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-26' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-27' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      # Cascade classification present (per T-02.2-27 acceptance) — one of the six classes must appear, or "cascade chain terminated"
      grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other|cascade chain terminated' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # Section 5 corrected-root-cause framing preserved — MUST cite resolveProject
      grep -F 'resolveProject' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # Cascade-7 root cause surfaced — MUST mention scope-completion OR createProjectHierarchy OR applyHierarchy apply scope
      grep -E 'createProjectHierarchy|scope.completion|apply scope|cascade-7' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # Manager-log evidence captured
      [ "$(grep -c 'starting manager\|flag provided but not defined\|tide-controller-manager\|task cleanup\|dispatch\|no project found' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      # Pod-state evidence captured
      [ "$(grep -c 'CrashLoopBackOff\|Running\|RESTARTS\|kubectl.*get pod' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      # 600s budget referenced
      [ "$(grep -c '600s\|≤ 600\|<= 600' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-21 marginal-flag check rendered
      grep -E 'dispatch latency marginal|dispatch latency.*not marginal|observed.*<.*90|observed.*<.*180' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # Carry-forward threat register acknowledgement: T-02.2-01 through T-02.2-23 noted as preserved
      grep -E 'T-02.2-(0[1-9]|1[0-9]|2[0-3])' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md && \
      # No production source modified by this task
      ! (git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-09-VERIFICATION.md' | grep -v '^$' >/dev/null) && \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-09-VERIFICATION.md ]` returns true
    - **Frontmatter required fields:** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 09' <artifact>` returns 1 AND `grep -c '^task: 2' <artifact>` returns 1 AND `grep -c '^artifact: verification' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches (PENDING is not a final state)
    - **All four runtime-observation fields populated:** `grep -E '^credproxy_pod_wait_observed_seconds: -?[0-9]+$' <artifact>` matches AND `grep -E '^ac3_phase_wait_observed_seconds: -?[0-9]+$' <artifact>` matches AND `grep -E '^caps_observed_seconds: -?[0-9]+$' <artifact>` matches AND `grep -E '^output_observed_seconds: -?[0-9]+$' <artifact>` matches
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: -?[0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: -?[0-9]+$' <artifact>` matches
    - **All six structural sections present:** each `grep -c '^## Section [1-6]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all four new threats referenced:** `grep -c 'T-02.2-24' <artifact>` ≥ 1 AND `grep -c 'T-02.2-25' <artifact>` ≥ 1 AND `grep -c 'T-02.2-26' <artifact>` ≥ 1 AND `grep -c 'T-02.2-27' <artifact>` ≥ 1
    - **Cascade-8 classification (or chain-terminated note) present:** `grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other|cascade chain terminated' <artifact>` matches
    - **Section 5 corrected-root-cause framing preserved (load-bearing):** `grep -F 'resolveProject' <artifact>` matches
    - **Cascade-7 root cause surfaced:** `grep -E 'createProjectHierarchy|scope.completion|apply scope|cascade-7' <artifact>` matches
    - **Manager-log evidence captured (load-bearing for gate decision):** the artifact includes a verbatim excerpt of `kubectl logs deploy/tide-controller-manager` showing absence of `no project found` for any of the 4 test namespaces (APPROVED case) OR evidence of cascade-8 (BLOCKED case)
    - **Pod-state evidence captured:** the artifact includes verbatim `kubectl get pod` output showing STATUS=Running RESTARTS ≤ 1 (APPROVED) or different failure shape (BLOCKED)
    - **600s budget referenced:** `grep -c '600s\|≤ 600\|<= 600' <artifact>` ≥ 1
    - **T-02.2-21 marginal-flag check rendered:** the artifact contains the literal `dispatch latency marginal` token (if observed values approach budgets) OR an explicit "not marginal" note
    - **Carry-forward threat register acknowledged:** `grep -E 'T-02.2-(0[1-9]|1[0-9]|2[0-3])' <artifact>` matches at least once (T-02.2-01 through T-02.2-23 carry-forward)
    - **Behavior gate (APPROVED case — ALL 5 conditions on BOTH runs):**
      - (a) Clean run captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (b) Rerun captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (c) `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600`; no `timeout 600s` exit-124 in either log
      - (d) Manager logs show "starting manager" AND zero `flag provided but not defined` AND zero `no project found in namespace <ns>` for any of caps-test/output-test/failure-test/credproxy-test (cascade-7 closure indicator — load-bearing)
      - (e) `kubectl get pod` shows STATUS=Running, restart count ≤ 1
      - (f) PVC tide-projects Bound RWO
      - (g) T-02.2-14 marginal-headroom check: if observed wall ≥ 500s, the artifact contains `budget headroom marginal` token
      - (h) T-02.2-21 marginal-dispatch-latency check: if observed pod-wait ≥ 90 OR phase-wait ≥ 180, the artifact contains `dispatch latency marginal` token
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, §Section 5 Root-Cause Summary MUST exist with: (a) confirmation that Plan 02.2-09 Task 1's helper + 3 call sites ARE in effect (`grep -c 'func createProjectHierarchy' suite_test.go` ≥ 1; one call-site per fixture); (b) confirmation that prior cascade fixes preserved (`grep -c 'func applyHierarchy' suite_test.go` ≥ 1; credproxy_test.go untouched); (c) which cascade-8 surfaced (one of T-02.2-27 enum); (d) why this was or was NOT predictable; (e) fix-landscape options A–F enumerated
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows only the new `02.2-09-VERIFICATION.md` (Task 1's edits pre-committed; Task 3 not yet fired)
  </acceptance_criteria>

  <done>
    `02.2-09-VERIFICATION.md` exists with the structural template mirroring `02.2-08-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, AND ALL FOUR runtime-observation fields (`credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds`), AND `gate_decision: APPROVED` or `gate_decision: BLOCKED` (PENDING is not a final state). Sections 1–6 captured with per-spec outcome tables, Pod-state evidence, manager-log evidence (load-bearing for cascade-7 closure — particularly the absence of `no project found in namespace <ns>` for any of the 4 Layer B test namespaces), four-namespace state listings, before/after comparison vs `02.2-08-VERIFICATION.md`, wall-time budget check (with T-02.2-14 marginal-headroom carry-forward flag if observed wall ≥ 500s AND T-02.2-21 marginal-dispatch-latency flag if observed pod-wait ≥ 90s OR observed phase-wait ≥ 180s), and threat-model mitigations (T-02.2-24/25/26/27 + carry-forward T-02.2-01 through T-02.2-23). §Section 5 preserves the corrected root cause framing (`resolveProject` is the gate, NOT a Wave Ready check) AND surfaces the cascade-7 root cause (Plan 02.2-07's `applyHierarchy` apply scope was incomplete; THIS plan's `createProjectHierarchy` is the scope-completion fix). If APPROVED: all 5 conditions hold on BOTH clean run AND rerun (both exit 0 with 7/7 specs PASS; both ≤ 600s; zero `flag provided but not defined` AND zero `no project found` for any of 4 namespaces; Pod Running ≤ 1 restart; PVC Bound RWO; manager log shows successful dispatch for all 4 fixtures' Tasks). Task 3 fires to wire Plan 02.2-02's checkpoint — this is the CHAIN TERMINATOR v2. If BLOCKED: Task 3 does NOT fire; §Section 5 classifies cascade-8 per T-02.2-27 enum AND names the specific spec(s) AND proposes Plan 02.2-10's fix shape from Options A–F. Committed with a `docs(02.2-09): runtime verification — APPROVED gate (cascade-7 harness-bug closed; 7/7 Layer B specs PASS clean + rerun within 600s; CHAIN TERMINATOR v2)` or `docs(02.2-09): runtime verification — BLOCKED gate (createProjectHierarchy applied to 3 fixtures; cascade-8 surfaced: <type>)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-09-VERIFICATION.md + bump depends_on (HARD-GATED on Task 2 APPROVED)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file; frontmatter — particularly the `depends_on: ["02.2-01"]` line, the `key_links` block, the `must_haves.truths` block; `<context>` block; Task 1 body — `<read_first>`, `<what-built>`, `<how-to-verify>`, `<verify>` automated block, `<acceptance_criteria>`, `<done>` — all of which currently reference `02.2-01-VERIFICATION.md` because Plans 02.2-03/04/05/06/07/08 Task 3 ALL DID NOT FIRE)
    - .planning/phases/02.2-.../02.2-09-VERIFICATION.md (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the HARD precondition for this Task 3 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - .planning/phases/02.2-.../02.2-08-PLAN.md Task 3 body (the structural model for this task's edits; mirror its surgical-edit pattern with `02.2-08` → `02.2-09` everywhere)
    - CLAUDE.md "Failure semantics at wave boundaries" contract — the load-bearing logic that this task fires ONLY on APPROVED; on BLOCKED, this task halts and Plan 02.2-02 stays gated on its current `02.2-01-VERIFICATION.md` wiring
  </read_first>

  <action>
    **Gate precondition (HARD GATE):** Task 3 fires ONLY IF Task 2's `02.2-09-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED` (or PENDING, which is not a final state), HALT Task 3 — Plan 02.2-02 stays unmodified (it will continue to reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, AND 02.2-08 Task 3 ALL did NOT fire); the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md (Plan 02.2-10 takes the next iteration).

    Verify the precondition by running:
    ```bash
    grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md
    ```
    If this returns 0 (no APPROVED match), HALT Task 3 and surface the BLOCKED gate. Exit with a clear message: "Task 3 halted: 02.2-09-VERIFICATION.md gate_decision is not APPROVED — routing back to planner for next iteration (cascade-8 or whatever surfaced)."

    If APPROVED (returns exactly 1), make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-09-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering. There are 31 references to `02.2-01-VERIFICATION.md` in `02.2-02-PLAN.md` (verified at plan-time by `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returning exactly 31); all 31 are redirected to `02.2-09-VERIFICATION.md` in one pass.

    **NOTE on recovery state:** Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, AND 02.2-08 Task 3 each tried to do the same surgical edit (each pointing 02.2-02-PLAN.md at its own VERIFICATION.md), but none fired because each Task 2 gate was BLOCKED on the cascade-of-the-day. As a consequence, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today. This task's edits land the long-pending rewire, redirecting to `02.2-09-VERIFICATION.md`. If this is the chain terminator (APPROVED gate), this is also the FIRST time the rewire actually lands in Phase 02.2's entire history.

    **Pre-edit count assertion (BEFORE any edits):** Run `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` and confirm the result is exactly `31`. If it is NOT 31, HALT — the rewire scope assumed at plan-time has drifted and the task needs planner re-evaluation.

    **Edit A — frontmatter `depends_on`:** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-09"]`. Single one-line change.

    **Edit B — frontmatter `must_haves.truths` block:** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-09-VERIFICATION.md`. "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → "Closeout fires ONLY when 02.2-09-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit C — frontmatter `key_links` block:** Update the entry that references `02.2-01-VERIFICATION.md` to reference `02.2-09-VERIFICATION.md`.

    **Edit D — `<context>` block:** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-09-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>`:** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-09-VERIFICATION.md (the gate artifact produced by Plan 09 Task 2 — Phase 02.2-09 closed the BLOCKED runtime gate from 02.2-08-VERIFICATION.md by adding a sibling createProjectHierarchy helper to suite_test.go and applying it to caps_test.go, output_test.go, and failure_test.go. All 7 prior cascades closed: cascade-1 PVC RWX→RWO; cascade-2 metrics-bind-address; cascade-3 webhook-cert-path; cascade-4 test-budget 300s→600s; cascade-5 credproxy-fixture-incomplete; cascade-6 Eventually-timeout-too-tight; cascade-7 caps/output/failure-fixture-incomplete (scope-completion of cascade-5). With cascade-7 closed, all 7 Layer B Ginkgo specs PASS on clean + idempotent rerun within the 600s outer budget — Phase 02.2 cascade chain terminated)`.

    **Edit F — Task 1 `<what-built>`:** Update the prose to reference Plan 02.2-09's deliverable (new createProjectHierarchy helper + 3 call-site additions + runtime re-verification + chain termination) in addition to Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08's deliverables. Append: "Plan 02.2-09 then added a sibling `createProjectHierarchy(ctx, ns) error` helper to `suite_test.go` (alongside the existing `applyHierarchy` from Plan 02.2-07; both helpers coexist as siblings; the new helper creates Namespace+Secret+Project+Milestone+Phase ONLY — Plan and Task are supplied by the caller's `applyYAML`). The new helper was applied to `caps_test.go`, `output_test.go`, and `failure_test.go` via a single `Expect(createProjectHierarchy(ctx, <ns>)).To(Succeed())` call inserted before each test's existing `applyYAML` call. The helper's Secret/Project/Milestone/Phase fields are byte-identical to `applyHierarchy`'s emissions (T-02.2-24 mitigation). Zero production code changes (Option β contract preserved); zero modifications to the existing `applyHierarchy` function body (cascade-5 fix preserved verbatim); zero modifications to `credproxy_test.go` (cascade-5/6 fixes preserved); the 600s outer Makefile budget from Plan 02.2-06 preserved; the cascade-6 Eventually timeout bumps from Plan 02.2-08 preserved. Cascade-7 is the same CLASS as cascade-5 (harness-bug — fixture-incomplete), differing only in WHICH fixture files were affected; the fix is a SCOPE-COMPLETION fix, not a new class. With cascade-7 closed, all 7 Layer B Ginkgo specs PASS on clean + idempotent rerun within the 600s outer budget, and Phase 02.2's cascade chain terminates after 7 tactical iterations."

    **Edit G — Task 1 `<how-to-verify>`:** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-09-VERIFICATION.md`.

    **Edit H — Task 1 `<verify>` automated block:** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-09-VERIFICATION.md`.

    **Edit I — Task 1 `<acceptance_criteria>`:** Update the references to `02.2-01-VERIFICATION.md` → `02.2-09-VERIFICATION.md`.

    **Edit J — Task 1 `<done>`:** Update the references to `02.2-01-VERIFICATION.md` → `02.2-09-VERIFICATION.md` if any are present.

    **Post-edit count assertion (AFTER all edits):** Run `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `0`. Run `grep -c '02.2-09-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `31`. If either count is not as expected, surface the diff and HALT before commit.

    **(Edits to ROADMAP/STATE content NOT performed by this task — Plan 02.2-02 reads live ROADMAP/STATE at execution time via its own `<read_first>` and `<action>` blocks. Plan 02.2-02 is responsible for its own ROADMAP/STATE shape decisions at execution time.)**

    **Constraints:**
    - Do NOT edit Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08, or 02.2-09 (this plan) — only `02.2-02-PLAN.md` is touched.
    - Do NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `Makefile`, `.github/`, `test/`. This task is documentation-only.
    - Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — those are owned by Plan 02.2-02's tasks and by the orchestrator.
    - The diff against `02.2-02-PLAN.md` must be minimal — only the 31 references to `02.2-01-VERIFICATION.md` are changed to `02.2-09-VERIFICATION.md`, plus the one `depends_on` value change. No new tasks added, no existing tasks removed, no structural reorganization.

    Commit with message:
    ```
    docs(02.2-09): wire Plan 02.2-02 checkpoint to 02.2-09-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-09; 31 references redirected; CHAIN TERMINATOR v2 — first APPROVED rewire in Phase 02.2's history)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Gate precondition: 02.2-09-VERIFICATION.md gate_decision is APPROVED
      [ "$(grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-VERIFICATION.md)" -eq 1 ] && \
      # Edit A: depends_on bumped to 02.2-09
      [ "$(grep -cE 'depends_on: \[.{0,5}02.2-09.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 1 ] && \
      # Old depends_on values 02.2-01 through 02.2-08 all purged from the depends_on field
      [ "$(grep -cE '^depends_on: \[.{0,5}02.2-0[1345678].{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-09-VERIFICATION.md is now the gate reference — count exactly 31
      [ "$(grep -c '02.2-09-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 31 ] && \
      # 02.2-01-VERIFICATION.md references purged from 02.2-02-PLAN.md
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Prior VERIFICATION refs (03/04/05/06/07/08) purged
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-04-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-05-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-06-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-07-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-08-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Task 1 checkpoint grep gate uses the new path
      grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-09-VERIFICATION.md" .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Confirm no other plans were modified by this task
      ! (git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-02-PLAN.md' | grep -v '^$' >/dev/null) && \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Gate precondition met (HARD GATE):** `grep -cE '^gate_decision: APPROVED$' 02.2-09-VERIFICATION.md` returns exactly 1
    - **depends_on bumped (Edit A):** `grep -cE 'depends_on: \[.{0,5}02.2-09.{0,5}\]' 02.2-02-PLAN.md` returns exactly 1 AND `grep -cE '^depends_on: \[.{0,5}02.2-0[1345678].{0,5}\]' 02.2-02-PLAN.md` returns 0 (old values 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08 all purged)
    - **New gate-artifact reference count (Edits B–J):** `grep -c '02.2-09-VERIFICATION.md' 02.2-02-PLAN.md` returns exactly 31 (one-for-one redirection from the 31 plan-time `02.2-01-VERIFICATION.md` references)
    - **Old gate-artifact references purged:** `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-03-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-04-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-05-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-06-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-07-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-08-VERIFICATION.md' 02.2-02-PLAN.md` returns 0
    - **Task 1 `<verify>` automated grep targets the new path:** the regex `grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-09-VERIFICATION.md"` matches inside `02.2-02-PLAN.md`
    - **No other plans or source modified (regression guard):** `git status --porcelain` shows ONLY `02.2-02-PLAN.md` modified
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has been surgically edited to redirect its checkpoint from `02.2-01-VERIFICATION.md` to `02.2-09-VERIFICATION.md`, with `depends_on` bumped from `["02.2-01"]` to `["02.2-09"]`. All 31 prior `02.2-01-VERIFICATION.md` references are now `02.2-09-VERIFICATION.md` (one-for-one, count exactly 31). No stale `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, `02.2-05-VERIFICATION.md`, `02.2-06-VERIFICATION.md`, `02.2-07-VERIFICATION.md`, OR `02.2-08-VERIFICATION.md` references exist. No other plans or production source are modified. **This is the CHAIN TERMINATOR v2 rewire — the FIRST APPROVED rewire in Phase 02.2's entire history (Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3, 02.2-07 Task 3, 02.2-08 Task 3 all halted on BLOCKED gates; this Task 3 is the first to fire).** Committed with the `docs(02.2-09): wire Plan 02.2-02 checkpoint to 02.2-09-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-09; 31 references redirected; CHAIN TERMINATOR v2 — first APPROVED rewire in Phase 02.2's history)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/integration/kind/suite_test.go` (new createProjectHierarchy helper) → kind cluster's TaskReconciler reconcile path | The new helper creates Namespace+Secret+Project+Milestone+Phase in the test namespace. The boundary is between "the test's `applyYAML` creates Plan+Task" and "the controller's `resolveProject` finds the deterministic `<ns>-project` Project CRD in the namespace". After the helper applies, `resolveProject` at `task_controller.go:217` returns the Project CRD on the fast-path label-miss + ProjectList fallback path. |
| `applyHierarchy` (existing) vs `createProjectHierarchy` (new) in suite_test.go | Both helpers coexist as siblings; both produce hierarchies with byte-identical Secret/Project/Milestone/Phase shapes (T-02.2-24 mitigation); the difference is `applyHierarchy` ALSO produces Plan+Task while `createProjectHierarchy` STOPS at Phase. Trust boundary: ensure the existing helper is preserved verbatim (T-02.2-25 mitigation — adding-not-refactoring). |
| Task 1 (helper + 3 call-site additions) → Task 2 (runtime verification) | Task 2 runs after Task 1's commit lands; the runtime reproducer exercises all 4 Layer B fixtures end-to-end with parent-Project hierarchies in their namespaces. |
| Task 2 (gate artifact + 4 observation fields) → Task 3 (Plan 02.2-02 wiring) | Task 3 reads Task 2's `gate_decision:` frontmatter. APPROVED → Task 3 fires; BLOCKED → Task 3 halts and the BLOCKED failure routes to a planner re-evaluation. |
| Plan 02.2-09 Task 3 edits → Plan 02.2-02 checkpoint target | Surgical edit only — `depends_on` and 31 verification-artifact references in `02.2-02-PLAN.md` are bumped; no other planning artifacts touched. |
| `02.2-09-VERIFICATION.md` runtime-observation fields → future-phase production-code fix decision | If observed pod-wait ≥ 90s OR phase-wait ≥ 180s even after cascade-7 closure (controller-pressure removed), the `dispatch latency marginal` flag surfaces — signaling a future-phase production-code fix for the ResourceVersion conflict race rather than continuing to bump test timeouts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-24 | Tampering (helper-shape divergence) | The new `createProjectHierarchy` helper's Secret+Project+Milestone+Phase YAML could subtly diverge from `applyHierarchy`'s Step 3 emissions (different field set, different API version, different Secret name, different budget, different Phase/Milestone shape). Tests using `createProjectHierarchy` would then create a structurally-different Project than what `resolveProject` expects in the credproxy namespace, OR `resolveProject` would succeed but the Plan/Task created by the test's `applyYAML` would point at a Phase shape that doesn't validate. | mitigate | The new helper's Secret+Project+Milestone+Phase YAML MUST be byte-identical (modulo namespace/name parameterization) to the corresponding section of `applyHierarchy`'s Step 3. **Acceptance gate:** Task 1 verify asserts `grep -c 'tide-provider-secret' suite_test.go` ≥ 2, `grep -c 'absoluteCapCents: 100000' suite_test.go` ≥ 2, `grep -c 'milestoneRef:' suite_test.go` ≥ 2, `grep -c 'projectRef:' suite_test.go` ≥ 2 (proving both helpers emit the same field shapes); `grep -c 'expectedPlanRef' suite_test.go` and `grep -c 'children:' suite_test.go` both return 0 (proving no forward references to Plan); `go build` and `go vet` exit 0 (proving compilation OK); the runtime reproducer in Task 2 verifies the helper actually closes the cascade-7 gate (zero `no project found` errors for any of the 4 namespaces). |
| T-02.2-25 | Tampering (cascade-5 regression — applyHierarchy displacement) | Adding a new helper to `suite_test.go` could accidentally damage the existing `applyHierarchy` (e.g., displacing its function body during the edit, breaking its imports, introducing a duplicate-function-name compile error, modifying its body when only an addition was intended). The cascade-5 fix (Plan 02.2-07) would regress. | mitigate | Task 1 verify block asserts (a) `applyHierarchy` still exists verbatim AFTER the edit (`grep -c 'func applyHierarchy' suite_test.go` ≥ 1, AND `git diff suite_test.go | grep -cE '^-.*func applyHierarchy'` returns 0 — proving the existing function header line was NOT deleted); (b) `test/integration/kind/credproxy_test.go` is UNTOUCHED (`git status --porcelain credproxy_test.go` returns empty, AND `grep -c 'applyHierarchy(' credproxy_test.go` ≥ 1, AND `grep -c '120\*time\.Second' credproxy_test.go` returns 1 — both Plan 02.2-07 refactor + Plan 02.2-08 timeout bump preserved); (c) `go build ./test/integration/kind/...` exits 0 (confirming no duplicate-symbol or import collision). The new helper is a SIBLING function, added alongside `applyHierarchy`, NOT a refactor of it. **Acceptance gate:** the four preservation grep counts above all hold post-edit. |
| T-02.2-26 | Tampering (forward-reference field shape) | The existing tests have specific Plan and Task names in their inline YAML (caps: `cap-plan`/`hang-task`; output: `output-plan`/`exceed-output-task`; failure: `fail-plan`/`alpha-fail`/`beta-fail`/`gamma-fail`). If `createProjectHierarchy`'s Phase has a forward reference (e.g., an `expectedPlanRef` field) pointing to a specific Plan name, the existing tests' Plan names might not match and the controller might reject the Plan-Phase link. | mitigate | Confirm by reading `applyHierarchy`'s Phase YAML at suite_test.go lines 597–603 — the Phase has ONLY `milestoneRef` (pointing UP at its Milestone parent), NOT any `planRef`/`expectedPlanRef`/`children` field (the parent-to-child link comes from the Plan's `spec.phaseRef`, not from the Phase). The new helper's Phase MUST also have ONLY `milestoneRef`. **Acceptance gate:** `grep -c 'expectedPlanRef' suite_test.go` returns 0 AND `grep -c 'children:' suite_test.go` returns 0 (no forward references introduced); `grep -c 'milestoneRef:' suite_test.go` ≥ 2 (both helpers' Phase emit the upward-pointing reference); runtime reproducer in Task 2 verifies the controller accepts the Plan-Phase link for each test's specific Plan name. |
| T-02.2-27 | Denial of Service (unforeseeable cascade-8) | Cascade-8 (if any) is genuinely unknown — but cascade-7 closes the structurally-complete harness-bug surface area (all 4 Layer B fixture files will use the parent-hierarchy fix pattern after Task 1). Possible cascade-8 shapes: (a) `budget-vs-actual` on the outer 600s wall-time (now that all 4 fixtures consume full per-spec budgets); (b) `spec-flake` in a wave/failure-graph dynamic that hadn't run end-to-end before; (c) `kind-infra` (resource pressure from 4 concurrent fixtures); (d) `harness-bug` regression (createProjectHierarchy diverges from applyHierarchy); (e) `flag-mismatch` regression of cascade-2/3; (f) a real production-code bug surfacing under 4-fixture load (reconcile contention, work-queue saturation). | accept | Accepted with the requirement that if Task 2's gate is BLOCKED, `02.2-09-VERIFICATION.md` §"Section 5 Root-Cause Summary" MUST classify the cascade type using one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other` AND identify which specific spec(s) failed AND propose Plan 02.2-10's fix shape from the start — so the next iteration is not a diagnostic round-trip. **Probability cascade-8 surfaces:** LOW — the prior 7 iterations have exhausted the harness-bug surface; remaining failure modes are structurally distinct from the chain so far. The most likely cascade-8 candidate (if any) is `budget-vs-actual` on the outer wall-time — fix shape: deferred Layer A/Layer B split. A `harness-bug` regression would indicate T-02.2-24 or T-02.2-25 mitigation failed — investigate the byte-diff between helpers. **Acceptance gate:** if Task 2 records `gate_decision: BLOCKED`, the artifact contains one of the six classification tokens AND §Section 5 names the specific failure evidence. If APPROVED, the artifact references "cascade chain terminated" in a closing note. |

</threat_model>

<verification>
After all three tasks complete (or after Task 2 if its gate is BLOCKED):

**Source gates (Task 1):**
- `test/integration/kind/suite_test.go` has NEW `func createProjectHierarchy(ctx context.Context, ns string) error` alongside EXISTING `func applyHierarchy(ctx context.Context, ns, planName, taskName string) error` (T-02.2-24 — byte-identical Secret/Project/Milestone/Phase fields)
- Existing `applyHierarchy` function body byte-identical to pre-edit state (T-02.2-25 — no `-` diff line removing the function header)
- `test/integration/kind/caps_test.go`, `output_test.go`, `failure_test.go` each have exactly one `createProjectHierarchy(ctx, <ns>)` call inserted BEFORE the existing `applyYAML(...)` call
- `test/integration/kind/credproxy_test.go` UNTOUCHED (cascade-5/6 fixes preserved — `grep -c 'applyHierarchy(' credproxy_test.go` ≥ 1; `grep -c '120\*time\.Second' credproxy_test.go` returns 1)
- `test/integration/kind/wave_test.go` and `test/integration/kind/testdata/` UNTOUCHED
- `go build ./test/integration/kind/...` and `go vet ./test/integration/kind/...` both exit 0
- No production source modified — `internal/`, `cmd/`, `api/`, `pkg/`, `charts/`, `Makefile` all untouched (Option β contract preserved)
- T-02.2-26 mitigation: `grep -c 'expectedPlanRef' suite_test.go` returns 0; `grep -c 'children:' suite_test.go` returns 0 (no forward references to Plan)

**Runtime gates (Task 2):**
- `02.2-09-VERIFICATION.md` exists with all required frontmatter fields populated (`phase: 02.2`, `plan: 09`, `task: 2`, `artifact: verification`, exit codes, wall seconds, spec counts, AND all FOUR runtime-observation fields: `credproxy_pod_wait_observed_seconds`, `ac3_phase_wait_observed_seconds`, `caps_observed_seconds`, `output_observed_seconds`)
- `gate_decision` is one of APPROVED or BLOCKED (PENDING not a final state)
- Sections 1–6 mirror `02.2-08-VERIFICATION.md`'s structural template
- §Section 5 prose preserves the corrected root cause framing (`resolveProject` is the gate, NOT a "Wave Ready check")
- §Section 5 prose surfaces the cascade-7 root cause (Plan 02.2-07's `applyHierarchy` apply scope was incomplete; THIS plan's `createProjectHierarchy` is the scope-completion fix)
- Manager-log evidence captured: zero `no project found in namespace <ns>` for any of caps-test/output-test/failure-test/credproxy-test (APPROVED case — load-bearing cascade-7 closure indicator); evidence of cascade-8 failure shape with classification (BLOCKED case)
- Pod-state evidence captured: STATUS=Running with restart count ≤ 1 (APPROVED) or different failure (BLOCKED)
- Wall-time evidence: both runs ≤ 600s for APPROVED
- T-02.2-14 marginal-headroom carry-forward flag present if observed wall ≥ 500s
- T-02.2-21 marginal-dispatch-latency flag present if observed pod-wait ≥ 90s OR phase-wait ≥ 180s
- All four new threats (T-02.2-24/25/26/27) have evidence in §6 + carry-forward T-02.2-01 through T-02.2-23
- Cascade-8 classification present per T-02.2-27 (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) — even if APPROVED, the artifact notes "cascade chain terminated"

**Wiring gates (Task 3 — HARD-GATED on Task 2 APPROVED):**
- `02.2-02-PLAN.md` has `depends_on: ["02.2-09"]` and ALL 31 references updated from `02.2-01-VERIFICATION.md` to `02.2-09-VERIFICATION.md` (exact count one-for-one)
- Stale references to `02.2-01/03/04/05/06/07/08-VERIFICATION.md` all purged from `02.2-02-PLAN.md`
- No other plans modified
- This is the CHAIN TERMINATOR v2 rewire — the FIRST APPROVED Task 3 in Phase 02.2's entire history

**Structural gates (T-02.2-24/25/26/27 mitigation):**
- T-02.2-24 byte-identical helper-shape preservation: both helpers' Secret/Project/Milestone/Phase fields match field-for-field; runtime reproducer confirms cascade-7 closure
- T-02.2-25 sibling-not-refactor preservation: existing `applyHierarchy` body byte-identical to pre-edit state; credproxy_test.go untouched
- T-02.2-26 no-forward-reference guarantee: new Phase has only `milestoneRef`, no `expectedPlanRef`/`children`/`planRef` fields
- T-02.2-27 cascade-8 classification token present (even on APPROVED, "cascade chain terminated" referenced)
- Chart `charts/tide/values.yaml` NOT touched
- Production source NOT touched (Option β contract)
- Existing `applyHierarchy` function body NOT modified (cascade-5 fix preserved)
- `test/integration/kind/credproxy_test.go` NOT touched (cascade-5/6 fixes preserved)
- Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08 NOT modified
- Plan 02.2-02 modified ONLY in Task 3 (gated on APPROVED)
- `.planning/STATE.md` and `.planning/ROADMAP.md` NOT touched (orchestrator owns)
</verification>

<success_criteria>
- Task 1 helper + call sites landed: `test/integration/kind/suite_test.go` has NEW `createProjectHierarchy` alongside EXISTING `applyHierarchy` (byte-identical hierarchy fields per T-02.2-24; existing function preserved verbatim per T-02.2-25; no forward references per T-02.2-26); `caps_test.go`, `output_test.go`, `failure_test.go` each have one `createProjectHierarchy(ctx, <ns>)` call inserted before their existing `applyYAML(...)` calls; `go build` + `go vet` both exit 0; no production source modified (Option β contract); `credproxy_test.go` untouched (cascade-5/6 fixes preserved); wave_test.go and testdata untouched
- Task 2 runtime verification artifact authored at `.planning/phases/02.2-.../02.2-09-VERIFICATION.md` with `gate_decision: APPROVED` or `gate_decision: BLOCKED` and all required structural sections, including ALL FOUR runtime-observation fields (`credproxy_pod_wait_observed_seconds`/`ac3_phase_wait_observed_seconds`/`caps_observed_seconds`/`output_observed_seconds`) + load-bearing wall-time + Pod-state + manager-log evidence (particularly the absence of `no project found in namespace <ns>` for any of the 4 Layer B test namespaces — cascade-7 closure indicator) + four-namespace state listings + cascade-7 root cause analysis + cascade-8 classification (if BLOCKED); §Section 5 preserves the corrected root cause from `02.2-07-VERIFICATION.md` (`resolveProject` is the gate, NOT a Wave Ready check) AND surfaces the cascade-7 root cause (Plan 02.2-07's apply scope incomplete; THIS plan's createProjectHierarchy is the scope-completion fix)
- Task 3 (HARD-GATED on Task 2 APPROVED) wired Plan 02.2-02's checkpoint to `02.2-09-VERIFICATION.md` with `depends_on: ["02.2-09"]` and all 31 stale `02.2-01-VERIFICATION.md` references one-for-one redirected — this is the CHAIN TERMINATOR v2 rewire (first APPROVED Task 3 in Phase 02.2's history; Plans 02.2-03/04/05/06/07/08 Task 3 all halted on BLOCKED)
- If Task 2 returns BLOCKED: Task 3 does NOT fire; Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring); the orchestrator routes to a planner re-evaluation for Plan 02.2-10 (or equivalent) per the wave-failure-semantics contract; the cascade-8 classification in the verification artifact directs the planner to the right fix shape (Option A budget-vs-actual via Layer A/B split; Option B spec-flake targeted fix; Option C kind-infra fix; Option D harness-bug regression fix; Option E flag-mismatch regression fix; Option F production-code fix for controller-side load issue)
- T-02.2-24/25/26/27 each have concrete evidence in the verification artifact and the source-edit acceptance gates
- Tactical scope honored: ONLY `suite_test.go` (one new function added) + `caps_test.go` + `output_test.go` + `failure_test.go` modified (Option β — sibling helper + 3 call-site additions); production dispatch path in `internal/controller/task_controller.go` NOT touched; existing `applyHierarchy` function body NOT modified (cascade-5 fix preserved); `credproxy_test.go` NOT touched (cascade-5/6 fixes preserved); `charts/tide/values.yaml` NOT touched; Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, 02.2-07, 02.2-08 NOT modified; Plan 02.2-06's Makefile `timeout 600s` preserved; Plan 02.2-08's Eventually timeout bumps preserved
- **Chain terminator v2 note:** if APPROVED, Phase 02.2's cascade chain has terminated after 7 tactical iterations (cascade-1 through cascade-7 all closed). Plan 02.2-02 dispatches in a follow-up wave to close Phase 02.2.
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-09-SUMMARY.md` documenting:
- The createProjectHierarchy helper authored (Task 1): function signature `func createProjectHierarchy(ctx context.Context, ns string) error` added alongside existing `applyHierarchy` in `suite_test.go`; byte-identical Secret+Project+Milestone+Phase fields to `applyHierarchy` (T-02.2-24 mitigation); the new call-site insertions in `caps_test.go` (1 call), `output_test.go` (1 call), `failure_test.go` (1 call); the non-modification of `applyHierarchy`'s body (T-02.2-25 mitigation); the non-modification of `credproxy_test.go` (cascade-5/6 fixes preserved)
- The runtime verification outcome (Task 2): gate decision (APPROVED or BLOCKED), per-spec outcomes, wall times, exit codes, Pod-state evidence, manager-log evidence (particularly the absence of `no project found in namespace <ns>` for any of the 4 Layer B namespaces — proving cascade-7 closure), all four runtime-observation fields (`credproxy_pod_wait_observed_seconds`/`ac3_phase_wait_observed_seconds`/`caps_observed_seconds`/`output_observed_seconds`) and whether the marginal-dispatch-latency flag fired, cascade-8 classification per T-02.2-27 (or "cascade chain terminated" if APPROVED)
- The Plan 02.2-02 wiring update (Task 3, if APPROVED): which references were updated (count exactly 31), depends_on bump from 02.2-01 → 02.2-09 — this is the CHAIN TERMINATOR v2 rewire (first APPROVED Task 3 in Phase 02.2's history)
- Any inconsistencies surfaced (marginal-headroom T-02.2-14 carry-forward flag; marginal-dispatch-latency T-02.2-21 carry-forward flag; cascade-8 surfacing on BLOCKED with its classification)
- The Phase 02.2 close-out chain progression: cascade-1 PVC + cascade-2 metrics flag + cascade-3 webhook-cert-path flag + cascade-4 Makefile timeout + cascade-5 credproxy fixture + cascade-6 Eventually timeouts + cascade-7 caps/output/failure fixtures → runtime verified once (APPROVED → chain terminator v2 → Plan 02.2-02 unblocked → Phase 02.2 closes) or routed back (BLOCKED → Plan 02.2-10) for next iteration
- Threat-model mitigations applied (T-02.2-24 / T-02.2-25 / T-02.2-26 / T-02.2-27) with concrete evidence + carry-forward T-02.2-01 through T-02.2-23
- A note on the structural shift: cascades 1–3 were flag-mismatch (predictable, terminated at 5/5 chart args defined); cascade-4 was budget-vs-actual (single-line Makefile fix); cascade-5 was harness-bug — fixture-incomplete for credproxy; cascade-6 was spec-flake (Eventually timeout bumps); cascade-7 was harness-bug — fixture-incomplete for caps/output/failure (SAME CLASS as cascade-5, different files — scope-completion fix); cascade-8 (if any) shape genuinely unknown per T-02.2-27. **Phase 02.2's pattern: tactical iteration with runtime verification at each step is the right shape; comprehensive audit would not have caught any of the 7 cascades because each surfaced at runtime AFTER the prior was closed.**
- An explicit framing-correction preservation note: `02.2-06-VERIFICATION.md` §Section 5 hypothesized the cascade-5 gate as a "parent Wave Ready check" — that hypothesis was WRONG. The debug investigation at `.planning/debug/credproxy-dispatch-guard.md` corrected the framing to the `resolveProject` lookup. THIS plan's `02.2-09-VERIFICATION.md` §Section 5 MUST preserve the corrected framing (the gate is `resolveProject` at `task_controller.go:217`, NOT a Wave Ready check) AND surface the cascade-7 root cause (Plan 02.2-07's apply scope was incomplete).
- **Future-phase target surfaced:** the underlying ResourceVersion conflict race between `PlanReconciler.stampTaskLabels` and `TaskReconciler` status updates is a real production-code concern (carry-forward from Plan 02.2-08). The cascades-5/6/7 fixes (test-fixture refactors + helper + timeout bumps) are TACTICAL ACCOMMODATIONS, not durable fixes for the production race. If T-02.2-21 marginal-dispatch-latency flag fires on this run even after cascade-7 closure (observed pod-wait ≥ 90s OR phase-wait ≥ 180s), the SUMMARY should explicitly recommend a future-phase production-code fix (debounce concurrent status updates, optimistic-concurrency-aware retry budgets) for `internal/controller/task_controller.go` and/or `internal/controller/plan_controller.go`.
</output>
</content>
