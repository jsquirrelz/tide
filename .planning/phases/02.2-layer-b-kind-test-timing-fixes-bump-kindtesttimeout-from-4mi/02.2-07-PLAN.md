---
phase: "02.2"
plan: "07"
type: execute
wave: 6
gap_closure: true
depends_on: ["02.2-06"]
files_modified:
  - test/integration/kind/suite_test.go
  - test/integration/kind/credproxy_test.go
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
requirements: []
tags: [gap-closure, debug, test-fixture, harness-bug, runtime-verification, kind-integration, phase-02.2, tactical, cascade-5, full-hierarchy-fixture]

threat_model_summary:
  - "T-02.2-17 (mitigate): the new `applyHierarchy` helper might subtly diverge from `testdata/three-task-wave.yaml`'s working hierarchy (e.g., wrong owner-ref direction, missing required Spec field, wrong API version), causing the credproxy test to fail for a DIFFERENT reason than the production dispatch path. Mitigation: the helper's CRD shapes MUST be structurally identical to `testdata/three-task-wave.yaml`'s — verify with field-by-field equivalence at plan-time and runtime. **Acceptance gate:** the helper produces CRDs whose API versions, kinds, and required Spec fields match `testdata/three-task-wave.yaml` verbatim (Project.spec.targetRepo + Project.spec.providerSecretRef + Project.spec.budget.absoluteCapCents; Milestone.spec.projectRef; Phase.spec.milestoneRef; Plan.spec.phaseRef + Plan.metadata.labels.\"tideproject.k8s/project\"; Task.spec.planRef + Task.spec.filesTouched + Task.spec.declaredOutputPaths + Task.metadata.labels.\"tideproject.k8s/project\" + Task.metadata.labels.\"tideproject.k8s/wave-index\")."
  - "T-02.2-18 (mitigate): the helper adds a Project CRD which references a Secret (per `testdata/three-task-wave.yaml`'s pattern). If the helper creates the Secret with the wrong name/key/value, the Project will be invalid and `resolveProject` will still fail or return a non-functional Project. Mitigation: the helper creates the Secret with the exact same key/value structure as `testdata/three-task-wave.yaml`'s Secret (Opaque type with the `ANTHROPIC_API_KEY` data key; the value is a placeholder because testMode=success Tasks make zero outbound calls per the stub-subagent contract; the Project's `providerSecretRef` field names this Secret by name). **Acceptance gate:** `grep -c 'kind: Secret' test/integration/kind/suite_test.go` returns ≥ 1 after the helper lands AND the Secret YAML in the helper carries `type: Opaque` AND the Secret's namespace matches the Project's namespace (both parameterized by the `ns` argument)."
  - "T-02.2-19 (accept): tactical scope means cascade-6 (if any) is genuinely unknown — all five prior cascades have been distinct classes (cascade-1 PVC RWX→RWO, cascade-2 metrics-bind-address, cascade-3 webhook-cert-path, cascade-4 test-budget, cascade-5 fixture-incomplete). Cascade-6 could be a spec-flake (real flakiness in wave/failure/caps/output specs that have not run end-to-end since Phase 02.1), a kind-infra issue, a stub-subagent image regression, or something else entirely. **Acceptance gate:** if Task 2 BLOCKED with cascade-6, `02.2-07-VERIFICATION.md` §Section 5 MUST classify the cascade type using the T-02.2-16 taxonomy (`flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) AND identify which specific spec(s) failed AND propose Plan 02.2-08's fix shape from the start so the next iteration is not a diagnostic round-trip."
  - "T-02.2-20 (accept): the helper signature `applyHierarchy(ctx, ns, planName, taskName)` is intentionally minimal — parameterizing only namespace + Plan name + Task name. Future tests may need additional parameters (e.g., custom Task.Spec.cmd, custom dependsOn graph, custom Project budget). Accepted that the v1 helper is credproxy-shape-only; if future fixtures need richer parameterization, they extend the helper signature in a follow-up plan rather than copy-paste-mutate the helper. **Acceptance gate:** the helper's signature is exactly `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` (4-arg form) — not a struct-options form, not a builder pattern; v1 is minimal-by-design."

must_haves:
  truths:
    - "`test/integration/kind/suite_test.go` exports a function `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` that creates the full Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy mirroring `testdata/three-task-wave.yaml`'s structural shape"
    - "`test/integration/kind/credproxy_test.go` calls `applyHierarchy(...)` to set up its fixture and contains zero inline `kind: Plan` or `kind: Task` YAML"
    - "`go build ./test/integration/kind/...` exits 0 AND `go vet ./test/integration/kind/...` exits 0 after Task 1"
    - "`02.2-07-VERIFICATION.md` exists with frontmatter `phase: 02.2`, `plan: 07`, `task: 2`, `artifact: verification`, a populated `gate_decision: APPROVED|BLOCKED` line, and Sections 1-6 mirroring `02.2-06-VERIFICATION.md`"
    - "APPROVED gate requires ALL 5 conditions on BOTH clean run AND rerun: wall ≤ 600s; Pod Running ≤ 1 restart; manager-log no `flag provided but not defined` errors; PVC `tide-projects` Bound RWO; 7/7 Layer B Ginkgo specs PASS"
    - "If `02.2-07-VERIFICATION.md` records `gate_decision: APPROVED`, Task 3 fires and Plan 02.2-02's `depends_on` becomes `[\"02.2-07\"]` and all 31 references to `02.2-01-VERIFICATION.md` are redirected to `02.2-07-VERIFICATION.md`"
    - "If `02.2-07-VERIFICATION.md` records `gate_decision: BLOCKED`, Task 3 does NOT fire — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (its current wiring) and the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md"
    - "Production-source-of-truth files (`internal/controller/task_controller.go`, `api/v1alpha1/task_types.go`, `internal/controller/plan_controller.go`, `cmd/manager/main.go`, `charts/tide/values.yaml`) are NOT modified by this plan — Option B is test-fixture-only"
  artifacts:
    - path: "test/integration/kind/suite_test.go"
      provides: "`applyHierarchy(ctx, ns, planName, taskName)` helper that creates the full CRD hierarchy required by the task controller's `resolveProject` dispatch path"
      contains: "func applyHierarchy"
    - path: "test/integration/kind/credproxy_test.go"
      provides: "credproxy specs that call `applyHierarchy()` instead of inline Plan+Task YAML; exercises the production dispatch path end-to-end"
      contains: "applyHierarchy("
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md"
      provides: "Runtime verification artifact with gate_decision frontmatter — authoritative gate for Plan 02.2-02 closeout"
      contains: "gate_decision:"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to [\"02.2-07\"] + 31 references redirected to 02.2-07-VERIFICATION.md (gated on Task 2 APPROVED)"
      contains: "02.2-07"
  key_links:
    - from: "test/integration/kind/credproxy_test.go (BeforeEach/It setup)"
      to: "applyHierarchy() helper in suite_test.go"
      via: "Go function call site"
      pattern: 'applyHierarchy\(\s*ctx\s*,'
    - from: "applyHierarchy helper's Project+Plan+Task CRDs"
      to: "task_controller.go:217 resolveProject Step 3"
      via: "Project CRD in the test namespace satisfies the label fast-path miss + ProjectList fallback that currently fails"
      pattern: '(no project found|resolveProject)'
    - from: "02.2-07-VERIFICATION.md frontmatter gate_decision"
      to: "Plan 02.2-02 Task 1 checkpoint:human-verify resume-signal"
      via: "Task 3 wires Plan 02.2-02's depends_on + checkpoint references (gated on APPROVED)"
      pattern: '^gate_decision: (APPROVED|BLOCKED)$'
    - from: "02.2-02-PLAN.md (depends_on field)"
      to: "this plan's verification artifact"
      via: "frontmatter depends_on: [\"02.2-07\"]"
      pattern: 'depends_on: \[.{0,5}02.2-07.{0,5}\]'
---

<objective>
TACTICAL gap closure for the BLOCKED runtime gate captured in `02.2-06-VERIFICATION.md` §"Section 5 Root-Cause Summary" (fifth iteration). The `credproxy_test.go` HARN-03 specs FAIL with "A Pod should be created for the credproxy-task Job: Timed out after 60.000s" because the inline fixture at lines 78–100 creates ONLY a Plan+Task pair without an accompanying Project CRD in the `credproxy-test` namespace. The task controller's `reconcileDispatch` Step 3 (`internal/controller/task_controller.go:217`) calls `resolveProject(ctx, task)` which returns `"no project found in namespace credproxy-test"` because (a) the Task carries no `tideproject.k8s/project` label (PlanReconciler's `stampTaskLabels` can't backfill it: `resolveProjectName` returns `""` when no Project exists in the namespace), and (b) the `ProjectList` fallback for the namespace returns zero items. The reconcile errors out before Step 11's Job create at line 393. No Job → no Pod → 60s `Eventually(...).Should(Succeed())` times out. The inner `-timeout=5m` Ginkgo cap then fires after the two credproxy spec failures consume 148s of the 5m budget, panicking before specs 3–7 (wave, failure, caps, output) run.

**CRITICAL HYPOTHESIS CORRECTION (load-bearing for this plan's framing — DO NOT inherit the wrong gate from prior verification artifact):** `02.2-06-VERIFICATION.md` §Section 5 hypothesized that the dispatch gate is a "parent Wave Ready check" — that hypothesis is WRONG. The corrected root cause was established by the debug investigation captured at `.planning/debug/credproxy-dispatch-guard.md` §Reasoning Checkpoint:

- The actual gate is `resolveProject` at `task_controller.go:217` (Step 3 of `reconcileDispatch`), NOT a parent Wave Ready check.
- Zero references to `Wave`, `Milestone`, or `Phase` CRDs exist in the entire `task_controller.go` dispatch chain.
- The Wave CRD is purely observational — materialized by `PlanReconciler.materializeWaves` (plan_controller.go:212-254) as a side effect of plan reconciliation; it is never consulted by TaskReconciler for dispatch gating.
- The manager-log `task cleanup` line at 60s post-Task-create is a downstream SYMPTOM of `AfterEach`'s `deleteNamespace` triggering the finalizer path — NOT evidence of dispatch routing.

Cascade-5 classification (from `02.2-06-VERIFICATION.md` frontmatter): `harness-bug` — confirmed by the corrected root cause. The credproxy test fixture is structurally incomplete (missing Project + parent hierarchy) relative to what the task controller's actual dispatch path requires.

**User decision: Option B (full-hierarchy fixture).** Per `.planning/debug/credproxy-dispatch-guard.md` §Reasoning Checkpoint:
- Extend `credproxy_test.go` (or its called helper) to apply the full Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy, mirroring `test/integration/kind/testdata/three-task-wave.yaml`.
- Zero production-code change. `internal/controller/task_controller.go`, `api/v1alpha1/task_types.go`, `internal/controller/plan_controller.go`, `cmd/manager/main.go`, `charts/tide/values.yaml` all stay untouched.
- Brittleness mitigated by adding a shared `applyHierarchy(ctx, ns, planName, taskName) error` helper in `test/integration/kind/suite_test.go` so future credproxy-style minimal-fixture tests can reuse the hierarchy without duplication (T-02.2-17 mitigation).
- Production dispatch path is exercised end-to-end (not bypassed via testMode shortcut), increasing the regression surface that the credproxy test actively defends against.

Option D (testMode dispatch shortcut in `resolveProject`) was the runner-up but rejected for higher blast radius — it would touch `BuildOptions.ProjectUID` flowing into PVC mount paths (`/workspaces/<projectUID>/workspace`) and could accidentally bypass budget+rate-limit gates if `Dev.TestMode` is set on a production Task. Option C (mock Wave Ready in BeforeSuite) is INVALID — its premise (Wave Ready gate) does not exist in the codebase per the debug investigation.

The orchestrator passes the user's Option-B decision down so the planner does not re-litigate.

This plan fixes the cascade-5 immediate blocker (credproxy fixture incompleteness) and re-runs the same end-to-end runtime reproducer captured in `02.2-06-VERIFICATION.md`. Because all 5 prior cascades are now closed (cascade-1 PVC RWX→RWO; cascade-2 metrics-bind-address; cascade-3 webhook-cert-path; cascade-4 test-budget; cascade-5 fixture-incomplete after Task 1 lands), achieving 7/7 Layer B Ginkgo specs PASS on clean + rerun within the 600s outer budget is achievable for the first time in the cascade chain. The expected outcome is one of:
- **APPROVED:** all 5 prior fixes preserved (manager Pod Running 0 restarts; no flag errors; PVC Bound RWO; 600s budget sufficient); the credproxy fixture now exercises the production dispatch path; all 7 Layer B specs (2 credproxy + 2 wave + 1 failure + 1 caps + 1 output) PASS on clean run AND idempotent rerun. Plan 02.2-02 (closeout) gets unblocked via Task 3's surgical edits.
- **BLOCKED (predicted cascade-6 unknown structural shape):** the fixture refactor reveals a different failure — a real spec flake in wave/failure/caps/output (none of which have run end-to-end since Phase 02.1), a kind-infra issue, a stub-subagent image regression, or something not foreseeable. The BLOCKED gate captures the new failure mode root cause AND classifies the cascade type per T-02.2-19 acceptance so Plan 02.2-08 (or equivalent next iteration) can pick the right fix shape from the start.

**TACTICAL SCOPE — user explicitly chose Option B (full-hierarchy fixture) over Option D (testMode dispatch shortcut in production source):**
- This plan adds a shared `applyHierarchy(ctx, ns, planName, taskName) error` helper to `test/integration/kind/suite_test.go` and refactors `test/integration/kind/credproxy_test.go` to call it
- This plan does NOT modify any production source under `internal/controller/`, `api/v1alpha1/`, `cmd/manager/`, `charts/tide/` — Option B is test-fixture-only
- This plan does NOT touch `charts/tide/values.yaml` — chart is FIXED contract per the long-standing rule (`.continue-here.md` anti-pattern #2)
- This plan does NOT modify the `Makefile` — the `timeout 600s` budget from Plan 02.2-06 is preserved verbatim
- This plan does NOT modify Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06 — only `02.2-02-PLAN.md` is touched (and only by Task 3, gated on APPROVED)
- This plan does NOT modify other test fixtures (`wave_test.go`, `failure_test.go`, `caps_test.go`, `output_test.go`) — those already use either `testdata/three-task-wave.yaml` or analog inline hierarchies that work; only the credproxy fixture needs refactoring
- This plan does NOT add testMode dispatch logic to the task controller — Option D rejected per the debug investigation

**Plan 02.2-06's pattern this plan mirrors:**
Plan 02.2-06 ran a 3-task shape: T1 source edit (Makefile timeout bump), T2 runtime verification (`02.2-06-VERIFICATION.md` with `gate_decision` frontmatter), T3 surgical edit of `02.2-02-PLAN.md` gated on APPROVED. This plan mirrors that shape exactly with cascade-5's target (test fixture refactor + shared helper) substituted for cascade-4's target (Makefile timeout). **The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic:** Task 3 fires ONLY if Task 2's `gate_decision: APPROVED`.

**Recovery state note (load-bearing for Task 3's rewire scope):** Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, and 02.2-06 Task 3 all DID NOT FIRE (their respective Task 2 gates were each BLOCKED on a different cascade). As a result, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (`grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 31 as of plan-time; verified during plan authoring). This plan's Task 3 lands the long-pending rewire if Task 2's gate is APPROVED, redirecting all 31 references to `02.2-07-VERIFICATION.md` in one pass.

**Recovery-lesson note from `.continue-here.md` Plan 02.2-05:** Write `02.2-07-VERIFICATION.md` WITH PRELIMINARY frontmatter immediately after the clean-run captures land in `/tmp/02.2-07-clean-run.log`, then add Sections 2-6 incrementally as the rerun completes and the cluster diagnostic snapshot is captured. This protects against mid-run executor API errors / context window pressure during the long-running runtime verification phase — the artifact's frontmatter (the load-bearing gate field) is preserved even if Sections 4-6 prose authoring is interrupted.

Purpose: close the BLOCKED runtime gate from `02.2-06-VERIFICATION.md` (cascade-4 budget fix preserved; cascade-5 surfaced as harness-bug per corrected root cause in `.planning/debug/credproxy-dispatch-guard.md`) by extending the credproxy test fixture to apply the same full Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy that `wave_test.go` already uses successfully. Tactical iteration #5: replace the fixture, re-verify, see if the cascade chain has terminated (5 distinct cascades now closed: PVC RWX/RWO, metrics flag, webhook-cert-path flag, test-budget, fixture-incomplete).

Output:
- `test/integration/kind/suite_test.go` with a new `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` helper that creates the full hierarchy mirroring `testdata/three-task-wave.yaml`
- `test/integration/kind/credproxy_test.go` refactored to call `applyHierarchy(...)` instead of inlining Plan+Task YAML (lines 78–100 in the existing file replaced)
- `.planning/phases/02.2-.../02.2-07-VERIFICATION.md` mirroring `02.2-06-VERIFICATION.md`'s structural template; `gate_decision: APPROVED` or `BLOCKED`
- If Task 2 APPROVED: `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-07"]` and ALL 31 `02.2-01-VERIFICATION.md` references redirected to `02.2-07-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
**Wave-dispatch concern for Plan 02.2-02 (executor MUST honor):** Plan 02.2-02 is the closeout plan and currently has `depends_on: ["02.2-01"]` with a `checkpoint:human-verify` Task 1 that reads `02.2-01-VERIFICATION.md`'s `gate_decision` field. Because Plan 02.2-01's verification artifact records `gate_decision: BLOCKED` (its checkpoint Task 3 was BLOCKED on the original cascade chain, never updated), Plan 02.2-02 MUST NOT be dispatched alongside Plan 02.2-07 in the same execute wave. The structural mitigation: execute-phase's wave-ordering reads `depends_on` from each plan's frontmatter and groups plans whose dependencies are all satisfied into the same wave. Plan 02.2-02's `depends_on` points at 02.2-01 (BLOCKED gate artifact); Plan 02.2-07's `depends_on` points at 02.2-06 (whose source-shape Makefile fix IS landed — APPROVED on Task 1 acceptance gates — even though its Task 2 runtime gate was BLOCKED on cascade-5 which THIS plan is closing). The wave-failure-semantics contract from CLAUDE.md is the load-bearing logic: failed task → siblings continue, **dependents in later waves never dispatch**. Plan 02.2-02 stays a dependent-in-later-wave of `02.2-01-VERIFICATION.md`'s BLOCKED gate until THIS plan's Task 3 rewrites its `depends_on` to point at `02.2-07-VERIFICATION.md`'s APPROVED gate. If the executor dispatches Plan 02.2-02 alongside Plan 02.2-07 (e.g., due to a misread wave-derivation), Plan 02.2-02's Task 1 checkpoint will halt at the BLOCKED gate AND nothing further fires — but the structural intent is that Plan 02.2-02 dispatches AFTER Plan 02.2-07's Task 3 has landed, in a separate wave 7.

**Critical anti-patterns from `.continue-here.md` "Critical Anti-Patterns" (do NOT trigger):**
1. Do NOT pitch a comprehensive audit. User has chosen tactical iteration 5 times now; cascade-5 was correctly classified as harness-bug only after runtime evidence + debug investigation pinpointed `resolveProject` as the actual gate (NOT the originally-hypothesized parent Wave Ready check). Tactical iteration 6 (THIS plan) is the right shape.
2. Do NOT touch `charts/tide/values.yaml`. Chart is FIXED contract.
3. Do NOT dispatch Plan 02.2-02 alongside Plan 02.2-07 in the same execute wave when Plan 02.2-02's checkpoint artifact records BLOCKED. (See preceding paragraph.)
4. Do NOT modify production source files in this plan. Option B is test-fixture-only — the entire dispatch chain in `internal/controller/task_controller.go` stays untouched.
5. Do NOT inherit the `02.2-06-VERIFICATION.md` §Section 5 hypothesis verbatim — the debug investigation corrected the gate from "parent Wave Ready check" to "`resolveProject` Project-in-namespace lookup". The plan's `<objective>` surfaces this correction so future readers don't re-inherit the wrong framing. Surface the corrected gate in the verification artifact's §Section 5 prose as well (Task 2's deliverable).
</execution_notes>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md
@.planning/debug/credproxy-dispatch-guard.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-06-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/.continue-here.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from testdata/three-task-wave.yaml + suite_test.go + credproxy_test.go + task_controller.go. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

**From `test/integration/kind/testdata/three-task-wave.yaml` (the structural template — verbatim shapes the helper MUST produce):**

API group + version: `tideproject.k8s/v1alpha1` (per CLAUDE.md project-rule; never `tide.io` or placeholders).

Hierarchy shapes (parameterize names; preserve field structure):

```yaml
# 1. Namespace (parameter: ns)
apiVersion: v1
kind: Namespace
metadata:
  name: <ns>

# 2. Secret (Provider Secret — Project's providerSecretRef target)
apiVersion: v1
kind: Secret
metadata:
  name: tide-provider-secret
  namespace: <ns>
type: Opaque
data:
  ANTHROPIC_API_KEY: dGVzdC1hcGkta2V5LXN0dWItc3ViYWdlbnQtZG9lcy1ub3QtdXNlLWl0  # base64 placeholder; stub-subagent makes ZERO outbound calls

# 3. Project (parameter: ns; name: <ns>-project — derive deterministically from ns)
apiVersion: tideproject.k8s/v1alpha1
kind: Project
metadata:
  name: <ns>-project           # e.g., "credproxy-test-project"
  namespace: <ns>
spec:
  targetRepo: "https://github.com/example/<ns>.git"
  providerSecretRef: "tide-provider-secret"
  budget:
    absoluteCapCents: 100000   # $1000 — no budget cap during tests (matches testdata/three-task-wave.yaml)

# 4. Milestone (parameter: ns)
apiVersion: tideproject.k8s/v1alpha1
kind: Milestone
metadata:
  name: <ns>-milestone
  namespace: <ns>
spec:
  projectRef: <ns>-project

# 5. Phase (parameter: ns)
apiVersion: tideproject.k8s/v1alpha1
kind: Phase
metadata:
  name: <ns>-phase
  namespace: <ns>
spec:
  milestoneRef: <ns>-milestone

# 6. Plan (parameters: ns, planName)
apiVersion: tideproject.k8s/v1alpha1
kind: Plan
metadata:
  name: <planName>             # e.g., "credproxy-plan"
  namespace: <ns>
  labels:
    tideproject.k8s/project: <ns>-project
spec:
  phaseRef: <ns>-phase

# 7. Task (parameters: ns, planName, taskName) — wave 0, no dependencies, testMode=success
apiVersion: tideproject.k8s/v1alpha1
kind: Task
metadata:
  name: <taskName>             # e.g., "credproxy-task"
  namespace: <ns>
  labels:
    tideproject.k8s/project: <ns>-project     # CRITICAL — fast-path label in resolveProject (task_controller.go:533)
    tideproject.k8s/wave-index: "0"
spec:
  planRef: <planName>
  filesTouched:
    - <taskName>.go
  declaredOutputPaths:
    - <taskName>.go
  dev:
    testMode: success          # preserves credproxy contract — stub-subagent makes ZERO outbound calls
```

**From `test/integration/kind/credproxy_test.go` (the call site to refactor — lines 78–100 in the existing file):**

Two specs in this file, both currently inline the same Plan+Task YAML (missing Project + parent hierarchy):
- Spec 1 (line 74): `HARN-03: credproxy sidecar container starts and becomes Ready` — uses `credproxyPlanYAML` at lines 78–100 with names `credproxy-plan` + `credproxy-task`
- Spec 2 (line 153): `HARN-03: credproxy container log contains 'credproxy listening on 127.0.0.1:8443'` — uses `logPlanYAML` at lines 157–179 with names `credproxy-log-plan` + `credproxy-log-task`

Both specs call `createNamespace(credproxyNS)` then `Expect(applyYAML(...)).To(Succeed())`. AfterEach calls `deleteNamespace(credproxyNS)`. The refactor replaces the inline YAML construction + `applyYAML` call with a single `applyHierarchy(ctx, credproxyNS, "<planName>", "<taskName>")` call per spec. The `createNamespace(credproxyNS)` call BECOMES PART OF `applyHierarchy` (the helper creates the Namespace explicitly per its Step 1) — so the existing `createNamespace(credproxyNS)` line in each spec MUST be removed to avoid duplicate-create errors (kubectl apply is idempotent so the error wouldn't be fatal, but the duplicate call is structurally noisy).

**From `test/integration/kind/suite_test.go` (the target file for the new helper):**

The file already has `applyYAML(yaml string) error` (line 486), `applyFile(path string) error` (line 530), `createNamespace`/`deleteNamespace` helpers, `ensureSubagentSA(ns string)` (line 518), `k8sClient`, `ctx`, `kubeconfigPath` package-level vars. The new `applyHierarchy` helper:
- Lives in the same file (suite_test.go)
- Uses the existing `applyYAML` helper internally — no new K8s client wiring needed
- Calls `ensureSubagentSA(ns)` after creating the Namespace (per the existing pattern at line 358 — every test-fixture namespace gets the tide-subagent SA)
- Returns `error` (non-fatal at the helper layer; spec body wraps with `Expect(...).To(Succeed())`)
- Signature: `func applyHierarchy(ctx context.Context, ns, planName, taskName string) error`

The helper internally constructs a single multi-document YAML string (using `fmt.Sprintf` with `%s` placeholders for `ns`, `planName`, `taskName`, AND a derived `<ns>-project` / `<ns>-milestone` / `<ns>-phase` triplet) and passes it to `applyYAML(...)`. The Secret + Project + Milestone + Phase + Plan + Task documents are separated by `---` per kubectl multi-doc convention. The Namespace document is created FIRST via `createNamespace(ns)` (helper internally calls it) because kubectl apply on a multi-doc YAML containing a Namespace + resources in that namespace can race if the Namespace isn't yet visible.

**From `internal/controller/task_controller.go` (production dispatch path — DO NOT MODIFY, but understand the gate this fixture must satisfy):**

`reconcileDispatch` Step 3 (line 217) calls `resolveProject(ctx, task)`. `resolveProject` (lines 531–548) tries:
1. **Fast path (line 533):** `task.Labels["tideproject.k8s/project"]` — present? Get Project by that name. The helper's Task carries this label (preset, mirroring `testdata/three-task-wave.yaml:73`).
2. **Fallback (lines 540–547):** `r.List(ctx, &projectList, client.InNamespace(task.Namespace))` — if list is empty, error. The helper's Project CRD in the namespace satisfies this fallback regardless of the fast-path outcome.

Both paths now succeed because the helper creates both (a) a Task with the `tideproject.k8s/project` label preset AND (b) a Project CRD in the same namespace. Either path is sufficient on its own; having both is belt-and-suspenders.

**From CLAUDE.md "Failure semantics at wave boundaries" contract:**
- Failed task → siblings in same wave continue
- Dependents in later waves never dispatch
- Plan 02.2-02 reads the gate artifact; if BLOCKED, Plan 02.2-02 stays gated
- Resumption state = indegree map + completed-task set, nothing more

**From `.planning/debug/credproxy-dispatch-guard.md` §Reasoning Checkpoint (the corrected root cause that this plan acts on):**
- Dispatch gate: `task_controller.go:217` (`resolveProject` in Step 3 of `reconcileDispatch`)
- NOT a parent Wave Ready check — `task_controller.go` has zero Wave/Milestone/Phase CRD fetches
- The Wave CRD is observational only; materialized by `PlanReconciler.materializeWaves` as a side effect; never consulted by TaskReconciler for dispatch gating
- The "task cleanup" manager-log line at 60s is a downstream SYMPTOM of `AfterEach`'s `deleteNamespace` triggering the finalizer path, NOT evidence of dispatch routing

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `applyHierarchy` helper to suite_test.go + refactor credproxy_test.go to call it</name>
  <files>test/integration/kind/suite_test.go, test/integration/kind/credproxy_test.go</files>

  <read_first>
    - test/integration/kind/suite_test.go (full file — 573 lines; particularly: lines 33–54 imports block (the helper will need `context` already imported); lines 84–89 package-level vars (`k8sClient`, `ctx`, `kubeconfigPath` are package-scope and usable); lines 100–142 BeforeSuite (for the pattern of `By("...")` GinkgoWriter logging — the helper SHOULD NOT use `By` because it runs inside a spec body, not BeforeSuite); lines 485–495 `applyYAML(yaml string) error` (the helper's internal apply primitive); lines 497–527 `ensureSubagentSA(ns string)` (the pattern for namespace-scoped SA creation — the helper calls this after Namespace creation); lines 540–545 `deleteNamespace(ns string)` (for understanding the cleanup contract — NOT called by the helper; AfterEach owns cleanup))
    - test/integration/kind/credproxy_test.go (full file — 220 lines; particularly: lines 41–54 imports; lines 56–69 `Describe`/`BeforeEach`/`AfterEach` structure; lines 74–150 spec 1 (`HARN-03: credproxy sidecar container starts and becomes Ready`) — particularly lines 75–102 the YAML construction + applyYAML call that gets replaced; lines 153–218 spec 2 (`HARN-03: credproxy container log contains ...`) — particularly lines 154–181 the YAML construction + applyYAML call that gets replaced)
    - test/integration/kind/testdata/three-task-wave.yaml (full file — 123 lines; the structural template; particularly lines 21–32 Project shape, lines 34–42 Milestone shape, lines 44–52 Phase shape, lines 54–63 Plan shape, lines 65–82 Task shape — note the `tideproject.k8s/project` AND `tideproject.k8s/wave-index` labels on the Task at lines 73–74)
    - .planning/debug/credproxy-dispatch-guard.md (the authoritative gap source — §Reasoning Checkpoint explains why this fixture refactor closes the dispatch gate; §"Where dispatch is actually gated" enumerates the 12 reconcileDispatch steps showing Step 3's resolveProject as the early-return gate)
    - .planning/phases/02.2-.../02.2-RESEARCH.md (full file — the Phase 02.2 research; particularly the kind cluster setup patterns and the test-fixture conventions established in Phase 02.1)
    - .planning/phases/02.2-.../02.2-06-PLAN.md Task 1 body (the structural model for THIS plan's Task 1 — single-file source edit pattern with regression guards on surrounding tokens; substitute the credproxy fixture refactor + helper addition for the Makefile timeout token replacement)
  </read_first>

  <action>
    Two-part task in this single auto-task: first add the shared helper to `suite_test.go`, then refactor `credproxy_test.go` to call it.

    **Part A — Add `applyHierarchy` helper to `test/integration/kind/suite_test.go`:**

    Add a new function with the exact signature:
    ```
    func applyHierarchy(ctx context.Context, ns, planName, taskName string) error
    ```

    Place the function in `suite_test.go` (e.g., after the existing `ensureSubagentSA` helper at line 527 or before `applyFile` at line 530 — anywhere structurally adjacent to the other apply/ensure helpers is acceptable, but keep it in `suite_test.go` not in `credproxy_test.go`; the helper must be reusable).

    The helper's body:
    1. Call `createNamespace(ns)` first (this helper exists elsewhere — confirm by grepping `func createNamespace` in `test/integration/kind/` if needed; if `createNamespace` is defined in a sibling test file like `failure_test.go`, the function-level scope still works because all files share the `kind_integration` package).
    2. Call `ensureSubagentSA(ns)` to create the `tide-subagent` ServiceAccount in the namespace (the helper at line 518 already does this; the helper must call it BEFORE applying the Task because Task Job dispatch requires the SA per the comment at line 510).
    3. Derive deterministic names for Project + Milestone + Phase from the namespace argument:
       - `projectName := ns + "-project"` (e.g., `credproxy-test-project`)
       - `milestoneName := ns + "-milestone"` (e.g., `credproxy-test-milestone`)
       - `phaseName := ns + "-phase"` (e.g., `credproxy-test-phase`)
    4. Construct a single multi-document YAML string via `fmt.Sprintf` with the seven shapes (Namespace is created via `createNamespace(ns)` separately in step 1; the multi-doc YAML covers Secret + Project + Milestone + Phase + Plan + Task — six documents separated by `\n---\n`). The shapes MUST mirror `testdata/three-task-wave.yaml` field-for-field (T-02.2-17 mitigation). Use the exact API versions, kinds, and required Spec fields enumerated in the `<interfaces>` block above.
    5. Pass the constructed YAML to the existing `applyYAML(yaml string) error` helper at suite_test.go:486.
    6. Return the error from `applyYAML` (or `nil` on success).

    The Secret document inside the helper:
    ```
    apiVersion: v1
    kind: Secret
    metadata:
      name: tide-provider-secret
      namespace: <ns>
    type: Opaque
    data:
      ANTHROPIC_API_KEY: dGVzdC1hcGkta2V5LXN0dWItc3ViYWdlbnQtZG9lcy1ub3QtdXNlLWl0
    ```
    (The data value is the same base64 placeholder as `testdata/three-task-wave.yaml:18` — `base64("test-api-key-stub-subagent-does-not-use-it")`. Stub-subagent makes zero outbound calls per the package comment at credproxy_test.go:27, so the value is irrelevant but the structural field is load-bearing per T-02.2-18 acceptance.)

    The Task document MUST carry both labels (T-02.2-17 mitigation — mirrors `testdata/three-task-wave.yaml:73-74`):
    ```
    labels:
      tideproject.k8s/project: <projectName>
      tideproject.k8s/wave-index: "0"
    ```

    Add a doc comment above the function describing: (a) the helper's purpose (creates the full hierarchy required by the task controller's `resolveProject` dispatch path), (b) why it exists (cascade-5 from 02.2-07 — credproxy fixture was structurally incomplete relative to production dispatch path), (c) what it parameterizes (`ns`, `planName`, `taskName`), (d) what it does NOT parameterize (Project name, Milestone name, Phase name — all derived deterministically from `ns`; budget — fixed at $1000 absoluteCapCents matching three-task-wave.yaml; Secret name — fixed at `tide-provider-secret`), (e) reference to `.planning/debug/credproxy-dispatch-guard.md` for the corrected root cause that motivated this helper.

    **Part B — Refactor `test/integration/kind/credproxy_test.go` to call `applyHierarchy`:**

    Two specs in this file both currently construct inline YAML containing only Plan+Task (missing Project + parent hierarchy). For BOTH specs:

    1. Delete the inline `<credproxyPlanYAML | logPlanYAML>` variable declaration and its multi-doc YAML literal (lines 78–100 for spec 1; lines 157–179 for spec 2).
    2. Delete the call to `createNamespace(credproxyNS)` immediately before the inline YAML construction (the new `applyHierarchy` helper creates the Namespace as its Part A Step 1 — calling `createNamespace` twice would be idempotent due to `kubectl apply` semantics but is structurally redundant).
    3. Delete the `Expect(applyYAML(...)).To(Succeed())` call that applies the inline YAML.
    4. Replace the entire block with a single line:
       - For spec 1: `Expect(applyHierarchy(ctx, credproxyNS, "credproxy-plan", "credproxy-task")).To(Succeed())`
       - For spec 2: `Expect(applyHierarchy(ctx, credproxyNS, "credproxy-log-plan", "credproxy-log-task")).To(Succeed())`

    The rest of each spec body (the `Eventually(...).Should(...)` assertions checking for the Task CRD, the Pod, the credproxy container, the startup log line) stays VERBATIM. The `By("Creating a Plan with a Task using testMode=success ...")` line stays VERBATIM (the helper IS creating that Plan + Task, just with the full surrounding hierarchy too).

    After refactoring, the imports block in `credproxy_test.go` may have an unused import: `fmt` was only needed for the inline `fmt.Sprintf` call constructing the YAML. If `fmt` is no longer used elsewhere in the file, `go vet` or `goimports` should be run to remove it (`go vet ./test/integration/kind/...` will surface "imported and not used: fmt" — if it does, remove the line from the import block). The other imports (`strings`, `time`, ginkgo/gomega, corev1, client, tideprojectv1alpha1) stay because they're used by the assertions.

    **Constraints:**
    - **DO NOT modify** any production source under `internal/`, `api/`, `cmd/`, `pkg/`, `charts/`, `Makefile`, `.github/` — this task is test-fixture-only per Option B's contract.
    - **DO NOT modify** other test files: `wave_test.go`, `failure_test.go`, `caps_test.go`, `output_test.go`. They use either `testdata/three-task-wave.yaml` directly (via `applyFile`) or analog inline hierarchies that already work. Only the credproxy fixture is broken (per the debug investigation).
    - **DO NOT modify** the testdata file `test/integration/kind/testdata/three-task-wave.yaml` — it is the working reference; the helper MIRRORS it but does not REPLACE it (wave_test.go still consumes it directly).
    - **DO NOT change** the helper signature beyond `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` — T-02.2-20 acceptance forbids struct-options or builder patterns in v1. Future plans may extend the signature.
    - **DO NOT add** new test specs to `credproxy_test.go` — the refactor is scope-limited to the two existing specs.
    - **DO NOT bypass** the production dispatch path with testMode shortcuts in any production source — Option D was rejected per the debug investigation.
    - **DO NOT touch** `charts/tide/values.yaml` per `.continue-here.md` anti-pattern #2.
    - **DO NOT modify** Plans 02.2-01, 02.2-02 (Task 3 will handle 02.2-02), 02.2-03, 02.2-04, 02.2-05, 02.2-06 — only the new `02.2-07-VERIFICATION.md` (Task 2) and `02.2-02-PLAN.md` (Task 3, gated) are touched by this plan.
    - **DO NOT use** the `K8s API group "tide.io"` or placeholders — the correct group is `tideproject.k8s` per CLAUDE.md "TIDE domain rule" (verify the helper's API version strings are `tideproject.k8s/v1alpha1`).

    Commit with message:
    ```
    fix(02.2-07): add applyHierarchy helper + refactor credproxy fixture (cascade-5 closure)
    ```

    Body should reference `.planning/debug/credproxy-dispatch-guard.md` §Reasoning Checkpoint (the corrected root cause); note that Option B (full-hierarchy fixture) is the user's decision over Option D (testMode dispatch shortcut); note that production source is untouched; note that the helper is reusable for future credproxy-style minimal-fixture tests (T-02.2-17 / T-02.2-20 mitigation).
  </action>

  <verify>
    <automated><![CDATA[
      # === Part A — applyHierarchy helper present in suite_test.go ===
      # Helper function definition exists
      [ "$(grep -c 'func applyHierarchy' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Helper signature is exactly 4-arg form (ctx, ns, planName, taskName) returning error — T-02.2-20 acceptance
      grep -E 'func applyHierarchy\(ctx[[:space:]]+context\.Context,[[:space:]]+ns,[[:space:]]+planName,[[:space:]]+taskName[[:space:]]+string\)[[:space:]]+error' test/integration/kind/suite_test.go && \
      # Helper creates a Secret resource (T-02.2-18 acceptance)
      [ "$(grep -c 'kind: Secret' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Helper creates a Project resource
      [ "$(grep -c 'kind: Project' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Helper creates a Milestone resource
      [ "$(grep -c 'kind: Milestone' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Helper creates a Phase resource
      [ "$(grep -c 'kind: Phase' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Helper uses the canonical API group tideproject.k8s (CLAUDE.md domain rule)
      [ "$(grep -c 'tideproject.k8s/v1alpha1' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Helper does NOT use the registered tide.io group or placeholders
      [ "$(grep -cE 'tide\.io/v1alpha1|placeholder\.io/v1alpha1' test/integration/kind/suite_test.go)" -eq 0 ] && \
      # Helper preserves the project label fast-path target (task_controller.go:533 — T-02.2-17 acceptance)
      [ "$(grep -c 'tideproject.k8s/project' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Helper preserves the wave-index label (mirrors testdata/three-task-wave.yaml:74)
      [ "$(grep -c 'tideproject.k8s/wave-index' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # === Part B — credproxy_test.go refactored ===
      # applyHierarchy is called at least twice (once per spec — there are 2 HARN-03 specs)
      [ "$(grep -c 'applyHierarchy(' test/integration/kind/credproxy_test.go)" -ge 2 ] && \
      # Inline Plan YAML purged
      [ "$(grep -c 'kind: Plan' test/integration/kind/credproxy_test.go)" -eq 0 ] && \
      # Inline Task YAML purged
      [ "$(grep -c 'kind: Task' test/integration/kind/credproxy_test.go)" -eq 0 ] && \
      # Inline apiVersion: tideproject.k8s/... YAML literals purged from credproxy_test.go (only suite_test.go's helper has them now)
      [ "$(grep -c 'apiVersion: tideproject.k8s/v1alpha1' test/integration/kind/credproxy_test.go)" -eq 0 ] && \
      # Both spec call sites name the right plans/tasks
      grep -E 'applyHierarchy\(.*credproxyNS.*credproxy-plan.*credproxy-task\)' test/integration/kind/credproxy_test.go && \
      grep -E 'applyHierarchy\(.*credproxyNS.*credproxy-log-plan.*credproxy-log-task\)' test/integration/kind/credproxy_test.go && \
      # === Compile + vet gates ===
      # go build exits 0
      go build ./test/integration/kind/... && \
      # go vet exits 0 (catches unused imports if `fmt` was removed correctly)
      go vet ./test/integration/kind/... && \
      # === Regression guards — production source untouched ===
      # No edits to internal/controller/task_controller.go (Option B is test-fixture-only)
      [ "$(git status --porcelain internal/controller/task_controller.go | wc -l | tr -d ' ')" -eq 0 ] && \
      # No edits to api/v1alpha1/task_types.go
      [ "$(git status --porcelain api/v1alpha1/task_types.go | wc -l | tr -d ' ')" -eq 0 ] && \
      # No edits to internal/controller/plan_controller.go
      [ "$(git status --porcelain internal/controller/plan_controller.go | wc -l | tr -d ' ')" -eq 0 ] && \
      # No edits to cmd/manager/main.go
      [ "$(git status --porcelain cmd/manager/main.go | wc -l | tr -d ' ')" -eq 0 ] && \
      # No edits to charts/tide/values.yaml (anti-pattern #2)
      [ "$(git status --porcelain charts/tide/values.yaml | wc -l | tr -d ' ')" -eq 0 ] && \
      # No edits to Makefile (Plan 02.2-06's timeout 600s preserved)
      [ "$(git status --porcelain Makefile | wc -l | tr -d ' ')" -eq 0 ] && \
      # No edits to other test files (wave_test.go, failure_test.go, caps_test.go, output_test.go)
      [ "$(git status --porcelain test/integration/kind/wave_test.go test/integration/kind/failure_test.go test/integration/kind/caps_test.go test/integration/kind/output_test.go | wc -l | tr -d ' ')" -eq 0 ] && \
      # No edits to testdata/three-task-wave.yaml (the helper mirrors it but does NOT modify it)
      [ "$(git status --porcelain test/integration/kind/testdata/three-task-wave.yaml | wc -l | tr -d ' ')" -eq 0 ] && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Helper present (Part A):** `grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` returns ≥ 1
    - **Helper signature exact (T-02.2-20 acceptance):** `grep -E 'func applyHierarchy\(ctx[[:space:]]+context\.Context,[[:space:]]+ns,[[:space:]]+planName,[[:space:]]+taskName[[:space:]]+string\)[[:space:]]+error' test/integration/kind/suite_test.go` matches (the 4-arg form; no struct-options or builder pattern)
    - **Helper creates Secret (T-02.2-18 acceptance):** `grep -c 'kind: Secret' test/integration/kind/suite_test.go` returns ≥ 1
    - **Helper creates Project:** `grep -c 'kind: Project' test/integration/kind/suite_test.go` returns ≥ 1
    - **Helper creates Milestone:** `grep -c 'kind: Milestone' test/integration/kind/suite_test.go` returns ≥ 1
    - **Helper creates Phase:** `grep -c 'kind: Phase' test/integration/kind/suite_test.go` returns ≥ 1
    - **Canonical API group used (CLAUDE.md domain rule):** `grep -c 'tideproject.k8s/v1alpha1' test/integration/kind/suite_test.go` returns ≥ 1
    - **No `tide.io` or placeholder API groups:** `grep -cE 'tide\.io/v1alpha1|placeholder\.io/v1alpha1' test/integration/kind/suite_test.go` returns 0
    - **Project label preset on Task (T-02.2-17 acceptance — fast-path target for resolveProject):** `grep -c 'tideproject.k8s/project' test/integration/kind/suite_test.go` returns ≥ 1
    - **Wave-index label preset on Task (mirrors testdata/three-task-wave.yaml:74):** `grep -c 'tideproject.k8s/wave-index' test/integration/kind/suite_test.go` returns ≥ 1
    - **credproxy_test.go calls helper twice (Part B):** `grep -c 'applyHierarchy(' test/integration/kind/credproxy_test.go` returns ≥ 2 (one call per HARN-03 spec)
    - **Inline Plan YAML purged from credproxy_test.go:** `grep -c 'kind: Plan' test/integration/kind/credproxy_test.go` returns 0
    - **Inline Task YAML purged from credproxy_test.go:** `grep -c 'kind: Task' test/integration/kind/credproxy_test.go` returns 0
    - **Inline tideproject.k8s/v1alpha1 YAML literals purged from credproxy_test.go:** `grep -c 'apiVersion: tideproject.k8s/v1alpha1' test/integration/kind/credproxy_test.go` returns 0 (the helper carries all CRD apiVersions; the call sites only have function arguments)
    - **Spec 1 names match:** `grep -E 'applyHierarchy\(.*credproxyNS.*credproxy-plan.*credproxy-task\)' test/integration/kind/credproxy_test.go` matches
    - **Spec 2 names match:** `grep -E 'applyHierarchy\(.*credproxyNS.*credproxy-log-plan.*credproxy-log-task\)' test/integration/kind/credproxy_test.go` matches
    - **go build exits 0:** `go build ./test/integration/kind/...` returns exit code 0
    - **go vet exits 0:** `go vet ./test/integration/kind/...` returns exit code 0 (catches unused-import errors if `fmt` removal was needed but missed)
    - **Production source untouched (Option B contract — load-bearing):** `git status --porcelain internal/controller/task_controller.go api/v1alpha1/task_types.go internal/controller/plan_controller.go cmd/manager/main.go charts/tide/values.yaml Makefile` shows zero modified lines (these files are the production-source-of-truth set; cascade-5 fix MUST be test-fixture-only)
    - **Other test files untouched:** `git status --porcelain test/integration/kind/wave_test.go test/integration/kind/failure_test.go test/integration/kind/caps_test.go test/integration/kind/output_test.go test/integration/kind/testdata/three-task-wave.yaml` shows zero modified lines
  </acceptance_criteria>

  <done>
    `test/integration/kind/suite_test.go` exports a new `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` helper that creates the full Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy mirroring `testdata/three-task-wave.yaml`'s structural shape (API group `tideproject.k8s/v1alpha1`; Secret of type Opaque with `ANTHROPIC_API_KEY` data key; Project with `targetRepo` + `providerSecretRef` + `budget.absoluteCapCents`; Milestone + Phase + Plan with their refs; Task with both `tideproject.k8s/project` AND `tideproject.k8s/wave-index` labels preset; testMode=success preserving the credproxy contract). `test/integration/kind/credproxy_test.go` has both HARN-03 specs refactored to call `applyHierarchy(ctx, credproxyNS, "<planName>", "<taskName>")` instead of inlining Plan+Task YAML; no `kind: Plan` or `kind: Task` literals remain in the file. `go build ./test/integration/kind/...` and `go vet ./test/integration/kind/...` both exit 0. No production source modified — `internal/controller/`, `api/v1alpha1/`, `cmd/manager/`, `charts/tide/`, `Makefile` all untouched (Option B contract). Other test files (`wave_test.go`, `failure_test.go`, `caps_test.go`, `output_test.go`, `testdata/three-task-wave.yaml`) untouched. Committed with the `fix(02.2-07): add applyHierarchy helper + refactor credproxy fixture (cascade-5 closure)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: End-to-End Runtime Re-Verification — author 02.2-07-VERIFICATION.md (clean run + rerun + gate decision)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-06-VERIFICATION.md (the structural template this artifact mirrors verbatim; frontmatter shape with `phase`/`plan`/`task`/`artifact`/`captured_on`/`captured_by_host`/`captured_by_branch`/`clean_run_exit`/`clean_run_wall_seconds`/`clean_run_specs_passed`/`clean_run_specs_skipped`/`clean_run_specs_failed`/`clean_run_phase`/`rerun_exit`/`rerun_wall_seconds`/`rerun_specs_passed`/`rerun_specs_skipped`/`rerun_specs_failed`/`rerun_phase`/`gate_decision`/`gate_reason` fields; Section 1/2/3/4/5/6 layout; per-spec outcome tables; root-cause analysis prose; threat-model-mitigations-applied list; "What This Artifact Did Not Touch" closing section; particularly note the §"Pod-State Evidence" and §"Manager Log Evidence" blocks which are LOAD-BEARING for the gate decision)
    - .planning/debug/credproxy-dispatch-guard.md (the authoritative gap source — §Reasoning Checkpoint explains the corrected root cause; this artifact's §Section 5 prose MUST cite this debug document explicitly and MUST NOT inherit `02.2-06-VERIFICATION.md` §Section 5's "parent Wave Ready check" hypothesis verbatim)
    - .planning/phases/02.2-.../02.2-05-VERIFICATION.md (the prior verification; for the spec list and the per-spec outcome table shape; the Before/After Comparison structure across multiple iterations)
    - Makefile (post-Plan-02.2-06 state — verify the `timeout 600s` budget is in place before this task runs; the runtime reproducer uses this budget)
    - test/integration/kind/suite_test.go (post-Task-1 state — verify `applyHierarchy` helper is committed before this task runs)
    - test/integration/kind/credproxy_test.go (post-Task-1 state — verify the helper-based fixture is committed before this task runs)
    - .planning/phases/02.2-.../02.2-06-SUMMARY.md (Plan 02.2-06's summary — for cascade-4 closure evidence and cascade-5 surfacing analysis; particularly the manager-log evidence that the task controller's reconcile loop processed credproxy-task)
    - .planning/phases/02.2-.../02.2-06-PLAN.md Task 2 body (the structural model for THIS plan's Task 2 — runtime verification reproducer pattern with clean run + idempotent rerun + diagnostic snapshot + per-spec outcome table + gate decision)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer mirroring `02.2-06-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md` using the same structural template.

    **Recovery-lesson PRE-STEP (from `.continue-here.md` Plan 02.2-05 lesson, now codified protocol):** BEFORE running the clean reproducer, author `02.2-07-VERIFICATION.md` with PRELIMINARY frontmatter — the load-bearing fields (`phase: 02.2`, `plan: 07`, `task: 2`, `artifact: verification`, `captured_on: <today>`, `captured_by_host: <host>`, `captured_by_branch: <branch>`, and placeholder `gate_decision: PENDING` line) — so that if mid-run executor API errors or context window pressure interrupt the long-running runtime verification, the artifact's gate field still exists with a recoverable PENDING state. Then update `gate_decision: PENDING` to APPROVED or BLOCKED after Sub-step C is complete.

    **Sub-step A — Clean Start Run:**
    ```bash
    # Pre-step (clean state):
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true

    # Capture wall + exit (set -o pipefail makes the bash captured exit reflect make's real exit, not tee's):
    set -o pipefail
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-07-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    **Immediately after the clean run completes (regardless of pass/fail), capture cluster + helm state for the artifact:**
    ```bash
    # Post-clean diagnostic snapshot — needed for both APPROVED and BLOCKED gate analysis
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-07-clean-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-07-clean-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-07-clean-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=20m 2>&1 | tee /tmp/02.2-07-clean-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-07-clean-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-07-clean-pvc-phase.txt || true
    # Cascade-5-specific evidence — verify credproxy-test namespace now has the full hierarchy AND the Pod actually dispatched
    kubectl --context kind-tide-test get all -n credproxy-test 2>&1 | tee /tmp/02.2-07-clean-credproxy-ns.txt || true
    kubectl --context kind-tide-test get project,milestone,phase,plan,task -n credproxy-test 2>&1 | tee /tmp/02.2-07-clean-credproxy-crds.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block from the captured log — APPROVED case shows 7 Pass / 0 Skip / 0 Fail; BLOCKED case shows 0/7 PASS or a partial outcome). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-06-VERIFICATION.md` §"Section 1" Per-Spec Outcome Table.

    **Critically: capture the manager logs AND the credproxy-test namespace state.** The clean-run's `tide-controller-manager` Pod state is the load-bearing evidence:
    - **APPROVED case:** Pod `READY 1/1 STATUS Running RESTARTS ≤ 1`; manager logs show "starting manager"; NO `flag provided but not defined: -<any-flag>` line (all prior cascade fixes preserved); manager logs show a `dispatch` or `creating Job` log line for `credproxy-task` BEFORE any `task cleanup` line (proving the production dispatch path now fires); Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed`; credproxy-test namespace contains `Project`, `Milestone`, `Phase`, `Plan`, `Task` CRDs (the full hierarchy from the helper) AND a Pod for credproxy-task.
    - **BLOCKED case A — fixture refactor didn't close the gate (credproxy still fails):** Pod state same as clean (`Running 0 restarts`); credproxy-test namespace shows the full hierarchy WAS created (the helper landed correctly) BUT the Task still didn't dispatch — likely indicates a SECOND structural issue in the credproxy fixture (e.g., helper has a typo, project label mismatch between Task and Project, missing required Spec field) that the debug investigation didn't surface. Section 5 Root-Cause Summary classifies cascade-6 as `harness-bug` and proposes a targeted helper-fix plan.
    - **BLOCKED case B — credproxy specs now pass but a downstream spec (wave/failure/caps/output) fails for the first time since Phase 02.1:** Pod state Running; manager-log activity confirms credproxy-task dispatched and succeeded; the Ginkgo summary shows 2/7 pass + 5/7 with a new failure mode. Section 5 Root-Cause Summary classifies cascade-6 using T-02.2-19's taxonomy (one of `spec-flake | harness-bug | kind-infra | other`) AND identifies the specific spec that failed AND proposes Plan 02.2-08's fix shape.
    - **BLOCKED case C — entirely new structural cascade (cascade-6 different bug class):** any other Pod state OR manager-log activity OR Ginkgo output that reveals a different failure shape — image-pull failure on stub-subagent, etcd race, kind networking quirk, etc. Section 5 Root-Cause Summary classifies cascade-6 per T-02.2-19's enum.

    **Sub-step B — Idempotent Re-run:**
    ```bash
    # Pre-state: tide-test cluster + cert-manager + tide release REVISION 1 persisted from clean run.
    # (KEEP_KIND_CLUSTER=true preserves the AfterSuite skip from Phase 02.1 D-03)
    set -o pipefail
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-07-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    **After rerun:** capture same diagnostic snapshot (`/tmp/02.2-07-rerun-*.txt`) for the rerun:
    ```bash
    kubectl --context kind-tide-test get pods -A 2>&1 | tee /tmp/02.2-07-rerun-pods.txt || true
    kubectl --context kind-tide-test get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager -o wide 2>&1 | tee /tmp/02.2-07-rerun-controller-pod.txt || true
    helm history tide -n tide-system 2>&1 | tee /tmp/02.2-07-rerun-helm-history.txt || true
    kubectl --context kind-tide-test logs -n tide-system deployment/tide-controller-manager --since=20m 2>&1 | tee /tmp/02.2-07-rerun-manager-logs.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}' 2>&1 | tee /tmp/02.2-07-rerun-pvc-accessmodes.txt || true
    kubectl --context kind-tide-test get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}' 2>&1 | tee /tmp/02.2-07-rerun-pvc-phase.txt || true
    kubectl --context kind-tide-test get all -n credproxy-test 2>&1 | tee /tmp/02.2-07-rerun-credproxy-ns.txt || true
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes. Verify the helm install on rerun succeeded due to `--replace` (Plan 02.2-01's fix). Verify the cluster was NOT deleted by AfterSuite (T-02.2-02 mitigation; re-verified at runtime). Verify the cascade-3 webhook-cert-path fix preserved (no `flag provided but not defined` errors).

    **Sub-step C — Author the artifact:**
    Write `02.2-07-VERIFICATION.md` mirroring `02.2-06-VERIFICATION.md`'s structural template. Required frontmatter (note `plan: 07`, `task: 2`; once Sub-step C completes, update the PRELIMINARY `gate_decision: PENDING` line to APPROVED or BLOCKED):
    ```
    ---
    phase: 02.2
    plan: 07
    task: 2
    artifact: verification
    captured_on: <YYYY-MM-DD>
    captured_by_host: <host>
    captured_by_branch: <git branch / worktree name>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary including the cascade type if BLOCKED>
    ---
    ```

    Required sections (mirror `02.2-06-VERIFICATION.md` headers verbatim):
    - **Section 1 — Clean Start Run** (reproducer + per-spec outcome table + wall-time + exit + Pod-state evidence + manager-log evidence as the load-bearing diagnostic + credproxy-test namespace CRD listing showing the full hierarchy now lands)
    - **Section 2 — Idempotent Re-run** (reproducer + per-spec outcome table + wall-time + exit + rerun-specific evidence — both runs are required for an APPROVED gate)
    - **Section 3 — Before/After Comparison** (table comparing Plan 02.2-06 BLOCKED outcome (0/7 PASS, 2/7 FAIL on credproxy, 5/7 NOT RUN) vs Plan 02.2-07 outcomes on clean + rerun; particularly: did the fixture refactor close the cascade-5 BLOCKED gate, and what is the NEW failure mode if BLOCKED again — what cascade type per T-02.2-19)
    - **Section 4 — Wall-time Budget Check** (same table shape as `02.2-06-VERIFICATION.md` §4; both runs must show `go test ≤ 600s` for APPROVED; ADDITIONAL row required per T-02.2-14 mitigation carry-forward: flag `budget headroom marginal` if observed wall ≥ 500s even on APPROVED so a future plan can preemptively escalate to Layer A / Layer B split targets)
    - **Section 5 — Gate Decision** (the APPROVED-requires-ALL table with 5 conditions on BOTH runs + Required Next Step + Root-Cause Summary if BLOCKED + Fix landscape if BLOCKED. **CRITICAL**: This section MUST explicitly cite `.planning/debug/credproxy-dispatch-guard.md` §Reasoning Checkpoint and MUST NOT inherit the wrong "parent Wave Ready check" hypothesis from `02.2-06-VERIFICATION.md` §Section 5. The corrected gate (`resolveProject` Project-in-namespace lookup at `task_controller.go:217`) is the framing.)
    - **Section 6 — Threat-Model Mitigations Applied** (T-02.2-17 / T-02.2-18 / T-02.2-19 / T-02.2-20 each with evidence + carry-forward T-02.2-01 through T-02.2-16 noted as preserved)
    - **"What This Artifact Did Not Touch"** closing section
    - **"Closing Prose"** with one-paragraph summary including the corrected root cause framing

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `test/`, `.github/` (Task 1's test-fixture edits are already committed before this task runs; the runtime reproducer only READS from the codebase, doesn't WRITE). Task 3 edits Plan 02.2-02 (a planning artifact, not source) — and only fires if `gate_decision: APPROVED`. Document this explicitly in the artifact's "What This Artifact Did Not Touch" closing section.

    **APPROVED gate (5-of-5 must hold on BOTH clean run AND idempotent rerun):**
    1. **Wall-time ≤ 600s:** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` (the Plan 02.2-06 budget preserved) AND no `timeout 600s` exit-124 line appears in either captured log on the go test step
    2. **Manager Pod state:** `tide-controller-manager` Pod `STATUS=Running` with `RESTARTS ≤ 1` (Plan 02.2-05's cascade-3 fix preserved — no CrashLoopBackOff)
    3. **Manager log clean:** zero `flag provided but not defined` errors of any kind in the captured manager-log excerpts (all 5 chart args defined per Plan 02.2-05's flag-mismatch cascade chain termination)
    4. **PVC preserved:** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO` (Plan 02.2-03's PVC fix preserved)
    5. **7/7 Layer B Ginkgo specs PASS:** Ginkgo summary line `Ran 7 Specs in <X>s` with `0 Failed` AND the per-spec outcome table records each of the 7 specs as PASS on both clean run AND rerun — **this is achievable for the first time in the cascade chain because all 5 prior cascades are closed (PVC RWX→RWO; metrics-bind-address flag; webhook-cert-path flag; test-budget; fixture-incomplete after Task 1 lands)**

    **BLOCKED gate fires if ANY of the 5 conditions fail on either run.** The artifact's §"Section 5 Root-Cause Summary" documents WHICH gate failed, with raw log evidence, and §"Fix landscape" enumerates the next-iteration options based on cascade-6 classification:
    - **Option A** (helper-fix): if cascade-6 is `harness-bug` AND the credproxy specs still fail (BLOCKED case A above), inspect the helper's CRD shapes against `testdata/three-task-wave.yaml` field-by-field to find the divergence (T-02.2-17 mitigation tightening).
    - **Option B** (spec-level fix): if cascade-6 is `spec-flake` AND a downstream spec (wave/failure/caps/output) failed for the first time (BLOCKED case B above), identify the specific spec from the Ginkgo summary's `Failed` list and route to a targeted fix plan in `test/integration/kind/<spec>_test.go`.
    - **Option C** (harness-level fix): if cascade-6 is `kind-infra` (image-pull, etcd race, networking) or a different `harness-bug`, route to `test/integration/kind/suite_test.go` (BeforeSuite/AfterSuite) or to kind configuration (`cluster.yaml`, `test-int-kind-prep`'s image-load step).
    - **Option D** (escalation to production-source change): if the fixture refactor is structurally correct but the production dispatch path still has a bug — route to a targeted fix in `internal/controller/task_controller.go` (NOT testMode shortcut — a real production-path bug fix).

    Each option in the BLOCKED §"Fix landscape" MUST include the cascade-6 classification (one of `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`) per T-02.2-19 acceptance.

    Task 3 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring — Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3 also did NOT fire).

    **If gate_decision = APPROVED:** Skip the Root-Cause Summary section. Note in the closing prose that Plan 02.2-02 (closeout) is unblocked once Task 3 wires its checkpoint to read this artifact, and that the cascade chain has terminated successfully after 5 tactical iterations (02.2-03 chart PVC, 02.2-04 metrics flag, 02.2-05 webhook-cert-path flag, 02.2-06 Makefile timeout, 02.2-07 credproxy fixture). T-02.2-14 mitigation carry-forward requires checking observed wall: if either run's wall ≥ 500s, the Section 4 table MUST contain the literal token `budget headroom marginal` and the closing prose MUST flag that preemptive escalation to Layer A / Layer B split targets for a future plan would be prudent even though this gate APPROVED.

    **Section 5 framing — load-bearing corrected root cause:** The artifact's §Section 5 prose MUST cite `.planning/debug/credproxy-dispatch-guard.md` and explicitly correct the prior framing. Suggested paragraph (adapt for clean/BLOCKED outcomes):
    > "Cascade-5 was originally hypothesized in `02.2-06-VERIFICATION.md` §Section 5 as a 'parent Wave Ready check' gating dispatch. That hypothesis was WRONG. The corrected root cause (from `.planning/debug/credproxy-dispatch-guard.md` §Reasoning Checkpoint) is that `task_controller.go:217` (Step 3 of `reconcileDispatch`) calls `resolveProject(ctx, task)` which returns `'no project found in namespace credproxy-test'` because the credproxy fixture creates only Plan+Task without an accompanying Project CRD. Zero references to `Wave`, `Milestone`, or `Phase` CRDs exist in the entire `task_controller.go` dispatch chain — the Wave CRD is purely observational. The 'task cleanup' manager-log line at 60s post-Task-create is a downstream symptom of `AfterEach`'s `deleteNamespace` triggering the finalizer path, NOT evidence of dispatch routing. This plan's Task 1 fix (adding the full hierarchy via `applyHierarchy` helper) addresses the corrected gate directly: the helper's Project CRD satisfies `resolveProject`'s label fast-path AND its ProjectList fallback."

    Commit with one of:
    ```
    docs(02.2-07): runtime verification — APPROVED gate (cascade-5 fixture closed; 7/7 Layer B specs PASS clean + rerun within 600s)
    ```
    OR
    ```
    docs(02.2-07): runtime verification — BLOCKED gate (credproxy fixture landed; cascade-6 surfaced: <cascade-type-and-summary>)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists at the correct path
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md ] && \
      # Frontmatter required fields
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 07' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^task: 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^artifact: verification' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -eq 1 ] && \
      # Gate decision is one of APPROVED or BLOCKED (PENDING is not acceptable as a final state)
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      # Required structural sections exist
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 3' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 4' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 6' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times (both runs)
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      grep -E '^rerun_exit: -?[0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      # Threat-model mitigations applied section exists
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-17/18/19/20 all referenced
      [ "$(grep -c 'T-02.2-17' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-18' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-19' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-20' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      # Cascade classification present (per T-02.2-19 acceptance) — one of the six classes must appear
      grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      # Section 5 corrected-root-cause framing — MUST cite the debug document and MUST mention resolveProject
      grep -F 'credproxy-dispatch-guard.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      grep -F 'resolveProject' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md && \
      # Manager-log evidence captured (the load-bearing diagnostic for APPROVED vs BLOCKED)
      [ "$(grep -c 'starting manager\|flag provided but not defined\|tide-controller-manager\|task cleanup\|dispatch' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      # Pod-state evidence captured (load-bearing per plan-checker tightening)
      [ "$(grep -c 'CrashLoopBackOff\|Running\|RESTARTS\|kubectl.*get pod' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      # 600s budget referenced (carry-forward from Plan 02.2-06)
      [ "$(grep -c '600s\|≤ 600\|<= 600' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -ge 1 ] && \
      # No production source modified by this task (Task 1's test-fixture edits are pre-committed; Task 3 has not fired)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-07-VERIFICATION.md' | grep -v '^$' && echo "FAIL: non-artifact files modified beyond Task 1's pre-committed edits" && exit 1 || \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-07-VERIFICATION.md ]` returns true
    - **Frontmatter has required fields:** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 07' <artifact>` returns 1 AND `grep -c '^task: 2' <artifact>` returns 1 AND `grep -c '^artifact: verification' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches (PENDING is not a final state)
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: -?[0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' <artifact>` matches (rerun_exit allows -1 to denote skipped, but APPROVED case requires both = 0)
    - **All six structural sections present:** each `grep -c '^## Section [1-6]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all four new threats referenced:** `grep -c 'Threat-Model Mitigations Applied' <artifact>` ≥ 1 AND `grep -c 'T-02.2-17' <artifact>` ≥ 1 AND `grep -c 'T-02.2-18' <artifact>` ≥ 1 AND `grep -c 'T-02.2-19' <artifact>` ≥ 1 AND `grep -c 'T-02.2-20' <artifact>` ≥ 1
    - **Cascade classification present (T-02.2-19 mitigation):** `grep -E 'flag-mismatch|budget-vs-actual|spec-flake|harness-bug|kind-infra|other' <artifact>` matches at least once — the artifact classifies the cascade type observed (or notes "cascade chain terminated" with no cascade-6 surfaced if APPROVED)
    - **Section 5 cites the corrected root cause (load-bearing — DO NOT inherit the wrong "Wave Ready check" framing from `02.2-06-VERIFICATION.md`):** `grep -F 'credproxy-dispatch-guard.md' <artifact>` matches AND `grep -F 'resolveProject' <artifact>` matches (the artifact's §Section 5 explicitly cites the debug document AND names the actual gate function)
    - **Manager-log evidence captured (load-bearing for gate decision):** the artifact includes a verbatim excerpt of `kubectl logs deploy/tide-controller-manager` from the clean run — APPROVED case shows "starting manager" + a `dispatch` line for credproxy-task BEFORE any `task cleanup` line + 7-spec Ginkgo summary; BLOCKED case shows evidence of the new failure shape
    - **Pod-state evidence captured (load-bearing per plan-checker tightening):** the artifact includes verbatim output of `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` showing either `STATUS=Running RESTARTS ≤ 1` (APPROVED) or evidence of a different failure (BLOCKED)
    - **600s budget referenced (carry-forward from Plan 02.2-06):** `grep -c '600s\|≤ 600\|<= 600' <artifact>` ≥ 1 (the artifact references the preserved 600s budget)
    - **Behavior gate (APPROVED case — ALL 5 conditions on BOTH runs must hold):**
      - (a) Clean run captured `EXIT=0` AND 7 Layer B specs marked PASS in the per-spec table
      - (b) Rerun captured `EXIT=0` AND 7 Layer B specs marked PASS
      - (c) **Wall-time budget (LOAD-BEARING):** `clean_run_wall_seconds ≤ 600` AND `rerun_wall_seconds ≤ 600` AND no `timeout 600s` exit-124 line appears in either captured log
      - (d) Manager logs show "starting manager" AND zero `flag provided but not defined` errors of any kind AND a `dispatch` or `creating Job` line for `credproxy-task` BEFORE any `task cleanup` line (proving the production dispatch path now fires for credproxy-task)
      - (e) **Pod-state (LOAD-BEARING):** `kubectl get pod -n tide-system -l app.kubernetes.io/name=tide-controller-manager` shows `STATUS=Running` AND restart count ≤ 1
      - (f) **PVC preservation (Plan 02.2-03 fix not regressed):** `kubectl get pvc tide-projects -n tide-system` shows `STATUS=Bound`, `ACCESS MODES=RWO`
      - (g) **T-02.2-14 marginal-headroom check (carry-forward):** if `clean_run_wall_seconds ≥ 500` OR `rerun_wall_seconds ≥ 500`, the artifact's §Section 4 table contains the literal token `budget headroom marginal` and the closing prose flags preemptive escalation as prudent (note: marginal-headroom does NOT block APPROVED, but must be surfaced)
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, a `Root-Cause Summary` section MUST exist with numbered analysis points mirroring `02.2-06-VERIFICATION.md` §"Root-Cause Summary" — particularly: (a) confirmation that Plan 02.2-07 Task 1's fixture refactor IS in effect (`grep -c 'func applyHierarchy' test/integration/kind/suite_test.go` ≥ 1 AND `grep -c 'applyHierarchy(' test/integration/kind/credproxy_test.go` ≥ 2 evidence cited); (b) which cascade-6 surfaced (one of the T-02.2-19 enum); (c) why this was or was NOT predictable; (d) fix-landscape options for Plan 02.2-08 (Options A–D enumerated per the action body)
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD)'` shows only the new `02.2-07-VERIFICATION.md` artifact (Task 1's test-fixture edits must be committed BEFORE this task runs; Task 3 has not fired yet)
  </acceptance_criteria>

  <done>
    `02.2-07-VERIFICATION.md` exists at the correct path with the structural template mirroring `02.2-06-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, and `gate_decision: APPROVED` or `gate_decision: BLOCKED` (PENDING is not a final state). Sections 1-6 captured with per-spec outcome tables, Pod-state evidence, manager-log evidence (load-bearing for APPROVED vs BLOCKED), credproxy-test namespace CRD listing (showing the full hierarchy now lands), before/after comparison vs `02.2-06-VERIFICATION.md`, wall-time budget check (with T-02.2-14 marginal-headroom carry-forward flag if observed wall ≥ 500s), and threat-model mitigations (T-02.2-17/18/19/20 + carry-forward T-02.2-01 through T-02.2-16). §Section 5 explicitly cites `.planning/debug/credproxy-dispatch-guard.md` and names `resolveProject` as the actual gate — does NOT inherit the wrong "parent Wave Ready check" framing from `02.2-06-VERIFICATION.md`. If APPROVED, all 5 APPROVED-gate conditions hold on BOTH clean run AND rerun: both runs exit 0 with 7/7 specs PASS, both ≤ 600s, no `flag provided but not defined` errors, Pod STATUS=Running with restart ≤ 1, PVC Bound RWO, manager log shows `dispatch` for credproxy-task BEFORE any `task cleanup`. Task 3 fires to wire Plan 02.2-02's checkpoint. If BLOCKED, Task 3 does NOT fire and the failure routes to a planner re-evaluation per the wave-failure-semantics contract — with §Section 5 Root-Cause Summary classifying cascade-6 per T-02.2-19's enum AND naming the specific spec(s) that failed AND proposing Plan 02.2-08's fix shape. Committed with a `docs(02.2-07): runtime verification — APPROVED gate (cascade-5 fixture closed; 7/7 Layer B specs PASS clean + rerun within 600s)` or `docs(02.2-07): runtime verification — BLOCKED gate (credproxy fixture landed; cascade-6 surfaced: <type>)` message.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Plan 02.2-02 to reference 02.2-07-VERIFICATION.md + bump depends_on (gated on Task 2 APPROVED gate)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file — 382 lines; frontmatter at lines 1-40 — particularly line 6 `depends_on: ["02.2-01"]`, lines 31-39 `key_links` block, lines 22-26 `must_haves.truths` block; `<context>` block at lines 63-68; Task 1 body at lines 72-136 — `<read_first>` at lines 77-79, `<what-built>` at lines 81-83, `<how-to-verify>` at lines 85-101, `<verify>` automated block at lines 117-123, `<acceptance_criteria>` at lines 125-129, `<done>` at lines 131-135 — all of which currently reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, and 02.2-06 Task 3 did NOT fire)
    - .planning/phases/02.2-.../02.2-07-VERIFICATION.md (the gate artifact produced by Task 2 above — `gate_decision: APPROVED` is the HARD precondition for this Task 3 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
    - .planning/phases/02.2-.../02.2-06-PLAN.md Task 3 body (the structural model for this task's edits; mirror its surgical-edit pattern with `02.2-06` substituted for `02.2-07`)
    - CLAUDE.md "Failure semantics at wave boundaries" contract — the load-bearing logic that this task fires ONLY on APPROVED; on BLOCKED, this task halts and Plan 02.2-02 stays gated on its current `02.2-01-VERIFICATION.md` wiring
  </read_first>

  <action>
    **Gate precondition (HARD GATE):** Task 3 fires ONLY IF Task 2's `02.2-07-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED` (or PENDING, which is not a final state), halt Task 3 — Plan 02.2-02 stays unmodified (it will continue to reference `02.2-01-VERIFICATION.md` because Plans 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, and 02.2-06 Task 3 also did NOT fire); the BLOCKED failure routes to a planner re-evaluation per the wave-failure-semantics contract from CLAUDE.md.

    Verify the precondition by running:
    ```bash
    grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md
    ```
    If this returns 0 (no APPROVED match), halt Task 3 and surface the BLOCKED gate. Exit with a clear message: "Task 3 halted: 02.2-07-VERIFICATION.md gate_decision is not APPROVED — routing back to planner for next iteration (cascade-6 or whatever surfaced)."

    If APPROVED (returns exactly 1), make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-07-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering. There are 31 references to `02.2-01-VERIFICATION.md` in `02.2-02-PLAN.md` (verified at plan-time by `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returning exactly 31); all 31 are redirected to `02.2-07-VERIFICATION.md` in one pass.

    **NOTE on recovery state:** Plan 02.2-03 Task 4, 02.2-04 Task 3, 02.2-05 Task 3, 02.2-06 Task 3 each tried to do the same surgical edit (each pointing 02.2-02-PLAN.md at its own VERIFICATION.md), but none fired because each Task 2 gate was BLOCKED on the cascade-of-the-day. As a consequence, `02.2-02-PLAN.md` STILL references `02.2-01-VERIFICATION.md` at all 31 of its read paths today (verified during plan authoring of THIS plan). This task's edits land the long-pending rewire, redirecting to `02.2-07-VERIFICATION.md`.

    **Pre-edit count assertion (BEFORE any edits):** Run `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md` and confirm the result is exactly `31`. If it is NOT 31 (e.g., some prior task fired partially OR a checker rewrote the file OR a developer manually edited it), HALT — the rewire scope assumed at plan-time has drifted and the task needs planner re-evaluation. If exactly 31, proceed with edits.

    **Edit A — frontmatter `depends_on` (line 6 of 02.2-02-PLAN.md):** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-07"]`. This is a single one-line change. The new dependency captures the gate-artifact dependency directly (Plan 02.2-07 produces the gate; Plan 02.2-02 reads it). The transitive dependency on Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06 (which Plan 02.2-07 depends on transitively) is implicit in execute-phase's wave-ordering logic.

    **Edit B — frontmatter `must_haves.truths` block (lines 22-26 of 02.2-02-PLAN.md):** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-07-VERIFICATION.md`. Specifically the bullet currently reads "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → change to "Closeout fires ONLY when 02.2-07-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit C — frontmatter `key_links` block (lines 31-39 of 02.2-02-PLAN.md):** Update the entry that references `02.2-01-VERIFICATION.md` to reference `02.2-07-VERIFICATION.md`. Concretely: change the `from:` line's path from `02.2-01-VERIFICATION.md` to `02.2-07-VERIFICATION.md` and keep the rest of the entry unchanged.

    **Edit D — `<context>` block (line 66 of 02.2-02-PLAN.md):** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-07-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>` (lines 77-79 of 02.2-02-PLAN.md):** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-07-VERIFICATION.md (the gate artifact produced by Plan 07 Task 2 — Phase 02.2-07 closed the BLOCKED runtime gate from 02.2-06-VERIFICATION.md by adding a shared applyHierarchy test helper and refactoring credproxy_test.go to apply the full Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy, satisfying the task controller's resolveProject Project-in-namespace lookup at task_controller.go:217 that the previous inline Plan+Task-only fixture was tripping; this allowed all 7 Layer B Ginkgo specs to PASS on clean + idempotent rerun within the 600s outer budget for the first time in the cascade chain)`. Update the analog Phase 02.1 reference if present.

    **Edit F — Task 1 `<what-built>` (lines 81-83 of 02.2-02-PLAN.md):** Update the prose to reference Plan 02.2-07's deliverable (test fixture refactor + shared helper + runtime re-verification) in addition to Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06's deliverables. Specifically append: "Plan 02.2-07 then added a shared `applyHierarchy(ctx, ns, planName, taskName)` helper to `test/integration/kind/suite_test.go` and refactored `test/integration/kind/credproxy_test.go` to apply the full Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy mirroring `test/integration/kind/testdata/three-task-wave.yaml`, closing the cascade-5 harness-bug (the credproxy fixture was structurally incomplete relative to the production dispatch path's resolveProject Project-in-namespace lookup at task_controller.go:217) and allowing the 7-spec end-to-end suite to PASS on both clean run and idempotent rerun within the 600s budget."

    **Edit G — Task 1 `<how-to-verify>` (lines 85-101 of 02.2-02-PLAN.md):** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-07-VERIFICATION.md`. There are multiple occurrences in the numbered steps; update each.

    **Edit H — Task 1 `<verify>` automated block (lines 117-123 of 02.2-02-PLAN.md):** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-07-VERIFICATION.md`. Two grep paths need bumping.

    **Edit I — Task 1 `<acceptance_criteria>` (lines 125-129 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-07-VERIFICATION.md`.

    **Edit J — Task 1 `<done>` (lines 131-135 of 02.2-02-PLAN.md):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-07-VERIFICATION.md` if any are present.

    **Post-edit count assertion (AFTER all edits):** Run `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `0`. Run `grep -c '02.2-07-VERIFICATION.md' 02.2-02-PLAN.md` and confirm the result is exactly `31`. If either count is not as expected, the redirection drifted — surface the diff and halt before commit.

    **(Edits to ROADMAP/STATE content NOT performed by this task — Plan 02.2-02 reads live ROADMAP/STATE at execution time via its own `<read_first>` and `<action>` blocks and will derive the current 7-plan / 6-done structure. Prescriptive content edits from THIS plan would (a) write a snapshot that is stale by the time Plan 02.2-02 runs, and (b) leak Plan 02.2-07's responsibilities into Plan 02.2-02's body. Plan 02.2-02 is responsible for its own ROADMAP/STATE shape decisions at execution time.)**

    **Constraints:**
    - Do NOT edit Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06, or 02.2-07 (this plan) — only `02.2-02-PLAN.md` is touched.
    - Do NOT touch any production source under `cmd/`, `internal/`, `api/`, `pkg/`, `charts/`, `Makefile`, `.github/`, `test/`. This task is documentation-only.
    - Do NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — those are owned by Plan 02.2-02's tasks and by the orchestrator.
    - The diff against `02.2-02-PLAN.md` must be minimal — only the 31 references to `02.2-01-VERIFICATION.md` are changed to `02.2-07-VERIFICATION.md`, plus the one `depends_on` value change. No new tasks added, no existing tasks removed, no structural reorganization, no plan-list/decisions edits.

    Commit with message:
    ```
    docs(02.2-07): wire Plan 02.2-02 checkpoint to 02.2-07-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-07; 31 references redirected)
    ```
  </action>

  <verify>
    <automated><![CDATA[
      # Gate precondition: 02.2-07-VERIFICATION.md gate_decision is APPROVED (else this task should have been halted)
      [ "$(grep -cE '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-VERIFICATION.md)" -eq 1 ] && \
      # Edit A: depends_on bumped to 02.2-07
      [ "$(grep -cE 'depends_on: \[.{0,5}02.2-07.{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 1 ] && \
      # Old depends_on removed (must not contain 02.2-01, 02.2-03, 02.2-04, 02.2-05, or 02.2-06 as the active depends_on value)
      [ "$(grep -cE '^depends_on: \[.{0,5}02.2-0[13456].{0,5}\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-07-VERIFICATION.md is now the gate reference — count should be exactly 31 (one-for-one rewire)
      [ "$(grep -c '02.2-07-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 31 ] && \
      # 02.2-01-VERIFICATION.md references purged from 02.2-02-PLAN.md (count must be exactly 0)
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # 02.2-03/04/05/06-VERIFICATION.md references purged (those Task 4/3/3/3 didn't fire, so they were never there; assert anyway)
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-04-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-05-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      [ "$(grep -c '02.2-06-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # Task 1 checkpoint grep gate uses the new path
      grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-07-VERIFICATION.md" .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # Confirm no other plans were modified by this task (regression guard)
      git status --porcelain | grep -E '^( M|MM| D|MD)' | grep -v '02.2-02-PLAN.md' | grep -v '^$' && echo "FAIL: non-target plan files modified" && exit 1 || \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Gate precondition met (HARD GATE):** `grep -cE '^gate_decision: APPROVED$' 02.2-07-VERIFICATION.md` returns exactly 1 (Task 3 ran ONLY because Task 2's gate was APPROVED; otherwise Task 3 was halted before edits landed)
    - **depends_on bumped (Edit A):** `grep -cE 'depends_on: \[.{0,5}02.2-07.{0,5}\]' 02.2-02-PLAN.md` returns exactly 1 AND `grep -cE '^depends_on: \[.{0,5}02.2-0[13456].{0,5}\]' 02.2-02-PLAN.md` returns 0 (old values 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06 all purged from the depends_on field)
    - **New gate-artifact reference count (Edits B-J):** `grep -c '02.2-07-VERIFICATION.md' 02.2-02-PLAN.md` returns exactly 31 (one-for-one redirection from the 31 plan-time `02.2-01-VERIFICATION.md` references; tighter than `02.2-06-PLAN.md`'s `≥ 25` because the rewire scope is now well-characterized after multiple BLOCKED gates left the original 31 references intact)
    - **Old gate-artifact references purged:** `grep -c '02.2-01-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-03-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-04-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-05-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 AND `grep -c '02.2-06-VERIFICATION.md' 02.2-02-PLAN.md` returns 0 (no stale references to any prior gate artifact)
    - **Task 1 `<verify>` automated grep targets the new path:** the regex `grep -E "grep -E '\^gate_decision: \(APPROVED.{0,5}\).{0,5}\$' .*02.2-07-VERIFICATION.md"` matches inside `02.2-02-PLAN.md`
    - **No other plans or source modified (regression guard):** `git status --porcelain` shows ONLY `02.2-02-PLAN.md` modified in this commit; no edits to `02.2-01-PLAN.md`, `02.2-03-PLAN.md`, `02.2-04-PLAN.md`, `02.2-05-PLAN.md`, `02.2-06-PLAN.md`, `02.2-07-PLAN.md` (this plan), or any production source
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has been surgically edited to redirect its checkpoint from `02.2-01-VERIFICATION.md` to `02.2-07-VERIFICATION.md`, with `depends_on` bumped from `["02.2-01"]` to `["02.2-07"]`. All 31 prior `02.2-01-VERIFICATION.md` references are now `02.2-07-VERIFICATION.md` (one-for-one, count exactly 31). No stale `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, `02.2-05-VERIFICATION.md`, or `02.2-06-VERIFICATION.md` references exist. No other plans or production source are modified. Committed with the `docs(02.2-07): wire Plan 02.2-02 checkpoint to 02.2-07-VERIFICATION.md (depends_on bump 02.2-01 -> 02.2-07; 31 references redirected)` message.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/integration/kind/suite_test.go` (new `applyHierarchy` helper) → `test/integration/kind/credproxy_test.go` (call sites) | The helper is the shared apply primitive consumed by credproxy specs (and potentially future minimal-fixture tests). Its CRD shapes MUST mirror `testdata/three-task-wave.yaml` field-for-field to avoid creating a SECOND structural mismatch (a different cascade hiding inside the helper). |
| `applyHierarchy` helper → `kubectl apply` (via existing `applyYAML` helper) | The helper constructs multi-doc YAML and passes it to the existing apply primitive at suite_test.go:486. The boundary is the YAML serialization correctness — a typo in field names (`targetRepo` vs `target-repo`, `providerSecretRef` vs `provider-secret-ref`) would cause the Project CRD to be invalid, leading to a SECOND failure mode that this plan must not introduce. |
| `applyHierarchy` helper's Project CRD → `internal/controller/task_controller.go:217 resolveProject` | The helper's Project + Task label preset are designed to satisfy BOTH paths of `resolveProject` (the fast-path label lookup at line 533 AND the ProjectList fallback at lines 540-547). If the helper's Task label key has a typo (`tideproject.k8s/projecT` vs `tideproject.k8s/project`), the fast path misses but the fallback succeeds — masking a real bug. Belt-and-suspenders is intentional. |
| Task 1 (helper + fixture refactor) → Task 2 (runtime verification) | Task 2 is run after Task 1's commits land; the runtime reproducer exercises the new fixture end-to-end against the cascade-1-through-4-fixed binary + chart from Plans 02.2-01 through 02.2-06. |
| Task 2 (gate artifact) → Task 3 (Plan 02.2-02 wiring) | Task 3 reads Task 2's `gate_decision:` frontmatter. APPROVED → Task 3 fires; BLOCKED → Task 3 halts and the BLOCKED failure routes to a planner re-evaluation. |
| Plan 02.2-07 Task 3 edits → Plan 02.2-02 checkpoint target | Surgical edit only — `depends_on` and 31 verification-artifact references in `02.2-02-PLAN.md` are bumped; no other planning artifacts touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-17 | Tampering | The new `applyHierarchy` helper might subtly diverge from `testdata/three-task-wave.yaml`'s working hierarchy (e.g., wrong owner-ref direction, missing required Spec field, wrong API version, typo in label keys), causing the credproxy test to fail for a DIFFERENT reason than the production dispatch path. This would manifest as a BLOCKED Task 2 gate where the credproxy specs still fail at 60s timeout but the credproxy-test namespace now shows Project+Milestone+Phase+Plan+Task CRDs created — indicating the helper landed but its shape is wrong. Without explicit field-by-field equivalence verification, this could pass plan-time review but fail runtime. | mitigate | The helper's CRD shapes MUST be structurally identical to `testdata/three-task-wave.yaml`'s — verified field-by-field at plan-time (in the `<interfaces>` block above) and at runtime (via Task 1's acceptance gates). **Acceptance gate:** the helper produces CRDs whose API versions are `tideproject.k8s/v1alpha1` (verified by `grep -c 'tideproject.k8s/v1alpha1' suite_test.go ≥ 1`), kinds include all six of Secret + Project + Milestone + Phase + Plan + Task (verified by separate `grep -c 'kind: <X>' ≥ 1` for each), and required Spec fields are present (Project.spec.targetRepo + providerSecretRef + budget.absoluteCapCents; Milestone.spec.projectRef; Phase.spec.milestoneRef; Plan.spec.phaseRef + labels.\"tideproject.k8s/project\"; Task.spec.planRef + filesTouched + declaredOutputPaths + labels.\"tideproject.k8s/project\" + labels.\"tideproject.k8s/wave-index\"). At runtime, Task 2's diagnostic snapshot captures the credproxy-test namespace's CRD listing — if the namespace shows fewer than 5 of the 5 tide CRDs (Project, Milestone, Phase, Plan, Task), the helper is structurally incomplete and Task 2 records BLOCKED with cascade-6 classified as `harness-bug` and §Section 5 pinpoints the missing CRD. |
| T-02.2-18 | Tampering | The helper creates a Secret CRD that the Project's `providerSecretRef` field names. If the helper creates the Secret with the wrong name (`tide-provider-secret` vs `tide-project-secret`), wrong data key (`ANTHROPIC_API_KEY` vs `anthropic-api-key`), wrong type (Opaque vs kubernetes.io/tls), or wrong namespace (cross-namespace), the Project will be invalid OR `resolveProject` might succeed but a downstream gate (Step 6 rate-limit Pattern 1 at task_controller.go:260-305) will fail because it expects `project.Spec.ProviderSecretRef` to resolve to a real Secret. | mitigate | The helper creates the Secret with the exact same key/value structure as `testdata/three-task-wave.yaml`'s Secret (lines 11-18 of testdata/three-task-wave.yaml): name `tide-provider-secret`, namespace = the `ns` arg, `type: Opaque`, `data.ANTHROPIC_API_KEY` = the base64 placeholder `dGVzdC1hcGkta2V5LXN0dWItc3ViYWdlbnQtZG9lcy1ub3QtdXNlLWl0`. The value is irrelevant because stub-subagent makes zero outbound calls (credproxy_test.go:27 package comment), but the structural fields are load-bearing. **Acceptance gate:** `grep -c 'kind: Secret' test/integration/kind/suite_test.go` returns ≥ 1 after the helper lands AND the Secret YAML in the helper carries `type: Opaque` AND the Secret's namespace matches the Project's namespace (both parameterized by the `ns` argument — verified at runtime by Task 2's diagnostic snapshot showing the Secret exists in the credproxy-test namespace). |
| T-02.2-19 | Denial of Service | Tactical scope means cascade-6 (if any) is genuinely unknown — all five prior cascades have been distinct classes (PVC RWX→RWO, metrics-bind-address flag, webhook-cert-path flag, test-budget, fixture-incomplete). Cascade-6 could be: (a) a real spec-flake in wave/failure/caps/output (none have run end-to-end since Phase 02.1; flakiness might surface for the first time); (b) a kind-infra issue (image-pull failure on stub-subagent, etcd race, networking quirk); (c) a stub-subagent image regression; (d) a budget-vs-actual reissue if the helper's six-CRD hierarchy takes meaningfully longer than the inline two-CRD fixture to reconcile, pushing total wall ≥ 600s; (e) something genuinely unforeseeable. The accepted risk is that even with the corrected fixture, the runtime gate may BLOCK again with cascade-6 — and unlike cascade-5 (which the debug investigation diagnosed precisely), cascade-6's shape is unforeseeable from plan-time. | accept | Accepted with the requirement that if Task 2's gate is BLOCKED, `02.2-07-VERIFICATION.md` §"Section 5 Root-Cause Summary" MUST classify the cascade type using one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other` AND identify which specific spec(s) failed AND propose Plan 02.2-08's fix shape from the start — so the next iteration is not a diagnostic round-trip. Note: a `flag-mismatch` classification at this point would be a regression of Plan 02.2-05's fix and would require immediate investigation; `budget-vs-actual` would indicate the helper's six-CRD apply is meaningfully slower than expected and route to Layer A / Layer B split targets (deferred Option B from Plan 02.2-05/06); `spec-flake` would surface real spec flakiness for the first time since Phase 02.1; `harness-bug` would indicate a second structural mismatch (the helper has a typo, T-02.2-17 mitigation activates); `kind-infra` would route to suite_test.go or cluster.yaml. **Acceptance gate:** if Task 2 records `gate_decision: BLOCKED`, the artifact contains one of the six classification tokens (`grep -E 'flag-mismatch\|budget-vs-actual\|spec-flake\|harness-bug\|kind-infra\|other' 02.2-07-VERIFICATION.md` matches) AND §Section 5 names the specific failure evidence (which spec failed, which log line surfaced the issue, etc.). If APPROVED, the artifact still references the cascade-6 enum in a "cascade chain terminated" closing note (no cascade-6 surfaced). |
| T-02.2-20 | Tampering | The helper signature `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` is intentionally minimal — parameterizing only namespace + Plan name + Task name. Future tests may want richer parameterization (custom Task.Spec.cmd, custom dependsOn graph, custom Project budget) and might be tempted to use copy-paste-mutate the helper into per-test variants. This would defeat the helper's reuse intent and create multiple subtly-divergent hierarchy creators — each a potential vector for a cascade-5-style structural mismatch. | accept | Accepted that the v1 helper is credproxy-shape-only; if future fixtures need richer parameterization, they extend the helper signature in a follow-up plan rather than copy-paste-mutate the helper. **Acceptance gate:** the helper's signature is exactly `func applyHierarchy(ctx context.Context, ns, planName, taskName string) error` (4-arg form) — verified by `grep -E 'func applyHierarchy\(ctx[[:space:]]+context\.Context,[[:space:]]+ns,[[:space:]]+planName,[[:space:]]+taskName[[:space:]]+string\)[[:space:]]+error' test/integration/kind/suite_test.go` matching. No struct-options form, no builder pattern; v1 is minimal-by-design. Future expansion is a follow-up plan, not this plan's scope. |

</threat_model>

<verification>
After all three tasks complete (or after Task 2 if its gate is BLOCKED):

**Source gates (Task 1):**
- `test/integration/kind/suite_test.go` has the `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` helper present (T-02.2-20)
- The helper creates Secret + Project + Milestone + Phase + Plan + Task with API group `tideproject.k8s/v1alpha1` (CLAUDE.md domain rule; T-02.2-17)
- The Secret has `type: Opaque` and the `ANTHROPIC_API_KEY` data key (T-02.2-18)
- The Task has both `tideproject.k8s/project` AND `tideproject.k8s/wave-index` labels preset (T-02.2-17)
- `test/integration/kind/credproxy_test.go` has both HARN-03 specs calling `applyHierarchy(...)` and zero inline `kind: Plan` or `kind: Task` literals
- `go build ./test/integration/kind/...` exits 0
- `go vet ./test/integration/kind/...` exits 0
- No production source modified (internal/controller/, api/v1alpha1/, cmd/manager/, charts/tide/, Makefile all untouched — Option B contract)
- Other test files untouched (wave_test.go, failure_test.go, caps_test.go, output_test.go, testdata/three-task-wave.yaml)

**Runtime gates (Task 2):**
- `02.2-07-VERIFICATION.md` exists with all required frontmatter fields populated (`phase: 02.2`, `plan: 07`, `task: 2`, `artifact: verification`, exit codes, wall seconds, spec counts)
- `gate_decision` is one of APPROVED or BLOCKED (PENDING not a final state)
- The artifact's Sections 1-6 mirror `02.2-06-VERIFICATION.md`'s structural template
- §Section 5 prose cites `.planning/debug/credproxy-dispatch-guard.md` AND names `resolveProject` as the actual gate (NOT the wrong "parent Wave Ready check" framing from 02.2-06's §Section 5)
- Manager-log evidence (load-bearing for gate decision) is captured verbatim in the artifact, including a `dispatch` or `creating Job` line for credproxy-task BEFORE any `task cleanup` line (APPROVED case)
- Pod-state evidence (load-bearing per plan-checker tightening) is captured: STATUS=Running with restart count ≤ 1 (APPROVED) or evidence of a different failure (BLOCKED)
- Wall-time evidence (load-bearing): both runs ≤ 600s for APPROVED
- T-02.2-14 marginal-headroom carry-forward flag present if observed wall ≥ 500s even on APPROVED
- All four new threats (T-02.2-17/18/19/20) have evidence in §6 + carry-forward T-02.2-01 through T-02.2-16
- Cascade-6 classification present per T-02.2-19 (one of: `flag-mismatch | budget-vs-actual | spec-flake | harness-bug | kind-infra | other`)

**Wiring gates (Task 3 — gated on Task 2 APPROVED):**
- `02.2-02-PLAN.md` has `depends_on: ["02.2-07"]` and ALL 31 references updated from `02.2-01-VERIFICATION.md` to `02.2-07-VERIFICATION.md` (exact count one-for-one)
- Stale `02.2-01-VERIFICATION.md`, `02.2-03-VERIFICATION.md`, `02.2-04-VERIFICATION.md`, `02.2-05-VERIFICATION.md`, `02.2-06-VERIFICATION.md` references purged from `02.2-02-PLAN.md`
- No other plans modified

**Structural gates (T-02.2-17/18/19/20 mitigation):**
- Helper signature exactly 4-arg form (T-02.2-20)
- Secret type Opaque + ANTHROPIC_API_KEY data key (T-02.2-18)
- All CRD shapes mirror testdata/three-task-wave.yaml (T-02.2-17)
- Cascade-6 (if any) classified per T-02.2-19 enum
- Chart `charts/tide/values.yaml` NOT touched
- Production source (`internal/controller/task_controller.go`, `api/v1alpha1/task_types.go`, `internal/controller/plan_controller.go`, `cmd/manager/main.go`) NOT touched
- Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06 NOT modified
- Plan 02.2-02 modified ONLY in Task 3 (gated on APPROVED)
- `.planning/STATE.md` and `.planning/ROADMAP.md` NOT touched (orchestrator owns)
</verification>

<success_criteria>
- Task 1 fixture edits landed: `test/integration/kind/suite_test.go` exports `applyHierarchy(ctx context.Context, ns, planName, taskName string) error` creating the full Namespace+Secret+Project+Milestone+Phase+Plan+Task hierarchy mirroring `testdata/three-task-wave.yaml`; `test/integration/kind/credproxy_test.go` refactored to call `applyHierarchy(...)` for both HARN-03 specs; `go build` + `go vet` both exit 0; no production source modified (Option B contract); no other test files modified
- Task 2 runtime verification artifact authored at `.planning/phases/02.2-.../02.2-07-VERIFICATION.md` with `gate_decision: APPROVED` or `gate_decision: BLOCKED` and all required structural sections including load-bearing wall-time, Pod-state, manager-log, credproxy-test namespace CRD listing, and cascade-6 classification evidence; §Section 5 cites the corrected root cause (`.planning/debug/credproxy-dispatch-guard.md` + `resolveProject`) and explicitly does NOT inherit the wrong "parent Wave Ready check" framing
- Task 3 (gated on Task 2 APPROVED) wired Plan 02.2-02's checkpoint to `02.2-07-VERIFICATION.md` with `depends_on: ["02.2-07"]` and all 31 stale `02.2-01-VERIFICATION.md` references one-for-one redirected
- If Task 2 returns BLOCKED: Task 3 does NOT fire; Plan 02.2-02 stays gated on `02.2-01-VERIFICATION.md` (current wiring); the orchestrator routes to a planner re-evaluation for Plan 02.2-08 (or equivalent) per the wave-failure-semantics contract and the tactical-iteration acceptance recorded in T-02.2-19; the cascade-6 classification in the verification artifact directs the planner to the right fix shape
- T-02.2-17/18/19/20 each have concrete evidence in the verification artifact and the source-edit acceptance gates
- Tactical scope honored: ONLY the test-fixture refactor + shared helper landed (Option B); production dispatch path in `internal/controller/task_controller.go` NOT touched (Option D rejected); `charts/tide/values.yaml` NOT touched; Plans 02.2-01, 02.2-03, 02.2-04, 02.2-05, 02.2-06 NOT modified; Plan 02.2-05's `cmd/manager/main.go` flag definitions preserved (verified by absence of `flag provided but not defined` errors in manager logs); Plan 02.2-06's Makefile `timeout 600s` preserved (verified by absence of timeout-124 SIGKILL in either run)
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-07-SUMMARY.md` documenting:
- The test-fixture edits applied (Task 1): the new `applyHierarchy(ctx, ns, planName, taskName)` helper in `suite_test.go` + the refactor of `credproxy_test.go`'s two HARN-03 specs to call it + the rationale (cascade-5 root cause from `.planning/debug/credproxy-dispatch-guard.md` §Reasoning Checkpoint — `resolveProject` Project-in-namespace lookup at `task_controller.go:217`, NOT the originally-hypothesized parent Wave Ready check) + the preserved Secret + Project + Milestone + Phase + Plan + Task hierarchy shapes (T-02.2-17 / T-02.2-18 mitigation)
- The runtime verification outcome (Task 2): gate decision (APPROVED or BLOCKED), per-spec outcomes, wall times, exit codes, Pod-state evidence, manager-log evidence (particularly the `dispatch` line for credproxy-task BEFORE any `task cleanup` line, proving production dispatch path now fires), cascade-6 classification per T-02.2-19
- The Plan 02.2-02 wiring update (Task 3, if APPROVED): which references were updated (count exactly 31), depends_on bump, no plan-list/decisions edits per plan-checker preempt
- Any inconsistencies surfaced (e.g., marginal-headroom T-02.2-14 carry-forward flag on APPROVED; cascade-6 surfacing on BLOCKED with its classification)
- The Phase 02.2 close-out chain progression: 4 source fixes (02.2-01) + 1 chart fix (02.2-03) + 1 binary metrics flag (02.2-04) + 1 binary webhook-cert-path flag (02.2-05) + 1 Makefile timeout bump (02.2-06) + 1 test-fixture refactor (this plan) landed → runtime verified once (APPROVED) or routed back (BLOCKED) for next iteration
- Threat-model mitigations applied (T-02.2-17 / T-02.2-18 / T-02.2-19 / T-02.2-20) with concrete evidence + carry-forward T-02.2-01 through T-02.2-16
- A note on the structural shift: cascades 1-3 were all flag-mismatch (predictable, terminated at 5/5 chart args defined); cascade-4 was budget-vs-actual (structurally different, single-line Makefile fix); cascade-5 was harness-bug — fixture-incomplete (per the corrected debug investigation); cascade-6 (if any) shape is genuinely unknown per T-02.2-19 acceptance. The cascade chain is NOT a simple linear sequence — each iteration uncovered a structurally distinct class.
- An explicit framing-correction note: `02.2-06-VERIFICATION.md` §Section 5 hypothesized the cascade-5 gate as a "parent Wave Ready check" — that hypothesis was WRONG. The debug investigation captured at `.planning/debug/credproxy-dispatch-guard.md` corrected the framing to the `resolveProject` Project-in-namespace lookup. This plan's Task 1 implementation acts on the CORRECTED framing (via Option B full-hierarchy fixture), and Task 2's verification artifact explicitly re-states the correction so future readers don't re-inherit the wrong framing.
</output>
</content>