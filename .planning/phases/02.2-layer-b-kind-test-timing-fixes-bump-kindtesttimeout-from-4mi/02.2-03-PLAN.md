---
phase: 02.2
plan: 03
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - charts/tide/templates/projects-pvc.yaml
  - charts/tide/values.yaml
  - test/integration/kind/suite_test.go
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md
  - .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
autonomous: true
gap_closure: true
requirements: []
tags: [chart, helm, pvc, accessModes, kind, ginkgo, helm-template, helm-lint, ReadWriteOnce, ReadWriteMany, local-path-provisioner, gap-closure]

threat_model_summary:
  - "T-02.2-05 (mitigate): chart change breaks production deployments that rely on the existing RWX hardcode — default value MUST remain ReadWriteMany; only the override path is added"
  - "T-02.2-06 (mitigate): test override (--set ReadWriteOnce) leaks RWO assumption into prod via misconfigured CI — override lives ONLY in suite_test.go helm install args, never in values.yaml"
  - "T-02.2-07 (mitigate): helm chart template rendering breaks (invalid YAML, helm template fails) — pre-runtime gates run `helm template charts/tide > /dev/null` and `helm lint charts/tide` BEFORE the kind reproducer fires"
  - "T-02.2-08 (accept): Plan 02.2-02 depends_on edit accidentally regresses an unrelated gate or checkpoint clause — acceptance grep gates explicitly verify the two and only two edits land (depends_on bump + verification path reference)"

must_haves:
  truths:
    - "charts/tide/templates/projects-pvc.yaml renders accessModes from .Values.workspaces.pvc.accessModes when set, defaulting to [ReadWriteMany] when omitted — production deployments unaffected"
    - "charts/tide/values.yaml documents workspaces.pvc.accessModes = [ReadWriteMany] as the chart default with a comment about production CSI driver expectations"
    - "test/integration/kind/suite_test.go's applyController() helm install args include --set workspaces.pvc.accessModes={ReadWriteOnce} so kind's rancher.io/local-path provisioner can bind the PVC"
    - "helm template charts/tide renders without error; helm lint charts/tide exits 0; the resulting PVC manifest contains the expected accessModes block per .Values overrides"
    - "Existing helm.sh/resource-policy: keep annotation on the PVC (T-02-12-07) preserved verbatim"
    - "make test-int clean run exits 0 with 7/7 Layer B specs PASS and `timeout 300s` does NOT fire (exit 124) on the go test step"
    - "KEEP_KIND_CLUSTER=true make test-int rerun exits 0 with 7/7 PASS and no exit-124 firing"
    - "02.2-03-VERIFICATION.md mirrors 02.2-01-VERIFICATION.md structural template: frontmatter with clean_run_/rerun_ exit + wall + spec count fields; Sections 1-6; gate_decision: APPROVED"
    - "Plan 02.2-02's checkpoint resume-signal targets 02.2-03-VERIFICATION.md (NOT 02.2-01-VERIFICATION.md) so the gated closeout reads the authoritative artifact"
    - "Plan 02.2-02's depends_on is bumped from ['02.2-01'] to ['02.2-03'] so the wave ordering implicit in execute-phase honors the new gate artifact"
  artifacts:
    - path: "charts/tide/templates/projects-pvc.yaml"
      provides: "templated accessModes range over .Values.workspaces.pvc.accessModes with default [ReadWriteMany]"
      contains: 'default (list "ReadWriteMany")'
    - path: "charts/tide/templates/projects-pvc.yaml"
      provides: "helm.sh/resource-policy: keep annotation (T-02-12-07) preserved"
      contains: "helm.sh/resource-policy: keep"
    - path: "charts/tide/values.yaml"
      provides: "documented workspaces.pvc.accessModes default with production CSI guidance comment"
      contains: "accessModes:"
    - path: "test/integration/kind/suite_test.go"
      provides: "test-only helm --set override for kind RWO compatibility"
      contains: "workspaces.pvc.accessModes={ReadWriteOnce}"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md"
      provides: "Runtime gate decision APPROVED with clean run + idempotent rerun outcomes"
      contains: "gate_decision: APPROVED"
    - path: ".planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md"
      provides: "depends_on bumped to ['02.2-03'] + checkpoint references 02.2-03-VERIFICATION.md"
      contains: "02.2-03-VERIFICATION.md"
  key_links:
    - from: "charts/tide/templates/projects-pvc.yaml (accessModes range)"
      to: "charts/tide/values.yaml (workspaces.pvc.accessModes default)"
      via: "Helm values lookup with default fallback"
      pattern: '\.Values\.workspaces\.pvc\.accessModes \| default'
    - from: "test/integration/kind/suite_test.go (applyController helm args)"
      to: "charts/tide/templates/projects-pvc.yaml (accessModes override surface)"
      via: 'helm install --set workspaces.pvc.accessModes={ReadWriteOnce}'
      pattern: 'workspaces\.pvc\.accessModes=\{ReadWriteOnce\}'
    - from: "02.2-03-VERIFICATION.md (gate_decision: APPROVED)"
      to: "02.2-02-PLAN.md (Task 1 checkpoint:human-verify resume-signal)"
      via: "checkpoint reads gate_decision from this verification artifact"
      pattern: "02.2-03-VERIFICATION.md"
    - from: "02.2-02-PLAN.md (depends_on field)"
      to: "execute-phase wave ordering for Phase 02.2"
      via: "frontmatter depends_on: ['02.2-03']"
      pattern: 'depends_on: \[.{0,5}02.2-03.{0,5}\]'
---

<objective>
Close the BLOCKED runtime gate from `02.2-01-VERIFICATION.md` by adding `accessModes` as an overridable Helm values key on the `tide-projects` PVC, preserving `ReadWriteMany` as the production default while letting the kind integration test override to `ReadWriteOnce` so the chart's PVC binds against kind's `rancher.io/local-path` provisioner. After source edits land, re-run the same end-to-end runtime reproducer captured in `02.2-01-VERIFICATION.md` and write the result to a NEW `02.2-03-VERIFICATION.md` artifact. Finally, wire Plan 02.2-02's checkpoint to read the new authoritative gate artifact and bump its `depends_on` to reflect the new wave ordering.

**Option A from `02.2-01-VERIFICATION.md` §"Fix landscape"** — the cheapest of the five options, matches the existing `workspaces.pvc.storageClassName` precedent (already exposed as a Helm values key with a no-op default), and never changes the chart's production-correct RWX intent. Two-line surface area: (a) replace the hardcoded `accessModes: - ReadWriteMany` in the PVC template with a templated `range` over `.Values.workspaces.pvc.accessModes` defaulting to `[ReadWriteMany]`; (b) add the documented default to `values.yaml`. Then the test consumes the new override via `helm --set workspaces.pvc.accessModes={ReadWriteOnce}` in `applyController()`.

This plan is ADDITIVE to Phase 02.2's existing plan set:
- Plan 02.2-01 (Wave 1) — already executed; source-shape fixes + 02.2-01-VERIFICATION.md (BLOCKED gate) — NOT touched
- Plan 02.2-03 (Wave 2, NEW, this plan) — chart PVC fix + re-verification → APPROVED gate
- Plan 02.2-02 (Wave 3, existing closeout) — checkpoint reads 02.2-03-VERIFICATION.md (was 02.2-01-VERIFICATION.md); `depends_on` bumped to `["02.2-03"]`

Purpose: the Phase 02.2 source-shape fixes (Plan 01) work correctly at the source layer — kindTestTimeout 7m, cert-manager v1.20.2, helm --replace, cleanupKindCluster() helper — verified by direct evidence in `02.2-01-VERIFICATION.md` §"Source-Shape Fix Evidence". A NEW, pre-existing, out-of-scope blocker surfaced during runtime verification: the `tide-projects` PVC's hardcoded `ReadWriteMany` accessMode is incompatible with kind's default `rancher.io/local-path` provisioner (`NodePath only supports ReadWriteOnce and ReadWriteOncePod (1.22+) access modes`). This plan addresses ONLY the PVC accessModes blocker — no scope creep into other PVC concerns (size, storage class) or other chart areas.

Output:
- `charts/tide/templates/projects-pvc.yaml` with `accessModes` templated from `.Values.workspaces.pvc.accessModes` (default `[ReadWriteMany]`)
- `charts/tide/values.yaml` with `workspaces.pvc.accessModes: [ReadWriteMany]` documented as default
- `test/integration/kind/suite_test.go` with `--set 'workspaces.pvc.accessModes={ReadWriteOnce}'` added to helm install args at `applyController()`
- `.planning/phases/02.2-.../02.2-03-VERIFICATION.md` capturing the clean run + idempotent rerun outcome with `gate_decision: APPROVED`
- `.planning/phases/02.2-.../02.2-02-PLAN.md` with `depends_on: ["02.2-03"]` and checkpoint references pointing at the new verification artifact
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-RESEARCH.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-VALIDATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-VERIFICATION.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-PLAN.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-SUMMARY.md
@.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md
@charts/tide/templates/projects-pvc.yaml
@charts/tide/values.yaml
@test/integration/kind/suite_test.go
@CLAUDE.md

<interfaces>
<!-- Key identifiers, line ranges, and existing chart-side patterns the executor needs.
     Executor uses these directly — no codebase exploration needed. -->

From charts/tide/templates/projects-pvc.yaml (the file to edit):
- Lines 1-22 — full file content; ALL of:
  - Line 1: `{{- if .Values.workspaces.pvc.create }}` (top-level guard — KEEP)
  - Lines 10-12: `helm.sh/resource-policy: keep` annotation block (T-02-12-07 protection — KEEP VERBATIM)
  - Lines 14-15: `accessModes:\n    - ReadWriteMany` (TARGET: replace with templated range)
  - Lines 16-18: `resources.requests.storage` block (KEEP)
  - Lines 19-21: `{{- with .Values.workspaces.pvc.storageClassName }}` precedent block (REFERENCE PATTERN — the storageClassName key is exactly the precedent the new accessModes key mirrors)

From charts/tide/values.yaml (the file to edit):
- Lines 151-171 — `workspaces:` block at the bottom of the file; specifically:
  - Lines 166-171: `workspaces:\n  pvc:\n    create: true\n    name: tide-projects\n    size: 10Gi\n    storageClassName: ""  # cluster default; ...`
  - TARGET: add `accessModes: [ReadWriteMany]  # production-default; ...` adjacent to `storageClassName` (mirrors the existing key's documented-default pattern)
- Lines 151-165 — explanatory comment block above `workspaces:` (describes single-shared-RWX-PVC architecture, single-node kind/minikube/dev tradeoff). The new `accessModes` key fits this story; extend the comment ONLY if needed for clarity (default: leave the existing comment alone and document the new key inline next to storageClassName).

From test/integration/kind/suite_test.go (the file to edit):
- Lines 343-422 — `applyController()` function
- Lines 399-411 — the `helmCmd := exec.CommandContext(ctx, "helm", ...)` block; specifically:
  - Lines 403-409 — existing `--set` flags (controllerManager image overrides + images.stubSubagent/credProxy tag+pullPolicy)
  - Line 410 — `"--wait", "--replace", "--timeout", "5m",` (the trailing flags after the --set group)
- TARGET: add `"--set", "workspaces.pvc.accessModes={ReadWriteOnce}",` as a new line adjacent to the existing `--set` flags (placement: after the credProxy --set lines, before the `--wait` line, to keep the --set group contiguous). The argument uses Helm's `{a,b,c}` list-literal syntax for a single-element list.
- Lines 394-398 — existing comment block explaining `--replace`; the executor should add a one-line code comment ABOVE the new `--set` line referencing `02.2-01-VERIFICATION.md` §"Fix landscape" Option A (cite the verification artifact + the kind RWX/RWO mismatch).

From .planning/phases/02.2-.../02.2-02-PLAN.md (the file to edit — minimal touch):
- Line 6 — `depends_on: ["02.2-01"]` (TARGET: → `depends_on: ["02.2-03"]`)
- Lines 33-39 — `key_links` block (one entry referencing `02.2-01-VERIFICATION.md` — TARGET: bump path to `02.2-03-VERIFICATION.md`)
- Line 66 — `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` in the `<context>` block (TARGET: → `02.2-03-VERIFICATION.md`)
- Lines 77, 86-89, 119-120, 126-127 — `02.2-01-VERIFICATION.md` references in Task 1's `<read_first>`, `<how-to-verify>`, `<verify>` automated block, and `<acceptance_criteria>` (TARGET: → `02.2-03-VERIFICATION.md`)
- DO NOT change: the threat_model, the Task 2 / Task 3 bodies, the must_haves, the Task 1 `<what-built>` prose (that section describes Plan 01's deliverables, which are unchanged)

From .planning/phases/02.2-.../02.2-01-VERIFICATION.md (the gate artifact — reference template):
- Frontmatter shape (line 1-23) — mirrored verbatim in `02.2-03-VERIFICATION.md` with updated values
- Section 1 (Clean Start Run), Section 2 (Idempotent Re-run), Section 3 (Before/After Comparison), Section 4 (Wall-time Budget Check), Section 5 (Gate Decision), Section 6 (Threat-Model Mitigations Applied) — structural template
- "Fix landscape" Option A at line 236 — the directed approach for this plan

From CLAUDE.md (anti-patterns that this plan honors):
- "Default the chart's `ServiceMonitor` to `prometheus.enabled=false` to avoid CRD-not-found on plain clusters" — orthogonal but reinforces "test override stays in test, not in values.yaml"
- "Don't hard-code one LLM provider, one git host, or one auth model in the controller" — generalization: don't hard-code storage access modes either when an override path is cheap
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expose workspaces.pvc.accessModes as overridable Helm values key (chart template + values.yaml)</name>
  <files>charts/tide/templates/projects-pvc.yaml, charts/tide/values.yaml</files>

  <read_first>
    - charts/tide/templates/projects-pvc.yaml (full file — 22 lines; existing `accessModes: - ReadWriteMany` block at lines 14-15; existing `{{- with .Values.workspaces.pvc.storageClassName }}` precedent at lines 19-21)
    - charts/tide/values.yaml (lines 151-171 — the `workspaces:` block at the bottom; existing `storageClassName: ""  # cluster default; ...` documented-default pattern at line 171)
    - .planning/phases/02.2-.../02.2-01-VERIFICATION.md §"Fix landscape" Option A (line 236) — the directed approach: "Expose accessModes as a values key (e.g., `.Values.workspaces.pvc.accessModes`) defaulting to `[ReadWriteMany]` and add `--set workspaces.pvc.accessModes={ReadWriteOnce}` to the test's helm install args"
    - CLAUDE.md §"Tech stack" (Helm 3, Kustomize/kubebuilder-bundled — no helmify regeneration step for this edit; the chart is hand-authored at this surface)
  </read_first>

  <action>
    Make two edits — one in `charts/tide/templates/projects-pvc.yaml` and one in `charts/tide/values.yaml`. Both edits are additive and preserve the production default.

    **Edit A — `charts/tide/templates/projects-pvc.yaml` lines 14-15:** Replace the hardcoded two-line block

    ```
      accessModes:
        - ReadWriteMany
    ```

    with a Helm-templated `range` over `.Values.workspaces.pvc.accessModes` defaulting to `[ReadWriteMany]`. The replacement uses the same Sprig idioms already in the chart and quotes each element (mirrors the `storageClassName: {{ . | quote }}` pattern at line 20). The intended rendering when `.Values.workspaces.pvc.accessModes` is unset (production default) produces the same two-line `accessModes:\n    - "ReadWriteMany"` shape that the chart emits today (modulo the quote characters, which Kubernetes accepts identically). When overridden to `{ReadWriteOnce}` via `--set`, the range yields `accessModes:\n    - "ReadWriteOnce"`.

    Concrete shape (executor pastes the templated block in place of lines 14-15; indentation matches the surrounding `spec:` block):

    ```
      accessModes:
        {{- range (.Values.workspaces.pvc.accessModes | default (list "ReadWriteMany")) }}
        - {{ . | quote }}
        {{- end }}
    ```

    Constraints:
    - **DO NOT remove the `helm.sh/resource-policy: keep` annotation** at lines 10-12 (T-02-12-07 protection — preserve verbatim).
    - **DO NOT change the top-level `{{- if .Values.workspaces.pvc.create }}` guard** at line 1.
    - **DO NOT change the `resources.requests.storage` block** at lines 16-18.
    - **DO NOT change the `storageClassName` block** at lines 19-21.
    - The chart's default value (resolved by `default (list "ReadWriteMany")` at template render time when the values key is unset) MUST remain `ReadWriteMany`. Production deployments using `helm install tide` without a `--set workspaces.pvc.accessModes=...` flag must render the identical PVC manifest they render today.

    **Edit B — `charts/tide/values.yaml` lines 166-171:** Add `accessModes: [ReadWriteMany]` as a documented key under `workspaces.pvc`, adjacent to the existing `storageClassName` key. Mirrors the existing `storageClassName: ""  # cluster default; set to a CSI RWX driver for production` documented-default pattern. The new key + comment:

    ```yaml
        accessModes: [ReadWriteMany]  # production-default; matches the chart's single-shared-RWX-PVC architecture (see comment above). Override to [ReadWriteOnce] for single-node kind/minikube test clusters whose default provisioner (rancher.io/local-path) only supports RWO.
    ```

    Place it directly after the `storageClassName: ""` line (line 171). The comment is part of the change.

    Constraints:
    - **DO NOT modify the existing `workspaces.pvc` comment block** at lines 151-165 — it's accurate (single-shared-RWX-PVC architecture, Phase 3 ART-02 evaluation note). The new `accessModes` line slots in alongside the other documented defaults without rewriting the upstream prose.
    - **DO NOT add new top-level keys to values.yaml** — only the one new `accessModes:` line within the existing `workspaces.pvc` block.
    - **DO NOT change the default value from `[ReadWriteMany]`** — the default MUST match the production chart contract (single-shared RWX PVC across all Projects' workspaces).

    Both edits MUST land atomically in one commit so a partial state (template edited but values undocumented, or vice versa) is never on disk.
  </action>

  <verify>
    <automated><![CDATA[
      # Template change applied (default (list "ReadWriteMany") template form present)
      [ "$(grep -c 'default (list "ReadWriteMany")' charts/tide/templates/projects-pvc.yaml)" -eq 1 ] && \
      [ "$(grep -c 'range (.Values.workspaces.pvc.accessModes' charts/tide/templates/projects-pvc.yaml)" -eq 1 ] && \
      # The OLD hardcoded form is gone from the template
      [ "$(grep -c -E '^\s+- ReadWriteMany\s*$' charts/tide/templates/projects-pvc.yaml)" -eq 0 ] && \
      # helm.sh/resource-policy: keep annotation preserved (T-02-12-07)
      [ "$(grep -c 'helm.sh/resource-policy: keep' charts/tide/templates/projects-pvc.yaml)" -eq 1 ] && \
      # The chart's other concerns are unchanged
      [ "$(grep -c 'storageClassName' charts/tide/templates/projects-pvc.yaml)" -ge 1 ] && \
      [ "$(grep -c '.Values.workspaces.pvc.create' charts/tide/templates/projects-pvc.yaml)" -eq 1 ] && \
      # values.yaml gets the new documented default ([ReadWriteMany])
      [ "$(grep -c 'accessModes: \[ReadWriteMany\]' charts/tide/values.yaml)" -eq 1 ] && \
      # values.yaml still has the workspaces.pvc block intact
      [ "$(grep -c '^workspaces:' charts/tide/values.yaml)" -eq 1 ] && \
      [ "$(grep -c '^  pvc:' charts/tide/values.yaml)" -eq 1 ] && \
      [ "$(grep -c 'storageClassName: ""' charts/tide/values.yaml)" -eq 1 ] && \
      # helm template renders without error AND emits a PersistentVolumeClaim with the default (ReadWriteMany)
      helm template tide charts/tide --namespace tide-system > /tmp/02.2-03-helm-render.yaml 2>&1 && \
      [ "$(grep -c 'kind: PersistentVolumeClaim' /tmp/02.2-03-helm-render.yaml)" -ge 1 ] && \
      [ "$(grep -c 'ReadWriteMany' /tmp/02.2-03-helm-render.yaml)" -ge 1 ] && \
      [ "$(grep -c 'ReadWriteOnce' /tmp/02.2-03-helm-render.yaml)" -eq 0 ] && \
      # helm template with --set override produces ReadWriteOnce (the test-path)
      helm template tide charts/tide --namespace tide-system --set 'workspaces.pvc.accessModes={ReadWriteOnce}' > /tmp/02.2-03-helm-render-rwo.yaml 2>&1 && \
      [ "$(grep -c 'kind: PersistentVolumeClaim' /tmp/02.2-03-helm-render-rwo.yaml)" -ge 1 ] && \
      [ "$(grep -c 'ReadWriteOnce' /tmp/02.2-03-helm-render-rwo.yaml)" -ge 1 ] && \
      [ "$(grep -c 'ReadWriteMany' /tmp/02.2-03-helm-render-rwo.yaml)" -eq 0 ] && \
      # helm lint passes
      helm lint charts/tide && \
      echo "TASK 1 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Template change landed:** `grep -c 'default (list "ReadWriteMany")' charts/tide/templates/projects-pvc.yaml` returns exactly 1 AND `grep -c 'range (.Values.workspaces.pvc.accessModes' charts/tide/templates/projects-pvc.yaml` returns exactly 1
    - **OLD hardcoded form removed:** `grep -c -E '^\s+- ReadWriteMany\s*$' charts/tide/templates/projects-pvc.yaml` returns 0 (the bare two-line `accessModes:\n    - ReadWriteMany` form is replaced by the templated range)
    - **T-02-12-07 protection preserved:** `grep -c 'helm.sh/resource-policy: keep' charts/tide/templates/projects-pvc.yaml` returns exactly 1
    - **Other chart concerns intact:** `grep -c 'storageClassName' charts/tide/templates/projects-pvc.yaml` returns ≥ 1 AND `grep -c '.Values.workspaces.pvc.create' charts/tide/templates/projects-pvc.yaml` returns exactly 1
    - **values.yaml default documented:** `grep -c 'accessModes: \[ReadWriteMany\]' charts/tide/values.yaml` returns exactly 1 (T-02.2-05 mitigation: production default preserved)
    - **values.yaml workspaces block intact:** `grep -c '^workspaces:' charts/tide/values.yaml` returns 1 AND `grep -c '^  pvc:' charts/tide/values.yaml` returns 1 AND `grep -c 'storageClassName: ""' charts/tide/values.yaml` returns 1
    - **T-02.2-07 mitigation (helm template renders cleanly with default):** `helm template tide charts/tide --namespace tide-system` exits 0 AND the resulting manifest contains ≥ 1 `kind: PersistentVolumeClaim` AND ≥ 1 `ReadWriteMany` occurrence AND 0 `ReadWriteOnce` occurrences
    - **T-02.2-07 mitigation (helm template renders cleanly with --set override):** `helm template tide charts/tide --namespace tide-system --set 'workspaces.pvc.accessModes={ReadWriteOnce}'` exits 0 AND the resulting manifest contains ≥ 1 `ReadWriteOnce` occurrence AND 0 `ReadWriteMany` occurrences in the PVC block
    - **helm lint passes:** `helm lint charts/tide` exits 0
  </acceptance_criteria>

  <done>
    `charts/tide/templates/projects-pvc.yaml` renders `accessModes` from `.Values.workspaces.pvc.accessModes` with a `[ReadWriteMany]` default; the `helm.sh/resource-policy: keep` annotation is preserved verbatim. `charts/tide/values.yaml` documents `workspaces.pvc.accessModes: [ReadWriteMany]` as the chart default with a comment explaining the kind/minikube override path. Both `helm template` (default + RWO override) and `helm lint charts/tide` exit 0. Production deployments continue to render the same RWX PVC they render today. Both edits committed atomically in one commit with a `feat(02.2-03): expose workspaces.pvc.accessModes — unblock kind RWX/RWO mismatch from 02.2-01-VERIFICATION.md` message (or similar — the commit message explains WHY the chart needs to be overridable, not just WHAT changed).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add --set workspaces.pvc.accessModes={ReadWriteOnce} override to suite_test.go applyController helm install args</name>
  <files>test/integration/kind/suite_test.go</files>

  <read_first>
    - test/integration/kind/suite_test.go lines 343-422 — the full `applyController()` function (helmCmd block, --set arg group, error handling)
    - test/integration/kind/suite_test.go lines 399-411 — the existing `--set` group + `--wait`, `--replace`, `--timeout` trailing flags (concrete placement target)
    - .planning/phases/02.2-.../02.2-01-VERIFICATION.md §"Fix landscape" Option A — the directed approach for the override semantics
    - charts/tide/templates/projects-pvc.yaml (POST-Task-1 state — `accessModes` is now templated; the test consumes the override surface that Task 1 created)
  </read_first>

  <action>
    Edit `test/integration/kind/suite_test.go` to add a single new `--set` flag at the existing `applyController()` helmCmd block, immediately consuming the override surface Task 1 created on the chart.

    **Edit — `applyController()` helmCmd block (lines 399-411):** Insert a new `--set` line for `workspaces.pvc.accessModes={ReadWriteOnce}` adjacent to the existing `--set` group (after the `images.credProxy.pullPolicy=IfNotPresent` line at line 409, before the `--wait`, `--replace`, `--timeout` trailing-flags line at line 410). The Helm `{a,b,c}` list-literal syntax is the standard way to pass a single-element list via `--set`.

    Add a one-line code comment ABOVE the new `--set` line citing the verification artifact + the kind RWX/RWO mismatch. The comment is part of the change.

    Concrete shape (executor pastes between line 409 and line 410, matching the surrounding indentation/quoting style):

    ```go
        // Override the chart's default accessModes [ReadWriteMany] to [ReadWriteOnce]
        // because kind's default rancher.io/local-path provisioner only supports
        // RWO/RWOPod. Single-node kind cluster doesn't need RWX semantics for these
        // tests. See .planning/phases/02.2-.../02.2-01-VERIFICATION.md §"Fix
        // landscape" Option A (chart-side override + test-side --set).
        "--set", "workspaces.pvc.accessModes={ReadWriteOnce}",
    ```

    Constraints:
    - **DO NOT change the existing `--set` lines** at lines 403-409 (controllerManager image overrides + images.stubSubagent/credProxy tag+pullPolicy). T-02.2-08 protection.
    - **DO NOT change the trailing `--wait`, `--replace`, `--timeout` line** at line 410.
    - **DO NOT add the override to `charts/tide/values.yaml`** — that would leak the RWO assumption into production deployments via misconfigured CI (T-02.2-06 mitigation). The override lives ONLY in the test's helm install args.
    - **DO NOT change the existing `--replace` flag** — that's Phase 02.2-01's fix and is correct.
    - **DO NOT use `ReadWriteOncePod`** — that's K8s 1.22+ only and stricter than RWO; RWO is the correct choice for single-node kind (per `02.2-01-VERIFICATION.md` §"Fix landscape" anti-patterns).
    - **DO NOT add a new CSI driver to the kind cluster** — Option B from the verification artifact was explicitly REJECTED in favor of Option A.
    - Code uses only existing imports (`exec`, `fmt`, etc.) — no new imports needed.
    - Preserve the existing `TIDE_REQUIRE_CONTROLLER` semantics at lines 417-419 — that env-var is the Phase 02.1 D-03 gate; this task does NOT touch it.

    Per the verification artifact's anti-patterns: do NOT add the override anywhere outside the helm install args. The override is local to the kind integration test's BeforeSuite — `make helm` / production `helm install tide` never sees the `--set` flag.
  </action>

  <verify>
    <automated><![CDATA[
      # The override --set flag is present
      [ "$(grep -c 'workspaces.pvc.accessModes={ReadWriteOnce}' test/integration/kind/suite_test.go)" -eq 1 ] && \
      # ReadWriteOncePod is NOT used (anti-pattern from 02.2-01-VERIFICATION.md)
      [ "$(grep -c 'ReadWriteOncePod' test/integration/kind/suite_test.go)" -eq 0 ] && \
      # The existing --replace flag is preserved
      [ "$(grep -c '"--replace"' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # The existing --set group is preserved (controllerManager image overrides remain)
      [ "$(grep -c 'controllerManager.manager.image.repository=controller' test/integration/kind/suite_test.go)" -eq 1 ] && \
      [ "$(grep -c 'images.stubSubagent.tag=test' test/integration/kind/suite_test.go)" -eq 1 ] && \
      [ "$(grep -c 'images.credProxy.tag=test' test/integration/kind/suite_test.go)" -eq 1 ] && \
      # T-02.2-06 mitigation: the RWO override appears ONLY in suite_test.go, NEVER in values.yaml
      [ "$(grep -c 'ReadWriteOnce' charts/tide/values.yaml)" -eq 0 ] && \
      # T-02.2-04 mitigation (from prior plans): TIDE_REQUIRE_CONTROLLER gate untouched
      [ "$(grep -c 'TIDE_REQUIRE_CONTROLLER' test/integration/kind/suite_test.go)" -ge 1 ] && \
      # Compile + vet clean
      go build ./test/integration/kind/... && \
      go vet ./test/integration/kind/... && \
      gofmt -l test/integration/kind/suite_test.go | tee /tmp/02.2-03-gofmt-t2 && [ ! -s /tmp/02.2-03-gofmt-t2 ] && \
      # Phase 1 import firewalls remain green
      make verify-dag-imports && \
      make verify-dispatch-imports && \
      make verify-import-firewall && \
      echo "TASK 2 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Override --set flag present:** `grep -c 'workspaces.pvc.accessModes={ReadWriteOnce}' test/integration/kind/suite_test.go` returns exactly 1
    - **Anti-pattern avoided (ReadWriteOncePod NOT used):** `grep -c 'ReadWriteOncePod' test/integration/kind/suite_test.go` returns 0
    - **Existing helm flags preserved:** `grep -c '"--replace"' test/integration/kind/suite_test.go` returns ≥ 1 AND `grep -c 'controllerManager.manager.image.repository=controller' test/integration/kind/suite_test.go` returns 1 AND `grep -c 'images.stubSubagent.tag=test' test/integration/kind/suite_test.go` returns 1 AND `grep -c 'images.credProxy.tag=test' test/integration/kind/suite_test.go` returns 1
    - **T-02.2-06 mitigation (RWO override ONLY in test, NOT in values.yaml):** `grep -c 'ReadWriteOnce' charts/tide/values.yaml` returns 0 (the test override is local; production deployments never see RWO via values.yaml)
    - **T-02.2-04 prior-plan mitigation preserved:** `grep -c 'TIDE_REQUIRE_CONTROLLER' test/integration/kind/suite_test.go` returns ≥ 1
    - **Compile + vet + gofmt clean:** `go build ./test/integration/kind/...` exits 0 AND `go vet ./test/integration/kind/...` exits 0 AND `gofmt -l test/integration/kind/suite_test.go` produces empty output
    - **Phase 1 import firewalls remain green:** `make verify-dag-imports` exits 0 AND `make verify-dispatch-imports` exits 0 AND `make verify-import-firewall` exits 0
  </acceptance_criteria>

  <done>
    `test/integration/kind/suite_test.go` includes the new `--set` flag at `applyController()`'s helmCmd block, overriding `workspaces.pvc.accessModes` to `{ReadWriteOnce}` for the kind test cluster. The existing `--set` group, `--replace` flag, and `TIDE_REQUIRE_CONTROLLER` semantics are preserved. The chart's values.yaml default remains `ReadWriteMany` (T-02.2-06 mitigation — RWO override is local to the test). Code compiles, vets clean, formats clean, and Phase 1 import firewalls remain green. Committed with a `test(02.2-03): override accessModes={ReadWriteOnce} in kind helm args — close 02.2 BLOCKED gate` message (or similar — the commit message explains WHY the override is needed at the test layer, not in production values).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: End-to-End Runtime Re-Verification — clean run + idempotent rerun + 02.2-03-VERIFICATION.md artifact with gate decision</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-01-VERIFICATION.md (full file — the structural template this artifact mirrors verbatim; frontmatter shape, Section 1 / Section 2 / Section 3 / Section 4 / Section 5 / Section 6 layout, per-spec outcome tables, root-cause analysis prose, threat-model-mitigations-applied list, "What This Artifact Did Not Touch" closing section)
    - .planning/phases/02.2-.../02.2-VALIDATION.md §"Manual-Only Verifications" §"Sampling Rate" — exact reproducer commands + gate criteria
    - .planning/phases/02.2-.../02.2-RESEARCH.md §"Validation Architecture" §"Phase Requirements → Test Map" — the four behaviors that must hold for APPROVED
    - test/integration/kind/suite_test.go (POST-Task-2 state — must compile + vet + gofmt clean before reproducer runs)
    - charts/tide/templates/projects-pvc.yaml (POST-Task-1 state)
    - charts/tide/values.yaml (POST-Task-1 state)
  </read_first>

  <action>
    Run the end-to-end runtime reproducer that mirrors `02.2-01-VERIFICATION.md` Sections 1 + 2, then capture the outcome in a new artifact at `.planning/phases/02.2-.../02.2-03-VERIFICATION.md` using the same structural template.

    **Step A — preflight self-check:** Confirm Task 1 and Task 2 are committed. Run `go build ./test/integration/kind/...`, `go vet ./test/integration/kind/...`, `gofmt -l test/integration/kind/suite_test.go`, `helm template charts/tide > /dev/null`, and `helm lint charts/tide`. If any of these fail, halt and surface — do NOT proceed with the runtime reproducer.

    **Step B — Section 1 (Clean Start Run):** Execute the reproducer verbatim from `02.2-VALIDATION.md` §"Manual-Only Verifications" row 1, capturing into a temp log:

    ```bash
    kind delete cluster --name tide-test 2>&1 || true
    helm uninstall tide -n tide-system 2>&1 || true
    docker rm -f -v $(docker ps -aq --filter label=io.x-k8s.kind.cluster=tide-test) 2>&1 || true
    START=$(date +%s); make test-int 2>&1 | tee /tmp/02.2-03-clean-run.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "clean: exit=$EXIT wall=${WALL}s"
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes (read the Ginkgo summary block from the captured log — should show 7 Pass / 0 Skip / 0 Fail if both prior plan fixes + this plan's chart fix worked together). Transcribe each spec's outcome into a per-spec outcome table mirroring `02.2-01-VERIFICATION.md` §"Section 1" Per-Spec Outcome Table. Also capture the BeforeSuite detail block — particularly: timestamp when `helm install` started, timestamp when controller-manager Pod went `Running` (instead of `Pending`), timestamp when `helm install --wait` completed. These are the evidence that this plan's chart fix unblocked the PVC binding.

    Also capture the PVC's actual access modes via `kubectl get pvc tide-projects -n tide-system -o jsonpath='{.spec.accessModes}'` — should print `["ReadWriteOnce"]` (the test override). And `kubectl get pvc tide-projects -n tide-system -o jsonpath='{.status.phase}'` — should print `Bound`.

    **Step C — Section 2 (Idempotent Re-run):** Without deleting the cluster from Section 1, execute the rerun reproducer:

    ```bash
    START=$(date +%s); KEEP_KIND_CLUSTER=true make test-int 2>&1 | tee /tmp/02.2-03-rerun.log; EXIT=$?; END=$(date +%s); WALL=$((END-START)); echo "rerun: exit=$EXIT wall=${WALL}s"
    ```

    Capture into the artifact: exit code, wall time, per-spec outcomes. Verify the helm install on rerun succeeded due to `--replace` (Plan 02.2-01's fix; not re-verified here, but the rerun must complete without "cannot re-use a name" error). Verify the cluster was NOT deleted by AfterSuite (T-02.2-02 mitigation from Plan 02.2-01). Verify the PVC remains `Bound` with `["ReadWriteOnce"]` access modes.

    **Step D — Section 3 (Before/After Comparison):** Write a 3-column comparison table:
    - BEFORE (cite `02.2-01-VERIFICATION.md` numbers: 0/7 NOT RUN clean, 0/7 NOT RUN rerun, 795s clean wall, 358s rerun wall, BLOCKED gate)
    - AFTER (Phase 02.2-03 clean run — this run)
    - AFTER (Phase 02.2-03 rerun — this run)

    Capture the failure-mode delta: what was broken in Plan 02.2-01 (PVC RWX/RWO mismatch → controller-manager Pod Pending → helm --wait blocked → outer timeout 300s fired) vs what's now passing (PVC binds as RWO → controller-manager Pod Running → helm --wait completes → 7 specs execute within 300s budget).

    **Step E — Section 4 (Wall-time Budget Check):** Table: clean wall vs 300s budget, rerun wall vs 300s budget. **CRITICAL:** the budget contract is `timeout 300s go test ...` from `Makefile:124`. Check `/tmp/02.2-03-clean-run.log` and `/tmp/02.2-03-rerun.log` for the `timeout` exit code 124 — neither should fire for APPROVED. Surface the same distinction as `02.2-01-VERIFICATION.md` §"Section 4": the contract is "go test wall ≤ 300s," not "total make wall ≤ 300s."

    **Step F — Section 5 (Gate Decision):** Apply the criteria from `02.2-VALIDATION.md` §"Manual-Only Verifications":
    - Clean run exit 0 AND 7/7 specs PASS AND go test ≤ 300s → CONTRIBUTES to APPROVED
    - Rerun exit 0 AND 7/7 specs PASS AND go test ≤ 300s → CONTRIBUTES to APPROVED
    - Captured PVC state from BOTH runs (`kubectl get pvc tide-projects -n tide-system -o yaml`) shows `accessModes: ["ReadWriteOnce"]` AND `phase: Bound` → CONTRIBUTES to APPROVED (this is load-bearing: it proves the chart fix actually took effect; without this bullet a cached/prior PVC binding could mask a non-functional template change)
    - All contributions GREEN → `gate_decision: APPROVED`
    - Any single GATE FAIL → `gate_decision: BLOCKED` (with root-cause analysis prose mirroring `02.2-01-VERIFICATION.md` §"Root-Cause Summary")

    Write `gate_decision: APPROVED` or `gate_decision: BLOCKED` in the frontmatter and a matching prose summary at the head of Section 5.

    **Step G — Section 6 (Threat-Model Mitigations Applied):** For each Phase 02.2-03 threat from this plan's `threat_model_summary`:
    - **T-02.2-05 (chart change breaks production):** confirm `grep -c 'accessModes: \[ReadWriteMany\]' charts/tide/values.yaml` returns 1 (production default preserved) AND confirm `helm template charts/tide` without `--set` renders `ReadWriteMany` (not `ReadWriteOnce`). Report counts + the rendered manifest excerpt.
    - **T-02.2-06 (test override leaks into prod):** confirm `grep -c 'ReadWriteOnce' charts/tide/values.yaml` returns 0 (the override is local to suite_test.go, not in values.yaml). Report count.
    - **T-02.2-07 (helm template rendering breaks):** confirm `helm template charts/tide > /dev/null` exits 0 (default render) AND `helm template charts/tide --set 'workspaces.pvc.accessModes={ReadWriteOnce}' > /dev/null` exits 0 (override render) AND `helm lint charts/tide` exits 0. Report exit codes.
    - **T-02.2-08 (Plan 02.2-02 depends_on edit regresses unrelated clauses):** deferred to Task 4 — the verification artifact records "edit applied in Task 4; verified by acceptance grep gates there." This row is informational here; Task 4's gates carry the actual mitigation.

    Also re-verify the prior plans' threat mitigations (T-02.2-01 through T-02.2-04) carry forward — same grep patterns as `02.2-01-VERIFICATION.md` §Section 6 against the new log paths `/tmp/02.2-03-clean-run.log` and `/tmp/02.2-03-rerun.log`.

    **Step H — frontmatter:** Write the artifact's frontmatter mirroring `02.2-01-VERIFICATION.md`:

    ```yaml
    ---
    phase: 02.2
    plan: 03
    task: 3
    artifact: verification
    captured_on: <ISO date>
    captured_by_host: <hostname>
    captured_by_branch: <git branch>
    clean_run_exit: <int>
    clean_run_wall_seconds: <int>
    clean_run_specs_passed: <int>
    clean_run_specs_skipped: <int>
    clean_run_specs_failed: <int>
    clean_run_phase: <phase description>
    rerun_exit: <int>
    rerun_wall_seconds: <int>
    rerun_specs_passed: <int>
    rerun_specs_skipped: <int>
    rerun_specs_failed: <int>
    rerun_phase: <phase description>
    gate_decision: APPROVED | BLOCKED
    gate_reason: <single-line summary>
    ---
    ```

    **What this artifact does NOT touch:** Per the wave-failure-semantics contract from CLAUDE.md — this verification task does NOT edit ROADMAP.md, does NOT edit STATE.md, does NOT modify any production source under `cmd/`, `internal/`, `api/`, `pkg/`. The chart edits (Task 1) and test edits (Task 2) are already committed; Task 3 only creates the `02.2-03-VERIFICATION.md` artifact. Task 4 edits Plan 02.2-02 (a planning artifact, not source). Document this explicitly in the artifact's "What This Artifact Did Not Touch" closing section mirroring `02.2-01-VERIFICATION.md`'s closing section.

    **If gate_decision = BLOCKED:** Write a Root-Cause Summary section with numbered analysis points (mirror `02.2-01-VERIFICATION.md` §"Root-Cause Summary"). Surface a fix landscape if a NEW blocker emerges. Task 4 does NOT fire while the artifact says BLOCKED — Plan 02.2-02 stays gated.

    **If gate_decision = APPROVED:** Skip the Root-Cause Summary section. Note in the closing prose that Plan 02.2-02 (closeout) is unblocked once Task 4 wires its checkpoint to read this artifact.
  </action>

  <verify>
    <automated><![CDATA[
      # Artifact exists and has the required frontmatter shape
      [ -f .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md ] && \
      [ "$(grep -c '^phase: 02.2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^plan: 03' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -eq 1 ] && \
      [ "$(grep -c '^gate_decision: ' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -eq 1 ] && \
      # Gate decision is one of APPROVED or BLOCKED
      grep -E '^gate_decision: (APPROVED|BLOCKED)$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md && \
      # Required structural sections exist
      [ "$(grep -c '^## Section 1' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 2' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c '^## Section 5' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -ge 1 ] && \
      # Required runtime evidence captured: exit codes + wall times
      grep -E '^clean_run_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md && \
      grep -E '^clean_run_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md && \
      grep -E '^rerun_exit: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md && \
      grep -E '^rerun_wall_seconds: [0-9]+$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md && \
      # Threat-model mitigations applied section exists
      [ "$(grep -c 'Threat-Model Mitigations Applied' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -ge 1 ] && \
      # T-02.2-05/06/07 all referenced
      [ "$(grep -c 'T-02.2-05' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-06' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-07' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md)" -ge 1 ] && \
      # No production source modified by this task (only the verification artifact + the prior tasks' edits already committed)
      git status --porcelain | grep -E '^( M|MM| D|MD|AM)' | grep -v '02.2-03-VERIFICATION.md' | grep -v '^$' && echo "FAIL: non-artifact files modified beyond Task 1+2 commits" && exit 1 || \
      echo "TASK 3 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Artifact created at correct path:** `[ -f .planning/phases/02.2-.../02.2-03-VERIFICATION.md ]` returns true
    - **Frontmatter has required fields:** `grep -c '^phase: 02.2' <artifact>` returns 1 AND `grep -c '^plan: 03' <artifact>` returns 1 AND `grep -E '^gate_decision: (APPROVED|BLOCKED)$' <artifact>` matches
    - **Required runtime fields populated:** `grep -E '^clean_run_exit: [0-9]+$' <artifact>` matches AND `grep -E '^clean_run_wall_seconds: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_exit: [0-9]+$' <artifact>` matches AND `grep -E '^rerun_wall_seconds: [0-9]+$' <artifact>` matches
    - **Section 1 (Clean Start Run), Section 2 (Idempotent Re-run), Section 5 (Gate Decision) all present:** each `grep -c '^## Section [125]' <artifact>` returns ≥ 1
    - **Threat-Model Mitigations Applied section exists with all three new threats referenced:** `grep -c 'Threat-Model Mitigations Applied' <artifact>` ≥ 1 AND `grep -c 'T-02.2-05' <artifact>` ≥ 1 AND `grep -c 'T-02.2-06' <artifact>` ≥ 1 AND `grep -c 'T-02.2-07' <artifact>` ≥ 1
    - **Behavior gate (APPROVED case):** if `gate_decision: APPROVED`, then BOTH of: clean run captured `EXIT=0` AND 7 Layer B specs marked PASS in the per-spec table; rerun captured `EXIT=0` AND 7 Layer B specs marked PASS. AND neither captured log (`/tmp/02.2-03-clean-run.log`, `/tmp/02.2-03-rerun.log`) contains a `timeout 300s` exit code 124 occurrence on the go test step. AND the captured PVC state shows `accessModes: ["ReadWriteOnce"]` AND `phase: Bound`.
    - **Behavior gate (BLOCKED case):** if `gate_decision: BLOCKED`, a `Root-Cause Summary` section MUST exist with numbered analysis points mirroring `02.2-01-VERIFICATION.md` §"Root-Cause Summary"
    - **No production source modified by this task:** `git status --porcelain | grep -E '^( M|MM| D|MD|AM)'` shows only the new `02.2-03-VERIFICATION.md` artifact (Tasks 1 + 2 chart + test source edits must be committed BEFORE this task runs; Task 4 has not fired yet)
  </acceptance_criteria>

  <done>
    `02.2-03-VERIFICATION.md` exists at the correct path with the structural template mirroring `02.2-01-VERIFICATION.md`. Frontmatter populated with clean + rerun exit codes, wall times, spec counts, and `gate_decision: APPROVED` or `gate_decision: BLOCKED`. Sections 1-6 captured with per-spec outcome tables, PVC binding evidence, before/after comparison vs `02.2-01-VERIFICATION.md`, and threat-model mitigations (T-02.2-05/06/07 + carry-forward T-02.2-01/02/03/04). If APPROVED, Task 4 fires to wire Plan 02.2-02's checkpoint; if BLOCKED, Task 4 does NOT fire and the failure routes to a planner re-evaluation per the wave-failure-semantics contract. Committed with a `docs(02.2-03): runtime verification — APPROVED gate (Layer B specs 7/7 PASS on clean run + rerun)` or `docs(02.2-03): runtime verification — BLOCKED gate (root cause: <one-line>)` message that explains the outcome.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update Plan 02.2-02 to reference 02.2-03-VERIFICATION.md + bump depends_on (gated on Task 3 APPROVED gate)</name>
  <files>.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md</files>

  <read_first>
    - .planning/phases/02.2-.../02.2-02-PLAN.md (full file — 382 lines; frontmatter at lines 1-40; threat_model_summary at lines 14-16; key_links at lines 31-39; context block at lines 63-68; Task 1 body at lines 72-136 — specifically the `<read_first>`, `<how-to-verify>`, `<verify>` automated block, and `<acceptance_criteria>` which all reference `02.2-01-VERIFICATION.md`)
    - .planning/phases/02.2-.../02.2-03-VERIFICATION.md (the gate artifact produced by Task 3 above — `gate_decision: APPROVED` is the precondition for this Task 4 to fire)
    - .planning/phases/02.2-.../02.2-01-PLAN.md (existing Plan 01 — DO NOT TOUCH; this task's edits are confined to Plan 02 only)
  </read_first>

  <action>
    **Gate precondition:** Task 4 fires ONLY IF Task 3's `02.2-03-VERIFICATION.md` recorded `gate_decision: APPROVED`. If `gate_decision: BLOCKED`, halt Task 4 — Plan 02.2-02 stays unmodified; the BLOCKED failure routes to a planner re-evaluation.

    Verify the precondition by running:
    ```bash
    grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md
    ```
    If this does NOT match, halt Task 4 and surface the BLOCKED gate.

    If APPROVED, make MINIMAL edits to `02.2-02-PLAN.md` so its checkpoint reads the new authoritative gate artifact `02.2-03-VERIFICATION.md` (NOT `02.2-01-VERIFICATION.md`) and its `depends_on` reflects the new wave ordering.

    **Edit A — frontmatter `depends_on` (line 6):** Replace `depends_on: ["02.2-01"]` with `depends_on: ["02.2-03"]`. This is a single one-line change. The new dependency captures the gate-artifact dependency directly (Plan 02.2-03 produces the gate; Plan 02.2-02 reads it). The transitive dependency on Plan 02.2-01 (which Plan 02.2-03 depends on) is implicit in execute-phase's wave-ordering logic.

    **Edit B — frontmatter `key_links` block (lines 31-39):** Update the one entry that references `02.2-01-VERIFICATION.md` to reference `02.2-03-VERIFICATION.md`. Concretely: change the `from:` line's path from `02.2-01-VERIFICATION.md` to `02.2-03-VERIFICATION.md` and keep the rest of the entry unchanged.

    **Edit C — frontmatter `must_haves.truths` block (lines 22-26):** Update the bullet that references `02.2-01-VERIFICATION.md` to reference `02.2-03-VERIFICATION.md`. Specifically the bullet currently reads "Closeout fires ONLY when 02.2-01-VERIFICATION.md frontmatter says gate_decision: APPROVED" → change to "Closeout fires ONLY when 02.2-03-VERIFICATION.md frontmatter says gate_decision: APPROVED".

    **Edit D — `<context>` block (line 66):** Replace `@.planning/phases/02.2-.../02.2-01-VERIFICATION.md` with `@.planning/phases/02.2-.../02.2-03-VERIFICATION.md`.

    **Edit E — Task 1 `<read_first>` (line 77):** Replace `02.2-01-VERIFICATION.md (the gate artifact produced by Plan 01 Task 3 ...)` with `02.2-03-VERIFICATION.md (the gate artifact produced by Plan 03 Task 3 — Phase 02.2-03 closes the BLOCKED runtime gate from 02.2-01-VERIFICATION.md by adding workspaces.pvc.accessModes as a Helm values override)`.

    **Edit F — Task 1 `<what-built>` (lines 81-83):** Update the prose to reference Plan 02.2-03's deliverables (chart PVC fix + test override + runtime re-verification) in addition to Plan 02.2-01's deliverables. Specifically: append a sentence describing Plan 02.2-03's chart fix unblocking the PVC binding so Layer B specs run.

    **Edit G — Task 1 `<how-to-verify>` (lines 85-101):** Replace all references to `02.2-01-VERIFICATION.md` with `02.2-03-VERIFICATION.md`. There are multiple occurrences in the numbered steps; update each.

    **Edit H — Task 1 `<verify>` automated block (lines 117-123):** Update the grep paths from `02.2-01-VERIFICATION.md` to `02.2-03-VERIFICATION.md`. Two grep paths need bumping.

    **Edit I — Task 1 `<acceptance_criteria>` (lines 125-129):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-03-VERIFICATION.md`.

    **Edit J — Task 1 `<done>` (lines 131-135):** Update the references to `02.2-01-VERIFICATION.md` → `02.2-03-VERIFICATION.md` if any are present.

    **Edit K — `02.2-02-PLAN.md` Task 3 `<action>` Edit B note (around line 254 of 02.2-02-PLAN.md):** If the frontmatter's `progress.completed_plans` reasoning references "this phase's 2 plans," update to "this phase's 3 plans (Plan 01 + Plan 02 + Plan 03)" so the increment math is correct. Similarly update the `Performance Metrics table append` to add a row for Phase 02.2-03 alongside the existing two rows.

    **Edit L — `02.2-02-PLAN.md` `<output>` SUMMARY note (lines 374-381 of 02.2-02-PLAN.md):** Update any references to "the 2 plans" → "the 3 plans" and add Plan 02.2-03 to the close-out chain narrative.

    Constraints:
    - **DO NOT change Plan 02.2-02's structural shape** — the checkpoint:human-verify task type, the threat_model, the must_haves.artifacts, the autonomous: false flag, the Wave 2 ordering (which becomes Wave 3 implicitly via execute-phase's wave-derivation from depends_on) — all stay the same.
    - **DO NOT change Plan 02.2-01** — it's landed; its content is correct as-shipped.
    - **DO NOT add new tasks or remove existing tasks** from Plan 02.2-02.
    - **DO NOT change the threat_model_summary or threat model register** — T-02.2-04 (closeout fires on BLOCKED gate) and T-02.2-05 (ROADMAP edits regress other phases) still apply identically.
    - The edits are surgical: bump three paths (depends_on, key_links from:, context @file, Task 1 read_first/how-to-verify/verify/acceptance_criteria references) + update the prose to describe Plan 02.2-03's role in the close-out chain.
  </action>

  <verify>
    <automated><![CDATA[
      # Precondition: Task 3 must have APPROVED before Task 4 fires
      grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-VERIFICATION.md && \
      # depends_on bumped to ["02.2-03"]
      grep -E '^depends_on:\s*\[.{0,3}"02\.2-03".{0,3}\]$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # The old depends_on form is gone
      [ "$(grep -c '^depends_on: \["02.2-01"\]' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -eq 0 ] && \
      # The new verification path is referenced multiple times (frontmatter, context, Task 1 read_first/how-to-verify/verify/acceptance_criteria/done)
      [ "$(grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 5 ] && \
      # The old verification path is removed from the active references (some prose may still describe 02.2-01 historically — but the active checkpoint path is bumped)
      [ "$(grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -le 2 ] && \
      # The autonomous: false flag is preserved (this plan still has a checkpoint)
      grep -E '^autonomous:\s*false$' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md && \
      # The checkpoint task type is preserved
      [ "$(grep -c 'checkpoint:human-verify' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      # The Plan 01 file is unchanged by this task
      git diff --name-only HEAD~1 HEAD 2>/dev/null | grep -q '02.2-01-PLAN.md' && echo "FAIL: 02.2-01-PLAN.md was modified" && exit 1 || true && \
      [ "$(git status --porcelain .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-01-PLAN.md 2>/dev/null)" = "" ] && \
      # T-02.2-04 / T-02.2-05 threat_model rows preserved
      [ "$(grep -c 'T-02.2-04' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      [ "$(grep -c 'T-02.2-05' .planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-02-PLAN.md)" -ge 1 ] && \
      echo "TASK 4 GATES PASSED"
    ]]></automated>
  </verify>

  <acceptance_criteria>
    - **Precondition gate:** `grep -E '^gate_decision: APPROVED$' .planning/phases/02.2-.../02.2-03-VERIFICATION.md` matches (Task 4 fires ONLY when Task 3's runtime gate was APPROVED)
    - **depends_on bumped:** `grep -E '^depends_on:\s*\[.{0,3}"02\.2-03".{0,3}\]$' .planning/phases/02.2-.../02.2-02-PLAN.md` matches AND `grep -c '^depends_on: \["02.2-01"\]' .planning/phases/02.2-.../02.2-02-PLAN.md` returns 0
    - **New verification path referenced ≥ 5 times:** `grep -c '02.2-03-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returns ≥ 5 (covers key_links from:, context @file, Task 1 read_first, how-to-verify, verify, acceptance_criteria, done — at minimum 5 of these 7 sites must reference the new path)
    - **Old verification path removed from active references:** `grep -c '02.2-01-VERIFICATION.md' .planning/phases/02.2-.../02.2-02-PLAN.md` returns ≤ 2 (a small number of historical mentions in prose context are acceptable; the active checkpoint paths are bumped)
    - **Structural shape preserved:** `grep -E '^autonomous:\s*false$' .planning/phases/02.2-.../02.2-02-PLAN.md` matches AND `grep -c 'checkpoint:human-verify' .planning/phases/02.2-.../02.2-02-PLAN.md` returns ≥ 1
    - **Plan 01 untouched (T-02.2-08 mitigation):** `git status --porcelain .planning/phases/02.2-.../02.2-01-PLAN.md` returns empty (the existing Plan 01 file is NOT modified by this task)
    - **Threat-model rows preserved:** `grep -c 'T-02.2-04' .planning/phases/02.2-.../02.2-02-PLAN.md` returns ≥ 1 AND `grep -c 'T-02.2-05' .planning/phases/02.2-.../02.2-02-PLAN.md` returns ≥ 1
  </acceptance_criteria>

  <done>
    `02.2-02-PLAN.md` has `depends_on: ["02.2-03"]`, the `<context>` reference bumped to `02.2-03-VERIFICATION.md`, and Task 1's `<read_first>`, `<how-to-verify>`, `<verify>`, `<acceptance_criteria>`, and `<done>` all reference the new authoritative gate artifact. The plan's structural shape (autonomous: false, checkpoint:human-verify task type, threat_model_summary, must_haves) is preserved. Plan 02.2-01 is unmodified. Plan 02.2-02 is now correctly gated on Plan 02.2-03's APPROVED runtime gate. Committed with a `docs(02.2-03): wire Plan 02.2-02 checkpoint to 02.2-03-VERIFICATION.md` message (or similar — explains WHY the wiring needs to bump from the BLOCKED gate to the APPROVED gate).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `charts/tide/templates/projects-pvc.yaml` (chart template) → production Helm install | The chart's PVC accessModes default is consumed at `helm install` time by any deployment target; the templated `range` over `.Values.workspaces.pvc.accessModes` MUST default to `[ReadWriteMany]` so existing production deployments render the identical manifest they render today (T-02.2-05) |
| `test/integration/kind/suite_test.go` `applyController()` helm args → test-only chart override | The `--set workspaces.pvc.accessModes={ReadWriteOnce}` flag is local to the kind integration test's BeforeSuite. Production `helm install tide` never sees this flag; CI's `test-int` job is the only consumer (T-02.2-06) |
| `helm template` / `helm lint` rendering → pre-runtime correctness gate | The chart's templated `range` over a values key with `default` MUST produce valid YAML for both the default (no `--set`) and override (`--set workspaces.pvc.accessModes={ReadWriteOnce}`) paths (T-02.2-07) |
| Plan 02.2-03 Task 3 verification artifact (`02.2-03-VERIFICATION.md`) → Plan 02.2-02 Task 1 checkpoint | The new gate artifact replaces `02.2-01-VERIFICATION.md` (BLOCKED) as the authoritative checkpoint source. Plan 02.2-02's `depends_on` and Task 1 references are surgically updated to point at the new artifact (T-02.2-08) |
| Plan 02.2-02 edits (Task 4) → other planning artifacts | The edits MUST be confined to `02.2-02-PLAN.md`. Plan 02.2-01-PLAN.md MUST NOT be modified; the only other planning-artifact change in this plan is the creation of `02.2-03-VERIFICATION.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-05 | Tampering | `charts/tide/templates/projects-pvc.yaml` change breaks production deployments by defaulting accessModes away from `ReadWriteMany` | mitigate | Task 1 acceptance criterion `grep -c 'default (list "ReadWriteMany")' charts/tide/templates/projects-pvc.yaml` returns exactly 1 (the chart template's `default` clause produces `[ReadWriteMany]` when the values key is unset). Task 1 acceptance criterion `grep -c 'accessModes: [ReadWriteMany]' charts/tide/values.yaml` returns exactly 1 (the documented default is preserved). Task 1 acceptance criterion `helm template tide charts/tide --namespace tide-system` without `--set` flag produces a manifest containing `ReadWriteMany` and zero `ReadWriteOnce` occurrences in the PVC block (rendered evidence). Task 3 §Section 6 re-verifies this gate as part of T-02.2-05's mitigation evidence. |
| T-02.2-06 | Information Disclosure (config leak) | Test helm override `--set workspaces.pvc.accessModes={ReadWriteOnce}` leaks RWO assumption into production via misconfigured CI or accidentally-edited values.yaml | mitigate | The `--set` flag lives ONLY in `test/integration/kind/suite_test.go` `applyController()` helm args (lines 399-411 region). It is NEVER added to `charts/tide/values.yaml` (Task 2 acceptance criterion: `grep -c 'ReadWriteOnce' charts/tide/values.yaml` returns 0). Production `helm install tide` never sees the `--set` flag. Task 3 §Section 6 re-verifies via grep. |
| T-02.2-07 | Denial of Service | Chart template rendering breaks (invalid YAML or `helm template` non-zero exit) due to Sprig-idiom error in the new `range`/`default` block | mitigate | Task 1 includes pre-runtime acceptance gates that run `helm template tide charts/tide --namespace tide-system` (default rendering — should emit RWX manifest) AND `helm template tide charts/tide --namespace tide-system --set 'workspaces.pvc.accessModes={ReadWriteOnce}'` (override rendering — should emit RWO manifest) AND `helm lint charts/tide` (chart-wide lint). All three must exit 0 with the correct manifest content before Task 2's test edit or Task 3's runtime reproducer fires. |
| T-02.2-08 | Tampering | Task 4's edits to `02.2-02-PLAN.md` accidentally regress an unrelated gate, threat-model row, or checkpoint clause | accept | Task 4 acceptance criteria explicitly verify: (a) `depends_on` is bumped from `["02.2-01"]` to `["02.2-03"]`; (b) `02.2-03-VERIFICATION.md` is referenced ≥ 5 times across the plan body; (c) `autonomous: false` is preserved; (d) `checkpoint:human-verify` task type is preserved; (e) `T-02.2-04` and `T-02.2-05` threat-model rows are preserved; (f) `02.2-01-PLAN.md` is NOT modified. The disposition is `accept` (not `mitigate`) because the edits are intrinsically narrow surgical-path-replacements; the acceptance grep gates carry the verification. |

</threat_model>

<verification>
After all four tasks complete, the overall plan verification:

**Source-shape gates (cumulative from Tasks 1 + 2):**
- `helm template tide charts/tide --namespace tide-system` exits 0 with `ReadWriteMany` in the PVC block (default render)
- `helm template tide charts/tide --namespace tide-system --set 'workspaces.pvc.accessModes={ReadWriteOnce}'` exits 0 with `ReadWriteOnce` in the PVC block (override render)
- `helm lint charts/tide` exits 0
- `go build ./test/integration/kind/...` exits 0; `go vet ./test/integration/kind/...` exits 0; `gofmt -l test/integration/kind/suite_test.go` produces empty output
- All grep gates from Tasks 1 + 2 pass

**Phase 1 import firewalls remain green:**
- `make verify-dag-imports` exits 0
- `make verify-dispatch-imports` exits 0
- `make verify-import-firewall` exits 0

**Verification artifact (Task 3):**
- `.planning/phases/02.2-.../02.2-03-VERIFICATION.md` exists with required frontmatter
- `gate_decision` is one of `APPROVED` or `BLOCKED`
- All runtime evidence fields populated (exit codes, wall times, spec counts)
- All three new threat mitigations (T-02.2-05/06/07) addressed in §Section 6

**Plan 02.2-02 wiring (Task 4 — gated on Task 3 APPROVED):**
- `.planning/phases/02.2-.../02.2-02-PLAN.md` `depends_on: ["02.2-03"]`
- `02.2-03-VERIFICATION.md` referenced ≥ 5 times in Plan 02.2-02
- `02.2-01-PLAN.md` is NOT modified
- T-02.2-04 / T-02.2-05 threat rows preserved
</verification>

<success_criteria>
- All four tasks committed atomically (Tasks 1, 2, 3 fire unconditionally; Task 4 fires only if Task 3's `02.2-03-VERIFICATION.md` records `gate_decision: APPROVED`)
- `charts/tide/templates/projects-pvc.yaml` renders `accessModes` from `.Values.workspaces.pvc.accessModes` with `[ReadWriteMany]` default; `helm.sh/resource-policy: keep` annotation preserved
- `charts/tide/values.yaml` documents `workspaces.pvc.accessModes: [ReadWriteMany]` as production default with kind/minikube override guidance
- `test/integration/kind/suite_test.go` `applyController()` helm args include `--set workspaces.pvc.accessModes={ReadWriteOnce}` (test-only override)
- `helm template` + `helm lint` exit 0 for both default and override paths
- `02.2-03-VERIFICATION.md` captures runtime reality with `gate_decision: APPROVED` on clean run + idempotent rerun (7/7 specs PASS, ≤ 300s go test wall, T-02.2-05/06/07 mitigations verified)
- If APPROVED: Plan 02.2-02's `depends_on` and Task 1 references bumped to `02.2-03-VERIFICATION.md`; Plan 02.2-02 is unblocked
- If BLOCKED: Task 4 does NOT fire; the failure surfaces a Root-Cause Summary for planner re-evaluation; Plan 02.2-02 stays gated on its existing `02.2-01-VERIFICATION.md` (which is also BLOCKED — so the structural gate continues to hold correctly)
- No production source files (`cmd/`, `internal/`, `api/`, `pkg/`) modified by this plan — the chart edit + test edit are the entire source surface, both already permitted by the project's `<files_modified>` discipline
- Phase 02.1's four BLOCKED root causes (now closed at the source-shape layer per `02.2-01-VERIFICATION.md`) are joined by the new Phase 02.2 PVC-binding fix to close the runtime gate fully
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-layer-b-kind-test-timing-fixes-bump-kindtesttimeout-from-4mi/02.2-03-SUMMARY.md` documenting:
- The chart edit (projects-pvc.yaml + values.yaml — `accessModes` now overridable, production default preserved)
- The test edit (suite_test.go — `--set workspaces.pvc.accessModes={ReadWriteOnce}` for kind RWO compatibility)
- The verification artifact's gate decision (APPROVED or BLOCKED)
- Per-spec outcomes from the clean run + rerun (with PVC binding evidence — `Bound` phase + `["ReadWriteOnce"]` access modes)
- Whether Task 4 fired (Plan 02.2-02 wiring update) — gated on Task 3 APPROVED
- Any deviations from the locked decisions (should be zero; this is the directed Option A approach from `02.2-01-VERIFICATION.md` §"Fix landscape")
- Threat-model mitigations applied (T-02.2-05/06/07/08) with concrete evidence (grep counts on rendered manifests; `helm template` + `helm lint` exit codes; values.yaml unchanged-for-RWO grep)
- Carry-forward verification of T-02.2-01/02/03/04 from Plan 02.2-01 (same threat patterns against the new log paths)

ROADMAP.md update (Plan list bump from 2 to 3 plans + add 02.2-03 to the Plans list with completion checkbox if APPROVED) is OUT OF SCOPE for Plan 02.2-03 — it belongs to Plan 02.2-02 Task 2 (the existing closeout plan, post-wiring-update). The orchestrator may choose to do the ROADMAP edit centrally after Task 4 lands, or defer to Plan 02.2-02 Task 2 to handle it as part of its existing scope.
</output>
</content>
