---
phase: 49-common-loop-contract-verdict-envelope-persistence-schema
plan: 03
type: execute
wave: 2
depends_on: [49-02]
files_modified:
  - cmd/tide-langgraph-verifier/verifier/verdict.py
  - cmd/tide-langgraph-verifier/verifier/envelope.py
  - cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py
  - cmd/tide-langgraph-verifier/verifier/tests/conftest.py
autonomous: true
requirements: [EVAL-03]

must_haves:
  truths:
    - "The Python classify_verdict maps empty / missing-verdict-field / malformed to BLOCKED, never APPROVED — the 1:1 mirror of the Go classifier"
    - "The Python GateDecision/Finding pair validates and re-emits the SAME pkg/dispatch/testdata/gate_decision_golden.json Plan 02 authored"
    - "EnvelopeIn accepts an optional nested `verify` object, extracted like provider (non-object => EnvelopeError, never an uncaught crash), and still round-trips unknown fields untouched"
    - "write_termination_stub accepts gate_decision/findings_count/high_severity_count and stays under 4096 bytes"
  artifacts:
    - path: "cmd/tide-langgraph-verifier/verifier/verdict.py"
      provides: "pydantic Finding/GateDecision + Verdict enum + classify_verdict + go.mod-anchored golden-fixture path"
      contains: "def classify_verdict"
    - path: "cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py"
      provides: "golden round-trip (reads Go's fixture) + 3-shape fail-closed parametrize"
      contains: "test_classify_verdict_fails_closed"
    - path: "cmd/tide-langgraph-verifier/verifier/envelope.py"
      provides: "verify extraction on EnvelopeIn + extended write_termination_stub"
      contains: "verify"
  key_links:
    - from: "verifier/tests/test_verdict.py"
      to: "pkg/dispatch/testdata/gate_decision_golden.json"
      via: "go.mod-anchored _repo_root() path walk"
      pattern: "gate_decision_golden.json"
    - from: "verifier/verdict.py classify_verdict"
      to: "Verdict.BLOCKED"
      via: "every non-exact-match branch returns BLOCKED (mirrors Go)"
      pattern: "return Verdict.BLOCKED"
---

<objective>
Author the Python half of the `gate_decision` verdict contract in the LangGraph verifier image: a new `verifier/verdict.py` (Pydantic `Finding`/`GateDecision` + `Verdict` enum + the fail-closed `classify_verdict`), the `verify` field on the Python `EnvelopeIn`, and the extended `write_termination_stub`. Prove Go↔Python parity by validating and re-emitting the SAME golden fixture Plan 02 authored.

Purpose: Complete EVAL-03 — the matched Go+Pydantic verdict pair kept honest by a shared golden fixture and a mirrored fail-closed classifier, under the import firewall (the Python image cannot import the Go types; the JSON tags/aliases are the contract, the fixture is the proof).

Output: `verifier/verdict.py`, modified `verifier/envelope.py`, new `verifier/tests/test_verdict.py`, `verifier/tests/conftest.py` factory.

Scope discipline (LOCKED): schema/parity ONLY. Do NOT build `create_agent(response_format=...)` dispatch, prompts, or any evaluation logic (Phase 51). The Pydantic models are shaped so Phase 51's structured-output can consume them, but nothing consumes `response_format` this phase. Severity/dimension stay FREE STRINGS (typed enums deferred to Phase 51).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-CONTEXT.md
@.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-RESEARCH.md
@.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-PATTERNS.md
@pkg/dispatch/verdict.go
@pkg/dispatch/testdata/gate_decision_golden.json

<interfaces>
<!-- The Python conventions this plan extends. Extracted from the tree; no exploration needed. -->

Module-docstring + discriminator convention — verifier/envelope.py:1-28 (VERIFIED):
  Module docstring names the Go source + import firewall; API_VERSION / KIND_IN /
  KIND_OUT / TERMINATION_STUB_MAX_BYTES=4096 constants. verdict.py mirrors this,
  referencing pkg/dispatch/verdict.go, and notes it is pydantic.BaseModel (NOT
  @dataclass like EnvelopeIn) — Phase 51's create_agent(response_format=) needs Pydantic.

EnvelopeIn dataclass + accept-and-ignore idiom — verifier/envelope.py:41-58 (VERIFIED):
  typed fields + `raw: dict = field(default_factory=dict)` catch-all. Adding a `verify`
  field MUST preserve the unknown-field-tolerance test (test_envelope.py:78-86, which
  literally passes futureField="something-phase-49-adds").

Validate-then-extract nested-object idiom — verifier/envelope.py:97-103 (VERIFIED):
  provider = raw.get("provider"); if None -> {}; elif not isinstance(provider, dict)
  -> raise EnvelopeError(...). This is the WR-01 fail-closed precedent
  (test_envelope.py:63-75). Apply the IDENTICAL guard to `verify`.

write_termination_stub truncation loop to extend — verifier/envelope.py:149-175 (VERIFIED):
  writes {exitCode, reason}, truncates `reason` in a while-loop until <4096. New params
  gate_decision/findings_count/high_severity_count are bounded (enum + ints) — join the
  dict unconditionally, no truncation needed (only `reason` is unbounded free text).

conftest factory to mirror — verifier/tests/conftest.py:61-98 (VERIFIED):
  envelope_in_dict() factory-with-overrides + API_VERSION/KIND_TASK_ENVELOPE_IN consts.
  Add a sibling gate_decision_dict factory seeded with APPROVED/REPAIRABLE/BLOCKED + findings.

Golden-fixture path resolution (NEW plumbing — no in-repo precedent): make
  test-langgraph-verifier cd's into cmd/tide-langgraph-verifier before pytest, so a fixed
  Path(__file__).parents[N] is fragile. Walk up from __file__ to the go.mod marker
  (RESEARCH Pitfall 4 / PATTERNS verdict.py section) and descend to
  pkg/dispatch/testdata/gate_decision_golden.json.

Go contract this mirrors — pkg/dispatch/verdict.go (Plan 02): Verdict = APPROVED|
  REPAIRABLE|BLOCKED; Finding{dimension,severity,confidence,evidence,suggestedFix};
  GateDecision{verdict,summary,findings[]}. JSON tags are the frozen contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verdict.py — pydantic GateDecision/Finding + classify_verdict + golden round-trip + fail-closed tests</name>
  <files>cmd/tide-langgraph-verifier/verifier/verdict.py, cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py, cmd/tide-langgraph-verifier/verifier/tests/conftest.py</files>
  <behavior>
    - classify_verdict("")                                        -> Verdict.BLOCKED
    - classify_verdict('{"summary":"looks fine","findings":[]}')  -> Verdict.BLOCKED
    - classify_verdict("{not valid json")                         -> Verdict.BLOCKED
    - classify_verdict('{"verdict":"APPROVED",...}')              -> Verdict.APPROVED
    - GateDecision.model_validate(golden_fixture_bytes) yields verdict==REPAIRABLE, non-empty summary, >=1 finding with all five fields; re-dumping and re-validating is value-equivalent (NOT byte-equal — key order differs Go↔Python).
  </behavior>
  <read_first>
    - verifier/envelope.py:1-28 (module docstring + discriminator-constant convention verdict.py copies)
    - verifier/tests/test_envelope.py:63-86 (parametrize + pytest.raises + unknown-field-tolerance conventions)
    - verifier/tests/conftest.py:61-98 (envelope_in_dict factory to add gate_decision_dict beside)
    - pkg/dispatch/verdict.go + pkg/dispatch/testdata/gate_decision_golden.json (Plan 02 output — the frozen contract + the fixture to read)
    - .planning/.../49-RESEARCH.md Pitfall 4 (go.mod-anchored path walk), Code Examples §2, Assumption A4 (BaseModel not dataclass); PATTERNS.md verdict.py section
  </read_first>
  <action>
    Create `cmd/tide-langgraph-verifier/verifier/verdict.py` with a module docstring referencing `pkg/dispatch/verdict.go` and the import firewall. Declare:
    - `class Verdict(str, Enum)` with `APPROVED = "APPROVED"`, `REPAIRABLE = "REPAIRABLE"`, `BLOCKED = "BLOCKED"` (str-Enum so JSON values compare directly).
    - `class Finding(pydantic.BaseModel)` with fields `dimension`, `severity`, `confidence`, `evidence`, `suggested_fix` — each mapped to the Go JSON tags via pydantic aliases (`suggested_fix` -> alias `suggestedFix`), all optional/str, `populate_by_name=True` so both snake and camel round-trip. Docstring: free strings this phase (coverage-not-conservatism).
    - `class GateDecision(pydantic.BaseModel)` with `verdict: Verdict`, `summary: str = ""`, `findings: list[Finding] = []`.
    - `def classify_verdict(raw: str | bytes) -> Verdict` — the identical 3-branch fail-closed shape as the Go classifier so test names line up 1:1: empty → BLOCKED; `json.JSONDecodeError` → BLOCKED; a dict whose `verdict` is one of the three terminals → that terminal; everything else (missing/unrecognized field, non-dict) → BLOCKED.
    - A `_repo_root()` helper that walks up from `__file__` until `go.mod` is found (raise a clear RuntimeError if none), and `GOLDEN_FIXTURE = _repo_root() / "pkg" / "dispatch" / "testdata" / "gate_decision_golden.json"`.

    Add a `gate_decision_dict` factory fixture to `verifier/tests/conftest.py` (mirroring `envelope_in_dict`'s factory-with-overrides shape), seeded with a valid REPAIRABLE verdict + one full finding.

    Create `verifier/tests/test_verdict.py`:
    - `test_golden_fixture_round_trip`: read `verdict.GOLDEN_FIXTURE` bytes, `GateDecision.model_validate_json` them, assert verdict==Verdict.REPAIRABLE, summary non-empty, >=1 finding with all five fields; re-dump via `model_dump_json(by_alias=True)` and re-validate to prove value-equivalence (NOT raw byte compare).
    - `test_classify_verdict_fails_closed`: a `@pytest.mark.parametrize` table over the four rows in the behavior block.
  </action>
  <acceptance_criteria>
    - `verifier/verdict.py` declares `Verdict`, `Finding`, `GateDecision` (pydantic.BaseModel), and `classify_verdict`.
    - `grep -c 'return Verdict.BLOCKED' cmd/tide-langgraph-verifier/verifier/verdict.py` returns >= 3.
    - The golden-fixture path is resolved by walking to `go.mod`, not a hardcoded `parents[N]` (grep: `go.mod` appears in verdict.py).
    - `make test-langgraph-verifier` exits 0 (both new tests green; existing verifier tests unbroken).
    - The Python test reads the SAME `pkg/dispatch/testdata/gate_decision_golden.json` (grep: `gate_decision_golden.json` in verdict.py or test_verdict.py).
  </acceptance_criteria>
  <verify>
    <automated>make test-langgraph-verifier</automated>
  </verify>
  <done>Pydantic verdict pair + mirrored fail-closed classifier + cross-language golden round-trip green; the fixture is shared, not duplicated; import firewall preserved (Python never imports Go).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: envelope.py — verify extraction on EnvelopeIn + extended write_termination_stub</name>
  <files>cmd/tide-langgraph-verifier/verifier/envelope.py, cmd/tide-langgraph-verifier/verifier/tests/test_verdict.py</files>
  <behavior>
    - read_envelope_in on a doc with a nested `verify` object extracts it; a `verify` that is present-but-not-an-object raises EnvelopeError (never an uncaught AttributeError) — the WR-01 fail-closed precedent applied to verify.
    - An envelope with NO verify key still parses, and unknown top-level keys still round-trip via `raw` untouched (test_envelope.py:78-86 stays green).
    - write_termination_stub(path, exit_code=0, gate_decision="REPAIRABLE", findings_count=3, high_severity_count=1) writes those keys and the serialized doc stays < 4096 bytes.
  </behavior>
  <read_first>
    - verifier/envelope.py:41-115 (EnvelopeIn dataclass + read_envelope_in validate-then-extract idiom; the provider extraction at :97-103 to mirror for verify)
    - verifier/envelope.py:149-175 (write_termination_stub truncation loop to extend)
    - verifier/tests/test_envelope.py:63-86 (the non-object-provider fail-closed test + unknown-field tolerance test to mirror for verify)
    - .planning/.../49-CONTEXT.md D-03 (verify field mirrors *DispatchMeta/*Dev; minimal) + D-05a (stub extension)
  </read_first>
  <action>
    Modify `cmd/tide-langgraph-verifier/verifier/envelope.py`:
    - Add an optional `verify: dict[str, Any] | None = None` field to the `EnvelopeIn` dataclass (or a typed nested extraction consistent with provider). In `read_envelope_in`, extract `verify = raw.get("verify")` with the IDENTICAL guard shape used for `provider` (:97-103): `None` → leave `None`; present-but-`not isinstance(dict)` → `raise EnvelopeError(...)`. Do NOT weaken the accept-and-ignore contract — `raw` still carries every key, so the unknown-field-tolerance test stays green.
    - Extend `write_termination_stub` signature to `(path, *, exit_code, reason="", gate_decision="", findings_count=0, high_severity_count=0)`. Add the three new keys to the `stub` dict (`gateDecision`/`findingsCount`/`highSeverityCount`, matching the Go JSON tags) unconditionally — they are bounded, so no truncation loop change is needed; only `reason` continues to truncate. Keep the ≤4096 invariant.

    Add to `verifier/tests/test_verdict.py` (or a sibling test): a `verify`-extraction test (populated verify round-trips; non-object verify raises EnvelopeError; missing verify parses fine) mirroring `test_envelope.py`'s non-object-provider parametrize, and a `write_termination_stub`-with-verdict-fields size assertion (< 4096 with the three new fields set).
  </action>
  <acceptance_criteria>
    - `EnvelopeIn` carries an optional `verify` field; `read_envelope_in` guards a non-object `verify` with `EnvelopeError` (grep: `verify` appears in the `read_envelope_in` extraction with an `isinstance` guard).
    - `write_termination_stub` accepts `gate_decision`, `findings_count`, `high_severity_count` params and emits `gateDecision`/`findingsCount`/`highSeverityCount` keys.
    - The existing `test_read_envelope_in_tolerates_unknown_fields` still passes (accept-and-ignore contract preserved).
    - `make test-langgraph-verifier` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>make test-langgraph-verifier</automated>
  </verify>
  <done>Python EnvelopeIn extracts `verify` fail-closed; write_termination_stub carries the bounded verdict summary under 4096 bytes; unknown-field tolerance intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in.json (controller-authored) → verifier image | The verifier reads `EnvelopeIn` (incl. the new `verify` object) from the PVC-mounted `in.json`. A malformed nested `verify` must fail closed, not crash the entrypoint into an undefined state. |
| verifier image → out.json / termination message → Manager | The verifier writes the verdict + `TerminationStub`; the size cap and the shared verdict schema are the controls that keep the Go side's parse honest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03-01 | Tampering / Elevation of Privilege | `classify_verdict` (a malformed/empty verdict misclassified as APPROVED on the Python side) | mitigate | Mirrored fail-closed classifier: empty/malformed/missing-field → `Verdict.BLOCKED`, 1:1 with the Go regression shapes; parametrized regression test is the control |
| T-49-03-02 | Tampering | Go↔Python schema drift (Python emits/accepts a shape the Go side misparses) | mitigate | The Python test validates and re-emits the SAME `pkg/dispatch/testdata/gate_decision_golden.json` Plan 02 authored — a single shared fixture, not a Python-local copy |
| T-49-03-03 | Denial of Service | A non-object `verify` (or malformed nested payload) crashing the verifier entrypoint with an uncaught exception | mitigate | `read_envelope_in` applies the WR-01 `isinstance`-then-`EnvelopeError` guard to `verify`, exactly as it already does for `provider` — a clean typed failure, never an AttributeError |
| T-49-03-04 | Denial of Service | `TerminationStub` overflow past the 4KB cap | mitigate | The new stub fields are bounded (enum string + two ints), added to the dict without expanding the unbounded-`reason` truncation budget; size test is the control |
| T-49-03-SC | Tampering | pip installs | accept | No new packages this phase (pydantic already pinned per Phase 48's audit); `make verify-langgraph-pins` continues to gate the verifier image's pins |
</threat_model>

<verification>
- `make test-langgraph-verifier` — pydantic verdict pair, mirrored fail-closed classifier, cross-language golden round-trip, verify-extraction fail-closed, stub-size all green.
- The golden fixture read by pytest is byte-identical to the one `go test ./pkg/dispatch/...` reads (same path under `pkg/dispatch/testdata/`).
- `make verify-langgraph-pins` — no new/unpinned Python dependency was introduced.
</verification>

<success_criteria>
- The matched Go+Pydantic `GateDecision`/`Finding` pair round-trips the shared golden fixture in both languages (Phase success criterion #2, Python half — completes EVAL-03 with Plan 02).
- Empty/missing-field/malformed verdict classifies fail-closed to BLOCKED in Python, mirroring Go (Phase success criterion #3, Python half).
- `EnvelopeIn.verify` extraction and the extended `write_termination_stub` preserve the fail-closed + ≤4KB contracts.
</success_criteria>

<output>
Create `.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-03-SUMMARY.md` when done.
</output>
