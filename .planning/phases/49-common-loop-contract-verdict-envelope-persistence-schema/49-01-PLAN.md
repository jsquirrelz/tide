---
phase: 49-common-loop-contract-verdict-envelope-persistence-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha3/loop_types.go
  - api/v1alpha3/loop_types_test.go
  - api/v1alpha3/zz_generated.deepcopy.go
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-03]

must_haves:
  truths:
    - "LoopPolicy, LoopStatus, and EvaluationSummary compile and deepcopy-generate with zero `make generate` diff"
    - "LoopStatus carries only the current-iteration summary + exit reason — no accumulating history slice (LOOP-03)"
    - "Both types embed into a synthetic test-only CRD Spec/Status and round-trip through JSON (embeddability proof)"
    - "LoopPolicy/LoopStatus type godoc names all five loop elements (goal · candidate · evaluator feedback · repeat policy · bounded exit) — LOOP-02"
    - "`make manifests` produces zero CRD-YAML diff (types are standalone, embedded in no Kind this phase)"
  artifacts:
    - path: "api/v1alpha3/loop_types.go"
      provides: "LoopPolicy, LoopStatus, EvaluationSummary + AutonomyLevel/EscalationPolicy/ExitReason enum types"
      contains: "type LoopPolicy struct"
    - path: "api/v1alpha3/loop_types_test.go"
      provides: "JSON round-trip, LOOP-03 compile-time structural guard, synthetic-embedder deepcopy test"
      contains: "LoopStatus{"
    - path: "api/v1alpha3/zz_generated.deepcopy.go"
      provides: "controller-gen DeepCopy/DeepCopyInto for the three new structs"
      contains: "func (in *LoopPolicy) DeepCopy"
  key_links:
    - from: "api/v1alpha3/loop_types.go"
      to: "controller-gen (make generate)"
      via: "package-level +kubebuilder:object:generate=true marker"
      pattern: "func \\(in \\*LoopStatus\\) DeepCopyInto"
    - from: "api/v1alpha3/loop_types_test.go"
      to: "api/v1alpha3.LoopStatus"
      via: "compile-time struct literal naming every field"
      pattern: "LoopStatus\\{"
---

<objective>
Author the shared, CRD-embeddable loop-contract API types — `LoopPolicy`, `LoopStatus`, and the bounded `EvaluationSummary` projection — as standalone, deepcopy-generated Go types in `api/v1alpha3/`, carrying the five-element-loop-test doc-comments (LOOP-02) and structurally guaranteed to hold no iteration history (LOOP-03).

Purpose: Lock the reusable primitives the Task loop (Phase 51) and every later loop level will embed, before any halt/reconciler logic is written on top of them. No generic `Loop` controller — shared types only (LOOP-01).

Output: `api/v1alpha3/loop_types.go`, `api/v1alpha3/loop_types_test.go`, regenerated `zz_generated.deepcopy.go`.

Scope discipline (LOCKED — do NOT do these): Do NOT embed `LoopPolicy`/`LoopStatus` into `TaskSpec`/`TaskStatus` — that is Phase 51's TASK-01. Do NOT add `ConditionVerifyHalt` or any condition constant (Phase 50). The types are standalone and referenced by no `Kind` this phase; `make manifests` MUST produce zero CRD-YAML diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-CONTEXT.md
@.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-RESEARCH.md
@.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-PATTERNS.md

<interfaces>
<!-- Field-type conventions and doc-comment idioms the executor MUST replicate. Extracted from the tree; no exploration needed. -->

Package-level deepcopy marker — api/v1alpha3/groupversion_info.go:17-20 (VERIFIED):
  // +kubebuilder:object:generate=true above `package v1alpha3` gives every
  // exported struct DeepCopy/DeepCopyInto for free. loop_types.go needs NO
  // per-type marker (Caps at task_types.go:31 and Gates at project_types.go:40
  // both get deepcopy with none).

Two-homes / bounded-projection doc-comment to copy — api/v1alpha3/task_types.go:23-30 (VERIFIED):
  "Design note: api/v1alpha3.Caps and pkg/dispatch.Caps are intentionally two
   separate types that serve different layers ... keeping the CRD schema and the
   dispatch interface decoupled." EvaluationSummary MUST carry the equivalent
   note (locally-scoped Decision string, NOT pkg/dispatch.GateDecision imported).

Enum idiom to copy — api/v1alpha3/shared_types.go:382-396 (VERIFIED):
  // +kubebuilder:validation:Enum=strict;conservative
  type FailureProfileType string
  const ( FailureProfileStrict FailureProfileType = "strict"; ... )

Numeric field-type table (grep-verified across api/v1alpha3/*.go):
  cost cents        -> int64            (BudgetConfig.AbsoluteCapCents project_types.go:99)
  bounded duration  -> *metav1.Duration (BudgetConfig.RollingWindowDuration :114)
  iteration counter -> int32            (Caps.Iterations task_types.go:40; use int32, NOT the bare-int TaskStatus.Attempt outlier)
  []metav1.Condition-> +listType=map / +listMapKey=type (TaskStatus.Conditions task_types.go:151-154)

Ref field convention — plain string, NEVER corev1.LocalObjectReference
  (CredsSecretRef string at project_types.go:224). EvaluatorRef follows this.

Compile-time forbidden-field guard to copy — pkg/dispatch/envelope_test.go:698-716
  (TestTerminationStub_NoForbiddenFields): a `_ = T{...}` literal naming every
  field so a later history-slice addition fails to COMPILE.

External-test-package + reflect convention — api/v1alpha3/schema_test.go:28,40:
  package v1alpha3_test ; reflect.TypeFor[tidev1alpha3.ProjectSpec]()

Five-element loop test wording — notes/five-loop-model.md:43 (via CONTEXT D-06):
  goal/spec · mutable candidate · evaluator/environment feedback · repeat policy
  · bounded exit/escalation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author loop_types.go (LoopPolicy, LoopStatus, EvaluationSummary) + regenerate deepcopy</name>
  <files>api/v1alpha3/loop_types.go, api/v1alpha3/zz_generated.deepcopy.go</files>
  <read_first>
    - api/v1alpha3/task_types.go:23-51 (the Caps two-homes decoupling doc-comment + field-type idiom EvaluationSummary mirrors)
    - api/v1alpha3/shared_types.go:382-396 (the FailureProfileType enum three-part shape: typed string + +kubebuilder:validation:Enum + doc-commented consts) for AutonomyLevel/EscalationPolicy/ExitReason
    - api/v1alpha3/project_types.go:99-115 (BudgetConfig — int64 cents + *metav1.Duration precedent)
    - api/v1alpha3/groupversion_info.go:17-20 (package-level deepcopy marker — confirm no per-type marker is needed)
    - .planning/.../49-CONTEXT.md decisions D-01 (two homes; LoopStatus.LastEvaluation is a bounded EvaluationSummary, never findings[]) and D-06 (five-element doc-comments)
  </read_first>
  <action>
    Create `api/v1alpha3/loop_types.go` in `package v1alpha3` with `metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"` imported. Declare, per D-01/D-06 and the field-type table:

    - `LoopPolicy` struct with fields: `MaxIterations int32 json:"maxIterations,omitempty"` (+kubebuilder:validation:Minimum=0), `MaxDuration *metav1.Duration json:"maxDuration,omitempty"`, `BudgetCents int64 json:"budgetCents,omitempty"` (+kubebuilder:validation:Minimum=0), `Autonomy AutonomyLevel json:"autonomy,omitempty"`, `EvaluatorRef string json:"evaluatorRef,omitempty"` (plain string same-namespace ref, NOT corev1.LocalObjectReference), `EscalationPolicy EscalationPolicy json:"escalationPolicy,omitempty"`.
    - `LoopStatus` struct with fields: `Iteration int32 json:"iteration,omitempty"`, `ParentRunID string json:"parentRunID,omitempty"`, `LastEvaluation *EvaluationSummary json:"lastEvaluation,omitempty"`, `ExitReason ExitReason json:"exitReason,omitempty"`, `CostCents int64 json:"costCents,omitempty"`, `Conditions []metav1.Condition json:"conditions,omitempty"` (+listType=map +listMapKey=type +optional). NO slice-of-summaries / history field — LOOP-03.
    - `EvaluationSummary` struct (the bounded verdict projection) with a "Design note" doc-comment mirroring Caps: `Decision string json:"decision,omitempty"` (+kubebuilder:validation:Enum=APPROVED;REPAIRABLE;BLOCKED), `FindingsCount int32 json:"findingsCount,omitempty"`, `HighSeverityCount int32 json:"highSeverityCount,omitempty"`, `CompletedAt *metav1.Time json:"completedAt,omitempty"`. Doc note: this is a LOCALLY-SCOPED projection, never the full pkg/dispatch.GateDecision imported (D-01 forbids that import; keeps CRD schema decoupled from the wire package like Caps/Caps).
    - Enum types `AutonomyLevel`, `EscalationPolicy`, `ExitReason` (typed strings) each with a `+kubebuilder:validation:Enum=...` marker and a doc-commented const block. For Autonomy/EscalationPolicy use the vocabulary implied by CONTEXT/five-loop-model (e.g. autonomous/supervised; escalate/requireApproval). Per RESEARCH Assumption A2, `ExitReason`'s value set is Claude's Discretion — pick a small documented set (do NOT copy Phase 50's Execution-loop terminal-reason set verbatim); doc-comment that the vocabulary may grow per consumer.

    On the `LoopPolicy` AND `LoopStatus` type-level godoc, name all FIVE loop elements explicitly (D-06 / LOOP-02): a goal/spec, a mutable candidate, evaluator/environment feedback, a repeat policy, and a bounded exit/escalation — so a future construct missing an element is provably a pipeline stage, not a loop. Add the "Fields are minimal for the two v1.0.9 consumers (Task loop + plan-check); grow per loop, never a speculative superset" framing. Do NOT add `+kubebuilder:object:root` markers (these are embeddable structs, not a Kind). Run `make generate` to regenerate `zz_generated.deepcopy.go`.
  </action>
  <acceptance_criteria>
    - `api/v1alpha3/loop_types.go` declares `LoopPolicy`, `LoopStatus`, `EvaluationSummary` and the three enum types with the exact JSON tags above.
    - `LoopStatus` has NO field whose type is a slice of `EvaluationSummary` (grep: `grep -c 'EvaluationSummary$' api/v1alpha3/loop_types.go` counts only the `*EvaluationSummary` pointer field usage — no `[]EvaluationSummary`).
    - Both `LoopPolicy` and `LoopStatus` godoc blocks contain all five loop-element phrases (grep for "candidate" and "bounded exit" / "escalation" in the doc-comments).
    - `go build ./api/v1alpha3/...` exits 0.
    - `make generate && git diff --exit-code` exits 0 (deepcopy regenerated and committed; `LoopPolicy`/`LoopStatus`/`EvaluationSummary` appear in `zz_generated.deepcopy.go`).
    - `make manifests && git diff --exit-code -- config/crd` exits 0 (zero CRD-YAML diff — types embedded in no Kind).
  </acceptance_criteria>
  <verify>
    <automated>go build ./api/v1alpha3/... && make generate && git diff --exit-code && make manifests && git diff --exit-code -- config/crd</automated>
  </verify>
  <done>All three structs + three enums exist with the locked field types/tags; five-element doc-comments present on both loop types; deepcopy regenerated with zero diff; zero CRD-YAML diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: loop_types_test.go — round-trip, LOOP-03 structural guard, synthetic-embedder proof</name>
  <files>api/v1alpha3/loop_types_test.go</files>
  <behavior>
    - JSON round-trip: a fully-populated LoopPolicy and LoopStatus marshal and unmarshal back to an equal value (reflect.DeepEqual), with LastEvaluation's bounded fields preserved.
    - LOOP-03 compile-time guard: a `_ = LoopStatus{...}` literal that names every LoopStatus field explicitly — if a future PR adds a history slice, this literal fails to COMPILE.
    - Embeddability (Success Criterion #1): a test-only synthetic struct embedding both LoopPolicy and LoopStatus deep-copies and JSON-round-trips, proving the "embeddable in any domain CRD" claim without touching a real Kind.
  </behavior>
  <read_first>
    - pkg/dispatch/envelope_test.go:698-716 (TestTerminationStub_NoForbiddenFields — the exact compile-time struct-literal guard shape to copy for LoopStatus)
    - api/v1alpha3/schema_test.go:28,40-41 (package v1alpha3_test + reflect.TypeFor convention)
    - api/v1alpha3/loop_types.go (Task 1 output — the fields to enumerate)
    - .planning/.../49-RESEARCH.md Pitfall 3 (history-creep) and Open Question 1 (synthetic embedder is the recommended embeddability proof)
  </read_first>
  <action>
    Create `api/v1alpha3/loop_types_test.go` (package `v1alpha3_test`, importing the api package as `tidev1alpha3` per schema_test.go). Add:

    - `TestLoopPolicy_JSONRoundTrip` and `TestLoopStatus_JSONRoundTrip`: marshal a fully-populated value (LoopStatus with a non-nil `LastEvaluation` carrying Decision/FindingsCount/HighSeverityCount/CompletedAt), unmarshal, assert `reflect.DeepEqual`.
    - `TestLoopStatus_NoForbiddenFields`: a compile-time `_ = tidev1alpha3.LoopStatus{ Iteration: 0, ParentRunID: "", LastEvaluation: nil, ExitReason: "", CostCents: 0, Conditions: nil }` literal naming EVERY field (mirrors TestTerminationStub_NoForbiddenFields) so adding a `PreviousEvaluations []EvaluationSummary`-style history field fails to compile; plus a runtime assertion that marshalled `LoopStatus` JSON contains no key implying history (assert `!strings.Contains(json, "previousEvaluations")` / no array-of-summaries key).
    - `TestLoopContract_Embeddable`: declare a test-only `type testEmbedder struct { Spec struct{ Policy tidev1alpha3.LoopPolicy } ; Status struct{ Loop tidev1alpha3.LoopStatus } }`, populate, call `.DeepCopy`-equivalent via the generated methods on the embedded types (or reflect), and JSON round-trip — proving embeddability. (Per RESEARCH Open Question 1: this is the checker-facing proof the "embeddable in any domain CRD" claim is not aspirational; no real CRD touched.)
  </action>
  <acceptance_criteria>
    - `go test ./api/v1alpha3/... -run 'TestLoop'` exits 0.
    - `TestLoopStatus_NoForbiddenFields` names every LoopStatus field in a compile-time literal (verified by the test compiling and by grep for the field names in the test file).
    - The embeddability test exists and passes, exercising both LoopPolicy and LoopStatus embedded in a synthetic parent struct.
    - `make test-only` exits 0 (no regression in the api/v1alpha3 tier).
  </acceptance_criteria>
  <verify>
    <automated>go test ./api/v1alpha3/... -run 'TestLoop' -count=1</automated>
  </verify>
  <done>Round-trip + LOOP-03 compile-time structural guard + synthetic-embedder embeddability proof all present and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler → etcd (`.status` write) | LoopStatus is written to CRD `.status`; an unbounded/history-accumulating shape would turn etcd into an event DB (LOOP-03 / CLAUDE.md CRD-status-only constraint). No untrusted-input boundary this plan — pure schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-01 | Tampering | `LoopStatus` shape drift (a later PR adds a history slice → etcd bloat / event-DB anti-pattern) | mitigate | `TestLoopStatus_NoForbiddenFields` compile-time struct-literal guard — a history field fails to compile, not merely fails a size assertion (RESEARCH Pitfall 3) |
| T-49-01-02 | Information Disclosure | `EvaluationSummary` accidentally embedding the full findings array (leaks evidence into `.status`) | mitigate | `LastEvaluation` is a single bounded struct (decision + counts + completedAt), never `[]Finding`; enforced by construction + the round-trip test asserting only bounded fields present |
| T-49-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: zero new deps); nothing to gate |
</threat_model>

<verification>
- `go build ./api/v1alpha3/...` — types compile.
- `make generate && git diff --exit-code` — deepcopy in sync.
- `make manifests && git diff --exit-code -- config/crd` — zero CRD-YAML diff (standalone types).
- `go test ./api/v1alpha3/... -run 'TestLoop' -count=1` — round-trip + structural guard + embeddability green.
- `make verify-no-aggregates` — LoopStatus trips no aggregate-schedule-field grep (it uses none by construction).
</verification>

<success_criteria>
- LoopPolicy/LoopStatus/EvaluationSummary exist as shared, deepcopy-generated `api/v1alpha3` types embeddable in any domain CRD (Phase success criterion #1).
- Type godoc names all five loop elements (LOOP-02).
- LoopStatus is structurally guaranteed current-iteration-only — no history (LOOP-03 / Phase success criterion #5).
- No generic `Loop` controller introduced (LOOP-01).
- `make manifests` zero-diff (no premature CRD embedding).
</success_criteria>

<output>
Create `.planning/phases/49-common-loop-contract-verdict-envelope-persistence-schema/49-01-SUMMARY.md` when done.
</output>
