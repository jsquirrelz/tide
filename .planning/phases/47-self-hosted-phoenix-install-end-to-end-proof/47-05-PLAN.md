---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 05
type: execute
wave: 4
depends_on: [47-04]
files_modified:
  - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md
  - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-trace-tree.png
  - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-llm-span-redacted.png
  - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-query.png
  - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-deeplink.png
autonomous: false
requirements: [PROOF-01]

must_haves:
  truths:
    - "Four evidence screenshots exist in the phase dir: five-level trace tree, Task-level LLM span with visibly-redacted message arrays, an enrichment-filter query result, and the dashboard deep link landing on the right Phoenix trace"
    - "47-EVIDENCE.md records full trace IDs, the exact query used, and the known-limitations honesty block (single-node clock skew unexercisable; Postgres path documented-not-proven; local-HEAD image provenance)"
    - "Redaction verifiably held: a search over the captured span content for the real key material returns zero hits, and no committed artifact contains secret values"
    - "Context decisions honored — D-13: full evidence set (trace tree, redacted LLM span detail, enrichment query, deep-link exercise) lands in the phase dir with recorded trace IDs; D-14: known-limitations honesty block present and any live-proof defect root-fixed, never worked around"
  artifacts:
    - path: ".planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md"
      provides: "Milestone-close evidence record"
      min_lines: 40
    - path: ".planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-trace-tree.png"
      provides: "Five-level tree screenshot"
  key_links:
    - from: "47-EVIDENCE.md"
      to: "the four PNGs"
      via: "screenshot index"
      pattern: "47-evidence-"
    - from: "47-EVIDENCE.md"
      to: "Phoenix trace store"
      via: "recorded 32-hex trace IDs"
      pattern: "[0-9a-f]{32}"
---

<objective>
Capture PROOF-01's milestone-close evidence from the live proof cluster (D-13): browser-driven screenshots of the five-level trace tree, the redacted Task-level LLM span detail, a Phoenix DSL query over the OBS-03 enrichment, and the OBS-04 dashboard deep link exercised live — then record it all in 47-EVIDENCE.md with D-14's known-limitations honesty, and gate phase close on human review of the evidence against the milestone acceptance bar.

Purpose: PROOF-01 IS the milestone's acceptance bar — "visible and queryable, with evidence captured at milestone close." Research's "Looks Done But Isn't" checklist only surfaces when a real trace tree is inspected; this plan is that inspection.
Output: 47-EVIDENCE.md + four PNGs, human-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-CONTEXT.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md

What "redacted message arrays" must visibly show (Phase 44 D-08/D-09, the evidence's load-bearing detail): head+tail elision markers on long message arrays and redact-before-truncate secret masking — the span detail should show message content present but with visible redaction/elision markers, NOT raw secrets and NOT empty content.

Defect rule (D-14, binding): a broken waterfall, missing level, orphaned span fragment, or unredacted content found during capture is a named defect root-fixed in-phase (report it as a gap in the SUMMARY for gap-closure planning) — never worked around to get the screenshot.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser-driven evidence capture (tree, redacted span, query, deep link) + redaction spot-check</name>
  <files>.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-trace-tree.png, .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-llm-span-redacted.png, .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-query.png, .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-deeplink.png</files>
  <read_first>
    - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md (trace IDs, Phoenix service name, API-key Secret name, run identity)
    - docs/observability.md (§Tracing attribute/enrichment vocabulary: session.id, metadata, tags — what the query filters on)
  </read_first>
  <action>
    Port-forward Phoenix (6006) and the TIDE dashboard (8080) from the still-running tide-phoenix-proof cluster. Capture via browser automation, the Phase 22/26 self-capture precedent (if no browser tool is available in this execution environment, stop and surface that at the Task 3 checkpoint with exact manual capture steps instead of faking it): (a) 47-evidence-trace-tree.png — the run's trace opened in Phoenix, tree fully expanded, all five AGENT levels visible (Project→Milestone→Phase→Plan→Task) with the LLM message spans beneath the Task level; the trace ID visible in-frame. (b) 47-evidence-llm-span-redacted.png — a Task-level LLM span's detail pane showing the message array with visible redaction/elision markers (zoom so the markers are legible). (c) 47-evidence-query.png — a Phoenix DSL filter over the OBS-03 enrichment (e.g. a tags/metadata filter selecting every span of one phase of the run — use the actual attribute names observed in the spans) with the filtered result list visible, query string in-frame. (d) 47-evidence-deeplink.png — the TIDE dashboard's PhoenixTraceLink on a node/task detail, and the resulting Phoenix page it lands on (a /redirects/... URL resolving to the right trace) — one composite or the landing page with the URL bar visible. REDACTION SPOT-CHECK before saving anything: search span content in Phoenix (UI search or authenticated API) for the first 8 characters of the real API key — must return zero hits; visually confirm no screenshot frame contains a secret value (the Authorization header value must never be on screen). Sampler sanity (Pitfall 6, now inert at the 1.0 default): confirm the tree is complete — no missing-by-sampling levels.
  </action>
  <verify>
    <automated>test "$(ls .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-*.png | wc -l | tr -d ' ')" = "4" && file .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-evidence-*.png | grep -c "PNG image data" | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 4 PNG files exist at the listed paths and `file` identifies each as PNG image data
    - Key-prefix search over span content returned 0 hits (result recorded for Task 2)
    - Each screenshot's content matches its letter: five visible levels (a), legible redaction markers (b), query string + filtered results (c), deep-link landing with URL visible (d)
    - Any defect observed (missing level, broken waterfall, unredacted content, dead deep link) is recorded verbatim — captured evidence shows reality, not a curated angle
  </acceptance_criteria>
  <done>Four honest screenshots on disk; redaction verified held; defects (if any) named.</done>
</task>

<task type="auto">
  <name>Task 2: Author 47-EVIDENCE.md — trace IDs, query, observations, known-limitations honesty</name>
  <files>.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PROOF-RUNLOG.md (all recorded facts: pin, commands, cost, trace IDs, divergences)
    - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-CONTEXT.md (D-13/D-14 — the evidence contract this file discharges)
  </read_first>
  <action>
    Write 47-EVIDENCE.md as the milestone-close evidence record, stating what was OBSERVED (never what "should" work): (1) Environment — date, cluster tide-phoenix-proof (kind version, single node), Phoenix chart pin + appVersion actually installed, TIDE at local HEAD (commit hash), storage path exercised (SQLite-on-PVC 2Gi, retention 7d), auth ON with Secret-sourced credentials (Secret names only). (2) Run — project medium-project / namespace tide-sample-medium, observed cost vs the $5 cap, wall time, reporter-Job tally. (3) Trace evidence — full 32-hex trace ID(s), the exact Phoenix DSL query string used and what it returned, the four-screenshot index (filename → what it shows), the deep-link URL shape exercised. (4) Redaction verification — the key-prefix search result (0 hits) and the visible elision/redaction markers observed in the LLM span. (5) Known limitations (D-14, verbatim honesty): single-node kind cannot surface cross-pod clock skew (Pitfall 5) — documented, NOT claimed verified; the bundled-Postgres path is documented as the chart's tested default but was not live-proven on this VM (D-07); chart/images were local HEAD builds, not the published OCI artifacts (the published chart gains this phase's wiring at the next release). (6) Defects found — named with root-fix disposition, or an explicit "none observed". Cross-check every claim against the runlog before writing it.
  </action>
  <verify>
    <automated>grep -qE '[0-9a-f]{32}' .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md && grep -q 'Known limitations' .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md && test "$(grep -c 'sk-ant' .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md)" = "0" && test "$(grep -c '47-evidence-' .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md)" -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - 47-EVIDENCE.md contains ≥1 32-hex trace ID, a 'Known limitations' section naming clock-skew (unexercised), Postgres path (not live-proven), and local-HEAD provenance
    - All four screenshot filenames are indexed with one-line descriptions
    - The exact query string is quoted; the redaction search result is stated
    - Zero secret material: `grep -c 'sk-ant'` returns 0; credentials referenced by Secret name only
    - A 'Defects found' section exists (either named defects with disposition, or 'none observed')
  </acceptance_criteria>
  <done>Evidence record complete, honest, secret-free, cross-checked against the runlog.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review — evidence vs the PROOF-01 milestone acceptance bar</name>
  <files>none (review-only)</files>
  <read_first>
    - .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md (the artifact under review)
  </read_first>
  <action>
    Present the committed evidence set to the user for review against the PROOF-01 bar and wait for the resume signal. Do not mark the phase or PROOF-01 complete without approval.
  </action>
  <what-built>The Phase 47 live proof is captured: a real medium-project run on a fresh kind cluster (stood up from the docs' own recipe, auth-ON Phoenix, SQLite-on-PVC) produced a five-level trace tree in self-hosted Phoenix; four screenshots + 47-EVIDENCE.md (trace IDs, query, redaction verification, known limitations) are committed in the phase dir.</what-built>
  <how-to-verify>
    1. Open .planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-EVIDENCE.md and the four 47-evidence-*.png files.
    2. Confirm the tree screenshot shows all five levels (Project→Milestone→Phase→Plan→Task) with LLM message spans under Task.
    3. Confirm the LLM span screenshot shows message arrays with visible redaction/elision markers — no raw secrets, no empty content.
    4. Confirm the query screenshot shows a Phoenix filter over the enrichment (tags/metadata) returning the run's spans, and the deep-link screenshot lands on the right trace.
    5. Optionally verify live: `kubectl -n phoenix port-forward svc/<phoenix-service> 6006:6006`, open http://localhost:6006, log in, and look up a recorded trace ID.
    6. Check the Known limitations section states what was NOT proven (clock skew, Postgres path, local-HEAD images).
  </how-to-verify>
  <verify>
    <automated>N/A — human gate; automated evidence gates ran in Tasks 1-2</automated>
  </verify>
  <acceptance_criteria>
    - User has responded with "approved" (or the described shortfalls have been converted into named gaps for in-phase root-fix per D-14)
    - The user's cluster keep/delete decision is recorded for the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "approved" to accept PROOF-01's evidence as the milestone acceptance bar, or describe what falls short (a real defect here becomes a named in-phase root-fix per D-14, planned via gap closure). After approval, say whether to keep or delete the tide-phoenix-proof cluster.</resume-signal>
  <done>Explicit "approved" received, or shortfalls converted to named gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix span store → committed evidence | screenshots/trace IDs enter the git repo permanently |
| operator browser → auth-ON Phoenix | authenticated UI session during capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05 | Information Disclosure | secrets in committed screenshots/evidence markdown | mitigate | pre-save key-prefix search over span content (0 hits required); visual frame check; `sk-ant` grep gate on 47-EVIDENCE.md; credentials by Secret name only |
| T-47-07 | Repudiation | evidence claims without observable backing | mitigate | every claim traces to a trace ID, query string, or screenshot; runlog cross-check before authoring; human checkpoint reviews the artifacts themselves |
| T-47-SC | Tampering | package installs | accept | none in this plan |
</threat_model>

<verification>
- Task 1/2 automated gates green (4 PNGs, evidence greps, zero secret material)
- Human checkpoint approved against the PROOF-01 bar
</verification>

<success_criteria>
- PROOF-01 discharged: five-level tree with redacted message arrays visible AND queryable in self-hosted Phoenix, evidence committed, honestly scoped, human-approved
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-05-SUMMARY.md` when done (note the user's cluster keep/delete decision from the checkpoint).
</output>
