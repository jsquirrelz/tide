---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/reporter_jobspec.go
  - internal/controller/reporter_jobspec_test.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/task_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - cmd/manager/main.go
autonomous: true
requirements: [PHX-02]

must_haves:
  truths:
    - "BuildReporterJob emits OTEL_EXPORTER_OTLP_HEADERS on the reporter Job env when both OTLPEndpoint and OTLPHeaders are non-empty"
    - "OTLPHeaders empty leaves reporter Job env byte-identical to today (no new entries, zero-entry case preserved)"
    - "All five reconciler levels forward OTLPHeaders from Deps into ReporterOptions"
  artifacts:
    - path: "internal/controller/reporter_jobspec.go"
      provides: "ReporterOptions.OTLPHeaders field + guarded env construction"
      contains: "OTLPHeaders string"
    - path: "internal/controller/reporter_jobspec_test.go"
      provides: "Paired presence/absence unit tests"
      contains: "TestBuildReporterJob_OTLPHeadersEnv"
    - path: "cmd/manager/main.go"
      provides: "Env read + Deps threading"
      contains: "OTEL_EXPORTER_OTLP_HEADERS"
  key_links:
    - from: "cmd/manager/main.go"
      to: "internal/controller/dispatch_helpers.go"
      via: "PlannerReconcilerDeps.OTLPHeaders assignment"
      pattern: "OTLPHeaders:\\s*otlpHeaders"
    - from: "internal/controller/{milestone,phase,plan,project,task}_controller.go"
      to: "internal/controller/reporter_jobspec.go"
      via: "ReporterOptions literal at all 5 spawn sites"
      pattern: "OTLPHeaders:\\s*r\\.Deps\\.OTLPHeaders"
---

<objective>
Close the Go half of RESEARCH.md Pitfall B (orchestrator-resolved: build the OTLP-headers wiring in-phase, option 1): thread a new `OTLPHeaders` value from the manager's `OTEL_EXPORTER_OTLP_HEADERS` env through both Deps structs and all five reconciler spawn sites into the reporter Job spec, exactly mirroring the existing `OTLPEndpoint` threading. This makes D-08's auth-ON Phoenix recipe functional end-to-end: the reporter Job's own `otlptracegrpc` TracerProvider honors `OTEL_EXPORTER_OTLP_HEADERS` automatically (verified against pinned v1.43.0 doc.go), so only Job-spec plumbing is needed — no reporter-binary change.

Purpose: without this, TIDE pods can never authenticate to an auth-on Phoenix — the documented recipe (Plan 47-03) would be aspirational, violating D-14's root-fix rule.
Output: compiled, unit-tested Go threading — one repeated mechanical edit applied at 9 sites (per 47-PATTERNS.md, which traces every site to an exact line).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PATTERNS.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-RESEARCH.md

<interfaces>
Existing shape being mirrored (from 47-PATTERNS.md, verified against source):

From internal/controller/reporter_jobspec.go:272-288 (env construction — extend, do not restructure):
```go
var env []corev1.EnvVar
if opts.OTLPEndpoint != "" {
    env = []corev1.EnvVar{
        {Name: "OTEL_EXPORTER_OTLP_ENDPOINT", Value: opts.OTLPEndpoint},
        {Name: "OTEL_BSP_MAX_EXPORT_BATCH_SIZE", Value: "6"},
    }
}
```

The five ReporterOptions spawn sites (add `OTLPHeaders: r.Deps.OTLPHeaders,` immediately after the `OTLPEndpoint:` line at each — ALL FIVE, not just the first):
- internal/controller/milestone_controller.go:656
- internal/controller/phase_controller.go:609
- internal/controller/plan_controller.go:664
- internal/controller/project_controller.go:1924
- internal/controller/task_controller.go:1106

Deps struct field sites (add `OTLPHeaders string` beside the existing `OTLPEndpoint string`):
- internal/controller/dispatch_helpers.go:223 (PlannerReconcilerDeps, struct starts :190)
- internal/controller/task_controller.go:130 (TaskReconcilerDeps, struct starts :93)

main.go threading sites:
- cmd/manager/main.go:285 (`otlpEndpoint := os.Getenv("OTEL_EXPORTER_OTLP_ENDPOINT")` — add sibling read)
- cmd/manager/main.go:438 (plannerDeps literal), :549 (TaskReconcilerDeps literal)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReporterOptions.OTLPHeaders field + guarded env construction + unit test pair</name>
  <files>internal/controller/reporter_jobspec.go, internal/controller/reporter_jobspec_test.go</files>
  <read_first>
    - internal/controller/reporter_jobspec.go (ReporterOptions struct ~line 89-97 for the OTLPEndpoint doc-comment voice; env construction ~line 272-288)
    - internal/controller/reporter_jobspec_test.go (lines 789-861: TestBuildReporterJob_OTLPEndpointEnv / TestBuildReporterJob_NoOTLPEndpointNoEnv — the exact map-comparison idiom to replicate)
  </read_first>
  <behavior>
    - Test 1 (TestBuildReporterJob_OTLPHeadersEnv): opts with ReporterImage, OTLPEndpoint "collector:4317", OTLPHeaders "Authorization=Bearer test-token" → container env has exactly 3 entries: OTEL_EXPORTER_OTLP_ENDPOINT=collector:4317, OTEL_BSP_MAX_EXPORT_BATCH_SIZE=6, OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer test-token
    - Test 2 (TestBuildReporterJob_OTLPHeadersWithoutEndpointNoEnv): opts with OTLPHeaders set but OTLPEndpoint empty → len(env) == 0 (headers without an endpoint are meaningless; the outer endpoint guard is preserved — this pins the planner-decided semantics from 47-PATTERNS.md's open question)
  </behavior>
  <action>
    Write both failing tests first (RED), then implement (GREEN). Add field `OTLPHeaders string` to ReporterOptions directly after `OTLPEndpoint string`, with a doc comment in the established voice: forwarded so the reporter Job's own TracerProvider authenticates to the SAME auth-enabled collector the manager uses (Phase 47 PHX-02/D-08); carried as Env because otlptracegrpc v1.43.0 honors OTEL_EXPORTER_OTLP_HEADERS automatically when WithHeaders is not called; only emitted when OTLPEndpoint is also non-empty. NOTE in the comment that the value is a literal on the Job spec in the project namespace — accepted per threat T-47-02 below (the same namespace already holds the strictly-more-sensitive ANTHROPIC_API_KEY in tide-secrets). In the env construction, inside the existing `if opts.OTLPEndpoint != ""` block, append `corev1.EnvVar{Name: "OTEL_EXPORTER_OTLP_HEADERS", Value: opts.OTLPHeaders}` only when `opts.OTLPHeaders != ""`. Do not touch the TraceParent/Args path or any other env logic.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'TestBuildReporterJob_OTLPHeaders' -v</automated>
  </verify>
  <acceptance_criteria>
    - reporter_jobspec.go contains `OTLPHeaders string` in ReporterOptions
    - `go test ./internal/controller/... -run 'TestBuildReporterJob' -v` exits 0 (new pair AND the existing OTLPEndpoint pair — proves the zero-entry posture is untouched)
    - Test 1 asserts exactly 3 env entries; Test 2 asserts exactly 0
    - Two commits: `test(47-01): add failing OTLPHeaders reporter-env tests` then `feat(47-01): emit OTEL_EXPORTER_OTLP_HEADERS on reporter Jobs`
  </acceptance_criteria>
  <done>New test pair green, existing reporter_jobspec tests green, RED commit precedes GREEN commit.</done>
</task>

<task type="auto">
  <name>Task 2: Thread OTLPHeaders through main.go, both Deps structs, and all five reconciler spawn sites</name>
  <files>cmd/manager/main.go, internal/controller/dispatch_helpers.go, internal/controller/task_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go</files>
  <read_first>
    - cmd/manager/main.go (lines 280-295 env-read block with its Phase 44 comment; lines 429-440 plannerDeps literal; lines 545-550 TaskReconcilerDeps literal)
    - internal/controller/dispatch_helpers.go (lines 190-230: PlannerReconcilerDeps, OTLPEndpoint field + comment at :223)
    - internal/controller/task_controller.go (lines 93-135: TaskReconcilerDeps; line ~1106 spawn site)
    - internal/controller/milestone_controller.go (lines 650-665: the representative spawn-site shape)
  </read_first>
  <action>
    One mechanical edit repeated at every site — apply to ALL listed sites, not just the first of each kind (project convention: state instruction scope explicitly). (1) dispatch_helpers.go: add `OTLPHeaders string` immediately after `OTLPEndpoint string` (:223) with a one-line comment mirroring the OTLPEndpoint comment (Phase 47 PHX-02/D-08 auth-header forward). (2) task_controller.go: same field addition after :130. (3) main.go: add `otlpHeaders := os.Getenv("OTEL_EXPORTER_OTLP_HEADERS")` directly after the `otlpEndpoint` read at :285 (extend the existing Phase 44 comment block to note the headers sibling, Phase 47 D-08); add `OTLPHeaders: otlpHeaders,` after the `OTLPEndpoint:` line in the plannerDeps literal (:438) and in the TaskReconcilerDeps literal (:549). (4) Add `OTLPHeaders: r.Deps.OTLPHeaders,` immediately after the `OTLPEndpoint:` line in the ReporterOptions literal at ALL FIVE spawn sites: milestone_controller.go:656, phase_controller.go:609, plan_controller.go:664, project_controller.go:1924, task_controller.go:1106. No other logic changes anywhere.
  </action>
  <verify>
    <automated>go build ./... && go vet ./cmd/... ./internal/controller/... && test "$(grep -l 'OTLPHeaders:[[:space:]]*r\.Deps\.OTLPHeaders' internal/controller/*_controller.go | wc -l | tr -d ' ')" = "5"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - grep `OTLPHeaders:` in cmd/manager/main.go matches exactly 2 lines; grep `os.Getenv("OTEL_EXPORTER_OTLP_HEADERS")` matches exactly 1
    - grep `OTLPHeaders: r.Deps.OTLPHeaders` across internal/controller/*_controller.go matches in exactly 5 files (milestone, phase, plan, project, task)
    - `go test ./internal/controller/... -run 'TestBuildReporterJob' -v` still exits 0
  </acceptance_criteria>
  <done>All 9 threading sites landed, build/vet/tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager pod env → reporter Job spec | secret-derived header value crosses from tide-system into project namespaces |
| TIDE pods → Phoenix OTLP 4317 | bearer-authenticated export channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information Disclosure | manager/dashboard Deployment manifests | mitigate | header value never a literal in chart templates — `valueFrom: secretKeyRef` only (enforced in Plan 47-02's render gate; this plan only handles the in-process string) |
| T-47-02 | Information Disclosure | reporter Job spec env (literal forward in project ns) | accept | Job-read RBAC in a project namespace already implies access to `tide-secrets` (ANTHROPIC_API_KEY) — a strictly more valuable credential; the Phoenix token is ingest-scoped. Documented honestly in observability.md (Plan 47-03) |
| T-47-SC | Tampering | package installs | accept | no npm/pip/cargo/go-module installs in this plan (RESEARCH.md Package Legitimacy Audit: zero packages in scope) |
</threat_model>

<verification>
- `go test ./internal/controller/... -run 'TestBuildReporterJob' -v` green (new pair + pre-existing OTLPEndpoint pair)
- `go build ./... && go vet ./...` clean
- Grep-count gates from both tasks hold (5 controller sites, 2 main.go assignments)
</verification>

<success_criteria>
- OTLPHeaders threads env→Deps→5 reconcilers→ReporterOptions→reporter Job env, mirroring OTLPEndpoint at every site
- Empty-headers and empty-endpoint cases produce byte-identical Job specs to today (proven by tests)
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-01-SUMMARY.md` when done.
</output>
