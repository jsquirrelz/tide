---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hack/helm/tide-values.yaml
  - charts/tide/values.yaml
  - hack/helm/augment-tide-chart.sh
  - charts/tide/templates/deployment.yaml
  - charts/tide/templates/dashboard-deployment.yaml
  - charts/tide/templates/NOTES.txt
  - hack/helm/assert-telemetry-render.sh
  - hack/helm/assert-otlp-headers-env.py
  - Makefile
autonomous: true
requirements: [PHX-02]

must_haves:
  truths:
    - "helm template with otel.exporter.headersSecretRef.name set renders OTEL_EXPORTER_OTLP_HEADERS via secretKeyRef on BOTH manager and dashboard containers; unset renders no such env anywhere"
    - "Rendered NOTES.txt warns toward the Phoenix doc step when otel.exporter.endpoint is empty and is silent about tracing when it is set"
    - "Running hack/helm/augment-tide-chart.sh reproduces every chart edit (idempotent — the nudge and env block survive helmify regeneration)"
  artifacts:
    - path: "hack/helm/tide-values.yaml"
      provides: "Source-of-truth values key"
      contains: "headersSecretRef"
    - path: "charts/tide/templates/deployment.yaml"
      provides: "Marker-keyed guarded env block"
      contains: "phase47-otlp-headers-env-injected"
    - path: "charts/tide/templates/NOTES.txt"
      provides: "Tracing-dark nudge"
      contains: "tracing is dark"
    - path: "hack/helm/assert-otlp-headers-env.py"
      provides: "Offline secretKeyRef render gate (manager + dashboard)"
      contains: "OTEL_EXPORTER_OTLP_HEADERS"
  key_links:
    - from: "charts/tide/values.yaml"
      to: "charts/tide/templates/deployment.yaml"
      via: "guarded env template"
      pattern: "otel\\.exporter\\.headersSecretRef\\.name"
    - from: "hack/helm/augment-tide-chart.sh"
      to: "charts/tide/templates/NOTES.txt"
      via: "step 4b heredoc overwrite"
      pattern: "tracing is dark"
    - from: "Makefile"
      to: "hack/helm/assert-otlp-headers-env.py"
      via: "helm-telemetry-assert target"
      pattern: "assert-otlp-headers-env"
---

<objective>
Close the chart half of Pitfall B plus the D-10 NOTES.txt nudge: a Secret-ref values key (`otel.exporter.headersSecretRef`) rendering `OTEL_EXPORTER_OTLP_HEADERS` via `valueFrom: secretKeyRef` on manager and dashboard; the tracing-dark NOTES.txt warning mirroring the `prometheus.enabled` block; and offline render gates for both. Every edit lands in its augment-script source of truth so `make helm-controller` regeneration cannot revert it (the Phase 02.2 chart-is-FIXED-contract discipline).

Orchestrator-binding constraints honored here: the NOTES.txt render gate EXTENDS the existing Permutation G `tpl`-probe mechanism in `hack/helm/assert-telemetry-render.sh` (PATTERNS.md's correction supersedes RESEARCH.md Pitfall A's raw-byte / live-cluster fallbacks — do NOT build those); NOTES.txt edits land in BOTH the augment heredoc and the tracked template.

CLAUDE.md `charts/tide/values.yaml` caveat, addressed explicitly (plan-checker warning #1): the "chart is FIXED contract — confirm first" rule targets ad-hoc hand edits to the tracked chart. This plan makes zero hand edits to tracked chart files owned by the augment pipeline — `hack/helm/tide-values.yaml` + `augment-tide-chart.sh` are the sanctioned chart-authoring path, and Phases 42–46 of this milestone (sampler flip, `phoenix.baseURL`, OTLP env) each modified `values.yaml` autonomously through exactly this pipeline inside an approved GSD plan. Same established exception applies; no extra checkpoint needed.

Purpose: PHX-02's letter — NOTES.txt nudges when tracing is dark — and the chart surface D-08's auth-ON recipe documents in Plan 47-03.
Output: chart wiring + nudge + `make helm-telemetry-assert` gates, all offline (no cluster).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-PATTERNS.md

<interfaces>
Exact NOTES.txt nudge block (locked wording — insert byte-identically in BOTH sites, directly after the existing prometheus warning block's `{{- end }}`):

```
{{- if not .Values.otel.exporter.endpoint }}

WARNING: tracing is dark — otel.exporter.endpoint is empty.
Run trace trees (five levels, plus redacted LLM message spans) are not
exported anywhere, and the dashboard's Phoenix deep links stay hidden.
Enable: see the "Enable tracing" step in docs/INSTALL.md.
{{- end }}
```

Exact values key (add under the existing `otel.exporter:` map in hack/helm/tide-values.yaml ~line 416, comment in the block's established voice noting Phase 47 PHX-02/D-08, name empty → no env emitted, never a literal value):

```yaml
otel:
  exporter:
    endpoint: ""
    headersSecretRef:
      name: ""
      key: "OTEL_EXPORTER_OTLP_HEADERS"
```

Exact guarded env block for the manager deployment (new marker-keyed augment-script injection, anchored `count=1` immediately after the `        # phase4-env-injected\n` line — same anchor idiom as the phase9 section; marker comment is the last line, 8-space indent):

```yaml
        {{- if .Values.otel.exporter.headersSecretRef.name }}
        # Phase 47 PHX-02/D-08: OTLP auth header for auth-enabled collectors
        # (self-hosted Phoenix). Secret-sourced only — never a literal value.
        - name: OTEL_EXPORTER_OTLP_HEADERS
          valueFrom:
            secretKeyRef:
              name: {{ .Values.otel.exporter.headersSecretRef.name }}
              key: {{ .Values.otel.exporter.headersSecretRef.key | default "OTEL_EXPORTER_OTLP_HEADERS" }}
        {{- end }}
        # phase47-otlp-headers-env-injected
```

The dashboard container gets the same `{{- if }}`-guarded entry hand-edited into charts/tide/templates/dashboard-deployment.yaml after its OTEL_SERVICE_NAME entry (~line 56), matching that file's existing PromQL-proxy conditional-env idiom at line 57. dashboard-deployment.yaml is NOT augment-script-owned (verified: the script never touches it) — direct edit is the established pattern there (Phase 46 PHOENIX_BASE_URL precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: headersSecretRef values key + guarded env on manager (via augment script) and dashboard (direct)</name>
  <files>hack/helm/tide-values.yaml, charts/tide/values.yaml, hack/helm/augment-tide-chart.sh, charts/tide/templates/deployment.yaml, charts/tide/templates/dashboard-deployment.yaml</files>
  <read_first>
    - hack/helm/augment-tide-chart.sh (header lines 1-30 ownership contract; the phase9/phase28/phase36/phase4 marker-keyed python injection sections ~lines 290-434 — the idiom the new section copies; step 2's values.yaml cp)
    - hack/helm/tide-values.yaml (otel block ~line 416 — the edit site; the prometheus block's comment voice)
    - charts/tide/templates/dashboard-deployment.yaml (lines 46-60: OTEL env block + the PromQL-proxy conditional at :57)
    - charts/tide/templates/deployment.yaml (lines 85-100: the phase4 OTel env block + marker the injection anchors on)
  </read_first>
  <action>
    (1) Add the headersSecretRef key from the interfaces block to hack/helm/tide-values.yaml under `otel.exporter:` with an explanatory comment (Phase 47 PHX-02/D-08; empty name → no env rendered; consumed via valueFrom secretKeyRef, forwarded by the manager onto reporter Jobs; add a one-line Phoenix pointer to the otel block's comment header referencing docs/observability.md's Self-hosted Phoenix section). (2) Add a new marker-keyed injection section to augment-tide-chart.sh (place it after the existing 8f/phase4 section, titled for Phase 47), marker `# phase47-otlp-headers-env-injected`, injecting the guarded env block from the interfaces section after the `# phase4-env-injected` line — copy the `if ENV..._MARKER not in content: re.sub(...)` idiom of the phase9 section exactly, count=1. (3) Hand-edit charts/tide/templates/dashboard-deployment.yaml: add the same guarded entry (same secretKeyRef shape, without the phase47 marker comment) after the OTEL_SERVICE_NAME env entry. (4) Run `bash hack/helm/augment-tide-chart.sh` to propagate values.yaml (via the step-2 cp) and deployment.yaml (via the new injection) into the tracked chart. Run it a second time and confirm no further changes (idempotency). Do NOT hand-edit charts/tide/values.yaml or charts/tide/templates/deployment.yaml directly — they must come out of the script run.
  </action>
  <verify>
    <automated>bash hack/helm/augment-tide-chart.sh && diff hack/helm/tide-values.yaml charts/tide/values.yaml && test "$(helm template charts/tide --set dashboard.enabled=true --set otel.exporter.headersSecretRef.name=tide-otlp-headers | grep -c 'name: OTEL_EXPORTER_OTLP_HEADERS')" = "2" && test "$(helm template charts/tide --set dashboard.enabled=true | grep -c OTEL_EXPORTER_OTLP_HEADERS)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `diff hack/helm/tide-values.yaml charts/tide/values.yaml` exits 0 (source and tracked copy identical)
    - Default render (`helm template charts/tide --set dashboard.enabled=true`) contains zero OTEL_EXPORTER_OTLP_HEADERS occurrences
    - Render with `--set otel.exporter.headersSecretRef.name=tide-otlp-headers` contains exactly 2 `name: OTEL_EXPORTER_OTLP_HEADERS` entries (manager + dashboard), each with `secretKeyRef` name `tide-otlp-headers` and key `OTEL_EXPORTER_OTLP_HEADERS`, and NO literal `value:` on either entry
    - `grep -c 'phase47-otlp-headers-env-injected' charts/tide/templates/deployment.yaml` returns 1 and the same grep on hack/helm/augment-tide-chart.sh returns ≥ 2 (marker constant + block)
    - Second `bash hack/helm/augment-tide-chart.sh` run produces no additional git diff (idempotent)
  </acceptance_criteria>
  <done>Values key + both container env blocks render correctly both ways, all sourced through the augment pipeline where the pipeline owns the file.</done>
</task>

<task type="auto">
  <name>Task 2: NOTES.txt tracing-dark nudge — augment heredoc AND tracked template</name>
  <files>hack/helm/augment-tide-chart.sh, charts/tide/templates/NOTES.txt</files>
  <read_first>
    - hack/helm/augment-tide-chart.sh (step 4b heredoc, lines ~103-124 — the authoritative NOTES.txt source; the prometheus warning block inside it)
    - charts/tide/templates/NOTES.txt (whole file, 15 lines — header comment names the ownership rule)
  </read_first>
  <action>
    Insert the locked nudge block from the interfaces section into the step-4b heredoc, directly after the existing prometheus warning's `{{- end }}` (before the heredoc's closing EOF). Wording is locked — use it verbatim: condition `{{- if not .Values.otel.exporter.endpoint }}` (empty string is falsy, mirroring how `prometheus.enabled: false` gates the sibling block), pointer text `see the "Enable tracing" step in docs/INSTALL.md` (Plan 47-03 creates that INSTALL.md section under the heading "Enable tracing (Phoenix)" — the short-form pointer mirrors the prometheus block's "Enable telemetry" short form). Then run `bash hack/helm/augment-tide-chart.sh` so the heredoc overwrites the tracked charts/tide/templates/NOTES.txt — commit both files. Do not edit the tracked NOTES.txt by hand (per D-10 and the file's own header: template-only edits revert).
  </action>
  <verify>
    <automated>bash hack/helm/augment-tide-chart.sh && grep -c "tracing is dark" charts/tide/templates/NOTES.txt && grep -c "tracing is dark" hack/helm/augment-tide-chart.sh && grep -qc "run telemetry beyond the budget tally" charts/tide/templates/NOTES.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tracing is dark' charts/tide/templates/NOTES.txt` returns 1 and the same grep on hack/helm/augment-tide-chart.sh returns 1 (both sites)
    - The existing prometheus warning block is unchanged (grep 'run telemetry beyond the budget tally' still present in NOTES.txt)
    - `git diff` after a second augment-script run shows no drift between heredoc and tracked file
  </acceptance_criteria>
  <done>Nudge present in both sites, prometheus block untouched, regeneration-stable.</done>
</task>

<task type="auto">
  <name>Task 3: Offline render gates — NOTES.txt permutation (extends Permutation G) + assert-otlp-headers-env.py + Makefile wiring</name>
  <files>hack/helm/assert-telemetry-render.sh, hack/helm/assert-otlp-headers-env.py, Makefile</files>
  <read_first>
    - hack/helm/assert-telemetry-render.sh (Permutation G, lines ~214-271 — the tpl+ConfigMap NOTES probe this task extends; the die/section-header conventions; note the probe dir + trap already exist and remain in scope for a new permutation placed after G)
    - hack/helm/assert-phoenix-env.py (full file, 127 lines — the model for the new python gate: argparse modes, Deployment walk, PASS/FAIL prints)
    - Makefile (helm-telemetry-assert target, lines ~710-718 — where the new render + assert lines go)
  </read_first>
  <action>
    (1) In assert-telemetry-render.sh, add a new permutation immediately after Permutation G (use the next unused letter; follow G's section-comment style). Reuse G's existing `${NOTES_PROBE_DIR}/chart` probe (already copied, still in scope — do not re-copy): render `helm template tide-notes "${NOTES_PROBE_DIR}/chart" --show-only templates/notes-probe.yaml` with default values and `die` unless output contains the nudge (grep -qF "tracing is dark"); render again with `--set otel.exporter.endpoint=tide-phoenix.phoenix.svc.cluster.local:4317` and `die` if the nudge text appears. This is the D-10 both-ways gate via the Phase 38 mechanism — do NOT add a raw-byte assertion or any live-cluster/`--dry-run` path (orchestrator-binding decision; RESEARCH.md Pitfall A's fallbacks are superseded). (2) Create hack/helm/assert-otlp-headers-env.py modeled on assert-phoenix-env.py with two divergences: it checks BOTH containers named `manager` and `dashboard` (fail if either is missing the expectation), and its present-mode flag is `--expect-secretref NAME KEY` asserting the env entry uses `valueFrom.secretKeyRef` with that name/key and has NO literal `value` field (never assert on a secret value — ASVS V6); absent mode stays `--expect-absent`. Module docstring names Phase 47 PHX-02/D-08 and the sibling lineage. (3) In Makefile's helm-telemetry-assert target, before the final assert-telemetry-render.sh line, add: assert absent on the existing default render `/tmp/tide-helm-render.yaml`; a new render with `--set dashboard.enabled=true --set otel.exporter.headersSecretRef.name=tide-otlp-headers` to `/tmp/tide-helm-render-otlpheaders.yaml`; assert `--expect-secretref tide-otlp-headers OTEL_EXPORTER_OTLP_HEADERS` on it (tab-indented recipe lines, `@` prefix, matching the existing phoenix-env lines).
  </action>
  <verify>
    <automated>make helm-telemetry-assert</automated>
  </verify>
  <acceptance_criteria>
    - `make helm-telemetry-assert` exits 0 with the new permutation's PASS output visible and both assert-otlp-headers-env.py invocations printing PASS
    - Sabotage probe (non-vacuity): `bash hack/helm/assert-telemetry-render.sh` run with a temporarily-set default endpoint in a scratch copy fails the new permutation — OR equivalently, confirm the present-case grep targets rendered probe output, not the raw template file (inspect: the new permutation greps `helm template` stdout)
    - hack/helm/assert-otlp-headers-env.py exits non-zero when given the default render with `--expect-secretref tide-otlp-headers OTEL_EXPORTER_OTLP_HEADERS` (checked once by hand during the task)
    - Makefile diff touches only the helm-telemetry-assert recipe
  </acceptance_criteria>
  <done>`make helm-assert` green end-to-end; both new gates provably non-vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart values → pod env | secret material must cross only as a Secret reference, never a rendered literal |
| helm install output → operator terminal | NOTES.txt guidance; must not echo secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information Disclosure | OTEL_EXPORTER_OTLP_HEADERS in Deployment manifests | mitigate | `valueFrom: secretKeyRef` only, `{{- if }}`-guarded; assert-otlp-headers-env.py fails any render carrying a literal `value:` on that env entry |
| T-47-03 | Spoofing | headersSecretRef default key fallback | accept | `default "OTEL_EXPORTER_OTLP_HEADERS"` is a key NAME convention, not credential material; operator controls the Secret contents |
| T-47-SC | Tampering | package installs | accept | no package-manager installs in this plan (python gate uses stdlib + PyYAML already required by augment-tide-chart.sh) |
</threat_model>

<verification>
- `make helm-telemetry-assert` and `make helm-assert` exit 0
- `helm template` both-ways checks from Task 1 hold
- `bash hack/helm/augment-tide-chart.sh` twice → zero drift (regeneration-safe)
</verification>

<success_criteria>
- headersSecretRef renders secretKeyRef env on manager + dashboard only when set; NOTES.txt nudges only when tracing is dark
- Every chart edit reproducible from hack/helm sources; offline gates block regressions both ways
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-02-SUMMARY.md` when done.
</output>
