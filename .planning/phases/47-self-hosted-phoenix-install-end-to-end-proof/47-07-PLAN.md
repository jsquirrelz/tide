---
phase: 47-self-hosted-phoenix-install-end-to-end-proof
plan: 07
type: execute
wave: 6
depends_on: [47-06]
files_modified:
  - api/v1alpha3/milestone_types.go
  - api/v1alpha3/phase_types.go
  - api/v1alpha3/plan_types.go
  - api/v1alpha3/project_types.go
  - api/v1alpha3/task_types.go
  - api/v1alpha3/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_milestones.yaml
  - config/crd/bases/tideproject.k8s_phases.yaml
  - config/crd/bases/tideproject.k8s_plans.yaml
  - config/crd/bases/tideproject.k8s_projects.yaml
  - config/crd/bases/tideproject.k8s_tasks.yaml
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/controller/task_controller.go
autonomous: true
gap_closure: true
requirements: [PROOF-01]

must_haves:
  truths:
    - "Each level's .status carries a durable reporter-spawned marker keyed on the completed planner/dispatch Job UID, surviving reporter-Job TTL-GC and manager restart"
    - "All five reporter spawn sites skip Create when the marker matches the current attempt's key — the 300s TTL-GC window can no longer re-open the spawn gate"
    - "The marker is stamped only after the reporter Job verifiably exists for the attempt (created, AlreadyExists, or found), mirroring the *RolledUpUID stamp ordering"
  artifacts:
    - path: "api/v1alpha3/milestone_types.go"
      provides: "MilestoneStatus.MilestoneReporterSpawnedUID"
      contains: "MilestoneReporterSpawnedUID"
    - path: "api/v1alpha3/phase_types.go"
      provides: "PhaseStatus.PhaseReporterSpawnedUID"
      contains: "PhaseReporterSpawnedUID"
    - path: "api/v1alpha3/plan_types.go"
      provides: "PlanStatus.PlanReporterSpawnedUID"
      contains: "PlanReporterSpawnedUID"
    - path: "api/v1alpha3/project_types.go"
      provides: "ProjectStatus.ProjectReporterSpawnedUID"
      contains: "ProjectReporterSpawnedUID"
    - path: "api/v1alpha3/task_types.go"
      provides: "TaskStatus.TaskTraceReporterSpawnedUID"
      contains: "TaskTraceReporterSpawnedUID"
  key_links:
    - from: "internal/controller/milestone_controller.go"
      to: "api/v1alpha3/milestone_types.go"
      via: "marker gate before spawnReporterIfNeeded + RetryOnConflict stamp after"
      pattern: "MilestoneReporterSpawnedUID"
    - from: "internal/controller/task_controller.go"
      to: "api/v1alpha3/task_types.go"
      via: "gate at spawnTaskTraceReporterIfNeeded entry keyed on completedJob.UID"
      pattern: "TaskTraceReporterSpawnedUID"
---

<objective>
Close verification gap #2 (CR-01, blocker — Phase-46/OBS-02 regression): the reporter-spawn gate at `internal/controller/dispatch_helpers.go:44-50` (and the inline project/task variants) is name-only (Get→IsNotFound→Create). The reporter Job's `TTLSecondsAfterFinished=300` (reporter_jobspec.go) garbage-collects the Job, after which a sustained-reconcile parent re-enters the gate and re-Creates a second reporter with freshly-recomputed ReporterOptions — the live proof measured only 115/386 LLM spans carrying the full OBS-02/OBS-03 enrichment. The budget-rollup path guards this exact window with durable `*RolledUpUID` markers (25 refs); the spawn path has no equivalent.

Fix: add a durable per-attempt "reporter spawned" marker to each level's `.status`, keyed on the completed planner/dispatch Job UID, and gate Create on that marker at all five spawn sites (milestone/phase/plan/project/task). The envtest proof lands in plan 47-10.

Output: five new CRD status fields + regenerated manifests + marker gate/stamp at all five spawn sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-VERIFICATION.md

The `*RolledUpUID` marker pattern to mirror (naming, doc-comment voice, +optional, stamp ordering, RetryOnConflict + MergeFromWithOptimisticLock): `api/v1alpha3/milestone_types.go:61-72` and the stamp block at `internal/controller/milestone_controller.go:676-700`.

Persistence constraint (CLAUDE.md): CRD `.status` only — a single string field per level is well within budget. This is additive schema (new optional status fields on the existing v1alpha3 storage version) — no CRD version bump, no conversion.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reporter-spawned marker fields to all five v1alpha3 status types and regenerate manifests</name>
  <files>api/v1alpha3/milestone_types.go, api/v1alpha3/phase_types.go, api/v1alpha3/plan_types.go, api/v1alpha3/project_types.go, api/v1alpha3/task_types.go, api/v1alpha3/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_milestones.yaml, config/crd/bases/tideproject.k8s_phases.yaml, config/crd/bases/tideproject.k8s_plans.yaml, config/crd/bases/tideproject.k8s_projects.yaml, config/crd/bases/tideproject.k8s_tasks.yaml</files>
  <read_first>
    - api/v1alpha3/milestone_types.go:55-75 (MilestoneRolledUpUID field: doc-comment voice, `+optional` marker, json tag casing — the exact shape to mirror)
    - api/v1alpha3/phase_types.go:55-70, api/v1alpha3/plan_types.go:110-125, api/v1alpha3/project_types.go:315-330 (sibling markers; note the project rollup marker lives under Budget — the NEW spawn marker goes on top-level ProjectStatus, it is not budget bookkeeping)
    - api/v1alpha3/task_types.go (TaskStatus field layout and comment conventions)
  </read_first>
  <action>
    Add ONE new optional string status field per level — all five types, same shape:

    - MilestoneStatus: `MilestoneReporterSpawnedUID string` with json tag `milestoneReporterSpawnedUID,omitempty` and `+optional`.
    - PhaseStatus: `PhaseReporterSpawnedUID string`, json `phaseReporterSpawnedUID,omitempty`.
    - PlanStatus: `PlanReporterSpawnedUID string`, json `planReporterSpawnedUID,omitempty`.
    - ProjectStatus (top-level, NOT under Budget): `ProjectReporterSpawnedUID string`, json `projectReporterSpawnedUID,omitempty`.
    - TaskStatus: `TaskTraceReporterSpawnedUID string`, json `taskTraceReporterSpawnedUID,omitempty`.

    Doc comment on each, mirroring the *RolledUpUID voice: the UID of the completed planner (Task: dispatch) Job whose reporter Job has been spawned for this level — the durable gate closing the CR-01 window where the name-only spawn gate re-opens after the reporter Job's 300s TTL-GC and a sustained-reconcile parent re-Creates a duplicate reporter with recomputed options (Phase 47 gap-closure; mirrors MilestoneRolledUpUID's role for budget rollup). Note in the comment that the value is the completed Job's UID (falls back to the deterministic planner-Job name when the caller observes a nil Job object).

    Then run `make manifests generate` to regenerate `config/crd/bases/*.yaml` and `api/v1alpha3/zz_generated.deepcopy.go`. Commit whatever the generators emit — do not hand-edit generated files.
  </action>
  <verify>
    <automated>make manifests generate && go build ./api/... && grep -l 'reporterSpawnedUID\|ReporterSpawnedUID' config/crd/bases/*.yaml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MilestoneReporterSpawnedUID' api/v1alpha3/milestone_types.go` returns >= 1; same for the other four types/fields.
    - All five `config/crd/bases/tideproject.k8s_{milestones,phases,plans,projects,tasks}.yaml` contain their respective camelCase property (e.g. `grep -c 'milestoneReporterSpawnedUID' config/crd/bases/tideproject.k8s_milestones.yaml` >= 1).
    - `go build ./api/...` exits 0 after `make manifests generate` (deepcopy regenerated cleanly).
    - `git diff --stat charts/tide/values.yaml` is empty (FIXED contract untouched).
  </acceptance_criteria>
  <done>Five status marker fields exist on the v1alpha3 types with regenerated CRD schemas and deepcopy — additive, optional, no version bump.</done>
</task>

<task type="auto">
  <name>Task 2: Gate reporter Create on the marker and stamp it durably at all five spawn sites</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/task_controller.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go:80-141 (spawnReporterIfNeeded contract: returns (true,nil) when ReporterImage=="" OR newly-spawned/AlreadyExists; (false,nil) when project==nil OR job found by name — callers must distinguish via their own project/ReporterImage checks)
    - internal/controller/milestone_controller.go:640-705 (the spawn call, the completedJob variable in scope, the milestoneJobName construction `tide-milestone-<uid>-1`, and the MilestoneRolledUpUID RetryOnConflict + MergeFromWithOptimisticLock stamp block at ~676-700 — the exact stamp idiom to replicate)
    - internal/controller/phase_controller.go:~590-660 and internal/controller/plan_controller.go:~645-715 (sibling spawn sites + their RolledUpUID stamp blocks)
    - internal/controller/project_controller.go:1894-1990 (the inline project spawn arm — isFirstCompletion flag, reporterJobName, and the plannerJobName `tide-project-<uid>-1` + PlannerRolledUpUID stamp below it; note nil-completedJob completion calls exist per project_planner_completion_test.go BYPASS-03)
    - internal/controller/task_controller.go:1081-1132 (spawnTaskTraceReporterIfNeeded — completedJob is guaranteed non-nil by the early return; the function's deliberate non-fatal "observability never gates" posture)
  </read_first>
  <action>
    Apply the SAME gate+stamp shape at every one of the five sites — all five, not just the milestone one. Shape:

    Gate (before any Get/Create of the reporter Job): compute `spawnKey := string(completedJob.UID)`; when the site can observe a nil completedJob (milestone/phase/plan/project completion paths), fall back to the site's existing deterministic planner-Job name (`milestoneJobName` / `phaseJobName` / `planJobName` / `plannerJobName` — the same strings the *RolledUpUID markers store). If the level's marker field already equals spawnKey, skip the spawn entirely and treat the completion as already-observed (`isFirstCompletion = false` on the planner-tier sites; plain return in spawnTaskTraceReporterIfNeeded). Comment each gate with the CR-01 rationale: the name-only gate re-opens after the reporter's 300s TTL-GC; this durable marker is the authoritative per-attempt guard.

    Stamp (after the spawn path has run without error): stamp the marker to spawnKey using the exact RetryOnConflict + re-fetch-latest + idempotent-equality-check + MergeFromWithOptimisticLock idiom of the adjacent *RolledUpUID stamp blocks. Stamp conditions and ordering:
    - milestone/phase/plan sites: stamp when spawnReporterIfNeeded returned nil error AND project != nil AND r.Deps.ReporterImage != "" (this covers newly-Created, AlreadyExists, and found-by-name — all states where a reporter Job verifiably exists for this attempt; it deliberately also back-fills the marker for reporters spawned before this fix). On RetryOnConflict exhaustion return the error to requeue (WR-03 ordering: the marker must be durable before the TTL-GC window re-opens the name gate).
    - project site: same, using the inline spawn arm's outcome — stamp when the arm completed without returned error and ReporterImage != "" (including the found-existing branch and the AlreadyExists branch). Use a fresh `tidev1alpha3.Project` re-fetch in the RetryOnConflict closure like the PlannerRolledUpUID stamp does.
    - task site (spawnTaskTraceReporterIfNeeded): gate at function entry right after the existing nil/empty guards on `task.Status.TaskTraceReporterSpawnedUID == string(completedJob.UID)`. Stamp after Create success, AlreadyExists, or the existing-by-name early return — but preserve the function's non-fatal posture: on stamp failure log via the existing logger and return (the gate stays open so the next reconcile retries the stamp; behavior degrades to today's, never worse).

    Do NOT change spawnReporterIfNeeded's signature or reporter_jobspec.go's TTL — the marker makes the 300s TTL safe as-is. Do NOT touch the *RolledUpUID logic; the two marker families are independent guards.
  </action>
  <verify>
    <automated>go build ./internal/controller/... && go vet ./internal/controller/... && grep -c 'ReporterSpawnedUID' internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go internal/controller/project_controller.go internal/controller/task_controller.go</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five controller files references its level's marker field at least twice (gate + stamp): `grep -c '<Level>ReporterSpawnedUID' <file>` >= 2 for all five.
    - `grep -A3 'ReporterSpawnedUID' internal/controller/milestone_controller.go | grep -c 'RetryOnConflict'` >= 1 (durable stamp idiom present; same check passes on phase/plan/project files).
    - `go build ./internal/controller/...` and `go vet ./internal/controller/...` exit 0.
    - Existing unit tier still green: `make test` exits 0 (the rollup idempotency envtests and reporter-spawn envtests in the -short tier must not regress).
    - `grep -c 'TTLSecondsAfterFinished' internal/controller/reporter_jobspec.go` unchanged from before this plan (TTL untouched; `git diff internal/controller/reporter_jobspec.go` is empty).
  </acceptance_criteria>
  <done>All five spawn sites are gated on a durable per-attempt marker stamped via the optimistic-lock retry idiom; a TTL-GC'd reporter Job can no longer be re-created for an already-observed completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → CRD .status subresource | markers writable only by the manager's SA (status subresource RBAC) |
| reporter Job → Phoenix ingest | duplicate reporters double span emission and API load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-07-01 | Denial of Service / telemetry integrity | reporter spawn sites (duplicate Jobs after TTL-GC) | mitigate | Durable per-attempt marker gates Create; stamp-before-TTL-window ordering enforced by returning the error on RetryOnConflict exhaustion at planner-tier sites |
| T-47-07-02 | Tampering | new .status marker fields | accept | Status subresource is writable only by the controller SA under existing RBAC; a tampered marker degrades to skipped/duplicate telemetry only — no dispatch, spend, or data-integrity surface |
| T-47-07-03 | Repudiation | spawn observability | mitigate | Existing "spawned reporter Job" / "already exists" log lines retained; gate-skip path keeps V(1) logging so duplicate-suppression is diagnosable from manager logs |
| T-47-SC | Tampering | package installs | n/a | No new dependencies (k8s.io/client-go retry + controller-runtime already in go.mod) |
</threat_model>

<verification>
- `make manifests generate` clean; five CRD schemas gained their camelCase marker property.
- `go build ./...` exits 0; `make test` (unit tier) exits 0.
- All five controllers gate + stamp (grep counts above).
- The envtest proof that a deleted reporter Job is NOT re-created lands in plan 47-10 (wave 7) — this plan's verify is build/unit-level per the gap-closure scope.
</verification>

<success_criteria>
Gap #2's implementation is complete: durable spawn markers exist on all five levels and gate all five spawn sites, mirroring the proven *RolledUpUID pattern, with the FIXED chart contract and reporter TTL untouched. Plan 47-10 proves the TTL-GC behavior in envtest.
</success_criteria>

<output>
Create `.planning/phases/47-self-hosted-phoenix-install-end-to-end-proof/47-07-SUMMARY.md` when done.
</output>
