---
phase: 30-resumable-import-partial-tree-resume-adopt-complete-re-plan
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - test/integration/kind/testdata/import-partial-fixture/project.yaml
  - test/integration/kind/testdata/import-partial-fixture/milestones.yaml
  - test/integration/kind/testdata/import-partial-fixture/phases.yaml
  - test/integration/kind/testdata/import-partial-fixture/plans.yaml
  - test/integration/kind/testdata/import-partial-fixture/seed-manifest.json
  - test/integration/kind/testdata/import-partial-fixture/pvc-envelopes.tgz
  - test/integration/kind/import_resume_test.go
autonomous: false
requirements: [RESUME-PARTIAL-03]

must_haves:
  truths:
    - "A partial import (mixed complete + empty-Status nodes) drives all the way to Project=Complete (or all owned Milestones Succeeded)."
    - "The complete (adopted) Plans do NOT dispatch a re-paid plan planner; the incomplete (empty-Status) Plans DO get re-planned (planner Jobs appear) and materialize Tasks."
    - "The whole partial tree resumes — no Running-with-no-envelope zombie stall (the run #2 failure shape) — proving RESUME-PARTIAL-01/02/04 end-to-end."
  artifacts:
    - path: "test/integration/kind/testdata/import-partial-fixture/seed-manifest.json"
      provides: "Mixed seed: some Plans status:\"Running\" (complete envelope), some status:\"\" (incomplete/no envelope)"
      contains: "\"status\": \"\""
    - path: "test/integration/kind/testdata/import-partial-fixture/pvc-envelopes.tgz"
      provides: "Envelopes for the complete Plans only"
    - path: "test/integration/kind/import_resume_test.go"
      provides: "Tier c spec driving partial import to Project=Complete"
      contains: "Tier c"
  key_links:
    - from: "import_resume_test.go Tier c"
      to: "Project.Status.Phase == Complete"
      via: "Eventually poll after partial import + apply"
      pattern: "PhaseComplete|Succeeded"
    - from: "import-partial-fixture/seed-manifest.json"
      to: "empty-Status Plan nodes"
      via: "re-plan path exercised end-to-end"
      pattern: "status.*\"\""
---

<objective>
Add the missing test tier (Fork 4 — REQUIRED). Tier b (`import_resume_test.go`) asserts only the adoption GATE (zero re-paid milestone/phase planners, ImportComplete=True) and explicitly does NOT drive plan-level planners or reach execution — which is exactly why the zombie stall shipped green. This plan adds a NEW Tier c that drives a PARTIAL import (mixed complete/incomplete) all the way to `Project=Complete`, asserting the END-TO-END outcome, not just the mechanism.

Purpose: A failing-before / passing-after regression guard for the run #2 defect — the only deliverable that proves the whole fix composes (export empty-Status from Plan 01 → re-plannable materialization → project guard from Plan 02 → re-plan cascade → completion).
Output: A hand-authored `testdata/import-partial-fixture/` bundle + a Tier c Ginkgo spec.
</objective>

<chosen_design>
## Fixture: minimal hand-authored mixed bundle (RESEARCH-recommended Option B)

Mirror the structure of the existing `testdata/import-small-fixture/` (project.yaml, milestones.yaml, phases.yaml, plans.yaml, seed-manifest.json, pvc-envelopes.tgz). Make it MIXED:
- 1 Milestone, 1 Phase.
- TWO Plans: `plan-complete` carries `status: "Running"` in seed-manifest.json AND has a complete envelope (out.json with exitCode==0, childCount matching its ChildCRDs) inside pvc-envelopes.tgz; `plan-incomplete` carries `status: ""` in seed-manifest.json AND has NO envelope (or an incomplete one) in the tgz.
- This is the controlled critical path: adopt 1, re-plan 1, all Tasks Succeed under stub subagents, Project completes — fast and deterministic, no dependency on the 60/40 salvage bundle (already exercised by Tier b for the gate-only assertion).

The fixture is authored so the per-node branch (Plan 01) and the project guard (Plan 02) are BOTH exercised: empty-Status Plan must re-plan; complete Plan must adopt; project planner must not re-dispatch post-ImportComplete.

**Stub-subagent dependency (RESEARCH OQ-2 of validation):** reuse the same stub-subagent pattern Tier a relies on (the empty-Status Plan gets re-planned by a plan planner; stub materializes Tasks that Succeed). Reuse the existing helpers (`createNamespace`, `ensureProviderSecret`, `resolveTideBinary`, `waitForImportComplete`, `assertNoPlannerJobsForLevelConsistently`) from import_resume_test.go.

**tide-import SA on the medium template (CONTEXT specific idea):** the medium per-ns template lacked the `tide-import` SA; if the Tier c run surfaces a missing-SA failure, add the SA to the template as part of this plan (it is in scope — the test cannot pass without it). Verify the import Job's SA requirement against importSAName ("tide-import") before assuming it is present.
</chosen_design>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-resumable-import-partial-tree-resume-adopt-complete-re-plan-/30-CONTEXT.md
@.planning/phases/30-resumable-import-partial-tree-resume-adopt-complete-re-plan-/30-RESEARCH.md

<interfaces>
<!-- Contracts + analogs the executor needs. Do not re-explore the codebase for these. -->

Existing analog (clone its structure, make it MIXED):
- test/integration/kind/testdata/import-small-fixture/{project.yaml,milestones.yaml,phases.yaml,plans.yaml,seed-manifest.json,pvc-envelopes.tgz}
- seed-manifest.json shape (per node): {"name","fqName","oldUID","status","projectRef|milestoneRef|phaseRef","sha256"}
  - Complete node: "status":"Running" (or "Succeeded") + sha256 present + envelope in tgz.
  - Incomplete node (NEW): "status":"" + NO envelope in tgz.

Existing test helpers in import_resume_test.go (REUSE, do not re-implement):
- resolveTideBinary() string
- createNamespace(ns), deleteNamespace(ns), ensureProviderSecret(ns)
- waitForImportComplete(ns)   // gates on ConditionImportComplete=True
- assertNoPlannerJobsForLevelConsistently(ns, level, dur, interval, msg)
- assertD02BundleShape(tgzPath)
- Package: kind_integration; Label("kind","long"); skipIfCRDsOnlyMode(); testing.Short() skip.
- exec.CommandContext(ctx, tideBin, "--kubeconfig", kubeconfigPath, "import-envelopes", <dir>, "--namespace", ns)

From api/v1alpha2: Project.Status.Phase terminal values — PhaseComplete; Milestone terminal Status.Phase == "Succeeded".
From internal/controller/import_controller.go: importSAName = "tide-import" (the import Job's ServiceAccount).
EnvelopeOut completeness (for authoring the fixture out.json): exitCode==0 AND len(childCRDs)==childCount (pkg/dispatch.IsEnvelopeComplete, added in Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the mixed partial-tree fixture bundle</name>
  <files>test/integration/kind/testdata/import-partial-fixture/project.yaml, test/integration/kind/testdata/import-partial-fixture/milestones.yaml, test/integration/kind/testdata/import-partial-fixture/phases.yaml, test/integration/kind/testdata/import-partial-fixture/plans.yaml, test/integration/kind/testdata/import-partial-fixture/seed-manifest.json, test/integration/kind/testdata/import-partial-fixture/pvc-envelopes.tgz</files>
  <read_first>
    - test/integration/kind/testdata/import-small-fixture/seed-manifest.json (exact JSON shape to mirror)
    - test/integration/kind/testdata/import-small-fixture/project.yaml (project shape + importSource wiring)
    - cmd/tide-import/main.go:202-227 (how the import Job decides complete vs incomplete — fixture must satisfy this for plan-complete and fail it / omit envelope for plan-incomplete)
  </read_first>
  <action>
    Create testdata/import-partial-fixture/ mirroring import-small-fixture's file set. Define 1 Milestone, 1 Phase, 2 Plans. In seed-manifest.json: plan-complete → "status":"Running" with a sha256 for its envelope; plan-incomplete → "status":"" with no sha256. Build pvc-envelopes.tgz so it contains a complete out.json for plan-complete's oldUID (exitCode:0, childCount matching its childCRDs — author at least one Task childCRD so the adopted plan has work) and NO envelope directory for plan-incomplete's oldUID (or an out.json with exitCode:1 so isEnvelopeComplete returns false). project.yaml wires spec.importSource.seedManifestConfigMap to the seed and a salvagedPVCSubPath consistent with the fixture. Keep UIDs in the fixed aaaa/bbbb/cccc/dddd style used by import-small-fixture. Generate pvc-envelopes.tgz with the same tar layout the importer expects (top-level envelopes/<uid>/out.json). If a generator script pattern exists for import-small-fixture, follow it; otherwise build the tgz directly and check the bytes in.
  </action>
  <verify>
    <automated>test -f test/integration/kind/testdata/import-partial-fixture/seed-manifest.json && grep -q '"status": ""' test/integration/kind/testdata/import-partial-fixture/seed-manifest.json && tar tzf test/integration/kind/testdata/import-partial-fixture/pvc-envelopes.tgz | grep -q 'envelopes/'</automated>
  </verify>
  <acceptance_criteria>
    - All six fixture files exist under test/integration/kind/testdata/import-partial-fixture/.
    - seed-manifest.json contains at least one Plan with `"status": ""` (incomplete) and at least one Plan with a non-empty status (complete).
    - pvc-envelopes.tgz contains an `envelopes/<plan-complete-uid>/out.json` whose contents satisfy pkg/dispatch.IsEnvelopeComplete, and does NOT contain a complete envelope for the incomplete Plan.
    - The verify command exits 0.
  </acceptance_criteria>
  <done>A minimal mixed complete/incomplete fixture bundle exists, parsable by the import path, exercising both the adopt and re-plan branches.</done>
</task>

<task type="auto">
  <name>Task 2: Tier c spec — partial import drives to Project=Complete</name>
  <files>test/integration/kind/import_resume_test.go</files>
  <read_first>
    - test/integration/kind/import_resume_test.go (Tier a for the import→apply→drain-to-Succeeded pattern and helper reuse; Tier b for the gate-only contrast this Tier c fills)
    - internal/controller/import_controller.go:226-233, 567-578 (state machine: all CRs materialized before ImportComplete) to justify the assertion ordering
  </read_first>
  <action>
    Add a third Describe block ("Tier c: partial fixture drains to Project=Complete") in import_resume_test.go, gated Label("kind","long") + skipIfCRDsOnlyMode() + testing.Short() skip, with its own namespace constant (e.g. importResumePartialNS) and BeforeEach/AfterEach mirroring Tier a (createNamespace + ensureProviderSecret + export-logs-on-failure). The It: (1) import testdata/import-partial-fixture via the real tide CLI; (2) kubectl apply the fixture project.yaml into the namespace; (3) waitForImportComplete(ns); (4) assert the incomplete Plan gets re-planned — a planner Job for level=plan appears (Eventually) — while asserting (Consistently) zero re-paid planner Jobs for the ADOPTED complete Plan (distinguish by Job name/label tied to the plan name, or assert plan-level planner count equals the number of incomplete Plans, i.e. 1); (5) Eventually-poll until the Project reaches Status.Phase == Complete (or all owned Milestones Succeeded) within a generous timeout (mirror Tier a's 8m). The completion assertion is the load-bearing one — it is the run #2 failure shape inverted. Add a top-of-block comment stating Tier c covers RESUME-PARTIAL-03 and proves RESUME-PARTIAL-01/02/04 end-to-end, contrasting with Tier b's gate-only scope.
  </action>
  <verify>
    <automated>set -o pipefail; go test ./test/integration/kind/ -run "TestKind" -count=1 2>&1 | grep -E "Tier c|Ran [0-9]+ of"</automated>
  </verify>
  <acceptance_criteria>
    - import_resume_test.go contains a Tier c Describe that imports the partial fixture and asserts Project Status.Phase == Complete (or all Milestones Succeeded).
    - Tier c asserts the incomplete Plan is re-planned (a level=plan planner Job appears) and the complete Plan is NOT re-planned (no re-paid planner for it).
    - The Tier c spec compiles and is collected by the kind suite (visible in `go test` spec listing).
    - In an environment with a kind cluster + built binary (`make test-int-kind-prep`), `make test-int-kind` runs Tier c to green; absent the cluster/binary it Skips cleanly (does not fail compilation).
  </acceptance_criteria>
  <done>A Tier c E2E proves a partial import resumes all the way to Project=Complete — the regression guard the zombie stall slipped past.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Tier c green against a real kind cluster</name>
  <files>(verify-only — no files modified by this task)</files>
  <action>Pause for human verification. The executor must FIRST run `make test-int-kind-prep` then `make test-int` (or `make test-int-kind`) against a kind cluster (do not stop at compilation). Then present the what-built summary and how-to-verify steps below; do NOT auto-approve — CLAUDE.md mandates reading MAKE_EXIT and grepping `^--- FAIL|^FAIL\s` because Ginkgo SUCCESS alone does not mean the package passed. Wait for the resume signal.</action>
  <what-built>The Tier c partial-import-to-completion E2E plus the full Plan 01/02 fix. Claude has run `make test-int-kind-prep` then `make test-int` (or `make test-int-kind`) against a kind cluster.</what-built>
  <how-to-verify>
    1. Confirm the executor ran the kind suite, not just compiled it: read the echoed `MAKE_EXIT` and `grep -nE '^--- FAIL|^FAIL\s'` on the run log (CLAUDE.md: `make test-int` exit != Ginkgo green; plain go-tests in the package can fail the package while Ginkgo prints SUCCESS).
    2. Confirm Tier c actually executed and passed (not Skipped): the log shows the Tier c spec ran and the Project reached Complete (look for the Eventually on Status.Phase==Complete succeeding, not a Skip line).
    3. Confirm the run #2 failure shape is gone: no Running-with-no-envelope stall, the incomplete Plan re-planned, the complete Plan adopted.
    4. If the medium per-ns template needed the tide-import SA added, confirm that change landed and the import Job ran (no SA-missing error in the manager/Job log).
  </how-to-verify>
  <verify>
    <human-check>Human confirms MAKE_EXIT=0 (or the only failure is confirmed-environmental per CLAUDE.md), Tier c ran (not Skipped) and reached Project Status.Phase==Complete, and the run #2 zombie-stall shape is absent.</human-check>
  </verify>
  <acceptance_criteria>
    - The kind suite was executed (not just compiled); MAKE_EXIT read and `^--- FAIL|^FAIL\s` grepped on the log.
    - Tier c ran and the partial import reached Project Status.Phase==Complete.
    - The complete Plan was adopted (not re-paid) and the incomplete Plan was re-planned and completed.
  </acceptance_criteria>
  <done>Human has verified Tier c green against a real kind cluster; the run #2 defect cannot recur without failing Tier c. Phase 30 is ready to close.</done>
  <resume-signal>Type "approved" once Tier c is verified green against a real kind cluster (with MAKE_EXIT=0 or the only failure confirmed-environmental per CLAUDE.md), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture bundle → import CLI/controller | The test fixture is repo-controlled (trusted), but it travels the same untrusted-input path a real salvage bundle would — so it doubles as a path-traversal / malformed-input smoke. |
| stub subagent → Task materialization | Stub subagents drive the re-plan cascade; they are test infra, not a production trust surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Tampering | malformed fixture envelope/seed driving status injection | mitigate | The fixture exercises the fail-closed export/import path from Plans 01/02: an incomplete envelope yields empty status (re-plannable), never a forged terminal status. The existing path-traversal defense in tide-import (containedJoin) is unchanged and is implicitly smoke-tested by the import Job running over the fixture. |
| T-30-03-02 | Denial of Service | re-plan cascade re-dispatching paid LLM work | accept (test-scoped) | Tier c runs under stub subagents (no real LLM calls, $0). It asserts the complete Plan is NOT re-planned, bounding re-dispatch to exactly the incomplete node — proving the cost-containment property rather than violating it. |
| T-30-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit skipped). No install tasks. |
</threat_model>

<verification>
- Tier c spec is collected by the kind suite and compiles (`go test ./test/integration/kind/ -run TestKind` lists it).
- Against a kind cluster (`make test-int-kind-prep` then `make test-int` or `make test-int-kind`): Tier c runs green; read MAKE_EXIT and grep `^--- FAIL|^FAIL\s` on the log per CLAUDE.md — Ginkgo SUCCESS alone is insufficient.
- The partial fixture files exist and the tgz/seed assertions in Task 1 pass.
- `golangci-lint run ./test/integration/kind/...` clean.
</verification>

<success_criteria>
- A partial import (mixed complete/incomplete) drives to Project=Complete under Tier c.
- Adopted complete Plan is not re-paid; incomplete Plan is re-planned and completes.
- The run #2 zombie-stall failure shape cannot recur without failing Tier c.
- Human-verified green against a real kind cluster (MAKE_EXIT=0 or confirmed-environmental-only failure).
</success_criteria>

<output>
Create `.planning/phases/30-resumable-import-partial-tree-resume-adopt-complete-re-plan-/30-03-SUMMARY.md` when done.
</output>
