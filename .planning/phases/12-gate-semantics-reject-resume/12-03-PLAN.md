---
phase: 12-gate-semantics-reject-resume
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - internal/controller/dispatch_helpers.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/controller/phase_gates_test.go
  - test/integration/envtest/gates_test.go
autonomous: true
requirements: [GATE-04]
must_haves:
  truths:
    - "While a Milestone is parked at AwaitingApproval, its materialized Phase children create ZERO planner Jobs (run-1 finding-1 regression: 5 phase planners fired ~1s after the park, ~$0.64 each)"
    - "Held children stay at Status.Phase empty — tide approve's findAwaiting* never targets a held child instead of the parked parent (Pitfall 5)"
    - "Once the parent is approved (Running), held children dispatch on a subsequent reconcile without manual intervention"
    - "The hold is symmetric: Phase holds on parent Milestone, Plan holds on parent Phase, Task holds on parent Plan"
  artifacts:
    - path: "internal/controller/dispatch_helpers.go"
      provides: "checkParentApproval shared helper (parent lookup by spec ref + AwaitingApproval check)"
      contains: "func checkParentApproval"
    - path: "internal/controller/phase_controller.go"
      provides: "parent-approval hold before PlannerPool.Acquire"
    - path: "internal/controller/plan_controller.go"
      provides: "parent-approval hold before PlannerPool.Acquire"
    - path: "internal/controller/task_controller.go"
      provides: "parent-approval hold in gateChecks before budget gate"
  key_links:
    - from: "internal/controller/phase_controller.go"
      to: "checkParentApproval"
      via: "dispatch-entry call with ph.Spec.MilestoneRef / kind Milestone"
      pattern: "checkParentApproval"
    - from: "internal/controller/task_controller.go gateChecks"
      to: "checkParentApproval"
      via: "task.Spec.PlanRef / kind Plan check between reject and budget gates"
      pattern: "checkParentApproval"
---

<objective>
Implement the descent hold (D-02/GATE-04): child CRs materialize immediately (reporter behavior, already exists) but child reconcilers refuse to dispatch planner/executor Jobs while the direct parent is parked at AwaitingApproval. The operator reviews the full DAG with zero spend; approval releases dispatch.

Purpose: Run-1 finding 1 — five phase planners fired one second after the milestone parked, spending ~$3.20 before the operator had reviewed anything. Gate-at-descent's promise is review-before-spend.
Output: checkParentApproval helper, dispatch holds in the three child reconcilers, finding-1 regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-gate-semantics-reject-resume/12-CONTEXT.md
@.planning/phases/12-gate-semantics-reject-resume/12-RESEARCH.md
@.planning/phases/12-gate-semantics-reject-resume/12-PATTERNS.md
@.planning/phases/12-gate-semantics-reject-resume/12-01-SUMMARY.md
</context>

<interfaces>
<!-- Verified at HEAD 2026-06-11 (pre-12-01; re-read the touched ranges after 12-01's changes land) -->

Parent ref fields (api/v1alpha1):
```go
Phase.Spec.MilestoneRef string  // phase_types.go:27
Plan.Spec.PhaseRef      string  // plan_types.go:27
Task.Spec.PlanRef       string  // task_types.go:72
```

Hold insertion points:
- phase_controller.go: after the Running branch in reconcilePlannerDispatch, BEFORE `r.PlannerPool.Acquire` (:233–238 pre-12-01; 12-01 adds an AwaitingApproval branch above this — the hold goes after all status-branch returns, before pool acquire)
- plan_controller.go: same position, before `r.PlannerPool.Acquire` (:244–249)
- task_controller.go gateChecks (:275+): after Step 3 resolveProject and the reject short-circuit (:313–317), before Step 4 budget gate (:319) — returns `taskGateResult{shouldHalt: true, result: ctrl.Result{RequeueAfter: 5 * time.Second}}`

Parent-lookup shape to copy (phase_controller.go resolveProject, per 12-PATTERNS.md):
```go
var ms tideprojectv1alpha1.Milestone
if err := c.Get(ctx, client.ObjectKey{Namespace: ns, Name: parentName}, &ms); err != nil {
    return false, client.IgnoreNotFound(err)  // NotFound = transient (informer lag) → (false, nil), dispatch continues
}
return ms.Status.Phase == "AwaitingApproval", nil
```

Test harness:
- internal/controller/phase_gates_test.go — makeProjectAndMilestone helper (:36–57), cleanup (:59–80), Eventually(Status.Phase) pattern
- test/integration/envtest/gates_test.go — suite helpers newMilestoneReconcilerForGateIT (:349), waitITCacheSync (:410), cleanupGateFlowFixture (:420)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkParentApproval helper + dispatch holds in Phase/Plan/Task reconcilers (D-02)</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go (full file — package layout, imports, existing helper conventions; new function appends at bottom)
    - internal/controller/phase_controller.go (reconcilePlannerDispatch through the PlannerPool.Acquire site — including 12-01's new AwaitingApproval branch)
    - internal/controller/plan_controller.go (lines 215–260 — terminal short-circuit, Running branch, PlannerPool.Acquire at :244)
    - internal/controller/task_controller.go (lines 268–340 — gateChecks steps 1–5, taskGateResult shape, the ParentUnresolved condition pattern at :290–304)
    - .planning/phases/12-gate-semantics-reject-resume/12-PATTERNS.md (sections "dispatch_helpers.go" and "task_controller.go")
  </read_first>
  <behavior>
    - Behavior is proven by the Task-2 reconciler specs (written RED before this implementation when executing this plan's tasks in order is impractical — instead, Task 2's specs are the regression vehicle; this task's own gate is compile-clean plus the existing suites staying green)
  </behavior>
  <action>
    1. In dispatch_helpers.go, add `func checkParentApproval(ctx context.Context, c client.Client, ns, parentName, parentKind string) (bool, error)`: empty parentName → `(false, nil)` (root level); type-switch on parentKind for "Milestone"/"Phase"/"Plan" using the Get + `client.IgnoreNotFound` shape from the interfaces block; returns true iff `parent.Status.Phase == "AwaitingApproval"`. Doc comment: implements the D-02 descent hold — children materialize but dispatch waits for parental approval (tidal lock pending); NotFound is transient informer lag, callers continue dispatch and the next reconcile re-checks.
    2. phase_controller.go: in reconcilePlannerDispatch, after all status-branch early returns (terminal, AwaitingApproval from 12-01, Running) and BEFORE `r.PlannerPool.Acquire`, call `checkParentApproval(ctx, r.Client, ph.Namespace, ph.Spec.MilestoneRef, "Milestone")`. On hold: log V(1) "dispatch held: parent Milestone awaiting approval" with phase + milestone names, `return ctrl.Result{RequeueAfter: 5 * time.Second}, nil`. Write NO status — the Phase stays at `Status.Phase=""` so `tide approve`'s findAwaitingPhase cannot target it (Pitfall 5; this is the resolved RESEARCH.md open question 1: no new condition, no new reason constant).
    3. plan_controller.go: same hold before `r.PlannerPool.Acquire` (:244), with `plan.Spec.PhaseRef` / "Phase".
    4. task_controller.go gateChecks: between the reject short-circuit (:313–317) and the budget gate (:319), call `checkParentApproval(ctx, r.Client, task.Namespace, task.Spec.PlanRef, "Plan")`. On hold: `return taskGateResult{shouldHalt: true, result: ctrl.Result{RequeueAfter: 5 * time.Second}}, nil` with no status write. Position matters: AFTER resolveProject (so ParentUnresolved handling still wins) and BEFORE budget/indegree/dispatch (so a held task spends nothing).
    5. Error propagation: a non-NotFound Get error from the helper returns the error from the reconciler (standard requeue-on-error).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -timeout 600s 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func checkParentApproval' internal/controller/dispatch_helpers.go` returns 1
    - `grep -c 'checkParentApproval' internal/controller/phase_controller.go internal/controller/plan_controller.go internal/controller/task_controller.go` shows >= 1 hit per file
    - Hold paths write no status: the three inserted hold blocks contain no `Status().Patch` call (source assertion)
    - Task hold sits between the CheckRejected block and the BudgetExceeded block in gateChecks (source assertion)
    - `go test ./internal/controller/... -timeout 600s` exits 0 (existing suites unaffected — holds only fire when a parent is AwaitingApproval, which existing dispatch fixtures don't set)
  </acceptance_criteria>
  <done>All three child reconcilers hold Job dispatch behind checkParentApproval with a 5s requeue poll, leaving held children at empty Status.Phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GATE-04 regression tests — zero child Jobs while parent parked (run-1 finding 1)</name>
  <files>internal/controller/phase_gates_test.go, test/integration/envtest/gates_test.go</files>
  <read_first>
    - internal/controller/phase_gates_test.go (full file — makeProjectAndMilestone :36–57, cleanup :59–80, reconciler construction, Eventually patterns)
    - test/integration/envtest/gates_test.go (suite layout :49+, TestGateApproveFlow as updated by 12-01, helpers :349–430)
    - internal/controller/dispatch_helpers.go (the Task-1 helper being exercised)
  </read_first>
  <behavior>
    - phase_gates_test.go NEW Describe "GATE-04 — dispatch hold while parent Milestone awaiting approval": Project + Milestone status-patched to AwaitingApproval + Phase with Spec.MilestoneRef → reconcile the Phase repeatedly → no Job named `tide-phase-<phase-uid>-1` exists AND Phase Status.Phase stays "" (never AwaitingApproval/Running); then status-patch the Milestone to "Running" (simulating 12-01 approval) → reconcile → the planner Job now exists.
    - envtest gates_test.go NEW spec (finding-1 regression): Milestone parked AwaitingApproval with FIVE Phase children materialized (MilestoneRef set, project labels stamped) → drive the PhaseReconciler over all five → List of batchv1.Jobs in the namespace shows ZERO planner Jobs for any of the 5 phases (the exact run-1 symptom: 5 planners fired ~1s after the park) → approve the Milestone via annotation + drive MilestoneReconciler (consumes, returns Running per 12-01) → drive PhaseReconcilers → planner Jobs appear for the children.
  </behavior>
  <action>
    Write both specs per the behavior block. Implementation notes:
    1. The phase_gates_test spec drives `PhaseReconciler.Reconcile` directly (existing pattern in the file); assert Job absence with a direct Get on batchv1.Job expecting NotFound across 3 consecutive reconciles (or a short Consistently window) rather than a single check, so the assertion catches a delayed dispatch.
    2. The envtest spec lives in the existing Describe tree of gates_test.go with the `envtest` label; reuse cleanupGateFlowFixture-style teardown extended for 5 phases (deterministic names, e.g. gate12-ph-1..5). Job-absence assertion: `List(&batchv1.JobList{}, client.InNamespace(ns))` filtered on the `tide-phase-` name prefix → empty while parked.
    3. Both specs assert held Phases report `Status.Phase == ""` while parked — guarding Pitfall 5 (a held child must be invisible to findAwaitingPhase).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -timeout 600s 2>&1 | tail -5 && make test-int-fast 2>&1 | tee /tmp/12-03-int.log | tail -10; echo "MAKE_EXIT=$?"; grep -nE '^--- FAIL|^FAIL ' /tmp/12-03-int.log || echo NO_FAIL_LINES</automated>
  </verify>
  <acceptance_criteria>
    - New phase_gates_test spec passes: zero planner Jobs while parent AwaitingApproval; Job exists after parent reaches Running
    - New envtest spec passes with 5 materialized children and zero Jobs while parked (finding-1 symptom reproduced and refuted)
    - Both specs assert held children stay at `Status.Phase == ""`
    - `make test-int-fast` MAKE_EXIT=0 AND `grep -nE '^--- FAIL|^FAIL ' /tmp/12-03-int.log` returns no matches (per CLAUDE.md exit-code discipline)
  </acceptance_criteria>
  <done>Finding-1 regression locked in at both the controller-unit and envtest layers; review-before-spend proven with five materialized children and zero dispatched Jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent CR status → child dispatch decision | child reconcilers read parent Status.Phase to decide whether to spend (create Jobs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Denial of service (spend) | child planner/executor dispatch | mitigate | the hold itself: zero Jobs while parent parked closes run-1's pre-review spend gap (~$0.64/planner); requeue is bounded at 5s polling, no hot loop |
| T-12-09 | Tampering | parent Status.Phase as hold signal | accept | forging a parent's Status.Phase requires status-patch RBAC on the CRD — same control that protects every other status field; no new surface |
| T-12-10 | Denial of service (wedge) | NotFound parent during informer lag | mitigate | helper returns (false, nil) on NotFound so a missing-parent blip never permanently blocks dispatch; non-NotFound errors propagate to the standard requeue-on-error path |
| T-12-SC | Tampering | package installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `go test ./internal/controller/... -timeout 600s` green including the new GATE-04 specs
- `make test-int-fast` MAKE_EXIT=0 and zero FAIL lines in the captured log
- ROADMAP success criterion 2 demonstrated by the envtest spec: children materialized + visible, zero child Jobs until approval
</verification>

<success_criteria>
- ROADMAP Phase 12 success criterion 2 satisfied: while a level is parked at AwaitingApproval, children are materialized and visible but zero child planner/executor Jobs exist; regression test reproduces the run-1 finding-1 symptom
- Hold is policy-driven by the parent's actual gate state (parent AwaitingApproval), not hardcoded gate policy in the controller — gate config remains in Project.Spec.Gates (CLAUDE.md constraint)
</success_criteria>

<output>
Create `.planning/phases/12-gate-semantics-reject-resume/12-03-SUMMARY.md` when done
</output>
