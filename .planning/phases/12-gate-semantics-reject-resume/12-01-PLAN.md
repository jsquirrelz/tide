---
phase: 12-gate-semantics-reject-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha1/shared_types.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/milestone_gates_test.go
  - internal/controller/phase_gates_test.go
  - test/integration/envtest/gates_test.go
  - docs/gates.md
autonomous: true
requirements: [GATE-01, GATE-02]
must_haves:
  truths:
    - "Approving a gated Milestone with 5 incomplete Phase children returns it to Running with an ApprovedByUser condition — it does NOT become Succeeded (run-1 finding-7 regression)"
    - "An approved level reaches Succeeded only via the ChildCount-gated succession path after all children complete"
    - "A Phase parked at AwaitingApproval no longer re-enters the planner dispatch body on every reconcile (finding-2 oscillation root cause)"
    - "gates.md no longer claims approval 'advances the level to Succeeded' and no longer sketches an Approved phase value"
  artifacts:
    - path: "api/v1alpha1/shared_types.go"
      provides: "ReasonApprovedByUser constant"
      contains: 'ReasonApprovedByUser = "ApprovedByUser"'
    - path: "internal/controller/milestone_controller.go"
      provides: "AwaitingApproval branch that consumes the annotation and returns to Running (never calls patchMilestoneSucceeded directly)"
    - path: "internal/controller/phase_controller.go"
      provides: "AwaitingApproval early-return branch (parity with milestone) + already-approved gate-hook check"
    - path: "internal/controller/milestone_gates_test.go"
      provides: "GATE-01 finding-7 regression spec (ChildCount=5, approve, assert Running not Succeeded)"
    - path: "docs/gates.md"
      provides: "Approve-at-descent documentation (GATE-02)"
  key_links:
    - from: "milestone_controller.go AwaitingApproval branch"
      to: "handleJobCompletion ChildCount-gated succession"
      via: "Status.Phase=Running transition + Requeue (Running branch re-enters handleJobCompletion)"
      pattern: 'ReasonApprovedByUser'
    - from: "milestone_controller.go gate-policy hook"
      to: "ApprovedByUser condition"
      via: "meta.FindStatusCondition check that prevents re-parking after approval"
      pattern: 'FindStatusCondition'
---

<objective>
Fix the run-1 finding-7 run-killer: approval of a gated level must never jump it to Succeeded past its incomplete children. Approval consumes the annotation, returns the level to Running with a permanent `ApprovedByUser` condition, and succession remains exclusively children-gated (D-01, D-03, D-04). Bring phase_controller.go to parity (it lacks the AwaitingApproval early-return entirely — finding-2 root cause). Rewrite docs/gates.md step 5 which currently encodes the bug (GATE-02).

Purpose: This is the trust-killer from dogfood run 1 — `tide approve` on a Milestone with 5 running Phases set the Milestone Succeeded and the Project Complete. v1.0.1 gates run 2 on this fix.
Output: Corrected approval routing in milestone + phase reconcilers, new ReasonApprovedByUser constant, regression tests reproducing the finding-7 symptom, rewritten gates.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-gate-semantics-reject-resume/12-CONTEXT.md
@.planning/phases/12-gate-semantics-reject-resume/12-RESEARCH.md
@.planning/phases/12-gate-semantics-reject-resume/12-PATTERNS.md
</context>

<interfaces>
<!-- Key contracts verified at HEAD 2026-06-11. Executor uses these directly. -->

From api/v1alpha1/shared_types.go (lines 128–155, existing gate vocabulary):
```go
ConditionWaveOrLevelPaused = "WaveOrLevelPaused"
ReasonAwaitingApproval     = "AwaitingApproval"
ReasonPausedAtBoundary     = "PausedAtBoundary"
ReasonRejectedByUser       = "RejectedByUser"   // :151
ReasonResumedByUser        = "ResumedByUser"    // :154
```

From internal/gates/annotation.go (primitives — DO NOT MODIFY, purity contract T-04-G2):
```go
func CheckApprove(obj metav1.Object, level string) bool
func ConsumeApprove(obj metav1.Object, level string) map[string]string  // returns NEW map
func EvaluatePolicy(g *tideprojectv1alpha1.Gates, level string) Policy  // PolicyApprove / PolicyPause / auto
func BoundaryDetected(ctx, c, parent, childKind) (bool, error)
```

milestone_controller.go key sites (verified at HEAD):
- :214–225 — AwaitingApproval branch in reconcilePlannerDispatch; :222 is THE BUG (`return r.patchMilestoneSucceeded(ctx, ms)` when planner Job is TTL'd)
- :229–245 — Running branch; calls `handleJobCompletion(ctx, ms, nil)` when Job is gone (TTL), `handleJobCompletion(ctx, ms, &job)` when terminal
- :466–481 — gate-policy hook inside handleJobCompletion: `if !gates.CheckApprove(ms, "milestone") { return r.patchMilestoneAwaitingApproval(...) }` then consume
- :495–525 — ChildCount-gated succession (envReadOK path): expected==0 → succeed; observed<expected → RequeueAfter 5s; BoundaryDetected → push+succeed
- :526–542 — fallback path (EnvReader nil or read error): gates on hasChildPhases, falls through to `patchMilestoneSucceeded` at :542
- :601–621 — patchMilestoneAwaitingApproval (MergeFrom + Status().Patch shape to copy)

phase_controller.go key sites (verified at HEAD):
- :186–189 — reconcilePlannerDispatch entry has ONLY `Succeeded || Failed` short-circuit; NO AwaitingApproval branch (finding-2 root cause)
- :233–238 — PlannerPool.Acquire before Job creation
- :381–394 — gate-policy hook in handleJobCompletion (same shape as milestone)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reroute milestone approval through children-gated succession (D-03/D-04) + GATE-01 regression spec</name>
  <files>api/v1alpha1/shared_types.go, internal/controller/milestone_controller.go, internal/controller/milestone_gates_test.go</files>
  <read_first>
    - internal/controller/milestone_controller.go (lines 200–260 reconcilePlannerDispatch, lines 390–545 handleJobCompletion — the exact branches being changed)
    - api/v1alpha1/shared_types.go (lines 128–155 — constant block receiving ReasonApprovedByUser)
    - internal/controller/milestone_gates_test.go (full file — Describe structure, driveToJobCompletion helper at ~:79–94, Test 2 "approve annotation present: resumes to Succeeded" at ~:137–196 which must be updated)
    - .planning/phases/12-gate-semantics-reject-resume/12-PATTERNS.md (section "internal/controller/milestone_controller.go" — replacement pattern with exact code shape)
  </read_first>
  <behavior>
    - Test 1 (NEW finding-7 regression spec): gates.milestone=approve, envReader.SetOut(uid, EnvelopeOut{ChildCount: 5}); drive planner Job to completion → Milestone parks AwaitingApproval; patch approve-milestone annotation; reconcile → Status.Phase=="Running" (NOT "Succeeded"), ConditionWaveOrLevelPaused has Status=False and Reason=="ApprovedByUser", annotation consumed (gone from metadata); further reconciles with 0 materialized children keep it un-Succeeded; create 5 child Phases owned by the Milestone each status-patched to Succeeded; reconcile → Milestone reaches Succeeded.
    - Test 2 (UPDATE existing "Test 2 — approve annotation present: resumes to Succeeded"): the approve-annotation path now first transitions to Running + ApprovedByUser, and reaches Succeeded only via the ChildCount gate (existing fixture is a leaf, ChildCount=0, so it still ends Succeeded — add the intermediate Running+ApprovedByUser assertion before the terminal one).
  </behavior>
  <action>
    1. In api/v1alpha1/shared_types.go, add `ReasonApprovedByUser = "ApprovedByUser"` immediately after `ReasonResumedByUser` (:154), with a doc comment noting it mirrors ReasonResumedByUser, records permanent human approval (D-04), and that no new Status.Phase enum value exists — the level returns to Running.

    2. In milestone_controller.go reconcilePlannerDispatch, rewrite the AwaitingApproval branch (:214–225). New behavior when `gates.CheckApprove(ms, "milestone")` is true: (a) consume via `newAnno := gates.ConsumeApprove(ms, "milestone")`, metadata patch with `client.MergeFrom(ms.DeepCopy())` + `r.Patch`; (b) separate status patch: `ms.Status.Phase = "Running"` + `meta.SetStatusCondition` with Type=ConditionWaveOrLevelPaused, Status=metav1.ConditionFalse, Reason=ReasonApprovedByUser, Message="Milestone approved; children will dispatch"; (c) `return ctrl.Result{Requeue: true}, nil`. Remove BOTH the `handleJobCompletion` call and the `patchMilestoneSucceeded` fast-path from this branch — the Running branch (:229) re-enters handleJobCompletion on the requeue, which owns succession. When CheckApprove is false, keep `return ctrl.Result{}, nil`. Update the branch's Phase 04.1 comment to describe the D-04 two-step (consume → Running+ApprovedByUser → children-gated succession), citing GATE-01.

    3. In handleJobCompletion's gate-policy hook (:466–481), prevent re-parking after approval: before the `!gates.CheckApprove` park, compute `alreadyApproved` via `meta.FindStatusCondition(ms.Status.Conditions, tideprojectv1alpha1.ConditionWaveOrLevelPaused)` — true when the condition exists with Status==metav1.ConditionFalse and Reason is ReasonApprovedByUser or ReasonResumedByUser. If alreadyApproved, skip the entire policy block (fall through to succession). If not approved and no annotation → park (unchanged). If the annotation IS present at the hook (operator approved before the park ever happened): consume it AND write the same Running+ApprovedByUser status patch as step 2 before falling through — otherwise the next reconcile re-parks because the annotation is gone but no approval record exists.

    4. Close Pitfall 1 (RESEARCH.md): in handleJobCompletion's envelope-read block, distinguish "EnvReader is nil" from "EnvReader.Read returned an error". When the reader exists but the read errors, `return ctrl.Result{RequeueAfter: 5 * time.Second}, nil` instead of falling into the :526–542 hasChildPhases fallback — a transient read failure must never let the fallback fire patchMilestoneSucceeded past incomplete children. The nil-EnvReader fallback (unit-test path) keeps its existing hasChildPhases-gated behavior.

    5. Write the failing regression spec FIRST (RED), then apply steps 1–4 (GREEN). New Describe block in milestone_gates_test.go: "GATE-01 regression (run-1 finding 7) — approve with incomplete children". Copy the existing driveToJobCompletion helper usage and the Eventually(...Status.Phase) assertion pattern. Per the behavior block above. Update existing Test 2 in the same commit.

    The invariant after this task (D-03): `patchMilestoneSucceeded` is reachable ONLY from handleJobCompletion's ChildCount gate (expected==0 leaf, or BoundaryDetected) and the nil-EnvReader fallback — never from the AwaitingApproval branch.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -timeout 600s 2>&1 | tail -20 && grep -n 'patchMilestoneSucceeded' internal/controller/milestone_controller.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ReasonApprovedByUser = "ApprovedByUser"' api/v1alpha1/shared_types.go` returns 1
    - The AwaitingApproval branch of reconcilePlannerDispatch (between the terminal short-circuit and the Running branch) contains no call to patchMilestoneSucceeded and no call to handleJobCompletion — confirmed by reading the branch
    - New spec asserts: after approve with ChildCount=5 and 0 complete children, Status.Phase=="Running" and FindStatusCondition(WaveOrLevelPaused).Reason=="ApprovedByUser" with Status=False
    - New spec asserts Succeeded is reached after all 5 child Phases are status-patched Succeeded
    - `go test ./internal/controller/... -timeout 600s` exits 0 with the new spec included
  </acceptance_criteria>
  <done>Milestone approval transitions AwaitingApproval → Running + ApprovedByUser; Succeeded fires only via ChildCount-gated succession; finding-7 regression spec green; gate hook does not re-park an approved level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase controller parity — AwaitingApproval branch + already-approved hook check; update approve-flow tests</name>
  <files>internal/controller/phase_controller.go, internal/controller/phase_gates_test.go, test/integration/envtest/gates_test.go</files>
  <read_first>
    - internal/controller/phase_controller.go (lines 183–340 reconcilePlannerDispatch + dispatch body, lines 360–460 handleJobCompletion — sites being changed)
    - internal/controller/milestone_controller.go (the Task-1 result — the exact branch shape to mirror one level down)
    - internal/controller/phase_gates_test.go (Test 5b "approve-phase annotation resumes Succeeded" at ~:141–196 — must be updated)
    - test/integration/envtest/gates_test.go (TestGateApproveFlow Describe at :84–160, suite helpers newMilestoneReconcilerForGateIT/:349, waitITCacheSync/:410, cleanupGateFlowFixture/:420)
  </read_first>
  <behavior>
    - Test 5b (UPDATE): approve-phase annotation now yields Status.Phase=="Running" + ConditionWaveOrLevelPaused{Status: False, Reason: "ApprovedByUser"} first; Succeeded only via the children-gated path (leaf fixture still terminates Succeeded).
    - TestGateApproveFlow (UPDATE, envtest): after `approve-milestone` annotation lands, Milestone transitions to Running with Reason=ApprovedByUser — NOT Succeeded; the spec then drives child completion (existing fixture chain) and only then asserts Milestone Succeeded. The It() description "approve-milestone annotation handshake transitions Milestone Succeeded" is renamed to reflect approve-then-wait-for-children.
    - NEW spec (phase oscillation, finding-2 root cause): a Phase status-patched to AwaitingApproval, reconciled 3 times with no approve annotation, stays AwaitingApproval every time and creates zero planner Jobs (the early-return holds).
  </behavior>
  <action>
    1. In phase_controller.go reconcilePlannerDispatch, insert an AwaitingApproval branch immediately after the `Succeeded || Failed` short-circuit (:187–189), before the jobName construction — the milestone branch shape from Task 1 with level string "phase": CheckApprove → ConsumeApprove + metadata patch → status patch Status.Phase="Running" + ConditionWaveOrLevelPaused{ConditionFalse, ReasonApprovedByUser, "Phase approved; children will dispatch"} → `ctrl.Result{Requeue: true}`. No annotation → `ctrl.Result{}, nil` (early return — this alone stops the finding-2 AwaitingApproval↔Running oscillation; cite Pitfall 2 of 12-RESEARCH.md in the comment).
    2. In phase_controller.go handleJobCompletion's gate-policy hook (:381–394), add the same alreadyApproved check as milestone Task-1 step 3 (FindStatusCondition on ConditionWaveOrLevelPaused, ConditionFalse + Reason in {ReasonApprovedByUser, ReasonResumedByUser} → skip the policy block) and the same consume-plus-condition write when the annotation is present at the hook.
    3. Verify the phase fallback path: phase handleJobCompletion's nil-EnvReader fallback already gates on hasChildPlans (~:440–455 per 12-PATTERNS.md). Apply the same read-error-vs-nil-reader distinction as milestone Task-1 step 4 (read error → RequeueAfter 5s) so a transient envelope failure cannot fire patchPhaseSucceeded past incomplete children.
    4. Update tests per the behavior block — write failing updates first, then implement. In TestGateApproveFlow, the assertion flip is: Eventually Running+ApprovedByUser → drive children (existing markGateITTaskSucceeded / fixture chain) → Eventually Succeeded.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -timeout 600s 2>&1 | tail -5 && make test-int-fast 2>&1 | tee /tmp/12-01-int.log | tail -10; echo "MAKE_EXIT=$?"; grep -nE '^--- FAIL|^FAIL ' /tmp/12-01-int.log || echo NO_FAIL_LINES</automated>
  </verify>
  <acceptance_criteria>
    - phase_controller.go reconcilePlannerDispatch contains `if ph.Status.Phase == "AwaitingApproval"` before jobName construction — `grep -n 'AwaitingApproval' internal/controller/phase_controller.go` shows the branch in reconcilePlannerDispatch
    - Updated TestGateApproveFlow asserts Reason=="ApprovedByUser" with Status.Phase=="Running" BEFORE any Succeeded assertion
    - New oscillation spec: 3 consecutive reconciles of an unapproved AwaitingApproval Phase leave Status.Phase=="AwaitingApproval" and zero Jobs in the namespace
    - `make test-int-fast` MAKE_EXIT=0 AND `grep -nE '^--- FAIL|^FAIL\s' /tmp/test-int-fast.log` (or equivalent captured output) returns no lines — per CLAUDE.md, do not trust the Ginkgo summary alone
  </acceptance_criteria>
  <done>Phase reconciler mirrors milestone approval routing; envtest approve-flow spec proves approve-then-wait-for-children end-to-end; oscillation early-return in place.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite docs/gates.md for approve-at-descent semantics (GATE-02)</name>
  <files>docs/gates.md</files>
  <read_first>
    - docs/gates.md (full file — verb table :38–43, annotation handshake :49–67, "End-to-end approve flow" :69–83, "Reject flow" :85–91, "What's coming" :93–100)
    - .planning/phases/12-gate-semantics-reject-resume/12-CONTEXT.md (D-01, D-04, D-05, D-06 — the semantics being documented)
  </read_first>
  <action>
    Rewrite four sections of docs/gates.md to match the Phase-12 contract. Match the repo's tight, declarative, em-dash-heavy voice.

    1. "End-to-end approve flow": rewrite steps 2 and 5 for gate-at-descent (D-01). Step 2 becomes: the gate parks the level at `AwaitingApproval` after its artifact is authored and BEFORE children dispatch — children are materialized by the reporter (visible in dashboard/kubectl) but their reconcilers hold all Job dispatch while the parent is parked (zero spend during review). Step 5 becomes the two-step: reconciler consumes the annotation via `gates.ConsumeApprove` (one-shot), patches `Status.Phase=Running` + a permanent `ApprovedByUser` condition; succession to `Succeeded` happens only when all children complete via the ChildCount-gated boundary check — approval never advances a level past its children. The exact sentence "advances the level to `Succeeded`" must be GONE (run-1 review-discipline grep).
    2. "What's coming" (:93–100): delete the four-state sketch line "(`Running` → `AwaitingApproval` → `Approved` → `Succeeded`)" — superseded by D-04 (no `Approved` phase value; the lifecycle is `Running` → `AwaitingApproval` → `Running` (+ApprovedByUser condition) → `Succeeded`). Replace the bullet with the actual transition just described.
    3. Verb table (:38–43): update `tide reject` effect to "parks the Project — sets a Rejected condition and halts further dispatch; children pause where they are; never writes Status.Phase=Failed (Failed is reserved for real failures)". Update `tide resume` to "lifts a reject park (clears the annotation); with `--retry-failed`, also resets Status.Phase on genuinely Failed levels so reconcilers re-dispatch (sets ResumedByUser)".
    4. "Reject flow": rewrite for D-05/D-06 — reject parks (condition `WaveOrLevelPaused` Reason=`RejectedByUser`), in-flight Jobs drain, no state is destroyed; recovery is `tide resume`; genuinely Failed levels (e.g. a failed planner Job) are recovered with `tide resume <project> --retry-failed`, and `tide approve` against a Failed level errors with a pointer to that verb (D-07). Add a short "Recovering Failed levels" subsection documenting both the sanctioned verb AND the raw kubectl recipe for legacy CRs that lack the `tideproject.k8s/project` label (the run-1 CRs predate the label fix, which ships in Phase 15 CUTS-01): `kubectl patch <kind> <name> --subresource=status --type=merge -p '{"status":{"phase":"","conditions":[]}}'`.

    Note: steps 3–4 document behavior delivered by plans 12-02 and 12-04 of this same phase — the doc and the fixes land within one phase, satisfying the STATE.md constraint that the doc change ships with the fix.
  </action>
  <verify>
    <automated>test "$(grep -c 'advances the level to' docs/gates.md)" -eq 0 && test "$(grep -cE '`Approved`' docs/gates.md)" -eq 0 && grep -c 'retry-failed' docs/gates.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "advances the level to" docs/gates.md` returns 0 (ROADMAP success criterion 3)
    - `grep -cE '\x60Approved\x60' docs/gates.md` returns 0 (the phase-value sketch is gone)
    - `grep -c 'retry-failed' docs/gates.md` returns >= 2 (verb table row + recovery subsection)
    - `grep -c 'ApprovedByUser' docs/gates.md` returns >= 1
    - The kubectl status-reset recipe appears verbatim in the recovery subsection
  </acceptance_criteria>
  <done>gates.md documents approve-at-descent, the Running+ApprovedByUser shape, park-not-fail reject, and resume --retry-failed recovery; the bug-encoding sentence and Approved sketch are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → K8s API | `tide approve` writes annotations with the operator's kubeconfig RBAC; annotations are the gate channel |
| annotation → reconciler | reconciler consumes operator intent from CR metadata; a replayed/stale annotation must not re-trigger passage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of privilege | milestone/phase approval routing | mitigate | D-03 invariant: approval can no longer elevate a level to Succeeded past incomplete children; succession exclusively children-gated (this plan IS the mitigation for the run-1 escalation) |
| T-12-02 | Spoofing/Replay | gates.ConsumeApprove sites | mitigate | preserve T-04-G2 one-shot consume (annotation removed in the same reconcile that acts on it); the new ApprovedByUser condition is a status record, not a trigger — re-adding the annotation after approval is consumed by the hook idempotently without state regression |
| T-12-03 | Tampering | ApprovedByUser condition | accept | anyone with status-patch RBAC on Milestone/Phase can forge the condition — identical posture to forging Status.Phase today; RBAC on CRD status subresource is the existing control |
| T-12-SC | Tampering | package installs | accept | no packages installed by this plan (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `go test ./internal/controller/... -timeout 600s` green including the new finding-7 regression and oscillation specs
- `make test-int-fast` MAKE_EXIT=0 and zero `^--- FAIL|^FAIL ` lines in captured output
- `grep -c "advances the level to" docs/gates.md` == 0
- Manual (optional, live kind cluster `tide` with run-1 CRs — NEVER delete the cluster or CRs): with a fixed controller image loaded, `tide approve` on the parked run-1 Milestone returns it to Running with ApprovedByUser, not Succeeded
</verification>

<success_criteria>
- ROADMAP Phase 12 success criterion 1 satisfied: approving a gated Milestone with 5 incomplete children does not set Succeeded; regression test reproduces finding 7
- ROADMAP success criterion 3 satisfied: gates.md step-5 bug text and Approved sketch gone
- All existing gate tests updated and green; no `patchMilestoneSucceeded`/`patchPhaseSucceeded` call reachable from an AwaitingApproval branch
</success_criteria>

<output>
Create `.planning/phases/12-gate-semantics-reject-resume/12-01-SUMMARY.md` when done
</output>
