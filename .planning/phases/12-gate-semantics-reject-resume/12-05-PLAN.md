---
phase: 12-gate-semantics-reject-resume
plan: 05
type: execute
wave: 4
depends_on: [12-01, 12-03]
files_modified:
  - internal/controller/plan_controller.go
  - internal/controller/plan_gates_test.go
autonomous: true
gap_closure: true
requirements: [GATE-01, GATE-04]

must_haves:
  truths:
    - "A Plan with gates.plan=approve and ChildCount>0 parks at AwaitingApproval when its planner Job completes — before any executor Task dispatches (GATE-01, Plan level)"
    - "While a Plan is parked at AwaitingApproval, zero executor Jobs and zero Wave CRs are created; the checkParentApproval(kind=Plan) descent hold is live (GATE-04, Plan→Task)"
    - "A parked Plan stays parked across reconciles with no approve annotation — never stomped back to Running (CR-02 oscillation closed)"
    - "Approving a parked Plan consumes the annotation, returns it to Running with the ApprovedByUser condition, and executor dispatch then proceeds (D-04)"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "AwaitingApproval early-return in reconcilePlannerDispatch (before the tasks-exist exit) + gate-policy hook repositioned before the ChildCount requeue in handlePlannerJobCompletion + reject short-circuit hoisted to top (milestone parity)"
      contains: "ReasonApprovedByUser"
    - path: "internal/controller/plan_gates_test.go"
      provides: "Test 6d (ChildCount>0 park + zero executor Jobs while parked) and Test 6e (parked leaf Plan stays parked) regression specs"
      contains: "ChildCount"
  key_links:
    - from: "internal/controller/plan_controller.go reconcilePlannerDispatch"
      to: "patchPlanAwaitingApproval hold"
      via: "AwaitingApproval early-return placed BEFORE the tasks-exist List, returning dispatched=true so reconcileWaveMaterialization never runs while parked"
      pattern: "AwaitingApproval"
    - from: "internal/controller/plan_controller.go handlePlannerJobCompletion gate hook"
      to: "patchPlanAwaitingApproval"
      via: "gate-policy check moved before the expected := out.ChildCount requeue gate"
      pattern: "EvaluatePolicy.*\"plan\""
    - from: "internal/controller/task_controller.go:326 checkParentApproval(kind=Plan)"
      to: "plan.Status.Phase == AwaitingApproval"
      via: "Plan now actually enters AwaitingApproval for ChildCount>0, making the Task descent hold live"
      pattern: "checkParentApproval"
---

<objective>
Close the two Phase 12 verification gaps (CR-01 + CR-02 from 12-REVIEW.md, GATE-01/GATE-04 at the Plan level from 12-VERIFICATION.md):

1. **CR-01 (gate bypass):** the plan-level approve gate hook lives only in `handlePlannerJobCompletion` AFTER the ChildCount requeue, and `reconcilePlannerDispatch` early-exits to the wave path the moment any Task with `planRef` exists. For every real planner output (ChildCount>0), `patchPlanAwaitingApproval` never fires and executor Tasks dispatch with zero approval — the run-1 finding-1 failure class one level down.
2. **CR-02 (park oscillation):** `reconcilePlannerDispatch` lacks the AwaitingApproval early-return that Milestone (:216-243) and Phase (:201-228) both have, so a parked leaf Plan falls through to the dispatch body and is stomped back to Running at the `PlannerDispatched` status patch with no annotation consumed.

Purpose: `Project.Spec.Gates.Plan` is a first-class operator-configurable field; gates.plan=approve must hold executor spend until review, exactly as Milestone and Phase already do (verified clean).
Output: plan_controller.go fix mirroring the milestone analog + regression specs reproducing both symptoms (TDD: RED commit first, GREEN commit second).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-gate-semantics-reject-resume/12-VERIFICATION.md
@.planning/phases/12-gate-semantics-reject-resume/12-CONTEXT.md
@internal/controller/plan_controller.go
@internal/controller/milestone_controller.go
@internal/controller/plan_gates_test.go

<interfaces>
<!-- Key contracts the executor needs. Extracted from merged HEAD. Use directly — no exploration needed. -->

From internal/controller/plan_controller.go (current flow, the defect):
- `func (r *PlanReconciler) reconcilePlannerDispatch(ctx, plan) (ctrl.Result, bool, error)` — line 207. Returns `(result, dispatched, error)`; `dispatched=true` means `reconcileWaveMaterialization` MUST NOT run this pass (Reconcile routing at :166-174). Function body order TODAY: tasks-exist List + early exit `(ctrl.Result{}, false, nil)` (:210-219) → terminal short-circuit Succeeded||Failed (:221-224) → Running branch → `handlePlannerJobCompletion` (:229-242) → D-02 parent-Phase hold (:244-255) → D-05 reject hold (:257-266) → pool/dispatch → `plan.Status.Phase = "Running"` stomp (:366-377). NO AwaitingApproval branch anywhere.
- `func (r *PlanReconciler) handlePlannerJobCompletion(ctx, plan, _ *batchv1.Job) (ctrl.Result, error)` — line 394. Body order TODAY: resolveProjectForPlan (:396-400) → EnvReader nil-fallback (:407-415) → ReadOut + EnvelopeReadFailed (:417-433) → reporter spawn (:440-465) → budget rollup (:467-472) → ValidationState=Validated stamp (:477-481) → ChildCount requeue gate `expected := out.ChildCount; if observed < expected → RequeueAfter 5s` (:490-498) → reject check + gate-policy hook (:503-519) → maybeTriggerBoundaryPush (:538-540) → clear Running to "" (:542-555).
- `func (r *PlanReconciler) patchPlanAwaitingApproval(ctx, plan, policy) (ctrl.Result, error)` — line 627. Sets Phase=AwaitingApproval + WaveOrLevelPaused=True (ReasonAwaitingApproval or ReasonPausedAtBoundary).
- `func (r *PlanReconciler) patchPlanRejected(ctx, plan, reason) (ctrl.Result, error)` — line 592.

From internal/controller/milestone_controller.go (THE ANALOG — copy these shapes):
- Step 1a AwaitingApproval early-return: lines 216-243. No annotation → `return ctrl.Result{}, nil` (keep paused). Annotation present → ConsumeApprove + metadata Patch, then status Patch Phase="Running" + condition {Type: ConditionWaveOrLevelPaused, Status: ConditionFalse, Reason: ReasonApprovedByUser}, then `return ctrl.Result{Requeue: true}, nil`.
- Reject short-circuit at TOP of handleJobCompletion: lines 442-449 ("Phase 04.1: reject short-circuit FIRST — operator stop should always halt").
- Gate-policy hook with alreadyApproved guard, positioned AFTER reporter spawn/budget rollup and BEFORE the ChildCount succession: lines 503-553. The alreadyApproved guard (:515-521): `meta.FindStatusCondition(ms.Status.Conditions, ConditionWaveOrLevelPaused)` with `Status == ConditionFalse && (Reason == ReasonApprovedByUser || Reason == ReasonResumedByUser)` → skip park. Annotation-present-at-hook path (:531-548): consume + write Running+ApprovedByUser so the next reconcile does not re-park.

From api/v1alpha1/shared_types.go:
- `ConditionWaveOrLevelPaused`, `ReasonAwaitingApproval`, `ReasonApprovedByUser`, `ReasonResumedByUser`, `ReasonRejectedByUser` — all exist (added in 12-01); no new constants needed.

From internal/gates/annotation.go:
- `gates.EvaluatePolicy(project.Spec.Gates, "plan")`, `gates.CheckApprove(plan, "plan")`, `gates.ConsumeApprove(plan, "plan")` (returns NEW map — T-04-G2 purity), `gates.PolicyApprove`, `gates.PolicyPause`, `gates.AnnotationApprovePrefix`.

Test helpers (all in package `controller`, shared across _test.go files):
- `makeProjectChain(projectName, msName, phaseName, gates)` + `cleanup(...)` + `driveToJobCompletion(planName, r, envReader)` — plan_gates_test.go:40-116. `driveToJobCompletion` sets `EnvelopeOut{TaskUID, ExitCode: 0}` with NO ChildCount (defaults 0 — the leaf-only fixture that masked CR-01).
- `makeTask(name, planRef string, dependsOn []string, projectName ...string)` — task_controller_test.go:134. `newTaskReconciler(envReader)` — task_controller_test.go:59. `reconcileN(r *TaskReconciler, name, n)` — task_controller_test.go:80. `getTaskUID(name)` — task_gates_test.go:205. `reconcileWithRetry(r.Reconcile, name, n)` — milestone_controller_test.go:43. `waitForCacheSync` — task_controller_test.go:114.
- Executor Job naming: `podjob.JobName(taskUID, attempt)` = `tide-task-<taskUID>-<attempt>` (internal/dispatch/podjob/names.go:37); executor Jobs carry label `tideproject.k8s/task-uid` (assertion pattern: task_gates_test.go:86-91). Planner Job for a Plan: `tide-plan-<planUID>-1`.
- Task descent hold: task_controller.go:326 `checkParentApproval(ctx, r.Client, task.Namespace, task.Spec.PlanRef, "Plan")` — holds with NO status write (held Task stays Phase=""), RequeueAfter 5s. Test 7c (task_gates_test.go:170+) proves `makeTask` + `reconcileN` dispatches an executor Job with gates.task=auto and no Wave CR required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — regression specs reproducing CR-01 (ChildCount>0 gate bypass) and CR-02 (park oscillation)</name>
  <files>internal/controller/plan_gates_test.go</files>
  <read_first>
    - internal/controller/plan_gates_test.go (whole file — helpers to extend, Test 6a/6b/6c structure to mirror)
    - internal/controller/task_gates_test.go:36-93 (Test 7a — TaskReconciler drive + zero-Job assertion via tideproject.k8s/task-uid label; Test 7c precedent that executor dispatch works in this suite)
    - internal/controller/task_controller_test.go:59-110 + :134-174 (newTaskReconciler, reconcileN, makeTask signatures)
    - internal/controller/plan_controller.go:207-255 + :483-519 (the defective flow the specs must catch)
  </read_first>
  <behavior>
    - Test 6d (CR-01 / GATE-01 / GATE-04): a Plan under gates.plan=approve whose planner reports ChildCount=2 parks at AwaitingApproval at planner-Job completion; with 2 reporter-style Tasks materialized it STAYS parked across repeated Plan reconciles; driving the TaskReconciler on both Tasks creates ZERO executor Jobs and ZERO Wave CRs while parked (descent hold live); annotating approve-plan then reconciling flips the Plan to Running+ApprovedByUser with the annotation consumed, after which a Task reconcile creates an executor Job.
    - Test 6e (CR-02): a leaf Plan parked at AwaitingApproval with NO approve annotation remains at AwaitingApproval after 3 further reconciles (today the first re-reconcile stomps it to Running via the PlannerDispatched status patch).
  </behavior>
  <action>
    Extend the existing `Describe("PlanReconciler — gate-policy hook ...")` block in plan_gates_test.go with two new Describe blocks, reusing the file's fixture conventions (per D-01/D-02/D-04; regression vehicle = envtest controller suite per CONTEXT "Claude's Discretion" — self-contained, no kind cluster).

    First, parameterize the drive helper: change the `driveToJobCompletion` closure to accept a trailing `childCount int` parameter and set `EnvelopeOut{TaskUID: ..., ExitCode: 0, ChildCount: childCount}`; update the two existing call sites (Test 6a:144, 6b:183) and the recovery call (6c:290) to pass 0. Do not change any other helper.

    **Test 6d — "Test 6d — GATE-01/GATE-04 regression: gates.plan=approve with ChildCount>0 parks before executor dispatch (CR-01)".** Fixture names: gate-proj-pl4 / gate-ms-pl4 / gate-phase-pl4 / gate-plan-4; Tasks gate-plan4-task-1, gate-plan4-task-2. Steps:
    1. `makeProjectChain(..., Gates{Plan: gates.PolicyApprove})` (Task gate stays auto — the hold under test is the PARENT hold, not the task gate). Create the Plan with `PhaseRef: phaseName`, build the PlanReconciler exactly as Test 6a does, and drive to planner-Job completion with childCount=2.
    2. Assert (Eventually, 5s/100ms): `Status.Phase == "AwaitingApproval"` and ConditionWaveOrLevelPaused is True with Reason=ReasonAwaitingApproval. THIS IS THE PRIMARY RED ASSERTION — today the ChildCount requeue (observed 0 < 2) fires before the gate hook and the Plan never parks.
    3. Simulate reporter materialization: `makeTask("gate-plan4-task-1", planName, nil, projectName)` and same for task-2 (mirrors what reporter.stampParentRef produces: Spec.PlanRef + project label). waitForCacheSync both.
    4. Drive the PlanReconciler 3 more times via `reconcileWithRetry(r.Reconcile, planNN, 3)`; Get the Plan after; assert `Status.Phase` is STILL "AwaitingApproval" (the tasks-exist exit must not route a parked Plan to the wave path).
    5. Drive `reconcileN(newTaskReconciler(envReader), taskNN, 3)` for BOTH tasks. Assert: each Task's `Status.Phase` is "" (held with no status write — Pitfall 5), no Job in namespace "default" carries label `tideproject.k8s/task-uid` equal to either `getTaskUID(...)`, the ONLY Job whose name has prefix "tide-task-" is none (zero executor Jobs), and `k8sClient.List` of `tideprojectv1alpha1.WaveList` in "default" returns zero items owned by/naming this plan (wave path never ran). SECOND RED ASSERTION — today executors dispatch unreviewed.
    6. Approve: MergeFrom-patch the Plan adding annotation `gates.AnnotationApprovePrefix+"plan" = "true"` (copy Test 6b:193-200). Drive `reconcileWithRetry(r.Reconcile, planNN, 5)`. Assert (Eventually): `Status.Phase == "Running"`, annotation absent, ConditionWaveOrLevelPaused has Status=ConditionFalse and Reason=ReasonApprovedByUser.
    7. Drive `reconcileN(taskReconciler, task1NN, 5)`. Assert (Eventually): a Job exists with label `tideproject.k8s/task-uid == string(getTaskUID("gate-plan4-task-1"))` — the hold lifted and executor dispatch proceeds.
    Cleanup in AfterEach: cleanupTask both tasks, then the existing `cleanup(...)` plus delete any Wave CRs listed in "default".

    **Test 6e — "Test 6e — CR-02 regression: parked leaf Plan stays parked (no Running stomp)".** Fixture names: gate-proj-pl5 / gate-ms-pl5 / gate-phase-pl5 / gate-plan-5. Steps: create chain with Gates{Plan: gates.PolicyApprove}, create Plan, drive to completion with childCount=0, Eventually-assert AwaitingApproval (passes today, same as 6a). Then loop 3 times: `reconcileWithRetry(r.Reconcile, planNN, 1)` followed by a Get, asserting `Status.Phase == "AwaitingApproval"` after EACH iteration. RED today: the first re-reconcile falls through to the dispatch body and patches Phase="Running" with no annotation consumed.

    Run the suite and CONFIRM both new specs FAIL for the stated reasons (gate test discipline: a RED spec that fails for a fixture error, not the defect, is not a valid RED). Commit: `test(12-05): add failing regression specs for plan gate bypass (CR-01/CR-02)`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make test 2>&1 | tee /tmp/12-05-red.log; echo "MAKE_EXIT=$?"; grep -E 'Test 6d|Test 6e' /tmp/12-05-red.log | head -20</automated>
  </verify>
  <acceptance_criteria>
    - plan_gates_test.go contains Describe blocks "Test 6d" and "Test 6e": `grep -c 'Test 6[de]' internal/controller/plan_gates_test.go` returns >= 2
    - `driveToJobCompletion` carries a childCount parameter: `grep -nE 'driveToJobCompletion := func\(.*childCount int' internal/controller/plan_gates_test.go` returns 1 line
    - RED run: `make test` exits non-zero AND /tmp/12-05-red.log shows Test 6d failing on the AwaitingApproval (or zero-executor-Jobs) assertion and Test 6e failing on the stays-parked assertion — failure messages reference the gate assertions, not fixture/compile errors
    - No production file modified in this task: `git diff --name-only HEAD~1` (after commit) lists only internal/controller/plan_gates_test.go
    - RED commit exists: `git log --oneline -1` matches `test(12-05):`
  </acceptance_criteria>
  <done>Both regression specs exist, compile, and fail by reproducing the CR-01 gate bypass and CR-02 oscillation symptoms; RED commit on the branch.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — reposition the plan gate hook and add the AwaitingApproval early-return (milestone parity)</name>
  <files>internal/controller/plan_controller.go</files>
  <read_first>
    - internal/controller/plan_controller.go:114-190 (Reconcile routing: dispatched=true suppresses reconcileWaveMaterialization), :207-380 (reconcilePlannerDispatch), :394-556 (handlePlannerJobCompletion)
    - internal/controller/milestone_controller.go:200-263 (Step 1a AwaitingApproval early-return — the analog to transplant) and :440-553 (reject-first ordering + gate hook with alreadyApproved guard — the analog for the hook move)
    - internal/controller/plan_gates_test.go (the RED specs from Task 1 — the contract)
  </read_first>
  <action>
    Three edits to plan_controller.go, each mirroring a verified-clean milestone shape (D-04 approved-state, D-03 children-gated succession, D-02 dispatch hold). Implement per D-01..D-04 from 12-CONTEXT.md; do NOT touch phase_controller.go, dispatch_helpers.go, or charts/.

    **Edit 1 — AwaitingApproval early-return in reconcilePlannerDispatch (closes CR-02 and the wave-path half of CR-01).** Insert a new branch at the VERY TOP of `reconcilePlannerDispatch`, BEFORE the tasks-exist `r.List` at :210 — this placement is the one deliberate divergence from the milestone analog (where Step 1a sits after the terminal short-circuit) and the comment must say why: a parked Plan with reporter-materialized Tasks would otherwise take the tasks-exist exit to `dispatched=false` and the wave path would dispatch executors while parked. Branch logic, adapted from milestone_controller.go:216-243 to this function's `(ctrl.Result, bool, error)` signature:
    - `if plan.Status.Phase == "AwaitingApproval"`: if `gates.CheckApprove(plan, "plan")` is false → `return ctrl.Result{}, true, nil` (hold; dispatched=true so reconcileWaveMaterialization never runs while parked).
    - If the annotation is present: `gates.ConsumeApprove(plan, "plan")` + MergeFrom metadata Patch (same two-step order as the milestone — keep parity, do not reorder; WR-02 is explicitly out of gap scope), then MergeFrom status Patch with `plan.Status.Phase = "Running"` and `meta.SetStatusCondition` {Type: ConditionWaveOrLevelPaused, Status: ConditionFalse, Reason: ReasonApprovedByUser, Message: "Plan approved; Tasks will dispatch"}, then `return ctrl.Result{Requeue: true}, true, nil`.

    **Edit 2 — hoist the reject short-circuit in handlePlannerJobCompletion (milestone parity; WR-10's plan half, trivially adjacent since the block is being moved anyway).** Move `if project != nil && gates.CheckRejected(project) { return r.patchPlanRejected(...) }` from its current position (:503-505) to immediately after the `project := r.resolveProjectForPlan(...)` / projectUID block at the top of the function (before the EnvReader read), mirroring milestone_controller.go:442-449 and its "operator stop should always halt" comment. Scope note: leave phase_controller.go's WR-10 instance alone — it is not in gap scope.

    **Edit 3 — move the gate-policy hook BEFORE the ChildCount requeue (closes the hook-unreachable half of CR-01).** Relocate the `policy := gates.EvaluatePolicy(project.Spec.Gates, "plan")` block from :506-519 to between the ValidationState=Validated stamp (:477-481) and the `expected := out.ChildCount` requeue gate (:490-498), and upgrade it to the full milestone shape (milestone_controller.go:510-553):
    - alreadyApproved guard: `meta.FindStatusCondition(plan.Status.Conditions, ConditionWaveOrLevelPaused)` with `Status == metav1.ConditionFalse && (Reason == ReasonApprovedByUser || Reason == ReasonResumedByUser)` → skip the park entirely (prevents re-parking after the Edit-1 branch approved the level — without this guard the consumed annotation re-parks on the next pass through this function).
    - Not alreadyApproved and `!gates.CheckApprove(plan, "plan")` → `return r.patchPlanAwaitingApproval(ctx, plan, policy)`. Position note for the comment: the reporter Job was already spawned above, so children keep materializing while parked — D-02's "materialize children, hold dispatch", and ValidationState=Validated is already stamped so the wave path is armed the moment approval lands.
    - Annotation present at the hook → consume + MergeFrom metadata Patch + status Patch Running+ApprovedByUser (copy milestone :531-548), then fall through to the ChildCount gate.
    Delete the old hook block at the post-requeue position. The downstream tail (maybeTriggerBoundaryPush, clear-Running-to-"") is unchanged.

    Behavior check before running tests — trace the three paths against the new code: (a) leaf approve flow (Test 6a/6b): completion → hook parks; approve → Edit-1 branch consumes → Running → requeue → Running branch → handler → alreadyApproved → expected==0 → clear Running. (b) ChildCount>0 flow (Test 6d): completion → hook parks BEFORE the requeue; Tasks materialize; every reconcile hits the Edit-1 hold (dispatched=true) → no wave path, Task hold live; approve → Running → tasks-exist exit → wave path dispatches. (c) parked-leaf stability (Test 6e): Edit-1 hold returns before the dispatch body — no Running stomp.

    Run the full suite. All Phase-12 specs must stay green (milestone/phase gates tests, boundary_push_test, envtest gates_test) — the wave-boundary failure semantics and ChildCount-gated succession contracts must be untouched. Commit: `fix(12-05): park Plan at approve gate before ChildCount wait; hold wave path while parked (CR-01/CR-02)`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make test 2>&1 | tee /tmp/12-05-clean-run.log; echo "MAKE_EXIT=$?"; grep -cE '^--- FAIL|^FAIL\s' /tmp/12-05-clean-run.log</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'AwaitingApproval' internal/controller/plan_controller.go` shows a branch inside reconcilePlannerDispatch positioned before the tasks-exist List (line number of the branch < line number of `taskPlanRefIndexKey` List call in that function)
    - Source: in handlePlannerJobCompletion, the `gates.EvaluatePolicy(project.Spec.Gates, "plan")` call appears BEFORE `expected := out.ChildCount` (grep -n both, compare line numbers), and `gates.CheckRejected(project)` appears before the `r.EnvReader.ReadOut` call
    - Source: alreadyApproved guard present: `grep -n 'ReasonApprovedByUser' internal/controller/plan_controller.go` returns >= 2 sites (Edit-1 consume branch + Edit-3 guard/consume)
    - Test: `make test` MAKE_EXIT=0 AND `grep -cE '^--- FAIL|^FAIL\s' /tmp/12-05-clean-run.log` returns 0 (per CLAUDE.md: read MAKE_EXIT and FAIL-grep, not just the Ginkgo summary)
    - Test: /tmp/12-05-clean-run.log shows Test 6a, 6b, 6c, 6d, 6e all passing (no skipped/pending among them)
    - Behavior: Test 6d's zero-executor-Jobs-while-parked and executor-Job-after-approval assertions both pass — checkParentApproval(kind=Plan) is live, not dead code
    - GREEN commit exists touching only internal/controller/plan_controller.go: `git log --oneline -1` matches `fix(12-05):` and `git show --name-only --format= HEAD` lists exactly that file
  </acceptance_criteria>
  <done>gates.plan=approve parks every Plan (leaf and ChildCount>0) at AwaitingApproval before executor dispatch, parked Plans never self-unpark, approval returns Running+ApprovedByUser with children-gated succession intact, and the full unit tier is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator gate config → reconciler dispatch | `Project.Spec.Gates.Plan=approve` is the operator's authorization boundary: no executor Job (LLM spend, repo write access) may be created across it without an explicit approve annotation |
| approve annotation → status transition | The one-shot `tideproject.k8s/approve-plan` annotation is the only token that lifts a park; consumption must be recorded (ApprovedByUser) or the gate replays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12gc-01 | Elevation of privilege | plan_controller.go reconcile routing (CR-01) | mitigate | Gate hook repositioned before the ChildCount requeue + AwaitingApproval early-return before the tasks-exist exit returning dispatched=true — no reconcile path reaches wave materialization while parked; Test 6d asserts zero executor Jobs while parked |
| T-12gc-02 | Tampering (state integrity) | reconcilePlannerDispatch Running stomp (CR-02) | mitigate | AwaitingApproval early-return holds before the dispatch body; Test 6e asserts the park survives repeated reconciles with no annotation consumed |
| T-12gc-03 | Repudiation | approve consume without record | mitigate | Both consume sites write the permanent ApprovedByUser condition (D-04), and the alreadyApproved guard prevents a re-park from erasing the approval record |
| T-12gc-04 | Denial of service | parent NotFound fail-open in checkParentApproval (WR-01) | accept | Out of gap scope (verifier classified WARNING, not gap); fail-open window is informer-lag-bounded; tracked in docs/audit backlog |
| T-12gc-SC | Tampering | package installs | accept | No npm/pip/cargo/go-module installs in this plan — go.mod untouched; no legitimacy gate needed |
</threat_model>

<verification>
- `make test` exits 0 with zero `^--- FAIL|^FAIL\s` lines in /tmp/12-05-clean-run.log (CLAUDE.md exit discipline)
- Gap 1 closed (GATE-01 Plan level): Test 6d proves a ChildCount>0 Plan under gates.plan=approve parks at AwaitingApproval, and approval yields Running+ApprovedByUser — never a routed-around hook
- Gap 2 closed (GATE-04 Plan→Task): Test 6d proves zero executor Jobs and zero Wave CRs while parked, and that the task_controller.go:326 checkParentApproval(kind=Plan) hold engages against a really-parked Plan
- CR-02 closed: Test 6e proves no AwaitingApproval→Running oscillation
- No regressions: milestone_gates_test, phase_gates_test, task_gates_test, boundary_push_test, envtest gates_test all green (wave-boundary failure semantics and ChildCount-gated succession untouched)
</verification>

<success_criteria>
- Both 12-VERIFICATION.md `gaps` entries have their `missing` items delivered: gate hook moved before the ChildCount requeue, wave path honors AwaitingApproval, AwaitingApproval early-return added, ChildCount>0 regression spec exists and passes
- Two commits on main: `test(12-05):` (RED, test file only) then `fix(12-05):` (GREEN, controller only)
- Re-verification can flip GATE-01 and GATE-04 to VERIFIED at the Plan level using the new specs as evidence
</success_criteria>

<output>
Create `.planning/phases/12-gate-semantics-reject-resume/12-05-SUMMARY.md` when done.
</output>
