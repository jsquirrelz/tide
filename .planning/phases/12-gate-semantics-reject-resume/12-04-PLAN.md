---
phase: 12-gate-semantics-reject-resume
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-03]
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/controller/milestone_gates_test.go
  - internal/controller/phase_gates_test.go
  - internal/controller/plan_gates_test.go
  - internal/controller/task_gates_test.go
  - test/integration/envtest/gates_test.go
autonomous: true
requirements: [RESUME-01, GATE-03]
must_haves:
  truths:
    - "tide reject parks levels with a RejectedByUser condition — NO level is ever written Status.Phase=Failed by the reject path (run-1 finding-9a regression)"
    - "After the reject annotation is cleared (resume), parked levels re-enter the normal dispatch/succession path on the next reconcile — no manual status surgery needed"
    - "A genuinely Failed Plan whose status is reset per the retry-failed recipe (phase='' + conditions cleared) re-dispatches its planner Job on the next reconcile (RESUME-01 reconciler half)"
    - "Failed remains reserved for real failures — patch*Failed helpers still exist and still fire on planner-Job failure classification"
    - "Reject lets in-flight Jobs drain (no Job deletion) — no state loss, per the resolved discretion call"
  artifacts:
    - path: "internal/controller/milestone_controller.go"
      provides: "patchMilestoneRejected park helper replacing the reject→Failed cascade at handleJobCompletion top"
      contains: "patchMilestoneRejected"
    - path: "internal/controller/phase_controller.go"
      provides: "patchPhaseRejected + dispatch-entry reject hold"
      contains: "patchPhaseRejected"
    - path: "internal/controller/plan_controller.go"
      provides: "patchPlanRejected replacing patchPlanFailed at the :478 reject site"
      contains: "patchPlanRejected"
    - path: "internal/controller/task_controller.go"
      provides: "patchTaskRejected replacing patchTaskFailed at the gateChecks reject short-circuit"
      contains: "patchTaskRejected"
    - path: "test/integration/envtest/gates_test.go"
      provides: "Updated TestRejectHalts (park not fail) + RESUME-01 retry-failed re-dispatch spec"
  key_links:
    - from: "the four CheckRejected sites (milestone :411, phase :378, plan :478, task :313)"
      to: "patch*Rejected park helpers"
      via: "call-site replacement — patch*Failed no longer reachable from reject"
      pattern: "patch(Milestone|Phase|Plan|Task)Rejected"
    - from: "status reset (phase='' + conditions cleared)"
      to: "planner re-dispatch"
      via: "terminal short-circuit gates only on Succeeded||Failed — empty phase re-enters dispatch"
      pattern: 'Status.Phase == "Succeeded" \|\| .*"Failed"'
---

<objective>
Make reject recoverable (D-05): `tide reject` parks every level with a `RejectedByUser` condition instead of fail-marking it — today's `patchPlanFailed` cascade wedges children behind the `Succeeded || Failed` terminal short-circuit, and `tide resume` (annotation-only) cannot lift them. Also prove the RESUME-01 reconciler half: a status-reset Failed level re-dispatches.

Purpose: Run-1 finding 9a — reject fail-marked children; recovery required raw kubectl status surgery. D-05 makes the docs' "preserves all state" promise true rather than reworded away. `Failed` is reserved for real failures.
Output: Four patch*Rejected park helpers, call-site replacements at all four CheckRejected sites, dispatch-entry reject holds for milestone/phase/plan, updated reject tests, RESUME-01 envtest regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-gate-semantics-reject-resume/12-CONTEXT.md
@.planning/phases/12-gate-semantics-reject-resume/12-RESEARCH.md
@.planning/phases/12-gate-semantics-reject-resume/12-PATTERNS.md
@.planning/phases/12-gate-semantics-reject-resume/12-01-SUMMARY.md
@.planning/phases/12-gate-semantics-reject-resume/12-03-SUMMARY.md
</context>

<interfaces>
<!-- Verified at HEAD 2026-06-11 (pre-12-01/12-03; line numbers will have shifted — re-locate by grep 'CheckRejected') -->

The four reject sites (all currently fail-mark — THE BUG):
```go
// milestone_controller.go:411 (top of handleJobCompletion, before envelope read)
if project != nil && gates.CheckRejected(project) {
    return r.patchMilestoneFailed(ctx, ms, tideprojectv1alpha1.ReasonRejectedByUser, gates.RejectedReason(project))
}
// phase_controller.go:378 — same shape, patchPhaseFailed
// plan_controller.go:478  — same shape, patchPlanFailed (inside handlePlannerJobCompletion)
// task_controller.go:313  — gateChecks: patchTaskFailed wrapped in taskGateResult{shouldHalt: true}
```

Park-helper shape to copy — patchMilestoneAwaitingApproval (milestone_controller.go:601–621): MergeFrom(DeepCopy) → set condition via meta.SetStatusCondition → Status().Patch. For the Rejected variants: DO NOT set Status.Phase (leave it untouched); condition Type=ConditionWaveOrLevelPaused, Status=metav1.ConditionTrue, Reason=ReasonRejectedByUser, Message="Rejected: <gates.RejectedReason(project)>".

Why park works: the terminal short-circuit in every reconciler gates on `Succeeded || Failed` only. A parked level (phase empty or Running) re-enters the normal path once `gates.CheckRejected(project)` flips false after resume clears the annotation.

Existing reject tests that assert the OLD Failed behavior (must be updated):
- internal/controller/milestone_gates_test.go ~:198–253 "Test 3 — reject annotation on Project: short-circuits to Failed/RejectedByUser"
- internal/controller/phase_gates_test.go ~:198–251 "Test 5c — reject annotation on Project: short-circuits Phase to Failed"
- internal/controller/plan_gates_test.go ~:265 (Reason assertion inside a reject spec)
- internal/controller/task_gates_test.go ~:136 (same)
- test/integration/envtest/gates_test.go :163–229 "TestRejectHalts" — "Milestone reaches Status.Phase=Failed with Reason=RejectedByUser"

gates primitives: `gates.CheckRejected(project) bool`, `gates.RejectedReason(project) string`, `gates.ConsumeReject(project) map[string]string` (resume's clear).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject parks — milestone + phase (D-05) with updated gate tests</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/milestone_gates_test.go, internal/controller/phase_gates_test.go</files>
  <read_first>
    - internal/controller/milestone_controller.go (handleJobCompletion top — the CheckRejected site; patchMilestoneAwaitingApproval as the helper shape; reconcilePlannerDispatch dispatch tail as left by 12-01)
    - internal/controller/phase_controller.go (handleJobCompletion CheckRejected site; dispatch path including 12-03's parent-approval hold seam)
    - internal/controller/milestone_gates_test.go (Test 3 reject spec ~:198–253) and internal/controller/phase_gates_test.go (Test 5c ~:198–251)
    - .planning/phases/12-gate-semantics-reject-resume/12-PATTERNS.md (sections on milestone/phase reject-park replacement)
  </read_first>
  <behavior>
    - Updated Test 3 (milestone): with reject annotation on the Project, driving the reconciler leaves the Milestone NOT Failed (Status.Phase keeps its prior value), with ConditionWaveOrLevelPaused{Status: True, Reason: "RejectedByUser", Message containing the operator reason}; then clearing the reject annotation (gates.ConsumeReject + patch, simulating tide resume) and re-driving lets the Milestone proceed (condition no longer blocks; succession continues)
    - Updated Test 5c (phase): same park-not-fail assertions one level down
    - NEW dispatch-hold assertion: a Pending (empty-phase) Phase under a rejected Project creates no planner Job
  </behavior>
  <action>
    1. milestone_controller.go: add `patchMilestoneRejected(ctx context.Context, ms *tideprojectv1alpha1.Milestone, reason string) (ctrl.Result, error)` copying the patchMilestoneAwaitingApproval shape but WITHOUT touching Status.Phase — condition only: ConditionWaveOrLevelPaused/ConditionTrue/ReasonRejectedByUser/Message `fmt.Sprintf("Rejected: %s", reason)`; return `ctrl.Result{RequeueAfter: 5 * time.Second}` so the park polls for the resume (the reconciler has no watch on Project annotations). Replace the handleJobCompletion CheckRejected call site: `return r.patchMilestoneRejected(ctx, ms, gates.RejectedReason(project))`. Keep patchMilestoneFailed itself — it still serves genuine planner-failure classification.
    2. milestone_controller.go dispatch path: before planner Job creation (next to where the dispatch decision is made in reconcilePlannerDispatch, after the status branches), resolve the Project and if `gates.CheckRejected(project)` → `return r.patchMilestoneRejected(ctx, ms, gates.RejectedReason(project))` — a rejected Project must halt NEW dispatch, not only post-completion advancement (D-05 "children pause where they are"). Reuse the existing project resolution if one is already in scope at that point; otherwise resolve via ms.Spec.ProjectRef.
    3. phase_controller.go: same two changes — `patchPhaseRejected` helper (copy patchPhaseAwaitingApproval :507–527 shape, no Status.Phase write), replace the handleJobCompletion site (:378), and add the dispatch-entry reject hold adjacent to 12-03's parent-approval hold (after it, before PlannerPool.Acquire), reusing the resolveProject call the dispatch body already makes (hoist it above the pool acquire if needed).
    4. Decision (resolved discretion): reject lets in-flight Jobs drain — no Job deletion logic. State a one-line comment at each park helper.
    5. Update the two existing reject specs FIRST (RED) per the behavior block, then implement. The resume half of the updated specs clears the annotation directly via gates.ConsumeReject + metadata patch (CLI seam is unexported from cmd/tide).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -timeout 600s 2>&1 | tail -5 && grep -n 'patchMilestoneFailed\|patchPhaseFailed' internal/controller/milestone_controller.go internal/controller/phase_controller.go | grep -i reject || echo NO_REJECT_FAILMARK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func (r \*MilestoneReconciler) patchMilestoneRejected' internal/controller/milestone_controller.go` returns 1; same for patchPhaseRejected in phase_controller.go
    - No CheckRejected block in milestone/phase controllers calls a patch*Failed function (source assertion: the grep pipeline in <automated> prints NO_REJECT_FAILMARK)
    - Updated Test 3 and Test 5c assert Status.Phase != "Failed" under reject AND recovery after annotation clear
    - patchMilestoneFailed/patchPhaseFailed still exist and are still called from genuine failure-classification paths (grep shows remaining non-reject call sites)
    - `go test ./internal/controller/... -timeout 600s` exits 0
  </acceptance_criteria>
  <done>Milestone and Phase reject paths park with RejectedByUser conditions, halt new dispatch, never write Failed, and recover on annotation clear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject parks — plan + task (D-05) with updated gate tests</name>
  <files>internal/controller/plan_controller.go, internal/controller/task_controller.go, internal/controller/plan_gates_test.go, internal/controller/task_gates_test.go</files>
  <read_first>
    - internal/controller/plan_controller.go (handlePlannerJobCompletion CheckRejected site ~:478; patchPlanAwaitingApproval :582–600 as helper shape; dispatch path with 12-03's hold seam; Failed early-exit at :222)
    - internal/controller/task_controller.go (gateChecks reject short-circuit ~:313; patchTaskAwaitingApproval :675–688 as helper shape)
    - internal/controller/plan_gates_test.go (~:265 reject spec) and internal/controller/task_gates_test.go (~:136 reject spec)
  </read_first>
  <behavior>
    - Updated plan reject spec: rejected Project → Plan parked with ConditionWaveOrLevelPaused{True, RejectedByUser}, Status.Phase != "Failed"; after annotation clear + re-drive, the Plan proceeds (planner completion path resumes)
    - Updated task reject spec: rejected Project → Task parked (condition, not Failed); gateChecks returns shouldHalt
    - NEW: a Pending Plan under a rejected Project creates no planner Job (dispatch-entry hold)
  </behavior>
  <action>
    1. plan_controller.go: add `patchPlanRejected` (copy patchPlanAwaitingApproval :582–600 shape; NO Status.Phase write; ConditionWaveOrLevelPaused/True/ReasonRejectedByUser/"Rejected: <reason>"; return RequeueAfter 5s). Replace the :478 site — this removes the patchPlanFailed reject cascade that run-1 finding 9a hit. Add the dispatch-entry reject hold before PlannerPool.Acquire (adjacent to 12-03's parent hold), resolving the project as the dispatch body already does.
    2. task_controller.go: add `patchTaskRejected` (copy patchTaskAwaitingApproval :675–688 shape; same condition contract). Replace the gateChecks site: `result, err := r.patchTaskRejected(ctx, task, gates.RejectedReason(project)); return taskGateResult{shouldHalt: true, result: result}, err`. The task reject check already fires pre-dispatch — no additional hold needed.
    3. Keep patchPlanFailed/patchTaskFailed for genuine failures (planner-Job failure classification, executor failure) — only the reject call sites change.
    4. Update the two existing reject specs FIRST (RED), then implement. Each updated spec asserts both the park condition and `Status.Phase != "Failed"`, then recovery after annotation clear.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -timeout 600s 2>&1 | tail -5 && grep -n 'CheckRejected' internal/controller/plan_controller.go internal/controller/task_controller.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func (r \*PlanReconciler) patchPlanRejected' internal/controller/plan_controller.go` returns 1; same for patchTaskRejected in task_controller.go
    - The CheckRejected blocks in plan/task controllers call patch*Rejected, not patch*Failed (source assertion)
    - Updated specs assert Status.Phase != "Failed" under reject and recovery after annotation clear
    - `go test ./internal/controller/... -timeout 600s` exits 0
  </acceptance_criteria>
  <done>Plan and Task reject paths park instead of fail-marking; the finding-9a wedge (Failed early-exit after reject) is structurally impossible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: envtest regressions — TestRejectHalts park semantics + RESUME-01 retry-failed re-dispatch</name>
  <files>test/integration/envtest/gates_test.go</files>
  <read_first>
    - test/integration/envtest/gates_test.go (full file — TestRejectHalts :163–229 being updated; suite helpers drivePlanReconcile-style loops :349–430; TestGateApproveFlow as updated by 12-01)
    - .planning/phases/12-gate-semantics-reject-resume/12-RESEARCH.md (section "RESUME-01 Root Cause" — the kubectl recipe being replicated, and Validation Architecture test map)
  </read_first>
  <behavior>
    - TestRejectHalts (UPDATE): reject annotation mid-run → Milestone gets ConditionWaveOrLevelPaused{True, RejectedByUser, Message contains operator reason} and Status.Phase is NOT "Failed"; no further child Jobs dispatch while rejected; clearing the annotation (simulated resume) lets the flow proceed to completion. Rename the It() text from "Milestone reaches Status.Phase=Failed..." to the park contract.
    - NEW spec "RESUME-01 — retry-failed status reset re-dispatches" (run-1 finding-9a/recipe regression): a Plan status-patched to Failed (simulating a genuinely failed planner, the run-1 wedge state) → drive PlanReconciler → no re-dispatch (terminal short-circuit holds, proving the wedge) → apply the retry-failed reset exactly as cmd/tide resume --retry-failed does (status subresource patch: Status.Phase="", Status.Conditions=nil, then SetStatusCondition ResumedByUser/ConditionFalse) → drive PlanReconciler → planner Job `tide-plan-<uid>-1` is created (re-dispatch) and the ResumedByUser condition is present. This proves GATE-03's "never wedged" contract end-to-end with 12-02's CLI unit tests covering the verb itself.
  </behavior>
  <action>
    Update TestRejectHalts and add the RESUME-01 spec per the behavior block. Implementation notes:
    1. The reset in the new spec mirrors resumeRun's --retry-failed logic verbatim (it cannot call the unexported CLI seam from this package) — keep a comment cross-referencing cmd/tide/resume.go so drift is caught in review.
    2. Drive loops follow the existing suite pattern (reconcile + Eventually with 5s/50ms cadence); the re-dispatch assertion is an Eventually on Get of the planner Job name.
    3. Keep the `envtest` Ginkgo label so the spec runs under make test-int-fast Layer A.
  </action>
  <verify>
    <automated>make test-int-fast 2>&1 | tee /tmp/12-04-int.log | tail -10; echo "MAKE_EXIT=$?"; grep -nE '^--- FAIL|^FAIL ' /tmp/12-04-int.log || echo NO_FAIL_LINES</automated>
  </verify>
  <acceptance_criteria>
    - Updated TestRejectHalts passes asserting park-not-fail and post-resume recovery
    - New RESUME-01 spec passes: Failed Plan does not re-dispatch until reset; after the recipe reset, the planner Job exists and ResumedByUser condition is present
    - `make test-int-fast` MAKE_EXIT=0 AND zero `^--- FAIL|^FAIL ` lines in /tmp/12-04-int.log
  </acceptance_criteria>
  <done>Reject-park and retry-failed recovery are locked in at the envtest layer; the run-1 reject wedge and its kubectl recipe are reproduced as automated regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reject annotation → reconciler halt | operator stop signal consumed from Project metadata by all four reconcilers |
| status reset → re-dispatch (spend) | clearing a Failed status re-triggers planner/executor Job creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Tampering (state destruction) | reject path | mitigate | D-05: reject writes only a condition — no Status.Phase=Failed, no state destroyed; in-flight Jobs drain; recovery is a metadata patch (resume), making reject safe to use as a routine pause |
| T-12-12 | Denial of service | reject park requeue | mitigate | parks return RequeueAfter 5s (bounded polling), and the reject check fires before any Job creation so a rejected Project spends nothing new |
| T-12-13 | Elevation of privilege (spend) | status-reset → re-dispatch | mitigate | reconciler re-dispatches only when Status.Phase is cleared via the status subresource — requires status-patch RBAC (same as kubectl recipe); reset path stamps ResumedByUser for auditability (pairs with T-12-04 in plan 12-02) |
| T-12-14 | Spoofing/Replay | reject annotation handshake | accept | gates.ConsumeReject one-shot clearing on resume preserves the existing T-04-G2-style contract; re-applying reject re-parks, which is the intended operator capability |
| T-12-SC | Tampering | package installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `go test ./internal/controller/... -timeout 600s` green
- `make test-int-fast` MAKE_EXIT=0, zero FAIL lines
- Phase gate (before /gsd:verify-work): full `make test-int` — read MAKE_EXIT AND `grep -nE '^--- FAIL|^FAIL '` per CLAUDE.md; one heavy run at a time on the 7.65 GiB VM
- Manual (optional, live kind cluster `tide` — NEVER delete the cluster or run-1 CRs): the run-1 fail-marked CRs lack the project label (CUTS-01, Phase 15), so recover them with the documented kubectl recipe from gates.md, not the CLI verb; observe re-dispatch with the fixed controller image
</verification>

<success_criteria>
- ROADMAP Phase 12 success criterion 4 satisfied end-to-end: `tide reject` parks children without Failed writes; `tide resume` lifts the park; `tide resume --retry-failed` recovers a genuinely Failed level (status reset → re-dispatch → ResumedByUser), matching the run-1 kubectl recipe
- No CheckRejected site in any of the four reconcilers reaches a patch*Failed function
- All updated and new specs green at unit + envtest layers
</success_criteria>

<output>
Create `.planning/phases/12-gate-semantics-reject-resume/12-04-SUMMARY.md` when done
</output>
