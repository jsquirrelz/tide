---
phase: 19-template-reorder-token-minimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/subagent/common/templates/milestone_planner.tmpl
  - internal/subagent/common/templates/project_planner.tmpl
  - internal/eval/testdata/goldie/milestone_planner.golden
  - internal/eval/testdata/goldie/project_planner.golden
  - internal/eval/testdata/ratchets/milestone_planner.txt
  - internal/eval/testdata/ratchets/project_planner.txt
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04]
must_haves:
  truths:
    - "milestone_planner and project_planner render in canonical D-03 order (role → fixed instructions → SharedContext slot → volatile suffix → prompt)"
    - "The STABLE PREFIX (everything before the `SharedContext slot` marker) of each template contains ZERO {{.TaskUID}}, {{.Provider.*}}, `Level:`, or `Role:` tokens; all {{.TaskUID}} occurrences are concentrated in the volatile suffix AFTER the marker (the suffix legitimately has 2: the `TaskUID:` label line + the task-dir path-mapping line, per D-01)"
    - "No {{.Provider.*}}, {{.Level}}, or {{.Role}} interpolation remains anywhere in either template"
    - "Every surviving load-bearing line carries a {{/* WHY ... */ -}} annotation committed before any trim"
    - "go test ./internal/eval/ exits 0 (goldens + ratchets + protocol gate all green) at every commit boundary"
    - "Both ratchet integers are LOWER than their v1.0.1 baselines (milestone 2214, project 2474)"
  artifacts:
    - path: "internal/subagent/common/templates/milestone_planner.tmpl"
      provides: "Reordered + annotated + trimmed milestone planner prompt"
      contains: "SharedContext slot — populated in Phase 20"
    - path: "internal/subagent/common/templates/project_planner.tmpl"
      provides: "Reordered + annotated + trimmed project planner prompt"
      contains: "SharedContext slot — populated in Phase 20"
    - path: "internal/eval/testdata/goldie/milestone_planner.golden"
      provides: "Regenerated golden reflecting reordered structure"
    - path: "internal/eval/testdata/goldie/project_planner.golden"
      provides: "Regenerated golden reflecting reordered structure"
    - path: "internal/eval/testdata/ratchets/milestone_planner.txt"
      provides: "Lowered frozen byte count"
    - path: "internal/eval/testdata/ratchets/project_planner.txt"
      provides: "Lowered frozen byte count"
  key_links:
    - from: "internal/subagent/common/templates/milestone_planner.tmpl"
      to: "internal/eval/testdata/goldie/milestone_planner.golden"
      via: "go test -update ./internal/eval/ -run TestGoldenRender_MilestonePlanner"
      pattern: "TestGoldenRender_MilestonePlanner"
    - from: "internal/subagent/common/templates/milestone_planner.tmpl"
      to: "internal/eval/testdata/ratchets/milestone_planner.txt"
      via: "manual byte-count update in same commit"
      pattern: "TestByteRatchet_MilestonePlanner"
---

<objective>
Reorder, annotate, and conservatively trim the `milestone_planner.tmpl` and `project_planner.tmpl` prompt templates into the canonical D-03 stable-prefix-first structure, keeping the Phase 18 eval harness green at every commit boundary.

Purpose: Move volatile per-dispatch metadata (TaskUID, Provider.*) out of the stable prefix so wave-sibling dispatches share an identical cache-eligible prefix (forward-looking for Phase 20), and cut token spend by trimming pure redundancy now (PROMPT-01/02/03/04).

Output: Two reordered+trimmed `.tmpl` files, two regenerated `.golden` files, two lowered ratchet `.txt` files. Per-template chains are strictly sequential (annotate → reorder → trim); the two templates are independent of each other.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md
@.planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md
@.planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md

<interfaces>
<!-- Eval harness contracts the executor drives — DO NOT explore the codebase, use these. -->

Per-template test function names (internal/eval/render_test.go):
  TestGoldenRender_MilestonePlanner   TestByteRatchet_MilestonePlanner
  TestGoldenRender_ProjectPlanner     TestByteRatchet_ProjectPlanner

Fixture (render_test.go baseEnvelope): TaskUID="eval-fixture-uid-000", Provider.Vendor="anthropic",
  Provider.Model="claude-sonnet-4-6", Params=nil. All path examples render with eval-fixture-uid-000.

Ratchet is STRICT equality (actual != frozen fails on BOTH grow and shrink). Golden fails on any byte diff.
EVERY commit that changes rendered bytes MUST update three files together: .tmpl + .golden + .txt.

Regenerate golden:  go test -update ./internal/eval/ -run TestGoldenRender_<Name>
Read new bytecount: go test ./internal/eval/ -run TestByteRatchet_<Name> -v 2>&1 | grep "byte count changed"
Update ratchet:     write the new integer into internal/eval/testdata/ratchets/<name>.txt
Per-commit gate:    go test ./internal/eval/   (fast, zero-network)

Current baselines (these DROP): milestone_planner=2214, project_planner=2474.

STABLE-PREFIX INVARIANT (the correctness check for the reorder — NOT a UID-count check):
  The volatile suffix legitimately contains MULTIPLE {{.TaskUID}} occurrences (the `TaskUID:` label
  line + the task-dir path-mapping line, per D-01). The invariant is that the STABLE PREFIX —
  everything BEFORE the `SharedContext slot` marker line — contains ZERO volatile tokens, and the
  suffix AFTER the marker contains all of them. Runnable check (exits 0 ⇔ zero TaskUID before marker):
    awk '/SharedContext slot/{f=1} !f && /\{\{\.TaskUID\}\}/{c++} END{exit c}' <template>.tmpl
  Plus: 0 `{{.TaskUID}}`/`{{.Provider`/`Level:`/`Role:` before the marker AND ≥1 `{{.TaskUID}}` after it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate → reorder → trim milestone_planner.tmpl (commits A, B, C+)</name>
  <files>internal/subagent/common/templates/milestone_planner.tmpl, internal/eval/testdata/goldie/milestone_planner.golden, internal/eval/testdata/ratchets/milestone_planner.txt</files>
  <read_first>
    - internal/subagent/common/templates/milestone_planner.tmpl (the file being edited — read full current content)
    - internal/eval/testdata/goldie/milestone_planner.golden (current rendered baseline)
    - internal/eval/testdata/ratchets/milestone_planner.txt (current frozen count = 2214)
    - internal/eval/render_test.go (goldenAssert + ratchetAssert + baseEnvelope fixture, lines 40-191)
    - .planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md (D-01..D-07 locked)
    - .planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md (§Go template whitespace mechanics, §Safe Sequencing, §Template Section Audit)
    - .planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md (§D-06 annotation, §D-07 slot, §volatile suffix shape, §commit protocol)
  </read_first>
  <behavior>
    - After commit A (annotation): go test ./internal/eval/ -run "TestGoldenRender_MilestonePlanner|TestByteRatchet_MilestonePlanner" exits 0. Annotations are zero-token (trim-right `{{/* WHY */ -}}`), so golden/ratchet ideally unchanged; if any whitespace shifts, golden+ratchet regenerated in the SAME commit.
    - After commit B (reorder): rendered output is role preamble → fixed instructions → `{{/* SharedContext slot — populated in Phase 20 (CACHE-02/03) */}}` → volatile suffix (TaskUID + task-dir mapping) → `Original prompt:` + `{{.Prompt}}`. STABLE-PREFIX INVARIANT holds: zero `{{.TaskUID}}`/`{{.Provider`/`Level:`/`Role:` BEFORE the `SharedContext slot` marker, and ≥1 `{{.TaskUID}}` AFTER it (the suffix legitimately has 2 — the `TaskUID:` label + the path-mapping line). grep for `.Provider`, `.Level`, `.Role` anywhere returns 0. golden+ratchet updated same commit; ratchet < 2214.
    - After each commit C+ (trim, one section each): protocol tests (TestDAGAcyclicity_*, TestDeclaredOutputPaths_Presence) stay green; ratchet decreases; golden regenerated each commit.
  </behavior>
  <action>
Execute the per-template safe sequence on `milestone_planner.tmpl` as a chain of commits; `go test ./internal/eval/` MUST be green at every commit boundary.

Commit A — annotation pass (PROMPT-03): For each load-bearing line (the spec-read directive, the HOW-TO-EMIT child-CRD shape, the "Markdown is the human-review surface; the JSON files are the machine contract" line, the children/ write-only directive), add an annotation comment on its OWN line immediately ABOVE the line, in the form `{{/* WHY: <one-clause justification> */ -}}` (trim-right ONLY — never `{{- ... -}}`, which would eat the preceding newline and merge lines per RESEARCH §whitespace). The trim-right consumes the comment line's own newline so rendered bytes are unchanged. After editing, run `go test ./internal/eval/ -run "TestGoldenRender_MilestonePlanner|TestByteRatchet_MilestonePlanner"`; if it reports a byte-count change, regenerate the golden (`go test -update ./internal/eval/ -run TestGoldenRender_MilestonePlanner`) and write the new integer into the ratchet `.txt` in this SAME commit. Commit message records that no content was removed.

Commit B — reorder (PROMPT-01/PROMPT-02, applying D-01/D-02/D-03/D-07): Remove the entire "Dispatch metadata for this run:" block (drops printed Level/Role/Provider.Vendor/Provider.Model per D-02 — these fields STAY on the EnvelopeIn struct, only the printed lines go). Rewrite the HOW-TO-EMIT path examples to be UID-free and abstract per D-01 ("write one JSON file per Phase into your children/ directory"). Append the volatile suffix in this exact order: a bare `{{/* SharedContext slot — populated in Phase 20 (CACHE-02/03) */}}` marker on its own line (NO trim markers per D-07 — this yields the section-separator blank line; `{{- ... -}}` collapses it, bare-with-trailing-source-blank double-blanks it), then `TaskUID: {{.TaskUID}}`, then a one-line task-dir path mapping `Your task dir: /workspace/envelopes/{{.TaskUID}}/  (children/  out.json  worktrees/...)`, then a blank line, then `Original prompt:` and `{{.Prompt}}`. Regenerate the golden with `go test -update ./internal/eval/ -run TestGoldenRender_MilestonePlanner`, read the new byte count via `go test ./internal/eval/ -run TestByteRatchet_MilestonePlanner -v`, write it into `internal/eval/testdata/ratchets/milestone_planner.txt`, all in this SAME commit. Verify the STABLE-PREFIX INVARIANT: `awk '/SharedContext slot/{f=1} !f && /\{\{\.TaskUID\}\}/{c++} END{exit c}' internal/subagent/common/templates/milestone_planner.tmpl` exits 0 (zero TaskUID in the prefix), and at least one `{{.TaskUID}}` appears after the marker.

Commits C+ — conservative trim (PROMPT-04, D-04), one section per commit: Compress the TIDE-paradigm preamble to its load-bearing core — KEEP the "Read the project spec at /workspace/repo.git/README.md ... it's load-bearing" directive verbatim (anti-pattern 3: never trim it); the acronym-expansion sentence may compress to one line. Dedupe any repeated "Markdown is the human-review surface ..." line only after its annotation confirms the semantic content survives elsewhere. Trim ONLY pure redundancy and formatting slack — every instruction keeps full semantic content; no aggressive imperative rewrite. Each trim is its own commit: edit → `go test -update ...` golden → update ratchet `.txt` DOWN → `go test ./internal/eval/` green. The commit message records WHY the removed text was safe to remove.
  </action>
  <verify>
    <automated>go test ./internal/eval/ -run "TestGoldenRender_MilestonePlanner|TestByteRatchet_MilestonePlanner|TestDAGAcyclicity_|TestDeclaredOutputPaths_Presence"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/eval/` exits 0.
    - STABLE PREFIX is clean: `awk '/SharedContext slot/{f=1} !f && /\{\{\.TaskUID\}\}/{c++} END{exit c}' internal/subagent/common/templates/milestone_planner.tmpl` exits 0 (zero {{.TaskUID}} before the marker). Equivalently, `sed '/SharedContext slot/,$d' internal/subagent/common/templates/milestone_planner.tmpl | grep -cE "\{\{\.TaskUID\}\}|\{\{\.Provider|^Level:|^Role:"` returns 0.
    - VOLATILE SUFFIX carries the UID: `sed -n '/SharedContext slot/,$p' internal/subagent/common/templates/milestone_planner.tmpl | grep -c "{{.TaskUID}}"` returns >= 1 (path mapping present; expected 2 — label + path-mapping line).
    - `grep -cE "\.Provider|\{\{\.Level\}\}|\{\{\.Role\}\}" internal/subagent/common/templates/milestone_planner.tmpl` returns 0 (no Provider/Level/Role interpolation anywhere).
    - `grep -c "SharedContext slot — populated in Phase 20" internal/subagent/common/templates/milestone_planner.tmpl` returns 1.
    - `grep -c "README.md" internal/subagent/common/templates/milestone_planner.tmpl` returns >= 1 (spec-read directive preserved).
    - The integer in `internal/eval/testdata/ratchets/milestone_planner.txt` is < 2214.
    - `grep -cE "\{\{/\* WHY" internal/subagent/common/templates/milestone_planner.tmpl` returns >= 2 (annotations present).
  </acceptance_criteria>
  <done>milestone_planner.tmpl is reordered to D-03 order, TaskUID/Provider/Level/Role removed from the stable prefix, load-bearing lines annotated, conservatively trimmed; golden + ratchet updated; eval package green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Annotate → reorder → trim project_planner.tmpl (commits A, B, C+)</name>
  <files>internal/subagent/common/templates/project_planner.tmpl, internal/eval/testdata/goldie/project_planner.golden, internal/eval/testdata/ratchets/project_planner.txt</files>
  <read_first>
    - internal/subagent/common/templates/project_planner.tmpl (the file being edited — read full current content; note "Original prompt (the project outcome):" terminator variant)
    - internal/eval/testdata/goldie/project_planner.golden (current rendered baseline)
    - internal/eval/testdata/ratchets/project_planner.txt (current frozen count = 2474)
    - internal/eval/render_test.go (goldenAssert + ratchetAssert + baseEnvelope fixture)
    - .planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md (D-01..D-07 locked)
    - .planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md (§Go template whitespace mechanics, §Safe Sequencing)
    - .planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md (§D-06 annotation, §D-07 slot, §volatile suffix shape)
  </read_first>
  <behavior>
    - After commit A (annotation): go test ./internal/eval/ -run "TestGoldenRender_ProjectPlanner|TestByteRatchet_ProjectPlanner" exits 0; golden/ratchet updated in same commit only if whitespace shifted.
    - After commit B (reorder): rendered output is role → fixed instructions → SharedContext slot → volatile suffix → terminator. The project_planner's terminator variant "Original prompt (the project outcome):" is PRESERVED (template-specific, not normalized away). STABLE-PREFIX INVARIANT holds: zero `{{.TaskUID}}`/`{{.Provider`/`Level:`/`Role:` before the `SharedContext slot` marker, ≥1 `{{.TaskUID}}` after it; `.Provider`/`.Level`/`.Role` gone everywhere. golden+ratchet updated same commit; ratchet < 2474.
    - After each commit C+ (trim): protocol tests green; ratchet decreases; golden regenerated each commit.
  </behavior>
  <action>
Apply the identical safe sequence as Task 1, on `project_planner.tmpl`. The skeleton matches milestone_planner with two template-specific differences to PRESERVE: (1) the prompt terminator is "Original prompt (the project outcome):" — keep that exact wording in the volatile suffix, do NOT normalize it to the generic "Original prompt:"; (2) the children/ emit examples produce phase-level child CRDs ("write one JSON file per Phase").

Commit A (PROMPT-03): annotate load-bearing lines with `{{/* WHY: ... */ -}}` (trim-right only) on the line above each. No content removed. Regenerate golden + ratchet in the SAME commit only if bytes shifted.

Commit B (PROMPT-01/02 via D-01/D-02/D-03/D-07): delete the "Dispatch metadata for this run:" block (drops printed Level/Role/Provider lines; `.Provider` stays on the struct). Make HOW-TO-EMIT paths UID-free/abstract per D-01. Append the volatile suffix in canonical order: bare `{{/* SharedContext slot — populated in Phase 20 (CACHE-02/03) */}}` (no trim markers, D-07), `TaskUID: {{.TaskUID}}`, the one-line task-dir mapping, blank line, `Original prompt (the project outcome):`, `{{.Prompt}}`. Regenerate golden (`go test -update ./internal/eval/ -run TestGoldenRender_ProjectPlanner`), read + write the new ratchet count, same commit. Verify the STABLE-PREFIX INVARIANT: `awk '/SharedContext slot/{f=1} !f && /\{\{\.TaskUID\}\}/{c++} END{exit c}' internal/subagent/common/templates/project_planner.tmpl` exits 0, and ≥1 `{{.TaskUID}}` appears after the marker.

Commits C+ (PROMPT-04 / D-04): conservative section-at-a-time trim — keep the README.md spec-read directive verbatim, compress only acronym/contextual slack, dedupe the machine-contract line only post-annotation. Each commit: edit → golden -update → ratchet down → `go test ./internal/eval/` green; commit message records removal rationale.
  </action>
  <verify>
    <automated>go test ./internal/eval/ -run "TestGoldenRender_ProjectPlanner|TestByteRatchet_ProjectPlanner|TestDAGAcyclicity_|TestDeclaredOutputPaths_Presence"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/eval/` exits 0.
    - STABLE PREFIX is clean: `awk '/SharedContext slot/{f=1} !f && /\{\{\.TaskUID\}\}/{c++} END{exit c}' internal/subagent/common/templates/project_planner.tmpl` exits 0. Equivalently, `sed '/SharedContext slot/,$d' internal/subagent/common/templates/project_planner.tmpl | grep -cE "\{\{\.TaskUID\}\}|\{\{\.Provider|^Level:|^Role:"` returns 0.
    - VOLATILE SUFFIX carries the UID: `sed -n '/SharedContext slot/,$p' internal/subagent/common/templates/project_planner.tmpl | grep -c "{{.TaskUID}}"` returns >= 1.
    - `grep -cE "\.Provider|\{\{\.Level\}\}|\{\{\.Role\}\}" internal/subagent/common/templates/project_planner.tmpl` returns 0.
    - `grep -c "SharedContext slot — populated in Phase 20" internal/subagent/common/templates/project_planner.tmpl` returns 1.
    - `grep -c "the project outcome" internal/subagent/common/templates/project_planner.tmpl` returns 1 (terminator variant preserved).
    - The integer in `internal/eval/testdata/ratchets/project_planner.txt` is < 2474.
    - `grep -cE "\{\{/\* WHY" internal/subagent/common/templates/project_planner.tmpl` returns >= 2.
  </acceptance_criteria>
  <done>project_planner.tmpl reordered to D-03 order with its terminator variant preserved, metadata removed from the stable prefix, annotated, conservatively trimmed; golden + ratchet updated; eval package green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered prompt → subagent LLM | Template text becomes the instruction set that governs a dispatched coding agent. The "instructions" are the only guardrail; there is no runtime sandbox on agent reasoning. |
| template change → eval gate | The Phase 18 deterministic gate (goldie + ratchet + protocol) is STRUCTURAL ONLY — it proves byte identity and child-CRD parseability, NOT that a trimmed instruction still steers the model correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering (instruction integrity) | Conservative trim of load-bearing lines in milestone/project planner | mitigate | D-04 conservative posture: trim only pure redundancy; PROMPT-03 annotation pass committed BEFORE any removal records each line's justification; per-section commit messages document removal rationale. Acceptance criteria assert the README.md spec-read directive survives. |
| T-19-02 | Tampering (silent guardrail weakening) | Structural-only eval gate cannot detect a semantically-weakened-but-still-parseable prompt | mitigate (residual: MEDIUM) | The deterministic gate catches structural breakage only. Residual semantic-regression risk is mitigated by the D-05 human-review-of-annotated-diff checkpoint (in plan 19-04) plus `make eval` token confirmation. NO LLM-judge this milestone (deferred EVAL-F1) — this residual is explicitly accepted at the phase level and flagged to the reviewer. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 19 — all tooling landed in Phase 18 (RESEARCH §Package Legitimacy Audit: zero external packages). No supply-chain surface this plan. |

No high-severity threats: this plan edits only `.tmpl` and `testdata/` files; no auth, input-handling, network, or crypto code. The realistic surface is instruction-integrity (T-19-01) and the structural-gate blind spot (T-19-02), both mitigated by the annotate-before-trim discipline and the downstream human-review checkpoint. ASVS does not apply (RESEARCH §Security Domain).
</threat_model>

<verification>
- `go test ./internal/eval/` exits 0 after the final commit of each template chain.
- Both ratchet integers are strictly below their v1.0.1 baselines (milestone < 2214, project < 2474).
- STABLE-PREFIX INVARIANT holds for each template: zero `{{.TaskUID}}`/`{{.Provider`/`Level:`/`Role:` before the `SharedContext slot` marker, and >= 1 `{{.TaskUID}}` after it; zero `.Provider`/`.Level`/`.Role` interpolation anywhere.
- Phase-wide `make test` is run in plan 19-04 (wave 2) as the merge gate.
</verification>

<success_criteria>
- milestone_planner.tmpl and project_planner.tmpl both render in canonical D-03 stable-prefix-first order.
- The stable prefix is free of volatile tokens; TaskUID and Provider.* live only in the volatile suffix (Provider printed lines dropped; struct field retained).
- Load-bearing lines annotated before trim; trims are conservative and per-section-committed.
- Goldens regenerated and ratchets lowered in the same commit as each byte change; eval package green throughout.
</success_criteria>

<output>
Create `.planning/phases/19-template-reorder-token-minimization/19-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
