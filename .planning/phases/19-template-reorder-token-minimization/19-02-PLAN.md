---
phase: 19-template-reorder-token-minimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/subagent/common/templates/phase_planner.tmpl
  - internal/subagent/common/templates/plan_planner.tmpl
  - internal/eval/testdata/goldie/phase_planner.golden
  - internal/eval/testdata/goldie/plan_planner.golden
  - internal/eval/testdata/ratchets/phase_planner.txt
  - internal/eval/testdata/ratchets/plan_planner.txt
autonomous: true
requirements: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04]
must_haves:
  truths:
    - "phase_planner and plan_planner render in canonical D-03 order (role → fixed instructions → SharedContext slot → volatile suffix → prompt)"
    - "{{.TaskUID}} appears exactly once in each of the two templates (volatile suffix only)"
    - "No {{.Provider.*}}, {{.Level}}, or {{.Role}} interpolation remains in either template"
    - "plan_planner's FILE-TOUCH RULE and JSON-escaping directives survive the trim intact (load-bearing machine contract)"
    - "Every surviving load-bearing line carries a {{/* WHY ... */ -}} annotation committed before any trim"
    - "go test ./internal/eval/ exits 0 at every commit boundary"
    - "Both ratchet integers are LOWER than their v1.0.1 baselines (phase 2271, plan 4281)"
  artifacts:
    - path: "internal/subagent/common/templates/phase_planner.tmpl"
      provides: "Reordered + annotated + trimmed phase planner prompt"
      contains: "SharedContext slot — populated in Phase 20"
    - path: "internal/subagent/common/templates/plan_planner.tmpl"
      provides: "Reordered + annotated + trimmed plan planner prompt (FILE-TOUCH RULE + JSON-escape directives preserved)"
      contains: "SharedContext slot — populated in Phase 20"
    - path: "internal/eval/testdata/goldie/phase_planner.golden"
      provides: "Regenerated golden"
    - path: "internal/eval/testdata/goldie/plan_planner.golden"
      provides: "Regenerated golden"
    - path: "internal/eval/testdata/ratchets/phase_planner.txt"
      provides: "Lowered frozen byte count"
    - path: "internal/eval/testdata/ratchets/plan_planner.txt"
      provides: "Lowered frozen byte count"
  key_links:
    - from: "internal/subagent/common/templates/plan_planner.tmpl"
      to: "internal/eval/testdata/goldie/plan_planner.golden"
      via: "go test -update ./internal/eval/ -run TestGoldenRender_PlanPlanner"
      pattern: "TestGoldenRender_PlanPlanner"
    - from: "internal/subagent/common/templates/phase_planner.tmpl"
      to: "internal/eval/testdata/ratchets/phase_planner.txt"
      via: "manual byte-count update in same commit"
      pattern: "TestByteRatchet_PhasePlanner"
---

<objective>
Reorder, annotate, and conservatively trim the `phase_planner.tmpl` and `plan_planner.tmpl` prompt templates into canonical D-03 stable-prefix-first structure, keeping the Phase 18 eval harness green at every commit boundary.

Purpose: Same as plan 19-01 — relocate volatile metadata to the suffix and cut redundant tokens (PROMPT-01/02/03/04). `plan_planner` is the largest template (4281 bytes) and carries the most load-bearing machine-contract text (FILE-TOUCH RULE, JSON escaping, full child-CRD shape) — extra care is required not to over-trim.

Output: Two reordered+trimmed `.tmpl` files, two regenerated `.golden` files, two lowered ratchet `.txt` files. Per-template chains are strictly sequential (annotate → reorder → trim); the two templates are independent of each other.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md
@.planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md
@.planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md

<interfaces>
<!-- Eval harness contracts — DO NOT explore the codebase, use these directly. -->

Per-template test function names (internal/eval/render_test.go):
  TestGoldenRender_PhasePlanner   TestByteRatchet_PhasePlanner
  TestGoldenRender_PlanPlanner    TestByteRatchet_PlanPlanner

Fixture (render_test.go baseEnvelope): TaskUID="eval-fixture-uid-000", Provider.Vendor="anthropic",
  Provider.Model="claude-sonnet-4-6", Params=nil.

Ratchet is STRICT equality (grow OR shrink fails). Golden fails on any byte diff. EVERY byte-changing
commit updates three files together: .tmpl + .golden + .txt.

Regenerate golden:  go test -update ./internal/eval/ -run TestGoldenRender_<Name>
Read new bytecount: go test ./internal/eval/ -run TestByteRatchet_<Name> -v 2>&1 | grep "byte count changed"
Update ratchet:     write the new integer into internal/eval/testdata/ratchets/<name>.txt
Per-commit gate:    go test ./internal/eval/

Current baselines (these DROP): phase_planner=2271, plan_planner=4281.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate → reorder → trim phase_planner.tmpl (commits A, B, C+)</name>
  <files>internal/subagent/common/templates/phase_planner.tmpl, internal/eval/testdata/goldie/phase_planner.golden, internal/eval/testdata/ratchets/phase_planner.txt</files>
  <read_first>
    - internal/subagent/common/templates/phase_planner.tmpl (the file being edited — read full current content; note "and the owning MILESTONE.md" variation in the preamble)
    - internal/eval/testdata/goldie/phase_planner.golden (current rendered baseline)
    - internal/eval/testdata/ratchets/phase_planner.txt (current frozen count = 2271)
    - internal/eval/render_test.go (goldenAssert + ratchetAssert + baseEnvelope fixture)
    - .planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md (D-01..D-07)
    - .planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md (§Go template whitespace mechanics, §Safe Sequencing)
    - .planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md (§D-06 annotation, §D-07 slot, §volatile suffix shape)
  </read_first>
  <behavior>
    - After commit A (annotation): go test ./internal/eval/ -run "TestGoldenRender_PhasePlanner|TestByteRatchet_PhasePlanner" exits 0; golden/ratchet updated same commit only if whitespace shifted.
    - After commit B (reorder): output is role → fixed instructions → SharedContext slot → volatile suffix → `Original prompt:` + prompt. The "and the owning MILESTONE.md" spec-read clause is PRESERVED. grep -c "{{.TaskUID}}" returns exactly 1; `.Provider`/`.Level`/`.Role` gone. golden+ratchet updated same commit; ratchet < 2271.
    - After each commit C+ (trim): protocol tests green; ratchet decreases; golden regenerated each commit.
  </behavior>
  <action>
Apply the standard safe sequence (Task 1 of plan 19-01 is the exemplar) to `phase_planner.tmpl`. Template-specific detail to preserve: the preamble references BOTH the README.md spec AND "the owning MILESTONE.md" — keep both clauses (the MILESTONE.md read is load-bearing context for this level); the children emit Plan CRDs ("write one JSON file per Plan").

Commit A (PROMPT-03): annotate each load-bearing line with `{{/* WHY: ... */ -}}` (trim-right only) on the line above. Targets: the README.md + MILESTONE.md spec-read directive, the child-CRD shape/kind=Plan rules, the "Do NOT emit Task or Wave CRDs" directive, the machine-contract line. No content removed; golden+ratchet updated same commit only if bytes shift.

Commit B (PROMPT-01/02 via D-01/D-02/D-03/D-07): delete the "Dispatch metadata for this run:" block; make HOW-TO-EMIT paths UID-free/abstract (D-01); append volatile suffix in canonical order — bare `{{/* SharedContext slot — populated in Phase 20 (CACHE-02/03) */}}` (no trim markers, D-07), `TaskUID: {{.TaskUID}}`, one-line task-dir mapping, blank line, `Original prompt:`, `{{.Prompt}}`. Regenerate golden, read+write ratchet, same commit. Verify exactly one `{{.TaskUID}}` remains.

Commits C+ (PROMPT-04 / D-04): conservative section-at-a-time trim — KEEP the README.md+MILESTONE.md spec-read directive verbatim, compress only the acronym/contextual slack, dedupe the machine-contract line only post-annotation. Each trim its own commit: edit → golden -update → ratchet down → `go test ./internal/eval/` green; commit message records why the removed text was safe.
  </action>
  <verify>
    <automated>go test ./internal/eval/ -run "TestGoldenRender_PhasePlanner|TestByteRatchet_PhasePlanner|TestDAGAcyclicity_|TestDeclaredOutputPaths_Presence"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/eval/` exits 0.
    - `grep -c "{{.TaskUID}}" internal/subagent/common/templates/phase_planner.tmpl` returns exactly 1.
    - `grep -cE "\.Provider|\{\{\.Level\}\}|\{\{\.Role\}\}" internal/subagent/common/templates/phase_planner.tmpl` returns 0.
    - `grep -c "SharedContext slot — populated in Phase 20" internal/subagent/common/templates/phase_planner.tmpl` returns 1.
    - `grep -c "README.md" internal/subagent/common/templates/phase_planner.tmpl` returns >= 1.
    - The integer in `internal/eval/testdata/ratchets/phase_planner.txt` is < 2271.
    - `grep -cE "\{\{/\* WHY" internal/subagent/common/templates/phase_planner.tmpl` returns >= 2.
  </acceptance_criteria>
  <done>phase_planner.tmpl reordered to D-03 order, metadata removed from the stable prefix, README+MILESTONE spec directive preserved, annotated, conservatively trimmed; golden + ratchet updated; eval package green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Annotate → reorder → trim plan_planner.tmpl (commits A, B, C+) — largest template, FILE-TOUCH + JSON-escape directives are load-bearing</name>
  <files>internal/subagent/common/templates/plan_planner.tmpl, internal/eval/testdata/goldie/plan_planner.golden, internal/eval/testdata/ratchets/plan_planner.txt</files>
  <read_first>
    - internal/subagent/common/templates/plan_planner.tmpl (the file being edited — read full current content; it is the largest, 4281 bytes, with the full Task child-CRD shape, FILE-TOUCH RULE, and JSON-escaping IMPORTANT blocks)
    - internal/eval/testdata/goldie/plan_planner.golden (current rendered baseline)
    - internal/eval/testdata/ratchets/plan_planner.txt (current frozen count = 4281)
    - internal/eval/render_test.go (goldenAssert + ratchetAssert + baseEnvelope fixture)
    - internal/eval/protocol_test.go (TestDAGAcyclicity_*, TestDeclaredOutputPaths_Presence — the protocol gate that plan_planner's child-CRD directives feed)
    - .planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md (D-01..D-07)
    - .planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md (§Common Pitfalls 3 over-trimming HOW-TO-EMIT, §Safe Sequencing)
    - .planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md (§D-06 annotation, §D-07 slot, §volatile suffix shape, §commit protocol)
  </read_first>
  <behavior>
    - After commit A (annotation): go test ./internal/eval/ -run "TestGoldenRender_PlanPlanner|TestByteRatchet_PlanPlanner" exits 0. Every load-bearing block (the four REQUIRED spec fields planRef/filesTouched/declaredOutputPaths/prompt, the FILE-TOUCH RULE, both JSON-escaping IMPORTANT paragraphs) carries a WHY annotation.
    - After commit B (reorder): output is role → fixed instructions → SharedContext slot → volatile suffix → `Original prompt:` + prompt. grep -c "{{.TaskUID}}" returns exactly 1; `.Provider`/`.Level`/`.Role` gone. The FILE-TOUCH RULE, the four REQUIRED-field descriptions, and the two JSON-escaping IMPORTANT blocks are ALL still present verbatim. golden+ratchet updated same commit; ratchet < 4281.
    - After each commit C+ (trim): TestDAGAcyclicity_* and TestDeclaredOutputPaths_Presence stay green; ratchet decreases; golden regenerated each commit.
  </behavior>
  <action>
Apply the safe sequence to `plan_planner.tmpl` with HEIGHTENED conservatism — this template carries the most machine-contract text and per RESEARCH §Pitfall 3 the HOW-TO-EMIT block IS the contract (path format, child-CRD shape, the four REQUIRED non-empty fields planRef/filesTouched/declaredOutputPaths/prompt, the FILE-TOUCH RULE, and the two JSON-escaping IMPORTANT paragraphs are all load-bearing; multiple production cascades were fixed here).

Commit A (PROMPT-03): annotate EVERY load-bearing line with `{{/* WHY: ... */ -}}` (trim-right only) on the line above — explicitly cover the four REQUIRED fields, the FILE-TOUCH RULE, and both JSON-escaping IMPORTANT blocks. No content removed. golden+ratchet updated same commit only if bytes shift. This annotation pass is the safety record that gates the later trim.

Commit B (PROMPT-01/02 via D-01/D-02/D-03/D-07): delete the "Dispatch metadata for this run:" block; convert the two inline `{{.TaskUID}}` children/ path examples (task-01.json / task-02.json) to UID-free abstract text ("write one JSON file per Task into your children/ directory"); append the volatile suffix in canonical order — bare `{{/* SharedContext slot — populated in Phase 20 (CACHE-02/03) */}}` (no trim markers, D-07), `TaskUID: {{.TaskUID}}`, one-line task-dir mapping, blank line, `Original prompt:`, `{{.Prompt}}`. Regenerate golden (`go test -update ./internal/eval/ -run TestGoldenRender_PlanPlanner`), read+write ratchet, same commit. Verify exactly one `{{.TaskUID}}` remains.

Commits C+ (PROMPT-04 / D-04): conservative section-at-a-time trim. DO NOT touch the four REQUIRED-field descriptions, the FILE-TOUCH RULE, or the JSON-escaping IMPORTANT blocks beyond pure formatting slack — they are the machine contract. Compress only the TIDE-acronym preamble (keep README.md spec-read verbatim) and dedupe the single "Markdown is the human-review surface; the JSON files are the machine contract" line if and only if its annotation confirms semantic content survives. Each trim its own commit with rationale in the message: edit → golden -update → ratchet DOWN → `go test ./internal/eval/` green (protocol tests included).
  </action>
  <verify>
    <automated>go test ./internal/eval/ -run "TestGoldenRender_PlanPlanner|TestByteRatchet_PlanPlanner|TestDAGAcyclicity_|TestDeclaredOutputPaths_Presence"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/eval/` exits 0.
    - `grep -c "{{.TaskUID}}" internal/subagent/common/templates/plan_planner.tmpl` returns exactly 1.
    - `grep -cE "\.Provider|\{\{\.Level\}\}|\{\{\.Role\}\}" internal/subagent/common/templates/plan_planner.tmpl` returns 0.
    - `grep -c "SharedContext slot — populated in Phase 20" internal/subagent/common/templates/plan_planner.tmpl` returns 1.
    - `grep -c "FILE-TOUCH RULE" internal/subagent/common/templates/plan_planner.tmpl` returns 1 (load-bearing rule preserved).
    - `grep -cE "planRef|filesTouched|declaredOutputPaths" internal/subagent/common/templates/plan_planner.tmpl` returns >= 3 (REQUIRED fields preserved).
    - The integer in `internal/eval/testdata/ratchets/plan_planner.txt` is < 4281.
    - `grep -cE "\{\{/\* WHY" internal/subagent/common/templates/plan_planner.tmpl` returns >= 4 (heightened annotation density on the contract blocks).
  </acceptance_criteria>
  <done>plan_planner.tmpl reordered to D-03 order; FILE-TOUCH RULE, four REQUIRED fields, and JSON-escaping blocks preserved verbatim; metadata removed from the stable prefix; densely annotated; conservatively trimmed; golden + ratchet updated; eval package green including protocol tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered prompt → subagent LLM | Template text is the only guardrail steering a dispatched planner agent's child-CRD emission. |
| plan_planner contract text → orchestrator admission webhook | The FILE-TOUCH RULE, REQUIRED-field, and JSON-escaping directives instruct the model to produce CRDs that the admission webhook accepts. Weakening them produces parseable-but-rejected or malformed child CRDs. |
| template change → eval gate | Structural-only gate (goldie + ratchet + protocol) cannot prove a trimmed instruction still steers the model correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering (instruction integrity) | Over-trim of plan_planner's FILE-TOUCH RULE / REQUIRED-field / JSON-escaping machine-contract blocks | mitigate | RESEARCH §Pitfall 3: these blocks ARE the contract (production-cascade origin). Heightened conservatism: annotate before trim (>= 4 WHY annotations asserted), trim posture EXCLUDES the contract blocks, acceptance criteria assert FILE-TOUCH RULE and the three REQUIRED field names survive. Protocol tests (TestDAGAcyclicity_*, TestDeclaredOutputPaths_Presence) gate each commit. |
| T-19-04 | Tampering (silent guardrail weakening) | Structural-only eval gate blind to a semantically-weakened-but-parseable prompt | mitigate (residual: MEDIUM) | Same as plan 19-01 T-19-02 — residual mitigated by the D-05 human-review checkpoint (plan 19-04) + `make eval` token confirmation. LLM-judge deferred (EVAL-F1); residual explicitly accepted phase-level. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 19 (RESEARCH §Package Legitimacy Audit: zero external packages). No supply-chain surface. |

No high-severity threats: edits are confined to `.tmpl` and `testdata/` files. The salient surface is instruction-integrity on plan_planner's contract blocks (T-19-03), mitigated by annotate-before-trim, the trim-exclusion list, the protocol gate, and the downstream human review. ASVS does not apply (RESEARCH §Security Domain).
</threat_model>

<verification>
- `go test ./internal/eval/` exits 0 after the final commit of each template chain (protocol tests included).
- Both ratchet integers strictly below baselines (phase < 2271, plan < 4281).
- Each template has exactly one `{{.TaskUID}}` and zero `.Provider`/`.Level`/`.Role`.
- plan_planner's FILE-TOUCH RULE and REQUIRED field descriptions survive.
- Phase-wide `make test` runs in plan 19-04 (wave 2).
</verification>

<success_criteria>
- phase_planner.tmpl and plan_planner.tmpl both render in canonical D-03 stable-prefix-first order.
- TaskUID/Provider.* only in the volatile suffix; printed Provider/Level/Role lines dropped.
- plan_planner machine-contract blocks preserved; load-bearing lines annotated before trim.
- Goldens regenerated and ratchets lowered in the same commit as each byte change; eval package green throughout.
</success_criteria>

<output>
Create `.planning/phases/19-template-reorder-token-minimization/19-02-SUMMARY.md` when done.
</output>
