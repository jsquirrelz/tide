---
phase: 19-template-reorder-token-minimization
plan: 04
type: execute
wave: 2
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - internal/eval/render_test.go
autonomous: false
requirements: [PROMPT-05, PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04]
must_haves:
  truths:
    - "A regression guard proves no prompt template interpolates map-typed / range-iterated data into the stable prefix (PROMPT-05 confirmed no-op, protected against future regression)"
    - "make test (the full UNIT tier, not just go test ./internal/eval/) is green with all five templates reordered + trimmed"
    - "All five templates satisfy the STABLE-PREFIX INVARIANT: zero {{.TaskUID}}/{{.Provider.*}}/`Level:`/`Role:` in the stable prefix (before the `SharedContext slot` marker), all {{.TaskUID}} occurrences concentrated in the volatile suffix after it, and zero {{.Provider.*}}/{{.Level}}/{{.Role}} interpolation anywhere"
    - "All five ratchet integers are strictly below their v1.0.1 baselines"
    - "A human has reviewed the annotated reorder/trim diffs (D-05) and confirmed no load-bearing instruction was weakened, plus make eval token confirmation"
  artifacts:
    - path: "internal/eval/render_test.go"
      provides: "PROMPT-05 regression guard test (asserts zero map/range interpolation across all five templates)"
      contains: "TestNoMapInterpolation"
  key_links:
    - from: "internal/eval/render_test.go"
      to: "internal/subagent/common/templates/*.tmpl"
      via: "guard test reads each .tmpl via go:embed FS and asserts no .Params/range/map token"
      pattern: "TestNoMapInterpolation"
---

<objective>
Close PROMPT-05 as a protected no-op (a regression guard test, not map-serialization work — RESEARCH §PROMPT-05 Analysis confirms today's templates interpolate only scalar fields), run the full-tier `make test` phase gate across all five reordered+trimmed templates, and gate the phase on a human review of the annotated diffs (D-05).

Purpose: PROMPT-05 ("structured data serialized deterministically") is satisfied because no template references the only map-typed field (`ProviderSpec.Params`). Rather than invent serialization work, add a guard test so a future template change that introduces map-range interpolation fails CI. The full `make test` is the wave-merge gate (CLAUDE.md: `make test-int` ≠ Ginkgo green — read MAKE_EXIT; here the UNIT tier `make test` is the relevant gate). The D-05 checkpoint catches the semantic-regression residual the structural gate cannot.

Output: A regression guard test in `internal/eval/render_test.go`, a green `make test`, and a recorded human approval of the annotated diffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md
@.planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md
@.planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md

<interfaces>
<!-- Eval harness contracts — DO NOT explore the codebase, use these directly. -->

internal/eval/render_test.go already contains: baseEnvelope fixture, templateCases slice
  (5 {role, level, name} entries), goldenAssert, ratchetAssert, and the go:embed-backed
  common.LoadPromptTemplate(role, level) loader. The guard test reuses LoadPromptTemplate +
  templateCases to walk all five templates.

PROMPT-05 finding (RESEARCH §PROMPT-05 Analysis, VERIFIED): the five templates interpolate only
  scalar string fields (.Level, .Role, .TaskUID, .Prompt, .Provider.Vendor, .Provider.Model).
  ProviderSpec.Params (map[string]string) is the ONLY map-typed field and is referenced by NO
  template. baseEnvelope pins Params=nil to avoid map-iteration nondeterminism. After the
  19-01/02/03 reorder, Provider.* printed lines were DROPPED and TaskUID moved to the volatile
  suffix (still scalar, still deterministic).

make test (Makefile:85): manifests generate fmt vet setup-envtest then
  `go test -short -timeout 120s` over ./... excluding /e2e and /test/integration.
  internal/eval/ is included (no build tag). This is the phase gate.

make eval (Makefile:206): online count_tokens via credproxy; requires
  TIDE_PROXY_ENDPOINT + TIDE_SIGNED_TOKEN. NOT a gate — maintainer token-confirmation report (D-05).

STABLE-PREFIX INVARIANT (the wave-1 correctness check this plan spot-confirms — NOT a UID-count check):
  Each template's volatile suffix legitimately contains MULTIPLE {{.TaskUID}} occurrences (planners: 2;
  task_executor: 5). The invariant is that the STABLE PREFIX — everything BEFORE the `SharedContext slot`
  marker — contains ZERO volatile tokens, and the suffix AFTER the marker contains all of them.
  Per-template check (exits 0 ⇔ zero TaskUID before marker):
    awk '/SharedContext slot/{f=1} !f && /\{\{\.TaskUID\}\}/{c++} END{exit c}' <template>.tmpl
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PROMPT-05 regression guard + run full-tier make test phase gate</name>
  <files>internal/eval/render_test.go</files>
  <read_first>
    - internal/eval/render_test.go (the file being edited — read baseEnvelope, templateCases, goldenAssert, ratchetAssert, the embed-backed LoadPromptTemplate usage)
    - internal/subagent/common/prompt_templates.go (the go:embed templateFS + LoadPromptTemplate single-file ParseFS — read to confirm how to access raw template source for the guard)
    - .planning/phases/19-template-reorder-token-minimization/19-RESEARCH.md (§PROMPT-05 Analysis — the no-op rationale; §Validation Architecture phase-requirements→test map)
    - .planning/phases/19-template-reorder-token-minimization/19-PATTERNS.md (§render_test.go fixture + templateCases)
    - .planning/phases/19-template-reorder-token-minimization/19-CONTEXT.md (PROMPT-05 scope — do NOT invent map-serialization work)
  </read_first>
  <behavior>
    - Test 1 (TestNoMapInterpolation): reads each of the five `.tmpl` source files (via the go:embed FS exposed by internal/subagent/common, or by re-reading the template files relative to the package) and asserts NONE contains a map-range interpolation token — specifically that the parsed template references no `.Params` field and uses no `{{range}}` over a map. Passes today (zero hits); would FAIL if a future edit introduces `{{range .Provider.Params}}` or similar. Guards PROMPT-05.
    - Test 1 is deterministic, zero-network, runs inside `go test ./internal/eval/` (no build tag).
    - After this task: `make test` exits 0 with all five reordered+trimmed templates and the new guard in place.
  </behavior>
  <action>
Add a guard test `TestNoMapInterpolation` to `internal/eval/render_test.go` that closes PROMPT-05 as a protected no-op (per RESEARCH §PROMPT-05 Analysis — DO NOT build map-serialization; the templates interpolate only scalars). The test walks all five templates from `templateCases` and asserts the template source contains no map-typed interpolation: assert the source string contains neither `.Params` nor a `range` action (the only map field on EnvelopeIn is `ProviderSpec.Params`, referenced by no template). Source the raw template bytes either via the existing `go:embed` FS in `internal/subagent/common` (preferred — exercises the shipped templates) or by reading `internal/subagent/common/templates/<name>.tmpl` relative to the test. Keep the assertion message explicit: a hit means a future change introduced nondeterministic map interpolation into a prompt and must add stable-key-order serialization (PROMPT-05).

Then run the full-tier phase gate `make test` and confirm exit 0 across all five reordered+trimmed templates (this is the wave-2 merge gate; per CLAUDE.md read the echoed exit status, do not trust a partial summary line). Commit the guard test.
  </action>
  <verify>
    <automated>go test ./internal/eval/ -run "TestNoMapInterpolation|TestGoldenRender_|TestByteRatchet_|TestDAGAcyclicity_|TestDeclaredOutputPaths_Presence" && make test</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/eval/ -run TestNoMapInterpolation` exits 0.
    - `grep -c "func TestNoMapInterpolation" internal/eval/render_test.go` returns 1.
    - `make test` exits 0 (full UNIT tier; confirm the echoed exit status, not just a Ginkgo/summary line).
    - Across all five templates: `grep -lE "\.Params|\{\{[[:space:]]*range" internal/subagent/common/templates/*.tmpl` returns no files (zero map/range interpolation — PROMPT-05 no-op confirmed).
    - STABLE-PREFIX INVARIANT confirmed for every template (the wave-1 reorders landed): `for t in milestone_planner project_planner phase_planner plan_planner task_executor; do awk '/SharedContext slot/{f=1} !f && /\{\{\.TaskUID\}\}/{c++} END{exit c}' internal/subagent/common/templates/$t.tmpl || echo "FAIL prefix $t"; sed -n '/SharedContext slot/,$p' internal/subagent/common/templates/$t.tmpl | grep -q "{{.TaskUID}}" || echo "FAIL suffix $t"; done` prints nothing (zero UID in each prefix, >=1 in each suffix).
    - Across all five templates: `grep -cE "\.Provider|\{\{\.Level\}\}|\{\{\.Role\}\}" internal/subagent/common/templates/*.tmpl` returns 0 for each (no Provider/Level/Role interpolation anywhere).
    - All five ratchet integers are below their baselines: project<2474, milestone<2214, phase<2271, plan<4281, task_executor<1961.
  </acceptance_criteria>
  <done>TestNoMapInterpolation guards PROMPT-05 against future regression; full-tier make test is green with all five reordered+trimmed templates; the no-op is protected, not faked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of annotated reorder/trim diffs (D-05) + make eval token confirmation</name>
  <files>(review-only — no files modified by this task)</files>
  <action>Pause for human verification. Present the what-built summary and the how-to-verify steps below to the user; do NOT auto-approve — D-05 mandates a human reviews the annotated diffs because the structural eval gate cannot catch a semantically weakened guardrail. Wait for the resume signal before closing the phase.</action>
  <what-built>All five prompt templates (milestone/project/phase/plan planner + task_executor) reordered to canonical D-03 stable-prefix-first order, load-bearing lines annotated with `{{/* WHY */ -}}` comments before any trim, conservatively trimmed one section per commit, goldens regenerated and ratchets lowered. A PROMPT-05 regression guard (TestNoMapInterpolation) added. The full-tier `make test` is green.</what-built>
  <how-to-verify>
    The structural eval gate (goldie + ratchet + protocol) proves byte identity and child-CRD parseability — it CANNOT prove a trimmed instruction still steers the model correctly (D-04/D-05 rationale; LLM-judge deferred to EVAL-F1). This checkpoint is the semantic safety net.

    1. Review the annotated diffs across the wave-1 commits:
       `git log --oneline -- internal/subagent/common/templates/` (the per-section trim commits, each carrying a removal-rationale message).
       For each removed line, confirm the commit message justifies WHY it was safe, and that the surviving `{{/* WHY */ -}}` annotations cover the load-bearing lines that remain.
    2. Confirm no load-bearing instruction was weakened — specifically that these survived verbatim across all templates: the "Read the project spec at /workspace/repo.git/README.md ... load-bearing" directive; plan_planner's FILE-TOUCH RULE + the four REQUIRED fields (planRef/filesTouched/declaredOutputPaths/prompt) + JSON-escaping IMPORTANT blocks; task_executor's DeclaredOutputPaths restriction + no-push/"no git credentials" directive + EnvelopeOut required fields.
    3. Run the online token confirmation (D-05, requires credproxy + signed token — macOS note from MEMORY.md: SSL_CERT_FILE is ignored on macOS, run credproxy + tide-eval in a golang:1.26.3 Linux container if needed):
       `TIDE_PROXY_ENDPOINT=https://127.0.0.1:8443 TIDE_SIGNED_TOKEN=<token> make eval`
       Confirm each template's real Anthropic token count DROPPED relative to the Phase 18 baseline and that the ratchet did not accidentally grow. (Expected: counts stay below the 1,024 cache floor — Phase 19's win is token reduction + Phase-20 readiness, not near-term cache hits per 19-CONTEXT.md.)
    4. If credproxy is unavailable, record that `make eval` was skipped and the offline ratchet drop is the substitute evidence; the human-review-of-diff step is still mandatory.
  </how-to-verify>
  <verify>
    <human-check>Human reviews the annotated reorder/trim diffs and per-commit removal rationales, confirms the named load-bearing directives survived, and confirms `make eval` token counts dropped (or records the skip with the offline ratchet drop as substitute evidence).</human-check>
  </verify>
  <acceptance_criteria>
    - Human has reviewed the annotated reorder/trim diffs and confirmed no load-bearing instruction was weakened.
    - The named load-bearing directives (README spec-read, FILE-TOUCH RULE, REQUIRED fields, JSON-escape blocks, DeclaredOutputPaths, no-push, EnvelopeOut fields) are confirmed present.
    - `make eval` token counts confirmed dropped (or skip explicitly recorded with offline ratchet drop as substitute evidence).
  </acceptance_criteria>
  <done>Human has approved the annotated diffs; no load-bearing instruction weakened; token counts confirmed dropped (or skip recorded). Phase 19 is ready to close.</done>
  <resume-signal>Type "approved" to close the phase, or describe which trimmed/weakened instruction needs restoring.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard test → future template edits | TestNoMapInterpolation is the regression boundary protecting PROMPT-05's determinism guarantee against a future map-range interpolation. |
| structural gate → semantic correctness | The deterministic gate proves bytes + parseability, NOT model-steering quality. The human-review checkpoint is the only semantic gate this milestone (LLM-judge deferred). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering (determinism regression) | A future template change introduces map-range interpolation into the stable prefix, producing nondeterministic rendered bytes | mitigate | TestNoMapInterpolation asserts zero `.Params`/`range` interpolation across all five templates and fails CI on any future introduction — converts PROMPT-05 from a one-time no-op into a standing guard. |
| T-19-08 | Tampering / Repudiation (silent guardrail weakening across the whole phase) | The cumulative reorder+trim across all five templates weakened a load-bearing instruction in a way the structural gate cannot see | mitigate | The blocking D-05 human-review checkpoint (Task 2) reviews the annotated diffs + per-commit removal rationales + `make eval` token confirmation. This is the phase-level semantic gate; it explicitly resolves the residual MEDIUM risk flagged in plans 19-01/02/03 (T-19-02/04/06). The checkpoint is blocking — the phase does not close on structural-green alone. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 19 (RESEARCH §Package Legitimacy Audit). No supply-chain surface. |

No high-severity threats: the guard test and `make test` touch only test code; the checkpoint is a review. The phase's one genuinely-uncatchable-by-machine risk (semantic weakening of a guardrail instruction, T-19-08) is the explicit reason this plan carries a blocking human-verify checkpoint rather than auto-advancing on structural green. ASVS does not apply (RESEARCH §Security Domain).
</threat_model>

<verification>
- `go test ./internal/eval/ -run TestNoMapInterpolation` exits 0; the function exists in render_test.go.
- `make test` (full UNIT tier) exits 0 — confirmed via echoed exit status, not just a summary line.
- All five templates satisfy the STABLE-PREFIX INVARIANT: zero `{{.TaskUID}}`/`{{.Provider`/`Level:`/`Role:` before the `SharedContext slot` marker, all UID occurrences in the volatile suffix after it; zero `.Provider`/`.Level`/`.Role` interpolation anywhere; all five ratchets below baseline.
- D-05 human review recorded; `make eval` token drop confirmed (or skip explicitly recorded).
</verification>

<success_criteria>
- PROMPT-05 closed as a protected no-op via TestNoMapInterpolation (no invented serialization work).
- Full-tier `make test` green with all five reordered+trimmed templates.
- Human-reviewed annotated diffs confirm no load-bearing instruction weakened; token counts confirmed dropped.
</success_criteria>

<output>
Create `.planning/phases/19-template-reorder-token-minimization/19-04-SUMMARY.md` when done.
</output>
</content>
