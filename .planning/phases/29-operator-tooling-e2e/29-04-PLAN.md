---
phase: 29-operator-tooling-e2e
plan: 04
type: execute
wave: 3
depends_on: ["29-01"]
files_modified:
  - examples/projects/dogfood/salvage-20260618/pvc-envelopes/envelopes/
  - scripts/check-salvage-childcount.sh
  - test/integration/kind/testdata/import-small-fixture/projects.yaml
  - test/integration/kind/testdata/import-small-fixture/milestones.yaml
  - test/integration/kind/testdata/import-small-fixture/phases.yaml
  - test/integration/kind/testdata/import-small-fixture/plans.yaml
  - test/integration/kind/testdata/import-small-fixture/seed-manifest.json
  - test/integration/kind/testdata/import-small-fixture/pvc-envelopes.tgz
  - test/integration/kind/testdata/import-small-fixture/project.yaml
  - Makefile
autonomous: true
requirements: [TOOL-02]
must_haves:
  truths:
    - "The committed salvage-20260618 out.json files carry childCount so the fixture imports as-is (D-16b)"
    - "Only the complete (exitCode==0, children-bearing) salvage envelopes are patched; failed plan envelopes are untouched and still re-plan (D-17)"
    - "A small purpose-built fixture exists (1 project / 1 milestone / 1 phase / 2 plans) with complete stub envelopes drainable to all-Milestones-Succeeded (D-11a)"
    - "make test-int-kind-prep builds the tide CLI binary so the kind E2E can exec it (D-10, RESEARCH A3)"
  artifacts:
    - path: "test/integration/kind/testdata/import-small-fixture/seed-manifest.json"
      provides: "BundleManifest for the small drain fixture (down-to-Plan seed)"
      contains: "milestones"
    - path: "scripts/check-salvage-childcount.sh"
      provides: "reusable assertion that every complete salvage envelope carries childCount==len(childCRDs)"
      contains: "childCount"
    - path: "Makefile"
      provides: "tide binary build wired into the kind-prep target"
      contains: "bin/tide"
  key_links:
    - from: "salvage-20260618 out.json"
      to: "cmd/tide-import isEnvelopeComplete"
      via: "childCount==len(childCRDs) now satisfied post-patch"
      pattern: "childCount"
    - from: "Makefile test-int-kind-prep"
      to: "bin/tide"
      via: "go build -o bin/tide ./cmd/tide"
      pattern: "bin/tide"
---

<objective>
Land the test data + build wiring the kind E2E (29-05) depends on, with zero production-code
changes: (1) the one-time in-repo childCount patch of the committed salvage-20260618 out.json
files (D-16b) so the real fixture imports as-is; (2) a small purpose-built fixture (down-to-Plan
seed + complete stub envelope tree) that drains to all-Milestones-Succeeded (D-11a); (3) the
Makefile change building the tide CLI as part of kind-prep so the E2E can exec the real binary
(D-10).

Purpose: De-risk the E2E by landing the offline-producible artifacts first. The salvage patch and
small fixture are pure data; the Makefile change is build wiring. None touch controllers or verbs.

Output: patched salvage out.json files, a reusable check script, the import-small-fixture testdata
tree, and the Makefile kind-prep tide build step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-operator-tooling-e2e/29-CONTEXT.md
@.planning/phases/29-operator-tooling-e2e/29-RESEARCH.md
@.planning/phases/29-operator-tooling-e2e/29-PATTERNS.md
@.planning/phases/29-operator-tooling-e2e/29-01-SUMMARY.md

<interfaces>
<!-- Source-of-truth shapes the fixtures must satisfy. -->

cmd/tide-import/main.go:299 isEnvelopeComplete — requires ExitCode==0 AND len(ChildCRDs)==ChildCount.
  The 18 successful salvage planner out.json lack childCount (decode to 0) so they are rejected.
  Patch: set childCount=len(childCRDs) in each, leaving exitCode=0 + childCRDs untouched. DO NOT
  touch incomplete/failed envelopes (exitCode!=0) — they must still re-plan (D-17).

pkg/bundle (29-01): BundleManifest/BundleEntry schema + the childCount-stamp logic the patch mirrors.

internal/controller/import_controller.go:92-130 seedEntry/seedManifest — the small fixture's
  seed-manifest.json must conform (keys milestones/phases/plans; each entry Name/FQName/OldUID/
  DependsOn/Status/refs). Plan entries carry PhaseRef; phases carry MilestoneRef; milestones ProjectRef.

test/integration/kind/testdata/bare-project.yaml — the minimal Project shape analog (58 lines).
api/v1alpha2/import_types.go — ImportSourceRef the small fixture's project.yaml populates.
Makefile:155 test-int-kind-prep (image build/load target); Makefile:263 has the go build -o bin/tide recipe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: one-time childCount patch of committed salvage out.json (D-16b)</name>
  <read_first>
    - cmd/tide-import/main.go:295-307 (isEnvelopeComplete — the invariant the patch must satisfy)
    - .planning/phases/29-operator-tooling-e2e/29-CONTEXT.md (D-16b + D-17 — patch ONLY the complete envelopes; failed plan envelopes stay re-plan)
    - .planning/phases/29-operator-tooling-e2e/29-RESEARCH.md (COLLISION #1 + COLLISION #2 — completeness distribution: project 1, milestone 3, phase 14 success; plan 0/38)
  </read_first>
  <action>
    Author scripts/check-salvage-childcount.sh — a reusable assertion that fails (exit 1, naming the
    offending file) if any salvage out.json with exitCode==0 and a non-empty childCRDs array has
    childCount != len(childCRDs). Then run a one-shot patch (Go or python/jq through Bash — no
    production code change) over every
    examples/projects/dogfood/salvage-20260618/pvc-envelopes/envelopes/UID/out.json: when exitCode==0
    AND childCRDs non-empty AND childCount absent/0, set childCount to len(childCRDs); keep every other
    field byte-stable and the diff readable. Never modify an out.json with exitCode != 0 (the failed
    plan planners must still re-plan — D-17). If the envelope content also lives inside
    pvc-envelopes.tgz, unpack, patch, and re-pack so the unpacked tree and the .tgz agree. Record in
    the SUMMARY the exact count patched (expect ~18) and confirm zero exitCode!=0 envelopes were touched.
  </action>
  <verify>
    <automated>bash scripts/check-salvage-childcount.sh</automated>
  </verify>
  <done>check-salvage-childcount.sh exits 0; every complete salvage out.json carries childCount==len(childCRDs); failed envelopes untouched; .tgz and unpacked tree agree.</done>
</task>

<task type="auto">
  <name>Task 2: small drain-to-Succeeded fixture + Makefile tide build</name>
  <read_first>
    - test/integration/kind/testdata/bare-project.yaml (minimal Project shape)
    - internal/controller/import_controller.go:92-130 (seedManifest the small seed-manifest.json conforms to)
    - .planning/phases/29-operator-tooling-e2e/29-RESEARCH.md (section "Small purpose-built fixture" + "Makefile target for tide binary")
    - .planning/phases/29-operator-tooling-e2e/29-PATTERNS.md (section "Small fixture for drain-to-Succeeded tier" + "Makefile: tide binary build")
    - Makefile (test-int-kind-prep :155, the existing go build -o bin/tide recipe :263)
  </read_first>
  <action>
    Build test/integration/kind/testdata/import-small-fixture/ as a complete, minimal bundle the kind
    E2E imports and drains: 1 Project, 1 Milestone, 1 Phase, 2 Plans (no Tasks — Tasks materialize from
    plan-level children). Author projects/milestones/phases/plans.yaml (the CR specs, v1alpha2),
    seed-manifest.json (BundleManifest with the four levels: FQName via the parent chain, fabricated but
    internally-consistent oldUID values, Status reflecting completed planning, per-envelope sha256), and
    pvc-envelopes.tgz containing complete stub envelopes (each out.json carries childCount==len(childCRDs)
    so isEnvelopeComplete passes — these are stub-authored, NOT legacy, so no repair is needed). The
    project.yaml must populate spec.importSource.seedManifestConfigMap + salvagedPVCSubPath so a
    subsequent tide apply triggers Phase 28 adoption. Keep the envelope children simple enough that the
    stub subagent can drive plan to task to executor to all-Milestones-Succeeded. Then edit the Makefile
    to add the go build -o bin/tide ./cmd/tide step to test-int-kind-prep so the binary exists in bin/
    before the kind suite runs; the E2E resolves it via TIDE_BINARY or PATH (29-05 owns that wiring).
  </action>
  <verify>
    <automated>test -f test/integration/kind/testdata/import-small-fixture/seed-manifest.json && grep -q '"milestones"' test/integration/kind/testdata/import-small-fixture/seed-manifest.json && grep -q '"phases"' test/integration/kind/testdata/import-small-fixture/seed-manifest.json && grep -q '"plans"' test/integration/kind/testdata/import-small-fixture/seed-manifest.json && grep -A40 '^test-int-kind-prep:' Makefile | grep -q 'bin/tide ./cmd/tide'</automated>
  </verify>
  <done>Small fixture seed-manifest.json has all three level arrays; pvc-envelopes.tgz + CR YAMLs + project.yaml present; test-int-kind-prep builds bin/tide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture → test runtime | the salvage + small fixtures are read by the import path during the E2E; they are in-repo (trusted), not third-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Tampering | salvage childCount patch scope | mitigate | The patch script touches ONLY exitCode==0 children-bearing envelopes; check-salvage-childcount.sh + a git diff review confirm no failed envelope (which must re-plan) was altered (D-17) (severity: medium) |
| T-29-04-02 | Tampering | small fixture authenticity | accept | Fixture is in-repo test data, authored by this plan, not external input; integrity is git-tracked (severity: low) |
| T-29-04-03 | Tampering | .tgz vs unpacked drift | mitigate | If both forms exist, the patch re-packs so the loader-staged .tgz and the import-read unpacked tree agree — a drift would silently skip the patch (severity: low) |
</threat_model>

<verification>
- `bash scripts/check-salvage-childcount.sh` exits 0
- `git diff --stat examples/projects/dogfood/salvage-20260618/` shows only out.json (+ .tgz) changes, no failed-envelope edits
- Small fixture tree present with all artifacts; `grep -A40 '^test-int-kind-prep:' Makefile` includes the bin/tide build
- `go build -o /tmp/tide ./cmd/tide` succeeds (the Makefile recipe is valid)
</verification>

<success_criteria>
- Every complete salvage envelope imports-valid (childCount stamped) — D-16b
- Failed plan envelopes untouched — they re-plan (D-17)
- Small drain fixture exists and conforms to the seed schema (D-11a)
- test-int-kind-prep builds the tide CLI (D-10)
- Zero production-code changes
</success_criteria>

<output>
Create `.planning/phases/29-operator-tooling-e2e/29-04-SUMMARY.md` when done
</output>
