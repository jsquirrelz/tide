---
phase: 09-cross-namespace-envelope-return-in-namespace-reporter
plan: 07
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03, 09-06]
files_modified:
  - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md
  - examples/projects/medium/README.md
autonomous: false
requirements: [REQ-09-04, REQ-09-06]
must_haves:
  truths:
    - "examples/projects/medium drives to a LEGITIMATE status.phase=Complete with real Claude (Haiku)"
    - "All descendants reach Succeeded; a per-run tide/run-medium-project-* branch is pushed to the in-cluster http:// remote with real authored code"
    - "status.budget.costSpentCents > 0 and under the cap"
    - "Phase 8 SC-2's real-Claude leg flips from DEFERRED to PASS; the v1.0.0 retag is unblocked (retag/push remains user-gated)"
  artifacts:
    - path: ".planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md"
      provides: "SC-2 real-Claude leg updated from DEFERRED to PASS with Phase 9 evidence"
      contains: "SC-2"
  key_links:
    - from: "examples/projects/medium reader Job"
      to: "child CRs in tide-sample-medium"
      via: "Manager spawns tide-reporter Job → children appear → tree drives to Complete"
      pattern: "Complete"
---

<objective>
Close the phase: drive `examples/projects/medium` to a LEGITIMATE end-to-end `status.phase=Complete` with REAL Claude (Haiku) over the cross-namespace reporter mechanism built in plans 09-01..06, on the parked minikube repro. This is the DoD live acceptance (SC-4/SC-5/SC-6) — NOT CI-gated (cost). On success, flip Phase 8 SC-2's real-Claude leg from DEFERRED to PASS and record that the v1.0.0 retag is unblocked (the retag/push itself stays user-gated, confirm-only per MEMORY.md).

Purpose: the medium-sample legitimate Complete is the bar v1.0 ships against; it proves the reporter mechanism end-to-end with real authored code.
Output: a verified Complete tree + updated 08-VERIFICATION.md SC-2 + README note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/real-claude-authoring-path.md
@.planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-06-SUMMARY.md

<interfaces>
Parked minikube repro (MEMORY + CONTEXT line 85): context `minikube`; TIDE in tide-system runtime-patched to
  --subagent-image=ghcr.io/jsquirrelz/tide-claude-subagent:1.0.0 (NOT chart-persisted; a controller rollout reverts it —
  re-patch arg index 5). tide-sample-medium namespace staged. Images may be stale after the #11 revert + Phase 9 changes —
  rebuild + `minikube image rmi` BEFORE `minikube image load` (load is NOT idempotent).
NEW image this phase: ghcr.io/jsquirrelz/tide-reporter must also be built + loaded; the controller must be patched/configured
  with TIDE_REPORTER_IMAGE (plan 09-06) and the tide-reporter SA+RBAC must exist in tide-sample-medium (plan 09-04 medium block).
Medium apply order (examples/projects/medium/project.yaml header): namespace → CRDs → per-namespace-resources (now incl.
  tide-reporter) → mirror tide-signing-key → demo-remote-init Job → git-http-server Deployment → tide-secrets (GIT_PAT="" anonymous
  http push) → apply project.yaml → wait jsonpath status.phase=Complete.
DoD model: subagent.model claude-haiku-4-5 (medium project.yaml) — must match the pricing.go table key (plan 09-01).
Phase 8 SC-2 current state: gate APPROVED, release_gate BLOCKED, SC-2 real-Claude leg DEFERRED (transport leg PASSES).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild + load all Phase-9-affected images onto the parked minikube</name>
  <read_first>
    - .planning/debug/real-claude-authoring-path.md (the parked-minikube repro state + the #11 revert image-staleness note)
    - examples/projects/medium/project.yaml header (apply order)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-05-SUMMARY.md + 09-06-SUMMARY.md (the new reporter image + TIDE_REPORTER_IMAGE wiring)
  </read_first>
  <action>
    On the minikube context: rebuild every image Phase 9 changed — claude-subagent (in-pod prompt read #10b + tiny-status write + cost compute), stub-subagent (tiny-status + SourcePath), tide-reporter (new), and the controller (manager spawn + handlers + reporter plumbing). For each, `minikube image rmi <ref>` BEFORE `minikube image load <ref>` (load is NOT idempotent — stale layers persist otherwise). Tag consistently with what the chart/controller resolves (the medium sample pins claude-subagent:1.0.0; ensure the reporter image tag matches TIDE_REPORTER_IMAGE). Ensure the tide-reporter SA+Role+RoleBinding exist in tide-sample-medium (apply the updated per-namespace-resources.yaml from plan 09-04). Re-patch the controller's --subagent-image arg if a rollout reverted it (arg index 5 per CONTEXT line 85) and confirm TIDE_REPORTER_IMAGE is set on the manager (plan 09-06 deployment env, or a runtime patch). Confirm the controller Pod rotated (new age) after any patch — a stale Pod silently runs old code (CLAUDE.md Observe-First rule 4).
  </action>
  <verify>
    <automated>echo "MANUAL: minikube image load is not scriptable in CI; this task is operator-run on the parked repro. The checkpoint task below verifies the result."</automated>
  </verify>
  <acceptance_criteria>
    - all four Phase-9-affected images rebuilt and loaded onto minikube (rmi-before-load)
    - tide-reporter SA+Role+RoleBinding present in tide-sample-medium (`kubectl -n tide-sample-medium get sa tide-reporter` succeeds)
    - the manager Pod carries TIDE_REPORTER_IMAGE and the correct --subagent-image, and has rotated to a fresh Pod (new age)
  </acceptance_criteria>
  <done>The parked minikube runs Phase-9 code across the controller + all subagent + reporter images, with the reporter identity provisioned in the sample namespace.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live real-Claude medium-sample end-to-end Complete (human-verify)</name>
  <action>Operator-run on the parked minikube: apply the medium sample in the documented order, watch the tree to status.phase=Complete, and verify the DoD assertions in <how-to-verify>. This is a human-verify checkpoint (the live real-Claude run is not CI-scriptable — cost + real cluster). Capture evidence before claiming.</action>
  <what-built>The cross-namespace reporter mechanism (plans 09-01..06): the Manager spawns a tide-reporter reader Job in the project namespace that reads out.json locally and creates child CRs via the K8s API; the executor reads its prompt in-pod; the tiny status rides the termination message; real Claude cost is computed. Task 1 rebuilt + loaded all affected images onto the parked minikube and provisioned the reporter RBAC in tide-sample-medium.</what-built>
  <how-to-verify>
    1. Apply the medium sample in the documented order (examples/projects/medium/project.yaml header): namespace → CRDs → per-namespace-resources.yaml (now incl. tide-reporter) → mirror tide-signing-key → demo-remote-init Job (wait Complete) → git-http-server Deployment (wait Available) → `kubectl create secret generic tide-secrets ... --from-literal=GIT_PAT=""` → `kubectl apply -f examples/projects/medium/project.yaml`.
    2. Watch the tree: `kubectl -n tide-sample-medium get project,milestone,phase,plan,task -w`. Confirm tide-reporter Jobs appear and complete, and child CRs appear after each planner dispatch.
    3. Wait for `kubectl -n tide-sample-medium wait --for=jsonpath='{.status.phase}'=Complete project/medium-project --timeout=...`.
    4. Assert ALL descendants Succeeded (no Failed/stuck Running).
    5. Assert a per-run branch was pushed: in the git-http-server / demo-remote, confirm a `tide/run-medium-project-*` branch exists with REAL authored code (not a stub placeholder).
    6. Assert cost: `kubectl -n tide-sample-medium get project/medium-project -o jsonpath='{.status.budget.costSpentCents}'` is > 0 and under the cap.
    7. If any leg fails, capture the manager log + the reporter Job log + the failing CRD's status/describe (Observe-First) and report — do NOT hypothesize before reading the evidence.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed Complete state (costSpentCents value + branch name), or describe the failure with the captured evidence.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip Phase 8 SC-2 to PASS + record retag-unblocked</name>
  <read_first>
    - .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md (the SC-2 PARTIAL section + deferred frontmatter + release_gate BLOCKED)
    - examples/projects/medium/README.md (apply-order docs to annotate)
  </read_first>
  <action>
    AFTER the checkpoint records an approved legitimate Complete: update 08-VERIFICATION.md — change the SC-2 real-Claude leg from DEFERRED to PASS, citing the Phase 9 evidence (the observed costSpentCents value + the pushed tide/run-medium-project-* branch + the Complete tree). Update the `deferred:` frontmatter block (remove the SC-2 real-Claude entry) and the `release_gate:` line to reflect that the v1.0.0 retag is now UNBLOCKED — but explicitly note the retag/push itself remains user-gated (confirm-only) per MEMORY.md (delete + re-create v1.0.0 at the post-Phase-9 HEAD, then push only on user OK). Add a short note to examples/projects/medium/README.md that the medium sample now drives to a legitimate Complete via the in-namespace reporter (Phase 9). Do NOT create, move, or push any git tag in this task — recording only.
  </action>
  <verify>
    <automated>grep -q 'SC-2' .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md && grep -iq 'unblock' .planning/phases/08-medium-sample-http-transport-and-production-git-transport-po/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 08-VERIFICATION.md SC-2 real-Claude leg reads PASS (not DEFERRED) with the Phase 9 evidence cited
    - the deferred frontmatter no longer lists the SC-2 real-Claude leg; release_gate reflects retag-unblocked
    - the note explicitly preserves the user-gated, confirm-only retag/push constraint (no tag operations performed)
    - examples/projects/medium/README.md notes the legitimate-Complete-via-reporter path
  </acceptance_criteria>
  <done>Phase 8 SC-2 is fully PASS, the v1.0.0 retag is recorded as unblocked, and the user-gated retag constraint is preserved — no tag operations performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real ANTHROPIC_API_KEY → credproxy → Claude | the live run uses a real key behind the credproxy (unchanged this phase); cost is real |
| pushed run branch → in-cluster http:// remote | real authored code is pushed anonymously (GIT_PAT="") to the demo remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Denial of service | runaway real-Claude spend during the live run | mitigate | the medium sample's budget cap bounds spend; costSpentCents asserted under the cap at the checkpoint |
| T-09-17 | Tampering | premature v1.0.0 tag push | mitigate | Task 3 performs NO tag operations; retag/push stays user-gated confirm-only (MEMORY.md) |
| T-09-18 | Repudiation | "Complete" claimed without evidence | mitigate | checkpoint requires observed costSpentCents + branch name + Succeeded tree before flipping SC-2 (Verify-Before-Claiming) |
</threat_model>

<verification>
- the checkpoint records an observed legitimate Complete (costSpentCents > 0, per-run branch pushed, all descendants Succeeded)
- 08-VERIFICATION.md SC-2 real-Claude leg = PASS; retag recorded as unblocked (user-gated)
- NO git tag created/moved/pushed by this plan
</verification>

<success_criteria>
REQ-09-04 + REQ-09-06 satisfied: examples/projects/medium drives to a legitimate Complete with real Claude (Haiku) over the reporter mechanism; Phase 8 SC-2 is fully PASS; the v1.0.0 retag is unblocked (push remains user-gated).
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-07-SUMMARY.md`
</output>
