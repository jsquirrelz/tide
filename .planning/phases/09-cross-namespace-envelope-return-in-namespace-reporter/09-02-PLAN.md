---
phase: 09-cross-namespace-envelope-return-in-namespace-reporter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/envelope.go
  - pkg/dispatch/envelope_test.go
  - cmd/claude-subagent/main.go
  - cmd/stub-subagent/main.go
autonomous: true
requirements: [REQ-09-03]
must_haves:
  truths:
    - "The dispatch Job's termination message carries only a tiny status (usage/git/exitCode/reason), never childCRDs or the verbose result"
    - "The tiny status is always < 4KB by construction (childCRDs and result excluded)"
    - "out.json on the PVC still carries the full EnvelopeOut (childCRDs + verbose result) as the audit artifact + reporter input"
    - "Stub-authored Task children carry SourcePath so the reporter can stamp Task.Spec.PromptPath (MinLength=1)"
  artifacts:
    - path: "pkg/dispatch/envelope.go"
      provides: "TerminationStub type + NewTerminationStub(EnvelopeOut) TerminationStub"
      contains: "TerminationStub"
    - path: "pkg/dispatch/envelope_test.go"
      provides: "TestNewTerminationStub_StaysSmall (<4KB) + subset-field assertions"
  key_links:
    - from: "cmd/claude-subagent/main.go"
      to: "TerminationStub"
      via: "writeEnvelope marshals NewTerminationStub(out) to /dev/termination-log (not full out)"
      pattern: "NewTerminationStub"
    - from: "cmd/stub-subagent/main.go"
      to: "TerminationStub"
      via: "writeTerminationMessage marshals a TerminationStub"
      pattern: "NewTerminationStub"
---

<objective>
Split the cross-namespace return: the tiny status (usage / git HeadSHA / exitCode / reason) rides the dispatch Job's 4KB termination message; the verbose `result` and `childCRDs` stay on the namespace PVC in `out.json` (the reporter Job's input, plan 09-05). Resurrect the #11 `TerminationStub` writer-half (the writer was sound; only its PVC-authoritative-read premise was wrong). Also fix the stub-compat action item: stub Task children must carry `SourcePath` so the reporter can stamp `Task.Spec.PromptPath` (MinLength=1).

Purpose: keep the termination message < 4KB once childCRDs are no longer in it (#11 root cause); keep CI's stub path working with the new reporter (09-05).
Output: `pkg/dispatch.TerminationStub` + `NewTerminationStub`; both subagent shims write the stub (not the full envelope); stub sets SourcePath.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-RESEARCH.md
@.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md

<interfaces>
From pkg/dispatch/envelope.go — EnvelopeOut fields the stub subsets:
  ExitCode int `json:"exitCode"`; Reason string `json:"reason"`; Usage Usage `json:"usage"`;
  Git *GitOutput `json:"git,omitempty"` (GitOutput.HeadSHA string). EXCLUDE: Result, ChildCRDs, Artifacts.
From pkg/dispatch/childcrd.go — ChildCRDSpec.SourcePath string `json:"sourcePath,omitempty"`
  (the real runner stamps it at anthropic/subagent.go:427; the stub does NOT — this plan adds it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TerminationStub type + NewTerminationStub + <4KB test</name>
  <read_first>
    - pkg/dispatch/envelope.go:130-249 (EnvelopeOut + Usage + GitOutput — the source fields to subset)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "pkg/dispatch/envelope.go TerminationStub (NEW — resurrect from #11)")
    - .planning/debug/real-claude-authoring-path.md (lines ~712-737 — the reverted #11 TerminationStub writer half)
  </read_first>
  <behavior>
    - NewTerminationStub copies ExitCode, Reason, Usage, and Git.HeadSHA from an EnvelopeOut
    - NewTerminationStub does NOT carry ChildCRDs, Result, or Artifacts (assert the type has no such fields)
    - marshalling a TerminationStub built from an EnvelopeOut with 50 childCRDs + a 10KB Result yields JSON < 4096 bytes
    - a nil EnvelopeOut.Git yields a stub with empty HeadSHA (no panic)
  </behavior>
  <action>
    In pkg/dispatch/envelope.go add an exported `TerminationStub` struct carrying ONLY: `ExitCode int json:"exitCode"`, `Reason string json:"reason"`, `Usage Usage json:"usage"`, and `HeadSHA string json:"headSHA,omitempty"` (flatten Git.HeadSHA — the Manager only needs the SHA). Add `NewTerminationStub(out EnvelopeOut) TerminationStub` that copies the subset and reads `out.Git.HeadSHA` guarding nil. Document on the type that it is the cross-namespace tiny-status carrier and MUST stay <4KB (childCRDs + verbose result live on the PVC). Add `pkg/dispatch/envelope_test.go` cases per <behavior>, including `TestNewTerminationStub_StaysSmall` that marshals a stub built from a deliberately oversized EnvelopeOut and asserts len(json) < 4096.
  </action>
  <verify>
    <automated>go test ./pkg/dispatch/ -run 'TerminationStub' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'type TerminationStub struct' pkg/dispatch/envelope.go` succeeds
    - `grep -q 'func NewTerminationStub' pkg/dispatch/envelope.go` succeeds
    - the struct has no ChildCRDs / Result / Artifacts field (verify by reading the type)
    - `go test ./pkg/dispatch/ -run 'TerminationStub' -count=1` passes including the <4KB assertion
  </acceptance_criteria>
  <done>TerminationStub carries the tiny-status subset and provably stays under 4KB regardless of childCRD/result size.</done>
</task>

<task type="auto">
  <name>Task 2: Both subagent shims write the tiny stub (not the full envelope)</name>
  <read_first>
    - cmd/claude-subagent/main.go:122-133 (writeEnvelope — marshals full out to /dev/termination-log today)
    - cmd/stub-subagent/main.go:189-210 (writeEnvelope + writeTerminationMessage — full out to termination-log today)
    - .planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-PATTERNS.md (section "cmd/claude-subagent/main.go + cmd/stub-subagent/main.go (MODIFIED — tiny status)")
  </read_first>
  <action>
    In cmd/claude-subagent/main.go writeEnvelope: keep `harness.WriteEnvelopeOut(path, out)` (full out.json on the PVC — UNCHANGED, this is the audit artifact + reporter input). Change the termination-log write to marshal `pkgdispatch.NewTerminationStub(out)` instead of the full `out`. In cmd/stub-subagent/main.go: the full out.json PVC write stays; change `writeTerminationMessage` (or its caller) so the termination-log carries `NewTerminationStub(out)` rather than the full marshalled `data`. Keep the `TIDE_TERMINATION_MESSAGE_PATH` env fallback to `/dev/termination-log` in both. Under Option C the SUBAGAENT container keeps writing the termination message (PodStatusEnvelopeReader still reads ContainerNameSubagent — no container-name change, per CONTEXT line 49 + PATTERNS backend.go note); only the PAYLOAD shrinks to the stub.
  </action>
  <verify>
    <automated>go test ./cmd/claude-subagent/... ./cmd/stub-subagent/... -short -count=1 && go build ./cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - both shims: `grep -q 'NewTerminationStub' cmd/claude-subagent/main.go cmd/stub-subagent/main.go` succeeds
    - the full-envelope marshal to termination-log is gone (grep that the termination-log write no longer marshals the full `out`/`data` directly)
    - `harness.WriteEnvelopeOut` (full out.json to PVC) call is retained in claude-subagent
    - `go build ./cmd/...` succeeds; existing cmd tests pass
  </acceptance_criteria>
  <done>Both subagent images write a <4KB TerminationStub to the termination message while still writing the full out.json to the PVC.</done>
</task>

<task type="auto">
  <name>Task 3: Stub sets SourcePath on its Task children (stub-compat)</name>
  <read_first>
    - cmd/stub-subagent/main.go:~325-353 (dispatchPlannerSuccess Task case — builds the "Task" ChildCRDSpec WITHOUT SourcePath today)
    - internal/subagent/anthropic/subagent.go:420-431 (real runner stamps spec.SourcePath = filepath.Join(relPrefix, name) — the parity target)
    - internal/controller/dispatch_helpers.go:341-349 (materializer copies child.SourcePath → tk.Spec.PromptPath; PromptPath MinLength=1)
  </read_first>
  <action>
    In cmd/stub-subagent/main.go dispatchPlannerSuccess, on the "Task"-level ChildCRDSpec it appends (around line 330, Name "stub-task-1"), set `SourcePath` to a deterministic workspace-relative path the stub also writes a prompt artifact to (mirror the real runner's children/<name>.json convention) — OR set SourcePath to an existing stub-written prompt file path. Ensure the materializer's `tk.Spec.PromptPath = child.SourcePath` will satisfy MinLength=1 and the executor can read the prompt in-pod (consumed by plan 09-03). Confirm the stub still writes whatever artifact the executor-level read expects so the kind Layer B stub path stays green.
  </action>
  <verify>
    <automated>go test ./cmd/stub-subagent/... -short -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'SourcePath' cmd/stub-subagent/main.go` succeeds within dispatchPlannerSuccess
    - the stub's Task ChildCRDSpec has a non-empty SourcePath (verify by reading the appended struct literal)
    - `go test ./cmd/stub-subagent/... -short -count=1` passes
  </acceptance_criteria>
  <done>Stub-authored Task children carry a non-empty SourcePath so the reporter's PromptPath stamp passes MinLength=1 validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent pod (project ns) → Manager (tide-system) | the only cross-namespace channel from this plan is the 4KB termination message (Pod-status API) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Denial of service | oversized termination message truncated by kubelet → Manager mis-classifies result (the #11 failure) | mitigate | TerminationStub excludes childCRDs + verbose result; TestNewTerminationStub_StaysSmall enforces <4KB by construction |
| T-09-04 | Tampering | stub Task child with empty SourcePath → reporter Create fails MinLength=1 → CI stub path breaks | mitigate | stub sets a deterministic non-empty SourcePath; covered by cmd/stub-subagent test |
</threat_model>

<verification>
- `go test ./pkg/dispatch/... ./cmd/claude-subagent/... ./cmd/stub-subagent/... -short -count=1` green
- `go build ./cmd/...` succeeds
</verification>

<success_criteria>
REQ-09-03 satisfied: the tiny status returns via the termination message (<4KB, childCRDs + verbose result excluded); out.json on the PVC remains the full audit artifact; stub Task children carry SourcePath for reporter compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cross-namespace-envelope-return-in-namespace-reporter/09-02-SUMMARY.md`
</output>
