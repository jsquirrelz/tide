---
phase: 32-d3-dispatch-concurrency-cap
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/controller/adoption_lifecycle_test.go
autonomous: true
requirements: [WR-01, WR-02, WR-03, WR-04]

must_haves:
  truths:
    - "Each *RolledUpUID marker stamp re-fetches the level object and retries on conflict, mirroring budget.RollUpUsage"
    - "A marker-stamp failure after retry-budget exhaustion requeues the reconcile rather than silently continuing"
    - "The project suppression patch comment no longer asserts a false conflict-retry invariant (or uses optimistic lock)"
    - "A test proves the adoption advance writes the Phase change and suppression condition in a single Status patch"
  artifacts:
    - path: "internal/controller/milestone_controller.go"
      provides: "RetryOnConflict-wrapped MilestoneRolledUpUID stamp"
      contains: "RetryOnConflict"
    - path: "internal/controller/phase_controller.go"
      provides: "RetryOnConflict-wrapped PhaseRolledUpUID stamp"
      contains: "RetryOnConflict"
    - path: "internal/controller/plan_controller.go"
      provides: "RetryOnConflict-wrapped PlanRolledUpUID stamp"
      contains: "RetryOnConflict"
    - path: "internal/controller/adoption_lifecycle_test.go"
      provides: "single-patch atomicity assertion for the adoption advance"
      contains: "patch"
  key_links:
    - from: "internal/controller/milestone_controller.go"
      to: "tideprojectv1alpha2.Milestone re-fetch"
      via: "RetryOnConflict + MergeFromWithOptimisticLock"
      pattern: "MergeFromWithOptimisticLock"
    - from: "internal/controller/project_controller.go"
      to: "suppression patch comment"
      via: "comment correction or optimistic-lock switch"
      pattern: "MergeFrom"
---

<objective>
Harden the Phase 31 carried-in debt (31-REVIEW.md WR-01/02/03/04): make the durable `*RolledUpUID` exactly-once marker stamp re-fetch + retry-on-conflict (and requeue on retry-budget exhaustion), fix the misleading conflict-retry comment on the project suppression patch, and add a test proving the adoption advance is a single Status patch.

Purpose: D1's exactly-once budget rollup is guarded solely by the `*RolledUpUID` marker, but the marker is stamped best-effort (`MergeFrom`, non-fatal swallow) against a level object never re-fetched after `RollUpUsage` patched the Project — safe today only by incidental per-key reconcile serialization. A marker-patch failure plus reporter-Job TTL-GC reopens the double-count window ADOPT-04 closed (CONTEXT D-06).
Output: RetryOnConflict-wrapped marker stamps at milestone/phase/plan, the corrected/locked project suppression patch, and the WR-04 single-patch assertion.

This plan carries NO CONCUR requirement — it is the carried-in hardening debt folded into Phase 32 per ROADMAP §"Carried-in debt"; the `requirements` field uses the WR-* markers as the source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-d3-dispatch-concurrency-cap/32-CONTEXT.md
@.planning/phases/32-d3-dispatch-concurrency-cap/32-RESEARCH.md
@.planning/phases/32-d3-dispatch-concurrency-cap/32-PATTERNS.md
@.planning/phases/31-d2-d1-adoption-lifecycle-seam/31-REVIEW.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase; no exploration required. -->

The canonical pattern to mirror — internal/budget/tally.go:57-88 RollUpUsage:
  retry.RetryOnConflict(retry.DefaultRetry, func() error { Get(latest) → MergeFromWithOptions(latest.DeepCopy(), MergeFromWithOptimisticLock{}) → Status().Patch }).
  Import: "k8s.io/client-go/util/retry" (already in go.mod, used by tally.go).

Existing marker-stamp sites (the best-effort MergeFrom blocks to REPLACE):
- milestone_controller.go:594-603 — MilestoneRolledUpUID (jobName var `milestoneJobName`), object `ms` type tideprojectv1alpha2.Milestone
- phase_controller.go:525-533 — PhaseRolledUpUID (var `phaseJobName`), object `ph` type tideprojectv1alpha2.Phase
- plan_controller.go:600-608 — PlanRolledUpUID (var `planJobName`), object `plan` type tideprojectv1alpha2.Plan
Each currently: markerPatch := client.MergeFrom(<obj>.DeepCopy()); <obj>.Status.<X>RolledUpUID = <jobName>; r.Status().Patch(...) with non-fatal logger.Error on failure.

Project suppression patch (WR-01) — project_controller.go:1154-1166: uses plain client.MergeFrom(project.DeepCopy()) with a comment claiming "Conflict is retryable; surface as err so controller retries." A plain MergeFrom embeds no resourceVersion and cannot conflict.

WR-04 test target — adoption_lifecycle_test.go:232-261 (ADOPT-01 test) asserts only end state (Phase==Running AND suppression condition present AND zero Jobs). Package doc claims the single-Status().Patch invariant (D-07) at lines ~20-21.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap the three *RolledUpUID marker stamps in RetryOnConflict + re-fetch (WR-02/WR-03)</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go</files>
  <read_first>
    - internal/controller/milestone_controller.go (file being modified — marker block 594-603)
    - internal/controller/phase_controller.go (file being modified — marker block 525-533)
    - internal/controller/plan_controller.go (file being modified — marker block 600-608)
    - internal/budget/tally.go:57-88 (analog: RollUpUsage RetryOnConflict + MergeFromWithOptimisticLock — the exact pattern to mirror)
  </read_first>
  <behavior>
    - When the marker is unset, the stamp re-fetches the latest level object, sets <X>RolledUpUID, and patches with optimistic lock
    - When a concurrent reconcile already set the marker, the retry func returns nil without re-patching (idempotent)
    - On a conflict, RetryOnConflict re-runs the func against a freshly-fetched object
    - When the retry budget is exhausted, the reconcile requeues (returns the error) instead of swallowing it (WR-03)
  </behavior>
  <action>
    At each of the three sites replace the best-effort `client.MergeFrom(<obj>.DeepCopy())` marker stamp with a `retry.RetryOnConflict(retry.DefaultRetry, func() error { ... })` block mirroring `budget.RollUpUsage`: Get the latest typed object via `r.Get(ctx, client.ObjectKeyFromObject(<obj>), latest)`; early-return nil if `latest.Status.<X>RolledUpUID == <jobName>` (already set — idempotent); else `client.MergeFromWithOptions(latest.DeepCopy(), client.MergeFromWithOptimisticLock{})`, set `latest.Status.<X>RolledUpUID = <jobName>`, `r.Status().Patch(ctx, latest, patch)`. On `RetryOnConflict` returning a non-nil error (WR-03 — retry budget exhausted), return the error to requeue the reconcile rather than only logging non-fatal — propagate via the reconcile's existing error-return shape at that site. Use the correct typed object per site: `tideprojectv1alpha2.Milestone`/`Phase`/`Plan` and the correct field/var (`MilestoneRolledUpUID`/`milestoneJobName`, etc). Add `"k8s.io/client-go/util/retry"` to each controller's import block if absent. Do not change rollup ordering (RollUpUsage still runs before the marker stamp).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run 'Rollup|Idempoten' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RetryOnConflict' internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go` returns 1 in each file (3 total)
    - `grep -c 'MergeFromWithOptimisticLock' internal/controller/milestone_controller.go` returns >= 1
    - `grep -c 'client.ObjectKeyFromObject' internal/controller/plan_controller.go` returns >= 1 (re-fetch present)
    - `grep -cE 'patch (Milestone|Phase|Plan)RolledUpUID failed \(non-fatal\)' internal/controller/milestone_controller.go` returns 0 (best-effort swallow replaced)
    - `go build ./...` exits 0
    - `go test ./internal/controller/... -run 'Rollup|Idempoten' -count=1` exits 0
  </acceptance_criteria>
  <done>All three child-level marker stamps re-fetch + retry-on-conflict with optimistic lock and requeue on exhaustion; existing idempotency specs green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the project suppression-patch comment / optimistic lock (WR-01) + WR-04 single-patch test</name>
  <files>internal/controller/project_controller.go, internal/controller/adoption_lifecycle_test.go</files>
  <read_first>
    - internal/controller/project_controller.go (file being modified — suppression patch 1154-1166; D-07 single-patch comment ~1149-1153)
    - internal/controller/adoption_lifecycle_test.go (file being modified — ADOPT-01 test 232-261, package doc ~20-21)
    - .planning/phases/31-d2-d1-adoption-lifecycle-seam/31-REVIEW.md WR-01 and WR-04 (exact issue + fix shape)
    - internal/budget/tally.go:57-88 (analog for the optimistic-lock option)
  </read_first>
  <behavior>
    - The suppression patch either uses MergeFromWithOptimisticLock (conflict-detecting) OR carries a comment that correctly states it is a last-write-wins server-side merge that cannot conflict
    - A test fails if the adoption advance (Phase=Running + suppression condition) is split into two sequential Status patches
  </behavior>
  <action>
    WR-01: at project_controller.go:1154-1166 switch the suppression patch to `client.MergeFromWithOptions(project.DeepCopy(), client.MergeFromWithOptimisticLock{})` (matching RollUpUsage's own pattern, making the "conflict is retryable" comment true), OR — if optimistic concurrency is not wanted here — correct the comment to state the patch is a last-write-wins server-side merge whose only error paths are I/O/transport. Per RESEARCH WR-01 recommendation prefer the optimistic-lock switch for internal consistency; keep the existing requeue-on-error return. WR-04: in adoption_lifecycle_test.go instrument the test reconciler's client with a Status-patch counter (wrap the fake client or use an interceptor) and assert exactly ONE Status patch is issued during the adoption advance, proving the Phase=Running change and suppression condition land in a single patch (D-07). At minimum assert there is never an observable Running-with-no-suppression-condition intermediate state. Place the assertion in or adjacent to the existing ADOPT-01 test (232-261).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'Adopt|Suppress|SinglePatch' -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - either `grep -c 'MergeFromWithOptimisticLock' internal/controller/project_controller.go` returns >= 1 (optimistic-lock switch) OR `grep -ciE 'last-write-wins|server-side merge|cannot conflict' internal/controller/project_controller.go` returns >= 1 (corrected comment) — at least one path satisfied
    - `grep -c 'Conflict is retryable; surface as err so controller retries' internal/controller/project_controller.go` returns 0 (false invariant removed/corrected)
    - the WR-04 patch-count/single-patch assertion exists: `grep -ciE 'patch.?count|single.?patch|one status patch|exactly one' internal/controller/adoption_lifecycle_test.go` returns >= 1
    - `go test ./internal/controller/... -run 'Adopt|Suppress|SinglePatch' -count=1` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>The suppression patch comment is no longer false (or uses optimistic lock); a test now fails if the adoption advance splits into two patches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent reconciles → level-object Status patch | Two reconciles (or a future earlier status write) could race on the level object's resourceVersion |
| reporter-Job TTL-GC → re-rollup eligibility | An unset marker + GC'd reporter Job can re-enter the rollup branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | double-count of budget accrual via stale/lost marker | mitigate | WR-02/WR-03: RetryOnConflict + re-fetch + MergeFromWithOptimisticLock makes the marker stamp durable against concurrent writes; requeue-on-exhaustion guarantees the marker is set before the reporter Job's TTL-GC can reopen the rollup branch. Closes the double-count window ADOPT-04 targeted. |
| T-32-05 | Repudiation | silent last-write-wins suppression patch | accept | WR-01: benign for the two fields (Phase + one condition) but the false "conflict is retryable" comment is corrected (or an optimistic lock added) so the invariant is honest. No exploit path; correctness/clarity fix. |
| T-32-06 | Tampering | undetected regression splitting the atomic adoption patch | mitigate | WR-04: a patch-count/single-patch test now fails if the Phase=Running advance and suppression condition are split into two patches (the exact race D-07 warns against — transient Running-without-suppression enabling re-dispatch). |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — all imports (incl. k8s.io/client-go/util/retry) already in go.mod (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `go test ./internal/controller/... -count=1` green (rollup idempotency + adoption + single-patch)
- `go build ./...` exits 0
- `make lint` green (no new analyzer violations)
- Full `make test` green before /gsd:verify-work (32-VALIDATION.md phase gate)
</verification>

<success_criteria>
- WR-02/WR-03: all three child marker stamps re-fetch + RetryOnConflict + optimistic lock, requeue on retry-budget exhaustion
- WR-01: project suppression-patch comment no longer asserts a false conflict-retry invariant (or uses optimistic lock)
- WR-04: a test proves the adoption advance is a single Status patch
- No CONCUR regression — this plan does not touch the dispatch-gate or pool code
</success_criteria>

<output>
Create `.planning/phases/32-d3-dispatch-concurrency-cap/32-02-SUMMARY.md` when done
</output>
