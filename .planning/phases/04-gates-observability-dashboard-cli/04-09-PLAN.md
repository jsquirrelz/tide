---
phase: 04
plan: 09
type: execute
wave: 3
depends_on: [04-07]
files_modified:
  - .goreleaser.yaml
  - .github/workflows/release.yaml
  - krew-plugins/tide.yaml
  - docs/cli.md
autonomous: true
requirements: [CLI-02]
threat_model_id: T-04-C2,T-04-Krew
must_haves:
  truths:
    - "`.goreleaser.yaml` defines a multi-OS/arch build of `cmd/tide` producing tarballs (linux/darwin/windows × amd64/arm64), a `checksums.txt`, and SLSA provenance attestation"
    - "GitHub Actions workflow `.github/workflows/release.yaml` triggers on tag push (`v*`), runs `goreleaser release --clean`, and publishes via GitHub Releases"
    - "`krew-plugins/tide.yaml` is a Krew v1alpha2 Plugin manifest template with five platform entries (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64) referencing the goreleaser-uploaded tarballs by URL + sha256"
    - "`docs/cli.md` documents both invocation forms — `tide` standalone AND `kubectl tide` via Krew install — and lists every D-C3 verb with example invocations"
    - "Goreleaser config includes cobra-aware completion script generation hooks so `kubectl tide completion bash|zsh|fish|powershell` works (Pitfall 27)"
  artifacts:
    - path: .goreleaser.yaml
      provides: "goreleaser v2.x config with builds + archives + checksum + release stanzas"
      contains: "tide"
    - path: .github/workflows/release.yaml
      provides: "GitHub Actions release workflow gated on tag push"
      contains: "goreleaser/goreleaser-action"
    - path: krew-plugins/tide.yaml
      provides: "Krew plugin manifest with 5 platforms"
      contains: "krew.googlecontainertools.github.com/v1alpha2"
    - path: docs/cli.md
      provides: "Install instructions for both invocation forms + verb reference"
      contains: "kubectl tide"
  key_links:
    - from: krew-plugins/tide.yaml
      to: github releases (uploaded by goreleaser)
      via: "uri: https://github.com/jsquirrelz/tide/releases/download/<version>/tide_<version>_<os>_<arch>.tar.gz"
      pattern: "github\\.com/jsquirrelz/tide/releases"
    - from: .github/workflows/release.yaml
      to: .goreleaser.yaml
      via: "goreleaser/goreleaser-action invokes goreleaser release"
      pattern: "goreleaser"
---

<objective>
Ship the release pipeline that distributes `cmd/tide` as multi-OS/arch binaries via GitHub Releases + a Krew plugin manifest so operators can install via either `go install ./cmd/tide`, downloading a release tarball, OR `kubectl krew install tide`. This plan does NOT cut a v1.0 release — it lands the configuration files and the CI workflow so the next tag push (Phase 5 work) produces a clean release.

Purpose: Without this, the CLI can't be distributed except via `go install` (which assumes a Go toolchain on the operator's machine). Krew is the K8s-standard plugin distribution channel (kubectl uses it); GitHub Releases is the OS-neutral binary delivery mechanism.

Output: 4 new files (.goreleaser.yaml, release.yaml workflow, Krew manifest template, cli.md doc). No binaries cut today — the workflow is gated on `v*` tag push, which Phase 5 will perform.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-07-PLAN.md
@.planning/phases/04-gates-observability-dashboard-cli/04-08-PLAN.md
@.github/workflows/ci.yaml

<interfaces>
<!-- goreleaser v2.x supported Go 1.26 — verified per RESEARCH §836-857 + assumption A4 -->

```yaml
# .goreleaser.yaml shape
version: 2

builds:
  - id: tide
    main: ./cmd/tide
    env: [CGO_ENABLED=0]
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags: ['-s -w -X main.version={{.Version}}']

archives:
  - id: tide
    builds: [tide]
    name_template: 'tide_{{.Version}}_{{.Os}}_{{.Arch}}'
    formats:
      - format: tar.gz
        goos: linux
      - format: tar.gz
        goos: darwin
      - format: zip
        goos: windows

checksum:
  name_template: 'checksums.txt'

release:
  github:
    owner: jsquirrelz
    name: tide
```

<!-- Krew v1alpha2 schema verified per RESEARCH §860-882 -->

```yaml
# krew-plugins/tide.yaml
apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: tide
spec:
  version: v0.1.0  # placeholder — krew-release-bot rewrites on release
  homepage: https://github.com/jsquirrelz/tide
  shortDescription: Manage TIDE Projects on Kubernetes
  description: |
    `tide` is the CLI for TIDE — a Kubernetes-native orchestrator for hierarchical
    agentic coding work.
  platforms:
    - selector: { matchLabels: { os: linux, arch: amd64 } }
      uri: https://github.com/jsquirrelz/tide/releases/download/{{ .TagName }}/tide_{{ .TagName }}_linux_amd64.tar.gz
      sha256: '{{ .Sha256 }}'
      files: [{ from: tide, to: . }]
      bin: tide
    # ... + 4 more platforms
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .goreleaser.yaml + .github/workflows/release.yaml</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 836-857 goreleaser config + lines 894-895 workflow guidance)
    - .github/workflows/ci.yaml (existing workflow shape — Go setup, checkout patterns)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (lines 110-118 GREENFIELD — no prior goreleaser in repo)
  </read_first>
  <acceptance_criteria>
    - `.goreleaser.yaml` exists at repo root and `goreleaser check` exits 0 (run via Docker-based `goreleaser/goreleaser:latest` if not locally installed: `docker run --rm -v $(pwd):/work -w /work goreleaser/goreleaser:latest check`).
    - `.goreleaser.yaml` defines exactly 1 build (id: tide), 1 archive (id: tide), and the 5 OS/arch combos: linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64. (Windows amd64 only; windows/arm64 dropped — not a K8s operator audience per CLAUDE.md.)
    - `.goreleaser.yaml` injects version via `ldflags: ['-s -w -X main.version={{.Version}}']` matching the cobra root's `var version` declaration from plan 04-07.
    - `.goreleaser.yaml` includes a `snapshot:` stanza for `goreleaser release --snapshot --skip publish` dev-mode runs (used by `make release-snapshot` target).
    - `.github/workflows/release.yaml` triggers ONLY on tag push matching `v*` (NOT on PR / main push — release is intentional manual action).
    - Workflow checks out source, sets up Go 1.26, runs `goreleaser/goreleaser-action@v6` with `args: release --clean`, supplies `GITHUB_TOKEN` env from `secrets.GITHUB_TOKEN`.
    - Workflow includes a job step `goreleaser check` BEFORE release to fail-fast on config errors.
    - Add `Makefile` target `release-snapshot: goreleaser release --snapshot --skip publish --clean` for local dev verification.
  </acceptance_criteria>
  <action>
    Create `.goreleaser.yaml` at repo root matching the interfaces block above. Notable settings to add for v1.0 ops:

    - `before: { hooks: ['go mod tidy', 'go build ./cmd/tide-lint', 'make tide-lint'] }` — fail the release if lint diagnostics exist.
    - `release: { prerelease: auto, mode: replace }` — v0.x tags treated as prerelease automatically.
    - `archives[0].files: ['LICENSE', 'README.md']` — include license + readme in tarballs.

    Do NOT add SLSA provenance or sigstore signing to v1.0 — assumption A4 already flags goreleaser v2 + Go 1.26 as MEDIUM confidence; keep config simple, deferring supply-chain hardening to v1.x per RESEARCH §A4 / Phase 5 docs.

    Create `.github/workflows/release.yaml`:
    ```yaml
    name: release
    on:
      push:
        tags: ['v*']
    permissions:
      contents: write   # for GitHub Release creation
    jobs:
      release:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
            with: { fetch-depth: 0 }
          - uses: actions/setup-go@v5
            with: { go-version: '1.26' }
          - uses: goreleaser/goreleaser-action@v6
            with: { distribution: goreleaser, version: '~> v2', args: 'check' }
          - uses: goreleaser/goreleaser-action@v6
            with: { distribution: goreleaser, version: '~> v2', args: 'release --clean' }
            env: { GITHUB_TOKEN: '${{ secrets.GITHUB_TOKEN }}' }
    ```

    Append `release-snapshot:` to the Makefile: `goreleaser release --snapshot --skip publish --clean`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; (which goreleaser &gt;/dev/null &amp;&amp; goreleaser check || docker run --rm -v $(pwd):/work -w /work goreleaser/goreleaser:latest check) &amp;&amp; ls .goreleaser.yaml .github/workflows/release.yaml</automated>
  </verify>
  <done>goreleaser check exits 0; workflow file exists; tag-only trigger configured; snapshot mode available via Makefile.</done>
</task>

<task type="auto">
  <name>Task 2: krew-plugins/tide.yaml + docs/cli.md</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 860-895 Krew v1alpha2 schema + lines 580-591 Pitfall 27 Krew naming)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-C2 + D-C3)
  </read_first>
  <acceptance_criteria>
    - `krew-plugins/tide.yaml` is a valid Krew v1alpha2 Plugin manifest with `apiVersion: krew.googlecontainertools.github.com/v1alpha2` (NOT v1alpha1; NOT a future schema).
    - Manifest declares 5 platforms (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64) — same set as the goreleaser archives.
    - Each platform's `uri` references the goreleaser-uploaded tarball: `https://github.com/jsquirrelz/tide/releases/download/{{ .TagName }}/tide_{{ .TagName }}_<os>_<arch>.<ext>` where ext is `.tar.gz` for linux/darwin and `.zip` for windows.
    - Each platform's `sha256` is a templated value `'{{ .Sha256 }}'` for krew-release-bot to fill in.
    - The `bin: tide` field is set so Krew creates a `kubectl-tide` symlink in `~/.krew/bin/`.
    - `docs/cli.md` exists, documents both `tide` and `kubectl tide` invocation forms, lists every D-C3 verb with one-line description + example invocation, and includes a "Pitfall 27" note about completion-script generation matching the binary name.
    - `kubectl-validate` or basic YAML lint passes on the manifest (use `yamllint krew-plugins/tide.yaml` or `python3 -c 'import yaml; yaml.safe_load(open("krew-plugins/tide.yaml"))'`).
  </acceptance_criteria>
  <action>
    Create `krew-plugins/tide.yaml`:
    ```yaml
    apiVersion: krew.googlecontainertools.github.com/v1alpha2
    kind: Plugin
    metadata:
      name: tide
    spec:
      version: "{{ .TagName }}"
      homepage: https://github.com/jsquirrelz/tide
      shortDescription: Manage TIDE Projects on Kubernetes
      description: |
        tide is the CLI for TIDE — a Kubernetes-native orchestrator for hierarchical
        agentic coding work organized as a five-level paradigm (Project → Milestone →
        Phase → Plan → Task) with topologically-derived Waves.

        Common verbs:
          kubectl tide apply -f project.yaml      # apply a Project CRD
          kubectl tide watch <project>             # live status
          kubectl tide tail <task>                 # stream pod logs
          kubectl tide approve <project>           # advance an awaiting-approval level
          kubectl tide inspect-wave <plan>         # tabular wave view
          kubectl tide describe-budget <project>   # budget status

      caveats: |
        After installation, run `kubectl tide completion bash > /etc/bash_completion.d/kubectl-tide`
        (or zsh / fish / powershell equivalent) for shell completion.

      platforms:
        - selector: { matchLabels: { os: linux, arch: amd64 } }
          uri: https://github.com/jsquirrelz/tide/releases/download/{{ .TagName }}/tide_{{ .TagName }}_linux_amd64.tar.gz
          sha256: "{{ .Sha256 }}"
          files: [{ from: tide, to: . }]
          bin: tide
        - selector: { matchLabels: { os: linux, arch: arm64 } }
          uri: https://github.com/jsquirrelz/tide/releases/download/{{ .TagName }}/tide_{{ .TagName }}_linux_arm64.tar.gz
          sha256: "{{ .Sha256 }}"
          files: [{ from: tide, to: . }]
          bin: tide
        - selector: { matchLabels: { os: darwin, arch: amd64 } }
          uri: https://github.com/jsquirrelz/tide/releases/download/{{ .TagName }}/tide_{{ .TagName }}_darwin_amd64.tar.gz
          sha256: "{{ .Sha256 }}"
          files: [{ from: tide, to: . }]
          bin: tide
        - selector: { matchLabels: { os: darwin, arch: arm64 } }
          uri: https://github.com/jsquirrelz/tide/releases/download/{{ .TagName }}/tide_{{ .TagName }}_darwin_arm64.tar.gz
          sha256: "{{ .Sha256 }}"
          files: [{ from: tide, to: . }]
          bin: tide
        - selector: { matchLabels: { os: windows, arch: amd64 } }
          uri: https://github.com/jsquirrelz/tide/releases/download/{{ .TagName }}/tide_{{ .TagName }}_windows_amd64.zip
          sha256: "{{ .Sha256 }}"
          files: [{ from: tide.exe, to: . }]
          bin: tide.exe
    ```

    Create `docs/cli.md` (Phase 4 stub — Phase 5 finalizes):
    - Section 1: Install — three paths (`go install`, `goreleaser tarball + place in $PATH`, `kubectl krew install tide`).
    - Section 2: Invocation forms — `tide ...` vs `kubectl tide ...`, citing Pitfall 27.
    - Section 3: Verb reference — one block per D-C3 verb with usage + 1-line description + 1 example invocation (10 verbs + completion = 11 entries).
    - Section 4: Completion — three sub-blocks (bash, zsh, fish) showing the install line; note that completion script's binary name matches `os.Args[0]` (Pitfall 27).
    - Section 5: Troubleshooting — if `kubectl tide` doesn't work after Krew install, run `kubectl plugin list | grep tide` and verify `~/.krew/bin/kubectl-tide` exists.

    For sha256 templating: the placeholder `"{{ .Sha256 }}"` is intentional — the `krew-release-bot` GitHub Action (deferred to Phase 5 wire-up) fills these in at release time by reading the goreleaser `checksums.txt`. Add a short comment block in the manifest explaining this.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('krew-plugins/tide.yaml'))" &amp;&amp; ls docs/cli.md &amp;&amp; grep -c "kubectl tide" docs/cli.md | grep -E "^\s*[1-9]" &amp;&amp; grep -c "Pitfall 27" docs/cli.md</automated>
  </verify>
  <done>Krew manifest parses as valid YAML and has 5 platforms; docs/cli.md documents both invocation forms and references Pitfall 27.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Goreleaser → GitHub Releases | Multi-OS binaries trust the GitHub-hosted runner's environment. No code signing in v1.0 (Apple notarization, sigstore/cosign deferred per RESEARCH §A4). |
| Krew manifest → operator's machine | Krew downloads tarballs by URL+sha256; sha256 mismatch fails install. krew-release-bot fills sha256 from checksums.txt — that automation is the v1.x trust anchor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-C2 | Tampering / supply chain | krew-plugins/tide.yaml | mitigate (partial) | Krew verifies sha256 against the manifest. v1.0 ships without cosign signatures — documented in docs/cli.md as a known v1.x hardening item. |
| T-04-Krew | Spoofing (kubectl-tide name confusion) | docs/cli.md + cmd/tide/main.go | mitigate | cobra root Use: filepath.Base(os.Args[0]) (per plan 04-07) so help text auto-aligns with invocation name. Pitfall 27 documented. |
| T-04-Release-leak | Info disclosure (build env) | .goreleaser.yaml | accept | goreleaser ldflags strip debug symbols (-s -w) — no source paths in binaries. No secrets baked at build time (per CLAUDE.md, ANTHROPIC_API_KEY is runtime-only). |
</threat_model>

<verification>
- `.goreleaser.yaml` passes `goreleaser check` (or via Docker if not installed locally).
- `.github/workflows/release.yaml` is syntactically valid (`yamllint .github/workflows/release.yaml` or GitHub's actionlint).
- `krew-plugins/tide.yaml` parses as valid YAML (`python3 -c "import yaml; yaml.safe_load(open('krew-plugins/tide.yaml'))"`).
- `docs/cli.md` has 5 sections and references Pitfall 27.
- `make release-snapshot` (or equivalent goreleaser invocation in snapshot mode) produces dist/ tarballs without errors.
</verification>

<success_criteria>
- 4 new files committed (.goreleaser.yaml, release.yaml workflow, Krew manifest, cli.md).
- Goreleaser config covers 5 OS/arch combos with version-injected ldflags.
- Release workflow is tag-gated (no accidental release on PR).
- Krew manifest is v1alpha2-compliant with templated sha256.
- docs/cli.md documents both invocation forms + each D-C3 verb.
- Phase 5 can cut v1.0 by simply pushing a `v1.0.0` tag.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-09-SUMMARY.md` documenting the goreleaser build matrix, the workflow trigger pattern, the Krew manifest's platform set, and what Phase 5 needs to do at tag time (push tag → workflow runs goreleaser → checksums.txt published → krew-release-bot is wired in Phase 5 to PR the Krew index).
</output>
