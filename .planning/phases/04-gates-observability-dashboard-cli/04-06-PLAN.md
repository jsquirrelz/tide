---
phase: 04
plan: 06
type: execute
wave: 4
depends_on: [04-01, 04-04, 04-05]
files_modified:
  - internal/controller/project_controller.go
  - internal/controller/project_controller_test.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/push_helpers.go
  - internal/controller/push_helpers_test.go
  - test/integration/envtest/boundary_push_test.go
  - test/integration/envtest/leak_blocked_test.go
autonomous: true
requirements: [OBS-02, OBS-01]
threat_model_id: T-04-W1,T-04-W2
must_haves:
  truths:
    - "Push Job exit code 10 (gitleaks finding, envelope.Reason=\"leak-detected\") causes ProjectReconciler to patch Status.Phase=PhasePushLeakBlocked, set Condition PushLeakBlocked True with Reason=LeakDetected, AND increment `metrics.SecretLeakBlockedTotal{project, phase, plan}` counter by 1"
    - "Push Job exit code 11 (lease rejection, envelope.Reason=\"lease-rejected\") preserves today's behavior: Status.Phase=PhasePushLeaseFailed, Condition PushLeaseFailed True with Reason=LeaseRejected"
    - "MilestoneReconciler dispatches a `tide-push-<project-uid>` Job at the all-children-Succeeded boundary with commit-message `tide: milestone <name> authored` (D-B2 shape)"
    - "PhaseReconciler does the same at its boundary with `tide: phase <name> authored`"
    - "PlanReconciler does the same at its boundary with `tide: plan <name> authored + executed`"
    - "Push Job creation at mid-stack boundaries uses the same deterministic name `tide-push-<project-uid>` (D-B5 serialization — multiple boundaries in flight collapse to one push Job per Project; K8s AlreadyExists handles the race)"
    - "Counter increment is bounded: `SecretLeakBlockedTotal.WithLabelValues(projectName, phase, plan).Inc()` uses ONLY {project, phase, plan} labels — phase + plan extracted from the project's currently-running level (empty string when at top-level)"
  artifacts:
    - path: internal/controller/project_controller.go
      provides: "isJobFailed → exit-code-reason split mapping leak-detected → PhasePushLeakBlocked and lease-rejected → PhasePushLeaseFailed; PodStatusEnvelope reader for push job"
      contains: "metrics.SecretLeakBlockedTotal"
    - path: internal/controller/milestone_controller.go
      provides: "maybeTriggerBoundaryPush(ctx, ms, project) helper called from handleJobCompletion AFTER gate-policy passes"
      contains: "maybeTriggerBoundaryPush"
    - path: internal/controller/push_helpers.go
      provides: "buildPushJob already exists (Phase 3) — extend signature OR add a wrapper for mid-stack callers using parent.UID-only Project-namespaced Job; PushOptions struct already supports CommitMessage"
      contains: "buildPushJob"
    - path: test/integration/envtest/leak_blocked_test.go
      provides: "TestProject_LeakBlocked — push Job exit-10 path drives Status.Phase=PushLeakBlocked + counter increment"
      contains: "PushLeakBlocked"
    - path: test/integration/envtest/boundary_push_test.go
      provides: "TestBoundaryPush_AllLevels — Milestone/Phase/Plan boundaries each dispatch tide-push with correct commit message"
      contains: "buildCommitMessage"
  key_links:
    - from: internal/controller/project_controller.go
      to: internal/metrics.SecretLeakBlockedTotal
      via: "Inc() on push-Job exit-10 path"
      pattern: "metrics\\.SecretLeakBlockedTotal"
    - from: internal/controller/milestone_controller.go
      to: internal/gates.BoundaryDetected
      via: "maybeTriggerBoundaryPush consults BoundaryDetected for child Phases"
      pattern: "gates\\.BoundaryDetected"
    - from: internal/controller/push_helpers.go
      to: cmd/tide-push push envelope reason field
      via: "writePushEnvelope writes Reason; ProjectReconciler reads via PodStatusEnvelope or fallback"
      pattern: "Reason"
---

<objective>
Close out Phase 3's two deferred work items: W-1 (`tide_secret_leak_blocked_total` counter wiring on push exit-10 path + PhasePushLeakBlocked phase constant — landed in plan 04-01) and W-2 (mid-stack boundary push triggers in Milestone, Phase, Plan reconcilers). Both items share the same seam — the post-MaterializeChildCRDs / post-gate-policy point in each up-stack reconciler — which is exactly the seam already opened by plan 04-05. This plan extends that seam with W-1 envelope-reason parsing and W-2 push-Job dispatch.

Purpose: D-W2 explicitly co-locates with D-G2 ("the same boundary-detection logic that consults gate policy is the place that triggers push"). By executing plan 04-05 first and this plan immediately after in the same wave, the reconciler diff in each level is contiguous and audit-able: gate hook → boundary push trigger → patchSucceeded.

Output: project_controller.go gains exit-10 vs exit-11 envelope-reason parsing + counter increment; milestone/phase/plan reconcilers gain maybeTriggerBoundaryPush helpers; push_helpers may need a `buildPushJobForLevel` variant accepting a non-Project owner; new envtest at test/integration/envtest/leak_blocked_test.go + boundary_push_test.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-01-PLAN.md
@.planning/phases/04-gates-observability-dashboard-cli/04-05-PLAN.md
@.planning/phases/03-up-stack-reconcilers-git-integration-real-subagent-resumptio/03-08-SUMMARY.md
@internal/controller/project_controller.go
@internal/controller/milestone_controller.go
@internal/controller/phase_controller.go
@internal/controller/plan_controller.go
@internal/controller/push_helpers.go
@cmd/tide-push/main.go

<interfaces>
<!-- Phase 3 push envelope schema -->

From cmd/tide-push/main.go (push-result envelope written at line 403-430):
```go
type pushResult struct {
    Kind       string    `json:"kind"`        // "PushResult"
    PushedHash string    `json:"pushedHash"`  // SHA of HEAD pushed (empty on early exit)
    ExitCode   int       `json:"exitCode"`    // 0, 2, 10, 11, 12, 13, 1
    Reason     string    `json:"reason"`      // "leak-detected", "lease-rejected", "auth-failed", "network-timeout", "invalid-branch", "missing-creds", "missing-commit-message", etc.
    Timestamp  time.Time `json:"timestamp"`
}
```
The push Job writes this file to `<workspace>/envelopes/push/<project-uid>.json` AND to `terminationMessagePath` (per K8s podtermination convention) for direct ProjectReconciler pickup.

From internal/controller/push_helpers.go (existing):
```go
func buildCommitMessage(level string, name string) (string, error)  // returns one of the 4 D-B2 shapes
func buildPushJob(project *v1alpha1.Project, pvcName string, opts PushOptions, scheme *runtime.Scheme) *batchv1.Job
type PushOptions struct {
    TidePushImage  string
    Branch         string
    LastPushedSHA  string
    CommitMessage  string
    LeaksConfigMap string
}
```

From internal/controller/project_controller.go (lines 425-443 — current isJobFailed → PushLeaseFailed mapping that this plan splits):
```go
} else if isJobFailed(&existingPush) {
    project.Status.Phase = tideprojectv1alpha1.PhasePushLeaseFailed
    project.Status.Git.LeaseFailureCount++
    meta.SetStatusCondition(&project.Status.Conditions, metav1.Condition{
        Type: tideprojectv1alpha1.ConditionPushLeaseFailed,
        Status: metav1.ConditionTrue,
        Reason: "LeaseRejected",
        Message: "Push Job ... rejected by --force-with-lease",
    })
    r.Status().Patch(ctx, project, patch)
}
```

This is the seam to split into exit-10 (leak) + exit-11 (lease) + fallback (other) arms via envelope.Reason field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: W-1 — Push envelope reason parsing + PhasePushLeakBlocked branch + counter</name>
  <read_first>
    - internal/controller/project_controller.go (lines 380-446 — push job creation + isJobFailed handling)
    - cmd/tide-push/main.go (lines 80-100 envelope schema; lines 320-345 exit-reason mapping; lines 399-430 writePushEnvelope)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (section M — W-1 exit-code mapping)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-W1)
    - internal/metrics/registry.go (from plan 04-01 — SecretLeakBlockedTotal)
  </read_first>
  <behavior>
    - Test 1: Given a push Job that completed with exit code 10 and the PVC envelope file at `<workspace>/envelopes/push/<project-uid>.json` containing `{"kind":"PushResult","reason":"leak-detected","exitCode":10}` (or equivalently via terminationMessagePath), ProjectReconciler's isJobFailed branch patches Status.Phase=PhasePushLeakBlocked, sets Condition PushLeakBlocked True with Reason=LeakDetected.
    - Test 2: After the test 1 patch, `testutil.ToFloat64(metrics.SecretLeakBlockedTotal.WithLabelValues(projectName, "", ""))` returns 1.0. The phase/plan labels are empty strings (no in-flight level scope at Project boundary — documented).
    - Test 3: Given exit code 11 + reason "lease-rejected", ProjectReconciler preserves today's behavior: Status.Phase=PhasePushLeaseFailed + ConditionPushLeaseFailed True.
    - Test 4: Given any other exit code with empty/unknown reason, ProjectReconciler falls back to PhasePushLeaseFailed (today's default) so the existing recovery annotation (`tideproject.k8s/bypass-push-lease=true`) still works.
    - Test 5: Counter for SecretLeakBlockedTotal does NOT increment on exit code 11 (no false positive on lease failures). Assert via testutil.ToFloat64 returning 0.0 after a lease-failure test run.
    - Test 6: A new `ConditionPushLeakBlocked = "PushLeakBlocked"` is added to api/v1alpha1/shared_types.go (plan 04-01 didn't add this — add it here under "Phase 4 W-1 follow-up").
  </behavior>
  <action>
    Add `ConditionPushLeakBlocked = "PushLeakBlocked"` to `api/v1alpha1/shared_types.go` (next to ConditionPushLeaseFailed).

    Modify `cmd/tide-push/main.go` to ALSO write the push envelope to `/dev/termination-log` (K8s terminationMessagePath default) — currently it only writes to the PVC. Adding terminationMessage write means ProjectReconciler can read the Reason without needing PVC mount. (If terminationMessagePath isn't already configured on the push Job's spec, add `TerminationMessagePath: "/dev/termination-log"` to the container spec in `buildPushJob` and `TerminationMessagePolicy: corev1.TerminationMessageFallbackToLogsOnError` for safety.) Cap the terminationMessage write at 4KB (K8s limit); the JSON envelope is small (<200 bytes), well under.

    In `internal/controller/project_controller.go`, modify the `isJobFailed(&existingPush)` arm (lines 425-443) to:
    1. Read the push Job's pod via `r.List(ctx, &podList, client.MatchingLabels{"job-name": pushJobName}, client.InNamespace(project.Namespace))` and pick the first pod's `Status.ContainerStatuses[0].State.Terminated.Message`.
    2. Parse it as `pushResult` JSON (use a local mirror of cmd/tide-push's pushResult type — or extract to a shared `internal/pushresult/` types package if cleaner; both acceptable, choose smaller diff).
    3. Switch on `pushResult.Reason`:
       - `"leak-detected"`: patch Status.Phase=PhasePushLeakBlocked + Condition PushLeakBlocked True (Reason="LeakDetected", Message="Push Job <name> blocked by gitleaks: secret pattern detected in diff"); `metrics.SecretLeakBlockedTotal.WithLabelValues(project.Name, "", "").Inc()`.
       - `"lease-rejected"`: preserve existing PhasePushLeaseFailed path (no behavior change).
       - default (empty / unknown reason / pod-status read failed): fall back to PhasePushLeaseFailed for backward compat.
    4. If terminationMessage read fails entirely (no pod, no message): increment a `metrics.PushJobsTotal.WithLabelValues(project.Name, "internal").Inc()` counter for observability and fall back to PhasePushLeaseFailed.

    Extend `internal/controller/project_controller_test.go` with 6 subtests covering tests 1-6 above. Use the envtest scaffolding + fake pod creation with synthetic terminationMessage JSON.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./internal/controller/... -run "PushLeak|PushLease" -race -v &amp;&amp; grep -c "metrics.SecretLeakBlockedTotal" internal/controller/project_controller.go</automated>
  </verify>
  <done>Exit-10 leak path increments SecretLeakBlockedTotal + sets PhasePushLeakBlocked; exit-11 lease path unchanged; fallback preserves recovery annotation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: W-2 — Mid-stack boundary push triggers in Milestone, Phase, Plan reconcilers</name>
  <read_first>
    - internal/controller/project_controller.go (lines 380-413 — existing push-Job creation at PhaseComplete; this is the template for the maybeTriggerBoundaryPush helper)
    - internal/controller/push_helpers.go (entire file — buildCommitMessage + buildPushJob + PushOptions)
    - internal/gates/boundary.go (from plan 04-04 — BoundaryDetected shared seam)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (section L — boundary push trigger pattern)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-W2 + D-B2 commit-message shapes)
  </read_first>
  <behavior>
    - Test 1 (Milestone boundary): When all child Phases of a Milestone reach Status.Phase=Succeeded, MilestoneReconciler.handleJobCompletion (post-gate-policy) calls maybeTriggerBoundaryPush which creates a `tide-push-<project-uid>` Job (AlreadyExists is tolerated — D-B5 serialization). The Job's container args include the commit message `tide: milestone <name> authored` (per buildCommitMessage("milestone", ms.Name)).
    - Test 2 (Phase boundary): Same for Phase → `tide: phase <name> authored`.
    - Test 3 (Plan boundary): Same for Plan → `tide: plan <name> authored + executed` (the only D-B2 shape with `+ executed` suffix).
    - Test 4 (idempotency): On a second reconcile after the push Job already exists, AlreadyExists is tolerated; no panic; no second Job created.
    - Test 5 (gate skips push): When Gates.Milestone="approve" and the gate is NOT yet approved, maybeTriggerBoundaryPush MUST NOT fire (it's downstream of the gate hook in the reconciler body — execution sequence preserves the D-G2 + D-W2 ordering).
    - Test 6 (reject skips push): When the Project has reject annotation, the push is also skipped (reject short-circuit upstream of maybeTriggerBoundaryPush).
  </behavior>
  <action>
    Add a private helper `maybeTriggerBoundaryPush(ctx context.Context, parent client.Object, project *tideprojectv1alpha1.Project) error` to each of `internal/controller/milestone_controller.go`, `phase_controller.go`, `plan_controller.go`. The helper:

    1. If `project.Spec.Git == nil || project.Spec.Git.RepoURL == ""`, return nil immediately (no git config → no push — preserve today's Project-boundary guard at project_controller.go:385).
    2. Determine boundary level from parent's type via type-switch: `*Milestone → "milestone"`, `*Phase → "phase"`, `*Plan → "plan"`.
    3. Verify boundary actually detected — for milestone the child kind is "Phase"; for phase, "Plan"; for plan, "Task". Use `gates.BoundaryDetected(ctx, r.Client, parent, childKind)`. If false, return nil.
    4. Compute `pushJobName := fmt.Sprintf("tide-push-%s", project.UID)` (D-B5 serialization — same name as Project boundary; AlreadyExists is the synchronization mechanism per RESEARCH §1091).
    5. `r.Get` to check existence; if AlreadyExists, return nil (idempotent).
    6. `msg, err := buildCommitMessage(level, parent.GetName())` — already exists in push_helpers.go.
    7. Build PushOptions with `TidePushImage: r.TidePushImage, Branch: project.Status.Git.BranchName, LastPushedSHA: project.Status.Git.LastPushedSHA, CommitMessage: msg, LeaksConfigMap: project.Spec.Git.LeaksConfigRef`. Call `buildPushJob(project, pvcName, pushOpts, r.Scheme)` — this constructs the same Job shape as the Project-boundary push. (The Job is owned by Project regardless of which level triggered it — owner ref keeps cleanup semantics intact.)
    8. `r.Create(ctx, pushJob)` with AlreadyExists tolerated.
    9. Log the dispatch with structured fields project/phase/plan, also increment `metrics.PushJobsTotal.WithLabelValues(project.Name, "dispatched").Inc()` (or "success" — pick label from D-O2 outcome enum; "dispatched" may need a constant addition documented in 04-01 — if the metric outcomes don't include "dispatched", use a transitional value or add the constant; keep choice consistent across plans).

    INSERT the call `if err := r.maybeTriggerBoundaryPush(ctx, ms, project); err != nil { return ctrl.Result{}, err }` in each up-stack reconciler's handleJobCompletion AFTER the gate-policy pass (from plan 04-05) and BEFORE patchMilestoneSucceeded. This co-located seam is exactly what D-W2 prescribes.

    Tests in milestone_controller_test.go / phase_controller_test.go / plan_controller_test.go: `TestBoundaryPush_TriggersAtMilestoneBoundary` etc. — use envtest, create Project + child CRDs, mark children Succeeded, drive a reconcile, Eventually assert a `tide-push-<project-uid>` Job exists with the correct args. Use the existing push-Job assertion patterns from `project_phase3_test.go`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./internal/controller/... -run "BoundaryPush" -race -v &amp;&amp; grep -c "maybeTriggerBoundaryPush" internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go</automated>
  </verify>
  <done>All three up-stack reconcilers dispatch boundary push Jobs with correct D-B2 commit messages; AlreadyExists tolerated; gate + reject upstream guards work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test/integration/envtest/{leak_blocked,boundary_push}_test.go — end-to-end envtest coverage</name>
  <read_first>
    - internal/controller/project_phase3_test.go (Phase 3's push-Job assertion patterns)
    - test/integration/envtest/gates_test.go (from plan 04-05 — same envtest scaffolding)
    - .planning/phases/04-gates-observability-dashboard-cli/04-VALIDATION.md (W-1 / W-2 entries)
    - internal/metrics/registry.go (SecretLeakBlockedTotal target)
  </read_first>
  <behavior>
    - Test 1 (TestProject_LeakBlocked): Apply a Project with `spec.git` configured; create a Pod with terminationMessage `{"kind":"PushResult","reason":"leak-detected","exitCode":10}` labeled to match the push Job; mark the push Job's Status.Failed=1. Eventually: project.Status.Phase=PhasePushLeakBlocked AND `testutil.ToFloat64(metrics.SecretLeakBlockedTotal.WithLabelValues(project.Name, "", ""))` returns ≥ 1.0.
    - Test 2 (TestProject_LeaseFailed_NoLeakCounter): Same but reason="lease-rejected". Eventually project.Status.Phase=PhasePushLeaseFailed AND SecretLeakBlockedTotal counter UNCHANGED.
    - Test 3 (TestBoundaryPush_AllLevels): Apply Project → Milestone → Phase → Plan → 3 Tasks. Mark Tasks Succeeded; observe Plan boundary push Job; mark Plans Succeeded; observe Phase boundary push Job; mark Phases Succeeded; observe Milestone boundary push Job. Assert all 3 boundary push Jobs share the same `tide-push-<project-uid>` name (AlreadyExists serialization), and via Pod label inspection the commit message arg matches the D-B2 shape per boundary level.
  </behavior>
  <action>
    Create `test/integration/envtest/leak_blocked_test.go` (separate file from gates_test.go because envtest assertions are cross-cutting; this file owns the W-1 scenario). Mirror the suite_test.go setup from plan 04-05's test/integration/envtest directory. Tests 1+2 share a namespace setup but isolate by Project name. Use `testutil.ToFloat64(metrics.SecretLeakBlockedTotal.WithLabelValues(...))` directly (the metric is package-level public).

    Create `test/integration/envtest/boundary_push_test.go` for W-2 coverage. Construct a multi-level fixture (Project → Milestone with 1 Phase with 1 Plan with 3 Tasks). Use `client.Patch` to set child statuses to Succeeded in cascade. Use Gomega Eventually with 30s timeout polling Job list. Assert commit message via inspecting the push Job's pod template container `Args` field — the Args array contains the `--commit-message` flag with the expected value.

    NOTE on commit message inspection: `buildPushJob` (Phase 3) passes the commit message as an env var or container arg — confirm via `grep -n "CommitMessage\|commit-message" internal/controller/push_helpers.go` and inspect the field accordingly. The assertion should be a direct equality check on the env value or arg substring, not a regex.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./test/integration/envtest/... -run "LeakBlocked|BoundaryPush" -race -timeout 5m -v</automated>
  </verify>
  <done>Two envtest files cover W-1 leak counter + Phase constant transition, and W-2 mid-stack push with correct D-B2 commit messages at all three levels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Push Job pod → orchestrator | The push Job's exit reason is operator-visible truth for "what blocked the push" — incorrect reason mapping silently mis-categorizes leak attempts as lease failures (T-04-W1) |
| Boundary detection → push dispatch | Multiple reconcilers may detect a boundary in the same tick (e.g., a Milestone whose only Phase just Succeeded); deterministic Job naming + AlreadyExists tolerance prevents duplicate push attempts (D-B5 serialization) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W1 | Info disclosure (silent leak) | internal/controller/project_controller.go | mitigate | Exit-10 vs exit-11 split via envelope.Reason; PhasePushLeakBlocked is operator-visible at `kubectl get project`; counter `tide_secret_leak_blocked_total` is operator-alertable. |
| T-04-W2 | Tampering (missing commit) | internal/controller/{milestone,phase,plan}_controller.go | mitigate | Mid-stack boundary triggers fire at all three levels — artifacts at Milestone/Phase/Plan completion are committed on the per-run branch with structured D-B2 commit messages. |
| T-04-W2-race | DoS (duplicate push) | internal/controller/push_helpers.go | mitigate | Deterministic Job name `tide-push-<project-uid>` per project (D-B5); AlreadyExists tolerated; concurrent boundary detections collapse to one push Job carrying the diff. |
| T-04-W1-bypass | Info disclosure (counter never fires) | internal/controller/project_controller.go | mitigate | Test 5 of Task 1 asserts counter remains 0 on lease-rejected path — guards against silent counter mis-wiring. |
</threat_model>

<verification>
- `go test ./internal/controller/... -run "PushLeak|PushLease|BoundaryPush" -race -v` passes.
- `go test ./test/integration/envtest/... -run "LeakBlocked|BoundaryPush" -race -timeout 5m -v` passes.
- `grep -c "metrics.SecretLeakBlockedTotal" internal/controller/project_controller.go` returns ≥ 1.
- `grep -c "maybeTriggerBoundaryPush" internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go` returns ≥ 3.
- `grep -c "PhasePushLeakBlocked" internal/controller/project_controller.go` returns ≥ 1.
- `make tide-lint` passes.
</verification>

<success_criteria>
- W-1: Push exit-10 (leak) correctly drives Status.Phase=PhasePushLeakBlocked + SecretLeakBlockedTotal counter increment; exit-11 (lease) preserves today's behavior.
- W-2: Milestone, Phase, Plan boundaries each dispatch the appropriate D-B2-shaped commit-message push Job.
- Idempotent dispatch via deterministic Job name + AlreadyExists tolerance.
- Gate + reject upstream guards correctly short-circuit boundary push.
- Envtest coverage for both W-1 and W-2 scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-06-SUMMARY.md` documenting: the envelope-reason-to-phase-constant mapping table; the three new maybeTriggerBoundaryPush helpers and their level-to-childKind mapping; the AlreadyExists serialization argument from D-B5/RESEARCH §1091; the envtest scenarios covered.
</output>
