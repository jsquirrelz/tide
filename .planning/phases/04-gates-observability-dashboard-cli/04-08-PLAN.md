---
phase: 04
plan: 08
type: execute
wave: 3
depends_on: [04-04, 04-07]
files_modified:
  - cmd/tide/approve.go
  - cmd/tide/reject.go
  - cmd/tide/cancel.go
  - cmd/tide/resume.go
  - cmd/tide/tail.go
  - cmd/tide/approve_test.go
  - cmd/tide/reject_test.go
  - cmd/tide/cancel_test.go
  - cmd/tide/resume_test.go
  - cmd/tide/tail_test.go
autonomous: true
requirements: [GATE-03, CLI-04]
threat_model_id: T-04-G2,T-04-G4,T-04-C4
must_haves:
  truths:
    - "`tide approve <project>` writes annotation `tideproject.k8s/approve-milestone: \"true\"` on the Project's current AwaitingApproval level — discovers the level from Project.Status conditions; if --wave <plan>/<N> given, writes `tideproject.k8s/approve-wave-<N>: \"true\"` on the Plan instead"
    - "`tide reject <project> [--reason ...]` writes annotation `tideproject.k8s/reject: <reason>` on the Project (defaults reason to \"rejected by operator\")"
    - "`tide cancel <project> --force` deletes the Project CRD with PropagationPolicyForeground (cascades to children); requires explicit --force or RunE returns an error"
    - "`tide resume <project>` clears the `tideproject.k8s/reject` annotation via gates.ConsumeReject + client.Patch"
    - "`tide tail <task>` streams logs via clientset.CoreV1().Pods(ns).GetLogs(podName, &PodLogOptions{Follow: true}).Stream(ctx); Ctrl-C cancels the stream within 1s (Pitfall 25); container picked = first non-credproxy/non-init container OR via --container flag"
    - "All annotation writes use client.Patch with MergeFrom against a deep-copied original — no full Update, no JSON-merge patches, no race against reconciler's annotation reads"
  artifacts:
    - path: cmd/tide/approve.go
      provides: "tide approve [--wave plan/N] subcommand"
      contains: "AnnotationApprovePrefix"
    - path: cmd/tide/tail.go
      provides: "tide tail subcommand using clientset.CoreV1().Pods().GetLogs(..., Follow:true).Stream(ctx)"
      contains: "GetLogs"
    - path: cmd/tide/cancel.go
      provides: "tide cancel --force using client.Delete(ctx, project, client.PropagationPolicy(metav1.DeletePropagationForeground))"
      contains: "DeletePropagationForeground"
  key_links:
    - from: cmd/tide/approve.go
      to: internal/gates
      via: "reuses gates.AnnotationApprovePrefix + gates.AnnotationApproveWavePrefix constants"
      pattern: "gates\\.Annotation"
    - from: cmd/tide/tail.go
      to: k8s.io/client-go/kubernetes
      via: "clientset.CoreV1().Pods(ns).GetLogs(name, opts).Stream(ctx)"
      pattern: "GetLogs"
---

<objective>
Replace the four annotation-writer stubs and the tail stub from plan 04-07 with real implementations. These five verbs are the operator's mutation surface to the cluster — all writes go through K8s API via client-go (no `kubectl exec` shell-out), all use `client.Patch` with MergeFrom for one-shot annotation semantics that mirror the reconciler's read pattern (plan 04-04). `tide tail` streams pod logs via the canonical `pods/log` subresource with signal-aware context cancellation.

Purpose: Cleanly close the loop between CLI annotation writes (this plan) and reconciler annotation reads (plan 04-04/04-05) — same constants, same one-shot semantics, no string drift. `tide tail` proves the apiserver streaming pattern that the dashboard's pod-log SSE handler will mirror in plan 04-11.

Output: 5 cobra subcommands fully implemented + per-subcommand tests using a fake client + a focused integration test for `tide tail` against a stub pod (or deferred to plan 04-14's kind harness).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-04-PLAN.md
@.planning/phases/04-gates-observability-dashboard-cli/04-07-PLAN.md
@internal/gates/annotation.go
@internal/controller/project_controller.go
@cmd/tide/main.go
@cmd/tide/root_flags.go

<interfaces>
<!-- From plan 04-04 — annotation constants re-exported for reuse -->

From internal/gates/annotation.go:
```go
const (
    AnnotationApprovePrefix     = "tideproject.k8s/approve-"      // + level (milestone|phase|plan|task)
    AnnotationApproveWavePrefix = "tideproject.k8s/approve-wave-" // + N (integer)
    AnnotationReject            = "tideproject.k8s/reject"
)
```

For pods/log streaming (CLI-04):
```go
import (
    "k8s.io/client-go/kubernetes"
    corev1 "k8s.io/api/core/v1"
)

req := clientset.CoreV1().Pods(ns).GetLogs(podName, &corev1.PodLogOptions{
    Follow:     true,
    Container:  containerName,  // explicit — Pod has subagent + credproxy containers
    TailLines:  ptr.To(int64(100)),
    Timestamps: true,
})
stream, err := req.Stream(ctx)
if err != nil { return err }
defer stream.Close()
io.Copy(cmd.OutOrStdout(), stream)  // blocks until EOF or ctx.Done()
```

Pitfall 25: cmd.Context() must come from ExecuteContext (already wired in plan 04-07 main.go).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tide approve / reject / resume — annotation writers via client.Patch</name>
  <read_first>
    - internal/gates/annotation.go (from plan 04-04 — annotation key constants + read-side helpers)
    - internal/controller/project_controller.go (lines 323-356 — reference write-side pattern using client.MergeFrom + r.Patch)
    - cmd/tide/main.go + root_flags.go (helper functions K8sClient, RESTConfig)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-G3 — tide approve --wave <plan>/<N>; D-G4 — reject/resume)
  </read_first>
  <behavior>
    - Test 1 (approve, level): `tide approve my-project` discovers the current AwaitingApproval level from `project.Status.Conditions` (looks for Condition Type=WaveOrLevelPaused Status=True Reason=AwaitingApproval; reads the level from message or from an associated child CRD). Writes annotation `tideproject.k8s/approve-<level>: "true"` via `client.Patch(ctx, obj, client.MergeFrom(original))`. If no AwaitingApproval level found, exit non-zero with `tide: no level awaiting approval on project my-project`.
    - Test 2 (approve --wave): `tide approve my-project --wave my-plan/3` writes `tideproject.k8s/approve-wave-3: "true"` on the Plan named `my-plan`. Validates --wave format `<plan-name>/<N>`; rejects bad formats with `--wave must be <plan-name>/<integer>`.
    - Test 3 (reject): `tide reject my-project --reason "operator stopped"` writes `tideproject.k8s/reject: "operator stopped"` on the Project. Empty --reason defaults to `"rejected by operator"`.
    - Test 4 (resume): `tide resume my-project` clears the `tideproject.k8s/reject` annotation via `gates.ConsumeReject(project)` returned map + client.Patch.
    - Test 5: All three writes use `client.MergeFrom(original.DeepCopy())` + `client.Patch(ctx, obj, patch)` — no Updates, no JSON-merge patches.
    - Test 6: When the Project doesn't exist, exit non-zero with `tide: project "my-project" not found in namespace "default"`.
  </behavior>
  <action>
    Replace `cmd/tide/approve.go` stub with real RunE:
    - `Args: cobra.ExactArgs(1)`. Flags: `--wave <plan/N>` (optional string), validated with regex `^[a-z0-9.-]+/\d+$`.
    - If --wave is set: split into plan name + integer N; fetch Plan; build new annotations map = deep-copy of existing + `AnnotationApproveWavePrefix + strconv.Itoa(N): "true"`; client.Patch.
    - Else: fetch Project; iterate `project.Status.Conditions` for WaveOrLevelPaused/AwaitingApproval; determine level by checking which child Status.Phase=AwaitingApproval (Milestone first, then Phase, etc.). Write `AnnotationApprovePrefix + level: "true"` on that child via client.Patch.

    Replace `cmd/tide/reject.go` stub: `Args: cobra.ExactArgs(1)`. Flag `--reason` (default "rejected by operator"). Fetch project, build annotations with `AnnotationReject: reason`, client.Patch.

    Replace `cmd/tide/resume.go` stub: `Args: cobra.ExactArgs(1)`. Fetch project; call `newAnnotations := gates.ConsumeReject(project)` (this returns the new map without the reject key); client.Patch the project with the new annotations.

    Tests use `controller-runtime/pkg/client/fake.NewClientBuilder()` to populate a Project + child CRDs; invoke `rootCmd.SetArgs([]string{"approve", "my-project"}); rootCmd.ExecuteContext(ctx)`; after execute, fetch the modified obj from the fake client and assert annotations contain the expected key.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./cmd/tide/... -run "Approve|Reject|Resume" -race -v</automated>
  </verify>
  <done>approve/reject/resume verbs use client.MergeFrom + client.Patch correctly; --wave format validated; no project / no AwaitingApproval level paths surface friendly errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tide cancel --force — destructive cascade delete</name>
  <read_first>
    - internal/controller/project_controller.go (look at finalizer deletion path lines 122-129 + cleanup logic — pattern reference)
    - cmd/tide/main.go (signal handling)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-G4 — cancel is destructive + requires --force)
  </read_first>
  <behavior>
    - Test 1: `tide cancel my-project` (no --force) exits non-zero with `tide: cancel is destructive — pass --force to confirm cascading delete of project, children, and PVC`. Project CRD untouched.
    - Test 2: `tide cancel my-project --force` deletes the Project CRD via `client.Delete(ctx, &project, client.PropagationPolicy(metav1.DeletePropagationForeground))` — K8s GC cascades to children (Milestones, Phases, Plans, Tasks) via owner refs. PVC cleanup is handled by the existing finalizer (CTRL-05, Phase 1).
    - Test 3: Confirmation banner before the actual delete: print `Deleting project my-project (foreground cascade)…` to stderr. After delete returns success: print `Project my-project deleted. Children cascade via owner refs; PVC cleanup runs via finalizer.`
    - Test 4: If project doesn't exist: exit non-zero with `tide: project "my-project" not found in namespace "default"`.
    - Test 5: With `--force --dry-run`, prints what WOULD be deleted (the Project name + a list of all owner-ref'd children) without actually calling Delete. Useful for operators verifying scope.
  </behavior>
  <action>
    Replace `cmd/tide/cancel.go` stub with real RunE:
    - `Args: cobra.ExactArgs(1)`. Flags: `--force` (bool, default false), `--dry-run` (bool, default false).
    - If --force is false: print the explicit "destructive — pass --force" message and `return errors.New("--force required")`. Cobra's SilenceErrors=false (from root) ensures the error prints.
    - Fetch project. If not found: friendly error.
    - If --dry-run: list children via `client.List(ctx, &v1alpha1.MilestoneList{}, client.InNamespace(ns), client.MatchingLabels{"tide.controller/owner": projectName})` and similar for other kinds; print the tree; return nil.
    - Else: `client.Delete(ctx, project, client.PropagationPolicy(metav1.DeletePropagationForeground))`. Print confirmation. Return.

    Tests: fake client with a Project + owner-ref'd children. Test 1 + 2 + 5 use the fake client; Test 3 verifies stderr capture; Test 4 uses empty fake client.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./cmd/tide/... -run "Cancel" -race -v</automated>
  </verify>
  <done>tide cancel requires --force; honors --dry-run; uses Foreground cascade; friendly error on missing project.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tide tail — pod-log streaming via client-go pods/log subresource</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 788-829 — pods/log subresource streaming + lines 555-566 Pitfall 25 ExecuteContext)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-C3 tide tail; CLI-04)
    - cmd/tide/main.go (signal.NotifyContext + ExecuteContext already wired)
    - k8s.io/client-go/kubernetes (PodLogOptions API)
  </read_first>
  <behavior>
    - Test 1: `tide tail my-task` resolves the Task CRD, finds its currently-active Pod via `client.List(ctx, &podList, client.MatchingLabels{"job-name": task.Status.JobName})`, picks the first non-Terminated pod, opens a `Follow: true` log stream, copies to stdout.
    - Test 2: With `--container <name>` flag, uses that container; without, defaults to the first container whose name does NOT match `credproxy` or `init-*` (the subagent main container).
    - Test 3: Ctrl-C during streaming — `cmd.Context().Done()` fires; the stream.Close() runs via defer; the function returns within 1 second. Test by spawning the RunE in a goroutine, sending SIGINT after 500ms, asserting completion within 1500ms total.
    - Test 4: With `--tail N` flag, sets `TailLines: ptr.To(int64(N))` on PodLogOptions. Default --tail 100.
    - Test 5: With `--timestamps` flag (default true), sets `Timestamps: true`.
    - Test 6: When the task has no pod: friendly error `tide: task "my-task" has no running pod (status: <phase>)`.
    - Test 7: When the pod's stream returns EOF (pod terminated mid-stream), print `(stream closed by pod termination)` to stderr and exit 0.
  </behavior>
  <action>
    Replace `cmd/tide/tail.go` stub with real RunE:
    - `Args: cobra.ExactArgs(1)`. Flags: `--container, -c` (string, default ""), `--tail` (int64, default 100), `--timestamps, -t` (bool, default true).
    - Build a kubernetes.Clientset from the rest.Config (`kubernetes.NewForConfig(restCfg)`). Build a controller-runtime client for the Task CRD lookup.
    - Fetch the Task; from `task.Status.JobName` find the active Pod: `clientset.CoreV1().Pods(ns).List(ctx, metav1.ListOptions{LabelSelector: "job-name=" + jobName})`. Pick the first pod whose `Status.Phase ∈ {Running, Pending}`. (Pending is allowed for follow=true; logs will start streaming once container is ready.)
    - Resolve container: if --container set, use it; else iterate pod.Spec.Containers, return the first whose Name is NOT `credproxy` and not starting with `init-`. If multiple candidates and ambiguous, error out asking for explicit --container.
    - Open the stream:
      ```go
      req := clientset.CoreV1().Pods(ns).GetLogs(podName, &corev1.PodLogOptions{
          Follow:     true,
          Container:  containerName,
          TailLines:  ptr.To(tailLines),
          Timestamps: timestamps,
      })
      stream, err := req.Stream(ctx)
      if err != nil { return fmt.Errorf("open log stream: %w", err) }
      defer stream.Close()
      _, err = io.Copy(cmd.OutOrStdout(), stream)
      if err != nil && ctx.Err() == nil { return fmt.Errorf("read log stream: %w", err) }
      ```
    - Wrap io.Copy in a goroutine so the main RunE can also watch `<-cmd.Context().Done()` and force-close the stream — actually, defer stream.Close() + closing the stream from another goroutine when ctx cancels is the cleanest pattern. Use a `go func() { <-cmd.Context().Done(); stream.Close() }()`.

    Tests: For Test 1+2+4+5+6+7 use a real envtest-set-up clientset wrapping fakerest.Server (or skip envtest entirely and use only the fake controller-runtime client + a stub for the GetLogs response). For Test 3 (signal handling), the cleanest approach is a focused unit test that spawns the RunE with a goroutine-cancelled context after 500ms and asserts return within 1500ms — uses `context.WithCancel` not real SIGINT. Integration test with kubectl-equivalent real apiserver lands in plan 04-14.

    NOTE: client-go's `kubernetes.Clientset` is a heavyweight import; ensure the existing controller-runtime build flags / go.mod cover it. If not in go.mod, `go get k8s.io/client-go@<version-matched-to-controller-runtime>`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./cmd/tide/... -run "Tail" -race -v &amp;&amp; go build ./cmd/tide/...</automated>
  </verify>
  <done>tide tail streams pod logs with Follow:true, respects --container/--tail/--timestamps flags, cancels cleanly on Ctrl-C, surfaces friendly errors for missing pods.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → cluster RBAC | CLI verbs honor user's kubeconfig RBAC; users without `patch projects.tideproject.k8s` cannot approve/reject (apiserver enforces) |
| Cancel + cascade delete | --force is the user's confirmation; --dry-run preview pattern lets operators verify scope before destructive op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-G2 | Tampering (over-approval) | cmd/tide/approve.go | mitigate | Annotation is one-shot (consumed by reconciler in plan 04-05); approve writes the canonical annotation key that gates.ConsumeApprove looks for. |
| T-04-G4 | DoS (delete-the-world) | cmd/tide/cancel.go | mitigate | --force flag required; --dry-run pattern available; foreground cascade is the K8s-native, reversible-by-no-one-mode but matches Project-scope deletion intent. |
| T-04-C4 | Resource exhaustion (stream leak) | cmd/tide/tail.go | mitigate | Defer stream.Close(); ctx.Done() watcher closes stream when SIGINT fires (Pitfall 25). Tested via Test 3 of Task 3. |
| T-04-C-typo | Mistakes (wrong project) | cmd/tide/{approve,reject,cancel}.go | accept | Operator workflow — friendly NotFound error returns non-zero; documenting `--context` flag in CLI startup mitigates cross-cluster typo. |
</threat_model>

<verification>
- `go test ./cmd/tide/... -race -v` passes (all 5 verbs).
- `bin/tide approve --help` documents `--wave <plan/N>` format.
- `bin/tide cancel my-project` (no --force) exits non-zero and prints the warning.
- `bin/tide cancel my-project --force --dry-run` prints what would be deleted.
- `bin/tide tail --help` documents --container/--tail/--timestamps flags.
- `grep -c "client.MergeFrom" cmd/tide/approve.go cmd/tide/reject.go cmd/tide/resume.go` returns ≥ 3.
- `grep -c "PropagationPolicyForeground" cmd/tide/cancel.go` returns ≥ 1.
- `grep -c "GetLogs" cmd/tide/tail.go` returns ≥ 1.
- `make tide-lint` passes.
</verification>

<success_criteria>
- 5 stub verbs from plan 04-07 are now real: approve, reject, cancel, resume, tail.
- approve/reject/resume use client.MergeFrom + client.Patch for one-shot annotation semantics matching reconciler-side ConsumeApprove (plan 04-04).
- cancel honors --force gate + offers --dry-run preview + uses Foreground cascade.
- tail streams via pods/log Follow:true with signal-aware ctx cancellation (Pitfall 25 mitigation).
- All tests pass with -race.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-08-SUMMARY.md` documenting the annotation key set written by each verb, the client.MergeFrom/Patch pattern, the cancel --force/--dry-run UX, and the tail container-resolution heuristic.
</output>
