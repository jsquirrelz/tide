---
phase: 04
plan: 12
type: execute
wave: 4
depends_on: [04-11]
files_modified:
  - dashboard/web/package.json
  - dashboard/web/vite.config.ts
  - dashboard/web/tsconfig.json
  - dashboard/web/vitest.config.ts
  - dashboard/web/.nvmrc
  - dashboard/web/index.html
  - dashboard/web/src/main.tsx
  - dashboard/web/src/App.tsx
  - dashboard/web/src/index.css
  - dashboard/web/src/components/AppShell.tsx
  - dashboard/web/src/components/Header.tsx
  - dashboard/web/src/components/ConnectionStatusIndicator.tsx
  - dashboard/web/src/components/Toast.tsx
  - dashboard/web/src/components/ToastContainer.tsx
  - dashboard/web/src/lib/clsx.ts
  - dashboard/web/src/__tests__/setup.ts
  - dashboard/web/src/components/Toast.test.tsx
  - dashboard/web/src/components/ConnectionStatusIndicator.test.tsx
ui_gap: []
autonomous: true
requirements: [DASH-01]
threat_model_id: T-04-D1
must_haves:
  truths:
    - "`dashboard/web/` is a fresh Vite v6 + React 18 + TypeScript 5 + Tailwind v4 + @xyflow/react v12 project"
    - "Tailwind v4 `@theme` directive in src/index.css declares the locked design tokens from UI-SPEC: 60/30/10 color split (dark default), 8-point spacing scale, 3-size + mono type system, status color tokens"
    - "AppShell + Header + ConnectionStatusIndicator + Toast (the dashboard's chrome) are implemented and rendered as the placeholder app — plan 04-15 adds the 4 status/primitive components on top of these tokens"
    - "Vitest + @testing-library/react bootstrap is in place; Toast + ConnectionStatusIndicator each have at least one passing smoke test"
    - "Zero `dangerouslySetInnerHTML` in any .tsx file (T-04-D1 XSS mitigation)"
    - "`npm run build` produces a working bundle; the <500KB gzipped bundle gate lands in plan 04-16 (after all components are in)"
    - "No webfonts loaded (system font stack only per UI-SPEC) — `@font-face` count in src/ is 0"
  artifacts:
    - path: dashboard/web/package.json
      provides: "pinned React 18, TypeScript 5, @xyflow/react v12, dagre v0.8, tailwindcss v4, vite v6, vitest, lucide-react, clsx"
      contains: "@xyflow/react"
    - path: dashboard/web/src/index.css
      provides: "Tailwind v4 @theme directive with all design tokens from UI-SPEC"
      contains: "@theme"
    - path: dashboard/web/src/components/AppShell.tsx
      provides: "top-level layout per UI-SPEC §1 — 48px header + two-pane body (children-driven)"
      contains: "AppShell"
    - path: dashboard/web/src/components/Header.tsx
      provides: "wordmark + ConnectionStatusIndicator slot + theme toggle"
      contains: "TIDE"
    - path: dashboard/web/src/components/ConnectionStatusIndicator.tsx
      provides: "3 states (connected/reconnecting/offline) with verbatim UI-SPEC copy strings"
      contains: "connected"
  key_links:
    - from: dashboard/web/src/index.css
      to: tailwindcss
      via: "@import 'tailwindcss'; + @theme directive"
      pattern: "@theme"
    - from: dashboard/web/src/components/Header.tsx
      to: dashboard/web/src/components/ConnectionStatusIndicator.tsx
      via: "imported and rendered inside Header"
      pattern: "ConnectionStatusIndicator"
---

<objective>
Bootstrap the `dashboard/web/` frontend with the Vite + React + Tailwind v4 + @xyflow/react v12 toolchain pinned to the UI-SPEC's Stack & Bundle Targets table; declare every design token (color/spacing/type) from UI-SPEC §Color/Spacing/Typography in a single Tailwind v4 `@theme` directive; ship the dashboard's chrome (AppShell, Header, ConnectionStatusIndicator, Toast/ToastContainer) and the test scaffolding (Vitest + @testing-library setup). Plans 04-15 (status/primitive components), 04-13 (DAG views + drawer), and 04-16 (log streamer + bundle gate) build on top.

Purpose: The frontend is entirely GREENFIELD — UI-SPEC.md is the contract. This plan locks the toolchain, the design tokens, and the chrome so plan 04-15 only authors the 4 status/primitive components without re-litigating colors, spacing, type, or status semantics.

Output: ~20 files in dashboard/web/ + package.json + Vite/TS/Tailwind/Vitest configs + 2 Vitest smoke tests for Toast and ConnectionStatusIndicator. After this plan: `npm run dev` shows the AppShell + Header + placeholder for the panes; `npm run test` is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md
@.planning/phases/04-gates-observability-dashboard-cli/04-11-PLAN.md

<interfaces>
<!-- Tailwind v4 @theme directive shape (verified against Tailwind v4 docs) -->

```css
/* src/index.css */
@import "tailwindcss";

@theme {
  --color-surface-base: #0B0F14;
  --color-surface-raised: #161B22;
  --color-surface-overlay: #1F2937;
  --color-accent: #06B6D4;
  --color-text-primary: #E6EDF3;
  --color-text-muted: #8B949E;
  /* ... full token set per UI-SPEC §Color */

  --spacing-1: 4px;
  --spacing-2: 8px;
  --spacing-3: 12px;
  /* ... per UI-SPEC §Spacing */

  --font-family-sans: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  --font-family-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace;

  --font-size-body: 14px;
  --font-size-label: 12px;
  --font-size-heading: 18px;
  --font-size-mono: 13px;
}

.light-theme {
  --color-surface-base: #FFFFFF;
  /* ... light-theme overrides */
}
```

<!-- vitest.config.ts -->

```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: { environment: "jsdom", setupFiles: ["./src/__tests__/setup.ts"], globals: true }
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dashboard/web/ scaffold — Vite + React + TS + Tailwind v4 + Vitest config + @theme tokens</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§Design System, §Stack & Bundle Targets, §Spacing, §Typography, §Color sections — ALL design tokens)
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 1007-1060 Vite/React/TS/Tailwind v4 toolchain)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (lines 74-100 frontend GREENFIELD — UI-SPEC is the contract)
  </read_first>
  <acceptance_criteria>
    - `dashboard/web/package.json` declares exact dependencies: `react@^18`, `react-dom@^18`, `@xyflow/react@^12`, `dagre@^0.8`, `@types/dagre@^0.7`, `tailwindcss@^4`, `@tailwindcss/vite@^4`, `vite@^6`, `vitest@^1`, `@testing-library/react@^16`, `@testing-library/jest-dom@^6`, `jsdom@^24`, `lucide-react@latest`, `clsx@latest`, `typescript@^5`, `@types/react@^18`, `@types/react-dom@^18`, `@vitejs/plugin-react@^4`.
    - `dashboard/web/.nvmrc` pins Node `22` (per RESEARCH §A3).
    - `dashboard/web/vite.config.ts` includes the React plugin + Tailwind v4 plugin + dev proxy: `server: { proxy: { '/api': 'http://localhost:8080', '/healthz': 'http://localhost:8081' } }`.
    - `dashboard/web/tsconfig.json` declares strict TypeScript with target ES2020, JSX preserve.
    - `dashboard/web/src/index.css` contains exactly one `@theme { ... }` block covering ALL tokens from UI-SPEC: dark-theme defaults (60/30/10 colors + status colors + spacing scale 1/2/3/4/6/8/12/16 + font families + 4 type sizes) and a `.light-theme { ... }` block with the light-theme overrides.
    - `dashboard/web/vitest.config.ts` configures jsdom environment + `src/__tests__/setup.ts` setupFile.
    - `dashboard/web/src/__tests__/setup.ts` imports `@testing-library/jest-dom`.
    - `dashboard/web/src/main.tsx` mounts `<App />` into `#root` (standard React 18 createRoot pattern).
    - `dashboard/web/src/App.tsx` renders `<AppShell>{<Header />} placeholder panes</AppShell>` — placeholder content for the two panes (plan 04-13 replaces with PlanningDAGView + ExecutionDAGView).
    - `npm install` (or `npm ci`) succeeds; `npm run dev` starts Vite without errors; `npm run build` produces `dist/`; `npm run test` runs Vitest with the Task 2 chrome tests passing.
    - No webfont imports anywhere in src/ (UI-SPEC bundle gate); grep `@font-face|@import url.*fonts` returns 0.
    - No CSS-in-JS libraries (UI-SPEC bundle gate); grep package.json for `styled-components|emotion|stitches` returns 0.
  </acceptance_criteria>
  <action>
    Create the directory tree `dashboard/web/` with these files:

    1. `package.json` — declare scripts: `dev: vite`, `build: vite build && tsc --noEmit`, `preview: vite preview`, `test: vitest run`, `test:watch: vitest`, `lint: tsc --noEmit`. Pin versions per the interfaces block. `"type": "module"`.

    2. `vite.config.ts` — import @vitejs/plugin-react + @tailwindcss/vite; export defineConfig({ plugins: [react(), tailwindcss()], server: { proxy: { '/api': 'http://localhost:8080', '/healthz': 'http://localhost:8081' } } }).

    3. `tsconfig.json` — minimal strict React 18 TS config (target ES2020, lib DOM + ES2020, jsx preserve, strict true, esModuleInterop true, forceConsistentCasingInFileNames true, skipLibCheck true).

    4. `vitest.config.ts` — per interfaces block.

    5. `.nvmrc` — single line `22`.

    6. `index.html` — minimal Vite-compatible HTML with `<div id="root"></div>` + `<script type="module" src="/src/main.tsx"></script>`. Title: `TIDE Dashboard`. Set `<meta name="viewport" content="width=1280">` (UI-SPEC §Layout — primary target ≥1280px laptop). Default theme via `<html class="">` (or via media-query in CSS — pick CSS-side for simplicity).

    7. `src/index.css` — `@import "tailwindcss";` then the full `@theme` block from UI-SPEC. Include the `.light-theme` override block. Add `body { background: var(--color-surface-base); color: var(--color-text-primary); font-family: var(--font-family-sans); font-size: var(--font-size-body); margin: 0; }` and `code, pre, .mono { font-family: var(--font-family-mono); font-size: var(--font-size-mono); }`. Set `:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }` per UI-SPEC §Accessibility. Respect `@media (prefers-reduced-motion: reduce)` to disable animations.

    8. `src/main.tsx` — `import { createRoot } from 'react-dom/client'; import './index.css'; import App from './App'; createRoot(document.getElementById('root')!).render(<App />);`.

    9. `src/App.tsx` — return `<AppShell><Header /><div className="grid grid-cols-2"><div>Planning DAG placeholder (plan 04-13)</div><div>Execution DAG placeholder (plan 04-13)</div></div></AppShell>`. (The grid is the simplest split — plan 04-13 replaces with the resizable two-pane.)

    10. `src/lib/clsx.ts` — re-export from "clsx" (or implement a 10-line zero-dep variant per UI-SPEC's "10-line alt acceptable" note).

    11. `src/__tests__/setup.ts` — `import '@testing-library/jest-dom';`.

    Run `cd dashboard/web && npm ci && npm run build` to verify the toolchain works against the placeholder App.tsx.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide/dashboard/web &amp;&amp; npm ci &amp;&amp; npm run build &amp;&amp; grep -cE "^[^#]*@theme" src/index.css | grep -E "^\s*1$" &amp;&amp; (grep -rE "@font-face|@import url.*fonts" src/ ; if [ $? -eq 0 ]; then echo "FAIL: webfont import"; exit 1; fi)</automated>
  </verify>
  <done>npm install + build succeed; @theme directive declares all UI-SPEC tokens; zero webfont imports; tsconfig + vitest config are valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chrome components — AppShell, Header, ConnectionStatusIndicator, Toast/ToastContainer</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§Component Inventory items 1-2, 11-12 — AppShell, Header, Toast, ConnectionStatusIndicator)
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§Copywriting Contract — locked Toast/ConnectionStatusIndicator copy strings)
  </read_first>
  <behavior>
    - Test 1 (Toast variants): Render Toast with variant info/success/warning/error; each variant renders with the correct border-left color from UI-SPEC tokens; variant locks the styling.
    - Test 2 (Toast copy strings): `toast({variant: 'success', title: 'Command copied', body: 'Paste in your terminal to run: tide apply'})` renders body that contains the verbatim UI-SPEC copy.
    - Test 3 (ConnectionStatusIndicator): Each of 3 states ("connected", "reconnecting", "offline") renders the correct verbatim copy label + colored dot + `role="status" aria-live="polite"` attribute.
    - Test 4 (no dangerouslySetInnerHTML): A scripted assertion via a Vitest test reads every .tsx file under `src/components` and asserts `!content.includes('dangerouslySetInnerHTML')`.
    - Test 5 (light-theme): Apply `document.documentElement.className = 'light-theme'`; render Toast; assert the resolved CSS variables match the UI-SPEC's light-theme palette via `getComputedStyle` in jsdom.
  </behavior>
  <action>
    Create the 4 chrome components. Each is a `.tsx` file in `dashboard/web/src/components/` with strict TypeScript types and minimal-prop interfaces matching UI-SPEC.

    `AppShell.tsx`: top-level layout per UI-SPEC §1 — header bar (height 48px) + two-pane body. State for `splitRatio` is initialized but the pane content is `props.children` (consumed by plan 04-13). Persists splitRatio to localStorage on resize (defer the resize handle UI to plan 04-13; for this plan, fixed 50/50).

    `Header.tsx`: wordmark "TIDE" in mono + ConnectionStatusIndicator slot (right side) + theme toggle (placeholder for v1.0 — a button toggling `document.documentElement.classList`). 48px height + bottom border per UI-SPEC §2. ProjectPicker slot is left as a `props.projectPicker?: React.ReactNode` prop — populated by plan 04-15 once ProjectPicker exists.

    `Toast.tsx` + `ToastContainer.tsx`: A toast portal at bottom-right. Provide a `<ToastContext>` with `useToast()` hook exposing `toast({variant, title, body, duration})`. Toast stack with slide-in animation per UI-SPEC §11. Locked toast copy strings from UI-SPEC's Copywriting Contract live as named constants in `src/lib/toast-copy.ts` and are imported by emitters.

    `ConnectionStatusIndicator.tsx`: Pill with 3 states + verbatim copy strings + `role="status" aria-live="polite"` per UI-SPEC §12.

    Create `dashboard/web/src/components/Toast.test.tsx` covering tests 1-2 and 5.

    Create `dashboard/web/src/components/ConnectionStatusIndicator.test.tsx` covering test 3.

    Add a Vitest test `src/__tests__/no-dangerous-html.test.ts` that uses Vitest's fs reading of every .tsx file under src/components and asserts `!content.includes('dangerouslySetInnerHTML')` (Test 4).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide/dashboard/web &amp;&amp; npm run test &amp;&amp; npm run build &amp;&amp; (grep -r "dangerouslySetInnerHTML" src/ ; if [ $? -eq 0 ]; then echo "FAIL: dangerouslySetInnerHTML present"; exit 1; fi) &amp;&amp; echo "PASS: no dangerouslySetInnerHTML"</automated>
  </verify>
  <done>AppShell, Header, ConnectionStatusIndicator, Toast/ToastContainer compile, render, and have at least one Vitest test each; zero dangerouslySetInnerHTML; bundle builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend JSON → React render | User-supplied CRD names render as React text nodes (escape-by-default). Zero dangerouslySetInnerHTML in any component file (T-04-D1 mitigation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-D1 | XSS via project/task name | dashboard/web/src/components/*.tsx | mitigate | React escapes text nodes by default; AST/grep gate asserts zero dangerouslySetInnerHTML uses. UI-SPEC §Color WCAG-AA contrast verified. |
| T-04-D1-bundle | DoS (bundle bloat) | dashboard/web/package.json | mitigate | Bundle gates documented in UI-SPEC; no shadcn registry; tree-shaken lucide; zero webfont bytes. The bundle-size assertion test ships in plan 04-16 (once all components are in). |
</threat_model>

<verification>
- `cd dashboard/web && npm ci && npm run build` succeeds.
- `cd dashboard/web && npm run test` passes (Toast + ConnectionStatusIndicator + no-dangerous-html tests).
- `rg "dangerouslySetInnerHTML" dashboard/web/src/` returns zero hits.
- `rg "@font-face|@import url.*fonts" dashboard/web/src/` returns zero hits.
- `grep -cE "^[^#]*@theme" dashboard/web/src/index.css` returns exactly 1.
</verification>

<success_criteria>
- dashboard/web/ scaffold compiles, builds, and tests pass.
- 4 chrome components shipped (AppShell, Header, Toast/Container, ConnectionStatusIndicator).
- All UI-SPEC tokens declared in a single @theme block.
- Zero dangerouslySetInnerHTML, zero webfont imports, zero CSS-in-JS libraries.
- Plan 04-15 can build the 4 status/primitive components on top without re-litigating any design tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-12-SUMMARY.md` documenting the package.json pinned versions, the @theme token set, the 4 chrome components shipped, and the XSS guard test pattern.
</output>
