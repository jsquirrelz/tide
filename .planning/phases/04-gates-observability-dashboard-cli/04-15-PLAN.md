---
phase: 04
plan: 15
type: execute
wave: 5
depends_on: [04-12]
files_modified:
  - dashboard/web/src/components/StatusBadge.tsx
  - dashboard/web/src/components/StatusBadge.test.tsx
  - dashboard/web/src/components/ProjectPicker.tsx
  - dashboard/web/src/components/ProjectPicker.test.tsx
  - dashboard/web/src/components/ClipboardCopyAction.tsx
  - dashboard/web/src/components/ClipboardCopyAction.test.tsx
  - dashboard/web/src/components/WaveBackground.tsx
  - dashboard/web/src/components/WaveBackground.test.tsx
  - dashboard/web/src/lib/toast-copy.ts
ui_gap: []
autonomous: true
requirements: [DASH-01]
threat_model_id: T-04-D1
must_haves:
  truths:
    - "10 CRD status values render as `<StatusBadge>` components — each with exact lucide icon + label + status color from UI-SPEC's Status Vocabulary table"
    - "`<ProjectPicker>` is a dropdown of projects with empty-state copy from UI-SPEC; receives `projects: Array<{name, namespace, phase}>` from parent"
    - "`<ClipboardCopyAction>` writes a string to clipboard via navigator.clipboard.writeText + emits a Toast (via the ToastContext from plan 04-12) using verbatim UI-SPEC copy. Variants primary / destructive / secondary."
    - "`<WaveBackground>` renders an SVG `<rect>` band at z-index 0 per UI-SPEC §6 — props {waveIndex, bounds, isActiveDispatch, taskCount}; consumed by plan 04-13's ExecutionDAGView as the per-wave background-band layer."
    - "Animation classes (`animate-spin` for Dispatching, `animate-pulse` for Running) applied via Tailwind className strings per UI-SPEC Status Vocabulary table"
    - "Zero `dangerouslySetInnerHTML` in any new .tsx file (T-04-D1 XSS mitigation re-asserted)"
  artifacts:
    - path: dashboard/web/src/components/StatusBadge.tsx
      provides: "<StatusBadge status='Running' /> + 9 other variants per UI-SPEC Status Vocabulary; inline-flex pill, mono 12px semibold"
      contains: "Hourglass"
    - path: dashboard/web/src/components/ProjectPicker.tsx
      provides: "<ProjectPicker projects={...} value={...} onChange={...} /> dropdown with empty/single/multi states"
      contains: "ProjectPicker"
    - path: dashboard/web/src/components/ClipboardCopyAction.tsx
      provides: "navigator.clipboard.writeText + Toast emission + variant {primary, destructive, secondary}"
      contains: "navigator.clipboard"
    - path: dashboard/web/src/components/WaveBackground.tsx
      provides: "SVG <rect> band component for ExecutionDAGView wave bands (UI-SPEC §6)"
      contains: "WAVE"
  key_links:
    - from: dashboard/web/src/components/StatusBadge.tsx
      to: lucide-react
      via: "individual icon imports — tree-shake-friendly"
      pattern: "from \"lucide-react\""
    - from: dashboard/web/src/components/ClipboardCopyAction.tsx
      to: dashboard/web/src/components/Toast.tsx
      via: "useToast() hook from ToastContext (plan 04-12)"
      pattern: "useToast"
---

<objective>
Ship the 4 status/primitive components that plans 04-13 (DAG views) and 04-16 (log streamer) depend on: `<StatusBadge>` (10 status variants per UI-SPEC Status Vocabulary), `<ProjectPicker>` (header-mounted dropdown), `<ClipboardCopyAction>` (the copy-to-clipboard pattern used in TaskDetailDrawer Actions row), and `<WaveBackground>` (the SVG band consumed by ExecutionDAGView). All four build on the design tokens + ToastContext from plan 04-12.

Purpose: These primitives are domain-specific (status semantics, clipboard-copy with locked Toast copy, wave bands) but visually independent — they slot into the chrome from 04-12 and are consumed by 04-13 without re-litigating tokens or animation classes.

Output: 4 component files + 4 Vitest test files under `dashboard/web/src/components/`. After this plan: `npm run test` is green; the components are importable from `dashboard/web/src/App.tsx` to render a primitives gallery for visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md
@.planning/phases/04-gates-observability-dashboard-cli/04-12-PLAN.md

<interfaces>
<!-- StatusBadge variant signature — driven by UI-SPEC §Status Vocabulary -->

```tsx
type StatusValue =
  | "Pending" | "Dispatching" | "Running" | "AwaitingApproval"
  | "Paused" | "Succeeded" | "Failed" | "PushLeaseFailed"
  | "PushLeakBlocked" | "Rejected";

const STATUS_TABLE: Record<StatusValue, {
  icon: LucideIcon; label: string; colorClass: string;
  srDescription: string; animationClass?: string;
}> = { /* per UI-SPEC Status Vocabulary table */ };
```

<!-- ClipboardCopyAction — uses useToast() from plan 04-12's ToastContext -->

```tsx
type Variant = "primary" | "destructive" | "secondary";
interface Props {
  command: string;
  label: string;
  variant?: Variant;
}
// onClick: await navigator.clipboard.writeText(command);
// toast({variant: 'success', title: 'Command copied', body: `Paste in your terminal to run: ${command}`});
// On clipboard failure: toast({variant: 'error', title: "Couldn't copy", body: `Clipboard API blocked. Command: ${command}`, duration: 8000});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StatusBadge + WaveBackground — 10 status variants + SVG band component</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§Status Vocabulary — 10 status rows with icon/label/color/description/animation columns)
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§6 WaveBackground — bounds, label, active-band stroke-dasharray)
    - dashboard/web/src/components/Toast.tsx (from plan 04-12 — reuse design tokens; no Toast usage here yet)
  </read_first>
  <behavior>
    - Test 1 (StatusBadge table-driven): For each of the 10 status values (Pending, Dispatching, Running, AwaitingApproval, Paused, Succeeded, Failed, PushLeaseFailed, PushLeakBlocked, Rejected), render `<StatusBadge status="<value>" />` and assert: (a) the correct lucide icon is rendered (verify via icon component identity or `data-icon` attribute); (b) the label text matches UI-SPEC exactly (e.g., "Awaiting approval" not "AwaitingApproval"); (c) the `aria-label` is `Status: <screen-reader description from UI-SPEC>`.
    - Test 2 (StatusBadge animation): Dispatching renders `animate-spin` className substring; Running renders `animate-pulse`. Other statuses have no animation class.
    - Test 3 (StatusBadge shape): Inline-flex pill with padding `gap-1 p-1 px-2` + border-radius 4px + mono 12px semibold per UI-SPEC Status Vocabulary "shape" diagram. Verify via className substring assertions.
    - Test 4 (WaveBackground render): Render `<WaveBackground waveIndex={2} bounds={{x:0,y:0,width:600,height:200}} isActiveDispatch={false} taskCount={5} />`; assert an SVG `<rect>` with width=600, height=200 is in the DOM AND the label "WAVE 2 · 5 tasks" appears.
    - Test 5 (WaveBackground active stroke): When `isActiveDispatch=true`, the rect has `stroke-dasharray` attribute (the accented active-band styling per UI-SPEC §6).
    - Test 6 (WaveBackground failed color): When `taskCount > 0` and a `failedCount` prop is set > 0, the band uses the status-blocked color token per UI-SPEC §6.
  </behavior>
  <action>
    Create `dashboard/web/src/components/StatusBadge.tsx`:
    - Typed `type StatusValue` per the interfaces block.
    - A `STATUS_TABLE: Record<StatusValue, {...}>` literal mapping every status to its {icon, label, colorClass, srDescription, animationClass?}. Sourced VERBATIM from UI-SPEC §Status Vocabulary table.
    - Functional component reads from the table and renders an inline-flex `<span>` with `aria-label="Status: <srDescription>"`, the lucide icon, the label text, and animation className when set.
    - Import individual lucide icons (Hourglass, Pause, CircleCheck, CircleX, LockKeyhole, ShieldAlert, Ban, Loader2, CircleDot, Circle, Hand — per UI-SPEC's Status Vocabulary table).

    Create `dashboard/web/src/components/WaveBackground.tsx`:
    - Props: `{ waveIndex: number; bounds: {x, y, width, height}; isActiveDispatch: boolean; taskCount: number; failedCount?: number }`.
    - Renders an SVG fragment: `<g>` containing `<rect>` (the band) + `<text>` (the "WAVE N · X tasks" label). z-index 0 (CSS class).
    - Active-dispatch styling: stroke + `stroke-dasharray` per UI-SPEC §6.
    - Failed band: derive color from token `--color-status-blocked` when `failedCount > 0`.

    Tests in `StatusBadge.test.tsx` (tests 1-3) and `WaveBackground.test.tsx` (tests 4-6) using table-driven Vitest + @testing-library/react.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide/dashboard/web &amp;&amp; npm run test -- StatusBadge WaveBackground</automated>
  </verify>
  <done>StatusBadge renders all 10 variants per UI-SPEC; WaveBackground renders SVG bands with correct geometry + active/failed styling; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProjectPicker + ClipboardCopyAction — header dropdown + the locked clipboard-Toast pattern</name>
  <read_first>
    - .planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md (§Component Inventory items for ProjectPicker + Clipboard-Copy + §10 Clipboard-Copy Action subsection + §Copywriting Contract — verbatim Toast copy strings)
    - dashboard/web/src/components/Toast.tsx (from plan 04-12 — useToast hook)
    - dashboard/web/src/components/Header.tsx (from plan 04-12 — ProjectPicker slot via header.projectPicker prop)
  </read_first>
  <behavior>
    - Test 1 (ProjectPicker empty): `<ProjectPicker projects={[]} value={null} onChange={fn} />` renders the empty-state copy from UI-SPEC ("No projects in this cluster" or similar — verify against UI-SPEC §13 E1).
    - Test 2 (ProjectPicker single): `<ProjectPicker projects={[{name:'p1',namespace:'ns',phase:'Running'}]} />` still renders as a dropdown (consistency); click opens, selecting fires onChange.
    - Test 3 (ProjectPicker multi): With 3 projects, all 3 appear in the dropdown; each row shows name + namespace + status badge (StatusBadge integration confirmed via DOM containment).
    - Test 4 (ClipboardCopyAction primary): Render `<ClipboardCopyAction command="tide apply" label="Apply" variant="primary" />`; click; assert `navigator.clipboard.writeText` called with `"tide apply"`; assert Toast emitted via captured ToastContext provider with body containing `"Paste in your terminal to run: tide apply"` (verbatim UI-SPEC copy).
    - Test 5 (ClipboardCopyAction failure): Mock `navigator.clipboard.writeText` to reject; click; assert error Toast emitted with verbatim copy `"Clipboard API blocked. Command: tide apply"` and duration 8000.
    - Test 6 (ClipboardCopyAction variant styling): primary/destructive/secondary each produce the correct UI-SPEC classNames (verify via className substring assertions).
  </behavior>
  <action>
    Create `dashboard/web/src/components/ProjectPicker.tsx`:
    - Props: `{ projects: Array<{name: string; namespace: string; phase: string}>; value: string | null; onChange: (name: string) => void }`.
    - Renders a clickable dropdown trigger (uppercase wordmark style per UI-SPEC §2 Header). Click toggles a panel below.
    - Empty state: trigger label "No projects" (or verbatim UI-SPEC copy); panel renders the E1 empty-state copy referenced in UI-SPEC §13.
    - Multi-state: each row renders `{name}` mono + `{namespace}` muted + `<StatusBadge status={project.phase} />`.
    - Selecting a row fires `onChange(name)`; clicking outside or pressing Escape closes the panel. Keyboard shortcut `/` to open is deferred to plan 04-13.

    Create `dashboard/web/src/components/ClipboardCopyAction.tsx`:
    - Props: `{ command: string; label: string; variant?: "primary" | "destructive" | "secondary" }`.
    - Renders a `<button>` with variant-specific Tailwind classes (`bg-accent` / `bg-destructive` / `bg-surface-overlay` per UI-SPEC).
    - On click: `try { await navigator.clipboard.writeText(command); toast({variant:'success', title:'Command copied', body: \`Paste in your terminal to run: ${command}\`}); } catch (e) { toast({variant:'error', title:"Couldn't copy", body: \`Clipboard API blocked. Command: ${command}\`, duration: 8000}); }`. Toast copy must come from the `toast-copy.ts` constants set in plan 04-12 (extend that module with the two new constants if not present).

    Tests in `ProjectPicker.test.tsx` (tests 1-3) and `ClipboardCopyAction.test.tsx` (tests 4-6). Mock `navigator.clipboard.writeText` via `vi.spyOn(navigator.clipboard, 'writeText').mockResolvedValue(undefined)` for success path and `mockRejectedValueOnce(new Error('blocked'))` for failure path. Wrap renders in the ToastContext provider from plan 04-12 to capture Toast emissions.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide/dashboard/web &amp;&amp; npm run test -- ProjectPicker ClipboardCopyAction &amp;&amp; (grep -r "dangerouslySetInnerHTML" src/components/ProjectPicker.tsx src/components/ClipboardCopyAction.tsx ; if [ $? -eq 0 ]; then echo "FAIL"; exit 1; fi) &amp;&amp; echo PASS</automated>
  </verify>
  <done>ProjectPicker renders empty/single/multi states; ClipboardCopyAction emits success + failure Toasts with verbatim UI-SPEC copy; all tests pass; zero dangerouslySetInnerHTML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend JSON → StatusBadge / ProjectPicker render | User-supplied project + status names render as React text nodes (escape-by-default). Zero dangerouslySetInnerHTML re-asserted in this plan's verify. |
| Clipboard write → operator terminal | ClipboardCopyAction writes UI-supplied commands via navigator.clipboard. The clipboard write itself is intentional — the user's terminal is the confirmation. No XSS surface here (we're writing OUT not IN). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-D1 | XSS via project/task name | dashboard/web/src/components/{StatusBadge,ProjectPicker,ClipboardCopyAction,WaveBackground}.tsx | mitigate | React escapes text nodes by default; grep gate in verify asserts zero dangerouslySetInnerHTML uses. |
| T-04-D1-clipboard | Spoofing (clipboard hijack) | dashboard/web/src/components/ClipboardCopyAction.tsx | accept | navigator.clipboard requires user-initiated event (click); browser-platform guarantees this. Toast feedback gives user a chance to verify the copied text before pasting. |
</threat_model>

<verification>
- `cd dashboard/web && npm run test` passes (all 4 new component tests + the chrome tests from 04-12).
- `cd dashboard/web && npm run build` succeeds.
- `rg "dangerouslySetInnerHTML" dashboard/web/src/components/` returns zero hits.
- All 10 StatusBadge variants render in the table-driven test.
</verification>

<success_criteria>
- 4 primitive components shipped (StatusBadge, ProjectPicker, ClipboardCopyAction, WaveBackground).
- StatusBadge implements all 10 status values from UI-SPEC verbatim.
- ClipboardCopyAction success + failure paths emit Toasts with the locked UI-SPEC copy.
- WaveBackground renders bands with active/failed styling — consumable by plan 04-13's ExecutionDAGView.
- All tests pass; zero dangerouslySetInnerHTML; bundle still builds cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-15-SUMMARY.md` documenting the 4 components, the StatusBadge table-driven shape, the Toast-copy constants extended in `src/lib/toast-copy.ts`, and the WaveBackground SVG geometry consumed by ExecutionDAGView.
</output>
