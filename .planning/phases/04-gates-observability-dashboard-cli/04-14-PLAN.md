---
phase: 04
plan: 14
type: execute
wave: 8
depends_on: [04-05, 04-06, 04-08, 04-10, 04-11, 04-13, 04-16]
files_modified:
  - charts/tide/templates/dashboard-deployment.yaml
  - charts/tide/templates/dashboard-rbac.yaml
  - charts/tide/templates/dashboard-service.yaml
  - charts/tide/templates/servicemonitor.yaml
  - charts/tide/values.yaml
  - Makefile
  - test/e2e/dashboard_test.go
  - test/e2e/gate_flow_test.go
  - docs/dashboard.md
  - docs/observability.md
  - docs/gates.md
autonomous: false
requirements: [DASH-01, DASH-04, OBS-06]
threat_model_id: T-04-D2,T-04-O6,T-04-D-pitfall23
must_haves:
  truths:
    - "`charts/tide/templates/dashboard-deployment.yaml` ships dashboard as a separate Deployment + Service (ClusterIP, port 8080)"
    - "`charts/tide/templates/dashboard-rbac.yaml` ships a ServiceAccount + ClusterRole with EXACTLY {get,list,watch} verbs on TIDE CRDs + {get,list,watch} on pods + {get} on pods/log — NO wildcards, NO write verbs"
    - "`charts/tide/templates/servicemonitor.yaml` is gated by `.Values.prometheus.serviceMonitor.enabled` (default false, per CLAUDE.md anti-pattern)"
    - "`charts/tide/values.yaml` adds `dashboard.*` block (enabled=true default, image repo+tag, resources) + `prometheus.serviceMonitor.*` block (enabled=false default) + `otel.*` env vars for the controller manager"
    - "Helm template assertion: `helm template charts/tide` shows the dashboard Deployment + RBAC YAMLs when dashboard.enabled=true; suppresses them when false"
    - "Helm RBAC assertion (`make helm-rbac-assert`): the dashboard ClusterRole walks every `rules[].verbs` and exits non-zero if any verb is not in {get, list, watch}"
    - "E2E smoke test (`make test-e2e` extended): kind cluster + helm install + kubectl apply of a demo Project with gates.milestone=approve → observe Milestone phase=AwaitingApproval → run `tide approve` → observe Milestone resumes; ping `tide-dashboard/healthz` returns 200 after informer sync; `tide tail` of a Task pod streams + Ctrl-C cancels within 1s"
    - "docs/{dashboard,observability,gates}.md exist as Phase 4 stubs documenting install, env vars, and gate vocabulary — Phase 5 expands"
  artifacts:
    - path: charts/tide/templates/dashboard-deployment.yaml
      provides: "Dashboard Deployment + Service, gated by dashboard.enabled"
      contains: "tide-dashboard"
    - path: charts/tide/templates/dashboard-rbac.yaml
      provides: "ServiceAccount + ClusterRole + ClusterRoleBinding, read-only verbs only"
      contains: "verbs:\n  - get\n  - list\n  - watch"
    - path: charts/tide/templates/servicemonitor.yaml
      provides: "Prometheus ServiceMonitor, gated by prometheus.serviceMonitor.enabled"
      contains: "ServiceMonitor"
    - path: test/e2e/dashboard_test.go
      provides: "E2E smoke: kind + helm install + dashboard /healthz + tide approve flow"
      contains: "TestDashboardE2E"
  key_links:
    - from: charts/tide/templates/dashboard-rbac.yaml
      to: charts/tide/templates/dashboard-deployment.yaml
      via: "Deployment.spec.serviceAccountName = '{{ include \"tide.fullname\" . }}-dashboard'"
      pattern: "tide.*-dashboard"
    - from: charts/tide/templates/servicemonitor.yaml
      to: charts/tide/values.yaml prometheus.serviceMonitor.enabled
      via: "{{ if .Values.prometheus.serviceMonitor.enabled }}"
      pattern: "\\.Values\\.prometheus\\.serviceMonitor\\.enabled"
---

<objective>
Ship the Helm chart additions for Phase 4 (dashboard Deployment + RBAC, ServiceMonitor template, values.yaml extensions) + an end-to-end smoke test in the existing kind harness + Phase-4 stubs of docs/{dashboard,observability,gates}.md (Phase 5 finalizes them). After this plan: `helm install tide` ships the controller + dashboard + (optionally) ServiceMonitor; the E2E test proves the kind harness produces a working integrated system.

Purpose: Plan 04-10 + 04-11 + 04-13 built the dashboard binary + frontend; this plan ships the chart that deploys it. The Helm RBAC assertion (T-04-D2 mitigation) enforces read-only ServiceAccount at template time so any future PR widening dashboard RBAC fails CI before it ships. The E2E test is the smallest possible "is the integrated system alive" check; full E2E + acceptance tests are Phase 5.

Output: 3 new Helm templates + values.yaml extensions + 2 new E2E test files + 3 docs stubs + Makefile target additions. This plan has a `checkpoint:human-verify` task because it touches the FIXED chart contract (per CLAUDE.md) and the operator should eyeball the chart diff + the rendered E2E output before approving.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-UI-SPEC.md
@charts/tide/values.yaml
@charts/tide/templates/deployment.yaml
@charts/tide/templates/manager-rbac.yaml
@charts/tide/templates/serviceaccount.yaml

<interfaces>
<!-- Helm value block additions (D-X3) -->

```yaml
# Additions to charts/tide/values.yaml
dashboard:
  enabled: true
  image:
    repository: ghcr.io/jsquirrelz/tide-dashboard
    tag: ""             # default to .Chart.AppVersion
    pullPolicy: IfNotPresent
  replicas: 1
  resources:
    requests: { cpu: 50m, memory: 64Mi }
    limits:   { cpu: 200m, memory: 256Mi }
  service:
    type: ClusterIP
    port: 80
    targetPort: 8080

prometheus:
  serviceMonitor:
    enabled: false   # CLAUDE.md anti-pattern: default false to avoid CRD-not-found
    interval: 30s
    scrapeTimeout: 10s

otel:
  exporter:
    endpoint: ""     # OTLP gRPC endpoint; empty disables OTel (no-op TracerProvider)
  tracesSampler: "parentbased_traceidratio"
  tracesSamplerArg: "0.1"
  serviceName: "tide-controller-manager"
```

<!-- Dashboard RBAC verbs — read-only only (T-04-D2) -->

```yaml
rules:
  - apiGroups: [tideproject.k8s]
    resources: [projects, milestones, phases, plans, tasks, waves]
    verbs: [get, list, watch]
  - apiGroups: [""]
    resources: [pods]
    verbs: [get, list, watch]
  - apiGroups: [""]
    resources: [pods/log]
    verbs: [get]
```

<!-- E2E test scaffolding — extends existing test/e2e/ from Phase 02.2 -->

Reference Layer B integration test structure: `test/integration/kind/` (Phase 02.2's harness). The new test/e2e/ tests reuse the same kind-cluster + helm-install scaffolding from `Makefile`'s existing `test-int-kind` target chain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helm chart additions — dashboard Deployment/Service/RBAC + ServiceMonitor + values.yaml</name>
  <read_first>
    - charts/tide/templates/deployment.yaml (controller-manager Deployment — full template; copy-and-adapt for dashboard)
    - charts/tide/templates/manager-rbac.yaml (ClusterRole structure)
    - charts/tide/templates/project-viewer-rbac.yaml (read-only verb set)
    - charts/tide/templates/serviceaccount.yaml (simple ServiceAccount shape)
    - charts/tide/values.yaml (existing values — model for nested Helm hierarchy)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (sections G, H, I — Helm chart additions)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-X3, D-O6)
  </read_first>
  <acceptance_criteria>
    - `charts/tide/templates/dashboard-deployment.yaml` is gated by `{{- if .Values.dashboard.enabled }}` (default true). Defines a Deployment named `{{ include "tide.fullname" . }}-dashboard` with serviceAccountName `{{ include "tide.fullname" . }}-dashboard` (the dashboard SA — different from controller SA). Single container named `dashboard` on port 8080 (HTTP) + 8081 (healthz/readyz); liveness probe `httpGet /healthz` port 8081; readiness probe `httpGet /readyz` port 8081. Resources from `.Values.dashboard.resources`.
    - `charts/tide/templates/dashboard-service.yaml` defines a `ClusterIP` Service exposing port 80 → targetPort 8080, gated by the same `dashboard.enabled` flag.
    - `charts/tide/templates/dashboard-rbac.yaml` declares ServiceAccount + ClusterRole + ClusterRoleBinding. ClusterRole rules contain ONLY {get, list, watch} verbs across the 6 TIDE CRDs + pods/pods/log per the interfaces block. Gated by `dashboard.enabled`. NO wildcards.
    - `charts/tide/templates/servicemonitor.yaml` is gated by `{{- if .Values.prometheus.serviceMonitor.enabled }}` (default false). When rendered, references the existing controller-manager metrics-service (selector: `control-plane: controller-manager`). Path `/metrics`. Interval from `.Values.prometheus.serviceMonitor.interval`.
    - `charts/tide/values.yaml` additions match the interfaces block exactly: `dashboard.*`, `prometheus.serviceMonitor.*`, `otel.*`. Default `dashboard.enabled=true`, `prometheus.serviceMonitor.enabled=false`, `otel.exporter.endpoint=""` (empty → no-op tracer per plan 04-03).
    - Controller-manager Deployment is extended to inject the OTel env vars from `.Values.otel.*` (OTEL_EXPORTER_OTLP_ENDPOINT, OTEL_TRACES_SAMPLER, OTEL_TRACES_SAMPLER_ARG, OTEL_SERVICE_NAME) via env block. (Empty endpoint → SDK no-op; no functional impact.)
    - `helm lint charts/tide` exits 0.
    - `helm template charts/tide --set dashboard.enabled=true` renders dashboard Deployment + Service + RBAC; `--set dashboard.enabled=false` suppresses them.
    - `helm template charts/tide --set prometheus.serviceMonitor.enabled=true` renders the ServiceMonitor; default (no flag) suppresses it.
  </acceptance_criteria>
  <action>
    Create the 4 new templates and update values.yaml. CRITICAL per CLAUDE.md: "chart is FIXED contract — binary catches up to chart, never reverse." The structural choices below mirror existing chart patterns; do NOT introduce new conventions.

    `charts/tide/templates/dashboard-deployment.yaml`: Copy `deployment.yaml`'s shell (apiVersion/kind/metadata blocks); replace name with `{{ include "tide.fullname" . }}-dashboard`; replace serviceAccountName with `{{ include "tide.fullname" . }}-dashboard`; replace container args/env with dashboard-appropriate values (no `--leader-elect`; dashboard is stateless). Drop the webhook port + cert volumes. Add OTel env vars sourced from `.Values.otel.*`. Top-level guard `{{- if .Values.dashboard.enabled }}` ... `{{- end }}`.

    `charts/tide/templates/dashboard-service.yaml`: ClusterIP Service, selector `app.kubernetes.io/name: tide-dashboard`, port 80 → targetPort 8080.

    `charts/tide/templates/dashboard-rbac.yaml`: Three resources separated by `---`: ServiceAccount + ClusterRole + ClusterRoleBinding. Verbs per the interfaces block. Use `{{ include "tide.labels" . }}` for standard label hash.

    `charts/tide/templates/servicemonitor.yaml`: Standard ServiceMonitor (apiVersion monitoring.coreos.com/v1) gated by `{{- if .Values.prometheus.serviceMonitor.enabled }}`. Selector matches the existing controller metrics-service.

    Update `charts/tide/values.yaml`: Append the 3 new top-level blocks (`dashboard:`, `prometheus:`, `otel:`) per the interfaces block. Include inline comments documenting each value's purpose + default rationale (especially `prometheus.serviceMonitor.enabled: false` per CLAUDE.md anti-pattern).

    Extend the controller Deployment template (`charts/tide/templates/deployment.yaml` — be surgical, only add env vars from `.Values.otel.*`; do NOT touch existing fields). The OTel env vars are read by the controller process at boot (plan 04-03 wired this).

    Add Makefile targets:
    - `helm-lint-validate: helm lint charts/tide && helm template charts/tide > /dev/null`
    - `helm-rbac-assert: helm template charts/tide --set dashboard.enabled=true | yq 'select(.kind == "ClusterRole" and .metadata.name | test("dashboard")) | .rules[] | .verbs[]' | sort -u | grep -vE "^(get|list|watch)$" ; if [ $? -eq 0 ]; then echo "FAIL: dashboard RBAC has non-readonly verbs"; exit 1; fi ; echo "PASS: dashboard RBAC is read-only"`
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; make helm-lint-validate &amp;&amp; make helm-rbac-assert &amp;&amp; helm template charts/tide --set dashboard.enabled=true | grep -c "tide.*-dashboard" | grep -E "^\s*[3-9]" &amp;&amp; helm template charts/tide --set prometheus.serviceMonitor.enabled=true | grep -c "kind: ServiceMonitor" | grep -E "^\s*1$"</automated>
  </verify>
  <done>Helm chart renders cleanly with all gates; dashboard RBAC has only {get,list,watch}; ServiceMonitor defaults off; helm lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: test/e2e/dashboard_test.go + test/e2e/gate_flow_test.go — kind harness smoke</name>
  <read_first>
    - test/integration/kind/ (Phase 02.2's existing kind harness — copy scaffolding pattern)
    - Makefile (existing test-int-kind* targets — chain pattern to reuse)
    - charts/tide/templates/dashboard-deployment.yaml (from Task 1 — the resource being tested)
    - .planning/phases/04-gates-observability-dashboard-cli/04-VALIDATION.md (CLI-04 + DASH-01 manual-only entries — these tests turn some of those into automated)
  </read_first>
  <acceptance_criteria>
    - `test/e2e/dashboard_test.go` contains `TestDashboardE2E` which:
      1. Reuses Phase 02.2's kind cluster setup (BeforeSuite spawns `kind create cluster` + helm install).
      2. Asserts dashboard Deployment reaches Ready (via `kubectl rollout status deploy/tide-dashboard -n tide-system --timeout=120s`).
      3. `kubectl port-forward svc/tide-dashboard 8080:80` in a goroutine; `curl http://localhost:8080/healthz` returns 200 (after informer cache sync, ~5s).
      4. Apply a demo Project YAML; `curl http://localhost:8080/api/v1/projects` returns the project in JSON.
    - `test/e2e/gate_flow_test.go` contains `TestGateApproveE2E` which:
      1. Same kind setup.
      2. Apply a Project with `spec.gates.milestone: approve`.
      3. Observe via kubectl that a Milestone reaches `Status.Phase=AwaitingApproval`.
      4. Run `tide approve <project>` (build the binary from cmd/tide first; the test invokes it via `os/exec.Command`).
      5. Eventually observe Milestone advances to `Status.Phase=Succeeded` (or further state in the chain).
    - Both tests timeout at 5 min; both clean up the kind cluster + helm release on completion (deferred via t.Cleanup or AfterSuite).
    - The new tests live under a `//go:build e2e` build tag so `go test ./...` (default) doesn't run them; only `make test-e2e` (with the tag) does.
    - Makefile target `test-e2e` is extended (or added) to include the new tests: `go test -tags e2e -timeout 10m ./test/e2e/...`.
  </acceptance_criteria>
  <action>
    Create `test/e2e/dashboard_test.go` with build tag `//go:build e2e`. Use the kind-cluster scaffolding from `test/integration/kind/`'s BeforeSuite (specifically `applyController()` and `ensureKindCluster()` helpers from Phase 02.2). If those helpers aren't exposed for cross-package use, vendor them into `test/e2e/setup.go`. Use Ginkgo v2 (per existing test convention).

    The test body uses `kubectl` via `os/exec.Command` (since the kind harness already proves kubectl works against the test cluster). For port-forward, use `client-go/tools/portforward` or shell out to `kubectl port-forward` in a goroutine with context cancel.

    Create `test/e2e/gate_flow_test.go` with the same build tag. The test:
    1. Builds `bin/tide` via `make tide-cli` (or `go build -o bin/tide ./cmd/tide`) in BeforeSuite.
    2. Applies the Project YAML.
    3. Eventually checks `kubectl get milestone -o jsonpath='{.status.phase}'` returns `AwaitingApproval`.
    4. Runs `bin/tide approve <project-name>` via os/exec.
    5. Eventually checks the Milestone advances.

    Update the Makefile:
    ```makefile
    test-e2e: dashboard-build tide-cli
    	go test -tags e2e -timeout 10m ./test/e2e/...
    ```

    Add a focused validation: the test ALSO verifies `tide tail` against a running Task pod. Spawn `bin/tide tail <task-name>` in a subprocess, capture stdout, after 2s send SIGINT, assert the process exits within 1 second AND captured stdout contains non-empty pod-log lines.

    Include `test/e2e/setup.go` with shared BeforeSuite/AfterSuite plus a `runCLI(args ...string) (stdout, stderr string, exit int, err error)` helper used by all E2E tests.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; (which kind &gt;/dev/null &amp;&amp; make test-e2e || echo "SKIP: kind not installed locally — CI runs this target")</automated>
  </verify>
  <done>Two E2E tests pass against a kind cluster; new tests behind `//go:build e2e` tag; Makefile target wires the build dependencies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — chart diff + E2E output</name>
  <action>Pause for the operator to eyeball the rendered Helm chart, confirm the RBAC stays read-only, run the E2E kind harness, and visually inspect the dashboard UI against the UI-SPEC contract. No code is written in this task — it is a gate that confirms Tasks 1 + 2 landed correctly before Phase 4 closes out.</action>
  <what-built>Tasks 1 + 2 built: dashboard Helm chart templates, RBAC assertion, ServiceMonitor template (gated off by default), values.yaml extensions, two E2E smoke tests in test/e2e/.</what-built>
  <how-to-verify>
    1. Run `git diff charts/tide/` and verify the chart additions match the CLAUDE.md FIXED-contract rule — no unintended changes to existing templates.
    2. Run `helm template charts/tide --set dashboard.enabled=true > /tmp/chart-on.yaml` and `helm template charts/tide --set dashboard.enabled=false > /tmp/chart-off.yaml`. Eyeball the diff to confirm the dashboard resources appear/disappear correctly.
    3. Run `make helm-rbac-assert` and confirm output is `PASS: dashboard RBAC is read-only`.
    4. Run `make test-e2e` (requires kind + Docker; if not available locally, defer to CI). Watch the test output to confirm:
       - Dashboard Deployment reaches Ready.
       - `/healthz` returns 200.
       - `tide approve` unblocks an AwaitingApproval milestone.
       - `tide tail` streams logs + cancels on SIGINT within 1s.
    5. Visit the dashboard UI manually via `kubectl port-forward svc/tide-dashboard 8080:80 -n tide-system` and confirm:
       - Side-by-side Planning + Execution DAGs render.
       - Status badges show with correct colors/icons per UI-SPEC.
       - Clicking a Task node opens the TaskDetailDrawer with the locked copy-command buttons.
       - Clicking "Open log stream" begins streaming.
    6. Confirm `cat docs/dashboard.md docs/observability.md docs/gates.md` shows the Phase 4 stubs (1-2 paragraphs each, pointing to Phase 5 for finalization).
  </how-to-verify>
  <resume-signal>Type "approved" if the chart diff is clean, E2E test passes, and the dashboard UI renders per UI-SPEC. Otherwise describe specific gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm chart → cluster RBAC | Dashboard ClusterRole MUST be read-only; any write verb is a privilege escalation surface (T-04-D2). |
| ServiceMonitor → Prometheus CRD | When `prometheus.serviceMonitor.enabled=false` (default) the template emits nothing — avoids CRD-not-found errors on plain clusters (CLAUDE.md anti-pattern). |
| Operator → ingress (Pitfall 23) | Documentation (docs/dashboard.md) covers nginx-ingress annotations needed for SSE through reverse proxies. Chart doesn't ship Ingress (per CONTEXT deferred). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-D2 | Privilege escalation | charts/tide/templates/dashboard-rbac.yaml | mitigate | `make helm-rbac-assert` walks rendered ClusterRole verbs; fails if any verb is not in {get, list, watch}. CI gate on every PR touching the chart. |
| T-04-O6 | Availability (ServiceMonitor breaks plain clusters) | charts/tide/templates/servicemonitor.yaml | mitigate | Gated by `.Values.prometheus.serviceMonitor.enabled` (default false) per CLAUDE.md anti-pattern. |
| T-04-D-pitfall23 | Availability (SSE through ingress) | docs/dashboard.md | accept | Operator-side ingress config is documented; chart doesn't impose an Ingress (deferred to v1.x). |
| T-04-Chart-Drift | Tampering (chart vs binary mismatch) | charts/tide/values.yaml + cmd/dashboard + cmd/manager | mitigate | Chart is FIXED contract; binary catches up (CLAUDE.md). Env vars OTEL_* + dashboard image tag tie chart to binary version. |
</threat_model>

<verification>
- `make helm-lint-validate` exits 0.
- `make helm-rbac-assert` exits 0 with `PASS: dashboard RBAC is read-only`.
- `helm template charts/tide --set dashboard.enabled=true | grep -c "tide.*-dashboard"` returns ≥ 3 (Deployment + Service + RBAC).
- `helm template charts/tide` (default values) returns 0 ServiceMonitor occurrences.
- `helm template charts/tide --set prometheus.serviceMonitor.enabled=true` returns exactly 1 ServiceMonitor.
- `make test-e2e` passes (requires kind + Docker — CI runs this).
- Manual verification (Task 3) approved.
- `docs/dashboard.md`, `docs/observability.md`, `docs/gates.md` exist as Phase 4 stubs.
</verification>

<success_criteria>
- 4 new Helm templates shipped (dashboard-deployment, dashboard-service, dashboard-rbac, servicemonitor).
- values.yaml has the dashboard / prometheus / otel blocks with documented defaults.
- Dashboard RBAC is provably read-only via `make helm-rbac-assert`.
- ServiceMonitor defaults off per CLAUDE.md anti-pattern.
- Two E2E tests cover the integrated system: dashboard health + gate approve flow + tide tail.
- 3 docs stubs exist for Phase 5 to expand.
- Human verification approves the chart diff + E2E output + manual dashboard render.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-14-SUMMARY.md` documenting: the 4 new chart templates + their gating flags; the OTel env injection on the controller Deployment; the helm-rbac-assert Makefile target; the E2E test surface (dashboard health + gate flow + tide tail); and the 3 docs stubs handed to Phase 5.
</output>
