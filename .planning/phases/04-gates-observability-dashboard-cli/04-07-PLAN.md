---
phase: 04
plan: 07
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - cmd/tide/main.go
  - cmd/tide/root_flags.go
  - cmd/tide/apply.go
  - cmd/tide/watch.go
  - cmd/tide/inspect_wave.go
  - cmd/tide/artifact_get.go
  - cmd/tide/describe_budget.go
  - cmd/tide/cmd_test.go
  - cmd/tide/inspect_wave_test.go
  - cmd/tide/describe_budget_test.go
  - go.mod
  - go.sum
  - Makefile
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
threat_model_id: T-04-C1,T-04-C2,T-04-C3
must_haves:
  truths:
    - "`tide --help` lists all 10 D-C3 subcommands (apply, watch, tail, approve, reject, cancel, resume, inspect-wave, artifact-get, describe-budget) plus `completion`"
    - "`tide` resolves kubeconfig via cli-runtime/genericclioptions chain (`$KUBECONFIG` → `~/.kube/config` → in-cluster SA); supports `--context` and `--namespace` flags"
    - "`tide apply -f project.yaml` wraps `kubectl apply -f` semantics by reading the YAML, doing client.Patch/Create against the K8s API, and surfacing CRD validation errors with friendly messages"
    - "`tide watch <project>` long-runs against a Watch on Project + child CRDs and prints status updates to stdout; Ctrl-C terminates cleanly within 1s via signal.NotifyContext + cmd.ExecuteContext (Pitfall 25)"
    - "`tide inspect-wave <plan> [--wave N]` prints a text/tabwriter table with columns NAME, STATUS, AGE, ATTEMPT, SCHEDULED-IN-WAVE"
    - "`tide describe-budget <project>` reads project.Status.Budget and renders cap/spend/bypass-state"
    - "`tide artifact-get <ref>` retrieves a PVC artifact via apiserver pod-exec proxy (short-lived); ref format documented in --help"
    - "`tide` writes ZERO files outside cmd-internal stdout/stderr (no local cache per D-C1)"
  artifacts:
    - path: cmd/tide/main.go
      provides: "cobra root command + rootCmd.AddCommand wiring + signal.NotifyContext + rootCmd.ExecuteContext"
      contains: "cobra.Command"
    - path: cmd/tide/root_flags.go
      provides: "genericclioptions.ConfigFlags wiring (--kubeconfig, --context, --namespace) + --output flag"
      contains: "genericclioptions.ConfigFlags"
    - path: cmd/tide/inspect_wave.go
      provides: "tide inspect-wave subcommand using text/tabwriter"
      contains: "text/tabwriter"
  key_links:
    - from: cmd/tide/main.go
      to: cmd/tide/{apply,watch,inspect_wave,artifact_get,describe_budget}.go
      via: "rootCmd.AddCommand in each subcommand's init()"
      pattern: "rootCmd\\.AddCommand"
    - from: cmd/tide/root_flags.go
      to: k8s.io/cli-runtime/pkg/genericclioptions
      via: "ConfigFlags.AddFlags"
      pattern: "genericclioptions\\.NewConfigFlags"
---

<objective>
Ship the cobra-based `cmd/tide` CLI skeleton with the five read-only verbs that don't write back to K8s API objects: `apply` (one-shot Create/Update wrapper), `watch` (long-running list+watch), `inspect-wave` (tabular task render), `artifact-get` (apiserver-proxied PVC fetch), `describe-budget` (status read). The annotation-writer verbs (approve/reject/cancel/resume) + tail land in plan 04-08; the release pipeline in 04-09. This plan establishes the binary shape and the read-side surface so subsequent CLI verbs slot in via `rootCmd.AddCommand` only.

Purpose: Cobra root + kubeconfig-via-genericclioptions + signal-aware ExecuteContext is the foundation every subcommand reuses. Establish it once with thorough tests; later plans add verbs as ~50-LOC additions each.

Output: cmd/tide/{main,root_flags,apply,watch,inspect_wave,artifact_get,describe_budget}.go + per-subcommand `*_test.go` + go.mod additions for cobra v1.9.1 and cli-runtime + Makefile `tide-cli` target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md
@.planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md
@.planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md
@.planning/phases/04-gates-observability-dashboard-cli/04-04-PLAN.md
@cmd/credproxy/main.go
@api/v1alpha1/project_types.go

<interfaces>
<!-- Cobra v1.9.x — verified shape per RESEARCH.md Context7 lookup -->

```go
import "github.com/spf13/cobra"

var rootCmd = &cobra.Command{
    Use:   filepath.Base(os.Args[0]),  // resolves to "tide" or "kubectl-tide" (Pitfall 27)
    Short: "Manage TIDE Projects on Kubernetes",
    SilenceUsage: true,
    SilenceErrors: false,
}

// In main:
ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
defer stop()
if err := rootCmd.ExecuteContext(ctx); err != nil { os.Exit(1) }
```

Each subcommand file:
```go
var subCmd = &cobra.Command{
    Use: "watch <project>",
    RunE: func(cmd *cobra.Command, args []string) error { return run(cmd.Context(), args) },
}
func init() { rootCmd.AddCommand(subCmd) }
```

From k8s.io/cli-runtime/pkg/genericclioptions:
```go
flags := genericclioptions.NewConfigFlags(true)  // true = use persistent flags
flags.AddFlags(rootCmd.PersistentFlags())
// In subcommand RunE:
restConfig, err := flags.ToRESTConfig()
```

From api/v1alpha1/project_types.go (Budget surfaces for describe-budget):
```go
type Budget struct {
    AbsoluteCap        string         // resource.Quantity-shaped string
    RollingWindowHours int
    CurrentSpend       resource.Quantity
    LastWindowSpend    resource.Quantity
    BypassExpiresAt    *metav1.Time
}
type ProjectStatus struct {
    Budget Budget
    // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cmd/tide skeleton — main.go + root_flags.go + apply + watch + tests</name>
  <read_first>
    - cmd/credproxy/main.go (signal-handling + context shape — but cobra needs different boot)
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 555-566 Pitfall 25 — ExecuteContext; lines 580-591 Pitfall 27 — Krew naming)
    - .planning/phases/04-gates-observability-dashboard-cli/04-PATTERNS.md (section K — standalone binary boot)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-C1, D-C2, D-C3, D-C4)
  </read_first>
  <behavior>
    - Test 1: `go build ./cmd/tide` produces a working binary. Running it with no args prints help and exits 0.
    - Test 2: `tide --help` output lists subcommand names `apply`, `watch`, `inspect-wave`, `artifact-get`, `describe-budget`, `completion` (the remaining 5 verbs are stubs that print "not yet implemented" until plan 04-08 — but `tide --help` MUST list them).
    - Test 3: `tide --version` prints a stable shape (e.g., `tide version v0.0.0-dev` — version is `-X main.version=...` ldflag-injected; default `dev` when unset).
    - Test 4: Persistent flags `--kubeconfig`, `--context`, `--namespace`, `--output|-o` are available on every subcommand via `genericclioptions.NewConfigFlags(true).AddFlags(rootCmd.PersistentFlags())`. Test via `tide --help` output greps.
    - Test 5: Signal-aware execution — when a long-running subcommand (`tide watch`) is sent SIGINT, the cmd.Context() Done() channel fires and the subcommand returns within 1 second. Verify via `signal.NotifyContext + cmd.ExecuteContext` pattern.
    - Test 6: `tide apply -f testdata/project.yaml` reads the YAML, unmarshals into a generic `unstructured.Unstructured`, and calls `client.Patch(ctx, obj, client.Apply, client.FieldOwner("tide-cli"))` with server-side apply semantics. On CRD validation error, prints a friendly error citing the violated field. (Use a fake REST config + envtest-style or table-driven mock to avoid needing a real cluster in the unit test.)
    - Test 7: `tide watch <project>` long-runs against Project watch; on each event prints a one-line status: `<project>/<status.phase>/<active-milestone-count>`. Streaming output uses `cmd.OutOrStdout()` (test-substitutable). Ctrl-C terminates within 1s.
  </behavior>
  <action>
    Add cobra + cli-runtime to go.mod: `go get github.com/spf13/cobra@v1.9.1 k8s.io/cli-runtime@latest`. Run `go mod tidy`.

    Create `cmd/tide/main.go` with:
    - `var version = "dev"` (overridden by ldflags `-X main.version=...`)
    - `var rootCmd = &cobra.Command{Use: filepath.Base(os.Args[0]), Short: "Manage TIDE Projects on Kubernetes", Version: version, SilenceUsage: true}` — `Use` is filepath.Base for Pitfall 27 (kubectl-tide / tide dual invocation).
    - `func main() { ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM); defer stop(); if err := rootCmd.ExecuteContext(ctx); err != nil { fmt.Fprintln(os.Stderr, err); os.Exit(1) } }`

    Create `cmd/tide/root_flags.go`:
    - Package-level `var configFlags = genericclioptions.NewConfigFlags(true)`. In root's `init()`: `configFlags.AddFlags(rootCmd.PersistentFlags())`.
    - Add a custom persistent flag `var outputFormat string`; `rootCmd.PersistentFlags().StringVarP(&outputFormat, "output", "o", "human", "Output format: human, json")`. Enum-validated in subcommand RunE.
    - Expose a helper `func RESTConfig() (*rest.Config, error) { return configFlags.ToRESTConfig() }` for subcommands.
    - Expose helper `func K8sClient() (client.Client, error)` — builds a controller-runtime client.Client from the REST config with the scheme registered (mirror what cmd/manager/main.go does at scheme registration; reuse `api/v1alpha1.AddToScheme`).

    Create `cmd/tide/apply.go`: cobra command `Use: "apply"`, flag `-f file` (required, string). RunE: read file → `yaml.Decode` into `unstructured.Unstructured` (use `k8s.io/apimachinery/pkg/apis/meta/v1/unstructured`); call `client.Patch(ctx, obj, client.Apply, &client.PatchOptions{FieldManager: "tide-cli", Force: ptr.To(true)})`. On CRD validation `IsInvalid` error: parse the `metav1.StatusError`'s Details.Causes and print field-by-field with line annotations.

    Create `cmd/tide/watch.go`: cobra command `Use: "watch <project>"`, `Args: cobra.ExactArgs(1)`. RunE: build a controller-runtime client; use `client.Watch` (via `clientset.Watch` from client-go corev1, but the cleanest path is `dynamicClient.Resource(projects).Watch(ctx, ListOptions{FieldSelector: "metadata.name=<arg>"})`). Loop on the watch channel; for each event, print a status line via `text/tabwriter` flushed per event. Terminates cleanly on `ctx.Done()`.

    Create stub files for the remaining 8 verbs (`tail.go`, `approve.go`, `reject.go`, `cancel.go`, `resume.go`, `inspect_wave.go`, `artifact_get.go`, `describe_budget.go`) — each with a `&cobra.Command{Use: "<verb>", RunE: func(...) error { return errors.New("not yet implemented — landing in plan 04-08") }}` plus `init() { rootCmd.AddCommand(cmd) }`. The stubs make `tide --help` list every verb today; subsequent plans fill in real RunE.

    Tests in `cmd/tide/cmd_test.go`: Test 1, 2, 4 via `rootCmd.Execute()` redirected to a `bytes.Buffer`. Test 3 via reading `rootCmd.Version`. Test 5 via a subcommand that blocks on `<-cmd.Context().Done()`; spawn in goroutine; SIGINT the test process and assert exit within 1s. Test 6/7 via a fake REST config — leverage `controller-runtime/pkg/client/fake` for the client.Client.

    Add Makefile target `tide-cli: go build -o bin/tide ./cmd/tide` (mirrors existing `dashboard-build` style).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go build -o /tmp/tide-test ./cmd/tide &amp;&amp; /tmp/tide-test --help | grep -E "(apply|watch|inspect-wave|artifact-get|describe-budget|completion)" | wc -l | grep -E "^\s*[6-9]" &amp;&amp; go test ./cmd/tide/... -race -v</automated>
  </verify>
  <done>Binary builds, --help lists all 10 verbs + completion, --version prints, signal cancel works, apply + watch are real, tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tide inspect-wave + describe-budget + artifact-get</name>
  <read_first>
    - cmd/tide/main.go + root_flags.go (from Task 1)
    - api/v1alpha1/project_types.go (lines 220-238 Budget schema)
    - api/v1alpha1/task_types.go (Task spec — for inspect-wave column data)
    - .planning/phases/04-gates-observability-dashboard-cli/04-CONTEXT.md (D-C3 verbs + D-C4 output format)
    - .planning/phases/04-gates-observability-dashboard-cli/04-RESEARCH.md (lines 1318-1320 — CLI-03 column set NAME/STATUS/AGE/ATTEMPT/SCHEDULED-IN-WAVE)
    - text/tabwriter stdlib documentation
  </read_first>
  <behavior>
    - Test 1 (inspect-wave): `tide inspect-wave my-plan` lists all Tasks of the plan via `client.List(ctx, &TaskList{}, client.MatchingLabels{tide.controller/owner: my-plan})`. Output is `text/tabwriter`-formatted with 5 columns: NAME, STATUS, AGE, ATTEMPT, SCHEDULED-IN-WAVE. Header row is bold-style (tabwriter doesn't bold; use uppercase column names). Empty results: print `No tasks for plan my-plan.` to stderr and exit 0.
    - Test 2 (inspect-wave --wave N): Filters to tasks whose wave label == N. If --wave provided but no tasks match: `No tasks in wave N for plan my-plan.`
    - Test 3 (inspect-wave --output json): When `-o json`, emit a JSON array of `{name, status, age, attempt, wave}` objects.
    - Test 4 (describe-budget): `tide describe-budget my-project` reads project.Status.Budget and prints a 6-line human-readable block:
      ```
      Project: my-project
      Absolute cap:        $50.00
      Current window spend: $12.34 (rolling 24h)
      Last window spend:   $48.91
      Bypass active:       no
      Status:              within budget
      ```
      With `-o json` emit `{cap, currentSpend, lastWindowSpend, bypassActive, withinBudget}`.
    - Test 5 (artifact-get): `tide artifact-get <project>/<artifact-path>` proxies to apiserver pod-exec on a short-lived inspector pod or relies on `kubectl cp` semantics. For v1.0 simplicity, document the form `<project-namespace>/<project-name>/<relative-path-under-/workspace/artifacts/>` and use `clientset.CoreV1().Pods().GetLogs` is NOT the right call here — use the pods/exec subresource via `remotecommand.NewSPDYExecutor`. v1.0 minimum: spawn an inspector pod with `kubectl exec`-equivalent that reads the file from the PVC mount, streams its bytes to stdout. Exit non-zero with helpful error if PVC isn't mounted.
    - Test 6: All three subcommands accept `--namespace`, `--context`, `-o` from root flags.
  </behavior>
  <action>
    Replace the stubs from Task 1 for `inspect_wave.go`, `describe_budget.go`, `artifact_get.go` with real implementations.

    `cmd/tide/inspect_wave.go`: RunE uses controller-runtime client + `client.List(ctx, &v1alpha1.TaskList{}, client.MatchingLabels{"tide.controller/owner": planName})`. If --wave provided, post-filter by `task.Labels["tide.controller/wave"]`. Build a `tabwriter.NewWriter(cmd.OutOrStdout(), 0, 0, 2, ' ', 0)`, write header, loop printing `fmt.Fprintf(tw, "%s\t%s\t%s\t%d\t%d\n", ...)`. Flush at end. JSON path: marshal a `[]TaskRow{{Name, Status, Age, Attempt, Wave}}` slice via `json.NewEncoder(cmd.OutOrStdout()).Encode(rows)`.

    `cmd/tide/describe_budget.go`: RunE uses controller-runtime client + `client.Get(ctx, types.NamespacedName{...}, &project)`. Compute "within budget" via `project.Status.Budget.CurrentSpend < AbsoluteCap`. Human output uses fmt.Fprintf with 6 lines. JSON output marshals an inline struct.

    `cmd/tide/artifact_get.go`: RunE parses the ref as `<namespace>/<project>/<path>` (3 slash-separated parts; reject malformed refs with a friendly error). Build a transient inspector pod spec — image `busybox:1.36`, command `["sh","-c","cat /workspace/artifacts/<path>"]`, PVC mount `/workspace` from the project's PVC. Create the pod via client.Create with `ttlSecondsAfterFinished: 30` (auto-cleanup); stream pod logs via clientset.CoreV1().Pods().GetLogs to stdout; delete the pod on context done. (V1.0 acceptable simplicity tradeoff — RESEARCH §1318 hint.)

    Tests in `cmd/tide/inspect_wave_test.go` and `cmd/tide/describe_budget_test.go`: use `controller-runtime/pkg/client/fake.NewClientBuilder()` to build a populated fake client with N Task fixtures or 1 Project with Budget fields set, then call the subcommand's RunE via `rootCmd.ExecuteContext(ctx)` with arg slice. Assert against captured stdout buffer.

    For artifact-get, since it requires a real cluster, ship a focused unit test asserting ref parsing + a stub run path (e.g., `--dry-run` flag prints the pod spec that WOULD be created). Real cluster test lands in the kind harness in plan 04-14.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide &amp;&amp; go test ./cmd/tide/... -race -v &amp;&amp; go build ./cmd/tide/...</automated>
  </verify>
  <done>inspect-wave + describe-budget render correctly with both human and json output; artifact-get parses refs + has dry-run; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell → K8s apiserver | CLI inherits user's kubeconfig RBAC — no privilege escalation surface. Document the `--context` flag visibly in startup so operators know which cluster they're targeting (T-04-C-cluster-confusion). |
| Local filesystem → CLI process | CLI MUST NOT write a local cache (D-C1) — every invocation is stateless. Test via Pitfall-25-style monitoring or grep against the cmd/tide source for `os.Create` outside test files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-C1 | Tampering / cache poisoning | cmd/tide/* | mitigate | No local cache per D-C1; grep assertion in tests confirms zero `os.Create` / `ioutil.WriteFile` outside test files (verified via `rg "os\.Create|os\.WriteFile" cmd/tide/ | grep -v _test`). |
| T-04-C2 | Spoofing (verb impersonation) | cmd/tide/main.go | mitigate | Cobra registry locks the verb set at compile time; `tide --help` lists exactly the D-C3 verbs + completion. No dynamic verb registration. |
| T-04-C3 | Info disclosure (secret in output) | cmd/tide/describe_budget.go + inspect_wave.go | mitigate | Output renders ONLY Status fields (Phase, Age, Attempt, BudgetSpend). Never reads/prints Spec.Secrets or kubeconfig tokens. Test 5 of Task 2 asserts no secret-shaped strings in describe-budget output. |
| T-04-C-cluster-confusion | Privilege confusion | cmd/tide/root_flags.go | accept | Operator workflow concern, not exploitable — document in docs/cli.md. CLI prints current `--context` + namespace at startup of long-running watch/tail. |
</threat_model>

<verification>
- `go build ./cmd/tide/...` succeeds.
- `go test ./cmd/tide/... -race -v` passes (all subcommand tests).
- `bin/tide --help | wc -l` returns ≥ 12 (10 verbs + completion + help line).
- `bin/tide inspect-wave --help` documents the 5 columns.
- `bin/tide --version` outputs a non-empty version string.
- `make tide-lint` passes — no provider firewall violations (cobra is allowed; cli-runtime is allowed).
- `rg "os\\.Create|os\\.WriteFile" cmd/tide/ | grep -v _test` returns zero matches (no local cache).
</verification>

<success_criteria>
- cmd/tide binary builds and `--help` lists all 10 D-C3 verbs.
- Persistent flags --kubeconfig/--context/--namespace/-o wired via genericclioptions.
- 5 verbs fully implemented (apply, watch, inspect-wave, artifact-get, describe-budget).
- 5 verbs stubbed for plan 04-08 (tail, approve, reject, cancel, resume).
- Signal handling (Pitfall 25) — Ctrl-C cancels long-running watch within 1s.
- Stateless — no local file writes outside test files.
- Tests pass with -race.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gates-observability-dashboard-cli/04-07-SUMMARY.md` documenting the cobra root configuration, the genericclioptions wiring, each implemented verb's behavior, the stubs left for 04-08, and the tabwriter column convention.
</output>
