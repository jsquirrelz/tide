---
phase: 46-observability-enrichment-dashboard-deep-link
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/tide/values.yaml
  - charts/tide/templates/dashboard-deployment.yaml
  - hack/helm/assert-phoenix-env.py
  - hack/helm/assert-telemetry-render.sh
  - Makefile
  - docs/observability.md
  - internal/otelinit/doc.go
  - internal/otelinit/provider.go
autonomous: true
requirements: [OBS-01, OBS-04]
must_haves:
  truths:
    - "helm template charts/tide renders OTEL_TRACES_SAMPLER_ARG value \"1.0\" by default — a demo run no longer has a 90% per-run chance of an empty Phoenix (OBS-01, Pitfall 6)"
    - "No surface anywhere in the repo still describes the sampler default as 0.1/10% — values.yaml comment, otelinit doc comments, and the docs/observability.md env table all read 1.0/100% (D-01: a silent leftover is a doc bug)"
    - "The opt-down documentation is HONEST about sampled-flag semantics: at ratios below 1.0 only the Project root span is ratio-gated; descendant AGENT spans always export; a dropped root leaves orphaned trace fragments (RESEARCH Pitfall 3 wording)"
    - "phoenix.baseURL chart value exists (default \"\"), renders PHOENIX_BASE_URL on the dashboard Deployment only when set, and renders nothing when empty (D-10 — the PROM_ENDPOINT conditional pattern)"
    - "make helm-assert fails if the default render loses tracesSamplerArg 1.0 or if PHOENIX_BASE_URL renders when unset / fails to render when set"
    - "internal/otelinit is comment-only touched — TestNoWithSamplerInSource (Pitfall 24 guard) still passes; zero Go behavior change"
  artifacts:
    - path: "charts/tide/values.yaml"
      provides: "tracesSamplerArg \"1.0\" + rewritten comment block + new phoenix: block with baseURL \"\""
      contains: "phoenix:"
    - path: "charts/tide/templates/dashboard-deployment.yaml"
      provides: "conditional PHOENIX_BASE_URL env"
      contains: "PHOENIX_BASE_URL"
    - path: "hack/helm/assert-phoenix-env.py"
      provides: "render gate: PHOENIX_BASE_URL absent by default, present with exact value when set"
      min_lines: 40
    - path: "docs/observability.md"
      provides: "sampler default 1.0 in env table + honest opt-down section + phoenix.baseURL operator doc"
      contains: "phoenix.baseURL"
  key_links:
    - from: "charts/tide/values.yaml"
      to: "charts/tide/templates/dashboard-deployment.yaml"
      via: ".Values.phoenix.baseURL conditional env render"
      pattern: "\\.Values\\.phoenix\\.baseURL"
    - from: "Makefile"
      to: "hack/helm/assert-phoenix-env.py"
      via: "helm-telemetry-assert target (CI already runs make helm-assert at ci.yaml:223)"
      pattern: "assert-phoenix-env"
---

<objective>
Flip the chart's trace-sampler default 0.1 → 1.0 with every stale "10%" surface deliberately updated, add the `phoenix.baseURL` chart value + `PHOENIX_BASE_URL` dashboard env (the OBS-04 config root), extend the helm render gates so CI pins both, and write the operator docs including the honest opt-down text D-02's research finding requires.

Purpose: OBS-01 in full, plus the chart/env root of OBS-04's config chain (Plan 46-03 consumes the env; Plan 46-05 consumes the config API).
Output: chart values + template change, new/extended helm assert gates wired into `make helm-assert`, docs/observability.md updates, otelinit comment corrections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-CONTEXT.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-RESEARCH.md
@.planning/phases/46-observability-enrichment-dashboard-deep-link/46-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sampler default flip + phoenix.baseURL value + dashboard env (all "0.1" surfaces)</name>
  <files>charts/tide/values.yaml, charts/tide/templates/dashboard-deployment.yaml, internal/otelinit/doc.go, internal/otelinit/provider.go</files>
  <read_first>
    - charts/tide/values.yaml (otel block lines ~401-415 — the flip site + comment block; prometheus block lines ~352-395 — the shape analog for the new phoenix block)
    - charts/tide/templates/dashboard-deployment.yaml (PROM_ENDPOINT conditional block lines ~57-68 — the byte-for-byte template for PHOENIX_BASE_URL)
    - internal/otelinit/doc.go line ~41 and internal/otelinit/provider.go lines ~56 and ~108 (the three Go doc-comment surfaces still describing the 0.1/10% chart default)
    - internal/otelinit/provider_test.go (TestNoWithSamplerInSource — the Pitfall 24 guard that must stay green; comment-only edits must not introduce a `WithSampler(` call token)
  </read_first>
  <action>
    D-01, exhaustive-surface rule ("a silent leftover reading 'defaults to 10%' is a doc bug"). The complete inventory of surfaces pinning 0.1 (verified by repo grep at planning time): charts/tide/values.yaml:408-414, internal/otelinit/doc.go:41, internal/otelinit/provider.go:56, internal/otelinit/provider.go:108, docs/observability.md:166 (docs handled in Task 3).
    1. values.yaml: change `tracesSamplerArg: "0.1"` to `"1.0"`. Rewrite the comment block (lines ~407-409): default now traces 100% of root spans (v1.0.8 OBS-01 — TIDE's dispatch volume is dozens-to-hundreds of spans per run, not web-service QPS); point opt-down readers to docs/observability.md's opt-down section and its sampled-flag coherence caveat (D-02). Sampler stays env-driven — do NOT touch `tracesSampler` or any Go sampler code.
    2. values.yaml: add a new top-level `phoenix:` block (place near the `prometheus:` block, matching its umbrella shape): single key `baseURL: ""` with a doc comment stating — base URL of a self-hosted Arize Phoenix instance (install docs are Phase 47's); empty default means the dashboard's GET /api/v1/config reports phoenixBaseURL "" and the SPA renders no deep-link affordance at all — no dead buttons (OBS-04 requirement letter); requires Phoenix ≥ 14.2.0 (the /redirects/ shareable-URL routes).
    3. dashboard-deployment.yaml: add the conditional env after the PROM_ENDPOINT block, byte-for-byte shape swap — `{{- if .Values.phoenix.baseURL }}` / `- name: PHOENIX_BASE_URL` / `value: {{ quote .Values.phoenix.baseURL }}` / `{{- end }}`. No always-rendered boolean twin (empty string IS the sentinel — RESEARCH Pattern 3 note; unlike PROMETHEUS_ENABLED there is no legacy fallback to disambiguate).
    4. internal/otelinit/doc.go + provider.go: update the three doc comments to describe the new chart default (1.0 / 100%) — comment-only edits, zero behavior change. Keep the "env-driven, never WithSampler" wording intact.
  </action>
  <verify>
    <automated>helm template charts/tide --set dashboard.enabled=true | grep -A1 'name: OTEL_TRACES_SAMPLER_ARG' | grep -q '"1.0"' && go test ./internal/otelinit/... -run TestNoWithSamplerInSource -v</automated>
  </verify>
  <acceptance_criteria>
    - Default `helm template charts/tide` render contains OTEL_TRACES_SAMPLER_ARG with value "1.0" and contains NO PHOENIX_BASE_URL entry
    - `helm template charts/tide --set dashboard.enabled=true --set phoenix.baseURL=http://phoenix:6006` render contains PHOENIX_BASE_URL with value "http://phoenix:6006" on the dashboard container
    - grep '0\.1' charts/tide/values.yaml internal/otelinit/doc.go internal/otelinit/provider.go | grep -i sampl returns 0 matching lines
    - go test ./internal/otelinit/... fully green (guard untouched)
  </acceptance_criteria>
  <done>Chart defaults to full sampling, the phoenix.baseURL→PHOENIX_BASE_URL chain renders correctly in both states, and no code/comment surface still claims 0.1.</done>
</task>

<task type="auto">
  <name>Task 2: Helm render gates — sampler 1.0 assertion + assert-phoenix-env.py wired into make helm-assert</name>
  <files>hack/helm/assert-phoenix-env.py, hack/helm/assert-telemetry-render.sh, Makefile</files>
  <read_first>
    - hack/helm/assert-prometheus-env.py (whole file, ~128 lines — the exact script shape to sibling: locate the dashboard container, collect env entries by name, assert --expect-absent / --expect-endpoint value)
    - hack/helm/assert-telemetry-render.sh (Permutation E grep idiom at lines ~182-186 — `grep -A1 name-line | grep value-line` — the shape for the sampler assertion)
    - Makefile helm-telemetry-assert + helm-assert targets (lines ~707-719 — where the new render+assert steps slot; CI runs `make helm-assert` at .github/workflows/ci.yaml:223, so extending this target rides CI with zero workflow edits)
  </read_first>
  <action>
    1. New hack/helm/assert-phoenix-env.py: sibling of assert-prometheus-env.py with PROM_ENDPOINT swapped for PHOENIX_BASE_URL throughout — supports `--expect-absent` (default render must carry no PHOENIX_BASE_URL env on the dashboard container) and `--expect-value <url>` (set render must carry exactly that value). Same PyYAML multi-doc walk, same exit-1-with-message contract, same "found_dashboard_container" guard.
    2. assert-telemetry-render.sh: add a default-render permutation asserting `name: OTEL_TRACES_SAMPLER_ARG` is followed by `value: "1.0"` using the existing Permutation-E grep idiom (grep -A1 on the name line piped to a value grep). Fail with a die message citing OBS-01 if 0.1 (or anything other than "1.0") renders.
    3. Makefile helm-telemetry-assert: after the existing prometheus asserts, add two steps — run assert-phoenix-env.py against the already-rendered default output with --expect-absent, then render once more with `--set dashboard.enabled=true --set phoenix.baseURL=http://phoenix:6006` to /tmp/tide-helm-render-phoenix.yaml and assert --expect-value http://phoenix:6006. Keep the target's existing comment style.
  </action>
  <verify>
    <automated>make helm-telemetry-assert</automated>
  </verify>
  <acceptance_criteria>
    - `make helm-telemetry-assert` exits 0 on the current chart
    - Mutation check: temporarily setting values.yaml tracesSamplerArg back to "0.1" makes `make helm-telemetry-assert` exit non-zero (run, observe failure, revert — prove the gate bites)
    - Mutation check: temporarily removing the PHOENIX_BASE_URL conditional from dashboard-deployment.yaml makes the --expect-value step exit non-zero (run, observe, revert)
    - `make helm-assert` (the CI entrypoint) exits 0
  </acceptance_criteria>
  <done>CI's existing make helm-assert now pins both the 1.0 sampler default and the phoenix env render contract, with both gates proven to bite via mutation checks.</done>
</task>

<task type="auto">
  <name>Task 3: docs/observability.md — sampler table, honest opt-down section, phoenix.baseURL doc</name>
  <files>docs/observability.md</files>
  <read_first>
    - docs/observability.md (env/chart-value table at lines ~159-166 — the 0.1 row; the doc's existing declarative em-dash-heavy voice to match)
    - .planning/phases/46-observability-enrichment-dashboard-deep-link/46-RESEARCH.md §Pitfall 3 "How to avoid" (the exact honest-limitation wording basis) and §Open Questions #2 (default-project footnote)
  </read_first>
  <action>
    1. Update the env table row: `OTEL_TRACES_SAMPLER_ARG` / `otel.tracesSamplerArg` default column `0.1` → `1.0`.
    2. Add an opt-down subsection for high-volume installs (OBS-01's "opt-down documented" letter). It MUST state honestly (D-02/RESEARCH Pitfall 3 — do not imply coherent per-span ratio sampling): at ratios below 1.0 only the Project-level root span is gated by the ratio sampler; once a run's root is sampled, every descendant level's AGENT spans export in full (the ratio controls run-level visibility, not per-span volume); if the root is NOT sampled, descendant-level spans still export as an orphaned/rootless trace fragment in Phoenix. Note the one coherent path: each level's reporter-emitted LLM message spans follow the sampled flag of the traceparent the manager hands that level's reporter Job — after this phase (Plan 46-04's D-02 fix) that flag carries the emitting span's real sampling decision, so a ratio-dropped Project root also drops its own reporter's LLM spans; the deeper cross-level cascade remains a documented limitation (fixing it would need a persisted per-level sampled bit — deliberately out of scope, RESEARCH Alternatives Considered).
    3. Add a `phoenix.baseURL` section: what it is (base URL of a self-hosted Phoenix the dashboard deep-links into), default "" = no link rendered (no dead buttons), requires Phoenix ≥ 14.2.0 for the /redirects/spans/{span_id} and /redirects/traces/{trace_id} shareable-URL routes (cite the version explicitly — RESEARCH A1 says Phase 47 re-verifies the chart pin), and a one-line footnote that TIDE sets no OpenInference project-name resource attribute so traces land in Phoenix's default project (relevant to an operator browsing Phoenix's project list instead of following the deep link).
    Keep Phase 47's install instructions OUT (explicit non-goal — this doc section links the value, Phase 47 owns the install walkthrough).
  </action>
  <verify>
    <automated>grep -q 'phoenix.baseURL' docs/observability.md && grep -q '14.2.0' docs/observability.md && ! grep -E '\`0\.1\`' docs/observability.md | grep -qi sampler</automated>
  </verify>
  <acceptance_criteria>
    - The env table's OTEL_TRACES_SAMPLER_ARG row reads 1.0
    - The opt-down section contains the orphaned-fragment sentence (grep for "orphaned" returns >= 1 line) and does NOT claim descendant spans are ratio-sampled
    - The phoenix.baseURL section names both /redirects/ route shapes and the ≥ 14.2.0 floor
    - No remaining sampler-related "0.1"/"10%" text anywhere in the file
  </acceptance_criteria>
  <done>Operators can flip, opt down (with accurate expectations), and configure the deep link from docs alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → chart values | phoenix.baseURL is operator-supplied config rendered into a Deployment env and, downstream, into an anchor href |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing | phoenix.baseURL chart value (could point operators at a lookalike domain) | accept | operator-controlled Helm value at the same trust tier as otel.exporter.endpoint — no new trust boundary; downstream mitigation (visible external-link affordance, JSX escaping) lands in Plans 46-03/46-05; values.yaml doc comment states what the value is used for |
| T-46-SC | Tampering | package installs | accept | zero new packages — assert script uses the already-required python3+PyYAML toolchain (hack/helm precedent) |
</threat_model>

<verification>
- `make helm-assert` green (runs in CI at ci.yaml:223 unchanged)
- `helm template` renders verified in both default and phoenix-set permutations
- `go test ./internal/otelinit/...` green (comment-only change, guard intact)
</verification>

<success_criteria>
- OBS-01 satisfied in full: default 1.0, every 0.1 surface updated, opt-down honestly documented
- OBS-04's chart/env root exists and is CI-pinned in both render states
</success_criteria>

<output>
Create `.planning/phases/46-observability-enrichment-dashboard-deep-link/46-02-SUMMARY.md` when done.
</output>
