---
phase: 45-runtime-neutral-adapter-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/vendor_capabilities.go
  - pkg/dispatch/vendor_capabilities_test.go
  - internal/controller/reporter_jobspec.go
  - internal/controller/reporter_jobspec_test.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/controller/task_controller.go
autonomous: true
requirements: [ADAPT-01]
must_haves:
  truths:
    - "SelfInstruments(\"anthropic\") returns false, and unknown/empty vendor strings return false (D-03 default-safe polarity, pinned by test)"
    - "BuildReporterJob includes the bareword arg --skip-message-spans in the reporter container's Args iff ReporterOptions.SkipMessageSpans is true, composing uniformly with both Job shapes (trace-only and materialization)"
    - "All 5 reporter-spawn sites compute SkipMessageSpans via pkgdispatch.SelfInstruments(ResolveProvider(project, \"<own level>\", r.Deps.HelmProviderDefaults).Vendor), each using the SAME level literal its neighboring synthesizePlannerSpan call already uses"
    - "No per-runtime `if vendor ==` branch exists anywhere in internal/controller — the lookup table is the sole decision point (D-01)"
  artifacts:
    - path: "pkg/dispatch/vendor_capabilities.go"
      provides: "SelfInstruments(vendor string) bool — the ADAPT-01 routing datum, doc-contracted with D-02 trust rationale and D-03 default-safe rule"
      exports: ["SelfInstruments"]
      min_lines: 20
    - path: "pkg/dispatch/vendor_capabilities_test.go"
      provides: "D-10 default-polarity guard tests"
      contains: "TestSelfInstruments_UnknownVendorDefaultsFalse"
    - path: "internal/controller/reporter_jobspec.go"
      provides: "ReporterOptions.SkipMessageSpans field + conditional --skip-message-spans Args append"
      contains: "SkipMessageSpans"
    - path: "internal/controller/reporter_jobspec_test.go"
      provides: "D-11 flag-threading test on the built Job's Args slice"
      contains: "TestBuildReporterJob_SkipMessageSpansArg"
  key_links:
    - from: "internal/controller/{milestone,phase,plan,project,task}_controller.go (5 spawn sites)"
      to: "pkg/dispatch/vendor_capabilities.go"
      via: "fresh ResolveProvider call feeding SelfInstruments at each spawn site"
      pattern: "SelfInstruments\\(ResolveProvider"
    - from: "internal/controller/reporter_jobspec.go"
      to: "reporter Job container Args"
      via: "append gated on opts.SkipMessageSpans, placed after the --traceparent append block"
      pattern: "--skip-message-spans"
---

<objective>
Land the manager-side half of the ADAPT-01 runtime-neutral adapter seam: the `pkg/dispatch.SelfInstruments` capability lookup (flag-as-data, D-01), the `ReporterOptions.SkipMessageSpans` transport field riding the reporter Job's Args (the manager-authored, pod-tamper-proof channel, D-02/D-04), and the flag computation at all 5 reporter-spawn call sites.

Pure forward-compatibility scaffolding — every vendor resolves `false` today, so `--skip-message-spans` is never emitted and all Phase 42–44 behavior is byte-identical. The seam exists now so double-emission isn't discovered for the first time when the LangGraph beachhead milestone lands.

Purpose: ADAPT-01 success criterion 1 — "a self-instrumenting capability flag travels as data derived from the manager's resolved `Provider.Vendor` — never a hard-coded per-runtime branch in the reporter or manager."
Output: new `pkg/dispatch/vendor_capabilities.go` + guard test; `ReporterOptions`/`BuildReporterJob` extension + D-11 test; 5 spawn-site wirings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-runtime-neutral-adapter-seam/45-CONTEXT.md
@.planning/phases/45-runtime-neutral-adapter-seam/45-PATTERNS.md

Discretion calls made by the planner (within CONTEXT.md's explicit grants):
- Flag name/polarity: bareword `--skip-message-spans`, appended only when true — Go zero-value `false` satisfies D-03's absent-means-synthesize rule at every layer (research Pattern 2 recommendation).
- `ReporterOptions` carries the boolean `SkipMessageSpans` (not the vendor string) — keeps the lookup at the manager-side spawn sites only.
- `SelfInstruments` is a bare func over a `switch` (no `Capabilities` struct — only one capability bit exists). No test-injection hook: tests call `SelfInstruments` directly, and downstream tests set the booleans (`ReporterOptions.SkipMessageSpans` / `reporterConfig.SkipMessageSpans`) directly, so the production table stays unpolluted.

<interfaces>
<!-- Verified against main this session. Executor: use these directly — no exploration needed. -->

From internal/controller/dispatch_helpers.go:271 (pure, nil-safe — call it fresh per site, per the span_emission.go:122-125 "second, envelope-independent call" precedent):
```go
func ResolveProvider(project *tideprojectv1alpha3.Project, level string, helmDefaults ProviderDefaults) pkgdispatch.ProviderSpec
```

From internal/controller/dispatch_helpers.go:93 (current trailing params `traceParent string, otlpEndpoint string`; returns `(bool, error)`):
```go
func spawnReporterIfNeeded(ctx context.Context, c client.Client, scheme *runtime.Scheme,
    parent metav1.Object, project *tideprojectv1alpha3.Project, parentKind string,
    reporterImage string, pvcName string, traceParent string, otlpEndpoint string) (bool, error)
```

From internal/controller/reporter_jobspec.go:74-113 — ReporterOptions fields today: `ReporterImage`, `TraceParent` (:86), `OTLPEndpoint`, `TraceOnly` (:105), `TraceOnlyJobKey` (:113). Args assembly: shape-selecting `if opts.TraceOnly` branch (:195-210), then `--traceparent` conditional append (:213-215) — the anchor the new append follows.

The 5 spawn sites (file:line, spawn shape, level literal — literal confirmed identical to each site's neighboring synthesizePlannerSpan call):
| Site | Spawn shape | Level literal | synthesizePlannerSpan anchor |
|------|-------------|---------------|------------------------------|
| milestone_controller.go:639 | spawnReporterIfNeeded call | "milestone" | :607 |
| phase_controller.go:592 | spawnReporterIfNeeded call | "phase" | :564 |
| plan_controller.go:647 | inline ReporterOptions{} literal | "plan" | :608 |
| project_controller.go:1902 | inline ReporterOptions{} literal | "project" | :1857 |
| task_controller.go:1080 | inline ReporterOptions{} literal inside spawnTaskTraceReporterIfNeeded (:1057; called at :1124 and :1153 — compute ONCE in the function body) | "task" | :1006 |

Import status (PATTERNS.md correction to RESEARCH Pitfall 4): all five reconciler files already import `pkgdispatch "github.com/jsquirrelz/tide/pkg/dispatch"` — zero import edits needed anywhere in internal/controller.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the SelfInstruments vendor-capability table with D-10 polarity guard tests</name>
  <files>pkg/dispatch/vendor_capabilities.go, pkg/dispatch/vendor_capabilities_test.go</files>
  <read_first>
    - pkg/dispatch/provider.go — the vendor vocabulary this table keys on (`ProviderSpec.Vendor` doc comment lists canonical values "anthropic", "openai", "google", "xai", "opencode") and the package's doc-comment-heavy, zero-import style to match
    - pkg/dispatch/provider_test.go — plain-func `testing` style (no testify), doc comment on each test naming the D-number it proves
    - .planning/phases/45-runtime-neutral-adapter-seam/45-PATTERNS.md §"pkg/dispatch/vendor_capabilities.go" — research-verified file content, ready to use near-verbatim
  </read_first>
  <behavior>
    - Test 1 (TestSelfInstruments_KnownVendorsDefaultFalse): SelfInstruments(v) == false for each of "anthropic", "openai", "google", "xai", "opencode" — no self-instrumenting runtime exists yet
    - Test 2 (TestSelfInstruments_UnknownVendorDefaultsFalse): SelfInstruments("some-future-unregistered-vendor") == false and SelfInstruments("") == false — D-03 fail-closed default, Pitfall 7's "an unset flag should default to 'synthesize', never to 'assume native'"
  </behavior>
  <action>
    Write the failing tests first, then the implementation. Create pkg/dispatch/vendor_capabilities.go in package dispatch exporting exactly `func SelfInstruments(vendor string) bool` (D-01): a switch listing the five canonical vendor literals from ProviderSpec.Vendor's doc comment returning false ("CLI/wrapper-shimmed — no in-process OTel SDK"), with a `default` arm also returning false ("fail-closed: unknown vendor never skips synthesis"). Zero imports.

    The file-level doc comment is the D-08 doc contract: state that this is the ADAPT-01 runtime-neutral adapter seam's routing datum; that the manager consults it at reporter-spawn time and the reporter trusts only the manager-computed boolean carried on the Job (D-02 — never pod-writable data like in.json); that internal/reporter/tracesynth.go is the anthropic-CLI runtime's events.jsonl trace adapter this datum routes around; and the D-03 default-safe rule (false "native" silently loses spans; false "synthesize" produces at-worst-visible duplicates — always fail toward visibility). PATTERNS.md carries verified draft text for both the doc comment and the tests — use it.

    Do NOT add a test-injection hook, package-level table variable, or exported registration func — tests call SelfInstruments directly; no stub vendor entry goes in the production table.
  </action>
  <verify>
    <automated>go test ./pkg/dispatch/... -run TestSelfInstruments -v</automated>
  </verify>
  <acceptance_criteria>
    - pkg/dispatch/vendor_capabilities.go contains `func SelfInstruments(vendor string) bool`
    - The switch enumerates all five literals "anthropic", "openai", "google", "xai", "opencode" and every arm including `default` returns false
    - File doc comment mentions D-02 (or "manager-computed"/"Job"-carried trust rationale), D-03 (or "default-safe"/"fail-closed"), and names internal/reporter/tracesynth.go as the anthropic-CLI adapter
    - vendor_capabilities_test.go contains TestSelfInstruments_KnownVendorsDefaultFalse and TestSelfInstruments_UnknownVendorDefaultsFalse, both covering the "" and unknown-vendor cases per the behavior block
    - `go test ./pkg/dispatch/... -run TestSelfInstruments -v` exits 0 with both tests PASS
    - vendor_capabilities.go has zero import statements
  </acceptance_criteria>
  <done>SelfInstruments exists as a pure fail-closed lookup, D-10's polarity pin is green, and the D-08 doc contract for the capability side is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ReporterOptions.SkipMessageSpans and the --skip-message-spans Args append with the D-11 flag-threading test</name>
  <files>internal/controller/reporter_jobspec.go, internal/controller/reporter_jobspec_test.go</files>
  <read_first>
    - internal/controller/reporter_jobspec.go — ReporterOptions struct (:74-113, follow TraceOnly's "zero value is the existing behavior" doc-comment discipline) and BuildReporterJob's args assembly (:195-215, the --traceparent conditional append at :213-215 is the placement anchor)
    - internal/controller/reporter_jobspec_test.go — TestBuildReporterJob_TraceparentArg (:146), the exact two-subtest present/absent shape to mirror; note package is controller_test with the file-local newTestScheme() helper
    - .planning/phases/45-runtime-neutral-adapter-seam/45-PATTERNS.md §"internal/controller/reporter_jobspec.go" and §"reporter_jobspec_test.go" — verified field doc-comment draft and test skeleton
  </read_first>
  <behavior>
    - Test 1 (present when true): BuildReporterJob with ReporterOptions{ReporterImage: ..., SkipMessageSpans: true} produces a Job whose container Args contain the exact string "--skip-message-spans"
    - Test 2 (present in trace-only shape): same with TraceOnly: true and TraceOnlyJobKey set — the arg also appears, proving the flag composes with BOTH Job shapes uniformly (D-05's "uniformly for both Job shapes")
    - Test 3 (absent when false — D-03 default-safe): with SkipMessageSpans unset, no Args entry equals or has prefix "--skip-message-spans"
  </behavior>
  <action>
    Write TestBuildReporterJob_SkipMessageSpansArg first (mirroring TestBuildReporterJob_TraceparentArg's structure: project/parent fixture objects, newTestScheme(), subtests iterating job.Spec.Template.Spec.Containers[0].Args), then implement.

    In ReporterOptions, add field `SkipMessageSpans bool` alongside TraceOnly/TraceOnlyJobKey with a doc comment citing ADAPT-01/Phase 45: set true when the manager's pkgdispatch.SelfInstruments(vendor) lookup reports the dispatching vendor emits OpenInference spans natively; the reporter skips tracesynth.go's events.jsonl synthesis entirely; zero value (false) is the existing behavior, byte-identical pre-Phase-45 (every vendor resolves false today, D-03 default-safe).

    In BuildReporterJob, immediately after the existing `if opts.TraceParent != ""` append block, add: append the single bareword string "--skip-message-spans" to args only when opts.SkipMessageSpans is true (bareword like "--trace-only", NOT a "=value" flag — Go flag's Bool parsing handles presence=true; absence resolves false in the reporter, which is D-03's absent-means-synthesize for free). Placing it after the shape-selecting branch means it rides both shapes without duplication.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestBuildReporterJob_SkipMessageSpansArg -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/controller/reporter_jobspec.go contains `SkipMessageSpans bool` inside the ReporterOptions struct with a doc comment referencing ADAPT-01 or Phase 45 and the zero-value-safe framing
    - BuildReporterJob contains an `if opts.SkipMessageSpans` block appending exactly "--skip-message-spans" (no "=" suffix), positioned after the opts.TraceParent append
    - reporter_jobspec_test.go contains TestBuildReporterJob_SkipMessageSpansArg with the three subtests from the behavior block (materialization-shape present, trace-only-shape present, absent-when-false)
    - `go test ./internal/controller/... -run TestBuildReporterJob_SkipMessageSpansArg -v` exits 0
    - No Env entry is added to the reporter container (Args-only transport, D-04)
  </acceptance_criteria>
  <done>The transport layer exists: the boolean rides the manager-authored Job Args in both shapes, absent by default, proven by the D-11 test.</done>
</task>

<task type="auto">
  <name>Task 3: Compute the flag at all 5 reporter-spawn sites from a fresh ResolveProvider call</name>
  <files>internal/controller/dispatch_helpers.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/controller/task_controller.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go — spawnReporterIfNeeded (:93, signature + doc comment + its internal ReporterOptions literal) and ResolveProvider (:271)
    - internal/controller/span_emission.go:122-125 — the "second, envelope-independent call to ResolveProvider" precedent this task applies 5 times
    - .planning/phases/45-runtime-neutral-adapter-seam/45-PATTERNS.md §"internal/controller/{milestone,phase}_controller.go", §"{plan,project}_controller.go", §"task_controller.go" — verified before/after shapes for every site
    - The five spawn-site neighborhoods listed in this plan's interfaces table (milestone_controller.go:600-645, phase_controller.go:555-600, plan_controller.go:600-660, project_controller.go:1850-1915, task_controller.go:1050-1160)
  </read_first>
  <action>
    Six mechanical edits, all following the interfaces table's level literals exactly (Pitfall 2: each site's literal MUST equal the one its neighboring synthesizePlannerSpan call passes — "milestone"/"phase"/"plan"/"project"/"task"; never the level below):

    1. dispatch_helpers.go — add trailing param `skipMessageSpans bool` to spawnReporterIfNeeded (after `otlpEndpoint string`, mirroring how traceParent/otlpEndpoint were appended); thread it into the function's internal ReporterOptions literal as `SkipMessageSpans: skipMessageSpans`; extend the function's doc comment to document the new param citing Phase 45/ADAPT-01 (house style: exhaustive doc comments citing the phase that introduced each param — research Open Question 2's resolution).
    2. milestone_controller.go:639 — immediately before the spawnReporterIfNeeded call, add `skipMessageSpans := pkgdispatch.SelfInstruments(ResolveProvider(project, "milestone", r.Deps.HelmProviderDefaults).Vendor)` and pass it as the new trailing argument.
    3. phase_controller.go:592 — same shape with literal "phase".
    4. plan_controller.go:647 — compute with literal "plan" immediately before the inline BuildReporterJob call; add `SkipMessageSpans: skipMessageSpans` to the inline ReporterOptions literal.
    5. project_controller.go:1902 — same inline shape with literal "project".
    6. task_controller.go — compute ONCE inside spawnTaskTraceReporterIfNeeded's body immediately before the BuildReporterJob call at :1080, with literal "task"; add the field to that ReporterOptions literal. Do NOT touch the two call sites at :1124/:1153 (they pass through unchanged — one computation covers both paths, research Open Question 1's resolution).

    No import edits anywhere — all five files already import pkgdispatch (verified in PATTERNS.md). No `if vendor ==` branch anywhere — the lookup call IS the decision (D-01). Do NOT add spawn-gating (D-06: trace-only reporter Jobs still spawn for self-instrumenting vendors this phase).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... ./pkg/dispatch/... && test "$(grep -rln "SelfInstruments(ResolveProvider" internal/controller | wc -l | tr -d ' ')" = "5" && ! grep -rn "if.*[Vv]endor.*==" internal/controller cmd/tide-reporter internal/reporter</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "SelfInstruments(ResolveProvider" internal/controller` returns exactly 5 hits, one each in milestone_controller.go, phase_controller.go, plan_controller.go, project_controller.go, task_controller.go
    - milestone_controller.go's hit contains `"milestone"`, phase_controller.go's contains `"phase"`, plan_controller.go's contains `"plan"`, project_controller.go's contains `"project"`, task_controller.go's contains `"task"` (Pitfall-2 level-literal check)
    - task_controller.go has the computation inside spawnTaskTraceReporterIfNeeded's body; lines 1124/1153's call expressions are unchanged
    - spawnReporterIfNeeded's signature ends `..., otlpEndpoint string, skipMessageSpans bool) (bool, error)` and its doc comment mentions the new param
    - `grep -rn "if.*[Vv]endor.*==" internal/controller cmd/tide-reporter internal/reporter` returns zero hits (exit 1)
    - `go build ./...` and `go test ./internal/controller/... ./pkg/dispatch/...` exit 0
  </acceptance_criteria>
  <done>All 5 spawn sites carry the manager-computed capability boolean end-to-end as data; today it is always false, so emitted Job specs are byte-identical to Phase 44's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager → reporter Job spec (Args) | manager-authored at Job-create time; the subagent pod cannot write to it — the tamper-proof carrier for the skip decision (D-02) |
| subagent pod → PVC (in.json / events.jsonl) | pod-writable, semi-trusted; carries Provider.Vendor for prompt-seeding but MUST NOT drive control decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Spoofing / Repudiation | reporter skip decision (flag origin) | mitigate | Flag computed exclusively manager-side via SelfInstruments(ResolveProvider(...).Vendor) and carried only on manager-authored Job Args (this plan's Tasks 2–3); the reporter never re-derives it from pod-writable PVC data. Same trust boundary ResolveProvider→EnvelopeIn.Provider already uses. |
| T-45-02 | Tampering (config drift) | pkg/dispatch/vendor_capabilities.go | mitigate | Capability table is a compiled-in, reviewable Go switch (never a CRD/ConfigMap value) with fail-closed `default: false` (D-03); TestSelfInstruments_* (Task 1) pins the polarity so a flipped entry fails CI. Change requires code review + redeploy, not live mutation. |
| T-45-SC | Tampering (supply chain) | package installs | accept (N/A) | Zero new dependencies this phase — no go get / npm / pip / cargo; RESEARCH.md §Package Legitimacy Audit confirms the gate protocol is out of scope. |
</threat_model>

<verification>
- `go test ./pkg/dispatch/... ./internal/controller/...` green (D-11 test + D-10 guard + all existing Phase 42–44 controller tests unmodified — the behavior-unchanged proof for the manager side)
- Source-shape gate (ADAPT-01 criterion 1): `grep -rn "if.*[Vv]endor.*==" internal/controller cmd/tide-reporter internal/reporter` returns zero hits — allowed exceptions (pkg/dispatch/vendor_capabilities.go's switch; internal/subagent/anthropic's pre-existing vendorSentinel fail-fast) live outside the searched directories
- `grep -rln "SelfInstruments(ResolveProvider" internal/controller | wc -l` returns 5 (Pitfall 1: all five levels wired, not just the shared-helper two)
- No chart, RBAC, or go.mod diffs: `git diff --stat charts/ go.mod` is empty
</verification>

<success_criteria>
- ADAPT-01 criterion 1 satisfied on the manager side: the capability flag travels as data from ResolveProvider's Vendor through SelfInstruments into the reporter Job's Args at all 5 spawn sites, with zero per-runtime branches
- D-03 polarity pinned by tests; D-11 flag-threading proven on the built Job's Args slice
- Behavior unchanged today: the arg is never emitted (every vendor false), existing test suites pass unmodified
</success_criteria>

<output>
Create `.planning/phases/45-runtime-neutral-adapter-seam/45-01-SUMMARY.md` when done
</output>
