---
phase: 37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta
plan: 10
type: execute
wave: 4
depends_on: ["37-09"]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-03, DASH-04]
must_haves:
  truths:
    - "D-15 live repro executed: the log drawer shows honest states against (a) a running task and (b) a completed/GC'd task on a real cluster — the original empty-drawer bug is confirmed gone"
    - "An operator can complete an approve-gate review from the dashboard alone: artifact rendered beside the copyable approve command, no PVC reader pod"
    - "The project view and both resize/collapse surfaces behave per the UI-SPEC on a real deployment"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the three Phase-37 surfaces on a live cluster. D-15 is a LOCKED decision requiring in-phase live repro of the log-drawer fix with both a running task and a GC'd task — pod GC timing cannot be automated meaningfully (RESEARCH validation map marks it manual-only). This checkpoint also walks the approve-gate review flow end-to-end, the phase's headline operator outcome.

Prep is automated (image build + deploy commands are Claude's job where the environment allows); the human confirms observed behavior.

Purpose: DASH-01/03/04 UAT — the operator-facing truths verified where they live.
Output: operator approval or a gap list for `/gsd-plan-phase 37 --gaps`.
</objective>

## Artifacts this phase produces

**By this plan:** none (verification only).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-CONTEXT.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live UAT — log-drawer states (D-15), artifact view at an approve gate, project view, resize</name>
  <what-built>All Phase-37 surfaces: git-transported artifact view with typed states and approve strip (37-02/03/05/06/07), project settings panel (37-07/08), four-state log drawer (37-01), resizable/collapsible panel + log area (37-04/08), all integrated and gate-green (37-09).</what-built>
  <how-to-verify>
    Prep (Claude automates what the environment allows; otherwise run on the minikube/kind UAT cluster per the CLAUDE.md constrained-VM recipe — one heavy cluster at a time):
    1. Build + load the manager, dashboard, and tide-push images; helm upgrade the test install; apply a small Project WITH spec.git (bare or hosted remote) and an approve-policy gate at milestone level.
    2. Port-forward the dashboard and open it in a browser.

    Verify — log drawer (D-15, the folded todo's repro):
    3. While a task pod is RUNNING: open its log stream → drawer streams lines (never blank; "Connecting…" then lines).
    4. After a task completes and its pod is GC'd: open its log stream → drawer shows "Logs no longer available — pod garbage-collected." with NO retry button, and the network tab shows NO repeating reconnect requests.
    5. Kill the port-forward mid-stream on a running task → drawer shows the reconnecting/stream-error copy with a Reconnect button (never blank, never claims pod-gone); restore and Reconnect resumes.

    Verify — artifact view + approve gate (DASH-01):
    6. When the milestone parks AwaitingApproval: click the milestone node → panel shows either the rendered artifact (markdown, GFM tables) or "Artifacts materializing" that resolves to the artifact within ~a minute, WITHOUT reopening the panel (10s poll).
    7. Confirm the pinned strip below the artifact: gate badge + "Approve"/"Reject" buttons that copy `tide approve <project>` / `tide reject <project>`; paste-run approve in a terminal → run proceeds. No PVC reader pod used at any point.
    8. Click a plan node after its planner ran → its own artifacts render (PLAN md + children JSON pretty-printed; the node's OWN planner output, not its parent's).

    Verify — project view (DASH-03):
    9. Click the project node → status strip labeled "Status", budget line, cards (outcome prompt verbatim, repository, models with the effort note, budget, gates, secrets shown as names with the value-not-shown suffix), collapsible raw YAML. Confirm no secret VALUE appears anywhere (spot-check against the actual Secret).

    Verify — resize/collapse (D-06):
    10. Drag the panel's left edge (clamps at 360px/70vw), collapse to the rail and re-expand, reload the page → width and collapsed state persist. Same for the log area's top-edge handle and collapse.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe failures (they route to /gsd-plan-phase 37 --gaps)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | Verification-only plan; no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-10-01 | Information Disclosure | UAT step 9 explicitly hunts for secret-value leakage in the live UI | high | mitigate | Human spot-check against the real Secret complements 37-07's automated raw-body test |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- Human confirmation of steps 3–10; any failure becomes a gap for `/gsd-plan-phase 37 --gaps`
</verification>

<success_criteria>
- D-15 satisfied on a live cluster; approve-gate review completed dashboard-only; project view + resize verified; no secret values visible
</success_criteria>

<output>
Create `.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-10-SUMMARY.md` when done.
</output>
