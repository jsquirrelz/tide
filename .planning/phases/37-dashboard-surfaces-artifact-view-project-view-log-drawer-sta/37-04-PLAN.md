---
phase: 37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/web/src/components/ResizeHandle.tsx
  - dashboard/web/src/components/ResizeHandle.test.tsx
  - dashboard/web/src/components/NodeDetailPanel.tsx
  - dashboard/web/src/components/NodeDetailPanel.test.tsx
autonomous: true
requirements: [DASH-01]
must_haves:
  truths:
    - "A right-side panel shell exists for all Planning-DAG node kinds with the full TaskDetailDrawer a11y kit — focus trap, Escape-close, focus restore, role=dialog (D-05)"
    - "The panel is drag-to-resize (left-edge handle, clamp 360px–70vw) and collapsible to a 32px rail; both persist across reloads via localStorage (D-06)"
    - "Collapse is distinct from close — Escape/backdrop close; the chevron collapses while keeping node selection alive"
    - "The resize handle is keyboard-operable: role=separator, arrow keys ±16px, Home/End jump to clamp limits"
  artifacts:
    - path: dashboard/web/src/components/ResizeHandle.tsx
      provides: "Hand-rolled shared drag/keyboard resize primitive (vertical + horizontal orientations) + usePersistedSize hook"
    - path: dashboard/web/src/components/NodeDetailPanel.tsx
      provides: "Generalized right-panel shell (kind/name header, resize, collapse rail, a11y kit) rendering caller-provided content"
  key_links:
    - "NodeDetailPanel embeds ResizeHandle on its left edge — width state drives the style the TaskDetailDrawer hardcodes at 420px"
    - "Plan 37-08 mounts ArtifactViewer / ProjectSettingsPanel as NodeDetailPanel children and reuses ResizeHandle for the App log-area height"
---

<objective>
Build the two frontend primitives the artifact and project views mount into: the hand-rolled ResizeHandle (react-resizable-panels was SUS-flagged and forces a layout refactor owned by the deferred IDE-layout phase — RESEARCH Alternatives) and NodeDetailPanel, the generalization of TaskDetailDrawer's right-side panel chrome for planning-node content (D-05, D-06).

Scope guard: this phase ships ONLY panel-width + log-area-height resize/collapse. The IDE-style dockable layout for all views is explicitly deferred (CONTEXT Deferred Ideas) — do not generalize beyond the two instances. TaskDetailDrawer itself is NOT modified — the task drawer keeps its existing behavior this phase.

Purpose: DASH-01 — the artifact viewer (37-05) and settings panel (37-08) need an a11y-complete, resizable shell.
Output: ResizeHandle + usePersistedSize; NodeDetailPanel shell with collapse rail; component tests.
</objective>

## Artifacts this phase produces

**By this plan:**
- `dashboard/web/src/components/ResizeHandle.tsx`: `ResizeHandle` component — props `{ orientation: "vertical" | "horizontal"; value: number; min: number; max: number; onChange(px: number): void; onCommit(px: number): void; label: string }`; exported hook `usePersistedSize(storageKey: string, defaultPx: number, min: number, max: number): [number, (px: number) => void, commit: () => void]`
- `dashboard/web/src/components/NodeDetailPanel.tsx`: `NodeDetailPanel` — props `{ open: boolean; kind: "project" | "milestone" | "phase" | "plan"; name: string; onClose(): void; children: ReactNode }`; localStorage keys `tide.dashboard.panel-width` and `tide.dashboard.panel-collapsed`
- Exported type `PlanningNodeKind = "project" | "milestone" | "phase" | "plan"` (from NodeDetailPanel.tsx; 37-08's kind-aware click routing imports it)

**By sibling plans:** 37-05 provides ArtifactViewer/ApproveStrip content; 37-08 wires clicks → panel and adds the log-area ResizeHandle instance in App.tsx.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-CONTEXT.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-UI-SPEC.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResizeHandle + usePersistedSize</name>
  <files>dashboard/web/src/components/ResizeHandle.tsx, dashboard/web/src/components/ResizeHandle.test.tsx</files>
  <read_first>dashboard/web/src/lib/sse.ts (the refs+effects hook style to mirror), dashboard/web/src/components/TaskDetailDrawer.tsx (a11y discipline + design-token styling idiom), .planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-UI-SPEC.md (section "6. ResizeHandle" — visual, a11y, persistence, clamps, motion)</read_first>
  <behavior>
    - Test 1: renders with role="separator", correct aria-orientation, and aria-valuenow/valuemin/valuemax reflecting value/min/max
    - Test 2: ArrowLeft/ArrowRight (vertical orientation) call onChange with value ∓/± 16; Home calls onChange(min); End calls onChange(max); values clamp at [min, max]
    - Test 3: pointer drag (pointerdown → pointermove → pointerup) calls onChange during move and onCommit exactly once on release
    - Test 4: usePersistedSize initializes from localStorage when a stored value exists (clamped), falls back to defaultPx otherwise, and commit() writes the current value to the storage key
  </behavior>
  <action>Hand-roll ResizeHandle per UI-SPEC §6: a 6px visible track (transparent; --color-border-strong on hover/drag/focus) with a 12px hit area via a before: pseudo-element, cursor col-resize/row-resize by orientation, no animation during drag (direct style writes are the CALLER's job — this component only reports px values). Pointer path: setPointerCapture on pointerdown, compute delta from the drag-start position and start value, clamp, onChange per move, onCommit on pointerup/pointercancel. Keyboard path: arrow keys ±16px per press (vertical: Left/Right; horizontal: Up/Down), Home/End jump to min/max, onCommit on keyup. A11y: role="separator", aria-orientation, aria-valuenow/min/max, aria-label from the label prop, tabIndex=0. usePersistedSize wraps useState + localStorage read-on-init (try/catch — storage may be unavailable; fall back to defaultPx) and exposes commit() that writes debounce-free on call (callers invoke it from onCommit, which already fires only on release/keyup — that IS the debounce). Styling via CSS custom properties + Tailwind utilities + clsx from ../lib/clsx; data-testid="resize-handle".</action>
  <verify>
    <automated>cd dashboard/web && npx vitest run src/components/ResizeHandle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx vitest run src/components/ResizeHandle.test.tsx` exits 0
    - `grep -c 'role="separator"' dashboard/web/src/components/ResizeHandle.tsx` returns 1
    - `grep -c 'setPointerCapture' dashboard/web/src/components/ResizeHandle.tsx` returns >= 1
    - No new npm dependency added for resizing (`grep -c 'resizable-panels' dashboard/web/package.json` returns 0)
  </acceptance_criteria>
  <done>Keyboard- and pointer-operable resize primitive with persistence hook, token-styled, zero new dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NodeDetailPanel shell — chrome, resize, collapse rail</name>
  <files>dashboard/web/src/components/NodeDetailPanel.tsx, dashboard/web/src/components/NodeDetailPanel.test.tsx</files>
  <read_first>dashboard/web/src/components/TaskDetailDrawer.tsx (COPY WHOLESALE: focus capture/restore effect ~lines 184-200, Escape-close ~203-210, Tab focus-trap onTrapKey ~213-231 with FOCUSABLE_SELECTOR ~168-169, backdrop + panel chrome ~237-267, the isOpen guard ~233), .planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-UI-SPEC.md (section "1. NodeDetailPanel" — header, resize, collapse, deep links)</read_first>
  <behavior>
    - Test 1: open panel renders role="dialog" aria-modal with header text `<kind>/<name>` (e.g. "milestone/m1"), children content, and a close button; Escape and backdrop click both invoke onClose
    - Test 2: the collapse chevron collapses the panel to a 32px rail (children hidden, rail visible with an expand affordance); clicking the rail expands back to the prior width; onClose is NOT called by collapse/expand
    - Test 3: dragging the left-edge ResizeHandle changes the panel width style; width persists to localStorage key tide.dashboard.panel-width on commit; collapsed state persists to tide.dashboard.panel-collapsed
    - Test 4: focus is captured into the panel on open and restored to the previously-focused element on close (mirror the TaskDetailDrawer test approach)
  </behavior>
  <action>Create NodeDetailPanel as a NEW component (do not modify TaskDetailDrawer): copy the drawer's backdrop + fixed top-0 right-0 panel chrome, focus trap, Escape-close, and focus-restore wholesale, then replace the hardcoded width:"420px" with state from usePersistedSize("tide.dashboard.panel-width", 420, 360, window.innerWidth * 0.7) — clamp max at 70vw computed at drag time, not mount time. Mount ResizeHandle (orientation "vertical", label "Resize panel") absolutely on the panel's left edge. Header: mono 18px/600 title `${kind}/${name}` (the exact existing task/{name} idiom), a collapse chevron button (lucide PanelRightOpen rotated per state, 32px hit target), and the existing X close button. Collapsed state renders a 32px-wide rail pinned right (button role, Enter/click expands; aria-label "Expand panel") — collapse hides content but keeps the component mounted so node selection survives (D-06: collapse ≠ close). Keep the transition: the existing 180ms cubic-bezier(0.4,0,0.2,1) slide for open/collapse, none during drag. Export PlanningNodeKind. All styling via tokens; data-testid on panel, rail, chevron, close.</action>
  <verify>
    <automated>cd dashboard/web && npx vitest run src/components/NodeDetailPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx vitest run src/components/NodeDetailPanel.test.tsx` exits 0 with all four behavior tests green
    - `grep -c 'tide.dashboard.panel-width' dashboard/web/src/components/NodeDetailPanel.tsx` returns >= 1 and `grep -c 'tide.dashboard.panel-collapsed' dashboard/web/src/components/NodeDetailPanel.tsx` returns >= 1
    - `git diff --stat dashboard/web/src/components/TaskDetailDrawer.tsx` shows no changes (task drawer untouched)
    - `grep -c 'role="dialog"' dashboard/web/src/components/NodeDetailPanel.tsx` returns 1
  </acceptance_criteria>
  <done>Resizable, collapsible, a11y-complete panel shell ready to host artifact and settings content; TaskDetailDrawer untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage ↔ UI state | Persisted layout preferences read at mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-04-01 | Tampering | localStorage layout values | low | mitigate | Values parsed as numbers and clamped to [min,max] on read; non-numeric falls back to default — no layout-injection possible |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (resize is hand-rolled by decision — the SUS-flagged library is excluded) |
</threat_model>

<verification>
- `cd dashboard/web && npx vitest run src/components/ResizeHandle.test.tsx src/components/NodeDetailPanel.test.tsx` green
- No changes to TaskDetailDrawer.tsx, App.tsx, or any DAG view (integration is 37-08's job)
</verification>

<success_criteria>
- D-05/D-06 primitives exist: generalized panel shell with full a11y kit, drag+keyboard resize, collapse-to-rail, persistence
- Zero new dependencies; deferred IDE-layout scope not touched
</success_criteria>

<output>
Create `.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-04-SUMMARY.md` when done.
</output>
