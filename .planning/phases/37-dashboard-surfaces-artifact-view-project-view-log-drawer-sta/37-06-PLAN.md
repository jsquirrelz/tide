---
phase: 37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta
plan: 06
type: execute
wave: 2
depends_on: ["37-02"]
files_modified:
  - internal/controller/push_helpers.go
  - internal/controller/boundary_push.go
  - internal/controller/artifact_push.go
  - internal/controller/artifact_push_test.go
  - internal/controller/project_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "Every planner completion at all four levels (project, milestone, phase, plan) triggers a push carrying the cumulative envelope→destination map — artifacts exist in git BEFORE approve gates park is approached via the trigger firing before the gate-park return (D-01)"
    - "Artifact pushes ride the SAME deterministic tide-push Job as boundary pushes — single-flight preserved, no second writer class, no force-with-lease anchor break (R-05)"
    - "A gate-parked level retries the artifact trigger via RequeueAfter until a push lands — the AwaitingApproval early-return cannot permanently swallow the trigger (Pitfall 8)"
    - "Every push — boundary or artifact-triggered — carries the full cumulative map; a busy Job name loses nothing because the next push self-heals"
  artifacts:
    - path: internal/controller/artifact_push.go
      provides: "collectStageEnvelopes (cumulative map from the K8s API) + triggerArtifactPush (idempotent deterministic-Job dispatch)"
    - path: internal/controller/push_helpers.go
      provides: "PushOptions.StageEnvelopes []string + buildPushJob rendering --stage-envelopes"
    - path: internal/controller/boundary_push.go
      provides: "Boundary pushes also attach the cumulative map (every push is cumulative)"
  key_links:
    - "buildPushJob renders StageEnvelopes into the exact `--stage-envelopes=<uid>:<destPrefix>,...` CSV plan 37-02's parser accepts"
    - "triggerArtifactPush uses the same deterministic Job name as triggerBoundaryPush — the Phase 34 single-flight gate serializes both"
---

<objective>
Wire the write-path trigger: at every planner completion, the level controller requests a push whose staging map carries ALL planner-completed levels (cumulative, idempotent — mirrors Phase 34's cumulative Succeeded-branch re-merge), so planning artifacts land on the run branch at materialization time, before approve gates park (D-01, R-01, R-05). No new push mechanism: PushOptions grows a StageEnvelopes field, buildPushJob renders it, and both the new triggerArtifactPush and the existing triggerBoundaryPush attach the map.

**PRECONDITION (hard):** Phase 34 is merged to main — it rewrites triggerBoundaryPush/tide-push internals (single-flight gate, verify-in-push, lastPushedSHA stamping). Phase 36 is merged (commit identity). Plan 37-02 is merged (the --stage-envelopes contract). The executor MUST re-read internal/controller/boundary_push.go, push_helpers.go, and the four handleJobCompletion sites at execution start and adapt the wiring to the LANDED Phase-34 shape — the pattern-map excerpts are pre-34 (RESEARCH "Dependency correction"). If Phase 34's landed surface diverges materially from the shapes assumed here (e.g. PushOptions renamed/moved), adapt to the landed shape and record the divergence in the SUMMARY — the CONTRACT (cumulative map on every push, single Job class, trigger-before-park, parked retry) is what must hold.

Purpose: DASH-02 (reworded per D-01/D-03 — see plan 37-02's objective for the verifier-facing wording) — the write path from planner completion to run branch.
Output: cumulative-map collection, trigger helper, four wiring sites + parked-arm retries, envtest coverage.
</objective>

## Artifacts this phase produces

**By this plan:**
- `internal/controller/push_helpers.go`: `PushOptions.StageEnvelopes []string` (each element `<uid>:<destPrefix>`); buildPushJob appends `--stage-envelopes=<csv>` when non-empty
- `internal/controller/artifact_push.go` (NEW): `func collectStageEnvelopes(ctx context.Context, c client.Client, project *tideprojectv1alpha2.Project) ([]string, error)`; `func triggerArtifactPush(ctx context.Context, c client.Client, scheme *runtime.Scheme, project *tideprojectv1alpha2.Project, level string, tidePushImage string) error`; per-reconciler thin wrappers mirroring the maybeTriggerBoundaryPush receiver shape
- Trigger call sites in all four level controllers: in handleJobCompletion immediately after spawnReporterIfNeeded, and in each AwaitingApproval early-return arm with RequeueAfter 30s
- destPrefix derivation rule (shared with 37-02/37-07): `project/<name>`, `milestone/<name>`, `phase/<name>`, `plan/<name>` — lowercase kind, CR name

**By sibling plans:** 37-02 implements the tide-push side of the flag; 37-09 locks the end-to-end truth in the kind suite.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-CONTEXT.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-RESEARCH.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-PATTERNS.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PushOptions.StageEnvelopes + collectStageEnvelopes + triggerArtifactPush</name>
  <files>internal/controller/push_helpers.go, internal/controller/boundary_push.go, internal/controller/artifact_push.go, internal/controller/artifact_push_test.go</files>
  <read_first>internal/controller/boundary_push.go (POST-34 shape: guard chain, deterministic Job name, PushOptions construction, per-reconciler wrappers — this is the mandatory re-verify), internal/controller/push_helpers.go (POST-34 buildPushJob arg rendering), internal/controller/dispatch_helpers.go (spawnReporterIfNeeded idempotency shape), one existing controller test using the fake/envtest client for Job-arg assertions (e.g. the boundary-push specs)</read_first>
  <behavior>
    - Test 1: collectStageEnvelopes on a project with 2 planner-completed Milestones, 1 planner-completed Phase, 1 still-planning Phase, and a planner-completed Project returns exactly 4 entries `<uid>:<kind>/<name>` (the still-planning Phase excluded); ordering is deterministic (sort by kind then name) so restages are byte-identical
    - Test 2: triggerArtifactPush creates a push Job whose args contain `--stage-envelopes=` with the collected CSV, plus the branch/lease/commit-message args the landed Phase-34 builder requires; commit message identifies an artifact-stage push and the triggering level
    - Test 3: when the deterministic push Job already exists, triggerArtifactPush is a no-op returning nil (single-flight respected; cumulative map self-heals later)
    - Test 4: guard chain — nil/git-less project (no spec.git or empty repoURL) and empty tidePushImage each skip without error (mirror triggerBoundaryPush's exact guard semantics)
  </behavior>
  <action>Add StageEnvelopes []string to PushOptions in push_helpers.go and render it in buildPushJob as a single `--stage-envelopes=` arg joining elements with commas, appended only when len > 0. Create internal/controller/artifact_push.go: collectStageEnvelopes lists the project's Milestone, Phase, and Plan CRs in the project namespace using the codebase's established owner/label lookup for project children (re-verify the exact selector idiom against how existing code lists a project's children — e.g. the wave-derivation or rollup paths — and reuse it, do not invent a new one), plus the Project itself; include a CR when its planner has completed — the predicate is "status shows planner output materialized", i.e. Status.Phase is beyond the planning states (confirm the exact status enum constants at execution start; exclude empty/Pending/Planning-class values); map each to fmt.Sprintf("%s:%s/%s", uid, lowercaseKind, name) and sort deterministically. triggerArtifactPush mirrors triggerBoundaryPush's landed shape: guard chain (nil project → nil; no git → silent skip; empty image → logger.Info skip), collect map (empty map → skip — nothing planner-completed yet), deterministic Job name Get → exists → return nil → NotFound → buildPushJob with PushOptions carrying Branch, LastPushedSHA, LeaksConfigMap, a commit message like the D-B2 convention identifying an artifact stage for the triggering level, and StageEnvelopes; Create tolerating AlreadyExists; increment the push-jobs metric with an artifact-reason label if the landed metric supports labels. ALSO: extend the landed triggerBoundaryPush construction so boundary pushes attach collectStageEnvelopes output too — every push cumulative (R-05). If Status.Git.BranchName is empty at trigger time, skip-and-return (the parked-arm requeue in Task 2 retries; Open Q4).</action>
  <verify>
    <automated>go test ./internal/controller/ -run TestArtifactPush</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/controller/ -run TestArtifactPush` exits 0 (Go-capable environment; use the package's existing envtest/fake harness idiom)
    - `grep -c 'StageEnvelopes' internal/controller/push_helpers.go` returns >= 2 (field + rendering)
    - `grep -c 'collectStageEnvelopes' internal/controller/boundary_push.go` returns >= 1 (boundary pushes cumulative too)
    - Deterministic ordering proven by a test asserting the exact CSV for a fixed fixture set
    - `go vet ./internal/controller/` clean
  </acceptance_criteria>
  <done>Cumulative map collection + trigger helper exist with triggerBoundaryPush parity; boundary pushes carry the map; all behavior unit/envtest-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the trigger at all four completion sites + parked-arm retries</name>
  <files>internal/controller/project_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go</files>
  <read_first>internal/controller/milestone_controller.go (handleJobCompletion around the spawnReporterIfNeeded call at ~line 594 and the AwaitingApproval early-return arm at ~line 256 — POST-34 positions may differ, anchor on the spawnReporterIfNeeded call and the Status.Phase=="AwaitingApproval" guard), internal/controller/phase_controller.go (~528 spawn site + its parked arm), internal/controller/plan_controller.go (~580 spawn site), internal/controller/project_controller.go (~1340 spawn site), .planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-RESEARCH.md (Pattern 2 + Pitfall 8)</read_first>
  <action>At each of the four reporter-spawn sites (project_controller ~1340, milestone_controller ~594, phase_controller ~528, plan_controller ~580 — anchor on the spawnReporterIfNeeded/BuildReporter call, not line numbers), invoke the per-reconciler triggerArtifactPush wrapper immediately after the reporter spawn and BEFORE any gate-park return, logging-and-continuing on error (artifact push failure must not fail planner-completion handling — the retry arms and the next push self-heal; log at Info with the error). In the milestone and phase AwaitingApproval early-return arms (the parked branches that return before handleJobCompletion on subsequent reconciles), add: attempt triggerArtifactPush (same log-and-continue), then return ctrl.Result{RequeueAfter: 30 * time.Second} instead of the bare return, so a parked level keeps retrying until a push lands (Pattern 2 option (a) — no new status fields; re-triggers are harmless: single-flight no-ops while busy, clean-tree skips empty commits once staged). If plan/project levels also have parked arms in the landed code, wire them identically; if a level has no parked arm, the completion-site trigger suffices. Add/extend an envtest spec in artifact_push_test.go: (a) a milestone planner completion creates a push Job with --stage-envelopes before the milestone parks at AwaitingApproval; (b) with the Job artificially pre-existing (busy), the parked milestone's reconcile requeues with a ~30s RequeueAfter and re-attempts on the next reconcile (the Pitfall 8 regression guard).</action>
  <verify>
    <automated>go test ./internal/controller/ -run 'TestArtifactPush|TestMilestone' </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'triggerArtifactPush' internal/controller/project_controller.go internal/controller/milestone_controller.go internal/controller/phase_controller.go internal/controller/plan_controller.go` shows >= 1 per file (four wiring sites)
    - Milestone + phase parked arms contain a RequeueAfter (grep the parked-arm region for RequeueAfter; region-scoped — other requeues elsewhere are fine)
    - Envtest proves: push Job with --stage-envelopes created at planner completion; parked-arm retry requeues while the Job name is busy
    - `go test ./internal/controller/` full package exits 0 (no regression in the existing controller specs)
  </acceptance_criteria>
  <done>All four levels trigger artifact pushes at planner completion; parked levels retry; failures degrade to retries, never to lost artifacts or failed reconciles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → push Job args | CR UIDs/names flow into Job args → tide-push filesystem paths |
| controller → K8s API | New list operations for child CRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-06-01 | Tampering | CR-name-derived destPrefix in Job args | medium | mitigate | Names are K8s DNS-1123 (no traversal chars possible at admission); tide-push validates fail-closed anyway (37-02 Task 1) — defense in depth, no path construction from user free-text |
| T-37-06-02 | Denial of Service | push-Job churn from 30s parked requeues | medium | mitigate | Single-flight deterministic name no-ops while busy; clean-tree skip prevents empty commits once staged; cadence ≈ tens of pushes per run (A3) |
| T-37-06-03 | Tampering | force-with-lease anchor break via a second push path | high | mitigate | By construction: zero new push mechanisms — StageEnvelopes rides the existing Job class that stamps lastPushedSHA (R-05/Pitfall 2); acceptance greps prove no separate push binary/client is introduced |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- `go test ./internal/controller/` fully green in the Go-capable environment
- Diff inspection: no new git/push client code in the controller — only PushOptions plumbing, map collection, trigger wiring
- End-to-end truth (artifacts actually on a real remote) is plan 37-09's kind-suite job
</verification>

<success_criteria>
- Planner completion at any level yields a push carrying the full cumulative envelope map, before gate-park; parked levels retry on a 30s cadence
- Single push-writer class preserved; boundary and artifact pushes are the same Job with the same lease/stamp machinery
</success_criteria>

<output>
Create `.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-06-SUMMARY.md` when done.
</output>
