---
phase: 37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta
plan: 09
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03", "37-04", "37-05", "37-06", "37-07", "37-08"]
files_modified:
  - test/integration/kind/artifact_staging_test.go
  - cmd/dashboard/embed/dist
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "A kind-suite test proves end-to-end: after planner completion, the bare remote's run branch carries .tide/planning/<kind>/<name>/ planning artifacts byte-identical to the envelopes, before/independent of any approval (reworded DASH-02 locked by regression test)"
    - "The embedded dashboard dist is rebuilt and committed — verify-dashboard-freshness green, bundle-size gate green with react-markdown included"
    - "The full Go unit tier and full frontend suite are green with all Phase-37 changes integrated"
  artifacts:
    - path: test/integration/kind/artifact_staging_test.go
      provides: "Layer B regression test for the git artifact write path (reworded DASH-02)"
    - path: cmd/dashboard/embed/dist
      provides: "Rebuilt embedded SPA including all Phase-37 frontend changes"
  key_links:
    - "The kind test asserts against the same .tide/planning/<kind>/<name>/ layout contract plans 37-02/37-06/37-07 share"
---

<objective>
Close the phase's integration loop: (1) a kind-suite (Layer B) regression test that locks the reworded DASH-02 truth end-to-end — planning artifacts land on the real remote run branch at planner completion via the tide-push pipeline, full-fidelity; and (2) the build closeout — embedded dist rebuild, bundle gate, freshness gate, full test tiers.

Verifier-facing note (Pitfall 10): DASH-02's REQUIREMENTS.md wording (size-capped ConfigMaps, truncation markers, owner-ref GC) is SUPERSEDED by CONTEXT D-01/D-03. The truth to verify is the reworded one carried in plan 37-02's objective. There is NO artifact-cleanup-on-CR-deletion behavior to test — artifacts deliberately persist on the run branch as the durable decision record (D-02).

**Environment discipline (CLAUDE.md):** `make test-int` runs in the dev Docker VM with the clean-cluster recipe (delete → recreate → pre-warm, one heavy run at a time). Read MAKE_EXIT and grep for go-test FAIL lines — Ginkgo-green is not suite-green.

Purpose: DASH-02 locked by regression; the phase's artifacts actually ship inside the binary operators run.
Output: kind regression test; rebuilt embed dist; all gates green.
</objective>

## Artifacts this phase produces

**By this plan:**
- `test/integration/kind/artifact_staging_test.go`: Layer B test (name it around artifact staging on the run branch) driving a small project through planner completion and asserting `.tide/planning/` content on the bare remote
- Rebuilt `cmd/dashboard/embed/dist` (committed — required by verify-dashboard-freshness)

**By sibling plans:** everything under test here was built in 37-01..37-08.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-CONTEXT.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-02-SUMMARY.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kind-suite regression — artifacts on the run branch at planner completion</name>
  <files>test/integration/kind/artifact_staging_test.go</files>
  <read_first>test/integration/kind/push_lease_test.go (the existing bare-remote fixture + push assertion idiom — reuse its remote-inspection helpers), test/integration/kind/suite_test.go (suite setup, image loading, namespaces), test/integration/kind/bare_project_test.go (how a minimal project is driven to planner completion), test/integration/kind/reporter_pod_test.go (how existing tests read envelope content for comparison)</read_first>
  <action>Add a Layer B test to the kind suite following the existing fixture idiom: create a project with a git remote (the suite's bare-repo fixture) and drive it until at least one level's planner completes and the artifact push lands (poll the bare remote). Assert: (1) the run branch contains `.tide/planning/<kind>/<name>/` entries for the planner-completed level(s), including at least one top-level `*.md` and — when the envelope has children — `children/*.json`; (2) content fidelity: each staged file's bytes equal the corresponding envelope file's bytes (read the envelope via the suite's established PVC/envelope-reading helper; if no such helper exists, assert exact byte equality against the fixture planner's known output instead — the suite's stub planner output is deterministic); (3) no dispatch-internal JSON files (the D-04 exclusions) appear anywhere under `.tide/`; (4) the artifacts are present WHILE the level is gate-parked if the fixture policy parks — or simply before project completion otherwise (D-01: materialization-time, not boundary-time). Also assert the boundary-push machinery still works after the artifact pushes (lastPushedSHA advances; no lease-rejected reasons in push envelopes — the Pitfall 2 guard). Respect suite conventions: deterministic timeouts via the suite's Eventually settings, cleanup via the suite's namespace teardown.</action>
  <verify>
    <automated>make test-int 2>&1 | tee /tmp/37-09-test-int.log; echo "MAKE_EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `make test-int` MAKE_EXIT is 0 (dev Docker VM, clean-cluster recipe) AND `grep -nE '^--- FAIL|^FAIL\s' /tmp/37-09-test-int.log` returns no lines (both layers green, not just Ginkgo)
    - The new test fails RED if the artifact-push trigger is disabled (verified once during development by temporarily running against a build without the 37-06 wiring or by asserting the specific `.tide/planning/` paths that only the new pipeline creates)
    - No lease-rejected push-envelope reasons appear in the run's push envelopes (grep the test's collected envelopes/log output)
  </acceptance_criteria>
  <done>The reworded DASH-02 is locked by a kind-level regression test proving materialization-time, full-fidelity, exclusion-correct artifact commits that coexist with the boundary-push lease machinery.</done>
</task>

<task type="auto">
  <name>Task 2: Build closeout — embed dist rebuild + all gates</name>
  <files>cmd/dashboard/embed/dist</files>
  <read_first>Makefile (dashboard-frontend at ~line 304, verify-dashboard-freshness at ~310 — read the recipe bodies so the outputs land where the gates expect)</read_first>
  <action>Run the full frontend build + gate chain: `make dashboard-frontend` (builds the SPA, runs the frontend test suite including the sub-500KB bundle-size gate with react-markdown/remark-gfm now in the bundle, and copies into cmd/dashboard/embed/dist), then `make verify-dashboard-freshness` (asserts the committed embed dist matches a fresh build). Commit the rebuilt embed dist per the executor's task protocol. Then run the Go unit tier `make test` and confirm green. If the bundle gate fails on size: first check whether the markdown libraries were accidentally imported into a shared chunk eagerly loaded by non-artifact views, and code-split the ArtifactViewer import (React.lazy) — do NOT remove features or raise the gate threshold.</action>
  <verify>
    <automated>make dashboard-frontend && make verify-dashboard-freshness && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make dashboard-frontend` exits 0 (bundle-size + no-dangerous-html gates green with the new deps)
    - `make verify-dashboard-freshness` exits 0 (embed dist committed and fresh)
    - `make test` exits 0 (full Go unit tier green with all Phase-37 Go changes)
    - `git status --porcelain cmd/dashboard/embed/dist` is empty after the closeout commit (nothing uncommitted)
  </acceptance_criteria>
  <done>The shipped binary embeds the Phase-37 dashboard; every automated gate in both languages is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/test cluster ↔ bare remote fixture | Test-only surfaces; no production boundary changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-09-01 | Repudiation | shipping an embed dist that diverges from source | low | mitigate | verify-dashboard-freshness gate is the mitigation — build reproducibility asserted |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | No NEW packages — `make dashboard-frontend` installs the lockfile pinned in 37-05 |
</threat_model>

<verification>
- `make test-int` MAKE_EXIT=0 + zero `--- FAIL` lines (VM, one heavy run, clean-cluster recipe)
- `make dashboard-frontend && make verify-dashboard-freshness && make test` all exit 0
</verification>

<success_criteria>
- Reworded DASH-02 regression-locked at the kind tier; no lease interference between artifact and boundary pushes
- Embedded dist current; bundle and freshness gates green; both full test tiers green
</success_criteria>

<output>
Create `.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-09-SUMMARY.md` when done.
</output>
