---
phase: 37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta
plan: 08
type: execute
wave: 2
depends_on: ["37-04", "37-05"]
files_modified:
  - dashboard/web/src/components/ProjectSettingsPanel.tsx
  - dashboard/web/src/components/ProjectSettingsPanel.test.tsx
  - dashboard/web/src/components/NodeClickContext.tsx
  - dashboard/web/src/components/TideNodeShell.tsx
  - dashboard/web/src/components/PlanningDAGView.tsx
  - dashboard/web/src/components/ExecutionDAGView.tsx
  - dashboard/web/src/components/GlobalExecutionDAGView.tsx
  - dashboard/web/src/components/__tests__/nodes.test.tsx
  - dashboard/web/src/App.tsx
autonomous: true
requirements: [DASH-01, DASH-03]
must_haves:
  truths:
    - "Clicking ANY Planning DAG node kind opens the right panel: project → settings, milestone/phase → artifacts, plan → existing plan detail behavior preserved plus artifacts (D-05, D-09)"
    - "The settings panel shows a live-status strip labeled `Status` (never the TIDE-Phase-colliding word — D-11), curated cards per D-10, and a collapsible raw-spec YAML section"
    - "Gate-parked milestone/phase nodes render the ApproveStrip pinned below the artifact content (D-08)"
    - "The App log area is drag-to-resize + collapsible via the shared ResizeHandle, persisted to localStorage (D-06 second instance)"
  artifacts:
    - path: dashboard/web/src/components/ProjectSettingsPanel.tsx
      provides: "D-10 card stack: status strip, outcome prompt, repository, models, budget, gates, secrets (names only), raw-spec disclosure"
    - path: dashboard/web/src/components/NodeClickContext.tsx
      provides: "Kind-aware click signature (kind, name) with no-op default"
    - path: dashboard/web/src/App.tsx
      provides: "Panel content routing per kind + resizable log area + preserved #/plan deep link"
  key_links:
    - "TideNodeShell passes its existing kind prop into the context callback — all Planning-DAG kinds become clickable"
    - "App passes gateParked (node status AwaitingApproval) into ArtifactViewer — the materializing state and ApproveStrip both key off it"
---

<objective>
Integrate the Phase-37 frontend: the ProjectSettingsPanel content component (DASH-03), kind-aware node-click routing (RESEARCH Pattern 5), and the App-level assembly that mounts ArtifactViewer/ApproveStrip/ProjectSettingsPanel inside NodeDetailPanel — plus the second D-06 resize instance on the App log area (replacing the hardcoded 240px bottom panel).

Vocabulary lock (D-11): the status strip labels the Project CR lifecycle string `Status` — the word that names TIDE Phase CRs is reserved for DAG nodes and must not label this strip.

Purpose: DASH-01 + DASH-03 — the three surfaces become reachable and reviewable from the DAG.
Output: settings panel, kind-aware clicks across all node kinds, App content routing + resizable log area.
</objective>

## Artifacts this phase produces

**By this plan:**
- `dashboard/web/src/components/ProjectSettingsPanel.tsx`: props `{ projectName: string; namespace?: string; statusPhase: string; budgetSpentCents: number; budgetCapCents: number; conditions: ProjectCondition[] }` (status-strip data flows from the already-fetched project detail; cards fetch via fetchProjectSettings)
- `dashboard/web/src/components/NodeClickContext.tsx`: context type becomes `(kind: TideNodeKind, name: string) => void`; `useNodeClick()` updated
- `dashboard/web/src/App.tsx`: `selectedNode: { kind, name } | null` state; content routing; `tide.dashboard.log-height` localStorage key via usePersistedSize; log-area clamps [120px, 70% of viewport height]
- localStorage key: `tide.dashboard.log-height`

**By sibling plans:** 37-04 provides NodeDetailPanel/ResizeHandle; 37-05 provides ArtifactViewer/ApproveStrip/fetchers; 37-07 serves the data.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-CONTEXT.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-UI-SPEC.md
@.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProjectSettingsPanel — status strip + D-10 cards + raw-spec disclosure</name>
  <files>dashboard/web/src/components/ProjectSettingsPanel.tsx, dashboard/web/src/components/ProjectSettingsPanel.test.tsx</files>
  <read_first>dashboard/web/src/components/TaskDetailDrawer.tsx (MetaRow dl-grid card idiom ~lines 308-334), dashboard/web/src/lib/api.ts (fetchProjectSettings + ProjectSettings type from 37-05), .planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-UI-SPEC.md (section "4. ProjectSettingsPanel" — card order/contents + "Copywriting Contract > Project settings panel" — copy is LOCKED)</read_first>
  <behavior>
    - Test 1: status strip renders the label "Status" (and the strip contains no label reading like a TIDE-Phase column header), the lifecycle chip, budget line `spent $X.XX of $Y.YY cap` (cents → dollars, two decimals), and condition badges when conditions exist
    - Test 2: cards render in D-10 order — outcome prompt in a mono pre-wrap block (verbatim, NOT markdown-rendered), repository (repoURL mono; baseRef shows "HEAD (default)" when empty; branch when present), models per level with the muted note "effort: not yet configurable", budget, gates, secrets with each name suffixed "(name only — value not shown)"
    - Test 3: the raw-spec section is collapsed by default showing "Show raw spec (YAML)"; expanding renders the YAML in a pre block and toggles to "Hide raw spec"
    - Test 4: fetch failure renders the ErrorState idiom with a retry affordance (never a silent empty panel)
  </behavior>
  <action>Build ProjectSettingsPanel fetching fetchProjectSettings(projectName, namespace) on mount. Top-to-bottom per UI-SPEC §4: (1) live-status strip from PROPS (statusPhase/budget/conditions — the App already holds project detail; do not refetch): StatusBadge-style mono chip labeled "Status" (D-11), budget as mono `spent $<X> of $<Y> cap` formatting cents/100 to two decimals, ConditionBadge for blocking conditions. (2) Settings cards — each a --color-surface-raised bordered card with the 11px-uppercase-muted header idiom: Outcome prompt (full spec.outcomePrompt in mono 13px pre-wrap on --color-surface-overlay — operator input rendered verbatim, UI-SPEC Assumption 1), Repository (repoURL, baseRef || "HEAD (default)", branchName if set), Models (four level rows, model IDs mono, plus the locked muted effort note — no effort column exists in v1.0.7, Pitfall 11: do NOT hunt for or invent an effort field), Budget (cap/spent/remaining mono), Gates (per-level policy plain text + pauseBetweenWaves), Secrets (each ref: purpose label + mono name + the locked suffix). (3) Raw spec: details-style disclosure with the locked collapsed/expanded labels; expanded renders rawSpecYAML in the standard pre treatment. Loading state via LoadingState component. data-testid per card.</action>
  <verify>
    <automated>cd dashboard/web && npx vitest run src/components/ProjectSettingsPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx vitest run src/components/ProjectSettingsPanel.test.tsx` exits 0 with all four behavior tests green
    - Locked copy verbatim: `grep -c 'HEAD (default)' dashboard/web/src/components/ProjectSettingsPanel.tsx` returns 1; `grep -c 'effort: not yet configurable' dashboard/web/src/components/ProjectSettingsPanel.tsx` returns 1; `grep -c 'name only — value not shown' dashboard/web/src/components/ProjectSettingsPanel.tsx` returns 1; `grep -c 'Show raw spec (YAML)' dashboard/web/src/components/ProjectSettingsPanel.tsx` returns 1
    - The status strip label string is exactly "Status" (test asserts the label text)
    - Outcome prompt renders inside a pre-wrap block, not through the markdown component (`grep -c 'react-markdown' dashboard/web/src/components/ProjectSettingsPanel.tsx` returns 0)
  </acceptance_criteria>
  <done>DASH-03 content component complete with locked copy, verbatim prompt rendering, names-only secrets display.</done>
</task>

<task type="auto">
  <name>Task 2: Kind-aware node clicks across the DAG views</name>
  <files>dashboard/web/src/components/NodeClickContext.tsx, dashboard/web/src/components/TideNodeShell.tsx, dashboard/web/src/components/PlanningDAGView.tsx, dashboard/web/src/components/ExecutionDAGView.tsx, dashboard/web/src/components/GlobalExecutionDAGView.tsx, dashboard/web/src/components/__tests__/nodes.test.tsx</files>
  <read_first>dashboard/web/src/components/NodeClickContext.tsx (23 lines — the whole file), dashboard/web/src/components/TideNodeShell.tsx (kind prop + clickable gating + useNodeClick call site), dashboard/web/src/components/PlanningDAGView.tsx + ExecutionDAGView.tsx + GlobalExecutionDAGView.tsx (where providers supply the callback and which kinds pass clickable), dashboard/web/src/components/__tests__/nodes.test.tsx (existing click-behavior tests to update)</read_first>
  <action>Change NodeClickContext's value type from a name-only callback to `(kind: TideNodeKind, name: string) => void` (keep the no-op default for provider-less unit tests; update useNodeClick's return type). TideNodeShell: pass its existing kind prop into the callback at the click/keyboard-activation site (one-line change), and flip clickable to true for ALL Planning-DAG kinds (project, milestone, phase, plan — today only Plan is clickable in the Planning DAG; task nodes in Execution DAGs keep their current behavior). Update every provider: PlanningDAGView forwards (kind, name) to its callback prop; ExecutionDAGView/GlobalExecutionDAGView adapt their existing name-consumers to the new signature (task clicks keep routing to the task drawer — adapt the lambda, not the behavior). Update nodes.test.tsx assertions to the two-arg callback and add coverage: clicking a milestone node invokes the callback with ("milestone", name); a project node with ("project", name). Type errors are the safety net — `npx tsc -b` must be clean, which forces every consumer site to be updated.</action>
  <verify>
    <automated>cd dashboard/web && npx tsc -b && npx vitest run src/components/__tests__/nodes.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx tsc -b` exits 0 (all consumer sites migrated — no name-only callback remains)
    - `npx vitest run src/components/__tests__/nodes.test.tsx` exits 0 including the new milestone/project click assertions
    - `grep -c 'kind' dashboard/web/src/components/NodeClickContext.tsx` returns >= 2 (signature + doc)
    - In the Planning DAG, milestone/phase/project nodes render with the clickable affordance (test asserts role="button" present on those kinds)
  </acceptance_criteria>
  <done>Every Planning-DAG node kind emits (kind, name) clicks; Execution-DAG task behavior unchanged; types enforce total migration.</done>
</task>

<task type="auto">
  <name>Task 3: App assembly — content routing, gate strip, resizable log area</name>
  <files>dashboard/web/src/App.tsx</files>
  <read_first>dashboard/web/src/App.tsx (WHOLE FILE — selectedPlan/selectedTask/streamingTask state, onPlanClick + hash deep-link effect at ~294-310, the streaming-task-panel fixed bottom div at ~532-545 with its hardcoded 240px height, the three-way body conditional), dashboard/web/src/components/NodeDetailPanel.tsx (37-04 props), dashboard/web/src/components/ArtifactViewer.tsx + ApproveStrip.tsx (37-05 props)</read_first>
  <action>Wire the surfaces in App: (1) Replace the plan-only click handler with a kind-aware handler feeding new state `selectedNode: {kind, name} | null`; plan clicks keep the EXISTING behavior exactly (setSelectedPlan + #/plan/<name> hash) AND additionally set selectedNode so the plan panel can host artifacts; the #/plan deep-link effect keeps working unchanged. (2) Render NodeDetailPanel when selectedNode is a project/milestone/phase: project → ProjectSettingsPanel (status-strip props from the already-fetched project detail state; namespace from the existing namespace threading); milestone/phase → ArtifactViewer with project = the selected project name, gateParked = the node's status is AwaitingApproval (derive from the same DAG data that renders the node badge — reuse the status the node components already receive), plus ApproveStrip rendered below the viewer when gateParked. For plan nodes, render the artifact section alongside the existing PlanDetail pane content (keep the current selectedPlan pane; mount ArtifactViewer for the plan inside the NodeDetailPanel or the existing pane — choose the existing pane if the panel would fight the pane, and record the choice in the SUMMARY; the requirement is: a plan node's artifacts are visible on click). (3) Log area: replace the hardcoded height:"240px" on the streaming-task-panel div with usePersistedSize("tide.dashboard.log-height", 240, 120, Math.round(window.innerHeight * 0.7)) driving the height style, a horizontal ResizeHandle on its top edge, and a collapse toggle that shrinks the panel to a slim bar with an expand affordance (collapse ≠ close — the stream stays mounted). Existing PodLogStreamer props unchanged.</action>
  <verify>
    <automated>cd dashboard/web && npx tsc -b && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard/web && npx tsc -b` exits 0 and the full `npx vitest run` suite exits 0 (note: run vitest without watch mode; the bundle-size test may require `npm run build` first — if it fails solely for a missing dist, run the build then re-run)
    - `grep -c 'tide.dashboard.log-height' dashboard/web/src/App.tsx` returns >= 1 and the streaming-task-panel div no longer carries a hardcoded height literal (`grep -c '"240px"' dashboard/web/src/App.tsx` returns 0)
    - `grep -c 'ApproveStrip' dashboard/web/src/App.tsx` returns >= 1 and `grep -c 'ProjectSettingsPanel' dashboard/web/src/App.tsx` returns >= 1
    - The #/plan/<name> deep-link effect is unchanged (hash handling still present: `grep -c '#/plan/' dashboard/web/src/App.tsx` returns >= 2)
  </acceptance_criteria>
  <done>All three surfaces reachable from the DAG; gate-parked review flow assembled; log area resizable/collapsible with persistence; existing plan/task flows preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API responses → browser DOM | Settings + artifact data rendered in the operator's browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-08-01 | Information Disclosure | secret values reaching the settings UI | high | mitigate | Defense in depth: server redacts (37-07); the UI renders only the names-only secrets array and rawSpecYAML from the curated endpoint — no client-side Secret access exists |
| T-37-08-02 | Tampering | outcome prompt rendered as active content | medium | mitigate | Verbatim pre-wrap block, never markdown/HTML rendering (acceptance-gated: no markdown component import in the settings panel) |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd dashboard/web && npx tsc -b && npx vitest run` fully green
- Manual spot-check (dev server or plan 37-10 checkpoint): click project/milestone/phase/plan nodes → correct panel content; drag both handles; collapse both surfaces
</verification>

<success_criteria>
- D-09/D-10/D-11 satisfied: ProjectNode opens the settings panel in the shared right panel, correctly labeled and curated
- Kind-aware clicks route every Planning-DAG node to its content; gate-parked nodes pair artifact + approve strip (D-08)
- Both D-06 resize instances live (panel width from 37-04, log height here), persisted
</success_criteria>

<output>
Create `.planning/phases/37-dashboard-surfaces-artifact-view-project-view-log-drawer-sta/37-08-SUMMARY.md` when done.
</output>
