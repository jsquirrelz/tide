---
phase: 14-budget-enforcement-pricing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/pricing.go
  - pkg/dispatch/pricing_test.go
  - internal/subagent/anthropic/pricing.go
  - internal/subagent/anthropic/pricing_test.go
  - internal/subagent/anthropic/subagent.go
  - cmd/claude-subagent/main.go
  - internal/dispatch/podjob/jobspec.go
  - internal/dispatch/podjob/jobspec_test.go
autonomous: true
requirements: [BUDGET-01]
tags: [pricing, budget, provider-firewall, go]
must_haves:
  truths:
    - "estimatedCostCents resolves claude-fable-5, claude-opus-4-8, claude-opus-4-7, claude-opus-4-6 without the 'pricing: unknown model' stderr warning"
    - "claude-opus-4-7 costs $5/$25 per MTok, not the wrong $15/$75 entry"
    - "An unknown model ID falls back to the fable-5 rate ($10/$50), the new most-expensive tier"
    - "A pricing override supplied as JSON replaces a compiled entry without mutating the package-level priceTable"
  artifacts:
    - path: "pkg/dispatch/pricing.go"
      provides: "Provider-agnostic PriceOverride type + ParsePricingOverrides with positive-cents validation"
      exports: ["PriceOverride", "ParsePricingOverrides"]
    - path: "internal/subagent/anthropic/pricing.go"
      provides: "6-entry corrected priceTable + conservativeTier = fable-5"
      contains: "claude-fable-5"
    - path: "internal/dispatch/podjob/jobspec.go"
      provides: "BuildOptions.PricingOverridesJSON → TIDE_PRICING_OVERRIDES_JSON env on subagent container"
      contains: "TIDE_PRICING_OVERRIDES_JSON"
  key_links:
    - from: "cmd/claude-subagent/main.go"
      to: "internal/subagent/anthropic/subagent.go"
      via: "anthropic.Options.PricingOverrides populated from TIDE_PRICING_OVERRIDES_JSON env"
      pattern: "TIDE_PRICING_OVERRIDES_JSON"
    - from: "internal/subagent/anthropic/subagent.go:Run"
      to: "effective per-instance price table"
      via: "a.estimatedCostCents method using merged clone, never the package var"
      pattern: "maps\\.Clone\\(priceTable\\)"
---

<objective>
Correct and extend the compiled Anthropic price table (D-01) and add the provider-firewalled pricing-override mechanism (D-02, provider side): a provider-agnostic override type in pkg/dispatch, a merge-over-clone in anthropic.New(), an env-var transport on the subagent container, and the env read in cmd/claude-subagent.

Purpose: BUDGET-01 — sessions on current model IDs stop falling to the conservative default ("pricing: unknown model" per session in run 1), and the wrong opus-4-7 entry ($15/$75, Opus 4.1-era) stops over-billing by 3x. The override path lets operators correct future price drift without a TIDE release.

Architecture note (corrects 14-RESEARCH.md Assumption A2 / Open Question 1): the priceTable is compiled into the claude-subagent binary that runs INSIDE dispatch Jobs — cmd/manager never imports internal/subagent/anthropic (verified: no non-test importer outside cmd/claude-subagent). "Merged at controller startup" therefore physically means: Helm value → manager flag (Plan 14-05) → BuildOptions.PricingOverridesJSON → TIDE_PRICING_OVERRIDES_JSON env var on the subagent container (this plan) → claude-subagent parses → anthropic.New() merges (this plan). The manager treats the JSON as an opaque validated string; only the anthropic package interprets prices — provider firewall intact.

Output: corrected 6-entry priceTable, pkg/dispatch.PriceOverride + ParsePricingOverrides, per-instance effective table in Anthropic, env plumbing through jobspec + claude-subagent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-budget-enforcement-pricing/14-CONTEXT.md
@.planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§pricing.go, §pricing_test.go, §maps.Clone shared pattern)
@internal/subagent/anthropic/pricing.go
@internal/subagent/anthropic/subagent.go
</context>

<interfaces>
Ground-truth prices (D-01, verified 2026-06-11 — use these EXACT cent values, do not re-derive):

| Model ID | input ¢/MTok | output ¢/MTok | cacheRead (0.1×in) | cacheWrite (1.25×in) |
|---|---|---|---|---|
| claude-fable-5 | 1000 | 5000 | 100 | 1250 |
| claude-opus-4-8 | 500 | 2500 | 50 | 625 |
| claude-opus-4-7 | 500 | 2500 | 50 | 625 |
| claude-opus-4-6 | 500 | 2500 | 50 | 625 |
| claude-sonnet-4-6 | 300 | 1500 | 30 | 375 (already correct) |
| claude-haiku-4-5 | 100 | 500 | 10 | 125 (already correct) |

New provider-agnostic contract (pkg/dispatch/pricing.go — manager and anthropic both import pkg/dispatch already):

```go
// PriceOverride is a provider-agnostic per-model price correction in US cents
// per million tokens. Zero cache fields derive from input (read=0.10x, write=1.25x).
type PriceOverride struct {
    InputCentsPerMTok      int64 `json:"inputCentsPerMTok"`
    OutputCentsPerMTok     int64 `json:"outputCentsPerMTok"`
    CacheReadCentsPerMTok  int64 `json:"cacheReadCentsPerMTok,omitempty"`
    CacheWriteCentsPerMTok int64 `json:"cacheWriteCentsPerMTok,omitempty"`
}

// ParsePricingOverrides unmarshals s (empty or "{}" → empty map, nil error) and
// validates: Input and Output MUST be > 0; cache fields MUST be >= 0.
func ParsePricingOverrides(s string) (map[string]PriceOverride, error)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Correct and extend priceTable + reassign conservativeTier, tests first</name>
  <files>internal/subagent/anthropic/pricing.go, internal/subagent/anthropic/pricing_test.go</files>
  <read_first>internal/subagent/anthropic/pricing.go, internal/subagent/anthropic/pricing_test.go, .planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§pricing.go section)</read_first>
  <behavior>
    - fable5_input_output: estimatedCostCents("claude-fable-5", Usage{InputTokens: 1_000_000, OutputTokens: 1_000_000}) == 6000
    - opus47_corrected: estimatedCostCents("claude-opus-4-7", Usage{OutputTokens: 1_000_000}) == 2500 (regression: was 7500)
    - opus48_present and opus46_present: 1M output == 2500 each, no stderr warning
    - conservative_tier_is_fable5: estimatedCostCents("unknown-model", Usage{OutputTokens: 1_000_000}) == 5000
    - cache rates: fable-5 1M cacheRead == 100, 1M cacheWrite == 1250
  </behavior>
  <action>
    Add the new test cases above to TestEstimatedCostCents in pricing_test.go following the existing t.Run subtests (lines 26-121). Run to confirm they fail against the current 3-entry table. Then in pricing.go: (a) correct the "claude-opus-4-7" entry from 1500/7500/150/1875 to 500/2500/50/625; (b) add "claude-fable-5" (1000/5000/100/1250), "claude-opus-4-8" (500/2500/50/625), "claude-opus-4-6" (500/2500/50/625) using the exact values in the interfaces table; (c) reassign `var conservativeTier = priceTable["claude-fable-5"]` (Pitfall 6: after the opus-4-7 fix, fable-5 at $50/MTok output is the most expensive entry); (d) update the file-header price-source comment block (lines 26-29) to cite "claude-api skill model table, verified 2026-06-11; drift-checked weekly by hack/check-pricing-drift.sh per D-03" — the old "Anthropic pricing page, 2026-06" citation on the opus-4-7 line was wrong and must not survive. Keep haiku-4-5 and sonnet-4-6 untouched. Per D-01, keep the conservative-default fallback behavior and its loud stderr warning exactly as-is.
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/... -run TestEstimatedCostCents -count=1</automated>
  </verify>
  <acceptance_criteria>
    - pricing.go contains `"claude-fable-5"` and `"claude-opus-4-8"` and `"claude-opus-4-6"` map keys
    - pricing.go contains `conservativeTier = priceTable["claude-fable-5"]`
    - grep -c 'outputCentsPerMTok:     7500' internal/subagent/anthropic/pricing.go returns 0
    - go test ./internal/subagent/anthropic/... -run TestEstimatedCostCents -count=1 exits 0
  </acceptance_criteria>
  <done>Six-entry table with D-01 values, conservativeTier is fable-5, regression cases green.</done>
</task>

<task type="auto">
  <name>Task 2: Provider-agnostic PriceOverride type + ParsePricingOverrides in pkg/dispatch</name>
  <files>pkg/dispatch/pricing.go, pkg/dispatch/pricing_test.go</files>
  <read_first>pkg/dispatch/envelope.go (package doc style, license header, JSON-tag conventions), .planning/phases/14-budget-enforcement-pricing/14-RESEARCH.md (§Security Domain V5 row)</read_first>
  <behavior>
    - ParsePricingOverrides("") and ParsePricingOverrides("{}") return empty non-nil map, nil error
    - Valid JSON map of model-ID → {inputCentsPerMTok, outputCentsPerMTok} parses; omitted cache fields stay 0
    - InputCentsPerMTok <= 0 or OutputCentsPerMTok <= 0 → error naming the offending model ID
    - Negative cache field → error; malformed JSON → error
  </behavior>
  <action>
    Create pkg/dispatch/pricing.go with the exact PriceOverride struct and ParsePricingOverrides signature from the plan interfaces block (Apache 2.0 header matching envelope.go). Validation per ASVS V5 / threat T-14-01: reject zero or negative InputCentsPerMTok/OutputCentsPerMTok and negative cache fields, with the model ID in the error string so the operator's fix is mechanical. This type lives in pkg/dispatch (the provider-agnostic dispatch contract package, already imported by both cmd/manager and internal/subagent/anthropic) so the manager can validate the JSON at startup (Plan 14-05) WITHOUT importing the anthropic package — the provider firewall stays intact. Create pkg/dispatch/pricing_test.go covering the behavior list with table-driven subtests.
  </action>
  <verify>
    <automated>go test ./pkg/dispatch/... -run TestParsePricingOverrides -count=1</automated>
  </verify>
  <acceptance_criteria>
    - pkg/dispatch/pricing.go exports PriceOverride and ParsePricingOverrides
    - ParsePricingOverrides rejects `{"m":{"inputCentsPerMTok":0,"outputCentsPerMTok":100}}` with non-nil error containing "m"
    - go test ./pkg/dispatch/... -run TestParsePricingOverrides -count=1 exits 0
  </acceptance_criteria>
  <done>Manager-importable override contract exists with positive-cents validation.</done>
</task>

<task type="auto">
  <name>Task 3: Override merge in anthropic.New + env transport (jobspec stamp, claude-subagent read)</name>
  <files>internal/subagent/anthropic/subagent.go, internal/subagent/anthropic/pricing.go, internal/subagent/anthropic/pricing_test.go, cmd/claude-subagent/main.go, internal/dispatch/podjob/jobspec.go, internal/dispatch/podjob/jobspec_test.go</files>
  <read_first>internal/subagent/anthropic/subagent.go (Options struct lines 80-130, Run cost call line 289), cmd/claude-subagent/main.go (env-read precedent: TIDE_ENVELOPE_PATH/TIDE_TASK_UID, builder at line 45), internal/dispatch/podjob/jobspec.go (BuildOptions lines 79-135, subagentEnv block lines 332-360), internal/dispatch/podjob/jobspec_test.go</read_first>
  <behavior>
    - override_merge: Anthropic built with Options{PricingOverrides: {"claude-haiku-4-5": {Input: 200, Output: 1000}}} prices haiku 1M input at 200 cents; a second Anthropic with no overrides still prices it at 100 (package var untouched)
    - new_model_via_override: unknown-to-table model present in overrides prices at override rates with cache fields derived (read=0.10×input, write=1.25×input) when omitted
    - jobspec: BuildOptions.PricingOverridesJSON non-empty → subagent container env contains TIDE_PRICING_OVERRIDES_JSON with that value; empty → env var absent
  </behavior>
  <action>
    (a) anthropic: add `PricingOverrides map[string]pkgdispatch.PriceOverride` to Options (doc comment: D-02 — merged over a clone of the compiled table; never mutates the package var). In New(), build `prices := maps.Clone(priceTable)` (import "maps"; Go 1.26 per go.mod), then for each override: start from the existing modelPrice if the key is already in the table, apply non-zero override fields, derive cacheRead = input/10 and cacheWrite = input*125/100 when the override's cache fields are 0; store the merged map in a new `prices map[string]modelPrice` field on the Anthropic struct. Convert estimatedCostCents from a package function to a method `func (a *Anthropic) estimatedCostCents(model string, u pkgdispatch.Usage) int64` reading a.prices (fallback warning + conservativeTier behavior unchanged); update the call at subagent.go:289 to `a.estimatedCostCents(...)` and update existing pricing_test.go callers to go through `New(Options{})`. Mirror the same wiring in NewWithExec. Per Pitfall 2: NEVER write to the package-level priceTable.
    (b) cmd/claude-subagent/main.go: at the builder (line 45), read os.Getenv("TIDE_PRICING_OVERRIDES_JSON"); if non-empty, parse via pkgdispatch.ParsePricingOverrides; on error, print a loud stderr warning ("pricing: invalid TIDE_PRICING_OVERRIDES_JSON, ignoring overrides: <err>") and continue with the compiled table — a bad override must not fail the session (the manager validates at startup in Plan 14-05; this is defense in depth). Pass the parsed map as Options.PricingOverrides.
    (c) jobspec.go: add `PricingOverridesJSON string` to BuildOptions (doc comment: D-02 transport — raw validated JSON forwarded opaquely to the subagent container; the manager does not interpret prices). In the subagentEnv block (line 332), append corev1.EnvVar{Name: "TIDE_PRICING_OVERRIDES_JSON", Value: opts.PricingOverridesJSON} only when the field is non-empty. Stamp it for BOTH JobKindExecutor and JobKindPlanner (planner sessions also compute costs). Add/extend a jobspec test asserting presence-when-set and absence-when-empty.
    Populating the new BuildOptions field at the controller call sites is Plans 14-03/14-05 — do NOT touch the controllers in this plan.
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/... ./internal/dispatch/podjob/... ./pkg/dispatch/... -count=1 && go vet ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - subagent.go Options contains PricingOverrides map[string]pkgdispatch.PriceOverride
    - pricing.go or subagent.go contains maps.Clone(priceTable)
    - jobspec.go contains "TIDE_PRICING_OVERRIDES_JSON"
    - cmd/claude-subagent/main.go contains TIDE_PRICING_OVERRIDES_JSON and ParsePricingOverrides
    - go test ./internal/subagent/anthropic/... -run TestEstimatedCostCents -count=1 exits 0 (override_merge subtest green)
    - go test -race ./internal/subagent/anthropic/... -count=1 exits 0 (no concurrent map write — Pitfall 2)
  </acceptance_criteria>
  <done>Override JSON flows env → parse → merged per-instance table; package var immutable; both Job kinds carry the env var when configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values → manager flag → Job env | Operator-supplied pricing JSON crosses into billing math |
| Job container env → claude-subagent | Env readable/settable by anyone with Job-edit RBAC in the namespace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | pricing overrides (zero/negative prices make sessions appear free) | mitigate | ParsePricingOverrides rejects Input/Output <= 0 and negative cache cents; manager fails fast at startup (Plan 14-05); subagent ignores invalid JSON loudly |
| T-14-02 | Tampering | concurrent mutation of package-level priceTable | mitigate | maps.Clone per instance in New(); verified by `go test -race` |
| T-14-03 | Information Disclosure | pricing JSON in Job env | accept | Prices are public list rates, not secrets; no PII |
| T-14-SC | Tampering | package installs | accept | No new dependencies this phase (stdlib "maps" only); RESEARCH §Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `go test ./internal/subagent/anthropic/... ./pkg/dispatch/... ./internal/dispatch/podjob/... -count=1` exits 0
- `go test -race ./internal/subagent/anthropic/... -count=1` exits 0
- `grep -rn '7500' internal/subagent/anthropic/pricing.go` returns no priceTable value lines (old opus-4-7 output rate gone)
- `make lint` (golangci-lint) passes on touched packages
</verification>

<success_criteria>
All six model IDs resolve at D-01 rates; unknown IDs fall back to fable-5 rates; overrides merge per-instance without mutating the compiled table; TIDE_PRICING_OVERRIDES_JSON plumbing exists end-to-end on the provider side (controller population deferred to 14-03/14-05).
</success_criteria>

<output>
Create `.planning/phases/14-budget-enforcement-pricing/14-01-SUMMARY.md` when done.
</output>
