---
phase: 14-budget-enforcement-pricing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha1/shared_types.go
  - internal/budget/reservation.go
  - internal/budget/reservation_test.go
  - internal/controller/budget_blocked.go
  - internal/controller/budget_blocked_test.go
autonomous: true
requirements: [BUDGET-02, BUDGET-03]
tags: [budget, conditions, controller-runtime, go]
must_haves:
  truths:
    - "ConditionBudgetBlocked and ReasonBudgetCapReached exist as api/v1alpha1 constants"
    - "setBudgetBlockedIfNeeded stamps BudgetBlocked=True on the Project when IsCapExceeded, idempotently"
    - "setBudgetBlockedIfNeeded flips BudgetBlocked to False when the cap is no longer exceeded (cap raise recovers dispatch)"
    - "ReservationStore tracks reserve/settle/release totals concurrently and rederives from in-flight Job labels"
    - "HasHeadroom returns false when spent + reserved + estimate exceeds the cap; nil store and zero cap are permissive"
  artifacts:
    - path: "api/v1alpha1/shared_types.go"
      provides: "Phase 14 condition vocabulary"
      contains: "ConditionBudgetBlocked"
    - path: "internal/budget/reservation.go"
      provides: "In-process rederivable ReservationStore (sync.Map, taskUID→cents)"
      exports: ["ReservationStore", "NewReservationStore", "RederiveReservations"]
      min_lines: 80
    - path: "internal/controller/budget_blocked.go"
      provides: "checkBudgetBlocked + setBudgetBlockedIfNeeded mirroring billing_halt.go"
      contains: "setBudgetBlockedIfNeeded"
  key_links:
    - from: "internal/controller/budget_blocked.go"
      to: "internal/budget/cap.go"
      via: "budget.IsCapExceeded decides set vs clear"
      pattern: "budget\\.IsCapExceeded"
    - from: "internal/budget/reservation.go"
      to: "Job labels"
      via: "tideproject.k8s/estimated-cost + tideproject.k8s/task-uid labels at rederive"
      pattern: "tideproject.k8s/estimated-cost"
---

<objective>
Build the Phase 14 foundations: the BudgetBlocked condition vocabulary (D-04), the checkBudgetBlocked/setBudgetBlockedIfNeeded controller helpers mirroring Phase 13's billing_halt.go, and the in-process ReservationStore with restart rederivation (D-05). New files only plus one constants append — zero overlap with Plan 14-01, runs in parallel in Wave 1.

Purpose: BUDGET-02's condition machinery and BUDGET-03's reservation accounting must exist (with unit coverage) before Plan 14-03 wires them into the TaskReconciler dispatch gate. These are the Wave-0 test scaffolds from 14-VALIDATION.md.

Output: shared_types constants, internal/budget/reservation.go(+tests), internal/controller/budget_blocked.go(+tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-budget-enforcement-pricing/14-CONTEXT.md
@.planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§reservation.go, §budget_blocked.go, §shared_types.go sections — full skeletons live there)
@internal/controller/billing_halt.go
@internal/budget/bucket.go
@internal/budget/precharge.go
@internal/budget/cap.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 14 condition constants in shared_types.go</name>
  <files>api/v1alpha1/shared_types.go</files>
  <read_first>api/v1alpha1/shared_types.go (lines 216-239: the Phase 13 ConditionBillingHalt block this mirrors)</read_first>
  <action>
    Append a Phase 14 const block immediately after the Phase 13 BillingHalt block (after line ~239), following the same comment style: `ConditionBudgetBlocked = "BudgetBlocked"` (operator's budget cap reached; new dispatch halted project-wide until the cap is raised via Spec.Budget.AbsoluteCapCents or the existing bypass annotation is applied; set by the TaskReconciler dispatch gate — D-04, BUDGET-02), `ReasonBudgetCapReached = "BudgetCapReached"`, and `ReasonBudgetCapCleared = "BudgetCapCleared"` (stamped when the cap is raised and the condition flips to False — without this, a raised cap could never unblock dispatch). Block doc comment must state BudgetBlocked is DISTINCT from BillingHalt (operator cap vs provider billing auth) and that both may be True simultaneously. No new Status.Phase enum values (D-04: the BudgetExceeded phase machinery stays as-is).
  </action>
  <verify>
    <automated>go build ./api/... && grep -c 'ConditionBudgetBlocked = "BudgetBlocked"' api/v1alpha1/shared_types.go</automated>
  </verify>
  <acceptance_criteria>
    - shared_types.go contains ConditionBudgetBlocked, ReasonBudgetCapReached, ReasonBudgetCapCleared constants
    - go build ./api/... exits 0
    - `make verify-no-aggregates` still passes (no new status aggregate fields were added — this task adds constants only)
  </acceptance_criteria>
  <done>Constants compile; vocabulary mirrors the BillingHalt block.</done>
</task>

<task type="auto">
  <name>Task 2: ReservationStore with nil-safe methods + restart rederivation</name>
  <files>internal/budget/reservation.go, internal/budget/reservation_test.go</files>
  <read_first>internal/budget/bucket.go (Store sync.Map pattern), internal/budget/precharge.go (PreCharge label-scan rederivation pattern + secretUIDLabel const style), internal/budget/cap.go (IsCapExceeded semantics, rolling-window note), internal/budget/precharge_test.go and internal/budget/cap_test.go (test helper + table-driven patterns), .planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§reservation.go — full skeleton)</read_first>
  <behavior>
    - Reserve(uid, cents) ×3 → TotalReserved == sum; Settle/Release each delete by uid → TotalReserved drops
    - HasHeadroom(project, est): false when CostSpentCents + TotalReserved() + est > AbsoluteCapCents; true at exactly == cap is NOT allowed (dispatch blocks when spent+reserved+est >= cap, per D-05 "dispatch blocks when spent + reserved ≥ cap"); true when cap <= 0; true for nil project; true for nil *ReservationStore receiver
    - RederiveReservations: active Job (Status.Active > 0) with labels tideproject.k8s/estimated-cost=250 and tideproject.k8s/task-uid=X → store holds X→250; terminated Jobs skipped; Jobs missing the estimated-cost label skipped (pre-Phase-14, Pitfall 5); malformed/zero label values skipped
  </behavior>
  <action>
    Create internal/budget/reservation.go per the PATTERNS.md skeleton: const reservedCostLabel = "tideproject.k8s/estimated-cost"; exported ReservationStore{m sync.Map} keyed taskUID(string)→int64 cents with Reserve/Settle/Release/TotalReserved/HasHeadroom; RederiveReservations(ctx, c client.Client, store *ReservationStore) listing Jobs via client.HasLabels{reservedCostLabel} and parsing with strconv.ParseInt, reading the task UID from the existing "tideproject.k8s/task-uid" label. Two deviations from the skeleton, both deliberate: (1) every method on *ReservationStore must be NIL-RECEIVER-SAFE (`if s == nil` → Reserve/Settle/Release no-op, TotalReserved 0, HasHeadroom true) because existing TaskReconciler test constructors across the suite do not set the new field and must not panic; (2) HasHeadroom blocks at >= cap: return `committed+estimatedCents < cap` exactly as the skeleton, which already encodes the D-05 inequality. Doc comments must state the PERSIST-02 contract: reservations are in-process only, NEVER persisted in CRD status, rederived from in-flight Job labels on restart (same doctrine as the indegree map). Create reservation_test.go covering the behavior list using the cap_test.go table-driven style for pure-Go cases and the precharge_test.go fake-client helper style for rederivation cases, including a nil-receiver case.
  </action>
  <verify>
    <automated>go test ./internal/budget/... -run 'TestReservation|TestRederiveReservations' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - reservation.go contains `reservedCostLabel = "tideproject.k8s/estimated-cost"`
    - All ReservationStore methods begin with a nil-receiver guard (grep -c 'if s == nil' internal/budget/reservation.go >= 4)
    - go test ./internal/budget/... -run 'TestReservation|TestRederiveReservations' -count=1 exits 0
    - go test -race ./internal/budget/... -count=1 exits 0
    - `make verify-no-aggregates` passes (no Reserved field added to api types)
  </acceptance_criteria>
  <done>Store + rederivation green under -race; PERSIST-02 honored (in-process only).</done>
</task>

<task type="auto">
  <name>Task 3: budget_blocked.go — check + bidirectional set/clear helpers</name>
  <files>internal/controller/budget_blocked.go, internal/controller/budget_blocked_test.go</files>
  <read_first>internal/controller/billing_halt.go (checkBillingHalt lines 72-89, setBillingHaltIfNeeded structure — the exact mirror), internal/controller/billing_halt_test.go (fakeSchemeWithAll + WithStatusSubresource patterns), internal/budget/cap.go (IsCapExceeded), .planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§budget_blocked.go — full skeleton)</read_first>
  <behavior>
    - checkBudgetBlocked: true iff project carries Type=ConditionBudgetBlocked Status=True; false for nil project, absent condition, or Status=False
    - setBudgetBlockedIfNeeded with cap exceeded and condition absent/False → patches BudgetBlocked=True Reason=ReasonBudgetCapReached, Message includes spent cents, reserved cents, cap cents
    - setBudgetBlockedIfNeeded with cap exceeded and condition already True → no patch issued (idempotent)
    - setBudgetBlockedIfNeeded with cap NOT exceeded and condition currently True → patches BudgetBlocked=False Reason=ReasonBudgetCapCleared (cap-raise recovery)
    - setBudgetBlockedIfNeeded with cap NOT exceeded and condition absent → no-op; nil project → no-op nil error
  </behavior>
  <action>
    Create internal/controller/budget_blocked.go in package controller per the PATTERNS.md skeleton: `checkBudgetBlocked(project *tideprojectv1alpha1.Project) bool` and `setBudgetBlockedIfNeeded(ctx context.Context, c client.Client, project *tideprojectv1alpha1.Project, reservedCents int64) error`. ONE deliberate extension beyond the skeleton (which is set-only): the function is BIDIRECTIONAL — when !budget.IsCapExceeded(project) and the existing condition is True, patch it to False with Reason=ReasonBudgetCapCleared. Without the clear path, an operator raising Spec.Budget.AbsoluteCapCents could never recover dispatch, because every dispatch gate (Plans 14-03/14-05) parks on the stale True condition with a 30s requeue. The bypass annotation does NOT clear the condition (the condition reflects cap reality; bypass is checked separately at each gate via budget.IsBypassed). Use meta.SetStatusCondition + client.MergeFrom status patch (the shared condition-stamp pattern), Reason constants from Task 1, message format "Cost spent %d cents (+ %d reserved) exceeds cap %d cents; dispatch halted project-wide". File header comment: fourth dispatch-entry hold after CheckRejected, checkParentApproval, checkBillingHalt; NOT mutually exclusive with BillingHalt (Pitfall 4). Create budget_blocked_test.go covering the behavior list with the billing_halt_test.go fake-client pattern (these are plain go tests with the fake client — no envtest needed).
  </action>
  <verify>
    <automated>go test ./internal/controller/ -run 'TestCheckBudgetBlocked|TestSetBudgetBlocked' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - budget_blocked.go contains both checkBudgetBlocked and setBudgetBlockedIfNeeded
    - setBudgetBlockedIfNeeded contains a clear branch setting Status=metav1.ConditionFalse with ReasonBudgetCapCleared
    - go test ./internal/controller/ -run 'TestCheckBudgetBlocked|TestSetBudgetBlocked' -count=1 exits 0
    - go vet ./internal/controller/... exits 0
  </acceptance_criteria>
  <done>Helpers compile and pass; set, idempotent re-set, and cap-raise clear all covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job labels → ReservationStore | Rederivation trusts labels stamped by the manager on Jobs it owns |
| Project.Status.Conditions | Condition writable by anyone with status RBAC; gates trust it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | estimated-cost Job label edited to shrink rederived reservations | accept | Same trust domain as the existing provider-secret-uid PreCharge label; Job-edit RBAC already implies dispatch control |
| T-14-05 | Denial of Service | forged BudgetBlocked=True condition halts dispatch | accept | Status-subresource RBAC on Project already gates this; identical exposure to existing BillingHalt; recovery via cap raise/bypass is operator-visible |
| T-14-06 | Tampering | check-then-act race between TotalReserved read and Reserve | mitigate (bounded) | Accept overshoot bounded to one session's estimate per concurrent reconcile, documented as by-design in reservation.go doc comment (RESEARCH §Security Domain) |
| T-14-SC | Tampering | package installs | accept | No new dependencies; RESEARCH §Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `go test ./internal/budget/... ./internal/controller/ -run 'TestReservation|TestRederive|TestCheckBudgetBlocked|TestSetBudgetBlocked' -count=1` exits 0
- `go test -race ./internal/budget/... -count=1` exits 0
- `make verify-no-aggregates` exits 0
- `make lint` passes on touched packages
</verification>

<success_criteria>
Condition vocabulary, gate helpers (with cap-raise clear path), and rederivable reservation store exist with green unit coverage — everything Plan 14-03 needs to wire the dispatch gate.
</success_criteria>

<output>
Create `.planning/phases/14-budget-enforcement-pricing/14-02-SUMMARY.md` when done.
</output>
