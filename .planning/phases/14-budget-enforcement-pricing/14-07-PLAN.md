---
phase: 14-budget-enforcement-pricing
plan: 07
type: execute
wave: 4
depends_on: []
files_modified:
  - dashboard/web/src/components/ConditionBadge.tsx
  - dashboard/web/src/components/ConditionBadge.test.tsx
  - dashboard/web/src/components/TideNodeShell.tsx
  - dashboard/web/src/components/ProjectNode.tsx
  - dashboard/web/src/components/PlanningDAGView.tsx
  - dashboard/web/src/lib/api.ts
  - dashboard/web/src/components/__tests__/nodes.test.tsx
  - dashboard/web/src/components/__tests__/dag-views.test.tsx
autonomous: true
gap_closure: true
requirements: [BUDGET-02]

must_haves:
  truths:
    - "A Project with a True BudgetBlocked condition shows a purple 'Budget blocked' pill (Wallet icon, message as native tooltip) on the dashboard project node"
    - "A True BillingHalt condition rides the same mechanism as a 'Billing halted' pill (CreditCard icon) — no BudgetBlocked-only special case"
    - "A blocked project node carries a purple border-l-4 readable at DAG-zoom scale; failed-family destructive red takes precedence when both apply"
    - "Zero blocking conditions renders the node byte-identical to today — existing node tests pass unmodified"
    - "The StatusValue enum gains no new members (D-04 'no new chip states')"
  artifacts:
    - path: "dashboard/web/src/components/ConditionBadge.tsx"
      provides: "ConditionBadge primitive + ProjectBlockingCondition type + CONDITION_TABLE export"
      exports: ["ProjectBlockingCondition", "CONDITION_TABLE"]
      min_lines: 60
    - path: "dashboard/web/src/components/ConditionBadge.test.tsx"
      provides: "vocabulary-table render tests + unknown-type-renders-nothing"
    - path: "dashboard/web/src/components/TideNodeShell.tsx"
      provides: "blockingConditions slot + purple border + data-blocked + aria extension"
      contains: "blockingConditions"
    - path: "dashboard/web/src/components/ProjectNode.tsx"
      provides: "ProjectNodeData.blockingConditions passthrough"
      contains: "blockingConditions"
    - path: "dashboard/web/src/components/PlanningDAGView.tsx"
      provides: "buildPlanningGraph maps detail.blockingConditions ?? [] into projectData"
      contains: "blockingConditions"
    - path: "dashboard/web/src/lib/api.ts"
      provides: "ProjectSummary.blockingConditions wire-type mirror"
      contains: "blockingConditions"
  key_links:
    - from: "dashboard/web/src/components/PlanningDAGView.tsx"
      to: "dashboard/web/src/components/ProjectNode.tsx"
      via: "projectData.blockingConditions = detail.blockingConditions ?? []"
      pattern: "blockingConditions.*\\?\\?"
    - from: "dashboard/web/src/components/TideNodeShell.tsx"
      to: "dashboard/web/src/components/ConditionBadge.tsx"
      via: "one ConditionBadge per blocking condition in the summary row"
      pattern: "ConditionBadge"
    - from: "dashboard/web/src/lib/api.ts"
      to: "cmd/dashboard/api/projects.go"
      via: "blockingConditions JSON field name (wire contract; plan 14-06 owns the Go side)"
      pattern: "blockingConditions"
---

<objective>
Close the frontend half of the 14-VERIFICATION.md gap (BUDGET-02, success criterion 2 dashboard half): a `BudgetBlocked` condition on the Project CR becomes visible on the dashboard project node, via the generalized blocking-conditions mechanism that 14-UI-SPEC.md (approved 2026-06-12, LOCKED) specifies — Phase 13's `BillingHalt` rides the same badge for free.

Purpose: today `ProjectNode` renders only a `StatusValue` chip; a tripped budget cap is invisible on the dashboard. Per locked decision D-04 ("no new chip states"), blocked state is a separate condition badge, not a phase chip — the `StatusValue` enum is untouched.

Output: new `ConditionBadge` primitive, `TideNodeShell` blocking-conditions slot with purple border-left, `ProjectNodeData`/`buildPlanningGraph`/`api.ts` wiring, and Vitest coverage at all three layers. Live updates need NO new wiring — `PlanningDAGView` already refetches `ProjectDetail` on `kind=Project` SSE events (the publish precondition is regression-locked by plan 14-06 Task 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-budget-enforcement-pricing/14-UI-SPEC.md
@dashboard/web/src/components/StatusBadge.tsx
@dashboard/web/src/components/TideNodeShell.tsx
@dashboard/web/src/components/ProjectNode.tsx

<interfaces>
<!-- Extracted from the codebase 2026-06-12. Use directly — no exploration needed. -->

From dashboard/web/src/components/StatusBadge.tsx (the anatomy ConditionBadge mirrors verbatim):
```tsx
// Pill: <span data-testid data-status role="status" aria-label
//   className={clsx("inline-flex items-center gap-1 p-1 px-2", className)}
//   style={{ color: row.colorVar,
//            background: `color-mix(in srgb, ${row.colorVar} 15%, transparent)`,
//            border: `1px solid color-mix(in srgb, ${row.colorVar} 40%, transparent)`,
//            borderRadius: "4px", fontFamily: "var(--font-mono)",
//            fontSize: "12px", fontWeight: 600, lineHeight: 1.4 }}>
//   <span data-icon={row.iconName}><Icon size={14} aria-hidden="true" /></span>
//   <span>{row.label}</span></span>
export { STATUS_TABLE };  // table re-export pattern CONDITION_TABLE mirrors
```

From dashboard/web/src/components/TideNodeShell.tsx (extension points):
```tsx
const FAILED_STATUSES = new Set<string>(["Failed","PushLeaseFailed","PushLeakBlocked","Rejected"]);
// isFailed border (the pattern the purple sibling mirrors):
//   isFailed && "border-l-4 border-l-[var(--color-destructive)]"
// data attributes on the container: data-testid={`tide-node-${kind}`}, data-kind,
//   data-selected, data-failed, data-clickable
// aria-label today: `${KIND_LABEL[kind]} ${name}, status ${status}`
// Summary row (badge insertion point):
//   <div className="flex items-center gap-2 px-3 py-2 ...">
//     <span className="shrink-0"><StatusBadge status={status} /></span>
//     <span className="min-w-0 truncate">{summary}</span>
//   </div>
```

From dashboard/web/src/components/ProjectNode.tsx:
```tsx
export type ProjectNodeData = {
  name: string; status: StatusValue;
  milestonesCount: number; phasesCount: number; plansCount: number;
} & Record<string, unknown>;
// renders <TideNodeShell kind="project" ... width={360} minHeight={92} clickable={false} />
```

From dashboard/web/src/components/PlanningDAGView.tsx (buildPlanningGraph, lines 94-116):
```tsx
const projectData: ProjectNodeData = {
  name: detail.name, status: coerce(detail.phase),
  milestonesCount: detail.milestones.length,
  phasesCount: detail.phases.length, plansCount: detail.plans.length,
};
// SSE: PLANNING_KINDS = new Set(["Project","Milestone","Phase","Plan"]) →
// debounced runFetch() refetches ProjectDetail. NO changes needed here beyond
// the projectData field. Component accepts an `initialData` prop tests use.
```

From dashboard/web/src/lib/api.ts (wire types — mirror Go structs verbatim):
```tsx
export type ProjectSummary = {
  name: string; namespace: string; phase: string;
  activeMilestoneCount: number; budget: BudgetSummary;
};
export type ProjectDetail = ProjectSummary & { milestones: ChildRef[]; phases: ChildRef[]; plans: ChildRef[]; };
```

Wire shape from plan 14-06 (Go side; field always present, but frontend treats as optional):
```json
"blockingConditions": [{"type":"BudgetBlocked","reason":"BudgetCapReached","message":"...","age":"4m 12s"}]
```

Test commands (dashboard/web/package.json): `npm test` = `vitest run`; `npm run lint` = `tsc -b`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConditionBadge primitive — the condition-vocabulary sibling of StatusBadge</name>
  <files>dashboard/web/src/components/ConditionBadge.tsx, dashboard/web/src/components/ConditionBadge.test.tsx</files>
  <read_first>
    - dashboard/web/src/components/StatusBadge.tsx (the anatomy to mirror verbatim: pill construction, STATUS_TABLE shape, doc-comment style, data-testid pattern)
    - dashboard/web/src/components/StatusBadge.test.tsx (the test file to mirror: per-variant assertions, vitest/@testing-library conventions)
    - .planning/phases/14-budget-enforcement-pricing/14-UI-SPEC.md section "C1" (the LOCKED vocabulary table — single source for icons/labels/tooltips/SR descriptions)
  </read_first>
  <behavior>
    - Test 1: type "BudgetBlocked" renders a pill with data-testid "condition-badge-BudgetBlocked", data-condition "BudgetBlocked", a Wallet icon (data-icon="Wallet", size 14), label text "Budget blocked", role="status"
    - Test 2: type "BillingHalt" renders data-testid "condition-badge-BillingHalt", CreditCard icon (data-icon="CreditCard"), label "Billing halted"
    - Test 3: the native tooltip equals the condition's message verbatim — `title` attribute is exactly the message prop value (e.g. "Cost spent 10100 cents (+ 220 reserved) exceeds cap 10000 cents; dispatch halted project-wide")
    - Test 4: aria-label carries the SR description from the vocabulary table (BudgetBlocked: "Budget cap reached — dispatch halted. Raise spec.budget.absoluteCapCents or apply the bypass annotation to resume"; BillingHalt: "Provider credit balance too low — dispatch halted. Refill credits and run `tide resume`")
    - Test 5: an unknown condition type (e.g. "Ready") renders nothing — container query for any condition-badge testid returns null
    - Test 6: CONDITION_TABLE is exported and iterable with exactly the keys BudgetBlocked and BillingHalt (mirrors STATUS_TABLE re-export; tests iterate it)
  </behavior>
  <action>
    Create dashboard/web/src/components/ConditionBadge.tsx per 14-UI-SPEC.md C1 (LOCKED). Contents:

    - Export `type ProjectBlockingCondition = { type: string; reason: string; message: string; age: string }` — the wire mirror of plan 14-06's `projectCondition` JSON shape (open string type: unknown types must render nothing, defensive against vocabulary drift, mirroring `coerce()`'s philosophy).
    - A `ConditionRow` shape and `CONDITION_TABLE: Record<string, ConditionRow>` with exactly two entries, all values verbatim from the UI-SPEC C1 vocabulary table: BudgetBlocked → icon `Wallet`, iconName "Wallet", label "Budget blocked", colorVar "var(--color-status-blocked)", srDescription "Budget cap reached — dispatch halted. Raise spec.budget.absoluteCapCents or apply the bypass annotation to resume"; BillingHalt → icon `CreditCard`, iconName "CreditCard", label "Billing halted", same colorVar, srDescription "Provider credit balance too low — dispatch halted. Refill credits and run `tide resume`". No animation on either. Export CONDITION_TABLE (mirror STATUS_TABLE's re-export comment).
    - Default export `ConditionBadge({ condition, className })` with props type `{ condition: ProjectBlockingCondition; className?: string }`. Lookup `CONDITION_TABLE[condition.type]`; on miss return null. On hit render the EXACT StatusBadge pill formula (do not diverge — see interfaces block): inline-flex items-center gap-1 p-1 px-2 via clsx; inline style with color = colorVar, background color-mix 15%, border 1px solid color-mix 40%, borderRadius 4px, font-mono 12px / 600 / lineHeight 1.4; 14px icon wrapped in a span with data-icon. Differences from StatusBadge: attributes are `data-testid={`condition-badge-${condition.type}`}`, `data-condition={condition.type}`, `role="status"`, `aria-label` = the row's srDescription, and `title={condition.message}` (native tooltip, controller message verbatim — never paraphrase).
    - Imports: `Wallet`, `CreditCard`, `type LucideIcon` from "lucide-react" (already-pinned dep — no new packages); `clsx` from "../lib/clsx"; `type CSSProperties` from "react". Match StatusBadge.tsx's doc-comment style including the UI-SPEC § reference.

    Write ConditionBadge.test.tsx first (RED) mirroring StatusBadge.test.tsx structure, then implement (GREEN).
  </action>
  <verify>
    <automated>cd dashboard/web && npx vitest run src/components/ConditionBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export type ProjectBlockingCondition' dashboard/web/src/components/ConditionBadge.tsx` returns 1
    - `grep -c 'CONDITION_TABLE' dashboard/web/src/components/ConditionBadge.tsx` returns ≥ 2 (definition + export)
    - `grep -c 'Wallet\|CreditCard' dashboard/web/src/components/ConditionBadge.tsx` returns ≥ 2; no other new lucide icons imported
    - `grep -c 'return null' dashboard/web/src/components/ConditionBadge.tsx` returns ≥ 1 (unknown-type guard)
    - Labels are exactly "Budget blocked" and "Billing halted"; color token is exactly var(--color-status-blocked) for both rows (grep confirms; no new color values anywhere in the diff)
    - StatusBadge.tsx is NOT modified — `git diff --name-only` does not list it (StatusValue enum untouched per D-04)
    - `cd dashboard/web && npx vitest run src/components/ConditionBadge.test.tsx` exits 0 with all 6 behavior tests passing
  </acceptance_criteria>
  <done>ConditionBadge renders the two-entry locked vocabulary with StatusBadge-identical anatomy, message-verbatim tooltips, SR descriptions, and a null render for unknown types; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TideNodeShell blocking-conditions slot — badges in the summary row + purple border at DAG zoom</name>
  <files>dashboard/web/src/components/TideNodeShell.tsx, dashboard/web/src/components/__tests__/nodes.test.tsx</files>
  <read_first>
    - dashboard/web/src/components/TideNodeShell.tsx (the file being extended — container clsx, FAILED_STATUSES, data attributes, aria-label, summary row markup)
    - dashboard/web/src/components/__tests__/nodes.test.tsx (existing shell coverage: Test 3 "failed border" is the analog for the new blocked-border specs; React Flow store wrapper noted at the top of the file)
    - .planning/phases/14-budget-enforcement-pricing/14-UI-SPEC.md section "C2" (LOCKED placement/border/aria contract)
  </read_first>
  <behavior>
    - Test 1: shell with blockingConditions=[BudgetBlocked True entry] renders condition-badge-BudgetBlocked in the summary row immediately after the StatusBadge, and the container has data-blocked="true" plus the classes border-l-4 and border-l-[var(--color-status-blocked)]
    - Test 2: destructive precedence — status "Failed" AND one blocking condition → container has border-l-[var(--color-destructive)], does NOT have border-l-[var(--color-status-blocked)], and still has data-blocked="true"
    - Test 3: zero conditions (prop omitted) → data-blocked="false", no border-l-4 unless failed, no condition badge — and ALL pre-existing nodes.test.tsx specs pass unmodified
    - Test 4: aria-label extends to "project alpha, status Running, blocked: Budget blocked" when blocked (labels from CONDITION_TABLE, comma-joined for two conditions); unchanged format when not blocked
    - Test 5: composability — both BudgetBlocked and BillingHalt entries render two badges in order
  </behavior>
  <action>
    Extend TideNodeShell.tsx per 14-UI-SPEC.md C2 (LOCKED):

    - Add optional prop `blockingConditions?: ProjectBlockingCondition[]` to TideNodeShellProps, defaulting to `[]` in the destructure. Import `ConditionBadge, { CONDITION_TABLE, type ProjectBlockingCondition }` from "./ConditionBadge".
    - Compute `isBlocked = blockingConditions.length > 0`.
    - Container classes: keep the existing failed line untouched; add a sibling line `!isFailed && isBlocked && "border-l-4 border-l-[var(--color-status-blocked)]"` — destructive red wins when both apply.
    - Container attributes: add `data-blocked={isBlocked ? "true" : "false"}` alongside data-failed.
    - aria-label: when isBlocked, append `, blocked: <labels>` where labels = blockingConditions mapped through CONDITION_TABLE to their `label` values (skip unknown types), joined with ", ". Not blocked → exactly today's string.
    - Summary row: immediately after the existing StatusBadge span and before the summary-text span, render one `<ConditionBadge condition={c} />` per entry, each wrapped in a `shrink-0` span keyed by `c.type` (the row's existing gap-2 provides the spec's badge separation). Summary text keeps `min-w-0 truncate` and absorbs the squeeze. Unknown types contribute no DOM (ConditionBadge returns null).
    - Match the file's comment conventions (UI-SPEC § references; note the precedence rule and the DAG-zoom rationale for the border).

    Add the 5 behavior specs to __tests__/nodes.test.tsx following its existing describe/Test-N naming and the React Flow store wrapper. Do NOT modify any existing spec — Test 3's regression claim depends on it.
  </action>
  <verify>
    <automated>cd dashboard/web && npx vitest run src/components/__tests__/nodes.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'blockingConditions' dashboard/web/src/components/TideNodeShell.tsx` returns ≥ 3 (prop type, destructure, render)
    - `grep -c 'data-blocked' dashboard/web/src/components/TideNodeShell.tsx` returns 1
    - `grep -c 'border-l-\[var(--color-status-blocked)\]' dashboard/web/src/components/TideNodeShell.tsx` returns 1, and that clsx line contains a `!isFailed`-style guard (destructive precedence in source)
    - The StatusValue type and FAILED_STATUSES set are unmodified (no new members — D-04)
    - `cd dashboard/web && npx vitest run src/components/__tests__/nodes.test.tsx` exits 0 — new specs pass AND every pre-existing spec passes without edits (git diff of the test file shows only additions)
  </acceptance_criteria>
  <done>TideNodeShell carries the optional blocking-conditions slot with locked placement, purple border with destructive precedence, data-blocked, and aria extension; zero-condition render is provably unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the payload — api.ts type, ProjectNodeData passthrough, buildPlanningGraph mapping</name>
  <files>dashboard/web/src/lib/api.ts, dashboard/web/src/components/ProjectNode.tsx, dashboard/web/src/components/PlanningDAGView.tsx, dashboard/web/src/components/__tests__/dag-views.test.tsx</files>
  <read_first>
    - dashboard/web/src/lib/api.ts (wire-type mirror doctrine in the header comment; ProjectSummary/ProjectDetail being extended)
    - dashboard/web/src/components/ProjectNode.tsx (ProjectNodeData + TideNodeShell call being extended)
    - dashboard/web/src/components/PlanningDAGView.tsx lines 94-116 (buildPlanningGraph projectData literal) and the PROJECT_PAYLOAD fixture + SSE-refetch specs in __tests__/dag-views.test.tsx
    - .planning/phases/14-budget-enforcement-pricing/14-UI-SPEC.md sections "C3" and "C4" final paragraph (LOCKED wiring + the no-new-SSE-wiring note)
  </read_first>
  <behavior>
    - Test 1 (dag-views.test.tsx): rendering PlanningDAGView with a ProjectDetail payload whose blockingConditions holds one True-shaped BudgetBlocked entry ({type:"BudgetBlocked", reason:"BudgetCapReached", message:"cap reached", age:"4m 12s"}) shows condition-badge-BudgetBlocked inside tide-node-project, and that node has data-blocked="true"
    - Test 2 (dag-views.test.tsx): a payload without the blockingConditions field (legacy shape) renders the project node with data-blocked="false" and no condition badge — the `?? []` default holds
  </behavior>
  <action>
    Per 14-UI-SPEC.md C3/C4 (LOCKED), three one-field extensions plus tests:

    1. dashboard/web/src/lib/api.ts — add `blockingConditions?: ProjectBlockingCondition[]` to `ProjectSummary` (the Go field lives on projectSummary, so the mirror does too; ProjectDetail inherits it). Type-only import: `import type { ProjectBlockingCondition } from "../components/ConditionBadge"`. Optional with a comment noting the backend always sends `[]`-not-null but the frontend degrades gracefully when the field is absent (UI-SPEC error-state contract). Update the "Mirrors cmd/dashboard/api/projects.go::projectSummary" doc comment to mention the new field.

    2. dashboard/web/src/components/ProjectNode.tsx — `ProjectNodeData` gains `blockingConditions: ProjectBlockingCondition[]` (required here: buildPlanningGraph always supplies it); pass `blockingConditions={data.blockingConditions}` through to TideNodeShell. No width/minHeight change — 360×92 stays; the badge lives in the existing summary row (UI-SPEC C3).

    3. dashboard/web/src/components/PlanningDAGView.tsx — in `buildPlanningGraph`, add `blockingConditions: detail.blockingConditions ?? []` to the `projectData` literal. NOTHING else changes in this file: PLANNING_KINDS already includes "Project" and the debounced refetch already delivers live updates (no new SSE wiring — UI-SPEC C4; the publish precondition is regression-locked by plan 14-06).

    4. dashboard/web/src/components/__tests__/dag-views.test.tsx — add the two behavior specs. Reuse the existing PROJECT_PAYLOAD fixture pattern: spread it into a blocked variant with the blockingConditions entry; drive the component the same way the existing PlanningDAGView specs do (fetch mock or the `initialData` prop — match whichever the surrounding specs use).

    Finish with the full frontend gate: all suites + `tsc -b` (catches any mirror-type drift across api.ts/ProjectNode/PlanningDAGView).
  </action>
  <verify>
    <automated>cd dashboard/web && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'blockingConditions?: ProjectBlockingCondition\[\]' dashboard/web/src/lib/api.ts` returns 1
    - `grep -c 'blockingConditions: ProjectBlockingCondition\[\]' dashboard/web/src/components/ProjectNode.tsx` returns 1 and the TideNodeShell call passes the prop
    - `grep -Ec 'blockingConditions: detail\.blockingConditions \?\? \[\]' dashboard/web/src/components/PlanningDAGView.tsx` returns 1
    - PLANNING_KINDS, REFETCH_DEBOUNCE_MS, and the SSE onMessage handler in PlanningDAGView.tsx are byte-unchanged (git diff shows only the projectData line)
    - `cd dashboard/web && npm test` exits 0 (all suites: ConditionBadge, nodes, dag-views, and every pre-existing suite)
    - `cd dashboard/web && npm run lint` (tsc -b) exits 0
  </acceptance_criteria>
  <done>A blockingConditions entry in the ProjectDetail payload reaches the project node's badge and border through typed wiring; legacy payloads degrade to today's render; full frontend test + typecheck gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard REST API → React render | Controller-stamped condition `message` strings enter the DOM (tooltip) |
| SSE stream → refetch trigger | Untrusted event payloads parsed client-side (existing surface, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | Tampering (XSS via condition message) | ConditionBadge `title` tooltip | mitigate | message rendered ONLY via JSX attribute binding (`title={condition.message}`) — React escapes attributes; no dangerouslySetInnerHTML anywhere in the diff; backend json.Encoder HTML-escape is the second layer |
| T-14-07-02 | Spoofing (vocabulary drift / unexpected types) | ConditionBadge type lookup | mitigate | client-side whitelist: unknown `type` values render nothing (CONDITION_TABLE miss → null), mirroring `coerce()`'s defensive posture |
| T-14-07-03 | Denial of Service (render storm on condition flap) | PlanningDAGView SSE refetch | accept | existing 250ms debounce already collapses event bursts; this plan adds zero new SSE subscriptions or fetch paths |
| T-14-SC | Tampering (supply chain) | npm installs | accept | zero new packages — `Wallet`/`CreditCard` come from the already-pinned lucide-react dependency (~2KB tree-shaken); package.json unmodified |
</threat_model>

<verification>
- `cd dashboard/web && npm test` green — ConditionBadge.test.tsx, nodes.test.tsx (pre-existing specs unmodified), dag-views.test.tsx, plus all untouched suites
- `cd dashboard/web && npm run lint` (tsc -b) clean
- `git diff --name-only` confirms StatusBadge.tsx and package.json untouched (D-04 / no-new-packages)
- End-to-end (live cluster: cap trips → badge appears without manual refresh) is covered by 14-VERIFICATION.md's existing human_verification item at phase re-verification; the SSE publish precondition is regression-locked in plan 14-06
</verification>

<success_criteria>
- BUDGET-02 dashboard half, frontend portion: a True BudgetBlocked condition in the project payload renders a purple "Budget blocked" badge on the dashboard project node with the controller message as tooltip, plus a purple border-l-4 readable at DAG zoom (destructive red precedence preserved)
- BillingHalt rides the identical mechanism — no special-casing
- StatusValue enum unchanged; zero-condition render provably identical to today; zero new dependencies
</success_criteria>

<output>
Create `.planning/phases/14-budget-enforcement-pricing/14-07-SUMMARY.md` when done
</output>
