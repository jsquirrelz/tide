---
phase: 14-budget-enforcement-pricing
plan: 04
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - hack/check-pricing-drift.sh
  - .github/workflows/pricing-drift.yaml
  - docs/releasing.md
autonomous: true
requirements: [BUDGET-01]
tags: [pricing, ci, github-actions, shell]
must_haves:
  truths:
    - "hack/check-pricing-drift.sh runs locally, fetches the published pricing page, and exits non-zero with a quoted model/table-value/live-value diff on drift"
    - "A weekly GitHub Action runs the script and opens/updates exactly one deduped, labeled issue on drift — no auto-PR"
    - "A release checklist line mandates verifying the pricing table against the live page before tagging"
  artifacts:
    - path: "hack/check-pricing-drift.sh"
      provides: "Fetch+parse+diff of compiled priceTable vs live pricing docs"
      min_lines: 60
    - path: ".github/workflows/pricing-drift.yaml"
      provides: "Weekly scheduled drift check with deduped issue management"
      contains: "workflow_dispatch"
    - path: "docs/releasing.md"
      provides: "Release checklist including the D-03(b) pricing verification line"
      contains: "check-pricing-drift"
  key_links:
    - from: ".github/workflows/pricing-drift.yaml"
      to: "hack/check-pricing-drift.sh"
      via: "workflow step executes the script"
      pattern: "check-pricing-drift\\.sh"
    - from: "hack/check-pricing-drift.sh"
      to: "internal/subagent/anthropic/pricing.go"
      via: "greps the compiled priceTable entries"
      pattern: "pricing\\.go"
---

<objective>
Implement the D-03 pricing-drift process: a locally-runnable fetch-and-diff script in hack/, a weekly GitHub Action that opens/updates a single deduped labeled issue when the compiled table drifts from the published pricing page, and the release-checklist line. Depends on Plan 14-01 only so the script diffs against the CORRECTED table (running it against the old wrong opus-4-7 entry would immediately report drift that 14-01 already fixes).

Purpose: D-01's table was wrong precisely because price corrections relied on memory ("the comment's 'Anthropic pricing page 2026-06' citation was wrong"). D-03 makes drift detection mechanical and keeps a human in the loop for billing-math changes.

Output: hack/check-pricing-drift.sh, .github/workflows/pricing-drift.yaml, docs/releasing.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-budget-enforcement-pricing/14-CONTEXT.md (D-03 + §Specific Ideas: "the drift-check issue should quote the diff (model, table value, live value) so the human fix is mechanical")
@.planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§check-pricing-drift.sh, §pricing-drift.yaml)
@internal/subagent/anthropic/pricing.go (post-14-01 table — the parse target)
</context>

<tasks>

<task type="auto">
  <name>Task 1: hack/check-pricing-drift.sh — fetch, parse, diff, report</name>
  <files>hack/check-pricing-drift.sh</files>
  <read_first>internal/subagent/anthropic/pricing.go (post-14-01: exact modelPrice field names and table formatting the parser must match), hack/scripts/verify-license.sh (script header/style conventions), .github/workflows/nightly-integration.yml (curl retry flags pattern)</read_first>
  <behavior>
    - Exit 0 + "no pricing drift detected" when compiled table matches the live page for every model ID present in both
    - Exit 1 on drift, printing one line per drifted entry: model ID, dimension (input/output), table value (cents/MTok), live value (cents/MTok)
    - Exit 2 (distinct from drift) with an error message when the fetch fails — the workflow must not file a drift issue for a network failure
    - Models on the live page but absent from the table are reported as "missing from table" drift; table models absent from the page are reported as informational only (page may rename sections)
  </behavior>
  <action>
    Create hack/check-pricing-drift.sh (chmod +x), bash with `set -euo pipefail`, usage comment "D-03: run weekly via .github/workflows/pricing-drift.yaml or locally: ./hack/check-pricing-drift.sh". Steps: (1) fetch https://platform.claude.com/docs/en/pricing.md (the EXACT D-03 URL — note PATTERNS.md sketched platform.anthropic.com; the CONTEXT.md locked URL wins) with `curl -fsSL --retry 5 --retry-delay 3 --retry-all-errors --connect-timeout 30` to a temp file, exit 2 on failure; (2) extract per-model dollar prices from the fetched markdown for the model IDs known to the compiled table (parse with grep/awk/sed only — POSIX tools, no jq/python dependency; dollars→cents/MTok via integer arithmetic); (3) extract compiled values by grepping internal/subagent/anthropic/pricing.go for each `"claude-..."` key block's inputCentsPerMTok/outputCentsPerMTok values (resolve the repo root relative to the script via `cd "$(dirname "$0")/.."` so it runs from anywhere); (4) compare and emit the model/table-value/live-value lines per the behavior contract — this exact text becomes the issue body, so the human fix must be mechanical (CONTEXT §Specific Ideas). Page-format drift (model heading found but price unparseable) must be reported as "unparseable — page format may have changed", exit 1, never a silent pass. Do NOT auto-edit pricing.go (D-03: no auto-PR; a human reviews billing-math changes).
  </action>
  <verify>
    <automated>bash -n hack/check-pricing-drift.sh && test -x hack/check-pricing-drift.sh && shellcheck hack/check-pricing-drift.sh || bash -n hack/check-pricing-drift.sh</automated>
  </verify>
  <acceptance_criteria>
    - Script is executable, passes `bash -n`, and contains the URL "platform.claude.com/docs/en/pricing.md"
    - Script contains the three distinct exit paths (0 no-drift, 1 drift, 2 fetch-failure) — grep for 'exit 2' near the curl block
    - Running `./hack/check-pricing-drift.sh` locally exits 0, 1, or 2 with the corresponding message (executor runs it once and records which; exit 0 expected against the 14-01 table, but live-page drift since 2026-06-11 is acceptable as long as the diff output format matches the behavior contract)
  </acceptance_criteria>
  <done>Locally-runnable drift detector with mechanical diff output and fetch-failure discrimination.</done>
</task>

<task type="auto">
  <name>Task 2: Weekly workflow with deduped issue + release checklist</name>
  <files>.github/workflows/pricing-drift.yaml, docs/releasing.md</files>
  <read_first>.github/workflows/nightly-integration.yml (schedule/checkout/permissions conventions), hack/check-pricing-drift.sh (exit-code contract from Task 1), docs/README.md (docs index style, to link releasing.md if an index list exists)</read_first>
  <action>
    (a) Create .github/workflows/pricing-drift.yaml per the PATTERNS.md skeleton: name pricing-drift; on schedule cron '0 9 * * 1' (Monday 09:00 UTC, weekly per D-03) plus workflow_dispatch; top-level `permissions: {}` with job-level `contents: read` + `issues: write`; actions/checkout@v4 with persist-credentials: false; a step running ./hack/check-pricing-drift.sh capturing stdout to a file and the exit code (continue-on-error or `|| echo "code=$?"` into GITHUB_OUTPUT). Only when the exit code is exactly 1 (drift — NOT 2/fetch-failure), run an actions/github-script@v7 step that: searches open issues with label `pricing-drift`; if one exists, updates its body; else creates one titled "Pricing table drift detected" with label `pricing-drift` and the script's diff output as the body. Script-injection hardening (T-14-09): pass the diff text into github-script via `process.env` from an `env:` mapping of the step output — never interpolate `${{ steps.*.outputs.* }}` directly inside the JS source string, because the body contains text fetched from an external page. No auto-PR step of any kind.
    (b) Create docs/releasing.md (no release checklist exists in the repo — verified by grep): a short "Release checklist" doc whose list includes the D-03(b) line: "Verify the pricing table against the live pricing page: run ./hack/check-pricing-drift.sh and resolve any drift in internal/subagent/anthropic/pricing.go before tagging", plus the existing-known release steps only as far as they are documented elsewhere (link docs/production.md / INSTALL.md rather than inventing process). Keep it minimal — this doc exists to carry the checklist line, not to re-spec the release process.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/pricing-drift.yaml'))" && grep -c "check-pricing-drift" docs/releasing.md</automated>
  </verify>
  <acceptance_criteria>
    - pricing-drift.yaml parses as YAML, contains cron '0 9 * * 1', workflow_dispatch, and job permissions issues: write
    - The github-script step receives the diff via env (grep the file for 'process.env'), and is gated on the drift exit code, not on any failure
    - The workflow contains no PR-creation action (grep -ci 'pull' .github/workflows/pricing-drift.yaml returns 0 for create-pull-request actions)
    - docs/releasing.md exists and contains "check-pricing-drift.sh"
  </acceptance_criteria>
  <done>Weekly deduped drift issue automation + release-checklist line, human-in-the-loop only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External pricing page → CI runner | Untrusted fetched markdown flows into issue bodies |
| Workflow → GitHub API | issues:write token scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | script injection via fetched page content interpolated into github-script JS | mitigate | Diff text passed via process.env, never `${{ }}` interpolation inside script source; issue body is data, never executed |
| T-14-10 | Elevation of Privilege | over-scoped workflow token | mitigate | Top-level `permissions: {}`; job grants only contents:read + issues:write; checkout persist-credentials: false |
| T-14-11 | Tampering | compromised page induces a wrong "drift" issue → human merges bad prices | accept (by design) | D-03 mandates no auto-PR; a human reviews every billing-math change against the issue's quoted diff |
| T-14-SC | Tampering | package installs | accept | No packages installed; pinned first-party actions (checkout@v4, github-script@v7) per repo convention |
</threat_model>

<verification>
- `bash -n hack/check-pricing-drift.sh` exits 0; script is executable
- pricing-drift.yaml is valid YAML with schedule + workflow_dispatch triggers
- `./hack/check-pricing-drift.sh` runs locally and terminates with a documented exit code
- docs/releasing.md carries the pricing verification line
</verification>

<success_criteria>
D-03 fully delivered: locally-runnable drift script with mechanical diff output, weekly deduped-issue automation with no auto-PR, and the release-checklist verification line.
</success_criteria>

<output>
Create `.planning/phases/14-budget-enforcement-pricing/14-04-SUMMARY.md` when done.
</output>
