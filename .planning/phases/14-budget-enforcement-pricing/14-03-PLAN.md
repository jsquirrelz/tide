---
phase: 14-budget-enforcement-pricing
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - internal/dispatch/podjob/jobspec.go
  - internal/dispatch/podjob/jobspec_test.go
  - internal/controller/task_controller.go
  - internal/controller/budget_blocked_regression_test.go
autonomous: true
requirements: [BUDGET-02, BUDGET-03]
tags: [budget, dispatch-gate, controller-runtime, envtest, go]
must_haves:
  truths:
    - "Cap trip stamps BudgetBlocked=True on Project.Status.Conditions within one task-reconcile cycle (run-1 regression: Project is no longer silent)"
    - "A task whose project carries BudgetBlocked=True parks with a 30s requeue — no Job created, no Failed status"
    - "Dispatch blocks when spent + reserved + estimate >= cap; each executor Job carries the tideproject.k8s/estimated-cost label"
    - "Task reaching terminal state removes its reservation; bypass annotation still allows dispatch while the condition is True"
  artifacts:
    - path: "internal/controller/task_controller.go"
      provides: "Hold #4 (BudgetBlocked) + reservation headroom gate + reserve/settle wiring"
      contains: "checkBudgetBlocked"
    - path: "internal/dispatch/podjob/jobspec.go"
      provides: "BuildOptions.EstimatedCostCents → tideproject.k8s/estimated-cost Job label"
      contains: "EstimatedCostCents"
    - path: "internal/controller/budget_blocked_regression_test.go"
      provides: "envtest run-1 regression (cap $100 trips → BudgetBlocked on Project)"
      min_lines: 100
  key_links:
    - from: "internal/controller/task_controller.go gateChecks"
      to: "internal/controller/budget_blocked.go"
      via: "setBudgetBlockedIfNeeded + checkBudgetBlocked at hold #4"
      pattern: "setBudgetBlockedIfNeeded"
    - from: "internal/controller/task_controller.go createDispatchJob"
      to: "internal/budget/reservation.go"
      via: "Reservations.Reserve at Job create; Settle in handleJobCompletion"
      pattern: "Reservations\\.(Reserve|Settle)"
    - from: "internal/controller/task_controller.go handleJobCompletion"
      to: "internal/controller/budget_blocked.go"
      via: "setBudgetBlockedIfNeeded immediately after budget.RollUpUsage (the moment spend is known)"
      pattern: "RollUpUsage"
---

<objective>
Wire BUDGET-02 and BUDGET-03 into the TaskReconciler: replace the fragile Phase=="BudgetExceeded" dispatch gate with the condition-based hold #4, stamp BudgetBlocked at the two points where cap breach is observable (after RollUpUsage, and at the dispatch gate), add the reservation pre-charge (reserve at Job create, settle at terminal state, estimated-cost label for restart rederivation), and prove the run-1 regression closed with an envtest.

Purpose: run 1's root cause is a watch-predicate gap — RollUpUsage's status patch never re-enqueues the ProjectReconciler (GenerationChangedPredicate ignores status patches), so handleBudgetGate never ran and Phase=BudgetExceeded was never set, so the task gate checking that phase never fired (~$40 overshoot, silent Project). Detection must live where spend is observed: the TaskReconciler (RESEARCH §Root Cause, Option A — locked).

Output: task_controller.go gate rewrite + reservation wiring, jobspec label, run-1 regression envtest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-budget-enforcement-pricing/14-CONTEXT.md
@.planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§task_controller.go, §budget_blocked_regression_test.go sections)
@.planning/phases/14-budget-enforcement-pricing/14-RESEARCH.md (§Root Cause Analysis, §Common Pitfalls 1/4/5)
@internal/controller/task_controller.go
@internal/controller/budget_blocked.go
@internal/budget/reservation.go
</context>

<interfaces>
From Plan 14-02 (read the actual files — these are the contracts):

```go
// internal/budget/reservation.go — all methods nil-receiver-safe
func (s *ReservationStore) Reserve(taskUID string, estimatedCents int64)
func (s *ReservationStore) Settle(taskUID string)   // delete on terminal success
func (s *ReservationStore) Release(taskUID string)  // delete on terminal failure / finalizer
func (s *ReservationStore) TotalReserved() int64
func (s *ReservationStore) HasHeadroom(project *tidev1alpha1.Project, estimatedCents int64) bool

// internal/controller/budget_blocked.go
func checkBudgetBlocked(project *tideprojectv1alpha1.Project) bool
func setBudgetBlockedIfNeeded(ctx context.Context, c client.Client, project *tideprojectv1alpha1.Project, reservedCents int64) error
```

New TaskReconcilerDeps fields (this plan adds them; main.go wiring is Plan 14-05 — until then they are zero-valued in production, which is safe because ReservationStore methods are nil-safe and a 0 estimate still blocks at spent >= cap):

```go
// Reservations is the in-process D-05 pre-charge store (nil-safe; wired in main.go).
Reservations *budget.ReservationStore
// ReserveEstimateCents is the flat per-dispatch reservation estimate (D-05 Option B;
// helm budget.reservePerDispatchCents via --budget-reserve-per-dispatch-cents; 0 = no pre-charge).
ReserveEstimateCents int64
// PricingOverridesJSON is the validated D-02 override JSON forwarded opaquely to Jobs.
PricingOverridesJSON string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: jobspec EstimatedCostCents field + estimated-cost Job label</name>
  <files>internal/dispatch/podjob/jobspec.go, internal/dispatch/podjob/jobspec_test.go</files>
  <read_first>internal/dispatch/podjob/jobspec.go (BuildOptions struct ~line 79, labels map ~lines 185-212 where attempt/provider-secret-uid/task-uid are stamped — note Plan 14-01 already added PricingOverridesJSON here), internal/budget/reservation.go (reservedCostLabel const value), internal/dispatch/podjob/jobspec_test.go</read_first>
  <behavior>
    - BuildOptions.EstimatedCostCents = 250 → Job labels contain "tideproject.k8s/estimated-cost": "250"
    - EstimatedCostCents = 0 → label absent (pre-Phase-14-compatible; Pitfall 5 treats absence as 0 reserved)
  </behavior>
  <action>
    Add `EstimatedCostCents int64` to BuildOptions (doc comment: D-05 — stamped as label tideproject.k8s/estimated-cost so budget.RederiveReservations can restore the in-process store after manager restart, same restart pattern as SecretUID/PreCharge; omitted when 0). In the labels map construction, stamp `labels["tideproject.k8s/estimated-cost"] = strconv.FormatInt(opts.EstimatedCostCents, 10)` only when opts.EstimatedCostCents > 0. Stamp for JobKindExecutor (executor sessions are the run-1 overshoot source); planner Jobs get no estimated-cost label this phase — the BudgetBlocked condition gate covers planner spend (documented scope decision, see Plan objective). Label VALUE must be a bare integer string (K8s label values: alphanumeric start/end — integer cents satisfies this). Extend jobspec_test.go with presence-when-set / absence-when-zero assertions.
  </action>
  <verify>
    <automated>go test ./internal/dispatch/podjob/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - jobspec.go BuildOptions contains EstimatedCostCents int64
    - jobspec.go contains "tideproject.k8s/estimated-cost" matching reservedCostLabel in internal/budget/reservation.go exactly
    - go test ./internal/dispatch/podjob/... -count=1 exits 0
  </acceptance_criteria>
  <done>Executor Jobs can carry the rederivation label; zero estimate produces no label.</done>
</task>

<task type="auto">
  <name>Task 2: TaskReconciler gate rewrite + reserve/settle wiring</name>
  <files>internal/controller/task_controller.go</files>
  <read_first>internal/controller/task_controller.go (TaskReconcilerDeps ~line 82; gateChecks holds #3/#4 lines 336-361; createDispatchJob ~line 608 and ensureJob ~line 1050 — both build BuildOptions; handleJobCompletion ~line 751 including all Failed branches and the RollUpUsage call ~line 857; taskFinalizer cleanup path from const at line 60), internal/controller/budget_blocked.go, internal/budget/reservation.go, internal/controller/billing_halt.go (hold shape being mirrored)</read_first>
  <action>
    Five integration points, in order:
    (1) Deps: add the three fields from the plan interfaces block (Reservations, ReserveEstimateCents, PricingOverridesJSON) to TaskReconcilerDeps with the doc comments given there.
    (2) Gate (gateChecks): immediately after the checkBillingHalt hold (line ~341), add hold #4. First call `setBudgetBlockedIfNeeded(ctx, r.Client, project, r.Deps.Reservations.TotalReserved())`, logging errors as non-fatal (pattern: project_controller.go:1167 non-fatal patch log). Then REPLACE the existing step-4 block (`project.Status.Phase == "BudgetExceeded"` check, lines 348-361) with: `if (checkBudgetBlocked(project) || project.Status.Phase == "BudgetExceeded") && !budget.IsBypassed(project, time.Now())` — the Phase check is kept ORed because D-04 mandates the BudgetExceeded phase machinery STAYS; the condition check fixes the run-1 silent path. Inside the hold: preserve the existing per-Task condition stamp but switch Type to tideprojectv1alpha1.ConditionBudgetBlocked and Reason to tideprojectv1alpha1.ReasonBudgetCapReached (the old code used a string literal Type and Reason=ConditionBudgetExceeded), keep the existing message, and change the halt result from ctrl.Result{} to ctrl.Result{RequeueAfter: 30 * time.Second} — the old empty result never requeued, so a cap raise or bypass could not revive a parked task without an external event. Log "dispatch held: project budget blocked" with task, project, spent, cap fields at V(1).
    (3) Headroom (D-05): after hold #4, add: `if !budget.IsBypassed(project, time.Now()) && !r.Deps.Reservations.HasHeadroom(project, r.Deps.ReserveEstimateCents) { park with RequeueAfter: 30s }`, logging "dispatch held: insufficient reservation headroom" with task, spent, reserved (TotalReserved), estimate, cap at V(1). No per-Task condition stamp here (transient headroom wait, not a cap breach).
    (4) Reserve + BuildOptions: in BOTH createDispatchJob (line ~632) and ensureJob (line ~1058), set `EstimatedCostCents: r.Deps.ReserveEstimateCents` and `PricingOverridesJSON: r.Deps.PricingOverridesJSON` on the podjob.BuildOptions, and call `r.Deps.Reservations.Reserve(string(task.UID), r.Deps.ReserveEstimateCents)` immediately after the successful r.Create(ctx, job) (including the AlreadyExists-as-success branch). Reserve only when ReserveEstimateCents > 0. Same-key re-Reserve on retry dispatch is an intentional overwrite.
    (5) Settle: in handleJobCompletion, call `r.Deps.Reservations.Settle(string(task.UID))` ONCE near the top (after the function commits to handling a terminal Job, before any early-return failure branches) — the Job is terminal regardless of outcome, so the reservation is dead in every branch; a single early call avoids missing one of the several Failed paths. Additionally call `r.Deps.Reservations.Release(string(task.UID))` in the taskFinalizer cleanup path so a deleted Task does not leak its reservation until restart. Immediately AFTER the budget.RollUpUsage call (line ~857), add the non-fatal `setBudgetBlockedIfNeeded(ctx, r.Client, project, r.Deps.Reservations.TotalReserved())` — this is the moment spend first crosses the cap (RESEARCH architecture diagram), so the Project condition appears within the same reconcile that rolled up the breaching cost, not 30s later at the next gate pass.
    Run the full existing controller unit tests after; existing tests construct TaskReconciler without the new Deps fields — nil-safe ReservationStore methods (Plan 14-02) keep them green.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/ -run 'Test' -short -count=1 -timeout 300s</automated>
  </verify>
  <acceptance_criteria>
    - task_controller.go contains setBudgetBlockedIfNeeded at exactly 2 call sites (gate + post-RollUpUsage): grep -c 'setBudgetBlockedIfNeeded' internal/controller/task_controller.go returns 2
    - The gate halt for budget returns ctrl.Result{RequeueAfter: 30 * time.Second} (grep the hold block), not ctrl.Result{}
    - grep -c 'Reservations.Reserve' internal/controller/task_controller.go returns 2 (createDispatchJob + ensureJob)
    - grep -c 'Reservations.Settle\|Reservations.Release' internal/controller/task_controller.go returns >= 2
    - The per-Task condition stamp uses tideprojectv1alpha1.ConditionBudgetBlocked as Type and ReasonBudgetCapReached as Reason (string literal "BudgetBlocked" Type gone from task_controller.go)
    - go build ./... exits 0 and existing controller unit tests pass
  </acceptance_criteria>
  <done>Hold #4 + headroom gate live; reservations reserved at create, settled at terminal, released at finalizer; condition stamped at rollup time.</done>
</task>

<task type="auto">
  <name>Task 3: Run-1 regression envtest — cap trips, Project carries BudgetBlocked, dispatch parks</name>
  <files>internal/controller/budget_blocked_regression_test.go</files>
  <read_first>internal/controller/billing_halt_regression_test.go (envtest structure: stamp helper ~lines 55-75, Describe/Label shape ~lines 652-713, reconciler constructor ~lines 938-998 — adapt field names to the ACTUAL TaskReconcilerDeps shape from Task 2, the PATTERNS.md sketch shows flat fields that do not match), internal/controller/suite_test.go (k8sClient/mgrClient bootstrap), .planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§budget_blocked_regression_test.go)</read_first>
  <behavior>
    - Run-1 scenario (CONTEXT §Specific Ideas): Project with Spec.Budget.AbsoluteCapCents=10000 ($100); patch Status.Budget.CostSpentCents=10001 (simulating RollUpUsage crossing the cap, run-1 numbers from project_dogfood_run1_findings); reconcile a pending Task → Eventually: (a) Project carries BudgetBlocked=True with Reason=BudgetCapReached, (b) no Job exists for the task (Consistently over ~3s), (c) Task is NOT Failed and Project.Status.Phase is NOT Failed
    - Reservation bound: cap=10000, spent=9000, ReserveEstimateCents=600; first pending task dispatches (9000+0+600 < 10000) and holds a 600c reservation; second pending task parks (9000+600+600 >= 10000) — total committed never exceeds cap + one estimate error (vs run-1's ~$40 wave-wide overshoot)
    - Cap raise recovery: after BudgetBlocked=True, patch Spec.Budget.AbsoluteCapCents=20000 → Eventually the condition flips to False (Reason=BudgetCapCleared) and the parked task dispatches
  </behavior>
  <action>
    Create the envtest regression file in package controller with Ginkgo labels Label("envtest", "phase14", "budget-blocked", "regression"). Build the TaskReconciler under test with Deps including budget.NewReservationStore() and ReserveEstimateCents per scenario. Use the stampBudgetSpend MergeFrom status-patch helper pattern and the Eventually condition-assert shared pattern from PATTERNS.md. Assert the BudgetBlocked condition via meta.FindStatusCondition with the api constants — never string literals. For the no-Job assertion use Consistently with the task-uid label selector. Header comment names the run-1 symptom being reproduced: "cap hit at $100, ~$40 overshoot, silent Project" (CONTEXT phase boundary).
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - budget_blocked_regression_test.go exists with Label("envtest", "phase14", "budget-blocked", "regression")
    - make test exits 0 AND its output contains the BudgetBlocked regression spec names (grep the run log for 'budget-blocked' or the Describe text)
    - The three scenarios in <behavior> each have at least one It block
  </acceptance_criteria>
  <done>Run-1 silence is irreproducible: cap trip → visible Project condition, parked dispatch, bounded commitment, cap-raise recovery — all asserted in envtest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task/Job events → dispatch gate | Gate decisions ride on Project status conditions + in-process store |
| Bypass annotation | Operator-controlled escape hatch over the cap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | concurrent headroom check race (two reconciles both see headroom) | mitigate (bounded) | Overshoot bounded to one session estimate per concurrent reconcile; documented by-design; regression test asserts the wave-wide overshoot class is gone |
| T-14-07 | Elevation of Privilege | bypass annotation dispatches past cap | accept | Existing D-D4 machinery, deliberately preserved (CONTEXT: "cap raises via Spec edit / bypass annotation already exist — don't invent a second unlock path") |
| T-14-08 | Denial of Service | stale BudgetBlocked=True permanently parks dispatch after cap raise | mitigate | Bidirectional setBudgetBlockedIfNeeded clears with ReasonBudgetCapCleared + 30s requeue on every park; covered by cap-raise recovery scenario |
| T-14-SC | Tampering | package installs | accept | No new dependencies; RESEARCH §Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `make test` exits 0 (unit tier including internal/controller envtest suite)
- `go test ./internal/dispatch/podjob/... -count=1` exits 0
- `grep -n 'Status.Phase == "BudgetExceeded"' internal/controller/task_controller.go` shows the check only inside the new ORed hold (machinery preserved, no longer the sole gate)
- `make verify-no-aggregates` exits 0
</verification>

<success_criteria>
BUDGET-02: cap trip is loudly visible on the Project (condition, kubectl-readable) within one task-reconcile cycle, with the run-1 regression asserted in envtest. BUDGET-03: dispatch is reservation-gated, executor Jobs carry the rederivation label, overshoot is bounded to estimate error, and terminal/finalizer paths release reservations.
</success_criteria>

<output>
Create `.planning/phases/14-budget-enforcement-pricing/14-03-SUMMARY.md` when done.
</output>
