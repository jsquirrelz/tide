---
phase: 14-budget-enforcement-pricing
plan: 05
type: execute
wave: 3
depends_on: [14-02, 14-03]
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/project_controller.go
  - internal/dispatch/podjob/backend.go
  - cmd/manager/main.go
  - charts/tide/values.yaml
  - charts/tide/templates/deployment.yaml
  - test/integration/kind/projects_pvc_test.go
autonomous: true
requirements: [BUDGET-01, BUDGET-02, BUDGET-03]
tags: [budget, helm, dispatch-gate, controller-runtime, go]
user_setup: []
must_haves:
  truths:
    - "All five dispatch sites (milestone, phase, plan, project, task) hold on BudgetBlocked=True — no new spend of any kind once the cap trips"
    - "Manager validates --pricing-overrides-json at startup and exits non-zero on invalid prices"
    - "Manager restart rederives reservations from in-flight Job estimated-cost labels before reconciling"
    - "helm template renders --pricing-overrides-json and --budget-reserve-per-dispatch-cents from values.yaml"
  artifacts:
    - path: "cmd/manager/main.go"
      provides: "Flags, startup validation, RederiveReservations runnable, Deps wiring"
      contains: "pricing-overrides-json"
    - path: "charts/tide/values.yaml"
      provides: "pricing.overrides + budget.reservePerDispatchCents documented values"
      contains: "pricing:"
    - path: "charts/tide/templates/deployment.yaml"
      provides: "Manager args carrying both new flags"
      contains: "budget-reserve-per-dispatch-cents"
  key_links:
    - from: "charts/tide/templates/deployment.yaml"
      to: "cmd/manager/main.go"
      via: "--pricing-overrides-json + --budget-reserve-per-dispatch-cents flags"
      pattern: "pricing-overrides-json"
    - from: "cmd/manager/main.go"
      to: "internal/budget/reservation.go"
      via: "mgr.Add runnable calling budget.RederiveReservations (mirrors budget.PreCharge at main.go:521-529)"
      pattern: "RederiveReservations"
    - from: "internal/controller/{milestone,phase,plan,project}_controller.go"
      to: "internal/controller/budget_blocked.go"
      via: "checkBudgetBlocked hold after checkBillingHalt"
      pattern: "checkBudgetBlocked"
---

<objective>
Complete the phase: roll the BudgetBlocked hold out to the four planner-dispatch sites (mirroring Phase 13's checkBillingHalt placement exactly — RESEARCH Open Question 2 resolved as "mirror BillingHalt at all five sites": planner Jobs burn budget too), and wire the operator-facing configuration end to end — manager flags with startup validation, reservation rederivation at restart, Deps population, and the additive Helm chart surface (D-02) with a helm-template contract test.

Purpose: after Plan 14-03, only the TaskReconciler holds on BudgetBlocked and the new Deps fields are zero-valued in production. This plan makes the cap project-wide-hard at every dispatch site and makes D-02/D-05 operator-tunable from values.yaml.

Chart note: CLAUDE.md marks charts/tide/values.yaml as a FIXED contract; D-02 explicitly locks this ADDITIVE chart change ("Additive chart change; document in values.yaml") — user-authorized, additive-only, no existing keys modified.

Output: four controller gates, main.go wiring, chart values + deployment args, helm contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-budget-enforcement-pricing/14-CONTEXT.md
@.planning/phases/14-budget-enforcement-pricing/14-PATTERNS.md (§values.yaml, §shared dispatch-park pattern)
@internal/controller/billing_halt.go
@cmd/manager/main.go
</context>

<interfaces>
Existing billing-halt hold sites the new hold mirrors (insert the BudgetBlocked hold IMMEDIATELY AFTER each, same shape — Pitfall 4: the two holds are separate and may both be true):

- internal/controller/milestone_controller.go:309 — `if checkBillingHalt(earlyProject) { ... RequeueAfter: 30s }`
- internal/controller/phase_controller.go:298 — same shape
- internal/controller/plan_controller.go:309 — same shape (note: returns `ctrl.Result{...}, true, nil` — three values)
- internal/controller/project_controller.go:949 — same shape

New hold shape at each site (adjust return arity per site):

```go
// Phase 14 BUDGET-02 / D-04: BudgetBlocked hold (operator cap) — separate from
// BillingHalt (provider billing); both may be true simultaneously.
if checkBudgetBlocked(earlyProject) && !budget.IsBypassed(earlyProject, time.Now()) {
    logf.FromContext(ctx).V(1).Info("dispatch held: project budget blocked", ...)
    return ctrl.Result{RequeueAfter: 30 * time.Second}, nil
}
```

Manager flags (this plan adds):

```go
flag.StringVar(&pricingOverridesJSON, "pricing-overrides-json", "",
    "JSON map of model-ID to price overrides in cents/MTok (Helm pricing.overrides, D-02); validated at startup")
flag.Int64Var(&budgetReservePerDispatchCents, "budget-reserve-per-dispatch-cents", 100,
    "Flat per-dispatch reservation estimate in cents (Helm budget.reservePerDispatchCents, D-05); 0 disables pre-charge")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BudgetBlocked hold at the four planner dispatch sites + override JSON on planner BuildOptions</name>
  <files>internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/project_controller.go, internal/dispatch/podjob/backend.go</files>
  <read_first>internal/controller/milestone_controller.go (lines ~300-320 billing-halt block + BuildOptions at ~398), internal/controller/phase_controller.go (~290-310, ~351), internal/controller/plan_controller.go (~300-320 — note three-value return, ~383), internal/controller/project_controller.go (~940-960, ~996), internal/dispatch/podjob/backend.go (PodJobBackend struct ~line 192 + its BuildOptions at ~273), internal/controller/budget_blocked.go</read_first>
  <action>
    At each of the four sites, insert the BudgetBlocked hold from the plan interfaces block immediately after the existing checkBillingHalt block, matching that site's exact return arity and log-field style (milestone logs "milestone"+"project", phase logs "phase", plan logs "plan", project logs "project"). Add the `internal/budget` and `time` imports where missing (needed for budget.IsBypassed). Do NOT stamp per-level conditions — comment at each site mirrors the billing-halt comment: "No per-<Level> condition written (operator signal is the single Project BudgetBlocked condition)". Where each of these four reconcilers constructs podjob.BuildOptions for planner dispatch (milestone ~398, phase ~351, plan ~383, project ~996), set `PricingOverridesJSON:` from that reconciler's config — add a `PricingOverridesJSON string` field to each reconciler's existing Helm-defaults/deps carrier (follow how CredproxyImage/HelmProviderDefaults reach each reconciler; keep the field name identical across all four). Also add a `PricingOverridesJSON string` field to PodJobBackend (backend.go ~192) and set it on the BuildOptions it builds at ~273, so the Dispatcher.Run path carries it too. Do NOT add EstimatedCostCents to planner BuildOptions (reservation scope is executor Jobs — Plan 14-03 decision).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/ -short -count=1 -timeout 300s</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'checkBudgetBlocked' for each of the four controller files returns >= 1
    - Each new hold sits AFTER checkBillingHalt in the same function (verify by reading each block)
    - grep -c 'PricingOverridesJSON' internal/dispatch/podjob/backend.go returns >= 2 (field + BuildOptions population)
    - go build ./... exits 0; existing controller tests pass
  </acceptance_criteria>
  <done>Cap breach halts planner and project dispatch too — zero new spend project-wide once BudgetBlocked is True, bypass honored everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: main.go — flags, startup validation, RederiveReservations runnable, Deps wiring</name>
  <files>cmd/manager/main.go</files>
  <read_first>cmd/manager/main.go (flag block ~lines 141-163, budget.PreCharge runnable registration ~lines 521-529, TaskReconciler wiring ~lines 478-496, and the four planner reconciler wiring blocks), internal/budget/reservation.go (RederiveReservations signature), pkg/dispatch/pricing.go (ParsePricingOverrides)</read_first>
  <action>
    (1) Add the two flags from the plan interfaces block (exact names/defaults: --pricing-overrides-json default "", --budget-reserve-per-dispatch-cents default 100).
    (2) Startup validation (T-14-01 / ASVS V5): after flag parsing, when pricingOverridesJSON is non-empty call pkgdispatch.ParsePricingOverrides; on error, setupLog.Error + os.Exit(1) — fail fast, never run with silently-broken billing config. Also os.Exit(1) on negative budgetReservePerDispatchCents.
    (3) Create `reservationStore := budget.NewReservationStore()` and register a manager Runnable mirroring the budget.PreCharge block at lines 521-529 (same NeedLeaderElection/cache-warm pattern and timeout style) that calls `budget.RederiveReservations(ctx, mgr.GetClient(), reservationStore)` — restores in-flight reservations after restart (Pitfall 3); log a startup line with the rederived TotalReserved.
    (4) Wire TaskReconciler Deps: Reservations: reservationStore, ReserveEstimateCents: budgetReservePerDispatchCents, PricingOverridesJSON: pricingOverridesJSON. Wire PricingOverridesJSON into the four planner reconcilers' carrier fields from Task 1 and into the PodJobBackend construction if main.go builds it.
    (5) Choice note for the doc comment: a dedicated flag (not the --config YAML) is used because it mirrors the established --rate-limit-default-rpm Helm→flag pattern (RESEARCH Open Question 1 resolved: consistency with existing tunables).
  </action>
  <verify>
    <automated>go build ./cmd/manager && go run ./cmd/manager --pricing-overrides-json='{"x":{"inputCentsPerMTok":0,"outputCentsPerMTok":1}}' --help 2>&1 | head -5; test $? -eq 0 || true; go vet ./cmd/manager</automated>
  </verify>
  <acceptance_criteria>
    - main.go contains both flag registrations with the exact flag names from the interfaces block
    - main.go contains ParsePricingOverrides followed by os.Exit(1) on error
    - main.go contains RederiveReservations registered as a mgr.Add runnable (not called synchronously before cache start — mirrors the PreCharge informer-warm comment)
    - TaskReconciler Deps wiring sets Reservations, ReserveEstimateCents, PricingOverridesJSON
    - go build ./cmd/manager exits 0
  </acceptance_criteria>
  <done>Operator config flows flags → validation → reconcilers; restart restores reservations before reconciling.</done>
</task>

<task type="auto">
  <name>Task 3: Chart surface (additive) + helm-template contract test</name>
  <files>charts/tide/values.yaml, charts/tide/templates/deployment.yaml, test/integration/kind/projects_pvc_test.go</files>
  <read_first>charts/tide/values.yaml (rateLimits block lines ~115-121 — the documented-tunable pattern to mirror; confirm no existing pricing/budget top-level keys), charts/tide/templates/deployment.yaml (args list lines ~32-33 rate-limit flags), test/integration/kind/projects_pvc_test.go (TestHelmDeploymentTemplate* helpers at lines 159-204 — how helm template is invoked and asserted)</read_first>
  <action>
    (a) values.yaml — ADDITIVE ONLY (D-02 authorizes; do not touch existing keys): after the rateLimits block add the documented `pricing:` stanza from PATTERNS.md (overrides: {} with the comment block explaining cents/MTok keys, the optional-fields-inherit rule, and the --pricing-overrides-json transport) and a `budget:` stanza: `reservePerDispatchCents: 100` with comment "D-05: flat per-dispatch reservation estimate in cents pre-charged against the budget cap at Job creation; bounds in-flight overshoot to estimate error instead of wave width; 0 disables pre-charge (cap still enforced at dispatch via the BudgetBlocked condition). Passed via --budget-reserve-per-dispatch-cents."
    (b) deployment.yaml — append two manager args next to the rate-limit args: `- --budget-reserve-per-dispatch-cents={{ .Values.budget.reservePerDispatchCents | default 100 }}` and, wrapped in `{{- if .Values.pricing.overrides }}`, `- --pricing-overrides-json={{ .Values.pricing.overrides | toJson }}` (conditional so the default empty map renders no flag — manager default "" means no overrides; avoids YAML-quoting churn for the common case).
    (c) Extend the helm-template contract tests in test/integration/kind/projects_pvc_test.go (same helpers as TestHelmDeploymentTemplateRendersManagerPodAnnotations): one test asserting default render contains --budget-reserve-per-dispatch-cents=100 and does NOT contain --pricing-overrides-json; one asserting render with `--set pricing.overrides.claude-test-model.inputCentsPerMTok=300 --set pricing.overrides.claude-test-model.outputCentsPerMTok=1500` contains --pricing-overrides-json with claude-test-model. These are plain go-tests that shell out to helm template (no cluster needed) — remember the Phase-7 lesson: one RED go-test here fails make test-int even when Ginkgo is green, so run them before committing.
  </action>
  <verify>
    <automated>go test ./test/integration/kind/ -run TestHelmDeploymentTemplate -count=1</automated>
  </verify>
  <acceptance_criteria>
    - values.yaml contains a `pricing:` top-level key with `overrides: {}` and a `budget:` key with `reservePerDispatchCents: 100`
    - deployment.yaml contains both new args; the pricing arg is conditional on non-empty overrides
    - go test ./test/integration/kind/ -run TestHelmDeploymentTemplate -count=1 exits 0 (new tests included)
    - `helm template charts/tide` exits 0 with defaults
  </acceptance_criteria>
  <done>D-02 chart surface shipped additively with rendered-args contract coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| values.yaml → manager flags | Operator config crosses into billing enforcement |
| Manager restart → Job-label scan | Rederivation trusts manager-stamped labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | zero/negative price overrides via Helm | mitigate | ParsePricingOverrides validation + os.Exit(1) at manager startup; subagent-side loud-ignore as defense in depth (14-01) |
| T-14-12 | Denial of Service | huge ReserveEstimateCents blocks all dispatch | accept | Operator self-misconfiguration of their own cap math; symptom is visible (headroom hold log + no Jobs), recoverable by values change |
| T-14-05 | Denial of Service | rederivation over-reservation after upgrade | mitigate | Pre-Phase-14 Jobs lack the label → treated as 0 reserved (under-reserve, never over-reserve; Pitfall 5) — verified by 14-02 rederive tests |
| T-14-SC | Tampering | package installs | accept | No new dependencies; RESEARCH §Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `go build ./...` and `make test` exit 0
- `go test ./test/integration/kind/ -run TestHelmDeploymentTemplate -count=1` exits 0
- `helm template charts/tide | grep -c 'budget-reserve-per-dispatch-cents'` returns >= 1
- All five dispatch sites grep-positive for checkBudgetBlocked: `grep -l checkBudgetBlocked internal/controller/{milestone,phase,plan,project,task}_controller.go | wc -l` returns 5
- `make lint` passes
</verification>

<success_criteria>
The cap is project-wide-hard: every dispatch site holds on BudgetBlocked (bypass-aware); operators tune overrides and the reservation estimate from values.yaml; a restarted manager re-arms reservations before reconciling; the chart contract is test-pinned.
</success_criteria>

<output>
Create `.planning/phases/14-budget-enforcement-pricing/14-05-SUMMARY.md` when done.
</output>
