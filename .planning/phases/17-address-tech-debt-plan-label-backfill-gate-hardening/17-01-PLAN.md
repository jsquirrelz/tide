---
phase: 17-address-tech-debt-plan-label-backfill-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/plan_controller.go
  - internal/controller/plan_controller_test.go
  - internal/reporter/materialize.go
  - internal/reporter/materialize_test.go
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "A pre-v1.0.1 Plan CR with no tideproject.k8s/project label gets the label stamped on its next reconcile, becoming visible to tide approve / tide resume label selectors"
    - "The backfill is idempotent: a second reconcile of an already-labeled Plan does not bump its ResourceVersion"
    - "A reporter-created Milestone whose parent is a *Project carries the tideproject.k8s/project label at create time (not only after backfill)"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "Plan-level project-label backfill block in PlanReconciler.Reconcile between owner-ref (step 4) and dispatch (step 5)"
      contains: "owner.LabelProject"
    - path: "internal/reporter/materialize.go"
      provides: "*Project special-case so the Project→Milestone reporter edge stamps the project name from parent.GetName()"
      contains: "StampProjectLabel"
    - path: "internal/controller/plan_controller_test.go"
      provides: "envtest spec proving Plan backfill + idempotency"
      contains: "backfill"
  key_links:
    - from: "PlanReconciler.Reconcile"
      to: "resolveProjectName"
      via: "label-absent guard before dispatch"
      pattern: "resolveProjectName"
    - from: "MaterializeChildCRDs"
      to: "owner.StampProjectLabel"
      via: "*Project type-switch resolving parent.GetName()"
      pattern: "StampProjectLabel"
---

<objective>
Close DEBT-01: the Plan-level project-label backfill gap (the audit's headline finding) plus the Project→Milestone reporter-edge create-site stamp (15-WR-03).

Milestones (`milestone_controller.go:178-194`) and Phases (`phase_controller.go:168-186`) backfill `tideproject.k8s/project` in-reconciler when absent; the PlanReconciler does not. A pre-v1.0.1 Plan CR on an upgraded cluster stays unlabeled and is therefore invisible to `tide approve`'s `findAwaitingPlan` selector and `tide resume --retry-failed`'s scans. This plan brings PlanReconciler up to the milestone/phase pattern, and stamps the one reporter edge (Project→Milestone) whose create-site label resolution is a no-op today.

Purpose: Self-healing label restores correct project-scoped discovery for parked Plan CRs on upgrade.
Output: Backfill block in PlanReconciler.Reconcile, `*Project` special-case in materialize.go, and regression specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-RESEARCH.md
@.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-PATTERNS.md

<interfaces>
<!-- Reuse these — do NOT hand-roll. Extracted from the live tree this session. -->

internal/owner/label.go:
- owner.LabelProject = "tideproject.k8s/project"  (const; never write the literal)
- owner.StampProjectLabel(obj, projectName string)  // idempotent, no-ops on empty string

internal/controller/plan_controller.go:
- func (r *PlanReconciler) resolveProjectName(ctx, *Plan) (string, error)  // :1415 — fast-path label + owner-chain walk; returns ErrParentUnresolved on miss
- func (r *PlanReconciler) resolveProjectForPlan(ctx, *Plan) *Project       // :829 — Plan→Phase→Milestone→Project (Items[0] mis-routing fallback already removed)
- Reconcile step 4 (owner-ref) ends ~:173; step 5 (Dispatcher seam) starts :175/:179

internal/reporter/materialize.go:
- func MaterializeChildCRDs(ctx, c, scheme, parent metav1.Object, children []ChildCRDSpec) error  // :188
- existing stamp call :257: owner.StampProjectLabel(obj, parent.GetLabels()[owner.LabelProject])
- parent type-switch precedent at :92 (case *tideprojectv1alpha1.Project)
- alias: tideprojectv1alpha1 "github.com/jsquirrelz/tide/api/v1alpha1"

internal/reporter/materialize_test.go:
- func TestMaterializeChildCRDsStampsParentRef(t)  // :247 — table-driven, already has a "project->milestone" row at :257-265 (Project parent, Milestone child); sibling host for the create-site label assertion
- helpers: fakeClientForTest(t), mustJSON(t, v), AddToScheme(scheme); alias tideprojectv1alpha1 already imported
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backfill tideproject.k8s/project on Plan CRs in PlanReconciler.Reconcile</name>
  <read_first>
    - internal/controller/plan_controller.go :127-203 (Reconcile head; the gap is between step 4 owner-ref ~:154-173 and step 5 Dispatcher :175-179) AND :829, :1408-1425 (resolveProjectForPlan + resolveProjectName)
    - internal/controller/phase_controller.go :168-186 (the analog backfill block to copy)
    - internal/controller/milestone_controller.go :178-194 (second analog for symmetry)
    - internal/controller/phase_controller_test.go :270-334 (the backfill spec to mirror — Describe "PhaseReconciler — D-03 project-label backfill")
    - 17-PATTERNS.md Item 1 (the REQUIRED ADAPTATION: resolver returns error not "", so guard is `err == nil && name != ""`)
  </read_first>
  <behavior>
    - A Plan with no tideproject.k8s/project label, but with a traversable Spec.PhaseRef → Phase → Milestone → Project chain, gets the label set to the project name after one reconcile.
    - Idempotency: reconciling an already-labeled Plan does not change its ResourceVersion.
    - Orphan safety: a Plan whose owner chain is unresolvable (resolveProjectName returns ErrParentUnresolved) is skipped silently — no patch, no error returned from the backfill block.
  </behavior>
  <action>
    Insert a label-absent backfill block into PlanReconciler.Reconcile BETWEEN step 4 (owner-ref Update, ends ~:173) and step 5 (Dispatcher seam, :175/:179) — Pitfall 2: it MUST run before dispatch so a parked AwaitingApproval Plan self-heals on first post-upgrade reconcile. Mirror phase_controller.go:168-186 exactly with the one REQUIRED ADAPTATION from 17-PATTERNS.md Item 1: the Plan's resolveProjectName returns `(string, error)` (ErrParentUnresolved on miss), NOT a bare "" like the milestone/phase helpers. So the absent-guard is `if name, err := r.resolveProjectName(ctx, &plan); err == nil && name != ""`. Use `owner.LabelProject` for the guard key and the set key — never the string literal (Don't-Hand-Roll: avoids label-literal drift). Reuse the existing `resolveProjectName` / `resolveProjectForPlan` (:1415 / :829) — do NOT add a new chain walker. Patch via `client.MergeFrom(plan.DeepCopy())` with nil-map init, exactly as the analog. Add the envtest spec to plan_controller_test.go mirroring phase_controller_test.go:270-334: create Project → Milestone(ProjectRef) → Phase(MilestoneRef) → Plan(Spec.PhaseRef, labels absent); construct PlanReconciler with NO Dispatcher (drives steps 1-5 only); reconcileWithRetry; assert `after.Labels[owner.LabelProject] == projName`; then idempotency — record ResourceVersion, reconcile again, assert unchanged.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run PlanReconciler -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: plan_controller.go contains a `plan.Labels[owner.LabelProject] == ""` guard with a `resolveProjectName` call and a `client.Patch`, positioned before the `if r.Dispatcher != nil` block. Verify: `grep -nE 'plan.Labels\[owner.LabelProject\] == ""' internal/controller/plan_controller.go` returns exactly 1 line, AND that line number is less than the line of `if r.Dispatcher != nil`.
    - Source: no string literal `"tideproject.k8s/project"` introduced in the new block. Verify: the backfill block references `owner.LabelProject`, not the literal.
    - Test: `go test ./internal/controller/... -run PlanReconciler -count=1` exits 0, and the new backfill Describe block runs (grep the test output or `grep -c 'project-label backfill' internal/controller/plan_controller_test.go` returns ≥ 1).
    - Behavior: the spec asserts both the labeled outcome AND ResourceVersion-unchanged idempotency.
  </acceptance_criteria>
  <done>Plan CR backfill block present and ordered before dispatch; envtest spec proves stamp + idempotency; PlanReconciler tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stamp the Project→Milestone reporter edge at create-site (15-WR-03)</name>
  <read_first>
    - internal/reporter/materialize.go :85-100 (parent type-switch precedent) AND :250-260 (the existing StampProjectLabel call at :257) AND :188 (MaterializeChildCRDs signature — confirm parent is passed as metav1.Object)
    - internal/owner/label.go :33,45 (LabelProject const + StampProjectLabel — already idempotent, no-ops on empty)
    - internal/reporter/materialize_test.go :240-329 (TestMaterializeChildCRDsStampsParentRef — the table-driven materialize test that already exercises a Project parent → Milestone child "project->milestone" row at :257-265; host the create-site label assertion here as a sibling test, NOT in milestone_controller_test.go)
    - 17-PATTERNS.md Item 6 (the one-line *Project type-switch fix shape)
  </read_first>
  <behavior>
    - When MaterializeChildCRDs is called with a *Project parent, the materialized Milestone child carries `tideproject.k8s/project == project.GetName()` at create time.
    - When the parent is a *Milestone/*Phase/*Plan, behavior is unchanged (label still resolved from parent.GetLabels()[owner.LabelProject]).
  </behavior>
  <action>
    At the existing stamp call (materialize.go:257), resolve the project name with a `*Project` special-case before calling StampProjectLabel: compute `projectName := parent.GetLabels()[owner.LabelProject]`, then `if _, isProject := parent.(*tideprojectv1alpha1.Project); isProject { projectName = parent.GetName() }`, then `owner.StampProjectLabel(obj, projectName)`. The type-switch precedent at :92 confirms `*tideprojectv1alpha1.Project` is the concrete type and the alias is already imported. Do NOT modify StampProjectLabel — it already no-ops on empty (fail-open). Add the create-time label assertion to internal/reporter/materialize_test.go as a sibling of TestMaterializeChildCRDsStampsParentRef (the file already has a "project->milestone" row and the fakeClientForTest / mustJSON helpers): materialize a Milestone under a *Project parent and assert `got.GetLabels()[owner.LabelProject] == project.GetName()` immediately after MaterializeChildCRDs returns (BEFORE any backfill reconcile). This keeps materialize_test.go as 17-01's exclusive test home and removes any shared edit with 17-02 on milestone_controller_test.go.
  </action>
  <verify>
    <automated>go test ./internal/reporter/... -run Materialize -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: materialize.go contains a `parent.(*tideprojectv1alpha1.Project)` type assertion guarding a `parent.GetName()` assignment immediately before the StampProjectLabel call. Verify: `grep -nE 'parent\.\(\*tideprojectv1alpha1\.Project\)' internal/reporter/materialize.go` returns ≥ 1.
    - Source: the StampProjectLabel call now passes the resolved `projectName` variable, not `parent.GetLabels()[owner.LabelProject]` inline.
    - Test: a test in internal/reporter/materialize_test.go asserts a *Project-parent materialized Milestone has `Labels[owner.LabelProject] == project.GetName()` at create time. `go test ./internal/reporter/... -run Materialize -count=1` exits 0.
  </acceptance_criteria>
  <done>materialize.go stamps the Project→Milestone edge at create-site; a reporter-side materialize_test.go assertion proves the label is present before any backfill; reporter tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconciler → CRD status/labels | Reconciler writes project-scoping labels that downstream auth-gate discovery (tide approve / resume) reads to decide which level an operator may approve |
| reporter → child CRD create | Reporter (in-namespace trusted component) stamps the project label that scopes the child to its Project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Backfill mis-resolves project in a multi-Project namespace → a Plan gets labeled with the wrong project, becoming approvable under the wrong scope | mitigate | Reuse `resolveProjectName`/`resolveProjectForPlan` (the Items[0] List-first mis-routing fallback was already removed, plan_controller.go:1413). Do NOT introduce a List-first resolver. The owner-chain walk is deterministic per Plan. |
| T-17-02 | Spoofing | Reporter stamps a *Project child with a project name not its own | accept | Reporter is an in-namespace trusted component; parent.GetName() is the authoritative project identity for a *Project parent. No external input crosses this boundary. |
| T-17-03 | Elevation | Backfill labels an orphan Plan (unresolvable chain) with a stale/guessed scope, exposing it to an unintended approver | mitigate | Orphan case (resolveProjectName → ErrParentUnresolved) is skipped silently; an unlabeled orphan stays invisible to discovery rather than mis-scoped. |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (zero external deps; RESEARCH Package Legitimacy Gate satisfied vacuously). |
</threat_model>

<verification>
- `go test ./internal/controller/... -run PlanReconciler -count=1` exits 0 (backfill + idempotency specs).
- `go test ./internal/reporter/... -run Materialize -count=1` exits 0 (Project-parent create-site stamp assertion).
- `grep -nE 'plan.Labels\[owner.LabelProject\] == ""' internal/controller/plan_controller.go` returns 1 line ordered before `if r.Dispatcher != nil`.
</verification>

<success_criteria>
- PlanReconciler.Reconcile backfills the project label before dispatch, idempotently, skipping orphans.
- The Project→Milestone reporter edge stamps the label at create-site.
- Regression specs prove both, and PlanReconciler/reporter tests are green.
</success_criteria>

<output>
Create `.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
