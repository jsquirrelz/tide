---
phase: 17-address-tech-debt-plan-label-backfill-gate-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/tide/approve.go
  - cmd/tide/approve_test.go
autonomous: true
requirements: [DEBT-03]

must_haves:
  truths:
    - "An operator can approve a healthy AwaitingApproval level even when an UNRELATED sibling level on the same project is Failed — approval is refused only when the approval TARGET itself is Failed"
    - "The strict failure profile is honored: a failed task blocks its dependents, not unrelated parked levels awaiting approval"
    - "The --wave path's guard semantics are consistent with the narrowed level-path guard and documented in the plan"
  artifacts:
    - path: "cmd/tide/approve.go"
      provides: "D-07 failed-level guard narrowed to the approval target (Option A) in approveLevel"
      contains: "buildFailureDetail"
  key_links:
    - from: "approveLevel"
      to: "findAwaiting* discovery"
      via: "discover target first, then refuse only if THAT object is Failed"
      pattern: "findAwaiting"
---

<objective>
Close DEBT-03 (WR-06) via the LOCKED decision: Option A — narrow the D-07 approve guard to the approval target.

Today `findFailedLevel` (approve.go:194) scans ALL four level kinds across the whole project; `approveLevel` (:155-163) refuses ANY approval if any one level is Failed. This over-blocks: an operator cannot approve an unrelated healthy AwaitingApproval level just because some sibling somewhere failed — contradicting the strict failure profile (siblings are independent; only dependents halt). Meanwhile the `--wave` path (approveRun:71-73) skips the guard entirely. Per the locked decision and project memory (fix-thoroughly-on-TIDE), narrow the guard so approval is refused only when the level being approved is itself Failed. Do NOT choose Option B (apply the project-wide guard to `--wave`).

Purpose: Align the approve gate with the strict-failure-profile contract; remove the project-wide over-block while preserving D-07's "approval never doubles as a spend-retry" intent for the targeted level.
Output: Narrowed guard in approveLevel + table-test rows pinning the new semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-RESEARCH.md
@.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-PATTERNS.md

<interfaces>
cmd/tide/approve.go:
- func approveRun(ctx, c, ns, projectName, waveFlag string, out io.Writer) error  // :70 — branches to approveWave (:73) when waveFlag set, else approveLevel (:77)
- func approveLevel(ctx, c, ns, projectName string) error  // :143; current project-wide guard :155-163 (calls findFailedLevel)
- func findFailedLevel(ctx, c, ns, projectName) (client.Object, string, error)  // :194 — scans all four kinds; retire or repurpose for the targeted check
- func findAwaitingMilestone/Phase/Plan/Task(...) (client.Object, level string, error)  // :304/:323/:340/:357 — discovery chain
- func buildFailureDetail(obj client.Object) string  // :258 — reuse for the error message

cmd/tide/approve_test.go:
- helpers makeProject (:31), makeFailedMilestone (:66); existing over-block tests TestApproveRunFailedLevelError (:173), TestApproveFailedLevelErrorIncludesReason (:192) — plain Go tests with fake.NewClientBuilder().WithScheme(testScheme(t)).WithObjects(...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow the D-07 approve guard to the approval target (Option A) + pin semantics with table tests</name>
  <read_first>
    - cmd/tide/approve.go :60-77 (approveRun branch), :143-188 (approveLevel: guard :155-163, discovery chain :167-187), :190-260 (findFailedLevel + buildFailureDetail)
    - cmd/tide/approve_test.go :30-78 (helpers), :170-217 (the existing over-block tests — the new rows are the inverse)
    - 17-PATTERNS.md Item 3 (Option-A fix shape: discover the AwaitingApproval target FIRST, then refuse only if THAT object is Failed; --wave stays guard-free in the level sense)
    - 17-RESEARCH.md Item 3 (decision locked to A; strict-failure-profile rationale)
  </read_first>
  <behavior>
    - Project has one Failed Plan AND one healthy AwaitingApproval Phase → approveRun succeeds against the Phase (the unrelated Failed Plan does not block).
    - Project's AwaitingApproval target IS itself Failed → approveRun returns the actionable error pointing at `tide resume --retry-failed` (D-07 intent preserved for the target).
    - `--wave` path semantics are explicit and documented (a `--wave` approve targets a specific Plan/wave, not a project-wide gate).
  </behavior>
  <action>
    In approveLevel, remove the project-wide `findFailedLevel` pre-guard (:155-163) and reorder so the AwaitingApproval target is discovered FIRST via the findAwaitingMilestone→Phase→Plan→Task chain (:167-187). Then refuse approval ONLY if that discovered target's `Status.Phase == "Failed"` — reuse `buildFailureDetail(obj)` for the same actionable message (points at `tide resume --retry-failed`). The project-wide `findFailedLevel` scan (:194) may be retired or kept solely as a helper for the targeted check — do not leave it wired as a blanket pre-guard. For the `--wave` path (approveRun:71-73), leave it returning approveWave without the level-path guard, and add a brief code comment documenting the Option-A decision (a `--wave` approve targets a specific Plan/wave, not a project-wide gate). Add table-test rows to approve_test.go: (a) one Failed Plan + one healthy AwaitingApproval Phase → assert approveRun SUCCEEDS against the Phase (inverse of TestApproveRunFailedLevelError); (b) the AwaitingApproval target itself Failed → assert the actionable error still fires; (c) a `--wave` row pinning the chosen semantics. Reuse makeProject / makeFailedMilestone and add a healthy-AwaitingApproval fixture as needed.
  </action>
  <verify>
    <automated>go test ./cmd/tide/... -run Approve -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: approveLevel no longer calls `findFailedLevel` as an unconditional pre-guard before the findAwaiting chain. Verify: `findFailedLevel` is not invoked before the first `findAwaiting*` call in approveLevel (the discovery now precedes any failed check).
    - Source: the failed-refusal now checks the discovered target's status (not a project-wide scan); `buildFailureDetail` is still used for the message.
    - Source: a code comment documents the Option-A `--wave` decision near approveRun:71-73.
    - Test: `go test ./cmd/tide/... -run Approve -count=1` exits 0. New rows assert: healthy unrelated level approvable despite an unrelated Failed Plan; Failed target still refused with the resume pointer; `--wave` semantics pinned.
  </acceptance_criteria>
  <done>Approve guard narrowed to the target (Option A); over-block removed; D-07 intent preserved for the target; `--wave` decision documented; approve tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → approve gate | `tide approve` is a human authorization gate; the guard decides whether spend-bearing child dispatch is unblocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Elevation (spend) | Guard narrowed too far → an operator approves a level whose OWN planner Job Failed, doubling approval as a spend-retry (the exact thing D-07 prevents) | mitigate | Option A keeps the refusal for the approval TARGET: if the discovered AwaitingApproval object is itself Failed, still error with the `tide resume --retry-failed` pointer. Table test (b) asserts this. |
| T-17-08 | Denial of Service | Over-broad guard (status quo) blocks all approvals project-wide on any one Failed level, stalling unrelated healthy work | mitigate | Narrowing to the target removes the project-wide over-block; table test (a) asserts the unrelated healthy level is approvable. Honors the strict-failure-profile spec contract. |
| T-17-09 | Tampering | `--wave` path silently diverges from the level-path semantics, creating an inconsistent gate an operator can exploit | accept | A `--wave` approve targets a specific Plan/wave by design, not a project-wide gate; the decision is documented in-code and pinned by a `--wave` test row. Low risk — both paths are now coherent under Option A. |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `go test ./cmd/tide/... -run Approve -count=1` exits 0.
- approveLevel discovers the target before any failed check; `findFailedLevel` is not an unconditional pre-guard.
- A `--wave` decision comment is present near approveRun.
</verification>

<success_criteria>
- An unrelated healthy AwaitingApproval level is approvable despite an unrelated Failed sibling.
- The approval target being Failed still yields the actionable resume error.
- `--wave` semantics are documented and test-pinned; approve tests green.
</success_criteria>

<output>
Create `.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-03-SUMMARY.md` when done.
</output>
