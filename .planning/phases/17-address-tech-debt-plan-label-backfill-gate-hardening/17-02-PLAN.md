---
phase: 17-address-tech-debt-plan-label-backfill-gate-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controller/milestone_controller.go
  - internal/controller/milestone_controller_test.go
  - internal/controller/phase_controller.go
  - internal/controller/phase_controller_test.go
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "When a rejected Project's planner Job completes, the phase completion handler parks the Phase Rejected WITHOUT spawning a new reporter Job"
    - "Same for the milestone completion handler: reject parks the Milestone before the reporter spawn / envelope read"
    - "In-flight reporter Jobs are never deleted by the fix — only a NEW spawn is prevented (D-05)"
  artifacts:
    - path: "internal/controller/phase_controller.go"
      provides: "gates.CheckRejected short-circuit relocated to the first statement after project resolution in handleJobCompletion, ahead of spawnReporterIfNeeded"
      contains: "patchPhaseRejected"
    - path: "internal/controller/milestone_controller.go"
      provides: "gates.CheckRejected short-circuit relocated ahead of the envelope read and reporter spawn in handleJobCompletion"
      contains: "patchMilestoneRejected"
  key_links:
    - from: "PhaseReconciler.handleJobCompletion"
      to: "patchPhaseRejected"
      via: "reject short-circuit before spawnReporterIfNeeded"
      pattern: "gates.CheckRejected"
    - from: "MilestoneReconciler.handleJobCompletion"
      to: "patchMilestoneRejected"
      via: "reject short-circuit before envelope read + spawnReporterIfNeeded"
      pattern: "gates.CheckRejected"
---

<objective>
Close DEBT-02 (WR-10): the reject short-circuit fires too late in the milestone and phase completion handlers.

In `PhaseReconciler.handleJobCompletion`, `spawnReporterIfNeeded` (:483) runs BEFORE the `gates.CheckRejected` short-circuit (:510) — so a rejected Project's completing planner Job still spawns a NEW reporter Job before the reject park fires. The milestone handler has the same ordering smell: its reject check (:515) sits after the envelope read (:534), though before its spawn (:556). The plan controller's 12-05 fix already moved its reject check to the FIRST statement after project resolution (plan_controller.go:470-471). This plan brings milestone + phase up to that template.

Purpose: A rejected Project must halt NEW dispatch at every level (D-05), without deleting in-flight Jobs.
Output: Reject short-circuit relocated in both handlers + net-new "no reporter Job created on reject" regression specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-RESEARCH.md
@.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-PATTERNS.md

<interfaces>
<!-- The 12-05 plan template and the per-level reject helpers. -->

internal/controller/plan_controller.go (the TEMPLATE — 12-05 reject-first):
- handlePlannerJobCompletion :458; reject short-circuit FIRST at :470-471, before envelope read :490 and reporter spawn

internal/controller/phase_controller.go:
- func (r *PhaseReconciler) handleJobCompletion(ctx, *Phase, completedJob) :446
  - project/projectUID derived ~:449-451
  - envelope read block ~:467
  - spawnReporterIfNeeded :483   ← reject currently AFTER this
  - current reject check :510-511 (relocate this 3-line block upward)
- func (r *PhaseReconciler) patchPhaseRejected(ctx, *Phase, reason) :677

internal/controller/milestone_controller.go:
- func (r *MilestoneReconciler) handleJobCompletion(ctx, *Milestone, completedJob) :495
  - projectUID derived ~:505-507
  - current reject check :515-516
  - envelope read :534
  - spawnReporterIfNeeded :556
- func (r *MilestoneReconciler) patchMilestoneRejected(ctx, *Milestone, reason) :764

gates.CheckRejected(project) bool · gates.RejectedReason(project) string
spawnReporterIfNeeded(ctx, client, scheme, obj, project, kind, image) (isFirstCompletion bool, err error)  // dispatch_helpers.go:67 — idempotent (AlreadyExists ok); DO NOT modify
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate the phase reject short-circuit ahead of the reporter spawn</name>
  <read_first>
    - internal/controller/phase_controller.go :446-512 (the full handleJobCompletion: project resolution :449-451, envelope read :467, spawnReporterIfNeeded :483, current reject :510-511) AND :677 (patchPhaseRejected)
    - internal/controller/plan_controller.go :458-472 (the 12-05 reject-first TEMPLATE)
    - internal/controller/phase_controller_test.go :270 (Describe scaffolding for envtest setup to mirror)
    - 17-PATTERNS.md Item 2 + Pitfall 3 (prevent a NEW spawn, NEVER delete an in-flight Job)
  </read_first>
  <behavior>
    - With a Project carrying the reject annotation (gates.CheckRejected true), driving PhaseReconciler.handleJobCompletion parks the Phase Rejected.
    - No `tide-reporter-<uid>` Job exists in the namespace after the reject park (the load-bearing assertion: assert NONE was created, NOT that one was deleted).
    - When the Project is NOT rejected, the handler still spawns the reporter as before (no regression).
  </behavior>
  <action>
    Move the existing `if project != nil && gates.CheckRejected(project) { return r.patchPhaseRejected(ctx, ph, gates.RejectedReason(project)) }` block from :510-511 to be the FIRST statement after `project`/`projectUID` are derived (right after ~:451, before the envelope-read block at :467). Delete the original block at :510-511. The early return must sit ABOVE `spawnReporterIfNeeded` (:483) so the spawn is never reached when rejected — that is the entire fix (Pitfall 3: do NOT add any Job deletion). Verbatim-copy the plan template (plan_controller.go:470-471), swapping `patchPlanRejected`→`patchPhaseRejected` and `plan`→`ph`. Re-read the handler fully before reordering: `spawnReporterIfNeeded` returns `(isFirstCompletion, err)` consumed by the budget-rollup guard below it — confirm relocating the reject block above the spawn does not strand a reference to `isFirstCompletion`. Add a net-new envtest spec to phase_controller_test.go (no existing reject-after-spawn spec — grep confirms): set up a Project with the reject annotation (grep existing `gates.CheckRejected` reject tests for the annotation-setup helper), drive handleJobCompletion, assert (a) Phase parked Rejected and (b) zero `tide-reporter-` Jobs in the namespace.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run PhaseReconciler -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: in phase_controller.go handleJobCompletion, the `gates.CheckRejected` line precedes the `spawnReporterIfNeeded` line. Verify: line number of `gates.CheckRejected(project)` inside handleJobCompletion (:446-560 range) is LESS than the line of `spawnReporterIfNeeded`.
    - Source: no `Delete(` call added near the reject path (Pitfall 3). Verify: the reject block is a relocation, not an addition of Job deletion.
    - Test: `go test ./internal/controller/... -run PhaseReconciler -count=1` exits 0; the new spec asserts zero `tide-reporter-` Jobs after reject.
  </acceptance_criteria>
  <done>Phase reject short-circuit fires before the reporter spawn; net-new spec proves no reporter Job is created on reject; PhaseReconciler tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relocate the milestone reject short-circuit ahead of the envelope read and reporter spawn</name>
  <read_first>
    - internal/controller/milestone_controller.go :495-560 (handleJobCompletion: projectUID :505-507, current reject :515-516, envelope read :534, spawnReporterIfNeeded :556) AND :764 (patchMilestoneRejected)
    - internal/controller/plan_controller.go :458-472 (the 12-05 reject-first TEMPLATE — reject FIRST, before BOTH read and spawn)
    - internal/controller/milestone_controller_test.go :436-488 (Describe scaffolding for envtest setup)
    - 17-PATTERNS.md Item 2 (milestone nuance: reject is already ahead of the spawn but AFTER the read; relocate above the read for 12-05 consistency)
  </read_first>
  <behavior>
    - With a Project carrying the reject annotation, driving MilestoneReconciler.handleJobCompletion parks the Milestone Rejected.
    - No `tide-reporter-<uid>` Job exists in the namespace after the reject park (assert NONE created).
    - Non-rejected path unchanged.
  </behavior>
  <action>
    Move the `if project != nil && gates.CheckRejected(project) { return r.patchMilestoneRejected(ctx, ms, gates.RejectedReason(project)) }` block from :515-516 to immediately after `projectUID` is derived (~:507), ahead of the envelope-read block at :534. Delete the original at :515-516. This matches the 12-05 template (reject is the FIRST thing after project resolution, ahead of BOTH the read and the spawn). The milestone reject is already ahead of `spawnReporterIfNeeded` (:556), so the "no new reporter Job" behavior is preserved; the relocation is for consistency and to avoid an unnecessary envelope read on a rejected milestone. Re-read the handler fully first — confirm nothing between :507 and :534 (e.g. projectUID, out-var declarations) is referenced by the relocated block beyond `project`. Pitfall 3: relocation only, no Job deletion. Add a net-new envtest spec to milestone_controller_test.go mirroring the phase spec: reject-annotated Project → drive handleJobCompletion → assert Milestone parked Rejected AND zero `tide-reporter-` Jobs.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run MilestoneReconciler -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: in milestone_controller.go handleJobCompletion, the `gates.CheckRejected` line precedes BOTH the `ReadOut` envelope-read line and the `spawnReporterIfNeeded` line. Verify: line of `gates.CheckRejected(project)` (within :495-560) < line of `r.EnvReader.ReadOut` < / and < line of `spawnReporterIfNeeded`.
    - Source: no Job `Delete(` added on the reject path.
    - Test: `go test ./internal/controller/... -run MilestoneReconciler -count=1` exits 0; new spec asserts zero `tide-reporter-` Jobs after reject.
  </acceptance_criteria>
  <done>Milestone reject short-circuit precedes the envelope read and reporter spawn; net-new spec proves no reporter Job on reject; MilestoneReconciler tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator reject annotation → reconciler dispatch | `tide reject` writes an annotation that the completion handlers read to decide whether to halt NEW Job dispatch (spend control) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Elevation (spend) | Reject is honored too late → a NEW reporter Job spawns under a rejected Project, burning a session past the operator's stop | mitigate | Relocate `gates.CheckRejected` ahead of `spawnReporterIfNeeded` in both handlers; regression spec asserts zero `tide-reporter-` Jobs created on reject. |
| T-17-05 | Denial of Service | Over-correction deletes in-flight reporter Jobs, dropping work the operator wanted to drain | mitigate | Fix is reordering only (D-05: in-flight Jobs drain). Acceptance asserts NONE created, never asserts deletion; no `Delete(` added on the reject path. |
| T-17-06 | Tampering | Relocated block strands a reference (e.g. `isFirstCompletion`) causing a compile/logic break that silently skips the gate | mitigate | Re-read both handlers fully before reordering; `go test` per handler must exit 0 (compile + behavior gate). |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `go test ./internal/controller/... -run 'PhaseReconciler|MilestoneReconciler' -count=1` exits 0.
- In both handlers, `gates.CheckRejected` precedes `spawnReporterIfNeeded` (line-order grep within each handleJobCompletion range).
- No `Delete(` introduced on either reject path.
</verification>

<success_criteria>
- Phase + milestone completion handlers short-circuit on reject before spawning a reporter Job.
- Net-new regression specs assert no reporter Job is created on reject (and none deleted).
- PhaseReconciler + MilestoneReconciler tests green.
</success_criteria>

<output>
Create `.planning/phases/17-address-tech-debt-plan-label-backfill-gate-hardening/17-02-SUMMARY.md` when done.
</output>
