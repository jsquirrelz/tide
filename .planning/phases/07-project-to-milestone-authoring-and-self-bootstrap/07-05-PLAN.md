---
phase: 07-project-to-milestone-authoring-and-self-bootstrap
plan: 05
type: execute
wave: 3
depends_on:
  - "07-03"
  - "07-04"
files_modified:
  - internal/controller/project_controller.go
  - cmd/manager/main.go
autonomous: true
requirements:
  - REQ-1
  - REQ-2
  - REQ-4

must_haves:
  truths:
    - "ProjectReconciler struct has 5 new fields: EnvReader, SigningKey, SubagentImage, CredproxyImage, HelmProviderDefaults"
    - "cmd/manager/main.go injects the 5 new fields (same values already wired for MilestoneReconciler)"
    - "reconcileProjectPlannerDispatch dispatches tide-project-<uid>-1 Job after Initialized; patches Status.Phase=Running"
    - "handleProjectJobCompletion reads EnvelopeOut and calls MaterializeChildCRDs to create the Milestone CR"
    - "checkProjectComplete patches Status.Phase=Complete when BoundaryDetected(project, Milestone) is true"
    - "checkProjectComplete is the first step of reconcilePhase3Lifecycle"
    - "cascade-13 idempotency guard is preserved — no re-dispatch after Running/Complete"
  artifacts:
    - path: "internal/controller/project_controller.go"
      provides: "reconcileProjectPlannerDispatch + handleProjectJobCompletion + checkProjectComplete"
      contains: "func (r \\*ProjectReconciler) reconcileProjectPlannerDispatch\nfunc (r \\*ProjectReconciler) handleProjectJobCompletion\nfunc (r \\*ProjectReconciler) checkProjectComplete"
    - path: "cmd/manager/main.go"
      provides: "5 new ProjectReconciler fields wired with values from MilestoneReconciler block"
      contains: "EnvReader.*envReader\|SigningKey.*signingKey"
  key_links:
    - from: "project_controller.go reconcilePhase3Lifecycle"
      to: "checkProjectComplete"
      via: "first call in reconcilePhase3Lifecycle before any other logic"
      pattern: "checkProjectComplete"
    - from: "project_controller.go reconcilePhase3Lifecycle"
      to: "reconcileProjectPlannerDispatch"
      via: "called after checkProjectComplete returns false"
      pattern: "reconcileProjectPlannerDispatch"
    - from: "project_controller.go handleProjectJobCompletion"
      to: "MaterializeChildCRDs"
      via: "called when planner Job is terminal"
      pattern: "MaterializeChildCRDs"
    - from: "cmd/manager/main.go ProjectReconciler block"
      to: "envReader, signingKey, subagentImage, credproxyImage, helmProviderDefaults"
      via: "struct literal fields"
      pattern: "EnvReader.*envReader"
---

<objective>
Wire the ProjectReconciler as the 5th D-A2 dispatch site: add the 5 missing struct fields, the project-level planner dispatch + job completion handler, and the Complete-detection method.

Purpose: This is the core of cascade-7 closure. After this plan, applying a bare Project will dispatch a tide-project-<uid>-1 planner Job, read the EnvelopeOut to create a Milestone CR, and then (once Milestone=Succeeded) patch Project.Status.Phase=Complete. This plan does NOT touch any other reconciler; only project_controller.go and cmd/manager/main.go are modified.

Output: Two modified files. project_controller.go gains 3 new methods (reconcileProjectPlannerDispatch, handleProjectJobCompletion, checkProjectComplete) and 5 new struct fields. cmd/manager/main.go wires those fields. The Layer B spec created in Plan 07-02 should now turn GREEN after this plan + Plans 07-03 + 07-04 are all executed.
</objective>

<execution_context>
@/Users/justinsearles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsearles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-SPEC.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-CONTEXT.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-RESEARCH.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-03-SUMMARY.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-04-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From internal/controller/milestone_controller.go (the pattern to mirror):
  // reconcilePlannerDispatch (lines 184-337): the proven pattern.
  // Key steps:
  //   1. Terminal short-circuit: Succeeded/Failed → return
  //   2. AwaitingApproval: NOT needed for Project (D-02: no new gate at Project level)
  //   3. Running: get jobName, fetch Job, if terminal call handleJobCompletion, else return
  //   4. Acquire PlannerPool (D-A4)
  //   5. Resolve project (for ProjectReconciler: project IS the reconciled object)
  //   6. Build planner envelope via BuildPlannerEnvelope("project", project, project, ...)
  //   7. Mint signed token via credproxy.Sign(r.SigningKey, string(project.UID), ttl)
  //   8. Build + Create Job via podjob.BuildJobSpec(opts) + owner.EnsureOwnerRef
  //   9. Patch Status.Phase=Running + Condition AuthoringPlanner=True
  //
  // Job name format (D-05): "tide-project-<uid>-1"
  // Level string: "project"
  // PVCName: "tide-projects" (hardcoded, same as all other reconcilers)

From internal/controller/dispatch_helpers.go:
  func BuildPlannerEnvelope(level string, parent metav1.Object, project *tideprojectv1alpha1.Project,
      attempt int, token string, caps pkgdispatch.Caps, proxyEndpoint string,
      helmDefaults ProviderDefaults) (pkgdispatch.EnvelopeIn, []byte, error)
  // For ProjectReconciler: level="project", parent=project, project=project (same object)
  // After Plan 07-03: parentName is auto-injected into Provider.Params via BuildPlannerEnvelope

  func MaterializeChildCRDs(ctx context.Context, c client.Client, scheme *runtime.Scheme,
      parent metav1.Object, children []pkgdispatch.ChildCRDSpec) error
  // Milestone is in the allowlist (dispatch_helpers.go:80 confirmed by research)

From internal/gates (boundary.go):
  func BoundaryDetected(ctx context.Context, c client.Client, parent metav1.Object, childKind string) (bool, error)
  // childKind="Milestone": lists Milestones owned by project in project.Namespace
  // Returns false if 0 Milestones (child-less guard — prevents premature Complete)
  // Returns true only when ALL owned Milestones have Status.Phase=="Succeeded"

From internal/dispatch/podjob:
  func BuildJobSpec(opts BuildOptions) *batchv1.Job
  const JobKindPlanner
  func DefaultCaps(caps interface{}, kind JobKind) Caps
  const DefaultWallClockGraceSeconds int  // used for token TTL calculation

From internal/credproxy:
  func Sign(signingKey []byte, subject string, ttl time.Duration) (string, error)

From internal/owner:
  func EnsureOwnerRef(obj client.Object, owner metav1.Object, scheme *runtime.Scheme) error

From internal/controller/project_controller.go (current state):
  // ProjectReconciler struct (lines 125-157): currently has Client, Scheme,
  //   MaxConcurrentReconciles, PlannerPool, ExecutorPool, Dispatcher,
  //   WatchNamespace, SharedPVCName, TidePushImage, Recorder.
  // MISSING (add these 5 fields):
  //   EnvReader      podjob.EnvelopeReader
  //   SigningKey      []byte
  //   SubagentImage   string
  //   CredproxyImage  string
  //   HelmProviderDefaults ProviderDefaults

  // reconcilePhase3Lifecycle (lines 381+):
  // CURRENT FIRST STEP: Branch-name init (project.Status.Git.BranchName == "")
  // INSERTION: checkProjectComplete MUST be called BEFORE branch-name init
  //   If complete, return immediately. This prevents the branch-name init
  //   from running on a Complete project (harmless but unnecessary).
  //
  // AFTER checkProjectComplete, BEFORE branch-name init:
  //   call r.reconcileProjectPlannerDispatch(ctx, project)
  //   if result.Requeue || err != nil { return result, err }
  //
  // RATIONALE: Planner dispatch slots into reconcilePhase3Lifecycle rather than
  //   reconcileProjectPhase2, because reconcilePhase3Lifecycle is the function
  //   that runs when Status.Phase is Initialized/Running/Complete/PushLeaseFailed
  //   (lines 272-277 of current project_controller.go). Both Initialized and
  //   Running route here, which gives us the correct entry condition for the
  //   dispatch state machine.

  // handleInitJobCompletion (lines 297-380):
  // The cascade-13 idempotency guard (lines 308-316) already short-circuits
  // if Phase is Running, Complete, PushLeaseFailed, PushLeakBlocked — it
  // does NOT re-patch Initialized. So:
  //   - On first reconcile after init Job: Phase=Initialized → passes guard →
  //     falls through to reconcilePhase3Lifecycle → reconcileProjectPlannerDispatch
  //     dispatches planner → patches Phase=Running
  //   - On subsequent reconciles: Phase=Running → cascade-13 guard returns early →
  //     reconcilePhase3Lifecycle still fires (line 273 includes PhaseRunning) →
  //     reconcileProjectPlannerDispatch Running-check fetches the Job and calls
  //     handleProjectJobCompletion when terminal

From cmd/manager/main.go (lines 326-343 for current ProjectReconciler, lines 345-367 for MilestoneReconciler):
  // Values already computed before the reconciler-registration block:
  //   envReader        *podjob.PodStatusEnvelopeReader   (line ~305)
  //   signingKey       []byte                            (line ~310)
  //   subagentImage    string                            (line ~315)
  //   credproxyImage   string                            (line ~318)
  //   helmProviderDefaults ProviderDefaults              (line ~322)
  // Wire these into ProjectReconciler exactly as MilestoneReconciler is wired.

API types:
  tideprojectv1alpha1.PhaseRunning    = "Running"
  tideprojectv1alpha1.PhaseComplete   = "Complete"
  tideprojectv1alpha1.PhaseInitialized = "Initialized"
  tideprojectv1alpha1.ConditionAuthoringPlanner  // verify constant name in shared_types.go
  tideprojectv1alpha1.ConditionSucceeded         // verify constant name in shared_types.go
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 struct fields to ProjectReconciler + 3 new methods in project_controller.go</name>
  <files>internal/controller/project_controller.go</files>
  <action>
    Read internal/controller/project_controller.go fully first. Read internal/controller/milestone_controller.go lines 184-458 second (the proven pattern). Read internal/controller/dispatch_helpers.go third (BuildPlannerEnvelope, MaterializeChildCRDs). Read api/v1alpha1/shared_types.go to confirm condition type constant names.

    Step A — Add 5 fields to ProjectReconciler struct after the existing TidePushImage field:

      EnvReader             podjob.EnvelopeReader
      SigningKey             []byte
      SubagentImage          string
      CredproxyImage         string
      HelmProviderDefaults   ProviderDefaults

    Add a comment block above them: "// Phase 7 (D-06): dispatch deps for project-level planner Job (mirrors MilestoneReconciler)."

    Ensure the necessary imports are present: internal/dispatch/podjob (for podjob.EnvelopeReader and podjob.BuildJobSpec), internal/credproxy, internal/gates. Some may already be imported — check before adding.

    Step B — Add checkProjectComplete method:

      func (r *ProjectReconciler) checkProjectComplete(ctx context.Context, project *tideprojectv1alpha1.Project) (bool, error)

    Implementation: call gates.BoundaryDetected(ctx, r.Client, project, "Milestone"). If error, return false, err. If not detected, return false, nil. If detected: create MergeFrom patch pair, set project.Status.Phase = tideprojectv1alpha1.PhaseComplete, set ConditionSucceeded condition (True, Reason="MilestonesSucceeded", Message="All owned Milestones reached Succeeded"), call r.Status().Patch, return true, err.

    Step C — Add reconcileProjectPlannerDispatch method (mirror of milestone_controller.go:184-337):

      func (r *ProjectReconciler) reconcileProjectPlannerDispatch(ctx context.Context, project *tideprojectv1alpha1.Project) (ctrl.Result, error)

    Terminal short-circuit: if project.Status.Phase is PhaseComplete or "Failed" or "InitFailed" (check which terminal phases Project uses — read project_controller.go), return ctrl.Result{}, nil.

    Running check: jobName := fmt.Sprintf("tide-project-%s-1", project.UID). If project.Status.Phase == PhaseRunning, fetch the Job; if not found, return; if terminal, call r.handleProjectJobCompletion(ctx, project, &job); else return.

    Acquire PlannerPool (if r.PlannerPool != nil, Acquire + defer Release).

    Build caps: plannerCaps := podjob.DefaultCaps(nil, podjob.JobKindPlanner); if plannerCaps.Iterations <= 0, plannerCaps.Iterations = 20.

    Build envelope: BuildPlannerEnvelope("project", project, project, 1, "", pkgdispatch.Caps{WallClockSeconds: int(plannerCaps.WallClockSeconds), Iterations: int(plannerCaps.Iterations)}, "https://127.0.0.1:8443", r.HelmProviderDefaults). For the Project level, project is both parent and project parameter — this is correct (the Project IS the parent).

    Mint token: credproxy.Sign(r.SigningKey, string(project.UID), time.Duration(plannerCaps.WallClockSeconds+podjob.DefaultWallClockGraceSeconds)*time.Second).

    Resolve secretUID: if project.Spec.ProviderSecretRef != "", fetch Secret and use its UID. Tolerate error (the $0 path has no providerSecretRef).

    Resolve SubagentImage: subagentImg := r.SubagentImage; if subagentImg == "", subagentImg = r.HelmProviderDefaults.Image.

    Build opts: podjob.BuildOptions{Kind: podjob.JobKindPlanner, ParentObj: project, Level: "project", Attempt: 1, Project: project, SignedToken: token, EnvelopeInJSON: envInJSON, SubagentImage: subagentImg, CredproxyImage: r.CredproxyImage, SecretUID: secretUID, PVCName: "tide-projects", ProjectUID: string(project.UID), Caps: plannerCaps}.

    Create Job: job := podjob.BuildJobSpec(opts); owner.EnsureOwnerRef(job, project, r.Scheme); r.Create(ctx, job) — treat AlreadyExists as success.

    Patch Status.Phase=Running + ConditionAuthoringPlanner=True.

    Step D — Add handleProjectJobCompletion method (mirror of milestone_controller.go:339-432):

      func (r *ProjectReconciler) handleProjectJobCompletion(ctx context.Context, project *tideprojectv1alpha1.Project, _ *batchv1.Job) (ctrl.Result, error)

    Tolerate nil EnvReader (log and skip envelope read, leave ChildCRDs empty — matches milestone pattern).

    If EnvReader is not nil: r.EnvReader.ReadOut(ctx, string(project.UID), string(project.UID)). Use project.UID as both projectUID and parentUID (the project IS the top-level; its own UID indexes the envelope). NOTE: ReadOut signature is ReadOut(ctx, projectUID, parentUID string) — check the podjob.EnvelopeReader interface to confirm parameter order, then use the correct values.

    Call MaterializeChildCRDs if len(envOut.ChildCRDs) > 0.

    Apply gate policy check: gates.EvaluatePolicy(project.Spec.Gates, "project") — but per D-02 there is no gate at the Project level. Pass through with auto policy. Do not add an AwaitingApproval state for the project level.

    Return ctrl.Result{}, nil (the Owns(&Milestone{}) watch re-enqueues the Project when the child Milestone status changes, which triggers checkProjectComplete to fire).

    Step E — Wire checkProjectComplete + reconcileProjectPlannerDispatch into reconcilePhase3Lifecycle:

    In reconcilePhase3Lifecycle, at the VERY TOP of the function body (before the existing branch-name init block), add:

      // Step 0: Check if all owned Milestones have Succeeded → Complete.
      if complete, err := r.checkProjectComplete(ctx, project); err != nil || complete {
          return ctrl.Result{}, err
      }

    Immediately after, add:

      // Step 0b: Dispatch project-level planner Job (D-A2 5th dispatch site).
      if result, err := r.reconcileProjectPlannerDispatch(ctx, project); err != nil || result.Requeue || result.RequeueAfter > 0 {
          return result, err
      }

    These two inserts come BEFORE the existing "Step 1: Branch-name init" block.

    CRITICAL: Read the existing code carefully to preserve existing formatting. Do not move or delete any existing steps in reconcilePhase3Lifecycle — only insert at the top.

    Also add the RBAC marker for Milestone reads to the ProjectReconciler's marker block (the reconciler now creates Milestone objects — it needs create+get+list+watch verbs on milestones):
      // +kubebuilder:rbac:groups=tideproject.k8s,resources=milestones,verbs=get;list;watch;create
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./internal/controller/... 2>&1 | tail -20</automated>
  </verify>
  <done>go test ./internal/controller/... exits 0 (no regressions); grep -c "func (r \*ProjectReconciler) reconcileProjectPlannerDispatch" internal/controller/project_controller.go returns 1; grep -c "func (r \*ProjectReconciler) handleProjectJobCompletion" internal/controller/project_controller.go returns 1; grep -c "func (r \*ProjectReconciler) checkProjectComplete" internal/controller/project_controller.go returns 1; grep -c "EnvReader.*podjob.EnvelopeReader\|podjob.EnvelopeReader.*EnvReader" internal/controller/project_controller.go returns >= 1</done>
</task>

<task type="auto">
  <name>Task 2: Wire 5 new ProjectReconciler fields in cmd/manager/main.go</name>
  <files>cmd/manager/main.go</files>
  <action>
    Read cmd/manager/main.go fully first. Locate the ProjectReconciler registration block (currently lines 326-343 per research). Locate the MilestoneReconciler registration block (currently lines 345-367) to see the exact field names and variable names used.

    In the ProjectReconciler struct literal, add the 5 new fields after the existing fields:

      EnvReader:            envReader,
      SigningKey:            signingKey,
      SubagentImage:        subagentImage,
      CredproxyImage:       credproxyImage,
      HelmProviderDefaults: helmProviderDefaults,

    The variable names envReader, signingKey, subagentImage, credproxyImage, helmProviderDefaults must already exist above the registration block (per research: computed at lines ~305-322). Read those lines to confirm variable names before writing — do not assume. Use the exact variable names found in the file.

    Do not add any new variable declarations — only reference variables already computed in main.go. If a variable name differs from what research assumed, use the actual name.

    No other changes to main.go are required. The Dispatcher field and other existing fields remain unchanged.

    After editing, run go build ./cmd/manager/... to confirm compilation before running tests.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./cmd/manager/... 2>&1 | head -10 && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <done>go build ./cmd/manager/... exits 0; go test ./... exits 0 (no regressions across all packages); grep -c "EnvReader.*envReader\|EnvReader:.*envReader" cmd/manager/main.go returns >= 1 in the ProjectReconciler block</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProjectReconciler → K8s API (Job create) | The new reconcileProjectPlannerDispatch creates a planner Job. Job is owner-ref'd to the Project (Controller=true, BlockOwnerDeletion=true) via owner.EnsureOwnerRef. |
| project planner Job pod → credproxy | The Job's credproxy sidecar authenticates with the HMAC token minted by credproxy.Sign(r.SigningKey, string(project.UID), ttl). Same contract as MilestoneReconciler. |
| planner Job EnvelopeOut → MaterializeChildCRDs | T-308 allowlist in MaterializeChildCRDs gates which Kinds can be created. Milestone is in the allowlist. Stub emits only Milestone at project level. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | T-308 allowlist — new project→Milestone path | mitigate | Milestone is already in childKindAllowlist (dispatch_helpers.go:80 per research). No new allowlist entry required. The allowlist enforces the only channel from a zero-K8s-verb subagent pod into typed CRDs. Verify: grep -n "\"Milestone\"" internal/controller/dispatch_helpers.go returns a line in the allowlist map init. |
| T-07-05-02 | Elevation of Privilege | credproxy signed token for project planner Job | mitigate | Token TTL = plannerCaps.WallClockSeconds + DefaultWallClockGraceSeconds (same formula as MilestoneReconciler line 274). Subject = string(project.UID). Same credproxy.Sign contract. TTL is bounded; token is per-dispatch-unique by UID+attempt. |
| T-07-05-03 | Information Disclosure | subagent pod RBAC | mitigate | Subagent pods have zero K8s verbs (tide-subagent SA has no ClusterRole bindings). The stub reads the EnvelopeIn from the termination message path only; it writes to the termination message. No K8s API calls from within the pod. This is unchanged from Phase 3 design. |
| T-07-05-04 | Spoofing | EnvelopeOut from planner Job | accept | EnvelopeOut is read from the pod's termination message via PodStatusEnvelopeReader. Only the Job's pod writes the termination message. Owner-ref enforced by K8s GC — orphaned pods cannot write to a different Job's termination slot. No new surface vs existing milestone planner. |
| T-07-05-05 | Denial of Service | Project planner Job TTL | accept | plannerCaps.WallClockSeconds = 600s floor (same as Milestone). activeDeadlineSeconds enforced at Job level by podjob.BuildJobSpec. K8s terminates the pod at deadline. |
</threat_model>

<verification>
After both tasks complete:
1. go build ./... exits 0
2. go test ./... exits 0 (no regressions in any package)
3. grep -c "func (r \*ProjectReconciler) reconcileProjectPlannerDispatch" internal/controller/project_controller.go returns 1
4. grep -c "func (r \*ProjectReconciler) handleProjectJobCompletion" internal/controller/project_controller.go returns 1
5. grep -c "func (r \*ProjectReconciler) checkProjectComplete" internal/controller/project_controller.go returns 1
6. grep -c "tide-project-.*-1" internal/controller/project_controller.go returns >= 1 (Job name format)
7. grep -c "EnvReader" cmd/manager/main.go | grep ProjectReconciler context returns >= 1
8. grep -c "BoundaryDetected.*Milestone\|Milestone.*BoundaryDetected" internal/controller/project_controller.go returns >= 1
9. grep -c "checkProjectComplete" internal/controller/project_controller.go returns >= 2 (definition + call site)
</verification>

<success_criteria>
ProjectReconciler is now the 5th D-A2 dispatch site. A bare Project will dispatch a tide-project-<uid>-1 planner Job, create a Milestone CR from the EnvelopeOut, and reach Project.Status.Phase=Complete when the Milestone reaches Succeeded. Manager wiring injects all 5 new fields. No regression in any existing test. Files committed to git. The Layer B spec (Plan 07-02) should now pass GREEN under make test-int.
</success_criteria>

<output>
After completion, create .planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-05-SUMMARY.md
</output>
