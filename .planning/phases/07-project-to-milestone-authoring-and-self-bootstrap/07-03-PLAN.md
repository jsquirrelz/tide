---
phase: 07-project-to-milestone-authoring-and-self-bootstrap
plan: 03
type: execute
wave: 1
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - cmd/stub-subagent/main.go
  - internal/controller/dispatch_helpers.go
autonomous: true
requirements:
  - REQ-3

must_haves:
  truths:
    - "Stub emits exactly 1 ChildCRD of the correct Kind for each planner level (project/milestone/phase/plan)"
    - "Stub emits 0 ChildCRDs for task level (existing executor path unchanged)"
    - "Each emitted ChildCRD carries the required parent *Ref field derived from Provider.Params[parentName]"
    - "Canned Task child carries filesTouched, declaredOutputPaths, and Dev.TestMode=success"
    - "BuildPlannerEnvelope injects parentName in Provider.Params for all dispatch sites"
    - "Unit tests in planner_test.go turn GREEN"
  artifacts:
    - path: "cmd/stub-subagent/main.go"
      provides: "dispatchPlannerSuccess function with Level switch + parentName from Params"
      contains: "func dispatchPlannerSuccess"
    - path: "internal/controller/dispatch_helpers.go"
      provides: "parentName injected in Provider.Params in BuildPlannerEnvelope"
      contains: "parentName"
  key_links:
    - from: "cmd/stub-subagent/main.go dispatchPlannerSuccess"
      to: "pkg/dispatch.EnvelopeOut.ChildCRDs"
      via: "json.Marshal + writeEnvelope"
      pattern: "ChildCRDs.*append|dispatchPlannerSuccess"
    - from: "internal/controller/dispatch_helpers.go BuildPlannerEnvelope"
      to: "pkg/dispatch.ProviderSpec.Params"
      via: "Provider.Params[\"parentName\"] = parent.GetName()"
      pattern: "parentName.*GetName|Params.*parentName"
---

<objective>
Implement stub planner-mode canned ChildCRD emission (REQ-3) and inject parentName into BuildPlannerEnvelope.

Purpose: The stub must emit typed ChildCRDs so that MaterializeChildCRDs can create real child CRs when the Project/Milestone/Phase/Plan reconciler Jobs complete. Without this, the cascade stalls at every planner Job — no children are authored. This plan turns the Wave 0 unit tests GREEN and enables the Layer B spec to observe real child CRs materializing.

Output: Two modified files — cmd/stub-subagent/main.go gains dispatchPlannerSuccess branching on env.Level, and internal/controller/dispatch_helpers.go gains parentName injection in BuildPlannerEnvelope. Unit tests in planner_test.go must turn GREEN after this plan.
</objective>

<execution_context>
@/Users/justinsearles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsearles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-SPEC.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-CONTEXT.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-RESEARCH.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-01-SUMMARY.md
@/Users/justinsearles/Projects/tide/.planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From pkg/dispatch/envelope.go:
  type EnvelopeIn struct {
      Role     string      `json:"role"`    // "planner" or "executor"
      Level    string      `json:"level"`   // "project"|"milestone"|"phase"|"plan"|"task"
      TaskUID  string      `json:"taskUID"` // UID of the dispatching CRD
      Provider ProviderSpec
  }
  type ProviderSpec struct {
      Vendor string
      Model  string
      Params map[string]string  // parentName injected here by BuildPlannerEnvelope
  }

From pkg/dispatch/childcrd.go:
  type ChildCRDSpec struct {
      Kind string                 `json:"kind"`
      Name string                 `json:"name"`
      Spec runtime.RawExtension   `json:"spec"`
  }

From pkg/dispatch/envelope.go (EnvelopeOut):
  type EnvelopeOut struct {
      APIVersion  string         `json:"apiVersion"`
      Kind        string         `json:"kind"`
      TaskUID     string         `json:"taskUID"`
      ExitCode    int            `json:"exitCode"`
      ChildCRDs   []ChildCRDSpec `json:"childCRDs,omitempty"`
      // ... other fields
  }
  const APIVersionV1Alpha1 = "tide.k8s/v1alpha1"
  const KindTaskEnvelopeOut = "TaskEnvelopeOut"

From cmd/stub-subagent/main.go:
  // Current dispatchSuccess (lines 204-244): handles testMode=="" or "success".
  // Does NOT branch on env.Role or env.Level — emits empty ChildCRDs.
  // The executor contract (DeclaredOutputPaths artifact write) MUST remain unchanged.
  //
  // dispatchPlannerSuccess is a NEW function to add in main.go:
  //   - Called from dispatchSuccess when env.Role == "planner"
  //   - Switches on env.Level, emits canned ChildCRDSpec per level
  //   - parentName := env.Provider.Params["parentName"]  (injected by BuildPlannerEnvelope)
  //
  // Insertion point in dispatchSuccess:
  //   At the TOP of dispatchSuccess, before the artifact-writing block:
  //   if env.Role == "planner" { return dispatchPlannerSuccess(ctx, env, outPath, stderr) }

From internal/controller/dispatch_helpers.go (function to modify):
  func BuildPlannerEnvelope(level string, parent metav1.Object, project *tideprojectv1alpha1.Project, attempt int, token string, caps pkgdispatch.Caps, proxyEndpoint string, helmDefaults ProviderDefaults) (pkgdispatch.EnvelopeIn, []byte, error) {
      envIn := pkgdispatch.EnvelopeIn{
          // ... existing fields ...
          Provider: ResolveProvider(project, level, helmDefaults),
      }
      // ADD AFTER Provider is assigned:
      // if envIn.Provider.Params == nil { envIn.Provider.Params = make(map[string]string) }
      // envIn.Provider.Params["parentName"] = parent.GetName()
      // ...
  }

CRD-admission minimum valid Specs (from 07-RESEARCH.md §CRD Admission Constraints):
  Milestone: {"projectRef": "<parentName>"}
  Phase:     {"milestoneRef": "<parentName>"}
  Plan:      {"phaseRef": "<parentName>"}
  Task:      {"planRef": "<parentName>", "filesTouched": ["stub-output.txt"], "declaredOutputPaths": ["/workspace/artifacts/stub"], "dependsOn": [], "dev": {"testMode": "success"}}
  NOTE: "Wave" must NOT be emitted — waves are derived by PlanReconciler (CLAUDE.md constraint)

Termination message size: A single ChildCRDSpec with minimal spec JSON is well under 4096 bytes (K8s limit). Canned tree stays small as required (D-08 constraint).

Deterministic child names (Claude's Discretion from 07-CONTEXT.md):
  "stub-milestone-1", "stub-phase-1", "stub-plan-1", "stub-task-1" — use these fixed names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dispatchPlannerSuccess to cmd/stub-subagent/main.go</name>
  <files>cmd/stub-subagent/main.go</files>
  <behavior>
    - When env.Role == "planner" and env.Level == "project": emits exactly 1 ChildCRD Kind="Milestone", Name="stub-milestone-1", Spec.Raw marshals to {"projectRef":"<parentName>"}
    - When env.Role == "planner" and env.Level == "milestone": emits Kind="Phase", Name="stub-phase-1", Spec.Raw={"milestoneRef":"<parentName>"}
    - When env.Role == "planner" and env.Level == "phase": emits Kind="Plan", Name="stub-plan-1", Spec.Raw={"phaseRef":"<parentName>"}
    - When env.Role == "planner" and env.Level == "plan": emits Kind="Task", Name="stub-task-1", Spec.Raw={"planRef":"<parentName>","filesTouched":["stub-output.txt"],"declaredOutputPaths":["/workspace/artifacts/stub"],"dependsOn":[],"dev":{"testMode":"success"}}
    - When env.Role == "planner" and env.Level == "task" (or any unknown level): falls through to existing executor dispatchSuccess path (no ChildCRDs — leaf)
    - When env.Role != "planner": existing executor path unchanged (no ChildCRDs, artifact write intact)
    - parentName := env.Provider.Params["parentName"]; if empty string, use "stub-parent" as fallback
    - EnvelopeOut.ExitCode == 0, EnvelopeOut.Result == "success", EnvelopeOut.ChildCRDs has expected count
  </behavior>
  <action>
    Read cmd/stub-subagent/main.go first. Read internal/controller/dispatch_helpers.go second.

    In cmd/stub-subagent/main.go, add a new function dispatchPlannerSuccess(ctx context.Context, env pkgdispatch.EnvelopeIn, outPath string, stderr io.Writer) int immediately after dispatchSuccess.

    The function reads parentName from env.Provider.Params["parentName"]. If the Params map is nil or the key is absent, fall back to "stub-parent".

    Use a switch on env.Level to build a []pkgdispatch.ChildCRDSpec slice. Each case marshals a minimal Go map into JSON bytes, wraps in runtime.RawExtension{Raw: rawBytes}, and appends a ChildCRDSpec{Kind: ..., Name: ..., Spec: ...}.

    Import k8s.io/apimachinery/pkg/runtime if not already imported. This is needed for runtime.RawExtension.

    The "task" case (and default): emit an empty ChildCRDs slice (the existing executor artifact-write path is NOT called from dispatchPlannerSuccess — just return an empty-ChildCRD envelope).

    Write the EnvelopeOut with ChildCRDs set, ExitCode=0, Result="success", Reason="planner stub success", TaskUID=env.TaskUID, APIVersion=pkgdispatch.APIVersionV1Alpha1, Kind=pkgdispatch.KindTaskEnvelopeOut, CompletedAt=time.Now().UTC().

    In dispatchSuccess, add a branch at the TOP of the function (BEFORE the artifact-writing block): if env.Role == "planner" { return dispatchPlannerSuccess(ctx, env, outPath, stderr) }. This must be the first statement after variable declarations.

    Do NOT remove or alter the existing artifact-writing block in dispatchSuccess — it remains for executor calls (env.Role != "planner").

    The function signature for dispatchPlannerSuccess must accept the same parameters as dispatchSuccess: (ctx context.Context, env pkgdispatch.EnvelopeIn, outPath string, stderr io.Writer) int.

    Ensure json.Marshal errors are handled (return 2 if marshal fails, write a best-effort failure envelope).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./cmd/stub-subagent/... -run "TestPlanner" -v 2>&1 | tail -30</automated>
  </verify>
  <done>go test ./cmd/stub-subagent/... -run TestPlanner exits 0; all TestPlannerProject/TestPlannerMilestone/TestPlannerPhase/TestPlannerPlan/TestPlannerTaskLeaf tests pass GREEN; grep -c "func dispatchPlannerSuccess" cmd/stub-subagent/main.go returns 1</done>
</task>

<task type="auto">
  <name>Task 2: Inject parentName in BuildPlannerEnvelope (dispatch_helpers.go)</name>
  <files>internal/controller/dispatch_helpers.go</files>
  <action>
    Read internal/controller/dispatch_helpers.go first. Read the full BuildPlannerEnvelope function (lines 149-177 per research).

    In BuildPlannerEnvelope, after the line that assigns Provider via ResolveProvider, add code to inject the parent object's name into Provider.Params["parentName"].

    The injection logic: if envIn.Provider.Params is nil, initialize it as make(map[string]string). Then set envIn.Provider.Params["parentName"] = parent.GetName().

    This must be done BEFORE the json.Marshal call so the injected key is serialized into the envelope JSON that the stub reads.

    The parameter parent metav1.Object is already the correct type — GetName() is available on it.

    No other changes to BuildPlannerEnvelope are needed. The function signature is unchanged.

    After this change, every existing caller of BuildPlannerEnvelope (MilestoneReconciler, PhaseReconciler, PlanReconciler) will automatically inject the correct parent name into Provider.Params. The ProjectReconciler (Plan 07-05) will also benefit when it calls BuildPlannerEnvelope("project", project, project, ...) — parent.GetName() returns the Project name.

    Verify that the existing tests that call BuildPlannerEnvelope still pass: the Params map is now non-nil (previously nil or absent from canned test envelopes) but that only adds a key, it doesn't break any existing assertions.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./internal/controller/... -run "TestBuildPlannerEnvelope|TestReconciler" 2>&1 | tail -20 && go test ./cmd/stub-subagent/... 2>&1 | tail -10</automated>
  </verify>
  <done>go test ./internal/controller/... exits 0 (no regressions); go test ./cmd/stub-subagent/... exits 0 (all stub tests including TestPlannerProject/Milestone/Phase/Plan pass GREEN); grep -c "parentName.*GetName\|GetName.*parentName" internal/controller/dispatch_helpers.go returns >= 1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub planner output → MaterializeChildCRDs | The stub emits ChildCRDSpec entries that MaterializeChildCRDs will server-side-create. The T-308 Kind allowlist in dispatch_helpers.go gates what Kinds are accepted (Milestone, Phase, Plan, Task, Wave only). |
| Provider.Params["parentName"] | Injected by the trusted reconciler via BuildPlannerEnvelope; read by the stub to populate child *Ref fields. This is not user-controlled input — it comes from parent.GetName() in the reconciler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | T-308 allowlist (MaterializeChildCRDs) | mitigate | Existing allowlist in dispatch_helpers.go:199-203 already gates Kind to {Milestone, Phase, Plan, Task, Wave}. Stub only emits those Kinds. No new allowlist entries required. Verify: grep -n "childKindAllowlist" internal/controller/dispatch_helpers.go confirms Milestone is present. |
| T-07-03-02 | Elevation of Privilege | stub ChildCRDs create K8s objects | mitigate | Stub runs as zero-K8s-verb container pod; it only writes to the termination message (EnvelopeOut). MaterializeChildCRDs runs in the manager (privileged reconciler) but is gated by the allowlist and the parent owner-ref enforced by owner.EnsureOwnerRef. No new privilege surface added. |
| T-07-03-03 | Information Disclosure | parentName in Provider.Params | accept | Provider.Params is serialized into the envelope JSON, which is written to the pod's termination message. The termination message is visible to the manager (intended). The parent CRD name is not a secret — it is already in the CRD's metadata. |
</threat_model>

<verification>
After both tasks complete:
1. go test ./cmd/stub-subagent/... exits 0 — all 6 TestPlanner* tests pass GREEN
2. go test ./internal/controller/... exits 0 — no regressions from parentName injection
3. go build ./... exits 0
4. grep -c "func dispatchPlannerSuccess" cmd/stub-subagent/main.go returns 1
5. grep -c "parentName" internal/controller/dispatch_helpers.go returns >= 1
6. grep -c "stub-milestone-1\|stub-phase-1\|stub-plan-1\|stub-task-1" cmd/stub-subagent/main.go returns 4
</verification>

<success_criteria>
Stub planner-mode ChildCRD emission implemented. All Wave 0 unit tests (TestPlannerProject/Milestone/Phase/Plan/TaskLeaf) pass GREEN. BuildPlannerEnvelope injects parentName for all existing dispatch sites. No regression in existing controller or stub tests. Files committed to git.
</success_criteria>

<output>
After completion, create .planning/phases/07-project-to-milestone-authoring-and-self-bootstrap/07-03-SUMMARY.md
</output>
