---
phase: 18-eval-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/eval/doc.go
  - internal/eval/render_test.go
  - internal/eval/testdata/goldie/project_planner.golden
  - internal/eval/testdata/goldie/milestone_planner.golden
  - internal/eval/testdata/goldie/phase_planner.golden
  - internal/eval/testdata/goldie/plan_planner.golden
  - internal/eval/testdata/goldie/task_executor.golden
  - internal/eval/testdata/ratchets/project_planner.txt
  - internal/eval/testdata/ratchets/milestone_planner.txt
  - internal/eval/testdata/ratchets/phase_planner.txt
  - internal/eval/testdata/ratchets/plan_planner.txt
  - internal/eval/testdata/ratchets/task_executor.txt
autonomous: true
requirements: [EVAL-01, EVAL-03, EVAL-06]

must_haves:
  truths:
    - "A maintainer runs `make test` and goldie snapshot tests confirm all five rendered templates match committed testdata/goldie/*.golden (zero-network)."
    - "D-01: A PR that grows any template's rendered byte count beyond the committed per-template ratchet ceiling fails `make test` automatically, no manual inspection (Phase 18 freezes ceilings at un-trimmed v1.0.1 counts)."
    - "D-02: The internal/eval package runs under the existing `make test` unit tier with no CI config change, no build tag, and no new `make test-unit` target."
  artifacts:
    - path: "internal/eval/doc.go"
      provides: "package eval doc comment + import-boundary statement"
      contains: "package eval"
    - path: "internal/eval/render_test.go"
      provides: "golden render + byte ratchet tests for all five templates"
      contains: "goldie.New"
    - path: "internal/eval/testdata/goldie/plan_planner.golden"
      provides: "frozen v1.0.1 golden render of plan_planner template"
    - path: "internal/eval/testdata/ratchets/plan_planner.txt"
      provides: "byte-count ceiling for plan_planner render"
  key_links:
    - from: "internal/eval/render_test.go"
      to: "internal/subagent/common.LoadPromptTemplate"
      via: "render each (role,level) pair then goldie.Assert"
      pattern: "common\\.LoadPromptTemplate"
    - from: "internal/eval/render_test.go"
      to: "testdata/ratchets/<name>.txt"
      via: "os.ReadFile + strconv.Atoi; fail if buf.Len() > ceiling"
      pattern: "testdata/ratchets"
---

<objective>
Establish the `internal/eval/` package and freeze the v1.0.1 baseline: goldie golden renders of all five prompt templates plus a per-template byte-count ratchet. This is the foundation wave — it creates the package directory (so wave 2's eval tests have a home) and the regression tripwire that catches accidental template growth on every PR.

Purpose: EVAL-01 (frozen baseline committed under testdata/), EVAL-03 (goldie snapshot of every template, zero-network, flags growth), EVAL-06 (auto-gates in CI because internal/eval/ runs under existing `make test`).
Output: `internal/eval/` package with doc.go + render_test.go, five `.golden` files, five `.txt` ratchet files, goldie/v2 added test-only to go.mod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-eval-harness/18-CONTEXT.md
@.planning/phases/18-eval-harness/18-RESEARCH.md
@.planning/phases/18-eval-harness/18-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From internal/subagent/common/prompt_templates.go:65 (verified):
```
func LoadPromptTemplate(role, level string) (*template.Template, error)
// name = fmt.Sprintf("templates/%s_%s.tmpl", level, role)
// returns wrapped fs.ErrNotExist if (role, level) has no template
```

The five (role, level) → goldie-name pairs (template files verified present):
- ("planner", "project")   → "project_planner"
- ("planner", "milestone") → "milestone_planner"
- ("planner", "phase")     → "phase_planner"
- ("planner", "plan")      → "plan_planner"
- ("executor", "task")     → "task_executor"

From pkg/dispatch/envelope.go:39 — EnvelopeIn fixture fields (set ALL to compile-time constants; leave Provider.Params nil to avoid map-iteration flap per RESEARCH Q1). Critical: TaskUID must be a fixed literal — it is interpolated in plan_planner.tmpl:29.

goldie/v2 API (Context7 /sebdah/goldie):
```
g := goldie.New(t, goldie.WithFixtureDir("testdata/goldie"))
g.Assert(t, "plan_planner", buf.Bytes())  // -update writes the .golden on first run
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add goldie test-only dep and create the eval package doc</name>
  <files>go.mod, go.sum, internal/eval/doc.go</files>
  <read_first>
    - go.mod (current module deps; module path github.com/jsquirrelz/tide; goldie absent — verified)
    - cmd/tide-lint/main.go (lines 1-48 — Apache 2.0 header + package doc-comment style, the doc.go analog)
    - .planning/phases/18-eval-harness/18-RESEARCH.md (Pitfall 6 + Q8 — the import boundary)
  </read_first>
  <action>
    Run `go get github.com/sebdah/goldie/v2@v2.8.0` to add it as a test-only dependency, then `go mod tidy` (it lands as indirect/test). Create `internal/eval/doc.go` with the Apache 2.0 header block (copy verbatim from cmd/tide-lint/main.go) followed by `package eval` and a doc comment that states: (1) eval is the quality + cost gate for template changes; (2) the deterministic tests (render_test.go, protocol_test.go, cost_replay_test.go) run under `make test` zero-network; (3) the online surface is cmd/tide-eval invoked by `make eval`; (4) the hard import boundary — internal/eval MUST NOT import internal/controller, internal/budget, internal/metrics, or api/v1alpha1 (per RESEARCH Q8/Pitfall 6, those would create an import cycle). Use byte-count (`len(rendered)`) as the offline ratchet proxy per D-01a and RESEARCH Q2 — note this choice in the doc comment.
  </action>
  <verify>
    <automated>grep -q "sebdah/goldie/v2" go.mod && go build ./internal/eval/... && grep -q "^package eval" internal/eval/doc.go</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sebdah/goldie/v2 v2.8" go.mod` succeeds (test-only dep pinned to v2.8.x)
    - `go build ./internal/eval/...` exits 0 (package compiles with only doc.go)
    - `internal/eval/doc.go` begins with the Apache 2.0 header and contains `package eval`
    - doc comment names all four forbidden imports (controller, budget, metrics, api/v1alpha1)
  </acceptance_criteria>
  <done>goldie/v2 v2.8.0 is a test-only go.mod entry; internal/eval package exists with doc.go; package compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden render + byte ratchet for all five templates</name>
  <files>internal/eval/render_test.go, internal/eval/testdata/goldie/*.golden, internal/eval/testdata/ratchets/*.txt</files>
  <read_first>
    - internal/subagent/common/prompt_templates.go (lines 60-80 — LoadPromptTemplate contract)
    - internal/subagent/anthropic/stream_parser_test.go (lines 1-58 — Apache header, package decl, t.Fatalf-for-setup / t.Errorf-for-assert convention)
    - pkg/dispatch/envelope.go (lines 39-100 — EnvelopeIn struct fields, to build the fixed fixture)
    - internal/subagent/common/templates/plan_planner.tmpl (confirm which EnvelopeIn fields it interpolates — esp. TaskUID — so the fixture sets them to fixed literals)
    - .planning/phases/18-eval-harness/18-PATTERNS.md (render_test.go section + Pitfall 2 fixed-fixture rule)
  </read_first>
  <behavior>
    - For each of the five (role,level) pairs: LoadPromptTemplate + tmpl.Execute(&buf, fixedEnvelope) succeeds, and goldie g.Assert matches the committed .golden.
    - For each template: rendered buf.Len() <= the integer in testdata/ratchets/<name>.txt; a render exceeding the ceiling fails with a clear t.Errorf naming the file to update.
    - A missing ratchet file fails with t.Fatalf instructing the maintainer to create it (not a silent pass).
    - Goldens are byte-identical across repeated `make test` runs (fixture is a package-level var of compile-time constants; Provider.Params nil).
  </behavior>
  <action>
    Create `internal/eval/render_test.go` (package eval, Apache header). Define a package-level `var fixedEnvelope pkgdispatch.EnvelopeIn` with every field set to a compile-time string literal (TaskUID="eval-fixture-uid-000", Provider.Vendor="anthropic", Provider.Model="claude-sonnet-4-6", DeclaredOutputPaths non-empty, Provider.Params left nil) per PATTERNS render_test.go section and Pitfall 2. Add one `TestGoldenRender_<name>` per template that loads via common.LoadPromptTemplate, executes into a bytes.Buffer, and calls `goldie.New(t, goldie.WithFixtureDir("testdata/goldie")).Assert(t, "<name>", buf.Bytes())`. Add one `TestByteRatchet_<name>` per template that re-renders, reads `testdata/ratchets/<name>.txt` via os.ReadFile + strconv.Atoi(strings.TrimSpace(...)), t.Fatalf on missing/malformed file, and t.Errorf if buf.Len() exceeds the ceiling — the error message must name the ratchet file and say "update if growth is intentional". Generate the goldens by running `go test ./internal/eval/... -update` once, then write each ratchet .txt with the freshly-rendered buf.Len() value (single integer + newline). Commit all five goldens and all five ratchet files. Do NOT use goldie.AssertJson (output is plain text, not JSON — RESEARCH anti-pattern).
  </action>
  <verify>
    <automated>go test ./internal/eval/... && ls internal/eval/testdata/goldie/*.golden | wc -l | grep -q 5 && ls internal/eval/testdata/ratchets/*.txt | wc -l | grep -q 5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/eval/...` exits 0 (all golden + ratchet tests pass against committed files)
    - Exactly five `.golden` files exist under internal/eval/testdata/goldie/
    - Exactly five `.txt` ratchet files exist, each containing a single integer
    - Re-running `go test ./internal/eval/...` twice produces no golden diff (determinism)
    - Temporarily appending a byte to any rendered output (e.g. via a throwaway fixture tweak) makes the corresponding TestByteRatchet fail — confirms the tripwire is live (revert after checking)
    - `grep -q "WithFixtureDir(\"testdata/goldie\")" internal/eval/render_test.go`
  </acceptance_criteria>
  <done>Five goldie goldens and five byte-ratchet ceilings are committed; `go test ./internal/eval/...` is green; the ratchet trips on growth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan is test-only + a doc file. No network, no credentials, no user-supplied input — fixtures are compile-time constants and `make test` is zero-network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | testdata/goldie/*.golden committed bytes | accept | Goldens are reviewed artifacts (same discipline as code); a malicious golden only weakens the local test, no runtime/prod surface. Low value target. |
| T-18-SC | Tampering | `go get github.com/sebdah/goldie/v2@v2.8.0` | mitigate | Package legitimacy approved in RESEARCH Package Legitimacy Audit (well-established, STACK.md-cited, High source reputation). Pinned to v2.8.0; test-only scope; `go mod tidy` + `git diff --exit-code go.mod go.sum` in CI catches drift. |
</threat_model>

<verification>
- `make test` runs internal/eval/ as part of the unit tier (no CI change) — confirms EVAL-06 auto-gating.
- `go test ./internal/eval/...` green: all goldens match, all ratchets hold.
- `git diff --exit-code go.mod go.sum` clean after `go mod tidy` (CI parity).
</verification>

<success_criteria>
- All five templates have committed goldie goldens and byte-ratchet ceilings.
- `go test ./internal/eval/...` and `make test` are green.
- goldie/v2 v2.8.0 is a test-only dependency; no production dep added.
- internal/eval imports none of: internal/controller, internal/budget, internal/metrics, api/v1alpha1.
</success_criteria>

<output>
Create `.planning/phases/18-eval-harness/18-01-SUMMARY.md` when done.
</output>
