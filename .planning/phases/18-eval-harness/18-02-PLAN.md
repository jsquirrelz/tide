---
phase: 18-eval-harness
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - internal/eval/protocol_test.go
  - internal/eval/cost_replay_test.go
  - internal/eval/testdata/fixtures/stream_real.jsonl
  - internal/subagent/anthropic/cost_parity_test.go
  - internal/subagent/anthropic/protocol_compliance_test.go
autonomous: true
requirements: [EVAL-02, EVAL-04]

must_haves:
  truths:
    - "A PR that breaks child-CRD parse success, declared-output-path presence, or DAG acyclicity is caught by the deterministic protocol-compliance gate in `make test`."
    - "The harness asserts the existing `(*Anthropic).estimatedCostCents` parity within 1 cent — no re-implemented cost math."
    - "The harness reports REALIZED per-wave savings (cache-write premium subtracted) using estimatedCostCents for both the cached and uncached scenario."
    - "No LLM-as-judge: the gate is deterministic and zero-network (D-05)."
  artifacts:
    - path: "internal/subagent/anthropic/cost_parity_test.go"
      provides: "in-package parity + realized-savings test calling unexported estimatedCostCents"
      contains: "estimatedCostCents"
    - path: "internal/subagent/anthropic/protocol_compliance_test.go"
      provides: "in-package readChildCRDs fixture tests (valid / bad-kind / missing-name)"
      contains: "readChildCRDs"
    - path: "internal/eval/protocol_test.go"
      provides: "DAG acyclicity (acyclic + cyclic) + declared-output-path structural check"
      contains: "dag.ComputeWaves"
    - path: "internal/eval/cost_replay_test.go"
      provides: "ParseStream replay of the synthetic four-field-Usage fixture"
      contains: "ParseStream"
    - path: "internal/eval/testdata/fixtures/stream_real.jsonl"
      provides: "synthetic events.jsonl with all four token dimensions non-zero"
  key_links:
    - from: "internal/subagent/anthropic/cost_parity_test.go"
      to: "(*Anthropic).estimatedCostCents"
      via: "in-package call (same package anthropic) over Usage fixtures"
      pattern: "estimatedCostCents"
    - from: "internal/subagent/anthropic/protocol_compliance_test.go"
      to: "readChildCRDs"
      via: "t.TempDir() JSON fixtures, assert err nil/non-nil"
      pattern: "readChildCRDs"
    - from: "internal/eval/protocol_test.go"
      to: "pkg/dag.ComputeWaves"
      via: "acyclic fixture → waves, cyclic fixture → *CycleError"
      pattern: "dag\\.ComputeWaves"
    - from: "internal/eval/cost_replay_test.go"
      to: "anthropic.ParseStream"
      via: "replay testdata/fixtures/stream_real.jsonl, assert four Usage fields"
      pattern: "ParseStream"
---

<objective>
Build the deterministic primary gate: protocol-compliance (child-CRD parse, declared-output-path presence, DAG acyclicity) and the cost-parity / realized-savings check that delegates to the existing `(*Anthropic).estimatedCostCents`. Every check reuses an already-correct codebase function — this is a test wrapper, not a reimplementation. All tests are zero-network and run under `make test`; no LLM judge (D-05).

Purpose: EVAL-02 (deterministic protocol-compliance gate, no LLM judge), EVAL-04 (cost delta delegates to estimatedCostCents, asserts parity within 1 cent, reports realized per-wave savings with cache-write premium subtracted).
Output: two in-package tests in internal/subagent/anthropic/ (to reach unexported estimatedCostCents + readChildCRDs without touching the locked production files), two tests in internal/eval/ (DAG + cost replay), and one synthetic events.jsonl fixture.

Depends on plan 01: the internal/eval package and the goldie/v2 go.mod entry must already exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-eval-harness/18-CONTEXT.md
@.planning/phases/18-eval-harness/18-RESEARCH.md
@.planning/phases/18-eval-harness/18-PATTERNS.md

<interfaces>
<!-- Extracted/verified from codebase. -->

Unexported symbols — MUST be reached via same-package test files (do NOT export, do NOT modify the locked production files pricing.go / subagent.go):
```
// pricing.go:132 — package anthropic
func (a *Anthropic) estimatedCostCents(model string, u pkgdispatch.Usage) int64
// subagent.go:436 — package anthropic
func readChildCRDs(childrenDir, relPrefix string) ([]pkgdispatch.ChildCRDSpec, error)
```
estimatedCostCents math (pricing.go:144-156): ceil((Input*inputRate + Output*outputRate + CacheRead*cacheReadRate + CacheCreation*cacheWriteRate) / 1_000_000), cents/MTok.
For claude-sonnet-4-6: input=300, output=1500, cacheRead=30, cacheWrite=375. Construct instance with `New(Options{})` (mirrors pricing_test.go).

Exported symbols — reachable from package eval:
```
// stream_parser.go:76 — package anthropic (exported)
func ParseStream(r io.Reader, rawSink io.Writer) (pkgdispatch.Usage, string, error)
// pkg/dag/kahn.go:46 (exported)
func ComputeWaves(nodes []NodeID, edges []Edge) ([][]NodeID, error)  // returns *CycleError on cycle (pkg/dag/errors.go:23)
```

Usage struct (pkg/dispatch/envelope.go:252): InputTokens, OutputTokens, EstimatedCostCents, CacheReadTokens, CacheCreationTokens.

readChildCRDs allowlist behavior: valid Kind must be in childKindAllowlist (e.g. "Task"), Name must be non-empty; otherwise returns an error.

events.jsonl shape (stream_parser_test.go:30-32 — the parser only reads the `result` event):
```
{"type":"result","result":"...","usage":{"input_tokens":N,"output_tokens":N,"cache_read_input_tokens":N,"cache_creation_input_tokens":N},"total_cost_usd":X}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-package cost-parity + realized-savings test</name>
  <files>internal/subagent/anthropic/cost_parity_test.go</files>
  <read_first>
    - internal/subagent/anthropic/pricing_test.go (EXACT analog — same package, New(Options{}), t.Run subtests with inline arithmetic comments, sentinel-value checks)
    - internal/subagent/anthropic/pricing.go (lines 132-160 — estimatedCostCents signature + four-field math + ceiling division; READ-ONLY, do not modify)
    - pkg/dispatch/envelope.go (lines 252-265 — Usage struct field names)
    - .planning/phases/18-eval-harness/18-PATTERNS.md (cost_parity_test.go section + Pattern 2)
    - .planning/phases/18-eval-harness/18-RESEARCH.md (Q4 realized-savings formula, Pitfall 1 no-export rule)
  </read_first>
  <behavior>
    - Parity: estimatedCostCents("claude-sonnet-4-6", a hand-computed four-field Usage) equals the independently hand-verified cents value (within 1 cent / exact for the ceiling result).
    - Realized savings: estimatedCostCents(uncached scenario) - estimatedCostCents(cached scenario) > 0 for a high read:write-ratio wave (cache-write premium absorbed, reads at 0.10x), proving REALIZED per-wave savings not gross per-dispatch discount.
    - Zero-usage: estimatedCostCents for an all-zero Usage returns 0.
  </behavior>
  <action>
    Create `internal/subagent/anthropic/cost_parity_test.go` with `package anthropic` (same package, NOT anthropic_test — this is how the unexported method is reached without modifying pricing.go; per Pitfall 1) and the Apache header. Construct `a := New(Options{})`. Write `TestCostParity_FourField` with t.Run subtests covering: a four-field Usage (Input/Output/CacheRead/CacheCreation all non-zero) asserting `a.estimatedCostCents("claude-sonnet-4-6", u)` equals an inline hand-computed cents value (include the arithmetic as a comment per pricing_test.go convention); a zero Usage asserting 0. Write `TestCostParity_RealizedSavings` constructing uNoCaching (all input, no cache fields) and uWithCaching (same effective work but with CacheReadTokens high and CacheCreationTokens non-zero), asserting `costWithCaching < costNoCaching` — this is the realized per-wave savings (delegate both branches to estimatedCostCents; do NOT recompute inputRate-cacheReadRate by hand, per RESEARCH anti-pattern). Do NOT add an exported wrapper; do NOT modify pricing.go.
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/ -run 'TestCostParity' && head -20 internal/subagent/anthropic/cost_parity_test.go | grep -q "^package anthropic" && git diff --quiet internal/subagent/anthropic/pricing.go</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/subagent/anthropic/ -run TestCostParity` exits 0
    - File declares `package anthropic` (not anthropic_test)
    - `git diff --quiet internal/subagent/anthropic/pricing.go` — pricing.go untouched (locked)
    - The realized-savings subtest delegates both branches to estimatedCostCents (no hand-rolled rate subtraction): `grep -c "estimatedCostCents" cost_parity_test.go` returns >= 3
    - The four-field parity subtest carries an inline comment showing the cents arithmetic
  </acceptance_criteria>
  <done>cost_parity_test.go (package anthropic) asserts estimatedCostCents parity within 1 cent and realized per-wave savings, calling only the existing unexported method; pricing.go unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: In-package readChildCRDs protocol-compliance test</name>
  <files>internal/subagent/anthropic/protocol_compliance_test.go</files>
  <read_first>
    - internal/subagent/anthropic/subagent.go (lines 436-540 — readChildCRDs: signature, childKindAllowlist, Name validation, json.Decoder per-file isolation; READ-ONLY)
    - internal/subagent/anthropic/stream_parser_test.go (lines 1-58 — package anthropic test conventions, t.Fatalf-for-setup)
    - .planning/phases/18-eval-harness/18-PATTERNS.md (protocol_compliance_test.go section — t.TempDir() fixture pattern, the three JSON bodies)
    - .planning/phases/18-eval-harness/18-RESEARCH.md (Q3 — the three checks + fixture descriptions)
  </read_first>
  <behavior>
    - Valid Task child-CRD JSON (kind in allowlist, non-empty name, non-empty spec) → readChildCRDs returns one spec, no error.
    - Forbidden kind → readChildCRDs returns an error.
    - Empty metadata.name → readChildCRDs returns an error.
  </behavior>
  <action>
    Create `internal/subagent/anthropic/protocol_compliance_test.go` with `package anthropic` + Apache header. For each case, write the JSON body to a file under `t.TempDir()` (auto-cleaned), call `readChildCRDs(dir, "")`, and assert: TestReadChildCRDs_ValidTask expects len(specs)==1 and err==nil for a valid Task CRD (apiVersion tideproject.k8s/v1alpha1, kind Task, metadata.name task-01, non-empty spec); TestReadChildCRDs_BadKind expects err!=nil for kind "Forbidden"; TestReadChildCRDs_MissingName expects err!=nil for an empty metadata.name. Use t.Fatalf for write/setup failures, t.Errorf for assertion failures. Do NOT pre-commit testdata fixtures for these — own the JSON inline via t.TempDir() (PATTERNS note); do NOT export readChildCRDs or modify subagent.go.
  </action>
  <verify>
    <automated>go test ./internal/subagent/anthropic/ -run 'TestReadChildCRDs' && git diff --quiet internal/subagent/anthropic/subagent.go && head -20 internal/subagent/anthropic/protocol_compliance_test.go | grep -q "^package anthropic"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/subagent/anthropic/ -run TestReadChildCRDs` exits 0 (all three cases pass)
    - File declares `package anthropic`
    - `git diff --quiet internal/subagent/anthropic/subagent.go` — subagent.go untouched (locked)
    - Tests use `t.TempDir()` (no pre-committed JSON under testdata for these cases)
    - Both negative cases assert err != nil; positive case asserts err == nil and one spec
  </acceptance_criteria>
  <done>protocol_compliance_test.go exercises readChildCRDs valid/bad-kind/missing-name paths via temp fixtures; subagent.go unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DAG acyclicity, output-path structural check, and cost-replay in internal/eval</name>
  <files>internal/eval/protocol_test.go, internal/eval/cost_replay_test.go, internal/eval/testdata/fixtures/stream_real.jsonl</files>
  <read_first>
    - pkg/dag/kahn.go (lines 46-90 — ComputeWaves signature + cycle return)
    - pkg/dag/errors.go (lines 20-30 — CycleError type + InvolvedNodes)
    - internal/subagent/anthropic/stream_parser_test.go (lines 29-58 — ParseStream call + the four-field fixture shape, the source of truth for the jsonl)
    - pkg/dispatch/envelope.go (lines 39-100 EnvelopeIn DeclaredOutputPaths; lines 252-265 Usage)
    - .planning/phases/18-eval-harness/18-PATTERNS.md (protocol_test.go + cost_replay_test.go sections + fixtures shape reference)
    - .planning/phases/18-eval-harness/18-RESEARCH.md (Q3 DAG/output-path; Q5 fixture; Pitfall 4 non-zero cache fields)
  </read_first>
  <behavior>
    - Acyclic 3-node fixture → ComputeWaves returns 3 waves, no error.
    - 2-node cyclic fixture → ComputeWaves returns an error that errors.As-matches *dag.CycleError.
    - An EnvelopeIn with non-empty DeclaredOutputPaths passes the structural output-path check; empty fails.
    - ParseStream over the committed stream_real.jsonl returns a Usage with all four token fields equal to the fixture's non-zero values.
  </behavior>
  <action>
    Create `internal/eval/cost_replay_test.go` (package eval): read `testdata/fixtures/stream_real.jsonl`, call `anthropic.ParseStream(reader, &rawSink)`, and assert the returned pkgdispatch.Usage has InputTokens/OutputTokens/CacheReadTokens/CacheCreationTokens equal to the fixture's values (this proves the four dimensions flow through; it does NOT call estimatedCostCents — that parity assertion lives in cost_parity_test.go in package anthropic). Create the fixture `internal/eval/testdata/fixtures/stream_real.jsonl` as a synthetic three-line events.jsonl (system/init, stream_event, result) modeling a cache-warm second dispatch with all four usage dimensions NON-ZERO (e.g. input 500 / output 100 / cache_read 800 / cache_creation 1200) per D-04 revised and Pitfall 4. Create `internal/eval/protocol_test.go` (package eval) with: TestDAGAcyclicity_AcyclicFixture (3 nodes, 2 edges → 3 waves, no err); TestDAGAcyclicity_CyclicFixture (2-node cycle → errors.As(*dag.CycleError)); TestDeclaredOutputPaths_Presence (structural: non-empty DeclaredOutputPaths passes, empty fails — harness.Validate needs real file timestamps so the deterministic gate uses the structural field check per RESEARCH Q3 note). Import only pkg/dag, pkg/dispatch, internal/subagent/anthropic (for ParseStream) — NOT internal/controller/budget/metrics/api (Pitfall 6).
  </action>
  <verify>
    <automated>go test ./internal/eval/ -run 'TestDAG|TestDeclaredOutputPaths|TestCostReplay|TestParseStream' && test -f internal/eval/testdata/fixtures/stream_real.jsonl && grep -q 'cache_creation_input_tokens":[1-9]' internal/eval/testdata/fixtures/stream_real.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/eval/` exits 0 (DAG, output-path, and cost-replay tests pass alongside plan-01's render tests)
    - `internal/eval/testdata/fixtures/stream_real.jsonl` exists with all four usage fields non-zero (cache_read_input_tokens and cache_creation_input_tokens both > 0)
    - Cyclic test asserts via `errors.As(err, &*dag.CycleError)` (not a string match)
    - protocol_test.go imports none of internal/controller, internal/budget, internal/metrics, api/v1alpha1
    - `go build ./internal/eval/...` exits 0 (no import cycle)
  </acceptance_criteria>
  <done>internal/eval DAG-acyclicity, declared-output-path, and ParseStream-replay tests are green; the synthetic four-field fixture is committed; no forbidden imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testdata fixture → readChildCRDs | A `.json` fixture (in t.TempDir()) is parsed by readChildCRDs. This is the same path used in production for LLM-authored child specs; the test deliberately exercises the allowlist/name rejection. No network, no secrets — `make test` is zero-network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | adversarial child-CRD JSON fixture | mitigate | readChildCRDs already applies kind allowlist, name validation, and per-file json.Decoder isolation (traversal defense). The bad-kind / missing-name tests assert these rejections fire — the gate proves the defense is live rather than introducing a new surface. |
| T-18-02-02 | Tampering | committed stream_real.jsonl fixture | accept | Synthetic fixture is a reviewed artifact; only feeds the deterministic cost-replay test. No runtime/prod consumption, no secrets. |
| T-18-02-SC | Tampering | (no new package installs) | accept | This plan adds no dependency — only test files + one jsonl fixture. Supply-chain surface is covered by Plan 01's goldie audit. |
</threat_model>

<verification>
- `make test` green: protocol-compliance + cost-parity + cost-replay all run in the unit tier, zero-network (EVAL-02, EVAL-04, EVAL-06 auto-gate).
- `git diff --quiet internal/subagent/anthropic/pricing.go internal/subagent/anthropic/subagent.go` — both locked files untouched.
- `go build ./internal/eval/...` green (no import cycle).
</verification>

<success_criteria>
- Protocol-compliance gate (child-CRD parse, declared-output-path presence, DAG acyclicity) is deterministic, zero-network, and reuses existing functions — no reimplementation, no LLM judge.
- Cost-parity test delegates to (*Anthropic).estimatedCostCents and asserts within 1 cent; realized per-wave savings shown by subtracting cached from uncached estimatedCostCents.
- pricing.go and subagent.go are unchanged.
- internal/eval imports none of: internal/controller, internal/budget, internal/metrics, api/v1alpha1.
</success_criteria>

<output>
Create `.planning/phases/18-eval-harness/18-02-SUMMARY.md` when done.
</output>
