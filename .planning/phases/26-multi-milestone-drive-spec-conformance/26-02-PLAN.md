---
phase: 26-multi-milestone-drive-spec-conformance
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - internal/controller/wave_controller.go
  - internal/controller/project_controller.go
  - internal/controller/task_controller.go
  - internal/controller/task_controller_predicate_test.go
autonomous: true
requirements: [MS-02, SPEC-01]
must_haves:
  truths:
    - "The wave aggregator distinguishes a zero-member wave (Phase ZeroMembers) from a wave with real in-flight Running tasks"
    - "The wave-prune guard deletes a stale wave only when it has zero members OR is Succeeded — never a wave with real in-flight tasks"
    - "The CR-01 PruneShrink regression test stays green (zero-member waves remain prunable)"
    - "globalDependentsMapper's Task watch fires only on status-phase transitions or dependsOn changes"
    - "A no-op resourceVersion-only Task update does not trigger global dependent re-derivation"
  artifacts:
    - path: "internal/controller/wave_controller.go"
      provides: "ZeroMembers phase in the aggregator switch"
      contains: "ZeroMembers"
    - path: "internal/controller/project_controller.go"
      provides: "OQ-3 in-flight-safe prune guard"
      contains: "skipping prune of in-flight wave"
    - path: "internal/controller/task_controller.go"
      provides: "WR-02 status-phase/dependsOn predicate on globalDependentsMapper watch"
      contains: "statusPhaseOrDepsChanged"
    - path: "internal/controller/task_controller_predicate_test.go"
      provides: "Unit test proving the WR-02 predicate fires only on phase/dependsOn changes"
  key_links:
    - from: "internal/controller/project_controller.go prune loop"
      to: "wave.Status.TaskRefs / wave.Status.Phase"
      via: "len(TaskRefs)==0 || Phase==Succeeded guard"
      pattern: "len\\(w\\.Status\\.TaskRefs\\) == 0"
    - from: "internal/controller/task_controller.go SetupWithManager"
      to: "globalDependentsMapper Watches"
      via: "builder.WithPredicates(statusPhaseOrDepsChanged)"
      pattern: "builder\\.WithPredicates\\(statusPhaseOrDepsChanged\\)"
---

<objective>
Close the two carried-in Phase-25 debts as proper root fixes: OQ-3 (D-08) — the wave aggregator currently marks BOTH zero-member waves and waves-with-real-in-flight-tasks as `Running`, so a naive prune guard either deletes in-flight waves or breaks the CR-01 PruneShrink test; and WR-02 (D-09) — `globalDependentsMapper`'s Task watch has no event predicate, so it full-re-derives global dependents on every Task event including no-op resourceVersion bumps.

Purpose: Wave CRs are display artifacts (`computeGlobalIndegree` reads Task `.status` only), so OQ-3 is display-correctness fixed at root, not a dispatch bug. WR-02 is a bounded perf fix. Both live in the reconcile/watch layer and pair naturally per D-09. Sequenced into Wave 2 because the OQ-3 prune guard edits `project_controller.go`, which Plan 01's idempotency guard also touches — exclusive file ownership requires Plan 01 to land first.
Output: aggregator with a ZeroMembers phase, an in-flight-safe prune guard, a WR-02 watch predicate, and a unit test proving the predicate's firing conditions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-multi-milestone-drive-spec-conformance/26-CONTEXT.md
@.planning/phases/26-multi-milestone-drive-spec-conformance/26-RESEARCH.md
@.planning/phases/26-multi-milestone-drive-spec-conformance/26-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Do NOT re-explore the codebase to rediscover these. -->

From internal/controller/wave_controller.go (aggregator switch ~lines 163-184):
  // allSucceeded := len(members) > 0 ... ; switch sets phase = "Succeeded" / "Failed" / "Running"
  // members is the label-indexed Task membership; Status.TaskRefs is populated from it

From internal/controller/project_controller.go (prune loop ~lines 1631-1657):
  // for w in allWaves.Items: if w.Spec.ProjectRef == project.Name && w.Spec.WaveIndex >= len(globalWaves) { Delete }
  // allWaves fetched fresh at ~line 1636 from the informer cache

From internal/controller/task_controller.go (SetupWithManager ~lines 1741-1758):
  annotationOnly := predicate.AnnotationChangedPredicate{}   // line 1740 — existing predicate pattern
  // globalDependentsMapper Watches at ~1744-1747 has NO predicate (WR-02 target)
  // imports present: event (pkg/event), predicate (pkg/predicate), builder; add "slices" stdlib

From api/v1alpha2 Task:
  Task.Status.Phase string ; Task.Spec.DependsOn []string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ZeroMembers phase to wave aggregator + in-flight-safe prune guard</name>
  <read_first>
    - internal/controller/wave_controller.go (lines 163-185 — the aggregator switch; analog = self per PATTERNS.md)
    - internal/controller/project_controller.go (lines 1631-1657 — the prune loop with the OQ-3-deferred NOTE)
    - test/integration/envtest/global_wave_derivation_test.go (lines 413-454 — CR-01 PruneShrink regression test that MUST stay green)
    - .planning/phases/26-multi-milestone-drive-spec-conformance/26-PATTERNS.md sections wave_controller.go and project_controller.go (exact target switch + prune patterns)
    - .planning/phases/26-multi-milestone-drive-spec-conformance/26-RESEARCH.md section D-08 (root-cause + Pitfall 5 staleness fence)
  </read_first>
  <files>internal/controller/wave_controller.go, internal/controller/project_controller.go</files>
  <action>
    In wave_controller.go, change the aggregator switch (~lines 174-184) so the zero-member case is FIRST and distinct: add `case len(members) == 0:` setting `phase = "ZeroMembers"`, message "No tasks assigned to this wave"; keep `case allSucceeded:` → "Succeeded"; `case failedTask != "":` → "Failed"; `default:` → "Running" (now reached only when members exist and none failed and not all succeeded — real in-flight). Use the target pattern in 26-PATTERNS.md section wave_controller.go (lines 256-273). This makes a zero-member wave (all tasks deleted during re-derive) distinguishable from a wave with real Running tasks.
    In project_controller.go, replace the prune guard at ~lines 1644-1656 (remove the OQ-3-deferred NOTE comment) with the in-flight-safe guard: inside the `WaveIndex >= len(globalWaves)` branch, only Delete when `len(w.Status.TaskRefs) == 0 || w.Status.Phase == "Succeeded"`; otherwise log at V(1) "skipping prune of in-flight wave" with wave/phase/memberCount. Use the target pattern in 26-PATTERNS.md section project_controller.go (lines 214-231). Per RESEARCH Pitfall 5 / assumption A1, add a secondary CreationTimestamp fence: if the wave was created very recently (e.g. within the last 5s) and TaskRefs is still empty, skip pruning it that pass (the aggregator may not have run yet) — this prevents a narrow race where a freshly-created wave shows empty TaskRefs before WaveReconciler stamps them.
    CR-01 PruneShrink must stay green: the fix makes zero-member waves prunable (which is exactly what PruneShrink asserts), so do NOT add a Phase!=Succeeded skip that would re-break it (Phase 25 commits 2a97a7a→e7c14f7 reverted that naive guard).
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./... && go test ./test/integration/envtest/... -count=1 -run 'PruneShrink|Prune|Wave' 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ZeroMembers' internal/controller/wave_controller.go` returns at least 1.
    - `grep -c 'skipping prune of in-flight wave' internal/controller/project_controller.go` returns 1.
    - `grep -c 'len(w.Status.TaskRefs) == 0' internal/controller/project_controller.go` returns at least 1.
    - The OQ-3-deferred NOTE comment ("an in-flight guard here would protect") is gone: `grep -c 'OQ-3 deferred' internal/controller/project_controller.go` returns 0.
    - `go build ./...` succeeds; the CR-01 PruneShrink envtest (global_wave_derivation_test.go:413) passes — read MAKE_EXIT, not just the Ginkgo summary.
  </acceptance_criteria>
  <done>Aggregator distinguishes zero-member from in-flight; prune guard never deletes a real in-flight wave; PruneShrink green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add WR-02 watch predicate to globalDependentsMapper + unit test</name>
  <read_first>
    - internal/controller/task_controller.go (lines 1497-1586 globalDependentsMapper; 1709-1758 SetupWithManager + the existing annotationOnly predicate usage; analog = self per PATTERNS.md)
    - .planning/phases/26-multi-milestone-drive-spec-conformance/26-PATTERNS.md section task_controller.go (exact predicate.Funcs target + wiring)
    - .planning/phases/26-multi-milestone-drive-spec-conformance/26-RESEARCH.md section D-09 (predicate soundness rationale + import notes)
    - any existing internal/controller/*_test.go that constructs event.UpdateEvent for a predicate (for the test harness shape)
  </read_first>
  <files>internal/controller/task_controller.go, internal/controller/task_controller_predicate_test.go</files>
  <behavior>
    - UpdateFunc returns true when oldTask.Status.Phase != newTask.Status.Phase (predecessor completed/failed → dependents must re-evaluate).
    - UpdateFunc returns true when oldTask.Spec.DependsOn changed (new edge declared) — use slices.Equal for the comparison.
    - UpdateFunc returns FALSE for a no-op update where Status.Phase and Spec.DependsOn are unchanged (resourceVersion-only bump).
    - UpdateFunc returns true (conservative) when either object fails the type assertion to *Task.
    - CreateFunc and DeleteFunc return true; GenericFunc returns false.
  </behavior>
  <action>
    Define `statusPhaseOrDepsChanged := predicate.Funcs{...}` in SetupWithManager before the builder chain, per the target in 26-PATTERNS.md section task_controller.go (lines 303-316): UpdateFunc type-asserts both objects to *tideprojectv1alpha2.Task, returns true on type-assert failure (conservative), otherwise returns `oldT.Status.Phase != newT.Status.Phase || !slices.Equal(oldT.Spec.DependsOn, newT.Spec.DependsOn)`; CreateFunc/DeleteFunc return true; GenericFunc returns false. Wire it onto the globalDependentsMapper Watches call (~1744-1747) via `builder.WithPredicates(statusPhaseOrDepsChanged)`. Add `"slices"` to the stdlib import group if absent. Do NOT touch the second Watches (annotationOnly self-watch) or WithEventFilter(nsPred).
    Write the failing unit test FIRST in internal/controller/task_controller_predicate_test.go: construct event.UpdateEvent values with two *Task objects and assert the predicate's UpdateFunc per the behavior block above (phase-changed→true; deps-changed→true; no-op→false; type-assert-failure→true). RED before GREEN.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./internal/controller/... -count=1 -run 'Predicate|StatusPhaseOrDeps|GlobalDependents' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'statusPhaseOrDepsChanged' internal/controller/task_controller.go` returns at least 2 (definition + WithPredicates wiring).
    - `grep -c 'builder.WithPredicates(statusPhaseOrDepsChanged)' internal/controller/task_controller.go` returns 1.
    - internal/controller/task_controller_predicate_test.go exists and `go test ./internal/controller/... -run Predicate -count=1` passes with assertions covering: phase change → true, dependsOn change → true, no-op → false, type-assert failure → true.
    - `go build ./...` succeeds (slices import resolved).
  </acceptance_criteria>
  <done>globalDependentsMapper fires only on phase/dependsOn changes; unit test proves the firing matrix; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (internal) reconcile/watch layer only | No external input crosses any boundary in this plan — wave aggregation, prune, and watch-predicate filtering are all in-process controller logic over CRDs the controller already owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Denial of Service | globalDependentsMapper full-re-derivation churn on every Task event | mitigate | The WR-02 predicate is itself the mitigation — it removes wasteful re-derivation on no-op updates, REDUCING reconcile load. Conservative fall-through on type-assert failure prevents dropped events (no correctness regression). |
| T-26-04 | Tampering | wave-prune deleting an in-flight wave | mitigate | The in-flight guard (TaskRefs/Phase check + CreationTimestamp fence) prevents premature deletion of waves with real Running tasks. Wave CRs are display artifacts (dispatch reads Task .status only), so even a mis-prune cannot corrupt dispatch — the guard is display-correctness defense-in-depth. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs — all changes in-repo Go + a Go unit test. No `## Package Legitimacy Audit` needed. |

Threat surface is minimal: no new external input, no auth/authz, no network. Both changes reduce or bound existing internal work; neither widens an attack surface.
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test ./internal/controller/... -count=1` green (incl. the new predicate test).
- CR-01 PruneShrink envtest green (`go test ./test/integration/envtest/... -run PruneShrink`).
- `make lint` green (golangci-lint with gosec/errcheck/staticcheck + import firewalls — CLAUDE.md: run the linter and fix offenses before declaring complete; wave_controller.go / project_controller.go / task_controller.go are structural Go changes). Run AFTER tests pass; fix any offense before the plan is done.
- `make verify-dag-imports` green (no scopeResolver relocation).
</verification>

<success_criteria>
- Wave aggregator emits a distinct ZeroMembers phase (D-08 root distinction).
- Prune guard never deletes a wave with real in-flight tasks; PruneShrink stays green (OQ-3 closed).
- globalDependentsMapper watch fires only on phase/dependsOn changes, proven by unit test (WR-02 closed).
</success_criteria>

<output>
Create `.planning/phases/26-multi-milestone-drive-spec-conformance/26-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
