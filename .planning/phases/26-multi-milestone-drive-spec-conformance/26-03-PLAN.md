---
phase: 26-multi-milestone-drive-spec-conformance
plan: 03
type: tdd
wave: 2
depends_on: ["26-01"]
files_modified:
  - test/integration/envtest/spec_conformance_test.go
autonomous: true
requirements: [SPEC-01, MS-01, MS-02, MS-03]
must_haves:
  truths:
    - "An envtest applies the README α…θ fixture as a 2-Milestone hierarchy and the real reconcilers derive global Wave CRs equal to [{α,β,γ,ζ}, {δ,η}, {ε,θ}]"
    - "The cross-milestone edge γ→η is honored (η is NOT in Wave 0)"
    - "ζ is in Wave 0 despite Milestone B depending on Milestone A (Milestone edge contributes zero execution edges)"
    - "N milestone-level AwaitingApproval planning holds compose in milestone-DAG order under gates.milestone: approve; both release on the approve-milestone annotation"
    - "gates.milestone: auto produces no milestone holds (full-auto expressible); gates.task: approve holds at task level (full-supervised expressible)"
    - "The conformance test is cross-linked to the README §Wave computation worked example so they cannot drift"
  artifacts:
    - path: "test/integration/envtest/spec_conformance_test.go"
      provides: "SPEC-01 wave-schedule conformance + MS-03 gate composition envtest"
      contains: "spec-conformance-project"
      min_lines: 120
  key_links:
    - from: "test/integration/envtest/spec_conformance_test.go"
      to: "tide-wave-spec-conformance-project-0/1/2"
      via: "assertWaveMembership polling Wave.Status.TaskRefs"
      pattern: "assertWaveMembership"
    - from: "spec_conformance_test.go fixture"
      to: "cross-milestone edge sc-gamma -> sc-eta"
      via: "Task.Spec.DependsOn crossing milestone boundary"
      pattern: "sc-gamma"
---

<objective>
Pin the README execution-DAG worked example as an executable conformance test (SPEC-01) and prove milestone-gate composition for N milestones (MS-03). Apply the literal α…θ fixture as a REAL 2-Milestone CRD hierarchy, let the actual reconcilers (assembled in Plan 01 with §6d removed) derive the global wave schedule, and assert it equals `[{α,β,γ,ζ}, {δ,η}, {ε,θ}]` — proving MS-01/MS-02 + EXEC end-to-end, not just the algorithm.

Purpose: D-06 requires an envtest with real CRDs (full stack), not a unit test, so the shipped orchestrator is proven to agree with the README. The load-bearing cross-milestone edge γ→η (γ in Milestone A, η in Milestone B) exercises §6a task-level cross-scope resolution across a milestone boundary; ζ free in Wave 0 proves the Milestone edge is planning-only (§6d removed in Plan 01). MS-03 (D-04/D-05) is a conformance test only — no new gate code — proving N planning holds compose via the existing approve-at-descent machinery.
Output: `spec_conformance_test.go` with the SPEC-01 wave-schedule assertion and the MS-03 gate-composition assertion, cross-linked to the README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-multi-milestone-drive-spec-conformance/26-CONTEXT.md
@.planning/phases/26-multi-milestone-drive-spec-conformance/26-RESEARCH.md
@.planning/phases/26-multi-milestone-drive-spec-conformance/26-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Reuse these — do NOT re-derive the test harness. -->

From test/integration/envtest/global_wave_derivation_test.go (the analog file — copy helpers/patterns):
  package envtest_integration
  globalWaveNamespace                         // the test namespace constant
  createSimpleMilestone(ctx, name, projectRef)        // ~line 56 — does NOT accept dependsOn
  createSimplePhase(ctx, name, milestoneRef)          // ~line 39
  createSimplePlan(ctx, name)                         // minimal Plan
  makeGlobalWaveTask(ctx, name, planRef, dependsOn, files)  // ~line 83 — Task with owner.LabelProject stamp
  assertWaveExists(ctx, projectName, waveIdx)         // ~line 113 — Eventually 30s/500ms
  makeBoundPVC(ctx, name, ns)                         // from indegree_test.go — needed for project reconcile
  // README fixture test at ~lines 191-216; PruneShrink at 413-454 (do NOT modify those)

From milestone_controller.go gate-descent (~lines 596-645) + internal/gates:
  gates.EvaluatePolicy(project.Spec.Gates, "milestone")  // returns PolicyApprove by DefaultGates()
  gates.CheckApprove(ms, "milestone")   // annotation tideproject.k8s/approve-milestone: true
  gates.ConsumeApprove(ms, "milestone")
  // Status.Phase "AwaitingApproval" when held; "Running" after approve

From api/v1alpha2:
  MilestoneSpec.DependsOn []string ; Wave.Status.TaskRefs []string ; Wave name = tide-wave-<project>-<N>
</interfaces>

<fixture_topology>
<!-- The literal SPEC-01 fixture (RESEARCH §D-06, PATTERNS lines 471-485). Project: "spec-conformance-project". -->
Milestone A (ms-spec-a, no dependsOn):
  Phase A.1 [Plan A.1.1: sc-alpha, sc-beta ; Plan A.1.2: sc-gamma]
  Phase A.2 [Plan A.2.1: sc-delta, sc-epsilon]
Milestone B (ms-spec-b, dependsOn: ["ms-spec-a"]  — PLANNING ORDER ONLY):
  Phase B.1 [Plan B.1.1: sc-zeta ; Plan B.1.2: sc-eta, sc-theta]

Dependency DIRECTION per README (edge X→Y means Y depends on X — encode on the dependent's DependsOn):
  sc-delta.DependsOn   = {sc-alpha, sc-beta}      (α→δ, β→δ)
  sc-epsilon.DependsOn = {sc-delta}               (δ→ε)
  sc-eta.DependsOn     = {sc-gamma, sc-zeta}      (γ→η CROSS-MILESTONE load-bearing edge; ζ→η)
  sc-theta.DependsOn   = {sc-eta}                 (η→θ)
  sc-alpha, sc-beta, sc-gamma, sc-zeta            = empty DependsOn → Wave 0

Expected waves: [{sc-alpha, sc-beta, sc-gamma, sc-zeta}, {sc-delta, sc-eta}, {sc-epsilon, sc-theta}]
</fixture_topology>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: SPEC-01 wave-schedule conformance + MS-03 gate composition envtest (RED then GREEN)</name>
  <read_first>
    - test/integration/envtest/global_wave_derivation_test.go (the analog: import block, BeforeEach/AfterEach, Eventually polling, helpers; README fixture test ~191-216; PruneShrink ~413-454 — do NOT modify)
    - test/integration/envtest/suite_test.go (TestIntegrationEnvtest entry ~164; newPhase2ReconcilersForTest ~234 — confirms all 6 reconcilers run)
    - internal/controller/milestone_controller.go (gate-descent ~596-645) + internal/gates/policy.go + annotation.go (EvaluatePolicy/CheckApprove/ConsumeApprove)
    - .planning/phases/26-multi-milestone-drive-spec-conformance/26-PATTERNS.md section spec_conformance_test.go (lines 332-487 — exact helper + fixture patterns)
    - .planning/phases/26-multi-milestone-drive-spec-conformance/26-RESEARCH.md sections D-06 (fixture mapping), D-04/D-05 (MS-03 gate flow), Pitfall 3/4 (project-name collision, edge-name match)
  </read_first>
  <files>test/integration/envtest/spec_conformance_test.go</files>
  <behavior>
    SPEC-01: Given the 2-Milestone α…θ fixture (see <fixture_topology>) applied as real CRDs, after the reconcilers assemble and derive, the Project-scoped Wave CRs satisfy:
      - tide-wave-spec-conformance-project-0 .Status.TaskRefs ⊇ {sc-alpha, sc-beta, sc-gamma, sc-zeta}
      - tide-wave-spec-conformance-project-1 .Status.TaskRefs ⊇ {sc-delta, sc-eta}
      - tide-wave-spec-conformance-project-2 .Status.TaskRefs ⊇ {sc-epsilon, sc-theta}
      - sc-eta is NOT in Wave 0 (γ→η honored — η waits on γ AND ζ)
      - sc-zeta IS in Wave 0 (Milestone B planning-dep on A contributes no execution edge)
    MS-03 (gates.milestone: approve):
      - Milestone A reaches Status.Phase "AwaitingApproval".
      - After annotating A with tideproject.k8s/approve-milestone: true, A proceeds (Running) and authors its phases.
      - Milestone B ALSO reaches "AwaitingApproval" (N holds compose in DAG order).
      - After annotating B, B proceeds.
    MS-03 (variant gates.milestone: auto): neither milestone holds (full-auto expressible). (full-supervised gates.task: approve is documented as expressible; assert at least the auto + approve milestone variants.)
  </behavior>
  <action>
    Write the tests RED first, then make them GREEN (the SPEC-01 block should pass against Plan 01's §6d-removed graph — if it FAILS asserting sc-eta in Wave 0 or sc-zeta missing from Wave 0, that signals §6d was not fully removed in Plan 01; surface it rather than working around it).
    Create test/integration/envtest/spec_conformance_test.go in package envtest_integration, copying the import block, BeforeEach/AfterEach structure, and Eventually polling patterns from global_wave_derivation_test.go per 26-PATTERNS.md section spec_conformance_test.go. Use the DISTINCT project name "spec-conformance-project" (NOT "global-wave-test-project") to avoid state collision (Pitfall 3). Add two new helpers locally in this file: createSimpleMilestoneWithDeps(ctx, name, projectRef, deps) (extends createSimpleMilestone with DependsOn) and assertWaveMembership(ctx, projectName, waveIdx, expectedTasks) (Eventually-polls Wave.Status.TaskRefs ⊇ expected, 30s/500ms). Reuse makeGlobalWaveTask / createSimplePhase / createSimplePlan / makeBoundPVC unchanged.
    Encode the fixture per <fixture_topology> — dependency DIRECTION matters (edge X→Y means Y depends on X; put the edge on the dependent's DependsOn). Match task names EXACTLY across DependsOn refs (Pitfall 4: a typo silently drops the edge via resolveScope). Assert the SPEC-01 wave membership via assertWaveMembership for all three waves PLUS the explicit negative (sc-eta NOT in Wave 0) and positive (sc-zeta IN Wave 0) checks.
    For MS-03, create milestones via createSimpleMilestoneWithDeps (RESEARCH OQ-3 open-question 3 — gate tests fixture-inject milestones, no planner Job dispatch) and drive AwaitingApproval→approve→Running with the tideproject.k8s/approve-milestone annotation; add the gates.milestone: auto variant asserting no holds. AfterEach cleanup order: Waves → Tasks → Plans → Phases → Milestones → Projects → PVCs.
    Add a top-of-file doc comment cross-linking README §"Wave computation — the topological sort" (≈ README lines 163-222) with the exact edge set and expected schedule, stating this test pins the worked example so README and implementation cannot drift (SPEC-01 textual conformance). Optionally (Claude's discretion per D-06) add a fast in-memory deriveGlobalWaves unit guard alongside — not required.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./test/integration/envtest/... -count=1 -run 'SpecConformance|MS03|Milestone.*Gate' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - test/integration/envtest/spec_conformance_test.go exists, package envtest_integration, uses project name "spec-conformance-project" (not "global-wave-test-project").
    - `grep -c 'assertWaveMembership' test/integration/envtest/spec_conformance_test.go` returns at least 1; `grep -c 'createSimpleMilestoneWithDeps' ...` returns at least 1.
    - The SPEC-01 assertion covers Wave 0 ⊇ {sc-alpha,sc-beta,sc-gamma,sc-zeta}, Wave 1 ⊇ {sc-delta,sc-eta}, Wave 2 ⊇ {sc-epsilon,sc-theta}, AND explicitly sc-eta NOT in Wave 0 and sc-zeta IN Wave 0.
    - The MS-03 assertion covers both milestones reaching AwaitingApproval under gates.milestone: approve, proceeding after the approve-milestone annotation, and gates.milestone: auto producing no holds.
    - Top-of-file comment cross-links README §"Wave computation" (≈ lines 163-222) with the edge set + expected schedule.
    - `go test ./test/integration/envtest/... -run 'SpecConformance|MS03' -count=1` passes — read MAKE_EXIT + `grep -nE '^--- FAIL|^FAIL\s'`, not just the Ginkgo summary.
    - The existing global_wave_derivation_test.go (incl. PruneShrink at 413) is UNMODIFIED and still green.
  </acceptance_criteria>
  <done>SPEC-01 envtest derives the documented schedule from real CRDs (γ→η honored, ζ free in Wave 0); MS-03 gate composition proven; cross-linked to the README; existing wave tests unmodified and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only) | This plan adds a test file exercising real reconcilers in envtest. No production code, no external input, no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | (none — test-only) | spec_conformance_test.go | accept | A conformance test introduces no production attack surface. The only risk is a flaky/colliding test (Pitfall 3: shared project name), mitigated by the distinct "spec-conformance-project" name + isolated BeforeEach/AfterEach. The test STRENGTHENS the security posture by pinning documented behavior against silent divergence (SPEC-01). |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs — uses the existing envtest/Ginkgo toolchain. No `## Package Legitimacy Audit` needed. |

Threat surface: none beyond test hygiene. No `high`-severity threats.
</threat_model>

<verification>
- `go test ./test/integration/envtest/... -run 'SpecConformance|MS03' -count=1` green (read MAKE_EXIT + `grep -nE '^--- FAIL|^FAIL\s'`).
- Existing global_wave_derivation_test.go unmodified and green (PruneShrink at 413).
- `make verify-dag-imports` / `verify-no-aggregates` unaffected (test-only).
</verification>

<success_criteria>
- SPEC-01 envtest derives `[{α,β,γ,ζ}, {δ,η}, {ε,θ}]` from real CRDs; γ→η honored; ζ free in Wave 0 (MS-01/MS-02 proven end-to-end).
- MS-03 envtest proves N milestone planning holds compose under approve-every-milestone and full-auto/full-supervised stay expressible (no new gate code).
- Test cross-linked to the README worked example so they cannot drift.
- Existing wave tests unmodified and green.
</success_criteria>

<output>
Create `.planning/phases/26-multi-milestone-drive-spec-conformance/26-03-SUMMARY.md` when done.
</output>
