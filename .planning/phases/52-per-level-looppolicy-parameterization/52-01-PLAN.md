---
phase: 52-per-level-looppolicy-parameterization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha3/loop_types.go
  - api/v1alpha3/plan_types.go
  - api/v1alpha3/project_types.go
  - api/v1alpha3/phase_types.go
  - api/v1alpha3/milestone_types.go
  - api/v1alpha3/shared_types.go
  - api/v1alpha3/loop_types_test.go
  - api/v1alpha3/zz_generated.deepcopy.go
  - config/crd/bases/
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "Plan.Spec accepts a verification contract with the identical VerificationSpec shape Task uses (D-01)"
    - "Project.Spec carries an optional per-level verification defaults map with one slot per level: task/plan/phase/milestone/project (D-01)"
    - "LoopPolicy carries a Level field validated against the five-level enum (D-02)"
    - "PlanStatus, PhaseStatus, MilestoneStatus, and ProjectStatus embed the shared LoopStatus with no accumulating history (D-06, LOOP-03)"
  artifacts:
    - path: "api/v1alpha3/loop_types.go"
      provides: "LoopLevel enum type + Level field on LoopPolicy"
      contains: "LoopLevel"
    - path: "api/v1alpha3/plan_types.go"
      provides: "PlanSpec.Verification + PlanStatus.LoopStatus"
      contains: "Verification VerificationSpec"
    - path: "api/v1alpha3/project_types.go"
      provides: "ProjectSpec.Verification per-level defaults map"
      contains: "VerificationDefaults"
  key_links:
    - from: "api/v1alpha3/plan_types.go"
      to: "api/v1alpha3/task_types.go VerificationSpec"
      via: "type re-embedding (same standalone type, CEL rule travels with it)"
      pattern: "Verification VerificationSpec"
    - from: "api/v1alpha3/loop_types_test.go"
      to: "new LoopStatus embeddings"
      via: "TestLoopStatus_NoForbiddenFields guard extension"
      pattern: "PlanStatus|PhaseStatus|MilestoneStatus|ProjectStatus"
---

<objective>
Land every CRD-schema addition Phase 52 needs, in one wave-1 plan: the `LoopLevel` enum + `LoopPolicy.Level` (D-02), `PlanSpec.Verification` + `PlanStatus.LoopStatus` (D-01/D-06), the `ProjectSpec` per-level `VerificationSpec` defaults map (D-01), `LoopStatus` embeddings on Phase/Milestone/Project statuses (D-07 discretion, resolved: uniform embedding so every level has a `LastEvaluation` + `ExitReason` surface), and the `shared_types.go` doc-scope generalization of `LevelPhaseVerifying`/`LevelPhaseVerifyHalted` (D-03 discretion, resolved: reuse the existing constants, update their Task-only doc comments).

Purpose: every downstream plan (resolver, plan-check loop, level-verify) compiles against these fields. CRD-YAML diffs are expected this phase (unlike Phase 49).
Output: schema fields + regenerated deepcopy/CRD manifests + extended no-history compile-time guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (sections: loop_types.go, plan_types.go, project_types.go, shared_types.go)
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (Pitfall 4 — CEL immutability on Project-level defaults)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the per-level verification schema fields (D-01/D-02/D-06 + level-status embeddings)</name>
  <files>api/v1alpha3/loop_types.go, api/v1alpha3/plan_types.go, api/v1alpha3/project_types.go, api/v1alpha3/phase_types.go, api/v1alpha3/milestone_types.go, api/v1alpha3/shared_types.go</files>
  <read_first>
    - api/v1alpha3/loop_types.go (full — the `EscalationPolicy`/`AutonomyLevel` enum idiom at lines 166-230 is the exact shape `LoopLevel` copies; `LoopPolicy` at :37-73 receives the new field)
    - api/v1alpha3/task_types.go lines 68-160 and 213-219, 324-330 (the `VerificationSpec` type + its CEL rule at :90; the `TaskSpec.Verification` and `TaskStatus.LoopStatus` embedding doc comments to mirror)
    - api/v1alpha3/plan_types.go (full — `PlanSpec` at :24-48, `PlanStatus` at :56-156 including `WaveIntegrationStatus.Attempts` which the new LoopStatus must stay documented-distinct from)
    - api/v1alpha3/project_types.go lines 39-52 and 365-458 (the `Gates` per-level struct D-01's defaults map mirrors; `ProjectSpec` field placement next to `Gates` at :400-402)
    - api/v1alpha3/phase_types.go, api/v1alpha3/milestone_types.go (locate PhaseStatus/MilestoneStatus for the LoopStatus embeddings)
    - api/v1alpha3/shared_types.go lines 337-380 and 468-511 (the VerifyHalt condition doc block + `LevelPhaseVerifying`/`LevelPhaseVerifyHalted` Task-only doc comments to generalize)
  </read_first>
  <action>
    Five edits, all following the file's own established idioms:

    1. `loop_types.go`: add a `LoopLevel` string enum type with `+kubebuilder:validation:Enum=task;plan;phase;milestone;project` and five constants `LoopLevelTask`, `LoopLevelPlan`, `LoopLevelPhase`, `LoopLevelMilestone`, `LoopLevelProject` — same doc-comment density as `EscalationPolicy`. Add `Level LoopLevel` (json tag `level,omitempty`, `+optional`) to `LoopPolicy`, doc-commented "stamped by the resolver (`ResolveLoopPolicy`), never authored directly — SC3's loop-level field" per D-02.

    2. `plan_types.go`: add `Verification VerificationSpec` (json `verification,omitempty`, `+optional`) to `PlanSpec` — the SAME standalone type from `task_types.go:91`, zero shape changes; the type-attached CEL immutable-once-Locked rule travels automatically. Add `LoopStatus LoopStatus` (json `loopStatus,omitempty`, `+optional`) to `PlanStatus` with a doc comment stating: `Iteration` counts quality re-plans ONLY (D-06) and is DISTINCT from `WaveIntegrationStatus.Attempts` (infra-retry ≠ quality-iteration, Phase 51 D-05 applied one level up) and from the planner Job attempt identity.

    3. `project_types.go`: add a new `VerificationDefaults` struct mirroring `Gates` exactly — five `+optional` pointer fields `Task`, `Plan`, `Phase`, `Milestone`, `Project`, each `*VerificationSpec` (pointers so absence is distinguishable from an empty spec; Gates uses value strings, this map holds structs). Add `Verification VerificationDefaults` (json `verification,omitempty`, `+optional`) to `ProjectSpec` positioned next to `Gates`. The struct doc comment MUST carry the Pitfall-4 warning: entries should be authored at `Phase: "Draft"` (or empty) — a `Locked` default inherits the type-level CEL immutability rule and cannot later be edited without the Locked→Superseded dance; flag this for Phase 53's chart-default authoring.

    4. `phase_types.go` + `milestone_types.go` + `project_types.go`: add `LoopStatus LoopStatus` (json `loopStatus,omitempty`, `+optional`) to `PhaseStatus`, `MilestoneStatus`, and `ProjectStatus` — doc comment notes these levels run `maxIterations:0` so only `LastEvaluation` + `ExitReason` are populated in practice (D-07 discretion, LOOP-03-compliant).

    5. `shared_types.go`: update the `LevelPhaseVerifying` and `LevelPhaseVerifyHalted` doc comments to drop "Task-only" and describe the generalized meaning (Plan enters Verifying for plan-check per D-03; Phase/Milestone/Project enter Verifying pre-Succeeded per D-07; VerifyHalted is where an exhausted loop with escalate policy parks at any level). Extend the `checkVerifyHalt`/`setVerifyHaltIfNeeded` "what SETS it" prose at :337-357 to name the new Plan/Phase/Milestone/Project call sites. Do NOT mint sibling constants or a second halt-condition family.
  </action>
  <verify>
    <automated>go build ./... && go vet ./api/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LoopLevel" api/v1alpha3/loop_types.go` ≥ 7 (type + 5 constants + field)
    - `grep -n "Verification VerificationSpec" api/v1alpha3/plan_types.go` returns exactly 1 line (PlanSpec)
    - `grep -n "LoopStatus LoopStatus" api/v1alpha3/{plan,phase,milestone,project}_types.go` returns 4 lines total (one per status struct)
    - `grep -n "type VerificationDefaults struct" api/v1alpha3/project_types.go` returns 1 line; the struct has 5 `*VerificationSpec` fields
    - `grep -cn "Task-only" api/v1alpha3/shared_types.go` returns 0 for the Verifying/VerifyHalted constants' doc comments (generalized)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>All five schema surfaces exist, compile, and carry the mandated doc comments (D-06 counter distinctness, Pitfall-4 Draft convention, generalized phase-constant scope).</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate deepcopy + CRD manifests and extend the no-history guard</name>
  <files>api/v1alpha3/loop_types_test.go, api/v1alpha3/zz_generated.deepcopy.go, config/crd/bases/</files>
  <read_first>
    - api/v1alpha3/loop_types_test.go (full — `TestLoopStatus_NoForbiddenFields` compile-time struct-literal guard pattern from Phase 49; understand exactly how it pins LoopStatus's field set before extending)
    - Makefile (confirm `manifests` and `generate` target names via `grep -nE '^(manifests|generate):' Makefile`)
  </read_first>
  <action>
    1. Extend `loop_types_test.go` so the LOOP-03 no-history enforcement covers every NEW embedding site: add compile-time assertions (same struct-literal guard style as `TestLoopStatus_NoForbiddenFields`) proving `PlanStatus`, `PhaseStatus`, `MilestoneStatus`, and `ProjectStatus` each embed `LoopStatus` (the guarded type) rather than any widened variant — e.g. a `var _ LoopStatus = PlanStatus{}.LoopStatus` line per status struct, plus keep the existing forbidden-fields literal intact.

    2. Run `make generate` then `make manifests`. CRD-YAML diffs under `config/crd/bases/` are EXPECTED this phase (CONTEXT integration-points note) — commit them. Verify the generated Plan CRD YAML contains the `verification` schema block with the CEL `x-kubernetes-validations` immutability rule text ("immutable once Locked"), and the Project CRD contains the five per-level `verification` default slots.

    3. Run the full api-package test suite.
  </action>
  <verify>
    <automated>make generate && make manifests && go test ./api/v1alpha3/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./api/v1alpha3/... -run TestLoopStatus` exits 0 and the guard file references all four new status structs (`grep -c "PlanStatus\|PhaseStatus\|MilestoneStatus\|ProjectStatus" api/v1alpha3/loop_types_test.go` ≥ 4)
    - `grep -rn "immutable once Locked" config/crd/bases/*plans.yaml` returns ≥ 1 (CEL rule propagated to the Plan CRD)
    - `grep -c "loopStatus" config/crd/bases/*.yaml | grep -v ":0"` shows loopStatus present in plan/phase/milestone/project CRDs
    - `git status --porcelain config/crd api/v1alpha3` shows only expected generated + edited files (no stray artifacts)
  </acceptance_criteria>
  <done>Deepcopy + CRD manifests regenerated and committed; the LOOP-03 guard structurally covers all four new LoopStatus embeddings; api test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/chart → Project.Spec.Verification | admin-authored per-level defaults become verification contracts consumed at dispatch time |
| planner subagent → Plan.Spec.Verification | LLM-authored child specs may carry a verification contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | VerificationSpec at new embedding sites | mitigate | Type-level CEL immutable-once-Locked rule travels automatically to Plan.Spec and Project defaults (D-01); Task 2 asserts the rule text appears in generated CRD YAML |
| T-52-02 | Tampering | Project-level Locked defaults (Pitfall 4) | accept | Convention documented in the VerificationDefaults doc comment (author at Draft); enforced schema change is locked out by D-01 — residual risk flagged to Phase 53 chart authoring |
| T-52-03 | Repudiation/Info | LoopStatus embeddings ballooning etcd | mitigate | LOOP-03 compile-time guard extended to all four new embeddings in Task 2 |
</threat_model>

<verification>
- `go build ./...` and `go test ./api/v1alpha3/...` green
- `make manifests` produces committed CRD diffs (expected this phase)
- `make lint` green (release-cascade lesson: run ci.yaml-only gates per phase, not at release pre-flight)
</verification>

<success_criteria>
- All D-01/D-02/D-06 schema fields exist with exact names pinned above; downstream plans compile against them
- CEL immutability propagated to every new VerificationSpec site; Pitfall-4 Draft convention documented
- LOOP-03 no-history guard covers PlanStatus/PhaseStatus/MilestoneStatus/ProjectStatus
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-01-SUMMARY.md` when done.
</output>
