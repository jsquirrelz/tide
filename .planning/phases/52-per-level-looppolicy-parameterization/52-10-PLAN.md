---
phase: 52-per-level-looppolicy-parameterization
plan: 10
type: execute
wave: 5
depends_on: ["52-08"]
files_modified:
  - internal/controller/phase_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/project_controller.go
  - internal/controller/level_verify_dispatch_test.go
  - internal/controller/verify_halt_test.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "Phase, Milestone, and Project dispatch a level-verify at the moment they would otherwise stamp Succeeded — Succeeded only fires after an APPROVED verdict; a level with no resolved contract behaves exactly as today (D-07, SC2)"
    - "A non-APPROVED level verdict escalates with ZERO auto-repair: default → AwaitingApproval (requireApproval); authored escalate → ConditionVerifyHalt (SC2, D-08)"
    - "Verify runs BEFORE the boundary push — unverified outcomes are never published outward"
    - "A VerifyHalt at the new levels never reinterprets Failed wave semantics: siblings and conservative-profile propagation are untouched (ESC-03 regression)"
  artifacts:
    - path: "internal/controller/phase_controller.go"
      provides: "maybeRunLevelVerify wired at every patchPhaseSucceeded call site"
      contains: "maybeRunLevelVerify"
    - path: "internal/controller/level_verify_dispatch_test.go"
      provides: "Ginkgo LevelVerify specs for all three levels"
      contains: "LevelVerify"
  key_links:
    - from: "internal/controller/{phase,milestone,project}_controller.go"
      to: "internal/controller/level_verify.go"
      via: "each pre-Succeeded seam calls the ONE shared entry"
      pattern: "maybeRunLevelVerify"
    - from: "internal/controller/verify_halt_test.go"
      to: "new-level halts"
      via: "ESC-03 sibling/conservative-profile regression extension"
      pattern: "Sibling"
---

<objective>
Wire 52-08's shared level-verify machinery into the three reconcilers' pre-Succeeded seams — Phase (the 3 `patchPhaseSucceeded` call-site family at :773-849), Milestone (its `patchMilestoneSucceeded` sites), Project (`checkProjectComplete`) — with verify ordered BEFORE `maybeTriggerBoundaryPush` at each site (publish only verified outcomes), and pin SC2 + ESC-03 in envtest.

Purpose: SC2 becomes live — these levels close on their observable outcome, verified, with escalation instead of repair.
Output: three wired controllers + LevelVerify Ginkgo specs + ESC-03 regression extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (section: phase/milestone/project controllers — the boundary-push-before-Succeeded seam excerpt + the errGitWriterBusy requeue precedent)
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md  (D-07, D-08, D-10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the three pre-Succeeded seams</name>
  <files>internal/controller/phase_controller.go, internal/controller/milestone_controller.go, internal/controller/project_controller.go</files>
  <read_first>
    - internal/controller/level_verify.go (52-08's maybeRunLevelVerify signature + levelVerifyTarget contract — the exact call each seam makes)
    - internal/controller/phase_controller.go lines 760-890 (all patchPhaseSucceeded call sites + the BoundaryDetected/maybeTriggerBoundaryPush ordering + the errGitWriterBusy requeue idiom)
    - internal/controller/milestone_controller.go lines 250-290 and 800-920 (its patchMilestoneSucceeded call-site family + triggerArtifactPush usage)
    - internal/controller/project_controller.go lines 505-596 (checkProjectComplete — where the project would stamp complete)
    - internal/controller/level_status.go (which status fields/conditions each level exposes for the levelVerifyTarget accessors)
  </read_first>
  <action>
    At EVERY call site in the three files where the level is about to stamp Succeeded/complete (grep each file for its patch*Succeeded calls to enumerate them — the instruction applies to every site, not just the first): insert `handled, res, err := maybeRunLevelVerify(ctx, r.Client, r.Deps, project, target)` with the level's own target struct (Obj, Conditions, PhasePtr = the level's Status.Phase field pointer, LoopStatus = &status.LoopStatus, Level = "phase"/"milestone"/"project", Goal = the level's authored goal/prompt text from its Spec). handled=true → return the machinery's result (verify in flight or terminal routed); handled=false → fall through to the EXISTING code path unchanged (no contract, or verdict already concluded via the ExitReason guard).

    Ordering rule at each site: the maybeRunLevelVerify call goes BEFORE `BoundaryDetected`/`maybeTriggerBoundaryPush`/`triggerArtifactPush` — an unverified outcome must not publish; once the verdict is APPROVED (handled=false with ExitReason set), the existing push-then-patchSucceeded flow runs untouched. Where the machinery defers (cap-hit/still-running), return `ctrl.Result{RequeueAfter: …}, nil` per the errGitWriterBusy transient-defer precedent — never propagate a transient as an error.

    Keep each seam edit minimal: no logic beyond the call + branch lands in the controller files (the machinery owns everything — SC3's no-kind-switch bar; the three files must contain zero gate-policy conditionals).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run TestControllers --ginkgo.focus='LevelVerify' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "maybeRunLevelVerify" internal/controller/phase_controller.go` equals the number of patchPhaseSucceeded call sites (every seam, not just the first — enumerate via grep and match counts); same rule for milestone/project files
    - `grep -n "MaxIterations\|EscalationPolicy" internal/controller/{phase,milestone,project}_controller.go` = 0 matches (no policy logic leaked into the controllers)
    - In each file the maybeRunLevelVerify call textually precedes the boundary/artifact push trigger at its site
  </acceptance_criteria>
  <done>All three levels verify-before-Succeeded through the one shared entry; zero policy conditionals in the controller files.</done>
</task>

<task type="auto">
  <name>Task 2: LevelVerify Ginkgo specs + ESC-03 regression extension</name>
  <files>internal/controller/level_verify_dispatch_test.go, internal/controller/verify_halt_test.go</files>
  <read_first>
    - internal/controller/task_verify_dispatch_test.go (fixture idiom for completing Jobs + writing verdict envelopes)
    - internal/controller/verify_halt_test.go (the existing ESC-03 sibling/conservative-profile assertions to extend to new levels)
    - internal/controller/plan_verify_dispatch_test.go (52-07's contracted-fixture setup via Project.Spec.Verification defaults — reuse for phase/milestone/project contracts)
  </read_first>
  <action>
    1. New `level_verify_dispatch_test.go` with `Describe("LevelVerify", …)`: (a) a Phase whose children all Succeeded, with a Project-level phase contract authored (Project.Spec.Verification.Phase, Locked + GateCommand) → phase enters Verifying, a `tide-verifier-phase-<uid>-1` Job exists, and Succeeded is NOT stamped while the Job runs; (b) APPROVED verdict → phase Succeeded, LoopStatus.ExitReason approved; (c) REPAIRABLE verdict (default policy) → phase AwaitingApproval (SC2's requireApproval default), project ConditionVerifyHalt NOT set; approve annotation → phase proceeds to Succeeded with NO second verifier Job (convergence guard); (d) authored `onExhaustion: escalate` + BLOCKED verdict → phase VerifyHalted + project ConditionVerifyHalt True; (e) no contract anywhere → phase stamps Succeeded exactly as today with zero verifier Jobs (off-switch pin); (f) one milestone-level and one project-level happy-path spec (contract → Verifying → APPROVED → Succeeded/complete) proving the same machinery serves all three (D-10 rails already pinned at the plan site in 52-07; assert here that the level Jobs carry role=verifier + the project label so verifierInFlightCount counts them).

    2. Extend `verify_halt_test.go` for ESC-03 at the new levels: a phase-level VerifyHalt (case (d)) leaves a SIBLING phase's status/dispatch untouched and does not flip any Failed-propagation machinery (conservative-profile FailureHalt untouched, wave semantics unchanged) — mirror the existing Task-level sibling assertions one level up.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestControllers --ginkgo.focus='LevelVerify|VerifyHalt' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Focus run reports ≥ 8 specs ran, 0 failures
    - Spec (c) asserts the full requireApproval arc: park → approve → Succeeded → no re-verify (Job count for the phase's verifiers stays 1)
    - Spec (e) pins today's behavior byte-for-byte (no Verifying phase transition observed)
    - The ESC-03 extension asserts sibling phase status AND ConditionFailureHalt absence in the same spec
  </acceptance_criteria>
  <done>SC2 + ESC-03 pinned at all three new levels in envtest; live worktree behavior remains 52-11's scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| level closure → outward publish (boundary push) | an unverified level outcome could publish to the operator's remote |
| operator approve → level closure | human sign-off closes a milestone/project despite findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-31 | Tampering | publishing an unverified run-branch outcome | mitigate | maybeRunLevelVerify ordered before boundary/artifact push at every seam; acceptance criterion checks textual ordering per site |
| T-52-32 | Elevation of Privilege | approval-gate bypass at the onExhaustion split (level closes without APPROVED verdict or human approval) | mitigate | Only two routes to Succeeded exist: APPROVED verdict, or consume-approve after park — spec (c) pins the arc; the consume-annotation-first race protection is 52-06's reused machinery |
| T-52-33 | DoS | requeue storms from a perpetually-deferring verify (cap-hit) | accept | Bounded RequeueAfter mirrors the errGitWriterBusy precedent; cap pressure resolves as sibling verifiers terminate |
| T-52-34 | Repudiation | ESC-03 drift — VerifyHalt bleeding into Failed semantics at new levels | mitigate | verify_halt_test.go extension asserts sibling + conservative-profile invariance |
</threat_model>

<verification>
- Full envtest suite green; `make lint` green
</verification>

<success_criteria>
- SC2 live at all three levels through one shared machine; off-switch behavior pinned
- ESC-03 regression coverage extended to the newly-verified levels (CONTEXT D-08's explicit requirement)
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-10-SUMMARY.md` when done.
</output>
