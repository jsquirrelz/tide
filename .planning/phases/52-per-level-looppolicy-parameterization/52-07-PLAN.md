---
phase: 52-per-level-looppolicy-parameterization
plan: 07
type: execute
wave: 4
depends_on: ["52-03", "52-04", "52-05", "52-06"]
files_modified:
  - internal/controller/plan_controller.go
  - internal/controller/dispatch_helpers.go
  - internal/controller/plan_verify_dispatch_test.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "A Plan with a resolved verification contract enters Verifying after planner completion + child materialization, and NO child Task dispatches until the plan-check verdict is APPROVED (D-03)"
    - "The plan-check verifier dispatch is counted by verifierInFlightCount, reserves through the shared ReservationStore, and consumes verdicts through the fail-closed ClassifyVerdict (D-10)"
    - "A Plan with no resolved contract behaves exactly as today — no Verifying phase, no verifier Job (absence of config is the off-switch)"
    - "The plan-check EVALUATOR sibling span is emitted with name tide.dispatch.plan.verify (D-10)"
  artifacts:
    - path: "internal/controller/plan_controller.go"
      provides: "checkPlanVerifyingState / dispatchPlanVerifier / handlePlanVerifierCompletion / markPlanVerifiedApproved"
      contains: "dispatchPlanVerifier"
    - path: "internal/controller/dispatch_helpers.go"
      provides: "checkParentApproval extended: Plan Verifying holds child Task dispatch"
      contains: "LevelPhaseVerifying"
    - path: "internal/controller/plan_verify_dispatch_test.go"
      provides: "Ginkgo PlanCheck dispatch/consume/rails specs"
      contains: "PlanCheck"
  key_links:
    - from: "internal/controller/plan_controller.go"
      to: "podjob.BuildJobSpec"
      via: "BuildOptions{Kind: JobKindVerifier, ParentObj: plan, Level: plan, WorktreeCheckoutImage/WorktreeBranch}"
      pattern: "JobKindVerifier"
    - from: "internal/controller/task_controller.go gateChecks"
      to: "dispatch_helpers.go checkParentApproval"
      via: "existing call site — the new Verifying OR-clause needs NO new caller"
      pattern: "checkParentApproval"
---

<objective>
Build the plan-check half of the loop: the Plan `Verifying` sub-state entered after planner completion + child materialization (D-03's cheapest pre-spend rejection point), the `checkParentApproval` hold extension that structurally blocks child Task dispatch during Verifying, and the dispatch/consume state machine mirroring Task's (`checkVerifyingState` → `dispatchVerifier` → `handleVerifierCompletion`), riding every D-10 rail unchanged. Non-APPROVED verdicts route to the shared `exhaustVerifyLoop` in this plan (conservative fail-closed intermediate); 52-09 inserts `repairOrHaltPlan` in front of it.

Purpose: SC1's dispatch side — a bad plan is rejected before any child Task spends money.
Output: plan-check dispatch/consume + Verifying hold + Ginkgo rails proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (section: plan_controller.go — points 1-4, 8, 9: the exact Task-side excerpts to mirror)
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (§Extending checkParentApproval; Code Examples §checkParentApproval-style hold)
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md  (D-03, D-10)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verifying entry + the child-dispatch hold</name>
  <files>internal/controller/plan_controller.go, internal/controller/dispatch_helpers.go</files>
  <read_first>
    - internal/controller/plan_controller.go lines 121-300 (Reconcile's dispatch ordering + reconcilePlannerDispatch's AwaitingApproval early-return at :236-263 — the placement-discipline comment at :237-241 — and the tasks-exist early-return at :267-276), lines 830-945 (handlePlannerJobCompletion + the "reporter still materializing Task children" seam at ~:866 and the phase-clear-to-empty at :907-919 — the exact point Verifying replaces "")
    - internal/controller/dispatch_helpers.go lines 570-604 (checkParentApproval — the case "Plan": arm gaining the OR-clause)
    - internal/controller/task_controller.go lines 440-460 (the existing checkParentApproval call site in gateChecks — read-only; confirms no new caller is needed)
  </read_first>
  <action>
    1. `handlePlannerJobCompletion` (plan_controller.go): at the post-materialization point where the Plan's phase currently clears to "", resolve `spec := ResolveVerificationSpec(project, plan, nil, "plan")`; if the contract is active (`spec.GateCommand != "" && spec.Phase == "Locked"` — the uniform hasVerificationContract activation rule) set `plan.Status.Phase = LevelPhaseVerifying` instead of ""; otherwise keep today's behavior byte-for-byte. Only transition to Verifying when the reporter has FINISHED materializing children (the :866 seam's existing completion condition — the rubric needs the authored child specs to exist, D-03).

    2. `checkParentApproval` (dispatch_helpers.go): extend the `case "Plan":` arm to also return true when `plan.Status.Phase == LevelPhaseVerifying` (the RESEARCH Code-Examples diff — one OR-clause). This is the SOLE hold mechanism; the existing gateChecks call site needs no change.

    3. `reconcilePlannerDispatch`: add a `LevelPhaseVerifying` early-return at the same top-of-function position as the AwaitingApproval check (same placement rationale as the :237-241 comment — it guards the crash window between a future re-plan's child deletion and its status patch; without it a mid-transition reconcile could double-dispatch a planner).

    4. Wire the Verifying branch into the Plan Reconcile flow: when `plan.Status.Phase == LevelPhaseVerifying`, route to `checkPlanVerifyingState` (Task 2) before any wave/dispatch processing.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run TestControllers --ginkgo.focus='PlanCheck' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LevelPhaseVerifying" internal/controller/dispatch_helpers.go` shows the Plan-case OR-clause
    - `grep -n "LevelPhaseVerifying" internal/controller/plan_controller.go` shows the entry transition + the reconcilePlannerDispatch early-return + the Reconcile routing (≥ 3 sites)
    - A no-contract Plan's post-materialization phase remains "" (spec asserted in Task 3)
  </acceptance_criteria>
  <done>Verifying entered only for contracted Plans after materialization; child Task dispatch structurally held; planner re-dispatch guarded during Verifying.</done>
</task>

<task type="auto">
  <name>Task 2: dispatchPlanVerifier + handlePlanVerifierCompletion (D-10 rails, fail-closed)</name>
  <files>internal/controller/plan_controller.go</files>
  <read_first>
    - internal/controller/task_controller.go lines 650-700 (checkVerifyingState's NotFound→dispatch / terminal→consume / running→no-op three-way), lines 2099-2240 (dispatchVerifier — the cap-before-reserve ordering at :2119-2143 is load-bearing, the AlreadyExists-as-success guard, the post-BuildJobSpec label stamp idiom), lines 2242-2320 (buildVerifierEnvelopeIn — ordered-commands-union + orchestrator-side prompt render), lines 2855-2929 (handleVerifierCompletion — the unreadable-envelope/nil-verdict fail-closed guards + the three-tier ClassifyVerdict switch + hasDeterministicFailure dominance)
    - internal/controller/span_emission.go lines 300-380 (synthesizeEvaluatorSpan — reuse with level "plan" so the span name is tide.dispatch.plan.verify)
    - internal/controller/task_controller.go around markVerifiedSucceeded + settleVerifierSpend (the approved-path stamping/settling to mirror)
    - api/v1alpha3/plan_types.go (PlanSpec fields — locate the Plan's authored goal/prompt text field for the .PlanGoal render value)
  </read_first>
  <action>
    Mirror the Task state machine function-for-function on PlanReconciler:

    1. `checkPlanVerifyingState(ctx, plan, project)`: Get Job by `podjob.VerifierJobName("plan", string(plan.UID), attempt)` where `attempt = int(plan.Status.LoopStatus.Iteration) + 1` (the D-06 counter doubles as the verifier attempt — Plan has no pre-existing infra-retry identity to preserve, RESEARCH Open Question 3's resolved answer; doc-comment this). NotFound → dispatchPlanVerifier (idempotent via deterministic name); terminal → handlePlanVerifierCompletion; running → requeue no-op.

    2. `dispatchPlanVerifier`: cap-before-reserve copied verbatim — `verifierInFlightCount(ctx, r.Client, plan.Namespace, project.Name, jobName)` against `defaultVerifierConcurrencyCap`, requeue on cap; then `r.Deps.Reservations.Reserve(string(plan.UID), r.Deps.ReserveEstimateCents)` with the release-on-error path. Build the envelope via a new `buildPlanVerifierEnvelopeIn`: Role "verifier", Level "plan", Verify populated from `ResolveVerificationSpec(project, plan, nil, "plan")` (GateCommand + ordered commands union + RequiredArtifacts + Evaluator), Branch from `project.Status.Git` run-branch field. Render the prompt orchestrator-side via `common.LoadPromptTemplate("verifier", "plan")` executed with the 52-03 pinned struct: `Verify` (the VerifyContext), `PlanGoal` (the Plan's authored goal/prompt text), `Children` (bounded summary built from a List of the Plan's child Tasks: Name, DependsOn, Files/declared paths, GateCommand — cap at 50 children). BuildOptions: `Kind: JobKindVerifier, ParentObj: plan, Level: "plan", Attempt: attempt, ReadOnly: true, SubagentImage: r.Deps.VerifierImage, WorktreeCheckoutImage: r.Deps.TidePushImage, WorktreeBranch: <run branch>` plus the provider fields ResolveProvider(project, "plan", …) supplies at the Task site. Post-BuildJobSpec label stamping mirrors dispatchVerifier's owner.LabelProject idiom. Guard: if `r.Deps.VerifierImage == ""` log-and-skip exactly as Task's :2113 guard does.

    3. `handlePlanVerifierCompletion`: read the verifier's out envelope with the SAME role-aware reader Task uses (Phase 51's ReadVerifierOut graft — grep task_controller.go for the reader call and reuse it); unreadable envelope or nil Verdict → fail-closed to the exhaustion path, never APPROVED. Three-tier switch on `pkgdispatch.ClassifyVerdict`: APPROVED without deterministic failure → `markPlanVerifiedApproved` (stamp LoopStatus.LastEvaluation + ExitReason approved, clear phase Verifying→"", settle the reservation, emit the evaluator span via synthesizeEvaluatorSpan with level "plan"); APPROVED with `hasDeterministicFailure` OR REPAIRABLE → route to the exhaustion path FOR NOW via `exhaustVerifyLoop(..., ResolveLoopPolicy(project, plan, nil, "plan"), ...)` with a `// 52-09 replaces this with repairOrHaltPlan` marker comment; BLOCKED/default → same exhaustion path. Every exit settles/releases the reservation exactly once (mirror settleVerifierSpend's placement).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/controller/... -run TestControllers --ginkgo.focus='PlanCheck' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "verifierInFlightCount" internal/controller/plan_controller.go` shows the cap check BEFORE the Reserve call (read the diff to confirm ordering)
    - `grep -n "ClassifyVerdict" internal/controller/plan_controller.go` shows exactly one three-tier switch; the default arm routes to exhaustion, never markPlanVerifiedApproved
    - `grep -n "52-09 replaces" internal/controller/plan_controller.go` = 1 (the marked seam for repairOrHaltPlan)
    - `grep -n "WorktreeCheckoutImage" internal/controller/plan_controller.go` = 1 (worktree provisioning wired for the plan verifier)
  </acceptance_criteria>
  <done>Plan-check dispatch/consume complete on the Task-precedent shape with every rail wired; REPAIRABLE conservatively escalates until 52-09.</done>
</task>

<task type="auto">
  <name>Task 3: Ginkgo PlanCheck specs — hold, rails, fail-closed, off-switch</name>
  <files>internal/controller/plan_verify_dispatch_test.go</files>
  <read_first>
    - internal/controller/task_verify_dispatch_test.go (full — the Phase-51 fixture idiom: how specs fabricate Locked contracts, complete Jobs, write out-envelopes, and assert dispatch effects; extend the same shape, don't fork)
    - internal/controller/suite_test.go lines 100-130 (TestControllers is the sole entry point — Pitfall 5)
  </read_first>
  <action>
    New `plan_verify_dispatch_test.go` with a `Describe("PlanCheck", …)` covering: (a) contracted Plan completes planner + materialization → phase Verifying, a `tide-verifier-plan-<uid>-1` Job exists, and a child Task in the Plan does NOT dispatch (its gateChecks hold returns held — assert no executor Job for it); (b) no-contract Plan → phase "" and zero verifier Jobs (today's behavior pinned); (c) rails: with `defaultVerifierConcurrencyCap` verifier Jobs already in-flight for the project, the plan-check dispatch defers (no Job created, requeue) — and the Reservation store shows a reservation after a successful dispatch and release/settle after consumption; (d) fail-closed: a terminal verifier Job with a malformed/empty out envelope drives the Plan to the exhaustion path (VerifyHalted or AwaitingApproval per resolved policy), NEVER to phase "" with dispatch unblocked; (e) APPROVED verdict → phase clears to "", child Task dispatch proceeds (executor Job appears).
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run TestControllers --ginkgo.focus='PlanCheck' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Focus run reports ≥ 5 specs ran (not vacuous — Pitfall 5), 0 failures
    - Spec (a) asserts BOTH the Verifying phase AND the absence of child executor Jobs (the D-03 pre-spend invariant)
    - Spec (d) asserts the malformed-envelope path cannot reach the unblocked state (fail-closed proof at the new site, D-10)
  </acceptance_criteria>
  <done>Plan-check dispatch/hold/rails/fail-closed pinned in envtest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner-authored child specs → plan-check verifier | LLM-authored artifacts are judged by an out-of-process evaluator whose verdict gates all child spend |
| verifier Job out-envelope → controller verdict consumption | untrusted file content crosses into dispatch-gating state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-19 | Elevation of Privilege | child Tasks dispatching while the plan is unverified (spends budget on rejected work) | mitigate | checkParentApproval Verifying OR-clause + spec (a)'s no-executor-Job assertion; the hold is structural, not advisory |
| T-52-20 | Tampering | malformed/empty verdict collapsing to APPROVED | mitigate | ClassifyVerdict fail-closed default + nil-Verdict guard mirrored from Task; spec (d) pins it |
| T-52-21 | DoS (budget) | plan-check verifiers escaping the concurrency cap / reservation pool | mitigate | Same verifierInFlightCount + shared ReservationStore (52-04's single-instance wiring); spec (c) pins count + reserve |
| T-52-22 | Spoofing | verifier Job name collision across levels | mitigate | 52-02's three-segment name includes the level; deterministic per (level, planUID, attempt) |
</threat_model>

<verification>
- `go test ./internal/controller/... -run TestControllers -count=1` full suite green
- `make lint` green
</verification>

<success_criteria>
- D-03's trigger + hold land exactly as specified; absence of config = today's behavior
- Every D-10 rail proven at the new site by a spec, not re-implemented
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-07-SUMMARY.md` when done.
</output>
