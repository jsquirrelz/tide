---
phase: 52-per-level-looppolicy-parameterization
plan: 05
type: execute
wave: 2
depends_on: ["52-02"]
files_modified:
  - pkg/git/worktree.go
  - pkg/git/worktree_test.go
  - cmd/tide-push/main.go
  - internal/dispatch/podjob/jobspec.go
  - internal/dispatch/podjob/jobspec_test.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "A read-only, detached worktree checkout at the run-branch tip can be provisioned for a level that never ran an executor (RESEARCH Pitfall 2 — the no-analog mechanism)"
    - "The checkout runs as an init container OUTSIDE the Python verifier image (EVAL-01 import firewall) and outside the manager process"
    - "The checkout init container carries no git-push credentials and no ANTHROPIC_API_KEY (RESEARCH threat: EoP)"
    - "Task-level verifier Jobs get NO checkout init container — their worktree is inherited from the executor"
  artifacts:
    - path: "pkg/git/worktree.go"
      provides: "AddReadOnlyWorktree — detached, no new branch, idempotent"
      contains: "func AddReadOnlyWorktree"
    - path: "cmd/tide-push/main.go"
      provides: "worktree-checkout mode invocable as an init container command"
      contains: "worktree-checkout"
    - path: "internal/dispatch/podjob/jobspec.go"
      provides: "conditional second init container on verifier Jobs"
      contains: "WorktreeCheckoutImage"
  key_links:
    - from: "internal/dispatch/podjob/jobspec.go"
      to: "cmd/tide-push worktree-checkout mode"
      via: "init container command args"
      pattern: "worktree-checkout"
    - from: "cmd/tide-push/main.go"
      to: "pkg/git.AddReadOnlyWorktree"
      via: "Go call (logic testable in pkg/git, not shell)"
      pattern: "AddReadOnlyWorktree"
---

<objective>
Build the level-verify worktree-provisioning mechanism — RESEARCH's flagged no-analog new-build (Assumption A1), resolved here as: (a) a new `pkg/git.AddReadOnlyWorktree` doing a DETACHED checkout (`git worktree add --detach <dir> <runBranch>` — no `-b`, so no dangling `tide/wt-*` branches AND no "branch already checked out" collision when multiple levels verify concurrently); (b) a new `worktree-checkout` mode on the existing `cmd/tide-push` binary (reuses the already-built, already-deployed git-capable image — RESEARCH Open Question 1's cheaper option — keeping logic in testable Go, no shell assumptions); (c) a conditional second init container composed in `BuildJobSpec` for verifier Jobs when the caller sets the new `BuildOptions.WorktreeCheckoutImage` + `WorktreeBranch` fields.

Purpose: without this, every Phase/Milestone/Project (and plan-level) verify finds an empty directory and escalates on first run regardless of project state (Pitfall 2). Plan-check (52-07) and level-verify (52-08) both set these options; Task dispatch never does.
Output: git helper + binary mode + jobspec composition + static tests. Live PVC behavior is proven in 52-11 (envtest cannot observe a real PVC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (§"The missing worktree" — the 4-point recommended shape; Security Domain row 4)
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (section: pkg/git/worktree.go — the AddWorktree body to mirror; §No Analog Found)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pkg/git.AddReadOnlyWorktree — detached, idempotent, guarded</name>
  <files>pkg/git/worktree.go, pkg/git/worktree_test.go</files>
  <behavior>
    - empty repoPath / uid / runBranch → error (same three guards as AddWorktree)
    - fresh call against a real bare repo fixture → worktree dir exists at filepath.Join(filepath.Dir(repoPath), "worktrees", uid), HEAD resolves to the runBranch tip commit, `git branch --show-current` in the worktree is empty (detached)
    - second call with same args (idempotency — retried init container) → returns the same dir, nil error, no git failure
    - two different uids against the SAME runBranch → both succeed (the --detach no-branch-claim property; a `-b`-style or plain branch checkout would fail the second with "already checked out")
    - no new branch is created in the repo (`git branch --list 'tide/wt-*'` count unchanged)
  </behavior>
  <read_first>
    - pkg/git/worktree.go (full — AddWorktree at :59-79: the guards, CombinedOutput error-wrap pattern, and directory convention to mirror; TaskBranchName NOT used here)
    - pkg/git/worktree_test.go (existing fixture idiom for creating throwaway bare repos in t.TempDir())
  </read_first>
  <action>
    Add `AddReadOnlyWorktree(repoPath, uid, runBranch string) (string, error)`: same three empty-string guards and error-wrap style as AddWorktree; directory convention `filepath.Join(filepath.Dir(repoPath), "worktrees", uid)` (keyed by the checked level's own UID, mirroring envelopeUID); idempotency pre-check — if the worktree dir already contains a resolvable checkout (`git -C <dir> rev-parse --git-dir` succeeds), return the dir with nil error; otherwise run `git -C <repoPath> worktree add --detach <dir> <runBranch>`. Doc comment states WHY --detach (verify-only checkout: no branch minted — avoids AddWorktree's dangling-branch anti-pattern from RESEARCH — and no branch-claim, so concurrent level verifies against the same run branch never collide). Write the test file first (real git CLI against t.TempDir() fixtures, matching the existing test idiom), then implement to green.
  </action>
  <verify>
    <automated>go test ./pkg/git/... -run TestAddReadOnlyWorktree -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior cases pass as named subtests
    - `grep -n "worktree add --detach\|\"--detach\"" pkg/git/worktree.go` returns ≥ 1 (detached form, no -b anywhere in the new function)
    - Idempotent re-call subtest proves nil error on second invocation
  </acceptance_criteria>
  <done>Helper implemented + tested against real git; detached/idempotent/no-new-branch proven.</done>
</task>

<task type="auto">
  <name>Task 2: tide-push checkout mode + verifier-Job init container composition</name>
  <files>cmd/tide-push/main.go, internal/dispatch/podjob/jobspec.go, internal/dispatch/podjob/jobspec_test.go</files>
  <read_first>
    - cmd/tide-push/main.go (full — existing flag/env parsing idiom and exit-code conventions; the new mode must match its style)
    - internal/dispatch/podjob/jobspec.go lines 540-620 (the `initContainers := []corev1.Container{envelopeWriter}` composition at ~:558; the workspace volume/volumeMount names and the bare-repo path convention used by executor mounts — extract the exact constants for repo path + mount name)
    - internal/harness/worktree.go lines 30-90 (how EnsureWorktree derives workspaceRoot/repoPath — the SAME path convention the init container must target)
    - internal/dispatch/podjob/jobspec_test.go (the TestBuildJobSpec_Verifier_* static-assertion idiom from Phase 48/51 to extend)
  </read_first>
  <action>
    1. `cmd/tide-push/main.go`: add a `worktree-checkout` mode selected by a mode flag (follow the binary's existing flag idiom — e.g. `--mode=worktree-checkout` with `--repo`, `--uid`, `--branch` string flags, or the closest match to its current arg style). The mode calls `pkggit.AddReadOnlyWorktree(repo, uid, branch)` and exits 0 on success / non-zero with the wrapped error on stderr. No push, no credentials, no network access in this mode.

    2. `internal/dispatch/podjob/jobspec.go`: add two BuildOptions fields — `WorktreeCheckoutImage string` and `WorktreeBranch string` (doc: set together by non-task verifier dispatch sites; empty = no checkout init container; task-level verifiers inherit the executor's worktree and never set these). In BuildJobSpec, when `opts.Kind == JobKindVerifier` and both fields are non-empty, append a second init container named `worktree-checkout` AFTER envelopeWriter: image = WorktreeCheckoutImage, command/args invoking the tide-push worktree-checkout mode with repo = the existing bare-repo path constant, uid = parentUID, branch = opts.WorktreeBranch; mount the workspace volume READ-WRITE at the same mount path the executor uses (the worktrees parent must be writable — note the verifier MAIN container's ReadOnly workspace mount is untouched); env EMPTY except anything the existing init container idiom requires — explicitly NO ANTHROPIC_API_KEY, no credproxy coupling, no push-credential env/volumes (scope: local bare-repo read only).

    3. Extend jobspec_test.go with TestBuildJobSpec_Verifier_WorktreeCheckout statics: (a) fields set → second init container present with expected image/args ordering after envelope-writer, workspace mounted RW in it, main container workspace still ReadOnly; (b) credential-absence: init container env contains no ANTHROPIC_API_KEY and no secret refs (mirror Phase 48's credential-absence regression style); (c) fields empty (task-shaped dispatch) → exactly one init container (envelope-writer only).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/dispatch/podjob/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go run` -level check: `grep -n "WorktreeCheckoutImage\|WorktreeBranch" internal/dispatch/podjob/jobspec.go` shows both fields + one composition site gated on Kind==JobKindVerifier && both non-empty
    - TestBuildJobSpec_Verifier_WorktreeCheckout passes all three statics (present/credential-absence/absent)
    - `grep -n "ANTHROPIC" internal/dispatch/podjob/jobspec.go` — no match inside the worktree-checkout container construction
    - cmd/tide-push builds; its checkout mode is reachable (`go build ./cmd/tide-push`)
  </acceptance_criteria>
  <done>Init-container mechanism composed and statically tested; live PVC proof deferred to 52-11 by design (Pitfall 2's envtest-blindness warning).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init container → shared workspace PVC | a git process writes worktree state into the PVC other Jobs read |
| controller → init container args | branch/uid values flow from CRD status into an exec'd git command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-12 | Elevation of Privilege | checkout container over-credentialed (RESEARCH Security row 4) | mitigate | Env explicitly empty of ANTHROPIC_API_KEY/push credentials; credential-absence static test is a hard gate; tide-push checkout mode does no network/push |
| T-52-13 | Tampering | branch/uid args injected into git argv | mitigate | Values pass as discrete exec args via AddReadOnlyWorktree (no shell interpolation anywhere in the path); empty-string guards reject blank args |
| T-52-14 | DoS | worktree accumulation on the PVC across verify runs | accept | Mirrors the existing executor-worktree lifecycle (no cleanup exists there either); bounded by one dir per level UID; noted for a future hygiene phase |
</threat_model>

<verification>
- `go test ./pkg/git/... ./internal/dispatch/podjob/...` green; `make lint` green
</verification>

<success_criteria>
- The no-analog mechanism exists with its scoped design decisions made (detached checkout, tide-push image reuse, init-container composition point) and statically tested
- Task-level dispatch shape provably unchanged (single init container)
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-05-SUMMARY.md` when done.
</output>
