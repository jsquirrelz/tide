---
phase: 52-per-level-looppolicy-parameterization
plan: 06
type: execute
wave: 3
depends_on: ["52-02", "52-04"]
files_modified:
  - internal/controller/task_controller.go
  - internal/controller/level_status.go
  - internal/controller/dispatch_helpers_loop_policy_test.go
  - internal/controller/task_verify_dispatch_test.go
  - cmd/tide/approve.go
autonomous: true
requirements: [ESC-01]

must_haves:
  truths:
    - "onExhaustion: requireApproval parks the checked level at AwaitingApproval through the EXISTING gate machinery; escalate fires the project-wide ConditionVerifyHalt — differentiated in ONE shared place (D-08)"
    - "Task inherits the per-value split from the same shared code path; at default settings (onExhaustion unset) Task behavior is byte-identical to Phase 51 (D-02's no-regression bar, escalate default)"
    - "An approved verify-exhausted Task proceeds to Succeeded — the executor is never re-dispatched after approval (no resurrect loop)"
    - "tide approve targets a parked Project deterministically: chain order Project → Milestone → Phase → Plan → Task (RESEARCH Security row 3)"
  artifacts:
    - path: "internal/controller/level_status.go"
      provides: "shared exhaustVerifyLoop — the ONE exhaustion/escalation branch point"
      contains: "func exhaustVerifyLoop"
    - path: "internal/controller/task_controller.go"
      provides: "repairOrHalt/haltVerify migrated onto ResolveLoopPolicy; post-approval sentinel branch"
      contains: "ResolveLoopPolicy"
    - path: "cmd/tide/approve.go"
      provides: "findAwaitingProject at the FRONT of the approveLevel chain"
      contains: "findAwaitingProject"
  key_links:
    - from: "internal/controller/task_controller.go"
      to: "internal/controller/level_status.go exhaustVerifyLoop"
      via: "haltVerify branches on policy.EscalationPolicy through the shared helper"
      pattern: "exhaustVerifyLoop"
    - from: "internal/controller/dispatch_helpers_loop_policy_test.go"
      to: "task_controller.go"
      via: "SC3 static guard exclusion REMOVED (no raw Spec.Verification policy reads remain)"
      pattern: "TestNoDirectVerificationPolicyReads"
---

<objective>
Close Phase 51's declared-but-uniform gap (`task_types.go:145`): implement per-value `onExhaustion` semantics in ONE shared place (`exhaustVerifyLoop` in `level_status.go`, fed by 52-04's resolver) and migrate the Task loop onto `ResolveLoopPolicy` — the deliberate, flagged behavior change (State-of-the-Art table row 2): `requireApproval` contracts now genuinely park at `AwaitingApproval` instead of freezing the whole project. Includes the post-approval sentinel (an approved exhausted loop closes the level rather than resurrecting the executor) and the `findAwaitingProject` CLI addition inserted at the FRONT of the approve chain.

Purpose: plan-check (52-07/52-09) and level-verify (52-08/52-10) call the same helper — Task inherits the split from the same shared code path that gives the other levels their semantics (D-08's one-place rule).
Output: shared exhaustion branch + Task migration + sentinel + CLI + SC3 guard exclusion removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-level-looppolicy-parameterization/52-PATTERNS.md  (sections: task_controller.go migration, level_status.go, approve.go)
@.planning/phases/52-per-level-looppolicy-parameterization/52-RESEARCH.md  (State of the Art rows 1-2; Security Domain row 3)
@.planning/phases/52-per-level-looppolicy-parameterization/52-CONTEXT.md  (D-02, D-08)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared exhaustVerifyLoop + Task migration + post-approval sentinel</name>
  <files>internal/controller/level_status.go, internal/controller/task_controller.go, internal/controller/dispatch_helpers_loop_policy_test.go</files>
  <read_first>
    - internal/controller/level_status.go (full — patchLevelStatus at :69-104 and consumeApproveAndResume at :106-155: the leaf primitives the new helper composes; its D-04 invariant "never advances to Succeeded" stays true of consumeApproveAndResume)
    - internal/controller/task_controller.go lines 340-520 (gateChecks — where phase==AwaitingApproval is consumed today via the gates package; the sentinel branch inserts near this readiness region), lines 980-1040 (patchTaskAwaitingApproval — the parking-entry shape to reuse), lines 2555-2680 (haltVerify + repairOrHalt — the migration targets; note haltVerify's span/spend/LoopStatus stamping order), line 2643 (the raw `task.Spec.Verification.MaxIterations` read to migrate)
    - internal/controller/verify_halt.go (setVerifyHaltIfNeeded — the escalate arm, reused verbatim, NOT modified)
    - api/v1alpha3/loop_types.go (ExitReason constants — ExitIterationsExhausted, ExitEscalated; EscalationPolicy constants)
  </read_first>
  <action>
    1. `level_status.go`: add `exhaustVerifyLoop(ctx, c client.Client, project *tideprojectv1alpha3.Project, obj client.Object, conditions *[]metav1.Condition, phasePtr *string, level string, policy tideprojectv1alpha3.LoopPolicy, completedAt time.Time, message string) (ctrl.Result, error)` — THE one D-08 branch point. Switch on `policy.EscalationPolicy`: `EscalationRequireApproval` → patch the level to `LevelPhaseAwaitingApproval` with a paused condition mirroring patchTaskAwaitingApproval's condition shape (reason names verify-exhaustion, message carries the caller's summary) — do NOT touch project conditions; `EscalationEscalate` and empty → patch the level to `LevelPhaseVerifyHalted` and call `setVerifyHaltIfNeeded(ctx, c, project, completedAt)` (existing Phase 51 behavior, unchanged). Doc comment: "the exhaustion/escalation path fed by ResolveLoopPolicy — all five levels route here; per-value differentiation lives here and ONLY here (D-08)".

    2. `task_controller.go` migration: in `repairOrHalt`, resolve `policy := ResolveLoopPolicy(project, nil, task, "task")` and replace the raw `task.Spec.Verification.MaxIterations` read with `policy.MaxIterations` (same `task.Status.Attempt >= int(...)` comparison — 51's counting semantics preserved: MaxIterations=1 allows zero repairs). In `haltVerify`, keep the existing LoopStatus/ExitReason stamping + span emission + spend settling exactly as-is, but replace the unconditional `setVerifyHaltIfNeeded` + VerifyHalted patch with a call to `exhaustVerifyLoop(..., policy, ...)`. BLOCKED verdicts route through the same call (uniform: every terminal non-APPROVED exit consults the policy — at Task defaults this is escalate → identical to Phase 51). Migrate any other raw `Spec.Verification.MaxIterations`/`.OnExhaustion` policy reads found by `grep -n "Spec.Verification.\(MaxIterations\|OnExhaustion\)" internal/controller/task_controller.go` onto the resolver (reads of contract CONTENT fields — GateCommand/Commands/RequiredArtifacts/Phase — stay direct; only the two POLICY knobs migrate).

    3. Post-approval sentinel: in the Task reconcile path where a `LevelPhaseRunning` task is processed, add an early branch — if `task.Status.LoopStatus.ExitReason != ""` and phase is Running, the only way this state is reachable is an operator's approve-resume of an exhausted verify loop (the live loop always keeps ExitReason empty — Phase 51-07's applyLoopStatus contract) → route to `markVerifiedSucceeded` (accept-the-findings semantics). Place the branch so it runs BEFORE any executor re-dispatch logic can fire. Doc comment explains the reachability argument.

    4. Remove the `task_controller.go` exclusion (and its TODO(52-06) comment) from `TestNoDirectVerificationPolicyReads` in dispatch_helpers_loop_policy_test.go — the guard now covers the whole package except dispatch_helpers.go itself.
  </action>
  <verify>
    <automated>go test ./internal/controller/... -run 'TestNoDirectVerificationPolicyReads' -count=1 && go test ./internal/controller/... -run TestControllers --ginkgo.focus='Verif' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Spec.Verification.MaxIterations\|Spec.Verification.OnExhaustion" internal/controller/task_controller.go` = 0 (comments excluded by the guard's stripping; the guard test passes with no exclusions beyond dispatch_helpers.go)
    - `grep -n "exhaustVerifyLoop" internal/controller/level_status.go internal/controller/task_controller.go` shows exactly one definition + ≥ 1 Task call site
    - Existing Phase-51 verifier Ginkgo specs all pass unmodified (the no-behavior-change-at-defaults bar) — focus run reports > 0 specs, 0 failures
    - The sentinel branch exists with the reachability doc comment (`grep -n "ExitReason" internal/controller/task_controller.go` shows the Running+ExitReason guard)
  </acceptance_criteria>
  <done>One shared exhaustion branch; Task migrated; sentinel prevents post-approval resurrect; SC3 guard fully armed.</done>
</task>

<task type="auto">
  <name>Task 2: findAwaitingProject + envtest coverage for both exhaustion arms</name>
  <files>cmd/tide/approve.go, internal/controller/task_verify_dispatch_test.go</files>
  <read_first>
    - cmd/tide/approve.go lines 170-200 (approveLevel's fixed dependency-order chain) and lines 358-436 (the four findAwaiting* sibling bodies — findAwaitingPlan is the exact shape to copy)
    - internal/controller/task_verify_dispatch_test.go (the Phase-51 Ginkgo fixture idiom for driving a Task through Verifying → verdict consumption — extend, don't fork)
  </read_first>
  <action>
    1. `cmd/tide/approve.go`: add `findAwaitingProject(ctx, c, ns, projectName)` following findAwaitingPlan's body shape (Get the single Project by name, return it with level "project" when `Status.Phase == LevelPhaseAwaitingApproval`). Insert it FIRST in approveLevel's chain — the deterministic order becomes Project → Milestone → Phase → Plan → Task (Project is the hierarchy root; RESEARCH Security row 3's confused-deputy ordering requirement). Update the chain's Option-A doc comment to name the new order.

    2. Extend the Ginkgo suite (task_verify_dispatch_test.go or a sibling Describe): (a) a Task whose Locked contract sets `onExhaustion: requireApproval` and exhausts its iterations → task phase becomes AwaitingApproval, `ConditionVerifyHalt` is NOT set on the project, LoopStatus.ExitReason non-empty; then applying the approve annotation (gates package idiom) → task reaches Succeeded and NO new executor Job is created (assert Job list count unchanged); (b) a Task with explicit `onExhaustion: escalate` exhausting → phase VerifyHalted + project ConditionVerifyHalt True (the Phase-51 shape, now via the explicit value); (c) unset onExhaustion → identical to (b) (default-escalate regression pin).
  </action>
  <verify>
    <automated>go build ./cmd/tide && go test ./internal/controller/... -run TestControllers --ginkgo.focus='Exhaust|RequireApproval' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "findAwaitingProject" cmd/tide/approve.go` shows the definition + the chain call BEFORE findAwaitingMilestone
    - Ginkgo focus run reports ≥ 3 specs ran (not vacuous — Pitfall 5), 0 failures
    - Spec (a) asserts BOTH the parked state (AwaitingApproval, no project halt) AND the post-approval terminal (Succeeded, no executor re-dispatch)
  </acceptance_criteria>
  <done>Both exhaustion arms proven at Task level; approve chain deterministically extended to Project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (`tide approve`) → parked level | a human authorization gate resumes/closes a level whose verify loop exhausted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-15 | Elevation of Privilege | approve-resume resurrecting the executor (paid re-run without human intent) | mitigate | Post-approval sentinel routes Running+ExitReason≠"" to markVerifiedSucceeded before any dispatch logic; envtest asserts Job count unchanged after approval |
| T-52-16 | Access Control (V4) | racing `tide approve` against an in-flight verify | mitigate | consumeApproveAndResume's existing consume-annotation-first two-step (T-04-G2 crash-safety) is reused verbatim, not reimplemented |
| T-52-17 | Repudiation | approve targeting the wrong parked level when several park at once | mitigate | Fixed deterministic chain order Project→Milestone→Phase→Plan→Task; doc comment updated; Security-row-3 requirement satisfied at insertion |
| T-52-18 | Tampering | requireApproval weakening fail-closed doctrine | accept | Parking still blocks all progress (no dispatch proceeds from AwaitingApproval); ESC-02 names the gate machinery as the intended backstop |
</threat_model>

<verification>
- Full envtest suite: `go test ./internal/controller/... -run TestControllers -count=1` green (51-suite regression bar)
- `make lint` green
</verification>

<success_criteria>
- D-08's per-value split lives in exactly one function; Task inherits it with zero behavior change at defaults
- Approved exhausted loops close the level; escalated ones freeze the project — both proven in envtest
</success_criteria>

<output>
Create `.planning/phases/52-per-level-looppolicy-parameterization/52-06-SUMMARY.md` when done.
</output>
