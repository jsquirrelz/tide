---
phase: 40-deprecate-v1alpha1-api
plan: 04
type: execute
wave: 3
depends_on: [40-03]
files_modified:
  - internal/controller/dispatch_helpers.go
  - internal/controller/project_controller.go
  - internal/controller/milestone_controller.go
  - internal/controller/phase_controller.go
  - internal/controller/plan_controller.go
  - internal/controller/task_controller.go
  - internal/dispatch/podjob/jobspec.go
  - test/integration/envtest/planner_dispatch_test.go
autonomous: true
requirements: [CRANK-04]

must_haves:
  truths:
    - "spec.subagent.levels.milestone resolves the model/image for the dispatch that authors MILESTONE.md (today it silently falls back — the exact first-run bug from the folded todo)"
    - "levels.phase resolves for phase-brief authoring; levels.plan resolves for BOTH the PLAN.md and task-DAG dispatches (D-11 collapse); levels.task unchanged"
    - "Envelope Level values, Job labels, PlannerJobName, and subagent template selection are byte-identical to before — the rename shifts ONLY override-key resolution"
    - "The resolved model is logged at every dispatch site (today it appears nowhere outside the PVC envelope)"
  artifacts:
    - path: "internal/controller/dispatch_helpers.go"
      provides: "levelOverrideKey mapping + remapped ResolveProvider/resolveImage resolution"
      contains: "levelOverrideKey"
    - path: "test/integration/envtest/planner_dispatch_test.go"
      provides: "per-level resolution assertions (RED against old semantics, GREEN after)"
      contains: "Levels"
  key_links:
    - from: "internal/controller/dispatch_helpers.go ResolveProvider/resolveImage"
      to: "api/v1alpha3 LevelOverrides slots"
      via: "levelOverrideKey(dispatchLevel) — project→Milestone, milestone→Phase, phase→Plan, plan→Plan, task→Task"
      pattern: "levelOverrideKey"
    - from: "EnvelopeIn.Level (UNCHANGED dispatch identity)"
      to: "internal/subagent/common/prompt_templates.go + cmd/stub-subagent/main.go template selection"
      via: "5-valued level switch (project/milestone/phase/plan/task) — must keep matching"
      pattern: "project_planner"
---

<objective>
Implement the D-02 `subagent.levels` semantic rename per the folded todo's DECIDED mapping: each `levels.X` key now means "level X is planned by this model." Implemented as an override-key mapping inside `ResolveProvider`/`resolveImage` — NOT as call-site literal shifts — plus resolved-model logging at every dispatch site.

Purpose: closes the first-external-run bug where the MILESTONE.md dispatch (level "project") matched no override key and silently ran on the fallback model; the operator ladder (Fable→milestone, Opus→phase, Sonnet→plan, Haiku→task) finally lands where operators read it. The chart's values.yaml per-level comments were already authored with these semantics — zero chart changes.

DESIGN NOTE (why not the RESEARCH.md call-site literal shift): `BuildPlannerEnvelope(level, ...)` sets BOTH `EnvelopeIn.Level` (which drives subagent prompt-template selection at internal/subagent/common/prompt_templates.go:39-43 and the stub-subagent's child-authoring switch at cmd/stub-subagent/main.go:240-303) AND `ResolveProvider(project, level, ...)`. Shifting the literals would make the PLAN.md dispatch and the task-DAG dispatch both send `Level: "plan"`, handing one of them the wrong template. The todo's own table shows two dispatch strings ("phase" + "plan") sharing ONE key — i.e. dispatch identity stays 5-valued; only the override-key derivation shifts. This is within CONTEXT.md's Claude's-Discretion lane ("Envelope Level string values ... within the todo's decided mapping").

Output: mapped resolution + per-level envtest assertions + dispatch-time model logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-deprecate-v1alpha1-api/40-CONTEXT.md
@.planning/todos/pending/2026-07-03-project-level-subagent-override-slot.md
@.planning/phases/40-deprecate-v1alpha1-api/40-PATTERNS.md

<interfaces>
The DECIDED mapping (todo table — the implementation spec):

| Dispatch level string (UNCHANGED, envelope/labels/templates) | Override key it now resolves | Authors |
|---|---|---|
| "project"   | Levels.Milestone | MILESTONE.md |
| "milestone" | Levels.Phase     | phase briefs |
| "phase"     | Levels.Plan      | PLAN.md |
| "plan"      | Levels.Plan      | task DAG (D-11 collapse — same slot as "phase") |
| "task"      | Levels.Task      | task execution |

From internal/controller/dispatch_helpers.go (post-40-03, v1alpha3 types):
```go
type ProviderDefaults struct {
    Image  string
    Models map[string]string  // level → model; chart writes keys milestone/phase/plan/task
}
func ResolveProvider(project *tideprojectv1alpha3.Project, level string, helmDefaults ProviderDefaults) pkgdispatch.ProviderSpec
func resolveImage(project *tideprojectv1alpha3.Project, level string, helmDefaults ProviderDefaults) string
func BuildPlannerEnvelope(level string, parent metav1.Object, project *tideprojectv1alpha3.Project, ...) (pkgdispatch.EnvelopeIn, []byte, error)
// BuildPlannerEnvelope sets EnvelopeIn.Level = level AND Provider = ResolveProvider(project, level, helmDefaults)
```
Chart default Models keys (charts/tide/values.yaml — FIXED, zero edits): milestone→claude-opus-4-8, phase→claude-sonnet-4-6, plan→claude-sonnet-4-6, task→claude-haiku-4-5. These were ALREADY authored with the new semantics, so `Models[levelOverrideKey(level)]` finally honors the chart author's intent.
</interfaces>

Forbidden-touch list (same vocabulary, different meaning — RESEARCH.md Pitfall 1 + verified sites):
- internal/gates/policy.go EvaluatePolicy/DefaultGates (Gates.X gates the CR's OWN boundary — no off-by-one exists)
- internal/controller/push_helpers.go:402 buildCommitMessage and project_controller.go:846 (git-commit boundary vocabulary)
- Any controller call-site level literal (project_controller.go:1214/1241/1246, milestone_controller.go:450/481/486, phase_controller.go:413/443/448, plan_controller.go:440/470/475, task_controller.go:1485) — these stay EXACTLY as they are
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: levelOverrideKey mapping in ResolveProvider/resolveImage, test-first</name>
  <files>internal/controller/dispatch_helpers.go, test/integration/envtest/planner_dispatch_test.go</files>
  <read_first>
    - internal/controller/dispatch_helpers.go (ResolveProvider ~:125-152 switch, resolveImage ~:260-291 switch + the ":267 Level 'project' has no entry" comment, BuildPlannerEnvelope ~:223-251, ProviderDefaults ~:114-123)
    - test/integration/envtest/planner_dispatch_test.go (existing fixture + Job assertions — extend, don't replace)
    - internal/subagent/common/prompt_templates.go (:39-46 — the level→template map that must keep matching; DO NOT EDIT)
    - cmd/stub-subagent/main.go (:240-303 level switch — DO NOT EDIT)
  </read_first>
  <behavior>
    Fixture project sets four DISTINCT per-level models, e.g. Levels.Milestone.Model="model-milestone-md", Levels.Phase.Model="model-phase-briefs", Levels.Plan.Model="model-plan-content", Levels.Task.Model="model-task-exec". Assertions on the dispatched envelope/Job per dispatch surface:
    - Test 1 (RED today): Project-CR planner dispatch (authors MILESTONE.md, Level "project") resolves Provider.Model == "model-milestone-md" (today: silent fallback to Spec.Subagent.Model)
    - Test 2 (RED today): Milestone-CR dispatch (Level "milestone") resolves "model-phase-briefs"
    - Test 3 (RED today): Phase-CR dispatch (Level "phase") resolves "model-plan-content"
    - Test 4: Plan-CR dispatch (Level "plan") resolves "model-plan-content" (D-11 collapse — same slot as Test 3)
    - Test 5: task executor resolves "model-task-exec" (was never off-by-one)
    - Test 6: envelope Level values and the tideproject.k8s/level Job label are UNCHANGED ("project"/"milestone"/"phase"/"plan"/"task") — the rename must not leak into dispatch identity
  </behavior>
  <action>
    Write the failing assertions first (extend planner_dispatch_test.go), run to confirm RED on tests 1-3, then implement:

    1. Add package-level `func levelOverrideKey(level string) string` to dispatch_helpers.go implementing the mapping table verbatim: "project"→"milestone", "milestone"→"phase", "phase"→"plan", "plan"→"plan", "task"→"task", default→return level unchanged. Doc comment cites the D-02 semantic rename and the todo's decided table (each levels.X key = "level X is planned by this model"; the dispatch-level string remains the 5-valued dispatch identity consumed by subagent template selection).
    2. In ResolveProvider AND resolveImage: derive `key := levelOverrideKey(level)` once, switch on `key` for the Levels struct slot (the four cases keep their existing bodies — case "milestone" still selects Levels.Milestone, etc.), and use `key` for the `helmDefaults.Models[...]` lookup. Callers keep passing dispatch levels — ZERO call-site edits.
    3. Delete the now-false ":267 Level 'project' has no entry in the switch" comment; rewrite both functions' precedence-ladder doc comments to read `Levels.<overrideKey>` with a one-line pointer to levelOverrideKey.
    4. Confirm the switch coverage is now COMPLETE for all five dispatch levels (post-mapping, "project" resolves via the "milestone" case — the silent fall-through class is dead).
  </action>
  <verify>
    <automated>go test ./test/integration/envtest/... -run 'planner' -count=1 && make test</automated>
  </verify>
  <acceptance_criteria>
    - New assertions RED before the dispatch_helpers.go change (executor must observe and note the failure), GREEN after
    - `grep -n 'func levelOverrideKey' internal/controller/dispatch_helpers.go` returns 1 hit; `grep -c 'levelOverrideKey(' internal/controller/dispatch_helpers.go` ≥ 3 (definition + both resolvers)
    - `git diff --stat internal/controller/{project,milestone,phase,plan,task}_controller.go` is EMPTY for this task (no call-site edits)
    - `git diff --stat internal/subagent/ cmd/stub-subagent/ internal/gates/ internal/controller/push_helpers.go` is empty
    - `make test` exits 0
  </acceptance_criteria>
  <done>All five dispatch surfaces resolve the todo-decided override slot; dispatch identity (envelope Level, labels, templates) provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Resolved-model dispatch logging + Level doc alignment</name>
  <files>internal/controller/project_controller.go, internal/controller/milestone_controller.go, internal/controller/phase_controller.go, internal/controller/plan_controller.go, internal/controller/task_controller.go, internal/dispatch/podjob/jobspec.go</files>
  <read_first>
    - internal/controller/project_controller.go (~:1214-1250 dispatch block — the envelope build + BuildOptions site to log after)
    - internal/controller/task_controller.go (~:1480-1490 executor dispatch — locate the task-envelope build for the same log shape)
    - internal/dispatch/podjob/jobspec.go (BuildOptions.Level field doc comment claiming the enum is "milestone"|"phase"|"plan"|"task")
  </read_first>
  <action>
    1. At each of the five dispatch sites (project_controller ~:1214 block, milestone_controller ~:450 block, phase_controller ~:413 block, plan_controller ~:440 block, task_controller executor dispatch ~:1485 block), after the envelope is built and before/at Job creation, add one structured Info log using the reconciler's ctx logger, e.g. key/values: msg "resolved subagent dispatch", "level" <dispatch level literal>, "model" envIn.Provider.Model, "image" <the resolved image value already computed at the site>. Match each file's existing logf idiom (same logger acquisition style as adjacent log lines). This closes the folded todo's observability gap: "today the resolved model appears nowhere in pod spec, events, or conditions — only inside the PVC envelope."
    2. internal/dispatch/podjob/jobspec.go — update BuildOptions.Level's doc comment to list the ACTUAL legal values: "project"|"milestone"|"phase"|"plan"|"task" (the "project" value predates this phase and is now documented rather than out-of-spec).
    3. No metric, no condition, no envelope change — log line only (cheap-extensible seed posture).
  </action>
  <verify>
    <automated>make test && grep -c 'resolved subagent dispatch' internal/controller/*.go | awk -F: '{s+=$2} END {print (s==5) ? "OK" : "FAIL: " s; exit (s==5)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 5 `resolved subagent dispatch` log call sites across the five controllers
    - `grep -n '"project"|"milestone"|"phase"|"plan"|"task"\|project.*milestone.*phase.*plan.*task' internal/dispatch/podjob/jobspec.go` shows the Level doc comment listing all five values
    - `make test` exits 0
  </acceptance_criteria>
  <done>Every dispatch logs its resolved model at Info level; BuildOptions.Level documentation matches reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator spec → model selection | Which LLM (and therefore cost/capability tier) runs a dispatch is operator-controlled config crossing into dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-11 | Tampering (silent misconfiguration) | ResolveProvider fall-through | mitigate | post-mapping, every dispatch level hits a real override slot — the silent-fallback class (MILESTONE.md on the wrong model with no error) is structurally dead; envtest pins all 5 resolutions |
| T-40-12 | Repudiation | dispatch model observability | mitigate | resolved model logged at all 5 dispatch sites — an operator can audit which model produced which artifact from manager logs |
| T-40-13 | Tampering | subagent template selection | mitigate | dispatch identity (EnvelopeIn.Level) deliberately unchanged; Test 6 pins envelope Level + Job label values so the wrong-template failure mode cannot ship |

No package-manager installs in this plan — supply-chain checkpoint N/A.
</threat_model>

<verification>
- planner_dispatch envtest green with the 6 new/extended assertions
- `make test` green; zero diffs in forbidden-touch files
</verification>

<success_criteria>
An operator setting `levels: {milestone: fable, phase: opus, plan: sonnet, task: haiku}` gets exactly that ladder on the artifacts those words name, verified by envtest; dispatch identity and subagent contract untouched; every dispatch's resolved model visible in manager logs.
</success_criteria>

<output>
Create `.planning/phases/40-deprecate-v1alpha1-api/40-04-SUMMARY.md` when done.
</output>
