---
phase: 40-deprecate-v1alpha1-api
plan: 07
type: execute
wave: 4
depends_on: [40-04, 40-05, 40-06]
files_modified:
  - Makefile
  - .github/workflows/ci.yaml
autonomous: true
requirements: [CRANK-07]

must_haves:
  truths:
    - "A durable, CI-wired gate asserts zero v1alpha1/v1alpha2 references outside the sanctioned exclusion set — the phase's terminal verification survives as a regression guard, not a one-off grep"
    - "The gate is provably alive: a seeded legacy string makes it exit 1"
    - "Full make test-int is green on the final tree (MAKE_EXIT read AND '^--- FAIL' grepped — not just the Ginkgo summary)"
  artifacts:
    - path: "Makefile"
      provides: "verify-no-legacy-api-refs target"
      contains: "verify-no-legacy-api-refs"
    - path: ".github/workflows/ci.yaml"
      provides: "CI invocation of the new gate"
      contains: "verify-no-legacy-api-refs"
  key_links:
    - from: ".github/workflows/ci.yaml"
      to: "Makefile verify-no-legacy-api-refs"
      via: "make invocation alongside verify-no-aggregates"
      pattern: "verify-no-legacy-api-refs"
---

<objective>
Close the phase: encode the CONTEXT.md end-state assertion ("zero v1alpha1/v1alpha2 grep hits outside docs/migration/ and .planning/") as a durable Makefile gate wired into CI, prove it alive, and run the full phase gate suite including kind Layer B.

Purpose: the terminal verification is itself a control — without a durable gate, the next contributor can silently reintroduce legacy references (the same failure class as the dead verify-no-aggregates glob, D-12).

Sanctioned exclusion set (each justified by a locked decision — document in the target's comment):
- `.planning/`, `.git/`, `.worktrees/` (untracked stale worktree), `node_modules`/build dirs — non-source
- `docs/migration/` — migration guides legitimately narrate old versions (CONTEXT end-state)
- `docs/audit/`, `docs/superpowers/` — dated historical snapshots, preserved (D-12)
- `AGENTS.md` — generic kubebuilder tutorial boilerplate, example.com groups (RESEARCH false positive)
- LINES containing `dispatch.tideproject.k8s/v1alpha1` — the envelope contract keeps its v1alpha1 version component by design (D-08)
- LINES containing `v1alpha2-to-v1alpha3` — references TO the migration doc path (guard constant, doc links)
- LINES containing `krew.googlecontainertools.github.com/v1alpha2` — external API group (krew manifest)

Output: verify-no-legacy-api-refs target, CI wiring, green full suite, phase-complete evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-deprecate-v1alpha1-api/40-CONTEXT.md

Constrained-VM recipe for the heavy run (CLAUDE.md operating notes): delete any stale kind clusters first, one heavy run at a time, fresh `tide-test` cluster per run; never run acceptance and test-int clusters concurrently. `make test-int` verdict protocol: read the echoed MAKE_EXIT AND `grep -nE '^--- FAIL|^FAIL\s'` the log — the kind package bundles plain go-tests beside Ginkgo, and a green Ginkgo summary does NOT mean the make target passed. Known pre-existing flake: the kind medium_http fixture class (Phase 25 note) — if a Layer B failure appears, git-archaeology (`git log -S '<token>' -- <file>`) for Phase-40 causality before dismissing it as pre-existing; a subagent's "pre-existing" claim is not verification.
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-no-legacy-api-refs Makefile gate + CI wiring + liveness proof</name>
  <files>Makefile, .github/workflows/ci.yaml</files>
  <read_first>
    - Makefile (the hardened verify-no-aggregates target from 40-05 — match its recipe style; place the new target adjacent)
    - .github/workflows/ci.yaml (locate the step invoking verify-no-aggregates ~:60 — wire the new target in the same step/block)
  </read_first>
  <action>
    1. Add `verify-no-legacy-api-refs` to the Makefile. CRITICAL self-match hazard: the recipe cannot contain the literal tokens it hunts, or it flags its own definition — build the pattern by concatenation, e.g. `PAT=v1alpha; grep -rnE "$${PAT}1|$${PAT}2" ...`. Recipe shape: recursive grep from repo root with `--exclude-dir` for .git, .planning, .worktrees, node_modules, bin, dist; pipe through path filters dropping `docs/migration/`, `docs/audit/`, `docs/superpowers/`, `AGENTS.md`; pipe through line filters dropping `dispatch\.tideproject\.k8s/$${PAT}1`, `$${PAT}2-to-$${PAT}3`, and `krew\.googlecontainertools\.github\.com/$${PAT}2` (also build these via $$PAT so the filter lines don't self-match). Non-empty result → print matches, exit 1; empty → "OK: no legacy v1alpha1/v1alpha2 references" (echo text may spell the versions — wait, NO: echo text must ALSO use $$PAT concatenation or be phrased without the literals, e.g. "OK: no legacy API version references"). Comment block above the target lists the exclusion justifications (D-08 envelope, D-12 snapshots, migration-doc references, external groups).
    2. Wire into .github/workflows/ci.yaml in the same job/step that runs verify-no-aggregates (one added `make verify-no-legacy-api-refs` invocation or appended to the existing make line — match the file's existing style).
    3. Liveness proof (transient, not committed): create a scratch file containing the OLD CRD group-version string (tideproject.k8s/ + v1alpha1 concatenated) somewhere greppable (e.g. internal/scratch.go comment), run the target, observe exit 1 listing the seed; delete the seed, run again, observe exit 0. Record both observations in the SUMMARY.
    4. Iterate on residual hits: if the clean run surfaces hits outside the sanctioned set, they are REAL phase leftovers — fix them at the source (or, if genuinely sanctioned-but-unlisted, add a justified filter with a comment). Do not blanket-exclude directories to force green.
  </action>
  <verify>
    <automated>make verify-no-legacy-api-refs && grep -q 'verify-no-legacy-api-refs' .github/workflows/ci.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-no-legacy-api-refs` exits 0 on the clean tree; exited 1 on the seeded scratch file (both observed and recorded)
    - `grep -c 'verify-no-legacy-api-refs' .github/workflows/ci.yaml` ≥ 1
    - The target's recipe contains no bare `v1alpha1`/`v1alpha2` literals (self-match-proof): `grep -A20 'verify-no-legacy-api-refs:' Makefile | grep -c 'v1alpha1\|v1alpha2'` == 0
    - Every filter in the recipe has a one-line justification comment citing D-08/D-12/false-positive
  </acceptance_criteria>
  <done>The end-state assertion is a living, CI-enforced, provably-alive gate.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase gate — verify suite + kind Layer B</name>
  <files>none (verification only — fixes discovered here route back as gap tasks)</files>
  <read_first>
    - .planning/phases/40-deprecate-v1alpha1-api/40-VALIDATION.md (sampling contract + manual-verification note)
  </read_first>
  <action>
    Run the full gate suite on the final tree, in order:
    1. `make verify-no-aggregates && make verify-no-legacy-api-refs && make verify-dispatch-imports && make verify-chart-reproducible` — all must exit 0.
    2. `go build ./... && make test` — unit tier green.
    3. Fresh-cluster heavy run: delete stale kind clusters (NEVER touch kind-tide-dogfood without explicit operator instruction — but note it is pre-Spring-Tide and slated for rebuild per D-03, so leave it alone entirely), then `make test-int 2>&1 | tee /tmp/40-07-test-int.log; echo MAKE_EXIT=$?`. Verdict: MAKE_EXIT == 0 AND `grep -nE '^--- FAIL|^FAIL\s' /tmp/40-07-test-int.log` empty. This run exercises the de-facto reinstall path: fresh cluster + v1alpha3-only CRDs + v1alpha3 fixtures reconciling end-to-end, plus the guard envtest covering stale-revision rejection — together substantially covering VALIDATION.md's manual reinstall walkthrough (note this equivalence in the SUMMARY).
    4. Cross-check the phase end-state facts for the SUMMARY: 6 single-version CRD files (`grep -c 'name: v1alpha' config/crd/bases/*.yaml` — six 1s), `ls api/` == v1alpha3 only, both new gates green.
    If any step fails: diagnose from the log evidence FIRST (observe-first rule), fix in place if the cause is a Phase-40 commit, and note it; if the failure predates Phase 40, prove it with `git log -S` archaeology before recording it as pre-existing.
  </action>
  <verify>
    <automated>bash -c 'make verify-no-aggregates && make verify-no-legacy-api-refs && make verify-dispatch-imports && make verify-chart-reproducible && make test'</automated>
  </verify>
  <acceptance_criteria>
    - All four verify-* gates exit 0
    - `make test` exits 0
    - `make test-int` MAKE_EXIT == 0 AND zero `^--- FAIL|^FAIL ` lines in /tmp/40-07-test-int.log (or a git-log-S-proven pre-existing failure documented with commit evidence)
    - `ls api/` prints exactly `v1alpha3`
  </acceptance_criteria>
  <done>The phase's terminal state is verified end to end with recorded evidence, ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future contributions → repo invariants | Without a durable gate, legacy references can silently return post-phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-20 | Tampering (invariant erosion) | verify-no-legacy-api-refs | mitigate | gate wired into ci.yaml; liveness proven by seeded-string exit-1 observation; self-match-proof pattern construction |
| T-40-21 | Repudiation (false-green verdicts) | make test-int verdict | mitigate | MAKE_EXIT + '^--- FAIL' grep protocol mandatory (Ginkgo-summary-only verdicts explicitly rejected — Phase 7 lesson) |

No package-manager installs in this plan — supply-chain checkpoint N/A.
</threat_model>

<verification>
- All acceptance criteria in both tasks, with observed (not asserted) exit codes recorded in the SUMMARY
</verification>

<success_criteria>
Phase 40's end state — v1alpha3 sole served+storage version, zero legacy references outside the sanctioned set — is verified by a green full suite and enforced going forward by a CI-wired, provably-alive gate.
</success_criteria>

<output>
Create `.planning/phases/40-deprecate-v1alpha1-api/40-07-SUMMARY.md` when done.
</output>
