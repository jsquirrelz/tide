---
phase: 40-deprecate-v1alpha1-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha3/groupversion_info.go
  - api/v1alpha3/shared_types.go
  - api/v1alpha3/project_types.go
  - api/v1alpha3/milestone_types.go
  - api/v1alpha3/phase_types.go
  - api/v1alpha3/plan_types.go
  - api/v1alpha3/task_types.go
  - api/v1alpha3/wave_types.go
  - api/v1alpha3/import_types.go
  - api/v1alpha3/zz_generated.deepcopy.go
  - api/v1alpha3/schema_test.go
  - api/v1alpha2/project_types.go
  - api/v1alpha2/milestone_types.go
  - api/v1alpha2/phase_types.go
  - api/v1alpha2/plan_types.go
  - api/v1alpha2/task_types.go
  - api/v1alpha2/wave_types.go
  - config/crd/bases/*.yaml
  - charts/tide-crds/templates/*.yaml
autonomous: true
requirements: [CRANK-01]

must_haves:
  truths:
    - "api/v1alpha3 compiles as a full copy of the v1alpha2 schema minus the dead ModelSelection field (D-10)"
    - "All 6 CRD manifests carry a v1alpha3 version block with served: true and storage: true, and exactly one storage: true total per CRD"
    - "The v1alpha3 LevelOverrides field docs describe the NEW artifact-first semantics (D-02 todo mapping), because these comments generate into user-facing CRD descriptions"
    - "charts/tide-crds regenerates reproducibly — no hand edits"
  artifacts:
    - path: "api/v1alpha3/groupversion_info.go"
      provides: "GroupVersion tideproject.k8s/v1alpha3 + AddToScheme"
      contains: "Version: \"v1alpha3\""
    - path: "api/v1alpha3/project_types.go"
      provides: "ProjectSpec with SchemaRevision Enum=v1alpha3, no ModelSelection"
      contains: "Enum=v1alpha3"
    - path: "api/v1alpha3/schema_test.go"
      provides: "reflect-based structural assertions (Wave 0 test)"
      min_lines: 40
  key_links:
    - from: "api/v1alpha3/*_types.go"
      to: "config/crd/bases/tideproject.k8s_projects.yaml"
      via: "make manifests (controller-gen paths=./api/... discovers the new package)"
      pattern: "name: v1alpha3"
    - from: "config/crd/bases/"
      to: "charts/tide-crds/templates/"
      via: "make helm-crds (kustomize | helmify | augment script)"
      pattern: "v1alpha3"
---

<objective>
Introduce `api/v1alpha3` as a copy-and-reshape of `api/v1alpha2` (Phase 23 precedent, commit `67cb313`): SchemaRevision enum bumped to `v1alpha3`, dead `ProjectSpec.ModelSelection` dropped (D-10), `+kubebuilder:storageversion` markers moved from v1alpha2 to v1alpha3 atomically, LevelOverrides doc comments rewritten to the artifact-first semantics (D-02), CRDs and the tide-crds chart regenerated.

Purpose: v1alpha3 is the landing zone for the whole phase — consumers migrate onto it in wave 2, removals follow in wave 3 (D-01 sequencing).
Output: a compiling `api/v1alpha3` package, 3-version transitional CRD manifests (v1alpha1 dead block, v1alpha2 served, v1alpha3 served+storage), regenerated chart templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-deprecate-v1alpha1-api/40-CONTEXT.md
@.planning/phases/40-deprecate-v1alpha1-api/40-PATTERNS.md
@.planning/todos/pending/2026-07-03-project-level-subagent-override-slot.md

Transitional-state notes (accepted, by design):
- v1alpha3 has NO webhooks until plan 40-03 migrates them. Nothing creates v1alpha3 objects until consumers flip — acceptable inside the phase.
- Exactly ONE version may carry `storage: true` per CRD. The marker move (add to v1alpha3, remove from v1alpha2) MUST land in the same commit as the manifest regen.
- Chart.yaml version/appVersion are NOT bumped in this phase — release-time batching (`chore(release)` commits at milestone close per repo history). Only helmify-generated templates change.
- `.worktrees/pr3-debug/` is a stale untracked worktree in the working dir — never edit or sed anything under it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create api/v1alpha3 as a copy-and-reshape of api/v1alpha2</name>
  <files>api/v1alpha3/*.go (new, copied), api/v1alpha2/{project,milestone,phase,plan,task,wave}_types.go (storageversion marker removal only)</files>
  <read_first>
    - api/v1alpha2/groupversion_info.go (the exact 41-line file to copy — package/version strings are the only edits)
    - api/v1alpha2/project_types.go (SchemaRevision field ~:316-329, ModelSelection type ~:34-40 + field ~:351-353, SubagentConfig/LevelOverrides docs ~:143-180, storageversion marker :476)
    - .planning/todos/pending/2026-07-03-project-level-subagent-override-slot.md (the DECIDED mapping table — source text for the new LevelOverrides field docs)
    - .planning/phases/40-deprecate-v1alpha1-api/40-PATTERNS.md (section "api/v1alpha3/*.go" — package doc, marker block, SchemaRevision prose shapes)
  </read_first>
  <action>
    Copy the 9 source files of api/v1alpha2 (groupversion_info.go, shared_types.go, project_types.go, milestone_types.go, phase_types.go, plan_types.go, task_types.go, wave_types.go, import_types.go) into a new api/v1alpha3/ directory. Do NOT copy schema_test.go or zz_generated.deepcopy.go (test is authored fresh in Task 2; deepcopy is regenerated). Then apply these edits to the copies:

    1. Every file: `package v1alpha2` → `package v1alpha3`; package doc comment bumped to v1alpha3.
    2. groupversion_info.go: `Version: "v1alpha2"` → `Version: "v1alpha3"`; comments bumped. Keep SchemeGroupVersion/GroupVersion/SchemeBuilder/AddToScheme structure byte-identical otherwise.
    3. project_types.go SchemaRevision: `+kubebuilder:validation:Enum=v1alpha2` → `Enum=v1alpha3`; rewrite the field comment to describe the v1alpha2→v1alpha3 transition (mirror the existing prose shape: "identifies the v1alpha3 schema shape. Required in v1alpha3; its absence on a reconciled object signals a Project authored under a prior schema revision that slipped into etcd before the CRD upgrade").
    4. project_types.go: DELETE the `ModelSelection` struct type AND the `ProjectSpec.ModelSelection` field + its doc comment (D-10 — grep-confirmed zero readers outside api/; it duplicates the wired Subagent.Levels).
    5. project_types.go: rewrite the LevelOverrides field doc comments to the NEW artifact-first semantics (these generate into CRD descriptions — user-facing):
       - Milestone: "model/image for the planner that authors MILESTONE.md (the Project-level planning dispatch)"
       - Phase: "model/image for the planner that authors phase briefs"
       - Plan: "model/image for the planner that authors PLAN.md and the task DAG (two dispatches share this one slot)"
       - Task: "model/image for the task executor (diffs)"
       Also update the SubagentConfig resolution-ladder prose (~:143) so it no longer implies the dispatching-CR semantics. Do NOT rename any Go field or JSON tag — D-02 is a semantic rename; Milestone/Phase/Plan/Task field names and json tags stay identical.
    6. Storageversion markers: the copies keep `+kubebuilder:storageversion` on all 6 Kinds. REMOVE the marker line from the 6 v1alpha2 originals (api/v1alpha2/milestone_types.go:72, task_types.go:173, phase_types.go:68, wave_types.go:64, plan_types.go:92, project_types.go:476) in this same task.
    7. Run `make generate` to produce api/v1alpha3/zz_generated.deepcopy.go.
  </action>
  <verify>
    <automated>go build ./api/... && make generate && test -f api/v1alpha3/zz_generated.deepcopy.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./api/...` exits 0
    - `grep -c 'kubebuilder:storageversion' api/v1alpha3/*_types.go` totals 6 (one per Kind file); `grep -c 'kubebuilder:storageversion' api/v1alpha2/*_types.go` totals 0
    - `grep -rn 'ModelSelection' api/v1alpha3/` returns 0 hits
    - `grep -n 'Enum=v1alpha3' api/v1alpha3/project_types.go` returns exactly 1 hit (SchemaRevision)
    - `grep -n 'authors MILESTONE.md' api/v1alpha3/project_types.go` returns ≥ 1 hit (new LevelOverrides docs)
    - LevelOverrides still declares fields Milestone, Phase, Plan, Task with unchanged json tags (`grep -c 'json:"milestone,omitempty"\|json:"phase,omitempty"\|json:"plan,omitempty"\|json:"task,omitempty"' api/v1alpha3/project_types.go` == 4)
  </acceptance_criteria>
  <done>api/v1alpha3 compiles with deepcopy generated; v1alpha2 no longer claims storage; ModelSelection is gone from v1alpha3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema test + manifest/chart regeneration</name>
  <files>api/v1alpha3/schema_test.go (new), config/crd/bases/*.yaml (regen), charts/tide-crds/templates/*.yaml (regen)</files>
  <read_first>
    - api/v1alpha2/schema_test.go (the reflect-based positive/negative field-assertion idiom to mirror — full file)
    - config/crd/bases/tideproject.k8s_projects.yaml (current 2-version shape, to understand what the regen adds)
  </read_first>
  <behavior>
    - Test 1: `TestProjectSpecV1alpha3` — reflect.TypeFor[ProjectSpec] has NO field `ModelSelection` (negative assertion catching a regression that reintroduces the dead field)
    - Test 2: `TestLevelOverridesShape` — LevelOverrides HAS fields Milestone, Phase, Plan, Task (proves D-02 shipped as a semantic rename, not a struct rename)
    - Test 3: `TestSchemaRevisionField` — ProjectSpec has field SchemaRevision with json tag `schemaRevision`
  </behavior>
  <action>
    Author api/v1alpha3/schema_test.go (`package v1alpha3_test`) with the three reflect-based tests above, mirroring the api/v1alpha2/schema_test.go idiom (reflect.TypeFor + FieldByName positive/negative assertions). Then regenerate downstream artifacts:

    1. `make manifests` — controller-gen discovers api/v1alpha3 automatically (paths=./api/... has no version hardcoded) and emits a third version block into each of the 6 config/crd/bases/*.yaml files.
    2. `make helm-crds` — regenerates charts/tide-crds/templates/ via kustomize | helmify | hack/helm/augment-tide-crds-chart.sh. NEVER hand-edit chart templates.
    3. `make verify-chart-reproducible` — must pass (proves no hand-edit drift).

    Do NOT touch charts/tide-crds/Chart.yaml version/appVersion (release-time batching).
  </action>
  <verify>
    <automated>go test ./api/v1alpha3/... && make manifests && make helm-crds && make verify-chart-reproducible</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./api/v1alpha3/...` exits 0 with the 3 tests green
    - For EACH of the 6 files in config/crd/bases/: `grep -c 'name: v1alpha' <file>` == 3 (v1alpha1, v1alpha2, v1alpha3) AND `grep -c 'storage: true' <file>` == 1 AND the v1alpha3 block carries `served: true`
    - `grep -rn 'modelSelection' config/crd/bases/tideproject.k8s_projects.yaml` shows the property ONLY inside the v1alpha1/v1alpha2 blocks (absent from the v1alpha3 block)
    - `make verify-chart-reproducible` exits 0
    - `git diff --stat charts/tide-crds/Chart.yaml` is empty (no version bump)
  </acceptance_criteria>
  <done>Six 3-version CRD manifests with v1alpha3 as sole storage version; chart regenerated reproducibly; Wave 0 schema test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator manifest → API server | Project specs cross admission here; CRD schema (CEL, enums) is the validation surface |
| Go types → generated CRD YAML → chart | Generation pipeline integrity: hand edits break the chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | config/crd/bases + charts/tide-crds | mitigate | never hand-edit generated YAML; `make manifests`/`make helm-crds` only; `make verify-chart-reproducible` gate must pass in-plan |
| T-40-02 | Denial of Service | CRD apply path | mitigate | two-storage-version invalid CRD rejected by API server at apply time — marker move + regen land in one commit; per-file `grep -c 'storage: true'` == 1 asserted |
| T-40-03 | Input Validation (ASVS V5) | v1alpha3 CEL rules | mitigate | CEL XValidation rules copy verbatim from v1alpha2 (copy-and-reshape, not scaffold-fresh) — schema_test + later spec-conformance envtests exercise them |

No package-manager installs in this plan — supply-chain checkpoint N/A.
</threat_model>

<verification>
- `go build ./... && make test` green (transitional 3-version state must not break the v1alpha2-typed consumers)
- All acceptance criteria grep gates above
</verification>

<success_criteria>
api/v1alpha3 exists, compiles, and is the sole storage version in regenerated, reproducible CRD manifests + chart templates, carrying D-10 (no ModelSelection) and the D-02 artifact-first field documentation. Existing v1alpha2 consumers stay green.
</success_criteria>

<output>
Create `.planning/phases/40-deprecate-v1alpha1-api/40-01-SUMMARY.md` when done.
</output>
