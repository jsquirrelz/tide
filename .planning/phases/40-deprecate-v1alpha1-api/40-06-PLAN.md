---
phase: 40-deprecate-v1alpha1-api
plan: 06
type: execute
wave: 3
depends_on: [40-03]
files_modified:
  - docs/migration/v1alpha2-to-v1alpha3.md (new)
  - docs/INSTALL.md
  - docs/gates.md
  - docs/git-hosts.md
  - docs/project-authoring.md
  - README.md
  - SECURITY.md
  - docs/rbac.md
  - "config/samples/tide_v1alpha3_*.yaml (12 files, renamed from tide_v1alpha1_*)"
  - config/samples/kustomization.yaml
autonomous: true
requirements: [CRANK-06]

must_haves:
  truths:
    - "Every user-facing Project example applies cleanly against the v1alpha3 CRD: apiVersion tideproject.k8s/v1alpha3 AND spec.schemaRevision: v1alpha3 (examples lacking schemaRevision would trip the fail-closed guard for copy-pasters)"
    - "The migration guide's v1alpha2→v1alpha3 chapter contains the levels-remap table (old key meaning vs new key meaning) so an operator can re-author their Project correctly (D-06)"
    - "SECURITY.md and docs/rbac.md no longer describe the conversion webhook Phase 23 retired (D-12); docs/audit/*.md remain byte-identical (dated snapshots)"
    - "Sample filenames match their contents (tide_v1alpha3_*) and kustomization.yaml lists the new names"
  artifacts:
    - path: "docs/migration/v1alpha2-to-v1alpha3.md"
      provides: "migration chapter: what changed, levels-remap table, ModelSelection removal, envelope note, reinstall recipe, guard section, dogfood note"
      min_lines: 80
    - path: "config/samples/tide_v1alpha3_project.yaml"
      provides: "root sample with schemaRevision"
      contains: "schemaRevision: v1alpha3"
  key_links:
    - from: "docs/migration/v1alpha2-to-v1alpha3.md"
      to: "internal/controller/project_controller.go migrationGuideDocPath constant (40-03)"
      via: "the guard's RequiresReinstall message cites this exact path — file must exist at it"
      pattern: "v1alpha2-to-v1alpha3"
    - from: "config/samples/kustomization.yaml"
      to: "config/samples/tide_v1alpha3_*.yaml"
      via: "resources: filename list (lockstep rename)"
      pattern: "tide_v1alpha3_"
---

<objective>
The D-06 deep accuracy pass: author the v1alpha2→v1alpha3 migration chapter (with the levels-remap table), land every user-facing doc example on v1alpha3 + schemaRevision (the quickstart examples are broken TODAY — v1alpha1 is not served), rename and update all 12 samples plus kustomization, and fix the two pre-Phase-23-stale conversion-webhook claims (D-12) while preserving dated audit snapshots.

Purpose: docs are the operator's only migration path under D-03 reinstall-only — no conversion machinery exists to save an operator from wrong docs.
Output: accurate docs, valid samples, complete migration guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-deprecate-v1alpha1-api/40-CONTEXT.md
@.planning/todos/pending/2026-07-03-project-level-subagent-override-slot.md

Scope fences:
- docs/audit/README.md, docs/audit/operator.md, docs/superpowers/specs/*: DO NOT EDIT (D-12 — dated historical snapshots, preserved as-is; the 40-07 terminal grep excludes them).
- AGENTS.md:86,245: DO NOT EDIT (generic kubebuilder tutorial boilerplate with example.com groups — false positive per RESEARCH.md).
- docs/migration/v1alpha1-to-v1alpha2.md: unchanged content-wise; it is the structural TEMPLATE for the new chapter.
- Voice: match the existing docs — tight, declarative, em-dash-heavy; keep "alternatives considered and rejected" framing where a design choice is argued.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/migration/v1alpha2-to-v1alpha3.md</name>
  <files>docs/migration/v1alpha2-to-v1alpha3.md (new)</files>
  <read_first>
    - docs/migration/v1alpha1-to-v1alpha2.md (FULL file — mirror its exact section order: What changed and why → Version bump → Reinstall Procedure → Fail-Closed Safety Net → Dogfood Cluster Note; reuse its "### <change> (D-XX)" prose-block shape)
    - .planning/todos/pending/2026-07-03-project-level-subagent-override-slot.md (the DECIDED mapping table — source data for the levels-remap table)
  </read_first>
  <action>
    Author the new chapter with these REQUIRED contents:
    1. "What changed and why" subsections: (a) `subagent.levels` semantic rename (D-02) with the LEVELS-REMAP TABLE — four rows, columns "Key | Old meaning (v1alpha2) | New meaning (v1alpha3)": milestone = "model the Milestone CR used to author phase briefs" → "model that authors MILESTONE.md"; phase = "model the Phase CR used to author PLAN.md" → "model that authors phase briefs"; plan = "model the Plan CR used to author the task DAG" → "model that authors PLAN.md AND the task DAG (one slot, two dispatches)"; task = unchanged (task execution). Plus the fallback note: under v1alpha2 the MILESTONE.md dispatch matched NO key and silently used spec.subagent.model — v1alpha3 closes that hole. (b) `spec.modelSelection` REMOVED (D-10): it was dead — no controller ever read it; move any values to spec.subagent.levels. (c) envelope group decoupling (D-08): subagent-image authors now receive `apiVersion: dispatch.tideproject.k8s/v1alpha1` (kubeadm-pattern subdomain group; version component unchanged). (d) v1alpha1 AND v1alpha2 removed entirely — v1alpha3 is the sole served+storage version; the SchemaRevision guard now requires `schemaRevision: v1alpha3`.
    2. "Reinstall Procedure": numbered kubectl/helm steps mirroring the prior guide — export any in-flight state, delete Projects, delete/reinstall the tide-crds chart (CRDs are single-version now), upgrade the tide chart, re-apply Projects with `apiVersion: tideproject.k8s/v1alpha3` + `spec.schemaRevision: v1alpha3` (re-authored per the remap table). No storedVersions pruning, no conversion webhook — reinstall-only per Phase 23 D-09 precedent.
    3. "Fail-Closed Safety Net": the RequiresReinstall condition YAML with the new message shape (cites v1alpha3 + this doc's path — must match the migrationGuideDocPath constant landed in 40-03).
    4. "Dogfood Cluster Note": kind-tide-dogfood is pre-Spring-Tide v1alpha1 — rebuilt, not upgraded; zero compatibility built (D-03).
  </action>
  <verify>
    <automated>test -f docs/migration/v1alpha2-to-v1alpha3.md && grep -q 'schemaRevision: v1alpha3' docs/migration/v1alpha2-to-v1alpha3.md && grep -q 'dispatch.tideproject.k8s/v1alpha1' docs/migration/v1alpha2-to-v1alpha3.md && grep -q 'modelSelection' docs/migration/v1alpha2-to-v1alpha3.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exactly docs/migration/v1alpha2-to-v1alpha3.md (the path the 40-03 guard constant cites)
    - Contains a 4-row levels-remap table with old-vs-new meaning columns (all four keys present)
    - Contains the ModelSelection removal, envelope-group, reinstall-recipe, RequiresReinstall, and dogfood-rebuild sections
    - Section order mirrors docs/migration/v1alpha1-to-v1alpha2.md
  </acceptance_criteria>
  <done>An operator holding only this doc can re-author a v1alpha2 Project as a correct v1alpha3 one, including the levels remap.</done>
</task>

<task type="auto">
  <name>Task 2: Living docs bump — quickstarts, authoring guide, SECURITY, rbac</name>
  <files>docs/INSTALL.md, docs/gates.md, docs/git-hosts.md, docs/project-authoring.md, README.md, SECURITY.md, docs/rbac.md</files>
  <read_first>
    - docs/project-authoring.md (header :5 "v1alpha1 schema lock", api-link lines :11,:33, example blocks :132,:210,:320 — the deepest of the doc edits; also check whether it documents spec.modelSelection or old levels semantics)
    - SECURITY.md (:40 — the "conversion webhook (no-op for v1)" claim retired in Phase 23)
    - docs/rbac.md (:210-224 — the stale "Conversion webhook (D-X7 — no-op for v1.0)" section)
  </read_first>
  <action>
    1. Example blocks — README.md:108, docs/INSTALL.md:210, docs/gates.md:14, docs/git-hosts.md:68, docs/project-authoring.md:132,:210,:320: bump `apiVersion: tideproject.k8s/v1alpha1` → `tideproject.k8s/v1alpha3` AND add `schemaRevision: v1alpha3` as the first spec key (v1alpha1 predates the field — without it every copy-pasted example trips the fail-closed guard).
    2. docs/project-authoring.md: header :5 → "at the `tideproject.k8s/v1alpha3` schema lock"; :11,:33 links → api/v1alpha3/project_types.go; if it documents `spec.modelSelection`, delete that section (D-10); if it documents `subagent.levels`, rewrite to the NEW artifact-first semantics (may cite the migration guide's remap table); sweep the whole file for remaining v1alpha1/v1alpha2 prose.
    3. Sweep each of the other five files for any remaining v1alpha1/v1alpha2 mention beyond the named lines (`grep -n 'v1alpha1\|v1alpha2' <file>`) and land them on the v1alpha3 reality — EXCEPT legitimate references to docs/migration/ chapter filenames.
    4. SECURITY.md:40 — replace the "conversion webhook (no-op for v1)" wording: the admission surface is `api/v1alpha3/*` schemas, CEL validators, and the validating webhooks; conversion webhooks were retired in Phase 23 (reinstall-only migration).
    5. docs/rbac.md:210-224 — delete or rewrite the stale conversion-webhook section to a short note that conversion was retired in Phase 23 and TIDE ships single-version CRDs with reinstall-only migration; keep surrounding RBAC content intact.
  </action>
  <verify>
    <automated>bash -c 'for f in docs/INSTALL.md docs/gates.md docs/git-hosts.md README.md docs/project-authoring.md; do grep -q "tideproject.k8s/v1alpha3" "$f" || exit 1; grep -n "tideproject.k8s/v1alpha1\b" "$f" && exit 1; done; exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - Zero `tideproject.k8s/v1alpha1` / `tideproject.k8s/v1alpha2` apiVersion strings in the five example-carrying docs; every Project example block includes `schemaRevision: v1alpha3`
    - docs/project-authoring.md:5 says v1alpha3; zero api/v1alpha1 links remain in it
    - `grep -in 'conversion' SECURITY.md docs/rbac.md` shows no claim that a conversion webhook exists/is scaffolded
    - `git diff --stat docs/audit/ docs/superpowers/ AGENTS.md` is empty (snapshots untouched)
  </acceptance_criteria>
  <done>All living docs describe the v1alpha3 world; broken quickstart examples work again; pre-existing staleness (D-12) fixed without rewriting history.</done>
</task>

<task type="auto">
  <name>Task 3: Samples — rename all 12, bump contents, update kustomization</name>
  <files>config/samples/tide_v1alpha1_*.yaml → tide_v1alpha3_*.yaml (git mv ×12), config/samples/kustomization.yaml</files>
  <read_first>
    - config/samples/tide_v1alpha1_project.yaml (14-line shape: header comment, apiVersion, labels — the pattern all 12 follow)
    - config/samples/kustomization.yaml (header comment + resources: list enumerating all sample filenames — must move in lockstep, easy to miss per RESEARCH.md)
  </read_first>
  <action>
    1. `git mv` each of the 12 `config/samples/tide_v1alpha1_*.yaml` files to `tide_v1alpha3_*` (project, milestone, phase, plan, and the 8 task_alpha..theta files — enumerate with ls, don't trust a fixed count).
    2. In each: `apiVersion: tideproject.k8s/v1alpha1` → `tideproject.k8s/v1alpha3`; header comment filename/version references bumped.
    3. tide_v1alpha3_project.yaml ONLY: add `schemaRevision: v1alpha3` as the first spec key (child Kinds have no schemaRevision field — do not add it to milestone/phase/plan/task samples).
    4. config/samples/kustomization.yaml: update every filename in the resources: list and the header comment to the new names.
  </action>
  <verify>
    <automated>bash -c 'ls config/samples/tide_v1alpha1_* 2>/dev/null && exit 1; test "$(ls config/samples/tide_v1alpha3_*.yaml | wc -l | tr -d " ")" -ge 12 && ! grep -rn "v1alpha1" config/samples/ && grep -q "schemaRevision: v1alpha3" config/samples/tide_v1alpha3_project.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - Zero tide_v1alpha1_* files remain; ≥12 tide_v1alpha3_*.yaml files exist
    - `grep -rn 'v1alpha1\|v1alpha2' config/samples/` returns 0 hits
    - Project sample carries schemaRevision: v1alpha3; child samples carry apiVersion only
    - kustomization.yaml resources list references only tide_v1alpha3_* names (kustomize would fail on a stale name — filename list matches `ls` exactly)
  </acceptance_criteria>
  <done>Samples are appliable against the v1alpha3-only CRDs and internally consistent with kustomization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator's cluster | Copy-pasted examples are executed verbatim by operators; wrong examples produce broken or guard-blocked Projects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-17 | Information Disclosure (misleading security docs) | SECURITY.md / docs/rbac.md | mitigate | delete the retired-conversion-webhook claims (D-12) so the documented admission surface matches the real one (CEL + validating webhooks only) |
| T-40-18 | Tampering (guard-tripping examples) | doc/sample Project examples | mitigate | every example carries schemaRevision: v1alpha3 — asserted by grep gates; a schemaRevision-less example would fail-close at reconcile, not silently misbehave (guard is the backstop) |
| T-40-19 | Repudiation (falsified history) | docs/audit snapshots | mitigate | explicitly untouched (D-12) — `git diff --stat docs/audit/` empty is an acceptance criterion |

No package-manager installs in this plan — supply-chain checkpoint N/A.
</threat_model>

<verification>
- All three tasks' automated gates green
- `grep -rn 'v1alpha1\|v1alpha2' docs/ README.md SECURITY.md | grep -v migration | grep -v audit | grep -v superpowers` returns 0 hits
</verification>

<success_criteria>
An operator following any quickstart or the migration guide lands on a working v1alpha3 Project; no living doc describes removed machinery; dated snapshots preserved byte-identical.
</success_criteria>

<output>
Create `.planning/phases/40-deprecate-v1alpha1-api/40-06-SUMMARY.md` when done.
</output>
