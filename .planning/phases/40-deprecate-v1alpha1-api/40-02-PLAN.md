---
phase: 40-deprecate-v1alpha1-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/dispatch/envelope.go
  - pkg/dispatch/doc.go
  - pkg/dispatch/envelope_test.go
  - cmd/tide-push/main.go
  - cmd/tide-eval/main.go
  - cmd/tide-import/main_test.go
  - pkg/bundle/seed_test.go
  - internal/eval/render_test.go
  - internal/dispatch/podjob/jobspec_test.go
  - test/integration/envtest/reporter_materialize_test.go
  - test/integration/envtest/leak_blocked_test.go
  - test/integration/envtest/boundary_push_test.go
  - test/e2e/dashboard_test.go
  - test/e2e/gate_flow_test.go
  - internal/controller/planner_job_absent_test.go
  - internal/controller/phase_controller_test.go
  - internal/controller/wave_controller_test.go
  - internal/controller/project_pushresult_test.go
  - internal/controller/project_boundary_push_test.go
  - internal/controller/boundary_push_test.go
  - internal/controller/task_controller_metrics_test.go
  - internal/controller/wave_webhook_test.go
  - internal/controller/milestone_controller_test.go
autonomous: true
requirements: [CRANK-02]

must_haves:
  truths:
    - "Envelopes carry apiVersion dispatch.tideproject.k8s/v1alpha1; the old CRD-group string tideproject.k8s/v1alpha1 is rejected by ValidateAPIVersionKind"
    - "cmd/tide-eval references the pkg/dispatch constant instead of a duplicate literal (drift vector closed permanently — Pitfall 4)"
    - "pkg/dispatch/doc.go documents the group decoupling with the kubeadm precedent and no longer documents the superseded v1beta1 hub/spoke bump plan"
  artifacts:
    - path: "pkg/dispatch/envelope.go"
      provides: "decoupled envelope contract constant"
      contains: "dispatch.tideproject.k8s/v1alpha1"
    - path: "cmd/tide-push/main.go"
      provides: "hand-synced independent literal (tide-push does not import pkg/dispatch by design)"
      contains: "dispatch.tideproject.k8s/v1alpha1"
    - path: "pkg/dispatch/envelope_test.go"
      provides: "rejection test for the old undecoupled group string"
      contains: "UnknownAPIVersionError"
  key_links:
    - from: "pkg/dispatch/envelope.go APIVersionV1Alpha1"
      to: "internal/subagent/, cmd/claude-subagent/, cmd/stub-subagent/"
      via: "programmatic constant reference (zero literals — bump propagates automatically)"
      pattern: "APIVersionV1Alpha1"
    - from: "cmd/tide-eval/main.go"
      to: "pkg/dispatch"
      via: "pkgdispatch.APIVersionV1Alpha1 (replaces the hardcoded literal)"
      pattern: "pkgdispatch\\.APIVersionV1Alpha1"
---

<objective>
Decouple the envelope contract from the CRD API group per D-08: `tideproject.k8s/v1alpha1` → `dispatch.tideproject.k8s/v1alpha1` (kubeadm pattern — a K8s-shaped document that is not a served resource gets its own subdomain group). Version component stays `v1alpha1`; the constant NAME `APIVersionV1Alpha1` stays. Fix the two independent literal copies and every inline test fixture.

Purpose: prevents the "one crank bumps two unrelated things" collision the old doc.go v1beta1 plan would have caused; makes the CRD-group crank (waves 2-3) unable to break the subagent-image contract.
Output: decoupled constant, rewritten contract doc, tide-push/tide-eval literals fixed, all envelope test fixtures green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-deprecate-v1alpha1-api/40-CONTEXT.md
@.planning/phases/40-deprecate-v1alpha1-api/40-PATTERNS.md

<interfaces>
From pkg/dispatch/envelope.go (current):
```go
// APIVersionV1Alpha1 is the envelope contract version shipped in TIDE v1alpha1.
const APIVersionV1Alpha1 = "tideproject.k8s/v1alpha1"

func ValidateAPIVersionKind(apiVersion, kind, expectedKind string) error {
    if apiVersion != APIVersionV1Alpha1 { return &UnknownAPIVersionError{APIVersion: apiVersion} }
    if kind != expectedKind { return &UnknownKindError{Kind: kind} }
    return nil
}
```
Confirmed by grep: `internal/subagent/`, `cmd/claude-subagent/`, `cmd/stub-subagent/` reference the constant programmatically (zero literals) — they follow the value change with no edits.
</interfaces>

Boundary notes:
- Do NOT touch `examples/projects/dogfood/salvage-20260618/` envelope JSONs here. That directory is a LIVE kind Layer B fixture (test/integration/kind/import_resume_test.go:302) and is regenerated coherently in plan 40-03 alongside its CRD-side schemaRevision bump. Layer B does not run per-task, so no red window.
- Do NOT touch the owner-ref dual-accepts (`task_controller.go:1205`, `internal/dispatch/podjob/backend.go:377`) or any test fixture constructing `metav1.OwnerReference{APIVersion: "tideproject.k8s/v1alpha1"}` — those are CRD group-version usages owned by plan 40-03 (D-05).
- `.worktrees/pr3-debug/` is a stale untracked worktree — never edit under it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip the constant, rewrite the contract doc, extend envelope tests</name>
  <files>pkg/dispatch/envelope.go, pkg/dispatch/doc.go, pkg/dispatch/envelope_test.go</files>
  <read_first>
    - pkg/dispatch/envelope.go (constant :24, ValidateAPIVersionKind ~:424-439)
    - pkg/dispatch/doc.go (the D-A3 versioning paragraph :22-32 — the exact v1beta1 sentence D-08 supersedes)
    - pkg/dispatch/envelope_test.go (TestValidateAPIVersionKind_RejectsUnknownAPIVersion ~:329, TestEnvelopeIn_Constants ~:496 — the errors.As idiom to reuse)
  </read_first>
  <behavior>
    - Test 1: TestEnvelopeIn_Constants asserts APIVersionV1Alpha1 == "dispatch.tideproject.k8s/v1alpha1"
    - Test 2: new TestValidateAPIVersionKind_RejectsOldCRDGroupString — ValidateAPIVersionKind("tideproject.k8s/v1alpha1", KindTaskEnvelopeIn, KindTaskEnvelopeIn) returns *UnknownAPIVersionError (errors.As idiom)
    - Test 3: existing accept-path tests pass with the new value (any inline "tideproject.k8s/v1alpha1" fixture in this file updated)
  </behavior>
  <action>
    1. envelope.go:24 — change the constant VALUE to `"dispatch.tideproject.k8s/v1alpha1"`. Keep the name `APIVersionV1Alpha1` (version component is unchanged per D-08 — "pure group decoupling, no stability claim"). Update the constant's doc comment to state the group is the dispatch-contract subdomain, distinct from the CRD group.
    2. doc.go:22-32 — rewrite the versioning paragraph: envelope JSON carries `apiVersion: dispatch.tideproject.k8s/v1alpha1`; the group is deliberately decoupled from the CRD group `tideproject.k8s` so CRD version cranks cannot break the subagent-image contract; cite the kubeadm precedent (kubeadm.k8s.io is kubeadm's own config-file API group for K8s-shaped documents that are not served resources). DELETE the sentence "Future breaking changes ... ride a v1beta1 apiVersion bump via the same hub/spoke conversion path the CRDs use (CRD-05 scaffold)" — superseded by D-08; future envelope breaks bump the dispatch group's own version component instead. Keep the ValidateAPIVersionKind-first consumer discipline (D-A3) prose intact.
    3. envelope_test.go — implement the three behaviors above.
  </action>
  <verify>
    <automated>go test ./pkg/dispatch/... && make verify-dispatch-imports</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/dispatch/...` exits 0
    - `grep -c '"dispatch.tideproject.k8s/v1alpha1"' pkg/dispatch/envelope.go` == 1
    - `grep -n 'v1beta1' pkg/dispatch/doc.go` returns 0 hits
    - `grep -n 'kubeadm' pkg/dispatch/doc.go` returns ≥ 1 hit
    - `make verify-dispatch-imports` exits 0 (import firewall intact — pkg/dispatch gains no new imports)
  </acceptance_criteria>
  <done>Constant decoupled, old group string rejected under test, contract doc describes the new world with rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the literal copies and every inline envelope test fixture</name>
  <files>cmd/tide-push/main.go, cmd/tide-eval/main.go, plus the *_test.go files in frontmatter files_modified carrying inline envelope literals</files>
  <read_first>
    - cmd/tide-push/main.go (envelopeAPIVersion local const :116 — deliberately does NOT import pkg/dispatch)
    - cmd/tide-eval/main.go (:61 pkgdispatch import, :83 hardcoded literal — the Pitfall 4 drift)
    - pkg/bundle/seed_test.go (:141,:177,:204,:222 — inline envelope JSON fixture shape)
  </read_first>
  <action>
    1. cmd/tide-push/main.go:116 — `envelopeAPIVersion = "dispatch.tideproject.k8s/v1alpha1"`. It stays a literal: tide-push intentionally has no pkg/dispatch import (documented independence); add/keep a comment noting it must be hand-synced with pkgdispatch.APIVersionV1Alpha1.
    2. cmd/tide-eval/main.go:83 — replace the literal with `pkgdispatch.APIVersionV1Alpha1` (the file already imports pkgdispatch at :61). This closes the drift vector permanently — future cranks never touch tide-eval again.
    3. Sweep every remaining inline `"tideproject.k8s/v1alpha1"` in *_test.go files where the context is an ENVELOPE usage (EnvelopeIn/EnvelopeOut struct literals, envelope JSON strings, `apiVersion` JSON keys): replace with a `pkgdispatch.APIVersionV1Alpha1` reference where the file already imports pkg/dispatch (or the import is natural), else the new full string. Known carriers: cmd/tide-import/main_test.go, pkg/bundle/seed_test.go, internal/eval/render_test.go, internal/dispatch/podjob/jobspec_test.go, test/integration/envtest/{reporter_materialize,leak_blocked,boundary_push}_test.go, test/e2e/{dashboard,gate_flow}_test.go, internal/controller/{planner_job_absent,phase_controller,wave_controller,project_pushresult,project_boundary_push,boundary_push,task_controller_metrics,wave_webhook,milestone_controller}_test.go. DISCRIMINATOR: if the literal sits in a `metav1.OwnerReference{APIVersion: ...}` / OwnerReferences fixture, it is a CRD group-version usage — LEAVE IT (plan 40-03/D-05 owns those). Practical method: flip the constant first (Task 1 already did), run `make test` — every failure is an envelope-literal site; then grep-audit test/e2e (env-gated, not run by make test) by hand.
    4. Do NOT edit examples/projects/dogfood/salvage-20260618/ (owned by 40-03) or anything under .worktrees/.
  </action>
  <verify>
    <automated>go build ./... && make test</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && make test` exits 0
    - `grep -rn '"tideproject.k8s/v1alpha1"' --include='*.go' . | grep -v worktrees` returns ONLY owner-ref-context lines (the two dual-accepts at internal/controller/task_controller.go and internal/dispatch/podjob/backend.go, plus OwnerReference test fixtures) — zero envelope-context hits
    - `grep -n 'pkgdispatch.APIVersionV1Alpha1' cmd/tide-eval/main.go` returns ≥ 1 hit and `grep -n '"tideproject.k8s/v1alpha1"' cmd/tide-eval/main.go` returns 0 hits
    - `grep -c '"dispatch.tideproject.k8s/v1alpha1"' cmd/tide-push/main.go` == 1
  </acceptance_criteria>
  <done>All envelope-value copies moved together (the exact drift class RESEARCH.md Pitfall 4 warned about); build and unit tier green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → subagent container | Envelope JSON crosses the pod boundary into externally-authored subagent images |
| pkg/dispatch import firewall | pkg/dispatch MUST NOT import controller-runtime/anthropic/internal (D-A3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Tampering | envelope apiVersion discriminator | mitigate | ValidateAPIVersionKind fail-closed on any non-matching string; new rejection test pins the old group string as rejected; all three literal copies (constant, tide-push, tide-eval) move in one plan |
| T-40-05 | Elevation of Privilege | pkg/dispatch import surface | mitigate | `make verify-dispatch-imports` gate run in-plan; no new imports added to pkg/dispatch |
| T-40-06 | Tampering | stale subagent image processing old-group envelopes | accept | single-user reality (D-01/D-03): all in-repo images rebuild from source at next release; no third-party images exist yet — contract doc + migration guide (40-06) carry the notice |

No package-manager installs in this plan — supply-chain checkpoint N/A.
</threat_model>

<verification>
- `go test ./pkg/dispatch/...`, `go build ./...`, `make test`, `make verify-dispatch-imports` all green
- Envelope-context literal grep returns zero non-owner-ref hits
</verification>

<success_criteria>
The envelope contract lives at `dispatch.tideproject.k8s/v1alpha1` with the old group string provably rejected, the superseded v1beta1 plan erased from doc.go, and zero remaining inline envelope literals that could drift on a future crank (tide-push's single documented hand-sync literal excepted, by design).
</success_criteria>

<output>
Create `.planning/phases/40-deprecate-v1alpha1-api/40-02-SUMMARY.md` when done.
</output>
