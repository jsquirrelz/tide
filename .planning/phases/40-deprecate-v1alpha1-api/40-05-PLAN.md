---
phase: 40-deprecate-v1alpha1-api
plan: 05
type: execute
wave: 3
depends_on: [40-03]
files_modified:
  - "api/v1alpha1/** (DELETED — 13 files)"
  - "api/v1alpha2/** (DELETED — 11 files)"
  - test/schema/dogfood_manifests_test.go (new — relocated)
  - Makefile
  - PROJECT
  - "config/crd/bases/*.yaml (regen — single-version)"
  - "charts/tide-crds/templates/*.yaml (regen)"
autonomous: true
requirements: [CRANK-05]

must_haves:
  truths:
    - "api/v1alpha1 and api/v1alpha2 no longer exist; the repo builds and the unit tier passes without them"
    - "All 6 CRD manifests carry exactly ONE version block (v1alpha3, served+storage) — roughly half their prior YAML"
    - "verify-no-aggregates checks api/v1alpha*/*_types.go version-agnostically AND fail-closes if the glob resolves to nothing (the silently-dead-gate class from RESEARCH Pitfall 3 is structurally fixed — D-12 mandatory scope)"
    - "The dogfood-fixture strict-decode coverage survives the package deletion (relocated, not dropped)"
  artifacts:
    - path: "test/schema/dogfood_manifests_test.go"
      provides: "relocated fixture strict-decode test, v1alpha3-only allowlist"
      contains: "tideproject.k8s/v1alpha3"
    - path: "Makefile"
      provides: "hardened verify-no-aggregates"
      contains: "api/v1alpha*"
    - path: "PROJECT"
      provides: "kubebuilder metadata pointing at api/v1alpha3 (stale since Phase 23)"
      contains: "version: v1alpha3"
  key_links:
    - from: "Makefile verify-no-aggregates"
      to: "api/v1alpha3/*_types.go"
      via: "version-agnostic glob + empty-glob fail-closed guard"
      pattern: "api/v1alpha\\*"
    - from: "test/schema/dogfood_manifests_test.go"
      to: "examples/projects/dogfood/0*.yaml"
      via: "dogfoodGlob + sigsyaml.UnmarshalStrict against tideprojectv1alpha3.Project"
      pattern: "UnmarshalStrict"
---

<objective>
Remove the v1alpha1 and v1alpha2 Go packages entirely and repoint version-hardcoded build tooling in the SAME commit (D-12 mandatory), relocating the one test that must survive (dogfood-fixture strict-decode), then regenerate single-version CRD manifests and chart templates.

Purpose: the sixth step Phase 23 never needed — under D-03 reinstall-only migration there is no legacy decode path to preserve, so v1alpha3 becomes the sole registered version end to end.
Output: deleted packages, hardened verify-no-aggregates, fixed PROJECT metadata, 6 single-version CRDs, reproducible chart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-deprecate-v1alpha1-api/40-CONTEXT.md
@.planning/phases/40-deprecate-v1alpha1-api/40-PATTERNS.md

Preconditions established by 40-03 (verify before starting): zero `api/v1alpha2` imports outside api/v1alpha2 itself, and the only `api/v1alpha1` importers are files INSIDE api/v1alpha1 (the guard test's import was dropped in 40-03). If `grep -rl 'api/v1alpha1\|api/v1alpha2' --include='*.go' . | grep -v worktrees | grep -v '^./api/v1alpha'` returns anything, STOP and fix the leak first — do not delete under it.

Coverage accounting for the 4 dying v1alpha1 test files: dogfood_manifests_test.go is RELOCATED (this plan); aggregates_guard_test.go's concern is carried by the hardened verify-no-aggregates gate; phase3_schema_test.go / phase4_constants_test.go assert v1alpha1-era shapes superseded by api/v1alpha3/schema_test.go (40-01) — intentionally dropped, note this in the SUMMARY.

Tier note: `make test` selects packages via `go list ./... | grep -v /e2e | grep -v /test/integration` — the relocated test goes to `test/schema/` (NOT test/integration/) precisely so it STAYS in the fast unit tier, where it lives today as part of the api/v1alpha1 package.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the dogfood test, delete both packages, repoint tooling — one commit</name>
  <files>test/schema/dogfood_manifests_test.go (new), api/v1alpha1/ (delete), api/v1alpha2/ (delete), Makefile, PROJECT</files>
  <read_first>
    - api/v1alpha1/dogfood_manifests_test.go (the multi-version strict-decode pattern; helpers dogfoodGlob/splitYAMLDocs/isProjectDoc/hasTopLevelKey/findRepoRoot copy verbatim — they are version-agnostic)
    - Makefile (:552-561 verify-no-aggregates — current hardcoded glob + the `|| true` that swallows no-such-file errors)
    - PROJECT (all 6 resources entries: path .../api/v1alpha1, version v1alpha1; the Plan entry's stale `conversion: true`)
  </read_first>
  <action>
    Order matters — relocate BEFORE deleting so the tree is never coverage-less:

    1. Create test/schema/dogfood_manifests_test.go, package `schema` (test-only package is valid Go): copy the version-agnostic helpers verbatim from api/v1alpha1/dogfood_manifests_test.go; collapse `supportedProjectAPIVersions` to the single entry `"tideproject.k8s/v1alpha3": true`; the decode switch keeps ONE case decoding into `tideprojectv1alpha3.Project` via sigsyaml.UnmarshalStrict; drop the tideprojectv1alpha1/tideprojectv1alpha2 imports. Adjust findRepoRoot's relative-depth logic for the new location (test/schema is 2 levels below root vs api/v1alpha1's 2 — verify by running).
    2. `git rm -r api/v1alpha1 api/v1alpha2`.
    3. Makefile verify-no-aggregates — harden per D-12's decided form: description/echo mention `api/v1alpha*` (version-agnostic); recipe resolves `FILES=$$(ls api/v1alpha*/*_types.go 2>/dev/null)` and EXITS 1 with "no api/v1alpha*/*_types.go files found — gate misconfigured" when empty (this is the fix for the silently-dead-gate class: the old `|| true` swallowed the glob error); then greps the resolved FILES for the existing pattern `Schedule|Waves *\[\]|IndegreeMap|CachedDag|DerivedDag` and fails on matches as today. Keep the PERSIST-02 references in the help text.
    4. PROJECT — all 6 resources entries: `path: github.com/jsquirrelz/tide/api/v1alpha3`, `version: v1alpha3`; DELETE `conversion: true` from the Plan entry's webhooks block (conversion webhook retired in Phase 23 commit c5f136e); keep `validation: true` where present. This file is decorative for make targets but feeds future `kubebuilder edit` invocations.
    5. Gate-liveness proof (transient, do not commit): append a line containing `CachedDag` to api/v1alpha3/project_types.go, run `make verify-no-aggregates`, observe exit 1; revert the line; run again, observe exit 0. Record both observations in the SUMMARY.
  </action>
  <verify>
    <automated>go build ./... && make test && make verify-no-aggregates && go test ./test/schema/...</automated>
  </verify>
  <acceptance_criteria>
    - `ls api/` shows ONLY v1alpha3
    - `go test ./test/schema/...` exits 0 and its package appears in `go list ./... | grep -v /e2e | grep -v /test/integration` output (unit tier retained)
    - `grep -c 'api/v1alpha\*' Makefile` ≥ 1 and `grep -n 'api/v1alpha1\|api/v1alpha2' Makefile` returns 0 hits
    - verify-no-aggregates exits 1 on the seeded `CachedDag` token and 0 after revert (liveness proven, both observed)
    - `grep -c 'version: v1alpha3' PROJECT` == 6; `grep -n 'conversion: true' PROJECT` returns 0 hits
    - `make test` exits 0
  </acceptance_criteria>
  <done>Both legacy packages gone, fixture-validation coverage relocated intact, tooling can never silently go dark on a future crank.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate single-version CRD manifests + chart</name>
  <files>config/crd/bases/*.yaml (regen), charts/tide-crds/templates/*.yaml (regen)</files>
  <read_first>
    - config/crd/bases/tideproject.k8s_projects.yaml (current 3-version transitional shape from 40-01 — understand what the regen removes)
  </read_first>
  <action>
    1. `make manifests` — with only api/v1alpha3 present, controller-gen emits exactly one version block per CRD (config/webhook/manifests.yaml regenerates with no diff — webhooks already v1alpha3 since 40-03).
    2. `make helm-crds` — propagate to charts/tide-crds/templates/. Never hand-edit.
    3. `make verify-chart-reproducible` — must pass.
    4. Chart.yaml version/appVersion still untouched (release-time batching at milestone close — bumping here would violate the Phase-35/36 chart-batching decision recorded in STATE.md).
  </action>
  <verify>
    <automated>make manifests && make helm-crds && make verify-chart-reproducible && make test-int-fast</automated>
  </verify>
  <acceptance_criteria>
    - For EACH of the 6 files in config/crd/bases/: `grep -c 'name: v1alpha' <file>` == 1 (v1alpha3 only) and `grep -c 'storage: true' <file>` == 1
    - `grep -rn 'v1alpha1\|v1alpha2' config/crd/bases/ charts/tide-crds/templates/` returns 0 hits
    - `wc -l config/crd/bases/tideproject.k8s_projects.yaml` ≈ 580-650 lines (one version block — the "roughly half" end state from CONTEXT.md)
    - `make verify-chart-reproducible` exits 0; `git diff --stat charts/tide-crds/Chart.yaml charts/tide/Chart.yaml` empty
    - `make test-int-fast` exits 0 (Layer A green against single-version CRDs)
  </acceptance_criteria>
  <done>Six single-version CRD manifests and matching chart templates; envtest Layer A green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → repo invariants | verify-no-aggregates is a CI-enforced PERSIST-02 control; a silently-dead gate is a security regression (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-14 | Tampering (dead control) | Makefile verify-no-aggregates | mitigate | version-agnostic glob + empty-glob fail-closed exit 1, landing in the SAME commit as the package deletion; liveness proven by transient bad-token seed (observed exit 1) |
| T-40-15 | Tampering | regenerated CRD/chart YAML | mitigate | generation-only (make manifests / make helm-crds); verify-chart-reproducible gate in-plan |
| T-40-16 | Denial of Service | fixture-validation coverage loss | mitigate | dogfood strict-decode test relocated to test/schema/ BEFORE deletion, stays in unit tier; dropped v1alpha1-era tests explicitly accounted (superseded by 40-01 schema test + hardened gate) |

No package-manager installs in this plan — supply-chain checkpoint N/A.
</threat_model>

<verification>
- `go build ./... && make test && make verify-no-aggregates && go test ./test/schema/... && make verify-chart-reproducible && make test-int-fast` all green
- Gate liveness observed both ways (seeded fail, clean pass)
</verification>

<success_criteria>
v1alpha3 is the only API package, the only CRD version block, and the only chart-template version; the aggregates gate is provably alive and future-crank-proof; no test coverage silently lost.
</success_criteria>

<output>
Create `.planning/phases/40-deprecate-v1alpha1-api/40-05-SUMMARY.md` when done.
</output>
