---
phase: 40-deprecate-v1alpha1-api
plan: 03
type: execute
wave: 2
depends_on: [40-01, 40-02]
files_modified:
  - "internal/controller/**/*.go (all api/v1alpha2 importers, ~18 non-test + tests)"
  - "internal/webhook/v1alpha3/** (git mv from internal/webhook/v1alpha2)"
  - "internal/dispatch/podjob/{backend,caps,jobspec}.go + tests"
  - "internal/gates/{boundary,policy}.go (import repoint ONLY — no logic change)"
  - "internal/budget/*.go, internal/reporter/materialize.go"
  - cmd/manager/main.go
  - cmd/dashboard/main.go
  - "cmd/dashboard/api/*.go"
  - "cmd/tide/*.go (root_flags + subcommands + tests)"
  - cmd/tide-import/main.go
  - cmd/tide-reporter/main.go
  - scripts/gen-salvage-seed/main.go
  - internal/controller/project_controller_v2_guard_test.go
  - "test/integration/envtest/*.go (imports + SchemaRevision literals + webhook wiring)"
  - "test/integration/kind/*.go + test/integration/kind/testdata/**/*.yaml"
  - "examples/projects/{small,medium,large}/project.yaml"
  - "examples/projects/dogfood/{01-analytics,02-codex-runtime,03-project-editor}-project.yaml, examples/projects/dogfood/run-2/project.yaml"
  - "examples/projects/dogfood/salvage-20260618/** (CR YAMLs + envelope JSONs + regenerated seed-manifest.json)"
  - test/e2e/testdata/live-claude-project.yaml
  - config/webhook/manifests.yaml (regen)
  - charts/tide/templates/validating-webhook-configuration.yaml (regen)
autonomous: true
requirements: [CRANK-03]

must_haves:
  truths:
    - "Every binary (manager, dashboard, tide CLI, tide-import, tide-reporter) and every test compiles and runs against api/v1alpha3 only — zero api/v1alpha2 imports remain"
    - "checkSchemaRevisionGuard fail-closes any Project whose schemaRevision != v1alpha3, with the expected revision and migration-doc path held in two constants (a v1alpha4 crank changes exactly those two lines — D-04)"
    - "Owner-ref resolution accepts ONLY tideproject.k8s/v1alpha3 (D-05 — both dual-accept clauses gone)"
    - "All live fixtures (kind testdata, examples, e2e testdata, salvage bundle) apply cleanly under v1alpha3 with schemaRevision: v1alpha3"
  artifacts:
    - path: "internal/webhook/v1alpha3/project_webhook.go"
      provides: "v1alpha3 admission webhooks (whole-package rename from v1alpha2)"
      contains: "versions=v1alpha3"
    - path: "internal/controller/project_controller.go"
      provides: "generalized SchemaRevision guard"
      contains: "expectedSchemaRevision"
    - path: "cmd/manager/main.go"
      provides: "single v1alpha3 scheme registration (duplicate AddToScheme bug fixed)"
      contains: "tidev1alpha3.AddToScheme"
  key_links:
    - from: "cmd/manager/main.go"
      to: "internal/webhook/v1alpha3"
      via: "webhookv1alpha3.Setup{Plan,Wave,Project}WebhookWithManager (3 call sites)"
      pattern: "webhookv1alpha3\\."
    - from: "test/integration/envtest/suite_test.go"
      to: "internal/webhook/v1alpha3"
      via: "same 3-call-site wiring as the manager"
      pattern: "webhookv1alpha3\\."
    - from: "internal/controller/project_controller.go checkSchemaRevisionGuard"
      to: "docs/migration/v1alpha2-to-v1alpha3.md"
      via: "migrationGuideDocPath constant referenced in the RequiresReinstall message"
      pattern: "v1alpha2-to-v1alpha3"
---

<objective>
Migrate every consumer from api/v1alpha2 to api/v1alpha3 in one atomic green unit: import/alias repoint (~53 non-test + ~114 test files), whole-package webhook rename, scheme registrations (fixing the duplicate-AddToScheme bug), SchemaRevision guard generalization (D-04), owner-ref dual-accept removal (D-05), Go fixture literal bumps, and the full YAML fixture/testdata sweep including the salvage bundle regeneration.

Purpose: this is Phase 23 Plan 23-04's shape re-run one crank later — after it, nothing outside api/v1alpha1+v1alpha2 themselves references the old versions, unblocking the wave-3 removal.
Output: all binaries and tests on v1alpha3; guard expecting v1alpha3; fixtures applying cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-deprecate-v1alpha1-api/40-CONTEXT.md
@.planning/phases/40-deprecate-v1alpha1-api/40-PATTERNS.md

Atomicity note (why Task 1 is one big commit): the import crank, the Go `SchemaRevision: "v1alpha2"` fixture literals, and the guard generalization CANNOT be split greenly — v1alpha3's CEL enum only admits `v1alpha3`, and the guard blocks whatever the enum admits if the guard string lags. Phase 23's 23-04 precedent (`8ec1dbe`..`bff8df9`) ran the same shape. YAML fixtures (Task 2) may lag one task because kind Layer B only runs at the phase gate.

Hard boundaries:
- `.worktrees/pr3-debug/` is a stale untracked worktree present in the working dir. EXCLUDE it from every sed/sweep (`grep -rl ... | grep -v worktrees`).
- Do NOT touch `internal/gates/policy.go` logic or `DefaultGates`/`EvaluatePolicy` semantics — Gates.{Milestone,...} gates the CR's OWN boundary; only the import line changes (RESEARCH.md Pitfall 1).
- Do NOT change any dispatch level string ("project"/"milestone"/"phase"/"plan"/"task") passed to BuildPlannerEnvelope/resolveImage/BuildOptions — that is plan 40-04's scope (D-02).
- `cmd/credproxy/` must gain NO api/ import (design firewall — RESEARCH.md Pitfall 5; no analyzer enforces this today, so it is an explicit acceptance criterion here).
- `charts/tide/values.yaml` is a FIXED contract — zero edits. Only helmify regeneration of templates is allowed (`make helm-controller`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic code crank — imports, webhook package, schemes, guard, Go literals, comments</name>
  <files>all *.go importing api/v1alpha2 (enumerate via grep), internal/webhook/v1alpha2→v1alpha3 (git mv), cmd/manager/main.go, cmd/dashboard/main.go, cmd/tide/root_flags.go, internal/controller/project_controller.go, internal/controller/project_controller_v2_guard_test.go, test/integration/envtest/suite_test.go, internal/controller/suite_test.go, scripts/gen-salvage-seed/main.go, config/webhook/manifests.yaml (regen), charts/tide/templates/validating-webhook-configuration.yaml (regen)</files>
  <read_first>
    - .planning/phases/40-deprecate-v1alpha1-api/40-PATTERNS.md (sections: webhook registration pattern, scheme registration triple, checkSchemaRevisionGuard current+target shape — exact code excerpts)
    - internal/controller/project_controller.go (guard :1662-1704 + call site ~:260)
    - internal/controller/project_controller_v2_guard_test.go (currently imports api/v1alpha1 for the old-shape simulation — that import must die here)
    - cmd/manager/main.go (:303-311 scheme block with the duplicate AddToScheme + factually-wrong comment; :592-604 webhook wiring; :65-67 import aliases)
    - internal/webhook/v1alpha2/project_webhook.go (the +kubebuilder:webhook marker line — THREE v1alpha2 occurrences per marker: path segment, versions=, name= suffix)
  </read_first>
  <action>
    Enumerate targets first: `grep -rl 'api/v1alpha2' --include='*.go' . | grep -v worktrees | grep -v '^./api/v1alpha2'` and `grep -rln 'SchemaRevision: *"v1alpha2"\|"tideproject.k8s/v1alpha2"' --include='*.go' . | grep -v worktrees`. Then apply, as ONE commit:

    1. IMPORT REPOINT: `github.com/jsquirrelz/tide/api/v1alpha2` → `github.com/jsquirrelz/tide/api/v1alpha3` in every hit; aliases `tidev1alpha2`→`tidev1alpha3`, `tideprojectv1alpha2`→`tideprojectv1alpha3` (preserve each file's existing alias style).
    2. WEBHOOK PACKAGE: `git mv internal/webhook/v1alpha2 internal/webhook/v1alpha3`; inside: `package v1alpha2`→`package v1alpha3`; import alias repoint; logger names (`project-webhook-v1alpha2` etc.) → v1alpha3; every `+kubebuilder:webhook:` marker — all THREE v1alpha2 occurrences per line (`path=/validate-tideproject-k8s-v1alpha3-<kind>`, `versions=v1alpha3`, `name=v<kind>-v1alpha3.kb.io`). A naive single find-replace under-edits these; verify per-marker.
    3. WIRING: `webhookv1alpha2.` → `webhookv1alpha3.` at cmd/manager/main.go:592-604, test/integration/envtest/suite_test.go:222-225, and internal/controller/plan_controller.go + internal/controller/suite_test.go (both import the webhook package — grep confirms).
    4. SCHEMES: cmd/manager/main.go:303-311 — collapse the DUPLICATE `tidev1alpha2.AddToScheme(scheme)` pair to a single `tidev1alpha3.AddToScheme(scheme)`; DELETE the stale comment claiming "v1alpha1 remains registered so the manager can decode surviving v1alpha1 objects" (false today; under D-03 reinstall-only NOTHING legacy is registered). Same single-registration bump in cmd/dashboard/main.go:97-102 and cmd/tide/root_flags.go:50-57 (fix the ":50/:86 v1alpha1" stale comments too).
    5. GUARD (D-04): rewrite checkSchemaRevisionGuard against tidev1alpha3.Project with two new package-level constants — `expectedSchemaRevision = "v1alpha3"` and `migrationGuideDocPath = "docs/migration/v1alpha2-to-v1alpha3.md"` — used in both the comparison and the RequiresReinstall condition message (message shape mirrors the current one: kubectl delete + re-apply with `schemaRevision: v1alpha3`, see <docpath>). Keep fail-closed behavior byte-equivalent: Ready=False, Reason=RequiresReinstall, reconcile.TerminalError. Rewrite the function comment revision-agnostically ("authored under a prior schema revision"), so a v1alpha4 crank changes exactly the two constants.
    6. GUARD TEST: project_controller_v2_guard_test.go — DROP the api/v1alpha1 import and its AddToScheme; the old-shape simulation constructs a v1alpha3 Project with `SchemaRevision: ""` (and a wrong-value case `"v1alpha2"`) directly — it never needed the cross-version import. Assert blocked=true, RequiresReinstall reason, and message contains both "v1alpha3" and the new doc path.
    7. GO LITERALS: every `SchemaRevision: "v1alpha2"` / `schemaRevision: v1alpha2` in Go files → `"v1alpha3"` (≈20 files: test/integration/envtest/* incl. spec_conformance_test.go's 2 occurrences and planner_dispatch_test.go, cmd/dashboard/api/{projects,tasks}_test.go, cmd/tide/{approve,resume,describe_budget}_test.go, test/integration/kind/fixtures_test.go). Every `"tideproject.k8s/v1alpha2"` Go literal (cmd/tide/export_envelopes_run_test.go, cmd/tide/import_envelopes_test.go, test/integration/kind/suite_test.go) → prefer `tideprojectv1alpha3.GroupVersion.String()`, else the literal `"tideproject.k8s/v1alpha3"`. scripts/gen-salvage-seed/main.go:201/:206 embedded template → `apiVersion: tideproject.k8s/v1alpha3` + `schemaRevision: v1alpha3`.
    8. COMMENTS (D-07): in every file this task touches, comments must describe the v1alpha3 world — stale "v1alpha1"/"Spring Tide Plan 23-0X"-era claims that are now false get corrected (cmd/manager/main.go:65-66,303-309 are the named offenders). Audit with `git diff --name-only | xargs grep -in 'v1alpha1'` before committing.
    9. REGEN: `make manifests` (config/webhook/manifests.yaml picks up the v1alpha3 webhook paths) + `make helm-controller` (charts/tide/templates/validating-webhook-configuration.yaml) + `make verify-chart-reproducible`. Never hand-edit either generated file. `charts/tide/values.yaml` and Chart.yaml untouched.

    EXECUTION-ORDER GUARDRAIL (plan-checker warning: ~53 non-test + ~114 test files in one task): the COMMIT stays atomic, but verify in stages so a late failure doesn't force restarting the whole sweep — after step 1 run `go build ./...` (import repoint compiles before any semantic edits); after steps 2-4 run `go build ./...` again (webhook/scheme wiring); after steps 5-7 run `make test`; steps 8-9 then close out. If a stage won't converge after ~3 attempts, stop and surface rather than brute-forcing — the stage boundary tells you which class of edit broke.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl 'api/v1alpha2' --include='*.go' . | grep -v worktrees | grep -v '^./api/v1alpha2'` returns 0 files
    - `ls internal/webhook/v1alpha2 2>/dev/null` fails; `grep -c 'versions=v1alpha3' internal/webhook/v1alpha3/*.go` ≥ 3 and `grep -c 'v1alpha2' internal/webhook/v1alpha3/*.go` == 0
    - `grep -c 'AddToScheme' cmd/manager/main.go` shows the duplicate collapsed (exactly one tide scheme registration; clientgoscheme separate)
    - `grep -n 'expectedSchemaRevision\|migrationGuideDocPath' internal/controller/project_controller.go` returns both constants; `grep -rn 'api/v1alpha1' internal/controller/project_controller_v2_guard_test.go` returns 0
    - `grep -rn 'SchemaRevision: "v1alpha2"' --include='*.go' . | grep -v worktrees | grep -v '^./api/'` returns 0
    - `grep -rn 'jsquirrelz/tide/api/' cmd/credproxy/` returns 0 (firewall intact)
    - `make test` exits 0; `make verify-chart-reproducible` exits 0
  </acceptance_criteria>
  <done>All consumers compile and pass the unit tier on v1alpha3; guard fail-closes on non-v1alpha3 revisions; webhooks serve v1alpha3 paths; generated webhook manifests/chart templates propagated.</done>
</task>

<task type="auto">
  <name>Task 2: Owner-ref dual-accept removal + YAML fixture/testdata sweep + salvage regeneration</name>
  <files>internal/controller/task_controller.go, internal/dispatch/podjob/backend.go, owner-ref test fixtures, test/integration/kind/testdata/**/*.yaml, examples/projects/**/project.yaml + dogfood YAMLs, examples/projects/dogfood/salvage-20260618/**, test/e2e/testdata/live-claude-project.yaml</files>
  <read_first>
    - internal/controller/task_controller.go (:1200-1210 walkOwnerChainToProjectDepth dual-accept)
    - internal/dispatch/podjob/backend.go (:372-380 walkOwnerChain — byte-identical pattern)
    - test/integration/kind/import_resume_test.go (:296-310 — proves salvage-20260618 is a LIVE Layer B fixture imported via the real CLI)
    - scripts/gen-salvage-seed/main.go (header comment — regeneration usage: `go run ./scripts/gen-salvage-seed examples/projects/dogfood/salvage-20260618`)
    - examples/projects/dogfood/salvage-20260618/projects.yaml (:36 carries `modelSelection: {}` — the field v1alpha3 dropped)
  </read_first>
  <action>
    1. D-05: in both walk functions, replace `ref.APIVersion == "tideproject.k8s/v1alpha1" || ref.APIVersion == tideprojectv1alpha3.GroupVersion.String()` (the alias already moved in Task 1) with the single clause `ref.APIVersion == tideprojectv1alpha3.GroupVersion.String()`. Update every test fixture that constructs `metav1.OwnerReference{APIVersion: "tideproject.k8s/v1alpha1"}` (the owner-ref-context residue plan 40-02 deliberately left — enumerate via `grep -rn '"tideproject.k8s/v1alpha1"' --include='*.go' . | grep -v worktrees`) to `tideprojectv1alpha3.GroupVersion.String()`. After this, that grep returns ZERO hits repo-wide.
    2. KIND TESTDATA: bump `apiVersion: tideproject.k8s/v1alpha2` → `v1alpha3` and `schemaRevision: v1alpha2` → `v1alpha3` in test/integration/kind/testdata/{bare-project,up-stack-project,push-lease-project,chaos-resume-three-task}.yaml and import-small-fixture/{project,projects,milestones,phases,plans}.yaml + import-partial-fixture/{project,milestones,phases,plans}.yaml (child-CR files carry only the apiVersion — schemaRevision is a ProjectSpec field).
    3. EXAMPLES: same bump in examples/projects/{small,medium,large}/project.yaml (small is the acceptance-smoke project), examples/projects/dogfood/{01-analytics,02-codex-runtime,03-project-editor}-project.yaml and run-2/project.yaml. 02-codex-runtime gets its SECOND conversion — refresh its Phase-23 migration prose comments to describe the v1alpha3 state.
    4. E2E: test/e2e/testdata/live-claude-project.yaml — bump apiVersion + schemaRevision AND fix its stale comment claiming sync with `api/v1alpha1/project_types.go` → `api/v1alpha3/project_types.go`.
    5. SALVAGE BUNDLE (live Layer B fixture): (a) sed every `"tideproject.k8s/v1alpha1"` envelope apiVersion in examples/projects/dogfood/salvage-20260618/pvc-envelopes/envelopes/**/{in,out}.json and push/*.json → `"dispatch.tideproject.k8s/v1alpha1"` (the 40-02 constant value); (b) sed the CR YAMLs (projects.yaml, milestones.yaml, phases.yaml, plans.yaml) apiVersion → v1alpha3, set `schemaRevision: v1alpha3` in projects.yaml, and DELETE the `modelSelection: {}` line (projects.yaml:36 — field removed in v1alpha3 per D-10); (c) re-run `go run ./scripts/gen-salvage-seed examples/projects/dogfood/salvage-20260618` to regenerate seed-manifest.json + project.yaml coherently (the template was bumped in Task 1 step 7 — verify the regenerated project.yaml says v1alpha3/schemaRevision v1alpha3).
  </action>
  <verify>
    <automated>make test && make test-int-fast</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '"tideproject.k8s/v1alpha1"' --include='*.go' . | grep -v worktrees` returns 0 hits (dual-accepts and all owner-ref fixtures gone)
    - `grep -rln 'schemaRevision: v1alpha2' --include='*.yaml' . | grep -v worktrees | grep -v .planning` returns 0 files
    - `grep -rln 'tideproject.k8s/v1alpha2' --include='*.yaml' . | grep -v worktrees | grep -v .planning | grep -v 'config/crd\|charts/tide-crds'` returns 0 files (CRD manifests still legitimately carry the v1alpha2 block until plan 40-05)
    - `grep -rn 'modelSelection' examples/ | grep -v worktrees` returns 0 hits
    - `grep -rc '"dispatch.tideproject.k8s/v1alpha1"' examples/projects/dogfood/salvage-20260618/pvc-envelopes/envelopes/*/in.json | awk -F: '$2==0' | wc -l` == 0 (every in.json carries the new group)
    - `make test-int-fast` exits 0 (Layer A green on v1alpha3 end-to-end)
  </acceptance_criteria>
  <done>Owner-chain resolution is v1alpha3-only; every live YAML/JSON fixture applies under the new schema; salvage bundle internally consistent (hash manifest regenerated, not hand-patched).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| etcd object → reconciler | Stale-schema objects must be rejected loudly, not silently mis-reconciled |
| child CR → parent Project resolution | Owner-ref walking grants dispatch context (budget, creds config) — must not resolve via spoofable legacy GroupVersions |
| credproxy | MUST NOT import api/ (unenforced-by-analyzer convention — manual gate here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Tampering / Info Disclosure | checkSchemaRevisionGuard | mitigate | generalized guard stays fail-closed (Ready=False / RequiresReinstall / TerminalError); guard test asserts empty AND wrong-value revisions both block |
| T-40-08 | Spoofing | owner-ref walk (task_controller, podjob backend) | mitigate | D-05 narrows accepted APIVersion to exactly one GroupVersion — smaller trust surface than the dual-accept |
| T-40-09 | Elevation of Privilege | cmd/credproxy import surface | mitigate | explicit acceptance criterion: zero `jsquirrelz/tide/api/` imports in cmd/credproxy (no analyzer covers this — RESEARCH Pitfall 5) |
| T-40-10 | Input Validation (ASVS V5) | v1alpha3 admission webhooks | mitigate | webhook package migrates whole (validator logic untouched, version-agnostic); envtest suite re-wires all 3 webhooks — cycle-detection and strict-mode coverage preserved |

No package-manager installs in this plan — supply-chain checkpoint N/A.
</threat_model>

<verification>
- `go build ./... && go vet ./... && make test && make test-int-fast` green
- All acceptance grep gates above
- Full `make test-int` (Layer B) deferred to the phase gate (plan 40-07) per VALIDATION.md sampling
</verification>

<success_criteria>
The entire consumer surface — controllers, webhooks, dispatch, CLI, dashboard, in-namespace Job images, tests, and every live fixture — runs on v1alpha3 with the generalized fail-closed guard and single-GroupVersion owner-ref resolution. api/v1alpha1 and api/v1alpha2 have zero remaining importers outside their own directories.
</success_criteria>

<output>
Create `.planning/phases/40-deprecate-v1alpha1-api/40-03-SUMMARY.md` when done.
</output>
