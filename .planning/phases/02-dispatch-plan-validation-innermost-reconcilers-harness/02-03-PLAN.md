---
phase: 2
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - api/v1alpha1/project_types.go
  - api/v1alpha1/task_types.go
  - api/v1alpha1/plan_types.go
  - api/v1alpha1/shared_types.go
  - api/v1alpha1/aggregates_guard_test.go
  - api/v1alpha1/zz_generated.deepcopy.go
  - config/crd/bases/tideproject.k8s_projects.yaml
  - config/crd/bases/tideproject.k8s_tasks.yaml
  - config/crd/bases/tideproject.k8s_plans.yaml
autonomous: true
requirements: [PERSIST-03]
must_haves:
  truths:
    - "`Project.Spec` carries `providerSecretRef`, `providers[]`, `budget`, `planAdmission`, `maxAttemptsPerTask` per RESEARCH.md K8s Resource Schemas — applied via kubectl + admitted by CEL."
    - "`Task.Spec.Dev.TestMode` accepts the four enum values success | fail-exit-1 | hang | exceed-output-paths and rejects anything else via kubebuilder Enum marker."
    - "`Plan.Status.ValidationState` accepts Pending | Validated | CycleDetected | FileTouchMismatch."
    - "`Project.Status.Budget` records tokensSpent + costSpentCents + windowStart — but is NOT an aggregate-schedule field (PERSIST-02 / Pitfall 4 stays clean)."
    - "Phase 2 condition constants (`Validated`, `BudgetExceeded`, `Running`, `Succeeded`) and reason constants (`CycleDetected`, `FileTouchMismatch`, `CapHit`, `RateLimitHit`, `BypassApplied`) ship in `shared_types.go`."
    - "`make verify-no-aggregates` continues to exit 0 — Phase 2 schema additions DO NOT introduce a `Schedule`, `Waves []`, `IndegreeMap`, `CachedDag`, or `DerivedDag` token anywhere under `api/v1alpha1/*_types.go`."
  artifacts:
    - path: api/v1alpha1/project_types.go
      provides: "Project.Spec.ProviderSecretRef, Providers, Budget, PlanAdmission, MaxAttemptsPerTask + Project.Status.Budget"
      contains: "ProviderSecretRef"
    - path: api/v1alpha1/task_types.go
      provides: "Task.Spec.Dev.TestMode enum + Task.Spec.DeclaredOutputPaths (MinItems=1)"
      contains: "Dev TaskDev"
    - path: api/v1alpha1/plan_types.go
      provides: "Plan.Status.ValidationState enum + Plan.Status.CycleEdges"
      contains: "ValidationState"
    - path: api/v1alpha1/shared_types.go
      provides: "Phase 2 condition + reason constants"
      contains: "ConditionValidated"
  key_links:
    - from: "api/v1alpha1/project_types.go"
      to: "Project.Status.Budget tally consumer in TaskReconciler (Plan 09)"
      via: "BudgetStatus struct read at completion roll-up"
      pattern: "BudgetStatus"
    - from: "api/v1alpha1/aggregates_guard_test.go"
      to: "extended regex preserving PERSIST-02 violation set"
      via: "test fixture covers Phase 2 additions"
      pattern: "TestAggregatesGuard"
---

<objective>
Land the v1alpha1 schema bumps Phase 2 needs across the `Project`, `Task`, and `Plan` CRDs, plus the shared condition/reason vocabulary expansion. Per BOOT-03 the schema additions stay inside the `v1alpha1` envelope — no breaking changes; only additive optional fields gated by CEL markers.

Purpose: Every downstream Phase 2 plan reads or writes these fields. The TaskReconciler dispatch body (Plan 09) checks `Project.Spec.MaxAttemptsPerTask`; the budget pre-charge (Plan 07) reads `Project.Spec.Budget`; the Plan admission webhook (Plan 11) resolves `Project.Spec.PlanAdmission.FileTouchMode` per D-E3 precedence; the stub-subagent integration tests (Plan 13) author Tasks with `Dev.TestMode`. Landing all schema additions in one plan keeps the codegen (`make manifests` + `make generate`) coherent and avoids Wave-2 plans racing each other on `zz_generated.deepcopy.go`.

Output: Modified `api/v1alpha1/*_types.go` files; regenerated `zz_generated.deepcopy.go` and `config/crd/bases/*.yaml`; an extended `aggregates_guard_test.go` that asserts the PERSIST-02 regex still flags genuine violations AND silently passes against the Phase 2 additions. NO helmify regen here — that's Plan 12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md
@api/v1alpha1/project_types.go
@api/v1alpha1/task_types.go
@api/v1alpha1/plan_types.go
@api/v1alpha1/shared_types.go
@api/v1alpha1/aggregates_guard_test.go
@Makefile

<interfaces>
<!-- Sourced verbatim from 02-RESEARCH.md §"K8s Resource Schemas". -->

`Project.Spec` ADD (preserve existing fields untouched):

- `ProviderSecretRef string` json:"providerSecretRef,omitempty" + `// +optional` marker
- `Providers []ProviderConfig` json:"providers,omitempty" + `// +optional`
- `Budget BudgetConfig` json:"budget,omitempty" + `// +optional`
- `PlanAdmission PlanAdmissionConfig` json:"planAdmission,omitempty" + `// +optional`
- `MaxAttemptsPerTask int32` json:"maxAttemptsPerTask,omitempty" + `// +kubebuilder:validation:Minimum=1` `// +kubebuilder:validation:Maximum=10` `// +optional`

Nested types in `project_types.go`:

- `ProviderConfig{ Name string (json:"name", Enum=anthropic), RequestsPerMinute *int32 (omitempty,optional), TokensPerMinute *int32 (omitempty,optional) }`
- `BudgetConfig{ AbsoluteCapCents int64 (json:"absoluteCapCents", Minimum=0), RollingWindowCapCents int64 (omitempty,optional) }`
- `PlanAdmissionConfig{ FileTouchMode string (json:"fileTouchMode", Enum=strict;warn, optional) }`

`Project.Status` ADD:

- `Budget BudgetStatus` json:"budget,omitempty" + `// +optional`
- `BudgetStatus{ TokensSpent int64 (omitempty), CostSpentCents int64 (omitempty), WindowStart *metav1.Time (omitempty) }`

`Task.Spec` ADD (preserve existing PlanRef, DependsOn, FilesTouched, PromptRef):

- `DeclaredOutputPaths []string` json:"declaredOutputPaths" + `// +kubebuilder:validation:MinItems=1`
- `Caps *Caps` json:"caps,omitempty" — type alias for `pkgdispatch.Caps` OR a local copy struct; planner picks (see action note)
- `Dev TaskDev` json:"dev,omitempty" + `// +optional` (NOT pointer — embed as zero-value struct, mirrors `shared_types.go:53-64` Gates pattern; the field-presence semantics come from the enum constraint)
- `TaskDev{ TestMode string (Enum=success;fail-exit-1;hang;exceed-output-paths, optional) }`

`Plan.Status` ADD:

- `ValidationState string` json:"validationState,omitempty" + `// +kubebuilder:validation:Enum=Pending;Validated;CycleDetected;FileTouchMismatch`
- `CycleEdges []string` json:"cycleEdges,omitempty"

`shared_types.go` ADD constants:

```
const (
    ConditionValidated      = "Validated"
    ConditionBudgetExceeded = "BudgetExceeded"
    ConditionRunning        = "Running"
    ConditionSucceeded      = "Succeeded"

    ReasonCycleDetected     = "CycleDetected"
    ReasonFileTouchMismatch = "FileTouchMismatch"
    ReasonCapHit            = "CapHit"
    ReasonRateLimitHit      = "RateLimitHit"
    ReasonBypassApplied     = "BypassApplied"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project.Spec/Status, Task.Spec, Plan.Status, shared_types additions + codegen</name>
  <files>api/v1alpha1/project_types.go, api/v1alpha1/task_types.go, api/v1alpha1/plan_types.go, api/v1alpha1/shared_types.go, api/v1alpha1/zz_generated.deepcopy.go, config/crd/bases/tideproject.k8s_projects.yaml, config/crd/bases/tideproject.k8s_tasks.yaml, config/crd/bases/tideproject.k8s_plans.yaml</files>
  <read_first>
    - api/v1alpha1/project_types.go (existing Phase 1 spec — preserve everything; nested-struct convention from lines 24-32, 36-45; CEL marker pattern from line 67)
    - api/v1alpha1/task_types.go (existing TaskSpec lines 24-40 — TaskDev sub-struct goes here)
    - api/v1alpha1/plan_types.go (existing PlanStatus — extend with ValidationState + CycleEdges)
    - api/v1alpha1/shared_types.go (lines 26-33 condition vocabulary — extend, don't break)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"`api/v1alpha1/project_types.go` (extend — Phase 2 schema additions)", §"`api/v1alpha1/task_types.go` (extend — add Dev.TestMode)")
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (§"K8s Resource Schemas" — full struct definitions)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md ("Helm chart additions for Phase 2" note: Project.Spec gains providerSecretRef, providers[], budget, planAdmission, maxAttemptsPerTask — confirmed scope)
    - Makefile (targets `manifests`, `generate`, and their scoping rules — controller-gen invocation scoped to ./api/..., ./internal/controller/..., ./internal/webhook/... per Phase 1 D-D1 carry-forward)
  </read_first>
  <behavior>
    - After this task, `make manifests` and `make generate` run clean (zero diff after a second invocation — idempotent).
    - `kubectl apply -f <test-project-with-new-fields.yaml>` admits a Project carrying `spec.budget.absoluteCapCents`, `spec.maxAttemptsPerTask=3`, `spec.planAdmission.fileTouchMode=warn`, `spec.providers[0].name=anthropic`.
    - `kubectl apply` REJECTS a Project with `spec.maxAttemptsPerTask=0` (CEL Minimum=1) and `spec.maxAttemptsPerTask=11` (CEL Maximum=10).
    - `kubectl apply` REJECTS a Task with `spec.dev.testMode=bogus` (CEL Enum violation).
    - `kubectl apply` REJECTS a Task with empty `spec.declaredOutputPaths` array (CEL MinItems=1).
    - `kubectl apply` REJECTS a Plan with `spec` unchanged but `status.validationState=Bogus` (Enum on status field; admitted via subresource).
    - `shared_types.go` exports the 4 new condition constants AND 5 new reason constants per <interfaces>.
  </behavior>
  <action>
    Edit `api/v1alpha1/project_types.go`: ADD the five nested struct types (`ProviderConfig`, `BudgetConfig`, `PlanAdmissionConfig`, `BudgetStatus`) AND extend the existing `ProjectSpec` / `ProjectStatus` blocks with the field additions per <interfaces>. Preserve every existing field and the `// +kubebuilder:validation:XValidation` markers on ProjectSpec (line 67 area). Apache 2.0 header stays per PATTERNS.md "Apache header convention" (api/v1alpha1/*.go always carries the header).

    Edit `api/v1alpha1/task_types.go`: ADD `DeclaredOutputPaths []string` field (with MinItems=1 marker) and `Dev TaskDev` (zero-value struct embed with omitempty) to `TaskSpec`. Add the `TaskDev` struct definition mirroring `shared_types.go:53-64`'s `Gates` struct shape. For `Caps`, declare a local struct (NOT a type alias to pkg/dispatch — controller-gen can't always traverse cross-module type aliases for CRD generation; the local `Caps` struct mirrors the pkg/dispatch field shape one-for-one and a runtime translation lives in Plan 09's TaskReconciler buildEnvelopeIn helper). Local struct: `Caps{ WallClockSeconds int32 (Minimum=1), Iterations int32 (Minimum=1), InputTokens int64 (Minimum=0), OutputTokens int64 (Minimum=0) }`.

    Edit `api/v1alpha1/plan_types.go`: ADD `ValidationState string` (Enum=Pending;Validated;CycleDetected;FileTouchMismatch) and `CycleEdges []string` to `PlanStatus`. Preserve existing `Phase` / `Conditions` fields.

    Edit `api/v1alpha1/shared_types.go`: ADD the 4 condition + 5 reason constants from <interfaces>. Group them into a SECOND `const ( ... )` block after the existing Phase 1 block (lines 26-33) so the Phase 1 vocabulary stays a visually-distinct unit.

    Run `make manifests generate` to regenerate `zz_generated.deepcopy.go` and the three CRD YAMLs under `config/crd/bases/`. Verify the regen is idempotent (second invocation produces zero diff). The controller-gen path scoping established in Phase 1 D-D1 (`./api/...`, `./internal/controller/...`, `./internal/webhook/...`) MUST be preserved — DO NOT broaden the scope to include `./tools/...` (which would pull in Plan 02's testdata fixtures by mistake).

    The pkg/dispatch Caps struct (from Plan 01) and api/v1alpha1.Caps (from this plan) are two separate types by design — they serve different layers (Go-only public API vs CEL-validated CRD field). Plan 09's TaskReconciler.buildEnvelopeIn translates one to the other. Document this in a doc-comment on `api/v1alpha1.Caps`.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && make manifests &amp;&amp; make generate &amp;&amp; go build ./... &amp;&amp; go vet ./api/...</automated>
  </verify>
  <done>
    - `make manifests` + `make generate` complete with exit 0; running them a second time produces zero git diff under `api/v1alpha1/zz_generated.deepcopy.go` and `config/crd/bases/*.yaml`
    - `go build ./...` succeeds (no downstream consumer of new fields yet in this plan, but the shape compiles)
    - `grep -nE 'ProviderSecretRef|Budget BudgetConfig|PlanAdmission PlanAdmissionConfig|MaxAttemptsPerTask|Dev TaskDev|ValidationState|ConditionValidated|ConditionBudgetExceeded' api/v1alpha1/*.go` returns ≥ 9 matches (all key additions present)
    - `grep -nE 'Schedule|Waves \[\]|IndegreeMap|CachedDag|DerivedDag' api/v1alpha1/*_types.go` returns ZERO matches (PERSIST-02 / Pitfall 4 untouched)
    - The 3 CRD YAMLs under `config/crd/bases/` contain `enum: [strict, warn]` and `enum: [success, fail-exit-1, hang, exceed-output-paths]` and `enum: [Pending, Validated, CycleDetected, FileTouchMismatch]` after regen
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend aggregates_guard_test.go to cover Phase 2 additions; verify PERSIST-02 gate stays clean</name>
  <files>api/v1alpha1/aggregates_guard_test.go</files>
  <read_first>
    - api/v1alpha1/aggregates_guard_test.go (existing Phase 1 test — lines 1-100 — regex denylist + clean-fixture sanity pattern)
    - api/v1alpha1/project_types.go (just edited — for the new `BudgetStatus` field, a non-aggregate per PERSIST-02 / Pitfall 4 because it's a TALLY object, not a SCHEDULE)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"Aggregates guard discipline" — explicitly notes Project.Status.Budget is a tally, not an aggregate)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (Pitfall reference: PERSIST-02 is preserved because Phase 2 materializes Wave as a separate Kind, not as an aggregate field)
    - Makefile (lines 312-320 — `verify-no-aggregates` target body; the regex denylist this test mirrors)
  </read_first>
  <behavior>
    - `TestAggregatesGuard_PreservesPhase1Denylist` asserts the regex `Schedule|Waves *\[\]|IndegreeMap|CachedDag|DerivedDag` returns ZERO matches against `api/v1alpha1/*_types.go` post-Phase-2-additions.
    - `TestAggregatesGuard_BudgetStatusIsNotAggregate` asserts `Project.Status.Budget` (newly added) is detected by name and explicitly documented as a non-aggregate (tally type). The test reads project_types.go and verifies `BudgetStatus` appears AND it does not contain any of the forbidden tokens. This is a positive-shape assertion: the new field is present + it is NOT an aggregate.
    - `TestAggregatesGuard_DetectsViolationInFixture` (existing pattern from Phase 1) continues to fire — adding a synthetic test-fixture line `Schedule string` to a temp file triggers the regex; this regression test is unchanged.
  </behavior>
  <action>
    Edit `api/v1alpha1/aggregates_guard_test.go`: extend the existing Phase 1 test surface by ADDING two new test functions per <behavior> while preserving the original Phase 1 cases. Keep the same `regexp.MustCompile(...)` pattern source (so the test and `Makefile:verify-no-aggregates` stay synchronized). Use `filepath.Walk` on `api/v1alpha1` matching `*_types.go` to scan post-Phase-2-addition source. The Phase 1 `TestAggregatesGuard_DetectsViolationInFixture` (or equivalent name) MUST continue to fire on a synthetic violation — verify with `t.Skip` or temp-file injection if Phase 1 uses that pattern.

    DO NOT change the Makefile `verify-no-aggregates` target body. The Go test mirrors the Makefile regex; both must stay in lock-step.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go test ./api/v1alpha1/... -count=1 -run TestAggregatesGuard &amp;&amp; make verify-no-aggregates</automated>
  </verify>
  <done>
    - `go test ./api/v1alpha1/... -run TestAggregatesGuard` reports PASS on all subtests including new Phase 2 additions
    - `make verify-no-aggregates` exits 0 against the post-Phase-2-addition tree (regex denylist still clean)
    - `grep -c "func TestAggregatesGuard" api/v1alpha1/aggregates_guard_test.go` returns ≥ 2 (Phase 1 functions preserved + at least one new Phase 2 case)
    - Synthetic injection: temporarily add `Schedule string` to a new test-only fixture file; `make verify-no-aggregates` exits non-zero; revert; gate clears (smoke check, not committed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user kubectl apply → kube-apiserver CRD admission | A Project / Task / Plan YAML crosses the apiserver admission boundary; CEL markers (validating-admission webhooks scaffolded in Phase 1) enforce shape constraints. |
| controller-gen scope expansion | A future planner might broaden controller-gen path scoping and accidentally pull testdata fixtures into CRD generation (Phase 1 D-Phase1-Plan02 explicitly prevented this). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Project.Spec.MaxAttemptsPerTask set to a runaway value | mitigate | CEL `Maximum=10` marker caps the field at admission; a Project requesting 1000 retries is rejected before reaching the reconciler. |
| T-02-03-02 | Denial of Service | Empty `declaredOutputPaths` on Task → harness has nothing to enforce → out-of-scope writes go undetected | mitigate | `+kubebuilder:validation:MinItems=1` on Task.Spec.DeclaredOutputPaths rejects empty arrays at admission. Plan 06's harness output validator (HARN-05) then enforces against the declared set. |
| T-02-03-03 | Information Disclosure | Operator setting `Dev.TestMode` in production accidentally | accept | The `Dev` field is documented as dev-only (mirrors pkg/dispatch.Dev contract); a future planner could add a CEL rule rejecting Dev set on non-test namespaces, but v1 ships without — Pitfall 9 (RESEARCH.md) just warns the planner to keep Dev rigidly scoped. |
| T-02-03-04 | Elevation of Privilege | Budget tally aggregate sneaks in as `Project.Status.Schedule` despite PERSIST-02 | mitigate | `make verify-no-aggregates` Makefile gate + the extended `aggregates_guard_test.go` both fire on the forbidden token set. Phase 2 deliberately uses `BudgetStatus` (tally — allowed) and rejects any field named `Schedule` / `Waves []` / `IndegreeMap` / `CachedDag` / `DerivedDag`. |
| T-02-03-05 | Tampering | Plan.Status.ValidationState set to a bogus value via status subresource | mitigate | CEL `Enum=Pending;Validated;CycleDetected;FileTouchMismatch` on the field rejects any other string at admission. |
</threat_model>

<verification>
- `make manifests` + `make generate` idempotent (zero diff on second run)
- `make verify-no-aggregates` exits 0 against the Phase 2 schema
- `go test ./api/v1alpha1/... -count=1` PASS
- `go build ./...` succeeds (downstream consumers in Plan 04+ build against the new fields)
- 3 CRD YAMLs under `config/crd/bases/` contain the new enum sets after regen
- No Apache header drift on `*_types.go` files (Phase 1 convention preserved)
- `internal/dispatch/doc.go` is NOT touched in this plan — that's Plan 08
</verification>

<success_criteria>
Phase 2 Success Criteria contribution:
- AC #1: Plan validates only after `Plan.Status.ValidationState=Validated` is set by the webhook (Plan 11 sets it; this plan defines the field).
- AC #4: Per-Project absolute cost cap requires `Project.Status.Budget` to track spend — this plan defines the BudgetStatus struct shape; Plan 09 writes to it; Plan 10's halt logic reads from it.
- AC #5: Per-Task caps require `Task.Spec.Caps` field — this plan defines it; Plan 09 stamps it into EnvelopeIn; Plan 06's harness enforces.

REQ-PERSIST-03 contribution: by defining `Plan.Status.ValidationState` (not `Plan.Status.Schedule`) and `Project.Status.Budget` (tally, not schedule), this plan keeps the PERSIST-02 / Pitfall 4 guard clean. Wave schedules remain derived per-reconcile from `pkg/dag.ComputeWaves` (Plan 09 / Plan 11), never cached on a parent CRD.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-03-SUMMARY.md` describing:
- Exact field set shipped on Project.Spec / Project.Status / Task.Spec / Plan.Status — including final JSON tag choices and CEL marker values
- The 9 new constants added to shared_types.go (4 conditions + 5 reasons)
- Confirmation that `make verify-no-aggregates` and `aggregates_guard_test.go` both pass after additions
- The api/v1alpha1.Caps vs pkg/dispatch.Caps two-type design (why they're separate and how Plan 09's TaskReconciler.buildEnvelopeIn translates)
</output>