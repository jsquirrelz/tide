---
phase: 2
plan: 10
type: execute
wave: 5
depends_on: [3, 7, 9]
files_modified:
  - internal/controller/project_controller.go
  - internal/controller/project_controller_test.go
autonomous: true
requirements: [ART-01, FAIL-04]
must_haves:
  truths:
    - "ProjectReconciler creates a one-shot init Job named `tide-init-{project.UID}` on first reconcile after PVC binds; the Job runs busybox `mkdir -p /workspace/{repo,artifacts,envelopes} && chmod 0775 ...` (D-G1)."
    - "On init Job's Pod terminating with exit 0, ProjectReconciler patches `Project.Status.Phase=Initialized`."
    - "Init Job creation is idempotent — re-applying the Project does nothing (deterministic name `tide-init-{project.UID}` is the dedup key)."
    - "Budget cap halt (FAIL-04): `budget.IsCapExceeded(&project)` AND NOT `budget.IsBypassed(&project, now)` → `Project.Status.Phase=BudgetExceeded` + Condition `BudgetExceeded=True, Reason=AbsoluteCapReached`."
    - "Bypass annotation watch (D-D4): on `tideproject.k8s/bypass-budget=true` (consumed) or `tideproject.k8s/bypass-budget-until=<RFC3339>` (TTL), clear `Project.Status.Phase=BudgetExceeded` + record K8s Event with Reason=BypassApplied."
    - "PVC bind check: ProjectReconciler waits for a PVC named `tide-project-{project.UID}` to exist + be Bound before kicking off the init Job."
  artifacts:
    - path: internal/controller/project_controller.go
      provides: "Phase 2 init Job + budget cap halt + bypass annotation watch inside the seam"
      contains: "tide-init"
  key_links:
    - from: "internal/controller/project_controller.go"
      to: "internal/budget.IsCapExceeded + IsBypassed + ConsumeBypass"
      via: "called in the budget gate inside the seam"
      pattern: "budget\\.(IsCapExceeded|IsBypassed|ConsumeBypass)"
    - from: "internal/controller/project_controller.go"
      to: "batch/v1.Job with deterministic name tide-init-{project.UID}"
      via: "Create with AlreadyExists treated as success (idempotent)"
      pattern: "tide-init"
---

<objective>
Fill the Phase 2 fill of `ProjectReconciler`: (1) one-shot init Job creation to bootstrap the `/workspace/{repo,artifacts,envelopes}` PVC layout (ART-01); (2) budget cap halt + bypass annotation watch (FAIL-04 / D-D4). Plan 09's TaskReconciler reads `Project.Status.Phase=BudgetExceeded` to halt dispatches; this plan is what sets/clears that phase.

Per D-G1: init Job is `tide-init-{project.UID}`, idempotent via deterministic name. After exit-0 Pod termination, `Project.Status.Phase=Initialized`. Per-Phase/Plan/Task artifact subdirs (`/workspace/artifacts/M-N/P-N/L-N/`) are NOT pre-computed here (D-G2 — lazy at dispatch time by the harness; Plan 06 wires).

Per D-D4: budget bypass via `tideproject.k8s/bypass-budget=true` (consumed) or `bypass-budget-until=<RFC3339>` (TTL). Operator annotates with kubectl; ProjectReconciler watches annotation changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md
@.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md
@internal/controller/project_controller.go
@internal/budget/cap.go
@internal/owner/owner.go
@api/v1alpha1/project_types.go
@api/v1alpha1/shared_types.go
@Makefile

<interfaces>
<!-- Per D-G1, D-D2, D-D4, D-G3. Init Job spec sourced from 02-RESEARCH.md "PVC bootstrap (ART-01)". -->

ProjectReconciler.Reconcile body (inside `if r.Dispatcher != nil` seam):

1. **Budget cap check + bypass:**
   - now := time.Now()
   - bypassed := `budget.IsBypassed(&project, now)`
   - capExceeded := `budget.IsCapExceeded(&project)`
   - If `project.Status.Phase == "BudgetExceeded"` AND bypassed → set `Project.Status.Phase` back to whatever it was before (Pending/Initialized/Running) + record K8s Event `Reason=BypassApplied`. If the one-shot `bypass-budget=true` form: patch annotations via `budget.ConsumeBypass(&project)`.
   - Else if `project.Status.Phase != "BudgetExceeded"` AND capExceeded AND NOT bypassed → patch `Project.Status.Phase=BudgetExceeded` + Condition `BudgetExceeded=True, Reason=AbsoluteCapReached` + record K8s Event `Reason=AbsoluteCapReached`.

2. **PVC bind check:**
   - PVC name: `tide-project-{project.UID}` (the Helm chart in Plan 12 provides a PVC template; for Phase 2 v0, the operator can `kubectl apply` the PVC by hand if Helm template not present yet — DO NOT block on PVC absence; just return ctrl.Result{RequeueAfter: 30s} for retry).
   - If PVC exists AND `Status.Phase=Bound` → proceed to init Job.
   - If PVC doesn't exist → return ctrl.Result{RequeueAfter: 30s} (Pitfall 1: this is RequeueAfter, NOT a blocking sleep).

3. **Init Job creation (idempotent):**
   - Init Job name: `tide-init-{project.UID}`.
   - Init Job body: single container `busybox` image (Plan 12 Helm value `images.busybox.repository:tag`); command `["sh","-c","mkdir -p /workspace/repo /workspace/artifacts /workspace/envelopes && chmod 0775 /workspace/repo /workspace/artifacts /workspace/envelopes"]`.
   - PodSpec: `serviceAccountName: tide-subagent` (zero verbs; same SA pattern as Task pods); `securityContext.fsGroup: 1000` (D-G3); container `runAsUser: 1000`, `readOnlyRootFilesystem: false` (it writes /workspace), capabilities.drop=[ALL], allowPrivilegeEscalation=false.
   - VolumeMount: `project-workspace` → PVC `tide-project-{project.UID}` at `/workspace`.
   - `backoffLimit: 2` (init can retry a couple times if PVC mount is slow).
   - `ttlSecondsAfterFinished: 300`.
   - OwnerReferences cascade to Project via `internal/owner.EnsureOwnerRef`.
   - Create with AlreadyExists treated as success.

4. **Watch init Job completion:**
   - On init Job's Pod terminating with exit 0: patch `Project.Status.Phase=Initialized` + Condition `Initialized=True`.
   - On exit non-zero: patch `Project.Status.Phase=InitFailed` + Condition `Failed=True, Reason=InitJobFailed`.

NEW WATCHES on `(r *ProjectReconciler).SetupWithManager`:
- Already has `Owns(&batchv1.Job{})` from Phase 1 → init Job termination triggers reconcile.
- Add annotation-change watch: `predicate.AnnotationChangedPredicate{}` so the reconciler wakes up when bypass annotations are set/removed. Use `builder.WithEventFilter(predicate.Or(predicate.GenerationChangedPredicate{}, predicate.AnnotationChangedPredicate{}))`.

NEW RBAC markers:
- `// +kubebuilder:rbac:groups=batch,resources=jobs,verbs=get;list;watch;create;delete` (already present from Phase 1 — confirm; if not, add)
- `// +kubebuilder:rbac:groups="",resources=persistentvolumeclaims,verbs=get;list;watch` (PVC bind check)
- `// +kubebuilder:rbac:groups="",resources=events,verbs=create;patch` (bypass + cap events; likely already present from Phase 1)

Helm chart additions for the busybox init Job image (Plan 12 wires):
- `images.busybox.repository: busybox`
- `images.busybox.tag: 1.36` (pinned for reproducibility)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProjectReconciler init Job + budget gate + bypass watch + envtest tests</name>
  <files>internal/controller/project_controller.go, internal/controller/project_controller_test.go</files>
  <read_first>
    - internal/controller/project_controller.go (Phase 1 stub — preserve six-step pattern; fill the seam at line ~111)
    - internal/budget/cap.go (Plan 07 — IsCapExceeded, IsBypassed, ConsumeBypass)
    - internal/owner/owner.go (EnsureOwnerRef helper)
    - api/v1alpha1/project_types.go (Plan 03 — Budget config + Budget status)
    - api/v1alpha1/shared_types.go (Plan 03 — ConditionBudgetExceeded, ReasonBypassApplied constants)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-CONTEXT.md (D-G1, D-D4, D-G3)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-RESEARCH.md (§"PVC bootstrap (ART-01)" + §"Pitfall 7 — D-D4 bypass annotation race" — TTL form recommended)
    - .planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-PATTERNS.md (§"`internal/controller/project_controller.go` (extend — init Job + budget)")
    - Makefile (verify-no-blocking + verify-rbac-marker-discipline gates)
  </read_first>
  <behavior>
    - `TestProjectReconciler_CreatesInitJobOnFirstReconcile` — apply Project + PVC (Bound); reconcile; assert Job `tide-init-{project.UID}` exists with the busybox mkdir command.
    - `TestProjectReconciler_InitJobIdempotent` — reconcile twice; assert exactly one init Job exists post-second-reconcile.
    - `TestProjectReconciler_OnInitJobSuccess_SetsPhaseInitialized` — pre-create a Succeeded init Job for the Project; reconcile; assert `Project.Status.Phase=Initialized`.
    - `TestProjectReconciler_OnInitJobFailure_SetsPhaseInitFailed` — pre-create a Failed init Job; reconcile; assert Phase=InitFailed.
    - `TestProjectReconciler_BudgetCapExceeded_SetsBudgetExceeded` — Status.Budget.CostSpentCents > Spec.Budget.AbsoluteCapCents; reconcile; assert Phase=BudgetExceeded.
    - `TestProjectReconciler_BypassAnnotation_ClearsBudgetExceeded` — Phase=BudgetExceeded + annotation `tideproject.k8s/bypass-budget=true`; reconcile; assert Phase != BudgetExceeded AND annotation consumed.
    - `TestProjectReconciler_BypassUntilAnnotation_TTLHonored` — Phase=BudgetExceeded + annotation `bypass-budget-until=<future-RFC3339>`; reconcile; assert Phase cleared. Then set `bypass-budget-until=<past-RFC3339>`; reconcile; assert Phase=BudgetExceeded again.
    - `TestProjectReconciler_NoPVC_RequeuesAfter30s` — apply Project without PVC; reconcile; assert returned `ctrl.Result.RequeueAfter == 30*time.Second` AND no init Job.
    - `make verify-no-blocking` exits 0; `make verify-rbac-marker-discipline` exits 0.
  </behavior>
  <action>
    Edit `internal/controller/project_controller.go`:

    1. Replace the comment placeholder inside the `if r.Dispatcher != nil` seam with the body per <interfaces>. Implement helpers:
       - `(r *ProjectReconciler) ensureInitJob(ctx, project, pvcName) error` — builds + Creates init Job (AlreadyExists treated as success).
       - `(r *ProjectReconciler) handleInitJobCompletion(ctx, project, job) error` — patches Phase based on Job exit code.
       - `(r *ProjectReconciler) handleBudgetGate(ctx, project, now time.Time) (ctrl.Result, error)` — runs the IsCapExceeded + IsBypassed + ConsumeBypass logic + emits K8s Events.
       - `(r *ProjectReconciler) buildInitJob(project, pvcName string) *batchv1.Job` — pure construction (no client call).

    2. Add a custom `predicate.Predicate` for annotation changes via `predicate.AnnotationChangedPredicate{}`. Wire via `Watches(&v1alpha1.Project{}, ..., builder.WithPredicates(...))` extension to SetupWithManager. The simplest path: `For(&v1alpha1.Project{}, builder.WithPredicates(predicate.Or(predicate.GenerationChangedPredicate{}, predicate.AnnotationChangedPredicate{})))` so reconciler wakes on both spec changes AND annotation changes.

    3. Add the 2 new RBAC markers per <interfaces> (PVCs read; events likely already present).

    4. NO `time.Sleep` — backoff via `ctrl.Result{RequeueAfter: 30 * time.Second}`.

    Extend `internal/controller/project_controller_test.go` (Phase 1 placeholder; extend with the test cases). Use Ginkgo `Describe("ProjectReconciler init + budget", Label("envtest", "phase2"), ...)`. Apache header per PATTERNS.md.

    For the init Job test: the test creates a fake PVC + a Project; the reconciler creates the Job; tests Eventually-assert against the fake client's Job list. For TTL bypass tests: use `time.Now().Add(time.Hour).Format(time.RFC3339)` for future and `time.Now().Add(-time.Hour).Format(time.RFC3339)` for past.
  </action>
  <verify>
    <automated>cd /Users/justinsearles/Projects/tide && go build ./internal/controller/... &amp;&amp; make test &amp;&amp; make verify-no-blocking &amp;&amp; make verify-rbac-marker-discipline</automated>
  </verify>
  <done>
    - `go build ./internal/controller/...` exits 0
    - `make test` PASS on all 8 ProjectReconciler test cases
    - `make verify-no-blocking` exits 0 (RequeueAfter, not Sleep)
    - `make verify-rbac-marker-discipline` exits 0
    - `grep -c 'tide-init' internal/controller/project_controller.go` returns ≥ 1
    - `grep -c 'budget.IsCapExceeded\|budget.IsBypassed' internal/controller/project_controller.go` returns ≥ 2
    - `grep -c 'AnnotationChangedPredicate' internal/controller/project_controller.go` returns ≥ 1
    - Apache header preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → bypass annotation | An operator with `kubectl annotate projects` permission can clear BudgetExceeded. RBAC controls who has this permission; v1 ships the annotation as the only halt-clearance mechanism (Phase 4 CLI wraps). |
| ProjectReconciler → init Job creation | Reconciler creates a Job that mounts the per-Project PVC. The init Job's SA is `tide-subagent` (zero K8s verbs — D-A4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Tampering | Operator setting bypass-budget to circumvent cap | accept | The annotation IS the documented operator override (D-D4); RBAC + K8s Events provide audit trail. |
| T-02-10-02 | Spoofing | Duplicate init Job created if reconcile races | mitigate | Deterministic name `tide-init-{project.UID}` + AlreadyExists handling. |
| T-02-10-03 | Denial of Service | PVC never binds → reconciler retries forever | accept | RequeueAfter 30s is unbounded; operator must fix the PVC. The reconciler doesn't crash. |
| T-02-10-04 | Information Disclosure | Init Job mounts the PVC → could read pre-existing files | mitigate | Pre-existing files are workspace state for THIS Project (no cross-Project access; PVC is per-Project per D-G1). Plan 04's subagent SA (tide-subagent) has zero K8s verbs so it can't enumerate other PVCs. |
| T-02-10-05 | Repudiation | Budget exceedance not logged | mitigate | K8s Event emitted with Reason=AbsoluteCapReached (Plan 03 added the ReasonBypassApplied + AbsoluteCapReached constants; reconciler records the Event). |
</threat_model>

<verification>
- `go build ./...` exits 0
- `make test` PASS on all ProjectReconciler tests
- `make verify-no-blocking` exits 0
- `make verify-rbac-marker-discipline` exits 0
- The bypass annotation TTL form test verifies the race-free path RESEARCH.md Open Question #7 recommended
- No regression in Phase 1 ProjectReconciler tests (preserve them all)
</verification>

<success_criteria>
Phase 2 Success Criterion #4: "per-Project absolute cost cap that pauses dispatch and requires `tide approve --bypass-budget` to resume" — Plan 10 implements the underlying annotation-driven contract. Phase 4's `tide approve --bypass-budget` becomes a thin wrapper over `kubectl annotate project foo tideproject.k8s/bypass-budget=true`.

Phase 2 Success Criterion #1: "wait for envelopes on the per-Project RWX PVC at /workspace/{repo,artifacts/M-N/P-N/L-N,envelopes}" — the init Job creates the top-level layout; per-Phase/Plan/Task subdirs are created lazily by the harness at dispatch time (D-G2).

REQ-ART-01 satisfied: PVC layout `/workspace/{repo,artifacts,envelopes}` materialized by the init Job; per-Phase/Plan/Task subdirs lazy.
REQ-FAIL-04 satisfied: per-Project budget caps halt dispatch + `tide approve --bypass-budget` annotation contract (the CLI verb arrives in Phase 4).
</success_criteria>

<output>
After completion, create `.planning/phases/02-dispatch-plan-validation-innermost-reconcilers-harness/02-10-SUMMARY.md` describing:
- The init Job spec (image, command, securityContext)
- The bypass annotation precedence (consumed vs TTL — both forms shipped)
- The K8s Events emitted (Reasons: AbsoluteCapReached, BypassApplied, InitJobFailed)
- The 30s RequeueAfter for the no-PVC case (justification: Pitfall 1 — non-blocking; PVC binding is operator-controlled, not orchestrator-controlled)
- How Plan 12's Helm chart hooks in (PVC template, busybox image values)
</output>