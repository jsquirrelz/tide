{
  "kind": "Plan",
  "name": "plan-01-level-config-provider-secret",
  "spec": {
    "phaseRef": "phase-04-credential-and-chart-wiring",
    "objective": "Extend LevelConfig with a ProviderSecretRef field so operators can pin a per-level credential Secret — enabling the task executor level to reference openai-secrets while planner levels continue reading from the Anthropic Secret. Add a ResolveProviderSecretRef helper to dispatch_helpers.go that walks the LevelConfig.ProviderSecretRef precedence chain without touching the vendor-resolution fix already delivered by phase-01-provider-switch.",
    "wave": 1,
    "dependsOn": [],
    "filesTouched": [
      "api/v1alpha1/project_types.go",
      "api/v1alpha1/zz_generated.deepcopy.go",
      "config/crd/bases/tideproject.k8s_projects.yaml",
      "internal/controller/dispatch_helpers.go",
      "internal/controller/dispatch_helpers_test.go"
    ]
  }
}
