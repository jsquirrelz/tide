# Codex-runtime dogfood — salvaged planning outline

Salvaged from run df5a0c1f (≈$90 of planning). Milestones=3, phases=15, plans=42.


## milestone-01-codex-subagent  (dependsOn=[])

### phase-01-provider-switch  (dependsOn=[])
- **plan-01-vendor-api-types** (dependsOn=[]): Extend the v1alpha1 API types with a per-level Vendor field. Add Vendor string (with +kubebuilder:validation:Enum=anthropic;openai CEL marker) to both LevelConfig and SubagentConfig in api/v1alpha1/project_types.go, then run make manifests generate to regenerate zz_generated.deepcopy.go and the CRD YAML under config/crd/bases/. No controller, chart, or cmd/ changes -- this plan establishes the schema foundation consumed by plan-02.
- **plan-02-resolve-provider-vendor** (dependsOn=['plan-01-vendor-api-types']): Rewire ResolveProvider in internal/controller/dispatch_helpers.go to walk the full vendor precedence chain: Levels.<level>.Vendor -> Spec.Subagent.Vendor -> helmDefaults.Vendor -> fallback 'anthropic'. Extend ProviderDefaults with a Vendor string field so the Helm default can propagate. Add dispatch_helpers_test.go cases proving: (a) a Codex-pinned task level resolves Vendor:'openai'; (b) a Claude-pinned phase level resolves Vendor:'anthropic'; (c) a Project with no vendor override resolves Vendor:'anthropic' at every level -- all offline, no network calls. No chart or cmd/ changes in this plan.
- **plan-03-manager-helm-vendor** (dependsOn=['plan-02-resolve-provider-vendor']): Thread the per-level vendor default through cmd/manager/env.go and the Helm chart. Add resolveDefaultVendor() helper (reads TIDE_DEFAULT_VENDOR env var, fallback 'anthropic') and extend tideHelmProviderDefaults to populate ProviderDefaults.Vendor. Extend cmd/manager/env_test.go with a round-trip test: TIDE_DEFAULT_VENDOR=openai -> ProviderDefaults.Vendor=='openai'. Add subagent.defaults.vendor (and optional per-level vendor keys under subagent.levels.*) to charts/tide/values.yaml; wire TIDE_DEFAULT_VENDOR into charts/tide/templates/deployment.yaml. Verify helm template renders the env under both default and --set override paths. make verify-import-firewall and make verify-dispatch-imports must remain green -- zero openai or codex imports in controller or cmd/manager.

### phase-02-codex-subagent  (dependsOn=[])
- **plan-01-codex-foundation** (dependsOn=[]): Author internal/subagent/codex/doc.go (package-level doc referencing D-C1 layering pattern and the 'openai' vendor sentinel) and internal/subagent/codex/client.go (Client struct, Options, New constructor, NewWithExec test-seam — mirroring the anthropic package's constructor shape exactly). These are the zero-dependency foundation pieces that run.go and the Dockerfile both import.

### phase-03-image-and-credentials  (dependsOn=['phase-02-codex-subagent'])
- **plan-03-chart-image-defaults** (dependsOn=[]): Wire the Codex subagent image default into the Helm chart and manager startup. Adds images.codexSubagent repository/tag/pullPolicy block to charts/tide/values.yaml (mirrors images.claudeSubagent). Adds CODEX_SUBAGENT_IMAGE env var to charts/tide/templates/deployment.yaml (mirrors CLAUDE_SUBAGENT_IMAGE pattern -- required= guard, bare-ref appVersion suffix). Adds codexSubagentImage helper to cmd/manager/env.go + env_test.go. Updates cmd/manager/main.go to read CODEX_SUBAGENT_IMAGE and stamp it onto ProviderDefaults.CodexImage (new field). Verification: helm template renders both CLAUDE_SUBAGENT_IMAGE and CODEX_SUBAGENT_IMAGE; make test green.
- **plan-04-jobspec-vendor-credentials** (dependsOn=[]): Add vendor-aware credential dispatch to internal/dispatch/podjob/jobspec.go. Adds a Vendor string field to BuildOptions (empty string defaults to anthropic behavior -- no existing callers broken). When Vendor == openai: credproxy sidecar is omitted (Codex calls the OpenAI API directly; no HMAC proxy needed), the ANTHROPIC_API_KEY signed-token env line is suppressed, and OPENAI_API_KEY is sourced from the injected providerSecretRef envFrom (already available since the operator puts OPENAI_API_KEY in that Secret). When Vendor == anthropic or empty: existing behavior is unchanged. Verification: jobspec_test.go covers anthropic (unchanged) and openai (no credproxy, no ANTHROPIC_API_KEY line, OPENAI_API_KEY present) cases; make test green; verify-import-firewall green (no codex import in jobspec).

### phase-04-mixed-provider-integration  (dependsOn=['phase-01-provider-switch', 'phase-03-image-and-credentials'])

## milestone-02-provider-dispatch  (dependsOn=['milestone-01-codex-subagent'])

### phase-01-codex-subagent-core  (dependsOn=[])
- **plan-01-codex-pricing** (dependsOn=[]): 
- **plan-03-codex-package-scaffold** (dependsOn=[]): 
- **plan-04-codex-run** (dependsOn=['plan-01-codex-pricing', 'plan-02-codex-stream-parser', 'plan-03-codex-package-scaffold']): 

### phase-02-codex-image-binary  (dependsOn=['phase-01-codex-subagent-core'])
- **plan-01-codex-subagent-entrypoint** (dependsOn=[]): 
- **plan-02-codex-subagent-dockerfile** (dependsOn=['plan-01-codex-subagent-entrypoint']): 

### phase-03-per-level-provider-switch  (dependsOn=[])
- **plan-02-vendor-dispatch-wire** (dependsOn=['plan-01-vendor-api-types']): Rewrite ResolveProvider in internal/controller/dispatch_helpers.go to walk the full vendor precedence chain (Levels.<level>.Vendor -> Spec.Subagent.Vendor -> ProviderDefaults.Vendor -> 'anthropic') instead of the current unconditional Vendor:'anthropic'. Add a Vendor string field to the ProviderDefaults struct. Thread a TIDE_DEFAULT_VENDOR environment variable (default 'anthropic') through cmd/manager/env.go (tideHelmProviderDefaults) and cmd/manager/main.go. Update existing dispatch_helpers_test.go and task_controller_extracted_test.go assertions that hardcode Vendor:'anthropic' with the now-conditional default; add new table-driven tests proving a Codex-pinned level resolves Vendor:'openai' while an unpinned level stays 'anthropic' in the same Project. No Codex imports anywhere — this plan is provider-agnostic; verify-import-firewall and verify-dispatch-imports stay green.

### phase-04-credential-and-chart-wiring  (dependsOn=['phase-03-per-level-provider-switch'])
- **plan-01-level-config-provider-secret** (dependsOn=[]): Extend LevelConfig with a ProviderSecretRef field so operators can pin a per-level credential Secret — enabling the task executor level to reference openai-secrets while planner levels continue reading from the Anthropic Secret. Add a ResolveProviderSecretRef helper to dispatch_helpers.go that walks the LevelConfig.ProviderSecretRef precedence chain without touching the vendor-resolution fix already delivered by phase-01-provider-switch.
- **plan-02-chart-codex-values** (dependsOn=[]): Add chart values for the Codex subagent image reference and the OpenAI credential Secret name so operators have first-class Helm surface for both. charts/tide/values.yaml gains images.codexSubagent (repository + tag, mirroring images.tideReporter shape) and a codexProviderSecretName key that documents the expected Secret name carrying OPENAI_API_KEY. No chart template changes are required — per-level image and ProviderSecretRef are Project-spec fields, not chart-managed ConfigMap keys — but the values additions provide clear guidance and enable future chart-managed defaults.
- **plan-03-jobspec-codex-creds** (dependsOn=['plan-01-level-config-provider-secret']): Extend BuildJobSpec and its callers to perform vendor-aware credential injection. When the resolved vendor is 'openai', the credproxy sidecar is skipped and OPENAI_API_KEY is injected directly from the per-level ProviderSecretRef Secret into the subagent container env — the Codex CLI has no proxy-URL concept, so direct env injection is the documented API-key auth path. When vendor is 'anthropic', the existing credproxy path is unchanged. BuildOptions gains Vendor string and LevelProviderSecretRef string fields; dispatch_helpers.go provides the ResolveProviderSecretRef helper (from plan-01); all four planner-level controllers and the task controller are updated to pass both. Offline unit tests in jobspec_test.go cover the Codex credential branch.
- **plan-04-demo-manifest** (dependsOn=['plan-01-level-config-provider-secret']): Finalize the dogfood Project manifest (examples/projects/dogfood/02-codex-runtime-project.yaml) with complete per-level credential and image wiring: planner levels pin the Claude image and tide-secrets; the task level pins ghcr.io/jsquirrelz/tide-codex-subagent plus the new LevelConfig.ProviderSecretRef field pointing to openai-secrets. Confirm the manifest is schema-valid against the v1alpha1 CRD. Update the dogfood README.md with the two-Secret creation steps (tide-secrets carrying ANTHROPIC_API_KEY, openai-secrets carrying OPENAI_API_KEY) and the operator sequencing note.
- **plan-05-integration-test** (dependsOn=['plan-03-jobspec-codex-creds', 'plan-04-demo-manifest']): Author an offline integration test that exercises the full heterogeneous credential path: a Project whose task level resolves vendor='openai' and LevelProviderSecretRef='openai-secrets' must produce a Job spec with OPENAI_API_KEY injected from that secret and without a credproxy sidecar; a Project whose task level resolves vendor='anthropic' must produce the existing credproxy-plus-signed-token shape unchanged. The test uses the existing jobspec test harness (no live API call, no OPENAI_API_KEY env guard required). A second table-driven test confirms make verify-dispatch-imports stays clean — no openai import leaks from controller or cmd/manager packages.

### phase-05-heterogeneous-dispatch-demo  (dependsOn=['phase-01-codex-subagent-core', 'phase-02-codex-image-binary', 'phase-03-per-level-provider-switch', 'phase-04-credential-and-chart-wiring'])
- **plan-01-chart-codex-provider-wiring** (dependsOn=[]): Add Helm chart surface for Codex provider: images.codexSubagent block, CODEX_SUBAGENT_IMAGE env var on manager Deployment, TIDE_DEFAULT_VENDOR_* env vars for per-level vendor defaults. Extend cmd/manager/env.go with resolvePerLevelVendors() and update tideHelmProviderDefaults() so vendor defaults reach ResolveProvider. helm template renders all new env vars cleanly; verify-import-firewall and verify-dispatch-imports green; make test exits 0.
- **plan-02-codex-secret-injection** (dependsOn=[]): Add CodexProviderSecretRef field to ProjectSpec (schema-additive, backwards-compatible) and wire it into internal/dispatch/podjob/jobspec.go as a second envFrom secretRef on dispatch Job pods, mirroring the existing ProviderSecretRef mount. Regenerate CRD manifest and deepcopy. Add jobspec_test.go assertion that CodexProviderSecretRef mounts as envFrom when set and is absent when unset. make manifests and make generate exit 0 with no uncommitted diff; make test exits 0.
- **plan-03-hetero-demo-and-integration-test** (dependsOn=['plan-01-chart-codex-provider-wiring', 'plan-02-codex-secret-injection']): Add examples/projects/hetero/project.yaml (reference Claude-planner plus Codex-executor Project using subagent.levels.task.vendor openai, subagent.levels.task.image ghcr.io/jsquirrelz/tide-codex-subagent, and codexProviderSecretRef). Update examples/projects/dogfood/02-codex-runtime-project.yaml to consume the new codexProviderSecretRef and per-level vendor fields. Add test/integration/kind/hetero_dispatch_test.go exercising mixed-provider waves: all real OpenAI API calls are guarded by OPENAI_API_KEY env set and testing.Short(); test reproduces D4 failure semantics (forced Codex task failure causes sibling to continue, dependents to never dispatch, non-dependents to dispatch in strict profile); asserts tide_cost_cents_total and cache-efficiency panel carry non-zero Codex spend under locked label set with task label absent. make test exits 0; metriccardinality analyzer green.

## milestone-03-hetero-integration  (dependsOn=['milestone-02-provider-dispatch'])

### phase-01-codex-pricing-usage  (dependsOn=[])
- **plan-01-codex-package-scaffold** (dependsOn=[]): Scaffold the internal/subagent/codex/ Go package: author doc.go with Apache 2.0 header, package-level doc referencing D-C1 layering and D-D2 Usage contract, and the vendorSentinel constant string openai. Establishes the compilable package declaration that all sibling plans depend on.
- **plan-02-codex-pricing-table** (dependsOn=['plan-01-codex-package-scaffold']): Author internal/subagent/codex/pricing.go: the modelPrice struct, the priceTable keyed on OpenAI/Codex model IDs (initial set: codex-1; cacheRead at the OpenAI discounted cached-input rate; no cacheWrite field because OpenAI caching is automatic and marker-free), the conservativeTier fallback pointing at the most-expensive known tier, and the estimatedCostCents method on the Codex receiver — exact structural mirror of internal/subagent/anthropic/pricing.go. CacheCreationTokens is never included in the cost sum: the OpenAI caching model has no cache-creation billing concept. Uses pkg/dispatch.PriceOverride for the per-instance override merge following the same clone-then-merge pattern as the Anthropic implementation to avoid T-14-02 package-var mutation.
- **plan-03-codex-usage-normalizer** (dependsOn=['plan-02-codex-pricing-table']): Author internal/subagent/codex/usage.go: the openaiUsage struct mirroring the OpenAI API usage response shape (prompt_tokens, completion_tokens, prompt_tokens_details.cached_tokens), and the normalizeUsage function that maps it into pkg/dispatch.Usage per the D5 contract: prompt_tokens to InputTokens, completion_tokens to OutputTokens, prompt_tokens_details.cached_tokens to CacheReadTokens, CacheCreationTokens fixed at 0 (invariant: OpenAI has no explicit cache-creation billing), EstimatedCostCents delegated to estimatedCostCents from pricing.go, and CacheSavingsCents computed as the savings from cache hits versus full input price for those tokens. This file is the sole import surface phase-02 needs from this phase.
- **plan-04-codex-pricing-tests** (dependsOn=['plan-02-codex-pricing-table']): Author internal/subagent/codex/pricing_test.go: exhaustive offline unit tests for the pricing table and estimatedCostCents covering per-model rate correctness for codex-1 (input, output, cacheRead), ceiling division on sub-cent amounts, unknown-model conservative fallback (non-zero, greater than or equal to any known-model cost), zero tokens returns zero cents, PricingOverrides merge (verify per-instance clone — package-level priceTable never mutated, T-14-02), CacheCreationTokens always 0 in cost sum (OpenAI cache-creation invariant), and conservative-tier identity (conservative tier equals the most-expensive known entry). No real OpenAI API calls; all offline. Mirrors the test structure and naming convention of internal/subagent/anthropic/pricing_test.go.
- **plan-05-codex-usage-tests** (dependsOn=['plan-03-codex-usage-normalizer']): Author internal/subagent/codex/usage_test.go: offline unit tests for normalizeUsage covering field mapping correctness (prompt_tokens to InputTokens, completion_tokens to OutputTokens, cached_tokens to CacheReadTokens), the CacheCreationTokens equals zero invariant for every input (including populated cached_tokens), CacheSavingsCents computation accuracy, zero usage produces zero-value pkg/dispatch.Usage, and EstimatedCostCents delegates to estimatedCostCents (non-zero for non-zero tokens). Confirm make test exits 0 with the full test suite passing offline as the plan's acceptance gate.

### phase-02-codex-subagent-core  (dependsOn=['phase-01-codex-pricing-usage'])
- **plan-01-codex-scaffold** (dependsOn=[]): Author the foundational scaffold for internal/subagent/codex/: doc.go naming the D-C1 layering pattern and the openai vendor sentinel, and client.go declaring the Client struct, Options, New() constructor, and NewWithExec() test-seam — mirroring internal/subagent/anthropic/ exactly. The execFunc field on Client is the indirection seam that lets tests inject a fake codex binary offline. No Run() method lives in this file. No OpenAI SDK imports introduced; the only provider dependency is the codex CLI binary resolved via PATH at runtime.
- **plan-02-codex-stream-parser** (dependsOn=[]): Author stream_parser.go and stream_parser_test.go for internal/subagent/codex/. ParseStream reads the Codex CLI --json JSONL event stream (one event per line), tees raw bytes to rawSink (the per-task events.jsonl audit log), and extracts the final message plus a pkg/dispatch.Usage struct per D5: usage.prompt_tokens to InputTokens, usage.completion_tokens to OutputTokens, usage.prompt_tokens_details.cached_tokens to CacheReadTokens, and CacheCreationTokens fixed at zero (OpenAI caching is automatic and marker-free — no cache-write billing concept exists). Uses common.ReadLines for the 16MB per-line budget and tolerates non-JSON lines silently per the anthropic pattern. Tests are fully offline: happy path, usage-mapping invariant (CacheCreationTokens == 0 always), tolerates-non-JSON, and no-result-event. No dependency on plan-01-codex-scaffold.
- **plan-03-codex-run** (dependsOn=['plan-01-codex-scaffold', 'plan-02-codex-stream-parser']): Author run.go and run_test.go for internal/subagent/codex/, completing the pkg/dispatch.Subagent implementation. run.go implements Subagent.Run(ctx, EnvelopeIn): (1) vendor fail-fast guard refusing any envelope whose Provider.Vendor != openai; (2) optional params allow-list for known Codex tuning keys; (3) in-pod prompt artifact read via readPromptArtifact when PromptPath is set; (4) events dir creation and D6 CLI invocation (codex exec --ephemeral --skip-git-repo-check --ignore-user-config --json --sandbox workspace-write --output-schema, with prompt via stdin); (5) OPENAI_API_KEY env wiring from in.SignedToken; (6) JSONL stream parsing via plan-02 ParseStream teed to events.jsonl; (7) Usage population via plan-01 estimatedCostCents from the phase-01 pricing table; (8) child-CRD file handoff for planner role via readChildCRDs (all traversal defenses, kind allowlist, double-object detection, sanitizeJSONStringControls — mirroring anthropic/subagent.go); (9) task-level vs dispatch-level error semantics per pkg/dispatch.Subagent godoc. run_test.go tests are fully offline using the execFunc seam from plan-01 Client and a fake codex binary (bash -c cat pattern): vendor mismatch, fixture JSONL stream end-to-end, child-CRD handoff, conservative-tier fallback for unknown model. Any test path that would invoke a real OpenAI endpoint is guarded by an explicit OPENAI_API_KEY env check and skipped when absent.

### phase-03-codex-image-entrypoint  (dependsOn=['phase-02-codex-subagent-core'])
- **plan-01-codex-entrypoint-shim** (dependsOn=[]): Author cmd/codex-subagent/main.go — the thin harness-wrapped entrypoint shim that loads EnvelopeIn, calls codex.Run, and writes EnvelopeOut — mirroring cmd/claude-subagent/main.go exactly. Include cmd/codex-subagent/main_test.go (six offline unit tests: HappyPath, EnvelopeLoadFailure, VendorMismatch, InvokesEnsureWorktreeBeforeRun, PassesEnvBranchToWorktree, IgnoresDevTestMode) and cmd/codex-subagent/commit_test.go (three executor-path tests: TestRunCommitsExecutorWorktree, TestRunEmptyDiffOverridesExitCode, TestRunPlannerSkipsCommit). Vendor sentinel is 'openai'. No Dev.TestMode branching. newSubagent is the injectable fake seam. All tests pass offline without OPENAI_API_KEY.
- **plan-02-codex-dockerfile** (dependsOn=['plan-01-codex-entrypoint-shim']): Author images/codex-subagent/Dockerfile — a multi-stage build that compiles the static cmd/codex-subagent binary in Stage 1 (golang:1.26-alpine, CGO_ENABLED=0) and produces a runtime image in Stage 2 (node:22-slim base with git installed, pinned @openai/codex npm CLI, Go binary copied, UID 1000, ENTRYPOINT /usr/local/bin/codex-subagent). Mirrors images/claude-subagent/Dockerfile structure exactly. Canonical image ref: ghcr.io/jsquirrelz/tide-codex-subagent. Add Makefile docker-build-codex-subagent and docker-push-codex-subagent targets using IMAGE_TAG convention, wired into the umbrella docker-build and docker-push goals. Dockerfile must build clean from repo root: docker build -t ghcr.io/jsquirrelz/tide-codex-subagent:test -f images/codex-subagent/Dockerfile .

### phase-04-per-level-vendor-switch  (dependsOn=[])
- **plan-01-vendor-field-schema** (dependsOn=[]): Add optional Vendor string field to LevelConfig and SubagentConfig in api/v1alpha1/project_types.go — +kubebuilder:validation:Enum=anthropic;openai, +optional, empty means inherit. Regenerate zz_generated.deepcopy.go and CRD YAML via make manifests generate. No controller logic; no test changes. Gate: make manifests generate exits 0 and CRD YAML carries vendor enum under spec.subagent.levels.*.vendor and spec.subagent.vendor.
- **plan-02-resolve-provider-rewire** (dependsOn=['plan-01-vendor-field-schema']): Replace the unconditional Vendor:anthropic in ResolveProvider (internal/controller/dispatch_helpers.go) with a full precedence chain: Levels.<level>.Vendor -> Spec.Subagent.Vendor -> helmDefaults.Vendor -> anthropic. Add Vendor string field to ProviderDefaults. Add TIDE_DEFAULT_VENDOR env-var support to cmd/manager/env.go (tideHelmProviderDefaults picks it up; default is anthropic for backward compat) and thread it into cmd/manager/main.go. Zero Codex imports in controller or cmd/manager. Gate: make build exits 0; verify-import-firewall and verify-dispatch-imports green.
- **plan-03-vendor-switch-tests** (dependsOn=['plan-02-resolve-provider-rewire']): Author unit tests proving the per-level vendor switch: (1) same Project with task-level Vendor:openai and phase-level unset resolves openai vs anthropic respectively; (2) Spec.Subagent.Vendor:openai propagates to un-overridden levels but is shadowed by a level-specific Vendor:anthropic; (3) Helm-default TIDE_DEFAULT_VENDOR env override reaches tideHelmProviderDefaults; (4) all existing TestResolveProvider* assertions still pass unmodified. Update dispatch_helpers_test.go and env_test.go. Gate: make test exits 0 with zero regressions.
- **plan-04-01-api-schema-vendor** (dependsOn=[]): 
- **plan-04-02-resolve-provider-rewire** (dependsOn=['plan-04-01-api-schema-vendor']): 
- **plan-04-03-vendor-resolution-tests** (dependsOn=['plan-04-02-resolve-provider-rewire']): 

### phase-05-chart-secret-wiring  (dependsOn=['phase-04-per-level-vendor-switch'])
- **plan-05-01-codex-image-helm-defaults** (dependsOn=[]): Add the Codex image default channel to the Helm chart and wire it into the controller image-resolution machinery. (1) In charts/tide/values.yaml add an images.codexSubagent block (repository ghcr.io/jsquirrelz/tide-codex-subagent, tag empty, pullPolicy IfNotPresent) mirroring the existing claudeSubagent entry. (2) In charts/tide/templates/deployment.yaml inject a CODEX_SUBAGENT_IMAGE env var into the manager Deployment using the same tag-or-appVersion logic used for CLAUDE_SUBAGENT_IMAGE. (3) In internal/controller/dispatch_helpers.go extend ProviderDefaults with a CodexImage string field and make resolveImage vendor-aware: when vendor is openai the Helm-default fallback is helmDefaults.CodexImage; when anthropic it stays helmDefaults.Image. (4) In cmd/manager/env.go extend tideHelmProviderDefaults to accept a codexImage parameter and populate ProviderDefaults.CodexImage from the CODEX_SUBAGENT_IMAGE env var. (5) Add tests: env_test.go proves CODEX_SUBAGENT_IMAGE round-trips into ProviderDefaults.CodexImage; dispatch_helpers_test.go and dispatch_image_test.go prove resolveImage is vendor-aware. make verify-import-firewall and make verify-dispatch-imports must remain green throughout.
- **plan-05-02-codex-provider-secret-ref** (dependsOn=[]): Add the CodexProviderSecretRef field to ProjectSpec in api/v1alpha1/project_types.go, mirroring the existing providerSecretRef field. The new field is a string carrying the same-namespace K8s Secret name that holds OPENAI_API_KEY or CODEX_API_KEY for dispatch Jobs running on the openai vendor. Add a +kubebuilder:validation:Optional marker and an inline doc comment referencing the D3 credential path decision (Secret-ref only, never the key bytes; no headless OAuth). Run make generate to regenerate api/v1alpha1/zz_generated.deepcopy.go. Confirm the api/v1alpha1 schema test (phase3_schema_test.go or equivalent) still passes. No controller, chart, or cmd/ changes in this plan.
- **plan-05-03-openai-secret-job-injection** (dependsOn=['plan-05-01-codex-image-helm-defaults', 'plan-05-02-codex-provider-secret-ref']): Extend all four Job-creating controller reconcilers to inject OPENAI_API_KEY from project.Spec.CodexProviderSecretRef when the dispatched level resolves to Vendor:openai. Concretely: in task_controller.go, milestone_controller.go, phase_controller.go, and plan_controller.go, after the call to ResolveProvider, if Vendor is openai and project.Spec.CodexProviderSecretRef is non-empty, append an envFrom secretRef entry pointing at the named Secret to the Job pod spec alongside (not replacing) the existing credproxy env path. This mirrors the providerSecretRef pattern: the controller validates the Secret exists (Get; NotFound is a soft error with a requeue), then stamps the Secret name into the Job pod spec as envFrom. Add unit tests covering: (a) a task-level Job with vendor=openai gets the openai envFrom entry; (b) a phase-level planner Job with vendor=anthropic does NOT get the openai envFrom entry; (c) a missing CodexProviderSecretRef when vendor=openai logs a warning and requeues rather than crashing. Also update examples/projects/dogfood/02-codex-runtime-project.yaml to add codexProviderSecretRef: openai-secrets. make test must exit 0 and make verify-import-firewall must stay green (no codex package imports anywhere in the controller).

### phase-06-hetero-integration  (dependsOn=['phase-02-codex-subagent-core', 'phase-03-codex-image-entrypoint', 'phase-04-per-level-vendor-switch', 'phase-05-chart-secret-wiring'])
- **plan-01-hetero-example-yaml** (dependsOn=[]): 
- **plan-02-mixed-provider-integration-test** (dependsOn=[]): 
- **plan-03-join-verify-and-fix** (dependsOn=['plan-01-hetero-example-yaml', 'plan-02-mixed-provider-integration-test']): 
