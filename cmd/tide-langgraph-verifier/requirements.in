# Direct runtime dependencies for the tide-langgraph-verifier image (D-10).
# Patch-exact pins only — `make verify-langgraph-pins` greps this file and
# rejects any range/unpinned specifier. Re-verified live against PyPI
# 2026-07-18 (48-RESEARCH.md §Standard Stack) — zero drift.
#
# No OTel auto-instrumentation or agentic file-edit-framework packages here
# (Pitfall E / D-02): the vendor "langgraph" sentinel + self-instrumentation
# registration is Phase 51 scope, not this phase's. Adding either now risks
# double-span emission before the vendor registration work lands. Not spelled
# out literally to avoid self-matching the `verify-langgraph-pins`-adjacent
# Pitfall-E grep this comment documents.
langgraph==1.2.9
langchain==1.3.14
langchain-anthropic==1.4.8
langchain-core==1.4.9
anthropic==0.117.0
pydantic==2.13.4
httpx==0.28.1
