apiVersion: v1
kind: ServiceAccount
metadata:
  name: tide-subagent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tide.labels" . | nindent 4 }}
automountServiceAccountToken: true
# No Role, no RoleBinding — subagent pods have zero K8s API verbs (D-A4 / T-02-12-04).
# This is a NEW file (Warning #9 fix) — Phase 1's serviceaccount.yaml is NOT modified.
