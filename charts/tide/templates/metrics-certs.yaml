apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "tide.fullname" . }}-metrics-certs
  labels:
  {{- include "tide.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "tide.fullname" . }}-SERVICE_NAME.SERVICE_NAMESPACE.svc'
  - '{{ include "tide.fullname" . }}-SERVICE_NAME.SERVICE_NAMESPACE.svc.{{ .Values.kubernetesClusterDomain
    }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "tide.fullname" . }}-selfsigned-issuer'
  secretName: metrics-server-cert