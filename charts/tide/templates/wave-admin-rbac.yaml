apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "tide.fullname" . }}-wave-admin-role
  labels:
  {{- include "tide.labels" . | nindent 4 }}
rules:
- apiGroups:
  - tideproject.k8s
  resources:
  - waves
  verbs:
  - '*'
- apiGroups:
  - tideproject.k8s
  resources:
  - waves/status
  verbs:
  - get