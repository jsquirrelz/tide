# tide_v1alpha3_task_alpha.yaml — α (Wave 1, no dependencies).
# Pinned to pkg/dag/kahn_test.go TestComputeWaves_AlphaThroughTheta fixture.
apiVersion: tideproject.k8s/v1alpha3
kind: Task
metadata:
  name: alpha
  namespace: tide-samples
  labels:
    app.kubernetes.io/name: tide
    app.kubernetes.io/managed-by: kustomize
    tideproject.k8s/sample: "true"
    tideproject.k8s/plan: sample-plan
spec:
  planRef: sample-plan
  filesTouched:
    - pkg/example/alpha.go
  declaredOutputPaths:
    - artifacts/alpha.json
